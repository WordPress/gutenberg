diff --git a/packages/design-system-mcp/src/test/parse-components.ts b/packages/design-system-mcp/src/test/parse-components.ts
index f66d5349bd2f0e..2011e2f8c0f74c 100644
--- a/packages/design-system-mcp/src/test/parse-components.ts
+++ b/packages/design-system-mcp/src/test/parse-components.ts
@@ -263,7 +263,7 @@ describe( 'parseComponents', () => {
 			'badge-intent': {
 				name: 'Badge',
 				description: 'A badge component.',
-				path: '../packages/ui/src/badge/stories/choosing-intent.story.tsx',
+				path: '../packages/ui/src/badge/stories/usage-guidelines.story.tsx',
 			},
 		} );
 
@@ -286,7 +286,7 @@ describe( 'parseComponents', () => {
 			'badge-intent': {
 				name: 'Badge',
 				description: 'A badge component.',
-				path: '../packages/ui/src/badge/stories/choosing-intent.story.tsx',
+				path: '../packages/ui/src/badge/stories/usage-guidelines.story.tsx',
 			},
 		} );
 
@@ -439,7 +439,7 @@ describe( 'parseComponentDetail', () => {
 			},
 			'badge-intent': {
 				name: 'Badge',
-				path: '../packages/ui/src/badge/stories/choosing-intent.story.tsx',
+				path: '../packages/ui/src/badge/stories/usage-guidelines.story.tsx',
 				stories: [
 					{
 						name: 'High',
@@ -472,7 +472,7 @@ describe( 'parseComponentDetail', () => {
 			'badge-intent': {
 				name: 'Badge',
 				description: 'A badge component.',
-				path: '../packages/ui/src/badge/stories/choosing-intent.story.tsx',
+				path: '../packages/ui/src/badge/stories/usage-guidelines.story.tsx',
 			},
 		} );
 
@@ -489,7 +489,7 @@ describe( 'parseComponentDetail', () => {
 			},
 			'badge-intent': {
 				name: 'Badge',
-				path: '../packages/ui/src/badge/stories/choosing-intent.story.tsx',
+				path: '../packages/ui/src/badge/stories/usage-guidelines.story.tsx',
 				reactComponentMeta: {
 					props: {
 						intent: {
diff --git a/packages/ui/src/badge/stories/choosing-intent.mdx b/packages/ui/src/badge/stories/choosing-intent.mdx
deleted file mode 100644
index 2e920951abc067..00000000000000
--- a/packages/ui/src/badge/stories/choosing-intent.mdx
+++ /dev/null
@@ -1,109 +0,0 @@
-import { Canvas, Meta } from '@storybook/addon-docs/blocks';
-import * as ChoosingIntentStories from './choosing-intent.story';
-
-<Meta of={ ChoosingIntentStories } />
-
-# Choosing intent
-
-<Canvas of={ ChoosingIntentStories.AllIntents } />
-
-It can be difficult to determine which badge intent to use because the component's properties are not tied to any specific product view. Those properties should be balanced against the requirements of the view in which the badge appears, all while keeping an eye on high-level consistency (global statuses that appear across multiple views).
-
-Here is a decision tree to help identify which badge to use.
-
-## 1. Ask first: should this draw the eye?
-
-If the user scans this screen, should their attention be drawn to this badge?
-
--   If **no** → use `none` (or even just plain text; no badge), even if the state is positive or "stable".
--   If **yes** → pick an intent based on how important the action or awareness is.
-
-## 2. High / Medium / Low = action priority
-
-Use when there's something for the user to act on.
-
-### `high` – Critical / top priority
-
-* Needs attention as soon as possible
-* _E.g. "Payment declined", "Security issue"_
-
-<Canvas of={ ChoosingIntentStories.High } />
-
-### `medium` – Important / blocks progress
-
-* Blocks a key task, should be handled soon
-* _E.g. "Approval required", "Review needed"_
-
-<Canvas of={ ChoosingIntentStories.Medium } />
-
-### `low` – Worth noticing / non‑urgent
-
-* Good to be aware of; action may be optional or later
-* _E.g. "Pending", "Queued", "Minor issues", "Optional setup"_
-
-<Canvas of={ ChoosingIntentStories.Low } />
-
-## 3. Informational / draft = special non-final states
-
-### `informational` – Notable, no action / fix needed
-
-* Context only; no clear action
-* _E.g. "Scheduled", "Beta", "Internal only"_
-
-<Canvas of={ ChoosingIntentStories.Informational } />
-
-### `draft` – Not final / work in progress
-
-* _E.g. "Draft", "Unpublished", "Work in progress"_
-
-<Canvas of={ ChoosingIntentStories.Draft } />
-
-## 4. Stable / none = normal states
-
-### `stable` – Positive / "healthy" state
-
-* Use when confirming success or "all good" is important in that view
-* _E.g. "Healthy", "Active", "Live"_
-
-<Canvas of={ ChoosingIntentStories.Stable } />
-
-### `none` – Default for normal / background states
-
-* Especially in dense lists where too much color creates visual noise
-* _E.g. "Inactive", "Expired"_
-
-<Canvas of={ ChoosingIntentStories.None } />
-
-## Examples
-
-### Comment status
-
-- "Approved" → `none`
-- "Approval required" → `medium`
-
-<Canvas of={ ChoosingIntentStories.CommentStatus } />
-
-### Page status
-
-- "Published" → `none`
-- "Pending" → `low`
-- "Draft" → `draft`
-- "Scheduled" → `informational`
-- "Private" → `informational`
-
-<Canvas of={ ChoosingIntentStories.PageStatus } />
-
-### Plugin status
-
-- "Active" → `stable`
-- "Inactive" → `none`
-
-<Canvas of={ ChoosingIntentStories.PluginStatus } />
-
-## 5. When in doubt…
-
-Use the least attention‑grabbing intent that still:
-
--   Makes it clear what needs attention,
--   Marks what isn't final, or
--   Confirms a key success state in that context.
diff --git a/packages/ui/src/badge/stories/usage-guidelines.mdx b/packages/ui/src/badge/stories/usage-guidelines.mdx
new file mode 100644
index 00000000000000..6a841f1c07d2e8
--- /dev/null
+++ b/packages/ui/src/badge/stories/usage-guidelines.mdx
@@ -0,0 +1,129 @@
+import { Canvas, Meta } from '@storybook/addon-docs/blocks';
+import * as UsageGuidelinesStories from './usage-guidelines.story';
+
+<Meta of={ UsageGuidelinesStories } />
+
+# Badge usage guidelines
+
+## Choosing intent
+
+<Canvas of={ UsageGuidelinesStories.AllIntents } />
+
+It can be difficult to determine which badge intent to use because the component's properties are not tied to any specific product view. Those properties should be balanced against the requirements of the view in which the badge appears, all while keeping an eye on high-level consistency (global statuses that appear across multiple views).
+
+Here is a decision tree to help identify which badge to use.
+
+### 1. Ask first: should this draw the eye?
+
+If the user scans this screen, should their attention be drawn to this badge?
+
+-   If **no** → use `none` (or even just plain text; no badge), even if the state is positive or "stable".
+-   If **yes** → pick an intent based on how important the action or awareness is.
+
+### 2. High / Medium / Low = action priority
+
+Use when there's something for the user to act on.
+
+#### `high` – Critical / top priority
+
+-   Needs attention as soon as possible
+-   _E.g. "Payment declined", "Security issue"_
+
+<Canvas of={ UsageGuidelinesStories.High } />
+
+#### `medium` – Important / blocks progress
+
+-   Blocks a key task, should be handled soon
+-   _E.g. "Approval required", "Review needed"_
+
+<Canvas of={ UsageGuidelinesStories.Medium } />
+
+#### `low` – Worth noticing / non‑urgent
+
+-   Good to be aware of; action may be optional or later
+-   _E.g. "Pending", "Queued", "Minor issues", "Optional setup"_
+
+<Canvas of={ UsageGuidelinesStories.Low } />
+
+### 3. Informational / draft = special non-final states
+
+#### `informational` – Notable, no action / fix needed
+
+-   Context only; no clear action
+-   _E.g. "Scheduled", "Beta", "Internal only"_
+
+<Canvas of={ UsageGuidelinesStories.Informational } />
+
+#### `draft` – Not final / work in progress
+
+-   _E.g. "Draft", "Unpublished", "Work in progress"_
+
+<Canvas of={ UsageGuidelinesStories.Draft } />
+
+### 4. Stable / none = normal states
+
+#### `stable` – Positive / "healthy" state
+
+-   Use when confirming success or "all good" is important in that view
+-   _E.g. "Healthy", "Active", "Live"_
+
+<Canvas of={ UsageGuidelinesStories.Stable } />
+
+#### `none` – Default for normal / background states
+
+-   Especially in dense lists where too much color creates visual noise
+-   _E.g. "Inactive", "Expired"_
+
+<Canvas of={ UsageGuidelinesStories.None } />
+
+### Examples
+
+#### Comment status
+
+-   "Approved" → `none`
+-   "Approval required" → `medium`
+
+<Canvas of={ UsageGuidelinesStories.CommentStatus } />
+
+#### Page status
+
+-   "Published" → `none`
+-   "Pending" → `low`
+-   "Draft" → `draft`
+-   "Scheduled" → `informational`
+-   "Private" → `informational`
+
+<Canvas of={ UsageGuidelinesStories.PageStatus } />
+
+#### Plugin status
+
+-   "Active" → `stable`
+-   "Inactive" → `none`
+
+<Canvas of={ UsageGuidelinesStories.PluginStatus } />
+
+### 5. When in doubt…
+
+Use the least attention‑grabbing intent that still:
+
+-   Makes it clear what needs attention,
+-   Marks what isn't final, or
+-   Confirms a key success state in that context.
+
+## Using with icons
+
+`Badge` is text-only. Intent is communicated through color and label — not through icons inside the badge.
+
+-   **Don't put icons inside badges.** A status icon duplicates what the intent color and label already convey.
+
+<Canvas of={ UsageGuidelinesStories.IncorrectBadgeWithIcon } />
+
+-   **Prefer text-only badges.** In most cases, the intent color and label are enough. Adding a status icon inside the badge duplicates information and adds visual noise, especially in dense lists.
+
+<Canvas of={ UsageGuidelinesStories.TextOnlyBadges } />
+
+-   **Keep nearby icons separate.** When a row already includes a content icon (e.g. post type, plugin), use that icon for identity and let the badge communicate status on its own.
+
+<Canvas of={ UsageGuidelinesStories.WithAdjacentContentIcon } />
+
+-   **Use `Notice` for icon-led messages.** If the UI needs a prominent status icon alongside a message, use [`Notice`](?path=/docs/design-system-components-notice--docs) instead of decorating a badge.
diff --git a/packages/ui/src/badge/stories/choosing-intent.story.tsx b/packages/ui/src/badge/stories/usage-guidelines.story.tsx
similarity index 59%
rename from packages/ui/src/badge/stories/choosing-intent.story.tsx
rename to packages/ui/src/badge/stories/usage-guidelines.story.tsx
index 9a54db2e5e9611..f42c30070eb880 100644
--- a/packages/ui/src/badge/stories/choosing-intent.story.tsx
+++ b/packages/ui/src/badge/stories/usage-guidelines.story.tsx
@@ -1,9 +1,12 @@
 import type { Meta, StoryObj } from '@storybook/react-vite';
+import { caution, error, page, plugins, published } from '@wordpress/icons';
 import { Badge } from '../index';
+import { Icon } from '../../icon';
 import { Stack } from '../../stack';
+import { Text } from '../../text';
 
 const meta: Meta< typeof Badge > = {
-	title: 'Design System/Components/Badge/Choosing intent',
+	title: 'Design System/Components/Badge/Usage Guidelines',
 	component: Badge,
 	decorators: [
 		( Story ) => (
@@ -127,3 +130,58 @@ export const PluginStatus: Story = {
 		</>
 	),
 };
+
+export const TextOnlyBadges: Story = {
+	render: () => (
+		<>
+			<Badge intent="stable">Active</Badge>
+			<Badge intent="none">Inactive</Badge>
+			<Badge intent="draft">Draft</Badge>
+		</>
+	),
+};
+
+export const WithAdjacentContentIcon: Story = {
+	render: () => (
+		<Stack direction="column" gap="sm">
+			<Stack direction="row" gap="sm" align="center">
+				<Icon icon={ page } size={ 24 } />
+				<Text variant="body-md">About page</Text>
+				<Badge intent="none">Published</Badge>
+			</Stack>
+			<Stack direction="row" gap="sm" align="center">
+				<Icon icon={ plugins } size={ 24 } />
+				<Text variant="body-md">Akismet</Text>
+				<Badge intent="stable">Active</Badge>
+			</Stack>
+		</Stack>
+	),
+};
+
+export const IncorrectBadgeWithIcon: Story = {
+	render: () => (
+		<>
+			{ /* @ts-expect-error Demonstrating incorrect Badge usage with icon children. */ }
+			<Badge intent="stable">
+				<Stack direction="row" gap="xs" align="center">
+					<Icon icon={ published } size={ 16 } />
+					Active
+				</Stack>
+			</Badge>
+			{ /* @ts-expect-error Demonstrating incorrect Badge usage with icon children. */ }
+			<Badge intent="medium">
+				<Stack direction="row" gap="xs" align="center">
+					<Icon icon={ caution } size={ 16 } />
+					Review needed
+				</Stack>
+			</Badge>
+			{ /* @ts-expect-error Demonstrating incorrect Badge usage with icon children. */ }
+			<Badge intent="high">
+				<Stack direction="row" gap="xs" align="center">
+					<Icon icon={ error } size={ 16 } />
+					Payment declined
+				</Stack>
+			</Badge>
+		</>
+	),
+};
