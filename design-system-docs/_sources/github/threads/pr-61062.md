---
number: 61062
type: pull_request
state: MERGED
merged_at: 2024-05-29T18:48:53Z
url: https://github.com/WordPress/gutenberg/pull/61062
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #61062: Components: Make the `ProgressBar` public

- URL: https://github.com/WordPress/gutenberg/pull/61062
- Author: fullofcaffeine
- Created: 2024-04-25T00:39:36Z
- Updated: 2024-05-29T19:12:24Z
- Comments: 18 of 18
- Review threads: 45 of 45
- Changed files: 13 of 13
- Diff: ../pr-diffs/61062.diff

## Body

Issue: https://github.com/WordPress/gutenberg/issues/59418.

## What?

This is part of a [larger effort](https://github.com/WordPress/gutenberg/issues/59418) to remove `__experimental` prefix from all "experimental" components, effectively promoting them to regular stable components. See the related issue for more context.

## Why?

The strategy of prefixing exports with `__experimental` has become deprecated after the introduction of private APIs. 

## How?

1. Export it from components without the `__experimental` prefix;
1. Keep the old `__experimental` export for backwards compatibility;
1. Change all imports of the old `__experimental` in GB and components to the one without the prefix (including in storybook stories). Also, update the docs to refer to the new unprefixed component;
1. Add the component storybook `id` (get it from the storybook URL) to the `PREVIOUSLY_EXPERIMENTAL_COMPONENTS` const array in `manager-head.html` so that old experimental story paths are redirected to the new one;
1. Add a changelog for the change.
1. Make the component public if needed, and update consumers to import it directly instead of using the private API.


- [x] Add changelog

## Issue comments

### mirka on 2024-05-07T13:11:20Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2098373670

The only thing I'm concerned about is the hardcoded width. If it's ok with @WordPress/gutenberg-design, I'd like to remove the width constraints so consumers can more easily set their own width using a wrapper div or something. In general, we are designing our components (especially input controls) to have an unconstrained width by default. However, I do understand that a progress bar may have different concerns, so let us know if this would be a problem.

### jasmussen on 2024-05-07T14:23:37Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2098532169

Can there be a default width, and a prop to override it? I think of this similar to the spinner, where it's designed to be a particular size for consistency, but where there's [a size prop](https://wordpress.github.io/gutenberg/?path=/docs/components-spinner--docs).

### fullofcaffeine on 2024-05-10T02:33:34Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2103751900

@mirka Are you referring to the `INDETERMINATE_TRACK_WIDTH` currently set to `50`? I don't know much about this component's API, but I can try adding the changes suggested by Joen. Let me know what you think would be the best path forward here and I'm happy to help! Thanks!

### tyxla on 2024-05-10T10:47:32Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2104390388

> @mirka Are you referring to the `INDETERMINATE_TRACK_WIDTH` currently set to `50`? I don't know much about this component's API, but I can try adding the changes suggested by Joen. Let me know what you think would be the best path forward here and I'm happy to help! Thanks!

If I understand correctly, @mirka referees not the progress bar track width, but rather to the entire progress bar component width (which is currently set as 160px through `max-width`):

https://github.com/WordPress/gutenberg/blob/2d33a9ed2e91efacedcec877f149061b1195c11d/packages/components/src/progress-bar/styles.ts#L28

> Can there be a default width, and a prop to override it? I think of this similar to the spinner, where it's designed to be a particular size for consistency, but where there's [a size prop](https://wordpress.github.io/gutenberg/?path=/docs/components-spinner--docs).

I'd be fine with a width prop, and then keeping the `160px` there as a reasonable default. We might want to think about how to make it fluid though and occupy the entire available width, in case that's what the 3rd party dev expects.

### github-actions on 2024-05-21T18:47:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2123240970

<!-- flaky-tests-report-comment -->
**Flaky tests detected in b096ca40543d6cd7a75150c984788e89a5c34ea9.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/9230871977
📝  Reported issues:
- #61701 in `/test/e2e/specs/site-editor/dataviews-list-layout-keyboard.spec.js`

### fullofcaffeine on 2024-05-22T00:21:35Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2123631556

I folks 👋🏻 - I added an optional `width` prop, a `number` that gets interpolated as a string in the style and is assumed to use the `px` unit in the CSS property:

<video src="https://github.com/WordPress/gutenberg/assets/81248/059ec5c0-a75d-4541-93b3-620ec4f9d857" controls="controls" style="max-width: 730px;"></video>

I also added a new story to illustrate a progress bar with a custom width, but please let me know if the default story suffices.

### github-actions on 2024-05-22T00:31:16Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2123645210

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: fullofcaffeine <fullofcaffeine@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
Co-authored-by: jasmussen <joen@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-05-22T00:40:19Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2123667934

**Size Change:** -89 B (-0.01%) 

**Total Size:** 1.74 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/components/index.min.js` | 222 kB | -61 B (-0.03%) |
| `build/edit-site/index.min.js` | 210 kB | -28 B (-0.01%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 955 B |
| `build/annotations/index.min.js` | 2.27 kB |
| `build/api-fetch/index.min.js` | 2.32 kB |
| `build/autop/index.min.js` | 2.1 kB |
| `build/blob/index.min.js` | 578 B |
| `build/block-directory/index.min.js` | 7.29 kB |
| `build/block-directory/style-rtl.css` | 1.03 kB |
| `build/block-directory/style.css` | 1.03 kB |
| `build/block-editor/content-rtl.css` | 4.58 kB |
| `build/block-editor/content.css` | 4.57 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 395 B |
| `build/block-editor/default-editor-styles.css` | 395 B |
| `build/block-editor/index.min.js` | 261 kB |
| `build/block-editor/style-rtl.css` | 15.6 kB |
| `build/block-editor/style.css` | 15.6 kB |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 126 B |
| `build/block-library/blocks/audio/theme.css` | 126 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 104 B |
| `build/block-library/blocks/avatar/style.css` | 104 B |
| `build/block-library/blocks/button/editor-rtl.css` | 307 B |
| `build/block-library/blocks/button/editor.css` | 307 B |
| `build/block-library/blocks/button/style-rtl.css` | 539 B |
| `build/block-library/blocks/button/style.css` | 539 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 113 B |
| `build/block-library/blocks/categories/editor.css` | 112 B |
| `build/block-library/blocks/categories/style-rtl.css` | 124 B |
| `build/block-library/blocks/categories/style.css` | 124 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 421 B |
| `build/block-library/blocks/columns/style.css` | 421 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 667 B |
| `build/block-library/blocks/cover/editor.css` | 670 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.62 kB |
| `build/block-library/blocks/cover/style.css` | 1.61 kB |
| `build/block-library/blocks/details/editor-rtl.css` | 65 B |
| `build/block-library/blocks/details/editor.css` | 65 B |
| `build/block-library/blocks/details/style-rtl.css` | 86 B |
| `build/block-library/blocks/details/style.css` | 86 B |
| `build/block-library/blocks/embed/editor-rtl.css` | 312 B |
| `build/block-library/blocks/embed/editor.css` | 312 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 126 B |
| `build/block-library/blocks/embed/theme.css` | 126 B |
| `build/block-library/blocks/file/editor-rtl.css` | 326 B |
| `build/block-library/blocks/file/editor.css` | 327 B |
| `build/block-library/blocks/file/style-rtl.css` | 280 B |
| `build/block-library/blocks/file/style.css` | 281 B |
| `build/block-library/blocks/file/view.min.js` | 324 B |
| `build/block-library/blocks/footnotes/style-rtl.css` | 201 B |
| `build/block-library/blocks/footnotes/style.css` | 199 B |
| `build/block-library/blocks/form-input/editor-rtl.css` | 227 B |
| `build/block-library/blocks/form-input/editor.css` | 227 B |
| `build/block-library/blocks/form-input/style-rtl.css` | 343 B |
| `build/block-library/blocks/form-input/style.css` | 343 B |
| `build/block-library/blocks/form-submission-notification/editor-rtl.css` | 340 B |
| `build/block-library/blocks/form-submission-notification/editor.css` | 340 B |
| `build/block-library/blocks/form-submit-button/style-rtl.css` | 69 B |
| `build/block-library/blocks/form-submit-button/style.css` | 69 B |
| `build/block-library/blocks/form/view.min.js` | 471 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.61 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.61 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 962 B |
| `build/block-library/blocks/gallery/editor.css` | 965 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.72 kB |
| `build/block-library/blocks/gallery/style.css` | 1.72 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 108 B |
| `build/block-library/blocks/gallery/theme.css` | 108 B |
| `build/block-library/blocks/group/editor-rtl.css` | 403 B |
| `build/block-library/blocks/group/editor.css` | 403 B |
| `build/block-library/blocks/group/style-rtl.css` | 103 B |
| `build/block-library/blocks/group/style.css` | 103 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 189 B |
| `build/block-library/blocks/heading/style.css` | 189 B |
| `build/block-library/blocks/html/editor-rtl.css` | 346 B |
| `build/block-library/blocks/html/editor.css` | 347 B |
| `build/block-library/blocks/image/editor-rtl.css` | 891 B |
| `build/block-library/blocks/image/editor.css` | 891 B |
| `build/block-library/blocks/image/style-rtl.css` | 1.52 kB |
| `build/block-library/blocks/image/style.css` | 1.52 kB |
| `build/block-library/blocks/image/theme-rtl.css` | 137 B |
| `build/block-library/blocks/image/theme.css` | 137 B |
| `build/block-library/blocks/image/view.min.js` | 1.54 kB |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 357 B |
| `build/block-library/blocks/latest-comments/style.css` | 357 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 205 B |
| `build/block-library/blocks/latest-posts/editor.css` | 205 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 512 B |
| `build/block-library/blocks/latest-posts/style.css` | 512 B |
| `build/block-library/blocks/list/style-rtl.css` | 102 B |
| `build/block-library/blocks/list/style.css` | 102 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 306 B |
| `build/block-library/blocks/media-text/editor.css` | 305 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 505 B |
| `build/block-library/blocks/media-text/style.css` | 503 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 668 B |
| `build/block-library/blocks/navigation-link/editor.css` | 669 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 193 B |
| `build/block-library/blocks/navigation-link/style.css` | 192 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 296 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 295 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.26 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.26 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.26 kB |
| `build/block-library/blocks/navigation/style.css` | 2.25 kB |
| `build/block-library/blocks/navigation/view.min.js` | 1.03 kB |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 377 B |
| `build/block-library/blocks/page-list/editor.css` | 377 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 235 B |
| `build/block-library/blocks/paragraph/editor.css` | 235 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 341 B |
| `build/block-library/blocks/paragraph/style.css` | 341 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 508 B |
| `build/block-library/blocks/post-comments-form/style.css` | 508 B |
| `build/block-library/blocks/post-content/editor-rtl.css` | 74 B |
| `build/block-library/blocks/post-content/editor.css` | 74 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 71 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 71 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 141 B |
| `build/block-library/blocks/post-excerpt/style.css` | 141 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 734 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 732 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 342 B |
| `build/block-library/blocks/post-featured-image/style.css` | 342 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 215 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 214 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 397 B |
| `build/block-library/blocks/post-template/style.css` | 396 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-time-to-read/style-rtl.css` | 69 B |
| `build/block-library/blocks/post-time-to-read/style.css` | 69 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 125 B |
| `build/block-library/blocks/preformatted/style.css` | 125 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 344 B |
| `build/block-library/blocks/pullquote/style.css` | 343 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 168 B |
| `build/block-library/blocks/pullquote/theme.css` | 168 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 288 B |
| `build/block-library/blocks/query-pagination/style.css` | 284 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 486 B |
| `build/block-library/blocks/query/editor.css` | 486 B |
| `build/block-library/blocks/query/view.min.js` | 958 B |
| `build/block-library/blocks/quote/style-rtl.css` | 237 B |
| `build/block-library/blocks/quote/style.css` | 237 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 140 B |
| `build/block-library/blocks/read-more/style.css` | 140 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 101 B |
| `build/block-library/blocks/rss/editor.css` | 101 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 184 B |
| `build/block-library/blocks/search/editor.css` | 184 B |
| `build/block-library/blocks/search/style-rtl.css` | 690 B |
| `build/block-library/blocks/search/style.css` | 689 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/search/view.min.js` | 478 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 99 B |
| `build/block-library/blocks/separator/editor.css` | 99 B |
| `build/block-library/blocks/separator/style-rtl.css` | 248 B |
| `build/block-library/blocks/separator/style.css` | 248 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 286 B |
| `build/block-library/blocks/shortcode/editor.css` | 286 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 805 B |
| `build/block-library/blocks/site-logo/editor.css` | 805 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 218 B |
| `build/block-library/blocks/site-logo/style.css` | 218 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 124 B |
| `build/block-library/blocks/site-title/editor.css` | 124 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 70 B |
| `build/block-library/blocks/site-title/style.css` | 70 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 335 B |
| `build/block-library/blocks/social-link/editor.css` | 335 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 683 B |
| `build/block-library/blocks/social-links/editor.css` | 681 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.51 kB |
| `build/block-library/blocks/social-links/style.css` | 1.51 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 350 B |
| `build/block-library/blocks/spacer/editor.css` | 350 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 395 B |
| `build/block-library/blocks/table/editor.css` | 395 B |
| `build/block-library/blocks/table/style-rtl.css` | 639 B |
| `build/block-library/blocks/table/style.css` | 639 B |
| `build/block-library/blocks/table/theme-rtl.css` | 146 B |
| `build/block-library/blocks/table/theme.css` | 146 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 265 B |
| `build/block-library/blocks/tag-cloud/style.css` | 266 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 393 B |
| `build/block-library/blocks/template-part/editor.css` | 393 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 112 B |
| `build/block-library/blocks/template-part/theme.css` | 112 B |
| `build/block-library/blocks/term-description/style-rtl.css` | 111 B |
| `build/block-library/blocks/term-description/style.css` | 111 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 185 B |
| `build/block-library/blocks/video/style.css` | 185 B |
| `build/block-library/blocks/video/theme-rtl.css` | 126 B |
| `build/block-library/blocks/video/theme.css` | 126 B |
| `build/block-library/classic-rtl.css` | 179 B |
| `build/block-library/classic.css` | 179 B |
| `build/block-library/common-rtl.css` | 1.11 kB |
| `build/block-library/common.css` | 1.11 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 12 kB |
| `build/block-library/editor.css` | 12 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/index.min.js` | 218 kB |
| `build/block-library/reset-rtl.css` | 472 B |
| `build/block-library/reset.css` | 472 B |
| `build/block-library/style-rtl.css` | 14.6 kB |
| `build/block-library/style.css` | 14.6 kB |
| `build/block-library/theme-rtl.css` | 703 B |
| `build/block-library/theme.css` | 706 B |
| `build/block-serialization-default-parser/index.min.js` | 1.12 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.87 kB |
| `build/blocks/index.min.js` | 51.8 kB |
| `build/commands/index.min.js` | 15.2 kB |
| `build/commands/style-rtl.css` | 953 B |
| `build/commands/style.css` | 951 B |
| `build/components/style-rtl.css` | 12 kB |
| `build/components/style.css` | 12 kB |
| `build/compose/index.min.js` | 12.8 kB |
| `build/core-commands/index.min.js` | 2.71 kB |
| `build/core-data/index.min.js` | 72.5 kB |
| `build/customize-widgets/index.min.js` | 10.9 kB |
| `build/customize-widgets/style-rtl.css` | 1.36 kB |
| `build/customize-widgets/style.css` | 1.36 kB |
| `build/data-controls/index.min.js` | 640 B |
| `build/data/index.min.js` | 9.01 kB |
| `build/date/index.min.js` | 17.9 kB |
| `build/deprecated/index.min.js` | 451 B |
| `build/dom-ready/index.min.js` | 324 B |
| `build/dom/index.min.js` | 4.65 kB |
| `build/edit-post/classic-rtl.css` | 578 B |
| `build/edit-post/classic.css` | 578 B |
| `build/edit-post/index.min.js` | 13.4 kB |
| `build/edit-post/style-rtl.css` | 2.35 kB |
| `build/edit-post/style.css` | 2.35 kB |
| `build/edit-site/style-rtl.css` | 12 kB |
| `build/edit-site/style.css` | 12 kB |
| `build/edit-widgets/index.min.js` | 17.5 kB |
| `build/edit-widgets/style-rtl.css` | 4.21 kB |
| `build/edit-widgets/style.css` | 4.21 kB |
| `build/editor/index.min.js` | 93.1 kB |
| `build/editor/style-rtl.css` | 8.86 kB |
| `build/editor/style.css` | 8.87 kB |
| `build/element/index.min.js` | 4.83 kB |
| `build/escape-html/index.min.js` | 537 B |
| `build/format-library/index.min.js` | 8.09 kB |
| `build/format-library/style-rtl.css` | 493 B |
| `build/format-library/style.css` | 492 B |
| `build/hooks/index.min.js` | 1.55 kB |
| `build/html-entities/index.min.js` | 448 B |
| `build/i18n/index.min.js` | 3.58 kB |
| `build/interactivity/debug.min.js` | 16.5 kB |
| `build/interactivity/file.min.js` | 447 B |
| `build/interactivity/image.min.js` | 1.67 kB |
| `build/interactivity/index.min.js` | 13.3 kB |
| `build/interactivity/navigation.min.js` | 1.17 kB |
| `build/interactivity/query.min.js` | 740 B |
| `build/interactivity/router.min.js` | 2.81 kB |
| `build/interactivity/search.min.js` | 618 B |
| `build/is-shallow-equal/index.min.js` | 527 B |
| `build/keyboard-shortcuts/index.min.js` | 1.31 kB |
| `build/keycodes/index.min.js` | 1.46 kB |
| `build/list-reusable-blocks/index.min.js` | 2.14 kB |
| `build/list-reusable-blocks/style-rtl.css` | 851 B |
| `build/list-reusable-blocks/style.css` | 851 B |
| `build/media-utils/index.min.js` | 2.92 kB |
| `build/modules/importmap-polyfill.min.js` | 12.2 kB |
| `build/notices/index.min.js` | 948 B |
| `build/nux/index.min.js` | 1.58 kB |
| `build/nux/style-rtl.css` | 748 B |
| `build/nux/style.css` | 744 B |
| `build/patterns/index.min.js` | 6.51 kB |
| `build/patterns/style-rtl.css` | 595 B |
| `build/patterns/style.css` | 595 B |
| `build/plugins/index.min.js` | 1.81 kB |
| `build/preferences-persistence/index.min.js` | 2.06 kB |
| `build/preferences/index.min.js` | 2.9 kB |
| `build/preferences/style-rtl.css` | 719 B |
| `build/preferences/style.css` | 721 B |
| `build/primitives/index.min.js` | 831 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/private-apis/index.min.js` | 1 kB |
| `build/react-i18n/index.min.js` | 629 B |
| `build/react-refresh-entry/index.min.js` | 9.47 kB |
| `build/react-refresh-runtime/index.min.js` | 6.78 kB |
| `build/redux-routine/index.min.js` | 2.7 kB |
| `build/reusable-blocks/index.min.js` | 2.72 kB |
| `build/reusable-blocks/style-rtl.css` | 256 B |
| `build/reusable-blocks/style.css` | 256 B |
| `build/rich-text/index.min.js` | 10.1 kB |
| `build/router/index.min.js` | 1.96 kB |
| `build/server-side-render/index.min.js` | 1.97 kB |
| `build/shortcode/index.min.js` | 1.39 kB |
| `build/style-engine/index.min.js` | 2.02 kB |
| `build/token-list/index.min.js` | 582 B |
| `build/url/index.min.js` | 3.74 kB |
| `build/vendors/react-dom.min.js` | 42.8 kB |
| `build/vendors/react-jsx-runtime.min.js` | 554 B |
| `build/vendors/react.min.js` | 2.65 kB |
| `build/viewport/index.min.js` | 964 B |
| `build/warning/index.min.js` | 249 B |
| `build/widgets/index.min.js` | 7.13 kB |
| `build/widgets/style-rtl.css` | 1.17 kB |
| `build/widgets/style.css` | 1.17 kB |
| `build/wordcount/index.min.js` | 1.02 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### mirka on 2024-05-22T13:31:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2124809518

Apologies for being late to the party, but I'm not sure how I feel about a `width` prop. It would be more straightforward for consumers to control that with a wrapper div or their own CSS class.

I'm also unsure about this 160px default we're setting, because we don't have a ton of in-app usage yet and it might be too early to be prescriptive about a default. It really must depend on the context it's used in, and we don't really have much context yet.

I wonder if we can start out by suggesting a default width via documentation?

### fullofcaffeine on 2024-05-22T17:10:28Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2125354187

> Apologies for being late to the party, but I'm not sure how I feel about a width prop. It would be more straightforward for consumers to control that with a wrapper div or their own CSS class.

Yeah, I can see a few problems with it, the biggest one being that it's currently tied to `px` units. 

> I wonder if we can start out by suggesting a default width via documentation?

Do you mean removing the default `width` and keeping `max-width: 100%` and documenting that the consumer should wrap it in a container div and set a default width (so that the component will expand to take it and allow the user to effectively control the width this way)?

Thanks for taking the time to review this!

### fullofcaffeine on 2024-05-22T23:39:22Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2125948665

> Apologies for being late to the party, but I'm not sure how I feel about a width prop. It would be more straightforward for consumers to control that with a wrapper div or their own CSS class.

@mirka I simplified the implementation as you suggested - it now relies on the immediate container `width` for its width:



<video src="https://github.com/WordPress/gutenberg/assets/81248/9ff3202f-5e90-42b3-b01f-fdb47573bce0" controls="controls" style="max-width: 730px;"></video>


I updated the docs and added a story depicting changing the parent element width to see how it affects the actual progress bar size. I'm not sure that's the right place for a "parent width" control (considering it's not a prop of the actual component), but I think it works nicely to show/document it and allow the user to see it in practice. Let me know if I got you right and what you think about it.

Thanks!

### fullofcaffeine on 2024-05-23T15:40:58Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2127451789

> Have you had a chance to test the existing <ProgressBar /> use cases?

Thanks for pointing that out! I'll fix that and look at other usages.

### fullofcaffeine on 2024-05-24T17:10:20Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2130015327

I'm going to split this into two PRs:
1. I'll keep working on the width behavior changes in a separate PR. The main reason is that this PR already has a branch name that ties it with the stabilization work. 
2. This PR will have the changes to remove the experimental designation + making the ProgressBar public.

### fullofcaffeine on 2024-05-25T00:19:26Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2130548637

> I'll keep working on the width behavior changes in a separate PR. 

See: https://github.com/WordPress/gutenberg/pull/61976.

### fullofcaffeine on 2024-05-27T17:04:01Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2133839520

It occurred to me that we should probably only merge this after we decide what to do with the default width/override behavior (https://github.com/WordPress/gutenberg/pull/61976). If so, both PRs should be merged at around the same time, in sequence. The reasoning is that if we merge this first and we take longer to decide/merge the other (and a GB release happens in the meantime), there's a (small) risk more consumers will start using it relying on the default width behavior, which might change when we merge subsequent PR.

### jasmussen on 2024-05-28T06:28:39Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2134438092

In case it helps the conversation, the progressbar was designed to always be a specific width. The fact that it can be shorter or longer than that was not actually a consideration. So a good default would be useful, even if we add flexibility on top of that.

### fullofcaffeine on 2024-05-29T17:57:15Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2137969676

There's a Playwright test [failing](https://github.com/WordPress/gutenberg/actions/runs/9290331396/job/25567476240?pr=61062), but it's [also failing](https://github.com/WordPress/gutenberg/actions/runs/9289883244/job/25564822251) on `trunk`, so not related to this changeset.

### fullofcaffeine on 2024-05-29T18:51:51Z

URL: https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2138059259

I forgot to paste the contributor text to the commit description; sorry! The PR had a lengthy list of discussions/messages and missed the [warning](https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2123645210) at the top. Will take private note to not forget it the next time.

## Review threads

### Review thread 1 (resolved)

#### fullofcaffeine on 2024-05-22T00:32:44Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609096438
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,8 @@
 
 ## Unreleased
 
+- `ProgressBar`: Remove "experimental" designation for `ProgressBar`, add optional custom `width` prop ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

Should we keep the "remove" experimental designation in the changelog or just list the `width` change? 🤔

#### tyxla on 2024-05-22T09:29:50Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609621414
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,8 @@
 
 ## Unreleased
 
+- `ProgressBar`: Remove "experimental" designation for `ProgressBar`, add optional custom `width` prop ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

Using a comma in the CHANGELOG is a good indication that we should have split the PR into 2 or more PRs. Have you considered moving the `width` feature (or the experimental stabilization) to a separate PR?

#### fullofcaffeine on 2024-05-23T03:38:02Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610922616
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,8 @@
 
 ## Unreleased
 
+- `ProgressBar`: Remove "experimental" designation for `ProgressBar`, add optional custom `width` prop ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

In this case, stabilizing/removing the experimental designation for this component is a very small change related to the storybook category, so I think we can add the API improvement as part of this PR, too. Also, by looking at other similar PRs, I can see that we shouldn't add the "remove experimental designation/stabilize" in the changelog, as it's considered an internal change. I'm happy to split, though. 

One question I have for @mirka is if we should also start exporting this component as part of this changeset to allow external users to start consuming it. I guess we should?

#### mirka on 2024-05-23T20:45:22Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612282628
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,8 @@
 
 ## Unreleased
 
+- `ProgressBar`: Remove "experimental" designation for `ProgressBar`, add optional custom `width` prop ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

> we should also start exporting this component as part of this changeset to allow external users to start consuming it

Oh, you're right. We need to remove the private API locking and export the component publicly.

FWIW I do agree with @tyxla that the width changes and the de-experimentalization are ideally separate PRs, if it isn't too much work.

### Review thread 2 (resolved)

#### tyxla on 2024-05-22T09:34:22Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609627968
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -10,9 +10,10 @@ import { ProgressBar } from '..';
 
 const meta: Meta< typeof ProgressBar > = {
 	component: ProgressBar,
-	title: 'Components (Experimental)/ProgressBar',
+	title: 'Components/ProgressBar',
 	argTypes: {
 		value: { control: { type: 'number', min: 0, max: 100, step: 1 } },
+		width: { control: { type: 'number', min: 0, step: 10 } },
```

A `step` of `10` for `width` when we might want to test more precise values feels off.

### Review thread 3 (resolved)

#### tyxla on 2024-05-22T09:34:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609628988
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -10,9 +10,10 @@ import { ProgressBar } from '..';
 
 const meta: Meta< typeof ProgressBar > = {
 	component: ProgressBar,
-	title: 'Components (Experimental)/ProgressBar',
+	title: 'Components/ProgressBar',
 	argTypes: {
 		value: { control: { type: 'number', min: 0, max: 100, step: 1 } },
+		width: { control: { type: 'number', min: 0, step: 10 } },
```

Also, should we set a `max` still? Storybook limits the container to 1000px or something, so perhaps we should have a default max width in the story.

### Review thread 4 (resolved)

#### tyxla on 2024-05-22T09:38:51Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609634423
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -30,3 +31,9 @@ const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+// The ProgressBar width can be overriden (in pixels) and it will expand to that size if
+// the parent has enough horizontal space. It is interpolated into the `width` property
```

I guess you mean `prop`?

```suggestion
// the parent has enough horizontal space. It is interpolated into the `width` prop
```

#### fullofcaffeine on 2024-05-22T17:05:59Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610360775
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -30,3 +31,9 @@ const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+// The ProgressBar width can be overriden (in pixels) and it will expand to that size if
+// the parent has enough horizontal space. It is interpolated into the `width` property
```

It's the same but unabbreviated, but I get your point on leveraging React's vocab 👍🏻

### Review thread 5 (resolved)

#### tyxla on 2024-05-22T09:41:40Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609638529
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -20,11 +20,11 @@ function UnforwardedProgressBar(
 	props: WordPressComponentProps< ProgressBarProps, 'progress', false >,
 	ref: ForwardedRef< HTMLProgressElement >
 ) {
-	const { className, value, ...progressProps } = props;
+	const { className, value, width, ...progressProps } = props;
```

I have a doubt with the prop naming. The `width` name sets the expectation that the progress bar will have a certain CSS `width`, which isn't the case. Imagine you provide `width: 400`, then if the container is 300px, the progress bar will be smaller than that. In other words, the API (`width` doesn't necessarily correspond to what happens under the hood (`max-width`). Should we rename the prop to `max-width` to make this behavior clearer?

#### fullofcaffeine on 2024-05-22T17:01:27Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610350844
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -20,11 +20,11 @@ function UnforwardedProgressBar(
 	props: WordPressComponentProps< ProgressBarProps, 'progress', false >,
 	ref: ForwardedRef< HTMLProgressElement >
 ) {
-	const { className, value, ...progressProps } = props;
+	const { className, value, width, ...progressProps } = props;
```

Yeah, I thought about that. But in the end, it does set the width, just that it will fit the container. I can rename it to `max-width` if it makes it more clear/explicit, np 👍🏻

### Review thread 6

#### tyxla on 2024-05-22T09:43:05Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609640374
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -30,3 +31,9 @@ const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+// The ProgressBar width can be overriden (in pixels) and it will expand to that size if
```

Is this the wrong place to document this? If the `width` prop is properly documented, it shouldn't be necessary to add a comment in this story IMHO.

#### fullofcaffeine on 2024-05-22T17:06:41Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610361508
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -30,3 +31,9 @@ const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+// The ProgressBar width can be overriden (in pixels) and it will expand to that size if
```

Yeah, fair point.

### Review thread 7

#### tyxla on 2024-05-22T09:44:07Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609641773
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -20,11 +20,11 @@ function UnforwardedProgressBar(
 	props: WordPressComponentProps< ProgressBarProps, 'progress', false >,
 	ref: ForwardedRef< HTMLProgressElement >
 ) {
-	const { className, value, ...progressProps } = props;
+	const { className, value, width, ...progressProps } = props;
```

This prop also needs to be added to the component README.

#### fullofcaffeine on 2024-05-22T17:06:52Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610361716
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -20,11 +20,11 @@ function UnforwardedProgressBar(
 	props: WordPressComponentProps< ProgressBarProps, 'progress', false >,
 	ref: ForwardedRef< HTMLProgressElement >
 ) {
-	const { className, value, ...progressProps } = props;
+	const { className, value, width, ...progressProps } = props;
```

Ah true, thanks for pointing that out!

### Review thread 8

#### tyxla on 2024-05-22T09:46:36Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609645099
Path: packages/components/src/progress-bar/types.ts

```diff
@@ -4,6 +4,12 @@ export type ProgressBarProps = {
 	 */
 	value?: number;
 
+	/**
+	 * An optional width of the progress bar (in pixels) for the progress bar
```

The type docs may be yet another place to clarify that it's actually a max width.

### Review thread 9

#### tyxla on 2024-05-22T09:49:47Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609649401
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -21,11 +21,12 @@ const animateProgressBar = keyframes( {
 // Width of the indicator for the indeterminate progress bar
 export const INDETERMINATE_TRACK_WIDTH = 50;
 
-export const Track = styled.div`
+export const Track = styled.div< { width?: number } >`
 	position: relative;
 	overflow: hidden;
 	width: 100%;
-	max-width: 160px;
+	max-width: ${ ( { width } ) =>
+		width ? width : CONFIG.progressBarWidth }px;
```

It may be limiting for some use cases that we're forcing the use of pixel units. If we build in the unit in the prop (and the config value), we can allow 3rd party developers to provide their unit of choice.

#### fullofcaffeine on 2024-05-22T17:07:43Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610362670
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -21,11 +21,12 @@ const animateProgressBar = keyframes( {
 // Width of the indicator for the indeterminate progress bar
 export const INDETERMINATE_TRACK_WIDTH = 50;
 
-export const Track = styled.div`
+export const Track = styled.div< { width?: number } >`
 	position: relative;
 	overflow: hidden;
 	width: 100%;
-	max-width: 160px;
+	max-width: ${ ( { width } ) =>
+		width ? width : CONFIG.progressBarWidth }px;
```

I followed the same pattern as the one used in the spinner component, but I agree. It might not be a good API after all.

### Review thread 10 (resolved)

#### tyxla on 2024-05-22T09:54:20Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609656562
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -79,4 +80,27 @@ describe( 'ProgressBar', () => {
 		);
 		expect( screen.getByRole( 'progressbar' ) ).toHaveStyle( style );
 	} );
+
+	it( 'should use default width if width for the track if the `width` prop is not passed', () => {
+		const { container } = render( <ProgressBar /> );
+
+		// eslint-disable-next-line testing-library/no-container, testing-library/no-node-access
```

Like in the pre-existing tests, it may be worth keeping the explanatory comment as to why we're accessing the `track` with inaccessible selectors.

#### fullofcaffeine on 2024-05-22T17:01:35Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610351222
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -79,4 +80,27 @@ describe( 'ProgressBar', () => {
 		);
 		expect( screen.getByRole( 'progressbar' ) ).toHaveStyle( style );
 	} );
+
+	it( 'should use default width if width for the track if the `width` prop is not passed', () => {
+		const { container } = render( <ProgressBar /> );
+
+		// eslint-disable-next-line testing-library/no-container, testing-library/no-node-access
```

Sure 👍🏻

### Review thread 11 (resolved)

#### tyxla on 2024-05-22T09:58:13Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609662242
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -79,4 +80,27 @@ describe( 'ProgressBar', () => {
 		);
 		expect( screen.getByRole( 'progressbar' ) ).toHaveStyle( style );
 	} );
+
+	it( 'should use default width if width for the track if the `width` prop is not passed', () => {
```

```suggestion
	it( 'should use default width if the `width` prop is not passed', () => {
```

#### fullofcaffeine on 2024-05-22T17:02:02Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610352552
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -79,4 +80,27 @@ describe( 'ProgressBar', () => {
 		);
 		expect( screen.getByRole( 'progressbar' ) ).toHaveStyle( style );
 	} );
+
+	it( 'should use default width if width for the track if the `width` prop is not passed', () => {
```

Shouldn't we be specific about what element we're setting the width for in the test? There's also the width for the inner bar.

### Review thread 12 (resolved)

#### tyxla on 2024-05-22T09:58:27Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609662534
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -79,4 +80,27 @@ describe( 'ProgressBar', () => {
 		);
 		expect( screen.getByRole( 'progressbar' ) ).toHaveStyle( style );
 	} );
+
+	it( 'should use default width if width for the track if the `width` prop is not passed', () => {
+		const { container } = render( <ProgressBar /> );
+
+		// eslint-disable-next-line testing-library/no-container, testing-library/no-node-access
+		const track = container.firstChild;
+
+		expect( track ).toHaveStyle( {
+			'max-width': `${ CONFIG.progressBarWidth }px`,
+		} );
+	} );
+
+	it( 'should apply custom width to tthe track if the `width` prop is passed', () => {
```

```suggestion
	it( 'should apply custom width to the track if the `width` prop is passed', () => {
```

#### fullofcaffeine on 2024-05-22T17:02:10Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610352864
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -79,4 +80,27 @@ describe( 'ProgressBar', () => {
 		);
 		expect( screen.getByRole( 'progressbar' ) ).toHaveStyle( style );
 	} );
+
+	it( 'should use default width if width for the track if the `width` prop is not passed', () => {
+		const { container } = render( <ProgressBar /> );
+
+		// eslint-disable-next-line testing-library/no-container, testing-library/no-node-access
+		const track = container.firstChild;
+
+		expect( track ).toHaveStyle( {
+			'max-width': `${ CONFIG.progressBarWidth }px`,
+		} );
+	} );
+
+	it( 'should apply custom width to tthe track if the `width` prop is passed', () => {
```

Ops, my bad.

### Review thread 13 (resolved)

#### tyxla on 2024-05-22T10:00:18Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609664887
Path: packages/components/src/utils/config-values.js

```diff
@@ -47,6 +47,7 @@ export default Object.assign( {}, CONTROL_PROPS, TOGGLE_GROUP_CONTROL_PROPS, {
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
 	spinnerSize: 16,
+	progressBarWidth: 160,
```

Two potential confusion points with naming here:

- Above, I mentioned the one that might be confusing because of `width` vs `max-width`. This also applies here
- We don't really use this for width, but rather for `default` width. That being said, this config value will be overridden by the prop too, so it might be useful to make it clear that it's a default (max) width.

#### fullofcaffeine on 2024-05-22T17:03:01Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610354959
Path: packages/components/src/utils/config-values.js

```diff
@@ -47,6 +47,7 @@ export default Object.assign( {}, CONTROL_PROPS, TOGGLE_GROUP_CONTROL_PROPS, {
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
 	spinnerSize: 16,
+	progressBarWidth: 160,
```

> Above, I mentioned the one that might be confusing because of width vs max-width. This also applies here
👍🏻 

> We don't really use this for width, but rather for default width. That being said, this config value will be overridden by the prop too, so it might be useful to make it clear that it's a default (max) width.

Cool, I'll rename it accordingly.

### Review thread 14 (resolved)

#### tyxla on 2024-05-22T10:02:35Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1609668061
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -21,11 +21,12 @@ const animateProgressBar = keyframes( {
 // Width of the indicator for the indeterminate progress bar
 export const INDETERMINATE_TRACK_WIDTH = 50;
 
-export const Track = styled.div`
+export const Track = styled.div< { width?: number } >`
```

Does it make sense to move this to its own `TrackProps` type and use those in the `ProgressBarProps` type as well? It could potentially help make an easier distinction of which types belong where.

#### fullofcaffeine on 2024-05-22T17:04:02Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1610356553
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -21,11 +21,12 @@ const animateProgressBar = keyframes( {
 // Width of the indicator for the indeterminate progress bar
 export const INDETERMINATE_TRACK_WIDTH = 50;
 
-export const Track = styled.div`
+export const Track = styled.div< { width?: number } >`
```

I can do it, but would it be worth it to create a whole new type for a single prop type? I think this simple and explicit.

### Review thread 15 (resolved)

#### mirka on 2024-05-23T14:15:54Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1611782731
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,27 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
```

Looks good! I noticed this component doesn't have a JSDoc description yet. Let's use this opportunity to add one ([example](https://github.com/WordPress/gutenberg/blob/811261494f035bd94c25cee4ae19e2159c147b72/packages/components/src/divider/component.tsx#L31-L52)).

#### fullofcaffeine on 2024-05-23T17:37:58Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612085074
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,27 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
```

Ah nice, thanks for pointing that out 👍🏻

#### fullofcaffeine on 2024-05-23T18:33:05Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612148254
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,27 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
```

Improved the docs in https://github.com/WordPress/gutenberg/pull/61062/commits/a5338e2f038c47f54463b8ee4e4742a46edf15cf and added the jsdoc as part of it.

### Review thread 16 (resolved)

#### mirka on 2024-05-23T14:16:45Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1611784096
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,27 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Render a ProgressBar.
```

```suggestion
```

Maybe we can remove this line, because we already have a one-line description in the first section ("A simple horizontal progress bar component").

### Review thread 17 (resolved)

#### mirka on 2024-05-23T17:17:00Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612058963
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
```

> I'm not sure that's the right place for a "parent width" control (considering it's not a prop of the actual component), but I think it works nicely to show/document it and allow the user to see it in practice.

Your concerns are spot on — in our Storybook, we've decided to have our controls maintain a strict 1:1 relationship with the actual props, for clarity and consistency. So we don't want a control that's not an actual prop.

But I do think there should be a story like this to show the suggested default width (160px).

#### fullofcaffeine on 2024-05-23T18:01:13Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612111452
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
```

OK, if the goal is to just show a default width, I'll simplify the story to just use a custom `className` that sets it to 160px. We can then improve the docs to show the different ways of setting the width.

#### fullofcaffeine on 2024-05-23T18:35:48Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612150947
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
```

Done in https://github.com/WordPress/gutenberg/pull/61062/commits/5e03b5254ea2dbfcfce6e37a943fde492c2af7c5. It now looks like this:

<img width="1493" alt="Screenshot 2024-05-23 at 12 35 39 p m" src="https://github.com/WordPress/gutenberg/assets/81248/063f00e5-9874-49f7-a094-6a3e15f1cb9a">

### Review thread 18

#### mirka on 2024-05-23T17:20:36Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612063348
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
+	parentWidth,
+	...args
+} ) => (
+	<div style={ { width: parentWidth } }>
+		<ProgressBar { ...args } />
+	</div>
+);
+
+ParentWidth.args = {
+	parentWidth: '300px', // Default width
+};
+
+ParentWidth.argTypes = {
+	parentWidth: { control: { type: 'text' }, name: 'parent width' },
+};
+
+ParentWidth.storyName = 'With Specific Width';
```

Nit: The conventions we've been using here are to just make the export const name the story name (especially when you don't need any special characters in the name). Also the story description can be defined as a JSDoc.

Example:

https://github.com/WordPress/gutenberg/blob/5fbeee2b829d98e327d9a7444ccf94f99280b964/packages/components/src/base-control/stories/index.story.tsx#L50-L57

#### fullofcaffeine on 2024-05-23T17:58:54Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612108783
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
+	parentWidth,
+	...args
+} ) => (
+	<div style={ { width: parentWidth } }>
+		<ProgressBar { ...args } />
+	</div>
+);
+
+ParentWidth.args = {
+	parentWidth: '300px', // Default width
+};
+
+ParentWidth.argTypes = {
+	parentWidth: { control: { type: 'text' }, name: 'parent width' },
+};
+
+ParentWidth.storyName = 'With Specific Width';
```

Thanks for clarifying that 👍🏻

#### fullofcaffeine on 2024-05-23T18:36:23Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612151524
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
+	parentWidth,
+	...args
+} ) => (
+	<div style={ { width: parentWidth } }>
+		<ProgressBar { ...args } />
+	</div>
+);
+
+ParentWidth.args = {
+	parentWidth: '300px', // Default width
+};
+
+ParentWidth.argTypes = {
+	parentWidth: { control: { type: 'text' }, name: 'parent width' },
+};
+
+ParentWidth.storyName = 'With Specific Width';
```

Done in https://github.com/WordPress/gutenberg/pull/61062/commits/5e03b5254ea2dbfcfce6e37a943fde492c2af7c5.

### Review thread 19 (resolved)

#### mirka on 2024-05-23T17:27:26Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612070490
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
+	parentWidth,
+	...args
+} ) => (
+	<div style={ { width: parentWidth } }>
+		<ProgressBar { ...args } />
+	</div>
+);
+
+ParentWidth.args = {
+	parentWidth: '300px', // Default width
+};
+
+ParentWidth.argTypes = {
+	parentWidth: { control: { type: 'text' }, name: 'parent width' },
+};
+
+ParentWidth.storyName = 'With Specific Width';
+ParentWidth.parameters = {
+	controls: { sort: 'none' },
+	docs: {
+		description: {
+			story: 'This story demonstrates how the ProgressBar takes the width of its parent container. You can edit the parent\'s width using the "Parent element width" control.',
+		},
```

Most of our components have unconstrained widths anyway, so it isn't that important to demonstrate _how_ to set a custom width here. Instead, I'm thinking we should highlight that we have a **suggested width**, mostly for Gutenberg app devs.

Maybe a description like "For most screens with a wide enough viewport, we currently suggest a max width of 160px"?

#### fullofcaffeine on 2024-05-23T17:40:10Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612088008
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
+	parentWidth,
+	...args
+} ) => (
+	<div style={ { width: parentWidth } }>
+		<ProgressBar { ...args } />
+	</div>
+);
+
+ParentWidth.args = {
+	parentWidth: '300px', // Default width
+};
+
+ParentWidth.argTypes = {
+	parentWidth: { control: { type: 'text' }, name: 'parent width' },
+};
+
+ParentWidth.storyName = 'With Specific Width';
+ParentWidth.parameters = {
+	controls: { sort: 'none' },
+	docs: {
+		description: {
+			story: 'This story demonstrates how the ProgressBar takes the width of its parent container. You can edit the parent\'s width using the "Parent element width" control.',
+		},
```

Cool, makes sense!

#### fullofcaffeine on 2024-05-23T18:44:21Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612159989
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,36 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const ParentWidth: StoryFn< { parentWidth: string } > = ( {
+	parentWidth,
+	...args
+} ) => (
+	<div style={ { width: parentWidth } }>
+		<ProgressBar { ...args } />
+	</div>
+);
+
+ParentWidth.args = {
+	parentWidth: '300px', // Default width
+};
+
+ParentWidth.argTypes = {
+	parentWidth: { control: { type: 'text' }, name: 'parent width' },
+};
+
+ParentWidth.storyName = 'With Specific Width';
+ParentWidth.parameters = {
+	controls: { sort: 'none' },
+	docs: {
+		description: {
+			story: 'This story demonstrates how the ProgressBar takes the width of its parent container. You can edit the parent\'s width using the "Parent element width" control.',
+		},
```

Done in https://github.com/WordPress/gutenberg/pull/61062/commits/5f3567e637f7208a160d48a1fd17d99289c95da3.

### Review thread 20 (resolved)

#### fullofcaffeine on 2024-05-23T17:27:47Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612070880
Path: packages/edit-site/src/components/canvas-loader/index.js

```diff
@@ -34,7 +34,9 @@ export default function CanvasLoader( { id } ) {
 	return (
 		<div className="edit-site-canvas-loader">
 			<Theme accent={ indicatorColor } background={ backgroundColor }>
-				<ProgressBar id={ id } max={ total } value={ elapsed } />
+				<div style={ { width: '160px' } }>
```

I don't like having to forcedly add these wrapper divs in the current consumers, I can't avoid thinking a self-contained default would be better here, but I'm looking forward to reading your thoughts. However, it does fix the issue [here](https://github.com/WordPress/gutenberg/pull/61062#pullrequestreview-2073018536) by making it look exactly like the current version on `trunk`.

#### mirka on 2024-05-23T17:33:44Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612078201
Path: packages/edit-site/src/components/canvas-loader/index.js

```diff
@@ -34,7 +34,9 @@ export default function CanvasLoader( { id } ) {
 	return (
 		<div className="edit-site-canvas-loader">
 			<Theme accent={ indicatorColor } background={ backgroundColor }>
-				<ProgressBar id={ id } max={ total } value={ elapsed } />
+				<div style={ { width: '160px' } }>
```

Doesn't necessarily have to be a wrapper div. Another standard way would be to pass your own class name:

```
.my-progress-bar {
  width: 160px;
}

<ProgressBar className="my-progress-bar" />
```

In either case I would prefer a CSS class rather than an inline style, since there is no reason for this to be inline.

#### fullofcaffeine on 2024-05-23T17:36:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612083566
Path: packages/edit-site/src/components/canvas-loader/index.js

```diff
@@ -34,7 +34,9 @@ export default function CanvasLoader( { id } ) {
 	return (
 		<div className="edit-site-canvas-loader">
 			<Theme accent={ indicatorColor } background={ backgroundColor }>
-				<ProgressBar id={ id } max={ total } value={ elapsed } />
+				<div style={ { width: '160px' } }>
```

Cool, will move it over to a CSS class there 👍🏻

#### fullofcaffeine on 2024-05-23T18:31:21Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612146225
Path: packages/edit-site/src/components/canvas-loader/index.js

```diff
@@ -34,7 +34,9 @@ export default function CanvasLoader( { id } ) {
 	return (
 		<div className="edit-site-canvas-loader">
 			<Theme accent={ indicatorColor } background={ backgroundColor }>
-				<ProgressBar id={ id } max={ total } value={ elapsed } />
+				<div style={ { width: '160px' } }>
```

Done in https://github.com/WordPress/gutenberg/pull/61062/commits/bc95336241c9240c4820d61703ac9052f652d896.

### Review thread 21 (resolved)

#### mirka on 2024-05-23T20:50:02Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612287232
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,35 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const WithDefaultSuggestedWidth: StoryFn = ( {
+	className,
+	...args
+} ) => (
+	<>
+		<style>
+			{ `
+				.progressbar-story-custom-width {
+					width: 160px;
+				}
+			` }
+		</style>
+		<ProgressBar { ...args } className={ className } />
+	</>
+);
+WithDefaultSuggestedWidth.args = {
+	className: 'progressbar-story-custom-width',
+};
+WithDefaultSuggestedWidth.parameters = {
+	docs: {
+		description: {
+			story: 'For most screens with a wide-enough viewport, we recommend a maximum width of 160px. You can set a custom width by passing a CSS class via the `className` prop (depicted below) or by setting the width of the parent container.',
```

We can write this description as a doc comment on the story export ([like this](https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612063348)), instead of in this `parameters.docs.description.story`. It's quite convenient!

#### fullofcaffeine on 2024-05-24T00:07:56Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612493334
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,35 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const WithDefaultSuggestedWidth: StoryFn = ( {
+	className,
+	...args
+} ) => (
+	<>
+		<style>
+			{ `
+				.progressbar-story-custom-width {
+					width: 160px;
+				}
+			` }
+		</style>
+		<ProgressBar { ...args } className={ className } />
+	</>
+);
+WithDefaultSuggestedWidth.args = {
+	className: 'progressbar-story-custom-width',
+};
+WithDefaultSuggestedWidth.parameters = {
+	docs: {
+		description: {
+			story: 'For most screens with a wide-enough viewport, we recommend a maximum width of 160px. You can set a custom width by passing a CSS class via the `className` prop (depicted below) or by setting the width of the parent container.',
```

Oh ok! I'll add it as a jsdoc there then 👍🏻

### Review thread 22 (resolved)

#### mirka on 2024-05-23T21:00:09Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612299923
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
+ *
+ * Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
+ *
+ * ## Usage
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = () => {
+ *   return <ProgressBar />
+ * }
+ * ```
+ *
+ * The ProgressBar will expand to take all the available horizontal space of its immediate parent container element. To control its width, you can:
+ *
+ * Pass a custom CSS `className` that takes care of setting the `width`:
+ *
+ * ```css
+ * .my-css-class {
+ *   width: 160px;
+ * }
+ * ```
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = () => {
+ *   return <ProgressBar className="my-css-class" />;
+ * };
+ * ```
+ *
+ * Wrap it in a container element (e.g `<div>`) that has a `width` specified:
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = ( props ) => {
+ *   return (
+ *     <div style={ { width: '160px' } }>
+ *       <ProgressBar />
+ *     </div>
+ *   );
+ * };
+ * ```
```

No hard rules, but in general we've been putting about this much in the component export JSDocs. A brief description, and a snippet that shows 1.) how to import it, and 2.) a basic usage example.

The main consumption points for this JSDoc data are IDE IntelliSense, and the Storybook Docs view (shown right below the default story).

```diff
 * A simple horizontal progress bar component.
 *
 * Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 *
-  * ## Usage
-  *
 * ```jsx
 * import { ProgressBar } from '@wordpress/components';
 *
 * const MyLoadingComponent = () => {
 *   return <ProgressBar />
 * }
 * ```
-  *
-  * The ProgressBar will expand to take all the available horizontal space of its immediate parent container element. To control its width, you can:
-  *
-  * Pass a custom CSS `className` that takes care of setting the `width`:
-  *
-  * ```css
-  * .my-css-class {
-  *   width: 160px;
-  * }
-  * ```
-  *
-  * ```jsx
-  * import { ProgressBar } from '@wordpress/components';
-  *
-  * const MyLoadingComponent = () => {
-  *   return <ProgressBar className="my-css-class" />;
-  * };
-  * ```
-  *
-  * Wrap it in a container element (e.g `<div>`) that has a `width` specified:
-  *
-  * ```jsx
-  * import { ProgressBar } from '@wordpress/components';
-  *
-  * const MyLoadingComponent = ( props ) => {
-  *   return (
-  *     <div style={ { width: '160px' } }>
-  *       <ProgressBar />
-  *     </div>
-  *   );
-  * };
-  * ```
```

#### fullofcaffeine on 2024-05-24T00:07:07Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1612492980
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
+ *
+ * Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
+ *
+ * ## Usage
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = () => {
+ *   return <ProgressBar />
+ * }
+ * ```
+ *
+ * The ProgressBar will expand to take all the available horizontal space of its immediate parent container element. To control its width, you can:
+ *
+ * Pass a custom CSS `className` that takes care of setting the `width`:
+ *
+ * ```css
+ * .my-css-class {
+ *   width: 160px;
+ * }
+ * ```
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = () => {
+ *   return <ProgressBar className="my-css-class" />;
+ * };
+ * ```
+ *
+ * Wrap it in a container element (e.g `<div>`) that has a `width` specified:
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = ( props ) => {
+ *   return (
+ *     <div style={ { width: '160px' } }>
+ *       <ProgressBar />
+ *     </div>
+ *   );
+ * };
+ * ```
```

Ah cool, thanks for clarifying that, that makes sense 👍🏻

### Review thread 23 (resolved)

#### tyxla on 2024-05-24T09:23:42Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613167841
Path: packages/edit-site/src/components/canvas-loader/style.scss

```diff
@@ -16,6 +16,10 @@
 	& > div {
```

What is this style used for? Perhaps for the progress bar? IF not, couldn't we reuse it for the progress bar?

#### fullofcaffeine on 2024-05-24T17:12:15Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613790333
Path: packages/edit-site/src/components/canvas-loader/style.scss

```diff
@@ -16,6 +16,10 @@
 	& > div {
```

Oh, you're right, this was probably already there to set the width to the bar (though it was redundant), totally missed it 🤦🏻 

I just discussed this with Lena, and we agreed to roll back to a default behavior of having a default `max-width` + a prop that makes the width "unconstrained." Will work on it in a separate PR.

### Review thread 24 (resolved)

#### tyxla on 2024-05-24T09:25:04Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613169796
Path: packages/edit-site/src/components/global-styles/font-library-modal/style.scss

```diff
@@ -48,6 +48,10 @@ $footer-height: 70px;
 		// Push down so that the progress bar is centered vertically.
 		// It should be 120px (72px modal header height + 48px tab height)
 		padding-top: $header-height + $grid-unit-15 + $grid-unit-60;
+
+		& > .font-library-modal__progressbar {
+			width: 160px;
```

What I don't like about this approach is that we have to declare it everywhere. Theoretically, if we have 20 places that use a progress bar, we need this style up to 20 times in 20 different locations. This doesn't seem sustainable and feels repetitive - can it be avoided?

#### fullofcaffeine on 2024-05-24T16:17:17Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613725251
Path: packages/edit-site/src/components/global-styles/font-library-modal/style.scss

```diff
@@ -48,6 +48,10 @@ $footer-height: 70px;
 		// Push down so that the progress bar is centered vertically.
 		// It should be 120px (72px modal header height + 48px tab height)
 		padding-top: $header-height + $grid-unit-15 + $grid-unit-60;
+
+		& > .font-library-modal__progressbar {
+			width: 160px;
```

Yes, I can understand the concern here. Maybe we could keep the default `width` of `160px` and allow the user to override with a CSS class? @mirka what do you think?

### Review thread 25 (resolved)

#### tyxla on 2024-05-24T09:26:20Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613171344
Path: packages/edit-site/src/components/canvas-loader/style.scss

```diff
@@ -16,6 +16,10 @@
 	& > div {
 		width: 160px;
 	}
+
+	& > .edit-site-canvas-loader__progressbar {
+		width: 160px;
```

Is there a good reason to prefer `width` to `max-width`?

#### fullofcaffeine on 2024-05-24T16:20:46Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613729630
Path: packages/edit-site/src/components/canvas-loader/style.scss

```diff
@@ -16,6 +16,10 @@
 	& > div {
 		width: 160px;
 	}
+
+	& > .edit-site-canvas-loader__progressbar {
+		width: 160px;
```

Actually no, we should indeed probably use `max-width` so that it adapts to smaller viewports. Thanks for catching that 👍🏻

### Review thread 26 (resolved)

#### tyxla on 2024-05-24T09:36:56Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613186008
Path: packages/edit-site/src/components/canvas-loader/style.scss

```diff
@@ -16,6 +16,10 @@
 	& > div {
 		width: 160px;
 	}
+
+	& > .edit-site-canvas-loader__progressbar {
+		width: 160px;
```

I still think the work to remove the width still needs too be in a separate PR.

#### fullofcaffeine on 2024-05-24T16:13:07Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613719470
Path: packages/edit-site/src/components/canvas-loader/style.scss

```diff
@@ -16,6 +16,10 @@
 	& > div {
 		width: 160px;
 	}
+
+	& > .edit-site-canvas-loader__progressbar {
+		width: 160px;
```

If the work to remove the width is considered to be separate from the stabilization work, then yes. I was under the impression it was part of it as per Lena's comment [here](https://github.com/WordPress/gutenberg/pull/61062#issuecomment-2098373670). Happy to split as well 👍🏻

### Review thread 27 (resolved)

#### tyxla on 2024-05-24T09:42:03Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613192435
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,35 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const WithDefaultSuggestedWidth: StoryFn = ( {
+	className,
+	...args
+} ) => (
+	<>
+		<style>
+			{ `
+				.progressbar-story-custom-width {
+					width: 160px;
+				}
+			` }
```

I must admit this is a very odd way to add a style. We either add it inline, or in a separate file, but this feels odd and it's [not really supported before React 19](https://react.dev/reference/react-dom/components/style) (we're still on v18.3.1).

#### mirka on 2024-05-24T10:17:33Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613240920
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,35 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const WithDefaultSuggestedWidth: StoryFn = ( {
+	className,
+	...args
+} ) => (
+	<>
+		<style>
+			{ `
+				.progressbar-story-custom-width {
+					width: 160px;
+				}
+			` }
```

Fair. In Storybook snippets we tend to use inline for simplicity, even though it's not always the best way in production.

I don't have a strong opinion about this. We don't necessarily have to document "the correct way to set a width", since that kind of thing hasn't really been a source of confusion. I'm fine as long as we showcase the suggested width.

#### tyxla on 2024-05-24T12:07:29Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613376395
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,35 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const WithDefaultSuggestedWidth: StoryFn = ( {
+	className,
+	...args
+} ) => (
+	<>
+		<style>
+			{ `
+				.progressbar-story-custom-width {
+					width: 160px;
+				}
+			` }
```

Less advanced folks tend to use those examples in 3rd party extension code, so if we suggest it in the docs, it increases the chance for them to think it's the best way to do things, and it will end up in their codebase.

#### fullofcaffeine on 2024-05-24T16:24:23Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613733732
Path: packages/components/src/progress-bar/stories/index.story.tsx

```diff
@@ -24,9 +24,35 @@ const meta: Meta< typeof ProgressBar > = {
 };
 export default meta;
 
-const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => {
-	return <ProgressBar { ...args } />;
-};
+const Template: StoryFn< typeof ProgressBar > = ( { ...args } ) => (
+	<ProgressBar { ...args } />
+);
 
 export const Default: StoryFn< typeof ProgressBar > = Template.bind( {} );
 Default.args = {};
+
+export const WithDefaultSuggestedWidth: StoryFn = ( {
+	className,
+	...args
+} ) => (
+	<>
+		<style>
+			{ `
+				.progressbar-story-custom-width {
+					width: 160px;
+				}
+			` }
```

Yeah, that's a fair point. I'll simplify that, but first, we also need to decide about [this](https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613725251).

### Review thread 28 (resolved)

#### tyxla on 2024-05-24T09:44:26Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613195957
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,51 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
+```
+
+The ProgressBar will expand to take all the available horizontal space of its immediate parent container element. To control its width, you can:
+
+Pass a custom CSS `className` that takes care of setting the `width`:
+
+```css
+.my-css-class {
+  width: 160px;
+}
+```
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar className="my-css-class" />;
+};
+```
+
+Wrap it in a container element (e.g `<div>`) that has a `width` specified:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = ( props ) => {
+  return (
+		<div style={ { width: '160px' } }>
+			<ProgressBar />
+		</div>
+	);
```

Nit: The code spacing seems off here.

### Review thread 29 (resolved)

#### tyxla on 2024-05-24T09:45:26Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613197238
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,51 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
```

I think the stabilization needs its own CHANGELOG entry.

#### fullofcaffeine on 2024-05-24T16:25:14Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613734721
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,51 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
```

I thought it was an internal thing and shouldn't be part of the changelog.

### Review thread 30 (resolved)

#### tyxla on 2024-05-24T09:46:05Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613198239
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -25,7 +25,6 @@ export const Track = styled.div`
 	position: relative;
 	overflow: hidden;
 	width: 100%;
-	max-width: 160px;
```

This needs its own CHANGELOG entry as well.

### Review thread 31

#### tyxla on 2024-05-24T09:51:41Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613205248
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
```

I've always felt we duplicate so much README and add overhead for maintaining these docs in the future 😕 I wonder if we can improve/automate that somehow - perhaps a command to allow the README to inherit content from here?

#### mirka on 2024-05-24T10:10:17Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613230851
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
```

My initial plans were to abandon the manual readmes altogether once the Storybook was "complete" with all the type data and documentation. I do consider it pretty much complete now. But @DaniGuardiola mentioned some kind of TSDoc to markdown docgen he has up his sleeve, so that could be another option.

#### DaniGuardiola on 2024-05-24T10:43:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613273788
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
```

ref: https://github.com/Create-Inc/archetype/?tab=readme-ov-file#createincarchetype-docgen

#### tyxla on 2024-05-24T12:06:35Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613375461
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
```

I see. It would be fun to try something like this out here @DaniGuardiola 👍

#### fullofcaffeine on 2024-05-24T16:26:19Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1613737236
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -46,6 +46,53 @@ function UnforwardedProgressBar(
 	);
 }
 
+/**
+ * A simple horizontal progress bar component.
```

Yeah, I share the non-DRY feelings here, too. A way to extract and keep everything from/in a central place would be nice 👍🏻

### Review thread 32 (resolved)

#### tyxla on 2024-05-27T10:29:59Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615855616
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,9 @@
 
 ## Unreleased
 
+### Enhancements
+
+- Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

There's already an Enhancements section, we'll have to use it instead of adding a new one.

#### fullofcaffeine on 2024-05-27T16:49:51Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616264098
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,9 @@
 
 ## Unreleased
 
+### Enhancements
+
+- Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

Oh, thanks for pointing that out.

#### fullofcaffeine on 2024-05-27T17:00:24Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616270368
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,9 @@
 
 ## Unreleased
 
+### Enhancements
+
+- Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

Fixed in https://github.com/WordPress/gutenberg/pull/61062/commits/47183b7d3fecf89869d23b43a49865e463417fa9.

### Review thread 33 (resolved)

#### tyxla on 2024-05-27T10:32:36Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615858936
Path: packages/components/src/index.ts

```diff
@@ -217,6 +217,7 @@ export {
 } from './higher-order/with-focus-return';
 export { default as withNotices } from './higher-order/with-notices';
 export { default as withSpokenMessages } from './higher-order/with-spoken-messages';
+export { ProgressBar } from './progress-bar';
```

This isn't really a high-order component, can you move the export above, where the rest of the regular components are? Another thing to keep in mind is that we usually maintain alphabetical order with exports here.

#### fullofcaffeine on 2024-05-27T16:50:21Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616264389
Path: packages/components/src/index.ts

```diff
@@ -217,6 +217,7 @@ export {
 } from './higher-order/with-focus-return';
 export { default as withNotices } from './higher-order/with-notices';
 export { default as withSpokenMessages } from './higher-order/with-spoken-messages';
+export { ProgressBar } from './progress-bar';
```

Ah, I didn't notice that this section was about HOCs. Will move.

#### fullofcaffeine on 2024-05-27T16:58:34Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616269081
Path: packages/components/src/index.ts

```diff
@@ -217,6 +217,7 @@ export {
 } from './higher-order/with-focus-return';
 export { default as withNotices } from './higher-order/with-notices';
 export { default as withSpokenMessages } from './higher-order/with-spoken-messages';
+export { ProgressBar } from './progress-bar';
```

Fixed in https://github.com/WordPress/gutenberg/pull/61062/commits/5ca947a879a9d0976fbd00d50728dc037c37ea27.

### Review thread 34 (resolved)

#### tyxla on 2024-05-27T10:35:16Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615861776
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
```

We'll need a semicolon and use tabs for this to be valid syntax according to ESLint and Prettier:

```suggestion
	return <ProgressBar />;
```

#### fullofcaffeine on 2024-05-27T16:51:15Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616264847
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
```

Sounds good. As a related question, do you have ESLint/prettier activated for JSX code blocks in markdown?

#### tyxla on 2024-05-28T10:24:59Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616979553
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
```

Yup, I do.

### Review thread 35 (resolved)

#### tyxla on 2024-05-27T10:35:50Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615862372
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
+```
+
+You can also make it controlled by passing a `value` (from 0 to 100) representing the progress:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = ( { progress } ) => {
+  return <ProgressBar value={ progress } />
```

```suggestion
	return <ProgressBar value={ progress } />;
```

### Review thread 36 (resolved)

#### tyxla on 2024-05-27T10:36:19Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615862924
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
+```
+
+You can also make it controlled by passing a `value` (from 0 to 100) representing the progress:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = ( { progress } ) => {
+  return <ProgressBar value={ progress } />
+}
+```
+
+You can customize its appearance by passing a custom CSS class name to `className`:
+
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar className='my-custom-css-class' />
```

In addition to the missing semicolon, note that we generally use double quotes for string literal props.

```suggestion
	return <ProgressBar className="my-custom-css-class" />;
```

### Review thread 37 (resolved)

#### tyxla on 2024-05-27T10:41:52Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615868670
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
```

When declaring a constant, you also need a semicolon at the end:

```suggestion
};
```

Won't be necessary if we use the hoisted function syntax.

#### fullofcaffeine on 2024-05-27T16:55:37Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616267341
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
```

Yeah, my bad; I don't have syntax high-checking for jsx code blocks, so this slipped through the cracks. Thanks for spotting those.

### Review thread 38 (resolved)

#### tyxla on 2024-05-27T10:41:58Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615868765
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
+```
+
+You can also make it controlled by passing a `value` (from 0 to 100) representing the progress:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = ( { progress } ) => {
+  return <ProgressBar value={ progress } />
+}
```

```suggestion
};
```

### Review thread 39 (resolved)

#### tyxla on 2024-05-27T10:42:04Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615868849
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
+```
+
+You can also make it controlled by passing a `value` (from 0 to 100) representing the progress:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = ( { progress } ) => {
+  return <ProgressBar value={ progress } />
+}
+```
+
+You can customize its appearance by passing a custom CSS class name to `className`:
+
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar className='my-custom-css-class' />
+}
```

```suggestion
};
```

### Review thread 40 (resolved)

#### tyxla on 2024-05-27T10:42:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615869636
Path: packages/components/src/progress-bar/README.md

```diff
@@ -1,13 +1,42 @@
 # ProgressBar
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
-
 A simple horizontal progress bar component.
 
 Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
 
+## Usage
+
+Basic usage:
+
+```jsx
+import { ProgressBar } from '@wordpress/components';
+
+const MyLoadingComponent = () => {
+  return <ProgressBar />
+}
+```
+
+You can also make it controlled by passing a `value` (from 0 to 100) representing the progress:
```

```suggestion
You can also make it determinate by passing a `value` (from 0 to 100) representing the progress:
```

### Review thread 41 (resolved)

#### tyxla on 2024-05-27T10:43:51Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615870524
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -44,6 +44,18 @@ function UnforwardedProgressBar(
 	);
 }
 
+/* A simple horizontal progress bar component.
+ *
+ * Supports two modes: determinate and indeterminate. A progress bar is determinate when a specific progress value has been specified (from 0 to 100), and indeterminate when a value hasn't been specified.
+ *
+ * ```jsx
+ * import { ProgressBar } from '@wordpress/components';
+ *
+ * const MyLoadingComponent = () => {
+ *  return <ProgressBar />
+ * }
```

Let's fix this syntax as well:

```suggestion
 * const MyLoadingComponent = () => {
 * 	return <ProgressBar />;
 * };
```

### Review thread 42 (resolved)

#### tyxla on 2024-05-28T10:25:55Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616980769
Path: packages/components/CHANGELOG.md

```diff
@@ -11,6 +11,7 @@
 
 -   `Tabs`: Animate indicator ([#60560](https://github.com/WordPress/gutenberg/pull/60560)).
 -   `ComboboxControl`: Introduce Combobox expandOnFocus prop ([#61705](https://github.com/WordPress/gutenberg/pull/61705)).
+- Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

Let's also fix the spacing: 
```suggestion
-   Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

### Review thread 43 (resolved)

#### tyxla on 2024-05-28T10:28:00Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1616983993
Path: packages/components/src/index.ts

```diff
@@ -201,6 +201,7 @@ export {
 } from './slot-fill';
 export { default as __experimentalStyleProvider } from './style-provider';
 export { ZStack as __experimentalZStack } from './z-stack';
+export { ProgressBar } from './progress-bar';
```

As I also suggested earlier [here](https://github.com/WordPress/gutenberg/pull/61062#discussion_r1615858936), can we move this up to follow the pre-existing alphabetical order?

#### fullofcaffeine on 2024-05-28T16:18:15Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1617580056
Path: packages/components/src/index.ts

```diff
@@ -201,6 +201,7 @@ export {
 } from './slot-fill';
 export { default as __experimentalStyleProvider } from './style-provider';
 export { ZStack as __experimentalZStack } from './z-stack';
+export { ProgressBar } from './progress-bar';
```

Ah, sure 👍🏻 will keep that in mind as well. Thanks for the reminder.

#### fullofcaffeine on 2024-05-28T16:44:48Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1617613240
Path: packages/components/src/index.ts

```diff
@@ -201,6 +201,7 @@ export {
 } from './slot-fill';
 export { default as __experimentalStyleProvider } from './style-provider';
 export { ZStack as __experimentalZStack } from './z-stack';
+export { ProgressBar } from './progress-bar';
```

Done: https://github.com/WordPress/gutenberg/pull/61062/commits/43e1a57eb2bc2cea2f7bb6639f293de4e700c1f0.

### Review thread 44 (resolved)

#### tyxla on 2024-05-29T12:06:24Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1618767129
Path: packages/components/src/index.ts

```diff
@@ -132,6 +132,7 @@ export { default as PanelHeader } from './panel/header';
 export { default as PanelRow } from './panel/row';
 export { default as Placeholder } from './placeholder';
 export { default as Popover } from './popover';
+export { ProgressBar } from './progress-bar';
```

Maybe we could use `default as ProgressBar` to be consistent with the rest.

#### fullofcaffeine on 2024-05-29T15:29:18Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1619093503
Path: packages/components/src/index.ts

```diff
@@ -132,6 +132,7 @@ export { default as PanelHeader } from './panel/header';
 export { default as PanelRow } from './panel/row';
 export { default as Placeholder } from './placeholder';
 export { default as Popover } from './popover';
+export { ProgressBar } from './progress-bar';
```

Sure, it'd be easier on the eyes.

#### fullofcaffeine on 2024-05-29T15:32:59Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1619098990
Path: packages/components/src/index.ts

```diff
@@ -132,6 +132,7 @@ export { default as PanelHeader } from './panel/header';
 export { default as PanelRow } from './panel/row';
 export { default as Placeholder } from './placeholder';
 export { default as Popover } from './popover';
+export { ProgressBar } from './progress-bar';
```

Done in https://github.com/WordPress/gutenberg/pull/61062/commits/2f978f4af873993a7aa2bea0c8494bb368d2453a.

### Review thread 45 (resolved)

#### tyxla on 2024-05-29T12:08:11Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1618769354
Path: packages/components/CHANGELOG.md

```diff
@@ -11,6 +11,7 @@
 
 -   `Tabs`: Animate indicator ([#60560](https://github.com/WordPress/gutenberg/pull/60560)).
 -   `ComboboxControl`: Introduce Combobox expandOnFocus prop ([#61705](https://github.com/WordPress/gutenberg/pull/61705)).
+-   Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

`Components` is a redundant prefix, since this entire package is about components. Here's a suggestion to remove the redundancy:

```suggestion
-   `ProgressBar`: Expose as public API ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

#### fullofcaffeine on 2024-05-29T15:28:10Z

URL: https://github.com/WordPress/gutenberg/pull/61062#discussion_r1619092003
Path: packages/components/CHANGELOG.md

```diff
@@ -11,6 +11,7 @@
 
 -   `Tabs`: Animate indicator ([#60560](https://github.com/WordPress/gutenberg/pull/60560)).
 -   `ComboboxControl`: Introduce Combobox expandOnFocus prop ([#61705](https://github.com/WordPress/gutenberg/pull/61705)).
+-   Components: Make the `ProgressBar` public ([#61062](https://github.com/WordPress/gutenberg/pull/61062)).
```

Sounds good 👍🏻

## Changed files

- docs/manifest.json (MODIFIED, +6/-0)
- docs/tool/manifest.js (MODIFIED, +0/-1)
- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/index.ts (MODIFIED, +1/-0)
- packages/components/src/private-apis.ts (MODIFIED, +0/-2)
- packages/components/src/progress-bar/README.md (MODIFIED, +33/-4)
- packages/components/src/progress-bar/index.tsx (MODIFIED, +15/-0)
- packages/components/src/progress-bar/stories/index.story.tsx (MODIFIED, +1/-2)
- packages/edit-site/src/components/canvas-loader/index.js (MODIFIED, +5/-2)
- packages/edit-site/src/components/global-styles/font-library-modal/font-collection.js (MODIFIED, +1/-3)
- packages/edit-site/src/components/global-styles/font-library-modal/installed-fonts.js (MODIFIED, +1/-3)
- packages/edit-site/src/components/global-styles/font-library-modal/upload-fonts.js (MODIFIED, +1/-4)
- storybook/manager-head.html (MODIFIED, +1/-0)
