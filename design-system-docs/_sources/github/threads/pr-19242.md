---
number: 19242
type: pull_request
state: MERGED
merged_at: 2020-02-26T08:12:43Z
url: https://github.com/WordPress/gutenberg/pull/19242
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #19242: Components: Refactor SlotFill

- URL: https://github.com/WordPress/gutenberg/pull/19242
- Author: diegohaz
- Created: 2019-12-19T14:54:01Z
- Updated: 2020-03-27T14:20:48Z
- Comments: 9 of 9
- Review threads: 18 of 18
- Changed files: 15 of 15
- Diff: ../pr-diffs/19242.diff

## Body

## Description

This PR is part of #18619.

### The problem

There are several problems with the current `slot-fill` implementation. For example, the context value never changes because it's a class property whose value is never updated during the `SlotFillProvider` lifecycle:

https://github.com/WordPress/gutenberg/blob/56cd1df587d48fcb7d710aaae12098832a40a152/packages/components/src/slot-fill/context.js#L38-L47

Because of that, both the `Slot` and the `Fill` components will never re-render when some action is done in the context (for example, when calling `registerSlot` or `registerFill`). Workarounds like `forceUpdate()` have been introduced to the code to overcome this:

https://github.com/WordPress/gutenberg/blob/56cd1df587d48fcb7d710aaae12098832a40a152/packages/components/src/slot-fill/context.js#L55-L65

Also, the `Fill` component won't re-render when new `fillProps` are passed to the `Slot` component (probably, a new `forceUpdate` call should be placed here to work around this issue):

https://github.com/WordPress/gutenberg/blob/56cd1df587d48fcb7d710aaae12098832a40a152/packages/components/src/slot-fill/fill.js#L56-L59

### This solution

This is a rewrite of the `slot-fill` module using React Hooks and exposing only the Portal API (activated by using the `bubblesVirtually` prop today). This greatly simplifies the code without workarounds.

The component should behave exactly the same as when using the `bubblesVirtually` prop today, except that it will properly re-render when `fillProps` are updated.

The idea is to gradually migrate the codebase to use this while this is experimental and then replace the current `slot-fill` with this one if the experiment succeeds. This would also validate whether the current usage of slots without `bubblesVirtually` could use it or not.

## How has this been tested?
<!-- Please describe in detail how you tested your changes. -->
<!-- Include details of your testing environment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->

This was tested using Storybook and unit tests.

## Types of changes

New feature

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->.

## Issue comments

### diegohaz on 2020-02-19T08:49:06Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-588104715

Thank you so much for the review @gziolo! ❤️

So, I've moved the `slot-fill2` module into the current `slot-fill` one and replaced the current `bubblesVirtually` implementation with it. This also means that the current tests are also testing the refactored implementation. Although I think it needs more tests.

But CI is passing and the code is ready for another review.

### github-actions on 2020-02-19T10:02:03Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-588135662

**Size Change:** +2.69 kB (0%) 

**Total Size:** 866 kB

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/annotations/index.js` | 3.43 kB | -1 B |  |
| `build/block-editor/index.js` | 104 kB | -73 B (0%) |  |
| `build/block-editor/style-rtl.css` | 9.8 kB | +20 B (0%) |  |
| `build/block-editor/style.css` | 9.79 kB | +21 B (0%) |  |
| `build/block-library/index.js` | 116 kB | +1.77 kB (1%) |  |
| `build/block-library/style-rtl.css` | 7.49 kB | +14 B (0%) |  |
| `build/block-library/style.css` | 7.49 kB | +13 B (0%) |  |
| `build/blocks/index.js` | 57.6 kB | +1 B |  |
| `build/components/index.js` | 191 kB | +815 B (0%) |  |
| `build/compose/index.js` | 5.76 kB | +1 B |  |
| `build/core-data/index.js` | 10.5 kB | +1 B |  |
| `build/data/index.js` | 8.22 kB | -2 B (0%) |  |
| `build/edit-post/index.js` | 90.8 kB | +79 B (0%) |  |
| `build/edit-site/index.js` | 4.59 kB | +14 B (0%) |  |
| `build/editor/index.js` | 45.1 kB | -3 B (0%) |  |
| `build/format-library/index.js` | 7.6 kB | +2 B (0%) |  |
| `build/list-reusable-blocks/style-rtl.css` | 226 B | +11 B (4%) |  |
| `build/list-reusable-blocks/style.css` | 226 B | +10 B (4%) |  |
| `build/media-utils/index.js` | 4.85 kB | -1 B |  |
| `build/nux/index.js` | 3.02 kB | +1 B |  |
| `build/redux-routine/index.js` | 2.84 kB | +2 B (0%) |  |
| `build/rich-text/index.js` | 14.3 kB | -3 B (0%) |  |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/a11y/index.js` | 1.01 kB | 0 B |  |
| `build/api-fetch/index.js` | 3.39 kB | 0 B |  |
| `build/autop/index.js` | 2.58 kB | 0 B |  |
| `build/blob/index.js` | 620 B | 0 B |  |
| `build/block-directory/index.js` | 6.02 kB | 0 B |  |
| `build/block-directory/style-rtl.css` | 760 B | 0 B |  |
| `build/block-directory/style.css` | 760 B | 0 B |  |
| `build/block-library/editor-rtl.css` | 7.67 kB | 0 B |  |
| `build/block-library/editor.css` | 7.67 kB | 0 B |  |
| `build/block-library/theme-rtl.css` | 669 B | 0 B |  |
| `build/block-library/theme.css` | 671 B | 0 B |  |
| `build/block-serialization-default-parser/index.js` | 1.65 kB | 0 B |  |
| `build/block-serialization-spec-parser/index.js` | 3.1 kB | 0 B |  |
| `build/components/style-rtl.css` | 16.1 kB | 0 B |  |
| `build/components/style.css` | 16 kB | 0 B |  |
| `build/data-controls/index.js` | 1.04 kB | 0 B |  |
| `build/date/index.js` | 5.36 kB | 0 B |  |
| `build/deprecated/index.js` | 771 B | 0 B |  |
| `build/dom-ready/index.js` | 569 B | 0 B |  |
| `build/dom/index.js` | 3.06 kB | 0 B |  |
| `build/edit-post/style-rtl.css` | 8.7 kB | 0 B |  |
| `build/edit-post/style.css` | 8.69 kB | 0 B |  |
| `build/edit-site/style-rtl.css` | 2.77 kB | 0 B |  |
| `build/edit-site/style.css` | 2.76 kB | 0 B |  |
| `build/edit-widgets/index.js` | 4.36 kB | 0 B |  |
| `build/edit-widgets/style-rtl.css` | 2.8 kB | 0 B |  |
| `build/edit-widgets/style.css` | 2.79 kB | 0 B |  |
| `build/editor/editor-styles-rtl.css` | 327 B | 0 B |  |
| `build/editor/editor-styles.css` | 328 B | 0 B |  |
| `build/editor/style-rtl.css` | 4.13 kB | 0 B |  |
| `build/editor/style.css` | 4.11 kB | 0 B |  |
| `build/element/index.js` | 4.45 kB | 0 B |  |
| `build/escape-html/index.js` | 733 B | 0 B |  |
| `build/format-library/style-rtl.css` | 500 B | 0 B |  |
| `build/format-library/style.css` | 501 B | 0 B |  |
| `build/hooks/index.js` | 1.92 kB | 0 B |  |
| `build/html-entities/index.js` | 621 B | 0 B |  |
| `build/i18n/index.js` | 3.45 kB | 0 B |  |
| `build/is-shallow-equal/index.js` | 711 B | 0 B |  |
| `build/keyboard-shortcuts/index.js` | 2.3 kB | 0 B |  |
| `build/keycodes/index.js` | 1.68 kB | 0 B |  |
| `build/list-reusable-blocks/index.js` | 2.99 kB | 0 B |  |
| `build/notices/index.js` | 1.57 kB | 0 B |  |
| `build/nux/style-rtl.css` | 616 B | 0 B |  |
| `build/nux/style.css` | 613 B | 0 B |  |
| `build/plugins/index.js` | 2.54 kB | 0 B |  |
| `build/primitives/index.js` | 1.49 kB | 0 B |  |
| `build/priority-queue/index.js` | 878 B | 0 B |  |
| `build/server-side-render/index.js` | 2.54 kB | 0 B |  |
| `build/shortcode/index.js` | 1.7 kB | 0 B |  |
| `build/token-list/index.js` | 1.27 kB | 0 B |  |
| `build/url/index.js` | 4 kB | 0 B |  |
| `build/viewport/index.js` | 1.61 kB | 0 B |  |
| `build/warning/index.js` | 1.14 kB | 0 B |  |
| `build/wordcount/index.js` | 1.18 kB | 0 B |  |


</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### diegohaz on 2020-02-23T06:26:14Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-590032108

So, it turns out that both the current code on master that uses `bubblesVirtually` and this PR don't cover the case where fills could be mounted asynchronously in different order.

I tried different approaches to solve that:

### Re-mount the portal when its position in the DOM has to be corrected

  This can be achieved by passing a different `key` to the portal:
  
  ```jsx
  createPortal( element, container, key );
  // or
  createPortal( <Fragment key={ key }>{ element }</Fragment>, container );
  ```

  To identify when the `key` should be changed, I tried different methods:

  - Render an empty `<span />` in place on the `Fill` component and use [`Node.compareDocumentPosition`](https://developer.mozilla.org/en-US/docs/Web/API/Node/compareDocumentPosition) to determine where to add the fill in the array. We can compare its position with the other fills' spans in the DOM. [This is what we use on Reakit](https://github.com/reakit/reakit/blob/88224725f4299f282cdaec357d2ed7b5e6c5ba70/packages/reakit/src/Rover/RoverState.ts#L140-L166) so we can add items to an array in the same order they're rendered in the DOM.

  - Only change the `key` when fills that were already registered tried to register themselves again, which would indicate a new render cycle. If the new fills array were different from the last cycle, that was because a fill has been added or removed.

  - A simpler solution: change `key` whenever the fills array changes. It resulted in several re-mounts.

  According to the [tests](https://github.com/WordPress/gutenberg/blob/aa6160521568848f4bec19510c569c8aa53427c5/packages/components/src/slot-fill/test/slot.js#L224-L275), all of those methods successfully kept fills in the expected order. But in all those cases the block toolbar behaved quite weirdly:

  ![Feb-22-2020 22-21-51](https://user-images.githubusercontent.com/3068563/75104296-58ddee80-55e6-11ea-99a6-24c48e924366.gif)

  This clearly had something to do with the portal re-mounting, but I couldn't figure out how to mitigate that. Maybe it's something specific to how block toolbars respond to mouse over and out? Tried more variations, but I was spending too much time on this `key` strategy. So I decided to research more and try other things.

### Instead of moving the portal itself, put it inside a `<span>` and work only on that

  ```jsx
  const [ span ] = useState( () => createElement( 'span' ) );
  // it'll be re-appended on every render so it'll always be in the correct position 
  useEffect( () => {
    slotNode.appendChild( span );
    return () => span.remove();
  } );
  // createPortal on `span` instead of `slotNode`
  return createPortal( children, span );
  ```

  This is based on a [comment by Dan Abramov](https://github.com/facebook/react/issues/12247#issuecomment-486378549). The issue they're trying to solve there is not the same, but this was the solution that, after a few tweaks, had the best result. As long as you keep the reference to the container element, you can move it freely and the portal element will not need to be recreated.

  Still, this presented a lot of problems: fill elements now would have a `<span>` wrapping them, which broke the styling. Adding `flex: inherit` and other css rules to the span worked around that. But, for some reason, now you could have two popovers open at the same time within the block toolbar. And some e2e tests were failing.

### Conclusion

I ended up reverting those attempts to fix the order issue. It looks like, at least for the modules that are currently using `bubblesVirtually`, they don't need this kind of precision?

It took so many workarounds for this to vaguely work that I think we should, in the future, re-consider the `bubblesVirtually` approach altogether. Maybe using React Portal wasn't a good idea? After all, it's not even supported by React Native.

This PR is already an improvement over the current implementation on master. It properly updates fills when `fillProps` change and gets rid of the `Consumer`. So I'm still in favor of landing it as is.

### youknowriad on 2020-02-24T08:20:09Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-590209048

So I do think we need this precision at some point but I agree that the fact that it's present in master makes it less important to fix in this particular PR.

### gziolo on 2020-02-24T12:24:16Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-590295412

There is one blocker to resolve:

![Screen Shot 2020-02-24 at 13 23 10](https://user-images.githubusercontent.com/699132/75152225-d8fc7500-5708-11ea-9587-2f90853828b7.png)

When you enable the top toolbar option in the More Menu, most of the controls are missing. Once you refresh the page everything goes back to normal. It must be something with the setting change.

### Master

![Screen Shot 2020-02-24 at 13 40 06](https://user-images.githubusercontent.com/699132/75153249-398cb180-570b-11ea-9058-cd142f8d2904.png)

### diegohaz on 2020-02-25T01:44:19Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-590638543

Good catch @gziolo! Thanks for the review! ❤️

So, I found the problem and fixed it. This is something that should be covered by the current tests (`should subsume another slot by the same name`). But `react-test-renderer` doesn't support portals (https://github.com/facebook/react/issues/11565), so even on master that is not being covered.

I'll propose in a follow-up PR a refactored set of tests for this module using react-testing-library (using the dependency directly, without the `@wordpress/test-utils` package proposed in #18855).

### diegohaz on 2020-02-25T03:07:31Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-590659622

Follow-up PR with improved tests: #20428

### aduth on 2020-03-02T16:29:45Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-593490432

This looks really cool! I'd like to circle back and dive into the specific changes, but at least on a cursory glance, the implementation looks solid.

Would you happen to have a sense whether this will help unblock #14845 / #17355 ? I noticed some earlier comments at https://github.com/WordPress/gutenberg/issues/17355#issuecomment-582270165 , but those were prior to some additional changes that came later in this pull request.

### draganescu on 2020-03-27T14:12:05Z

URL: https://github.com/WordPress/gutenberg/pull/19242#issuecomment-605022181

Advised by @aduth I tried reverting this PR and now this #18560 new editor crash is fixed. So this PR apparently did something that crashes the editor with the console message:

`TypeError: "slot is undefined" `

when doing this:

![gif-reusable-crash](https://user-images.githubusercontent.com/107534/77764701-c053e780-7045-11ea-963f-baa08b9ed06f.gif)

## Review threads

### Review thread 1 (resolved)

#### diegohaz on 2019-12-21T05:54:20Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r360631603
Path: packages/components/src/slot-fill2/context.js

```diff
@@ -0,0 +1,94 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	createContext,
+	useMemo,
+	useCallback,
+	useState,
+	useContext,
+} from '@wordpress/element';
+
+export const SlotFillContext = createContext();
+
+function useSlotRegistry() {
+	const [ slots, setSlots ] = useState( {} );
```

On master, we have a `getSlots` function whose reference will never change. By putting the `slots` object into state directly, this will automatically trigger an update/re-render on consumers (`Slot` and `Fill` components) when new slots are added, removed or updated (`fillProps`).

#### gziolo on 2020-02-16T21:00:19Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r379932986
Path: packages/components/src/slot-fill2/context.js

```diff
@@ -0,0 +1,94 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	createContext,
+	useMemo,
+	useCallback,
+	useState,
+	useContext,
+} from '@wordpress/element';
+
+export const SlotFillContext = createContext();
+
+function useSlotRegistry() {
+	const [ slots, setSlots ] = useState( {} );
```

This is neat. I like this proposal. It removes the need for the existence of the `occurrences` local variable.

### Review thread 2 (resolved)

#### diegohaz on 2019-12-21T05:55:59Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r360631688
Path: packages/components/src/slot-fill2/fill.js

```diff
@@ -0,0 +1,23 @@
+/**
+ * WordPress dependencies
+ */
+import { createPortal } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useSlot } from './context';
+
+export default function Fill( { name, children } ) {
+	const slot = useSlot( name );
```

This will trigger a re-render on this component when:
- `name` is updated.
- a new slot is added.
- a slot is updated (`fillProps` or `ref`).
- a slot is removed.

### Review thread 3 (resolved)

#### diegohaz on 2019-12-21T05:59:08Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r360631795
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -3444,6 +3444,54 @@ exports[`Storyshots Components|ScrollLock Default 1`] = `
 </div>
 `;
 
+exports[`Storyshots Components|SlotFill Default 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Context 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Fill Props 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
```

It looks like storyshots doesn't support React Portal? :(

#### gziolo on 2020-01-13T08:40:34Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r365687584
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -3444,6 +3444,54 @@ exports[`Storyshots Components|ScrollLock Default 1`] = `
 </div>
 `;
 
+exports[`Storyshots Components|SlotFill Default 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Context 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Fill Props 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
```

I wouldn't be surprised if you would have to mock it somehow. See also:

https://github.com/WordPress/gutenberg/blob/0f2ec15ed40192a165f79790effa8edaf6504597/storybook/test/index.js#L9-L27

#### gziolo on 2020-02-16T21:05:53Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r379933312
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -3444,6 +3444,54 @@ exports[`Storyshots Components|ScrollLock Default 1`] = `
 </div>
 `;
 
+exports[`Storyshots Components|SlotFill Default 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Context 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Fill Props 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
```

I tried to run old unit tests that exist for the slot component:

https://github.com/WordPress/gutenberg/blob/master/packages/components/src/slot-fill/test/slot.js

They all fail, so it might be an actual issue with the implementation. Well, many of those tests are for the case that doesn't bubble virtually, so it's hard to tell :)

#### diegohaz on 2020-02-19T08:17:44Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381134523
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -3444,6 +3444,54 @@ exports[`Storyshots Components|ScrollLock Default 1`] = `
 </div>
 `;
 
+exports[`Storyshots Components|SlotFill Default 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Context 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
+exports[`Storyshots Components|SlotFill With Fill Props 1`] = `
+Array [
+  <h2>
+    Profile
+  </h2>,
+  <p>
+    Name: 
+    <span />
+  </p>,
+  <p>
+    Age: 
+    <span />
+  </p>,
+]
+`;
+
```

I moved `slot-fill2` into `slot-fill` and replaced the current `bubblesVirtually` implementation with it. I had to remove only one assertion that was testing the component instance, but the rest is working fine. I'll add more tests later.

### Review thread 4 (resolved)

#### gziolo on 2020-02-16T20:18:17Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r379930177
Path: packages/components/src/slot-fill2/README.md

```diff
@@ -0,0 +1,66 @@
+# Slot Fill
+
+Slot and Fill are a pair of components which enable developers to render elsewhere in a React element tree, a pattern often referred to as "portal" rendering. It is a pattern for component extensibility, where a single Slot may be occupied by an indeterminate number of Fills elsewhere in the application.
+
+Slot Fill is heavily inspired by the [`react-slot-fill` library](https://github.com/camwest/react-slot-fill), but uses [React's own portal rendering API](https://reactjs.org/docs/portals.html).
+
+## Usage
+
+At the root of your application, you must render a `SlotFillProvider` which coordinates Slot and Fill rendering.
+
+Then, render a Slot component anywhere in your application, giving it a name.
+
+Any Fill will automatically occupy this Slot space, even if rendered elsewhere in the application.
+
+You can either use the Fill component directly, or a wrapper component type as in the below example to abstract the slot name from consumer awareness.
+
+```jsx
+import { SlotFillProvider, Slot, Fill, Panel, PanelBody } from '@wordpress/components';
+
+const MySlotFillProvider = () => {
+	const MyPanelSlot = () => (
+		<Panel header="Panel with slot">
+			<PanelBody>
+				<Slot name="MyPanelSlot"/>
+			</PanelBody>
+		</Panel>
+	);
+	
+	MyPanelSlot.Content = () => (
+		<Fill name="MyPanelSlot">
+			Panel body
+		</Fill>
+	);
+
+	return (
+		<SlotFillProvider>
+			<MyPanelSlot />
+			<MyPanelSlot.Content />
+		</SlotFillProvider>
+	);
+};
+```
+
+There is also `createSlotFill` helper method which was created to simplify the process of matching the corresponding `Slot` and `Fill` components:
+
+```jsx
+const { Fill, Slot } = createSlotFill( 'Toolbar' );
+
+const ToolbarItem = () => (
+	<Fill>
+		My item
+	</Fill>
+);
+
+const Toolbar = () => (
+	<div className="toolbar">
+		<Slot />
+	</div>
+); 
+```
+
+## Props
+
+The `SlotFillProvider` component does not accept any props.
+
+Both `Slot` and `Fill` accept a `name` string prop, where a `Slot` with a given `name` will render the `children` of any associated `Fill`s.
```

I don't see the following part and the corresponding example:

> `Slot` also accepts optional `children` function prop, which takes `fills` as a param. It allows to perform additional processing and wrap `fills` conditionally.

What's the reason for the removal? It's very convenient if you want to render wrappers conditionally.

#### diegohaz on 2020-02-19T08:21:17Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381135974
Path: packages/components/src/slot-fill2/README.md

```diff
@@ -0,0 +1,66 @@
+# Slot Fill
+
+Slot and Fill are a pair of components which enable developers to render elsewhere in a React element tree, a pattern often referred to as "portal" rendering. It is a pattern for component extensibility, where a single Slot may be occupied by an indeterminate number of Fills elsewhere in the application.
+
+Slot Fill is heavily inspired by the [`react-slot-fill` library](https://github.com/camwest/react-slot-fill), but uses [React's own portal rendering API](https://reactjs.org/docs/portals.html).
+
+## Usage
+
+At the root of your application, you must render a `SlotFillProvider` which coordinates Slot and Fill rendering.
+
+Then, render a Slot component anywhere in your application, giving it a name.
+
+Any Fill will automatically occupy this Slot space, even if rendered elsewhere in the application.
+
+You can either use the Fill component directly, or a wrapper component type as in the below example to abstract the slot name from consumer awareness.
+
+```jsx
+import { SlotFillProvider, Slot, Fill, Panel, PanelBody } from '@wordpress/components';
+
+const MySlotFillProvider = () => {
+	const MyPanelSlot = () => (
+		<Panel header="Panel with slot">
+			<PanelBody>
+				<Slot name="MyPanelSlot"/>
+			</PanelBody>
+		</Panel>
+	);
+	
+	MyPanelSlot.Content = () => (
+		<Fill name="MyPanelSlot">
+			Panel body
+		</Fill>
+	);
+
+	return (
+		<SlotFillProvider>
+			<MyPanelSlot />
+			<MyPanelSlot.Content />
+		</SlotFillProvider>
+	);
+};
+```
+
+There is also `createSlotFill` helper method which was created to simplify the process of matching the corresponding `Slot` and `Fill` components:
+
+```jsx
+const { Fill, Slot } = createSlotFill( 'Toolbar' );
+
+const ToolbarItem = () => (
+	<Fill>
+		My item
+	</Fill>
+);
+
+const Toolbar = () => (
+	<div className="toolbar">
+		<Slot />
+	</div>
+); 
+```
+
+## Props
+
+The `SlotFillProvider` component does not accept any props.
+
+Both `Slot` and `Fill` accept a `name` string prop, where a `Slot` with a given `name` will render the `children` of any associated `Fill`s.
```

I removed this README.md file as the contents of `slot-fill2` were moved over to `slot-fill`. But, the reason to remove that was because `<Slot bubblesVirtually />` didn't accept children prop. Since this refactor only considers that use case, it didn't make sense keeping that in the readme.

But now it's possible to access fills through `useSlot`. See https://github.com/WordPress/gutenberg/pull/19242#discussion_r381127219

### Review thread 5 (resolved)

#### gziolo on 2020-02-16T20:38:53Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r379931560
Path: packages/components/src/slot-fill2/slot.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	useEffect,
+	useRef,
+	useLayoutEffect,
+	useContext,
+} from '@wordpress/element';
+import isShallowEqual from '@wordpress/is-shallow-equal';
+
+/**
+ * Internal dependencies
+ */
+import { SlotFillContext, useSlot } from './context';
+
+export default function Slot( {
+	name,
+	fillProps = {},
+	as: Component = 'div',
+	...props
+} ) {
+	const registry = useContext( SlotFillContext );
+	const ref = useRef();
+	const slot = useSlot( name );
+
+	useEffect( () => {
+		registry.register( name, ref, fillProps );
+		return () => {
+			registry.unregister( name );
```

It is also how it is implemented in the old implementation:

https://github.com/WordPress/gutenberg/blob/78de49ede8fac2cf76600e9048d653d56ab30c11/packages/components/src/slot-fill/slot.js#L31
https://github.com/WordPress/gutenberg/blob/78de49ede8fac2cf76600e9048d653d56ab30c11/packages/components/src/slot-fill/slot.js#L37

It's interesting that we no longer need to pass the `ref` here. I couldn't find any issues with this approach. Noting only 👍

### Review thread 6 (resolved)

#### gziolo on 2020-02-16T20:42:41Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r379931782
Path: packages/components/src/slot-fill2/slot.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	useEffect,
+	useRef,
+	useLayoutEffect,
+	useContext,
+} from '@wordpress/element';
+import isShallowEqual from '@wordpress/is-shallow-equal';
+
+/**
+ * Internal dependencies
+ */
+import { SlotFillContext, useSlot } from './context';
+
+export default function Slot( {
+	name,
+	fillProps = {},
+	as: Component = 'div',
+	...props
+} ) {
+	const registry = useContext( SlotFillContext );
+	const ref = useRef();
+	const slot = useSlot( name );
+
+	useEffect( () => {
+		registry.register( name, ref, fillProps );
+		return () => {
+			registry.unregister( name );
+		};
+		// We are not including fillProps in the deps because we don't want to
+		// unregister and register the slot whenever fillProps change, which would
+		// cause the fill to be re-mounted. We are only considering the initial value
+		// of fillProps.
+	}, [ registry.register, registry.unregister, name ] );
+
+	// fillProps may be an update that interact with the layout, so
+	// we useLayoutEffect
+	useLayoutEffect( () => {
+		if ( slot && ! isShallowEqual( slot.fillProps, fillProps ) ) {
+			registry.update( name, ref, fillProps );
+		}
+	} );
+
+	return <Component ref={ ref } { ...props } />;
```

Just noting that it covers only `bubblesVirtually` set to `true` case.

### Review thread 7 (resolved)

#### gziolo on 2020-02-16T21:01:09Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r379933032
Path: packages/components/src/slot-fill2/index.js

```diff
@@ -0,0 +1,23 @@
+/**
```

What about the set of tests for the slot implementation? I don't see them ported.

I even copied them and tried to run locally but they all fail.

#### diegohaz on 2020-02-19T08:26:27Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381138275
Path: packages/components/src/slot-fill2/index.js

```diff
@@ -0,0 +1,23 @@
+/**
```

I moved `slot-fill2` into `slot-fill` and replaced the current `bubblesVirtually` implementation with it. I had to remove only one assertion that was testing the component instance, but the rest is working fine. I'll add more tests later.

### Review thread 8

#### diegohaz on 2020-02-19T07:44:32Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381121547
Path: packages/block-editor/src/components/block-inspector/index.js

```diff
@@ -69,21 +72,15 @@ const BlockInspector = ( {
 			) }
 			<InspectorControls.Slot bubblesVirtually />
 			<div>
-				<__experimentalSlotFillConsumer>
-					{ ( { hasFills } ) =>
-						hasFills( InspectorAdvancedControls.slotName ) && (
-							<PanelBody
-								className="block-editor-block-inspector__advanced"
-								title={ __( 'Advanced' ) }
-								initialOpen={ false }
-							>
-								<InspectorAdvancedControls.Slot
-									bubblesVirtually
-								/>
-							</PanelBody>
-						)
-					}
-				</__experimentalSlotFillConsumer>
+				{ hasFills && (
+					<PanelBody
+						className="block-editor-block-inspector__advanced"
+						title={ __( 'Advanced' ) }
+						initialOpen={ false }
+					>
+						<InspectorAdvancedControls.Slot bubblesVirtually />
+					</PanelBody>
+				) }
```

Related discussion: https://github.com/WordPress/gutenberg/pull/16807#pullrequestreview-268314678

### Review thread 9 (resolved)

#### diegohaz on 2020-02-19T07:59:34Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381127219
Path: packages/components/src/index.js

```diff
@@ -98,7 +98,7 @@ export {
 	Slot,
 	Fill,
 	Provider as SlotFillProvider,
-	Consumer as __experimentalSlotFillConsumer,
+	useSlot as __experimentalUseSlot,
```

If I got it right, we only had to expose `Consumer` for the cases where we needed to check if slots or fills have been registered.

- On `BlockInspector`, we check the presence of fills so we can conditionally render the slot wrapper ([source](https://github.com/WordPress/gutenberg/blob/59b07cc57adff90630fc9d5cf2317269a0f4f158/packages/block-editor/src/components/block-inspector/index.js#L72-L86))
- On `Popover`, we check the presence of the slot in order to render the contents within a `<Fill>` ([source](https://github.com/WordPress/gutenberg/blob/59b07cc57adff90630fc9d5cf2317269a0f4f158/packages/components/src/popover/index.js#L606-L622))

Links to both changes:
- https://github.com/WordPress/gutenberg/pull/19242/files#r381121547
- https://github.com/WordPress/gutenberg/pull/19242/files#r381128710

#### youknowriad on 2020-02-19T08:48:03Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381148214
Path: packages/components/src/index.js

```diff
@@ -98,7 +98,7 @@ export {
 	Slot,
 	Fill,
 	Provider as SlotFillProvider,
-	Consumer as __experimentalSlotFillConsumer,
+	useSlot as __experimentalUseSlot,
```

Yes, I like the hook better, nice work here.

### Review thread 10

#### diegohaz on 2020-02-19T08:03:18Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381128710
Path: packages/components/src/popover/index.js

```diff
@@ -602,25 +603,15 @@ const Popover = ( {
 		content = <FocusManaged>{ content }</FocusManaged>;
 	}
 
-	return (
-		<Consumer>
-			{ ( { getSlot } ) => {
-				// In case there is no slot context in which to render,
-				// default to an in-place rendering.
-				if ( getSlot && getSlot( __unstableSlotName ) ) {
-					content = (
-						<Fill name={ __unstableSlotName }>{ content }</Fill>
-					);
-				}
+	if ( slot.ref ) {
+		content = <Fill name={ __unstableSlotName }>{ content }</Fill>;
+	}
 
-				if ( anchorRef || anchorRect ) {
-					return content;
-				}
+	if ( anchorRef || anchorRect ) {
+		return content;
+	}
 
-				return <span ref={ anchorRefFallback }>{ content }</span>;
-			} }
-		</Consumer>
-	);
+	return <span ref={ anchorRefFallback }>{ content }</span>;
```

[Looks like PopoverSlot always uses bubblesVirtually](https://github.com/WordPress/gutenberg/blob/0b11256824a1aae503ade5d7071c42d2b2c354ce/packages/components/src/popover/index.js#L619-L621), so I also changed this snippet to use `useSlot` instead of `Consumer`. With that, `Consumer` is only used on the `SlotFill` module internally.

### Review thread 11 (resolved)

#### diegohaz on 2020-02-19T08:07:04Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381130266
Path: packages/components/src/slot-fill/bubbles-virtually/fill.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, createPortal } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useSlot from './use-slot';
+
+export default function Fill( { name, children } ) {
+	const slot = useSlot( name );
+	const ref = useRef();
+
+	useEffect( () => {
+		slot.registerFill( ref );
+		return () => {
+			slot.unregisterFill( ref );
+		};
+	}, [ slot.registerFill, slot.unregisterFill ] );
```

This is just so we can keep track of fills and check their existence later (on `BlockInspector`, for example).

### Review thread 12 (resolved)

#### diegohaz on 2020-02-19T08:13:12Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381132716
Path: packages/components/src/slot-fill/index.js

```diff
@@ -1,13 +1,28 @@
 /**
  * Internal dependencies
  */
-import Slot from './slot';
-import Fill from './fill';
-import Provider, { Consumer } from './context';
+import BaseSlot from './slot';
+import BaseFill from './fill';
+import Provider from './context';
+import BubblesVirtuallySlot from './bubbles-virtually/slot';
+import BubblesVirtuallyFill from './bubbles-virtually/fill';
+import useSlot from './bubbles-virtually/use-slot';
 
-export { Slot };
-export { Fill };
-export { Provider, Consumer };
+export function Slot( { bubblesVirtually, ...props } ) {
+	if ( bubblesVirtually ) {
+		return <BubblesVirtuallySlot { ...props } />;
+	}
+	return <BaseSlot { ...props } />;
+}
+
+export function Fill( props ) {
+	return (
+		<>
+			<BaseFill { ...props } />
+			<BubblesVirtuallyFill { ...props } />
+		</>
+	);
+}
```

This probably needs some code comments. The reason I'm including both `Fill`s there is because we have to register them even before their respective slot is registered (so `BlockInspector` can check if there are fills before rendering the slot, for example).

They both registers themselves and return `null` if there's no slot.

### Review thread 13 (resolved)

#### diegohaz on 2020-02-19T08:15:42Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381133743
Path: packages/components/src/slot-fill/test/slot.js

```diff
@@ -261,10 +259,6 @@ describe( 'Slot', () => {
 				);
 
 				expect( testRenderer.toJSON() ).toMatchSnapshot();
-
-				expect( testRenderer.getInstance().slots ).toHaveProperty(
-					'egg'
-				);
```

I can't really reproduce this with a function component. I'll add more tests.

### Review thread 14

#### youknowriad on 2020-02-19T08:55:41Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381152030
Path: packages/components/src/slot-fill/bubbles-virtually/fill.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, createPortal } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useSlot from './use-slot';
+
+export default function Fill( { name, children } ) {
```

This is a simple and nice implementation but I believe we had issues with fills being reordered when we switch between blocks (toolbar and inspector) and we had to add a custom logic to keep the fills rendering in the exact same order they appear on the React tree (even with conditions).

I see that this new implementation passes the existing unit tests right which I believe account for this (and I'm surprised it does without any tweaks to the rendering order). We might just want to confirm in the UI too.

#### diegohaz on 2020-02-19T09:52:44Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381184137
Path: packages/components/src/slot-fill/bubbles-virtually/fill.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, createPortal } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useSlot from './use-slot';
+
+export default function Fill( { name, children } ) {
```

Hmm, from what I can see, the current unit tests are not covering that case for `bubblesVirtually`. Actually, it seems that `bubblesVirtually` is not being covered at all? `react-test-renderer` doesn't seem to support portals. But I'll check if `react-dom/test-utils` can do the job.

Also, I can't find how it's been solved for the current implementation (using `bubblesVirtually`). Do you have an idea?

I'm pretty sure that my implementation doesn't cover that. `ReactDOM.createPortal`, by default, will just attach elements in the order they're rendered.

I've just made some experiments with the `key` prop here, which seems promising: https://codesandbox.io/s/keeping-react-portal-order-uk9xb

#### youknowriad on 2020-02-19T09:59:54Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381188356
Path: packages/components/src/slot-fill/bubbles-virtually/fill.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, createPortal } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useSlot from './use-slot';
+
+export default function Fill( { name, children } ) {
```

It's related to the `occurrence` counter we have on fills and there's  a function to reorder the fills in the Provider I believe. That said, If we manage to run the unit tests on the new implementation, that would be enough to validate it I think.

### Review thread 15 (resolved)

#### youknowriad on 2020-02-19T09:22:54Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r381166076
Path: packages/components/src/slot-fill/index.js

```diff
@@ -1,13 +1,28 @@
 /**
  * Internal dependencies
  */
-import Slot from './slot';
-import Fill from './fill';
-import Provider, { Consumer } from './context';
+import BaseSlot from './slot';
+import BaseFill from './fill';
```

Should we remove all the bubblesVirtually logic from the BaseSlot, BaseFill and Provider?

### Review thread 16 (resolved)

#### gziolo on 2020-02-24T12:48:52Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r383245464
Path: packages/e2e-tests/specs/editor/various/writing-flow.test.js

```diff
@@ -512,9 +512,9 @@ describe( 'Writing Flow', () => {
 		const y = paragraphRect.y + paragraphRect.height + 1;
 
 		await page.mouse.move( x, y );
-		await page.waitForSelector(
-			'.block-editor-block-list__insertion-point-inserter'
-		);
+		// await page.waitForSelector(
```

Why is it commented out? It doesn't seem like it should have any impact on toolbars.

#### diegohaz on 2020-02-25T01:01:55Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r383604169
Path: packages/e2e-tests/specs/editor/various/writing-flow.test.js

```diff
@@ -512,9 +512,9 @@ describe( 'Writing Flow', () => {
 		const y = paragraphRect.y + paragraphRect.height + 1;
 
 		await page.mouse.move( x, y );
-		await page.waitForSelector(
-			'.block-editor-block-list__insertion-point-inserter'
-		);
+		// await page.waitForSelector(
```

Ah, I forgot it there. That testing was failing while I was trying to fix the ordering issue. I was playing with the test code to see if there was some problem in the test implementation. Just reverted this.

### Review thread 17

#### gziolo on 2020-02-24T13:22:42Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r383260905
Path: packages/block-editor/src/components/block-inspector/index.js

```diff
@@ -30,6 +30,9 @@ const BlockInspector = ( {
 	selectedBlockName,
 	showNoBlockSelectedMessage = true,
 } ) => {
+	const slot = useSlot( InspectorAdvancedControls.slotName );
```

Yes, let's keep `__experimentalUseSlot` experimental until we come up with a better way to handle it. It seems fine for the time being.

### Review thread 18

#### aduth on 2020-03-27T14:20:45Z

URL: https://github.com/WordPress/gutenberg/pull/19242#discussion_r399298774
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.js

```diff
@@ -0,0 +1,92 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo, useCallback, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import SlotFillContext from './slot-fill-context';
+
+function useSlotRegistry() {
+	const [ slots, setSlots ] = useState( {} );
+	const [ fills, setFills ] = useState( {} );
+
+	const registerSlot = useCallback( ( name, ref, fillProps ) => {
+		setSlots( ( prevSlots ) => ( {
+			...prevSlots,
+			[ name ]: {
+				...prevSlots[ name ],
+				ref: ref || prevSlots[ name ].ref,
+				fillProps: fillProps || prevSlots[ name ].fillProps || {},
+			},
+		} ) );
+	}, [] );
+
+	const unregisterSlot = useCallback( ( name, ref ) => {
+		setSlots( ( prevSlots ) => {
+			// eslint-disable-next-line no-unused-vars
+			const { [ name ]: slot, ...nextSlots } = prevSlots;
+			// Make sure we're not unregistering a slot registered by another element
+			// See https://github.com/WordPress/gutenberg/pull/19242#issuecomment-590295412
+			if ( slot.ref === ref ) {
```

Should it be safe to assume `slot` is defined here?

In debugging the issue mentioned by @draganescu at https://github.com/WordPress/gutenberg/issues/18560#issuecomment-604920204 , the exact error occurs at this line of code:

![image](https://user-images.githubusercontent.com/1779930/77765354-874f4f00-7014-11ea-97a2-54cf7c32cc24.png)

It's not yet clear to me if the bug is in how the slot came to be `undefined` in the first place, or if we should be guarded in how we expect `slot` to be available at this line of code.

## Changed files

- packages/block-editor/src/components/block-inspector/index.js (MODIFIED, +13/-16)
- packages/components/src/index.js (MODIFIED, +1/-1)
- packages/components/src/popover/index.js (MODIFIED, +9/-18)
- packages/components/src/slot-fill/bubbles-virtually/fill.js (ADDED, +34/-0)
- packages/components/src/slot-fill/bubbles-virtually/slot-fill-context.js (ADDED, +15/-0)
- packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.js (ADDED, +92/-0)
- packages/components/src/slot-fill/bubbles-virtually/slot.js (ADDED, +48/-0)
- packages/components/src/slot-fill/bubbles-virtually/use-slot.js (ADDED, +54/-0)
- packages/components/src/slot-fill/context.js (MODIFIED, +8/-1)
- packages/components/src/slot-fill/fill.js (MODIFIED, +2/-2)
- packages/components/src/slot-fill/index.js (MODIFIED, +26/-6)
- packages/components/src/slot-fill/slot.js (MODIFIED, +1/-12)
- packages/components/src/slot-fill/stories/index.js (ADDED, +87/-0)
- packages/components/src/slot-fill/test/slot.js (MODIFIED, +1/-7)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +50/-0)
