---
number: 45472
type: pull_request
state: MERGED
merged_at: 2022-11-16T16:07:31Z
url: https://github.com/WordPress/gutenberg/pull/45472
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #45472: Convert the `Snackbar` component to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/45472
- Author: kienstra
- Created: 2022-11-01T22:10:24Z
- Updated: 2022-11-16T16:17:44Z
- Comments: 10 of 10
- Review threads: 25 of 25
- Changed files: 9 of 9
- Diff: ../pr-diffs/45472.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Convert the `Snackbar` component to TypeScript

## Why?
As part of an [effort](https://github.com/WordPress/gutenberg/issues/35744) to convert `@wordpress/components` to TypeScript

## How?
Mainly by adding types to the `Snackbar` and `SnackbarList` components

## Testing Instructions
1. `npm run storybook:dev`
2. Expected: The Storybook still works

## Screenshots or screencast <!-- if applicable -->
<img width="1015" alt="Screen Shot 2022-11-01 at 3 56 43 PM" src="https://user-images.githubusercontent.com/4063887/199349991-afd841f5-4873-4b2b-a4e5-5fbf1d0caaf4.png">

## Follow-up

 - `SnackbarList` is not very well-documented (it doesn't even have a README) and prior to this PR, didn't have Storybook examples either. We should re-organize the folder structure, improve the docs and come up with a better Storybook strategy (e.g. use subcomponents and merge `Snackbar` and `SnackbarList` in the same file)
 - We should  refactor the `Notice` component to TypeScript and re-use its types in `Snackbar`
 - I came across a framer-motion bug when clicking rapidly on two `Snackbar`s in a list — the second `Snackbar` goes transparent but doesn't get fully removed
 - The timeout logic for `Snackbar` to be dismissed works in a bit of an unexpected way (snackbar items don't get dismissed in order
 - Take `isDismissible` prop from notice data is not taken into account by `Snackbar` (although this could be on purpose)
 - The `Snackbar` icon (passed through its icon prop) is not vertically centered, doesn't seem to work well with WordPress icons

## Issue comments

### codesandbox on 2022-11-01T22:10:27Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1299293112

<img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.light.svg#gh-dark-mode-only" /><img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.svg#gh-light-mode-only" />&nbsp; Open in CodeSandbox <a href="https://codesandbox.io/p/github/kienstra/gutenberg/update/snackbar-to-ts?workspace=%7B%22gitSidebarPanel%22:%22PR%22,%22sidebarPanel%22:%22GIT%22%7D">Web Editor</a> | <a href="https://codesandbox.io/p/vscode?owner=kienstra&repo=gutenberg&branch=update/snackbar-to-ts">VS Code</a> | <a href="https://codesandbox.io/p/vscode?owner=kienstra&repo=gutenberg&branch=update/snackbar-to-ts&insiders=true">VS Code Insiders</a>

<!-- open-in-codesandbox:complete -->

### kienstra on 2022-11-02T05:28:31Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1299594252

Hi @ciampo,
Would you have time to review this? Thanks so much for helping me with the `ColorPalette` one.

Thanks, Marco!

### kienstra on 2022-11-02T16:05:45Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1300777571

Thanks a lot for reviewing this, Marco! I'll apply your suggestions today.

### kienstra on 2022-11-02T20:36:28Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1301196783

Hi @ciampo,
Thanks, your ideas made this a lot cleaner. 

Just finished all of your suggestions.

### kienstra on 2022-11-03T21:12:05Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1302668310

Hi @ciampo,
Thanks for your ideas and code. Just finished with your suggestions.

### kienstra on 2022-11-04T21:30:09Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1304276505

Also, I'll be out until Tuesday, November 8th.

### kienstra on 2022-11-14T17:36:39Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1314132585

Hi @ciampo,
Would you have time for another round of review?

Thanks for your huge amount of patience on my PRs 😄

### ciampo on 2022-11-15T09:15:07Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1315014026

Hey @kienstra , I've been partially AFK last week and will be a bit slow to respond this week too — but I'll definitely get back to you!

### kienstra on 2022-11-15T18:55:23Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1315732899

Thanks, @ciampo! Just applied your suggestions. 

No hurry responding, thanks for responding when you're AFK.

### kienstra on 2022-11-16T16:17:39Z

URL: https://github.com/WordPress/gutenberg/pull/45472#issuecomment-1317278385

Thanks so much, Marco! You're so patient.

## Review threads

### Review thread 1 (resolved)

#### kienstra on 2022-11-01T22:17:23Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1010952179
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
```

`false` caused a TypeScript error: 
<img width="852" alt="Screen Shot 2022-11-01 at 4 17 47 PM" src="https://user-images.githubusercontent.com/4063887/199352682-467c6aee-1676-4c5c-ba76-213c58d1bd6f.png">

### Review thread 2 (resolved)

#### kienstra on 2022-11-01T22:24:14Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1010956955
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
```

It'd be better to import [WPNotice](https://github.com/WordPress/gutenberg/blob/62987d7e995a6d24f6b7d5a491c73aff5a1097c8/packages/notices/src/store/selectors.js#L20) from `packages/notices/`, instead of defining this `Notice` type.

But I couldn't find a way to import from `packages/notices/`, given that it's JSDoc, and it doesn't export a type.

#### ciampo on 2022-11-02T09:01:36Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011396300
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
```

The `@wordpress/notices` package is not currently build with the TypeScript compiler, and therefore it's not currently possible to import the `WPNotice` type.

But even if it was possible, I would still be unsure about adding a depending in the components package from a "Gutenberg App"-related package — we'd like to keep the components package as decoupled as possible from the Gutenberg app.

Let's keep the `Notice` type in this file for now.

On top of that, let's add a comment about these types potentially being moved to the `Notice` component once it gets typed

`// TODO: move this type to the Notice component once it gets typed`

#### kienstra on 2022-11-02T18:04:16Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012130128
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
```

Good idea, I committed the TODO in https://github.com/WordPress/gutenberg/pull/45472/commits/5636934be887f7efdc19536ec4e6f5c749ab15fa

#### ciampo on 2022-11-03T10:26:44Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012723391
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
```

Apologies for not being 100% clear — I was proposing to add the TODO comment in `packages/components/src/snackbar/types.ts`, above the `NoticeAction` type. We don't want to touch the types in `@wordpress/notices`, because those types are for the data store, and should therefore not be related to the component.


Would you mind moving the comment over? Thank you!

#### kienstra on 2022-11-03T18:35:55Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013274196
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
```

Ah, sorry. Moved it in https://github.com/WordPress/gutenberg/pull/45472/commits/9af48f2ed27ce98a5e531f05909a85dcc7aab75b

### Review thread 3 (resolved)

#### kienstra on 2022-11-01T22:36:59Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1010965343
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,54 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
```

I changed the type of `spokenMessage` from `string` to `ReactNode`.

The type `ReactNode` includes `string`.

And the fact that [children is the default of spokenMessage](https://github.com/kienstra/gutenberg/blob/62987d7e995a6d24f6b7d5a491c73aff5a1097c8/packages/components/src/snackbar/index.js#L46) was a problem:

```jsx
function Snackbar(
	{
		className,
		children,
		spokenMessage = children,
```

The type of `children`, `ReactNode`, isn't assignable to `string`:

<img width="861" alt="Screen Shot 2022-11-01 at 4 34 06 PM" src="https://user-images.githubusercontent.com/4063887/199356175-350fc371-090a-40f7-9c76-dae70b3d2a8d.png">

#### ciampo on 2022-11-02T08:04:01Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011338504
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,54 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
```

Looking at the old component's docs, `children` used to be of type `string` too. We should therefore mark both `children` and `spokenMessage` as `string`, especially because `spokenMessage` comes from the `Notice` type, which should be representing some kind of raw data, and should not be related to React.

#### kienstra on 2022-11-02T18:09:45Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012135548
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,54 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
```

Thanks, https://github.com/WordPress/gutenberg/pull/45472/commits/7b0c570fac9e8c67b41c3b32752e5f710d77188b changes both to `string`.

### Review thread 4 (resolved)

#### ciampo on 2022-11-02T08:08:49Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011342821
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
```

I wonder if we could use `ReactNode` here instead of `WPElement`

#### kienstra on 2022-11-02T18:26:47Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012152446
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
```

Sure, that's cleaner. Changed in https://github.com/WordPress/gutenberg/pull/45472/commits/ecb238793f1f05b3677350ff89ce912e7c232fd6.

### Review thread 5 (resolved)

#### ciampo on 2022-11-02T09:14:05Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011423793
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
 };
 
 /**
  * Renders a list of notices.
  *
- * @param {Object}   $0           Props passed to the component.
- * @param {Array}    $0.notices   Array of notices to render.
- * @param {Function} $0.onRemove  Function called when a notice should be removed / dismissed.
- * @param {Object}   $0.className Name of the class used by the component.
- * @param {Object}   $0.children  Array of children to be rendered inside the notice list.
- *
- * @return {Object} The rendered notices list.
+ * ```jsx
+ * const MySnackbarListNotice = () => (
+ *   <SnackbarList
+ *     notices={ notices }
+ *     onRemove={ removeNotice }
+ *   />
+ * );
+ * ```
  */
-function SnackbarList( { notices, className, children, onRemove = noop } ) {
-	const listRef = useRef();
+export function SnackbarList( {
+	notices,
+	className,
+	children,
+	onRemove = noop,
+}: WordPressComponentProps< SnackbarListProps, 'div' > ) {
+	const listRef = useRef() as MutableRefObject< HTMLDivElement >;
```

Let's not override the return type of `useRef` with the `as` keyword — instead, we can specific the type of ref with the `useRef< RefType >( ... )` syntax.

Doing so highlights two more issues that the `as` override was hiding:
- in this file, when passing the `ref` to the `<div>`, there's a typescript error caused by the fact that the ref's value can also be `null`. We can solve that easily by changing the starting value of the `ref` from `undefined` to `null`, and by adding `null` to the ref's type
- in `index.tsx`, when calling `listRef?.current.focus()`, there is the chance that `listRef.current` is still `undefined`

Finally, we should be a bit less specific when declaring the type of `listRef` in `types.ts` — using `HTMLElement` instead of `HTMLDivElement` should be enough, while allowing us to change the element representing the list from a `div` to any other HTML element.

```diff
diff --git a/packages/components/src/snackbar/index.tsx b/packages/components/src/snackbar/index.tsx
index 84ee319ad5..f2b223276d 100644
--- a/packages/components/src/snackbar/index.tsx
+++ b/packages/components/src/snackbar/index.tsx
@@ -70,7 +70,7 @@ function UnforwardedSnackbar(
 		}
 
 		// Prevent focus loss by moving it to the list element.
-		listRef?.current.focus();
+		listRef?.current?.focus();
 
 		onDismiss();
 		onRemove();
diff --git a/packages/components/src/snackbar/list.tsx b/packages/components/src/snackbar/list.tsx
index a98bbc5bc1..b073a251bb 100644
--- a/packages/components/src/snackbar/list.tsx
+++ b/packages/components/src/snackbar/list.tsx
@@ -1,7 +1,6 @@
 /**
  * External dependencies
  */
-import type { MutableRefObject } from 'react';
 import classnames from 'classnames';
 
 /**
@@ -66,7 +65,7 @@ export function SnackbarList( {
 	children,
 	onRemove = noop,
 }: WordPressComponentProps< SnackbarListProps, 'div' > ) {
-	const listRef = useRef() as MutableRefObject< HTMLDivElement >;
+	const listRef = useRef< HTMLDivElement | null >( null );
 	const isReducedMotion = useReducedMotion();
 	className = classnames( 'components-snackbar-list', className );
 	const removeNotice = ( notice: Notice ) => () => onRemove( notice.id );
diff --git a/packages/components/src/snackbar/types.ts b/packages/components/src/snackbar/types.ts
index 502b6b27ab..52d8eb534b 100644
--- a/packages/components/src/snackbar/types.ts
+++ b/packages/components/src/snackbar/types.ts
@@ -95,7 +95,7 @@ export type SnackbarProps = {
 	/**
 	 * A ref to the list that contains the snackbar.
 	 */
-	listRef?: MutableRefObject< HTMLDivElement >;
+	listRef?: MutableRefObject< HTMLElement | null >;
 };
 
 export type SnackbarListProps = {
```

#### kienstra on 2022-11-02T18:35:07Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012160381
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
 };
 
 /**
  * Renders a list of notices.
  *
- * @param {Object}   $0           Props passed to the component.
- * @param {Array}    $0.notices   Array of notices to render.
- * @param {Function} $0.onRemove  Function called when a notice should be removed / dismissed.
- * @param {Object}   $0.className Name of the class used by the component.
- * @param {Object}   $0.children  Array of children to be rendered inside the notice list.
- *
- * @return {Object} The rendered notices list.
+ * ```jsx
+ * const MySnackbarListNotice = () => (
+ *   <SnackbarList
+ *     notices={ notices }
+ *     onRemove={ removeNotice }
+ *   />
+ * );
+ * ```
  */
-function SnackbarList( { notices, className, children, onRemove = noop } ) {
-	const listRef = useRef();
+export function SnackbarList( {
+	notices,
+	className,
+	children,
+	onRemove = noop,
+}: WordPressComponentProps< SnackbarListProps, 'div' > ) {
+	const listRef = useRef() as MutableRefObject< HTMLDivElement >;
```

Good idea, better to set the correct type than cast it.

Committed in https://github.com/WordPress/gutenberg/pull/45472/commits/cef1524fca812d5d51a8706f4b40155327695109

### Review thread 6 (resolved)

#### ciampo on 2022-11-02T09:27:06Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011459224
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
+	onDismiss?: Function;
+	status: string;
+	content: string;
+	__unstableHTML?: string;
+	isDismissible: boolean;
+	type: string;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+	 */
+	children: ReactNode;
+	/**
+	 * Used to provide a custom spoken message in place of the `children` default.
+	 *
+	 * @default children
+	 */
+	spokenMessage?: Notice[ 'spokenMessage' ];
+	/**
+	 * A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+	 *
+	 * A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+	 * A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+	 *
+	 * @default 'polite'
+	 */
+	politeness?: 'polite' | 'assertive';
+	/**
+	 * An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+	 *
+	 * @default []
+	 */
+	actions?: Notice[ 'actions' ];
+	/**
+	 * Called to remove the snackbar from the UI.
+	 *
+	 * @default () => void
+	 */
+	onRemove?: Function;
+	/**
+	 * The icon to render in the snackbar.
+	 *
+	 * @default null
+	 */
+	icon?: Notice[ 'icon' ];
+	/**
+	 * Whether to require user action to dismiss the snackbar.
+	 *
+	 * By default, this is dismissed on a timeout, without user interaction.
+	 *
+	 * @default false
+	 */
+	explicitDismiss?: Notice[ 'explicitDismiss' ];
+	/**
+	 * A callback executed when the snackbar is dismissed.
+	 *
+	 * It is distinct from onRemove, which _looks_ like a callback but is
+	 * actually the function to call to remove the snackbar from the UI.
+	 *
+	 * @default () => void
+	 */
+	onDismiss?: Notice[ 'onDismiss' ];
+	/**
+	 * A ref to the list that contains the snackbar.
+	 */
+	listRef?: MutableRefObject< HTMLDivElement >;
+};
+
+export type SnackbarListProps = {
+	/**
+	 * Array of notices to render.
+	 */
+	notices: Notice[];
+	/**
+	 * Children to be rendered inside the notice list.
+	 */
+	children?: ReactNode;
+	/**
+	 * Function called when a notice should be removed / dismissed.
+	 *
+	 * @default () => void
```

Actually, no need to keep the `noop` default — let's clean up the code a little bit, and use optional chaining syntax instead

<details>

<summary>Like so</summary>

```diff
diff --git a/packages/components/src/snackbar/index.tsx b/packages/components/src/snackbar/index.tsx
index 84ee319ad5..70eb1ed2e5 100644
--- a/packages/components/src/snackbar/index.tsx
+++ b/packages/components/src/snackbar/index.tsx
@@ -19,7 +19,6 @@ import Button from '../button';
 import type { Action, ButtonEvent, SnackbarProps } from './types';
 import type { WordPressComponentProps } from '../ui/context';
 
-const noop = () => {};
 const NOTICE_TIMEOUT = 10000;
 
 /**
@@ -47,19 +46,17 @@ function UnforwardedSnackbar(
 		spokenMessage = children,
 		politeness = 'polite',
 		actions = [],
-		onRemove = noop,
+		onRemove,
 		icon = null,
 		explicitDismiss = false,
 		// onDismiss is a callback executed when the snackbar is dismissed.
 		// It is distinct from onRemove, which _looks_ like a callback but is
 		// actually the function to call to remove the snackbar from the UI.
-		onDismiss = noop,
+		onDismiss,
 		listRef,
 	}: WordPressComponentProps< SnackbarProps, 'div' >,
 	ref: ForwardedRef< any >
 ) {
-	onDismiss = onDismiss || noop;
-
 	function dismissMe(
 		event:
 			| KeyboardEvent< HTMLDivElement | HTMLSpanElement >
@@ -72,14 +69,14 @@ function UnforwardedSnackbar(
 		// Prevent focus loss by moving it to the list element.
 		listRef?.current.focus();
 
-		onDismiss();
-		onRemove();
+		onDismiss?.();
+		onRemove?.();
 	}
 
 	function onActionClick( event: ButtonEvent, onClick: Action[ 'onClick' ] ) {
 		event.stopPropagation();
 
-		onRemove();
+		onRemove?.();
 
 		if ( onClick ) {
 			onClick( event );
@@ -92,8 +89,8 @@ function UnforwardedSnackbar(
 	useEffect( () => {
 		const timeoutHandle = setTimeout( () => {
 			if ( ! explicitDismiss ) {
-				onDismiss();
-				onRemove();
+				onDismiss?.();
+				onRemove?.();
 			}
 		}, NOTICE_TIMEOUT );
 
@@ -123,10 +120,10 @@ function UnforwardedSnackbar(
 		<div
 			ref={ ref }
 			className={ classes }
-			onClick={ ! explicitDismiss ? dismissMe : noop }
+			onClick={ ! explicitDismiss ? dismissMe : undefined }
 			tabIndex={ 0 }
 			role={ ! explicitDismiss ? 'button' : '' }
-			onKeyPress={ ! explicitDismiss ? dismissMe : noop }
+			onKeyPress={ ! explicitDismiss ? dismissMe : undefined }
 			aria-label={ ! explicitDismiss ? __( 'Dismiss this notice' ) : '' }
 		>
 			<div className={ snackbarContentClassnames }>
diff --git a/packages/components/src/snackbar/list.tsx b/packages/components/src/snackbar/list.tsx
index a98bbc5bc1..1b49016a95 100644
--- a/packages/components/src/snackbar/list.tsx
+++ b/packages/components/src/snackbar/list.tsx
@@ -21,7 +21,6 @@ import {
 import type { Notice, SnackbarListProps } from './types';
 import type { WordPressComponentProps } from '../ui/context';
 
-const noop = () => {};
 const SNACKBAR_VARIANTS = {
 	init: {
 		height: 0,
@@ -64,12 +63,12 @@ export function SnackbarList( {
 	notices,
 	className,
 	children,
-	onRemove = noop,
+	onRemove,
 }: WordPressComponentProps< SnackbarListProps, 'div' > ) {
 	const listRef = useRef() as MutableRefObject< HTMLDivElement >;
 	const isReducedMotion = useReducedMotion();
 	className = classnames( 'components-snackbar-list', className );
-	const removeNotice = ( notice: Notice ) => () => onRemove( notice.id );
+	const removeNotice = ( notice: Notice ) => () => onRemove?.( notice.id );
 	return (
 		<div className={ className } tabIndex={ -1 } ref={ listRef }>
 			{ children }
diff --git a/packages/components/src/snackbar/types.ts b/packages/components/src/snackbar/types.ts
index 502b6b27ab..b21b915e87 100644
--- a/packages/components/src/snackbar/types.ts
+++ b/packages/components/src/snackbar/types.ts
@@ -65,8 +65,6 @@ export type SnackbarProps = {
 	actions?: Notice[ 'actions' ];
 	/**
 	 * Called to remove the snackbar from the UI.
-	 *
-	 * @default () => void
 	 */
 	onRemove?: Function;
 	/**
@@ -88,8 +86,6 @@ export type SnackbarProps = {
 	 *
 	 * It is distinct from onRemove, which _looks_ like a callback but is
 	 * actually the function to call to remove the snackbar from the UI.
-	 *
-	 * @default () => void
 	 */
 	onDismiss?: Notice[ 'onDismiss' ];
 	/**
@@ -109,8 +105,6 @@ export type SnackbarListProps = {
 	children?: ReactNode;
 	/**
 	 * Function called when a notice should be removed / dismissed.
-	 *
-	 * @default () => void
 	 */
 	onRemove?: Function;
 };
```
</details>

#### kienstra on 2022-11-02T19:11:12Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012197433
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
+	onDismiss?: Function;
+	status: string;
+	content: string;
+	__unstableHTML?: string;
+	isDismissible: boolean;
+	type: string;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+	 */
+	children: ReactNode;
+	/**
+	 * Used to provide a custom spoken message in place of the `children` default.
+	 *
+	 * @default children
+	 */
+	spokenMessage?: Notice[ 'spokenMessage' ];
+	/**
+	 * A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+	 *
+	 * A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+	 * A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+	 *
+	 * @default 'polite'
+	 */
+	politeness?: 'polite' | 'assertive';
+	/**
+	 * An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+	 *
+	 * @default []
+	 */
+	actions?: Notice[ 'actions' ];
+	/**
+	 * Called to remove the snackbar from the UI.
+	 *
+	 * @default () => void
+	 */
+	onRemove?: Function;
+	/**
+	 * The icon to render in the snackbar.
+	 *
+	 * @default null
+	 */
+	icon?: Notice[ 'icon' ];
+	/**
+	 * Whether to require user action to dismiss the snackbar.
+	 *
+	 * By default, this is dismissed on a timeout, without user interaction.
+	 *
+	 * @default false
+	 */
+	explicitDismiss?: Notice[ 'explicitDismiss' ];
+	/**
+	 * A callback executed when the snackbar is dismissed.
+	 *
+	 * It is distinct from onRemove, which _looks_ like a callback but is
+	 * actually the function to call to remove the snackbar from the UI.
+	 *
+	 * @default () => void
+	 */
+	onDismiss?: Notice[ 'onDismiss' ];
+	/**
+	 * A ref to the list that contains the snackbar.
+	 */
+	listRef?: MutableRefObject< HTMLDivElement >;
+};
+
+export type SnackbarListProps = {
+	/**
+	 * Array of notices to render.
+	 */
+	notices: Notice[];
+	/**
+	 * Children to be rendered inside the notice list.
+	 */
+	children?: ReactNode;
+	/**
+	 * Function called when a notice should be removed / dismissed.
+	 *
+	 * @default () => void
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/202787848cb96ce57a4c683a342de016e29add8c

### Review thread 7 (resolved)

#### ciampo on 2022-11-02T09:36:02Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011470568
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
```

It looks like [`url` is optional](https://github.com/WordPress/gutenberg/blob/62987d7e995a6d24f6b7d5a491c73aff5a1097c8/packages/notices/src/store/actions.js#L10) in the original `WPNoticeAction` type.

But also, the old README said "Each member object should contain a `label` and either a `url` link string or `onClick` callback function" — from the sound of this, it looks like a more correct type could be

```diff
diff --git a/packages/components/src/snackbar/types.ts b/packages/components/src/snackbar/types.ts
index 502b6b27ab..cadc8aa7b4 100644
--- a/packages/components/src/snackbar/types.ts
+++ b/packages/components/src/snackbar/types.ts
@@ -17,12 +17,20 @@ export type ButtonEvent =
 	| KeyboardEvent< HTMLButtonElement >
 	| MouseEvent< HTMLButtonElement >;
 
-export type Action = {
+type NoticeActionWithURL = {
 	label: string;
 	url: string;
 	onClick?: ( event: ButtonEvent ) => void;
 };
 
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: ButtonEvent ) => void;
+};
+
+export type Action = NoticeActionWithURL | NoticeActionWithOnClick;
+
 export type Notice = {
 	id: string;
 	spokenMessage: ReactNode;
```

#### kienstra on 2022-11-02T19:17:26Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012202562
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/50f5f9827e99816a837c6bf53fb3ee87a167ee35

### Review thread 8 (resolved)

#### ciampo on 2022-11-02T09:37:05Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011471891
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
```

`NoticeAction` seems like a better name for this type

#### kienstra on 2022-11-02T19:19:44Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012204392
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
```

Good idea, renamed in https://github.com/WordPress/gutenberg/pull/45472/commits/72fdce954661efa7b21dc3076db81bae0fe4916c

### Review thread 9 (resolved)

#### ciampo on 2022-11-02T09:41:25Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011477082
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -54,24 +55,28 @@ function Snackbar(
 		// actually the function to call to remove the snackbar from the UI.
 		onDismiss = noop,
 		listRef,
-	},
-	ref
+	}: WordPressComponentProps< SnackbarProps, 'div' >,
+	ref: ForwardedRef< any >
 ) {
 	onDismiss = onDismiss || noop;
 
-	function dismissMe( event ) {
+	function dismissMe(
+		event:
+			| KeyboardEvent< HTMLDivElement | HTMLSpanElement >
+			| MouseEvent< HTMLDivElement | HTMLSpanElement >
+	) {
```

No need to be this specific with the HTML element being clicked, a generic mouse or keybaord event will do

```suggestion
	function dismissMe( event: KeyboardEvent | MouseEvent ) {
```

### Review thread 10 (resolved)

#### ciampo on 2022-11-02T09:44:21Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011480265
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
+	onDismiss?: Function;
```

Let's avoid using the `Function` type — we can use `() => void` instead

#### kienstra on 2022-11-02T19:26:39Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012210364
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
+	onDismiss?: Function;
```

Sure, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/20056c37459277ea495c48199b2728fe6372083e

### Review thread 11 (resolved)

#### ciampo on 2022-11-02T09:49:29Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011486261
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
```

This type is being used for the `onClick` event on the `Button` component — the `Button` component, when the `href` attribute is passed, renders an `HTMLAnchorElement` and not a `HTMLButtonElement` (this error is not flagged by TypeScript because the `Button` component is not typed yet).

We can actually avoid using this `ButtonEvent` type altogether if we slightly relax the event-related types, using the more generic `SyntheticEvent`

<details>

<summary>Click to expand</summary>

```diff
diff --git a/packages/components/src/snackbar/index.tsx b/packages/components/src/snackbar/index.tsx
index 84ee319ad5..268762a98e 100644
--- a/packages/components/src/snackbar/index.tsx
+++ b/packages/components/src/snackbar/index.tsx
@@ -16,7 +16,7 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import Button from '../button';
-import type { Action, ButtonEvent, SnackbarProps } from './types';
+import type { Action, SnackbarProps } from './types';
 import type { WordPressComponentProps } from '../ui/context';
 
 const noop = () => {};
@@ -76,7 +76,7 @@ function UnforwardedSnackbar(
 		onRemove();
 	}
 
-	function onActionClick( event: ButtonEvent, onClick: Action[ 'onClick' ] ) {
+	function onActionClick( event: MouseEvent, onClick: Action[ 'onClick' ] ) {
 		event.stopPropagation();
 
 		onRemove();
@@ -140,7 +140,7 @@ function UnforwardedSnackbar(
 							key={ index }
 							href={ url }
 							variant="tertiary"
-							onClick={ ( event: ButtonEvent ) =>
+							onClick={ ( event: MouseEvent ) =>
 								onActionClick( event, onClick )
 							}
 							className="components-snackbar__action"
diff --git a/packages/components/src/snackbar/types.ts b/packages/components/src/snackbar/types.ts
index 502b6b27ab..4856585c6d 100644
--- a/packages/components/src/snackbar/types.ts
+++ b/packages/components/src/snackbar/types.ts
@@ -1,26 +1,17 @@
 /**
  * External dependencies
  */
-import type {
-	KeyboardEvent,
-	MouseEvent,
-	MutableRefObject,
-	ReactNode,
-} from 'react';
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
 
 /**
  * WordPress dependencies
  */
 import type { WPElement } from '@wordpress/element';
 
-export type ButtonEvent =
-	| KeyboardEvent< HTMLButtonElement >
-	| MouseEvent< HTMLButtonElement >;
-
 export type Action = {
 	label: string;
 	url: string;
-	onClick?: ( event: ButtonEvent ) => void;
+	onClick?: ( event: SyntheticEvent ) => void;
 };
 
 export type Notice = {
```

</details>

#### kienstra on 2022-11-02T19:32:21Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012215210
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
```

Nice, that's cleaner without `ButtonEvent`.

Committed in https://github.com/WordPress/gutenberg/pull/45472/commits/e3d2448b3cf9e098dc2b2648f7e097e4a9467b2e

### Review thread 12 (resolved)

#### ciampo on 2022-11-02T10:07:24Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011506981
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
```

Have you tried testing that these new values work as expected for `reducedMotion` users (i.e. the notices don't animate in/out) ?

A cleaner alternative to passing `{}` could be to remove the `SNACKBAR_REDUCE_MOTION_VARIANTS` variable altogether and simply pass `undefined` to the `variant` prop of `motion.div` when `isReducedMotion === true`

#### kienstra on 2022-11-02T20:10:26Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012251741
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
```

>Have you tried testing that these new values work as expected for reducedMotion users (i.e. the notices don't animate in/out) ?

I just tested it, and it looks like they don't animate in/out:
![snackbarlist-no-animate](https://user-images.githubusercontent.com/4063887/199592516-f79dde99-8b0f-475b-9be4-6bd703738200.gif)

If it's alright, I just [added](https://github.com/kienstra/gutenberg/blob/9818ca5ff6b337fffb8eb2283916a572be12fe69/packages/components/src/snackbar/stories/list.tsx) a story for `SnackbarList`.

#### kienstra on 2022-11-02T20:11:34Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012252765
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
```

> A cleaner alternative to passing {} could be to remove the SNACKBAR_REDUCE_MOTION_VARIANTS variable altogether and simply pass undefined to the variant prop of motion.div when isReducedMotion === true

Thanks, that's much cleaner than keeping an object with 3 properties of `{}`.

Removed `SNACKBAR_REDUCE_MOTION_VARIANTS` in https://github.com/WordPress/gutenberg/pull/45472/commits/8667eb15b12f57617a935b49bda42ad44917f5fb

#### ciampo on 2022-11-03T11:46:00Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012796871
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
```

> If it's alright, I just [added](https://github.com/kienstra/gutenberg/blob/9818ca5ff6b337fffb8eb2283916a572be12fe69/packages/components/src/snackbar/stories/list.tsx) a story for `SnackbarList`

Thank you! 

<details>

<summary>I went ahead and made a few additions locally, in order to be able to test the manual dismissal of the notices and their animation (you may want to commit these changes)</summary>

```diff
diff --git a/packages/components/src/snackbar/stories/list.tsx b/packages/components/src/snackbar/stories/list.tsx
index d6567f3feb..a12b2c171a 100644
--- a/packages/components/src/snackbar/stories/list.tsx
+++ b/packages/components/src/snackbar/stories/list.tsx
@@ -3,6 +3,11 @@
  */
 import type { ComponentMeta, ComponentStory } from '@storybook/react';
 
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
@@ -29,9 +34,26 @@ export default meta;
 
 export const Default: ComponentStory< typeof SnackbarList > = ( {
 	children,
+	notices: noticesProp,
 	...props
 } ) => {
-	return <SnackbarList { ...props }>{ children }</SnackbarList>;
+	const [ notices, setNotices ] = useState( noticesProp );
+
+	const onRemove = ( id: string ) => {
+		const matchIndex = notices.findIndex( ( n ) => n.id === id );
+		if ( matchIndex > -1 ) {
+			setNotices( [
+				...notices.slice( 0, matchIndex ),
+				...notices.slice( matchIndex + 1 ),
+			] );
+		}
+	};
+
+	return (
+		<SnackbarList { ...props } notices={ notices } onRemove={ onRemove }>
+			{ children }
+		</SnackbarList>
+	);
 };
 
 Default.args = {
@@ -39,7 +61,7 @@ Default.args = {
 		'Use SnackbarList to communicate multiple low priority, non-interruptive messages to the user.',
 	notices: [
 		{
-			id: 'SAVE_POST_NOTICE_ID',
+			id: 'SAVE_POST_NOTICE_ID_1',
 			spokenMessage: 'Post published.',
 			actions: [
 				{
@@ -52,7 +74,7 @@ Default.args = {
 			explicitDismiss: false,
 		},
 		{
-			id: 'SAVE_POST_NOTICE_ID',
+			id: 'SAVE_POST_NOTICE_ID_2',
 			spokenMessage: 'Post updated',
 			actions: [
 				{
```
</details>

Let's review the Storybook examples later (maybe even in a different PRs) — but this is already a small improvement!

#### kienstra on 2022-11-03T19:11:09Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013304694
Path: packages/components/src/snackbar/list.tsx

```diff
@@ -40,27 +43,33 @@ const SNACKBAR_VARIANTS = {
 };
 
 const SNACKBAR_REDUCE_MOTION_VARIANTS = {
-	init: false,
-	open: false,
-	exit: false,
+	init: {},
+	open: {},
+	exit: {},
```

Oh thanks! Committed this verbatim in https://github.com/WordPress/gutenberg/pull/45472/commits/fc26baf7fd0f852122728c92db0b17c98c10f0c8

### Review thread 13 (resolved)

#### ciampo on 2022-11-02T10:27:13Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1011528267
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
+	onDismiss?: Function;
+	status: string;
+	content: string;
+	__unstableHTML?: string;
+	isDismissible: boolean;
+	type: string;
```

```suggestion
	content: string;
	isDismissible: boolean;
```

Let's remove those `Notice` props, since they're currently not used in this component. We will add them back and type them properly when working on the `Notice` component

#### kienstra on 2022-11-02T19:34:24Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012216935
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,116 @@
+/**
+ * External dependencies
+ */
+import type {
+	KeyboardEvent,
+	MouseEvent,
+	MutableRefObject,
+	ReactNode,
+} from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+
+export type ButtonEvent =
+	| KeyboardEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
+
+export type Action = {
+	label: string;
+	url: string;
+	onClick?: ( event: ButtonEvent ) => void;
+};
+
+export type Notice = {
+	id: string;
+	spokenMessage: ReactNode;
+	actions: Action[];
+	icon?: WPElement | null;
+	onDismiss?: Function;
+	status: string;
+	content: string;
+	__unstableHTML?: string;
+	isDismissible: boolean;
+	type: string;
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/61f5caa469c97cc78cdccf4190be7b1472312c33

### Review thread 14 (resolved)

#### ciampo on 2022-11-03T11:51:05Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012801172
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
+
+export type NoticeActionWithURL = {
+	label: string;
+	url: string;
+	onClick?: ( event: SyntheticEvent ) => void;
+};
+
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: SyntheticEvent ) => void;
+};
+
+export type NoticeAction = NoticeActionWithURL | NoticeActionWithOnClick;
+
+export type Notice = {
+	id: string;
+	spokenMessage: string;
+	actions: NoticeAction[];
+	icon?: ReactNode;
+	onDismiss?: () => void;
+	content: string;
+	isDismissible: boolean;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+	 */
+	children: string;
+	/**
+	 * Used to provide a custom spoken message in place of the `children` default.
+	 *
+	 * @default children
+	 */
+	spokenMessage?: Notice[ 'spokenMessage' ];
+	/**
+	 * A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+	 *
+	 * A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+	 * A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+	 *
+	 * @default 'polite'
+	 */
+	politeness?: 'polite' | 'assertive';
+	/**
+	 * An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+	 *
+	 * @default []
+	 */
+	actions?: Notice[ 'actions' ];
+	/**
+	 * Called to remove the snackbar from the UI.
+	 */
+	onRemove?: () => void;
+	/**
+	 * The icon to render in the snackbar.
+	 *
+	 * @default null
+	 */
+	icon?: Notice[ 'icon' ];
+	/**
+	 * Whether to require user action to dismiss the snackbar.
+	 *
+	 * By default, this is dismissed on a timeout, without user interaction.
+	 *
+	 * @default false
+	 */
+	explicitDismiss?: Notice[ 'explicitDismiss' ];
+	/**
+	 * A callback executed when the snackbar is dismissed.
+	 *
+	 * It is distinct from onRemove, which _looks_ like a callback but is
+	 * actually the function to call to remove the snackbar from the UI.
+	 */
+	onDismiss?: Notice[ 'onDismiss' ];
+	/**
+	 * A ref to the list that contains the snackbar.
+	 */
+	listRef?: MutableRefObject< HTMLDivElement | null >;
+};
+
+export type SnackbarListProps = {
+	/**
+	 * Array of notices to render.
+	 */
+	notices: Notice[];
+	/**
+	 * Children to be rendered inside the notice list.
+	 */
+	children?: string;
```

The list's `children` prop should be a `ReactNode`

#### kienstra on 2022-11-03T19:36:10Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013324291
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
+
+export type NoticeActionWithURL = {
+	label: string;
+	url: string;
+	onClick?: ( event: SyntheticEvent ) => void;
+};
+
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: SyntheticEvent ) => void;
+};
+
+export type NoticeAction = NoticeActionWithURL | NoticeActionWithOnClick;
+
+export type Notice = {
+	id: string;
+	spokenMessage: string;
+	actions: NoticeAction[];
+	icon?: ReactNode;
+	onDismiss?: () => void;
+	content: string;
+	isDismissible: boolean;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+	 */
+	children: string;
+	/**
+	 * Used to provide a custom spoken message in place of the `children` default.
+	 *
+	 * @default children
+	 */
+	spokenMessage?: Notice[ 'spokenMessage' ];
+	/**
+	 * A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+	 *
+	 * A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+	 * A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+	 *
+	 * @default 'polite'
+	 */
+	politeness?: 'polite' | 'assertive';
+	/**
+	 * An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+	 *
+	 * @default []
+	 */
+	actions?: Notice[ 'actions' ];
+	/**
+	 * Called to remove the snackbar from the UI.
+	 */
+	onRemove?: () => void;
+	/**
+	 * The icon to render in the snackbar.
+	 *
+	 * @default null
+	 */
+	icon?: Notice[ 'icon' ];
+	/**
+	 * Whether to require user action to dismiss the snackbar.
+	 *
+	 * By default, this is dismissed on a timeout, without user interaction.
+	 *
+	 * @default false
+	 */
+	explicitDismiss?: Notice[ 'explicitDismiss' ];
+	/**
+	 * A callback executed when the snackbar is dismissed.
+	 *
+	 * It is distinct from onRemove, which _looks_ like a callback but is
+	 * actually the function to call to remove the snackbar from the UI.
+	 */
+	onDismiss?: Notice[ 'onDismiss' ];
+	/**
+	 * A ref to the list that contains the snackbar.
+	 */
+	listRef?: MutableRefObject< HTMLDivElement | null >;
+};
+
+export type SnackbarListProps = {
+	/**
+	 * Array of notices to render.
+	 */
+	notices: Notice[];
+	/**
+	 * Children to be rendered inside the notice list.
+	 */
+	children?: string;
```

Sure, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/a55a083aad978453a97d258cd9cf0a7f104102d8

### Review thread 15 (resolved)

#### ciampo on 2022-11-03T12:03:10Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012811407
Path: packages/components/src/snackbar/stories/index.tsx

```diff
@@ -0,0 +1,96 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * Internal dependencies
+ */
+import Snackbar from '..';
+
+const meta: ComponentMeta< typeof Snackbar > = {
+	title: 'Components/Snackbar',
+	component: Snackbar,
+	argTypes: {
+		as: { control: { type: null } },
+		onRemove: {
+			action: 'onRemove',
+			control: { type: null },
+		},
+		onDismiss: {
+			action: 'onDismiss',
+			control: { type: null },
+		},
+		listRef: {
+			control: { type: null },
+		},
+	},
+	parameters: {
+		controls: {
+			expanded: true,
+		},
+		docs: { source: { state: 'open' } },
+	},
+};
+export default meta;
+
+const DefaultTemplate: ComponentStory< typeof Snackbar > = ( {
+	children,
+	...props
+} ) => {
+	return <Snackbar { ...props }>{ children }</Snackbar>;
+};
+
+export const Default: ComponentStory< typeof Snackbar > = DefaultTemplate.bind(
+	{}
+);
+Default.args = {
+	children:
+		'Use Snackbars to communicate low priority, non-interruptive messages to the user.',
+};
+
+export const WithActions: ComponentStory< typeof Snackbar > =
+	DefaultTemplate.bind( {} );
+WithActions.args = {
+	actions: [
+		{
+			label: 'Open WP.org',
+			url: 'https://wordpress.org',
+		},
+	],
+	children: 'Use Snackbars with an action link to an external page.',
+};
+
+export const WithIcon: ComponentStory< typeof Snackbar > = DefaultTemplate.bind(
+	{}
+);
+WithIcon.args = {
+	children: 'Add an icon to make your snackbar stand out',
+	icon: (
+		<span role="img" aria-label="Icon" style={ { fontSize: 21 } }>
+			🌮
+		</span>
+	),
+};
+
+export const WithExplicitDismiss: ComponentStory< typeof Snackbar > =
+	DefaultTemplate.bind( {} );
+WithExplicitDismiss.args = {
+	children:
+		'Add a cross to explicitly close the snackbar, and do not hide it automatically',
+	explicitDismiss: true,
+};
+
+export const WithActionAndExpicitDismiss: ComponentStory< typeof Snackbar > =
+	DefaultTemplate.bind( {} );
+WithActionAndExpicitDismiss.args = {
```

Typo

```suggestion
export const WithActionAndExplicitDismiss: ComponentStory< typeof Snackbar > =
	DefaultTemplate.bind( {} );
WithActionAndExplicitDismiss.args = {
```

### Review thread 16 (resolved)

#### ciampo on 2022-11-03T12:05:37Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012813383
Path: packages/components/src/snackbar/stories/index.tsx

```diff
@@ -0,0 +1,96 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * Internal dependencies
+ */
+import Snackbar from '..';
+
+const meta: ComponentMeta< typeof Snackbar > = {
+	title: 'Components/Snackbar',
+	component: Snackbar,
+	argTypes: {
+		as: { control: { type: null } },
+		onRemove: {
+			action: 'onRemove',
+			control: { type: null },
+		},
+		onDismiss: {
+			action: 'onDismiss',
+			control: { type: null },
+		},
+		listRef: {
+			control: { type: null },
+		},
+	},
+	parameters: {
+		controls: {
+			expanded: true,
+		},
+		docs: { source: { state: 'open' } },
+	},
+};
+export default meta;
+
+const DefaultTemplate: ComponentStory< typeof Snackbar > = ( {
+	children,
+	...props
+} ) => {
+	return <Snackbar { ...props }>{ children }</Snackbar>;
+};
+
+export const Default: ComponentStory< typeof Snackbar > = DefaultTemplate.bind(
+	{}
+);
+Default.args = {
+	children:
+		'Use Snackbars to communicate low priority, non-interruptive messages to the user.',
+};
+
+export const WithActions: ComponentStory< typeof Snackbar > =
+	DefaultTemplate.bind( {} );
+WithActions.args = {
+	actions: [
+		{
+			label: 'Open WP.org',
+			url: 'https://wordpress.org',
+		},
+	],
+	children: 'Use Snackbars with an action link to an external page.',
+};
+
+export const WithIcon: ComponentStory< typeof Snackbar > = DefaultTemplate.bind(
+	{}
+);
+WithIcon.args = {
+	children: 'Add an icon to make your snackbar stand out',
+	icon: (
+		<span role="img" aria-label="Icon" style={ { fontSize: 21 } }>
+			🌮
+		</span>
+	),
```

It would be cool if we could use `@wordpress/icons` like we do in other components (eg. [`Button`](https://github.com/WordPress/gutenberg/blob/0ad21d5efcbdcc074806c6252d3836c85a6c0ee9/packages/components/src/button/stories/index.js)), but I gave it a quick try and it looks like those icons are not well supported by the component currently (layout and color especially).

One more thing to improve on this component after it gets refactored to TypeScript

#### kienstra on 2022-11-03T19:37:06Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013324936
Path: packages/components/src/snackbar/stories/index.tsx

```diff
@@ -0,0 +1,96 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * Internal dependencies
+ */
+import Snackbar from '..';
+
+const meta: ComponentMeta< typeof Snackbar > = {
+	title: 'Components/Snackbar',
+	component: Snackbar,
+	argTypes: {
+		as: { control: { type: null } },
+		onRemove: {
+			action: 'onRemove',
+			control: { type: null },
+		},
+		onDismiss: {
+			action: 'onDismiss',
+			control: { type: null },
+		},
+		listRef: {
+			control: { type: null },
+		},
+	},
+	parameters: {
+		controls: {
+			expanded: true,
+		},
+		docs: { source: { state: 'open' } },
+	},
+};
+export default meta;
+
+const DefaultTemplate: ComponentStory< typeof Snackbar > = ( {
+	children,
+	...props
+} ) => {
+	return <Snackbar { ...props }>{ children }</Snackbar>;
+};
+
+export const Default: ComponentStory< typeof Snackbar > = DefaultTemplate.bind(
+	{}
+);
+Default.args = {
+	children:
+		'Use Snackbars to communicate low priority, non-interruptive messages to the user.',
+};
+
+export const WithActions: ComponentStory< typeof Snackbar > =
+	DefaultTemplate.bind( {} );
+WithActions.args = {
+	actions: [
+		{
+			label: 'Open WP.org',
+			url: 'https://wordpress.org',
+		},
+	],
+	children: 'Use Snackbars with an action link to an external page.',
+};
+
+export const WithIcon: ComponentStory< typeof Snackbar > = DefaultTemplate.bind(
+	{}
+);
+WithIcon.args = {
+	children: 'Add an icon to make your snackbar stand out',
+	icon: (
+		<span role="img" aria-label="Icon" style={ { fontSize: 21 } }>
+			🌮
+		</span>
+	),
```

Yeah, that'd be nice to simply have a `@wordpress/icon` as the icon.

### Review thread 17 (resolved)

#### ciampo on 2022-11-03T12:10:03Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012817282
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,52 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
```

Could we sort the props alphabetically?

#### kienstra on 2022-11-03T19:43:54Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013330065
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,52 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
```

Sure! Committed in https://github.com/WordPress/gutenberg/pull/45472/commits/84071c6354a66ce487477d632b64ee3e14cdf5c7

### Review thread 18 (resolved)

#### ciampo on 2022-11-03T12:12:24Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012819321
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,52 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `children`: `string`
+The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+-   Required: Yes
+
+#### `spokenMessage`: `string`
+Used to provide a custom spoken message in place of the `children` default.
+-   Required: No
+-   Default: `children`
+
+#### `politeness`: `'polite' | 'assertive'`
+A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+-   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+-   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+-   Required: No
+-   Default: `'polite'`
+
+#### `actions`: `Action[]`
+An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+-   Required: No
+-   Default: `[]`
```

Some nitty formatting:

- let's add an empty line in between the prop's heading and the description paragraph
- let's add an empty line after the description paragraph and before the required/default part

```suggestion
#### `actions`: `Action[]`

An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.

-   Required: No
-   Default: `[]`
```

Let's apply this formatting for all props

#### kienstra on 2022-11-03T19:40:30Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013327506
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,52 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `children`: `string`
+The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+-   Required: Yes
+
+#### `spokenMessage`: `string`
+Used to provide a custom spoken message in place of the `children` default.
+-   Required: No
+-   Default: `children`
+
+#### `politeness`: `'polite' | 'assertive'`
+A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+-   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+-   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+-   Required: No
+-   Default: `'polite'`
+
+#### `actions`: `Action[]`
+An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+-   Required: No
+-   Default: `[]`
```

Ah, good idea. Committed in https://github.com/WordPress/gutenberg/pull/45472/commits/44a53712146af88dc39fe17e895b0fbb8d7d7cd7.

### Review thread 19 (resolved)

#### ciampo on 2022-11-03T12:15:53Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012822345
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
+
+export type NoticeActionWithURL = {
+	label: string;
+	url: string;
+	onClick?: ( event: SyntheticEvent ) => void;
+};
+
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: SyntheticEvent ) => void;
+};
+
+export type NoticeAction = NoticeActionWithURL | NoticeActionWithOnClick;
+
+export type Notice = {
+	id: string;
+	spokenMessage: string;
+	actions: NoticeAction[];
+	icon?: ReactNode;
+	onDismiss?: () => void;
+	content: string;
+	isDismissible: boolean;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+	 */
+	children: string;
+	/**
+	 * Used to provide a custom spoken message in place of the `children` default.
+	 *
+	 * @default children
+	 */
+	spokenMessage?: Notice[ 'spokenMessage' ];
+	/**
+	 * A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+	 *
+	 * A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+	 * A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+	 *
+	 * @default 'polite'
+	 */
```

- no need to mention the default value in the description text, we already have a `default` tag
- let's not use markdown syntax for JSDoc links, instead we can use the `@see` notation
- let's keep each line within the 80 chars limit for improved legibility

```suggestion
	/**
	 * A politeness level for the notice's spoken message. Should be provided as
	 * one of the valid options for an `aria-live` attribute value. Note that this
	 * value should be considered a suggestion; assistive technologies may
	 * override it based on internal heuristics.
	 *
	 * A value of `'assertive'` is to be used for important, and usually
	 * time-sensitive, information. It will interrupt anything else the screen
	 * reader is announcing in that moment.
	 * A value of `'polite'` is to be used for advisory information. It should
	 * not interrupt what the screen reader is announcing in that moment
	 * (the "speech queue") or interrupt the current task.
	 *
	 * @see https://www.w3.org/TR/wai-aria-1.1/#aria-live
	 *
	 * @default 'polite'
	 */
```

#### kienstra on 2022-11-03T20:03:25Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013348205
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
+
+export type NoticeActionWithURL = {
+	label: string;
+	url: string;
+	onClick?: ( event: SyntheticEvent ) => void;
+};
+
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: SyntheticEvent ) => void;
+};
+
+export type NoticeAction = NoticeActionWithURL | NoticeActionWithOnClick;
+
+export type Notice = {
+	id: string;
+	spokenMessage: string;
+	actions: NoticeAction[];
+	icon?: ReactNode;
+	onDismiss?: () => void;
+	content: string;
+	isDismissible: boolean;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+	 */
+	children: string;
+	/**
+	 * Used to provide a custom spoken message in place of the `children` default.
+	 *
+	 * @default children
+	 */
+	spokenMessage?: Notice[ 'spokenMessage' ];
+	/**
+	 * A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+	 *
+	 * A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+	 * A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
+	 *
+	 * @default 'polite'
+	 */
```

Good ideas, these lines were long.

Committed in https://github.com/WordPress/gutenberg/pull/45472/commits/3939a0c8d6aec36f00786ca8c6e8e695846031c4 and https://github.com/WordPress/gutenberg/pull/45472/commits/70bfe595c8de58542f45de8ac4bb7b90fd31e75a

### Review thread 20 (resolved)

#### ciampo on 2022-11-03T12:17:39Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012823879
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
+
+export type NoticeActionWithURL = {
+	label: string;
+	url: string;
+	onClick?: ( event: SyntheticEvent ) => void;
+};
+
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: SyntheticEvent ) => void;
+};
+
+export type NoticeAction = NoticeActionWithURL | NoticeActionWithOnClick;
+
+export type Notice = {
+	id: string;
+	spokenMessage: string;
+	actions: NoticeAction[];
+	icon?: ReactNode;
+	onDismiss?: () => void;
+	content: string;
+	isDismissible: boolean;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
```

Let's break the line and make sure that comments in this file don't go beyond the 80 characters mark in each line (`children`, `actions` and `politeness`)

#### kienstra on 2022-11-03T20:04:27Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013348967
Path: packages/components/src/snackbar/types.ts

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode, SyntheticEvent } from 'react';
+
+export type NoticeActionWithURL = {
+	label: string;
+	url: string;
+	onClick?: ( event: SyntheticEvent ) => void;
+};
+
+type NoticeActionWithOnClick = {
+	label: string;
+	url?: string;
+	onClick: ( event: SyntheticEvent ) => void;
+};
+
+export type NoticeAction = NoticeActionWithURL | NoticeActionWithOnClick;
+
+export type Notice = {
+	id: string;
+	spokenMessage: string;
+	actions: NoticeAction[];
+	icon?: ReactNode;
+	onDismiss?: () => void;
+	content: string;
+	isDismissible: boolean;
+	explicitDismiss: boolean;
+};
+
+export type SnackbarProps = {
+	/**
+	 * The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
```

Sounds good, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/70bfe595c8de58542f45de8ac4bb7b90fd31e75a

### Review thread 21 (resolved)

#### ciampo on 2022-11-03T12:20:28Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012826422
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,52 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `children`: `string`
+The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+-   Required: Yes
+
+#### `spokenMessage`: `string`
+Used to provide a custom spoken message in place of the `children` default.
+-   Required: No
+-   Default: `children`
+
+#### `politeness`: `'polite' | 'assertive'`
+A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+-   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+-   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
```

- no need to mention the default value (it's already listed below)
- let's not use bullet points for the prop's description, as it will be joined with the required/default list below

```suggestion
A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.

A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.

A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
```

#### kienstra on 2022-11-03T20:10:25Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013353567
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,52 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `children`: `string`
+The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+-   Required: Yes
+
+#### `spokenMessage`: `string`
+Used to provide a custom spoken message in place of the `children` default.
+-   Required: No
+-   Default: `children`
+
+#### `politeness`: `'polite' | 'assertive'`
+A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
+-   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
+-   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/4f823d39433212a35e9e37498745ae344b5328c4

### Review thread 22 (resolved)

#### ciampo on 2022-11-03T12:55:08Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1012863082
Path: packages/components/CHANGELOG.md

```diff
@@ -56,6 +56,7 @@
 -   `Autocomplete`: use Popover's new `placement` prop instead of legacy `position` prop ([#44396](https://github.com/WordPress/gutenberg/pull/44396/)).
 -   `SelectControl`: Add `onChange`, `onBlur` and `onFocus` to storybook actions ([#45432](https://github.com/WordPress/gutenberg/pull/45432/)).
 -   `FontSizePicker`: Add more comprehensive tests ([#45298](https://github.com/WordPress/gutenberg/pull/45298)).
+-   `Snackbar`: Convert to TypeScript ([#45472](https://github.com/WordPress/gutenberg/pull/45472)).
```

Also, we should rebase this PR and move this CHANGELOG entry under "Unreleased", in the "### Internal" section

#### kienstra on 2022-11-03T20:40:32Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1013377824
Path: packages/components/CHANGELOG.md

```diff
@@ -56,6 +56,7 @@
 -   `Autocomplete`: use Popover's new `placement` prop instead of legacy `position` prop ([#44396](https://github.com/WordPress/gutenberg/pull/44396/)).
 -   `SelectControl`: Add `onChange`, `onBlur` and `onFocus` to storybook actions ([#45432](https://github.com/WordPress/gutenberg/pull/45432/)).
 -   `FontSizePicker`: Add more comprehensive tests ([#45298](https://github.com/WordPress/gutenberg/pull/45298)).
+-   `Snackbar`: Convert to TypeScript ([#45472](https://github.com/WordPress/gutenberg/pull/45472)).
```

Ah, good idea. I just did that.

Is it alright that I didn't do `git rebase`? I just merged in `trunk` and resolved the conflict.

#### ciampo on 2022-11-15T13:50:37Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1022810897
Path: packages/components/CHANGELOG.md

```diff
@@ -56,6 +56,7 @@
 -   `Autocomplete`: use Popover's new `placement` prop instead of legacy `position` prop ([#44396](https://github.com/WordPress/gutenberg/pull/44396/)).
 -   `SelectControl`: Add `onChange`, `onBlur` and `onFocus` to storybook actions ([#45432](https://github.com/WordPress/gutenberg/pull/45432/)).
 -   `FontSizePicker`: Add more comprehensive tests ([#45298](https://github.com/WordPress/gutenberg/pull/45298)).
+-   `Snackbar`: Convert to TypeScript ([#45472](https://github.com/WordPress/gutenberg/pull/45472)).
```

Sure, you can either merge or rebase — your preference.

### Review thread 23 (resolved)

#### ciampo on 2022-11-15T15:44:22Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1022957378
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,72 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `actions`: `Action[]`
```

```suggestion
#### `actions`: `NoticeAction[]`
```

#### kienstra on 2022-11-15T18:38:03Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1023140077
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,72 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `actions`: `Action[]`
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/45472/commits/8c33d6d519f45c40620692312e34020b39ab24ba

### Review thread 24 (resolved)

#### ciampo on 2022-11-15T15:44:36Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1022957665
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,72 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `actions`: `Action[]`
+
+An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+
+-   Required: No
+-   Default: `[]`
+
+#### `children`: `string`
+
+The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+
+-   Required: Yes
+
+#### `explicitDismiss`: `boolean`
+
+Whether to require user action to dismiss the snackbar. By default, this is dismissed on a timeout, without user interaction.
+
+-   Required: No
+-   Default: `false`
+
+#### `icon`: `ReactNode`
+
+The icon to render in the snackbar.
+
+-   Required: No
+-   Default: `null`
+
+#### `listRef`: `MutableRefObject< HTMLDivElement >`
```

```suggestion
#### `listRef`: `MutableRefObject< HTMLDivElement | null >`
```

#### kienstra on 2022-11-15T18:38:22Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1023140332
Path: packages/components/src/snackbar/README.md

```diff
@@ -36,17 +36,72 @@ const MySnackbarNotice = () => (
 );
 ```
 
-#### Props
+### Props
 
 The following props are used to control the display of the component.
 
--   `children`: (string) The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
--   `spokenMessage`: (string) Used to provide a custom spoken message in place of the `children` default.
--   `politeness`: (string) A politeness level for the notice's spoken message. Should be provided as one of the valid options for [an `aria-live` attribute value](https://www.w3.org/TR/wai-aria-1.1/#aria-live). Defaults to `"polite"`. Note that this value should be considered a suggestion; assistive technologies may override it based on internal heuristics.
-    -   A value of `'assertive'` is to be used for important, and usually time-sensitive, information. It will interrupt anything else the screen reader is announcing in that moment.
-    -   A value of `'polite'` is to be used for advisory information. It should not interrupt what the screen reader is announcing in that moment (the "speech queue") or interrupt the current task.
--   `onRemove`: function called when dismissing the notice.
--   `actions`: (array) an array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+#### `actions`: `Action[]`
+
+An array of action objects. Each member object should contain a `label` and either a `url` link string or `onClick` callback function.
+
+-   Required: No
+-   Default: `[]`
+
+#### `children`: `string`
+
+The displayed message of a notice. Also used as the spoken message for assistive technology, unless `spokenMessage` is provided as an alternative message.
+
+-   Required: Yes
+
+#### `explicitDismiss`: `boolean`
+
+Whether to require user action to dismiss the snackbar. By default, this is dismissed on a timeout, without user interaction.
+
+-   Required: No
+-   Default: `false`
+
+#### `icon`: `ReactNode`
+
+The icon to render in the snackbar.
+
+-   Required: No
+-   Default: `null`
+
+#### `listRef`: `MutableRefObject< HTMLDivElement >`
```

Committed in https://github.com/WordPress/gutenberg/pull/45472/commits/fe394e6149e7f63b4259ab5c5fbd145e78ef6b9e

### Review thread 25 (resolved)

#### ciampo on 2022-11-15T15:47:06Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1022960755
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -14,21 +15,20 @@ import warning from '@wordpress/warning';
 /**
  * Internal dependencies
  */
-import { Button } from '../';
+import Button from '../button';
+import type { NoticeAction, SnackbarProps } from './types';
+import type { WordPressComponentProps } from '../ui/context';
 
-const noop = () => {};
 const NOTICE_TIMEOUT = 10000;
 
-/** @typedef {import('@wordpress/element').WPElement} WPElement */
-
 /**
  * Custom hook which announces the message with the given politeness, if a
  * valid message is provided.
- *
- * @param {string|WPElement}     [message]  Message to announce.
- * @param {'polite'|'assertive'} politeness Politeness to announce.
  */
```

It's probably better to leave these JSDocs, we can just remove the types (since they're now defined in TypeScript)

```js
/**
 * Custom hook which announces the message with the given politeness, if a
 * valid message is provided.
 *
 * @param  message    Message to announce.
 * @param  politeness Politeness to announce.
 */
```

#### kienstra on 2022-11-15T18:45:27Z

URL: https://github.com/WordPress/gutenberg/pull/45472#discussion_r1023146358
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -14,21 +15,20 @@ import warning from '@wordpress/warning';
 /**
  * Internal dependencies
  */
-import { Button } from '../';
+import Button from '../button';
+import type { NoticeAction, SnackbarProps } from './types';
+import type { WordPressComponentProps } from '../ui/context';
 
-const noop = () => {};
 const NOTICE_TIMEOUT = 10000;
 
-/** @typedef {import('@wordpress/element').WPElement} WPElement */
-
 /**
  * Custom hook which announces the message with the given politeness, if a
  * valid message is provided.
- *
- * @param {string|WPElement}     [message]  Message to announce.
- * @param {'polite'|'assertive'} politeness Politeness to announce.
  */
```

Sounds good, committed that verbatim: https://github.com/WordPress/gutenberg/pull/45472/commits/b145a587b113ed2381fbb606584e287fe9624794

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/snackbar/README.md (MODIFIED, +63/-8)
- packages/components/src/snackbar/index.tsx (RENAMED, +46/-28)
- packages/components/src/snackbar/list.tsx (RENAMED, +20/-21)
- packages/components/src/snackbar/stories/index.js (DELETED, +0/-89)
- packages/components/src/snackbar/stories/index.tsx (ADDED, +96/-0)
- packages/components/src/snackbar/stories/list.tsx (ADDED, +98/-0)
- packages/components/src/snackbar/types.ts (ADDED, +116/-0)
- packages/components/tsconfig.json (MODIFIED, +0/-1)
