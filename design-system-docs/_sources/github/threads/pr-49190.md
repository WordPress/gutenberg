---
number: 49190
type: pull_request
state: MERGED
merged_at: 2023-05-02T13:21:14Z
url: https://github.com/WordPress/gutenberg/pull/49190
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components","First-time Contributor"]
---

# PR #49190: Refactor/toolbar item component to typescript

- URL: https://github.com/WordPress/gutenberg/pull/49190
- Author: margolisj
- Created: 2023-03-20T04:15:14Z
- Updated: 2023-05-07T23:29:56Z
- Comments: 8 of 8
- Review threads: 6 of 6
- Changed files: 4 of 4
- Diff: ../pr-diffs/49190.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->
Convert the ToolbarItem component to TypeScript.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
This PR is needed as part of an effort to convert ToolbarItem to TypeScript. As mentioned in #35744 .

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
By adding types to ToolbarItem.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a Post or Page. -->
<!-- 2. Insert a Heading Block. -->
<!-- 3. etc. -->
Create or otherwise open a post
Add a block that would have some toolbar controls (e.g. Image)
Expected: Toolbar buttons should fundamentally work the same as before:

## Issue comments

### github-actions on 2023-03-20T04:15:46Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1475600655

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @margolisj! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/), where we hold [regularly weekly meetings](https://make.wordpress.org/core/tag/core-editor-summary/) open to anyone to coordinate with each other.

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### margolisj on 2023-03-21T14:16:46Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1477919249

@mike-day hey I just saw in another thread you were going to start more Toolbar related typings and just wanted to let you know I started this and ToolbarGroup. Sry for the @ couldn't think of a better way to send a message to you.

### mike-day on 2023-03-21T15:38:26Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1478064233

Thanks for the heads up, @margolisj!

### margolisj on 2023-04-18T20:23:21Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1513751267

@mirka I'll be able to respond to your comments and possibly fix them, time permitting, but I also did ToolBarGroup. Would you like me to pull that into this and make it a larger PR or leave this here and open another on top of this?

### mirka on 2023-04-19T15:14:28Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1514916470

> @mirka I'll be able to respond to your comments and possibly fix them, time permitting, but I also did ToolBarGroup. Would you like me to pull that into this and make it a larger PR or leave this here and open another on top of this?

Great, thank you! Let's make it a separate PR for easier review 👍

### margolisj on 2023-04-24T16:30:55Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1520487894

@mirka + @ciampo Looks like there is a unit test using the function based toolbar item line 85 `toolbar/stores/index.tsx`:
```ts
{
		children,
		as: Component,
		...props
}: React.ComponentPropsWithoutRef< typeof BaseToolbarItem >
```
I added a ts-ignore to that. Another issue from that causes as/Component parameter to default to `(parameter) Component: "button" | undefined`.

### margolisj on 2023-04-28T14:45:38Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1527678557

@mirka @ciampo Changelog resolved and rest of comments resolved. let me know if there is anything else. Once this is merged I'll rebased / rewrite the ToolbarGroup to use this instead of the stubs I have.

### github-actions on 2023-05-02T13:21:41Z

URL: https://github.com/WordPress/gutenberg/pull/49190#issuecomment-1531466354

Congratulations on your first merged pull request, @margolisj! We'd like to credit you for your contribution in the post announcing the next WordPress release, but we can't find a WordPress.org profile associated with your GitHub account. When you have a moment, visit the following URL and click "link your GitHub account" under "GitHub Username" to link your accounts:

https://profiles.wordpress.org/me/profile/edit/

And if you don't have a WordPress.org account, you can create one on this page:

https://login.wordpress.org/register

Kudos!

## Review threads

### Review thread 1 (resolved)

#### margolisj on 2023-03-21T14:09:46Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1143444577
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -15,8 +14,18 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import ToolbarContext from '../toolbar-context';
+import type { ToolbarItemProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
 
-function ToolbarItem( { children, as: Component, ...props }, ref ) {
+function ToolbarItem(
+	{
+		children,
+		as,
+		...props
+	}: WordPressComponentProps< ToolbarItemProps, 'div', false >,
```

Possible this should be button.

#### mirka on 2023-04-18T10:58:04Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1169860077
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -15,8 +14,18 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import ToolbarContext from '../toolbar-context';
+import type { ToolbarItemProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
 
-function ToolbarItem( { children, as: Component, ...props }, ref ) {
+function ToolbarItem(
+	{
+		children,
+		as,
+		...props
+	}: WordPressComponentProps< ToolbarItemProps, 'div', false >,
```

Possibly, but we've been pretty loose about the ref types up to this point so I'm fine with `any`.

### Review thread 2 (resolved)

#### margolisj on 2023-03-21T14:12:48Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1143449844
Path: packages/components/src/toolbar/toolbar-item/types.ts

```diff
@@ -0,0 +1,21 @@
+/**
+ * External dependencies
+ */
+import type { ElementType } from 'react';
+
+export type ToolbarItemProps = {
```

We could easily forward the types from reakit/Toolbar/ToolbarItem as well. Props is covered a lot of space here.

#### mirka on 2023-04-18T12:26:11Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1169958508
Path: packages/components/src/toolbar/toolbar-item/types.ts

```diff
@@ -0,0 +1,21 @@
+/**
+ * External dependencies
+ */
+import type { ElementType } from 'react';
+
+export type ToolbarItemProps = {
```

I think so! We can straight up do:

```tsx
function ToolbarItem(
	{
		children,
		as: Component,
		...props
	}: React.ComponentPropsWithoutRef< typeof BaseToolbarItem >,
```

### Review thread 3 (resolved)

#### margolisj on 2023-03-21T14:14:01Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1143452105
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -15,8 +14,18 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import ToolbarContext from '../toolbar-context';
+import type { ToolbarItemProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
 
-function ToolbarItem( { children, as: Component, ...props }, ref ) {
+function ToolbarItem(
+	{
+		children,
+		as,
+		...props
+	}: WordPressComponentProps< ToolbarItemProps, 'div', false >,
+	ref: ForwardedRef< any >
+) {
+	const Component = as;
```

Could easily clean this code up using `<As />`, which I've seen in another project, instead of recasting it to `Component`.

### Review thread 4 (resolved)

#### mirka on 2023-04-18T10:57:10Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1169859120
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -15,8 +14,18 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import ToolbarContext from '../toolbar-context';
+import type { ToolbarItemProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
 
-function ToolbarItem( { children, as: Component, ...props }, ref ) {
+function ToolbarItem(
+	{
+		children,
+		as,
+		...props
+	}: WordPressComponentProps< ToolbarItemProps, 'div', false >,
+	ref: ForwardedRef< any >
+) {
+	const Component = as;
```

Any reason why we'd want this instead of the original, which was renaming it directly in the destructure?

```jsx
// L19 in the original file
function ToolbarItem( { children, as: Component, ...props }, ref ) {
```

#### margolisj on 2023-04-24T15:39:41Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1175473233
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -15,8 +14,18 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import ToolbarContext from '../toolbar-context';
+import type { ToolbarItemProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
 
-function ToolbarItem( { children, as: Component, ...props }, ref ) {
+function ToolbarItem(
+	{
+		children,
+		as,
+		...props
+	}: WordPressComponentProps< ToolbarItemProps, 'div', false >,
+	ref: ForwardedRef< any >
+) {
+	const Component = as;
```

No reason. My logic was doing destructuring with a capitalized name, ex for a class or JSX.Element, always leads me to think its a type declaration, so I try to avoid it. Happy to leave it in the function declaration I personally think it can be confusing.

#### mirka on 2023-04-28T11:03:09Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1180273329
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -15,8 +14,18 @@ import warning from '@wordpress/warning';
  * Internal dependencies
  */
 import ToolbarContext from '../toolbar-context';
+import type { ToolbarItemProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
 
-function ToolbarItem( { children, as: Component, ...props }, ref ) {
+function ToolbarItem(
+	{
+		children,
+		as,
+		...props
+	}: WordPressComponentProps< ToolbarItemProps, 'div', false >,
+	ref: ForwardedRef< any >
+) {
+	const Component = as;
```

Interesting, never thought of that angle! I guess one downside of moving it to a separate `const` is that the `as` variable will continue to be in scope, potentially causing inconsistencies where some lines of code refer to `Component` and others to `as`.

### Review thread 5 (resolved)

#### mirka on 2023-04-18T12:15:37Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1169944834
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -33,14 +42,21 @@ function ToolbarItem( { children, as: Component, ...props }, ref ) {
 		if ( Component ) {
 			return <Component { ...allProps }>{ children }</Component>;
 		}
+		if ( typeof children !== 'function' ) {
+			warning(
+				'`ToolbarItem` is a generic headless component. You must pass either a `children` prop as a function or an `as` prop as a component. ' +
+					'See https://developer.wordpress.org/block-editor/components/toolbar-item/'
+			);
+			return null;
+		}
```

If I'm reading the logic correctly, we'll never end up in this conditional block, so I think we can just drop the `warning()` call.

```suggestion
		if ( typeof children !== 'function' ) {
			return null;
		}
```

(Ideally we'd rewrite the logic to be more straightforward so TypeScript can follow better, but I think this is an ok compromise to reduce runtime changes at the moment.)

### Review thread 6 (resolved)

#### mirka on 2023-04-18T12:27:51Z

URL: https://github.com/WordPress/gutenberg/pull/49190#discussion_r1169960474
Path: packages/components/src/toolbar/toolbar-item/index.tsx

```diff
@@ -33,14 +42,21 @@ function ToolbarItem( { children, as: Component, ...props }, ref ) {
 		if ( Component ) {
 			return <Component { ...allProps }>{ children }</Component>;
 		}
+		if ( typeof children !== 'function' ) {
+			warning(
+				'`ToolbarItem` is a generic headless component. You must pass either a `children` prop as a function or an `as` prop as a component. ' +
+					'See https://developer.wordpress.org/block-editor/components/toolbar-item/'
+			);
+			return null;
+		}
 		return children( allProps );
 	}
 
 	return (
 		<BaseToolbarItem
 			{ ...accessibleToolbarState }
 			{ ...allProps }
-			as={ Component }
+			as={ Component ?? 'button' }
```

This fallback may not be necessary if we use the types from Reakit.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/toolbar/stories/index.tsx (MODIFIED, +26/-24)
- packages/components/src/toolbar/toolbar-button/index.tsx (MODIFIED, +10/-13)
- packages/components/src/toolbar/toolbar-item/index.tsx (RENAMED, +12/-3)
