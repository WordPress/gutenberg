---
number: 57338
type: pull_request
state: MERGED
merged_at: 2023-12-25T11:58:42Z
url: https://github.com/WordPress/gutenberg/pull/57338
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #57338: ToggleGroupControl: Update large button size to 32px

- URL: https://github.com/WordPress/gutenberg/pull/57338
- Author: mirka
- Created: 2023-12-22T13:18:05Z
- Updated: 2023-12-25T11:59:03Z
- Comments: 1 of 1
- Review threads: 3 of 3
- Changed files: 5 of 5
- Diff: ../pr-diffs/57338.diff

## Body

Closes #45532

## What?

Updates the size of the ToggleGroupControl buttons in the large size variant to be 32px (used to be 34px).

Also fixes a bug where icon buttons did not have square proportions when `size = 'default'` && `__next40pxDefaultSize = true`.

## Why?

As [requested](https://github.com/WordPress/gutenberg/pull/45492#pullrequestreview-1166448565), to be more cohesive with the overall sizing scheme.

## Testing Instructions

In the Storybook stories for ToggleGroupControl, see that the button sizes for `size = '__unstable-large'` and `__next40pxDefaultSize = true` are now 32px. Check both the text label buttons and icon buttons.

The overall footprint (height) including the border around the entire button group should remain the same at 40px.

## Screenshots or screencast <!-- if applicable -->

Bug fix for icon buttons where `size = 'default'` && `__next40pxDefaultSize = true`:

| Before | After |
|--------|-------|
|<img src="https://github.com/WordPress/gutenberg/assets/555336/b06745ba-67c2-4ab3-a76d-3d93468e1ca8" alt="Button doesn't have square proportions" width="104">|<img src="https://github.com/WordPress/gutenberg/assets/555336/7c21242d-3689-4397-bb46-77aafb0bf0f9" alt="Button has square proportions" width="104">|

## Issue comments

### github-actions on 2023-12-22T14:46:51Z

URL: https://github.com/WordPress/gutenberg/pull/57338#issuecomment-1867771951

<!-- flaky-tests-report-comment -->
**Flaky tests detected in a80319295108ec15e09d2fa1525008e3ec35ff05.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/7321454753
📝  Reported issues:
- #40369 in `specs/editor/various/block-switcher.test.js`

## Review threads

### Review thread 1

#### mirka on 2023-12-22T13:24:54Z

URL: https://github.com/WordPress/gutenberg/pull/57338#discussion_r1435054547
Path: packages/components/src/toggle-group-control/toggle-group-control/component.tsx

```diff
@@ -47,6 +47,8 @@ function UnconnectedToggleGroupControl(
 	} = useContextSystem( props, 'ToggleGroupControl' );
 
 	const baseId = useInstanceId( ToggleGroupControl, 'toggle-group-control' );
+	const normalizedSize =
+		__next40pxDefaultSize && size === 'default' ? '__unstable-large' : size;
```

We usually don't do this kind of preemptive size normalization in other components, but in this specific case it was a lot cleaner this way. Otherwise we'd have to drill the prop down to multiple subcomponents, touching a handful of more files.

#### ciampo on 2023-12-22T16:02:16Z

URL: https://github.com/WordPress/gutenberg/pull/57338#discussion_r1435185881
Path: packages/components/src/toggle-group-control/toggle-group-control/component.tsx

```diff
@@ -47,6 +47,8 @@ function UnconnectedToggleGroupControl(
 	} = useContextSystem( props, 'ToggleGroupControl' );
 
 	const baseId = useInstanceId( ToggleGroupControl, 'toggle-group-control' );
+	const normalizedSize =
+		__next40pxDefaultSize && size === 'default' ? '__unstable-large' : size;
```

Agreed. We should be able to clean this up after the migration is over and we remove temporary props and `size` values

### Review thread 2

#### mirka on 2023-12-22T13:30:58Z

URL: https://github.com/WordPress/gutenberg/pull/57338#discussion_r1435058779
Path: packages/components/src/toggle-group-control/toggle-group-control/styles.ts

```diff
@@ -57,21 +52,20 @@ const enclosingBorders = ( isBlock: ToggleGroupControlProps[ 'isBlock' ] ) => {
 };
 
 export const toggleGroupControlSize = (
-	size: NonNullable< ToggleGroupControlProps[ 'size' ] >,
-	__next40pxDefaultSize: ToggleGroupControlProps[ '__next40pxDefaultSize' ]
+	size: NonNullable< ToggleGroupControlProps[ 'size' ] >
 ) => {
-	const heights = {
-		default: '40px',
-		'__unstable-large': '40px',
+	const styles = {
+		default: css`
+			min-height: 36px;
+			padding: 2px;
+		`,
+		'__unstable-large': css`
+			min-height: 40px;
+			padding: 3px;
+		`,
```

I am tempted to simplify this down to a

```js
if ( size === 'default` ) {
  return css`/* ... */`;
}
return css`/* ... */`;
```
but I'm going to keep this size variant object around because I think we'll be asked to make a 32px compact size variant soon.

### Review thread 3 (resolved)

#### ciampo on 2023-12-22T16:01:39Z

URL: https://github.com/WordPress/gutenberg/pull/57338#discussion_r1435185509
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/styles.ts

```diff
@@ -102,12 +102,13 @@ const isIconStyles = ( {
 }: Pick< ToggleGroupControlProps, 'size' > ) => {
 	const iconButtonSizes = {
 		default: '30px',
-		'__unstable-large': '34px',
+		'__unstable-large': '32px',
 	};
 
 	return css`
 		color: ${ COLORS.gray[ 900 ] };
 		width: ${ iconButtonSizes[ size ] };
+		height: ${ iconButtonSizes[ size ] };
```

More than a FYI, but I've recently discovered and I quite like it as a way to more clearly state that an element should be a square

```suggestion
		height: ${ iconButtonSizes[ size ] };
		aspect-ratio: 1;
```

#### mirka on 2023-12-25T11:35:00Z

URL: https://github.com/WordPress/gutenberg/pull/57338#discussion_r1436067157
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/styles.ts

```diff
@@ -102,12 +102,13 @@ const isIconStyles = ( {
 }: Pick< ToggleGroupControlProps, 'size' > ) => {
 	const iconButtonSizes = {
 		default: '30px',
-		'__unstable-large': '34px',
+		'__unstable-large': '32px',
 	};
 
 	return css`
 		color: ${ COLORS.gray[ 900 ] };
 		width: ${ iconButtonSizes[ size ] };
+		height: ${ iconButtonSizes[ size ] };
```

Ooh, I haven't had a chance to use this yet! Great suggestion 🙏

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/toggle-group-control/test/__snapshots__/index.tsx.snap (MODIFIED, +8/-4)
- packages/components/src/toggle-group-control/toggle-group-control-option-base/styles.ts (MODIFIED, +3/-2)
- packages/components/src/toggle-group-control/toggle-group-control/component.tsx (MODIFIED, +5/-4)
- packages/components/src/toggle-group-control/toggle-group-control/styles.ts (MODIFIED, +13/-19)
