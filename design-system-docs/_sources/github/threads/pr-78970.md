---
number: 78970
type: pull_request
state: OPEN
url: https://github.com/WordPress/gutenberg/pull/78970
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","[Package] Block library","[Block] RSS"]
---

# PR #78970: RSS Block: Add support for displaying featured images from feed items

- URL: https://github.com/WordPress/gutenberg/pull/78970
- Author: dhruvikpatel18
- Created: 2026-06-05T05:53:23Z
- Updated: 2026-06-22T11:39:04Z
- Comments: 1 of 1
- Review threads: 5 of 5
- Changed files: 9 of 9
- Diff: ../pr-diffs/78970.diff

## Body

Closes #42827

## What?
This PR adds a new **Display featured image** toggle to the RSS block's inspector controls, allowing images from RSS/Atom feed items to appear alongside each entry in the block output.

## Why?
The RSS block currently renders only the title, date, author, and excerpt for each feed item, there is no way to display accompanying images. Users reported that images from incoming RSS feeds were silently dropped, with no option to surface them. This is a commonly requested feature and a natural complement to the existing display toggles (`displayDate`, `displayAuthor`, `displayExcerpt`).

## Testing Instructions
1. Open a post or page in the block editor.
2. Insert an **RSS** block.
3. Enter a valid RSS feed URL that includes images (e.g. a feed from a news site, a YouTube channel feed, or any feed using `<enclosure>` or `<media:thumbnail>`). Apply the URL.
4. In the **Settings** sidebar, locate the **Display featured image** toggle and enable it.
5. Verify that images appear above each feed item's title in the block preview.
6. Disable the toggle and verify images disappear.
7. Switch the block layout to **Grid view** and verify images are responsive within grid columns.
8. Save the post and view it on the frontend, confirm images render correctly there too.

## Screenshots or screencast <!-- if applicable -->
https://github.com/user-attachments/assets/1e440720-6808-4a37-908d-ca48ef055a76

## Issue comments

### github-actions on 2026-06-05T09:52:35Z

URL: https://github.com/WordPress/gutenberg/pull/78970#issuecomment-4630261141

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @msjudyh.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: msjudyh.

Co-authored-by: dhruvikpatel18 <dhruvik18@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### ciampo on 2026-06-08T17:10:46Z

URL: https://github.com/WordPress/gutenberg/pull/78970#discussion_r3375041937
Path: packages/block-library/src/rss/index.php

```diff
@@ -104,7 +104,45 @@ function render_block_core_rss( $attributes ) {
 			$excerpt = '<div class="wp-block-rss__item-excerpt">' . esc_html( $excerpt ) . '</div>';
 		}
 
-		$list_items .= "<li class='wp-block-rss__item'>{$title}{$date_markup}{$author}{$excerpt}</li>";
+		// Build featured image markup if the attribute is enabled.
+		$featured_image = '';
+		if ( ! empty( $attributes['displayFeaturedImage'] ) ) {
+			$image_url = '';
+
+			$enclosure = $item->get_enclosure();
+			if ( $enclosure ) {
+				$mime_type = (string) $enclosure->get_type();
+				$medium    = (string) $enclosure->get_medium();
+				if ( 'image' === $medium || str_starts_with( $mime_type, 'image/' ) ) {
+					$image_url = $enclosure->get_link();
+				}
+			}
+
+			if ( empty( $image_url ) ) {
+				$thumbnail = $item->get_thumbnail();
+				if ( ! empty( $thumbnail ) ) {
+					$image_url = $thumbnail;
```

It seems like `get_thumbnail()` a few lines above returns the `media:thumbnail` attributes as an array (e.g. `[ 'url' => '…', 'width' => …, 'height' => … ]`), or `null`.

This means that the later `esc_url( $image_url )` is passed an array. This breaks the `media:thumbnail` path entirely (and emits a PHP warning).

This should probably be

```php
$thumbnail = $item->get_thumbnail();
if ( ! empty( $thumbnail['url'] ) ) {
    $image_url = $thumbnail['url'];
}
```

### Review thread 2

#### ciampo on 2026-06-08T17:21:12Z

URL: https://github.com/WordPress/gutenberg/pull/78970#discussion_r3375106010
Path: packages/block-library/src/rss/index.php

```diff
@@ -16,7 +16,7 @@
  */
 function render_block_core_rss( $attributes ) {
 	if ( in_array( untrailingslashit( $attributes['feedURL'] ), array( site_url(), home_url() ), true ) ) {
-		return '<div class="components-placeholder"><div class="notice notice-error">' . __( 'Adding an RSS feed to this site’s homepage is not supported, as it could lead to a loop that slows down your site. Try using another block, like the <strong>Latest Posts</strong> block, to list posts from the site.' ) . '</div></div>';
+		return '<div class="components-placeholder"><div class="notice notice-error">' . __( 'Adding an RSS feed to this site\'s homepage is not supported, as it could lead to a loop that slows down your site. Try using another block, like the <strong>Latest Posts</strong> block, to list posts from the site.' ) . '</div></div>';
```

This change also feels unreleated, should we undo it?

#### dhruvikpatel18 on 2026-06-22T11:14:52Z

URL: https://github.com/WordPress/gutenberg/pull/78970#discussion_r3451762094
Path: packages/block-library/src/rss/index.php

```diff
@@ -16,7 +16,7 @@
  */
 function render_block_core_rss( $attributes ) {
 	if ( in_array( untrailingslashit( $attributes['feedURL'] ), array( site_url(), home_url() ), true ) ) {
-		return '<div class="components-placeholder"><div class="notice notice-error">' . __( 'Adding an RSS feed to this site’s homepage is not supported, as it could lead to a loop that slows down your site. Try using another block, like the <strong>Latest Posts</strong> block, to list posts from the site.' ) . '</div></div>';
+		return '<div class="components-placeholder"><div class="notice notice-error">' . __( 'Adding an RSS feed to this site\'s homepage is not supported, as it could lead to a loop that slows down your site. Try using another block, like the <strong>Latest Posts</strong> block, to list posts from the site.' ) . '</div></div>';
```

The original line used an unescaped single quote inside a single-quoted string ('site's homepage'), which causes a PHP parse error:
`Parse error: syntax error, unexpected identifier "s", expecting ")" in rss.php on line 19`

### Review thread 3

#### ciampo on 2026-06-08T17:22:05Z

URL: https://github.com/WordPress/gutenberg/pull/78970#discussion_r3375110550
Path: packages/block-library/src/rss/edit.js

```diff
@@ -180,6 +182,24 @@ export default function RSSEdit( { attributes, setAttributes, name } ) {
 						/>
 					</ToolsPanelItem>
 
+					<ToolsPanelItem
+						label={ __( 'Display featured image' ) }
+						hasValue={ () => !! displayFeaturedImage }
+						onDeselect={ () =>
+							setAttributes( { displayFeaturedImage: false } )
+						}
+						isShownByDefault
+					>
+						<ToggleControl
+							__next40pxDefaultSize
```

`__next40pxDefaultSize` is not a valid prop on `ToggleControl`, we should remove it in this instance and throughout the file

### Review thread 4

#### ciampo on 2026-06-08T17:26:15Z

URL: https://github.com/WordPress/gutenberg/pull/78970#discussion_r3375133181
Path: packages/block-library/src/rss/index.php

```diff
@@ -104,7 +104,45 @@ function render_block_core_rss( $attributes ) {
 			$excerpt = '<div class="wp-block-rss__item-excerpt">' . esc_html( $excerpt ) . '</div>';
 		}
 
-		$list_items .= "<li class='wp-block-rss__item'>{$title}{$date_markup}{$author}{$excerpt}</li>";
+		// Build featured image markup if the attribute is enabled.
+		$featured_image = '';
+		if ( ! empty( $attributes['displayFeaturedImage'] ) ) {
+			$image_url = '';
+
+			$enclosure = $item->get_enclosure();
+			if ( $enclosure ) {
+				$mime_type = (string) $enclosure->get_type();
+				$medium    = (string) $enclosure->get_medium();
+				if ( 'image' === $medium || str_starts_with( $mime_type, 'image/' ) ) {
+					$image_url = $enclosure->get_link();
+				}
+			}
+
+			if ( empty( $image_url ) ) {
+				$thumbnail = $item->get_thumbnail();
+				if ( ! empty( $thumbnail ) ) {
+					$image_url = $thumbnail;
+				}
+			}
+
+			if ( empty( $image_url ) ) {
+				$content = $item->get_content();
+				if ( ! empty( $content ) ) {
+					if ( preg_match( '/<img[^>]+src=[\'"]([^\'"]+)[\'"][^>]*>/i', $content, $matches ) ) {
+						$image_url = $matches[1];
+					}
+				}
+			}
+
+			if ( ! empty( $image_url ) ) {
+				$featured_image = sprintf(
+					'<div class="wp-block-rss__item-featured-image"><img src="%s" alt="" /></div>',
```

The featured image should hold meaningful content (ie. not decorative) and, as such, it should not have an empty`alt` attribute. A good candidate to populate `alt` is the image's title (captured as plain text)

### Review thread 5

#### ciampo on 2026-06-08T17:28:53Z

URL: https://github.com/WordPress/gutenberg/pull/78970#discussion_r3375148473
Path: packages/block-library/src/rss/index.php

```diff
@@ -104,7 +104,45 @@ function render_block_core_rss( $attributes ) {
 			$excerpt = '<div class="wp-block-rss__item-excerpt">' . esc_html( $excerpt ) . '</div>';
 		}
 
-		$list_items .= "<li class='wp-block-rss__item'>{$title}{$date_markup}{$author}{$excerpt}</li>";
+		// Build featured image markup if the attribute is enabled.
+		$featured_image = '';
+		if ( ! empty( $attributes['displayFeaturedImage'] ) ) {
+			$image_url = '';
+
+			$enclosure = $item->get_enclosure();
+			if ( $enclosure ) {
+				$mime_type = (string) $enclosure->get_type();
+				$medium    = (string) $enclosure->get_medium();
+				if ( 'image' === $medium || str_starts_with( $mime_type, 'image/' ) ) {
+					$image_url = $enclosure->get_link();
+				}
+			}
+
+			if ( empty( $image_url ) ) {
+				$thumbnail = $item->get_thumbnail();
+				if ( ! empty( $thumbnail ) ) {
+					$image_url = $thumbnail;
+				}
+			}
+
+			if ( empty( $image_url ) ) {
+				$content = $item->get_content();
+				if ( ! empty( $content ) ) {
+					if ( preg_match( '/<img[^>]+src=[\'"]([^\'"]+)[\'"][^>]*>/i', $content, $matches ) ) {
+						$image_url = $matches[1];
+					}
```

An AI-assisted review flagged that this regex can match tracking pixels, which are often the first embedded image in the content — which means that the tracking pixel would be flagged as the featured image.

We could improve this fallback behaviour by excluding images that are obviously tracking pixels (ag 1x1 size), but it would definitely need some real-world testing against real feeds

## Changed files

- packages/block-library/src/rss/block.json (MODIFIED, +4/-0)
- packages/block-library/src/rss/edit.js (MODIFIED, +19/-0)
- packages/block-library/src/rss/index.php (MODIFIED, +68/-2)
- packages/block-library/src/rss/style.scss (MODIFIED, +12/-0)
- packages/components/src/form-file-upload/README.md (MODIFIED, +1/-1)
- packages/components/src/menu/README.md (MODIFIED, +7/-7)
- packages/components/src/tabs/README.md (MODIFIED, +1/-1)
- packages/components/src/tree-select/README.md (MODIFIED, +1/-1)
- test/integration/fixtures/blocks/core__rss.json (MODIFIED, +2/-1)
