---
number: 48779
type: pull_request
state: MERGED
merged_at: 2023-03-14T14:07:22Z
url: https://github.com/WordPress/gutenberg/pull/48779
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #48779: `QueryControls`: refactor away from `lodash.groupBy`

- URL: https://github.com/WordPress/gutenberg/pull/48779
- Author: flootr
- Created: 2023-03-06T12:30:19Z
- Updated: 2023-03-21T10:46:23Z
- Comments: 2 of 2
- Review threads: 4 of 4
- Changed files: 2 of 2
- Diff: ../pr-diffs/48779.diff

## Body

## What?

This PR removes Lodash's `_.groupBy()` from the `buildTermsTree` function used in `QueryControls`

## Why?

Lodash is known to unnecessarily inflate the bundle size of packages, and in most cases, it can be replaced with native language functionality. See these for more information and rationale:

* https://github.com/WordPress/gutenberg/issues/16938#issuecomment-602837246
* https://github.com/WordPress/gutenberg/issues/17025
* https://github.com/WordPress/gutenberg/issues/39495 

## How?

Replaces `_.groupBy` with a native `.reduce` function.

## Testing Instructions

1. Verify unit tests still pass
2. Verify stories for `QueryControls` still work as expected

## Issue comments

### github-actions on 2023-03-13T10:38:54Z

URL: https://github.com/WordPress/gutenberg/pull/48779#issuecomment-1465895759

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 9f9e3c83df58cbfb1e289759e47ac59cbd64742a.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/4403573166
📝  Reported issues:
- #42139 in `/test/e2e/specs/site-editor/template-part.spec.js`

### tyxla on 2023-03-21T10:46:22Z

URL: https://github.com/WordPress/gutenberg/pull/48779#issuecomment-1477618281

I've just opened a very similar PR in the editor package: #49224.

Those should likely come from the same place, but it was like that before, so this is definitely work for another PR.

## Review threads

### Review thread 1

#### tyxla on 2023-03-07T10:32:33Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1127659928
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

I wonder if we could just inline `ensureParents()`. For me it was easier to read this line:

```
terms.every( ( term ) => term.parent !== null )
```

than having to read what `ensureParents()` does. It's not immediately clear what the function does too - does it add a parent to each term? Or does it filter only terms with parents? I wouldn't expect from a function with this name to return `true` only when ALL passed terms have parents. Inlining the condition will solve this mystery IMHO.

#### flootr on 2023-03-07T10:40:18Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1127669027
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

Inlining didn't work for me because TypeScript doesn't infer that `parent` isn't `null` anymore. That's why I created a type guard. That's the important part:

> `terms is ( TermWithParentAndChildren & { parent: number } )[]` (L13)

#### tyxla on 2023-03-07T10:43:59Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1127672969
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

I see. Let's think of a name that matches the purpose better then.

#### ciampo on 2023-03-07T16:14:21Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1128131829
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

I have an idea — what if we tweaked the way the `termsByParent` dictionary is created, by literally using the `'null'` string as a fallback key when the `parent` is actually `null` ?

<details>
<summary>Something like this</summary>

```diff
diff --git a/packages/components/src/query-controls/terms.ts b/packages/components/src/query-controls/terms.ts
index 951ca626be..9f5688fc91 100644
--- a/packages/components/src/query-controls/terms.ts
+++ b/packages/components/src/query-controls/terms.ts
@@ -8,11 +8,6 @@ import type {
 	TermsByParent,
 } from './types';
 
-const ensureParents = (
-	terms: TermWithParentAndChildren[]
-): terms is ( TermWithParentAndChildren & { parent: number } )[] => {
-	return terms.every( ( term ) => term.parent !== null );
-};
 /**
  * Returns terms in a tree form.
  *
@@ -29,22 +24,23 @@ export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 			id: String( term.id ),
 		} ) );
 
-	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
-		return flatTermsWithParentAndChildren;
-	}
-
 	const termsByParent = flatTermsWithParentAndChildren.reduce(
 		( acc: TermsByParent, term ) => {
 			const { parent } = term;
-			if ( ! acc[ parent ] ) {
-				acc[ parent ] = [];
+			const parentKey = parent ?? 'null';
+			if ( ! acc[ parentKey ] ) {
+				acc[ parentKey ] = [];
 			}
-			acc[ parent ].push( term );
+			acc[ parentKey ].push( term );
 			return acc;
 		},
 		{}
 	);
 
+	if ( termsByParent.null && termsByParent.null.length ) {
+		return flatTermsWithParentAndChildren;
+	}
+
 	const fillWithChildren = (
 		terms: TermWithParentAndChildren[]
 	): TermWithParentAndChildren[] => {

```
</details>

We could even decide to use another string instead of `'null'` — there shouldn't be any risk of conflict with other dictionary keys, since`parent` should be a `number`

#### flootr on 2023-03-07T16:50:58Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1128207232
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

I think that's essentially how it was solved before. I'll have another look.

#### ciampo on 2023-03-07T18:39:01Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1128379684
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

> I think that's essentially how it was solved before.

Yup, it looks like it. We could keep the same logic, but make it more explicit with a better key name than `'null'` ? It could be a quick way to refactor this PR while introducing the least amount of runtime changes possible.

#### flootr on 2023-03-08T07:57:26Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1129079899
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

I do think that's a good use case for `.every` (or `.some`) and drop out early before constructing the object and then throwing it away (in case). I'll have another look and see what I come up with.

#### ciampo on 2023-03-08T11:25:11Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1129286017
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

Sounds good. It looks like the `buildTermsTree` already has some unit tests, so we should have good confidence in applying the changes you suggested!

#### flootr on 2023-03-13T09:54:42Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1133681534
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

In https://github.com/WordPress/gutenberg/pull/48779/commits/a3745b0f8ce3d082cb511bfd832676110518f480 I tried to find a better name for the type guard and added a comment about why we're using it. I think that should address the original concern. Please let me know if there's any other feedback!

#### tyxla on 2023-03-13T11:14:08Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1133775018
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
 
-	const termsByParent: TermsByParent = groupBy(
-		flatTermsWithParentAndChildren,
-		'parent'
-	);
-	if ( termsByParent.null && termsByParent.null.length ) {
+	if ( ! ensureParents( flatTermsWithParentAndChildren ) ) {
```

I'd name the guard function `areAllParentsDefined` or `allTermsHaveParents` but then I'd leave it to your judgment.

### Review thread 2

#### tyxla on 2023-03-07T10:32:59Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1127660445
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -22,22 +22,29 @@ import type {
  */
 export function buildTermsTree( flatTerms: readonly ( Author | Category )[] ) {
 	const flatTermsWithParentAndChildren: TermWithParentAndChildren[] =
-		flatTerms.map( ( term ) => {
-			return {
-				children: [],
-				parent: null,
-				...term,
-				id: String( term.id ),
-			};
-		} );
+		flatTerms.map( ( term ) => ( {
+			children: [],
+			parent: null,
+			...term,
+			id: String( term.id ),
+		} ) );
```

The shorter version wins 👍

### Review thread 3 (resolved)

#### tyxla on 2023-03-07T10:35:23Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1127663296
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -1,8 +1,3 @@
-/**
- * External dependencies
- */
-import { groupBy } from 'lodash';
```

I'd suggest adding a CHANGELOG entry.

#### flootr on 2023-03-07T10:40:40Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1127669400
Path: packages/components/src/query-controls/terms.ts

```diff
@@ -1,8 +1,3 @@
-/**
- * External dependencies
- */
-import { groupBy } from 'lodash';
```

Agreed 👍🏻 

Update: https://github.com/WordPress/gutenberg/pull/48779/commits/9f9e3c83df58cbfb1e289759e47ac59cbd64742a

### Review thread 4

#### tyxla on 2023-03-13T11:12:12Z

URL: https://github.com/WordPress/gutenberg/pull/48779#discussion_r1133773076
Path: packages/components/CHANGELOG.md

```diff
@@ -23,6 +23,7 @@
 -   `navigateRegions` HOC: Convert to TypeScript ([#48632](https://github.com/WordPress/gutenberg/pull/48632)).
 -   `withSpokenMessages`: HOC: Convert to TypeScript ([#48163](https://github.com/WordPress/gutenberg/pull/48163)).
 -   `DimensionControl(Experimental)`: Convert to TypeScript ([#47351](https://github.com/WordPress/gutenberg/pull/47351)).
+-   `QueryControls`: Refactor away from Lodash (`.groupBy`) ([#48779](https://github.com/WordPress/gutenberg/pull/48779))
```

Nit:
```suggestion
-   `QueryControls`: Refactor away from Lodash (`.groupBy`) ([#48779](https://github.com/WordPress/gutenberg/pull/48779)).
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/query-controls/terms.ts (MODIFIED, +29/-18)
