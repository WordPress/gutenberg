---
number: 17297
type: pull_request
state: MERGED
merged_at: 2019-09-04T04:22:18Z
url: https://github.com/WordPress/gutenberg/pull/17297
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Focus] Accessibility (a11y)","[Feature] UI Components","[Package] Components"]
---

# PR #17297: Fix modal escape key propagation

- URL: https://github.com/WordPress/gutenberg/pull/17297
- Author: talldan
- Created: 2019-09-02T10:00:24Z
- Updated: 2019-09-04T05:08:10Z
- Comments: 2 of 2
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/17297.diff

## Body

## Description
`Modal` components handle an `escape` key down as a global event. Unfortunately this means that the modal cannot stop propagation of an event to the outer context. It can only cancel bubbling to an inner context.

With our current implementation of modals, this hasn't really been an issue. On https://github.com/WordPress/gutenberg/pull/17265 I'm implementing a modal that's launched from a block toolbar, and pressing `esc` to close the modal is triggering navigation mode.

This change in this PR is to stop using a global event and instead use an event on the `IsolatedEventContainer` component. This matches how the `Popover` component is implemented:
https://github.com/WordPress/gutenberg/blob/f198997e2c8e377423beb230ce5283914076d396/packages/components/src/popover/index.js#L390-L399

https://github.com/WordPress/gutenberg/blob/f198997e2c8e377423beb230ce5283914076d396/packages/components/src/popover/index.js#L299-L311

This PR also moves the `IsolatedEventContainer` component into `ModalFrame`. This component is responsible for rendering the overlay behind the modal. Nesting it inside `withFocusReturn` ensures that when the overlay is clicked, focus is returned to the element that opened the modal.

## How has this been tested?
Test that modals:
- Close when pressing escape. Focus should be returned to the element that opened the modal (or as close to the element as possible).
- Close clicking the close button. Focus should be returned to the element that opened the modal (or as close to the element as possible).
- Close when clicking outside. Focus should be returned to the element that opened the modal (or as close to the element as possible).

<!-- Please describe in detail how you tested your changes. -->
<!-- Include details of your testing environment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->

## Screenshots <!-- if applicable -->

## Types of changes
<!-- What types of changes does your code introduce?  -->
Bug fix (non-breaking change which fixes an issue)
<!-- New feature (non-breaking change which adds functionality) -->
<!-- Breaking change (fix or feature that would cause existing functionality to not work as expected) -->

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->

## Issue comments

### gziolo on 2019-09-03T06:44:21Z

URL: https://github.com/WordPress/gutenberg/pull/17297#issuecomment-527326851

Noting that I have a similar issue in https://github.com/WordPress/gutenberg/pull/16964.

### gziolo on 2019-09-03T20:21:03Z

URL: https://github.com/WordPress/gutenberg/pull/17297#issuecomment-527623556

Re-tested again. I can confirm it all works as advertised. Excellent job @talldan 🎉

## Review threads

### Review thread 1

#### talldan on 2019-09-02T10:04:15Z

URL: https://github.com/WordPress/gutenberg/pull/17297#discussion_r319891823
Path: packages/components/src/modal/index.js

```diff
@@ -101,6 +106,43 @@ class Modal extends Component {
 		ariaHelper.showApp();
 	}
 
+	/**
+	 * Callback function called when clicked outside the modal.
+	 *
+	 * @param {Object} event Mouse click event.
+	 */
+	handleFocusOutside( event ) {
+		if ( this.props.shouldCloseOnClickOutside ) {
+			this.onRequestClose( event );
+		}
+	}
+
+	/**
+	 * Calls the onRequestClose callback props when it is available.
+	 *
+	 * @param {Object} event Event object.
+	 */
+	onRequestClose( event ) {
+		const { onRequestClose } = this.props;
+		if ( onRequestClose ) {
+			onRequestClose( event );
+		}
+	}
+
+	/**
+	 * Stops proagation of the escape key outside the context of the modal.
```

Fix typo and describe that it triggers the `onRequestClose` prop.

### Review thread 2 (resolved)

#### gziolo on 2019-09-03T20:07:47Z

URL: https://github.com/WordPress/gutenberg/pull/17297#discussion_r320455302
Path: packages/components/src/modal/index.js

```diff
@@ -108,8 +101,6 @@ class Modal extends Component {
 	 */
 	render() {
 		const {
-			overlayClassName,
-			className,
 			onRequestClose,
```

In the README file we keep those two props documented:
- `overlayClassName`
- `className`
See https://github.com/WordPress/gutenberg/tree/d10fa3862f79e11206569b379909e6bbac124fb8/packages/components/src/modal#props.

Should we keep those two props explicitly listed here for documentation purposes? I guess we use `otherProps` to pass other props as well. Example: `role`. What do you think?

#### talldan on 2019-09-04T03:39:10Z

URL: https://github.com/WordPress/gutenberg/pull/17297#discussion_r320560594
Path: packages/components/src/modal/index.js

```diff
@@ -108,8 +101,6 @@ class Modal extends Component {
 	 */
 	render() {
 		const {
-			overlayClassName,
-			className,
 			onRequestClose,
```

Good call, I'll make that more explicit.

#### talldan on 2019-09-04T03:55:02Z

URL: https://github.com/WordPress/gutenberg/pull/17297#discussion_r320562924
Path: packages/components/src/modal/index.js

```diff
@@ -108,8 +101,6 @@ class Modal extends Component {
 	 */
 	render() {
 		const {
-			overlayClassName,
-			className,
 			onRequestClose,
```

On closer investigation it looks like there are lots of props that are handled by `ModalFrame`, some like `shouldCloseOnEsc` are only used in the component's methods. I've added a comment to `Modal` to explain that they're passed through to `ModalFrame` rather than explicitly destructuring them.

#### gziolo on 2019-09-04T05:08:10Z

URL: https://github.com/WordPress/gutenberg/pull/17297#discussion_r320574257
Path: packages/components/src/modal/index.js

```diff
@@ -108,8 +101,6 @@ class Modal extends Component {
 	 */
 	render() {
 		const {
-			overlayClassName,
-			className,
 			onRequestClose,
```

I was afraid that it is the case already. It's fine as is. Fortunately, we don't auto-generate docs for components 😅

## Changed files

- packages/components/src/modal/frame.js (MODIFIED, +30/-17)
- packages/components/src/modal/index.js (MODIFIED, +21/-36)
