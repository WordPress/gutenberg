---
number: 79153
type: pull_request
state: MERGED
merged_at: 2026-06-16T08:50:54Z
url: https://github.com/WordPress/gutenberg/pull/79153
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Enhancement","[Package] Theme"]
---

# PR #79153: Theme: forward ThemeProvider cornerRadius preset to :root for root providers

- URL: https://github.com/WordPress/gutenberg/pull/79153
- Author: ciampo
- Created: 2026-06-12T14:04:14Z
- Updated: 2026-06-22T13:27:35Z
- Comments: 5 of 5
- Review threads: 3 of 3
- Changed files: 4 of 4
- Diff: ../pr-diffs/79153.diff

## Body

Follow-up to #78816 ([review thread](https://github.com/WordPress/gutenberg/pull/78816#discussion_r3381983801)).

## What

A **root** `ThemeProvider` (`isRoot`) now forwards its `cornerRadius` preset to the document element, so `--wpds-border-radius-*` applies to `<html>` — not just the provider's scoping `<div>`. This matches how `color` and `cursor` already behave.

## Why

`color`/`cursor` from a root provider already cascade to `:root`; `cornerRadius` was the odd one out. Mixed pages (React UI + PHP-rendered admin HTML) consume the tokens outside the React root, so the radius preset should reach `<html>` too.

Only `isRoot` providers are affected — non-root nested providers still scope their preset locally.

## How

Each `corner-radius-*` mode in `terrazzo.config.ts` gains a `:root:has([data-wpds-root-provider="true"][data-wpds-corner-radius="…"])` selector (plus regenerated CSS + CHANGELOG).

<details>
<summary>Test plan</summary>

- [ ] In Storybook (Design System → Theme → Theme Provider), render a root `ThemeProvider` and switch the preset; confirm `--wpds-border-radius-*` updates on `:root` in DevTools.
- [ ] Confirm a non-root provider still scopes its preset locally and does not affect `:root`.
- [ ] Confirm `color`/`cursor` root forwarding still works and the default (`subtle`) preset matches existing appearance.

</details>

<details>
<summary>Note on the generated CSS</summary>

`src/prebuilt/css/design-tokens.css` is regenerated by `npm run build` (in `packages/theme`); running the full build produces this exact diff with no other changes.

</details>

## Use of AI

Made with Cursor.

## Issue comments

### github-actions on 2026-06-12T14:17:56Z

URL: https://github.com/WordPress/gutenberg/pull/79153#issuecomment-4692105671

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-12T14:56:50Z

URL: https://github.com/WordPress/gutenberg/pull/79153#issuecomment-4692420752

**Size Change:** +38 B (0%) 

**Total Size:** 8.58 MB

<details open><summary>📦 <strong>View Changed</strong></summary>

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/modules/boot/index.min.js` | 52.9 kB | +38 B (+0.07%) |

</details>

<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-15T16:33:57Z

URL: https://github.com/WordPress/gutenberg/pull/79153#issuecomment-4710100745

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 708164e756db176c6999c228cddf5a95434e0f13.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27604014052
📝  Reported issues:
- #75767 in `/test/e2e/specs/interactivity/router-regions.spec.ts`

### aduth on 2026-06-15T19:05:32Z

URL: https://github.com/WordPress/gutenberg/pull/79153#issuecomment-4711392317

A couple clarifying questions on the difference in implementation between radius & color/cursor:

- Does this mean that someone _must_ load `design-tokens.css` for corner radius customization to take effect? My understanding is that up until now it's been somewhat optional to load that stylesheet, with how color and cursor styles are implemented and build upon the defaults embedded through the CSS fallback values.
- I'm not sure I fully follow _why_ they're different; i.e. why couldn't `cornerRadius` be implemented through ThemeProvider inline styles, or vice-versa why cursor shouldn't be implemented through `design-tokens.css` modifier selectors. Should we _want_ them to be distinctly implemented?

### ciampo on 2026-06-16T08:14:37Z

URL: https://github.com/WordPress/gutenberg/pull/79153#issuecomment-4716380308

> * Does this mean that someone _must_ load `design-tokens.css` for corner radius customization to take effect? My understanding is that up until now it's been somewhat optional to load that stylesheet, with how color and cursor styles are implemented and build upon the defaults embedded through the CSS fallback values.

Correct, it is mandatory.

Although, from my point of view, that was always the expectation — `ThemeProvider` only writes the tokens whose value changes dynamically based on its props, the full list of tokens is only available via the `design-tokens.css` file.

> * I'm not sure I fully follow _why_ they're different; i.e. why couldn't `cornerRadius` be implemented through ThemeProvider inline styles, or vice-versa why cursor shouldn't be implemented through `design-tokens.css` modifier selectors. Should we _want_ them to be distinctly implemented?

The rationale is to have `ThemeProvider` only inline the styles that have arbitrary, or computed at runtime, values. The corner radius presets are static, and as such, we can simplify the way they are defined, and we can skip the runtime computation too.
This approach is already used for `cursor` (and was used for `density` before we removed it); this PR only aligns the "root forwarding" behavior of `cornerRadius` to what `cursor` already does.

Having said that, if we ever allow custom arbitrary values for the `cornerRadius` prop, we will switch to a hybrid approach (like we do for `color` where some values are defined statically and some values are computed by `ThemeProvider`at runtime (discussed [here](https://github.com/WordPress/gutenberg/pull/78816#discussion_r3361646103))

## Review threads

### Review thread 1 (resolved)

#### aduth on 2026-06-15T19:38:59Z

URL: https://github.com/WordPress/gutenberg/pull/79153#discussion_r3416086945
Path: packages/theme/src/test/theme-provider.test.tsx

```diff
@@ -127,6 +129,71 @@ describe( 'ThemeProvider', () => {
 
 			expect( readProp( document.documentElement, BRAND_BG ) ).toBe( '' );
 		} );
+
+		// Unlike color/cursor (emitted at runtime in the provider's own
+		// `<style>`), the `cornerRadius` preset resolves to
+		// `--wpds-border-radius-*` values through the prebuilt design-token CSS
+		// (the modes authored in `terrazzo.config.ts`). A root provider relies
+		// on that stylesheet's `:root:has( [data-wpds-root-provider='true']… )`
+		// rule to forward the preset to the document element, so these tests
+		// load the prebuilt CSS to exercise it. It is injected only for this
+		// block (it also defines base tokens on `:root`) to avoid interfering
+		// with the color assertions above.
+		describe( 'cornerRadius forwarding', () => {
+			const BORDER_RADIUS = '--wpds-border-radius-sm';
```

Nit: I'd only expect SCREAMING_SNAKE_CASE constant variables at the top-level of the module ([ref](https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/#constants)), so either moving this to be with the rest or renaming it to a camelCase naming scheme.

#### ciampo on 2026-06-16T08:27:00Z

URL: https://github.com/WordPress/gutenberg/pull/79153#discussion_r3419204117
Path: packages/theme/src/test/theme-provider.test.tsx

```diff
@@ -127,6 +129,71 @@ describe( 'ThemeProvider', () => {
 
 			expect( readProp( document.documentElement, BRAND_BG ) ).toBe( '' );
 		} );
+
+		// Unlike color/cursor (emitted at runtime in the provider's own
+		// `<style>`), the `cornerRadius` preset resolves to
+		// `--wpds-border-radius-*` values through the prebuilt design-token CSS
+		// (the modes authored in `terrazzo.config.ts`). A root provider relies
+		// on that stylesheet's `:root:has( [data-wpds-root-provider='true']… )`
+		// rule to forward the preset to the document element, so these tests
+		// load the prebuilt CSS to exercise it. It is injected only for this
+		// block (it also defines base tokens on `:root`) to avoid interfering
+		// with the color assertions above.
+		describe( 'cornerRadius forwarding', () => {
+			const BORDER_RADIUS = '--wpds-border-radius-sm';
```

Makes sense, moved that variable up with the rest

### Review thread 2

#### aduth on 2026-06-15T19:48:11Z

URL: https://github.com/WordPress/gutenberg/pull/79153#discussion_r3416144873
Path: packages/theme/src/test/theme-provider.test.tsx

```diff
@@ -127,6 +129,71 @@ describe( 'ThemeProvider', () => {
 
 			expect( readProp( document.documentElement, BRAND_BG ) ).toBe( '' );
 		} );
+
+		// Unlike color/cursor (emitted at runtime in the provider's own
+		// `<style>`), the `cornerRadius` preset resolves to
+		// `--wpds-border-radius-*` values through the prebuilt design-token CSS
+		// (the modes authored in `terrazzo.config.ts`). A root provider relies
+		// on that stylesheet's `:root:has( [data-wpds-root-provider='true']… )`
+		// rule to forward the preset to the document element, so these tests
+		// load the prebuilt CSS to exercise it. It is injected only for this
+		// block (it also defines base tokens on `:root`) to avoid interfering
+		// with the color assertions above.
+		describe( 'cornerRadius forwarding', () => {
+			const BORDER_RADIUS = '--wpds-border-radius-sm';
+			let prebuiltStyle: HTMLStyleElement;
+
+			beforeAll( () => {
+				prebuiltStyle = document.createElement( 'style' );
+				prebuiltStyle.textContent = readFileSync(
+					join( __dirname, '../prebuilt/css/design-tokens.css' ),
```

Tangent: I wonder if we could save ourselves some future headache adopting something like [`babel-plugin-transform-import-meta`](https://www.npmjs.com/package/babel-plugin-transform-import-meta) so we could start using (and avoid future migration to) `import.meta.dirname` since [`__dirname` doesn't exist in true ESM](https://nodejs.org/api/esm.html#no-__filename-or-__dirname). Ideally we'd [upgrade Jest](https://github.com/WordPress/gutenberg/issues/72593) and try to start using some of their native ESM support, but that's a bigger task.

#### ciampo on 2026-06-16T08:28:00Z

URL: https://github.com/WordPress/gutenberg/pull/79153#discussion_r3419210025
Path: packages/theme/src/test/theme-provider.test.tsx

```diff
@@ -127,6 +129,71 @@ describe( 'ThemeProvider', () => {
 
 			expect( readProp( document.documentElement, BRAND_BG ) ).toBe( '' );
 		} );
+
+		// Unlike color/cursor (emitted at runtime in the provider's own
+		// `<style>`), the `cornerRadius` preset resolves to
+		// `--wpds-border-radius-*` values through the prebuilt design-token CSS
+		// (the modes authored in `terrazzo.config.ts`). A root provider relies
+		// on that stylesheet's `:root:has( [data-wpds-root-provider='true']… )`
+		// rule to forward the preset to the document element, so these tests
+		// load the prebuilt CSS to exercise it. It is injected only for this
+		// block (it also defines base tokens on `:root`) to avoid interfering
+		// with the color assertions above.
+		describe( 'cornerRadius forwarding', () => {
+			const BORDER_RADIUS = '--wpds-border-radius-sm';
+			let prebuiltStyle: HTMLStyleElement;
+
+			beforeAll( () => {
+				prebuiltStyle = document.createElement( 'style' );
+				prebuiltStyle.textContent = readFileSync(
+					join( __dirname, '../prebuilt/css/design-tokens.css' ),
```

That's a good idea, I'll add it to https://github.com/WordPress/gutenberg/issues/77462 as a separate task

#### aduth on 2026-06-22T13:27:35Z

URL: https://github.com/WordPress/gutenberg/pull/79153#discussion_r3452559330
Path: packages/theme/src/test/theme-provider.test.tsx

```diff
@@ -127,6 +129,71 @@ describe( 'ThemeProvider', () => {
 
 			expect( readProp( document.documentElement, BRAND_BG ) ).toBe( '' );
 		} );
+
+		// Unlike color/cursor (emitted at runtime in the provider's own
+		// `<style>`), the `cornerRadius` preset resolves to
+		// `--wpds-border-radius-*` values through the prebuilt design-token CSS
+		// (the modes authored in `terrazzo.config.ts`). A root provider relies
+		// on that stylesheet's `:root:has( [data-wpds-root-provider='true']… )`
+		// rule to forward the preset to the document element, so these tests
+		// load the prebuilt CSS to exercise it. It is injected only for this
+		// block (it also defines base tokens on `:root`) to avoid interfering
+		// with the color assertions above.
+		describe( 'cornerRadius forwarding', () => {
+			const BORDER_RADIUS = '--wpds-border-radius-sm';
+			let prebuiltStyle: HTMLStyleElement;
+
+			beforeAll( () => {
+				prebuiltStyle = document.createElement( 'style' );
+				prebuiltStyle.textContent = readFileSync(
+					join( __dirname, '../prebuilt/css/design-tokens.css' ),
```

Follow-up at https://github.com/WordPress/gutenberg/pull/79362

### Review thread 3

#### aduth on 2026-06-15T19:51:03Z

URL: https://github.com/WordPress/gutenberg/pull/79153#discussion_r3416161398
Path: packages/theme/CHANGELOG.md

```diff
@@ -7,6 +7,10 @@
 -   Add `--wpds-border-radius-xl` for page and app shell surfaces so nested cards and notices can stay on `--wpds-border-radius-lg` without matching the parent radius ([#78913](https://github.com/WordPress/gutenberg/pull/78913)).
 -   Add `cornerRadius` prop to `ThemeProvider` for configuring the border-radius preset (`none`, `subtle`, `moderate`, `pronounced`) via prebuilt design token modes. [#78816](https://github.com/WordPress/gutenberg/pull/78816).
 
+### Enhancements
+
+-   `ThemeProvider`: forward the `cornerRadius` preset to the document element when `isRoot` is set, matching `color` and `cursor`. [#79153](https://github.com/WordPress/gutenberg/pull/79153).
```

Arguably a bug fix, but since it hasn't released yet I suppose it's not a bug quite yet!

## Changed files

- packages/theme/CHANGELOG.md (MODIFIED, +4/-0)
- packages/theme/src/prebuilt/css/design-tokens.css (MODIFIED, +14/-4)
- packages/theme/src/test/theme-provider.test.tsx (MODIFIED, +69/-0)
- packages/theme/terrazzo.config.ts (MODIFIED, +25/-4)
