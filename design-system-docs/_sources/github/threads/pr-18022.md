---
number: 18022
type: pull_request
state: MERGED
merged_at: 2019-10-24T20:53:25Z
url: https://github.com/WordPress/gutenberg/pull/18022
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Feature] UI Components","[Package] Components"]
---

# PR #18022: Components: Add VisuallyHidden component

- URL: https://github.com/WordPress/gutenberg/pull/18022
- Author: mkaz
- Created: 2019-10-18T13:52:26Z
- Updated: 2019-10-28T08:12:51Z
- Comments: 3 of 3
- Review threads: 12 of 12
- Changed files: 8 of 8
- Diff: ../pr-diffs/18022.diff

## Body

## Description

Adds a new component for rendering ScreenReaderText and the necessary CSS.
Per @youknowriad <a href="https://github.com/WordPress/gutenberg/pull/17997#issuecomment-543584382">comment here</a>


In testing this, I noticed one of the issues also for Storybook is they weren't setup to load the style.scss along with the components. This PR also adds the necessary CSS loaders in the Storybook webpack.config.js so a story can do this to include the CSS.

```
import ScreenReaderText from '../';
import '../style.scss';

export default { title: 'ScreenReaderText', component: ScreenReaderText };

export const _default = () => (
	<>
		<ScreenReaderText>
			This should not show.
		</ScreenReaderText>
		<div>
			This text will always show.
		</div>
	</>
);
```
This is my first new component being added, so not sure all the pieces that need to be properly updated.

## How has this been tested?

- Add `<ScreenReaderText> Test text. </ScreenReaderText>` and confirm it includes a div and proper style rules.

- Run Storybook: `npm run designs-system:dev` confirm ScreenReaderText does/doesn't show

## Types of changes
- Adds new component `ScreenReaderText`
- Updates Storybook to support loading .scss

## Checklist:
- [ ] My code is tested.
- [ ] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->

## Issue comments

### gziolo on 2019-10-18T22:08:19Z

URL: https://github.com/WordPress/gutenberg/pull/18022#issuecomment-543974141

It's often named `VisuallyHidden` in UI components libraries. See:
- https://reakit.io/docs/visually-hidden/ (@diegohaz)
- https://reacttraining.com/reach-ui/visually-hidden

### diegohaz on 2019-10-23T22:29:12Z

URL: https://github.com/WordPress/gutenberg/pull/18022#issuecomment-545662936

It looks great! I'd say only that the `renderAsRenderProps` util could be kept inside the component folder for now. There is a few things we should define before making it available for other components (naming, React Native version etc.), and that could be done in another PR. If this PR is merged and people start using the util in other components, it'll be harder to change later.

### mkaz on 2019-10-24T00:06:07Z

URL: https://github.com/WordPress/gutenberg/pull/18022#issuecomment-545686087

@diegohaz Thanks. I moved the utils file, inside VisuallyHidden component folder for now. We can adjust as things get defined.

## Review threads

### Review thread 1

#### gziolo on 2019-10-18T22:09:40Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336692825
Path: packages/components/src/screen-reader-text/README.md

```diff
@@ -0,0 +1,9 @@
+# ScreenReaderText
+
+A component used to render text intended for a screen reader, not visible elsewhere.
```

You can reference WordPress docs here, maybe also copy some parts of that document:
https://make.wordpress.org/accessibility/handbook/markup/the-css-class-screen-reader-text/

### Review thread 2

#### gziolo on 2019-10-18T22:10:45Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336693059
Path: packages/components/src/screen-reader-text/README.md

```diff
@@ -0,0 +1,9 @@
+# ScreenReaderText
+
+A component used to render text intended for a screen reader, not visible elsewhere.
+
+### Usage
+
+```jsx
+<ScreenReaderText> Show text for screenreader. </ScreenReaderText>
```

In other places we add full examples:

```jsx
import { Dashicon } from '@wordpress/components';

const MyDashicon = () => (
	<div>
		<Dashicon icon="admin-home" />
		<Dashicon icon="products" />
		<Dashicon icon="wordpress" />
	</div>
);
```

### Review thread 3

#### gziolo on 2019-10-18T22:11:33Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336693219
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

You often will want to use a different tag name as well.

#### mkaz on 2019-10-18T22:25:58Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336696071
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

Instead of a div? I'm open to suggestions, should it b a prop?

#### youknowriad on 2019-10-21T08:24:25Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336889835
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

The className here doesn't follow our guidelines. I guess it was done this way to match the existing className. I think it's fine to change it for consistency.

#### youknowriad on 2019-10-21T08:26:02Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336890534
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

In another component we used `isInline` prop to switch between `span` and `div` to ensure HTML stays valid in all situations. I wouldn't mind starting with div and adding that prop if we find a place where we need something inline.

#### gziolo on 2019-10-21T08:26:22Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336890672
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

The thing is that WP core uses `screen-reader-text`.

See also https://github.com/WordPress/gutenberg/pull/18022#issuecomment-543974141 about `VisuallyHidden` proposal.

#### youknowriad on 2019-10-21T08:34:36Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r336893982
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

> The thing is that WP core uses screen-reader-text.

I don't mind changing the component name to `VisuallyHidden` but I don't see any reason for sticking with Core's className. Am I missing something?

#### mkaz on 2019-10-22T02:36:35Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337313181
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

One of the reasons for using `screen-reader-text` className was to introduce the CSS into the components built-style CSS. There are several components already using that className relying on core's definition of it.

This was discovered when creating the stories which only include the build-style CSS. The color-picker is an example component which uses `screen-reader-text` within, when shown in Storybook the text that should be hidden isn't.

It's easy enough to change the className here and within the other components, if that is best.

#### youknowriad on 2019-10-22T10:06:55Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337429944
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

I think we should update all the usage of this class in our codebase to use this component instead, that way it's more portable, the className becomes an implementation detail. It could be done part of this PR or separately.

#### mkaz on 2019-10-22T13:26:26Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337514275
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

ok, I switched the className to `components-visually-hidden` 

I'll switch the other components over in a separate PR. I like keeping each PR as focused as possible and thus one adding a component doesn't really change the other behavior. Just enables the ability to fix them.

#### gziolo on 2019-10-22T14:02:24Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337535477
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

I think we have `label`, `p` and `leged` with the `screen-reader-text` class name as well.

We can iterate if you prefer to tackle it separately. See my related comment: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337530787.

#### youknowriad on 2019-10-22T14:03:33Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337536092
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

Are all these solved by nesting VisuallyHidden inline inside the tags?

#### youknowriad on 2019-10-22T14:24:48Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337549423
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

I think we should decide now whether we want `isInline` prop or a render prop. I feel they address the same issue and I don't like API duplication :)

#### gziolo on 2019-10-22T15:55:32Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337604571
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

I don't think it solves the issue because the wrapping `label` or `legend` can have additional visual styles applied which you want to remove with `VisuallyHidden`. We would have to double-check that though. I'm making the case based on my assumptions :)

#### mkaz on 2019-10-22T16:25:06Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337620471
Path: packages/components/src/screen-reader-text/index.js

```diff
@@ -0,0 +1,10 @@
+
+function ScreenReaderText( props ) {
+	return (
+		<div className="screen-reader-text">
```

You can look at `BaseControl` for an example usage. That solution inserts `<label className='screen-reader-text'>...</label>` 
This makes me think we would want to use the `as` prop. So the implementation would be `<VisuallyHidden as="label">...</VisuallyHidden>`

Also looks like a bug that if you pass in `hideLabelFromVision` prop but do not pass an `id` than the `BaseControl.VisualLabel` will be used, so would not include the class.

### Review thread 4 (resolved)

#### gziolo on 2019-10-22T13:46:27Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337525768
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,13 @@
+.components-visually-hidden {
```

This is missing:
```css
.screen-reader-text:focus {
  background-color: #eee;
  clip: auto !important;
  clip-path: none;
  color: #444;
  display: block;
  font-size: 1em;
  height: auto;
  left: 5px;
  line-height: normal;
  padding: 15px 23px 14px;
  text-decoration: none;
  top: 5px;
  width: auto;
  z-index: 100000; /* Above WP toolbar. */
}
```

See https://make.wordpress.org/accessibility/handbook/markup/the-css-class-screen-reader-text/

#### mkaz on 2019-10-22T14:09:45Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337539705
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,13 @@
+.components-visually-hidden {
```

oh yeh, good catch

### Review thread 5

#### gziolo on 2019-10-22T13:54:52Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337530787
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

Reakit uses an interesting pattern, they have `as` prop which you can pass to change the tag name, or even use a component. See: https://reakit.io/docs/composition/#as-prop

```js
<VisuallyHidden as="span">
    This should not show.
</VisuallyHidden>
```

They also offer an alternative way to use render props. See: https://reakit.io/docs/composition/#render-props

```js
<VisuallyHidden>
    { ( props ) => (
        <span { ...props }>This should not show.</span> 
    ) }
</VisuallyHidden>
```

@diegohaz, how much work would it be to replicate that here?

#### youknowriad on 2019-10-22T14:00:32Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337534364
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

`as` seem to "web" focused for me especially if we want the API of these components to be DOM agnostic. and the render props are great but I'd like to know if there's a potential use-case that the current API doesn't solve?

#### gziolo on 2019-10-22T15:56:39Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337605239
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

In Reakit `as` can be a string or Component, the native version:

```js
<VisuallyHidden as={ View }>
    This should not show.
</VisuallyHidden>
```

#### youknowriad on 2019-10-22T15:58:26Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337606267
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

mmm interesting

#### diegohaz on 2019-10-22T19:16:24Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337702290
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

It's simple to implement:
```jsx
function renderAsRenderProps({ as: T = "div", ...props }) {
  if (typeof props.children === "function") {
    return props.children(props);
  }
  return <T {...props} />;
}

function Component({ as = "span", ...props }) {
  return renderAsRenderProps({ as, ...props });
}
```

Using children as a function is better than the `as` prop to compose multiple components:
```jsx
<Component>
  {props => <div><Button {...props} /></div>}
</Component>
```

#### diegohaz on 2019-10-22T19:19:33Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337703637
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

The `as` prop is quite popular nowadays. The first library I saw using it was [Semantic UI](https://react.semantic-ui.com/#augmentation). Not sure if there are other libraries combining it with render props though. But I think it's a simple and powerful combination.

#### mkaz on 2019-10-22T20:56:17Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337745991
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,14 @@
+
+function VisuallyHidden( {
+	children,
+	isInline = false,
+} ) {
+	const TagName = isInline ? 'span' : 'div';
```

Thanks @diegohaz - I updated the component to use the "as" method as described, works well.

I defined the `renderAsRenderProps` function within the VisuallyHidden component, not exported. It  seems useful that we may want to use it elsewhere. If so, where would I move it to?

### Review thread 6

#### youknowriad on 2019-10-23T08:52:08Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r337923308
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * VisuallyHidden component to render text out non-visually
+ * for use in devices such as a screen reader.
+ */
+function VisuallyHidden( {
+	as = 'div',
+	...props
+} ) {
+	return renderAsRenderProps( {
+		as,
+		className: 'components-visually-hidden',
+		...props,
+	} );
+}
+
+// render "as" to be either string (tag name) or component (fn)
+function renderAsRenderProps( { as: T = 'div', ...props } ) {
```

I like that pattern a lot and this can probably be extracted in a utils folder in the components package. That said a `<Component as="div" />` doesn't mean anything for mobile since there's no "div" on mobile and there's the expectation that the same props should be valid in all platform.

Not a blocker since mobile can decide to just ignore the "as"  prop but I thought I'd raise for awareness.

#### mkaz on 2019-10-23T16:16:49Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338146229
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * VisuallyHidden component to render text out non-visually
+ * for use in devices such as a screen reader.
+ */
+function VisuallyHidden( {
+	as = 'div',
+	...props
+} ) {
+	return renderAsRenderProps( {
+		as,
+		className: 'components-visually-hidden',
+		...props,
+	} );
+}
+
+// render "as" to be either string (tag name) or component (fn)
+function renderAsRenderProps( { as: T = 'div', ...props } ) {
```

I moved it to a `utils.js` file.

The div is just the default value, if used on Mobile they can pass in something different, or not use :-)

#### youknowriad on 2019-10-23T16:45:58Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338160433
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * VisuallyHidden component to render text out non-visually
+ * for use in devices such as a screen reader.
+ */
+function VisuallyHidden( {
+	as = 'div',
+	...props
+} ) {
+	return renderAsRenderProps( {
+		as,
+		className: 'components-visually-hidden',
+		...props,
+	} );
+}
+
+// render "as" to be either string (tag name) or component (fn)
+function renderAsRenderProps( { as: T = 'div', ...props } ) {
```

The idea is that mobile and web use the exact same code in the `edit` function of blocks... So there's no mobile do that and web do this.

#### mkaz on 2019-10-23T19:40:53Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338241439
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * VisuallyHidden component to render text out non-visually
+ * for use in devices such as a screen reader.
+ */
+function VisuallyHidden( {
+	as = 'div',
+	...props
+} ) {
+	return renderAsRenderProps( {
+		as,
+		className: 'components-visually-hidden',
+		...props,
+	} );
+}
+
+// render "as" to be either string (tag name) or component (fn)
+function renderAsRenderProps( { as: T = 'div', ...props } ) {
```

@youknowriad ah, I see.  Sounds like its not a blocker though, so should be fine.
Is the utils.js extraction fine?

#### mkaz on 2019-10-23T19:42:22Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338242023
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * VisuallyHidden component to render text out non-visually
+ * for use in devices such as a screen reader.
+ */
+function VisuallyHidden( {
+	as = 'div',
+	...props
+} ) {
+	return renderAsRenderProps( {
+		as,
+		className: 'components-visually-hidden',
+		...props,
+	} );
+}
+
+// render "as" to be either string (tag name) or component (fn)
+function renderAsRenderProps( { as: T = 'div', ...props } ) {
```

Actually @gziolo has the change requested for the CSS. Greg, when you get the chance, can you do a quick review again. I think everything has been addressed. Thanks! :bowing_man:

### Review thread 7 (resolved)

#### gziolo on 2019-10-24T09:24:17Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338470209
Path: packages/components/src/visually-hidden/utils.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * Utility Functions
+ */
+
+/**
+ * renderAsRenderProps is used to wrap a component and convert
+ * the passed property "as" either a string or component, to the
+ * rendered tag if a string, or component.
+ *
+ * See VisuallyHidden hidden for example.
+ *
+ * @param {string|Component} as what tag/component to render
+ * @return {Component} rendered component
+ */
+function renderAsRenderProps( { as: T = 'div', ...props } ) {
```

Can we avoid using shortcuts, please. It makes it hard to follow for those not familiar with the pattern:
```suggestion
function renderAsRenderProps( { as: Component = 'div', ...props } ) {
```

### Review thread 8 (resolved)

#### gziolo on 2019-10-24T09:25:25Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338470756
Path: packages/components/src/visually-hidden/utils.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * Utility Functions
+ */
+
+/**
+ * renderAsRenderProps is used to wrap a component and convert
+ * the passed property "as" either a string or component, to the
+ * rendered tag if a string, or component.
+ *
+ * See VisuallyHidden hidden for example.
+ *
+ * @param {string|Component} as what tag/component to render
```

Nit:
```suggestion
 * @param {string|Component} as A tag or component to render.
```

### Review thread 9 (resolved)

#### gziolo on 2019-10-24T09:25:37Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338470855
Path: packages/components/src/visually-hidden/utils.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * Utility Functions
+ */
+
+/**
+ * renderAsRenderProps is used to wrap a component and convert
+ * the passed property "as" either a string or component, to the
+ * rendered tag if a string, or component.
+ *
+ * See VisuallyHidden hidden for example.
+ *
+ * @param {string|Component} as what tag/component to render
+ * @return {Component} rendered component
```

```suggestion
 * @return {Component} The rendered component.
```

### Review thread 10

#### gziolo on 2019-10-24T09:28:42Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338472281
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,22 @@
+
```

This empty space is not necessary. Why is ESLint not complaining? :) We really need Prettier 😍

#### mkaz on 2019-10-24T13:54:57Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338587099
Path: packages/components/src/visually-hidden/index.js

```diff
@@ -0,0 +1,22 @@
+
```

Funny because the space should be below, newline at the end.

### Review thread 11

#### gziolo on 2019-10-24T09:29:50Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338472867
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,30 @@
+.components-visually-hidden {
+	border: 0;
+	clip: rect(1px, 1px, 1px, 1px);
+	-webkit-clip-path: inset(50%);
+	clip-path: inset(50%);
+	height: 1px;
+	margin: -1px;
+	overflow: hidden;
+	padding: 0;
+	position: absolute;
+	width: 1px;
+	word-wrap: normal !important;
+}
+
+.components-visually-hidden:focus {
+	background-color: #eee;
```

Can this be converted to one of the variables used in SCSS files?

#### mkaz on 2019-10-24T13:49:09Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338583591
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,30 @@
+.components-visually-hidden {
+	border: 0;
+	clip: rect(1px, 1px, 1px, 1px);
+	-webkit-clip-path: inset(50%);
+	clip-path: inset(50%);
+	height: 1px;
+	margin: -1px;
+	overflow: hidden;
+	padding: 0;
+	position: absolute;
+	width: 1px;
+	word-wrap: normal !important;
+}
+
+.components-visually-hidden:focus {
+	background-color: #eee;
```

This raises another issue, it doesn't look like the SCSS variables are defined within Components package, or if they are I've missed where. It seems they rely on Block Editor styling, which can be used, but not sure where this separation should be. So this might need to become a separate issue for how to define and use SCSS with Components.

#### youknowriad on 2019-10-24T13:55:21Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338587308
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,30 @@
+.components-visually-hidden {
+	border: 0;
+	clip: rect(1px, 1px, 1px, 1px);
+	-webkit-clip-path: inset(50%);
+	clip-path: inset(50%);
+	height: 1px;
+	margin: -1px;
+	overflow: hidden;
+	padding: 0;
+	position: absolute;
+	width: 1px;
+	word-wrap: normal !important;
+}
+
+.components-visually-hidden:focus {
+	background-color: #eee;
```

That's an already solved problem. variables are now defined in the `base-styles` package and these are made available to all packages using SASS including the @wordpress/components package.

#### mkaz on 2019-10-24T14:11:08Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338596291
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,30 @@
+.components-visually-hidden {
+	border: 0;
+	clip: rect(1px, 1px, 1px, 1px);
+	-webkit-clip-path: inset(50%);
+	clip-path: inset(50%);
+	height: 1px;
+	margin: -1px;
+	overflow: hidden;
+	padding: 0;
+	position: absolute;
+	width: 1px;
+	word-wrap: normal !important;
+}
+
+.components-visually-hidden:focus {
+	background-color: #eee;
```

:+1: Great. So sounds like mostly a matter of documentation and examples for the case that someone wants to use the components package, show they would define their own or use base-styles package.

### Review thread 12

#### gziolo on 2019-10-24T09:30:59Z

URL: https://github.com/WordPress/gutenberg/pull/18022#discussion_r338473404
Path: packages/components/src/visually-hidden/style.scss

```diff
@@ -0,0 +1,30 @@
+.components-visually-hidden {
+	border: 0;
+	clip: rect(1px, 1px, 1px, 1px);
+	-webkit-clip-path: inset(50%);
+	clip-path: inset(50%);
+	height: 1px;
+	margin: -1px;
+	overflow: hidden;
+	padding: 0;
+	position: absolute;
+	width: 1px;
+	word-wrap: normal !important;
+}
+
+.components-visually-hidden:focus {
+	background-color: #eee;
+	clip: auto !important;
+	clip-path: none;
+	color: #444;
+	display: block;
+	font-size: 1em;
+	height: auto;
+	left: 5px;
+	line-height: normal;
+	padding: 15px 23px 14px;
+	text-decoration: none;
+	top: 5px;
+	width: auto;
+	z-index: 100000;
```

In the context of Gutenberg, we probably should use a special helper which processes all `z-indexes`:

https://github.com/WordPress/gutenberg/blob/7515397fef469e8cbdaf68162d6d6e1f2a5f9aa1/packages/base-styles/_z-index.scss#L127-L133

## Changed files

- docs/manifest-devhub.json (MODIFIED, +6/-0)
- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/style.scss (MODIFIED, +1/-0)
- packages/components/src/visually-hidden/README.md (ADDED, +9/-0)
- packages/components/src/visually-hidden/index.js (ADDED, +22/-0)
- packages/components/src/visually-hidden/stories/index.js (ADDED, +17/-0)
- packages/components/src/visually-hidden/style.scss (ADDED, +30/-0)
- packages/components/src/visually-hidden/utils.js (ADDED, +22/-0)
