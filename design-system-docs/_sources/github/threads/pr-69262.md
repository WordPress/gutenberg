---
number: 69262
type: pull_request
state: OPEN
url: https://github.com/WordPress/gutenberg/pull/69262
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #69262: FormTokenField: Validate input before tokenizing on space

- URL: https://github.com/WordPress/gutenberg/pull/69262
- Author: dhruvikpatel18
- Created: 2025-02-20T11:20:55Z
- Updated: 2026-01-28T10:51:31Z
- Comments: 4 of 4
- Review threads: 0 of 0
- Changed files: 1 of 1
- Diff: ../pr-diffs/69262.diff

## Body

Closes #69252

## What?
This PR fixes the `FormTokenField` component to properly validate input before creating tokens when pressing the space key.

## Why?
Currently, when the `tokenizeOnSpace` prop is enabled, the FormTokenField component creates tokens immediately on space press without checking the `experimentalValidateInput` function. This can lead to invalid tokens being created, which is inconsistent with the validation behavior when using other methods of token creation (like pressing Enter or comma).

## How?
Modified the space key handler in the `onKeyDown` function to:
1. Check if there's valid input using `inputHasValidValue()`
2. Call `experimentalValidateInput` with the current value
3. Only create a token if both checks pass

## Issue comments

### github-actions on 2025-02-21T04:46:40Z

URL: https://github.com/WordPress/gutenberg/pull/69262#issuecomment-2673428370

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: dhruvikpatel18 <dhruvik18@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: alshakero <alshakero@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### Mamaduka on 2025-02-24T07:40:38Z

URL: https://github.com/WordPress/gutenberg/pull/69262#issuecomment-2677635416

Thank you, @dhruvikpatel18!

Does this also affect tokenization on the <kbd>Ented</kbd> key? Both are using `addCurrentToken`, and it seems the method has a handler for validation. Another unique key for this component is the comma, which uses a different callback - `handleCommaKey`.

Before proceeding with the fix, it would be a good idea to check the "records" and determine the reason for the differences.

### Mamaduka on 2025-03-06T06:48:04Z

URL: https://github.com/WordPress/gutenberg/pull/69262#issuecomment-2702964931

Hi, @dhruvikpatel18

Are you still interested in continuing to work on this issue?

### dhruvikpatel18 on 2025-03-06T07:03:12Z

URL: https://github.com/WordPress/gutenberg/pull/69262#issuecomment-2702988517

> Does this also affect tokenization on the Ented key? Both are using `addCurrentToken`, and it seems the method has a handler for validation. Another unique key for this component is the comma, which uses a different callback - `handleCommaKey`.
> 
> Before proceeding with the fix, it would be a good idea to check the "records" and determine the reason for the differences.

@Mamaduka Thank you for the feedback! You're right - I found that neither `addCurrentToken()` nor `handleCommaKey()` check the `__experimentalValidateInput` function. I will update my approach to fix validation at the function level rather than in the key handlers, ensuring consistent validation across Enter, Space, and comma tokenization methods.

## Review threads

_No review threads._
## Changed files

- packages/components/src/form-token-field/index.tsx (MODIFIED, +10/-2)
