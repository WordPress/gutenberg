---
number: 72943
type: pull_request
state: MERGED
merged_at: 2025-11-04T15:52:20Z
url: https://github.com/WordPress/gutenberg/pull/72943
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Developer Documentation","[Package] Theme"]
---

# PR #72943: Theme: Fix ThemeProvider documentation for accurate color prop

- URL: https://github.com/WordPress/gutenberg/pull/72943
- Author: aduth
- Created: 2025-11-03T19:58:58Z
- Updated: 2025-11-04T15:52:51Z
- Comments: 2 of 2
- Review threads: 1 of 1
- Changed files: 1 of 1
- Diff: ../pr-diffs/72943.diff

## Body

## What?
<!-- Link this PR to its associated issue with an appropriate keyword: Closes, See, Follow up to, etc. -->
Closes #72932

Updates documentation for the `ThemeProvider` to accurately reflect accepting a `color` object of `bg` and `primary` color values.

## Why?

Documentation should be accurate to expected usage, to avoid confusion.

## How?

Documents and describes the current prop structure.

## Testing Instructions

Validate accuracy of documentation.

## Issue comments

### github-actions on 2025-11-03T19:59:08Z

URL: https://github.com/WordPress/gutenberg/pull/72943#issuecomment-3482307311

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: nerrad <nerrad@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2025-11-03T20:26:57Z

URL: https://github.com/WordPress/gutenberg/pull/72943#issuecomment-3482462749

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 35a3c4a2253b78b04d1f5d319e2485141add7307.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/19047599207
📝  Reported issues:
- #51533 in `/test/e2e/specs/editor/various/list-view.spec.js`

## Review threads

### Review thread 1

#### nerrad on 2025-11-03T22:09:18Z

URL: https://github.com/WordPress/gutenberg/pull/72943#discussion_r2487987439
Path: packages/theme/README.md

```diff
@@ -70,26 +70,34 @@ import { ThemeProvider } from '@wordpress/theme';
 
 function App() {
 	return (
-		<ThemeProvider color={ { scheme: 'light', accent: 'blue' } }>
+		<ThemeProvider color={ { primary: 'blue' } }>
 			{ /* Your app content */ }
 		</ThemeProvider>
 	);
 }
 ```
 
+The `color` prop accepts an object with the following optional properties:
+
+-   `primary`: The primary/accent seed color (default: `'#3858e9'`)
+-   `bg`: The background seed color (default: `'#f8f8f8'`)
+
+Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
```

I wonder if there should be any additional info on what happens when just one of the seed properties is provided?

#### tyxla on 2025-11-04T08:53:56Z

URL: https://github.com/WordPress/gutenberg/pull/72943#discussion_r2489323336
Path: packages/theme/README.md

```diff
@@ -70,26 +70,34 @@ import { ThemeProvider } from '@wordpress/theme';
 
 function App() {
 	return (
-		<ThemeProvider color={ { scheme: 'light', accent: 'blue' } }>
+		<ThemeProvider color={ { primary: 'blue' } }>
 			{ /* Your app content */ }
 		</ThemeProvider>
 	);
 }
 ```
 
+The `color` prop accepts an object with the following optional properties:
+
+-   `primary`: The primary/accent seed color (default: `'#3858e9'`)
+-   `bg`: The background seed color (default: `'#f8f8f8'`)
+
+Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
```

I'm reading that all of them are optional, and we have defaults too, so it should be pretty straightforward what happens? I might be missing something though

#### aduth on 2025-11-04T14:58:52Z

URL: https://github.com/WordPress/gutenberg/pull/72943#discussion_r2490852213
Path: packages/theme/README.md

```diff
@@ -70,26 +70,34 @@ import { ThemeProvider } from '@wordpress/theme';
 
 function App() {
 	return (
-		<ThemeProvider color={ { scheme: 'light', accent: 'blue' } }>
+		<ThemeProvider color={ { primary: 'blue' } }>
 			{ /* Your app content */ }
 		</ThemeProvider>
 	);
 }
 ```
 
+The `color` prop accepts an object with the following optional properties:
+
+-   `primary`: The primary/accent seed color (default: `'#3858e9'`)
+-   `bg`: The background seed color (default: `'#f8f8f8'`)
+
+Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
```

I think there's room for improvement on describing what the component actually does, which I updated in 908a57e60f. But yeah, beyond that, the expectation should be that if one of the values is omitted, the "generates" behavior would assume the default of the omitted value. Happy to make further revisions if there's a way to make this clearer.

## Changed files

- packages/theme/README.md (MODIFIED, +17/-9)
