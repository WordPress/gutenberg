---
number: 30557
type: pull_request
state: MERGED
merged_at: 2022-01-11T10:25:21Z
url: https://github.com/WordPress/gutenberg/pull/30557
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Type] Bug","[Package] Components","First-time Contributor","[Feature] Component System"]
---

# PR #30557: Stop keypresses being caught by other elements when they happen in a CustomSelectControl

- URL: https://github.com/WordPress/gutenberg/pull/30557
- Author: opr
- Created: 2021-04-07T10:02:19Z
- Updated: 2022-01-17T22:53:39Z
- Comments: 5 of 5
- Review threads: 12 of 12
- Changed files: 3 of 3
- Diff: ../pr-diffs/30557.diff

## Body

<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/repository-management.md#pull-requests. -->

## Description
<!-- Please describe what you have changed or added -->
In the `<ul>` output by `CustomSelectControl` I have added a function to handle `onKeyDown` - this stops the event from propagating, but still calls the `onKeyDown` handler (which is derived ultimately from `downshift`). 

This PR fixes #30204.

## How has this been tested?
<!-- Please describe in detail how you tested your changes. -->
<!-- Include details of your testing environment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->
In the linked issue (#30204) the reproducible steps can also be used to test:

1. Open the storybook for this project: `npm run storybook:dev`
2. Go to the `CustomSelectControl > Default` component, click it and start typing `s` to get to the entry `Small`
3. Notice the storybook sidebar no longer closes due to the `s` keypress being cancelled.

## Types of changes
<!-- What types of changes does your code introduce?  -->
<!-- Bug fix (non-breaking change which fixes an issue) -->
<!-- New feature (non-breaking change which adds functionality) -->
<!-- Breaking change (fix or feature that would cause existing functionality to not work as expected) -->
Bug fix

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ --> **Unsure, I had to add eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions but I don't see any other way of capturing this event**
- [x] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->

## Issue comments

### github-actions on 2021-04-07T10:02:41Z

URL: https://github.com/WordPress/gutenberg/pull/30557#issuecomment-814784589

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @opr! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/), where we hold [regularly weekly meetings](https://make.wordpress.org/core/tag/core-editor-summary/) open to anyone to coordinate with each other.

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### opr on 2021-06-21T09:24:56Z

URL: https://github.com/WordPress/gutenberg/pull/30557#issuecomment-864880558

Hi folks - coming in with an update to provide context. Here's a real-world example of what this PR aims to solve, when a user has a WordPress site, connected via Jetpack, there is a sidebar that can be opened by pressing N.

When a customer is using WooCommerce Blocks, there are input fields for Country/State that use `CustomSelectControl`.

If the user tries to type into these boxes, for example 'United Kingdom', then the `N` key is hit, causing the sidebar to open.

here's a video to show it happening (note the inputs given while the select box is focused are `u -> n` and the sidebar appears on the right-hand side immediately after `n`.)

https://www.loom.com/share/2dbe6a589c4541599438782d066151a7

### opr on 2021-11-26T14:14:14Z

URL: https://github.com/WordPress/gutenberg/pull/30557#issuecomment-980006542

Thanks @ntsekouras I've made a change to stop the synthetic event propagating and it has solved the issue you mentioned in your review.

### opr on 2022-01-10T20:13:57Z

URL: https://github.com/WordPress/gutenberg/pull/30557#issuecomment-1009304621

Hey @ciampo thanks for the review and sorry for the delay! All done and ready for a re-review :)

### opr on 2022-01-11T10:23:47Z

URL: https://github.com/WordPress/gutenberg/pull/30557#issuecomment-1009819308

> Thank you @opr for swiftly addressing the feedback! Code changes LGTM 🚀
> 
> The e2e test failures don't seem related, maybe rebasing would solve them?

Rebasing worked, thanks @ciampo

## Review threads

### Review thread 1 (resolved)

#### ntsekouras on 2021-11-01T14:54:44Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r740276240
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -128,7 +128,14 @@ export default function CustomSelectControl( {
 					className="components-custom-select-control__button-icon"
 				/>
 			</Button>
-			<ul { ...menuProps }>
+			{ /* eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions */ }
+			<ul
+				{ ...menuProps }
+				onKeyDown={ ( e ) => {
+					e.nativeEvent.stopImmediatePropagation();
```

Why you're using `nativeEvent` here and also `stopImmediatePropagation` instead of `stopPropagation`?

#### opr on 2021-11-26T14:12:59Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r757528675
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -128,7 +128,14 @@ export default function CustomSelectControl( {
 					className="components-custom-select-control__button-icon"
 				/>
 			</Button>
-			<ul { ...menuProps }>
+			{ /* eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions */ }
+			<ul
+				{ ...menuProps }
+				onKeyDown={ ( e ) => {
+					e.nativeEvent.stopImmediatePropagation();
```

Hi thanks for the review. I'm not sure why I did it that way, it was quite a while ago and I can't think of any justification for it.

I've changed it now and it has stopped the issue of `Escape` propagating to parent listeners.

### Review thread 2 (resolved)

#### ciampo on 2021-11-30T13:35:39Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r759283672
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -141,7 +141,14 @@ export default function CustomSelectControl( {
 					className="components-custom-select-control__button-icon"
 				/>
 			</Button>
-			<ul { ...menuProps }>
+			{ /* eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions */ }
+			<ul
+				{ ...menuProps }
+				onKeyDown={ ( e ) => {
+					e.stopPropagation();
+					menuProps.onKeyDown( e );
```

I'm not sure we're guaranteed that `onKeyDown` is always defined on `menuProps` — it's probably safer to use optional chaining?

```suggestion
					menuProps?.onKeyDown?.( e );
```

### Review thread 3 (resolved)

#### ciampo on 2021-11-30T13:37:46Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r759285439
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -141,7 +141,14 @@ export default function CustomSelectControl( {
 					className="components-custom-select-control__button-icon"
 				/>
 			</Button>
-			<ul { ...menuProps }>
+			{ /* eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions */ }
+			<ul
+				{ ...menuProps }
+				onKeyDown={ ( e ) => {
```

Could we refactor this inline function into a `handleKeyDown` function? We could even use the `useCallback` hook to memoize (with `menuProps` as the only dependency)

#### opr on 2022-01-10T20:12:45Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781515650
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -141,7 +141,14 @@ export default function CustomSelectControl( {
 					className="components-custom-select-control__button-icon"
 				/>
 			</Button>
-			<ul { ...menuProps }>
+			{ /* eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions */ }
+			<ul
+				{ ...menuProps }
+				onKeyDown={ ( e ) => {
```

Good suggestion, thanks!

### Review thread 4 (resolved)

#### opr on 2022-01-10T20:14:44Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781516715
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
```

To stop eslint reporting an issue with the div having a `keyDown` listener but no role.

#### ciampo on 2022-01-10T22:10:32Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781605857
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
```

Sounds good! Could you add this as an inline comment too?

### Review thread 5 (resolved)

#### ciampo on 2022-01-10T22:13:20Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781608918
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
```

It's probably better if the `onKeyDown` spy gets instantiated _inside_ the test, to avoid potential bugs in the future when adding more tests and forgetting to reset the spy in between tests. i.e.

```js

describe( 'CustomSelectControl', () => {
  it( 'Captures the keypress event and does not let it propagate', () => {
    const onKeyDown = jest.fn();
    // ...
```

#### opr on 2022-01-10T22:54:08Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781635676
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
```

Thanks, good point!

### Review thread 6 (resolved)

#### ciampo on 2022-01-10T22:15:32Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781611257
Path: packages/components/CHANGELOG.md

```diff
@@ -4,7 +4,8 @@
 
 ### Bug Fix
 
--   Add missing styles to the `BaseControl.VisualLabel` component. ([#37747](https://github.com/WordPress/gutenberg/pull/37747))
+- Add missing styles to the `BaseControl.VisualLabel` component. ([#37747](https://github.com/WordPress/gutenberg/pull/37747))
+- Prevent keyDown events from propagating up in `CustomSelectControl` ([#30557](https://github.com/WordPress/gutenberg/pull/30557))
```

Let's match the spacing with the rest of the CHANGELOG entries:

```suggestion
-   Add missing styles to the `BaseControl.VisualLabel` component. ([#37747](https://github.com/WordPress/gutenberg/pull/37747))
-   Prevent keyDown events from propagating up in `CustomSelectControl` ([#30557](https://github.com/WordPress/gutenberg/pull/30557))
```

#### opr on 2022-01-10T22:56:42Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781636881
Path: packages/components/CHANGELOG.md

```diff
@@ -4,7 +4,8 @@
 
 ### Bug Fix
 
--   Add missing styles to the `BaseControl.VisualLabel` component. ([#37747](https://github.com/WordPress/gutenberg/pull/37747))
+- Add missing styles to the `BaseControl.VisualLabel` component. ([#37747](https://github.com/WordPress/gutenberg/pull/37747))
+- Prevent keyDown events from propagating up in `CustomSelectControl` ([#30557](https://github.com/WordPress/gutenberg/pull/30557))
```

Thanks!

### Review thread 7 (resolved)

#### ciampo on 2022-01-10T22:29:19Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781622988
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
```

The `options` prop should be an array of objects with a `key` and a `name` properties

```js
// ...
const options = [
  {
    key: 'one',
    name: 'Option one',
  },
  {
    key: 'two',
    name: 'Option two',
  },
  {
    key: 'three',
    name: 'Option three',
  },
];

render(
  <div role="none" onKeyDown={ onKeyDown }>
    <CustomSelectControl options={ options } />
  </div>
);
// ...
```

### Review thread 8 (resolved)

#### ciampo on 2022-01-10T22:29:42Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781623191
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
```

I believe this className can be removed?

### Review thread 9 (resolved)

#### ciampo on 2022-01-10T22:32:02Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781624466
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
+			</div>
+		);
+		const { container } = render( wrapper );
```

We could simplify the code by rendering the markup directly inside the `render` function:

```js
const { container } = render(
  <div role="none" onKeyDown={ onKeyDown }>
    <CustomSelectControl options={ options } />
  </div>
);
```

Actually, since we don't need `container` (see next comment), this could be just written as

```js
render(
  <div role="none" onKeyDown={ onKeyDown }>
    <CustomSelectControl options={ options } />
  </div>
);
```

#### opr on 2022-01-10T23:07:30Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781641865
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
+			</div>
+		);
+		const { container } = render( wrapper );
```

Thanks, this is great to know :)

### Review thread 10 (resolved)

#### ciampo on 2022-01-10T22:37:23Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781627145
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
+			</div>
+		);
+		const { container } = render( wrapper );
+		fireEvent.keyDown(
+			container.getElementsByClassName(
+				'components-custom-select-control__menu'
+			)[ 0 ]
+		);
```

Using `getElementsByClassName` is not ideal, since we're [testing the implementation details](https://kentcdodds.com/blog/testing-implementation-details) of this component. Instead, we should write our tests from the point of view of the user, relying more on the accessible roles and the content that gets rendered — i.e:

1. open the select by clicking the toggle button
2. look for the element with role listbox (i.e. the equivalent of a `select`)
3. fire the event on that element

It is also a good practice to use the `screen` object instead of calling methods on the `container` (see [link](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library#not-using-screen))

This would roughly translate to this code:

```js
import { render, fireEvent, screen } from '@testing-library/react';

// ...

const toggleButton = screen.getByRole( 'button' );
fireEvent.click( toggleButton );

const customSelect = screen.getByRole( 'listbox' );
fireEvent.keyDown( customSelectControl );
```

#### opr on 2022-01-10T23:08:06Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781642116
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
+			</div>
+		);
+		const { container } = render( wrapper );
+		fireEvent.keyDown(
+			container.getElementsByClassName(
+				'components-custom-select-control__menu'
+			)[ 0 ]
+		);
```

Nice, thanks a lot for helping me out here! 👍🏼

### Review thread 11 (resolved)

#### ciampo on 2022-01-10T22:38:46Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781627952
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
+			</div>
+		);
+		const { container } = render( wrapper );
+		fireEvent.keyDown(
+			container.getElementsByClassName(
+				'components-custom-select-control__menu'
+			)[ 0 ]
+		);
+		expect( onKeyDown.mock.calls.length ).toBe( 0 );
```

We could use the higher-level `toHaveBeenCalledTimes` [assertion](https://jestjs.io/docs/expect#tohavebeencalledtimesnumber):

```suggestion
		expect( onKeyDown ).toHaveBeenCalledTimes( 0 );
```

#### opr on 2022-01-10T23:08:24Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781642356
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
+ * WordPress dependencies
+ */
+import { CustomSelectControl } from '@wordpress/components';
+
+describe( 'CustomSelectControl', () => {
+	const onKeyDown = jest.fn();
+
+	it( 'Captures the keypress event and does not let it propagate', () => {
+		const wrapper = (
+			<div
+				role="none"
+				className="test-class-name"
+				onKeyDown={ onKeyDown }
+			>
+				<CustomSelectControl options={ [ 'a', 'b', 'c' ] } />
+			</div>
+		);
+		const { container } = render( wrapper );
+		fireEvent.keyDown(
+			container.getElementsByClassName(
+				'components-custom-select-control__menu'
+			)[ 0 ]
+		);
+		expect( onKeyDown.mock.calls.length ).toBe( 0 );
```

Should have known this 🤦🏼  cheers.

### Review thread 12 (resolved)

#### ciampo on 2022-01-10T22:41:17Z

URL: https://github.com/WordPress/gutenberg/pull/30557#discussion_r781629352
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -0,0 +1,31 @@
+/**
+ * External dependencies
+ */
+import { render, fireEvent } from '@testing-library/react';
+/**
```

Let's add an empty line here

```suggestion

/**
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/custom-select-control/index.js (MODIFIED, +13/-1)
- packages/components/src/custom-select-control/test/index.js (ADDED, +46/-0)
