---
number: 44559
type: pull_request
state: MERGED
merged_at: 2022-09-29T23:53:27Z
url: https://github.com/WordPress/gutenberg/pull/44559
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Type] Code Quality","[Package] Components"]
---

# PR #44559: FontSizePicker: Make control take up full width

- URL: https://github.com/WordPress/gutenberg/pull/44559
- Author: noisysocks
- Created: 2022-09-29T04:50:05Z
- Updated: 2022-10-12T14:11:27Z
- Comments: 3 of 3
- Review threads: 0 of 0
- Changed files: 3 of 3
- Diff: ../pr-diffs/44559.diff

## Body

## What?
Removes the 280px `max-width` from `FontSizePicker` so that it takes up the full width of its parent container.

The Reset button will now also be 40px high when `__size` is set to `__unstable-large`.

Neither of these changes make any practical difference to Gutenberg which only uses `FontSizePicker` in the (narrow) sidebar and always hides the _Reset_ button.

## Why?
- More consistent with most other components in `@wordpress/components` which take up the full width.
- Styling such as this is the responsibility of the caller. `@wordpress/components` shouldn't know what a sidebar is or how wide it is.
- 30px tall Reset button looks weird.

## How?

## Testing Instructions
Storybook:
1. `npm run storybook:dev` 
2. Go to `FontSizePicker`
3. Toggle on _Set custom size_

Gutenberg:
1. `npm run dev`
2. Global Styles → Typography → Text
3. Check that the _Size_ control looks the same as it does in `trunk`

## Issue comments

### noisysocks on 2022-09-29T04:57:32Z

URL: https://github.com/WordPress/gutenberg/pull/44559#issuecomment-1261752804

To me this is a bug but I classified it as an enhancement so that the minor version of `@wordpress/components` is bumped. I don't think it's a significant enough of a change to warrant a major version bump, but nor is it something that I'd want to slip in via a patch bump. I'm not really sure though so let me know how you'd approach this.

### mirka on 2022-10-12T11:18:45Z

URL: https://github.com/WordPress/gutenberg/pull/44559#issuecomment-1275998386

Just for context, we have been [classifying](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#criteria-for-putting-styles-changes-behind-a-feature-flag) changes like this (removing a width constraint) as a substantial enough change to warrant a formal deprecation (e.g. #43230).

For this relatively higher-level component though, there is a much lower chance of consumers using it in arbitrary layouts, so it might be ok.

### ciampo on 2022-10-12T14:11:26Z

URL: https://github.com/WordPress/gutenberg/pull/44559#issuecomment-1276248704

> Just for context, we have been [classifying](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#criteria-for-putting-styles-changes-behind-a-feature-flag) changes like this (removing a width constraint) as a substantial enough change to warrant a formal deprecation (e.g. #43230).
> 
> For this relatively higher-level component though, there is a much lower chance of consumers using it in arbitrary layouts, so it might be ok.

That is my bad for not flagging this as warranting a formal deprecation — sorry!

As you said, this _should_ be ok 🤞  but I'm also fine in case we feel that putting this change under a temporary `__nextUnconstrainedWidth` prop is the right thing to do here

## Review threads

_No review threads._
## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/font-size-picker/index.tsx (MODIFIED, +3/-5)
- packages/components/src/font-size-picker/styles.ts (MODIFIED, +6/-6)
