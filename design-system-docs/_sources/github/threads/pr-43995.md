---
number: 43995
type: pull_request
state: MERGED
merged_at: 2022-09-12T17:01:19Z
url: https://github.com/WordPress/gutenberg/pull/43995
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Type] Code Quality","[Package] Components"]
---

# PR #43995: Flex: Remove flex `gap` polyfill

- URL: https://github.com/WordPress/gutenberg/pull/43995
- Author: mirka
- Created: 2022-09-08T19:01:11Z
- Updated: 2022-09-12T17:01:46Z
- Comments: 1 of 1
- Review threads: 4 of 4
- Changed files: 18 of 18
- Diff: ../pr-diffs/43995.diff

## Body

## What?

Removes the margin-based polyfill implementation of flex `gap`.

## Why?

Flex `gap` is now at an acceptable [adoption](https://caniuse.com/mdn-css_properties_gap_flex_context) percentage.

Using the browser's native flex gap will make our code much simpler, and will avoid complications with child components having legacy style overrides involving margins.

## Testing Instructions

1. `npm run storybook:dev` and check that Flex, VStack, and HStack still work as expected when the `gap` and `wrap` props are set.
2. `npm run dev` and smoke test the editor UI.

There is a good chance of whitespace regressions where there are margin overrides involved. The only one I could find was a regression in CustomSelectControl, which I fixed at the root by removing margin overrides in InputControl.

## Screenshots

This cleanup also fixes a bug in InputControl when `labelPosition='bottom'`:

| Before | After |
|--------|-------|
|<img src="https://user-images.githubusercontent.com/555336/189200561-5dbcb348-7e38-477e-bfef-131aa039c541.png" alt="InputControl with bottom label, before" width="400">|<img src="https://user-images.githubusercontent.com/555336/189200571-68d933c0-a90c-4968-a5ff-3e92fccb0cb7.png" alt="InputControl with bottom label, after" width="400">|

Otherwise, there shouldn't be any visual changes.

## Issue comments

### jasmussen on 2022-09-09T06:40:06Z

URL: https://github.com/WordPress/gutenberg/pull/43995#issuecomment-1241561757

Thanks for working on this, it's a deligt to see so much red code!

## Review threads

### Review thread 1

#### mirka on 2022-09-08T19:08:48Z

URL: https://github.com/WordPress/gutenberg/pull/43995#discussion_r966329025
Path: packages/components/src/flex/flex/hook.ts

```diff
@@ -138,7 +90,6 @@ export function useFlex( props: WordPressComponentProps< FlexProps, 'div' > ) {
 		isReverse,
 		justify,
 		wrap,
-		rtlWatchResult,
```

We don't need to rely on `rtl.watch()` anymore.

### Review thread 2

#### mirka on 2022-09-08T19:10:53Z

URL: https://github.com/WordPress/gutenberg/pull/43995#discussion_r966330633
Path: packages/components/src/input-control/input-base.tsx

```diff
@@ -91,20 +90,19 @@ export function InputBase(
 			{ ...props }
 			{ ...getUIFlexProps( labelPosition ) }
 			className={ className }
+			gap={ 2 }
 			isFocused={ isFocused }
 			labelPosition={ labelPosition }
 			ref={ ref }
 		>
-			<LabelWrapper>
```

This `LabelWrapper` was moved into `Label` itself, so it can be rendered only when the label is visible. Otherwise it stays in the DOM even when the label is visually hidden, and messes with the flex gap.

### Review thread 3

#### mirka on 2022-09-08T19:13:22Z

URL: https://github.com/WordPress/gutenberg/pull/43995#discussion_r966332534
Path: packages/components/src/input-control/styles/input-control-styles.tsx

```diff
@@ -33,33 +33,11 @@ const rootFocusedStyles = ( { isFocused }: RootProps ) => {
 	return css( { zIndex: 1 } );
 };
 
-const rootLabelPositionStyles = ( { labelPosition }: RootProps ) => {
-	switch ( labelPosition ) {
-		case 'top':
-			return css`
-				align-items: flex-start;
-				flex-direction: column;
-			`;
-		case 'bottom':
-			return css`
-				align-items: flex-start;
-				flex-direction: column-reverse;
-			`;
-		case 'edge':
-			return css`
-				justify-content: space-between;
```

This was actually redundant because there was already a hook that added this logic and passed it as props to the Flex component:

https://github.com/WordPress/gutenberg/blob/6987dd87b8c1fa73aac7e0595bb7cdac0d3fee95/packages/components/src/input-control/input-base.tsx#L35-L52

### Review thread 4

#### mirka on 2022-09-08T19:13:45Z

URL: https://github.com/WordPress/gutenberg/pull/43995#discussion_r966332839
Path: packages/components/src/input-control/styles/input-control-styles.tsx

```diff
@@ -70,11 +48,6 @@ const containerDisabledStyles = ( { disabled }: ContainerProps ) => {
 	return css( { backgroundColor } );
 };
 
-// Normalizes the margins from the <Flex /> (components/ui/flex/) container.
-const containerMarginStyles = ( { hideLabel }: ContainerProps ) => {
-	return hideLabel ? css( { margin: '0 !important' } ) : null;
-};
```

🧹 Bye hacks

## Changed files

- packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap (MODIFIED, +3/-12)
- packages/components/CHANGELOG.md (MODIFIED, +2/-0)
- packages/components/src/base-field/test/__snapshots__/index.js.snap (MODIFIED, +3/-5)
- packages/components/src/card/test/__snapshots__/index.tsx.snap (MODIFIED, +20/-32)
- packages/components/src/color-palette/test/__snapshots__/index.js.snap (MODIFIED, +65/-89)
- packages/components/src/dimension-control/test/__snapshots__/index.test.js.snap (MODIFIED, +4/-60)
- packages/components/src/flex/flex/hook.ts (MODIFIED, +5/-54)
- packages/components/src/flex/test/__snapshots__/index.tsx.snap (MODIFIED, +4/-12)
- packages/components/src/h-stack/test/__snapshots__/index.tsx.snap (MODIFIED, +3/-12)
- packages/components/src/input-control/input-base.tsx (MODIFIED, +9/-11)
- packages/components/src/input-control/label.tsx (MODIFIED, +9/-4)
- packages/components/src/input-control/styles/input-control-styles.tsx (MODIFIED, +0/-43)
- packages/components/src/ui/control-group/test/__snapshots__/index.js.snap (MODIFIED, +1/-4)
- packages/components/src/unit-control/test/__snapshots__/index.tsx.snap (MODIFIED, +4/-6)
- packages/components/src/v-stack/test/__snapshots__/index.tsx.snap (MODIFIED, +3/-12)
- packages/edit-post/src/components/preferences-modal/options/test/__snapshots__/enable-custom-fields.js.snap (MODIFIED, +4/-16)
- packages/edit-post/src/components/preferences-modal/test/__snapshots__/index.js.snap (MODIFIED, +2/-8)
- packages/edit-post/src/components/preferences-modal/test/__snapshots__/meta-boxes-section.js.snap (MODIFIED, +3/-12)
