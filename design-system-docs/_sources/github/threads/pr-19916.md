---
number: 19916
type: pull_request
state: MERGED
merged_at: 2020-02-13T09:04:08Z
url: https://github.com/WordPress/gutenberg/pull/19916
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #19916: RangeControls: Improve UI + UX

- URL: https://github.com/WordPress/gutenberg/pull/19916
- Author: ItsJonQ
- Created: 2020-01-27T17:56:22Z
- Updated: 2020-02-27T14:00:44Z
- Comments: 16 of 16
- Review threads: 23 of 23
- Changed files: 17 of 17
- Diff: ../pr-diffs/19916.diff

## Body

## Description
<img width="661" alt="Screen Shot 2020-01-27 at 11 42 09 AM" src="https://user-images.githubusercontent.com/2322354/73198465-069dd080-4101-11ea-9292-78477d39f2ed.png">

This update improves the UI and UX for the `RangeControl` component with `@wordpress/components`.

### Features ✨
* Visual tooltip that displays current value (Tooltip). Follows the "thumb" (circle)
* **Larger hitbox**, making it easier to click/drag
* More consistent UI between slider, input, and Reset button
* Renders step markers with a simple + custom prop (marks)
* Tooltip auto positions top or bottom, based on ideal rendering position
* Can easily change the color of the "track" using a color prop
* Can customize the tooltip's content with a custom renderProp
* Ability to show/hide the accompanying `input[type="number"]`

Example of step marks + Tooltip rendering:

![](https://user-images.githubusercontent.com/2322354/73027347-3bf4a680-3e01-11ea-83b0-a144f12b3fc0.gif)

### A11Y Considerations 💪
* Uses native `input[range]` under the hood, which gives us native keyboard interaction handling
* Uses `aria` roles on elements to emphasize changes / reduce noise (via `aria-hidden`)
* Respects reduce-motion to disable all animations


### Other Considerations ✌️

<img width="792" alt="Screen Shot 2020-01-27 at 12 25 03 PM" src="https://user-images.githubusercontent.com/2322354/73199090-34374980-4102-11ea-81bd-ffaf577654b4.png">

* RTL support
* README file has been updated with additive API changes


## How has this been tested?

![](https://user-images.githubusercontent.com/2322354/73111050-dbd03400-3ed6-11ea-8f10-a1f2070c59ef.gif)

This update was developed within Storybook. When ready, it was integrated and tested with Gutenberg, local dev.

### Voiceover
This component was tested **with Mac OS Voiceover**, to ensure the value change announcements + keyboard interactions worked as expected.

### Mobile

![IMAGE 2020-01-27 11:35:06](https://user-images.githubusercontent.com/2322354/73199567-10c0ce80-4103-11ea-9cb9-fd9214e57fe9.jpg)

Although, this component only updates the **Web** version, the changes were tested on both iOS (iPhone 8) and Android (Samsung A50)

[iPhone 8 Screen Recording](https://d.pr/v/gH5nKS)

Note: The `inputMode` for the number input was set to `decimal`, which allows for the `numpad` keyboard to appear for mobile.

## Types of changes

### CSS

The CSS for the updated component was written with Emotion, leveraging the new/experimental CSS-in-JS system.

### Tooltip

The Tooltip that appears to indicate the value is **not** a `@wordpress/components` one, but rather, a simple/custom implementation.

**Reason**

I had originally tried using the Popover/Tooltip from `@wordpress/components`, but this led to (perceived) performance rendering issues:

https://d.pr/i/a6i3R4

The positioning render cycle was not in sync with the quick changes, which made the experience look and feel really laggy.

As a work-around, I created a simple Tooltip which is currently only being used in the new `RangeControl` component. (I'm open to changing this approach if anyone has suggestions)


### Backwards Compatibility
This update preserves all of the Component APIs. Therefore, it shouldn't break backwards compatibility. It passes all of the previous unit tests and appears to be working as expected when testing within Gutenberg. Component API changes are **additive**.


## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->.

Resolves: https://github.com/WordPress/gutenberg/issues/19845

## Issue comments

### karmatosed on 2020-01-28T22:07:51Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-579481443

This is great!  A few pieces of feedback to talk about and then happy to review.

- Could we have no shadow perhaps as I'm not sure we had before on the circle?
- Are we able to increase the size of the arrows on input, I would love that to be bigger.

### ItsJonQ on 2020-01-29T14:27:55Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-579779854

@karmatosed Haii!

> Could we have no shadow perhaps as I'm not sure we had before on the circle?

Shadow removed. In doing so, I noticed that there was barely any visual feedback to indicate a focus state. I've adjusted it so that the circle has a blue ring (similar to `input` fields) when focused:

<img width="588" alt="Screen Shot 2020-01-29 at 9 06 58 AM" src="https://user-images.githubusercontent.com/2322354/73364812-366fe400-4279-11ea-9073-68e107a5114b.png">

> Are we able to increase the size of the arrows on input, I would love that to be bigger.

That `input` field is currently using an out of the box number `input`. How they render is based on your browser x OS.

To make the arrows bigger, we'd need to do quite a bit of custom UI/CSS work. In which case, I'd advise perhaps updating the base `TextControl` to support this (in a separate PR).

Hope that makes sense!

### karmatosed on 2020-01-29T21:16:01Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-579963295

> To make the arrows bigger, we'd need to do quite a bit of custom UI/CSS work. In which case, I'd advise perhaps updating the base TextControl to support this (in a separate PR).

That makes sense, I would love to get that happening if we can as I think it could benefit us and avoid those tiny arrows.

This all looks great and let's look at getting this in.

### ItsJonQ on 2020-02-06T17:00:53Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583005124

@youknowriad Thanks for your thorough review!! Really appreciate it 😊 

I've made several updates regarding your concerns.

### Reduce motion mixin

I've abstracted the function into a new `reduceMotion` mixin that works just like the SCSS counterpart. There is now a file (`style-mixins.js`) that exports it, along with other utils for CSS-in-JS styling.

### Better RTL style handling

There isn't seamless RTL rendering for CSS-in-JS yet. However, all of the proposed techniques use the same transformation engine under the hood, [CSS Janus](https://github.com/cssjanus/cssjanus) ([4.3kb, 1.6kb gzipped](https://bundlephobia.com/result?p=cssjanus@1.3.2)). I've added it to `@wordpress/components`.

To use it, I've created an `rtl` mixin that can be used in CSS-in-JS for transformations.
It isn't zero overhead, but it's much better than my previous implementation (thanks again for pointing it out).

Let me know what you think

### ~Dashicon~ 👉 `@wordpress/icons`

I kept `Dashicon` for the original PR, as that was the previous implementation. I know there's been a lot of icon work (great job BTW!), so it makes sense for us to make that update. Based on your suggestion, I've updated the code to use `@wordpress/icons` and removed `Dashicon` entirely from `RangeControl`

### ItsJonQ on 2020-02-06T17:14:01Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583011140

@youknowriad Thank you 🙏 !

I'm not sure the best way to proceed with the Icons.

Previously, you could pass a string, and it would render the desired Icon (`Dashicon`).
However, this isn't the case anymore if we want to adhere to the new `<Icon icon={iconComponent} />` API.

I did the wildcard `*` import to preserve that functionality (passing `string` into `beforeIcon` and `afterIcon`). However, you make a very valid point in regards to it nullify'ing the benefits of treeshaking

### youknowriad on 2020-02-06T17:15:14Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583011652

> Previously, you could pass a string, and it would render the desired Icon (Dashicon).

Oh right! I guess we can keep the previous behavior for icons for now until we figure out a sunset plan for Dashicon (See #20003 )

### youknowriad on 2020-02-06T17:16:10Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583012066

<img width="274" alt="Capture d’écran 2020-02-06 à 6 14 04 PM" src="https://user-images.githubusercontent.com/272444/73961328-a7b03680-490c-11ea-8c23-fae905e33741.png">

I noticed a small visual breakage in the font size selector of the paragraph block (the custom input is large). I'm not sure it's entirely related to this branch though.

**Edit** I confirm, it's specific to this branch.

### ItsJonQ on 2020-02-06T17:43:29Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583024383

> Edit I confirm, it's specific to this branch.

@youknowriad 😱 ! I see it too. Investigating 💪

### ItsJonQ on 2020-02-06T17:49:03Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583027009

@youknowriad I just pushed my fix. The issue was the `.components-range-control__number` className was being used ad-hoc outside of `RangeControl`, specifically in `FontSizePicker`.

I've fixed it by adding the style rule back in, with a comment:

```scss
// Keeping this className style rule as it is currently being
// used OUTSIDE of the RangeControl component.
.components-range-control__number {
	display: inline-block;
	margin-left: $grid-size;
	font-weight: 500;
	width: 54px;
}
```

### ItsJonQ on 2020-02-06T19:57:24Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583082309

@youknowriad 

> Module cssjanus has an incompatible license 'Apache-2.0'

Aww. Does that mean we can't use this package?

### ZebulanStanphill on 2020-02-06T20:11:46Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583087992

Just to clarify, Apache 2.0 is compatible with GPLv3, but not earlier versions of the GPL. WordPress is GPLv2+ (which means it could legally switch to GPLv3+ to use this library, but dropping GPLv2 compatibility is probably not a reasonable option here).

If you really want to use this particular library, you could ask the library contributors (there are 12 of them) to consider relicensing or dual-licensing under a license compatible with GPLv2+.

### ItsJonQ on 2020-02-06T20:20:00Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583092085

@ZebulanStanphill Thanks for your insight! I'm going to explore alternative solutions now

### ItsJonQ on 2020-02-06T21:04:13Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583109625

Updates! I took a look at various solutions, and nothing seemed to feel right. I've opted to remove cssjanus, and wrote a tiny albeit naively simple and basic utility.

cc'ing @youknowriad and @aduth for thoughts!

(Sidenote: My Regex game is quite weak 😂 . Please feel free to improve the implementation!)

It's definitely not as seamless as `cssjanus` or the `postcss` auto-generated workflow.
I can see this working short term... but would definitely love something better for the long term.

### youknowriad on 2020-02-07T07:37:27Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583270736

Looks like there's some valid e2e test failures.

### ItsJonQ on 2020-02-07T15:11:18Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583443751

@youknowriad Ah. Looks like I need to update the E2E selectors for `FontSizePicker`, since we renamed them

### ItsJonQ on 2020-02-07T20:30:56Z

URL: https://github.com/WordPress/gutenberg/pull/19916#issuecomment-583597648

Yay! E2E is fixed 😍

## Review threads

### Review thread 1 (resolved)

#### ZebulanStanphill on 2020-01-27T20:29:45Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r371465943
Path: packages/components/src/range-control/README.md

```diff
@@ -152,6 +152,22 @@ If this property is true, a button to reset the the slider is rendered.
 - Required: No
 - Platform: Web | Mobile
 
+#### color
```

What's the use cases for changing the color of the filled range? Wouldn't you just use CSS for this sort of thing?

#### ItsJonQ on 2020-01-27T20:55:33Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r371477282
Path: packages/components/src/range-control/README.md

```diff
@@ -152,6 +152,22 @@ If this property is true, a button to reset the the slider is rendered.
 - Required: No
 - Platform: Web | Mobile
 
+#### color
```

Halloo 👋 @ZebulanStanphill !

That's a good question :). For the components rendering Gutenberg/app UI (the `@wordpress/components` package), I believe customization should be done based on what a component exposes (it's API). 

Sort of like how a `<Button />`'s size can be adjusted with an `isSmall`, rather than using CSS for `height` and so on.

The UI can certainly be adjusted with CSS (any UI could really). But doing so is brittle, as selectors (usually classNames) are subject to change in the future.

In the case of the updated `RangeControl`, I've exposed the (track) colour, which can better accommodate upcoming UI updates (from the G2 initiative), as well as custom block developers.

That being said, we don't have to go with this implementation

Would love to hear what others think :)

#### youknowriad on 2020-01-28T09:25:39Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r371687442
Path: packages/components/src/range-control/README.md

```diff
@@ -152,6 +152,22 @@ If this property is true, a button to reset the the slider is rendered.
 - Required: No
 - Platform: Web | Mobile
 
+#### color
```

While I agree that we need a consistent way to change the color for different controls, this PR is the first that tries to introduce one. 

Options here could be a "theme" prop as well instead of very specific design props. 

So while I'm not against it in principle, I wonder if we should avoid adding these props for now and think about this kind of customization more globally?

#### ItsJonQ on 2020-01-28T21:48:05Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r372076844
Path: packages/components/src/range-control/README.md

```diff
@@ -152,6 +152,22 @@ If this property is true, a button to reset the the slider is rendered.
 - Required: No
 - Platform: Web | Mobile
 
+#### color
```

@youknowriad Oo! A `theme` prop sounds like a good API to encapsulate design props.

> I wonder if we should avoid adding these props for now and think about this kind of customization more globally?

I can omit the addition of this property for now. A global system for handling theming would be lovely, and it's something that [Emotion Theming](https://emotion.sh/docs/theming) supports. 

For instances where you may want to customize the aesthetic of a very specific, the `ThemeProvider` can be added to handle that.

#### ItsJonQ on 2020-01-29T14:28:18Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r372413137
Path: packages/components/src/range-control/README.md

```diff
@@ -152,6 +152,22 @@ If this property is true, a button to reset the the slider is rendered.
 - Required: No
 - Platform: Web | Mobile
 
+#### color
```

@youknowriad Update! For now, I've removed the `color` prop. We can approach theming in another PR.

### Review thread 2

#### youknowriad on 2020-01-30T07:47:50Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r372798701
Path: packages/components/src/range-control/tooltip.js

```diff
@@ -0,0 +1,82 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Tooltip } from './styles/range-control-styles';
+
+const TOOLTIP_OFFSET_HEIGHT = 32;
+
+export default function SimpleTooltip( {
```

We already have a Tooltip component, what's preventing us from using it instead?

#### ItsJonQ on 2020-01-31T19:15:10Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r373640947
Path: packages/components/src/range-control/tooltip.js

```diff
@@ -0,0 +1,82 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Tooltip } from './styles/range-control-styles';
+
+const TOOLTIP_OFFSET_HEIGHT = 32;
+
+export default function SimpleTooltip( {
```

@youknowriad Haii! That's a good question :). I explained it a bit in the description. I'm happy to expand on it further if it helps.

Essentially, the positioning mechanism of the `Tooltip` component (aka. `Popover` under the hood) is visually laggy, as seen here:

https://d.pr/i/a6i3R4

(Note: in that demo, I used `Popover`, but the mechanism is the same)

Because of that, I opted to create a simple Tooltip just for this component. Granted, I don't think that's the ideal solution long term.

Hope this helps!

#### aduth on 2020-02-14T20:26:34Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379629489
Path: packages/components/src/range-control/tooltip.js

```diff
@@ -0,0 +1,82 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Tooltip } from './styles/range-control-styles';
+
+const TOOLTIP_OFFSET_HEIGHT = 32;
+
+export default function SimpleTooltip( {
```

Can we create an issue to track this technical debt?

### Review thread 3 (resolved)

#### youknowriad on 2020-02-04T12:27:28Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r374641058
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -150,28 +151,30 @@ export const ThumbWrapper = styled.span`
 	top: 0;
 	user-select: none;
 
-	[dir=rtl] & {
+	[dir='rtl'] & {
```

is it possible to have bable plugin or something that generates the RTL styles like we do with sass?

#### ItsJonQ on 2020-02-04T17:45:36Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r374823252
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -150,28 +151,30 @@ export const ThumbWrapper = styled.span`
 	top: 0;
 	user-select: none;
 
-	[dir=rtl] & {
+	[dir='rtl'] & {
```

@youknowriad Not at this very moment, although having an automated solution would be lovely!

CSS-in-JS libraries need some way to adjust the CSS compiler output (`stylis`), typically with a Provider of some kind, ([discussion + examples](https://github.com/emotion-js/emotion/issues/793))

### Review thread 4 (resolved)

#### youknowriad on 2020-02-05T08:53:16Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375126015
Path: packages/components/src/range-control/README.md

```diff
@@ -193,6 +229,86 @@ The maximum value accepted. If higher values are inserted onChange will not be c
 - Required: No
 - Platform: Web | Mobile
 
+#### renderTooltipContent
+
+A way to customize the rendered UI of the value. Example:
+
+```jsx
+const customTooltipContent = value => `${value}%`
+
+const MyRangeControl() {
+	return (<RangeControl renderTooltipContent={ customTooltipContent } />)
+}
+```
+
+- Type: `Function`
+- Required: No
+- Platform: Web
+
+#### showTooltip
+
+Forcing the Tooltip UI to show or hide.
+
+- Type: `Boolean`
+- Required: No
+- Platform: Web
+
+#### step
+
+The stepping interval between `min` and `max` values. Step is used both for user interface and validation purposes.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipPosition
+
+Determines the render position of the Tooltip. Either `auto` (default), `top`, or `bottom`.
+
+- Type: `String`
+- Required: No
+- Platform: Web
+
+#### tooltipTimeout
+
+Determines the `timeout` (ms) to hide the Tooltip based on mouse movements.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipZIndex
+
+Determines the CSS `z-index` property of the Tooltip.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
```

Do we really need all these customization options: `tooltiipPosition`, `tooltipTimetout` and `tooltipZIndex`? I think generally speaking for regular design systems, they tend to be very liberal to add props and variations but in the context of @wordpress/components we prefer the opposite because the backward compatibility strategy is stricter than what npm packages do in general.

Maybe we can just provide sane defaults and introduce props if we really get important use-cases for them?

#### ItsJonQ on 2020-02-05T15:58:28Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375345500
Path: packages/components/src/range-control/README.md

```diff
@@ -193,6 +229,86 @@ The maximum value accepted. If higher values are inserted onChange will not be c
 - Required: No
 - Platform: Web | Mobile
 
+#### renderTooltipContent
+
+A way to customize the rendered UI of the value. Example:
+
+```jsx
+const customTooltipContent = value => `${value}%`
+
+const MyRangeControl() {
+	return (<RangeControl renderTooltipContent={ customTooltipContent } />)
+}
+```
+
+- Type: `Function`
+- Required: No
+- Platform: Web
+
+#### showTooltip
+
+Forcing the Tooltip UI to show or hide.
+
+- Type: `Boolean`
+- Required: No
+- Platform: Web
+
+#### step
+
+The stepping interval between `min` and `max` values. Step is used both for user interface and validation purposes.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipPosition
+
+Determines the render position of the Tooltip. Either `auto` (default), `top`, or `bottom`.
+
+- Type: `String`
+- Required: No
+- Platform: Web
+
+#### tooltipTimeout
+
+Determines the `timeout` (ms) to hide the Tooltip based on mouse movements.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipZIndex
+
+Determines the CSS `z-index` property of the Tooltip.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
```

> Maybe we can just provide sane defaults and introduce props if we really get important use-cases for them?

Can do. They already have default values. I'll remove the public facing APIs for these.

### Review thread 5 (resolved)

#### youknowriad on 2020-02-05T08:53:52Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375126252
Path: packages/components/src/range-control/README.md

```diff
@@ -193,6 +229,86 @@ The maximum value accepted. If higher values are inserted onChange will not be c
 - Required: No
 - Platform: Web | Mobile
 
+#### renderTooltipContent
+
+A way to customize the rendered UI of the value. Example:
+
+```jsx
+const customTooltipContent = value => `${value}%`
+
+const MyRangeControl() {
+	return (<RangeControl renderTooltipContent={ customTooltipContent } />)
+}
+```
+
+- Type: `Function`
+- Required: No
+- Platform: Web
+
+#### showTooltip
+
+Forcing the Tooltip UI to show or hide.
+
+- Type: `Boolean`
+- Required: No
+- Platform: Web
+
+#### step
+
+The stepping interval between `min` and `max` values. Step is used both for user interface and validation purposes.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipPosition
+
+Determines the render position of the Tooltip. Either `auto` (default), `top`, or `bottom`.
+
+- Type: `String`
+- Required: No
+- Platform: Web
+
+#### tooltipTimeout
+
+Determines the `timeout` (ms) to hide the Tooltip based on mouse movements.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipZIndex
+
+Determines the CSS `z-index` property of the Tooltip.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### value
+
+The current value of the range slider.
+
+- Type: `Number`
+- Required: Yes
+- Platform: Web | Mobile
+
+#### width
```

Do we need the width prop or can the component be like 100% by default?

#### ItsJonQ on 2020-02-05T15:57:47Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375345053
Path: packages/components/src/range-control/README.md

```diff
@@ -193,6 +229,86 @@ The maximum value accepted. If higher values are inserted onChange will not be c
 - Required: No
 - Platform: Web | Mobile
 
+#### renderTooltipContent
+
+A way to customize the rendered UI of the value. Example:
+
+```jsx
+const customTooltipContent = value => `${value}%`
+
+const MyRangeControl() {
+	return (<RangeControl renderTooltipContent={ customTooltipContent } />)
+}
+```
+
+- Type: `Function`
+- Required: No
+- Platform: Web
+
+#### showTooltip
+
+Forcing the Tooltip UI to show or hide.
+
+- Type: `Boolean`
+- Required: No
+- Platform: Web
+
+#### step
+
+The stepping interval between `min` and `max` values. Step is used both for user interface and validation purposes.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipPosition
+
+Determines the render position of the Tooltip. Either `auto` (default), `top`, or `bottom`.
+
+- Type: `String`
+- Required: No
+- Platform: Web
+
+#### tooltipTimeout
+
+Determines the `timeout` (ms) to hide the Tooltip based on mouse movements.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### tooltipZIndex
+
+Determines the CSS `z-index` property of the Tooltip.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### value
+
+The current value of the range slider.
+
+- Type: `Number`
+- Required: Yes
+- Platform: Web | Mobile
+
+#### width
```

Ah! Good catch. This prop isn't required. Will make that change.

Edit: Misread the code-line reference. The `width` defaults to `100%`. I exposed this to enable the varying rendering `RangeControl`, as depending on implementation, you may not want/need `100%`. However, as you suggested with the Tooltip props, I can remove this.

### Review thread 6

#### youknowriad on 2020-02-06T08:59:55Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375709245
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
```

I see several cases of "blue" usage in this file. Do these adapt to the WordPress admin scheme colors? Should they?
we have the `theme(something)` colors in SCSS to solve that problem there.

#### ItsJonQ on 2020-02-06T14:28:48Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375864752
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
```

@youknowriad These colours correspond to the [(SCSS) based colour values](https://github.com/WordPress/gutenberg/blob/master/packages/base-styles/_colors.scss), but are retrieved via the `color()` util function. The original mappings are here:

https://github.com/WordPress/gutenberg/blob/master/packages/components/src/utils/colors-values.js#L76

#### youknowriad on 2020-02-06T14:55:59Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375881401
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
```

but what is the expectation here, do we want these colors to adapt to WordPress's admin scheme colors or not?

### Review thread 7 (resolved)

#### youknowriad on 2020-02-06T09:01:07Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375709842
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
```

Do we need an RTL style for this?

### Review thread 8 (resolved)

#### youknowriad on 2020-02-06T09:01:15Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375709902
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-left: 16px;
```

Do we need an RTL style for this?

### Review thread 9

#### youknowriad on 2020-02-06T09:02:16Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375710400
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
```

Where are we using Dashicons here? can we replace them with an icon from the icons package?

#### ItsJonQ on 2020-02-06T16:53:52Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375957140
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
```

@youknowriad I preserved the same functionality as the previous version, which uses Dashicon.

I just pushed an update that removes `Dashicon` usage and opts for the new `@wordpress/icon` instead. (Tests have also been updated)

### Review thread 10 (resolved)

#### youknowriad on 2020-02-06T09:03:33Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375710967
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-left: 16px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
+	margin-top: 5px;
+	width: 20px;
+	box-sizing: border-box;
+	display: flex;
+	height: 20px;
+	justify-content: center;
+	outline: 0;
+	position: absolute;
+	pointer-events: none;
+	top: 0;
+	user-select: none;
+
+	[dir='rtl'] & {
+		margin-left: 0;
+		margin-right: -10px;
+	}
+`;
+
+const handleReducedMotion = () => {
```

Feels like something that could be moved to the utils folder (like color) and should we align its implementation with the SASS version we have?

#### ItsJonQ on 2020-02-06T14:31:01Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375866068
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-left: 16px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
+	margin-top: 5px;
+	width: 20px;
+	box-sizing: border-box;
+	display: flex;
+	height: 20px;
+	justify-content: center;
+	outline: 0;
+	position: absolute;
+	pointer-events: none;
+	top: 0;
+	user-select: none;
+
+	[dir='rtl'] & {
+		margin-left: 0;
+		margin-right: -10px;
+	}
+`;
+
+const handleReducedMotion = () => {
```

Ah, good idea 👍

### Review thread 11 (resolved)

#### youknowriad on 2020-02-06T09:04:54Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375711552
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-left: 16px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
+	margin-top: 5px;
+	width: 20px;
+	box-sizing: border-box;
+	display: flex;
+	height: 20px;
+	justify-content: center;
+	outline: 0;
+	position: absolute;
+	pointer-events: none;
+	top: 0;
+	user-select: none;
+
+	[dir='rtl'] & {
+		margin-left: 0;
+		margin-right: -10px;
+	}
+`;
+
+const handleReducedMotion = () => {
+	return css`
+		@media ( prefers-reduced-motion: reduce ) {
+			transition: none !important;
+		}
+	`;
+};
+
+const thumbFocus = ( { isFocused } ) => {
+	return css( {
+		borderColor: isFocused
+			? color( 'blue.medium.focus' )
+			: color( 'darkGray.200' ),
+		boxShadow: isFocused
+			? `
+				0 0 0 1px ${ color( 'blue.medium.focus' ) }
+			`
+			: `
+				0 0 0 rgba(0, 0, 0, 0)
+			`,
+	} );
+};
+
+export const Thumb = styled.span`
+	align-items: center;
+	background-color: white;
+	border-radius: 50%;
+	border: 1px solid ${color( 'darkGray.200' )};
+	box-sizing: border-box;
+	height: 100%;
+	outline: 0;
+	pointer-events: none;
+	position: absolute;
+	user-select: none;
+	width: 100%;
+
+	${thumbFocus};
+`;
+
+export const InputRange = styled.input`
+	box-sizing: border-box;
+	cursor: pointer;
+	display: block;
+	height: 100%;
+	left: 0;
+	margin: 0;
+	opacity: 0;
+	outline: none;
+	position: absolute;
+	right: 0;
+	top: 0;
+	width: 100%;
+`;
+
+const tooltipShow = ( { show } ) => {
+	return css( {
+		opacity: show ? 1 : 0,
+	} );
+};
+
+const tooltipPosition = ( { position } ) => {
+	const isTop = position === 'top';
+
+	if ( isTop ) {
+		return css`
+			margin-top: -4px;
+			top: -100%;
+
+			&::after {
+				border-bottom: none;
+				border-top-style: solid;
+				bottom: -6px;
+			}
+		`;
+	}
+
+	return css`
+		margin-bottom: -4px;
+		bottom: -100%;
+
+		&::after {
+			border-bottom-style: solid;
+			border-top: none;
+			top: -6px;
+		}
+	`;
+};
+
+export const Tooltip = styled.span`
+	background: ${color( 'darkGray.800' )};
+	border-radius: 3px;
+	box-sizing: border-box;
+	color: white;
+	display: inline-block;
+	font-size: 11px;
+	min-width: 32px;
+	opacity: 0;
+	padding: 8px;
+	position: absolute;
+	text-align: center;
+	transform: translateX( -50% );
+	transition: opacity 120ms ease;
+	user-select: none;
+
+	&::after {
+		border: 6px solid ${color( 'darkGray.800' )};
+		border-left-color: transparent;
+		border-right-color: transparent;
+		bottom: -6px;
+		box-sizing: border-box;
+		content: '';
+		height: 0;
+		left: 50%;
+		line-height: 0;
+		margin-left: -6px;
+		position: absolute;
+		width: 0;
+	}
+
+	${tooltipShow};
+	${tooltipPosition};
+	${handleReducedMotion};
+
+	[dir='rtl'] & {
+		transform: translateX( 50% );
+	}
+`;
+
+export const InputNumber = styled.input`
+	box-sizing: border-box;
+	display: inline-block;
+	margin-left: 16px;
+	margin-top: 0;
+	min-width: 54px;
+	max-width: 120px;
+
+	input[type='number']& {
+		${rangeHeight};
+	}
+`;
+
+export const ActionRightWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	margin-left: 8px;
```

Each time I see margin-left (or right or padding), I wonder whether we need RTL equivalents. Maybe we can solve these with mixins for now, otherwise, we'll quickly forget about these.

#### youknowriad on 2020-02-06T17:07:07Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375964956
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,310 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/colors';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+const rootRtl = ( { isRTL } ) => css( { direction: isRTL ? 'rtl' : 'ltr' } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rootRtl}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	margin-left: 10px;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	[dir='rtl'] & {
+		margin-right: 10px;
+	}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-right: 6px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-left: 16px;
+	margin-top: 5px;
+
+	> .dashicon {
+		margin: 0;
+	}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
+	margin-top: 5px;
+	width: 20px;
+	box-sizing: border-box;
+	display: flex;
+	height: 20px;
+	justify-content: center;
+	outline: 0;
+	position: absolute;
+	pointer-events: none;
+	top: 0;
+	user-select: none;
+
+	[dir='rtl'] & {
+		margin-left: 0;
+		margin-right: -10px;
+	}
+`;
+
+const handleReducedMotion = () => {
+	return css`
+		@media ( prefers-reduced-motion: reduce ) {
+			transition: none !important;
+		}
+	`;
+};
+
+const thumbFocus = ( { isFocused } ) => {
+	return css( {
+		borderColor: isFocused
+			? color( 'blue.medium.focus' )
+			: color( 'darkGray.200' ),
+		boxShadow: isFocused
+			? `
+				0 0 0 1px ${ color( 'blue.medium.focus' ) }
+			`
+			: `
+				0 0 0 rgba(0, 0, 0, 0)
+			`,
+	} );
+};
+
+export const Thumb = styled.span`
+	align-items: center;
+	background-color: white;
+	border-radius: 50%;
+	border: 1px solid ${color( 'darkGray.200' )};
+	box-sizing: border-box;
+	height: 100%;
+	outline: 0;
+	pointer-events: none;
+	position: absolute;
+	user-select: none;
+	width: 100%;
+
+	${thumbFocus};
+`;
+
+export const InputRange = styled.input`
+	box-sizing: border-box;
+	cursor: pointer;
+	display: block;
+	height: 100%;
+	left: 0;
+	margin: 0;
+	opacity: 0;
+	outline: none;
+	position: absolute;
+	right: 0;
+	top: 0;
+	width: 100%;
+`;
+
+const tooltipShow = ( { show } ) => {
+	return css( {
+		opacity: show ? 1 : 0,
+	} );
+};
+
+const tooltipPosition = ( { position } ) => {
+	const isTop = position === 'top';
+
+	if ( isTop ) {
+		return css`
+			margin-top: -4px;
+			top: -100%;
+
+			&::after {
+				border-bottom: none;
+				border-top-style: solid;
+				bottom: -6px;
+			}
+		`;
+	}
+
+	return css`
+		margin-bottom: -4px;
+		bottom: -100%;
+
+		&::after {
+			border-bottom-style: solid;
+			border-top: none;
+			top: -6px;
+		}
+	`;
+};
+
+export const Tooltip = styled.span`
+	background: ${color( 'darkGray.800' )};
+	border-radius: 3px;
+	box-sizing: border-box;
+	color: white;
+	display: inline-block;
+	font-size: 11px;
+	min-width: 32px;
+	opacity: 0;
+	padding: 8px;
+	position: absolute;
+	text-align: center;
+	transform: translateX( -50% );
+	transition: opacity 120ms ease;
+	user-select: none;
+
+	&::after {
+		border: 6px solid ${color( 'darkGray.800' )};
+		border-left-color: transparent;
+		border-right-color: transparent;
+		bottom: -6px;
+		box-sizing: border-box;
+		content: '';
+		height: 0;
+		left: 50%;
+		line-height: 0;
+		margin-left: -6px;
+		position: absolute;
+		width: 0;
+	}
+
+	${tooltipShow};
+	${tooltipPosition};
+	${handleReducedMotion};
+
+	[dir='rtl'] & {
+		transform: translateX( 50% );
+	}
+`;
+
+export const InputNumber = styled.input`
+	box-sizing: border-box;
+	display: inline-block;
+	margin-left: 16px;
+	margin-top: 0;
+	min-width: 54px;
+	max-width: 120px;
+
+	input[type='number']& {
+		${rangeHeight};
+	}
+`;
+
+export const ActionRightWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	margin-left: 8px;
```

What about this one, does it need to be wrapped in rtl?

### Review thread 12

#### youknowriad on 2020-02-06T16:58:31Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375959791
Path: packages/components/src/range-control/index.js

```diff
@@ -15,14 +15,14 @@ import {
 	useState,
 	forwardRef,
 } from '@wordpress/element';
+import * as WPIcons from '@wordpress/icons';
```

`import *` should be avoided for the icons package because it breaks tree-shaking causing us to bundle the whole script here. We should only pick the icons we're using.

### Review thread 13 (resolved)

#### youknowriad on 2020-02-06T17:04:49Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375963600
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,288 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rtl( {
```

I think, I'm not sure that the `rtl` mixin is based on the direction of the document. Whicih makes me wonder whether this style here is needed since the direction is already applied above?

#### ItsJonQ on 2020-02-06T17:07:23Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375965073
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,288 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rtl( {
```

Ah, you are correct! I had thought I needed it during early development of this. I just tried removing it, and it works as expected 👍

### Review thread 14 (resolved)

#### youknowriad on 2020-02-06T17:05:41Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375964097
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,288 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rtl( {
+		direction: 'ltr',
+	} )}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
```

Should we remove this one? (as it's added with rtl support down)

### Review thread 15 (resolved)

#### youknowriad on 2020-02-06T17:06:33Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375964631
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,288 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rtl( {
+		direction: 'ltr',
+	} )}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
+	margin-top: 5px;
+	width: 20px;
+	box-sizing: border-box;
+	display: flex;
+	height: 20px;
+	justify-content: center;
+	outline: 0;
+	position: absolute;
+	pointer-events: none;
+	top: 0;
+	user-select: none;
+
+	${rtl( { marginLeft: -10 } )}
+`;
+
+const thumbFocus = ( { isFocused } ) => {
+	return css( {
+		borderColor: isFocused
+			? color( 'blue.medium.focus' )
+			: color( 'darkGray.200' ),
+		boxShadow: isFocused
+			? `
+				0 0 0 1px ${ color( 'blue.medium.focus' ) }
+			`
+			: `
+				0 0 0 rgba(0, 0, 0, 0)
+			`,
+	} );
+};
+
+export const Thumb = styled.span`
+	align-items: center;
+	background-color: white;
+	border-radius: 50%;
+	border: 1px solid ${color( 'darkGray.200' )};
+	box-sizing: border-box;
+	height: 100%;
+	outline: 0;
+	pointer-events: none;
+	position: absolute;
+	user-select: none;
+	width: 100%;
+
+	${thumbFocus};
+`;
+
+export const InputRange = styled.input`
+	box-sizing: border-box;
+	cursor: pointer;
+	display: block;
+	height: 100%;
+	left: 0;
+	margin: 0;
+	opacity: 0;
+	outline: none;
+	position: absolute;
+	right: 0;
+	top: 0;
+	width: 100%;
+`;
+
+const tooltipShow = ( { show } ) => {
+	return css( {
+		opacity: show ? 1 : 0,
+	} );
+};
+
+const tooltipPosition = ( { position } ) => {
+	const isTop = position === 'top';
+
+	if ( isTop ) {
+		return css`
+			margin-top: -4px;
+			top: -100%;
+
+			&::after {
+				border-bottom: none;
+				border-top-style: solid;
+				bottom: -6px;
+			}
+		`;
+	}
+
+	return css`
+		margin-bottom: -4px;
+		bottom: -100%;
+
+		&::after {
+			border-bottom-style: solid;
+			border-top: none;
+			top: -6px;
+		}
+	`;
+};
+
+export const Tooltip = styled.span`
+	background: ${color( 'darkGray.800' )};
+	border-radius: 3px;
+	box-sizing: border-box;
+	color: white;
+	display: inline-block;
+	font-size: 11px;
+	min-width: 32px;
+	opacity: 0;
+	padding: 8px;
+	position: absolute;
+	text-align: center;
+	transition: opacity 120ms ease;
+	user-select: none;
+
+	&::after {
+		border: 6px solid ${color( 'darkGray.800' )};
+		border-left-color: transparent;
+		border-right-color: transparent;
+		bottom: -6px;
+		box-sizing: border-box;
+		content: '';
+		height: 0;
+		left: 50%;
+		line-height: 0;
+		margin-left: -6px;
```

do we need rtl for this?

#### ItsJonQ on 2020-02-06T17:10:45Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375966853
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,288 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+
+	${rtl( {
+		direction: 'ltr',
+	} )}
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
+`;
+
+const markLabelFill = ( { isFilled } ) => {
+	return css( {
+		color: isFilled ? color( 'darkGray.300' ) : color( 'lightGray.600' ),
+	} );
+};
+
+export const MarkLabel = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'lightGray.600' )};
+	left: 0;
+	font-size: 11px;
+	position: absolute;
+	top: 12px;
+	transform: translateX( -50% );
+	white-space: nowrap;
+
+	${markLabelFill};
+`;
+
+export const ThumbWrapper = styled.span`
+	align-items: center;
+	margin-left: -10px;
+	margin-top: 5px;
+	width: 20px;
+	box-sizing: border-box;
+	display: flex;
+	height: 20px;
+	justify-content: center;
+	outline: 0;
+	position: absolute;
+	pointer-events: none;
+	top: 0;
+	user-select: none;
+
+	${rtl( { marginLeft: -10 } )}
+`;
+
+const thumbFocus = ( { isFocused } ) => {
+	return css( {
+		borderColor: isFocused
+			? color( 'blue.medium.focus' )
+			: color( 'darkGray.200' ),
+		boxShadow: isFocused
+			? `
+				0 0 0 1px ${ color( 'blue.medium.focus' ) }
+			`
+			: `
+				0 0 0 rgba(0, 0, 0, 0)
+			`,
+	} );
+};
+
+export const Thumb = styled.span`
+	align-items: center;
+	background-color: white;
+	border-radius: 50%;
+	border: 1px solid ${color( 'darkGray.200' )};
+	box-sizing: border-box;
+	height: 100%;
+	outline: 0;
+	pointer-events: none;
+	position: absolute;
+	user-select: none;
+	width: 100%;
+
+	${thumbFocus};
+`;
+
+export const InputRange = styled.input`
+	box-sizing: border-box;
+	cursor: pointer;
+	display: block;
+	height: 100%;
+	left: 0;
+	margin: 0;
+	opacity: 0;
+	outline: none;
+	position: absolute;
+	right: 0;
+	top: 0;
+	width: 100%;
+`;
+
+const tooltipShow = ( { show } ) => {
+	return css( {
+		opacity: show ? 1 : 0,
+	} );
+};
+
+const tooltipPosition = ( { position } ) => {
+	const isTop = position === 'top';
+
+	if ( isTop ) {
+		return css`
+			margin-top: -4px;
+			top: -100%;
+
+			&::after {
+				border-bottom: none;
+				border-top-style: solid;
+				bottom: -6px;
+			}
+		`;
+	}
+
+	return css`
+		margin-bottom: -4px;
+		bottom: -100%;
+
+		&::after {
+			border-bottom-style: solid;
+			border-top: none;
+			top: -6px;
+		}
+	`;
+};
+
+export const Tooltip = styled.span`
+	background: ${color( 'darkGray.800' )};
+	border-radius: 3px;
+	box-sizing: border-box;
+	color: white;
+	display: inline-block;
+	font-size: 11px;
+	min-width: 32px;
+	opacity: 0;
+	padding: 8px;
+	position: absolute;
+	text-align: center;
+	transition: opacity 120ms ease;
+	user-select: none;
+
+	&::after {
+		border: 6px solid ${color( 'darkGray.800' )};
+		border-left-color: transparent;
+		border-right-color: transparent;
+		bottom: -6px;
+		box-sizing: border-box;
+		content: '';
+		height: 0;
+		left: 50%;
+		line-height: 0;
+		margin-left: -6px;
```

Not needed for this one. The combination of `left: 50%` with `margin-left: -6px` is used for centering. Flipping them to `right` and `margin-right` would yield the same result.

### Review thread 16

#### youknowriad on 2020-02-06T17:49:06Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375987143
Path: packages/components/src/range-control/style.scss

```diff
@@ -0,0 +1,8 @@
+// Keeping this className style rule as it is currently being
+// used OUTSIDE of the RangeControl component.
```

Bad :)

#### youknowriad on 2020-02-06T17:51:00Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375988163
Path: packages/components/src/range-control/style.scss

```diff
@@ -0,0 +1,8 @@
+// Keeping this className style rule as it is currently being
+// used OUTSIDE of the RangeControl component.
```

I did a quick search and I noticed that it's only used in FontSizePicker. 
Maybe we can just move the style there and rename the class to follow our guidelines `components-font-size-picker__number`

#### ItsJonQ on 2020-02-06T17:51:30Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r375988420
Path: packages/components/src/range-control/style.scss

```diff
@@ -0,0 +1,8 @@
+// Keeping this className style rule as it is currently being
+// used OUTSIDE of the RangeControl component.
```

Sounds good to me! I'll do that now

### Review thread 17

#### aduth on 2020-02-14T20:16:46Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379625631
Path: packages/components/src/range-control/README.md

```diff
@@ -160,13 +168,41 @@ If no value exists this prop contains the slider starting position.
 - Required: No
 - Platform: Web | Mobile
 
-#### value
 
-The current value of the range slider.
+#### marks
 
-- Type: `Number`
-- Required: Yes
-- Platform: Web | Mobile
+Renders a visual representation of `step` ticks. Custom mark indicators can be provided by an `Array`.
+
+Example:
+
+```jsx
+const marks = [
+	{
+		value: 0,
+		label: '0',
```

If it's a common use case that the label would be the same as value, could we make it optional and fall back to a stringified `value`?

#### ItsJonQ on 2020-02-27T14:00:44Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r385143008
Path: packages/components/src/range-control/README.md

```diff
@@ -160,13 +168,41 @@ If no value exists this prop contains the slider starting position.
 - Required: No
 - Platform: Web | Mobile
 
-#### value
 
-The current value of the range slider.
+#### marks
 
-- Type: `Number`
-- Required: Yes
-- Platform: Web | Mobile
+Renders a visual representation of `step` ticks. Custom mark indicators can be provided by an `Array`.
+
+Example:
+
+```jsx
+const marks = [
+	{
+		value: 0,
+		label: '0',
```

I'm not sure if it's common yet :).
I can see cases where you may want to render `'10%'` instead

### Review thread 18

#### aduth on 2020-02-14T20:17:58Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379626117
Path: packages/components/src/range-control/README.md

```diff
@@ -193,6 +229,54 @@ The maximum value accepted. If higher values are inserted onChange will not be c
 - Required: No
 - Platform: Web | Mobile
 
+#### renderTooltipContent
+
+A way to customize the rendered UI of the value. Example:
+
+```jsx
+const customTooltipContent = value => `${value}%`
+
+const MyRangeControl() {
+	return (<RangeControl renderTooltipContent={ customTooltipContent } />)
+}
+```
+
+- Type: `Function`
+- Required: No
+- Platform: Web
+
+#### showTooltip
+
+Forcing the Tooltip UI to show or hide.
+
+- Type: `Boolean`
+- Required: No
+- Platform: Web
+
+#### step
+
+The stepping interval between `min` and `max` values. Step is used both for user interface and validation purposes.
+
+- Type: `Number`
+- Required: No
+- Platform: Web
+
+#### value
+
+The current value of the range slider.
+
+- Type: `Number`
+- Required: Yes
+- Platform: Web | Mobile
+
+#### withInputField
+
+Determines if the `input` number field will render next to the RangeControl.
+
+- Type: `Boolean`
+- Required: No
+- Platform: Web
```

Would be good to describe the default here, since it might be unexpected that the default is `true` (that it's an _opt-out_, not an _opt-in_).

### Review thread 19

#### aduth on 2020-02-14T20:19:34Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379626784
Path: packages/components/src/font-size-picker/style.scss

```diff
@@ -5,12 +5,15 @@
 	margin-bottom: $grid-size * 3;
 
 	// Apply the same height as the isSmall Reset button.
-	.components-range-control__number {
+	.components-font-size-picker__number {
+		display: inline-block;
+		font-weight: 500;
```

Why?

### Review thread 20

#### aduth on 2020-02-14T20:21:25Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379627533
Path: packages/components/src/range-control/index.js

```diff
@@ -1,124 +1,345 @@
 /**
  * External dependencies
  */
-import { isFinite } from 'lodash';
 import classnames from 'classnames';
+import { clamp, noop } from 'lodash';
 
 /**
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { compose, withInstanceId, withState } from '@wordpress/compose';
+import {
+	useCallback,
+	useRef,
+	useEffect,
+	useState,
+	forwardRef,
+} from '@wordpress/element';
+import { compose, withInstanceId } from '@wordpress/compose';
 
 /**
  * Internal dependencies
  */
-import { BaseControl, Button, Dashicon } from '../';
-
-function RangeControl( {
-	className,
-	currentInput,
-	label,
-	value,
-	instanceId,
-	onChange,
-	beforeIcon,
-	afterIcon,
-	help,
-	allowReset,
-	initialPosition,
-	min,
-	max,
-	setState,
-	...props
-} ) {
-	const id = `inspector-range-control-${ instanceId }`;
-	const currentInputValue = currentInput === null ? value : currentInput;
-	const resetValue = () => {
-		resetCurrentInput();
-		onChange();
-	};
-	const resetCurrentInput = () => {
-		if ( currentInput !== null ) {
-			setState( {
-				currentInput: null,
-			} );
-		}
-	};
+import BaseControl from '../base-control';
+import Button from '../button';
+import Dashicon from '../dashicon';
 
-	const onChangeValue = ( event ) => {
-		const newValue = event.target.value;
-		// If the input value is invalid temporarily save it to the state,
-		// without calling on change.
-		if ( ! event.target.checkValidity() ) {
-			setState( {
-				currentInput: newValue,
-			} );
-			return;
-		}
-		// The input is valid, reset the local state property used to temporaly save the value,
-		// and call onChange with the new value as a number.
-		resetCurrentInput();
-		onChange( newValue === '' ? undefined : parseFloat( newValue ) );
-	};
+import { color } from '../utils/colors';
+import RangeRail from './rail';
+import SimpleTooltip from './tooltip';
+import {
+	ActionRightWrapper,
+	AfterIconWrapper,
+	BeforeIconWrapper,
+	InputRange,
+	InputNumber,
+	Root,
+	Track,
+	ThumbWrapper,
+	Thumb,
+	Wrapper,
+} from './styles/range-control-styles';
+import { useRtl } from '../utils/rtl';
+
+const BaseRangeControl = forwardRef(
+	(
+		{
+			afterIcon,
+			allowReset = false,
+			beforeIcon,
+			className,
+			color: colorProp = color( 'blue.wordpress.700' ),
+			disabled = false,
+			help,
+			instanceId,
+			initialPosition,
+			label,
+			marks = false,
+			max = 100,
+			min = 0,
+			onBlur = noop,
+			onChange = noop,
+			onFocus = noop,
+			onMouseEnter = noop,
+			onMouseLeave = noop,
+			renderTooltipContent = ( v ) => v,
+			showTooltip: showTooltipProp,
+			step = 1,
+			value: valueProp = 0,
+			withInputField = true,
+			...props
+		},
+		ref
+	) => {
+		const isRTL = useRtl();
+
+		const sliderValue = initialPosition || valueProp;
+		const [ value, setValue ] = useControlledRangeValue( {
+			min,
+			max,
+			value: sliderValue,
+		} );
+		const [ showTooltip, setShowTooltip ] = useState( showTooltipProp );
+		const [ isFocused, setIsFocused ] = useState( false );
+		const originalValueRef = useRef( value );
+
+		const inputRef = useRef();
+
+		const setRef = ( nodeRef ) => {
+			inputRef.current = nodeRef;
+
+			if ( ref ) {
+				ref( nodeRef );
+			}
+		};
+
+		const isThumbFocused = ! disabled && isFocused;
+		const fillValue = ( ( value - min ) / ( max - min ) ) * 100;
+		const fillValueOffset = `${ clamp( fillValue, 0, 100 ) }%`;
+
+		const classes = classnames( 'components-range-control', className );
+
+		const wrapperClasses = classnames(
+			'components-range-control__wrapper',
+			!! marks && 'is-marked'
+		);
+
+		const id = `inspector-range-control-${ instanceId }`;
+
+		const describedBy = !! help ? `${ id }__help` : undefined;
+		const enableTooltip = showTooltipProp !== false;
+
+		const handleOnChange = ( event ) => {
+			if ( ! event.target.checkValidity() ) {
+				return;
+			}
+
+			const nextValue = parseFloat( event.target.value );
+
+			setValue( nextValue );
+			onChange( nextValue );
+		};
+
+		const handleOnReset = () => {
+			const nextValue = originalValueRef.current;
+
+			setValue( nextValue );
+			onChange( nextValue );
+		};
+
+		const handleShowTooltip = () => setShowTooltip( true );
+		const handleHideTooltip = () => setShowTooltip( false );
+
+		const handleOnBlur = ( event ) => {
+			onBlur( event );
+			setIsFocused( false );
+			handleHideTooltip();
+		};
+
+		const handleOnFocus = ( event ) => {
+			onFocus( event );
+			setIsFocused( true );
+			handleShowTooltip();
+		};
+
+		const hoverInteractions = useDebouncedHoverInteraction( {
+			onShow: handleShowTooltip,
+			onHide: handleHideTooltip,
+			onMouseEnter,
+			onMouseLeave,
+		} );
+
+		const offsetStyle = {
+			[ isRTL ? 'right' : 'left' ]: fillValueOffset,
+		};
 
-	const initialFallbackValue = isFinite( initialPosition )
-		? initialPosition
-		: '';
-
-	const initialSliderValue = isFinite( currentInputValue )
-		? currentInputValue
-		: initialFallbackValue;
-
-	return (
-		<BaseControl
-			label={ label }
-			id={ id }
-			help={ help }
-			className={ classnames( 'components-range-control', className ) }
-		>
-			{ beforeIcon && <Dashicon icon={ beforeIcon } /> }
-			<input
-				className="components-range-control__slider"
+		return (
+			<BaseControl
+				className={ classes }
+				label={ label }
 				id={ id }
-				type="range"
-				value={ initialSliderValue }
-				onChange={ onChangeValue }
-				aria-describedby={ !! help ? id + '__help' : undefined }
-				min={ min }
-				max={ max }
-				{ ...props }
-			/>
-			{ afterIcon && <Dashicon icon={ afterIcon } /> }
-			<input
-				className="components-range-control__number"
-				type="number"
-				onChange={ onChangeValue }
-				aria-label={ label }
-				value={ currentInputValue }
-				min={ min }
-				max={ max }
-				onBlur={ resetCurrentInput }
-				{ ...props }
-			/>
-			{ allowReset && (
-				<Button
-					onClick={ resetValue }
-					disabled={ value === undefined }
-					isSmall
-					isSecondary
-					className="components-range-control__reset"
+				help={ help }
+			>
+				<Root
+					className="components-range-control__root"
+					isRTL={ isRTL }
 				>
-					{ __( 'Reset' ) }
-				</Button>
-			) }
-		</BaseControl>
+					{ beforeIcon && (
+						<BeforeIconWrapper>
+							<Dashicon icon={ beforeIcon } />
+						</BeforeIconWrapper>
+					) }
+					<Wrapper
+						className={ wrapperClasses }
+						color={ colorProp }
+						marks={ !! marks }
+					>
+						<InputRange
+							{ ...props }
+							{ ...hoverInteractions }
+							aria-describedby={ describedBy }
+							aria-label={ label }
+							aria-hidden={ false }
+							className="components-range-control__slider"
+							disabled={ disabled }
+							id={ id }
+							max={ max }
+							min={ min }
+							onBlur={ handleOnBlur }
+							onChange={ handleOnChange }
+							onFocus={ handleOnFocus }
+							ref={ setRef }
+							step={ step }
+							tabIndex={ 0 }
+							type="range"
+							value={ value }
+						/>
+						<RangeRail
+							aria-hidden={ true }
+							marks={ marks }
+							max={ max }
+							min={ min }
+							step={ step }
+							value={ value }
+						/>
+						<Track
+							aria-hidden={ true }
+							className="components-range-control__track"
+							style={ { width: fillValueOffset } }
+						/>
+						<ThumbWrapper style={ offsetStyle }>
+							<Thumb
+								aria-hidden={ true }
+								isFocused={ isThumbFocused }
+							/>
+						</ThumbWrapper>
+						{ enableTooltip && (
+							<SimpleTooltip
+								{ ...hoverInteractions }
+								className="components-range-control__tooltip"
+								inputRef={ inputRef }
+								renderTooltipContent={ renderTooltipContent }
+								show={ showTooltip || showTooltip }
+								style={ offsetStyle }
+								value={ value }
+							/>
+						) }
+					</Wrapper>
+					{ afterIcon && (
+						<AfterIconWrapper>
+							<Dashicon icon={ afterIcon } />
+						</AfterIconWrapper>
+					) }
+					{ withInputField && (
+						<InputNumber
+							aria-label={ label }
+							className="components-range-control__number"
+							inputMode="decimal"
+							max={ max }
+							min={ min }
+							onChange={ handleOnChange }
+							step={ step }
+							type="number"
+							value={ value }
+						/>
+					) }
+					{ allowReset && (
+						<ActionRightWrapper>
+							<Button
+								className="components-range-control__reset"
+								disabled={ value === undefined }
+								isSecondary
+								isSmall
+								onClick={ handleOnReset }
+							>
+								{ __( 'Reset' ) }
+							</Button>
+						</ActionRightWrapper>
+					) }
+				</Root>
+			</BaseControl>
+		);
+	}
+);
+
+/**
+ * A float supported clamp function for a specific value.
+ *
+ * @param {number} value The value to clamp
+ * @param {number} min The minimum value
+ * @param {number} max The maxinum value
+ * @return {number} A (float) number
+ */
+function floatClamp( value, min, max ) {
+	return parseFloat( clamp( value, min, max ) );
+}
+
+/**
+ * Hook to store a clamped value, derived from props.
+ */
+function useControlledRangeValue( { min, max, value: valueProp = 0 } ) {
+	const [ value, _setValue ] = useState( floatClamp( valueProp, min, max ) );
+	const valueRef = useRef( value );
+
+	const setValue = useCallback(
+		( nextValue ) => {
+			_setValue( floatClamp( nextValue, min, max ) );
+		},
+		[ _setValue, min, max ]
 	);
+
+	useEffect( () => {
+		if ( valueRef.current !== valueProp ) {
+			setValue( valueProp );
+			valueRef.current = valueProp;
+		}
+	}, [ valueRef, valueProp, setValue ] );
+
+	return [ value, setValue ];
+}
+
+/**
+ * Hook to encapsulate the debouncing "hover" to better handle the showing
+ * and hiding of the Tooltip.
+ */
+function useDebouncedHoverInteraction( {
+	onShow = noop,
+	onHide = noop,
+	onMouseEnter = noop,
+	onMouseLeave = noop,
+	timeout = 250,
+} ) {
+	const [ show, setShow ] = useState( false );
+	const timeoutRef = useRef();
+
+	const handleOnMouseEnter = useCallback( ( event ) => {
+		onMouseEnter( event );
+
+		if ( timeoutRef.current ) {
+			window.clearTimeout( timeoutRef.current );
+		}
+
+		if ( ! show ) {
+			setShow( true );
+			onShow();
+		}
+	}, [] );
+
+	const handleOnMouseLeave = useCallback( ( event ) => {
+		onMouseLeave( event );
+
+		timeoutRef.current = setTimeout( () => {
+			setShow( false );
+			onHide();
+		}, timeout );
+	}, [] );
+
+	return {
+		onMouseEnter: handleOnMouseEnter,
+		onMouseLeave: handleOnMouseLeave,
+	};
 }
 
-export default compose( [
-	withInstanceId,
-	withState( {
-		currentInput: null,
-	} ),
-] )( RangeControl );
+export const RangeControlNext = compose( withInstanceId )( BaseRangeControl );
```

We could have dropped `compose` here rather easily, as it really only provides value when composing multiple:

```suggestion
export const RangeControlNext = withInstanceId( BaseRangeControl );
```

### Review thread 21

#### aduth on 2020-02-14T20:22:03Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379627779
Path: packages/components/src/range-control/stories/index.js

```diff
@@ -1,17 +1,18 @@
 /**
  * External dependencies
  */
-import { number, text } from '@storybook/addon-knobs';
+import styled from '@emotion/styled';
+import { boolean, number, text } from '@storybook/addon-knobs';
 
 /**
  * WordPress dependencies
  */
-import { useState } from '@wordpress/element';
+import { useEffect, useState } from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
-import RangeControl from '../';
+import RangeControl from '../index';
```

Why?

### Review thread 22

#### aduth on 2020-02-14T20:25:32Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379629077
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,287 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
```

I see it's not really documented anywhere (other than ["when in doubt, space it out"](https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/#spacing)), but: I would expect this should be formatted as:

```suggestion
	${ markFill };
```

@jsnajdr Is this something we should/can integrate into `wp-prettier`?

**Edit:** Now I'm confused, because I see the expected spacing [in another example below](https://github.com/WordPress/gutenberg/pull/19916/files#diff-87ed5af7c77464d77ddf8610d16ca500R28). Is this something that Prettier doesn't have opinions about?

#### jsnajdr on 2020-02-25T21:50:10Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r384147420
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,287 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
```

It's a template string with backticks, isn't it? Then Prettier should have an opinion here and should do the `` `${ spaced }` `` formatting.

#### aduth on 2020-02-25T21:52:08Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r384148413
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,287 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
```

@jsnajdr That's what I would expect as well, but it does not appear to be working correctly.

#### jsnajdr on 2020-02-25T22:03:20Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r384154050
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,287 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
```

It looks like a bug in `wp-prettier`. If the template literal is a styled component, with the `styled.span` tag, it's treated specially. Thanks for finding and reporting!

#### jsnajdr on 2020-02-26T11:13:52Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r384427496
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -0,0 +1,287 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils/style-mixins';
+
+const rangeHeight = () => css( { height: 30, minHeight: 30 } );
+
+export const Root = styled.span`
+	-webkit-tap-highlight-color: transparent;
+	box-sizing: border-box;
+	cursor: pointer;
+	align-items: flex-start;
+	display: inline-flex;
+	justify-content: flex-start;
+	padding: 0;
+	position: relative;
+	touch-action: none;
+	width: 100%;
+`;
+
+const wrapperMargin = ( { marks } ) =>
+	css( { marginBottom: marks ? 16 : null } );
+
+export const Wrapper = styled.span`
+	box-sizing: border-box;
+	color: ${color( 'blue.medium.focus' )};
+	display: block;
+	padding-top: 15px;
+	position: relative;
+	width: 100%;
+
+	${rangeHeight};
+	${wrapperMargin};
+
+	${rtl( { marginLeft: 10 } )}
+`;
+
+export const BeforeIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginRight: 6 } )}
+`;
+
+export const AfterIconWrapper = styled.span`
+	margin-top: 3px;
+
+	${rtl( { marginLeft: 16 } )}
+`;
+
+export const Rail = styled.span`
+	background-color: ${color( 'lightGray.600' )};
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	right: 0;
+	display: block;
+	height: 3px;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const Track = styled.span`
+	background-color: currentColor;
+	border-radius: 1px;
+	box-sizing: border-box;
+	height: 3px;
+	pointer-events: none;
+	display: block;
+	position: absolute;
+	margin-top: 14px;
+	top: 0;
+`;
+
+export const MarksWrapper = styled.span`
+	box-sizing: border-box;
+	display: block;
+	position: relative;
+	width: 100%;
+	user-select: none;
+`;
+
+const markFill = ( { isFilled } ) => {
+	return css( {
+		backgroundColor: isFilled ? 'currentColor' : color( 'lightGray.600' ),
+	} );
+};
+
+export const Mark = styled.span`
+	box-sizing: border-box;
+	height: 9px;
+	left: 0;
+	position: absolute;
+	top: -4px;
+	width: 1px;
+
+	${markFill};
```

I created a `wp-prettier` fix in this commit: https://github.com/Automattic/wp-prettier/commit/01fd780c7c5a8edd9e3ef7daea49e650fa42cee5

A fix was needed for all the template literals that have a recognized format (CSS, GraphQL, HTML...) and where the string itself is being formatted.

I usually release such patches when a new upstream version comes out. In this case, I'll be waiting for `prettier@1.19.2`.

### Review thread 23

#### aduth on 2020-02-14T20:32:59Z

URL: https://github.com/WordPress/gutenberg/pull/19916#discussion_r379631860
Path: packages/components/src/utils/rtl.js

```diff
@@ -0,0 +1,58 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/core';
+
+function getRtl() {
+	return !! ( document && document.documentElement.dir === 'rtl' );
+}
+
+/**
+ * Simple hook to retrieve RTL direction value
+ */
+export function useRtl() {
+	return getRtl();
+}
+
+/**
+ * An incredibly basic ltr -> rtl converter for style properties
+ *
+ * @param {Object} ltrStyles
+ * @return {Object} Converted ltr -> rtl styles
+ */
+const convertLtrToRtl = ( ltrStyles = {} ) => {
+	const nextStyles = {};
+
+	for ( const key in ltrStyles ) {
+		const value = ltrStyles[ key ];
+		let nextKey = key;
+		if ( /left/gi.test( key ) ) {
+			nextKey = [ key.replace( 'left', 'right' ) ];
+		}
+		if ( /Left/gi.test( key ) ) {
+			nextKey = [ key.replace( 'Left', 'Right' ) ];
+		}
+		nextStyles[ nextKey ] = value;
+	}
+
+	return nextStyles;
+};
```

Seems like a thing we'd want unit tests for.

I see at least one possible issue: It won't handle anything short-hand that's still relevant for RTL, e.g.

```css
margin: 0 10px 0 0;
```

Also, I'm confused about the reassignment of `nextKey`. It's being replaced into an array? But then the array is being used as the key of an object? An array is not a valid key of an object.

At which point: The `RegExp#test`s are redundant, because `String#replace` will do nothing if the pattern is not found.

Also: `String#replace` only replaces the _first instance_ when provided a string, not all instances. You'd want your regular expression `/left/gi` for that (`key.replace( /left/gi, 'right' `).

## Changed files

- packages/components/src/font-size-picker/index.js (MODIFIED, +5/-5)
- packages/components/src/font-size-picker/style.scss (MODIFIED, +5/-2)
- packages/components/src/range-control/README.md (MODIFIED, +89/-5)
- packages/components/src/range-control/index.js (MODIFIED, +325/-104)
- packages/components/src/range-control/mark.js (ADDED, +49/-0)
- packages/components/src/range-control/rail.js (ADDED, +86/-0)
- packages/components/src/range-control/stories/index.js (MODIFIED, +83/-6)
- packages/components/src/range-control/style.scss (DELETED, +0/-129)
- packages/components/src/range-control/styles/range-control-styles.js (ADDED, +287/-0)
- packages/components/src/range-control/tooltip.js (ADDED, +82/-0)
- packages/components/src/style.scss (MODIFIED, +0/-1)
- packages/components/src/utils/colors-values.js (MODIFIED, +1/-1)
- packages/components/src/utils/reduce-motion.js (ADDED, +31/-0)
- packages/components/src/utils/rtl.js (ADDED, +58/-0)
- packages/components/src/utils/style-mixins.js (ADDED, +3/-0)
- packages/e2e-tests/specs/editor/various/font-size-picker.test.js (MODIFIED, +5/-5)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +2707/-218)
