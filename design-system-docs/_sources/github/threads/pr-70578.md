---
number: 70578
type: pull_request
state: MERGED
merged_at: 2025-07-08T10:14:43Z
url: https://github.com/WordPress/gutenberg/pull/70578
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","First-time Contributor"]
---

# PR #70578: Components: Add `DateCalendar` and `DateRangeCalendar` components

- URL: https://github.com/WordPress/gutenberg/pull/70578
- Author: chihsuan
- Created: 2025-07-01T09:24:20Z
- Updated: 2025-07-10T16:52:40Z
- Comments: 9 of 9
- Review threads: 17 of 17
- Changed files: 24 of 24
- Diff: ../pr-diffs/70578.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- Link this PR to its associated issue with an appropriate keyword: Closes, See, Follow up to, etc. -->

Related to https://github.com/Automattic/wp-calypso/pull/102989

This PR introduces new `DateCalendar` and `DateRangeCalendar` components built on the [react-day-picker](https://daypicker.dev/) library, originally implemented by @ciampo, with the following changes to make it follow the Gutenberg guidelines/patterns:

- Converted CSS modules to standard SCSS: `styles.module.scss` to `style.scss`
- Updated class names from `.calendar__*` to `.components-calendar__*`
- Moved Storybook and tests files from individual component folders to centralized `stories/` and `tests/` directory respectively
- Fixed linting errors
- Fixed unit tests (failed due to Node version differences in Intl API handling)

cc @oandregal @youknowriad 

<!-- In a few words, what is the PR actually doing? -->

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Current limitations: `DatePicker` (@wordpress/components) lacks date range support, while `DateRange` (Calypso) only handles ranges and requires moment.js.

Create a unified Calendar component that supports both single dates and date ranges, with flexible layout options.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->


## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

1. Start the storybook
2. Navigate to the `DateCalendar` and `DateRangeCalendar` stories

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

<!-- If you would like to upload screenshots, feel free to use the table below when it is useful to show the difference between before and after the change. -->

<img width="1280" alt="Screenshot 2025-07-01 at 15 48 25" src="https://github.com/user-attachments/assets/8c3c534f-9f6d-4531-9a21-547afb3dd5fe" />

<img width="1273" alt="Screenshot 2025-07-01 at 15 48 33" src="https://github.com/user-attachments/assets/2daf9f53-95ed-4e68-aa19-0be32214a0b2" />


https://github.com/user-attachments/assets/af3138e6-90f3-4e48-9194-b7c11da84ae9

## Issue comments

### github-actions on 2025-07-01T09:24:29Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3022937393

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: chihsuan <chihsuan@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2025-07-01T09:24:53Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3022939227

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @chihsuan! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### ciampo on 2025-07-03T13:47:04Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3032344048

The styles also seem to be off compared to the original copy of the component:

| original copy | this branch |
|---|---|
| <video src="https://github.com/user-attachments/assets/dda025d7-ed85-474c-b1d0-734e1f62ec3d" /> | <video src="https://github.com/user-attachments/assets/731de36e-4886-4cdb-8e27-145a615dcf99" /> |

- missing focus styles for previous/next month buttons
- wrong focus styles for the calendar days
- missing background color for the dates selected in a range
- wrong dashed border color when previewing the date selection

Likely, something broke either when refactoring classnames and selectors, or when refactoring colro variables.

### chihsuan on 2025-07-04T03:54:22Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3034352249

> Thank you, @chihsuan!
> 
> Before exporting the component from the package (thus making it a public API), I wonder if we should test the component in real-case scenario first — unless you're already using this component in WooCommerce?
> 

Thanks for the detailed review! @ciampo. As discussed in Slack, we're using this component in WooCommerce Analytics and don’t have any issues with it.

> _(One small note — for ease of review, it would have been better to have an initial commit only containing the raw pasted code. It would have made it easier to distinguish the actual code changes added in Gutenberg compared to the original component.)_
 
I apologize for not clarifying this in the PR description. The initial commit does contain only the raw code without any modifications.

### chihsuan on 2025-07-04T04:00:15Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3034359938

> The styles also seem to be off compared to the original copy of the component:
> 
> original copy	this branch
>  Kapture.2025-07-03.at.15.41.31.mp4 
>  Kapture.2025-07-03.at.15.42.43.mp4 
> * missing focus styles for previous/next month buttons
> * wrong focus styles for the calendar days
> * missing background color for the dates selected in a range
> * wrong dashed border color when previewing the date selection
> 
> Likely, something broke either when refactoring classnames and selectors, or when refactoring colro variables.

Good catch! @ciampo  Yes, color variables were not set correctly.

I've fixed it in cf6c3d5568bfc5d09001f49eace2bf44accb9e5c.

<img width="874" alt="Screenshot 2025-07-04 at 11 58 11" src="https://github.com/user-attachments/assets/acbca0c7-4624-4c38-b9ca-bb07553f807e" />

https://github.com/user-attachments/assets/6de889f8-7471-4e36-be3f-e075a6eeb22d

### chihsuan on 2025-07-07T04:02:38Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3043408964

Thanks, @ciampo, I've made the suggested changes. 🙇‍♂️

### fabiankaegy on 2025-07-08T10:26:30Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3048312115

I added the "Needs Dev Note" Label because I think this is a great new component we should defiantly highlight when it ships in WordPress Core  👍 :)

### chihsuan on 2025-07-09T02:28:36Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3050902881

> Would you be able to explore/iterate on [#70578 (comment)](https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182557264) in a follow-up PR?

Yes, I can help with that. 🙂

### ciampo on 2025-07-10T16:52:13Z

URL: https://github.com/WordPress/gutenberg/pull/70578#issuecomment-3058228079

@fabiankaegy I'm removing the "needs dev note" label for now, as I think we want to be a bit more cautious with the way we introduce the component — in #70681 I'm changing it to be a private API. Happy to add the label back in case the component is made public, though!

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2025-07-03T11:05:48Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182504283
Path: packages/components/src/calendar/date-calendar/test/index.tsx

```diff
@@ -194,7 +233,7 @@ describe( 'DateCalendar', () => {
 			[ 'Controlled', ControlledDateCalendar ],
 		] )( '[`%s`]', ( _mode, Component ) => {
 			it( 'should select a date when a date button is clicked', async () => {
-				const user = setupUserEvent();
+				const user = await setupUserEvent();
```

The `setupUserEvent` is not asynchronous, and therefore it shouldn't require to be `await`ed — what's the reason for adding `await`? (across both test files)

#### chihsuan on 2025-07-04T03:28:13Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184228262
Path: packages/components/src/calendar/date-calendar/test/index.tsx

```diff
@@ -194,7 +233,7 @@ describe( 'DateCalendar', () => {
 			[ 'Controlled', ControlledDateCalendar ],
 		] )( '[`%s`]', ( _mode, Component ) => {
 			it( 'should select a date when a date button is clicked', async () => {
-				const user = setupUserEvent();
+				const user = await setupUserEvent();
```

Thanks for raising this! You're right that `setupUserEvent()` itself isn’t async. However, the ESLint warning comes from the `testing-library/await-async-events` rule, which expects the return value from `setupUserEvent()` to be awaited if the methods it returns (like .click) are async. The linting tool automatically added the `await` keyword to ensure the expected behavior.

Alternatively, we could suppress the lint warning with a comment, but there are too many of these in the codebase.

<img width="922" alt="Screenshot 2025-07-04 at 11 18 20" src="https://github.com/user-attachments/assets/2be84b5e-b1bc-44b1-91f0-f0f71aa773ef" />

#### ciampo on 2025-07-04T09:53:31Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184905310
Path: packages/components/src/calendar/date-calendar/test/index.tsx

```diff
@@ -194,7 +233,7 @@ describe( 'DateCalendar', () => {
 			[ 'Controlled', ControlledDateCalendar ],
 		] )( '[`%s`]', ( _mode, Component ) => {
 			it( 'should select a date when a date button is clicked', async () => {
-				const user = setupUserEvent();
+				const user = await setupUserEvent();
```

This looks like a false positive, since the [ESLint rule](https://github.com/testing-library/eslint-plugin-testing-library/blob/main/docs/rules/await-async-events.md) is targeted at async events, while `userEvent.setup()` is a synchronous method.

After doing some digging, it looks like [tweaking the `setupUserEvent` function fixes the false-positive](https://github.com/testing-library/eslint-plugin-testing-library/issues/881).  If we tweak it to:

```
function setupUserEvent() {
	// The `advanceTimersByTime` is needed since we're using jest
	// fake timers to simulate a fixed date for tests.
	const user = userEvent.setup( { advanceTimers: jest.advanceTimersByTime } );
	return user;
}
```

we should be able to also remove all the `await`s too.

### Review thread 2 (resolved)

#### ciampo on 2025-07-03T11:09:00Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182510170
Path: packages/components/src/calendar/utils/use-controlled-value.ts

```diff
@@ -20,6 +23,10 @@ type Props< T > = {
  * regardless of controlled / uncontrolled. It is expected that the consumer
  * of the component will handle setting the value to `null` to indicate no date
  * selected in controlled mode.
+ * @param root0
+ * @param root0.defaultValue
+ * @param root0.onChange
+ * @param root0.value
```

Can we avoid this code change, or otherwise document it better?

#### chihsuan on 2025-07-04T03:40:53Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184245789
Path: packages/components/src/calendar/utils/use-controlled-value.ts

```diff
@@ -20,6 +23,10 @@ type Props< T > = {
  * regardless of controlled / uncontrolled. It is expected that the consumer
  * of the component will handle setting the value to `null` to indicate no date
  * selected in controlled mode.
+ * @param root0
+ * @param root0.defaultValue
+ * @param root0.onChange
+ * @param root0.value
```

These JSDoc block lines were added to fix ESLint warnings. I've updated parameter documentation for clarity by renaming `root0` to `props` and adjusting the parameter descriptions accordingly in be1c1f19c30c1d556c8c71ca232328ae60759af0.

```
Expected JSDoc block lines to be aligned.eslint[jsdoc/check-line-alignment](https://github.com/gajus/eslint-plugin-jsdoc/blob/main/docs/rules/check-line-alignment.md#repos-sticky-header)
Missing JSDoc @param "root0" declaration.eslintjsdoc/require-param
Missing JSDoc @param "root0.defaultValue" declaration.eslint[jsdoc/require-param](https://github.com/gajus/eslint-plugin-jsdoc/blob/main/docs/rules/require-param.md#repos-sticky-header)
Missing JSDoc @param "root0.onChange" declaration.eslintjsdoc/require-param
Missing JSDoc @param "root0.value" declaration.eslint[jsdoc/require-param](https://github.com/gajus/eslint-plugin-jsdoc/blob/main/docs/rules/require-param.md#repos-sticky-header)
```

#### ciampo on 2025-07-04T09:53:43Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184905701
Path: packages/components/src/calendar/utils/use-controlled-value.ts

```diff
@@ -20,6 +23,10 @@ type Props< T > = {
  * regardless of controlled / uncontrolled. It is expected that the consumer
  * of the component will handle setting the value to `null` to indicate no date
  * selected in controlled mode.
+ * @param root0
+ * @param root0.defaultValue
+ * @param root0.onChange
+ * @param root0.value
```

Great, thanks!

### Review thread 3 (resolved)

#### ciampo on 2025-07-03T11:11:55Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182515028
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,19 +1,16 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+.components-calendar {
 	// TODO: add font family rule when Theme is ready
 
 	// Internal variables
-	--a8c-calendar-outline-focus: 1.5px solid #{theme.$components-color-accent};
-	--a8c-calendar-button-height: 2rem;
-	--a8c-calendar-button-width: 2rem;
-	--a8c-calendar-nav-height: 2rem;
+	--components-calendar-outline-focus: 1.5px solid $components-color-accent;
+	--components-calendar-button-height: 2rem;
+	--components-calendar-button-width: 2rem;
+	--components-calendar-nav-height: 2rem;
 
 	// TODO: themify / align to DS tokens
-	--a8c-calendar-range-middle-background-color: color-mix(in srgb, #{theme.$components-color-accent} 4%, transparent);
-	--a8c-calendar-preview-border-color: color-mix(in srgb, #{theme.$components-color-accent} 16%, transparent);
+	--components-calendar-range-middle-background-color: color-mix(in srgb, $components-color-accent 4%, transparent);
+	--components-calendar-preview-border-color: color-mix(in srgb, $components-color-accent 16%, transparent);
```

Given this is a Sass file, we can refactor these internal variables to sass variables.

Also, for better namespacing, I'd prefix the variables with `wp-components`, and not only `components`.

#### chihsuan on 2025-07-04T04:00:45Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184272280
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,19 +1,16 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+.components-calendar {
 	// TODO: add font family rule when Theme is ready
 
 	// Internal variables
-	--a8c-calendar-outline-focus: 1.5px solid #{theme.$components-color-accent};
-	--a8c-calendar-button-height: 2rem;
-	--a8c-calendar-button-width: 2rem;
-	--a8c-calendar-nav-height: 2rem;
+	--components-calendar-outline-focus: 1.5px solid $components-color-accent;
+	--components-calendar-button-height: 2rem;
+	--components-calendar-button-width: 2rem;
+	--components-calendar-nav-height: 2rem;
 
 	// TODO: themify / align to DS tokens
-	--a8c-calendar-range-middle-background-color: color-mix(in srgb, #{theme.$components-color-accent} 4%, transparent);
-	--a8c-calendar-preview-border-color: color-mix(in srgb, #{theme.$components-color-accent} 16%, transparent);
+	--components-calendar-range-middle-background-color: color-mix(in srgb, $components-color-accent 4%, transparent);
+	--components-calendar-preview-border-color: color-mix(in srgb, $components-color-accent 16%, transparent);
```

Sounds good! I've refactored these to sass variables and prefixed them with `wp-components`. cf6c3d5568bfc5d09001f49eace2bf44accb9e5c

#### ciampo on 2025-07-04T09:53:54Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184905992
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,19 +1,16 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+.components-calendar {
 	// TODO: add font family rule when Theme is ready
 
 	// Internal variables
-	--a8c-calendar-outline-focus: 1.5px solid #{theme.$components-color-accent};
-	--a8c-calendar-button-height: 2rem;
-	--a8c-calendar-button-width: 2rem;
-	--a8c-calendar-nav-height: 2rem;
+	--components-calendar-outline-focus: 1.5px solid $components-color-accent;
+	--components-calendar-button-height: 2rem;
+	--components-calendar-button-width: 2rem;
+	--components-calendar-nav-height: 2rem;
 
 	// TODO: themify / align to DS tokens
-	--a8c-calendar-range-middle-background-color: color-mix(in srgb, #{theme.$components-color-accent} 4%, transparent);
-	--a8c-calendar-preview-border-color: color-mix(in srgb, #{theme.$components-color-accent} 16%, transparent);
+	--components-calendar-range-middle-background-color: color-mix(in srgb, $components-color-accent 4%, transparent);
+	--components-calendar-preview-border-color: color-mix(in srgb, $components-color-accent 16%, transparent);
```

🙏

### Review thread 4 (resolved)

#### ciampo on 2025-07-03T11:15:33Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182521945
Path: packages/components/src/calendar/test/date-range-calendar.tsx

```diff
@@ -1505,10 +1515,14 @@ describe( 'DateRangeCalendar', () => {
 	} );
 
 	describe( 'usePreviewRange', () => {
-		today = new Date( '2024-03-15' );
-		tomorrow = addDays( today, 1 );
-		yesterday = subDays( today, 1 );
-		const nextWeek = addDays( today, 7 );
+		let nextWeek: Date;
+
+		beforeEach( () => {
+			today = new Date( '2024-03-15' );
+			tomorrow = addDays( today, 1 );
+			yesterday = subDays( today, 1 );
+			nextWeek = addDays( today, 7 );
+		} );
```

Why was this change necessary (re-defining the variables before each test, instead of having them as constants)?

#### chihsuan on 2025-07-04T03:31:42Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184237249
Path: packages/components/src/calendar/test/date-range-calendar.tsx

```diff
@@ -1505,10 +1515,14 @@ describe( 'DateRangeCalendar', () => {
 	} );
 
 	describe( 'usePreviewRange', () => {
-		today = new Date( '2024-03-15' );
-		tomorrow = addDays( today, 1 );
-		yesterday = subDays( today, 1 );
-		const nextWeek = addDays( today, 7 );
+		let nextWeek: Date;
+
+		beforeEach( () => {
+			today = new Date( '2024-03-15' );
+			tomorrow = addDays( today, 1 );
+			yesterday = subDays( today, 1 );
+			nextWeek = addDays( today, 7 );
+		} );
```

It's not allowed to declare these variables inside this block because they are already declared in the upper scope.

And these variables are re-assigned in some tests, so I use `beforeEach` instead of `before` to ensure tests pass consistently. An alternative approach would be to use different names for the variables; however, that would require modifying the tests directly.

```
'today' is already declared in the upper scope on line 84 column 6.eslint[@typescript-eslint/no-shadow](https://typescript-eslint.io/rules/no-shadow)
```

#### ciampo on 2025-07-04T11:04:40Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2185068013
Path: packages/components/src/calendar/test/date-range-calendar.tsx

```diff
@@ -1505,10 +1515,14 @@ describe( 'DateRangeCalendar', () => {
 	} );
 
 	describe( 'usePreviewRange', () => {
-		today = new Date( '2024-03-15' );
-		tomorrow = addDays( today, 1 );
-		yesterday = subDays( today, 1 );
-		const nextWeek = addDays( today, 7 );
+		let nextWeek: Date;
+
+		beforeEach( () => {
+			today = new Date( '2024-03-15' );
+			tomorrow = addDays( today, 1 );
+			yesterday = subDays( today, 1 );
+			nextWeek = addDays( today, 7 );
+		} );
```

Got it — I guess I'd almost prefer using different variables to completely avoid the risk that the order in which tests run may affect the test result.

### Review thread 5 (resolved)

#### ciampo on 2025-07-03T11:17:50Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182528165
Path: packages/components/src/calendar/style.scss

```diff
@@ -15,8 +15,8 @@
 	position: relative; /* Required to position the navigation toolbar. */
 	box-sizing: border-box;
 	display: inline flow-root;
-	color: colors.$gray-900;
-	background-color: colors.$white;
+	color: $gray-900;
```

@mirka can you confirm that this is still the correct way to consume Sass variables? (ie. without `@use` at the top of the file and namespace (ie. `colors.$gray-900` )?

#### ciampo on 2025-07-04T09:54:17Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184906871
Path: packages/components/src/calendar/style.scss

```diff
@@ -15,8 +15,8 @@
 	position: relative; /* Required to position the navigation toolbar. */
 	box-sizing: border-box;
 	display: inline flow-root;
-	color: colors.$gray-900;
-	background-color: colors.$white;
+	color: $gray-900;
```

Looking at the rest of the components package, this seems indeeed still the correct way to consume Sass variables.

#### mirka on 2025-07-09T22:21:09Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2196100943
Path: packages/components/src/calendar/style.scss

```diff
@@ -15,8 +15,8 @@
 	position: relative; /* Required to position the navigation toolbar. */
 	box-sizing: border-box;
 	display: inline flow-root;
-	color: colors.$gray-900;
-	background-color: colors.$white;
+	color: $gray-900;
```

`@use` is the correct way. Just, we haven't done the migration work yet 🙃

### Review thread 6

#### ciampo on 2025-07-03T11:34:41Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182557264
Path: packages/components/src/calendar/utils/use-controlled-value.ts

```diff
@@ -0,0 +1,61 @@
+/**
+ * WordPress dependencies
+ */
+/**
+ * External dependencies
+ */
+import { useState, useCallback } from '@wordpress/element';
+
+type Props< T > = {
+	defaultValue?: T;
+	value?: T | null | undefined;
+	onChange?: ( newValue: T, ...args: any[] ) => void;
+};
+
+/**
+ * Handles controlled and uncontrolled state for the selected calendar value.
+ * It is assumed that the `value` prop is controlled when it's not undefined:
+ * - initial date selected, uncontrolled: use a non-undefined`defaultValue`
+ * - initial date selected, controlled: use a non-undefined `value`
+ * - no date selected, controlled: set `value` to `null`
+ *
+ * The `onChange` prop will return `undefined` when no date is selected,
+ * regardless of controlled / uncontrolled. It is expected that the consumer
+ * of the component will handle setting the value to `null` to indicate no date
+ * selected in controlled mode.
+ * @param root0
+ * @param root0.defaultValue
+ * @param root0.onChange
+ * @param root0.value
+ */
+export function useControlledValue< T >( {
```

As a follow-up, we should try to re-use the existing `useControlledValue` hook in the package https://github.com/WordPress/gutenberg/blob/2dd93a3b957906412f6b4d8c547d8326f2ba8b78/packages/components/src/calendar/utils/use-controlled-value.ts

### Review thread 7 (resolved)

#### ciampo on 2025-07-03T11:42:59Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182571977
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -963,7 +973,7 @@ describe( 'DateCalendar', () => {
 			render(
 				<DateCalendar
 					defaultSelected={ tomorrowAtMidnightInTokyo }
-					timeZone="-02:00"
+					timeZone="Etc/GMT+2"
```

My preference would be to change the jest settings so that the tests always run in the `UTC` timezone, instead of twaking the test code — you can refer to [this comment](https://github.com/Automattic/wp-calypso/pull/103676/files#r2105315544) to see how I achieved it in the Calypso repository.

#### chihsuan on 2025-07-04T03:35:37Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184242032
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -963,7 +973,7 @@ describe( 'DateCalendar', () => {
 			render(
 				<DateCalendar
 					defaultSelected={ tomorrowAtMidnightInTokyo }
-					timeZone="-02:00"
+					timeZone="Etc/GMT+2"
```

I modified this value because Node.js 20 treats `-02:00` as an invalid timezone format. This change was necessary due to Node.js version compatibility.

```
node -p "new Intl.DateTimeFormat('en-US', { dateStyle: 'long', timeZone: '-02:00' }).format(new Date())"
[eval]:1
new Intl.DateTimeFormat('en-US', { dateStyle: 'long', timeZone: '-02:00' }).format(new Date())
^

RangeError: Invalid time zone specified: -02:00
    at new DateTimeFormat (<anonymous>)
    at [eval]:1:1
    at runScriptInThisContext (node:internal/vm:209:10)
    at node:internal/process/execution:118:14
    at [eval]-wrapper:6:24
    at runScript (node:internal/process/execution:101:62)
    at evalScript (node:internal/process/execution:133:3)
    at node:internal/main/eval_string:51:3

Node.js v20.14.0
```

#### ciampo on 2025-07-04T10:00:36Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184919702
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -963,7 +973,7 @@ describe( 'DateCalendar', () => {
 			render(
 				<DateCalendar
 					defaultSelected={ tomorrowAtMidnightInTokyo }
-					timeZone="-02:00"
+					timeZone="Etc/GMT+2"
```

Got it. In that case, let's leave an inline comment about it for future reference — something like `/Note: using "Etc/GMT+2" instead of "-02:00" because support for raw offsets was introduced in Node v22 (while currently the repository still targets Node v20).` — in both test files

### Review thread 8 (resolved)

#### ciampo on 2025-07-03T12:14:34Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182633556
Path: packages/components/src/calendar/stories/date-calendar.story.tsx

```diff
@@ -0,0 +1,220 @@
+/**
+ * External dependencies
+ */
+import { fn } from '@storybook/test';
+import {
+	enUS,
+	fr,
+	es,
+	de,
+	it,
+	he,
+	ru,
+	ja,
+	ptBR,
+	nl,
+	ko,
+	tr,
+	id,
+	zhCN,
+	zhTW,
+	ar,
+	sv,
+} from 'date-fns/locale';
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { DateCalendar, TZDate } from '..';
+import type { Meta, StoryObj } from '@storybook/react';
+
+const meta: Meta< typeof DateCalendar > = {
+	title: 'Components/DateCalendar',
```

```suggestion
	title: 'Components/Selection & Input/Time & Date/DateCalendar',
```

### Review thread 9 (resolved)

#### ciampo on 2025-07-03T12:14:48Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2182634003
Path: packages/components/src/calendar/stories/date-range-calendar.story.tsx

```diff
@@ -0,0 +1,230 @@
+/**
+ * External dependencies
+ */
+import { fn } from '@storybook/test';
+import {
+	enUS,
+	fr,
+	es,
+	de,
+	it,
+	he,
+	ru,
+	ja,
+	ptBR,
+	nl,
+	ko,
+	tr,
+	id,
+	zhCN,
+	zhTW,
+	ar,
+	sv,
+} from 'date-fns/locale';
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { DateRangeCalendar, TZDate } from '..';
+import type { Meta, StoryObj } from '@storybook/react';
+
+const meta: Meta< typeof DateRangeCalendar > = {
+	title: 'Components/DateRangeCalendar',
```

```suggestion
	title: 'Components/Selection & Input/Time & Date/DateRangeCalendar',
```

### Review thread 10 (resolved)

#### ciampo on 2025-07-04T09:31:57Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184858787
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,52 +1,49 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+// Internal variables
+$wp-components-calendar-outline-focus: 1.5px solid $components-color-accent;
```

There should be a reusable variable (or mixin) in Gutenbegr for focus outlines

#### chihsuan on 2025-07-07T03:29:21Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188893457
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,52 +1,49 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+// Internal variables
+$wp-components-calendar-outline-focus: 1.5px solid $components-color-accent;
```

After examining the existing styles, I can see that different components implement focus outlines in varying ways.

The existing mixins don't quite match our existing styles. 🤔 

https://github.com/WordPress/gutenberg/blob/309338a3bc19eff8010e4d7e8eb9193f4d67c138/packages/base-styles/_mixins.scss#L158-L180


I also found there is a `--wp-admin-border-width-focus` variable that I think we could use here. What do you think?

https://github.com/WordPress/gutenberg/blob/309338a3bc19eff8010e4d7e8eb9193f4d67c138/packages/base-styles/_mixins.scss#L524-L530

#### chihsuan on 2025-07-07T04:02:53Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188919244
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,52 +1,49 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+// Internal variables
+$wp-components-calendar-outline-focus: 1.5px solid $components-color-accent;
```

Changed in 5116112c6cfac82b12b4e6cda4ff961a5430378a

#### ciampo on 2025-07-08T09:54:45Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2192030373
Path: packages/components/src/calendar/style.scss

```diff
@@ -1,52 +1,49 @@
-@use "@wordpress/base-styles/colors";
-@use '../utils/theme-variables' as theme;
-
 /* Root of the component. */
-.calendar {
+// Internal variables
+$wp-components-calendar-outline-focus: 1.5px solid $components-color-accent;
```

Makes sense, we definitely want to use that variable

### Review thread 11 (resolved)

#### ciampo on 2025-07-04T09:35:14Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184866273
Path: packages/components/src/calendar/utils/use-controlled-value.ts

```diff
@@ -1,7 +1,10 @@
 /**
  * WordPress dependencies
  */
-import { useState, useCallback } from 'react';
+/**
+ * External dependencies
+ */
+import { useState, useCallback } from '@wordpress/element';
```

Probably an auto-suggest hiccup

```suggestion
import { useState, useCallback } from '@wordpress/element';
```

### Review thread 12 (resolved)

#### ciampo on 2025-07-04T09:35:54Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184867790
Path: packages/components/src/calendar/utils/use-localization-props.ts

```diff
@@ -1,5 +1,14 @@
+/**
+ * WordPress dependencies
+ */
 import { __, sprintf } from '@wordpress/i18n';
-import { useMemo } from 'react';
+/**
+ * External dependencies
+ */
+import { useMemo } from '@wordpress/element';
```

Same here

```suggestion
import { useMemo } from '@wordpress/element';
```

### Review thread 13 (resolved)

#### ciampo on 2025-07-04T09:39:21Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184876125
Path: packages/components/src/calendar/stories/date-calendar.story.tsx

```diff
@@ -18,12 +21,18 @@ import {
 	ar,
 	sv,
 } from 'date-fns/locale';
-import { useState, useEffect } from 'react';
-import { DateCalendar, TZDate } from '../';
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { DateCalendar, TZDate } from '..';
 import type { Meta, StoryObj } from '@storybook/react';
```

Imports from `@storybook/react` should be moved under "External dependencies"

#### chihsuan on 2025-07-07T03:07:19Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188876925
Path: packages/components/src/calendar/stories/date-calendar.story.tsx

```diff
@@ -18,12 +21,18 @@ import {
 	ar,
 	sv,
 } from 'date-fns/locale';
-import { useState, useEffect } from 'react';
-import { DateCalendar, TZDate } from '../';
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { DateCalendar, TZDate } from '..';
 import type { Meta, StoryObj } from '@storybook/react';
```

Done. d3ddf3c3d7bfe131054451fd61a7934778289efd

### Review thread 14 (resolved)

#### ciampo on 2025-07-04T09:39:45Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184877029
Path: packages/components/src/calendar/stories/date-range-calendar.story.tsx

```diff
@@ -18,12 +21,18 @@ import {
 	ar,
 	sv,
 } from 'date-fns/locale';
-import { useState, useEffect } from 'react';
-import { DateRangeCalendar, TZDate } from '../';
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { DateRangeCalendar, TZDate } from '..';
 import type { Meta, StoryObj } from '@storybook/react';
```

Same here. re moving to external dependencies

#### chihsuan on 2025-07-07T03:07:14Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188876898
Path: packages/components/src/calendar/stories/date-range-calendar.story.tsx

```diff
@@ -18,12 +21,18 @@ import {
 	ar,
 	sv,
 } from 'date-fns/locale';
-import { useState, useEffect } from 'react';
-import { DateRangeCalendar, TZDate } from '../';
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { DateRangeCalendar, TZDate } from '..';
 import type { Meta, StoryObj } from '@storybook/react';
```

Done. d3ddf3c3d7bfe131054451fd61a7934778289efd

### Review thread 15 (resolved)

#### ciampo on 2025-07-04T09:40:31Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184878679
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -14,16 +17,22 @@ import {
 	addHours,
 } from 'date-fns';
 import { ar } from 'date-fns/locale';
-import { useState, default as React } from 'react';
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
 import '@testing-library/jest-dom';
```

Potentially it may not be necessary to import `'@testing-library/jest-dom'` in Gutenberg test files.

Otherwise, same here re. moving line to external dependencies

#### chihsuan on 2025-07-07T03:06:47Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188876573
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -14,16 +17,22 @@ import {
 	addHours,
 } from 'date-fns';
 import { ar } from 'date-fns/locale';
-import { useState, default as React } from 'react';
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
 import '@testing-library/jest-dom';
```

Removed in aa50f6483aeb152329feb7aac45221fb856a915f

### Review thread 16 (resolved)

#### ciampo on 2025-07-04T09:42:23Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2184882557
Path: packages/components/src/calendar/test/date-range-calendar.tsx

```diff
@@ -14,12 +17,18 @@ import {
 	addHours,
 } from 'date-fns';
 import { ar } from 'date-fns/locale';
-import { useState, default as React } from 'react';
 import '@testing-library/jest-dom';
```

Potentially it may not be necessary to import `'@testing-library/jest-dom'` in Gutenberg test files.

#### chihsuan on 2025-07-07T03:06:35Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188876488
Path: packages/components/src/calendar/test/date-range-calendar.tsx

```diff
@@ -14,12 +17,18 @@ import {
 	addHours,
 } from 'date-fns';
 import { ar } from 'date-fns/locale';
-import { useState, default as React } from 'react';
 import '@testing-library/jest-dom';
```

Yeah, it's not needed to import it. I've removed it in aa50f6483aeb152329feb7aac45221fb856a915f

### Review thread 17 (resolved)

#### ciampo on 2025-07-04T10:57:08Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2185052176
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -731,26 +904,44 @@ describe( 'DateCalendar', () => {
 			expect( grid ).toBeVisible();
 
 			// Check today button
-			expect( getDateButton( today, {}, 'ar' ) ).toHaveAccessibleName( /today/i );
+			expect( getDateButton( today, {}, 'ar' ) ).toHaveAccessibleName(
+				/today/i
+			);
 
 			await user.tab();
 			await user.tab();
 			await user.tab();
 			expect( getDateButton( today, {}, 'ar' ) ).toHaveFocus();
 
 			await user.keyboard( '{Home}' );
-			expect( getDateButton( startOfWeek( today, { locale: ar } ), {}, 'ar' ) ).toHaveFocus();
+			expect(
+				getDateButton( startOfWeek( today, { locale: ar } ), {}, 'ar' )
+			).toHaveFocus();
 		} );
 
 		it( 'should support timezones according to the `timeZone` prop', async () => {
-			const user = setupUserEvent();
+			const user = await setupUserEvent();
 			const onSelect = jest.fn();
 
-			render( <DateCalendar timeZone="Asia/Tokyo" onSelect={ onSelect } /> );
+			render(
+				<DateCalendar timeZone="Asia/Tokyo" onSelect={ onSelect } />
+			);
+
+			const now = new Date();
+			today = new Date(
+				Date.UTC(
+					now.getUTCFullYear(),
+					now.getUTCMonth(),
+					now.getUTCDate()
+				)
+			);
+			tomorrow = addDays( today, 1 );
```

Can we avoid this additional code (and any other similar UTC-related code) by changing the Jest seattings as suggested in https://github.com/WordPress/gutenberg/pull/70578/files/df7065c6b73e12b29a82d1ff6d6814897c557b8a..3053be4528a19a3f31cb368368ec2cf24318981f#r2182571977?

#### chihsuan on 2025-07-07T03:54:08Z

URL: https://github.com/WordPress/gutenberg/pull/70578#discussion_r2188912359
Path: packages/components/src/calendar/test/date-calendar.tsx

```diff
@@ -731,26 +904,44 @@ describe( 'DateCalendar', () => {
 			expect( grid ).toBeVisible();
 
 			// Check today button
-			expect( getDateButton( today, {}, 'ar' ) ).toHaveAccessibleName( /today/i );
+			expect( getDateButton( today, {}, 'ar' ) ).toHaveAccessibleName(
+				/today/i
+			);
 
 			await user.tab();
 			await user.tab();
 			await user.tab();
 			expect( getDateButton( today, {}, 'ar' ) ).toHaveFocus();
 
 			await user.keyboard( '{Home}' );
-			expect( getDateButton( startOfWeek( today, { locale: ar } ), {}, 'ar' ) ).toHaveFocus();
+			expect(
+				getDateButton( startOfWeek( today, { locale: ar } ), {}, 'ar' )
+			).toHaveFocus();
 		} );
 
 		it( 'should support timezones according to the `timeZone` prop', async () => {
-			const user = setupUserEvent();
+			const user = await setupUserEvent();
 			const onSelect = jest.fn();
 
-			render( <DateCalendar timeZone="Asia/Tokyo" onSelect={ onSelect } /> );
+			render(
+				<DateCalendar timeZone="Asia/Tokyo" onSelect={ onSelect } />
+			);
+
+			const now = new Date();
+			today = new Date(
+				Date.UTC(
+					now.getUTCFullYear(),
+					now.getUTCMonth(),
+					now.getUTCDate()
+				)
+			);
+			tomorrow = addDays( today, 1 );
```

Yes, thanks for pointing that out. This works perfectly. Changed in e3df37604cfdec9ed91f103c47709d1da04730ec

## Changed files

- docs/manifest.json (MODIFIED, +12/-0)
- package-lock.json (MODIFIED, +40/-0)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/components/src/calendar/date-calendar/README.md (ADDED, +250/-0)
- packages/components/src/calendar/date-calendar/index.tsx (ADDED, +55/-0)
- packages/components/src/calendar/date-range-calendar/README.md (ADDED, +287/-0)
- packages/components/src/calendar/date-range-calendar/index.tsx (ADDED, +203/-0)
- packages/components/src/calendar/index.tsx (ADDED, +3/-0)
- packages/components/src/calendar/stories/date-calendar.story.tsx (ADDED, +221/-0)
- packages/components/src/calendar/stories/date-range-calendar.story.tsx (ADDED, +230/-0)
- packages/components/src/calendar/style.scss (ADDED, +431/-0)
- packages/components/src/calendar/test/__utils__/index.ts (ADDED, +56/-0)
- packages/components/src/calendar/test/date-calendar.tsx (ADDED, +975/-0)
- packages/components/src/calendar/test/date-range-calendar.tsx (ADDED, +1701/-0)
- packages/components/src/calendar/types.ts (ADDED, +342/-0)
- packages/components/src/calendar/utils/constants.ts (ADDED, +62/-0)
- packages/components/src/calendar/utils/day-cell.tsx (ADDED, +133/-0)
- packages/components/src/calendar/utils/misc.ts (ADDED, +3/-0)
- packages/components/src/calendar/utils/use-controlled-value.ts (ADDED, +61/-0)
- packages/components/src/calendar/utils/use-localization-props.ts (ADDED, +169/-0)
- packages/components/src/index.ts (MODIFIED, +1/-0)
- packages/components/src/style.scss (MODIFIED, +1/-0)
- test/unit/jest.config.js (MODIFIED, +3/-0)
