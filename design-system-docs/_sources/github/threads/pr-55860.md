---
number: 55860
type: pull_request
state: MERGED
merged_at: 2023-11-07T18:41:22Z
url: https://github.com/WordPress/gutenberg/pull/55860
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #55860: Tabs: Update subcomponents to accept full HTML element props

- URL: https://github.com/WordPress/gutenberg/pull/55860
- Author: chad1008
- Created: 2023-11-03T19:50:32Z
- Updated: 2023-11-07T18:46:08Z
- Comments: 2 of 2
- Review threads: 0 of 0
- Changed files: 7 of 7
- Diff: ../pr-diffs/55860.diff

## Body

Related [comment](https://github.com/WordPress/gutenberg/pull/53960#pullrequestreview-1661817575).

## What?
Updates the subcomponents (TabList, Tab, and TabPanel) to accept all of the HTML element props relevant to their underlying DOM elements.

This PR also removes some artifacts of an old `icon` prop that was removed earlier in the component's development. There were still some references in the types and tests files that needed to be removed.

## Why?
Previously the Tabs subcomponents explicitly accepted props like `className` and `style`, but not the general props for their respective DOM elements. This made it hard to pass basic props, and made it necessary to overuse the `render` prop to gain access to them ([example](https://github.com/WordPress/gutenberg/pull/55360#discussion_r1381348291))

## How?
Each of the subcomponents now leverages `WordPressComponentProps` to include all of the relevant HTML element props.

## Testing Instructions
1. Open the `Tabs` default story in Storybook.
2. Edit the default story by adding some `div` related HTML props to a `TabList` and `TabPanel` subcomponent (`hidden`, `style`, `className` for example, but experiment with any prop that's valid on a `div`.)
3. Confirm that the props you've added are correctly passed to the rendered `div` for the subcomponent you modified.
4. Edit the default story by adding some `button` related props to a `Tab` subcomponent. Specifically, test an `onClick` to confirm that it's applied and functions correctly.
5. Have a snack. You deserve it.

## Issue comments

### chad1008 on 2023-11-06T20:45:33Z

URL: https://github.com/WordPress/gutenberg/pull/55860#issuecomment-1796406248

Thanks @brookewp! I think I'll hold off on tests now... I feel like I'd be writing tests for `WordPressComponentProps` more than for `Tabs` itself - but I'm happy to add them in a followup later if we decide it's warranted!

### github-actions on 2023-11-06T21:23:50Z

URL: https://github.com/WordPress/gutenberg/pull/55860#issuecomment-1796514168

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 98094eea1a353e52361786feb366d32ae5c0108a.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/6788313246
📝  Reported issues:
- #53524 in `/test/e2e/specs/editor/various/multi-block-selection.spec.js`

## Review threads

_No review threads._
## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/tabs/README.md (MODIFIED, +0/-37)
- packages/components/src/tabs/tab.tsx (MODIFIED, +6/-6)
- packages/components/src/tabs/tablist.tsx (MODIFIED, +21/-20)
- packages/components/src/tabs/tabpanel.tsx (MODIFIED, +23/-25)
- packages/components/src/tabs/test/index.tsx (MODIFIED, +6/-15)
- packages/components/src/tabs/types.ts (MODIFIED, +0/-33)
