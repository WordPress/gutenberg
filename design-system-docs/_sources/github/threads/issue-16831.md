---
number: 16831
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/16831
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Status] In Progress","[Package] Components"]
---

# Issue #16831: SandBox not re-rendered after HTML changes

- URL: https://github.com/WordPress/gutenberg/issues/16831
- Author: mmtr
- Created: 2019-07-31T05:33:34Z
- Updated: 2020-02-13T18:53:09Z
- Comments: 2 of 2

## Body

**Describe the bug**
The `SandBox` component from the `@wordpress/components` package is not re-rendered when the injected `html` prop changes.

**To reproduce**
* Paste the following code in `playground/src/index.js`:
```js
import { render, useState, Fragment } from '@wordpress/element';
import { SandBox, SelectControl } from '@wordpress/components';
import './style.scss';
import '@wordpress/components/build-style/style.css';

function App() {
	const [ fruit, setFruit ] = useState( 'Bananas' );
	const html = `<div>Selected fruit: <strong>${ fruit }</strong>!</div>`;

	return (
		<Fragment>
			<SelectControl
				label="Fruit"
				value={ fruit }
				options={ [
					{ label: 'Bananas', value: 'Bananas' },
					{ label: 'Oranges', value: 'Oranges' },
					{ label: 'Apples', value: 'Apples' },
				] }
				onChange={ ( value ) => { setFruit( value ) } }
			/>
			<SandBox html={ html } />
		</Fragment>
	);
}

render(
	<App />,
	document.querySelector( '#app' )
);
```

* Run `npm run playground:start`.
* Go to `http://localhost:1234`.
* Select any other fruit than _Bananas_.

**Expected behavior**
The selected fruit text displayed below the fruit selector should be updated after selecting any other fruit, but it keeps unchanged:

**Screenshots**
<img width="642" alt="Screen Shot 2019-07-31 at 14 32 38" src="https://user-images.githubusercontent.com/1233880/62186151-1df14080-b3a0-11e9-8d17-61c784ac207b.png">

## Issue comments

### swissspidy on 2019-07-31T08:12:41Z

URL: https://github.com/WordPress/gutenberg/issues/16831#issuecomment-516746038

Sounds like a duplicate of #16612.

Can you test #16669 to see if that fixes the issue?

### mmtr on 2019-08-01T00:09:16Z

URL: https://github.com/WordPress/gutenberg/issues/16831#issuecomment-517067662

> Sounds like a duplicate of #16612.
> 
> Can you test #16669 to see if that fixes the issue?

I don't think it's related to #16612. I verified I can still reproduce the issue with #16669.

Seems to be more a problem on how the `SandBox` component is implemented. The `render` function [doesn't use at all the `html` prop](https://github.com/WordPress/gutenberg/blob/467e45f787f93d35030ce266a500c627db0e18e5/packages/components/src/sandbox/index.js#L200-L214), which is why any change to that prop doesn't trigger any update. The `html` prop is only [used on `trySandbox`](https://github.com/WordPress/gutenberg/blob/467e45f787f93d35030ce266a500c627db0e18e5/packages/components/src/sandbox/index.js#L175), but that method is only called after the `SandBox` component has been already rendered.

