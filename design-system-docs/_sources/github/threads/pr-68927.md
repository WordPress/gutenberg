---
number: 68927
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/68927
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","Needs Design Feedback","[Package] Components"]
---

# PR #68927: Add loading indicator to combo box control author selector

- URL: https://github.com/WordPress/gutenberg/pull/68927
- Author: adamsilverstein
- Created: 2025-01-28T23:05:53Z
- Updated: 2025-01-31T16:40:46Z
- Comments: 9 of 9
- Review threads: 8 of 8
- Changed files: 6 of 6
- Diff: ../pr-diffs/68927.diff

## Body

This PR is a rework of the effort in https://github.com/WordPress/gutenberg/pull/27865 - see that ticket for additional details.

## Testing instructions
1. Create a large number of editors (see below for command).
2. Load a page in the editor
3. Open DevTools and throttle network connections to a 3G speed to slow down the rest API responses
4. Click the author to edit, then try typing in part of a user name

Expected results:
- A spinner is shown while the search callback executes
- the "No items found' string only shows when the search completes (and no items are found)


### Helpful wp-cli commands for testing:
Generate many users to test searching: `wp user generate --role=editor --count=100`
Delete test users: `wp user delete $(wp user list --role=editor --field=ID) --reassign=2`


## Screencasts

### Before changes (trunk)

https://github.com/user-attachments/assets/355cbe2d-80e7-4fd0-979c-54d102b075b2


### Add spinner
(note message "No items found" shows during search callback)

https://github.com/user-attachments/assets/55de9eae-e7fd-471a-8813-0e23b4a95d13

### Final PR 
(including removal of "No items found" during load)

https://github.com/user-attachments/assets/c0c2c7fe-3716-499f-acf6-c6dda7ab2961

## Issue comments

### adamsilverstein on 2025-01-28T23:09:36Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2620244014

Note: the drop-down shows "No items found." during the search, a bug clearly visible currently (before this PR) when throttling the search callback.

### github-actions on 2025-01-29T14:30:51Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2621817234

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: adamsilverstein <adamsilverstein@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### adamsilverstein on 2025-01-29T14:31:25Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2621818582

I updated the PR to avoid showing "No items found" string while the results are still loading.

### adamsilverstein on 2025-01-29T21:26:10Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2622887803

> My suggestion is to limit new loading state changes to the ComboboxControl. The FormTokenField component is a bit tricky, as input and tokens can wrap on multiple lines. So, I'm not sure if the spinner suffix design will work for it.

Thanks for the review.

I will add some screenshots/screencasts to show what this change does (when I am back at my desk Friday). I can also take a look at other consumers of the component to make sure they are unaffected. The main goal of the change here is to avoid showing "No items found" string while the (author) search is underway. Since it isn't directly related to adding a spinner, I can break it out into a follow up PR and we can explore the issue separately.

### Mamaduka on 2025-01-30T03:45:25Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2623453173

I think it's okay to change the `SuggestionsList` here, but let's avoid adding the `isLoading` prop to the `FormTokenField`. This way we can focus only on `ComboboxControl` improvements.

### adamsilverstein on 2025-01-31T14:59:02Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2627552464

> I think it's okay to change the `SuggestionsList` here, but let's avoid adding the `isLoading` prop to the `FormTokenField`. This way we can focus only on `ComboboxControl` improvements.

I will remove these changes for now and we can consider adding them in a follow up PR. 

I have included screencasts above so you can see why I added this change in the first place.

### adamsilverstein on 2025-01-31T15:01:50Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2627558923

> I think it's okay to change the `SuggestionsList` here, but let's avoid adding the `isLoading` prop to the `FormTokenField`. This way we can focus only on `ComboboxControl` improvements.

Reverted in https://github.com/WordPress/gutenberg/pull/68927/commits/fd97afccde43da4d5a55a24cb6bf08bb4ef220b9

### adamsilverstein on 2025-01-31T15:23:59Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2627609965

@ciampo - In https://github.com/WordPress/gutenberg/pull/68927/commits/3a357c49486459f4c74e7911396ab08c174fd323 I added the !isLoading conditional so the suggestion list only shows when the component isn't loading data, what do you think? I will add a screencast to show how this works.

### adamsilverstein on 2025-01-31T16:40:45Z

URL: https://github.com/WordPress/gutenberg/pull/68927#issuecomment-2627776313

Closing in favor of https://github.com/WordPress/gutenberg/pull/68991 and https://github.com/WordPress/gutenberg/pull/68990

## Review threads

### Review thread 1

#### Mamaduka on 2025-01-29T14:45:03Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1934023188
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
```

Typo?

#### adamsilverstein on 2025-01-29T21:22:22Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1934662649
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
```

ha, this was me trying to get my build working and not seeing changes. will revert!

### Review thread 2

#### Mamaduka on 2025-01-29T15:38:23Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1934119840
Path: packages/components/src/form-token-field/suggestions-list.tsx

```diff
@@ -157,7 +158,7 @@ export function SuggestionsList<
 				);
 				/* eslint-enable jsx-a11y/click-events-have-key-events */
 			} ) }
-			{ suggestions.length === 0 && (
+			{ suggestions.length === 0 && ! isLoading && (
```

The `SuggestionsList` component is shared, which is why we're passing the `isLoading` prop, correct?

#### adamsilverstein on 2025-01-29T21:21:38Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1934661961
Path: packages/components/src/form-token-field/suggestions-list.tsx

```diff
@@ -157,7 +158,7 @@ export function SuggestionsList<
 				);
 				/* eslint-enable jsx-a11y/click-events-have-key-events */
 			} ) }
-			{ suggestions.length === 0 && (
+			{ suggestions.length === 0 && ! isLoading && (
```

right

### Review thread 3 (resolved)

#### ciampo on 2025-01-31T14:32:41Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937410510
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -396,6 +399,7 @@ function ComboboxControl( props: ComboboxControlProps ) {
 							__experimentalRenderItem={
 								__experimentalRenderItem
 							}
+							isLoading={ isLoading }
```

Instead of passing the `isLoading` prop to `SuggestionsList`, could instead use the `isLoading` boolean flag to render the component conditionally (something like `isExpanded && ! isLoading`) ?

That would allow us to focus the changes only on the `ComboboxControl` component.

#### adamsilverstein on 2025-01-31T15:02:41Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937453889
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -396,6 +399,7 @@ function ComboboxControl( props: ComboboxControlProps ) {
 							__experimentalRenderItem={
 								__experimentalRenderItem
 							}
+							isLoading={ isLoading }
```

Ah that makes sense - just hide the suggestion list entirely when the suggestions are still loading. I will try that.

#### adamsilverstein on 2025-01-31T15:24:25Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937486032
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -396,6 +399,7 @@ function ComboboxControl( props: ComboboxControlProps ) {
 							__experimentalRenderItem={
 								__experimentalRenderItem
 							}
+							isLoading={ isLoading }
```

try in https://github.com/WordPress/gutenberg/pull/68927/commits/3a357c49486459f4c74e7911396ab08c174fd323

### Review thread 4

#### ciampo on 2025-01-31T14:35:08Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937414128
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
 			options={ authorOptions }
 			value={ authorId }
 			onFilterValueChange={ debounce( handleKeydown, 300 ) }
 			onChange={ handleSelect }
 			allowReset={ false }
 			hideLabelFromVision
+			isLoading={ isLoading }
```

My preference would we to focus this PR solely on the `ComboboxControl` component.

We should add a Storybook example [here](https://github.com/WordPress/gutenberg/blob/f45e35da9bf7c633411f2690853a783472e3cfb7/packages/components/src/combobox-control/stories/index.story.tsx) to allow for easy testing of the component in isolation.

In a separate, follow-up PR, we could then tweak the post author combobox to use the new prop

#### adamsilverstein on 2025-01-31T15:05:54Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937458204
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
 			options={ authorOptions }
 			value={ authorId }
 			onFilterValueChange={ debounce( handleKeydown, 300 ) }
 			onChange={ handleSelect }
 			allowReset={ false }
 			hideLabelFromVision
+			isLoading={ isLoading }
```

The entire point of this PR is to improve the author selector - changing only the combobox won't actually change anything, shouldn't we aim for PRs that actually improve things?

I can look into adding to the storybook, would this just be a variation with the `isLoading` prop?

#### ciampo on 2025-01-31T15:34:09Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937500554
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
 			options={ authorOptions }
 			value={ authorId }
 			onFilterValueChange={ debounce( handleKeydown, 300 ) }
 			onChange={ handleSelect }
 			allowReset={ false }
 			hideLabelFromVision
+			isLoading={ isLoading }
```

The `@wordpress/component` package is not only used in the Gutenberg repository, but it's also used by third-party developers. Therefore, we usually try to keep changes to the "library" separate from changes to the Gutenberg app (hence why I suggest testing the changes in isolation in Storybook).

Having smaller and more focused PRs is also easier to review, and understand for a future developer stumbling upon it.

#### adamsilverstein on 2025-01-31T16:26:06Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937574161
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
 			options={ authorOptions }
 			value={ authorId }
 			onFilterValueChange={ debounce( handleKeydown, 300 ) }
 			onChange={ handleSelect }
 			allowReset={ false }
 			hideLabelFromVision
+			isLoading={ isLoading }
```

> Having smaller and more focused PRs is also easier to review, and understand for a future developer stumbling upon it.

I understand the principal, however this entire PR is currently _less than 35 lines modified_ so I think is already easy to review and understand! Given the _testing instructions_ I provided, I feel having the PR do something makes it **easier** to test and understand.

That said, I sense that you feel strongly about this and I will defer to your more recent experience building here. I will split the combobox and author changes into separate PRs.

#### adamsilverstein on 2025-01-31T16:40:16Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937591578
Path: packages/editor/src/components/post-author/combobox.js

```diff
@@ -44,13 +45,14 @@ export default function PostAuthorCombobox() {
 		<ComboboxControl
 			__nextHasNoMarginBottom
 			__next40pxDefaultSize
-			label={ __( 'Author' ) }
+			label={ __( 'Authorz' ) }
 			options={ authorOptions }
 			value={ authorId }
 			onFilterValueChange={ debounce( handleKeydown, 300 ) }
 			onChange={ handleSelect }
 			allowReset={ false }
 			hideLabelFromVision
+			isLoading={ isLoading }
```

I created a follow up PR with just the Comobox changes here: 

https://github.com/WordPress/gutenberg/pull/68990

This is a follow up PR to introduce the post author changes: 

https://github.com/WordPress/gutenberg/pull/68991


Let's continue the conversation on those PRs.

### Review thread 5 (resolved)

#### ciampo on 2025-01-31T14:37:24Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937417186
Path: packages/components/src/combobox-control/types.ts

```diff
@@ -86,4 +86,9 @@ export type ComboboxControlProps = Pick<
 	 * If passed, the combobox input will show a placeholder string if no values are present.
 	 */
 	placeholder?: string;
+
+	/**
+	 * When loading, combobox will show a spinner
```

For better clarity:

```suggestion
	 * Show a spinner (and hide the suggestions dropdown) while data
	 * about the matching suggestions (ie the `options` prop) is loading
```

We should also add this prop to the component's README file

#### adamsilverstein on 2025-01-31T15:25:06Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937487180
Path: packages/components/src/combobox-control/types.ts

```diff
@@ -86,4 +86,9 @@ export type ComboboxControlProps = Pick<
 	 * If passed, the combobox input will show a placeholder string if no values are present.
 	 */
 	placeholder?: string;
+
+	/**
+	 * When loading, combobox will show a spinner
```

Added prop to readme in https://github.com/WordPress/gutenberg/pull/68927/commits/374b2b887626aa2b964a4e6c2b8222d2df53b956

### Review thread 6

#### ciampo on 2025-01-31T14:43:41Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937426126
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -126,6 +127,7 @@ function ComboboxControl( props: ComboboxControlProps ) {
 		help,
 		allowReset = true,
 		className,
+		isLoading = false,
```

Adding this prop won't affect existing usages, although I wonder if it's a good idea, API-wise.

The component doesn't have a canonical example of how to use it with asynchronously loaded options, and I wonder if adding this prop will improve the DevX (related to https://github.com/WordPress/gutenberg/pull/55574) — @WordPress/gutenberg-components what do you think?

If we want to encourage this pattern, we should at least add a Storybook example about it?

#### adamsilverstein on 2025-01-31T15:10:52Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937465126
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -126,6 +127,7 @@ function ComboboxControl( props: ComboboxControlProps ) {
 		help,
 		allowReset = true,
 		className,
+		isLoading = false,
```

> I wonder if adding this prop will improve the DevX 

Not sure about DevX, this change is about improving _User Experience_. As is, the combobox selector provides a poor UX when the internet connection is slow or unreliable. The user has no way of knowing that a search is underway during the async search. This shortcoming was overlooked when [we originally built the component](https://github.com/WordPress/gutenberg/pull/23237) and I feel this more of a bug than an enhancement.

### Review thread 7

#### ciampo on 2025-01-31T15:34:51Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937501459
Path: packages/components/src/combobox-control/README.md

```diff
@@ -112,13 +113,20 @@ If the control is clicked, the dropdown will expand regardless of this prop.
 -   Required: No
 -   Default: `true`
 
-### placeholder
+#### placeholder
```

🙏

### Review thread 8

#### ciampo on 2025-01-31T15:35:15Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937501970
Path: packages/components/src/combobox-control/README.md

```diff
@@ -112,13 +113,20 @@ If the control is clicked, the dropdown will expand regardless of this prop.
 -   Required: No
 -   Default: `true`
 
-### placeholder
+#### placeholder
 
 If passed, the combobox input will show a placeholder string if no values are present.
 
 -   Type: `string`
 -   Required: No
 
+#### isLoading
+
+If true, the dropdown will show a loading indicator.
```

Let's match the same description as the one from `types.ts`

#### adamsilverstein on 2025-01-31T16:40:28Z

URL: https://github.com/WordPress/gutenberg/pull/68927#discussion_r1937591809
Path: packages/components/src/combobox-control/README.md

```diff
@@ -112,13 +113,20 @@ If the control is clicked, the dropdown will expand regardless of this prop.
 -   Required: No
 -   Default: `true`
 
-### placeholder
+#### placeholder
 
 If passed, the combobox input will show a placeholder string if no values are present.
 
 -   Type: `string`
 -   Required: No
 
+#### isLoading
+
+If true, the dropdown will show a loading indicator.
```

matched

## Changed files

- packages/components/src/combobox-control/README.md (MODIFIED, +9/-1)
- packages/components/src/combobox-control/index.tsx (MODIFIED, +4/-1)
- packages/components/src/combobox-control/stories/index.story.tsx (MODIFIED, +1/-0)
- packages/components/src/combobox-control/types.ts (MODIFIED, +6/-0)
- packages/editor/src/components/post-author/combobox.js (MODIFIED, +3/-1)
- packages/editor/src/components/post-author/hook.js (MODIFIED, +4/-3)
