---
number: 79442
type: pull_request
state: MERGED
merged_at: 2026-06-24T08:31:33Z
url: https://github.com/WordPress/gutenberg/pull/79442
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #79442: Components: add Emotion migration guardrails

- URL: https://github.com/WordPress/gutenberg/pull/79442
- Author: ciampo
- Created: 2026-06-23T14:10:47Z
- Updated: 2026-06-24T13:56:22Z
- Comments: 2 of 2
- Review threads: 5 of 5
- Changed files: 4 of 4
- Diff: ../pr-diffs/79442.diff

## Body

## What?

Part of #66806.

Adds the first set of docs and lint guardrails for migrating `@wordpress/components` away from Emotion.

## Why?
New component styles should use the existing SCSS Modules pattern while the current Emotion usage is migrated incrementally. This prevents the migration backlog from growing.

## How?
- Updates `packages/components/CONTRIBUTING.md` to recommend SCSS Modules and `clsx` for new component-local styles.
- Documents CSS custom properties for dynamic values and conditional CSS Module classes for variants/state.
- Replaces the Emotion-based deprecation styling example with a SCSS Modules example.
- Adds a `packages/components/src` import guard that blocks new `@emotion/*` imports while temporarily allowlisting current Emotion files.

## Testing Instructions
1. Run `npm run lint:md:docs -- packages/components/CONTRIBUTING.md`.
2. Run `npm run lint:js -- tools/eslint/config.mjs`.
3. Run `npm run lint:js -- packages/components/src --pass-on-unpruned-suppressions`.
4. Optionally add a new `@emotion/react` import to a non-allowlisted file under `packages/components/src` and confirm linting reports it.

### Testing Instructions for Keyboard
Not applicable; this PR only changes documentation and lint rules.

## Screenshots or screencast
Not applicable.

## Use of AI Tools
Drafted with assistance from ChatGPT/Codex. The changes were reviewed before opening this PR.

## Issue comments

### github-actions on 2026-06-23T14:19:29Z

URL: https://github.com/WordPress/gutenberg/pull/79442#issuecomment-4780206304

**Size Change:** 0 B 

**Total Size:** 7.51 MB



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-23T16:18:35Z

URL: https://github.com/WordPress/gutenberg/pull/79442#issuecomment-4781214292

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1 (resolved)

#### copilot-pull-request-reviewer on 2026-06-23T15:45:03Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3461033523
Path: tools/eslint/config.mjs

```diff
@@ -672,6 +770,21 @@ export default dedupePlugins( [
 		},
 	},
 
+	// Override: Components src — prevent new Emotion usage while existing
+	// Emotion styles are migrated incrementally.
+	{
+		files: [ 'packages/components/src/**/*.[tj]s?(x)' ],
+		ignores: componentsEmotionMigrationAllowlist,
+		rules: {
+			'no-restricted-imports': [
+				'error',
+				{
+					paths: componentsSourceRestrictedImports,
+				},
```

This blocks exact `@emotion/*` package imports, but subpath imports (e.g. `@emotion/css/create-instance`) would still be allowed in new files; add a `patterns` restriction to cover `@emotion/**`.

### Review thread 2

#### ciampo on 2026-06-23T16:17:57Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3461248750
Path: tools/eslint/config.mjs

```diff
@@ -665,8 +666,9 @@ export default dedupePlugins( [
 						( { name } ) =>
 							! [ '@ariakit/react', 'framer-motion' ].includes(
 								name
-							)
+							) && ! name.startsWith( '@emotion/' )
```

This check is to prevent emotion imports from being flagged twice

#### mirka on 2026-06-24T07:09:38Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3465191058
Path: tools/eslint/config.mjs

```diff
@@ -665,8 +666,9 @@ export default dedupePlugins( [
 						( { name } ) =>
 							! [ '@ariakit/react', 'framer-motion' ].includes(
 								name
-							)
+							) && ! name.startsWith( '@emotion/' )
```

I also considered just restricting all `@emotion` imports everywhere, but… yeah I guess this is fine as a short term thing.

#### ciampo on 2026-06-24T07:49:55Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3465426878
Path: tools/eslint/config.mjs

```diff
@@ -665,8 +666,9 @@ export default dedupePlugins( [
 						( { name } ) =>
 							! [ '@ariakit/react', 'framer-motion' ].includes(
 								name
-							)
+							) && ! name.startsWith( '@emotion/' )
```

I thought we could start with the components package, and then apply the refactor to the whole repo after

### Review thread 3 (resolved)

#### mirka on 2026-06-24T06:49:06Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3465091949
Path: packages/components/CONTRIBUTING.md

```diff
@@ -416,48 +416,56 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 ## Styling
 
-All new component should be styled using [Emotion](https://emotion.sh/docs/introduction).
+All new components should be styled using SCSS Modules.
 
-Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer `data-*` attributes or explicit module classes.
+
+Legacy components may still use Emotion while they are being migrated, but new Emotion usage should not be added.
 
 ### Deprecating styles
 
 Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
 
 ```jsx
 // component.tsx
+import clsx from 'clsx';
 import deprecated from '@wordpress/deprecated';
-import { Wrapper } from './styles.ts';
+import styles from './style.module.scss';
 
-function MyComponent( { __nextHasNoOuterMargins = false } ) {
+function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 	if ( ! __nextHasNoOuterMargins ) {
 		deprecated( 'Outer margin styles for wp.components.MyComponent', {
 			since: '6.0',
 			version: '6.2', // Set a reasonable grace period depending on impact
 			hint: 'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
 		} );
 	}
-	return <Wrapper __nextHasNoOuterMargins={ __nextHasNoOuterMargins } />;
+	return (
+		<div
+			className={ clsx(
+				'components-my-component',
+				styles.root,
+				className
+			) }
+			data-has-no-outer-margins={
+				__nextHasNoOuterMargins ? true : undefined
+			}
```

Why not just a normal class name in the CSS module? I don't think we should be exposing data attributes for this.

#### ciampo on 2026-06-24T08:02:51Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3465500142
Path: packages/components/CONTRIBUTING.md

```diff
@@ -416,48 +416,56 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 ## Styling
 
-All new component should be styled using [Emotion](https://emotion.sh/docs/introduction).
+All new components should be styled using SCSS Modules.
 
-Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer `data-*` attributes or explicit module classes.
+
+Legacy components may still use Emotion while they are being migrated, but new Emotion usage should not be added.
 
 ### Deprecating styles
 
 Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
 
 ```jsx
 // component.tsx
+import clsx from 'clsx';
 import deprecated from '@wordpress/deprecated';
-import { Wrapper } from './styles.ts';
+import styles from './style.module.scss';
 
-function MyComponent( { __nextHasNoOuterMargins = false } ) {
+function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 	if ( ! __nextHasNoOuterMargins ) {
 		deprecated( 'Outer margin styles for wp.components.MyComponent', {
 			since: '6.0',
 			version: '6.2', // Set a reasonable grace period depending on impact
 			hint: 'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
 		} );
 	}
-	return <Wrapper __nextHasNoOuterMargins={ __nextHasNoOuterMargins } />;
+	return (
+		<div
+			className={ clsx(
+				'components-my-component',
+				styles.root,
+				className
+			) }
+			data-has-no-outer-margins={
+				__nextHasNoOuterMargins ? true : undefined
+			}
```

Agreed, this one escaped my internal review. AI is very keen on adding data attributes as they match base ui approach and feel very "platform native" 😅  but ignores the implication of adding to the public contract of the component

### Review thread 4 (resolved)

#### mirka on 2026-06-24T06:50:23Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3465097737
Path: packages/components/CONTRIBUTING.md

There are still mentions of `styles.ts` and `useCx()` in this file.

### Review thread 5

#### jsnajdr on 2026-06-24T08:39:45Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3465710567
Path: packages/components/CONTRIBUTING.md

```diff
@@ -416,48 +416,55 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 ## Styling
 
-All new component should be styled using [Emotion](https://emotion.sh/docs/introduction).
+All new components should be styled using SCSS Modules.
 
-Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`.
+
+Legacy components may still use Emotion while they are being migrated, but new Emotion usage should not be added.
 
 ### Deprecating styles
 
 Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
 
 ```jsx
 // component.tsx
+import clsx from 'clsx';
 import deprecated from '@wordpress/deprecated';
-import { Wrapper } from './styles.ts';
+import styles from './style.module.scss';
 
-function MyComponent( { __nextHasNoOuterMargins = false } ) {
+function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 	if ( ! __nextHasNoOuterMargins ) {
 		deprecated( 'Outer margin styles for wp.components.MyComponent', {
 			since: '6.0',
 			version: '6.2', // Set a reasonable grace period depending on impact
 			hint: 'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
 		} );
 	}
-	return <Wrapper __nextHasNoOuterMargins={ __nextHasNoOuterMargins } />;
+	return (
+		<div
+			className={ clsx(
+				'components-my-component',
+				styles.root,
+				! __nextHasNoOuterMargins &&
+					styles.deprecatedOuterMargins,
+				className
+			) }
```

nit: this is a suboptimal usage of `clsx`, it should be:
```js
clsx( 'components-my-component', styles.root, className, {
  [ styles.deprecatedOuterMargins ]: ! __nextHasNoOuterMargins,
} );
```

#### mirka on 2026-06-24T11:58:15Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3466874906
Path: packages/components/CONTRIBUTING.md

```diff
@@ -416,48 +416,55 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 ## Styling
 
-All new component should be styled using [Emotion](https://emotion.sh/docs/introduction).
+All new components should be styled using SCSS Modules.
 
-Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`.
+
+Legacy components may still use Emotion while they are being migrated, but new Emotion usage should not be added.
 
 ### Deprecating styles
 
 Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
 
 ```jsx
 // component.tsx
+import clsx from 'clsx';
 import deprecated from '@wordpress/deprecated';
-import { Wrapper } from './styles.ts';
+import styles from './style.module.scss';
 
-function MyComponent( { __nextHasNoOuterMargins = false } ) {
+function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 	if ( ! __nextHasNoOuterMargins ) {
 		deprecated( 'Outer margin styles for wp.components.MyComponent', {
 			since: '6.0',
 			version: '6.2', // Set a reasonable grace period depending on impact
 			hint: 'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
 		} );
 	}
-	return <Wrapper __nextHasNoOuterMargins={ __nextHasNoOuterMargins } />;
+	return (
+		<div
+			className={ clsx(
+				'components-my-component',
+				styles.root,
+				! __nextHasNoOuterMargins &&
+					styles.deprecatedOuterMargins,
+				className
+			) }
```

I've noticed that this object key style can emit a literal `undefined` class in Jest tests. (Perhaps not super important, but a notable difference between the two styles.)

#### jsnajdr on 2026-06-24T12:16:11Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3466985920
Path: packages/components/CONTRIBUTING.md

```diff
@@ -416,48 +416,55 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 ## Styling
 
-All new component should be styled using [Emotion](https://emotion.sh/docs/introduction).
+All new components should be styled using SCSS Modules.
 
-Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`.
+
+Legacy components may still use Emotion while they are being migrated, but new Emotion usage should not be added.
 
 ### Deprecating styles
 
 Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
 
 ```jsx
 // component.tsx
+import clsx from 'clsx';
 import deprecated from '@wordpress/deprecated';
-import { Wrapper } from './styles.ts';
+import styles from './style.module.scss';
 
-function MyComponent( { __nextHasNoOuterMargins = false } ) {
+function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 	if ( ! __nextHasNoOuterMargins ) {
 		deprecated( 'Outer margin styles for wp.components.MyComponent', {
 			since: '6.0',
 			version: '6.2', // Set a reasonable grace period depending on impact
 			hint: 'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
 		} );
 	}
-	return <Wrapper __nextHasNoOuterMargins={ __nextHasNoOuterMargins } />;
+	return (
+		<div
+			className={ clsx(
+				'components-my-component',
+				styles.root,
+				! __nextHasNoOuterMargins &&
+					styles.deprecatedOuterMargins,
+				className
+			) }
```

That can happen when `styles.deprecatedOuterMargins` is `undefined`. The value used as a key. And yes, the non-object form avoids that, because the result of the `&&` expression is `undefined` and therefore ignored.

#### ciampo on 2026-06-24T13:56:22Z

URL: https://github.com/WordPress/gutenberg/pull/79442#discussion_r3467632863
Path: packages/components/CONTRIBUTING.md

```diff
@@ -416,48 +416,55 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 ## Styling
 
-All new component should be styled using [Emotion](https://emotion.sh/docs/introduction).
+All new components should be styled using SCSS Modules.
 
-Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`.
+
+Legacy components may still use Emotion while they are being migrated, but new Emotion usage should not be added.
 
 ### Deprecating styles
 
 Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
 
 ```jsx
 // component.tsx
+import clsx from 'clsx';
 import deprecated from '@wordpress/deprecated';
-import { Wrapper } from './styles.ts';
+import styles from './style.module.scss';
 
-function MyComponent( { __nextHasNoOuterMargins = false } ) {
+function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 	if ( ! __nextHasNoOuterMargins ) {
 		deprecated( 'Outer margin styles for wp.components.MyComponent', {
 			since: '6.0',
 			version: '6.2', // Set a reasonable grace period depending on impact
 			hint: 'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
 		} );
 	}
-	return <Wrapper __nextHasNoOuterMargins={ __nextHasNoOuterMargins } />;
+	return (
+		<div
+			className={ clsx(
+				'components-my-component',
+				styles.root,
+				! __nextHasNoOuterMargins &&
+					styles.deprecatedOuterMargins,
+				className
+			) }
```

Good point, adding a small tweak  in https://github.com/WordPress/gutenberg/pull/79490

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/CONTRIBUTING.md (MODIFIED, +33/-26)
- tools/eslint/config.mjs (MODIFIED, +4/-2)
- tools/eslint/suppressions.json (MODIFIED, +340/-0)
