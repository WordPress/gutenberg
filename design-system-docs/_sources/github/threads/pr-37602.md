---
number: 37602
type: pull_request
state: MERGED
merged_at: 2022-02-04T15:02:04Z
url: https://github.com/WordPress/gutenberg/pull/37602
matched_labels: ["[Feature] Component System"]
all_labels: ["[Type] Enhancement","[Package] Editor","[Feature] Component System"]
---

# PR #37602: Migrate post privacy confirmation from `confirm()` to `ConfirmDialog`

- URL: https://github.com/WordPress/gutenberg/pull/37602
- Author: chad1008
- Created: 2021-12-22T20:22:00Z
- Updated: 2022-02-04T15:21:03Z
- Comments: 11 of 11
- Review threads: 8 of 8
- Changed files: 2 of 2
- Diff: ../pr-diffs/37602.diff

## Body

## Description
This PR aims to migrate the post editor's `Switch to draft` button away from the current `confirm()` implementation and instead use the new experimental `ConfirmDialog` component.

## How has this been tested?
Before testing, cherrypick https://github.com/WordPress/gutenberg/pull/37959 on top of this PR. That will ensure the `ConfirmDialog` has the proper z-index to render on top of its parent component.

Running WordPress 5.8.2 via `wp-env`:
1. Create and publish a new post
2. In the **Post > Status & visibility** settings, click the current visibility (Public) to open the popover
3.  Choose **Private** and look for any unexpected console errors
4. Click **Cancel** and confirm that the post's visibility is not changed (no toast notification, and it should still appear private if you check the post list in another tab).
5. Open the Visibility popover again, and this time click **OK**
6. Confirm there are no unexpected console errors
7. Confirm the post updates to Private. You should see the usual updating animation on the Update/Publish button, and a toast notification should appear when complete
8. Confirm the post has been updated to Private.

I've tested in the latest Chrome, Firefox, and Safari.

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->
ironment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->

## Issue comments

### chad1008 on 2022-01-07T21:59:08Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1007769763

Updated this PR to simplify the implementation of ConfirmDialog (cc @ciampo)

### chad1008 on 2022-01-11T21:22:42Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1010370637

Thanks for the review @ciampo!

> I noticed that the dropdown with the Post visibility options sits on top of the confirm dialog.
Good point. I'll need to look into this a bit more. The easiest approach would be to modify the DOM directly and set something like `style.visibility('hidden')` on the dropdown while the confirmation is open, but that felt a little janky.

I also tried lifting the `ConfirmDialog` up a level in the component tree, and got it set up so that opening the confirmation stopped the dropdown from rendering... but not rendering the dropdown means the `ConfirmDialog` looses the ability (at least when paired with my current knowledge of React) to invoke the necessary functions to actually process the post update. I'll have to play with a bit more, but feel free to share any thoughts you have :)

> Otherwise, my feedback is basically the same as the one I left in PR 37491, including the consideration about unit/e2e tests.

Excellent, thank you again. I'll look into the e2e side of things and circle back.

### chad1008 on 2022-01-13T22:04:29Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1012552511

Regarding the `PostVisibility` dropdown rendering on top of the confirmation dialog:

There's a z-index issue behind this that I've proposed a change for in https://github.com/WordPress/gutenberg/pull/37959.

[See related comment here](https://github.com/WordPress/gutenberg/pull/37535#issuecomment-1012550139) :)

### ciampo on 2022-01-21T09:33:00Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1018336085

Update:

- I marked all of the memoization-related conversations as resolved for now, as discussed in [this comment](https://github.com/WordPress/gutenberg/pull/37492#discussion_r780807376).
- there's still an [open comment](https://github.com/WordPress/gutenberg/pull/37535#discussion_r780810448) that needs addressing
- not sure if you managed to look into fixing any broken e2e/unit tests (if any) and potentially adding any new ones

> There's a z-index issue behind this that I've proposed a change for in #37959.

Thank you for looking into the problem and opening #37959! As also explained in https://github.com/WordPress/gutenberg/pull/37535#issuecomment-1018332646, we'll likely have to wait until this issue is fixed before being able to merge this PR.

### fullofcaffeine on 2022-01-25T01:37:06Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1020721026

> not sure if you managed to look into fixing any broken e2e/unit tests (if any) and potentially adding any new ones

Agreed on adding a new E2E with some examples to test the `Set to Private` + `cancel` and `confirm` scenarios (the ones where the dialog is actually used). @chad1008 let me know if you need help with that :)

### fullofcaffeine on 2022-01-25T02:09:27Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1020737662

> Agreed on adding a new E2E with some examples to test the Set to Private + cancel and confirm scenarios (the ones where the dialog is actually used). @chad1008 let me know if you need help with that :)

Ah, there's already an E2E for this feature [here](https://github.com/WordPress/gutenberg/blob/trunk/packages/e2e-tests/specs/editor/various/post-visibility.test.js) (and it's currently failing in this PR), though I don't understand at first glance how it was supposed to deal with the blocking `confirm` call. There are other E2Es failing as part of this changeset, but some look flaky (like `specs/site-editor/template-part.test.js`). I'll have a look again tomorrow.

### fullofcaffeine on 2022-01-25T16:59:03Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1021404811

> though I don't understand at first glance how it was supposed to deal with the blocking confirm call

It looks like it was bypassing the logic that shows the native `confirm` dialog on purpose, and that makes sense: I assume there's no way to test a blocking `confirm` call given the orchestration code will also be blocked. Good news is that we will be able to include the dialog in this test now that we're using `ConfirmDialog` :)

### chad1008 on 2022-01-25T22:05:52Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1021653467

I've updated `packages/e2e-tests/specs/editor/various/post-visibility.test.js` to account for the new `ConfirmDialog`.

I'd also like to add a test that ensures the `Cancel` button of the dialog works as expected, but I'm running into an odd error that the button isn't clickable when the test runs, that I'll need to investigate further.

For some reason

```
await cancelButton.click();
```

fails and returns that the node is either not clickable or not an HTMLElement... but

```
await cancelButton.evaluate( ( b ) => b.click() );
```

works perfectly. Not sure why Puppeteer's `click()` is having trouble where JavaScript's isn't, but I'll investigate more tomorrow.

### ciampo on 2022-01-31T19:48:47Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1026146315

I'll give this PR a proper look tomorrow and hopefully we'll be able to merge!

### chad1008 on 2022-02-01T22:31:12Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1027352389

Thanks for the e2e review @ciampo. I was focused on tests for one of the other PRs today, but I'll address your suggestions here in the morning!

### chad1008 on 2022-02-02T12:36:19Z

URL: https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1027898912

Updated tests based on @ciampo's helpful suggestions ✅

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-01-09T17:53:12Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r780812630
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -145,6 +147,16 @@ export class PostVisibility extends Component {
 					/>
 				</div>
 			),
+			<ConfirmDialog
+				key={ 'private-publish-confirmation' }
```

What's this `key` used for?

Also, plain strings can also be passed as HTML attributes:

```suggestion
				key="private-publish-confirmation"
```

#### chad1008 on 2022-01-11T21:13:58Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r782529571
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -145,6 +147,16 @@ export class PostVisibility extends Component {
 					/>
 				</div>
 			),
+			<ConfirmDialog
+				key={ 'private-publish-confirmation' }
```

> What's this key used for?

Without it, we run into a `Each child in a list should have a unique "key" prop` warning. I _think_ this happens because the visibility options are dynamically rendered via `visibilityOptions.map` but I could be wrong. 

> Also, plain strings can also be passed as HTML attributes

Good point, thank you!

### Review thread 2 (resolved)

#### ciampo on 2022-01-09T17:57:58Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r780813158
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -145,6 +147,16 @@ export class PostVisibility extends Component {
 					/>
 				</div>
 			),
+			<ConfirmDialog
+				key={ 'private-publish-confirmation' }
+				isOpen={ this.state.showPrivateConfirmDialog }
+				onConfirm={ this.confirmPrivate }
+				onCancel={ () => {
+					this.setState( { showPrivateConfirmDialog: false } );
+				} }
```

Should we create a `handleDialogCancel` callback instead of creating a new function every re-render?

#### chad1008 on 2022-01-11T21:17:30Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r782531607
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -145,6 +147,16 @@ export class PostVisibility extends Component {
 					/>
 				</div>
 			),
+			<ConfirmDialog
+				key={ 'private-publish-confirmation' }
+				isOpen={ this.state.showPrivateConfirmDialog }
+				onConfirm={ this.confirmPrivate }
+				onCancel={ () => {
+					this.setState( { showPrivateConfirmDialog: false } );
+				} }
```

Good idea, thank you. Added in 45dd8ac

#### chad1008 on 2022-01-11T22:04:33Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r782560036
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -145,6 +147,16 @@ export class PostVisibility extends Component {
 					/>
 				</div>
 			),
+			<ConfirmDialog
+				key={ 'private-publish-confirmation' }
+				isOpen={ this.state.showPrivateConfirmDialog }
+				onConfirm={ this.confirmPrivate }
+				onCancel={ () => {
+					this.setState( { showPrivateConfirmDialog: false } );
+				} }
```

Ah. I now see (and learned from, thank you!) your [other comment regarding memoization via `useCallback()`](https://github.com/WordPress/gutenberg/pull/37491#discussion_r780804949) - so my fix here likely doesn't address your underlying concern. I'll hold off on further updates here pending a reply on [this question I added to yet another thread thread re: whether or not these memoizations are the way to go](https://github.com/WordPress/gutenberg/pull/37492#discussion_r782556597).

#### ciampo on 2022-01-11T22:19:55Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r782568655
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -145,6 +147,16 @@ export class PostVisibility extends Component {
 					/>
 				</div>
 			),
+			<ConfirmDialog
+				key={ 'private-publish-confirmation' }
+				isOpen={ this.state.showPrivateConfirmDialog }
+				onConfirm={ this.confirmPrivate }
+				onCancel={ () => {
+					this.setState( { showPrivateConfirmDialog: false } );
+				} }
```

The change you added is actually ok, since this component is still written as a class component. We can keep it as-is currently :)

### Review thread 3

#### chad1008 on 2022-01-26T20:38:30Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793028842
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

I'd prefer to locate the Cancel button using an xpath selection rather than CSS, but for reasons I haven't yet identified,
`page.$x( '//button[text()="Cancel"]' )`
and
`page.waitForXPath( '//button[text()="Cancel"]' )`
both cause Puppeteer's `ElementHandle.click()` to error out stating the node being passed in isn't visible or isn't an HTML element. The only way I've found to get either of those to successfully click the button is to swap out the `click()` method [as described previously](https://github.com/WordPress/gutenberg/pull/37602#issuecomment-1021653467).

So basically it seems like a choice between the alternate `click()` method, or the CSS selector... I'm not sure which would be the lesser of two evils...

#### ciampo on 2022-01-27T09:59:40Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793441391
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

I'm not an expert of Puppeteer and XPath.

> the node being passed in isn't visible or isn't an HTML element.

This, paired with the previous comment about having to `await`, makes me think that you may be trying to access the Cancel button too early? Maybe the dialog is still opening?

#### fullofcaffeine on 2022-01-27T18:42:15Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793898884
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

I've found out what's wrong: there are actually two buttons on the page with the `Cancel` inner text that happened to match that XPath expression. We were selecting the wrong one, which happened to not be accessible at the time puppeteer tried to click it (haven't dug into it, though). 

The fix is as simple as changing the XPath expression to `//div[contains(@class, "components-confirm-dialog")]//button[text()="Cancel"]')`. This still has the downside of relying on a CSS class (`components-confirm-dialog`) which might change in the future but is slightly better than relying on two CSS classes, which is the case with the CSS selector now. We could also traverse to the dialog div by `role` (which has a value of `dialog` atm). 

On the other hand, I think it's not a huge deal to leave it with the CSS selector you've used, @chad1008.

#### ciampo on 2022-01-27T18:59:52Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793911945
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

Instead of using the `components-confirm-dialog` classname, could we use something like `role="dialog"` ?

Alternatively, if/when we will add props for confirm/cancel button labels, we'll be able to add a unique label that will avoid the problem altogether

#### fullofcaffeine on 2022-01-27T19:00:44Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793912589
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

> Instead of using the components-confirm-dialog classname, could we use something like role="dialog" ?

~~Yep, we can do that 👍🏻~~

#### ciampo on 2022-01-27T19:02:11Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793913596
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

> Yep, we can do that 👍🏻

Awesome! Using roles for querying the dom is a great way to add resilience against future changes, and tests the component more from the point of view of the user (instead of testing implementation details)

#### fullofcaffeine on 2022-01-27T19:08:31Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793918177
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

Sorry, wrote too fast :(... I just tested traversing using the `role` attribute and found the same issue. It seems there are more divs on the page with the same role, so it's not specific enough for our use-case :(

#### fullofcaffeine on 2022-01-27T19:10:51Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r793919885
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

I think the best trade-off is to use the `//div[contains(@class, "components-confirm-dialog")]//button[text()="Cancel"]')` xpath.

#### ciampo on 2022-01-28T09:28:35Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r794335859
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

> It seems there are more divs on the page with the same role, so it's not specific enough for our use-case :(

This may be worth looking into a little bit — it's weird that there are 2 open dialogs in the page (with 2 cancel buttons). It almost sounds as if the `ConfirmDialog` is being rendered twice? And if not the same `ConfirmDialog`, it's still weird that there are 2 open dialogs in the same page?

> I think the best trade-off is to use the `//div[contains(@class, "components-confirm-dialog")]//button[text()="Cancel"]')` xpath.

One last attempt: what about`[role='dialog'] > [role='document'] > button[text='Cancel`]` (of course translated to the correct xpath syntax)?

If nothing else works, we can definitely go with your suggestion. But I'd definitely look a bit more into why there isn't only one "Cancel` button inside a `dialog` in the page.

#### chad1008 on 2022-01-31T12:28:08Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r795622177
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

> But I'd definitely look a bit more into why there isn't only one "Cancelbutton inside adialog` in the page.

Found it. It's the core UI link inserter:
![image](https://user-images.githubusercontent.com/13856531/151785401-86a8e914-b2f8-4767-a6b1-8d26536f439b.png)

It gets rendered when the page loads, but is hidden by a couple of `display: none` on `#wp-link-wrap` (one inline style and one added via `editor.css`). The modal (and our phantom button) only actually appear on screen in the Classic Editor when adding a link to text on the page. 

I'll work on an alternate XPath with this in mind and update when I have something!

#### chad1008 on 2022-01-31T15:23:06Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r795775850
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

`//*[@role="dialog"][not(@id="wp-link-wrap")]//button[text()="Cancel"]` should do the trick. (https://github.com/WordPress/gutenberg/pull/37602/commits/423ff74056c998361aab7ec73abba24693a4ee4b)

We're relying on the other button's wrapper not to go and change its ID on us, but I don't know that we have a better way to differentiate between the two. Thoughts? 🙂

#### ciampo on 2022-01-31T19:47:51Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r796010324
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,41 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const cancelButton = await page.waitForSelector(
+				'.components-confirm-dialog button.is-tertiary'
```

Of all alternatives, this is my favourite! This test will break only if:

- the dialog's id changes
- a new dialog with a "Cancel" button is added

In both scenarios, it should be quite easy to understand that the change caused the test failure.

I also think that we should update this test as soon as we introduce props for changing the confirm/cancel button labels on `ConfirmDialog`:

> Alternatively, if/when we will add props for confirm/cancel button labels, we'll be able to add a unique label that will avoid the problem altogether

### Review thread 4 (resolved)

#### ciampo on 2022-02-01T09:54:20Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r796427937
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -26,6 +26,11 @@ describe( 'Post visibility', () => {
 			);
 			await privateLabel.click();
 
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const [ confirmButton ] = await page.$x( '//button[text()="OK"]' );
```

To make this test a bit more resilient, should we specify that the button should be inside a `@role="dialog"` ?

#### chad1008 on 2022-02-02T12:29:32Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r797555989
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -26,6 +26,11 @@ describe( 'Post visibility', () => {
 			);
 			await privateLabel.click();
 
+			await page.waitForSelector( '.components-confirm-dialog' );
+
+			const [ confirmButton ] = await page.$x( '//button[text()="OK"]' );
```

👍

### Review thread 5 (resolved)

#### ciampo on 2022-02-01T09:55:45Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r796429114
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -26,6 +26,11 @@ describe( 'Post visibility', () => {
 			);
 			await privateLabel.click();
 
+			await page.waitForSelector( '.components-confirm-dialog' );
```

Nit, but should we also change the selector here to avoid a hardcoded classname? Maybe we could look for the `'Would you like to privately publish this post now?` text?

#### chad1008 on 2022-02-02T12:29:29Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r797555945
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -26,6 +26,11 @@ describe( 'Post visibility', () => {
 			);
 			await privateLabel.click();
 
+			await page.waitForSelector( '.components-confirm-dialog' );
```

👍

### Review thread 6 (resolved)

#### ciampo on 2022-02-01T09:56:06Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r796429360
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,40 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
```

Same as previous comment

#### chad1008 on 2022-02-02T12:29:25Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r797555902
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -34,6 +39,40 @@ describe( 'Post visibility', () => {
 
 			expect( currentStatus ).toBe( 'private' );
 		} );
+
+		it( `can be canceled when the viewport is ${ viewport }`, async () => {
+			await setBrowserViewport( viewport );
+
+			await createNewPost();
+
+			await openDocumentSettingsSidebar();
+
+			const initialStatus = await page.evaluate( () => {
+				return wp.data
+					.select( 'core/editor' )
+					.getEditedPostAttribute( 'status' );
+			} );
+
+			await page.click( '.edit-post-post-visibility__toggle' );
+
+			const [ privateLabel ] = await page.$x(
+				'//label[text()="Private"]'
+			);
+			await privateLabel.click();
+			await page.waitForSelector( '.components-confirm-dialog' );
```

👍

### Review thread 7 (resolved)

#### ciampo on 2022-02-01T10:06:34Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r796437629
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -60,6 +99,11 @@ describe( 'Post visibility', () => {
 		const [ privateLabel ] = await page.$x( '//label[text()="Private"]' );
 		await privateLabel.click();
 
+		await page.waitForSelector( '.components-confirm-dialog' );
+
+		const [ confirmButton ] = await page.$x( '//button[text()="OK"]' );
```

Same observations as above

#### chad1008 on 2022-02-02T12:29:25Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r797555893
Path: packages/e2e-tests/specs/editor/various/post-visibility.test.js

```diff
@@ -60,6 +99,11 @@ describe( 'Post visibility', () => {
 		const [ privateLabel ] = await page.$x( '//label[text()="Private"]' );
 		await privateLabel.click();
 
+		await page.waitForSelector( '.components-confirm-dialog' );
+
+		const [ confirmButton ] = await page.$x( '//button[text()="OK"]' );
```

👍

### Review thread 8

#### ciampo on 2022-02-04T14:41:39Z

URL: https://github.com/WordPress/gutenberg/pull/37602#discussion_r799523979
Path: packages/editor/src/components/post-visibility/index.js

```diff
@@ -35,21 +39,23 @@ export class PostVisibility extends Component {
 	}
 
 	setPrivate() {
-		if (
-			// eslint-disable-next-line no-alert
-			! window.confirm(
-				__( 'Would you like to privately publish this post now?' )
-			)
-		) {
-			return;
-		}
+		this.setState( { showPrivateConfirmDialog: true } );
+	}
 
+	confirmPrivate = () => {
 		const { onUpdateVisibility, onSave } = this.props;
 
 		onUpdateVisibility( 'private' );
-		this.setState( { hasPassword: false } );
+		this.setState( {
+			hasPassword: false,
+			showPrivateConfirmDialog: false,
+		} );
 		onSave();
-	}
+	};
```

As a future improvement, it'd be good to investigate what happens in case of failure to set the post visibility to `private` (for example, if the internet connection stops working of is the server returns an error).

How do the `onUpdateVisibility` and `onSave` functions behave? In case of error, should we close the dialog and display a message? Or should we keep the dialog open until we have a confirmation of success, and potentially show an error message within the dialog?

This task is actually partially related to the conversation in https://github.com/WordPress/gutenberg/pull/37492#discussion_r791483132 regarding adding a `isDisabled` prop to the `ConfirmDialog` component

## Changed files

- packages/e2e-tests/specs/editor/various/post-visibility.test.js (MODIFIED, +54/-0)
- packages/editor/src/components/post-visibility/index.js (MODIFIED, +25/-11)
