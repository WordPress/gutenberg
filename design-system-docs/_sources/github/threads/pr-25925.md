---
number: 25925
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/25925
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #25925: FormToggle: Use CSS-in-JS

- URL: https://github.com/WordPress/gutenberg/pull/25925
- Author: sarayourfriend
- Created: 2020-10-07T21:22:16Z
- Updated: 2020-11-09T17:28:26Z
- Comments: 4 of 4
- Review threads: 8 of 8
- Changed files: 12 of 12
- Diff: ../pr-diffs/25925.diff

## Body

<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/master/docs/contributors/repository-management.md#pull-requests. -->

## Description
This PR updates the `FormToggle` component to use CSS-in-JS similar to how newer components do like the `AnglePickerControl` and `Text`.

## How has this been tested?
I've compared the form toggle on storybook for this branch and the upstream storybook.

## Screenshots <!-- if applicable -->
There are no visual changes.

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

## Issue comments

### ItsJonQ on 2020-10-19T14:16:48Z

URL: https://github.com/WordPress/gutenberg/pull/25925#issuecomment-712190761

@saramarcondes Looks like we have to rebase with `master` after some of your other CSS-in-JS updates ❤️

### ItsJonQ on 2020-10-21T22:31:30Z

URL: https://github.com/WordPress/gutenberg/pull/25925#issuecomment-713914142

@saramarcondes Haiiiii! I did another pass. It looks good! The only thing that I wasn't sure of was the `todo` for the variable. Another (but very minor) observation would be the indentation for a couple of lines. I would assume the linter would be able to auto format these on commit 🤔

### ItsJonQ on 2020-10-23T19:43:18Z

URL: https://github.com/WordPress/gutenberg/pull/25925#issuecomment-715554290

Why are the E2E tests sad 😭 .

@saramarcondes would you be able to rebase with latest `master`? Maybe that'll help 🤞

### sarayourfriend on 2020-11-09T17:28:26Z

URL: https://github.com/WordPress/gutenberg/pull/25925#issuecomment-724158972

Closing in favor of focusing on G2-components integration.

## Review threads

### Review thread 1 (resolved)

#### sarayourfriend on 2020-10-08T15:36:09Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r501818128
Path: packages/components/src/form-toggle/test/index.js

```diff
@@ -17,16 +18,14 @@ describe( 'FormToggle', () => {
 				true
 			);
 			expect( formToggle.hasClass( 'is-checked' ) ).toBe( false );
-			expect( formToggle.type() ).toBe( 'span' );
+			expect( formToggle.type().__emotion_base ).toBe( 'span' );
```

I really hate this change I had to make to allow this test to pass, reaching for a private, internal variable like this never feels good to me. Should this just be replaced by snapshot or is there something else we can test that gets at the same intention without depending on such a specific implementation detail?

#### ItsJonQ on 2020-10-08T21:31:31Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r502024365
Path: packages/components/src/form-toggle/test/index.js

```diff
@@ -17,16 +18,14 @@ describe( 'FormToggle', () => {
 				true
 			);
 			expect( formToggle.hasClass( 'is-checked' ) ).toBe( false );
-			expect( formToggle.type() ).toBe( 'span' );
+			expect( formToggle.type().__emotion_base ).toBe( 'span' );
```

I agree with your sentiment. I think tests like these should either be snapshot or probably deleted 🙈

### Review thread 2 (resolved)

#### ItsJonQ on 2020-10-09T21:23:36Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r502677414
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,108 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
+	background-color: ${ color( 'darkGray.primary' ) };
+	border: 5px solid ${ color(
+		'darkGray.primary'
+	) }; // Has explicit border to act as a fill in Windows High Contrast Mode.
+`;
+
+export const StyledToggleInput = styled.input`
+	position: absolute;
+	top: 0;
+	${ rtl( { left: 0 } ) };
+	width: 100%;
+	height: 100%;
+	opacity: 0;
+	margin: 0;
+	padding: 0;
+	z-index: z-index( '.components-form-toggle__input' );
```

We'll need to change this :). This is probably a Sass mixin.
To start, we can create a variable with the `zIndex` (similar to what you did with others in `utils`).

There's going to be some disconnect between the Sass values and JS values during this migration process.

Hopefully we can introduce a JS based zIndex system - perhaps something similar to the one within G2 Components:

https://github.com/ItsJonQ/g2/blob/master/packages/styles/src/mixins/zIndex.js
https://github.com/ItsJonQ/g2/blob/master/packages/styles/src/presets/zIndex.js#L4

### Review thread 3 (resolved)

#### ItsJonQ on 2020-10-09T22:01:18Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r502687525
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,108 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
+	background-color: ${ color( 'darkGray.primary' ) };
+	border: 5px solid ${ color(
+		'darkGray.primary'
+	) }; // Has explicit border to act as a fill in Windows High Contrast Mode.
+`;
+
+export const StyledToggleInput = styled.input`
+	position: absolute;
+	top: 0;
+	${ rtl( { left: 0 } ) };
+	width: 100%;
+	height: 100%;
+	opacity: 0;
+	margin: 0;
+	padding: 0;
+	z-index: z-index( '.components-form-toggle__input' );
+
+	// This overrides a border style that is inherited from parent checkbox styles.
+	border: none;
+	&:checked {
+		background: none;
+	}
+
+	// Don't show custom checkbox checkmark.
+	&::before {
+		content: '';
+	}
+`;
+
+export const StyledFormToggle = styled.span`
+	position: relative;
+	display: inline-block;
+
+	${ ( props ) =>
+		props.isChecked
```

I would probably abstract this out, similar to what you did in some of your other CSS-in-JS PRs 😊 .
It might make the conditional styles easier to read/work with

### Review thread 4 (resolved)

#### ItsJonQ on 2020-10-09T22:04:04Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r502688087
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,108 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
+	background-color: ${ color( 'darkGray.primary' ) };
+	border: 5px solid ${ color(
+		'darkGray.primary'
+	) }; // Has explicit border to act as a fill in Windows High Contrast Mode.
+`;
+
+export const StyledToggleInput = styled.input`
+	position: absolute;
+	top: 0;
+	${ rtl( { left: 0 } ) };
+	width: 100%;
+	height: 100%;
+	opacity: 0;
+	margin: 0;
+	padding: 0;
+	z-index: z-index( '.components-form-toggle__input' );
+
+	// This overrides a border style that is inherited from parent checkbox styles.
+	border: none;
+	&:checked {
+		background: none;
+	}
+
+	// Don't show custom checkbox checkmark.
+	&::before {
+		content: '';
+	}
+`;
+
+export const StyledFormToggle = styled.span`
+	position: relative;
+	display: inline-block;
+
+	${ ( props ) =>
+		props.isChecked
+			? css`
+					${ StyledToggleTrack } {
```

This is interesting...

Targeting another component is supported in Emotion, but only with the [babel plugin](https://emotion.sh/docs/styled#targeting-another-emotion-component).

It looks like we're using it for the project, so I think it should be 👍 .
Just pointing that out for extra context + info.

#### sarayourfriend on 2020-10-09T23:33:07Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r502712214
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,108 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
+	background-color: ${ color( 'darkGray.primary' ) };
+	border: 5px solid ${ color(
+		'darkGray.primary'
+	) }; // Has explicit border to act as a fill in Windows High Contrast Mode.
+`;
+
+export const StyledToggleInput = styled.input`
+	position: absolute;
+	top: 0;
+	${ rtl( { left: 0 } ) };
+	width: 100%;
+	height: 100%;
+	opacity: 0;
+	margin: 0;
+	padding: 0;
+	z-index: z-index( '.components-form-toggle__input' );
+
+	// This overrides a border style that is inherited from parent checkbox styles.
+	border: none;
+	&:checked {
+		background: none;
+	}
+
+	// Don't show custom checkbox checkmark.
+	&::before {
+		content: '';
+	}
+`;
+
+export const StyledFormToggle = styled.span`
+	position: relative;
+	display: inline-block;
+
+	${ ( props ) =>
+		props.isChecked
+			? css`
+					${ StyledToggleTrack } {
```

I didn't realize that was only due to a babel plugin, TIL! Thanks!

### Review thread 5 (resolved)

#### ItsJonQ on 2020-10-21T22:25:03Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r509756321
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl, zIndex } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
+	background-color: ${ color( 'darkGray.primary' ) };
+	border: 5px solid ${ color(
+		'darkGray.primary'
+	) }; // Has explicit border to act as a fill in Windows High Contrast Mode.
+`;
+
+export const StyledToggleInput = styled.input`
+	position: absolute;
+	top: 0;
+	${ rtl( { left: 0 } ) };
+	width: 100%;
+	height: 100%;
+	opacity: 0;
+	margin: 0;
+	padding: 0;
+	${ zIndex( 'StyledToggleInput' ) };
+
+	// This overrides a border style that is inherited from parent checkbox styles.
+	border: none;
+	&:checked {
+		background: none;
+	}
+
+	// Don't show custom checkbox checkmark.
+	&::before {
+		content: '';
+	}
+`;
+
+const checkedStyles = css`
+	${ StyledToggleTrack } {
+		background-color: var( --wp-admin-theme-color );
+		border: ${ toggleBorderWidth }px solid var( --wp-admin-theme-color );
+		border: ${ toggleHeight / 2 }px solid transparent; // Expand the border to fake a solid in Windows High Contrast Mode.
+	}
+
+	${ StyledToggleThumb } {
+		background-color: ${ color( 'white' ) };
+		border-width: 0; // Zero out the border color to make the thumb invisible in Windows High Contrast Mode.
+		${ appearTransform() }
+	}
+`;
+
+export const StyledFormToggle = styled.span`
+	position: relative;
+	display: inline-block;
+
+	${ ( props ) => ( props.isChecked ? checkedStyles : '' ) }
+
+    ${ StyledToggleInput }:focus + ${ StyledToggleTrack } {
```

Looks like there's something funky with the indentation here 🤔

### Review thread 6 (resolved)

#### ItsJonQ on 2020-10-21T22:27:16Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r509758330
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl, zIndex } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
+	background-color: ${ color( 'darkGray.primary' ) };
+	border: 5px solid ${ color(
+		'darkGray.primary'
+	) }; // Has explicit border to act as a fill in Windows High Contrast Mode.
+`;
+
+export const StyledToggleInput = styled.input`
+	position: absolute;
+	top: 0;
+	${ rtl( { left: 0 } ) };
+	width: 100%;
+	height: 100%;
+	opacity: 0;
+	margin: 0;
+	padding: 0;
+	${ zIndex( 'StyledToggleInput' ) };
+
+	// This overrides a border style that is inherited from parent checkbox styles.
+	border: none;
+	&:checked {
+		background: none;
+	}
+
+	// Don't show custom checkbox checkmark.
+	&::before {
+		content: '';
+	}
+`;
+
+const checkedStyles = css`
+	${ StyledToggleTrack } {
+		background-color: var( --wp-admin-theme-color );
+		border: ${ toggleBorderWidth }px solid var( --wp-admin-theme-color );
+		border: ${ toggleHeight / 2 }px solid transparent; // Expand the border to fake a solid in Windows High Contrast Mode.
+	}
+
+	${ StyledToggleThumb } {
+		background-color: ${ color( 'white' ) };
+		border-width: 0; // Zero out the border color to make the thumb invisible in Windows High Contrast Mode.
+		${ appearTransform() }
+	}
+`;
+
+export const StyledFormToggle = styled.span`
+	position: relative;
+	display: inline-block;
+
+	${ ( props ) => ( props.isChecked ? checkedStyles : '' ) }
+
+    ${ StyledToggleInput }:focus + ${ StyledToggleTrack } {
+		box-shadow: 0 0 0 2px ${ color( 'white' ) },
+			0 0 0 ${
+				2 + 1.5 /* @todo(saramarcondes) remove inlined variable */
```

Haha!

@saramarcondes remove inlined variable 😊 

If needed, we can do this in the utils (similar to how you've done the other updates)

### Review thread 7 (resolved)

#### ItsJonQ on 2020-10-21T22:28:47Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r509759784
Path: packages/components/src/form-toggle/styles/form-toggle-styles.js

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import { color, reduceMotion, rtl, zIndex } from '../../utils';
+import {
+	toggleBorderWidth,
+	toggleWidth,
+	toggleHeight,
+	appearTransform,
+} from './utils';
+
+export const StyledToggleTrack = styled.span`
+	content: '';
+	display: inline-block;
+	box-sizing: border-box;
+	vertical-align: top;
+	background-color: ${ color( 'white' ) };
+	border: ${ toggleBorderWidth }px solid ${ color( 'darkGray.primary' ) };
+	width: ${ toggleWidth }px;
+	height: ${ toggleHeight }px;
+	border-radius: ${ toggleHeight / 2 }px;
+	transition: 0.2s background ease;
+	${ reduceMotion( 'transition' ) }
+`;
+
+export const StyledToggleThumb = styled.span`
+	display: block;
+	position: absolute;
+	box-sizing: border-box;
+	top: ${ toggleBorderWidth * 3 }px;
+	${ rtl( { left: `${ toggleBorderWidth * 3 }px` } ) };
+	width: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	height: ${ toggleHeight - toggleBorderWidth * 6 }px;
+	border-radius: 50%;
+	transition: 0.1s transform ease;
+    ${ reduceMotion( 'transition' ) }
```

Indentation looks funky here. I wonder if something is up with the linter (or prettier?) not being able to catch/format this 🤔

### Review thread 8

#### ItsJonQ on 2020-10-21T22:29:50Z

URL: https://github.com/WordPress/gutenberg/pull/25925#discussion_r509760212
Path: packages/components/src/utils/z-index.js

```diff
@@ -0,0 +1,15 @@
+/**
+ * External dependencies
+ */
+import { get } from 'lodash';
+import { css } from '@emotion/core';
+
+/**
+ * Internal dependencies
+ */
+import Z_INDEX from './z-index-values';
+
+export const zIndex = ( key ) =>
```

Yay! The starting point for the zIndex system :)

## Changed files

- packages/block-editor/src/components/link-control/test/__snapshots__/index.js.snap (MODIFIED, +1/-1)
- packages/block-editor/src/components/responsive-block-control/test/__snapshots__/index.js.snap (MODIFIED, +1/-1)
- packages/components/src/form-toggle/index.js (MODIFIED, +15/-5)
- packages/components/src/form-toggle/style.scss (DELETED, +0/-90)
- packages/components/src/form-toggle/styles/form-toggle-styles.js (ADDED, +109/-0)
- packages/components/src/form-toggle/styles/utils.js (ADDED, +30/-0)
- packages/components/src/form-toggle/test/__snapshots__/index.js.snap (ADDED, +137/-0)
- packages/components/src/form-toggle/test/index.js (MODIFIED, +18/-29)
- packages/components/src/style.scss (MODIFIED, +0/-1)
- packages/components/src/utils/style-mixins.js (MODIFIED, +1/-0)
- packages/components/src/utils/z-index-values.js (ADDED, +5/-0)
- packages/components/src/utils/z-index.js (ADDED, +15/-0)
