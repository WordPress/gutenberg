---
number: 63985
type: pull_request
state: MERGED
merged_at: 2024-07-29T17:00:42Z
url: https://github.com/WordPress/gutenberg/pull/63985
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","First-time Contributor"]
---

# PR #63985: CustomSelectControl: support generic props type

- URL: https://github.com/WordPress/gutenberg/pull/63985
- Author: meteorlxy
- Created: 2024-07-26T09:56:07Z
- Updated: 2024-07-29T17:01:09Z
- Comments: 3 of 3
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/63985.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Fixes #63984

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

It would be helpful to infer the option type

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Making the `CustomSelectProps` a generic type
- Exporting `CustomSelectOption` and `CustomSelectChangeObject` type

## Testing Instructions

Using the following snippet to see if the `selectedItem` type is inferred correctly:

```tsx
import { CustomSelectControl } from '@wordpress/components';
import type { CustomSelectOption } from '@wordpress/components/build-types/custom-select-control/types';

type MyOption = CustomSelectOption & {
  foo: string;
};

const options: MyOption[] = [
  {
    key: 'none',
    name: 'None',
    foo: 'bar',
  },
  {
    key: 'foo',
    name: 'Foo',
    foo: 'bar',
  },
];

const render = () => (
  <CustomSelectControl
    label="foo"
    onChange={({ selectedItem }) => {
      console.log(selectedItem.foo);
    }}
    options={options}
    value={options[0]}
  />
);
```

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

N/A

## Screenshots or screencast <!-- if applicable -->

**Before**

<img width="760" alt="image" src="https://github.com/user-attachments/assets/241e95bb-5bf8-4c3e-aecd-ff46cfb03944">

**After**

<img width="533" alt="image" src="https://github.com/user-attachments/assets/6d9982f0-562c-4b17-ab06-903d9aef131c">

## Issue comments

### github-actions on 2024-07-26T09:56:20Z

URL: https://github.com/WordPress/gutenberg/pull/63985#issuecomment-2252393252

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: meteorlxy <meteorlxy@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-07-26T09:56:32Z

URL: https://github.com/WordPress/gutenberg/pull/63985#issuecomment-2252393964

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @meteorlxy! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### meteorlxy on 2024-07-29T14:52:23Z

URL: https://github.com/WordPress/gutenberg/pull/63985#issuecomment-2256156619

@tyxla updated

## Review threads

### Review thread 1

#### tyxla on 2024-07-29T12:55:16Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695165428
Path: packages/components/CHANGELOG.md

```diff
@@ -6,6 +6,10 @@
 
 -   `CustomSelectControl`: Restore `describedBy` functionality ([#63957](https://github.com/WordPress/gutenberg/pull/63957)).
 
+### Enhancements
```

Since this is a type change, I believe it should be under `Internal` instead.

#### meteorlxy on 2024-07-29T13:58:25Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695292517
Path: packages/components/CHANGELOG.md

```diff
@@ -6,6 +6,10 @@
 
 -   `CustomSelectControl`: Restore `describedBy` functionality ([#63957](https://github.com/WordPress/gutenberg/pull/63957)).
 
+### Enhancements
```

Cuz the type will be used by user, and will improve the developer experience, I think it's more of an improvement / enhancement?

#### tyxla on 2024-07-29T14:22:08Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695329962
Path: packages/components/CHANGELOG.md

```diff
@@ -6,6 +6,10 @@
 
 -   `CustomSelectControl`: Restore `describedBy` functionality ([#63957](https://github.com/WordPress/gutenberg/pull/63957)).
 
+### Enhancements
```

IMHO, types and type changes are an internal kind of enhancement, and we have this "Internal" group to distinguish them. General Enhancements would usually be targeted towards end users.

### Review thread 2

#### tyxla on 2024-07-29T13:07:11Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695193170
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -51,7 +51,9 @@ function getDescribedBy( currentValue: string, describedBy?: string ) {
 	return sprintf( __( 'Currently selected: %s' ), currentValue );
 }
 
-function CustomSelectControl( props: CustomSelectProps ) {
+function CustomSelectControl< T extends CustomSelectOption >(
```

Should we add a default to the generic so any existing consumers will continue working without changing anything? 

The same question applies to all instances where we're adding a generic and there wasn't one before.

#### meteorlxy on 2024-07-29T14:03:26Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695300452
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -51,7 +51,9 @@ function getDescribedBy( currentValue: string, describedBy?: string ) {
 	return sprintf( __( 'Currently selected: %s' ), currentValue );
 }
 
-function CustomSelectControl( props: CustomSelectProps ) {
+function CustomSelectControl< T extends CustomSelectOption >(
```

The generic param is not going to be set manually by user. It's used for type inferring. To be specific, it will use the item type of the `options` prop automatically. So consumers would not need to change anything.

You could checkout this branch and test with the snippets that pasted in the "Testing Instructions" section.

#### jsnajdr on 2024-07-29T14:08:37Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695308387
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -51,7 +51,9 @@ function getDescribedBy( currentValue: string, describedBy?: string ) {
 	return sprintf( __( 'Currently selected: %s' ), currentValue );
 }
 
-function CustomSelectControl( props: CustomSelectProps ) {
+function CustomSelectControl< T extends CustomSelectOption >(
```

TypeScript should always be able to infer the `T` type. `CustomSelectProps` have a mandatory `options: Array<T>` field, so `T` is always the type of whatever you pass as `options`, it's easy to infer. TypeScript will merely check if that type is compatible with `CustomSelectOption`.

### Review thread 3

#### tyxla on 2024-07-29T15:09:41Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695407839
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,15 +24,15 @@ type Option = {
 /**
  * The object returned from the onChange event.
  */
-type ChangeObject = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

Do we need to export this one? Seems like we don't.

#### meteorlxy on 2024-07-29T15:16:16Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695418304
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,15 +24,15 @@ type Option = {
 /**
  * The object returned from the onChange event.
  */
-type ChangeObject = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

It would be useful if user want to get the `ChangeObject` type, otherwise we have to use the following snippet to get the type:

<img width="548" alt="image" src="https://github.com/user-attachments/assets/e616364c-9dfc-4efd-bfdb-135276b22b56">

#### tyxla on 2024-07-29T15:28:41Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695437209
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,15 +24,15 @@ type Option = {
 /**
  * The object returned from the onChange event.
  */
-type ChangeObject = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

Right 👍 

Exporting it here doesn't export it from the package, though. And if we're not importing it inside the package, then it's effectively unnecessary to export it, IMHO. We can always start exporting it if we need to in the future.

#### mirka on 2024-07-29T15:47:02Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695464196
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,15 +24,15 @@ type Option = {
 /**
  * The object returned from the onChange event.
  */
-type ChangeObject = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

Yes, at the moment we are avoiding public type exports as much as possible, thanks 🙏

#### meteorlxy on 2024-07-29T16:25:57Z

URL: https://github.com/WordPress/gutenberg/pull/63985#discussion_r1695520371
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,15 +24,15 @@ type Option = {
 /**
  * The object returned from the onChange event.
  */
-type ChangeObject = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

ok, removed

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/custom-select-control/index.tsx (MODIFIED, +7/-5)
- packages/components/src/custom-select-control/types.ts (MODIFIED, +7/-7)
