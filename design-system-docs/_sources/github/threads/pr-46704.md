---
number: 46704
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/46704
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #46704: TabPanel: add tabName prop (controlled component)

- URL: https://github.com/WordPress/gutenberg/pull/46704
- Author: madhusudhand
- Created: 2022-12-21T12:43:34Z
- Updated: 2025-04-22T10:29:10Z
- Comments: 14 of 14
- Review threads: 7 of 7
- Changed files: 6 of 6
- Diff: ../pr-diffs/46704.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Add new prop `selectedTabName` to control the component tab selection programatically.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Currently it is not possible to activate a tab using component props. This allows to activate a tab programatically.
The first usecase for this is to auto select the tab in `style book` based on the selected block in global styles.

![tab-selection](https://user-images.githubusercontent.com/1935113/209305187-60e235b4-c052-4d68-9996-dcdb984e7af2.gif)

## Issue comments

### madhusudhand on 2023-01-03T08:46:15Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1369509889

> The disabled tab feature will be landing soon (https://github.com/WordPress/gutenberg/pull/46471), so controlled mode also needs to take that into account.

Thanks for the mention. Now that the PR is merged, updated the code to consider disabled tab scenario.

### ciampo on 2023-01-03T15:11:27Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1369880872

Just linking to [this comment](https://github.com/WordPress/gutenberg/pull/46275#discussion_r1038088781) that I had made a while ago about `TabPanel` and its controlled mode. A few notes in particular:

- prop name: in my comment I proposed `tabName`, I thought it made more sense (usually inputs have a `value` prop, not a `selectedValue` prop)
- controlled/uncontrolled: have we considered using the [`useControlledValue` hook](https://github.com/WordPress/gutenberg/blob/8633ae8baf401ad3236e5c6debf0d5cb127af44a/packages/components/src/utils/hooks/use-controlled-value.ts)?
- have we checked that these API changes are still compatible with our plans of migrating this component to use `ariakit` under the hood ? (I personally don't think there should be any problems, but it would be good to double-check)
- we need to make sure that current uncontrolled usages of the component still work as expected (we should be adding specific unit tests)

### github-actions on 2023-01-06T09:53:10Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1373413632

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 4bfaeb9f95e3ada296c795b5bb866ef837663852.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/6271247960
📝  Reported issues:
- #53524 in `/test/e2e/specs/editor/various/multi-block-selection.spec.js`

### ciampo on 2023-01-12T14:17:31Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1380421052

This PR needs a rebase after the fix in #47100 was merged. I can take another look at the code once the rebase has happened :)

### ciampo on 2023-01-26T23:18:30Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1405802254

Hey @madhusudhand ,  are you still able to work on this PR ?

### madhusudhand on 2023-01-31T12:15:52Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1410246165

Hey @ciampo 

Updated the component and following are the test scenarios.

<img width="838" alt="image" src="https://user-images.githubusercontent.com/1935113/215757074-f3394cf9-7f16-413b-884b-37caa7f793d7.png">

### ciampo on 2023-02-07T18:09:55Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1421233588

Hey @madhusudhand , do you still have the capacity to work on this?

### madhusudhand on 2023-02-08T05:17:42Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1422030265

@ciampo I would be able to continue the work after 16th Feb. If you have capacity feel free to take it from me. Thanks.

### aaronrobertshaw on 2023-02-15T02:20:05Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1430659215

> We open a new PR, where we add the new unit tests to the current implementation of TabPanel. Of course, we'll be able only to test the component in uncontrolled mode, but adding those tests separately can help us to certify the behaviour of the component on trunk

I put together a draft PR (https://github.com/WordPress/gutenberg/pull/48086) that you might use as a base for verifying the current component behaviour on trunk. I'll be AFK for the next week so feel free to pick it up or close it if it doesn't help.

### madhusudhand on 2023-02-27T15:13:31Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1446512132

**Update**: Rebased the PR with unit testes from #48086 and they all are running great. Will push more tests for controlled mode.

### ciampo on 2023-02-27T15:22:30Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1446529437

> **Update**: Rebased the PR with unit testes from #48086 and they all are running great. Will push more tests for controlled mode.

Nice! You can take inspiration from `ComboboxControl`'s tests on how to add the controlled version of the component to the tests:

https://github.com/WordPress/gutenberg/blob/dc32380b2dc744a8cfb7725c0d7f145a471ec3f3/packages/components/src/combobox-control/test/index.tsx#L61-L85

### madhusudhand on 2023-09-22T07:09:16Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1730914654

Updated the PR with revised changes and included unit tests for controlled and uncontrolled behaviours.

Note: removed `useControlledValue` hook from this revision as it is becoming redundant with `Ariakit.useTabStore`

### ciampo on 2023-09-22T07:17:00Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1730924257

Hey @madhusudhand , thank you for continuing the work here!

In the past months, we've started working on improving the component:

- We've [refactored `TabPanel` to Ariakit](https://github.com/WordPress/gutenberg/pull/52133)
- We started work on [a new `Tabs` component](https://github.com/WordPress/gutenberg/pull/53960) — the name better follows the WAI-ARIA pattern, and having a new component allows us to introduce better APIs (including controlled mode and granular components for better composition)

The plan is to leave `TabPanel` around for backwards compatibility reasons, but to stop maintaining it (if not for bug fixes).

Furthermore, these components have quite an opinionated behaviour when uncontrolled around making sure that a tab gets selected even when the currently selected tab becomes disabled / gets removed. When allowing the component to be controlled, we have to ask ourselves how much of that custom behaviour we want to inherit (you can follow the conversation in https://github.com/WordPress/gutenberg/pull/53960 ).

### madhusudhand on 2023-11-06T11:55:24Z

URL: https://github.com/WordPress/gutenberg/pull/46704#issuecomment-1794661382

Closing this change. Use the [new Tabs component](https://github.com/WordPress/gutenberg/pull/53960) instead.

## Review threads

### Review thread 1 (resolved)

#### mirka on 2022-12-23T12:24:20Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1056307109
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -110,6 +111,12 @@ export function TabPanel( {
 		}
 	}, [ tabs, selectedTab?.name, initialTabName, handleTabSelection ] );
 
+	useEffect( () => {
+		if ( selectedTabName ) {
+			setSelected( selectedTabName );
```

I think the convention is that the `onSelect()` should only be triggered from a user interaction on a tab button, not whenever the provided value changes.

You might want to look into the [`useControlledValue` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-controlled-value.ts#L21) to make the controlled/uncontrolled handling easier. The code is a bit hard to follow at the moment because of the unclear naming between `selectedTabName` and `selectedTab.name`.

#### madhusudhand on 2023-01-03T07:02:59Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1060317575
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -110,6 +111,12 @@ export function TabPanel( {
 		}
 	}, [ tabs, selectedTab?.name, initialTabName, handleTabSelection ] );
 
+	useEffect( () => {
+		if ( selectedTabName ) {
+			setSelected( selectedTabName );
```

> I think the convention is that the onSelect() should only be triggered from a user interaction on a tab button, not whenever the provided value changes

I think this is taken care. Calling `setSelected` updates the `selectedTab` object, and doesn't call `handleTabSelection` so it avoids the `onSelect` call. Can you confirm if it is still an issue.

> The code is a bit hard to follow at the moment because of the unclear naming between selectedTabName and selectedTab.name

Agree. But to maintain the consistency with the other prop `initialTabName` I had to use `selectedTabName`
I skipped updating `selectedTab` variable as it represents tab object and `selectedTabName` is a string.
Please suggest if we should rename any of them or add some comments for better readability.

#### mirka on 2023-01-04T16:17:26Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1061654871
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -110,6 +111,12 @@ export function TabPanel( {
 		}
 	}, [ tabs, selectedTab?.name, initialTabName, handleTabSelection ] );
 
+	useEffect( () => {
+		if ( selectedTabName ) {
+			setSelected( selectedTabName );
```

> I think this is taken care. Calling `setSelected` updates the `selectedTab` object, and doesn't call `handleTabSelection` so it avoids the `onSelect` call. Can you confirm if it is still an issue.

Yes, it looks good now 👍  I don't remember what it was before, but maybe I misread!

> Agree. But to maintain the consistency with the other prop `initialTabName` I had to use `selectedTabName` I skipped updating `selectedTab` variable as it represents tab object and `selectedTabName` is a string. Please suggest if we should rename any of them or add some comments for better readability.

One way I've seen it being handled in our codebase is to immediately rename it at destructuring time and append `Prop`. So in this case that would be to destructure our prop as `selectedTabName: selectedTabNameProp`. (Search the codebase for `valueProp` for some examples of this pattern to distinguish a prop value from an internal state value.)

### Review thread 2 (resolved)

#### mirka on 2022-12-23T12:57:06Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1056326603
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -105,6 +105,18 @@ describe( 'TabPanel', () => {
 		expect( selectedTab ).toHaveTextContent( 'Beta' );
 	} );
 
+	it( 'should render with a tab selected by prop selectedTabName', () => {
+		render(
+			<TabPanel
+				selectedTabName="beta"
+				tabs={ TABS }
+				children={ () => undefined }
+			/>
```

I think it's worth improving the test coverage here a bit, because this isn't a sufficient representation of how controlled mode is used. I feel like we should at least have a `useState` and test a tab change, coming from both a user interaction and from a `rerender()`.

And now that we support both controlled/uncontrolled mode, I think the tests should acknowledge that. Ideally we want all the relevant functionality to be tested in both modes (abstracted into `it.each()` blocks?), but at the least the tests should be grouped into controlled/uncontrolled so that we can easily see what coverage is missing.

#### madhusudhand on 2023-01-03T08:44:55Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1060369618
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -105,6 +105,18 @@ describe( 'TabPanel', () => {
 		expect( selectedTab ).toHaveTextContent( 'Beta' );
 	} );
 
+	it( 'should render with a tab selected by prop selectedTabName', () => {
+		render(
+			<TabPanel
+				selectedTabName="beta"
+				tabs={ TABS }
+				children={ () => undefined }
+			/>
```

Thanks for the suggestions. I have included more tests to test the change of input values for `selectedTabName` and for `user` interactions. Also re-organized the tests for better clarity.

### Review thread 3 (resolved)

#### mirka on 2022-12-23T12:58:54Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1056327644
Path: packages/components/src/tab-panel/types.ts

```diff
@@ -55,6 +55,10 @@ export type TabPanelProps = {
 	 * If this prop is not set, the first tab will be selected by default.
 	 */
 	initialTabName?: string;
+	/**
+	 * The name of the tab to be selected.
+	 */
+	selectedTabName?: string;
```

Let's also make sure this is added to the component's readme file.

#### madhusudhand on 2023-01-03T07:32:04Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1060329641
Path: packages/components/src/tab-panel/types.ts

```diff
@@ -55,6 +55,10 @@ export type TabPanelProps = {
 	 * If this prop is not set, the first tab will be selected by default.
 	 */
 	initialTabName?: string;
+	/**
+	 * The name of the tab to be selected.
+	 */
+	selectedTabName?: string;
```

Updated 👍

### Review thread 4 (resolved)

#### madhusudhand on 2023-01-06T09:31:52Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1063274277
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -93,193 +93,298 @@ describe( 'TabPanel', () => {
 		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
 	} );
 
-	it( 'should render with a tab initially selected by prop initialTabIndex', () => {
-		render(
-			<TabPanel
-				initialTabName="beta"
-				tabs={ TABS }
-				children={ () => undefined }
-			/>
-		);
-		const selectedTab = screen.getByRole( 'tab', { selected: true } );
-		expect( selectedTab ).toHaveTextContent( 'Beta' );
-	} );
+	describe( 'prop: initialTabName', () => {
+		it( 'should render with a tab initially selected by prop initialTabName', () => {
+			render(
+				<TabPanel
+					initialTabName="beta"
+					tabs={ TABS }
+					children={ () => undefined }
+				/>
+			);
+			expect( getSelectedTab() ).toHaveTextContent( 'Beta' );
+		} );
 
-	it( 'should apply the `activeClass` to the selected tab', async () => {
-		const user = setupUser();
-		const activeClass = 'my-active-tab';
+		it( 'should not render a different tab when initialTabName is changed', () => {
+			const { rerender } = render(
+				<TabPanel
+					initialTabName="beta"
+					tabs={ TABS }
+					children={ () => undefined }
+				/>
+			);
 
-		render(
-			<TabPanel
-				activeClass={ activeClass }
-				tabs={ TABS }
-				children={ () => undefined }
-			/>
-		);
-		expect( getSelectedTab() ).toHaveClass( activeClass );
-		screen
-			.getAllByRole( 'tab', { selected: false } )
-			.forEach( ( unselectedTab ) => {
-				expect( unselectedTab ).not.toHaveClass( activeClass );
-			} );
+			rerender(
+				<TabPanel
+					initialTabName="alpha"
+					tabs={ TABS }
+					children={ () => undefined }
+				/>
+			);
+			expect( getSelectedTab() ).toHaveTextContent( 'Beta' );
+		} );
 
-		await user.click( screen.getByRole( 'tab', { name: 'Beta' } ) );
+		it( 'should fallback to initial tab if active tab is removed', async () => {
```

with the current change, this test case fails as it fallbacks to `first active tab` irrespective of `initialTabName` is set or not, when the active tab is removed. I think it should be the right behaviour.

#### ciampo on 2023-01-12T17:21:11Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1068395307
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -93,193 +93,298 @@ describe( 'TabPanel', () => {
 		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
 	} );
 
-	it( 'should render with a tab initially selected by prop initialTabIndex', () => {
-		render(
-			<TabPanel
-				initialTabName="beta"
-				tabs={ TABS }
-				children={ () => undefined }
-			/>
-		);
-		const selectedTab = screen.getByRole( 'tab', { selected: true } );
-		expect( selectedTab ).toHaveTextContent( 'Beta' );
-	} );
+	describe( 'prop: initialTabName', () => {
+		it( 'should render with a tab initially selected by prop initialTabName', () => {
+			render(
+				<TabPanel
+					initialTabName="beta"
+					tabs={ TABS }
+					children={ () => undefined }
+				/>
+			);
+			expect( getSelectedTab() ).toHaveTextContent( 'Beta' );
+		} );
 
-	it( 'should apply the `activeClass` to the selected tab', async () => {
-		const user = setupUser();
-		const activeClass = 'my-active-tab';
+		it( 'should not render a different tab when initialTabName is changed', () => {
+			const { rerender } = render(
+				<TabPanel
+					initialTabName="beta"
+					tabs={ TABS }
+					children={ () => undefined }
+				/>
+			);
 
-		render(
-			<TabPanel
-				activeClass={ activeClass }
-				tabs={ TABS }
-				children={ () => undefined }
-			/>
-		);
-		expect( getSelectedTab() ).toHaveClass( activeClass );
-		screen
-			.getAllByRole( 'tab', { selected: false } )
-			.forEach( ( unselectedTab ) => {
-				expect( unselectedTab ).not.toHaveClass( activeClass );
-			} );
+			rerender(
+				<TabPanel
+					initialTabName="alpha"
+					tabs={ TABS }
+					children={ () => undefined }
+				/>
+			);
+			expect( getSelectedTab() ).toHaveTextContent( 'Beta' );
+		} );
 
-		await user.click( screen.getByRole( 'tab', { name: 'Beta' } ) );
+		it( 'should fallback to initial tab if active tab is removed', async () => {
```

Let's see if there's any change here after rebasing and including changes from  https://github.com/WordPress/gutenberg/pull/47100

### Review thread 5 (resolved)

#### madhusudhand on 2023-01-06T11:32:51Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1063366222
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -100,28 +102,12 @@ export function TabPanel( {
 	) => {
 		child.click();
 	};
-	const selectedTab = tabs.find( ( { name } ) => name === selected );
+	const selectedTab = getTab( tabName ) || firstEnabledTab;
 	const selectedId = `${ instanceId }-${ selectedTab?.name ?? 'none' }`;
 
-	useEffect( () => {
-		const firstEnabledTab = tabs.find( ( tab ) => ! tab.disabled );
-		const initialTab = tabs.find( ( tab ) => tab.name === initialTabName );
-		if ( ! selectedTab?.name && firstEnabledTab ) {
-			handleTabSelection(
-				initialTab && ! initialTab.disabled
-					? initialTab.name
-					: firstEnabledTab.name
-			);
-		} else if ( selectedTab?.disabled && firstEnabledTab ) {
-			handleTabSelection( firstEnabledTab.name );
-		}
-	}, [
-		tabs,
-		selectedTab?.name,
-		selectedTab?.disabled,
-		initialTabName,
-		handleTabSelection,
-	] );
+	// useEffect( () => {
+	// 	setTabName( selectedTab?.name );
+	// }, [ selectedTab?.name ] );
```

Without this hook, `onSelect` is not called on non user interactions such as changes to prop `tabName`, or removal of selected tab from `tabs`. Having this hook creates a side effect of calling the `onSelect` method twice for user actions such as click.

Please suggest ideas to handle this.

### Review thread 6

#### ciampo on 2023-02-02T14:23:40Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1094597762
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -9,7 +9,12 @@ import userEvent from '@testing-library/user-event';
  */
 import TabPanel from '..';
 
-const setupUser = () => userEvent.setup();
+jest.useFakeTimers();
```

We've been recently working across the codebase to refactor unit tests _away_ from using take timers and `act()` call.

That would mean removing the `jest.useFakeTimers();` call and the `advanceTimers: jest.advanceTimersByTime,` option when setting `userEvent` up.

cc @tyxla

#### tyxla on 2023-02-03T11:18:30Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1095691032
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -9,7 +9,12 @@ import userEvent from '@testing-library/user-event';
  */
 import TabPanel from '..';
 
-const setupUser = () => userEvent.setup();
+jest.useFakeTimers();
```

Yes, we use fake timers only when we really have to. That means removing `jest.useFakeTimers()` and calling `const user = userEvent.setup();` without the `advanceTimers` setting.

Here are some PRs where we refactored from fake to real timers if that will help:

* https://github.com/WordPress/gutenberg/pull/47318
* https://github.com/WordPress/gutenberg/pull/47218
* https://github.com/WordPress/gutenberg/pull/47144
* https://github.com/WordPress/gutenberg/pull/47056
* https://github.com/WordPress/gutenberg/pull/46714

### Review thread 7

#### ciampo on 2023-02-02T14:28:55Z

URL: https://github.com/WordPress/gutenberg/pull/46704#discussion_r1094604391
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -49,289 +54,404 @@ describe( 'TabPanel', () => {
 		window.HTMLElement.prototype.getClientRects = originalGetClientRects;
 	} );
 
-	it( 'should render a tabpanel, and clicking should change tabs', async () => {
-		const user = setupUser();
-		const panelRenderFunction = jest.fn();
-		const mockOnSelect = jest.fn();
-
-		render(
-			<TabPanel
-				tabs={ TABS }
-				children={ panelRenderFunction }
-				onSelect={ mockOnSelect }
-			/>
-		);
-
-		expect( getSelectedTab() ).toHaveTextContent( 'Alpha' );
-		expect(
-			screen.getByRole( 'tabpanel', { name: 'Alpha' } )
-		).toBeInTheDocument();
-		expect( panelRenderFunction ).toHaveBeenLastCalledWith( TABS[ 0 ] );
-		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
-
-		await user.click( screen.getByRole( 'tab', { name: 'Beta' } ) );
-
-		expect( getSelectedTab() ).toHaveTextContent( 'Beta' );
-		expect(
-			screen.getByRole( 'tabpanel', { name: 'Beta' } )
-		).toBeInTheDocument();
-		expect( panelRenderFunction ).toHaveBeenLastCalledWith( TABS[ 1 ] );
-		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
-
-		await user.click( screen.getByRole( 'tab', { name: 'Alpha' } ) );
-
-		expect( getSelectedTab() ).toHaveTextContent( 'Alpha' );
-		expect(
-			screen.getByRole( 'tabpanel', { name: 'Alpha' } )
-		).toBeInTheDocument();
-		expect( panelRenderFunction ).toHaveBeenLastCalledWith( TABS[ 0 ] );
-		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
-	} );
+	describe( 'Uncontrolled mode', () => {
```

A pattern that we've started to adopt when writing unit tests for components that can be used both in controlled and uncontrolled mode, is to use `describe.each` and run the same suite against the controlled and uncontrolled version.

You can see it in action for the `ComboboxControl` component:

https://github.com/WordPress/gutenberg/blob/56c33cf1f529d286b9c0db49534a2f5617fdcc84/packages/components/src/combobox-control/test/index.js#L80-L83


Of course, if there are certain tests that are specifically aimed at one version of the component, they can be added outside of the `describe.each` call.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +2/-0)
- packages/components/src/tab-panel/README.md (MODIFIED, +8/-0)
- packages/components/src/tab-panel/index.tsx (MODIFIED, +15/-6)
- packages/components/src/tab-panel/stories/index.story.tsx (MODIFIED, +20/-0)
- packages/components/src/tab-panel/test/index.tsx (MODIFIED, +120/-6)
- packages/components/src/tab-panel/types.ts (MODIFIED, +4/-0)
