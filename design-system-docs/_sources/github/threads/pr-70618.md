---
number: 70618
type: pull_request
state: MERGED
merged_at: 2026-06-19T09:48:55Z
url: https://github.com/WordPress/gutenberg/pull/70618
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Compose","[Package] Components"]
---

# PR #70618: Migrate compose package to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/70618
- Author: im3dabasia
- Created: 2025-07-04T10:50:43Z
- Updated: 2026-06-19T10:15:16Z
- Comments: 10 of 10
- Review threads: 25 of 25
- Changed files: 19 of 19
- Diff: ../pr-diffs/70618.diff

## Body

## What?
Part of: https://github.com/WordPress/gutenberg/issues/67691
Migrating the hooks package to Typescript.

## Why?
Type safety.

## Issue comments

### manzoorwanijk on 2026-01-11T09:30:03Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-3734299504

Is this still relevant? May be we want to update the PR?

### github-actions on 2026-04-30T05:30:19Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4349929676

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: im3dabasia <im3dabasia1@git.wordpress.org>
Co-authored-by: manzoorwanijk <manzoorwanijk@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### im3dabasia on 2026-05-06T08:42:19Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4386385728

I think it would be appropriate to skip adding a log in `packages/components/CHANGELOG.md`,  as the changes were made to update the compose package. Let me know if it's required, I'd be happy to add an entry.


---

The static lint test is failing, and I see a number of warnings (no errors) in the CI test logs. How should we proceed?

### manzoorwanijk on 2026-05-06T08:57:57Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4386479482

> The static lint test is failing, and I see a number of warnings (no errors) in the CI test logs. How should we proceed?

The error is burried deep under the warnings

```
/home/runner/work/gutenberg/gutenberg/packages/compose/src/hooks/use-dragging/index.ts: line 57, col 44, Error - Error: Cannot access variable before it is declared

`endDrag` is accessed before it is declared, which prevents the earlier access from updating when this value changes over time.

/home/runner/work/gutenberg/gutenberg/packages/compose/src/hooks/use-dragging/index.ts:57:44
  55 | 		eventsRef.current.onDragEnd?.( event );
  56 | 		document.removeEventListener( 'mousemove', onMouseMove );
> 57 | 		document.removeEventListener( 'mouseup', endDrag );
     | 		                                         ^^^^^^^ `endDrag` accessed before it is declared
  58 | 		setIsDragging( false );
  59 | 	}, [] );
  60 |
```

### manzoorwanijk on 2026-06-16T10:20:34Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4717535733

Can we refresh this please?

### im3dabasia on 2026-06-17T12:08:41Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4729840411

@manzoorwanijk, thank you for the continuous iterations on this PR. I really appreciate it. Please let me know if we are close to shipping this, and also if you think there’s anything we can improve in this PR.

### manzoorwanijk on 2026-06-17T12:14:47Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4729913760

Let us wait for a final Copilot review.

### manzoorwanijk on 2026-06-17T12:17:06Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4729938134

> Copilot was unable to review this pull request because the user who requested the review has reached their quota limit.

😄

### manzoorwanijk on 2026-06-18T13:29:52Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4742427973

E2E tests are failing repeatedly. I have re-run them one more time after updating the branch from trunk. If they pass, then we can merge this; if not, we need to look into it.

### im3dabasia on 2026-06-19T10:15:16Z

URL: https://github.com/WordPress/gutenberg/pull/70618#issuecomment-4750627250

Thank you @manzoorwanijk

## Review threads

### Review thread 1 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:47Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520277
Path: packages/compose/README.md

```diff
@@ -252,12 +252,12 @@ _Related_
 _Parameters_
 
 -   _fn_ `TFunc`: The function to debounce.
--   _wait_ `[number]`: The number of milliseconds to delay.
--   _options_ `[import('../../utils/debounce').DebounceOptions]`: The options object.
+-   _wait_ `number`: The number of milliseconds to delay.
+-   _options_ `DebounceOptions`: The options object.
```

The README now documents `wait` and `options` as required, but the hook signature makes them optional (both have defaults). Please update the docs to reflect optional parameters (e.g., mark them as optional and/or mention default behavior when omitted).
```suggestion
-   _[wait]_ `number`: Optional. The number of milliseconds to delay. If omitted, the hook uses its default delay.
-   _[options]_ `DebounceOptions`: Optional. The options object. If omitted, the hook uses its default options.
```

### Review thread 2 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:48Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520331
Path: packages/compose/README.md

```diff
@@ -370,27 +370,27 @@ Adds the unmount behavior of returning focus to the element which had it previou
 
 _Usage_
 
-```js
+```ts
 import { useFocusReturn } from '@wordpress/compose';
 
 const WithFocusReturn = () => {
-	const ref = useFocusReturn();
-	return (
-		<div ref={ ref }>
-			<Button />
-			<Button />
-		</div>
-	);
-};
+    const ref = useFocusReturn()
+    return (
+        <div ref={ ref }>
+            <Button />
+            <Button />
+        </div>
+    );
+}
 ```
 
 _Parameters_
 
--   _onFocusReturn_ `[() => void]`: Overrides the default return behavior.
+-   _onFocusReturn_ `() => void`: Overrides the default return behavior.
```

`onFocusReturn` is optional in the hook API, but the docs currently list it as a required parameter. Please mark it as optional (and clarify the default focus-restore behavior when it’s omitted).
```suggestion
-   _onFocusReturn_ `[() => void]`: Optional. Overrides the default return behavior. When omitted, focus is restored on unmount to the element that previously had focus.
```

#### im3dabasia on 2026-05-06T04:47:04Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3193058767
Path: packages/compose/README.md

```diff
@@ -370,27 +370,27 @@ Adds the unmount behavior of returning focus to the element which had it previou
 
 _Usage_
 
-```js
+```ts
 import { useFocusReturn } from '@wordpress/compose';
 
 const WithFocusReturn = () => {
-	const ref = useFocusReturn();
-	return (
-		<div ref={ ref }>
-			<Button />
-			<Button />
-		</div>
-	);
-};
+    const ref = useFocusReturn()
+    return (
+        <div ref={ ref }>
+            <Button />
+            <Button />
+        </div>
+    );
+}
 ```
 
 _Parameters_
 
--   _onFocusReturn_ `[() => void]`: Overrides the default return behavior.
+-   _onFocusReturn_ `() => void`: Overrides the default return behavior.
```

For optional parameters like `_onFocusReturn_`, it is denoted with square brackets ([]) in the name of the parameter, not in the type. Fixed this.

### Review thread 3 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:48Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520350
Path: packages/compose/README.md

```diff
@@ -420,9 +420,13 @@ _Related_
 
 _Parameters_
 
--   _shortcuts_ `string[]|string`: Keyboard Shortcuts.
--   _callback_ `(e: Mousetrap.ExtendedKeyboardEvent, combo: string) => void`: Shortcut callback.
--   _options_ `WPKeyboardShortcutConfig`: Shortcut options.
+-   _shortcuts_ `string[] | string`: Keyboard Shortcuts.
+-   _callback_ `( e: ExtendedKeyboardEvent, combo: string ) => void`: Shortcut callback.
+-   _options_ `Partial< WPKeyboardShortcutConfig >`: Shortcut options.
+-   _options.bindGlobal_ `Partial< WPKeyboardShortcutConfig >[ 'bindGlobal' ]`:
+-   _options.eventName_ `Partial< WPKeyboardShortcutConfig >[ 'eventName' ]`:
+-   _options.isDisabled_ `Partial< WPKeyboardShortcutConfig >[ 'isDisabled' ]`:
+-   _options.target_ `Partial< WPKeyboardShortcutConfig >[ 'target' ]`:
```

The `useKeyboardShortcut` option sub-parameters (`bindGlobal`, `eventName`, `isDisabled`, `target`) are listed but have no descriptions after the colon, which makes the generated docs incomplete. Please add short descriptions (or remove the sub-parameter bullets if the generator can’t populate them).
```suggestion
-   _options.bindGlobal_ `Partial< WPKeyboardShortcutConfig >[ 'bindGlobal' ]`: Whether to bind the shortcut globally.
-   _options.eventName_ `Partial< WPKeyboardShortcutConfig >[ 'eventName' ]`: The keyboard event name to listen for.
-   _options.isDisabled_ `Partial< WPKeyboardShortcutConfig >[ 'isDisabled' ]`: Whether the shortcut should be disabled.
-   _options.target_ `Partial< WPKeyboardShortcutConfig >[ 'target' ]`: The event target to bind the shortcut to.
```

#### im3dabasia on 2026-05-06T04:49:34Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3193067130
Path: packages/compose/README.md

```diff
@@ -420,9 +420,13 @@ _Related_
 
 _Parameters_
 
--   _shortcuts_ `string[]|string`: Keyboard Shortcuts.
--   _callback_ `(e: Mousetrap.ExtendedKeyboardEvent, combo: string) => void`: Shortcut callback.
--   _options_ `WPKeyboardShortcutConfig`: Shortcut options.
+-   _shortcuts_ `string[] | string`: Keyboard Shortcuts.
+-   _callback_ `( e: ExtendedKeyboardEvent, combo: string ) => void`: Shortcut callback.
+-   _options_ `Partial< WPKeyboardShortcutConfig >`: Shortcut options.
+-   _options.bindGlobal_ `Partial< WPKeyboardShortcutConfig >[ 'bindGlobal' ]`:
+-   _options.eventName_ `Partial< WPKeyboardShortcutConfig >[ 'eventName' ]`:
+-   _options.isDisabled_ `Partial< WPKeyboardShortcutConfig >[ 'isDisabled' ]`:
+-   _options.target_ `Partial< WPKeyboardShortcutConfig >[ 'target' ]`:
```

Added the comments as per suggestion

### Review thread 4 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:48Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520370
Path: packages/compose/README.md

```diff
@@ -571,29 +575,29 @@ _Related_
 _Parameters_
 
 -   _fn_ `TFunc`: The function to throttle.
--   _wait_ `[number]`: The number of milliseconds to throttle invocations to.
--   _options_ `[import('../../utils/throttle').ThrottleOptions]`: The options object. See linked documentation for details.
+-   _wait_ `number`: The number of milliseconds to throttle invocations to.
+-   _options_ `ThrottleOptions`: The options object.
```

The README now documents `wait` and `options` as required, but `useThrottle` accepts both as optional (with default behavior). Please update the docs to reflect the optional nature of these parameters.
```suggestion
-   [_wait_] `number`: Optional. The number of milliseconds to throttle invocations to. Defaults to the hook's built-in throttle behavior when omitted.
-   [_options_] `ThrottleOptions`: Optional. The options object. Uses the default options when omitted.
```

### Review thread 5 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:49Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520385
Path: packages/compose/README.md

```diff
@@ -571,29 +575,29 @@ _Related_
 _Parameters_
 
 -   _fn_ `TFunc`: The function to throttle.
--   _wait_ `[number]`: The number of milliseconds to throttle invocations to.
--   _options_ `[import('../../utils/throttle').ThrottleOptions]`: The options object. See linked documentation for details.
+-   _wait_ `number`: The number of milliseconds to throttle invocations to.
+-   _options_ `ThrottleOptions`: The options object.
 
 _Returns_
 
--   `import('../../utils/debounce').DebouncedFunc<TFunc>`: Throttled function.
+-   `DebouncedFunc< TFunc >`: Throttled function.
 
 ### useViewportMatch
 
 Returns true if the viewport matches the given query, or false otherwise.
 
 _Usage_
 
-```js
+```ts
 useViewportMatch( 'huge', '<' );
 useViewportMatch( 'medium' );
 ```
 
 _Parameters_
 
 -   _breakpoint_ `WPBreakpoint`: Breakpoint size name.
--   _operator_ `[WPViewportOperator]`: Viewport operator.
--   _view_ `[Window]`: Window instance in which to perform viewport matching.
+-   _operator_ `WPViewportOperator`: Viewport operator.
+-   _view_ `Window`: Window instance in which to perform viewport matching.
```

`operator` and `view` are optional in `useViewportMatch` (both have defaults), but the README currently lists them as required. Please mark them as optional and/or document the defaults.
```suggestion
-   _operator_ `WPViewportOperator` _(optional, default: `'>='`)_: Viewport operator.
-   _view_ `Window` _(optional, default: `window`)_: Window instance in which to perform viewport matching.
```

### Review thread 6 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:49Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520419
Path: packages/compose/src/hooks/use-warn-on-change/index.ts

```diff
@@ -0,0 +1,42 @@
+/**
+ * Internal dependencies
+ */
+import usePrevious from '../use-previous';
+
+/**
+ * Hook that performs a shallow comparison between the preview value of an object
+ * and the new one, if there's a difference, it prints it to the console.
+ * this is useful in performance related work, to check why a component re-renders.
```

The phrase "preview value" looks like a typo in this context (the hook compares against the *previous* value). Consider changing it to "previous value" (and also capitalizing the next sentence start: "This is useful...").
```suggestion
 * Hook that performs a shallow comparison between the previous value of an object
 * and the new one, if there's a difference, it prints it to the console.
 * This is useful in performance related work, to check why a component re-renders.
```

### Review thread 7 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:49Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520443
Path: packages/compose/README.md

```diff
@@ -605,8 +609,8 @@ Hook that performs a shallow comparison between the preview value of an object a
```

Same typo as the hook JSDoc: "preview value" should be "previous value" (and the following sentence should start with a capital "This").

### Review thread 8 (resolved)

#### copilot-pull-request-reviewer on 2026-04-30T08:13:49Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3166520465
Path: packages/compose/src/hooks/use-drop-zone/index.ts

```diff
@@ -0,0 +1,184 @@
+/**
+ * Internal dependencies
+ */
+import useRefEffect from '../use-ref-effect';
+import useEvent from '../use-event';
+
+interface UseDropZoneProps {
+	dropZoneElement?: HTMLElement | null;
+	isDisabled?: boolean;
+	onDragStart?: ( e: DragEvent ) => void;
+	onDragEnter?: ( e: DragEvent ) => void;
+	onDragOver?: ( e: DragEvent ) => void;
+	onDragLeave?: ( e: DragEvent ) => void;
+	onDragEnd?: ( e: MouseEvent ) => void;
+	onDrop?: ( e: DragEvent ) => void;
+}
+
+/**
+ * A hook to facilitate drag and drop handling.
+ *
+ * @param props                 Hook options
+ * @param props.dropZoneElement
+ * @param props.isDisabled
+ * @param props.onDrop
+ * @param props.onDragStart
+ * @param props.onDragEnter
+ * @param props.onDragLeave
+ * @param props.onDragEnd
+ * @param props.onDragOver
+ * @return Ref callback to be passed to the drop zone element.
+ */
+export default function useDropZone( {
+	dropZoneElement,
+	isDisabled,
+	onDrop: _onDrop,
+	onDragStart: _onDragStart,
+	onDragEnter: _onDragEnter,
+	onDragLeave: _onDragLeave,
+	onDragEnd: _onDragEnd,
+	onDragOver: _onDragOver,
+}: UseDropZoneProps ): React.RefCallback< HTMLElement > {
+	const onDropEvent = useEvent( _onDrop );
+	const onDragStartEvent = useEvent( _onDragStart );
+	const onDragEnterEvent = useEvent( _onDragEnter );
+	const onDragLeaveEvent = useEvent( _onDragLeave );
+	const onDragEndEvent = useEvent( _onDragEnd );
+	const onDragOverEvent = useEvent( _onDragOver );
+
+	return useRefEffect(
+		( elem: HTMLElement ) => {
+			if ( isDisabled ) {
+				return;
+			}
+
+			const element = dropZoneElement ?? elem;
+			let isDragging = false;
+			const { ownerDocument } = element;
+
+			function isElementInZone(
+				targetToCheck: EventTarget | null
+			): boolean {
+				const { defaultView } = ownerDocument;
+
+				if (
+					! targetToCheck ||
+					! defaultView ||
+					! ( targetToCheck instanceof defaultView.HTMLElement ) ||
+					! element.contains( targetToCheck )
+				) {
+					return false;
+				}
+
+				let elementToCheck: HTMLElement | null = targetToCheck;
+
+				do {
+					if ( elementToCheck.dataset.isDropZone ) {
+						return elementToCheck === element;
+					}
+				} while ( ( elementToCheck = elementToCheck.parentElement ) );
+
+				return false;
+			}
+
+			function maybeDragStart( event: DragEvent ): void {
+				if ( isDragging ) {
+					return;
+				}
+
+				isDragging = true;
+
+				ownerDocument.addEventListener( 'dragend', maybeDragEnd );
+				ownerDocument.addEventListener( 'mousemove', maybeDragEnd );
+
+				if ( _onDragStart ) {
+					onDragStartEvent( event );
+				}
```

The guard `if ( _onDragStart )` is captured by the effect closure, but `_onDragStart` is not in the dependency list. If `onDragStart` is initially undefined and later becomes defined, the handler will never be invoked (even though `useEvent` would correctly point to the latest callback). Consider calling `onDragStartEvent( event )` unconditionally (it already no-ops when undefined), or include the callback presence in the dependencies / a ref check so updates are respected.
```suggestion
				onDragStartEvent( event );
```

### Review thread 9 (resolved)

#### manzoorwanijk on 2026-05-03T11:58:20Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3178073751
Path: packages/compose/src/hooks/use-keyboard-shortcut/index.ts

```diff
@@ -0,0 +1,111 @@
+/**
+ * External dependencies
+ */
+import Mousetrap from 'mousetrap';
+import 'mousetrap/plugins/global-bind/mousetrap-global-bind.js';
+import type { ExtendedKeyboardEvent } from 'mousetrap';
+
+/**
+ * WordPress dependencies
+ */
+import type { RefObject } from '@wordpress/element';
+import { useEffect, useRef } from '@wordpress/element';
+import { isAppleOS } from '@wordpress/keycodes';
+
+type WPKeyboardShortcutConfig = {
+	/**
+	 * Handle keyboard events anywhere including inside textarea/input fields.
+	 */
+	bindGlobal: boolean;
+	/**
+	 * Event name used to trigger the handler, defaults to keydown.
+	 */
+	eventName: string;
+	/**
+	 * Disables the keyboard handler if the value is true.
+	 */
+	isDisabled: boolean;
+	/**
+	 * React reference to the DOM element used to catch the keyboard event.
+	 */
+	target: RefObject< HTMLElement >;
+};
+
+/**
+ * Attach a keyboard shortcut handler.
+ *
+ * @see https://craig.is/killing/mice#api.bind for information about the `callback` parameter.
+ *
+ * @param shortcuts          Keyboard Shortcuts.
+ * @param callback           Shortcut callback.
+ * @param options            Shortcut options.
+ * @param options.bindGlobal
+ * @param options.eventName
+ * @param options.isDisabled
+ * @param options.target
+ */
+function useKeyboardShortcut(
+	shortcuts: string[] | string,
+	callback: ( e: ExtendedKeyboardEvent, combo: string ) => void,
+	{
+		bindGlobal = false,
+		eventName = 'keydown',
+		isDisabled = false,
+		target,
+	}: Partial< WPKeyboardShortcutConfig > = {}
+) {
+	const currentCallbackRef = useRef( callback );
+
+	useEffect( () => {
+		currentCallbackRef.current = callback;
+	}, [ callback ] );
+
+	useEffect( () => {
+		if ( isDisabled ) {
+			return;
+		}
+
+		const mousetrap = new Mousetrap(
+			target && target.current
+				? target.current
+				: ( document as unknown as Element )
```

I would rather add an expect-error comment explaining the reason


```suggestion
			// @ts-expect-error We have been using document here for a long time, but the constructor expects Element.
			target && target.current ? target.current : document
```

#### im3dabasia on 2026-05-06T05:09:46Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3193143748
Path: packages/compose/src/hooks/use-keyboard-shortcut/index.ts

```diff
@@ -0,0 +1,111 @@
+/**
+ * External dependencies
+ */
+import Mousetrap from 'mousetrap';
+import 'mousetrap/plugins/global-bind/mousetrap-global-bind.js';
+import type { ExtendedKeyboardEvent } from 'mousetrap';
+
+/**
+ * WordPress dependencies
+ */
+import type { RefObject } from '@wordpress/element';
+import { useEffect, useRef } from '@wordpress/element';
+import { isAppleOS } from '@wordpress/keycodes';
+
+type WPKeyboardShortcutConfig = {
+	/**
+	 * Handle keyboard events anywhere including inside textarea/input fields.
+	 */
+	bindGlobal: boolean;
+	/**
+	 * Event name used to trigger the handler, defaults to keydown.
+	 */
+	eventName: string;
+	/**
+	 * Disables the keyboard handler if the value is true.
+	 */
+	isDisabled: boolean;
+	/**
+	 * React reference to the DOM element used to catch the keyboard event.
+	 */
+	target: RefObject< HTMLElement >;
+};
+
+/**
+ * Attach a keyboard shortcut handler.
+ *
+ * @see https://craig.is/killing/mice#api.bind for information about the `callback` parameter.
+ *
+ * @param shortcuts          Keyboard Shortcuts.
+ * @param callback           Shortcut callback.
+ * @param options            Shortcut options.
+ * @param options.bindGlobal
+ * @param options.eventName
+ * @param options.isDisabled
+ * @param options.target
+ */
+function useKeyboardShortcut(
+	shortcuts: string[] | string,
+	callback: ( e: ExtendedKeyboardEvent, combo: string ) => void,
+	{
+		bindGlobal = false,
+		eventName = 'keydown',
+		isDisabled = false,
+		target,
+	}: Partial< WPKeyboardShortcutConfig > = {}
+) {
+	const currentCallbackRef = useRef( callback );
+
+	useEffect( () => {
+		currentCallbackRef.current = callback;
+	}, [ callback ] );
+
+	useEffect( () => {
+		if ( isDisabled ) {
+			return;
+		}
+
+		const mousetrap = new Mousetrap(
+			target && target.current
+				? target.current
+				: ( document as unknown as Element )
```

I noticed I had left out the older comments for this section. I’ve added them back to preserve originality and provide more information.

### Review thread 10 (resolved)

#### manzoorwanijk on 2026-05-03T11:59:39Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3178076086
Path: packages/compose/src/hooks/use-keyboard-shortcut/index.ts

```diff
@@ -0,0 +1,111 @@
+/**
+ * External dependencies
+ */
+import Mousetrap from 'mousetrap';
+import 'mousetrap/plugins/global-bind/mousetrap-global-bind.js';
+import type { ExtendedKeyboardEvent } from 'mousetrap';
+
+/**
+ * WordPress dependencies
+ */
+import type { RefObject } from '@wordpress/element';
+import { useEffect, useRef } from '@wordpress/element';
+import { isAppleOS } from '@wordpress/keycodes';
+
+type WPKeyboardShortcutConfig = {
+	/**
+	 * Handle keyboard events anywhere including inside textarea/input fields.
+	 */
+	bindGlobal: boolean;
+	/**
+	 * Event name used to trigger the handler, defaults to keydown.
+	 */
+	eventName: string;
+	/**
+	 * Disables the keyboard handler if the value is true.
+	 */
+	isDisabled: boolean;
+	/**
+	 * React reference to the DOM element used to catch the keyboard event.
+	 */
+	target: RefObject< HTMLElement >;
+};
+
+/**
+ * Attach a keyboard shortcut handler.
+ *
+ * @see https://craig.is/killing/mice#api.bind for information about the `callback` parameter.
+ *
+ * @param shortcuts          Keyboard Shortcuts.
+ * @param callback           Shortcut callback.
+ * @param options            Shortcut options.
+ * @param options.bindGlobal
+ * @param options.eventName
+ * @param options.isDisabled
+ * @param options.target
+ */
+function useKeyboardShortcut(
+	shortcuts: string[] | string,
+	callback: ( e: ExtendedKeyboardEvent, combo: string ) => void,
+	{
+		bindGlobal = false,
+		eventName = 'keydown',
+		isDisabled = false,
+		target,
+	}: Partial< WPKeyboardShortcutConfig > = {}
+) {
+	const currentCallbackRef = useRef( callback );
+
+	useEffect( () => {
+		currentCallbackRef.current = callback;
+	}, [ callback ] );
+
+	useEffect( () => {
+		if ( isDisabled ) {
+			return;
+		}
+
+		const mousetrap = new Mousetrap(
+			target && target.current
+				? target.current
+				: ( document as unknown as Element )
+		);
+
+		const shortcutsArray = Array.isArray( shortcuts )
+			? shortcuts
+			: [ shortcuts ];
+
+		shortcutsArray.forEach( ( shortcut: string ) => {
+			const keys = shortcut.split( '+' );
+			const modifiers = new Set(
+				keys.filter( ( value ) => value.length > 1 )
+			);
+			const hasAlt = modifiers.has( 'alt' );
+			const hasShift = modifiers.has( 'shift' );
+
+			if (
+				isAppleOS() &&
+				( ( modifiers.size === 1 && hasAlt ) ||
+					( modifiers.size === 2 && hasAlt && hasShift ) )
+			) {
+				throw new Error(
+					`Cannot bind ${ shortcut }. Alt and Shift+Alt modifiers are reserved for character input.`
+				);
+			}
+
+			const bindFn = bindGlobal ? 'bindGlobal' : 'bind';
+			( mousetrap as any )[ bindFn ](
+				shortcut,
+				( ...args: [ Mousetrap.ExtendedKeyboardEvent, string ] ) =>
+					currentCallbackRef.current( ...args ),
+				eventName
+			);
```

If we cast it as `'bind'`, we can get rid of the `any` type below


```suggestion
			const bindFn = ( bindGlobal ? 'bindGlobal' : 'bind' ) as 'bind';

			mousetrap[ bindFn ](
				shortcut,
				( ...args ) => currentCallbackRef.current( ...args ),
				eventName
			);
```

#### im3dabasia on 2026-05-06T05:15:53Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3193161503
Path: packages/compose/src/hooks/use-keyboard-shortcut/index.ts

```diff
@@ -0,0 +1,111 @@
+/**
+ * External dependencies
+ */
+import Mousetrap from 'mousetrap';
+import 'mousetrap/plugins/global-bind/mousetrap-global-bind.js';
+import type { ExtendedKeyboardEvent } from 'mousetrap';
+
+/**
+ * WordPress dependencies
+ */
+import type { RefObject } from '@wordpress/element';
+import { useEffect, useRef } from '@wordpress/element';
+import { isAppleOS } from '@wordpress/keycodes';
+
+type WPKeyboardShortcutConfig = {
+	/**
+	 * Handle keyboard events anywhere including inside textarea/input fields.
+	 */
+	bindGlobal: boolean;
+	/**
+	 * Event name used to trigger the handler, defaults to keydown.
+	 */
+	eventName: string;
+	/**
+	 * Disables the keyboard handler if the value is true.
+	 */
+	isDisabled: boolean;
+	/**
+	 * React reference to the DOM element used to catch the keyboard event.
+	 */
+	target: RefObject< HTMLElement >;
+};
+
+/**
+ * Attach a keyboard shortcut handler.
+ *
+ * @see https://craig.is/killing/mice#api.bind for information about the `callback` parameter.
+ *
+ * @param shortcuts          Keyboard Shortcuts.
+ * @param callback           Shortcut callback.
+ * @param options            Shortcut options.
+ * @param options.bindGlobal
+ * @param options.eventName
+ * @param options.isDisabled
+ * @param options.target
+ */
+function useKeyboardShortcut(
+	shortcuts: string[] | string,
+	callback: ( e: ExtendedKeyboardEvent, combo: string ) => void,
+	{
+		bindGlobal = false,
+		eventName = 'keydown',
+		isDisabled = false,
+		target,
+	}: Partial< WPKeyboardShortcutConfig > = {}
+) {
+	const currentCallbackRef = useRef( callback );
+
+	useEffect( () => {
+		currentCallbackRef.current = callback;
+	}, [ callback ] );
+
+	useEffect( () => {
+		if ( isDisabled ) {
+			return;
+		}
+
+		const mousetrap = new Mousetrap(
+			target && target.current
+				? target.current
+				: ( document as unknown as Element )
+		);
+
+		const shortcutsArray = Array.isArray( shortcuts )
+			? shortcuts
+			: [ shortcuts ];
+
+		shortcutsArray.forEach( ( shortcut: string ) => {
+			const keys = shortcut.split( '+' );
+			const modifiers = new Set(
+				keys.filter( ( value ) => value.length > 1 )
+			);
+			const hasAlt = modifiers.has( 'alt' );
+			const hasShift = modifiers.has( 'shift' );
+
+			if (
+				isAppleOS() &&
+				( ( modifiers.size === 1 && hasAlt ) ||
+					( modifiers.size === 2 && hasAlt && hasShift ) )
+			) {
+				throw new Error(
+					`Cannot bind ${ shortcut }. Alt and Shift+Alt modifiers are reserved for character input.`
+				);
+			}
+
+			const bindFn = bindGlobal ? 'bindGlobal' : 'bind';
+			( mousetrap as any )[ bindFn ](
+				shortcut,
+				( ...args: [ Mousetrap.ExtendedKeyboardEvent, string ] ) =>
+					currentCallbackRef.current( ...args ),
+				eventName
+			);
```

Wouldn’t using as 'bind' just silence the error by essentially pretending `bindGlobal` doesn’t exist? It still shows up later at runtime, and even though it behaves similarly to bind, it feels more like a workaround than a proper solution.

#### manzoorwanijk on 2026-05-06T05:30:57Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3193220193
Path: packages/compose/src/hooks/use-keyboard-shortcut/index.ts

```diff
@@ -0,0 +1,111 @@
+/**
+ * External dependencies
+ */
+import Mousetrap from 'mousetrap';
+import 'mousetrap/plugins/global-bind/mousetrap-global-bind.js';
+import type { ExtendedKeyboardEvent } from 'mousetrap';
+
+/**
+ * WordPress dependencies
+ */
+import type { RefObject } from '@wordpress/element';
+import { useEffect, useRef } from '@wordpress/element';
+import { isAppleOS } from '@wordpress/keycodes';
+
+type WPKeyboardShortcutConfig = {
+	/**
+	 * Handle keyboard events anywhere including inside textarea/input fields.
+	 */
+	bindGlobal: boolean;
+	/**
+	 * Event name used to trigger the handler, defaults to keydown.
+	 */
+	eventName: string;
+	/**
+	 * Disables the keyboard handler if the value is true.
+	 */
+	isDisabled: boolean;
+	/**
+	 * React reference to the DOM element used to catch the keyboard event.
+	 */
+	target: RefObject< HTMLElement >;
+};
+
+/**
+ * Attach a keyboard shortcut handler.
+ *
+ * @see https://craig.is/killing/mice#api.bind for information about the `callback` parameter.
+ *
+ * @param shortcuts          Keyboard Shortcuts.
+ * @param callback           Shortcut callback.
+ * @param options            Shortcut options.
+ * @param options.bindGlobal
+ * @param options.eventName
+ * @param options.isDisabled
+ * @param options.target
+ */
+function useKeyboardShortcut(
+	shortcuts: string[] | string,
+	callback: ( e: ExtendedKeyboardEvent, combo: string ) => void,
+	{
+		bindGlobal = false,
+		eventName = 'keydown',
+		isDisabled = false,
+		target,
+	}: Partial< WPKeyboardShortcutConfig > = {}
+) {
+	const currentCallbackRef = useRef( callback );
+
+	useEffect( () => {
+		currentCallbackRef.current = callback;
+	}, [ callback ] );
+
+	useEffect( () => {
+		if ( isDisabled ) {
+			return;
+		}
+
+		const mousetrap = new Mousetrap(
+			target && target.current
+				? target.current
+				: ( document as unknown as Element )
+		);
+
+		const shortcutsArray = Array.isArray( shortcuts )
+			? shortcuts
+			: [ shortcuts ];
+
+		shortcutsArray.forEach( ( shortcut: string ) => {
+			const keys = shortcut.split( '+' );
+			const modifiers = new Set(
+				keys.filter( ( value ) => value.length > 1 )
+			);
+			const hasAlt = modifiers.has( 'alt' );
+			const hasShift = modifiers.has( 'shift' );
+
+			if (
+				isAppleOS() &&
+				( ( modifiers.size === 1 && hasAlt ) ||
+					( modifiers.size === 2 && hasAlt && hasShift ) )
+			) {
+				throw new Error(
+					`Cannot bind ${ shortcut }. Alt and Shift+Alt modifiers are reserved for character input.`
+				);
+			}
+
+			const bindFn = bindGlobal ? 'bindGlobal' : 'bind';
+			( mousetrap as any )[ bindFn ](
+				shortcut,
+				( ...args: [ Mousetrap.ExtendedKeyboardEvent, string ] ) =>
+					currentCallbackRef.current( ...args ),
+				eventName
+			);
```

`as any` does that anyway, then it's better to have a `// @ts-expect-error` comment there.

#### im3dabasia on 2026-05-06T05:32:18Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3193225171
Path: packages/compose/src/hooks/use-keyboard-shortcut/index.ts

```diff
@@ -0,0 +1,111 @@
+/**
+ * External dependencies
+ */
+import Mousetrap from 'mousetrap';
+import 'mousetrap/plugins/global-bind/mousetrap-global-bind.js';
+import type { ExtendedKeyboardEvent } from 'mousetrap';
+
+/**
+ * WordPress dependencies
+ */
+import type { RefObject } from '@wordpress/element';
+import { useEffect, useRef } from '@wordpress/element';
+import { isAppleOS } from '@wordpress/keycodes';
+
+type WPKeyboardShortcutConfig = {
+	/**
+	 * Handle keyboard events anywhere including inside textarea/input fields.
+	 */
+	bindGlobal: boolean;
+	/**
+	 * Event name used to trigger the handler, defaults to keydown.
+	 */
+	eventName: string;
+	/**
+	 * Disables the keyboard handler if the value is true.
+	 */
+	isDisabled: boolean;
+	/**
+	 * React reference to the DOM element used to catch the keyboard event.
+	 */
+	target: RefObject< HTMLElement >;
+};
+
+/**
+ * Attach a keyboard shortcut handler.
+ *
+ * @see https://craig.is/killing/mice#api.bind for information about the `callback` parameter.
+ *
+ * @param shortcuts          Keyboard Shortcuts.
+ * @param callback           Shortcut callback.
+ * @param options            Shortcut options.
+ * @param options.bindGlobal
+ * @param options.eventName
+ * @param options.isDisabled
+ * @param options.target
+ */
+function useKeyboardShortcut(
+	shortcuts: string[] | string,
+	callback: ( e: ExtendedKeyboardEvent, combo: string ) => void,
+	{
+		bindGlobal = false,
+		eventName = 'keydown',
+		isDisabled = false,
+		target,
+	}: Partial< WPKeyboardShortcutConfig > = {}
+) {
+	const currentCallbackRef = useRef( callback );
+
+	useEffect( () => {
+		currentCallbackRef.current = callback;
+	}, [ callback ] );
+
+	useEffect( () => {
+		if ( isDisabled ) {
+			return;
+		}
+
+		const mousetrap = new Mousetrap(
+			target && target.current
+				? target.current
+				: ( document as unknown as Element )
+		);
+
+		const shortcutsArray = Array.isArray( shortcuts )
+			? shortcuts
+			: [ shortcuts ];
+
+		shortcutsArray.forEach( ( shortcut: string ) => {
+			const keys = shortcut.split( '+' );
+			const modifiers = new Set(
+				keys.filter( ( value ) => value.length > 1 )
+			);
+			const hasAlt = modifiers.has( 'alt' );
+			const hasShift = modifiers.has( 'shift' );
+
+			if (
+				isAppleOS() &&
+				( ( modifiers.size === 1 && hasAlt ) ||
+					( modifiers.size === 2 && hasAlt && hasShift ) )
+			) {
+				throw new Error(
+					`Cannot bind ${ shortcut }. Alt and Shift+Alt modifiers are reserved for character input.`
+				);
+			}
+
+			const bindFn = bindGlobal ? 'bindGlobal' : 'bind';
+			( mousetrap as any )[ bindFn ](
+				shortcut,
+				( ...args: [ Mousetrap.ExtendedKeyboardEvent, string ] ) =>
+					currentCallbackRef.current( ...args ),
+				eventName
+			);
```

Okay!

### Review thread 11 (resolved)

#### manzoorwanijk on 2026-05-03T12:00:15Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3178076832
Path: packages/compose/src/hooks/use-viewport-match/index.ts

```diff
@@ -8,18 +8,23 @@ import { createContext, useContext } from '@wordpress/element';
  */
 import useMediaQuery from '../use-media-query';
 
-/**
- * @typedef {"xhuge" | "huge" | "wide" | "xlarge" | "large" | "medium" | "small" | "mobile"} WPBreakpoint
- */
+type WPBreakpoint =
+	| 'xhuge'
+	| 'huge'
+	| 'wide'
+	| 'xlarge'
+	| 'large'
+	| 'medium'
+	| 'small'
+	| 'mobile';
+type WPViewportOperator = '>=' | '<';
```

Let us not carry over the WP prefix. Let us drop it.

### Review thread 12 (resolved)

#### manzoorwanijk on 2026-05-03T12:01:38Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3178078420
Path: packages/compose/src/hooks/use-drop-zone/index.ts

```diff
@@ -0,0 +1,184 @@
+/**
+ * Internal dependencies
+ */
+import useRefEffect from '../use-ref-effect';
+import useEvent from '../use-event';
+
+interface UseDropZoneProps {
+	dropZoneElement?: HTMLElement | null;
+	isDisabled?: boolean;
+	onDragStart?: ( e: DragEvent ) => void;
+	onDragEnter?: ( e: DragEvent ) => void;
+	onDragOver?: ( e: DragEvent ) => void;
+	onDragLeave?: ( e: DragEvent ) => void;
+	onDragEnd?: ( e: MouseEvent ) => void;
+	onDrop?: ( e: DragEvent ) => void;
+}
```

We have lost the JSDoc comments for all these properties here. Can we please retain that the way we did for `useKetboardShortcut`?

### Review thread 13 (resolved)

#### manzoorwanijk on 2026-05-03T12:04:20Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3178081475
Path: packages/compose/src/hooks/use-drop-zone/index.ts

```diff
@@ -0,0 +1,184 @@
+/**
+ * Internal dependencies
+ */
+import useRefEffect from '../use-ref-effect';
+import useEvent from '../use-event';
+
+interface UseDropZoneProps {
+	dropZoneElement?: HTMLElement | null;
+	isDisabled?: boolean;
+	onDragStart?: ( e: DragEvent ) => void;
+	onDragEnter?: ( e: DragEvent ) => void;
+	onDragOver?: ( e: DragEvent ) => void;
+	onDragLeave?: ( e: DragEvent ) => void;
+	onDragEnd?: ( e: MouseEvent ) => void;
+	onDrop?: ( e: DragEvent ) => void;
+}
+
+/**
+ * A hook to facilitate drag and drop handling.
+ *
+ * @param props                 Hook options
+ * @param props.dropZoneElement
+ * @param props.isDisabled
+ * @param props.onDrop
+ * @param props.onDragStart
+ * @param props.onDragEnter
+ * @param props.onDragLeave
+ * @param props.onDragEnd
+ * @param props.onDragOver
```

We can use the type here to avoid repeating the props here


```suggestion
 * @param {UseDropZoneProps} props Hook options
```

### Review thread 14 (resolved)

#### copilot-pull-request-reviewer on 2026-05-12T14:07:22Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3227060377
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,88 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+
+interface DraggingProps {
+	onDragStart?: ( e: React.MouseEvent ) => void;
+	onDragMove?: ( e: MouseEvent ) => void;
+	onDragEnd?: ( e?: MouseEvent ) => void;
+}
+
+interface DraggingReturn {
+	startDrag: ( e: React.MouseEvent ) => void;
+	endDrag: ( e?: MouseEvent ) => void;
+	isDragging: boolean;
+}
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
```

`endDrag` does not remove the `mouseup` listener that `startDrag` adds (it relies on `{ once: true }`). If consumers call `endDrag()` manually (or the component unmounts after `endDrag` sets `isDragging` false), the pending `mouseup` listener can still fire later, causing a second `endDrag` call and potentially calling `setIsDragging` on an unmounted component. Please always remove the `mouseup` listener inside `endDrag` (as the previous implementation did).

#### manzoorwanijk on 2026-06-17T12:29:53Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428094562
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,88 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+
+interface DraggingProps {
+	onDragStart?: ( e: React.MouseEvent ) => void;
+	onDragMove?: ( e: MouseEvent ) => void;
+	onDragEnd?: ( e?: MouseEvent ) => void;
+}
+
+interface DraggingReturn {
+	startDrag: ( e: React.MouseEvent ) => void;
+	endDrag: ( e?: MouseEvent ) => void;
+	isDragging: boolean;
+}
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
```

This is a valid concern

#### im3dabasia on 2026-06-17T12:43:37Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428179773
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,88 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+
+interface DraggingProps {
+	onDragStart?: ( e: React.MouseEvent ) => void;
+	onDragMove?: ( e: MouseEvent ) => void;
+	onDragEnd?: ( e?: MouseEvent ) => void;
+}
+
+interface DraggingReturn {
+	startDrag: ( e: React.MouseEvent ) => void;
+	endDrag: ( e?: MouseEvent ) => void;
+	isDragging: boolean;
+}
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
```

Doing this brings the following error. Also I am not able to commit.

```
`endDrag` is accessed before it is declared, which prevents the earlier access from updating when this value changes over time.

```

#### im3dabasia on 2026-06-17T12:47:30Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428205465
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,88 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+
+interface DraggingProps {
+	onDragStart?: ( e: React.MouseEvent ) => void;
+	onDragMove?: ( e: MouseEvent ) => void;
+	onDragEnd?: ( e?: MouseEvent ) => void;
+}
+
+interface DraggingReturn {
+	startDrag: ( e: React.MouseEvent ) => void;
+	endDrag: ( e?: MouseEvent ) => void;
+	isDragging: boolean;
+}
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
```

I have added this snippet 

<details>
<summary> Click to expand </summary>
<code>const endDrag = useCallback( function endDrag( event?: MouseEvent ): void {
	  eventsRef.current.onDragEnd?.( event );
	  document.removeEventListener( 'mousemove', onMouseMove );
	  document.removeEventListener( 'mouseup', endDrag );
	  setIsDragging( false );
  }, [ onMouseMove ] );

</code>
</details>

#### manzoorwanijk on 2026-06-17T12:50:09Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428222833
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,88 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+
+interface DraggingProps {
+	onDragStart?: ( e: React.MouseEvent ) => void;
+	onDragMove?: ( e: MouseEvent ) => void;
+	onDragEnd?: ( e?: MouseEvent ) => void;
+}
+
+interface DraggingReturn {
+	startDrag: ( e: React.MouseEvent ) => void;
+	endDrag: ( e?: MouseEvent ) => void;
+	isDragging: boolean;
+}
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
```

We can suppress the lint error there. The goal is to avoid runtime changes in these TS refactors.

### Review thread 15 (resolved)

#### copilot-pull-request-reviewer on 2026-05-12T14:07:23Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3227060472
Path: packages/compose/src/hooks/use-warn-on-change/index.ts

```diff
@@ -0,0 +1,42 @@
+/**
+ * Internal dependencies
+ */
+import usePrevious from '../use-previous';
+
+/**
+ * Hook that performs a shallow comparison between the preview value of an object
+ * and the new one, if there's a difference, it prints it to the console.
+ * this is useful in performance related work, to check why a component re-renders.
```

Typo/grammar in the hook description: it says "preview value" and starts a new sentence with lowercase "this". Please change to "previous value" and capitalize the sentence for clarity.

### Review thread 16 (resolved)

#### copilot-pull-request-reviewer on 2026-05-12T14:07:23Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3227060530
Path: packages/compose/README.md

```diff
@@ -194,23 +194,23 @@ In Dialogs/modals, the tabbing must be constrained to the content of the wrapper
 
 _Usage_
 
-```js
+```ts
 import { useConstrainedTabbing } from '@wordpress/compose';
 
 const ConstrainedTabbingExample = () => {
-	const constrainedTabbingRef = useConstrainedTabbing();
-	return (
-		<div ref={ constrainedTabbingRef }>
-			<Button />
-			<Button />
-		</div>
-	);
-};
+    const constrainedTabbingRef = useConstrainedTabbing()
+    return (
+        <div ref={ constrainedTabbingRef }>
+            <Button />
+            <Button />
+        </div>
+    );
+}
```

The updated TypeScript usage example is missing semicolons and uses inconsistent indentation compared to other examples in this README, which hurts readability. Please format the snippet consistently (and keep it valid TS/TSX).

### Review thread 17 (resolved)

#### copilot-pull-request-reviewer on 2026-05-12T14:07:24Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3227060592
Path: packages/compose/README.md

```diff
@@ -252,12 +252,12 @@ _Related_
 _Parameters_
 
 -   _fn_ `TFunc`: The function to debounce.
--   _wait_ `[number]`: The number of milliseconds to delay.
--   _options_ `[import('../../utils/debounce').DebounceOptions]`: The options object.
+-   _wait_ `number`: The number of milliseconds to delay.
+-   _options_ `DebounceOptions`: The options object.
 
 _Returns_
 
--   `import('../../utils/debounce').DebouncedFunc<TFunc>`: Debounced function.
+-   `DebouncedFunc< TFunc >`: Debounced function.
```

In the generated docs, several optional parameters are currently shown as required (e.g. `useDebounce`/`useThrottle` `wait`+`options`, `useViewportMatch` `operator`+`view`, `useWarnOnChange` `prefix`, `useFocusReturn` `onFocusReturn`). After fixing the JSDoc optional param notation in the source hooks, please regenerate this README so it reflects the actual optionality.

### Review thread 18 (resolved)

#### copilot-pull-request-reviewer on 2026-05-12T14:07:24Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3227060655
Path: packages/compose/README.md

```diff
@@ -420,9 +420,13 @@ _Related_
 
 _Parameters_
 
--   _shortcuts_ `string[]|string`: Keyboard Shortcuts.
--   _callback_ `(e: Mousetrap.ExtendedKeyboardEvent, combo: string) => void`: Shortcut callback.
--   _options_ `WPKeyboardShortcutConfig`: Shortcut options.
+-   _shortcuts_ `string[] | string`: Keyboard Shortcuts.
+-   _callback_ `( e: ExtendedKeyboardEvent, combo: string ) => void`: Shortcut callback.
+-   _options_ `Partial< KeyboardShortcutConfig >`: Shortcut options.
+-   _options.bindGlobal_ `Partial< KeyboardShortcutConfig >[ 'bindGlobal' ]`:
+-   _options.eventName_ `Partial< KeyboardShortcutConfig >[ 'eventName' ]`:
+-   _options.isDisabled_ `Partial< KeyboardShortcutConfig >[ 'isDisabled' ]`:
+-   _options.target_ `Partial< KeyboardShortcutConfig >[ 'target' ]`:
```

The `useKeyboardShortcut` option sub-parameters (`bindGlobal`, `eventName`, `isDisabled`, `target`) are listed but have no descriptions, resulting in incomplete generated docs. Please ensure these bullets include short descriptions (or stop generating the sub-parameter list).

### Review thread 19 (resolved)

#### manzoorwanijk on 2026-06-17T10:17:08Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3427325457
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,88 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+
+interface DraggingProps {
+	onDragStart?: ( e: React.MouseEvent ) => void;
+	onDragMove?: ( e: MouseEvent ) => void;
+	onDragEnd?: ( e?: MouseEvent ) => void;
+}
+
+interface DraggingReturn {
+	startDrag: ( e: React.MouseEvent ) => void;
+	endDrag: ( e?: MouseEvent ) => void;
+	isDragging: boolean;
+}
```

Let us move these two interfaces to an inline `types.ts` file.

### Review thread 20 (resolved)

#### manzoorwanijk on 2026-06-17T10:17:29Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3427327436
Path: packages/compose/src/hooks/use-drop-zone/index.ts

```diff
@@ -0,0 +1,200 @@
+/**
+ * Internal dependencies
+ */
+import useRefEffect from '../use-ref-effect';
+import useEvent from '../use-event';
+
+interface UseDropZoneProps {
```

Let us move this interface to an inline `types.ts` file.

#### manzoorwanijk on 2026-06-17T10:26:35Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3427380611
Path: packages/compose/src/hooks/use-drop-zone/index.ts

```diff
@@ -0,0 +1,200 @@
+/**
+ * Internal dependencies
+ */
+import useRefEffect from '../use-ref-effect';
+import useEvent from '../use-event';
+
+interface UseDropZoneProps {
```

Also, we can retain the git history of this file by minimizing the changes.

<details><summary>Minimal changes (click to expand)</summary>

```ts
/**
 * Internal dependencies
 */
import useRefEffect from '../use-ref-effect';
import useEvent from '../use-event';
import type { UseDropZoneProps } from './types';

/**
 * A hook to facilitate drag and drop handling.
 *
 * @param {UseDropZoneProps} props Hook options
 * @return Ref callback to be passed to the drop zone element.
 */
export default function useDropZone( {
	dropZoneElement,
	isDisabled,
	onDrop: _onDrop,
	onDragStart: _onDragStart,
	onDragEnter: _onDragEnter,
	onDragLeave: _onDragLeave,
	onDragEnd: _onDragEnd,
	onDragOver: _onDragOver,
}: UseDropZoneProps ): React.RefCallback< HTMLElement > {
	const onDropEvent = useEvent( _onDrop );
	const onDragStartEvent = useEvent( _onDragStart );
	const onDragEnterEvent = useEvent( _onDragEnter );
	const onDragLeaveEvent = useEvent( _onDragLeave );
	const onDragEndEvent = useEvent( _onDragEnd );
	const onDragOverEvent = useEvent( _onDragOver );

	return useRefEffect(
		( elem: HTMLElement ) => {
			if ( isDisabled ) {
				return;
			}

			// If a custom dropZoneRef is passed, use that instead of the element.
			// This allows the dropzone to cover an expanded area, rather than
			// be restricted to the area of the ref returned by this hook.
			const element = dropZoneElement ?? elem;

			let isDragging = false;

			const { ownerDocument } = element;

			/**
			 * Checks if an element is in the drop zone.
			 *
			 * @param {EventTarget|null} targetToCheck
			 *
			 * @return {boolean} True if in drop zone, false if not.
			 */
			function isElementInZone(
				targetToCheck: EventTarget | null
			): boolean {
				const { defaultView } = ownerDocument;

				if (
					! targetToCheck ||
					! defaultView ||
					! ( targetToCheck instanceof defaultView.HTMLElement ) ||
					! element.contains( targetToCheck )
				) {
					return false;
				}

				let elementToCheck: HTMLElement | null = targetToCheck;

				do {
					if ( elementToCheck.dataset.isDropZone ) {
						return elementToCheck === element;
					}
				} while ( ( elementToCheck = elementToCheck.parentElement ) );

				return false;
			}

			function maybeDragStart( event: DragEvent ): void {
				if ( isDragging ) {
					return;
				}

				isDragging = true;

				// Note that `dragend` doesn't fire consistently for file and
				// HTML drag events where the drag origin is outside the browser
				// window. In Firefox it may also not fire if the originating
				// node is removed.
				ownerDocument.addEventListener( 'dragend', maybeDragEnd );
				ownerDocument.addEventListener( 'mousemove', maybeDragEnd );

				if ( _onDragStart ) {
					onDragStartEvent( event );
				}
			}

			function onDragEnter( event: DragEvent ): void {
				event.preventDefault();

				// The `dragenter` event will also fire when entering child
				// elements, but we only want to call `onDragEnter` when
				// entering the drop zone, which means the `relatedTarget`
				// (element that has been left) should be outside the drop zone.
				if ( element.contains( event.relatedTarget as Node ) ) {
					return;
				}

				if ( _onDragEnter ) {
					onDragEnterEvent( event );
				}
			}

			function onDragOver( event: DragEvent ): void {
				if ( ! event.defaultPrevented && _onDragOver ) {
					onDragOverEvent( event );
				}

				event.preventDefault();
			}

			function onDragLeave( event: DragEvent ): void {
				// The `dragleave` event will also fire when leaving child
				// elements, but we only want to call `onDragLeave` when
				// leaving the drop zone, which means the `relatedTarget`
				// (element that has been entered) should be outside the drop
				// zone.
				// Note: This is not entirely reliable in Safari due to this bug
				// https://bugs.webkit.org/show_bug.cgi?id=66547

				if ( isElementInZone( event.relatedTarget ) ) {
					return;
				}

				if ( _onDragLeave ) {
					onDragLeaveEvent( event );
				}
			}

			function onDrop( event: DragEvent ): void {
				// Don't handle drop if an inner drop zone already handled it.
				if ( event.defaultPrevented ) {
					return;
				}

				// Prevent the browser default while also signalling to parent
				// drop zones that `onDrop` is already handled.
				event.preventDefault();

				// This seemingly useless line has been shown to resolve a
				// Safari issue where files dragged directly from the dock are
				// not recognized.
				// eslint-disable-next-line no-unused-expressions
				event.dataTransfer && event.dataTransfer.files.length;

				if ( _onDrop ) {
					onDropEvent( event );
				}

				maybeDragEnd( event );
			}

			function maybeDragEnd( event: Event ): void {
				if ( ! isDragging ) {
					return;
				}

				isDragging = false;

				ownerDocument.removeEventListener( 'dragend', maybeDragEnd );
				ownerDocument.removeEventListener( 'mousemove', maybeDragEnd );

				if ( _onDragEnd ) {
					onDragEndEvent( event as MouseEvent );
				}
			}

			element.setAttribute( 'data-is-drop-zone', 'true' );
			element.addEventListener( 'drop', onDrop );
			element.addEventListener( 'dragenter', onDragEnter );
			element.addEventListener( 'dragover', onDragOver );
			element.addEventListener( 'dragleave', onDragLeave );
			// The `dragstart` event doesn't fire if the drag started outside
			// the document.
			ownerDocument.addEventListener( 'dragenter', maybeDragStart );

			return () => {
				element.removeAttribute( 'data-is-drop-zone' );
				element.removeEventListener( 'drop', onDrop );
				element.removeEventListener( 'dragenter', onDragEnter );
				element.removeEventListener( 'dragover', onDragOver );
				element.removeEventListener( 'dragleave', onDragLeave );
				ownerDocument.removeEventListener( 'dragend', maybeDragEnd );
				ownerDocument.removeEventListener( 'mousemove', maybeDragEnd );
				ownerDocument.removeEventListener(
					'dragenter',
					maybeDragStart
				);
			};
		},
		[ isDisabled, dropZoneElement ] // Refresh when the passed in dropZoneElement changes.
	);
}
```

</details>

#### im3dabasia on 2026-06-17T11:57:11Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3427899407
Path: packages/compose/src/hooks/use-drop-zone/index.ts

```diff
@@ -0,0 +1,200 @@
+/**
+ * Internal dependencies
+ */
+import useRefEffect from '../use-ref-effect';
+import useEvent from '../use-event';
+
+interface UseDropZoneProps {
```

I'm not sure why this happened. Ideally, it should have been mapped from JS to TS like most of the other files, so this seems to be an exception.

I'm guessing I'll need to copy and paste the snippet you've added above and somehow expect the Git history to be retained. Not sure if that'll work, but I'll give it a try.

### Review thread 21

#### manzoorwanijk on 2026-06-17T12:14:03Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428001960
Path: packages/compose/src/hooks/use-drop-zone/index.ts

Now this looks better with history and comments retained. Thanks

### Review thread 22 (resolved)

#### copilot-pull-request-reviewer on 2026-06-17T12:28:53Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428088429
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,77 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+import type { DraggingProps, DraggingReturn } from './types';
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
+			setIsDragging( false );
+		},
```

`endDrag` can be called programmatically (e.g. onBlur) before the `mouseup` listener fires, but the `mouseup` listener isn’t removed here, so `onDragEnd` can run a second time on the next mouseup.

#### im3dabasia on 2026-06-17T12:47:58Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428208601
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,77 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+import type { DraggingProps, DraggingReturn } from './types';
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		( event?: MouseEvent ): void => {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
+			setIsDragging( false );
+		},
```

Resolved in this commit https://github.com/WordPress/gutenberg/pull/70618/commits/ee2912f70a77e0645088dc9cbb9969129fe0bc82

### Review thread 23 (resolved)

#### manzoorwanijk on 2026-06-17T12:38:09Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428145073
Path: packages/compose/src/hooks/use-focus-return/index.ts

```diff
@@ -72,11 +75,14 @@ function useFocusReturn( onFocusReturn ) {
 			if ( onFocusReturnRef.current ) {
 				onFocusReturnRef.current();
 			} else {
-				/** @type {null|HTMLElement} */ (
-					! focusedBeforeMount.current.isConnected
-						? origin
-						: focusedBeforeMount.current
-				)?.focus();
+				const elementToFocus = ! focusedBeforeMountRef.current
+					.isConnected
+					? origin
+					: focusedBeforeMountRef.current;
+
+				if ( elementToFocus instanceof HTMLElement ) {
+					elementToFocus.focus();
+				}
```

This could here be `SVGElement` as well. So I think we need an assertion here.

```suggestion
				(
					( ! focusedBeforeMountRef.current.isConnected
						? origin
						: focusedBeforeMountRef.current ) as HTMLElement
				 )?.focus();
```

#### manzoorwanijk on 2026-06-17T13:05:51Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428332002
Path: packages/compose/src/hooks/use-focus-return/index.ts

```diff
@@ -72,11 +75,14 @@ function useFocusReturn( onFocusReturn ) {
 			if ( onFocusReturnRef.current ) {
 				onFocusReturnRef.current();
 			} else {
-				/** @type {null|HTMLElement} */ (
-					! focusedBeforeMount.current.isConnected
-						? origin
-						: focusedBeforeMount.current
-				)?.focus();
+				const elementToFocus = ! focusedBeforeMountRef.current
+					.isConnected
+					? origin
+					: focusedBeforeMountRef.current;
+
+				if ( elementToFocus instanceof HTMLElement ) {
+					elementToFocus.focus();
+				}
```

Let us commit this suggestion and then I think we are good here.

#### im3dabasia on 2026-06-17T13:09:09Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428357182
Path: packages/compose/src/hooks/use-focus-return/index.ts

```diff
@@ -72,11 +75,14 @@ function useFocusReturn( onFocusReturn ) {
 			if ( onFocusReturnRef.current ) {
 				onFocusReturnRef.current();
 			} else {
-				/** @type {null|HTMLElement} */ (
-					! focusedBeforeMount.current.isConnected
-						? origin
-						: focusedBeforeMount.current
-				)?.focus();
+				const elementToFocus = ! focusedBeforeMountRef.current
+					.isConnected
+					? origin
+					: focusedBeforeMountRef.current;
+
+				if ( elementToFocus instanceof HTMLElement ) {
+					elementToFocus.focus();
+				}
```

Would go with `instanceof` here since it actually checks the element type before calling `.focus()`, so it won't break if the value isn't focusable, as `HTMLElement` doesn't really check anything, it just assumes.

Something like this. 

```
if ( elementToFocus instanceof HTMLElement || elementToFocus instanceof SVGElement ) {
	elementToFocus.focus();
}
```

#### manzoorwanijk on 2026-06-17T13:12:31Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428382833
Path: packages/compose/src/hooks/use-focus-return/index.ts

```diff
@@ -72,11 +75,14 @@ function useFocusReturn( onFocusReturn ) {
 			if ( onFocusReturnRef.current ) {
 				onFocusReturnRef.current();
 			} else {
-				/** @type {null|HTMLElement} */ (
-					! focusedBeforeMount.current.isConnected
-						? origin
-						: focusedBeforeMount.current
-				)?.focus();
+				const elementToFocus = ! focusedBeforeMountRef.current
+					.isConnected
+					? origin
+					: focusedBeforeMountRef.current;
+
+				if ( elementToFocus instanceof HTMLElement ) {
+					elementToFocus.focus();
+				}
```

Even better. Thanks

#### im3dabasia on 2026-06-17T13:16:05Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428411993
Path: packages/compose/src/hooks/use-focus-return/index.ts

```diff
@@ -72,11 +75,14 @@ function useFocusReturn( onFocusReturn ) {
 			if ( onFocusReturnRef.current ) {
 				onFocusReturnRef.current();
 			} else {
-				/** @type {null|HTMLElement} */ (
-					! focusedBeforeMount.current.isConnected
-						? origin
-						: focusedBeforeMount.current
-				)?.focus();
+				const elementToFocus = ! focusedBeforeMountRef.current
+					.isConnected
+					? origin
+					: focusedBeforeMountRef.current;
+
+				if ( elementToFocus instanceof HTMLElement ) {
+					elementToFocus.focus();
+				}
```

Done https://github.com/WordPress/gutenberg/pull/70618/commits/c63c6783064d6970b76322a8d11e9346cfeda917

### Review thread 24 (resolved)

#### manzoorwanijk on 2026-06-17T12:51:19Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428230389
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+import type { DraggingProps, DraggingReturn } from './types';
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		function endDrag( event?: MouseEvent ): void {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
+			document.removeEventListener( 'mouseup', endDrag );
+			setIsDragging( false );
+		},
+		[ onMouseMove ]
+	);
+
+	const startDrag = useCallback(
+		( event: React.MouseEvent ): void => {
+			eventsRef.current.onDragStart?.( event );
+			document.addEventListener( 'mousemove', onMouseMove );
+			document.addEventListener( 'mouseup', endDrag, { once: true } );
```

The JS file didn't have `{ once: true }`

```suggestion
			document.addEventListener( 'mouseup', endDrag );
```

### Review thread 25 (resolved)

#### manzoorwanijk on 2026-06-17T13:01:18Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428299370
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+import type { DraggingProps, DraggingReturn } from './types';
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		function endDrag( event?: MouseEvent ): void {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
+			document.removeEventListener( 'mouseup', endDrag );
```

```suggestion
			document.removeEventListener( 'mousemove', onMouseMove );
			// eslint-disable-next-line react-hooks/immutability --- `endDrag` is stable; it references itself to detach its own `mouseup` listener
			document.removeEventListener( 'mouseup', endDrag );
			setIsDragging( false );
```

#### manzoorwanijk on 2026-06-17T13:05:18Z

URL: https://github.com/WordPress/gutenberg/pull/70618#discussion_r3428328007
Path: packages/compose/src/hooks/use-dragging/index.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useRef, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import useIsomorphicLayoutEffect from '../use-isomorphic-layout-effect';
+import type { DraggingProps, DraggingReturn } from './types';
+
+/**
+ * Hook to manage drag events with proper event handling
+ *
+ * @param props             Drag handlers
+ * @param props.onDragStart
+ * @param props.onDragMove
+ * @param props.onDragEnd
+ * @return Drag state and handlers
+ */
+export default function useDragging( {
+	onDragStart,
+	onDragMove,
+	onDragEnd,
+}: DraggingProps ): DraggingReturn {
+	const [ isDragging, setIsDragging ] = useState( false );
+
+	const eventsRef = useRef< DraggingProps >( {
+		onDragStart,
+		onDragMove,
+		onDragEnd,
+	} );
+	useIsomorphicLayoutEffect( () => {
+		eventsRef.current.onDragStart = onDragStart;
+		eventsRef.current.onDragMove = onDragMove;
+		eventsRef.current.onDragEnd = onDragEnd;
+	}, [ onDragStart, onDragMove, onDragEnd ] );
+
+	const onMouseMove = useCallback( ( event: MouseEvent ): void => {
+		eventsRef.current.onDragMove?.( event );
+	}, [] );
+
+	const endDrag = useCallback(
+		function endDrag( event?: MouseEvent ): void {
+			eventsRef.current.onDragEnd?.( event );
+			document.removeEventListener( 'mousemove', onMouseMove );
+			document.removeEventListener( 'mouseup', endDrag );
```

I guess we don't need this now

## Changed files

- packages/components/src/angle-picker-control/angle-circle.tsx (MODIFIED, +3/-3)
- packages/compose/README.md (MODIFIED, +42/-38)
- packages/compose/src/hooks/use-constrained-tabbing/index.ts (RENAMED, +7/-11)
- packages/compose/src/hooks/use-debounce/index.ts (RENAMED, +11/-6)
- packages/compose/src/hooks/use-disabled/index.ts (MODIFIED, +3/-1)
- packages/compose/src/hooks/use-dragging/index.js (DELETED, +0/-74)
- packages/compose/src/hooks/use-dragging/index.ts (ADDED, +78/-0)
- packages/compose/src/hooks/use-dragging/types.ts (ADDED, +11/-0)
- packages/compose/src/hooks/use-drop-zone/index.ts (RENAMED, +17/-31)
- packages/compose/src/hooks/use-drop-zone/types.ts (ADDED, +34/-0)
- packages/compose/src/hooks/use-fixed-window-list/index.ts (RENAMED, +46/-37)
- packages/compose/src/hooks/use-focus-return/index.ts (RENAMED, +26/-21)
- packages/compose/src/hooks/use-isomorphic-layout-effect/index.ts (RENAMED, +0/-0)
- packages/compose/src/hooks/use-keyboard-shortcut/index.ts (RENAMED, +43/-28)
- packages/compose/src/hooks/use-reduced-motion/index.ts (RENAMED, +2/-2)
- packages/compose/src/hooks/use-throttle/index.ts (RENAMED, +12/-6)
- packages/compose/src/hooks/use-viewport-match/index.ts (RENAMED, +30/-29)
- packages/compose/src/hooks/use-warn-on-change/index.js (DELETED, +0/-43)
- packages/compose/src/hooks/use-warn-on-change/index.ts (ADDED, +42/-0)
