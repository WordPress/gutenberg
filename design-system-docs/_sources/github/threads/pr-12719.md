---
number: 12719
type: pull_request
state: MERGED
merged_at: 2018-12-20T08:31:02Z
url: https://github.com/WordPress/gutenberg/pull/12719
matched_labels: ["[Package] Components"]
all_labels: ["Internationalization (i18n)","[Package] Components"]
---

# PR #12719: Fix RTL support for datepicker

- URL: https://github.com/WordPress/gutenberg/pull/12719
- Author: ocean90
- Created: 2018-12-09T13:53:37Z
- Updated: 2018-12-20T08:31:06Z
- Comments: 0 of 0
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/12719.diff

## Body

## Description

Instead of converting the styles the `isRTL` flag needs to passed to `DayPickerSingleDateController`.

Fixes #12718.

## How has this been tested?
* Set site language to a RTL language
* Check that the arrows are pointing to the right direction.

## Issue comments

_No issue comments._
## Review threads

### Review thread 1 (resolved)

#### ocean90 on 2018-12-09T14:01:05Z

URL: https://github.com/WordPress/gutenberg/pull/12719#discussion_r240038044
Path: packages/components/src/date-time/date.js

```diff
@@ -13,6 +13,7 @@ import { Component } from '@wordpress/element';
  * Module Constants
  */
 const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+const isRTL = () => document.documentElement.dir === 'rtl';
```

This was also used in https://github.com/WordPress/gutenberg/commit/f91e66ca2bcd9e1e7b97aa39c01f2888fc751712. I think it's fine to use it here too instead of `getEditorSettings().isRTL`.

cc @youknowriad

#### youknowriad on 2018-12-09T15:47:19Z

URL: https://github.com/WordPress/gutenberg/pull/12719#discussion_r240042246
Path: packages/components/src/date-time/date.js

```diff
@@ -13,6 +13,7 @@ import { Component } from '@wordpress/element';
  * Module Constants
  */
 const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+const isRTL = () => document.documentElement.dir === 'rtl';
```

Yes, so the components package shouldn't depend on the editor because these are generic components that can be used anywhere. 

So this approach is a workaround, It's ok.
A better alternative here for me is to have an `isRtl` prop for this component and pass the correct value  from the editor module using `getEditorSettings().isRTL`.  (In the Popover component it was harder to achieve because it's used in too many places).

That said I'm still ok with this.

### Review thread 2 (resolved)

#### talldan on 2018-12-10T04:57:35Z

URL: https://github.com/WordPress/gutenberg/pull/12719#discussion_r240084533
Path: packages/components/src/date-time/style.scss

```diff
@@ -1,5 +1,7 @@
 // We can't reference this package with ~ because of how Lerna handles packages. 😩
+/*rtl:begin:ignore*/
```

It might be good to have an explanation in a comment here and a link to the issue.

## Changed files

- packages/components/src/date-time/date.js (MODIFIED, +2/-0)
- packages/components/src/date-time/style.scss (MODIFIED, +4/-1)
