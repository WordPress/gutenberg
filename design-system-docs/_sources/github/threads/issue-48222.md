---
number: 48222
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/48222
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Status] In Progress","[Type] Code Quality","[Package] Components"]
---

# Issue #48222: Tooltip: refactor using ariakit

- URL: https://github.com/WordPress/gutenberg/issues/48222
- Author: brookewp
- Created: 2023-02-18T05:36:05Z
- Updated: 2023-10-10T09:15:20Z
- Comments: 4 of 4

## Body

## What problem does this address?

The decision was made to refactor the 'legacy' Tooltip component due to the complexity of the existing component, the time it would take to refactor and migrate to typescript, and to utilize an accessible library that is actively maintained. 
 
This issue branches off of what @t-hamano started in #42753, and aims to track [the completion of the second step outlined](https://github.com/WordPress/gutenberg/issues/42753#issuecomment-1206230142) by @ciampo:

> Work on the new implementation using `ariakit` under the hood:
> 	-   MVP is to create a component with the same APIs as the "legacy" one, which in theory doesn't introduce any regressions
> 	-  Component to be written following the latest [contributing guidelines](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md) (typescript, emotion, ...)
> 	- This would be a good time to assess the component's design
> 	- Optionally, as we work on the new component, understand if we want to introduce any new feature / expand the APIs

## What is your proposed solution?

The following is a WIP, but my general tl;dr plan so far is: 

### First phase (MVP): 

#### Research 

- [x] review the 'legacy' tooltip component to see which features it supports 
- [x] look at current bugs for the legacy tooltip
- [x] research how new features could be implemented and how current bugs could be solved using `ariakit`

#### Create

- [x] create a rough tooltip component to start exploring API
- [x] add legacy features to the new component
	- [x] children
	- [x] delay (web only)
	- [x] position (to be deprecated)
	- [x] placement 
	- [x] text
	- [x] shortcut (web only)

#### Testing

- [x] look at legacy tests and add any missing tests as needed
- [x] add those tests to the new tooltip to ensure they meet the requirements of the legacy component 
- add additional tests for:
	- [x] close on Esc ([ARIA requirement](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/tooltip_role#keyboard_interactions))

#### Documentation

- [x] add `README.md`
- [x] add and refine JS Docs and types
- [x] add storybook examples 

#### Implementation

- [x] remove existing unused/unfinished experimental tooltip 
- [x] replace existing legacy tooltip in GB usages

### Second phase (TBD): 

- [x] review the 'experimental' tooltip component to see which features it supports 
- [x] research how these existing features could be implemented using `ariakit`
- [x] see if there are any new features we want to expose 
- [x] add any experimental features and new features worth having 
- [x] look at implementing a facade for the legacy version 
- [x] request design input and implement styles

## Issue comments

### afercia on 2023-03-24T14:55:08Z

URL: https://github.com/WordPress/gutenberg/issues/48222#issuecomment-1482937115

Looked also at the [pending PR](https://github.com/WordPress/gutenberg/pull/48440), seems to me an important point has been missed and there's a bit of confusion on what an accessible name and an accessible description are.

In both [ARIA 1.1](https://www.w3.org/TR/wai-aria-1.1/#tooltip) and [ARIA 1.2](https://www.w3.org/TR/wai-aria-1.2/#tooltip), the role=tooltip is meant to provide an accessible description. It must not be used to provide the accessible name.
> A contextual popup that displays a description for an element.
> Authors SHOULD ensure that elements with the role tooltip are referenced through the use of **aria-describedby** ...

Focus on the part 'referenced through the use of aria-describedby'.

From what I see, [Ariakit uses aria-labelledby instead](https://ariakit.org/examples/tooltip), thus providing the accessible name by referencing the element with role=tooltip. That's not valid. It's incorrect and can lead to potential accessibility issues when the visible text of a control and its actual accessible name mismatch.

Worth noting there's no official example of the [ARIA tooltip design pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/) yet. Work to develop a tooltip example is tracked by [issue 127](https://github.com/w3c/aria-practices/issues/127). It's still in the works. The ARIA Authoring Practices Task Force are still discussing what a role=tooltip even is and what it should do.

I'd strongly suggest to have a read at the relevant issues on their GitHub repo before continuing this implementation with Ariakit. 

**Clarify the use of role=tooltip**
https://github.com/w3c/aria/issues/979

**Tooltip role should allow referencing by aria-labelledby**
https://github.com/w3c/aria/issues/987

To my understanding, the current state of the tooltip pattern example is here, where it's clear it must not be used to provide the accessible name.

**Tooltip code example for ARIA Authoring Practices Guide (APG)**
https://github.com/ZoeBijl/apg-tooltip
**Demo:**
https://zoebijl.github.io/apg-tooltip/

It's perfectly fine revealing the accessible name on hover/focus by the means of a 'visual tooltip' as long as it doesn't use a role=tooltip. That's what the current `Tooltip` component does (in most of the cases).

### ciampo on 2023-03-24T16:15:01Z

URL: https://github.com/WordPress/gutenberg/issues/48222#issuecomment-1483066153

Hey @afercia , thank you for the early feedback!

We definitely plan to have a conservative approach with introducing the new `Tooltip` and replacing the legacy one, especially because we want to make sure that we don't cause any disruption to downstream developers and end users (and getting the accessibility part right is fundamental).

Regarding the `aria-describedby` issue that you raise, Brooke and I discussed about it just yesterday — we plan on using [the `described` prop](https://github.com/ariakit/ariakit/blob/5d8a1f047fcadcf117073c70359663a3946b73bf/packages/ariakit/src/tooltip/tooltip-anchor.ts#L122-L136) to change the behaviour of the component and use `aria-describedby` instead of `aria-labelledby`.

You can test the new behaviour by adding the `described={true}` prop to `TooltipAnchor` in the [interactive example](https://ariakit.org/examples/tooltip).

Would that change be enough, in your opinion?

### brookewp on 2023-03-24T17:37:15Z

URL: https://github.com/WordPress/gutenberg/issues/48222#issuecomment-1483177007

Hi @afercia, thank you for taking a look! I initially added `aria-describedby` after reviewing ARIA's guidelines for tooltip but then swapped it for the`described` prop which is in my most recent commit. After seeing your comment, I [added a note to the PR](https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147814263) to give context on this prop but we could also add a more explicit inline comment so it is more clear. 

After my discussion with @ciampo, I opened a related issue in the ariakit repo to ask the authors on their decision to not have `aria-describedby` as the default option: https://github.com/ariakit/ariakit/issues/2228

### diegohaz on 2023-05-17T16:17:29Z

URL: https://github.com/WordPress/gutenberg/issues/48222#issuecomment-1551701033

The points mentioned by @afercia were fixed in the latest version of Ariakit, and a section about using Tooltip as a label was added to https://ariakit.org/components/tooltip#tooltips-are-descriptions

