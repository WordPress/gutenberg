---
number: 68249
type: pull_request
state: MERGED
merged_at: 2025-01-09T16:17:21Z
url: https://github.com/WordPress/gutenberg/pull/68249
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components"]
---

# PR #68249: Menu: auto-generate README

- URL: https://github.com/WordPress/gutenberg/pull/68249
- Author: ciampo
- Created: 2024-12-23T14:19:39Z
- Updated: 2025-01-09T16:21:43Z
- Comments: 3 of 3
- Review threads: 6 of 6
- Changed files: 20 of 20
- Diff: ../pr-diffs/68249.diff

## Body

## What?

Convert the Menu README to an auto-generated one.

Supplementary information that was in the existing README is moved to other appropriate locations (JSDocs, new "Best Practices" Storybook page).

## Why?

To decrease maintenance cost and consolidate the canonical docs for our audience.

## Testing Instructions

- In your IDE, check imports of `Menu.Item` for example, and see that the JSDoc includes the subcomponent description.
- `npm run docs:components` to regenerate READMEs.
- See Storybook docs for Menu.

## Issue comments

### github-actions on 2024-12-23T16:26:36Z

URL: https://github.com/WordPress/gutenberg/pull/68249#issuecomment-2559986615

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-12-23T17:18:53Z

URL: https://github.com/WordPress/gutenberg/pull/68249#issuecomment-2560057110

<!-- flaky-tests-report-comment -->
**Flaky tests detected in c5d0b26557d7c111718aead098368e9b39e99e62.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/12692406417
📝  Reported issues:
- #51348 in `/test/e2e/specs/editor/blocks/navigation-frontend-interactivity.spec.js`
- #47897 in `/test/e2e/specs/site-editor/template-revert.spec.js`

### ciampo on 2025-01-08T17:52:29Z

URL: https://github.com/WordPress/gutenberg/pull/68249#issuecomment-2578283661

@tyxla rebased and re-generated README. Could you give a final round of review?

## Review threads

### Review thread 1

#### ciampo on 2024-12-23T16:15:12Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1895917369
Path: packages/components/src/menu/stories/index.story.tsx

```diff
@@ -20,10 +20,10 @@ import Button from '../../button';
 import Modal from '../../modal';
 import { createSlotFill, Provider as SlotFillProvider } from '../../slot-fill';
 import { ContextSystemProvider } from '../../context';
-import type { MenuProps } from '../types';
+import type { Props } from '../types';
 
 const meta: Meta< typeof Menu > = {
-	id: 'components-experimental-menu',
+	id: 'components-menu',
```

This change was necessary because the generated README links to `?path=/docs/components-menu--docs`. I've updated `storybook/manager-head.html` to add a redirect from the old experimental URL.

### Review thread 2

#### ciampo on 2024-12-23T16:19:44Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1895921389
Path: packages/components/src/menu/index.tsx

```diff
@@ -84,49 +94,119 @@ const UnconnectedMenu = ( props: MenuProps ) => {
 	);
 
 	return (
-		<MenuContext.Provider value={ contextValue }>
-			{ children }
-		</MenuContext.Provider>
+		<Context.Provider value={ contextValue }>{ children }</Context.Provider>
 	);
 };
 
+/**
+ * Menu is a collection of React components that combine to render
+ * ARIA-compliant [menu](https://www.w3.org/WAI/ARIA/apg/patterns/menu/) and
+ * [menu button](https://www.w3.org/WAI/ARIA/apg/patterns/menubutton/) patterns.
+ *
+ * `Menu` itself is a wrapper component and context provider.
+ * It is responsible for managing the state of the menu and its items, and for
+ * rendering the `Menu.TriggerButton` (or the `Menu.SubmenuTriggerItem`)
+ * component, and the `Menu.Popover` component.
+ */
 export const Menu = Object.assign(
 	contextConnectWithoutRef( UnconnectedMenu, 'Menu' ),
 	{
-		Context: Object.assign( MenuContext, {
+		Context: Object.assign( Context, {
 			displayName: 'Menu.Context',
 		} ),
-		Item: Object.assign( MenuItem, {
+		/**
+		 * Renders a menu item inside the `Menu.Popover` or `Menu.Group` components.
+		 *
+		 * It can optionally contain one instance of the `Menu.ItemLabel` component
+		 * and one instance of the `Menu.ItemHelpText` component.
+		 */
+		Item: Object.assign( Item, {
```

With the previous subcomponent named export scheme (ie. `MenuItem` being assigned to the `Item` property of the `Menu` object), the README generator could not pick up the JSDoc correctly,

I managed to fix it by changing the internal naming convention of all subcomponent by removing the `Menu` prefix: for example, `MenuItem` got renamed to `Item`. A lot of the code changes in this file are related to this refactor and were applied as a [single commit](https://github.com/WordPress/gutenberg/pull/68249/commits/364338ab1c494989e9c7b606078114bc9599417e). You can review the rest of the changes commit by commit to remove the noise.

### Review thread 3

#### ciampo on 2024-12-23T16:22:14Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1895924082
Path: packages/components/src/menu/README.md

```diff
@@ -1,344 +1,589 @@
-# `Menu`
+# Menu
 
-<div class="callout callout-alert">
-This feature is still experimental. “Experimental” means this is an early implementation subject to drastic and breaking changes.
-</div>
+<!-- This file is generated automatically and cannot be edited directly. Make edits via TypeScript types and TSDocs. -->
 
-`Menu` displays a menu to the user (such as a set of actions or functions). The menu is rendered in a popover (this pattern is also known as a "Dropdown menu"), which is triggered by a button.
+<p class="callout callout-info">See the <a href="https://wordpress.github.io/gutenberg/?path=/docs/components-menu--docs">WordPress Storybook</a> for more detailed, interactive documentation.</p>
 
-## Design guidelines
+Menu is a collection of React components that combine to render
+ARIA-compliant [menu](https://www.w3.org/WAI/ARIA/apg/patterns/menu/) and
+[menu button](https://www.w3.org/WAI/ARIA/apg/patterns/menubutton/) patterns.
 
-### Usage
+`Menu` itself is a wrapper component and context provider.
+It is responsible for managing the state of the menu and its items, and for
+rendering the `Menu.TriggerButton` (or the `Menu.SubmenuTriggerItem`)
+component, and the `Menu.Popover` component.
 
-#### When to use a `Menu`
+## Props
 
-Use a `Menu` when you want users to:
+### `as`
 
--   Choose an action or change a setting from a list, AND
--   Only see the available choices contextually.
+The HTML element or React component to render the component as.
 
-`Menu` is a React component to render an expandable menu of buttons. It is similar in purpose to a `<select>` element, with the distinction that it does not maintain a value. Instead, each option behaves as an action button.
+ - Type: `"symbol" | "object" | "a" | "abbr" | "address" | "area" | "article" | "aside" | "audio" | "b" | "base" | "bdi" | "bdo" | "big" | "blockquote" | "body" | "br" | "button" | "canvas" | ... 516 more ... | ("view" & FunctionComponent<...>)`
+ - Required: No
 
-If you need to display all the available options at all times, consider using a Toolbar instead. Use a `Menu` to display a list of actions after the user interacts with a button.
+### `children`
 
-**Do**
-Use a `Menu` to display a list of actions after the user interacts with an icon.
+The elements, which should include one instance of the `Menu.TriggerButton`
+component and one instance of the `Menu.Popover` component.
 
-**Don’t** use a `Menu` for important actions that should always be visible. Use a `Toolbar` instead.
+ - Type: `ReactNode`
+ - Required: No
 
-**Don’t**
-Don’t use a `Menu` for frequently used actions. Use a `Toolbar` instead.
+### `defaultOpen`
 
-#### Behavior
+Whether the menu popover and its contents should be visible by default.
 
-Generally, the parent button should indicate that interacting with it will show a `Menu`.
+Note: this prop will be overridden by the `open` prop if it is
+provided (meaning the component will be used in "controlled" mode).
 
-The parent button should retain the same visual styling regardless of whether the `Menu` is displayed or not.
+ - Type: `boolean`
+ - Required: No
+ - Default: `false`
 
-#### Placement
+### `open`
 
-The `Menu` should typically appear directly below, or below and to the left of, the parent button. If there isn’t enough space below to display the full `Menu`, it can be displayed instead above the parent button.
+Whether the menu popover and its contents should be visible.
+Should be used in conjunction with `onOpenChange` in order to control
+the open state of the menu popover.
 
-## Development guidelines
+Note: this prop will set the component in "controlled" mode, and it will
+override the `defaultOpen` prop.
 
-This component is still highly experimental, and it's not normally accessible to consumers of the `@wordpress/components` package.
+ - Type: `boolean`
+ - Required: No
 
-The component exposes a set of components that are meant to be used in combination with each other in order to implement a `Menu` correctly.
+### `onOpenChange`
 
-### `Menu`
+A callback that gets called when the `open` state changes.
 
-The root component, used to specify the menu's trigger and its contents.
+ - Type: `(open: boolean) => void`
+ - Required: No
+
+### `placement`
+
+The placement of the menu popover.
+
+ - Type: `"top" | "bottom" | "left" | "right" | "top-start" | "bottom-start" | "left-start" | "right-start" | "top-end" | "bottom-end" | ...`
+ - Required: No
+ - Default: `'bottom-start' for root-level menus, 'right-start' for submenus`
+
+## Subcomponents
+
+### Menu.TriggerButton
+
+Renders a menu button that toggles the visibility of a sibling
+`Menu.Popover` component when clicked or when using arrow keys.
 
 #### Props
 
-The component accepts the following props:
+##### `accessibleWhenDisabled`
+
+Indicates whether the element should be focusable even when it is
+`disabled`.
 
-##### `trigger`: `React.ReactNode`
+This is important when discoverability is a concern. For example:
 
-The button triggering the menu popover.
+> A toolbar in an editor contains a set of special smart paste functions
+that are disabled when the clipboard is empty or when the function is not
+applicable to the current content of the clipboard. It could be helpful to
+keep the disabled buttons focusable if the ability to discover their
+functionality is primarily via their presence on the toolbar.
 
--   Required: yes
+Learn more on [Focusability of disabled
+controls](https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#focusabilityofdisabledcontrols).
 
-##### `children`: `React.ReactNode`
+ - Type: `boolean`
+ - Required: No
 
-The contents of the menu (ie. one or more menu items).
+##### `children`
 
--   Required: yes
+The contents of the menu trigger button.
 
-##### `defaultOpen`: `boolean`
+ - Type: `ReactNode`
+ - Required: No
 
-The open state of the menu popover when it is initially rendered. Use when not wanting to control its open state.
+##### `disabled`
 
--   Required: no
--   Default: `false`
+Determines if the element is disabled. This sets the `aria-disabled`
+attribute accordingly, enabling support for all elements, including those
+that don't support the native `disabled` attribute.
 
-##### `open`: `boolean`
+This feature can be combined with the `accessibleWhenDisabled` prop to
+make disabled elements still accessible via keyboard.
 
-The controlled open state of the menu popover. Must be used in conjunction with `onOpenChange`.
+ - Type: `boolean`
+ - Required: No
+ - Default: `false`
 
--   Required: no
+##### `render`
 
-##### `onOpenChange`: `(open: boolean) => void`
+Allows the component to be rendered as a different HTML element or React
+component. The value can be a React element or a function that takes in the
+original component props and gives back a React element with the props
+merged.
 
-Event handler called when the open state of the menu popover changes.
+ - Type: `ReactElement<any, string | JSXElementConstructor<any>> | RenderProp<HTMLAttributes<any> & { ref?: Ref<any>; }>`
+ - Required: No
 
--   Required: no
+### Menu.Popover
 
-##### `modal`: `boolean`
+Renders a dropdown menu element that's controlled by a sibling
+`Menu.TriggerButton` component. It renders a popover and automatically
+focuses on items when the menu is shown.
 
-The modality of the menu popover. When set to true, interaction with outside elements will be disabled and only menu content will be visible to screen readers.
+The only valid children of `Menu.Popover` are `Menu.Item`,
+`Menu.RadioItem`, `Menu.CheckboxItem`, `Menu.Group`, `Menu.Separator`,
+and `Menu` (for nested dropdown menus).
 
--   Required: no
--   Default: `true`
+#### Props
 
-##### `placement`: ``'top' | 'top-start' | 'top-end' | 'right' | 'right-start' | 'right-end' | 'bottom' | 'bottom-start' | 'bottom-end' | 'left' | 'left-start' | 'left-end'`
+##### `children`
 
-The placement of the menu popover.
+The contents of the menu popover, which should include instances of the
+`Menu.Item`, `Menu.CheckboxItem`, `Menu.RadioItem`, `Menu.Group`, and
+`Menu.Separator` components.
+
+ - Type: `ReactNode`
+ - Required: No
+
+##### `gutter`
+
+The distance between the popover and the anchor element.
+
+ - Type: `number`
+ - Required: No
+ - Default: `8 for root-level menus, 16 for nested menus`
+
+##### `hideOnEscape`
+
+Determines if the menu popover will hide when the user presses the
+Escape key.
 
--   Required: no
--   Default: `'bottom-start'` for root-level menus, `'right-start'` for nested menus
+This prop can be either a boolean or a function that accepts an event as an
+argument and returns a boolean. The event object represents the keydown
+event that initiated the hide action, which could be either a native
+keyboard event or a React synthetic event.
 
-##### `gutter`: `number`
+ - Type: `BooleanOrCallback<KeyboardEvent | React.KeyboardEvent<Element>>`
+ - Required: No
+ - Default: ``( event ) => { event.preventDefault(); return true; }``
 
-The distance in pixels from the trigger.
+##### `modal`
 
--   Required: no
--   Default: `8` for root-level menus, `16` for nested menus
+The modality of the menu popover. When set to true, interaction with
+outside elements will be disabled and only menu content will be visible to
+screen readers.
 
-##### `shift`: `number`
+Determines whether the menu popover is modal. Modal dialogs have distinct
+states and behaviors:
+- The `portal` and `preventBodyScroll` props are set to `true`. They can
+  still be manually set to `false`.
+- When the dialog is open, element tree outside it will be inert.
```

@mirka here's another instance of the problem you were mentioning with list being merged when rendered to HTML ( https://github.com/WordPress/gutenberg/pull/68209#discussion_r1894224140 )

### Review thread 4 (resolved)

#### ciampo on 2024-12-23T16:24:28Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1895926109
Path: packages/components/src/menu/README.md

```diff
@@ -1,344 +1,589 @@
-# `Menu`
+# Menu
```

@mirka there's no way currently to mark the component as private in the README, right?

#### mirka on 2024-12-23T18:14:52Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1896011211
Path: packages/components/src/menu/README.md

```diff
@@ -1,344 +1,589 @@
-# `Menu`
+# Menu
```

Right. I'll figure something out as a follow up.

#### mirka on 2024-12-26T11:48:24Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1897871502
Path: packages/components/src/menu/README.md

```diff
@@ -1,344 +1,589 @@
-# `Menu`
+# Menu
```

Proposed in #68317

#### tyxla on 2024-12-30T15:33:00Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1899624688
Path: packages/components/src/menu/README.md

```diff
@@ -1,344 +1,589 @@
-# `Menu`
+# Menu
```

https://github.com/WordPress/gutenberg/pull/68317 landed so we'll need to rebase and rerun the command to get the private API warning.

### Review thread 5 (resolved)

#### tyxla on 2025-01-09T11:03:47Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1908568820
Path: packages/components/src/menu/context.tsx

```diff
@@ -6,8 +6,6 @@ import { createContext } from '@wordpress/element';
 /**
  * Internal dependencies
  */
-import type { MenuContext as MenuContextType } from './types';
+import type { ContextProps } from './types';
 
-export const MenuContext = createContext< MenuContextType | undefined >(
-	undefined
-);
+export const Context = createContext< ContextProps | undefined >( undefined );
```

I feel like those renames should have happened in a separate PR, but no big deal I guess.

#### ciampo on 2025-01-09T14:44:48Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1908937841
Path: packages/components/src/menu/context.tsx

```diff
@@ -6,8 +6,6 @@ import { createContext } from '@wordpress/element';
 /**
  * Internal dependencies
  */
-import type { MenuContext as MenuContextType } from './types';
+import type { ContextProps } from './types';
 
-export const MenuContext = createContext< MenuContextType | undefined >(
-	undefined
-);
+export const Context = createContext< ContextProps | undefined >( undefined );
```

Agreed, and agreed.

### Review thread 6 (resolved)

#### tyxla on 2025-01-09T11:05:00Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1908571011
Path: packages/components/src/menu/index.tsx

```diff
@@ -13,21 +13,31 @@ import { isRTL as isRTLFn } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { useContextSystem, contextConnectWithoutRef } from '../context';
-import type { MenuContext as MenuContextType, MenuProps } from './types';
-import { MenuContext } from './context';
-import { MenuItem } from './item';
-import { MenuCheckboxItem } from './checkbox-item';
-import { MenuRadioItem } from './radio-item';
-import { MenuGroup } from './group';
-import { MenuGroupLabel } from './group-label';
-import { MenuSeparator } from './separator';
-import { MenuItemLabel } from './item-label';
-import { MenuItemHelpText } from './item-help-text';
-import { MenuTriggerButton } from './trigger-button';
-import { MenuSubmenuTriggerItem } from './submenu-trigger-item';
-import { MenuPopover } from './popover';
+import type { ContextProps, Props } from './types';
+import { Context } from './context';
+import { Item } from './item';
+import { CheckboxItem } from './checkbox-item';
+import { RadioItem } from './radio-item';
+import { Group } from './group';
+import { GroupLabel } from './group-label';
+import { Separator } from './separator';
+import { ItemLabel } from './item-label';
+import { ItemHelpText } from './item-help-text';
+import { TriggerButton } from './trigger-button';
+import { SubmenuTriggerItem } from './submenu-trigger-item';
+import { Popover } from './popover';
 
-const UnconnectedMenu = ( props: MenuProps ) => {
+/**
+ * Menu is a collection of React components that combine to render
+ * ARIA-compliant [menu](https://www.w3.org/WAI/ARIA/apg/patterns/menu/) and
+ * [menu button](https://www.w3.org/WAI/ARIA/apg/patterns/menubutton/) patterns.
+ *
+ * `Menu` itself is a wrapper component and context provider.
+ * It is responsible for managing the state of the menu and its items, and for
+ * rendering the `Menu.TriggerButton` (or the `Menu.SubmenuTriggerItem`)
+ * component, and the `Menu.Popover` component.
+ */
```

Did we mean to add this comment here? Isn't adding it to the connected version enough?

#### ciampo on 2025-01-09T14:54:17Z

URL: https://github.com/WordPress/gutenberg/pull/68249#discussion_r1908955503
Path: packages/components/src/menu/index.tsx

```diff
@@ -13,21 +13,31 @@ import { isRTL as isRTLFn } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { useContextSystem, contextConnectWithoutRef } from '../context';
-import type { MenuContext as MenuContextType, MenuProps } from './types';
-import { MenuContext } from './context';
-import { MenuItem } from './item';
-import { MenuCheckboxItem } from './checkbox-item';
-import { MenuRadioItem } from './radio-item';
-import { MenuGroup } from './group';
-import { MenuGroupLabel } from './group-label';
-import { MenuSeparator } from './separator';
-import { MenuItemLabel } from './item-label';
-import { MenuItemHelpText } from './item-help-text';
-import { MenuTriggerButton } from './trigger-button';
-import { MenuSubmenuTriggerItem } from './submenu-trigger-item';
-import { MenuPopover } from './popover';
+import type { ContextProps, Props } from './types';
+import { Context } from './context';
+import { Item } from './item';
+import { CheckboxItem } from './checkbox-item';
+import { RadioItem } from './radio-item';
+import { Group } from './group';
+import { GroupLabel } from './group-label';
+import { Separator } from './separator';
+import { ItemLabel } from './item-label';
+import { ItemHelpText } from './item-help-text';
+import { TriggerButton } from './trigger-button';
+import { SubmenuTriggerItem } from './submenu-trigger-item';
+import { Popover } from './popover';
 
-const UnconnectedMenu = ( props: MenuProps ) => {
+/**
+ * Menu is a collection of React components that combine to render
+ * ARIA-compliant [menu](https://www.w3.org/WAI/ARIA/apg/patterns/menu/) and
+ * [menu button](https://www.w3.org/WAI/ARIA/apg/patterns/menubutton/) patterns.
+ *
+ * `Menu` itself is a wrapper component and context provider.
+ * It is responsible for managing the state of the menu and its items, and for
+ * rendering the `Menu.TriggerButton` (or the `Menu.SubmenuTriggerItem`)
+ * component, and the `Menu.Popover` component.
+ */
```

Well spotted, probably a remnant of my various tests while refactoring the component. I removed it in the latest commit.

## Changed files

- packages/components/src/menu/README.md (MODIFIED, +421/-174)
- packages/components/src/menu/checkbox-item.tsx (MODIFIED, +12/-12)
- packages/components/src/menu/context.tsx (MODIFIED, +2/-4)
- packages/components/src/menu/docs-manifest.json (ADDED, +62/-0)
- packages/components/src/menu/group-label.tsx (MODIFIED, +7/-7)
- packages/components/src/menu/group.tsx (MODIFIED, +7/-11)
- packages/components/src/menu/index.tsx (MODIFIED, +101/-31)
- packages/components/src/menu/item-help-text.tsx (MODIFIED, +5/-7)
- packages/components/src/menu/item-label.tsx (MODIFIED, +5/-7)
- packages/components/src/menu/item.tsx (MODIFIED, +12/-12)
- packages/components/src/menu/popover.tsx (MODIFIED, +9/-9)
- packages/components/src/menu/radio-item.tsx (MODIFIED, +12/-12)
- packages/components/src/menu/separator.tsx (MODIFIED, +7/-7)
- packages/components/src/menu/stories/best-practices.mdx (ADDED, +38/-0)
- packages/components/src/menu/stories/index.story.tsx (MODIFIED, +8/-8)
- packages/components/src/menu/styles.ts (MODIFIED, +24/-24)
- packages/components/src/menu/submenu-trigger-item.tsx (MODIFIED, +9/-9)
- packages/components/src/menu/trigger-button.tsx (MODIFIED, +6/-6)
- packages/components/src/menu/types.ts (MODIFIED, +10/-10)
- storybook/manager-head.html (MODIFIED, +1/-0)
