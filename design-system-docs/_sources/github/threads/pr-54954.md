---
number: 54954
type: pull_request
state: MERGED
merged_at: 2023-10-07T12:56:17Z
url: https://github.com/WordPress/gutenberg/pull/54954
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #54954: ConfirmDialog: ts unit test storybook

- URL: https://github.com/WordPress/gutenberg/pull/54954
- Author: margolisj
- Created: 2023-09-30T22:35:03Z
- Updated: 2023-10-07T12:56:42Z
- Comments: 2 of 2
- Review threads: 6 of 6
- Changed files: 6 of 6
- Diff: ../pr-diffs/54954.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->
Upgrades ConfirmDialog to TS.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
Typescript is the only sane was to maintain a JS codebase this large.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

## Issue comments

### margolisj on 2023-09-30T22:36:04Z

URL: https://github.com/WordPress/gutenberg/pull/54954#issuecomment-1741873874

@mirka @ciampo https://github.com/WordPress/gutenberg/issues/35744. Like always, lmk if there are any other changes necessary. Was working on this a week ago or so. Getting the following:
`JSX element type 'ConfirmDialog' does not have any construct or call signatures.`

I don't see what wrong exactly. Tried a few type combination.

### ciampo on 2023-10-07T12:56:13Z

URL: https://github.com/WordPress/gutenberg/pull/54954#issuecomment-1751706033

The failing e2e test doesn't seem related to the changes in this PR, will merge nonetheless.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2023-10-02T08:35:38Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342416209
Path: packages/components/src/confirm-dialog/component.tsx

For better consistency, let's remove the React type imports, and instead use the `React` global — ie. `React.ForwardedRef`, `React.KeyboardEvent`

#### margolisj on 2023-10-02T15:09:20Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342817703
Path: packages/components/src/confirm-dialog/component.tsx

Done.

### Review thread 2 (resolved)

#### ciampo on 2023-10-02T08:37:23Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342417897
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -23,10 +23,10 @@ import { VStack } from '../v-stack';
 import * as styles from './styles';
 import { useCx } from '../utils/hooks/use-cx';
 
-function ConfirmDialog(
-	props: WordPressComponentProps< OwnProps, 'div', false >,
+const ConfirmDialog = (
```

For consistency, let's call this unexported component `UnconnectedConfirmDialog` and the exported component `ConfirmDialog`

```diff
diff --git a/packages/components/src/confirm-dialog/component.tsx b/packages/components/src/confirm-dialog/component.tsx
index 7f3b62fe33..dbe5bf5f90 100644
--- a/packages/components/src/confirm-dialog/component.tsx
+++ b/packages/components/src/confirm-dialog/component.tsx
@@ -23,9 +23,9 @@ import { VStack } from '../v-stack';
 import * as styles from './styles';
 import { useCx } from '../utils/hooks/use-cx';
 
-const ConfirmDialog = (
-	props: WordPressComponentProps< OwnProps, 'div' >,
-	forwardedRef: ForwardedRef< any >
+const UnconnectedConfirmDialog = (
+	props: WordPressComponentProps< OwnProps, 'div', false >,
+	forwardedRef: React.ForwardedRef< any >
 ) => {
 	const {
 		isOpen: isOpenProp,
@@ -122,5 +122,9 @@ const ConfirmDialog = (
 	);
 };
 
-const ConnectedConfirmDialog = contextConnect( ConfirmDialog, 'ConfirmDialog' );
-export default ConnectedConfirmDialog;
+const ConfirmDialog = contextConnect(
+	UnconnectedConfirmDialog,
+	'ConfirmDialog'
+);
+
+export default ConfirmDialog;

```

#### margolisj on 2023-10-02T14:44:04Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342787388
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -23,10 +23,10 @@ import { VStack } from '../v-stack';
 import * as styles from './styles';
 import { useCx } from '../utils/hooks/use-cx';
 
-function ConfirmDialog(
-	props: WordPressComponentProps< OwnProps, 'div', false >,
+const ConfirmDialog = (
```

Done.

### Review thread 3 (resolved)

#### ciampo on 2023-10-02T08:37:57Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342418408
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -23,10 +23,10 @@ import { VStack } from '../v-stack';
 import * as styles from './styles';
 import { useCx } from '../utils/hooks/use-cx';
 
-function ConfirmDialog(
-	props: WordPressComponentProps< OwnProps, 'div', false >,
```

What is the reason for removing the `false` argument ?

#### margolisj on 2023-10-02T14:41:22Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342784207
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -23,10 +23,10 @@ import { VStack } from '../v-stack';
 import * as styles from './styles';
 import { useCx } from '../utils/hooks/use-cx';
 
-function ConfirmDialog(
-	props: WordPressComponentProps< OwnProps, 'div', false >,
```

Like I said was just messing around with different combinations to see if I could get the connectedProps to drop out correctly. Reverted.

### Review thread 4 (resolved)

#### margolisj on 2023-10-02T15:21:35Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1342831372
Path: packages/components/src/confirm-dialog/test/index.tsx

```diff
@@ -142,6 +142,17 @@ describe( 'Confirm', () => {
 
 				const confirmDialog = screen.getByRole( 'dialog' );
 
+				// TODO: Is there a better way you'd like this checked?
```

@ciampo Lmk how you'd prefer this null check. Looks like there are a few eslint rules for this.

#### ciampo on 2023-10-03T10:27:08Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1343876498
Path: packages/components/src/confirm-dialog/test/index.tsx

```diff
@@ -142,6 +142,17 @@ describe( 'Confirm', () => {
 
 				const confirmDialog = screen.getByRole( 'dialog' );
 
+				// TODO: Is there a better way you'd like this checked?
```

Probably a quicker way would be to force TypeScript to consider `parentElement` non-null by adding a `!` at the end of the variable

`await user.click( confirmDialog.parentElement! );`

This way, we can keep the amount of changes in the file down to a minimum. The test will fail anyway in case `parentElement` is `null`, so I don't see issues with the approach

#### margolisj on 2023-10-03T13:58:49Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1344155518
Path: packages/components/src/confirm-dialog/test/index.tsx

```diff
@@ -142,6 +142,17 @@ describe( 'Confirm', () => {
 
 				const confirmDialog = screen.getByRole( 'dialog' );
 
+				// TODO: Is there a better way you'd like this checked?
```

Done.

### Review thread 5

#### ciampo on 2023-10-04T08:11:33Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1345385530
Path: packages/components/src/confirm-dialog/stories/index.story.tsx

The Storybook examples are still not displaying correctly all of the inferred type information — we need to:

- update the `meta` object, adding the correct configuration for generating controls and removing the hardcoded ones
- export the `ConfirmDialog` component as a named export in `component.tsx` and use the name export in Storybook (there is abug that prevents storybook from generating controls from default exports)

In the process, we can also:
- tweak the `args` configuration, so that it follows the same way we've been configuring it in other stories
- tweak the code so that it doesn't need to import types explicitly

<details>

<summary>Here is a diff with all those changes applied</summary>

```diff
diff --git a/packages/components/src/confirm-dialog/component.tsx b/packages/components/src/confirm-dialog/component.tsx
index ff46d77c05..750e7030de 100644
--- a/packages/components/src/confirm-dialog/component.tsx
+++ b/packages/components/src/confirm-dialog/component.tsx
@@ -184,8 +184,8 @@ const UnconnectedConfirmDialog = (
  * }
  * ```
  */
-const ConnectedConfirmDialog = contextConnect(
+export const ConfirmDialog = contextConnect(
 	UnconnectedConfirmDialog,
 	'ConfirmDialog'
 );
-export default ConnectedConfirmDialog;
+export default ConfirmDialog;
diff --git a/packages/components/src/confirm-dialog/stories/index.story.tsx b/packages/components/src/confirm-dialog/stories/index.story.tsx
index 3eb72b2829..85636c0ddc 100644
--- a/packages/components/src/confirm-dialog/stories/index.story.tsx
+++ b/packages/components/src/confirm-dialog/stories/index.story.tsx
@@ -12,32 +12,21 @@ import { useState } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../../button';
-import { ConfirmDialog } from '..';
-import type { ConfirmDialogProps, DialogInputEvent } from '../types';
+import { ConfirmDialog } from '../component';
 
 const meta: Meta< typeof ConfirmDialog > = {
 	component: ConfirmDialog,
 	title: 'Components (Experimental)/ConfirmDialog',
 	argTypes: {
-		children: {
-			control: { type: 'text' },
-		},
-		confirmButtonText: {
-			control: { type: 'text' },
-		},
-		cancelButtonText: {
-			control: { type: 'text' },
-		},
 		isOpen: {
 			control: { type: null },
 		},
-		onConfirm: { action: 'onConfirm' },
-		onCancel: { action: 'onCancel' },
-	},
-	args: {
-		children: 'Would you like to privately publish the post now?',
 	},
 	parameters: {
+		actions: { argTypesRegex: '^on.*' },
+		controls: {
+			expanded: true,
+		},
 		docs: { canvas: { sourceState: 'shown' } },
 	},
 };
@@ -48,15 +37,15 @@ const Template: StoryFn< typeof ConfirmDialog > = ( {
 	onConfirm,
 	onCancel,
 	...args
-}: ConfirmDialogProps ) => {
+} ) => {
 	const [ isOpen, setIsOpen ] = useState( false );
 
-	const handleConfirm = ( confirmArgs: DialogInputEvent ) => {
+	const handleConfirm: typeof onConfirm = ( confirmArgs ) => {
 		onConfirm( confirmArgs );
 		setIsOpen( false );
 	};
 
-	const handleCancel = ( cancelArgs: DialogInputEvent ) => {
+	const handleCancel: typeof onCancel = ( cancelArgs ) => {
 		onCancel?.( cancelArgs );
 		setIsOpen( false );
 	};
@@ -80,7 +69,7 @@ const Template: StoryFn< typeof ConfirmDialog > = ( {
 };
 
 // Simplest usage: just declare the component with the required `onConfirm` prop. Note: the `onCancel` prop is optional here, unless you'd like to render the component in Controlled mode (see below)
-export const _default = Template.bind( {} );
+export const Default = Template.bind( {} );
 const _defaultSnippet = `() => {
   const [ isOpen, setIsOpen ] = useState( false );
   const [ confirmVal, setConfirmVal ] = useState('');
@@ -113,8 +102,10 @@ const _defaultSnippet = `() => {
     </>
   );
 };`;
-_default.args = {};
-_default.parameters = {
+Default.args = {
+	children: 'Would you like to privately publish the post now?',
+};
+Default.parameters = {
 	docs: {
 		source: {
 			code: _defaultSnippet,
@@ -127,6 +118,7 @@ _default.parameters = {
 // To customize button text, pass the `cancelButtonText` and/or `confirmButtonText` props.
 export const WithCustomButtonLabels = Template.bind( {} );
 WithCustomButtonLabels.args = {
+	...Default.args,
 	cancelButtonText: 'No thanks',
 	confirmButtonText: 'Yes please!',
 };

```
</details>

#### margolisj on 2023-10-05T15:32:50Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1347619416
Path: packages/components/src/confirm-dialog/stories/index.story.tsx

Hopefully I got that copied right. Really should just look up how to apply a git patch haha. Lmk if there's anything else.

#### ciampo on 2023-10-05T20:27:50Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1347933322
Path: packages/components/src/confirm-dialog/stories/index.story.tsx

After copying the contents of the patch, on MacOS I can do that by executing `pbpaste | git apply`

#### ciampo on 2023-10-05T20:30:01Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1347935268
Path: packages/components/src/confirm-dialog/stories/index.story.tsx

> Hopefully I got that copied right

[Almost](https://github.com/WordPress/gutenberg/pull/54954/files#r1347934900)

### Review thread 6 (resolved)

#### ciampo on 2023-10-05T20:29:35Z

URL: https://github.com/WordPress/gutenberg/pull/54954#discussion_r1347934900
Path: packages/components/src/confirm-dialog/stories/index.story.tsx

```diff
@@ -7,47 +12,44 @@ import { useState } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../../button';
-import { ConfirmDialog } from '..';
+import { ConfirmDialog } from '../component';
 
-const meta = {
+const meta: Meta< typeof ConfirmDialog > = {
 	component: ConfirmDialog,
 	title: 'Components (Experimental)/ConfirmDialog',
 	argTypes: {
-		children: {
-			control: { type: 'text' },
-		},
-		confirmButtonText: {
-			control: { type: 'text' },
-		},
-		cancelButtonText: {
-			control: { type: 'text' },
-		},
 		isOpen: {
 			control: { type: null },
 		},
-		onConfirm: { action: 'onConfirm' },
-		onCancel: { action: 'onCancel' },
 	},
 	args: {
 		children: 'Would you like to privately publish the post now?',
 	},
```

This can be deleted

```suggestion
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/confirm-dialog/README.md (MODIFIED, +1/-1)
- packages/components/src/confirm-dialog/component.tsx (MODIFIED, +79/-13)
- packages/components/src/confirm-dialog/stories/index.story.tsx (RENAMED, +26/-24)
- packages/components/src/confirm-dialog/test/index.tsx (RENAMED, +3/-3)
- packages/components/src/confirm-dialog/types.ts (MODIFIED, +32/-12)
