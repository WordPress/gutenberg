---
number: 65720
type: pull_request
state: MERGED
merged_at: 2024-10-03T11:32:09Z
url: https://github.com/WordPress/gutenberg/pull/65720
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components","Backported to WP Core"]
---

# PR #65720: Composite: make items tabbable if active element gets removed

- URL: https://github.com/WordPress/gutenberg/pull/65720
- Author: ciampo
- Created: 2024-09-28T20:17:40Z
- Updated: 2024-10-08T16:16:33Z
- Comments: 8 of 8
- Review threads: 10 of 10
- Changed files: 3 of 3
- Diff: ../pr-diffs/65720.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

May fix https://github.com/WordPress/gutenberg/issues/65283

This PR tweaks the behavior of the `Composite` component so that the composite widget can continue to receive keyboard focus even when its active ID doesn't correspond to an element rendered in the DOM.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

The fact that `Composite` can end up in a situation where the whole widget is not focusable is bad for assistive technology, and we should try to fix it.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Following [a suggestion](https://github.com/ariakit/ariakit/issues/4129) from the Ariakit maintainers, this PR makes all composite items tabbable if the element associated with the active ID is not connected to the store.

In this case, when the focus is moved back to the composite widget, the items will be able to receive focus. As soon as one item receives focus, all composite items will stop being tabbable and revert back to the standard behavior of a composite widget.

## Review suggestions

Review commit-by-commit. The actual fix is applied in the first commit. The rest of the commits are accessories to the main fix.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

In Storybook:
- Load the "Remove active item" example
- Focus the composite widget, and with the arrow keys move focus on the third item
- Tab to the "Toggle third item button" and click it
- Tab back to the composite widget: the second item should receive focus and become active
- Moving arrow keys should work as expected in the composite widget

<details>

<summary>Edit: it looks like the UI has changed and it's impossible to reproduce the bug in the editor as of now</summary>

In the editor:
- Follow the testing instructions indicated in https://github.com/WordPress/gutenberg/issues/65283
- When clicking the "randomize colors" button, tab back to the color palette.
- The last item in the palette should receive keyboard focus
- Moving arrow keys should work as expected in the composite widget

</details>

## Screenshots or screencast <!-- if applicable -->

| Before (trunk) | After (this PR) |
|---|---|
| <video src="https://github.com/user-attachments/assets/d7535e1c-d70e-4ee2-a7e3-bbdadeec1e32" /> | <video src="https://github.com/user-attachments/assets/d91a8172-e2d8-460f-967d-cc9b65bff417" /> |

## Issue comments

### github-actions on 2024-09-30T14:47:05Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2383419563

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: diegohaz <hazdiego@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: afercia <afercia@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-09-30T15:02:57Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2383463508

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 7de8ae65388b8bdc305e7c631b4bef63410e8fa1.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/11160667834
📝  Reported issues:
- #38832 in `/test/e2e/specs/widgets/customizing-widgets.spec.js`

### ciampo on 2024-10-02T15:38:18Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2388997345

Rebased to include changes from https://github.com/WordPress/gutenberg/pull/65823 and https://github.com/WordPress/gutenberg/pull/65821 (which were merged in the meantime), and addressed other pending feedback

@tyxla this is ready for another round of review

### ciampo on 2024-10-03T11:04:05Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2391130413

I'll enable backporting once https://github.com/WordPress/gutenberg/pull/65853 gets merged

### github-actions on 2024-10-03T14:20:25Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2391548753

I just cherry-picked this PR to the wp/6.7 branch to get it included in the next release: 54ee0ee073bf3ce9e59aeb597eca9a2265669ba3

### diegohaz on 2024-10-08T09:13:23Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2399302185

@ciampo Thanks for trying this. Is there anything noteworthy about this change that we should know before implementing it on Ariakit? Have you encountered any issues?

### ciampo on 2024-10-08T11:32:30Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2399594009

I haven't encountered issues myself, not have received any reports — @afercia , do you have any feedback?

### afercia on 2024-10-08T12:32:13Z

URL: https://github.com/WordPress/gutenberg/pull/65720#issuecomment-2399718132

@ciampo @diegohaz I haven't tested the progress on this fix since I reported it iin https://github.com/WordPress/gutenberg/issues/65283

Do I get it correctly that the fix is:
- When there's no element referenced by the activeId, all the items become focusable.
- As soon as one of the items receives focus, the original behavior is restored with only one item tabbable (tabindex=0) and the other ones not tabbable (tabindex=-1). Arrows navigation will work as expected.

If that's correct, it seems to me it's a reasonable and effective fix.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2024-09-30T14:42:11Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781276323
Path: packages/components/src/composite/item.tsx

```diff
@@ -20,9 +20,24 @@ export const CompositeItem = forwardRef<
 	WordPressComponentProps< CompositeItemProps, 'button', false >
 >( function CompositeItem( props, ref ) {
 	const context = useCompositeContext();
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
+
+	// If the active item is not connected, Composite may end up in a state
+	// where none of the items are tabbable. In this case, we force all items to
+	// be tabbable, so that as soon as an item received focus, it becomes active
+	// and Composite goes back to working as expected.
+	const tabbable = Ariakit.useStoreState( store, ( state ) => {
+		return ! store?.item( state?.activeId )?.element?.isConnected;
+	} );
+
```

This is the actual fix

#### diegohaz on 2024-09-30T20:16:10Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781685436
Path: packages/components/src/composite/item.tsx

```diff
@@ -20,9 +20,24 @@ export const CompositeItem = forwardRef<
 	WordPressComponentProps< CompositeItemProps, 'button', false >
 >( function CompositeItem( props, ref ) {
 	const context = useCompositeContext();
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
+
+	// If the active item is not connected, Composite may end up in a state
+	// where none of the items are tabbable. In this case, we force all items to
+	// be tabbable, so that as soon as an item received focus, it becomes active
+	// and Composite goes back to working as expected.
+	const tabbable = Ariakit.useStoreState( store, ( state ) => {
+		return ! store?.item( state?.activeId )?.element?.isConnected;
+	} );
+
```

Just a heads up: it's probably better to check if `activeId` is not `null` beforehand. See https://github.com/ariakit/ariakit/issues/4129#issuecomment-2367928139

#### ciampo on 2024-09-30T21:44:47Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781775261
Path: packages/components/src/composite/item.tsx

```diff
@@ -20,9 +20,24 @@ export const CompositeItem = forwardRef<
 	WordPressComponentProps< CompositeItemProps, 'button', false >
 >( function CompositeItem( props, ref ) {
 	const context = useCompositeContext();
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
+
+	// If the active item is not connected, Composite may end up in a state
+	// where none of the items are tabbable. In this case, we force all items to
+	// be tabbable, so that as soon as an item received focus, it becomes active
+	// and Composite goes back to working as expected.
+	const tabbable = Ariakit.useStoreState( store, ( state ) => {
+		return ! store?.item( state?.activeId )?.element?.isConnected;
+	} );
+
```

Thank you, updated!

### Review thread 2 (resolved)

#### ciampo on 2024-09-30T14:43:25Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781278199
Path: packages/components/src/composite/stories/index.story.tsx

I'll remove this story before merging

### Review thread 3 (resolved)

#### ciampo on 2024-09-30T14:44:50Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781280427
Path: packages/components/src/composite/legacy/test/index.tsx

While reviewing some unit test failures, I realised that `renderAndValidate` is an async function but is never `await`ed. If folks think this change is worth applying, I'll do so in a separate PR.

#### ciampo on 2024-10-02T11:12:09Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784317930
Path: packages/components/src/composite/legacy/test/index.tsx

https://github.com/WordPress/gutenberg/pull/65823

### Review thread 4 (resolved)

#### ciampo on 2024-09-30T14:46:34Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781283496
Path: packages/components/src/composite/group.tsx

```diff
@@ -20,11 +20,11 @@ export const CompositeGroup = forwardRef<
 	WordPressComponentProps< CompositeGroupProps, 'div', false >
 >( function CompositeGroup( props, ref ) {
 	const context = useCompositeContext();
-	return (
-		<Ariakit.CompositeGroup
-			store={ context.store as Ariakit.CompositeStore }
-			{ ...props }
-			ref={ ref }
-		/>
-	);
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
```

Reading the store passed via `props` is a fix to how the legacy compat layer works. I believe that this is a bug that's been around since we wrote the legacy compat layer, but it was surfaced by the changes in this PR. If folks agree with it, I'll apply it in a separate PR.

#### ciampo on 2024-10-02T11:04:59Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784310958
Path: packages/components/src/composite/group.tsx

```diff
@@ -20,11 +20,11 @@ export const CompositeGroup = forwardRef<
 	WordPressComponentProps< CompositeGroupProps, 'div', false >
 >( function CompositeGroup( props, ref ) {
 	const context = useCompositeContext();
-	return (
-		<Ariakit.CompositeGroup
-			store={ context.store as Ariakit.CompositeStore }
-			{ ...props }
-			ref={ ref }
-		/>
-	);
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
```

https://github.com/WordPress/gutenberg/pull/65821

### Review thread 5 (resolved)

#### ciampo on 2024-09-30T15:00:41Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1781306954
Path: packages/components/src/composite/test/index.tsx

We should probably add a suite of tests similar to the legacy version. I can do that in a follow-up PR

#### tyxla on 2024-10-03T09:44:29Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1785936307
Path: packages/components/src/composite/test/index.tsx

Sounds like a plan

#### ciampo on 2024-10-08T16:16:33Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1792167352
Path: packages/components/src/composite/test/index.tsx

https://github.com/WordPress/gutenberg/pull/65952

### Review thread 6 (resolved)

#### tyxla on 2024-10-02T12:01:46Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784370396
Path: packages/components/src/composite/item.tsx

```diff
@@ -20,9 +20,27 @@ export const CompositeItem = forwardRef<
 	WordPressComponentProps< CompositeItemProps, 'button', false >
 >( function CompositeItem( props, ref ) {
 	const context = useCompositeContext();
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
+
+	// If the active item is not connected, Composite may end up in a state
+	// where none of the items are tabbable. In this case, we force all items to
+	// be tabbable, so that as soon as an item received focus, it becomes active
+	// and Composite goes back to working as expected.
+	const tabbable = Ariakit.useStoreState( store, ( state ) => {
+		return (
+			state?.activeId !== null &&
+			! store?.item( state?.activeId )?.element?.isConnected
+		);
+	} );
```

Good catch 👍 

I wonder if this is something that Ariakit should resolve internally?

#### ciampo on 2024-10-02T12:50:21Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784472935
Path: packages/components/src/composite/item.tsx

```diff
@@ -20,9 +20,27 @@ export const CompositeItem = forwardRef<
 	WordPressComponentProps< CompositeItemProps, 'button', false >
 >( function CompositeItem( props, ref ) {
 	const context = useCompositeContext();
+
+	// @ts-expect-error The store prop in undocumented and only used by the
+	// legacy compat layer.
+	const storeViaProps = props.store as Ariakit.CompositeStore;
+	const store = storeViaProps ?? ( context.store as Ariakit.CompositeStore );
+
+	// If the active item is not connected, Composite may end up in a state
+	// where none of the items are tabbable. In this case, we force all items to
+	// be tabbable, so that as soon as an item received focus, it becomes active
+	// and Composite goes back to working as expected.
+	const tabbable = Ariakit.useStoreState( store, ( state ) => {
+		return (
+			state?.activeId !== null &&
+			! store?.item( state?.activeId )?.element?.isConnected
+		);
+	} );
```

I already discussed it in https://github.com/ariakit/ariakit/issues/4129. I think Ariakit maintainers are cautious about implementing such a fix directly in the library, and instead asked if we wanted to try the fix out in userland first

### Review thread 7 (resolved)

#### tyxla on 2024-10-02T12:03:08Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784371878
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +353,28 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+export const RemoveActiveItem: StoryObj< typeof Composite > = {
```

Was going to say this story doesn't make much sense to keep, but I see you created it solely for testing and plan to remove it - thanks!

### Review thread 8 (resolved)

#### tyxla on 2024-10-02T12:19:40Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784411227
Path: packages/components/src/composite/test/index.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import { queryByAttribute, render, screen } from '@testing-library/react';
+import { click, press, waitFor } from '@ariakit/test';
+import type { ComponentProps } from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Composite } from '..';
+
+// This is necessary because of how Ariakit calculates page up and
+// page down. Without this, nothing has a height, and so paging up
+// and down doesn't behave as expected in tests.
```

Ah, jsdom strikes again. Makes you wonder if this kind of tests isn't better suited for e2e. I guess not, because we're not really testing anything related to calculating the height.

#### ciampo on 2024-10-02T12:51:25Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784474297
Path: packages/components/src/composite/test/index.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import { queryByAttribute, render, screen } from '@testing-library/react';
+import { click, press, waitFor } from '@ariakit/test';
+import type { ComponentProps } from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Composite } from '..';
+
+// This is necessary because of how Ariakit calculates page up and
+// page down. Without this, nothing has a height, and so paging up
+// and down doesn't behave as expected in tests.
```

Not singularly for this test, but in general we way need to consider this. The closer our environment is to the real browser, the more confidence we get from the tests

### Review thread 9 (resolved)

#### tyxla on 2024-10-02T12:22:29Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784414711
Path: packages/components/src/composite/test/index.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import { queryByAttribute, render, screen } from '@testing-library/react';
+import { click, press, waitFor } from '@ariakit/test';
+import type { ComponentProps } from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Composite } from '..';
+
+// This is necessary because of how Ariakit calculates page up and
+// page down. Without this, nothing has a height, and so paging up
+// and down doesn't behave as expected in tests.
+
+let clientHeightSpy: jest.SpiedGetter<
+	typeof HTMLElement.prototype.clientHeight
+>;
+
+beforeAll( () => {
+	clientHeightSpy = jest
+		.spyOn( HTMLElement.prototype, 'clientHeight', 'get' )
+		.mockImplementation( function getClientHeight( this: HTMLElement ) {
+			if ( this.tagName === 'BODY' ) {
+				return window.outerHeight;
+			}
+			return 50;
+		} );
+} );
+
+afterAll( () => {
+	clientHeightSpy?.mockRestore();
+} );
+
+async function renderAndValidate( ...args: Parameters< typeof render > ) {
+	const view = render( ...args );
+	await waitFor( () => {
+		const activeButton = queryByAttribute(
+			'data-active-item',
+			view.baseElement,
+			'true'
+		);
+		expect( activeButton ).not.toBeNull();
+	} );
+	return view;
+}
+
+function RemoveItemTest( props: ComponentProps< typeof Composite > ) {
+	const [ showThirdItem, setShowThirdItem ] = useState( true );
+	return (
+		<>
+			<button>Focus trap before composite</button>
+			<Composite { ...props }>
+				<Composite.Item>Item 1</Composite.Item>
+				<Composite.Item>Item 2</Composite.Item>
+				{ showThirdItem && <Composite.Item>Item 3</Composite.Item> }
+			</Composite>
+			<button onClick={ () => setShowThirdItem( ( value ) => ! value ) }>
+				Toggle third item
+			</button>
+		</>
+	);
+}
+
+describe( 'Composite', () => {
+	it( 'should remain focussable even when there is no elements in the DOM associated with the currently active ID', async () => {
```

Nit: let's use the US English version like for anything else we do in code:

```suggestion
	it( 'should remain focusable even when there is no elements in the DOM associated with the currently active ID', async () => {
```

#### ciampo on 2024-10-02T12:51:53Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784474896
Path: packages/components/src/composite/test/index.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import { queryByAttribute, render, screen } from '@testing-library/react';
+import { click, press, waitFor } from '@ariakit/test';
+import type { ComponentProps } from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Composite } from '..';
+
+// This is necessary because of how Ariakit calculates page up and
+// page down. Without this, nothing has a height, and so paging up
+// and down doesn't behave as expected in tests.
+
+let clientHeightSpy: jest.SpiedGetter<
+	typeof HTMLElement.prototype.clientHeight
+>;
+
+beforeAll( () => {
+	clientHeightSpy = jest
+		.spyOn( HTMLElement.prototype, 'clientHeight', 'get' )
+		.mockImplementation( function getClientHeight( this: HTMLElement ) {
+			if ( this.tagName === 'BODY' ) {
+				return window.outerHeight;
+			}
+			return 50;
+		} );
+} );
+
+afterAll( () => {
+	clientHeightSpy?.mockRestore();
+} );
+
+async function renderAndValidate( ...args: Parameters< typeof render > ) {
+	const view = render( ...args );
+	await waitFor( () => {
+		const activeButton = queryByAttribute(
+			'data-active-item',
+			view.baseElement,
+			'true'
+		);
+		expect( activeButton ).not.toBeNull();
+	} );
+	return view;
+}
+
+function RemoveItemTest( props: ComponentProps< typeof Composite > ) {
+	const [ showThirdItem, setShowThirdItem ] = useState( true );
+	return (
+		<>
+			<button>Focus trap before composite</button>
+			<Composite { ...props }>
+				<Composite.Item>Item 1</Composite.Item>
+				<Composite.Item>Item 2</Composite.Item>
+				{ showThirdItem && <Composite.Item>Item 3</Composite.Item> }
+			</Composite>
+			<button onClick={ () => setShowThirdItem( ( value ) => ! value ) }>
+				Toggle third item
+			</button>
+		</>
+	);
+}
+
+describe( 'Composite', () => {
+	it( 'should remain focussable even when there is no elements in the DOM associated with the currently active ID', async () => {
```

There's also a grammar error that I'll fix ("there IS no elements" -> "there ARE no elements")

### Review thread 10 (resolved)

#### tyxla on 2024-10-02T12:22:41Z

URL: https://github.com/WordPress/gutenberg/pull/65720#discussion_r1784414976
Path: packages/components/src/composite/test/index.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import { queryByAttribute, render, screen } from '@testing-library/react';
+import { click, press, waitFor } from '@ariakit/test';
+import type { ComponentProps } from 'react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Composite } from '..';
+
+// This is necessary because of how Ariakit calculates page up and
+// page down. Without this, nothing has a height, and so paging up
+// and down doesn't behave as expected in tests.
+
+let clientHeightSpy: jest.SpiedGetter<
+	typeof HTMLElement.prototype.clientHeight
+>;
+
+beforeAll( () => {
+	clientHeightSpy = jest
+		.spyOn( HTMLElement.prototype, 'clientHeight', 'get' )
+		.mockImplementation( function getClientHeight( this: HTMLElement ) {
+			if ( this.tagName === 'BODY' ) {
+				return window.outerHeight;
+			}
+			return 50;
+		} );
+} );
+
+afterAll( () => {
+	clientHeightSpy?.mockRestore();
+} );
+
+async function renderAndValidate( ...args: Parameters< typeof render > ) {
+	const view = render( ...args );
+	await waitFor( () => {
+		const activeButton = queryByAttribute(
+			'data-active-item',
+			view.baseElement,
+			'true'
+		);
+		expect( activeButton ).not.toBeNull();
+	} );
+	return view;
+}
+
+function RemoveItemTest( props: ComponentProps< typeof Composite > ) {
+	const [ showThirdItem, setShowThirdItem ] = useState( true );
+	return (
+		<>
+			<button>Focus trap before composite</button>
+			<Composite { ...props }>
+				<Composite.Item>Item 1</Composite.Item>
+				<Composite.Item>Item 2</Composite.Item>
+				{ showThirdItem && <Composite.Item>Item 3</Composite.Item> }
+			</Composite>
+			<button onClick={ () => setShowThirdItem( ( value ) => ! value ) }>
+				Toggle third item
+			</button>
+		</>
+	);
+}
+
+describe( 'Composite', () => {
```

Thanks for adding a comprehensive test 👍

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/composite/item.tsx (MODIFIED, +19/-1)
- packages/components/src/composite/test/index.tsx (ADDED, +123/-0)
