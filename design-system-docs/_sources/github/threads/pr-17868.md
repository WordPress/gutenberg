---
number: 17868
type: pull_request
state: MERGED
merged_at: 2019-10-13T20:48:03Z
url: https://github.com/WordPress/gutenberg/pull/17868
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components","Storybook"]
---

# PR #17868: Components: Add stories for Icon and IconButton

- URL: https://github.com/WordPress/gutenberg/pull/17868
- Author: ItsJonQ
- Created: 2019-10-09T15:44:16Z
- Updated: 2019-10-14T12:36:42Z
- Comments: 8 of 8
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/17868.diff

## Body

![Screen Shot 2019-10-09 at 11 31 03 AM](https://user-images.githubusercontent.com/2322354/66497172-14fca980-ea8a-11e9-924d-c52049b467fa.png)

This update adds some initial Storybook stories for the Icon and IconButton
component.

Most of the stories were derived from the component's README.md documentation.
Additional stories were added to showcase prop variation (e.g. `Icon` size),
as well as composition (e.g. `IconButton` grouping).

In order to render certain UI for the stories, some simple stateless
functional components were created in the `.stories.js` file.

## Issue comments

### youknowriad on 2019-10-10T07:44:57Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540441376

@epiqueras do we have a live deploy in netlify for these PRs? How can we get the URL?

### gziolo on 2019-10-10T10:01:59Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540496762

_Aside:_ it was discussed some time ago that it might make sense to deprecate `IconButton` or maybe leave it is an alias for the `Button` component with the icon passed as prop, see:
- https://github.com/WordPress/gutenberg/issues/7534
- https://github.com/WordPress/gutenberg/issues/16541

### ItsJonQ on 2019-10-10T13:16:12Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540574506

@gziolo Thanks for that context! In that case, should I remove the `IconButton` stories for now?

### gziolo on 2019-10-10T13:34:47Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540584845

> @gziolo Thanks for that context! In that case, should I remove the `IconButton` stories for now?

I guess it's fine to proceed. We will have to keep `IconButton` in WordPress core for very long even when we decide to merge it into `Button` :)

### ItsJonQ on 2019-10-10T13:37:47Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540586578

> We will have to keep IconButton in WordPress core for very long even when we decide to merge it into Button :)

😅

### epiqueras on 2019-10-10T15:05:54Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540631205

@youknowriad The link is in the PR checks, `https://deploy-preview-${PR_NUMBER}--gutenberg-playground.netlify.com`.

You'll need to manually add `/design-system/components` at the end, because there's no link to it yet from the playground.

I just updated the Netlify config to match the new order of build scripts though, so we'll need a new commit in this PR to trigger a new build.

### youknowriad on 2019-10-10T16:53:54Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540676859

@epiqueras https://deploy-preview-17868--gutenberg-playground.netlify.com/design-system/components This doesn't seem to work?

### epiqueras on 2019-10-10T17:46:35Z

URL: https://github.com/WordPress/gutenberg/pull/17868#issuecomment-540696688

@youknowriad 

>I just updated the Netlify config to match the new order of build scripts though, so we'll need a new commit in this PR to trigger a new build.

## Review threads

### Review thread 1 (resolved)

#### epiqueras on 2019-10-10T23:36:24Z

URL: https://github.com/WordPress/gutenberg/pull/17868#discussion_r333777763
Path: packages/components/src/icon/stories/index.js

```diff
@@ -0,0 +1,85 @@
+/**
+ * Internal dependencies
+ */
+import Icon from '../';
+import { SVG, Path } from '../../';
+
+export default { title: 'Icon', component: Icon };
+
+const IconSizeLabel = ( { size } ) => <div style={ { fontSize: 12 } }>{ size }px</div>;
+
+export const _default = () => (
+	<div>
+		<Icon icon="screenoptions" />
+		<IconSizeLabel size={ 24 } />
+	</div>
+);
+
+export const Sizes = () => {
```

I think the convention is to use `camelCase` for story names: https://storybook.js.org/docs/formats/component-story-format/.

I don't have a strong opinion, but we used that in the stories we already merged so it's just for consistency.

#### ItsJonQ on 2019-10-11T14:51:25Z

URL: https://github.com/WordPress/gutenberg/pull/17868#discussion_r334030990
Path: packages/components/src/icon/stories/index.js

```diff
@@ -0,0 +1,85 @@
+/**
+ * Internal dependencies
+ */
+import Icon from '../';
+import { SVG, Path } from '../../';
+
+export default { title: 'Icon', component: Icon };
+
+const IconSizeLabel = ( { size } ) => <div style={ { fontSize: 12 } }>{ size }px</div>;
+
+export const _default = () => (
+	<div>
+		<Icon icon="screenoptions" />
+		<IconSizeLabel size={ 24 } />
+	</div>
+);
+
+export const Sizes = () => {
```

@epiqueras Thank you!

### Review thread 2 (resolved)

#### epiqueras on 2019-10-10T23:37:29Z

URL: https://github.com/WordPress/gutenberg/pull/17868#discussion_r333778022
Path: packages/components/src/icon-button/stories/index.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * Internal dependencies
+ */
+import IconButton from '../';
+
+export default { title: 'IconButton', component: IconButton };
+
+export const _default = () => <IconButton icon="ellipsis" label="More" />;
+
+export const Grouped = () => {
+	const GroupContainer = ( props ) => (
+		<div style={ { display: 'inline-flex' } } { ...props } />
+	);
```

We could remove the props forwarding here to make things simpler.

#### ItsJonQ on 2019-10-11T14:51:59Z

URL: https://github.com/WordPress/gutenberg/pull/17868#discussion_r334031272
Path: packages/components/src/icon-button/stories/index.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * Internal dependencies
+ */
+import IconButton from '../';
+
+export default { title: 'IconButton', component: IconButton };
+
+export const _default = () => <IconButton icon="ellipsis" label="More" />;
+
+export const Grouped = () => {
+	const GroupContainer = ( props ) => (
+		<div style={ { display: 'inline-flex' } } { ...props } />
+	);
```

Yes! Can do. Thank you! <3

## Changed files

- packages/components/src/icon-button/stories/index.js (ADDED, +22/-0)
- packages/components/src/icon/stories/index.js (ADDED, +85/-0)
