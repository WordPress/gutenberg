---
number: 54290
type: pull_request
state: MERGED
merged_at: 2023-09-15T16:03:10Z
url: https://github.com/WordPress/gutenberg/pull/54290
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","has dev note"]
---

# PR #54290: Add non-listbox functionality back to `CircularOptionPicker`

- URL: https://github.com/WordPress/gutenberg/pull/54290
- Author: andrewhayward
- Created: 2023-09-08T09:19:47Z
- Updated: 2023-10-12T20:36:37Z
- Comments: 8 of 8
- Review threads: 17 of 17
- Changed files: 22 of 22
- Diff: ../pr-diffs/54290.diff

## Body

## What?
This PR puts back the ability to use `CircularOptionPicker` as buttons, which was removed in #52255, while maintaining the new (now default) `listbox` behaviour. (Closes #54239.)

## Why?
In #35292, `CircularOptionPicker` was refactored to present better semantics and keyboard navigation for picking options. However, as per #54239, it was discovered that `CircularOptionPicker` is not just used as an "option picker", but also as an access method for editing options. As such, we need to put the original functionality back in place, while maintaining the new functionality, so that the control can be used in either way.

## How?
A new `asButtons` flag has been added to `CircularOptionPicker`, allowing component consumers to decide how it should be rendered, depending on context. This is passed on by `ColorPalette`, `DuotonePicker` and `GradientPicker`, the higher-order components that expose it.

## Testing Instructions
Superficially, nothing should change, and `CircularOptionPicker` should continue to behave as before. Clicking each option should fire an `onClick` event, and in Storybook, the selected option should show as checked. Toggling `asButtons` shouldn't impact this behaviour.

### Testing Instructions for Keyboard
With a keyboard, the default behaviour should also continue as before. The `CircularOptionPicker` control should still present as a single tab stop, and arrow keys should navigate between the different options. Semantically, it should still present as a `listbox` with multiple `option`s, marked `aria-selected` as appropriate.

Enabling `asButtons` should change this, so that each option becomes its own tab stop. The `listbox` semantics should be removed, and each option should be a `button`, marked `aria-pressed` where needed.

## Issue comments

### alexstine on 2023-09-08T13:45:21Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1711700345

That's annoying that we have to maintain two versions.

### andrewhayward on 2023-09-08T13:53:43Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1711712107

> That's annoying that we have to maintain two versions.

Unfortunately, despite what `CircularOptionPicker` might be called, it was built in such a way that it could be used for purposes other than picking things. As such, it was used for purposes other than picking things. And here we are.

The joys of component libraries!

### ciampo on 2023-09-12T14:11:13Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1715805555

One more question:

> This is passed on by ColorPalette, DuotonePicker and GradientPicker, the higher-order components that expose it.

Currently, do `ColorPalette`, `DuotonePicker` and `GradientPicker` all need to expose this prop? Do they all render in scenarios which required them to sometimes be a listbox, sometimes a list of separate buttons?

My instinct is usually to add the least amount of public APIs necessary.

### andrewhayward on 2023-09-13T12:49:35Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1717567276

> One more question:
> 
> > This is passed on by ColorPalette, DuotonePicker and GradientPicker, the higher-order components that expose it.
> 
> Currently, do `ColorPalette`, `DuotonePicker` and `GradientPicker` all need to expose this prop? Do they all render in scenarios which required them to sometimes be a listbox, sometimes a list of separate buttons?
> 
> My instinct is usually to add the least amount of public APIs necessary.

`ColorPalette` and `GradientPicker` definitely do, in the theme style editor. I think `DuotonePicker` does too, in the same way, but that actually seems broken to me, having looked.

### andrewhayward on 2023-09-13T22:45:52Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1718409005

Definitely agreed with the `Omit` feedback. It's been a bit of a slog to refactor them out, but I think everything is happy now.

### andrewhayward on 2023-09-13T23:14:10Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1718429062

> I think `DuotonePicker` does too, in the same way, but that actually seems broken to me, having looked.

For whatever reason, `DuotonePicker` here is seemingly intentionally useless, with `onChange={ noop }`... not sure why!
https://github.com/WordPress/gutenberg/blob/fec5ffc78a55a40bfa45d161cde5fa5ec0f8cb7d/packages/edit-site/src/components/global-styles/gradients-palette-panel.js#L115-L121

### andrewhayward on 2023-09-14T09:05:21Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1719053975

Overrode the code sample for `WithLoopingDisabled` to make sure that `loop={false}` shows up. Would prefer to be able to override how `loop` is handled at the Story level so that the live sample works correctly too, but for now this will have to do.

### ciampo on 2023-10-12T20:36:37Z

URL: https://github.com/WordPress/gutenberg/pull/54290#issuecomment-1760331369

See https://github.com/WordPress/gutenberg/pull/52255 for the dev note

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2023-09-12T13:44:48Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323069848
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
```

It feels counter-intuitive to omit keys from `ListboxCircularOptionPickerProps` when describing the type for the props of the `ListboxCircularOptionPicker` component.

We should probably rewrite types in such a way that those extra props are simply not part of `ListboxCircularOptionPickerProps`.

Same goes for `ButtonsCircularOptionPickerProps`

#### andrewhayward on 2023-09-14T00:06:29Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1325189812
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
```

Refactored these out. They had to remain in `types.ts`, as it was the only way I could find to make TypeScript happy, but that fact is at least now not immediately apparent.

### Review thread 2 (resolved)

#### ciampo on 2023-09-12T13:46:33Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323072427
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
+) {
+	const { id, disableLooping, children, ...additionalProps } = props;
+	const baseId = useInstanceId( CircularOptionPicker, 'option-picker', id );
+	const rtl = isRTL();
+	const loop = ! disableLooping;
+
+	const compositeState = useCompositeState( { baseId, loop, rtl } );
+
+	// This is necessary as `useCompositeState` is sealed after
+	// the first render, so although unlikely to happen, if a state
+	// property should change, we need to process it accordingly.
+	useEffect( () => {
+		compositeState.setBaseId( baseId );
+		compositeState.setLoop( !! loop );
+		compositeState.setRTL( rtl );
+		// Disabling exhaustive-deps check because it expects
+		// `compositeState` to be present, but doing so causes
+		// an infinite loop.
+		// eslint-disable-next-line react-hooks/exhaustive-deps
+	}, [ baseId, loop, rtl ] );
```

Have you considered splitting this effect into three different effects? That way, we only `set` the prop the changes. Also, curious if that would also allow us to add `compositeState` back into the list of dependencies?

#### andrewhayward on 2023-09-13T10:05:58Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324285940
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
+) {
+	const { id, disableLooping, children, ...additionalProps } = props;
+	const baseId = useInstanceId( CircularOptionPicker, 'option-picker', id );
+	const rtl = isRTL();
+	const loop = ! disableLooping;
+
+	const compositeState = useCompositeState( { baseId, loop, rtl } );
+
+	// This is necessary as `useCompositeState` is sealed after
+	// the first render, so although unlikely to happen, if a state
+	// property should change, we need to process it accordingly.
+	useEffect( () => {
+		compositeState.setBaseId( baseId );
+		compositeState.setLoop( !! loop );
+		compositeState.setRTL( rtl );
+		// Disabling exhaustive-deps check because it expects
+		// `compositeState` to be present, but doing so causes
+		// an infinite loop.
+		// eslint-disable-next-line react-hooks/exhaustive-deps
+	}, [ baseId, loop, rtl ] );
```

I've split them up, but it still breaks adding `compositeState` as a dependency, unfortunately.

#### ciampo on 2023-09-14T17:49:06Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1326327323
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
+) {
+	const { id, disableLooping, children, ...additionalProps } = props;
+	const baseId = useInstanceId( CircularOptionPicker, 'option-picker', id );
+	const rtl = isRTL();
+	const loop = ! disableLooping;
+
+	const compositeState = useCompositeState( { baseId, loop, rtl } );
+
+	// This is necessary as `useCompositeState` is sealed after
+	// the first render, so although unlikely to happen, if a state
+	// property should change, we need to process it accordingly.
+	useEffect( () => {
+		compositeState.setBaseId( baseId );
+		compositeState.setLoop( !! loop );
+		compositeState.setRTL( rtl );
+		// Disabling exhaustive-deps check because it expects
+		// `compositeState` to be present, but doing so causes
+		// an infinite loop.
+		// eslint-disable-next-line react-hooks/exhaustive-deps
+	}, [ baseId, loop, rtl ] );
```

Have you tried to extract the setters from `compositeState` ? If they work like standard react state setters, they shouldn't mutate between renders, and therefore they shouldn't cause an infinite loop

something like:

```tsx
	const { setBaseId, setLoop, setRTL } = compositeState;

	// These are necessary as `useCompositeState` is sealed after
	// the first render, so although unlikely to happen, if a state
	// property should change, we need to process it accordingly.
	useEffect( () => {
		setBaseId( baseId );
	}, [ setBaseId, baseId ] );

	useEffect( () => {
		setLoop( loop );
	}, [ setLoop, loop ] );

	useEffect( () => {
		setRTL( rtl );
	}, [ setRTL, rtl ] );
```

Also, I believe that `loop` is guaranteed to be a `boolean`, so no need for the `!!` notation.

### Review thread 3 (resolved)

#### ciampo on 2023-09-12T13:48:35Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323075466
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
+) {
+	const { id, disableLooping, children, ...additionalProps } = props;
+	const baseId = useInstanceId( CircularOptionPicker, 'option-picker', id );
+	const rtl = isRTL();
+	const loop = ! disableLooping;
```

This feels a bit redundant, maybe we can use `disableLooping` directly in the code?

#### andrewhayward on 2023-09-13T09:54:01Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324272069
Path: packages/components/src/circular-option-picker/option-picker.tsx

```diff
@@ -0,0 +1,171 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useEffect } from '@wordpress/element';
+import { isRTL } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import { Composite, useCompositeState } from '../composite';
+import type {
+	CircularOptionPickerProps,
+	ListboxCircularOptionPickerProps,
+	ButtonsCircularOptionPickerProps,
+} from './types';
+import { Option } from './option-picker-option';
+import { OptionGroup } from './option-picker-option-group';
+import { ButtonAction, DropdownLinkAction } from './option-picker-actions';
+
+/**
+ *`CircularOptionPicker` is a component that displays a set of options as circular buttons.
+ *
+ * ```jsx
+ * import { CircularOptionPicker } from '../circular-option-picker';
+ * import { useState } from '@wordpress/element';
+ *
+ * const Example = () => {
+ * 	const [ currentColor, setCurrentColor ] = useState();
+ * 	const colors = [
+ * 		{ color: '#f00', name: 'Red' },
+ * 		{ color: '#0f0', name: 'Green' },
+ * 		{ color: '#00f', name: 'Blue' },
+ * 	];
+ * 	const colorOptions = (
+ * 		<>
+ * 			{ colors.map( ( { color, name }, index ) => {
+ * 				return (
+ * 					<CircularOptionPicker.Option
+ * 						key={ `${ color }-${ index }` }
+ * 						tooltipText={ name }
+ * 						style={ { backgroundColor: color, color } }
+ * 						isSelected={ index === currentColor }
+ * 						onClick={ () => setCurrentColor( index ) }
+ * 						aria-label={ name }
+ * 					/>
+ * 				);
+ * 			} ) }
+ * 		</>
+ * 	);
+ * 	return (
+ * 		<CircularOptionPicker
+ * 				options={ colorOptions }
+ * 				actions={
+ * 					<CircularOptionPicker.ButtonAction
+ * 						onClick={ () => setCurrentColor( undefined ) }
+ * 					>
+ * 						{ 'Clear' }
+ * 					</CircularOptionPicker.ButtonAction>
+ * 				}
+ * 			/>
+ * 	);
+ * };
+ * ```
+ */
+
+function ListboxCircularOptionPicker(
+	props: Omit<
+		ListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>
+) {
+	const { id, disableLooping, children, ...additionalProps } = props;
+	const baseId = useInstanceId( CircularOptionPicker, 'option-picker', id );
+	const rtl = isRTL();
+	const loop = ! disableLooping;
```

We can – I don't have a strong feeling either way. `loop` is used in a couple of different places, including as a shorthand property assignment, so I went with defining it here rather than using `! disableLooping` in multiple locations. But happy to do so if that's preferred.

### Review thread 4 (resolved)

#### ciampo on 2023-09-12T13:52:45Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323081580
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -41,21 +41,40 @@ export type CircularOptionPickerProps = {
 	 */
 	children?: ReactNode;
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
 	 *
-	 * @default true
+	 * @default false
 	 */
-	loop?: boolean;
-} & (
-	| {
-			'aria-label': string;
-			'aria-labelledby'?: never;
-	  }
-	| {
-			'aria-label'?: never;
-			'aria-labelledby': string;
-	  }
- );
+	asButtons?: boolean;
+};
+
+export type ListboxCircularOptionPickerProps =
+	CommonCircularOptionPickerProps & {
+		asButtons: false | undefined;
+		/**
+		 * Prevents keyboard interaction from wrapping around.
+		 * Only used when `asButtons` is not true.
+		 *
+		 * @default false
+		 */
+		disableLooping?: boolean;
```

Depending on whether the previous `loop` prop got release into a previous version of `Gutenberg`, we may have to preserve the `loop` prop name, otherwise we'd be introducing a breaking change.

#### andrewhayward on 2023-09-13T08:41:22Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324180633
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -41,21 +41,40 @@ export type CircularOptionPickerProps = {
 	 */
 	children?: ReactNode;
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
 	 *
-	 * @default true
+	 * @default false
 	 */
-	loop?: boolean;
-} & (
-	| {
-			'aria-label': string;
-			'aria-labelledby'?: never;
-	  }
-	| {
-			'aria-label'?: never;
-			'aria-labelledby': string;
-	  }
- );
+	asButtons?: boolean;
+};
+
+export type ListboxCircularOptionPickerProps =
+	CommonCircularOptionPickerProps & {
+		asButtons: false | undefined;
+		/**
+		 * Prevents keyboard interaction from wrapping around.
+		 * Only used when `asButtons` is not true.
+		 *
+		 * @default false
+		 */
+		disableLooping?: boolean;
```

It's still [listed under `Unreleased`](https://github.com/WordPress/gutenberg/blob/b05bee7e2e8eefb2be06c228002e4f9783847747/packages/components/CHANGELOG.md#unreleased), so I'm assuming that means there's no public consumption yet.

Either way, [it can't be used outside of `@wordpress/components`](https://github.com/WordPress/gutenberg/blob/b05bee7e2e8eefb2be06c228002e4f9783847747/packages/components/src/circular-option-picker/README.md?plain=1#L3-L5), so any breaking changes should be constrained to the package, and we'd be aware of them.

#### ciampo on 2023-09-13T09:30:59Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324243943
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -41,21 +41,40 @@ export type CircularOptionPickerProps = {
 	 */
 	children?: ReactNode;
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
 	 *
-	 * @default true
+	 * @default false
 	 */
-	loop?: boolean;
-} & (
-	| {
-			'aria-label': string;
-			'aria-labelledby'?: never;
-	  }
-	| {
-			'aria-label'?: never;
-			'aria-labelledby': string;
-	  }
- );
+	asButtons?: boolean;
+};
+
+export type ListboxCircularOptionPickerProps =
+	CommonCircularOptionPickerProps & {
+		asButtons: false | undefined;
+		/**
+		 * Prevents keyboard interaction from wrapping around.
+		 * Only used when `asButtons` is not true.
+		 *
+		 * @default false
+		 */
+		disableLooping?: boolean;
```

Sounds good. Forgot to ask before — what is the reason for the prop name change? I found `loop` simpler, the double negation of `disableLooping="false"` may require a moment to understand

#### andrewhayward on 2023-09-13T10:38:09Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324320136
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -41,21 +41,40 @@ export type CircularOptionPickerProps = {
 	 */
 	children?: ReactNode;
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
 	 *
-	 * @default true
+	 * @default false
 	 */
-	loop?: boolean;
-} & (
-	| {
-			'aria-label': string;
-			'aria-labelledby'?: never;
-	  }
-	| {
-			'aria-label'?: never;
-			'aria-labelledby': string;
-	  }
- );
+	asButtons?: boolean;
+};
+
+export type ListboxCircularOptionPickerProps =
+	CommonCircularOptionPickerProps & {
+		asButtons: false | undefined;
+		/**
+		 * Prevents keyboard interaction from wrapping around.
+		 * Only used when `asButtons` is not true.
+		 *
+		 * @default false
+		 */
+		disableLooping?: boolean;
```

I guess it's just more my preference for value-less boolean props, as with HTML.

`<Component disableLooping>` feels cleaner/more natural than `<Component loop={false}>`, when the default value is `true`. But I can go either way – happy to change it back.

### Review thread 5 (resolved)

#### ciampo on 2023-09-12T13:54:41Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323084412
Path: packages/components/src/circular-option-picker/option-picker.tsx

Nitty, but let's rename these individual component file names as the actual components — ie. let's add a `circular-` prefix to their filenames

### Review thread 6 (resolved)

#### ciampo on 2023-09-12T13:54:44Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323084479
Path: packages/components/src/circular-option-picker/option-picker.tsx

Nitty, but let's rename these individual component file names as the actual components — ie. let's add a `circular-` prefix to their filenames

### Review thread 7 (resolved)

#### ciampo on 2023-09-12T13:57:01Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323087785
Path: packages/components/src/circular-option-picker/option-picker-option-group.tsx

```diff
@@ -0,0 +1,33 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import type { OptionGroupProps } from './types';
+
+export function OptionGroup( {
+	className,
+	options,
+	...additionalProps
+}: OptionGroupProps ) {
+	const { 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledby } =
+		additionalProps as any;
```

I must have missed it from the previous PR, but we don't want to leave `any` as types in our component.

Let's open a follow-up PR in which we replace all instances of `any` with the correct types, and we also add types to `CircularOptionPickerContext`

#### andrewhayward on 2023-09-13T14:56:07Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324653720
Path: packages/components/src/circular-option-picker/option-picker-option-group.tsx

```diff
@@ -0,0 +1,33 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import type { OptionGroupProps } from './types';
+
+export function OptionGroup( {
+	className,
+	options,
+	...additionalProps
+}: OptionGroupProps ) {
+	const { 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledby } =
+		additionalProps as any;
```

I think I've fixed this now.

### Review thread 8 (resolved)

#### ciampo on 2023-09-12T13:59:28Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323091270
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
+			} ) }
+			role="option"
+			aria-selected={ !! isSelected }
+		/>
+	);
+}
+
+export function Option( {
+	className,
+	isSelected,
+	selectedIconProps,
+	tooltipText,
+	...additionalProps
+}: OptionProps ) {
+	const compositeState = useContext( CircularOptionPickerContext );
+	const { baseId, currentId, setCurrentId } = compositeState as any;
+	const isComposite = !! baseId;
+	const id = useInstanceId( Option, baseId );
+
+	useEffect( () => {
+		// If we call `setCurrentId` here, it doesn't update for other
+		// Option renders in the same pass. So we have to store our own
+		// map to make sure that we only set the first selected option.
+		// We still need to check `currentId` because the control will
+		// update this as the user moves around, and that state should
+		// be maintained as the group gains and loses focus.
+		if (
+			isComposite &&
+			isSelected &&
+			! currentId &&
+			! hasSelectedOption.get( baseId )
+		) {
+			hasSelectedOption.set( baseId, true );
+			setCurrentId( id );
+		}
+	}, [ baseId, currentId, id, isComposite, isSelected, setCurrentId ] );
+
+	const optionControl = isComposite
+		? renderOptionAsOption( {
+				id,
+				className: 'components-circular-option-picker__option',
+				isSelected,
+				compositeState,
+				...additionalProps,
+		  } )
+		: renderOptionAsButton( {
+				id,
+				className: 'components-circular-option-picker__option',
+				isPressed: isSelected,
+				...additionalProps,
+		  } );
```

Maybe we can refactor `id`, `classname` and `additionalProps` to be all part of the same commonprops object?

### Review thread 9

#### ciampo on 2023-09-12T14:01:20Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323094031
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

Any reason why we're not passing `isPressed` as a prop here?

#### andrewhayward on 2023-09-13T09:13:20Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324221474
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

Doing so [also sets `aria-pressed` on the control](https://github.com/WordPress/gutenberg/blob/b05bee7e2e8eefb2be06c228002e4f9783847747/packages/components/src/button/index.tsx#L143-L150), which we don't want for `role="option"`. I thought I'd tried to override that and failed, but clearly not hard enough. Definitely better to let `Button` worry about which classes to apply.

#### ciampo on 2023-09-13T09:34:49Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324248577
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

Sounds good. Maybe let's add an inline comment above the `aria-pressed` override explaining the reason for it.

#### andrewhayward on 2023-09-13T13:54:49Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324555325
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

Turns out I possibly had tried hard enough, because `aria-pressed={ null }` doesn't "work"...

```
Types of property ''aria-pressed'' are incompatible.
Type 'null' is not assignable to type 'boolean | "true" | "false" | "mixed" | undefined'.
```

Setting it to `null` does properly override the value, and prevent `aria-pressed` from showing up as an attribute in the HTML, but TypeScript doesn't like it.

#### ciampo on 2023-09-14T18:06:38Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1326345211
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

From what you say, it sounds like passing `undefined` would correctly remove the `aria-pressed` attribute from the button.

Maybe something like this would work?

```
{ ...( isSelected ? { 'aria-pressed': undefined } : {} ) }
```

Let's still add an inline comment in case.

#### andrewhayward on 2023-09-15T12:27:27Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1327218542
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

Unfortunately passing `undefined` as a value for `aria-pressed` gets it ignored entirely. I've added a note to explain the class addition.

> [!NOTE]
> **Follow-up task:** Update `Button` to check for `role` before setting `aria-pressed`, and set a more appropriate attribute (e.g. `aria-selected`) as needed.

#### ciampo on 2023-09-15T12:46:23Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1327239910
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
```

Sounds good — could you open a new issue about it? You could work on it as a follow-up in the upcoming weeks.

### Review thread 10 (resolved)

#### ciampo on 2023-09-12T14:02:03Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323095077
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
```

Any reason why these are render functions, instead of components?

#### andrewhayward on 2023-09-13T08:56:46Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1324200819
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
```

No reason in particular!

### Review thread 11 (resolved)

#### ciampo on 2023-09-12T14:04:38Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323098761
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
+			} ) }
+			role="option"
+			aria-selected={ !! isSelected }
+		/>
+	);
+}
+
+export function Option( {
+	className,
+	isSelected,
+	selectedIconProps,
+	tooltipText,
+	...additionalProps
+}: OptionProps ) {
+	const compositeState = useContext( CircularOptionPickerContext );
+	const { baseId, currentId, setCurrentId } = compositeState as any;
+	const isComposite = !! baseId;
```

This check feels brittle — why don't we add a `isComposite` prop to the `CircularOptionPickerContext` object?

### Review thread 12 (resolved)

#### ciampo on 2023-09-12T14:06:19Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1323101143
Path: packages/components/src/circular-option-picker/option-picker-option.tsx

```diff
@@ -0,0 +1,118 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { useContext, useEffect } from '@wordpress/element';
+import { Icon, check } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { CircularOptionPickerContext } from './option-picker-context';
+import Button from '../button';
+import { CompositeItem } from '../composite';
+import Tooltip from '../tooltip';
+import type { OptionProps } from './types';
+
+const hasSelectedOption = new Map();
+
+function renderOptionAsButton( props: {
+	className?: string;
+	isPressed?: boolean;
+} ) {
+	return <Button { ...props }></Button>;
+}
+
+function renderOptionAsOption( props: {
+	id: string;
+	className?: string;
+	isSelected?: boolean;
+	compositeState: any;
+} ) {
+	const { className, isSelected, compositeState, ...additionalProps } = props;
+
+	return (
+		<CompositeItem
+			{ ...additionalProps }
+			{ ...compositeState }
+			as={ Button }
+			className={ classnames( className, {
+				'is-pressed': isSelected,
+			} ) }
+			role="option"
+			aria-selected={ !! isSelected }
+		/>
+	);
+}
+
+export function Option( {
+	className,
+	isSelected,
+	selectedIconProps,
+	tooltipText,
+	...additionalProps
+}: OptionProps ) {
+	const compositeState = useContext( CircularOptionPickerContext );
+	const { baseId, currentId, setCurrentId } = compositeState as any;
+	const isComposite = !! baseId;
+	const id = useInstanceId( Option, baseId );
+
+	useEffect( () => {
+		// If we call `setCurrentId` here, it doesn't update for other
+		// Option renders in the same pass. So we have to store our own
+		// map to make sure that we only set the first selected option.
+		// We still need to check `currentId` because the control will
+		// update this as the user moves around, and that state should
+		// be maintained as the group gains and loses focus.
+		if (
+			isComposite &&
+			isSelected &&
+			! currentId &&
+			! hasSelectedOption.get( baseId )
+		) {
+			hasSelectedOption.set( baseId, true );
+			setCurrentId( id );
+		}
+	}, [ baseId, currentId, id, isComposite, isSelected, setCurrentId ] );
+
+	const optionControl = isComposite
+		? renderOptionAsOption( {
+				id,
+				className: 'components-circular-option-picker__option',
+				isSelected,
+				compositeState,
+				...additionalProps,
+		  } )
+		: renderOptionAsButton( {
+				id,
+				className: 'components-circular-option-picker__option',
+				isPressed: isSelected,
+				...additionalProps,
+		  } );
+
+	return (
+		<div
+			className={ classnames(
+				className,
+				'components-circular-option-picker__option-wrapper'
+			) }
+		>
+			{ tooltipText ? (
+				<Tooltip text={ tooltipText }>{ optionControl }</Tooltip>
+			) : (
+				optionControl
+			) }
+			{ isSelected && (
+				<Icon
+					icon={ check }
+					{ ...( selectedIconProps ? selectedIconProps : {} ) }
```

We could add a default value of `{}` when destructuring the prop to simplify this line

### Review thread 13 (resolved)

#### ciampo on 2023-09-14T18:09:08Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1326348128
Path: packages/components/src/color-palette/types.ts

```diff
@@ -82,6 +82,20 @@ export type ColorPaletteProps = Pick< PaletteProps, 'onChange' > & {
 	 * Currently active value.
 	 */
 	value?: string;
+	/**
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
+	 *
+	 * @default false
+	 */
+	asButtons?: boolean;
+	/**
+	 * Prevents keyboard interaction from wrapping around.
+	 * Only used when `asButtons` is not true.
+	 *
+	 * @default true
+	 */
+	loop?: boolean;
```

We should update the component's README to include these new props. Same goes for `DuotonePicker` and `GradientPicker`

### Review thread 14 (resolved)

#### ciampo on 2023-09-14T18:10:02Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1326349002
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -41,21 +42,54 @@ export type CircularOptionPickerProps = {
 	 */
 	children?: ReactNode;
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
+	 *
+	 * @default false
+	 */
+	asButtons?: boolean;
```

Let's also update `CircularOptionPicker`'s README to include the new `asButtons` prop.

### Review thread 15 (resolved)

#### ciampo on 2023-09-14T19:32:43Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1326430258
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -41,21 +42,54 @@ export type CircularOptionPickerProps = {
 	 */
 	children?: ReactNode;
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
+	 *
+	 * @default false
+	 */
+	asButtons?: boolean;
+};
+
+type WithBaseId = {
+	baseId: string;
+};
+
+type FullListboxCircularOptionPickerProps = CommonCircularOptionPickerProps & {
+	/**
+	 * Prevents keyboard interaction from wrapping around.
+	 * Only used when `asButtons` is not true.
 	 *
 	 * @default true
 	 */
 	loop?: boolean;
 } & (
-	| {
-			'aria-label': string;
-			'aria-labelledby'?: never;
-	  }
-	| {
-			'aria-label'?: never;
-			'aria-labelledby': string;
-	  }
- );
+		| {
+				'aria-label': string;
+				'aria-labelledby'?: never;
+		  }
+		| {
+				'aria-label'?: never;
+				'aria-labelledby': string;
+		  }
+	 );
+
+export type ListboxCircularOptionPickerProps = WithBaseId &
+	Omit<
+		FullListboxCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>;
+
+type FullButtonsCircularOptionPickerProps = CommonCircularOptionPickerProps;
+
+export type ButtonsCircularOptionPickerProps = WithBaseId &
+	Omit<
+		FullButtonsCircularOptionPickerProps,
+		'asButtons' | 'actions' | 'options'
+	>;
+
+export type CircularOptionPickerProps =
+	| ( { asButtons?: false } & FullListboxCircularOptionPickerProps )
+	| ( { asButtons: true } & FullButtonsCircularOptionPickerProps );
```

Let's refactor the way `asButtons` is defined:

```diff
diff --git a/packages/components/src/circular-option-picker/types.ts b/packages/components/src/circular-option-picker/types.ts
index 2c2b92ba09..463e519f57 100644
--- a/packages/components/src/circular-option-picker/types.ts
+++ b/packages/components/src/circular-option-picker/types.ts
@@ -41,13 +41,6 @@ type CommonCircularOptionPickerProps = {
 	 * The child elements.
 	 */
 	children?: ReactNode;
-	/**
-	 * Whether the control should present as a set of buttons,
-	 * each with its own tab stop.
-	 *
-	 * @default false
-	 */
-	asButtons?: boolean;
 };
 
 type WithBaseId = {
@@ -55,6 +48,13 @@ type WithBaseId = {
 };
 
 type FullListboxCircularOptionPickerProps = CommonCircularOptionPickerProps & {
+	/**
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
+	 *
+	 * @default false
+	 */
+	asButtons?: false;
 	/**
 	 * Prevents keyboard interaction from wrapping around.
 	 * Only used when `asButtons` is not true.
@@ -79,7 +79,15 @@ export type ListboxCircularOptionPickerProps = WithBaseId &
 		'asButtons' | 'actions' | 'options'
 	>;
 
-type FullButtonsCircularOptionPickerProps = CommonCircularOptionPickerProps;
+type FullButtonsCircularOptionPickerProps = CommonCircularOptionPickerProps & {
+	/**
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
+	 *
+	 * @default false
+	 */
+	asButtons: true;
+};
 
 export type ButtonsCircularOptionPickerProps = WithBaseId &
 	Omit<
@@ -88,8 +96,8 @@ export type ButtonsCircularOptionPickerProps = WithBaseId &
 	>;
 
 export type CircularOptionPickerProps =
-	| ( { asButtons?: false } & FullListboxCircularOptionPickerProps )
-	| ( { asButtons: true } & FullButtonsCircularOptionPickerProps );
+	| FullListboxCircularOptionPickerProps
+	| FullButtonsCircularOptionPickerProps;
 
 export type DropdownLinkActionProps = {
 	buttonProps?: Omit<
```

This way has, IMO, a couple of advantages:
- the `FullListboxCircularOptionPickerProps` and `FullButtonsCircularOptionPickerProps` types are _actually_ representing the full list of props
- the JSDocs for the `asButtons` prop are correctly parsed by intellisense

### Review thread 16 (resolved)

#### ciampo on 2023-09-14T19:34:18Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1326431754
Path: packages/components/src/composite/index.ts

```diff
@@ -17,3 +17,6 @@ export {
 	CompositeItem,
 	useCompositeState,
 } from 'reakit';
+
+/* eslint-disable-next-line no-restricted-imports */
+export type { CompositeStateReturn as CompositeState } from 'reakit';
```

Let's delete this export, since it's not used anywhere 🧹

#### andrewhayward on 2023-09-15T09:46:05Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1327064723
Path: packages/components/src/composite/index.ts

```diff
@@ -17,3 +17,6 @@ export {
 	CompositeItem,
 	useCompositeState,
 } from 'reakit';
+
+/* eslint-disable-next-line no-restricted-imports */
+export type { CompositeStateReturn as CompositeState } from 'reakit';
```

It's used in `circular-option-picker/types.ts`...

```ts
export type CircularOptionPickerCompositeState = CompositeState;
export type CircularOptionPickerContextProps =
	| { isComposite?: false; baseId?: string }
	| ( { isComposite: true } & CircularOptionPickerCompositeState );
```

#### ciampo on 2023-09-15T09:59:33Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1327078947
Path: packages/components/src/composite/index.ts

```diff
@@ -17,3 +17,6 @@ export {
 	CompositeItem,
 	useCompositeState,
 } from 'reakit';
+
+/* eslint-disable-next-line no-restricted-imports */
+export type { CompositeStateReturn as CompositeState } from 'reakit';
```

My bad, I must have missed that — feel free to ignore my comment.

### Review thread 17 (resolved)

#### ciampo on 2023-09-15T13:15:42Z

URL: https://github.com/WordPress/gutenberg/pull/54290#discussion_r1327275083
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -40,22 +41,57 @@ export type CircularOptionPickerProps = {
 	 * The child elements.
 	 */
 	children?: ReactNode;
+};
+
+type WithBaseId = {
+	baseId: string;
+};
+
+type FullListboxCircularOptionPickerProps = CommonCircularOptionPickerProps & {
 	/**
-	 * Whether the keyboard interaction should wrap around.
+	 * Whether the control should present as a set of buttons,
+	 * each with its own tab stop.
+	 *
+	 * @default false
+	 */
```

The extra default value is unnecessary and causes Storybook to display it twice in its generated docs.

```suggestion
	 * Whether the control should present as a set of buttons,
	 * each with its own tab stop.
	 */
```

## Changed files

- packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap (MODIFIED, +30/-27)
- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/circular-option-picker/README.md (MODIFIED, +14/-0)
- packages/components/src/circular-option-picker/circular-option-picker-actions.tsx (ADDED, +60/-0)
- packages/components/src/circular-option-picker/circular-option-picker-context.tsx (ADDED, +12/-0)
- packages/components/src/circular-option-picker/circular-option-picker-option-group.tsx (ADDED, +34/-0)
- packages/components/src/circular-option-picker/circular-option-picker-option.tsx (ADDED, +139/-0)
- packages/components/src/circular-option-picker/circular-option-picker.tsx (ADDED, +202/-0)
- packages/components/src/circular-option-picker/index.tsx (MODIFIED, +7/-258)
- packages/components/src/circular-option-picker/stories/index.story.tsx (MODIFIED, +34/-9)
- packages/components/src/circular-option-picker/test/index.tsx (ADDED, +133/-0)
- packages/components/src/circular-option-picker/types.ts (MODIFIED, +50/-11)
- packages/components/src/color-palette/README.md (MODIFIED, +14/-0)
- packages/components/src/color-palette/index.tsx (MODIFIED, +32/-11)
- packages/components/src/color-palette/types.ts (MODIFIED, +14/-0)
- packages/components/src/composite/index.ts (MODIFIED, +3/-0)
- packages/components/src/duotone-picker/README.md (MODIFIED, +14/-0)
- packages/components/src/duotone-picker/duotone-picker.tsx (MODIFIED, +28/-7)
- packages/components/src/duotone-picker/types.ts (MODIFIED, +14/-0)
- packages/components/src/gradient-picker/README.md (MODIFIED, +14/-0)
- packages/components/src/gradient-picker/index.tsx (MODIFIED, +35/-12)
- packages/components/src/gradient-picker/types.ts (MODIFIED, +14/-0)
