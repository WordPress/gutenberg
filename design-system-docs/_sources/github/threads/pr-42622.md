---
number: 42622
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/42622
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #42622: useBaseField: Refactor to typescript

- URL: https://github.com/WordPress/gutenberg/pull/42622
- Author: manzoorwanijk
- Created: 2022-07-22T06:58:04Z
- Updated: 2022-11-01T09:06:41Z
- Comments: 2 of 2
- Review threads: 7 of 7
- Changed files: 8 of 8
- Diff: ../pr-diffs/42622.diff

## Body

## What?
Converts `useBaseField` to TypeScript.

## Why?
Part of the @wordpress/components's TypeScript migration #35744.

## How?
- Converts `useBaseField` to TypeScript
- Converts Unit Tests to TypeScript

## Testing Instructions
- Run `npm run dev` and confirm no typing errors

**Note**

GitHub doesn't show the diff for `hook.ts` even though I used `git mv` to rename the file. So, you can check https://github.com/WordPress/gutenberg/commit/99a4f7b46131b3d361272f23b6b627eb8a66e17a to see the changes to the file.

## Issue comments

### manzoorwanijk on 2022-11-01T06:38:36Z

URL: https://github.com/WordPress/gutenberg/pull/42622#issuecomment-1298090703

I could finish this and many other TS-related improvements around the time this PR was created but now I may not get more time in the near future to work on this. Someone else may take it over in their fork.

### ciampo on 2022-11-01T09:06:41Z

URL: https://github.com/WordPress/gutenberg/pull/42622#issuecomment-1298241030

Thank you for the heads-up! And feel free to reach out again anytime you'd like to contribute in the future!

## Review threads

### Review thread 1

#### ciampo on 2022-10-25T08:42:21Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004180547
Path: packages/components/src/base-field/hook.ts

```diff
@@ -0,0 +1,97 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useContextSystem, WordPressComponentProps } from '../ui/context';
+import { useControlGroupContext } from '../ui/control-group';
+import { useFlex } from '../flex';
+import * as styles from './styles';
+import { useCx } from '../utils/hooks/use-cx';
+import type { UseBaseFieldProps } from './types';
+
+/**
+ * `useBaseField` is an internal (i.e., not exported in the `index.js`) hook
+ * used for building more complex fields like `TextField`.
+ * It provides error handling and focus styles for field components.
+ * It does _not_ handle layout of the component aside from wrapping the field in a `Flex` wrapper.
+ *
+ * ```js
+ * function useExampleField( props ) {
+ * 	const {
+ * 		as = 'input',
+ * 		...baseProps,
+ * 	} = useBaseField( props );
+ *
+ * 	const inputProps = {
+ * 		as,
+ * 		// more cool stuff here
+ * 	}
+ *
+ * 	return { inputProps, ...baseProps };
+ * }
+ *
+ * function ExampleField( props, forwardRef ) {
+ * 	const {
+ * 		preFix,
+ * 		affix,
+ * 		disabled,
+ * 		inputProps,
+ * 		...baseProps
+ * 	} = useExampleField( props );
+ *
+ * 	return (
+ * 		<View { ...baseProps } disabled={ disabled }>
+ * 			{preFix}
+ * 			<View
+ * 				autocomplete="off"
+ * 				{ ...inputProps }
+ * 				disabled={ disabled }
+ * 			/>
+ * 			{affix}
+ * 		</View>
+ * 	);
+ * }
+ * ```
+ *
+ * @param {WordPressComponentProps< UseBaseFieldProps, 'div' >} props
```

No need to defined types twice — the TypeScript notation should be enough

```suggestion
 * @param  props
```

### Review thread 2

#### ciampo on 2022-10-25T08:42:55Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004181225
Path: packages/components/src/base-field/hook.ts

```diff
@@ -0,0 +1,97 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useContextSystem, WordPressComponentProps } from '../ui/context';
+import { useControlGroupContext } from '../ui/control-group';
+import { useFlex } from '../flex';
+import * as styles from './styles';
+import { useCx } from '../utils/hooks/use-cx';
+import type { UseBaseFieldProps } from './types';
+
+/**
+ * `useBaseField` is an internal (i.e., not exported in the `index.js`) hook
+ * used for building more complex fields like `TextField`.
+ * It provides error handling and focus styles for field components.
+ * It does _not_ handle layout of the component aside from wrapping the field in a `Flex` wrapper.
+ *
+ * ```js
+ * function useExampleField( props ) {
+ * 	const {
+ * 		as = 'input',
+ * 		...baseProps,
+ * 	} = useBaseField( props );
+ *
+ * 	const inputProps = {
+ * 		as,
+ * 		// more cool stuff here
+ * 	}
+ *
+ * 	return { inputProps, ...baseProps };
+ * }
+ *
+ * function ExampleField( props, forwardRef ) {
+ * 	const {
+ * 		preFix,
+ * 		affix,
+ * 		disabled,
+ * 		inputProps,
+ * 		...baseProps
+ * 	} = useExampleField( props );
+ *
+ * 	return (
+ * 		<View { ...baseProps } disabled={ disabled }>
+ * 			{preFix}
+ * 			<View
+ * 				autocomplete="off"
+ * 				{ ...inputProps }
+ * 				disabled={ disabled }
+ * 			/>
+ * 			{affix}
+ * 		</View>
+ * 	);
+ * }
+ * ```
+ *
+ * @param {WordPressComponentProps< UseBaseFieldProps, 'div' >} props
+ */
+export function useBaseField(
+	props: WordPressComponentProps< UseBaseFieldProps, 'div' >
+) {
+	const {
+		className,
+		hasError = false,
+		isInline = false,
+		isSubtle = false,
+		// Extract these because useFlex doesn't accept it.
+		defaultValue,
+		disabled,
+		...flexProps
+	} = useContextSystem( props, 'BaseField' );
+
+	const { styles: controlGroupStyles } = useControlGroupContext();
+	const cx = useCx();
+
+	const classes = useMemo(
+		() =>
+			cx(
+				styles.BaseField,
+				controlGroupStyles,
+				isSubtle && styles.subtle,
+				hasError && styles.error,
+				isInline && styles.inline,
+				className
+			),
+		[ className, controlGroupStyles, cx, hasError, isInline, isSubtle ]
+	);
+
+	return {
+		...useFlex( { children: null, ...flexProps, className: classes } ),
```

Why are we overriding `children: null` ?

### Review thread 3

#### ciampo on 2022-10-25T08:45:47Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004184572
Path: packages/components/src/base-field/types.ts

```diff
@@ -0,0 +1,23 @@
+/**
+ * Internal dependencies
+ */
+import type { FlexProps } from '../flex/types';
+
+export interface UseBaseFieldProps extends Partial< FlexProps > {
```

We usually use `type` instead of `interface`, would you mind switching to that?

### Review thread 4

#### ciampo on 2022-10-25T08:47:15Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004186244
Path: packages/components/src/base-field/types.ts

```diff
@@ -0,0 +1,23 @@
+/**
+ * Internal dependencies
+ */
+import type { FlexProps } from '../flex/types';
+
+export interface UseBaseFieldProps extends Partial< FlexProps > {
```

Can we more specific to which `FlexProps` should be picked here? Any reason why we're not allowing all props from `FlexProps` ?

### Review thread 5

#### ciampo on 2022-10-25T08:55:29Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004196591
Path: packages/components/src/base-field/test/index.tsx

```diff
@@ -8,13 +8,17 @@ import { render } from '@testing-library/react';
  */
 import { useBaseField } from '../index';
 import { View } from '../../view';
+import type { WordPressComponentProps } from '../../ui/context';
+import type { UseBaseFieldProps } from '../types';
 
-const TestField = ( props ) => {
+const TestField = (
+	props: WordPressComponentProps< UseBaseFieldProps, 'div' >
+) => {
```

`WordPressComponentProps` is not necessary here

```suggestion
const TestField = ( props: UseBaseFieldProps ) => {
```

### Review thread 6

#### ciampo on 2022-10-25T08:59:00Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004201056
Path: packages/components/src/base-field/test/index.tsx

```diff
@@ -52,10 +56,18 @@ describe( 'base field', () => {
 			// wrap this in a component so that `useContext` calls don't fail inside the hook
 			// assertions will still run as normal when we `render` the component :)
 			const Component = () => {
-				const disabled = Symbol.for( 'disabled' );
-				const defaultValue = Symbol.for( 'defaultValue' );
+				let disabled = true;
+				let defaultValue = 'test';
 
-				const result = useBaseField( { disabled, defaultValue } );
+				let result = useBaseField( { disabled, defaultValue } );
+
+				expect( result.disabled ).toBe( disabled );
+				expect( result.defaultValue ).toBe( defaultValue );
+
+				disabled = false;
+				defaultValue = 'something-else';
+
+				result = useBaseField( { disabled, defaultValue } );
```

Let's rebase first, and then we can see if this test can be written without this "wrapping in the component" hack

### Review thread 7

#### ciampo on 2022-10-25T09:00:05Z

URL: https://github.com/WordPress/gutenberg/pull/42622#discussion_r1004202277
Path: packages/components/src/base-field/types.ts

```diff
@@ -0,0 +1,23 @@
+/**
+ * Internal dependencies
+ */
+import type { FlexProps } from '../flex/types';
+
+export interface UseBaseFieldProps extends Partial< FlexProps > {
+	/**
+	 * Renders an error.
+	 */
+	hasError?: boolean;
+	/**
+	 * Whether the field is disabled.
+	 */
+	disabled?: boolean;
+	/**
+	 * Renders as an inline element (layout).
+	 */
+	isInline?: boolean;
+	/**
+	 * Renders a subtle variant.
+	 */
+	isSubtle?: boolean;
```

`hasError`, `isInline` and `isSubtle` are missing the `@default` value in their JSDocs

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-1)
- packages/components/src/base-field/hook.js (DELETED, +0/-61)
- packages/components/src/base-field/hook.ts (ADDED, +97/-0)
- packages/components/src/base-field/index.ts (RENAMED, +0/-0)
- packages/components/src/base-field/styles.ts (RENAMED, +0/-0)
- packages/components/src/base-field/test/__snapshots__/index.tsx.snap (RENAMED, +0/-0)
- packages/components/src/base-field/test/index.tsx (RENAMED, +20/-8)
- packages/components/src/base-field/types.ts (ADDED, +23/-0)
