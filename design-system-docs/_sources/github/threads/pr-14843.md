---
number: 14843
type: pull_request
state: MERGED
merged_at: 2019-06-03T07:35:14Z
url: https://github.com/WordPress/gutenberg/pull/14843
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Feature] UI Components","[Package] Components","[Package] Block editor"]
---

# PR #14843: UI Components: Refactor BlockSettingsMenu and MoreMenu as DropdownMenu

- URL: https://github.com/WordPress/gutenberg/pull/14843
- Author: aduth
- Created: 2019-04-05T16:01:06Z
- Updated: 2019-06-03T15:17:59Z
- Comments: 18 of 18
- Review threads: 12 of 12
- Changed files: 25 of 25
- Diff: ../pr-diffs/14843.diff

## Body

Previously: https://github.com/WordPress/gutenberg/pull/13410#discussion_r272586986 , #2989
Related: #14754 
Supersedes: #13059
Fixes #12505, fixes #15318 (Single visible menu is made up of multiple menus)

This pull request seeks to refactor the block settings menu to use the [`DropdownMenu` component](https://github.com/WordPress/gutenberg/tree/master/packages/components/src/dropdown-menu). This is part of a larger effort to consolidate and normalize behaviors between dropdown menus. Prior to this pull request, there was a fair amount of duplication and divergent behaviors between the block settings menu and the standard `DropdownMenu` component.

There is not expected to be a difference in the look and feel of the block settings menu.

**Design Notes:**

The changes here err on the side of treating the block settings menu styles as the more correct iteration of dropdown menu styling. Notably, this has the following impact:

- The right margin of an icon in DropdownMenu has increased by `1px`, from `4px` to `5px`
- Default left- and right-padding of the DropdownMenu contents has been removed. This affects existing usage in the table block Edit Table dropdown ([before](https://user-images.githubusercontent.com/1779930/55639585-b92e2c80-5797-11e9-9abf-2ecdce0db1c2.png), [after](https://user-images.githubusercontent.com/1779930/55639502-86843400-5797-11e9-9821-22f54e7e8961.png)).
- Small viewport hover styling has been restored (previously removed by #10333 in an attempt to remove tap-drag hover activation described in #10320). Again noting that this never applied previously for block settings menu, but did apply for "More Options" and "Edit Table" dropdowns. This may need design confirmation. However, I would argue that it is not safe to assume that the relationship between touch device and small viewports are mutually inclusive (e.g. laptops with touchscreen support). 

**Implementation Notes:**

I operated on an assumption that the fact that block settings menus and "More Options" menu were not implemented as `DropdownMenu` was at least partly motivated by the amount of additional customization necessary for these menus being unsupportable by the base component. It's for this reason that I introduced a new `children` render function prop to serve as an alternative to the existing (limiting) `controls` prop.

As an alternative, I had also explored whether to allow elements to be intermixed with `control` object definitions in the default `controls` prop. There is a possible route here, but it is made difficult by the fact that (a) the menu items are likely to need access to `onClose`, (b) each element in an array needing a `key`, and (c) the confusion surrounding an overloaded array supports of an object and element formats. It's for this reason that I decided to stick with the render function and, if anything, I would suggest that in retrospect this makes more sense as a default anyways.

It may help to review using [GitHub's "Ignore white space changes" option](https://github.blog/2018-05-01-ignore-white-space-in-code-review/) (especially `BlockSettingsMenu`).

**Testing Instructions:**

Verify that the block settings menu ([right-most in toolbar](https://user-images.githubusercontent.com/1779930/55640179-18407100-5799-11e9-8ca5-5f36d142c607.png)) looks and behaves effectively identical as to `master`.

Verify that More Menu ([right-most in header](https://user-images.githubusercontent.com/699132/56042574-aab5b700-5d3b-11e9-8c76-0f6df6df3e2b.png)) looks and behaves identically as to `master` with one exception:
- using up/down arrow keys inside the menu allows you to navigate between all items, it doesn't lock you down inside a single group as proposed in #12505

![more-menu](https://user-images.githubusercontent.com/699132/56042662-e18bcd00-5d3b-11e9-99b1-0de6904c3075.gif)


Ensure unit tests pass:

```
npm run test-unit
```

**Included Tasks:**
- Refactor "More Options" as `DropdownMenu`
- Refactor `MenuGroup` to stop using `NavigableMenu` which wrongly narrows down arrow keys navigation

**Known bugs**:

There are 2 bugs which are being tackled separately:

- Focus wrongly transitions to post title when toggling Visual/Code (#14850)
- More Options menu toggle buttons cause focus loss when toggled (#14849)

**Future Tasks:**

- Refactor `DropdownMenu` to use `MenuItem` component in its default rendering of `controls`

## Issue comments

### mapk on 2019-04-05T22:04:47Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-480436716

> Verify that the block settings menu (right-most in toolbar) looks and behaves effectively identical as to master.

Here's a comparison of what I see in Firefox between this feature branch and master.

![side-by-side](https://user-images.githubusercontent.com/617986/55658817-dcff6b80-57b3-11e9-8688-965f869786bc.png)

1. The ellipses (3 dots) is now vertically aligned.
2. Top padding in the popover is increased before the first menu item.
3. There a horizontal rule separating out the "Remove block" option.
4. Bottom padding in the popover is increased after the last menu item.

### jasmussen on 2019-04-08T07:05:09Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-480709785

Really nice work. Thank you for doing this.

> Verify that the block settings menu (right-most in toolbar) looks and behaves effectively identical as to master.

This is what I see in both this branch and in master:

![branch](https://user-images.githubusercontent.com/1204802/55704719-28e12900-59dd-11e9-81f9-a3e7a3a78099.gif)

In other words, I don't see the same "old" style as @mapk does in master, not sure why. But in any case as far as my testing goes, this GIF is correct looking, and very nice.

I did notice one thing. Master:

<img width="460" alt="Screenshot 2019-04-08 at 09 02 49" src="https://user-images.githubusercontent.com/1204802/55704749-41514380-59dd-11e9-82b6-765cb1b49b95.png">

This branch:

<img width="504" alt="Screenshot 2019-04-08 at 09 02 27" src="https://user-images.githubusercontent.com/1204802/55704755-444c3400-59dd-11e9-8013-7fbc4a66bcd0.png">

Since that menu is not part of the scope of this branch, feel free to ignore it. But as you can see that overflow menu is actually _slightly improved_ in this branch, all that's missing is the correct hover style. If fixing that hover style is within reach, it'd be nice to fix!

Otherwise, feel free to ship once we work out why Mark saw something different than I did.

### aduth on 2019-04-08T13:02:21Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-480822226

Thanks @mapk @jasmussen for spotting those styling issues! It's quite possible I'd overlooked a few, so I'll plan to take another pass at it. I'll note that it's proving to be a useful exercise in consistency, as those "inaccuracies" are exactly what one would expect the behavior for their own default `DropdownMenu` usage prior to this pull request.

>Since that menu is not part of the scope of this branch, feel free to ignore it. But as you can see that overflow menu is actually slightly improved in this branch, all that's missing is the correct hover style. If fixing that hover style is within reach, it'd be nice to fix!

I must have missed that that this menu was using `DropdownMenu`. I was largely operating under the assumption that only the "Edit Table" dropdown was using it. I think it's fair to say that the scope of this pull request would be to align the behaviors of all `DropdownMenu` toward what's implemented today as the block settings menu. I'll work to fix it up!

### gziolo on 2019-04-12T13:43:15Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-482579364

I applied the following changes to this PR:
- rebased this branch with `master` first
- refactored `MoreMenu` component to use `DropdownMenu`
- refactored `DropdownMenu` to use all props that `MoreMenu` was using
- refactored `MenuGroup` to stop using `NavigableMenu` and added the same attributes as proposed in #12505

I left more comments inline. `DropdownMenu` still needs to be verified whether the current proposal offers solid public API after changes applied. Once it is sorted out, docs need to be updated with new props added and CHANGELOG file as well.

### gziolo on 2019-04-12T14:44:57Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-482601338

There are 3 failing e2e tests to investigate:

https://travis-ci.com/WordPress/gutenberg/jobs/192501278#L1107

```
Summary of all failing tests
FAIL specs/plugins/annotations.test.js (13.298s)
  ● Using Plugins API › Annotations › Allows a block to be annotated
    Element .block-editor-block-settings-menu__toggle not found
      waiting for function failed: timeout 500ms exceeded
FAIL specs/keyboard-navigable-blocks.test.js
  ● Order of block keyboard navigation › permits tabbing through paragraph blocks in the expected order
    expect(received).toBe(expected) // Object.is equality
    Expected: true
```

https://travis-ci.com/WordPress/gutenberg/jobs/192501279#L1093

```
Summary of all failing tests
FAIL specs/block-deletion.test.js (20.357s)
  ● block deletion - › deleting the third block using the Remove Block menu item › results in two remaining blocks and positions the caret at the end of the second block
    Element .block-editor-block-settings-menu__toggle not found
      waiting for function failed: timeout 500ms exceeded
  ● deleting all blocks › results in the default block getting selected
    Element .block-editor-block-settings-menu__toggle not found
      waiting for function failed: timeout 500ms exceeded
```

### gziolo on 2019-05-14T11:58:22Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-492207147

I rebased this branch with `master` and applied `__unstable` prefix to all those props we don't want to expose in `DropdownMenu` component. There are 2 remaining items:
- https://github.com/WordPress/gutenberg/pull/14843#discussion_r276425204 - remove obsolete CSS style
- https://github.com/WordPress/gutenberg/pull/14843#discussion_r273031796 - revert the change which introduces `DropdownMenueSeparator`

Other than that this branch should be good to go.

### gziolo on 2019-05-28T08:43:09Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-496422758

> There are 2 remaining items:
> 
> * [#14843 (comment)](https://github.com/WordPress/gutenberg/pull/14843#discussion_r276425204) - remove obsolete CSS style
> * [#14843 (comment)](https://github.com/WordPress/gutenberg/pull/14843#discussion_r273031796) - revert the change which introduces `DropdownMenueSeparator`

Both issues were addressed in dda8295. This PR is ready for the final review.

### gziolo on 2019-05-30T14:09:12Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497342747

In afa7bfa I moved common styles to the `DropdownMenu` component. I would appreciate sanity check from one of the designers:

![Screen Shot 2019-05-30 at 16 07 25](https://user-images.githubusercontent.com/699132/58638407-08ef2700-82f5-11e9-82cf-08dbe70cbe42.png)

![Screen Shot 2019-05-30 at 16 07 53](https://user-images.githubusercontent.com/699132/58638433-199f9d00-82f5-11e9-8796-4a2cdae6042c.png)

Those 2 menus should render exactly the same now. I'm wondering if `padding: 0` should be applied by default when there are groups used for `DropdownMenu` in general. I'm not quite sure how it could be codified though.

### gziolo on 2019-05-30T14:12:43Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497343952

There is one more thing as noted by @jasmussen:

Master:
![Screen Shot 2019-05-30 at 16 11 15](https://user-images.githubusercontent.com/699132/58638632-9b8fc600-82f5-11e9-9836-5669b7338020.png)

This branch:
![Screen Shot 2019-05-30 at 16 11 27](https://user-images.githubusercontent.com/699132/58638634-9b8fc600-82f5-11e9-9070-68aa3b870a27.png)

How should we proceed?

### jasmussen on 2019-05-30T14:15:23Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497344918

I'm afk today, but just merged a tweak to some menus. Try master again, and maybe rebase.

### aduth on 2019-05-30T14:16:19Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497345246

@gziolo From your screenshot: I recall that this was considered as something which should be converged anyways, with the "This branch" screenshot (outline flush to edges) being the preferred, more-recently-updated approach.

### jasmussen on 2019-05-30T14:18:13Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497345958

Correct. "This branch" is preferred.

### gziolo on 2019-05-30T14:35:17Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497352075

> I'm afk today, but just merged a tweak to some menus. Try master again, and maybe rebase.

Rebased, I think I did it 2 hours ago already :) I think that the current proposal is very consistent and I'm personally happy with it.

I also addressed the last remaining comment from @aduth (https://github.com/WordPress/gutenberg/pull/14843#discussion_r288704952) with https://github.com/WordPress/gutenberg/pull/14843/commits/032167abd4a4e61be165fee0344e9bfdfdf9499a.

### youknowriad on 2019-05-31T10:03:29Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-497654247

Tested this quickly and it seems to work well.

### gziolo on 2019-06-03T07:35:06Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-498146127

Let's get this in. I will open a follow-up issue to get rid of all `__unstable` props and see if there is a quick way to solve it.

### gziolo on 2019-06-03T07:47:25Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-498149776

> Let's get this in. I will open a follow-up issue to get rid of all `__unstable` props and see if there is a quick way to solve it.

I opened #15960 to address it. @aduth - do we have issues opened for observed focus loss in More Menu?

### afercia on 2019-06-03T08:15:01Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-498158344

Thanks everyone, this is a big improvement for the menu ❤️ 

@gziolo re: the focus loss, I've created https://github.com/WordPress/gutenberg/issues/15501 a while ago.

### gziolo on 2019-06-03T15:17:59Z

URL: https://github.com/WordPress/gutenberg/pull/14843#issuecomment-498301251

> > Let's get this in. I will open a follow-up issue to get rid of all `__unstable` props and see if there is a quick way to solve it.
> 
> I opened #15960 to address it.

PR is ready: #15968.

## Review threads

### Review thread 1 (resolved)

#### aduth on 2019-04-05T16:06:01Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r272651932
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
```

**_Needs Accessibility Feedback_**

Note: We previously would change the label of the block settings toggle button to reflect whether the menu was opened or closed. This is not supported in DropdownMenu. It's unclear whether this would add value anyways. I would appreciate accessibility feedback here, noting also that the toggle button does apply specific props to indicate the expanded status of the menu (`aria-expanded` as it did before, and an additional `aria-haspopup` which did not exist for BlockSettingsMenu, but is applied in DropdownMenu).

If it should be supported, then support should be made available via `DropdownMenu` to not limit the `label` prop to a single static string value, but rather as a pair, or a function which returns a string given the open state.

#### gziolo on 2019-04-12T13:33:43Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r274907424
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
```

I refactored it to allow using different values depending on `isOpen` value to make it work the same as before. However, we can revert that change as soon as we get accessibility feedback to use the revised approach.

#### afercia on 2019-04-12T14:47:38Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r274939560
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
```

If I get it correctly, this refers to the current button label (and tooltip) changing from “Show more tools…” to “Hide more tools…” depending on the state, correct? Personally, I’m wary of dynamically changing the accessible name of a control. A name is a name, it shouldn’t change. The actual _state_ should be represented by other means. So if I’m getting it correctly, it would be ok for me.

#### gziolo on 2019-04-15T11:15:41Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r275312363
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
```

Updated as discussed in 07d3716750f094fb72fc29b4dfa6303d3ebc4578.

### Review thread 2 (resolved)

#### aduth on 2019-04-05T16:07:38Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r272652501
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
-
-						return (
-							<Toolbar controls={ [ {
-								icon: 'ellipsis',
-								title: label,
-								onClick: () => {
-									if ( count === 1 ) {
-										onSelect( firstBlockClientId );
```

Note: I could not discover what this was trying to do. I think it may have been related to a much earlier design where toolbars could have been shown without the block having already been selected. As far as I can tell, this is no longer the case. And if it were, I would suggest instead to enhance the [existing `onFocus` behavior of `BlockListBlock`](https://github.com/WordPress/gutenberg/blob/6cb0b3268217020a101561d6f9b7d37ab08fa7da/packages/block-editor/src/components/block-list/block.js#L236-L247) instead.

### Review thread 3 (resolved)

#### aduth on 2019-04-05T16:10:36Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r272653497
Path: packages/block-editor/src/components/block-settings-menu/style.scss

```diff
@@ -1,59 +1,11 @@
-.block-editor-block-settings-menu__toggle .dashicon {
+.block-editor-block-settings-menu .components-dropdown-menu__toggle .dashicon {
 	transform: rotate(90deg);
 }
 
-// Popout menu
-.block-editor-block-settings-menu__popover {
-	&::before,
-	&::after {
-		margin-left: 2px;
-	}
-
-	.block-editor-block-settings-menu__content {
-		padding: ($grid-size - $border-width) 0;
-	}
-
-	.block-editor-block-settings-menu__separator {
-		margin-top: $grid-size;
-		margin-bottom: $grid-size;
-		margin-left: 0;
-		margin-right: 0;
-		border-top: $border-width solid $light-gray-500;
-
-		// Check if the separator is the last child in the node and if so, hide itself
-		&:last-child {
-			display: none;
-		}
-	}
-
-	.block-editor-block-settings-menu__title {
-		display: block;
-		padding: 6px;
-		color: $dark-gray-300;
-	}
-
-	// Menu items
-	.block-editor-block-settings-menu__control {
-		width: 100%;
-		justify-content: flex-start;
-		background: none;
-		outline: none;
-		border-radius: 0;
-		color: $dark-gray-500;
-		text-align: left;
-		cursor: pointer;
-		@include menu-style__neutral;
-
-		&:hover:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__hover;
-		}
-
-		&:focus:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__focus;
-		}
+.block-editor-block-settings-menu .components-icon-button.has-text svg {
+	margin-right: 0;
+}
 
-		.dashicon {
-			margin-right: 5px;
-		}
-	}
+.block-editor-block-settings-menu .components-dropdown-menu__indicator {
+	display: none;
```

**_Needs Design Feedback_**

Note: It strikes me as worth exploring further why an indicator (downward caret) should be a default behavior of a DropdownMenu if we then choose to hide it in specific circumstances. Should it be shown for block settings? Should it not be shown for any dropdown menu? Should it be a proper option of a DropdownMenu (a prop)?

What goes into the decision to say that the block settings menu shouldn't have the indicator? Is it the fact that an ellipsis is enough of an indicator in its own right to hint that there's an expanded set of actions?

#### gziolo on 2019-04-15T11:26:17Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r275315566
Path: packages/block-editor/src/components/block-settings-menu/style.scss

```diff
@@ -1,59 +1,11 @@
-.block-editor-block-settings-menu__toggle .dashicon {
+.block-editor-block-settings-menu .components-dropdown-menu__toggle .dashicon {
 	transform: rotate(90deg);
 }
 
-// Popout menu
-.block-editor-block-settings-menu__popover {
-	&::before,
-	&::after {
-		margin-left: 2px;
-	}
-
-	.block-editor-block-settings-menu__content {
-		padding: ($grid-size - $border-width) 0;
-	}
-
-	.block-editor-block-settings-menu__separator {
-		margin-top: $grid-size;
-		margin-bottom: $grid-size;
-		margin-left: 0;
-		margin-right: 0;
-		border-top: $border-width solid $light-gray-500;
-
-		// Check if the separator is the last child in the node and if so, hide itself
-		&:last-child {
-			display: none;
-		}
-	}
-
-	.block-editor-block-settings-menu__title {
-		display: block;
-		padding: 6px;
-		color: $dark-gray-300;
-	}
-
-	// Menu items
-	.block-editor-block-settings-menu__control {
-		width: 100%;
-		justify-content: flex-start;
-		background: none;
-		outline: none;
-		border-radius: 0;
-		color: $dark-gray-500;
-		text-align: left;
-		cursor: pointer;
-		@include menu-style__neutral;
-
-		&:hover:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__hover;
-		}
-
-		&:focus:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__focus;
-		}
+.block-editor-block-settings-menu .components-icon-button.has-text svg {
+	margin-right: 0;
+}
 
-		.dashicon {
-			margin-right: 5px;
-		}
-	}
+.block-editor-block-settings-menu .components-dropdown-menu__indicator {
+	display: none;
```

I updated the code to display this dropdown when the icon name isn't `ellipsis` which isn't great as well. This still needs to be addressed.

### Review thread 4

#### gziolo on 2019-04-08T08:46:25Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r272940807
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
-
-						return (
-							<Toolbar controls={ [ {
-								icon: 'ellipsis',
-								title: label,
-								onClick: () => {
-									if ( count === 1 ) {
-										onSelect( firstBlockClientId );
-									}
-									onToggle();
-								},
-								className: toggleClassname,
-								extraProps: { 'aria-expanded': isOpen },
-							} ] } />
-						);
-					} }
-					renderContent={ ( { onClose } ) => (
-						<NavigableMenu className="editor-block-settings-menu__content block-editor-block-settings-menu__content">
-							<__experimentalBlockSettingsMenuFirstItem.Slot fillProps={ { onClose } } />
-							{ count === 1 && (
-								<BlockUnknownConvertButton
-									clientId={ firstBlockClientId }
-								/>
-							) }
-							{ count === 1 && (
-								<BlockHTMLConvertButton
-									clientId={ firstBlockClientId }
+				<Toolbar>
+					<DropdownMenu
+						icon="ellipsis"
+						label={ __( 'Block options' ) }
+						position="bottom right"
+						className="block-editor-block-settings-menu"
+					>
+						{ ( { onClose } ) => (
+							<Fragment>
+								<__experimentalBlockSettingsMenuFirstItem.Slot
+									fillProps={ { onClose } }
 								/>
-							) }
-							{ ! isLocked && canDuplicate && (
-								<MenuItem
-									className="editor-block-settings-menu__control block-editor-block-settings-menu__control"
-									onClick={ onDuplicate }
-									icon="admin-page"
-									shortcut={ shortcuts.duplicate.display }
-								>
-									{ __( 'Duplicate' ) }
-								</MenuItem>
-							) }
-							{ ! isLocked && (
-								<Fragment>
+								{ count === 1 && (
+									<BlockUnknownConvertButton
+										clientId={ firstBlockClientId }
+									/>
+								) }
+								{ count === 1 && (
+									<BlockHTMLConvertButton
+										clientId={ firstBlockClientId }
+									/>
+								) }
+								{ ! isLocked && canDuplicate && (
 									<MenuItem
-										className="editor-block-settings-menu__control block-editor-block-settings-menu__control"
-										onClick={ onInsertBefore }
-										icon="insert-before"
-										shortcut={ shortcuts.insertBefore.display }
+										onClick={ onDuplicate }
+										icon="admin-page"
+										shortcut={ shortcuts.duplicate.display }
 									>
-										{ __( 'Insert Before' ) }
+										{ __( 'Duplicate' ) }
 									</MenuItem>
+								) }
+								{ ! isLocked && (
+									<Fragment>
+										<MenuItem
+											onClick={ onInsertBefore }
+											icon="insert-before"
+											shortcut={ shortcuts.insertBefore.display }
+										>
+											{ __( 'Insert Before' ) }
+										</MenuItem>
+										<MenuItem
+											onClick={ onInsertAfter }
+											icon="insert-after"
+											shortcut={ shortcuts.insertAfter.display }
+										>
+											{ __( 'Insert After' ) }
+										</MenuItem>
+									</Fragment>
+								) }
+								{ count === 1 && (
+									<BlockModeToggle
+										clientId={ firstBlockClientId }
+										onToggle={ onClose }
+									/>
+								) }
+								<__experimentalBlockSettingsMenuPluginsExtension.Slot
+									fillProps={ { clientIds, onClose } }
+								/>
+								<DropdownMenuSeparator />
```

I have some concerns about this component which is 100% visual and won't be noticeable for those having sight issues. The question is whether this separator exists only for better aesthetics or should menu groups be used instead?

#### aduth on 2019-04-08T12:58:14Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r273031796
Path: packages/block-editor/src/components/block-settings-menu/index.js

```diff
@@ -23,113 +26,89 @@ import BlockUnknownConvertButton from './block-unknown-convert-button';
 import __experimentalBlockSettingsMenuFirstItem from './block-settings-menu-first-item';
 import __experimentalBlockSettingsMenuPluginsExtension from './block-settings-menu-plugins-extension';
 
-export function BlockSettingsMenu( { clientIds, onSelect } ) {
+export function BlockSettingsMenu( { clientIds } ) {
 	const blockClientIds = castArray( clientIds );
 	const count = blockClientIds.length;
 	const firstBlockClientId = blockClientIds[ 0 ];
 
 	return (
 		<BlockActions clientIds={ clientIds }>
 			{ ( { onDuplicate, onRemove, onInsertAfter, onInsertBefore, canDuplicate, isLocked } ) => (
-				<Dropdown
-					contentClassName="editor-block-settings-menu__popover block-editor-block-settings-menu__popover"
-					position="bottom right"
-					renderToggle={ ( { onToggle, isOpen } ) => {
-						const toggleClassname = classnames( 'editor-block-settings-menu__toggle block-editor-block-settings-menu__toggle', {
-							'is-opened': isOpen,
-						} );
-						const label = isOpen ? __( 'Hide options' ) : __( 'More options' );
-
-						return (
-							<Toolbar controls={ [ {
-								icon: 'ellipsis',
-								title: label,
-								onClick: () => {
-									if ( count === 1 ) {
-										onSelect( firstBlockClientId );
-									}
-									onToggle();
-								},
-								className: toggleClassname,
-								extraProps: { 'aria-expanded': isOpen },
-							} ] } />
-						);
-					} }
-					renderContent={ ( { onClose } ) => (
-						<NavigableMenu className="editor-block-settings-menu__content block-editor-block-settings-menu__content">
-							<__experimentalBlockSettingsMenuFirstItem.Slot fillProps={ { onClose } } />
-							{ count === 1 && (
-								<BlockUnknownConvertButton
-									clientId={ firstBlockClientId }
-								/>
-							) }
-							{ count === 1 && (
-								<BlockHTMLConvertButton
-									clientId={ firstBlockClientId }
+				<Toolbar>
+					<DropdownMenu
+						icon="ellipsis"
+						label={ __( 'Block options' ) }
+						position="bottom right"
+						className="block-editor-block-settings-menu"
+					>
+						{ ( { onClose } ) => (
+							<Fragment>
+								<__experimentalBlockSettingsMenuFirstItem.Slot
+									fillProps={ { onClose } }
 								/>
-							) }
-							{ ! isLocked && canDuplicate && (
-								<MenuItem
-									className="editor-block-settings-menu__control block-editor-block-settings-menu__control"
-									onClick={ onDuplicate }
-									icon="admin-page"
-									shortcut={ shortcuts.duplicate.display }
-								>
-									{ __( 'Duplicate' ) }
-								</MenuItem>
-							) }
-							{ ! isLocked && (
-								<Fragment>
+								{ count === 1 && (
+									<BlockUnknownConvertButton
+										clientId={ firstBlockClientId }
+									/>
+								) }
+								{ count === 1 && (
+									<BlockHTMLConvertButton
+										clientId={ firstBlockClientId }
+									/>
+								) }
+								{ ! isLocked && canDuplicate && (
 									<MenuItem
-										className="editor-block-settings-menu__control block-editor-block-settings-menu__control"
-										onClick={ onInsertBefore }
-										icon="insert-before"
-										shortcut={ shortcuts.insertBefore.display }
+										onClick={ onDuplicate }
+										icon="admin-page"
+										shortcut={ shortcuts.duplicate.display }
 									>
-										{ __( 'Insert Before' ) }
+										{ __( 'Duplicate' ) }
 									</MenuItem>
+								) }
+								{ ! isLocked && (
+									<Fragment>
+										<MenuItem
+											onClick={ onInsertBefore }
+											icon="insert-before"
+											shortcut={ shortcuts.insertBefore.display }
+										>
+											{ __( 'Insert Before' ) }
+										</MenuItem>
+										<MenuItem
+											onClick={ onInsertAfter }
+											icon="insert-after"
+											shortcut={ shortcuts.insertAfter.display }
+										>
+											{ __( 'Insert After' ) }
+										</MenuItem>
+									</Fragment>
+								) }
+								{ count === 1 && (
+									<BlockModeToggle
+										clientId={ firstBlockClientId }
+										onToggle={ onClose }
+									/>
+								) }
+								<__experimentalBlockSettingsMenuPluginsExtension.Slot
+									fillProps={ { clientIds, onClose } }
+								/>
+								<DropdownMenuSeparator />
```

> I have some concerns about this component which is 100% visual and won't be noticeable for those having sight issues. The question is whether this separator exists only for better aesthetics or should menu groups be used instead?

It's a very good point. Between your comment and #12505, it seems clear that proper groupings would have preferable semantics than what can be communicated with this line. I could see an attempt at an argument that some lines are truly intended to be purely visual flourishes, though it seems a weak one and certainly one which should be limited, if not actively discouraged. At the very least, it's not clear to me why not to use a proper [`hr` element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/hr) over the existing `div`.

We have the [`MenuGroup` component](https://github.com/WordPress/gutenberg/tree/master/packages/components/src/menu-group) which might serve some help here, though it seems to assume a label not currently present in the block settings menu.

The more I look at [the menu entries](https://user-images.githubusercontent.com/1779930/55725309-693fa780-59db-11e9-833d-164b5094e7bc.png) and trying to consider what these groupings might be, the more I wonder if Remove Block really is in any way of a different classification than the other items, or if the line truly does just serve as visually emphasis (for its frequency? as an indicator of danger?).

Action items:

- Don't expose this as a publicly-available component
- Decide whether `MenuGroup` is possible, recommended (advice welcome)

### Review thread 5 (resolved)

#### gziolo on 2019-04-12T13:36:11Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r274908421
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

I think I added `contentClassName` and `labelPosition` as well. This needs to be reviewed as I brought them for backward compatibility.

#### gziolo on 2019-04-15T13:51:00Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r275369685
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

The current list of new props which might need to be documented:
- `labelPosition`
- `menuClassName`
- `popoverClassName`
- `toggleClassName`

All 3 class names props exist only for backward compatibility. I'm hesitant whether we should document it all.

#### aduth on 2019-04-17T20:50:12Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276428231
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

Interesting, it hadn't occurred to me the need to provide customization options for the rendered toggle. My next thought was whether we could just expose `renderToggle` as a render prop to be customized by the consuming renderer. I guess the problem with that is how to manage on their behalf some of the best practice behaviors (i.e. ArrowDown to open), as I don't think it would be wise to leave open the option that this be possible to overlook (footgun).

#### aduth on 2019-04-17T20:55:09Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276429937
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

Understanding the desire to retain existing behaviors, I do wonder how we could limit or consolidate the customization options. Something like `labelPosition`, for example, seems to only exist where otherwise if we allow the standard "top" behavior to apply for the "More Menu", it might escape the page in a way which is undesirable. But it's the sort of thing we try to build-in to the behavior of Popover (auto-reverse when otherwise the popover would exceed bounds), so it seems like something which should be programmatically determined.

#### gziolo on 2019-04-18T19:11:06Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276797203
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

Yes, I agree that we should hide as much as possible from public API if it is only possible 👍
I’m going to give it more thoughts with fresh mind.

#### gziolo on 2019-05-10T14:54:48Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r282918967
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

How about we prefix all of them with `__untable` to conclude this PR and we attack it later with some nice automation as discussed above. I don't feel good about blocking this PR as it's solving a couple of issues at once and gives a better chance to tackle further a11y issues.

#### aduth on 2019-05-10T14:58:08Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r282920379
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

> How about we prefix all of them with `__untable` to conclude this PR and we attack it later with some nice automation as discussed above. I don't feel good about blocking this PR as it's solving a couple of issues at once and gives a better chance to tackle further a11y issues.

I would be okay with this 👍

#### gziolo on 2019-05-14T11:53:28Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r283758432
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
```

Applied in c25dc8a.

### Review thread 6 (resolved)

#### gziolo on 2019-04-12T13:37:00Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r274908749
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
 	menuLabel,
-	controls,
-	className,
 	position,
 } ) {
-	if ( ! controls || ! controls.length ) {
+	if ( isEmpty( controls ) && ! isFunction( children ) ) {
 		return null;
 	}
 
 	// Normalize controls to nested array of objects (sets of controls)
-	let controlSets = controls;
-	if ( ! Array.isArray( controlSets[ 0 ] ) ) {
-		controlSets = [ controlSets ];
+	let controlSets;
+	if ( ! isEmpty( controls ) ) {
+		controlSets = controls;
+		if ( ! Array.isArray( controlSets[ 0 ] ) ) {
+			controlSets = [ controlSets ];
+		}
 	}
 
 	return (
 		<Dropdown
 			className={ classnames( 'components-dropdown-menu', className ) }
-			contentClassName="components-dropdown-menu__popover"
+			contentClassName={ classnames( 'components-dropdown-menu__popover', contentClassName ) }
 			position={ position }
 			renderToggle={ ( { isOpen, onToggle } ) => {
+				const buttonLabel = isFunction( label ) ? label( isOpen ) : label;
```

A workaround to allow providing different labels depending on the `isOpen` value.

#### gziolo on 2019-04-15T13:58:52Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r275373597
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -12,34 +12,42 @@ import { DOWN } from '@wordpress/keycodes';
 /**
  * Internal dependencies
  */
+import DropdownMenuSeparator from './separator';
 import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
 function DropdownMenu( {
+	contentClassName,
+	children,
+	className,
+	controls,
 	icon = 'menu',
 	label,
+	labelPosition,
 	menuLabel,
-	controls,
-	className,
 	position,
 } ) {
-	if ( ! controls || ! controls.length ) {
+	if ( isEmpty( controls ) && ! isFunction( children ) ) {
 		return null;
 	}
 
 	// Normalize controls to nested array of objects (sets of controls)
-	let controlSets = controls;
-	if ( ! Array.isArray( controlSets[ 0 ] ) ) {
-		controlSets = [ controlSets ];
+	let controlSets;
+	if ( ! isEmpty( controls ) ) {
+		controlSets = controls;
+		if ( ! Array.isArray( controlSets[ 0 ] ) ) {
+			controlSets = [ controlSets ];
+		}
 	}
 
 	return (
 		<Dropdown
 			className={ classnames( 'components-dropdown-menu', className ) }
-			contentClassName="components-dropdown-menu__popover"
+			contentClassName={ classnames( 'components-dropdown-menu__popover', contentClassName ) }
 			position={ position }
 			renderToggle={ ( { isOpen, onToggle } ) => {
+				const buttonLabel = isFunction( label ) ? label( isOpen ) : label;
```

This is no longer the case.

### Review thread 7 (resolved)

#### gziolo on 2019-04-12T13:38:44Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r274909402
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -55,27 +63,33 @@ function DropdownMenu( {
 						onKeyDown={ openOnArrowDown }
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
-						label={ label }
-						tooltip={ label }
+						label={ buttonLabel }
+						labelPosition={ labelPosition }
+						tooltip={ buttonLabel }
 					>
-						<span className="components-dropdown-menu__indicator" />
+						{ icon !== 'ellipsis' && <span className="components-dropdown-menu__indicator" /> }
```

It feels like `ellipsis` should be the default icon. It doesn't make sense to render an indicator when the ellipsis is selected as an icon. I'm not happy about this solution but otherwise, we will have to hide `components-dropdown-menu__indicator` with CSS in 2 places.

#### aduth on 2019-04-17T20:48:29Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276427629
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -55,27 +63,33 @@ function DropdownMenu( {
 						onKeyDown={ openOnArrowDown }
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
-						label={ label }
-						tooltip={ label }
+						label={ buttonLabel }
+						labelPosition={ labelPosition }
+						tooltip={ buttonLabel }
 					>
-						<span className="components-dropdown-menu__indicator" />
+						{ icon !== 'ellipsis' && <span className="components-dropdown-menu__indicator" /> }
```

> It feels like `ellipsis` should be the default icon. It doesn't make sense to render an indicator when the ellipsis is selected as an icon. I'm not happy about this solution but otherwise, we will have to hide `components-dropdown-menu__indicator` with CSS in 2 places.

From https://github.com/WordPress/gutenberg/pull/14843#discussion_r272653497, I think it could be reasonable with design confirmation that there's a redundancy between the ellipsis and indicator which makes it reasonable to consolidate to one (i.e. hide the indicator by code when the icon is `ellipsis`).

#### jasmussen on 2019-05-14T14:14:52Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r283820788
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -55,27 +63,33 @@ function DropdownMenu( {
 						onKeyDown={ openOnArrowDown }
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
-						label={ label }
-						tooltip={ label }
+						label={ buttonLabel }
+						labelPosition={ labelPosition }
+						tooltip={ buttonLabel }
 					>
-						<span className="components-dropdown-menu__indicator" />
+						{ icon !== 'ellipsis' && <span className="components-dropdown-menu__indicator" /> }
```

From a brief chat with @gziolo, I _think_ the current behavior is right.

Although there doesn't appear to be any super firm guidelines for a canonical "dropdown menu icon", commonly it _is_ the triangle. [Material uses it a lot](https://material.io/design/components/menus.html#usage), at least. The ellipsis, however, is the exception. It should be used when you know what you're doing, because the ellipsis is "overflow", so it's:

- always at the end of a series of buttons
- there's only one in that series of buttons

So it seems like defaulting to the triangle but allowing the user to feed it another icon like the ellipsis, is the right behavior.

#### aduth on 2019-05-29T18:19:01Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288704952
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -55,27 +63,33 @@ function DropdownMenu( {
 						onKeyDown={ openOnArrowDown }
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
-						label={ label }
-						tooltip={ label }
+						label={ buttonLabel }
+						labelPosition={ labelPosition }
+						tooltip={ buttonLabel }
 					>
-						<span className="components-dropdown-menu__indicator" />
+						{ icon !== 'ellipsis' && <span className="components-dropdown-menu__indicator" /> }
```

So, this is generally agreeable to me. But I wonder if we should be considering this in a more formalized way than an ad hoc exception. For example, I could imagine this would be sensible if the _default_ `icon` for this component was `'ellipsis'`, and the separator was included as an expression of "If you're not using the safe default, we'll assume you need the indicator". But the default is currently `'menu'`, which itself is a concern as well, since I think the same justification could be applied to the [menu icon](https://developer.wordpress.org/resource/dashicons/#menu) as not needing the indicator.

A few options I might propose:

- Collect a whitelist of icons we think do not need to have the indicator assigned
- Make the exemption explicit, so that the consumer must define whether it is or isn't to be shown

#### gziolo on 2019-05-30T14:32:48Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r289016585
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -55,27 +63,33 @@ function DropdownMenu( {
 						onKeyDown={ openOnArrowDown }
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
-						label={ label }
-						tooltip={ label }
+						label={ buttonLabel }
+						labelPosition={ labelPosition }
+						tooltip={ buttonLabel }
 					>
-						<span className="components-dropdown-menu__indicator" />
+						{ icon !== 'ellipsis' && <span className="components-dropdown-menu__indicator" /> }
```

https://github.com/WordPress/gutenberg/blob/master/packages/block-editor/src/components/rich-text/format-toolbar/index.js#L23-L28

It looks like we display indicator only when `false` is explicitly set for the `icon` prop. I checked all other occurrences in our code. It probably should be the default icon but as you already pointed out, let's not introduce breaking changes. I will adjust the code to check against falseness of `icon` to move forward and make the implementation simpler.

#### gziolo on 2019-05-30T14:35:35Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r289017970
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -55,27 +63,33 @@ function DropdownMenu( {
 						onKeyDown={ openOnArrowDown }
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
-						label={ label }
-						tooltip={ label }
+						label={ buttonLabel }
+						labelPosition={ labelPosition }
+						tooltip={ buttonLabel }
 					>
-						<span className="components-dropdown-menu__indicator" />
+						{ icon !== 'ellipsis' && <span className="components-dropdown-menu__indicator" /> }
```

Fixed in https://github.com/WordPress/gutenberg/pull/14843/commits/032167abd4a4e61be165fee0344e9bfdfdf9499a.

### Review thread 8

#### gziolo on 2019-04-15T13:36:56Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r275363244
Path: packages/block-editor/src/components/block-settings-menu/style.scss

```diff
@@ -1,59 +1,7 @@
-.block-editor-block-settings-menu__toggle .dashicon {
+.block-editor-block-settings-menu .components-dropdown-menu__toggle .dashicon {
 	transform: rotate(90deg);
 }
 
-// Popout menu
-.block-editor-block-settings-menu__popover {
-	&::before,
-	&::after {
-		margin-left: 2px;
-	}
-
-	.block-editor-block-settings-menu__content {
-		padding: ($grid-size - $border-width) 0;
-	}
-
-	.block-editor-block-settings-menu__separator {
-		margin-top: $grid-size;
-		margin-bottom: $grid-size;
-		margin-left: 0;
-		margin-right: 0;
-		border-top: $border-width solid $light-gray-500;
-
-		// Check if the separator is the last child in the node and if so, hide itself
-		&:last-child {
-			display: none;
-		}
-	}
-
-	.block-editor-block-settings-menu__title {
-		display: block;
-		padding: 6px;
-		color: $dark-gray-300;
-	}
-
-	// Menu items
-	.block-editor-block-settings-menu__control {
-		width: 100%;
-		justify-content: flex-start;
-		background: none;
-		outline: none;
-		border-radius: 0;
-		color: $dark-gray-500;
-		text-align: left;
-		cursor: pointer;
-		@include menu-style__neutral;
-
-		&:hover:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__hover;
-		}
-
-		&:focus:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__focus;
-		}
-
-		.dashicon {
-			margin-right: 5px;
-		}
-	}
+.block-editor-block-settings-menu .components-icon-button.has-text svg {
```

What is the purpose of this rule? I can't see it applied.

#### aduth on 2019-04-17T20:42:05Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276425204
Path: packages/block-editor/src/components/block-settings-menu/style.scss

```diff
@@ -1,59 +1,7 @@
-.block-editor-block-settings-menu__toggle .dashicon {
+.block-editor-block-settings-menu .components-dropdown-menu__toggle .dashicon {
 	transform: rotate(90deg);
 }
 
-// Popout menu
-.block-editor-block-settings-menu__popover {
-	&::before,
-	&::after {
-		margin-left: 2px;
-	}
-
-	.block-editor-block-settings-menu__content {
-		padding: ($grid-size - $border-width) 0;
-	}
-
-	.block-editor-block-settings-menu__separator {
-		margin-top: $grid-size;
-		margin-bottom: $grid-size;
-		margin-left: 0;
-		margin-right: 0;
-		border-top: $border-width solid $light-gray-500;
-
-		// Check if the separator is the last child in the node and if so, hide itself
-		&:last-child {
-			display: none;
-		}
-	}
-
-	.block-editor-block-settings-menu__title {
-		display: block;
-		padding: 6px;
-		color: $dark-gray-300;
-	}
-
-	// Menu items
-	.block-editor-block-settings-menu__control {
-		width: 100%;
-		justify-content: flex-start;
-		background: none;
-		outline: none;
-		border-radius: 0;
-		color: $dark-gray-500;
-		text-align: left;
-		cursor: pointer;
-		@include menu-style__neutral;
-
-		&:hover:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__hover;
-		}
-
-		&:focus:not(:disabled):not([aria-disabled="true"]) {
-			@include menu-style__focus;
-		}
-
-		.dashicon {
-			margin-right: 5px;
-		}
-	}
+.block-editor-block-settings-menu .components-icon-button.has-text svg {
```

> What is the purpose of this rule? I can't see it applied.

Hmm, is it something I had added? Honestly, I cannot recall if or why it would be needed.

Considering that the `IconButton` cannot be a direct descendent of the `.block-editor-block-settings-menu` (due to portaling of the popover content), I don't think it's possible that this would be applied. I suspect then it should be safe to remove.

### Review thread 9

#### aduth on 2019-04-17T20:51:21Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276428634
Path: packages/components/src/index.js

```diff
@@ -18,7 +18,7 @@ export { default as Draggable } from './draggable';
 export { default as DropZone } from './drop-zone';
 export { default as DropZoneProvider } from './drop-zone/provider';
 export { default as Dropdown } from './dropdown';
-export { default as DropdownMenu } from './dropdown-menu';
+export { default as DropdownMenu, DropdownMenuSeparator } from './dropdown-menu';
```

I'm not sure if you had left it as a remaining task, but I'm expecting we don't want to be exposing this, based on prior remarks about semantics of menu groupings.

#### gziolo on 2019-04-18T19:06:53Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276795878
Path: packages/components/src/index.js

```diff
@@ -18,7 +18,7 @@ export { default as Draggable } from './draggable';
 export { default as DropZone } from './drop-zone';
 export { default as DropZoneProvider } from './drop-zone/provider';
 export { default as Dropdown } from './dropdown';
-export { default as DropdownMenu } from './dropdown-menu';
+export { default as DropdownMenu, DropdownMenuSeparator } from './dropdown-menu';
```

Yes. I must have missed this one. I focused my efforts on bringing backward compatibility for More Menu.

### Review thread 10 (resolved)

#### aduth on 2019-04-17T20:57:02Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276430594
Path: packages/components/src/menu-group/index.js

```diff
@@ -33,11 +28,12 @@ export function MenuGroup( {
 	return (
 		<div className={ classNames }>
 			{ label &&
-				<div className="components-menu-group__label" id={ labelId }>{ label }</div>
+				<div
+					className="components-menu-group__label" id={ labelId } aria-hidden="true">{ label }</div>
```

Unsure if by the indented newline you had planned to split this across multiple lines? Seems like it could be an improvement for line length readability.

```suggestion
					className="components-menu-group__label"
					id={ labelId }
					aria-hidden="true"
				>
					{ label }
				</div>
```

#### gziolo on 2019-04-18T19:05:29Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r276795435
Path: packages/components/src/menu-group/index.js

```diff
@@ -33,11 +28,12 @@ export function MenuGroup( {
 	return (
 		<div className={ classNames }>
 			{ label &&
-				<div className="components-menu-group__label" id={ labelId }>{ label }</div>
+				<div
+					className="components-menu-group__label" id={ labelId } aria-hidden="true">{ label }</div>
```

👍

### Review thread 11 (resolved)

#### aduth on 2019-05-29T18:24:19Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288707187
Path: packages/components/src/menu-group/index.js

```diff
@@ -33,11 +28,17 @@ export function MenuGroup( {
 	return (
 		<div className={ classNames }>
 			{ label &&
-				<div className="components-menu-group__label" id={ labelId }>{ label }</div>
+				<div
+					className="components-menu-group__label"
+					id={ labelId }
+					aria-hidden="true"
```

I'm not really sure at what point this attribute was added. Do you recall the rationale? In any case, from testing in screen reader, it doesn't seem to affect whether the inputs in the group are announced as being associated with the group (Chrome + macOS Voiceover).

![image](https://user-images.githubusercontent.com/1779930/58581374-66b04000-821d-11e9-9c1c-c04e683897fa.png)

#### afercia on 2019-05-29T19:52:53Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288740521
Path: packages/components/src/menu-group/index.js

```diff
@@ -33,11 +28,17 @@ export function MenuGroup( {
 	return (
 		<div className={ classNames }>
 			{ label &&
-				<div className="components-menu-group__label" id={ labelId }>{ label }</div>
+				<div
+					className="components-menu-group__label"
+					id={ labelId }
+					aria-hidden="true"
```

I guess this was added to avoid a double announcement. The group _should_  be announced with its name because `aria-labelledby` targets the visible label. Actually, checking this kind of stuff in the Chrome dev tools is now pretty easy. Go in Elements > right panel > accessibility tab, select the element and check its accessible name. It also tells you where the accessible name comes from.

Should be tested with other screen readers on Windows but looks good to me at a first glance.

### Review thread 12 (resolved)

#### aduth on 2019-05-29T18:30:56Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288710055
Path: packages/edit-post/src/components/header/more-menu/index.js

```diff
@@ -13,24 +13,16 @@ import ToolsMoreMenuGroup from '../tools-more-menu-group';
 import OptionsMenuItem from '../options-menu-item';
 import WritingMenu from '../writing-menu';
 
-const ariaClosed = __( 'Show more tools & options' );
```

I looked back to when this was introduced in #10900, and the related issue #7203.

@anevins12 Do you know if the opposing action states would be necessary to describe by a label, considering the button's role as a pop-up button? The button is [rendered with both `aria-haspopup="true"` and `aria-expanded={ isOpen }`](https://github.com/WordPress/gutenberg/blob/dda829527618a4a75a323b1db13b4a695a588b3e/packages/components/src/dropdown-menu/index.js#L67-L68), which seems like it would make these labels redundant. Or should a developer be required to provide both of these labels when rendering any DropdownMenu ?

#### anevins12 on 2019-05-29T18:46:32Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288716599
Path: packages/edit-post/src/components/header/more-menu/index.js

```diff
@@ -13,24 +13,16 @@ import ToolsMoreMenuGroup from '../tools-more-menu-group';
 import OptionsMenuItem from '../options-menu-item';
 import WritingMenu from '../writing-menu';
 
-const ariaClosed = __( 'Show more tools & options' );
```

I think implementing `aria-haspopup` and `aria-expanded` would convey the "Show" bit of the label, but I do think the "more tools & options" bit is still valuable.

#### afercia on 2019-05-29T20:05:27Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288745255
Path: packages/edit-post/src/components/header/more-menu/index.js

```diff
@@ -13,24 +13,16 @@ import ToolsMoreMenuGroup from '../tools-more-menu-group';
 import OptionsMenuItem from '../options-menu-item';
 import WritingMenu from '../writing-menu';
 
-const ariaClosed = __( 'Show more tools & options' );
```

I'd agree.
- `aria-haspopup` is a property (doesn't change dynamically) and informs users there's something to expand, should be used only for controls that require user activation 
- `aria-expanded` is a state (does change dynamically) and informs users on the "expanded state"
- "more tools & options" helps in differentiating this button from other "more" buttons but it's a bit longish and I'd consider to simplify. Also, controls names shouldn't change dynamically: they represent _what_ the control is about and should not represent the _state_. I'd reconsider it and simplify to "Tools & options" with no dynamic change. Better wording welcome!

Aside: applies also the block toolbar: it's a "More options/Hide options" which is slightly confusing and inconsistent.

#### aduth on 2019-05-29T21:00:48Z

URL: https://github.com/WordPress/gutenberg/pull/14843#discussion_r288766623
Path: packages/edit-post/src/components/header/more-menu/index.js

```diff
@@ -13,24 +13,16 @@ import ToolsMoreMenuGroup from '../tools-more-menu-group';
 import OptionsMenuItem from '../options-menu-item';
 import WritingMenu from '../writing-menu';
 
-const ariaClosed = __( 'Show more tools & options' );
```

> I do think the "more tools & options" bit is still valuable.

Yes, to clarify, the label "More tools & options" remains ([source](https://github.com/WordPress/gutenberg/blob/dda829527618a4a75a323b1db13b4a695a588b3e/packages/edit-post/src/components/header/more-menu/index.js#L21)). As proposed, it will no longer switch between "Show more tools & options" and "Hide more tools & options".

>Also, controls names shouldn't change dynamically: they represent _what_ the control is about and should not represent the _state_.  I'd reconsider it and simplify to "Tools & options" with no dynamic change. Better wording welcome!

Thanks, that's exactly what I was hoping. The further simplification seems reasonable to me as well.

## Changed files

- packages/block-editor/CHANGELOG.md (MODIFIED, +6/-0)
- packages/block-editor/src/components/block-settings-menu/index.js (MODIFIED, +90/-101)
- packages/block-editor/src/components/block-settings-menu/style.scss (MODIFIED, +4/-56)
- packages/components/CHANGELOG.md (MODIFIED, +10/-0)
- packages/components/src/dropdown-menu/README.md (MODIFIED, +75/-1)
- packages/components/src/dropdown-menu/index.js (MODIFIED, +31/-14)
- packages/components/src/dropdown-menu/style.scss (MODIFIED, +16/-2)
- packages/components/src/dropdown-menu/test/index.js (MODIFIED, +36/-29)
- packages/components/src/menu-group/index.js (MODIFIED, +9/-8)
- packages/components/src/menu-group/test/__snapshots__/index.js.snap (MODIFIED, +4/-3)
- packages/components/src/menu-item/style.scss (MODIFIED, +1/-1)
- packages/e2e-test-utils/src/click-on-more-menu-item.js (MODIFIED, +1/-1)
- packages/e2e-test-utils/src/switch-editor-mode-to.js (MODIFIED, +1/-1)
- packages/e2e-tests/specs/adding-blocks.test.js (MODIFIED, +1/-1)
- packages/e2e-tests/specs/block-deletion.test.js (MODIFIED, +2/-1)
- packages/e2e-tests/specs/blocks/preformatted.test.js (MODIFIED, +3/-6)
- packages/e2e-tests/specs/editor-modes.test.js (MODIFIED, +6/-9)
- packages/e2e-tests/specs/keyboard-navigable-blocks.test.js (MODIFIED, +1/-3)
- packages/e2e-tests/specs/nux.test.js (MODIFIED, +2/-2)
- packages/e2e-tests/specs/plugins/annotations.test.js (MODIFIED, +2/-1)
- packages/e2e-tests/specs/reusable-blocks.test.js (MODIFIED, +4/-5)
- packages/edit-post/src/components/header/more-menu/index.js (MODIFIED, +9/-17)
- packages/edit-post/src/components/header/more-menu/style.scss (MODIFIED, +2/-11)
- packages/edit-post/src/components/header/more-menu/test/__snapshots__/index.js.snap (MODIFIED, +73/-56)
- packages/edit-post/src/components/header/plugin-more-menu-item/test/__snapshots__/index.js.snap (MODIFIED, +2/-3)
