---
number: 58459
type: pull_request
state: MERGED
merged_at: 2024-02-08T17:55:29Z
url: https://github.com/WordPress/gutenberg/pull/58459
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #58459: FocalPointPicker: Apply modern styling

- URL: https://github.com/WordPress/gutenberg/pull/58459
- Author: richtabor
- Created: 2024-01-30T16:10:10Z
- Updated: 2024-02-08T17:55:55Z
- Comments: 8 of 8
- Review threads: 10 of 10
- Changed files: 3 of 3
- Diff: ../pr-diffs/58459.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?


## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
1. Open a post or page. 
2. Insert a media and text block.
3. Add an image. 
4. Open block inspector.
5. See changes. 

## Screenshots or screencast <!-- if applicable -->

### Current: 

https://github.com/WordPress/gutenberg/assets/1813435/cf268f24-6ae1-4b5e-b7f0-5325f75bffd8

### Proposed: 

https://github.com/WordPress/gutenberg/assets/1813435/b43d4b61-0332-45bd-a976-94db2f924410

## Issue comments

### jasmussen on 2024-01-31T09:38:53Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1918732992

Looks good!

### github-actions on 2024-02-05T15:35:26Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1927273667

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Core SVN

Core Committers: Use this line as a base for the props when committing in SVN:
```
Props richtabor, wildworks, mciampini, joen.
```

## GitHub Merge commits

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: richtabor <richtabor@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: jasmussen <joen@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### richtabor on 2024-02-06T19:38:13Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1930627893

> Depending on the color of the background, the pointer becomes very difficult to see. How about restoring the pointer outline, perhaps the `blur` value?

I made a small adjustment here: https://github.com/WordPress/gutenberg/pull/58459/commits/a27697c9506a53e1937a397ba7478fa825bd57f2

![CleanShot 2024-02-06 at 14 36 23](https://github.com/WordPress/gutenberg/assets/1813435/7da91e7e-74c4-44fe-89d0-fe1d82d66d0b)

### t-hamano on 2024-02-07T04:43:48Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1931272899

Thank you for the update. The pointer is now very clear, but when I move the pointer over the outline, it seems like there is a lot of blurring. I don't have a strong opinion on this, but what do you think about reducing `blur` from 16px to 8px or 4px?

Current (blur: 16px):

![image](https://github.com/WordPress/gutenberg/assets/54422211/993b4eb1-c9ca-4130-b5e2-2cb884fe7e51)

blur: 8px
![image](https://github.com/WordPress/gutenberg/assets/54422211/8d517dd8-14a7-421e-87c7-d1a19f86ac5a)

blur: 4px

![image](https://github.com/WordPress/gutenberg/assets/54422211/905048ad-72ea-499b-a030-93a914c595ac)

### ciampo on 2024-02-07T11:04:37Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1931805850

I don't have any additional feedback to give on this PR, so I'll let @t-hamano given the final approval here

### richtabor on 2024-02-07T14:48:14Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1932204632

> The pointer is now very clear, but when I move the pointer over the outline, it seems like there is a lot of blurring. I don't have a strong opinion on this, but what do you think about reducing blur from 16px to 8px or 4px?

I considered it, but I picked 16 for two reasons: 

16px blur is used predominantly throughout, and could perhaps be standardized further. And second, I don't think we should allow the picker to go "off canvas", like it does now—I did not want to do that effort as part of this PR. If we don't have that conflict, we don't have the blurred edge issue. 

![CleanShot 2024-02-07 at 09 45 28](https://github.com/WordPress/gutenberg/assets/1813435/78993a11-8573-46ac-88fd-989d357930e9)

### t-hamano on 2024-02-07T15:43:28Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1932322094

> I don't think we should allow the picker to go "off canvas", like it does now—I did not want to do that effort as part of this PR. If we don't have that conflict, we don't have the blurred edge issue.

If you want to indicate a value of 0 or 100 in this component, the picker should come to the contour. Isn't it inevitable that the picker will go off canvas? 🤔

![focal-point](https://github.com/WordPress/gutenberg/assets/54422211/c51c9d6d-74f8-4beb-860f-3284a801af3b)

### richtabor on 2024-02-07T15:50:30Z

URL: https://github.com/WordPress/gutenberg/pull/58459#issuecomment-1932337170

> If you want to indicate a value of 0 or 100 in this component, the picker should come to the contour. Isn't it inevitable that the picker will go off canvas? 🤔

I would think that the first 20px of the bounds would equate to the 0/100% mark (whatever keeps the picker from spilling into the rest of the UI).

## Review threads

### Review thread 1 (resolved)

#### t-hamano on 2024-01-31T10:56:47Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1472655017
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		position: absolute;
+		position: absolute;
```

```suggestion
		position: absolute;
```

### Review thread 2 (resolved)

#### t-hamano on 2024-01-31T10:58:47Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1472657365
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -91,29 +104,29 @@ export const GridView = styled.div`
 	position: absolute;
 	top: 50%;
 	transform: translate3d( -50%, -50%, 0 );
-	transition: opacity 120ms linear;
+	transition: opacity 100ms linear;
```

It would be good to add the `reduceMotion` utility. example: https://github.com/WordPress/gutenberg/blob/46fec08dd7acb054d0a216f64000fb87e8ebddff/packages/components/src/tabs/styles.ts#L70

### Review thread 3 (resolved)

#### t-hamano on 2024-01-31T11:00:19Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1472659182
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -91,29 +104,29 @@ export const GridView = styled.div`
 	position: absolute;
 	top: 50%;
 	transform: translate3d( -50%, -50%, 0 );
-	transition: opacity 120ms linear;
+	transition: opacity 100ms linear;
 	z-index: 1;
 
 	opacity: ${ ( { showOverlay }: { showOverlay?: boolean } ) =>
 		showOverlay ? 1 : 0 };
 `;
 
 export const GridLine = styled.div`
-	background: white;
-	box-shadow: 0 0 2px rgba( 0, 0, 0, 0.6 );
+	background: rgba( 255, 255, 255, 0.4 );
+	backdrop-filter: blur( 16px ) saturate( 200% );
 	position: absolute;
-	opacity: 0.4;
+	opacity: 1;
```

Isn't it okay to delete this property itself?

#### richtabor on 2024-02-05T15:38:48Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478450735
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -91,29 +104,29 @@ export const GridView = styled.div`
 	position: absolute;
 	top: 50%;
 	transform: translate3d( -50%, -50%, 0 );
-	transition: opacity 120ms linear;
+	transition: opacity 100ms linear;
 	z-index: 1;
 
 	opacity: ${ ( { showOverlay }: { showOverlay?: boolean } ) =>
 		showOverlay ? 1 : 0 };
 `;
 
 export const GridLine = styled.div`
-	background: white;
-	box-shadow: 0 0 2px rgba( 0, 0, 0, 0.6 );
+	background: rgba( 255, 255, 255, 0.4 );
+	backdrop-filter: blur( 16px ) saturate( 200% );
 	position: absolute;
-	opacity: 0.4;
+	opacity: 1;
```

Yup, applied in https://github.com/WordPress/gutenberg/pull/58459/commits/13bc32959aa67b6ae559168c35dd698abd3cccae.

### Review thread 4 (resolved)

#### t-hamano on 2024-01-31T11:01:02Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1472660002
Path: packages/components/src/focal-point-picker/styles/focal-point-style.ts

```diff
@@ -6,18 +6,25 @@ import styled from '@emotion/styled';
 export const PointerCircle = styled.div`
 	background-color: transparent;
 	cursor: grab;
-	height: 48px;
-	margin: -24px 0 0 -24px;
+	height: 40px;
+	margin: -20px 0 0 -20px;
 	position: absolute;
 	user-select: none;
-	width: 48px;
+	width: 40px;
 	will-change: transform;
 	z-index: 10000;
-	background: rgba( 255, 255, 255, 0.6 );
+	background: rgba( 255, 255, 255, 0.4 );
+	border: 1px solid rgba( 255, 255, 255, 0.3 );
 	border-radius: 50%;
-	backdrop-filter: blur( 4px );
-	box-shadow: rgb( 0 0 0 / 20% ) 0px 0px 10px;
+	backdrop-filter: blur( 16px ) saturate( 180% );
+	box-shadow: rgb( 0 0 0 / 8% ) 0px 0px 8px;
+	transition: transform 100ms linear;
```

I think we need `reduceMotion` here as well.

#### richtabor on 2024-02-05T15:36:37Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478447867
Path: packages/components/src/focal-point-picker/styles/focal-point-style.ts

```diff
@@ -6,18 +6,25 @@ import styled from '@emotion/styled';
 export const PointerCircle = styled.div`
 	background-color: transparent;
 	cursor: grab;
-	height: 48px;
-	margin: -24px 0 0 -24px;
+	height: 40px;
+	margin: -20px 0 0 -20px;
 	position: absolute;
 	user-select: none;
-	width: 48px;
+	width: 40px;
 	will-change: transform;
 	z-index: 10000;
-	background: rgba( 255, 255, 255, 0.6 );
+	background: rgba( 255, 255, 255, 0.4 );
+	border: 1px solid rgba( 255, 255, 255, 0.3 );
 	border-radius: 50%;
-	backdrop-filter: blur( 4px );
-	box-shadow: rgb( 0 0 0 / 20% ) 0px 0px 10px;
+	backdrop-filter: blur( 16px ) saturate( 180% );
+	box-shadow: rgb( 0 0 0 / 8% ) 0px 0px 8px;
+	transition: transform 100ms linear;
```

Added in https://github.com/WordPress/gutenberg/pull/58459/commits/e4ee0d84e5375a9b95f7901de08643d8342c384c.

### Review thread 5 (resolved)

#### ciampo on 2024-02-01T08:44:02Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1474026849
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
```

Is there a particular reason for switching from block to inline flex? This change could cause breakage for some consumers of the component

#### richtabor on 2024-02-05T15:50:22Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478467128
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
```

Inline flex leads to unintended space applied below it, which messes with the metrics of the fields below. 

 ![CleanShot 2024-02-05 at 10 48 19](https://github.com/WordPress/gutenberg/assets/1813435/3916073e-0063-4a16-af8d-c949bdb3f7d2)

#### ciampo on 2024-02-05T17:08:36Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478594490
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
```

Got it! In that case, we should be able to remove the white space _without_ changing the `display` value of the `MediaContainer`, which allows us to improve spacing without potentially causing a visually-breaking change. We just need to set `display: flex` on the parent container:

```diff
diff --git a/packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts b/packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts
index beb528bcf3..893f5b2ebd 100644
--- a/packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts
+++ b/packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts
@@ -15,6 +15,7 @@ import { INITIAL_BOUNDS } from '../utils';
 
 export const MediaWrapper = styled.div`
 	background-color: transparent;
+	display: flex;
 	text-align: center;
 	width: 100%;
 `;
@@ -23,7 +24,7 @@ export const MediaContainer = styled.div`
 	align-items: center;
 	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: flex;
+	display: inline-flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;

```

#### richtabor on 2024-02-05T19:14:09Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478767969
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
```

good deal. I've made the change in https://github.com/WordPress/gutenberg/pull/58459/commits/cbfc0ef70a0d9b563752edcba8423fd9c36b74a6

### Review thread 6 (resolved)

#### ciampo on 2024-02-01T09:05:30Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1474064532
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		position: absolute;
+		position: absolute;
+		right: 0px;
+		top: 0px;
+	}
```

Currently, the border / box shadow doesn't follow the parent's border radius, we'll need to add an explicit rule for that.

`border-radius: inherit` should do the job!

#### richtabor on 2024-02-05T15:42:53Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478456654
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		position: absolute;
+		position: absolute;
+		right: 0px;
+		top: 0px;
+	}
```

Thanks! Resolved in https://github.com/WordPress/gutenberg/pull/58459/commits/97f3dbafaf1cc0e6e570614900a6b616914016fe.

### Review thread 7 (resolved)

#### ciampo on 2024-02-01T09:12:20Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1474075831
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		position: absolute;
+		position: absolute;
+		right: 0px;
+		top: 0px;
+	}
+
 	img,
 	video {
+		border-radius: ${ CONFIG.radiusBlockUi };
```

Personal preference, but I think that using `border-radius: inherit` is a cleaner way and it shows the author's intentions better.

#### richtabor on 2024-02-05T15:42:41Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478456362
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		position: absolute;
+		position: absolute;
+		right: 0px;
+		top: 0px;
+	}
+
 	img,
 	video {
+		border-radius: ${ CONFIG.radiusBlockUi };
```

Good point, applied in https://github.com/WordPress/gutenberg/pull/58459/commits/97f3dbafaf1cc0e6e570614900a6b616914016fe.

### Review thread 8 (resolved)

#### ciampo on 2024-02-01T09:12:24Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1474075953
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -45,6 +57,7 @@ export const MediaContainer = styled.div`
 
 export const MediaPlaceholder = styled.div`
 	background: ${ COLORS.gray[ 100 ] };
+	border-radius: ${ CONFIG.radiusBlockUi };
```

Same as above re. `border-radius: inherit`

#### richtabor on 2024-02-05T15:44:18Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478458582
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -45,6 +57,7 @@ export const MediaContainer = styled.div`
 
 export const MediaPlaceholder = styled.div`
 	background: ${ COLORS.gray[ 100 ] };
+	border-radius: ${ CONFIG.radiusBlockUi };
```

Thanks! https://github.com/WordPress/gutenberg/pull/58459/commits/89e0e6ba34a185897e8736ab56acef7f496d91a0

### Review thread 9 (resolved)

#### ciampo on 2024-02-01T10:26:36Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1474199245
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
```

Probably better to add `pointer-events: none` to this `after` pseudoelement.

#### richtabor on 2024-02-05T15:45:41Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1478460476
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -21,16 +21,28 @@ export const MediaWrapper = styled.div`
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
-	display: inline-flex;
+	display: flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
```

Added https://github.com/WordPress/gutenberg/pull/58459/commits/1b477c6e0f4e4ca114a06b3b4868f4899b6a2612

### Review thread 10 (resolved)

#### t-hamano on 2024-02-06T04:23:31Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1479205084
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -9,28 +9,42 @@ import styled from '@emotion/styled';
  */
 import { Flex } from '../../flex';
 import UnitControl from '../../unit-control';
-import { COLORS } from '../../utils';
+import { COLORS, CONFIG, reduceMotion } from '../../utils';
 import type { FocalPointPickerControlsProps } from '../types';
 import { INITIAL_BOUNDS } from '../utils';
 
 export const MediaWrapper = styled.div`
 	background-color: transparent;
+	display: flex;
 	text-align: center;
 	width: 100%;
 `;
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
 	display: inline-flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		border-radius: inherit;
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		pointer-events: none;
+		position: absolute;
+		right: 0px;
+		top: 0px;
```

```suggestion
		bottom: 0;
		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
		content: '';
		left: 0;
		pointer-events: none;
		position: absolute;
		right: 0;
		top: 0;
```

#### richtabor on 2024-02-06T19:37:18Z

URL: https://github.com/WordPress/gutenberg/pull/58459#discussion_r1480446370
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts

```diff
@@ -9,28 +9,42 @@ import styled from '@emotion/styled';
  */
 import { Flex } from '../../flex';
 import UnitControl from '../../unit-control';
-import { COLORS } from '../../utils';
+import { COLORS, CONFIG, reduceMotion } from '../../utils';
 import type { FocalPointPickerControlsProps } from '../types';
 import { INITIAL_BOUNDS } from '../utils';
 
 export const MediaWrapper = styled.div`
 	background-color: transparent;
+	display: flex;
 	text-align: center;
 	width: 100%;
 `;
 
 export const MediaContainer = styled.div`
 	align-items: center;
-	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	border-radius: ${ CONFIG.radiusBlockUi };
 	cursor: pointer;
 	display: inline-flex;
 	justify-content: center;
 	margin: auto;
 	position: relative;
 	height: 100%;
 
+	&:after {
+		border-radius: inherit;
+		bottom: 0px;
+		box-shadow: inset 0 0 0 1px rgba( 0, 0, 0, 0.1 );
+		content: '';
+		left: 0px;
+		pointer-events: none;
+		position: absolute;
+		right: 0px;
+		top: 0px;
```

thnx https://github.com/WordPress/gutenberg/pull/58459/commits/5003e4295a6b0e758b0876727ba1a108b02c5b09

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-1)
- packages/components/src/focal-point-picker/styles/focal-point-picker-style.ts (MODIFIED, +26/-10)
- packages/components/src/focal-point-picker/styles/focal-point-style.ts (MODIFIED, +21/-7)
