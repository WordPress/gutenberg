---
number: 73471
type: pull_request
state: MERGED
merged_at: 2025-12-11T13:56:34Z
url: https://github.com/WordPress/gutenberg/pull/73471
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #73471: Validated form controls: Suppress native popover

- URL: https://github.com/WordPress/gutenberg/pull/73471
- Author: mirka
- Created: 2025-11-20T20:51:18Z
- Updated: 2026-03-19T18:05:34Z
- Comments: 8 of 8
- Review threads: 4 of 4
- Changed files: 4 of 4
- Diff: ../pr-diffs/73471.diff

## Body

## What?

Closes #73015

Try suppressing the native error popover that appears when a form is validated, while keeping the focus behavior as is.

## Why?

The popover can obscure our custom error messages, and are redundant.

## How?

`preventDefault()` on `invalid` events on the validity target, and then add back the focus logic.

The concern here is whether we're overlooking any other "default behavior" that would be bad to prevent.

One that I noticed so far is, at least in VoiceOver on Chrome and Safari, hitting the submit button on an invalid form _plays a special sound effect_ in addition to announcing the first error again. This behavior is lost by doing `preventDefault()`. We can mimic the announcement programmatically, but not the sound effect.

## Testing Instructions

Try the Validated Form Control stories in the three major browsers.

## Screenshots or screencast <!-- if applicable -->

### Before

https://github.com/user-attachments/assets/529119c9-2e37-45b5-a5e5-9ade4ee17c96

### After

https://github.com/user-attachments/assets/f178c8bb-c8a7-4fe2-b0cb-d1ad11c18440

## Issue comments

### github-actions on 2025-11-20T20:57:15Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-3560014574

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: jasmussen <joen@git.wordpress.org>
Co-authored-by: juanfra <juanfra@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### mirka on 2025-11-20T20:57:48Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-3560015995

Not sure if we'll merge, but I'll open this up to feedback.

### jasmussen on 2025-11-21T07:55:21Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-3561855801

At a very high level glance at just the before and after, purely conceptually the path forward is clear: it should be one or the other, not both.

### mirka on 2025-12-08T21:05:13Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-3628999353

I think we're all aligned that suppressing the native popovers is better for general UX. Since this is trivial to revert if it causes complications down the line, I would be comfortable trying it out to see how it goes.

I've rebased this with the latest validation logic, so the PR is ready for review (with the assumption that it will actually be merged) 🙂

### joedolson on 2026-03-19T15:51:03Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-4091182341

I'm trying to assess this as I review accessibility related changes in 7.0, but I'm not sure where I can test this within core. Can you point me to some examples of inputs in core using this validation? I'm aware of the stories, but I want to be testing this in a use case.

### mirka on 2026-03-19T17:47:49Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-4092101183

@joedolson Thanks for looking at this. I believe the only use case in 7.0 is the `LinkControl` when you try to submit an invalid URL:

https://github.com/user-attachments/assets/7b165eb1-d8c5-427d-bf3a-81cf5907ec5a

### joedolson on 2026-03-19T18:01:30Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-4092186677

And it seems like there was no validation on that in 6.9, so it's actually a completely new addition, from the user perspective. Checking on it!

### joedolson on 2026-03-19T18:05:34Z

URL: https://github.com/WordPress/gutenberg/pull/73471#issuecomment-4092211471

No problems with suppressing the native error, but there is an issue with the error message - it's not explicitly connected to the input field; I'm going to open a new issue.

## Review threads

### Review thread 1

#### mirka on 2025-11-20T20:55:36Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2547645203
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -101,17 +101,65 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 	>();
 	const [ isTouched, setIsTouched ] = useState( false );
 	const previousCustomValidityType = usePrevious( customValidity?.type );
+	const className = 'components-validated-control';
 
-	// Ensure that error messages are visible after user attemps to submit a form
-	// with multiple invalid fields.
 	useEffect( () => {
 		const validityTarget = getValidityTarget();
+
+		// Ensure that error messages are visible after user attemps to submit a form
+		// with multiple invalid fields.
 		const showValidationMessage = () =>
 			setErrorMessage( validityTarget?.validationMessage );
 
+		// Suppress the native error popover, while keeping the focus behavior intact.
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
+		};
+
+		// Radio inputs need special handling because all radio inputs with the
+		// same `name` will be marked as invalid.
+		const radioSibilings =
```

One of the topics discussed in the original issue was whether this would be "worth the extra trouble".

So far, from the set of use cases that we currently have, radio inputs were the only thing that needed special handling like this. There may be more, and I have some qualms about the screen reader behavior, but perhaps not too bad to count as hard blockers.

#### aduth on 2025-12-09T15:51:02Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2603256379
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -101,17 +101,65 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 	>();
 	const [ isTouched, setIsTouched ] = useState( false );
 	const previousCustomValidityType = usePrevious( customValidity?.type );
+	const className = 'components-validated-control';
 
-	// Ensure that error messages are visible after user attemps to submit a form
-	// with multiple invalid fields.
 	useEffect( () => {
 		const validityTarget = getValidityTarget();
+
+		// Ensure that error messages are visible after user attemps to submit a form
+		// with multiple invalid fields.
 		const showValidationMessage = () =>
 			setErrorMessage( validityTarget?.validationMessage );
 
+		// Suppress the native error popover, while keeping the focus behavior intact.
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
+		};
+
+		// Radio inputs need special handling because all radio inputs with the
+		// same `name` will be marked as invalid.
+		const radioSibilings =
```

I'm having a hard time understanding what happens if we just... didn't have this code for radio inputs.

#### mirka on 2025-12-10T14:55:02Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2606998718
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -101,17 +101,65 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 	>();
 	const [ isTouched, setIsTouched ] = useState( false );
 	const previousCustomValidityType = usePrevious( customValidity?.type );
+	const className = 'components-validated-control';
 
-	// Ensure that error messages are visible after user attemps to submit a form
-	// with multiple invalid fields.
 	useEffect( () => {
 		const validityTarget = getValidityTarget();
+
+		// Ensure that error messages are visible after user attemps to submit a form
+		// with multiple invalid fields.
 		const showValidationMessage = () =>
 			setErrorMessage( validityTarget?.validationMessage );
 
+		// Suppress the native error popover, while keeping the focus behavior intact.
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
+		};
+
+		// Radio inputs need special handling because all radio inputs with the
+		// same `name` will be marked as invalid.
+		const radioSibilings =
```

My bad. Here's what happens when you click the submit button on an untouched radio group. The "Before" version has the radio handling in this hook commented out.

| Before | After |
|--------|-------|
|<img width="1084" height="802" alt="Radio group, before" src="https://github.com/user-attachments/assets/96fe0415-9744-4db0-9510-9b052f7cb322" />|<img width="1084" height="802" alt="Radio group, after" src="https://github.com/user-attachments/assets/720d9e3d-f59d-4402-b74e-c5dfe9f08101" />|


I expanded on the code comment a bit (793ace03a6d7625649297f5880fa3045870f389d). Ideally we have a unit test, but unfortunately this is not accurately testable in jsdom.

#### aduth on 2025-12-10T19:21:03Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2607927917
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -101,17 +101,65 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 	>();
 	const [ isTouched, setIsTouched ] = useState( false );
 	const previousCustomValidityType = usePrevious( customValidity?.type );
+	const className = 'components-validated-control';
 
-	// Ensure that error messages are visible after user attemps to submit a form
-	// with multiple invalid fields.
 	useEffect( () => {
 		const validityTarget = getValidityTarget();
+
+		// Ensure that error messages are visible after user attemps to submit a form
+		// with multiple invalid fields.
 		const showValidationMessage = () =>
 			setErrorMessage( validityTarget?.validationMessage );
 
+		// Suppress the native error popover, while keeping the focus behavior intact.
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
+		};
+
+		// Radio inputs need special handling because all radio inputs with the
+		// same `name` will be marked as invalid.
+		const radioSibilings =
```

Gotcha 👍 

Thinking out loud a bit: Rather than dealing with `getValidityTarget` as assuming there's some "one element" to be validated (which your comment seems to suggest is not actually the case), could we bind to the wrapper and watch for the event in capture phase?

Something like...

```tsx
wrapperRef.current?.addEventListener( 'invalid', suppressNativePopover, true );
```

Or maybe it should be `getValidityTargets`, which is usually an array of one, but could be multiple?

Definitely not a blocker.

#### mirka on 2025-12-11T13:56:20Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2610687523
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -101,17 +101,65 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 	>();
 	const [ isTouched, setIsTouched ] = useState( false );
 	const previousCustomValidityType = usePrevious( customValidity?.type );
+	const className = 'components-validated-control';
 
-	// Ensure that error messages are visible after user attemps to submit a form
-	// with multiple invalid fields.
 	useEffect( () => {
 		const validityTarget = getValidityTarget();
+
+		// Ensure that error messages are visible after user attemps to submit a form
+		// with multiple invalid fields.
 		const showValidationMessage = () =>
 			setErrorMessage( validityTarget?.validationMessage );
 
+		// Suppress the native error popover, while keeping the focus behavior intact.
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
+		};
+
+		// Radio inputs need special handling because all radio inputs with the
+		// same `name` will be marked as invalid.
+		const radioSibilings =
```

At the moment I'm not considering supporting multiple validity targets for a control, because there's only one slot to show an error message.

But listening to _all_ invalid events within a control wrapper might work… 🤔 That might be preferable if we encounter additional cases that need a similar workaround.

### Review thread 2

#### aduth on 2025-12-08T21:47:05Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2600254621
Path: packages/components/src/validated-form-controls/test/control-with-error.tsx

```diff
@@ -282,4 +282,57 @@ describe( 'ControlWithError', () => {
 			expect( onSubmit ).not.toHaveBeenCalled();
 		} );
 	} );
+
+	describe( 'Focus behavior', () => {
+		it( 'should focus the first error in the form', async () => {
+			const user = userEvent.setup();
+			render(
+				<form>
+					<ValidatedInputControl label="Text1" required />
+					<ValidatedInputControl label="Text2" required />
+					<button type="submit">Submit</button>
+				</form>
+			);
+
+			await user.click(
+				screen.getByRole( 'button', { name: 'Submit' } )
+			);
+
+			expect(
+				screen.getByRole( 'textbox', { name: /^Text1/ } )
```

Nit: Should we expect that the name would be _exactly_ `Text1` ?

```suggestion
				screen.getByRole( 'textbox', { name: 'Text1' } )
```

#### mirka on 2025-12-10T14:45:24Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2606952515
Path: packages/components/src/validated-form-controls/test/control-with-error.tsx

```diff
@@ -282,4 +282,57 @@ describe( 'ControlWithError', () => {
 			expect( onSubmit ).not.toHaveBeenCalled();
 		} );
 	} );
+
+	describe( 'Focus behavior', () => {
+		it( 'should focus the first error in the form', async () => {
+			const user = userEvent.setup();
+			render(
+				<form>
+					<ValidatedInputControl label="Text1" required />
+					<ValidatedInputControl label="Text2" required />
+					<button type="submit">Submit</button>
+				</form>
+			);
+
+			await user.click(
+				screen.getByRole( 'button', { name: 'Submit' } )
+			);
+
+			expect(
+				screen.getByRole( 'textbox', { name: /^Text1/ } )
```

This is just side-stepping the detail that the `required` prop adds a `(Required)` suffix to the labels. Kind of wanted to exclude that detail from these tests.

### Review thread 3 (resolved)

#### aduth on 2025-12-09T15:22:17Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2603124661
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -109,6 +111,58 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 		return () => validityTarget?.removeEventListener( 'invalid', handler );
 	}, [ getValidityTarget ] );
 
+	// Suppress the native error popover, while keeping the focus behavior intact.
+	useEffect( () => {
+		const validityTarget = getValidityTarget();
+
+		const supressNativePopover = ( event: Event ) => {
```

Nit: Typo


```suggestion
		const suppressNativePopover = ( event: Event ) => {
```

#### mirka on 2025-12-10T14:44:06Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2606947595
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -109,6 +111,58 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 		return () => validityTarget?.removeEventListener( 'invalid', handler );
 	}, [ getValidityTarget ] );
 
+	// Suppress the native error popover, while keeping the focus behavior intact.
+	useEffect( () => {
+		const validityTarget = getValidityTarget();
+
+		const supressNativePopover = ( event: Event ) => {
```

297b474

### Review thread 4

#### aduth on 2025-12-09T15:49:51Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2603249594
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -109,6 +111,58 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 		return () => validityTarget?.removeEventListener( 'invalid', handler );
 	}, [ getValidityTarget ] );
 
+	// Suppress the native error popover, while keeping the focus behavior intact.
+	useEffect( () => {
+		const validityTarget = getValidityTarget();
+
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
```

Could we use `:invalid` query selector to simplify this?


```suggestion
			if ( ! target.form || target.form.querySelector( ':invalid' ) === target ) {
				target.focus();
			}
```

#### mirka on 2025-12-10T14:48:57Z

URL: https://github.com/WordPress/gutenberg/pull/73471#discussion_r2606967533
Path: packages/components/src/validated-form-controls/control-with-error.tsx

```diff
@@ -109,6 +111,58 @@ function UnforwardedControlWithError< C extends React.ReactElement >(
 		return () => validityTarget?.removeEventListener( 'invalid', handler );
 	}, [ getValidityTarget ] );
 
+	// Suppress the native error popover, while keeping the focus behavior intact.
+	useEffect( () => {
+		const validityTarget = getValidityTarget();
+
+		const supressNativePopover = ( event: Event ) => {
+			event.preventDefault();
+
+			const target = event.target as ValidityTarget;
+
+			if ( ! target.form ) {
+				target.focus();
+				return;
+			}
+
+			const firstErrorInForm = Array.from( target.form.elements ).find(
+				( el ) => ! ( el as ValidityTarget ).validity.valid
+			);
+
+			if ( firstErrorInForm === target ) {
+				target.focus();
+			}
```

Ugh, this is a great idea I didn't think of, but unfortunately this `querySelector( ':invalid' )` seems to trigger a recursive validation check in jsdom and makes every test hang 😭 I reshuffled the original code to make it a bit simpler though (41d019f).

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/validated-form-controls/components/stories/overview.story.tsx (MODIFIED, +55/-1)
- packages/components/src/validated-form-controls/control-with-error.tsx (MODIFIED, +50/-5)
- packages/components/src/validated-form-controls/test/control-with-error.tsx (MODIFIED, +54/-1)
