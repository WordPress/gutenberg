---
number: 79226
type: pull_request
state: MERGED
merged_at: 2026-06-16T15:10:29Z
url: https://github.com/WordPress/gutenberg/pull/79226
matched_labels: ["[Package] Components","[Package] Theme"]
all_labels: ["[Type] Code Quality","[Package] Components","[Package] Block library","[Package] Theme"]
---

# PR #79226: Refactor: move stylelint config and deps to tools/stylelint workspace

- URL: https://github.com/WordPress/gutenberg/pull/79226
- Author: USERSATOSHI
- Created: 2026-06-16T10:21:54Z
- Updated: 2026-06-17T12:39:03Z
- Comments: 3 of 3
- Review threads: 2 of 2
- Changed files: 13 of 13
- Diff: ../pr-diffs/79226.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- Link this PR to its associated issue with an appropriate keyword: Closes, See, Follow up to, etc. -->
Related to https://github.com/WordPress/gutenberg/issues/75041

<!-- In a few words, what is the PR actually doing? -->

Moves the root `.stylelintrc.js` configuration and its `stylelint-plugin-logical-css` dependency into a new `tools/stylelint` workspace package (`@wordpress/stylelint-tools`), following the same pattern used by `@wordpress/eslint-tools`.

## Why?

Keeps root `package.json` lean and aligns with the `tools/eslint/` workspace pattern.

## How?

1. Create `tools/stylelint/` workspace with `@wordpress/stylelint-tools` package exporting `./config`.
2. Move root `.stylelintrc.js` → `tools/stylelint/config.js`; root gets a thin `.stylelintrc.mjs` wrapper.
3. Remove `stylelint-plugin-logical-css` from root deps; sub-packages reference config via workspace name.
4. Extend `wp-scripts lint-style` to detect `.cjs`/`.mjs` config files.
5. Fix stylelint bin resolution in test utilities.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

1. Run `npm install` to link the new workspace.
2. Run `npm run lint:css` — verify same lint results as on `trunk` (no new warnings/errors).
3. Confirm `npm run lint:css` detects `.stylelintrc.mjs` automatically (no `--config` flag needed).
4. Run stylelint-config tests: `npm run test:unit packages/stylelint-config/test/`.
5. Verify lint-staged still works: stage a `.scss` file and confirm `wp-scripts lint-style` picks up the right config.

## Use of AI Tools

AI tooling deepseek v4 flash free was used for code exploration and locating all files affected by this refactor. 

All changes have been manually reviewed and verified by me.

## Issue comments

### USERSATOSHI on 2026-06-16T11:24:55Z

URL: https://github.com/WordPress/gutenberg/pull/79226#issuecomment-4718214524

checking what is causing storybook to break

### manzoorwanijk on 2026-06-16T14:54:01Z

URL: https://github.com/WordPress/gutenberg/pull/79226#issuecomment-4720129109

Storybook failure was caused by #79003 and is being reverted in #79243

### github-actions on 2026-06-16T14:55:43Z

URL: https://github.com/WordPress/gutenberg/pull/79226#issuecomment-4720148013

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: USERSATOSHI <tusharbharti@git.wordpress.org>
Co-authored-by: manzoorwanijk <manzoorwanijk@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1 (resolved)

#### manzoorwanijk on 2026-06-16T14:10:13Z

URL: https://github.com/WordPress/gutenberg/pull/79226#discussion_r3421408801
Path: .stylelintrc.mjs

```diff
@@ -0,0 +1,5 @@
+/** @type {import('stylelint').Config} */
+export default {
+	extends: '@wordpress/stylelint-tools/config',
+	reportDescriptionlessDisables: true,
+};
```

Let us resolve the package instead of passing a string. Also, `stylelint` is not in root, so the JSDoc will break insolation more, and we don't need that `reportDescriptionlessDisables` here.

```suggestion
import { createRequire } from 'module';

const require = createRequire( import.meta.url );

export default {
	extends: require.resolve( '@wordpress/stylelint-tools/config' ),
};
```

### Review thread 2

#### aduth on 2026-06-16T18:02:18Z

URL: https://github.com/WordPress/gutenberg/pull/79226#discussion_r3422936204
Path: packages/scripts/scripts/lint-style.js

```diff
@@ -24,10 +24,14 @@ const defaultFilesArgs = hasFileArgInCLI() ? [] : [ '**/*.{css,pcss,scss}' ];
 const hasLintConfig =
 	hasArgInCLI( '--config' ) ||
 	hasProjectFile( '.stylelintrc.js' ) ||
+	hasProjectFile( '.stylelintrc.cjs' ) ||
+	hasProjectFile( '.stylelintrc.mjs' ) ||
 	hasProjectFile( '.stylelintrc.json' ) ||
 	hasProjectFile( '.stylelintrc.yaml' ) ||
 	hasProjectFile( '.stylelintrc.yml' ) ||
 	hasProjectFile( 'stylelint.config.js' ) ||
+	hasProjectFile( 'stylelint.config.cjs' ) ||
+	hasProjectFile( 'stylelint.config.mjs' ) ||
 	hasProjectFile( '.stylelintrc' ) ||
 	hasPackageProp( 'stylelint' );
```

I wonder if we should be using Stylelint's actual resolution behavior via its `resolveConfig` export or the [cosmiconfig](https://www.npmjs.com/package/cosmiconfig) library it's using, rather than trying to maintain this list ourselves and keep it in sync. Case in point: it still doesn't capture [all of the supported extensions](https://github.com/cosmiconfig/cosmiconfig/blob/006fc0baa0c998a42afc3bff3e8257b9f19a9b22/src/defaults.ts#L10-L34), such `.ts`, and trying to chase all these down feels like a losing battle.

#### manzoorwanijk on 2026-06-16T18:10:24Z

URL: https://github.com/WordPress/gutenberg/pull/79226#discussion_r3422982102
Path: packages/scripts/scripts/lint-style.js

```diff
@@ -24,10 +24,14 @@ const defaultFilesArgs = hasFileArgInCLI() ? [] : [ '**/*.{css,pcss,scss}' ];
 const hasLintConfig =
 	hasArgInCLI( '--config' ) ||
 	hasProjectFile( '.stylelintrc.js' ) ||
+	hasProjectFile( '.stylelintrc.cjs' ) ||
+	hasProjectFile( '.stylelintrc.mjs' ) ||
 	hasProjectFile( '.stylelintrc.json' ) ||
 	hasProjectFile( '.stylelintrc.yaml' ) ||
 	hasProjectFile( '.stylelintrc.yml' ) ||
 	hasProjectFile( 'stylelint.config.js' ) ||
+	hasProjectFile( 'stylelint.config.cjs' ) ||
+	hasProjectFile( 'stylelint.config.mjs' ) ||
 	hasProjectFile( '.stylelintrc' ) ||
 	hasPackageProp( 'stylelint' );
```

That will be great.

#### USERSATOSHI on 2026-06-17T11:57:53Z

URL: https://github.com/WordPress/gutenberg/pull/79226#discussion_r3427903430
Path: packages/scripts/scripts/lint-style.js

```diff
@@ -24,10 +24,14 @@ const defaultFilesArgs = hasFileArgInCLI() ? [] : [ '**/*.{css,pcss,scss}' ];
 const hasLintConfig =
 	hasArgInCLI( '--config' ) ||
 	hasProjectFile( '.stylelintrc.js' ) ||
+	hasProjectFile( '.stylelintrc.cjs' ) ||
+	hasProjectFile( '.stylelintrc.mjs' ) ||
 	hasProjectFile( '.stylelintrc.json' ) ||
 	hasProjectFile( '.stylelintrc.yaml' ) ||
 	hasProjectFile( '.stylelintrc.yml' ) ||
 	hasProjectFile( 'stylelint.config.js' ) ||
+	hasProjectFile( 'stylelint.config.cjs' ) ||
+	hasProjectFile( 'stylelint.config.mjs' ) ||
 	hasProjectFile( '.stylelintrc' ) ||
 	hasPackageProp( 'stylelint' );
```

Hi, I checked and cosmiconfig is already in gutenbeg root node_modules since it is a transitive dep 

We can add the dependency to `@wordpress/scripts` and use it there.

I also came across https://github.com/WordPress/gutenberg/issues/30842 and a stale PR that was eventually closed: https://github.com/WordPress/gutenberg/pull/53911.

After reviewing both, it seems that integrating this approach was considered acceptable. If we decide to move forward with it, I can work on adding the implementation.


cc: @manzoorwanijk @aduth

#### manzoorwanijk on 2026-06-17T12:05:34Z

URL: https://github.com/WordPress/gutenberg/pull/79226#discussion_r3427948890
Path: packages/scripts/scripts/lint-style.js

```diff
@@ -24,10 +24,14 @@ const defaultFilesArgs = hasFileArgInCLI() ? [] : [ '**/*.{css,pcss,scss}' ];
 const hasLintConfig =
 	hasArgInCLI( '--config' ) ||
 	hasProjectFile( '.stylelintrc.js' ) ||
+	hasProjectFile( '.stylelintrc.cjs' ) ||
+	hasProjectFile( '.stylelintrc.mjs' ) ||
 	hasProjectFile( '.stylelintrc.json' ) ||
 	hasProjectFile( '.stylelintrc.yaml' ) ||
 	hasProjectFile( '.stylelintrc.yml' ) ||
 	hasProjectFile( 'stylelint.config.js' ) ||
+	hasProjectFile( 'stylelint.config.cjs' ) ||
+	hasProjectFile( 'stylelint.config.mjs' ) ||
 	hasProjectFile( '.stylelintrc' ) ||
 	hasPackageProp( 'stylelint' );
```

Thank you for the context. Please go ahead.

#### aduth on 2026-06-17T12:39:03Z

URL: https://github.com/WordPress/gutenberg/pull/79226#discussion_r3428150603
Path: packages/scripts/scripts/lint-style.js

```diff
@@ -24,10 +24,14 @@ const defaultFilesArgs = hasFileArgInCLI() ? [] : [ '**/*.{css,pcss,scss}' ];
 const hasLintConfig =
 	hasArgInCLI( '--config' ) ||
 	hasProjectFile( '.stylelintrc.js' ) ||
+	hasProjectFile( '.stylelintrc.cjs' ) ||
+	hasProjectFile( '.stylelintrc.mjs' ) ||
 	hasProjectFile( '.stylelintrc.json' ) ||
 	hasProjectFile( '.stylelintrc.yaml' ) ||
 	hasProjectFile( '.stylelintrc.yml' ) ||
 	hasProjectFile( 'stylelint.config.js' ) ||
+	hasProjectFile( 'stylelint.config.cjs' ) ||
+	hasProjectFile( 'stylelint.config.mjs' ) ||
 	hasProjectFile( '.stylelintrc' ) ||
 	hasPackageProp( 'stylelint' );
```

Heh, wasn't aware of that existing issue, thanks for finding and sharing it! It's good reassurance that this would be an improvement.

## Changed files

- .stylelintrc.mjs (ADDED, +7/-0)
- package-lock.json (MODIFIED, +15/-2)
- package.json (MODIFIED, +1/-1)
- packages/block-library/.stylelintrc.mjs (MODIFIED, +2/-2)
- packages/block-library/package.json (MODIFIED, +1/-0)
- packages/components/.stylelintrc.mjs (MODIFIED, +1/-1)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/scripts/CHANGELOG.md (MODIFIED, +4/-0)
- packages/scripts/scripts/lint-style.js (MODIFIED, +4/-0)
- packages/stylelint-config/test/utils/index.js (MODIFIED, +13/-1)
- packages/theme/src/stylelint-plugins/test/utils/index.ts (MODIFIED, +13/-1)
- tools/stylelint/config.js (RENAMED, +0/-0)
- tools/stylelint/package.json (ADDED, +31/-0)
