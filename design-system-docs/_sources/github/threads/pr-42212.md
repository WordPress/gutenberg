---
number: 42212
type: pull_request
state: MERGED
merged_at: 2022-07-22T03:31:06Z
url: https://github.com/WordPress/gutenberg/pull/42212
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components","[Feature] Design Tools"]
---

# PR #42212: BorderControl: Render border color/style dropdown as UnitControl prefix

- URL: https://github.com/WordPress/gutenberg/pull/42212
- Author: aaronrobertshaw
- Created: 2022-07-07T06:21:03Z
- Updated: 2022-07-22T03:31:29Z
- Comments: 1 of 1
- Review threads: 4 of 4
- Changed files: 5 of 5
- Diff: ../pr-diffs/42212.diff

## Body

## What?

Renders the `BorderControl` dropdown as a `prefix` within its `UnitControl`.

## Why?

Rendering as a prefix on the UnitControl allows us to reduce some of the CSS overrides.

## How?

1. Fixes `UnitControlProps` type to allow a `ReactNode` for the `prefix` prop.
2. Renders the existing border control dropdown via the `UnitControl`'s `prefix`.
3. Removes the obsolete `HStack` wrapper and cleans up/fixes styles

## Testing Instructions
1. Ensure there are no type errors: `npm run build:package-types`
2. Check `BorderControl` unit tests still pass: `npm run test-unit packages/components/src/border-control/test`
3. Confirm `BorderBoxControl` unit tests pass: `npm run test-unit packages/components/src/border-box-control/test`
4. Load Storybook and confirm no visual regressions for both `BorderControl` & `BorderBoxControl`
    - http://localhost:50240/?path=/story/components-experimental-bordercontrol--default
    - http://localhost:50240/?path=/story/components-experimental-borderboxcontrol--default
    - In particular, test controlling the input width via the `isCompact` and `width` props.
5. Switch to the block editor, edit a post, add a group block, and select it. Ensure it looks and functions as it should.

## Issue comments

### andreszs on 2022-07-13T13:07:38Z

URL: https://github.com/WordPress/gutenberg/pull/42212#issuecomment-1183201763

Nice PR, too bad it was ignored by the **6.0.1** update from earlier today.

## Review threads

### Review thread 1 (resolved)

#### andrewserong on 2022-07-08T06:45:33Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r916510898
Path: packages/components/src/unit-control/types.ts

```diff
@@ -76,7 +76,7 @@ export type UnitSelectControlProps = {
 export type UnitControlProps = Omit< UnitSelectControlProps, 'unit' > &
 	Pick<
 		InputControlProps,
-		'hideLabelFromVision' | '__next36pxDefaultSize'
+		'hideLabelFromVision' | 'prefix' | '__next36pxDefaultSize'
```

Very cool, I didn't realise that `InputControl` had a `prefix` prop we can expose in `UnitControl` 👍

#### aaronrobertshaw on 2022-07-08T07:06:32Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r916523740
Path: packages/components/src/unit-control/types.ts

```diff
@@ -76,7 +76,7 @@ export type UnitSelectControlProps = {
 export type UnitControlProps = Omit< UnitSelectControlProps, 'unit' > &
 	Pick<
 		InputControlProps,
-		'hideLabelFromVision' | '__next36pxDefaultSize'
+		'hideLabelFromVision' | 'prefix' | '__next36pxDefaultSize'
```

I can see why it might not have been exposed initially as it's a bit of an edge case using a prefix with a UnitControl.  That said, it's a perfect fit for the `BorderControl`.

### Review thread 2

#### mirka on 2022-07-08T21:01:20Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r917136334
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
 
 export const innerWrapper = () => css`
-	flex: 1 0 40%;
-
-	/*
-	 * Needs more thought. Aim is to prevent the border for BorderBoxControl
-	 * showing through the control. Likely needs to take into account
-	 * light/dark themes etc.
-	 */
-	background: #fff;
-
-	/*
-	 * Forces the width control to fill available space given UnitControl
-	 * passes its className directly through to the input.
-	 */
-	${ UnitControlWrapper } {
-		flex: 1;
-		${ rtl( { marginLeft: -1 } )() }
-	}
-
 	&& ${ UnitSelect } {
-		/* Prevent default styles forcing heights larger than BorderControl */
+		/* Prevent unit select forcing min height larger than its UnitControl */
 		min-height: 0;
 		${ rtl(
-			{
-				borderRadius: '0 1px 1px 0',
-				marginRight: 0,
-			},
-			{
-				borderRadius: '1px 0 0 1px',
-				marginLeft: 0,
-			}
+			{ borderRadius: '0 2px 2px 0' },
+			{ borderRadius: '2px 0 0 2px' }
```

I'm not sure I understand what these styles are for 🤔 Are they no longer needed...?

The `:focus` stuff too, should they be moved to `UnitControl` itself? (Not necessarily in this PR)

#### aaronrobertshaw on 2022-07-13T05:44:10Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r919668962
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
 
 export const innerWrapper = () => css`
-	flex: 1 0 40%;
-
-	/*
-	 * Needs more thought. Aim is to prevent the border for BorderBoxControl
-	 * showing through the control. Likely needs to take into account
-	 * light/dark themes etc.
-	 */
-	background: #fff;
-
-	/*
-	 * Forces the width control to fill available space given UnitControl
-	 * passes its className directly through to the input.
-	 */
-	${ UnitControlWrapper } {
-		flex: 1;
-		${ rtl( { marginLeft: -1 } )() }
-	}
-
 	&& ${ UnitSelect } {
-		/* Prevent default styles forcing heights larger than BorderControl */
+		/* Prevent unit select forcing min height larger than its UnitControl */
 		min-height: 0;
 		${ rtl(
-			{
-				borderRadius: '0 1px 1px 0',
-				marginRight: 0,
-			},
-			{
-				borderRadius: '1px 0 0 1px',
-				marginLeft: 0,
-			}
+			{ borderRadius: '0 2px 2px 0' },
+			{ borderRadius: '2px 0 0 2px' }
```

I swear that when I was putting this PR together I still needed these styles or the unit select showed square corners. I must have been seeing things. After retesting the border-radius rules can be removed.

While testing the focus styles it looks like the focus styling in general for the `UnitControl` is a little broken e.g. wrapper's border overlays the unit selects focus border/box-shadow etc.

I've moved these focus styles to the `UnitControl` in a separate PR in the hope that might provide a better historical record if we need to revisit them in the future. See https://github.com/WordPress/gutenberg/pull/42383.

For this PR, I'll update it shortly to target the https://github.com/WordPress/gutenberg/pull/42383 branch as its base and simply remove the overrides from the `BorderControl`.

### Review thread 3 (resolved)

#### mirka on 2022-07-08T21:16:50Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r917143297
Path: packages/components/src/border-control/styles.ts

```diff
@@ -77,8 +51,10 @@ export const innerWrapper = () => css`
 
 export const wrapperWidth = ( width: CSSProperties[ 'width' ] ) => {
 	return css`
-		width: ${ width };
-		flex: 0 0 auto;
+		${ UnitControlWrapper } {
+			width: ${ width };
+			flex: 0 0 auto;
+		}
```

Maybe we could remove this override by:

- Applying the width directly on `UnitControl` via the `__unstableInputWidth` prop
- Applying the flex style on our own div wrapper around the `UnitControl`

#### aaronrobertshaw on 2022-07-13T06:55:37Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r919713277
Path: packages/components/src/border-control/styles.ts

```diff
@@ -77,8 +51,10 @@ export const innerWrapper = () => css`
 
 export const wrapperWidth = ( width: CSSProperties[ 'width' ] ) => {
 	return css`
-		width: ${ width };
-		flex: 0 0 auto;
+		${ UnitControlWrapper } {
+			width: ${ width };
+			flex: 0 0 auto;
+		}
```

This PR has been updated to now pass the width to the `UnitControl` via the `__unstableWidth` prop. I found that I still needed the flex style on the unit control wrapper to maintain the border control's desired layout.

I've added some more explanatory comments to the code as well an extra style that fixes the intended default 40/60 ratio between the unit control and slider when no width is set.

### Review thread 4 (resolved)

#### mirka on 2022-07-08T21:17:46Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r917143729
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
```

🧹 ?

#### aaronrobertshaw on 2022-07-13T00:47:49Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r919547957
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
```

Sorry, I should have added an explanatory comment to this PR.

I couldn't see or recall why this style was in place and in my testing it didn't appear to have an impact now. So I've cleaned it up.

#### ciampo on 2022-07-14T18:28:55Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r921456138
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
```

I believe @mirka is suggesting deleting this empty variable?

#### mirka on 2022-07-15T02:32:51Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r921753568
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
```

Yes, sorry that was unclear! 🙈

#### aaronrobertshaw on 2022-07-15T07:45:14Z

URL: https://github.com/WordPress/gutenberg/pull/42212#discussion_r921903202
Path: packages/components/src/border-control/styles.ts

```diff
@@ -29,41 +29,15 @@ const focusBoxShadow = css`
 	box-shadow: inset 0 0 0 ${ CONFIG.borderWidth } ${ COLORS.ui.borderFocus };
 `;
 
-export const borderControl = css`
-	position: relative;
-`;
+export const borderControl = css``;
```

Thanks for the clarification.

The `borderControl` variable is required now after https://github.com/WordPress/gutenberg/pull/42348 adds a few styles there to wrangle the introduced `fieldset`. So it will live to see another day after I rebase this PR. 🙂

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +2/-0)
- packages/components/src/border-control/border-control/component.tsx (MODIFIED, +31/-31)
- packages/components/src/border-control/border-control/hook.ts (MODIFIED, +4/-9)
- packages/components/src/border-control/styles.ts (MODIFIED, +13/-61)
- packages/components/src/unit-control/types.ts (MODIFIED, +1/-1)
