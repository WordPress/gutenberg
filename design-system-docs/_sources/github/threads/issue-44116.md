---
number: 44116
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/44116
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components"]
---

# Issue #44116: Add basic theming to the `@wordpress/components` package

- URL: https://github.com/WordPress/gutenberg/issues/44116
- Author: ciampo
- Created: 2022-09-13T10:06:03Z
- Updated: 2026-04-16T15:05:51Z
- Comments: 5 of 5

## Body

## What

Allow the components package to be themed by its consumers

## How

The components package would allow theming by exposing a few CSS variables (e.g "primary color", "text color", "background color", "border radius"...). The package would have default values for each variable, but any consumer of the package would be able to override those values.

These variables would be applied to all components in the package, regardless of how they are used within the editor. But given the nature of CSS and CSS variables, different overrides could be applied in different DOM subtrees (e.g the sidebar wrapper could set a different background colors from the navigation panel, etc etc).

Having the package expose its own variables (instead of using gutenberg-specific variables) would help to decouple the package from the rest of the editor, and make it more reusable.

Things to discuss / refine:

- Which variables should we exposed? Ideally for the first iteration, as least as possible
- How do we document these variables?
- Should the package have any concepts of dark/light themes? (personally I don't think so, as that would being a lot of extra complexity).

## Action plan

- [x] Introduce experimental `Theme` component #44668
- [x] Storybook decorator #44715
- [x] #45249
- [x] Potentially look at more theme variables (background) #45466
- [x] Investigate feasibility of automatically choosing a text color with enough contrast, given the background #45466
- [ ] Limit as much as possible the usage of the Theming APIs outside of the components package until they are ready
    - [x] Making sure that the package doesn’t export anything related to its APIs (especially for WP Core releases) #46427
    - [ ] Potentially adding a console.warn when using <Theme /> about it not being ready for prod?
    - [x] Stating it very clearly in README that it’s experimental (and potentially removing the relevant section from CONTRIBUTING) #46427
    - [ ] We can’t avoid CSS leaks (CSS variables will just cascade), but we could try to limit them as much as possible — i.e.:
      - [x] by adding custom style lints to prevent usages of those variables outside of the components package #59020
      - [ ] by adding inline comments where the variables are declared
- [ ] Should the generated grayscale colors be desaturated a little?
- [ ] Is `gray` a good name for the theming variables? (see [convo](https://github.com/WordPress/gutenberg/pull/45466#discussion_r1023059133))
- [ ] Move away from static SASS functions (like `lighten` and `rgba`) which don't work with dynamic, runtime theme colors ([see convo](https://github.com/WordPress/gutenberg/pull/45466#discussion_r1030872724))
- [ ] Investigate what to do about components using `Slot/Fill` and portals in general (ie. `Popover` and components using `Popover`s that may want their popover content to be themed accordingly)
  - [ ] Including [`Tooltip`s](https://github.com/WordPress/gutenberg/pull/45466/files#r1018407696)
- [ ] Should we use `hsl` (and expose each color's component as a separate variable)? Should use OKLCH + polyfills?
- [x] Search for remaining `--wp-admin*` css variables and see if we should something about it
- [ ] Deal with the `@base-styles` and `@block-editor` overrides
- [ ] Update contributing guidelines
- [x] Add style lints to [catch](https://github.com/WordPress/gutenberg/pull/46276#discussion_r1041306894) direct `--wp-admin-theme-color` usage in wp-components #58098 #58130
- [ ] [Semantically named variables](https://github.com/WordPress/gutenberg/pull/46252#discussion_r1039324351) or not?

## Context

- Previous attempt: https://github.com/WordPress/gutenberg/pull/36042
- Conversation https://github.com/WordPress/gutenberg/pull/43994#issuecomment-1242320706

## Issue comments

### mirka on 2022-09-13T12:50:37Z

URL: https://github.com/WordPress/gutenberg/issues/44116#issuecomment-1245367907

I'm thinking of starting with something like (in order of priority):

1. accent color
2. background color
3. foreground color

Very simple, three official variables. Everything else (borders, shadows, darkened accent colors, etc.) we can try and automatically generate internally based on the given colors. It might be possible that we can manage without even exposing the foreground color (i.e. text), because we could just infer a black or white scheme based on the background color contrast.

The approach diverges on how to handle the auto-generation. We *could* calculate everything in CSS only, by enforcing consumers to set the CSS variable in distinct RGB or HSL values (some variation on [this technique](https://blog.jim-nielsen.com/2019/dynamically-darken-a-color-in-css/)). But I think it would be easier for everyone if we added a nestable theme provider component to handle the calculations in JS:

```jsx
<ThemeProvider accent="#007cba" background="#1e1e1e">
  <MyHeader />
  <ThemeProvider background="#fff">
    <MyEditor />
  </ThemeProvider>
</ThemeProvider>
```

This way we can more easily do contrast-based manipulation with [`colord`](https://github.com/omgovich/colord), and consumers can just provide hex colors if they want. The theme provider will calculate all the colors in the scheme and inject it as CSS variables.

I feel like this would cover most sensible use cases. And the very hardcore consumer could still override the generated CSS variables at the CSS level if they wanted to get more granular.

### ciampo on 2022-09-13T14:25:47Z

URL: https://github.com/WordPress/gutenberg/issues/44116#issuecomment-1245493740

> I'm thinking of starting with something like [...] Very simple, three official variables

Agreed. The simpler we start, the better IMO.

> the auto-generation

Based on personal experience with implementing a similar system, I'm a bit skeptical that we'll manage to find a formula that auto-generates colors in a way that is pleasing and well "proportioned" to the human eye for all possible colors — but we should definitely give it a shot, see where we get, and iterate.

> We could calculate everything in CSS only [...] But I think it would be easier for everyone if we added a nestable theme provider component to handle the calculations in JS

I liked the idea of using vanilla CSS variables, since it feels cleaner and "closer to the metal" (i.e. using CSS APIs for styling components).

At the same time, I agree that using React context will give us extra flexibility in handling and transforming the values, while still preserving the same "cascade" mechanism that we would have with CSS

### mirka on 2022-09-13T15:06:14Z

URL: https://github.com/WordPress/gutenberg/issues/44116#issuecomment-1245550772

> At the same time, I agree that using React context will give us extra flexibility in handling and transforming the values, while still preserving the same "cascade" mechanism that we would have with CSS

I was thinking we don't even need context, just render a ThemeProvider as `<div class="asdf">` and inject the generated CSS variables as CSS `.asdf { --color-foo: #eee; --color-bar: #fff; }` etc.

### ciampo on 2022-10-05T10:02:35Z

URL: https://github.com/WordPress/gutenberg/issues/44116#issuecomment-1268219863

_Edit: the plane of action originally written in this comment has been moved to the issue's description_

### mirka on 2026-04-16T15:05:51Z

URL: https://github.com/WordPress/gutenberg/issues/44116#issuecomment-4261137007

I guess we can close this now that we have the new theming system?

