---
number: 53703
type: pull_request
state: MERGED
merged_at: 2023-08-16T18:31:51Z
url: https://github.com/WordPress/gutenberg/pull/53703
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #53703: `AlignmentMatrixControl`: replace `act()` with `userEvent`

- URL: https://github.com/WordPress/gutenberg/pull/53703
- Author: brookewp
- Created: 2023-08-15T22:26:25Z
- Updated: 2023-08-16T18:32:23Z
- Comments: 0 of 0
- Review threads: 1 of 1
- Changed files: 1 of 1
- Diff: ../pr-diffs/53703.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Update test to utilize `userEvent` and remove `act()`.

## Why?
While working on #48222, I came across this test which was failing due to `...wrapped into act(...)` errors. 

## How?

Removed `act()` and added `userEvent.click`. This moves the test closer to how a user would interact via `click` instead of setting the focus. I've added another assertion to check the element has focus after the click, instead. 

## Testing Instructions

Verify tests still pass: `packages/components/src/alignment-matrix-control/test/index.tsx`

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### tyxla on 2023-08-16T11:59:33Z

URL: https://github.com/WordPress/gutenberg/pull/53703#discussion_r1295788870
Path: packages/components/src/alignment-matrix-control/test/index.tsx

```diff
@@ -37,7 +39,9 @@ describe( 'AlignmentMatrixControl', () => {
 					<AlignmentMatrixControl value="center" onChange={ spy } />
 				);
 
-				await act( () => getCell( alignment ).focus() );
+				await user.click( getCell( alignment ) );
+
+				expect( getCell( alignment ) ).toHaveFocus();
```

Replacing a `.focus()` with a `.click()` makes the intention a bit less clear, but the "have focus" assertion definitely helps clarify it, so thanks for adding that assertion 👍

#### brookewp on 2023-08-16T18:31:17Z

URL: https://github.com/WordPress/gutenberg/pull/53703#discussion_r1296288283
Path: packages/components/src/alignment-matrix-control/test/index.tsx

```diff
@@ -37,7 +39,9 @@ describe( 'AlignmentMatrixControl', () => {
 					<AlignmentMatrixControl value="center" onChange={ spy } />
 				);
 
-				await act( () => getCell( alignment ).focus() );
+				await user.click( getCell( alignment ) );
+
+				expect( getCell( alignment ) ).toHaveFocus();
```

Thank you for sharing! It's helpful to know it was a useful addition.

## Changed files

- packages/components/src/alignment-matrix-control/test/index.tsx (MODIFIED, +6/-2)
