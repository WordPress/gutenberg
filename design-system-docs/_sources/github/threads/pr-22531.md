---
number: 22531
type: pull_request
state: MERGED
merged_at: 2020-07-15T17:59:15Z
url: https://github.com/WordPress/gutenberg/pull/22531
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","[Block] Cover","[Feature] Design Tools"]
---

# PR #22531: Cover: Video - Add position controls

- URL: https://github.com/WordPress/gutenberg/pull/22531
- Author: ItsJonQ
- Created: 2020-05-21T19:05:03Z
- Updated: 2020-07-15T18:26:12Z
- Comments: 13 of 13
- Review threads: 12 of 12
- Changed files: 20 of 20
- Diff: ../pr-diffs/22531.diff

## Body

![focal-demo](https://user-images.githubusercontent.com/2322354/82595085-05daef80-9b73-11ea-9a2c-d2560084b371.gif)

This update adds positioning controls for video backgrounds within the **Cover block**. Previous to this image, the positioning controls (powered by the `FocalPointPicker` component) were available only to image backgrounds.

## ✨ New Features

##### Click to reposition

The position values can be updated by clicking anywhere in the preview area.

##### Keyboard arrow key support

![Screen Capture on 2020-05-21 at 14-53-25](https://user-images.githubusercontent.com/2322354/82595317-65d19600-9b73-11ea-8ed2-a5210db75ed6.gif)

Up, Down, Left, Right keyboard keys will adjust the position values.

##### Hold Shift to jump by 10

Holding <kbd>Shift</kbd> will jump the values, rounded to the nearest `10`. This works for boht dragging and keyboard arrow inputs.

##### New InputControls

The `top` and `left` values use a new `NumberControl` component. This component includes many features, such as dragging to update, jumping values by holding <kbd>Shift</kbd>, and automatic number rounding.

##### Grid

A 9x9 grid appears anytime a value is updated by **any** interaction (drag, click, etc...)

## 🛠 Fixes

* Enable `<FocalPointPicker />` to reset values based on undo actions in the editor.


## How has this been tested?

This has been tested locally in Storybook and local Gutenberg.

To test...

* Pull down the branch
* Run `npm install`
* Run `npm run dev` to fire up local Gutenberg
* Add a Cover block
* Add a video background (Here's a [sample video to download](https://www.w3schools.com/html/mov_bbb.mp4))
* Adjust the position
* Ensure it works in the editor and on the front-end of the site


## Types of changes

* Add video preview support to `FocalPointPicker`
* Substantial refactors to `FocalPointPicker`
* Adds `Flex` component to `@wordpress/components` as a layout-based primitive
* Update Cover video position rendering in `edit.js` and `save.js`

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

Resolves: https://github.com/WordPress/gutenberg/issues/22509

## Issue comments

### github-actions on 2020-05-21T19:10:58Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-632290335

**Size Change:** +1.41 kB (0%) 

**Total Size:** 1.14 MB

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/block-directory/index.js` | 7.68 kB | +1 B |  |
| `build/block-editor/index.js` | 115 kB | +1 B |  |
| `build/block-library/index.js` | 132 kB | +90 B (0%) |  |
| `build/components/index.js` | 201 kB | +1.76 kB (0%) |  |
| `build/components/style-rtl.css` | 15.6 kB | -223 B (1%) |  |
| `build/components/style.css` | 15.6 kB | -218 B (1%) |  |
| `build/edit-post/index.js` | 304 kB | +1 B |  |
| `build/edit-site/index.js` | 16.8 kB | -1 B |  |
| `build/edit-widgets/index.js` | 9.35 kB | +1 B |  |
| `build/format-library/index.js` | 7.72 kB | -1 B |  |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/a11y/index.js` | 1.14 kB | 0 B |  |
| `build/annotations/index.js` | 3.67 kB | 0 B |  |
| `build/api-fetch/index.js` | 3.39 kB | 0 B |  |
| `build/autop/index.js` | 2.82 kB | 0 B |  |
| `build/blob/index.js` | 620 B | 0 B |  |
| `build/block-directory/style-rtl.css` | 944 B | 0 B |  |
| `build/block-directory/style.css` | 945 B | 0 B |  |
| `build/block-editor/style-rtl.css` | 10.8 kB | 0 B |  |
| `build/block-editor/style.css` | 10.8 kB | 0 B |  |
| `build/block-library/editor-rtl.css` | 7.6 kB | 0 B |  |
| `build/block-library/editor.css` | 7.59 kB | 0 B |  |
| `build/block-library/style-rtl.css` | 7.77 kB | 0 B |  |
| `build/block-library/style.css` | 7.77 kB | 0 B |  |
| `build/block-library/theme-rtl.css` | 728 B | 0 B |  |
| `build/block-library/theme.css` | 729 B | 0 B |  |
| `build/block-serialization-default-parser/index.js` | 1.88 kB | 0 B |  |
| `build/block-serialization-spec-parser/index.js` | 3.1 kB | 0 B |  |
| `build/blocks/index.js` | 48.2 kB | 0 B |  |
| `build/compose/index.js` | 9.67 kB | 0 B |  |
| `build/core-data/index.js` | 11.5 kB | 0 B |  |
| `build/data-controls/index.js` | 1.29 kB | 0 B |  |
| `build/data/index.js` | 8.46 kB | 0 B |  |
| `build/date/index.js` | 5.38 kB | 0 B |  |
| `build/deprecated/index.js` | 772 B | 0 B |  |
| `build/dom-ready/index.js` | 569 B | 0 B |  |
| `build/dom/index.js` | 3.23 kB | 0 B |  |
| `build/edit-navigation/index.js` | 10.8 kB | 0 B |  |
| `build/edit-navigation/style-rtl.css` | 1.08 kB | 0 B |  |
| `build/edit-navigation/style.css` | 1.08 kB | 0 B |  |
| `build/edit-post/style-rtl.css` | 5.6 kB | 0 B |  |
| `build/edit-post/style.css` | 5.6 kB | 0 B |  |
| `build/edit-site/style-rtl.css` | 3.04 kB | 0 B |  |
| `build/edit-site/style.css` | 3.04 kB | 0 B |  |
| `build/edit-widgets/style-rtl.css` | 2.45 kB | 0 B |  |
| `build/edit-widgets/style.css` | 2.45 kB | 0 B |  |
| `build/editor/editor-styles-rtl.css` | 537 B | 0 B |  |
| `build/editor/editor-styles.css` | 539 B | 0 B |  |
| `build/editor/index.js` | 45.1 kB | 0 B |  |
| `build/editor/style-rtl.css` | 3.78 kB | 0 B |  |
| `build/editor/style.css` | 3.78 kB | 0 B |  |
| `build/element/index.js` | 4.65 kB | 0 B |  |
| `build/escape-html/index.js` | 733 B | 0 B |  |
| `build/format-library/style-rtl.css` | 547 B | 0 B |  |
| `build/format-library/style.css` | 548 B | 0 B |  |
| `build/hooks/index.js` | 2.13 kB | 0 B |  |
| `build/html-entities/index.js` | 622 B | 0 B |  |
| `build/i18n/index.js` | 3.56 kB | 0 B |  |
| `build/is-shallow-equal/index.js` | 709 B | 0 B |  |
| `build/keyboard-shortcuts/index.js` | 2.51 kB | 0 B |  |
| `build/keycodes/index.js` | 1.94 kB | 0 B |  |
| `build/list-reusable-blocks/index.js` | 3.12 kB | 0 B |  |
| `build/list-reusable-blocks/style-rtl.css` | 476 B | 0 B |  |
| `build/list-reusable-blocks/style.css` | 476 B | 0 B |  |
| `build/media-utils/index.js` | 5.32 kB | 0 B |  |
| `build/notices/index.js` | 1.79 kB | 0 B |  |
| `build/nux/index.js` | 3.4 kB | 0 B |  |
| `build/nux/style-rtl.css` | 671 B | 0 B |  |
| `build/nux/style.css` | 668 B | 0 B |  |
| `build/plugins/index.js` | 2.56 kB | 0 B |  |
| `build/primitives/index.js` | 1.4 kB | 0 B |  |
| `build/priority-queue/index.js` | 789 B | 0 B |  |
| `build/redux-routine/index.js` | 2.85 kB | 0 B |  |
| `build/rich-text/index.js` | 13.9 kB | 0 B |  |
| `build/server-side-render/index.js` | 2.71 kB | 0 B |  |
| `build/shortcode/index.js` | 1.7 kB | 0 B |  |
| `build/token-list/index.js` | 1.27 kB | 0 B |  |
| `build/url/index.js` | 4.06 kB | 0 B |  |
| `build/viewport/index.js` | 1.85 kB | 0 B |  |
| `build/warning/index.js` | 1.13 kB | 0 B |  |
| `build/wordcount/index.js` | 1.17 kB | 0 B |  |


</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### ItsJonQ on 2020-05-22T14:13:56Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-632714790

@jasmussen Provided a design suggestion in the issue. Will attempt it now 🙌 

![](https://user-images.githubusercontent.com/1204802/82664585-49486300-9c32-11ea-83b0-5bcf93a2d36c.png)

### ItsJonQ on 2020-05-22T17:36:20Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-632822553

@jasmussen We have "side" label alignments for Inputs :D

<img width="254" alt="Screen Shot 2020-05-22 at 1 35 08 PM" src="https://user-images.githubusercontent.com/2322354/82694336-194c9000-9c31-11ea-8749-5bde37208fff.png">

Note! This is done at the `InputControl` component level. That means that we can more easily side-align labels in the future in a consistent manner (if we choose to)

### ItsJonQ on 2020-05-22T17:40:22Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-632824022

@jasmussen Based on your mockup, do you think we should remove the extra padding around the media (when possible)?

<img width="475" alt="Screen Shot 2020-05-22 at 1 38 00 PM" src="https://user-images.githubusercontent.com/2322354/82694592-82340800-9c31-11ea-88bc-716def0d91d3.png">

Doing so would change the height of the preview. We could set min/max values to ensure it doesn't get too wonky.

It comes down to... do we want the preview to have a consistent height (currently `200px`) or not 😊 

Would love your thoughts!

### jasmussen on 2020-06-08T06:49:45Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-640404279

> @jasmussen Based on your mockup, do you think we should remove the extra padding around the media (when possible)?

OMG @ItsJonQ I'm so sorry I missed this ping! I don't know how that got lost. Sorry about that.

Yes, I think we should remove both the border, and the inner padding — I can't quite figure out what that padding is for, if it serves a purpose then I can add a new design.

Border too, yes, I know that we might end up with a situation where a white background bleeds in. If it turns out to be a problem, we can fo a subtler shadow border that should only be visible in those cases, such as `box-shadow: inset 0 0 0 $border-width rgba($black, 0.4)`; or something like that.

### ItsJonQ on 2020-06-08T19:50:02Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-640850479

@jasmussen No worries! Thanks for the followup :)

I've updated the UI to remove the excess padding, and added a subtle border (via box-shadow):

<img width="259" alt="Screen Shot 2020-06-08 at 3 48 08 PM" src="https://user-images.githubusercontent.com/2322354/84073911-ac125c00-a99f-11ea-92eb-2861aae794cf.png">

Lemme know how it feels!

P.S. Excuse ma spooky 👻 cover image, haha

### jasmussen on 2020-06-09T07:08:00Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-641079454

This is way better.

All this needs is a code review 👍 👍

### ItsJonQ on 2020-06-09T13:26:36Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-641296349

@jasmussen Amazing! Thank you <3

### ItsJonQ on 2020-06-10T14:13:52Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-642038118

Update! Re: Code Review

I intentionally left the `FocalPointPicker` component as a class component for this PR. There have been many changes. I think converting it to a functional component on top of it will make it harder to review.

### ItsJonQ on 2020-07-15T17:59:10Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-658914817

@youknowriad Thank you for reviewing this @youknowriad + @jasmussen  ! I'll merge it up 🙌

### youknowriad on 2020-07-15T18:20:49Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-658925876

Looks like this PR might have introduced a JavaScript unit test error on master 😬

### ItsJonQ on 2020-07-15T18:21:59Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-658926490

@youknowriad Uh oh!! Will look into that now

### ItsJonQ on 2020-07-15T18:26:12Z

URL: https://github.com/WordPress/gutenberg/pull/22531#issuecomment-658928807

@youknowriad PR incoming with fix~!

## Review threads

### Review thread 1

#### ItsJonQ on 2020-05-21T19:08:26Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r428855718
Path: packages/components/src/focal-point-picker/styles/focal-point-style.js

```diff
@@ -0,0 +1,48 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * WordPress dependencies
+ */
+import { Path, SVG } from '@wordpress/primitives';
+
+/**
+ * Internal dependencies
+ */
+import { color } from '../../utils/style-mixins';
+
```

Migrated and improved styles from the previous `styles.scss` file.

### Review thread 2 (resolved)

#### youknowriad on 2020-07-14T08:55:38Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454206963
Path: packages/block-library/src/cover/edit.js

```diff
@@ -327,28 +331,36 @@ function CoverEdit( {
 			<InspectorControls>
 				{ !! url && (
 					<PanelBody title={ __( 'Media settings' ) }>
-						{ IMAGE_BACKGROUND_TYPE === backgroundType && (
+						{ isImageBackground && (
 							<ToggleControl
 								label={ __( 'Fixed background' ) }
 								checked={ hasParallax }
 								onChange={ toggleParallax }
 							/>
 						) }
-						{ IMAGE_BACKGROUND_TYPE === backgroundType &&
-							! hasParallax && (
-								<FocalPointPicker
-									label={ __( 'Focal point picker' ) }
-									url={ url }
-									value={ focalPoint }
-									onChange={ ( newFocalPoint ) =>
-										setAttributes( {
-											focalPoint: newFocalPoint,
-										} )
-									}
-								/>
-							) }
-						{ VIDEO_BACKGROUND_TYPE === backgroundType && (
-							<video autoPlay muted loop src={ url } />
+						{ isImageBackground && ! hasParallax && (
```

Should this include a `|| isVideoBackground` and avoid the duplicated code?

#### ItsJonQ on 2020-07-14T16:42:44Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454493772
Path: packages/block-library/src/cover/edit.js

```diff
@@ -327,28 +331,36 @@ function CoverEdit( {
 			<InspectorControls>
 				{ !! url && (
 					<PanelBody title={ __( 'Media settings' ) }>
-						{ IMAGE_BACKGROUND_TYPE === backgroundType && (
+						{ isImageBackground && (
 							<ToggleControl
 								label={ __( 'Fixed background' ) }
 								checked={ hasParallax }
 								onChange={ toggleParallax }
 							/>
 						) }
-						{ IMAGE_BACKGROUND_TYPE === backgroundType &&
-							! hasParallax && (
-								<FocalPointPicker
-									label={ __( 'Focal point picker' ) }
-									url={ url }
-									value={ focalPoint }
-									onChange={ ( newFocalPoint ) =>
-										setAttributes( {
-											focalPoint: newFocalPoint,
-										} )
-									}
-								/>
-							) }
-						{ VIDEO_BACKGROUND_TYPE === backgroundType && (
-							<video autoPlay muted loop src={ url } />
+						{ isImageBackground && ! hasParallax && (
```

Oh, that works 👍 .  I'll created another `const` above. Something like `const showFocalPointPicker` so that it's easier to follow.

### Review thread 3 (resolved)

#### youknowriad on 2020-07-14T08:56:31Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454207517
Path: packages/block-library/src/cover/save.js

```diff
@@ -104,6 +115,7 @@ export default function save( { attributes } ) {
 					loop
 					playsInline
 					src={ url }
+					style={ { objectPosition: positionValue } }
```

What happens if no focal point is defined, will we end up with `style=""` in the markup causing invalidation issues?

#### ItsJonQ on 2020-07-14T14:45:53Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454411702
Path: packages/block-library/src/cover/save.js

```diff
@@ -104,6 +115,7 @@ export default function save( { attributes } ) {
 					loop
 					playsInline
 					src={ url }
+					style={ { objectPosition: positionValue } }
```

Ah, good catch. I'll make that update 👍

### Review thread 4 (resolved)

#### youknowriad on 2020-07-14T08:59:27Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454209249
Path: packages/components/src/focal-point-picker/controls.js

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import {
+	UnitControl as BaseUnitControl,
+	ControlWrapper,
+} from './styles/focal-point-picker-style';
+import { fractionToPercentage } from './utils';
+
+const TEXTCONTROL_MIN = 0;
+const TEXTCONTROL_MAX = 100;
+
+export default function FocalPointPickerControls( {
+	onChange = noop,
+	percentages = {
+		x: 0.5,
+		y: 0.5,
+	},
+} ) {
+	const valueX = fractionToPercentage( percentages.x );
+	const valueY = fractionToPercentage( percentages.y );
+
+	const handleOnXChange = ( next ) => {
+		onChange( { ...percentages, x: parseInt( next ) / 100 } );
+	};
+	const handleOnYChange = ( next ) => {
+		onChange( { ...percentages, y: parseInt( next ) / 100 } );
+	};
+
+	return (
+		<ControlWrapper className="focal-point-picker__controls">
+			<UnitControl
+				label={ __( 'Left' ) }
+				value={ valueX }
+				onChange={ handleOnXChange }
+				dragDirection="e"
+			/>
+			<UnitControl
+				label={ __( 'Top' ) }
+				value={ valueY }
+				onChange={ handleOnYChange }
+				dragDirection="s"
+			/>
+		</ControlWrapper>
+	);
+}
+
+function UnitControl( props ) {
+	return (
+		<BaseUnitControl
+			className="focal-point-picker__controls-position-unit-control"
```

Do we really need all the classNames in all the components? Since we're using CSS  in JS maybe not?

#### ItsJonQ on 2020-07-14T14:44:42Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454410653
Path: packages/components/src/focal-point-picker/controls.js

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import {
+	UnitControl as BaseUnitControl,
+	ControlWrapper,
+} from './styles/focal-point-picker-style';
+import { fractionToPercentage } from './utils';
+
+const TEXTCONTROL_MIN = 0;
+const TEXTCONTROL_MAX = 100;
+
+export default function FocalPointPickerControls( {
+	onChange = noop,
+	percentages = {
+		x: 0.5,
+		y: 0.5,
+	},
+} ) {
+	const valueX = fractionToPercentage( percentages.x );
+	const valueY = fractionToPercentage( percentages.y );
+
+	const handleOnXChange = ( next ) => {
+		onChange( { ...percentages, x: parseInt( next ) / 100 } );
+	};
+	const handleOnYChange = ( next ) => {
+		onChange( { ...percentages, y: parseInt( next ) / 100 } );
+	};
+
+	return (
+		<ControlWrapper className="focal-point-picker__controls">
+			<UnitControl
+				label={ __( 'Left' ) }
+				value={ valueX }
+				onChange={ handleOnXChange }
+				dragDirection="e"
+			/>
+			<UnitControl
+				label={ __( 'Top' ) }
+				value={ valueY }
+				onChange={ handleOnYChange }
+				dragDirection="s"
+			/>
+		</ControlWrapper>
+	);
+}
+
+function UnitControl( props ) {
+	return (
+		<BaseUnitControl
+			className="focal-point-picker__controls-position-unit-control"
```

`className` is not required for styling ✌️. However, I kept them to maintain current style workflows + expectations within the project.

#### youknowriad on 2020-07-14T15:01:36Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454423897
Path: packages/components/src/focal-point-picker/controls.js

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import {
+	UnitControl as BaseUnitControl,
+	ControlWrapper,
+} from './styles/focal-point-picker-style';
+import { fractionToPercentage } from './utils';
+
+const TEXTCONTROL_MIN = 0;
+const TEXTCONTROL_MAX = 100;
+
+export default function FocalPointPickerControls( {
+	onChange = noop,
+	percentages = {
+		x: 0.5,
+		y: 0.5,
+	},
+} ) {
+	const valueX = fractionToPercentage( percentages.x );
+	const valueY = fractionToPercentage( percentages.y );
+
+	const handleOnXChange = ( next ) => {
+		onChange( { ...percentages, x: parseInt( next ) / 100 } );
+	};
+	const handleOnYChange = ( next ) => {
+		onChange( { ...percentages, y: parseInt( next ) / 100 } );
+	};
+
+	return (
+		<ControlWrapper className="focal-point-picker__controls">
+			<UnitControl
+				label={ __( 'Left' ) }
+				value={ valueX }
+				onChange={ handleOnXChange }
+				dragDirection="e"
+			/>
+			<UnitControl
+				label={ __( 'Top' ) }
+				value={ valueY }
+				onChange={ handleOnYChange }
+				dragDirection="s"
+			/>
+		</ControlWrapper>
+	);
+}
+
+function UnitControl( props ) {
+	return (
+		<BaseUnitControl
+			className="focal-point-picker__controls-position-unit-control"
```

I think I'm personally ok to remove all these internal classNames for new components using CSS in JS. i'd just leave them for existing ones to avoid dev notes... Your call though.

#### ItsJonQ on 2020-07-14T15:06:23Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454427443
Path: packages/components/src/focal-point-picker/controls.js

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import {
+	UnitControl as BaseUnitControl,
+	ControlWrapper,
+} from './styles/focal-point-picker-style';
+import { fractionToPercentage } from './utils';
+
+const TEXTCONTROL_MIN = 0;
+const TEXTCONTROL_MAX = 100;
+
+export default function FocalPointPickerControls( {
+	onChange = noop,
+	percentages = {
+		x: 0.5,
+		y: 0.5,
+	},
+} ) {
+	const valueX = fractionToPercentage( percentages.x );
+	const valueY = fractionToPercentage( percentages.y );
+
+	const handleOnXChange = ( next ) => {
+		onChange( { ...percentages, x: parseInt( next ) / 100 } );
+	};
+	const handleOnYChange = ( next ) => {
+		onChange( { ...percentages, y: parseInt( next ) / 100 } );
+	};
+
+	return (
+		<ControlWrapper className="focal-point-picker__controls">
+			<UnitControl
+				label={ __( 'Left' ) }
+				value={ valueX }
+				onChange={ handleOnXChange }
+				dragDirection="e"
+			/>
+			<UnitControl
+				label={ __( 'Top' ) }
+				value={ valueY }
+				onChange={ handleOnYChange }
+				dragDirection="s"
+			/>
+		</ControlWrapper>
+	);
+}
+
+function UnitControl( props ) {
+	return (
+		<BaseUnitControl
+			className="focal-point-picker__controls-position-unit-control"
```

Oh? Interesting :). Thank you for your thoughts!

It would encourage a more component (API) driven approach for adjustments. I know that ad-hoc className targeting has proved to be a bit problematic in the block/editor part of the stylesheets. I also understand that those were the most practical solutions at the time.

There are some advantages for having them though. The 2 being that it's easier for debugging via Inspect Element. The other is for selector targeting in testing.

I think we should probably leave them for now :)

Edited: Adding more context

### Review thread 5 (resolved)

#### youknowriad on 2020-07-14T09:03:55Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454211815
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.js

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Flex from '../../flex';
+import BaseUnitControl from '../../unit-control';
+import { color } from '../../utils/style-mixins';
+
+export const MediaWrapper = styled.div`
+	background-color: transparent;
+	box-sizing: border-box;
+	text-align: center;
+	width: 100%;
+`;
+
+export const MediaContainer = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	cursor: pointer;
+	display: inline-flex;
+	justify-content: center;
+	margin: auto;
+	position: relative;
+	height: 100%;
+
+	img,
+	video {
+		box-sizing: border-box;
+		display: block;
+		height: auto;
+		margin: 0;
+		max-height: 100%;
+		max-width: 100%;
+		pointer-events: none;
+		user-select: none;
+		width: auto;
+	}
+`;
+
+export const MediaPlaceholder = styled.div`
+	background: ${ color( 'lightGray.300' ) };
+	box-sizing: border-box;
+	height: 170px;
+	max-width: 280px;
+	min-width: 200px;
+	width: 100%;
+`;
+
+export const UnitControl = styled( BaseUnitControl )`
+	width: 100px;
+`;
```

So for my own knowledge, this means the sub-styles are passed as a `style` prop right? Are there any other magic-tricks done here? 

Also, this seems to force us to add  a `style` prop for most of our components. How do you see React native support fitting here. One principle we had for some time in `@wordpress/components` is that the public API for our components should  be as DOM agnostic as possible.

#### ItsJonQ on 2020-07-14T14:34:26Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454402819
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.js

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Flex from '../../flex';
+import BaseUnitControl from '../../unit-control';
+import { color } from '../../utils/style-mixins';
+
+export const MediaWrapper = styled.div`
+	background-color: transparent;
+	box-sizing: border-box;
+	text-align: center;
+	width: 100%;
+`;
+
+export const MediaContainer = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	cursor: pointer;
+	display: inline-flex;
+	justify-content: center;
+	margin: auto;
+	position: relative;
+	height: 100%;
+
+	img,
+	video {
+		box-sizing: border-box;
+		display: block;
+		height: auto;
+		margin: 0;
+		max-height: 100%;
+		max-width: 100%;
+		pointer-events: none;
+		user-select: none;
+		width: auto;
+	}
+`;
+
+export const MediaPlaceholder = styled.div`
+	background: ${ color( 'lightGray.300' ) };
+	box-sizing: border-box;
+	height: 170px;
+	max-width: 280px;
+	min-width: 200px;
+	width: 100%;
+`;
+
+export const UnitControl = styled( BaseUnitControl )`
+	width: 100px;
+`;
```

`styled()` generated CSS are passed as classNames ✌️ .

```
styled.div`...`

// or

styled(SomeComponent)`...`
```

Creates...

```
<div className="css-12xjx1" />
```

With the generated CSS injected (and managed) as:

```
<style>
    ...
    .css-12xjx1 {...}
    ...
</style>
```

Hope this helps!

#### youknowriad on 2020-07-14T14:35:53Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454404010
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.js

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Flex from '../../flex';
+import BaseUnitControl from '../../unit-control';
+import { color } from '../../utils/style-mixins';
+
+export const MediaWrapper = styled.div`
+	background-color: transparent;
+	box-sizing: border-box;
+	text-align: center;
+	width: 100%;
+`;
+
+export const MediaContainer = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	cursor: pointer;
+	display: inline-flex;
+	justify-content: center;
+	margin: auto;
+	position: relative;
+	height: 100%;
+
+	img,
+	video {
+		box-sizing: border-box;
+		display: block;
+		height: auto;
+		margin: 0;
+		max-height: 100%;
+		max-width: 100%;
+		pointer-events: none;
+		user-select: none;
+		width: auto;
+	}
+`;
+
+export const MediaPlaceholder = styled.div`
+	background: ${ color( 'lightGray.300' ) };
+	box-sizing: border-box;
+	height: 170px;
+	max-width: 280px;
+	min-width: 200px;
+	width: 100%;
+`;
+
+export const UnitControl = styled( BaseUnitControl )`
+	width: 100px;
+`;
```

Oh cool! How does this translate in react native?

#### ItsJonQ on 2020-07-14T14:54:00Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454417987
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.js

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Flex from '../../flex';
+import BaseUnitControl from '../../unit-control';
+import { color } from '../../utils/style-mixins';
+
+export const MediaWrapper = styled.div`
+	background-color: transparent;
+	box-sizing: border-box;
+	text-align: center;
+	width: 100%;
+`;
+
+export const MediaContainer = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	cursor: pointer;
+	display: inline-flex;
+	justify-content: center;
+	margin: auto;
+	position: relative;
+	height: 100%;
+
+	img,
+	video {
+		box-sizing: border-box;
+		display: block;
+		height: auto;
+		margin: 0;
+		max-height: 100%;
+		max-width: 100%;
+		pointer-events: none;
+		user-select: none;
+		width: auto;
+	}
+`;
+
+export const MediaPlaceholder = styled.div`
+	background: ${ color( 'lightGray.300' ) };
+	box-sizing: border-box;
+	height: 170px;
+	max-width: 280px;
+	min-width: 200px;
+	width: 100%;
+`;
+
+export const UnitControl = styled( BaseUnitControl )`
+	width: 100px;
+`;
```

I believe it generates a React Native `Stylesheet` under the hood:

https://emotion.sh/docs/@emotion/native

https://github.com/emotion-js/emotion/blob/master/packages/native/src/styled.js#L7

I've used `styled` from `@emotion/native` in a pure React Native project. It worked great! However, I've personally not worked on project that required the exact same (styled) component to be compatible for both Web and Native.

That would be an interesting challenge!

#### youknowriad on 2020-07-14T15:02:19Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454424473
Path: packages/components/src/focal-point-picker/styles/focal-point-picker-style.js

```diff
@@ -0,0 +1,101 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Flex from '../../flex';
+import BaseUnitControl from '../../unit-control';
+import { color } from '../../utils/style-mixins';
+
+export const MediaWrapper = styled.div`
+	background-color: transparent;
+	box-sizing: border-box;
+	text-align: center;
+	width: 100%;
+`;
+
+export const MediaContainer = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	box-shadow: 0 0 0 1px rgba( 0, 0, 0, 0.2 );
+	cursor: pointer;
+	display: inline-flex;
+	justify-content: center;
+	margin: auto;
+	position: relative;
+	height: 100%;
+
+	img,
+	video {
+		box-sizing: border-box;
+		display: block;
+		height: auto;
+		margin: 0;
+		max-height: 100%;
+		max-width: 100%;
+		pointer-events: none;
+		user-select: none;
+		width: auto;
+	}
+`;
+
+export const MediaPlaceholder = styled.div`
+	background: ${ color( 'lightGray.300' ) };
+	box-sizing: border-box;
+	height: 170px;
+	max-width: 280px;
+	min-width: 200px;
+	width: 100%;
+`;
+
+export const UnitControl = styled( BaseUnitControl )`
+	width: 100px;
+`;
```

Would be cool to see if these new components work well for the mobile team.

### Review thread 6

#### youknowriad on 2020-07-14T09:15:44Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454218659
Path: packages/components/src/focal-point-picker/focal-point.js

```diff
@@ -0,0 +1,49 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import {
+	PointerIconSVG,
+	PointerIconPathFill,
+	PointerIconPathOutline,
+	FocalPointWrapper,
+} from './styles/focal-point-style';
+
+export default function FocalPoint( {
+	coordinates = { left: '50%', top: '50%' },
+	isDragging = false,
+	...props
```

I noticed you do that often (spread extra props) and I'm having trouble figuring out why or whether I consider this a good or bad practice. Are these needed? I know I personally try to avoid them if possible as they can lead to react warnings and can be hard to follow.

#### ItsJonQ on 2020-07-14T14:41:16Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454408215
Path: packages/components/src/focal-point-picker/focal-point.js

```diff
@@ -0,0 +1,49 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import {
+	PointerIconSVG,
+	PointerIconPathFill,
+	PointerIconPathOutline,
+	FocalPointWrapper,
+} from './styles/focal-point-style';
+
+export default function FocalPoint( {
+	coordinates = { left: '50%', top: '50%' },
+	isDragging = false,
+	...props
```

For component libraries, prop spreading is common and often the default. The reason is because the consumer should be able to use HTML approved properties (like `aria-*` or `data-*`) just like they would on any HTML element. Not only that, but `on*` event callbacks should be available as well.

A (mostly) reliable solution for handling React warnings is that inner components often have prop filtering built in. In the case of components created with `@emotion/styled`, they filter out non React/HTML props before passing it down to elements like `<div />`, `<input />`, etc...

#### youknowriad on 2020-07-14T15:05:59Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454427179
Path: packages/components/src/focal-point-picker/focal-point.js

```diff
@@ -0,0 +1,49 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import {
+	PointerIconSVG,
+	PointerIconPathFill,
+	PointerIconPathOutline,
+	FocalPointWrapper,
+} from './styles/focal-point-style';
+
+export default function FocalPoint( {
+	coordinates = { left: '50%', top: '50%' },
+	isDragging = false,
+	...props
```

> For component libraries, prop spreading is common and often the default. The reason is because the consumer should be able to use HTML approved properties (like aria-* or data-*) just like they would on any HTML element. Not only that, but on* event callbacks should be available as well.

When I use `FocalPointPicker` as a user, I don't know its internals whether it's a div, a button, or just a string... which makes me think, I don't know what HTML  prop are approved. This also makes it harder to be compatible with mobile. A block doing `< FocalPointPicker some-dom-specific-prop="something" />` won't translate properly to mobile. 

It makes me wonder if we should try to move away from these iteratively.

### Review thread 7

#### youknowriad on 2020-07-14T09:19:38Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454220816
Path: packages/components/src/focal-point-picker/grid.js

```diff
@@ -0,0 +1,79 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	GridView,
+	GridLineX,
+	GridLineY,
+} from './styles/focal-point-picker-style';
+
+export default function FocalPointPickerGrid( {
+	bounds = {},
```

Do you think bounds is better than height/width? I'm mainly thinking in terms of consistency or should it be `dimensions` like the prop for `FocalPointPicker`?

### Review thread 8 (resolved)

#### youknowriad on 2020-07-14T09:22:28Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454222520
Path: packages/components/src/focal-point-picker/grid.js

```diff
@@ -0,0 +1,79 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	GridView,
+	GridLineX,
+	GridLineY,
+} from './styles/focal-point-picker-style';
+
+export default function FocalPointPickerGrid( {
+	bounds = {},
+	percentages = {
```

Seems like the only thing we need here is a new instance of the value when it changed. Do you think we should just name this `value` and avoid any computation/destructuring? As written it gives the impression that we do need each value separately but by looking at the code it's not really the case, we just need a new instance.

#### ItsJonQ on 2020-07-14T16:29:25Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454485302
Path: packages/components/src/focal-point-picker/grid.js

```diff
@@ -0,0 +1,79 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	GridView,
+	GridLineX,
+	GridLineY,
+} from './styles/focal-point-picker-style';
+
+export default function FocalPointPickerGrid( {
+	bounds = {},
+	percentages = {
```

Works for me

### Review thread 9 (resolved)

#### youknowriad on 2020-07-14T09:25:21Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454224133
Path: packages/components/src/focal-point-picker/grid.js

```diff
@@ -0,0 +1,79 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	GridView,
+	GridLineX,
+	GridLineY,
+} from './styles/focal-point-picker-style';
+
+export default function FocalPointPickerGrid( {
+	bounds = {},
+	percentages = {
+		x: 0.5,
+		y: 0.5,
+	},
+	...props
+} ) {
+	const percentageValue = percentages.x + percentages.y;
+	const animationProps = useRevealAnimation( percentageValue );
+	const style = {
+		width: bounds.width,
+		height: bounds.height,
+	};
+
+	return (
+		<GridView
+			{ ...props }
+			{ ...animationProps }
+			className="components-focal-point-picker__grid"
+			style={ style }
+		>
+			<GridLineX style={ { top: '33%' } } />
+			<GridLineX style={ { top: '66%' } } />
+			<GridLineY style={ { left: '33%' } } />
+			<GridLineY style={ { left: '66%' } } />
+		</GridView>
+	);
+}
+
+/**
+ * Custom hook that renders the "flash" animation whenever the value changes.
+ *
+ * @param {string} value Value of (box) side.
+ */
+function useRevealAnimation( value ) {
+	const [ isActive, setIsActive ] = useState( false );
+	const valueRef = useRef( value );
+	const timeoutRef = useRef();
+
+	const clearTimer = () => {
+		if ( timeoutRef.current ) {
+			window.clearTimeout( timeoutRef.current );
+		}
+	};
+
+	useEffect( () => {
+		if ( value !== valueRef.current ) {
+			setIsActive( true );
+			valueRef.current = value;
+
+			clearTimer();
+
+			timeoutRef.current = setTimeout( () => {
+				setIsActive( false );
+			}, 600 );
+		}
+
+		return () => clearTimer();
+	}, [ value ] );
```

Seems like this could be simplied to just:

```
useEffect( () => {
 	setIsActive( true );
 	const timeout = setTimeout( () => {
 		setIsActive( false );
 	}, 600 );

 	return () => window.clearTimeout( timeout );
 }, [ value ] );
```

and avoid all refs. Am I missing anything?

#### ItsJonQ on 2020-07-14T16:40:19Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454492206
Path: packages/components/src/focal-point-picker/grid.js

```diff
@@ -0,0 +1,79 @@
+/**
+ * WordPress dependencies
+ */
+import { useRef, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	GridView,
+	GridLineX,
+	GridLineY,
+} from './styles/focal-point-picker-style';
+
+export default function FocalPointPickerGrid( {
+	bounds = {},
+	percentages = {
+		x: 0.5,
+		y: 0.5,
+	},
+	...props
+} ) {
+	const percentageValue = percentages.x + percentages.y;
+	const animationProps = useRevealAnimation( percentageValue );
+	const style = {
+		width: bounds.width,
+		height: bounds.height,
+	};
+
+	return (
+		<GridView
+			{ ...props }
+			{ ...animationProps }
+			className="components-focal-point-picker__grid"
+			style={ style }
+		>
+			<GridLineX style={ { top: '33%' } } />
+			<GridLineX style={ { top: '66%' } } />
+			<GridLineY style={ { left: '33%' } } />
+			<GridLineY style={ { left: '66%' } } />
+		</GridView>
+	);
+}
+
+/**
+ * Custom hook that renders the "flash" animation whenever the value changes.
+ *
+ * @param {string} value Value of (box) side.
+ */
+function useRevealAnimation( value ) {
+	const [ isActive, setIsActive ] = useState( false );
+	const valueRef = useRef( value );
+	const timeoutRef = useRef();
+
+	const clearTimer = () => {
+		if ( timeoutRef.current ) {
+			window.clearTimeout( timeoutRef.current );
+		}
+	};
+
+	useEffect( () => {
+		if ( value !== valueRef.current ) {
+			setIsActive( true );
+			valueRef.current = value;
+
+			clearTimer();
+
+			timeoutRef.current = setTimeout( () => {
+				setIsActive( false );
+			}, 600 );
+		}
+
+		return () => clearTimer();
+	}, [ value ] );
```

That works. I initially created refs so that the animation would not flash on first render. I was able to combine your solution with the `useUpdateEffect` hook from another PR:

https://github.com/WordPress/gutenberg/blob/8a35791d60f8a69e2d155990e9c7e564248a4713/packages/components/src/utils/use-update-effect.js

### Review thread 10 (resolved)

#### youknowriad on 2020-07-14T09:26:35Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454224884
Path: packages/components/src/focal-point-picker/index.js

```diff
@@ -9,62 +10,86 @@ import classnames from 'classnames';
 import { __ } from '@wordpress/i18n';
 import { Component, createRef } from '@wordpress/element';
 import { withInstanceId, compose } from '@wordpress/compose';
-import { Path, SVG } from '@wordpress/primitives';
+import { UP, DOWN, LEFT, RIGHT } from '@wordpress/keycodes';
 
 /**
  * Internal dependencies
  */
-import BaseControl from '../base-control';
 import withFocusOutside from '../higher-order/with-focus-outside';
-
-const TEXTCONTROL_MIN = 0;
-const TEXTCONTROL_MAX = 100;
+import BaseControl from '../base-control';
+import Controls from './controls';
+import FocalPoint from './focal-point';
+import Grid from './grid';
+import Media from './media';
+import {
+	MediaWrapper,
+	MediaContainer,
+} from './styles/focal-point-picker-style';
+import { roundClamp } from '../utils/math';
+import { INITIAL_BOUNDS } from './utils';
 
 export class FocalPointPicker extends Component {
-	constructor( props ) {
-		super( props );
-		this.onMouseMove = this.onMouseMove.bind( this );
+	constructor( props, context ) {
+		super( props, context );
```

in general we used to use this pattern `super( ...arguments )` but it's the same thing.

#### ItsJonQ on 2020-07-14T15:59:59Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454465682
Path: packages/components/src/focal-point-picker/index.js

```diff
@@ -9,62 +10,86 @@ import classnames from 'classnames';
 import { __ } from '@wordpress/i18n';
 import { Component, createRef } from '@wordpress/element';
 import { withInstanceId, compose } from '@wordpress/compose';
-import { Path, SVG } from '@wordpress/primitives';
+import { UP, DOWN, LEFT, RIGHT } from '@wordpress/keycodes';
 
 /**
  * Internal dependencies
  */
-import BaseControl from '../base-control';
 import withFocusOutside from '../higher-order/with-focus-outside';
-
-const TEXTCONTROL_MIN = 0;
-const TEXTCONTROL_MAX = 100;
+import BaseControl from '../base-control';
+import Controls from './controls';
+import FocalPoint from './focal-point';
+import Grid from './grid';
+import Media from './media';
+import {
+	MediaWrapper,
+	MediaContainer,
+} from './styles/focal-point-picker-style';
+import { roundClamp } from '../utils/math';
+import { INITIAL_BOUNDS } from './utils';
 
 export class FocalPointPicker extends Component {
-	constructor( props ) {
-		super( props );
-		this.onMouseMove = this.onMouseMove.bind( this );
+	constructor( props, context ) {
+		super( props, context );
```

That makes sense to me 👍

### Review thread 11 (resolved)

#### youknowriad on 2020-07-14T09:32:43Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454228476
Path: packages/components/src/input-control/index.js

```diff
@@ -37,6 +37,7 @@ export function InputControl(
 		isPressEnterToChange = false,
 		isFloatingLabel = false,
 		label,
+		labelPosition = 'top',
```

Do you see this as something available in all *Control components?

#### ItsJonQ on 2020-07-14T14:30:13Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454399535
Path: packages/components/src/input-control/index.js

```diff
@@ -37,6 +37,7 @@ export function InputControl(
 		isPressEnterToChange = false,
 		isFloatingLabel = false,
 		label,
+		labelPosition = 'top',
```

Unsure yet! However, I know there are certain UIs where a top vs bottom label position would be beneficial. ColorPicker is a good example. Also BoxControl. I guess we'll see :)

### Review thread 12 (resolved)

#### youknowriad on 2020-07-14T09:33:39Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454228999
Path: packages/e2e-tests/fixtures/blocks/core__cover__deprecated-4.serialized.html

```diff
@@ -1,5 +1,5 @@
 <!-- wp:cover {"url":"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==","id":35,"focalPoint":{"x":"0.07","y":"0.07"}} -->
-<div class="wp-block-cover has-background-dim" style="background-image:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==);background-position:7% 7%"><div class="wp-block-cover__inner-container"><!-- wp:paragraph {"align":"center","placeholder":"Write title…","fontSize":"large"} -->
+<div class="wp-block-cover has-background-dim" style="background-image:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==);background-position:7.000000000000001% 7.000000000000001%"><div class="wp-block-cover__inner-container"><!-- wp:paragraph {"align":"center","placeholder":"Write title…","fontSize":"large"} -->
```

Seems like this change is an indication that something is wrong with the "rounding" of the focal point value.

#### ItsJonQ on 2020-07-14T14:27:47Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454397651
Path: packages/e2e-tests/fixtures/blocks/core__cover__deprecated-4.serialized.html

```diff
@@ -1,5 +1,5 @@
 <!-- wp:cover {"url":"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==","id":35,"focalPoint":{"x":"0.07","y":"0.07"}} -->
-<div class="wp-block-cover has-background-dim" style="background-image:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==);background-position:7% 7%"><div class="wp-block-cover__inner-container"><!-- wp:paragraph {"align":"center","placeholder":"Write title…","fontSize":"large"} -->
+<div class="wp-block-cover has-background-dim" style="background-image:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==);background-position:7.000000000000001% 7.000000000000001%"><div class="wp-block-cover__inner-container"><!-- wp:paragraph {"align":"center","placeholder":"Write title…","fontSize":"large"} -->
```

This is really odd... looking into this now!

#### ItsJonQ on 2020-07-14T17:21:31Z

URL: https://github.com/WordPress/gutenberg/pull/22531#discussion_r454517970
Path: packages/e2e-tests/fixtures/blocks/core__cover__deprecated-4.serialized.html

```diff
@@ -1,5 +1,5 @@
 <!-- wp:cover {"url":"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==","id":35,"focalPoint":{"x":"0.07","y":"0.07"}} -->
-<div class="wp-block-cover has-background-dim" style="background-image:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==);background-position:7% 7%"><div class="wp-block-cover__inner-container"><!-- wp:paragraph {"align":"center","placeholder":"Write title…","fontSize":"large"} -->
+<div class="wp-block-cover has-background-dim" style="background-image:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAACklEQVR4nGMAAQAABQABDQottAAAAABJRU5ErkJggg==);background-position:7.000000000000001% 7.000000000000001%"><div class="wp-block-cover__inner-container"><!-- wp:paragraph {"align":"center","placeholder":"Write title…","fontSize":"large"} -->
```

Fixed! I needed to wrap the values with `Math.round` in `block/save.js`

## Changed files

- packages/block-library/src/cover/edit.js (MODIFIED, +27/-24)
- packages/block-library/src/cover/save.js (MODIFIED, +25/-10)
- packages/components/src/focal-point-picker/README.md (MODIFIED, +48/-34)
- packages/components/src/focal-point-picker/controls.js (ADDED, +70/-0)
- packages/components/src/focal-point-picker/focal-point.js (ADDED, +49/-0)
- packages/components/src/focal-point-picker/grid.js (ADDED, +64/-0)
- packages/components/src/focal-point-picker/index.js (MODIFIED, +239/-182)
- packages/components/src/focal-point-picker/media.js (ADDED, +78/-0)
- packages/components/src/focal-point-picker/stories/index.js (ADDED, +46/-0)
- packages/components/src/focal-point-picker/style.scss (DELETED, +0/-75)
- packages/components/src/focal-point-picker/styles/focal-point-picker-style.js (ADDED, +101/-0)
- packages/components/src/focal-point-picker/styles/focal-point-style.js (ADDED, +49/-0)
- packages/components/src/focal-point-picker/test/media.js (ADDED, +58/-0)
- packages/components/src/focal-point-picker/utils.js (ADDED, +53/-0)
- packages/components/src/input-control/index.js (MODIFIED, +5/-0)
- packages/components/src/input-control/stories/index.js (MODIFIED, +9/-0)
- packages/components/src/input-control/styles/input-control-styles.js (MODIFIED, +34/-5)
- packages/components/src/style.scss (MODIFIED, +0/-1)
- packages/components/src/utils/hooks/index.js (MODIFIED, +1/-0)
- packages/components/src/utils/hooks/use-update-effect.js (ADDED, +23/-0)
