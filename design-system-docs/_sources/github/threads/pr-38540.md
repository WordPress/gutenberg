---
number: 38540
type: pull_request
state: MERGED
merged_at: 2022-02-10T17:32:19Z
url: https://github.com/WordPress/gutenberg/pull/38540
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components"]
---

# PR #38540: Add documentation for deprecating styles.

- URL: https://github.com/WordPress/gutenberg/pull/38540
- Author: mirka
- Created: 2022-02-04T21:34:00Z
- Updated: 2022-02-26T19:00:53Z
- Comments: 0 of 0
- Review threads: 4 of 4
- Changed files: 1 of 1
- Diff: ../pr-diffs/38540.diff

## Body

## Description

Add a "Deprecating styles" section to the wp-components contributors doc.

I mostly want to discuss and align on the style deprecation strategy here, not necessarily saying we have to include it in the devdocs. I thought through a [bunch of options](https://github.com/WordPress/gutenberg/files/8006332/Deprecate.Styles.pdf), and this is the strategy I am proposing. Thoughts?

## Types of changes

Docs only.

## Issue comments

_No issue comments._
## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-02-08T14:43:49Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r801708398
Path: packages/components/CONTRIBUTING.md

```diff
@@ -32,7 +32,10 @@ In these situations, one possible approach is to "soft-deprecate" a given legacy
 
 When adding new components or new props to existing components, it's recommended to prefix them with `__unstable` or `__experimental` until they're stable enough to be exposed as part of the public API.
 
-Learn more on [How to preserve backward compatibility for a React Component](/docs/how-to-guides/backward-compatibility/README.md#how-to-preserve-backward-compatibility-for-a-react-component) and [Experimental and Unstable APIs](/docs/contributors/code/coding-guidelines.md#experimental-and-unstable-apis).
+### Learn more
```

Nit: when opened locally, my linter warns about a missing blank line under the title

### Review thread 2

#### ciampo on 2022-02-08T14:48:01Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r801713175
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

Should this deprecation strategy be recommended also beyond styles? It seems like a reasonable strategy to use also for behavioural changes, API changes, etc...

#### mirka on 2022-02-08T21:56:49Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r802092930
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

I do see this as a special case of deprecating APIs, where there is no actual prop to deprecate and it's just a change in default behavior. Looking at the instances in `components/*` where `deprecated()` is called, I'm not sure this kind of "change in default" deprecation has ever happened. Are you aware of any?

I did choose the `__next*` prefix so it would be appropriate for behavioral changes as well though, so it probably would be generally applicable when it happens. I think we could reuse or refactor this section when we write a more detailed [deprecation guide for components](https://github.com/WordPress/gutenberg/blob/docs/deprecating-styles/docs/contributors/code/backward-compatibility.md#how-to-preserve-backward-compatibility-for-a-react-component).

By the way I just learned about [this section](https://github.com/WordPress/gutenberg/blob/docs/deprecating-styles/docs/contributors/code/backward-compatibility.md#dev-notes) that asks us to add a "Needs Dev Note" label to the PR. I'll add this into the doc.

#### ciampo on 2022-02-09T22:25:27Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803140895
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

> Are you aware of any?

On top of my mind, no. Hey @sarayourfriend (sorry for the ping!) — do you ever remember an instance of a deprecation where the meaning of a style/prop was changed (causing a potentially breaking change)?

> I did choose the __next* prefix so it would be appropriate for behavioral changes as well though, so it probably would be generally applicable when it happens. I think we could reuse or refactor this section when we write a more detailed [deprecation guide for components](https://github.com/WordPress/gutenberg/blob/docs/deprecating-styles/docs/contributors/code/backward-compatibility.md#how-to-preserve-backward-compatibility-for-a-react-component).

Sounds good to me. Having a defined way to "safely" introduce a breaking change is great and we should absolutely iterate on this procedure in order to be able to apply it to more deprecation scenarios

> By the way I just learned about [this section](https://github.com/WordPress/gutenberg/blob/docs/deprecating-styles/docs/contributors/code/backward-compatibility.md#dev-notes) that asks us to add a "Needs Dev Note" label to the PR. I'll add this into the doc.

TIL !

#### sarayourfriend on 2022-02-10T02:09:59Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803243649
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

> do you ever remember an instance of a deprecation where the meaning of a style/prop was changed (causing a potentially breaking change)

I can't think of any from my time working on wp/components but that's mostly because I'm fairly certain we only "soft" deprecated things.

We did change the `variant` prop on the `Text` component, but it was experimental. We originally wrote an adapter for it IIRC but it isn't there anymore, or maybe it was never merged because the component was experimental anyway.

Although, does this count? https://github.com/WordPress/gutenberg/pull/33490

#### ciampo on 2022-02-10T08:33:35Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803417441
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

Yeah, that's what I thought as well.

> Although, does this count? https://github.com/WordPress/gutenberg/pull/33490

Not sure if it does, I'd probably see it as more of a bug fix — `isScrollable` was implicitly introduced to `CardBody` in a previous PR, and the PR that you highlighted changes its default value to _fix_ that regression.

#### sarayourfriend on 2022-02-10T11:34:23Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803582146
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

Right right... I feel like I remember seeing a bug raised where we'd accidentally left a deprecation or prop-meaning change out of the changelogs somewhere and it'd caused problems for someone consuming the package. Just can't remember how to find it now 😞

#### ciampo on 2022-02-10T13:16:03Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803663336
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

I believe the original "breaking change" was caused by my refactor of `Card` in #32566

#### sarayourfriend on 2022-02-10T13:36:18Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803680632
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

Ahhh yes! That is what I was remembering. But it was just an accidental prop value rename 😬 Doesn't really match what we're talking about here.

#### mirka on 2022-02-10T17:32:10Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r803931560
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
```

Good to know, thanks!

### Review thread 3 (resolved)

#### ciampo on 2022-02-08T14:51:20Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r801716906
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
+
+Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
+
+```jsx
+// component.tsx
+import deprecated from '@wordpress/deprecated';
+import { Wrapper } from './styles.ts';
+
+function MyComponent({ __nextHasNoOuterMargins = false }) {
+	if ( ! __nextHasNoOuterMargins ) {
+		deprecated( 'Outer margin styles for wp.components.MyComponent', {
+			since: '6.0',
+			version: '6.2', // Set a reasonable grace period depending on impact
+			hint:
+				'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
+		} );
+	}
+	return <Wrapper __nextHasNoOuterMargins={__nextHasNoOuterMargins} />
+}
+```
+
+Styles should be structured so the deprecated styles are cleanly encapsulated, and can be easily removed when the deprecation version arrives.
+
+```js
+// styles.ts
+const deprecatedMargins = ({ __nextHasNoOuterMargins }) => {
+  if ( ! __nextHasNoOuterMargins ) {
+    return css`
+      margin: 8px;
+    `;
+  }
+};
+
+export const Wrapper = styled.div`
+  margin: 0;
+
+  ${deprecatedMargins}
+`;
+```
+
+Once deprecated, code examples in docs/stories should include the opt-in prop set to `true` so that new consumers are encouraged to adopt it from the start.
```

To explain the procedure more explicitly, we could mention that after the grace period is over, the `__next` property (with its deprecation notice) should be removed from the code completely and that the new styles/behaviour should be always applied.

#### mirka on 2022-02-08T21:29:20Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r802074194
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
+
+Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
+
+```jsx
+// component.tsx
+import deprecated from '@wordpress/deprecated';
+import { Wrapper } from './styles.ts';
+
+function MyComponent({ __nextHasNoOuterMargins = false }) {
+	if ( ! __nextHasNoOuterMargins ) {
+		deprecated( 'Outer margin styles for wp.components.MyComponent', {
+			since: '6.0',
+			version: '6.2', // Set a reasonable grace period depending on impact
+			hint:
+				'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
+		} );
+	}
+	return <Wrapper __nextHasNoOuterMargins={__nextHasNoOuterMargins} />
+}
+```
+
+Styles should be structured so the deprecated styles are cleanly encapsulated, and can be easily removed when the deprecation version arrives.
+
+```js
+// styles.ts
+const deprecatedMargins = ({ __nextHasNoOuterMargins }) => {
+  if ( ! __nextHasNoOuterMargins ) {
+    return css`
+      margin: 8px;
+    `;
+  }
+};
+
+export const Wrapper = styled.div`
+  margin: 0;
+
+  ${deprecatedMargins}
+`;
+```
+
+Once deprecated, code examples in docs/stories should include the opt-in prop set to `true` so that new consumers are encouraged to adopt it from the start.
```

Good idea 👍

### Review thread 4 (resolved)

#### ciampo on 2022-02-08T14:52:12Z

URL: https://github.com/WordPress/gutenberg/pull/38540#discussion_r801717887
Path: packages/components/CONTRIBUTING.md

```diff
@@ -180,6 +183,65 @@ All new component should be styled using [Emotion](https://emotion.sh/docs/intro
 
 Note: Instead of using Emotion's standard `cx` function, the custom [`useCx` hook](/packages/components/src/utils/hooks/use-cx.ts) should be used instead.
 
+
+### Deprecating styles
+
+Changing the styles of a non-experimental component must be done with care. To prevent serious breakage in third-party usage, in some cases we may want a grace period before fully removing the old styles. This can be done by temporarily placing the new styles behind a feature flag prop prefixed by `__next`, accompanied by a `deprecate()` warning in the console. The feature flag should be opt-in (false by default), and have a reasonably descriptive name (**not** `__nextHasNewStyles`). A descriptive name allows for multiple deprecations to proceed in parallel, separated by concerns or by deprecation version.
+
+```jsx
+// component.tsx
+import deprecated from '@wordpress/deprecated';
+import { Wrapper } from './styles.ts';
+
+function MyComponent({ __nextHasNoOuterMargins = false }) {
+	if ( ! __nextHasNoOuterMargins ) {
+		deprecated( 'Outer margin styles for wp.components.MyComponent', {
+			since: '6.0',
+			version: '6.2', // Set a reasonable grace period depending on impact
+			hint:
+				'Set the `__nextHasNoOuterMargins` prop to true to start opting into the new styles, which will become the default in a future version.',
+		} );
+	}
+	return <Wrapper __nextHasNoOuterMargins={__nextHasNoOuterMargins} />
+}
+```
+
+Styles should be structured so the deprecated styles are cleanly encapsulated, and can be easily removed when the deprecation version arrives.
+
+```js
+// styles.ts
+const deprecatedMargins = ({ __nextHasNoOuterMargins }) => {
+  if ( ! __nextHasNoOuterMargins ) {
+    return css`
+      margin: 8px;
+    `;
+  }
+};
+
+export const Wrapper = styled.div`
+  margin: 0;
+
+  ${deprecatedMargins}
+`;
+```
+
+Once deprecated, code examples in docs/stories should include the opt-in prop set to `true` so that new consumers are encouraged to adopt it from the start.
+
+#### Criteria for putting styles changes behind a feature flag
+
+Not all style changes justify a formal deprecation process. The main thing to look for is whether the changes could cause layouts to break in an obvious or harmful way, given that the component is being used in a standard fashion.
+
+##### DOES need formal deprecation
+
+- Removing an outer margin.
+- Substantial changes to width/height, such as adding or removing a size restriction.
+
+##### DOES NOT need formal deprecation
+
+- Breakage only occurs in non-standard usage, such as when the consumer is overriding component internals.
+- Minor layout shifts of a few pixels.
+- Internal layout changes of a higher-level component.
```

Really like this section with DOs and DON'Ts

## Changed files

- packages/components/CONTRIBUTING.md (MODIFIED, +68/-1)
