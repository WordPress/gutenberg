---
number: 79460
type: pull_request
state: MERGED
merged_at: 2026-06-25T14:43:52Z
url: https://github.com/WordPress/gutenberg/pull/79460
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components","Storybook","[Package] DataViews"]
---

# PR #79460: Components: Add missing descriptions for design system components

- URL: https://github.com/WordPress/gutenberg/pull/79460
- Author: aduth
- Created: 2026-06-23T20:31:34Z
- Updated: 2026-06-25T14:44:31Z
- Comments: 5 of 5
- Review threads: 4 of 4
- Changed files: 14 of 14
- Diff: ../pr-diffs/79460.diff

## Body

## What?

Adds JSDoc descriptions for all components which are surfaced as design system components through the Storybook manifest file, which in turn are used by the `@wordpress/design-system-mcp` package.

In doing so, it also fixes missing component props for three components: ColorPicker, DataViews, and Navigator.

## Why?

Descriptions are useful in order to understand what a component does and whether it's usage is applicable to a given use-case. 

With AI agent workflows leveraging the `@wordpress/design-system-mcp`, descriptions are a particularly important part of the agent workflow, since an AI agent is [instructed](https://github.com/WordPress/gutenberg/blob/8d6753830067140240d9337a631746cf4ee72baf/packages/design-system-mcp/src/index.ts#L15) to get a list of components via the `get_components` tools before diving into individual component details with `get_component_details`. Since the only information surfaced on `get_components` listing is the component name and description, the description is a pivotal detail to cue an agent to retrieve more information if it would understand it to be relevant for a given request.

## How?

Adds missing JSDoc documentation to components.

For a few of these, I had to employ the same workaround as in #77382 to ensure that the docgen tool correctly parses these components, via inline `render` configuration in the component stories. This should be temporary and [may already be resolved upstream](https://github.com/WordPress/gutenberg/pull/77382#issuecomment-4770996707). Worth noting that this fixes a preexisting issue with these components props not being surfaced in the manifest (see "3/66 prop type errors" on [live components HTML manifest](https://wordpress.github.io/gutenberg/manifests/components.html)).

## Testing Instructions

Verify that built Storybook manifest parses all components successfully and all components have descriptions:

1. Run `npm run storybook:build`
2. Run `open storybook/build/manifests/components.html`
3. Observe "66 components okay" (compare to "3/66 prop type errors" on [live components HTML manifest](https://wordpress.github.io/gutenberg/manifests/components.html))
4. Observe every listed component has a description (compare to [live components HTML manifest](https://wordpress.github.io/gutenberg/manifests/components.html) missing descriptions for affected components like ColorPicker, DataViews, etc.)

Verify that descriptions appear in live Storybook site:

1. Run `npm run storybook:dev`
2. In the launched Storybook site at http://localhost:50240/, navigate to a Storybook page for one of the affected components (e.g. ColorPicker)
3. Observe that the Storybook shows a description

## Screenshots or screencast <!-- if applicable -->

<img width="1557" height="1173" alt="Screenshot 2026-06-23 at 4 29 18 PM" src="https://github.com/user-attachments/assets/8c6aa0da-b66c-434d-b2bf-df0b0c0bbd1a" />

## Use of AI Tools

Assessment and initial implementation (including descriptions themselves) with Claude Opus 4.8. Manual review and edits of most of the descriptions for readability and conciseness.

## Issue comments

### github-actions on 2026-06-23T20:40:20Z

URL: https://github.com/WordPress/gutenberg/pull/79460#issuecomment-4783269051

**Size Change:** 0 B 

**Total Size:** 7.51 MB



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-23T20:45:47Z

URL: https://github.com/WordPress/gutenberg/pull/79460#issuecomment-4783318691

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: ntsekouras <ntsekouras@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### aduth on 2026-06-23T20:46:54Z

URL: https://github.com/WordPress/gutenberg/pull/79460#issuecomment-4783329848

> Worth noting that this fixes a preexisting issue with these components props not being surfaced in the manifest (see "3/66 prop type errors" on [live components HTML manifest](https://wordpress.github.io/gutenberg/manifests/components.html)).

Note that I'm actively working on a solution for better detection of this kind of problem through the issue tracked at #79415.

### aduth on 2026-06-23T20:53:07Z

URL: https://github.com/WordPress/gutenberg/pull/79460#issuecomment-4783373376

I'm not sure why the CHANGELOG CI check is failing for the `theme` package 🤔 I'm not touching the theme package.

### aduth on 2026-06-25T14:03:38Z

URL: https://github.com/WordPress/gutenberg/pull/79460#issuecomment-4800170037

> I'm not sure why the CHANGELOG CI check is failing for the `theme` package 🤔 I'm not touching the theme package.

Found the issue and fixed separately: #79548

## Review threads

### Review thread 1 (resolved)

#### ntsekouras on 2026-06-24T06:51:15Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3465101688
Path: packages/components/src/color-picker/component.tsx

```diff
@@ -247,6 +247,10 @@ const UnconnectedColorPicker = (
 	);
 };
 
+/**
+ * ColorPicker lets users select a color from a visual color surface, or by
```

```suggestion
 * ColorPicker lets users select a color from a visual color surface, or
```

#### aduth on 2026-06-24T13:35:55Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3467491326
Path: packages/components/src/color-picker/component.tsx

```diff
@@ -247,6 +247,10 @@ const UnconnectedColorPicker = (
 	);
 };
 
+/**
+ * ColorPicker lets users select a color from a visual color surface, or by
```

Thanks, updated in ac6abdc967896d0eacc3b2c807e7f4169f1caa64

### Review thread 2 (resolved)

#### ntsekouras on 2026-06-24T07:04:20Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3465165366
Path: tools/eslint/config.mjs

```diff
@@ -557,12 +557,7 @@ export default dedupePlugins( [
 	// component always receives props and returns a React element, and its
 	// props should be documented through its TypeScript props types.
 	{
-		files: [
-			'**/@(storybook|stories)/**',
-			'packages/components/src/**/*.tsx',
-			'packages/theme/src/**/*.tsx',
-			'packages/ui/src/**/*.tsx',
-		],
+		files: [ '**/@(storybook|stories)/**', '**/*.tsx' ],
```

Should we add the dataviews package in the list instead of widening the rule?

#### aduth on 2026-06-24T10:56:45Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3466515823
Path: tools/eslint/config.mjs

```diff
@@ -557,12 +557,7 @@ export default dedupePlugins( [
 	// component always receives props and returns a React element, and its
 	// props should be documented through its TypeScript props types.
 	{
-		files: [
-			'**/@(storybook|stories)/**',
-			'packages/components/src/**/*.tsx',
-			'packages/theme/src/**/*.tsx',
-			'packages/ui/src/**/*.tsx',
-		],
+		files: [ '**/@(storybook|stories)/**', '**/*.tsx' ],
```

The reason for this override as described in the comment above (which, granted, I had written myself in a previous pull request) isn't really specific to any package, so maintaining an allowlist of individual packages doesn't really make much sense to me.

https://github.com/WordPress/gutenberg/blob/f879379368d21b780449e01c9705ece49d508577/tools/eslint/config.mjs#L554-L556

Or, vice-versa, if we don't agree with the reasoning for this override, then we should question why any of these packages are exempt.

### Review thread 3 (resolved)

#### ciampo on 2026-06-24T07:47:37Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3465414389
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -52,6 +52,11 @@ function getDescribedBy( currentName: string, describedBy?: string ) {
 	return sprintf( __( 'Currently selected: %s' ), currentName );
 }
 
+/**
+ * CustomSelectControl is a dropdown for selecting a single option from a list,
```

Nit: formatting 

```suggestion
 * `CustomSelectControl` is a dropdown for selecting a single option from a list,
```

#### ciampo on 2026-06-24T07:47:52Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3465415730
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -52,6 +52,11 @@ function getDescribedBy( currentName: string, describedBy?: string ) {
 	return sprintf( __( 'Currently selected: %s' ), currentName );
 }
 
+/**
+ * CustomSelectControl is a dropdown for selecting a single option from a list,
```

Here and throughout the PR

#### aduth on 2026-06-24T13:36:05Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3467492404
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -52,6 +52,11 @@ function getDescribedBy( currentName: string, describedBy?: string ) {
 	return sprintf( __( 'Currently selected: %s' ), currentName );
 }
 
+/**
+ * CustomSelectControl is a dropdown for selecting a single option from a list,
```

👍 Updated in cbe3ac06b029d0cd6bed341377aa1d8d2db0b6b4

### Review thread 4 (resolved)

#### ciampo on 2026-06-24T07:48:42Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3465419980
Path: packages/components/src/number-control/index.tsx

```diff
@@ -276,6 +276,9 @@ function UnforwardedNumberControl(
 	);
 }
 
+/**
+ * NumberControl lets users enter and adjust a numeric value.
```

Should e somehow mention the word "input" ?

#### aduth on 2026-06-24T13:36:39Z

URL: https://github.com/WordPress/gutenberg/pull/79460#discussion_r3467496171
Path: packages/components/src/number-control/index.tsx

```diff
@@ -276,6 +276,9 @@ function UnforwardedNumberControl(
 	);
 }
 
+/**
+ * NumberControl lets users enter and adjust a numeric value.
```

> Should e somehow mention the word "input" ?

Yeah good call. I recall the initial AI suggestion was quite wordy but I might have gone overboard with trimming it down 🙂 Hopefully clearer in 0c286d33e8b6c6e1a2e11ac28219d1dcf0d9a253 ?

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/color-picker/component.tsx (MODIFIED, +4/-0)
- packages/components/src/color-picker/stories/index.story.tsx (MODIFIED, +5/-0)
- packages/components/src/custom-select-control/index.tsx (MODIFIED, +5/-0)
- packages/components/src/navigator/stories/index.story.tsx (MODIFIED, +5/-0)
- packages/components/src/number-control/index.tsx (MODIFIED, +4/-0)
- packages/components/src/resizable-box/index.tsx (MODIFIED, +4/-0)
- packages/components/src/slot-fill/index.tsx (MODIFIED, +5/-0)
- packages/dataviews/CHANGELOG.md (MODIFIED, +2/-1)
- packages/dataviews/src/dataform/index.tsx (MODIFIED, +5/-0)
- packages/dataviews/src/dataviews-picker/index.tsx (MODIFIED, +5/-0)
- packages/dataviews/src/dataviews/index.tsx (MODIFIED, +5/-0)
- packages/dataviews/src/dataviews/stories/index.story.tsx (MODIFIED, +5/-0)
- tools/eslint/config.mjs (MODIFIED, +1/-6)
