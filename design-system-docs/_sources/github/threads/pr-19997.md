---
number: 19997
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/19997
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #19997: Update/modal component

- URL: https://github.com/WordPress/gutenberg/pull/19997
- Author: fernandovbs
- Created: 2020-02-02T21:27:53Z
- Updated: 2021-05-15T23:11:50Z
- Comments: 6 of 6
- Review threads: 1 of 1
- Changed files: 4 of 4
- Diff: ../pr-diffs/19997.diff

## Body

## Description
This pull request is related to #7698. 
Updated focusOnMount prop so it can be defined as string or boolean. If "firstElement"  is set as value, the first tabbable element will be focused. If any other truthy value is set, then the container will be focused.

## How has this been tested?
I will create another pull request with changes to the component stories. It covers possible values to the prop. 

## Checklist:
- [:x:  ] My code is tested.
- [ :x: ] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [ :x: ] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [ :x: ] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->

## Issue comments

### ItsJonQ on 2020-02-10T22:34:25Z

URL: https://github.com/WordPress/gutenberg/pull/19997#issuecomment-584391309

@fernandovbs Haiii! I left a comment re: the updated `focusOnMount` logic. 

I saw your other PR as well re: Storybook updates for Modal. I don't think it needs to be a separate PR.
Are you open to adding those changes from https://github.com/WordPress/gutenberg/pull/19999 to this PR?

That way, it can all ship as a single merge :)

Final note! We'll need to update the `Modal` `README.md` as well to reflect the new API change.

Thanks again!!

### aduth on 2020-04-20T19:30:47Z

URL: https://github.com/WordPress/gutenberg/pull/19997#issuecomment-616762581

> This pull request is related to #7698.

Can you clarify if you'd intended for this to close (satisfy the requirements of) #7698?

### fernandovbs on 2020-04-21T23:27:30Z

URL: https://github.com/WordPress/gutenberg/pull/19997#issuecomment-617463507

Hello @aduth. Yes, the idea here is to solve the issue.
I've updated the component to accept a string or boolean for the focusOnMount prop. 
The default value is 'firstElement', but you can also set the value to 'container', true or false, where 'container' and true have the same behavior.
If you have any suggestions about how to make it better, please let me know.

### paaljoachim on 2021-02-20T20:39:51Z

URL: https://github.com/WordPress/gutenberg/pull/19997#issuecomment-782746185

Here is another modal component by @youknowriad 
https://github.com/WordPress/gutenberg/pull/28574

### draganescu on 2021-02-26T09:51:23Z

URL: https://github.com/WordPress/gutenberg/pull/19997#issuecomment-786537677

Howdy @fernandovbs lovely PR! What is a way to test this? Also we seem to have a conflict. Other than that the change is super welcome.

### fernandovbs on 2021-05-15T23:11:17Z

URL: https://github.com/WordPress/gutenberg/pull/19997#issuecomment-841737744

Hi @draganescu I think that the changes introduced in https://github.com/WordPress/gutenberg/pull/27699 have made this PR unnecessary. I'm closing it. Thanks for taking a look.

## Review threads

### Review thread 1 (resolved)

#### ItsJonQ on 2020-02-10T22:32:35Z

URL: https://github.com/WordPress/gutenberg/pull/19997#discussion_r377357050
Path: packages/components/src/modal/frame.js

```diff
@@ -28,15 +28,27 @@ class ModalFrame extends Component {
 		this.handleKeyDown = this.handleKeyDown.bind( this );
 		this.handleFocusOutside = this.handleFocusOutside.bind( this );
 		this.focusFirstTabbable = this.focusFirstTabbable.bind( this );
+		this.focusOnMount = this.focusOnMount.bind( this );
 	}
 
 	/**
-	 * Focuses the first tabbable element when props.focusOnMount is true.
+	 * Focuses the container or first tabbable element based on props.focusOnMount.
 	 */
 	componentDidMount() {
 		// Focus on mount
 		if ( this.props.focusOnMount ) {
+			this.focusOnMount();
+		}
+	}
+
+	/**
+	 * Handle focus on mount.
+	 */
+	focusOnMount() {
+		if ( this.props.focusOnMount === 'firstElement' ) {
 			this.focusFirstTabbable();
+		} else {
+			this.containerRef.current.focus();
```

@fernandovbs Hai 👋 !

Thank you for working on this :). I took an initial look. Pulled it down and tested locally.
This `else` would qualify `focusOnMount="container"`. Reading it though, it took me a bit to understand it.

What are your thoughts on clarifying the condition a bit. Maybe something like:

```
if (this.props.focusOnMount === 'container' || this.props.focusOnMount === true)
```

It's slightly more verbose, but I feel like it's clearer.
Alternatively, we could use a `switch` statement for this logic flow

Would love your thoughts!

#### fernandovbs on 2020-02-13T16:49:19Z

URL: https://github.com/WordPress/gutenberg/pull/19997#discussion_r378985825
Path: packages/components/src/modal/frame.js

```diff
@@ -28,15 +28,27 @@ class ModalFrame extends Component {
 		this.handleKeyDown = this.handleKeyDown.bind( this );
 		this.handleFocusOutside = this.handleFocusOutside.bind( this );
 		this.focusFirstTabbable = this.focusFirstTabbable.bind( this );
+		this.focusOnMount = this.focusOnMount.bind( this );
 	}
 
 	/**
-	 * Focuses the first tabbable element when props.focusOnMount is true.
+	 * Focuses the container or first tabbable element based on props.focusOnMount.
 	 */
 	componentDidMount() {
 		// Focus on mount
 		if ( this.props.focusOnMount ) {
+			this.focusOnMount();
+		}
+	}
+
+	/**
+	 * Handle focus on mount.
+	 */
+	focusOnMount() {
+		if ( this.props.focusOnMount === 'firstElement' ) {
 			this.focusFirstTabbable();
+		} else {
+			this.containerRef.current.focus();
```

Hi @ItsJonQ !
I think the first option is great :). 
I'll update my PR and also include the stories update into this one.
Thank you for taking the time to review it.

## Changed files

- packages/components/src/modal/README.md (MODIFIED, +6/-4)
- packages/components/src/modal/frame.js (MODIFIED, +46/-0)
- packages/components/src/modal/index.js (MODIFIED, +1/-1)
- packages/components/src/modal/stories/index.js (MODIFIED, +7/-2)
