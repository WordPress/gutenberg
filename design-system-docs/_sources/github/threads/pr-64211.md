---
number: 64211
type: pull_request
state: MERGED
merged_at: 2024-08-05T17:14:00Z
url: https://github.com/WordPress/gutenberg/pull/64211
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #64211: SelectControl: Pass through `options` props

- URL: https://github.com/WordPress/gutenberg/pull/64211
- Author: mirka
- Created: 2024-08-02T17:31:44Z
- Updated: 2024-08-05T18:52:52Z
- Comments: 1 of 1
- Review threads: 7 of 7
- Changed files: 5 of 5
- Diff: ../pr-diffs/64211.diff

## Body

Prerequisite for #63815

## What?

In `SelectControl`, pass through any extra props defined in `options` to the underlying `<option>` element.

## Why?

To support any additional `option` attributes, for example `aria-label`.

## Testing Instructions

In Storybook, pass some additional `option` attributes and see what gets rendered in HTML.

✅ Unit test passes

## Issue comments

### github-actions on 2024-08-02T17:40:25Z

URL: https://github.com/WordPress/gutenberg/pull/64211#issuecomment-2265858209

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### mirka on 2024-08-02T17:33:39Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1702135549
Path: packages/components/src/select-control/index.tsx

```diff
@@ -26,6 +26,22 @@ function useUniqueId( idProp?: string ) {
 	return idProp || id;
 }
 
+function SelectOptions( {
```

Extracted this out from the main component function because we need to pick props from the `options.map` and the names are going to conflict with the main component props.

### Review thread 2

#### mirka on 2024-08-02T17:35:17Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1702137055
Path: packages/components/src/select-control/index.tsx

```diff
@@ -26,6 +26,22 @@ function useUniqueId( idProp?: string ) {
 	return idProp || id;
 }
 
+function SelectOptions( {
+	options,
+}: {
+	options: NonNullable< SelectControlProps[ 'options' ] >;
+} ) {
+	return options.map( ( { id, label, value, ...optionProps }, index ) => {
+		const key = id || `${ label }-${ value }-${ index }`;
```

As a `key`, `${ label }-${ value }-${ index }` seems functionally equivalent to `index` 🤔 Doesn't seem particularly intentional, either (#6122).  I'm not touching this in this PR though.

### Review thread 3

#### mirka on 2024-08-02T17:35:36Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1702137337
Path: packages/components/src/select-control/test/select-control.tsx

```diff
@@ -17,8 +17,8 @@ describe( 'SelectControl', () => {
 		expect( screen.queryByRole( 'combobox' ) ).not.toBeInTheDocument();
 	} );
 
-	it( 'should not render its children', async () => {
-		const user = await userEvent.setup();
+	it( 'should render its children', async () => {
+		const user = userEvent.setup();
```

Cleaned up some typos.

### Review thread 4

#### mirka on 2024-08-02T17:36:38Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1702138279
Path: packages/components/src/select-control/types.ts

```diff
@@ -34,20 +37,10 @@ type SelectControlBaseProps = Pick<
 			 * This is also the value passed to `onChange` when the option is selected.
 			 */
 			value: string;
-			id?: string;
-			/**
-			 * Whether or not the option should have the disabled attribute.
-			 *
-			 * @default false
-			 */
-			disabled?: boolean;
-			/**
-			 * Whether or not the option should be hidden.
-			 *
-			 * @default false
-			 */
-			hidden?: boolean;
```

Removing these explicit types since they are included in `React.OptionHTMLAttributes`.

### Review thread 5

#### mirka on 2024-08-02T17:39:36Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1702140769
Path: packages/components/src/select-control/types.ts

```diff
@@ -22,9 +22,12 @@ type SelectControlBaseProps = Pick<
 	| 'suffix'
 > &
 	Pick< BaseControlProps, 'help' | '__nextHasNoMarginBottom' > & {
-		onBlur?: ( event: FocusEvent< HTMLSelectElement > ) => void;
-		onFocus?: ( event: FocusEvent< HTMLSelectElement > ) => void;
```

Removing these explicit event listener props since they are mixed in here:

https://github.com/WordPress/gutenberg/blob/2c01c7dd9dbea28e8afb85078ac5aef670f206cc/packages/components/src/select-control/index.tsx#L45-L47

This will remove them from the props table and auto-generated code snippets in Storybook, which is good because we have no reason to explicitly show `onBlur` and `onFocus`.

### Review thread 6

#### tyxla on 2024-08-05T13:05:49Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1704090876
Path: packages/components/src/select-control/types.ts

```diff
@@ -70,6 +63,11 @@ export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
 	 * @default false
 	 */
 	multiple?: false;
+	/**
+	 * The value of the selected option.
+	 *
+	 * If `multiple` is true, the `value` should be an array with the values of the selected options.
+	 */
```

Does it make sense to make this more specific, so it's clear it's for a single select? TypeScript can often see if the component has `multiple` or not and can determine which description to display in the IDE properly:

![Screenshot 2024-08-05 at 16 14 25](https://github.com/user-attachments/assets/d4c7df73-7a4d-4950-9189-f4412dbf0ef1)
![Screenshot 2024-08-05 at 16 14 43](https://github.com/user-attachments/assets/f6d3a207-3ca4-4ad8-9ff8-954dbab38a05)

#### mirka on 2024-08-05T17:11:46Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1704415710
Path: packages/components/src/select-control/types.ts

```diff
@@ -70,6 +63,11 @@ export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
 	 * @default false
 	 */
 	multiple?: false;
+	/**
+	 * The value of the selected option.
+	 *
+	 * If `multiple` is true, the `value` should be an array with the values of the selected options.
+	 */
```

I'd love to, but I've been avoiding writing different descriptions for "conditional" props, because they don't work well with the Storybook props table. They simply get concatenated when they differ:

<img width="490" alt="Concatenated description in Storybook props table" src="https://github.com/user-attachments/assets/a7ec0724-280b-4d5c-9514-ca13f53a3f81">

#### tyxla on 2024-08-05T17:19:32Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1704423193
Path: packages/components/src/select-control/types.ts

```diff
@@ -70,6 +63,11 @@ export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
 	 * @default false
 	 */
 	multiple?: false;
+	/**
+	 * The value of the selected option.
+	 *
+	 * If `multiple` is true, the `value` should be an array with the values of the selected options.
+	 */
```

That's not a great reason for the type description verbosity TBH 😞 I wish Storybook could handle that in a more clever way. Are we the first to have that problem? Seems like this is similar to what we need: https://github.com/storybookjs/storybook/issues/21281

#### mirka on 2024-08-05T18:52:52Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1704513069
Path: packages/components/src/select-control/types.ts

```diff
@@ -70,6 +63,11 @@ export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
 	 * @default false
 	 */
 	multiple?: false;
+	/**
+	 * The value of the selected option.
+	 *
+	 * If `multiple` is true, the `value` should be an array with the values of the selected options.
+	 */
```

I don't necessarily dislike this limitation though. I think it is useful to know that a prop is conditionally polymorphic.

### Review thread 7

#### tyxla on 2024-08-05T13:12:40Z

URL: https://github.com/WordPress/gutenberg/pull/64211#discussion_r1704099971
Path: packages/components/src/select-control/types.ts

```diff
@@ -92,6 +90,11 @@ export type SelectControlMultipleSelectionProps = SelectControlBaseProps & {
 	 * @default false
 	 */
 	multiple: true;
+	/**
+	 * The value of the selected option.
+	 *
+	 * If `multiple` is true, the `value` should be an array with the values of the selected options.
+	 */
```

As above: Does it make sense to make this more multiple-specific, so it's clear that it's always an array of value**s** and not a single value since it's only when the select field is a multiple select one?

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/select-control/README.md (MODIFIED, +8/-1)
- packages/components/src/select-control/index.tsx (MODIFIED, +17/-17)
- packages/components/src/select-control/test/select-control.tsx (MODIFIED, +23/-4)
- packages/components/src/select-control/types.ts (MODIFIED, +21/-18)
