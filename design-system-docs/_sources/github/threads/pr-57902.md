---
number: 57902
type: pull_request
state: MERGED
merged_at: 2024-02-10T01:03:37Z
url: https://github.com/WordPress/gutenberg/pull/57902
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #57902: CustomSelect: Adapt component for legacy props

- URL: https://github.com/WordPress/gutenberg/pull/57902
- Author: brookewp
- Created: 2024-01-17T02:20:29Z
- Updated: 2024-02-10T01:04:00Z
- Comments: 4 of 4
- Review threads: 31 of 31
- Changed files: 13 of 13
- Diff: ../pr-diffs/57902.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Another option to replace CustomSelectControl with the new ariakit verison without breaking changes. This started in #57000, which was using a similiar approach to ColorPicker's legacy adapter. It was getting a bit complicated, so this PR ius based on another approach shared by @ciampo. This has the same goal but aims to be less convoluted. 

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

To transition away from using CustomSelectControl to the new version for more flexibility.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

No changes were made to CustomSelectItem, aside from moving it to a separate file. 
The same for CustomSelect, and the store has been moved outside of the component and instead is forwarded the store. 

In this version, there are two components, one for the legacy and another for the new/soon-to-be-default version. There is a check to see which props have been passed through. If it's the legacy, the props are translated to the new version of the component. 

This also adds the same tests from CustomSelectControl. The only changes made were to the role which is now combobox instead of button, and the tests for the custom event handlers have been removed.

## To Do

- [x] `unmountOnHide` - add to both or just legacy? Related: https://github.com/ariakit/ariakit/issues/3374
- [x] Edit README

### Legacy 

- [x] fix ~arrow~checkmark size 
- [x] legacy compact/default sizing, [related comment](https://github.com/WordPress/gutenberg/pull/57902#discussion_r1460162850)
- [ ] return object: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1454382191

### New

- [x] re-rendering on selection: https://github.com/WordPress/gutenberg/pull/57902/commits/e3c0be1af2e83df75b23ae5af98f8bcb0891657c

## Testing Instructions
1. `run npm storybook:dev`
2. See the two separated stories 
3. Ensure that the Default version works as it did before, in trunk. 
4. Ensure the Legacy version behaves and looks the same as the original [CustomSelectControl](https://wordpress.github.io/gutenberg/?path=/docs/components-customselectcontrol--docs)

## Issue comments

### github-actions on 2024-01-18T20:37:43Z

URL: https://github.com/WordPress/gutenberg/pull/57902#issuecomment-1899169097

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 3a2be417f87ad46269f7d89484b4db48118c377c.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/7850850332
📝  Reported issues:
- #57881 in `/test/e2e/specs/interactivity/directive-bind.spec.ts`

### github-actions on 2024-02-01T20:28:01Z

URL: https://github.com/WordPress/gutenberg/pull/57902#issuecomment-1922181343

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: brookewp <brookemk@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: diegohaz <hazdiego@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### brookewp on 2024-02-02T20:57:30Z

URL: https://github.com/WordPress/gutenberg/pull/57902#issuecomment-1924686765

If [this solution](https://github.com/WordPress/gutenberg/pull/57902#discussion_r1475113337) is something we can do to handle `defaultValue` then I believe these are the remaining items: 

- [x] Decide if we want to use `BaseControl` instead of `Ariakit.SelectLabel`: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1478435778
- [x] Children vs render for `Ariakit.SelectArrow` - related: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1476719479
- [x] Refine README

And then, of course, smoke-testing it in the editor once the above is resolved. 

cc @mirka @ciampo

### brookewp on 2024-02-06T22:27:33Z

URL: https://github.com/WordPress/gutenberg/pull/57902#issuecomment-1930875953

I've combined the tests from https://github.com/WordPress/gutenberg/pull/58583 in this commit: https://github.com/WordPress/gutenberg/pull/57902/commits/c73ce18cc3250f341bf19072ba83ddd01e4117ce

It's super verbose, with some tests being in both the suites for the legacy and default sections. But with the uncontrolled/controlled sections, I wasn't sure how to do it without overcomplicating it / making it very confusing. 

I've let @mirka know about this, as she'll be taking this over this PR at this stage. 🙇‍♀️

## Review threads

### Review thread 1

#### brookewp on 2024-01-17T02:30:57Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1454382191
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
+				},
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				isOpen: state.open,
+			};
+			props.onChange( changeObject );
```

Just to note, I'm still looking into the onChange return object. I've implemented the suggestion by @diegohaz from an earlier PR: https://github.com/WordPress/gutenberg/pull/55234#discussion_r1361394388, minus the `type` and `inputValue`. I'm looking to see if it makes sense to include these / how we could implement them. 

The value of `type` is the interaction method used to make a selection, i.e. `__item_click__`, `__menu_keydown_enter__`

The value of `inputValue` is the first character entered via keydown to make a selection (with `type` returning `__togglebutton_keydown_character__`). If keydown isn't used to make a selection, then it returns an empty string.

#### ciampo on 2024-01-18T09:47:16Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457186884
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
+				},
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				isOpen: state.open,
+			};
+			props.onChange( changeObject );
```

> The value of `type` is the interaction method used to make a selection, i.e. `__item_click__`, `__menu_keydown_enter__`
> [...]
> The value of `inputValue` is the first character entered via keydown to make a selection (with `type` returning `__togglebutton_keydown_character__`). If keydown isn't used to make a selection, then it returns an empty string.

Mhh, I'm not sure we can reliably access those pieces of information. Maybe Diego can advise.

In the meantime, we can give them a fixed `''` value to at least make sure that if anyone is accessing those properties, they are defined.

#### diegohaz on 2024-01-26T05:25:04Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1467231903
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
+				},
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				isOpen: state.open,
+			};
+			props.onChange( changeObject );
```

I'd keep the `type` as `__item_click__` by default for simplicity's sake. If anyone depends on these values, it's likely the type they're handling as standard.

You could get this value by listening to DOM events and storing the information in a ref for subsequent use in Ariakit's `setValue` prop. But honestly, I wouldn't sweat it. It's not even mentioned in the Downshift docs. It doesn't seem to be part of their public API.

Edit: I was wrong. It is part of their public API (see [`stateChangeTypes`](https://github.com/downshift-js/downshift/tree/master/src/hooks/useSelect#statechangetypes)). Still, if these constants weren't exposed by WordPress components, I'd avoid re-implementing this logic.

### Review thread 2 (resolved)

#### ciampo on 2024-01-18T09:28:15Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457162755
Path: packages/components/src/custom-select-control-v2/stories/legacy.story.tsx

```diff
@@ -0,0 +1,109 @@
+//@ts-nocheck
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import LegacyCustomSelect from '../legacy-component';
+import { CustomSelect, CustomSelectItem } from '..';
+
+const meta: Meta< typeof LegacyCustomSelect > = {
+	title: 'Components (Experimental)/CustomSelectControl v2/Legacy',
+	component: LegacyCustomSelect,
+	argTypes: {
```

This story doesn't display interactive controls — it could be related to the fact that the Story templates are not specifying the component inside the `StoryFn` type, and that they are not forwarding `props` down to the `CustomSelect` component.

Rewriting stories that way, also helps with type safety — for example, the controlled story could be rewritten like this

<details>

<summary>Click to expand</summary>

```tsx
export const Controlled: StoryFn< typeof LegacyCustomSelect > = ( props ) => {
	const [ fontSize, setFontSize ] = useState( props.options[ 0 ] );

	const onChange: ( typeof props )[ 'onChange' ] = ( changeObject ) => {
		setFontSize( changeObject.selectedItem );
		props.onChange?.( changeObject );
	};

	return (
		<CustomSelect { ...props } onChange={ onChange } value={ fontSize } />
	);
};
Controlled.args = {
	label: 'Font Size',
	options: [
		{
			key: 'small',
			name: 'Small',
			style: { fontSize: '50%' },
		},
		{
			key: 'normal',
			name: 'Normal',
			style: { fontSize: '100%' },
		},
		{
			key: 'large',
			name: 'Large',
			style: { fontSize: '200%' },
		},
		{
			key: 'huge',
			name: 'Huge',
			style: { fontSize: '300%' },
		},
	],
};
```

</details>

#### brookewp on 2024-01-18T20:19:31Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457938301
Path: packages/components/src/custom-select-control-v2/stories/legacy.story.tsx

```diff
@@ -0,0 +1,109 @@
+//@ts-nocheck
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import LegacyCustomSelect from '../legacy-component';
+import { CustomSelect, CustomSelectItem } from '..';
+
+const meta: Meta< typeof LegacyCustomSelect > = {
+	title: 'Components (Experimental)/CustomSelectControl v2/Legacy',
+	component: LegacyCustomSelect,
+	argTypes: {
```

Makes sense! And helpful if more story examples need to be added later. 

I've updated the Default story with this in mind, and so we get controls there too https://github.com/WordPress/gutenberg/pull/57902/commits/007aba02b20b341290a45a219aca2ff8a02e6d58

### Review thread 3 (resolved)

#### ciampo on 2024-01-18T09:33:11Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457169272
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,23 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return (
+		typeof props.options !== 'undefined' ||
+		props.__experimentalShowSelectedHint !== undefined
```

Any reasons why we're checking for those props to be undefined in two different ways?

#### brookewp on 2024-01-18T20:30:32Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457947594
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,23 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return (
+		typeof props.options !== 'undefined' ||
+		props.__experimentalShowSelectedHint !== undefined
```

No, nice catch!

### Review thread 4 (resolved)

#### ciampo on 2024-01-18T09:35:34Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457172130
Path: packages/components/src/custom-select-control-v2/stories/legacy.story.tsx

```diff
@@ -0,0 +1,109 @@
+//@ts-nocheck
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import LegacyCustomSelect from '../legacy-component';
+import { CustomSelect, CustomSelectItem } from '..';
```

Is `CustomSelectItem` needed in this file?

#### brookewp on 2024-01-18T20:11:04Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457931360
Path: packages/components/src/custom-select-control-v2/stories/legacy.story.tsx

```diff
@@ -0,0 +1,109 @@
+//@ts-nocheck
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import LegacyCustomSelect from '../legacy-component';
+import { CustomSelect, CustomSelectItem } from '..';
```

Nope! It's a result from duplicating the story + the [ts-nocheck](https://github.com/WordPress/gutenberg/pull/57902/files#r1457929860). Will remove that!

### Review thread 5 (resolved)

#### ciampo on 2024-01-18T09:43:23Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457182018
Path: packages/components/src/custom-select-control-v2/stories/legacy.story.tsx

```diff
@@ -0,0 +1,109 @@
+//@ts-nocheck
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import LegacyCustomSelect from '../legacy-component';
+import { CustomSelect, CustomSelectItem } from '..';
+
+const meta: Meta< typeof LegacyCustomSelect > = {
+	title: 'Components (Experimental)/CustomSelectControl v2/Legacy',
+	component: LegacyCustomSelect,
+	argTypes: {
+		value: { control: { type: null } },
+	},
+	parameters: {
+		actions: { argTypesRegex: '^on.*' },
+		controls: { expanded: true },
+		docs: {
+			canvas: { sourceState: 'shown' },
+			source: { excludeDecorators: true },
+		},
+	},
+	decorators: [
+		( Story ) => (
+			<div
+				style={ {
+					minHeight: '150px',
+				} }
+			>
+				<Story />
+			</div>
+		),
+	],
+};
+export default meta;
+
+export const Default: StoryFn = () => {
```

Probably not in the scope of this PR, but I noticed that currently the component's default `height` is `49px`. I know that #57865 will likely fix that, but IMO it will just "ignore" a symptom instead of fixing the root cause:

![Screenshot 2024-01-18 at 10 36 26](https://github.com/WordPress/gutenberg/assets/1083581/64e7908b-fd8d-46cb-9e01-f92b1ff848a5)

If I force the button to have `height: 40px`, you can see that the inner content gets "squashed":

![Screenshot 2024-01-18 at 10 39 01](https://github.com/WordPress/gutenberg/assets/1083581/4fd90f7b-100d-4a5a-af31-6d88a732f5e9)

In short, we may have to reassess top/bottom padding, although that can also happen in a separate PR where we look at filling all the gaps so that the component looks exactly like the legacy `CustomSelectControl`.

#### brookewp on 2024-01-29T20:37:21Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1470179147
Path: packages/components/src/custom-select-control-v2/stories/legacy.story.tsx

```diff
@@ -0,0 +1,109 @@
+//@ts-nocheck
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import LegacyCustomSelect from '../legacy-component';
+import { CustomSelect, CustomSelectItem } from '..';
+
+const meta: Meta< typeof LegacyCustomSelect > = {
+	title: 'Components (Experimental)/CustomSelectControl v2/Legacy',
+	component: LegacyCustomSelect,
+	argTypes: {
+		value: { control: { type: null } },
+	},
+	parameters: {
+		actions: { argTypesRegex: '^on.*' },
+		controls: { expanded: true },
+		docs: {
+			canvas: { sourceState: 'shown' },
+			source: { excludeDecorators: true },
+		},
+	},
+	decorators: [
+		( Story ) => (
+			<div
+				style={ {
+					minHeight: '150px',
+				} }
+			>
+				<Story />
+			</div>
+		),
+	],
+};
+export default meta;
+
+export const Default: StoryFn = () => {
```

This has since been resolved, with the default height being 40px in total. 😄

### Review thread 6 (resolved)

#### ciampo on 2024-01-18T09:48:45Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457188868
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
+				},
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				isOpen: state.open,
+			};
+			props.onChange( changeObject );
+		},
+	} );
+
+	const transformOptionsToChildren = () => {
```

Any reason why this is a function and not just a variable? Ie.

`const children = props.options.map( ....`

#### brookewp on 2024-01-18T20:28:19Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457945855
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
+				},
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				isOpen: state.open,
+			};
+			props.onChange( changeObject );
+		},
+	} );
+
+	const transformOptionsToChildren = () => {
```

Initially, it had some arguments but was modified a bit since, so it makes sense for it to be a constant now. 👍 I think I also gravitate towards functions when returning JSX (not necessarily for any reason other than habit). I am curious if there would be any differences between them, in this case, besides semantics?

### Review thread 7 (resolved)

#### ciampo on 2024-01-18T10:12:55Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457219562
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -48,6 +57,69 @@ export type CustomSelectProps = {
 	 */
 	value?: string | string[];
 };
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Pass in a description that will be shown to screen readers associated with the
+	 * select trigger button. If no value is passed, the text "Currently selected:
+	 * selectedItem.name" will be used fully translated.
+	 */
+	describedBy?: string;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+	/**
+	 * A function that receives the new value of the input.
+	 */
+	onChange?: ( newValue: OnChangeObject ) => void;
+	/**
+	 * The options that can be chosen from.
+	 */
+	options: Array< Option >;
+	/**
+	 * The size of the control.
+	 *
+	 * @default 'default'
+	 */
+	size?: CustomSelectProps[ 'size' ];
```

It looks like the legacy component [supports the same values for `size` as `SelectControl`](https://github.com/WordPress/gutenberg/blob/d2450c88f42e22827718ca233a94cb9c5e1b873f/packages/components/src/custom-select-control/index.js#L78-L79), which are ultimately the same as `InputControl`.

Question is: are all those different values effectively having an impact on the legacy `CustomSelectControl`? If so, we should probably accept all those values and retain the correct behavior.

Otherwise, we could be able to trim down the list of accepted `size` values.

### Review thread 8 (resolved)

#### ciampo on 2024-01-18T10:14:28Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457221361
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -48,6 +57,69 @@ export type CustomSelectProps = {
 	 */
 	value?: string | string[];
 };
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
```

Looking at [the legacy implementation](https://github.com/WordPress/gutenberg/blob/d2450c88f42e22827718ca233a94cb9c5e1b873f/packages/components/src/custom-select-control/index.js), we're missing a few props:

- `__next40pxDefaultSize`
- `className`
- `onMouseOver`
- `onMouseOut`
- `onFocus`
- `onBlur`

#### brookewp on 2024-01-18T20:42:13Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457957139
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -48,6 +57,69 @@ export type CustomSelectProps = {
 	 */
 	value?: string | string[];
 };
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
```

In the first iteration (https://github.com/WordPress/gutenberg/pull/57000), 
the legacy props were being translated to the new version which used `WordPressComponentProps`, so we didn't get type errors for these (aside from `__next40pxDefaultSize`). 

Is that something that we should look into adding or is it preferred to explicitly add just these props?

#### ciampo on 2024-01-19T16:27:12Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1459293430
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -48,6 +57,69 @@ export type CustomSelectProps = {
 	 */
 	value?: string | string[];
 };
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
```

I would probably avoid using `WordPressComponentProps` for the legacy, so that we keep it as close as possible to the original, at least for the moment

### Review thread 9 (resolved)

#### ciampo on 2024-01-18T10:20:44Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457230888
Path: packages/components/src/custom-select-control-v2/stories/default.story.tsx

```diff
@@ -11,11 +12,12 @@ import { useState } from '@wordpress/element';
 /**
  * Internal dependencies
  */
+import NewCustomSelect from '../default-component';
 import { CustomSelect, CustomSelectItem } from '..';
 
-const meta: Meta< typeof CustomSelect > = {
-	title: 'Components (Experimental)/CustomSelectControl v2',
-	component: CustomSelect,
+const meta: Meta< typeof NewCustomSelect > = {
```

The change from `typeof CustomSelect` to `typeof NewCustomSelect` should be also applied later in the file where we use `StoryFn< ... >`.

That should help remove the `//@ts-nocheck` annotation.

#### brookewp on 2024-01-18T20:09:21Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457929860
Path: packages/components/src/custom-select-control-v2/stories/default.story.tsx

```diff
@@ -11,11 +12,12 @@ import { useState } from '@wordpress/element';
 /**
  * Internal dependencies
  */
+import NewCustomSelect from '../default-component';
 import { CustomSelect, CustomSelectItem } from '..';
 
-const meta: Meta< typeof CustomSelect > = {
-	title: 'Components (Experimental)/CustomSelectControl v2',
-	component: CustomSelect,
+const meta: Meta< typeof NewCustomSelect > = {
```

Good catch! The no-check was added to run the storybook build, and I forgot to remove it. Otherwise it would be pointing these things out to me. 😅 Thank you!

### Review thread 10 (resolved)

#### ciampo on 2024-01-18T10:42:47Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457257685
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
```

I believe that the `key` here is supposed to match the options's `key` (see this screenshot showing a log of the `onChange` function on the legacy component)

![Screenshot 2024-01-18 at 11 41 45](https://github.com/WordPress/gutenberg/assets/1083581/285fe8f7-96e4-4960-9277-94aeb4974d4e)


We should probably add unit tests to check the argument of the `onChange` function too.

#### brookewp on 2024-01-19T00:32:42Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1458138775
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,76 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! props.onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				selectedItem: {
+					// value will always be a string for the legacy component
+					name: value as string,
+					key: state.activeId as string,
```

I added a couple tests for this: https://github.com/WordPress/gutenberg/pull/57902/commits/82a475e83f7fa5418a4dad44376ebae416f3fa6c

Hopefully, this was what you had in mind; otherwise, let me know how I can modify them. 

I could see how the second test could be considered redundant, but I added it as it's the most common way I've seen the `onChange` implemented for `CustomSelectControl` in the codebase.

### Review thread 11 (resolved)

#### brookewp on 2024-01-20T03:12:07Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1460162850
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

The 'default' size for the legacy is 32px, but the new one is 40px. 

Ideally, we keep the 40px default size for the new one and find a way for the legacy to have 'default' and 'compact' as the same size. Similar to what is there for `__next40pxDefaultSize` and `__unstable-large` right now. But I'm encountering type errors when trying a similar method for that. 

But hopefully, we can find a cleaner method for handling the sizes. 🙂 But I think it'll be a next week thing at this point.

#### mirka on 2024-01-23T17:55:32Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1463720163
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

Two approaches off the top of my head:

1. On `_CustomSelect`, add a boolean prop named something like `isLegacySizeSmall`. In JSDoc, mark it as `@deprecated` and `@ignore`, and make it clear that it is just for one-off internal use.
2. Connect `CustomSelectButton` to the Component Context system. This will allow `_LegacyCustomSelect` to set a `size="small"` prop on CustomSelectButton "under the table" via context, without `_CustomSelect` having to publicly expose that as an API. (I recently had to do something similar for [SearchControl](https://github.com/WordPress/gutenberg/pull/56524/files#diff-bfea4a989e6dfea0b870119ceee556bcaa0ec6df3c5ec56550114ac37f51595bR73-R79) to reach a [BaseControl](https://github.com/WordPress/gutenberg/pull/57408) prop that wasn't exposed on InputControl.)

#### brookewp on 2024-01-24T02:38:59Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1464227505
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

Thank you for these suggestions! I will give them a try tomorrow. 😄

#### brookewp on 2024-01-25T05:19:17Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1465853446
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

The second option works great for adding the 'small' size, but then there is still the problem of having a `default` that needs to be a different value in the two different scenarios. 

So I am thinking that we might need to use the first option, but have something more generic like `isLegacy`. Since we also need to alter styles if an experimental hint is added. Currently, I have inline styles for that but I realize that it likely isn't a good idea in case consumers have overwritten the styles with CSS. If we could have inline styles, this could be wrapped up in a few lines. 

For option one, how would we communicate a boolean value of `isLegacy` or `isLegacySizeSmall`? Since the legacy props are adapted before reaching `_CustomSelect` so any check for the legacy would be false unless we pass it through the `_LegacyCustomSelect`. But I've run into other issues when trying that. 😅 This seems to be getting a bit messy.

#### mirka on 2024-01-25T11:07:23Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1466208107
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

> there is still the problem of having a `default` that needs to be a different value in the two different scenarios

I might be missing something obvious but I don't immediately understand this problem 🤔 I thought some translation logic like [this](https://github.com/WordPress/gutenberg/pull/57902/files#diff-098732f0ef5f857dc8edaffd70c0cbd3b1388defa30a6689ba197b2cd70e3a9aR123) would've been sufficient. If you could push the changes you have so far, I'd love to take a look! We definitely don't want to overcomplicate this part, so thanks for alerting early.

#### brookewp on 2024-01-25T21:45:24Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1466994453
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

I've pushed the changes with my interpretation of your suggestion, here: https://github.com/WordPress/gutenberg/pull/57902/commits/9048ebfeda88175fe4dfbc1d3413444a97b2d130

>I thought some translation logic like this would've been sufficient.

When using this, the legacy 'default' size is 40px rather than '32px'. It should only be '40px' when `__next40pxDefaultSize` is true. 

The only way I see around that is by adding a prop to check if it's legacy, like:
`isLegacySize ? 32 : 40,`

Which would be helpful for the experimental hint styling differences. But this is where it seems to get a bit messy, with the prop forwarding from _LegacyCustomSelect to _CustomSelect to the CustomSelectButton.

#### brookewp on 2024-01-29T20:39:07Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1470181066
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
```

I discussed this with @mirka on a call and followed up with these changes here: https://github.com/WordPress/gutenberg/pull/57902/commits/116e38a7b3e31ce84354bf75a14c237a040dc319

### Review thread 12 (resolved)

#### mirka on 2024-01-23T17:34:11Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1463695218
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,104 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
+		value: valueProp,
+		...restProps
+	} = props;
+
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				inputValue: '',
+				isOpen: state.open,
+				selectedItem: {
+					// Value will always be a string for the legacy component
+					name: value as string,
+					key: value as string,
+				},
+				type: '',
+			};
+			onChange( changeObject );
+		},
+	} );
+
+	const children = options.map(
+		( { name, key, __experimentalHint, ...rest } ) => {
+			const withHint = (
+				<>
+					<span>{ name }</span>
+					<span className="components-custom-select-control__item-hint">
+						{ __experimentalHint }
+					</span>
+				</>
+			);
+
+			return (
+				<CustomSelectItem
+					{ ...rest }
+					key={ key }
+					value={ name }
+					children={
+						__experimentalShowSelectedHint ? withHint : name
+					}
+				/>
+			);
+		}
+	);
+
+	if ( __nextUnconstrainedWidth ) {
+		deprecated(
+			'Constrained width styles for wp.components.CustomSelectControl',
+			{
+				hint: 'This behaviour is now built-in.',
+				since: '6.4',
+			}
+		);
+	}
+
+	const translatedProps = {
+		'aria-describedby': props.describedBy,
+		children,
+		size:
+			__next40pxDefaultSize || size === '__unstable-large'
```

```suggestion
			( __next40pxDefaultSize && size === 'default' ) || size === '__unstable-large'
```

### Review thread 13 (resolved)

#### mirka on 2024-01-23T17:42:26Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1463705612
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,14 +51,100 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
```

```suggestion
	size?: 'compact' | 'default';
```

Like @ciampo suggested, we can probably get away with just these two sizes for the V2.

### Review thread 14

#### brookewp on 2024-01-24T02:36:52Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1464226390
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

When adding `unmountOnHide`, the initial selected value was an empty string because it no longer had access to the items through the store (see details here: https://github.com/ariakit/ariakit/issues/3374#issuecomment-1905072489)

I'm thinking it might be cleanest to require a `defaultValue` (for the new version, the legacy would use the hardcoded default to behave the same), as it's just a string for the consumer to implement. 

Otherwise, we'd have to map over the children to find a default value. The problem here is that the children could be a fragment or a div or anything that the consumer implements. So it seems like it could get a bit messy to extract a value. 

It's also likely I'm overcomplicating it and there is an easy way to get the value. I'm happy to hear if there is a better solution. 🙂

#### diegohaz on 2024-01-24T03:12:33Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1464244402
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

Is `defaultValue` still required if the `value` prop is provided?

#### ciampo on 2024-01-25T16:52:02Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1466665455
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

As Diego suggests, `defaultValue` shouldn't be required when `value` is provided — it would actually cause more confusion, since `defaultValue` is used to provide an initial value to the component when used as uncontrolled, while `value` is used to set the component's value in a controlled mode.

> Otherwise, we'd have to map over the children to find a default value. The problem here is that the children could be a fragment or a div or anything that the consumer implements

If we expect children to be instances of `CustomSelectControlItem`, then we could read the `value` prop, which is required on those components, and assume as a default value the first defined `value` that we find when mapping over the children.

Would that work?

#### brookewp on 2024-01-25T23:49:32Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1467087785
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

Is there a way we can access the `value` prop without mapping through the children? 

That is the only way I am aware of. Other than doing something that might be considered strange and passing it from the child to parent through context. 

If we map though the children to find the `value`, then we're back to the same problem I shared earlier where we might not know how many layers deep the value is. For example, if they are wrapped in a `div`: 

<img width="616" alt="Screenshot 2024-01-25 at 3 45 12 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/3f8041a5-a8a7-420c-8566-8c7cdc61c23b">

Unless we have a recursive function but that's where I thought it was getting messy or that I was overcomplicating it.

#### diegohaz on 2024-01-26T00:09:48Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1467097511
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

Yeah, it's tricky. If the `CustomSelectControlItem` component gets abstracted into another custom component, you won't have access to it, even with recursion. It seems odd to impose such artificial restrictions on JSX, where users expect certain features to work seamlessly. It's like trying to use JSX as a DSL for an array of objects, when simply using the array of objects might be the answer.

I'd suggest that either `defaultValue` or `value` should be required. There are ways to express this in TypeScript, but a runtime warning might be necessary because the TypeScript error may not be very clear.

#### brookewp on 2024-01-30T00:02:52Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1470390768
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

Thanks for the suggestion! 

There is another conflict due to `unmountOnHide` — since we don't have access to the values, entering characters via keydown no longer works unless the popover is open/mounted. This behavior makes sense with how the ariakit component works, but it conflicts with the legacy behavior. And I think it's a nice feature to have in the new version. 

In our legacy version, we have a fallback added for this scenario: 

https://github.com/WordPress/gutenberg/blob/fa802d10e65c7d0951cc0f2357c367b0ab19861c/packages/components/src/custom-select-control/index.js#L27-L64

However, with the limitation of using compound components, this would be complicated to implement.

In addition to this, there are other related test failures (that I still need to look into, but have confirmed it's a result of the `unmountOnHide` prop). 

So it's getting a bit messy, but perhaps I'm overcomplicating this again. Curious to hear anyone's thoughts and/or ideas on this!

#### ciampo on 2024-01-31T10:33:51Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472624944
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

Could we use `unmountOnHide` only on the new version, and set it to `false` when in legacy?

#### ciampo on 2024-01-31T16:11:21Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1473087447
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

Update:

@mirka and I took a deeper look and observed a few things:

- the native `select` element does support typeahead, but it opens the dropdown popover when pressing the arrow keys
- it looks like `ariakit` doesn't support typeahead natively when `unmountOnHide` is `true` (cc @diegohaz , is this something that ariakit should fix ?)
- however, Lena suggested a little hack which seems to work: set `unmountOnHide` to `false` when focusing the button that is triggering the dropdown (see [example](https://stackblitz.com/edit/utdlzl?file=select%2Fstyle.css,select%2Findex.tsx&theme=dark))

With respect to the initial value, it should be determined by the consumer by passing either the `value` prop (controlled mode) or the `defaultValue` prop (uncontrolled mode)

#### brookewp on 2024-01-31T19:07:11Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1473333329
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

>however, Lena suggested a little hack which seems to work: set unmountOnHide to false when focusing the button that is triggering the dropdown 

Great idea!

#### brookewp on 2024-02-01T02:22:38Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1473718689
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

>With respect to the initial value, it should be determined by the consumer by passing either the value prop (controlled mode) or the defaultValue prop (uncontrolled mode)

Would that involve typing so only one is required when the other isn't present? Something like:

```
type CustomSelectModeProps =
	| {
			/**
			 * A default value to show as the currently selected item. Not required when adding a
			 * value to use component in controlled mode.
			 */
			defaultValue: string | string[];
			value?: string | string[];
	  }
	| {
			defaultValue?: string | string[];
			/**
			 * Can be used to externally control the value of the control.
			 */
			value: string | string[];
	  };
```

There are still some issues with the `defaultValue` for the legacy component. When a default value is provided, tests are failing, but if we don't pass one, we have the empty initial state due to the `unmountOnHide` prop. So I'm still looking into that

#### brookewp on 2024-02-01T04:37:47Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1473784345
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

>however, Lena suggested a little hack which seems to work: set unmountOnHide to false when focusing the button that is triggering the dropdown (see [example](https://stackblitz.com/edit/utdlzl?file=select%2Fstyle.css,select%2Findex.tsx&theme=dark))

This suggestion worked well in combination with [these additional props ](https://github.com/WordPress/gutenberg/blob/5babba6053e05e44a1d4cb4b9f9bdd8415ec0862/packages/components/src/custom-select-control-v2/custom-select.tsx#L80-L81). When testing in SB, it works as expected, but the test for this behavior is still failing: https://github.com/WordPress/gutenberg/blob/5babba6053e05e44a1d4cb4b9f9bdd8415ec0862/packages/components/src/custom-select-control-v2/test/index.tsx#L376 🤔

#### brookewp on 2024-02-01T20:34:02Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1475113337
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

This might be a strange idea, but after seeing Lena's work I thought maybe we could add another hack for `unmountOnHide` 😄 

If we mount and then unmount it on the initial render, then we get the first item as the defaultValue without explicitly setting it, like before: 

https://github.com/WordPress/gutenberg/blob/e3c328f779971ae0c031b17cf5fef2e7aef33ae0/packages/components/src/custom-select-control-v2/custom-select.tsx#L112-L116

Could this be a potential solution?

#### ciampo on 2024-02-02T12:05:58Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1475978326
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

> but the test for this behavior is still failing:

From personal experience, `user-event` may cause some incompatibilities with ariakit-based components, especially when we make some async changes like in the suggestion above. Maybe switching to `@ariakit/test` fixes it?

> Could this be a potential solution?

It could work, as an additional hack. I've updated the example and it [seems to work](https://stackblitz.com/edit/utdlzl?file=select%2Fstyle.css,select%2Findex.tsx&theme=dark) as expected. It may have some performance ripercussion, though — so we could go with this implementation at first (which allows us to keep `defaultValue` as optional), and only switch to requiring `defaultValue` as a fallback? What do folks think?

#### brookewp on 2024-02-02T20:32:15Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1476699432
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

>Maybe switching to @ariakit/test fixes it?

It does! I just thought we might want to keep the tests as close to the original `CustomSelectControl` as possible, so I planned to move to `ariakit/test` after. But since the tests are the same aside from the interaction method, maybe it will be okay to do that now.

#### mirka on 2024-02-05T15:53:08Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1478470939
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

> If we mount and then unmount it on the initial render

I feel like the initial render is exactly the most important time to reap the performance benefits of `unmountOnHide`, so I'm not sure it makes sense to mount on initial render but hide it after.

So going back to basics, if we don't already have proof that just keeping `unmountOnHide={ false }` _all the time_ has a perceivable performance hit to the overall Gutenberg app, I'd even be fine with not messing with that prop at all for the first iteration. I have to imagine that there are much fewer CustomSelectControls in any given view than a generic Tooltip, for example.

#### brookewp on 2024-02-05T23:01:50Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1479030024
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -22,7 +27,13 @@ export type CustomSelectProps = {
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
-	defaultValue?: string | string[];
+	defaultValue: string | string[];
```

So, we remove `unmountOnHide` and all of the related workarounds? 🙈 If I understood correctly, then that is done through: https://github.com/WordPress/gutenberg/pull/57902/commits/6baa6426c7e2cc12052a2779807ac0d1767f716b

Just to note: the tests will pass using `@testing-library/user-event` or with `@ariakit/test`. I believe the former is faster, but I think we will likely need to move to `@ariakit/test` when integrating the new tests (https://github.com/WordPress/gutenberg/pull/58583) — based on seeing flaky test results when merging the new and legacy tests in another version of the legacy adapter: https://github.com/WordPress/gutenberg/pull/57000

We could revert to RTL, merge this PR, and then merge the tests/change to `@ariakit/test` if needed in a follow-up. Or we could keep it with `@ariakit/test`, and I can merge the new tests into this. 

Any preferences?

### Review thread 15 (resolved)

#### mirka on 2024-01-25T10:54:03Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1466192138
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,143 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import deprecated from '@wordpress/deprecated';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+import { ExperimentalHint, ExperimentalHintItem } from '../styles';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		__nextUnconstrainedWidth,
+		options,
+		onChange,
+		// TO-DO 'default' should be the 'compact' size
+		size = 'default',
+		value: valueProp,
+		...restProps
+	} = props;
+
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				inputValue: '',
+				isOpen: state.open,
+				selectedItem: {
+					// Value will always be a string for the legacy component
+					name: value as string,
+					key: value as string,
+				},
+				type: '',
+			};
+			onChange( changeObject );
+		},
+		defaultValue: options[ 0 ].name,
+	} );
+
+	const children = options.map(
+		( { name, key, style, __experimentalHint, ...rest } ) => {
+			const withHint = (
+				<>
+					<span>{ name }</span>
+					<ExperimentalHintItem className="components-custom-select-control__item-hint">
+						{ __experimentalHint }
+					</ExperimentalHintItem>
+				</>
+			);
+
+			const hintStyles = {
+				gridTemplateColumns: __experimentalShowSelectedHint
+					? '1fr auto 30px'
+					: undefined,
+			};
+
+			return (
+				<CustomSelectItem
+					key={ key }
+					value={ name }
+					children={
+						__experimentalShowSelectedHint ? withHint : name
+					}
+					style={ { ...hintStyles, ...style } }
+					{ ...rest }
+				/>
+			);
+		}
+	);
+
+	if ( __nextUnconstrainedWidth ) {
+		deprecated(
+			'Constrained width styles for wp.components.CustomSelectControl',
+			{
+				hint: 'This behaviour is now built-in.',
+				since: '6.4',
+			}
+		);
+	}
```

From the discussions that we're having in https://github.com/WordPress/gutenberg/issues/55401#issuecomment-1907998703, we'll probably want to remove this deprecation message, and keep the prop in the typedefs but marked as `@deprecated` & `@ignore`. (I think the JSDoc description you updated is helpful and we should keep it so people can refer to it in IntelliSense — just suppress it from the public docs so it doesn't add clutter.)

### Review thread 16 (resolved)

#### brookewp on 2024-01-26T19:39:55Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1468075438
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -3,34 +3,27 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as Ariakit from '@ariakit/react';
+import type { FocusEventHandler, MouseEventHandler } from 'react';
 
-export type CustomSelectContext =
-	| {
-			/**
-			 * The store object returned by Ariakit's `useSelectStore` hook.
-			 */
-			store: Ariakit.SelectStore;
-	  }
-	| undefined;
-
-export type CustomSelectProps = {
+export type CustomSelectStore = {
 	/**
-	 * The child elements. This should be composed of CustomSelectItem components.
+	 * The store object returned by Ariakit's `useSelectStore` hook.
 	 */
-	children: React.ReactNode;
+	store: Ariakit.SelectStore;
+};
+
+export type CustomSelectContext = CustomSelectStore | undefined;
+
+export type CustomSelectButtonProps = {
 	/**
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
 	defaultValue?: string | string[];
-	/**
-	 * Label for the control.
-	 */
-	label: string;
 	/**
 	 * A function that receives the new value of the input.
 	 */
-	onChange?: ( newValue: string | string[] ) => void;
+	onSelectionChange?: ( newValue: string | string[] ) => void;
```

There was a conflict with `onChange` from 'button' from using `WordPressComponentProps`. I've renamed it, but not sure if this is a good solution or if there is a better approach.

#### brookewp on 2024-01-29T21:24:52Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1470235846
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -3,34 +3,27 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as Ariakit from '@ariakit/react';
+import type { FocusEventHandler, MouseEventHandler } from 'react';
 
-export type CustomSelectContext =
-	| {
-			/**
-			 * The store object returned by Ariakit's `useSelectStore` hook.
-			 */
-			store: Ariakit.SelectStore;
-	  }
-	| undefined;
-
-export type CustomSelectProps = {
+export type CustomSelectStore = {
 	/**
-	 * The child elements. This should be composed of CustomSelectItem components.
+	 * The store object returned by Ariakit's `useSelectStore` hook.
 	 */
-	children: React.ReactNode;
+	store: Ariakit.SelectStore;
+};
+
+export type CustomSelectContext = CustomSelectStore | undefined;
+
+export type CustomSelectButtonProps = {
 	/**
 	 * An optional default value for the control. If left `undefined`, the first
 	 * non-disabled item will be used.
 	 */
 	defaultValue?: string | string[];
-	/**
-	 * Label for the control.
-	 */
-	label: string;
 	/**
 	 * A function that receives the new value of the input.
 	 */
-	onChange?: ( newValue: string | string[] ) => void;
+	onSelectionChange?: ( newValue: string | string[] ) => void;
```

And now I'm thinking it would be better to omit it and keep the naming which I've done here: https://github.com/WordPress/gutenberg/pull/57902/commits/940cdb1eed59d625ec97d2c02d89fe5d8645cd86

### Review thread 17 (resolved)

#### mirka on 2024-01-30T17:49:09Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471696214
Path: packages/components/src/custom-select-control-v2/custom-select-item.tsx

```diff
@@ -0,0 +1,35 @@
+/**
+ * WordPress dependencies
+ */
+import { useContext } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { CustomSelectItemProps } from './types';
+import type { WordPressComponentProps } from '../context';
+import * as Styled from './styles';
+import { CustomSelectContext } from './custom-select';
+
+export function CustomSelectItem( {
+	children,
+	...props
+}: WordPressComponentProps< CustomSelectItemProps, 'div', false > ) {
+	const customSelectContext = useContext( CustomSelectContext );
+	return (
+		<Styled.CustomSelectItem
+			store={ customSelectContext?.store }
+			{ ...props }
+		>
+			{ children ?? props.value }
+			<Styled.SelectedItemCheckmark
```

I believe we need to continue using the checkmark icon from `@wordpress/icons` for consistency. It [says](https://ariakit.org/reference/select-item-check) we can override the icon by providing a different one as children.

#### brookewp on 2024-01-31T03:12:03Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472254969
Path: packages/components/src/custom-select-control-v2/custom-select-item.tsx

```diff
@@ -0,0 +1,35 @@
+/**
+ * WordPress dependencies
+ */
+import { useContext } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { CustomSelectItemProps } from './types';
+import type { WordPressComponentProps } from '../context';
+import * as Styled from './styles';
+import { CustomSelectContext } from './custom-select';
+
+export function CustomSelectItem( {
+	children,
+	...props
+}: WordPressComponentProps< CustomSelectItemProps, 'div', false > ) {
+	const customSelectContext = useContext( CustomSelectContext );
+	return (
+		<Styled.CustomSelectItem
+			store={ customSelectContext?.store }
+			{ ...props }
+		>
+			{ children ?? props.value }
+			<Styled.SelectedItemCheckmark
```

Ah, nice! I thought we'd have to use the `render` prop so this is much nicer. 🙂

### Review thread 18 (resolved)

#### mirka on 2024-01-30T18:01:22Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471713501
Path: packages/components/src/custom-select-control-v2/custom-select-item.tsx

```diff
@@ -0,0 +1,35 @@
+/**
+ * WordPress dependencies
+ */
+import { useContext } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { CustomSelectItemProps } from './types';
+import type { WordPressComponentProps } from '../context';
+import * as Styled from './styles';
+import { CustomSelectContext } from './custom-select';
+
+export function CustomSelectItem( {
+	children,
+	...props
+}: WordPressComponentProps< CustomSelectItemProps, 'div', false > ) {
+	const customSelectContext = useContext( CustomSelectContext );
+	return (
+		<Styled.CustomSelectItem
+			store={ customSelectContext?.store }
+			{ ...props }
+		>
+			{ children ?? props.value }
+			<Styled.SelectedItemCheckmark
+				// set initial size of check to prevent inline overrides
+				// due to legacy style options
+				style={ {
+					fontSize: 'initial',
+				} }
```

Is this meant to counteract the `style: { fontSize: '50%' }` stuff in the readme/storybook examples? Or something more specific and visually noticeable?

With the old API, any `style` could've been inlined and applied to the entire item row, including stuff like `fill: 'red'` that would've changed the checkmark color. So I don't think we need to be counteracting anything. Wdyt?

#### brookewp on 2024-01-31T03:13:41Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472255655
Path: packages/components/src/custom-select-control-v2/custom-select-item.tsx

```diff
@@ -0,0 +1,35 @@
+/**
+ * WordPress dependencies
+ */
+import { useContext } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { CustomSelectItemProps } from './types';
+import type { WordPressComponentProps } from '../context';
+import * as Styled from './styles';
+import { CustomSelectContext } from './custom-select';
+
+export function CustomSelectItem( {
+	children,
+	...props
+}: WordPressComponentProps< CustomSelectItemProps, 'div', false > ) {
+	const customSelectContext = useContext( CustomSelectContext );
+	return (
+		<Styled.CustomSelectItem
+			store={ customSelectContext?.store }
+			{ ...props }
+		>
+			{ children ?? props.value }
+			<Styled.SelectedItemCheckmark
+				// set initial size of check to prevent inline overrides
+				// due to legacy style options
+				style={ {
+					fontSize: 'initial',
+				} }
```

It didn't affect the checkmark in the legacy verison because there was a hard size (`24px`) whereas in ariakit's it's `1em`. But this was resolved when updating to the `wordpress/icon` checkmark. 😄

### Review thread 19 (resolved)

#### mirka on 2024-01-30T18:07:20Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471721830
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
```

I believe this should be replaced with the caret icon from `@wordpress/icons` as well.

#### ciampo on 2024-01-31T15:11:03Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472969791
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
```

One thing that we should consider is to keep using `SelectArrow` to enjoy its internal logic (like showing only if the item is selected), and use its `render` prop to show the correct icon from `@wordpress/icons`.

Haven't tested it on my machine, but maybe something like `<Ariakit.SelectArrow render={ <Icon icon={ caret } /> } />`  works?

#### brookewp on 2024-01-31T23:46:33Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1473623920
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
```

>like showing only if the item is selected
I think maybe you're referring to the checkmark? The arrow should be rendered at all times: 
<img width="270" alt="Screenshot 2024-01-31 at 3 38 20 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/70d2d339-273e-41a1-adec-efd444e542e2">

But perhaps we could do the same as with the checkmark and add it as a child?
https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471696214

#### ciampo on 2024-02-02T12:07:34Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1475979665
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
```

Ah, yeah — I got confused 😅  Although, as you say, using the `render` prop is probably the best way to keep using `ariakit` and its logic as much as possible, while using our icons

#### brookewp on 2024-02-02T20:56:05Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1476719479
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
```

If we pass the Icon component to the `render` prop, the Ariakit component still seems to override the styles:

```
<Ariakit.SelectArrow render={ <Icon icon={ chevronDown } /> } />
```

We could have more control by passing it as an explicit function, but I am unsure how to merge the props to keep the benefits from Ariakit and have the `wordpress/icon` element. From what I've found, we need to exclude the `children` from Ariakit to override the element, but that would also remove features gained from Ariakit's arrow. 
```
<Ariakit.SelectArrow
				render={ ( { children, ...iconProps } ) => (
					<Icon { ...iconProps } icon={ chevronDown } />
				) }
/>
```

Whereas adding it as a child like we did with `SelectItemCheck` seems to work as we intended: https://github.com/WordPress/gutenberg/blob/5f7e5cf7205922478dff06d77a396d48d660f4fb/packages/components/src/custom-select-control-v2/custom-select-item.tsx#L25-L29

#### mirka on 2024-02-05T16:01:26Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1478482890
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
```

I don't think the built-in logic of [`Ariakit.SelectArrow`](https://ariakit.org/reference/select-arrow) is particularly useful for us at this time, since it's basically just contextual rotation logic based on `placement`. I don't foresee a use case within our design system where we'd want a select popover positioned horizontally to the trigger button. And even if we did, we could address that pretty easily.

### Review thread 20 (resolved)

#### mirka on 2024-01-30T18:20:48Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471743187
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
+		</Styled.CustomSelectButton>
+	);
+};
+
+const CustomSelectButton = contextConnectWithoutRef(
+	UnconnectedCustomSelectButton,
+	'CustomSelectControlButton'
+);
+
+function _CustomSelect( props: _CustomSelectProps & CustomSelectStore ) {
+	const {
+		children,
+		hideLabelFromVision = false,
+		label,
+		store,
+		...restProps
+	} = props;
+
+	return (
+		<>
+			{ hideLabelFromVision ? (
+				<VisuallyHidden as="label">{ label }</VisuallyHidden>
+			) : (
+				<Styled.CustomSelectLabel store={ store }>
+					{ label }
+				</Styled.CustomSelectLabel>
+			) }
```

Not a blocker, but did we consider using `BaseControl` so we don't have to reimplement this custom labeling logic and styles?

#### brookewp on 2024-01-31T03:22:36Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472260418
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
+		</Styled.CustomSelectButton>
+	);
+};
+
+const CustomSelectButton = contextConnectWithoutRef(
+	UnconnectedCustomSelectButton,
+	'CustomSelectControlButton'
+);
+
+function _CustomSelect( props: _CustomSelectProps & CustomSelectStore ) {
+	const {
+		children,
+		hideLabelFromVision = false,
+		label,
+		store,
+		...restProps
+	} = props;
+
+	return (
+		<>
+			{ hideLabelFromVision ? (
+				<VisuallyHidden as="label">{ label }</VisuallyHidden>
+			) : (
+				<Styled.CustomSelectLabel store={ store }>
+					{ label }
+				</Styled.CustomSelectLabel>
+			) }
```

We didn't discuss it, but I considered it. I initially hesitated because the Ariakit label accepts the `store` prop. But I don't see a reason we'd need that right now, since the label is static. So we could swap for BaseControl if that is preferred!

#### mirka on 2024-02-05T15:28:02Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1478435778
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,128 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+		>
+			<div>{ computedRenderSelectedValue( currentValue ) }</div>
+			<Ariakit.SelectArrow />
+		</Styled.CustomSelectButton>
+	);
+};
+
+const CustomSelectButton = contextConnectWithoutRef(
+	UnconnectedCustomSelectButton,
+	'CustomSelectControlButton'
+);
+
+function _CustomSelect( props: _CustomSelectProps & CustomSelectStore ) {
+	const {
+		children,
+		hideLabelFromVision = false,
+		label,
+		store,
+		...restProps
+	} = props;
+
+	return (
+		<>
+			{ hideLabelFromVision ? (
+				<VisuallyHidden as="label">{ label }</VisuallyHidden>
+			) : (
+				<Styled.CustomSelectLabel store={ store }>
+					{ label }
+				</Styled.CustomSelectLabel>
+			) }
```

Let's merge as is, and we'll swap it out for BaseControl in a future iteration.

### Review thread 21 (resolved)

#### mirka on 2024-01-30T18:25:50Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471749455
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } >;
+
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
```

```suggestion
	style?: React.CSSProperties;
```

### Review thread 22

#### mirka on 2024-01-30T18:30:08Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471754879
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } >;
+
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
+	/**
+	 * Optional classname for the component.
+	 */
+	className?: string;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Pass in a description that will be shown to screen readers associated with the
+	 * select trigger button. If no value is passed, the text "Currently selected:
+	 * selectedItem.name" will be used fully translated.
+	 */
+	describedBy?: string;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+	/**
+	 * Function called with the control's internal state changes. The `selectedItem`
+	 * property contains the next selected item.
+	 */
+	onChange?: ( newValue: OnChangeObject ) => void;
+	/**
+	 * A handler for `onBlur` events.
+	 */
+	onBlur?: FocusEventHandler< HTMLButtonElement >;
+	/**
+	 * A handler for `onFocus` events.
+	 */
+	onFocus?: FocusEventHandler< HTMLButtonElement >;
+	/**
+	 * A handler for `onMouseOver` events.
+	 */
+	onMouseOut?: MouseEventHandler< HTMLButtonElement >;
+	/**
+	 * A handler for `onMouseOut` events.
+	 */
+	onMouseOver?: MouseEventHandler< HTMLButtonElement >;
+	/**
+	 * The options that can be chosen from.
+	 */
+	options: Array< Option >;
+	/**
+	 * The size of the control.
+	 *
+	 * @default default
+	 */
+	size?: 'default' | 'small' | '__unstable-large';
+	/**
+	 * Can be used to externally control the value of the control.
+	 */
+	value?: Option;
+	/**
+	 * Legacy way to add additional text to the right of each option.
+	 *
+	 * @default false
+	 */
+	__experimentalShowSelectedHint?: boolean;
+	/**
+	 * Opt-in prop for an unconstrained width style which became the default in
+	 * WordPress 6.4. The prop is no longer needed and can be safely removed.
```

```suggestion
	 * WordPress 6.5. The prop is no longer needed and can be safely removed.
```

### Review thread 23

#### mirka on 2024-01-30T18:50:23Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471783141
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
```

We've been using this convention for string values. The quotations marks look a bit redundant in Storybook prop tables but it's technically correct.

```suggestion
	 * @default 'default'
```

### Review thread 24

#### mirka on 2024-01-30T19:04:16Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471802171
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } >;
```

`CustomSelectButtonProps` itself is not a union type so I don't think we can exclude it directly. How about something like this:

```suggestion
export type CustomSelectProps = _CustomSelectProps &
	Omit< CustomSelectButtonProps, 'size' > & {
		/**
		 * The size of the control.
		 *
		 * @default 'default'
		 */
		size?: Exclude< CustomSelectButtonProps[ 'size' ], 'small' >;
	};
```

### Review thread 25 (resolved)

#### mirka on 2024-01-30T19:08:56Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471808245
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
```

Unnecessary export?

```suggestion
function isLegacy( props: any ): props is LegacyCustomSelectProps {
```

#### brookewp on 2024-01-31T03:23:55Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472260928
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
```

Nice catch! A leftover from when I thought we could use it in styling many moons ago. 😅

### Review thread 26 (resolved)

#### mirka on 2024-01-30T19:21:33Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471823860
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } >;
+
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
```

Nit: Improves clarity when looking at unionized `onChange` signatures in IntelliSense.

```suggestion
type LegacyOnChangeObject = {
```

### Review thread 27 (resolved)

#### mirka on 2024-01-30T20:35:42Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471919343
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return typeof props.options !== 'undefined';
+}
+
+export function DefaultExport(
+	props: LegacyCustomSelectProps | CustomSelectProps
```

```suggestion
	props: WordPressComponentProps<
		LegacyCustomSelectProps | CustomSelectProps,
		'button'
	>
```

It seems like this is the best place to mix in the button event handler types (and everything else that could be passed onto the final `<button>`), rather than defining them explicitly in the `LegacyCustomSelectProps` type (the `onBlur`, `onFocus`, etc).

I imagine you did it explicitly because the legacy version explicitly passes only that subset of event handlers down to the underlying component, but since we're going with full `...restProps` passing in the new implementation I think it's fine to be honest with our types.

#### brookewp on 2024-01-31T03:29:02Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472263153
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return typeof props.options !== 'undefined';
+}
+
+export function DefaultExport(
+	props: LegacyCustomSelectProps | CustomSelectProps
```

I believe I had something like that, but I explicitly added them after a review: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1457221361

But curious if we should go back to it or if I could adjust what I had before to improve it.

#### mirka on 2024-02-01T13:20:09Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1474457407
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return typeof props.options !== 'undefined';
+}
+
+export function DefaultExport(
+	props: LegacyCustomSelectProps | CustomSelectProps
```

I don't have a strong opinion about the specifics, only that we have to accept `button` prop types at this top-level adapter _for the non-legacy case as well_, in some way or another.

So for example, this cannot be a TS error:

```jsx
<CustomSelect label="foo" onClick={ () => {} }>
	foobar
</CustomSelect>
```

#### brookewp on 2024-02-01T23:45:59Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1475326715
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return typeof props.options !== 'undefined';
+}
+
+export function DefaultExport(
+	props: LegacyCustomSelectProps | CustomSelectProps
```

Makes sense!

#### ciampo on 2024-02-02T12:17:20Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1475988442
Path: packages/components/src/custom-select-control-v2/legacy-adapter.tsx

```diff
@@ -0,0 +1,20 @@
+/**
+ * Internal dependencies
+ */
+import _LegacyCustomSelect from './legacy-component';
+import _NewCustomSelect from './default-component';
+import type { CustomSelectProps, LegacyCustomSelectProps } from './types';
+
+export function isLegacy( props: any ): props is LegacyCustomSelectProps {
+	return typeof props.options !== 'undefined';
+}
+
+export function DefaultExport(
+	props: LegacyCustomSelectProps | CustomSelectProps
```

I don't think that the two comments (mine and Lena's) are in opposition — I still believe that, in order to support the same API surface of the legacy component, we should _not_ wrap `LegacyProps` in `WordPressComponentProps`.
At the same time, Lena's suggestion is about supporting those props for the non-legacy version

What about

```ts
	props:
		| LegacyCustomSelectProps
		| WordPressComponentProps< CustomSelectProps, 'button' >
```

?

### Review thread 28 (resolved)

#### mirka on 2024-01-30T21:02:23Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471954361
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } >;
+
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
```

One important thing we need to do to make all these type defs usable in practice is to let the TS compiler know whether the consumer is trying to use the component with the legacy or current APIs.

For example, when a consumer does `<CustomSelectControl options={ [] }>` we want the compiler to know that the `onChange` should have the legacy signature. Similarly, if the consumer passes `children`, it should know that the `onChange` should have the new signature.

We've been doing this by adding an explicit `never` type like this, so TS knows that the presence of `children` means we are not in a legacy context:

```suggestion
export type LegacyCustomSelectProps = {
	children?: never;
```

Similarly, we would need to add `options?: never;` to `CustomSelectProps`.

When the types get complex like this, I've found it useful to add "static typing tests" like for the [Button component here](https://github.com/WordPress/gutenberg/blob/8c80c0e2be01b4868aa55727b6cfcfca0ef438ba/packages/components/src/button/test/index.tsx#L622). It verifies that the types are working as expected.

So some basic things we could check for would be (pseudocode):

```jsx
<CustomSelect
	label="foo"
	options={ [] }
	onChange={ ( _: string | string[] ) => undefined } // this should error
	onChange={ ( _: { selectedItem: unknown } ) => undefined } // this shouldn't
/>;

<CustomSelect
	label="foo"
	onChange={ ( _: string | string[] ) => undefined } // this shouldn't error
	onChange={ ( _: { selectedItem: unknown } ) => undefined } // this should
>
	foobar
</CustomSelect>;
```

#### brookewp on 2024-01-31T03:30:08Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472263648
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -40,15 +33,131 @@ export type CustomSelectProps = {
 	/**
 	 * The size of the control.
 	 *
-	 * @default 'default'
+	 * @default default
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } >;
+
+/**
+ * The legacy object structure for the options array.
+ */
+type Option = {
+	key: string;
+	name: string;
+	style?: {};
+	className?: string;
+	__experimentalHint?: string;
+};
+
+/**
+ * The legacy object returned from the onChange event.
+ */
+type OnChangeObject = {
+	selectedItem: Option;
+	highlightedIndex?: number;
+	isOpen?: boolean;
+};
+
+export type LegacyCustomSelectProps = {
```

Woah, TIL about static typing tests. Very cool!

### Review thread 29 (resolved)

#### mirka on 2024-01-30T21:13:01Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471968284
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,135 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+import * as Styled from '../styles';
+import { ContextSystemProvider } from '../../context';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		options,
+		onChange,
+		size = 'default',
+		value: valueProp,
+		...restProps
+	} = props;
+
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				inputValue: '',
+				isOpen: state.open,
+				selectedItem: {
+					// Value will always be a string for the legacy component
+					name: value as string,
+					key: value as string,
```

It looks like the Ariakit types can infer from the `defaultValue` type we've passed that `value` will also be `string`!

```suggestion
					name: value,
					key: value,
```

#### brookewp on 2024-01-31T03:25:59Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1472261811
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -0,0 +1,135 @@
+/**
+ * External dependencies
+ */
+// eslint-disable-next-line no-restricted-imports
+import * as Ariakit from '@ariakit/react';
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import _CustomSelect from '../custom-select';
+import type { LegacyCustomSelectProps } from '../types';
+import { CustomSelectItem } from '..';
+import * as Styled from '../styles';
+import { ContextSystemProvider } from '../../context';
+
+function _LegacyCustomSelect( props: LegacyCustomSelectProps ) {
+	const {
+		__experimentalShowSelectedHint,
+		__next40pxDefaultSize = false,
+		options,
+		onChange,
+		size = 'default',
+		value: valueProp,
+		...restProps
+	} = props;
+
+	// Forward props + store from v2 implementation
+	const store = Ariakit.useSelectStore( {
+		async setValue( value ) {
+			if ( ! onChange ) return;
+
+			// Executes the logic in a microtask after the popup is closed.
+			// This is simply to ensure the isOpen state matches that in Downshift.
+			await Promise.resolve();
+			const state = store.getState();
+
+			const changeObject = {
+				highlightedIndex: state.renderedItems.findIndex(
+					( item ) => item.value === value
+				),
+				inputValue: '',
+				isOpen: state.open,
+				selectedItem: {
+					// Value will always be a string for the legacy component
+					name: value as string,
+					key: value as string,
```

Amazing! That wasn't always the case 😅 I should track down why it changed

### Review thread 30 (resolved)

#### brookewp on 2024-01-31T23:50:33Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1473626470
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,137 @@
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo, useState } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+import { Icon, chevronDown } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		setUnmountOnHide,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+			moveOnKeyDown
+			showOnKeyDown={ false }
```

These props are needed in combination with the onBlur/onFocus unmount logic for keydown behaviour to work the same as legacy

#### ciampo on 2024-02-05T16:17:41Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1478506507
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -0,0 +1,137 @@
+/**
+ * WordPress dependencies
+ */
+import { createContext, useMemo, useState } from '@wordpress/element';
+import { __, sprintf } from '@wordpress/i18n';
+import { Icon, chevronDown } from '@wordpress/icons';
+
+/**
+ * Internal dependencies
+ */
+import { VisuallyHidden } from '..';
+import * as Styled from './styles';
+import type {
+	CustomSelectContext as CustomSelectContextType,
+	CustomSelectStore,
+	CustomSelectButtonProps,
+	_CustomSelectProps,
+} from './types';
+import {
+	contextConnectWithoutRef,
+	useContextSystem,
+	type WordPressComponentProps,
+} from '../context';
+
+export const CustomSelectContext =
+	createContext< CustomSelectContextType >( undefined );
+
+function defaultRenderSelectedValue(
+	value: CustomSelectButtonProps[ 'value' ]
+) {
+	const isValueEmpty = Array.isArray( value )
+		? value.length === 0
+		: value === undefined || value === null;
+
+	if ( isValueEmpty ) {
+		return __( 'Select an item' );
+	}
+
+	if ( Array.isArray( value ) ) {
+		return value.length === 1
+			? value[ 0 ]
+			: // translators: %s: number of items selected (it will always be 2 or more items)
+			  sprintf( __( '%s items selected' ), value.length );
+	}
+
+	return value;
+}
+
+const UnconnectedCustomSelectButton = (
+	props: Omit<
+		WordPressComponentProps<
+			CustomSelectButtonProps & CustomSelectStore,
+			'button',
+			false
+		>,
+		'onChange'
+	>
+) => {
+	const {
+		renderSelectedValue,
+		setUnmountOnHide,
+		size = 'default',
+		store,
+		...restProps
+	} = useContextSystem( props, 'CustomSelectControlButton' );
+
+	const { value: currentValue } = store.useState();
+
+	const computedRenderSelectedValue = useMemo(
+		() => renderSelectedValue ?? defaultRenderSelectedValue,
+		[ renderSelectedValue ]
+	);
+
+	return (
+		<Styled.CustomSelectButton
+			{ ...restProps }
+			size={ size }
+			hasCustomRenderProp={ !! renderSelectedValue }
+			store={ store }
+			moveOnKeyDown
+			showOnKeyDown={ false }
```

Should we add an inline comment about that?

### Review thread 31 (resolved)

#### brookewp on 2024-02-06T00:30:38Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1479087082
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -42,13 +35,134 @@ export type CustomSelectProps = {
 	 *
 	 * @default 'default'
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } > & {
```

I'm still seeing `'small'` as an option in Storybook, and I'm not getting an error when adding it to the new CustomSelect. 🤔 Is `Exclude` not the correct method?

#### mirka on 2024-02-06T00:43:22Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1479094146
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -42,13 +35,134 @@ export type CustomSelectProps = {
 	 *
 	 * @default 'default'
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } > & {
```

Try [this one](https://github.com/WordPress/gutenberg/pull/57902#discussion_r1471802171)!

#### brookewp on 2024-02-06T17:45:36Z

URL: https://github.com/WordPress/gutenberg/pull/57902#discussion_r1480327702
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -42,13 +35,134 @@ export type CustomSelectProps = {
 	 *
 	 * @default 'default'
 	 */
-	size?: 'default' | 'small';
+	size?: 'compact' | 'default' | 'small';
 	/**
 	 * Can be used to externally control the value of the control.
 	 */
 	value?: string | string[];
 };
 
+export type _CustomSelectProps = {
+	/**
+	 * The child elements. This should be composed of CustomSelectItem components.
+	 */
+	children: React.ReactNode;
+	/**
+	 * Used to visually hide the label. It will always be visible to screen readers.
+	 *
+	 * @default false
+	 */
+	hideLabelFromVision?: boolean;
+	/**
+	 * Label for the control.
+	 */
+	label: string;
+};
+
+export type CustomSelectProps = _CustomSelectProps &
+	Exclude< CustomSelectButtonProps, { size: 'small' } > & {
```

Ah, sorry I missed that; thank you!

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/custom-select-control-v2/README.md (MODIFIED, +97/-7)
- packages/components/src/custom-select-control-v2/custom-select-item.tsx (ADDED, +29/-0)
- packages/components/src/custom-select-control-v2/custom-select.tsx (ADDED, +122/-0)
- packages/components/src/custom-select-control-v2/default-component/index.tsx (ADDED, +24/-0)
- packages/components/src/custom-select-control-v2/index.tsx (MODIFIED, +2/-102)
- packages/components/src/custom-select-control-v2/legacy-adapter.tsx (ADDED, +25/-0)
- packages/components/src/custom-select-control-v2/legacy-component/index.tsx (ADDED, +133/-0)
- packages/components/src/custom-select-control-v2/stories/default.story.tsx (RENAMED, +26/-33)
- packages/components/src/custom-select-control-v2/stories/legacy.story.tsx (ADDED, +88/-0)
- packages/components/src/custom-select-control-v2/styles.ts (MODIFIED, +82/-38)
- packages/components/src/custom-select-control-v2/test/index.tsx (MODIFIED, +788/-147)
- packages/components/src/custom-select-control-v2/types.ts (MODIFIED, +148/-20)
