---
number: 45787
type: pull_request
state: MERGED
merged_at: 2022-12-21T11:59:00Z
url: https://github.com/WordPress/gutenberg/pull/45787
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #45787: Refactor Dropdown component to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/45787
- Author: kienstra
- Created: 2022-11-15T23:14:28Z
- Updated: 2023-01-05T23:05:02Z
- Comments: 4 of 4
- Review threads: 18 of 18
- Changed files: 9 of 9
- Diff: ../pr-diffs/45787.diff

## Body

## What?
Convert the `Dropdown` component to TypeScript

## Why?
As part of an [effort](https://github.com/WordPress/gutenberg/issues/35744) to convert `@wordpress/components` to TypeScript

## How?
Mainly by adding types to the `Dropdown` component

## Testing Instructions
1. `npm run storybook:dev`
2. Expected: The Storybook still works

## Screenshots

<img width="780" alt="Screen Shot 2022-12-07 at 7 50 54 PM" src="https://user-images.githubusercontent.com/4063887/206336640-7ad013f8-63b2-4fcb-9061-d0951df5f4a7.png">

## Issue comments

### codesandbox on 2022-11-15T23:14:30Z

URL: https://github.com/WordPress/gutenberg/pull/45787#issuecomment-1315996449

<img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.light.svg#gh-dark-mode-only" /><img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.svg#gh-light-mode-only" />&nbsp; Open in CodeSandbox <a href="https://codesandbox.io/p/github/kienstra/gutenberg/update/dropdown-to-ts?workspace=%7B%22gitSidebarPanel%22:%22PR%22,%22sidebarPanel%22:%22GIT%22%7D">Web Editor</a> | <a href="https://codesandbox.io/p/vscode?owner=kienstra&repo=gutenberg&branch=update/dropdown-to-ts">VS Code</a> | <a href="https://codesandbox.io/p/vscode?owner=kienstra&repo=gutenberg&branch=update/dropdown-to-ts&insiders=true">VS Code Insiders</a>

<!-- open-in-codesandbox:complete -->

### kienstra on 2022-12-08T04:20:41Z

URL: https://github.com/WordPress/gutenberg/pull/45787#issuecomment-1341976496

Hi @ciampo,
Could you use this PR?

Hope you're doing well! It's been a while 😄

### kienstra on 2022-12-20T20:16:52Z

URL: https://github.com/WordPress/gutenberg/pull/45787#issuecomment-1360113439

Hi @mirka,
Thanks for your great ideas, the [as const](https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053241111) one is really nice.

I committed all of your suggestions.

Also, thanks for your idea of joining the GitHub Gutenberg team. I just requested that in `#core-editor`.

### kienstra on 2022-12-21T15:42:25Z

URL: https://github.com/WordPress/gutenberg/pull/45787#issuecomment-1361523529

Thanks so much, Lena! You had really good ideas, like `typeof Popover`.

## Review threads

### Review thread 1 (resolved)

#### kienstra on 2022-11-30T04:09:43Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1035509472
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -165,7 +164,7 @@ const BorderControlDropdown = (
 		? 'bottom left'
 		: undefined;
 
-	const renderToggle = ( { onToggle = noop } ) => (
+	const renderToggle = ( { onToggle }: { onToggle: () => void } ) => (
```

There's always an `onToggle` prop, so no need for a default value of `noop`

### Review thread 2

#### kienstra on 2022-12-08T02:17:39Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042856744
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -165,7 +165,9 @@ const BorderControlDropdown = (
 		? 'bottom left'
 		: undefined;
 
-	const renderToggle = ( { onToggle = noop } ) => (
+	const renderToggle: DropdownComponentProps[ 'renderToggle' ] = ( {
+		onToggle,
```

`renderToggle` always has an `onToggle` prop, so no need for the default of  `noop`.

### Review thread 3

#### kienstra on 2022-12-08T02:20:17Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042857779
Path: packages/components/src/dropdown/README.md

```diff
@@ -32,96 +32,85 @@ const MyDropdown = () => (
 
 The component accepts the following props. Props not included in this set will be applied to the element wrapping Popover content.
 
-### className
+### `className`: `string`
 
 className of the global container
 
--   Type: `String`
 -   Required: No
 
-### contentClassName
+### `contentClassName`: `string`
 
 If you want to target the dropdown menu for styling purposes, you need to provide a contentClassName because it's not being rendered as a child of the container node.
 
--   Type: `String`
 -   Required: No
 
-### position
+### `expandOnMobile`: `boolean`
```

The props are now alphabetized, so this diff is a little hard to read.

### Review thread 4

#### kienstra on 2022-12-08T02:21:15Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042858154
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -1,21 +1,26 @@
-// @ts-nocheck
 /**
  * External dependencies
  */
 import classnames from 'classnames';
+import type { ForwardedRef } from 'react';
 
 /**
  * WordPress dependencies
  */
-import { useRef, useEffect, useState } from '@wordpress/element';
+import { forwardRef, useEffect, useRef, useState } from '@wordpress/element';
 import { useMergeRefs } from '@wordpress/compose';
 
 /**
  * Internal dependencies
  */
 import Popover from '../popover';
+import type { WordPressComponentProps } from '../ui/context';
+import type { DropdownProps } from './types';
 
-function useObservableState( initialState, onStateChange ) {
+function useObservableState(
+	initialState: boolean,
+	onStateChange?: ( newState: boolean ) => void
+): [ boolean, ( newState: boolean ) => void ] {
```

You'd hope that TS would infer the return type from the `return` statement below, but it doesn't.

#### mirka on 2022-12-20T12:03:11Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053241111
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -1,21 +1,26 @@
-// @ts-nocheck
 /**
  * External dependencies
  */
 import classnames from 'classnames';
+import type { ForwardedRef } from 'react';
 
 /**
  * WordPress dependencies
  */
-import { useRef, useEffect, useState } from '@wordpress/element';
+import { forwardRef, useEffect, useRef, useState } from '@wordpress/element';
 import { useMergeRefs } from '@wordpress/compose';
 
 /**
  * Internal dependencies
  */
 import Popover from '../popover';
+import type { WordPressComponentProps } from '../ui/context';
+import type { DropdownProps } from './types';
 
-function useObservableState( initialState, onStateChange ) {
+function useObservableState(
+	initialState: boolean,
+	onStateChange?: ( newState: boolean ) => void
+): [ boolean, ( newState: boolean ) => void ] {
```

```diff
	return [
		state,
+		( value: boolean ) => {
			setState( value );
			if ( onStateChange ) {
				onStateChange( value );
			}
		},
+	] as const;
```

☝️ The trick is to append an `as const` here ✨

#### kienstra on 2022-12-20T18:49:38Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053643616
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -1,21 +1,26 @@
-// @ts-nocheck
 /**
  * External dependencies
  */
 import classnames from 'classnames';
+import type { ForwardedRef } from 'react';
 
 /**
  * WordPress dependencies
  */
-import { useRef, useEffect, useState } from '@wordpress/element';
+import { forwardRef, useEffect, useRef, useState } from '@wordpress/element';
 import { useMergeRefs } from '@wordpress/compose';
 
 /**
  * Internal dependencies
  */
 import Popover from '../popover';
+import type { WordPressComponentProps } from '../ui/context';
+import type { DropdownProps } from './types';
 
-function useObservableState( initialState, onStateChange ) {
+function useObservableState(
+	initialState: boolean,
+	onStateChange?: ( newState: boolean ) => void
+): [ boolean, ( newState: boolean ) => void ] {
```

Whoa, thanks! So much cleaner to let TS infer the type.

Committed in https://github.com/WordPress/gutenberg/pull/45787/commits/1f9ff853fa72a2a784be08449b7f99561bc647ad

### Review thread 5

#### kienstra on 2022-12-08T02:23:45Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042859204
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -70,8 +77,13 @@ export default function Dropdown( props ) {
 	 * case a dialog has opened, allowing focus to return when it's dismissed.
 	 */
 	function closeIfFocusOutside() {
+		if ( ! containerRef.current ) {
+			return;
```

This is hacky, but without it there are TS errors:

<img width="928" alt="Screen Shot 2022-12-07 at 8 22 48 PM" src="https://user-images.githubusercontent.com/4063887/206340980-d7c27ea8-a57f-477c-86fe-bb79ec37af94.png">

#### mirka on 2022-12-20T13:00:11Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053289503
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -70,8 +77,13 @@ export default function Dropdown( props ) {
 	 * case a dialog has opened, allowing focus to return when it's dismissed.
 	 */
 	function closeIfFocusOutside() {
+		if ( ! containerRef.current ) {
+			return;
```

FWIW I don't think this is hacky. It's a proper guard 🙂 I love early returns!

#### kienstra on 2022-12-20T18:50:47Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053644567
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -70,8 +77,13 @@ export default function Dropdown( props ) {
 	 * case a dialog has opened, allowing focus to return when it's dismissed.
 	 */
 	function closeIfFocusOutside() {
+		if ( ! containerRef.current ) {
+			return;
```

Thanks!

### Review thread 6

#### kienstra on 2022-12-08T02:29:03Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042861328
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,20 +27,27 @@ export default {
 		renderContent: { control: { type: null } },
 		renderToggle: { control: { type: null } },
 	},
+	parameters: {
```

Without `parameters`, there's an error with `npm run storybook:build`:

```
ERR! => Failed to build the preview
ERR! Module build failed (from ./storybook/webpack/source-link-loader.js):
ERR! TypeError: wp-content/plugins/gutenberg/packages/components/src/dropdown/stories/index.tsx: Property value of ObjectProperty expected node to be of a type ["Expression","PatternLike"] but instead got undefined
```

#### mirka on 2022-12-20T10:30:19Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053160175
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,20 +27,27 @@ export default {
 		renderContent: { control: { type: null } },
 		renderToggle: { control: { type: null } },
 	},
+	parameters: {
```

Ah, you caught a bug, thank you! I prepped a fix in #46670.

#### kienstra on 2022-12-20T18:51:09Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053644862
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,20 +27,27 @@ export default {
 		renderContent: { control: { type: null } },
 		renderToggle: { control: { type: null } },
 	},
+	parameters: {
```

Ah, thanks for finding that!

### Review thread 7

#### kienstra on 2022-12-08T02:34:56Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042863733
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -136,3 +152,32 @@ export default function Dropdown( props ) {
 		</div>
 	);
 }
+
+/**
+ * Renders a button that opens a floating content modal when clicked.
+ *
+ * ```jsx
+ * import { Button, Dropdown } from '@wordpress/components';
+ *
+ * const MyDropdown = () => (
+ *   <Dropdown
+ *     className="my-container-class-name"
+ *     contentClassName="my-popover-content-classname"
+ *     position="bottom right"
+ *     renderToggle={ ( { isOpen, onToggle } ) => (
+ *       <Button
+ *         variant="primary"
+ *         onClick={ onToggle }
+ *         aria-expanded={ isOpen }
+ *       >
+ *         Toggle Popover!
+ *       </Button>
+ *     ) }
+ *     renderContent={ () => <div>This is the content of the popover.</div> }
+ *   />
+ * );
+ * ```
+ */
+export const Dropdown = forwardRef( UnforwardedDropdown );
```

Maybe I did this wrong.

But `<Dropdown>` is passed a `ref` prop [here](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/border-control/border-control-dropdown/component.tsx#L250).

So the Dropdown needs to be wrapped in `forwardRef()` to accept that `ref`.

#### mirka on 2022-12-20T13:04:51Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053293566
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -136,3 +152,32 @@ export default function Dropdown( props ) {
 		</div>
 	);
 }
+
+/**
+ * Renders a button that opens a floating content modal when clicked.
+ *
+ * ```jsx
+ * import { Button, Dropdown } from '@wordpress/components';
+ *
+ * const MyDropdown = () => (
+ *   <Dropdown
+ *     className="my-container-class-name"
+ *     contentClassName="my-popover-content-classname"
+ *     position="bottom right"
+ *     renderToggle={ ( { isOpen, onToggle } ) => (
+ *       <Button
+ *         variant="primary"
+ *         onClick={ onToggle }
+ *         aria-expanded={ isOpen }
+ *       >
+ *         Toggle Popover!
+ *       </Button>
+ *     ) }
+ *     renderContent={ () => <div>This is the content of the popover.</div> }
+ *   />
+ * );
+ * ```
+ */
+export const Dropdown = forwardRef( UnforwardedDropdown );
```

Good catch 👍 Looks good to me.

#### kienstra on 2022-12-20T18:51:42Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053645261
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -136,3 +152,32 @@ export default function Dropdown( props ) {
 		</div>
 	);
 }
+
+/**
+ * Renders a button that opens a floating content modal when clicked.
+ *
+ * ```jsx
+ * import { Button, Dropdown } from '@wordpress/components';
+ *
+ * const MyDropdown = () => (
+ *   <Dropdown
+ *     className="my-container-class-name"
+ *     contentClassName="my-popover-content-classname"
+ *     position="bottom right"
+ *     renderToggle={ ( { isOpen, onToggle } ) => (
+ *       <Button
+ *         variant="primary"
+ *         onClick={ onToggle }
+ *         aria-expanded={ isOpen }
+ *       >
+ *         Toggle Popover!
+ *       </Button>
+ *     ) }
+ *     renderContent={ () => <div>This is the content of the popover.</div> }
+ *   />
+ * );
+ * ```
+ */
+export const Dropdown = forwardRef( UnforwardedDropdown );
```

Oh thanks!

### Review thread 8

#### kienstra on 2022-12-08T02:36:52Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042864507
Path: packages/components/src/dropdown/test/index.tsx

```diff
@@ -24,14 +24,13 @@ describe( 'Dropdown', () => {
 					</button>
 				) }
 				renderContent={ () => <span>test</span> }
-				popoverProps={ { 'data-testid': 'popover' } }
```

This caused a TS error:
<img width="958" alt="Screen Shot 2022-12-07 at 8 36 11 PM" src="https://user-images.githubusercontent.com/4063887/206342581-d052a2e2-7392-4124-84a1-aa5e0e241c49.png">

#### mirka on 2022-12-20T13:20:57Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053309990
Path: packages/components/src/dropdown/test/index.tsx

```diff
@@ -24,14 +24,13 @@ describe( 'Dropdown', () => {
 					</button>
 				) }
 				renderContent={ () => <span>test</span> }
-				popoverProps={ { 'data-testid': 'popover' } }
```

Huh, interesting! Looks like other component libraries are also experiencing this issue.
https://github.com/mui/material-ui/issues/20160#issuecomment-600277849

#### kienstra on 2022-12-20T18:57:52Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053650000
Path: packages/components/src/dropdown/test/index.tsx

```diff
@@ -24,14 +24,13 @@ describe( 'Dropdown', () => {
 					</button>
 				) }
 				renderContent={ () => <span>test</span> }
-				popoverProps={ { 'data-testid': 'popover' } }
```

Oh, good to see we're not the only ones to find that problem

### Review thread 9

#### kienstra on 2022-12-08T02:39:58Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042865742
Path: packages/components/src/dropdown/test/index.tsx

```diff
@@ -24,14 +24,13 @@ describe( 'Dropdown', () => {
 					</button>
 				) }
 				renderContent={ () => <span>test</span> }
-				popoverProps={ { 'data-testid': 'popover' } }
 			/>
 		);
 
 		const button = screen.getByRole( 'button', { expanded: false } );
 
 		expect( button ).toBeVisible();
-		expect( screen.queryByTestId( 'popover' ) ).not.toBeInTheDocument();
+		expect( screen.queryByText( 'test' ) ).not.toBeInTheDocument();
```

Changed because `popoverProps` [can't accept](https://github.com/WordPress/gutenberg/pull/45787/files#r1042864507) `{ 'data-testid': 'popover' }`.

Instead of doing a `@ts-ignore` for that type error, I changed this.

This is testing the same thing as before: whether `<Popover>` is rendering.

If `<Popover>` is rendering, it [will call](https://github.com/WordPress/gutenberg/blob/c56dc5cd31ef66a37b79f963f6c73d9f279a25c4/packages/components/src/dropdown/index.tsx#L149) `renderContent()`, which will render `<span>test</span>`.

#### mirka on 2022-12-20T13:21:42Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053310645
Path: packages/components/src/dropdown/test/index.tsx

```diff
@@ -24,14 +24,13 @@ describe( 'Dropdown', () => {
 					</button>
 				) }
 				renderContent={ () => <span>test</span> }
-				popoverProps={ { 'data-testid': 'popover' } }
 			/>
 		);
 
 		const button = screen.getByRole( 'button', { expanded: false } );
 
 		expect( button ).toBeVisible();
-		expect( screen.queryByTestId( 'popover' ) ).not.toBeInTheDocument();
+		expect( screen.queryByText( 'test' ) ).not.toBeInTheDocument();
```

Good call. Works for me 👍

### Review thread 10

#### kienstra on 2022-12-08T04:10:44Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1042904680
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -134,7 +135,7 @@ export function CustomColorPickerDropdown( {
 	popoverProps: receivedPopoverProps,
 	...props
 }: CustomColorPickerDropdownProps ) {
-	const popoverProps = useMemo(
+	const popoverProps = useMemo< DropdownProps[ 'popoverProps' ] >(
```

Maybe this is wrong

#### mirka on 2022-12-20T13:51:50Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053339655
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -134,7 +135,7 @@ export function CustomColorPickerDropdown( {
 	popoverProps: receivedPopoverProps,
 	...props
 }: CustomColorPickerDropdownProps ) {
-	const popoverProps = useMemo(
+	const popoverProps = useMemo< DropdownProps[ 'popoverProps' ] >(
```

Looks good to me 👍

### Review thread 11 (resolved)

#### mirka on 2022-12-20T11:48:35Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053229672
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,17 +26,24 @@ export default {
 		renderContent: { control: { type: null } },
 		renderToggle: { control: { type: null } },
 	},
+	parameters: {
+		controls: {
+			expanded: true,
+		},
+		docs: { source: { state: 'open' } },
```

```suggestion
		controls: {
			expanded: true,
		},
```

I guess we should keep the expanded controls, but remove the open docs since the code snippets aren't very helpful in this case?

#### kienstra on 2022-12-20T18:38:58Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053635506
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,17 +26,24 @@ export default {
 		renderContent: { control: { type: null } },
 		renderToggle: { control: { type: null } },
 	},
+	parameters: {
+		controls: {
+			expanded: true,
+		},
+		docs: { source: { state: 'open' } },
```

Good idea. Committed in https://github.com/WordPress/gutenberg/pull/45787/commits/775f10b1de7fb5faf0c3ab3cf9588496c672f078

Open docs don't help.

### Review thread 12 (resolved)

#### mirka on 2022-12-20T12:10:32Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053247190
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -42,12 +47,14 @@ export default function Dropdown( props ) {
 		onClose,
 		onToggle,
 		style,
-	} = props;
+	}: WordPressComponentProps< DropdownProps, 'div' >,
```

The `WordPressComponentProps< SomeProps, 'div' >` construct will allow _all_ props that a `div` can take, so it basically only makes sense when we're passing the `...restProps` to the div.

Since were not passing rest props here, I think it would be better to not use `WordPressComponentProps`, and instead explicitly add types for `className` and `style`.

```suggestion
	}: DropdownProps,
```

#### kienstra on 2022-12-20T19:16:41Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053665258
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -42,12 +47,14 @@ export default function Dropdown( props ) {
 		onClose,
 		onToggle,
 		style,
-	} = props;
+	}: WordPressComponentProps< DropdownProps, 'div' >,
```

Good point, this doesn't accept all of `WordPressComponentProps`, only `className` and `style.`

Committed in https://github.com/WordPress/gutenberg/compare/1f9ff853fa72a2a784be08449b7f99561bc647ad...181cfdae721e7c1dccf043fb19ec92cbab9f3a63

### Review thread 13 (resolved)

#### mirka on 2022-12-20T12:55:28Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053285196
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -42,12 +47,14 @@ export default function Dropdown( props ) {
 		onClose,
 		onToggle,
 		style,
-	} = props;
+	}: WordPressComponentProps< DropdownProps, 'div' >,
+	forwardedRef: ForwardedRef< any >
+) {
 	// Use internal state instead of a ref to make sure that the component
 	// re-renders when the popover's anchor updates.
 	const [ fallbackPopoverAnchor, setFallbackPopoverAnchor ] =
-		useState( null );
-	const containerRef = useRef();
+		useState< HTMLDivElement | null >( null );
+	const containerRef = useRef< HTMLDivElement | null >( null );
```

This bottom one can just remain undefined, since it never took `null` to begin with:

```suggestion
	const containerRef = useRef< HTMLDivElement >();
```

#### kienstra on 2022-12-20T19:18:09Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053666354
Path: packages/components/src/dropdown/index.tsx

```diff
@@ -42,12 +47,14 @@ export default function Dropdown( props ) {
 		onClose,
 		onToggle,
 		style,
-	} = props;
+	}: WordPressComponentProps< DropdownProps, 'div' >,
+	forwardedRef: ForwardedRef< any >
+) {
 	// Use internal state instead of a ref to make sure that the component
 	// re-renders when the popover's anchor updates.
 	const [ fallbackPopoverAnchor, setFallbackPopoverAnchor ] =
-		useState( null );
-	const containerRef = useRef();
+		useState< HTMLDivElement | null >( null );
+	const containerRef = useRef< HTMLDivElement | null >( null );
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/45787/commits/82bbf50f6e0f54353a58b69fc8bae3343b92c29b

### Review thread 14

#### mirka on 2022-12-20T13:08:55Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053298772
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,17 +26,24 @@ export default {
 		renderContent: { control: { type: null } },
```

We can remove these two now that the types are coming automatically from TypeScript:

```diff
	argTypes: {
-		expandOnMobile: { control: { type: 'boolean' } },
		focusOnMount: {
			control: {
				type: 'radio',
				options: [ 'firstElement', true, false ],
			},
		},
-		headerTitle: { control: { type: 'text' } },
		renderContent: { control: { type: null } },
```

#### kienstra on 2022-12-20T19:21:40Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053669167
Path: packages/components/src/dropdown/stories/index.tsx

```diff
@@ -21,17 +26,24 @@ export default {
 		renderContent: { control: { type: null } },
```

Sounds good, committed in https://github.com/WordPress/gutenberg/pull/45787/commits/0ebe8ced30679ab12c32127d5c6c31b8f5a74299

### Review thread 15

#### mirka on 2022-12-20T13:54:22Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053342236
Path: packages/components/src/color-palette/types.ts

```diff
@@ -37,15 +37,8 @@ export type MultiplePalettesProps = PaletteProps & {
 	colors: PaletteObject[];
 };
 
-// TODO: should extend `Dropdown`'s props once it gets refactored to TypeScript
```

🎉

### Review thread 16 (resolved)

#### mirka on 2022-12-20T14:13:51Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053361573
Path: packages/components/src/dropdown/README.md

```diff
@@ -32,96 +32,85 @@ const MyDropdown = () => (
 
 The component accepts the following props. Props not included in this set will be applied to the element wrapping Popover content.
 
-### className
+### `className`: `string`
 
 className of the global container
 
--   Type: `String`
 -   Required: No
 
-### contentClassName
+### `contentClassName`: `string`
 
 If you want to target the dropdown menu for styling purposes, you need to provide a contentClassName because it's not being rendered as a child of the container node.
 
--   Type: `String`
 -   Required: No
 
-### position
+### `expandOnMobile`: `boolean`
 
-The direction in which the popover should open relative to its parent node. Specify a y- and an x-axis as a space-separated string. Supports `"top"`, `"bottom"` y-axis, and `"left"`, `"center"`, `"right"` x-axis.
+Opt-in prop to show popovers fullscreen on mobile.
 
--   Type: `String`
 -   Required: No
--   Default: `"top center"`
-
-### renderToggle
-
-A callback invoked to render the Dropdown Toggle Button.
-
--   Type: `Function`
--   Required: Yes
+-   Default: `false`
 
-The first argument of the callback is an object containing the following properties:
+### `focusOnMount`: `'firstElement' | boolean`
 
--   `isOpen`: whether the dropdown menu is opened or not
--   `onToggle`: A function switching the dropdown menu's state from open to closed and vice versa
--   `onClose`: A function that closes the menu if invoked
+By default, the _first tabbable element_ in the popover will receive focus when it mounts. This is the same as setting this prop to `"firstElement"`.
 
-### renderContent
+Specifying a `true` value will focus the container instead.
 
-A callback invoked to render the content of the dropdown menu. Its first argument is the same as the `renderToggle` prop.
+Specifying a `false` value disables the focus handling entirely (this should only be done when an appropriately accessible substitute behavior exists).
 
--   Type: `Function`
--   Required: Yes
+-   Required: No
+-   Default: `"firstElement"`
 
-### expandOnMobile
+### `headerTitle`: `string`
 
-Opt-in prop to show popovers fullscreen on mobile.
+Set this to customize the text that is shown in the dropdown's header when it is fullscreen on mobile.
 
--   Type: `Boolean`
 -   Required: No
--   Default: `false`
 
-### headerTitle
+### `onClose`: `() => void`
 
-Set this to customize the text that is shown in the dropdown's header when it is fullscreen on mobile.
+A callback invoked when the popover should be closed.
 
--   Type: `String`
 -   Required: No
 
-### focusOnMount
+### `onToggle`: `( willOpen: boolean ) => void`
 
-By default, the _first tabbable element_ in the popover will receive focus when it mounts. This is the same as setting this prop to `"firstElement"`.
-
-Specifying a `true` value will focus the container instead.
+A callback invoked when the state of the popover changes from open to closed and vice versa.
 
-Specifying a `false` value disables the focus handling entirely (this should only be done when an appropriately accessible substitute behavior exists).
+The callback receives a boolean as a parameter. If `true`, the popover will open. If `false`, the popover will close.
 
--   Type: `'firstElement' | boolean`
 -   Required: No
--   Default: `"firstElement"`
 
-### popoverProps
+### `popoverProps`: `WordPressComponentProps< Omit< PopoverProps, 'children' > 'div', false	>`
 
 Properties of popoverProps object will be passed as props to the `Popover` component.
 
 Use this object to access properties/features of the `Popover` component that are not already exposed in the `Dropdown` component, e.g.: the ability to have the popover without an arrow.
 
--   Type: `Object`
 -   Required: No
 
-### onClose
+### `position`: `PopoverProps[ 'position' ]`
 
-A callback invoked when the popover should be closed.
+The direction in which the popover should open relative to its parent node. Specify a y- and an x-axis as a space-separated string. Supports `"top"`, `"bottom"` y-axis, and `"left"`, `"center"`, `"right"` x-axis.
 
--   Type: `Function`
 -   Required: No
+-   Default: `"top center"`
 
-### onToggle
+### `renderContent`: `( props: CallbackProps ) => ReactNode`
 
-A callback invoked when the state of the popover changes from open to closed and vice versa.
+A callback invoked to render the content of the dropdown menu.
 
-The callback receives a boolean as a parameter. If `true`, the popover will open. If `false`, the popover will close.
+`isOpen`: whether the dropdown menu is opened or not
+`onToggle`: A function switching the dropdown menu's state from open to closed and vice versa
+`onClose`: A function that closes the menu if invoked
```

```suggestion
- `isOpen`: whether the dropdown menu is opened or not
- `onToggle`: A function switching the dropdown menu's state from open to closed and vice versa
- `onClose`: A function that closes the menu if invoked
```

Looks like the markdown list bullets got lost here.

#### kienstra on 2022-12-20T19:36:15Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053679697
Path: packages/components/src/dropdown/README.md

```diff
@@ -32,96 +32,85 @@ const MyDropdown = () => (
 
 The component accepts the following props. Props not included in this set will be applied to the element wrapping Popover content.
 
-### className
+### `className`: `string`
 
 className of the global container
 
--   Type: `String`
 -   Required: No
 
-### contentClassName
+### `contentClassName`: `string`
 
 If you want to target the dropdown menu for styling purposes, you need to provide a contentClassName because it's not being rendered as a child of the container node.
 
--   Type: `String`
 -   Required: No
 
-### position
+### `expandOnMobile`: `boolean`
 
-The direction in which the popover should open relative to its parent node. Specify a y- and an x-axis as a space-separated string. Supports `"top"`, `"bottom"` y-axis, and `"left"`, `"center"`, `"right"` x-axis.
+Opt-in prop to show popovers fullscreen on mobile.
 
--   Type: `String`
 -   Required: No
--   Default: `"top center"`
-
-### renderToggle
-
-A callback invoked to render the Dropdown Toggle Button.
-
--   Type: `Function`
--   Required: Yes
+-   Default: `false`
 
-The first argument of the callback is an object containing the following properties:
+### `focusOnMount`: `'firstElement' | boolean`
 
--   `isOpen`: whether the dropdown menu is opened or not
--   `onToggle`: A function switching the dropdown menu's state from open to closed and vice versa
--   `onClose`: A function that closes the menu if invoked
+By default, the _first tabbable element_ in the popover will receive focus when it mounts. This is the same as setting this prop to `"firstElement"`.
 
-### renderContent
+Specifying a `true` value will focus the container instead.
 
-A callback invoked to render the content of the dropdown menu. Its first argument is the same as the `renderToggle` prop.
+Specifying a `false` value disables the focus handling entirely (this should only be done when an appropriately accessible substitute behavior exists).
 
--   Type: `Function`
--   Required: Yes
+-   Required: No
+-   Default: `"firstElement"`
 
-### expandOnMobile
+### `headerTitle`: `string`
 
-Opt-in prop to show popovers fullscreen on mobile.
+Set this to customize the text that is shown in the dropdown's header when it is fullscreen on mobile.
 
--   Type: `Boolean`
 -   Required: No
--   Default: `false`
 
-### headerTitle
+### `onClose`: `() => void`
 
-Set this to customize the text that is shown in the dropdown's header when it is fullscreen on mobile.
+A callback invoked when the popover should be closed.
 
--   Type: `String`
 -   Required: No
 
-### focusOnMount
+### `onToggle`: `( willOpen: boolean ) => void`
 
-By default, the _first tabbable element_ in the popover will receive focus when it mounts. This is the same as setting this prop to `"firstElement"`.
-
-Specifying a `true` value will focus the container instead.
+A callback invoked when the state of the popover changes from open to closed and vice versa.
 
-Specifying a `false` value disables the focus handling entirely (this should only be done when an appropriately accessible substitute behavior exists).
+The callback receives a boolean as a parameter. If `true`, the popover will open. If `false`, the popover will close.
 
--   Type: `'firstElement' | boolean`
 -   Required: No
--   Default: `"firstElement"`
 
-### popoverProps
+### `popoverProps`: `WordPressComponentProps< Omit< PopoverProps, 'children' > 'div', false	>`
 
 Properties of popoverProps object will be passed as props to the `Popover` component.
 
 Use this object to access properties/features of the `Popover` component that are not already exposed in the `Dropdown` component, e.g.: the ability to have the popover without an arrow.
 
--   Type: `Object`
 -   Required: No
 
-### onClose
+### `position`: `PopoverProps[ 'position' ]`
 
-A callback invoked when the popover should be closed.
+The direction in which the popover should open relative to its parent node. Specify a y- and an x-axis as a space-separated string. Supports `"top"`, `"bottom"` y-axis, and `"left"`, `"center"`, `"right"` x-axis.
 
--   Type: `Function`
 -   Required: No
+-   Default: `"top center"`
 
-### onToggle
+### `renderContent`: `( props: CallbackProps ) => ReactNode`
 
-A callback invoked when the state of the popover changes from open to closed and vice versa.
+A callback invoked to render the content of the dropdown menu.
 
-The callback receives a boolean as a parameter. If `true`, the popover will open. If `false`, the popover will close.
+`isOpen`: whether the dropdown menu is opened or not
+`onToggle`: A function switching the dropdown menu's state from open to closed and vice versa
+`onClose`: A function that closes the menu if invoked
```

Good catch. Committed in https://github.com/WordPress/gutenberg/pull/45787/commits/94640d31c21a1418154fb2db58e3be5765c6baac

### Review thread 17 (resolved)

#### mirka on 2022-12-20T14:14:58Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053362753
Path: packages/components/src/dropdown/types.ts

```diff
@@ -6,3 +23,86 @@ export type DropdownContentWrapperProps = {
 	 */
 	paddingSize?: 'none' | 'small' | 'medium';
 };
+
+export type DropdownProps = {
+	/**
+	 * If you want to target the dropdown menu for styling purposes,
+	 * you need to provide a contentClassName because it's not being rendered
+	 * as a child of the container node.
+	 */
+	contentClassName?: string;
+	/**
+	 * Opt-in prop to show popovers fullscreen on mobile.
+	 *
+	 * @default false
+	 */
+	expandOnMobile?: boolean;
+	/**
+	 * By default, the first tabbable element in the popover will receive focus
+	 * when it mounts. This is the same as setting this prop to "firstElement".
+	 * Specifying a true value will focus the container instead.
+	 * Specifying a false value disables the focus handling entirely
+	 * (this should only be done when an appropriately accessible
+	 * substitute behavior exists).
+	 *
+	 * @default 'firstElement'
+	 */
+	focusOnMount?: 'firstElement' | boolean;
+	/**
+	 * Set this to customize the text that is shown in the dropdown's header
+	 * when it is fullscreen on mobile.
+	 */
+	headerTitle?: string;
+	/**
+	 * A callback invoked when the popover should be closed.
+	 */
+	onClose?: () => void;
+	/**
+	 * A callback invoked when the state of the popover changes
+	 * from open to closed and vice versa.
+	 * The callback receives a boolean as a parameter.
+	 * If true, the popover will open.
+	 * If false, the popover will close.
+	 */
+	onToggle?: ( willOpen: boolean ) => void;
+	/**
+	 * Properties of popoverProps object will be passed as props
+	 * to the Popover component.
+	 * Use this object to access properties/features
+	 * of the Popover component that are not already exposed
+	 * in the Dropdown component,
+	 * e.g.: the ability to have the popover without an arrow.
+	 */
+	popoverProps?: WordPressComponentProps<
+		Omit< PopoverProps, 'children' >,
+		'div',
+		false
+	>;
+	/**
+	 * The direction in which the popover should open
+	 * relative to its parent node.
+	 * Specify a y- and an x-axis as a space-separated string.
+	 * Supports "top", "bottom" y-axis,
+	 * and "left", "center", "right" x-axis.
+	 *
+	 * @default 'top center'
+	 */
+	position?: PopoverProps[ 'position' ];
+	/**
+	 * A callback invoked to render the content of the dropdown menu.
+	 * Its first argument is the same as the renderToggle prop.
+	 */
+	renderContent: ( props: CallbackProps ) => ReactNode;
+	/**
+	 * A callback invoked to render the Dropdown Toggle Button.
+	 *
+	 * The first argument of the callback is an object
+	 * containing the following properties:
+	 *
+	 * isOpen: whether the dropdown menu is opened or not
+	 * onToggle: A function switching the dropdown menu's state
+	 * from open to closed and vice versa
+	 * onClose: A function that closes the menu if invoked
```

```suggestion
	 * - isOpen: whether the dropdown menu is opened or not
	 * - onToggle: A function switching the dropdown menu's state
	 * from open to closed and vice versa
	 * - onClose: A function that closes the menu if invoked
```

Let's keep the markdown list bullets here as well.

#### kienstra on 2022-12-20T19:25:46Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053672158
Path: packages/components/src/dropdown/types.ts

```diff
@@ -6,3 +23,86 @@ export type DropdownContentWrapperProps = {
 	 */
 	paddingSize?: 'none' | 'small' | 'medium';
 };
+
+export type DropdownProps = {
+	/**
+	 * If you want to target the dropdown menu for styling purposes,
+	 * you need to provide a contentClassName because it's not being rendered
+	 * as a child of the container node.
+	 */
+	contentClassName?: string;
+	/**
+	 * Opt-in prop to show popovers fullscreen on mobile.
+	 *
+	 * @default false
+	 */
+	expandOnMobile?: boolean;
+	/**
+	 * By default, the first tabbable element in the popover will receive focus
+	 * when it mounts. This is the same as setting this prop to "firstElement".
+	 * Specifying a true value will focus the container instead.
+	 * Specifying a false value disables the focus handling entirely
+	 * (this should only be done when an appropriately accessible
+	 * substitute behavior exists).
+	 *
+	 * @default 'firstElement'
+	 */
+	focusOnMount?: 'firstElement' | boolean;
+	/**
+	 * Set this to customize the text that is shown in the dropdown's header
+	 * when it is fullscreen on mobile.
+	 */
+	headerTitle?: string;
+	/**
+	 * A callback invoked when the popover should be closed.
+	 */
+	onClose?: () => void;
+	/**
+	 * A callback invoked when the state of the popover changes
+	 * from open to closed and vice versa.
+	 * The callback receives a boolean as a parameter.
+	 * If true, the popover will open.
+	 * If false, the popover will close.
+	 */
+	onToggle?: ( willOpen: boolean ) => void;
+	/**
+	 * Properties of popoverProps object will be passed as props
+	 * to the Popover component.
+	 * Use this object to access properties/features
+	 * of the Popover component that are not already exposed
+	 * in the Dropdown component,
+	 * e.g.: the ability to have the popover without an arrow.
+	 */
+	popoverProps?: WordPressComponentProps<
+		Omit< PopoverProps, 'children' >,
+		'div',
+		false
+	>;
+	/**
+	 * The direction in which the popover should open
+	 * relative to its parent node.
+	 * Specify a y- and an x-axis as a space-separated string.
+	 * Supports "top", "bottom" y-axis,
+	 * and "left", "center", "right" x-axis.
+	 *
+	 * @default 'top center'
+	 */
+	position?: PopoverProps[ 'position' ];
+	/**
+	 * A callback invoked to render the content of the dropdown menu.
+	 * Its first argument is the same as the renderToggle prop.
+	 */
+	renderContent: ( props: CallbackProps ) => ReactNode;
+	/**
+	 * A callback invoked to render the Dropdown Toggle Button.
+	 *
+	 * The first argument of the callback is an object
+	 * containing the following properties:
+	 *
+	 * isOpen: whether the dropdown menu is opened or not
+	 * onToggle: A function switching the dropdown menu's state
+	 * from open to closed and vice versa
+	 * onClose: A function that closes the menu if invoked
```

Sure, committed in https://github.com/WordPress/gutenberg/pull/45787/commits/33c67154869b08a170d6d78f8c4a55b2e3ed5b78

### Review thread 18 (resolved)

#### mirka on 2022-12-20T14:19:48Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053367652
Path: packages/components/src/dropdown/types.ts

```diff
@@ -6,3 +23,86 @@ export type DropdownContentWrapperProps = {
 	 */
 	paddingSize?: 'none' | 'small' | 'medium';
 };
+
+export type DropdownProps = {
+	/**
+	 * If you want to target the dropdown menu for styling purposes,
+	 * you need to provide a contentClassName because it's not being rendered
+	 * as a child of the container node.
+	 */
+	contentClassName?: string;
+	/**
+	 * Opt-in prop to show popovers fullscreen on mobile.
+	 *
+	 * @default false
+	 */
+	expandOnMobile?: boolean;
+	/**
+	 * By default, the first tabbable element in the popover will receive focus
+	 * when it mounts. This is the same as setting this prop to "firstElement".
+	 * Specifying a true value will focus the container instead.
+	 * Specifying a false value disables the focus handling entirely
+	 * (this should only be done when an appropriately accessible
+	 * substitute behavior exists).
+	 *
+	 * @default 'firstElement'
+	 */
+	focusOnMount?: 'firstElement' | boolean;
+	/**
+	 * Set this to customize the text that is shown in the dropdown's header
+	 * when it is fullscreen on mobile.
+	 */
+	headerTitle?: string;
+	/**
+	 * A callback invoked when the popover should be closed.
+	 */
+	onClose?: () => void;
+	/**
+	 * A callback invoked when the state of the popover changes
+	 * from open to closed and vice versa.
+	 * The callback receives a boolean as a parameter.
+	 * If true, the popover will open.
+	 * If false, the popover will close.
+	 */
+	onToggle?: ( willOpen: boolean ) => void;
+	/**
+	 * Properties of popoverProps object will be passed as props
+	 * to the Popover component.
+	 * Use this object to access properties/features
+	 * of the Popover component that are not already exposed
+	 * in the Dropdown component,
+	 * e.g.: the ability to have the popover without an arrow.
+	 */
+	popoverProps?: WordPressComponentProps<
+		Omit< PopoverProps, 'children' >,
+		'div',
+		false
+	>;
```

For this one, I'm thinking it would be better to do it this way:

```suggestion
	popoverProps?: Omit<
		ComponentPropsWithoutRef< typeof Popover >,
		'children'
	>;
```

The reason is so we can avoid duplicating the `WordPressComponentProps< ..., 'div', false >` part of it which is defined in Popover.

#### kienstra on 2022-12-20T19:33:43Z

URL: https://github.com/WordPress/gutenberg/pull/45787#discussion_r1053677855
Path: packages/components/src/dropdown/types.ts

```diff
@@ -6,3 +23,86 @@ export type DropdownContentWrapperProps = {
 	 */
 	paddingSize?: 'none' | 'small' | 'medium';
 };
+
+export type DropdownProps = {
+	/**
+	 * If you want to target the dropdown menu for styling purposes,
+	 * you need to provide a contentClassName because it's not being rendered
+	 * as a child of the container node.
+	 */
+	contentClassName?: string;
+	/**
+	 * Opt-in prop to show popovers fullscreen on mobile.
+	 *
+	 * @default false
+	 */
+	expandOnMobile?: boolean;
+	/**
+	 * By default, the first tabbable element in the popover will receive focus
+	 * when it mounts. This is the same as setting this prop to "firstElement".
+	 * Specifying a true value will focus the container instead.
+	 * Specifying a false value disables the focus handling entirely
+	 * (this should only be done when an appropriately accessible
+	 * substitute behavior exists).
+	 *
+	 * @default 'firstElement'
+	 */
+	focusOnMount?: 'firstElement' | boolean;
+	/**
+	 * Set this to customize the text that is shown in the dropdown's header
+	 * when it is fullscreen on mobile.
+	 */
+	headerTitle?: string;
+	/**
+	 * A callback invoked when the popover should be closed.
+	 */
+	onClose?: () => void;
+	/**
+	 * A callback invoked when the state of the popover changes
+	 * from open to closed and vice versa.
+	 * The callback receives a boolean as a parameter.
+	 * If true, the popover will open.
+	 * If false, the popover will close.
+	 */
+	onToggle?: ( willOpen: boolean ) => void;
+	/**
+	 * Properties of popoverProps object will be passed as props
+	 * to the Popover component.
+	 * Use this object to access properties/features
+	 * of the Popover component that are not already exposed
+	 * in the Dropdown component,
+	 * e.g.: the ability to have the popover without an arrow.
+	 */
+	popoverProps?: WordPressComponentProps<
+		Omit< PopoverProps, 'children' >,
+		'div',
+		false
+	>;
```

Good idea to do `typeof Popover`, much more DRY.

Committed in https://github.com/WordPress/gutenberg/compare/33c67154869b08a170d6d78f8c4a55b2e3ed5b78...e693603136d8ebd61f2ab23b4b5bfe6c100d0ddc

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/border-control/border-control-dropdown/component.tsx (MODIFIED, +7/-4)
- packages/components/src/color-palette/index.tsx (MODIFIED, +2/-1)
- packages/components/src/color-palette/types.ts (MODIFIED, +3/-10)
- packages/components/src/dropdown/README.md (MODIFIED, +41/-46)
- packages/components/src/dropdown/index.tsx (RENAMED, +57/-13)
- packages/components/src/dropdown/stories/index.tsx (RENAMED, +21/-8)
- packages/components/src/dropdown/test/index.tsx (RENAMED, +7/-9)
- packages/components/src/dropdown/types.ts (MODIFIED, +107/-0)
