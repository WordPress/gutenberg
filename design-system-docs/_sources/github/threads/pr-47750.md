---
number: 47750
type: pull_request
state: MERGED
merged_at: 2023-03-14T10:47:22Z
url: https://github.com/WordPress/gutenberg/pull/47750
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components","First-time Contributor"]
---

# PR #47750: Refactor/toolbar button component to typescript

- URL: https://github.com/WordPress/gutenberg/pull/47750
- Author: mike-day
- Created: 2023-02-03T22:08:29Z
- Updated: 2023-03-14T14:03:36Z
- Comments: 7 of 7
- Review threads: 5 of 5
- Changed files: 6 of 6
- Diff: ../pr-diffs/47750.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

Part of #35744.

_**To be merged after https://github.com/WordPress/gutenberg/pull/47087.**_

## What?
<!-- In a few words, what is the PR actually doing? -->
Convert the `ToolbarButton` component to TypeScript.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
This PR is needed as part of an effort to convert `Toolbar` and related `Toolbar` subcomponents to TypeScript.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
By adding types to `ToolbarButton` and `ToolbarButtonContainer`.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
1. Create or otherwise open a post
2. Add a block that would have some toolbar controls (e.g. `Image`)
3. Expected: Toolbar buttons should fundamentally work the same as before:

## Issue comments

### github-actions on 2023-02-03T22:08:58Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1416465603

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @mike-day! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/), where we hold [regularly weekly meetings](https://make.wordpress.org/core/tag/core-editor-summary/) open to anyone to coordinate with each other.

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### mike-day on 2023-02-03T22:11:40Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1416468687

@ciampo @mirka here is some initial work for `ToolbarButton`, intended to be merged after the [refactoring of `Toolbar` to TypeScript](https://github.com/WordPress/gutenberg/pull/47087) is merged.

### mike-day on 2023-02-03T23:50:07Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1416537457

Note that commit and file change history will be much smaller before this is merged — https://github.com/WordPress/gutenberg/pull/47087 was pulled in to avoid duplicate work.

### ciampo on 2023-02-07T17:27:41Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1421158760

Hey @mike-day , we haven't taken a look at this PR as it's still marked as a draft — just let us know when and we'll take a look!

### mike-day on 2023-02-07T18:15:42Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1421240900

> Hey @mike-day , we haven't taken a look at this PR as it's still marked as a draft — just let us know when and we'll take a look!

Checks are running from a fresh pull of `trunk` and resolving of `CHANGELOG`, but this should be ready for review, @ciampo!

I opted to **_not_** rebase and leave commit history alone, but just let me know if I should clean that up (pulling in [47087](https://github.com/WordPress/gutenberg/pull/47087) in the beginning of this work made commit history look more extensive than it really is).

### ciampo on 2023-03-02T10:39:49Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1451656170

Hey @mike-day , do you still have capacity to work on this PR?

### mike-day on 2023-03-02T23:02:18Z

URL: https://github.com/WordPress/gutenberg/pull/47750#issuecomment-1452690599

> Hey @mike-day , do you still have capacity to work on this PR?

Yes, apologies for the radio silence! I plan to jump back on this and work through comments tomorrow.

## Review threads

### Review thread 1

#### mike-day on 2023-02-07T18:00:45Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1099019088
Path: packages/components/src/toolbar/toolbar-button/types.ts

```diff
@@ -0,0 +1,42 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+export type ToolbarButtonProps = {
+	/**
+	 * Children to be rendered inside the button.
+	 */
+	children?: ReactNode;
+	/**
+	 * An optional class name for the button container.
+	 */
+	containerClassName?: string;
+	/**
+	 * Additional props to be passed alongside props.
+	 */
+	extraProps?: any;
```

I am not sure about the purpose of `extraProps` — both `props` and `extraProps` are spread into `Button`.

#### mirka on 2023-02-08T17:38:23Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1100476960
Path: packages/components/src/toolbar/toolbar-button/types.ts

```diff
@@ -0,0 +1,42 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+export type ToolbarButtonProps = {
+	/**
+	 * Children to be rendered inside the button.
+	 */
+	children?: ReactNode;
+	/**
+	 * An optional class name for the button container.
+	 */
+	containerClassName?: string;
+	/**
+	 * Additional props to be passed alongside props.
+	 */
+	extraProps?: any;
```

Agreed. The props passing seems fishy overall, especially with how `props.foo` is used for some things. I have to guess that this code has degraded over time.

I'm also not entirely convinced that we should be accepting _all_ Button props, since some of them are probably not appropriate in this specific toolbar context. But perhaps we can revisit this later when there is a more pressing need to improve these typings and APIs.

For posterity, I'll also note that I'm fine with resorting to an `any` type here for the time being. Ideally it should be some variation of `ButtonProps`, but that forces us to deal with the `button`/`anchor` polymorphism to an extent that I'd rather do it as a separate issue. I kind of feel like we should only allow "Button as a `<button>`" inside toolbars, but even that decision requires some more investigation and effort!

#### ciampo on 2023-02-09T10:45:50Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1101277201
Path: packages/components/src/toolbar/toolbar-button/types.ts

```diff
@@ -0,0 +1,42 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+export type ToolbarButtonProps = {
+	/**
+	 * Children to be rendered inside the button.
+	 */
+	children?: ReactNode;
+	/**
+	 * An optional class name for the button container.
+	 */
+	containerClassName?: string;
+	/**
+	 * Additional props to be passed alongside props.
+	 */
+	extraProps?: any;
```

This actually relates well to the work that @chad1008 is doing on `CircularOptionPicker`'s TypeScript refactor. For now, we've decided to use `ButtonAsButtonProps` (so that we can avoid the `anchor` case), but I'd definitely be interested in your opinion once the PR is ready for review

#### mike-day on 2023-03-09T22:19:53Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1131679606
Path: packages/components/src/toolbar/toolbar-button/types.ts

```diff
@@ -0,0 +1,42 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+export type ToolbarButtonProps = {
+	/**
+	 * Children to be rendered inside the button.
+	 */
+	children?: ReactNode;
+	/**
+	 * An optional class name for the button container.
+	 */
+	containerClassName?: string;
+	/**
+	 * Additional props to be passed alongside props.
+	 */
+	extraProps?: any;
```

@ciampo @mirka this has been left as `any` for now. Let me know if a different approach should be taken.

Also, I believe I have worked through the suggestions left prior. This should be ready for review again.

### Review thread 2 (resolved)

#### mirka on 2023-02-08T16:07:37Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1100359441
Path: packages/components/src/toolbar/toolbar-button/toolbar-button-container.tsx

```diff
@@ -0,0 +1,14 @@
+/**
+ * Internal dependencies
+ */
+import type { ToolbarButtonContainerProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
+
+const ToolbarButtonContainer = ( {
+	className,
+	children,
+}: WordPressComponentProps< ToolbarButtonContainerProps, 'div', false > ) => (
```

This component doesn't pass down the rest props to the `div`, so we'll need to explicitly add just `className` and `children` to `ToolbarButtonContainerProps`.

```suggestion
const ToolbarButtonContainer = ( {
	className,
	children,
}: ToolbarButtonContainerProps ) => (
```

#### mike-day on 2023-03-03T20:20:02Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1124968210
Path: packages/components/src/toolbar/toolbar-button/toolbar-button-container.tsx

```diff
@@ -0,0 +1,14 @@
+/**
+ * Internal dependencies
+ */
+import type { ToolbarButtonContainerProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
+
+const ToolbarButtonContainer = ( {
+	className,
+	children,
+}: WordPressComponentProps< ToolbarButtonContainerProps, 'div', false > ) => (
```

Done with [e7ae519](https://github.com/WordPress/gutenberg/pull/47750/commits/e7ae519baf9e6429fc15989588a6b5c612ca5eb3) and [d29a6bf](https://github.com/WordPress/gutenberg/pull/47750/commits/d29a6bf9973b147f2de214804fadb416033b478e).

### Review thread 3 (resolved)

#### mirka on 2023-02-08T16:17:09Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1100373163
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -4,30 +4,36 @@
  * External dependencies
```

We need to remove the `ts-nocheck` at the top of this file.

#### mike-day on 2023-03-03T20:22:22Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1124970142
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -4,30 +4,36 @@
  * External dependencies
```

Whoops, I missed that one! Done with [54459c3](https://github.com/WordPress/gutenberg/pull/47750/commits/54459c381dd065785cdf3ec5d381feafee9722f9).

### Review thread 4 (resolved)

#### mirka on 2023-02-08T16:57:01Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1100423325
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -40,7 +46,12 @@ function ToolbarButton(
 					label={ title }
 					shortcut={ props.shortcut }
 					data-subscript={ props.subscript }
-					onClick={ ( event ) => {
+					onClick={ (
+						event: React.MouseEvent<
+							HTMLButtonElement & HTMLAnchorElement,
+							MouseEvent
+						>
+					) => {
 						event.stopPropagation();
 						if ( props.onClick ) {
 							props.onClick( event );
```

Hmm, using `props.onClick` here instead of a destructured `onClick` means that this onClick prop will just be overwritten when the rest props are spread back in 😅 This isn't directly related to the changes in this PR, but could you add a TODO comment here to flag that it's likely a bug?

#### mike-day on 2023-03-09T22:17:27Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1131677390
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -40,7 +46,12 @@ function ToolbarButton(
 					label={ title }
 					shortcut={ props.shortcut }
 					data-subscript={ props.subscript }
-					onClick={ ( event ) => {
+					onClick={ (
+						event: React.MouseEvent<
+							HTMLButtonElement & HTMLAnchorElement,
+							MouseEvent
+						>
+					) => {
 						event.stopPropagation();
 						if ( props.onClick ) {
 							props.onClick( event );
```

TODO added in https://github.com/WordPress/gutenberg/pull/47750/commits/4035a8174d3ea645e1c79c5b40dfa74f432ffea8.

### Review thread 5

#### ciampo on 2023-03-14T10:16:02Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1135310717
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -1,33 +1,37 @@
-// @ts-nocheck
-
 /**
  * External dependencies
  */
 import classnames from 'classnames';
+import type { ForwardedRef } from 'react';
+
 /**
  * WordPress dependencies
  */
 import { useContext, forwardRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import Button from '../../button';
 import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
 import ToolbarButtonContainer from './toolbar-button-container';
+import type { ToolbarButtonProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
+import type React from 'react';
 
-function ToolbarButton(
+function UnforwardedToolbarButton(
 	{
-		containerClassName,
+		children,
 		className,
+		containerClassName,
 		extraProps,
-		children,
-		title,
 		isActive,
 		isDisabled,
+		title,
 		...props
-	},
-	ref
+	}: WordPressComponentProps< ToolbarButtonProps, typeof Button, false >,
```

I believe this is the first time that I see us using props from another React element (`Button`) as the second argument for the `WordPressComponentProps` — @mirka do you see any potential issue with this?

#### mirka on 2023-03-14T13:02:04Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1135507330
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -1,33 +1,37 @@
-// @ts-nocheck
-
 /**
  * External dependencies
  */
 import classnames from 'classnames';
+import type { ForwardedRef } from 'react';
+
 /**
  * WordPress dependencies
  */
 import { useContext, forwardRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import Button from '../../button';
 import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
 import ToolbarButtonContainer from './toolbar-button-container';
+import type { ToolbarButtonProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
+import type React from 'react';
 
-function ToolbarButton(
+function UnforwardedToolbarButton(
 	{
-		containerClassName,
+		children,
 		className,
+		containerClassName,
 		extraProps,
-		children,
-		title,
 		isActive,
 		isDisabled,
+		title,
 		...props
-	},
-	ref
+	}: WordPressComponentProps< ToolbarButtonProps, typeof Button, false >,
```

Good question. Considering this from several angles:

- Can Emotion handle polymorphism like this? — Yes. The Emotion `as` prop allows `React.ElementType`, so it _can_ potentially handle `as={ Button }` if this was a polymorphic WordPressComponent.
- Is it a good idea to allow something like `as={ Button }`? — In general, I don't like it because it's hard to predict what will happen. But it may make sense in some abstract components.
- Should we prefer `ToolbarButtonProps & ButtonProps` syntax in cases like this? — No strong opinion, given that there are no serious downsides to the `WordPressComponentProps` syntax. I like how we can just do `typeof Button` without importing the `ButtonProps` type though.

#### ciampo on 2023-03-14T14:03:28Z

URL: https://github.com/WordPress/gutenberg/pull/47750#discussion_r1135600794
Path: packages/components/src/toolbar/toolbar-button/index.tsx

```diff
@@ -1,33 +1,37 @@
-// @ts-nocheck
-
 /**
  * External dependencies
  */
 import classnames from 'classnames';
+import type { ForwardedRef } from 'react';
+
 /**
  * WordPress dependencies
  */
 import { useContext, forwardRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import Button from '../../button';
 import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
 import ToolbarButtonContainer from './toolbar-button-container';
+import type { ToolbarButtonProps } from './types';
+import type { WordPressComponentProps } from '../../ui/context';
+import type React from 'react';
 
-function ToolbarButton(
+function UnforwardedToolbarButton(
 	{
-		containerClassName,
+		children,
 		className,
+		containerClassName,
 		extraProps,
-		children,
-		title,
 		isActive,
 		isDisabled,
+		title,
 		...props
-	},
-	ref
+	}: WordPressComponentProps< ToolbarButtonProps, typeof Button, false >,
```

Good observations. Let's keep as-is, keep an eye for any unintended consequences, and otherwise consider it as another tool in our belt

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/toolbar/toolbar-button/README.md (MODIFIED, +13/-1)
- packages/components/src/toolbar/toolbar-button/index.tsx (RENAMED, +55/-20)
- packages/components/src/toolbar/toolbar-button/toolbar-button-container.js (DELETED, +0/-6)
- packages/components/src/toolbar/toolbar-button/toolbar-button-container.tsx (ADDED, +13/-0)
- packages/components/src/toolbar/toolbar-button/types.ts (ADDED, +46/-0)
