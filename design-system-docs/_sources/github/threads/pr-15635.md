---
number: 15635
type: pull_request
state: MERGED
merged_at: 2019-06-05T15:56:08Z
url: https://github.com/WordPress/gutenberg/pull/15635
matched_labels: ["[Package] Components"]
all_labels: ["[Priority] High","[Type] Code Quality","npm Packages","[Package] Components","[Feature] Widgets Screen"]
---

# PR #15635: Extract ServerSideRender component to an independent package

- URL: https://github.com/WordPress/gutenberg/pull/15635
- Author: jorgefilipecosta
- Created: 2019-05-14T18:01:20Z
- Updated: 2020-03-23T14:31:07Z
- Comments: 3 of 3
- Review threads: 18 of 18
- Changed files: 24 of 24
- Diff: ../pr-diffs/15635.diff

## Body

## Description
ServerSideRender component was part of WordPress component. I think ServerSideRender is WordPress specific. In WordPress components, we try to keep artifacts that are generic and don't rely on WordPress API's like the rest endpoints.

In wordpress/editor we exposed a similar component that uses the WordPress components version and adds the post id as a property to be passed to the server.
Blocks that used ServerSideRender should have the wordpress/editor version because without the post id blocks, may not render as expected for that post.

WordPress/editor will not be available on the widgets screen and blocks should not rely on WordPress/editor.
This PR extracts ServerSideRender to an independent package. The extracted component checks if it is possible to query the post id from the editor store, if yes the post id is passed to the server, if not the post id is not passed to the server.

We are back-compatible with usages of wp.editor.ServerSideRender.
We are not back-compatible with usages of wp.components.ServerSideRender. To be back-compatible we would create circular reference: wp.components would depend wp.serverSideRender and wp.serverSideRender would depend on wp.components

## How has this been tested?
I checked blocks using server-side render( Archives, Calendar, Lastest comments, Legacy Widgets ) still work as expected.

## Issue comments

### jorgefilipecosta on 2019-05-24T17:52:38Z

URL: https://github.com/WordPress/gutenberg/pull/15635#issuecomment-495730450

I added an inline script so we can keep back-compatibility with wp.components.ServerSideRender calls.

### jorgefilipecosta on 2019-06-03T17:10:07Z

URL: https://github.com/WordPress/gutenberg/pull/15635#issuecomment-498344194

I added a high priority label as this is a blocker to having legacy widgets on the widgets screen.

### jorgefilipecosta on 2019-06-05T15:57:20Z

URL: https://github.com/WordPress/gutenberg/pull/15635#issuecomment-499145451

Thank you for the reviews @gziolo, I think we should follow the ServerSideRenderProvider approach, and I will gladly submit that change in a follow-up PR. @youknowriad do you have any concerns related to that approach?

## Review threads

### Review thread 1 (resolved)

#### gziolo on 2019-05-15T14:42:59Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r284293034
Path: packages/server-side-render/package.json

```diff
@@ -0,0 +1,36 @@
+{
+	"name": "@wordpress/server-side-render",
+	"version": "1.3.0",
```

Version is incorrect. It should be anything lower than `1.0.0`: `1.0.0-alpha.1`?

### Review thread 2 (resolved)

#### gziolo on 2019-05-15T14:44:02Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r284293608
Path: packages/server-side-render/README.md

```diff
@@ -43,7 +43,7 @@ E.g: `{ post_id: 12 }`.
 Render core/archives preview.
 
 ```jsx
-import { ServerSideRender } from '@wordpress/components';
+import { ServerSideRender } from '@wordpress/server-side-render';
```

Installation details are missing.

### Review thread 3 (resolved)

#### gziolo on 2019-05-15T14:45:27Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r284294380
Path: packages/components/src/index.js

```diff
@@ -51,7 +51,6 @@ export { default as ResponsiveWrapper } from './responsive-wrapper';
 export { default as SandBox } from './sandbox';
 export { default as SelectControl } from './select-control';
 export { default as Spinner } from './spinner';
-export { default as ServerSideRender } from './server-side-render';
```

I guess it should be still exposed in `wp.components` for backward compatibility reasons.

#### youknowriad on 2019-05-15T14:48:44Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r284295985
Path: packages/components/src/index.js

```diff
@@ -51,7 +51,6 @@ export { default as ResponsiveWrapper } from './responsive-wrapper';
 export { default as SandBox } from './sandbox';
 export { default as SelectControl } from './select-control';
 export { default as Spinner } from './spinner';
-export { default as ServerSideRender } from './server-side-render';
```

Maybe we can add this proxy as an inline script to avoid the need to keep the npm dependency forever.

### Review thread 4 (resolved)

#### gziolo on 2019-05-15T14:45:41Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r284294493
Path: packages/components/package.json

```diff
@@ -23,7 +23,6 @@
 	"dependencies": {
 		"@babel/runtime": "^7.4.4",
 		"@wordpress/a11y": "file:../a11y",
-		"@wordpress/api-fetch": "file:../api-fetch",
```

nice 👍

### Review thread 5 (resolved)

#### gziolo on 2019-05-15T14:46:29Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r284294879
Path: packages/components/CHANGELOG.md

```diff
@@ -8,6 +8,10 @@
 
 - Fixed display of reset button when using RangeControl `allowReset` prop.
 
+### Breaking Change
+
+- `ServerSideRender` is no longer part of components. It was extracted to an independent package `@wordpress/server-side-render`.
```

I don't think we can fully deprecate it in WordPress context. It's fine for npm but inside WP-Admin it still should be possible to use `wp.components. ServerSideRender`.

### Review thread 6 (resolved)

#### gziolo on 2019-05-27T06:45:31Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r287664233
Path: packages/server-side-render/README.md

```diff
@@ -8,6 +8,19 @@ ServerSideRender should be regarded as a fallback or legacy mechanism, it is not
 
 New blocks should be built in conjunction with any necessary REST API endpoints, so that JavaScript can be used for rendering client-side in the `edit` function. This gives the best user experience, instead of relying on using the PHP `render_callback`. The logic necessary for rendering should be included in the endpoint, so that both the client-side JavaScript and server-side PHP logic should require a minimal amount of differences.
 
+## Installation
+
+Install the module
+
+```bash
+npm install @wordpress/server-side-render --save
+```
+
+## Usage
+
+The props accepted by the component are described bellow.
```

Typo:

```suggestion
The props accepted by the component are described below.
```

### Review thread 7 (resolved)

#### gziolo on 2019-05-27T06:48:11Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r287664824
Path: packages/server-side-render/README.md

```diff
@@ -8,6 +8,19 @@ ServerSideRender should be regarded as a fallback or legacy mechanism, it is not
 
 New blocks should be built in conjunction with any necessary REST API endpoints, so that JavaScript can be used for rendering client-side in the `edit` function. This gives the best user experience, instead of relying on using the PHP `render_callback`. The logic necessary for rendering should be included in the endpoint, so that both the client-side JavaScript and server-side PHP logic should require a minimal amount of differences.
 
+## Installation
+
+Install the module
+
+```bash
+npm install @wordpress/server-side-render --save
+```
+
+## Usage
```

A note about Babel is missing:
```suggestion
_This package assumes that your code will run in an **ES2015+** environment. If you're using an environment that has limited or no support for ES2015+ such as lower versions of IE then using [core-js](https://github.com/zloirock/core-js) or [@babel/polyfill](https://babeljs.io/docs/en/next/babel-polyfill) will add support for these methods. Learn more about it in [Babel docs](https://babeljs.io/docs/en/next/caveats)._

## Usage
```

### Review thread 8

#### gziolo on 2019-05-27T06:56:06Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r287666698
Path: packages/server-side-render/src/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+import { withSelect } from '@wordpress/data';
+
+/**
+ * Internal dependencies
+ */
+import ServerSideRender from './server-side-render';
+
+/**
+ * Constants
+ */
+const EMPTY_OBJECT = {};
+
+export default withSelect(
+	( select ) => {
+		const coreEditorSelect = select( 'core/editor' );
+		if ( coreEditorSelect ) {
+			const currentPostId = coreEditorSelect.getCurrentPostId();
+			if ( currentPostId ) {
+				return {
+					currentPostId,
+				};
+			}
+		}
+		return EMPTY_OBJECT;
+	}
+)(
+	( { urlQueryArgs = EMPTY_OBJECT, currentPostId, ...props } ) => {
+		urlQueryArgs = useMemo( () => {
```

I don't think that props should be ever mutated. It might cause some unexpected behavior. In addition, it makes this code harder to follow.

#### jorgefilipecosta on 2019-05-27T12:54:42Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r287780068
Path: packages/server-side-render/src/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+import { withSelect } from '@wordpress/data';
+
+/**
+ * Internal dependencies
+ */
+import ServerSideRender from './server-side-render';
+
+/**
+ * Constants
+ */
+const EMPTY_OBJECT = {};
+
+export default withSelect(
+	( select ) => {
+		const coreEditorSelect = select( 'core/editor' );
+		if ( coreEditorSelect ) {
+			const currentPostId = coreEditorSelect.getCurrentPostId();
+			if ( currentPostId ) {
+				return {
+					currentPostId,
+				};
+			}
+		}
+		return EMPTY_OBJECT;
+	}
+)(
+	( { urlQueryArgs = EMPTY_OBJECT, currentPostId, ...props } ) => {
+		urlQueryArgs = useMemo( () => {
```

Hi @gziolo, I think we were not mutating the props. If we do urlQueryArgs = someObject; the props the component receives stay the same as we just updated the reference to point to another object, we would be mutation the props if we did urlQueryArgs.someKey = value; I may be missing something if that's the case, please correct me, so I avoid these problems in the future. That said, I agree that the code becomes more straightforward if we use another variable, so I updated the code.

### Review thread 9

#### gziolo on 2019-06-04T06:49:27Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290148353
Path: packages/server-side-render/package.json

```diff
@@ -0,0 +1,36 @@
+{
+	"name": "@wordpress/server-side-render",
+	"version": "1.0.0-alpha.1",
+	"description": "ServerSideRender is a component used for server-side rendering a preview of dynamic blocks to display in the editor.",
+	"author": "The WordPress Contributors",
+	"license": "GPL-2.0-or-later",
+	"keywords": [
+		"wordpress",
+		"server side",
+		"render"
+	],
+	"homepage": "https://github.com/WordPress/gutenberg/tree/master/packages/server-side-render/README.md",
+	"repository": {
+		"type": "git",
+		"url": "https://github.com/WordPress/gutenberg.git",
+		"directory": "packages/server-side-render"
+	},
+	"bugs": {
+		"url": "https://github.com/WordPress/gutenberg/issues"
+	},
+	"main": "build/index.js",
+	"module": "build-module/index.js",
+	"react-native": "src/index",
+	"dependencies": {
+		"@babel/runtime": "^7.4.4",
+		"@wordpress/components": "file:../components",
```

`@wordpress/api-fetch` is missing on the list of dependencies.

### Review thread 10

#### gziolo on 2019-06-04T07:16:34Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290156306
Path: packages/server-side-render/src/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+import { withSelect } from '@wordpress/data';
+
+/**
+ * Internal dependencies
+ */
+import ServerSideRender from './server-side-render';
+
+/**
+ * Constants
+ */
+const EMPTY_OBJECT = {};
+
+export default withSelect(
+	( select ) => {
+		const coreEditorSelect = select( 'core/editor' );
```

This part is a bit concerning in my opinion. This package doesn't depend on `@wordpress/editor` explicitly but it will use it when it happens to be loaded on the page. While it solves the original issue of removing the dependency of core blocks on `@wordpress/editor`, in fact, it only hides it.

@jorgefilipecosta and @youknowriad, have you considered feeding the block editor provider with some commonly used data which is specific to the page context? In this case, it would be the current post id. In the context of widget areas, it might be necessary to know the id of the widget area. Well, it might turn out that this is the only component which would take advantage of it, so it's all academic :) I definitely don't want to block this PR but I wanted to hear your opinions.

#### jorgefilipecosta on 2019-06-04T07:32:28Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290161381
Path: packages/server-side-render/src/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+import { withSelect } from '@wordpress/data';
+
+/**
+ * Internal dependencies
+ */
+import ServerSideRender from './server-side-render';
+
+/**
+ * Constants
+ */
+const EMPTY_OBJECT = {};
+
+export default withSelect(
+	( select ) => {
+		const coreEditorSelect = select( 'core/editor' );
```

Hi @gziolo, if we follow the context approach (feeding the block editor provider with some commonly used data which is specific to the page context), wouldn't the context consumer component need to be part of @wordpress/editor? In that case, we would even need to have a direct dependency on @wordpress/editor.

Based on your idea, I think a possible solution may be to put context as part of the ServerSideRender package and expose a component ServerSideRenderAdditionalDataProvider that allows to other components to pass additional props that are used in server-side render calls (the default is an empty object and no additional props are passed). The editor would use this component to pass the post id, later if the widgets also need to pass something specific it is possible to do the same.

#### gziolo on 2019-06-05T12:45:16Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290718605
Path: packages/server-side-render/src/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * WordPress dependencies
+ */
+import { useMemo } from '@wordpress/element';
+import { withSelect } from '@wordpress/data';
+
+/**
+ * Internal dependencies
+ */
+import ServerSideRender from './server-side-render';
+
+/**
+ * Constants
+ */
+const EMPTY_OBJECT = {};
+
+export default withSelect(
+	( select ) => {
+		const coreEditorSelect = select( 'core/editor' );
```

Yes, `ServerSideRenderProvider` would solve this issue as well. I don't think there is a perfect solution to this problem. All I can think of have some drawbacks. @youknowriad will know better what is the most anticipated one 😄

### Review thread 11 (resolved)

#### gziolo on 2019-06-04T07:18:21Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290156886
Path: packages/server-side-render/README.md

```diff
@@ -8,6 +8,21 @@ ServerSideRender should be regarded as a fallback or legacy mechanism, it is not
 
 New blocks should be built in conjunction with any necessary REST API endpoints, so that JavaScript can be used for rendering client-side in the `edit` function. This gives the best user experience, instead of relying on using the PHP `render_callback`. The logic necessary for rendering should be included in the endpoint, so that both the client-side JavaScript and server-side PHP logic should require a minimal amount of differences.
```

In oher packages which are tightly coupled to WordPress we add this note:
```suggestion
New blocks should be built in conjunction with any necessary REST API endpoints, so that JavaScript can be used for rendering client-side in the `edit` function. This gives the best user experience, instead of relying on using the PHP `render_callback`. The logic necessary for rendering should be included in the endpoint, so that both the client-side JavaScript and server-side PHP logic should require a minimal amount of differences.

> This package is meant to be used only with WordPress core. Feel free to use it in your own project but please keep in mind that it might never get fully documented.
```

### Review thread 12

#### gziolo on 2019-06-05T12:37:07Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290715419
Path: packages/components/CHANGELOG.md

```diff
@@ -15,6 +15,10 @@
 - Fixed display of reset button when using RangeControl `allowReset` prop.
 - Fixed minutes field of `DateTimePicker` missed '0' before single digit values.
 
+### Breaking Change
```

This should be moved now to `Master` section. We really need to land this PR :)

### Review thread 13 (resolved)

#### gziolo on 2019-06-05T12:38:16Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290715802
Path: packages/server-side-render/package.json

```diff
@@ -0,0 +1,36 @@
+{
+	"name": "@wordpress/server-side-render",
+	"version": "1.0.0-alpha.1",
+	"description": "ServerSideRender is a component used for server-side rendering a preview of dynamic blocks to display in the editor.",
+	"author": "The WordPress Contributors",
+	"license": "GPL-2.0-or-later",
+	"keywords": [
+		"wordpress",
+		"server side",
```

Typo?:
```suggestion
		"server-side",
```

### Review thread 14 (resolved)

#### gziolo on 2019-06-05T12:48:00Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r290719667
Path: packages/server-side-render/package.json

```diff
@@ -0,0 +1,36 @@
+{
+	"name": "@wordpress/server-side-render",
+	"version": "1.0.0-alpha.1",
+	"description": "ServerSideRender is a component used for server-side rendering a preview of dynamic blocks to display in the editor.",
```

How about we make it more explicit that this component won't work outside of WordPress?
```suggestion
	"description": "The component used with WordPress to server-side render a preview of dynamic blocks to display in the editor.",
```

### Review thread 15

#### youknowriad on 2019-06-06T10:24:17Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r291117745
Path: packages/editor/src/components/deprecated.js

```diff
@@ -117,3 +117,4 @@ export {
 	withColors,
 	withFontSizes,
 };
+export { default as ServerSideRender } from '@wordpress/server-side-render';
```

Should we add a `deprecated` call, when using it from this location?

#### mcsf on 2020-03-23T13:30:52Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r396451063
Path: packages/editor/src/components/deprecated.js

```diff
@@ -117,3 +117,4 @@ export {
 	withColors,
 	withFontSizes,
 };
+export { default as ServerSideRender } from '@wordpress/server-side-render';
```

I just ran into this while writing a plugin. Because `wp.components.ServerSideRender` is available with no deprecation warnings, it's easy to think that this is still the canonical place for that component.

~The other thing is that the `wp-components` module hasn't listed `wp-server-side-render` as a dependency, so as a plugin author I have to know that my plugin depends on `wp-server-side-render` on top of the rest. Finally, we are in a strange situation now because those two modules are interdependent. I believe that, going forward, the only proper solution is to remove `ServerSideRender` from `components`'s deprecated components.~

Of course, before that is possible, we should at least wrap `wp.components.ServerSideRender` so that it logs a deprecation warning.

#### mcsf on 2020-03-23T14:15:37Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r396482365
Path: packages/editor/src/components/deprecated.js

```diff
@@ -117,3 +117,4 @@ export {
 	withColors,
 	withFontSizes,
 };
+export { default as ServerSideRender } from '@wordpress/server-side-render';
```

Ah, this line was about `wp-editor` and I missed that. I guess it speaks to the general confusion. :) Never mind the bit about mutual dependencies, then.

#### mcsf on 2020-03-23T14:31:07Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r396493814
Path: packages/editor/src/components/deprecated.js

```diff
@@ -117,3 +117,4 @@ export {
 	withColors,
 	withFontSizes,
 };
+export { default as ServerSideRender } from '@wordpress/server-side-render';
```

https://github.com/WordPress/gutenberg/pull/21081

### Review thread 16

#### aduth on 2019-06-25T17:12:57Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r297299007
Path: lib/client-assets.php

```diff
@@ -289,6 +289,21 @@ function gutenberg_register_scripts_and_styles() {
 		)
 	);
 
+	// Add back compatibility for calls to wp.components.ServerSideRender.
+	wp_add_inline_script(
+		'wp-server-side-render',
+		implode(
+			"\n",
+			array(
+				'( function() {',
+				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
```

An issue with this is if `wp-components` is enqueued after `wp-server-side-render`, it will never be shimmed. We could potentially resolve this by defining `wp-components` as a dependency of `wp-server-side-render` (optionally checking that it's at least registered).

### Review thread 17

#### aduth on 2019-06-25T17:13:14Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r297299115
Path: lib/client-assets.php

```diff
@@ -289,6 +289,21 @@ function gutenberg_register_scripts_and_styles() {
 		)
 	);
 
+	// Add back compatibility for calls to wp.components.ServerSideRender.
+	wp_add_inline_script(
+		'wp-server-side-render',
+		implode(
+			"\n",
+			array(
+				'( function() {',
+				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
+				'		wp.components.ServerSideRender = wp.serverSideRender;',
```

If this is a component, the capitalization of `serverSideRender` is wrong. It should be `ServerSideRender`?

### Review thread 18

#### aduth on 2019-06-25T17:13:33Z

URL: https://github.com/WordPress/gutenberg/pull/15635#discussion_r297299251
Path: lib/client-assets.php

```diff
@@ -289,6 +289,21 @@ function gutenberg_register_scripts_and_styles() {
 		)
 	);
 
+	// Add back compatibility for calls to wp.components.ServerSideRender.
+	wp_add_inline_script(
+		'wp-server-side-render',
+		implode(
+			"\n",
+			array(
+				'( function() {',
+				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
+				'		wp.components.ServerSideRender = wp.serverSideRender;',
+				'	};',
```

The semi-colon here is unnecessary.

## Changed files

- docs/manifest-devhub.json (MODIFIED, +6/-6)
- lib/client-assets.php (MODIFIED, +15/-0)
- package-lock.json (MODIFIED, +13/-1)
- package.json (MODIFIED, +1/-0)
- packages/block-library/package.json (MODIFIED, +1/-0)
- packages/block-library/src/archives/edit.js (MODIFIED, +1/-1)
- packages/block-library/src/latest-comments/edit.js (MODIFIED, +1/-1)
- packages/block-library/src/legacy-widget/edit/index.js (MODIFIED, +1/-1)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/package.json (MODIFIED, +0/-1)
- packages/components/src/index.js (MODIFIED, +0/-1)
- packages/editor/CHANGELOG.md (MODIFIED, +3/-0)
- packages/editor/src/components/deprecated.js (MODIFIED, +1/-0)
- packages/editor/src/components/index.js (MODIFIED, +0/-1)
- packages/editor/src/components/server-side-render/README.md (DELETED, +0/-3)
- packages/editor/src/components/server-side-render/index.js (DELETED, +0/-18)
- packages/server-side-render/.npmrc (ADDED, +1/-0)
- packages/server-side-render/CHANGELOG.md (ADDED, +5/-0)
- packages/server-side-render/README.md (RENAMED, +18/-1)
- packages/server-side-render/package.json (ADDED, +37/-0)
- packages/server-side-render/src/index.js (ADDED, +46/-0)
- packages/server-side-render/src/server-side-render.js (RENAMED, +4/-6)
- packages/server-side-render/src/test/index.js (RENAMED, +1/-1)
- webpack.config.js (MODIFIED, +1/-0)
