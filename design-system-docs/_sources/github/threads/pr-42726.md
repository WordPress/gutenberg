---
number: 42726
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/42726
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components"]
---

# PR #42726: InputControl: Add optional Tooltip for inner input field

- URL: https://github.com/WordPress/gutenberg/pull/42726
- Author: aaronrobertshaw
- Created: 2022-07-27T08:44:25Z
- Updated: 2025-04-22T10:21:39Z
- Comments: 3 of 3
- Review threads: 6 of 6
- Changed files: 5 of 5
- Diff: ../pr-diffs/42726.diff

## Body

Related: 
- https://github.com/WordPress/gutenberg/issues/42630
- https://github.com/WordPress/gutenberg/pull/42661

## What?

Adds the ability to provide a tooltip for the `InputControl`'s inner input field.

## Why?

This helps avoid the possibility of nested tooltips when the `InputControl` has an interactive prefix or suffix which might have its own tooltip. 

## How?

- Adds new `showTooltip` & `tooltipText` props to the `InputControl` API
- If `showTooltip` is true along with `tooltipText` or a string `label`, the inner `InputField` is wrapped in its own `Tooltip`
- If rendering a `Tooltip` the `InputField` is wrapped in a `div` to [avoid issues](https://github.com/WordPress/gutenberg/pull/24966#issuecomment-685875026)

## Testing Instructions
1. Open up Storybook
2. Confirm the `InputControl` component displays the same on trunk as this PR (with or without `showTooltip` enabled)
3. Enable `showTooltip` in the story controls and confirm the tooltip displays both on hover and via keyboard focus
4. Provide a custom `tooltipText` value via story controls and confirm that text is used in the tooltip.
5. Take a look at the other Storybook examples for `InputControl` and confirm Tooltips
6. Check higher-level components that leverage `InputControl` still function/look correct e.g. `UnitControl` etc.

## Screenshots or screencast <!-- if applicable -->


https://user-images.githubusercontent.com/60436221/182095163-21214525-3654-4ab2-8d45-9e0799c601b6.mp4

<details>
<summary>Original demo without custom tooltip text support</summary>

<video src="https://user-images.githubusercontent.com/60436221/181202708-abe690a4-f160-4a96-b3d7-1dabec8d6b57.mp4" />

</details>

## Issue comments

### aaronrobertshaw on 2022-08-01T07:34:44Z

URL: https://github.com/WordPress/gutenberg/pull/42726#issuecomment-1200827097

> but if we think that the Tooltip functionality may be useful as a always-present feature of InputControl, then we should leave it as is.

I think this could be handy to have around.

> Also, another thought: to avoid nested tooltips, we should find a way to only render this tooltip if there aren't any other parent Tooltips in the virtual DOM tree — which is being discussed in https://github.com/WordPress/gutenberg/issues/42630

Agreed. I'd like to tackle that within a follow-up once we have mechanism to achieve it e.g. a disable flag via context etc.

### ciampo on 2022-08-10T12:44:20Z

URL: https://github.com/WordPress/gutenberg/pull/42726#issuecomment-1210622853

I don't have any further feedback on this PR at the moment — I guess the next step would be to understand in https://github.com/WordPress/gutenberg/pull/42661 if this is the way to go. 

If that's the case, I'd argue that we may want to add some unit tests for tooltip functionality and mark this PR as ready for review — at that point, I'll give it a final round of review & testing.

### aaronrobertshaw on 2024-03-21T04:37:15Z

URL: https://github.com/WordPress/gutenberg/pull/42726#issuecomment-2011202567

Closing this old PR. If we come back to this or a similar approach it can be reopened.

## Review threads

### Review thread 1 (resolved)

#### aaronrobertshaw on 2022-07-27T08:46:27Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r930789631
Path: packages/components/src/input-control/index.tsx

```diff
@@ -28,6 +30,18 @@ function useUniqueId( idProp?: string ) {
 	return idProp || id;
 }
 
+const WithTooltip = ( { showTooltip, text, children }: WithTooltipProps ) => {
+	if ( showTooltip && text ) {
+		return (
+			<Tooltip text={ text } position="top center">
+				<TooltipWrapper>{ children }</TooltipWrapper>
```

I found directly wrapping the `InputField` in the `Tooltip` didn't render. Following the approach noted in https://github.com/WordPress/gutenberg/pull/24966#issuecomment-685875026 which adds a `div` around the `Tooltip` children works.

#### ciampo on 2022-08-05T07:41:49Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r938538166
Path: packages/components/src/input-control/index.tsx

```diff
@@ -28,6 +30,18 @@ function useUniqueId( idProp?: string ) {
 	return idProp || id;
 }
 
+const WithTooltip = ( { showTooltip, text, children }: WithTooltipProps ) => {
+	if ( showTooltip && text ) {
+		return (
+			<Tooltip text={ text } position="top center">
+				<TooltipWrapper>{ children }</TooltipWrapper>
```

Maybe we could add an inline comment about this?

### Review thread 2 (resolved)

#### aaronrobertshaw on 2022-07-27T08:47:30Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r930790740
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,6 +122,12 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
```

Would allowing custom tooltip text be beneficial or should we be ensuring that any tooltip matches the label?

#### ciampo on 2022-07-28T11:19:20Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r932094106
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,6 +122,12 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
```

I think we could benefit from allowing consumers of `InputControl` to set a custom tooltip text. We could fallback to `label` in case it's not specified?

#### aaronrobertshaw on 2022-08-01T07:32:21Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r934220959
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,6 +122,12 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
```

Added option to set custom tooltip text in https://github.com/WordPress/gutenberg/pull/42726/commits/a858d7092a7bd9ddb73f9c3e8f67547a9c2ce7ff.

### Review thread 3 (resolved)

#### ciampo on 2022-08-05T07:20:53Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r938518749
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,7 +122,18 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
```

A bit of a nit, but maybe it's better not to expose implementation details to our documentation (in case we ever change how this feature is achieved)

```suggestion
	 * Whether to wrap input field in a tooltip.
```

#### aaronrobertshaw on 2022-08-08T05:34:55Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r939837728
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,7 +122,18 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
```

👍 Good call. Thanks. https://github.com/WordPress/gutenberg/pull/42726/commits/8c0acf553ad986880561af11323deabf73e70c17

### Review thread 4 (resolved)

#### ciampo on 2022-08-05T07:21:37Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r938519271
Path: packages/components/src/input-control/types.ts

```diff
@@ -67,6 +67,23 @@ export type InputChangeCallback<
 	P = {}
 > = ( nextValue: string | undefined, extra: { event: E } & P ) => void;
 
+export type WithTooltipProps = {
+	/**
+	 * React children.
+	 */
+	children: ReactNode;
+	/**
+	 * Whether or not to wrap children in a Tooltip component.
```

Probably forgot to update this after copy-pasting

#### aaronrobertshaw on 2022-08-08T05:39:01Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r939839376
Path: packages/components/src/input-control/types.ts

```diff
@@ -67,6 +67,23 @@ export type InputChangeCallback<
 	P = {}
 > = ( nextValue: string | undefined, extra: { event: E } & P ) => void;
 
+export type WithTooltipProps = {
+	/**
+	 * React children.
+	 */
+	children: ReactNode;
+	/**
+	 * Whether or not to wrap children in a Tooltip component.
```

Was this comment relating to the `@default false`? I think the description is otherwise relevant to `WithTooltip` as `showTooltip` will determine whether or not the children are wrapped in a tooltip and div.

#### ciampo on 2022-08-10T12:40:23Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r942399714
Path: packages/components/src/input-control/types.ts

```diff
@@ -67,6 +67,23 @@ export type InputChangeCallback<
 	P = {}
 > = ( nextValue: string | undefined, extra: { event: E } & P ) => void;
 
+export type WithTooltipProps = {
+	/**
+	 * React children.
+	 */
+	children: ReactNode;
+	/**
+	 * Whether or not to wrap children in a Tooltip component.
```

I honestly don't remember the exact reason why I left this comment — the `@default` value was definitely part of the reason, but I don't remember about the rest. Anyway, your reply makes sense, I'll mark this convo and the following one as resolved.

### Review thread 5 (resolved)

#### ciampo on 2022-08-05T07:21:41Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r938519310
Path: packages/components/src/input-control/types.ts

```diff
@@ -67,6 +67,23 @@ export type InputChangeCallback<
 	P = {}
 > = ( nextValue: string | undefined, extra: { event: E } & P ) => void;
 
+export type WithTooltipProps = {
+	/**
+	 * React children.
+	 */
+	children: ReactNode;
+	/**
+	 * Whether or not to wrap children in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
+	/**
+	 * Text to display within the Tooltip component.
```

Probably forgot to update this after copy-pasting

#### aaronrobertshaw on 2022-08-08T05:40:16Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r939839917
Path: packages/components/src/input-control/types.ts

```diff
@@ -67,6 +67,23 @@ export type InputChangeCallback<
 	P = {}
 > = ( nextValue: string | undefined, extra: { event: E } & P ) => void;
 
+export type WithTooltipProps = {
+	/**
+	 * React children.
+	 */
+	children: ReactNode;
+	/**
+	 * Whether or not to wrap children in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
+	/**
+	 * Text to display within the Tooltip component.
```

Similar to my [reply](https://github.com/WordPress/gutenberg/pull/42726#discussion_r939839376) above, this description does reflect the prop's purpose for `WithTooltip`.

### Review thread 6 (resolved)

#### ciampo on 2022-08-05T07:44:28Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r938540277
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,7 +122,18 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
 	stateReducer?: StateReducer;
+	/**
+	 * Custom text to display when tooltips are enabled. If not provided enabled
+	 * tooltips will fallback to using the label value.
+	 */
+	tooltipText?: string;
```

Do you think we'll ever be in a situation in which we'd like to pass a `ReactNode` as the tooltip's content, rather than just a string? Or do you think it's actually a good idea to only accept a simple string of text in this scenario?

#### ciampo on 2022-08-05T08:05:39Z

URL: https://github.com/WordPress/gutenberg/pull/42726#discussion_r938557641
Path: packages/components/src/input-control/types.ts

```diff
@@ -105,7 +122,18 @@ export interface InputFieldProps extends BaseProps {
 	paddingInlineStart?: CSSProperties[ 'paddingInlineStart' ];
 	paddingInlineEnd?: CSSProperties[ 'paddingInlineEnd' ];
 	setIsFocused: ( isFocused: boolean ) => void;
+	/**
+	 * Whether to wrap input field in a Tooltip component.
+	 *
+	 * @default false
+	 */
+	showTooltip?: boolean;
 	stateReducer?: StateReducer;
+	/**
+	 * Custom text to display when tooltips are enabled. If not provided enabled
+	 * tooltips will fallback to using the label value.
+	 */
+	tooltipText?: string;
```

Nevermind, it actually looks like `Tooltip` only support text anyway.

## Changed files

- packages/components/src/input-control/index.tsx (MODIFIED, +44/-19)
- packages/components/src/input-control/input-base.tsx (MODIFIED, +1/-1)
- packages/components/src/input-control/styles/input-control-styles.tsx (MODIFIED, +4/-0)
- packages/components/src/input-control/types.ts (MODIFIED, +26/-0)
- packages/components/src/unit-control/test/__snapshots__/index.tsx.snap (MODIFIED, +3/-3)
