---
number: 78808
type: pull_request
state: OPEN
url: https://github.com/WordPress/gutenberg/pull/78808
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","[Package] Block library","[Package] Block editor","[Package] Icons","[Package] Edit Site","[Feature] Icons"]
---

# PR #78808: Icons: Redraw 35 prominent icons to strokes, and maintain absolute stroke-width

- URL: https://github.com/WordPress/gutenberg/pull/78808
- Author: jasmussen
- Created: 2026-05-29T10:35:33Z
- Updated: 2026-06-19T09:28:54Z
- Comments: 49 of 49
- Review threads: 25 of 25
- Changed files: 51 of 51
- Diff: ../pr-diffs/78808.diff

## Body

## What?

Followup/replacement to #78774, which now includes the change including resilience for compatibility.

In this PR, I redrew/changed 34 icons to be stroke-based rather than purely fill-based. An existing icon was already  stroke-based, `square`. This brings an enormous amount of flexibility as far as future animation and customisation properties, it simplifies the Figma to Icon flow, but notably it means that icons now maintain their stroke-width across sizes. Paragraph shown as example, 16, 24, 32px sizes:

<img width="75" height="50" alt="Skærmbillede 2026-05-29 kl  12 25 11" src="https://github.com/user-attachments/assets/b55bfced-e9af-4481-94d9-89277450b310" />
<img width="76" height="58" alt="Skærmbillede 2026-05-29 kl  12 25 17" src="https://github.com/user-attachments/assets/6ea7a734-613d-44e4-9b99-0a2fdea8d6f8" />
<img width="78" height="65" alt="Skærmbillede 2026-05-29 kl  12 25 42" src="https://github.com/user-attachments/assets/ae9f7761-90c5-4625-b5a1-881582195730" />

## Why?

For the majority of these icons, **no visual change should be perceptible in where they are used today**, because we use them mostly in their standard 24x24px sizes. And we do that specifically because of the shortcoming in how they scale, shifting stroke-width making them look out of place next to each other. 

By enforcing a monoline stroke-width, icons can now be shown at virtually arbitrary sizes (though they can get fuzzy if shown really small), and yet still feel as if they belong together since their stroke-widths are maintained. A future perspective is even to allow consumers of the componentry to either animate the stroke, both the width and the dash-style, offering some interesting options, such as [how this WordPress logo is animated](https://codepen.io/joen/pen/jepZrB).

## How?

This PR includes both the tech to maintain a uniform stroke-width across icon dimensions, as well as 35 icons that take advantage of it. I am working in a WIP Figma file to go through all existing icons, as such:

<img width="688" height="888" alt="Icons" src="https://github.com/user-attachments/assets/574e0cd4-3e3a-410d-936f-be8b358574a5" />

In black, those are the icons in this PR, done/redrawn. In red, still todo. In blue, icons we will not be redrawing as they are not/can't be stroke-based. In gray, icons that need redesigns anyway. Once this effort is done, these icons will move to the main Figma design library. I believe there are some additional icons shipping in trunk that are not depicted in this list, but I will be sure to identify those once I'm through this effort. 

## Testing Instructions

Check out the PR; run npm run storybook:dev, go to Icons > Library, and then observe that at both 16, 24, and 32, that icons look as intended. These icons:

- add-card
- add-template
- cancel-circle-filled
- caution
- caution-filled
- code
- comment-author-avatar
- cover
- currency-dollar
- currency-euro
- currency-pound
- drafts
- help
- help-filled
- image
- info
- lifesaver
- link
- link-off
- navigation
- not-allowed
- paragraph
- pending
- plus-circle
- plus-circle-filled
- published
- scheduled
- site-logo
- star-empty
- star-filled
- star-half
- styles
- square
- time
- tip

## Use of AI Tools

Claude Code was used to extract a todo-list of icons for me, but the rest is hand-rolled, human spirit work.

## Issue comments

### github-actions on 2026-05-29T10:35:51Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4573960607

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: jasmussen <joen@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: westonruter <westonruter@git.wordpress.org>
Co-authored-by: jameskoster <jameskoster@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: fcoveram <fcoveram@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### jasmussen on 2026-05-29T10:41:29Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4574012429

A note that I forgot to mention: as I carefully hand-redraw these, some curves and vectors become more precisely positioned, clearly mistakes from previous iteration. In 95% of the cases in this batch, the end result when seen at 24x24 is invisible. However I want to point out a couple of icons were very slightly visually refreshed as part of this, in case that invites feedback. IMO, it's not enough to warrant a changelog entry beyond what this PR can benefit from regardless, but for your awareness:

* time-to-read has changed from using sharp hands to using the same style as that of the "pending" clock
* the stars (star-empty, star-half, star-filled) now have fully sharp corners instead of barely perceptibly visible rounded corners. This was necessary to convert it to strokes.

### github-actions on 2026-05-29T11:18:41Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4574307149

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 932fcc0f269e47b332c6282a3615ed5f164acbf3.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27810898468
📝  Reported issues:
- #78689 in `/test/e2e/specs/editor/plugins/wp-editor-meta-box.spec.js`

### jameskoster on 2026-05-29T12:26:24Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4574967036

Wonderful!

Since this involves redrawing icons, would it make sense to address some of the low-hanging fruit in https://github.com/WordPress/gutenberg/issues/65786 as well? For instance applying endcap-roundness consistently, pixel grid alignment, and footprint consistency?

### jasmussen on 2026-05-29T12:59:38Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4575316145

Yes it does indeed! And notably the pixel grid alignment I've been fixing here. There's been a _lot_ discovered. I've also unified a couple of circles that were off-size.

I'm hesitant to do too much, but would be happy to follow up after we land a good new stroke based baseline. One really potent improvement as I do this: the new icons are subtantially easier to edit and work with, because what we used to call the "icon source", the stroke based version we would go ahead and convert to fills, that is now _the icon_ that we ship, one and the same, so the Figma to GitHub flow is now very much improved. 

The short is, once this is done, it should be both easier to contribute, to edit, to consolidate, and all that.

### jameskoster on 2026-05-29T13:37:54Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4575833173

Yes, appreciate it's a balance; we want to avoid scope creep. I'm just aware that it might be easier to tweak these things in one go rather than sequentially, with no meaningful trade-offs that I can think of.

One other thing that springs to mind from a previous exploration around converting the icons to be stroke-based... there is quite a bit of css in Gutenberg that targets icons with `fill` styles. This can have a very different effect when the icons are outlined. We might need to swap some of those `fill` styles to use `stroke` instead. Probably worth smoke testing.

### jasmussen on 2026-05-29T13:43:55Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4575892343

It's funny you should mention that, because in testing other things, I found issues with exactly that:

<img width="290" height="422" alt="Skærmbillede 2026-05-29 kl  15 32 20" src="https://github.com/user-attachments/assets/b016badc-a73c-410d-a033-7283daa01f86" />

This is now fixed, but I will write some additional detail on the fix in context of the code.

<img width="282" height="390" alt="image" src="https://github.com/user-attachments/assets/4f128e3c-5517-4246-bc05-22ea92427ab6" />

### github-actions on 2026-05-29T13:51:36Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4575959917

**Size Change:** +1.91 kB (+0.02%) 

**Total Size:** 8.6 MB

<details open><summary>📦 <strong>View Changed</strong></summary>

| Filename | Size | Change |  |
| :--- | :---: | :---: | :---: |
| `build/modules/boot/index.min.js` | 52.9 kB | +18 B (+0.03%) |  |
| `build/modules/content-types/index.min.js` | 159 kB | +101 B (+0.06%) |  |
| `build/modules/edit-site-init/index.min.js` | 1.48 kB | +83 B (+5.93%) | 🔍 |
| `build/modules/workflow/index.min.js` | 19.9 kB | +14 B (+0.07%) |  |
| `build/scripts/block-directory/index.min.js` | 43.6 kB | -92 B (-0.21%) |  |
| `build/scripts/block-editor/index.min.js` | 380 kB | +129 B (+0.03%) |  |
| `build/scripts/block-library/index.min.js` | 324 kB | +11 B (0%) |  |
| `build/scripts/commands/index.min.js` | 21 kB | +11 B (+0.05%) |  |
| `build/scripts/components/index.min.js` | 264 kB | -8 B (0%) |  |
| `build/scripts/core-commands/index.min.js` | 4.41 kB | +74 B (+1.71%) |  |
| `build/scripts/edit-site/index.min.js` | 298 kB | +974 B (+0.33%) |  |
| `build/scripts/edit-widgets/index.min.js` | 22.1 kB | -53 B (-0.24%) |  |
| `build/scripts/editor/index.min.js` | 472 kB | +21 B (0%) |  |
| `build/scripts/format-library/index.min.js` | 31 kB | +111 B (+0.36%) |  |
| `build/scripts/media-utils/index.min.js` | 114 kB | +116 B (+0.1%) |  |
| `build/scripts/preferences/index.min.js` | 3.31 kB | +15 B (+0.46%) |  |
| `build/styles/block-library/icon/style-rtl.css` | 323 B | +52 B (+19.19%) | ⚠️ |
| `build/styles/block-library/icon/style-rtl.min.css` | 252 B | +44 B (+21.15%) | 🚨 |
| `build/styles/block-library/icon/style.css` | 323 B | +52 B (+19.19%) | ⚠️ |
| `build/styles/block-library/icon/style.min.css` | 252 B | +44 B (+21.15%) | 🚨 |
| `build/styles/block-library/style-rtl.css` | 21.7 kB | +51 B (+0.24%) |  |
| `build/styles/block-library/style-rtl.min.css` | 18.2 kB | +45 B (+0.25%) |  |
| `build/styles/block-library/style.css` | 21.8 kB | +51 B (+0.23%) |  |
| `build/styles/block-library/style.min.css` | 18.2 kB | +46 B (+0.25%) |  |

</details>

<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### jasmussen on 2026-06-01T13:06:25Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4592857473

This PR has been updated to include changes from the now-reverted #78774 (as well as a rename from `time-to-read` to `time`). It was reverted due to certain contexts that enforced a fill on SVGs that shouldn't have them, an issue which is fixed also as part of this PR ([details start here](https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4575892343)).

It remains ready for review and input on the overall approach, and available for testing in both storybook and the editor. I've not been able to "break it". #78812 is a followup PR that adds even more icons, but I will pause that PR for this one to land first once an approach is agreed on, then follow up/rebase. After that, I hope to explore separate linting PR that ensures SVGs added work well for the stroke-based system. After that, I'll re-draw the remaining icons, per [this comment](https://github.com/WordPress/gutenberg/pull/78808#discussion_r3332936818).

### jasmussen on 2026-06-03T12:41:30Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4612401641

This PR has been rebased. I'd love your input on the approach! @ciampo, @mirka, any instincts?

### ciampo on 2026-06-08T13:59:07Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4649735062

One thing that is not clear to me is whether having a mix of fill-based and some stroke-based icons is a good idea. For example, I guess the main advantage of stroke-based icons is that we can tweak the stroke width to custom values. But does it make sense to enable such features only for a subset of the icons?

It may cause confusion in the consumers, requiring them to investigate and understand what icons are stroke-based and what are fill-based. Thus, inviting them to learn about implementation details that can change over time.


This comment is coming more from a place of "ignorance" than "adversarial review", I'm happy to lean on your expertise on dealing with icons.

> One other thing that springs to mind from a previous exploration around converting the icons to be stroke-based... there is quite a bit of css in Gutenberg that targets icons with `fill` styles. This can have a very different effect when the icons are outlined. We might need to swap some of those `fill` styles to use `stroke` instead. Probably worth smoke testing.

This is definitely something that came to mind when going through the changes in this PR.

### jasmussen on 2026-06-08T14:09:13Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4649832244

> One thing that is not clear to me is whether having a mix of fill-based and some stroke-based icons is a good idea. 

I appreciate the question, because I doubt you can find a set that can be 100% strokes only, but even when using fills, you can cleverly combine them in a way that is useful. 

<img width="553" height="1092" alt="image" src="https://github.com/user-attachments/assets/31766d6d-dd46-4180-97c8-9a1b1190a1e5" />

In this case, consider the star, filled, outline, half. It feels valid to maintain. While you could theoretically place a lot of lines next to each other to emulate a fill, that feels the wrong approach.

So what I did was keep the fill, but for the filled star have BOTH fill and outline, so when the stroke changes for star-half and star-outline, so does the outline of the filled variant. They all maintain the same footprint regardless of stroke width. Same with the paragraph icon, which has a fill in the half-circle on the left.

### jameskoster on 2026-06-08T14:28:48Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4650025741

From what I've seen most stroke-based icon libraries leave it to the consumer to handle whether stars are filled or not (via `fill`), rather than providing dedicated filled/unfilled icons.

### jasmussen on 2026-06-08T14:46:04Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4650186818

> From what I've seen most stroke-based icon libraries leave it to the consumer to handle whether stars are filled or not (via fill), rather than providing dedicated filled/unfilled icons.

Can you expand on that: how would you make a filled star without a fill?

### jasmussen on 2026-06-08T15:06:23Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4650364686

Fantastic feedback, keep it all coming. I'm EOD now, but I will address it first thing tomorrow, and then hopefully we can merge this first iteration.

### jameskoster on 2026-06-09T08:49:04Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4658042452

> Can you expand on that: how would you make a filled star without a fill?

I'm saying that if a consumer needed a filled star they'd use the (outlined) icon from the library and use `fill` to fill it.

### jasmussen on 2026-06-09T08:54:00Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4658088009

> I'm saying that if a consumer needed a filled star they'd use the (outlined) icon from the library and use fill to fill it.

How about a half-star? E.g. 3.5 stars out of 5 in a rating setup.

### jasmussen on 2026-06-09T09:40:53Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4658479955

Okay, thank you for the reviews and feedback. With my assistant Claude I've tried my best to address them.

@ciampo 
Added most of your suggestions verbatim, and added regression tests and fixed changelogs. Also updated the Cover icon.

@westonruter
Good point about the semicolon split. My assistant made a tokenizer that ignores semicolons inside url() and quoted strings, so base64 data: URIs stay intact (key/value split also takes the first colon only). And added unit tests. 

I realise this stuff is hard to review, so I appreciate very much your patience. The PR is growing slightly bigger than I preferred, but I think it's necessary to make this change towards stroke-based icons, and that every subsequent followup PR (SVG build normalisation, the rest of the icons redrawn) will be smaller and easier.

### jasmussen on 2026-06-09T13:42:13Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4660367118

Nice, thank you, this should be fixed. I'm going to let this branch sit at least a day and gather more feedback, if any, and then merge so it has a good chance to be tested in trunk and the plugin.

### jameskoster on 2026-06-09T15:51:04Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4661528374

> How about a half-star? E.g. 3.5 stars out of 5 in a rating setup.

Same deal. It's drawn/shipped as an outline (e.g. [Lucide](https://lucide.dev/icons/star-half)) but can be filled with `fill`.

### jasmussen on 2026-06-10T07:51:32Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4667858805

Indeed, that answers my question, which is to say if we were to go that way, it would be a semi-large shift away from the current aesthetic. With fills, a 3.5 star rating would arguably look like this:

<img width="496" height="96" alt="fills" src="https://github.com/user-attachments/assets/c0f73fb5-470c-4870-8b40-1d6df6010ea8" />

In the outline aesthetic, correct me if I'm wrong, it would look like this:

<img width="496" height="96" alt="outlines" src="https://github.com/user-attachments/assets/685d047a-cbba-4f5e-baee-30133486b663" />

You note that the fill is optional, but unless I'm missing something, you'd still be looking at a bit of a shift:

<img width="496" height="96" alt="outlines with fills" src="https://github.com/user-attachments/assets/75b562b4-beae-49d4-963b-b2b321cd1eb9" />

These are examples that focus on a rating system, mostly because it's a very visual example of the utilitarian effect this has on the set. But there are additional questions that will need answering if we go in that direction. E.g. it more or less spells doom for the solid versions of these icons, where the letter inside is _knocked out_ of the fill:

<img width="384" height="96" alt="dual versions" src="https://github.com/user-attachments/assets/82058ff8-c4fd-464f-be6a-65be0c2ce5a7" />

That can arguably be acceptable. But there are also icons that rely very heavily on fills to support their concept. Coming to mind, symbols notably, but also verse and paragraph, both of which would get a more complex silhouette if we were to move to silhouettes only. Verse roughly redrawn below:

<img width="384" height="96" alt="concepts" src="https://github.com/user-attachments/assets/7e886bf8-718d-461e-a698-79f8b3421533" />
<br>
<img width="288" height="96" alt="concepts-outlines" src="https://github.com/user-attachments/assets/7e87f63e-b711-45ab-ab6a-5d42a9efe421" />

We can pursue this direction, but it's a big shift that I think is worth sleeping on. So my main question is: where does this leave us in the mean-time? If we focus on Paragraph, for example, this is the code for the redrawn version:

```
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
	<path d="M10.75 20V4.75L14.75 4.75M14.75 20V4.75M14.75 4.75H18.5" vector-effect="non-scaling-stroke" />
	<path d="M5.75 9C5.75 11.3472 7.65279 13.25 10 13.25H10.75V4.75H10C7.65279 4.75 5.75 6.65279 5.75 9Z" fill="currentColor" vector-effect="non-scaling-stroke" />
</svg>
```

In Figma, it looks like this: 

<img width="171" height="100" alt="image" src="https://github.com/user-attachments/assets/f6aa68fa-979c-4019-bbd7-6a8ef1c7722f" />

I.e. There's a half-circle _fill + stroke_ on the left that perfectly overlaps a paragraph-shaped "pi" symbol on the right:

<img width="702" height="714" alt="paragraph" src="https://github.com/user-attachments/assets/aa229334-1851-449b-a383-e2db648ccf8e" />

The shapes are perfectly placed, so when you change the stroke-width, the entire symbol grows. You _can_ remove the fill. But I would like to argue in favor for us **having the fill there by default**. Would that work for you?

### jasmussen on 2026-06-10T09:28:11Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4668615032

Noting that we can actually make variable width "knockout" strokes, a la the filled help icon. [Example](https://codepen.io/joen/pen/rajeGPQ). That's a bit more code inside the SVG, and not a question for this PR, but sharing in context of the fill vs. stroke questions above.

### fcoveram on 2026-06-10T12:14:14Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4670054621

Exciting change. After reading the messages that involve shaping icons, here is my approach.

Shapes should support stroke and fill properties to create the excluded shape and support the current `caution` and `cautionFilled`. This creates a new duotone variant that we're not showing by default, but it could be possible as the ingredients are there. When a shape needs to work as a mask and extract a filled area, the stroke value of that shape should work as such.

Here is a quick and simple diagram of what I'm saying.

<img width="1460" height="716" alt="image" src="https://github.com/user-attachments/assets/5d41a1f0-766f-40aa-ad78-d259f045976a" />

This should reduce the set and treat the outline and filled variants as generated from the same icon.

I was looking at [Phosphor icons](https://phosphoricons.com/?q=info&size=72&weight=duotone) to understand this.

### jameskoster on 2026-06-10T12:58:30Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4670446487

I think the star rating example is a bit of a tangent because it's a higher level abstraction. The appearance is decided by the consumer not inherently in the icons themselves. Outlined icons provide the most flexibility to approach it however you like; all filled, all outlined, or a mixture:

<img width="176" height="132" alt="stars" src="https://github.com/user-attachments/assets/7cdf241e-48a9-4491-9356-8e4e504be136" />

> E.g. it more or less spells doom for the solid versions of these icons, where the letter inside is knocked out of the fill

Potentially controversially I see that as an enhancement. Providing both in the same set means it's never really clear when to use one or the other. My overall perspective remains; fill is a library level concern, not a per icon concern. The entire set should use the same approach. If desirable, we could offer filled, outlined, and duotone sets and let the consumer choose which to use globally via theming.

---

More generally I share the concerns about tackling this piecemeal. I worry the unpredictability will be frustrating for consumers who are styling icons ("Do I need to use `fill` or `stroke` here??"). I don't know how common that is though, or whether it should be a blocker.

### jasmussen on 2026-06-10T14:06:02Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4671098785

Great clarification, thank you.

> Potentially controversially I see that as an enhancement. Providing both in the same set means it's never really clear when to use one or the other. 

I actually agree, and many of those icons piled up from 1:1 Dashicons conversions. I agree it's in our interest to see ways to reduce that, perhaps as part of #65786.

And that, really, is the larger picture. My hope with this PR is to make an invisible change (visually there's no change from before and after this PR), that nevertheless brings a lot of energy and excitement to the set. Specifically it unblocks [the next PR](#78812), which expands the range of stroke-based icons by **65**, and most of those are arrows and chevrons that truly benefit from the stroke-unification in terms of utility. 

I genuinely believe that I can have the remaining icons updated before 7.1 ships, including some normalisation scripts that improve the build process. That would leave us in a substantially better place to go into polish mode: deprecate poor or older icons, update optical footprints, unify similar shapes and radii.

But the task for today is to decide: **is it okay to land this PR, which has minimal breaking changes, and no visual changes**?

Personally, an emphatic yes, because while this PR still includes fills, it does so in a pragmatic way: yes all icon types are welcome and will work in the **engine**. Actually redrawing mixed fill+stroke based icons to be purely stroke-based can still happen.

But as those would be breaking changes, I'd like to keep such conversations separate. What do you think? Okay if I press the green button?

### jasmussen on 2026-06-10T14:15:30Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4671185059

I included the wrong quote above, edited. To this point:

> More generally I share the concerns about tackling this piecemeal. I worry the unpredictability will be frustrating for consumers who are styling icons ("Do I need to use fill or stroke here??"). I don't know how common that is though, or whether it should be a blocker.

That's the thing, consumers should not be styling either fills or strokes at all, they should be styling [color](https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373707049). To me this feels like the main tech maturation point here, and honestly part of what gets me most excited about this. The base usage of this gets simpler, but we still open the door for cool new [stroke-based animation effects](https://codepen.io/joen/pen/jepZrB), for those that want to.

### fcoveram on 2026-06-10T15:02:04Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4671618278

> > More generally I share the concerns about tackling this piecemeal. I worry the unpredictability will be frustrating for consumers who are styling icons ("Do I need to use fill or stroke here??"). I don't know how common that is though, or whether it should be a blocker.
>
> That's the thing, consumers should not be styling either fills or strokes at all, they should be styling https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373707049.

That's could be a good starting point, but this update also opens the door to define hierarchy levels for when to use the filled or stroke version depending on the type of message and icon's role. For instance, information icon next to a label triggering a tooltip versus next to a heading inside notice.

My only concern with merging this is some unexpected outcomes.

### jasmussen on 2026-06-10T15:16:34Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4671726463

> My only concern with merging this is some unexpected outcomes.

Mine as well, and for that reason I went quite deep in ensuring that all outcomes are accounted for, that's why there's a fair bit of additional code changes and tests in this PR than just the icon changes. In my own testing, the before and after with this PR is identical, as it should be.

### t-hamano on 2026-06-11T08:43:10Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4678767427

I found that the display of icons in the Icon block unexpectedly changes.

| Before | After |
|--------|--------|
| <img width="131" height="498" alt="image" src="https://github.com/user-attachments/assets/f2c6fba0-75c8-4489-a281-7762d5e8ccf4" /> | <img width="117" height="492" alt="image" src="https://github.com/user-attachments/assets/b3aae8d4-a507-4b30-8d07-202e62dfeb0b" /> |

I found that one of the causes was that the icon registry unintentionally removed attributes when sanitizing icon content, so I fixed it with 107cb53600ff202c2c580ae1ae29065d4ed6d12b. We plan to address icon content sanitization comprehensively with #75550.

However, this does not solve the problem either. We need to investigate further whether something is still missing from the icon registry or if there is an issue with the Icon block.

### jasmussen on 2026-06-11T09:03:48Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4678928474

> If we're confident we can get all the icons redrawn (and deprecate any dedicated 'Filled' variants) in a timely fashion then I'd say we're good 👍 

I'm confident I can get all the icons redrawn. I've also confidence we _can_ deprecate filled variants. I'm not sure we can do the latter in a timely manner though, just to be honest. Hoping that doesn't resurface the blocker.

> I found that the display of icons in the Icon block unexpectedly changes.

Thanks for checking, and for blocking the PR. 

I will also dive into looking at the effect this has on the icon block, beyond the spot testing I've done so far, and see if I can push either an update to this branch, or a separate one.

### t-hamano on 2026-06-11T09:08:15Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4678962999

I believe the issue has been fixed by 932a4394cc1f906c9832a801a165d29fd583c46a. It seems the cause was that `fill:none` was removed from the SVG element. Could you re-verify the following for both stroke-based and non-stroke-based icons?

- You can set the default icon color, background color, and width via global styles.
- You can override their styles in block instances.
- Ensure the stroke-based icon doesn't appear distorted when a large width is applied.

### jameskoster on 2026-06-11T09:19:08Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4679051883

> I'm not sure we can do the latter in a timely manner though, just to be honest

How come? It doesn't have to be a hard deprecation; we could just use the same glyph in both icons.

### jasmussen on 2026-06-11T09:32:35Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4679161074

> Could you re-verify the following for both stroke-based and non-stroke-based icons?
> You can set the default icon color, background color, and width via global styles.
> You can override their styles in block instances.
> Ensure the stroke-based icon doesn't appear distorted when a large width is applied.

Confirming your fix works for me. Before:

<img width="1739" height="785" alt="Skærmbillede 2026-06-11 kl  11 22 33" src="https://github.com/user-attachments/assets/12aee38f-1b58-40fe-ae32-f8d6f114fb9d" />

After:

<img width="1762" height="856" alt="Skærmbillede 2026-06-11 kl  11 26 09" src="https://github.com/user-attachments/assets/ef18b526-b7e0-4422-812c-31a020856f53" />

I can also set the color and such. I will note another discovery, and I mean this is intentional but it does beg for a stroke-width-slider:

<img width="1072" height="693" alt="Skærmbillede 2026-06-11 kl  11 26 58" src="https://github.com/user-attachments/assets/323aba84-d4a0-4848-b4fe-f1f3ca726677" />

This is one of the things I'm the most excited for us being able to provide now. This also benefits our icons when they get small. With this new state, when you scale them down, some of them can become fuzzy when they still default to the 1.5px default width:

<img width="60" height="70" alt="1 5" src="https://github.com/user-attachments/assets/d0c0c365-9b20-41ed-8b5c-61da56907501" />

Previously we were locked to the 1.5px stroke of the 24x24 becoming 1px at 16x16, which we can still do by passing a prop contextually, e.g. inside Badge:

<img width="58" height="62" alt="1" src="https://github.com/user-attachments/assets/14419dcb-6a51-4d37-9154-602a45978192" />

But we could also pass a stroke-width of 0.66—it becomes a bit thinner, but it aligns it to the nearest whole integer pixel:

<img width="59" height="69" alt="0 66" src="https://github.com/user-attachments/assets/63da93d4-99c6-490e-8c3b-eabfa892f1fc" />

We could also supply a stroke-width of 1.33, aligning it to the nearest whole pixel in the upwards direction:

<img width="59" height="64" alt="1 33" src="https://github.com/user-attachments/assets/74b626b8-5610-48aa-b079-533788e20f17" />

So the cool things though, is that by being able to adjust the stroke-width, we can ensure crispness at that size.

### jasmussen on 2026-06-11T09:35:29Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4679185197

@jameskoster 

> How come? It doesn't have to be a hard deprecation; we could just use the same glyph in both icons.

I think I'm missing nuance, so forgive me if these questions are dumb. But to confirm, I think we could deprecate the fill based versions of these two in short order:

<img width="384" height="96" alt="dual versions" src="https://github.com/user-attachments/assets/24a35911-f492-424d-be08-c0cb95087034" />

These are harder, arguably the icons have character and intent with the fills, so if we were to embody a new strict outlines-only philosophy for the set, these would need conceptual redesigns. 

<img width="384" height="96" alt="concepts" src="https://github.com/user-attachments/assets/558c880e-af57-4060-a01e-aae716e263e7" />

What do you think?

### jasmussen on 2026-06-11T09:42:44Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4679243401

@t-hamano Thank you for all the help. I've rebased this to pick up changes from conflicting changelogs. I tested your steps, everything seems to work.

Based on the stroke-width being flexible I suspect we might want to update the changelog for the Icon block at least, to indicate what changed. I'm also happy to follow up with a new feature addition to the Icon block, to add a stroke-width slider to the block. What do you think is the best approach there?

### jasmussen on 2026-06-11T09:48:48Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4679291530

The easiest interim thing we could do for the Icon block, is to explicitly turn **off** the stroke scaling effect, using `vector-effect: none;`:

<img width="441" height="541" alt="Skærmbillede 2026-06-11 kl  11 47 37" src="https://github.com/user-attachments/assets/5864f941-773b-483b-a96f-f9b14ca4e796" />

This would allow me to _complete_ the conversion of the set to be stroke-based, and then the value can be removed alongside the addition of a stroke-slider control. What do you think?

### jasmussen on 2026-06-11T10:00:46Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4679386637

Noting that if we did want to temporarily turn off stroke-based scaling, I'd prefer to do it in a separate PR that theoretically could be as little as this:

```
.wp-block-icon svg :where(path, rect, circle, ellipse, line, polygon, polyline) {
	vector-effect: none;
}
```

### jasmussen on 2026-06-11T12:59:01Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4680803391

Noting that I created #79116 that we can land before this PR, to maintain the visuals of the icon block even as I complete the stroke-based conversion.

### jasmussen on 2026-06-17T10:45:52Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4729040695

Addressed to the best of my ability the feedback, and rebased this branch. 

To note, the sanitization-side feedback (DRY, polygon attributes, sanitization tests) is being more comprehensively addressed in @t-hamano's #75550, which is a substantial rework of sanitize_icon_content with a broader allowlist (including polygon with full stroke support) and dedicated tests. Doing intermediate refactors here would create a conflict with that PR. How would you feel about letting 75550 be the single source of truth for the sanitizer?

As for [the icon block render tests](https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403335286), here,  happy to do a followup. The empty-style edge case should be guarded in code, so perhaps the test could be a followup. 

As far as next steps, I wanted to note I'm heading AFK for my summer vacation starting Tuesday next week. I'd love to be able to land this before then, as I believe it's a strong step forward for the icon tech, and it embodies changes so far that are purely invisible, all under-the-hood stuff that we can take advantage of later, notably when the rest of the set is updated to be stroke-based. But if we don't land it before then, I just wanted to make clear that **others should feel free to commandeer and push/edit this PR, as well as merge, if they would like**.

Finally, I want to flag again that I think it would be useful to land #79116 as a safeguard for the Icon block. It ensures strokes scale like they do today, even as some icons become stroke-based. It would be a temporary safeguard because once the whole set is updated, we would change this to become a toggle instead, so you could choose your appearance, capitalising on the change.

### mirka on 2026-06-17T20:26:51Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4734987386

I think I would classify the [`currentColor` inconsistency](https://github.com/WordPress/gutenberg/pull/78808#discussion_r3423526273) as a blocker to this PR merging, unless we're sure we can follow up with the consistency fix before the next `@wordpress/icons` package release.

### jasmussen on 2026-06-18T09:13:23Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4740149923

Thanks for the clarity @mirka. I'd like to propose a way to address that consistency concern, I have it working locally but it's unpushed, since it will involve adding an additional 295 changes to this commit, I'd love to share the brief and pitch with you first, before I commit.

The pitch is that we add a small source-normalization step to `packages/icons/lib/generate-library.cjs` (the icons package's existing build script), then run it once to canonicalize all current source SVGs.

This script I've run locally, and validated that it works. What it does is add a new function to the existing build script. It's wired into main() so it runs on every `npm run build`, which means future icon additions self-normalize. Here's the script:

```
async function normalizeSourceIcons() {
	const svgFiles = ( await readdir( ICON_LIBRARY_DIR ) ).filter( ( file ) =>
		file.match( /^[a-z0-9--]+\.svg$/ )
	);

	await Promise.all(
		svgFiles.map( async ( svgFile ) => {
			const svgPath = path.join( ICON_LIBRARY_DIR, svgFile );
			const original = await readFile( svgPath, 'utf8' );

			const updated = original.replace(
				/^(<svg\b)([^>]*)>/,
				( match, openTag, attrs ) => {
					// Skip if outer <svg> already declares fill via attribute.
					if ( /\sfill\s*=/.test( attrs ) ) {
						return match;
					}
					// Skip if outer <svg> declares fill via inline style.
					if ( /\sstyle\s*=\s*"[^"]*\bfill\b/.test( attrs ) ) {
						return match;
					}
					return `${ openTag }${ attrs } fill="currentColor">`;
				}
			);

			if ( updated !== original ) {
				await writeFile( svgPath, updated );
			}
		} )
	);
}
```

When I re-run it on already-normalised source files, it produces no changes.

In testing locally, it changed 295 icons, left 35 unchanged (the ones redrawn in this PR), coming to 330 total. 

It ignored the 35 as they all have style="fill: none" on the outer element. For the icons changed, it looks like this:

```
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
```

If you approve, I'd make two commits:

1. The script: generate-library.cjs
2. The normalized icons, a 295-file diff, each with a single-line attribute addition.

I propose to do it in this PR, but it could also be a separte PR, or a "branch on the branch". I'd like to do it in this PR as I will be AFK from Tuesday next week, back July 8th. But I an also put it in a separate branch, so long as we remember to land both in short succession. E.g. I already think #79116 would be good to pair with this.

Also to be clear: I do welcome commits from others, and to merge or even revert if something happens, in my absence.

Let me know!

### mirka on 2026-06-18T12:19:35Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4741859778

The color normalization is a completely separate concern to this PR, and warrants dedicated review and documentation of our reasoning. So let's do it in a separate PR. If you can push the relevant changes that you made, I can drive it forward while you're out 👍

In terms of specifics, I have a feeling we should just normalize the existing SVGs with your script now and commit the changes permanently, rather than keeping the script as part of the build script. If we find that new icons get added without the necessary colors, we can maybe add a lint rule later.

### jasmussen on 2026-06-18T12:52:47Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4742113828

Solid feedback, and thanks so much for all the help.

I've now pushed an entirely separate PR, #79320, which does as you propose. First it updates all the icons. The second is the optional helper script, but no longer tied to npm run build. We can keep or delete that second commit. But honestly it works pretty well even as a test: if you run it and it doesn't change a thing, that means all the icons are as they should.

Let me know if this works for you! Thanks @mirka.

### t-hamano on 2026-06-19T07:00:10Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4749252198

Update: Since #79172 has been merged, we no longer need to hack the `safe_style_css` filter. I have removed them in 136d5ef4def1531d5350f49e9fbff555115d58b5 and dd1c3d05dac574737a2da3529408bd5037d48441.

### t-hamano on 2026-06-19T07:51:28Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4749577749

> Finally, I want to flag again that I think it would be useful to land https://github.com/WordPress/gutenberg/pull/79116 as a safeguard for the Icon block. It ensures strokes scale like they do today, even as some icons become stroke-based. It would be a temporary safeguard because once the whole set is updated, we would change this to become a toggle instead, so you could choose your appearance, capitalising on the change.

Since the visual changes to the icon block are caused by this PR, I think it is best to add the safeguard directly in this PR.

Let's close #79116 as I've pushed the bd8c908e78eb759854274bc570103d0ff8658774 to this PR.

### jasmussen on 2026-06-19T07:52:16Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4749582669

Thanks for all the help @t-hamano, much appreciated 🏅 

I saw you added [icon-block changes](https://github.com/WordPress/gutenberg/pull/78808/changes/932fcc0f269e47b332c6282a3615ed5f164acbf3..cdade30d21557ee3c43e04bfb441d6ad7e979e65) to this PR. I agree with those changes to safeguard the Icon block, but just noting that means we won't need the separate #79116.

### jasmussen on 2026-06-19T07:52:40Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4749585047

Hah, we commented at the same time. Full agreement! I'll close 79116

### t-hamano on 2026-06-19T09:05:39Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4750143545

As a side note, if this PR is shipped, it might require a dev note, as it could significantly impact consumers using the `@wordpress/icons` package.

- Why were the icons redrawn with a stroke base
- What CSS should be written to maintain the previous appearance
- What are the plans for future icon enhancements

### jasmussen on 2026-06-19T09:28:54Z

URL: https://github.com/WordPress/gutenberg/pull/78808#issuecomment-4750303790

If we need such a dev note, here's a draft:

The icons were redrawn with a stroke base primarily to enable size-independent stroke widths. They use `vector-effect="non-scaling-stroke"`, which keeps stroke thickness constant regardless of the icon's display size, so icons of different sizes (16 / 20 / 24 / 32 px) can be mixed in the same UI and still read as a uniform set. This also lays the foundation for future enhancements: both the design system and the Icon block can gain a way to adjust stroke-width as a design property, and a parallel effort is updating all icons in @wordpress/icons to self-declare their colour via fill="currentColor" so they render correctly without consumers having to inject fill: currentColor via CSS.

No CSS changes should be necessary after this change. Stroke-only icons additionally set an inline style="fill: none" so they remain resilient to ordinary overrides. The one case to watch for is CSS that sets fill on svg (or its children) using `!important`, which will defeat that protection and may cause stroke icons to render as solid shapes. In this case, remove the `!important`. Separately, if your design intentionally relied on the older behaviour where stroke weight scaled with the icon's size, you can restore it by overriding vector-effect:

```
svg :where(path, rect, circle, ellipse, line, polygon, polyline) {
	vector-effect: none;
}
```

## Review threads

### Review thread 1

#### jasmussen on 2026-05-29T13:47:12Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3324730408
Path: packages/components/src/item-group/styles.ts

```diff
@@ -19,8 +19,7 @@ export const unstyledButton = ( as: 'a' | 'button' ) => {
 		text-align: start;
 		text-decoration: ${ as === 'a' ? 'none' : undefined };
 
-		svg,
-		path {
+		svg {
```

This change was necessary because without it, _everything gets a fill_, including what is meant to just be a stroke based circle (with a hole in the middle). 

I prefer to keep PRs minimal, but this one was necessary in order for this PR to be possible to land. I have some additional changes I want to push—a normalisation script and some more resilience, but this was a minimal change I was able to do. I wasn't able to find any ill consequences from this change, but CC'ing some component experts for a gut check: @mirka @ciampo

#### ciampo on 2026-06-09T13:17:40Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3380898578
Path: packages/components/src/item-group/styles.ts

```diff
@@ -19,8 +19,7 @@ export const unstyledButton = ( as: 'a' | 'button' ) => {
 		text-align: start;
 		text-decoration: ${ as === 'a' ? 'none' : undefined };
 
-		svg,
-		path {
+		svg {
```

This should work well

#### tyxla on 2026-06-12T12:26:58Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403315638
Path: packages/components/src/item-group/styles.ts

```diff
@@ -19,8 +19,7 @@ export const unstyledButton = ( as: 'a' | 'button' ) => {
 		text-align: start;
 		text-decoration: ${ as === 'a' ? 'none' : undefined };
 
-		svg,
-		path {
+		svg {
```

I trust @ciampo's judgment here but just wanted to confirm that this has been tested and verified to not cause any unexpected consequences.

#### ciampo on 2026-06-15T12:41:44Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3413503965
Path: packages/components/src/item-group/styles.ts

```diff
@@ -19,8 +19,7 @@ export const unstyledButton = ( as: 'a' | 'button' ) => {
 		text-align: start;
 		text-decoration: ${ as === 'a' ? 'none' : undefined };
 
-		svg,
-		path {
+		svg {
```

To expand a bit:

All in-repo usage shouldn't be affected, because `@wordpress/icons` icons are either:
  - Single filled path (with no associated `fill` rule), where the child `path` will nherit the `fill` rule from the parent `svg` anyway;
  - Stroke-based with explicit `fill: none` as an inline style, which takes precedence


I scanned the codebase for all usages of `ItemGroup` `Item` + SVGs and all first-party usage should be good (still better to run smoke tests, though).


The risk for visual regressions exists but is limited to third-party usage, in case the third-party icon relied on that `path { fill: currentColor }` rule that is being removed here. In particular, I believe the main sources where this regression could manifest are:

- The block list in the Global Styles screen (for third party blocks);
- The sidebar navigation of the recently added `boot` package, for consumer-registered icons

I think we should be good with a CHANGELOG entry. The regression in this case will be purely visual and won't  prevent users from interacting with the Gutenberg app.

### Review thread 2

#### jasmussen on 2026-05-29T13:48:05Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3324735274
Path: packages/edit-site/src/components/sidebar-navigation-item/index.js

```diff
@@ -63,13 +63,7 @@ export default function SidebarNavigationItem( {
 			{ ...props }
 		>
 			<HStack justify="flex-start">
-				{ icon && (
-					<Icon
-						style={ { fill: 'currentcolor' } }
```

This rule overrode the SVG that was inserted, and forced a fill rule on it, making it impossible to use stroke-based icons.

### Review thread 3

#### jasmussen on 2026-05-29T13:51:10Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3324753013
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -262,6 +262,31 @@ function svgToTsx( svgContent ) {
 		}
 	);
 
+	// Convert CSS-string style attributes (e.g. `style="fill: none"`) into JSX
+	// style object syntax (e.g. `style={ { fill: 'none' } }`). Source SVGs use
+	// the CSS-string form so they remain valid SVG when read by non-React
+	// renderers (the PHP path, webpack SVG loaders, raw file preview).
+	jsxContent = jsxContent.replace(
+		/\sstyle="([^"]*)"/g,
+		( _, cssString ) => {
+			const declarations = cssString
+				.split( ';' )
+				.map( ( decl ) => decl.trim() )
+				.filter( Boolean )
+				.map( ( decl ) => {
+					const colonIndex = decl.indexOf( ':' );
+					const key = decl.slice( 0, colonIndex ).trim();
+					const value = decl.slice( colonIndex + 1 ).trim();
+					const camelKey = key.replace( /-([a-z])/g, ( _m, c ) =>
+						c.toUpperCase()
+					);
+					return `${ camelKey }: '${ value }'`;
+				} )
+				.join( ', ' );
+			return ` style={ { ${ declarations } } }`;
```

This change was required so the inline style="fill: none" on stroke icons gets translated to React's object form during build. Without it, React ignores the style and the icons render filled.

Claude Code helped me with this, btw.

### Review thread 4 (resolved)

#### mirka on 2026-05-29T20:03:49Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3326652094
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,10 @@
 
 ## Unreleased
 
+### Bug Fixes
+
+-   `ItemGroup`: Drop the blanket `path { fill: currentColor }` rule that was overriding stroke-based icons' intended fill via inheritance bypass. Old fill-based icons remain coloured via the surrounding `svg { fill: currentColor }` rule plus normal SVG inheritance. ([#78804](https://github.com/WordPress/gutenberg/pull/78804))
```

The PR numbers in all the changelogs are off.

```suggestion
-   `ItemGroup`: Drop the blanket `path { fill: currentColor }` rule that was overriding stroke-based icons' intended fill via inheritance bypass. Old fill-based icons remain coloured via the surrounding `svg { fill: currentColor }` rule plus normal SVG inheritance. ([#78808](https://github.com/WordPress/gutenberg/pull/78808))
```

### Review thread 5

#### mirka on 2026-05-29T20:32:44Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3326778259
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

We're now adding a lot of explicit `currentColor` in the icons themselves. This is going to result in inconsistent behavior across icons.

Here's the icon library in a `color: red` context:

| Before | After |
|--------|-------|
|<img width="1960" height="840" alt="Icon library, red context" src="https://github.com/user-attachments/assets/e08a8a86-5e82-48f2-a602-256b99fd8382" />|<img width="1960" height="840" alt="Icon library, red context" src="https://github.com/user-attachments/assets/20237a5c-7210-42b4-b5f3-29f74f879f80" />|

Unlike `Icon` in either wp-components or wp-icons, the new `Icon` from wp-ui applies a `fill="currentColor"` by default. Here's the icon library modified to use the wp-ui version:

<img width="1958" height="838" alt="Icon library using the Icon primitive from wp-ui" src="https://github.com/user-attachments/assets/5974d547-5a28-48b9-8418-77e4949c61fe" />

If we're going with these stroke-based icons, I think we're probably forced to add explicit `currentColor` in the icons themselves, for **all** the icons, not just the ones that have been redrawn here.

#### jasmussen on 2026-06-01T09:07:55Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3332936818
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

> If we're going with these stroke-based icons, I think we're probably forced to add explicit currentColor in the icons themselves, for all the icons, not just the ones that have been redrawn here.

I volunteer to do that 🙋‍♂️

Before I do, I want to just check with you if you're okay with this? Is it functionally a good choice? IMO yes as this is the implied behavior of the icon set and it's worth making explicit. I also expect to follow up with a PR that adds a script to do this on every build of the set.

#### ciampo on 2026-06-08T13:46:51Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373679492
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

I think it's a good choice. It just has the potential to introduce unexpected visual glitches for consumers receiving this update, but it's probably for the best in the long term and it alings the current behaviour to the new behaviour that `@wordpress/ui`'s `Icon` component already applies

#### ciampo on 2026-06-08T13:50:52Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373707049
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

It will also expose a cleaner way to change the icon color: ie. via CSS `color`, rather than guessing if `fill` or `stroke` should be used

#### jasmussen on 2026-06-08T13:55:41Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373744040
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

I agree. I think it's ultimately a tech enhancement that opens up new opportunities.

In that vein, what are some action items for _this PR_? Do you think we can land this one as-is, or do I need to include all the icons in one go?

#### ciampo on 2026-06-15T12:34:43Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3413463119
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

Probably best not to increase the scope of this PR further, but we should definitely aim to have a coherent behavior for all icons within the same WordPress release (and possible even Gutenberg release)

#### jasmussen on 2026-06-15T12:38:26Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3413484772
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

The icons are easy to redraw, I can do it in days. The problem is this first PR, I'm reluctant to redraw ALL the icons in this PR, if there's a risk this PR doesn't land. 

A note, I will be AFK for the summer vacation starting Tuesday, so if something has to happen for 7.1, it has to be this week. I don't think it **has** to be a 7.1 thing, but sharing that context for clarity regardless.

#### mirka on 2026-06-16T19:38:02Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3423526273
Path: packages/icons/src/library/add-card.svg

```diff
@@ -1,3 +1,3 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.5 5.5V8H20V5.5h2.5V4H20V1.5h-1.5V4H16v1.5h2.5zM12 4H6a2 2 0 00-2 2v12a2 2 0 002 2h12a2 2 0 002-2v-6h-1.5v6a.5.5 0 01-.5.5H6a.5.5 0 01-.5-.5V6a.5.5 0 01.5-.5h6V4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
```

I'd say that "adding explicit `currentColor` to all icons" is a good thing to do, regardless of this redrawing a subset of the icons to strokes.

Still, if we want to keep PRs as small as possible, I think the order needs to be:

1. Add `currentColor` to all the icons.
2. Redraw a subset of the icons to strokes.

Otherwise, landing the stroke redraw first will get the `@wordpress/icons` package in a state where some icons get `currentColor` and some do not. Since the `@wordpress/icons` is a bundled package and not tied to WordPress/Gutenberg releases, the only thing that matters is the package release. We'll want to avoid any situation where there could be a release of the icons package in a mixed color state.

### Review thread 6 (resolved)

#### ciampo on 2026-06-08T14:06:47Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373825312
Path: packages/icons/src/library/cover.svg

```diff
@@ -1,3 +1,5 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.7 3H5.3C4 3 3 4 3 5.3v13.4C3 20 4 21 5.3 21h13.4c1.3 0 2.3-1 2.3-2.3V5.3C21 4 20 3 18.7 3zm.8 15.7c0 .4-.4.8-.8.8H5.3c-.4 0-.8-.4-.8-.8V5.3c0-.4.4-.8.8-.8h6.2v8.9l2.5-3.1 2.5 3.1V4.5h2.2c.4 0 .8.4.8.8v13.4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
+	<path d="M15.75 11.25L14 9L12.25 11.25V3.75H15.75V11.25Z" fill="currentColor" stroke="none" />
+	<path d="M15.75 11.25L14 9L12.25 11.25V3.75H15.75V11.25Z" vector-effect="non-scaling-stroke" />
```

These two `path`s share the same geometry, they can be probably collapsed into one `path` with that geometry, with `fill="currentColor" stroke="currentColor" vector-effect="non-scaling-stroke"`

#### jasmussen on 2026-06-08T15:05:43Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3374266577
Path: packages/icons/src/library/cover.svg

```diff
@@ -1,3 +1,5 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
-	<path d="M18.7 3H5.3C4 3 3 4 3 5.3v13.4C3 20 4 21 5.3 21h13.4c1.3 0 2.3-1 2.3-2.3V5.3C21 4 20 3 18.7 3zm.8 15.7c0 .4-.4.8-.8.8H5.3c-.4 0-.8-.4-.8-.8V5.3c0-.4.4-.8.8-.8h6.2v8.9l2.5-3.1 2.5 3.1V4.5h2.2c.4 0 .8.4.8.8v13.4z" />
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" style="fill: none" stroke="currentColor" stroke-width="1.5">
+	<path d="M15.75 11.25L14 9L12.25 11.25V3.75H15.75V11.25Z" fill="currentColor" stroke="none" />
+	<path d="M15.75 11.25L14 9L12.25 11.25V3.75H15.75V11.25Z" vector-effect="non-scaling-stroke" />
```

I will revisit Cover, good point.

Howver it is likely you will see a couple of SVGs with repeating geometry like this, _intentionally_, such as star-filled, because it's two shapes, one is only the fill, and one is only the stroke. It's late Monday now so I don't recall why these needed to be separate, but I feel like there was a good reason for it.

### Review thread 7 (resolved)

#### ciampo on 2026-06-08T14:07:15Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373828350
Path: packages/icons/CHANGELOG.md

```diff
@@ -5,10 +5,14 @@
 ### Code Quality
 
 -   Add missing `@types/react` dependency. [#78882](https://github.com/WordPress/gutenberg/pull/78882).
+### Enhancements
+
+-   Redraw 35 icons as stroke-based for variable stroke-width support, and introduce the `vector-effect="non-scaling-stroke"` convention so stroke weight stays consistent across icon sizes (originally landed in [#78774](https://github.com/WordPress/gutenberg/pull/78774), reverted in [#78854](https://github.com/WordPress/gutenberg/pull/78854) due to CSS-override regressions, re-introduced here with the resilience fix): `addCard`, `addTemplate`, `cancelCircleFilled`, `caution`, `cautionFilled`, `code`, `commentAuthorAvatar`, `cover`, `currencyDollar`, `currencyEuro`, `currencyPound`, `drafts`, `help`, `helpFilled`, `image`, `info`, `lifesaver`, `link`, `linkOff`, `navigation`, `notAllowed`, `paragraph`, `pending`, `plusCircle`, `plusCircleFilled`, `published`, `scheduled`, `siteLogo`, `square`, `starEmpty`, `starFilled`, `starHalf`, `styles`, `time`, `tip`. ([#78804](https://github.com/WordPress/gutenberg/pull/78804))
```

Wrong PR number linked in this CHANGELOG entry (this is PR 78808)

### Review thread 8 (resolved)

#### ciampo on 2026-06-08T14:07:35Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373830490
Path: packages/icons/CHANGELOG.md

```diff
@@ -5,10 +5,14 @@
 ### Code Quality
 
 -   Add missing `@types/react` dependency. [#78882](https://github.com/WordPress/gutenberg/pull/78882).
+### Enhancements
+
+-   Redraw 35 icons as stroke-based for variable stroke-width support, and introduce the `vector-effect="non-scaling-stroke"` convention so stroke weight stays consistent across icon sizes (originally landed in [#78774](https://github.com/WordPress/gutenberg/pull/78774), reverted in [#78854](https://github.com/WordPress/gutenberg/pull/78854) due to CSS-override regressions, re-introduced here with the resilience fix): `addCard`, `addTemplate`, `cancelCircleFilled`, `caution`, `cautionFilled`, `code`, `commentAuthorAvatar`, `cover`, `currencyDollar`, `currencyEuro`, `currencyPound`, `drafts`, `help`, `helpFilled`, `image`, `info`, `lifesaver`, `link`, `linkOff`, `navigation`, `notAllowed`, `paragraph`, `pending`, `plusCircle`, `plusCircleFilled`, `published`, `scheduled`, `siteLogo`, `square`, `starEmpty`, `starFilled`, `starHalf`, `styles`, `time`, `tip`. ([#78804](https://github.com/WordPress/gutenberg/pull/78804))
 
 ### Breaking Changes
 
 -   Rename `timeToRead` icon to `time`. ([#78804](https://github.com/WordPress/gutenberg/pull/78804))
+-   Stroke-based icons now declare `fill` via inline `style` on the outer `<svg>` instead of the `fill` attribute, so the source's intent survives ordinary 3rd-party CSS overrides like `.foo svg { fill: currentColor }` without using `!important`. ([#78804](https://github.com/WordPress/gutenberg/pull/78804))
```

Same re. wrong PR number

### Review thread 9 (resolved)

#### ciampo on 2026-06-08T14:07:51Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3373832336
Path: packages/edit-site/CHANGELOG.md

```diff
@@ -6,6 +6,10 @@
 
 -   The Site Editor now follows the user's admin color scheme ([#78397](https://github.com/WordPress/gutenberg/pull/78397)).
 
+### Bug Fixes
+
+-   `SidebarNavigationItem`: Stop forcing `style={ { fill: 'currentcolor' } }` on the icon. The explicit override was clobbering stroke-based icons' intrinsic styling. Colour is inherited via CSS `color` and the icon's own declared fills/strokes. ([#78804](https://github.com/WordPress/gutenberg/pull/78804))
```

Same re. wrong PR number

### Review thread 10 (resolved)

#### ciampo on 2026-06-08T14:55:02Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3374194738
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -262,6 +262,28 @@ function svgToTsx( svgContent ) {
 		}
 	);
 
+	// Convert CSS-string style attributes (e.g. `style="fill: none"`) into JSX
+	// style object syntax (e.g. `style={ { fill: 'none' } }`). Source SVGs use
+	// the CSS-string form so they remain valid SVG when read by non-React
+	// renderers (the PHP path, webpack SVG loaders, raw file preview).
+	jsxContent = jsxContent.replace( /\sstyle="([^"]*)"/g, ( _, cssString ) => {
+		const declarations = cssString
+			.split( ';' )
+			.map( ( decl ) => decl.trim() )
+			.filter( Boolean )
+			.map( ( decl ) => {
+				const colonIndex = decl.indexOf( ':' );
+				const key = decl.slice( 0, colonIndex ).trim();
+				const value = decl.slice( colonIndex + 1 ).trim();
+				const camelKey = key.replace( /-([a-z])/g, ( _m, c ) =>
+					c.toUpperCase()
+				);
+				return `${ camelKey }: '${ value }'`;
+			} )
+			.join( ', ' );
+		return ` style={ { ${ declarations } } }`;
+	} );
```

Nit: A couple of suggestions:

- early return for malformed declarations (ie. no colon);
- escape `value` via `JSON.stringify`

```suggestion
	jsxContent = jsxContent.replace( /\sstyle="([^"]*)"/g, ( _, cssString ) => {
		const declarations = cssString
			.split( ';' )
			.map( ( decl ) => decl.trim() )
			.filter( Boolean )
			.map( ( decl ) => {
				const colonIndex = decl.indexOf( ':' );
				// Skip malformed declarations that have no `key: value` colon.
				if ( colonIndex === -1 ) {
					return null;
				}
				const key = decl.slice( 0, colonIndex ).trim();
				const value = decl.slice( colonIndex + 1 ).trim();
				const camelKey = key.replace( /-([a-z])/g, ( _m, c ) =>
					c.toUpperCase()
				);
				// JSON.stringify yields a safely-escaped, double-quoted JS
				// string, so values containing quotes/backslashes stay valid.
				return `${ camelKey }: ${ JSON.stringify( value ) }`;
			} )
			.filter( Boolean )
			.join( ', ' );
		return ` style={ { ${ declarations } } }`;
	} );
```

### Review thread 11

#### westonruter on 2026-06-08T15:18:18Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3374356212
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -262,6 +262,28 @@ function svgToTsx( svgContent ) {
 		}
 	);
 
+	// Convert CSS-string style attributes (e.g. `style="fill: none"`) into JSX
+	// style object syntax (e.g. `style={ { fill: 'none' } }`). Source SVGs use
+	// the CSS-string form so they remain valid SVG when read by non-React
+	// renderers (the PHP path, webpack SVG loaders, raw file preview).
+	jsxContent = jsxContent.replace( /\sstyle="([^"]*)"/g, ( _, cssString ) => {
+		const declarations = cssString
+			.split( ';' )
```

CSS values can have semicolons in them, for example base64-encoded `data:` URLs. Is this a risk here?

### Review thread 12 (resolved)

#### ciampo on 2026-06-09T13:13:50Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3380872244
Path: packages/components/src/icon/index.tsx

For completeness, we should apply a similar fix (ie. merging `style` explicitly) here, too

### Review thread 13 (resolved)

#### ciampo on 2026-06-09T13:14:09Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3380874393
Path: packages/icons/CHANGELOG.md

```diff
@@ -5,10 +5,18 @@
 ### Code Quality
 
 -   Add missing `@types/react` dependency. [#78882](https://github.com/WordPress/gutenberg/pull/78882).
+### Enhancements
```

Nit: spacing

```suggestion

### Enhancements
```

### Review thread 14

#### t-hamano on 2026-06-11T09:09:41Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3394656975
Path: lib/compat/wordpress-7.0/class-wp-icons-registry.php

```diff
@@ -203,7 +217,31 @@ protected function sanitize_icon_content( $icon_content ) {
 					'focusable' => true,
 				),
 			);
-			return wp_kses( $icon_content, $allowed_tags );
+
+			// `wp_kses()` filters the `style` attribute value through
+			// `safecss_filter_attr()`, which only keeps allowlisted CSS properties.
+			// SVG presentation properties such as `fill` (e.g. `style="fill: none"`
+			// on stroke-based icons) are not in the default allowlist, so temporarily
+			// allow them while sanitizing.
+			$allow_svg_css = static function ( $properties ) {
+				return array_merge(
+					$properties,
+					array(
+						'fill',
+						'stroke',
+						'stroke-width',
+						'stroke-linecap',
+						'stroke-linejoin',
+						'stroke-miterlimit',
+					)
+				);
+			};
+
+			add_filter( 'safe_style_css', $allow_svg_css );
+			$sanitized_content = wp_kses( $icon_content, $allowed_tags );
+			remove_filter( 'safe_style_css', $allow_svg_css );
```

This is a temporary fix. I plan to update [the `safe_style_css` filter](https://github.com/t-hamano/wordpress-develop/blob/d08cbfa12bfba43b9d16b8cd53c32d29744a1a18/src/wp-includes/kses.php#L2585-L2587) itself in a separate PR.

#### tyxla on 2026-06-12T12:20:39Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403282263
Path: lib/compat/wordpress-7.0/class-wp-icons-registry.php

```diff
@@ -203,7 +217,31 @@ protected function sanitize_icon_content( $icon_content ) {
 					'focusable' => true,
 				),
 			);
-			return wp_kses( $icon_content, $allowed_tags );
+
+			// `wp_kses()` filters the `style` attribute value through
+			// `safecss_filter_attr()`, which only keeps allowlisted CSS properties.
+			// SVG presentation properties such as `fill` (e.g. `style="fill: none"`
+			// on stroke-based icons) are not in the default allowlist, so temporarily
+			// allow them while sanitizing.
+			$allow_svg_css = static function ( $properties ) {
+				return array_merge(
+					$properties,
+					array(
+						'fill',
+						'stroke',
+						'stroke-width',
+						'stroke-linecap',
+						'stroke-linejoin',
+						'stroke-miterlimit',
+					)
+				);
+			};
+
+			add_filter( 'safe_style_css', $allow_svg_css );
+			$sanitized_content = wp_kses( $icon_content, $allowed_tags );
+			remove_filter( 'safe_style_css', $allow_svg_css );
```

Agreed, this should be addressed. 

Maybe we should declare it in a single place so it is easier to replace later?

Also, let's make sure we add tests. This is an important change that warrants tests.

### Review thread 15 (resolved)

#### westonruter on 2026-06-11T16:38:50Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3397592610
Path: lib/class-wp-icons-registry-gutenberg.php

```diff
@@ -171,6 +171,82 @@ protected function register( $icon_name, $icon_properties ) {
 		return true;
 	}
 
+	/**
+	 * Sanitizes the icon SVG content.
+	 *
+	 * Overrides the base class to allow stroke-related attributes and inline
+	 * styles required by stroke-based icons.
+	 *
+	 * @param string $icon_content The icon SVG content to sanitize.
+	 * @return string The sanitized icon SVG content.
+	 */
+	protected function sanitize_icon_content( $icon_content ) {
```

```suggestion
	protected function sanitize_icon_content( string $icon_content ): string {
```

### Review thread 16 (resolved)

#### westonruter on 2026-06-11T16:40:45Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3397603322
Path: packages/block-library/src/icon/index.php

```diff
@@ -90,7 +90,14 @@ function render_block_core_icon( $attributes ) {
 	$processor->next_tag( 'svg' );
 
 	if ( ! empty( $styles['css'] ) ) {
-		$processor->set_attribute( 'style', $styles['css'] );
+		// Merge with the SVG's intrinsic style (e.g. `fill: none` on
+		// stroke-based icons) so it is preserved. The block styles come last so
+		// they win on any conflicting property.
+		$existing_style = $processor->get_attribute( 'style' );
+		$merged_style   = $existing_style
```

Technically, `style` could be `true` in case no value is provided. So this is safer, although surely not going to be common:

```suggestion
		$merged_style   = is_string( $existing_style )
```

### Review thread 17 (resolved)

#### westonruter on 2026-06-11T16:43:52Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3397621100
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -231,6 +231,52 @@ async function generateIndex() {
 	await writeFile( path.join( ICON_LIBRARY_DIR, 'index.ts' ), indexTemplate );
 }
 
+// Split a CSS declaration list (the contents of a `style` attribute) on `;`,
+// ignoring semicolons that appear inside parentheses (e.g. an `url()` value) or
+// quoted strings (e.g. a base64 `data:` URI). A naive `split( ';' )` would
+// break such values apart.
```

```suggestion
 * Splits a CSS declaration list (the contents of a `style` attribute) on `;`,
 * ignoring semicolons that appear inside parentheses (e.g. an `url()` value) or
 * quoted strings (e.g. a base64 `data:` URI). A naive `split( ';' )` would
 * break such values apart.
 *
 * @param {string} cssString - String of CSS properties.
 * @return {string[]} Declarations.
 */
```

#### westonruter on 2026-06-11T16:52:27Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3397667890
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -231,6 +231,52 @@ async function generateIndex() {
 	await writeFile( path.join( ICON_LIBRARY_DIR, 'index.ts' ), indexTemplate );
 }
 
+// Split a CSS declaration list (the contents of a `style` attribute) on `;`,
+// ignoring semicolons that appear inside parentheses (e.g. an `url()` value) or
+// quoted strings (e.g. a base64 `data:` URI). A naive `split( ';' )` would
+// break such values apart.
```

But see suggestion below where this could instead return `{Map}` or array of tuples as `{Array<[string, string]>}`

### Review thread 18

#### westonruter on 2026-06-11T16:51:08Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3397660659
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -262,6 +308,36 @@ function svgToTsx( svgContent ) {
 		}
 	);
 
+	// Convert CSS-string style attributes (e.g. `style="fill: none"`) into JSX
+	// style object syntax (e.g. `style={ { fill: 'none' } }`). Source SVGs use
+	// the CSS-string form so they remain valid SVG when read by non-React
+	// renderers (the PHP path, webpack SVG loaders, raw file preview).
+	jsxContent = jsxContent.replace( /\sstyle="([^"]*)"/g, ( _, cssString ) => {
+		const declarations = splitStyleDeclarations( cssString )
+			.map( ( decl ) => decl.trim() )
+			.filter( Boolean )
+			.map( ( decl ) => {
+				// Split on the first colon only: values can contain colons too
+				// (e.g. a `data:` URI), so only the first separates key/value.
+				const colonIndex = decl.indexOf( ':' );
```

Since `splitStyleDeclarations` is already going to the trouble of tokenizing the CSS, it would seem preferable to have it return a `Map` of properties, or an array of `[ key, value ]` tuples. Then this wouldn't have to try to parse out the value here.

#### jasmussen on 2026-06-12T10:12:07Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3402479346
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -262,6 +308,36 @@ function svgToTsx( svgContent ) {
 		}
 	);
 
+	// Convert CSS-string style attributes (e.g. `style="fill: none"`) into JSX
+	// style object syntax (e.g. `style={ { fill: 'none' } }`). Source SVGs use
+	// the CSS-string form so they remain valid SVG when read by non-React
+	// renderers (the PHP path, webpack SVG loaders, raw file preview).
+	jsxContent = jsxContent.replace( /\sstyle="([^"]*)"/g, ( _, cssString ) => {
+		const declarations = splitStyleDeclarations( cssString )
+			.map( ( decl ) => decl.trim() )
+			.filter( Boolean )
+			.map( ( decl ) => {
+				// Split on the first colon only: values can contain colons too
+				// (e.g. a `data:` URI), so only the first separates key/value.
+				const colonIndex = decl.indexOf( ':' );
```

Thanks for all the suggestions. I've tried to apply them, and address the feedback here. Let me know if I got this right.

### Review thread 19

#### tyxla on 2026-06-12T12:22:08Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403290294
Path: lib/compat/wordpress-7.0/class-wp-icons-registry.php

```diff
@@ -180,20 +180,34 @@ protected function register( $icon_name, $icon_properties ) {
 		protected function sanitize_icon_content( $icon_content ) {
 			$allowed_tags = array(
 				'svg'     => array(
-					'class'       => true,
-					'xmlns'       => true,
-					'width'       => true,
-					'height'      => true,
-					'viewbox'     => true,
-					'aria-hidden' => true,
-					'role'        => true,
-					'focusable'   => true,
+					'class'             => true,
+					'xmlns'             => true,
+					'width'             => true,
+					'height'            => true,
+					'viewbox'           => true,
+					'aria-hidden'       => true,
+					'role'              => true,
+					'focusable'         => true,
+					'style'             => true,
+					'stroke'            => true,
+					'stroke-width'      => true,
+					'stroke-linecap'    => true,
+					'stroke-linejoin'   => true,
+					'stroke-miterlimit' => true,
+					'vector-effect'     => true,
 				),
 				'path'    => array(
-					'fill'      => true,
-					'fill-rule' => true,
-					'd'         => true,
-					'transform' => true,
+					'fill'              => true,
+					'fill-rule'         => true,
+					'd'                 => true,
+					'transform'         => true,
+					'style'             => true,
+					'stroke'            => true,
+					'stroke-width'      => true,
+					'stroke-linecap'    => true,
+					'stroke-linejoin'   => true,
+					'stroke-miterlimit' => true,
+					'vector-effect'     => true,
 				),
 				'polygon' => array(
```

Any reason to miss adding the above attributes to `polygon` as well?

### Review thread 20

#### tyxla on 2026-06-12T12:23:44Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403298714
Path: lib/class-wp-icons-registry-gutenberg.php

```diff
@@ -171,6 +171,82 @@ protected function register( $icon_name, $icon_properties ) {
 		return true;
 	}
 
+	/**
+	 * Sanitizes the icon SVG content.
+	 *
+	 * Overrides the base class to allow stroke-related attributes and inline
+	 * styles required by stroke-based icons.
+	 *
+	 * @param string $icon_content The icon SVG content to sanitize.
+	 * @return string The sanitized icon SVG content.
+	 */
+	protected function sanitize_icon_content( string $icon_content ): string {
```

Is there a better way to avoid this one being duplicated in multiple places?

### Review thread 21

#### tyxla on 2026-06-12T12:25:31Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403307875
Path: packages/block-library/src/icon/index.php

```diff
@@ -90,7 +90,14 @@ function render_block_core_icon( $attributes ) {
 	$processor->next_tag( 'svg' );
 
 	if ( ! empty( $styles['css'] ) ) {
-		$processor->set_attribute( 'style', $styles['css'] );
+		// Merge with the SVG's intrinsic style (e.g. `fill: none` on
+		// stroke-based icons) so it is preserved. The block styles come last so
+		// they win on any conflicting property.
+		$existing_style = $processor->get_attribute( 'style' );
+		$merged_style   = is_string( $existing_style )
```

Should we check the length of the style attribute too? If it's an empty attribute, won't we get a leading `;`?

### Review thread 22

#### tyxla on 2026-06-12T12:29:29Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403331175
Path: packages/components/src/icon/index.tsx

```diff
@@ -93,23 +93,39 @@ function Icon( {
 	}
 
 	if ( icon && ( icon.type === 'svg' || icon.type === SVG ) ) {
+		const { style: consumerStyle, ...restProps } =
+			additionalProps as SVGProps< SVGSVGElement >;
+
 		const appliedProps = {
 			...icon.props,
 			width: size,
 			height: size,
-			...additionalProps,
+			...restProps,
+			// Merge styles so the icon's intrinsic style (e.g. `fill: none` on
+			// stroke-based icons) is preserved unless the consumer overrides
+			// the same property explicitly.
+			style: { ...icon.props.style, ...consumerStyle },
 		};
 
 		return <SVG { ...appliedProps } />;
 	}
 
 	if ( isValidElement( icon ) ) {
+		const { style: consumerStyle, ...restProps } =
+			additionalProps as SVGProps< SVGSVGElement >;
+
 		return cloneElement( icon, {
 			// @ts-ignore Just forwarding the size prop along
 			size,
 			width: size,
 			height: size,
-			...additionalProps,
+			// Merge styles so the icon's intrinsic style is preserved unless
+			// the consumer overrides the same property explicitly.
+			style: {
+				...( icon.props as { style?: CSSProperties } ).style,
+				...consumerStyle,
+			},
+			...restProps,
```

For consistency, we might want to move `...restProps` above `style` here, or the other way around in the other place.

### Review thread 23

#### tyxla on 2026-06-12T12:30:14Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403335286
Path: packages/block-library/src/icon/index.php

```diff
@@ -90,7 +90,14 @@ function render_block_core_icon( $attributes ) {
 	$processor->next_tag( 'svg' );
 
 	if ( ! empty( $styles['css'] ) ) {
-		$processor->set_attribute( 'style', $styles['css'] );
+		// Merge with the SVG's intrinsic style (e.g. `fill: none` on
+		// stroke-based icons) so it is preserved. The block styles come last so
+		// they win on any conflicting property.
+		$existing_style = $processor->get_attribute( 'style' );
+		$merged_style   = is_string( $existing_style )
+			? rtrim( trim( $existing_style ), ';' ) . '; ' . $styles['css']
+			: $styles['css'];
+		$processor->set_attribute( 'style', $merged_style );
```

May be something else that needs a test.

### Review thread 24

#### tyxla on 2026-06-12T12:31:21Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3403340926
Path: lib/class-wp-icons-registry-gutenberg.php

```diff
@@ -171,6 +171,82 @@ protected function register( $icon_name, $icon_properties ) {
 		return true;
 	}
 
+	/**
+	 * Sanitizes the icon SVG content.
+	 *
+	 * Overrides the base class to allow stroke-related attributes and inline
+	 * styles required by stroke-based icons.
+	 *
+	 * @param string $icon_content The icon SVG content to sanitize.
+	 * @return string The sanitized icon SVG content.
+	 */
+	protected function sanitize_icon_content( string $icon_content ): string {
```

Those changes may need tests

### Review thread 25

#### westonruter on 2026-06-16T03:26:56Z

URL: https://github.com/WordPress/gutenberg/pull/78808#discussion_r3417925932
Path: packages/icons/lib/generate-library.cjs

```diff
@@ -262,6 +339,24 @@ function svgToTsx( svgContent ) {
 		}
 	);
 
+	// Convert CSS-string style attributes (e.g. `style="fill: none"`) into JSX
+	// style object syntax (e.g. `style={ { fill: 'none' } }`). Source SVGs use
+	// the CSS-string form so they remain valid SVG when read by non-React
+	// renderers (the PHP path, webpack SVG loaders, raw file preview).
+	jsxContent = jsxContent.replace( /\sstyle="([^"]*)"/g, ( _, cssString ) => {
+		const declarations = parseStyleDeclarations( cssString )
+			.map( ( [ key, value ] ) => {
+				const camelKey = key.replace( /-([a-z])/g, ( _m, c ) =>
```

I believe this isn't accounting for CSS custom properties, like `--wp-admin-theme-color`. This should come out as `wpAdminThemeColor`, but currently it is coming out as `-WpAdminThemeColor`, right?

```suggestion
				const camelKey = key.replace(/^--/, '').replace( /-([a-z])/g, ( _m, c ) =>
```

## Changed files

- lib/class-wp-icons-registry-gutenberg.php (MODIFIED, +57/-0)
- lib/compat/wordpress-7.0/class-wp-icons-registry.php (MODIFIED, +26/-12)
- packages/block-editor/src/components/block-icon/test/__snapshots__/index.js.snap (MODIFIED, +6/-1)
- packages/block-library/src/icon/index.php (MODIFIED, +11/-1)
- packages/block-library/src/icon/style.scss (MODIFIED, +7/-0)
- packages/components/CHANGELOG.md (MODIFIED, +5/-0)
- packages/components/src/icon/index.tsx (MODIFIED, +19/-3)
- packages/components/src/icon/test/index.tsx (MODIFIED, +53/-0)
- packages/components/src/item-group/styles.ts (MODIFIED, +1/-2)
- packages/components/src/item-group/test/__snapshots__/index.js.snap (MODIFIED, +15/-15)
- packages/edit-site/CHANGELOG.md (MODIFIED, +4/-0)
- packages/edit-site/src/components/sidebar-navigation-item/index.js (MODIFIED, +1/-7)
- packages/icons/CHANGELOG.md (MODIFIED, +10/-1)
- packages/icons/lib/generate-library.cjs (MODIFIED, +98/-0)
- packages/icons/lib/test/generate-library.js (ADDED, +138/-0)
- packages/icons/src/icon/index.ts (MODIFIED, +9/-2)
- packages/icons/src/library/add-card.svg (MODIFIED, +2/-2)
- packages/icons/src/library/add-template.svg (MODIFIED, +2/-2)
- packages/icons/src/library/cancel-circle-filled.svg (MODIFIED, +3/-2)
- packages/icons/src/library/caution-filled.svg (MODIFIED, +3/-2)
- packages/icons/src/library/caution.svg (MODIFIED, +2/-2)
- packages/icons/src/library/code.svg (MODIFIED, +2/-2)
- packages/icons/src/library/comment-author-avatar.svg (MODIFIED, +4/-2)
- packages/icons/src/library/cover.svg (MODIFIED, +3/-2)
- packages/icons/src/library/currency-dollar.svg (MODIFIED, +3/-2)
- packages/icons/src/library/currency-euro.svg (MODIFIED, +3/-2)
- packages/icons/src/library/currency-pound.svg (MODIFIED, +3/-2)
- packages/icons/src/library/drafts.svg (MODIFIED, +3/-2)
- packages/icons/src/library/help-filled.svg (MODIFIED, +3/-2)
- packages/icons/src/library/help.svg (MODIFIED, +3/-2)
- packages/icons/src/library/image.svg (MODIFIED, +2/-2)
- packages/icons/src/library/info.svg (MODIFIED, +2/-2)
- packages/icons/src/library/lifesaver.svg (MODIFIED, +11/-2)
- packages/icons/src/library/link-off.svg (MODIFIED, +2/-2)
- packages/icons/src/library/link.svg (MODIFIED, +2/-2)
- packages/icons/src/library/navigation.svg (MODIFIED, +3/-2)
- packages/icons/src/library/not-allowed.svg (MODIFIED, +2/-2)
- packages/icons/src/library/paragraph.svg (MODIFIED, +3/-2)
- packages/icons/src/library/pending.svg (MODIFIED, +3/-2)
- packages/icons/src/library/plus-circle-filled.svg (MODIFIED, +3/-2)
- packages/icons/src/library/plus-circle.svg (MODIFIED, +3/-2)
- packages/icons/src/library/published.svg (MODIFIED, +3/-2)
- packages/icons/src/library/scheduled.svg (MODIFIED, +3/-2)
- packages/icons/src/library/site-logo.svg (MODIFIED, +2/-2)
- packages/icons/src/library/square.svg (MODIFIED, +2/-2)
- packages/icons/src/library/star-empty.svg (MODIFIED, +2/-2)
- packages/icons/src/library/star-filled.svg (MODIFIED, +2/-2)
- packages/icons/src/library/star-half.svg (MODIFIED, +3/-2)
- packages/icons/src/library/styles.svg (MODIFIED, +3/-2)
- packages/icons/src/library/time.svg (MODIFIED, +3/-2)
- packages/icons/src/library/tip.svg (MODIFIED, +2/-2)
