---
number: 65892
type: pull_request
state: MERGED
merged_at: 2024-10-09T19:27:55Z
url: https://github.com/WordPress/gutenberg/pull/65892
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Focus] Accessibility (a11y)","[Package] Components","Backported to WP Core"]
---

# PR #65892: ToggleGroupControl: Don't autoselect option on first group focus

- URL: https://github.com/WordPress/gutenberg/pull/65892
- Author: mirka
- Created: 2024-10-05T23:55:24Z
- Updated: 2024-10-14T02:40:30Z
- Comments: 3 of 3
- Review threads: 2 of 2
- Changed files: 6 of 6
- Diff: ../pr-diffs/65892.diff

## Body

Fixes #62981

## What?

Fixes a bug in ToggleGroupControl where the first <kbd>Tab</kbd> focus to a toggle group with an unselected option automatically selected the first option.

## Why?

This isn't how a radio group should work. When controlled by keyboard, only an arrow key, space, or enter key should select an option when the radio group doesn't yet have an option selected.

## Testing Instructions

See the ToggleGroupControl story in Storybook. It should work as expected via keyboard and via mouse. (The mechanics should basically mirror how a native radio group works — compare with an [MDN example](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/radio#data_representation_of_a_radio_group).)

## Screenshots or screencast <!-- if applicable -->

https://github.com/user-attachments/assets/f3e4d322-97f7-49c1-9ee0-0f8c2496c2e5

## Issue comments

### github-actions on 2024-10-07T12:35:26Z

URL: https://github.com/WordPress/gutenberg/pull/65892#issuecomment-2396808260

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: jeryj <jeryj@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### ciampo on 2024-10-07T22:18:51Z

URL: https://github.com/WordPress/gutenberg/pull/65892#issuecomment-2398021432

I wonder if we should show a visual hint for focused but not selected option items — ie. what happens when moving focus to the `ToggleGroupControl` while there's no selected option item

### github-actions on 2024-10-09T19:29:13Z

URL: https://github.com/WordPress/gutenberg/pull/65892#issuecomment-2403277053

There was a conflict while trying to cherry-pick the commit to the wp/6.7 branch. Please resolve the conflict manually and create a PR to the wp/6.7 branch.

  PRs to wp/6.7 are similar to PRs to trunk, but you should base your PR on the wp/6.7 branch instead of trunk.

  ```
  # Checkout the wp/6.7 branch instead of trunk.
  git checkout wp/6.7
  # Create a new branch for your PR.
  git checkout -b my-branch
  # Cherry-pick the commit.
  git cherry-pick e86152bc8b45d5fe213ed688f145e82f73bb2fd4
  # Check which files have conflicts.
  git status
  # Resolve the conflict...
  # Add the resolved files to the staging area.
  git status
  git add .
  git cherry-pick --continue
  # Push the branch to the repository
  git push origin my-branch
  # Create a PR and set the base to the wp/6.7 branch.
  # See https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/changing-the-base-branch-of-a-pull-request.
  ```

## Review threads

### Review thread 1

#### mirka on 2024-10-05T23:59:21Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1788762105
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

This `activeItemIsNotFirstItem()` check will only trigger once in the toggle group's lifecycle, as it will have a non-null `value` in subsequent renders.

#### ciampo on 2024-10-07T13:32:42Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1790236725
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

I'm not sure I understand the logic here — if the current `value` is `null`, why would we call `setValue` if the active item is not the first item?

#### mirka on 2024-10-07T13:47:18Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1790262799
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

The logic in trunk was that, whenever an option is focused (i.e. becomes the active item), set that value in the Ariakit store explicitly (because we're managing this component in controlled mode). We want to prevent this behavior when there is no value selected yet and the active item is the first item. Once the active item is the second item, that means the user not only "focused on the ToggleGroupControl" but also actively selected an option, so we are safe to start setting values. This is how native radios behave.

Hope that made better sense, but also I'd like your input on how to improve the code comment so it is easier to understand.

#### ciampo on 2024-10-07T19:42:00Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1790779958
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

Is the `toggleGroupControlContext.activeItemIsNotFirstItem` check necessary, though? Does it matter which item is the active item?

#### mirka on 2024-10-07T20:19:48Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1790823928
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

Yes. See for example the default example for [Ariakit Radio](https://ariakit.org/components/radio). When the focus first enters the radio group, the active item is the first item ("apple"). It's only when the active item becomes something else ("orange") that we want to start setting values.

https://github.com/user-attachments/assets/1190d90c-96b1-4f25-b58a-b4e94816505e

Once we start setting values (i.e. the `value` is non-null), then it no longer matters which is the active item.

#### ciampo on 2024-10-07T22:17:47Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1790946339
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

Got it. I wish we could rely on the built-in behavior of the ariakit `Radio` component, without the need for us to re-implement the logic (I guess we can't since we're not using native `input type="radio"` elements?)

Also, if we even want to tweak the initial position of the active element before a selection is made, this solution won't cut it - but it's unlikely, so that doesn't seem like a big issue

#### mirka on 2024-10-08T19:46:38Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1792418815
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

Right, I experimented a bit, and we can't rely on the built-in behavior if we use `button`. I think it is ultimately possible to rewrite it so we use `input type="radio"`, but that would be a larger refactor with possibly breaking changes (not sure).

#### ciampo on 2024-10-08T22:50:29Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1792592757
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
-									toggleGroupControlContext.setValue( value );
-								} }
-							/>
-						}
+						onFocusVisible={ () => {
+							// Conditions ensure that the first visible focus to a radio group
+							// without a selected option will not automatically select the option.
+							if (
+								toggleGroupControlContext.value !== null ||
+								toggleGroupControlContext.activeItemIsNotFirstItem?.()
```

Sounds good — we may want to consider a v2 with such changes when we have the new design spec

### Review thread 2

#### tyxla on 2024-10-08T12:14:48Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1791764546
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
```

We used to check if the event was `defaultPrevented` or not - is this something we want to keep, and if not, why?

#### mirka on 2024-10-08T19:47:53Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1792420281
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
```

I can't see why we would need it, especially now that we're using `onFocusVisible` instead of `onFocus`. @ciampo Do you remember why you initially prevented the default (#50278)?

#### ciampo on 2024-10-08T22:50:07Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1792592466
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
```

I can't find a specific reason, I believe it was just to respect a consumer wanting to `preventDefault`. I don't think we need it, as you say, since we're not using `onFocus` anymore

#### tyxla on 2024-10-09T08:04:36Z

URL: https://github.com/WordPress/gutenberg/pull/65892#discussion_r1793061997
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -142,19 +140,17 @@ function ToggleGroupControlOptionBase(
 				) : (
 					<Ariakit.Radio
 						disabled={ disabled }
-						render={
-							<button
-								type="button"
-								{ ...commonProps }
-								onFocus={ ( event ) => {
-									onFocusProp?.( event );
-									if ( event.defaultPrevented ) {
-										return;
-									}
```

Thanks for confirming, folks 👍

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/toggle-group-control/test/__snapshots__/index.tsx.snap (MODIFIED, +6/-6)
- packages/components/src/toggle-group-control/test/index.tsx (MODIFIED, +13/-0)
- packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx (MODIFIED, +11/-15)
- packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx (MODIFIED, +3/-0)
- packages/components/src/toggle-group-control/types.ts (MODIFIED, +1/-0)
