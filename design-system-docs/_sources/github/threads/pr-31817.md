---
number: 31817
type: pull_request
state: MERGED
merged_at: 2021-05-26T13:45:55Z
url: https://github.com/WordPress/gutenberg/pull/31817
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Type] Code Quality","[Package] Components","[Feature] Component System"]
---

# PR #31817: Components: require explicit `children` prop for all components

- URL: https://github.com/WordPress/gutenberg/pull/31817
- Author: ciampo
- Created: 2021-05-14T07:33:30Z
- Updated: 2021-05-26T14:00:02Z
- Comments: 6 of 6
- Review threads: 8 of 8
- Changed files: 18 of 18
- Diff: ../pr-diffs/31817.diff

## Body

<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/repository-management.md#pull-requests. -->

<!-- Gutenberg's license is in the process of updating to be dual-licensed under the GPL and MPL. As part of that transition, all new contributions are dual-licensed. For more information, see: https://github.com/WordPress/gutenberg/blob/trunk/LICENSE.md -->

## Description
<!-- Please describe what you have changed or added -->

As part of the tasks listed in #30503, this PR aims at removing the implicit `children` prop from `ViewOwnProps` and from the `useContentSystem` hook. 

The `children` prop is added to all components that require it explicitly, instead of relying on the implicit definition.

## Testing instructions
<!-- Please describe in detail how you tested your changes. -->
<!-- Include details of your testing environment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->

- [ ] The project builds correctly and all tests pass
- [ ] Run local storybook `npm run storybook:dev` and compare the components against what's currently in [production](https://wordpress.github.io/gutenberg)
- [ ] Play around with components code and manually check the type of the `children` prop

## Screenshots <!-- if applicable -->

N/A

## Types of changes
<!-- What types of changes does your code introduce?  -->
<!-- Bug fix (non-breaking change which fixes an issue) -->
<!-- New feature (non-breaking change which adds functionality) -->
<!-- Breaking change (fix or feature that would cause existing functionality to not work as expected) -->

Refactor

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- N/A I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- N/A I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- N/A I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->

## Issue comments

### ciampo on 2021-05-14T07:34:28Z

URL: https://github.com/WordPress/gutenberg/pull/31817#issuecomment-841071193

Sorry for the ping, @ajitbohra and @chrisvanpatten  — forgot to mark this as Draft from the start. You can ignore this PR for now

### ciampo on 2021-05-14T07:57:36Z

URL: https://github.com/WordPress/gutenberg/pull/31817#issuecomment-841082052

Hey @sarayourfriend , just leaving a few notes here for the next time we discuss this issue.

### Removing the implicit `children` props

As agreed, I've started by removing the explicit `children` prop from `ViewOwnProps` in `packages/components/src/ui/context/polymorphic-component.ts` (see [footnote 1](https://github.com/WordPress/gutenberg/pull/31817#discussion_r632346031))

But then, when working on each individual components, I realised that TS was still computing the `children` prop as part of `ViewOwnProps`. I figured that potentially `React.ComponentPropsWithRef` includes the `children` props too, and so I've `omit`-ted the `children` prop explicitly (see [footnote 2](https://github.com/WordPress/gutenberg/pull/31817#discussion_r632346099)).

Not sure if adding `children: never` to `ViewOwnProps` is a better solution here — what do you think?

Finally, I noticed that the `children` prop was still somehow expected by TS / intellisense on all components that use the Context system — and then I realised that the `ConnectedProps` `typedef` also includes the `children` prop. I tried removing that too (see [footnote 3](https://github.com/WordPress/gutenberg/pull/31817#discussion_r632346162)), but that causes a few build errors that I haven't had the time to investigate more in-depth yet.

### Adding explicit `children` props to components

I also started adding explicit `children` props to all components that needed it.

In case a component's Props are an extension of another component's, I've only added `children` to the base props (e.g. I've added `children` to the Flex component's props, but not to `VStack` and `HStack` since they extend those props)

Lastly, I've left a few `TODO` comments when I wasn't sure about the approach to take in specific situations.

### sarayourfriend on 2021-05-17T15:09:51Z

URL: https://github.com/WordPress/gutenberg/pull/31817#issuecomment-842403864

> Not sure if adding children: never to ViewOwnProps is a better solution here — what do you think?

I think omitting it is the right way to go. Adding `children: never` seems like a hack, right?

I'll take a look at the rest soon.

Thanks for starting this PR!

### sarayourfriend on 2021-05-17T15:22:42Z

URL: https://github.com/WordPress/gutenberg/pull/31817#issuecomment-842414062

@ciampo I believe we may just be able to remove the stuff about `children` in `useContextSystem`. I'm not sure how this "renderChildren" override is supposed to work and it doesn't appear to be used anywhere in the G2 repository. It's also going to be a huge pain if not nearly impossible to correctly type that stuff, so I think it's best if we just remove that functionality completely. It seems just like overcomplicating something there, especially if it's not actually used.

> But then, when working on each individual components, I realised that TS was still computing the children prop as part of ViewOwnProps. I figured that potentially React.ComponentPropsWithRef includes the children props too, and so I've omit-ted the children prop explicitly (see footnote 2).

You're correct, it comes from `ComponentProps<'div'>` for example, where the `div` will have `children`. I think omitting it is the correct option here even though it's also kind of hacky still.

### ciampo on 2021-05-21T15:52:09Z

URL: https://github.com/WordPress/gutenberg/pull/31817#issuecomment-846053455

@sarayourfriend I believe this PR is now ready for a final round of review

### ciampo on 2021-05-26T13:13:39Z

URL: https://github.com/WordPress/gutenberg/pull/31817#issuecomment-848759425

@sarayourfriend could you give a final review round? All tests are passing :)

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2021-05-14T07:42:43Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r632346031
Path: packages/components/src/ui/context/polymorphic-component.ts

```diff
@@ -3,16 +3,20 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as React from 'react';
-import type { As, RenderProp, ExtractHTMLAttributes } from 'reakit-utils/types';
+import type {
+	As,
+	// RenderProp,
+	// ExtractHTMLAttributes
+} from 'reakit-utils/types';
 import type { Interpolation } from 'create-emotion';
 
 /**
  * Based on https://github.com/reakit/reakit/blob/master/packages/reakit-utils/src/types.ts
  */
 export type ViewOwnProps< P, T extends As > = P &
-	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P > & {
+	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P | 'children' > & {
 		as?: T | keyof JSX.IntrinsicElements;
-		children?: React.ReactNode | RenderProp< ExtractHTMLAttributes< any > >;
+		// children?: React.ReactNode | RenderProp< ExtractHTMLAttributes< any > >;
```

Footnote 1

### Review thread 2 (resolved)

#### ciampo on 2021-05-14T07:42:51Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r632346099
Path: packages/components/src/ui/context/polymorphic-component.ts

```diff
@@ -3,16 +3,20 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as React from 'react';
-import type { As, RenderProp, ExtractHTMLAttributes } from 'reakit-utils/types';
+import type {
+	As,
+	// RenderProp,
+	// ExtractHTMLAttributes
+} from 'reakit-utils/types';
 import type { Interpolation } from 'create-emotion';
 
 /**
  * Based on https://github.com/reakit/reakit/blob/master/packages/reakit-utils/src/types.ts
  */
 export type ViewOwnProps< P, T extends As > = P &
-	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P > & {
+	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P | 'children' > & {
```

Footnote 2

### Review thread 3 (resolved)

#### ciampo on 2021-05-14T07:43:00Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r632346162
Path: packages/components/src/ui/context/use-context-system.js

```diff
@@ -15,12 +15,10 @@ import { useComponentsContext } from './context-system-provider';
 import { getNamespace, getConnectedNamespace } from './utils';
 import { getStyledClassNameFromKey } from './get-styled-class-name-from-key';
 
-/* eslint-disable jsdoc/valid-types */
 /**
  * @template TProps
- * @typedef {TProps & { className: string; children?: import('react').ReactNode }} ConnectedProps
```

Footnote 3

### Review thread 4 (resolved)

#### sarayourfriend on 2021-05-19T17:39:56Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r635454368
Path: packages/components/src/draggable/index.js

```diff
@@ -17,7 +17,7 @@ const bodyClass = 'is-dragging-components-draggable';
 
 /**
  * @typedef Props
- * @property {(props: RenderProp) => JSX.Element | null} children Children.
+ * @property {(props: RenderProp) => JSX.Element | null} children Children. TODO: any action needed here?
```

`draggable` is currently outside of the `ViewOwnProps` context system so we don't need to worry about it.

#### ciampo on 2021-05-20T11:03:28Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r635998766
Path: packages/components/src/draggable/index.js

```diff
@@ -17,7 +17,7 @@ const bodyClass = 'is-dragging-components-draggable';
 
 /**
  * @typedef Props
- * @property {(props: RenderProp) => JSX.Element | null} children Children.
+ * @property {(props: RenderProp) => JSX.Element | null} children Children. TODO: any action needed here?
```

Done in https://github.com/WordPress/gutenberg/pull/31817/commits/e250f060a87d5ecfb483d9cdd08c7e4d35df6d3e

### Review thread 5 (resolved)

#### sarayourfriend on 2021-05-19T17:40:53Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r635455052
Path: packages/components/src/ui/context/polymorphic-component.ts

```diff
@@ -3,16 +3,15 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as React from 'react';
-import type { As, RenderProp, ExtractHTMLAttributes } from 'reakit-utils/types';
+import type { As } from 'reakit-utils/types';
 import type { Interpolation } from 'create-emotion';
 
 /**
  * Based on https://github.com/reakit/reakit/blob/master/packages/reakit-utils/src/types.ts
  */
 export type ViewOwnProps< P, T extends As > = P &
-	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P > & {
+	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P | 'children' > & {
```

Maybe we should add a comment noting why we're omitting `children` in this case? Just something saying that `ComponentPropsWithRef` will add it implicitly and we want to make sure it's explicit when needed.

#### ciampo on 2021-05-20T11:03:46Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r635998925
Path: packages/components/src/ui/context/polymorphic-component.ts

```diff
@@ -3,16 +3,15 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as React from 'react';
-import type { As, RenderProp, ExtractHTMLAttributes } from 'reakit-utils/types';
+import type { As } from 'reakit-utils/types';
 import type { Interpolation } from 'create-emotion';
 
 /**
  * Based on https://github.com/reakit/reakit/blob/master/packages/reakit-utils/src/types.ts
  */
 export type ViewOwnProps< P, T extends As > = P &
-	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P > & {
+	Omit< React.ComponentPropsWithRef< T >, 'as' | keyof P | 'children' > & {
```

Done in https://github.com/WordPress/gutenberg/pull/31817/commits/8011e6d835f75c1102a1147def7ef254f75f1217

### Review thread 6 (resolved)

#### sarayourfriend on 2021-05-19T17:41:16Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r635455306
Path: packages/components/src/ui/control-group/types.ts

```diff
@@ -12,7 +12,7 @@ import type { FlexProps } from '../../flex/types';
 export type ControlGroupContext = {
 	isFirst?: boolean;
 	isLast?: boolean;
-	isMidde?: boolean;
+	isMiddle?: boolean;
```

😅 I'm surprised this didn't cause an error before?

#### ciampo on 2021-05-20T11:08:02Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r636001462
Path: packages/components/src/ui/control-group/types.ts

```diff
@@ -12,7 +12,7 @@ import type { FlexProps } from '../../flex/types';
 export type ControlGroupContext = {
 	isFirst?: boolean;
 	isLast?: boolean;
-	isMidde?: boolean;
+	isMiddle?: boolean;
```

It looks like the repo doesn't contain any usage of the `<ControlGroup />` component, and that there are no specific tests for the `ControlGroupContext`.

Maybe we should add a test for that in a separate PR?

### Review thread 7 (resolved)

#### ciampo on 2021-05-21T14:21:51Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r636959520
Path: packages/components/src/ui/context/polymorphic-component.ts

```diff
@@ -3,16 +3,19 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as React from 'react';
-import type { As, RenderProp, ExtractHTMLAttributes } from 'reakit-utils/types';
+import type { As } from 'reakit-utils/types';
```

This can also be replaced with the vanilla React equivalent `ElementType`

#### ciampo on 2021-05-21T15:29:53Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r637010652
Path: packages/components/src/ui/context/polymorphic-component.ts

```diff
@@ -3,16 +3,19 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type * as React from 'react';
-import type { As, RenderProp, ExtractHTMLAttributes } from 'reakit-utils/types';
+import type { As } from 'reakit-utils/types';
```

Done in https://github.com/WordPress/gutenberg/pull/31817/commits/b071cf061a0df939fbae9de2e97f4f087fb20686 — cc @sarayourfriend  and @diegohaz is this what you had in mind too?

### Review thread 8 (resolved)

#### sarayourfriend on 2021-05-21T15:56:15Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r637030983
Path: packages/components/src/ui/visually-hidden/hook.js

```diff
@@ -12,7 +12,7 @@ import * as styles from './styles';
 /** @typedef {import('../context').PolymorphicComponentProps<{}, 'div'>} Props */
 
 /**
- * @param {import('../context').PolymorphicComponentProps<{}, 'div'>} props
+ * @param {import('../context').PolymorphicComponentProps<{ children?: import('react').ReactNode }, 'div'>} props
```

Should `children` be required on `VisuallyHidden`?

```suggestion
 * @param {import('../context').PolymorphicComponentProps<{ children: import('react').ReactNode }, 'div'>} props
```

#### ciampo on 2021-05-24T15:57:40Z

URL: https://github.com/WordPress/gutenberg/pull/31817#discussion_r638073812
Path: packages/components/src/ui/visually-hidden/hook.js

```diff
@@ -12,7 +12,7 @@ import * as styles from './styles';
 /** @typedef {import('../context').PolymorphicComponentProps<{}, 'div'>} Props */
 
 /**
- * @param {import('../context').PolymorphicComponentProps<{}, 'div'>} props
+ * @param {import('../context').PolymorphicComponentProps<{ children?: import('react').ReactNode }, 'div'>} props
```

Done in https://github.com/WordPress/gutenberg/pull/31817/commits/63fa373cc12e15dfa92f87416e1d2266c05575d9

## Changed files

- packages/components/src/divider/component.tsx (MODIFIED, +1/-1)
- packages/components/src/elevation/types.ts (MODIFIED, +0/-1)
- packages/components/src/flex/types.ts (MODIFIED, +8/-0)
- packages/components/src/grid/types.ts (MODIFIED, +4/-0)
- packages/components/src/spacer/hook.ts (MODIFIED, +4/-0)
- packages/components/src/truncate/types.ts (MODIFIED, +5/-1)
- packages/components/src/ui/card/types.ts (MODIFIED, +8/-0)
- packages/components/src/ui/context/polymorphic-component.ts (MODIFIED, +13/-8)
- packages/components/src/ui/context/use-context-system.js (MODIFIED, +2/-3)
- packages/components/src/ui/context/with-next.js (MODIFIED, +1/-1)
- packages/components/src/ui/control-group/types.ts (MODIFIED, +5/-1)
- packages/components/src/ui/popover/types.ts (MODIFIED, +5/-0)
- packages/components/src/ui/scrollable/types.ts (MODIFIED, +4/-0)
- packages/components/src/ui/surface/types.ts (MODIFIED, +4/-0)
- packages/components/src/ui/tooltip/content.js (MODIFIED, +1/-1)
- packages/components/src/ui/tooltip/types.ts (MODIFIED, +7/-3)
- packages/components/src/ui/visually-hidden/hook.js (MODIFIED, +2/-2)
- packages/components/src/view/component.js (MODIFIED, +1/-1)
