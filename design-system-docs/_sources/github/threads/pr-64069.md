---
number: 64069
type: pull_request
state: MERGED
merged_at: 2024-08-06T22:08:17Z
url: https://github.com/WordPress/gutenberg/pull/64069
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","has dev note"]
---

# PR #64069: SelectControl: Infer `value` type from `options`

- URL: https://github.com/WordPress/gutenberg/pull/64069
- Author: mirka
- Created: 2024-07-29T19:30:05Z
- Updated: 2024-08-10T03:27:39Z
- Comments: 7 of 7
- Review threads: 7 of 7
- Changed files: 6 of 6
- Diff: ../pr-diffs/64069.diff

## Body

Updated take on #60293 (props @mikeybinns)

## What?

Tighten the TypeScript type checking on `SelectControl`, so the `value` type matches in the `value`, `options`, and `onChange` props.

See Dev Notes at the bottom for how this looks like to a consumer.

## Why?

It can catch potential bugs.

## How?

Adding a generic that can either be inferred from the `options` array (if it is immutable), or be set explicitly.

## Testing Instructions

See some of the static type tests in the unit test file, and play around by adding your own cases and see what errors.

---

## ✍️ Dev Note

### Stricter type checking in `SelectControl`

In TypeScript codebases, `SelectControl` will now be able to type check whether the `value` type matches in the `value`, `options`, and `onChange` props.

<details>
<summary>This will be inferred automatically if the <code>options</code> array is declared as immutable.</summary>
<p>

```tsx
<SelectControl
	// @ts-expect-error "baz" is not in `options` array
	value="baz"
	options={ [ // array literal that cannot be mutated
		{
			value: 'foo',
			label: 'Foo',
		},
		{
			value: 'bar',
			label: 'Bar',
		},
	] }
/>;
```

```tsx
const options = [
	{
		value: 'foo',
		label: 'Foo',
	},
	{
		value: 'bar',
		label: 'Bar',
	},
] as const; // const assertion to mark as readonly

<SelectControl
	// @ts-expect-error "baz" is not in `options` array
	value="baz"
	options={ options }
/>;
```

</p>
</details>

<details>
<summary>If the <code>options</code> array is mutable, <code>value</code> will remain a simple <code>string</code> type.</summary>
<p>

This is effectively the same type checking behavior as before.

```tsx
const options = [
	{
		value: 'foo',
		label: 'Foo',
	},
	{
		value: 'bar',
		label: 'Bar',
	},
];

<SelectControl
	// No error, because `value` has type `string`
	value="baz"
	options={ options }
/>;
```

</p>
</details>

<details>
<summary>You can also set an explicit <code>value</code> type.</summary>
<p>

```tsx
<SelectControl< 'foo' | 'bar' >
	// @ts-expect-error "baz" is not in `'foo' | 'bar'`
	value="baz"
	// @ts-expect-error "baz" is not in `'foo' | 'bar'`
	options={ [ { value: 'baz', label: 'Baz' } ] }
/>;
```

</p>
</details>

## Issue comments

### mikeybinns on 2024-07-29T20:42:47Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2256893981

One thing that's missing from my PR is the automated tests, perhaps it's worth copying those over?

### mirka on 2024-08-01T19:20:20Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2263799956

> One thing that's missing from my PR is the automated tests, perhaps it's worth copying those over?

You're right, I brought over your tests and reformatted them a bit to mesh with our [existing type tests](https://github.com/search?q=repo%3AWordPress%2Fgutenberg+%22static+typ%22+path%3A%22test%22&type=code) (ad20004). I tried to pare it down so we have a good cost/benefit balance between safety and maintenance burden — hope that's reasonable.

### github-actions on 2024-08-01T19:26:56Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2263810459

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: mikeybinns <mikeybinns@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### mirka on 2024-08-01T19:35:21Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2263823468

I discussed this with a few other folks and nobody is outright opposed, so I think we can move forward to an official review so this can be merged 🙏

### mirka on 2024-08-06T21:45:42Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2272205800

> Nice touch with the type tests, though it makes me wonder if we should add standards for type testing and/or support for tooling like https://github.com/MichiganTypeScript/type-testing - the eslint ignore comments for jest feel a bit like a hack.

@DaniGuardiola It has come up, most recently in https://github.com/WordPress/gutenberg/pull/62400#issuecomment-2202602135. I don't think we're quite at the point where we need dedicated type test tooling, as the downsides are still outweighing the upsides in my opinion (e.g. having to manage the tooling package updates, forcing contributors to learn yet another testing library, etc).

But I do think we should start to document some standards/conventions like you suggest, as this is the fourth time we're [doing](https://github.com/search?q=repo%3AWordPress%2Fgutenberg+%22static+typ%22+path%3A%22%2Ftest%2F%22&type=code) static type tests here!

### mikeybinns on 2024-08-06T23:16:48Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2272318231

Thanks for all your work on this @mirka ☺️

### mirka on 2024-08-07T10:29:32Z

URL: https://github.com/WordPress/gutenberg/pull/64069#issuecomment-2273147355

@mikeybinns Thanks for all your work as well 🙌

## Review threads

### Review thread 1

#### mirka on 2024-07-29T19:55:28Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695784976
Path: packages/components/src/date-time/time/index.tsx

The changes we have to make in this file are what make me a bit hesitant about adding this `value` type inference.  The stricter types do not break runtime code obviously, but we will be forcing some consumers to deal with this new strictness, which may seem like overkill depending on who you ask.

I _think_ we can get away with it, because the strictness will only kick in if the `options` array is defined as immutable (either a literal array in the component call, or with an `as const`). For example, there is a [potential bug](https://github.com/WordPress/gutenberg/blob/f50e045c1ce75acd026ab760891cd5bf57234fd3/packages/components/src/custom-gradient-picker/index.tsx#L112-L114) in `CustomGradientPicker` but it isn't caught with the new strictness because `GRADIENT_OPTIONS` is mutable.

We don't have a ton of data points in the type checked realm of this repo, but it seems like `TimePicker` is the only "false positive" in the sense that it didn't surface a potential bug. (I just want to be sure that this type tightening is a net positive and not a nuisance 😅)

#### mikeybinns on 2024-07-29T20:53:52Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695909444
Path: packages/components/src/date-time/time/index.tsx

I agree with what you're saying. It would likely be a good thing to document how to specify a loose type (by using `SelectControl<string>()`) so that users who don't want to tighten their other code (in this case, the `format` function) can still have the same level of leniency as they previously had, and then this change empowers users to use tighter types if they like.

Though, saying that, I'm not sure where this would be documented. In the `SelectControl` docs as a note perhaps?

#### ciampo on 2024-07-30T07:24:34Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1696453288
Path: packages/components/src/date-time/time/index.tsx

> I just want to be sure that this type tightening is a net positive and not a nuisance 😅

This is definitely a big aspect to keep in mind. Overall, I think this change is a net positive — I hope that in the future we'll have a way to always apply the strictness, and not only when the `options` array is immutable.

We should probably write a dev note for this change, to help consumers of the package deal with this change correctly (especially the ones who need to support multiple versions of Wordpress / Gutenberg).

#### mirka on 2024-08-01T19:05:57Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1700689929
Path: packages/components/src/date-time/time/index.tsx

I added a Dev Note so we at least have something to link to if anybody asks. If we do get some questions, we might need to document it somewhere more permanent. But in general I don't think we should have "TypeScript documentation" aside from our actual type files.

### Review thread 2

#### mirka on 2024-07-29T19:56:15Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695785808
Path: packages/components/src/query-controls/index.tsx

```diff
@@ -85,7 +85,11 @@ export function QueryControls( {
 						__next40pxDefaultSize={ __next40pxDefaultSize }
 						key="query-controls-order-select"
 						label={ __( 'Order by' ) }
-						value={ `${ orderBy }/${ order }` }
+						value={
+							orderBy === undefined || order === undefined
+								? undefined
+								: `${ orderBy }/${ order }`
+						}
```

This was an actual bug.

### Review thread 3

#### mirka on 2024-07-29T19:57:58Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695787551
Path: packages/components/src/select-control/index.tsx

```diff
@@ -66,12 +66,14 @@ function UnforwardedSelectControl(
 			const selectedOptions = Array.from( event.target.options ).filter(
 				( { selected } ) => selected
 			);
-			const newValues = selectedOptions.map( ( { value } ) => value );
+			const newValues = selectedOptions.map(
+				( { value } ) => value as T
+			);
 			props.onChange?.( newValues, { event } );
 			return;
 		}
 
-		props.onChange?.( event.target.value, { event } );
+		props.onChange?.( event.target.value as T, { event } );
```

These type assertions should be safe, as they will always match the `options` types.

#### mikeybinns on 2024-07-29T20:34:03Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695842059
Path: packages/components/src/select-control/index.tsx

```diff
@@ -66,12 +66,14 @@ function UnforwardedSelectControl(
 			const selectedOptions = Array.from( event.target.options ).filter(
 				( { selected } ) => selected
 			);
-			const newValues = selectedOptions.map( ( { value } ) => value );
+			const newValues = selectedOptions.map(
+				( { value } ) => value as T
+			);
 			props.onChange?.( newValues, { event } );
 			return;
 		}
 
-		props.onChange?.( event.target.value, { event } );
+		props.onChange?.( event.target.value as T, { event } );
```

Technically, this isn't quite true because the select could be edited by someone using dev tools, however, I think this is fine because
1. If you edit a website with dev tools, it's your fault you broke the code, not the code maintainer
2. The likelihood of this happening is next to none

#### mikeybinns on 2024-07-29T20:37:51Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695852952
Path: packages/components/src/select-control/index.tsx

```diff
@@ -66,12 +66,14 @@ function UnforwardedSelectControl(
 			const selectedOptions = Array.from( event.target.options ).filter(
 				( { selected } ) => selected
 			);
-			const newValues = selectedOptions.map( ( { value } ) => value );
+			const newValues = selectedOptions.map(
+				( { value } ) => value as T
+			);
 			props.onChange?.( newValues, { event } );
 			return;
 		}
 
-		props.onChange?.( event.target.value, { event } );
+		props.onChange?.( event.target.value as T, { event } );
```

Would you be willing to use `OptionType` instead of `T` here? `T` doesn't really mean anything, and while it's a common practice in TypeScript, I see it as equivilent to naming a variable like `const a = 'something'`.

#### mirka on 2024-08-01T19:10:26Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1700694614
Path: packages/components/src/select-control/index.tsx

```diff
@@ -66,12 +66,14 @@ function UnforwardedSelectControl(
 			const selectedOptions = Array.from( event.target.options ).filter(
 				( { selected } ) => selected
 			);
-			const newValues = selectedOptions.map( ( { value } ) => value );
+			const newValues = selectedOptions.map(
+				( { value } ) => value as T
+			);
 			props.onChange?.( newValues, { event } );
 			return;
 		}
 
-		props.onChange?.( event.target.value, { event } );
+		props.onChange?.( event.target.value as T, { event } );
```

I changed them to `V` (for `value` — it's not actually the option type), does that read a bit better? We don't have official naming conventions for this project yet, so eventually we may decide on a prefixed convention like `TValue`. But to write out a full name like `Value`, I think it's harder to signal that it's a generic. And `ValueType` is redundant because everything is a type 😅 We can revisit, but I hope `V` is a good compromise for now.

### Review thread 4

#### mirka on 2024-07-29T19:59:38Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695789142
Path: packages/components/src/select-control/types.ts

```diff
@@ -24,7 +24,7 @@ type SelectControlBaseProps = Pick<
 	Pick< BaseControlProps, 'help' | '__nextHasNoMarginBottom' > & {
 		onBlur?: ( event: FocusEvent< HTMLSelectElement > ) => void;
 		onFocus?: ( event: FocusEvent< HTMLSelectElement > ) => void;
-		options?: {
+		options?: readonly {
```

Adding a `readonly` here so it allows immutable arrays. (Otherwise it would be a type error when passing an immutable array.)

### Review thread 5

#### mirka on 2024-07-29T20:08:14Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1695797879
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
```

`NoInfer<>` is a [new utility type](https://devblogs.microsoft.com/typescript/announcing-typescript-5-4/#the-noinfer-utility-type) added in TS 5.4, which tells TS not to use this field to infer the generic type. We need this because we want it to infer from the values in the `options` array, not the `value` prop.

This kind of "don't infer" signaling [used to be done](https://www.typescriptlang.org/play/?ts=5.4.5#code/JYWwDg9gTgLgBAKjgQwM5wEoFNkGN4BmUEIcA5FDvmQNwBQdMAnmFnAArFioA8AKnCwAPGFgB2AE3SoYUYGIDmAPjgBeOAG86cOADdkAGwCuWAFxwBAMjgy5i+johgYwCGNTmtOnfuNmLDnAAvgDaALr0QfR0BEZi+K5icADCJJBi4jD8giLiUjay8soAFGBcHhzl-EoAlJracJQwRlBJYkYGBpEMAPQ9cACiUMRQ6EZgcDAQegCsAHQAzItwAEZG8GIQ8MgEolCzcwAsczN0PKngbpl6hiaqZAQQEGRwTi5uqKoaIRq+JuZkFbIKBkIJhIJwHpKejnNJXMTwP5Ye5AkGvZyJT7fX63fyA4Gg8GQ6FAA) with `T & string`, but does not work anymore in TS 5.4+.

#### jsnajdr on 2024-08-02T18:15:56Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1702174615
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
```

Can it be demonstrated when the `NoInfer` can be useful? A new test case that fails without it? I don't understand very well what it does.

#### mikeybinns on 2024-08-02T18:35:11Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1702191679
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
```

Sure, Without `NoInfer` Typescript will infer the generic type from the value prop as well as the options prop.

What this means is it won't detect that your value is not one of the provided options, and therefore won't provide an error for it.

I've created a [simple TS Playground example](https://www.typescriptlang.org/play/?#) to illustrate the difference for you. Hope that helps 🙂

#### jsnajdr on 2024-08-04T07:11:36Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1703088341
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
```

Thanks, this is nice 🙂 I didn't realize that passing something as `value` can expand the `T` type to also include the type of `value`. But we want the type to be checked against `options` instead.

#### tyxla on 2024-08-05T12:30:59Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1704041509
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
```

Nice to see a clever usage of `NoInfer` 👍 I had read about it, but struggled to think of a good use case for it.

#### meteorlxy on 2024-08-10T03:27:39Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1712525381
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
```

Also found a good article about NoInfer: https://www.totaltypescript.com/noinfer

### Review thread 6

#### ciampo on 2024-07-30T07:18:13Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1696443537
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
+		/**
+		 * A function that receives the value of the new option that is being selected as input.
+		 *
+		 * If `multiple` is `true`, the value received is an array of the selected value.
+		 * Otherwise, the value received is a single value with the new selected value.
+		 */
+		onChange?: (
+			value: T,
```

Should we also use the `NoInfer` utility for the `onChange`'s arguments, in case a consumer of the component explicitly types `onChange`?

#### mirka on 2024-08-01T19:11:23Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1700696191
Path: packages/components/src/select-control/types.ts

```diff
@@ -61,50 +61,52 @@ type SelectControlBaseProps = Pick<
 		variant?: 'default' | 'minimal';
 	};
 
-export type SelectControlSingleSelectionProps = SelectControlBaseProps & {
-	/**
-	 * If this property is added, multiple values can be selected. The `value` passed should be an array.
-	 *
-	 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
-	 *
-	 * @default false
-	 */
-	multiple?: false;
-	value?: string;
-	/**
-	 * A function that receives the value of the new option that is being selected as input.
-	 *
-	 * If `multiple` is `true`, the value received is an array of the selected value.
-	 * Otherwise, the value received is a single value with the new selected value.
-	 */
-	onChange?: (
-		value: string,
-		extra?: { event?: ChangeEvent< HTMLSelectElement > }
-	) => void;
-};
+export type SelectControlSingleSelectionProps< T extends string = string > =
+	SelectControlBaseProps< T > & {
+		/**
+		 * If this property is added, multiple values can be selected. The `value` passed should be an array.
+		 *
+		 * In most cases, it is preferable to use the `FormTokenField` or `CheckboxControl` components instead.
+		 *
+		 * @default false
+		 */
+		multiple?: false;
+		value?: NoInfer< T >;
+		/**
+		 * A function that receives the value of the new option that is being selected as input.
+		 *
+		 * If `multiple` is `true`, the value received is an array of the selected value.
+		 * Otherwise, the value received is a single value with the new selected value.
+		 */
+		onChange?: (
+			value: T,
```

Good one, I added the constraints (and type tests) for those too.

### Review thread 7

#### DaniGuardiola on 2024-08-06T17:48:09Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1705910122
Path: packages/components/src/select-control/index.tsx

```diff
@@ -164,6 +166,14 @@ function UnforwardedSelectControl(
  * };
  * ```
  */
-export const SelectControl = forwardRef( UnforwardedSelectControl );
+export const SelectControl = forwardRef( UnforwardedSelectControl ) as <
+	V extends string,
+>(
+	props: WordPressComponentProps<
+		SelectControlProps< V >,
+		'select',
+		false
+	> & { ref?: React.Ref< HTMLSelectElement > }
+) => ReturnType< typeof UnforwardedSelectControl >;
```

I see what you were going for here, but I think I'd simplify the return type to just `React.JSX.Element | null`. That's the only thing that a component can ever return anyway, and it looks clearer to me this way. I don't really mind though, this is a nit.

#### mirka on 2024-08-06T21:20:03Z

URL: https://github.com/WordPress/gutenberg/pull/64069#discussion_r1706126733
Path: packages/components/src/select-control/index.tsx

```diff
@@ -164,6 +166,14 @@ function UnforwardedSelectControl(
  * };
  * ```
  */
-export const SelectControl = forwardRef( UnforwardedSelectControl );
+export const SelectControl = forwardRef( UnforwardedSelectControl ) as <
+	V extends string,
+>(
+	props: WordPressComponentProps<
+		SelectControlProps< V >,
+		'select',
+		false
+	> & { ref?: React.Ref< HTMLSelectElement > }
+) => ReturnType< typeof UnforwardedSelectControl >;
```

That makes sense, thanks 🙏 [2f195c2](https://github.com/WordPress/gutenberg/pull/64069/commits/2f195c27ecf036e1f7359323fbaf87e53478071c)

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/date-time/time/index.tsx (MODIFIED, +20/-15)
- packages/components/src/query-controls/index.tsx (MODIFIED, +5/-1)
- packages/components/src/select-control/index.tsx (MODIFIED, +15/-5)
- packages/components/src/select-control/test/select-control.tsx (MODIFIED, +125/-0)
- packages/components/src/select-control/types.ts (MODIFIED, +60/-58)
