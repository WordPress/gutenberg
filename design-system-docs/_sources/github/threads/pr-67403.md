---
number: 67403
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/67403
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Feature] Block API","[Status] Stale","[Package] Components"]
---

# PR #67403: Fix: Fixed Position Toggle Button Not Taking Full Height

- URL: https://github.com/WordPress/gutenberg/pull/67403
- Author: Mayank-Tripathi32
- Created: 2024-11-28T19:09:49Z
- Updated: 2025-08-21T05:40:21Z
- Comments: 4 of 4
- Review threads: 1 of 1
- Changed files: 3 of 3
- Diff: ../pr-diffs/67403.diff

## Body

resolves #67401

## What?
This PR adds a prop to `CustomSelectControl` to disable the legacy behavior and enable the new behavior of custom-select-v2. The new behavior, which includes flipping the dropdown, fixes the issue.

## Why?
In the WordPress block editor, the “Position” toggle button options in the Group block settings do not take up the full height of the settings panel. This causes a visual inconsistency, as users are required to scroll to see the full options.

## How?
This PR introduces a new prop to `CustomSelectControl` that enables the new dropdown flipping behavior when there isn’t enough space below. It also adds the isLegacy={false} prop in the position support of the inspector. The custom `isLegacy` prop in `CustomSelectControl` defaults to true to preserve the original behavior by default.

## Testing Instructions

1. Open the WordPress block editor.
2. Add a “Group” block to the page.
3. Select the “Group” block.
4. In the block settings panel on the right, locate the “Position” toggle button.
5. Observe that the “Position” toggle options do not extend to fill the full height of the settings panel.
6. Attempt to interact with the toggle options

## Screencast 

https://github.com/user-attachments/assets/c3db38e7-12c6-407d-af10-6a59009b046d

## Issue comments

### Mayank-Tripathi32 on 2024-11-28T19:29:11Z

URL: https://github.com/WordPress/gutenberg/pull/67403#issuecomment-2506681044

I am not sure why unit test failed on action.

![image](https://github.com/user-attachments/assets/6399a691-3e4a-4b9d-aa76-bc6a16468cf9)

### github-actions on 2024-11-28T19:31:20Z

URL: https://github.com/WordPress/gutenberg/pull/67403#issuecomment-2506682940

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: Mayank-Tripathi32 <mayanktripathi32@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: Infinite-Null <ankitkumarshah@git.wordpress.org>
Co-authored-by: SainathPoojary <sainathpoojary@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### Mamaduka on 2025-08-05T04:06:08Z

URL: https://github.com/WordPress/gutenberg/pull/67403#issuecomment-3153205538

It looks like we can close this as "stale". Other issues are blocking the suggested resolution - https://github.com/WordPress/gutenberg/issues/67401#issuecomment-2514926383.

### Mamaduka on 2025-08-21T05:40:21Z

URL: https://github.com/WordPress/gutenberg/pull/67403#issuecomment-3209070745

Closing as "stale".

## Review threads

### Review thread 1

#### mirka on 2024-11-29T20:48:14Z

URL: https://github.com/WordPress/gutenberg/pull/67403#discussion_r1863925684
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -120,4 +120,13 @@ export type CustomSelectProps< T extends CustomSelectOption > = {
 	 * @default false
 	 */
 	__next40pxDefaultSize?: boolean;
+	/**
+	 * Enables legacy behavior for the custom select dropdown.
+	 *
+	 * When set to `true`, the dropdown will follow the legacy implementation, including
+	 * positioning and interaction styles. Set to `false` to use the updated behavior.
+	 *
+	 * @default true
+	 */
+	isLegacy?: boolean;
```

This is not a prop intended to be exposed publicly, and is likely to cause unintended side effects or maintenance headaches when used in this way. I also looked into the `blame` for the flip logic and it seems like it was a pretty intentional hotfix for #63180. We do need to find a good fix for it though, to unblock CustomSelectControlV2. (cc @ciampo)

#### mirka on 2024-11-29T20:51:45Z

URL: https://github.com/WordPress/gutenberg/pull/67403#discussion_r1863927305
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -120,4 +120,13 @@ export type CustomSelectProps< T extends CustomSelectOption > = {
 	 * @default false
 	 */
 	__next40pxDefaultSize?: boolean;
+	/**
+	 * Enables legacy behavior for the custom select dropdown.
+	 *
+	 * When set to `true`, the dropdown will follow the legacy implementation, including
+	 * positioning and interaction styles. Set to `false` to use the updated behavior.
+	 *
+	 * @default true
+	 */
+	isLegacy?: boolean;
```

(Also I think I'm open to setting `flip` to `true` for the legacy version as well, once the z-index issue is addressed.)

#### Mayank-Tripathi32 on 2024-11-29T23:06:59Z

URL: https://github.com/WordPress/gutenberg/pull/67403#discussion_r1863984726
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -120,4 +120,13 @@ export type CustomSelectProps< T extends CustomSelectOption > = {
 	 * @default false
 	 */
 	__next40pxDefaultSize?: boolean;
+	/**
+	 * Enables legacy behavior for the custom select dropdown.
+	 *
+	 * When set to `true`, the dropdown will follow the legacy implementation, including
+	 * positioning and interaction styles. Set to `false` to use the updated behavior.
+	 *
+	 * @default true
+	 */
+	isLegacy?: boolean;
```

Can't we expose flip behaviour publicly via props? I think It would make more sense to allow for it instead of changing anything in legacy.

## Changed files

- packages/block-editor/src/hooks/position.js (MODIFIED, +1/-0)
- packages/components/src/custom-select-control/index.tsx (MODIFIED, +2/-1)
- packages/components/src/custom-select-control/types.ts (MODIFIED, +9/-0)
