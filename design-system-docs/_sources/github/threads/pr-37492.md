---
number: 37492
type: pull_request
state: MERGED
merged_at: 2022-01-31T10:32:39Z
url: https://github.com/WordPress/gutenberg/pull/37492
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Package] Components","[Feature] Component System"]
---

# PR #37492: Migrate Edit Navigation screen "Delete menu" button from `confirm()` …

- URL: https://github.com/WordPress/gutenberg/pull/37492
- Author: chad1008
- Created: 2021-12-17T16:37:21Z
- Updated: 2022-01-31T10:52:10Z
- Comments: 5 of 5
- Review threads: 4 of 4
- Changed files: 1 of 1
- Diff: ../pr-diffs/37492.diff

## Body

Related: https://github.com/WordPress/gutenberg/pull/34153

<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/repository-management.md#pull-requests. -->

<!-- Gutenberg's license is in the process of updating to be dual-licensed under the GPL and MPL. As part of that transition, all new contributions are dual-licensed. For more information, see: https://github.com/WordPress/gutenberg/blob/trunk/LICENSE.md -->

## Description
This PR aims to migrate the beta Navigation Editor's `Delete menu` button away from the current `confirm()` implementation and instead use the new experimental `ConfirmDialog` component.

## How has this been tested?
Running WordPress 5.8.2 via `wp-env`:
1. Ensure the beta Navigation editor is activated from `Gutenberg > Experiments`
2. Open `Gutenberg > Navigation (beta)`
3. Create multiple menus (two or more)
4. Click the `Delete menu` button. Confirm there are no unexpected console errors and the new `ConfirmDialog` component is rendered instead of the browser's `confirm()` prompt.
5. Click the `Cancel` button. Confirm that the menu is still intact.
6. Click the `Delete menu` button again, and this time select `OK`
7. Confirm the menu in question is, in fact, deleted
8. Deleting one menu should automatically load the next menu that's in the database. Use `Delete menu` on all of them
9. Confirm that deleting the final/only menu returns you to the `Create your first menu` UI

Note, if you already have a menu saved from a previous session, attempting to delete it will result in an error. This doesn't appear to be related to this PR, and has been reported in #37794

I've tested in the latest Chrome, Firefox, and Safari.

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->

## Issue comments

### chad1008 on 2021-12-17T20:46:18Z

URL: https://github.com/WordPress/gutenberg/pull/37492#issuecomment-997021577

Marking as `Draft` - there's a bug when cancel is clicked on the `ConfirmDialog` that prevents future clicks of the Delete menu button from being registered. I'll investigate and reopen once that's fixed!

### chad1008 on 2021-12-20T19:52:53Z

URL: https://github.com/WordPress/gutenberg/pull/37492#issuecomment-998223847

Fixed - state is now handled more effectively, and the confirmation dialog is reset after the cancel button is clicked.

### chad1008 on 2022-01-07T21:24:36Z

URL: https://github.com/WordPress/gutenberg/pull/37492#issuecomment-1007751445

Updated this PR to simplify the implementation of ConfirmDialog (cc @ciampo)

### ciampo on 2022-01-21T09:16:20Z

URL: https://github.com/WordPress/gutenberg/pull/37492#issuecomment-1018323191

Update:

- I marked all of the memoization-related conversations as resolved for now, as discussed in [this comment](https://github.com/WordPress/gutenberg/pull/37492#discussion_r780807376)
- there's still an [open comment](https://github.com/WordPress/gutenberg/pull/37492#discussion_r780808161) that needs addressing
- not sure if you managed to look into fixing any broken e2e/unit tests (if any) and potentially adding any new ones

Even with the last comment addressed, I'd still be reluctant to merge this PR until we find a good fix for the `z-index` issue (see https://github.com/WordPress/gutenberg/pull/37959).

### ciampo on 2022-01-31T10:52:10Z

URL: https://github.com/WordPress/gutenberg/pull/37492#issuecomment-1025608916

Since this PR has been merged by @draganescu, let's add any remaining changes (potential e2e test improvements, and the points discussed in [this conversation](https://github.com/WordPress/gutenberg/pull/37492#discussion_r791483132)) in separate follow-up PRs (cc @chad1008 ).

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-01-09T17:05:45Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r780807376
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
```

Small optimisation: you could look into memoizing this callback so that the function doesn't get recreated every time the `DeleteMenu` component renders (see [related comment](https://github.com/WordPress/gutenberg/pull/37491#discussion_r780804949))

#### kevin940726 on 2022-01-11T01:47:23Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r781701204
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
```

It isn't necessarily a performance optimization though, [sometimes it might get worse](https://kentcdodds.com/blog/usememo-and-usecallback). I'd prefer to keep it simple until we audit it.

#### ciampo on 2022-01-11T08:57:14Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r781910523
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
```

Sure, we can keep it as-is unless an optimisation is needed

#### chad1008 on 2022-01-11T21:58:44Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r782556597
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
```

@ciampo I know several of your other comments on this PR (and the other's you've reviewed for me) also touched on memoizing callbacks. They're all similar callbacks, does the approach of holding off until optimization is needed apply to those recommendations as well?

#### ciampo on 2022-01-11T22:17:32Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r782567445
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
```

Let's hold off for now, it's always something that we can add later on — it will also allow you to focus on other aspects of the feedback first :)

### Review thread 2 (resolved)

#### ciampo on 2022-01-09T17:11:35Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r780808042
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
+				>
+					{ __( 'Delete menu' ) }
+				</Button>
+				<ConfirmDialog
+					isOpen={ showConfirmDialog }
+					onConfirm={ onDeleteMenu }
+					onCancel={ () => setShowConfirmDialog( false ) }
```

Similar comment re. memoizing this callback instead of re-creating for every render.

### Review thread 3 (resolved)

#### ciampo on 2022-01-09T17:12:44Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r780808161
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
+				>
+					{ __( 'Delete menu' ) }
+				</Button>
+				<ConfirmDialog
+					isOpen={ showConfirmDialog }
+					onConfirm={ onDeleteMenu }
```

Should we close the dialog when confirming it? The current implementation (using the native `confirm()`) seems to behave that way. (see also [related comment](https://github.com/WordPress/gutenberg/pull/37491#discussion_r780805958))

#### chad1008 on 2022-01-21T19:17:46Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r789933324
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,40 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
 	return (
 		<PanelBody>
-			<Button
-				className="edit-navigation-inspector-additions__delete-menu-button"
-				variant="secondary"
-				isDestructive
-				isBusy={ isMenuBeingDeleted }
-				onClick={ () => {
-					if (
-						// eslint-disable-next-line no-alert
-						window.confirm(
-							__(
-								'Are you sure you want to delete this navigation? This action cannot be undone.'
-							)
-						)
-					) {
-						onDeleteMenu();
-					}
-				} }
-			>
-				{ __( 'Delete menu' ) }
-			</Button>
+			<>
+				<Button
+					className="edit-navigation-inspector-additions__delete-menu-button"
+					variant="secondary"
+					isDestructive
+					isBusy={ isMenuBeingDeleted }
+					onClick={ () => {
+						setShowConfirmDialog( true );
+					} }
+				>
+					{ __( 'Delete menu' ) }
+				</Button>
+				<ConfirmDialog
+					isOpen={ showConfirmDialog }
+					onConfirm={ onDeleteMenu }
```

done in 4667075!

### Review thread 4

#### kevin940726 on 2022-01-24T09:11:08Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r790537810
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,45 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
+	const handleConfirm = () => {
+		setShowConfirmDialog( false );
+		onDeleteMenu();
```

Maybe we should `await` for `onDeleteMenu()` to finish? (I think it returns a promise?)

Bonus point if we can also catch errors here and display the message somewhere, but that's probably best for a different PR.

Another micro-optimization we can make is to show a loading state when the menu is still being deleted. Something like disabling the confirm button and showing a loading icon on the button. But again, probably best for a follow-up PR (I don't think it's possible now with the current API in `__experimentalConfirmDialog` anyway).

These are all just nitpicking though, I think we can merge this and work on those if needed.

#### ciampo on 2022-01-24T12:34:11Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r790699092
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,45 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
+	const handleConfirm = () => {
+		setShowConfirmDialog( false );
+		onDeleteMenu();
```

> Maybe we should `await` for `onDeleteMenu()` to finish? (I think it returns a promise?)`

Hard to say for sure without any docs in this component.

I did a quick search in the codebase, and it looks like the only usage is in [`packages/edit-navigation/src/components/layout/index.js`](https://github.com/WordPress/gutenberg/blob/11294e370b29a237080b946e405c61ed75fa9509/packages/edit-navigation/src/components/layout/index.js#L195). In that case, the `deleteMenu` function comes from the [`useNavigationEditor()` hook](https://github.com/WordPress/gutenberg/blob/ca4cadf63e28998e463615eab269b5b56cc26bd8/packages/edit-navigation/src/hooks/use-navigation-editor.js#L77-L97), and it is indeed an `async` function.

So yeah, we could technically

```js
await onDeleteMenu();
setShowConfirmDialog( false );
```

> Bonus point if we can also catch errors here and display the message somewhere, but that's probably best for a different PR.

Given the [delete function definition](https://github.com/WordPress/gutenberg/blob/ca4cadf63e28998e463615eab269b5b56cc26bd8/packages/edit-navigation/src/hooks/use-navigation-editor.js#L77-L97), it looks like errors are handled via the `createErrorNotice` function, which seems to be connected to the notices store — I'm not familiar with what's the best "gutenberg" way to handle that?

> Another micro-optimization we can make is to show a loading state when the menu is still being deleted. Something like disabling the confirm button and showing a loading icon on the button. But again, probably best for a follow-up PR (I don't think it's possible now with the current API in __experimentalConfirmDialog anyway).

We should have access to the loading state via the `isMenuBeingDeleted` prop passed to this component, but I don't think that the `ConfirmDialog` supports a way to disable its confirm button from outside the component (@fullofcaffeine can you confirm?)

#### kevin940726 on 2022-01-24T12:51:06Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r790711833
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,45 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
+	const handleConfirm = () => {
+		setShowConfirmDialog( false );
+		onDeleteMenu();
```

c.c. @adamziel as this is a good example of lacking a clear and simple way to do loading state + error handling with the current API 😆 . (#38135)

#### fullofcaffeine on 2022-01-25T01:10:27Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r791276843
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,45 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
+	const handleConfirm = () => {
+		setShowConfirmDialog( false );
+		onDeleteMenu();
```

> supports a way to disable its confirm button from outside the component (@fullofcaffeine can you confirm?)

You're right, that's not supported at the moment. I think it's a great improvement idea! What would that API look like do you think? A simple `isLoading` boolean prop that would disable the button and show a loading spinner when `true`?

#### kevin940726 on 2022-01-25T02:21:17Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r791304711
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,45 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
+	const handleConfirm = () => {
+		setShowConfirmDialog( false );
+		onDeleteMenu();
```

> A simple `isLoading` boolean prop that would disable the button and show a loading spinner when `true`?

Yeah, I think so. Since it should be a common feature, we can allow a simple prop like `isLoading` or `disabled` to do the job.

#### ciampo on 2022-01-25T08:51:04Z

URL: https://github.com/WordPress/gutenberg/pull/37492#discussion_r791483132
Path: packages/edit-navigation/src/components/sidebar/delete-menu.js

```diff
@@ -2,31 +2,45 @@
  * WordPress dependencies
  */
 import { __ } from '@wordpress/i18n';
-import { Button, PanelBody } from '@wordpress/components';
+import {
+	Button,
+	PanelBody,
+	__experimentalConfirmDialog as ConfirmDialog,
+} from '@wordpress/components';
+import { useState } from '@wordpress/element';
 
 export default function DeleteMenu( { onDeleteMenu, isMenuBeingDeleted } ) {
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
+
+	const handleConfirm = () => {
+		setShowConfirmDialog( false );
+		onDeleteMenu();
```

>  A simple `isLoading` boolean prop that would disable the button and show a loading spinner when true?

I think I like the sound of `isDisabled` more as it is closer to what we want to achieve — disable interactions with the modal. Let's also add a `is` in front of the prop to make it clear that it's a boolean flag.

We also have to disable pressing the ESC key and clicking on the background, and we'll have to make this feature work both in controlled and uncontrolled mode. We can discuss further these details and refine the UX separately in the PR implementing the feature.

## Changed files

- packages/edit-navigation/src/components/sidebar/delete-menu.js (MODIFIED, +35/-21)
