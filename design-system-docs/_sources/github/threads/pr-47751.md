---
number: 47751
type: pull_request
state: MERGED
merged_at: 2023-03-01T18:03:15Z
url: https://github.com/WordPress/gutenberg/pull/47751
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #47751: Autocomplete: refactor to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/47751
- Author: chad1008
- Created: 2023-02-03T22:19:12Z
- Updated: 2023-03-02T03:20:11Z
- Comments: 4 of 4
- Review threads: 31 of 31
- Changed files: 8 of 8
- Diff: ../pr-diffs/47751.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Refactor `Autocomplete` component to TypeScript

Part of https://github.com/WordPress/gutenberg/issues/35744

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
The refactor to TypeScript has many benefits (auto-generated docs, static linting and error prevention, better IDE experience). See https://github.com/WordPress/gutenberg/issues/35744 for more details

## How?
Followed the steps in the [TypeScript migration guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md?rgh-link-date=2022-11-29T14%3A45%3A34Z#refactoring-a-component-to-typescript)

## Testing Instructions
- Ensure all unit and e2e tests still pass
- In the editor, test an autocompleter like the block inserter or `@` mentions, confirm they work as expected

## Notes:
- There are a couple of items I've commented on the PR that I'd love specific feedback/advice on
- There are also a handful of runtime changes that felt needed. I've highlighted them with comments as well for feedback/alternate approaches. TIA 😄

## Issue comments

### github-actions on 2023-02-04T15:48:19Z

URL: https://github.com/WordPress/gutenberg/pull/47751#issuecomment-1416786120

**Size Change:** +43 B (0%) 

**Total Size:** 1.33 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/components/index.min.js` | 207 kB | +43 B (0%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 993 B |
| `build/annotations/index.min.js` | 2.78 kB |
| `build/api-fetch/index.min.js` | 2.27 kB |
| `build/autop/index.min.js` | 2.15 kB |
| `build/blob/index.min.js` | 483 B |
| `build/block-directory/index.min.js` | 7.2 kB |
| `build/block-directory/style-rtl.css` | 1.04 kB |
| `build/block-directory/style.css` | 1.04 kB |
| `build/block-editor/content-rtl.css` | 4.11 kB |
| `build/block-editor/content.css` | 4.1 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 403 B |
| `build/block-editor/default-editor-styles.css` | 403 B |
| `build/block-editor/index.min.js` | 196 kB |
| `build/block-editor/style-rtl.css` | 14.4 kB |
| `build/block-editor/style.css` | 14.4 kB |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 138 B |
| `build/block-library/blocks/audio/theme.css` | 138 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 91 B |
| `build/block-library/blocks/avatar/style.css` | 91 B |
| `build/block-library/blocks/block/editor-rtl.css` | 305 B |
| `build/block-library/blocks/block/editor.css` | 305 B |
| `build/block-library/blocks/button/editor-rtl.css` | 587 B |
| `build/block-library/blocks/button/editor.css` | 587 B |
| `build/block-library/blocks/button/style-rtl.css` | 628 B |
| `build/block-library/blocks/button/style.css` | 627 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 84 B |
| `build/block-library/blocks/categories/editor.css` | 83 B |
| `build/block-library/blocks/categories/style-rtl.css` | 100 B |
| `build/block-library/blocks/categories/style.css` | 100 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 406 B |
| `build/block-library/blocks/columns/style.css` | 406 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 612 B |
| `build/block-library/blocks/cover/editor.css` | 613 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.57 kB |
| `build/block-library/blocks/cover/style.css` | 1.56 kB |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 138 B |
| `build/block-library/blocks/embed/theme.css` | 138 B |
| `build/block-library/blocks/file/editor-rtl.css` | 300 B |
| `build/block-library/blocks/file/editor.css` | 300 B |
| `build/block-library/blocks/file/style-rtl.css` | 265 B |
| `build/block-library/blocks/file/style.css` | 265 B |
| `build/block-library/blocks/file/view.min.js` | 353 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.44 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.44 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 984 B |
| `build/block-library/blocks/gallery/editor.css` | 988 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.55 kB |
| `build/block-library/blocks/gallery/style.css` | 1.55 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 654 B |
| `build/block-library/blocks/group/editor.css` | 654 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 76 B |
| `build/block-library/blocks/heading/style.css` | 76 B |
| `build/block-library/blocks/html/editor-rtl.css` | 332 B |
| `build/block-library/blocks/html/editor.css` | 333 B |
| `build/block-library/blocks/image/editor-rtl.css` | 830 B |
| `build/block-library/blocks/image/editor.css` | 829 B |
| `build/block-library/blocks/image/style-rtl.css` | 652 B |
| `build/block-library/blocks/image/style.css` | 652 B |
| `build/block-library/blocks/image/theme-rtl.css` | 137 B |
| `build/block-library/blocks/image/theme.css` | 137 B |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 357 B |
| `build/block-library/blocks/latest-comments/style.css` | 357 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 213 B |
| `build/block-library/blocks/latest-posts/editor.css` | 212 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 478 B |
| `build/block-library/blocks/latest-posts/style.css` | 478 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 507 B |
| `build/block-library/blocks/media-text/style.css` | 505 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 716 B |
| `build/block-library/blocks/navigation-link/editor.css` | 715 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.13 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.14 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.22 kB |
| `build/block-library/blocks/navigation/style.css` | 2.2 kB |
| `build/block-library/blocks/navigation/view-modal.min.js` | 2.81 kB |
| `build/block-library/blocks/navigation/view.min.js` | 447 B |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 376 B |
| `build/block-library/blocks/page-list/editor.css` | 376 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 174 B |
| `build/block-library/blocks/paragraph/editor.css` | 174 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 279 B |
| `build/block-library/blocks/paragraph/style.css` | 281 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 501 B |
| `build/block-library/blocks/post-comments-form/style.css` | 501 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 71 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 71 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 134 B |
| `build/block-library/blocks/post-excerpt/style.css` | 134 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 586 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 584 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 318 B |
| `build/block-library/blocks/post-featured-image/style.css` | 318 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 153 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 153 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 282 B |
| `build/block-library/blocks/post-template/style.css` | 282 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 103 B |
| `build/block-library/blocks/preformatted/style.css` | 103 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 326 B |
| `build/block-library/blocks/pullquote/style.css` | 325 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 167 B |
| `build/block-library/blocks/pullquote/theme.css` | 167 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 288 B |
| `build/block-library/blocks/query-pagination/style.css` | 284 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 463 B |
| `build/block-library/blocks/query/editor.css` | 463 B |
| `build/block-library/blocks/quote/style-rtl.css` | 222 B |
| `build/block-library/blocks/quote/style.css` | 222 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 149 B |
| `build/block-library/blocks/rss/editor.css` | 149 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 165 B |
| `build/block-library/blocks/search/editor.css` | 165 B |
| `build/block-library/blocks/search/style-rtl.css` | 409 B |
| `build/block-library/blocks/search/style.css` | 406 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 234 B |
| `build/block-library/blocks/separator/style.css` | 234 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 474 B |
| `build/block-library/blocks/shortcode/editor.css` | 474 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 489 B |
| `build/block-library/blocks/site-logo/editor.css` | 489 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 203 B |
| `build/block-library/blocks/site-logo/style.css` | 203 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 116 B |
| `build/block-library/blocks/site-title/editor.css` | 116 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 57 B |
| `build/block-library/blocks/site-title/style.css` | 57 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 184 B |
| `build/block-library/blocks/social-link/editor.css` | 184 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 674 B |
| `build/block-library/blocks/social-links/editor.css` | 673 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.4 kB |
| `build/block-library/blocks/social-links/style.css` | 1.39 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 332 B |
| `build/block-library/blocks/spacer/editor.css` | 332 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 433 B |
| `build/block-library/blocks/table/editor.css` | 433 B |
| `build/block-library/blocks/table/style-rtl.css` | 651 B |
| `build/block-library/blocks/table/style.css` | 650 B |
| `build/block-library/blocks/table/theme-rtl.css` | 157 B |
| `build/block-library/blocks/table/theme.css` | 157 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 251 B |
| `build/block-library/blocks/tag-cloud/style.css` | 253 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 404 B |
| `build/block-library/blocks/template-part/editor.css` | 404 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 179 B |
| `build/block-library/blocks/video/style.css` | 179 B |
| `build/block-library/blocks/video/theme-rtl.css` | 139 B |
| `build/block-library/blocks/video/theme.css` | 139 B |
| `build/block-library/classic-rtl.css` | 179 B |
| `build/block-library/classic.css` | 179 B |
| `build/block-library/common-rtl.css` | 1.11 kB |
| `build/block-library/common.css` | 1.11 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 11.6 kB |
| `build/block-library/editor.css` | 11.6 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/index.min.js` | 201 kB |
| `build/block-library/reset-rtl.css` | 478 B |
| `build/block-library/reset.css` | 478 B |
| `build/block-library/style-rtl.css` | 12.7 kB |
| `build/block-library/style.css` | 12.7 kB |
| `build/block-library/theme-rtl.css` | 698 B |
| `build/block-library/theme.css` | 703 B |
| `build/block-serialization-default-parser/index.min.js` | 1.13 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.83 kB |
| `build/blocks/index.min.js` | 51 kB |
| `build/components/style-rtl.css` | 11.7 kB |
| `build/components/style.css` | 11.7 kB |
| `build/compose/index.min.js` | 12.4 kB |
| `build/core-data/index.min.js` | 16.2 kB |
| `build/customize-widgets/index.min.js` | 12.2 kB |
| `build/customize-widgets/style-rtl.css` | 1.41 kB |
| `build/customize-widgets/style.css` | 1.41 kB |
| `build/data-controls/index.min.js` | 663 B |
| `build/data/index.min.js` | 8.58 kB |
| `build/date/index.min.js` | 40.4 kB |
| `build/deprecated/index.min.js` | 518 B |
| `build/dom-ready/index.min.js` | 336 B |
| `build/dom/index.min.js` | 4.72 kB |
| `build/edit-post/classic-rtl.css` | 571 B |
| `build/edit-post/classic.css` | 571 B |
| `build/edit-post/index.min.js` | 34.8 kB |
| `build/edit-post/style-rtl.css` | 7.53 kB |
| `build/edit-post/style.css` | 7.52 kB |
| `build/edit-site/index.min.js` | 64 kB |
| `build/edit-site/style-rtl.css` | 10 kB |
| `build/edit-site/style.css` | 9.99 kB |
| `build/edit-widgets/index.min.js` | 17.3 kB |
| `build/edit-widgets/style-rtl.css` | 4.55 kB |
| `build/edit-widgets/style.css` | 4.55 kB |
| `build/editor/index.min.js` | 45.7 kB |
| `build/editor/style-rtl.css` | 3.54 kB |
| `build/editor/style.css` | 3.53 kB |
| `build/element/index.min.js` | 4.95 kB |
| `build/escape-html/index.min.js` | 548 B |
| `build/format-library/index.min.js` | 7.27 kB |
| `build/format-library/style-rtl.css` | 557 B |
| `build/format-library/style.css` | 556 B |
| `build/hooks/index.min.js` | 1.66 kB |
| `build/html-entities/index.min.js` | 454 B |
| `build/i18n/index.min.js` | 3.79 kB |
| `build/is-shallow-equal/index.min.js` | 535 B |
| `build/keyboard-shortcuts/index.min.js` | 1.79 kB |
| `build/keycodes/index.min.js` | 1.94 kB |
| `build/list-reusable-blocks/index.min.js` | 2.14 kB |
| `build/list-reusable-blocks/style-rtl.css` | 865 B |
| `build/list-reusable-blocks/style.css` | 865 B |
| `build/media-utils/index.min.js` | 2.99 kB |
| `build/notices/index.min.js` | 977 B |
| `build/plugins/index.min.js` | 1.95 kB |
| `build/preferences-persistence/index.min.js` | 2.23 kB |
| `build/preferences/index.min.js` | 1.35 kB |
| `build/primitives/index.min.js` | 960 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/private-apis/index.min.js` | 937 B |
| `build/react-i18n/index.min.js` | 702 B |
| `build/react-refresh-entry/index.min.js` | 8.44 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.75 kB |
| `build/reusable-blocks/index.min.js` | 2.26 kB |
| `build/reusable-blocks/style-rtl.css` | 265 B |
| `build/reusable-blocks/style.css` | 265 B |
| `build/rich-text/index.min.js` | 10.8 kB |
| `build/server-side-render/index.min.js` | 2.09 kB |
| `build/shortcode/index.min.js` | 1.52 kB |
| `build/style-engine/index.min.js` | 1.53 kB |
| `build/token-list/index.min.js` | 650 B |
| `build/url/index.min.js` | 3.69 kB |
| `build/vendors/inert-polyfill.min.js` | 2.48 kB |
| `build/vendors/react-dom.min.js` | 41.8 kB |
| `build/vendors/react.min.js` | 4.02 kB |
| `build/viewport/index.min.js` | 1.09 kB |
| `build/warning/index.min.js` | 280 B |
| `build/widgets/index.min.js` | 7.31 kB |
| `build/widgets/style-rtl.css` | 1.18 kB |
| `build/widgets/style.css` | 1.18 kB |
| `build/wordcount/index.min.js` | 1.06 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2023-02-05T14:01:16Z

URL: https://github.com/WordPress/gutenberg/pull/47751#issuecomment-1417927724

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 0dc0b0f81f872663fc59f9ebf9dda8391651937b.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/4306226891
📝  Reported issues:
- #36939 in `specs/editor/plugins/custom-post-types.test.js`
- #47001 in `specs/editor/various/block-hierarchy-navigation.test.js`

### chad1008 on 2023-02-28T18:37:44Z

URL: https://github.com/WordPress/gutenberg/pull/47751#issuecomment-1448676069

PR is updated with recommended changes and rebased to include updates to the component from a few recently merged updates which meant addressing some new merge conflicts.

There are some new typing issues around the `VisuallyHidden` implementation in #47907. @ciampo I may pick your brain a bit there as the solution wasn't immediately obvious to me.

### chad1008 on 2023-03-02T03:20:10Z

URL: https://github.com/WordPress/gutenberg/pull/47751#issuecomment-1451239033

Thank you again @ciampo!

## Review threads

### Review thread 1 (resolved)

#### chad1008 on 2023-02-04T15:49:34Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096552121
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -42,7 +46,10 @@ export function getAutoCompleterUI( autocompleter ) {
 
 		const popoverRef = useRef();
 
-		useOnClickOutside( popoverRef, reset );
+		useOnClickOutside(
+			popoverRef as unknown as MutableRefObject< HTMLElement >,
```

This assertion feels a little heavy-handed, I'd love some feedback on it.

#### mirka on 2023-02-07T18:30:17Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099048814
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -42,7 +46,10 @@ export function getAutoCompleterUI( autocompleter ) {
 
 		const popoverRef = useRef();
 
-		useOnClickOutside( popoverRef, reset );
+		useOnClickOutside(
+			popoverRef as unknown as MutableRefObject< HTMLElement >,
```

I think the standard way to go is:

```tsx
		const popoverRef = useRef< HTMLElement >( null );
```

☝️ When using a ref to reference HTML elements (as opposed to using it as an instance variable), you want to initialize it to null. This signals to TS that you as the consumer won't be mutating the ref (only React will), so you get back an immutable ref object.

And then the first argument to `useOnClickOutside()` can be typed as `React.RefObject<HTMLElement>`. No `as` assertions necessary on the `popoverRef` ✨

#### chad1008 on 2023-02-09T21:46:42Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1102056445
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -42,7 +46,10 @@ export function getAutoCompleterUI( autocompleter ) {
 
 		const popoverRef = useRef();
 
-		useOnClickOutside( popoverRef, reset );
+		useOnClickOutside(
+			popoverRef as unknown as MutableRefObject< HTMLElement >,
```

Yep, that works nicely, thank you! 8bf0c0c

### Review thread 2 (resolved)

#### chad1008 on 2023-02-04T15:51:14Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096552258
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -51,7 +58,7 @@ export function getAutoCompleterUI( autocompleter ) {
 			// eslint-disable-next-line react-hooks/exhaustive-deps
 		}, [ items ] );
 
-		if ( ! items.length > 0 ) {
+		if ( ! ( items.length > 0 ) ) {
```

Small runtime change to allow TypeScript to parse the expression properly. Previously it was `! items.length` first, and then trying to apply the `>` operator to a boolean. This change ensures the proper order of operations.

#### mirka on 2023-02-07T19:55:18Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099138696
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -51,7 +58,7 @@ export function getAutoCompleterUI( autocompleter ) {
 			// eslint-disable-next-line react-hooks/exhaustive-deps
 		}, [ items ] );
 
-		if ( ! items.length > 0 ) {
+		if ( ! ( items.length > 0 ) ) {
```

If we're making a runtime change anyway, I think we should rewrite it so it isn't needlessly complicated. Like `! items.length` or `items.length === 0`.

#### chad1008 on 2023-02-09T21:46:45Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1102056493
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -51,7 +58,7 @@ export function getAutoCompleterUI( autocompleter ) {
 			// eslint-disable-next-line react-hooks/exhaustive-deps
 		}, [ items ] );
 
-		if ( ! items.length > 0 ) {
+		if ( ! ( items.length > 0 ) ) {
```

Filing this under "Thinks I wish I'd thought of myself"

✅ a3f56ce

### Review thread 3 (resolved)

#### chad1008 on 2023-02-04T15:52:01Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096552341
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -96,11 +103,17 @@ export function getAutoCompleterUI( autocompleter ) {
 	return AutocompleterUI;
 }
 
-function useOnClickOutside( ref, handler ) {
+function useOnClickOutside(
+	ref: MutableRefObject< HTMLElement >,
+	handler: AutocompleterUIProps[ 'reset' ]
+) {
 	useEffect( () => {
-		const listener = ( event ) => {
+		const listener = ( event: MouseEvent | TouchEvent ) => {
 			// Do nothing if clicking ref's element or descendent elements, or if the ref is not referencing an element
-			if ( ! ref.current || ref.current.contains( event.target ) ) {
+			if (
+				! ref.current ||
+				ref.current.contains( event.target as Node )
```

Similar to above, this assertion also feels a bit forced, and I wonder if there's a more elegant solution?

#### mirka on 2023-02-07T18:14:33Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099032611
Path: packages/components/src/autocomplete/autocompleter-ui.tsx

```diff
@@ -96,11 +103,17 @@ export function getAutoCompleterUI( autocompleter ) {
 	return AutocompleterUI;
 }
 
-function useOnClickOutside( ref, handler ) {
+function useOnClickOutside(
+	ref: MutableRefObject< HTMLElement >,
+	handler: AutocompleterUIProps[ 'reset' ]
+) {
 	useEffect( () => {
-		const listener = ( event ) => {
+		const listener = ( event: MouseEvent | TouchEvent ) => {
 			// Do nothing if clicking ref's element or descendent elements, or if the ref is not referencing an element
-			if ( ! ref.current || ref.current.contains( event.target ) ) {
+			if (
+				! ref.current ||
+				ref.current.contains( event.target as Node )
```

Stack Overflow has convinced me that this is the most reasonable workaround 👍

### Review thread 4 (resolved)

#### chad1008 on 2023-02-04T15:54:33Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096552677
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -43,9 +48,11 @@ function filterOptions( search, options = [], maxResults = 10 ) {
 	return filtered;
 }
 
-export default function getDefaultUseItems( autocompleter ) {
+export default function getDefaultUseItems(
+	autocompleter: WPCompleter
+): NonNullable< WPCompleter[ 'useItems' ] > {
```

`NonNullable` needed here because `useItems` is optional and potentially undefined, but this default function's return value should not be.

#### mirka on 2023-02-07T20:17:32Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099166641
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -43,9 +48,11 @@ function filterOptions( search, options = [], maxResults = 10 ) {
 	return filtered;
 }
 
-export default function getDefaultUseItems( autocompleter ) {
+export default function getDefaultUseItems(
+	autocompleter: WPCompleter
+): NonNullable< WPCompleter[ 'useItems' ] > {
```

For function returns, I generally find it simpler to rely on inference if possible. (Unless for example we need to guarantee a stable return type for a publicly exported function, which is not the case here.)

```suggestion
) {
return ( filterValue: string ) => {
```

#### chad1008 on 2023-02-09T21:46:57Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1102056640
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -43,9 +48,11 @@ function filterOptions( search, options = [], maxResults = 10 ) {
 	return filtered;
 }
 
-export default function getDefaultUseItems( autocompleter ) {
+export default function getDefaultUseItems(
+	autocompleter: WPCompleter
+): NonNullable< WPCompleter[ 'useItems' ] > {
```

I usually do the same. The goal here was to ensure that the return value of `getDefaultUseItems` always had the same shape as the `Autocomplete`'s `useItems` prop, because if `useItems` isn't provided, [this function is called in its place](https://github.com/WordPress/gutenberg/blob/a3f56ce01fe7deddbd5153decdebf0b9dc656c3f/packages/components/src/autocomplete/autocompleter-ui.tsx#L23).

Looking at it now, I don't see a problem with inference, but I worry I'm forgetting something from the original change. @ciampo if I remember right, this return value typing declaration came out of one of our pairing sessions. I don't know if it rings any bells or if you have thoughts one way on the other regarding an explicit return type vs. inference in this case?

#### ciampo on 2023-02-13T12:24:18Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104398759
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -43,9 +48,11 @@ function filterOptions( search, options = [], maxResults = 10 ) {
 	return filtered;
 }
 
-export default function getDefaultUseItems( autocompleter ) {
+export default function getDefaultUseItems(
+	autocompleter: WPCompleter
+): NonNullable< WPCompleter[ 'useItems' ] > {
```

As Lena said, we should try to let TypeScript infer the return type.

Applying Lena's suggestion would allow TS to infer a return type of  `KeyedOption[][]`, but I see that `WPCompleter[ 'useItems' ]`'s type is a bit stricter than that: the outer array is expected to contain exactly one item (`[ Array< KeyedOption > ]`).

If we need to make sure that `getDefaultUseItems`'s return type is also guaranteeing[ an outer array with exactly one item](https://github.com/WordPress/gutenberg/pull/47751/files#r1102057122), we can use the `as const` assertion to tell TS to narrow the return type as much as possible

```diff
diff --git a/packages/components/src/autocomplete/get-default-use-items.tsx b/packages/components/src/autocomplete/get-default-use-items.tsx
index 71ce5cde9e..cb42c32a79 100644
--- a/packages/components/src/autocomplete/get-default-use-items.tsx
+++ b/packages/components/src/autocomplete/get-default-use-items.tsx
@@ -50,8 +50,8 @@ function filterOptions(
 
 export default function getDefaultUseItems(
 	autocompleter: WPCompleter
-): NonNullable< WPCompleter[ 'useItems' ] > {
-	return ( filterValue ) => {
+) {
+	return ( filterValue: string ) => {
 		const [ items, setItems ] = useState< Array< KeyedOption > >( [] );
 		/*
 		 * We support both synchronous and asynchronous retrieval of completer options
@@ -119,6 +119,6 @@ export default function getDefaultUseItems(
 			};
 		}, [ filterValue ] );
 
-		return [ items ];
+		return [ items ] as const;
 	};
 }
```

With these changes, TS now infers a return type of `(filterValue: string) => readonly [KeyedOption[]]`, which should be correct and in line with `WPCompleter[ 'useItems' ]`'s type

#### chad1008 on 2023-02-13T12:46:35Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104422780
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -43,9 +48,11 @@ function filterOptions( search, options = [], maxResults = 10 ) {
 	return filtered;
 }
 
-export default function getDefaultUseItems( autocompleter ) {
+export default function getDefaultUseItems(
+	autocompleter: WPCompleter
+): NonNullable< WPCompleter[ 'useItems' ] > {
```

Sounds good. Implemented in 3ca9a1dd2f

### Review thread 5 (resolved)

#### chad1008 on 2023-02-04T15:59:07Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096553137
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -146,11 +90,20 @@ function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			const { action, value } =
-				undefined === completion.action ||
-				undefined === completion.value
-					? { action: 'insert-at-caret', value: completion }
-					: completion;
+			function isOptionCompletion(
+				completionData: any
+			): completionData is OptionCompletion {
+				return (
+					undefined === completionData.action ||
+					undefined === completionData.value
+				);
+			}
+			const action = isOptionCompletion( completion )
+				? completion.action
+				: 'insert-at-caret';
+			const value = isOptionCompletion( completion )
+				? completion.value
+				: completion;
```

Runtime change: custom typeguard and new ternaries. The original ternary wasn't able to adequately narrow `completion` to avoid errors (unless it can, in which case I'd love to learn that 🪄 )

### Review thread 6 (resolved)

#### chad1008 on 2023-02-04T16:00:49Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096553370
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -374,7 +327,7 @@ function useAutocomplete( {
 				? getAutoCompleterUI( completer )
 				: AutocompleterUI
 		);
-		setFilterValue( query );
+		setFilterValue( query === null ? '' : query );
```

Runtime change. `match` (above) can potentially return `null`, when until now `filterValue` has always been a string.

### Review thread 7 (resolved)

#### chad1008 on 2023-02-04T16:03:15Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1096553626
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -385,7 +338,7 @@ function useAutocomplete( {
 	const isExpanded = !! autocompleter && filteredOptions.length > 0;
 	const listBoxId = isExpanded
 		? `components-autocomplete-listbox-${ instanceId }`
-		: null;
+		: undefined;
```

Runtime change. This prop is passed to `AutocompleterUI` (below), which renders a `Popover` component. That `Popover` component expects `listBoxId` to be `string | undefined`, but not `null` because it's used as a `div`'s `id`.

### Review thread 8 (resolved)

#### mirka on 2023-02-07T17:57:52Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099016186
Path: packages/components/src/autocomplete/README.md

```diff
@@ -2,6 +2,56 @@
 
 This component is used to provide autocompletion support for a child input component.
 
+## Props
+
+The following props are used to control the behavior of the component.
+
+### record
+
+The rich text value object the autocomleter is being applied to.
+
+-   Required: Yes
+-   Type: `RichTextValue`
+
+### onChange
+
+A function to be called when an option is selected to insert into the existing text.
+
+-   Required: Yes
+-   Type: `( value: string ) => void`
+
+A function to be called when an option is selected to replace the existing text.
+
+-   Required: Yes
+-   Type: `( arg: [ OptionCompletion[ 'value' ] ] ) => void;`
+
+### completers
+
+An array of all of the completers to apply to the current element.
+
+-   Required: Yes
+-   Type: `Array< WPCompleter >`
+ 
+ ### contentRef
+
+ A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+
+ -   Required: Yes
+ -   `MutableRefObject< HTMLElement | undefined >
+### children
```

Some minor formatting issues:

```suggestion
### contentRef

 A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.

 -   Required: Yes
 -   Type: `MutableRefObject< HTMLElement | undefined >

### children
```

### Review thread 9 (resolved)

#### mirka on 2023-02-07T20:32:19Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099188997
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -61,7 +68,7 @@ export default function getDefaultUseItems( autocompleter ) {
 			const { options, isDebounced } = autocompleter;
 			const loadOptions = debounce(
 				() => {
-					const promise = Promise.resolve(
+					const promise: DebouncedPromise = Promise.resolve(
```

Hmm, interesting. Should this be called `CancelablePromise` or something? The cancelability is independent of the debounce, if I'm reading it correctly. (Well, _technically_ it is a secondary debounce layer I guess...)

#### chad1008 on 2023-02-13T12:52:50Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104431547
Path: packages/components/src/autocomplete/get-default-use-items.tsx

```diff
@@ -61,7 +68,7 @@ export default function getDefaultUseItems( autocompleter ) {
 			const { options, isDebounced } = autocompleter;
 			const loadOptions = debounce(
 				() => {
-					const promise = Promise.resolve(
+					const promise: DebouncedPromise = Promise.resolve(
```

Ha, great minds think alike. I'd intended to comment on the declaration for this in `types.ts` because I was struggling with the naming. `Debounced` didn't feel totally accurate, but `Cancelable` also felt like it lacked context, so I kept going back and forth.

I'm happy with `CancelablePromise`, thanks for spotting this :) 86a102f713

### Review thread 10 (resolved)

#### mirka on 2023-02-07T20:54:08Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099223702
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -146,11 +90,20 @@ function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			const { action, value } =
-				undefined === completion.action ||
-				undefined === completion.value
-					? { action: 'insert-at-caret', value: completion }
-					: completion;
+			function isCompletionObject(
+				completionData: any
+			): completionData is OptionCompletion {
```

🔥😎🔥

### Review thread 11 (resolved)

#### mirka on 2023-02-07T21:04:48Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099238691
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -146,11 +90,20 @@ function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			const { action, value } =
-				undefined === completion.action ||
-				undefined === completion.value
-					? { action: 'insert-at-caret', value: completion }
-					: completion;
+			function isCompletionObject(
+				completionData: any
+			): completionData is OptionCompletion {
+				return ! (
+					undefined === completionData.action ||
+					undefined === completionData.value
+				);
```

Just me, or is the negated `OR` harder to grok? 🙈

```suggestion
				return (
					undefined !== completionData.action &&
					undefined !== completionData.value
				);
```

#### chad1008 on 2023-02-13T13:46:07Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104491650
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -146,11 +90,20 @@ function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			const { action, value } =
-				undefined === completion.action ||
-				undefined === completion.value
-					? { action: 'insert-at-caret', value: completion }
-					: completion;
+			function isCompletionObject(
+				completionData: any
+			): completionData is OptionCompletion {
+				return ! (
+					undefined === completionData.action ||
+					undefined === completionData.value
+				);
```

Agreed, thank you! 41ac316375

### Review thread 12 (resolved)

#### mirka on 2023-02-07T21:13:38Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099249073
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -146,11 +90,20 @@ function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			const { action, value } =
-				undefined === completion.action ||
-				undefined === completion.value
-					? { action: 'insert-at-caret', value: completion }
-					: completion;
+			function isCompletionObject(
+				completionData: any
+			): completionData is OptionCompletion {
+				return ! (
+					undefined === completionData.action ||
+					undefined === completionData.value
+				);
+			}
+			const action = isCompletionObject( completion )
+				? completion.action
+				: 'insert-at-caret';
+			const value = isCompletionObject( completion )
+				? completion.value
+				: completion;
```

Any reason why we didn't keep the original destructuring style? Do you find it more readable?

```jsx
			const { action, value } = isCompletionObject( completion )
				? completion
				: { action: 'insert-at-caret', value: completion };
```

#### chad1008 on 2023-02-13T14:23:17Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104538261
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -146,11 +90,20 @@ function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			const { action, value } =
-				undefined === completion.action ||
-				undefined === completion.value
-					? { action: 'insert-at-caret', value: completion }
-					: completion;
+			function isCompletionObject(
+				completionData: any
+			): completionData is OptionCompletion {
+				return ! (
+					undefined === completionData.action ||
+					undefined === completionData.value
+				);
+			}
+			const action = isCompletionObject( completion )
+				? completion.action
+				: 'insert-at-caret';
+			const value = isCompletionObject( completion )
+				? completion.value
+				: completion;
```

No reason at all... apparently I just like to over think things 😆 249d50476c

### Review thread 13 (resolved)

#### mirka on 2023-02-07T21:19:40Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099257249
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -412,11 +365,13 @@ function useAutocomplete( {
 	};
 }
 
-export function useAutocompleteProps( options ) {
+export function useAutocompleteProps( options: UseAutocompleteProps ) {
 	const [ isVisible, setIsVisible ] = useState( false );
-	const ref = useRef();
-	const recordAfterInput = useRef();
-	const onKeyDownRef = useRef();
+	const ref = useRef< HTMLElement | undefined >();
```

Same thing here.

```suggestion
	const ref = useRef< HTMLElement >( null );
```

#### chad1008 on 2023-02-13T14:38:16Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104557846
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -412,11 +365,13 @@ function useAutocomplete( {
 	};
 }
 
-export function useAutocompleteProps( options ) {
+export function useAutocompleteProps( options: UseAutocompleteProps ) {
 	const [ isVisible, setIsVisible ] = useState( false );
-	const ref = useRef();
-	const recordAfterInput = useRef();
-	const onKeyDownRef = useRef();
+	const ref = useRef< HTMLElement | undefined >();
```

Thank you! This one had a slight domino effect into other areas where this ref is referenced/passed. Let me know if you see any issues. 8c17bb9e48

### Review thread 14 (resolved)

#### mirka on 2023-02-07T21:22:40Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099261904
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -440,9 +395,11 @@ export function useAutocompleteProps( options ) {
 
 	const mergedRefs = useMergeRefs( [
 		ref,
-		useRefEffect( ( element ) => {
-			function _onKeyDown( event ) {
-				onKeyDownRef.current( event );
+		useRefEffect( ( element: HTMLElement ) => {
+			function _onKeyDown( event: KeyboardEvent ) {
+				if ( typeof onKeyDownRef.current !== 'undefined' ) {
+					onKeyDownRef.current( event );
+				}
```

Is this simpler with just an optional?

```suggestion
				onKeyDownRef.current?.( event );
```

#### chad1008 on 2023-02-13T14:40:32Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104560884
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -440,9 +395,11 @@ export function useAutocompleteProps( options ) {
 
 	const mergedRefs = useMergeRefs( [
 		ref,
-		useRefEffect( ( element ) => {
-			function _onKeyDown( event ) {
-				onKeyDownRef.current( event );
+		useRefEffect( ( element: HTMLElement ) => {
+			function _onKeyDown( event: KeyboardEvent ) {
+				if ( typeof onKeyDownRef.current !== 'undefined' ) {
+					onKeyDownRef.current( event );
+				}
```

It looks like my original comment replying to this was lost somehow... but yes, that looks a lot simpler. It's also a cool use of an optional. I've used it for properties many times but I don't think I've encountered it on a n actual call like this before. I like it!

### Review thread 15

#### mirka on 2023-02-07T23:54:26Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099433484
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,214 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type getOptionCompletion< TCompleterOption = any > = (
+	option: TCompleterOption,
+	query: string
+) => OptionCompletion | OptionCompletion[ 'value' ];
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: getOptionCompletion;
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
```

Just checking: Is this intentional? An array that always contains a single array, instead of a variable number of arrays like `Array< KeyedOption >[]`?

#### chad1008 on 2023-02-09T21:47:33Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1102057122
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,214 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type getOptionCompletion< TCompleterOption = any > = (
+	option: TCompleterOption,
+	query: string
+) => OptionCompletion | OptionCompletion[ 'value' ];
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: getOptionCompletion;
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
```

It is intentional... this one was a bit of a rabbit hole for me actually. I probably should have left a note on it.

`filteredOptions` [returns an array](https://github.com/WordPress/gutenberg/blob/057c37c5ff20b8e5c8b4552143f0ff45a5b41712/packages/components/src/autocomplete/get-default-use-items.tsx#L18) of `KeyedOptions`.

`getDefaultUseItems` uses `filteredOptions` [to update its list of items](https://github.com/WordPress/gutenberg/blob/057c37c5ff20b8e5c8b4552143f0ff45a5b41712/packages/components/src/autocomplete/get-default-use-items.tsx#L104) in a layout effect. From there, `getDefaultUseItems` [wraps that array in an outer array as its return value](https://github.com/WordPress/gutenberg/blob/057c37c5ff20b8e5c8b4552143f0ff45a5b41712/packages/components/src/autocomplete/get-default-use-items.tsx#L122).

The really fun part is that when `getAutoCompleterUI` ultimately calls `getDefaultUseItems` (here being called under a different variable name), [it destructures the original array back out again](https://github.com/WordPress/gutenberg/blob/a3f56ce01fe7deddbd5153decdebf0b9dc656c3f/packages/components/src/autocomplete/autocompleter-ui.tsx#L40).

So basically, an array is cast as a tuple to serve as a return value, and then immediately destructured back to an array when the function executes. I'm not sure why array is tupled then immediately de-tupled (I don't think that's a word but I'm rolling with it 😆 ) but it's only ever one array of `KeyedOptions` and even then it never exists long enough to be acted upon.

What all of this boils down to (unless I'm mistaken, which is always possible) is that if a consumer _did_ end up providing a `useItems` function prop that returned more than one `Array< KeyedOption>` the first one would get destructured to the `items` variable, and everything else would be ignored.

#### ciampo on 2023-02-13T12:30:31Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104406937
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,214 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type getOptionCompletion< TCompleterOption = any > = (
+	option: TCompleterOption,
+	query: string
+) => OptionCompletion | OptionCompletion[ 'value' ];
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: getOptionCompletion;
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
```

Weird, but makes sense to me 😅

#### ciampo on 2023-02-13T15:05:28Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104598325
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,214 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type getOptionCompletion< TCompleterOption = any > = (
+	option: TCompleterOption,
+	query: string
+) => OptionCompletion | OptionCompletion[ 'value' ];
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: getOptionCompletion;
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
```

Expanding on this one: it would be interesting to git blame and research the original reasons for why this is expected to be a single element tuple, and see if it's possible to remove what feels like an unnecessary impediment... but given the nature of a TS migration, it's definitely not part of this PR's scope, and in general definitely low priority

### Review thread 16 (resolved)

#### mirka on 2023-02-07T23:59:18Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099440002
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,214 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type getOptionCompletion< TCompleterOption = any > = (
```

We probably want to PascalCase all types for convention:

```suggestion
type GetOptionCompletion< TCompleterOption = any > = (
```

(Or alternatively, this one could also just be inlined instead of using a type alias.)

#### chad1008 on 2023-02-09T21:47:42Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1102057256
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,214 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type getOptionCompletion< TCompleterOption = any > = (
```

gah, nice catch on the casing, thank you.

I do like the inline suggestion. Simplifies the file and also lets this prop use the same generic as the rest of the `WPCompelter` type instead of declaring its own 👍 14351698d7

### Review thread 17 (resolved)

#### mirka on 2023-02-08T00:18:48Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099460389
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
```

The fact that we have an explicit `WPCompleter` annotation on line 25 _and_ an `as` assertion here makes me feel like something might be off. Like, something in this mock is wrong, or our types are wrong, or the DevEx could somehow be better.

For example, TS can already tell that `autocompleter.options` here is that fruit array we just assigned, but it's being thrown off because we explicitly annotated the broader `WPCompleter` type on line 25. It's not very elegant.

This isn't necessarily critical to address before merging, but it might be fun to figure out how you can make this work without an `as`. (I'd probably start by removing the `WPCompleter` annotation and work from there?)

#### ciampo on 2023-02-13T14:02:23Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104510822
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
```

I agree with Lena — the fact that we need to "nudge" TS so often means poor DevX, and usually is the symptom that we could have typed our code better.

**I tried removing the `WPCompleter` type on line `25`** as Lena suggested, and worked my way through the errors:
- I added a `FuitOption` type to describe the objects in the `options` array, and used it to type the arguments of `getOptionLabel`
- in `useItems`:
  - I removed the type cast to `autocompleter.options`, since TS can now infer the type directly
  - I removed the ternary operators around `autocompleter.getOptionKeywords` and ` autocompleter.isOptionDisabled` — TS was complaining because effectively they are not defined on the `autocompleter` object.[ I also thought it was safe to remove them, since they don't seem to be used in the tests.](https://github.com/WordPress/gutenberg/pull/47751/files#r1099464037) Another option here would be to define them on the `autocompleter` object
  - I used `as const` to narrow the return type of the function, similarly to what I've suggested for `getDefaultUseItems`
  - as a consequence of this last change, I also had to mark the type of `WPCompleter['useItems']` as `readonly` (since `as const` adds `readonly` automatically)

<details>

<summary>Here's the full diff with those changes applied</summary>

```diff
diff --git a/packages/components/src/autocomplete/test/index.tsx b/packages/components/src/autocomplete/test/index.tsx
index 081e441d13..9322ad7a35 100644
--- a/packages/components/src/autocomplete/test/index.tsx
+++ b/packages/components/src/autocomplete/test/index.tsx
@@ -13,7 +13,12 @@ import { useRef } from '@wordpress/element';
  * Internal dependencies
  */
 import { getAutoCompleterUI } from '../autocompleter-ui';
-import type { WPCompleter } from '../types';
+
+type FruitOption = {
+	visual: string;
+	name: string;
+	id: number;
+};
 
 describe( 'AutocompleterUI', () => {
 	describe( 'click outside behavior', () => {
@@ -22,7 +27,7 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter: WPCompleter = {
+			const autocompleter = {
 				name: 'fruit',
 				options: [
 					{ visual: '🍎', name: 'Apple', id: 1 },
@@ -31,36 +36,28 @@ describe( 'AutocompleterUI', () => {
 				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
-				getOptionLabel: ( option ) => (
+				getOptionLabel: ( option: FruitOption ) => (
 					<span>
 						<span className="icon">{ option.visual }</span>
 						{ option.name }
 					</span>
 				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: ( filterValue ) => {
-					const options = autocompleter.options as Array< {
-						visual: 'string';
-						name: 'string';
-						id: number;
-					} >;
+				useItems: ( filterValue: string ) => {
+					const options = autocompleter.options;
 					const keyedOptions = options.map(
 						( optionData, optionIndex ) => ( {
 							key: `${ autocompleter.name }-${ optionIndex }`,
 							value: optionData,
 							label: autocompleter.getOptionLabel( optionData ),
-							keywords: autocompleter.getOptionKeywords
-								? autocompleter.getOptionKeywords( optionData )
-								: [],
-							isDisabled: autocompleter.isOptionDisabled
-								? autocompleter.isOptionDisabled( optionData )
-								: false,
+							keywords: [],
+							isDisabled: false,
 						} )
 					);
 					const filteredOptions = keyedOptions.filter( ( option ) =>
 						option.value.name.includes( filterValue )
 					);
-					return [ filteredOptions ];
+					return [ filteredOptions ] as const;
 				},
 			};
 
diff --git a/packages/components/src/autocomplete/types.ts b/packages/components/src/autocomplete/types.ts
index a6a8d10790..a2441437c3 100644
--- a/packages/components/src/autocomplete/types.ts
+++ b/packages/components/src/autocomplete/types.ts
@@ -80,7 +80,7 @@ export type WPCompleter< TCompleterOption = any > = {
 	 * Autocomplete UI. These items have uniform shape and have been filtered by
 	 * `AutocompleterUIProps.filterValue`.
 	 */
-	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
 	/**
 	 * Whether or not changes to the `filterValue` should be debounced.
 	 */
```

</details>

**I then tried the "opposite" approach** — keeping `WPComplete` on line `25` and trying to leverage it as much as possible.

- I noticed that `WPComplete` accepts a `TCompleterOption` generic — and so I decided to pass `FruitOption` as the value for the generic
- This still didn't make TS happy about `autocompleter.options` in the `useItems` function. That is because `WPCompleter['options']` can either be an `Array< TCompleterOption >`, but it can also be of type `GetOptions`. When writing a custom `useItems` function, TS cannot exclude the `GetOptions` possibility, and therefore it errors
- I still think that we can improve the types of options, by forwarding `TCompleterOption` to `GetOptions`
- Still, in `useItems` we should _technically_ handle the case in which options is of type `GetOptions< TCompleterOption >`.
- Since in this specific case we know that `options` is a play array, using the typecast `as FruitOption[]` is safe and avoids us from writing a lot of unnecessary code.

<details>

<summary>Here's a summary of the changes</summary>

```diff
diff --git a/packages/components/src/autocomplete/test/index.tsx b/packages/components/src/autocomplete/test/index.tsx
index 081e441d13..cba1cd91c0 100644
--- a/packages/components/src/autocomplete/test/index.tsx
+++ b/packages/components/src/autocomplete/test/index.tsx
@@ -15,6 +15,12 @@ import { useRef } from '@wordpress/element';
 import { getAutoCompleterUI } from '../autocompleter-ui';
 import type { WPCompleter } from '../types';
 
+type FruitOption = {
+	visual: string;
+	name: string;
+	id: number;
+};
+
 describe( 'AutocompleterUI', () => {
 	describe( 'click outside behavior', () => {
 		it( 'should call reset function when a click on another element occurs', async () => {
@@ -22,7 +28,7 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter: WPCompleter = {
+			const autocompleter: WPCompleter< FruitOption > = {
 				name: 'fruit',
 				options: [
 					{ visual: '🍎', name: 'Apple', id: 1 },
@@ -39,11 +45,7 @@ describe( 'AutocompleterUI', () => {
 				),
 				// Mock useItems function to return a autocomplete item.
 				useItems: ( filterValue ) => {
-					const options = autocompleter.options as Array< {
-						visual: 'string';
-						name: 'string';
-						id: number;
-					} >;
+					const options = autocompleter.options as FruitOption[];
 					const keyedOptions = options.map(
 						( optionData, optionIndex ) => ( {
 							key: `${ autocompleter.name }-${ optionIndex }`,
diff --git a/packages/components/src/autocomplete/types.ts b/packages/components/src/autocomplete/types.ts
index a6a8d10790..25bc1411ed 100644
--- a/packages/components/src/autocomplete/types.ts
+++ b/packages/components/src/autocomplete/types.ts
@@ -43,7 +43,7 @@ export type WPCompleter< TCompleterOption = any > = {
 	 * Options may be of any type or shape. The completer declares how those
 	 * options are rendered and what their completions should be when selected.
 	 */
-	options: Array< TCompleterOption > | GetOptions;
+	options: Array< TCompleterOption > | GetOptions< TCompleterOption >;
 	/**
 	 * A function that returns the keywords for the specified option.
 	 */
```
</details>

----

Final thoughts:

- the cleanest approach seems to be the first one, in which I removed the `WPCompleter` type 
- working on the second approach was still interesting, as it makes me wonder if there's a better way to type `options`. Right now, the fact that it can be an array / or a function returning an array / or a function returning a promise for an array does not provide great DevX 🤔

#### chad1008 on 2023-02-13T16:40:51Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104741435
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
```

Wow, thanks for digging in here, both of you. I looked into a bit before the weekend but couldn't quite resolve the typing conflicts, and I see why now!

I've implemented @ciampo's first suggestion of removing the `WPCompleter` and using `const` in 1bd1cfc8d4

I then got to thinking about how to address they typing of `options` so custom `useItems` functions wouldn't run into this problem in 4bb27eecce. I _think_ that should allow TypeScript to parse the difference between an array of options and a function. Curious what you think!

#### ciampo on 2023-02-15T11:56:02Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107030138
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
```

Editing as this comment was supposed to be sent as part of my next round of review, and I'm still in the process of assessing whether the solution that you proposed is effective or not :)

#### ciampo on 2023-02-15T12:07:58Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107041903
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
```

> I've implemented @ciampo's first suggestion of removing the `WPCompleter` and using `const` in [1bd1cfc](https://github.com/WordPress/gutenberg/commit/1bd1cfc8d45d76a1addcb6830e5ee1d9c63428f2)

Thank you!

> I then got to thinking about how to address they typing of `options` so custom `useItems` functions wouldn't run into this problem in [4bb27ee](https://github.com/WordPress/gutenberg/commit/4bb27eeccefe4fda4b4b9463a8de86496fa86e1f). I _think_ that should allow TypeScript to parse the difference between an array of options and a function. Curious what you think!

The reasoning behind your idea is correct, but your implementation won't work. `TCompleterOption` in fact represents the type of single option, and therefore we won't be able to assess if `options` is an array, a function or a promise just by looking at it.

In fact, the `TCompleterOption extends Function` check will always return false, unless each option is a function (which is not what we want). Unfortunately, we'll have to roll back the changes to the `options` type

#### chad1008 on 2023-02-16T14:28:01Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1108549140
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
```

Good point, I got ahead of myself there. Rolled back: 43adf01ae7

### Review thread 18 (resolved)

#### mirka on 2023-02-08T00:22:17Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1099464037
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
+						visual: 'string';
+						name: 'string';
+						id: number;
+					} >;
+					const keyedOptions = options.map(
+						( optionData, optionIndex ) => ( {
+							key: `${ autocompleter.name }-${ optionIndex }`,
+							value: optionData,
+							label: autocompleter.getOptionLabel( optionData ),
+							keywords: autocompleter.getOptionKeywords
+								? autocompleter.getOptionKeywords( optionData )
+								: [],
+							isDisabled: autocompleter.isOptionDisabled
+								? autocompleter.isOptionDisabled( optionData )
```

Will these ternary conditionals ever be `true`?

#### chad1008 on 2023-02-13T16:44:09Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104746446
Path: packages/components/src/autocomplete/test/index.tsx

```diff
@@ -21,27 +22,45 @@ describe( 'AutocompleterUI', () => {
 
 			const resetSpy = jest.fn();
 
-			const autocompleter = {
+			const autocompleter: WPCompleter = {
 				name: 'fruit',
+				options: [
+					{ visual: '🍎', name: 'Apple', id: 1 },
+					{ visual: '🍊', name: 'Orange', id: 2 },
+					{ visual: '🍇', name: 'Grapes', id: 3 },
+				],
 				// The prefix that triggers this completer
 				triggerPrefix: '~',
+				getOptionLabel: ( option ) => (
+					<span>
+						<span className="icon">{ option.visual }</span>
+						{ option.name }
+					</span>
+				),
 				// Mock useItems function to return a autocomplete item.
-				useItems: () => {
-					return [
-						[
-							{
-								isDisabled: false,
-								key: 'Apple',
-								value: 'Apple',
-								label: (
-									<span>
-										<span className="icon">🍎</span>
-										{ 'Apple' }
-									</span>
-								),
-							},
-						],
-					];
+				useItems: ( filterValue ) => {
+					const options = autocompleter.options as Array< {
+						visual: 'string';
+						name: 'string';
+						id: number;
+					} >;
+					const keyedOptions = options.map(
+						( optionData, optionIndex ) => ( {
+							key: `${ autocompleter.name }-${ optionIndex }`,
+							value: optionData,
+							label: autocompleter.getOptionLabel( optionData ),
+							keywords: autocompleter.getOptionKeywords
+								? autocompleter.getOptionKeywords( optionData )
+								: [],
+							isDisabled: autocompleter.isOptionDisabled
+								? autocompleter.isOptionDisabled( optionData )
```

Nope, they weren't actually needed, and have now been removed in 1bd1cfc8d4 as part of the broader fixing up of the unit test.

### Review thread 19 (resolved)

#### ciampo on 2023-02-13T12:29:15Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104405656
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
```

Partial nit: we should sort dependencies in order external / WP / internal.

Over the past week I've started to personally prefer using `React.Type` instad of adding an import line at the top of the file. I find it particularly useful when mixing an matching types that exist in vanilla DOM APIs and React, like `KeyboardEvent`. Reading `React.KeyboardEvent` inline in the code clarifies it immediately

<details>
<summary>Here's what the changes would look like if we applied this same strategy to this file</summary>

```diff
diff --git a/packages/components/src/autocomplete/types.ts b/packages/components/src/autocomplete/types.ts
index 78bc5a3695..a6a8d10790 100644
--- a/packages/components/src/autocomplete/types.ts
+++ b/packages/components/src/autocomplete/types.ts
@@ -2,10 +2,7 @@
  * WordPress dependencies
  */
 import type { WPElement } from '@wordpress/element';
-/**
- * External dependencies
- */
-import type { MutableRefObject, ReactNode } from 'react';
+
 /**
  * Internal dependencies
  */
@@ -94,7 +91,7 @@ export type WPCompleter< TCompleterOption = any > = {
 	className?: string;
 };
 
-type ContentRef = MutableRefObject< HTMLElement | undefined >;
+type ContentRef = React.MutableRefObject< HTMLElement | undefined >;
 
 export type AutocompleterUIProps = {
 	/**
@@ -203,7 +200,7 @@ export type AutocompleteProps = UseAutocompleteProps & {
 	 */
 	children: (
 		props: Omit< ReturnType< typeof useAutocomplete >, 'popover' >
-	) => ReactNode;
+	) => React.ReactNode;
 	/**
 	 * Whether or not the Autocomplte componenet is selected, and if its `Popover`
 	 * should be displayed.
```
</details>

#### chad1008 on 2023-02-13T16:49:28Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104754418
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
```

Thanks for the clarification on the order of dependencies - and the `React.Type` tip makes a lot of sense as well, thanks! 7911e6b0ad

### Review thread 20 (resolved)

#### ciampo on 2023-02-13T13:52:57Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104499405
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
```

Any reason why we're not passing `TCompleterOption` to `GetOptions` here?

```suggestion
	options: Array< TCompleterOption > | GetOptions< TCompleterOption >;
```

#### chad1008 on 2023-02-13T16:50:25Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104755871
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
```

No reason, this was an oversight on my part. It got addressed with the changes in 4bb27eecce, assuming we ultimately go that route.

### Review thread 21 (resolved)

#### ciampo on 2023-02-13T14:04:09Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104512931
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = MutableRefObject< HTMLElement | undefined >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void; //should the param be an index?
```

Could you explain more what did you mean with the inline comment here?

#### chad1008 on 2023-02-13T16:52:21Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104758750
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = MutableRefObject< HTMLElement | undefined >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void; //should the param be an index?
```

Oops, that's a note-to-self that I forgot to flag as TODO early on in the migration when I was taking a rough first pass through the props.

Removed in a91f230cc0, thanks for spotting it!

### Review thread 22 (resolved)

#### ciampo on 2023-02-13T14:08:51Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104519084
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = MutableRefObject< HTMLElement | undefined >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void; //should the param be an index?
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
+	 */
+	value: RichTextValue;
+	/**
+	 * A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+	 */
+	contentRef: ContentRef;
+};
+
+/**
+ * A debounced promise used retrieve completer options.
+ *
+ * @see `get-default-use-items.tsx`
```

Is this JSDoc necessary? I think we can probably do without — a developer would definitely be able to search the package for this type and find its usage

### Review thread 23 (resolved)

#### ciampo on 2023-02-13T14:10:31Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1104522788
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,212 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * External dependencies
+ */
+import type { MutableRefObject, ReactNode } from 'react';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: Array< TCompleterOption > | GetOptions;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = MutableRefObject< HTMLElement | undefined >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void; //should the param be an index?
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
+	 */
+	value: RichTextValue;
+	/**
+	 * A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+	 */
+	contentRef: ContentRef;
+};
+
+/**
+ * A debounced promise used retrieve completer options.
+ *
+ * @see `get-default-use-items.tsx`
+ */
+export type DebouncedPromise< T = void > = Promise< T > & {
+	canceled?: boolean;
+};
+
+/**
+ * When `@wordpress/rich-text` is fully typed, the following
+ * types should be moved to and imported from there
+ *
+ * @see @wordpress/rich-text/src/create.js
```

I'm not sure about the format of this `@see` tag, since it uses a mix of `npm` package name (the `@` sign) and the actual folder path in the repo (`src/create.js`).

Perhaps we could just use the file's absolute path in the repo instead ? (ie. `/packages/rich-text/....)`

### Review thread 24 (resolved)

#### ciampo on 2023-02-15T11:13:56Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1106990078
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: TCompleterOption extends Function
+		? GetOptions< TCompleterOption >
+		: Array< TCompleterOption >;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = React.RefObject< HTMLElement >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void;
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
+	 */
+	value: RichTextValue;
+	/**
+	 * A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+	 */
+	contentRef: ContentRef;
+};
+
+export type CancelablePromise< T = void > = Promise< T > & {
+	canceled?: boolean;
+};
+
+/**
+ * When `@wordpress/rich-text` is fully typed, the following
+ * types should be moved to and imported from there
+ *
+ * @see /packages/rich-text/src/create.js
+ */
+type RichTextFormat = {
+	type: string;
+};
+type RichTextFormatList = Array< RichTextFormat >;
+type RichTextValue = {
+	text: string;
+	formats?: Array< RichTextFormatList >;
+	replacements?: Array< RichTextFormat >;
+	start: number | undefined;
+	end: number | undefined;
+};
+
+export type UseAutocompleteProps = {
+	/**
+	 * The rich text value object the autocomleter is being applied to.
```

Typo 
```suggestion
	 * The rich text value object the autocompleter is being applied to.
```

#### chad1008 on 2023-02-15T13:16:57Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107112912
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: TCompleterOption extends Function
+		? GetOptions< TCompleterOption >
+		: Array< TCompleterOption >;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = React.RefObject< HTMLElement >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void;
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
+	 */
+	value: RichTextValue;
+	/**
+	 * A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+	 */
+	contentRef: ContentRef;
+};
+
+export type CancelablePromise< T = void > = Promise< T > & {
+	canceled?: boolean;
+};
+
+/**
+ * When `@wordpress/rich-text` is fully typed, the following
+ * types should be moved to and imported from there
+ *
+ * @see /packages/rich-text/src/create.js
+ */
+type RichTextFormat = {
+	type: string;
+};
+type RichTextFormatList = Array< RichTextFormat >;
+type RichTextValue = {
+	text: string;
+	formats?: Array< RichTextFormatList >;
+	replacements?: Array< RichTextFormat >;
+	start: number | undefined;
+	end: number | undefined;
+};
+
+export type UseAutocompleteProps = {
+	/**
+	 * The rich text value object the autocomleter is being applied to.
```

fixed, thanks! 84bdd37751

### Review thread 25 (resolved)

#### ciampo on 2023-02-15T11:14:07Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1106990257
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: TCompleterOption extends Function
+		? GetOptions< TCompleterOption >
+		: Array< TCompleterOption >;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = React.RefObject< HTMLElement >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void;
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
```

Typo
```suggestion
	 * The rich text value object the autocompleter is being applied to.
```

#### chad1008 on 2023-02-15T13:17:03Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107113024
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: TCompleterOption extends Function
+		? GetOptions< TCompleterOption >
+		: Array< TCompleterOption >;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = React.RefObject< HTMLElement >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void;
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
```

fixed, thanks! 84bdd37751

### Review thread 26 (resolved)

#### ciampo on 2023-02-15T11:14:49Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1106990914
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: TCompleterOption extends Function
+		? GetOptions< TCompleterOption >
+		: Array< TCompleterOption >;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = React.RefObject< HTMLElement >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void;
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
+	 */
+	value: RichTextValue;
+	/**
+	 * A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
```

Nit: let's keep the JSDoc comments at 80 characters max per line throughout the file

#### chad1008 on 2023-02-15T14:52:53Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107230495
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
+
+type OptionLabel = string | WPElement | Array< string | WPElement >;
+export type KeyedOption = {
+	key: string;
+	value: any;
+	label: OptionLabel;
+	keywords: Array< string >;
+	isDisabled: boolean;
+};
+
+export type WPCompleter< TCompleterOption = any > = {
+	/**
+	 * The name of the completer. Useful for identifying a specific completer to
+	 * be overridden via extensibility hooks.
+	 */
+	name: string;
+	/**
+	 * The string prefix that should trigger the completer. For example,
+	 * Gutenberg's block completer is triggered when the '/' character is entered.
+	 */
+	triggerPrefix: string;
+	/**
+	 * The raw options for completion. May be an array, a function that returns
+	 * an array, or a function that returns a promise for an array.
+	 * Options may be of any type or shape. The completer declares how those
+	 * options are rendered and what their completions should be when selected.
+	 */
+	options: TCompleterOption extends Function
+		? GetOptions< TCompleterOption >
+		: Array< TCompleterOption >;
+	/**
+	 * A function that returns the keywords for the specified option.
+	 */
+	getOptionKeywords?: ( option: TCompleterOption ) => Array< string >;
+	/**
+	 * A function that returns whether or not the specified option is disabled.
+	 * Disabled options cannot be selected.
+	 */
+	isOptionDisabled?: ( option: TCompleterOption ) => boolean;
+	/**
+	 * A function that returns the label for a given option. A label may be a
+	 * string or a mixed array of strings, elements, and components.
+	 */
+	getOptionLabel: ( option: TCompleterOption ) => OptionLabel;
+	/**
+	 * A function that takes a Range before and a Range after the autocomplete
+	 * trigger and query text and returns a boolean indicating whether the
+	 * completer should be considered for that context.
+	 */
+	allowContext?: ( before: string, after: string ) => boolean;
+	/**
+	 * A function that takes an option and returns how the option should
+	 * be completed. By default, the result is a value to be inserted in the text.
+	 * However, a completer may explicitly declare how a completion should be
+	 * treated by returning an object with `action` and `value` properties. The
+	 * `action` declares what should be done with the `value`.
+	 */
+	getOptionCompletion?: (
+		option: TCompleterOption,
+		query: string
+	) => OptionCompletion | OptionCompletion[ 'value' ];
+	/**
+	 * A function that returns an array of items to be displayed in the
+	 * Autocomplete UI. These items have uniform shape and have been filtered by
+	 * `AutocompleterUIProps.filterValue`.
+	 */
+	useItems?: ( filterValue: string ) => readonly [ Array< KeyedOption > ];
+	/**
+	 * Whether or not changes to the `filterValue` should be debounced.
+	 */
+	isDebounced?: boolean;
+	/**
+	 * A CSS class name to be applied to the completion menu.
+	 */
+	className?: string;
+};
+
+type ContentRef = React.RefObject< HTMLElement >;
+
+export type AutocompleterUIProps = {
+	/**
+	 * The value to filter the options by.
+	 */
+	filterValue: string;
+	/**
+	 * An id unique to each instance of the component, used in the IDs of the
+	 * buttons generated for individual options.
+	 */
+	instanceId: number;
+	/**
+	 * The id of to be applied to the listbox of options.
+	 */
+	listBoxId: string | undefined;
+	/**
+	 * The class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The index of the currently selected option.
+	 */
+	selectedIndex: number;
+	/**
+	 * A function to be called when the filterValue changes.
+	 */
+	onChangeOptions: ( items: Array< KeyedOption > ) => void;
+	/**
+	 * A function to be called when an option is selected.
+	 */
+	onSelect: ( option: KeyedOption ) => void;
+	/**
+	 * A function to be called when the completer is reset (e.g. when the user hits the escape key).
+	 */
+	onReset?: () => void;
+	/**
+	 * A function that defines the behavior of the completer when it is reset
+	 */
+	reset: ( event: Event ) => void;
+	/**
+	 * The rich text value object the autocomleter is being applied to.
+	 */
+	value: RichTextValue;
+	/**
+	 * A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
```

Thanks for pointing that out e06a79dfe9

### Review thread 27 (resolved)

#### ciampo on 2023-02-15T11:34:01Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107009528
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
```

Something I've been meaning to ask before but I forgot about — could you point to where Autocomplete is receiving the `options` prop as a Promise?

#### chad1008 on 2023-02-15T15:53:10Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107310319
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
```

I can't actually, but looking back I'm pretty confident this typing came from[ the original README description](https://github.com/WordPress/gutenberg/blob/086b77ed409a70a6c6a6e74dee704851eff812f2/packages/components/src/autocomplete/README.md#L85-L86).

I need to think a bit on how this would impact the [logic](https://github.com/WordPress/gutenberg/blob/86a102f71383fb9d726fdccef7560064f3fc72b6/packages/components/src/autocomplete/get-default-use-items.tsx#L69) in `getDefaultUseItems` if a Promise promise were passed in.

#### ciampo on 2023-02-15T20:46:42Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107705071
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
```

Calling `Promise.resolve()` on a promise [should be fine](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve#resolving_another_promise),

This also seems to match with definitely typed types:

https://github.com/DefinitelyTyped/DefinitelyTyped/blob/master/types/wordpress__components/autocomplete/index.d.ts#L40

(the definitely typed way almost reads better, BTW — what do you think?)

#### chad1008 on 2023-02-16T16:40:31Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1108743574
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,205 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
```

TIL about `PromiseLike`, thank you. That _does_ look a lot cleaner. e29c615f59

### Review thread 28 (resolved)

#### ciampo on 2023-02-15T12:28:41Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107062052
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -34,100 +36,42 @@ import { speak } from '@wordpress/a11y';
  */
 import { getAutoCompleterUI } from './autocompleter-ui';
 import { escapeRegExp } from '../utils/strings';
-
-/**
- * A raw completer option.
- *
- * @typedef {*} CompleterOption
- */
-
-/**
- * @callback FnGetOptions
- *
- * @return {(CompleterOption[]|Promise.<CompleterOption[]>)} The completer options or a promise for them.
```

Interestingly, in the original types `options` was either an array, or a promise returning an array. No signs of it being a function returning an array?

#### chad1008 on 2023-02-15T15:53:13Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107310384
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -34,100 +36,42 @@ import { speak } from '@wordpress/a11y';
  */
 import { getAutoCompleterUI } from './autocompleter-ui';
 import { escapeRegExp } from '../utils/strings';
-
-/**
- * A raw completer option.
- *
- * @typedef {*} CompleterOption
- */
-
-/**
- * @callback FnGetOptions
- *
- * @return {(CompleterOption[]|Promise.<CompleterOption[]>)} The completer options or a promise for them.
```

You make a good point, there is a disconnect between the original types and the README description.
Original types list an array of options or a promise returning an array.
[README lists](https://github.com/WordPress/gutenberg/blob/086b77ed409a70a6c6a6e74dee704851eff812f2/packages/components/src/autocomplete/README.md#L85-L86) an array, a function that returns an array, or a function that returns a promise for an array

#### ciampo on 2023-02-15T20:52:07Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107709440
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -34,100 +36,42 @@ import { speak } from '@wordpress/a11y';
  */
 import { getAutoCompleterUI } from './autocompleter-ui';
 import { escapeRegExp } from '../utils/strings';
-
-/**
- * A raw completer option.
- *
- * @typedef {*} CompleterOption
- */
-
-/**
- * @callback FnGetOptions
- *
- * @return {(CompleterOption[]|Promise.<CompleterOption[]>)} The completer options or a promise for them.
```

marking this conversation as resolved, since the same topic is being discussed in https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107705071

### Review thread 29 (resolved)

#### ciampo on 2023-02-15T20:42:40Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107701641
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

On definitely typed, the types for `OptionCompletion` differ a little

https://github.com/DefinitelyTyped/DefinitelyTyped/blob/master/types/wordpress__components/autocomplete/index.d.ts#L13-L16

I think they are actually a cleaner solution, as they also allow to type the return type of `getOptionCompletion` simply as `OptionCompletion` https://github.com/DefinitelyTyped/DefinitelyTyped/blob/master/types/wordpress__components/autocomplete/index.d.ts#L79

#### chad1008 on 2023-02-16T16:40:54Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1108744065
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

I like the more specific `OptionCompletion` objects, I did notice there were really only two possible combinations, but typing them as such for some reason didn't occur to me.

I do think it makes sense to keep `OptionCompletion` object and type `getOptionCompletion`'s return as a union, to avoid introducing a conflict when `action` and `value` are destructured from the `OptionCompletion` [later on where we've introduced a new typeguard](https://github.com/WordPress/gutenberg/blob/8c17bb9e488b7ba76ed764fbef257257c9846552/packages/components/src/autocomplete/index.tsx#L101-L102).

<details>

<summary>I'm thinking of something like this:</summary>

```diff
diff --git a/packages/components/src/autocomplete/types.ts b/packages/components/src/autocomplete/types.ts
index 6783808683..588807a5e3 100644
--- a/packages/components/src/autocomplete/types.ts
+++ b/packages/components/src/autocomplete/types.ts
@@ -7,10 +7,9 @@ import type { WPElement } from '@wordpress/element';
  */
 import type { useAutocomplete } from '.';
 
-export type OptionCompletion = {
-	action: 'insert-at-caret' | 'replace';
-	value: string | WPElement;
-};
+export type OptionCompletion =
+	| { action: 'insert-at-caret'; value: React.ReactNode }
+	| { action: 'replace'; value: string | React.ReactNode };
 
 type OptionLabel = string | WPElement | Array< string | WPElement >;
 export type KeyedOption = {
@@ -77,7 +76,7 @@ export type WPCompleter< TCompleterOption = any > = {
 	getOptionCompletion?: (
 		option: TCompleterOption,
 		query: string
-	) => OptionCompletion | OptionCompletion[ 'value' ];
+	) => OptionCompletion | React.ReactNode;
 	/**
 	 * A function that returns an array of items to be displayed in the
 	 * Autocomplete UI. These items have uniform shape and have been filtered by

```

</summary>

#### ciampo on 2023-02-17T13:39:02Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1109823605
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Good point, overall it looks like a good solution to me.

While checking with the types from definitely typed, I noticed another difference: when the `action ` is 'replace', the types from definitely typed say [that `value` should be of type `Value`](https://github.com/DefinitelyTyped/DefinitelyTyped/blob/master/types/wordpress__components/autocomplete/index.d.ts#L16), which in this PR you called `RichTextValue`.

But when I tried type `OptionCompletion` as

```ts
export type OptionCompletion =
	| { action: 'insert-at-caret'; value: React.ReactNode }
	| { action: 'replace'; value: RichTextValue };
```

TypeScript complains in the `insertCompletion` function, saying that a `RichTextValue` object can't be passed to the `renderToString` funciton.

https://github.com/WordPress/gutenberg/blob/8c17bb9e488b7ba76ed764fbef257257c9846552/packages/components/src/autocomplete/index.tsx

We need to understand where is the issue:
- either the definitely typed types are out of date, and therefore Chad's types for the `value` property are correct
- or, if the definitely typed types are correct, we'd need to improve the code in the `insertCompletion` function.

@ellatrix , you may be the right person with enough context here to help us understand how to fix this error?

#### ellatrix on 2023-02-20T15:25:58Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112089599
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Tbh, I'm not sure how the completers API works, I think the best person to ask is @draganescu. The `React.ReactNode` type doesn't make any sense to me, it's should probably either be a rich text value or just a string?

#### ellatrix on 2023-02-21T06:31:42Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112596478
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Sorry, not @draganescu. I meant @brandonpayton.

#### ciampo on 2023-02-21T09:03:43Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112760535
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Alright, I tinkered a bit with the types and I think I should have landed on a good solution:

- Refactored how the `OptionCompletion` type is declaired, extracting separate `InsertOption` and `ReplaceOption` types, and adding `React.ReactNode` as well
- Refined the `onReplace` callback to only accept `RichTextValue[]` as argument, since it seems to be called only with `ReplaceOption` objects
- Refined the `insertCompletion` function to only accept `ReactNode` as argument, since it seems to be only called with `InsertOption` objects
- Rewrote the `isCompletionObject` typeguard to also check against `ReactNode`, and to more explicitly return only `InsertOption | ReplaceOption`
- Removed the `{action, value}` object destructuring — referring the whole `completionObject` variable actually helps TypeScript to narrow types in the `if` statements

<details>

<summary>Here's the diff</summary>

```diff
diff --git a/packages/components/src/autocomplete/index.tsx b/packages/components/src/autocomplete/index.tsx
index a7f86d1a1c..e62d018e7d 100644
--- a/packages/components/src/autocomplete/index.tsx
+++ b/packages/components/src/autocomplete/index.tsx
@@ -43,6 +43,8 @@ import type {
 	OptionCompletion,
 	UseAutocompleteProps,
 	WPCompleter,
+	InsertOption,
+	ReplaceOption,
 } from './types';
 
 export function useAutocomplete( {
@@ -68,7 +70,7 @@ export function useAutocomplete( {
 	>( null );
 	const backspacing = useRef( false );
 
-	function insertCompletion( replacement: OptionCompletion[ 'value' ] ) {
+	function insertCompletion( replacement: React.ReactNode ) {
 		if ( autocompleter === null ) {
 			return;
 		}
@@ -90,26 +92,31 @@ export function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			function isCompletionObject(
-				completionData: any
-			): completionData is OptionCompletion {
+			const isCompletionObject = (
+				obj: OptionCompletion
+			): obj is InsertOption | ReplaceOption => {
 				return (
-					undefined !== completionData.action &&
-					undefined !== completionData.value
+					obj !== null &&
+					typeof obj === 'object' &&
+					'action' in obj &&
+					'value' in obj
 				);
-			}
+			};
 
-			const { action, value } = isCompletionObject( completion )
+			const completionObject = isCompletionObject( completion )
 				? completion
-				: { action: 'insert-at-caret', value: completion };
+				: ( {
+						action: 'insert-at-caret',
+						value: completion,
+				  } as InsertOption );
 
-			if ( 'replace' === action ) {
-				onReplace( [ value ] );
+			if ( 'replace' === completionObject.action ) {
+				onReplace( [ completionObject.value ] );
 				// When replacing, the component will unmount, so don't reset
 				// state (below) on an unmounted component.
 				return;
-			} else if ( 'insert-at-caret' === action ) {
-				insertCompletion( value );
+			} else if ( 'insert-at-caret' === completionObject.action ) {
+				insertCompletion( completionObject.value );
 			}
 		}
 
diff --git a/packages/components/src/autocomplete/types.ts b/packages/components/src/autocomplete/types.ts
index 6783808683..f318322ed6 100644
--- a/packages/components/src/autocomplete/types.ts
+++ b/packages/components/src/autocomplete/types.ts
@@ -7,11 +7,17 @@ import type { WPElement } from '@wordpress/element';
  */
 import type { useAutocomplete } from '.';
 
-export type OptionCompletion = {
-	action: 'insert-at-caret' | 'replace';
-	value: string | WPElement;
+// Insert the `value` into the text.
+export type InsertOption = {
+	action: 'insert-at-caret';
+	value: React.ReactNode;
 };
 
+// Replace the current block with the block specified in the `value` property
+export type ReplaceOption = { action: 'replace'; value: RichTextValue };
+
+export type OptionCompletion = React.ReactNode | InsertOption | ReplaceOption;
+
 type OptionLabel = string | WPElement | Array< string | WPElement >;
 export type KeyedOption = {
 	key: string;
@@ -77,7 +83,7 @@ export type WPCompleter< TCompleterOption = any > = {
 	getOptionCompletion?: (
 		option: TCompleterOption,
 		query: string
-	) => OptionCompletion | OptionCompletion[ 'value' ];
+	) => OptionCompletion;
 	/**
 	 * A function that returns an array of items to be displayed in the
 	 * Autocomplete UI. These items have uniform shape and have been filtered by
@@ -185,7 +191,7 @@ export type UseAutocompleteProps = {
 	 * A function to be called when an option is selected to replace the
 	 * existing text.
 	 */
-	onReplace: ( arg: [ OptionCompletion[ 'value' ] ] ) => void;
+	onReplace: ( values: RichTextValue[] ) => void;
 	/**
 	 * An array of all of the completers to apply to the current element.
 	 */
```
</details>

What do y'all think ?

#### ellatrix on 2023-02-21T12:01:02Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112960091
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Why allow `React.ReactNode`? This shouldn't be possible. I think probably only strings and rich text values are allowed, but surely no React nodes?

#### ciampo on 2023-02-21T17:37:38Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1113382248
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

I was assuming `ReactNode` because [that value is passed to the `renderToString` function](https://github.com/WordPress/gutenberg/blob/8c17bb9e488b7ba76ed764fbef257257c9846552/packages/components/src/autocomplete/index.tsx#L78), which accepts a `ReactNode`

<details>

<summary>But I'm totally fine with replacing `ReactNode` with `string`</summary>

```diff
diff --git a/packages/components/src/autocomplete/index.tsx b/packages/components/src/autocomplete/index.tsx
index a7f86d1a1c..4275fed1c3 100644
--- a/packages/components/src/autocomplete/index.tsx
+++ b/packages/components/src/autocomplete/index.tsx
@@ -43,6 +43,8 @@ import type {
 	OptionCompletion,
 	UseAutocompleteProps,
 	WPCompleter,
+	InsertOption,
+	ReplaceOption,
 } from './types';
 
 export function useAutocomplete( {
@@ -68,7 +70,7 @@ export function useAutocomplete( {
 	>( null );
 	const backspacing = useRef( false );
 
-	function insertCompletion( replacement: OptionCompletion[ 'value' ] ) {
+	function insertCompletion( replacement: string ) {
 		if ( autocompleter === null ) {
 			return;
 		}
@@ -90,26 +92,33 @@ export function useAutocomplete( {
 		if ( getOptionCompletion ) {
 			const completion = getOptionCompletion( option.value, filterValue );
 
-			function isCompletionObject(
-				completionData: any
-			): completionData is OptionCompletion {
+			const isCompletionObject = (
+				obj: OptionCompletion
+			): obj is InsertOption | ReplaceOption => {
 				return (
-					undefined !== completionData.action &&
-					undefined !== completionData.value
+					// Since `typeof null === 'object'`,
+					// we need to check explicitely for `null`
+					obj !== null &&
+					typeof obj === 'object' &&
+					'action' in obj &&
+					'value' in obj
 				);
-			}
+			};
 
-			const { action, value } = isCompletionObject( completion )
+			const completionObject = isCompletionObject( completion )
 				? completion
-				: { action: 'insert-at-caret', value: completion };
+				: ( {
+						action: 'insert-at-caret',
+						value: completion,
+				  } as InsertOption );
 
-			if ( 'replace' === action ) {
-				onReplace( [ value ] );
+			if ( 'replace' === completionObject.action ) {
+				onReplace( [ completionObject.value ] );
 				// When replacing, the component will unmount, so don't reset
 				// state (below) on an unmounted component.
 				return;
-			} else if ( 'insert-at-caret' === action ) {
-				insertCompletion( value );
+			} else if ( 'insert-at-caret' === completionObject.action ) {
+				insertCompletion( completionObject.value );
 			}
 		}
 
diff --git a/packages/components/src/autocomplete/types.ts b/packages/components/src/autocomplete/types.ts
index 6783808683..bfdd826031 100644
--- a/packages/components/src/autocomplete/types.ts
+++ b/packages/components/src/autocomplete/types.ts
@@ -7,11 +7,17 @@ import type { WPElement } from '@wordpress/element';
  */
 import type { useAutocomplete } from '.';
 
-export type OptionCompletion = {
-	action: 'insert-at-caret' | 'replace';
-	value: string | WPElement;
+// Insert the `value` into the text.
+export type InsertOption = {
+	action: 'insert-at-caret';
+	value: string;
 };
 
+// Replace the current block with the block specified in the `value` property
+export type ReplaceOption = { action: 'replace'; value: RichTextValue };
+
+export type OptionCompletion = string | InsertOption | ReplaceOption;
+
 type OptionLabel = string | WPElement | Array< string | WPElement >;
 export type KeyedOption = {
 	key: string;
@@ -77,7 +83,7 @@ export type WPCompleter< TCompleterOption = any > = {
 	getOptionCompletion?: (
 		option: TCompleterOption,
 		query: string
-	) => OptionCompletion | OptionCompletion[ 'value' ];
+	) => OptionCompletion;
 	/**
 	 * A function that returns an array of items to be displayed in the
 	 * Autocomplete UI. These items have uniform shape and have been filtered by
@@ -185,7 +191,7 @@ export type UseAutocompleteProps = {
 	 * A function to be called when an option is selected to replace the
 	 * existing text.
 	 */
-	onReplace: ( arg: [ OptionCompletion[ 'value' ] ] ) => void;
+	onReplace: ( values: RichTextValue[] ) => void;
 	/**
 	 * An array of all of the completers to apply to the current element.
 	 */

```

</details>

#### ellatrix on 2023-02-22T07:12:36Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1113916451
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

It looks like that was introduced in #2896. Mentions are now just inserting a string though, not a link. Since we're only using it with strings and we don't have it documented anywhere, I would prefer removing it.

#### ellatrix on 2023-02-22T07:14:57Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1113918116
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Ugh, looks like it is documented. Ok, never mind :)

#### ciampo on 2023-02-22T14:30:15Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1114405277
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Just to make sure I understand your train of thoughts — your preference was to replace `ReactNode` with `string`, but since it's documented we should keep `ReactNode` in place ?

#### chad1008 on 2023-02-22T14:35:49Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1114412375
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Wow, thanks @ciampo. It looks solid to me! Just to make sure I'm on the right page before applying the recommended change, we're going to stick with allowing `React.ReactNode`, correct?

#### ciampo on 2023-02-22T18:23:10Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1114769202
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Yeah I think so. We can always open a quick follow-up if it turns our that `string` was a better option

#### ellatrix on 2023-02-23T09:58:38Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1115454923
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Yes, ReactNode is fine. Wasn't aware that we're using it.

#### chad1008 on 2023-02-28T18:35:27Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1120591015
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
+	value: string | WPElement;
+};
```

Thank you both - I've applied @ciampo's recommended changes, and rebased the PR to include the changes to the component from other recent updates.

### Review thread 30 (resolved)

#### ciampo on 2023-02-15T20:43:19Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1107702217
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
```

Borrowing from definitely typed, would split `Action` to its separate type and adding these docs help?

#### chad1008 on 2023-02-16T16:41:19Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1108744583
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
```

With the current implementation I think adding the docs might be helpful, but the separate `Action` type doesn't look like it would be used, unless I'm overlooking something.

That said, this would be impacted by the potential change I've shared [here](https://github.com/WordPress/gutenberg/pull/47751#discussion_r1108744065), because at that point `action` is even more narrowly defined, depending on which `value` type it's being paired with. In that context, would you still like to see some docs here?

#### ciampo on 2023-02-17T13:40:38Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1109825525
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
```

We may add the docs directly to the `OptionCompletion` type or to the `getOptionCompletion` function?

#### ciampo on 2023-02-21T09:54:58Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112826358
Path: packages/components/src/autocomplete/types.ts

```diff
@@ -0,0 +1,213 @@
+/**
+ * WordPress dependencies
+ */
+import type { WPElement } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import type { useAutocomplete } from '.';
+
+type GetOptions< TCompleterOption = any > = (
+	filterValue: AutocompleterUIProps[ 'filterValue' ]
+) => Array< TCompleterOption > | Promise< Array< TCompleterOption > >;
+
+export type OptionCompletion = {
+	action: 'insert-at-caret' | 'replace';
```

Marking as solved, as this will likely be part of the changes proposed in https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112760535

### Review thread 31 (resolved)

#### draganescu on 2023-02-21T06:20:20Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1112588404
Path: packages/components/src/autocomplete/README.md

```diff
@@ -2,6 +2,57 @@
 
 This component is used to provide autocompletion support for a child input component.
 
+## Props
+
+The following props are used to control the behavior of the component.
+
+### record
+
+The rich text value object the autocomleter is being applied to.
+
+-   Required: Yes
+-   Type: `RichTextValue`
+
+### onChange
+
+A function to be called when an option is selected to insert into the existing text.
+
+-   Required: Yes
+-   Type: `( value: string ) => void`
+
+A function to be called when an option is selected to replace the existing text.
+
+-   Required: Yes
+-   Type: `( arg: [ OptionCompletion[ 'value' ] ] ) => void;`
+
+### completers
+
+An array of all of the completers to apply to the current element.
+
+-   Required: Yes
+-   Type: `Array< WPCompleter >`
+ 
+### contentRef
+
+A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+
+-   Required: Yes
+-   `MutableRefObject< HTMLElement | undefined >
```

missing closing "`"

#### chad1008 on 2023-02-22T14:37:29Z

URL: https://github.com/WordPress/gutenberg/pull/47751#discussion_r1114414505
Path: packages/components/src/autocomplete/README.md

```diff
@@ -2,6 +2,57 @@
 
 This component is used to provide autocompletion support for a child input component.
 
+## Props
+
+The following props are used to control the behavior of the component.
+
+### record
+
+The rich text value object the autocomleter is being applied to.
+
+-   Required: Yes
+-   Type: `RichTextValue`
+
+### onChange
+
+A function to be called when an option is selected to insert into the existing text.
+
+-   Required: Yes
+-   Type: `( value: string ) => void`
+
+A function to be called when an option is selected to replace the existing text.
+
+-   Required: Yes
+-   Type: `( arg: [ OptionCompletion[ 'value' ] ] ) => void;`
+
+### completers
+
+An array of all of the completers to apply to the current element.
+
+-   Required: Yes
+-   Type: `Array< WPCompleter >`
+ 
+### contentRef
+
+A ref containing the editable element that will serve as the anchor for `Autocomplete`'s `Popover`.
+
+-   Required: Yes
+-   `MutableRefObject< HTMLElement | undefined >
```

Thank you! ✅

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +2/-0)
- packages/components/src/autocomplete/README.md (MODIFIED, +51/-0)
- packages/components/src/autocomplete/autocompleter-ui.tsx (RENAMED, +21/-8)
- packages/components/src/autocomplete/get-default-use-items.tsx (RENAMED, +11/-6)
- packages/components/src/autocomplete/index.tsx (RENAMED, +77/-111)
- packages/components/src/autocomplete/test/index.tsx (RENAMED, +36/-20)
- packages/components/src/autocomplete/types.ts (ADDED, +219/-0)
- packages/components/tsconfig.json (MODIFIED, +0/-1)
