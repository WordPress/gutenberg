---
number: 42166
type: pull_request
state: MERGED
merged_at: 2022-07-14T22:25:19Z
url: https://github.com/WordPress/gutenberg/pull/42166
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components"]
---

# PR #42166: InputControl: Decrease large default padding if has prefix/suffix

- URL: https://github.com/WordPress/gutenberg/pull/42166
- Author: mirka
- Created: 2022-07-05T17:53:54Z
- Updated: 2022-07-14T22:25:46Z
- Comments: 3 of 3
- Review threads: 6 of 6
- Changed files: 7 of 7
- Diff: ../pr-diffs/42166.diff

## Body

## What?

Ensures that the padding between the `prefix`/`suffix` and the text input doesn't get too big in the large size variant of the component.

## Why?

The large size variant of the component increases the side paddings of the text input to 16px (previously 8px). In most cases, this is too big when using with a `prefix` or `suffix` element. Some actual examples of this can be seen in the large variants of `UnitControl` and `ColorPicker`:

<img width="135" alt="Paddings in the UnitControl component" src="https://user-images.githubusercontent.com/555336/177383373-1d44c274-f82d-432c-85cf-66344abaeee9.png">
<img width="154" alt="Paddings in the hex input of a ColorPicker component" src="https://user-images.githubusercontent.com/555336/177383416-81eb7329-7c8d-4edb-8cbd-0c2a08527bff.png">

## How?

Added an internal API to specify custom side padding on the `InputField` subcomponent. This API is internal-only for now, and is meant to minimally satisfy the internal customization needs in the components library. Though, I tried to make it generic enough so it would be easy to expose the API externally on `InputControl` itself, if we want to do so in the future.

## Testing Instructions

1. `npm run storybook:dev`
2. Check the "With Prefix" and "With Suffix" stories for the InputControl component. The padding between the prefix/suffix and the text input should remain at 8px, regardless of the `size` variant. This should also work in RTL mode.
3. Check the stories for UnitControl. There should be no regression in padding between the text input and the unit dropdown.

## Screenshots or screencast <!-- if applicable -->

| Before | After |
|--------|-------|
|<img width="139" alt="Large size InputControl with a prefix, 16px padding in between" src="https://user-images.githubusercontent.com/555336/177385312-12d50ca2-4031-457b-9df6-b52b6c19ae3b.png">|<img width="154" alt="Large size InputControl with a prefix, 8px padding in between" src="https://user-images.githubusercontent.com/555336/177385233-1e89289e-10a0-4397-bd83-46d278833941.png">|

## Issue comments

### jameskoster on 2022-07-06T09:14:19Z

URL: https://github.com/WordPress/gutenberg/pull/42166#issuecomment-1175983050

Seems to be working as expected, though ideally we should increase the margin/padding on the prefix/suffix as well, so that it matches the input:

<img width="806" alt="Screenshot 2022-07-06 at 10 11 15" src="https://user-images.githubusercontent.com/846565/177515450-4592ae3b-88a2-454a-bef9-2b11c86f8799.png">

### mirka on 2022-07-06T19:50:55Z

URL: https://github.com/WordPress/gutenberg/pull/42166#issuecomment-1176633862

> ideally we should increase the margin/padding on the prefix/suffix as well

Currently `InputControl` doesn't add any left padding to the prefix, instead leaving it to the prefix element itself to handle it. I believe this is because sometimes the prefix/suffix elements need to be flush with the text field.

But I agree that it's kind of necessary that the prefix/suffix elements know what the standard padding is for a given size variant. As a follow-up task, I'll see if I can propose something where `InputControl` provides the prefix component with a `paddingInlineStart` prop so the prefix can use that padding value for styling if they want.

### mirka on 2022-07-12T21:01:06Z

URL: https://github.com/WordPress/gutenberg/pull/42166#issuecomment-1182496770

> But I agree that it's kind of necessary that the prefix/suffix elements know what the standard padding is for a given size variant. As a follow-up task, I'll see if I can propose something where `InputControl` provides the prefix component with a `paddingInlineStart` prop so the prefix can use that padding value for styling if they want.

Solution proposed in #42378 👍 

This PR can proceed independently of that PR though.

## Review threads

### Review thread 1

#### mirka on 2022-07-05T17:54:24Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914052505
Path: packages/components/src/input-control/stories/index.tsx

```diff
@@ -40,13 +40,13 @@ Default.args = {
 export const WithPrefix = Template.bind( {} );
 WithPrefix.args = {
 	...Default.args,
-	prefix: <span style={ { paddingLeft: 8 } }>@</span>,
+	prefix: <span style={ { marginInlineStart: 8 } }>@</span>,
 };
 
 export const WithSuffix = Template.bind( {} );
 WithSuffix.args = {
 	...Default.args,
-	suffix: <button style={ { marginRight: 4 } }>Send</button>,
+	suffix: <button style={ { marginInlineEnd: 4 } }>Send</button>,
```

Tweaked examples for better RTL.

### Review thread 2

#### mirka on 2022-07-05T17:54:48Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914052806
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -18,22 +18,13 @@ type SelectProps = {
 
 type InputProps = {
 	disableUnits?: boolean;
-	size: SelectSize;
```

Unrelated, but minor cleanup of an unused type.

### Review thread 3

#### mirka on 2022-07-05T17:59:58Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914056960
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -18,22 +18,13 @@ type SelectProps = {
 
 type InputProps = {
 	disableUnits?: boolean;
-	size: SelectSize;
 };
 
 export const Root = styled.div`
 	box-sizing: border-box;
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits }: InputProps ) => {
-	if ( disableUnits ) return '';
-
-	return css`
-		${ rtl( { paddingRight: 8 } )() };
-	`;
-};
-
```

CSS override removed 🎉 

Btw @aaronrobertshaw: When building `BorderControl`, do you remember if there were any blockers to rendering the `BorderControlDropdown` as a `prefix` on the `UnitControl`? I'm wondering if we can remove some CSS overrides in there too if this PR lands.

#### aaronrobertshaw on 2022-07-06T06:00:44Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914443699
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -18,22 +18,13 @@ type SelectProps = {
 
 type InputProps = {
 	disableUnits?: boolean;
-	size: SelectSize;
 };
 
 export const Root = styled.div`
 	box-sizing: border-box;
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits }: InputProps ) => {
-	if ( disableUnits ) return '';
-
-	return css`
-		${ rtl( { paddingRight: 8 } )() };
-	`;
-};
-
```

My memory is failing me on this one, I don't recall any blockers. It might have been as simple as I didn't see the `prefix` prop in the `UnitControl` readme and didn't follow the props through to the underlying `InputControl`. 

It also looks like we might need to tweak the `UnitControl` types, as attempting to pass the border control dropdown via a `prefix` prop on the `UnitControl` results in a type error that it expects `prefix` to be `string | undefined`.

I'd be happy to work on rendering the border control's dropdown as a prefix in a follow up to this PR.

#### mirka on 2022-07-06T19:56:41Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r915205667
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -18,22 +18,13 @@ type SelectProps = {
 
 type InputProps = {
 	disableUnits?: boolean;
-	size: SelectSize;
 };
 
 export const Root = styled.div`
 	box-sizing: border-box;
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits }: InputProps ) => {
-	if ( disableUnits ) return '';
-
-	return css`
-		${ rtl( { paddingRight: 8 } )() };
-	`;
-};
-
```

That sounds great, thanks! It's nice to see things getting less hacky as we converge on common patterns 🙂

#### aaronrobertshaw on 2022-07-07T06:26:28Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r915502147
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -18,22 +18,13 @@ type SelectProps = {
 
 type InputProps = {
 	disableUnits?: boolean;
-	size: SelectSize;
 };
 
 export const Root = styled.div`
 	box-sizing: border-box;
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits }: InputProps ) => {
-	if ( disableUnits ) return '';
-
-	return css`
-		${ rtl( { paddingRight: 8 } )() };
-	`;
-};
-
```

I've put together a PR (https://github.com/WordPress/gutenberg/pull/42212) that now renders the dropdown via the `UnitControl` prefix and attempts to clean up the styles. There could still be more to polish on that control when it comes time to add a large 40px size variant.

### Review thread 4 (resolved)

#### ciampo on 2022-07-05T18:28:53Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914086372
Path: packages/components/src/input-control/styles/input-control-styles.tsx

```diff
@@ -235,6 +244,7 @@ export const Input = styled.input< InputProps >`
 		${ disabledStyles }
 		${ fontSizeStyles }
 		${ sizeStyles }
+		${ customPaddings }
```

For the sake of making the logic of this override more explicit, I wonder if we should move the application of the custom paddings directly into the `sizeStyles` function.

Alternatively, we could at least add an inline comment making the padding override more obvious?

#### mirka on 2022-07-05T21:44:11Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914228425
Path: packages/components/src/input-control/styles/input-control-styles.tsx

```diff
@@ -235,6 +244,7 @@ export const Input = styled.input< InputProps >`
 		${ disabledStyles }
 		${ fontSizeStyles }
 		${ sizeStyles }
+		${ customPaddings }
```

Yes, something like this maybe? 38069ad

The custom padding API (as currently in this PR) is designed to be separate from the sizing concerns, but depending on the kind of use cases we discover, it might eventually make sense to make it a part of the sizing concerns.

### Review thread 5 (resolved)

#### ciampo on 2022-07-05T18:36:15Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914094954
Path: packages/components/src/input-control/index.tsx

```diff
@@ -85,6 +86,8 @@ export function UnforwardedInputControl(
 				isPressEnterToChange={ isPressEnterToChange }
 				onKeyDown={ onKeyDown }
 				onValidate={ onValidate }
+				paddingInlineStart={ prefix ? space( 2 ) : undefined }
+				paddingInlineEnd={ suffix ? space( 2 ) : undefined }
```

Instead of adding the internal `paddingInlineStart` and `paddingInlineEnd` internal props, we could also consider adding two `boolean` props called something like `reducedPaddingInlineStart` and `reducedPaddingInlineEnd`?

This is mostly to minimize exposing design-related props, and instead expose props that are more "semantic" instead (which I believe make the component more resilient to design changes).

#### mirka on 2022-07-05T21:36:04Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914223910
Path: packages/components/src/input-control/index.tsx

```diff
@@ -85,6 +86,8 @@ export function UnforwardedInputControl(
 				isPressEnterToChange={ isPressEnterToChange }
 				onKeyDown={ onKeyDown }
 				onValidate={ onValidate }
+				paddingInlineStart={ prefix ? space( 2 ) : undefined }
+				paddingInlineEnd={ suffix ? space( 2 ) : undefined }
```

Exactly, I did consider booleans (called `hasPrefix`/`hasSuffix`, for example). The upside, from a maintainer's standpoint, is that we would make fewer or no promises on what effect the prop is going to have on the visual styling, especially in regards to the different size variants. The downside is that, it's less generic, so it wouldn't be flexible enough to cover possible cases where someone needs more granular control over the padding.

In any case, `InputField` itself is internal-only, so I'm not overly concerned about the specific API and contracts at this point. It's quite possible that we'll never expose custom padding props at the top-level `InputControl`. I slightly prefer to keep it flexible for now, since we're still kind of in the middle of discovering use cases for this custom padding.

### Review thread 6 (resolved)

#### aaronrobertshaw on 2022-07-06T06:07:48Z

URL: https://github.com/WordPress/gutenberg/pull/42166#discussion_r914447283
Path: packages/components/src/input-control/styles/input-control-styles.tsx

```diff
@@ -145,6 +147,7 @@ const sizeStyles = ( {
 	inputSize: size,
 	__next36pxDefaultSize,
 }: InputProps ) => {
+	// Paddings may be overriden by the custom paddings props.
```

```suggestion
	// Paddings may be overridden by the custom paddings props.
```

Nit: Typo.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/input-control/index.tsx (MODIFIED, +3/-0)
- packages/components/src/input-control/stories/index.tsx (MODIFIED, +2/-2)
- packages/components/src/input-control/styles/input-control-styles.tsx (MODIFIED, +11/-0)
- packages/components/src/input-control/types.ts (MODIFIED, +10/-3)
- packages/components/src/unit-control/styles/unit-control-styles.ts (MODIFIED, +0/-10)
- packages/components/src/unit-control/test/__snapshots__/index.tsx.snap (MODIFIED, +3/-3)
