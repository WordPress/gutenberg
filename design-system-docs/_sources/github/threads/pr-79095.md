---
number: 79095
type: pull_request
state: MERGED
merged_at: 2026-06-15T17:32:42Z
url: https://github.com/WordPress/gutenberg/pull/79095
matched_labels: ["[Package] Components","[Package] UI","[Package] Theme"]
all_labels: ["[Type] Code Quality","[Package] Data","[Package] Server Side Render","[Package] Compose","[Package] Core data","[Package] Viewport","[Package] Keycodes","[Package] Plugins","[Package] Components","[Package] Blocks","[Package] Editor","[Package] Block library","[Package] Notices","[Package] Rich text","[Package] Block editor","[Package] Primitives","[Package] Icons","[Package] Keyboard Shortcuts","[Package] React i18n","[Package] Style Engine","[Package] Preferences","[Package] Media Utils","[Package] Interactivity","[Package] DataViews","[Package] Fields","[Package] Router","[Package] Theme","[Package] UI"]
---

# PR #79095: Packages: Fix the published dependency surface of npm packages

- URL: https://github.com/WordPress/gutenberg/pull/79095
- Author: manzoorwanijk
- Created: 2026-06-10T15:28:14Z
- Updated: 2026-06-15T17:43:21Z
- Comments: 6 of 6
- Review threads: 3 of 3
- Changed files: 49 of 49
- Diff: ../pr-diffs/79095.diff

## Body

## What?

Follow up to #78882 ([discussion](https://github.com/WordPress/gutenberg/pull/78882#issuecomment-4650348761)).

Fixes every remaining case where a package's **published artifact** — its runtime JS or emitted `build-types/*.d.ts` — references a module that consumers installing from npm cannot resolve, as surfaced by auditing all packages with [`@mawesome/dependency-audit`](https://www.npmjs.com/package/@mawesome/dependency-audit) (wired into the repo as `npm run lint:published-deps` in #79094, so regressions are caught going forward).

## Why?

#78882 declared `@types/react` where packages' emitted declarations reference React types, but a full audit of the published artifacts showed more gaps, and one placement issue in #78882 itself:

-   **`@types/react` as a hard `dependency` while `react` is a peer is the wrong placement.** When `react` is supplied by the consumer, the React *type* version must match the consumer's React too. A hard dependency drags in our own copy, which can collide with the consumer's `@types/react` (two structurally-incompatible JSX namespaces) and defeats the decoupling that made `react` a peer in the first place. The type surface should mirror the runtime surface: an optional `peerDependency` (optional so pure-JS consumers see no install warning). This is the established pattern in the React ecosystem.
-   **Some packages import `react/jsx-runtime` at runtime without declaring `react` at all.**
-   **Some packages reference modules in their published files that resolve today only via root hoisting or a workspace link** — the exact bug class of #74310 and #74655.
-   **`@wordpress/data` leaked `rememo`'s types into six consumers' declarations.** `createSelector` was a bare re-export, so consumers' inferred selector types were emitted as `... & import("rememo").EnhancedSelector` in *their* `.d.ts`, and they do not declare `rememo`. With `skipLibCheck: true` those exports silently degrade to `any` for npm consumers.

## How?

-   `@types/react` moves from `dependencies` to an optional `peerDependency` in the 38 packages where `react` is a peer (or where only the types are referenced). `@wordpress/element` keeps it as a dependency since it depends on `react` directly.
-   `react` (`^18.0.0`) is added as a peer to the four packages that import `react/jsx-runtime` at runtime but never declared it: `connectors`, `content-types`, `lazy-editor`, `react-i18n`.
-   Genuinely missing dependencies are declared: `@ariakit/react-core` (components), `redux` (editor), `@preact/signals-core` (interactivity), `rimraf` (scripts); <s>`storybook` moves from dev to prod dependencies in `design-system-mcp` (its emitted types reference it)</s>.
-   `@wordpress/theme` declares its bundler plugins' host tools — `postcss`, `esbuild`, `vite` — as optional peers, following its existing `stylelint` plugin pattern.
-   `@wordpress/data` now owns the `createSelector` types: `create-selector.ts` declares `EnhancedSelector`/`GetDependants` and annotates the export explicitly (same runtime function, zero behavior change), so consumers' emitted declarations reference `import("@wordpress/data")` instead of `rememo`. The two types are re-exported from the package index because TypeScript requires the type to be nameable through the package's public entry to emit a portable reference (TS2883 otherwise).

No runtime changes anywhere; `npm run build` type-checks clean and the regenerated `packages/data/README.md` API docs are included.

## Testing Instructions

1. `npm ci`
2. `npm run build` — confirm the build and type checks pass.
3. `grep -r 'import("rememo")' packages/*/build-types/` — confirm zero hits (on trunk, 6 packages emit such references).
4. Optionally audit any package's published surface: `npx @mawesome/dependency-audit packages/<name>`.

### Testing Instructions for Keyboard

N/A — dependency declaration and type-only changes, no UI.

## Screenshots or screencast

N/A.

## Use of AI Tools

Used Claude Code (with the dependency-audit tool) to identify the affected packages, apply the dependency moves, and validate the results; the Codex CLI reviewed the `createSelector` type-ownership approach. The diff was reviewed before submission.

## Issue comments

### github-actions on 2026-06-10T15:28:28Z

URL: https://github.com/WordPress/gutenberg/pull/79095#issuecomment-4671805199

<!-- Enforce labels on Pull Request/type-related-labels/__mheap_github-action-required-labels -->
**Warning: Type of PR label mismatch**

 To merge this PR, it requires exactly 1 label indicating the type of PR. Other labels are optional and not being checked here. 
- **Required label**: Any label starting with `[Type]`.
- **Labels found**: .

Read more about [Type labels in Gutenberg](https://github.com/WordPress/gutenberg/labels?q=type). Don't worry if you don't have the required permissions to add labels; the PR reviewer should be able to help with the task.

### github-actions on 2026-06-10T15:28:29Z

URL: https://github.com/WordPress/gutenberg/pull/79095#issuecomment-4671805228

<!-- Enforce labels on Pull Request/type-related-labels/__mheap_github-action-required-labels -->
**Warning: Type of PR label mismatch**

 To merge this PR, it requires exactly 1 label indicating the type of PR. Other labels are optional and not being checked here. 
- **Required label**: Any label starting with `[Type]`.
- **Labels found**: .

Read more about [Type labels in Gutenberg](https://github.com/WordPress/gutenberg/labels?q=type). Don't worry if you don't have the required permissions to add labels; the PR reviewer should be able to help with the task.

### github-actions on 2026-06-10T15:39:52Z

URL: https://github.com/WordPress/gutenberg/pull/79095#issuecomment-4671880256

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: manzoorwanijk <manzoorwanijk@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-10T15:57:30Z

URL: https://github.com/WordPress/gutenberg/pull/79095#issuecomment-4672006419

**Size Change:** +3 B (0%) 

**Total Size:** 8.58 MB

<details open><summary>📦 <strong>View Changed</strong></summary>

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/scripts/data/index.min.js` | 9.74 kB | +3 B (+0.03%) |

</details>

<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-10T16:21:58Z

URL: https://github.com/WordPress/gutenberg/pull/79095#issuecomment-4672167049

<!-- flaky-tests-report-comment -->
**Flaky tests detected in fd1f7ade8794e4ba84744713381db1ea2f71ad81.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27562091144
📝  Reported issues:
- #78706 in `/test/e2e/specs/editor/various/autosave.spec.js`

### aduth on 2026-06-11T19:25:27Z

URL: https://github.com/WordPress/gutenberg/pull/79095#issuecomment-4684171305

> * **Some packages import `react/jsx-runtime` at runtime without declaring `react` at all.**

Maybe we don't care anymore because we'd want to move away from `@wordpress/element` as an abstraction in favor of React directly, but this feels like it's a leakiness in the abstraction that `react` as a direct dependency makes its way into built outputs, instead of `@wordpress/element`.

## Review threads

### Review thread 1 (resolved)

#### aduth on 2026-06-11T19:27:56Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3398607235
Path: packages/design-system-mcp/package.json

```diff
@@ -43,11 +43,11 @@
 	"dependencies": {
 		"@cfworker/json-schema": "^4.1.1",
 		"@modelcontextprotocol/server": "^2.0.0-alpha.2",
+		"storybook": "^10.2.8",
```

Ooof this isn't really acceptable to me to impose this as a dependency, especially with how the package is consumed by AI clients. I follow that emitted types reference it, but I'm eager to try to fix _that_ rather than making it a dependency. I can look into this.

#### aduth on 2026-06-11T19:36:18Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3398659602
Path: packages/design-system-mcp/package.json

```diff
@@ -43,11 +43,11 @@
 	"dependencies": {
 		"@cfworker/json-schema": "^4.1.1",
 		"@modelcontextprotocol/server": "^2.0.0-alpha.2",
+		"storybook": "^10.2.8",
```

This looks to be related to this past discussion and change where we reused Storybook's internal types: https://github.com/WordPress/gutenberg/pull/78185#discussion_r3227505056 (commit https://github.com/WordPress/gutenberg/commit/4c026d4b62af639703ac53dcbb6c681d69474c28)

I think I'd rather just go back to maintaining a duplicate copy if that's the only alternative to making Storybook a downstream dependency.

#### aduth on 2026-06-11T19:46:30Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3398726385
Path: packages/design-system-mcp/package.json

```diff
@@ -43,11 +43,11 @@
 	"dependencies": {
 		"@cfworker/json-schema": "^4.1.1",
 		"@modelcontextprotocol/server": "^2.0.0-alpha.2",
+		"storybook": "^10.2.8",
```

Fixing this at #79132

#### manzoorwanijk on 2026-06-13T05:57:46Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3407514772
Path: packages/design-system-mcp/package.json

```diff
@@ -43,11 +43,11 @@
 	"dependencies": {
 		"@cfworker/json-schema": "^4.1.1",
 		"@modelcontextprotocol/server": "^2.0.0-alpha.2",
+		"storybook": "^10.2.8",
```

This is now removed from the dependencies.

### Review thread 2

#### aduth on 2026-06-15T17:12:18Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3415246260
Path: packages/scripts/package.json

```diff
@@ -80,6 +80,7 @@
 		"react-refresh": "^0.14.0",
 		"read-pkg-up": "^7.0.1",
 		"resolve-bin": "^0.4.0",
+		"rimraf": "^5.0.10",
```

Should this be a `devDependencies`? I only see it used in test files.

#### manzoorwanijk on 2026-06-15T17:22:38Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3415300960
Path: packages/scripts/package.json

```diff
@@ -80,6 +80,7 @@
 		"react-refresh": "^0.14.0",
 		"read-pkg-up": "^7.0.1",
 		"resolve-bin": "^0.4.0",
+		"rimraf": "^5.0.10",
```

The reason is that `test` directory gets published to npm, which we should probably avoid. I will do that as a follow-up.

#### manzoorwanijk on 2026-06-15T17:43:21Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3415417705
Path: packages/scripts/package.json

```diff
@@ -80,6 +80,7 @@
 		"react-refresh": "^0.14.0",
 		"read-pkg-up": "^7.0.1",
 		"resolve-bin": "^0.4.0",
+		"rimraf": "^5.0.10",
```

Created #79204

### Review thread 3

#### aduth on 2026-06-15T17:15:04Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3415261896
Path: packages/theme/package.json

```diff
@@ -98,16 +97,33 @@
 		"@types/node": "^20.19.39",
 		"esbuild": "^0.27.2",
 		"esbuild-esm-loader": "^0.3.3",
-		"storybook": "^10.2.8"
+		"storybook": "^10.2.8",
+		"stylelint": "^16.8.2"
```

Is the idea with making this a devDependency as well as an optional peer dependency to make sure that it's available in Gutenberg itself for type-checking?

Should that also apply to everything else we're defining as an optional peer dependency ?

#### manzoorwanijk on 2026-06-15T17:23:32Z

URL: https://github.com/WordPress/gutenberg/pull/79095#discussion_r3415306213
Path: packages/theme/package.json

```diff
@@ -98,16 +97,33 @@
 		"@types/node": "^20.19.39",
 		"esbuild": "^0.27.2",
 		"esbuild-esm-loader": "^0.3.3",
-		"storybook": "^10.2.8"
+		"storybook": "^10.2.8",
+		"stylelint": "^16.8.2"
```

Yes, if you declare a dependency as peer and you also consume it in the package itself, then dev dependency is what works locally and peer for consumers.

## Changed files

- package-lock.json (MODIFIED, +260/-40)
- packages/admin-ui/package.json (MODIFIED, +6/-1)
- packages/block-editor/package.json (MODIFIED, +6/-1)
- packages/block-library/package.json (MODIFIED, +6/-1)
- packages/blocks/package.json (MODIFIED, +6/-1)
- packages/boot/package.json (MODIFIED, +6/-1)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/package.json (MODIFIED, +6/-1)
- packages/components/src/card/card-divider/hook.ts (MODIFIED, +5/-1)
- packages/compose/package.json (MODIFIED, +6/-1)
- packages/connectors/package.json (MODIFIED, +9/-1)
- packages/content-types/package.json (MODIFIED, +9/-1)
- packages/core-data/package.json (MODIFIED, +6/-1)
- packages/data/README.md (MODIFIED, +12/-2)
- packages/data/package.json (MODIFIED, +6/-1)
- packages/data/src/create-selector.ts (MODIFIED, +36/-2)
- packages/data/src/index.ts (MODIFIED, +1/-0)
- packages/dataviews/CHANGELOG.md (MODIFIED, +4/-0)
- packages/dataviews/package.json (MODIFIED, +6/-1)
- packages/editor/package.json (MODIFIED, +7/-1)
- packages/fields/package.json (MODIFIED, +6/-1)
- packages/global-styles-ui/package.json (MODIFIED, +6/-1)
- packages/grid/package.json (MODIFIED, +6/-1)
- packages/icons/package.json (MODIFIED, +6/-1)
- packages/image-cropper/package.json (MODIFIED, +6/-1)
- packages/interactivity/package.json (MODIFIED, +1/-0)
- packages/keyboard-shortcuts/package.json (MODIFIED, +6/-1)
- packages/keycodes/package.json (MODIFIED, +8/-1)
- packages/lazy-editor/package.json (MODIFIED, +9/-1)
- packages/list-reusable-blocks/package.json (MODIFIED, +6/-1)
- packages/media-editor/package.json (MODIFIED, +6/-1)
- packages/media-fields/package.json (MODIFIED, +6/-1)
- packages/media-utils/package.json (MODIFIED, +6/-1)
- packages/notices/package.json (MODIFIED, +6/-1)
- packages/plugins/package.json (MODIFIED, +6/-1)
- packages/preferences/package.json (MODIFIED, +6/-1)
- packages/primitives/package.json (MODIFIED, +6/-1)
- packages/react-i18n/package.json (MODIFIED, +9/-1)
- packages/rich-text/package.json (MODIFIED, +6/-1)
- packages/router/package.json (MODIFIED, +6/-1)
- packages/scripts/package.json (MODIFIED, +1/-0)
- packages/server-side-render/package.json (MODIFIED, +6/-1)
- packages/style-engine/package.json (MODIFIED, +8/-1)
- packages/theme/CHANGELOG.md (MODIFIED, +4/-0)
- packages/theme/package.json (MODIFIED, +19/-3)
- packages/ui/CHANGELOG.md (MODIFIED, +4/-0)
- packages/ui/package.json (MODIFIED, +6/-1)
- packages/upload-media/package.json (MODIFIED, +6/-1)
- packages/viewport/package.json (MODIFIED, +6/-1)
