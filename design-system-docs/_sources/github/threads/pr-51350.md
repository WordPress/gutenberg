---
number: 51350
type: pull_request
state: MERGED
merged_at: 2023-09-28T13:40:30Z
url: https://github.com/WordPress/gutenberg/pull/51350
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #51350: SlotFill: Migrate to Typescript.

- URL: https://github.com/WordPress/gutenberg/pull/51350
- Author: torounit
- Created: 2023-06-08T16:22:56Z
- Updated: 2023-09-28T13:40:55Z
- Comments: 9 of 9
- Review threads: 49 of 49
- Changed files: 21 of 21
- Diff: ../pr-diffs/51350.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->
part of #35744. Replaced some class Component with functional components.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->


See https://github.com/WordPress/gutenberg/issues/35744

## How?
* Refactored the component TypeScript

## Testing Instructions
* `npm run test:unit -- --testPathPattern packages/components/src/slot-fill/`

## Issue comments

### github-actions on 2023-06-08T17:08:07Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1583043450

**Size Change:** -65 B (0%) 

**Total Size:** 1.62 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/components/index.min.js` | 247 kB | -65 B (0%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 964 B |
| `build/annotations/index.min.js` | 2.71 kB |
| `build/api-fetch/index.min.js` | 2.29 kB |
| `build/autop/index.min.js` | 2.11 kB |
| `build/blob/index.min.js` | 461 B |
| `build/block-directory/index.min.js` | 7.05 kB |
| `build/block-directory/style-rtl.css` | 1.04 kB |
| `build/block-directory/style.css` | 1.04 kB |
| `build/block-editor/content-rtl.css` | 4.28 kB |
| `build/block-editor/content.css` | 4.27 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 403 B |
| `build/block-editor/default-editor-styles.css` | 403 B |
| `build/block-editor/index.min.js` | 218 kB |
| `build/block-editor/style-rtl.css` | 15.6 kB |
| `build/block-editor/style.css` | 15.6 kB |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 138 B |
| `build/block-library/blocks/audio/theme.css` | 138 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 104 B |
| `build/block-library/blocks/avatar/style.css` | 104 B |
| `build/block-library/blocks/block/editor-rtl.css` | 305 B |
| `build/block-library/blocks/block/editor.css` | 305 B |
| `build/block-library/blocks/button/editor-rtl.css` | 587 B |
| `build/block-library/blocks/button/editor.css` | 587 B |
| `build/block-library/blocks/button/style-rtl.css` | 633 B |
| `build/block-library/blocks/button/style.css` | 632 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 113 B |
| `build/block-library/blocks/categories/editor.css` | 112 B |
| `build/block-library/blocks/categories/style-rtl.css` | 124 B |
| `build/block-library/blocks/categories/style.css` | 124 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 421 B |
| `build/block-library/blocks/columns/style.css` | 421 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 647 B |
| `build/block-library/blocks/cover/editor.css` | 650 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.7 kB |
| `build/block-library/blocks/cover/style.css` | 1.69 kB |
| `build/block-library/blocks/details/editor-rtl.css` | 65 B |
| `build/block-library/blocks/details/editor.css` | 65 B |
| `build/block-library/blocks/details/style-rtl.css` | 98 B |
| `build/block-library/blocks/details/style.css` | 98 B |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 138 B |
| `build/block-library/blocks/embed/theme.css` | 138 B |
| `build/block-library/blocks/file/editor-rtl.css` | 316 B |
| `build/block-library/blocks/file/editor.css` | 316 B |
| `build/block-library/blocks/file/style-rtl.css` | 311 B |
| `build/block-library/blocks/file/style.css` | 312 B |
| `build/block-library/blocks/file/view.min.js` | 321 B |
| `build/block-library/blocks/footnotes/style-rtl.css` | 201 B |
| `build/block-library/blocks/footnotes/style.css` | 199 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.61 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.61 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 957 B |
| `build/block-library/blocks/gallery/editor.css` | 962 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.55 kB |
| `build/block-library/blocks/gallery/style.css` | 1.55 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 654 B |
| `build/block-library/blocks/group/editor.css` | 654 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 189 B |
| `build/block-library/blocks/heading/style.css` | 189 B |
| `build/block-library/blocks/html/editor-rtl.css` | 340 B |
| `build/block-library/blocks/html/editor.css` | 341 B |
| `build/block-library/blocks/image/editor-rtl.css` | 834 B |
| `build/block-library/blocks/image/editor.css` | 833 B |
| `build/block-library/blocks/image/style-rtl.css` | 1.42 kB |
| `build/block-library/blocks/image/style.css` | 1.41 kB |
| `build/block-library/blocks/image/theme-rtl.css` | 137 B |
| `build/block-library/blocks/image/theme.css` | 137 B |
| `build/block-library/blocks/image/view.min.js` | 1.83 kB |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 357 B |
| `build/block-library/blocks/latest-comments/style.css` | 357 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 213 B |
| `build/block-library/blocks/latest-posts/editor.css` | 212 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 478 B |
| `build/block-library/blocks/latest-posts/style.css` | 478 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 505 B |
| `build/block-library/blocks/media-text/style.css` | 503 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 671 B |
| `build/block-library/blocks/navigation-link/editor.css` | 672 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.26 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.26 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.26 kB |
| `build/block-library/blocks/navigation/style.css` | 2.25 kB |
| `build/block-library/blocks/navigation/view.min.js` | 1.01 kB |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 401 B |
| `build/block-library/blocks/page-list/editor.css` | 401 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 235 B |
| `build/block-library/blocks/paragraph/editor.css` | 235 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 335 B |
| `build/block-library/blocks/paragraph/style.css` | 335 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 508 B |
| `build/block-library/blocks/post-comments-form/style.css` | 508 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 71 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 71 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 141 B |
| `build/block-library/blocks/post-excerpt/style.css` | 141 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 588 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 586 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 322 B |
| `build/block-library/blocks/post-featured-image/style.css` | 322 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 215 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 214 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 314 B |
| `build/block-library/blocks/post-template/style.css` | 314 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-time-to-read/style-rtl.css` | 69 B |
| `build/block-library/blocks/post-time-to-read/style.css` | 69 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 125 B |
| `build/block-library/blocks/preformatted/style.css` | 125 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 335 B |
| `build/block-library/blocks/pullquote/style.css` | 335 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 168 B |
| `build/block-library/blocks/pullquote/theme.css` | 168 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 288 B |
| `build/block-library/blocks/query-pagination/style.css` | 284 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 486 B |
| `build/block-library/blocks/query/editor.css` | 486 B |
| `build/block-library/blocks/query/style-rtl.css` | 375 B |
| `build/block-library/blocks/query/style.css` | 372 B |
| `build/block-library/blocks/query/view.min.js` | 609 B |
| `build/block-library/blocks/quote/style-rtl.css` | 222 B |
| `build/block-library/blocks/quote/style.css` | 222 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 149 B |
| `build/block-library/blocks/rss/editor.css` | 149 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 184 B |
| `build/block-library/blocks/search/editor.css` | 184 B |
| `build/block-library/blocks/search/style-rtl.css` | 613 B |
| `build/block-library/blocks/search/style.css` | 613 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/search/view.min.js` | 471 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 234 B |
| `build/block-library/blocks/separator/style.css` | 234 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 329 B |
| `build/block-library/blocks/shortcode/editor.css` | 329 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 760 B |
| `build/block-library/blocks/site-logo/editor.css` | 760 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 204 B |
| `build/block-library/blocks/site-logo/style.css` | 204 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 116 B |
| `build/block-library/blocks/site-title/editor.css` | 116 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 57 B |
| `build/block-library/blocks/site-title/style.css` | 57 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 184 B |
| `build/block-library/blocks/social-link/editor.css` | 184 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 682 B |
| `build/block-library/blocks/social-links/editor.css` | 681 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.45 kB |
| `build/block-library/blocks/social-links/style.css` | 1.45 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 359 B |
| `build/block-library/blocks/spacer/editor.css` | 359 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 432 B |
| `build/block-library/blocks/table/editor.css` | 432 B |
| `build/block-library/blocks/table/style-rtl.css` | 646 B |
| `build/block-library/blocks/table/style.css` | 645 B |
| `build/block-library/blocks/table/theme-rtl.css` | 157 B |
| `build/block-library/blocks/table/theme.css` | 157 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 251 B |
| `build/block-library/blocks/tag-cloud/style.css` | 253 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 403 B |
| `build/block-library/blocks/template-part/editor.css` | 403 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/term-description/style-rtl.css` | 111 B |
| `build/block-library/blocks/term-description/style.css` | 111 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 191 B |
| `build/block-library/blocks/video/style.css` | 191 B |
| `build/block-library/blocks/video/theme-rtl.css` | 139 B |
| `build/block-library/blocks/video/theme.css` | 139 B |
| `build/block-library/classic-rtl.css` | 179 B |
| `build/block-library/classic.css` | 179 B |
| `build/block-library/common-rtl.css` | 1.11 kB |
| `build/block-library/common.css` | 1.11 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 12.2 kB |
| `build/block-library/editor.css` | 12.2 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/index.min.js` | 207 kB |
| `build/block-library/reset-rtl.css` | 472 B |
| `build/block-library/reset.css` | 472 B |
| `build/block-library/style-rtl.css` | 14 kB |
| `build/block-library/style.css` | 14 kB |
| `build/block-library/theme-rtl.css` | 700 B |
| `build/block-library/theme.css` | 705 B |
| `build/block-serialization-default-parser/index.min.js` | 1.13 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.87 kB |
| `build/blocks/index.min.js` | 51.5 kB |
| `build/commands/index.min.js` | 15.5 kB |
| `build/commands/style-rtl.css` | 947 B |
| `build/commands/style.css` | 942 B |
| `build/components/style-rtl.css` | 11.8 kB |
| `build/components/style.css` | 11.8 kB |
| `build/compose/index.min.js` | 12.7 kB |
| `build/core-commands/index.min.js` | 2.71 kB |
| `build/core-data/index.min.js` | 70.5 kB |
| `build/customize-widgets/index.min.js` | 12 kB |
| `build/customize-widgets/style-rtl.css` | 1.51 kB |
| `build/customize-widgets/style.css` | 1.5 kB |
| `build/data-controls/index.min.js` | 651 B |
| `build/data/index.min.js` | 8.87 kB |
| `build/date/index.min.js` | 17.9 kB |
| `build/deprecated/index.min.js` | 462 B |
| `build/dom-ready/index.min.js` | 336 B |
| `build/dom/index.min.js` | 4.68 kB |
| `build/edit-post/classic-rtl.css` | 571 B |
| `build/edit-post/classic.css` | 571 B |
| `build/edit-post/index.min.js` | 35.6 kB |
| `build/edit-post/style-rtl.css` | 7.92 kB |
| `build/edit-post/style.css` | 7.91 kB |
| `build/edit-site/index.min.js` | 185 kB |
| `build/edit-site/style-rtl.css` | 14 kB |
| `build/edit-site/style.css` | 14 kB |
| `build/edit-widgets/index.min.js` | 17 kB |
| `build/edit-widgets/style-rtl.css` | 4.84 kB |
| `build/edit-widgets/style.css` | 4.84 kB |
| `build/editor/index.min.js` | 45.9 kB |
| `build/editor/style-rtl.css` | 3.58 kB |
| `build/editor/style.css` | 3.58 kB |
| `build/element/index.min.js` | 4.87 kB |
| `build/escape-html/index.min.js` | 548 B |
| `build/format-library/index.min.js` | 7.78 kB |
| `build/format-library/style-rtl.css` | 577 B |
| `build/format-library/style.css` | 577 B |
| `build/hooks/index.min.js` | 1.57 kB |
| `build/html-entities/index.min.js` | 454 B |
| `build/i18n/index.min.js` | 3.61 kB |
| `build/interactivity/index.min.js` | 11.4 kB |
| `build/is-shallow-equal/index.min.js` | 535 B |
| `build/keyboard-shortcuts/index.min.js` | 1.74 kB |
| `build/keycodes/index.min.js` | 1.9 kB |
| `build/list-reusable-blocks/index.min.js` | 2.2 kB |
| `build/list-reusable-blocks/style-rtl.css` | 865 B |
| `build/list-reusable-blocks/style.css` | 865 B |
| `build/media-utils/index.min.js` | 2.92 kB |
| `build/notices/index.min.js` | 964 B |
| `build/nux/index.min.js` | 2 kB |
| `build/nux/style-rtl.css` | 775 B |
| `build/nux/style.css` | 771 B |
| `build/patterns/index.min.js` | 3.56 kB |
| `build/patterns/style-rtl.css` | 325 B |
| `build/patterns/style.css` | 325 B |
| `build/plugins/index.min.js` | 1.8 kB |
| `build/preferences-persistence/index.min.js` | 1.85 kB |
| `build/preferences/index.min.js` | 1.25 kB |
| `build/primitives/index.min.js` | 994 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/private-apis/index.min.js` | 972 B |
| `build/react-i18n/index.min.js` | 624 B |
| `build/react-refresh-entry/index.min.js` | 9.46 kB |
| `build/react-refresh-runtime/index.min.js` | 6.78 kB |
| `build/redux-routine/index.min.js` | 2.71 kB |
| `build/reusable-blocks/index.min.js` | 2.72 kB |
| `build/reusable-blocks/style-rtl.css` | 265 B |
| `build/reusable-blocks/style.css` | 265 B |
| `build/rich-text/index.min.js` | 10.2 kB |
| `build/router/index.min.js` | 1.78 kB |
| `build/server-side-render/index.min.js` | 1.95 kB |
| `build/shortcode/index.min.js` | 1.4 kB |
| `build/style-engine/index.min.js` | 1.98 kB |
| `build/token-list/index.min.js` | 587 B |
| `build/url/index.min.js` | 3.84 kB |
| `build/vendors/inert-polyfill.min.js` | 2.48 kB |
| `build/vendors/react-dom.min.js` | 41.8 kB |
| `build/vendors/react.min.js` | 4.02 kB |
| `build/viewport/index.min.js` | 968 B |
| `build/warning/index.min.js` | 259 B |
| `build/widgets/index.min.js` | 7.17 kB |
| `build/widgets/style-rtl.css` | 1.18 kB |
| `build/widgets/style.css` | 1.18 kB |
| `build/wordcount/index.min.js` | 1.03 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2023-06-08T19:33:41Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1583217834

<!-- flaky-tests-report-comment -->
**Flaky tests detected in ff18ed55abb9b89e83e0d0175efc729cf835b90c.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/6334034411
📝  Reported issues:
- #53524 in `/test/e2e/specs/editor/various/multi-block-selection.spec.js`

### ciampo on 2023-06-28T18:07:43Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1611862729

Sorry for the delay in reviewing, @torounit . I'll try to take a look, but in the meantime I've also added a few more reviewers.

### mirka on 2023-07-21T12:01:19Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1645471399

Thanks for going through the feedback! Is this ready for re-review now?

### torounit on 2023-07-21T15:37:51Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1645776040

@mirka Thanks. It will still take a little more time. I will mensh when I am ready to re-review!

### margolisj on 2023-08-28T05:22:34Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1695028511

Really dope @torounit !!

### torounit on 2023-09-06T16:15:46Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1708695571

@mirka @tyxla Could you re-review?

### ciampo on 2023-09-07T16:12:31Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1710430275

@mirka is not going to be around for a while!

@tyxla or I will try to take a look at this soon enough — sorry for the delay!

### torounit on 2023-09-28T07:01:54Z

URL: https://github.com/WordPress/gutenberg/pull/51350#issuecomment-1738591256

https://github.com/WordPress/gutenberg/pull/51350#pullrequestreview-1633288376

@ciampo @tyxla  Thank you for a lot of reviewing!!!

I will submit another pull request for improvements to unit test.

I think I'm ready to merge for now, but what do you think?

## Review threads

### Review thread 1 (resolved)

#### torounit on 2023-06-08T18:52:58Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1223430769
Path: packages/components/src/dropdown-menu-v2/index.tsx

```diff
@@ -75,7 +75,7 @@ const UnconnectedDropdownMenu = ( props: DropdownMenuProps ) => {
 
 	// Render the portal in the default slot used by the legacy Popover component.
 	const slot = useSlot( POPOVER_DEFAULT_SLOT_NAME );
-	const portalContainer = slot.ref?.current;
+	const portalContainer = slot.ref?.current || null;
```

https://github.com/WordPress/gutenberg/blob/3b4ae6043f422cfbd60abd0e2aaac1cd227422e2/packages/components/src/dropdown-menu-v2/index.tsx#L47-L51

portalContainer is not undefined.

### Review thread 2 (resolved)

#### torounit on 2023-06-08T18:57:55Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1223434900
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -41,7 +49,11 @@ function Slot(
 	} );
 
 	return (
-		<Component ref={ useMergeRefs( [ forwardedRef, ref ] ) } { ...props } />
+		<View
+			as={ Component }
+			ref={ useMergeRefs( [ forwardedRef, ref ] ) }
+			{ ...restProps }
+		/>
```

When WordPressComponentProps is IsPolymorphic, using with ref,

`TS2590: Expression produces a union type that is too complex to represent.`

error occurs, so we use `View` as a workaround.

#### tyxla on 2023-06-30T13:16:21Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247853511
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -41,7 +49,11 @@ function Slot(
 	} );
 
 	return (
-		<Component ref={ useMergeRefs( [ forwardedRef, ref ] ) } { ...props } />
+		<View
+			as={ Component }
+			ref={ useMergeRefs( [ forwardedRef, ref ] ) }
+			{ ...restProps }
+		/>
```

Is this something we should fix separately?

#### mirka on 2023-06-30T21:04:55Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248284719
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -41,7 +49,11 @@ function Slot(
 	} );
 
 	return (
-		<Component ref={ useMergeRefs( [ forwardedRef, ref ] ) } { ...props } />
+		<View
+			as={ Component }
+			ref={ useMergeRefs( [ forwardedRef, ref ] ) }
+			{ ...restProps }
+		/>
```

Yes, I think I'd be more comfortable fixing it separately, since it will introduce some subtle changes (e.g. add a `.components-view` class).

Ultimately, I think using `View` like this is the correct way to fix it though. Also we don't need to destructure the `as` prop (`as: Component = 'div'`), since View defaults to `div` anyway.

### Review thread 3 (resolved)

#### torounit on 2023-06-08T19:31:49Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1223464527
Path: packages/components/src/dropdown-menu-v2/types.ts

```diff
@@ -261,5 +261,5 @@ export type DropdownMenuPrivateContext = Pick<
 	DropdownMenuInternalContext,
 	'variant'
 > & {
-	portalContainer: HTMLElement | null;
+	portalContainer?: HTMLElement | null;
```

https://github.com/WordPress/gutenberg/blob/70ff141088ee7b732cd8eb907bfb3a9b08bd7359/packages/components/src/dropdown-menu-v2/index.tsx#L78

The above line type is an error.


If I use `const portalContainer = slot.ref?.current || null` it passes the type check, but the unit test fails.

#### torounit on 2023-06-08T19:33:06Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1223465715
Path: packages/components/src/dropdown-menu-v2/types.ts

```diff
@@ -261,5 +261,5 @@ export type DropdownMenuPrivateContext = Pick<
 	DropdownMenuInternalContext,
 	'variant'
 > & {
-	portalContainer: HTMLElement | null;
+	portalContainer?: HTMLElement | null;
```

https://github.com/radix-ui/primitives/blob/eca6babd188df465f64f23f3584738b85dba610e/packages/react/portal/src/Portal.tsx#L19

Originally, container was optional, so the type definition was modified.

### Review thread 4 (resolved)

#### torounit on 2023-06-22T19:30:49Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1238946807
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -13,12 +17,25 @@ import { useMergeRefs } from '@wordpress/compose';
 /**
  * Internal dependencies
  */
+import { View } from '../../view';
 import SlotFillContext from './slot-fill-context';
+import type { WordPressComponentProps } from '../../ui/context';
+import type { SlotComponentProps } from '../types';
 
 function Slot(
-	{ name, fillProps = {}, as: Component = 'div', ...props },
-	forwardedRef
+	props: WordPressComponentProps<
+		Omit< SlotComponentProps, 'bubblesVirtually' >,
+		'div'
+	>,
+	forwardedRef: ForwardedRef< any >
 ) {
+	const {
+		name,
+		fillProps = {},
+		children,
```

When `ReactNode` is passed to `children` in `Slot`, it is rendered, so remove `children` from `restProps`.

### Review thread 5 (resolved)

#### torounit on 2023-06-22T19:34:38Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1238949760
Path: packages/components/src/slot-fill/index.tsx

```diff
@@ -27,29 +39,39 @@ export function Fill( props ) {
 		</>
 	);
 }
-export const Slot = forwardRef( ( { bubblesVirtually, ...props }, ref ) => {
+
+export function UnforwardedSlot(
+	props: WordPressComponentProps< SlotComponentProps, 'div' >,
+	ref: ForwardedRef< any >
+) {
+	const { bubblesVirtually, ...restProps } = props;
 	if ( bubblesVirtually ) {
-		return <BubblesVirtuallySlot { ...props } ref={ ref } />;
+		return <BubblesVirtuallySlot { ...restProps } ref={ ref } />;
 	}
-	return <BaseSlot { ...props } />;
-} );
+	return <BaseSlot { ...restProps } />;
+}
+export const Slot = forwardRef( UnforwardedSlot );
 
-export function Provider( { children, ...props } ) {
+export function Provider( { children }: SlotFillProviderProps ) {
 	return (
-		<SlotFillProvider { ...props }>
+		<SlotFillProvider>
```

`SlotFillProvider` accepts only `children`.

### Review thread 6

#### torounit on 2023-06-22T19:38:43Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1238953200
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
+	bubblesVirtually: true,
+	as: 'span',
+};
+
+export const WithFillProps: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name:{ ' ' }
+				<Slot
+					{ ...props }
+					name="name"
+					fillProps={ { name: 'Grace' } }
+				/>
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" fillProps={ { age: 33 } } />
+			</p>
+
+			<Fill name="name">{ ( fillProps ) => fillProps.name }</Fill>
+			<Fill name="age">{ ( fillProps ) => fillProps.age }</Fill>
+		</SlotFillProvider>
+	);
+};
+WithFillProps.args = {
+	...Default.args,
+};
+
+export const WithSlotChildren: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name:{ ' ' }
+				<Slot { ...props } name="name">
+					{ ( fills ) => {
+						return (
+							<span style={ { color: 'red' } }>{ fills }</span>
+						);
+					} }
+				</Slot>
+			</p>
+			<p>
+				Age:{ ' ' }
+				<Slot { ...props } name="age">
+					{ ( fills ) => {
+						return (
+							<span style={ { color: 'red' } }>{ fills }</span>
+						);
+					} }
+				</Slot>
+			</p>
+			<Fill name="name">Alice</Fill>
+			<Fill name="age">18</Fill>
+		</SlotFillProvider>
+	);
+};
+WithSlotChildren.args = {
+	...Default.args,
+};
```

Stories when a function is passed to a `children` of `slot`.  If `bubblesVirtually` is true, The `children` are ignored.

### Review thread 7 (resolved)

#### torounit on 2023-06-22T19:42:29Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1238956679
Path: packages/components/src/slot-fill/README.md

```diff
@@ -70,7 +70,7 @@ Both `Slot` and `Fill` accept a `name` string prop, where a `Slot` with a given
 
 `Slot` with `bubblesVirtually` set to true also accept an optional `className` to add to the slot container.
 
-`Slot` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
+`Slot` **without** `bubblesVirtually` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
```

If `bubblesVirtually ` is true, `children` does not seem to be supported.

#### torounit on 2023-06-23T03:39:30Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1239288451
Path: packages/components/src/slot-fill/README.md

```diff
@@ -70,7 +70,7 @@ Both `Slot` and `Fill` accept a `name` string prop, where a `Slot` with a given
 
 `Slot` with `bubblesVirtually` set to true also accept an optional `className` to add to the slot container.
 
-`Slot` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
+`Slot` **without** `bubblesVirtually` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
```

@mirka Should we add `children` support?

#### tyxla on 2023-06-30T12:40:20Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247818251
Path: packages/components/src/slot-fill/README.md

```diff
@@ -70,7 +70,7 @@ Both `Slot` and `Fill` accept a `name` string prop, where a `Slot` with a given
 
 `Slot` with `bubblesVirtually` set to true also accept an optional `className` to add to the slot container.
 
-`Slot` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
+`Slot` **without** `bubblesVirtually` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
```

I'd suggest keeping that for another PR since this one is on the large side anyway.

#### torounit on 2023-08-24T15:27:17Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1304505578
Path: packages/components/src/slot-fill/README.md

```diff
@@ -70,7 +70,7 @@ Both `Slot` and `Fill` accept a `name` string prop, where a `Slot` with a given
 
 `Slot` with `bubblesVirtually` set to true also accept an optional `className` to add to the slot container.
 
-`Slot` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
+`Slot` **without** `bubblesVirtually` accepts an optional `children` function prop, which takes `fills` as a param. It allows you to perform additional processing and wrap `fills` conditionally.
```

fixed in #53272

### Review thread 8 (resolved)

#### torounit on 2023-06-23T06:39:12Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1239378390
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -43,11 +50,11 @@ class SlotComponent extends Component {
 		unregisterSlot( this.props.name, this );
 	}
 
-	componentDidUpdate( prevProps ) {
+	componentDidUpdate( prevProps: BaseSlotComponentProps ) {
 		const { name, unregisterSlot, registerSlot } = this.props;
 
 		if ( prevProps.name !== name ) {
-			unregisterSlot( prevProps.name );
+			unregisterSlot( prevProps.name, this );
```

Small bug fix. 

If I could find a way to use something other than `this`, I could change it to a function component.

#### tyxla on 2023-06-30T13:09:05Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247845477
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -43,11 +50,11 @@ class SlotComponent extends Component {
 		unregisterSlot( this.props.name, this );
 	}
 
-	componentDidUpdate( prevProps ) {
+	componentDidUpdate( prevProps: BaseSlotComponentProps ) {
 		const { name, unregisterSlot, registerSlot } = this.props;
 
 		if ( prevProps.name !== name ) {
-			unregisterSlot( prevProps.name );
+			unregisterSlot( prevProps.name, this );
```

This may deserve its own PR TBH 😉

#### ciampo on 2023-09-08T15:00:08Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1319984534
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -43,11 +50,11 @@ class SlotComponent extends Component {
 		unregisterSlot( this.props.name, this );
 	}
 
-	componentDidUpdate( prevProps ) {
+	componentDidUpdate( prevProps: BaseSlotComponentProps ) {
 		const { name, unregisterSlot, registerSlot } = this.props;
 
 		if ( prevProps.name !== name ) {
-			unregisterSlot( prevProps.name );
+			unregisterSlot( prevProps.name, this );
```

Agreed with Marin, we should extract this fix to a separate PR

#### torounit on 2023-09-24T09:31:45Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1335151970
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -43,11 +50,11 @@ class SlotComponent extends Component {
 		unregisterSlot( this.props.name, this );
 	}
 
-	componentDidUpdate( prevProps ) {
+	componentDidUpdate( prevProps: BaseSlotComponentProps ) {
 		const { name, unregisterSlot, registerSlot } = this.props;
 
 		if ( prevProps.name !== name ) {
-			unregisterSlot( prevProps.name );
+			unregisterSlot( prevProps.name, this );
```

@ciampo 

https://github.com/WordPress/gutenberg/pull/54765

Pull Request created.

### Review thread 9 (resolved)

#### tyxla on 2023-06-30T12:38:58Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247817032
Path: packages/components/CHANGELOG.md

```diff
@@ -34,7 +36,6 @@
 -   Wrapped `TabPanel` in a `forwardRef` call ([#50199](https://github.com/WordPress/gutenberg/pull/50199)).
 -   `ColorPalette`: Improve readability of color name and value, and improve rendering of partially transparent colors ([#50450](https://github.com/WordPress/gutenberg/pull/50450)).
 -   `Button`: Add `__next32pxSmallSize` prop to opt into the new 32px size when the `isSmall` prop is enabled ([#51012](https://github.com/WordPress/gutenberg/pull/51012)).
--   `ItemGroup`: Update styles so all SVGs inherit color from their parent element ([#50819](https://github.com/WordPress/gutenberg/pull/50819)).
```

Did we mean to remove this? Bad rebase perhaps?

#### torounit on 2023-07-01T14:37:21Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248862288
Path: packages/components/CHANGELOG.md

```diff
@@ -34,7 +36,6 @@
 -   Wrapped `TabPanel` in a `forwardRef` call ([#50199](https://github.com/WordPress/gutenberg/pull/50199)).
 -   `ColorPalette`: Improve readability of color name and value, and improve rendering of partially transparent colors ([#50450](https://github.com/WordPress/gutenberg/pull/50450)).
 -   `Button`: Add `__next32pxSmallSize` prop to opt into the new 32px size when the `isSmall` prop is enabled ([#51012](https://github.com/WordPress/gutenberg/pull/51012)).
--   `ItemGroup`: Update styles so all SVGs inherit color from their parent element ([#50819](https://github.com/WordPress/gutenberg/pull/50819)).
```

This is a wrong rebase. :sweat_smile:

### Review thread 10 (resolved)

#### tyxla on 2023-06-30T12:41:12Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247819038
Path: packages/components/src/slot-fill/bubbles-virtually/fill.tsx

```diff
@@ -47,17 +48,16 @@ export default function Fill( { name, children } ) {
 		return null;
 	}
 
-	if ( typeof children === 'function' ) {
-		children = children( slot.fillProps );
-	}
+	const newChildren =
+		typeof children === 'function' ? children( slot.fillProps ) : children;
```

IMHO this can directly be inlined down there instead of assigning it to a new constant that's used just once.

### Review thread 11 (resolved)

#### tyxla on 2023-06-30T12:42:44Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247820359
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
```

I'd suggest renaming the type to `SlotFillBubblesVirtuallyContext` or something similar, so it would be clearer that these are 2 different versions of the same thing. Currently, when we prefix `BubblesVirtually` in the beginning, this isn't immediately clear.

### Review thread 12

#### tyxla on 2023-06-30T12:47:26Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247824621
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
+	slots: Map<
+		SlotKey,
+		{
+			ref: MutableRefObject< HTMLElement | undefined >;
+			fillProps: any;
+		}
+	>;
+	fills: Map< SlotKey, MutableRefObject< { rerender: () => void } >[] >;
+	registerSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >,
+		fillProps: any
+	) => void;
+	unregisterSlot: (
```

There are a lot of similarities between the two different context types. Should we try to generalize them a little bit? One way could be with some generic types under the hood.

#### ciampo on 2023-09-08T15:22:04Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320008990
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
+	slots: Map<
+		SlotKey,
+		{
+			ref: MutableRefObject< HTMLElement | undefined >;
+			fillProps: any;
+		}
+	>;
+	fills: Map< SlotKey, MutableRefObject< { rerender: () => void } >[] >;
+	registerSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >,
+		fillProps: any
+	) => void;
+	unregisterSlot: (
```

That would be neat, although not a blocker IMO. Maybe it can be a timeboxed effort?

### Review thread 13 (resolved)

#### tyxla on 2023-06-30T12:54:14Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247830941
Path: packages/components/src/slot-fill/test/__snapshots__/slot.js.snap

```diff
@@ -42,12 +42,16 @@ exports[`Slot bubblesVirtually true should subsume another slot by the same name
   <div
     data-position="first"
   >
-    <div />
+    <div
+      class="emotion-0 emotion-1"
```

Why were these changes to the snapshot necessary?

#### mirka on 2023-06-30T21:55:53Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248309108
Path: packages/components/src/slot-fill/test/__snapshots__/slot.js.snap

```diff
@@ -42,12 +42,16 @@ exports[`Slot bubblesVirtually true should subsume another slot by the same name
   <div
     data-position="first"
   >
-    <div />
+    <div
+      class="emotion-0 emotion-1"
```

I think it's because we changed it from a `div` to a `View`.

#### torounit on 2023-08-24T15:28:17Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1304506881
Path: packages/components/src/slot-fill/test/__snapshots__/slot.js.snap

```diff
@@ -42,12 +42,16 @@ exports[`Slot bubblesVirtually true should subsume another slot by the same name
   <div
     data-position="first"
   >
-    <div />
+    <div
+      class="emotion-0 emotion-1"
```

fixed in #53272

### Review thread 14 (resolved)

#### tyxla on 2023-06-30T12:56:13Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247832848
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
```

I think we should be able to improve `fillProps` a little bit. Everything is better than `any`. The props should be some sort of a record at least, even if it's vague - that will still be better than `any`.

### Review thread 15 (resolved)

#### tyxla on 2023-06-30T13:00:24Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247836717
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
```

Could be an improvement to try with the distributive types patterns and mark `children` as `never` when `bubblesVirtually` is `true`. That pattern is utilized here:

https://github.com/WordPress/gutenberg/blob/fbf1bcd6316235865b456820f63e51026370a4a2/packages/components/src/animate/types.ts#L26

#### torounit on 2023-08-02T16:54:36Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1282185703
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
```

```
export type SlotComponentProps< T extends boolean > = SlotPropBase & {
	bubblesVirtually: T;
	children?: T extends true ? never : ( fills: ReactNode ) => ReactNode;
};
```

I have tried the above code and others, but the type inference does not work. :vomiting_face:

#### tyxla on 2023-08-04T14:46:42Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1284519671
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
```

That's a bummer. It seems to work in some other components - did you spot any difference in how we're using it? Perhaps this is a good opportunity to report a TS bug upstream.

#### torounit on 2023-08-04T15:57:38Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1284594775
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
```

Perhaps the type of <Slot /> changes with or without bubblesVirtually, or the internal <Slot /> does not pass bubblesVirtually, which complicates the type definition.

Rather than a bug in TypeScript, I believe it is due to the complexity of the many places where SlotComponentProps are applied.

#### ciampo on 2023-09-19T13:18:40Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1330111825
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
```

Discussed and solved in https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320548263

### Review thread 16 (resolved)

#### tyxla on 2023-06-30T13:01:10Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247837601
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
+	slots: Map<
+		SlotKey,
+		{
+			ref: MutableRefObject< HTMLElement | undefined >;
+			fillProps: any;
+		}
+	>;
+	fills: Map< SlotKey, MutableRefObject< { rerender: () => void } >[] >;
+	registerSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >,
+		fillProps: any
+	) => void;
+	unregisterSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >
+	) => void;
+	updateSlot: ( name: SlotKey, fillProps: any ) => void;
+	registerFill: (
+		name: SlotKey,
+		ref: MutableRefObject< { rerender: () => void } >
```

`MutableRefObject< { rerender: () => void }` gets repetitive, maybe we can make it its own type.

### Review thread 17 (resolved)

#### tyxla on 2023-06-30T13:01:27Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247837896
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
+	slots: Map<
+		SlotKey,
+		{
+			ref: MutableRefObject< HTMLElement | undefined >;
+			fillProps: any;
+		}
+	>;
+	fills: Map< SlotKey, MutableRefObject< { rerender: () => void } >[] >;
+	registerSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >,
```

`MutableRefObject< HTMLElement | undefined >` gets repetitive, maybe we can make it its own type.

#### mirka on 2023-06-30T15:28:19Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247997977
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
+	slots: Map<
+		SlotKey,
+		{
+			ref: MutableRefObject< HTMLElement | undefined >;
+			fillProps: any;
+		}
+	>;
+	fills: Map< SlotKey, MutableRefObject< { rerender: () => void } >[] >;
+	registerSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >,
```

Are all these `MutableRefObject< HTMLElement | undefined >` ref objects intended to be passed to a `ref` prop on a component? If so, I think it's a little more conventional and safer to type this as `RefObject< HTMLElement >`, which will allow only React (not the consumer) to modify `.current`.

And when creating such a ref, you type it like this and `null` the initial value:

```diff
diff --git a/packages/components/src/slot-fill/bubbles-virtually/slot.tsx b/packages/components/src/slot-fill/bubbles-virtually/slot.tsx
index beac813388..a8cf729ce7 100644
--- a/packages/components/src/slot-fill/bubbles-virtually/slot.tsx
+++ b/packages/components/src/slot-fill/bubbles-virtually/slot.tsx
@@ -39,7 +39,7 @@ function Slot(
 
 	const { registerSlot, unregisterSlot, ...registry } =
 		useContext( SlotFillContext );
-	const ref = useRef();
+	const ref = useRef< HTMLElement >( null );
 
 	useLayoutEffect( () => {
 		registerSlot( name, ref, fillProps );
```

#### torounit on 2023-07-01T15:59:36Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248884248
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
+	 */
+	children?: ReactNode | ( ( fillProps: any ) => ReactNode );
+};
+
+export type SlotFillProviderProps = {
+	/**
+	 * The children elements.
+	 */
+	children: ReactNode;
+};
+
+export type BubblesVirtuallySlotFillContext = {
+	slots: Map<
+		SlotKey,
+		{
+			ref: MutableRefObject< HTMLElement | undefined >;
+			fillProps: any;
+		}
+	>;
+	fills: Map< SlotKey, MutableRefObject< { rerender: () => void } >[] >;
+	registerSlot: (
+		name: SlotKey,
+		ref: MutableRefObject< HTMLElement | undefined >,
```

These can probably be changed to `RefObject`.

### Review thread 18 (resolved)

#### tyxla on 2023-06-30T13:02:36Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247838941
Path: packages/components/src/slot-fill/use-slot.ts

```diff
@@ -8,16 +12,17 @@ import { useContext, useSyncExternalStore } from '@wordpress/element';
  * Internal dependencies
  */
 import SlotFillContext from './context';
+import type { SlotKey } from './types';
 
 /**
  * React hook returning the active slot given a name.
  *
  * @param {string} name Slot name.
- * @return {Object} Slot object.
+ * @return {Component|undefined} Slot object.
  */
-const useSlot = ( name ) => {
+const useSlot = ( name: SlotKey ) => {
 	const { getSlot, subscribe } = useContext( SlotFillContext );
-	return useSyncExternalStore(
+	return useSyncExternalStore< Component | undefined >(
```

It's not immediately clear why `Component | undefined ` is necessary here. Perhaps it would be more explicit if we pass it as another, separately defined type to `getSlot()`?

#### torounit on 2023-07-22T12:02:15Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1271287855
Path: packages/components/src/slot-fill/use-slot.ts

```diff
@@ -8,16 +12,17 @@ import { useContext, useSyncExternalStore } from '@wordpress/element';
  * Internal dependencies
  */
 import SlotFillContext from './context';
+import type { SlotKey } from './types';
 
 /**
  * React hook returning the active slot given a name.
  *
  * @param {string} name Slot name.
- * @return {Object} Slot object.
+ * @return {Component|undefined} Slot object.
  */
-const useSlot = ( name ) => {
+const useSlot = ( name: SlotKey ) => {
 	const { getSlot, subscribe } = useContext( SlotFillContext );
-	return useSyncExternalStore(
+	return useSyncExternalStore< Component | undefined >(
```

This change was unnecessary in the first place because type inference is fully functional in this area. It will be removed.

### Review thread 19 (resolved)

#### tyxla on 2023-06-30T13:05:22Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247841681
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
```

Can we get a bit more specific and actually parameterize the examples that pass data to `fillProps`? For example `age` could be its own control, regardless of the fact that it passes to `fillProps`.

#### mirka on 2023-06-30T21:48:11Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248305949
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
```

I think the current `null` config is fine. This generation of Storybook doesn't really allow arbitrary controls, they need to map 1:1 with the main component's props.

### Review thread 20 (resolved)

#### tyxla on 2023-06-30T13:06:14Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247842476
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
```

Should all those static values be dynamically controlled by fields in the story?

### Review thread 21 (resolved)

#### tyxla on 2023-06-30T13:06:57Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247843202
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
+	bubblesVirtually: true,
+	as: 'span',
+};
+
+export const WithFillProps: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name:{ ' ' }
+				<Slot
+					{ ...props }
+					name="name"
+					fillProps={ { name: 'Grace' } }
+				/>
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" fillProps={ { age: 33 } } />
+			</p>
+
+			<Fill name="name">{ ( fillProps ) => fillProps.name }</Fill>
+			<Fill name="age">{ ( fillProps ) => fillProps.age }</Fill>
+		</SlotFillProvider>
+	);
+};
+WithFillProps.args = {
+	...Default.args,
+};
+
+export const WithSlotChildren: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: { /* @ts-ignore */ }
+				<Slot { ...props } name="name">
+					{ ( fills ) => {
+						return (
+							<span style={ { color: 'red' } }>{ fills }</span>
+						);
+					} }
+				</Slot>
+			</p>
+			<p>
+				Age: { /* @ts-ignore */ }
+				<Slot { ...props } name="age">
+					{ ( fills ) => {
+						return (
+							<span style={ { color: 'red' } }>{ fills }</span>
+						);
+					} }
+				</Slot>
+			</p>
+			<Fill name="name">Alice</Fill>
+			<Fill name="age">18</Fill>
+		</SlotFillProvider>
+	);
+};
+WithSlotChildren.args = {
+	...Default.args,
+};
+
+export const WithContext: ComponentStory< typeof Slot > = ( props ) => {
+	const Context = createContext( null as string | number | null | undefined );
+	const ContextFill = ( { name }: { name: string } ) => {
+		const value = useContext( Context );
+		return <Fill name={ name }>{ value }</Fill>;
+	};
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Context.Provider value="Grace">
+				<ContextFill name="name" />
+			</Context.Provider>
+			<Context.Provider value={ 33 }>
+				<ContextFill name="age" />
+			</Context.Provider>
+		</SlotFillProvider>
+	);
+};
+WithContext.args = {
+	...Default.args,
+};
```

Feel free to disregard feedback on the story. I noticed a bit late that it's pre-existing.

### Review thread 22 (resolved)

#### tyxla on 2023-06-30T13:08:25Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247844695
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -61,34 +68,42 @@ class SlotComponent extends Component {
 
 	render() {
 		const { children, name, fillProps = {}, getFills } = this.props;
-
-		const fills = ( getFills( name, this ) ?? [] )
+		const fills: ReactNode[] = ( getFills( name, this ) ?? [] )
 			.map( ( fill ) => {
 				const fillChildren = isFunction( fill.children )
 					? fill.children( fillProps )
 					: fill.children;
-
 				return Children.map( fillChildren, ( child, childIndex ) => {
 					if ( ! child || typeof child === 'string' ) {
 						return child;
 					}
+					let childKey: string | number = childIndex;
```

Isn't `key` supposed to be of the `React.Key` type?

### Review thread 23 (resolved)

#### tyxla on 2023-06-30T13:10:32Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247847319
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -13,20 +17,23 @@ import {
  * Internal dependencies
  */
 import SlotFillContext from './context';
+import type { BaseSlotComponentProps, SlotComponentProps } from './types';
 
 /**
  * Whether the argument is a function.
  *
  * @param {*} maybeFunc The argument to check.
  * @return {boolean} True if the argument is a function, false otherwise.
  */
-function isFunction( maybeFunc ) {
+function isFunction( maybeFunc: any ): maybeFunc is Function {
 	return typeof maybeFunc === 'function';
 }
 
-class SlotComponent extends Component {
-	constructor() {
-		super( ...arguments );
+class SlotComponent extends Component< BaseSlotComponentProps, {} > {
```

The second `Component` generic argument is optional, no?

#### torounit on 2023-07-03T16:09:02Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1251079044
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -13,20 +17,23 @@ import {
  * Internal dependencies
  */
 import SlotFillContext from './context';
+import type { BaseSlotComponentProps, SlotComponentProps } from './types';
 
 /**
  * Whether the argument is a function.
  *
  * @param {*} maybeFunc The argument to check.
  * @return {boolean} True if the argument is a function, false otherwise.
  */
-function isFunction( maybeFunc ) {
+function isFunction( maybeFunc: any ): maybeFunc is Function {
 	return typeof maybeFunc === 'function';
 }
 
-class SlotComponent extends Component {
-	constructor() {
-		super( ...arguments );
+class SlotComponent extends Component< BaseSlotComponentProps, {} > {
```

`{}` is not necessary.

### Review thread 24 (resolved)

#### tyxla on 2023-06-30T13:11:48Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247848912
Path: packages/components/src/slot-fill/index.tsx

```diff
@@ -27,29 +39,39 @@ export function Fill( props ) {
 		</>
 	);
 }
-export const Slot = forwardRef( ( { bubblesVirtually, ...props }, ref ) => {
+
+export function UnforwardedSlot(
+	props: WordPressComponentProps< SlotComponentProps, 'div' >,
+	ref: ForwardedRef< any >
+) {
+	const { bubblesVirtually, ...restProps } = props;
```

This destructuring could be done during function definition, too.

#### torounit on 2023-07-08T19:32:59Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1257341907
Path: packages/components/src/slot-fill/index.tsx

```diff
@@ -27,29 +39,39 @@ export function Fill( props ) {
 		</>
 	);
 }
-export const Slot = forwardRef( ( { bubblesVirtually, ...props }, ref ) => {
+
+export function UnforwardedSlot(
+	props: WordPressComponentProps< SlotComponentProps, 'div' >,
+	ref: ForwardedRef< any >
+) {
+	const { bubblesVirtually, ...restProps } = props;
```

@tyxla I modified it this way referring to the migration guide, but should I do it in the function definition?

https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#refactoring-a-component-to-typescript:~:text=If%20the%20components%20in%20the%20ignored%20files%20are%20destructuring%20props%20directly%20from%20the%20function%27s%20arguments%2C%20move%20the%20props%20destructuring%20to%20the%20function%27s%20body%20(this%20is%20to%20avoid%20TypeScript%20errors%20from%20trying%20to%20infer%20the%20props%20type)%3A

#### tyxla on 2023-07-10T14:02:00Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1258320204
Path: packages/components/src/slot-fill/index.tsx

```diff
@@ -27,29 +39,39 @@ export function Fill( props ) {
 		</>
 	);
 }
-export const Slot = forwardRef( ( { bubblesVirtually, ...props }, ref ) => {
+
+export function UnforwardedSlot(
+	props: WordPressComponentProps< SlotComponentProps, 'div' >,
+	ref: ForwardedRef< any >
+) {
+	const { bubblesVirtually, ...restProps } = props;
```

My understanding is that this is done only when type inference doesn't work as expected, but given that this is specifically mentioned in the migration guide, feel free to leave it as is 👍

### Review thread 25 (resolved)

#### tyxla on 2023-06-30T13:14:35Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247851709
Path: packages/components/src/slot-fill/bubbles-virtually/use-slot.ts

```diff
@@ -24,10 +24,18 @@ export default function useSlot( name ) {
 
 	const api = useMemo(
 		() => ( {
-			updateSlot: ( fillProps ) => registry.updateSlot( name, fillProps ),
-			unregisterSlot: ( ref ) => registry.unregisterSlot( name, ref ),
-			registerFill: ( ref ) => registry.registerFill( name, ref ),
-			unregisterFill: ( ref ) => registry.unregisterFill( name, ref ),
+			updateSlot: (
+				fillProps: Parameters< typeof registry.updateSlot >[ 1 ]
```

Should this be the same type as the rest of the `fillProps`?

### Review thread 26 (resolved)

#### tyxla on 2023-06-30T13:15:43Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247852911
Path: packages/components/src/slot-fill/bubbles-virtually/use-slot.ts

```diff
@@ -24,10 +24,18 @@ export default function useSlot( name ) {
 
 	const api = useMemo(
 		() => ( {
-			updateSlot: ( fillProps ) => registry.updateSlot( name, fillProps ),
-			unregisterSlot: ( ref ) => registry.unregisterSlot( name, ref ),
-			registerFill: ( ref ) => registry.registerFill( name, ref ),
-			unregisterFill: ( ref ) => registry.unregisterFill( name, ref ),
+			updateSlot: (
+				fillProps: Parameters< typeof registry.updateSlot >[ 1 ]
+			) => registry.updateSlot( name, fillProps ),
+			unregisterSlot: (
+				ref: Parameters< typeof registry.unregisterSlot >[ 1 ]
```

I wonder if we can be specific here rather than being so verbose. Or at least infer the types in a more general way that doesn't require us to specify them for each of the properties in this object.

### Review thread 27 (resolved)

#### tyxla on 2023-06-30T13:17:02Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247854177
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -13,12 +17,26 @@ import { useMergeRefs } from '@wordpress/compose';
 /**
  * Internal dependencies
  */
+import { View } from '../../view';
 import SlotFillContext from './slot-fill-context';
+import type { WordPressComponentProps } from '../../ui/context';
+import type { SlotComponentProps } from '../types';
 
 function Slot(
-	{ name, fillProps = {}, as: Component = 'div', ...props },
-	forwardedRef
+	props: WordPressComponentProps<
+		Omit< SlotComponentProps, 'bubblesVirtually' >,
+		'div'
+	>,
+	forwardedRef: ForwardedRef< any >
 ) {
+	const { name, fillProps = {}, as: Component = 'div', ...restProps } = props;
+
+	// `children` is not allowed. However, if it is passed,
+	// it will be displayed as is, so remove `children`.
+	if ( 'children' in restProps ) {
+		delete restProps.children;
+	}
```

This mutation isn't great. Can we figure out some other way to do it? Using another object that omits children for example.

#### mirka on 2023-06-30T20:46:06Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248272441
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -13,12 +17,26 @@ import { useMergeRefs } from '@wordpress/compose';
 /**
  * Internal dependencies
  */
+import { View } from '../../view';
 import SlotFillContext from './slot-fill-context';
+import type { WordPressComponentProps } from '../../ui/context';
+import type { SlotComponentProps } from '../types';
 
 function Slot(
-	{ name, fillProps = {}, as: Component = 'div', ...props },
-	forwardedRef
+	props: WordPressComponentProps<
+		Omit< SlotComponentProps, 'bubblesVirtually' >,
+		'div'
+	>,
+	forwardedRef: ForwardedRef< any >
 ) {
+	const { name, fillProps = {}, as: Component = 'div', ...restProps } = props;
+
+	// `children` is not allowed. However, if it is passed,
+	// it will be displayed as is, so remove `children`.
+	if ( 'children' in restProps ) {
+		delete restProps.children;
+	}
```

Better yet, maybe this kind of runtime change should be done in a separate PR?

#### torounit on 2023-07-05T14:56:24Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1253239605
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -13,12 +17,26 @@ import { useMergeRefs } from '@wordpress/compose';
 /**
  * Internal dependencies
  */
+import { View } from '../../view';
 import SlotFillContext from './slot-fill-context';
+import type { WordPressComponentProps } from '../../ui/context';
+import type { SlotComponentProps } from '../types';
 
 function Slot(
-	{ name, fillProps = {}, as: Component = 'div', ...props },
-	forwardedRef
+	props: WordPressComponentProps<
+		Omit< SlotComponentProps, 'bubblesVirtually' >,
+		'div'
+	>,
+	forwardedRef: ForwardedRef< any >
 ) {
+	const { name, fillProps = {}, as: Component = 'div', ...restProps } = props;
+
+	// `children` is not allowed. However, if it is passed,
+	// it will be displayed as is, so remove `children`.
+	if ( 'children' in restProps ) {
+		delete restProps.children;
+	}
```

Once this PR is organized, create a separate PR for this section only.

#### torounit on 2023-09-06T16:00:18Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1317512669
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -13,12 +17,26 @@ import { useMergeRefs } from '@wordpress/compose';
 /**
  * Internal dependencies
  */
+import { View } from '../../view';
 import SlotFillContext from './slot-fill-context';
+import type { WordPressComponentProps } from '../../ui/context';
+import type { SlotComponentProps } from '../types';
 
 function Slot(
-	{ name, fillProps = {}, as: Component = 'div', ...props },
-	forwardedRef
+	props: WordPressComponentProps<
+		Omit< SlotComponentProps, 'bubblesVirtually' >,
+		'div'
+	>,
+	forwardedRef: ForwardedRef< any >
 ) {
+	const { name, fillProps = {}, as: Component = 'div', ...restProps } = props;
+
+	// `children` is not allowed. However, if it is passed,
+	// it will be displayed as is, so remove `children`.
+	if ( 'children' in restProps ) {
+		delete restProps.children;
+	}
```

fixed: https://github.com/WordPress/gutenberg/pull/53272.

### Review thread 28 (resolved)

#### tyxla on 2023-06-30T13:17:58Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247855129
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	BubblesVirtuallySlotFillContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): BubblesVirtuallySlotFillContext {
+	const slots: BubblesVirtuallySlotFillContext[ 'slots' ] = proxyMap();
+	const fills: BubblesVirtuallySlotFillContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: BubblesVirtuallySlotFillContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = ( slots.get( name ) || {} ) as {
+			ref: typeof ref;
+			fillProps: typeof fillProps;
+		};
```

I wonder if can help TS to better infer those types.

#### mirka on 2023-06-30T19:14:27Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248210069
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	BubblesVirtuallySlotFillContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): BubblesVirtuallySlotFillContext {
+	const slots: BubblesVirtuallySlotFillContext[ 'slots' ] = proxyMap();
+	const fills: BubblesVirtuallySlotFillContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: BubblesVirtuallySlotFillContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = ( slots.get( name ) || {} ) as {
+			ref: typeof ref;
+			fillProps: typeof fillProps;
+		};
```

We could make our own utility type, or export a more granular alias from the types file, but may be overkill 🤷

```ts
		type MapValueType< T > = T extends Map< any, infer V > ? V : never;

		const slot:
			| MapValueType< BubblesVirtuallySlotFillContext[ 'slots' ] >
			| {} = slots.get( name ) || {};
```

#### torounit on 2023-07-15T11:18:22Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1264387481
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	BubblesVirtuallySlotFillContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): BubblesVirtuallySlotFillContext {
+	const slots: BubblesVirtuallySlotFillContext[ 'slots' ] = proxyMap();
+	const fills: BubblesVirtuallySlotFillContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: BubblesVirtuallySlotFillContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = ( slots.get( name ) || {} ) as {
+			ref: typeof ref;
+			fillProps: typeof fillProps;
+		};
```

How about something like this.

```tsx
		const slot = slots.get( name ) || {
			ref: undefined,
			fillProps: undefined,
		};
```

### Review thread 29 (resolved)

#### mirka on 2023-06-30T14:33:00Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247939046
Path: packages/components/src/slot-fill/README.md

```diff
@@ -103,14 +103,14 @@ const ToolbarItem = () => (
 	</Fill>
 );
 
-const Toolbar = () => 
-	const hideToolbar() => {
+const Toolbar = () => {
+	const hideToolbar = () => {
 		console.log( 'Hide toolbar' );
 	}
```

```suggestion
	};
```

### Review thread 30 (resolved)

#### mirka on 2023-06-30T14:34:19Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1247940581
Path: packages/components/src/slot-fill/README.md

```diff
@@ -103,14 +103,14 @@ const ToolbarItem = () => (
 	</Fill>
 );
 
-const Toolbar = () => 
-	const hideToolbar() => {
+const Toolbar = () => {
+	const hideToolbar = () => {
 		console.log( 'Hide toolbar' );
 	}
 	return (
 		<div className="toolbar">
-			<Slot fillProps={ { hideToolbar } } />
+			<Slot fillProps={ { hideToolbar } }/>
```

```suggestion
			<Slot fillProps={ { hideToolbar } } />
```

### Review thread 31 (resolved)

#### mirka on 2023-06-30T19:15:23Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248210666
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	BubblesVirtuallySlotFillContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): BubblesVirtuallySlotFillContext {
+	const slots: BubblesVirtuallySlotFillContext[ 'slots' ] = proxyMap();
+	const fills: BubblesVirtuallySlotFillContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: BubblesVirtuallySlotFillContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = ( slots.get( name ) || {} ) as {
```

The `as` cast is covering up a type safety issue (e.g. the `slot.ref` on L41 could be undefined). Was this intentional, to avoid runtime changes? I'm fine with that but if so we should perhaps add a TODO comment to warn future devs.

#### torounit on 2023-07-22T12:06:54Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1271288307
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	BubblesVirtuallySlotFillContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): BubblesVirtuallySlotFillContext {
+	const slots: BubblesVirtuallySlotFillContext[ 'slots' ] = proxyMap();
+	const fills: BubblesVirtuallySlotFillContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: BubblesVirtuallySlotFillContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = ( slots.get( name ) || {} ) as {
```

If I replace `|| {}` with `|| { ref: undefined, fillProps: undefined}`, the `as` is not needed and the TypeScript error disappears. I don't like to use `as` too much, so I will change to this method.

### Review thread 32 (resolved)

#### mirka on 2023-06-30T20:43:59Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248271025
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
```

```suggestion
```

### Review thread 33 (resolved)

#### mirka on 2023-06-30T21:26:29Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248296200
Path: packages/components/src/slot-fill/provider.tsx

```diff
@@ -0,0 +1,129 @@
+/**
+ * WordPress dependencies
+ */
+import type { Component } from '@wordpress/element';
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import SlotFillContext from './context';
+import type {
+	FillComponentProps,
+	BaseSlotFillContext,
+	BaseSlotComponentProps,
+	SlotFillProviderProps,
+	SlotKey,
+} from './types';
+
+function createSlotRegistry(): BaseSlotFillContext {
+	const slots: Record< SlotKey, Component< BaseSlotComponentProps > > = {};
+	const fills: Record< SlotKey, FillComponentProps[] > = {};
+	let listeners: Array< () => void > = [];
+
+	function registerSlot(
+		name: SlotKey,
+		slot: Component< BaseSlotComponentProps >
+	) {
+		const previousSlot = slots[ name ];
+		slots[ name ] = slot;
+		triggerListeners();
+
+		// Sometimes the fills are registered after the initial render of slot
+		// But before the registerSlot call, we need to rerender the slot.
+		forceUpdateSlot( name );
+
+		// If a new instance of a slot is being mounted while another with the
+		// same name exists, force its update _after_ the new slot has been
+		// assigned into the instance, such that its own rendering of children
+		// will be empty (the new Slot will subsume all fills for this name).
+		if ( previousSlot ) {
+			previousSlot.forceUpdate();
+		}
+	}
+
+	function registerFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] = [ ...( fills[ name ] || [] ), instance ];
+		forceUpdateSlot( name );
+	}
+
+	function unregisterSlot(
+		name: SlotKey,
+		instance: Component< BaseSlotComponentProps >
+	) {
+		// If a previous instance of a Slot by this name unmounts, do nothing,
+		// as the slot and its fills should only be removed for the current
+		// known instance.
+		if ( slots[ name ] !== instance ) {
+			return;
+		}
+
+		delete slots[ name ];
+		triggerListeners();
+	}
+
+	function unregisterFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] =
+			fills[ name ]?.filter( ( fill ) => fill !== instance ) ?? [];
+		forceUpdateSlot( name );
+	}
+
+	function getSlot(
+		name: SlotKey
+	): Component< BaseSlotComponentProps > | undefined {
+		return slots[ name ];
+	}
+
+	function getFills(
+		name: SlotKey,
+		slotInstance: Component< BaseSlotComponentProps >
+	): FillComponentProps[] {
+		// Fills should only be returned for the current instance of the slot
+		// in which they occupy.
+		if ( slots[ name ] !== slotInstance ) {
+			return [];
+		}
+		return fills[ name ];
+	}
+
+	function forceUpdateSlot( name: SlotKey ) {
+		const slot = getSlot( name );
+
+		if ( slot ) {
+			slot.forceUpdate();
+		}
+	}
+
+	function triggerListeners() {
+		listeners.forEach( ( listener ) => listener() );
+	}
+
+	function subscribe( listener: () => void ) {
+		listeners.push( listener );
+
+		return () => {
+			listeners = listeners.filter( ( l ) => l !== listener );
+		};
+	}
+
+	return {
+		registerSlot,
+		unregisterSlot,
+		registerFill,
+		unregisterFill,
+		getSlot,
+		getFills,
+		subscribe,
+	};
+}
+
+export function SlotFillProvider( { children }: SlotFillProviderProps ) {
+	const [ contextValue ] = useState( createSlotRegistry );
```

This is an interesting way to use `useState`, I've actually never seen this pattern before! But I feel like doing a `useMemo` is more idiomatic and captures our intent better. What do you think? Are you aware of any reasons for preferring a `useState` here?

```suggestion
	const contextValue = useMemo( createSlotRegistry, [] );
```

#### torounit on 2023-07-03T15:52:12Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1251060880
Path: packages/components/src/slot-fill/provider.tsx

```diff
@@ -0,0 +1,129 @@
+/**
+ * WordPress dependencies
+ */
+import type { Component } from '@wordpress/element';
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import SlotFillContext from './context';
+import type {
+	FillComponentProps,
+	BaseSlotFillContext,
+	BaseSlotComponentProps,
+	SlotFillProviderProps,
+	SlotKey,
+} from './types';
+
+function createSlotRegistry(): BaseSlotFillContext {
+	const slots: Record< SlotKey, Component< BaseSlotComponentProps > > = {};
+	const fills: Record< SlotKey, FillComponentProps[] > = {};
+	let listeners: Array< () => void > = [];
+
+	function registerSlot(
+		name: SlotKey,
+		slot: Component< BaseSlotComponentProps >
+	) {
+		const previousSlot = slots[ name ];
+		slots[ name ] = slot;
+		triggerListeners();
+
+		// Sometimes the fills are registered after the initial render of slot
+		// But before the registerSlot call, we need to rerender the slot.
+		forceUpdateSlot( name );
+
+		// If a new instance of a slot is being mounted while another with the
+		// same name exists, force its update _after_ the new slot has been
+		// assigned into the instance, such that its own rendering of children
+		// will be empty (the new Slot will subsume all fills for this name).
+		if ( previousSlot ) {
+			previousSlot.forceUpdate();
+		}
+	}
+
+	function registerFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] = [ ...( fills[ name ] || [] ), instance ];
+		forceUpdateSlot( name );
+	}
+
+	function unregisterSlot(
+		name: SlotKey,
+		instance: Component< BaseSlotComponentProps >
+	) {
+		// If a previous instance of a Slot by this name unmounts, do nothing,
+		// as the slot and its fills should only be removed for the current
+		// known instance.
+		if ( slots[ name ] !== instance ) {
+			return;
+		}
+
+		delete slots[ name ];
+		triggerListeners();
+	}
+
+	function unregisterFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] =
+			fills[ name ]?.filter( ( fill ) => fill !== instance ) ?? [];
+		forceUpdateSlot( name );
+	}
+
+	function getSlot(
+		name: SlotKey
+	): Component< BaseSlotComponentProps > | undefined {
+		return slots[ name ];
+	}
+
+	function getFills(
+		name: SlotKey,
+		slotInstance: Component< BaseSlotComponentProps >
+	): FillComponentProps[] {
+		// Fills should only be returned for the current instance of the slot
+		// in which they occupy.
+		if ( slots[ name ] !== slotInstance ) {
+			return [];
+		}
+		return fills[ name ];
+	}
+
+	function forceUpdateSlot( name: SlotKey ) {
+		const slot = getSlot( name );
+
+		if ( slot ) {
+			slot.forceUpdate();
+		}
+	}
+
+	function triggerListeners() {
+		listeners.forEach( ( listener ) => listener() );
+	}
+
+	function subscribe( listener: () => void ) {
+		listeners.push( listener );
+
+		return () => {
+			listeners = listeners.filter( ( l ) => l !== listener );
+		};
+	}
+
+	return {
+		registerSlot,
+		unregisterSlot,
+		registerFill,
+		unregisterFill,
+		getSlot,
+		getFills,
+		subscribe,
+	};
+}
+
+export function SlotFillProvider( { children }: SlotFillProviderProps ) {
+	const [ contextValue ] = useState( createSlotRegistry );
```

https://github.com/WordPress/gutenberg/pull/51350/files/ef5a64d427953f06b7c2a5e92ea92415a647c9d1#diff-e0f964e993ca91fda60b836c0e5e6286b5af387e8f4d632d20a366b28a8da133L89

This is the same implementation as this line. I too think it would be easier to understand using `useMemo`.

#### ciampo on 2023-09-08T14:54:35Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1319977930
Path: packages/components/src/slot-fill/provider.tsx

```diff
@@ -0,0 +1,129 @@
+/**
+ * WordPress dependencies
+ */
+import type { Component } from '@wordpress/element';
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import SlotFillContext from './context';
+import type {
+	FillComponentProps,
+	BaseSlotFillContext,
+	BaseSlotComponentProps,
+	SlotFillProviderProps,
+	SlotKey,
+} from './types';
+
+function createSlotRegistry(): BaseSlotFillContext {
+	const slots: Record< SlotKey, Component< BaseSlotComponentProps > > = {};
+	const fills: Record< SlotKey, FillComponentProps[] > = {};
+	let listeners: Array< () => void > = [];
+
+	function registerSlot(
+		name: SlotKey,
+		slot: Component< BaseSlotComponentProps >
+	) {
+		const previousSlot = slots[ name ];
+		slots[ name ] = slot;
+		triggerListeners();
+
+		// Sometimes the fills are registered after the initial render of slot
+		// But before the registerSlot call, we need to rerender the slot.
+		forceUpdateSlot( name );
+
+		// If a new instance of a slot is being mounted while another with the
+		// same name exists, force its update _after_ the new slot has been
+		// assigned into the instance, such that its own rendering of children
+		// will be empty (the new Slot will subsume all fills for this name).
+		if ( previousSlot ) {
+			previousSlot.forceUpdate();
+		}
+	}
+
+	function registerFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] = [ ...( fills[ name ] || [] ), instance ];
+		forceUpdateSlot( name );
+	}
+
+	function unregisterSlot(
+		name: SlotKey,
+		instance: Component< BaseSlotComponentProps >
+	) {
+		// If a previous instance of a Slot by this name unmounts, do nothing,
+		// as the slot and its fills should only be removed for the current
+		// known instance.
+		if ( slots[ name ] !== instance ) {
+			return;
+		}
+
+		delete slots[ name ];
+		triggerListeners();
+	}
+
+	function unregisterFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] =
+			fills[ name ]?.filter( ( fill ) => fill !== instance ) ?? [];
+		forceUpdateSlot( name );
+	}
+
+	function getSlot(
+		name: SlotKey
+	): Component< BaseSlotComponentProps > | undefined {
+		return slots[ name ];
+	}
+
+	function getFills(
+		name: SlotKey,
+		slotInstance: Component< BaseSlotComponentProps >
+	): FillComponentProps[] {
+		// Fills should only be returned for the current instance of the slot
+		// in which they occupy.
+		if ( slots[ name ] !== slotInstance ) {
+			return [];
+		}
+		return fills[ name ];
+	}
+
+	function forceUpdateSlot( name: SlotKey ) {
+		const slot = getSlot( name );
+
+		if ( slot ) {
+			slot.forceUpdate();
+		}
+	}
+
+	function triggerListeners() {
+		listeners.forEach( ( listener ) => listener() );
+	}
+
+	function subscribe( listener: () => void ) {
+		listeners.push( listener );
+
+		return () => {
+			listeners = listeners.filter( ( l ) => l !== listener );
+		};
+	}
+
+	return {
+		registerSlot,
+		unregisterSlot,
+		registerFill,
+		unregisterFill,
+		getSlot,
+		getFills,
+		subscribe,
+	};
+}
+
+export function SlotFillProvider( { children }: SlotFillProviderProps ) {
+	const [ contextValue ] = useState( createSlotRegistry );
```

Agreed with both, I think we can switch to a `useMemo`  here

#### torounit on 2023-09-09T00:53:42Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320445225
Path: packages/components/src/slot-fill/provider.tsx

```diff
@@ -0,0 +1,129 @@
+/**
+ * WordPress dependencies
+ */
+import type { Component } from '@wordpress/element';
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import SlotFillContext from './context';
+import type {
+	FillComponentProps,
+	BaseSlotFillContext,
+	BaseSlotComponentProps,
+	SlotFillProviderProps,
+	SlotKey,
+} from './types';
+
+function createSlotRegistry(): BaseSlotFillContext {
+	const slots: Record< SlotKey, Component< BaseSlotComponentProps > > = {};
+	const fills: Record< SlotKey, FillComponentProps[] > = {};
+	let listeners: Array< () => void > = [];
+
+	function registerSlot(
+		name: SlotKey,
+		slot: Component< BaseSlotComponentProps >
+	) {
+		const previousSlot = slots[ name ];
+		slots[ name ] = slot;
+		triggerListeners();
+
+		// Sometimes the fills are registered after the initial render of slot
+		// But before the registerSlot call, we need to rerender the slot.
+		forceUpdateSlot( name );
+
+		// If a new instance of a slot is being mounted while another with the
+		// same name exists, force its update _after_ the new slot has been
+		// assigned into the instance, such that its own rendering of children
+		// will be empty (the new Slot will subsume all fills for this name).
+		if ( previousSlot ) {
+			previousSlot.forceUpdate();
+		}
+	}
+
+	function registerFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] = [ ...( fills[ name ] || [] ), instance ];
+		forceUpdateSlot( name );
+	}
+
+	function unregisterSlot(
+		name: SlotKey,
+		instance: Component< BaseSlotComponentProps >
+	) {
+		// If a previous instance of a Slot by this name unmounts, do nothing,
+		// as the slot and its fills should only be removed for the current
+		// known instance.
+		if ( slots[ name ] !== instance ) {
+			return;
+		}
+
+		delete slots[ name ];
+		triggerListeners();
+	}
+
+	function unregisterFill( name: SlotKey, instance: FillComponentProps ) {
+		fills[ name ] =
+			fills[ name ]?.filter( ( fill ) => fill !== instance ) ?? [];
+		forceUpdateSlot( name );
+	}
+
+	function getSlot(
+		name: SlotKey
+	): Component< BaseSlotComponentProps > | undefined {
+		return slots[ name ];
+	}
+
+	function getFills(
+		name: SlotKey,
+		slotInstance: Component< BaseSlotComponentProps >
+	): FillComponentProps[] {
+		// Fills should only be returned for the current instance of the slot
+		// in which they occupy.
+		if ( slots[ name ] !== slotInstance ) {
+			return [];
+		}
+		return fills[ name ];
+	}
+
+	function forceUpdateSlot( name: SlotKey ) {
+		const slot = getSlot( name );
+
+		if ( slot ) {
+			slot.forceUpdate();
+		}
+	}
+
+	function triggerListeners() {
+		listeners.forEach( ( listener ) => listener() );
+	}
+
+	function subscribe( listener: () => void ) {
+		listeners.push( listener );
+
+		return () => {
+			listeners = listeners.filter( ( l ) => l !== listener );
+		};
+	}
+
+	return {
+		registerSlot,
+		unregisterSlot,
+		registerFill,
+		unregisterFill,
+		getSlot,
+		getFills,
+		subscribe,
+	};
+}
+
+export function SlotFillProvider( { children }: SlotFillProviderProps ) {
+	const [ contextValue ] = useState( createSlotRegistry );
```

Switched to useMemo.

### Review thread 34 (resolved)

#### mirka on 2023-06-30T21:31:58Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248298852
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -13,20 +17,23 @@ import {
  * Internal dependencies
  */
 import SlotFillContext from './context';
+import type { BaseSlotComponentProps, SlotComponentProps } from './types';
 
 /**
  * Whether the argument is a function.
  *
  * @param {*} maybeFunc The argument to check.
  * @return {boolean} True if the argument is a function, false otherwise.
```

Let's remove the types in the JSDoc since they're redundant.

```suggestion
 * @param maybeFunc The argument to check.
 * @return True if the argument is a function, false otherwise.
```

### Review thread 35 (resolved)

#### mirka on 2023-06-30T21:38:18Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248301873
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -61,34 +68,42 @@ class SlotComponent extends Component {
 
 	render() {
 		const { children, name, fillProps = {}, getFills } = this.props;
-
-		const fills = ( getFills( name, this ) ?? [] )
+		const fills: ReactNode[] = ( getFills( name, this ) ?? [] )
 			.map( ( fill ) => {
 				const fillChildren = isFunction( fill.children )
 					? fill.children( fillProps )
 					: fill.children;
-
 				return Children.map( fillChildren, ( child, childIndex ) => {
 					if ( ! child || typeof child === 'string' ) {
 						return child;
 					}
+					let childKey: string | number = childIndex;
+					if (
+						typeof child === 'object' &&
+						'key' in child &&
+						child?.key
+					) {
+						childKey = child.key;
+					}
 
-					const childKey = child.key || childIndex;
-					return cloneElement( child, { key: childKey } );
+					return cloneElement( child as ReactElement, {
+						key: childKey,
+					} );
 				} );
 			} )
 			.filter(
 				// In some cases fills are rendered only when some conditions apply.
 				// This ensures that we only use non-empty fills when rendering, i.e.,
 				// it allows us to render wrappers only when the fills are actually present.
-				( element ) => ! isEmptyElement( element )
+				( element ): element is Exclude< typeof element, undefined > =>
+					! isEmptyElement( element )
```

Are these explicit types necessary?

#### torounit on 2023-07-08T19:46:33Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1257343191
Path: packages/components/src/slot-fill/slot.tsx

```diff
@@ -61,34 +68,42 @@ class SlotComponent extends Component {
 
 	render() {
 		const { children, name, fillProps = {}, getFills } = this.props;
-
-		const fills = ( getFills( name, this ) ?? [] )
+		const fills: ReactNode[] = ( getFills( name, this ) ?? [] )
 			.map( ( fill ) => {
 				const fillChildren = isFunction( fill.children )
 					? fill.children( fillProps )
 					: fill.children;
-
 				return Children.map( fillChildren, ( child, childIndex ) => {
 					if ( ! child || typeof child === 'string' ) {
 						return child;
 					}
+					let childKey: string | number = childIndex;
+					if (
+						typeof child === 'object' &&
+						'key' in child &&
+						child?.key
+					) {
+						childKey = child.key;
+					}
 
-					const childKey = child.key || childIndex;
-					return cloneElement( child, { key: childKey } );
+					return cloneElement( child as ReactElement, {
+						key: childKey,
+					} );
 				} );
 			} )
 			.filter(
 				// In some cases fills are rendered only when some conditions apply.
 				// This ensures that we only use non-empty fills when rendering, i.e.,
 				// it allows us to render wrappers only when the fills are actually present.
-				( element ) => ! isEmptyElement( element )
+				( element ): element is Exclude< typeof element, undefined > =>
+					! isEmptyElement( element )
```

This was previously required, but after reviewing the molds, it appears this is no longer necessary.

### Review thread 36 (resolved)

#### mirka on 2023-06-30T21:49:49Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248306633
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
```

Do you think it's useful to have a `className` in the example? We usually don't include this in other component stories.

#### torounit on 2023-07-08T20:10:55Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1257346728
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
```

https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/slot-fill/README.md#props

Normally, I believe that className is unnecessary, but in this case, whether or not className is accepted depends on bubblesVirtually, so I thought it would be better to make it visible.

#### ciampo on 2023-09-19T13:26:19Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1330125891
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
```

Just wanted to flag that at the moment I don't see this information reflected in the `types` — TypeScript is allowing me to pass a `className` even when `bubblesVirtually` is `false`

#### torounit on 2023-09-20T16:51:55Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1331930512
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
```

@ciampo 
Enabled className only if bubblesVirtually:true.
https://github.com/WordPress/gutenberg/pull/51350/files#diff-7102b9f3a6fad3b8e9a616948e5c757e6982548b299628fa8ab170aee7399302R37-R47

### Review thread 37 (resolved)

#### mirka on 2023-06-30T21:55:09Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248308866
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,138 @@
+/**
+ * External dependencies
+ */
+import type { ComponentMeta, ComponentStory } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: ComponentMeta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
+	bubblesVirtually: true,
+	as: 'span',
+};
+
+export const WithFillProps: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name:{ ' ' }
+				<Slot
+					{ ...props }
+					name="name"
+					fillProps={ { name: 'Grace' } }
+				/>
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" fillProps={ { age: 33 } } />
+			</p>
+
+			<Fill name="name">{ ( fillProps ) => fillProps.name }</Fill>
+			<Fill name="age">{ ( fillProps ) => fillProps.age }</Fill>
+		</SlotFillProvider>
+	);
+};
+WithFillProps.args = {
+	...Default.args,
+};
+
+export const WithSlotChildren: ComponentStory< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: { /* @ts-ignore */ }
+				<Slot { ...props } name="name">
+					{ ( fills ) => {
+						return (
+							<span style={ { color: 'red' } }>{ fills }</span>
+						);
+					} }
+				</Slot>
+			</p>
+			<p>
+				Age: { /* @ts-ignore */ }
+				<Slot { ...props } name="age">
+					{ ( fills ) => {
+						return (
+							<span style={ { color: 'red' } }>{ fills }</span>
+						);
+					} }
+				</Slot>
+			</p>
+			<Fill name="name">Alice</Fill>
+			<Fill name="age">18</Fill>
+		</SlotFillProvider>
+	);
+};
+WithSlotChildren.args = {
+	...Default.args,
+};
+
+export const WithContext: ComponentStory< typeof Slot > = ( props ) => {
+	const Context = createContext( null as string | number | null | undefined );
```

Maybe cleaner?

```suggestion
	const Context = createContext< string | number >( '' );
```

### Review thread 38 (resolved)

#### mirka on 2023-06-30T21:57:20Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248309727
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
```

Typo?

#### torounit on 2023-07-03T14:27:36Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1250966201
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * Children elements or.
```

:sweat_smile:

### Review thread 39 (resolved)

#### mirka on 2023-06-30T21:58:15Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248310100
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode } from 'react';
+
+export type SlotKey = string | symbol;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: any;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
+	  } )
+	| ( SlotPropBase & {
+			bubblesVirtually?: false;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 *
+			 * @param fills
+			 */
+			children?: ( fills: ReactNode ) => ReactNode;
+	  } );
+
+export type FillComponentProps = {
+	/**
+	 * Slot name.
```

Maybe a bit more helpful?

```suggestion
	 * The name of the slot to fill into.
```

### Review thread 40 (resolved)

#### mirka on 2023-06-30T22:00:56Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1248311112
Path: packages/components/src/slot-fill/use-slot.ts

```diff
@@ -8,16 +12,17 @@ import { useContext, useSyncExternalStore } from '@wordpress/element';
  * Internal dependencies
  */
 import SlotFillContext from './context';
+import type { SlotKey } from './types';
 
 /**
  * React hook returning the active slot given a name.
  *
  * @param {string} name Slot name.
- * @return {Object} Slot object.
+ * @return {Component|undefined} Slot object.
```

We can remove the redundant types in the JSDoc here too.

```
 * @param name Slot name.
 * @return Slot object.
 ```

### Review thread 41

#### torounit on 2023-09-07T23:59:37Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1319219252
Path: packages/components/src/slot-fill/index.tsx

```diff
@@ -28,33 +40,43 @@ export function Fill( props ) {
 		</>
 	);
 }
-export const Slot = forwardRef( ( { bubblesVirtually, ...props }, ref ) => {
+
+export function UnforwardedSlot(
+	props: WordPressComponentProps< SlotComponentProps, 'div' >,
+	ref: ForwardedRef< any >
+) {
+	const { bubblesVirtually, ...restProps } = props;
 	if ( bubblesVirtually ) {
-		return <BubblesVirtuallySlot { ...props } ref={ ref } />;
+		return <BubblesVirtuallySlot { ...restProps } ref={ ref } />;
 	}
-	return <BaseSlot { ...props } />;
-} );
+	return <BaseSlot { ...restProps } />;
+}
+export const Slot = forwardRef( UnforwardedSlot );
 
-export function Provider( { children, ...props } ) {
+export function Provider( { children }: SlotFillProviderProps ) {
 	const parent = useContext( SlotFillContext );
 	if ( ! parent.isDefault ) {
-		return children;
+		return <>{ children }</>;
```

fix: `TS2786: Provider  cannot be used as a JSX component.`

### Review thread 42 (resolved)

#### tyxla on 2023-09-08T11:33:41Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1319747315
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-context.ts

```diff
@@ -8,8 +8,12 @@ import { proxyMap } from 'valtio/utils';
  */
 import { createContext } from '@wordpress/element';
 import warning from '@wordpress/warning';
+/**
+ * Internal dependencies
+ */
+import type { SlotFillBubblesVirtuallyContext } from '../types';
 
-const SlotFillContext = createContext( {
+const initialContextValue: SlotFillBubblesVirtuallyContext = {
```

After typing the file and renaming it to `.ts`, did you also mean to remove the `@ts-nocheck` comment?

#### ciampo on 2023-09-08T14:22:38Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1319939164
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-context.ts

```diff
@@ -8,8 +8,12 @@ import { proxyMap } from 'valtio/utils';
  */
 import { createContext } from '@wordpress/element';
 import warning from '@wordpress/warning';
+/**
+ * Internal dependencies
+ */
+import type { SlotFillBubblesVirtuallyContext } from '../types';
 
-const SlotFillContext = createContext( {
+const initialContextValue: SlotFillBubblesVirtuallyContext = {
```

Yup, `// @ts-nocheck` should be removed

#### torounit on 2023-09-08T17:56:10Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320179144
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-context.ts

```diff
@@ -8,8 +8,12 @@ import { proxyMap } from 'valtio/utils';
  */
 import { createContext } from '@wordpress/element';
 import warning from '@wordpress/warning';
+/**
+ * Internal dependencies
+ */
+import type { SlotFillBubblesVirtuallyContext } from '../types';
 
-const SlotFillContext = createContext( {
+const initialContextValue: SlotFillBubblesVirtuallyContext = {
```

I thought I turned it off, but it leaked. :sweat_smile:

### Review thread 43 (resolved)

#### ciampo on 2023-09-08T14:33:36Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1319953052
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	SlotFillBubblesVirtuallyContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
+	const slots: SlotFillBubblesVirtuallyContext[ 'slots' ] = proxyMap();
+	const fills: SlotFillBubblesVirtuallyContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: SlotFillBubblesVirtuallyContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = slots.get( name ) || {
+			ref: undefined,
```

If I'm not mistaken, `ref` is supposed to be of type `RefObject< HTMLElement >`. How is it possible to assign a value of `undefined` to it, without causing errors? I would expect `slots.set` to complain because we're possibly creating an object that is different from the expected one.

#### torounit on 2023-09-20T17:09:32Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1331948834
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	SlotFillBubblesVirtuallyContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
+	const slots: SlotFillBubblesVirtuallyContext[ 'slots' ] = proxyMap();
+	const fills: SlotFillBubblesVirtuallyContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: SlotFillBubblesVirtuallyContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = slots.get( name ) || {
+			ref: undefined,
```

slot.get ( name ) normally returns ` slot: {ref: RefObject< HTMLElement >, fillProps: FillProps} `.

However, the definition of the Map type is as follows.

```
interface Map<K, V> {
    get(key: K): V | undefined; }
}
```

These lines are written to deal with ``slot.ref`` and ``slot.fillProps``, which would result in type errors.

#### ciampo on 2023-09-21T12:56:16Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1333009428
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	SlotFillBubblesVirtuallyContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
+	const slots: SlotFillBubblesVirtuallyContext[ 'slots' ] = proxyMap();
+	const fills: SlotFillBubblesVirtuallyContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: SlotFillBubblesVirtuallyContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = slots.get( name ) || {
+			ref: undefined,
```

What do you think than about this alternative version? 

```tsx
		const slot = slots.get( name );

		slots.set(
			name,
			valRef( {
				...slot,
				ref: ref || slot?.ref,
				fillProps: fillProps || slot?.fillProps || {},
			} )
		);
```

It avoids creating the fallback object with `ref` and `fillProps`, and the result should be the same

#### torounit on 2023-09-28T02:54:25Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1339456264
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -15,13 +14,25 @@ import isShallowEqual from '@wordpress/is-shallow-equal';
  * Internal dependencies
  */
 import SlotFillContext from './slot-fill-context';
+import type {
+	SlotFillProviderProps,
+	SlotFillBubblesVirtuallyContext,
+} from '../types';
 
-function createSlotRegistry() {
-	const slots = proxyMap();
-	const fills = proxyMap();
+function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
+	const slots: SlotFillBubblesVirtuallyContext[ 'slots' ] = proxyMap();
+	const fills: SlotFillBubblesVirtuallyContext[ 'fills' ] = proxyMap();
+
+	const registerSlot: SlotFillBubblesVirtuallyContext[ 'registerSlot' ] = (
+		name,
+		ref,
+		fillProps
+	) => {
+		const slot = slots.get( name ) || {
+			ref: undefined,
```

I remember the last time I tried it I got an error, but now I have no problem at all. Thanks! @ciampo

### Review thread 44 (resolved)

#### ciampo on 2023-09-08T15:17:27Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320004244
Path: packages/components/src/slot-fill/stories/index.tsx

This file should be renamed to `index.story.tsx` in order to be picked up by Storybook

### Review thread 45 (resolved)

#### ciampo on 2023-09-08T15:35:33Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320023925
Path: packages/components/src/slot-fill/stories/index.tsx

```diff
@@ -0,0 +1,139 @@
+/**
+ * External dependencies
+ */
+import type { Meta, StoryFn } from '@storybook/react';
+
+/**
+ * WordPress dependencies
+ */
+import { createContext, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { Slot, Fill, Provider as SlotFillProvider } from '../';
+
+const meta: Meta< typeof Slot > = {
+	component: Slot,
+	title: 'Components/SlotFill',
+	// @ts-expect-error - See https://github.com/storybookjs/storybook/issues/23170
+	subcomponents: { Fill, SlotFillProvider },
+	argTypes: {
+		name: { control: { type: null } },
+		as: { control: { type: 'text' } },
+		fillProps: { control: { type: null } },
+	},
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
+	},
+};
+
+export default meta;
+
+export const Default: StoryFn< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: <Slot { ...props } name="name" />
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" />
+			</p>
+			<Fill name="name">Grace</Fill>
+			<Fill name="age">33</Fill>
+		</SlotFillProvider>
+	);
+};
+Default.args = {
+	className: 'my-slot',
+	bubblesVirtually: true,
+	as: 'span',
+};
+
+export const WithFillProps: StoryFn< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name:{ ' ' }
+				<Slot
+					{ ...props }
+					name="name"
+					fillProps={ { name: 'Grace' } }
+				/>
+			</p>
+			<p>
+				Age: <Slot { ...props } name="age" fillProps={ { age: 33 } } />
+			</p>
+
+			<Fill name="name">{ ( fillProps ) => fillProps.name }</Fill>
+			<Fill name="age">{ ( fillProps ) => fillProps.age }</Fill>
+		</SlotFillProvider>
+	);
+};
+WithFillProps.args = {
+	...Default.args,
+};
+
+export const WithSlotChildren: StoryFn< typeof Slot > = ( props ) => {
+	return (
+		<SlotFillProvider>
+			<h2>Profile</h2>
+			<p>
+				Name: { /* @ts-ignore */ }
```

Why the `ts-ignore` here?

It seems to be that TS is correct in flagging this error: `Slot` is receiving `bubbleVirtually={true}` via the story args (same as the default story), and therefore it should not support `children`.

In case this was on purpose, we should change the comment to use `@ts-expect-error`, and add a comment explaining the reason why

### Review thread 46 (resolved)

#### ciampo on 2023-09-08T15:37:42Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320025972
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,129 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: FillProps;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
```

What's the reason for having `bubblesVirtually` in the base props, since it's being overridden later?

If it's for specifying JS docs only in one place, I checked on my local machine and I'm not getting the JSDoc correctly — I'm afraid we'd have to remove the prop from base props, and add the JSDoc snippet twice.

### Review thread 47 (resolved)

#### ciampo on 2023-09-08T15:37:56Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320026212
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,129 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: FillProps;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
```

For extra clarity?

```suggestion
			bubblesVirtually: true;
			children?: never;
```

#### torounit on 2023-09-08T18:13:04Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320195817
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,129 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: FillProps;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
```

If added `children?: never` , I get an error on the following line.

https://github.com/WordPress/gutenberg/blob/48890827dfd74dd536c2ce24e0a0a5f7a69882d4/packages/components/src/slot-fill/index.tsx#L78

Is there any way to make it better?

#### ciampo on 2023-09-09T11:52:13Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1320548263
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,129 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: FillProps;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
```

Yup, that's because the `Omit` utility doesn't do well with type unions.

Something like this should fix it:

```diff
diff --git a/packages/components/src/slot-fill/index.tsx b/packages/components/src/slot-fill/index.tsx
index 57a9bfbe58..043b87c1b9 100644
--- a/packages/components/src/slot-fill/index.tsx
+++ b/packages/components/src/slot-fill/index.tsx
@@ -67,6 +67,14 @@ export function Provider( { children }: SlotFillProviderProps ) {
 	);
 }
 
+type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+type SlotComponentPropsWithoutName = DistributiveOmit<
+	SlotComponentProps,
+	'name'
+>;
+
 export function createSlotFill( key: SlotKey ) {
 	const baseName = typeof key === 'symbol' ? key.description : key;
 	const FillComponent = ( props: Omit< FillComponentProps, 'name' > ) => (
@@ -74,7 +82,7 @@ export function createSlotFill( key: SlotKey ) {
 	);
 	FillComponent.displayName = `${ baseName }Fill`;
 
-	const SlotComponent = ( props: Omit< SlotComponentProps, 'name' > ) => (
+	const SlotComponent = ( props: SlotComponentPropsWithoutName ) => (
 		<Slot name={ key } { ...props } />
 	);
 	SlotComponent.displayName = `${ baseName }Slot`;
diff --git a/packages/components/src/slot-fill/types.ts b/packages/components/src/slot-fill/types.ts
index c16715d6fe..da07dac01d 100644
--- a/packages/components/src/slot-fill/types.ts
+++ b/packages/components/src/slot-fill/types.ts
@@ -28,6 +28,7 @@ export type SlotComponentProps =
 			 * also accept an optional `className`, `id`, etc.  to add to the slot container.
 			 */
 			bubblesVirtually: true;
+			children?: never;
 	  } )
 	| ( SlotPropBase & {
 			/**

```

Source: https://stackoverflow.com/questions/57103834/typescript-omit-a-property-from-all-interfaces-in-a-union-but-keep-the-union-s

#### torounit on 2023-09-19T01:37:49Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1329454582
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,129 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: FillProps;
+
+	/**
+	 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+	 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+	 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+	 */
+	bubblesVirtually?: boolean;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			bubblesVirtually: true;
```

@ciampo 
Sorry for the delay in getting back to you.

Excellent! Works correctly!!

### Review thread 48 (resolved)

#### ciampo on 2023-09-19T13:31:09Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1330132889
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
```

Let's use something slightly more defined, maybe 

```suggestion
export type FillProps = Record< string, any >;
``` ?

#### torounit on 2023-09-20T14:47:13Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1331752212
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+
+export type SlotKey = string | symbol;
+
+export type FillProps = any;
```

It would be better to use Record type.

### Review thread 49 (resolved)

#### ciampo on 2023-09-21T13:00:09Z

URL: https://github.com/WordPress/gutenberg/pull/51350#discussion_r1333014389
Path: packages/components/src/slot-fill/types.ts

```diff
@@ -0,0 +1,155 @@
+/**
+ * External dependencies
+ */
+import type { Component, MutableRefObject, ReactNode, RefObject } from 'react';
+
+export type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+
+export type SlotKey = string | symbol;
+
+export type FillProps = Record< string, any >;
+
+type SlotPropBase = {
+	/**
+	 * Slot name.
+	 */
+	name: SlotKey;
+
+	/**
+	 * props to pass from `Slot` to `Fill`.
+	 *
+	 * @default {}
+	 */
+	fillProps?: FillProps;
+};
+
+export type SlotComponentProps =
+	| ( SlotPropBase & {
+			/**
+			 * By default, events will bubble to their parents on the DOM hierarchy (native event bubbling).
+			 * If set to true, events will bubble to their virtual parent in the React elements hierarchy instead,
+			 * also accept an optional `className`, `id`, etc.  to add to the slot container.
+			 */
+			bubblesVirtually: true;
+
+			/**
+			 * A function that returns nodes to be rendered.
+			 * Not supported when `bubblesVirtually` is true.
+			 */
+			children?: never;
+
+			/**
+			 * className.
+			 * Not supported when `bubblesVirtually` is true.
+			 */
+			className?: string | undefined;
```

Is the `| undefined` redundant?

```suggestion
			/**
			 * A function that returns nodes to be rendered.
			 * Not supported when `bubblesVirtually` is true.
			 */
			children?: never;

			/**
			 * className.
			 * Not supported when `bubblesVirtually` is true.
			 */
			className?: string;
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +3/-0)
- packages/components/src/dropdown-menu-v2/types.ts (MODIFIED, +1/-1)
- packages/components/src/popover/index.tsx (MODIFIED, +0/-1)
- packages/components/src/slot-fill/bubbles-virtually/fill.tsx (RENAMED, +6/-7)
- packages/components/src/slot-fill/bubbles-virtually/slot-fill-context.ts (RENAMED, +8/-3)
- packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.js (DELETED, +0/-95)
- packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx (ADDED, +115/-0)
- packages/components/src/slot-fill/bubbles-virtually/slot.tsx (RENAMED, +16/-3)
- packages/components/src/slot-fill/bubbles-virtually/use-slot-fills.ts (RENAMED, +2/-2)
- packages/components/src/slot-fill/bubbles-virtually/use-slot.ts (RENAMED, +15/-6)
- packages/components/src/slot-fill/context.js (DELETED, +0/-17)
- packages/components/src/slot-fill/context.ts (ADDED, +21/-0)
- packages/components/src/slot-fill/fill.ts (RENAMED, +2/-3)
- packages/components/src/slot-fill/index.tsx (RENAMED, +37/-13)
- packages/components/src/slot-fill/provider.js (DELETED, +0/-119)
- packages/components/src/slot-fill/provider.tsx (ADDED, +129/-0)
- packages/components/src/slot-fill/slot.tsx (RENAMED, +28/-14)
- packages/components/src/slot-fill/stories/index.story.js (DELETED, +0/-89)
- packages/components/src/slot-fill/stories/index.story.tsx (ADDED, +140/-0)
- packages/components/src/slot-fill/types.ts (ADDED, +155/-0)
- packages/components/src/slot-fill/use-slot.ts (RENAMED, +4/-4)
