---
number: 56909
type: pull_request
state: MERGED
merged_at: 2023-12-13T11:56:04Z
url: https://github.com/WordPress/gutenberg/pull/56909
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Performance","[Package] Components"]
---

# PR #56909: Navigator: use CSS animations instead of framer-motion

- URL: https://github.com/WordPress/gutenberg/pull/56909
- Author: ciampo
- Created: 2023-12-08T17:18:00Z
- Updated: 2024-01-03T16:40:21Z
- Comments: 4 of 4
- Review threads: 6 of 6
- Changed files: 5 of 5
- Diff: ../pr-diffs/56909.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Related to #55892

Use vanilla CSS animations for the `Navigator` components instead of `framer-motion`

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

**This PR doesn't fix the real cause for #55892, but aims at improving a visual side-effect caused by it**

`framer-motion` animations updated the `style` attribute of the animating DOM element, setting a new value for every frame. In other words, `framer-motion` performed some calculations in every animation frame to interpolate the exact values of the animation (ie. translation). These calculations happen on the main thread, and therefore the animation can get quite janky if the main thread is busy with other tasks (see #55892).

Using vanilla CSS allows the animation to [run directly on the GPU](https://www.smashingmagazine.com/2016/12/gpu-animation-doing-it-right/), running smoothly regardless of how busy the CPU is.

Furthermore, the animations required for this component are simple enough to be written with vanilla CSS, and don't necessarily require the sophisticated orchestration features that `framer-motion` offers.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Moved all styles to separate `styles.ts` file
- Replaced `framer-motion` with CSS animations
- added [`contain: strict`](https://developer.mozilla.org/en-US/docs/Web/CSS/contain) to the provider screen wrapper to further potentially enhance rendering performance

Note: with this PR, **navigator screens don't have an exit animation anymore**, as it would require a larger refactor (that's because the previous `NavigatorScreen`'s wrapper element gets removed from the DOM as soon the selected screen `id` changes).

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

1. Interact with the `Navigator` component in Storybook:
    - Make sure that the first screen doesn't have an enter animation
    - Make sure that all subsequent animations (both backward and forward) animate as expected
    - Make sure that the animations are inverted in contexts with RTL direction
    - Make sure that the animations are disabled when the `prefers-reduced-motion` option is enabled
2. Open the side editor, and interact with the sidebar:
    - Make sure that navigating across submenus works as expected and the animation looks as expected
    - Make sure that the animation doesn't slow down and get janky like on `trunk` (see #55892)
3. Open the global styles sidebar in the site editor:
    - Make sure that the sidebar works and looks as on `trunk`, especially when navigating across the submenus

## Screenshots or screencast <!-- if applicable -->

### Before (`trunk`)

Notice the jankiness (even while testing on a high-end machine), especially while navigating to/from the "Patterns" navigator screen

https://github.com/WordPress/gutenberg/assets/1083581/31eafcd0-60e0-4d66-8127-a4711ae46282



### After (this PR)

No jankiness

https://github.com/WordPress/gutenberg/assets/1083581/7d3593ea-3fa5-4350-9801-078addad3af9

## Issue comments

### ciampo on 2023-12-08T17:19:20Z

URL: https://github.com/WordPress/gutenberg/pull/56909#issuecomment-1847553122

@tyxla feel free to test this version of the component and let me know if it's worth continuing the work on this PR.

### github-actions on 2023-12-08T17:36:50Z

URL: https://github.com/WordPress/gutenberg/pull/56909#issuecomment-1847573527

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 6599008baf45833ddc10a0df6bee6af6d3586065.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/7192313789
📝  Reported issues:
- #56776 in `/test/e2e/specs/site-editor/font-library.spec.js`

### draganescu on 2023-12-13T14:49:52Z

URL: https://github.com/WordPress/gutenberg/pull/56909#issuecomment-1854058254

For me this PR in Firefox works a bit weird:


https://github.com/WordPress/gutenberg/assets/107534/d623089f-ff86-4664-9cc3-834dcaa65a9a

- Navigating is sometimes animated, sometimes doesn't look animated
- Navigating back to templates from its children shows a blank sidebar for a bit
- Navigating through navigations still stutters

### ciampo on 2023-12-13T15:22:03Z

URL: https://github.com/WordPress/gutenberg/pull/56909#issuecomment-1854133224

Hey @draganescu , I just tested `Navigator` [in isolation in Storybook](https://wordpress.github.io/gutenberg/iframe.html?args=&id=components-experimental-navigator--default&viewMode=story) on Firefox and the animations seems to work as expected.

My suspicion is that the site editor is slower in Firefox compared to Chrome, which causes the animations to stutter to the point that it looks like they don't happen at all and/or are glitchy. This is especially true when animating to/from the patterns screen.

As explained above, the changes in this PR can only partially improve the perception of smoothness on the `Navigator` animation, but really it is just trying to improve a symptom and not the actual issue. The real issue is IMO caused by the site editor canvas performing some CPU-heavy operations.

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2023-12-11T13:13:30Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1422450837
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+
+	contain: strict;
+`;
+
+const fadeInFromRight = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+const fadeInFromLeft = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(-50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+type NavigatorScreenAnimationProps = {
+	isInitial?: boolean;
+	isBack?: boolean;
+	isRTL: boolean;
+};
+
+const navigatorScreenAnimation = ( {
+	isInitial,
+	isBack,
+	isRTL,
+}: NavigatorScreenAnimationProps ) => {
+	if ( isInitial && ! isBack ) {
+		return css``;
+	}
+
+	const animationName =
+		( isRTL && isBack ) || ( ! isRTL && ! isBack )
+			? fadeInFromRight
+			: fadeInFromLeft;
+
+	return css`
+		animation-duration: 0.14s;
+		/* easeInOutCubic */
+		animation-timing-function: cubic-bezier( 0.65, 0, 0.35, 1 );
```

Isn't this a better mirror representation of the originally used `easeInOut` easing function:

```suggestion
		animation-timing-function: cubic-bezier( 0.42, 0, 0.58, 1 );
```

Or are we intentionally adding a bit of an acceleration effect by using the cubic version?

Note that this is mostly a question and I don't have strong preferences on the easing we're using.

#### ciampo on 2023-12-13T06:53:21Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1424919312
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+
+	contain: strict;
+`;
+
+const fadeInFromRight = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+const fadeInFromLeft = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(-50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+type NavigatorScreenAnimationProps = {
+	isInitial?: boolean;
+	isBack?: boolean;
+	isRTL: boolean;
+};
+
+const navigatorScreenAnimation = ( {
+	isInitial,
+	isBack,
+	isRTL,
+}: NavigatorScreenAnimationProps ) => {
+	if ( isInitial && ! isBack ) {
+		return css``;
+	}
+
+	const animationName =
+		( isRTL && isBack ) || ( ! isRTL && ! isBack )
+			? fadeInFromRight
+			: fadeInFromLeft;
+
+	return css`
+		animation-duration: 0.14s;
+		/* easeInOutCubic */
+		animation-timing-function: cubic-bezier( 0.65, 0, 0.35, 1 );
```

Good point, I actually switched to the standard CSS `ease-in-out` keyword (which represents the same bezier curve that you suggested)

https://github.com/WordPress/gutenberg/pull/56909/commits/5d662f78041410ab97511e9a8582b030d464aa0e

### Review thread 2 (resolved)

#### tyxla on 2023-12-11T13:15:39Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1422453136
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+
+	contain: strict;
+`;
+
+const fadeInFromRight = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+const fadeInFromLeft = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(-50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+type NavigatorScreenAnimationProps = {
+	isInitial?: boolean;
+	isBack?: boolean;
+	isRTL: boolean;
+};
+
+const navigatorScreenAnimation = ( {
+	isInitial,
+	isBack,
+	isRTL,
+}: NavigatorScreenAnimationProps ) => {
+	if ( isInitial && ! isBack ) {
+		return css``;
+	}
+
+	const animationName =
+		( isRTL && isBack ) || ( ! isRTL && ! isBack )
+			? fadeInFromRight
+			: fadeInFromLeft;
+
+	return css`
+		animation-duration: 0.14s;
+		/* easeInOutCubic */
+		animation-timing-function: cubic-bezier( 0.65, 0, 0.35, 1 );
+		will-change: transform, opacity;
+		/* Default animation.*/
```

This comment feels redundant.

```suggestion
```

#### ciampo on 2023-12-13T06:53:43Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1424919540
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+
+	contain: strict;
+`;
+
+const fadeInFromRight = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+const fadeInFromLeft = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(-50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+type NavigatorScreenAnimationProps = {
+	isInitial?: boolean;
+	isBack?: boolean;
+	isRTL: boolean;
+};
+
+const navigatorScreenAnimation = ( {
+	isInitial,
+	isBack,
+	isRTL,
+}: NavigatorScreenAnimationProps ) => {
+	if ( isInitial && ! isBack ) {
+		return css``;
+	}
+
+	const animationName =
+		( isRTL && isBack ) || ( ! isRTL && ! isBack )
+			? fadeInFromRight
+			: fadeInFromLeft;
+
+	return css`
+		animation-duration: 0.14s;
+		/* easeInOutCubic */
+		animation-timing-function: cubic-bezier( 0.65, 0, 0.35, 1 );
+		will-change: transform, opacity;
+		/* Default animation.*/
```

https://github.com/WordPress/gutenberg/pull/56909/commits/92acce9831deb9524381a8f98a2fc6366640d3fc

### Review thread 3

#### tyxla on 2023-12-11T13:16:49Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1422454402
Path: packages/components/src/navigator/navigator-screen/component.tsx

```diff
@@ -31,22 +26,11 @@ import { contextConnect, useContextSystem } from '../../context';
 import { useCx } from '../../utils/hooks/use-cx';
 import { View } from '../../view';
 import { NavigatorContext } from '../context';
+import * as styles from '../styles';
 import type { NavigatorScreenProps } from '../types';
 
-const animationEnterDelay = 0;
-const animationEnterDuration = 0.14;
-const animationExitDuration = 0.14;
-const animationExitDelay = 0;
-
-// Props specific to `framer-motion` can't be currently passed to `NavigatorScreen`,
-// as some of them would overlap with HTML props (e.g. `onAnimationStart`, ...)
-type Props = Omit<
-	WordPressComponentProps< NavigatorScreenProps, 'div', false >,
-	Exclude< keyof MotionProps, 'style' | 'children' >
->;
-
 function UnconnectedNavigatorScreen(
-	props: Props,
+	props: WordPressComponentProps< NavigatorScreenProps, 'div', false >,
```

Nice simplification of the types 👍 

Any backward compatibility concerns? For example, we won't be able to use any of the framer motion animation events like `onAnimationStart` / `onAnimationComplete`.

#### ciampo on 2023-12-13T06:25:54Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1424900779
Path: packages/components/src/navigator/navigator-screen/component.tsx

```diff
@@ -31,22 +26,11 @@ import { contextConnect, useContextSystem } from '../../context';
 import { useCx } from '../../utils/hooks/use-cx';
 import { View } from '../../view';
 import { NavigatorContext } from '../context';
+import * as styles from '../styles';
 import type { NavigatorScreenProps } from '../types';
 
-const animationEnterDelay = 0;
-const animationEnterDuration = 0.14;
-const animationExitDuration = 0.14;
-const animationExitDelay = 0;
-
-// Props specific to `framer-motion` can't be currently passed to `NavigatorScreen`,
-// as some of them would overlap with HTML props (e.g. `onAnimationStart`, ...)
-type Props = Omit<
-	WordPressComponentProps< NavigatorScreenProps, 'div', false >,
-	Exclude< keyof MotionProps, 'style' | 'children' >
->;
-
 function UnconnectedNavigatorScreen(
-	props: Props,
+	props: WordPressComponentProps< NavigatorScreenProps, 'div', false >,
```

If I recall correctly (and interpret the previous TypeScript code correctly), that shouldn't be a problem because `Navigator` was previously _not_ documenting props specific to `framer-motion` that would overlap with standard HTML properties (like `onAnimationStart`).

So, with the changes from this PR, the component should now accept the default HTML `onAnimationStart` etc

### Review thread 4 (resolved)

#### tyxla on 2023-12-11T13:20:58Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1422459116
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+
+	contain: strict;
+`;
+
+const fadeInFromRight = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+const fadeInFromLeft = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(-50px)`,
```

Spacing nits. Our linter seems to ignore spacing in that context.

```suggestion
		transform: `translateX( 50px )`,
	},
	'100%': { opacity: 1, transform: 'none' },
} );

const fadeInFromLeft = keyframes( {
	'0%': {
		opacity: 0,
		transform: `translateX( -50px )`,
```

#### ciampo on 2023-12-13T06:52:12Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1424918551
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,70 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+
+	contain: strict;
+`;
+
+const fadeInFromRight = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(50px)`,
+	},
+	'100%': { opacity: 1, transform: 'none' },
+} );
+
+const fadeInFromLeft = keyframes( {
+	'0%': {
+		opacity: 0,
+		transform: `translateX(-50px)`,
```

https://github.com/WordPress/gutenberg/pull/56909/commits/9fbbc088a05df64510cc2dbc2aa80152573267a1

### Review thread 5 (resolved)

#### tyxla on 2023-12-11T13:25:03Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1422463779
Path: packages/components/src/navigator/navigator-screen/component.tsx

```diff
@@ -149,73 +133,11 @@ function UnconnectedNavigatorScreen(
 
 	const mergedWrapperRef = useMergeRefs( [ forwardedRef, wrapperRef ] );
 
-	if ( ! isMatch ) {
-		return null;
-	}
-
-	if ( prefersReducedMotion ) {
-		return (
-			<View
-				ref={ mergedWrapperRef }
-				className={ classes }
-				{ ...otherProps }
-			>
-				{ children }
-			</View>
-		);
-	}
-
-	const animate = {
-		opacity: 1,
-		transition: {
-			delay: animationEnterDelay,
-			duration: animationEnterDuration,
-			ease: 'easeInOut',
-		},
-		x: 0,
-	};
-	// Disable the initial animation if the screen is the very first screen to be
-	// rendered within the current `NavigatorProvider`.
-	const initial =
-		location.isInitial && ! location.isBack
-			? false
-			: {
-					opacity: 0,
-					x:
-						( isRTL() && location.isBack ) ||
-						( ! isRTL() && ! location.isBack )
-							? 50
-							: -50,
-			  };
-	const exit = {
-		delay: animationExitDelay,
-		opacity: 0,
-		x:
-			( ! isRTL() && location.isBack ) || ( isRTL() && ! location.isBack )
-				? 50
-				: -50,
-		transition: {
-			duration: animationExitDuration,
-			ease: 'easeInOut',
-		},
-	};
-
-	const animatedProps = {
-		animate,
-		exit,
-		initial,
-	};
-
-	return (
-		<motion.div
-			ref={ mergedWrapperRef }
-			className={ classes }
-			{ ...otherProps }
-			{ ...animatedProps }
-		>
+	return isMatch ? (
+		<View ref={ mergedWrapperRef } className={ classes } { ...otherProps }>
```

Since the animations will now be processed with CSS, the test related to verifying animations run will fail. I think it's safe to delete it.

#### ciampo on 2023-12-13T06:54:41Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1424920234
Path: packages/components/src/navigator/navigator-screen/component.tsx

```diff
@@ -149,73 +133,11 @@ function UnconnectedNavigatorScreen(
 
 	const mergedWrapperRef = useMergeRefs( [ forwardedRef, wrapperRef ] );
 
-	if ( ! isMatch ) {
-		return null;
-	}
-
-	if ( prefersReducedMotion ) {
-		return (
-			<View
-				ref={ mergedWrapperRef }
-				className={ classes }
-				{ ...otherProps }
-			>
-				{ children }
-			</View>
-		);
-	}
-
-	const animate = {
-		opacity: 1,
-		transition: {
-			delay: animationEnterDelay,
-			duration: animationEnterDuration,
-			ease: 'easeInOut',
-		},
-		x: 0,
-	};
-	// Disable the initial animation if the screen is the very first screen to be
-	// rendered within the current `NavigatorProvider`.
-	const initial =
-		location.isInitial && ! location.isBack
-			? false
-			: {
-					opacity: 0,
-					x:
-						( isRTL() && location.isBack ) ||
-						( ! isRTL() && ! location.isBack )
-							? 50
-							: -50,
-			  };
-	const exit = {
-		delay: animationExitDelay,
-		opacity: 0,
-		x:
-			( ! isRTL() && location.isBack ) || ( isRTL() && ! location.isBack )
-				? 50
-				: -50,
-		transition: {
-			duration: animationExitDuration,
-			ease: 'easeInOut',
-		},
-	};
-
-	const animatedProps = {
-		animate,
-		exit,
-		initial,
-	};
-
-	return (
-		<motion.div
-			ref={ mergedWrapperRef }
-			className={ classes }
-			{ ...otherProps }
-			{ ...animatedProps }
-		>
+	return isMatch ? (
+		<View ref={ mergedWrapperRef } className={ classes } { ...otherProps }>
```

It's a pity that we can't easily test that the initial screen doesn't animate forward, but it's a current limitation of `jsdom` 🤷‍♂️ 

https://github.com/WordPress/gutenberg/pull/56909/commits/f84568c9c229a244fa80cbc944f2937ca7ccfdf5

### Review thread 6 (resolved)

#### ktmn on 2024-01-03T15:28:50Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1440584767
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,71 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+	/* Mark this subsection of the DOM as isolated, providing performance benefits
+	 * by limiting calculations of layout, style, paint, size, or any combination
+	 * to a DOM subtree rather than the entire page.
+	 */
+	contain: strict;
```

The `contain: strict` makes everything (that doesn't have height or isn't inside a flex with grow) have 0 height for me. I use Navigator in 4 places and all of them "broke", (which is fine, since it's __experimental), but just an FYI.

#### ciampo on 2024-01-03T16:40:21Z

URL: https://github.com/WordPress/gutenberg/pull/56909#discussion_r1440662912
Path: packages/components/src/navigator/styles.ts

```diff
@@ -0,0 +1,71 @@
+/**
+ * External dependencies
+ */
+import { css, keyframes } from '@emotion/react';
+
+export const navigatorProviderWrapper = css`
+	/* Prevents horizontal overflow while animating screen transitions */
+	overflow-x: hidden;
+	/* Mark this subsection of the DOM as isolated, providing performance benefits
+	 * by limiting calculations of layout, style, paint, size, or any combination
+	 * to a DOM subtree rather than the entire page.
+	 */
+	contain: strict;
```

Thank you for letting us know! The fix has been merged in #57498

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/navigator/navigator-provider/component.tsx (MODIFIED, +3/-4)
- packages/components/src/navigator/navigator-screen/component.tsx (MODIFIED, +15/-93)
- packages/components/src/navigator/styles.ts (ADDED, +71/-0)
- packages/components/src/navigator/test/index.tsx (MODIFIED, +0/-64)
