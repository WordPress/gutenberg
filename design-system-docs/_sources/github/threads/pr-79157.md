---
number: 79157
type: pull_request
state: MERGED
merged_at: 2026-06-18T12:59:36Z
url: https://github.com/WordPress/gutenberg/pull/79157
matched_labels: ["Design System","[Package] Theme"]
all_labels: ["[Type] Developer Documentation","Design System","[Package] Theme"]
---

# PR #79157: Theme: Add design tokens maintainer's guide documentation

- URL: https://github.com/WordPress/gutenberg/pull/79157
- Author: aduth
- Created: 2026-06-12T15:17:42Z
- Updated: 2026-06-18T13:00:13Z
- Comments: 2 of 2
- Review threads: 8 of 8
- Changed files: 3 of 3
- Diff: ../pr-diffs/79157.diff

## Body

## What?

Related: https://github.com/WordPress/gutenberg/pull/79032#issuecomment-4672707439

Adds a new `README.md` within the `tokens/` directory of the `@wordpress/theme` package to serve as a maintainer's guide for the WordPress Design System design tokens.

## Why?

- Adds additional context for how tokens are maintained which has been undocumented until now:
   - The difference between primitive and semantic tokens and why they exist the way they do
   - Explicit support for Figma variables import, including `$extensions['com.figma.scopes']` and where to find information about maintaining those values
- Improves the relevance of the package's root `README.md` to include information about how to use the theme package. Most people shouldn't care about the existence of `tokens/color.json` and how these files are structured, for example.
- Avoids a confusing dead-end if someone were to link to [the `packages/themes/tokens` directory](https://github.com/WordPress/gutenberg/tree/trunk/packages/theme/tokens). This would be reasonable to do as it is the source of truth for the design system's design tokens, but currently it lacks any documentation context around what that directory is for if someone arrives there directly.

## How?

- Adds new `packages/theme/tokens/README.md`
- Shifts "Structure" and "Token Naming" sub-sections of the root README.md into this new file
- Maintains existing "Design Tokens" section of the root README.md and adds a link to this new document for more information

## Testing Instructions

Review updated documentation for relevance and accuracy.

## Use of AI Tools

No AI was used.

## Issue comments

### github-actions on 2026-06-12T15:18:50Z

URL: https://github.com/WordPress/gutenberg/pull/79157#issuecomment-4692600296

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: shail-mehta <shailu25@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-12T15:50:40Z

URL: https://github.com/WordPress/gutenberg/pull/79157#issuecomment-4692847835

<!-- flaky-tests-report-comment -->
**Flaky tests detected in af0d860cb1aea93f2a53537390888f1bb27201d8.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27717062397
📝  Reported issues:
- #78706 in `/test/e2e/specs/editor/various/autosave.spec.js`

## Review threads

### Review thread 1 (resolved)

#### shail-mehta on 2026-06-14T04:24:43Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3409009767
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
```

```suggestion
# Design Tokens Maintainer's Guide
```

#### aduth on 2026-06-15T20:52:26Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3416495477
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
```

Good catch! Updated in https://github.com/WordPress/gutenberg/pull/79157/commits/a89dbac4bd90dea45f49b0f4e8f65cb15df56837.

### Review thread 2 (resolved)

#### ciampo on 2026-06-15T15:07:00Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3414505870
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
+
+**Primitive tokens** are internal, reusable, raw values emitted by the design system's theming. They are not part of the public API, but instead are referenced by semantic tokens, which use the underlying values and provide purpose and meaning to how those values are used.
+
+**Semantic tokens** are the public API of the design system's tokens. They map to primitive token values, but the values are incidental, and a consumer is expected to choose a semantic token based on their specific use-case. The design system provides semantic tokens to cover a breadth of use-cases that are standardized at a design systems level.
+
+This structure is meant to shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
+
+Example:
+
+```jsonc
+{
+	"wpds-dimension": {
+		"$type": "dimension",
+		"primitive": {
+			"space": {
+				"10": {
+					"$value": { "value": 4, "unit": "px" }
+				}
+            },
+            // ...
+        }
+		"padding": {
+			"xs": {
+				"$value": "{wpds-dimension.primitive.space.10}"
+			},
+            // ...
+        }
+    }
+}
```

Looks like this snippet is invalid / malformatted


```suggestion
{
	"wpds-dimension": {
		"$type": "dimension",
		"primitive": {
			"space": {
				"10": {
					"$value": { "value": 4, "unit": "px" }
				}
				// ...
			}
		},
		"padding": {
			"xs": {
				"$value": "{wpds-dimension.primitive.space.10}"
			}
			// ...
		}
	}
}
```

#### aduth on 2026-06-15T20:53:56Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3416502842
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
+
+**Primitive tokens** are internal, reusable, raw values emitted by the design system's theming. They are not part of the public API, but instead are referenced by semantic tokens, which use the underlying values and provide purpose and meaning to how those values are used.
+
+**Semantic tokens** are the public API of the design system's tokens. They map to primitive token values, but the values are incidental, and a consumer is expected to choose a semantic token based on their specific use-case. The design system provides semantic tokens to cover a breadth of use-cases that are standardized at a design systems level.
+
+This structure is meant to shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
+
+Example:
+
+```jsonc
+{
+	"wpds-dimension": {
+		"$type": "dimension",
+		"primitive": {
+			"space": {
+				"10": {
+					"$value": { "value": 4, "unit": "px" }
+				}
+            },
+            // ...
+        }
+		"padding": {
+			"xs": {
+				"$value": "{wpds-dimension.primitive.space.10}"
+			},
+            // ...
+        }
+    }
+}
```

> Looks like this snippet is invalid / malformatted

Good eye. These are the hazards of doing things by hand still these days 😅

### Review thread 3 (resolved)

#### ciampo on 2026-06-15T15:09:25Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3414521988
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
```

This new section has a lot of overlap with the existing `Architecture` section in `packages/theme/README.md`.  Should we DRY the docs up, maybe merging all info under this section, and replacing the contents of the `Architecture` section in the package's README with a link to this doc?

#### aduth on 2026-06-15T16:16:05Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3414939422
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
```

Heh, I missed that Architecture section in the README despite having written both of them myself 🤦 Should definitely consolidate. I waffle a bit on whether there's some value in sharing this hierarchy to end-users (and keeping some explanation in the README), though given that ultimately they only ever interact with semantic tokens, maybe the distinction isn't too valuable.

#### aduth on 2026-06-15T21:09:52Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3416583489
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
```

I opted to remove the Architecture section altogether in 6436f837886b1bf8a3908753afd709922f5a2231. I don't think there's anything there that isn't really covered or needs to be covered in the new document. I considered pulling over "what the browsers eventually interpret", but I don't think that's entirely true of how primitive tokens make their way into the browser now. And "primitive values can be updated" is kinda captured by 6f7558ccf0ca0475e6ba6a7c06e53b1ca8a173fe.

### Review thread 4 (resolved)

#### ciampo on 2026-06-15T15:11:31Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3414536853
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
```

I would potentially also add another key concept related to the tokens: consuming the tokens ensures that the UI componentry will always evolve and adapt to the latest WPDS changes automatically, and to any theming change applied via `ThemeProvider`.

#### aduth on 2026-06-15T20:58:40Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3416526549
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
```

> I would potentially also add another key concept related to the tokens: consuming the tokens ensures that the UI componentry will always evolve and adapt to the latest WPDS changes automatically, and to any theming change applied via `ThemeProvider`.

Yeah I thought I touched on that a bit with "alter these values based on factors like density, which could affect the value of the primitive token and cascade automatically to the CSS properties", but I think it could be improved to emphasize how components use these tokens and how we could do internal refactoring unrelated to configurability like density and have those changes cascade as well.

#### aduth on 2026-06-15T21:08:24Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3416576463
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
```

@ciampo How do you feel about the paragraph added in 6f7558ccf0ca0475e6ba6a7c06e53b1ca8a173fe ?

#### ciampo on 2026-06-16T14:27:44Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3421542769
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
```

Great!

### Review thread 5 (resolved)

#### ciampo on 2026-06-15T15:12:48Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3414547221
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
+
+**Primitive tokens** are internal, reusable, raw values emitted by the design system's theming. They are not part of the public API, but instead are referenced by semantic tokens, which use the underlying values and provide purpose and meaning to how those values are used.
+
+**Semantic tokens** are the public API of the design system's tokens. They map to primitive token values, but the values are incidental, and a consumer is expected to choose a semantic token based on their specific use-case. The design system provides semantic tokens to cover a breadth of use-cases that are standardized at a design systems level.
+
+This structure is meant to shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
```

Nit: highlighting how consumers should reason around tokens

```suggestion
This structure is meant to **shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent**. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
```

#### aduth on 2026-06-15T20:54:43Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3416506731
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,76 @@
+# Design Tokens Mantainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
+
+**Primitive tokens** are internal, reusable, raw values emitted by the design system's theming. They are not part of the public API, but instead are referenced by semantic tokens, which use the underlying values and provide purpose and meaning to how those values are used.
+
+**Semantic tokens** are the public API of the design system's tokens. They map to primitive token values, but the values are incidental, and a consumer is expected to choose a semantic token based on their specific use-case. The design system provides semantic tokens to cover a breadth of use-cases that are standardized at a design systems level.
+
+This structure is meant to shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
```

> Nit: highlighting how consumers should reason around tokens

👍 Updated in https://github.com/WordPress/gutenberg/pull/79157/commits/ac2d376d24b6f4991978e543efc63df9eff9c014.

### Review thread 6

#### ciampo on 2026-06-16T14:30:58Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3421566170
Path: packages/theme/README.md

I'm now noticing that this document has a `h2` `Design Tokens` (linking to the separate doc) and also an `h3` `Design Tokens` section . Is this another opportunity for consolidation / removal of duplicated content?

#### aduth on 2026-06-17T19:54:41Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3431051783
Path: packages/theme/README.md

That's a great catch, especially since it's literally in the same hierarchy 😂 🤦 (Theme > Design Tokens > Design Tokens). I consolidated in 62dbe35c688f530eb8c54400cdfbf641142adfbf.

### Review thread 7

#### ciampo on 2026-06-16T14:31:58Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3421573345
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,78 @@
+# Design Tokens Maintainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+Components that use these design tokens benefit from the consistency they guarantee with other components that extend from the same system. Future theming improvements or configurations like color theming (or "dark mode"), density, or roundness will cascade automatically to these components without any additional effort on the part of the component maintainer.
```

Density is not a tweakable aspect of the theme anymore; we should remove all mentions

#### aduth on 2026-06-17T14:48:44Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3429100986
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,78 @@
+# Design Tokens Maintainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+Components that use these design tokens benefit from the consistency they guarantee with other components that extend from the same system. Future theming improvements or configurations like color theming (or "dark mode"), density, or roundness will cascade automatically to these components without any additional effort on the part of the component maintainer.
```

> Density is not a tweakable aspect of the theme anymore; we should remove all mentions

Yeah I mentioned it in regards to general configurability as something we _could_ do (and AFAIK still have ambitions to do), and even tying it to "Future" as the first word of the sentence, but happy to remove if it risks confusion.

#### aduth on 2026-06-17T20:03:24Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3431101848
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,78 @@
+# Design Tokens Maintainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+Components that use these design tokens benefit from the consistency they guarantee with other components that extend from the same system. Future theming improvements or configurations like color theming (or "dark mode"), density, or roundness will cascade automatically to these components without any additional effort on the part of the component maintainer.
```

> Density is not a tweakable aspect of the theme anymore; we should remove all mentions

Removed density references in 72c2bd76cc199637e4df9d14051798d034d82986.

### Review thread 8

#### ciampo on 2026-06-16T14:33:07Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3421581445
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,78 @@
+# Design Tokens Maintainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+Components that use these design tokens benefit from the consistency they guarantee with other components that extend from the same system. Future theming improvements or configurations like color theming (or "dark mode"), density, or roundness will cascade automatically to these components without any additional effort on the part of the component maintainer.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
+
+**Primitive tokens** are internal, reusable, raw values emitted by the design system's theming. They are not part of the public API, but instead are referenced by semantic tokens, which use the underlying values and provide purpose and meaning to how those values are used.
+
+**Semantic tokens** are the public API of the design system's tokens. They map to primitive token values, but the values are incidental, and a consumer is expected to choose a semantic token based on their specific use-case. The design system provides semantic tokens to cover a breadth of use-cases that are standardized at a design systems level.
+
+This structure is meant to **shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent**. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
+
+Example:
+
+```jsonc
+{
+	"wpds-dimension": {
+		"$type": "dimension",
+		"primitive": {
+			"space": {
+				"10": {
+					"$value": { "value": 4, "unit": "px" }
+				}
+				// ...
+			}
+		},
+		"padding": {
+			"xs": {
+				"$value": "{wpds-dimension.primitive.space.10}"
+			}
+			// ...
+		}
+	}
+}
+```
+
+In the example above, the CSS properties generated from these tokens would include:
+
+```css
+--wpds-dimension-padding-xs: 4px;
+```
+
+Someone using the design system should never see or concern themselves with either the `primitive.space.10` token or the underlying 4 pixel base unit. This also enables the design system to alter these values based on factors like density, which could affect the value of the primitive token and cascade automatically to the CSS properties.
```

We should remove the mention of `density` here, too.

Also, the "cascading" part here is slightly redundant after the recent addition of the paragraph at line 5

#### aduth on 2026-06-17T20:03:58Z

URL: https://github.com/WordPress/gutenberg/pull/79157#discussion_r3431104943
Path: packages/theme/tokens/README.md

```diff
@@ -0,0 +1,78 @@
+# Design Tokens Maintainer's Guide
+
+Design tokens are the visual design atoms of a design system. They are named entities that store visual design attributes like colors, spacing, typography, and shadows. They serve as a single source of truth that bridges design and development, ensuring consistency across platforms and making it easy to maintain and evolve the visual language of an application.
+
+Components that use these design tokens benefit from the consistency they guarantee with other components that extend from the same system. Future theming improvements or configurations like color theming (or "dark mode"), density, or roundness will cascade automatically to these components without any additional effort on the part of the component maintainer.
+
+This document includes information about how the design system maintains its tokens implementation. For information about how to use design tokens, refer to the [`@wordpress/theme` package README](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/README.md) and [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md).
+
+## Structure
+
+The design system follows the [Design Tokens Community Group (DTCG)](https://design-tokens.github.io/community-group/format/) specification and organizes tokens into distinct types based on what kind of visual property they represent. Token definitions are stored as JSON files in the `/tokens` directory:
+
+| File              | Description                                                                                                                      |
+| ----------------- | -------------------------------------------------------------------------------------------------------------------------------- |
+| `color.json`      | Color palettes including primitive color ramps and semantic color tokens for backgrounds, foregrounds, strokes, and focus states |
+| `dimension.json`  | Spacing scale and semantic spacing tokens for padding, margins, and sizing                                                       |
+| `typography.json` | Font family stacks, font sizes, and line heights                                                                                 |
+| `border.json`     | Border radius and width values                                                                                                   |
+| `elevation.json`  | Shadow definitions for creating depth and layering                                                                               |
+| `motion.json`     | Animation durations and easing curves                                                                                            |
+
+Each JSON file contains both primitive and semantic token definitions in a hierarchical structure. These files are the source of truth for the design system and are processed during the build step to generate CSS custom properties and other output formats in `/src/prebuilt`.
+
+## Token Naming
+
+Semantic tokens follow a consistent naming pattern that encodes the token's purpose. See the [Design Tokens Reference](https://github.com/WordPress/gutenberg/blob/trunk/packages/theme/docs/tokens.md) for the naming pattern, the meaning of each segment (type, property, target, tone, emphasis, state), and guidance on how to pick the right token.
+
+## Primitive and Semantic Tokens
+
+**Primitive tokens** are internal, reusable, raw values emitted by the design system's theming. They are not part of the public API, but instead are referenced by semantic tokens, which use the underlying values and provide purpose and meaning to how those values are used.
+
+**Semantic tokens** are the public API of the design system's tokens. They map to primitive token values, but the values are incidental, and a consumer is expected to choose a semantic token based on their specific use-case. The design system provides semantic tokens to cover a breadth of use-cases that are standardized at a design systems level.
+
+This structure is meant to **shift the emphasis away from the values themselves and toward the meaning and purpose that the tokens represent**. Ultimately, the tokens still map to raw values that affect how a component is styled and those values should be internally consistent, but the primitive layer is an incidental concern of the theming internals and not a consideration of the end-user of the design system.
+
+Example:
+
+```jsonc
+{
+	"wpds-dimension": {
+		"$type": "dimension",
+		"primitive": {
+			"space": {
+				"10": {
+					"$value": { "value": 4, "unit": "px" }
+				}
+				// ...
+			}
+		},
+		"padding": {
+			"xs": {
+				"$value": "{wpds-dimension.primitive.space.10}"
+			}
+			// ...
+		}
+	}
+}
+```
+
+In the example above, the CSS properties generated from these tokens would include:
+
+```css
+--wpds-dimension-padding-xs: 4px;
+```
+
+Someone using the design system should never see or concern themselves with either the `primitive.space.10` token or the underlying 4 pixel base unit. This also enables the design system to alter these values based on factors like density, which could affect the value of the primitive token and cascade automatically to the CSS properties.
```

> We should remove the mention of `density` here, too.
> 
> Also, the "cascading" part here is slightly redundant after the recent addition of the paragraph at line 5

I removed this entire sentence as part of 72c2bd76cc199637e4df9d14051798d034d82986. And then built upon the paragraph in 49e31e7c22b208ca413c66cba1f7def54ab69e2a with a more concrete example of how someone could think about semantics (swapped padding example with element size).

## Changed files

- packages/theme/CHANGELOG.md (MODIFIED, +6/-1)
- packages/theme/README.md (MODIFIED, +7/-35)
- packages/theme/tokens/README.md (ADDED, +81/-0)
