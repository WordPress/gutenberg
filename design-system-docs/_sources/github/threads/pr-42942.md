---
number: 42942
type: pull_request
state: MERGED
merged_at: 2022-08-15T06:36:12Z
url: https://github.com/WordPress/gutenberg/pull/42942
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","First-time Contributor"]
---

# PR #42942: ComboboxControl: Normalize hyphen-like unicode characters to ASCII hyphens when matching search queries

- URL: https://github.com/WordPress/gutenberg/pull/42942
- Author: markbiek
- Created: 2022-08-03T15:30:23Z
- Updated: 2022-08-18T16:17:27Z
- Comments: 2 of 2
- Review threads: 5 of 5
- Changed files: 4 of 4
- Diff: ../pr-diffs/42942.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
When searching, treat hyphen-like characters as an ASCII hyphen (%2D).

## Why?
This is the implementation agreed upon in discussions on #41952. This improves the search but doesn't affect search result highlighting or cause any other accessibility regressions.

## How?
Following the [example outlined](https://github.com/WordPress/gutenberg/pull/41952#pullrequestreview-1037737092) by @ciampo , we have a list of hyphen-like characters that are replaced with an ASCII `-` when looking for search matches.

## Testing Instructions
Run
```
npm run test:unit ./packages/components/src/utils/test/strings.js
```
to verify string normalization is functioning properly.

## Screenshots or screencast <!-- if applicable -->

![image](https://user-images.githubusercontent.com/917632/182648024-75798a4c-cb2b-423d-977c-f7a1e1bab980.png)

![image](https://user-images.githubusercontent.com/917632/182648041-9c0eb230-6d40-48d7-a6ab-cd1ddd8a6980.png)

## Issue comments

### github-actions on 2022-08-03T15:31:28Z

URL: https://github.com/WordPress/gutenberg/pull/42942#issuecomment-1204113016

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @markbiek! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/), where we hold [regularly weekly meetings](https://make.wordpress.org/core/tag/core-editor-summary/) open to anyone to coordinate with each other.

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### github-actions on 2022-08-15T06:36:35Z

URL: https://github.com/WordPress/gutenberg/pull/42942#issuecomment-1214670996

Congratulations on your first merged pull request, @markbiek! We'd like to credit you for your contribution in the post announcing the next WordPress release, but we can't find a WordPress.org profile associated with your GitHub account. When you have a moment, visit the following URL and click "link your GitHub account" under "GitHub Username" to link your accounts:

https://profiles.wordpress.org/me/profile/edit/

And if you don't have a WordPress.org account, you can create one on this page:

https://login.wordpress.org/register

Kudos!

## Review threads

### Review thread 1 (resolved)

#### mirka on 2022-08-09T13:06:06Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r941315896
Path: packages/components/src/combobox-control/index.js

```diff
@@ -33,6 +33,74 @@ import { useControlledValue } from '../utils/hooks';
 
 const noop = () => {};
 
+const ALL_UNICODE_DASH_CHARACTERS = new RegExp(
+	`[${ [
+		// - (hyphen-minus)
+		'\u002d',
+		// ~ (tilde)
+		'\u007e',
+		// ­ (soft hyphen)
+		'\u00ad',
+		// ֊ (armenian hyphen)
+		'\u058a',
+		// ־ (hebrew punctuation maqaf)
+		'\u05be',
+		// ᐀ (canadian syllabics hyphen)
+		'\u1400',
+		// ᠆ (mongolian todo soft hyphen)
+		'\u1806',
+		// ‐ (hyphen)
+		'\u2010',
+		// non-breaking hyphen)
+		'\u2011',
+		// ‒ (figure dash)
+		'\u2012',
+		// – (en dash)
+		'\u2013',
+		// — (em dash)
+		'\u2014',
+		// ― (horizontal bar)
+		'\u2015',
+		// ⁓ (swung dash)
+		'\u2053',
+		// superscript minus)
+		'\u207b',
+		// subscript minus)
+		'\u208b',
+		// − (minus sign)
+		'\u2212',
+		// ⸗ (double oblique hyphen)
+		'\u2e17',
+		// ⸺ (two-em dash)
+		'\u2e3a',
+		// ⸻ (three-em dash)
+		'\u2e3b',
+		// 〜 (wave dash)
+		'\u301c',
+		// 〰 (wavy dash)
+		'\u3030',
+		// ゠ (katakana-hiragana double hyphen)
+		'\u30a0',
+		// ︱ (presentation form for vertical em dash)
+		'\ufe31',
+		// ︲ (presentation form for vertical en dash)
+		'\ufe32',
+		// ﹘ (small em dash)
+		'\ufe58',
+		// ﹣ (small hyphen-minus)
+		'\ufe63',
+		// － (fullwidth hyphen-minus)
+		'\uff0d',
+	].join( '' ) }]`,
+	'g'
+);
+
+const normalizeTextString = ( value ) => {
+	return removeAccents( value )
+		.toLocaleLowerCase()
+		.replace( ALL_UNICODE_DASH_CHARACTERS, '-' );
+};
```

Feel free to extract this stuff out to a utils file if you want.

#### ciampo on 2022-08-11T07:18:58Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r943167878
Path: packages/components/src/combobox-control/index.js

```diff
@@ -33,6 +33,74 @@ import { useControlledValue } from '../utils/hooks';
 
 const noop = () => {};
 
+const ALL_UNICODE_DASH_CHARACTERS = new RegExp(
+	`[${ [
+		// - (hyphen-minus)
+		'\u002d',
+		// ~ (tilde)
+		'\u007e',
+		// ­ (soft hyphen)
+		'\u00ad',
+		// ֊ (armenian hyphen)
+		'\u058a',
+		// ־ (hebrew punctuation maqaf)
+		'\u05be',
+		// ᐀ (canadian syllabics hyphen)
+		'\u1400',
+		// ᠆ (mongolian todo soft hyphen)
+		'\u1806',
+		// ‐ (hyphen)
+		'\u2010',
+		// non-breaking hyphen)
+		'\u2011',
+		// ‒ (figure dash)
+		'\u2012',
+		// – (en dash)
+		'\u2013',
+		// — (em dash)
+		'\u2014',
+		// ― (horizontal bar)
+		'\u2015',
+		// ⁓ (swung dash)
+		'\u2053',
+		// superscript minus)
+		'\u207b',
+		// subscript minus)
+		'\u208b',
+		// − (minus sign)
+		'\u2212',
+		// ⸗ (double oblique hyphen)
+		'\u2e17',
+		// ⸺ (two-em dash)
+		'\u2e3a',
+		// ⸻ (three-em dash)
+		'\u2e3b',
+		// 〜 (wave dash)
+		'\u301c',
+		// 〰 (wavy dash)
+		'\u3030',
+		// ゠ (katakana-hiragana double hyphen)
+		'\u30a0',
+		// ︱ (presentation form for vertical em dash)
+		'\ufe31',
+		// ︲ (presentation form for vertical en dash)
+		'\ufe32',
+		// ﹘ (small em dash)
+		'\ufe58',
+		// ﹣ (small hyphen-minus)
+		'\ufe63',
+		// － (fullwidth hyphen-minus)
+		'\uff0d',
+	].join( '' ) }]`,
+	'g'
+);
+
+const normalizeTextString = ( value ) => {
+	return removeAccents( value )
+		.toLocaleLowerCase()
+		.replace( ALL_UNICODE_DASH_CHARACTERS, '-' );
+};
```

Should we also add some unit tests for the string normalization process?

### Review thread 2 (resolved)

#### mirka on 2022-08-09T13:06:56Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r941316768
Path: packages/components/src/combobox-control/index.js

```diff
@@ -82,11 +150,9 @@ function ComboboxControl( {
 	const matchingSuggestions = useMemo( () => {
 		const startsWithMatch = [];
 		const containsMatch = [];
-		const match = removeAccents( inputValue.toLocaleLowerCase() );
+		const match = normalizeTextString( inputValue );
```

Really tasteful refactor here, thank you 💛

### Review thread 3 (resolved)

#### mirka on 2022-08-09T13:30:18Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r941343172
Path: packages/components/src/combobox-control/stories/index.js

```diff
@@ -287,3 +287,39 @@ _default.args = {
 	__next36pxDefaultSize: false,
 	allowReset: false,
 };
+
+const NormalizedHyphensComboboxControl = () => {
+	const [ value, setValue ] = useState( null );
```

Our Storybook is still quite a mixed bag, but we are moving in a direction where we try to highlight practical usage examples, rather than treating it as a place to keep testing fixtures. And as much as I like this story, I feel like this normalization feature is a bit too specific to dedicate a story to. How about we move it to a unit test, instead of a story? Doesn't have to be comprehensive, just some assertion of the expected normalization behavior like:

```js
it( 'should normalize hyphen-like characters to hyphens', () => {
  expect( normalizeTextString( 'foo~bar' ).toBe( 'foo-bar' );
};
```

I wish the `matchingSuggestions` would be refactored to a pure function so that whole logic could be unit tested, but that's for another occasion 😄

### Review thread 4 (resolved)

#### ciampo on 2022-08-11T15:52:42Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r943649245
Path: packages/components/src/utils/strings.js

```diff
@@ -0,0 +1,73 @@
+/**
+ * External dependencies
+ */
+import removeAccents from 'remove-accents';
+
+const ALL_UNICODE_DASH_CHARACTERS = new RegExp(
+	`[${ [
+		// - (hyphen-minus)
+		'\u002d',
+		// ~ (tilde)
+		'\u007e',
+		// ­ (soft hyphen)
+		'\u00ad',
+		// ֊ (armenian hyphen)
+		'\u058a',
+		// ־ (hebrew punctuation maqaf)
+		'\u05be',
+		// ᐀ (canadian syllabics hyphen)
+		'\u1400',
+		// ᠆ (mongolian todo soft hyphen)
+		'\u1806',
+		// ‐ (hyphen)
+		'\u2010',
+		// non-breaking hyphen)
+		'\u2011',
+		// ‒ (figure dash)
+		'\u2012',
+		// – (en dash)
+		'\u2013',
+		// — (em dash)
+		'\u2014',
+		// ― (horizontal bar)
+		'\u2015',
+		// ⁓ (swung dash)
+		'\u2053',
+		// superscript minus)
+		'\u207b',
+		// subscript minus)
+		'\u208b',
+		// − (minus sign)
+		'\u2212',
+		// ⸗ (double oblique hyphen)
+		'\u2e17',
+		// ⸺ (two-em dash)
+		'\u2e3a',
+		// ⸻ (three-em dash)
+		'\u2e3b',
+		// 〜 (wave dash)
+		'\u301c',
+		// 〰 (wavy dash)
+		'\u3030',
+		// ゠ (katakana-hiragana double hyphen)
+		'\u30a0',
+		// ︱ (presentation form for vertical em dash)
+		'\ufe31',
+		// ︲ (presentation form for vertical en dash)
+		'\ufe32',
+		// ﹘ (small em dash)
+		'\ufe58',
+		// ﹣ (small hyphen-minus)
+		'\ufe63',
+		// － (fullwidth hyphen-minus)
+		'\uff0d',
+	].join( '' ) }]`,
+	'g'
+);
+
+// @ts-ignore
```

Best way here would be to change the extension to `.ts` and add type annotations to the function.

Alternatively, adding a JSDoc comment should suffice

```suggestion
/**
 * Normalizes a string by removing accents, converting to lowercase, and
 * swapping all types of hyphen-like characters with the standard "-" character.
 *
 * @param {string} value The string to normalize
 * @return {string} The normalized string
 */
```

#### markbiek on 2022-08-11T15:55:42Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r943652372
Path: packages/components/src/utils/strings.js

```diff
@@ -0,0 +1,73 @@
+/**
+ * External dependencies
+ */
+import removeAccents from 'remove-accents';
+
+const ALL_UNICODE_DASH_CHARACTERS = new RegExp(
+	`[${ [
+		// - (hyphen-minus)
+		'\u002d',
+		// ~ (tilde)
+		'\u007e',
+		// ­ (soft hyphen)
+		'\u00ad',
+		// ֊ (armenian hyphen)
+		'\u058a',
+		// ־ (hebrew punctuation maqaf)
+		'\u05be',
+		// ᐀ (canadian syllabics hyphen)
+		'\u1400',
+		// ᠆ (mongolian todo soft hyphen)
+		'\u1806',
+		// ‐ (hyphen)
+		'\u2010',
+		// non-breaking hyphen)
+		'\u2011',
+		// ‒ (figure dash)
+		'\u2012',
+		// – (en dash)
+		'\u2013',
+		// — (em dash)
+		'\u2014',
+		// ― (horizontal bar)
+		'\u2015',
+		// ⁓ (swung dash)
+		'\u2053',
+		// superscript minus)
+		'\u207b',
+		// subscript minus)
+		'\u208b',
+		// − (minus sign)
+		'\u2212',
+		// ⸗ (double oblique hyphen)
+		'\u2e17',
+		// ⸺ (two-em dash)
+		'\u2e3a',
+		// ⸻ (three-em dash)
+		'\u2e3b',
+		// 〜 (wave dash)
+		'\u301c',
+		// 〰 (wavy dash)
+		'\u3030',
+		// ゠ (katakana-hiragana double hyphen)
+		'\u30a0',
+		// ︱ (presentation form for vertical em dash)
+		'\ufe31',
+		// ︲ (presentation form for vertical en dash)
+		'\ufe32',
+		// ﹘ (small em dash)
+		'\ufe58',
+		// ﹣ (small hyphen-minus)
+		'\ufe63',
+		// － (fullwidth hyphen-minus)
+		'\uff0d',
+	].join( '' ) }]`,
+	'g'
+);
+
+// @ts-ignore
```

I went ahead and switched it to a `.ts` file. Thanks!

### Review thread 5 (resolved)

#### mirka on 2022-08-11T17:23:12Z

URL: https://github.com/WordPress/gutenberg/pull/42942#discussion_r943737760
Path: packages/components/CHANGELOG.md

```diff
@@ -18,6 +18,7 @@
 
 ### Enhancements
 
+- `ComboboxControl`: Normalize hyphen-like characters to an ASCII hyphn ([#42942](https://github.com/WordPress/gutenberg/pull/42942))
```

```suggestion
- `ComboboxControl`: Normalize hyphen-like characters to an ASCII hyphen ([#42942](https://github.com/WordPress/gutenberg/pull/42942))
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/combobox-control/index.js (MODIFIED, +3/-5)
- packages/components/src/utils/strings.ts (ADDED, +72/-0)
- packages/components/src/utils/test/strings.js (ADDED, +15/-0)
