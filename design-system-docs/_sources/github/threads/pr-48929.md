---
number: 48929
type: pull_request
state: MERGED
merged_at: 2023-03-17T15:32:35Z
url: https://github.com/WordPress/gutenberg/pull/48929
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Type] Code Quality","[Package] Components","[Feature] Component System"]
---

# PR #48929: CustomGradientPicker: Refactor to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/48929
- Author: chad1008
- Created: 2023-03-08T14:38:19Z
- Updated: 2023-03-17T15:33:50Z
- Comments: 6 of 6
- Review threads: 30 of 30
- Changed files: 21 of 21
- Diff: ../pr-diffs/48929.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Refactor `CustomGradientPicker` component to TypeScript

Part of https://github.com/WordPress/gutenberg/issues/35744

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
The refactor to TypeScript has many benefits (auto-generated docs, static linting and error prevention, better IDE experience). See https://github.com/WordPress/gutenberg/issues/35744 for more details

## How?
Followed the steps in the [TypeScript migration guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md?rgh-link-date=2022-11-29T14%3A45%3A34Z#refactoring-a-component-to-typescript)

## Testing Instructions
- After applying the patch, run `npm ci`, as a new `DefinitelyTyped` package has been added for the `gradientParser` library this component relies on.
- Ensure all unit and e2e tests still pass.
- In Storybook confirm the component functions as expected and docs tab is loading properly.
- In the editor, test a gradient picker (e.g. on a background color) to ensure it functions as expected.

## Notes:
- There were some interesting challenges with the `DefinitelyTyped` values for the `gradientParser` library. This led to what felt like pretty liberal use of type assertions. Many of these were to avoid conflicts between union members. Anyway, I know there's a bunch of them and I'm very open to feedback them... so anywhere you see me using an `as`, please take it with a grain of salt and don't hesitate to redirect me if you see a better option.
- I've left inline comments where I felt they'd help clarify what I was doing/thinking at the time, but this one felt like it had more moving parts than I'm used to, so let me know if anything seems off.

## Issue comments

### github-actions on 2023-03-08T14:48:10Z

URL: https://github.com/WordPress/gutenberg/pull/48929#issuecomment-1460274159

**Size Change:** +655 B (0%) 

**Total Size:** 1.34 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/block-editor/index.min.js` | 197 kB | +83 B (0%) |
| `build/block-editor/style-rtl.css` | 14.4 kB | -5 B (0%) |
| `build/block-editor/style.css` | 14.4 kB | -3 B (0%) |
| `build/block-library/index.min.js` | 201 kB | +172 B (0%) |
| `build/components/index.min.js` | 208 kB | +68 B (0%) |
| `build/compose/index.min.js` | 12.4 kB | +48 B (0%) |
| `build/edit-site/index.min.js` | 65.3 kB | +292 B (0%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 993 B |
| `build/annotations/index.min.js` | 2.78 kB |
| `build/api-fetch/index.min.js` | 2.27 kB |
| `build/autop/index.min.js` | 2.15 kB |
| `build/blob/index.min.js` | 483 B |
| `build/block-directory/index.min.js` | 7.2 kB |
| `build/block-directory/style-rtl.css` | 1.04 kB |
| `build/block-directory/style.css` | 1.04 kB |
| `build/block-editor/content-rtl.css` | 4.11 kB |
| `build/block-editor/content.css` | 4.1 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 403 B |
| `build/block-editor/default-editor-styles.css` | 403 B |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 138 B |
| `build/block-library/blocks/audio/theme.css` | 138 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 91 B |
| `build/block-library/blocks/avatar/style.css` | 91 B |
| `build/block-library/blocks/block/editor-rtl.css` | 305 B |
| `build/block-library/blocks/block/editor.css` | 305 B |
| `build/block-library/blocks/button/editor-rtl.css` | 587 B |
| `build/block-library/blocks/button/editor.css` | 587 B |
| `build/block-library/blocks/button/style-rtl.css` | 628 B |
| `build/block-library/blocks/button/style.css` | 627 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 84 B |
| `build/block-library/blocks/categories/editor.css` | 83 B |
| `build/block-library/blocks/categories/style-rtl.css` | 100 B |
| `build/block-library/blocks/categories/style.css` | 100 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 406 B |
| `build/block-library/blocks/columns/style.css` | 406 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 612 B |
| `build/block-library/blocks/cover/editor.css` | 613 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.6 kB |
| `build/block-library/blocks/cover/style.css` | 1.59 kB |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 138 B |
| `build/block-library/blocks/embed/theme.css` | 138 B |
| `build/block-library/blocks/file/editor-rtl.css` | 300 B |
| `build/block-library/blocks/file/editor.css` | 300 B |
| `build/block-library/blocks/file/style-rtl.css` | 265 B |
| `build/block-library/blocks/file/style.css` | 265 B |
| `build/block-library/blocks/file/view.min.js` | 353 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.44 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.44 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 984 B |
| `build/block-library/blocks/gallery/editor.css` | 988 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.55 kB |
| `build/block-library/blocks/gallery/style.css` | 1.55 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 654 B |
| `build/block-library/blocks/group/editor.css` | 654 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 76 B |
| `build/block-library/blocks/heading/style.css` | 76 B |
| `build/block-library/blocks/html/editor-rtl.css` | 332 B |
| `build/block-library/blocks/html/editor.css` | 333 B |
| `build/block-library/blocks/image/editor-rtl.css` | 830 B |
| `build/block-library/blocks/image/editor.css` | 829 B |
| `build/block-library/blocks/image/style-rtl.css` | 652 B |
| `build/block-library/blocks/image/style.css` | 652 B |
| `build/block-library/blocks/image/theme-rtl.css` | 137 B |
| `build/block-library/blocks/image/theme.css` | 137 B |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 357 B |
| `build/block-library/blocks/latest-comments/style.css` | 357 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 213 B |
| `build/block-library/blocks/latest-posts/editor.css` | 212 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 478 B |
| `build/block-library/blocks/latest-posts/style.css` | 478 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 507 B |
| `build/block-library/blocks/media-text/style.css` | 505 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 716 B |
| `build/block-library/blocks/navigation-link/editor.css` | 715 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.13 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.14 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.22 kB |
| `build/block-library/blocks/navigation/style.css` | 2.2 kB |
| `build/block-library/blocks/navigation/view-modal.min.js` | 2.81 kB |
| `build/block-library/blocks/navigation/view.min.js` | 447 B |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 401 B |
| `build/block-library/blocks/page-list/editor.css` | 401 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 174 B |
| `build/block-library/blocks/paragraph/editor.css` | 174 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 279 B |
| `build/block-library/blocks/paragraph/style.css` | 281 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 501 B |
| `build/block-library/blocks/post-comments-form/style.css` | 501 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 71 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 71 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 134 B |
| `build/block-library/blocks/post-excerpt/style.css` | 134 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 586 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 584 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 322 B |
| `build/block-library/blocks/post-featured-image/style.css` | 322 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 153 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 153 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 282 B |
| `build/block-library/blocks/post-template/style.css` | 282 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 103 B |
| `build/block-library/blocks/preformatted/style.css` | 103 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 326 B |
| `build/block-library/blocks/pullquote/style.css` | 325 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 167 B |
| `build/block-library/blocks/pullquote/theme.css` | 167 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 288 B |
| `build/block-library/blocks/query-pagination/style.css` | 284 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 463 B |
| `build/block-library/blocks/query/editor.css` | 463 B |
| `build/block-library/blocks/quote/style-rtl.css` | 222 B |
| `build/block-library/blocks/quote/style.css` | 222 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 149 B |
| `build/block-library/blocks/rss/editor.css` | 149 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 165 B |
| `build/block-library/blocks/search/editor.css` | 165 B |
| `build/block-library/blocks/search/style-rtl.css` | 409 B |
| `build/block-library/blocks/search/style.css` | 406 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 234 B |
| `build/block-library/blocks/separator/style.css` | 234 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 474 B |
| `build/block-library/blocks/shortcode/editor.css` | 474 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 489 B |
| `build/block-library/blocks/site-logo/editor.css` | 489 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 203 B |
| `build/block-library/blocks/site-logo/style.css` | 203 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 116 B |
| `build/block-library/blocks/site-title/editor.css` | 116 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 57 B |
| `build/block-library/blocks/site-title/style.css` | 57 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 184 B |
| `build/block-library/blocks/social-link/editor.css` | 184 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 674 B |
| `build/block-library/blocks/social-links/editor.css` | 673 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.4 kB |
| `build/block-library/blocks/social-links/style.css` | 1.39 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 332 B |
| `build/block-library/blocks/spacer/editor.css` | 332 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 433 B |
| `build/block-library/blocks/table/editor.css` | 433 B |
| `build/block-library/blocks/table/style-rtl.css` | 651 B |
| `build/block-library/blocks/table/style.css` | 650 B |
| `build/block-library/blocks/table/theme-rtl.css` | 157 B |
| `build/block-library/blocks/table/theme.css` | 157 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 251 B |
| `build/block-library/blocks/tag-cloud/style.css` | 253 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 404 B |
| `build/block-library/blocks/template-part/editor.css` | 404 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 179 B |
| `build/block-library/blocks/video/style.css` | 179 B |
| `build/block-library/blocks/video/theme-rtl.css` | 139 B |
| `build/block-library/blocks/video/theme.css` | 139 B |
| `build/block-library/classic-rtl.css` | 179 B |
| `build/block-library/classic.css` | 179 B |
| `build/block-library/common-rtl.css` | 1.11 kB |
| `build/block-library/common.css` | 1.11 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 11.6 kB |
| `build/block-library/editor.css` | 11.6 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/reset-rtl.css` | 478 B |
| `build/block-library/reset.css` | 478 B |
| `build/block-library/style-rtl.css` | 12.7 kB |
| `build/block-library/style.css` | 12.7 kB |
| `build/block-library/theme-rtl.css` | 698 B |
| `build/block-library/theme.css` | 703 B |
| `build/block-serialization-default-parser/index.min.js` | 1.13 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.83 kB |
| `build/blocks/index.min.js` | 51 kB |
| `build/components/style-rtl.css` | 11.7 kB |
| `build/components/style.css` | 11.7 kB |
| `build/core-data/index.min.js` | 16.3 kB |
| `build/customize-widgets/index.min.js` | 12.2 kB |
| `build/customize-widgets/style-rtl.css` | 1.41 kB |
| `build/customize-widgets/style.css` | 1.41 kB |
| `build/data-controls/index.min.js` | 663 B |
| `build/data/index.min.js` | 8.58 kB |
| `build/date/index.min.js` | 40.4 kB |
| `build/deprecated/index.min.js` | 518 B |
| `build/dom-ready/index.min.js` | 336 B |
| `build/dom/index.min.js` | 4.72 kB |
| `build/edit-post/classic-rtl.css` | 571 B |
| `build/edit-post/classic.css` | 571 B |
| `build/edit-post/index.min.js` | 34.8 kB |
| `build/edit-post/style-rtl.css` | 7.55 kB |
| `build/edit-post/style.css` | 7.54 kB |
| `build/edit-site/style-rtl.css` | 10.2 kB |
| `build/edit-site/style.css` | 10.2 kB |
| `build/edit-widgets/index.min.js` | 17.3 kB |
| `build/edit-widgets/style-rtl.css` | 4.56 kB |
| `build/edit-widgets/style.css` | 4.56 kB |
| `build/editor/index.min.js` | 45.8 kB |
| `build/editor/style-rtl.css` | 3.54 kB |
| `build/editor/style.css` | 3.53 kB |
| `build/element/index.min.js` | 4.95 kB |
| `build/escape-html/index.min.js` | 548 B |
| `build/format-library/index.min.js` | 7.26 kB |
| `build/format-library/style-rtl.css` | 557 B |
| `build/format-library/style.css` | 556 B |
| `build/hooks/index.min.js` | 1.66 kB |
| `build/html-entities/index.min.js` | 454 B |
| `build/i18n/index.min.js` | 3.79 kB |
| `build/is-shallow-equal/index.min.js` | 535 B |
| `build/keyboard-shortcuts/index.min.js` | 1.79 kB |
| `build/keycodes/index.min.js` | 1.94 kB |
| `build/list-reusable-blocks/index.min.js` | 2.14 kB |
| `build/list-reusable-blocks/style-rtl.css` | 865 B |
| `build/list-reusable-blocks/style.css` | 865 B |
| `build/media-utils/index.min.js` | 2.99 kB |
| `build/notices/index.min.js` | 977 B |
| `build/plugins/index.min.js` | 1.95 kB |
| `build/preferences-persistence/index.min.js` | 2.23 kB |
| `build/preferences/index.min.js` | 1.35 kB |
| `build/primitives/index.min.js` | 960 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/private-apis/index.min.js` | 937 B |
| `build/react-i18n/index.min.js` | 702 B |
| `build/react-refresh-entry/index.min.js` | 8.44 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.75 kB |
| `build/reusable-blocks/index.min.js` | 2.26 kB |
| `build/reusable-blocks/style-rtl.css` | 265 B |
| `build/reusable-blocks/style.css` | 265 B |
| `build/rich-text/index.min.js` | 11 kB |
| `build/server-side-render/index.min.js` | 2.09 kB |
| `build/shortcode/index.min.js` | 1.52 kB |
| `build/style-engine/index.min.js` | 1.53 kB |
| `build/token-list/index.min.js` | 650 B |
| `build/url/index.min.js` | 3.74 kB |
| `build/vendors/inert-polyfill.min.js` | 2.48 kB |
| `build/vendors/react-dom.min.js` | 41.8 kB |
| `build/vendors/react.min.js` | 4.02 kB |
| `build/viewport/index.min.js` | 1.09 kB |
| `build/warning/index.min.js` | 280 B |
| `build/widgets/index.min.js` | 7.3 kB |
| `build/widgets/style-rtl.css` | 1.18 kB |
| `build/widgets/style.css` | 1.18 kB |
| `build/wordcount/index.min.js` | 1.06 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### chad1008 on 2023-03-14T16:38:22Z

URL: https://github.com/WordPress/gutenberg/pull/48929#issuecomment-1468444787

Whew, thanks for all of the detailed feedback, as always @ciampo.

I think I've addressed everything. There are a couple of unresolved conversations in the thread I had questions or needed feedback on.

Beyond that, the conflicts listed are updating the CHANGELOG and addressing some added `@ts-nocheck` comments that were added in #47764. Once we've resolved other open feedback I'll rebase those in and remove them.

### chad1008 on 2023-03-15T21:10:46Z

URL: https://github.com/WordPress/gutenberg/pull/48929#issuecomment-1470848637

Rebased and incorporated changes. I did need to make one small change (7595d97dcc2569332eed97b6b2a99b1677bc2bd4) to the new types in `PaletteEdit`, which treated the gradient string as a required value, when it's actually optional. `CustomGradientPicker` will apply a default gradient if one isn't provided.

### github-actions on 2023-03-16T17:01:28Z

URL: https://github.com/WordPress/gutenberg/pull/48929#issuecomment-1472359577

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 465a59d285bfd7b0ca91f0a11a9794dcdaa26c12.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/4448835977
📝  Reported issues:
- #49025 in `/test/e2e/specs/site-editor/writing-flow.spec.js`
- #35176 in `specs/editor/various/autosave.test.js`

### chad1008 on 2023-03-16T17:33:47Z

URL: https://github.com/WordPress/gutenberg/pull/48929#issuecomment-1472416156

Applied most recent feedback. I believe there is only [one remaining open item](https://github.com/WordPress/gutenberg/pull/48929#discussion_r1139017342).

I did introduce one additional [small change](https://github.com/WordPress/gutenberg/pull/48929/commits/8f9e1887b43b5333daab9d336422d05b8a0df081). tl;dr: `CustomGradientBar` was treating `__experimentalIsRenderedInSidebar` as required, when it shouldn't be, and isn't provided by consumers like `DuotonPicker` so it's now optional with a default of `false`, which aligns with the main `CustomGradientPicker` component.

### chad1008 on 2023-03-17T15:03:09Z

URL: https://github.com/WordPress/gutenberg/pull/48929#issuecomment-1473979193

Last change implemented, waiting on CI before merging...

## Review threads

### Review thread 1 (resolved)

#### chad1008 on 2023-03-08T14:52:46Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129564807
Path: packages/components/src/custom-gradient-picker/gradient-bar/index.tsx

```diff
@@ -74,19 +86,27 @@ export default function CustomGradientBar( {
 	disableInserter = false,
 	disableAlpha = false,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const gradientMarkersContainerDomRef = useRef();
+}: CustomGradientBarProps ) {
+	const gradientMarkersContainerDomRef: LegacyRef< HTMLDivElement > =
+		useRef( null );
 
 	const [ gradientBarState, gradientBarStateDispatch ] = useReducer(
 		customGradientBarReducer,
 		customGradientBarReducerInitialState
 	);
-	const onMouseEnterAndMove = ( event ) => {
+	const onMouseEnterAndMove: MouseEventHandler< HTMLDivElement > = (
+		event
+	) => {
 		const insertPosition = getHorizontalRelativeGradientPosition(
 			event.clientX,
 			gradientMarkersContainerDomRef.current
 		);
 
+		// NTS: `getHorizontalRelativeGradientPosition` will return `undefined` if the `containerElement` arg is falsey.
+		if ( insertPosition === undefined ) {
+			return;
+		}
```

`getHorizontalRelativeGradientPosition` will return `undefined` if the `containerElement` arg is falsey

### Review thread 2 (resolved)

#### chad1008 on 2023-03-08T14:54:18Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129567264
Path: packages/components/src/custom-gradient-picker/test/serializer.ts

```diff
@@ -12,24 +12,31 @@ import {
 describe( 'It should serialize a gradient', () => {
 	test( 'serializeGradientColor', () => {
 		expect(
-			serializeGradientColor( { type: 'rgba', value: [ 1, 2, 3, 0.5 ] } )
+			serializeGradientColor( {
+				type: 'rgba',
+				//NTS: For entire file - our tests were using numbers, but `gradientParser` (according to DefinitelyTyped) expects strings. I've updated the tests accordingly. Alternatively we could add some logic to our serializer so it accepts both and converts numbers to strings on the fly.
+				value: [ '1', '2', '3', '0.5' ],
```

Note for this entire file: our tests were using numbers, but `gradientParser` (according to DefinitelyTyped) expects strings. I've updated the tests accordingly. Alternatively we could add some logic to our serializer so it accepts both and converts numbers to strings on the fly.

#### ciampo on 2023-03-09T19:59:48Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131529069
Path: packages/components/src/custom-gradient-picker/test/serializer.ts

```diff
@@ -12,24 +12,31 @@ import {
 describe( 'It should serialize a gradient', () => {
 	test( 'serializeGradientColor', () => {
 		expect(
-			serializeGradientColor( { type: 'rgba', value: [ 1, 2, 3, 0.5 ] } )
+			serializeGradientColor( {
+				type: 'rgba',
+				//NTS: For entire file - our tests were using numbers, but `gradientParser` (according to DefinitelyTyped) expects strings. I've updated the tests accordingly. Alternatively we could add some logic to our serializer so it accepts both and converts numbers to strings on the fly.
+				value: [ '1', '2', '3', '0.5' ],
```

Are these changes (from number to string) completely internal to the component, or do they affect the way consumers of `CustomGradientPicker` interact with it?

#### chad1008 on 2023-03-09T20:33:36Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131560865
Path: packages/components/src/custom-gradient-picker/test/serializer.ts

```diff
@@ -12,24 +12,31 @@ import {
 describe( 'It should serialize a gradient', () => {
 	test( 'serializeGradientColor', () => {
 		expect(
-			serializeGradientColor( { type: 'rgba', value: [ 1, 2, 3, 0.5 ] } )
+			serializeGradientColor( {
+				type: 'rgba',
+				//NTS: For entire file - our tests were using numbers, but `gradientParser` (according to DefinitelyTyped) expects strings. I've updated the tests accordingly. Alternatively we could add some logic to our serializer so it accepts both and converts numbers to strings on the fly.
+				value: [ '1', '2', '3', '0.5' ],
```

Internal. These changes are strictly part of a unit test, and the tests themselves are written specifically internal serialization functions that don't get called by a consumer.

#### ciampo on 2023-03-10T09:36:22Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1132149621
Path: packages/components/src/custom-gradient-picker/test/serializer.ts

```diff
@@ -12,24 +12,31 @@ import {
 describe( 'It should serialize a gradient', () => {
 	test( 'serializeGradientColor', () => {
 		expect(
-			serializeGradientColor( { type: 'rgba', value: [ 1, 2, 3, 0.5 ] } )
+			serializeGradientColor( {
+				type: 'rgba',
+				//NTS: For entire file - our tests were using numbers, but `gradientParser` (according to DefinitelyTyped) expects strings. I've updated the tests accordingly. Alternatively we could add some logic to our serializer so it accepts both and converts numbers to strings on the fly.
+				value: [ '1', '2', '3', '0.5' ],
```

If they're internal, we should be OK.

### Review thread 3 (resolved)

#### chad1008 on 2023-03-08T14:56:16Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129570399
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -68,7 +83,8 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 					? {}
 					: { orientation: HORIZONTAL_GRADIENT_ORIENTATION } ),
 				type: 'linear-gradient',
-			} )
+				// NTS: this isn't very elegant, but the `orientation` props between linear and radial gradients don't overlap (according to DefinitelyTyped) so TypeScript gets confused by this manual change.
+			} as gradientParser.LinearGradientNode )
```

This doesn't feel very elegant, but the `orientation` props between linear and radial gradients don't overlap (according to DefinitelyTyped) so TypeScript gets confused by this manual change.

#### ciampo on 2023-03-09T19:01:58Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131470553
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -68,7 +83,8 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 					? {}
 					: { orientation: HORIZONTAL_GRADIENT_ORIENTATION } ),
 				type: 'linear-gradient',
-			} )
+				// NTS: this isn't very elegant, but the `orientation` props between linear and radial gradients don't overlap (according to DefinitelyTyped) so TypeScript gets confused by this manual change.
+			} as gradientParser.LinearGradientNode )
```

Yup, adding this type cast actually hides the real problem: `HORIZONTAL_GRADIENT_ORIENTATION` does not match the expected type for the `orientation` property.

We'd like it to be of type `AngularNode`, but TS currently infers it as `{ type: string; value: number }`, which poses two issues:

- `type` is not narrow enough
- `value`, according to the `gradient-parser` types, should be a `string`, not a `number`

Furthermore, the way the `orientation` prop is re-assigned to the linear gradient object, makes it impossible for TS to understand what's going on.

<details>
<summary>Here's how we could apply a few tweaks and improve the correctness of the code, while making TS happy</summary>

```diff
diff --git a/packages/components/src/custom-gradient-picker/constants.ts b/packages/components/src/custom-gradient-picker/constants.ts
index 9924ff2b3d..842aea9dcb 100644
--- a/packages/components/src/custom-gradient-picker/constants.ts
+++ b/packages/components/src/custom-gradient-picker/constants.ts
@@ -10,8 +10,8 @@ export const DEFAULT_LINEAR_GRADIENT_ANGLE = 180;
 
 export const HORIZONTAL_GRADIENT_ORIENTATION = {
 	type: 'angular',
-	value: 90,
-};
+	value: '90',
+} as const;
 
 export const GRADIENT_OPTIONS = [
 	{ value: 'linear-gradient', label: __( 'Linear' ) },
diff --git a/packages/components/src/custom-gradient-picker/index.tsx b/packages/components/src/custom-gradient-picker/index.tsx
index 8c9b9c7908..a8ffb86e6f 100644
--- a/packages/components/src/custom-gradient-picker/index.tsx
+++ b/packages/components/src/custom-gradient-picker/index.tsx
@@ -76,14 +76,15 @@ const GradientTypePicker = ( {
 	const { type } = gradientAST;
 
 	const onSetLinearGradient = () => {
+		const { orientation, ...restGradientAST } = gradientAST;
 		onChange(
 			serializeGradient( {
-				...gradientAST,
-				...( gradientAST.orientation
-					? {}
-					: { orientation: HORIZONTAL_GRADIENT_ORIENTATION } ),
+				...restGradientAST,
+				orientation: gradientAST.orientation
+					? undefined
+					: HORIZONTAL_GRADIENT_ORIENTATION,
 				type: 'linear-gradient',
-			} as gradientParser.LinearGradientNode )
+			} )
 		);
 	};
 
```
</details>

If we wanted to be more explicit, we could also type `HORIZONTAL_GRADIENT_ORIENTATION` as an `AngularNode`, instead of using `as const` — up to you!

#### chad1008 on 2023-03-09T21:32:13Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131631736
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -68,7 +83,8 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 					? {}
 					: { orientation: HORIZONTAL_GRADIENT_ORIENTATION } ),
 				type: 'linear-gradient',
-			} )
+				// NTS: this isn't very elegant, but the `orientation` props between linear and radial gradients don't overlap (according to DefinitelyTyped) so TypeScript gets confused by this manual change.
+			} as gradientParser.LinearGradientNode )
```

> `value`, according to the `gradient-parser` types, should be a `string`, not a `number`

Shoot - I remember noticing that and then I must have lost track of it while testing different solutions. Good catch, thank you for highlighting it.

> Here's how we could apply a few tweaks and improve the correctness of the code, while making TS happy

Thanks for the diff, very helpful! A question for my own education... Is the `const { orientation, ...restGradientAST } = gradientAST` destructuring, a functional change or more for readability?

<details>
<summary>I wondered if this would work as well as your diff, or if there's a nuance I didn't see:</summary>

```diff

diff --git a/packages/components/src/custom-gradient-picker/constants.ts b/packages/components/src/custom-gradient-picker/constants.ts
index 9924ff2b3d..842aea9dcb 100644
--- a/packages/components/src/custom-gradient-picker/constants.ts
+++ b/packages/components/src/custom-gradient-picker/constants.ts
@@ -10,8 +10,8 @@ export const DEFAULT_LINEAR_GRADIENT_ANGLE = 180;
 
 export const HORIZONTAL_GRADIENT_ORIENTATION = {
 	type: 'angular',
-	value: 90,
-};
+	value: '90',
+} as const;
 
 export const GRADIENT_OPTIONS = [
 	{ value: 'linear-gradient', label: __( 'Linear' ) },
diff --git a/packages/components/src/custom-gradient-picker/index.tsx b/packages/components/src/custom-gradient-picker/index.tsx
index 8c9b9c7908..e19ac3d55f 100644
--- a/packages/components/src/custom-gradient-picker/index.tsx
+++ b/packages/components/src/custom-gradient-picker/index.tsx
@@ -79,11 +79,11 @@ const GradientTypePicker = ( {
 		onChange(
 			serializeGradient( {
 				...gradientAST,
-				...( gradientAST.orientation
-					? {}
-					: { orientation: HORIZONTAL_GRADIENT_ORIENTATION } ),
+				orientation: gradientAST.orientation
+					? undefined
+					: HORIZONTAL_GRADIENT_ORIENTATION,
 				type: 'linear-gradient',
-			} as gradientParser.LinearGradientNode )
+			} )
 		);
 	};
 
```
</details>

#### ciampo on 2023-03-10T09:39:35Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1132152859
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -68,7 +83,8 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 					? {}
 					: { orientation: HORIZONTAL_GRADIENT_ORIENTATION } ),
 				type: 'linear-gradient',
-			} )
+				// NTS: this isn't very elegant, but the `orientation` props between linear and radial gradients don't overlap (according to DefinitelyTyped) so TypeScript gets confused by this manual change.
+			} as gradientParser.LinearGradientNode )
```

It was mostly for readability. Your proposed solution works well too and produces a smaller diff 🙌

### Review thread 4 (resolved)

#### chad1008 on 2023-03-08T14:56:56Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129571428
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -82,11 +98,13 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 		);
 	};
 
-	const handleOnChange = ( next ) => {
-		if ( next === 'linear-gradient' ) {
+	const handleOnChange = ( next: string | string[] ) => {
+		// NTS: Because `SelectControl.onChange` (below) can accept an array, we need to accept one here. We shouldn't ever see an array here, but I'm adding a check just in case.
+		const nextValue = Array.isArray( next ) ? next[ 0 ] : next;
```

Because `SelectControl.onChange` (below) can accept an array, we need to accept one here. I don't think we should ever actually see an array here, but I'm adding a check just in case.

#### ciampo on 2023-03-09T19:17:46Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131489682
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -82,11 +98,13 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 		);
 	};
 
-	const handleOnChange = ( next ) => {
-		if ( next === 'linear-gradient' ) {
+	const handleOnChange = ( next: string | string[] ) => {
+		// NTS: Because `SelectControl.onChange` (below) can accept an array, we need to accept one here. We shouldn't ever see an array here, but I'm adding a check just in case.
+		const nextValue = Array.isArray( next ) ? next[ 0 ] : next;
```

I recently improved types for `SelectControl` in #47390. Can you try to rebase and see if these changes are still necessary?

#### chad1008 on 2023-03-13T15:19:25Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1134151748
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -82,11 +98,13 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 		);
 	};
 
-	const handleOnChange = ( next ) => {
-		if ( next === 'linear-gradient' ) {
+	const handleOnChange = ( next: string | string[] ) => {
+		// NTS: Because `SelectControl.onChange` (below) can accept an array, we need to accept one here. We shouldn't ever see an array here, but I'm adding a check just in case.
+		const nextValue = Array.isArray( next ) ? next[ 0 ] : next;
```

Very nice, those changes made this much cleaner. Updated in cf33a070b4

### Review thread 5 (resolved)

#### chad1008 on 2023-03-08T14:57:26Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129572210
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -100,30 +118,58 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 			onChange={ handleOnChange }
 			options={ GRADIENT_OPTIONS }
 			size="__unstable-large"
-			value={ hasGradient && type }
+			// NTS: Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.
+			value={ hasGradient ? type : undefined }
```

Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.

### Review thread 6 (resolved)

#### chad1008 on 2023-03-08T14:59:40Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129575737
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -100,30 +118,58 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 			onChange={ handleOnChange }
 			options={ GRADIENT_OPTIONS }
 			size="__unstable-large"
-			value={ hasGradient && type }
+			// NTS: Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.
+			value={ hasGradient ? type : undefined }
 		/>
 	);
 };
 
-export default function CustomGradientPicker( {
+/**
+ * CustomGradientPicker is a React component that renders a UI for specifying
+ * linear or radial gradients. Radial gradients are displayed in the picker as
+ * a slice of the gradient from the center to the outside.
+ *
+ * ```jsx
+ * import { CustomGradientPicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyCustomGradientPicker = () => {
+ *   const [ gradient, setGradient ] = useState();
+ *
+ *   return (
+ *     <CustomGradientPicker
+ *			value={ gradient }
+ *			onChange={ setGradient }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function CustomGradientPicker( {
 	/** Start opting into the new margin-free styles that will become the default in a future version. */
 	__nextHasNoMargin = false,
 	value,
 	onChange,
-	__experimentalIsRenderedInSidebar,
-} ) {
-	const gradientAST = getGradientAstWithDefault( value );
+	__experimentalIsRenderedInSidebar = false,
+}: CustomGradientPickerProps ) {
+	const { gradientAST, gradientAstValue } =
+		getGradientAstWithDefault( value );
 	// On radial gradients the bar should display a linear gradient.
 	// On radial gradients the bar represents a slice of the gradient from the center until the outside.
 	// On liner gradients the bar represents the color stops from left to right independently of the angle.
 	const background = getLinearGradientRepresentation( gradientAST );
-	const hasGradient = gradientAST.value !== DEFAULT_GRADIENT;
+	const hasGradient = gradientAstValue !== DEFAULT_GRADIENT;
 	// Control points color option may be hex from presets, custom colors will be rgb.
 	// The position should always be a percentage.
-	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => ( {
-		color: getStopCssColor( colorStop ),
-		position: parseInt( colorStop.length.value ),
-	} ) );
+	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => {
+		// Although it's already been checked by `hasUnsupportedLength` in `getGradientAstWithDefault`,
+		// TypeScript doesn't know that `colorStop.length` is not undefined here.
+		//NTS: Is there may be a more elegant way to help TS see that for itself? 🤔
+		return {
+			color: getStopCssColor( colorStop ),
+			position: parseInt( colorStop.length!.value ),
```

Is there a more elegant way to help TS see for itself that `colorStop.length` is no longer undefined, and avoid using that `!`? 🤔

#### ciampo on 2023-03-09T19:35:23Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131505499
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -100,30 +118,58 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 			onChange={ handleOnChange }
 			options={ GRADIENT_OPTIONS }
 			size="__unstable-large"
-			value={ hasGradient && type }
+			// NTS: Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.
+			value={ hasGradient ? type : undefined }
 		/>
 	);
 };
 
-export default function CustomGradientPicker( {
+/**
+ * CustomGradientPicker is a React component that renders a UI for specifying
+ * linear or radial gradients. Radial gradients are displayed in the picker as
+ * a slice of the gradient from the center to the outside.
+ *
+ * ```jsx
+ * import { CustomGradientPicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyCustomGradientPicker = () => {
+ *   const [ gradient, setGradient ] = useState();
+ *
+ *   return (
+ *     <CustomGradientPicker
+ *			value={ gradient }
+ *			onChange={ setGradient }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function CustomGradientPicker( {
 	/** Start opting into the new margin-free styles that will become the default in a future version. */
 	__nextHasNoMargin = false,
 	value,
 	onChange,
-	__experimentalIsRenderedInSidebar,
-} ) {
-	const gradientAST = getGradientAstWithDefault( value );
+	__experimentalIsRenderedInSidebar = false,
+}: CustomGradientPickerProps ) {
+	const { gradientAST, gradientAstValue } =
+		getGradientAstWithDefault( value );
 	// On radial gradients the bar should display a linear gradient.
 	// On radial gradients the bar represents a slice of the gradient from the center until the outside.
 	// On liner gradients the bar represents the color stops from left to right independently of the angle.
 	const background = getLinearGradientRepresentation( gradientAST );
-	const hasGradient = gradientAST.value !== DEFAULT_GRADIENT;
+	const hasGradient = gradientAstValue !== DEFAULT_GRADIENT;
 	// Control points color option may be hex from presets, custom colors will be rgb.
 	// The position should always be a percentage.
-	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => ( {
-		color: getStopCssColor( colorStop ),
-		position: parseInt( colorStop.length.value ),
-	} ) );
+	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => {
+		// Although it's already been checked by `hasUnsupportedLength` in `getGradientAstWithDefault`,
+		// TypeScript doesn't know that `colorStop.length` is not undefined here.
+		//NTS: Is there may be a more elegant way to help TS see that for itself? 🤔
+		return {
+			color: getStopCssColor( colorStop ),
+			position: parseInt( colorStop.length!.value ),
```

Mhh, a couple of alternatives that come to mind:

1. Add an explicit `colorStop.length` check where it makes the most sense. The benefit compared to using the `!.` operator is that we would ensure that the length is correct also at runtime. The downside is that we'd introduce a runtime change, which is never great
2. Another approach could be to modify the return type of the `getGradientAstWithDefault` function. Basically, we could tell TS that the return type is a tweaked version of `gradientParser.GradientNode`, where we override the type of `colorStop.length` and remove the possibility that it is `undefined`. Since `gradientParser.GradientNode` is a union type, you'll probably need to use something similar to the `DistributiveOmit` type to preserve the type union.

I would probably discard option 1, and look into option 2 with a time limit. If you can't work it out in a certain amount of time, we can leave the code as-is and improve it another time.

#### chad1008 on 2023-03-13T16:49:38Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1134305784
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -100,30 +118,58 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 			onChange={ handleOnChange }
 			options={ GRADIENT_OPTIONS }
 			size="__unstable-large"
-			value={ hasGradient && type }
+			// NTS: Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.
+			value={ hasGradient ? type : undefined }
 		/>
 	);
 };
 
-export default function CustomGradientPicker( {
+/**
+ * CustomGradientPicker is a React component that renders a UI for specifying
+ * linear or radial gradients. Radial gradients are displayed in the picker as
+ * a slice of the gradient from the center to the outside.
+ *
+ * ```jsx
+ * import { CustomGradientPicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyCustomGradientPicker = () => {
+ *   const [ gradient, setGradient ] = useState();
+ *
+ *   return (
+ *     <CustomGradientPicker
+ *			value={ gradient }
+ *			onChange={ setGradient }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function CustomGradientPicker( {
 	/** Start opting into the new margin-free styles that will become the default in a future version. */
 	__nextHasNoMargin = false,
 	value,
 	onChange,
-	__experimentalIsRenderedInSidebar,
-} ) {
-	const gradientAST = getGradientAstWithDefault( value );
+	__experimentalIsRenderedInSidebar = false,
+}: CustomGradientPickerProps ) {
+	const { gradientAST, gradientAstValue } =
+		getGradientAstWithDefault( value );
 	// On radial gradients the bar should display a linear gradient.
 	// On radial gradients the bar represents a slice of the gradient from the center until the outside.
 	// On liner gradients the bar represents the color stops from left to right independently of the angle.
 	const background = getLinearGradientRepresentation( gradientAST );
-	const hasGradient = gradientAST.value !== DEFAULT_GRADIENT;
+	const hasGradient = gradientAstValue !== DEFAULT_GRADIENT;
 	// Control points color option may be hex from presets, custom colors will be rgb.
 	// The position should always be a percentage.
-	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => ( {
-		color: getStopCssColor( colorStop ),
-		position: parseInt( colorStop.length.value ),
-	} ) );
+	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => {
+		// Although it's already been checked by `hasUnsupportedLength` in `getGradientAstWithDefault`,
+		// TypeScript doesn't know that `colorStop.length` is not undefined here.
+		//NTS: Is there may be a more elegant way to help TS see that for itself? 🤔
+		return {
+			color: getStopCssColor( colorStop ),
+			position: parseInt( colorStop.length!.value ),
```

Good idea. I took a stab at the second option of tweaking the return value. It meant using an `Exclude` to remove `undefined` as an option, and then a couple of the `DistributiveOmits` to selectively replace parent props as we work back up the tree.

<details>

<summary>Here's a diff of the proposed changes:</summary>

```diff

diff --git a/packages/components/src/custom-gradient-picker/index.tsx b/packages/components/src/custom-gradient-picker/index.tsx
index ada7b7392c..fda0df040a 100644
--- a/packages/components/src/custom-gradient-picker/index.tsx
+++ b/packages/components/src/custom-gradient-picker/index.tsx
@@ -162,7 +162,7 @@ export function CustomGradientPicker( {
 		// TypeScript doesn't know that `colorStop.length` is not undefined here.
 		return {
 			color: getStopCssColor( colorStop ),
-			position: parseInt( colorStop.length!.value ),
+			position: parseInt( colorStop.length.value ),
 		};
 	} );
 
diff --git a/packages/components/src/custom-gradient-picker/types.ts b/packages/components/src/custom-gradient-picker/types.ts
index 68d026eb7f..cc1e9fc105 100644
--- a/packages/components/src/custom-gradient-picker/types.ts
+++ b/packages/components/src/custom-gradient-picker/types.ts
@@ -125,3 +125,24 @@ export type InsertPointProps = {
 	disableAlpha: boolean;
 	__experimentalIsRenderedInSidebar: boolean;
 };
+
+// The following types are used to define a modified version of the
+// `gradientParser` package's `GradientNode` type. In this version,
+// the `length` property of the `ColorStops` contained in the node cannot be
+// `undefined`. The `getGradientAstWithDefault` and `hasUnsupportedLength` utils
+// ensure this is the case, but TypeScript couldn't detect the narrowing without
+// a more explicit type.
+type DefinedColorStopLength = Exclude<
+	gradientParser.ColorStop[ 'length' ],
+	undefined
+>;
+
+type ColorStopWithDefinedLength = DistributiveOmit<
+	gradientParser.ColorStop,
+	'length'
+> & { length: DefinedColorStopLength };
+
+export type GradientNodeWithDefinedStopLengths = DistributiveOmit<
+	gradientParser.GradientNode,
+	'colorStops'
+> & { colorStops: ColorStopWithDefinedLength[] };
diff --git a/packages/components/src/custom-gradient-picker/utils.ts b/packages/components/src/custom-gradient-picker/utils.ts
index 5b21f94c3e..b355949dc6 100644
--- a/packages/components/src/custom-gradient-picker/utils.ts
+++ b/packages/components/src/custom-gradient-picker/utils.ts
@@ -14,7 +14,7 @@ import {
 	DIRECTIONAL_ORIENTATION_ANGLE_MAP,
 } from './constants';
 import { serializeGradient } from './serializer';
-import type { ControlPoint } from './types';
+import type { ControlPoint, GradientNodeWithDefinedStopLengths } from './types';
 
 extend( [ namesPlugin ] );
 
@@ -75,7 +75,10 @@ export function getGradientAstWithDefault( value?: string ) {
 		gradientAstValue = serializeGradient( gradientAST );
 	}
 
-	return { gradientAST, gradientAstValue };
+	return { gradientAST, gradientAstValue } as {
+		gradientAST: GradientNodeWithDefinedStopLengths;
+		gradientAstValue: string;
+	};
 }
 
 export function getGradientAstWithControlPoints(

```

</details>

If it helps, try messing with this mock `GradientNode`:

```
const test: GradientNodeWithDefinedStopLengths = {
	type: 'linear-gradient',
	colorStops: [
		{
			type: 'literal',
			value: 'cross your fingers...',
			length: { type: '%', value: 'here we go...' },
		},
	],
};
```

With the above diff applied, if you play around with the different `ColorStop` types (`literal`, `hex`, `rgb`, or `rgba`), TypeScript should still expect the right `value` type for that `ColorStop` type, but it will never accept an `undefined` length. Thoughts? 😄

#### ciampo on 2023-03-15T16:44:35Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137428474
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -100,30 +118,58 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 			onChange={ handleOnChange }
 			options={ GRADIENT_OPTIONS }
 			size="__unstable-large"
-			value={ hasGradient && type }
+			// NTS: Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.
+			value={ hasGradient ? type : undefined }
 		/>
 	);
 };
 
-export default function CustomGradientPicker( {
+/**
+ * CustomGradientPicker is a React component that renders a UI for specifying
+ * linear or radial gradients. Radial gradients are displayed in the picker as
+ * a slice of the gradient from the center to the outside.
+ *
+ * ```jsx
+ * import { CustomGradientPicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyCustomGradientPicker = () => {
+ *   const [ gradient, setGradient ] = useState();
+ *
+ *   return (
+ *     <CustomGradientPicker
+ *			value={ gradient }
+ *			onChange={ setGradient }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function CustomGradientPicker( {
 	/** Start opting into the new margin-free styles that will become the default in a future version. */
 	__nextHasNoMargin = false,
 	value,
 	onChange,
-	__experimentalIsRenderedInSidebar,
-} ) {
-	const gradientAST = getGradientAstWithDefault( value );
+	__experimentalIsRenderedInSidebar = false,
+}: CustomGradientPickerProps ) {
+	const { gradientAST, gradientAstValue } =
+		getGradientAstWithDefault( value );
 	// On radial gradients the bar should display a linear gradient.
 	// On radial gradients the bar represents a slice of the gradient from the center until the outside.
 	// On liner gradients the bar represents the color stops from left to right independently of the angle.
 	const background = getLinearGradientRepresentation( gradientAST );
-	const hasGradient = gradientAST.value !== DEFAULT_GRADIENT;
+	const hasGradient = gradientAstValue !== DEFAULT_GRADIENT;
 	// Control points color option may be hex from presets, custom colors will be rgb.
 	// The position should always be a percentage.
-	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => ( {
-		color: getStopCssColor( colorStop ),
-		position: parseInt( colorStop.length.value ),
-	} ) );
+	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => {
+		// Although it's already been checked by `hasUnsupportedLength` in `getGradientAstWithDefault`,
+		// TypeScript doesn't know that `colorStop.length` is not undefined here.
+		//NTS: Is there may be a more elegant way to help TS see that for itself? 🤔
+		return {
+			color: getStopCssColor( colorStop ),
+			position: parseInt( colorStop.length!.value ),
```

Although this exploration is interesting and works as expected, my inclination would be to either keep the original `!` syntax, or to add a `@ts-expect-error` comment with the reason why.

#### chad1008 on 2023-03-15T20:19:21Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137729739
Path: packages/components/src/custom-gradient-picker/index.tsx

```diff
@@ -100,30 +118,58 @@ const GradientTypePicker = ( { gradientAST, hasGradient, onChange } ) => {
 			onChange={ handleOnChange }
 			options={ GRADIENT_OPTIONS }
 			size="__unstable-large"
-			value={ hasGradient && type }
+			// NTS: Small runtime change. Previously, if `type` was undefined (which I don't think should ever happen) this check would return `false`, which would be an invalid value.
+			value={ hasGradient ? type : undefined }
 		/>
 	);
 };
 
-export default function CustomGradientPicker( {
+/**
+ * CustomGradientPicker is a React component that renders a UI for specifying
+ * linear or radial gradients. Radial gradients are displayed in the picker as
+ * a slice of the gradient from the center to the outside.
+ *
+ * ```jsx
+ * import { CustomGradientPicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyCustomGradientPicker = () => {
+ *   const [ gradient, setGradient ] = useState();
+ *
+ *   return (
+ *     <CustomGradientPicker
+ *			value={ gradient }
+ *			onChange={ setGradient }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function CustomGradientPicker( {
 	/** Start opting into the new margin-free styles that will become the default in a future version. */
 	__nextHasNoMargin = false,
 	value,
 	onChange,
-	__experimentalIsRenderedInSidebar,
-} ) {
-	const gradientAST = getGradientAstWithDefault( value );
+	__experimentalIsRenderedInSidebar = false,
+}: CustomGradientPickerProps ) {
+	const { gradientAST, gradientAstValue } =
+		getGradientAstWithDefault( value );
 	// On radial gradients the bar should display a linear gradient.
 	// On radial gradients the bar represents a slice of the gradient from the center until the outside.
 	// On liner gradients the bar represents the color stops from left to right independently of the angle.
 	const background = getLinearGradientRepresentation( gradientAST );
-	const hasGradient = gradientAST.value !== DEFAULT_GRADIENT;
+	const hasGradient = gradientAstValue !== DEFAULT_GRADIENT;
 	// Control points color option may be hex from presets, custom colors will be rgb.
 	// The position should always be a percentage.
-	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => ( {
-		color: getStopCssColor( colorStop ),
-		position: parseInt( colorStop.length.value ),
-	} ) );
+	const controlPoints = gradientAST.colorStops.map( ( colorStop ) => {
+		// Although it's already been checked by `hasUnsupportedLength` in `getGradientAstWithDefault`,
+		// TypeScript doesn't know that `colorStop.length` is not undefined here.
+		//NTS: Is there may be a more elegant way to help TS see that for itself? 🤔
+		return {
+			color: getStopCssColor( colorStop ),
+			position: parseInt( colorStop.length!.value ),
```

Added a `@ts-expect-error` ✅

### Review thread 7 (resolved)

#### chad1008 on 2023-03-08T15:01:08Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129578048
Path: packages/components/src/custom-gradient-picker/utils.ts

```diff
@@ -0,0 +1,121 @@
+/**
+ * External dependencies
+ */
+import gradientParser from 'gradient-parser';
+import { colord, extend } from 'colord';
+import namesPlugin from 'colord/plugins/names';
+
+/**
+ * Internal dependencies
+ */
+import {
+	DEFAULT_GRADIENT,
+	HORIZONTAL_GRADIENT_ORIENTATION,
+	DIRECTIONAL_ORIENTATION_ANGLE_MAP,
+} from './constants';
+import { serializeGradient } from './serializer';
+import type { ControlPoint } from './types';
+
+extend( [ namesPlugin ] );
+
+export function getLinearGradientRepresentation(
+	gradientAST: gradientParser.GradientNode
+) {
+	return serializeGradient( {
+		type: 'linear-gradient',
+		orientation: {
+			...HORIZONTAL_GRADIENT_ORIENTATION,
+			value: `${ HORIZONTAL_GRADIENT_ORIENTATION.value }`,
+		},
+		colorStops: gradientAST.colorStops,
+		// NTS: As noted elsewhere, gradientParser's DefinetelyTyped values provide no overlap between the different `orientation` values for linear vs radial nodes.
+	} as gradientParser.LinearGradientNode );
```

As noted elsewhere, gradientParser's `DefinetelyTyped` values provide no overlap between the different `orientation` values for linear vs radial nodes.

#### ciampo on 2023-03-16T11:49:36Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1138528241
Path: packages/components/src/custom-gradient-picker/utils.ts

```diff
@@ -0,0 +1,121 @@
+/**
+ * External dependencies
+ */
+import gradientParser from 'gradient-parser';
+import { colord, extend } from 'colord';
+import namesPlugin from 'colord/plugins/names';
+
+/**
+ * Internal dependencies
+ */
+import {
+	DEFAULT_GRADIENT,
+	HORIZONTAL_GRADIENT_ORIENTATION,
+	DIRECTIONAL_ORIENTATION_ANGLE_MAP,
+} from './constants';
+import { serializeGradient } from './serializer';
+import type { ControlPoint } from './types';
+
+extend( [ namesPlugin ] );
+
+export function getLinearGradientRepresentation(
+	gradientAST: gradientParser.GradientNode
+) {
+	return serializeGradient( {
+		type: 'linear-gradient',
+		orientation: {
+			...HORIZONTAL_GRADIENT_ORIENTATION,
+			value: `${ HORIZONTAL_GRADIENT_ORIENTATION.value }`,
+		},
+		colorStops: gradientAST.colorStops,
+		// NTS: As noted elsewhere, gradientParser's DefinetelyTyped values provide no overlap between the different `orientation` values for linear vs radial nodes.
+	} as gradientParser.LinearGradientNode );
```

It looks like this type cast is unnecessary (at least, removing it on my machine doesn't upset TS)

### Review thread 8 (resolved)

#### chad1008 on 2023-03-08T15:53:49Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129661135
Path: packages/components/src/custom-gradient-picker/serializer.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+/**
+ * Internal dependencies
+ */
+import type { ColorStopTypeAndValue } from './types';
+
+export function serializeGradientColor( {
+	type,
+	value,
+}: ColorStopTypeAndValue ) {
+	if ( type === 'literal' ) {
+		return value;
+	}
+	if ( type === 'hex' ) {
+		return `#${ value }`;
+	}
+	return `${ type }(${ value.join( ',' ) })`;
+}
+
+export function serializeGradientPosition(
+	position: gradientParser.ColorStop[ 'length' ]
+) {
+	if ( ! position ) {
+		return '';
+	}
+	const { value, type } = position;
+	return `${ value }${ type }`;
+}
+
+export function serializeGradientColorStop( {
+	type,
+	value,
+	length,
+}: gradientParser.ColorStop ) {
+	return `${ serializeGradientColor( {
+		type,
+		value,
+	} as ColorStopTypeAndValue ) } ${ serializeGradientPosition( length ) }`;
+}
+
+export function serializeGradientOrientation(
+	orientation: gradientParser.GradientNode[ 'orientation' ]
+) {
+	if (
+		// NTS: Because we're narrowing to `orientation.type !== 'angular'`, ruling out arrays should have no runtime impact
+		Array.isArray( orientation ) ||
```

Because we're narrowing to `orientation.type !== 'angular'`, ruling out arrays should have no runtime impact

#### ciampo on 2023-03-09T19:43:30Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131512744
Path: packages/components/src/custom-gradient-picker/serializer.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+/**
+ * Internal dependencies
+ */
+import type { ColorStopTypeAndValue } from './types';
+
+export function serializeGradientColor( {
+	type,
+	value,
+}: ColorStopTypeAndValue ) {
+	if ( type === 'literal' ) {
+		return value;
+	}
+	if ( type === 'hex' ) {
+		return `#${ value }`;
+	}
+	return `${ type }(${ value.join( ',' ) })`;
+}
+
+export function serializeGradientPosition(
+	position: gradientParser.ColorStop[ 'length' ]
+) {
+	if ( ! position ) {
+		return '';
+	}
+	const { value, type } = position;
+	return `${ value }${ type }`;
+}
+
+export function serializeGradientColorStop( {
+	type,
+	value,
+	length,
+}: gradientParser.ColorStop ) {
+	return `${ serializeGradientColor( {
+		type,
+		value,
+	} as ColorStopTypeAndValue ) } ${ serializeGradientPosition( length ) }`;
+}
+
+export function serializeGradientOrientation(
+	orientation: gradientParser.GradientNode[ 'orientation' ]
+) {
+	if (
+		// NTS: Because we're narrowing to `orientation.type !== 'angular'`, ruling out arrays should have no runtime impact
+		Array.isArray( orientation ) ||
```

Note to future Marco: take a deeper look at this change, make sure it has no runtime impacts

#### ciampo on 2023-03-10T11:22:09Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1132253413
Path: packages/components/src/custom-gradient-picker/serializer.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+/**
+ * Internal dependencies
+ */
+import type { ColorStopTypeAndValue } from './types';
+
+export function serializeGradientColor( {
+	type,
+	value,
+}: ColorStopTypeAndValue ) {
+	if ( type === 'literal' ) {
+		return value;
+	}
+	if ( type === 'hex' ) {
+		return `#${ value }`;
+	}
+	return `${ type }(${ value.join( ',' ) })`;
+}
+
+export function serializeGradientPosition(
+	position: gradientParser.ColorStop[ 'length' ]
+) {
+	if ( ! position ) {
+		return '';
+	}
+	const { value, type } = position;
+	return `${ value }${ type }`;
+}
+
+export function serializeGradientColorStop( {
+	type,
+	value,
+	length,
+}: gradientParser.ColorStop ) {
+	return `${ serializeGradientColor( {
+		type,
+		value,
+	} as ColorStopTypeAndValue ) } ${ serializeGradientPosition( length ) }`;
+}
+
+export function serializeGradientOrientation(
+	orientation: gradientParser.GradientNode[ 'orientation' ]
+) {
+	if (
+		// NTS: Because we're narrowing to `orientation.type !== 'angular'`, ruling out arrays should have no runtime impact
+		Array.isArray( orientation ) ||
```

I spent some time investigating how the component is currently used on `trunk`, and I can confirm that we're currently never passing an array to this function.

Therefore, your change here is correct — it is a runtime change, but it doesn't seem to cause any regressions. Additionally, it prevents the function from returning an incorrectly formatted value (`undefineddeg`)

### Review thread 9 (resolved)

#### ciampo on 2023-03-08T17:26:05Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129807142
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -73,9 +87,9 @@ function GradientColorPickerDropdown( {
 	isRenderedInSidebar,
 	className,
 	...props
-} ) {
+}: WordPressComponentProps< CustomColorPickerDropdownProps, 'div', true > ) {
```

```suggestion
}: CustomColorPickerDropdownProps ) {
```

### Review thread 10 (resolved)

#### ciampo on 2023-03-08T17:29:35Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129812097
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -73,9 +87,9 @@ function GradientColorPickerDropdown( {
 	isRenderedInSidebar,
 	className,
 	...props
-} ) {
+}: WordPressComponentProps< CustomColorPickerDropdownProps, 'div', true > ) {
 	// Open the popover below the gradient control/insertion point
-	const popoverProps = useMemo(
+	const popoverProps: DropdownProps[ 'popoverProps' ] = useMemo(
```

We could use `as const` to narrow down the type, and therefore avoid the need of using `DropdownProps`

```tsx
const popoverProps = useMemo(
		() =>
			( {
				placement: 'bottom',
				offset: 8,
			} as const ),
		[]
	);
```

#### chad1008 on 2023-03-13T16:55:08Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1134314985
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -73,9 +87,9 @@ function GradientColorPickerDropdown( {
 	isRenderedInSidebar,
 	className,
 	...props
-} ) {
+}: WordPressComponentProps< CustomColorPickerDropdownProps, 'div', true > ) {
 	// Open the popover below the gradient control/insertion point
-	const popoverProps = useMemo(
+	const popoverProps: DropdownProps[ 'popoverProps' ] = useMemo(
```

👍 0f56c0b5a9

### Review thread 11 (resolved)

#### ciampo on 2023-03-08T17:30:41Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129813361
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+export type ColorStopTypeAndValue = DistributiveOmit<
+	gradientParser.ColorStop,
+	'length'
+>;
+
+export type CustomGradientBarProps = {
+	background: React.CSSProperties[ 'background' ];
+	hasGradient: boolean;
+	value: ControlPoint[];
+	onChange: ( newControlPoints: ControlPoint[] ) => void;
+	disableInserter?: boolean;
+	disableAlpha?: boolean;
+	__experimentalIsRenderedInSidebar: boolean;
+};
+
+export type CustomGradientBarIdleState = { id: 'IDLE' };
+type CustomGradientBarMovingInserterState = {
+	id: 'MOVING_INSERTER';
+	insertPosition: number;
+};
+type CustomGradientBarInsertingControlPointState = {
+	id: 'INSERTING_CONTROL_POINT';
+	insertPosition: number;
+};
+type CustomGradientBarMovingControlPointState = { id: 'MOVING_CONTROL_POINT' };
+
+export type CustomGradientBarReducerState =
+	| CustomGradientBarIdleState
+	| CustomGradientBarMovingInserterState
+	| CustomGradientBarInsertingControlPointState
+	| CustomGradientBarMovingControlPointState;
+
+export type CustomGradientBarReducerAction =
+	| { type: 'MOVE_INSERTER'; insertPosition: number }
+	| { type: 'STOP_INSERTER_MOVE' }
+	| { type: 'OPEN_INSERTER' }
+	| { type: 'CLOSE_INSERTER' }
+	| { type: 'START_CONTROL_CHANGE' }
+	| { type: 'STOP_CONTROL_CHANGE' };
+
+export type ControlPointButtonProps = {
+	isOpen: boolean;
+	position: ControlPoint[ 'position' ];
+	color: string;
+};
+
+export type ControlPointsProps = {
+	disableRemove: boolean;
+	disableAlpha: boolean;
+	gradientPickerDomRef: React.RefObject< HTMLDivElement >;
+	ignoreMarkerPosition?: number;
+	value: ControlPoint[];
+	onChange: ( conrolPoints: ControlPoint[] ) => void;
```

```suggestion
	onChange: ( controlPoints: ControlPoint[] ) => void;
```

### Review thread 12 (resolved)

#### ciampo on 2023-03-08T17:32:03Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129814885
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -108,16 +122,28 @@ function ControlPoints( {
 	onStartControlPointChange,
 	onStopControlPointChange,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const controlPointMoveState = useRef();
+}: ControlPointsProps ) {
+	const controlPointMoveState: React.MutableRefObject<
+		ControlPointMoveState | undefined
+	> = useRef();
```

```suggestion
	const controlPointMoveState = useRef< ControlPointMoveState >();
```

### Review thread 13 (resolved)

#### ciampo on 2023-03-08T17:57:24Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129841142
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -108,16 +122,28 @@ function ControlPoints( {
 	onStartControlPointChange,
 	onStopControlPointChange,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const controlPointMoveState = useRef();
+}: ControlPointsProps ) {
+	const controlPointMoveState: React.MutableRefObject<
+		ControlPointMoveState | undefined
+	> = useRef();
 
-	const onMouseMove = ( event ) => {
+	const onMouseMove = ( event: MouseEvent ) => {
```

From our pairing review:

```diff
diff --git a/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx b/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx
index 3fddb545ba..e82f5ec79a 100644
--- a/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx
+++ b/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx
@@ -128,22 +128,28 @@ function ControlPoints( {
 	> = useRef();
 
 	const onMouseMove = ( event: MouseEvent ) => {
-		const relativePosition = getHorizontalRelativeGradientPosition(
-			event.clientX,
-			gradientPickerDomRef.current
-		);
-
-		const { initialPosition, index, significantMoveHappened } =
-			controlPointMoveState?.current ?? {};
+		// TODO: make sure that the "gradientPickerDomRef.current === null" makes sense
+		// ie. if gradientPickerDomRef.current === null, relativePosition would be
+		// `undefined`, which means that `updateControlPointPosition` would end up creating
+		// control points with an `undefined` position. We need to make sure that that's
+		// not a desirable state.
 		if (
 			controlPointMoveState.current === undefined ||
-			initialPosition === undefined ||
-			index === undefined ||
-			relativePosition === undefined
+			gradientPickerDomRef.current === null
 		) {
 			return;
 		}
 
+		// TODO: type getHorizontalRelativeGradientPosition so that it knows that
+		// its return value is `undefined` only if the second argument is null
+		const relativePosition = getHorizontalRelativeGradientPosition(
+			event.clientX,
+			gradientPickerDomRef.current
+		) as number;
+
+		const { initialPosition, index, significantMoveHappened } =
+			controlPointMoveState.current;
+
 		if (
 			! significantMoveHappened &&
 			Math.abs( initialPosition - relativePosition ) >=

```

#### chad1008 on 2023-03-14T00:28:29Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1134746630
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -108,16 +122,28 @@ function ControlPoints( {
 	onStartControlPointChange,
 	onStopControlPointChange,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const controlPointMoveState = useRef();
+}: ControlPointsProps ) {
+	const controlPointMoveState: React.MutableRefObject<
+		ControlPointMoveState | undefined
+	> = useRef();
 
-	const onMouseMove = ( event ) => {
+	const onMouseMove = ( event: MouseEvent ) => {
```

> make sure that the "gradientPickerDomRef.current === null" makes sense

I did some digging and it looks like the `gradientPickerDomRef.current === null` check is the right move to me - I can't see a case where control points with `undefined` position is desirable 👍 

> type getHorizontalRelativeGradientPosition so that it knows that its return value is `undefined` only if the second argument is null

I tried using a conditional type here, but couldn't get it working so that TypeScript could infer the correct return type.
<details>

<summary>I've experimented with an override instead:</summary>

```diff

diff --git a/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx b/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx
index 9668022c62..2f07b6e3b4 100644
--- a/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx
+++ b/packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx
@@ -136,7 +136,7 @@ function ControlPoints( {
 		const relativePosition = getHorizontalRelativeGradientPosition(
 			event.clientX,
 			gradientPickerDomRef.current
-		) as number;
+		);
 
 		const { initialPosition, index, significantMoveHappened } =
 			controlPointMoveState.current;
diff --git a/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts b/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts
index 271d4fbc16..f82aaac4f0 100644
--- a/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts
+++ b/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts
@@ -93,6 +93,17 @@ export function updateControlPointColorByPosition(
 	return updateControlPointColor( points, index, newColor );
 }
 
+// `getHorizontalRelativeGradientPosition` should only return `undefined` if
+// the `containerElement` argument is `null` (i.e. when the relevant ref is
+// first created, but before the element is rendered and assigned to the ref).
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement
+): number;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: null
+): undefined;
 export function getHorizontalRelativeGradientPosition(
 	mouseXCoordinate: number,
 	containerElement: HTMLDivElement | null


```

</details>

But I'm not sure if that's the right way to go.

#### ciampo on 2023-03-15T17:04:53Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137460529
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -108,16 +122,28 @@ function ControlPoints( {
 	onStartControlPointChange,
 	onStopControlPointChange,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const controlPointMoveState = useRef();
+}: ControlPointsProps ) {
+	const controlPointMoveState: React.MutableRefObject<
+		ControlPointMoveState | undefined
+	> = useRef();
 
-	const onMouseMove = ( event ) => {
+	const onMouseMove = ( event: MouseEvent ) => {
```

Using overload signatures looks good to me, let's go with your proposed solution :)

#### chad1008 on 2023-03-15T20:26:01Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137736841
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -108,16 +122,28 @@ function ControlPoints( {
 	onStartControlPointChange,
 	onStopControlPointChange,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const controlPointMoveState = useRef();
+}: ControlPointsProps ) {
+	const controlPointMoveState: React.MutableRefObject<
+		ControlPointMoveState | undefined
+	> = useRef();
 
-	const onMouseMove = ( event ) => {
+	const onMouseMove = ( event: MouseEvent ) => {
```

Done!

#### chad1008 on 2023-03-15T20:43:56Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137752782
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -108,16 +122,28 @@ function ControlPoints( {
 	onStartControlPointChange,
 	onStopControlPointChange,
 	__experimentalIsRenderedInSidebar,
-} ) {
-	const controlPointMoveState = useRef();
+}: ControlPointsProps ) {
+	const controlPointMoveState: React.MutableRefObject<
+		ControlPointMoveState | undefined
+	> = useRef();
 
-	const onMouseMove = ( event ) => {
+	const onMouseMove = ( event: MouseEvent ) => {
```

Additional note - when we looked at this change earlier, you floated the idea of a third overload that would accept and return a union of the two possible types, but we didn't feel it was needed. It turns out it is, otherwise we get an error [when inserting a control point](https://github.com/WordPress/gutenberg/blob/9e2185782f1f913a5f9b49c68fab9312d3cf3bb0/packages/components/src/custom-gradient-picker/gradient-bar/index.tsx#L101).

Apparently if the call could accept a union, but none of the overrides do, it errors... even if each union member matches an available override. I did a little hunting and [it looks like this is intentional](https://github.com/WordPress/gutenberg/blob/9e2185782f1f913a5f9b49c68fab9312d3cf3bb0/packages/components/src/custom-gradient-picker/gradient-bar/index.tsx#L101), so I've added the third override for the union.

### Review thread 14 (resolved)

#### ciampo on 2023-03-08T17:57:56Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129841746
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
```

This is nice! Let's add a code comment to give more context to why we can't use `Omit` directly

#### ciampo on 2023-03-08T17:58:13Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1129842031
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
```

https://stackoverflow.com/questions/57103834/typescript-omit-a-property-from-all-interfaces-in-a-union-but-keep-the-union-s

### Review thread 15 (resolved)

#### ciampo on 2023-03-09T18:09:26Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131414518
Path: packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx

```diff
@@ -148,142 +172,158 @@ function ControlPoints( {
 	// Adding `cleanEventListeners` to the dependency array below requires the function itself to be wrapped in a `useCallback`
 	// This memoization would prevent the event listeners from being properly cleaned.
 	// Instead, we'll pass a ref to the function in our `useEffect` so `cleanEventListeners` itself is no longer a dependency.
-	const cleanEventListenersRef = useRef();
+	const cleanEventListenersRef = useRef< ( () => void ) | undefined >();
```

Small nit: the `| undefined` type is redundant, TypeScript would be able to infer that on its own

```suggestion
	const cleanEventListenersRef = useRef< () => void >();
```

### Review thread 16 (resolved)

#### ciampo on 2023-03-09T18:20:14Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131427756
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
```

We should probably keep the JSDoc comments from the original JS code for all the functions in this file, while removing the type info (which is now specified in TS)

#### chad1008 on 2023-03-14T14:25:28Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1135634504
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
```

✅ a33606ef4f

### Review thread 17 (resolved)

#### ciampo on 2023-03-09T18:22:51Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131430245
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: string
```

In the original code, the `color` argument was typed as an object with the `r`, `g`, `b` and `a` properties. Interesting that this type was not correct, and that we we passing a `string` instead

#### ciampo on 2023-03-09T18:40:09Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131449291
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: string
```

Also, following the same convention used below for the `position` prop, we could rewrite this to be

```
	position: ControlPoint[ 'position' ]
	color: ControlPoint[ 'color' ]
```

#### chad1008 on 2023-03-14T14:48:47Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1135676168
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: string
```

Yes, I thought that disconnect on the typing was odd as well... there are one or two others in the file as well that were actually strings despite referring back to that `Color` object.

Good point on the convention consistency, updating that and the other instances mentioned below in 7a171c6aa5

### Review thread 18 (resolved)

#### ciampo on 2023-03-09T18:40:47Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131449887
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: string
+) {
+	const nextIndex = points.findIndex(
+		( point ) => point.position > position
+	);
+	const newPoint = { color, position };
+	const newPoints = points.slice();
+	newPoints.splice( nextIndex - 1, 0, newPoint );
+	return newPoints;
+}
+
+export function removeControlPoint( points: ControlPoint[], index: number ) {
+	return points.filter( ( _point, pointIndex ) => {
+		return pointIndex !== index;
+	} );
+}
+
+export function updateControlPoint(
+	points: ControlPoint[],
+	index: number,
+	newPoint: ControlPoint
+) {
+	const newValue = points.slice();
+	newValue[ index ] = newPoint;
+	return newValue;
+}
+
+export function updateControlPointPosition(
+	points: ControlPoint[],
+	index: number,
+	newPosition: ControlPoint[ 'position' ]
+) {
+	if ( isOverlapping( points, index, newPosition ) ) {
+		return points;
+	}
+	const newPoint = {
+		...points[ index ],
+		position: newPosition,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+export function updateControlPointColor(
+	points: ControlPoint[],
+	index: number,
+	newColor: string
```

Same as above

```suggestion
	newColor: ControlPoint[ 'color' ]
```

### Review thread 19 (resolved)

#### ciampo on 2023-03-09T18:40:54Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131449979
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,108 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: string
+) {
+	const nextIndex = points.findIndex(
+		( point ) => point.position > position
+	);
+	const newPoint = { color, position };
+	const newPoints = points.slice();
+	newPoints.splice( nextIndex - 1, 0, newPoint );
+	return newPoints;
+}
+
+export function removeControlPoint( points: ControlPoint[], index: number ) {
+	return points.filter( ( _point, pointIndex ) => {
+		return pointIndex !== index;
+	} );
+}
+
+export function updateControlPoint(
+	points: ControlPoint[],
+	index: number,
+	newPoint: ControlPoint
+) {
+	const newValue = points.slice();
+	newValue[ index ] = newPoint;
+	return newValue;
+}
+
+export function updateControlPointPosition(
+	points: ControlPoint[],
+	index: number,
+	newPosition: ControlPoint[ 'position' ]
+) {
+	if ( isOverlapping( points, index, newPosition ) ) {
+		return points;
+	}
+	const newPoint = {
+		...points[ index ],
+		position: newPosition,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+export function updateControlPointColor(
+	points: ControlPoint[],
+	index: number,
+	newColor: string
+) {
+	const newPoint = {
+		...points[ index ],
+		color: newColor,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+export function updateControlPointColorByPosition(
+	points: ControlPoint[],
+	position: ControlPoint[ 'position' ],
+	newColor: string
```

Same as above

```suggestion
	newColor: ControlPoint[ 'color' ]
```

### Review thread 20

#### ciampo on 2023-03-09T19:42:02Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131511475
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+export type ColorStopTypeAndValue = DistributiveOmit<
```

A bit of a nitpick, but the name of the type is a bit misleading. What if the `ColorStop` type had more than 3 props, or if it updated during a release and added/removed props? 

I would either:

- call this time `ColorStopWithoutLength` (simple, but less future-proof)
- keep the `ColorStopTypeAndValue` name, but instead of `Omit`ting the `length` property, I'd explicitly `Pick` only the `type` and `value` props

What do you think?

#### chad1008 on 2023-03-14T16:10:51Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1135806157
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+export type ColorStopTypeAndValue = DistributiveOmit<
```

Never liking to do things the easy way, I think going with a `Pick` approach is best. That said, `Pick` suffers the same "squashing" problem as `Omit` on a union of objects, so we need a `DistributivePick` to make this work. Yes, the geek in me is excited.

I've implemented that in 05b4a18464

Note: I've left `DistributiveOmit` in place, even though it technically isn't used, as it will get used if we apply [some changes](https://github.com/WordPress/gutenberg/pull/48929#discussion_r1134305784) I've proposed elsewhere. Can pull it later if we don't end up needing it.

#### ciampo on 2023-03-15T18:41:56Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137592971
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+export type ColorStopTypeAndValue = DistributiveOmit<
```

Sounds good. Let's keep this convo open as a reminder to check before merging

#### chad1008 on 2023-03-15T20:44:45Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137753471
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
+	/**
+	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
+	 */
+	__experimentalIsRenderedInSidebar?: boolean;
+};
+
+export type GradientAnglePickerProps = {
+	gradientAST:
+		| gradientParser.LinearGradientNode
+		| gradientParser.RepeatingLinearGradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type GradientTypePickerProps = {
+	gradientAST: gradientParser.GradientNode;
+	hasGradient: boolean;
+	onChange: ( gradient: string ) => void;
+};
+
+export type ControlPoint = { color: string; position: number };
+
+type DistributiveOmit< T, K extends keyof any > = T extends any
+	? Omit< T, K >
+	: never;
+export type ColorStopTypeAndValue = DistributiveOmit<
```

Leaving open for now in case we need it, but I went ahead and removed `DistributiveOmit`. Since we didn't keep the changes I mentioned above, the type was no longer being used.

### Review thread 21 (resolved)

#### ciampo on 2023-03-09T19:52:07Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131521992
Path: packages/components/src/custom-gradient-picker/serializer.ts

```diff
@@ -0,0 +1,82 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+/**
+ * Internal dependencies
+ */
+import type { ColorStopTypeAndValue } from './types';
+
+export function serializeGradientColor( {
+	type,
+	value,
+}: ColorStopTypeAndValue ) {
+	if ( type === 'literal' ) {
+		return value;
+	}
+	if ( type === 'hex' ) {
+		return `#${ value }`;
+	}
+	return `${ type }(${ value.join( ',' ) })`;
+}
+
+export function serializeGradientPosition(
+	position: gradientParser.ColorStop[ 'length' ]
+) {
+	if ( ! position ) {
+		return '';
+	}
+	const { value, type } = position;
+	return `${ value }${ type }`;
+}
+
+export function serializeGradientColorStop( {
+	type,
+	value,
+	length,
+}: gradientParser.ColorStop ) {
+	return `${ serializeGradientColor( {
+		type,
+		value,
+	} as ColorStopTypeAndValue ) } ${ serializeGradientPosition( length ) }`;
+}
+
+export function serializeGradientOrientation(
+	orientation: gradientParser.GradientNode[ 'orientation' ]
+) {
+	if (
+		Array.isArray( orientation ) ||
+		! orientation ||
+		orientation.type !== 'angular'
+	) {
+		return;
+	}
+	return `${ orientation.value }deg`;
+}
+
+export function serializeGradient( {
+	type,
+	orientation,
+	colorStops,
+}: gradientParser.GradientNode ) {
+	const serializedOrientation = serializeGradientOrientation( orientation );
+	const serializedColorStops = colorStops
+		.sort( ( colorStop1, colorStop2 ) => {
+			const getNumericStopValue = (
+				colorStop: gradientParser.ColorStop
+			) => {
+				return colorStop?.length?.value === undefined
+					? 0
+					: parseInt( colorStop?.length?.value );
```

The optional chaining here shouldn't be necessary, because of the `colorStop?.length?.value === undefined` check.

Also, for added safety, I wondered if we should check for `NaN` when calling `parseInt`, but then I realised that the result of `getNumericStopValue` is used as the return function of `array.sort`, and I verified that `array.sort(() => NaN)` actually runs corrently, and doesn't change the order of the array elements.

### Review thread 22 (resolved)

#### ciampo on 2023-03-09T19:52:45Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131522565
Path: packages/components/src/custom-gradient-picker/stories/index.tsx

```diff
@@ -8,16 +12,20 @@ import { useState } from '@wordpress/element';
  */
 import CustomGradientPicker from '../';
 
-export default {
+const meta: ComponentMeta< typeof CustomGradientPicker > = {
 	title: 'Components/CustomGradientPicker',
 	component: CustomGradientPicker,
-	argTypes: {
-		__nextHasNoMargin: { control: { type: 'boolean' } },
+	parameters: {
+		controls: { expanded: true },
+		docs: { source: { state: 'open' } },
 	},
 };
+export default meta;
 
-const CustomGradientPickerWithState = ( props ) => {
-	const [ gradient, setGradient ] = useState();
+const CustomGradientPickerWithState: ComponentStory<
+	typeof CustomGradientPicker
+> = ( props ) => {
+	const [ gradient, setGradient ] = useState< string | undefined >();
```

Nit: We can probably avoid adding the `undefined` type, TS should be able to infer it

### Review thread 23 (resolved)

#### ciampo on 2023-03-09T20:00:51Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131529950
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
```

The optional `null` value is not reflected in the type (`string | undefined`)

#### chad1008 on 2023-03-14T16:30:12Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1135861769
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
```

Good catch, thank you!

Updated in 709aad9ba9. The `null` will hit [the same nullish coalescing operator as an `undefined` value](https://github.com/WordPress/gutenberg/blob/709aad9ba92456affd9b9e2868902d661c83071c/packages/components/src/custom-gradient-picker/utils.ts#L44), so this shouldn't have any side effects.

### Review thread 24 (resolved)

#### ciampo on 2023-03-09T20:01:29Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1131530509
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,127 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
+	 */
+	onChange: ( currentGradient: string | undefined ) => void;
```

Nit: shorter type

```suggestion
	onChange: ( currentGradient?: string ) => void;
```

### Review thread 25 (resolved)

#### ciampo on 2023-03-15T16:45:40Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137430086
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -0,0 +1,140 @@
+/**
+ * External dependencies
+ */
+import type gradientParser from 'gradient-parser';
+
+export type CustomGradientPickerProps = {
+	/**
+	 * Start opting in to the new margin-free styles that will become the default
+	 * in a future version, currently scheduled to be WordPress 6.4. (The prop
+	 * can be safely removed once this happens.)
+	 *
+	 * @default false
+	 */
+	__nextHasNoMargin?: boolean;
+	/**
+	 * The current value of the gradient. Pass a css gradient string (See default value for example).
+	 * Optionally pass in a `null` value to specify no gradient is currently selected.
+	 *
+	 * @default 'linear-gradient(135deg,rgba(6,147,227,1) 0%,rgb(155,81,224) 100%)'
+	 */
+	value?: string | null;
+	/**
+	 * The function called when a new gradient has been defined. It is passed to
+	 * the `currentGradient` as an arugment.
```

```suggestion
	 * the `currentGradient` as an argument.
```

### Review thread 26 (resolved)

#### ciampo on 2023-03-16T11:08:58Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1138484502
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,197 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+/**
+ * Clamps a number between 0 and 100.
+ *
+ * @param value Value to clamp.
+ *
+ * @return Value clamped between 0 and 100.
+ */
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+/**
+ * Check if a control point is overlapping with another.
+ *
+ * @param value        Array of control points.
+ * @param initialIndex Index of the position to test.
+ * @param newPosition  New position of the control point.
+ * @param minDistance  Distance considered to be overlapping.
+ *
+ * @return True if the point is overlapping.
+ */
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
```

It probably doesn't hurt to specify that `minDistance` is a `number`

```suggestion
	minDistance: number = MINIMUM_DISTANCE_BETWEEN_POINTS
```

### Review thread 27

#### ciampo on 2023-03-16T11:11:23Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1138487164
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,197 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+/**
+ * Clamps a number between 0 and 100.
+ *
+ * @param value Value to clamp.
+ *
+ * @return Value clamped between 0 and 100.
+ */
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+/**
+ * Check if a control point is overlapping with another.
+ *
+ * @param value        Array of control points.
+ * @param initialIndex Index of the position to test.
+ * @param newPosition  New position of the control point.
+ * @param minDistance  Distance considered to be overlapping.
+ *
+ * @return True if the point is overlapping.
+ */
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+/**
+ * Adds a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position to insert the new point.
+ * @param color    Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: ControlPoint[ 'color' ]
+) {
+	const nextIndex = points.findIndex(
+		( point ) => point.position > position
+	);
+	const newPoint = { color, position };
+	const newPoints = points.slice();
+	newPoints.splice( nextIndex - 1, 0, newPoint );
+	return newPoints;
+}
+
+/**
+ * Removes a control point from an array and returns the new array.
+ *
+ * @param points Array of control points.
+ * @param index  Index to remove.
+ *
+ * @return New array of control points.
+ */
+export function removeControlPoint( points: ControlPoint[], index: number ) {
+	return points.filter( ( _point, pointIndex ) => {
+		return pointIndex !== index;
+	} );
+}
+/**
+ * Updates a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newPoint New control point to replace the index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPoint(
+	points: ControlPoint[],
+	index: number,
+	newPoint: ControlPoint
+) {
+	const newValue = points.slice();
+	newValue[ index ] = newPoint;
+	return newValue;
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points      Array of control points.
+ * @param index       Index to update.
+ * @param newPosition Position to move the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointPosition(
+	points: ControlPoint[],
+	index: number,
+	newPosition: ControlPoint[ 'position' ]
+) {
+	if ( isOverlapping( points, index, newPosition ) ) {
+		return points;
+	}
+	const newPoint = {
+		...points[ index ],
+		position: newPosition,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColor(
+	points: ControlPoint[],
+	index: number,
+	newColor: ControlPoint[ 'color' ]
+) {
+	const newPoint = {
+		...points[ index ],
+		color: newColor,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position of the color stop.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColorByPosition(
+	points: ControlPoint[],
+	position: ControlPoint[ 'position' ],
+	newColor: ControlPoint[ 'color' ]
+) {
+	const index = points.findIndex( ( point ) => point.position === position );
+	return updateControlPointColor( points, index, newColor );
+}
+
+/**
+ * Gets the horizontal coordinate when dragging a control point with the mouse.
+ *
+ * @param mouseXcoordinate Horizontal coordinate of the mouse position.
+ * @param containerElement Container for the gradient picker.
+ *
+ * @return Whole number percentage from the left.
+ */
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement
+): number;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: null
+): undefined;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement | null
+): number | undefined;
```

I think that your initial implementation was correct — we don't really need to advertise this type for the `getHorizontalRelativeGradientPosition` function

```suggestion
```

#### chad1008 on 2023-03-16T16:32:25Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1139017342
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,197 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+/**
+ * Clamps a number between 0 and 100.
+ *
+ * @param value Value to clamp.
+ *
+ * @return Value clamped between 0 and 100.
+ */
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+/**
+ * Check if a control point is overlapping with another.
+ *
+ * @param value        Array of control points.
+ * @param initialIndex Index of the position to test.
+ * @param newPosition  New position of the control point.
+ * @param minDistance  Distance considered to be overlapping.
+ *
+ * @return True if the point is overlapping.
+ */
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+/**
+ * Adds a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position to insert the new point.
+ * @param color    Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: ControlPoint[ 'color' ]
+) {
+	const nextIndex = points.findIndex(
+		( point ) => point.position > position
+	);
+	const newPoint = { color, position };
+	const newPoints = points.slice();
+	newPoints.splice( nextIndex - 1, 0, newPoint );
+	return newPoints;
+}
+
+/**
+ * Removes a control point from an array and returns the new array.
+ *
+ * @param points Array of control points.
+ * @param index  Index to remove.
+ *
+ * @return New array of control points.
+ */
+export function removeControlPoint( points: ControlPoint[], index: number ) {
+	return points.filter( ( _point, pointIndex ) => {
+		return pointIndex !== index;
+	} );
+}
+/**
+ * Updates a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newPoint New control point to replace the index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPoint(
+	points: ControlPoint[],
+	index: number,
+	newPoint: ControlPoint
+) {
+	const newValue = points.slice();
+	newValue[ index ] = newPoint;
+	return newValue;
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points      Array of control points.
+ * @param index       Index to update.
+ * @param newPosition Position to move the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointPosition(
+	points: ControlPoint[],
+	index: number,
+	newPosition: ControlPoint[ 'position' ]
+) {
+	if ( isOverlapping( points, index, newPosition ) ) {
+		return points;
+	}
+	const newPoint = {
+		...points[ index ],
+		position: newPosition,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColor(
+	points: ControlPoint[],
+	index: number,
+	newColor: ControlPoint[ 'color' ]
+) {
+	const newPoint = {
+		...points[ index ],
+		color: newColor,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position of the color stop.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColorByPosition(
+	points: ControlPoint[],
+	position: ControlPoint[ 'position' ],
+	newColor: ControlPoint[ 'color' ]
+) {
+	const index = points.findIndex( ( point ) => point.position === position );
+	return updateControlPointColor( points, index, newColor );
+}
+
+/**
+ * Gets the horizontal coordinate when dragging a control point with the mouse.
+ *
+ * @param mouseXcoordinate Horizontal coordinate of the mouse position.
+ * @param containerElement Container for the gradient picker.
+ *
+ * @return Whole number percentage from the left.
+ */
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement
+): number;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: null
+): undefined;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement | null
+): number | undefined;
```

[This comment probably got lost](https://github.com/WordPress/gutenberg/pull/48929#discussion_r1137752782) in the mountain of threads we've accumulated, but I think we do actually need the union override.

If you don't get the same error, or if there's a better solution, I'm happy to investigate further!

#### ciampo on 2023-03-17T14:24:43Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1140305322
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,197 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+/**
+ * Clamps a number between 0 and 100.
+ *
+ * @param value Value to clamp.
+ *
+ * @return Value clamped between 0 and 100.
+ */
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+/**
+ * Check if a control point is overlapping with another.
+ *
+ * @param value        Array of control points.
+ * @param initialIndex Index of the position to test.
+ * @param newPosition  New position of the control point.
+ * @param minDistance  Distance considered to be overlapping.
+ *
+ * @return True if the point is overlapping.
+ */
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+/**
+ * Adds a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position to insert the new point.
+ * @param color    Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: ControlPoint[ 'color' ]
+) {
+	const nextIndex = points.findIndex(
+		( point ) => point.position > position
+	);
+	const newPoint = { color, position };
+	const newPoints = points.slice();
+	newPoints.splice( nextIndex - 1, 0, newPoint );
+	return newPoints;
+}
+
+/**
+ * Removes a control point from an array and returns the new array.
+ *
+ * @param points Array of control points.
+ * @param index  Index to remove.
+ *
+ * @return New array of control points.
+ */
+export function removeControlPoint( points: ControlPoint[], index: number ) {
+	return points.filter( ( _point, pointIndex ) => {
+		return pointIndex !== index;
+	} );
+}
+/**
+ * Updates a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newPoint New control point to replace the index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPoint(
+	points: ControlPoint[],
+	index: number,
+	newPoint: ControlPoint
+) {
+	const newValue = points.slice();
+	newValue[ index ] = newPoint;
+	return newValue;
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points      Array of control points.
+ * @param index       Index to update.
+ * @param newPosition Position to move the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointPosition(
+	points: ControlPoint[],
+	index: number,
+	newPosition: ControlPoint[ 'position' ]
+) {
+	if ( isOverlapping( points, index, newPosition ) ) {
+		return points;
+	}
+	const newPoint = {
+		...points[ index ],
+		position: newPosition,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColor(
+	points: ControlPoint[],
+	index: number,
+	newColor: ControlPoint[ 'color' ]
+) {
+	const newPoint = {
+		...points[ index ],
+		color: newColor,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position of the color stop.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColorByPosition(
+	points: ControlPoint[],
+	position: ControlPoint[ 'position' ],
+	newColor: ControlPoint[ 'color' ]
+) {
+	const index = points.findIndex( ( point ) => point.position === position );
+	return updateControlPointColor( points, index, newColor );
+}
+
+/**
+ * Gets the horizontal coordinate when dragging a control point with the mouse.
+ *
+ * @param mouseXcoordinate Horizontal coordinate of the mouse position.
+ * @param containerElement Container for the gradient picker.
+ *
+ * @return Whole number percentage from the left.
+ */
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement
+): number;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: null
+): undefined;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement | null
+): number | undefined;
```

I definitely missed it 😓 

Anyway, I took a look and I see why we'd need the third overload. Although I also looked at the TS error, and I think that a better solution would be to tweak the early return check in the `GradientBar`, which would then allows us to remove that third function overload:

```diff
diff --git a/packages/components/src/custom-gradient-picker/gradient-bar/index.tsx b/packages/components/src/custom-gradient-picker/gradient-bar/index.tsx
index 526ee53bff..225a5fb757 100644
--- a/packages/components/src/custom-gradient-picker/gradient-bar/index.tsx
+++ b/packages/components/src/custom-gradient-picker/gradient-bar/index.tsx
@@ -96,15 +96,15 @@ export default function CustomGradientBar( {
 	const onMouseEnterAndMove: MouseEventHandler< HTMLDivElement > = (
 		event
 	) => {
+		if ( ! gradientMarkersContainerDomRef.current ) {
+			return;
+		}
+
 		const insertPosition = getHorizontalRelativeGradientPosition(
 			event.clientX,
 			gradientMarkersContainerDomRef.current
 		);
 
-		if ( insertPosition === undefined ) {
-			return;
-		}
-
 		// If the insert point is close to an existing control point don't show it.
 		if (
 			controlPoints.some( ( { position } ) => {
diff --git a/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts b/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts
index 6e9417328c..88daf2b2c1 100644
--- a/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts
+++ b/packages/components/src/custom-gradient-picker/gradient-bar/utils.ts
@@ -178,10 +178,6 @@ export function getHorizontalRelativeGradientPosition(
 	mouseXcoordinate: number,
 	containerElement: null
 ): undefined;
-export function getHorizontalRelativeGradientPosition(
-	mouseXcoordinate: number,
-	containerElement: HTMLDivElement | null
-): number | undefined;
 export function getHorizontalRelativeGradientPosition(
 	mouseXCoordinate: number,
 	containerElement: HTMLDivElement | null
```

What do you think?

#### chad1008 on 2023-03-17T15:02:46Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1140352237
Path: packages/components/src/custom-gradient-picker/gradient-bar/utils.ts

```diff
@@ -0,0 +1,197 @@
+/**
+ * Internal dependencies
+ */
+import { MINIMUM_DISTANCE_BETWEEN_POINTS } from './constants';
+import type { ControlPoint } from '../types';
+
+/**
+ * Clamps a number between 0 and 100.
+ *
+ * @param value Value to clamp.
+ *
+ * @return Value clamped between 0 and 100.
+ */
+export function clampPercent( value: number ) {
+	return Math.max( 0, Math.min( 100, value ) );
+}
+
+/**
+ * Check if a control point is overlapping with another.
+ *
+ * @param value        Array of control points.
+ * @param initialIndex Index of the position to test.
+ * @param newPosition  New position of the control point.
+ * @param minDistance  Distance considered to be overlapping.
+ *
+ * @return True if the point is overlapping.
+ */
+export function isOverlapping(
+	value: ControlPoint[],
+	initialIndex: number,
+	newPosition: number,
+	minDistance = MINIMUM_DISTANCE_BETWEEN_POINTS
+) {
+	const initialPosition = value[ initialIndex ].position;
+	const minPosition = Math.min( initialPosition, newPosition );
+	const maxPosition = Math.max( initialPosition, newPosition );
+
+	return value.some( ( { position }, index ) => {
+		return (
+			index !== initialIndex &&
+			( Math.abs( position - newPosition ) < minDistance ||
+				( minPosition < position && position < maxPosition ) )
+		);
+	} );
+}
+
+/**
+ * Adds a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position to insert the new point.
+ * @param color    Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function addControlPoint(
+	points: ControlPoint[],
+	position: number,
+	color: ControlPoint[ 'color' ]
+) {
+	const nextIndex = points.findIndex(
+		( point ) => point.position > position
+	);
+	const newPoint = { color, position };
+	const newPoints = points.slice();
+	newPoints.splice( nextIndex - 1, 0, newPoint );
+	return newPoints;
+}
+
+/**
+ * Removes a control point from an array and returns the new array.
+ *
+ * @param points Array of control points.
+ * @param index  Index to remove.
+ *
+ * @return New array of control points.
+ */
+export function removeControlPoint( points: ControlPoint[], index: number ) {
+	return points.filter( ( _point, pointIndex ) => {
+		return pointIndex !== index;
+	} );
+}
+/**
+ * Updates a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newPoint New control point to replace the index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPoint(
+	points: ControlPoint[],
+	index: number,
+	newPoint: ControlPoint
+) {
+	const newValue = points.slice();
+	newValue[ index ] = newPoint;
+	return newValue;
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points      Array of control points.
+ * @param index       Index to update.
+ * @param newPosition Position to move the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointPosition(
+	points: ControlPoint[],
+	index: number,
+	newPosition: ControlPoint[ 'position' ]
+) {
+	if ( isOverlapping( points, index, newPosition ) ) {
+		return points;
+	}
+	const newPoint = {
+		...points[ index ],
+		position: newPosition,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param index    Index to update.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColor(
+	points: ControlPoint[],
+	index: number,
+	newColor: ControlPoint[ 'color' ]
+) {
+	const newPoint = {
+		...points[ index ],
+		color: newColor,
+	};
+	return updateControlPoint( points, index, newPoint );
+}
+
+/**
+ * Updates the position of a control point from an array and returns the new array.
+ *
+ * @param points   Array of control points.
+ * @param position Position of the color stop.
+ * @param newColor Color to update the control point at index.
+ *
+ * @return New array of control points.
+ */
+export function updateControlPointColorByPosition(
+	points: ControlPoint[],
+	position: ControlPoint[ 'position' ],
+	newColor: ControlPoint[ 'color' ]
+) {
+	const index = points.findIndex( ( point ) => point.position === position );
+	return updateControlPointColor( points, index, newColor );
+}
+
+/**
+ * Gets the horizontal coordinate when dragging a control point with the mouse.
+ *
+ * @param mouseXcoordinate Horizontal coordinate of the mouse position.
+ * @param containerElement Container for the gradient picker.
+ *
+ * @return Whole number percentage from the left.
+ */
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement
+): number;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: null
+): undefined;
+export function getHorizontalRelativeGradientPosition(
+	mouseXcoordinate: number,
+	containerElement: HTMLDivElement | null
+): number | undefined;
```

Love the change.

Hate that I didn't think of it myself.

Implemented: 465a59d285

### Review thread 28 (resolved)

#### ciampo on 2023-03-16T11:48:36Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1138527279
Path: packages/components/src/custom-gradient-picker/utils.ts

```diff
@@ -16,60 +14,73 @@ import {
 	DIRECTIONAL_ORIENTATION_ANGLE_MAP,
 } from './constants';
 import { serializeGradient } from './serializer';
+import type { ControlPoint } from './types';
 
 extend( [ namesPlugin ] );
 
-export function getLinearGradientRepresentation( gradientAST ) {
+export function getLinearGradientRepresentation(
+	gradientAST: gradientParser.GradientNode
+) {
 	return serializeGradient( {
 		type: 'linear-gradient',
-		orientation: HORIZONTAL_GRADIENT_ORIENTATION,
+		orientation: {
+			...HORIZONTAL_GRADIENT_ORIENTATION,
+			value: `${ HORIZONTAL_GRADIENT_ORIENTATION.value }`,
+		},
```

I believe we can undo this change, since we change the `value` property to be a string directly where `HORIZONTAL_GRADIENT_ORIENTATION` is declared

### Review thread 29 (resolved)

#### ciampo on 2023-03-16T12:15:30Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1138559938
Path: packages/components/src/palette-edit/types.ts

```diff
@@ -16,7 +16,7 @@ export type Color = {
 };
 
 export type Gradient = {
-	gradient: string;
+	gradient?: string;
```

I think that a better change would be to change the type of `CustomGradientPicker`'s `onChange` function, since from a quick look it seems that it can never have `undefined` as its argument:

```diff
diff --git a/packages/components/src/custom-gradient-picker/types.ts b/packages/components/src/custom-gradient-picker/types.ts
index 3d9d570382..9e241dd043 100644
--- a/packages/components/src/custom-gradient-picker/types.ts
+++ b/packages/components/src/custom-gradient-picker/types.ts
@@ -23,7 +23,7 @@ export type CustomGradientPickerProps = {
 	 * The function called when a new gradient has been defined. It is passed to
 	 * the `currentGradient` as an argument.
 	 */
-	onChange: ( currentGradient?: string ) => void;
+	onChange: ( currentGradient: string ) => void;
 	/**
 	 * Whether this is rendered in the sidebar.
 	 *
diff --git a/packages/components/src/palette-edit/index.tsx b/packages/components/src/palette-edit/index.tsx
index 7882950431..9b54c5ed0c 100644
--- a/packages/components/src/palette-edit/index.tsx
+++ b/packages/components/src/palette-edit/index.tsx
@@ -133,7 +133,7 @@ function ColorPickerPopover< T extends Color | Gradient >( {
 						__nextHasNoMargin
 						__experimentalIsRenderedInSidebar
 						value={ element.gradient }
-						onChange={ ( newGradient: Gradient[ 'gradient' ] ) => {
+						onChange={ ( newGradient ) => {
 							onChange( {
 								...element,
 								gradient: newGradient,

```

On a separate note, `PaletteEdit` types are quite convoluted due to the `Color | Gradient` union — I had a time-boxed go to see if I could simplify them further, but didn't manage to get to a good point on time

#### chad1008 on 2023-03-16T17:23:54Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1139103328
Path: packages/components/src/palette-edit/types.ts

```diff
@@ -16,7 +16,7 @@ export type Color = {
 };
 
 export type Gradient = {
-	gradient: string;
+	gradient?: string;
```

Sounds good to me! ef06890dee

### Review thread 30 (resolved)

#### ciampo on 2023-03-16T12:28:55Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1138578384
Path: packages/components/src/custom-gradient-picker/stories/index.tsx

```diff
@@ -8,16 +12,20 @@ import { useState } from '@wordpress/element';
  */
 import CustomGradientPicker from '../';
 
-export default {
+const meta: ComponentMeta< typeof CustomGradientPicker > = {
 	title: 'Components/CustomGradientPicker',
 	component: CustomGradientPicker,
-	argTypes: {
-		__nextHasNoMargin: { control: { type: 'boolean' } },
+	parameters: {
```

Let's add support for logging callbacks directly in Storybook by passing the `actions` property here, and by forwarding the `onChange` prop in the story code

```diff
diff --git a/packages/components/src/custom-gradient-picker/stories/index.tsx b/packages/components/src/custom-gradient-picker/stories/index.tsx
index 98c50b778b..3648db3abe 100644
--- a/packages/components/src/custom-gradient-picker/stories/index.tsx
+++ b/packages/components/src/custom-gradient-picker/stories/index.tsx
@@ -16,6 +16,7 @@ const meta: ComponentMeta< typeof CustomGradientPicker > = {
 	title: 'Components/CustomGradientPicker',
 	component: CustomGradientPicker,
 	parameters: {
+		actions: { argTypesRegex: '^on.*' },
 		controls: { expanded: true },
 		docs: { source: { state: 'open' } },
 	},
@@ -24,13 +25,16 @@ export default meta;
 
 const CustomGradientPickerWithState: ComponentStory<
 	typeof CustomGradientPicker
-> = ( props ) => {
+> = ( { onChange, ...props } ) => {
 	const [ gradient, setGradient ] = useState< string >();
 	return (
 		<CustomGradientPicker
 			{ ...props }
 			value={ gradient }
-			onChange={ setGradient }
+			onChange={ ( newGradient ) => {
+				setGradient( newGradient );
+				onChange( newGradient );
+			} }
 		/>
 	);
 };

```

#### chad1008 on 2023-03-16T17:26:30Z

URL: https://github.com/WordPress/gutenberg/pull/48929#discussion_r1139107645
Path: packages/components/src/custom-gradient-picker/stories/index.tsx

```diff
@@ -8,16 +12,20 @@ import { useState } from '@wordpress/element';
  */
 import CustomGradientPicker from '../';
 
-export default {
+const meta: ComponentMeta< typeof CustomGradientPicker > = {
 	title: 'Components/CustomGradientPicker',
 	component: CustomGradientPicker,
-	argTypes: {
-		__nextHasNoMargin: { control: { type: 'boolean' } },
+	parameters: {
```

✅ d0f1d81195

## Changed files

- package-lock.json (MODIFIED, +5/-0)
- package.json (MODIFIED, +1/-0)
- packages/components/CHANGELOG.md (MODIFIED, +7/-3)
- packages/components/src/custom-gradient-picker/constants.ts (RENAMED, +2/-2)
- packages/components/src/custom-gradient-picker/gradient-bar/constants.ts (RENAMED, +0/-0)
- packages/components/src/custom-gradient-picker/gradient-bar/control-points.tsx (RENAMED, +171/-134)
- packages/components/src/custom-gradient-picker/gradient-bar/index.tsx (RENAMED, +25/-9)
- packages/components/src/custom-gradient-picker/gradient-bar/test/utils.ts (RENAMED, +58/-31)
- packages/components/src/custom-gradient-picker/gradient-bar/utils.js (DELETED, +0/-189)
- packages/components/src/custom-gradient-picker/gradient-bar/utils.ts (ADDED, +193/-0)
- packages/components/src/custom-gradient-picker/index.tsx (RENAMED, +63/-21)
- packages/components/src/custom-gradient-picker/serializer.js (DELETED, +0/-48)
- packages/components/src/custom-gradient-picker/serializer.ts (ADDED, +82/-0)
- packages/components/src/custom-gradient-picker/stories/index.js (DELETED, +0/-33)
- packages/components/src/custom-gradient-picker/stories/index.tsx (ADDED, +45/-0)
- packages/components/src/custom-gradient-picker/styles/custom-gradient-picker-styles.tsx (RENAMED, +0/-0)
- packages/components/src/custom-gradient-picker/test/serializer.ts (RENAMED, +39/-33)
- packages/components/src/custom-gradient-picker/types.ts (ADDED, +137/-0)
- packages/components/src/custom-gradient-picker/utils.ts (RENAMED, +33/-22)
- packages/components/src/palette-edit/index.tsx (MODIFIED, +1/-1)
- packages/components/src/palette-edit/types.ts (MODIFIED, +1/-1)
