---
number: 27374
type: pull_request
state: MERGED
merged_at: 2021-10-16T00:02:32Z
url: https://github.com/WordPress/gutenberg/pull/27374
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components","First-time Contributor"]
---

# PR #27374: Repositioned RangeControl tooltip and adjusted image zoom control dropdown height

- URL: https://github.com/WordPress/gutenberg/pull/27374
- Author: Tonnie-Exelero
- Created: 2020-11-30T11:02:34Z
- Updated: 2022-01-24T18:32:29Z
- Comments: 2 of 2
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/27374.diff

## Body

## Description
- Repositioned RangeControl tooltip to the bottom so that it doesn't obscure labels.
- Adjusted Image block zoom control dropdown height to accommodate RangeControl tooltip's new position.

## How has this been tested?
- I've tested the changes on Chrome and simulated different devices and it works as intended.
- I've run unit tests.

## Screenshots 
<img width="280" alt="Screenshot 2020-11-30 at 03 18 14" src="https://user-images.githubusercontent.com/23191929/100557996-68990a80-32bd-11eb-821b-7319c9016b1a.png">


## Types of changes
This is a Bug Fix that introduces changes that affect the positioning of the RangeControl's tooltip to prevent it from obscuring labels. 

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

Fixes [#27261](https://github.com/WordPress/gutenberg/issues/27261)

## Issue comments

### github-actions on 2021-10-16T00:03:01Z

URL: https://github.com/WordPress/gutenberg/pull/27374#issuecomment-944817084

Congratulations on your first merged pull request, @Tonnie-Exelero! We'd like to credit you for your contribution in the post announcing the next WordPress release, but we can't find a WordPress.org profile associated with your GitHub account. When you have a moment, visit the following URL and click "link your GitHub account" under "GitHub Username" to link your accounts:

https://profiles.wordpress.org/me/profile/edit/

And if you don't have a WordPress.org account, you can create one on this page:

https://login.wordpress.org/register

Kudos!

### jeffpaul on 2022-01-24T18:32:29Z

URL: https://github.com/WordPress/gutenberg/pull/27374#issuecomment-1020414254

@Tonnie-Exelero pinging you again to see if you can help confirm your WordPress.org username so you can be properly credited in the WordPress 5.9 release? Thanks!

## Review threads

### Review thread 1 (resolved)

#### talldan on 2020-12-04T09:13:56Z

URL: https://github.com/WordPress/gutenberg/pull/27374#discussion_r535948060
Path: packages/block-library/src/image/editor.scss

```diff
@@ -107,6 +107,7 @@ figure.wp-block-image:not(.wp-block) {
 	.components-popover__content {
 		overflow: visible;
 		min-width: 260px;
+		min-height: 110px;
```

The increased size doesn't look quite right when the input is focused and the tooltip isn't visible:
<img width="266" alt="Screenshot 2020-12-04 at 5 12 38 pm" src="https://user-images.githubusercontent.com/677833/101144900-f8d7a680-3653-11eb-8e8f-3f0522e2aa08.png">

My feeling is that increasing the size of the popover isn't needed, it looks ok to me when the tooltip is overlapping the edge of the popover:
<img width="268" alt="Screenshot 2020-12-04 at 5 09 54 pm" src="https://user-images.githubusercontent.com/677833/101144769-c9289e80-3653-11eb-967a-0f9564a0bb6d.png">

#### Tonnie-Exelero on 2020-12-08T18:14:33Z

URL: https://github.com/WordPress/gutenberg/pull/27374#discussion_r538683021
Path: packages/block-library/src/image/editor.scss

```diff
@@ -107,6 +107,7 @@ figure.wp-block-image:not(.wp-block) {
 	.components-popover__content {
 		overflow: visible;
 		min-width: 260px;
+		min-height: 110px;
```

Great, I concur @talldan. I'm removing the height adjustment in a moment.

### Review thread 2 (resolved)

#### talldan on 2020-12-04T09:24:18Z

URL: https://github.com/WordPress/gutenberg/pull/27374#discussion_r535954774
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -232,16 +232,16 @@ const tooltipShow = ( { show } ) => {
 };
 
 const tooltipPosition = ( { position } ) => {
-	const isTop = position === 'top';
+	const isBottom = position === 'bottom';
 
-	if ( isTop ) {
+	if ( isBottom ) {
 		return css`
-			top: -80%;
+			bottom: -80%;
 		`;
 	}
 
 	return css`
-		bottom: -80%;
+		top: -80%;
```

I'm not sure all of the changes here are needed. It looks to me like it should be possible to add a `position="bottom"` prop to `SimpleTooltip` here:
https://github.com/WordPress/gutenberg/blob/149502cc880594ba3608538c57145ac586d24bf3/packages/components/src/range-control/index.js#L258-L265

That gets passed through to `Tooltip` and then to the `useTooltipPosition` hook, and finally on to `tooltipPosition`.

#### Tonnie-Exelero on 2020-12-08T22:15:57Z

URL: https://github.com/WordPress/gutenberg/pull/27374#discussion_r538848393
Path: packages/components/src/range-control/styles/range-control-styles.js

```diff
@@ -232,16 +232,16 @@ const tooltipShow = ( { show } ) => {
 };
 
 const tooltipPosition = ( { position } ) => {
-	const isTop = position === 'top';
+	const isBottom = position === 'bottom';
 
-	if ( isTop ) {
+	if ( isBottom ) {
 		return css`
-			top: -80%;
+			bottom: -80%;
 		`;
 	}
 
 	return css`
-		bottom: -80%;
+		top: -80%;
```

Right, this makes sense.

### Review thread 3 (resolved)

#### talldan on 2020-12-04T09:29:25Z

URL: https://github.com/WordPress/gutenberg/pull/27374#discussion_r535958175
Path: packages/components/src/range-control/tooltip.js

```diff
@@ -49,17 +49,17 @@ export default function SimpleTooltip( {
 }
 
 function useTooltipPosition( { inputRef, position: positionProp } ) {
-	const [ position, setPosition ] = useState( 'top' );
+	const [ position, setPosition ] = useState( 'bottom' );
 
 	const calculatePosition = useCallback( () => {
 		if ( inputRef && inputRef.current ) {
 			let nextPosition = positionProp;
 
 			if ( positionProp === 'auto' ) {
-				const { top } = inputRef.current.getBoundingClientRect();
-				const isOffscreenTop = top - TOOLTIP_OFFSET_HEIGHT < 0;
+				const { bottom } = inputRef.current.getBoundingClientRect();
+				const isOffscreenBottom = bottom - TOOLTIP_OFFSET_HEIGHT < 0;
 
-				nextPosition = isOffscreenTop ? 'bottom' : 'top';
+				nextPosition = isOffscreenBottom ? 'top' : 'bottom';
```

Similar to the previous comment, I'm not sure this is needed if `position` is added as a prop to `SimpleTooltip`. It looks like this hook will bypass all the logic in the inner `if` statement if `positionProp` has a value other than `auto`.

#### Tonnie-Exelero on 2020-12-08T22:19:00Z

URL: https://github.com/WordPress/gutenberg/pull/27374#discussion_r538850399
Path: packages/components/src/range-control/tooltip.js

```diff
@@ -49,17 +49,17 @@ export default function SimpleTooltip( {
 }
 
 function useTooltipPosition( { inputRef, position: positionProp } ) {
-	const [ position, setPosition ] = useState( 'top' );
+	const [ position, setPosition ] = useState( 'bottom' );
 
 	const calculatePosition = useCallback( () => {
 		if ( inputRef && inputRef.current ) {
 			let nextPosition = positionProp;
 
 			if ( positionProp === 'auto' ) {
-				const { top } = inputRef.current.getBoundingClientRect();
-				const isOffscreenTop = top - TOOLTIP_OFFSET_HEIGHT < 0;
+				const { bottom } = inputRef.current.getBoundingClientRect();
+				const isOffscreenBottom = bottom - TOOLTIP_OFFSET_HEIGHT < 0;
 
-				nextPosition = isOffscreenTop ? 'bottom' : 'top';
+				nextPosition = isOffscreenBottom ? 'top' : 'bottom';
```

Do we really need an `auto` position? I don't see the need there. The only positions making sense to me are `bottom` and `top`. I stand corrected though.

## Changed files

- packages/components/src/range-control/index.js (MODIFIED, +1/-0)
- packages/components/src/range-control/styles/range-control-styles.js (MODIFIED, +4/-4)
- packages/components/src/range-control/tooltip.js (MODIFIED, +11/-22)
