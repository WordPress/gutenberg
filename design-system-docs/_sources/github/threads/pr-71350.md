---
number: 71350
type: pull_request
state: MERGED
merged_at: 2025-09-10T11:20:06Z
url: https://github.com/WordPress/gutenberg/pull/71350
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #71350: Add support for ValidatedFormTokenField

- URL: https://github.com/WordPress/gutenberg/pull/71350
- Author: elazzabi
- Created: 2025-08-26T09:53:24Z
- Updated: 2025-09-10T11:20:46Z
- Comments: 4 of 4
- Review threads: 11 of 11
- Changed files: 5 of 5
- Diff: ../pr-diffs/71350.diff

## Body

https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
This adds support for the ValidatedArrayControl 


## Why?

This add a validated control to be used in different parts of the admin, including the country selector.

## How?

https://github.com/user-attachments/assets/42d3292c-6544-4cc7-810b-5c419bd1d8fd

## Testing Instructions
1. Get this PR locally
2. Start Storybook in dev mode
3. Visit `ValidatedInputControl` and scroll down to Array Control
4. Play with the component

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

Included above

## Issue comments

### github-actions on 2025-08-26T09:53:33Z

URL: https://github.com/WordPress/gutenberg/pull/71350#issuecomment-3223469558

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: elazzabi <elazzabi@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: oandregal <oandregal@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### elazzabi on 2025-08-26T09:54:48Z

URL: https://github.com/WordPress/gutenberg/pull/71350#issuecomment-3223473717

cc @mirka

### elazzabi on 2025-09-09T16:11:38Z

URL: https://github.com/WordPress/gutenberg/pull/71350#issuecomment-3271390995

Looks good after the recent changes, thank you for your help @mirka !

https://github.com/user-attachments/assets/6ebf8ca4-0b1b-4b9c-a70e-ba5684978bdd

### elazzabi on 2025-09-10T10:13:45Z

URL: https://github.com/WordPress/gutenberg/pull/71350#issuecomment-3274272493

Thank you, @mirka ! I agree, I pushed a change to remove the forwardref in https://github.com/WordPress/gutenberg/pull/71350/commits/94948260d5fcbc847e719945342589626d6cf00a 🙏 

EDIT: Looks like I need at least these to make the testing pass 4603300347bd77845238b16257aefb9e49b9391e

## Review threads

### Review thread 1 (resolved)

#### oandregal on 2025-08-28T16:25:36Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2307930535
Path: packages/dataviews/CHANGELOG.md

```diff
@@ -15,6 +15,7 @@
 - Add `enableMoving` option to the `table` layout to allow or disallow column moving left and right. [#71120](https://github.com/WordPress/gutenberg/pull/71120)
 - Add infinite scroll support across all layout types (grid, list, table). Enable infinite scroll by providing an `infiniteScrollHandler` function in the `paginationInfo` prop and toggling the feature in the view configuration. ([#70955](https://github.com/WordPress/gutenberg/pull/70955))
 - Add support for modal in DataForm panel layouts. [#71212](https://github.com/WordPress/gutenberg/pull/71212)
+- Add support for ValidatedArrayControl [#71350](https://github.com/WordPress/gutenberg/pull/71350)
```

This need to be moved to `packages/COMPONENTS/changelog`.

#### elazzabi on 2025-08-29T14:20:25Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2310306709
Path: packages/dataviews/CHANGELOG.md

```diff
@@ -15,6 +15,7 @@
 - Add `enableMoving` option to the `table` layout to allow or disallow column moving left and right. [#71120](https://github.com/WordPress/gutenberg/pull/71120)
 - Add infinite scroll support across all layout types (grid, list, table). Enable infinite scroll by providing an `infiniteScrollHandler` function in the `paginationInfo` prop and toggling the feature in the view configuration. ([#70955](https://github.com/WordPress/gutenberg/pull/70955))
 - Add support for modal in DataForm panel layouts. [#71212](https://github.com/WordPress/gutenberg/pull/71212)
+- Add support for ValidatedArrayControl [#71350](https://github.com/WordPress/gutenberg/pull/71350)
```

Thanks! I moved it here c948fb1b51631f2b924de2f1ac0e9d8a92ca78d7

### Review thread 2 (resolved)

#### mirka on 2025-08-28T19:28:24Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2308344424
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedArrayControl = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLDivElement >
+) => {
+	const valueRef = useRef< Value >( restProps.value );
+	const formTokenFieldRef = useRef< HTMLDivElement >( null );
+
+	// Create a function to find the input element within the FormTokenField
+	const getInputElement = () => {
+		return formTokenFieldRef.current?.querySelector(
+			'.components-form-token-field__input'
+		) as HTMLInputElement | null;
+	};
+
+	return (
+		<ControlWithError
+			ref={ forwardedRef }
+			required={ required }
+			markWhenOptional={ markWhenOptional }
+			onValidate={ () => {
+				return onValidate?.( valueRef.current );
+			} }
+			customValidity={ customValidity }
+			getValidityTarget={ getInputElement }
+		>
+			<div ref={ formTokenFieldRef }>
+				<FormTokenField
+					__next40pxDefaultSize
+					__nextHasNoMarginBottom
+					onChange={ ( value, ...args ) => {
+						valueRef.current = value;
+						onChange?.( value, ...args );
+					} }
+					{ ...restProps }
+				/>
+			</div>
+		</ControlWithError>
+	);
+};
+
+export const ValidatedArrayControl = forwardRef(
```

I see you probably tried to match the name with the other controls ending with `Control`, but unfortunately `FormTokenField` was originally named irregularly like this and we have to keep it 🥲 Let's name it `ValidatedFormTokenField` so it's clear what component it is.

#### elazzabi on 2025-08-29T14:20:05Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2310305948
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedArrayControl = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLDivElement >
+) => {
+	const valueRef = useRef< Value >( restProps.value );
+	const formTokenFieldRef = useRef< HTMLDivElement >( null );
+
+	// Create a function to find the input element within the FormTokenField
+	const getInputElement = () => {
+		return formTokenFieldRef.current?.querySelector(
+			'.components-form-token-field__input'
+		) as HTMLInputElement | null;
+	};
+
+	return (
+		<ControlWithError
+			ref={ forwardedRef }
+			required={ required }
+			markWhenOptional={ markWhenOptional }
+			onValidate={ () => {
+				return onValidate?.( valueRef.current );
+			} }
+			customValidity={ customValidity }
+			getValidityTarget={ getInputElement }
+		>
+			<div ref={ formTokenFieldRef }>
+				<FormTokenField
+					__next40pxDefaultSize
+					__nextHasNoMarginBottom
+					onChange={ ( value, ...args ) => {
+						valueRef.current = value;
+						onChange?.( value, ...args );
+					} }
+					{ ...restProps }
+				/>
+			</div>
+		</ControlWithError>
+	);
+};
+
+export const ValidatedArrayControl = forwardRef(
```

Gotcha! Renamed in 2b869a81d8f7158b1d1a21e3ccc6450829c9fddb

### Review thread 3 (resolved)

#### mirka on 2025-08-28T19:33:45Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2308358770
Path: packages/components/src/validated-form-controls/components/stories/input-control.story.tsx

```diff
@@ -164,3 +166,53 @@ Password.argTypes = {
 	suffix: { control: false },
 	type: { control: false },
 };
+
+/**
+ * This demonstrates how array validation would work with the ValidatedArrayControl component.
+ */
+export const ArrayControl: StoryObj< typeof ValidatedArrayControl > = {
```

Why was this tacked onto the page for InputControl and not on its own component page?

#### elazzabi on 2025-08-29T14:21:29Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2310310148
Path: packages/components/src/validated-form-controls/components/stories/input-control.story.tsx

```diff
@@ -164,3 +166,53 @@ Password.argTypes = {
 	suffix: { control: false },
 	type: { control: false },
 };
+
+/**
+ * This demonstrates how array validation would work with the ValidatedArrayControl component.
+ */
+export const ArrayControl: StoryObj< typeof ValidatedArrayControl > = {
```

At first, I thought that, given it's an input, it would be good to have it on the input page. But after your comment, I saw we have a separate page for every input type, so I moved it to a separate page as you suggested a45ebff65a631f095bcc16d741bc8aa13988473a

### Review thread 4

#### mirka on 2025-08-28T20:00:32Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2308407912
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedArrayControl = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLDivElement >
+) => {
+	const valueRef = useRef< Value >( restProps.value );
+	const formTokenFieldRef = useRef< HTMLDivElement >( null );
+
+	// Create a function to find the input element within the FormTokenField
+	const getInputElement = () => {
+		return formTokenFieldRef.current?.querySelector(
+			'.components-form-token-field__input'
+		) as HTMLInputElement | null;
+	};
+
+	return (
+		<ControlWithError
+			ref={ forwardedRef }
+			required={ required }
+			markWhenOptional={ markWhenOptional }
+			onValidate={ () => {
+				return onValidate?.( valueRef.current );
+			} }
+			customValidity={ customValidity }
+			getValidityTarget={ getInputElement }
+		>
+			<div ref={ formTokenFieldRef }>
```

Certain props are passed down from `ControlWithError` to the inner control, so this simple div wrapper is not going to be sufficient. (I notice this wasn't mentioned anywhere in the documentation, so I'll add it at #71392.)

https://github.com/WordPress/gutenberg/blob/775f0cea9ea3b0a5ea8a39982475f82a9a6ddae0/packages/components/src/validated-form-controls/control-with-error.tsx#L223-L231

As for the ref passing, we can now make changes to the `@wordpress/components` components directly (unlike when I was working on the other controls 😅), so I think it would be cleaner to enhance [`FormTokenField` itself](https://github.com/WordPress/gutenberg/blob/775f0cea9ea3b0a5ea8a39982475f82a9a6ddae0/packages/components/src/form-token-field/index.tsx#L47) so it forwards a ref to the [underlying `input`](https://github.com/WordPress/gutenberg/blob/775f0cea9ea3b0a5ea8a39982475f82a9a6ddae0/packages/components/src/form-token-field/token-input.tsx#L57). Right now `FormTokenField` doesn't take a ref at all. We should also enhance it so the `required` prop on `FormTokenField`would also be passed down to the underlying `input`. This would allow us to be less hacky in this implementation. Could you try that?

#### elazzabi on 2025-09-01T15:09:41Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2314198625
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedArrayControl = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLDivElement >
+) => {
+	const valueRef = useRef< Value >( restProps.value );
+	const formTokenFieldRef = useRef< HTMLDivElement >( null );
+
+	// Create a function to find the input element within the FormTokenField
+	const getInputElement = () => {
+		return formTokenFieldRef.current?.querySelector(
+			'.components-form-token-field__input'
+		) as HTMLInputElement | null;
+	};
+
+	return (
+		<ControlWithError
+			ref={ forwardedRef }
+			required={ required }
+			markWhenOptional={ markWhenOptional }
+			onValidate={ () => {
+				return onValidate?.( valueRef.current );
+			} }
+			customValidity={ customValidity }
+			getValidityTarget={ getInputElement }
+		>
+			<div ref={ formTokenFieldRef }>
```

Thank you for the review!

I looked into this, and this component is somewhat more challenging than the others. Since the checks for the required field only verify the input's value (i.e., not the tokens), it would always trigger an error, even when the field contains tokens.

I made some changes to disable the default `required` checks, but I'm unsure if this is the best approach or if you have other suggestions I can try.

Thank you!

#### mirka on 2025-09-03T16:48:46Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2319548153
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedArrayControl = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLDivElement >
+) => {
+	const valueRef = useRef< Value >( restProps.value );
+	const formTokenFieldRef = useRef< HTMLDivElement >( null );
+
+	// Create a function to find the input element within the FormTokenField
+	const getInputElement = () => {
+		return formTokenFieldRef.current?.querySelector(
+			'.components-form-token-field__input'
+		) as HTMLInputElement | null;
+	};
+
+	return (
+		<ControlWithError
+			ref={ forwardedRef }
+			required={ required }
+			markWhenOptional={ markWhenOptional }
+			onValidate={ () => {
+				return onValidate?.( valueRef.current );
+			} }
+			customValidity={ customValidity }
+			getValidityTarget={ getInputElement }
+		>
+			<div ref={ formTokenFieldRef }>
```

I see! This has happened on two other components ([`ToggleGroupControl`](https://github.com/WordPress/gutenberg/blob/37da29577ddf2fb5b790a1df0a931312ce1d50cf/packages/components/src/validated-form-controls/components/toggle-group-control.tsx#L60) and [`CustomSelectControl`](https://github.com/WordPress/gutenberg/blob/37da29577ddf2fb5b790a1df0a931312ce1d50cf/packages/components/src/validated-form-controls/components/custom-select-control.tsx#L64)), and I worked around it by using a hidden ["delegate" element](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/validated-form-controls/components/stories/overview.mdx#delegate-elements). At the time I thought it was hacky, but I later discovered that at least [one other library](https://base-ui.com/) uses a similar technique for _all_ their validated form components, so I guess it isn't that weird.

I think we could try a normal `<input type="text">` delegate here, since the built-in error message for `required` ("Please fill out this field.") is generic enough for `FormTokenField`. A `<select multiple>` delegate would be a better match for our array data type, but the built-in error message ("Please select an item in the list.") doesn't fit cases where `FormTokenField` can accept new items generated by the user.

Basically you append a hidden `<input value={ hasTokens ? "has value" : "" }>` or something so the `required` check reflects the selected token state. Does that make sense?

#### elazzabi on 2025-09-08T14:02:14Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2330352190
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedArrayControl = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLDivElement >
+) => {
+	const valueRef = useRef< Value >( restProps.value );
+	const formTokenFieldRef = useRef< HTMLDivElement >( null );
+
+	// Create a function to find the input element within the FormTokenField
+	const getInputElement = () => {
+		return formTokenFieldRef.current?.querySelector(
+			'.components-form-token-field__input'
+		) as HTMLInputElement | null;
+	};
+
+	return (
+		<ControlWithError
+			ref={ forwardedRef }
+			required={ required }
+			markWhenOptional={ markWhenOptional }
+			onValidate={ () => {
+				return onValidate?.( valueRef.current );
+			} }
+			customValidity={ customValidity }
+			getValidityTarget={ getInputElement }
+		>
+			<div ref={ formTokenFieldRef }>
```

Gotcha! Thank you 🙏 I pushed changes in https://github.com/WordPress/gutenberg/pull/71350/commits/b6c9b91a89e5bbbb5e486d71aceb5a060995491e, let me know how that looks

### Review thread 5

#### mirka on 2025-08-28T20:36:37Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2308490326
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
```

I noticed some Sass variable handling is wrong in the stylesheet, so I'll fix it in #71391. With that in mind though, I think we'll need some error styles for `FormTokenField` as well, similar to what we have for `ComboboxControl`.

https://github.com/WordPress/gutenberg/blob/775f0cea9ea3b0a5ea8a39982475f82a9a6ddae0/packages/components/src/validated-form-controls/style.scss#L16-L21

#### mirka on 2025-09-09T15:50:59Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334084450
Path: packages/components/src/validated-form-controls/components/array-control.tsx

```diff
@@ -0,0 +1,69 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
```

I pushed some styles we can use in abc76d7403bbd1f47787046a4b6bacb5ed70a3a2.

### Review thread 6 (resolved)

#### mirka on 2025-09-09T15:09:18Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2333960073
Path: packages/components/src/validated-form-controls/components/index.ts

```diff
@@ -1,3 +1,4 @@
+export * from './form-token-field';
```

Nit: Let's keep this sorted alphabetically.

#### elazzabi on 2025-09-09T16:07:01Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334124683
Path: packages/components/src/validated-form-controls/components/index.ts

```diff
@@ -1,3 +1,4 @@
+export * from './form-token-field';
```

Fixed in 9b8e7ccfd3dfc73908339b4102f394c36b7bb242

### Review thread 7 (resolved)

#### mirka on 2025-09-09T15:10:01Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2333962294
Path: packages/components/src/validated-form-controls/components/stories/input-control.story.tsx

Let's remove this change now.

#### elazzabi on 2025-09-09T16:06:57Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334124559
Path: packages/components/src/validated-form-controls/components/stories/input-control.story.tsx

Agree! Removed in 03cf1e04abde2a071e2ab3886b63e4638e78cac1

### Review thread 8 (resolved)

#### mirka on 2025-09-09T15:16:37Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2333983595
Path: packages/components/src/validated-form-controls/components/form-token-field.tsx

```diff
@@ -0,0 +1,86 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef, useId } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedFormTokenField = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLInputElement >
+) => {
+	const validityTargetRef = useRef< HTMLInputElement >( null );
+	const valueRef = useRef< Value >( restProps.value );
+
+	const nameAttr = useId();
+
+	return (
+		<div className="components-validated-control__wrapper-with-error-delegate">
+			<ControlWithError
+				required={ required }
+				markWhenOptional={ markWhenOptional }
+				onValidate={ () => {
+					return onValidate?.( valueRef.current );
+				} }
+				customValidity={ customValidity }
+				getValidityTarget={ () => validityTargetRef.current }
+			>
+				<FormTokenField
+					__next40pxDefaultSize
+					__nextHasNoMarginBottom
+					ref={ forwardedRef }
+					{ ...restProps }
+					onChange={ ( value, ...args ) => {
+						valueRef.current = value;
+						onChange?.( value, ...args );
+					} }
+				/>
+			</ControlWithError>
+			<input
+				className="components-validated-control__error-delegate"
+				type="text"
+				ref={ validityTargetRef }
+				required={ required }
+				value={
+					valueRef.current && valueRef.current.length > 0
+						? 'hasvalue'
+						: ''
+				}
+				tabIndex={ -1 }
+				// A name attribute is needed for the `required` behavior to work.
+				name={ nameAttr }
```

This `name` was necessary for the `radio` case, but not necessary here.

#### elazzabi on 2025-09-09T16:06:53Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334124396
Path: packages/components/src/validated-form-controls/components/form-token-field.tsx

```diff
@@ -0,0 +1,86 @@
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useRef, useId } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { ControlWithError } from '../control-with-error';
+import type { ValidatedControlProps } from './types';
+import { FormTokenField } from '../../form-token-field';
+import type { FormTokenFieldProps } from '../../form-token-field/types';
+
+type Value = FormTokenFieldProps[ 'value' ];
+
+const UnforwardedValidatedFormTokenField = (
+	{
+		required,
+		onValidate,
+		customValidity,
+		onChange,
+		markWhenOptional,
+		...restProps
+	}: Omit<
+		React.ComponentProps< typeof FormTokenField >,
+		'__next40pxDefaultSize' | '__nextHasNoMarginBottom'
+	> &
+		ValidatedControlProps< FormTokenFieldProps[ 'value' ] >,
+	forwardedRef: React.ForwardedRef< HTMLInputElement >
+) => {
+	const validityTargetRef = useRef< HTMLInputElement >( null );
+	const valueRef = useRef< Value >( restProps.value );
+
+	const nameAttr = useId();
+
+	return (
+		<div className="components-validated-control__wrapper-with-error-delegate">
+			<ControlWithError
+				required={ required }
+				markWhenOptional={ markWhenOptional }
+				onValidate={ () => {
+					return onValidate?.( valueRef.current );
+				} }
+				customValidity={ customValidity }
+				getValidityTarget={ () => validityTargetRef.current }
+			>
+				<FormTokenField
+					__next40pxDefaultSize
+					__nextHasNoMarginBottom
+					ref={ forwardedRef }
+					{ ...restProps }
+					onChange={ ( value, ...args ) => {
+						valueRef.current = value;
+						onChange?.( value, ...args );
+					} }
+				/>
+			</ControlWithError>
+			<input
+				className="components-validated-control__error-delegate"
+				type="text"
+				ref={ validityTargetRef }
+				required={ required }
+				value={
+					valueRef.current && valueRef.current.length > 0
+						? 'hasvalue'
+						: ''
+				}
+				tabIndex={ -1 }
+				// A name attribute is needed for the `required` behavior to work.
+				name={ nameAttr }
```

Gotcha! Removed in 98a55a88b344c6a0142df0716b275a10c1b5c9b8

### Review thread 9

#### mirka on 2025-09-09T15:34:46Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334036056
Path: packages/components/src/form-token-field/index.tsx

I think we can revert the changes here in `FormTokenField`, since we're going with the delegate now. Passing down the `required` prop is actually problematic now, because it creates a required input that can never be filled 😄

#### elazzabi on 2025-09-09T16:06:39Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334123832
Path: packages/components/src/form-token-field/index.tsx

I think only the `required` passing can be reverted, not the whole file. Done in ba0bd0f04f5dffa4c40dd29b7c395f4379d2690d

#### mirka on 2025-09-10T09:56:50Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2336227536
Path: packages/components/src/form-token-field/index.tsx

I don't think the ref forwarding is strictly necessary for the functionality in this PR? It's more of an unrelated enhancement for `FormTokenField` at this point. No strong opinion though.

### Review thread 10 (resolved)

#### mirka on 2025-09-09T15:35:18Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334037497
Path: packages/components/src/form-token-field/types.ts

Also revertable.

#### elazzabi on 2025-09-09T16:06:33Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334123618
Path: packages/components/src/form-token-field/types.ts

Fixed in ba0bd0f04f5dffa4c40dd29b7c395f4379d2690d

### Review thread 11 (resolved)

#### mirka on 2025-09-09T15:38:16Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334045469
Path: packages/components/src/validated-form-controls/components/stories/form-token-field.story.tsx

```diff
@@ -0,0 +1,82 @@
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * External dependencies
+ */
+import type { StoryObj, Meta } from '@storybook/react';
+
+/**
+ * Internal dependencies
+ */
+import { ValidatedFormTokenField } from '../form-token-field';
+import { formDecorator } from './story-utils';
+import type { TokenItem } from '../../../form-token-field/types';
+
+const meta: Meta< typeof ValidatedFormTokenField > = {
+	title: 'Components/Selection & Input/Validated Form Controls/ValidatedFormTokenField',
+	id: 'components-validatedformtokenfield',
+	component: ValidatedFormTokenField,
+	tags: [ 'status-private' ],
+	decorators: formDecorator,
+	args: { onChange: () => {} },
+	argTypes: {
+		onChange: { control: false },
+		value: { control: false },
+		customValidity: { control: false },
+	},
+};
+export default meta;
+
+/**
+ * This demonstrates how array validation would work with the ValidatedFormTokenField component.
+ */
+export const Default: StoryObj< typeof ValidatedFormTokenField > = {
+	render: function Template( { onChange, ...args } ) {
+		const [ value, setValue ] = useState< ( string | TokenItem )[] >( [] );
+		const [ customValidity, setCustomValidity ] =
+			useState<
+				React.ComponentProps<
+					typeof ValidatedFormTokenField
+				>[ 'customValidity' ]
+			>( undefined );
+
+		return (
+			<ValidatedFormTokenField
+				{ ...args }
+				value={ value }
+				onChange={ ( newValue, ...rest ) => {
+					setValue( newValue );
+					onChange?.( newValue, ...rest );
+				} }
+				onValidate={ ( v ) => {
+					if (
+						v?.some( ( token ) => {
+							const tokenValue =
+								typeof token === 'string' ? token : token.value;
+							return tokenValue.toLowerCase() === 'error';
+						} )
+					) {
+						setCustomValidity( {
+							type: 'invalid',
+							message: 'The tag "error" is not allowed.',
+						} );
+						return;
+					}
+
+					setCustomValidity( undefined );
+				} }
+				customValidity={ customValidity }
+			/>
+		);
+	},
+};
+
+Default.args = {
+	required: true,
+	label: 'Tags',
+	placeholder: 'Add tags...',
+	suggestions: [ 'Posts', 'Pages', 'Media', 'Error' ],
```

```suggestion
	suggestions: [ 'Posts', 'Pages', 'Media', 'Error' ],
	__experimentalExpandOnFocus: true,
```

We can add this again for discoverability.

#### elazzabi on 2025-09-09T16:06:25Z

URL: https://github.com/WordPress/gutenberg/pull/71350#discussion_r2334123322
Path: packages/components/src/validated-form-controls/components/stories/form-token-field.story.tsx

```diff
@@ -0,0 +1,82 @@
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * External dependencies
+ */
+import type { StoryObj, Meta } from '@storybook/react';
+
+/**
+ * Internal dependencies
+ */
+import { ValidatedFormTokenField } from '../form-token-field';
+import { formDecorator } from './story-utils';
+import type { TokenItem } from '../../../form-token-field/types';
+
+const meta: Meta< typeof ValidatedFormTokenField > = {
+	title: 'Components/Selection & Input/Validated Form Controls/ValidatedFormTokenField',
+	id: 'components-validatedformtokenfield',
+	component: ValidatedFormTokenField,
+	tags: [ 'status-private' ],
+	decorators: formDecorator,
+	args: { onChange: () => {} },
+	argTypes: {
+		onChange: { control: false },
+		value: { control: false },
+		customValidity: { control: false },
+	},
+};
+export default meta;
+
+/**
+ * This demonstrates how array validation would work with the ValidatedFormTokenField component.
+ */
+export const Default: StoryObj< typeof ValidatedFormTokenField > = {
+	render: function Template( { onChange, ...args } ) {
+		const [ value, setValue ] = useState< ( string | TokenItem )[] >( [] );
+		const [ customValidity, setCustomValidity ] =
+			useState<
+				React.ComponentProps<
+					typeof ValidatedFormTokenField
+				>[ 'customValidity' ]
+			>( undefined );
+
+		return (
+			<ValidatedFormTokenField
+				{ ...args }
+				value={ value }
+				onChange={ ( newValue, ...rest ) => {
+					setValue( newValue );
+					onChange?.( newValue, ...rest );
+				} }
+				onValidate={ ( v ) => {
+					if (
+						v?.some( ( token ) => {
+							const tokenValue =
+								typeof token === 'string' ? token : token.value;
+							return tokenValue.toLowerCase() === 'error';
+						} )
+					) {
+						setCustomValidity( {
+							type: 'invalid',
+							message: 'The tag "error" is not allowed.',
+						} );
+						return;
+					}
+
+					setCustomValidity( undefined );
+				} }
+				customValidity={ customValidity }
+			/>
+		);
+	},
+};
+
+Default.args = {
+	required: true,
+	label: 'Tags',
+	placeholder: 'Add tags...',
+	suggestions: [ 'Posts', 'Pages', 'Media', 'Error' ],
```

Good idea! Pushed in 200d26316f5d93aa9a40c33bf5c9523caeaded5e

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/validated-form-controls/components/form-token-field.tsx (ADDED, +84/-0)
- packages/components/src/validated-form-controls/components/index.ts (MODIFIED, +1/-0)
- packages/components/src/validated-form-controls/components/stories/form-token-field.story.tsx (ADDED, +83/-0)
- packages/components/src/validated-form-controls/style.scss (MODIFIED, +6/-0)
