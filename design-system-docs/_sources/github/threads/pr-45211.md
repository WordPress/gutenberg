---
number: 45211
type: pull_request
state: MERGED
merged_at: 2022-10-24T13:56:00Z
url: https://github.com/WordPress/gutenberg/pull/45211
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #45211: TabPanel: Add tests and changelog for onSelect behavior change

- URL: https://github.com/WordPress/gutenberg/pull/45211
- Author: mirka
- Created: 2022-10-21T17:23:09Z
- Updated: 2022-10-24T13:56:37Z
- Comments: 1 of 1
- Review threads: 1 of 1
- Changed files: 2 of 2
- Diff: ../pr-diffs/45211.diff

## Body

Follow-up to #44028

## What?

Adds changelog and tests for a bug fix added in #44028.

## Why?

This surfaced in a rebase while reviewing #44935. It's a notable change to what triggers the `onSelect` callback, and warrants a changelog for affected consumers.

## Testing Instructions

✅ Tests pass

## Issue comments

### youknowriad on 2022-10-24T10:01:37Z

URL: https://github.com/WordPress/gutenberg/pull/45211#issuecomment-1288767781

Thanks for the follow-up here

## Review threads

### Review thread 1

#### chad1008 on 2022-10-24T09:53:08Z

URL: https://github.com/WordPress/gutenberg/pull/45211#discussion_r1003116513
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -117,4 +127,71 @@ describe( 'TabPanel', () => {
 			'gamma-class'
 		);
 	} );
+
+	it( 'should select `initialTabname` if defined', () => {
+		const mockOnSelect = jest.fn();
+
+		render(
+			<TabPanel
+				tabs={ TABS }
+				initialTabName="beta"
+				children={ () => undefined }
+				onSelect={ mockOnSelect }
+			/>
+		);
+		expect( getSelectedTab() ).toHaveTextContent( 'Beta' );
+		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
+	} );
+
+	describe( 'fallbacks when new tab list invalidates current selection', () => {
+		it( 'should select `initialTabName` if defined', async () => {
+			const user = setupUser();
+			const mockOnSelect = jest.fn();
+
+			const { rerender } = render(
+				<TabPanel
+					tabs={ TABS }
+					initialTabName="gamma"
+					children={ () => undefined }
+					onSelect={ mockOnSelect }
+				/>
+			);
+			await user.click( screen.getByRole( 'tab', { name: 'Alpha' } ) );
+
+			rerender(
+				<TabPanel
+					tabs={ TABS.slice( 1 ) /* remove alpha */ }
+					initialTabName="gamma"
+					children={ () => undefined }
+					onSelect={ mockOnSelect }
+				/>
+			);
+			expect( getSelectedTab() ).toHaveTextContent( 'Gamma' );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'gamma' );
+		} );
+
+		it( 'should select first tab if `initialTabName` not defined', async () => {
+			const user = setupUser();
+			const mockOnSelect = jest.fn();
+
+			const { rerender } = render(
```

TIL - this is a cool little destructure!

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/tab-panel/test/index.tsx (MODIFIED, +78/-1)
