---
number: 41641
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/41641
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Type] Code Quality","[Package] Components","[Feature] Component System"]
---

# PR #41641: RadioControl: add ref forwarding

- URL: https://github.com/WordPress/gutenberg/pull/41641
- Author: ciampo
- Created: 2022-06-09T17:52:21Z
- Updated: 2025-02-10T16:43:39Z
- Comments: 0 of 0
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/41641.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Adds ref forwarding to the `RadioControl` component

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Consistency with other components in the package (both in terms of ref forwarding, and in terms of code patterns specifically around how we export a component). 

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Wrapped the component into `forwardRef`, renamed original component to `UnforwardedRadioControl`, moved JSDocs

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a Post or Page. -->
<!-- 2. Insert a Heading Block. -->
<!-- 3. etc. -->

- Component should behave like before in Storybook and the editor
- Tests should pass

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### alexstine on 2022-06-09T19:14:06Z

URL: https://github.com/WordPress/gutenberg/pull/41641#discussion_r893870230
Path: packages/components/src/radio-control/index.tsx

```diff
@@ -104,4 +77,31 @@ export function RadioControl(
 	);
 }
 
+/**
+ * Render a user interface to select the user type using radio inputs.
+ *
+ * ```jsx
+ * import { RadioControl } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyRadioControl = () => {
+ *   const [ option, setOption ] = useState( 'a' );
+ *
+ *   return (
+ *     <RadioControl
+ *       label="User type"
+ *       help="The type of the current user"
+ *       selected={ option }
+ *       options={ [
+ *         { label: 'Author', value: 'a' },
+ *         { label: 'Editor', value: 'e' },
+ *       ] }
+ *       onChange={ ( value ) => setOption( value ) }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export const RadioControl = forwardRef( UnforwardedRadioControl );
```

@ciampo Does that function name seem confusing? UnforwardedRadioControl? Not sure if other components use that but it just seems weird especially reading from start to finish as the function now forwards the ref.

#### ciampo on 2022-06-10T06:28:07Z

URL: https://github.com/WordPress/gutenberg/pull/41641#discussion_r894192509
Path: packages/components/src/radio-control/index.tsx

```diff
@@ -104,4 +77,31 @@ export function RadioControl(
 	);
 }
 
+/**
+ * Render a user interface to select the user type using radio inputs.
+ *
+ * ```jsx
+ * import { RadioControl } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyRadioControl = () => {
+ *   const [ option, setOption ] = useState( 'a' );
+ *
+ *   return (
+ *     <RadioControl
+ *       label="User type"
+ *       help="The type of the current user"
+ *       selected={ option }
+ *       options={ [
+ *         { label: 'Author', value: 'a' },
+ *         { label: 'Editor', value: 'e' },
+ *       ] }
+ *       onChange={ ( value ) => setOption( value ) }
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export const RadioControl = forwardRef( UnforwardedRadioControl );
```

This is a pattern that @mirka and I have adopted in the context of the TypeScript migration that we're undertaking across the components in the package (see [this guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#refactoring-a-component-to-typescript) for more details)

In order to have Storybook extract and show prop types correctly, we need to have a named export for the component ("RadioControl" in this case). Therefore, we had to find a suitable name for the intermediate representation of the component, and we opted for:

- adding the "unforwarded" prefix when the component is later wrapped in a `forwardRef` call
- adding the "unconnected" prefix when the component is later wrapped in a `contextConnect` call

Hope that's a bit more clear!

### Review thread 2

#### walbo on 2022-06-09T19:30:05Z

URL: https://github.com/WordPress/gutenberg/pull/41641#discussion_r893883733
Path: packages/components/src/radio-control/index.tsx

```diff
@@ -93,6 +65,7 @@ export function RadioControl(
 						aria-describedby={
 							!! help ? `${ id }__help` : undefined
 						}
+						ref={ forwardedRef }
```

Havn't used forwardRef inside an `Array.map` before. Anything we need to think about or is this pattern ok?

#### ciampo on 2022-06-10T07:20:49Z

URL: https://github.com/WordPress/gutenberg/pull/41641#discussion_r894226787
Path: packages/components/src/radio-control/index.tsx

```diff
@@ -93,6 +65,7 @@ export function RadioControl(
 						aria-describedby={
 							!! help ? `${ id }__help` : undefined
 						}
+						ref={ forwardedRef }
```

Good point — I had actually missed that `map` statement while making these changes quickly.

It turns out that this task is more complicated than expected — we could forward the `ref` to `BaseControl`, but `BaseControl` currently doesn't accept refs.

I tried to quickly add ref forwarding to `BaseControl`, but I ran into type issues — mainly to do with the fact that `VisualLabel` is currently defined as a static property on `BaseControl` (e.g. `BaseControl.VisualLabel`), and adding ref forwarding causes an error with that

![Screenshot 2022-06-10 at 09 19 08](https://user-images.githubusercontent.com/1083581/173011835-85ba2a33-6655-4ca2-bf42-254ab4acde05.png)

Since this is not currently a priority, I've decided to stop investigating this further for now. What do folks think is the best way forward? I'm also happy to drop this entirely for the time being.

#### mirka on 2022-06-10T14:47:18Z

URL: https://github.com/WordPress/gutenberg/pull/41641#discussion_r894612604
Path: packages/components/src/radio-control/index.tsx

```diff
@@ -93,6 +65,7 @@ export function RadioControl(
 						aria-describedby={
 							!! help ? `${ id }__help` : undefined
 						}
+						ref={ forwardedRef }
```

I agree we can drop it for now, unless we have a concrete use case. I came across [this note in the React docs](https://reactjs.org/docs/forwarding-refs.html#note-for-component-library-maintainers) that forwarding refs is a breaking change, and while I'm not 100% sure about what that entails, it did give me pause.

#### ciampo on 2022-06-10T17:02:20Z

URL: https://github.com/WordPress/gutenberg/pull/41641#discussion_r894730647
Path: packages/components/src/radio-control/index.tsx

```diff
@@ -93,6 +65,7 @@ export function RadioControl(
 						aria-describedby={
 							!! help ? `${ id }__help` : undefined
 						}
+						ref={ forwardedRef }
```

Makes sense. Will close this PR for now

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +3/-0)
- packages/components/src/radio-control/index.tsx (MODIFIED, +33/-33)
