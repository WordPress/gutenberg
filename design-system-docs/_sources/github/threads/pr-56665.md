---
number: 56665
type: pull_request
state: MERGED
merged_at: 2024-01-12T03:04:17Z
url: https://github.com/WordPress/gutenberg/pull/56665
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #56665: `BoxControl`: update design

- URL: https://github.com/WordPress/gutenberg/pull/56665
- Author: brookewp
- Created: 2023-11-30T04:27:58Z
- Updated: 2024-01-12T03:04:44Z
- Comments: 8 of 8
- Review threads: 32 of 32
- Changed files: 11 of 11
- Diff: ../pr-diffs/56665.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

While working on adding updated sizing in #56185, we noticed that the larger sizing affects the visibility of the input for `BoxControl` when the width is constrained. And while there is a max-width on this component, the main concern is this happens regardless due to the width of the inspector:

<img width="264" alt="Screenshot 2023-11-15 at 6 32 19 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/1f766e83-bed0-4809-aa94-d85820b64303">

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

`BoxControl` is only used in the dimension panel for backward compatibility. So it was decided to change the design of the component to match custom padding when presets are provided (excluding the settings icon): https://github.com/WordPress/gutenberg/pull/56185#discussion_r1398989244

<img width="281" alt="Screenshot 2023-11-20 at 10 29 10" src="https://github.com/WordPress/gutenberg/assets/846565/3cb7d916-277e-44e9-a90d-eec785f88fc5">

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Since this component isn't used in many places, the main goal was to match the design specs so we could move forward with updating to the 40px sizing. So there could be potential follow-ups to reduce some redundancy currently present, but I don't believe it is a priority due to its current usage. Therefore, the main changes are: 
- changing flex-direction to column
- adding a `RangeControl` component to each side
- adjusting where/when the icons are shown
- grouping the buttons together (reset and linked)

## Screenshots 

### AllInputControl

| Before  | After |
| ------------- | ------------- |
|  <img width="290" alt="Screenshot 2023-11-30 at 5 13 29 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/e14d89d3-20b1-456b-859a-765e846f4265"> |  <img width="269" alt="Screenshot 2023-11-30 at 5 30 42 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/b1b75e98-d013-4b86-90eb-c9aa4440650b"> |

### InputControls

| Before  | After |
| ------------- | ------------- |
|  <img width="270" alt="Screenshot 2023-11-30 at 5 13 36 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/dd6636ac-c110-419a-a012-ec0609375b3c"> | <img width="276" alt="Screenshot 2023-12-01 at 6 31 05 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/2b262120-56f5-4866-a16e-22e8e16f200e">  | 

### AxialInputControls

Will look similar but with two inputs when unlinked and the icons highlighting two sides (vertical and horizontal)

<img width="283" alt="Screenshot 2023-12-01 at 6 31 13 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/65614cda-4574-4e50-849b-336049213d39">

## Testing Instructions

Ensure tests pass and component behaves the same as before. 

For new changes: 

Ensure that RangeControl updates to the correct value when typing in the input.

## Issue comments

### brookewp on 2023-12-01T02:04:39Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1835294904

There are a couple things that I'd like to run by design (cc @jameskoster) before finishing up this PR and marking it ready for review.

**Buttons**

The placement of the linked button doesn't work as well with the vertical inputs, especially if `allowReset` is false. So, I grouped both buttons in the same area as the icon in SpacingInputControl:

<img width="270" alt="Screenshot 2023-11-30 at 5 31 44 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/7200757f-3f07-4de4-9f43-13ddc89940d7">

And this is how it looks if `allowReset` is false:

| Before  | After |
| ------------- | ------------- |
| <img width="279" alt="Screenshot 2023-11-29 at 6 14 09 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/efcf29cb-9dc8-47a2-b58f-dded4778e65f">  | <img width="276" alt="Screenshot 2023-11-30 at 5 50 57 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/5acd7bad-3bfc-4b41-8f9f-6263fe68b884"> |

When the Reset button is present, should it also have the 40px height when the inputs do? 

**Input Size**

The input is a bit bigger than the one in SpacingInputControl. This is due to the missing settings icon. In lieu of that, do we want to:

Keep it as it is now, with the UnitControl and RangeControl as equal sizes? 

<img width="269" alt="Screenshot 2023-11-30 at 5 30 42 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/b1b75e98-d013-4b86-90eb-c9aa4440650b">

Or add a max size of 90px to the UnitControl to match the sizing of SpacingInputControl? Or maybe a compact size option for this? 

<img width="271" alt="Screenshot 2023-11-30 at 6 03 00 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/09b2c95a-8820-414d-a10f-ddebd44f8803">


---

And if there's any other feedback, please share! One thing to note is the Axial Controls haven't been updated with the changes yet so those will look strange right now.

### jameskoster on 2023-12-01T09:57:06Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1835800460

The placement of the `link` button seems sensible 👍

> When the Reset button is present, should it also have the 40px height when the inputs do?

I think the current size is okay, and consistent with other reset buttons.

For the input size, a max-width is probably a good idea as it's unlikely to be populated with more than 3 characters. Using the compact variant could make sense too (while retaining the 40px row height), to avoid the inputs butting up against each other. 

<img width="382" alt="Screenshot 2023-12-01 at 09 56 29" src="https://github.com/WordPress/gutenberg/assets/846565/bd4037c5-fbb9-44d0-a9cf-2a4cc6d62747">

What do you think?

### brookewp on 2023-12-01T23:59:35Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1836938327

>What do you think?

That looks great, thank you for the info and example! I initially didn't add spacing between the inputs to match the current `SpacingInputControl`, but I prefer what you've shared instead.

### brookewp on 2023-12-08T22:53:09Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1847943686

>When the slider has its maximum value, both the thumb and the tooltip (shown when the thumb is focused) render partially outside of the component's boundary. This may be problematic in the sidebar, as they would either cause a horizontal scrollbar to appear, or they could be potentially cut off.

Good find! `SpacingSizesControl` has a settings icon to the right, so that problem wasn't there. But I'll look at other use cases of RangeControl to be consistent with those. 

>The sliders have a fixed max value of 100, which, depending on the selected unit, may be too little or too much. We should take inspiration from SpacingSizesControl and adapt max and step values per unit ([see code](https://github.com/WordPress/gutenberg/blob/aa91684eb5e58384e6d9b6244d7d9dd3791ffb52/packages/block-editor/src/components/spacing-sizes-control/input-controls/spacing-input-control.js#L239-L243))

Thank you for sharing that example! [I was initially concerned](https://github.com/WordPress/gutenberg/pull/56185#discussion_r1403850114) about using `RangeControl` because of the max prop limitation since `BoxControl` doesn't have a max limit. But if we're happy to use the unit/max/step pairing from `SpacingSizesControl` then that works for me!

### ciampo on 2023-12-12T18:43:58Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1852610872

> I'll look at other use cases of RangeControl to be consistent with those.

I see that you added an `8px` right margin, which mitigates the issue but doesn't necessarily solve it entirely

![Screenshot 2023-12-12 at 19 42 51](https://github.com/WordPress/gutenberg/assets/1083581/6bea1518-65f3-4a76-9557-8cc18b340720)

Adding more right margin would feel awkward, though. I wonder if the component would still look alright in the editor, since the sidebar should have some padding?

### brookewp on 2023-12-12T21:49:52Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1852864189

>I wonder if the component would still look alright in the editor, since the sidebar should have some padding?

As you suspected, the thumb isn't cut off because of the sidebar's padding. I think the small right margin helps the slider track edge align a bit more with the ellipsis menu:

| With Margin  | Without |
| ------------- | ------------- |
|  <img width="290" alt="Screenshot 2023-12-12 at 1 42 55 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/c2b05133-9c0b-4a78-8727-fff7191da62c"> | <img width="292" alt="Screenshot 2023-12-12 at 1 43 38 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/39e57770-b27f-4487-9b98-adbabdb691b5"> |

### github-actions on 2023-12-15T00:04:07Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1857031193

<!-- flaky-tests-report-comment -->
**Flaky tests detected in f8e096601dbf4af46d81bb56c1e7b48bb8cc0a05.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/7298330984
📝  Reported issues:
- #40369 in `specs/editor/various/block-switcher.test.js`

### ciampo on 2023-12-22T09:33:01Z

URL: https://github.com/WordPress/gutenberg/pull/56665#issuecomment-1867455792

I rebased to include the latest trunk changes (including updated node/npm versions).

As I will be AFK for the next couple of week, I will pass the PR review baton to @mirka for a final round of review.

@andrewhayward , it would be great if you could also give this PR a look, specifically in terms of input labels.

Thank you 🙏

## Review threads

### Review thread 1 (resolved)

#### brookewp on 2023-12-01T02:53:31Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1411542544
Path: packages/components/src/box-control/input-controls.tsx

```diff
@@ -39,6 +44,15 @@ export default function BoxInputControls( {
 		onChange( nextValues );
 	};
 
+	const createSliderOnChange = (
+		side: keyof BoxControlValue,
+		next: string
+	) => {
+		const nextValues = { ...values };
+		nextValues[ side ] = next;
```

@ciampo 

I have a diff with changes to combine InputControls and AxialInputControls as they are nearly identical. This is done by checking for `splitOnAxis`, but one problem I've encountered is Typescript doesn't like this change. 

I was hoping I could combine what is there already
https://github.com/WordPress/gutenberg/blob/7b1f24c60e7ee71f3391349acf58d14f0bd03ce1/packages/components/src/box-control/axial-input-controls.tsx#L10-L11

into something like:

`type BoxControlType = BoxControlValue | GroupedSide`

Is it possible to have a conditional type that relies on the boolean value of a prop like `splitOnAxis`? 

--- 

I can also just add the same changes I've included for InputControl to AxialInputControls to get this PR wrapped up quicker. I was just curious if it was possible with the type issues.

#### ciampo on 2023-12-01T10:59:35Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1411939335
Path: packages/components/src/box-control/input-controls.tsx

```diff
@@ -39,6 +44,15 @@ export default function BoxInputControls( {
 		onChange( nextValues );
 	};
 
+	const createSliderOnChange = (
+		side: keyof BoxControlValue,
+		next: string
+	) => {
+		const nextValues = { ...values };
+		nextValues[ side ] = next;
```

There are many approaches that could be taken, and it's hard for me to suggest an exact one without spending some time with this component and understand its internals. For example, the component combining both `InputControls` and `AxialInputControls` could have its props declared with a type union ([example](https://www.typescriptlang.org/play?#code/JYWwDg9gTgLgBAKjgQwM5wEoFNkGN4BmUEIcA5FDvmQNwBQdMAnmFnAEIQAeAwhAHYxiAGwBqyYQFc2AXjgBvOgEgYEMAH4AXHFRDg-AOb0lUYAYAWMLTr2HjAIwgxVIa7tN3lwrASvb3+kZ0AL70zKxwAMqB3tEAJrJwANZYTBAEHNx8giLiUlj0DLgCunAGxJKscfFY6HIA2uQAbliwwLgSZAA05ObQwABeAjCdcAC6KOjF-Lo0cIwsbADiFVU1cHIAFHDhWOllq1jVwAnoAJSN-JIg9q3jcwwLEXzgAliCAArEYHUKdHA6E5YbTRQyxIH0AGoMDCYAwADy--X84n-ICQJnD18ItKCYKY7EEKQo9OKleAeL4lTBIDYTzUghjLJYoj93E4cgA)).

My advice here is to timebox this effort, and if it takes too long, to just leave it. Maybe it's good material for a follow-up PR.

#### brookewp on 2023-12-02T00:00:21Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1412673336
Path: packages/components/src/box-control/input-controls.tsx

```diff
@@ -39,6 +44,15 @@ export default function BoxInputControls( {
 		onChange( nextValues );
 	};
 
+	const createSliderOnChange = (
+		side: keyof BoxControlValue,
+		next: string
+	) => {
+		const nextValues = { ...values };
+		nextValues[ side ] = next;
```

Sounds like a good plan. Thanks for the example and suggestion!

### Review thread 2 (resolved)

#### brookewp on 2023-12-02T02:36:15Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1412705421
Path: packages/components/src/box-control/test/index.tsx

```diff
@@ -1,7 +1,7 @@
 /**
  * External dependencies
  */
-import { render, screen } from '@testing-library/react';
+import { fireEvent, render, screen } from '@testing-library/react';
```

Added `fireEvent` due to `slider` actions not being supported by `UserEvent`: https://github.com/testing-library/user-event/issues/871

I did wonder if it was possible with a pointer event but I saw that for other sliders in the codebase, we are using `fireEvent`.

### Review thread 3 (resolved)

#### brookewp on 2023-12-02T02:37:11Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1412705482
Path: packages/components/src/box-control/test/index.tsx

Tests were added in three different sections to account for all three components that had `RangeControl` added.

### Review thread 4 (resolved)

#### ciampo on 2023-12-08T19:06:16Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1420911566
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
```

Is passing `initialPosition` necessary, since we're already passing `value` ?

#### brookewp on 2023-12-08T22:47:47Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1421086736
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
```

If we don't, then the `RangeControl` thumb starts in the center of the min and max:

<img width="666" alt="Screenshot 2023-12-08 at 1 27 09 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/4292ecb7-eb2a-4e51-812d-0c6297c484fc">

This seems a bit confusing since the UnitControl input is empty initially.

#### brookewp on 2023-12-11T18:57:19Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1422998767
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
```

We discussed on a call that having `initialPosition={ parsedQuantity ?? 0 }` would be a good solution here.

#### ciampo on 2023-12-12T18:31:32Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1424427611
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
```

Apologies if I didn't explain myself clearly. My suggestion was to remove the `initialPosition` prop, and instead have `value={ parsedQuantity ?? 0 }`.

That way we are always using the `value` prop, which is the prop that should be used to use the component in controlled mode (as opposed to `initialPosition`, which is supposed to be used to set the initial value when the component is used in uncontrolled mode, as per the [docs](https://github.com/WordPress/gutenberg/blob/da4d91f8e0077910252c7c0feb3c5e67ec335d6d/packages/components/src/range-control/types.ts#L121-L126))

#### brookewp on 2023-12-13T18:18:25Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425725274
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
```

No worries, thank you for clarifying! I'll make the change 👍

### Review thread 5 (resolved)

#### ciampo on 2023-12-08T19:56:15Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1420974856
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -66,6 +71,21 @@ export default function AxialInputControls( {
 		}
 	};
 
+	const createSliderOnChange = ( side: GroupedSide, next: string ) => {
```

Nit: since this is not a function _creating_ an event listener function, we should probably not use `create` in its name.  Maybe `onSliderChange` ?

### Review thread 6 (resolved)

#### ciampo on 2023-12-08T19:57:08Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1420975825
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
+							label={ LABELS[ side ] }
+							onChange={ ( newValue ) => {
+								createSliderOnChange?.(
```

The `createSliderOnChange` function should be always defined, no need for optional chaining

```suggestion
								createSliderOnChange(
```

### Review thread 7 (resolved)

#### ciampo on 2023-12-08T20:04:41Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1420981268
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
+							label={ LABELS[ side ] }
```

I'm not sure if having a separate `label` for the slider is the best option here, I remember discussing this topic  with @andrewhayward

#### brookewp on 2023-12-08T22:44:53Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1421083101
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
+							label={ LABELS[ side ] }
```

I'd be curious to know what the best way is to label these and ensure they are associated together. 

I was looking at `FontSizePicker` since it is similar and saw the two labels there: 

https://github.com/WordPress/gutenberg/blob/83434bf46dc0529d9a97e8c4a5155d2a57c407f2/packages/components/src/font-size-picker/index.tsx#L219-L253

Initially, I didn't have the label on `RangeControl` but then I saw a violation in SB:

>Ensures every form element has a label
[More info...](https://dequeuniversity.com/rules/axe/4.7/label?application=axeAPI)
#inspector-range-control-19

So if there is a preferred alternative, we might want to note a follow-up for `FontSizePicker` as well.

#### brookewp on 2023-12-12T22:12:53Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1424630696
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -129,25 +145,45 @@ export default function AxialInputControls( {
 						? selectedUnits.top
 						: selectedUnits.left;
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<HStack key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
+							value={ [
+								parsedQuantity,
+								selectedUnit ?? parsedUnit,
+							].join( '' ) }
+							onChange={ createHandleOnChange( side ) }
+							onUnitChange={ createHandleOnUnitChange( side ) }
+							onFocus={ createHandleOnFocus( side ) }
+							onHoverOn={ createHandleOnHoverOn( side ) }
+							onHoverOff={ createHandleOnHoverOff( side ) }
+							label={ LABELS[ side ] }
+							key={ side }
+						/>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							hideLabelFromVision
+							initialPosition={ 0 }
+							label={ LABELS[ side ] }
```

I followed up on this in [another comment](https://github.com/WordPress/gutenberg/pull/56665/files#r1424628822)

### Review thread 8 (resolved)

#### brookewp on 2023-12-12T21:53:11Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1424614341
Path: packages/components/src/box-control/unit-control.tsx

```diff
@@ -32,7 +32,7 @@ export default function BoxUnitControl( {
 
 	return (
 		<UnitControlWrapper { ...bindHoverGesture() }>
-			<Tooltip text={ label }>
+			<Tooltip placement="top" text={ label }>
```

I replaced the Tooltip as it was just creating another version of the component with no differences. The `div` isn't needed anymore, but if it was, it could be added directly around the `UnitControl` anyway.

### Review thread 9 (resolved)

#### brookewp on 2023-12-12T21:56:43Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1424617496
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

I moved from HStack/VStack to Grid because the semantics were incorrect. When tabbing through, the LinkedButton had focus first rather than the input. 

Moving the components in the expected order and adjusting for visual changes using grid seemed like the best approach here.

#### ciampo on 2023-12-13T18:58:18Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425764775
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

Using flex or grid in theory shouldn't influence the semantics (we're still using the same `role` and `aria-labelledby` attributes), but I see what you mean — using CSS grid gives us more freedom to arrange items in the right DOM order, and then arrange them visually in a different order from DOM order.

#### ciampo on 2023-12-13T19:08:20Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425774777
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

On a separate note, I have a few questions about the grid implementation:

- why does the grid have 3 columns of equal width (1fr) ?
- why does each "row" span for 4 columns, if the grid should have only 3 columns ?
- why is the link/unlink button assigned to the 4th column, if the grid should have only 3 columns?
- why is the reset button not explicitly assigned to a grid cell? Relying on the grid algorithm to automatically place it could easily introduce unexpected changes in the future

#### brookewp on 2023-12-14T01:54:19Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426077036
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

>Using flex or grid in theory shouldn't influence the semantics (we're still using the same role and aria-labelledby attributes)

Yes, it's a bit odd! There were many layers of Flex/Flex Item/Flex Block before so it's likely that simplifying to HStack/VStack made a bit of an impact there. I took a quick look to see if there was a different order or reversing set previously, but I didn't see anything. So I figured it was worth switching to Grid for the flexibility rather than digging into how it was in a different order before, but the tab order was correct. 

>why does the grid have 3 columns of equal width (1fr) ?
why does each "row" span for 4 columns, if the grid should have only 3 ?
why is the link/unlink button assigned to the 4th column, if the grid should have only 3 columns?
why is the reset button not explicitly assigned to a grid cell? Relying on the grid algorithm to automatically place it could easily introduce unexpected changes in the future

It was a bit organic and mainly related to your last question. When changing the number of columns in the Grid column, I noticed that an additional column was generated outside of the set number, due to the span. It seemed to work well because the last column appeared to have an inline width, rather than having another equal width fourth column.

But it makes sense that it would be an unstable way to approach it, so I can more explicitly set up four columns with the various widths.

#### brookewp on 2023-12-14T02:45:28Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426109538
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

Not sure if there is another way to achieve this without any hardcoded values:

https://github.com/WordPress/gutenberg/blob/493e8d9d7f9a26b8820b3435d943f43a511a6546/packages/components/src/box-control/index.tsx#L160

With the two values being for the buttons and the icon/input/slider taking up the remaining space.

#### ciampo on 2023-12-14T11:24:52Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426592376
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

> But it makes sense that it would be an unstable way to approach it, so I can more explicitly set up four columns with the various widths.

Three columns should be enough — one for the label, one for the reset button, and one for the link/unlink button. Which is how it's currently set up, so we're good!

> Not sure if there is another way to achieve this without any hardcoded values:

There is! We can use [the `fr` unit](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout/Basic_concepts_of_grid_layout#the_fr_unit) and the [`min-content` CSS keyword](https://developer.mozilla.org/en-US/docs/Web/CSS/min-content)

Something like 

`templateColumns="1fr min-content min-content"`

should work

#### brookewp on 2023-12-14T23:30:47Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1427405429
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
```

Ah yes, I meant three columns. And thanks for sharing, TIL about `min-content`!

### Review thread 10 (resolved)

#### brookewp on 2023-12-12T22:00:12Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1424620968
Path: packages/components/src/box-control/index.tsx

```diff
@@ -155,57 +154,51 @@ function BoxControl( {
 	};
 
 	return (
-		<Root id={ id } role="group" aria-labelledby={ headingId }>
-			<Header className="component-box-control__header">
-				<FlexItem>
-					<BaseControl.VisualLabel id={ headingId }>
-						{ label }
-					</BaseControl.VisualLabel>
-				</FlexItem>
-				{ allowReset && (
-					<FlexItem>
-						<Button
-							className="component-box-control__reset-button"
-							variant="secondary"
-							isSmall
-							onClick={ handleOnReset }
-							disabled={ ! isDirty }
-						>
-							{ __( 'Reset' ) }
-						</Button>
-					</FlexItem>
-				) }
-			</Header>
-			<HeaderControlWrapper className="component-box-control__header-control-wrapper">
-				<FlexItem>
-					<BoxControlIcon side={ side } sides={ sides } />
-				</FlexItem>
-				{ isLinked && (
-					<FlexBlock>
-						<AllInputControl
-							aria-label={ label }
-							{ ...inputControlProps }
-						/>
-					</FlexBlock>
-				) }
-				{ ! isLinked && splitOnAxis && (
-					<FlexBlock>
-						<AxialInputControls { ...inputControlProps } />
-					</FlexBlock>
-				) }
-				{ ! hasOneSide && (
-					<FlexItem>
-						<LinkedButton
-							onClick={ toggleLinked }
-							isLinked={ isLinked }
-						/>
-					</FlexItem>
-				) }
-			</HeaderControlWrapper>
+		<Grid
+			id={ id }
+			columns={ 3 }
+			role="group"
+			aria-labelledby={ headingId }
+		>
+			<BaseControl.VisualLabel id={ headingId }>
+				{ label }
+			</BaseControl.VisualLabel>
+			{ isLinked && (
+				<InputWrapper>
+					<FlexedBoxControlIcon side={ side } sides={ sides } />
+					<AllInputControl
+						aria-label={ label }
+						{ ...inputControlProps }
+					/>
+				</InputWrapper>
+			) }
+			{ ! hasOneSide && (
+				<LinkedButtonWrapper>
+					<LinkedButton
+						onClick={ toggleLinked }
+						isLinked={ isLinked }
+					/>
+				</LinkedButtonWrapper>
+			) }
+
+			{ ! isLinked && splitOnAxis && (
+				<AxialInputControls { ...inputControlProps } />
+			) }
 			{ ! isLinked && ! splitOnAxis && (
 				<InputControls { ...inputControlProps } />
 			) }
-		</Root>
+			{ allowReset && (
```

I moved the ResetButton below the inputs because it made sense that the option to tab to reset would be available after first adjusting the sizes. 

Otherwise, if you adjust the sizes, you have to navigate backward to get back to the ResetButton (since it starts as disabled until a change is made to an input).

### Review thread 11

#### brookewp on 2023-12-12T22:10:19Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1424628822
Path: packages/components/src/box-control/input-controls.tsx

```diff
@@ -124,11 +138,35 @@ export default function BoxInputControls( {
 							onHoverOn={ createHandleOnHoverOn( side ) }
 							onHoverOff={ createHandleOnHoverOff( side ) }
 							label={ LABELS[ side ] }
-							key={ `box-control-${ side }` }
 						/>
-					);
-				} ) }
-			</Layout>
-		</LayoutContainer>
+
+						<FlexedRangeControl
+							aria-controls={ inputId }
+							aria-labelledby={ inputId }
```

I based this on a similar example I saw from @andrewhayward: 

```
 <label id="lbl-2" for="ipt-2">Label 2</label>
  <input id="ipt-2">
  <input id="ipt-3" aria-labelledby="lbl-2" aria-controls="ipt-2">
```

BoxControl has very different markup, so it isn't implemented the same. In this instance, it may be completely wrong, so I would appreciate a more expert eye on this. However, this did get rid of the redundant label on the slider, while avoiding a violation for having no label. 

**Some additional context:** 

When unlinking the sides, the unit control labels are labelled by aria-labels. So I added `aria-controls` and `aria-labelledby` with the intention to connect it to those inputs. 

In AllInputControl when it's a single/mixed set of values, there isn't an explicit label, but it inherits one from the BaseControl.VisualLabel: https://github.com/WordPress/gutenberg/blob/3192ad7db08dce203d49df96ace4d7b08a534919/packages/components/src/box-control/index.tsx#L157-L165

### Review thread 12 (resolved)

#### ciampo on 2023-12-13T19:09:50Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425776042
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -69,18 +83,43 @@ export default function AllInputControl( {
 		} );
 	};
 
+	const [ parsedQuantity, parsedUnit ] =
+		parseQuantityAndUnitFromRawValue( allValue );
+
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ parsedQuantity }
```

Why are we passing only the quantity to `UnitControl`, instead of the complete quantity + unit value (`allValue`) ?

#### brookewp on 2023-12-14T01:36:19Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426068298
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -69,18 +83,43 @@ export default function AllInputControl( {
 		} );
 	};
 
+	const [ parsedQuantity, parsedUnit ] =
+		parseQuantityAndUnitFromRawValue( allValue );
+
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ parsedQuantity }
```

I was trying to keep it closer to trunk: 

https://github.com/WordPress/gutenberg/blob/59e89580e4a0e5b9fbf68d07470a339bc8fd7949/packages/components/src/box-control/all-input-control.tsx#L77

I thought it may be implemented this way because the units can be disabled when `isMixed` is true. But then we could check against that if it was the case.  😅 So to be honest, I'm not 100% sure, I just was trying to make as few changes as possible, but I needed the unit for RangeControl. 

I can look into it if it seems like a good change to include here.

#### ciampo on 2023-12-14T08:41:22Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426406198
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -69,18 +83,43 @@ export default function AllInputControl( {
 		} );
 	};
 
+	const [ parsedQuantity, parsedUnit ] =
+		parseQuantityAndUnitFromRawValue( allValue );
+
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ parsedQuantity }
```

Looking at the diff, it seems that on `trunk` we are already passing `allValue`, and this PR introduced a change (passing `parsedQuantity` instead of `allValue`). So, by passing `allValue` we would be retaining `trunk`'s behaviour.

It should be completely fine for `UnitControl` to receive a string value including quantity+unit (see [docs](https://github.com/WordPress/gutenberg/blob/d2450c88f42e22827718ca233a94cb9c5e1b873f/packages/components/src/unit-control/types.ts#L97-L101))

### Review thread 13 (resolved)

#### ciampo on 2023-12-13T19:10:31Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425776659
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -36,6 +45,11 @@ export default function AllInputControl( {
 		onFocus( event, { side: 'all' } );
 	};
 
+	const sliderOnChange = ( next: string ) => {
+		const nextValues = applyValueToSides( values, String( next ), sides );
```

is `next` is already a `string`, why do we need to wrap it in `String( )` ?

#### brookewp on 2023-12-14T01:26:39Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426063475
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -36,6 +45,11 @@ export default function AllInputControl( {
 		onFocus( event, { side: 'all' } );
 	};
 
+	const sliderOnChange = ( next: string ) => {
+		const nextValues = applyValueToSides( values, String( next ), sides );
```

Good catch! This will be removed. 

(It is a leftover from a past iteration where RangeControl wanted a number, and onChange wanted a string).

#### brookewp on 2023-12-14T02:48:54Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426111298
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -36,6 +45,11 @@ export default function AllInputControl( {
 		onFocus( event, { side: 'all' } );
 	};
 
+	const sliderOnChange = ( next: string ) => {
+		const nextValues = applyValueToSides( values, String( next ), sides );
```

So it's very relevant to see [what you did in your suggested refactor](
https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425925419), and helpful!

### Review thread 14 (resolved)

#### ciampo on 2023-12-13T21:55:01Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1425925419
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -69,18 +83,43 @@ export default function AllInputControl( {
 		} );
 	};
 
+	const [ parsedQuantity, parsedUnit ] =
+		parseQuantityAndUnitFromRawValue( allValue );
+
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ parsedQuantity }
+				onChange={ handleOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
+				hideLabelFromVision
+				initialPosition={ parsedQuantity ?? 0 }
+				label={ LABELS.all }
+				onChange={ ( newValue ) => {
+					sliderOnChange( [ newValue, parsedUnit ].join( '' ) );
```

We could refactor the code slightly and resuse as much as possible of the existing logic, which not only makes the code less redundant, but also ensures that change events on `RangeControl` would have the exact same effect as the ones on `UnitControl`

<details>

<summary>Click to expand</summary>

```diff
diff --git a/packages/components/src/box-control/all-input-control.tsx b/packages/components/src/box-control/all-input-control.tsx
index a6aefab80d..e1f09a16a3 100644
--- a/packages/components/src/box-control/all-input-control.tsx
+++ b/packages/components/src/box-control/all-input-control.tsx
@@ -39,18 +39,16 @@ export default function AllInputControl( {
 	const isMixed = hasValues && isValuesMixed( values, selectedUnits, sides );
 	const allPlaceholder = isMixed ? LABELS.mixed : undefined;
 
+	const [ parsedQuantity, parsedUnit ] =
+		parseQuantityAndUnitFromRawValue( allValue );
+
 	const handleOnFocus: React.FocusEventHandler< HTMLInputElement > = (
 		event
 	) => {
 		onFocus( event, { side: 'all' } );
 	};
 
-	const sliderOnChange = ( next: string ) => {
-		const nextValues = applyValueToSides( values, String( next ), sides );
-		onChange( nextValues );
-	};
-
-	const handleOnChange: UnitControlProps[ 'onChange' ] = ( next ) => {
+	const onValueChange = ( next?: string ) => {
 		const isNumeric = next !== undefined && ! isNaN( parseFloat( next ) );
 		const nextValue = isNumeric ? next : undefined;
 		const nextValues = applyValueToSides( values, nextValue, sides );
@@ -58,6 +56,16 @@ export default function AllInputControl( {
 		onChange( nextValues );
 	};
 
+	const unitControlOnChange: UnitControlProps[ 'onChange' ] = ( next ) => {
+		onValueChange( next );
+	};
+
+	const sliderOnChange = ( next?: number ) => {
+		onValueChange(
+			next !== undefined ? [ next, parsedUnit ].join( '' ) : undefined
+		);
+	};
+
 	// Set selected unit so it can be used as fallback by unlinked controls
 	// when individual sides do not have a value containing a unit.
 	const handleOnUnitChange: UnitControlProps[ 'onUnitChange' ] = ( unit ) => {
@@ -83,9 +91,6 @@ export default function AllInputControl( {
 		} );
 	};
 
-	const [ parsedQuantity, parsedUnit ] =
-		parseQuantityAndUnitFromRawValue( allValue );
-
 	return (
 		<HStack>
 			<UnitControl
@@ -94,7 +99,7 @@ export default function AllInputControl( {
 				id={ inputId }
 				isOnly
 				value={ parsedQuantity }
-				onChange={ handleOnChange }
+				onChange={ unitControlOnChange }
 				onUnitChange={ handleOnUnitChange }
 				onFocus={ handleOnFocus }
 				onHoverOn={ handleOnHoverOn }
@@ -109,9 +114,7 @@ export default function AllInputControl( {
 				hideLabelFromVision
 				initialPosition={ parsedQuantity ?? 0 }
 				label={ LABELS.all }
-				onChange={ ( newValue ) => {
-					sliderOnChange( [ newValue, parsedUnit ].join( '' ) );
-				} }
+				onChange={ sliderOnChange }
 				min={ 0 }
 				max={ CUSTOM_VALUE_SETTINGS[ parsedUnit ?? 'px' ]?.max ?? 10 }
 				step={

```
</details>

### Review thread 15 (resolved)

#### ciampo on 2023-12-14T09:13:41Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1426442013
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -66,6 +75,21 @@ export default function AxialInputControls( {
 		}
 	};
 
+	const sliderOnChange = ( side: GroupedSide, next: string ) => {
+		const nextValues = { ...values };
+
+		if ( side === 'vertical' ) {
+			nextValues.top = next;
+			nextValues.bottom = next;
+		}
+
+		if ( side === 'horizontal' ) {
+			nextValues.left = next;
+			nextValues.right = next;
+		}
+		onChange?.( nextValues );
+	};
+
```

Similarly to what suggested for the "all input controls" scenario, we could apply the same refactor everywhere.

<details>

<summary>It could look something like this</summary>

```diff
diff --git a/packages/components/src/box-control/all-input-control.tsx b/packages/components/src/box-control/all-input-control.tsx
index 814363b3b6..863cf13ab3 100644
--- a/packages/components/src/box-control/all-input-control.tsx
+++ b/packages/components/src/box-control/all-input-control.tsx
@@ -48,7 +48,7 @@ export default function AllInputControl( {
 		onFocus( event, { side: 'all' } );
 	};
 
-	const onValueChange = ( next?: string ) => {
+	const handleOnValueChange = ( next?: string ) => {
 		const isNumeric = next !== undefined && ! isNaN( parseFloat( next ) );
 		const nextValue = isNumeric ? next : undefined;
 		const nextValues = applyValueToSides( values, nextValue, sides );
@@ -56,16 +56,6 @@ export default function AllInputControl( {
 		onChange( nextValues );
 	};
 
-	const unitControlOnChange: UnitControlProps[ 'onChange' ] = ( next ) => {
-		onValueChange( next );
-	};
-
-	const sliderOnChange = ( next?: number ) => {
-		onValueChange(
-			next !== undefined ? [ next, parsedUnit ].join( '' ) : undefined
-		);
-	};
-
 	// Set selected unit so it can be used as fallback by unlinked controls
 	// when individual sides do not have a value containing a unit.
 	const handleOnUnitChange: UnitControlProps[ 'onUnitChange' ] = ( unit ) => {
@@ -99,7 +89,7 @@ export default function AllInputControl( {
 				id={ inputId }
 				isOnly
 				value={ parsedQuantity }
-				onChange={ unitControlOnChange }
+				onChange={ handleOnValueChange }
 				onUnitChange={ handleOnUnitChange }
 				onFocus={ handleOnFocus }
 				onHoverOn={ handleOnHoverOn }
@@ -113,7 +103,13 @@ export default function AllInputControl( {
 				aria-labelledby={ inputId }
 				hideLabelFromVision
 				label={ LABELS.all }
-				onChange={ sliderOnChange }
+				onChange={ ( next ) =>
+					handleOnValueChange(
+						next !== undefined
+							? [ next, parsedUnit ].join( '' )
+							: undefined
+					)
+				}
 				min={ 0 }
 				max={ CUSTOM_VALUE_SETTINGS[ parsedUnit ?? 'px' ]?.max ?? 10 }
 				step={
diff --git a/packages/components/src/box-control/axial-input-controls.tsx b/packages/components/src/box-control/axial-input-controls.tsx
index 9fba588b77..34720a7ef6 100644
--- a/packages/components/src/box-control/axial-input-controls.tsx
+++ b/packages/components/src/box-control/axial-input-controls.tsx
@@ -75,22 +75,7 @@ export default function AxialInputControls( {
 		}
 	};
 
-	const sliderOnChange = ( side: GroupedSide, next: string ) => {
-		const nextValues = { ...values };
-
-		if ( side === 'vertical' ) {
-			nextValues.top = next;
-			nextValues.bottom = next;
-		}
-
-		if ( side === 'horizontal' ) {
-			nextValues.left = next;
-			nextValues.right = next;
-		}
-		onChange?.( nextValues );
-	};
-
-	const createHandleOnChange = ( side: GroupedSide ) => ( next?: string ) => {
+	const handleOnValueChange = ( side: GroupedSide, next?: string ) => {
 		if ( ! onChange ) {
 			return;
 		}
@@ -161,7 +146,9 @@ export default function AxialInputControls( {
 								parsedQuantity,
 								selectedUnit ?? parsedUnit,
 							].join( '' ) }
-							onChange={ createHandleOnChange( side ) }
+							onChange={ ( newValue ) =>
+								handleOnValueChange( side, newValue )
+							}
 							onUnitChange={ createHandleOnUnitChange( side ) }
 							onFocus={ createHandleOnFocus( side ) }
 							onHoverOn={ createHandleOnHoverOn( side ) }
@@ -175,15 +162,17 @@ export default function AxialInputControls( {
 							aria-labelledby={ inputId }
 							hideLabelFromVision
 							label={ LABELS[ side ] }
-							onChange={ ( newValue ) => {
-								sliderOnChange(
+							onChange={ ( newValue ) =>
+								handleOnValueChange(
 									side,
-									[
-										newValue,
-										selectedUnit ?? parsedUnit,
-									].join( '' )
-								);
-							} }
+									newValue !== undefined
+										? [
+												newValue,
+												selectedUnit ?? parsedUnit,
+										  ].join( '' )
+										: undefined
+								)
+							}
 							min={ 0 }
 							max={
 								CUSTOM_VALUE_SETTINGS[ selectedUnit ?? 'px' ]
diff --git a/packages/components/src/box-control/input-controls.tsx b/packages/components/src/box-control/input-controls.tsx
index 82a9701959..98713c78d8 100644
--- a/packages/components/src/box-control/input-controls.tsx
+++ b/packages/components/src/box-control/input-controls.tsx
@@ -14,7 +14,7 @@ import {
 	InputWrapper,
 } from './styles/box-control-styles';
 import type { BoxControlInputControlProps, BoxControlValue } from './types';
-import type { UnitControlProps } from '../unit-control/types';
+// import type { UnitControlProps } from '../unit-control/types';
 
 const noop = () => {};
 
@@ -49,50 +49,43 @@ export default function BoxInputControls( {
 		onChange( nextValues );
 	};
 
-	const sliderOnChange = ( side: keyof BoxControlValue, next: string ) => {
+	const handleOnValueChange = (
+		side: keyof BoxControlValue,
+		next?: string,
+		extra?: { event: React.SyntheticEvent< Element, Event > }
+	) => {
 		const nextValues = { ...values };
-		nextValues[ side ] = next;
-		handleOnChange( nextValues );
-	};
-
-	const createHandleOnChange: (
-		side: keyof BoxControlValue
-	) => UnitControlProps[ 'onChange' ] =
-		( side ) =>
-		( next, { event } ) => {
-			const nextValues = { ...values };
-			const isNumeric =
-				next !== undefined && ! isNaN( parseFloat( next ) );
-			const nextValue = isNumeric ? next : undefined;
-
-			nextValues[ side ] = nextValue;
-
-			/**
-			 * Supports changing pair sides. For example, holding the ALT key
-			 * when changing the TOP will also update BOTTOM.
-			 */
-			// @ts-expect-error - TODO: event.altKey is only present when the change event was
-			// triggered by a keyboard event. Should this feature be implemented differently so
-			// it also works with drag events?
-			if ( event.altKey ) {
-				switch ( side ) {
-					case 'top':
-						nextValues.bottom = nextValue;
-						break;
-					case 'bottom':
-						nextValues.top = nextValue;
-						break;
-					case 'left':
-						nextValues.right = nextValue;
-						break;
-					case 'right':
-						nextValues.left = nextValue;
-						break;
-				}
+		const isNumeric = next !== undefined && ! isNaN( parseFloat( next ) );
+		const nextValue = isNumeric ? next : undefined;
+
+		nextValues[ side ] = nextValue;
+
+		/**
+		 * Supports changing pair sides. For example, holding the ALT key
+		 * when changing the TOP will also update BOTTOM.
+		 */
+		// @ts-expect-error - TODO: event.altKey is only present when the change event was
+		// triggered by a keyboard event. Should this feature be implemented differently so
+		// it also works with drag events?
+		if ( extra?.event.altKey ) {
+			switch ( side ) {
+				case 'top':
+					nextValues.bottom = nextValue;
+					break;
+				case 'bottom':
+					nextValues.top = nextValue;
+					break;
+				case 'left':
+					nextValues.right = nextValue;
+					break;
+				case 'right':
+					nextValues.left = nextValue;
+					break;
 			}
+		}
 
-			handleOnChange( nextValues );
-		};
+		handleOnChange( nextValues );
+	};
 
 	const createHandleOnUnitChange =
 		( side: keyof BoxControlValue ) => ( next?: string ) => {
@@ -132,7 +125,9 @@ export default function BoxInputControls( {
 							value={ [ parsedQuantity, computedUnit ].join(
 								''
 							) }
-							onChange={ createHandleOnChange( side ) }
+							onChange={ ( nextValue, extra ) =>
+								handleOnValueChange( side, nextValue, extra )
+							}
 							onUnitChange={ createHandleOnUnitChange( side ) }
 							onFocus={ createHandleOnFocus( side ) }
 							onHoverOn={ createHandleOnHoverOn( side ) }
@@ -146,9 +141,11 @@ export default function BoxInputControls( {
 							__nextHasNoMarginBottom
 							hideLabelFromVision
 							onChange={ ( newValue ) => {
-								sliderOnChange(
+								handleOnValueChange(
 									side,
-									[ newValue, computedUnit ].join( '' )
+									newValue !== undefined
+										? [ newValue, computedUnit ].join( '' )
+										: undefined
 								);
 							} }
 							min={ 0 }

```
</details>

The main benefit is that we don't duplicate the "on change" logic, which should also help making sure we don't introduce unexpected behaviours

#### brookewp on 2023-12-14T23:40:43Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1427410116
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -66,6 +75,21 @@ export default function AxialInputControls( {
 		}
 	};
 
+	const sliderOnChange = ( side: GroupedSide, next: string ) => {
+		const nextValues = { ...values };
+
+		if ( side === 'vertical' ) {
+			nextValues.top = next;
+			nextValues.bottom = next;
+		}
+
+		if ( side === 'horizontal' ) {
+			nextValues.left = next;
+			nextValues.right = next;
+		}
+		onChange?.( nextValues );
+	};
+
```

Thank you for clarifying and for the diff!

### Review thread 16 (resolved)

#### brookewp on 2023-12-15T00:43:50Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1427439402
Path: packages/components/src/box-control/test/index.tsx

~What's strange is now the tests are failing because the role `slider` can't be found. But when logging via `screen.logTestingPlaygroundURL()`, the suggested query is: `screen.getByRole('slider', {
  name: /box control/i
})`~

~It started failing after adding the ids in this commit: https://github.com/WordPress/gutenberg/pull/56665/commits/a2639cb7031f3714ab03ff39fdb8339630d100da~

~But reverting that change isn't resolving the failures/missing role 🤔~

Edit: Looks like I was tired and I think I know the issue... to be updated soon!

#### brookewp on 2023-12-15T21:55:05Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1428524561
Path: packages/components/src/box-control/test/index.tsx

So it was a silly mistake, where the same ID was being added to all mapped inputs. Fixed in https://github.com/WordPress/gutenberg/pull/56665/commits/7f7d54b5b85d9a65868aafe40ec49c53583b8c15

### Review thread 17 (resolved)

#### mirka on 2023-12-26T14:50:11Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436491609
Path: packages/components/src/box-control/types.ts

```diff
@@ -16,6 +16,10 @@ export type BoxControlValue = {
 	left?: string;
 };
 
+export type CustomValueUnits = {
+	[ index: string ]: { max: number; steps: number };
```

Nit

```suggestion
	[ key: string ]: { max: number; steps: number };
```

### Review thread 18 (resolved)

#### mirka on 2023-12-26T17:23:51Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436547388
Path: packages/components/src/box-control/styles/box-control-styles.ts

```diff
@@ -6,42 +6,41 @@ import styled from '@emotion/styled';
 /**
  * Internal dependencies
  */
-import { Flex } from '../../flex';
 import BaseUnitControl from '../../unit-control';
+import BoxControlIcon from '../icon';
+import Button from '../../button';
+import { HStack } from '../../h-stack';
+import RangeControl from '../../range-control';
 import { rtl } from '../../utils';
+import { space } from '../../utils/space';
 import type { BoxUnitControlProps } from '../types';
 
-export const Root = styled.div`
+export const UnitControlWrapper = styled.div`
 	box-sizing: border-box;
-	max-width: 235px;
-	padding-bottom: 12px;
-	width: 100%;
+	max-width: 90px;
 `;
 
-export const Header = styled( Flex )`
-	margin-bottom: 8px;
+export const InputWrapper = styled( HStack )`
+	grid-column: 1 / span 3;
 `;
 
-export const HeaderControlWrapper = styled( Flex )`
-	min-height: 30px;
-	gap: 0;
+export const ResetButton = styled( Button )`
+	grid-area: 1 / 2;
+	justify-self: end;
 `;
 
-export const UnitControlWrapper = styled.div`
-	box-sizing: border-box;
-	max-width: 80px;
+export const LinkedButtonWrapper = styled.div`
+	grid-area: 1 / 3;
+	justify-self: end;
 `;
 
-export const LayoutContainer = styled( Flex )`
-	justify-content: center;
-	padding-top: 8px;
+export const FlexedBoxControlIcon = styled( BoxControlIcon )`
+	flex: 0 0 auto;
 `;
 
-export const Layout = styled( Flex )`
-	position: relative;
-	height: 100%;
+export const FlexedRangeControl = styled( RangeControl )`
 	width: 100%;
-	justify-content: flex-start;
+	margin-right: ${ space( 2 ) };
```

For RTL support:

```suggestion
	margin-inline-end: ${ space( 2 ) };
```

#### brookewp on 2024-01-03T19:17:25Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440838155
Path: packages/components/src/box-control/styles/box-control-styles.ts

```diff
@@ -6,42 +6,41 @@ import styled from '@emotion/styled';
 /**
  * Internal dependencies
  */
-import { Flex } from '../../flex';
 import BaseUnitControl from '../../unit-control';
+import BoxControlIcon from '../icon';
+import Button from '../../button';
+import { HStack } from '../../h-stack';
+import RangeControl from '../../range-control';
 import { rtl } from '../../utils';
+import { space } from '../../utils/space';
 import type { BoxUnitControlProps } from '../types';
 
-export const Root = styled.div`
+export const UnitControlWrapper = styled.div`
 	box-sizing: border-box;
-	max-width: 235px;
-	padding-bottom: 12px;
-	width: 100%;
+	max-width: 90px;
 `;
 
-export const Header = styled( Flex )`
-	margin-bottom: 8px;
+export const InputWrapper = styled( HStack )`
+	grid-column: 1 / span 3;
 `;
 
-export const HeaderControlWrapper = styled( Flex )`
-	min-height: 30px;
-	gap: 0;
+export const ResetButton = styled( Button )`
+	grid-area: 1 / 2;
+	justify-self: end;
 `;
 
-export const UnitControlWrapper = styled.div`
-	box-sizing: border-box;
-	max-width: 80px;
+export const LinkedButtonWrapper = styled.div`
+	grid-area: 1 / 3;
+	justify-self: end;
 `;
 
-export const LayoutContainer = styled( Flex )`
-	justify-content: center;
-	padding-top: 8px;
+export const FlexedBoxControlIcon = styled( BoxControlIcon )`
+	flex: 0 0 auto;
 `;
 
-export const Layout = styled( Flex )`
-	position: relative;
-	height: 100%;
+export const FlexedRangeControl = styled( RangeControl )`
 	width: 100%;
-	justify-content: flex-start;
+	margin-right: ${ space( 2 ) };
```

This is an amazing TIL. 😍 THANK YOU!

### Review thread 19 (resolved)

#### mirka on 2023-12-26T17:39:38Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436552503
Path: packages/components/src/box-control/utils.ts

```diff
@@ -7,7 +7,42 @@ import { __ } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { parseQuantityAndUnitFromRawValue } from '../unit-control/utils';
-import type { BoxControlProps, BoxControlValue } from './types';
+import type {
+	BoxControlProps,
+	BoxControlValue,
+	CustomValueUnits,
+} from './types';
+
+export const CUSTOM_VALUE_SETTINGS: CustomValueUnits = {
+	px: { max: 300, steps: 1 },
```

Nit: Shouldn't these be `step` (singular)?

```suggestion
	px: { max: 300, step: 1 },
```

#### brookewp on 2024-01-03T19:30:22Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440852169
Path: packages/components/src/box-control/utils.ts

```diff
@@ -7,7 +7,42 @@ import { __ } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { parseQuantityAndUnitFromRawValue } from '../unit-control/utils';
-import type { BoxControlProps, BoxControlValue } from './types';
+import type {
+	BoxControlProps,
+	BoxControlValue,
+	CustomValueUnits,
+} from './types';
+
+export const CUSTOM_VALUE_SETTINGS: CustomValueUnits = {
+	px: { max: 300, steps: 1 },
```

I think that makes more sense. I initially grabbed this from the component we've been emulating a bit for this change: https://github.com/WordPress/gutenberg/blob/84715c01148344c318344978508b5d442526ed0c/packages/block-editor/src/components/spacing-sizes-control/input-controls/spacing-input-control.js#L34-L63

### Review thread 20 (resolved)

#### mirka on 2023-12-26T17:49:36Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436555921
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -25,25 +33,41 @@ export default function AllInputControl( {
 	setSelectedUnits,
 	...props
 }: BoxControlInputControlProps ) {
+	const generatedId = useInstanceId( AllInputControl, 'box-control-input' );
+	const inputId = [ generatedId, LABELS.all ].join( '-' );
```

```suggestion
	const inputId = useInstanceId( AllInputControl, 'box-control-input-all' );
```

I think we should just do this because `LABELS.all` will be a translated string and not suitable for `id`s.

### Review thread 21 (resolved)

#### mirka on 2023-12-26T17:58:13Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436558942
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
```

Not sure if this was intentional, but it looks like we have a separate `unitControlOnChange` function which is basically just a renaming of `onValueChange`. Maybe it was to balance it out with `sliderOnChange`, but perhaps not worth the extra overhead?

```suggestion
				onChange={ onValueChange }
```

#### ciampo on 2024-01-02T17:28:11Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1439662747
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
```

It was probably the easiest way to get around the TS signature of `UnitControlProps[ 'onChange' ]`

#### mirka on 2024-01-09T13:30:54Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446092271
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
```

Maybe it was an issue along the way? 🤔  But as of now `onChange={ onValueChange }` passes type checks.

### Review thread 22

#### mirka on 2023-12-26T18:29:36Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436568657
Path: packages/components/src/box-control/utils.ts

```diff
@@ -7,7 +7,42 @@ import { __ } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { parseQuantityAndUnitFromRawValue } from '../unit-control/utils';
-import type { BoxControlProps, BoxControlValue } from './types';
+import type {
+	BoxControlProps,
+	BoxControlValue,
+	CustomValueUnits,
+} from './types';
+
+export const CUSTOM_VALUE_SETTINGS: CustomValueUnits = {
+	px: { max: 300, steps: 1 },
+	'%': { max: 100, steps: 1 },
+	vw: { max: 100, steps: 1 },
+	vh: { max: 100, steps: 1 },
+	em: { max: 10, steps: 0.1 },
+	rm: { max: 10, steps: 0.1 },
+	svw: { max: 100, steps: 1 },
+	lvw: { max: 100, steps: 1 },
+	dvw: { max: 100, steps: 1 },
+	svh: { max: 100, steps: 1 },
+	lvh: { max: 100, steps: 1 },
+	dvh: { max: 100, steps: 1 },
+	vi: { max: 100, steps: 1 },
+	svi: { max: 100, steps: 1 },
+	lvi: { max: 100, steps: 1 },
+	dvi: { max: 100, steps: 1 },
+	vb: { max: 100, steps: 1 },
+	svb: { max: 100, steps: 1 },
+	lvb: { max: 100, steps: 1 },
+	dvb: { max: 100, steps: 1 },
+	vmin: { max: 100, steps: 1 },
+	svmin: { max: 100, steps: 1 },
+	lvmin: { max: 100, steps: 1 },
+	dvmin: { max: 100, steps: 1 },
+	vmax: { max: 100, steps: 1 },
+	svmax: { max: 100, steps: 1 },
+	lvmax: { max: 100, steps: 1 },
+	dvmax: { max: 100, steps: 1 },
+};
 
 export const LABELS = {
 	all: __( 'All' ),
```

I feel like it would be a bit confusing to a screen reader user without the word "sides". Same with "horizontal" and "vertical" 🤔 It might be clearer and more translation-friendly if we straight up said "Top and bottom sides" and "Left and right sides". Or at least "Horizontal sides"?

```suggestion
	all: __( 'All sides' ),
```

#### brookewp on 2024-01-03T19:35:58Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440857837
Path: packages/components/src/box-control/utils.ts

```diff
@@ -7,7 +7,42 @@ import { __ } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { parseQuantityAndUnitFromRawValue } from '../unit-control/utils';
-import type { BoxControlProps, BoxControlValue } from './types';
+import type {
+	BoxControlProps,
+	BoxControlValue,
+	CustomValueUnits,
+} from './types';
+
+export const CUSTOM_VALUE_SETTINGS: CustomValueUnits = {
+	px: { max: 300, steps: 1 },
+	'%': { max: 100, steps: 1 },
+	vw: { max: 100, steps: 1 },
+	vh: { max: 100, steps: 1 },
+	em: { max: 10, steps: 0.1 },
+	rm: { max: 10, steps: 0.1 },
+	svw: { max: 100, steps: 1 },
+	lvw: { max: 100, steps: 1 },
+	dvw: { max: 100, steps: 1 },
+	svh: { max: 100, steps: 1 },
+	lvh: { max: 100, steps: 1 },
+	dvh: { max: 100, steps: 1 },
+	vi: { max: 100, steps: 1 },
+	svi: { max: 100, steps: 1 },
+	lvi: { max: 100, steps: 1 },
+	dvi: { max: 100, steps: 1 },
+	vb: { max: 100, steps: 1 },
+	svb: { max: 100, steps: 1 },
+	lvb: { max: 100, steps: 1 },
+	dvb: { max: 100, steps: 1 },
+	vmin: { max: 100, steps: 1 },
+	svmin: { max: 100, steps: 1 },
+	lvmin: { max: 100, steps: 1 },
+	dvmin: { max: 100, steps: 1 },
+	vmax: { max: 100, steps: 1 },
+	svmax: { max: 100, steps: 1 },
+	lvmax: { max: 100, steps: 1 },
+	dvmax: { max: 100, steps: 1 },
+};
 
 export const LABELS = {
 	all: __( 'All' ),
```

Definitely, great suggestion! I think "Top and bottom sides" and "Left and right sides" makes the most sense.

#### brookewp on 2024-01-03T19:39:07Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440861055
Path: packages/components/src/box-control/utils.ts

```diff
@@ -7,7 +7,42 @@ import { __ } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { parseQuantityAndUnitFromRawValue } from '../unit-control/utils';
-import type { BoxControlProps, BoxControlValue } from './types';
+import type {
+	BoxControlProps,
+	BoxControlValue,
+	CustomValueUnits,
+} from './types';
+
+export const CUSTOM_VALUE_SETTINGS: CustomValueUnits = {
+	px: { max: 300, steps: 1 },
+	'%': { max: 100, steps: 1 },
+	vw: { max: 100, steps: 1 },
+	vh: { max: 100, steps: 1 },
+	em: { max: 10, steps: 0.1 },
+	rm: { max: 10, steps: 0.1 },
+	svw: { max: 100, steps: 1 },
+	lvw: { max: 100, steps: 1 },
+	dvw: { max: 100, steps: 1 },
+	svh: { max: 100, steps: 1 },
+	lvh: { max: 100, steps: 1 },
+	dvh: { max: 100, steps: 1 },
+	vi: { max: 100, steps: 1 },
+	svi: { max: 100, steps: 1 },
+	lvi: { max: 100, steps: 1 },
+	dvi: { max: 100, steps: 1 },
+	vb: { max: 100, steps: 1 },
+	svb: { max: 100, steps: 1 },
+	lvb: { max: 100, steps: 1 },
+	dvb: { max: 100, steps: 1 },
+	vmin: { max: 100, steps: 1 },
+	svmin: { max: 100, steps: 1 },
+	lvmin: { max: 100, steps: 1 },
+	dvmin: { max: 100, steps: 1 },
+	vmax: { max: 100, steps: 1 },
+	svmax: { max: 100, steps: 1 },
+	lvmax: { max: 100, steps: 1 },
+	dvmax: { max: 100, steps: 1 },
+};
 
 export const LABELS = {
 	all: __( 'All' ),
```

I think it would also make sense to update 'Mixed' as well. Do you think 'Mixed sides' is clear?

#### mirka on 2024-01-09T13:20:32Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446082233
Path: packages/components/src/box-control/utils.ts

```diff
@@ -7,7 +7,42 @@ import { __ } from '@wordpress/i18n';
  * Internal dependencies
  */
 import { parseQuantityAndUnitFromRawValue } from '../unit-control/utils';
-import type { BoxControlProps, BoxControlValue } from './types';
+import type {
+	BoxControlProps,
+	BoxControlValue,
+	CustomValueUnits,
+} from './types';
+
+export const CUSTOM_VALUE_SETTINGS: CustomValueUnits = {
+	px: { max: 300, steps: 1 },
+	'%': { max: 100, steps: 1 },
+	vw: { max: 100, steps: 1 },
+	vh: { max: 100, steps: 1 },
+	em: { max: 10, steps: 0.1 },
+	rm: { max: 10, steps: 0.1 },
+	svw: { max: 100, steps: 1 },
+	lvw: { max: 100, steps: 1 },
+	dvw: { max: 100, steps: 1 },
+	svh: { max: 100, steps: 1 },
+	lvh: { max: 100, steps: 1 },
+	dvh: { max: 100, steps: 1 },
+	vi: { max: 100, steps: 1 },
+	svi: { max: 100, steps: 1 },
+	lvi: { max: 100, steps: 1 },
+	dvi: { max: 100, steps: 1 },
+	vb: { max: 100, steps: 1 },
+	svb: { max: 100, steps: 1 },
+	lvb: { max: 100, steps: 1 },
+	dvb: { max: 100, steps: 1 },
+	vmin: { max: 100, steps: 1 },
+	svmin: { max: 100, steps: 1 },
+	lvmin: { max: 100, steps: 1 },
+	dvmin: { max: 100, steps: 1 },
+	vmax: { max: 100, steps: 1 },
+	svmax: { max: 100, steps: 1 },
+	lvmax: { max: 100, steps: 1 },
+	dvmax: { max: 100, steps: 1 },
+};
 
 export const LABELS = {
 	all: __( 'All' ),
```

Thinking about [this placeholder behavior](https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446076146) made me realize that "Mixed" is mixed _values_, not mixed _sides_. Given the limited space for the placeholder, it might be better to just keep it "Mixed", and maybe add a translators note in case that helps.

`/* translators: Shown as input placeholder when there are mixed values. */` or something like that.

### Review thread 23 (resolved)

#### mirka on 2023-12-26T19:07:38Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436580153
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
```

To be honest I'm not sure if this is 100% advisable, since I couldn't really find any examples that set an `aria-controls` on a slider. But it doesn't seem harmful, and it seems to allow [element jumping in JAWS](https://a11ysupport.io/tech/aria/aria-controls_attribute), which can be convenient I guess.

#### brookewp on 2024-01-03T19:14:07Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440834506
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
```

I shared a bit on this in another comment: https://github.com/WordPress/gutenberg/pull/56665/files#r1424628822

#### andrewhayward on 2024-01-04T10:29:41Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1441583658
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
```

Unfortunately, there is limited support for `aria-controls`, and the little support that does exist isn't very good. [The spec](https://w3c.github.io/aria/#aria-controls) isn't very detailed, and most <abbr title="Assistive Technology">AT</abbr> providers have avoided dealing with it.

However, I don't see that as a developer issue, and while the attribute exists, I would advocate using it where appropriate, if only in the hopes that the spec is clarified, and the <abbr title="User Agent">UA</abbr>/AT support is improved.

### Review thread 24 (resolved)

#### mirka on 2023-12-26T19:08:58Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436580573
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
```

This one is invalid since `inputId` points to the input field and not a label/string.

#### mirka on 2024-01-09T13:08:18Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446070908
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
```

Sorry, we still need to give this RangeControl the same `label` as the UnitControl. Just the way we were doing it with `aria-labelledby` was invalid.

#### brookewp on 2024-01-09T20:36:59Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446588779
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
```

Ah, makes sense. Thanks for clarifying!

#### brookewp on 2024-01-09T23:00:14Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446707562
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
```

It was awhile ago, but I just recalled a previous [related discussion](https://github.com/WordPress/gutenberg/pull/56665/commits/94db8094068828ac1e22a3cb3ed71d949c263bd2#r1420981268). We initially had another label on `RangeControl` but we weren't sure about it since the slider is a redundant control. 

So that's why I tried to implement `aria-labelledby={ inputId }` instead, but as you pointed out, it wasn't valid since it pointed to the `inputId` rather than to a label. 

But if we add the label to `RangeControl`, we end up with two of the same labels in the DOM.

So I'm thinking it might make sense to add a label with `<VisuallyHidden>`, and then we can use `aria-labelledby` to indicate both controls are for the same input with the one label.  Like:

```diff
diff --git a/packages/components/src/box-control/all-input-control.tsx b/packages/components/src/box-control/all-input-control.tsx
index 8dcefc118b..30ec2c0396 100644
--- a/packages/components/src/box-control/all-input-control.tsx
+++ b/packages/components/src/box-control/all-input-control.tsx
@@ -18,6 +18,7 @@ import {
 	isValuesDefined,
 	CUSTOM_VALUE_SETTINGS,
 } from './utils';
+import { VisuallyHidden } from '../visually-hidden';
 
 const noop = () => {};
 
@@ -73,6 +74,13 @@ export default function AllInputControl( {
 
 	return (
 		<HStack>
+			<VisuallyHidden
+				as="label"
+				htmlFor={ inputId }
+				id={ `${ inputId }-label` }
+			>
+				{ LABELS.all }
+			</VisuallyHidden>
 			<UnitControl
 				{ ...props }
 				className="component-box-control__unit-control"
@@ -84,13 +92,12 @@ export default function AllInputControl( {
 				onUnitChange={ handleOnUnitChange }
 				onFocus={ handleOnFocus }
 				placeholder={ allPlaceholder }
-				label={ LABELS.all }
-				hideLabelFromVision
 			/>
 
 			<FlexedRangeControl
 				__nextHasNoMarginBottom
-				aria-controls={ inputId }
+				aria-labelledby={ `${ inputId }-label` }
 				onChange={ sliderOnChange }
 				min={ 0 }
 				max={ CUSTOM_VALUE_SETTINGS[ parsedUnit ?? 'px' ]?.max ?? 10 }

```

Thoughts?

#### mirka on 2024-01-10T11:01:07Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1447221136
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
```

I don't think there is any benefit in the two inputs being associated to the same `label` node, aside from the number of characters it may or may not save in the bundle. I wouldn't mind it if it weren't adding complexity to the codebase, but in this case I think it does add unnecessary complexity by not using the labeling mechanisms that are already built into UnitControl and RangeControl.

I would do it the simple way (`aria-label`, or `label` + `hideLabelFromVision`), similar to how we do it in RangeControl or FontSizePicker.

#### andrewhayward on 2024-01-11T09:33:15Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1448557933
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
+				disableUnits={ isMixed }
+				id={ inputId }
+				isOnly
+				value={ allValue }
+				onChange={ unitControlOnChange }
+				onUnitChange={ handleOnUnitChange }
+				onFocus={ handleOnFocus }
+				onHoverOn={ handleOnHoverOn }
+				onHoverOff={ handleOnHoverOff }
+				placeholder={ allPlaceholder }
+			/>
+
+			<FlexedRangeControl
+				__nextHasNoMarginBottom
+				aria-controls={ inputId }
+				aria-labelledby={ inputId }
```

> I don't think there is any benefit in the two inputs being associated to the same label node... I would do it the simple way.

Agreed that we should proceed in whatever way makes the most sense from a development perspective here. The important thing is that the fields are labelled correctly, so as long as that's the case, if it takes two labels, it takes two labels.

### Review thread 25 (resolved)

#### mirka on 2023-12-26T19:58:27Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436595163
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
```

This is a pre-existing issue, but I believe the `label` here should not be the `label` coming from the BoxControl itself, but `LABELS.all`.

#### brookewp on 2024-01-03T21:17:06Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440961875
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
```

I believe it was handled this way so a tooltip wouldn't show for 'all': 

<img width="498" alt="Screenshot 2024-01-03 at 1 15 27 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/9e6ead13-d90d-49b9-ada9-8d1b314dbc60">

I can look into separating it from the Tooltip, so the labelling is better and more consistent.

#### brookewp on 2024-01-04T01:54:30Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1441187803
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
```

This led me down a bit of a journey. 😅 But I was able to do a bunch more cleanup. The styles were never removed for 'visualizer' [when the rest was replaced.](https://github.com/WordPress/gutenberg/pull/40505)

And there were [custom hover functions for the visualizer](https://github.com/WordPress/gutenberg/pull/23041), so those could be removed, which led me to remove the custom UnitControl. 

So now there isn't a tooltip for all inputs and the label should be correct.

#### mirka on 2024-01-09T15:49:03Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446269941
Path: packages/components/src/box-control/all-input-control.tsx

```diff
@@ -70,17 +94,34 @@ export default function AllInputControl( {
 	};
 
 	return (
-		<UnitControl
-			{ ...props }
-			disableUnits={ isMixed }
-			isOnly
-			value={ allValue }
-			onChange={ handleOnChange }
-			onUnitChange={ handleOnUnitChange }
-			onFocus={ handleOnFocus }
-			onHoverOn={ handleOnHoverOn }
-			onHoverOff={ handleOnHoverOff }
-			placeholder={ allPlaceholder }
-		/>
+		<HStack>
+			<UnitControl
+				{ ...props }
```

Wow, I see! Thanks for looking into it, and great cleanup 🚀

### Review thread 26 (resolved)

#### mirka on 2023-12-26T20:45:08Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436608245
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -128,26 +137,62 @@ export default function AxialInputControls( {
 					side === 'vertical'
 						? selectedUnits.top
 						: selectedUnits.left;
+
+				const inputId = [ generatedId, side ].join( '-' );
+
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<InputWrapper key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							id={ inputId }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
```

All this logic (and the associated CSS) for the border radius stuff is now obsolete, right?

#### brookewp on 2024-01-03T21:08:46Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440952849
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -128,26 +137,62 @@ export default function AxialInputControls( {
 					side === 'vertical'
 						? selectedUnits.top
 						: selectedUnits.left;
+
+				const inputId = [ generatedId, side ].join( '-' );
+
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<InputWrapper key={ `box-control-${ side }` }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<UnitControl
+							{ ...props }
+							id={ inputId }
+							isFirst={ first === side }
+							isLast={ last === side }
+							isOnly={ only === side }
```

Yes, good catch! This made for a really nice cleanup in https://github.com/WordPress/gutenberg/pull/56665/commits/7cdab786a529851e31fd08e6af3742e978613eb1

### Review thread 27 (resolved)

#### mirka on 2023-12-26T20:50:34Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436609675
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -128,26 +137,62 @@ export default function AxialInputControls( {
 					side === 'vertical'
 						? selectedUnits.top
 						: selectedUnits.left;
+
+				const inputId = [ generatedId, side ].join( '-' );
+
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<InputWrapper key={ `box-control-${ side }` }>
```

Nit: This is a React `key` and not an HTML `id`, so we only have to worry about uniqueness among the items of this particular array (which is `filteredSides`).

```suggestion
					<InputWrapper key={ side }>
```

#### brookewp on 2024-01-03T21:09:34Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1440953582
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -128,26 +137,62 @@ export default function AxialInputControls( {
 					side === 'vertical'
 						? selectedUnits.top
 						: selectedUnits.left;
+
+				const inputId = [ generatedId, side ].join( '-' );
+
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<InputWrapper key={ `box-control-${ side }` }>
```

A very helpful nit — thank you!

### Review thread 28

#### brookewp on 2024-01-08T20:46:12Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1445310827
Path: packages/components/src/box-control/test/index.tsx

```diff
@@ -33,7 +33,10 @@ describe( 'BoxControl', () => {
 			render( <BoxControl onChange={ () => {} } /> );
 
 			expect(
-				screen.getByRole( 'textbox', { name: 'Box Control' } )
+				screen.getByRole( 'group', { name: 'Box Control' } )
+			).toBeVisible();
+			expect(
+				screen.getByRole( 'textbox', { name: 'All sides' } )
```

The labels have been updated to be more clear, as discussed: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436568657

**Context of the change:** 

All of the changes in https://github.com/WordPress/gutenberg/pull/56665/commits/25cf3f686f4c4f9cc3b2e69d9e991674328e1263 will just be naming updates, except for the highlighted portion above. The change here was adding an additional assertion to still check for the input, but with the new name. Before, the label for the component (`group` role) was also the label of the first input, but only when the sides were linked. Therefore the assertion would pass if the query was `screen.getByRole( 'group', { name: 'Box Control' }` or `screen.getByRole( 'textbox', { name: 'Box Control' }`. Which is why the query could be updated to `group` from `textbox` with no additional changes needed. The original labelling could be confusing when using a screenreader, with just the one input having a different labelling scheme and it being so generic. 

**Tl;dr, what has been changed:** 

So instead, now, the label implemented by the consumer for the purpose of the component will only be applied to the `group`. And each input is labelled, so the linked input will now be labelled as 'All sides' or 'Mixed sides'. And unlinked will be 'Top side', 'Left side', 'Left and Right sides', etc.

### Review thread 29 (resolved)

#### brookewp on 2024-01-08T21:36:57Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1445368727
Path: packages/components/src/box-control/test/index.tsx

```diff
@@ -181,18 +238,71 @@ describe( 'BoxControl', () => {
 
 			await user.type(
 				screen.getByRole( 'textbox', {
-					name: 'Vertical',
+					name: 'Top and bottom sides',
 				} ),
 				'100'
 			);
 
 			expect(
-				screen.getByRole( 'textbox', { name: 'Vertical' } )
+				screen.getByRole( 'textbox', { name: 'Top and bottom sides' } )
 			).toHaveValue( '100' );
 			expect(
-				screen.getByRole( 'textbox', { name: 'Horizontal' } )
+				screen.getByRole( 'textbox', { name: 'Left and right sides' } )
 			).not.toHaveValue();
 		} );
+
+		it( 'should update a whole axis using a slider when value is changed when unlinked', async () => {
+			const user = userEvent.setup();
+
+			render( <Example splitOnAxis /> );
+
+			await user.click(
+				screen.getByRole( 'button', { name: 'Unlink sides' } )
+			);
+
+			const slider = screen.getByRole( 'slider', {
+				name: 'Left and right sides',
+			} );
+
+			fireEvent.change( slider, { target: { value: 50 } } );
+
+			expect( slider ).toHaveValue( '50' );
+			expect(
+				screen.getByRole( 'textbox', { name: 'Top and bottom sides' } )
+			).not.toHaveValue();
+			expect(
+				screen.getByRole( 'textbox', { name: 'Left and right sides' } )
+			).toHaveValue( '50' );
+		} );
+
+		it( 'should show "Mixed sides" label when sides have different values but are linked', async () => {
+			const user = userEvent.setup();
+
+			render( <Example /> );
+
+			const unlinkButton = screen.getByRole( 'button', {
+				name: 'Unlink sides',
+			} );
+
+			await user.click( unlinkButton );
+
+			await user.type(
+				screen.getByRole( 'textbox', {
+					name: 'Right side',
+				} ),
+				'13'
+			);
+
+			expect(
+				screen.getByRole( 'textbox', { name: 'Right side' } )
+			).toHaveValue( '13' );
+
+			await user.click( unlinkButton );
+
+			expect(
+				screen.getByRole( 'textbox', { name: 'Mixed sides' } )
+			).toHaveValue( '' );
+		} );
```

I added this test after updating the labels as I noticed there wasn't a test for 'Mixed sides'. 

I updated the label with a ternary so the label could be 'Mixed sides', but I'm curious to see what others think: 
https://github.com/WordPress/gutenberg/blob/3f9ec19bb0bc2e6d2c77f0305117215035c6d341/packages/components/src/box-control/all-input-control.tsx#L87

If we revert back to just `LABELS.all,` it would have the label 'All sides' with 'Mixed sides' as the placeholder.

#### mirka on 2024-01-09T13:14:08Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446076146
Path: packages/components/src/box-control/test/index.tsx

```diff
@@ -181,18 +238,71 @@ describe( 'BoxControl', () => {
 
 			await user.type(
 				screen.getByRole( 'textbox', {
-					name: 'Vertical',
+					name: 'Top and bottom sides',
 				} ),
 				'100'
 			);
 
 			expect(
-				screen.getByRole( 'textbox', { name: 'Vertical' } )
+				screen.getByRole( 'textbox', { name: 'Top and bottom sides' } )
 			).toHaveValue( '100' );
 			expect(
-				screen.getByRole( 'textbox', { name: 'Horizontal' } )
+				screen.getByRole( 'textbox', { name: 'Left and right sides' } )
 			).not.toHaveValue();
 		} );
+
+		it( 'should update a whole axis using a slider when value is changed when unlinked', async () => {
+			const user = userEvent.setup();
+
+			render( <Example splitOnAxis /> );
+
+			await user.click(
+				screen.getByRole( 'button', { name: 'Unlink sides' } )
+			);
+
+			const slider = screen.getByRole( 'slider', {
+				name: 'Left and right sides',
+			} );
+
+			fireEvent.change( slider, { target: { value: 50 } } );
+
+			expect( slider ).toHaveValue( '50' );
+			expect(
+				screen.getByRole( 'textbox', { name: 'Top and bottom sides' } )
+			).not.toHaveValue();
+			expect(
+				screen.getByRole( 'textbox', { name: 'Left and right sides' } )
+			).toHaveValue( '50' );
+		} );
+
+		it( 'should show "Mixed sides" label when sides have different values but are linked', async () => {
+			const user = userEvent.setup();
+
+			render( <Example /> );
+
+			const unlinkButton = screen.getByRole( 'button', {
+				name: 'Unlink sides',
+			} );
+
+			await user.click( unlinkButton );
+
+			await user.type(
+				screen.getByRole( 'textbox', {
+					name: 'Right side',
+				} ),
+				'13'
+			);
+
+			expect(
+				screen.getByRole( 'textbox', { name: 'Right side' } )
+			).toHaveValue( '13' );
+
+			await user.click( unlinkButton );
+
+			expect(
+				screen.getByRole( 'textbox', { name: 'Mixed sides' } )
+			).toHaveValue( '' );
+		} );
```

> If we revert back to just `LABELS.all,` it would have the label 'All sides' with 'Mixed sides' as the placeholder.

Interesting. I think it does makes more sense _without_ the ternary, because the `Mixed` placeholder signifies that we can't show a single value due to there being mixed values. Whereas, the `All sides` label signifies that if you input a value into the field, it will set that value to all sides, which is true whether or not the values are currently mixed.

#### brookewp on 2024-01-09T20:36:00Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446587985
Path: packages/components/src/box-control/test/index.tsx

```diff
@@ -181,18 +238,71 @@ describe( 'BoxControl', () => {
 
 			await user.type(
 				screen.getByRole( 'textbox', {
-					name: 'Vertical',
+					name: 'Top and bottom sides',
 				} ),
 				'100'
 			);
 
 			expect(
-				screen.getByRole( 'textbox', { name: 'Vertical' } )
+				screen.getByRole( 'textbox', { name: 'Top and bottom sides' } )
 			).toHaveValue( '100' );
 			expect(
-				screen.getByRole( 'textbox', { name: 'Horizontal' } )
+				screen.getByRole( 'textbox', { name: 'Left and right sides' } )
 			).not.toHaveValue();
 		} );
+
+		it( 'should update a whole axis using a slider when value is changed when unlinked', async () => {
+			const user = userEvent.setup();
+
+			render( <Example splitOnAxis /> );
+
+			await user.click(
+				screen.getByRole( 'button', { name: 'Unlink sides' } )
+			);
+
+			const slider = screen.getByRole( 'slider', {
+				name: 'Left and right sides',
+			} );
+
+			fireEvent.change( slider, { target: { value: 50 } } );
+
+			expect( slider ).toHaveValue( '50' );
+			expect(
+				screen.getByRole( 'textbox', { name: 'Top and bottom sides' } )
+			).not.toHaveValue();
+			expect(
+				screen.getByRole( 'textbox', { name: 'Left and right sides' } )
+			).toHaveValue( '50' );
+		} );
+
+		it( 'should show "Mixed sides" label when sides have different values but are linked', async () => {
+			const user = userEvent.setup();
+
+			render( <Example /> );
+
+			const unlinkButton = screen.getByRole( 'button', {
+				name: 'Unlink sides',
+			} );
+
+			await user.click( unlinkButton );
+
+			await user.type(
+				screen.getByRole( 'textbox', {
+					name: 'Right side',
+				} ),
+				'13'
+			);
+
+			expect(
+				screen.getByRole( 'textbox', { name: 'Right side' } )
+			).toHaveValue( '13' );
+
+			await user.click( unlinkButton );
+
+			expect(
+				screen.getByRole( 'textbox', { name: 'Mixed sides' } )
+			).toHaveValue( '' );
+		} );
```

That makes sense! After seeing 'Mixed sides' as the placeholder, I think it might be confusing. So I'm thinking of going back to 'Mixed' or maybe 'Mixed values'?

### Review thread 30 (resolved)

#### mirka on 2024-01-09T15:53:12Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446275365
Path: packages/components/src/box-control/axial-input-controls.tsx

```diff
@@ -128,26 +96,64 @@ export default function AxialInputControls( {
 					side === 'vertical'
 						? selectedUnits.top
 						: selectedUnits.left;
+
+				const inputId = [ generatedId, side ].join( '-' );
+
 				return (
-					<UnitControl
-						{ ...props }
-						isFirst={ first === side }
-						isLast={ last === side }
-						isOnly={ only === side }
-						value={ [
-							parsedQuantity,
-							selectedUnit ?? parsedUnit,
-						].join( '' ) }
-						onChange={ createHandleOnChange( side ) }
-						onUnitChange={ createHandleOnUnitChange( side ) }
-						onFocus={ createHandleOnFocus( side ) }
-						onHoverOn={ createHandleOnHoverOn( side ) }
-						onHoverOff={ createHandleOnHoverOff( side ) }
-						label={ LABELS[ side ] }
-						key={ side }
-					/>
+					<InputWrapper key={ side }>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<Tooltip placement="top-end" text={ LABELS[ side ] }>
+							<UnitControl
+								{ ...props }
+								className="component-box-control__unit-control"
+								id={ inputId }
+								isPressEnterToChange
+								value={ [
+									parsedQuantity,
+									selectedUnit ?? parsedUnit,
+								].join( '' ) }
+								onChange={ ( newValue ) =>
+									handleOnValueChange( side, newValue )
+								}
+								onUnitChange={ createHandleOnUnitChange(
+									side
+								) }
+								onFocus={ createHandleOnFocus( side ) }
+								label={ LABELS[ side ] }
+								hideLabelFromVision
+								key={ side }
+							/>
+						</Tooltip>
+						<FlexedRangeControl
+							__nextHasNoMarginBottom
+							aria-controls={ inputId }
+							aria-labelledby={ inputId }
```

Invalid labeling here, same as in the [`all-input-control`](https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436580573).

### Review thread 31 (resolved)

#### mirka on 2024-01-09T15:57:19Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446280805
Path: packages/components/src/box-control/input-controls.tsx

```diff
@@ -90,45 +89,73 @@ export default function BoxInputControls( {
 		? ALL_SIDES.filter( ( side ) => sides.includes( side ) )
 		: ALL_SIDES;
 
-	const first = filteredSides[ 0 ];
-	const last = filteredSides[ filteredSides.length - 1 ];
-	const only = first === last && first;
-
 	return (
-		<LayoutContainer className="component-box-control__input-controls-wrapper">
-			<Layout
-				gap={ 0 }
-				align="top"
-				className="component-box-control__input-controls"
-			>
-				{ filteredSides.map( ( side ) => {
-					const [ parsedQuantity, parsedUnit ] =
-						parseQuantityAndUnitFromRawValue( values[ side ] );
+		<>
+			{ filteredSides.map( ( side ) => {
+				const [ parsedQuantity, parsedUnit ] =
+					parseQuantityAndUnitFromRawValue( values[ side ] );
+
+				const computedUnit = values[ side ]
+					? parsedUnit
+					: selectedUnits[ side ];
+
+				const inputId = [ generatedId, side ].join( '-' );
 
-					const computedUnit = values[ side ]
-						? parsedUnit
-						: selectedUnits[ side ];
+				return (
+					<InputWrapper key={ `box-control-${ side }` } expanded>
+						<FlexedBoxControlIcon side={ side } sides={ sides } />
+						<Tooltip placement="top-end" text={ LABELS[ side ] }>
+							<UnitControl
+								{ ...props }
+								className="component-box-control__unit-control"
+								id={ inputId }
+								isPressEnterToChange
+								value={ [ parsedQuantity, computedUnit ].join(
+									''
+								) }
+								onChange={ ( nextValue, extra ) =>
+									handleOnValueChange(
+										side,
+										nextValue,
+										extra
+									)
+								}
+								onUnitChange={ createHandleOnUnitChange(
+									side
+								) }
+								onFocus={ createHandleOnFocus( side ) }
+								label={ LABELS[ side ] }
+								hideLabelFromVision
+							/>
+						</Tooltip>
 
-					return (
-						<UnitControl
-							{ ...props }
-							isFirst={ first === side }
-							isLast={ last === side }
-							isOnly={ only === side }
-							value={ [ parsedQuantity, computedUnit ].join(
-								''
-							) }
-							onChange={ createHandleOnChange( side ) }
-							onUnitChange={ createHandleOnUnitChange( side ) }
-							onFocus={ createHandleOnFocus( side ) }
-							onHoverOn={ createHandleOnHoverOn( side ) }
-							onHoverOff={ createHandleOnHoverOff( side ) }
-							label={ LABELS[ side ] }
-							key={ `box-control-${ side }` }
+						<FlexedRangeControl
+							aria-controls={ inputId }
+							aria-labelledby={ inputId }
```

Invalid labeling here, same as in the [`all-input-control`](https://github.com/WordPress/gutenberg/pull/56665#discussion_r1436580573).

### Review thread 32 (resolved)

#### mirka on 2024-01-09T16:00:31Z

URL: https://github.com/WordPress/gutenberg/pull/56665#discussion_r1446284989
Path: packages/components/src/box-control/styles/box-control-styles.ts

```diff
@@ -1,80 +1,40 @@
 /**
  * External dependencies
  */
-import { css } from '@emotion/react';
 import styled from '@emotion/styled';
 /**
  * Internal dependencies
  */
-import { Flex } from '../../flex';
+import BoxControlIcon from '../icon';
+import Button from '../../button';
+import { HStack } from '../../h-stack';
+import RangeControl from '../../range-control';
 import BaseUnitControl from '../../unit-control';
-import { rtl } from '../../utils';
-import type { BoxUnitControlProps } from '../types';
+import { space } from '../../utils/space';
 
-export const Root = styled.div`
-	box-sizing: border-box;
-	max-width: 235px;
-	padding-bottom: 12px;
-	width: 100%;
+export const UnitControl = styled( BaseUnitControl )`
```

Nit: For consistency with the other styled components.

```suggestion
export const StyledUnitControl = styled( UnitControl )`
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/box-control/all-input-control.tsx (MODIFIED, +54/-34)
- packages/components/src/box-control/axial-input-controls.tsx (MODIFIED, +76/-69)
- packages/components/src/box-control/index.tsx (MODIFIED, +45/-54)
- packages/components/src/box-control/input-controls.tsx (MODIFIED, +111/-83)
- packages/components/src/box-control/styles/box-control-styles.ts (MODIFIED, +21/-61)
- packages/components/src/box-control/styles/box-control-visualizer-styles.ts (DELETED, +0/-75)
- packages/components/src/box-control/test/index.tsx (MODIFIED, +126/-18)
- packages/components/src/box-control/types.ts (MODIFIED, +4/-21)
- packages/components/src/box-control/unit-control.tsx (DELETED, +0/-74)
- packages/components/src/box-control/utils.ts (MODIFIED, +43/-8)
