---
number: 14335
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/14335
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Status] In Progress","[Feature] Drag and Drop","[Package] Components"]
---

# Issue #14335: DropZone: Allow overriding children

- URL: https://github.com/WordPress/gutenberg/issues/14335
- Author: swissspidy
- Created: 2019-03-08T09:20:26Z
- Updated: 2025-05-28T13:25:42Z
- Comments: 2 of 2

## Body

**Is your feature request related to a problem? Please describe.**

I am trying to use the `DropZone` component together with `Draggable` to implement a simple sorting mechanism.

While doing so, I realized I can change the default "Drop files to upload" label, but not the upload icon.

In the 'BlockDropZone' component, this is "solved" by just hiding the content using CSS, which isn't really nice, but currently the only way to achieve this.

**Describe the solution you'd like**

It would be way easier if we could pass custom content as children or have some other way to remove the default content in `DropZone`.

## Issue comments

### youknowriad on 2020-04-10T23:02:50Z

URL: https://github.com/WordPress/gutenberg/issues/14335#issuecomment-612257084

Just nothing that there's a `__unstableUseDropZone` react hook allowing alternative UIs for drop zones. We should probably consider making this API stable instead of adding children support to DropZone.

Ideally, we'd also avoid the need for a DropzoneProvider at some point.

### Mamaduka on 2025-05-08T15:41:55Z

URL: https://github.com/WordPress/gutenberg/issues/14335#issuecomment-2863513729

I think it makes sense, considering that `__unstableUseDropZone` still requires somewhat complex configuration, even in core, we use `DropZone` in a few places.

We can either allow overriding contents via `children` or support `icon` prop and only override the icon alongside the label.

P.S. The `DropzoneProvider` has been deprecated since WP 5.8.

