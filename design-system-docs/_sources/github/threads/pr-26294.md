---
number: 26294
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/26294
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #26294: RadioControl: Use CSS-in-JS

- URL: https://github.com/WordPress/gutenberg/pull/26294
- Author: sarayourfriend
- Created: 2020-10-19T18:48:11Z
- Updated: 2020-11-09T17:27:09Z
- Comments: 5 of 5
- Review threads: 1 of 1
- Changed files: 5 of 5
- Diff: ../pr-diffs/26294.diff

## Body

<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/master/docs/contributors/repository-management.md#pull-requests. -->

## Description
Use CSS-in-JS for `RadioControl`.

Note: I discovered a bug documented here while working on this: https://github.com/WordPress/gutenberg/issues/26293. This is a pre-existing bug that I'm not sure exactly how to fix. I think it should be done in the context of a separate pull request once this one is merged and once there is an answer about the intention for the radio control in the bug issue.

## How has this been tested?
Verified there are no differences between this branch and production in Storybook. Also checked in Gutenberg itself.

<img width="299" alt="Captura de Tela 2020-10-27 às 08 22 16" src="https://user-images.githubusercontent.com/24264157/97322989-90640100-182d-11eb-860a-e24316a33ba0.png">


## Types of changes
Non-breaking CSS-in-JS refactor. All classnames are preserved allowing folks to continue targeting the classes that were already there.

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

## Issue comments

### ItsJonQ on 2020-10-28T16:15:47Z

URL: https://github.com/WordPress/gutenberg/pull/26294#issuecomment-718043898

@saramarcondes We're starting to venture into components that require references to the `var( --wp-admin-theme-color )` variable. We currently don't have an automated way to handle this yet, which means it won't work currently in IE11.

I left some more thoughts here:
https://github.com/WordPress/gutenberg/pull/26340#issuecomment-717952369

I don't think there's a good solution yet, other than to manually hardcode the fallbacks 🙈 

We could create a "mixin" that can output the property for use.. something like:

```
const StyledInput = styled.input`
  ${variable('background', 'wp-admin-theme-color', 'blue')}
`
```

That's kind of how you'd do it in Sass. I **really don't** like this solution. It's quite limited (e.g. handling multiple values) and feels... too abstract.  But it is an option if we don't want to hand-write fallbacks.

Would love your thoughts!

### sarayourfriend on 2020-10-28T17:03:19Z

URL: https://github.com/WordPress/gutenberg/pull/26294#issuecomment-718075580

@ItsJonQ Shall we compromise and update `ui.theme` in the colors values to be the following:

```js
css`
  color: blue;
  color: var( --... );
`;
```

I think that might be a better option than trying to come up with a new solution for this when hopefully we could find an already baked one?

### ItsJonQ on 2020-10-28T17:09:57Z

URL: https://github.com/WordPress/gutenberg/pull/26294#issuecomment-718079586

@saramarcondes We can certainly give that a shot 🎉

### sarayourfriend on 2020-10-28T17:11:24Z

URL: https://github.com/WordPress/gutenberg/pull/26294#issuecomment-718080433

I'll update this in the FormToggle PR as it's older and I'm hoping to get it merged sooner 🙂

### sarayourfriend on 2020-11-09T17:27:09Z

URL: https://github.com/WordPress/gutenberg/pull/26294#issuecomment-724158216

Closing in favor of focusing on G2-components integration.

## Review threads

### Review thread 1 (resolved)

#### sarayourfriend on 2020-10-19T18:55:46Z

URL: https://github.com/WordPress/gutenberg/pull/26294#discussion_r507989976
Path: packages/components/src/radio-control/styles/radio-control-styles.js

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { breakpoint, config, color } from '../../utils';
+import { inputControl } from '../../utils/input';
+import {
+	StyledHelp as BaseControlHelp,
+	StyledField as BaseControlField,
+} from '../../base-control/styles/base-control-styles';
+import BaseControl from '../../base-control';
+
+export const RadioControlWrapper = styled( BaseControl )`
+	display: flex;
+	flex-direction: column;
+
+	${ BaseControlHelp } {
+		margin-top: 0;
+	}
+
+	${ BaseControlField } {
+		margin-bottom: 0;
+	}
+`;
+
+export const RadioControlOption = styled.div`
+	&:not( :last-child ) {
+		margin-bottom: 4px;
+	}
+`;
+
+export const RadioControlInput = styled.input`
+	${ inputControl }
+
+	border: ${ config( 'borderWidth' ) } solid ${ color( 'gray.900' ) };
+    margin-top: 0;
+	margin-right: 6px;
+	transition: none;
+	border-radius: ${ config( 'radiusRound' ) };
+
+	&:checked::before {
+		width: 7px;
+		height: 7px;
+		margin: 8px 0 0 8px;
+		background-color: ${ color( 'white' ) };
+
+		// This border serves as a background color in Windows High Contrast mode.
+		border: 3px solid ${ color( 'white' ) };
+
+		${ breakpoint( 'medium' ) } {
+			width: 6px;
+			height: 6px;
+			margin: 4px 0 0 4px;
+		}
+	}
+
+	&:focus {
+		box-shadow: 0 0 0 calc( ${ config( 'borderWidth' ) } * 2 ) ${ color(
+	'white'
+) },
+			0 0 0 calc( ${ config( 'borderWidth' ) } * 2 + ${ config(
+	'borderWidthFocus'
+) } )
```

`prettier` sure did a number on these lines. Not sure if it's worth trying to do anything about it.

#### ItsJonQ on 2020-10-23T19:51:33Z

URL: https://github.com/WordPress/gutenberg/pull/26294#discussion_r511123776
Path: packages/components/src/radio-control/styles/radio-control-styles.js

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { breakpoint, config, color } from '../../utils';
+import { inputControl } from '../../utils/input';
+import {
+	StyledHelp as BaseControlHelp,
+	StyledField as BaseControlField,
+} from '../../base-control/styles/base-control-styles';
+import BaseControl from '../../base-control';
+
+export const RadioControlWrapper = styled( BaseControl )`
+	display: flex;
+	flex-direction: column;
+
+	${ BaseControlHelp } {
+		margin-top: 0;
+	}
+
+	${ BaseControlField } {
+		margin-bottom: 0;
+	}
+`;
+
+export const RadioControlOption = styled.div`
+	&:not( :last-child ) {
+		margin-bottom: 4px;
+	}
+`;
+
+export const RadioControlInput = styled.input`
+	${ inputControl }
+
+	border: ${ config( 'borderWidth' ) } solid ${ color( 'gray.900' ) };
+    margin-top: 0;
+	margin-right: 6px;
+	transition: none;
+	border-radius: ${ config( 'radiusRound' ) };
+
+	&:checked::before {
+		width: 7px;
+		height: 7px;
+		margin: 8px 0 0 8px;
+		background-color: ${ color( 'white' ) };
+
+		// This border serves as a background color in Windows High Contrast mode.
+		border: 3px solid ${ color( 'white' ) };
+
+		${ breakpoint( 'medium' ) } {
+			width: 6px;
+			height: 6px;
+			margin: 4px 0 0 4px;
+		}
+	}
+
+	&:focus {
+		box-shadow: 0 0 0 calc( ${ config( 'borderWidth' ) } * 2 ) ${ color(
+	'white'
+) },
+			0 0 0 calc( ${ config( 'borderWidth' ) } * 2 + ${ config(
+	'borderWidthFocus'
+) } )
```

REKT. I'm seeing that on my side as well. Prettier is super unhappy with this. I don't blame it... there are a lot of things going on 🤣 

Maybe we can create a mixin for it. It'll make it easier to read (kinda), but it should make the linters happier.

Something like this:

```js
const radioFocusBoxShadow = () => {
	const boxShadow = [
		`0 0 0 calc( ${ config( 'borderWidth' ) } * 2 ) ${ color( 'white' ) }`,
		`0 0 0 calc( ${ config( 'borderWidth' ) } * 2 + ${ config(
			'borderWidthFocus'
		) } ) var( --wp-admin-theme-color )`,
	].join( ',' );

	return css( { boxShadow } );
};
```

#### ItsJonQ on 2020-10-28T16:04:28Z

URL: https://github.com/WordPress/gutenberg/pull/26294#discussion_r513568128
Path: packages/components/src/radio-control/styles/radio-control-styles.js

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import { breakpoint, config, color } from '../../utils';
+import { inputControl } from '../../utils/input';
+import {
+	StyledHelp as BaseControlHelp,
+	StyledField as BaseControlField,
+} from '../../base-control/styles/base-control-styles';
+import BaseControl from '../../base-control';
+
+export const RadioControlWrapper = styled( BaseControl )`
+	display: flex;
+	flex-direction: column;
+
+	${ BaseControlHelp } {
+		margin-top: 0;
+	}
+
+	${ BaseControlField } {
+		margin-bottom: 0;
+	}
+`;
+
+export const RadioControlOption = styled.div`
+	&:not( :last-child ) {
+		margin-bottom: 4px;
+	}
+`;
+
+export const RadioControlInput = styled.input`
+	${ inputControl }
+
+	border: ${ config( 'borderWidth' ) } solid ${ color( 'gray.900' ) };
+    margin-top: 0;
+	margin-right: 6px;
+	transition: none;
+	border-radius: ${ config( 'radiusRound' ) };
+
+	&:checked::before {
+		width: 7px;
+		height: 7px;
+		margin: 8px 0 0 8px;
+		background-color: ${ color( 'white' ) };
+
+		// This border serves as a background color in Windows High Contrast mode.
+		border: 3px solid ${ color( 'white' ) };
+
+		${ breakpoint( 'medium' ) } {
+			width: 6px;
+			height: 6px;
+			margin: 4px 0 0 4px;
+		}
+	}
+
+	&:focus {
+		box-shadow: 0 0 0 calc( ${ config( 'borderWidth' ) } * 2 ) ${ color(
+	'white'
+) },
+			0 0 0 calc( ${ config( 'borderWidth' ) } * 2 + ${ config(
+	'borderWidthFocus'
+) } )
```

It looks like the linter is still somewhat confused by this. I guess I don't blame it.
Totally not your fault! This makes me think that we need a better strategy (maybe) to handle complex string interpolation for CSS shorthands especially if it involves functions like `calc()`.

## Changed files

- packages/components/src/radio-control/index.js (MODIFIED, +10/-6)
- packages/components/src/radio-control/style.scss (DELETED, +0/-22)
- packages/components/src/radio-control/styles/radio-control-styles.js (ADDED, +88/-0)
- packages/components/src/style.scss (MODIFIED, +0/-1)
- packages/components/src/utils/config-values.js (MODIFIED, +1/-0)
