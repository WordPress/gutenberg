---
number: 70756
type: pull_request
state: MERGED
merged_at: 2025-07-17T14:59:05Z
url: https://github.com/WordPress/gutenberg/pull/70756
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components","[Feature] DataViews"]
---

# PR #70756: Update Icon component to support bundled dataviews package

- URL: https://github.com/WordPress/gutenberg/pull/70756
- Author: youknowriad
- Created: 2025-07-17T09:44:48Z
- Updated: 2025-07-17T14:59:45Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/70756.diff

## Body

## What?

When using the DataViews component from `dataviews/wp` in a WordPress plugin, you can end up with icons not rendering properly. The reason is that the package bundles its own version of `components` and `primitives` packages which means that the test to check whether `SVG` component is used fails (two different instances of SVG).

I think that check is fragile to be honest and shouldn't have existed in the first place. We could in theory just pass "height"/"width" too all components (instead of "size") but to avoid disruptions and potential small breaking changes, I kept the "size" passing for now but just added the height/width props that way the rendering is not broken at least..

## Testing instructions

There should be no impact here, but DataViews actions would look properly if used in a plugin.

## Issue comments

### github-actions on 2025-07-17T09:44:57Z

URL: https://github.com/WordPress/gutenberg/pull/70756#issuecomment-3083384738

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: youknowriad <youknowriad@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1 (resolved)

#### mirka on 2025-07-17T10:45:14Z

URL: https://github.com/WordPress/gutenberg/pull/70756#discussion_r2213002767
Path: packages/components/CHANGELOG.md

```diff
@@ -12,6 +12,8 @@
 -   `SelectControl`: Fix font-size for medium screens to ensure consistency with other inputs ([#70619](https://github.com/WordPress/gutenberg/pull/70619)).
 -   `SelectControl`: Move classnames to the root ([#70643](https://github.com/WordPress/gutenberg/pull/70643)).
 -   `Autocomplete`: Prevent text cursor position loss when clicking to insert an item ([#70660](https://github.com/WordPress/gutenberg/pull/70660)).
+-   Fix the rendering of icons from bundled `DataViews` packages ([#70756](https://github.com/WordPress/gutenberg/pull/70756)).
```

Let's frame this in a way that's useful for the `@wordpress/components` consumer. For example:

```suggestion
-   `Icon`: Pass `size` prop value as `width`/`height` attributes when `icon` is a JSX element that doesn't exactly match type `svg` or `SVG` ([#70756](https://github.com/WordPress/gutenberg/pull/70756)).
```

### Review thread 2 (resolved)

#### mirka on 2025-07-17T11:13:07Z

URL: https://github.com/WordPress/gutenberg/pull/70756#discussion_r2213056607
Path: packages/components/src/icon/index.tsx

```diff
@@ -107,6 +107,8 @@ function Icon( {
 		return cloneElement( icon, {
 			// @ts-ignore Just forwarding the size prop along
 			size,
+			width: size,
+			height: size,
 			...additionalProps,
```

This potentially generates invalid HTML or is a breaking change in cases like this:

```jsx
function MyIconDiv( { size, ...props } ) {
	return (
		<div style={ { width: size, height: size } } { ...props }>
			🏝️
		</div>
	);
}

function MyIconImg( { size, ...props } ) {
	return (
		<img
			width={ size + 1 }
			height={ size + 1 }
			src="https://s.w.org/style/images/about/WordPress-logotype-wmark.png"
			alt=""
			{ ...props }
		/>
	);
}

// Renders invalid attributes (width/height) on a div element
<Icon icon={ <MyIconDiv /> } />

// Overrides existing width/height values
<Icon icon={ <MyIconImg /> } />
```

A non-breaking fix would be preferable, but if not, we might want to note this as a breaking change.

#### youknowriad on 2025-07-17T11:15:05Z

URL: https://github.com/WordPress/gutenberg/pull/70756#discussion_r2213060946
Path: packages/components/src/icon/index.tsx

```diff
@@ -107,6 +107,8 @@ function Icon( {
 		return cloneElement( icon, {
 			// @ts-ignore Just forwarding the size prop along
 			size,
+			width: size,
+			height: size,
 			...additionalProps,
```

I think invalid HTML is fine here because removing the size would be a bigger breaking change.

#### youknowriad on 2025-07-17T11:51:35Z

URL: https://github.com/WordPress/gutenberg/pull/70756#discussion_r2213131970
Path: packages/components/src/icon/index.tsx

```diff
@@ -107,6 +107,8 @@ function Icon( {
 		return cloneElement( icon, {
 			// @ts-ignore Just forwarding the size prop along
 			size,
+			width: size,
+			height: size,
 			...additionalProps,
```

I updated the changelog and marked this as breaking.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +5/-0)
- packages/components/src/icon/index.tsx (MODIFIED, +2/-0)
