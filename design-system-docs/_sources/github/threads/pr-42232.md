---
number: 42232
type: pull_request
state: MERGED
merged_at: 2022-07-26T21:54:27Z
url: https://github.com/WordPress/gutenberg/pull/42232
matched_labels: ["[Package] Components"]
all_labels: ["[Feature] UI Components","[Package] Components","[Feature] Design Tools","[Feature] Colors"]
---

# PR #42232: `ColorPalette`: Display checkered preview background when `value` is transparent

- URL: https://github.com/WordPress/gutenberg/pull/42232
- Author: danielbachhuber
- Created: 2022-07-07T12:18:34Z
- Updated: 2022-07-26T21:54:54Z
- Comments: 7 of 7
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/42232.diff

## Body

## What?

Avoids styling `background` when the provided `value` is transparent (either `transparent`, or a RGBA/HEX value with transparent alpha channel).

### After

<img width="341" alt="image" src="https://user-images.githubusercontent.com/36432/177771289-ba374a0f-57ff-4c22-afaa-796468a65e30.png">

### Before

<img width="373" alt="image" src="https://user-images.githubusercontent.com/36432/177771235-050e05b8-16ac-45fa-8f4d-9119ebb2e735.png">

## Why?

Fixes #42230

## Issue comments

### annezazu on 2022-07-11T19:25:46Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1180779464

@WordPress/gutenberg-design for thoughts here :)

### jameskoster on 2022-07-12T09:38:09Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1181544870

Makes sense to me 👍 

It would be good to fix this holistically to account for alpha channels:

<img width="537" alt="Screenshot 2022-07-12 at 10 37 52" src="https://user-images.githubusercontent.com/846565/178460349-aeaaf9c9-6399-47a5-a3a0-f7af0f9f0f4c.png">

### danielbachhuber on 2022-07-14T18:35:45Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1184773859

> It would be good to fix this holistically to account for alpha channels:

@jameskoster What's the best way to detect the alpha channel?

### jameskoster on 2022-07-15T08:53:09Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1185328413

Sorry, I'm not sure 🙈

### danielbachhuber on 2022-07-15T16:17:09Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1185695334

> > It would be good to fix this holistically to account for alpha channels:
> 
> What's the best way to detect the alpha channel?

@jameskoster It turned out to be pretty simple! `colord( currentValue ).alpha()`

Updated in bcfb85ab5b

### danielbachhuber on 2022-07-26T19:12:31Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1195878286

> Thanks for the fix! If you could add a quick [changelog entry](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CHANGELOG.md) before merging, that would be great.

@mirka You're welcome 😄 Did with 23f9b5188933363d350d1dee408a71119fed3869

> In a follow-up, we may want to look into tweaking the background implementation so the checkered background can be seen through a semi-transparent color. This is already the case with the circular swatch:

I actually looked into this. I think it will involve a bit of CSS retooling, though. I didn't feel comfortable contributing the change because I felt like it might delay the review process (and solving 'transparent' is my immediate need).

### danielbachhuber on 2022-07-26T21:35:24Z

URL: https://github.com/WordPress/gutenberg/pull/42232#issuecomment-1196006545

> I think we're good to go then! 🚀

Awesome, thanks!

## Review threads

### Review thread 1 (resolved)

#### mirka on 2022-07-26T16:58:10Z

URL: https://github.com/WordPress/gutenberg/pull/42232#discussion_r930211656
Path: packages/components/src/color-palette/test/utils.ts

```diff
@@ -21,4 +24,16 @@ describe( 'ColorPalette: Utils', () => {
 			expect( result ).toBe( 'Blue' );
 		} );
 	} );
+	describe( 'showTransparentBackground', () => {
+		test( 'should return true for transparent colors', () => {
+			expect( showTransparentBackground( 'transparent' ) ).toBe( true );
+			expect( showTransparentBackground( '#75757500' ) ).toBe( true );
+		} );
+
+		test( 'should return true for transparent colors', () => {
```

```suggestion
		test( 'should return false for non-transparent colors', () => {
```

### Review thread 2

#### mirka on 2022-07-26T17:04:53Z

URL: https://github.com/WordPress/gutenberg/pull/42232#discussion_r930217723
Path: packages/components/src/color-palette/index.js

```diff
@@ -162,6 +162,10 @@ export const extractColorNameFromCurrentValue = (
 	return __( 'Custom' );
 };
 
+export const showTransparentBackground = ( currentValue ) => {
+	return colord( currentValue ).alpha() === 0;
+};
```

Can we add handling (and a test case) for an `undefined` color value? It will be undefined when the "Clear" button is clicked.

#### danielbachhuber on 2022-07-26T19:07:10Z

URL: https://github.com/WordPress/gutenberg/pull/42232#discussion_r930322206
Path: packages/components/src/color-palette/index.js

```diff
@@ -162,6 +162,10 @@ export const extractColorNameFromCurrentValue = (
 	return __( 'Custom' );
 };
 
+export const showTransparentBackground = ( currentValue ) => {
+	return colord( currentValue ).alpha() === 0;
+};
```

> Can we add handling (and a test case) for an `undefined` color value? It will be undefined when the "Clear" button is clicked.

@mirka Yep, did with d482da9668142d7b8fefb39ec2f59487c9d11b10

### Review thread 3

#### mirka on 2022-07-26T20:42:11Z

URL: https://github.com/WordPress/gutenberg/pull/42232#discussion_r930394208
Path: packages/components/src/color-palette/test/utils.ts

```diff
@@ -21,4 +24,19 @@ describe( 'ColorPalette: Utils', () => {
 			expect( result ).toBe( 'Blue' );
 		} );
 	} );
+	describe( 'showTransparentBackground', () => {
+		test( 'should return undefined for transparent colors', () => {
+			expect( undefined ).toBe( undefined );
+		} );
```

Ah sorry this was unclear, I think the behavior we want is for it to return true:

```suggestion
		test( 'should return true for undefined color values', () => {
			expect( undefined ).toBe( true );
		} );
```

The fact that it "works" as expected with the current `background` implementation is only coincidental. It will be misleading to return a falsy value from `showTransparentBackground( undefined )`.

#### danielbachhuber on 2022-07-26T21:05:26Z

URL: https://github.com/WordPress/gutenberg/pull/42232#discussion_r930411360
Path: packages/components/src/color-palette/test/utils.ts

```diff
@@ -21,4 +24,19 @@ describe( 'ColorPalette: Utils', () => {
 			expect( result ).toBe( 'Blue' );
 		} );
 	} );
+	describe( 'showTransparentBackground', () => {
+		test( 'should return undefined for transparent colors', () => {
+			expect( undefined ).toBe( undefined );
+		} );
```

> The fact that it "works" as expected with the current `background` implementation is only coincidental.

@mirka It's actually because the test is missing the call to `showTransparentBackground()` 🙈 

Fixed with 43afb0cdc47f06010449984cf644548b4fbe42fc

#### mirka on 2022-07-26T21:13:37Z

URL: https://github.com/WordPress/gutenberg/pull/42232#discussion_r930417119
Path: packages/components/src/color-palette/test/utils.ts

```diff
@@ -21,4 +24,19 @@ describe( 'ColorPalette: Utils', () => {
 			expect( result ).toBe( 'Blue' );
 		} );
 	} );
+	describe( 'showTransparentBackground', () => {
+		test( 'should return undefined for transparent colors', () => {
+			expect( undefined ).toBe( undefined );
+		} );
```

LOL

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/color-palette/index.js (MODIFIED, +19/-8)
- packages/components/src/color-palette/test/utils.ts (MODIFIED, +19/-1)
