---
number: 44632
type: pull_request
state: MERGED
merged_at: 2022-10-26T08:24:08Z
url: https://github.com/WordPress/gutenberg/pull/44632
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components"]
---

# PR #44632: Convert the `ColorPalette` component to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/44632
- Author: kienstra
- Created: 2022-10-02T22:17:47Z
- Updated: 2022-11-14T16:01:18Z
- Comments: 19 of 19
- Review threads: 27 of 27
- Changed files: 10 of 10
- Diff: ../pr-diffs/44632.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Convert the `ColorPalette` component to TypeScript

## Why?
As part of an [effort](https://github.com/WordPress/gutenberg/issues/35744) to convert `@wordpress/components` to TypeScript

## How?
By adding types to the `ColorPalette` component and its story.

## Testing Instructions
1. `npm run storybook:dev`
2. Verify that the `ColorPalette` component works the same as before

## Screenshots or screencast <!-- if applicable -->
![color-palette](https://user-images.githubusercontent.com/4063887/193477342-2ef0e1fe-9e06-4615-8dfb-7a95515fff4e.gif)

## Potential follow-ups

- Refactor all styles to Emotion
- Improve APIs — currently `__experimentalHasMultipleOrigins` relies on the fact that the format of `color` is correct. What if we inferred the single/multiple palette based on the format of `color` instead?

## Issue comments

### kienstra on 2022-10-03T18:15:38Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1265844622

Hi @ciampo, would you have time to review this?

You probably have a ton on your plate, thanks for any time you can give it.

Thanks, Marco!

### kienstra on 2022-10-11T19:45:03Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1275190507

Hi @ajitbohra,
Would you happen to have time to review this? You're probably really busy, so thanks for whatever you can spend on this.

Thanks, Ajit!

### ciampo on 2022-10-12T07:44:23Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1275732091

Hey @kienstra , I will try to review this today. I would normally be much faster, but I've been AFK last week and I'm still catching up — apologies!

In the meantime, I just wanted to thank you for submitting this PR in the first place!

### kienstra on 2022-10-12T15:47:57Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1276393384

Hi @ciampo, thanks so much for reviewing this and for your suggestions! I'll apply them today.

### kienstra on 2022-10-12T20:13:04Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1276682136

>Out of curiosity — how did you come across the TypeScript migration?

Haha, I'm not sure. 

I love TypeScript, and searched for TypeScript issues in Gutenberg 😄

### kienstra on 2022-10-12T20:41:04Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1276715073

>Following the observations above, I was able to make a few changes to the code that seem to make the code a bit more tidy and remove the need for a couple of `@ts-ignore` (click to expand)

Thanks, I committed your [diff](https://github.com/WordPress/gutenberg/pull/44632#pullrequestreview-1139113416) verbatim in https://github.com/WordPress/gutenberg/pull/44632/commits/4a602dd3812c72f3422b286136a65dfd5d4a2bb3.

### kienstra on 2022-10-12T21:19:48Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1276748461

Hi @ciampo, 
Just finished applying your suggestions. 

Thanks for all the care you put into your review. 

When you suggested code, I committed it verbatim.

### ciampo on 2022-10-18T15:37:29Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1282594808

Thank you! I'll have another round of review today or tomorrow

### kienstra on 2022-10-19T16:33:46Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1284283926

Thanks a lot, Marco! I'll apply your suggestions today.

### kienstra on 2022-10-19T21:49:35Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1284610064

Hi @ciampo,
Thanks a lot for your suggestions, they're applied now.

>One thing that certainly needs improving is the component's README — we'll need to basically rewrite it so that it adheres to the latest format that we've been using, and including all the props information.

Would you like to suggest the changes in `README`, or should I start those?

Thanks, Marco!

Update: sorry for spelling your name Marcos before, a friend is named Marcos 😄

### ciampo on 2022-10-21T12:20:59Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1286888443

> Would you like to suggest the changes in README, or should I start those?

Let's leave the README as the last thing, in case we make further adjustments to the component in the meantime.

As the next steps, could you refactor `styles.js` and `test/index.js` to TypeScript too?

The unit tests in particular are a great way to double check that our tests are actually using the component by respecting the types that we've just added in this PR.

### kienstra on 2022-10-21T20:39:32Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1287415639

Hi @ciampo,
>As the next steps, could you refactor styles.js and test/index.js to TypeScript too?

Sure, I just did that.

* [styles.js](https://github.com/WordPress/gutenberg/blob/430fb9faadb00a5be74e414a7975bec672a3bba4/packages/components/src/color-palette/styles.js ) is renamed to `.ts`, with no change
* [test__snapshots__/index.js.snap](https://github.com/WordPress/gutenberg/blob/430fb9faadb00a5be74e414a7975bec672a3bba4/packages/components/src/color-palette/test/__snapshots__/index.js.snap) is renamed to `.tsx`, with no change
* [test/index.js](https://github.com/WordPress/gutenberg/blob/430fb9faadb00a5be74e414a7975bec672a3bba4/packages/components/src/color-palette/test/index.js) is renamed to [.tsx](https://github.com/WordPress/gutenberg/blob/293fafe9564f54dfbaa8b101eda29032e92e9e2d/packages/components/src/color-palette/test/index.tsx), with only a [few changes](https://github.com/WordPress/gutenberg/pull/44632/files#diff-0e9f6fa7c3d085a5471e4eac0d4afc563cb68f6487fbeaa208dc0f117077048b)

Maybe I missed some changes, though.

Thanks for your huge amount of patience!

### kienstra on 2022-10-24T19:50:56Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1289522345

Hi @ciampo,
The commits above address your points 😄 

>We will need to provide a generic component description — we can probably go with something like "ColorPalette allows the user to pick a color from a list of pre-defined color entries" — What do you think?

That's a good description, I [committed it verbatim](https://github.com/WordPress/gutenberg/pull/44632/commits/626b25d6babda6f03115903cc759254acb939184). It's not too long, and lets you know what it does.

>For the list of props, let's use the props defined in types.ts (and their descriptions) as the source of truth

Thanks, I [pasted the props from types.ts](https://github.com/WordPress/gutenberg/pull/44632/commits/905c40e29544059746b109aac4d5cb74a2c95f91).


>Finally, we should update [the JSDoc for the default export](https://github.com/WordPress/gutenberg/blob/a79c187b64cec79c2026cf800dcaff7c2393d63b/packages/components/src/color-palette/index.tsx#L360-L383) with whatever description and usage code snippet were used in the README.

Sounds good, I [updated the description](https://github.com/WordPress/gutenberg/pull/44632/commits/eee647c93db0a7faf6777eb1bd54766b415c96ab). The usage example was already the same as in `README.md`.

### kienstra on 2022-10-25T16:16:36Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1290819973

Thanks, good ideas! I'll apply these today.

### kienstra on 2022-10-25T21:21:37Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1291158123

Hi @ciampo,
Thanks a lot for your patience. The commits above apply your suggestions.

### kienstra on 2022-10-25T22:00:16Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1291188533

Thanks, Marco! Just committed your suggestions.

### kienstra on 2022-10-26T15:58:08Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1292266457

Hi Marco, thanks for your great ideas on the types and documentation here! And thanks for your patience.

### ciampo on 2022-10-26T16:11:13Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1292282540

> Hi Marco, thanks for your great ideas on the types and documentation here! And thanks for your patience.

Thank you!

If you enjoyed this type of work, we have more components in need of a TypeScript refactor #35744 

I'm sure that the next refactors will be quicker, as you get used to the migration requirements :)

### kienstra on 2022-10-26T18:10:36Z

URL: https://github.com/WordPress/gutenberg/pull/44632#issuecomment-1292417433

Thanks, Marco! I'll open another PR to convert another component for https://github.com/WordPress/gutenberg/issues/35744.

## Review threads

### Review thread 1 (resolved)

#### kienstra on 2022-10-02T22:19:41Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r985305048
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -212,7 +223,7 @@ export default function ColorPalette( {
 		</DropdownContentWrapper>
 	);
 
-	const colordColor = colord( value );
+	const colordColor = colord( value ?? '' );
```

`value` [can be undefined](https://github.com/WordPress/gutenberg/tree/6ffdde0670c687dee4b0569853ff76ef656961f1/packages/components/src/color-palette#value), which causes this error:

>Argument of type 'string | undefined' is not assignable to parameter of type 'AnyColor | Colord'.
>  Type 'undefined' is not assignable to type 'AnyColor | Colord'.ts(2345)
<img width="845" alt="Screen Shot 2022-10-02 at 4 26 54 PM" src="https://user-images.githubusercontent.com/4063887/193477013-8e49ce50-2b49-4ed2-8c7b-cd13a36778fd.png">

#### ciampo on 2022-10-12T14:01:52Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993507561
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -212,7 +223,7 @@ export default function ColorPalette( {
 		</DropdownContentWrapper>
 	);
 
-	const colordColor = colord( value );
+	const colordColor = colord( value ?? '' );
```

Makes sense for the scope of this PR.

From the recent discoveries while working on #44668 I also realized that `colord` will silently fail while parsing CSS variables and fallback to `#000000`, which means that all the checks / features based on `colord` are not really working for CSS variables. We should investigate this separately (cc @mirka @chad1008 )

#### ciampo on 2022-10-12T17:52:41Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993749218
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -212,7 +223,7 @@ export default function ColorPalette( {
 		</DropdownContentWrapper>
 	);
 
-	const colordColor = colord( value );
+	const colordColor = colord( value ?? '' );
```

Opened #44904 to track this separately

### Review thread 2 (resolved)

#### kienstra on 2022-10-02T22:19:58Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r985305108
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -196,10 +207,10 @@ export default function ColorPalette( {
 	value,
 	__experimentalHasMultipleOrigins = false,
 	__experimentalIsRenderedInSidebar = false,
-} ) {
+}: ColorPaletteProps ) {
 	const clearColor = useCallback( () => onChange( undefined ), [ onChange ] );
 	const showMultiplePalettes =
-		__experimentalHasMultipleOrigins && colors?.length;
+		__experimentalHasMultipleOrigins && !! colors?.length;
```

The type of `colors?.length` is `number`, so this ensures both `__experimentalHasMultipleOrigins` and `!! colors?.length` have the type `boolean`

### Review thread 3 (resolved)

#### kienstra on 2022-10-02T22:20:17Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r985305139
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -148,9 +157,9 @@ export function CustomColorPickerDropdown( {
 }
 
 export const extractColorNameFromCurrentValue = (
-	currentValue,
-	colors = [],
-	showMultiplePalettes = false
+	currentValue?: string,
```

`value` is passed as the `currentValue` argument, and it [can be undefined](https://github.com/WordPress/gutenberg/blob/6ffdde0670c687dee4b0569853ff76ef656961f1/packages/components/src/color-palette/README.md#value)

### Review thread 4 (resolved)

#### kienstra on 2022-10-02T22:39:41Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r985307306
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -287,11 +298,13 @@ export default function ColorPalette( {
 			<Component
 				clearable={ clearable }
 				clearColor={ clearColor }
+				// @ts-ignore Component can be MultiplePalettes or SinglePalette, which have different colors prop types.
```

Maybe I could have avoided this `@ts-ignore`, but I don't know how.

#### ciampo on 2022-10-12T14:03:54Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993510587
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -287,11 +298,13 @@ export default function ColorPalette( {
 			<Component
 				clearable={ clearable }
 				clearColor={ clearColor }
+				// @ts-ignore Component can be MultiplePalettes or SinglePalette, which have different colors prop types.
```

I've left a suggestion on how to avoid it in https://github.com/WordPress/gutenberg/pull/44632#pullrequestreview-1139113416

#### kienstra on 2022-10-12T20:52:04Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993885663
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -287,11 +298,13 @@ export default function ColorPalette( {
 			<Component
 				clearable={ clearable }
 				clearColor={ clearColor }
+				// @ts-ignore Component can be MultiplePalettes or SinglePalette, which have different colors prop types.
```

Thanks, I committed your suggestion in https://github.com/WordPress/gutenberg/pull/44632/commits/4a602dd3812c72f3422b286136a65dfd5d4a2bb3

### Review thread 5 (resolved)

#### ciampo on 2022-10-12T13:39:29Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993475756
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
```

I wonder if we could improve the names for the `Color` and `MultipleColors` type, in a way that makes it more clear that they are related.

Maybe they could be called `ColorObject` and `PaletteObject` ? Thoughts?

#### kienstra on 2022-10-12T20:33:00Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993871501
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
```

Good idea, changed in https://github.com/WordPress/gutenberg/pull/44632/commits/fcd61ee3c78d1dd3f22017a96aa0de509ce7f5f5 to `ColorObject` and `PaletteObject`.

### Review thread 6 (resolved)

#### ciampo on 2022-10-12T13:44:12Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993481594
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions: ( ReactElement | null ) | ( false | ReactElement );
```

We should be able to just use `ReactNode` for this:

```suggestion
	actions?: ReactNode;
```

#### kienstra on 2022-10-12T19:38:30Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993830675
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions: ( ReactElement | null ) | ( false | ReactElement );
```

That's much better, thanks!

Committed in https://github.com/WordPress/gutenberg/pull/44632/commits/c9f5d8a2f85a36641d557aae049ac2dbcb0ab6c0

#### ciampo on 2022-10-18T15:35:56Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r998405065
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions: ( ReactElement | null ) | ( false | ReactElement );
```

Apologies in case it wasn't clear, but my suggestion was also to make `actions` non-required (via the `?` syntax) — could we do that?

#### kienstra on 2022-10-18T15:46:18Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r998417141
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions: ( ReactElement | null ) | ( false | ReactElement );
```

Sorry, your suggestion was pretty clear! I'll commit that now.

#### kienstra on 2022-10-18T15:50:00Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r998421487
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,83 @@
+/**
+ * External dependencies
+ */
+import type { MouseEventHandler, ReactElement } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type Color = {
+	name: string;
+	color: string;
+};
+
+export type MultipleColors = {
+	name: string;
+	colors: Color[];
+}[];
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions: ( ReactElement | null ) | ( false | ReactElement );
```

Just committed in https://github.com/WordPress/gutenberg/pull/44632/commits/a07c43fbc934b2c75504c29fc81fcf1fabcd2664

### Review thread 7 (resolved)

#### ciampo on 2022-10-12T13:51:39Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993492968
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -33,13 +40,14 @@ function SinglePalette( {
 	onChange,
 	value,
 	actions,
-} ) {
+}: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
 		return colors.map( ( { color, name }, index ) => {
 			const colordColor = colord( color );
 			const isSelected = value === color;
 
 			return (
+				// @ts-ignore Required className prop.
```

This TS error can be avoided by tweaking how props are declared in the `CircularOptionPicker` component (as explained in the [migration guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#refactoring-a-component-to-typescript))

<details>

<summary>Click to expand</summary>

```diff
diff --git a/packages/components/src/circular-option-picker/index.js b/packages/components/src/circular-option-picker/index.js
index 6b85a49220..f41813906f 100644
--- a/packages/components/src/circular-option-picker/index.js
+++ b/packages/components/src/circular-option-picker/index.js
@@ -16,13 +16,14 @@ import Button from '../button';
 import Dropdown from '../dropdown';
 import Tooltip from '../tooltip';
 
-function Option( {
-	className,
-	isSelected,
-	selectedIconProps,
-	tooltipText,
-	...additionalProps
-} ) {
+function Option( props ) {
+	const {
+		className,
+		isSelected,
+		selectedIconProps,
+		tooltipText,
+		...additionalProps
+	} = props;
 	const optionButton = (
 		<Button
 			isPressed={ isSelected }
@@ -52,12 +53,8 @@ function Option( {
 	);
 }
 
-function DropdownLinkAction( {
-	buttonProps,
-	className,
-	dropdownProps,
-	linkText,
-} ) {
+function DropdownLinkAction( props ) {
+	const { buttonProps, className, dropdownProps, linkText } = props;
 	return (
 		<Dropdown
 			className={ classnames(
@@ -80,7 +77,8 @@ function DropdownLinkAction( {
 	);
 }
 
-function ButtonAction( { className, children, ...additionalProps } ) {
+function ButtonAction( props ) {
+	const { className, children, ...additionalProps } = props;
 	return (
 		<Button
 			className={ classnames(
@@ -95,12 +93,8 @@ function ButtonAction( { className, children, ...additionalProps } ) {
 	);
 }
 
-export default function CircularOptionPicker( {
-	actions,
-	className,
-	options,
-	children,
-} ) {
+export default function CircularOptionPicker( props ) {
+	const { actions, className, options, children } = props;
 	return (
 		<div
 			className={ classnames(
diff --git a/packages/components/src/color-palette/index.tsx b/packages/components/src/color-palette/index.tsx
index 2330d8f61e..5ec239365f 100644
--- a/packages/components/src/color-palette/index.tsx
+++ b/packages/components/src/color-palette/index.tsx
@@ -88,7 +88,6 @@ function SinglePalette( {
 	}
 
 	return (
-		// @ts-ignore Required children prop.
 		<CircularOptionPicker
 			className={ className }
 			options={ colorOptions }
@@ -286,7 +285,6 @@ export default function ColorPalette( {
 		onChange,
 		value,
 		actions: !! clearable && (
-			// @ts-ignore Required className property.
 			<CircularOptionPicker.ButtonAction onClick={ clearColor }>
 				{ __( 'Clear' ) }
 			</CircularOptionPicker.ButtonAction>
```
</details>

#### kienstra on 2022-10-12T19:34:19Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993827661
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -33,13 +40,14 @@ function SinglePalette( {
 	onChange,
 	value,
 	actions,
-} ) {
+}: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
 		return colors.map( ( { color, name }, index ) => {
 			const colordColor = colord( color );
 			const isSelected = value === color;
 
 			return (
+				// @ts-ignore Required className prop.
```

Great idea! Thanks for the link to the [migration guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#refactoring-a-component-to-typescript), too. I didn't know about that.

Applied in https://github.com/WordPress/gutenberg/pull/44632/commits/54af17c89e963fec22550188d0aa78529d85a2df

### Review thread 8 (resolved)

#### kienstra on 2022-10-12T20:43:41Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993879351
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -198,20 +198,22 @@ const BorderControlDropdown = (
 							/>
 						</HStack>
 					) : undefined }
-					<ColorPalette
-						className={ popoverContentClassName }
-						value={ color }
-						onChange={ onColorChange }
-						{ ...{ colors, disableCustomColors } }
-						__experimentalHasMultipleOrigins={
-							__experimentalHasMultipleOrigins
-						}
-						__experimentalIsRenderedInSidebar={
-							__experimentalIsRenderedInSidebar
-						}
-						clearable={ false }
-						enableAlpha={ enableAlpha }
-					/>
+					{ colors ? (
+						<ColorPalette
+							className={ popoverContentClassName }
+							value={ color }
+							onChange={ onColorChange }
+							{ ...{ colors, disableCustomColors } }
```

There was a TS error from the `colors` prop:

```sh
Types of property 'colors' are incompatible.
Type 'Colors | undefined' is not assignable to type '(Color | MultipleColors)[]'.
Type 'undefined' is not assignable to type '(Color | MultipleColors)[]'.
```

`colors` can be undefined, and `ColorPalette` [doesn't allow](https://github.com/kienstra/gutenberg/blob/6ee6987a2c392b9f154339e3a11d5f9ca0bb22e5/packages/components/src/color-palette/types.ts#L58) an undefined `colors` prop.

So I added a ternary to only render `ColorPalette` if `colors` is truthy.

#### kienstra on 2022-10-12T21:00:11Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r993891763
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -198,20 +198,22 @@ const BorderControlDropdown = (
 							/>
 						</HStack>
 					) : undefined }
-					<ColorPalette
-						className={ popoverContentClassName }
-						value={ color }
-						onChange={ onColorChange }
-						{ ...{ colors, disableCustomColors } }
-						__experimentalHasMultipleOrigins={
-							__experimentalHasMultipleOrigins
-						}
-						__experimentalIsRenderedInSidebar={
-							__experimentalIsRenderedInSidebar
-						}
-						clearable={ false }
-						enableAlpha={ enableAlpha }
-					/>
+					{ colors ? (
+						<ColorPalette
+							className={ popoverContentClassName }
+							value={ color }
+							onChange={ onColorChange }
+							{ ...{ colors, disableCustomColors } }
```

Reverted this

### Review thread 9 (resolved)

#### ciampo on 2022-10-19T10:43:38Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999266701
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -92,10 +100,11 @@ MultipleOrigins.args = {
 	],
 };
 
-export const CSSVariables = ( args ) => {
+export const CSSVariables = ( args: ColorPaletteProps ) => {
 	return (
 		<div
 			style={ {
+				// @ts-ignore
```

We can typecast the styles to avoid this `@ts-ignore` comment:

```diff
diff --git a/packages/components/src/color-palette/stories/index.tsx b/packages/components/src/color-palette/stories/index.tsx
index 51d165652d..63fac4c347 100644
--- a/packages/components/src/color-palette/stories/index.tsx
+++ b/packages/components/src/color-palette/stories/index.tsx
@@ -1,6 +1,7 @@
 /**
  * External dependencies
  */
+import type { CSSProperties } from 'react';
 import type { ComponentMeta, ComponentStory } from '@storybook/react';
 
 /**
@@ -103,12 +104,13 @@ MultipleOrigins.args = {
 export const CSSVariables = ( args: ColorPaletteProps ) => {
 	return (
 		<div
-			style={ {
-				// @ts-ignore
-				'--red': '#f00',
-				'--yellow': '#ff0',
-				'--blue': '#00f',
-			} }
+			style={
+				{
+					'--red': '#f00',
+					'--yellow': '#ff0',
+					'--blue': '#00f',
+				} as CSSProperties
+			}
 		>
 			<Template { ...args } />
 		</div>
```

#### kienstra on 2022-10-19T20:45:13Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999917512
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -92,10 +100,11 @@ MultipleOrigins.args = {
 	],
 };
 
-export const CSSVariables = ( args ) => {
+export const CSSVariables = ( args: ColorPaletteProps ) => {
 	return (
 		<div
 			style={ {
+				// @ts-ignore
```

Great idea, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/ea5b1f3c8d0946022b351e569e825b1049b742ae

### Review thread 10 (resolved)

#### ciampo on 2022-10-19T11:03:39Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999292168
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -6,11 +11,12 @@ import { useState } from '@wordpress/element';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
 import Popover from '../../popover';
 import { Provider as SlotFillProvider } from '../../slot-fill';
+import type { ColorObject, PaletteObject, ColorPaletteProps } from '../types';
 
-const meta = {
+const meta: ComponentMeta< typeof ColorPalette > = {
```

Here's some improvements that we can make to the Storybook examples:

- Add a named export to `ColorPalette` to allow the Storybook docs plugin to extract type info
- remove unnecerssary type info (now that we have TypeScript) from the Storybook `meta` object
- Disable controls for `value` and `onChange`
- Add `onChange` as a Storybook action
- use the `ComponentStory` type for the `CSSVariables` story

```diff
diff --git a/packages/components/src/color-palette/index.tsx b/packages/components/src/color-palette/index.tsx
index a49c45e430..61f32c45b3 100644
--- a/packages/components/src/color-palette/index.tsx
+++ b/packages/components/src/color-palette/index.tsx
@@ -213,7 +213,7 @@ const areColorsMultiplePalette = (
 	);
 };
 
-export default function ColorPalette( {
+export function ColorPalette( {
 	clearable = true,
 	className,
 	colors = [],
@@ -350,3 +350,5 @@ export default function ColorPalette( {
 		</VStack>
 	);
 }
+
+export default ColorPalette;
diff --git a/packages/components/src/color-palette/stories/index.tsx b/packages/components/src/color-palette/stories/index.tsx
index 51d165652d..a68d1b3c88 100644
--- a/packages/components/src/color-palette/stories/index.tsx
+++ b/packages/components/src/color-palette/stories/index.tsx
@@ -14,32 +14,21 @@ import { useState } from '@wordpress/element';
 import ColorPalette from '..';
 import Popover from '../../popover';
 import { Provider as SlotFillProvider } from '../../slot-fill';
-import type { ColorObject, PaletteObject, ColorPaletteProps } from '../types';
+import type { ColorObject, PaletteObject } from '../types';
 
 const meta: ComponentMeta< typeof ColorPalette > = {
 	title: 'Components/ColorPalette',
 	component: ColorPalette,
 	argTypes: {
+		// Removing the control because setting this prop without changing the
+		// format of the `colors` prop can break the component.
 		__experimentalHasMultipleOrigins: {
 			control: {
 				type: null,
 			},
 		},
-		__experimentalIsRenderedInSidebar: {
-			control: {
-				type: 'boolean',
-			},
-		},
-		clearable: {
-			control: {
-				type: 'boolean',
-			},
-		},
-		disableCustomColors: {
-			control: {
-				type: 'boolean',
-			},
-		},
+		onChange: { action: 'onChange', control: { type: null } },
+		value: { control: { type: null } },
 	},
 	parameters: {
 		controls: { expanded: true },
@@ -48,7 +37,10 @@ const meta: ComponentMeta< typeof ColorPalette > = {
 };
 export default meta;
 
-const Template: ComponentStory< typeof ColorPalette > = ( args ) => {
+const Template: ComponentStory< typeof ColorPalette > = ( {
+	onChange,
+	...args
+} ) => {
 	const firstColor =
 		( args.colors as ColorObject[] )[ 0 ].color ||
 		( args.colors as PaletteObject[] )[ 0 ].colors[ 0 ].color;
@@ -56,7 +48,14 @@ const Template: ComponentStory< typeof ColorPalette > = ( args ) => {
 
 	return (
 		<SlotFillProvider>
-			<ColorPalette { ...args } value={ color } onChange={ setColor } />
+			<ColorPalette
+				{ ...args }
+				value={ color }
+				onChange={ ( newColor ) => {
+					setColor( newColor );
+					onChange?.( newColor );
+				} }
+			/>
 			{ /* @ts-ignore Property 'Slot' does not exist on type. */ }
 			<Popover.Slot />
 		</SlotFillProvider>
@@ -100,7 +99,7 @@ MultipleOrigins.args = {
 	],
 };
 
-export const CSSVariables = ( args: ColorPaletteProps ) => {
+export const CSSVariables: ComponentStory< typeof ColorPalette > = ( args ) => {
 	return (
 		<div
 			style={ {
```

#### kienstra on 2022-10-19T21:33:02Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999953056
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -6,11 +11,12 @@ import { useState } from '@wordpress/element';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
 import Popover from '../../popover';
 import { Provider as SlotFillProvider } from '../../slot-fill';
+import type { ColorObject, PaletteObject, ColorPaletteProps } from '../types';
 
-const meta = {
+const meta: ComponentMeta< typeof ColorPalette > = {
```

Good ideas, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/c4202328e9cca48c41fb7d32e13bf9fb3e370771

### Review thread 11 (resolved)

#### ciampo on 2022-10-19T11:07:53Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999295847
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,85 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
+	popoverProps?: string[];
+	renderToggle: ( props: {
+		isOpen: boolean;
+		onToggle: MouseEventHandler< HTMLButtonElement >;
+	} ) => JSX.Element;
+};
+
+export type ColorPaletteProps = {
+	/**
+	 * Whether the palette should have a clearing button.
+	 */
+	clearable?: boolean;
```

Looking at the [component's props destructuring](https://github.com/WordPress/gutenberg/blob/6ee6987a2c392b9f154339e3a11d5f9ca0bb22e5/packages/components/src/color-palette/index.tsx#L217-L225), a few props are missing their `@default` type annotation

```diff
diff --git a/packages/components/src/color-palette/types.ts b/packages/components/src/color-palette/types.ts
index 1c8f6ec1a8..a940ff09a0 100644
--- a/packages/components/src/color-palette/types.ts
+++ b/packages/components/src/color-palette/types.ts
@@ -44,6 +44,8 @@ export type CustomColorPickerDropdownProps = {
 export type ColorPaletteProps = {
 	/**
 	 * Whether the palette should have a clearing button.
+	 *
+	 * @default true
 	 */
 	clearable?: boolean;
 	/**
@@ -58,6 +60,8 @@ export type ColorPaletteProps = {
 	colors?: ( PaletteObject | ColorObject )[];
 	/**
 	 * Whether to allow custom color or not.
+	 *
+	 * @default false
 	 */
 	disableCustomColors?: boolean;
 	/**
@@ -76,10 +80,14 @@ export type ColorPaletteProps = {
 	/**
 	 * Whether the colors prop is an array of color palettes,
 	 * rather than an array of color objects.
+	 *
+	 * @default false
 	 */
 	__experimentalHasMultipleOrigins?: boolean;
 	/**
 	 * Whether this is rendered in the sidebar.
+	 *
+	 * @default false
 	 */
 	__experimentalIsRenderedInSidebar?: boolean;
 };
```

#### kienstra on 2022-10-19T20:45:45Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999917935
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,85 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
+	popoverProps?: string[];
+	renderToggle: ( props: {
+		isOpen: boolean;
+		onToggle: MouseEventHandler< HTMLButtonElement >;
+	} ) => JSX.Element;
+};
+
+export type ColorPaletteProps = {
+	/**
+	 * Whether the palette should have a clearing button.
+	 */
+	clearable?: boolean;
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/9b770d3b07dd216aafb9c566deadd15ea5842794

### Review thread 12 (resolved)

#### ciampo on 2022-10-19T11:24:23Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999310616
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -179,28 +198,58 @@ export const extractColorNameFromCurrentValue = (
 	return __( 'Custom' );
 };
 
-export const showTransparentBackground = ( currentValue ) => {
+export const showTransparentBackground = ( currentValue?: string ) => {
 	if ( typeof currentValue === 'undefined' ) {
 		return true;
 	}
 	return colord( currentValue ).alpha() === 0;
 };
 
+const areColorsMultiplePalette = (
+	colors: NonNullable< ColorPaletteProps[ 'colors' ] >
+): colors is PaletteObject[] => {
+	return colors.every( ( colorObj ) =>
+		Array.isArray( ( colorObj as PaletteObject ).colors )
+	);
+};
+
 export default function ColorPalette( {
 	clearable = true,
 	className,
-	colors,
+	colors = [],
 	disableCustomColors = false,
 	enableAlpha,
 	onChange,
 	value,
 	__experimentalHasMultipleOrigins = false,
 	__experimentalIsRenderedInSidebar = false,
-} ) {
+}: ColorPaletteProps ) {
```

Let's use the [`WordPressComponentProps` utility type](https://github.com/WordPress/gutenberg/blob/3c679aa7591f64e62305daeb8235f60bdcacca25/packages/components/src/ui/context/wordpress-component.ts), and refactor the component accordingly:

- the diff includes some duplicate changes to add a named export, as suggested in a previous comment from this round of review
- The `WordPressComponentProps` type adds support for polymorphism (ie. the `as` prop)
- The `WordPressComponentProps` type also includes types for all HTML attributes, including `className` — which therefore we can remove from the `ColorPaletteProps`
- We can forward all remaining `...otherProps` to the outer component (`VStack`, in this case)
- We can also add a forwarded ref in the process


```diff
diff --git a/packages/components/src/color-palette/index.tsx b/packages/components/src/color-palette/index.tsx
index a49c45e430..c777212759 100644
--- a/packages/components/src/color-palette/index.tsx
+++ b/packages/components/src/color-palette/index.tsx
@@ -1,6 +1,7 @@
 /**
  * External dependencies
  */
+import type { ForwardedRef } from 'react';
 import { colord, extend } from 'colord';
 import namesPlugin from 'colord/plugins/names';
 import a11yPlugin from 'colord/plugins/a11y';
@@ -9,7 +10,7 @@ import a11yPlugin from 'colord/plugins/a11y';
  * WordPress dependencies
  */
 import { __, sprintf } from '@wordpress/i18n';
-import { useCallback, useMemo } from '@wordpress/element';
+import { useCallback, useMemo, forwardRef } from '@wordpress/element';
 
 /**
  * Internal dependencies
@@ -30,6 +31,7 @@ import type {
 	PaletteObject,
 	SinglePaletteProps,
 } from './types';
+import type { WordPressComponentProps } from '../ui/context';
 
 extend( [ namesPlugin, a11yPlugin ] );
 
@@ -213,17 +215,21 @@ const areColorsMultiplePalette = (
 	);
 };
 
-export default function ColorPalette( {
-	clearable = true,
-	className,
-	colors = [],
-	disableCustomColors = false,
-	enableAlpha,
-	onChange,
-	value,
-	__experimentalHasMultipleOrigins = false,
-	__experimentalIsRenderedInSidebar = false,
-}: ColorPaletteProps ) {
+function UnforwardedColorPalette(
+	props: WordPressComponentProps< ColorPaletteProps, 'div' >,
+	forwardedRef: ForwardedRef< any >
+) {
+	const {
+		clearable = true,
+		colors = [],
+		disableCustomColors = false,
+		enableAlpha,
+		onChange,
+		value,
+		__experimentalHasMultipleOrigins = false,
+		__experimentalIsRenderedInSidebar = false,
+		...otherProps
+	} = props;
 	const clearColor = useCallback( () => onChange( undefined ), [ onChange ] );
 
 	const buttonLabelName = useMemo(
@@ -291,7 +297,7 @@ export default function ColorPalette( {
 	};
 
 	return (
-		<VStack spacing={ 3 } className={ className }>
+		<VStack spacing={ 3 } ref={ forwardedRef } { ...otherProps }>
 			{ ! disableCustomColors && (
 				<CustomColorPickerDropdown
 					isRenderedInSidebar={ __experimentalIsRenderedInSidebar }
@@ -350,3 +356,16 @@ export default function ColorPalette( {
 		</VStack>
 	);
 }
+
+/**
+ * TBD Needs a description of the component (which should be taken from the README )
+ *
+ * ```jsx
+ * import { ColorPalette } from '@wordpress/components';
+ *
+ * TBD code example
+ * ```
+ */
+export const ColorPalette = forwardRef( UnforwardedColorPalette );
+
+export default ColorPalette;
diff --git a/packages/components/src/color-palette/types.ts b/packages/components/src/color-palette/types.ts
index 1c8f6ec1a8..91e598faf1 100644
--- a/packages/components/src/color-palette/types.ts
+++ b/packages/components/src/color-palette/types.ts
@@ -46,10 +46,6 @@ export type ColorPaletteProps = {
 	 * Whether the palette should have a clearing button.
 	 */
 	clearable?: boolean;
-	/**
-	 * Classes to be applied to the container.
-	 */
-	className?: string;
 	/**
 	 * Array with the colors to be shown.
 	 *
```

Let me know if you have any questions on `WordPressComponentProps`

#### kienstra on 2022-10-19T21:09:58Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999937000
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -179,28 +198,58 @@ export const extractColorNameFromCurrentValue = (
 	return __( 'Custom' );
 };
 
-export const showTransparentBackground = ( currentValue ) => {
+export const showTransparentBackground = ( currentValue?: string ) => {
 	if ( typeof currentValue === 'undefined' ) {
 		return true;
 	}
 	return colord( currentValue ).alpha() === 0;
 };
 
+const areColorsMultiplePalette = (
+	colors: NonNullable< ColorPaletteProps[ 'colors' ] >
+): colors is PaletteObject[] => {
+	return colors.every( ( colorObj ) =>
+		Array.isArray( ( colorObj as PaletteObject ).colors )
+	);
+};
+
 export default function ColorPalette( {
 	clearable = true,
 	className,
-	colors,
+	colors = [],
 	disableCustomColors = false,
 	enableAlpha,
 	onChange,
 	value,
 	__experimentalHasMultipleOrigins = false,
 	__experimentalIsRenderedInSidebar = false,
-} ) {
+}: ColorPaletteProps ) {
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/a79c187b64cec79c2026cf800dcaff7c2393d63b

### Review thread 13 (resolved)

#### kienstra on 2022-10-19T21:12:05Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999938492
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -284,22 +342,45 @@ export default function ColorPalette( {
 					) }
 				/>
 			) }
-			<Component
-				clearable={ clearable }
-				clearColor={ clearColor }
-				colors={ colors }
-				onChange={ onChange }
-				value={ value }
-				actions={
-					!! clearable && (
-						<CircularOptionPicker.ButtonAction
-							onClick={ clearColor }
-						>
-							{ __( 'Clear' ) }
-						</CircularOptionPicker.ButtonAction>
-					)
-				}
-			/>
+			{ __experimentalHasMultipleOrigins ? (
+				<MultiplePalettes
+					{ ...paletteCommonProps }
+					colors={ colors as PaletteObject[] }
+				/>
+			) : (
+				<SinglePalette
+					{ ...paletteCommonProps }
+					colors={ colors as ColorObject[] }
+				/>
+			) }
 		</VStack>
 	);
 }
+
+/**
+ * Allows selecting from given colors.
```

Maybe this JSDoc block isn't what you [had in mind](https://github.com/WordPress/gutenberg/pull/44632#discussion_r999310616). 

I didn't see a description in [README.md](https://github.com/WordPress/gutenberg/blob/5a82cf5723b0feb0de9ec23e267f6b190d2a5ad0/packages/components/src/color-palette/README.md), so I added this, which might be completely off 😄

#### ciampo on 2022-10-20T14:14:27Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1000691824
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -284,22 +342,45 @@ export default function ColorPalette( {
 					) }
 				/>
 			) }
-			<Component
-				clearable={ clearable }
-				clearColor={ clearColor }
-				colors={ colors }
-				onChange={ onChange }
-				value={ value }
-				actions={
-					!! clearable && (
-						<CircularOptionPicker.ButtonAction
-							onClick={ clearColor }
-						>
-							{ __( 'Clear' ) }
-						</CircularOptionPicker.ButtonAction>
-					)
-				}
-			/>
+			{ __experimentalHasMultipleOrigins ? (
+				<MultiplePalettes
+					{ ...paletteCommonProps }
+					colors={ colors as PaletteObject[] }
+				/>
+			) : (
+				<SinglePalette
+					{ ...paletteCommonProps }
+					colors={ colors as ColorObject[] }
+				/>
+			) }
 		</VStack>
 	);
 }
+
+/**
+ * Allows selecting from given colors.
```

It looks good for now, we can always refine it before merging — thank you!

#### kienstra on 2022-10-20T16:40:41Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1000874386
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -284,22 +342,45 @@ export default function ColorPalette( {
 					) }
 				/>
 			) }
-			<Component
-				clearable={ clearable }
-				clearColor={ clearColor }
-				colors={ colors }
-				onChange={ onChange }
-				value={ value }
-				actions={
-					!! clearable && (
-						<CircularOptionPicker.ButtonAction
-							onClick={ clearColor }
-						>
-							{ __( 'Clear' ) }
-						</CircularOptionPicker.ButtonAction>
-					)
-				}
-			/>
+			{ __experimentalHasMultipleOrigins ? (
+				<MultiplePalettes
+					{ ...paletteCommonProps }
+					colors={ colors as PaletteObject[] }
+				/>
+			) : (
+				<SinglePalette
+					{ ...paletteCommonProps }
+					colors={ colors as ColorObject[] }
+				/>
+			) }
 		</VStack>
 	);
 }
+
+/**
+ * Allows selecting from given colors.
```

Thanks, Marco!

### Review thread 14 (resolved)

#### kienstra on 2022-10-19T21:13:13Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r999939318
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -284,22 +342,45 @@ export default function ColorPalette( {
 					) }
 				/>
 			) }
-			<Component
-				clearable={ clearable }
-				clearColor={ clearColor }
-				colors={ colors }
-				onChange={ onChange }
-				value={ value }
-				actions={
-					!! clearable && (
-						<CircularOptionPicker.ButtonAction
-							onClick={ clearColor }
-						>
-							{ __( 'Clear' ) }
-						</CircularOptionPicker.ButtonAction>
-					)
-				}
-			/>
+			{ __experimentalHasMultipleOrigins ? (
+				<MultiplePalettes
+					{ ...paletteCommonProps }
+					colors={ colors as PaletteObject[] }
+				/>
+			) : (
+				<SinglePalette
+					{ ...paletteCommonProps }
+					colors={ colors as ColorObject[] }
+				/>
+			) }
 		</VStack>
 	);
 }
+
+/**
+ * Allows selecting from given colors.
+ *
+ * ```jsx
+ * import { ColorPalette } from '@wordpress/components';
```

Copy-pasted verbatim from [README.md](https://github.com/WordPress/gutenberg/blob/5a82cf5723b0feb0de9ec23e267f6b190d2a5ad0/packages/components/src/color-palette/README.md#usage).

Maybe that's not what you were thinking, or maybe we could use more examples.

#### ciampo on 2022-10-20T14:14:52Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1000692369
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -284,22 +342,45 @@ export default function ColorPalette( {
 					) }
 				/>
 			) }
-			<Component
-				clearable={ clearable }
-				clearColor={ clearColor }
-				colors={ colors }
-				onChange={ onChange }
-				value={ value }
-				actions={
-					!! clearable && (
-						<CircularOptionPicker.ButtonAction
-							onClick={ clearColor }
-						>
-							{ __( 'Clear' ) }
-						</CircularOptionPicker.ButtonAction>
-					)
-				}
-			/>
+			{ __experimentalHasMultipleOrigins ? (
+				<MultiplePalettes
+					{ ...paletteCommonProps }
+					colors={ colors as PaletteObject[] }
+				/>
+			) : (
+				<SinglePalette
+					{ ...paletteCommonProps }
+					colors={ colors as ColorObject[] }
+				/>
+			) }
 		</VStack>
 	);
 }
+
+/**
+ * Allows selecting from given colors.
+ *
+ * ```jsx
+ * import { ColorPalette } from '@wordpress/components';
```

Same as above — it's definitely good enough for now, we can always polish it later

#### kienstra on 2022-10-20T16:40:34Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1000874249
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -284,22 +342,45 @@ export default function ColorPalette( {
 					) }
 				/>
 			) }
-			<Component
-				clearable={ clearable }
-				clearColor={ clearColor }
-				colors={ colors }
-				onChange={ onChange }
-				value={ value }
-				actions={
-					!! clearable && (
-						<CircularOptionPicker.ButtonAction
-							onClick={ clearColor }
-						>
-							{ __( 'Clear' ) }
-						</CircularOptionPicker.ButtonAction>
-					)
-				}
-			/>
+			{ __experimentalHasMultipleOrigins ? (
+				<MultiplePalettes
+					{ ...paletteCommonProps }
+					colors={ colors as PaletteObject[] }
+				/>
+			) : (
+				<SinglePalette
+					{ ...paletteCommonProps }
+					colors={ colors as ColorObject[] }
+				/>
+			) }
 		</VStack>
 	);
 }
+
+/**
+ * Allows selecting from given colors.
+ *
+ * ```jsx
+ * import { ColorPalette } from '@wordpress/components';
```

Thanks!

### Review thread 15 (resolved)

#### ciampo on 2022-10-21T12:44:46Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1001743986
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -6,34 +12,24 @@ import { useState } from '@wordpress/element';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
 import Popover from '../../popover';
 import { Provider as SlotFillProvider } from '../../slot-fill';
+import type { ColorObject, PaletteObject } from '../types';
 
-const meta = {
+const meta: ComponentMeta< typeof ColorPalette > = {
 	title: 'Components/ColorPalette',
 	component: ColorPalette,
 	argTypes: {
+		// Removing the control because setting this prop without changing the
+		// format of the `colors` prop can break the component.
 		__experimentalHasMultipleOrigins: {
 			control: {
 				type: null,
 			},
 		},
-		__experimentalIsRenderedInSidebar: {
-			control: {
-				type: 'boolean',
-			},
-		},
-		clearable: {
-			control: {
-				type: 'boolean',
-			},
-		},
-		disableCustomColors: {
-			control: {
-				type: 'boolean',
-			},
-		},
+		onChange: { action: 'onChange', control: { type: null } },
```

Let's also disable controls for the `as` prop:
```suggestion
		as: { control: { type: null } },
		onChange: { action: 'onChange', control: { type: null } },
```

#### kienstra on 2022-10-21T16:20:55Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1001974578
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -6,34 +12,24 @@ import { useState } from '@wordpress/element';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
 import Popover from '../../popover';
 import { Provider as SlotFillProvider } from '../../slot-fill';
+import type { ColorObject, PaletteObject } from '../types';
 
-const meta = {
+const meta: ComponentMeta< typeof ColorPalette > = {
 	title: 'Components/ColorPalette',
 	component: ColorPalette,
 	argTypes: {
+		// Removing the control because setting this prop without changing the
+		// format of the `colors` prop can break the component.
 		__experimentalHasMultipleOrigins: {
 			control: {
 				type: null,
 			},
 		},
-		__experimentalIsRenderedInSidebar: {
-			control: {
-				type: 'boolean',
-			},
-		},
-		clearable: {
-			control: {
-				type: 'boolean',
-			},
-		},
-		disableCustomColors: {
-			control: {
-				type: 'boolean',
-			},
-		},
+		onChange: { action: 'onChange', control: { type: null } },
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/8001f145d7633d013b27d369e80d83bdc89b2864

### Review thread 16 (resolved)

#### ciampo on 2022-10-21T12:50:49Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1001749490
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,89 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
+	popoverProps?: string[];
+	renderToggle: ( props: {
+		isOpen: boolean;
+		onToggle: MouseEventHandler< HTMLButtonElement >;
+	} ) => JSX.Element;
+};
+
+export type ColorPaletteProps = {
+	/**
+	 * Whether the palette should have a clearing button.
+	 *
+	 * @default true
+	 */
+	clearable?: boolean;
+	/**
+	 * Array with the colors to be shown.
+	 *
+	 * @default []
+	 */
+	colors?: ( PaletteObject | ColorObject )[];
+	/**
+	 * Whether to allow custom color or not.
```

Let's clarify this prop description

```suggestion
	 * Whether to allow the user to pick a custom color on top of the predefined
	 * choices (defined via the `colors` prop).
```

#### kienstra on 2022-10-21T16:21:13Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1001974820
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,89 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
+	popoverProps?: string[];
+	renderToggle: ( props: {
+		isOpen: boolean;
+		onToggle: MouseEventHandler< HTMLButtonElement >;
+	} ) => JSX.Element;
+};
+
+export type ColorPaletteProps = {
+	/**
+	 * Whether the palette should have a clearing button.
+	 *
+	 * @default true
+	 */
+	clearable?: boolean;
+	/**
+	 * Array with the colors to be shown.
+	 *
+	 * @default []
+	 */
+	colors?: ( PaletteObject | ColorObject )[];
+	/**
+	 * Whether to allow custom color or not.
```

Committed in https://github.com/WordPress/gutenberg/pull/44632/commits/e793921d09c7bafdbf90d170efcfdf28630990bc

### Review thread 17 (resolved)

#### ciampo on 2022-10-24T13:14:49Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1003299549
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,90 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+export type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
+	popoverProps?: string[];
+	renderToggle: ( props: {
+		isOpen: boolean;
+		onToggle: MouseEventHandler< HTMLButtonElement >;
+	} ) => JSX.Element;
+};
+
+export type ColorPaletteProps = {
+	/**
+	 * Whether the palette should have a clearing button.
+	 *
+	 * @default true
+	 */
+	clearable?: boolean;
+	/**
+	 * Array with the colors to be shown.
```

```suggestion
	 * Array with the colors to be shown. When displaying multiple color palettes
	 * to choose from, the format of the array changes from an array of colors
	 * objects, to an array of color palettes.
```

### Review thread 18 (resolved)

#### ciampo on 2022-10-24T16:33:24Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1003524148
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -7,16 +7,17 @@ import userEvent from '@testing-library/user-event';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
+import type { ColorObject, OnColorChange } from '../types';
 
 describe( 'ColorPalette', () => {
-	const colors = [
+	const colors: ColorObject[] = [
 		{ name: 'red', color: '#f00' },
 		{ name: 'white', color: '#fff' },
 		{ name: 'blue', color: '#00f' },
 	];
 	const currentColor = '#f00';
-	const onChange = jest.fn();
+	const onChange: jest.Mock< OnColorChange > = jest.fn();
```

There shouldn't be a need for those changes — have you tried not adding those extra types?

If we can avoid the types, then I'd argue that we also don't need to export the `OnColorChange` type from `types.ts` either.

#### kienstra on 2022-10-24T16:45:45Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1003536572
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -7,16 +7,17 @@ import userEvent from '@testing-library/user-event';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
+import type { ColorObject, OnColorChange } from '../types';
 
 describe( 'ColorPalette', () => {
-	const colors = [
+	const colors: ColorObject[] = [
 		{ name: 'red', color: '#f00' },
 		{ name: 'white', color: '#fff' },
 		{ name: 'blue', color: '#00f' },
 	];
 	const currentColor = '#f00';
-	const onChange = jest.fn();
+	const onChange: jest.Mock< OnColorChange > = jest.fn();
```

Good idea, I don't think those are needed either. I'll test it and revert them.

#### kienstra on 2022-10-24T18:46:20Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1003639948
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -7,16 +7,17 @@ import userEvent from '@testing-library/user-event';
 /**
  * Internal dependencies
  */
-import ColorPalette from '../';
+import ColorPalette from '..';
+import type { ColorObject, OnColorChange } from '../types';
 
 describe( 'ColorPalette', () => {
-	const colors = [
+	const colors: ColorObject[] = [
 		{ name: 'red', color: '#f00' },
 		{ name: 'white', color: '#fff' },
 		{ name: 'blue', color: '#00f' },
 	];
 	const currentColor = '#f00';
-	const onChange = jest.fn();
+	const onChange: jest.Mock< OnColorChange > = jest.fn();
```

Reverted in https://github.com/WordPress/gutenberg/pull/44632/commits/d77ca5b7f913e10dcc9e0607382ce0681dfe2666

### Review thread 19

#### ciampo on 2022-10-25T09:29:47Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004234482
Path: packages/components/src/color-palette/README.md

```diff
@@ -44,12 +54,28 @@ classes to be applied to the container.
 
 ### clearable
 
-Whether the palette should have a clearing button or not.
+Whether the palette should have a clearing button.
 
 -   Type: `Boolean`
 -   Required: No
 -   Default: true
 
+### __experimentalHasMultipleOrigins
+
+Whether the colors prop is an array of color palettes, rather than an array of color objects.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
+### __experimentalIsRenderedInSidebar
+
+Whether this is rendered in the sidebar.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
 ## Usage
```

As suggested in the [guidelines](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#documentation), let's move the `Usage` section at the top of the document

```diff
diff --git a/packages/components/src/color-palette/README.md b/packages/components/src/color-palette/README.md
index 5d3ed4601c..49612e3f91 100644
--- a/packages/components/src/color-palette/README.md
+++ b/packages/components/src/color-palette/README.md
@@ -2,6 +2,36 @@
 
 `ColorPalette` allows the user to pick a color from a list of pre-defined color entries.
 
+## Usage
+
+```jsx
+import { ColorPalette } from '@wordpress/components';
+import { useState } from '@wordpress/element';
+
+const MyColorPalette = () => {
+	const [ color, setColor ] = useState ( '#f00' )
+	const colors = [
+		{ name: 'red', color: '#f00' },
+		{ name: 'white', color: '#fff' },
+		{ name: 'blue', color: '#00f' },
+	];
+
+	return (
+		<ColorPalette
+			colors={ colors }
+			value={ color }
+			onChange={ ( color ) => setColor( color ) }
+		/>
+	);
+} );
+```
+
+If you're using this component outside the editor, you can
+[ensure `Tooltip` positioning](/packages/components/README.md#popovers-and-tooltips)
+for the `ColorPalette`'s color swatches, by rendering your `ColorPalette` with a
+`Popover.Slot` further up the element tree and within a
+`SlotFillProvider` overall.
+
 ## Props
 
 The component accepts the following props.
@@ -75,33 +105,3 @@ Whether this is rendered in the sidebar.
 -   Type: `Boolean`
 -   Required: No
 -   Default: false
-
-## Usage
-
-```jsx
-import { ColorPalette } from '@wordpress/components';
-import { useState } from '@wordpress/element';
-
-const MyColorPalette = () => {
-	const [ color, setColor ] = useState ( '#f00' )
-	const colors = [
-		{ name: 'red', color: '#f00' },
-		{ name: 'white', color: '#fff' },
-		{ name: 'blue', color: '#00f' },
-	];
-
-	return (
-		<ColorPalette
-			colors={ colors }
-			value={ color }
-			onChange={ ( color ) => setColor( color ) }
-		/>
-	);
-} );
-```
-
-If you're using this component outside the editor, you can
-[ensure `Tooltip` positioning](/packages/components/README.md#popovers-and-tooltips)
-for the `ColorPalette`'s color swatches, by rendering your `ColorPalette` with a
-`Popover.Slot` further up the element tree and within a
-`SlotFillProvider` overall.
```

#### kienstra on 2022-10-25T20:59:56Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004960009
Path: packages/components/src/color-palette/README.md

```diff
@@ -44,12 +54,28 @@ classes to be applied to the container.
 
 ### clearable
 
-Whether the palette should have a clearing button or not.
+Whether the palette should have a clearing button.
 
 -   Type: `Boolean`
 -   Required: No
 -   Default: true
 
+### __experimentalHasMultipleOrigins
+
+Whether the colors prop is an array of color palettes, rather than an array of color objects.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
+### __experimentalIsRenderedInSidebar
+
+Whether this is rendered in the sidebar.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
 ## Usage
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/4fd5d9c8b1fc2af1e70ce66c02506ed426463da3

### Review thread 20 (resolved)

#### ciampo on 2022-10-25T09:29:58Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004234684
Path: packages/components/src/color-palette/README.md

```diff
@@ -1,21 +1,31 @@
 # ColorPalette
 
+ColorPalette allows the user to pick a color from a list of pre-defined color entries.
```

```suggestion
`ColorPalette` allows the user to pick a color from a list of pre-defined color entries.
```

#### kienstra on 2022-10-25T21:00:50Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004960700
Path: packages/components/src/color-palette/README.md

```diff
@@ -1,21 +1,31 @@
 # ColorPalette
 
+ColorPalette allows the user to pick a color from a list of pre-defined color entries.
```

Thanks, committed in https://github.com/WordPress/gutenberg/pull/44632/commits/d65a832ab8b85ab4f993eb3c2ad3b4c098ad5ead

### Review thread 21

#### ciampo on 2022-10-25T09:31:02Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004235864
Path: packages/components/src/color-palette/README.md

```diff
@@ -1,21 +1,31 @@
 # ColorPalette
 
+ColorPalette allows the user to pick a color from a list of pre-defined color entries.
+
 ## Props
 
 The component accepts the following props.
 
-{ colors, disableCustomColors = false, value, onChange, className, clearable = true }
+{ colors = [],  disableCustomColors = false, enableAlpha, value, onchange, className, clearable = true, __experimentalHasMultipleOrigins = false, __experimentalIsRenderedInSidebar = false }
```

```diff
diff --git a/packages/components/src/color-palette/README.md b/packages/components/src/color-palette/README.md
index 49612e3f91..b194f3cef0 100644
--- a/packages/components/src/color-palette/README.md
+++ b/packages/components/src/color-palette/README.md
@@ -36,8 +36,6 @@ for the `ColorPalette`'s color swatches, by rendering your `ColorPalette` with a
 
 The component accepts the following props.
 
-{ colors = [],  disableCustomColors = false, enableAlpha, value, onchange, className, clearable = true, __experimentalHasMultipleOrigins = false, __experimentalIsRenderedInSidebar = false }
-
 ### colors
 Array with the colors to be shown. When displaying multiple color palettes to choose from, the format of the array changes from an array of colors objects, to an array of color palettes.
```

#### kienstra on 2022-10-25T21:02:04Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004961578
Path: packages/components/src/color-palette/README.md

```diff
@@ -1,21 +1,31 @@
 # ColorPalette
 
+ColorPalette allows the user to pick a color from a list of pre-defined color entries.
+
 ## Props
 
 The component accepts the following props.
 
-{ colors, disableCustomColors = false, value, onChange, className, clearable = true }
+{ colors = [],  disableCustomColors = false, enableAlpha, value, onchange, className, clearable = true, __experimentalHasMultipleOrigins = false, __experimentalIsRenderedInSidebar = false }
```

Committed in https://github.com/WordPress/gutenberg/pull/44632/commits/29984c0cd55611a12d80b6e2d2cedb5debb2b8bc

### Review thread 22

#### ciampo on 2022-10-25T09:33:19Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004238227
Path: packages/components/src/color-palette/README.md

```diff
@@ -1,21 +1,31 @@
 # ColorPalette
 
+ColorPalette allows the user to pick a color from a list of pre-defined color entries.
+
 ## Props
 
 The component accepts the following props.
 
-{ colors, disableCustomColors = false, value, onChange, className, clearable = true }
+{ colors = [],  disableCustomColors = false, enableAlpha, value, onchange, className, clearable = true, __experimentalHasMultipleOrigins = false, __experimentalIsRenderedInSidebar = false }
 
 ### colors
-
-Array with the colors to be shown.
+Array with the colors to be shown. When displaying multiple color palettes to choose from, the format of the array changes from an array of colors objects, to an array of color palettes.
 
 -   Type: `Array`
--   Required: Yes
+-   Required: No
+-   Default: `[]`
```

A bit nitty, but let's try to follow the format highlighted in the [guidelines](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#readme-example):

```markdown
### `propName`: Typescript style type i.e `string`, `number`, `( nextValue: string ) => void`

Prop description. With a new line before and after the description and before and after type/required blocks.

-   Required: Either `Yes` or `No`
<!-- If the prop has a default value, add the following line: -->
-   Default: [default value]
```

For example, for the `color` prop the docs would change to

```markdown
### `colors`: `( PaletteObject | ColorObject )[]`

Array with the colors to be shown. When displaying multiple color palettes to choose from, the format of the array changes from an array of colors objects, to an array of color palettes.

-   Required: No
-   Default: `[]`
```

#### kienstra on 2022-10-25T21:11:05Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004968385
Path: packages/components/src/color-palette/README.md

```diff
@@ -1,21 +1,31 @@
 # ColorPalette
 
+ColorPalette allows the user to pick a color from a list of pre-defined color entries.
+
 ## Props
 
 The component accepts the following props.
 
-{ colors, disableCustomColors = false, value, onChange, className, clearable = true }
+{ colors = [],  disableCustomColors = false, enableAlpha, value, onchange, className, clearable = true, __experimentalHasMultipleOrigins = false, __experimentalIsRenderedInSidebar = false }
 
 ### colors
-
-Array with the colors to be shown.
+Array with the colors to be shown. When displaying multiple color palettes to choose from, the format of the array changes from an array of colors objects, to an array of color palettes.
 
 -   Type: `Array`
--   Required: Yes
+-   Required: No
+-   Default: `[]`
```

Oh, good idea. Sorry for missing that in CONTRIBUTING.md. 

Committed in https://github.com/WordPress/gutenberg/pull/44632/commits/988e9401da364236c2dfe0296fe4cd234029ae4b

### Review thread 23

#### ciampo on 2022-10-25T09:35:54Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004241062
Path: packages/components/src/color-palette/README.md

```diff
@@ -44,12 +54,28 @@ classes to be applied to the container.
 
 ### clearable
 
-Whether the palette should have a clearing button or not.
+Whether the palette should have a clearing button.
 
 -   Type: `Boolean`
 -   Required: No
 -   Default: true
 
+### __experimentalHasMultipleOrigins
+
+Whether the colors prop is an array of color palettes, rather than an array of color objects.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
+### __experimentalIsRenderedInSidebar
+
+Whether this is rendered in the sidebar.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
```

Let's actually not list experimental props in the README — apologies if I forgot to mention that earlier!

#### kienstra on 2022-10-25T20:59:35Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004959736
Path: packages/components/src/color-palette/README.md

```diff
@@ -44,12 +54,28 @@ classes to be applied to the container.
 
 ### clearable
 
-Whether the palette should have a clearing button or not.
+Whether the palette should have a clearing button.
 
 -   Type: `Boolean`
 -   Required: No
 -   Default: true
 
+### __experimentalHasMultipleOrigins
+
+Whether the colors prop is an array of color palettes, rather than an array of color objects.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
+### __experimentalIsRenderedInSidebar
+
+Whether this is rendered in the sidebar.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: false
+
```

Good idea, removed in https://github.com/WordPress/gutenberg/pull/44632/commits/4fd5d9c8b1fc2af1e70ce66c02506ed426463da3

### Review thread 24 (resolved)

#### ciampo on 2022-10-25T21:31:32Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004982302
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -42,14 +39,26 @@ const meta = {
 };
 export default meta;
 
-const Template = ( args ) => {
+const Template: ComponentStory< typeof ColorPalette > = ( {
+	onChange,
+	...args
+} ) => {
 	const firstColor =
-		args.colors[ 0 ].color || args.colors[ 0 ].colors[ 0 ].color;
-	const [ color, setColor ] = useState( firstColor );
+		( args.colors as ColorObject[] )[ 0 ].color ||
+		( args.colors as PaletteObject[] )[ 0 ].colors[ 0 ].color;
+	const [ color, setColor ] = useState< string | undefined >( firstColor );
 
 	return (
 		<SlotFillProvider>
-			<ColorPalette { ...args } value={ color } onChange={ setColor } />
+			<ColorPalette
+				{ ...args }
+				value={ color }
+				onChange={ ( newColor ) => {
+					setColor( newColor );
+					onChange?.( newColor );
+				} }
+			/>
+			{ /* @ts-ignore Property 'Slot' does not exist on type. */ }
```

Let's use `@ts-expect-error` instead and add a more specific reason

```suggestion
			{ /* @ts-expect-error The 'Slot' component hasn't been typed yet. */ }
```

### Review thread 25 (resolved)

#### ciampo on 2022-10-25T21:42:13Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004989309
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,92 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
```

```suggestion
// TODO: should extend `Dropdown`'s props once it gets refactored to TypeScript
export type CustomColorPickerDropdownProps = {
```

### Review thread 26 (resolved)

#### ciampo on 2022-10-25T21:43:42Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004990230
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,92 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
```

Let's use `ReactNode` as the return type

```suggestion
	renderContent: () => ReactNode;
```

### Review thread 27 (resolved)

#### ciampo on 2022-10-25T21:43:56Z

URL: https://github.com/WordPress/gutenberg/pull/44632#discussion_r1004990381
Path: packages/components/src/color-palette/types.ts

```diff
@@ -0,0 +1,92 @@
+/**
+ * External dependencies
+ */
+import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';
+
+type OnColorChange = ( newColor?: string ) => void;
+
+export type ColorObject = {
+	name: string;
+	color: NonNullable< CSSProperties[ 'color' ] >;
+};
+
+export type PaletteObject = {
+	name: string;
+	colors: ColorObject[];
+};
+
+type PaletteProps = {
+	className?: string;
+	clearColor: () => void;
+	onChange: OnColorChange;
+	value?: string;
+	actions?: ReactNode;
+};
+
+export type SinglePaletteProps = PaletteProps & {
+	colors: ColorObject[];
+};
+
+export type MultiplePalettesProps = PaletteProps & {
+	colors: PaletteObject[];
+};
+
+export type CustomColorPickerDropdownProps = {
+	isRenderedInSidebar: boolean;
+	renderContent: () => JSX.Element;
+	popoverProps?: string[];
+	renderToggle: ( props: {
+		isOpen: boolean;
+		onToggle: MouseEventHandler< HTMLButtonElement >;
+	} ) => JSX.Element;
```

Same as above:

```suggestion
	} ) => ReactNode;
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/border-control/types.ts (MODIFIED, +1/-1)
- packages/components/src/circular-option-picker/index.js (MODIFIED, +14/-20)
- packages/components/src/color-palette/README.md (MODIFIED, +51/-49)
- packages/components/src/color-palette/index.tsx (RENAMED, +132/-51)
- packages/components/src/color-palette/stories/index.tsx (RENAMED, +38/-27)
- packages/components/src/color-palette/styles.ts (RENAMED, +0/-0)
- packages/components/src/color-palette/test/__snapshots__/index.tsx.snap (RENAMED, +0/-0)
- packages/components/src/color-palette/test/index.tsx (RENAMED, +1/-1)
- packages/components/src/color-palette/types.ts (ADDED, +93/-0)
