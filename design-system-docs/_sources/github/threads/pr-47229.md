---
number: 47229
type: pull_request
state: MERGED
merged_at: 2023-02-06T10:48:18Z
url: https://github.com/WordPress/gutenberg/pull/47229
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #47229: CustomSelectControl: Privatise __experimentalShowSelectedHint using @wordpress/experiments

- URL: https://github.com/WordPress/gutenberg/pull/47229
- Author: noisysocks
- Created: 2023-01-18T04:37:19Z
- Updated: 2023-02-09T11:45:49Z
- Comments: 25 of 25
- Review threads: 9 of 9
- Changed files: 30 of 30
- Diff: ../pr-diffs/47229.diff

## Body

## What?
Part of https://github.com/WordPress/gutenberg/issues/47196. Uses `@wordpress/experiments` (https://github.com/WordPress/gutenberg/pull/46131) to make `__experimentalShowSelectedHint` in `CustomSelectControl` private.

## Why?
We don't want to add any new experimental APIs to 6.2 as part of an effort to no longer expose experimental APIs in Core.

## How?
https://github.com/WordPress/gutenberg/blob/trunk/docs/contributors/code/coding-guidelines.md#experimental-react-component-properties

## Testing Instructions
1. Use a block theme with more than 5 font sizes or manually edit `theme.json` to contain more than 5 font sizes in `settings.typography.fontSizes`.
2. Open the site editor. Appearance → Editor → Edit.
3. Go to Styles → Typography → Headings.
4. Select a heading level.
5. Toggle off the custom font size picker.
6. You should see a hint alongside the selected font size preset.

## Issue comments

### noisysocks on 2023-01-18T04:43:15Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1386479455

An alternative to this would be to stabilise `__experimentalHint` and `__experimentalShowSelectedHint` which, to be honest, I'd be fine with, since it seems to work fine and I doubt it would add that much extra burden to any rewrite of `CustomSelectControl`. Maybe @ciampo can weigh in there.

### ciampo on 2023-01-24T23:00:53Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1402800263

Hey all! Trying to catch up on a long queue, I initially missed the pings in this PR.

> An alternative to this would be to stabilise `__experimentalHint` and `__experimentalShowSelectedHint` which, to be honest, I'd be fine with, since it seems to work fine and I doubt it would add that much extra burden to any rewrite of `CustomSelectControl`. Maybe @ciampo can weigh in there.

We _could_, although my preference would be not to commit to those APIs just yet. I'd really love to finish the `CustomSelectControl` refactor before 6.2, but at this point in time I'm not in a position to guarantee that either.

Plus, most of the challenges that highlighted in this PR would apply to any other component — and therefore, we would need to overcome them anyways.

Related to the other conversations in this PR, I'll answer inline :)

### noisysocks on 2023-01-30T03:15:05Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1407931214

Think I've addressed all feedback. Can I get a final review?

### ciampo on 2023-01-30T21:18:42Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1409354739

Thank you all for collaborating on this!

While reading through this PR I started reflecting about the `lock`/`unlock` approach with its pros and cons.

The APIs definitely do a good job at drawing a clear boundary and making it explicit that locked (experimental) APIs are not guaranteed to be backwards-compatible in future releases.

At the same time, as a core contributor, I see some potential drawbacks:
 - Given that we’re still experimenting with the lock/unlock APIs, will we be able to make (breaking) changes to them, including removing them from the experiments package, after the upcoming 6.2 release?
 - Using these APIs adds one more layer of complexity not only for the consumers of the experimental APIs but also for the authors who would need to prepare 2 different exports for each “API” (including types & docs)
 - As Lena [mentioned](https://github.com/WordPress/gutenberg/pull/47229#discussion_r1087729398) above, removing type defs from experimental code could be a huge loss for devX and code quality.

I realise that this extra “friction” may be in place exactly to act as a deterrent against adding and consuming unstable/experimental APIs.

Zooming out a little bit and looking at the broader approach to compatibility, it’s important that we try to align the interests of both the core Gutenberg contributors and the broader WP community. In order to nurture a healthier developer ecosystem that is sustainable and modern, we need to allow the core Gutenberg contributors to be as effective as possible, trying to avoid technical debt having repercussions on the wider community.

### noisysocks on 2023-01-31T03:45:07Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1409710192

> * Using these APIs adds one more layer of complexity not only for the consumers of the experimental APIs but also for the authors who would need to prepare 2 different exports for each “API” (including types & docs)

Yes we'll be able to make changes to `lock()`ed APIs that are merge into WP 6.2 after 6.2 is released because they are not easily available to extenders. This is the motivation behind `lock()` and `unlock()`. If the `__experimental` API lands in WP 6.2 without being `lock()`ed then it is de facto stabilised because the `__experimental` prefix is a Gutenberg convention that essentially has no meaning in Core. Core maintains backwards compatibility for all publicly available APIs even if they have a `_` prefix or `@access private` doc comment. See https://make.wordpress.org/core/2022/08/10/proposal-stop-merging-experimental-apis-from-gutenberg-to-wordpress-core/ for previous discussion on that.

> * As Lena [mentioned](https://github.com/WordPress/gutenberg/pull/47229#discussion_r1087729398) above, removing type defs from experimental code could be a huge loss for devX and code quality.
> *  I realise that this extra “friction” may be in place exactly to act as a deterrent against adding and consuming unstable/experimental APIs.

@adamziel can probably speak better to these points but I feel that they may be solvable with enhancements to `@wordpress/experiments`. I recall Adam intending to improve the TypeScript situation. 

I might spin up a GitHub Discussion for the higher level discussion about `lock()` / `unlock()`, experimental APIs, DevX, and backwards compatibility. In this PR I'm aiming to make one experimental API not become a part of the WP 6.2 public API per https://github.com/WordPress/gutenberg/issues/47196 using the tools we have available today as immature as they are.

### github-actions on 2023-01-31T05:50:04Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1409799616

**Size Change:** +428 B (0%) 

**Total Size:** 1.31 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/block-editor/index.min.js` | 192 kB | -518 B (0%) |
| `build/block-editor/style-rtl.css` | 14.4 kB | +60 B (0%) |
| `build/block-editor/style.css` | 14.4 kB | +58 B (0%) |
| `build/block-library/index.min.js` | 199 kB | -28 B (0%) |
| `build/blocks/index.min.js` | 51 kB | +608 B (+1%) |
| `build/components/index.min.js` | 203 kB | +266 B (0%) |
| `build/edit-post/style-rtl.css` | 7.5 kB | +44 B (+1%) |
| `build/edit-post/style.css` | 7.5 kB | +43 B (+1%) |
| `build/edit-site/index.min.js` | 64.1 kB | -292 B (0%) |
| `build/edit-site/style-rtl.css` | 9.68 kB | +24 B (0%) |
| `build/edit-site/style.css` | 9.67 kB | +23 B (0%) |
| `build/edit-widgets/style-rtl.css` | 4.52 kB | +31 B (+1%) |
| `build/edit-widgets/style.css` | 4.52 kB | +30 B (+1%) |
| `build/editor/style-rtl.css` | 3.58 kB | +3 B (0%) |
| `build/editor/style.css` | 3.57 kB | +2 B (0%) |
| `build/experiments/index.min.js` | 938 B | +33 B (+4%) |
| `build/keycodes/index.min.js` | 1.92 kB | +41 B (+2%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 993 B |
| `build/annotations/index.min.js` | 2.78 kB |
| `build/api-fetch/index.min.js` | 2.27 kB |
| `build/autop/index.min.js` | 2.15 kB |
| `build/blob/index.min.js` | 483 B |
| `build/block-directory/index.min.js` | 7.2 kB |
| `build/block-directory/style-rtl.css` | 1.04 kB |
| `build/block-directory/style.css` | 1.04 kB |
| `build/block-editor/content-rtl.css` | 4.11 kB |
| `build/block-editor/content.css` | 4.1 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 403 B |
| `build/block-editor/default-editor-styles.css` | 403 B |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 138 B |
| `build/block-library/blocks/audio/theme.css` | 138 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 91 B |
| `build/block-library/blocks/avatar/style.css` | 91 B |
| `build/block-library/blocks/block/editor-rtl.css` | 305 B |
| `build/block-library/blocks/block/editor.css` | 305 B |
| `build/block-library/blocks/button/editor-rtl.css` | 485 B |
| `build/block-library/blocks/button/editor.css` | 485 B |
| `build/block-library/blocks/button/style-rtl.css` | 532 B |
| `build/block-library/blocks/button/style.css` | 532 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 84 B |
| `build/block-library/blocks/categories/editor.css` | 83 B |
| `build/block-library/blocks/categories/style-rtl.css` | 100 B |
| `build/block-library/blocks/categories/style.css` | 100 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 406 B |
| `build/block-library/blocks/columns/style.css` | 406 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 612 B |
| `build/block-library/blocks/cover/editor.css` | 613 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.57 kB |
| `build/block-library/blocks/cover/style.css` | 1.56 kB |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 138 B |
| `build/block-library/blocks/embed/theme.css` | 138 B |
| `build/block-library/blocks/file/editor-rtl.css` | 300 B |
| `build/block-library/blocks/file/editor.css` | 300 B |
| `build/block-library/blocks/file/style-rtl.css` | 253 B |
| `build/block-library/blocks/file/style.css` | 254 B |
| `build/block-library/blocks/file/view.min.js` | 353 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.44 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.44 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 984 B |
| `build/block-library/blocks/gallery/editor.css` | 988 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.55 kB |
| `build/block-library/blocks/gallery/style.css` | 1.55 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 654 B |
| `build/block-library/blocks/group/editor.css` | 654 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 76 B |
| `build/block-library/blocks/heading/style.css` | 76 B |
| `build/block-library/blocks/html/editor-rtl.css` | 332 B |
| `build/block-library/blocks/html/editor.css` | 333 B |
| `build/block-library/blocks/image/editor-rtl.css` | 829 B |
| `build/block-library/blocks/image/editor.css` | 828 B |
| `build/block-library/blocks/image/style-rtl.css` | 627 B |
| `build/block-library/blocks/image/style.css` | 630 B |
| `build/block-library/blocks/image/theme-rtl.css` | 137 B |
| `build/block-library/blocks/image/theme.css` | 137 B |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 298 B |
| `build/block-library/blocks/latest-comments/style.css` | 298 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 213 B |
| `build/block-library/blocks/latest-posts/editor.css` | 212 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 478 B |
| `build/block-library/blocks/latest-posts/style.css` | 478 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 507 B |
| `build/block-library/blocks/media-text/style.css` | 505 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 716 B |
| `build/block-library/blocks/navigation-link/editor.css` | 715 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.13 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.14 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.22 kB |
| `build/block-library/blocks/navigation/style.css` | 2.2 kB |
| `build/block-library/blocks/navigation/view-modal.min.js` | 2.81 kB |
| `build/block-library/blocks/navigation/view.min.js` | 447 B |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 376 B |
| `build/block-library/blocks/page-list/editor.css` | 376 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 174 B |
| `build/block-library/blocks/paragraph/editor.css` | 174 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 279 B |
| `build/block-library/blocks/paragraph/style.css` | 281 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 501 B |
| `build/block-library/blocks/post-comments-form/style.css` | 501 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 73 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 73 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 134 B |
| `build/block-library/blocks/post-excerpt/style.css` | 134 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 586 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 584 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 318 B |
| `build/block-library/blocks/post-featured-image/style.css` | 318 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 153 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 153 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 282 B |
| `build/block-library/blocks/post-template/style.css` | 282 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 103 B |
| `build/block-library/blocks/preformatted/style.css` | 103 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 326 B |
| `build/block-library/blocks/pullquote/style.css` | 325 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 167 B |
| `build/block-library/blocks/pullquote/theme.css` | 167 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 288 B |
| `build/block-library/blocks/query-pagination/style.css` | 284 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 458 B |
| `build/block-library/blocks/query/editor.css` | 457 B |
| `build/block-library/blocks/quote/style-rtl.css` | 213 B |
| `build/block-library/blocks/quote/style.css` | 213 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 149 B |
| `build/block-library/blocks/rss/editor.css` | 149 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 165 B |
| `build/block-library/blocks/search/editor.css` | 165 B |
| `build/block-library/blocks/search/style-rtl.css` | 409 B |
| `build/block-library/blocks/search/style.css` | 406 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 234 B |
| `build/block-library/blocks/separator/style.css` | 234 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 474 B |
| `build/block-library/blocks/shortcode/editor.css` | 474 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 490 B |
| `build/block-library/blocks/site-logo/editor.css` | 490 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 203 B |
| `build/block-library/blocks/site-logo/style.css` | 203 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 116 B |
| `build/block-library/blocks/site-title/editor.css` | 116 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 57 B |
| `build/block-library/blocks/site-title/style.css` | 57 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 184 B |
| `build/block-library/blocks/social-link/editor.css` | 184 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 674 B |
| `build/block-library/blocks/social-links/editor.css` | 673 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.4 kB |
| `build/block-library/blocks/social-links/style.css` | 1.39 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 332 B |
| `build/block-library/blocks/spacer/editor.css` | 332 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 433 B |
| `build/block-library/blocks/table/editor.css` | 433 B |
| `build/block-library/blocks/table/style-rtl.css` | 651 B |
| `build/block-library/blocks/table/style.css` | 650 B |
| `build/block-library/blocks/table/theme-rtl.css` | 157 B |
| `build/block-library/blocks/table/theme.css` | 157 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 251 B |
| `build/block-library/blocks/tag-cloud/style.css` | 253 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 404 B |
| `build/block-library/blocks/template-part/editor.css` | 404 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 179 B |
| `build/block-library/blocks/video/style.css` | 179 B |
| `build/block-library/blocks/video/theme-rtl.css` | 139 B |
| `build/block-library/blocks/video/theme.css` | 139 B |
| `build/block-library/classic-rtl.css` | 162 B |
| `build/block-library/classic.css` | 162 B |
| `build/block-library/common-rtl.css` | 1.11 kB |
| `build/block-library/common.css` | 1.11 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 11.5 kB |
| `build/block-library/editor.css` | 11.5 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/reset-rtl.css` | 478 B |
| `build/block-library/reset.css` | 478 B |
| `build/block-library/style-rtl.css` | 12.5 kB |
| `build/block-library/style.css` | 12.5 kB |
| `build/block-library/theme-rtl.css` | 698 B |
| `build/block-library/theme.css` | 703 B |
| `build/block-serialization-default-parser/index.min.js` | 1.13 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.83 kB |
| `build/components/style-rtl.css` | 11.6 kB |
| `build/components/style.css` | 11.7 kB |
| `build/compose/index.min.js` | 12.3 kB |
| `build/core-data/index.min.js` | 15.9 kB |
| `build/customize-widgets/index.min.js` | 11.8 kB |
| `build/customize-widgets/style-rtl.css` | 1.41 kB |
| `build/customize-widgets/style.css` | 1.41 kB |
| `build/data-controls/index.min.js` | 663 B |
| `build/data/index.min.js` | 8.06 kB |
| `build/date/index.min.js` | 32.1 kB |
| `build/deprecated/index.min.js` | 518 B |
| `build/dom-ready/index.min.js` | 336 B |
| `build/dom/index.min.js` | 4.71 kB |
| `build/edit-post/classic-rtl.css` | 571 B |
| `build/edit-post/classic.css` | 571 B |
| `build/edit-post/index.min.js` | 34.5 kB |
| `build/edit-widgets/index.min.js` | 16.9 kB |
| `build/editor/index.min.js` | 45.5 kB |
| `build/element/index.min.js` | 4.93 kB |
| `build/escape-html/index.min.js` | 548 B |
| `build/format-library/index.min.js` | 7.23 kB |
| `build/format-library/style-rtl.css` | 598 B |
| `build/format-library/style.css` | 597 B |
| `build/hooks/index.min.js` | 1.66 kB |
| `build/html-entities/index.min.js` | 454 B |
| `build/i18n/index.min.js` | 3.79 kB |
| `build/is-shallow-equal/index.min.js` | 535 B |
| `build/keyboard-shortcuts/index.min.js` | 1.79 kB |
| `build/list-reusable-blocks/index.min.js` | 2.14 kB |
| `build/list-reusable-blocks/style-rtl.css` | 865 B |
| `build/list-reusable-blocks/style.css` | 865 B |
| `build/media-utils/index.min.js` | 2.99 kB |
| `build/notices/index.min.js` | 977 B |
| `build/plugins/index.min.js` | 1.95 kB |
| `build/preferences-persistence/index.min.js` | 2.23 kB |
| `build/preferences/index.min.js` | 1.35 kB |
| `build/primitives/index.min.js` | 960 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/react-i18n/index.min.js` | 702 B |
| `build/react-refresh-entry/index.min.js` | 8.44 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.75 kB |
| `build/reusable-blocks/index.min.js` | 2.26 kB |
| `build/reusable-blocks/style-rtl.css` | 265 B |
| `build/reusable-blocks/style.css` | 265 B |
| `build/rich-text/index.min.js` | 10.8 kB |
| `build/server-side-render/index.min.js` | 2.09 kB |
| `build/shortcode/index.min.js` | 1.52 kB |
| `build/style-engine/index.min.js` | 1.53 kB |
| `build/token-list/index.min.js` | 650 B |
| `build/url/index.min.js` | 3.69 kB |
| `build/vendors/inert-polyfill.min.js` | 2.48 kB |
| `build/vendors/react-dom.min.js` | 41.8 kB |
| `build/vendors/react.min.js` | 4.02 kB |
| `build/viewport/index.min.js` | 1.09 kB |
| `build/warning/index.min.js` | 280 B |
| `build/widgets/index.min.js` | 7.31 kB |
| `build/widgets/style-rtl.css` | 1.18 kB |
| `build/widgets/style.css` | 1.18 kB |
| `build/wordcount/index.min.js` | 1.06 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### adamziel on 2023-01-31T21:50:56Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1411116307

> Using these APIs adds one more layer of complexity not only for the consumers of the experimental APIs 

@ciampo The only consumers are packages in the Gutenberg repository. The experimental APIs are private and should not be used anywhere outside. 

> but also for the authors who would need to prepare 2 different exports for each “API” (including types & docs)

The friction is in separating experimental components from stable components for WordPress core. `lock()`/`unlock()` is just a delivery mechanism. 

> * As Lena [mentioned](https://github.com/WordPress/gutenberg/pull/47229#discussion_r1087729398) above, removing type defs from experimental code could be a huge loss for devX and code quality.

We can retain type defs – it's just not implemented at the moment. I fleshed out [the basic idea](https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073418480) in one of the comments in this PR.

> * I realise that this extra “friction” may be in place exactly to act as a deterrent against adding and consuming unstable/experimental APIs.

Ideally consuming experimental APIs would be easy in core and impossible outside of core. If you have any suggestions how to reduce any internal friction without reducing the privacy, I'd love to discuss!

### ciampo on 2023-02-01T11:50:44Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1411930759

> The only consumers are packages in the Gutenberg repository. The experimental APIs are private and should not be used anywhere outside.

Thank you for the clarification — what I meant is that even if the consumers of the APIs are other Gutenberg packages, this  extra layer of complexity could make it harder for core contributors (especially new/less experienced members of the community) to learn the codebase.

But, as I mentioned before, this could also be "on purpose" — ie. discouraging folks from adding experimental APIs in the first place.

> We can retain type defs – it's just not implemented at the moment. I fleshed out https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073418480 in one of the comments in this PR.
 
I saw that, and it definitely looks promising 😄 

> Ideally consuming experimental APIs would be easy in core and impossible outside of core. If you have any suggestions how to reduce any internal friction without reducing the privacy, I'd love to discuss!

Absolutely — and thank you again for the work you've been doing on this. I'll definitely make sure to share feedback / suggestions if I come up with anything.

### noisysocks on 2023-02-03T02:57:01Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1414744629

Thanks @adamziel, appreciate you. I fixed the merge conflicts here. Should be ready again for final review.

### github-actions on 2023-02-03T03:24:59Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1414791349

<!-- flaky-tests-report-comment -->
**Flaky tests detected in e2b8e351cda0058478435be304535489aacb68a2.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/4099603874
📝  Reported issues:
- #47632 in `specs/editor/blocks/query.test.js`
- #44353 in `specs/editor/various/multi-block-selection.test.js`

### ciampo on 2023-02-03T09:28:44Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415464925

It looks like the double-registration check is failing in [the "Unit Tests / Mobile" check](https://github.com/WordPress/gutenberg/actions/runs/4081846478/jobs/7035598346)

### adamziel on 2023-02-03T11:19:52Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415732722

@ciampo Just pushed 15c5915db369baed6996f441452da3994c852d78 🤞

### ciampo on 2023-02-03T11:57:19Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415768244

For some reasons the CI checks are not running 🤔

### adamziel on 2023-02-03T12:02:18Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415775352

Yay, no errors then!

### adamziel on 2023-02-03T12:03:07Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415776947

I'll squash and push again

### adamziel on 2023-02-03T12:09:10Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415786557

Just squashed, let's hope the checks work this time.

### adamziel on 2023-02-03T12:25:59Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415804158

@ciampo The mobile tests are still failing, but it's no longer about the experimental check. The failures don't seem to be related at all so I rebased this one and pushed again.

### adamziel on 2023-02-03T12:54:09Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1415832952

@ciampo Mobile unit tests passed!

### noisysocks on 2023-02-06T02:00:17Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1418384661

Thanks once again @adamziel! Pushed a fix for @ciampo's latest comment as well as a few minor things I noticed while reviewing the diff. It looks good to me.

@adamziel: Hopefully tests pass and we can merge but, if not, could you take over this PR? I'll be out on parental leave for 8 weeks as of tomorrow 👶

### adamziel on 2023-02-06T08:38:40Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1418705994

@noisysocks Native e2e tests failed, I just restarted them. Fingers crossed!

Also – that's awesome! 👶  Congratulations man!

### adamziel on 2023-02-06T10:47:47Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1418879333

@ciampo the bad news is that the same native test is failing for 5 restarts now. The good news is it also failed for the last 5 commits to trunk. Let's merge this PR!

### jsnajdr on 2023-02-06T12:14:08Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1418986705

> the bad news is that the same native test is failing for 5 restarts now. The good news is it also failed for the last 5 commits to trunk.

It's an error when installing Playwright, trying to download stuff:
```
E: Failed to fetch http://azure.archive.ubuntu.com/ubuntu/pool/main/m/mpg123/libmpg123-0_1.29.3-1build1_amd64.deb  Unable to connect to azure.archive.ubuntu.com:http: [IP: 52.252.75.106 80]
```
@WunderBart Is this really an intermittent failure that eventually fixes itself, or maybe it's a sign that we have an outdated Ubuntu or something like that?

### geriux on 2023-02-06T16:55:11Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1419406561

> @ciampo the bad news is that the same native test is failing for 5 restarts now. The good news is it also failed for the last 5 commits to trunk. Let's merge this PR!

Heads up that this indeed [broke the mobile editor](https://github.com/WordPress/gutenberg/pull/47792/files), it looks like we have some flakiness there on the mobile E2E tests, we will check those but we are always happy to double-check any CI checks failing in any PRs before merging to avoid future failures in `trunk`.

### adamziel on 2023-02-07T14:52:22Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1420904372

Oops, sorry about that @geriux – I'll let you know next time this happens!

### ntsekouras on 2023-02-09T11:45:42Z

URL: https://github.com/WordPress/gutenberg/pull/47229#issuecomment-1424057289

Cherry-picked this PR to the wp/6.2 branch.

## Review threads

### Review thread 1 (resolved)

#### noisysocks on 2023-01-18T04:38:52Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073088066
Path: packages/experiments/tsconfig.json

```diff
@@ -0,0 +1,9 @@
+{
+	"extends": "../../tsconfig.base.json",
+	"compilerOptions": {
+		"rootDir": "src",
+		"declarationDir": "build-types",
+		"checkJs": false
+	},
+	"include": [ "src/**/*" ]
+}
```

I had to configure `@wordpress/experiments` to generate type definitions so that I could import it into `@wordpress/components`. The generated definitions seemed fine to me after I added a doc comment to `__dangerousOptInToUnstableAPIsOnlyForCoreModules`. cc. @adamziel

### Review thread 2

#### noisysocks on 2023-01-18T04:40:09Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073088604
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

This check doesn't play well with Story Book. There, `__dangerousOptInToUnstableAPIsOnlyForCoreModules` is called multiple times assumedly due to hot module loading. This causes an error when navigating to a different component.

![image](https://user-images.githubusercontent.com/612155/213085518-f94b551d-6772-4fe3-9aee-03d0df03b78a.png)

I'm not sure what to do to get around this. Thoughts @adamziel?

#### adamziel on 2023-01-18T11:40:27Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073427089
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

Could this be unrelated to HMR? I wonder if the storybook is loading the same file twice.

If this is indeed a hot reloading issue:

Ideally `__dangerousOptInToUnstableAPIsOnlyForCoreModules` wouldn't be called for the second time on hot reload. The module has already opted-in and the global state knows it, there's no need to go through the motions all over again. The easiest solution would be:

```js
try {
    __dangerousOptInToUnstableAPIsOnlyForCoreModules()
} catch(e) {
    // .. ignore
}
```
However, this would obscure genuine errors.

I have two ideas:

1. Do not re-evaluate experiments.js during HMR
2. Detect we're inside HMR and bale out

#### adamziel on 2023-01-18T12:27:04Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073470270
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

That check matters mostly in the WordPress build. We can build the Gutenberg plugin without that error:

```js
	if ( ! IS_GUTENBERG_PLUGIN ) {
		// This check doesn't play well with Hot Module Reloading and isn't included in
		// the Gutenberg plugin. It only matters in the WordPress core release.
		if ( registeredExperiments.includes( moduleName ) ) {
			throw new Error(
				`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
					'This feature is only for JavaScript modules shipped with WordPress core. ' +
					'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
					'without a warning. If you ignore this error and depend on unstable features, ' +
					'your product will inevitably break on one of the next WordPress releases.'
			);
		}
	}
```

#### noisysocks on 2023-01-19T02:33:09Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080751731
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

Will defer to you on this 😅 I'm not really sure what's going on. I'll update the PR with your `! IS_GUTENBERG_PLUGIN` suggestion.

#### noisysocks on 2023-01-19T02:38:15Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080753761
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

Oh, that doesn't fix this because `IS_GUTENBERG_PLUGIN` is not set when running the story book (`npm run storybook:dev`).

@ciampo: Is it safe to set `process.env.IS_GUTENBERG_PLUGIN = true` in `storybook/main.js`?

#### noisysocks on 2023-01-19T02:51:37Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080759219
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> Could this be unrelated to HMR? I wonder if the storybook is loading the same file twice.

Yes you're right it does seem to be due to story book lazily loading script dependencies. Just a guess: by calling into `__dangerousOptInToUnstableAPIsOnlyForCoreModules` which mutates a global object are we now violating the `"sideEffects": true` condition that e.g. `@wordpress/components` has?

#### ntsekouras on 2023-01-19T08:05:26Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080919764
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

I'm not sure about the proper solution right now, but if we want an easy way out, the suggested `try catch` could just ignore the specific errors and not the rest..

#### adamziel on 2023-01-19T10:21:56Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1081062688
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> I'm not sure about the proper solution right now, but if we want an easy way out, the suggested `try catch` could just ignore the specific errors and not the rest..

Oh I meant if someone tampers with the opt-in process in their plugin, we want to throw an exception. However, in the storybook context, we don't. But it's the same exception.

#### adamziel on 2023-01-19T10:25:58Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1081066931
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> sideEffects": true

@noisysocks a global state is definitely a side effect. Fortunately, there's no good reason to ever re-evaluate `experiments.js` – if we can make the tooling only run it once, we're good. One idea is to set `"sideEffects": true` in the experiments package and `"sideEffects": ["./src/experiments.js"]` in any package opting-in to experiments ([webpack docs say files arrays are valid values](https://webpack.js.org/guides/tree-shaking/)).

#### noisysocks on 2023-01-20T01:08:06Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1082023885
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

I configured `sideEffects` with your suggestions but that didn't seem to affect anything.

I also tried wrapping the call to `__dangerousOptInToUnstableAPIsOnlyForCoreModules` in a gate (`if ( ! hasLoaded ) { }`) to no effect.

I can't figure this out 😢 @adamziel: can you help? You can see the error by:

1. Un-comment the check in `packages/experiments/src/implementation.js`.
1. `npm run storybook:dev`
2. Use left sidebar to navigate to the _Block Editor_ story.
3. Use left sidebar to navigate to the _CustomSelectControl_ story.

#### adamziel on 2023-01-24T19:15:41Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1085798937
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> I configured `sideEffects` with your suggestions but that didn't seem to affect anything.

I can't get it to work either! Let's just remove that check from the Gutenberg plugin with `IS_GUTENBERG_PLUGIN` or a custom constant like `IS_WORDPRESS_CORE` in case we can't use the former in the storybook.

> I also tried wrapping the call to `__dangerousOptInToUnstableAPIsOnlyForCoreModules` in a gate (`if ( ! hasLoaded ) { }`) to no effect.

That would likely have to be `! window.hasLoaded` – I assume all the local variables get wiped out. This worked for me:

```js
export const { lock, unlock } =
	window.hasLoaded ||
	__dangerousOptInToUnstableAPIsOnlyForCoreModules(
		'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.',
		'@wordpress/block-editor'
	);

window.hasLoaded = { lock, unlock };
```

But of course that's not a solution.

#### ciampo on 2023-01-24T23:03:01Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086030970
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

>  Is it safe to set process.env.IS_GUTENBERG_PLUGIN = true in storybook/main.js?

I _think_ so, although I'd also like to double-check with @mirka and @gziolo (when he's back) to make sure! (to clarify, my preference would still be to get to a cleaner solution, e.g somehow avoiding running/evaluating the check twice)

#### noisysocks on 2023-01-25T03:25:38Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086172886
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

OK, I've added a check for `IS_GUTENBERG_PLUGIN` and defined this var in `storybook/main.js`.

#### mirka on 2023-01-25T11:54:49Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086545997
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> Is it safe to set process.env.IS_GUTENBERG_PLUGIN = true in storybook/main.js?

Just so I understand this, does the `IS_GUTENBERG_PLUGIN` env var really have no other effect on the behavior of wp-components (and wp-block-editor I guess), and can we be sure that it will stay that way in the future?

Basically this goes against the isolation work we've been doing to untangle implicit Gutenberg/WP dependencies from the default Storybook environment. It also means that setting `IS_GUTENBERG_PLUGIN=true` is now a requirement for getting these packages to work, at least in some environments that are HMR'ed or chunked or whatever the root cause is.

Even if it's "safe", it doesn't make sense from an architectural standpoint, so I think it's something worth resolving more cleanly.

#### adamziel on 2023-01-25T19:26:40Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1087082922
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

@noisysocks in this case I'd go for `ALLOW_EXPERIMENTS_REREGISTRATION` that would be `true` everywhere except the WordPress core merge

#### noisysocks on 2023-01-31T23:44:01Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1092600317
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

It feels overkill to me that there should be a global env variable for this one specific thing. Can we kill the reregistration check altogether? In my mind the only check that matters is the consent string.

#### adamziel on 2023-02-01T08:20:44Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1092890066
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> It feels overkill to me that there should be a global env variable for this one specific thing. 

To me it's the opposite – removing that check just for this one specific thing feels like overkill.

The consent string is a carrot and this checks is a stick, I think it's important to have both. A global variable is just a feature flag – they are commonly used in similar scenarios. Why would it be overkill? I could prepare the necessary commit if you wish.

#### jsnajdr on 2023-02-01T09:59:27Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1093002091
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

@adamziel I'm coming late to this discussion so I don't have all the context -- but it seems to me that allowing double registration of the same module is just fine. We check if the `moduleName` is one of the core modules, and if it is, return the `{ lock, unlock }` pair. We can do the same on the second or the 100th call, there's no reason to allow just a single call.

The `registeredExperiments` array could be removed completely. It's used only to check for these repeated registrations and nothing else. And we could get rid of `resetRegisteredExperiments`, too, making the experiments module stateless.

#### noisysocks on 2023-02-01T23:52:52Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1093860745
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> The consent string is a carrot and this checks is a stick, I think it's important to have both. A global variable is just a feature flag – they are commonly used in similar scenarios. Why would it be overkill? I could prepare the necessary commit if you wish.

Hmmm ok I suppose. I just dislike global config 😅

> The `registeredExperiments` array could be removed completely. It's used only to check for these repeated registrations and nothing else. And we could get rid of `resetRegisteredExperiments`, too, making the experiments module stateless.

Making experiments stateless is appealing to me. I imagine we'll face this exact problem again in a different context that requires loading modules multiple times e.g. adding HMR, adding lazy loading, turning WP Admin into more of a SPA.

Up to you @adamziel, it's your baby 😀

#### adamziel on 2023-02-02T13:13:32Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1094516181
Path: packages/experiments/src/implementation.js

```diff
@@ -51,15 +60,15 @@ export const __dangerousOptInToUnstableAPIsOnlyForCoreModules = (
 				'your product will inevitably break on one of the next WordPress releases.'
 		);
 	}
-	if ( registeredExperiments.includes( moduleName ) ) {
-		throw new Error(
-			`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
-				'This feature is only for JavaScript modules shipped with WordPress core. ' +
-				'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
-				'without a warning. If you ignore this error and depend on unstable features, ' +
-				'your product will inevitably break on one of the next WordPress releases.'
-		);
-	}
+	// if ( registeredExperiments.includes( moduleName ) ) {
+	// 	throw new Error(
+	// 		`You tried to opt-in to unstable APIs as module "${ moduleName }" which is already registered. ` +
+	// 			'This feature is only for JavaScript modules shipped with WordPress core. ' +
+	// 			'Please do not use it in plugins and themes as the unstable APIs will be removed ' +
+	// 			'without a warning. If you ignore this error and depend on unstable features, ' +
+	// 			'your product will inevitably break on one of the next WordPress releases.'
+	// 	);
+	// }
```

> @adamziel I'm coming late to this discussion so I don't have all the context -- but it seems to me that allowing double registration of the same module is just fine.

@jsnajdr The double registration protection is to prevent developers from opting-in to experiments by pretending to be one of the core packages. Everything would still work without it, but the private APIs would be less protected.

> I imagine we'll face this exact problem again (...) adding HMR, adding lazy loading, turning WP Admin into more of a SPA.

@noisysocks IMHO Including strict checks in the initial launch will discourage tampering more effectively than not including them.  Of course – the time may come when they have to be removed. That's fine. It's not like we'll advertise the adjustment anywhere. I'll update this PR to include the additional feature flag.

Edit: See b2585fcf9df5461eb7e43609722fa6557aef8149

### Review thread 3 (resolved)

#### youknowriad on 2023-01-18T10:43:09Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073372651
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

Should we "lock" the experimental component within the stable one or have a unique "experiments" object that holds all the experimental components/functions of a given package? 

While in this case, these two are related, sometimes we might not having anything "obvious" to attach too (like a new experimental component...) so I wonder if we should be consistent and have some guideline to attach all top level items to a single "experiments" object?

#### adamziel on 2023-01-18T11:53:15Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073438817
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

To visualise, it's between these two:

```js
import { experiments as blockEditorExperiments, CustomSelectControl } from '@wordpress/block-editor';
import { unlock } from './experiments';

const ExperimentalCustomSelectControl = unlock( CustomSelectControl );
const { topLevelFunction } = unlock( blockEditorExperiments );
```

vs 

```js
import { experiments as blockEditorExperiments } from '@wordpress/block-editor';
import { unlock } from './experiments';

const { ExperimentalCustomSelectControl, topLevelFunction } = unlock( blockEditorExperiments );
```

I like the first one better:

* It feels consistent with how a store can be unlocked (`unlock( select( store ) )`)
* It reflects the strong coupling between the private and the public component
* It forces loading the specific component file which is handy for async loading
* [Typehints can be added relatively easily in the first scenario](https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073418480) but not in the second one.

Also cc @jsnajdr who came up with the idea of locking.

#### youknowriad on 2023-01-18T11:59:12Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073444259
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

I like the second one personally because it's needed anyway for new components, APIs that are not tied to anything but the package and it's consistent. With the first one, we're going to be asking ourselves the same question and creating inconsistencies as we add locked APIs

#### adamziel on 2023-01-18T12:11:39Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073455745
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

Also cc @ntsekouras @ramonjd @dmsnell @gziolo @mcsf as that's a substantial discussion that will affect all the future experimental APIs.

#### dmsnell on 2023-01-18T21:05:03Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080554499
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

I'd normally consider the second one more heavily given the manual work involved in creating and maintaining all these experimental points of contact, but given that part of the goal here is to make this stuff as annoying as possible, I think the first isn't so bad.

is the problem that we're running this code on module initialization? that it's in the module global scope vs. in some kind of `init()` function? if so, could we _lock the lock_ behind a static latch?

```js
export let moduleAlreadyUnlocked = false;
```

sorry for not being very coherent here; I may not be that helpful on this issue.

#### noisysocks on 2023-01-19T04:23:32Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080792297
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

Something else I wondered re. conventions is if we should still use the `__experimental` prefix for a prop or a component that is locked?

#### ntsekouras on 2023-01-19T07:57:48Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080913649
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

>because it's needed anyway for new components, APIs that are not tied to anything but the package and it's consistent.

That sounds right. My understanding([based on docs](https://github.com/WordPress/gutenberg/blob/trunk/docs/contributors/code/coding-guidelines.md#experimental-functions-classes-and-variables)) is that we should already do something like that by creating an object to attach the new ones and then export to unlock. Unless I'm wrong :) 

>Something else I wondered re. conventions is if we should still use the __experimental prefix for a prop or a component that is locked?

If we have them locked I don't think it's that important. In some cases we might not be able to avoid it(components), but I guess we can lose them for props, etc.. 🤔

#### youknowriad on 2023-01-19T08:52:30Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080963985
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

> the goal here is to make this stuff as annoying as possible, I think the first isn't so bad.

The goal is to make it annoying for consumers, not for producers of these APIs. This impacts mostly producers and how we organize our code. Not sure it has any impact on whether it's annoying to consume it.

> prefix

Agree here that there's no need for prefixes anymore.

#### adamziel on 2023-01-19T10:19:58Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1081060687
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

The consensus seems to be with locking a top-level module export so move forward with that 👍

#### noisysocks on 2023-01-20T01:03:59Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1082021737
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

I locked the component onto a top-level `experiments` object and and removed the `__experimental` prefix.

#### mirka on 2023-01-25T11:34:18Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086526651
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

Can't speak for the other packages, but at least for the wp-components case, I think there is still a strong need for a `__experimental` prefix because otherwise it's hard to tell visually/programatically whether something is a locked prop.

Some kind of note in a JSDoc would be better than nothing, but I think we will run into a lot of confusion if the prop name isn't clearly prefixed. In fact, I'd even prefer a *different* prefix for locked experimental props vs. non-locked (grandfathered) experimental props, because as a consumer I need to know whether I'll have to do an explicit `unlock` to use it. And by "consumer" I am mostly talking about devs in this repo, so the "it's experimental so we don't have to document it, nobody should be using it in the first place" argument doesn't apply.

When somebody looks at the Storybook props table, it must be very clear that a particular prop is experimental and/or requires explicit unlocking.

#### noisysocks on 2023-01-30T00:56:38Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1090095576
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +258,16 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			__experimentalShowSelectedHint={ false }
+		/>
+	);
+}
+
+lock( CustomSelectControl, ExperimentalCustomSelectControl );
```

Fair enough. OK, putting back the `__experimental` prefix.

### Review thread 4 (resolved)

#### adamziel on 2023-01-18T10:57:12Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073386873
Path: packages/block-editor/src/hooks/position.js

```diff
@@ -255,7 +258,7 @@ export function PositionEdit( props ) {
 		web: (
 			<>
 				<BaseControl className="block-editor-hooks__position-selection">
-					<CustomSelectControl
+					<ExperimentalCustomSelectControl
```

nice!

### Review thread 5 (resolved)

#### adamziel on 2023-01-18T11:30:43Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1073418480
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

@noisysocks As I mentioned in the lock()/unlock() PR, there are ways to still get autocompletion in TypeScript. It's similar scenario as with getting autocompletion for `select( store )`. Here's the basic idea:

```ts
function unlock<T>( obj:any ) : T { /* ... */ }
unlock< ExperimentalComponent >( component );
```

Now, this is an additional burden on each usage and you may have to import the experimental component **type**. We can do better than that:

```ts
type Unlockable<Self, LockedType> = Self & { LockedType: LockedType };
type UnlockReturn< Arg > =
	Arg extends {LockedType: infer T}
		? T
		: Arg;

function unlock<T>( arg: T ) : UnlockReturn<T> { return {} as any; }

// Declare the experimental component with one signature
function experimentalComponent(experimentalArg: number) {
	return '2';
}

// Declare the public component with another signature
function publicComponent(publicArg: string) {
	return 1;
}

// Export the public component with experimental type signature embedded
const exportedComponent = publicComponent as Unlockable<typeof publicComponent, typeof experimentalComponent>;

// The public component is wrapped in Unlockable, but retains the original type signature and can be
// called.
const locked = exportedComponent;

// Unlocked has the experimental type signature:
const unlocked = unlock( exportedComponent );
```

[TS Playground](https://www.typescriptlang.org/play?#-cn9hQe+SM4CEC2UywUtzmaHOUGGPG+0n69Kgj4roYMGBIHQoCF2zROxAKFh9LGsRgQj5BfdtpYQodsxpc2r+cM9yhhlBUUGvwhcDoAA6UtcgrKBmnoDgKSjat3DoCd9mnRkQ3ZecuRmJoRD3VYMBveNR2pI5PUeZ483VcRQPLWMCmw6DaPQcoqxrBCA2lWpkODOdVCAA) 

`@wordpress/experiments` can be converted to TypeScript to express these types. The consumer packages would have to express the `as Unlockable` conversion with tsdoc annotations. Perhaps this would do the trick:

```
/**
  * @type {Unlockable<typeof publicComponent, typeof experimentalComponent>}
  */
const exportedComponent = publicComponent;
```

[It seems to work for simple types](https://www.typescriptlang.org/play?filetype=js#code/PQKhFgCgAIWgBALgTwA4FNoG8ByBXAWwCN0AnAXylmCgGMB7AOwGdFoAPaAXmgBYBuKFFAQYcJGkxYUGegDMOlMTUgMWbZN2gAiAIbbBkIA). CC @ciampo

#### noisysocks on 2023-01-19T00:54:00Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1080710244
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

Hmm, does exporting the experimental type in the package's type definitions not defeat the point of `lock()` which is that these experimental features should be hidden? We want to be able to make changes to the experimental type without much concern for backwards compatibility. Maybe it's fine—just thinking out loud.

At any rate, this isn't necessary for this PR. I only needed to add a very simple return type to `lock()` so that the generated type definitions for `@wordpress/experiments` weren't completely wrong. `CustomSelectControl` doesn't have types so `unlock( CustomSelectControl )` returning `any` isn't a problem.

It will be _eventually_ be a problem when we try to use these APIs on a typed component. I recommend we (you? 😛) open a separate PR that converts `@wordpress/experiments` to TypeScript and implements one of the approaches you describe above.

#### dmsnell on 2023-01-23T19:54:24Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1084483647
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

would it make sense to prefer typing these in the official types as `any`? anything that's experimental carries zero guarantees about the types

#### noisysocks on 2023-01-23T23:20:51Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1084671547
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

What do you mean? Update the JSDoc here to be `@return any`?

#### dmsnell on 2023-01-24T05:41:03Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1084832625
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

yeah, so `unlock()` specifically returns `any`, meaning any time we `const experimentalButton = unlock( ExperimentalButton )` we get `experimentalButton: any`

point is that we can be permissive in the types so that TypeScript doesn't tell people they are in error by using the experimental thing, but we'll preserve the unstable nature of it so that we don't accidentally lock-in a specific experimental type or thing when it might change before the types are updated in projects that use them.

#### noisysocks on 2023-01-25T03:25:55Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086172981
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

Makes sense—done.

#### mirka on 2023-01-25T11:07:32Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086500754
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

> point is that we can be permissive in the types so that TypeScript doesn't tell people they are in error by using the experimental thing, but we'll preserve the unstable nature of it so that we don't accidentally lock-in a specific experimental type or thing when it might change before the types are updated in projects that use them.

Assuming that the Gutenberg app will almost always use the experimental version of the component by definition, wouldn't this be a huge loss for devex and code quality in this repo? I also think accurate types are exactly what help devs identify issues when the API changes.

We are working towards providing first-party types with the components package (removing our reliance on DefinitelyTyped), which we're aiming to ship in the coming weeks. So I'm concerned that the Gutenberg app will not be able to benefit from a lot of this effort.

#### dmsnell on 2023-01-25T19:11:06Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1087070001
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

> Assuming that the Gutenberg app will almost always use the experimental version of the component by definition, wouldn't this be a huge loss for devex and code quality in this repo?

There's this inherent tension we've almost all been feeling with `__experimental`, which is that _intentionally_ the code is expected to change and break. I don't think the quality and devex we're looking for is there to begin with; otherwise they wouldn't be marked with the scary `__experimental`. In other words I think types on experimental code is a case of trying to have our cake and eat it too: we want stability so that consuming code doesn't break, while simultaneously we want the freedom to break things because we're not sure what the end design should look like yet.

We've had a number of cases where the inaccurate types (particularly on experimental code which has its abnormally high rate of churn) end up rejecting valid code. That's more frustrating to me at least than when TypeScript is unable to provide more assistance.

Once we move from experimental code to stable code, presumably we'll have stronger types on the stable code and get back those devex helps we wanted with the experimental code.

#### mirka on 2023-01-26T11:33:39Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1087729398
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

I'll leave it up to the app-side devs to decide which is preferable since it impacts them the most, but I just want to make sure the decision is made based on as much context as possible.

At least for the components package:

1. Forgoing type data for _the entire component_ just for one experimental prop seems extreme.
2. Inaccurate types are due to it being reliant on DefinitelyTyped. Once we have first-party types, the types should always be up to date, and if there's some kind of oversight it can/should easily be fixed in-repo. I've been working a lot on in-repo component TS for a while now (not DT types), and I've seen no indication that types for experimental props would be any less accurate than stable ones.

#### dmsnell on 2023-01-26T18:28:35Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1088203149
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

> Inaccurate types are due to it being reliant on DefinitelyTyped. Once we have first-party types, the types should always be up to date

This is something I don't think is actually as true as it seems like it obviously must be. For components it may be more true than for other things, but lots of the existing types come from incomplete or flat-out inaccurate JSDoc comments, the legacy of early-on favoring those over compiler-checked types.

> Forgoing type data for the entire component just for one experimental prop seems extreme.

It may not be trivial to know if it's just one experimental prop though, as we could also be exporting experimental versions of a component that remove props, or change props, or operate entirely differently.

And even if we _do_ have a situation where it's a single new prop, I think we still might have a type conundrum, because if we omit the experimental prop TypeScript will complain if people use the component, claiming "this attribute doesn't exit," but if we extend the types generically, as we're looking to do with `unlock`, we end up with messy types and in a lot of cases end up losing all the original props anyway because of things like `Props & Record<string, any>` widening to `Record<string, any>`.

I'm not strongly arguing that as a principle we should take away all constraints when people use experimental code; I'm merely trying to share that the effective downsides of dropping to `any` may not amount to what we think they are, and given the very frustrating experience and likelihood of TypeScript rejecting valid working code, the downsides of trying to retain the types here may practically far outweigh any gains we get from it.

#### noisysocks on 2023-01-30T00:55:18Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1090095276
Path: packages/experiments/src/implementation.js

```diff
@@ -38,6 +39,14 @@ const registeredExperiments = [];
 const requiredConsent =
 	'I know using unstable features means my plugin or theme will inevitably break on the next WordPress release.';
 
+/**
+ * Called by a @wordpress package wishing to opt-in to accessing or exposing
+ * private experimental APIs.
+ *
+ * @param {string} consent    The consent string.
+ * @param {string} moduleName The name of the module that is opting in.
+ * @return {{lock: Function, unlock: Function}} An object containing the lock and unlock functions.
```

@adamziel mentioned converting `@wordpress/experiments` into TypeScript. I imagine we can revisit this discussion then, yeah?

### Review thread 6 (resolved)

#### mirka on 2023-01-24T16:47:46Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1085605733
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +257,12 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+export default function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			showSelectedHint={ false }
+		/>
+	);
+}
```

Could this be the other way around, where the default export is the experimental version named simply `CustomSelectControl`, and the named export is called `StableCustomSelectControl`?

In wp-components internally we will almost never use the non-experimental version, so I think it would cause less disruption if it were named the other way around. It would be error-prone if we had to remember to do `import { ExperimentalCustomSelectControl as CustomSelectControl }` every time some component had a locked version.

#### noisysocks on 2023-01-25T03:26:28Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1086173142
Path: packages/components/src/custom-select-control/index.js

```diff
@@ -257,3 +257,12 @@ export default function CustomSelectControl( props ) {
 		</div>
 	);
 }
+
+export default function CustomSelectControl( props ) {
+	return (
+		<ExperimentalCustomSelectControl
+			{ ...props }
+			showSelectedHint={ false }
+		/>
+	);
+}
```

Done. We'll just have to be careful when exporting a component that `{ ...props }` into an experimental component.

### Review thread 7 (resolved)

#### ciampo on 2023-01-31T18:13:25Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1092313275
Path: packages/components/src/custom-select-control/test/index.js

```diff
@@ -4,9 +4,9 @@
 import { render, fireEvent, screen } from '@testing-library/react';
 
 /**
- * WordPress dependencies
+ * Internal dependencies
  */
-import { CustomSelectControl } from '@wordpress/components';
+import CustomSelectControl from '..';
```

Nice catch

### Review thread 8 (resolved)

#### ciampo on 2023-01-31T18:17:06Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1092316741
Path: packages/experiments/src/test/index.js

```diff
@@ -37,18 +37,6 @@ describe( '__dangerousOptInToUnstableAPIsOnlyForCoreModules', () => {
 			/This feature is only for JavaScript modules shipped with WordPress core/
 		);
 	} );
-	it( 'Should not register the same module twice', () => {
```

Why is this test not necessary anymore?

#### noisysocks on 2023-02-01T04:24:16Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1092751122
Path: packages/experiments/src/test/index.js

```diff
@@ -37,18 +37,6 @@ describe( '__dangerousOptInToUnstableAPIsOnlyForCoreModules', () => {
 			/This feature is only for JavaScript modules shipped with WordPress core/
 		);
 	} );
-	it( 'Should not register the same module twice', () => {
```

`IS_GUTENBERG_PLUGIN` is set when running unit tests so it fails because we've disabled the error in this case. We could mock unset `IS_GUTENBERG_PLUGIN`, but I'm not sure if that's right... the unit tests are supposed to test the _plugin_, right?

#### ciampo on 2023-02-01T10:22:17Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1093028160
Path: packages/experiments/src/test/index.js

```diff
@@ -37,18 +37,6 @@ describe( '__dangerousOptInToUnstableAPIsOnlyForCoreModules', () => {
 			/This feature is only for JavaScript modules shipped with WordPress core/
 		);
 	} );
-	it( 'Should not register the same module twice', () => {
```

Right, it makes sense. I guess we can bring it back if necessary, depending on the outcome of [this conversation](https://github.com/WordPress/gutenberg/pull/47229#discussion_r1093002091)

#### noisysocks on 2023-02-06T01:57:11Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1096877062
Path: packages/experiments/src/test/index.js

```diff
@@ -37,18 +37,6 @@ describe( '__dangerousOptInToUnstableAPIsOnlyForCoreModules', () => {
 			/This feature is only for JavaScript modules shipped with WordPress core/
 		);
 	} );
-	it( 'Should not register the same module twice', () => {
```

Now that we're using a different env variable we can restore this test, which I've done.

### Review thread 9 (resolved)

#### ciampo on 2023-02-03T16:00:05Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1095968632
Path: storybook/main.js

```diff
@@ -30,20 +28,11 @@ module.exports = {
 		emotionAlias: false,
 		storyStoreV7: true,
 	},
-	// Workaround:
-	// https://github.com/storybookjs/storybook/issues/12270
-	webpackFinal: async ( config ) => {
-		// Find the DefinePlugin.
-		const plugin = config.plugins.find( ( p ) => {
-			return p.definitions && p.definitions[ 'process.env' ];
-		} );
-		// Add custom env variables.
-		Object.keys( customEnvVariables ).forEach( ( key ) => {
-			plugin.definitions[ 'process.env' ][ key ] = JSON.stringify(
-				customEnvVariables[ key ]
-			);
-		} );
-
-		return config;
-	},
+	env: ( config ) => ( {
+		...config,
+		// Pretend we're in the Gutenberg plugin.
+		// Disable reason: We're defining IS_GUTENBERG_PLUGIN, not using it.
+		// eslint-disable-next-line @wordpress/is-gutenberg-plugin
+		IS_GUTENBERG_PLUGIN: true,
```

I can still see the error about registering a module more than once in Storybook:

![Screenshot 2023-02-03 at 16 59 24](https://user-images.githubusercontent.com/1083581/216649162-2b911571-75ad-4bdb-82e5-ad38911bcd51.png)


Should we set the `ALLOW_EXPERIMENT_REREGISTRATION` flag here as well? Is setting the `IS_GUTENBERG_PLUGIN` variable still necessary?

#### noisysocks on 2023-02-06T01:56:54Z

URL: https://github.com/WordPress/gutenberg/pull/47229#discussion_r1096876910
Path: storybook/main.js

```diff
@@ -30,20 +28,11 @@ module.exports = {
 		emotionAlias: false,
 		storyStoreV7: true,
 	},
-	// Workaround:
-	// https://github.com/storybookjs/storybook/issues/12270
-	webpackFinal: async ( config ) => {
-		// Find the DefinePlugin.
-		const plugin = config.plugins.find( ( p ) => {
-			return p.definitions && p.definitions[ 'process.env' ];
-		} );
-		// Add custom env variables.
-		Object.keys( customEnvVariables ).forEach( ( key ) => {
-			plugin.definitions[ 'process.env' ][ key ] = JSON.stringify(
-				customEnvVariables[ key ]
-			);
-		} );
-
-		return config;
-	},
+	env: ( config ) => ( {
+		...config,
+		// Pretend we're in the Gutenberg plugin.
+		// Disable reason: We're defining IS_GUTENBERG_PLUGIN, not using it.
+		// eslint-disable-next-line @wordpress/is-gutenberg-plugin
+		IS_GUTENBERG_PLUGIN: true,
```

Strangely, I wasn't seeing that error locally. But I agree with you! Fixed in [22bd457](https://github.com/WordPress/gutenberg/pull/47229/commits/22bd4575b7e99350cf4ffdac97e5bd58a774282e).

## Changed files

- package-lock.json (MODIFIED, +1/-0)
- package.json (MODIFIED, +2/-1)
- packages/block-editor/src/components/block-tools/selected-block-popover.js (MODIFIED, +1/-1)
- packages/block-editor/src/components/off-canvas-editor/block-contents.js (MODIFIED, +1/-1)
- packages/block-editor/src/components/provider/index.js (MODIFIED, +1/-1)
- packages/block-editor/src/experiments.js (MODIFIED, +1/-11)
- packages/block-editor/src/hooks/dimensions.js (MODIFIED, +1/-1)
- packages/block-editor/src/hooks/position.js (MODIFIED, +7/-1)
- packages/block-editor/src/lock-unlock.js (ADDED, +10/-0)
- packages/block-editor/src/store/index.js (MODIFIED, +1/-1)
- packages/block-editor/tsconfig.json (MODIFIED, +1/-0)
- packages/block-library/tsconfig.json (MODIFIED, +1/-0)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/components/src/custom-select-control/index.js (MODIFIED, +9/-0)
- packages/components/src/custom-select-control/stories/index.js (MODIFIED, +1/-1)
- packages/components/src/custom-select-control/test/index.js (MODIFIED, +2/-2)
- packages/components/src/experiments.js (ADDED, +20/-0)
- packages/components/src/index.js (MODIFIED, +4/-1)
- packages/components/tsconfig.json (MODIFIED, +1/-0)
- packages/data/tsconfig.json (MODIFIED, +1/-0)
- packages/experiments/package.json (MODIFIED, +1/-0)
- packages/experiments/src/implementation.js (MODIFIED, +35/-8)
- packages/experiments/tsconfig.json (ADDED, +9/-0)
- storybook/main.js (MODIFIED, +6/-18)
- test/native/setup.js (MODIFIED, +8/-0)
- test/unit/config/gutenberg-env.js (RENAMED, +7/-0)
- test/unit/jest.config.js (MODIFIED, +1/-1)
- tools/webpack/shared.js (MODIFIED, +3/-0)
- tsconfig.json (MODIFIED, +2/-0)
- typings/gutenberg-env/index.d.ts (MODIFIED, +2/-0)
