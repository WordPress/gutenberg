---
number: 8189
type: pull_request
state: MERGED
merged_at: 2018-08-14T04:48:52Z
url: https://github.com/WordPress/gutenberg/pull/8189
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Breaking Change","[Package] Components"]
---

# PR #8189: Components: Deprecate `withContext` HOC and remove its usage

- URL: https://github.com/WordPress/gutenberg/pull/8189
- Author: gziolo
- Created: 2018-07-25T10:27:33Z
- Updated: 2018-08-14T04:48:55Z
- Comments: 3 of 3
- Review threads: 8 of 8
- Changed files: 8 of 8
- Diff: ../pr-diffs/8189.diff

## Body

## Description
It's part of our efforts to stablize API.

## How has this been tested?
`npm test`

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->

## Issue comments

### gziolo on 2018-08-06T10:20:56Z

URL: https://github.com/WordPress/gutenberg/pull/8189#issuecomment-410661376

Almost there, I made all the tests pass, which is very promising. When comparing the current implementation with `react-dom` I came up with a conclusion that it will fail when there are multiple providers used. I added a unit test which indeed fails. It might be a more complex refactor though. Any ideas how to fix it with the minimal amount of work? 😄

### gziolo on 2018-08-09T13:55:16Z

URL: https://github.com/WordPress/gutenberg/pull/8189#issuecomment-411765999

I took advantage of the fact we use recursion and add another param which transports values from the providers created with new Context API: https://github.com/WordPress/gutenberg/pull/8189/commits/88e833cd12ea71c8e0cf1a2c9d03159085852e7a

It needs some polishing but it solves all the issues we had.

### gziolo on 2018-08-13T07:23:00Z

URL: https://github.com/WordPress/gutenberg/pull/8189#issuecomment-412430189

I pushed two more commits:
- fix for the issues raised with the recent review iteration (b6b7da7)
- one more tests which ensures we can properly serialize nested providers (https://github.com/WordPress/gutenberg/pull/8189/commits/e32503efc6462a101e44e3adefae75662dd876f7)

## Review threads

### Review thread 1

#### gziolo on 2018-07-25T10:29:34Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r205059104
Path: packages/element/src/serialize.js

```diff
@@ -445,6 +445,8 @@ export function renderElement( element, context = {} ) {
 				},
 				context
 			);
+		default:
+			throw new Error( tagName );
```

I couldn' make it throw so I figured out we don't use the source code in all cases. It turned out it is even worse, we don't use in a majority of cases. I opened #8188 to fix it before I can continue debugging.

### Review thread 2 (resolved)

#### gziolo on 2018-08-06T10:11:46Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r207839285
Path: packages/components/src/higher-order/with-context/index.js

```diff
@@ -8,9 +8,17 @@ import { noop } from 'lodash';
  */
 import { Component } from '@wordpress/element';
 import { createHigherOrderComponent } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
 
 export default ( contextName ) => ( mapSettingsToProps ) => createHigherOrderComponent(
 	( OriginalComponent ) => {
+		deprecated( 'wp.components.withContext', {
+			version: '3.6',
```

Note to myself: `3.7`.

### Review thread 3

#### gziolo on 2018-08-06T10:13:20Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r207839694
Path: packages/element/src/test/serialize.js

```diff
@@ -254,12 +256,82 @@ describe( 'renderElement()', () => {
 		expect( result ).toBe( 'Hello' );
 	} );
 
+	it( 'renders StrictMode with undefined children', () => {
+		const result = renderElement( <StrictMode /> );
+
+		expect( result ).toBe( '' );
+	} );
+
+	it( 'renders StrictMode as its inner children', () => {
+		const result = renderElement( <StrictMode>Hello</StrictMode> );
+
+		expect( result ).toBe( 'Hello' );
+	} );
+
 	it( 'renders Fragment with undefined children', () => {
 		const result = renderElement( <Fragment /> );
 
 		expect( result ).toBe( '' );
 	} );
 
+	it( 'renders default value from Context API', () => {
+		const { Consumer } = createContext( {
+			value: 'default',
+		} );
+
+		const result = renderElement(
+			<Consumer>
+				{ ( context ) => context.value }
+			</Consumer>
+		);
+
+		expect( result ).toBe( 'default' );
+	} );
+
+	it( 'renders provided value through Context API', () => {
+		const { Consumer, Provider } = createContext( {
+			value: 'default',
+		} );
+
+		const result = renderElement(
+			<Provider value={ { value: 'provided' } }>
+				<Consumer>
+					{ ( context ) => context.value }
+				</Consumer>
+			</Provider>
+		);
+
+		expect( result ).toBe( 'provided' );
+	} );
+
+	it( 'renders proper value through Context API when multiple providers present', () => {
+		const { Consumer, Provider } = createContext( {
+			value: 'default',
+		} );
+
+		const result = renderElement(
+			<Fragment>
+				<Provider value={ { value: '1st provided' } }>
+					<Consumer>
+						{ ( context ) => context.value }
+					</Consumer>
+				</Provider>
+				{ '|' }
+				<Provider value={ { value: '2nd provided' } }>
+					<Consumer>
+						{ ( context ) => context.value }
+					</Consumer>
+				</Provider>
+				{ '|' }
+				<Consumer>
```

It fails here because the mutation present in here: https://github.com/WordPress/gutenberg/pull/8189/files#diff-a72c42fcaf462e92ab6cd7a52680df76R472

#### gziolo on 2018-08-13T07:25:51Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r209512207
Path: packages/element/src/test/serialize.js

```diff
@@ -254,12 +256,82 @@ describe( 'renderElement()', () => {
 		expect( result ).toBe( 'Hello' );
 	} );
 
+	it( 'renders StrictMode with undefined children', () => {
+		const result = renderElement( <StrictMode /> );
+
+		expect( result ).toBe( '' );
+	} );
+
+	it( 'renders StrictMode as its inner children', () => {
+		const result = renderElement( <StrictMode>Hello</StrictMode> );
+
+		expect( result ).toBe( 'Hello' );
+	} );
+
 	it( 'renders Fragment with undefined children', () => {
 		const result = renderElement( <Fragment /> );
 
 		expect( result ).toBe( '' );
 	} );
 
+	it( 'renders default value from Context API', () => {
+		const { Consumer } = createContext( {
+			value: 'default',
+		} );
+
+		const result = renderElement(
+			<Consumer>
+				{ ( context ) => context.value }
+			</Consumer>
+		);
+
+		expect( result ).toBe( 'default' );
+	} );
+
+	it( 'renders provided value through Context API', () => {
+		const { Consumer, Provider } = createContext( {
+			value: 'default',
+		} );
+
+		const result = renderElement(
+			<Provider value={ { value: 'provided' } }>
+				<Consumer>
+					{ ( context ) => context.value }
+				</Consumer>
+			</Provider>
+		);
+
+		expect( result ).toBe( 'provided' );
+	} );
+
+	it( 'renders proper value through Context API when multiple providers present', () => {
+		const { Consumer, Provider } = createContext( {
+			value: 'default',
+		} );
+
+		const result = renderElement(
+			<Fragment>
+				<Provider value={ { value: '1st provided' } }>
+					<Consumer>
+						{ ( context ) => context.value }
+					</Consumer>
+				</Provider>
+				{ '|' }
+				<Provider value={ { value: '2nd provided' } }>
+					<Consumer>
+						{ ( context ) => context.value }
+					</Consumer>
+				</Provider>
+				{ '|' }
+				<Consumer>
```

it's no longer an issue.

### Review thread 4

#### aduth on 2018-08-09T14:51:37Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r208960279
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -23,28 +30,43 @@ import { serialize } from '../api';
  * </BlockContentProvider>
  * ```
  */
-class BlockContentProvider extends Component {
-	getChildContext() {
-		const { innerBlocks } = this.props;
+const BlockContentProvider = ( { children, innerBlocks } ) => {
+	const BlockContent = () => {
+		// Value is an array of blocks, so defer to block serializer
+		const html = serialize( innerBlocks );
 
-		return {
-			BlockContent() {
-				// Value is an array of blocks, so defer to block serializer
-				const html = serialize( innerBlocks );
+		// Use special-cased raw HTML tag to avoid default escaping
+		return <RawHTML>{ html }</RawHTML>;
+	};
 
-				// Use special-cased raw HTML tag to avoid default escaping
-				return <RawHTML>{ html }</RawHTML>;
-			},
-		};
-	}
-
-	render() {
-		return this.props.children;
-	}
-}
-
-BlockContentProvider.childContextTypes = {
-	BlockContent: () => {},
+	return (
+		<Provider value={ { BlockContent } }>
```

We pass a new value each render. I suppose this isn't a huge issue since it's used only in the serialization step anyways, but why does the context need to be an object anyways? Why not just the `BlockContent` value directly?

### Review thread 5

#### aduth on 2018-08-09T14:52:30Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r208960736
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -23,28 +30,43 @@ import { serialize } from '../api';
  * </BlockContentProvider>
  * ```
  */
-class BlockContentProvider extends Component {
-	getChildContext() {
-		const { innerBlocks } = this.props;
+const BlockContentProvider = ( { children, innerBlocks } ) => {
+	const BlockContent = () => {
+		// Value is an array of blocks, so defer to block serializer
+		const html = serialize( innerBlocks );
 
-		return {
-			BlockContent() {
-				// Value is an array of blocks, so defer to block serializer
-				const html = serialize( innerBlocks );
+		// Use special-cased raw HTML tag to avoid default escaping
+		return <RawHTML>{ html }</RawHTML>;
+	};
 
-				// Use special-cased raw HTML tag to avoid default escaping
-				return <RawHTML>{ html }</RawHTML>;
-			},
-		};
-	}
-
-	render() {
-		return this.props.children;
-	}
-}
-
-BlockContentProvider.childContextTypes = {
-	BlockContent: () => {},
+	return (
+		<Provider value={ { BlockContent } }>
+			{ children }
+		</Provider>
+	);
 };
 
+/**
+ * A Higher Order Component used to inject BlockContent context to the
+ * wrapped component.
+ *
+ * @param {Function} mapContextToProps Function called on every context change,
+ *                                     expected to return object of props to
+ *                                     merge with the component's own props.
+ *
+ * @return {Component} Enhanced component with injected context as props.
+ */
+export const withBlockContentContext = ( mapContextToProps ) => createHigherOrderComponent( ( OriginalComponent ) => {
```

To my previous point, I don't see why we'd be passing anything other than the content component. We shouldn't need to map anything.

#### gziolo on 2018-08-09T14:55:09Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r208961873
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -23,28 +30,43 @@ import { serialize } from '../api';
  * </BlockContentProvider>
  * ```
  */
-class BlockContentProvider extends Component {
-	getChildContext() {
-		const { innerBlocks } = this.props;
+const BlockContentProvider = ( { children, innerBlocks } ) => {
+	const BlockContent = () => {
+		// Value is an array of blocks, so defer to block serializer
+		const html = serialize( innerBlocks );
 
-		return {
-			BlockContent() {
-				// Value is an array of blocks, so defer to block serializer
-				const html = serialize( innerBlocks );
+		// Use special-cased raw HTML tag to avoid default escaping
+		return <RawHTML>{ html }</RawHTML>;
+	};
 
-				// Use special-cased raw HTML tag to avoid default escaping
-				return <RawHTML>{ html }</RawHTML>;
-			},
-		};
-	}
-
-	render() {
-		return this.props.children;
-	}
-}
-
-BlockContentProvider.childContextTypes = {
-	BlockContent: () => {},
+	return (
+		<Provider value={ { BlockContent } }>
+			{ children }
+		</Provider>
+	);
 };
 
+/**
+ * A Higher Order Component used to inject BlockContent context to the
+ * wrapped component.
+ *
+ * @param {Function} mapContextToProps Function called on every context change,
+ *                                     expected to return object of props to
+ *                                     merge with the component's own props.
+ *
+ * @return {Component} Enhanced component with injected context as props.
+ */
+export const withBlockContentContext = ( mapContextToProps ) => createHigherOrderComponent( ( OriginalComponent ) => {
```

I did it for consistency with other context related HOCs. However, you made a good point. I will refactor as suggested.

### Review thread 6

#### aduth on 2018-08-09T15:34:29Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r208977260
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -1,20 +1,27 @@
 /**
  * WordPress dependencies
  */
-import { Component, RawHTML } from '@wordpress/element';
+import { createHigherOrderComponent } from '@wordpress/compose';
+import { createContext, RawHTML } from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
 import { serialize } from '../api';
 
+const { Consumer, Provider } = createContext( {
```

I think this still needs to be updated to not set the initial context as an object, but rather the noop directly.

#### gziolo on 2018-08-09T15:43:03Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r208980502
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -1,20 +1,27 @@
 /**
  * WordPress dependencies
  */
-import { Component, RawHTML } from '@wordpress/element';
+import { createHigherOrderComponent } from '@wordpress/compose';
+import { createContext, RawHTML } from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
 import { serialize } from '../api';
 
+const { Consumer, Provider } = createContext( {
```

Right, missed this one :(

### Review thread 7

#### aduth on 2018-08-09T15:35:24Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r208977635
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -1,20 +1,27 @@
 /**
  * WordPress dependencies
  */
-import { Component, RawHTML } from '@wordpress/element';
+import { createHigherOrderComponent } from '@wordpress/compose';
+import { createContext, RawHTML } from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
 import { serialize } from '../api';
 
+const { Consumer, Provider } = createContext( {
+	BlockContent: () => {},
+} );
+
 /**
  * An internal block component used in block content serialization to inject
  * nested block content within the `save` implementation of the ancestor
  * component in which it is nested. The component provides a pre-bound
  * `BlockContent` component via context, which is used by the developer-facing
  * `InnerBlocks.Content` component to render block content.
  *
+ * @return {WPElement} Element with BlockContent injected via context.
```

Minor: We might want to establish conventions around tag order. How I've treated it, I always have `@return` last, preceded by `@param`, preceded by anything else.

#### gziolo on 2018-08-13T07:12:08Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r209509443
Path: packages/blocks/src/block-content-provider/index.js

```diff
@@ -1,20 +1,27 @@
 /**
  * WordPress dependencies
  */
-import { Component, RawHTML } from '@wordpress/element';
+import { createHigherOrderComponent } from '@wordpress/compose';
+import { createContext, RawHTML } from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
 import { serialize } from '../api';
 
+const { Consumer, Provider } = createContext( {
+	BlockContent: () => {},
+} );
+
 /**
  * An internal block component used in block content serialization to inject
  * nested block content within the `save` implementation of the ancestor
  * component in which it is nested. The component provides a pre-bound
  * `BlockContent` component via context, which is used by the developer-facing
  * `InnerBlocks.Content` component to render block content.
  *
+ * @return {WPElement} Element with BlockContent injected via context.
```

Yes, makes sense. In general, we could make JSDoc validation more strict since we started using it more consistently.

### Review thread 8

#### aduth on 2018-08-13T13:28:50Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r209608165
Path: packages/components/src/higher-order/with-context/test/index.js

```diff
@@ -39,6 +39,7 @@ describe( 'withContext', () => {
 		);
 
 		expect( wrapper.root.findByType( 'div' ).children[ 0 ] ).toBe( 'ok' );
+		expect( console ).toHaveWarned();
```

Minor: Question came up from @danielbachhuber about testing with deprecated warnings, and potential for conflicts with its internal memoization. Made me think over the weekend that the fact we test against console logging is a bit of an abstraction piercing; while it seems obvious `deprecated` would log to console by its description, we can't know for certain, and seems we should test that `deprecated` was merely called, regardless of its internal implementation.

I'd like to think this would be as simple as? ...

```js
const deprecated = jest.mock( '@wordpress/deprecated' );

// ...

expect( deprecated ).toHaveBeenCalled();
```

#### gziolo on 2018-08-14T04:30:57Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r209825704
Path: packages/components/src/higher-order/with-context/test/index.js

```diff
@@ -39,6 +39,7 @@ describe( 'withContext', () => {
 		);
 
 		expect( wrapper.root.findByType( 'div' ).children[ 0 ] ).toBe( 'ok' );
+		expect( console ).toHaveWarned();
```

I think it should need:

```js
import deprecated from '@wordpress/deprecated';

jest.mock( '@wordpress/deprecated', () => jest.fn() );

expect( deprecated ).toHaveBeenCalled();
```

I can give it a try.

#### gziolo on 2018-08-14T04:31:57Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r209825799
Path: packages/components/src/higher-order/with-context/test/index.js

```diff
@@ -39,6 +39,7 @@ describe( 'withContext', () => {
 		);
 
 		expect( wrapper.root.findByType( 'div' ).children[ 0 ] ).toBe( 'ok' );
+		expect( console ).toHaveWarned();
```

Related docs: https://jestjs.io/docs/en/mock-functions#mocking-modules

#### gziolo on 2018-08-14T04:48:46Z

URL: https://github.com/WordPress/gutenberg/pull/8189#discussion_r209827594
Path: packages/components/src/higher-order/with-context/test/index.js

```diff
@@ -39,6 +39,7 @@ describe( 'withContext', () => {
 		);
 
 		expect( wrapper.root.findByType( 'div' ).children[ 0 ] ).toBe( 'ok' );
+		expect( console ).toHaveWarned();
```

Refactored in https://github.com/WordPress/gutenberg/pull/8189/commits/cf41540bed800c32db0105cad762c92af9a57ed4.

## Changed files

- docs/reference/deprecated.md (MODIFIED, +1/-0)
- packages/blocks/src/block-content-provider/index.js (MODIFIED, +37/-21)
- packages/blocks/src/index.js (MODIFIED, +1/-0)
- packages/components/src/higher-order/with-context/index.js (MODIFIED, +8/-0)
- packages/components/src/higher-order/with-context/test/index.js (MODIFIED, +8/-4)
- packages/editor/src/components/inner-blocks/index.js (MODIFIED, +4/-7)
- packages/element/src/serialize.js (MODIFIED, +72/-32)
- packages/element/src/test/serialize.js (MODIFIED, +97/-3)
