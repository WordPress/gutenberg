---
number: 53328
type: pull_request
state: MERGED
merged_at: 2023-08-09T04:07:39Z
url: https://github.com/WordPress/gutenberg/pull/53328
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","Needs Design","[Package] Components","has dev note"]
---

# PR #53328: Modal: add `headerActions` prop to render buttons in the header

- URL: https://github.com/WordPress/gutenberg/pull/53328
- Author: ramonjd
- Created: 2023-08-04T06:04:27Z
- Updated: 2023-10-12T22:04:37Z
- Comments: 18 of 18
- Review threads: 6 of 6
- Changed files: 6 of 6
- Diff: ../pr-diffs/53328.diff

## Body

## What?
Not sure about this one yet. I would value some input 🙇🏻 

This is an experiment to add a new prop `headerActions: ReacNode` to the `<Modal />` component. 

The prop allows folks to inject buttons and other elements into the header.


## Why?
See: https://github.com/WordPress/gutenberg/issues/53258#issuecomment-1663326667

Some folks working in the classic block require things to be roomier, and it'd be nice to be able to toggle fullscreen mode in the modal. Why? There might be a lot of complex content in there, for which it would be helpful to have more real estate to work.

## How?
Adding a new prop.

## TODO
- [x] add stories

## Testing Instructions

A good place to test this is by applying the new prop to the Class block modal:

<details>

<summary>Example diff</summary>

```diff
diff --git a/packages/block-library/src/freeform/editor.scss b/packages/block-library/src/freeform/editor.scss
index a3be7ffc5e..c09cf9aace 100644
--- a/packages/block-library/src/freeform/editor.scss
+++ b/packages/block-library/src/freeform/editor.scss
@@ -376,14 +376,16 @@ div[data-type="core/freeform"] {
 }
 
 .block-editor-freeform-modal {
-	.components-modal__frame {
+	.block-editor-freeform-modal__content {
 		// On large screens, make the TinyMCE edit area grow to take all the
 		// available height so that the Cancel/Save buttons are always into the
 		// view. On smaller screens, the modal content is scrollable.
 		@include break-large() {
 			// On medium and large screens, the modal component sets a max-height.
 			// We want the modal to be as tall as possible also when the content is short.
-			height: 9999rem;
+			&:not(.is-full-screen) {
+				height: 9999rem;
+			}
 
 			.components-modal__header + div {
 				height: 100%;
diff --git a/packages/block-library/src/freeform/modal.js b/packages/block-library/src/freeform/modal.js
index 9f7b20460c..61a6b89415 100644
--- a/packages/block-library/src/freeform/modal.js
+++ b/packages/block-library/src/freeform/modal.js
@@ -13,6 +13,29 @@ import {
 import { useEffect, useState, RawHTML } from '@wordpress/element';
 import { __ } from '@wordpress/i18n';
 import { useSelect } from '@wordpress/data';
+import { fullscreen } from '@wordpress/icons';
+import { useViewportMatch } from '@wordpress/compose';
+
+function ModalAuxiliaryActions( { onClick, isModalFullScreen } ) {
+	// 'small' to match the rules in editor.scss.
+	const isMobileViewport = useViewportMatch( 'small', '<' );
+	if ( isMobileViewport ) {
+		return null;
+	}
+
+	return (
+		<Button
+			onClick={ onClick }
+			icon={ fullscreen }
+			isPressed={ isModalFullScreen }
+			label={
+				isModalFullScreen
+					? __( 'Exit fullscreen' )
+					: __( 'Enter fullscreen' )
+			}
+		/>
+	);
+}
 
 function ClassicEdit( props ) {
 	const styles = useSelect(
@@ -58,6 +81,7 @@ export default function ModalEdit( props ) {
 		onReplace,
 	} = props;
 	const [ isOpen, setOpen ] = useState( false );
+	const [ isModalFullScreen, setIsModalFullScreen ] = useState( true );
 	const id = `editor-${ clientId }`;
 
 	const onClose = () => ( content ? setOpen( false ) : onReplace( [] ) );
@@ -78,6 +102,16 @@ export default function ModalEdit( props ) {
 					onRequestClose={ onClose }
 					shouldCloseOnClickOutside={ false }
 					overlayClassName="block-editor-freeform-modal"
+					isFullScreen={ isModalFullScreen }
+					className="block-editor-freeform-modal__content"
+					headerActions={
+						<ModalAuxiliaryActions
+							onClick={ () =>
+								setIsModalFullScreen( ! isModalFullScreen )
+							}
+							isModalFullScreen={ isModalFullScreen }
+						/>
+					}
 				>
 					<ClassicEdit id={ id } defaultValue={ content } />
 					<Flex

```


</details>

Also checkout the Story Book variation: `npm run storybook:dev`



## Screenshots or screencast <!-- if applicable -->



https://github.com/WordPress/gutenberg/assets/6458278/31076ec3-f07f-41ae-ace3-39586be7301e



https://github.com/WordPress/gutenberg/assets/6458278/f9422bda-f795-410f-acde-785bcb1463af


## ✍️  Dev note

Thanks to a new headerActions prop, consumers of Modal can inject buttons (and other elements) into the Modal's header, next to the close button.

## Issue comments

### t-hamano on 2023-08-04T13:28:29Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1665609379

Thanks for the review, @ramonjd!

> I've pulled the icons from the available ones in the library. They don't _quite_ work. I think we need a minimize icon (or the opposite of `resizeCornerNE`)?

How about using only the `fullscreen` icon and toggling the `isPressed` prop?

![is-pressed](https://github.com/WordPress/gutenberg/assets/54422211/5c9776c2-1c13-49ab-96b0-fcd79eb9b0f7)

Then, it might be better to have a gap of about 8px between the buttons:

![gap](https://github.com/WordPress/gutenberg/assets/54422211/af912ee0-b68f-49f5-a00e-aa438743bb96)

Also, when in mobile view, the modal is always in full screen, so it is better to hide this button

![mobile](https://github.com/WordPress/gutenberg/assets/54422211/23ba7f73-a02a-4240-99b2-8328b7977b61)

Another approach is to make the Classic Editor modal full-screen from the start, instead of switching to full-screen status. When a user presses the Edit button in the Classic block, would they be surprised to see a large modal? 🤔 I would like to hear other opinions on this point.

### ramonjd on 2023-08-06T00:27:30Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1666653787

> Are we expecting this "full screen mode" modal to have different styling, depending on the use case? I'm asking because in the example, the styling is added via the block-library package.

I suppose that's a possibility. The freeform block does overwrite the modal styles (`.components-modal__frame`) at the moment so I expect it would be more sustainable if it didn't have to.

> We either want the fullscreen styling built into the Modal component itself, or else support this feature in a more generic way. For example, we could enhance Modal so that a consumer can inject auxiliary buttons next to the Close button. Then, the Freeform block would inject a Fullscreen button, and do its own styling via the className prop.

Oh yeah, that's an interesting idea. Thanks! I wasn't too sure about this one when I pushed it so I'll play around. 🙇🏻 


> How about using only the fullscreen icon and toggling the isPressed prop?
> Also, when in mobile view, the modal is always in full screen, so it is better to hide this button

Good ideas, thank you!

### ramonjd on 2023-08-07T01:05:51Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1667039533

> For example, we could enhance Modal so that a consumer can inject auxiliary buttons next to the Close button

I was thinking about this. What's the preferred way of defining these props? Do we need to be strict about what is passed? 

I had started with a simple `ReactNode` for maximum flexibility, which would mean users could inject anything. Example:

```jsx
<Modal
   auxiliaryActions={ <ButtonGroup /> }
/>
```

But I suppose there's also the option of reining it into an array of `buttonProps` objects each of which would render a `<Button ... />`. 

```jsx
<Modal
   auxiliaryActions={ [
       {
          ...someButtonProps,
       }
    ] }
/>
```

### ramonjd on 2023-08-07T01:42:05Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1667062216

> I had started with a simple ReactNode for maximum flexibility, which would mean users could inject anything

I've done this for now, just as a placeholder. Happy to implement further advice/ideas. Thank you @t-hamano and @mirka !!

### andrewserong on 2023-08-07T01:43:29Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1667063096

> I had started with a simple ReactNode for maximum flexibility, which would mean users could inject anything.

With `Modal` being fairly generic, perhaps allowing folks to inject anything might be preferable?

### ramonjd on 2023-08-07T01:44:35Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1667063989

I've added a TODO to add a story once we've decided on an approach. 👍🏻

### ramonjd on 2023-08-08T06:55:32Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1669013908

> I don't feel too strongly about it, but I was wondering if secondaryActions might work? Mostly because auxiliary can be hard to spell

Thanks @andrewserong 

You know, I probably had to fix my own typos about 20 times when typing it out. 😆 

I'm also agnostic here. 'complementary'?

### andrewserong on 2023-08-08T07:00:12Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1669019614

> I'm also agnostic here. 'complementary'?

Oh, that could work, too. And I only see [one typo for `complimentary`](https://github.com/WordPress/gutenberg/blob/9534ac6733f4abc9162eb704f2cbfbe051ce9e1a/packages/interface/src/components/complementary-area/README.md#L8) in the repo 😁. The words `auxiliary`, `secondary`, and `complementary` appear to be used in at least one other place in the repo, so all sound like reasonable options to me. Let's see if @mirka or anyone else have any preferences 😄

### ramonjd on 2023-08-08T08:13:13Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1669131284

> And I only see [one typo for complimentary](https://github.com/WordPress/gutenberg/blob/9534ac6733f4abc9162eb704f2cbfbe051ce9e1a/packages/interface/src/components/complementary-area/README.md#L8) in the repo 😁.

Nice find! 

I couldn't help myself 😄 

- https://github.com/WordPress/gutenberg/pull/53413

### ramonjd on 2023-08-09T00:16:35Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1670474946

`headerActions` was suggested as an alternative. I like it. I'll update it to that and apologize later 😄

### noisysocks on 2023-08-09T00:16:41Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1670475007

I like `actions` or `headerActions` as a name because tertiary, secondary, auxiliary, etc. are a bit too abstract for my dumb brain.

Just flagging hat we have [`InspectorPopoverHeader`](https://github.com/WordPress/gutenberg/tree/trunk/packages/block-editor/src/components/inspector-popover-header) which I added as a separate component because I wasn't sure back then whether we'd want something like this for **all** modals. How do you feel about deprecating/removing `InspectorPopverHeader` as part of this?

### ramonjd on 2023-08-09T00:21:26Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1670478347

> I like actions or headerActions as a name because tertiary, secondary, auxiliary, etc. are a bit too abstract for my dumb brain.

You got it!!! 👍🏻 

> Just flagging hat we have [InspectorPopoverHeader](https://github.com/WordPress/gutenberg/tree/trunk/packages/block-editor/src/components/inspector-popover-header) which I added as a separate component because I wasn't sure back then whether we'd want something like this for all modals. How do you feel about deprecating/removing InspectorPopverHeader as part of this?

If you think we should, for sure. 

I can do a follow up PR because it might involve extricating it from the `PublishDateTimePicker`

https://github.com/WordPress/gutenberg/blob/63b1f40e10281b8dfa899bb5a48887cacf361f16/packages/block-editor/src/components/publish-date-time-picker/index.js#L12

### noisysocks on 2023-08-09T00:38:33Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1670489477

True. I guess those components need to contain the `Popover`. A little bit of refactoring required.

### ramonjd on 2023-08-09T02:01:36Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1670545861

Thanks for re-testing @andrewserong 🙇🏻 

I'll let this PR stew overnight in case there's further feedback and merge tomorrow.

### ramonjd on 2023-08-09T04:18:09Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1670627409

> True. I guess those components need to contain the Popover. A little bit of refactoring required.

Looking at this a bit more closely, `InspectorPopverHeader` is used in other contexts, e.g., the `<Dropdown />` popover in the [post template controls](https://github.com/WordPress/gutenberg/blob/717776157f0cee73e9a9f9366817e0c539054491/packages/edit-post/src/components/sidebar/post-template/index.js) and [page panel status](https://github.com/WordPress/gutenberg/blob/948b8efcfd43b301ff89ca268b01cd3c5977163d/packages/edit-site/src/components/sidebar-edit-mode/page-panels/page-status.js#L181). It still seems useful in those contexts and not sure there's a specific crossover with the `<Modal />` component.

### noisysocks on 2023-08-10T00:00:57Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1672335787

Oh right I've mixed `Modal` and `Popover` up. I still think it might make sense for `Modal`, `Popover` and `Dropdown` to all have a `headerActions` prop which works consistently.

### ciampo on 2023-10-12T19:27:44Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1760245333

Added a dev note

### ramonjd on 2023-10-12T22:04:36Z

URL: https://github.com/WordPress/gutenberg/pull/53328#issuecomment-1760437333

Thanks @ciampo 🙇🏻

## Review threads

### Review thread 1 (resolved)

#### andrewserong on 2023-08-07T01:44:15Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1285303894
Path: packages/components/src/modal/index.tsx

```diff
@@ -257,6 +258,7 @@ function UnforwardedModal(
 										</h1>
 									) }
 								</div>
+								{ auxiliaryActions }
```

I like this, keeps the injection nice and simple 👍

### Review thread 2 (resolved)

#### andrewserong on 2023-08-08T06:32:10Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1286654125
Path: packages/components/CHANGELOG.md

```diff
@@ -4,14 +4,13 @@
 
 ### New Feature
 
--   Add a new `ProgressBar` component. ([#53030](https://github.com/WordPress/gutenberg/pull/53030)).
-
```

Looks like this removal might be accidental via a rebase?

#### ramonjd on 2023-08-08T06:50:02Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1286669774
Path: packages/components/CHANGELOG.md

```diff
@@ -4,14 +4,13 @@
 
 ### New Feature
 
--   Add a new `ProgressBar` component. ([#53030](https://github.com/WordPress/gutenberg/pull/53030)).
-
```

Oh dang, thanks for picking that up. Yeah, dodgy rebase 🤦🏻‍♂️

### Review thread 3 (resolved)

#### andrewserong on 2023-08-09T01:04:51Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1287823914
Path: packages/components/src/modal/README.md

```diff
@@ -194,6 +194,13 @@ If this property is true, it will focus the first tabbable element rendered in t
 -   Required: No
 -   Default: `true`
 
+#### headerActions
+
+An optional React node intended to contain additional actions or other elements related the modal, for example, buttons. Content is rendered in the top right corner of the modal and to the left of the close button, if visible.
```

Tiny nit:

```suggestion
An optional React node intended to contain additional actions or other elements related to the modal, for example, buttons. Content is rendered in the top right corner of the modal and to the left of the close button, if visible.
```

#### ramonjd on 2023-08-09T01:12:26Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1287827348
Path: packages/components/src/modal/README.md

```diff
@@ -194,6 +194,13 @@ If this property is true, it will focus the first tabbable element rendered in t
 -   Required: No
 -   Default: `true`
 
+#### headerActions
+
+An optional React node intended to contain additional actions or other elements related the modal, for example, buttons. Content is rendered in the top right corner of the modal and to the left of the close button, if visible.
```

Whoops. Thanks for the proof read 🙇🏻

### Review thread 4 (resolved)

#### mirka on 2023-08-09T02:48:03Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1287872316
Path: packages/components/src/modal/stories/index.tsx

```diff
@@ -98,3 +150,17 @@ Default.parameters = {
 		},
 	},
 };
+
+export const WithHeaderActions: ComponentStory< typeof Modal > =
+	TemplateWithHeaderActions.bind( {} );
+WithHeaderActions.args = {
+	...Default.args,
+};
+WithHeaderActions.argTypes = {
+	isDismissible: {
+		control: { type: 'boolean' },
+	},
```

Let's put this up in the main `meta` object at the top of the file, as it's not exclusive to this story.

### Review thread 5 (resolved)

#### mirka on 2023-08-09T02:50:55Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1287873564
Path: packages/components/src/modal/stories/index.tsx

```diff
@@ -87,6 +89,56 @@ const Template: ComponentStory< typeof Modal > = ( {
 	);
 };
 
+const TemplateWithHeaderActions: ComponentStory< typeof Modal > = ( {
+	onRequestClose,
+	...args
+} ) => {
+	const [ isLiked, setIsLiked ] = useState( false );
+	const [ isOpen, setOpen ] = useState( false );
+	const openModal = () => setOpen( true );
+	const closeModal: ModalProps[ 'onRequestClose' ] = ( event ) => {
+		setOpen( false );
+		onRequestClose( event );
+	};
+	const headerActions = (
+		<>
+			<Button
+				icon={ isLiked ? starFilled : starEmpty }
+				label="Like"
+				onClick={ () => setIsLiked( ! isLiked ) }
+			/>
+			<Tooltip text="We are here to help!">
+				<Button icon={ help } label="Help" />
+			</Tooltip>
+		</>
+	);
+
+	return (
+		<>
+			<Button variant="secondary" onClick={ openModal }>
+				Open Modal with Header Actions
+			</Button>
+			{ isOpen && (
+				<Modal
+					style={ { maxWidth: '600px' } }
+					isDismissible={ false }
+					onRequestClose={ closeModal }
+					headerActions={ headerActions }
+					{ ...args }
+				>
+					<p>
+						Lorem ipsum dolor sit amet, consectetur adipiscing elit,
+						sed do eiusmod tempor incididunt ut labore et magna
+						aliqua.
+					</p>
+					<Button variant="secondary" onClick={ closeModal }>
+						Close Modal
+					</Button>
+				</Modal>
+			) }
+		</>
+	);
+};
```

Could we simplify this to reduce maintenance cost? I'm thinking something like this would suffice:

```tsx
const LikeButton = () => {
	const [ isLiked, setIsLiked ] = useState( false );
	return (
		<Button
			icon={ isLiked ? starFilled : starEmpty }
			label="Like"
			onClick={ () => setIsLiked( ! isLiked ) }
		/>
	);
};

export const WithHeaderActions: ComponentStory< typeof Modal > = Template.bind(
	{}
);
WithHeaderActions.args = {
	...Default.args,
	headerActions: <LikeButton />,
	isDismissible: false,
};
```

#### ramonjd on 2023-08-09T03:12:33Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1287883451
Path: packages/components/src/modal/stories/index.tsx

```diff
@@ -87,6 +89,56 @@ const Template: ComponentStory< typeof Modal > = ( {
 	);
 };
 
+const TemplateWithHeaderActions: ComponentStory< typeof Modal > = ( {
+	onRequestClose,
+	...args
+} ) => {
+	const [ isLiked, setIsLiked ] = useState( false );
+	const [ isOpen, setOpen ] = useState( false );
+	const openModal = () => setOpen( true );
+	const closeModal: ModalProps[ 'onRequestClose' ] = ( event ) => {
+		setOpen( false );
+		onRequestClose( event );
+	};
+	const headerActions = (
+		<>
+			<Button
+				icon={ isLiked ? starFilled : starEmpty }
+				label="Like"
+				onClick={ () => setIsLiked( ! isLiked ) }
+			/>
+			<Tooltip text="We are here to help!">
+				<Button icon={ help } label="Help" />
+			</Tooltip>
+		</>
+	);
+
+	return (
+		<>
+			<Button variant="secondary" onClick={ openModal }>
+				Open Modal with Header Actions
+			</Button>
+			{ isOpen && (
+				<Modal
+					style={ { maxWidth: '600px' } }
+					isDismissible={ false }
+					onRequestClose={ closeModal }
+					headerActions={ headerActions }
+					{ ...args }
+				>
+					<p>
+						Lorem ipsum dolor sit amet, consectetur adipiscing elit,
+						sed do eiusmod tempor incididunt ut labore et magna
+						aliqua.
+					</p>
+					<Button variant="secondary" onClick={ closeModal }>
+						Close Modal
+					</Button>
+				</Modal>
+			) }
+		</>
+	);
+};
```

Yeah it is a bit verbose, will tidy it up. Thank you!

### Review thread 6

#### mirka on 2023-08-09T02:55:22Z

URL: https://github.com/WordPress/gutenberg/pull/53328#discussion_r1287875630
Path: packages/components/src/modal/README.md

```diff
@@ -194,6 +194,13 @@ If this property is true, it will focus the first tabbable element rendered in t
 -   Required: No
 -   Default: `true`
 
+#### headerActions
```

I think this name is good too 👍 We should definitely have "header" in the name. For a moment I thought maybe "extras" instead of "actions" would be more accurately generic. But on the other hand I think it's good to restrict/suggest that it's intended for button-ish actions, just for the sake of UI design consistency.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/modal/README.md (MODIFIED, +7/-0)
- packages/components/src/modal/index.tsx (MODIFIED, +2/-0)
- packages/components/src/modal/stories/index.tsx (MODIFIED, +27/-0)
- packages/components/src/modal/test/index.tsx (MODIFIED, +14/-0)
- packages/components/src/modal/types.ts (MODIFIED, +8/-0)
