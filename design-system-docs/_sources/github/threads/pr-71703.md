---
number: 71703
type: pull_request
state: MERGED
merged_at: 2025-10-24T02:36:07Z
url: https://github.com/WordPress/gutenberg/pull/71703
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components","Global Styles","[Feature] Typography","Backported to WP Core"]
---

# PR #71703: Fix selection of font size presets if presets have the same `size` value

- URL: https://github.com/WordPress/gutenberg/pull/71703
- Author: luminuu
- Created: 2025-09-16T16:17:56Z
- Updated: 2025-10-24T02:37:31Z
- Comments: 25 of 25
- Review threads: 18 of 18
- Changed files: 14 of 14
- Diff: ../pr-diffs/71703.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- Link this PR to its associated issue with an appropriate keyword: Closes, See, Follow up to, etc. -->
Closes #42683
Closes #64760
Closes #71510
Closes #52191
Closes #69939

<!-- In a few words, what is the PR actually doing? -->

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
This PR solves multiple issues regarding the selection of font size preset, if those presets have the same size. Right now selecting a font size preset in the editor will result in always falling back to the first item in the list that has the exact same `size` set.

Even if a user sets custom fluid values, the original `size` value is stored and used as a selection reference. This causes issues in the user experience of the custom fluid values not working correctly as they just can not be selected, if the size value (which will be disabled) is not changed before entering custom fluid values.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
This PR introduces a `selectedSlug` variable that is used in every relevant spot to hold the actual selection, instead of falling back on a specific `size` that can be added multiple times through the font size presets interface. 

The code fixes this issue in both selection variations: the horizontal S - M - L - XL - XXL selection like in Twenty Twenty-Five, as well as in the dropdown, if more font size presets exist than the horizontal selection can show.

I also went ahead on updating the display of the font size preset name and actual value in the dropdown, despite the issue is waiting for more Design feedback. The reason for this is I found that when not adjusting this, if the preset name was short (like `Small`) and it had a fixed value, it would all be in one line. Together with items that break into two lines, this would have looked confusing, as information for different items have showed differently. I went ahead and added feedback by @karmatosed already, but this can be discussed here too.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->
Depending on the theme it's tested and optionally additional font size presets added, both variations can be tested:

### Horizontal view
<img width="550" height="250" alt="CleanShot 2025-09-16 at 17 57 07" src="https://github.com/user-attachments/assets/3752b34a-6872-472d-864e-0a3e4b09b12b" />

1. This can be tested with Twenty Twenty-Five without any additional font size presets added.
2. Go to Styles -> Typography -> Font size presets.
3. You can pick any existing font size. Small has a fixed value, so you can edit for example Medium, by deactivating both toggles and adding `0.875rem` as font size.
4. Save settings.
5. Go to any paragraph or heading and select the font size preset. Without this PR, you should not be able to select it, with this PR, both original and updated selections are possible.

### Dropdown view
<img width="554" height="878" alt="CleanShot 2025-09-16 at 18 04 40" src="https://github.com/user-attachments/assets/da04aa22-8243-4f0e-9de4-545f6f4d17a5" />

1. In order to test this view, you need to add at least one additional font size preset, two to make it easier testing.
2. Go to Styles -> Typography -> Font size presets.
3. Add a new custom font size with a fixed value of `1rem` for example.
4. Add a second custom font size with adding `1rem` in the size input as well, before activating both of the fluid toggles and adding `1rem` and `3rem` as respective values.
5. Save settings.
6. Go to any paragraph or heading and select the second font size with the fluid values added. It should stay selected and not fall back on the first custom font size with the fixed value. 

## Screenshots or screencast <!-- if applicable -->

<!-- If you would like to upload screenshots, feel free to use the table below when it is useful to show the difference between before and after the change. -->

The screencasts only show the dropdown version.

### Before

https://github.com/user-attachments/assets/8589da36-7ea8-47e5-9371-043c182a0478


### After
https://github.com/user-attachments/assets/7588c5dd-3e32-483c-a0d4-648def30ca7e

## Issue comments

### github-actions on 2025-09-16T16:18:07Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3299479622

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: luminuu <luminuu@git.wordpress.org>
Co-authored-by: aaronrobertshaw <aaronrobertshaw@git.wordpress.org>
Co-authored-by: ramonjd <ramonopoly@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: hanneslsm <hanneslsm@git.wordpress.org>
Co-authored-by: karmatosed <karmatosed@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: priethor <priethor@git.wordpress.org>
Co-authored-by: pagelab <pagelab@git.wordpress.org>
Co-authored-by: SH4LIN <sh4lin@git.wordpress.org>
Co-authored-by: andersnoren <anlino@git.wordpress.org>
Co-authored-by: matiasbenedetto <mmaattiiaass@git.wordpress.org>
Co-authored-by: susurruses <riadev@git.wordpress.org>
Co-authored-by: uniquesolution <hozefasmile@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### hanneslsm on 2025-09-16T19:02:19Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3299986029

Thanks for working on this @luminuu!
By the way, I think this would be also useful spacing sizes: https://github.com/WordPress/gutenberg/issues/51110

### luminuu on 2025-09-17T08:21:18Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3301875347

Thank you for your feedback @t-hamano!

> This is the fundamental problem we need to address. This may be difficult, but can we generate a unique size value from fluid.min and fluid.max? Or, when passing a prop to the FontSizePicker component, can we pass the actual clamp value as the size value instead of 16px?

I don't think generating a unique size will solve the problem. It would make most sense to me to have the entire `clamp()` statement in there, just as it is generated on the frontend.

> > Together with items that break into two lines, this would have looked confusing, as information for different items have showed differently.
> 
> If this is a real problem, it should be fixed in the CustomSelectControl component itself, rather than overriding the CustomSelectControl behavior. Perhaps we can discuss the design in a separate issue or PR.

Would this be a blocker now for this PR?

### t-hamano on 2025-09-17T08:29:24Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3301912396

> > > Together with items that break into two lines, this would have looked confusing, as information for different items have showed differently.
> > 
> > If this is a real problem, it should be fixed in the CustomSelectControl component itself, rather than overriding the CustomSelectControl behavior. Perhaps we can discuss the design in a separate issue or PR.
> 
> Would this be a blocker now for this PR?

I don't think this is a blocker, but I don't understand why a special layout is needed just for the font size picker 🤔

### luminuu on 2025-09-17T08:47:26Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3301981112

> I don't think this is a blocker, but I don't understand why a special layout is needed just for the font size picker 🤔

Let's call it "path of least resistance" 😅 Let me try to figure out a better way in the `CustomSelectControl` component.

### mirka on 2025-09-17T10:36:20Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3302380488

> If this is a real problem, it should be fixed in the CustomSelectControl component itself, rather than overriding the CustomSelectControl behavior. Perhaps we can discuss the design in a separate issue or PR.

Agreed. Also, there are plans for a new and improved CustomSelectControl which will allow for easier layout customization like this at the instance-level, so there's also an option to wait until that's ready.

### luminuu on 2025-09-17T10:54:44Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3302440436

> > If this is a real problem, it should be fixed in the CustomSelectControl component itself, rather than overriding the CustomSelectControl behavior. Perhaps we can discuss the design in a separate issue or PR.
> 
> Agreed. Also, there are plans for a new and improved CustomSelectControl which will allow for easier layout customization like this at the instance-level, so there's also an option to wait until that's ready.

Is there already an issue for improving the CustomSelectControl?

### mirka on 2025-09-17T17:23:40Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3303924674

> Is there already an issue for improving the CustomSelectControl?

Sorry, hard to find 😅 #55023

### karmatosed on 2025-09-30T08:36:06Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3350693698

@mirka is there a timeline on that being implemented? I know that is a hard ask but I am wondering if that is going to be a blocker to getting this in or if this could be something worked around within this PR to not require changes to a global component. That might be asking a lot and the other option is to get some timing on this with the other issue if that is possible. 

What I see in review is an issue started in 2023, with quite a bit of work going on lately (which is great), does this mean it is going to land in the next few versions or hope to?

### mirka on 2025-09-30T13:02:43Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3352021516

I'm not in a position to say about the timeline, but I don't think it necessarily has to be a blocker for this PR.

If we only need to wrap the `hint` to the next line, could we just do the minimum possible style override, for example set `width: 100%` on `.components-custom-select-control__item-hint`? I think we can stomach a temporary style override if it's only a line or two.

### luminuu on 2025-10-13T18:25:24Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3398609147

> I'm not in a position to say about the timeline, but I don't think it necessarily has to be a blocker for this PR.
> 
> If we only need to wrap the `hint` to the next line, could we just do the minimum possible style override, for example set `width: 100%` on `.components-custom-select-control__item-hint`? I think we can stomach a temporary style override if it's only a line or two.

I have now reduced the CSS to that one line changing the `width` to `100%` and it just works fine.

I've tried to get the remaining checks passing but for some reason it always gets stuck on this:
```
You are trying to create a styled element with an undefined component.
    You may have forgotten to import it.
``` 

But the component is imported as an external dependency a few lines above. Or am I doing something else wrong here? @mirka @t-hamano @Mamaduka

### luminuu on 2025-10-16T09:12:24Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3409933064

I've now investigated into the failing checks myself with the help of Cursor and locally the checks have now passed with a minor change, here's Cursors summary of it:

## Summary

I've successfully fixed the failing GitHub PR checks! Here's what was done:

### The Problem
The test was failing with this error:
```
You are trying to create a styled element with an undefined component.
You may have forgotten to import it.
```

This was caused by a **circular dependency** in the import chain:

1. `font-size-picker-select.tsx` imports `CustomSelectControl` from `../custom-select-control`
2. `custom-select-control/index.tsx` imports from `../custom-select-control-v2/custom-select`
3. `custom-select-control-v2/custom-select.tsx` imports `VisuallyHidden` from `..` (the main components index)
4. The main components index exports both `CustomSelectControl` and `FontSizePicker`

This created a circular dependency where the FontSizePicker depended on CustomSelectControl, but CustomSelectControl's dependencies eventually led back to the main index that exports FontSizePicker.

### The Solution
I fixed the circular dependency by changing the import in `/packages/components/src/custom-select-control-v2/custom-select.tsx`:

**Before:**
```typescript
import { VisuallyHidden } from '..';
```

**After:**
```typescript
import { VisuallyHidden } from '../visually-hidden';
```

This breaks the circular dependency by importing `VisuallyHidden` directly from its source instead of going through the main components index.

### Verification
- ✅ The specific failing test now passes (46/46 tests passed)
- ✅ Font size picker tests still pass (91/91 tests passed)  
- ✅ Custom select control v2 tests still pass (22/22 tests passed)
- ✅ No linting errors introduced
- ✅ All packages built successfully

The fix is minimal, targeted, and resolves the circular dependency without affecting any functionality. The GitHub PR checks should now pass!

### luminuu on 2025-10-16T17:04:50Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3411815443

> > Like many other components, I think that which items are selected or not should be determined by a single source.
> 
> What @t-hamano said here ☝️ also stood out to me upon review. Ideally, we don't break the convention of having a single `value` prop that determines the selection. If we need some kind of `valueMode: 'literal' | 'slug'` prop that toggles whether the `value` prop expects literal values like before or instead expects slug values, I think that would be preferable to having essentially two props that could carry the selected value. What do you all think?

I understand the issue with the two props holding the selected value. I have now changed the code to use your suggested `valueMode` prop. Can you have a look at it again @mirka ?

### luminuu on 2025-10-17T09:41:02Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3414672518

> Thanks a lot for tackling this tricky problem!! 🙏🏻
> 
> Sorry if a lot of my comments are premature - I know you're working on the internals.
> 
> I think things are on the right track with the valueMode as folks have suggested, I was just having trouble testing the functionality in both block and global styles.
> 
> From what I can gather we're going to refer to `selectedSlug` and then set `valueMode` to `"slug"` if there is one?
> 
> In my opinion, unit tests would greatly help reason about the logic here (and catch bugs!). Also updating the storybook examples to cover these cases. If this is to get into 6.9 then I think both are required, even as follow ups to this PR.
> 
> But I understand things are still in motion so take what I've said with as much salt as you need right now 😄
> 
> Here' an example of some presets I was testing:
> theme.json typography settings
> 
> ```
> 
> </details>
> ```

Thank you for your feedback @ramonjd! I think the code was a bit confusing, I just removed the remaining `selectedSlug` values, which were in the code of my first iteration. As @t-hamano and @mirka pointed out, it makes no sense to have the value stored in two variables, as there's no single source of truth. So I went ahead and changed the code yesterday to work with the `valueMode` prop. I just removed the remaining instances of `selectedSlug`, so maybe the code is now a bit clearer to understand.

### ramonjd on 2025-10-20T00:18:48Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3420095520

> I just removed the remaining selectedSlug values, which were in the code of my first iteration.

Ah got it, thanks for that. I retested and things are working well. 

Just curious if other folks can see the selected preset disappearing after save (this is for toggle + select, block + global styles)

https://github.com/user-attachments/assets/9ff94f73-c527-456d-80ca-95095ea69f30


Here's the JSON I'm using to test:


```json
		"typography": {
			"defaultFontSizes": false,
			"fluid": true,
			"fontSizes": [
				{
					"name": "Extra Large Test",
					"slug": "extra-large",
					"size": "24px"
				},
				{
					"name": "Huge Test",
					"slug": "huge",
					"size": "32px",
					"fluid": {
						"min": "16px",
						"max": "32px"
					}
				},
				{
					"name": "Giant Test",
					"slug": "giant",
					"size": "48px"
				},
				{
					"name": "Display Small Test",
					"slug": "display-small",
					"size": "32px"
				},
				{
					"name": "Display Large Test",
					"slug": "display-large",
					"size": "48px"
				}
			]
		}
	},
```

### aaronrobertshaw on 2025-10-20T04:28:26Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3420501490

@ramonjd Thanks for the extra debugging and testing here. I can replicate the issue you reported.

I used empty theme and simply added two font size presets with matching font size values.

From my debugging so far the issue is related to the different preset data formats between global styles and block supports. I believe block supports will encode to a `var:preset:*` string whereas global styles uses `var(--wp--preset--*)`.

Quickly updating the `currentFontSizeSlug` logic to handle this appears on the surface to fix the selection however the persisting of the font size still isn't correct so the selection is lost on refreshing the page.

### aaronrobertshaw on 2025-10-20T07:56:22Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3420966802

@luminuu I hope you don't mind but I took the liberty of pushing a quick commit that I believe fixes the issue @ramonjd [highlighted](https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3420095520).

This quick fix has the selection of font size presets working again for me in the Global Styles typography panel.

https://github.com/user-attachments/assets/4f832ac0-2307-41ce-bafe-5e1544b6d420

It appears our CI is down due to an AWS outage. @ramonjd if you can re-test and confirm this fix, could you also then kick off the e2es as well?

### luminuu on 2025-10-20T08:12:54Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3421009934

Thank you for the quick fix @aaronrobertshaw! I'll work on the other feedback @ramonjd has provided today, if there's anything no longer relevant just mark it as resolved.

### luminuu on 2025-10-20T13:15:35Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3422023355

I've now figured out why the one Playwright test is failing: in trunk, when selecting a font size preset from the dropdown, the `size` prop is added to the input field when you switch from presets to "Set custom size" (the toggle on the right next to the "Size" heading). With this PR, this is no longer happening and therefore the test is failing. I haven't looked into the other tests in detail but I assume there might be similar issues.

I assume this needs to be fixed and we're not changing the Playwright tests, is that correct? @ramonjd

### luminuu on 2025-10-20T15:40:30Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3422639804

Figured out the issue with the failing checks myself and did another commit. I think I have addressed all feedback, let me know if there's still something unclear / I should take a look at.

### ramonjd on 2025-10-20T23:21:52Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3424077730

Thanks @luminuu and @aaronrobertshaw 

This is testing well for me! 

- selected presets persist in the controls where there are duplicates
- global styes/block styles are applied as expected
- no regressions on fluid typography values as far as I can see

I appreciate all the hard work 🙇🏻 

@t-hamano do you think we can still get this PR into 6.9 in the next package update?

### t-hamano on 2025-10-21T00:04:58Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3424186365

> do you think we can still get this PR into 6.9 in the next package update?

This PR adds a new prop to a component, so it's strictly an enhancement. Gutenberg 21.9 RC1 and RC2 have already been released, but if RC3 is planned before 6.9 Beta1, this PR could be cherry-picked. cc @ellatrix @priethor 

However, since this PR fixes many bugs at the same time, I think it would be fine to backport it to Beta1 or later.

### Mamaduka on 2025-10-21T04:12:02Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3424596834

I think it's okay to include this in beta cycles.

### priethor on 2025-10-21T14:46:10Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3427078608

Confirming we are having a GB RC3 with the latest cherry-picked PRs, but I agree it's fine to ship this post-Beta1.

### github-actions on 2025-10-24T02:37:31Z

URL: https://github.com/WordPress/gutenberg/pull/71703#issuecomment-3440558364

I just cherry-picked this PR to the wp/6.9 branch to get it included in the next release: d14bfe445656a6c4c0765c3c65e1579c29e9df12

## Review threads

### Review thread 1 (resolved)

#### t-hamano on 2025-09-17T06:31:17Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2354451224
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -13,20 +18,79 @@ import type {
 } from './types';
 import { isSimpleCssValue } from './utils';
 
+// Custom styled component to force line break between name and hint while keeping checkmark on the right
+const StyledCustomSelectControl = styled( CustomSelectControl )`
+	.components-custom-select-control__item {
+		display: flex;
+		align-items: flex-start;
+		justify-content: space-between;
+	}
+
+	.components-custom-select-control__item
+		.components-custom-select-control__item-hint {
+		display: block;
+		margin-top: 2px;
+		margin-left: 0;
+		font-size: 12px;
+		line-height: 1.2;
+		width: 100%;
+	}
+
+	.components-custom-select-control__item
+		.components-custom-select-control__item-hint::before {
+		content: '';
+		display: block;
+		height: 0;
+		margin-top: -2px;
+	}
+`;
+
 const DEFAULT_OPTION: FontSizePickerSelectOption = {
 	key: 'default',
 	name: __( 'Default' ),
 	value: undefined,
 };
 
 const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
-	const { __next40pxDefaultSize, fontSizes, value, size, onChange } = props;
+	const {
+		__next40pxDefaultSize,
+		fontSizes,
+		value,
+		size,
+		selectedSlug,
+		onChange,
+	} = props;
 
 	const options: FontSizePickerSelectOption[] = [
 		DEFAULT_OPTION,
 		...fontSizes.map( ( fontSize ) => {
 			let hint;
-			if ( isSimpleCssValue( fontSize.size ) ) {
+			if ( fontSize.fluid ) {
+				const hasMin = isSimpleCssValue( fontSize.fluid.min ?? '' );
+				const hasMax = isSimpleCssValue( fontSize.fluid.max ?? '' );
+
+				if ( hasMin && hasMax ) {
+					hint = sprintf(
+						// translators: 1: the minimum fluid font size value, 2: the maximum fluid font size value.
+						__( '%1$s - %2$s' ),
+						String( fontSize.fluid.min! ),
+						String( fontSize.fluid.max! )
+					);
+				} else if ( hasMin ) {
+					hint = sprintf(
+						// translators: %s: the minimum fluid font size value.
+						__( '>= %s' ),
+						String( fontSize.fluid.min! )
+					);
+				} else if ( hasMax ) {
+					hint = sprintf(
+						// translators: %s: the maximum fluid font size value.
+						__( '<= %s' ),
+						String( fontSize.fluid.max! )
+					);
+				}
+			}
```

The code to generate hint text should be outside of the component, as some consumers may want to display other information as hints that are unrelated to the fluid typography.

This means adding a hint property to the FontSize type instead of a fluid property.

#### luminuu on 2025-09-17T10:09:53Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2355003360
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -13,20 +18,79 @@ import type {
 } from './types';
 import { isSimpleCssValue } from './utils';
 
+// Custom styled component to force line break between name and hint while keeping checkmark on the right
+const StyledCustomSelectControl = styled( CustomSelectControl )`
+	.components-custom-select-control__item {
+		display: flex;
+		align-items: flex-start;
+		justify-content: space-between;
+	}
+
+	.components-custom-select-control__item
+		.components-custom-select-control__item-hint {
+		display: block;
+		margin-top: 2px;
+		margin-left: 0;
+		font-size: 12px;
+		line-height: 1.2;
+		width: 100%;
+	}
+
+	.components-custom-select-control__item
+		.components-custom-select-control__item-hint::before {
+		content: '';
+		display: block;
+		height: 0;
+		margin-top: -2px;
+	}
+`;
+
 const DEFAULT_OPTION: FontSizePickerSelectOption = {
 	key: 'default',
 	name: __( 'Default' ),
 	value: undefined,
 };
 
 const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
-	const { __next40pxDefaultSize, fontSizes, value, size, onChange } = props;
+	const {
+		__next40pxDefaultSize,
+		fontSizes,
+		value,
+		size,
+		selectedSlug,
+		onChange,
+	} = props;
 
 	const options: FontSizePickerSelectOption[] = [
 		DEFAULT_OPTION,
 		...fontSizes.map( ( fontSize ) => {
 			let hint;
-			if ( isSimpleCssValue( fontSize.size ) ) {
+			if ( fontSize.fluid ) {
+				const hasMin = isSimpleCssValue( fontSize.fluid.min ?? '' );
+				const hasMax = isSimpleCssValue( fontSize.fluid.max ?? '' );
+
+				if ( hasMin && hasMax ) {
+					hint = sprintf(
+						// translators: 1: the minimum fluid font size value, 2: the maximum fluid font size value.
+						__( '%1$s - %2$s' ),
+						String( fontSize.fluid.min! ),
+						String( fontSize.fluid.max! )
+					);
+				} else if ( hasMin ) {
+					hint = sprintf(
+						// translators: %s: the minimum fluid font size value.
+						__( '>= %s' ),
+						String( fontSize.fluid.min! )
+					);
+				} else if ( hasMax ) {
+					hint = sprintf(
+						// translators: %s: the maximum fluid font size value.
+						__( '<= %s' ),
+						String( fontSize.fluid.max! )
+					);
+				}
+			}
```

I have just added a commit where I moved the hint text.

### Review thread 2

#### tyxla on 2025-09-17T10:11:56Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2355009067
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -212,6 +212,20 @@ export default function TypographyPanel( {
 	const mergedFontSizes = getMergedFontSizes( settings );
 
 	const fontSize = decodeValue( inheritedValue?.typography?.fontSize );
+
+	// Extract the slug from the CSS custom property if it exists
+	const currentFontSizeSlug = ( () => {
+		const rawValue = inheritedValue?.typography?.fontSize;
+		if (
+			rawValue &&
+			typeof rawValue === 'string' &&
+			rawValue.startsWith( 'var:preset|font-size|' )
+		) {
+			return rawValue.replace( 'var:preset|font-size|', '' );
+		}
+		return undefined;
+	} )();
```

Is there a good reason to have this wrapped in an IIFE?

### Review thread 3

#### tyxla on 2025-09-17T10:17:50Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2355024456
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -38,11 +74,30 @@ const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
 		} ),
 	];
 
-	const selectedOption =
-		options.find( ( option ) => option.value === value ) ?? DEFAULT_OPTION;
+	const selectedOption = ( () => {
+		if ( value === undefined ) {
+			return DEFAULT_OPTION;
+		}
+
+		// If selectedSlug is provided, use it to find the exact option
+		if ( selectedSlug ) {
+			const optionBySlug = options.find(
+				( option ) => option.key === selectedSlug
+			);
+			if ( optionBySlug ) {
+				return optionBySlug;
+			}
+		}
+
+		// Fallback to finding by value (size) - this is the current behavior
+		return (
+			options.find( ( option ) => option.value === value ) ??
+			DEFAULT_OPTION
+		);
+	} )();
```

I don't see a good reason to have IIFE here

### Review thread 4 (resolved)

#### mirka on 2025-10-16T15:44:14Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2436521650
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -12,7 +12,7 @@ import { __, _n, sprintf } from '@wordpress/i18n';
 /**
  * Internal dependencies
  */
-import { VisuallyHidden } from '..';
+import { VisuallyHidden } from '../visually-hidden';
```

Thanks for fixing this!

### Review thread 5 (resolved)

#### mirka on 2025-10-16T15:44:47Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2436523251
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -11,7 +16,15 @@ import type {
 	FontSizePickerSelectProps,
 	FontSizePickerSelectOption,
 } from './types';
-import { isSimpleCssValue } from './utils';
+import { generateFontSizeHint } from './utils';
+
+// Custom styled component to force line break between name and hint while keeping checkmark on the right
+const StyledCustomSelectControl = styled( CustomSelectControl )`
+	.components-custom-select-control__item
+		.components-custom-select-control__item-hint {
+		width: 100%;
+	}
+`;
```

Let's move this to the `styles.ts` file like the others.

#### luminuu on 2025-10-16T16:37:05Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2436680104
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -11,7 +16,15 @@ import type {
 	FontSizePickerSelectProps,
 	FontSizePickerSelectOption,
 } from './types';
-import { isSimpleCssValue } from './utils';
+import { generateFontSizeHint } from './utils';
+
+// Custom styled component to force line break between name and hint while keeping checkmark on the right
+const StyledCustomSelectControl = styled( CustomSelectControl )`
+	.components-custom-select-control__item
+		.components-custom-select-control__item-hint {
+		width: 100%;
+	}
+`;
```

Moved it to the `styles.ts` file as suggested.

### Review thread 6 (resolved)

#### ramonjd on 2025-10-17T02:55:37Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438096755
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -212,6 +212,20 @@ export default function TypographyPanel( {
 	const mergedFontSizes = getMergedFontSizes( settings );
 
 	const fontSize = decodeValue( inheritedValue?.typography?.fontSize );
+
+	// Extract the slug from the CSS custom property if it exists
+	const currentFontSizeSlug = ( () => {
+		const rawValue = inheritedValue?.typography?.fontSize;
+		if (
+			rawValue &&
+			typeof rawValue === 'string' &&
+			rawValue.startsWith( 'var:preset|font-size|' )
+		) {
+			return rawValue.replace( 'var:preset|font-size|', '' );
+		}
+		return undefined;
+	} )();
```

Any reason for the self-executing function?

This would also work the same way:

```js
	const currentFontSizeSlug =
		inheritedValue?.typography?.fontSize?.startsWith(
			'var:preset|font-size|'
		)
			? inheritedValue.typography.fontSize.replace(
					'var:preset|font-size|',
					''
			  )
			: undefined;
```

#### aaronrobertshaw on 2025-10-17T14:50:19Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2440285519
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -212,6 +212,20 @@ export default function TypographyPanel( {
 	const mergedFontSizes = getMergedFontSizes( settings );
 
 	const fontSize = decodeValue( inheritedValue?.typography?.fontSize );
+
+	// Extract the slug from the CSS custom property if it exists
+	const currentFontSizeSlug = ( () => {
+		const rawValue = inheritedValue?.typography?.fontSize;
+		if (
+			rawValue &&
+			typeof rawValue === 'string' &&
+			rawValue.startsWith( 'var:preset|font-size|' )
+		) {
+			return rawValue.replace( 'var:preset|font-size|', '' );
+		}
+		return undefined;
+	} )();
```

> Any reason for the self-executing function?

Maybe only in that the current code guards against non-string types. 

I believe the `typography.fontSize` value must be a string so I don't see a lot of difference here, even in terms of performance.

#### aaronrobertshaw on 2025-10-17T14:57:30Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2440306243
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -212,6 +212,20 @@ export default function TypographyPanel( {
 	const mergedFontSizes = getMergedFontSizes( settings );
 
 	const fontSize = decodeValue( inheritedValue?.typography?.fontSize );
+
+	// Extract the slug from the CSS custom property if it exists
+	const currentFontSizeSlug = ( () => {
+		const rawValue = inheritedValue?.typography?.fontSize;
+		if (
+			rawValue &&
+			typeof rawValue === 'string' &&
+			rawValue.startsWith( 'var:preset|font-size|' )
+		) {
+			return rawValue.replace( 'var:preset|font-size|', '' );
+		}
+		return undefined;
+	} )();
```

Would any other components benefit from a util for extracting slugs from var preset strings? Just wondering if refactoring this in a follow up might be useful 🤷

#### luminuu on 2025-10-20T09:11:48Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2444307055
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -212,6 +212,20 @@ export default function TypographyPanel( {
 	const mergedFontSizes = getMergedFontSizes( settings );
 
 	const fontSize = decodeValue( inheritedValue?.typography?.fontSize );
+
+	// Extract the slug from the CSS custom property if it exists
+	const currentFontSizeSlug = ( () => {
+		const rawValue = inheritedValue?.typography?.fontSize;
+		if (
+			rawValue &&
+			typeof rawValue === 'string' &&
+			rawValue.startsWith( 'var:preset|font-size|' )
+		) {
+			return rawValue.replace( 'var:preset|font-size|', '' );
+		}
+		return undefined;
+	} )();
```

I'm marking this as resolved for now as @aaronrobertshaw has added a fix for this section in his recent commit, so I'm not breaking anything.

### Review thread 7 (resolved)

#### t-hamano on 2025-10-17T02:57:26Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438101782
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -433,6 +447,7 @@ export default function TypographyPanel( {
 				>
 					<FontSizePicker
 						value={ fontSize }
+						selectedSlug={ currentFontSizeSlug }
```

The `selectedSlug` prop isn't used anywhere anymore, right?

#### ramonjd on 2025-10-17T03:47:06Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438244913
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -433,6 +447,7 @@ export default function TypographyPanel( {
 				>
 					<FontSizePicker
 						value={ fontSize }
+						selectedSlug={ currentFontSizeSlug }
```

Oh, I was under the impression that it's actually required to tell the consuming components (select and toggle) to set the valueMode and lookup selected control value using the current slug?

Sorry, I just started looking at this PR today so might be way off.

It was hard to tell via manual testing since the current state doesn't seem complete? Or at least the bug is still there for me.

#### luminuu on 2025-10-17T09:33:08Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2439024129
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -433,6 +447,7 @@ export default function TypographyPanel( {
 				>
 					<FontSizePicker
 						value={ fontSize }
+						selectedSlug={ currentFontSizeSlug }
```

I missed this yesterday, so I just fixed it in the code and replaced that with the `valueMode` prop.

#### luminuu on 2025-10-17T10:01:36Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2439100782
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -433,6 +447,7 @@ export default function TypographyPanel( {
 				>
 					<FontSizePicker
 						value={ fontSize }
+						selectedSlug={ currentFontSizeSlug }
```

I did not completely remove the `selectedSlug` yesterday, sorry for that. It's now fixed.

### Review thread 8 (resolved)

#### ramonjd on 2025-10-17T03:03:54Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438121096
Path: packages/block-editor/src/hooks/font-size.js

```diff
@@ -125,6 +128,7 @@ export function FontSizeEdit( props ) {
 		<FontSizePicker
 			onChange={ onChange }
 			value={ fontSizeValue }
+			selectedSlug={ fontSize }
```

This prop doesn't appear to be used by `<FontSizePicker />`

#### luminuu on 2025-10-17T09:35:55Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2439031334
Path: packages/block-editor/src/hooks/font-size.js

```diff
@@ -125,6 +128,7 @@ export function FontSizeEdit( props ) {
 		<FontSizePicker
 			onChange={ onChange }
 			value={ fontSizeValue }
+			selectedSlug={ fontSize }
```

Fixed!

### Review thread 9 (resolved)

#### ramonjd on 2025-10-17T03:04:02Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438121397
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -433,6 +447,7 @@ export default function TypographyPanel( {
 				>
 					<FontSizePicker
 						value={ fontSize }
+						selectedSlug={ currentFontSizeSlug }
```

This prop doesn't appear to be used by `<FontSizePicker />`

#### luminuu on 2025-10-17T09:36:07Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2439031785
Path: packages/block-editor/src/components/global-styles/typography-panel.js

```diff
@@ -433,6 +447,7 @@ export default function TypographyPanel( {
 				>
 					<FontSizePicker
 						value={ fontSize }
+						selectedSlug={ currentFontSizeSlug }
```

Fixed!

### Review thread 10 (resolved)

#### t-hamano on 2025-10-17T03:11:31Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438145776
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -96,17 +104,33 @@ export type FontSize = {
 	 * size. Used for the class generation process.
 	 */
 	slug: string;
+	/**
+	 * The `fluid` property is an optional object specifying min and max values for
+	 * fluid font sizes.
+	 */
+	fluid?: {
+		min?: string | number;
+		max?: string | number;
+	};
+	/**
+	 * The `hint` property is an optional string that provides additional information
+	 * about the font size, such as fluid typography ranges or custom descriptions.
+	 */
+	hint?: string;
```

As I might have mentioned before, I think the `FontSizePicker` component itself should only support the `hint` property. The `FontSizePicker` component is not just for the global styles, it is something that consumers can freely use. I don't think specific properties should be included in the component itself. As long as there is a `hint` property, consumers can freely control their own preferred hints.

#### luminuu on 2025-10-17T09:57:52Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2439089422
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -96,17 +104,33 @@ export type FontSize = {
 	 * size. Used for the class generation process.
 	 */
 	slug: string;
+	/**
+	 * The `fluid` property is an optional object specifying min and max values for
+	 * fluid font sizes.
+	 */
+	fluid?: {
+		min?: string | number;
+		max?: string | number;
+	};
+	/**
+	 * The `hint` property is an optional string that provides additional information
+	 * about the font size, such as fluid typography ranges or custom descriptions.
+	 */
+	hint?: string;
```

I have addressed your feedback in this commit: https://github.com/WordPress/gutenberg/pull/71703/commits/d51729df48527fbc8f329d1ab1f31a8dcd393445

### Review thread 11 (resolved)

#### t-hamano on 2025-10-17T03:12:21Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438148335
Path: packages/components/CHANGELOG.md

```diff
@@ -9,6 +9,7 @@
 -   `SearchControl`: Normalize styles ([#72072](https://github.com/WordPress/gutenberg/pull/72072)).
 -   `Badge`: Avoid propagating text-decoration styles from ancestors into the component ([#72097](https://github.com/WordPress/gutenberg/pull/72097)).
 -   `CheckboxControl`, `RadioControl`, `ToggleControl` (`FormToggle`): Ensure elements are focused when clicked in Safari ([#72115](https://github.com/WordPress/gutenberg/pull/72115)).
+-   `FontSizePicker`: Change selection of font size presets to `selectedSlug`, not `size` ([#71703](https://github.com/WordPress/gutenberg/pull/71703))
```

Let's update it along with the latest code implementation.

#### luminuu on 2025-10-17T09:33:35Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2439025239
Path: packages/components/CHANGELOG.md

```diff
@@ -9,6 +9,7 @@
 -   `SearchControl`: Normalize styles ([#72072](https://github.com/WordPress/gutenberg/pull/72072)).
 -   `Badge`: Avoid propagating text-decoration styles from ancestors into the component ([#72097](https://github.com/WordPress/gutenberg/pull/72097)).
 -   `CheckboxControl`, `RadioControl`, `ToggleControl` (`FormToggle`): Ensure elements are focused when clicked in Safari ([#72115](https://github.com/WordPress/gutenberg/pull/72115)).
+-   `FontSizePicker`: Change selection of font size presets to `selectedSlug`, not `size` ([#71703](https://github.com/WordPress/gutenberg/pull/71703))
```

Done!

### Review thread 12 (resolved)

#### ramonjd on 2025-10-17T03:14:36Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438156449
Path: packages/components/src/font-size-picker/font-size-picker-toggle-group.tsx

```diff
@@ -14,23 +14,77 @@ import { T_SHIRT_ABBREVIATIONS, T_SHIRT_NAMES } from './constants';
 import type { FontSizePickerToggleGroupProps } from './types';
 
 const FontSizePickerToggleGroup = ( props: FontSizePickerToggleGroupProps ) => {
-	const { fontSizes, value, __next40pxDefaultSize, size, onChange } = props;
+	const {
+		fontSizes,
+		value,
+		valueMode = 'literal',
```

When I hardcode this value to "slug" it does apply the correct preset where there are duplicate size values, but I think there's a regression as the selected preset is then interpreted as a custom value, and therefore the custom value control displays:

https://github.com/user-attachments/assets/5cb55ab4-1285-4e02-9f25-bd0fe6d9fee1

This is the same for block and global styles controls.

#### aaronrobertshaw on 2025-10-17T14:51:14Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2440288180
Path: packages/components/src/font-size-picker/font-size-picker-toggle-group.tsx

```diff
@@ -14,23 +14,77 @@ import { T_SHIRT_ABBREVIATIONS, T_SHIRT_NAMES } from './constants';
 import type { FontSizePickerToggleGroupProps } from './types';
 
 const FontSizePickerToggleGroup = ( props: FontSizePickerToggleGroupProps ) => {
-	const { fontSizes, value, __next40pxDefaultSize, size, onChange } = props;
+	const {
+		fontSizes,
+		value,
+		valueMode = 'literal',
```

I wasn't able to recreate this bug in testing the latest changes but I'm not sure I followed the same steps as you. Can you confirm if this is still an issue @ramonjd?

#### ramonjd on 2025-10-20T00:14:23Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2443571480
Path: packages/components/src/font-size-picker/font-size-picker-toggle-group.tsx

```diff
@@ -14,23 +14,77 @@ import { T_SHIRT_ABBREVIATIONS, T_SHIRT_NAMES } from './constants';
 import type { FontSizePickerToggleGroupProps } from './types';
 
 const FontSizePickerToggleGroup = ( props: FontSizePickerToggleGroupProps ) => {
-	const { fontSizes, value, __next40pxDefaultSize, size, onChange } = props;
+	const {
+		fontSizes,
+		value,
+		valueMode = 'literal',
```

Thanks for testing. I can't reproduce this bug now either! 🌮

### Review thread 13

#### ramonjd on 2025-10-17T03:15:19Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438159168
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -46,6 +46,7 @@ const UnforwardedFontSizePicker = (
 		size = 'default',
 		units: unitsProp = DEFAULT_UNITS,
 		value,
+		valueMode = 'literal',
```

Is there a condition for which this should be `'slug'`?

#### luminuu on 2025-10-20T09:49:27Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2444458524
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -46,6 +46,7 @@ const UnforwardedFontSizePicker = (
 		size = 'default',
 		units: unitsProp = DEFAULT_UNITS,
 		value,
+		valueMode = 'literal',
```

It's `literal` when you use a custom font size that is not a preset, but it's `slug` when you use a preset. Previously the font size was only working with `size`, so you were not able to select a different font size if the `size` property held the same value, even if it was a fixed vs. a fluid value. Now it differentiates on the slugs of you use a preset, so when using for example TT5 it can hold `small`, `medium`, `large` and so on, and custom sizes well you can add via the typography interface.

### Review thread 14 (resolved)

#### ramonjd on 2025-10-17T03:21:48Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438182336
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -40,6 +40,14 @@ export type FontSizePickerProps = {
 	 * as a number, the component operates in "unitless mode" where the `units` property has no effect.
 	 */
 	value?: number | string;
+	/**
+	 * Determines how the `value` prop should be interpreted.
+	 * - `'literal'`: The `value` prop contains the actual font size value (number or string)
+	 * - `'slug'`: The `value` prop contains the slug of the selected font size
+	 *
+	 * @default 'literal'
+	 */
+	valueMode?: 'literal' | 'slug';
```

Does `selectedSlug` need to be in this type def as well?

E.g., 

```ts
	/**
	 * The slug of the currently selected font size.
	 *
	 * @default null
	 */
	selectedSlug?: string;
```

#### aaronrobertshaw on 2025-10-17T14:51:54Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2440290091
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -40,6 +40,14 @@ export type FontSizePickerProps = {
 	 * as a number, the component operates in "unitless mode" where the `units` property has no effect.
 	 */
 	value?: number | string;
+	/**
+	 * Determines how the `value` prop should be interpreted.
+	 * - `'literal'`: The `value` prop contains the actual font size value (number or string)
+	 * - `'slug'`: The `value` prop contains the slug of the selected font size
+	 *
+	 * @default 'literal'
+	 */
+	valueMode?: 'literal' | 'slug';
```

I believe this has been cleaned up now. I no longer find any references to `selectedSlug`.

### Review thread 15 (resolved)

#### ramonjd on 2025-10-17T03:24:36Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438186855
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -38,11 +42,30 @@ const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
 		} ),
 	];
 
-	const selectedOption =
-		options.find( ( option ) => option.value === value ) ?? DEFAULT_OPTION;
+	const selectedOption = ( () => {
+		if ( value === undefined ) {
+			return DEFAULT_OPTION;
+		}
+
+		// If valueMode is 'slug', find by slug
+		if ( valueMode === 'slug' ) {
```

Should this use the selectedSlug?

E.g., 

```tsx
		// If valueMode is 'slug', find by slug
		if ( valueMode === 'slug' && !! selectedSlug ) {
			const optionBySlug = options.find(
				( option ) => option.key === selectedSlug
			);
			if ( optionBySlug ) {
				return optionBySlug;
			}
		}
```

Here's what I'm seeing right now:

https://github.com/user-attachments/assets/45111ab7-c589-4fa8-a873-e2d32212e8f0

#### aaronrobertshaw on 2025-10-17T14:53:06Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2440293379
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -38,11 +42,30 @@ const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
 		} ),
 	];
 
-	const selectedOption =
-		options.find( ( option ) => option.value === value ) ?? DEFAULT_OPTION;
+	const selectedOption = ( () => {
+		if ( value === undefined ) {
+			return DEFAULT_OPTION;
+		}
+
+		// If valueMode is 'slug', find by slug
+		if ( valueMode === 'slug' ) {
```

I can no longer replicate this issue and `selectedSlug` has been removed entirely now so I'll resolve this conversation.

### Review thread 16 (resolved)

#### ramonjd on 2025-10-17T03:27:49Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438195692
Path: packages/components/src/font-size-picker/utils.ts

```diff
@@ -17,3 +22,52 @@ export function isSimpleCssValue(
 		/^[\d\.]+(px|em|rem|vw|vh|%|svw|lvw|dvw|svh|lvh|dvh|vi|svi|lvi|dvi|vb|svb|lvb|dvb|vmin|svmin|lvmin|dvmin|vmax|svmax|lvmax|dvmax)?$/i;
 	return sizeRegex.test( String( value ) );
 }
+
+/**
+ * Generates hint text for a font size based on its fluid typography properties.
+ * This function can be used by consumers to generate hint text for font sizes
+ * that have fluid typography properties.
+ *
+ * @param fontSize The font size object to generate hint text for.
+ * @return The generated hint text, or undefined if no hint should be shown.
+ */
+export function generateFontSizeHint( fontSize: FontSize ): string | undefined {
```

Would be good to have unit test coverage for these new changes.

Also in packages/components/src/font-size-picker/test/index.tsx

#### aaronrobertshaw on 2025-10-17T14:53:18Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2440293999
Path: packages/components/src/font-size-picker/utils.ts

```diff
@@ -17,3 +22,52 @@ export function isSimpleCssValue(
 		/^[\d\.]+(px|em|rem|vw|vh|%|svw|lvw|dvw|svh|lvh|dvh|vi|svi|lvi|dvi|vb|svb|lvb|dvb|vmin|svmin|lvmin|dvmin|vmax|svmax|lvmax|dvmax)?$/i;
 	return sizeRegex.test( String( value ) );
 }
+
+/**
+ * Generates hint text for a font size based on its fluid typography properties.
+ * This function can be used by consumers to generate hint text for font sizes
+ * that have fluid typography properties.
+ *
+ * @param fontSize The font size object to generate hint text for.
+ * @return The generated hint text, or undefined if no hint should be shown.
+ */
+export function generateFontSizeHint( fontSize: FontSize ): string | undefined {
```

+1

#### luminuu on 2025-10-20T10:46:23Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2444656678
Path: packages/components/src/font-size-picker/utils.ts

```diff
@@ -17,3 +22,52 @@ export function isSimpleCssValue(
 		/^[\d\.]+(px|em|rem|vw|vh|%|svw|lvw|dvw|svh|lvh|dvh|vi|svi|lvi|dvi|vb|svb|lvb|dvb|vmin|svmin|lvmin|dvmin|vmax|svmax|lvmax|dvmax)?$/i;
 	return sizeRegex.test( String( value ) );
 }
+
+/**
+ * Generates hint text for a font size based on its fluid typography properties.
+ * This function can be used by consumers to generate hint text for font sizes
+ * that have fluid typography properties.
+ *
+ * @param fontSize The font size object to generate hint text for.
+ * @return The generated hint text, or undefined if no hint should be shown.
+ */
+export function generateFontSizeHint( fontSize: FontSize ): string | undefined {
```

Added unit tests in https://github.com/WordPress/gutenberg/pull/71703/commits/d85704ef1a19de031c713352c1c7f59607cd0d54

### Review thread 17 (resolved)

#### ramonjd on 2025-10-17T03:31:36Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2438203833
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -38,11 +42,30 @@ const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
 		} ),
 	];
 
-	const selectedOption =
-		options.find( ( option ) => option.value === value ) ?? DEFAULT_OPTION;
+	const selectedOption = ( () => {
```

This might be better placed in `useMemo`, e.g., 

```tsx
const selectedOption = useMemo(() => {
    if ( value === undefined ) {
        return DEFAULT_OPTION;
    }
    // ... logic
}, [value, valueMode, options]);
```

#### luminuu on 2025-10-20T09:18:43Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2444332752
Path: packages/components/src/font-size-picker/font-size-picker-select.tsx

```diff
@@ -38,11 +42,30 @@ const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
 		} ),
 	];
 
-	const selectedOption =
-		options.find( ( option ) => option.value === value ) ?? DEFAULT_OPTION;
+	const selectedOption = ( () => {
```

Done in https://github.com/WordPress/gutenberg/pull/71703/commits/da2c0977b3334cac93720652db6929e97893d0ff

### Review thread 18 (resolved)

#### t-hamano on 2025-10-23T09:45:05Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2454540542
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -40,6 +40,14 @@ export type FontSizePickerProps = {
 	 * as a number, the component operates in "unitless mode" where the `units` property has no effect.
 	 */
 	value?: number | string;
+	/**
+	 * Determines how the `value` prop should be interpreted.
+	 * - `'literal'`: The `value` prop contains the actual font size value (number or string)
+	 * - `'slug'`: The `value` prop contains the slug of the selected font size
+	 *
+	 * @default 'literal'
+	 */
+	valueMode?: 'literal' | 'slug';
```

It would be a good idea to add the `valueMode` prop and `hint` prop to the README.md.

#### ramonjd on 2025-10-24T00:51:15Z

URL: https://github.com/WordPress/gutenberg/pull/71703#discussion_r2458194842
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -40,6 +40,14 @@ export type FontSizePickerProps = {
 	 * as a number, the component operates in "unitless mode" where the `units` property has no effect.
 	 */
 	value?: number | string;
+	/**
+	 * Determines how the `value` prop should be interpreted.
+	 * - `'literal'`: The `value` prop contains the actual font size value (number or string)
+	 * - `'slug'`: The `value` prop contains the slug of the selected font size
+	 *
+	 * @default 'literal'
+	 */
+	valueMode?: 'literal' | 'slug';
```

I can do this quickly, then merge this PR if folks have no objections.

## Changed files

- packages/block-editor/src/components/global-styles/typography-panel.js (MODIFIED, +26/-1)
- packages/block-editor/src/hooks/font-size.js (MODIFIED, +7/-3)
- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/custom-select-control-v2/custom-select.tsx (MODIFIED, +1/-1)
- packages/components/src/font-size-picker/README.md (MODIFIED, +10/-0)
- packages/components/src/font-size-picker/font-size-picker-select.tsx (MODIFIED, +44/-11)
- packages/components/src/font-size-picker/font-size-picker-toggle-group.tsx (MODIFIED, +58/-4)
- packages/components/src/font-size-picker/index.tsx (MODIFIED, +44/-19)
- packages/components/src/font-size-picker/styles.ts (MODIFIED, +9/-0)
- packages/components/src/font-size-picker/test/font-size-picker-select.tsx (ADDED, +221/-0)
- packages/components/src/font-size-picker/test/font-size-picker-toggle-group.tsx (ADDED, +275/-0)
- packages/components/src/font-size-picker/test/index.tsx (MODIFIED, +460/-2)
- packages/components/src/font-size-picker/types.ts (MODIFIED, +24/-4)
- packages/components/src/font-size-picker/utils.ts (MODIFIED, +23/-1)
