---
number: 12963
type: pull_request
state: MERGED
merged_at: 2019-02-08T20:23:34Z
url: https://github.com/WordPress/gutenberg/pull/12963
matched_labels: ["[Package] Components"]
all_labels: ["[Feature] UI Components","[Package] Components"]
---

# PR #12963: Datepicker: Allow null value for currentDate on mounting

- URL: https://github.com/WordPress/gutenberg/pull/12963
- Author: psealock
- Created: 2018-12-18T01:34:38Z
- Updated: 2019-03-05T08:33:40Z
- Comments: 7 of 7
- Review threads: 5 of 5
- Changed files: 4 of 4
- Diff: ../pr-diffs/12963.diff

## Body

## Description
When a `null` value is passed to `<DatePicker >`, don't set the date to current date. Instead, allow the value to remain null such that no selection is visible on the calendar.

`undefined` values for `currentDate` behave as they did previously, which is to fall back to the today's date.

## Testing
1. Confirm that a dependency on setting `currentDate` to todays date by default does not exist by pulling down the branch and seeing that nothing breaks. 
1. Force a `null` value for the `currentDate` prop on  `<DatePicker >` to ensure nothing breaks and no date is selected.
2. Remove the `currentDate` prop and see the date picker default to today's date.

https://github.com/WordPress/gutenberg/blob/0c734e1867b50a9ee1eb19a08d6b740d66c5f2ad/packages/components/src/date-time/index.js#L48-L51

## Screenshots <!-- if applicable -->

![screen shot 2018-12-18 at 2 33 12 pm](https://user-images.githubusercontent.com/1922453/50126466-e54d9c80-02d1-11e9-8c7c-840ba3adea62.png)

## Types of changes
<!-- What types of changes does your code introduce?  -->
<!-- Bug fix (non-breaking change which fixes an issue) -->
<!-- New feature (non-breaking change which adds functionality) -->
<!-- Breaking change (fix or feature that would cause existing functionality to not work as expected) -->
New feature: Allow null value of date selection to persist such that no date is selected on the calendar.

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://wordpress.org/gutenberg/handbook/designers-developers/ -->

## Issue comments

### aduth on 2019-01-24T20:18:03Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-457341347

Wouldn't this be considered a breaking change for someone who might have relied on the current behavior?

Is a middle-ground option that we allow for an explicit `null` value to signify "unselected", allowing `undefined` to continue to represent the current-date defaulting?

### psealock on 2019-01-24T22:23:04Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-457380929

Great feedback here, thank you @aduth !

I've added a method `getMomentDate` to explicitly check for `null` such that `undefined` values for `currentDate` have the same experience as before.

### psealock on 2019-01-30T00:50:01Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-458767443

Thanks for the edits to the jsDoc @aduth.

I added testing to `onChangeMoment` also.

### psealock on 2019-02-07T20:37:28Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-461586452

Hi @aduth, would you have a moment to give this another look?

I changed the tests to save the argument passed to onChange for later comparison using moment's `isSame` function. Then I compare with a granularity of `minute`, which would preclude all but the rarest of circumstances where a pause in execution time ticks into the next second AND minute.

Many thanks!

### aduth on 2019-02-26T19:10:04Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-467571260

> When it comes to intermittent test failures, I'd prefer "impossible" over "rare", but I'll take what I can get.

Encountered my first failure today:

![image](https://user-images.githubusercontent.com/1779930/53439521-31067b80-39d0-11e9-807f-4a846e18812d.png)

### psealock on 2019-02-27T08:31:50Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-467770670

Yikes, thanks for the heads up. I'll look into this to evaluate other solutions

### psealock on 2019-03-05T08:33:40Z

URL: https://github.com/WordPress/gutenberg/pull/12963#issuecomment-469588299

@aduth Here is a PR to fix the intermittent failures, https://github.com/WordPress/gutenberg/pull/14230

## Review threads

### Review thread 1

#### aduth on 2019-01-25T19:28:06Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r251107689
Path: packages/components/src/date-time/date.js

```diff
@@ -25,7 +25,7 @@ class DatePicker extends Component {
 	onChangeMoment( newDate ) {
 		const { currentDate, onChange } = this.props;
 
-		const momentDate = currentDate ? moment( currentDate ) : moment();
+		const momentDate = this.getMomentDate( currentDate );
 		const momentTime = {
 			hours: momentDate.hours(),
```

`getMomentDate` can return `null`, at which point this would throw an error.

It would be good to document any function we introduce, as it may make this sort of thing easier to catch, when recognizing that the return value of the function is [nullable](http://usejsdoc.org/tags-type.html).

#### aduth on 2019-01-25T19:29:36Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r251108193
Path: packages/components/src/date-time/date.js

```diff
@@ -25,7 +25,7 @@ class DatePicker extends Component {
 	onChangeMoment( newDate ) {
 		const { currentDate, onChange } = this.props;
 
-		const momentDate = currentDate ? moment( currentDate ) : moment();
+		const momentDate = this.getMomentDate( currentDate );
 		const momentTime = {
 			hours: momentDate.hours(),
```

Unit tests, as well, would be good to capture this sort of issue and the expected behavior.

#### psealock on 2019-01-27T23:23:56Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r251260000
Path: packages/components/src/date-time/date.js

```diff
@@ -25,7 +25,7 @@ class DatePicker extends Component {
 	onChangeMoment( newDate ) {
 		const { currentDate, onChange } = this.props;
 
-		const momentDate = currentDate ? moment( currentDate ) : moment();
+		const momentDate = this.getMomentDate( currentDate );
 		const momentTime = {
 			hours: momentDate.hours(),
```

Eesh, good call. I've gone back to the original implementation here and included a comment about to explain why `moment()` is used if `currentDate` is undefined or null.

Unit tests added. Thanks

### Review thread 2 (resolved)

#### aduth on 2019-01-28T19:20:17Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r251556575
Path: packages/components/src/date-time/date.js

```diff
@@ -35,10 +36,24 @@ class DatePicker extends Component {
 		onChange( newDate.set( momentTime ).format( TIMEZONELESS_FORMAT ) );
 	}
 
+	/**
+	 * Create a Moment object from a date string. With no currentDate supplied, default to a Moment
+	 * object representing now. If a null value is passed, return a null value.
+	 *
+	 * @param {string|null} [currentDate] - Date representing the currently selected date or null to signify no selection.
```

While I think this is technically valid JSDoc, it doesn't really follow the conventions from the rest of the project, specifically:

- Using `?` to signify a nullable type
- Omitting the `[` `]` brackets around the variable name
- Omitting the `-` delimiter between the variable name and description.

```suggestion
	 * @param {?string} currentDate Date representing the currently selected
	 *                              date or null to signify no selection.
```

### Review thread 3 (resolved)

#### aduth on 2019-01-28T19:21:06Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r251556893
Path: packages/components/src/date-time/date.js

```diff
@@ -35,10 +36,24 @@ class DatePicker extends Component {
 		onChange( newDate.set( momentTime ).format( TIMEZONELESS_FORMAT ) );
 	}
 
+	/**
+	 * Create a Moment object from a date string. With no currentDate supplied, default to a Moment
+	 * object representing now. If a null value is passed, return a null value.
+	 *
+	 * @param {string|null} [currentDate] - Date representing the currently selected date or null to signify no selection.
+	 * @return {Moment|null} - Moment object for selected date or null.
```

Similarly, there should be a newline separating the `@return` and `@param` tag segments.

https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/#functions

```suggestion
	 * @return {?Moment} Moment object for selected date or null.
```

**Edit:** I see from the link above there's a different syntax for describing the optional parameter using the square bracket notation instead of the nullable type `?`. So I guess it's technically valid (preferred?) but not what's actually used through most of Gutenberg.

### Review thread 4

#### aduth on 2019-01-28T19:23:13Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r251557689
Path: packages/components/src/date-time/test/date.js

```diff
@@ -0,0 +1,59 @@
+/**
+ * External dependencies
+ */
+import { shallow } from 'enzyme';
+import moment from 'moment';
+
+/**
+ * Internal dependencies
+ */
+import DatePicker from '../date';
+
+describe( 'DatePicker', () => {
```

I'm glad to see tests, but for what it's worth these tests as written would not have caught the error described in my previous review, since there is zero coverage for `onChangeMoment`.

### Review thread 5

#### aduth on 2019-01-30T20:11:36Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r252419625
Path: packages/components/src/date-time/test/date.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { shallow } from 'enzyme';
+import moment from 'moment';
+
+/**
+ * Internal dependencies
+ */
+import DatePicker from '../date';
+
+const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+
+describe( 'DatePicker', () => {
+	it( 'should pass down a moment object for currentDate', () => {
+		const currentDate = '1986-10-18T23:00:00';
+		const wrapper = shallow( <DatePicker currentDate={ currentDate } /> );
+		const date = wrapper.children().props().date;
+		expect( moment.isMoment( date ) ).toBe( true );
+		expect( date.isSame( moment( currentDate ) ) ).toBe( true );
+	} );
+
+	it( 'should pass down a null date when currentDate is set to null', () => {
+		const wrapper = shallow( <DatePicker currentDate={ null } /> );
+		expect( wrapper.children().props().date ).toBeNull();
+	} );
+
+	it( 'should pass down a moment object for now when currentDate is undefined', () => {
+		const wrapper = shallow( <DatePicker /> );
+		const date = wrapper.children().props().date;
+		expect( moment.isMoment( date ) ).toBe( true );
+		expect( date.isSame( moment(), 'second' ) ).toBe( true );
+	} );
+
+	describe( 'getMomentDate', () => {
+		it( 'should return a Moment object representing a given date string', () => {
+			const currentDate = '1986-10-18T23:00:00';
+			const wrapper = shallow( <DatePicker /> );
+			const momentDate = wrapper.instance().getMomentDate( currentDate );
+
+			expect( moment.isMoment( momentDate ) ).toBe( true );
+			expect( momentDate.isSame( moment( currentDate ) ) ).toBe( true );
+		} );
+
+		it( 'should return null when given a null agrument', () => {
+			const currentDate = null;
+			const wrapper = shallow( <DatePicker /> );
+			const momentDate = wrapper.instance().getMomentDate( currentDate );
+
+			expect( momentDate ).toBeNull();
+		} );
+
+		it( 'should return a Moment object representing now when given an undefined argument', () => {
+			const wrapper = shallow( <DatePicker /> );
+			const momentDate = wrapper.instance().getMomentDate();
+
+			expect( moment.isMoment( momentDate ) ).toBe( true );
+			expect( momentDate.isSame( moment(), 'second' ) ).toBe( true );
+		} );
+	} );
+
+	describe( 'onChangeMoment', () => {
+		it( 'should call onChange with a formated date of the input', () => {
+			const onChangeSpy = jest.fn();
+			const currentDate = '1986-10-18T11:00:00';
+			const wrapper = shallow( <DatePicker currentDate={ currentDate } onChange={ onChangeSpy } /> );
+			const newDate = moment();
+
+			wrapper.instance().onChangeMoment( newDate );
+
+			expect( onChangeSpy ).toHaveBeenCalledWith( newDate.format( TIMEZONELESS_FORMAT ) );
+		} );
+
+		it( 'should call onChange with hours, minutes, seconds of the current time when currentDate is undefined', () => {
+			const onChangeSpy = jest.fn();
+			const wrapper = shallow( <DatePicker onChange={ onChangeSpy } /> );
+			const newDate = moment( '1986-10-18T11:00:00' );
+			const current = moment();
```

This worries me a bit; I feel like it will be prone to test fragility, where the time required to execute the test is such that `moment()` called here will produce a different time than `moment()` called within the implementation of `onChangeMoment`. Usually this should take a mere fraction of a second, but I've witnessed in the past where it's enough time to tick into the next second and cause an uenxpected test failure.

I think our options would be:

- Find a way to provide or otherwise control the current time as referenced within `onChangeMoment`
- Mock `moment` to have full control over what it returns†
- Provide some flexibility in the assertion (i.e. within a +/- allowable timeframe)
- Don't test it

#### psealock on 2019-01-31T00:05:11Z

URL: https://github.com/WordPress/gutenberg/pull/12963#discussion_r252489032
Path: packages/components/src/date-time/test/date.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { shallow } from 'enzyme';
+import moment from 'moment';
+
+/**
+ * Internal dependencies
+ */
+import DatePicker from '../date';
+
+const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+
+describe( 'DatePicker', () => {
+	it( 'should pass down a moment object for currentDate', () => {
+		const currentDate = '1986-10-18T23:00:00';
+		const wrapper = shallow( <DatePicker currentDate={ currentDate } /> );
+		const date = wrapper.children().props().date;
+		expect( moment.isMoment( date ) ).toBe( true );
+		expect( date.isSame( moment( currentDate ) ) ).toBe( true );
+	} );
+
+	it( 'should pass down a null date when currentDate is set to null', () => {
+		const wrapper = shallow( <DatePicker currentDate={ null } /> );
+		expect( wrapper.children().props().date ).toBeNull();
+	} );
+
+	it( 'should pass down a moment object for now when currentDate is undefined', () => {
+		const wrapper = shallow( <DatePicker /> );
+		const date = wrapper.children().props().date;
+		expect( moment.isMoment( date ) ).toBe( true );
+		expect( date.isSame( moment(), 'second' ) ).toBe( true );
+	} );
+
+	describe( 'getMomentDate', () => {
+		it( 'should return a Moment object representing a given date string', () => {
+			const currentDate = '1986-10-18T23:00:00';
+			const wrapper = shallow( <DatePicker /> );
+			const momentDate = wrapper.instance().getMomentDate( currentDate );
+
+			expect( moment.isMoment( momentDate ) ).toBe( true );
+			expect( momentDate.isSame( moment( currentDate ) ) ).toBe( true );
+		} );
+
+		it( 'should return null when given a null agrument', () => {
+			const currentDate = null;
+			const wrapper = shallow( <DatePicker /> );
+			const momentDate = wrapper.instance().getMomentDate( currentDate );
+
+			expect( momentDate ).toBeNull();
+		} );
+
+		it( 'should return a Moment object representing now when given an undefined argument', () => {
+			const wrapper = shallow( <DatePicker /> );
+			const momentDate = wrapper.instance().getMomentDate();
+
+			expect( moment.isMoment( momentDate ) ).toBe( true );
+			expect( momentDate.isSame( moment(), 'second' ) ).toBe( true );
+		} );
+	} );
+
+	describe( 'onChangeMoment', () => {
+		it( 'should call onChange with a formated date of the input', () => {
+			const onChangeSpy = jest.fn();
+			const currentDate = '1986-10-18T11:00:00';
+			const wrapper = shallow( <DatePicker currentDate={ currentDate } onChange={ onChangeSpy } /> );
+			const newDate = moment();
+
+			wrapper.instance().onChangeMoment( newDate );
+
+			expect( onChangeSpy ).toHaveBeenCalledWith( newDate.format( TIMEZONELESS_FORMAT ) );
+		} );
+
+		it( 'should call onChange with hours, minutes, seconds of the current time when currentDate is undefined', () => {
+			const onChangeSpy = jest.fn();
+			const wrapper = shallow( <DatePicker onChange={ onChangeSpy } /> );
+			const newDate = moment( '1986-10-18T11:00:00' );
+			const current = moment();
```

Lets go with `Provide some flexibility in the assertion (i.e. within a +/- allowable timeframe)` for $1000 please, Alex.

I figured comparison on basis of seconds was enough to overcome differences in execution time, but in an effort to prevent future headaches, a change is a good idea.

I changed the tests to save the argument passed to onChange for later comparison using moment's `isSame` function. Then I compare with a granularity of `minute`, which would preclude all but the rarest of circumstances where a pause in execution time ticks into the next second AND minute.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/date-time/README.md (MODIFIED, +3/-2)
- packages/components/src/date-time/date.js (MODIFIED, +17/-2)
- packages/components/src/date-time/test/date.js (ADDED, +110/-0)
