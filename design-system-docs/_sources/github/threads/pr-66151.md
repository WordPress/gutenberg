---
number: 66151
type: pull_request
state: MERGED
merged_at: 2024-10-16T16:26:29Z
url: https://github.com/WordPress/gutenberg/pull/66151
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components","Backported to WP Core"]
---

# PR #66151: ToggleGroupControl: Don't set value on focus after a reset

- URL: https://github.com/WordPress/gutenberg/pull/66151
- Author: mirka
- Created: 2024-10-16T00:03:50Z
- Updated: 2024-10-17T09:56:08Z
- Comments: 5 of 5
- Review threads: 4 of 4
- Changed files: 4 of 4
- Diff: ../pr-diffs/66151.diff

## Body

Bug discovered in https://github.com/WordPress/gutenberg/pull/65386#discussion_r1798762479

## What?

Fixes an incomplete bug fix in #65892 where ToggleGroupControl could still autoselect an option on focus after the selected value was reset by the consumer.

### Testing Instructions for Keyboard

Modify the Storybook story so it has a reset button, like this:

```diff
diff --git a/packages/components/src/toggle-group-control/stories/index.story.tsx b/packages/components/src/toggle-group-control/stories/index.story.tsx
index 92f1e60762..01e5fc4eae 100644
--- a/packages/components/src/toggle-group-control/stories/index.story.tsx
+++ b/packages/components/src/toggle-group-control/stories/index.story.tsx
@@ -48,15 +48,18 @@ const Template: StoryFn< typeof ToggleGroupControl > = ( {
 		useState< ToggleGroupControlProps[ 'value' ] >();
 
 	return (
-		<ToggleGroupControl
-			__nextHasNoMarginBottom
-			{ ...props }
-			onChange={ ( ...changeArgs ) => {
-				setValue( ...changeArgs );
-				onChange?.( ...changeArgs );
-			} }
-			value={ value }
-		/>
+		<>
+			<ToggleGroupControl
+				__nextHasNoMarginBottom
+				{ ...props }
+				onChange={ ( ...changeArgs ) => {
+					setValue( ...changeArgs );
+					onChange?.( ...changeArgs );
+				} }
+				value={ value }
+			/>
+			<button onClick={ () => setValue( undefined ) }>Reset</button>
+		</>
 	);
 };

1. Select a value.
2. Reset the value using the reset button.
3. Using the keyboard, focus back onto the component. A value should not be selected automatically.

```

## Issue comments

### github-actions on 2024-10-16T00:04:09Z

URL: https://github.com/WordPress/gutenberg/pull/66151#issuecomment-2415368927

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-10-16T00:32:19Z

URL: https://github.com/WordPress/gutenberg/pull/66151#issuecomment-2415396168

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 1c8b6895caede98a5f6107250a6277c3b5169ceb.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/11356382384
📝  Reported issues:
- #66051 in `/test/e2e/specs/site-editor/zoom-out.spec.js`

### github-actions on 2024-10-16T16:27:48Z

URL: https://github.com/WordPress/gutenberg/pull/66151#issuecomment-2417328998

There was a conflict while trying to cherry-pick the commit to the wp/6.7 branch. Please resolve the conflict manually and create a PR to the wp/6.7 branch.

  PRs to wp/6.7 are similar to PRs to trunk, but you should base your PR on the wp/6.7 branch instead of trunk.

  ```
  # Checkout the wp/6.7 branch instead of trunk.
  git checkout wp/6.7
  # Create a new branch for your PR.
  git checkout -b my-branch
  # Cherry-pick the commit.
  git cherry-pick b9b633059bb448fa4281c43d8fa8d696ad6881cd
  # Check which files have conflicts.
  git status
  # Resolve the conflict...
  # Add the resolved files to the staging area.
  git status
  git add .
  git cherry-pick --continue
  # Push the branch to the repository
  git push origin my-branch
  # Create a PR and set the base to the wp/6.7 branch.
  # See https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/changing-the-base-branch-of-a-pull-request.
  ```

### ciampo on 2024-10-16T21:58:22Z

URL: https://github.com/WordPress/gutenberg/pull/66151#issuecomment-2418038766

@mirka just a ping in case you didn't notice the failed backport

### mirka on 2024-10-17T09:56:07Z

URL: https://github.com/WordPress/gutenberg/pull/66151#issuecomment-2419086135

Backported at 4a7de72e65f54a13a315492abd2c52988264f514

## Review threads

### Review thread 1

#### mirka on 2024-10-16T00:09:14Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802147786
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -141,10 +141,14 @@ function ToggleGroupControlOptionBase(
 					<Ariakit.Radio
 						disabled={ disabled }
 						onFocusVisible={ () => {
+							const selectedValueIsNullish =
+								toggleGroupControlContext.value === null ||
+								toggleGroupControlContext.value === '';
```

Due to how we add a layer of [`value` conversion](https://github.com/WordPress/gutenberg/blob/26e108deabcb6f58ffdf424f06887efb9f88333c/packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx#L51-L56) in controlled mode, the Ariakit store will hold a `null` value on first render with no value provided, but then an empty string after a consumer resets the `value` to `undefined`.

### Review thread 2

#### mirka on 2024-10-16T00:10:01Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802148199
Path: packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx

```diff
@@ -73,6 +73,13 @@ function UnforwardedToggleGroupControlAsRadioGroup(
 	const selectedValue = useStoreState( radio, 'value' );
 	const setValue = radio.setValue;
 
+	// Ensures that the active id is also reset after the value is "reset" by the consumer.
+	useEffect( () => {
+		if ( selectedValue === '' ) {
+			radio.setActiveId( undefined );
+		}
+	}, [ radio, selectedValue ] );
```

I can feel the tech debt catching up to us 😅

#### tyxla on 2024-10-16T06:54:21Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802466810
Path: packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx

```diff
@@ -73,6 +73,13 @@ function UnforwardedToggleGroupControlAsRadioGroup(
 	const selectedValue = useStoreState( radio, 'value' );
 	const setValue = radio.setValue;
 
+	// Ensures that the active id is also reset after the value is "reset" by the consumer.
+	useEffect( () => {
+		if ( selectedValue === '' ) {
+			radio.setActiveId( undefined );
+		}
+	}, [ radio, selectedValue ] );
```

Food for thought: I wonder if this can be improved with a centralized controlled component layer that all controlled components would use. Probably a too idealistic idea, since some components use Ariakit and others do not, and this layer would be quite complicated to cover all cases.

#### ciampo on 2024-10-16T09:33:23Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802739932
Path: packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx

```diff
@@ -73,6 +73,13 @@ function UnforwardedToggleGroupControlAsRadioGroup(
 	const selectedValue = useStoreState( radio, 'value' );
 	const setValue = radio.setValue;
 
+	// Ensures that the active id is also reset after the value is "reset" by the consumer.
+	useEffect( () => {
+		if ( selectedValue === '' ) {
+			radio.setActiveId( undefined );
+		}
+	}, [ radio, selectedValue ] );
```

I think that the most practical fix here is to change the APIs to follow the standard convention for controlled / uncontrolled component. Since that would be a breaking change, we could just release a V2 of the component that would give us also a chance to:

- implement / test [the new design spec](https://github.com/wordPress/gutenberg/issues/64439) alongside the legacy one
- review the current API strategy around "deselectability" and potentially multiple selection (should they be separate components? Or different subcomponents?)

#### mirka on 2024-10-16T15:49:08Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1803381404
Path: packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx

```diff
@@ -73,6 +73,13 @@ function UnforwardedToggleGroupControlAsRadioGroup(
 	const selectedValue = useStoreState( radio, 'value' );
 	const setValue = radio.setValue;
 
+	// Ensures that the active id is also reset after the value is "reset" by the consumer.
+	useEffect( () => {
+		if ( selectedValue === '' ) {
+			radio.setActiveId( undefined );
+		}
+	}, [ radio, selectedValue ] );
```

For this case specifically, I think there might be room to argue that resetting the active id on empty-ish (or invalid) values should be handled by Ariakit out of the box. I'll try and follow up.

### Review thread 3

#### tyxla on 2024-10-16T06:50:43Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802462734
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -141,10 +141,14 @@ function ToggleGroupControlOptionBase(
 					<Ariakit.Radio
 						disabled={ disabled }
 						onFocusVisible={ () => {
+							const selectedValueIsNullish =
```

Naming nit: `nullish` often refers to `null` or `undefined`, so if someone isn't reading closely, they might be confused and expect this checks for `undefined` and not an empty string.

#### ciampo on 2024-10-16T09:34:44Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802743466
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -141,10 +141,14 @@ function ToggleGroupControlOptionBase(
 					<Ariakit.Radio
 						disabled={ disabled }
 						onFocusVisible={ () => {
+							const selectedValueIsNullish =
```

Maybe `selectedValueIsEmpty` ? Or even inlining the check?

#### tyxla on 2024-10-16T09:58:54Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802781369
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -141,10 +141,14 @@ function ToggleGroupControlOptionBase(
 					<Ariakit.Radio
 						disabled={ disabled }
 						onFocusVisible={ () => {
+							const selectedValueIsNullish =
```

I'd be fine with inlining it 👍

#### mirka on 2024-10-16T15:52:36Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1803386722
Path: packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx

```diff
@@ -141,10 +141,14 @@ function ToggleGroupControlOptionBase(
 					<Ariakit.Radio
 						disabled={ disabled }
 						onFocusVisible={ () => {
+							const selectedValueIsNullish =
```

Good point, I'll go with `selectedValueIsEmpty` here. (It started as an inline condition, but it was hard to parse/understand so I prefer it named.)

### Review thread 4

#### tyxla on 2024-10-16T06:55:27Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1802467998
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -175,6 +175,32 @@ describe.each( [
 		expect( radio ).not.toBeChecked();
 	} );
 
+	if ( mode === 'controlled' ) {
```

Should we move it next to the other `controlled`-only tests?

#### mirka on 2024-10-16T15:54:36Z

URL: https://github.com/WordPress/gutenberg/pull/66151#discussion_r1803390123
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -175,6 +175,32 @@ describe.each( [
 		expect( radio ).not.toBeChecked();
 	} );
 
+	if ( mode === 'controlled' ) {
```

No strong opinion, but I think I prefer it here because it's closely related to the test right above it.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/toggle-group-control/test/index.tsx (MODIFIED, +26/-0)
- packages/components/src/toggle-group-control/toggle-group-control-option-base/component.tsx (MODIFIED, +5/-1)
- packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx (MODIFIED, +8/-1)
