---
number: 14599
type: pull_request
state: MERGED
merged_at: 2019-03-25T13:32:01Z
url: https://github.com/WordPress/gutenberg/pull/14599
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #14599: Fix react warning for instanceId prop on a DOM element (<Button> component)

- URL: https://github.com/WordPress/gutenberg/pull/14599
- Author: nerrad
- Created: 2019-03-24T13:42:49Z
- Updated: 2019-03-25T13:32:05Z
- Comments: 4 of 4
- Review threads: 2 of 2
- Changed files: 4 of 4
- Diff: ../pr-diffs/14599.diff

## Body

## Description
<!-- Please describe what you have changed or added -->

Was getting this warning appear whenever I clicked the menu toggle on a block:

> Warning: React does not recognize the `instanceId` prop on a DOM element. If you intentionally want it to appear in the DOM as a custom attribute, spell it as lowercase `instanceid` instead. If you accidentally passed it from a parent component, remove it from the DOM element.

This results from the `instanceId` prop getting passed through via `<MenuItem />`


## How has this been tested?
<!-- Please describe in detail how you tested your changes. -->
<!-- Include details of your testing environment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->

* [x] Verify unit tests/ e2e tests pass
* [x] Ensure console error doesn't appear in this branch.

## Types of changes
<!-- What types of changes does your code introduce?  -->
<!-- Bug fix (non-breaking change which fixes an issue) -->
<!-- New feature (non-breaking change which adds functionality) -->
<!-- Breaking change (fix or feature that would cause existing functionality to not work as expected) -->

Non-breaking change

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://wordpress.org/gutenberg/handbook/designers-developers/ -->

## Issue comments

### nerrad on 2019-03-24T18:35:21Z

URL: https://github.com/WordPress/gutenberg/pull/14599#issuecomment-475986869

I'm on the fence about this.  An alternative way to approach this would be to review all components wrapped by the `withInstanceId` HOC and ensure they do not pass through the `instanceId` prop to children.  What's unclear to me is whether there is current use-case in GB _for_ passing through the `instanceId` prop, in which case that alternative might not be an option there.

Another alternative might be to create a HOC wrapping all leaf DOM elements to remove any props that shouldn't be a dom element attribute - so something like `withCleanProps`.

### gziolo on 2019-03-25T06:42:20Z

URL: https://github.com/WordPress/gutenberg/pull/14599#issuecomment-476073736

> Another alternative might be to create a HOC wrapping all leaf DOM elements to remove any props that shouldn't be a dom element attribute - so something like `withCleanProps`.

This might be much nicer. However, it might have also some performance implications if you wrap all leaf components with such HOC. I'm wondering whether passing thru props a few levels down isn't the root cause of this in the first place.

### nerrad on 2019-03-25T12:23:39Z

URL: https://github.com/WordPress/gutenberg/pull/14599#issuecomment-476173154

The source of the `instanceId` appears to be the `MenuItem` component which is wrapped by `withInstanceId`.  However, I don't see any actual _use_ of `instanceId` in the MenuItem` component (or in the child components implemented there).  So I'll give a go at simply removing that wrap there.

### nerrad on 2019-03-25T12:39:20Z

URL: https://github.com/WordPress/gutenberg/pull/14599#issuecomment-476178540

So turns out the `<MenuItem>` component did not need `withInstanceId` wrapping it.  Removing that fixes the issue.  This also required updating a couple snapshots in different packages.

## Review threads

### Review thread 1 (resolved)

#### youknowriad on 2019-03-25T07:05:20Z

URL: https://github.com/WordPress/gutenberg/pull/14599#discussion_r268507743
Path: packages/components/src/button/index.js

```diff
@@ -42,9 +43,13 @@ export function Button( props, ref ) {
 	const tag = href !== undefined && ! disabled ? 'a' : 'button';
 	const tagProps = tag === 'a' ? { href, target } : { type: 'button', disabled };
 
+	const passThruProps = additionalProps.instanceId ?
+		omit( additionalProps, [ 'instanceId' ] ) :
```

This assumes this element explicitly uses the `instanceId` prop for me. I think this prop shouldn't be passed at all instead.

#### nerrad on 2019-03-25T12:39:42Z

URL: https://github.com/WordPress/gutenberg/pull/14599#discussion_r268619812
Path: packages/components/src/button/index.js

```diff
@@ -42,9 +43,13 @@ export function Button( props, ref ) {
 	const tag = href !== undefined && ! disabled ? 'a' : 'button';
 	const tagProps = tag === 'a' ? { href, target } : { type: 'button', disabled };
 
+	const passThruProps = additionalProps.instanceId ?
+		omit( additionalProps, [ 'instanceId' ] ) :
```

Yup agreed, and the culprit was `<MenuItem/>`

### Review thread 2

#### youknowriad on 2019-03-25T12:41:09Z

URL: https://github.com/WordPress/gutenberg/pull/14599#discussion_r268620336
Path: packages/components/CHANGELOG.md

```diff
@@ -1,3 +1,9 @@
+7.2.1 (Unreleased)
```

I think this is missing some `##` :)

## Changed files

- packages/block-editor/src/components/block-settings-menu/test/__snapshots__/reusable-block-delete-button.js.snap (MODIFIED, +2/-2)
- packages/components/CHANGELOG.md (MODIFIED, +6/-0)
- packages/components/src/menu-item/index.js (MODIFIED, +1/-2)
- packages/edit-post/src/components/header/plugin-more-menu-item/test/__snapshots__/index.js.snap (MODIFIED, +0/-1)
