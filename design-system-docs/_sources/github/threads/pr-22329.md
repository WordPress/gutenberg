---
number: 22329
type: pull_request
state: MERGED
merged_at: 2020-05-20T19:38:34Z
url: https://github.com/WordPress/gutenberg/pull/22329
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #22329: UnitControl: Improve interactions + add unit parsing

- URL: https://github.com/WordPress/gutenberg/pull/22329
- Author: ItsJonQ
- Created: 2020-05-13T17:52:43Z
- Updated: 2020-09-14T16:12:43Z
- Comments: 14 of 14
- Review threads: 16 of 16
- Changed files: 29 of 29
- Diff: ../pr-diffs/22329.diff

## Body

## Description

![Screen Capture on 2020-05-13 at 13-21-43](https://user-images.githubusercontent.com/2322354/81846628-66897d00-9520-11ea-9e0f-9f6a6b09687a.gif)

(GIF: Demo of dragging UnitControl to update Cover height)

This update adds rich features and interactions to the UnitControl component. Originally, this work was in support of adding [padding controls to the Cover block](https://github.com/WordPress/gutenberg/pull/21492). This update abstracts just the UnitControls from that effort.

Notable features for UnitControl include:

* Drag (mouse) to update values
* Unit parsing from input. Example: `10px` is parsed and understood as `[10, 'px']`
* Improved step increment calculations (holding <kbd>Shift</kbd> when updating)

UnitControl and NumberControl are now powered by a new lower level primitive: InputControl.

InputControl handles the majority of the interaction and event logic. Components using it, like NumberControl, are able to tap into the state flow and add features like "drag to update".

Notable features for InputControl include:

* Floating label UI interaction - Not enabled by default
* Styled with G2 designs, out of the box

## How has this been tested?

* Tested in Storybook
* Tested in local Gutenberg
* Tested in Jest (via unit tests)

## How to test?

### Storybook

* Run `npm install` to download dependencies
* Fire up storybook with `npm run storybook:dev`
* Search for `unit control`
* Play around with the component to ensure it works as expected

### Gutenberg

* Run `npm install` to download dependencies
* Fire up local development with `npm run dev`
* Add a Cover block
* Adjust the height. Ensure it works as expected (Note: Unit parsing feature is not enabled. This would involve updating the Cover block component a bit)

## Types of changes

* Adding experimental components

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

## Issue comments

### github-actions on 2020-05-13T17:58:53Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-628152285

**Size Change:** +9.98 kB (0%) 

**Total Size:** 1.12 MB

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/block-directory/index.js` | 6.93 kB | +1 B |  |
| `build/block-editor/index.js` | 105 kB | +1 B |  |
| `build/components/index.js` | 190 kB | +9.98 kB (5%) | 🔍 |
| `build/data/index.js` | 8.42 kB | -6 B (0%) |  |
| `build/edit-site/index.js` | 12.8 kB | +1 B |  |
| `build/editor/index.js` | 44.3 kB | +2 B (0%) |  |
| `build/element/index.js` | 4.65 kB | -1 B |  |
| `build/redux-routine/index.js` | 2.85 kB | -1 B |  |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/a11y/index.js` | 1.02 kB | 0 B |  |
| `build/annotations/index.js` | 3.62 kB | 0 B |  |
| `build/api-fetch/index.js` | 3.39 kB | 0 B |  |
| `build/autop/index.js` | 2.83 kB | 0 B |  |
| `build/blob/index.js` | 620 B | 0 B |  |
| `build/block-directory/style-rtl.css` | 790 B | 0 B |  |
| `build/block-directory/style.css` | 791 B | 0 B |  |
| `build/block-editor/style-rtl.css` | 10.8 kB | 0 B |  |
| `build/block-editor/style.css` | 10.8 kB | 0 B |  |
| `build/block-library/editor-rtl.css` | 7.17 kB | 0 B |  |
| `build/block-library/editor.css` | 7.17 kB | 0 B |  |
| `build/block-library/index.js` | 119 kB | 0 B |  |
| `build/block-library/style-rtl.css` | 7.48 kB | 0 B |  |
| `build/block-library/style.css` | 7.48 kB | 0 B |  |
| `build/block-library/theme-rtl.css` | 683 B | 0 B |  |
| `build/block-library/theme.css` | 685 B | 0 B |  |
| `build/block-serialization-default-parser/index.js` | 1.88 kB | 0 B |  |
| `build/block-serialization-spec-parser/index.js` | 3.1 kB | 0 B |  |
| `build/blocks/index.js` | 48.1 kB | 0 B |  |
| `build/components/style-rtl.css` | 17.1 kB | 0 B |  |
| `build/components/style.css` | 17.1 kB | 0 B |  |
| `build/compose/index.js` | 9.24 kB | 0 B |  |
| `build/core-data/index.js` | 11.4 kB | 0 B |  |
| `build/data-controls/index.js` | 1.29 kB | 0 B |  |
| `build/date/index.js` | 5.47 kB | 0 B |  |
| `build/deprecated/index.js` | 772 B | 0 B |  |
| `build/dom-ready/index.js` | 568 B | 0 B |  |
| `build/dom/index.js` | 3.11 kB | 0 B |  |
| `build/edit-navigation/index.js` | 6.6 kB | 0 B |  |
| `build/edit-navigation/style-rtl.css` | 857 B | 0 B |  |
| `build/edit-navigation/style.css` | 856 B | 0 B |  |
| `build/edit-post/index.js` | 302 kB | 0 B |  |
| `build/edit-post/style-rtl.css` | 12.2 kB | 0 B |  |
| `build/edit-post/style.css` | 12.2 kB | 0 B |  |
| `build/edit-site/style-rtl.css` | 5.22 kB | 0 B |  |
| `build/edit-site/style.css` | 5.22 kB | 0 B |  |
| `build/edit-widgets/index.js` | 7.73 kB | 0 B |  |
| `build/edit-widgets/style-rtl.css` | 4.59 kB | 0 B |  |
| `build/edit-widgets/style.css` | 4.59 kB | 0 B |  |
| `build/editor/editor-styles-rtl.css` | 425 B | 0 B |  |
| `build/editor/editor-styles.css` | 428 B | 0 B |  |
| `build/editor/style-rtl.css` | 5.07 kB | 0 B |  |
| `build/editor/style.css` | 5.08 kB | 0 B |  |
| `build/escape-html/index.js` | 733 B | 0 B |  |
| `build/format-library/index.js` | 7.64 kB | 0 B |  |
| `build/format-library/style-rtl.css` | 502 B | 0 B |  |
| `build/format-library/style.css` | 502 B | 0 B |  |
| `build/hooks/index.js` | 2.13 kB | 0 B |  |
| `build/html-entities/index.js` | 622 B | 0 B |  |
| `build/i18n/index.js` | 3.56 kB | 0 B |  |
| `build/is-shallow-equal/index.js` | 711 B | 0 B |  |
| `build/keyboard-shortcuts/index.js` | 2.51 kB | 0 B |  |
| `build/keycodes/index.js` | 1.94 kB | 0 B |  |
| `build/list-reusable-blocks/index.js` | 3.13 kB | 0 B |  |
| `build/list-reusable-blocks/style-rtl.css` | 226 B | 0 B |  |
| `build/list-reusable-blocks/style.css` | 226 B | 0 B |  |
| `build/media-utils/index.js` | 5.29 kB | 0 B |  |
| `build/notices/index.js` | 1.79 kB | 0 B |  |
| `build/nux/index.js` | 3.4 kB | 0 B |  |
| `build/nux/style-rtl.css` | 616 B | 0 B |  |
| `build/nux/style.css` | 613 B | 0 B |  |
| `build/plugins/index.js` | 2.56 kB | 0 B |  |
| `build/primitives/index.js` | 1.5 kB | 0 B |  |
| `build/priority-queue/index.js` | 789 B | 0 B |  |
| `build/rich-text/index.js` | 14.8 kB | 0 B |  |
| `build/server-side-render/index.js` | 2.68 kB | 0 B |  |
| `build/shortcode/index.js` | 1.7 kB | 0 B |  |
| `build/token-list/index.js` | 1.28 kB | 0 B |  |
| `build/url/index.js` | 4.02 kB | 0 B |  |
| `build/viewport/index.js` | 1.84 kB | 0 B |  |
| `build/warning/index.js` | 1.14 kB | 0 B |  |
| `build/wordcount/index.js` | 1.17 kB | 0 B |  |


</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### ItsJonQ on 2020-05-20T16:16:51Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-631577592

Thanks for the feedback all! I'll merge this once Travis is green and happy ✨

### ItsJonQ on 2020-05-20T18:29:21Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-631647527

Rebasing with latest `master`

### ItsJonQ on 2020-05-20T18:29:57Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-631647844

Accidentally clicked "Close and comment" 🙈

### ItsJonQ on 2020-05-20T19:21:09Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-631674652

Hmm... looks like Travis has been failing the E2E (not just this PR, but for others as well)

### ItsJonQ on 2020-05-20T19:38:30Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-631683576

Yay! Travis is happy! Merging :D

### ItsJonQ on 2020-05-20T19:42:40Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-631685630

Thanks again everyone! I can't wait to use these 🎉

### afercia on 2020-09-12T12:37:41Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-691481613

Late to the party. I came across this while testing the new Navigation page.

> This is the best I could do given the (surprisingly) limited CSS rendering control over `<fieldset>`.

I'm not sure I understand why a fieldset + legend elements are used for this pattern. They're semantic elements meant to group logically-related form controls. The group name is given by the legend element. Instead, in this case:
- both the fieldset and the legend elements have an `aria-hidden=true` attribute so they're hidden from assistive technologies, thus defeating the elements purpose to be _semantic elements_
- in any case, the fieldset does not group any form element: it's an empty fieldset
- to my understanding, the fieldset is absolutely positioned over the form element in question and uses `pointer-events: none;` to allow click-throughs
- at this point why use a fieldset element in the first place? It could be a simple div element
- if there's no `<label>` element correctly associated to the input, or an `aria-label`, the input will be unlabelled even if there's some "visible text": all form controls need to be properly labelled
- lastly, (noticed while testing the new Navigation page), it seems the fieldset is _always rendered_ even when there's no need for it, see the screenshot below:

<img width="896" alt="Screenshot 2020-09-12 at 13 53 35" src="https://user-images.githubusercontent.com/1682452/92995380-6962a180-f503-11ea-9696-30b0355b3cc7.png">

This is confusing: why we should render a fieldset when it's not needed?

@ItsJonQ can you please shed some light on the reason why a fieldset + legend elements are used here?

Regarding the "floating labels" pattern: in general, anything that "moves" on the screen without user activation is potentially confusing and triggers cognitive load. I'm not sure this pattern should be used without an in-depth discussion between the accessibility and design teams.

In any case, there are better ways to implement "floating labels" that make sure the input is labelled. See for example what GMail does:

<img width="659" alt="floating label gmail visible text and aria label 02" src="https://user-images.githubusercontent.com/1682452/92995535-c4e15f00-f504-11ea-9f01-88f272818128.png">

where it's of fundamental importance that the visible text matches the input aria-label.

### ItsJonQ on 2020-09-14T12:42:19Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-692026476

> This is confusing: why we should render a fieldset when it's not needed?

@afercia Hi there! Thank you for digging in!

I had used `fieldset` as it enabled the borders to render correctly when "floating" the label. I had followed the structure of other examples to achieve this affect. However, as you had noted, there appears to be a better methods (I need to poke at Google/Gmail to figure it out). The current implementation is not worth the degradation of a11y for inputs.

I'll follow up with improvements on this as soon as I can. Would it be okay if I `@mention` you on the PR (when it's available)?

### afercia on 2020-09-14T12:47:03Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-692028871

> Would it be okay if I @mention you on the PR (when it's available)?

Sure. I'd consider to also post a quick heads up on the Slack accessibility channel so that if I'm not available, someone else can provide feedback. Thanks! ❤️

### ItsJonQ on 2020-09-14T12:48:10Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-692029389

@afercia Will do! Will also be sure to tag the PR with "Needs Accessibility Feedback"

### afercia on 2020-09-14T16:04:56Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-692155667

I just noticed that now that the styles from `input-control-styles` are applied to some `<label>` elements, weird things happen as in: clicking the label doesn't focus the associated form element. Will create a separate issue...

### ItsJonQ on 2020-09-14T16:06:58Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-692156875

> I just noticed that now that the styles from input-control-styles are applied to some <label> elements, weird things happen as in: clicking the label doesn't focus the associated form element. Will create a separate issue...

@afercia I noticed this too when working on the fix. This has been resolved! (Good timing, I'm writing the PR as we speak!)

### afercia on 2020-09-14T16:12:43Z

URL: https://github.com/WordPress/gutenberg/pull/22329#issuecomment-692160092

Ah good! I was just writing the new issue :D  Too many `pointer-events: none;`, right? Will wait then and check your PR.

## Review threads

### Review thread 1

#### ZebulanStanphill on 2020-05-13T18:39:23Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r424651865
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

This description seems a bit vague; even I'm not entirely sure what it means. I assume it means the label appears inside the input until the input is focused? Also, is this prop even a good idea, accessibility-wise?

Also, all this type info belongs in a JSDoc comment, doesn't it?

#### ItsJonQ on 2020-05-14T19:07:48Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425368698
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

Thanks for noticing this! I'm not sure how best to describe this interaction/design.

It works like the TextFields from Material UI:
https://material-ui.com/components/text-fields/

From a design perspective, it offers more versatility, especially if controls need to be combined in cramped spaces.

As for as accessibility goes, how its setup, I believe it it acceptable from both a motion perspective and aria label + HTML markup perspective.

cc'ing some a11y folks!

@enriquesanchez / @diegohaz  Are there any concerns for having floating labels in Inputs like these?

<img width="241" alt="Screen Shot 2020-05-14 at 3 07 28 PM" src="https://user-images.githubusercontent.com/2322354/81975175-a7eb5c80-95f4-11ea-8820-de32a2f194cd.png">


https://material-ui.com/components/text-fields/

Thank you!

#### diegohaz on 2020-05-15T22:02:21Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r426066242
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

I don't have strong opinions about that. But there's this article with arguments against floating labels: [Floating labels are problematic](https://medium.com/simple-human/floating-labels-are-a-bad-idea-82edb64220f6)

And this other one is a response to that: [Are float labels really that problematic after all?](http://mds.is/still-floating-labels/)

#### enriquesanchez on 2020-05-18T22:48:14Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r426933005
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

I always check Adam Silver's articles when in doubt about forms and a11y. That first link Diego shared is from him :)

- Sorry if this is obvious, but where is this floating label going to be used? Only on this control or on all input fields?
- I don't see the floating label in the gif example above, but how would it look like when the floating label is longer than the field itself?

#### ItsJonQ on 2020-05-19T16:47:14Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427448847
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

@diegohaz + @enriquesanchez Thanks for replying!

> where is this floating label going to be used? Only on this control or on all input fields?

It's situational! By default, floating labels are **disabled**. They're useful if the input is narrower or used in compact spaces. For example, within a smaller Popover.

> but how would it look like when the floating label is longer than the field itself?

Ah! the GIF above does not have a floating label. The best example of this interaction would be from the Material TextField demo:

https://material-ui.com/components/text-fields/

My screen capture -> GIF software doesn't do a good job at capturing the various stages.

The key frames work like this ✨ 

No value, label works like a placeholder:

<img width="219" alt="Screen Shot 2020-05-19 at 12 45 31 PM" src="https://user-images.githubusercontent.com/2322354/82354318-a8a63900-99ce-11ea-900c-3bc58defa4d0.png">

No value, focused, the label floats up:

<img width="235" alt="Screen Shot 2020-05-19 at 12 46 20 PM" src="https://user-images.githubusercontent.com/2322354/82354419-c5427100-99ce-11ea-8da9-113d02840dfe.png">

With value, the label stays up:
<img width="227" alt="Screen Shot 2020-05-19 at 12 46 27 PM" src="https://user-images.githubusercontent.com/2322354/82354451-d0959c80-99ce-11ea-8b52-2d35eaa752f7.png">

#### ZebulanStanphill on 2020-05-19T17:15:22Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427466497
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

That doesn't really answer the question, though: what happens when the field label is longer than the field itself?

#### ItsJonQ on 2020-05-19T17:24:09Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427472067
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

Ah! Gotcha. I misread. My apologies. There isn't handling of this at the moment.

The Material TextField doesn't handle this use case too well either:
https://codesandbox.io/s/material-demo-e49dz

I can try some truncation techniques really quickly. I don't think this should be a blocker though.

#### ZebulanStanphill on 2020-05-19T17:31:03Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427476678
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

The big issue is that input labels have to be translated, and some languages will use much longer words than others. An input with a floating label of "left" might be correctly sized, but once you translate that to another language, it may suddenly be twice as long.

WordPress generally prefers decisions over options. If there's not a really good reason to provide an alternate input style, we shouldn't provide it. I think it's okay to merge this PR since the `InputControl` will be experimental, but I don't think we should change the component to stable until we know whether the floating label options should be kept or not.

#### ItsJonQ on 2020-05-19T18:01:19Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427496238
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

![Screen Capture on 2020-05-19 at 13-52-02](https://user-images.githubusercontent.com/2322354/82361425-d2fcf400-99d8-11ea-9c53-4da6ffafdcd6.gif)

This is the best I could do given the (surprisingly) limited CSS rendering control over `<fieldset>`.

> I don't think we should change the component to stable until we know whether the floating label options should be kept or not

Yup! I agree. We can also drop the floating label feature if it's causing issues.

#### enriquesanchez on 2020-05-19T22:19:29Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427633176
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

Thanks @ItsJonQ! I was trying to make a point with my question, apologies if it wasn't clear 😃.

I feel that at this point there's some uncertainty re: floating labels (a11y, localization, placement, etc.) that my recommendation is to stick to regular ol' fashioned text labels.

#### ItsJonQ on 2020-05-20T13:34:50Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428014771
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
+
+```js
+import { __experimentalInputControl as InputControl } from '@wordpress/components';
+import { useState } from '@wordpress/compose';
+
+const Example = () => {
+	const [ value, setValue ] = useState( '' );
+
+	return (
+		<InputControl
+			value={ value }
+			onChange={ ( nextValue ) => setValue( nextValue ) }
+		/>
+	);
+};
+```
+
+## Props
+
+### disabled
+
+If true, the `input` will be disabled.
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isPressEnterToChange
+
+If true, the `ENTER` key press is required in order to trigger an `onChange`. If enabled, a change is also triggered when tabbing away (`onBlur`).
+
+-   Type: `Boolean`
+-   Required: No
+-   Default: `false`
+
+### isFloatingLabel
+
+If true, the `label` will render with a floating interaction.
```

> I feel that at this point there's some uncertainty re: floating labels (a11y, localization, placement, etc.) that my recommendation is to stick to regular ol' fashioned text labels.

I agree! Ol' fashion text labels are **default** 😊 

The floating labels are a new feature, which can be experimentally trialed in certain scenarios

### Review thread 2

#### ZebulanStanphill on 2020-05-13T18:55:20Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r424661468
Path: packages/components/src/input-control/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
```

I've seen this code pattern used in several places, but given that we can now use optional chaining, I think that instead of doing this, we could just let the functions be `undefined` by default. I'm just guessing (and could be completely wrong about this), but it seems like that would perform slightly better. To avoid calling functions that are `undefined`, we would use optional chaining like so:

```js
onBlur?.( event )`
```

Again, this is just an idea; it probably doesn't matter much either way.

#### ItsJonQ on 2020-05-14T18:19:00Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425341162
Path: packages/components/src/input-control/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
```

Interesting idea! I thought about it for a bit (considering optional chaining is a new thing)

From my experience in the React library/module space, I've found it smoother for callbacks to fire without guards. It kept the code tidier and there was less to think about.

That being said, optional chaining does tidy it up a lot.

I'm perhaps biased in that I still prefer `noop` callbacks.
I'm open to using optional chaining instead, if that's what others prefer 👍

### Review thread 3 (resolved)

#### mcsf on 2020-05-14T09:24:39Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r424995936
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
```

I really don't know how hot this path is in practice, but if it turns out to be hot it could be best to run `CSS_UNITS` through `lodash#keyBy`, so as to avoid `units.find`-type traversals.

#### ItsJonQ on 2020-05-14T18:28:48Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425346663
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
```

I didn't know about `keyBy`! Just curious, what is the advantage between the approaches? (Asking to learn. Totally open to changing it 😊 )

#### mcsf on 2020-05-20T10:31:00Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427906562
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
```

Ah, well, the idea was that you'd have a plain JS object as returned by `keyBy`:

```js
keyBy(
  [
    { value: 'px', label: 'px', default: 0 },
    { value: '%', label: '%', default: 10 },
  ],
  'value'
)
// returns
{
  px: { value: 'px', label: 'px', default: 0 },
  '%': { value: '%', label: '%', default: 10 }
}
```

which would be handy when directly accessing properties: `obj[ value ]`, in contrast with `arr.find( … )`.

Again, it's not a blocker, but something to consider depending on how frequently this will run!

#### ZebulanStanphill on 2020-05-20T13:57:57Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428033642
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
```

If it does turn out to be a performance concern, perhaps we ought to be keying them from the start when the units are added.

#### ItsJonQ on 2020-05-20T14:58:24Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428081977
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
```

@mcsf Thank you for the context! It doesn't run super often (not on every keystroke). Still, I think it's a worth while perf boost!

### Review thread 4 (resolved)

#### mcsf on 2020-05-14T09:27:08Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r424997502
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
+		unit = match?.value;
+	}
+
+	output[ 0 ] = isNaN( num ) ? '' : num;
+	output[ 1 ] = unit;
+
+	return output;
```

I get the feeling that, the way `output` is being set here, it might be the remnants of a different implementation. In particular, the initial assignment at the beginning of the function (`output = [ 0, '' ]`) is never used. Seems we can directly `return [ ... ]` and simplify a bit.

#### ItsJonQ on 2020-05-14T18:30:55Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425347810
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
+
+	const num = parseFloat( value, 10 );
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
+	unit = unit.toLowerCase();
+
+	if ( hasUnits( units ) ) {
+		const match = units.find( ( item ) => item.value === unit );
+		unit = match?.value;
+	}
+
+	output[ 0 ] = isNaN( num ) ? '' : num;
+	output[ 1 ] = unit;
+
+	return output;
```

Thank you! Will update

### Review thread 5 (resolved)

#### mcsf on 2020-05-14T09:29:07Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r424998840
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
```

Minor, but `.trim` is unnecessary, given the subsequent handling by `parseFloat` and `String#match`.

#### ItsJonQ on 2020-05-14T18:36:23Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425350844
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,93 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const output = [ 0, '' ];
+	const value = String( initialValue ).trim();
```

I just tested it out! It turns out `.trim()` is needed as `value` is used later for unit parsing.

### Review thread 6 (resolved)

#### mcsf on 2020-05-14T09:33:26Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425001438
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
```

Is this comment up to date? :)

#### ItsJonQ on 2020-05-14T18:37:34Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425351428
Path: packages/components/src/input-control/README.md

```diff
@@ -0,0 +1,100 @@
+# InputControl
+
+InputControl components let users enter and edit text. This is an experimental component intended to (in time) merge with or replace [TextControl](../text-control).
+
+## Usage
+
+Render a user interface to input the name of an additional css class.
```

Oops! Forgot to remove this from copy/paste 🙈 

Thank you!

### Review thread 7 (resolved)

#### mcsf on 2020-05-14T11:03:11Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425052747
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,253 @@
+/**
+ * External dependencies
+ */
+import { flattenDeep } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...any} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const combineStateReducers = ( ...fns ) => {
```

Looking at this with a little more distance, it's worth pointing out that this is very similar to a function composition, to the point that maybe it ought to be named `composeReducers`. In the traditional `combineReducers`, the order doesn't matter, as each subreducer gets its own branch in the resulting reducer. In contrast, here order does matter in that a subreducer can squash another's state.

#### ItsJonQ on 2020-05-14T18:56:07Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425362099
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,253 @@
+/**
+ * External dependencies
+ */
+import { flattenDeep } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...any} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const combineStateReducers = ( ...fns ) => {
```

Oo! Interesting observation. I like this a lot! Will make that change

### Review thread 8 (resolved)

#### mcsf on 2020-05-14T11:04:30Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425053364
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,253 @@
+/**
+ * External dependencies
+ */
+import { flattenDeep } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...any} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const combineStateReducers = ( ...fns ) => {
+	// Using lodash.flattenDeep until we have an Array.flat polyfill.
+	const reducers = flattenDeep( fns );
```

Should we keep this flattening? Do you have concrete examples of when you'd want to traverse a deep list of reducers? Otherwise we might keep a stricter interface and only take `...fns` with `fns :: Array<Reducer>`.

I think being strict about this idea that reducers are _linear input_ rather than tree-like is also more consistent with my above comment on `composeReducers`. Saves us and API consumers from confusion. :)

#### ItsJonQ on 2020-05-14T18:54:28Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425361101
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,253 @@
+/**
+ * External dependencies
+ */
+import { flattenDeep } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...any} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const combineStateReducers = ( ...fns ) => {
+	// Using lodash.flattenDeep until we have an Array.flat polyfill.
+	const reducers = flattenDeep( fns );
```

Ah, good catch! Yes, since we're explicitly using it between the layers (example below), we no longer need to flatten the reducer args.

```jsx
<Input
	__unstableStateReducer={ combineStateReducers(
		numberControlStateReducer,
		stateReducer
	) }
/>
```

### Review thread 9 (resolved)

#### ZebulanStanphill on 2020-05-14T19:20:26Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425375126
Path: packages/components/src/unit-control/utils.js

```diff
@@ -6,3 +11,91 @@ export const CSS_UNITS = [
 	{ value: 'vw', label: 'vw', default: 10 },
 	{ value: 'vh', label: 'vh', default: 10 },
 ];
+
+export const DEFAULT_UNIT = CSS_UNITS[ 0 ];
+
+/**
+ * Handles legacy value + unit handling.
+ * This component use to manage both incoming value and units separately.
+ *
+ * Moving forward, ideally the value should be a string that contains both
+ * the value and unit, example: '10px'
+ *
+ * @param {number|string} value Value
+ * @param {string} unit Unit value
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function getParsedValue( value, unit, units ) {
+	const initialValue = unit ? `${ value }${ unit }` : value;
+
+	return parseUnit( initialValue, units );
+}
+
+/**
+ * Checks if units are defined.
+ *
+ * @param {any} units Units to check.
+ * @return {boolean} Whether units are defined.
+ */
+export function hasUnits( units ) {
+	return ! isEmpty( units ) && units.length > 1 && units !== false;
+}
+
+/**
+ * Parses a number and unit from a value.
+ *
+ * @param {string} initialValue Value to parse
+ * @param {Array<Object>} units Units to derive from.
+ * @return {Array<number, string>} The extracted number and unit.
+ */
+export function parseUnit( initialValue, units = CSS_UNITS ) {
+	const value = String( initialValue ).trim();
+
+	let num = parseFloat( value, 10 );
+	num = isNaN( num ) ? '' : num;
+
+	const unitMatch = value.match( /[\d.\-\+]*\s*(.*)/ )[ 1 ];
+
+	let unit = unitMatch !== 'undefined' ? unitMatch : '';
```

Shouldn't this be:
```suggestion
	let unit = unitMatch !== undefined ? unitMatch : '';
```

### Review thread 10 (resolved)

#### ZebulanStanphill on 2020-05-14T19:24:57Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425377494
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
```

Components should have [JSDoc comments with type info](https://developer.wordpress.org/block-editor/contributors/develop/coding-guidelines/#documenting-wordpresselement-react-components).

#### ItsJonQ on 2020-05-14T19:32:33Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425381281
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
```

Just to confirm, it sounds like we need to add this to every component, correct?

Looking at previous components, there doesn't appear to be many examples of JSDoc comments:

https://github.com/WordPress/gutenberg/blob/master/packages/components/src/select-control/index.js

I'm guessing the goal is to add these over time to increase doc coverage?

#### mcsf on 2020-05-20T10:37:30Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427909901
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
```

When dealing with _private_ components such as Backdrop, for now, I wouldn't let this block development.

### Review thread 11 (resolved)

#### ZebulanStanphill on 2020-05-14T19:26:18Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425378222
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
```

What's the difference between these two?

#### ItsJonQ on 2020-05-14T19:29:23Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425379748
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
```

One tracks the setting preference (and validity) of it via the `isFloatingLabel` prop. The other tracks the floating state.

The varying combinations adjust the styles for the HTML elements

#### ZebulanStanphill on 2020-05-14T19:34:04Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425382071
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
```

Perhaps `isFloatingLabel` should be renamed to `useFloatingLabel` or `floatingLabel` or something like that?

#### ItsJonQ on 2020-05-14T19:35:28Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425382824
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
```

I don't think we can use the `use` prefix, since that's associated with hooks. I think ESLint might actually complain.

Prefixing with `is` typically indicates a `boolean` value, which this one is.

#### ZebulanStanphill on 2020-05-14T19:37:58Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425384159
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
```

How about `isFloatingLabelStyle` or `isFloatingLabelType`?

#### ItsJonQ on 2020-05-14T19:57:54Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425394985
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
```

Hmm.. I'm finding those names trickier to understand. I'm not sure what the best names may be.

At the moment, both props are shared throughout various sub-components of `InputControl` for both functionality and aesthetics. Because of that, they are consistent within `InputControl`. Unless the current names are overly confusing or broken, I think it may be simpler to keep them.

### Review thread 12 (resolved)

#### ZebulanStanphill on 2020-05-14T19:26:59Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425378589
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
+	isFocused,
```

Should this have a default argument?

#### ItsJonQ on 2020-05-14T19:33:48Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r425381919
Path: packages/components/src/input-control/backdrop.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * WordPress dependencies
+ */
+import { memo } from '@wordpress/element';
+/**
+ * Internal dependencies
+ */
+import { Fieldset, Legend, LegendText } from './styles/input-control-styles';
+
+function Backdrop( {
+	disabled = false,
+	isFloating = false,
+	isFloatingLabel = false,
+	isFocused,
```

Ah yes. Thank you for catching

### Review thread 13 (resolved)

#### mcsf on 2020-05-20T10:43:46Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427913058
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,246 @@
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...Function} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const composeStateReducers = ( ...fns ) => {
+	return ( ...args ) => {
+		return fns.reduceRight( ( state, fn ) => {
+			return { ...state, ...fn( ...args ) };
```

Consider optimising this by making sure that `fn( ...args )` actually returns a non-empty object before concatenating with `state` in a new object. Otherwise, we can avoid returning new references. Something like:

```js
const fnState = fn( ...args );
return isEmpty( fnState ) ? state : { ...state, ...fnState };
```

Whether you'd like to tackle this now or as a separate task, it's your call. :) It may also depend on actual usage of `composeStateReducers`.

#### ItsJonQ on 2020-05-20T14:49:15Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428074601
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,246 @@
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...Function} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const composeStateReducers = ( ...fns ) => {
+	return ( ...args ) => {
+		return fns.reduceRight( ( state, fn ) => {
+			return { ...state, ...fn( ...args ) };
```

I think this makes sense! Thank you for your suggestion! I just tested, and it seems to work as expected 👍

### Review thread 14 (resolved)

#### mcsf on 2020-05-20T10:45:11Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427913766
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,246 @@
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...Function} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const composeStateReducers = ( ...fns ) => {
+	return ( ...args ) => {
+		return fns.reduceRight( ( state, fn ) => {
+			return { ...state, ...fn( ...args ) };
+		}, {} );
+	};
+};
+
+/**
+ * Creates a reducer that opens the channel for external state subscription
+ * and modification.
+ *
+ * This technique uses the "stateReducer" design pattern:
+ * https://kentcdodds.com/blog/the-state-reducer-pattern/
+ *
+ * @param {Function} combinedStateReducers A custom reducer that can subscribe and modify state.
+ * @return {Function} The reducer.
+ */
+function inputControlStateReducer( combinedStateReducers ) {
+	return ( state, action ) => {
+		const nextState = { ...state };
+		const { type, payload } = action;
+
+		switch ( type ) {
+			/**
+			 * Keyboard events
+			 */
+			case actionTypes.PRESS_UP:
+				nextState.isDirty = false;
+				break;
+
+			case actionTypes.PRESS_DOWN:
+				nextState.isDirty = false;
+				break;
+
+			/**
+			 * Drag events
+			 */
+			case actionTypes.DRAG_START:
+				nextState.isDragging = true;
+				break;
+
+			case actionTypes.DRAG_END:
+				nextState.isDragging = false;
+				break;
+
+			/**
+			 * Input events
+			 */
+			case actionTypes.CHANGE:
+				nextState.error = null;
+				nextState.value = payload.value;
+
+				if ( state.isPressEnterToChange ) {
+					nextState.isDirty = true;
+				}
+
+				break;
+
+			case actionTypes.COMMIT:
+				nextState.value = payload.value;
+				nextState.isDirty = false;
+				break;
+
+			case actionTypes.RESET:
+				nextState.error = null;
+				nextState.isDirty = false;
+				nextState.value = payload.value || state.initialValue;
+				break;
+
+			case actionTypes.UPDATE:
+				if ( payload.value !== state.value ) {
+					nextState.value = payload.value;
+					nextState.isDirty = false;
+				}
+				break;
+
+			/**
+			 * Validation
+			 */
+			case actionTypes.INVALIDATE:
+				nextState.error = payload.error;
+				break;
+		}
+
+		if ( payload.event ) {
+			nextState._event = payload.event;
+		}
+
+		/**
+		 * Send the nextState + action to the combinedReducers via
+		 * this "bridge" mechanism. This allows external stateReducers
+		 * to hook into actions, and modify state if needed.
+		 */
+		return combinedStateReducers( nextState, action );
+	};
+}
+
+/**
+ * A custom hook that connects and external stateReducer with an internal
+ * reducer. This hook manages the internal state of InputControl.
+ * However, by connecting an external stateReducer function, other
+ * components can react to actions as well as modify state before it is
+ * applied.
+ *
+ * This technique uses the "stateReducer" design pattern:
+ * https://kentcdodds.com/blog/the-state-reducer-pattern/
+ *
+ * @param {Function} stateReducer An external state reducer.
+ * @param {Object} initialState The initial state for the reducer.
+ * @return {Object} State, dispatch, and a collection of actions.
+ */
+export function useInputControlStateReducer(
+	stateReducer = initialStateReducer,
+	initialState = initialInputControlState
+) {
+	const combinedReducers = composeStateReducers( stateReducer );
```

Seems like this is no longer needed, there's nothing to compose.

#### ItsJonQ on 2020-05-20T14:36:41Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428064690
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,246 @@
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
+ * the pipeline to control the flow for state and actions.
+ *
+ * @param  {...Function} fns State reducers.
+ * @return {Function} The single combined stateReducer.
+ */
+export const composeStateReducers = ( ...fns ) => {
+	return ( ...args ) => {
+		return fns.reduceRight( ( state, fn ) => {
+			return { ...state, ...fn( ...args ) };
+		}, {} );
+	};
+};
+
+/**
+ * Creates a reducer that opens the channel for external state subscription
+ * and modification.
+ *
+ * This technique uses the "stateReducer" design pattern:
+ * https://kentcdodds.com/blog/the-state-reducer-pattern/
+ *
+ * @param {Function} combinedStateReducers A custom reducer that can subscribe and modify state.
+ * @return {Function} The reducer.
+ */
+function inputControlStateReducer( combinedStateReducers ) {
+	return ( state, action ) => {
+		const nextState = { ...state };
+		const { type, payload } = action;
+
+		switch ( type ) {
+			/**
+			 * Keyboard events
+			 */
+			case actionTypes.PRESS_UP:
+				nextState.isDirty = false;
+				break;
+
+			case actionTypes.PRESS_DOWN:
+				nextState.isDirty = false;
+				break;
+
+			/**
+			 * Drag events
+			 */
+			case actionTypes.DRAG_START:
+				nextState.isDragging = true;
+				break;
+
+			case actionTypes.DRAG_END:
+				nextState.isDragging = false;
+				break;
+
+			/**
+			 * Input events
+			 */
+			case actionTypes.CHANGE:
+				nextState.error = null;
+				nextState.value = payload.value;
+
+				if ( state.isPressEnterToChange ) {
+					nextState.isDirty = true;
+				}
+
+				break;
+
+			case actionTypes.COMMIT:
+				nextState.value = payload.value;
+				nextState.isDirty = false;
+				break;
+
+			case actionTypes.RESET:
+				nextState.error = null;
+				nextState.isDirty = false;
+				nextState.value = payload.value || state.initialValue;
+				break;
+
+			case actionTypes.UPDATE:
+				if ( payload.value !== state.value ) {
+					nextState.value = payload.value;
+					nextState.isDirty = false;
+				}
+				break;
+
+			/**
+			 * Validation
+			 */
+			case actionTypes.INVALIDATE:
+				nextState.error = payload.error;
+				break;
+		}
+
+		if ( payload.event ) {
+			nextState._event = payload.event;
+		}
+
+		/**
+		 * Send the nextState + action to the combinedReducers via
+		 * this "bridge" mechanism. This allows external stateReducers
+		 * to hook into actions, and modify state if needed.
+		 */
+		return combinedStateReducers( nextState, action );
+	};
+}
+
+/**
+ * A custom hook that connects and external stateReducer with an internal
+ * reducer. This hook manages the internal state of InputControl.
+ * However, by connecting an external stateReducer function, other
+ * components can react to actions as well as modify state before it is
+ * applied.
+ *
+ * This technique uses the "stateReducer" design pattern:
+ * https://kentcdodds.com/blog/the-state-reducer-pattern/
+ *
+ * @param {Function} stateReducer An external state reducer.
+ * @param {Object} initialState The initial state for the reducer.
+ * @return {Object} State, dispatch, and a collection of actions.
+ */
+export function useInputControlStateReducer(
+	stateReducer = initialStateReducer,
+	initialState = initialInputControlState
+) {
+	const combinedReducers = composeStateReducers( stateReducer );
```

Ah! Good catch 🤗 . Will push a fix for this

### Review thread 15 (resolved)

#### mcsf on 2020-05-20T10:46:56Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r427914624
Path: packages/components/src/input-control/state.js

```diff
@@ -0,0 +1,246 @@
+/**
+ * WordPress dependencies
+ */
+import { useReducer } from '@wordpress/element';
+
+const initialStateReducer = ( state ) => state;
+
+const initialInputControlState = {
+	_event: {},
+	error: null,
+	initialValue: '',
+	isDirty: false,
+	isDragEnabled: false,
+	isDragging: false,
+	isPressEnterToChange: false,
+	value: '',
+};
+
+const actionTypes = {
+	CHANGE: 'CHANGE',
+	COMMIT: 'COMMIT',
+	DRAG_END: 'DRAG_END',
+	DRAG_START: 'DRAG_START',
+	DRAG: 'DRAG',
+	INVALIDATE: 'INVALIDATE',
+	PRESS_DOWN: 'PRESS_DOWN',
+	PRESS_ENTER: 'PRESS_ENTER',
+	PRESS_UP: 'PRESS_UP',
+	RESET: 'RESET',
+	UPDATE: 'UPDATE',
+};
+
+export const inputControlActionTypes = actionTypes;
+
+/**
+ * Prepares initialState for the reducer.
+ *
+ * @param {Object} initialState The initial state.
+ * @return {Object} Prepared initialState for the reducer
+ */
+function mergeInitialState( initialState = initialInputControlState ) {
+	const { value } = initialState;
+
+	return {
+		...initialInputControlState,
+		...initialState,
+		initialValue: value,
+	};
+}
+
+/**
+ * Combines multiple stateReducers into a single stateReducer, building
```

Minor point: in this function and the rest of the file I'd replace all mentions of "combine", as they may become ambiguous. In some cases, "compose" is apt; in others, I think we can drop the adjectives altogether, e.g. `const reducer = composeStateReducers( fooReducer, barReducer )`.

### Review thread 16

#### mcsf on 2020-05-20T16:19:42Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428141131
Path: packages/components/src/unit-control/utils.js

```diff
@@ -61,7 +61,7 @@ export function parseUnit( initialValue, units = CSS_UNITS ) {
 	unit = unit.toLowerCase();
 
 	if ( hasUnits( units ) ) {
-		const match = units.find( ( item ) => item.value === unit );
+		const match = keyBy( units, 'value' )[ unit ];
```

@ItsJonQ: Ah, I wasn't clear, but I meant that a keyed version of `units` could be kept where it can be reused — so possibly as a module constant. Otherwise we're still iterating over the collection (and in this case doing extra work) every time!

#### ItsJonQ on 2020-05-20T16:24:48Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428144442
Path: packages/components/src/unit-control/utils.js

```diff
@@ -61,7 +61,7 @@ export function parseUnit( initialValue, units = CSS_UNITS ) {
 	unit = unit.toLowerCase();
 
 	if ( hasUnits( units ) ) {
-		const match = units.find( ( item ) => item.value === unit );
+		const match = keyBy( units, 'value' )[ unit ];
```

@mcsf Ah, gotcha! It's a little tricky, as the units coming in may not be constant. Ultimately, this is determined by the `add_theme_support` config within the context of Gutenberg.

We could memoize it? I'm wondering if it's worth it, considering this operation is quite small (within the grand scheme of things)

#### mcsf on 2020-05-20T17:19:45Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428180117
Path: packages/components/src/unit-control/utils.js

```diff
@@ -61,7 +61,7 @@ export function parseUnit( initialValue, units = CSS_UNITS ) {
 	unit = unit.toLowerCase();
 
 	if ( hasUnits( units ) ) {
-		const match = units.find( ( item ) => item.value === unit );
+		const match = keyBy( units, 'value' )[ unit ];
```

We could memoise, yeah, but I don't want to take more of your time on this now, so I suggest reverting to the initial array and `find` situation, and we can improve from there. :)

#### ItsJonQ on 2020-05-20T17:50:31Z

URL: https://github.com/WordPress/gutenberg/pull/22329#discussion_r428198470
Path: packages/components/src/unit-control/utils.js

```diff
@@ -61,7 +61,7 @@ export function parseUnit( initialValue, units = CSS_UNITS ) {
 	unit = unit.toLowerCase();
 
 	if ( hasUnits( units ) ) {
-		const match = units.find( ( item ) => item.value === unit );
+		const match = keyBy( units, 'value' )[ unit ];
```

Sounds good! Thank you @mcsf !!

## Changed files

- docs/manifest.json (MODIFIED, +6/-0)
- package-lock.json (MODIFIED, +6/-0)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/input-control/README.md (ADDED, +98/-0)
- packages/components/src/input-control/backdrop.js (ADDED, +44/-0)
- packages/components/src/input-control/index.js (ADDED, +136/-0)
- packages/components/src/input-control/input-field.js (ADDED, +212/-0)
- packages/components/src/input-control/label.js (ADDED, +28/-0)
- packages/components/src/input-control/state.js (ADDED, +249/-0)
- packages/components/src/input-control/stories/index.js (ADDED, +56/-0)
- packages/components/src/input-control/styles/input-control-styles.js (ADDED, +358/-0)
- packages/components/src/input-control/test/index.js (ADDED, +116/-0)
- packages/components/src/input-control/utils.js (ADDED, +74/-0)
- packages/components/src/number-control/README.md (MODIFIED, +57/-6)
- packages/components/src/number-control/index.js (MODIFIED, +155/-52)
- packages/components/src/number-control/stories/index.js (MODIFIED, +17/-3)
- packages/components/src/number-control/styles/number-control-styles.js (ADDED, +25/-0)
- packages/components/src/number-control/test/index.js (MODIFIED, +102/-25)
- packages/components/src/number-control/utils.js (ADDED, +88/-0)
- packages/components/src/unit-control/README.md (MODIFIED, +77/-21)
- packages/components/src/unit-control/index.js (MODIFIED, +136/-36)
- packages/components/src/unit-control/stories/index.js (MODIFIED, +10/-6)
- packages/components/src/unit-control/styles/unit-control-styles.js (MODIFIED, +43/-58)
- packages/components/src/unit-control/test/index.js (MODIFIED, +105/-12)
- packages/components/src/unit-control/unit-select-control.js (MODIFIED, +12/-5)
- packages/components/src/unit-control/utils.js (MODIFIED, +93/-0)
- packages/components/src/utils/colors-values.js (MODIFIED, +6/-0)
- packages/components/src/utils/style-mixins.js (MODIFIED, +1/-1)
