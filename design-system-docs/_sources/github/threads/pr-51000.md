---
number: 51000
type: pull_request
state: MERGED
merged_at: 2023-05-26T14:47:27Z
url: https://github.com/WordPress/gutenberg/pull/51000
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Automated Testing","[Package] Components","[Package] E2E Tests"]
---

# PR #51000: Button: Add basic visual regression tests

- URL: https://github.com/WordPress/gutenberg/pull/51000
- Author: mirka
- Created: 2023-05-26T09:53:59Z
- Updated: 2023-06-07T14:40:01Z
- Comments: 0 of 0
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/51000.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?

Adds some basic visual regression tests for the Button component.

## Why?

The Button component is particularly complicated, so we want more confidence when making style changes.

## How?

Given the number of variants and modifiers this component has, brute force checking all permutations is not cost effective. Plus, some combinations of variants/modifiers are non-sensical or broken to begin with. Instead, I suggest we be strategic about what and how we test, taking into account which combinations are likely to occur, or which combinations are likely to break.

To start out, I want to test the basic variant/modifier combinations and their sizes. A matrix is rendered in a single story for efficiency.

<img width="640" alt="A matrix of Button variants and state modifiers, with text content" src="https://github.com/WordPress/gutenberg/assets/555336/c9a79e15-e1b3-44ab-b942-c7c46b4ac853">
<img width="237" alt="A matrix of Button variants and state modifiers, with icon content" src="https://github.com/WordPress/gutenberg/assets/555336/7aa03346-bab1-48e5-8f47-f94bf2083185">

We'll continue to add more stories as needed.

## Testing Instructions

1. Check out `trunk`.
1. `npm run storybook:e2e:dev` to build and serve the E2E Storybook. Keep this running while you're testing.
1. Run `npm run test:e2e:storybook -- --update-snapshots`.
1. Check out this PR branch.
1. Run `npm run test:e2e:storybook` a couple times to verify that everything is still passing and there is no flakiness.

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### ciampo on 2023-05-26T14:11:22Z

URL: https://github.com/WordPress/gutenberg/pull/51000#discussion_r1206847761
Path: test/storybook-playwright/utils.ts

```diff
@@ -99,5 +100,7 @@ export const testSnapshotForPropsConfig = async (
 
 	await submitButton.click();
 
-	expect( await page.screenshot() ).toMatchSnapshot();
+	expect(
+		await page.screenshot( { animations: 'disabled' } )
```

Nice gotcha! I hope this didn't give you too much of a headache debugging why `isBusy` buttons were failing the tests 😅

### Review thread 2

#### ciampo on 2023-05-26T14:11:32Z

URL: https://github.com/WordPress/gutenberg/pull/51000#discussion_r1206848045
Path: test/storybook-playwright/utils.ts

```diff
@@ -62,9 +62,10 @@ export const getAllPropsPermutations = (
 
 		// Test all values for the given prop.
 		for ( const value of propObject.valuesToTest ) {
+			const valueAsString = value === undefined ? 'undefined' : value;
 			const newAccProps = {
 				...accProps,
-				[ propObject.propName ]: value,
+				[ propObject.propName ]: valueAsString,
```

👌

### Review thread 3

#### ciampo on 2023-05-26T14:14:22Z

URL: https://github.com/WordPress/gutenberg/pull/51000#discussion_r1206852811
Path: test/storybook-playwright/specs/button.spec.ts

```diff
@@ -0,0 +1,57 @@
+/**
+ * External dependencies
+ */
+import { expect, test } from '@playwright/test';
+
+/**
+ * Internal dependencies
+ */
+import {
+	gotoStoryId,
+	getAllPropsPermutations,
+	testSnapshotForPropsConfig,
+} from '../utils';
+
+test.describe( 'Button', () => {
+	test.describe( 'variant states', () => {
+		test.beforeEach( async ( { page } ) => {
+			gotoStoryId( page, 'components-button--variant-states', {
+				decorators: { customE2EControls: 'show' },
+			} );
+		} );
+
+		getAllPropsPermutations( [
+			{
+				propName: '__next40pxDefaultSize',
+				valuesToTest: [ true, false ],
+			},
+		] ).forEach( ( propsConfig ) => {
+			test( `should render with ${ JSON.stringify(
+				propsConfig
+			) }`, async ( { page } ) => {
+				await testSnapshotForPropsConfig( page, propsConfig );
+			} );
+		} );
+	} );
+
+	test.describe( 'icon', () => {
+		test.beforeEach( async ( { page } ) => {
+			gotoStoryId( page, 'components-button--icon', {
+				decorators: { customE2EControls: 'show' },
+			} );
+		} );
+
+		getAllPropsPermutations( [
+			{
+				propName: '__next40pxDefaultSize',
+				valuesToTest: [ true, false ],
+			},
+		] ).forEach( ( propsConfig ) => {
+			test( `should render with ${ JSON.stringify(
+				propsConfig
+			) }`, async ( { page } ) => {
+				await testSnapshotForPropsConfig( page, propsConfig );
```

In other test specs (ie. `HStack`), I had added `await page.waitForSelector( /* component's selector */ );`

Is that superfluous, or should we add it in this test too, for increased test robustness ?

#### mirka on 2023-05-26T14:36:04Z

URL: https://github.com/WordPress/gutenberg/pull/51000#discussion_r1206884261
Path: test/storybook-playwright/specs/button.spec.ts

```diff
@@ -0,0 +1,57 @@
+/**
+ * External dependencies
+ */
+import { expect, test } from '@playwright/test';
+
+/**
+ * Internal dependencies
+ */
+import {
+	gotoStoryId,
+	getAllPropsPermutations,
+	testSnapshotForPropsConfig,
+} from '../utils';
+
+test.describe( 'Button', () => {
+	test.describe( 'variant states', () => {
+		test.beforeEach( async ( { page } ) => {
+			gotoStoryId( page, 'components-button--variant-states', {
+				decorators: { customE2EControls: 'show' },
+			} );
+		} );
+
+		getAllPropsPermutations( [
+			{
+				propName: '__next40pxDefaultSize',
+				valuesToTest: [ true, false ],
+			},
+		] ).forEach( ( propsConfig ) => {
+			test( `should render with ${ JSON.stringify(
+				propsConfig
+			) }`, async ( { page } ) => {
+				await testSnapshotForPropsConfig( page, propsConfig );
+			} );
+		} );
+	} );
+
+	test.describe( 'icon', () => {
+		test.beforeEach( async ( { page } ) => {
+			gotoStoryId( page, 'components-button--icon', {
+				decorators: { customE2EControls: 'show' },
+			} );
+		} );
+
+		getAllPropsPermutations( [
+			{
+				propName: '__next40pxDefaultSize',
+				valuesToTest: [ true, false ],
+			},
+		] ).forEach( ( propsConfig ) => {
+			test( `should render with ${ JSON.stringify(
+				propsConfig
+			) }`, async ( { page } ) => {
+				await testSnapshotForPropsConfig( page, propsConfig );
```

Good question. In my testing, it was unnecessary when screenshotting with the `testSnapshotForPropsConfig()` util, but it is necessary otherwise.

I think we might want to try baking the wait into the `gotoStoryId()` util, in combination with a decorator. So `gotoStoryId()` would automatically `waitForSelector( '[data-testid="screenshot-container"]' )` or something. This way we wouldn't have to rely on component classnames. Having a `screenshot-container` wrapper may also be useful for excluding the `customE2EControls` stuff from the screenshots.

## Changed files

- packages/components/src/button/stories/e2e/index.tsx (ADDED, +58/-0)
- test/storybook-playwright/specs/button.spec.ts (ADDED, +57/-0)
- test/storybook-playwright/utils.ts (MODIFIED, +5/-2)
