---
number: 78953
type: pull_request
state: MERGED
merged_at: 2026-06-05T22:46:52Z
url: https://github.com/WordPress/gutenberg/pull/78953
matched_labels: ["[Package] UI"]
all_labels: ["[Type] Bug","[Package] UI"]
---

# PR #78953: ui/AlertDialog: Fix footer layout style override

- URL: https://github.com/WordPress/gutenberg/pull/78953
- Author: ciampo
- Created: 2026-06-04T14:44:34Z
- Updated: 2026-06-08T13:23:38Z
- Comments: 9 of 9
- Review threads: 0 of 0
- Changed files: 4 of 4
- Diff: ../pr-diffs/78953.diff

## Body

## What?

Make `AlertDialog`'s `.footer-column` compose the shared `.footer` and place the override in the `wp-ui-compositions` CSS layer.

## Why?

The footer's custom column layout could randomly collapse back to a row:

-   `.footer-column` overrides the shared `.footer` style, but both sat in the same CSS layer.
-   With no layer difference, the winner is decided by the order the stylesheets load.
-   Loading another overlay (e.g. hovering the `Drawer` sidebar item in Storybook) reshuffles that order, so the shared `.footer` wins and the override is lost.
-   Reproducible in Firefox.

Putting the override in a higher layer (`wp-ui-compositions`) makes it win every time, regardless of load order.

## How?

-   The override now lives in the higher `wp-ui-compositions` layer, so it wins by layer precedence independent of injection order.
-   `.footer-column` `composes` the shared `.footer` (instead of pairing the two classes via `clsx` in `popup.tsx`), so the base chrome and the override are always applied together and the relationship is self-documented in CSS.
-   Added a short note in CONTRIBUTING documenting both points.

## Testing Instructions

Use Firefox.

1. In Storybook, open the `AlertDialog` example (with footer actions).
2. Hover the `Drawer` item in the sidebar to trigger its styles loading.
3. The alert dialog footer must keep its column layout (actions + error message stacked) and not collapse to a row.

## Screen recordings

Before:

https://github.com/user-attachments/assets/814d3d06-d976-46b4-ab1d-806090bc369f

After:

https://github.com/user-attachments/assets/c2a61a0c-7d75-4558-bf45-f30e554aa053

## Use of AI Tools

Cursor + Opus 4.8

## Issue comments

### github-actions on 2026-06-04T15:04:47Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4623397221

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: jameskoster <jameskoster@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-04T15:09:51Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4623445844

**Size Change:** 0 B 

**Total Size:** 8.21 MB



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-04T15:31:46Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4623654071

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 6f2a508ac6d364569aa0bf894960d39b1f036c91.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27042905548
📝  Reported issues:
- #76772 in `/test/e2e/specs/editor/plugins/block-hooks.spec.js`

### aduth on 2026-06-04T17:16:32Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4624505645

Would another option be to move the [application of the shared footer styles](https://github.com/WordPress/gutenberg/blob/1946796a059b64a08922a1c99a9ca31c43b8ffa1/packages/ui/src/alert-dialog/popup.tsx#L101-L104) to be `composes` within the existing `.footer-column` styles that AlertDialog defines? I guess this is a bit concerning to me that we have to use `compositions` for a component's own custom classes any time that class is used in combination with some other class on an element that is meant to take priority.

### ciampo on 2026-06-05T17:35:57Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4633969097

> Would another option be to move the [application of the shared footer styles](https://github.com/WordPress/gutenberg/blob/1946796a059b64a08922a1c99a9ca31c43b8ffa1/packages/ui/src/alert-dialog/popup.tsx#L101-L104) to be `composes` within the existing `.footer-column` styles that AlertDialog defines? I guess this is a bit concerning to me that we have to use `compositions` for a component's own custom classes any time that class is used in combination with some other class on an element that is meant to take priority.

I considered that option, but I wonder if we'd still need to define the styles in the `compositions` layer anyway, since the `AlertDialog`'s styles need to partially override the composed/shared styles?

In other words, I don't think that using `compose` will fix the bug (since it will only concatenate the class names, without moving rules to a separate layer or adding specificity).

To me, using the `compositions` layer here feels like the correct use of it: we're extending a base style/component and we're composing on top of it.

Is this helping with your concern, or is it still standing?

### aduth on 2026-06-05T17:43:12Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4634024599

Ok, that suggestion may have been a misunderstanding on my part then, because this is surprising to me and I would have thought the additional styles beyond the `composes` would take precedent:

> I don't think that using `compose` will fix the bug (since it will only concatenate the class names, without moving rules to a separate layer or adding specificity).

Although it could help to do both, perhaps (use `composes` _and_ put in `compositions` layer)? That way, `composes` being a signal for why some styles are in `compositions` layer, if styles are extending (composing) some shared base styling. Even the name conveniently overlaps 😄

### ciampo on 2026-06-05T22:20:21Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4636025146

Makes sense. Refactored to use `composes` instead of two separate classnames 🚀

### aduth on 2026-06-08T13:09:01Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4649294653

Separately, do we need to audit existing usage of `composes` to use the `compositions` layer?

### ciampo on 2026-06-08T13:23:38Z

URL: https://github.com/WordPress/gutenberg/pull/78953#issuecomment-4649424824

> Separately, do we need to audit existing usage of `composes` to use the `compositions` layer?

I did as part of the work in this PR, and didn't find other usages of `composes` that were also applying overrides on those composed styles 👌

## Review threads

_No review threads._
## Changed files

- packages/ui/CHANGELOG.md (MODIFIED, +1/-0)
- packages/ui/CONTRIBUTING.md (MODIFIED, +13/-0)
- packages/ui/src/alert-dialog/popup.tsx (MODIFIED, +1/-6)
- packages/ui/src/alert-dialog/style.module.css (MODIFIED, +12/-5)
