---
number: 79490
type: pull_request
state: MERGED
merged_at: 2026-06-25T11:21:08Z
url: https://github.com/WordPress/gutenberg/pull/79490
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components"]
---

# PR #79490: Components: document CSS module class composition

- URL: https://github.com/WordPress/gutenberg/pull/79490
- Author: ciampo
- Created: 2026-06-24T13:55:11Z
- Updated: 2026-06-25T13:19:12Z
- Comments: 8 of 8
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/79490.diff

## Body

## What?
Follow up to #79442.

Clarifies the recommended `clsx` pattern for conditional CSS Module classes in `packages/components/CONTRIBUTING.md`.

## Why?
Computed object keys can produce a literal `undefined` class when a CSS Module export is missing in tests.

## How?
- Documents the safer `condition && styles.className` pattern.
- Updates the example to use that pattern with `className` as a direct `clsx` argument.
- Adds a `packages/components` changelog entry.

## Testing Instructions
1. Run `npm run lint:md:docs -- packages/components/CONTRIBUTING.md`.

### Testing Instructions for Keyboard
Not applicable; documentation-only change.

## Screenshots or screencast
Not applicable.

## Use of AI Tools
Created with assistance from ChatGPT/Codex and reviewed before opening this PR.

## Issue comments

### github-actions on 2026-06-24T15:23:13Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4790869438

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### jsnajdr on 2026-06-25T11:22:25Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798679907

> When a conditional class comes from a CSS module, use `condition && styles.className` instead of a computed object key (`{ [ styles.className ]: condition }`), because CSS modules are mocked as empty objects in Jest tests, which result in a literal `undefined` class.

This makes me sad, it's a horrible recommendation 😄 The object syntax is the main point of the `clsx` library ☹️

### github-actions on 2026-06-25T11:24:07Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798691555

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 2b798e641f3b3b273c96959e6f602c7650adc6d9.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/28164804854
📝  Reported issues:
- #78689 in `/test/e2e/specs/editor/plugins/wp-editor-meta-box.spec.js`
- #78753 in `/test/e2e/specs/editor/collaboration/http-only/collaboration-document-size-lock.spec.ts`

### ciampo on 2026-06-25T11:26:24Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798707615

> > When a conditional class comes from a CSS module, use `condition && styles.className` instead of a computed object key (`{ [ styles.className ]: condition }`), because CSS modules are mocked as empty objects in Jest tests, which result in a literal `undefined` class.
> 
> This makes me sad, it's a horrible recommendation 😄 The object syntax is the main point of the `clsx` library ☹️

Are you suggesting we revert the recommendation, or simply omit it? I personally don't have a strong opinion either way.

### jsnajdr on 2026-06-25T11:35:42Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798777972

If the only problem is Jest mocking `styles` as empty objects, that can be solved with a simple proxy:
```js
const styles = new Proxy({}, {
  get: (_, prop) => kebabCase(prop),
});
```
Here `styles.className` returns `class-name`, `style.singleLineClamp` returns `single-line-clamp`, etc.

### jsnajdr on 2026-06-25T11:37:42Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798792556

> Are you suggesting we revert the recommendation, or simply omit it?

I think we're holding back our `clsx` devex if we decide to practically ban the object syntax. If we can fix the entire problem by fixing the Jest mocks, I'm for doing that.

### aduth on 2026-06-25T12:03:09Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798995396

Alternatively, we could use something like [`identity-obj-proxy`](https://www.npmjs.com/package/identity-obj-proxy) instead of the [empty object](https://github.com/WordPress/gutenberg/blob/trunk/packages/jest-preset-default/scripts/style-mock.js) we [configure](https://github.com/WordPress/gutenberg/blob/8e8385c76a2ca3ab4bc10c4a2d0878acf762918a/packages/jest-preset-default/jest-preset.js#L3)? Or some more authentic loader for CSS modules in Jest? Tools like [`jest-css-modules-transform`](https://www.npmjs.com/package/jest-css-modules-transform) being another option.

My first thought was "yeah, if the style property is undefined, that's not great", but in reality the style property shouldn't ever be undefined, and we ideally wouldn't contort our runtime implementation in service of this testing inaccuracy.

### ciampo on 2026-06-25T13:19:12Z

URL: https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4799704302

Testing an approach in https://github.com/WordPress/gutenberg/pull/79535

## Review threads

### Review thread 1

#### ciampo on 2026-06-24T15:18:57Z

URL: https://github.com/WordPress/gutenberg/pull/79490#discussion_r3468224059
Path: packages/components/CONTRIBUTING.md

```diff
@@ -445,9 +445,9 @@ function MyComponent( { __nextHasNoOuterMargins = false, className } ) {
 			className={ clsx(
 				'components-my-component',
 				styles.root,
+				className,
 				! __nextHasNoOuterMargins &&
-					styles.deprecatedOuterMargins,
-				className
+					styles.deprecatedOuterMargins
```

Keeping the non-object form here, since the object form can produce `undefined` as an object key

### Review thread 2 (resolved)

#### mirka on 2026-06-25T10:18:53Z

URL: https://github.com/WordPress/gutenberg/pull/79490#discussion_r3473611759
Path: packages/components/CONTRIBUTING.md

```diff
@@ -418,7 +418,7 @@ On the component's main named export, add a JSDoc comment that includes the main
 
 All new components should be styled using SCSS Modules.
 
-Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`.
+Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`. When a conditional class comes from a CSS module, use `condition && styles.className` instead of a computed object key (`{ [ styles.className ]: condition }`), because a missing module export can otherwise become a literal `undefined` class in tests.
```

```suggestion
Place component-local styles in a `style.module.scss` file next to the component, import the module from JavaScript or TypeScript, and compose class names with `clsx`. Preserve existing public `components-*` class names where consumers may rely on them. For dynamic values, prefer inline CSS custom properties consumed by the SCSS module. For variants and state, prefer conditional module classes composed with `clsx`. When a conditional class comes from a CSS module, use `condition && styles.className` instead of a computed object key (`{ [ styles.className ]: condition }`), because CSS modules are mocked as empty objects in Jest tests, which result in a literal `undefined` class.
```

Maybe something like this, to make it clear that styles will _always_ be missing in tests.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/CONTRIBUTING.md (MODIFIED, +3/-3)
