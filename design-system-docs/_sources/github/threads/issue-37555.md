---
number: 37555
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/37555
matched_labels: ["[Package] Components"]
all_labels: ["[Status] In Progress","Browser Issues","[Package] Components"]
---

# Issue #37555: Some components don't render in the intended font stack in Chrome

- URL: https://github.com/WordPress/gutenberg/issues/37555
- Author: mirka
- Created: 2021-12-21T14:03:03Z
- Updated: 2022-03-01T14:49:49Z
- Comments: 1 of 1

## Body

`InputControl` does not have an explicit `font-family` on its `<input>` element, so depending on the user agent stylesheet it could be rendered in an unintended font.

In my environment (Chrome on Mac), the expected font is `system-ui`, but instead it's rendered in Arial. (`TextControl`, on the other hand, does have an explicit font-family set, so is consistent across browsers.)

Any components based on `InputControl` are likely also affected.

## Issue comments

### mirka on 2022-02-11T08:32:57Z

URL: https://github.com/WordPress/gutenberg/issues/37555#issuecomment-1035978170

This problem doesn't manifest in contexts where the core WP `forms.css` is loaded (i.e. wp-admin), since that includes some normalizing rules:

```scss
// forms.css
input, select, textarea, button {
  font-family: inherit;
}
```

## Directly affected components

- Button
- ColorPalette
- ComboboxControl
- DateTimePicker
- FormTokenField
- InputControl
- SelectControl
- ToggleGroupControl

