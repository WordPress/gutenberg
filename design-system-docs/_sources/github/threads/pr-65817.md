---
number: 65817
type: pull_request
state: MERGED
merged_at: 2024-10-03T05:55:50Z
url: https://github.com/WordPress/gutenberg/pull/65817
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components"]
---

# PR #65817: Composite: add "With Tooltip" storybook example

- URL: https://github.com/WordPress/gutenberg/pull/65817
- Author: ciampo
- Created: 2024-10-02T08:53:56Z
- Updated: 2024-10-03T05:56:14Z
- Comments: 3 of 3
- Review threads: 5 of 5
- Changed files: 1 of 1
- Diff: ../pr-diffs/65817.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Closes #65615

Add a Storybook example to illustrate how to correctly compose `Tooltip` and `Composite`

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Composite the two components is not trivial, such an example can be of help to consumers of the library

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Added a new Storybook example following the suggestions from #65615

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

- Load the new Storybook example
- Make sure that composite and tooltip functionality work as expected

## Screenshots or screencast <!-- if applicable -->


https://github.com/user-attachments/assets/478244ea-c1e1-4cce-a03f-ffb7b0f4f514

## Issue comments

### github-actions on 2024-10-02T08:55:26Z

URL: https://github.com/WordPress/gutenberg/pull/65817#issuecomment-2387960564

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: diegohaz <hazdiego@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-10-02T09:15:13Z

URL: https://github.com/WordPress/gutenberg/pull/65817#issuecomment-2388001387

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 26fc8879694ce52165ff830cfa7da4c0f8d793b1.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/11140795519
📝  Reported issues:
- #64334 in `/test/e2e/specs/site-editor/dataviews-list-layout-keyboard.spec.js`

### ciampo on 2024-10-02T16:29:09Z

URL: https://github.com/WordPress/gutenberg/pull/65817#issuecomment-2389103201

Updated this PR:

- only recommending one way to compose `Tooltip` and `Composite` (ie. by rendering `Composite.Item` as a child of `Tooltip`)
- reworked JSDocs accordingly, with a  more clear code snipped showing the dos and don'ts 
- the third composite item in the story uses the `render` prop to use a custom `Button` instead of the default vanilla `button` that is rendered by default by `Composite.Item`. I thought it could be useful

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2024-10-02T08:55:12Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784071741
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
```

I wonder if it is a good idea to show a snipped that doesn't work as expected 🤔

#### tyxla on 2024-10-02T10:21:18Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784265822
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
```

FWIW, it's not immediately clear what doesn't work and why. Should we further elaborate on that?

#### diegohaz on 2024-10-02T11:04:44Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784310765
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
```

I think it makes sense to show a broken snippet if it's a common mistake. A good way to distinguish it is by adding comments to the snippet, along with some emojis:

```jsx
// 🔴 Does not work
<Composite.Item
...

// 🟢 Good
<Composite.Item
...
```

This should help those who scan code snippets without reading the descriptions.

Example: https://react.dev/learn/you-might-not-need-an-effect

### Review thread 2 (resolved)

#### ciampo on 2024-10-02T08:55:35Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784072244
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
+ * ```
+ *
+ * Take a look at this story's source code to see how to correctly compose
+ * these two components.
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item render={ <button /> }>
+						Item one
+					</Composite.Item>
+				</Tooltip>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip two">
+							<button { ...props }>Item two</button>
+						</Tooltip>
+					) }
+				/>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip three">
+							<button { ...props } />
+						</Tooltip>
+					) }
+				>
+					Item three
+				</Composite.Item>
```

Is it better show all possible ways, or should we pick a recommended one and apply it to all composite items?

#### tyxla on 2024-10-02T10:21:58Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784266507
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
+ * ```
+ *
+ * Take a look at this story's source code to see how to correctly compose
+ * these two components.
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item render={ <button /> }>
+						Item one
+					</Composite.Item>
+				</Tooltip>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip two">
+							<button { ...props }>Item two</button>
+						</Tooltip>
+					) }
+				/>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip three">
+							<button { ...props } />
+						</Tooltip>
+					) }
+				>
+					Item three
+				</Composite.Item>
```

Right now, it doesn't make it clear why we have three different ways to do it. I think it's helpful to have them all, but have some additional description / clarification - what to keep in mind and why.

#### diegohaz on 2024-10-02T11:09:13Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784315084
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
+ * ```
+ *
+ * Take a look at this story's source code to see how to correctly compose
+ * these two components.
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item render={ <button /> }>
+						Item one
+					</Composite.Item>
+				</Tooltip>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip two">
+							<button { ...props }>Item two</button>
+						</Tooltip>
+					) }
+				/>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip three">
+							<button { ...props } />
+						</Tooltip>
+					) }
+				>
+					Item three
+				</Composite.Item>
```

I'd keep only the first option. It's still unclear to me why the other two are necessary (why wouldn't you be able to wrap `<Composite.Item>`?). Unless there's a known use case for them, I'd exclude them for now.

#### ciampo on 2024-10-02T11:18:25Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784324291
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
+ * ```
+ *
+ * Take a look at this story's source code to see how to correctly compose
+ * these two components.
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item render={ <button /> }>
+						Item one
+					</Composite.Item>
+				</Tooltip>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip two">
+							<button { ...props }>Item two</button>
+						</Tooltip>
+					) }
+				/>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip three">
+							<button { ...props } />
+						</Tooltip>
+					) }
+				>
+					Item three
+				</Composite.Item>
```

I feel the same as Diego here — I initially added all three versions, but my gut tells me that we should endorse the first option. It feels cleaner and more clear in terms of how the components compose with each other.

#### tyxla on 2024-10-02T11:33:35Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784340253
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
+ * ```
+ *
+ * Take a look at this story's source code to see how to correctly compose
+ * these two components.
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item render={ <button /> }>
+						Item one
+					</Composite.Item>
+				</Tooltip>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip two">
+							<button { ...props }>Item two</button>
+						</Tooltip>
+					) }
+				/>
+				<Composite.Item
+					render={ ( props ) => (
+						<Tooltip text="Tooltip three">
+							<button { ...props } />
+						</Tooltip>
+					) }
+				>
+					Item three
+				</Composite.Item>
```

Definitely no objections to keeping the first one 👍

### Review thread 3 (resolved)

#### tyxla on 2024-10-02T10:21:36Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784266141
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
+ * ```
+ *
+ * Take a look at this story's source code to see how to correctly compose
+ * these two components.
```

This might be a good place to mention that there are multiple valid ways to make it work and we're demoing them all.

### Review thread 4 (resolved)

#### tyxla on 2024-10-02T10:24:32Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784269999
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +354,50 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats.
+ * For example, the following code won't work as expected:
+ *
+ * ```jsx
+ * <Composite.Item
+ *   render={
+ *   <Tooltip text="Tooltip">
+ *     <button>Button</button>
+ *   </Tooltip>
```

Should we make this a more complete example? It's lacking the closing markup (`}` and `</Composite.Item>`.

### Review thread 5 (resolved)

#### mirka on 2024-10-02T17:53:06Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1784971594
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +355,46 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats. And while there are a few ways to compose these two components, our recommendation is to render `Composite.Item` as a child of `Tooltip`.
+ *
+ * ```jsx
+ * // 🔴 Does not work
+ * <Composite.Item
+ *   render={
+ *     <Tooltip text="Tooltip">
+ *       <button>Item</button>
+ *     </Tooltip>
+ *   }
+ * />
+ *
+ * // 🟢 Good
+ * <Tooltip text="Tooltip one">
+ *   <Composite.Item>
+ *     Item one
+ *   </Composite.Item>
+ * </Tooltip>
+ * ```
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item>Item one</Composite.Item>
+				</Tooltip>
+				<Tooltip text="Tooltip two">
+					<Composite.Item>Item two</Composite.Item>
+				</Tooltip>
+				<Tooltip text="Tooltip three">
+					<Composite.Item render={ <Button /> }>
+						Item three
+					</Composite.Item>
+				</Tooltip>
```

Not sure if we need this third example, because the normal thing to do is to use the tooltip feature included in `Button`, and that already works:

```tsx
<Ariakit.CompositeItem
  render={
    <Button label="Tooltip" showTooltip>
      Button
    </Button>
  }
/>
```

#### ciampo on 2024-10-02T23:14:46Z

URL: https://github.com/WordPress/gutenberg/pull/65817#discussion_r1785381378
Path: packages/components/src/composite/stories/index.story.tsx

```diff
@@ -353,3 +355,46 @@ const Fill = ( { children } ) => {
 		},
 	},
 };
+
+/**
+ * Combining the `Tooltip` and `Composite` component has a few caveats. And while there are a few ways to compose these two components, our recommendation is to render `Composite.Item` as a child of `Tooltip`.
+ *
+ * ```jsx
+ * // 🔴 Does not work
+ * <Composite.Item
+ *   render={
+ *     <Tooltip text="Tooltip">
+ *       <button>Item</button>
+ *     </Tooltip>
+ *   }
+ * />
+ *
+ * // 🟢 Good
+ * <Tooltip text="Tooltip one">
+ *   <Composite.Item>
+ *     Item one
+ *   </Composite.Item>
+ * </Tooltip>
+ * ```
+ */
+export const WithTooltips: StoryObj< typeof Composite > = {
+	...Default,
+	args: {
+		...Default.args,
+		children: (
+			<>
+				<Tooltip text="Tooltip one">
+					<Composite.Item>Item one</Composite.Item>
+				</Tooltip>
+				<Tooltip text="Tooltip two">
+					<Composite.Item>Item two</Composite.Item>
+				</Tooltip>
+				<Tooltip text="Tooltip three">
+					<Composite.Item render={ <Button /> }>
+						Item three
+					</Composite.Item>
+				</Tooltip>
```

Sounds good, I will remove it before merging

## Changed files

- packages/components/src/composite/stories/index.story.tsx (MODIFIED, +42/-0)
