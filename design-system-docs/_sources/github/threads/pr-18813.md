---
number: 18813
type: pull_request
state: MERGED
merged_at: 2019-12-04T13:34:35Z
url: https://github.com/WordPress/gutenberg/pull/18813
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Feature] UI Components","[Package] Components"]
---

# PR #18813: Popover: smoothly reposition on scroll

- URL: https://github.com/WordPress/gutenberg/pull/18813
- Author: ellatrix
- Created: 2019-11-28T20:23:28Z
- Updated: 2019-12-09T11:44:27Z
- Comments: 21 of 21
- Review threads: 5 of 5
- Changed files: 10 of 10
- Diff: ../pr-diffs/18813.diff

## Body

## Description

Fixes #16643.
Blocks #18779.

This PR refactors the `Popover` component so that it smoothly repositions on scroll. Currently we do have logic to reposition it for every scroll frame, but the `Popover` is always rerendered too late (and thus the style attributes reset too late). Why is that? Because currently we set it with React state, which then rerenders the component. Since this is not done synchronously, there's a small delay.

The only way we can fix this is by avoiding setting state. Instead, we should update the DOM immediately. This is also more performant. There's no way around the DOM, unfortunately.

As a result, I think the component has also become simpler. There's no need for the many nested hooks and passing around props. I also swapped out the `is-*` classes in favour of `data-` attributes, which are much easier to update.

## How has this been tested?

Open some popovers and play with scrolling and resizing. Nothing else should be different from `master`.

## Screenshots <!-- if applicable -->

## Types of changes

Bug fix

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->.

## Issue comments

### ZebulanStanphill on 2019-11-28T21:48:05Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559608277

Are you sure you couldn't just use `useLayoutEffect`?

> The signature is identical to useEffect, but it fires synchronously after all DOM mutations. Use this to read layout from the DOM and synchronously re-render. Updates scheduled inside useLayoutEffect will be flushed synchronously, before the browser has a chance to paint.

https://reactjs.org/docs/hooks-reference.html#useref

### ellatrix on 2019-11-28T22:04:15Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559610260

@ZebulanStanphill How would you use that for a `scroll` callback? It doesn't fire in that case, it only fires when any props change. In other words, there is no re-render happening, and I'm not sure how you _trigger_ a synchronous render.

### ZebulanStanphill on 2019-11-28T22:21:23Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559612149

@ellatrix Good point. I didn't think that through enough.

### ZebulanStanphill on 2019-11-29T03:05:26Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559644406

The animation when opening the block <kbd><samp>More options</samp></kbd> or the <kbd><samp>More tools & options</samp></kbd> popovers is gone.

### ellatrix on 2019-11-29T07:50:26Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559693952

@ZebulanStanphill I don’t see the animation in master either.

### youknowriad on 2019-11-29T09:59:08Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559732636

@ellatrix I didn't test this branch but in master I do see the animation (make sure you're not in prefers reduced mode and the "disable animation" plugin should be disabled)

### ellatrix on 2019-11-29T10:08:42Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559735813

@youknowriad Ah, for some reason I had only that test plugin enabled... Strange :)

### ellatrix on 2019-11-29T10:10:10Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559736284

@ZebulanStanphill For me the animation works with this branch as well, if you make sure the "disable animation" plugin is disabled.

### ZebulanStanphill on 2019-11-29T13:25:40Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559789298

I just checked, and yep, the "Gutenberg Test Plugin, Disables the CSS animations" was enabled. I never enabled it. Is it supposed to be enabled by default when you create a dev environment? (I'm using the `@wordpress/env` environment.)

Anyway, it looks like there are no issues (that I know of) on this branch after all.

### ellatrix on 2019-11-29T13:28:23Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-559789985

@ZebulanStanphill I have the same issue. The plugin always gets activated for some reason, I suspect by e2e tests? Thank you for testing the PR!

### ellatrix on 2019-12-04T09:24:45Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561551796

@youknowriad I'd love us to have more use cases in Gutenberg for `Popover` before we decide to wrap an external component. There's a few more interesting use cases in #18779.

### ellatrix on 2019-12-04T09:29:27Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561553559

@youknowriad I cannot scroll the document sidebar on mobile in `master` either.

### ellatrix on 2019-12-04T10:44:32Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561585827

> I noticed that the popover doesn't reposition properly on resize (mobile)

This is fixed too.

### youknowriad on 2019-12-04T10:55:23Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561591082

This is working well for me. I was hoping it would fix the issue where sometimes the scrollbar in popovers don't appear (you can trigger it inconsistently in the More menu or the block settings menu) but unfortunately, it doesn't, it still happens sometimes. I was curious whether you have an idea why this happens sometimes.

### ellatrix on 2019-12-04T11:13:58Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561599118

> I was hoping it would fix the issue where sometimes the scrollbar in popovers don't appear (you can trigger it inconsistently in the More menu or the block settings menu) but unfortunately, it doesn't, it still happens sometimes. I was curious whether you have an idea why this happens sometimes.

I see what you mean. Looks like a bug somewhere else in the code, but I'll have a look.

### youknowriad on 2019-12-04T11:17:39Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561600408

<img width="636" alt="Capture d’écran 2019-12-04 à 12 16 28 PM" src="https://user-images.githubusercontent.com/272444/70138147-ea952a00-168f-11ea-84d4-2d24bc2c7d23.png">

Mmm, I found another little bug here where scrolling the content area doesn't resize the popover properly (notice that the height of the popover should reach the bottom of the window in the screenshot but t seems stuck.

### youknowriad on 2019-12-04T11:18:38Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561600732

so basically, it scales down, but doesn't scale up again.

### ellatrix on 2019-12-04T11:41:59Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561607836

@youknowriad I think I fixed both those issues now.

### youknowriad on 2019-12-04T12:44:07Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561627486

This is cool. I think there's a small refresh issue still. If you scroll or resize quickly. You'll notice that sometimes the popover is not adjusting its size, sometimes it stops doing so.

<img width="283" alt="Capture d’écran 2019-12-04 à 1 41 25 PM" src="https://user-images.githubusercontent.com/272444/70143499-14544e00-169c-11ea-9285-64a9ad6ea1f0.png">

Notice the scrollbars in the screenshot. I do that by resizing up and down quickly.

### ellatrix on 2019-12-04T12:52:16Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561629946

@youknowriad Thanks for catching that. It should be fixed now. The problem was that I didn't unset the max height if none was provided (and kept the old one instead).

### ellatrix on 2019-12-04T13:34:07Z

URL: https://github.com/WordPress/gutenberg/pull/18813#issuecomment-561647811

Many thanks for the review, @youknowriad!

## Review threads

### Review thread 1

#### youknowriad on 2019-12-04T09:09:34Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353621002
Path: packages/components/src/popover/index.js

```diff
@@ -361,38 +219,109 @@ const Popover = ( {
 } ) => {
 	const anchorRefFallback = useRef( null );
 	const contentRef = useRef( null );
+	const containerRef = useRef();
+	const [ isMobileViewport, setIsMobileViewport ] = useState( false );
```

This should help here to avoid an Adhoc solution https://github.com/WordPress/gutenberg/pull/18816

### Review thread 2 (resolved)

#### youknowriad on 2019-12-04T09:10:48Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353621566
Path: packages/components/src/popover/index.js

```diff
@@ -361,38 +219,109 @@ const Popover = ( {
 } ) => {
 	const anchorRefFallback = useRef( null );
 	const contentRef = useRef( null );
+	const containerRef = useRef();
+	const [ isMobileViewport, setIsMobileViewport ] = useState( false );
+	const [ animateOrigin, setAnimateOrigin ] = useState();
 
-	// Animation
-	const [ isReadyToAnimate, setIsReadyToAnimate ] = useState( false );
-
-	// Anchor position
-	const anchor = useAnchor(
-		anchorRefFallback,
-		contentRef,
-		anchorRect,
-		getAnchorRect,
-		anchorRef,
-		shouldAnchorIncludePadding,
-		anchorVerticalBuffer,
-		anchorHorizontalBuffer
-	);
+	useEffect( () => {
+		const resize = () => setIsMobileViewport( window.innerWidth < 782 );
+		window.addEventListener( 'resize', resize );
+		return () => window.removeEventListener( 'resize', resize );
+	} );
 
 	// Content size
 	const contentSize = useInitialContentSize( contentRef );
+
+	const refresh = () => {
+		let anchor = computeAnchorRect(
+			anchorRefFallback,
+			anchorRect,
+			getAnchorRect,
+			anchorRef,
+			shouldAnchorIncludePadding
+		);
+
+		if ( ! anchor ) {
+			return;
+		}
+
+		anchor = addBuffer(
+			anchor,
+			anchorVerticalBuffer,
+			anchorHorizontalBuffer
+		);
+
+		const {
+			popoverTop,
+			popoverLeft,
+			xAxis,
+			yAxis,
+			contentHeight,
+			contentWidth,
+		} = computePopoverPosition( anchor, contentSize, position );
+
+		// Compute the animation position
+		const yAxisMapping = {
+			top: 'bottom',
+			bottom: 'top',
+		};
+		const xAxisMapping = {
+			left: 'right',
+			right: 'left',
+		};
+		const animateYAxis = yAxisMapping[ yAxis ] || 'middle';
+		const animateXAxis = xAxisMapping[ xAxis ] || 'center';
+
+		containerRef.current.setAttribute( 'data-x-axis', xAxis );
+		containerRef.current.setAttribute( 'data-y-axis', yAxis );
+		containerRef.current.style.top = popoverTop + 'px';
+		containerRef.current.style.left = popoverLeft + 'px';
+
+		if ( contentHeight ) {
+			contentRef.current.style.maxHeight = contentHeight + 'px';
+		}
+
+		if ( contentWidth ) {
+			contentRef.current.style.maxWidth = contentWidth + 'px';
+		}
+
+		if ( xAxis === 'center' && yAxis === 'middle' ) {
+			contentRef.current.classList.add( 'is-without-arrow' );
+		} else {
+			contentRef.current.classList.remove( 'is-without-arrow' );
+		}
+
+		setAnimateOrigin( animateXAxis + ' ' + animateYAxis );
+	};
+
 	useEffect( () => {
-		if ( contentSize ) {
-			setIsReadyToAnimate( true );
+		if ( isMobileViewport && expandOnMobile ) {
+			return;
 		}
-	}, [ contentSize ] );
-
-	// Compute the position
-	const popoverPosition = usePopoverPosition(
-		anchor,
-		contentSize,
-		position,
-		expandOnMobile,
-		contentRef
-	);
+
+		if ( ! contentSize ) {
+			return;
+		}
+
+		refresh();
+
+		/*
+		 * There are sometimes we need to reposition or resize the popover that
+		 * are not handled by the resize/scroll window events (i.e. CSS changes
+		 * in the layout that changes the position of the anchor).
+		 *
+		 * For these situations, we refresh the popover every 0.5s
+		 */
+		const intervalHandle = window.setInterval( refresh, 500 );
+		window.addEventListener( 'resize', refresh );
+		window.addEventListener( 'scroll', refresh, true );
+
+		return () => {
+			window.clearInterval( intervalHandle );
+			window.removeEventListener( 'resize', refresh );
+			window.removeEventListener( 'scroll', refresh, true );
+		};
+	} );
```

Do we want to trigger a refresh on each render?

#### ellatrix on 2019-12-04T09:52:52Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353643210
Path: packages/components/src/popover/index.js

```diff
@@ -361,38 +219,109 @@ const Popover = ( {
 } ) => {
 	const anchorRefFallback = useRef( null );
 	const contentRef = useRef( null );
+	const containerRef = useRef();
+	const [ isMobileViewport, setIsMobileViewport ] = useState( false );
+	const [ animateOrigin, setAnimateOrigin ] = useState();
 
-	// Animation
-	const [ isReadyToAnimate, setIsReadyToAnimate ] = useState( false );
-
-	// Anchor position
-	const anchor = useAnchor(
-		anchorRefFallback,
-		contentRef,
-		anchorRect,
-		getAnchorRect,
-		anchorRef,
-		shouldAnchorIncludePadding,
-		anchorVerticalBuffer,
-		anchorHorizontalBuffer
-	);
+	useEffect( () => {
+		const resize = () => setIsMobileViewport( window.innerWidth < 782 );
+		window.addEventListener( 'resize', resize );
+		return () => window.removeEventListener( 'resize', resize );
+	} );
 
 	// Content size
 	const contentSize = useInitialContentSize( contentRef );
+
+	const refresh = () => {
+		let anchor = computeAnchorRect(
+			anchorRefFallback,
+			anchorRect,
+			getAnchorRect,
+			anchorRef,
+			shouldAnchorIncludePadding
+		);
+
+		if ( ! anchor ) {
+			return;
+		}
+
+		anchor = addBuffer(
+			anchor,
+			anchorVerticalBuffer,
+			anchorHorizontalBuffer
+		);
+
+		const {
+			popoverTop,
+			popoverLeft,
+			xAxis,
+			yAxis,
+			contentHeight,
+			contentWidth,
+		} = computePopoverPosition( anchor, contentSize, position );
+
+		// Compute the animation position
+		const yAxisMapping = {
+			top: 'bottom',
+			bottom: 'top',
+		};
+		const xAxisMapping = {
+			left: 'right',
+			right: 'left',
+		};
+		const animateYAxis = yAxisMapping[ yAxis ] || 'middle';
+		const animateXAxis = xAxisMapping[ xAxis ] || 'center';
+
+		containerRef.current.setAttribute( 'data-x-axis', xAxis );
+		containerRef.current.setAttribute( 'data-y-axis', yAxis );
+		containerRef.current.style.top = popoverTop + 'px';
+		containerRef.current.style.left = popoverLeft + 'px';
+
+		if ( contentHeight ) {
+			contentRef.current.style.maxHeight = contentHeight + 'px';
+		}
+
+		if ( contentWidth ) {
+			contentRef.current.style.maxWidth = contentWidth + 'px';
+		}
+
+		if ( xAxis === 'center' && yAxis === 'middle' ) {
+			contentRef.current.classList.add( 'is-without-arrow' );
+		} else {
+			contentRef.current.classList.remove( 'is-without-arrow' );
+		}
+
+		setAnimateOrigin( animateXAxis + ' ' + animateYAxis );
+	};
+
 	useEffect( () => {
-		if ( contentSize ) {
-			setIsReadyToAnimate( true );
+		if ( isMobileViewport && expandOnMobile ) {
+			return;
 		}
-	}, [ contentSize ] );
-
-	// Compute the position
-	const popoverPosition = usePopoverPosition(
-		anchor,
-		contentSize,
-		position,
-		expandOnMobile,
-		contentRef
-	);
+
+		if ( ! contentSize ) {
+			return;
+		}
+
+		refresh();
+
+		/*
+		 * There are sometimes we need to reposition or resize the popover that
+		 * are not handled by the resize/scroll window events (i.e. CSS changes
+		 * in the layout that changes the position of the anchor).
+		 *
+		 * For these situations, we refresh the popover every 0.5s
+		 */
+		const intervalHandle = window.setInterval( refresh, 500 );
+		window.addEventListener( 'resize', refresh );
+		window.addEventListener( 'scroll', refresh, true );
+
+		return () => {
+			window.clearInterval( intervalHandle );
+			window.removeEventListener( 'resize', refresh );
+			window.removeEventListener( 'scroll', refresh, true );
+		};
+	} );
```

I'll add a list of dependencies, but it's pretty much everything :) Still may be worth it to skip the functions.

### Review thread 3 (resolved)

#### youknowriad on 2019-12-04T09:11:58Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353622105
Path: packages/components/src/popover/index.js

```diff
@@ -451,53 +380,33 @@ const Popover = ( {
 		onClickOutside( clickEvent );
 	}
 
-	// Compute the animation position
-	const yAxisMapping = {
-		top: 'bottom',
-		bottom: 'top',
-	};
-	const xAxisMapping = {
-		left: 'right',
-		right: 'left',
-	};
-	const animateYAxis = yAxisMapping[ popoverPosition.yAxis ] || 'middle';
-	const animateXAxis = xAxisMapping[ popoverPosition.xAxis ] || 'center';
-
-	const classes = classnames(
-		'components-popover',
-		className,
-		'is-' + popoverPosition.yAxis,
-		'is-' + popoverPosition.xAxis,
-		{
-			'is-mobile': popoverPosition.isMobile,
-			'is-without-arrow': noArrow || (
-				popoverPosition.xAxis === 'center' &&
-				popoverPosition.yAxis === 'middle'
-			),
-		}
-	);
-
 	// Disable reason: We care to capture the _bubbled_ events from inputs
 	// within popover as inferring close intent.
 
 	let content = (
 		<PopoverDetectOutside onFocusOutside={ handleOnFocusOutside }>
 			<Animate
-				type={ animate && isReadyToAnimate ? 'appear' : null }
-				options={ { origin: animateYAxis + ' ' + animateXAxis } }
+				type={ animate && animateOrigin ? 'appear' : null }
+				options={ { origin: animateOrigin } }
 			>
 				{ ( { className: animateClassName } ) => (
-					<IsolatedEventContainer
```

Do you know why we had IsolatedEventContainer and why it's removed now?

#### ellatrix on 2019-12-04T09:19:23Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353625585
Path: packages/components/src/popover/index.js

```diff
@@ -451,53 +380,33 @@ const Popover = ( {
 		onClickOutside( clickEvent );
 	}
 
-	// Compute the animation position
-	const yAxisMapping = {
-		top: 'bottom',
-		bottom: 'top',
-	};
-	const xAxisMapping = {
-		left: 'right',
-		right: 'left',
-	};
-	const animateYAxis = yAxisMapping[ popoverPosition.yAxis ] || 'middle';
-	const animateXAxis = xAxisMapping[ popoverPosition.xAxis ] || 'center';
-
-	const classes = classnames(
-		'components-popover',
-		className,
-		'is-' + popoverPosition.yAxis,
-		'is-' + popoverPosition.xAxis,
-		{
-			'is-mobile': popoverPosition.isMobile,
-			'is-without-arrow': noArrow || (
-				popoverPosition.xAxis === 'center' &&
-				popoverPosition.yAxis === 'middle'
-			),
-		}
-	);
-
 	// Disable reason: We care to capture the _bubbled_ events from inputs
 	// within popover as inferring close intent.
 
 	let content = (
 		<PopoverDetectOutside onFocusOutside={ handleOnFocusOutside }>
 			<Animate
-				type={ animate && isReadyToAnimate ? 'appear' : null }
-				options={ { origin: animateYAxis + ' ' + animateXAxis } }
+				type={ animate && animateOrigin ? 'appear' : null }
+				options={ { origin: animateOrigin } }
 			>
 				{ ( { className: animateClassName } ) => (
-					<IsolatedEventContainer
```

`IsolatedEventContainer` only stops propagation for `mousedown` events, which I added to the `div` element. I'm not sure why propagation needs to be stopped for that event. There was no comment about it. `IsolatedEventContainer` seems a bit like a strange name for this specific behaviour, which should ideally be commented on in this `Popover` component itself.

#### youknowriad on 2019-12-04T09:21:14Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353626492
Path: packages/components/src/popover/index.js

```diff
@@ -451,53 +380,33 @@ const Popover = ( {
 		onClickOutside( clickEvent );
 	}
 
-	// Compute the animation position
-	const yAxisMapping = {
-		top: 'bottom',
-		bottom: 'top',
-	};
-	const xAxisMapping = {
-		left: 'right',
-		right: 'left',
-	};
-	const animateYAxis = yAxisMapping[ popoverPosition.yAxis ] || 'middle';
-	const animateXAxis = xAxisMapping[ popoverPosition.xAxis ] || 'center';
-
-	const classes = classnames(
-		'components-popover',
-		className,
-		'is-' + popoverPosition.yAxis,
-		'is-' + popoverPosition.xAxis,
-		{
-			'is-mobile': popoverPosition.isMobile,
-			'is-without-arrow': noArrow || (
-				popoverPosition.xAxis === 'center' &&
-				popoverPosition.yAxis === 'middle'
-			),
-		}
-	);
-
 	// Disable reason: We care to capture the _bubbled_ events from inputs
 	// within popover as inferring close intent.
 
 	let content = (
 		<PopoverDetectOutside onFocusOutside={ handleOnFocusOutside }>
 			<Animate
-				type={ animate && isReadyToAnimate ? 'appear' : null }
-				options={ { origin: animateYAxis + ' ' + animateXAxis } }
+				type={ animate && animateOrigin ? 'appear' : null }
+				options={ { origin: animateOrigin } }
 			>
 				{ ( { className: animateClassName } ) => (
-					<IsolatedEventContainer
```

I think it's probably @aduth's addition. Pinging him just in case.

#### ellatrix on 2019-12-04T09:21:44Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353626787
Path: packages/components/src/popover/index.js

```diff
@@ -451,53 +380,33 @@ const Popover = ( {
 		onClickOutside( clickEvent );
 	}
 
-	// Compute the animation position
-	const yAxisMapping = {
-		top: 'bottom',
-		bottom: 'top',
-	};
-	const xAxisMapping = {
-		left: 'right',
-		right: 'left',
-	};
-	const animateYAxis = yAxisMapping[ popoverPosition.yAxis ] || 'middle';
-	const animateXAxis = xAxisMapping[ popoverPosition.xAxis ] || 'center';
-
-	const classes = classnames(
-		'components-popover',
-		className,
-		'is-' + popoverPosition.yAxis,
-		'is-' + popoverPosition.xAxis,
-		{
-			'is-mobile': popoverPosition.isMobile,
-			'is-without-arrow': noArrow || (
-				popoverPosition.xAxis === 'center' &&
-				popoverPosition.yAxis === 'middle'
-			),
-		}
-	);
-
 	// Disable reason: We care to capture the _bubbled_ events from inputs
 	// within popover as inferring close intent.
 
 	let content = (
 		<PopoverDetectOutside onFocusOutside={ handleOnFocusOutside }>
 			<Animate
-				type={ animate && isReadyToAnimate ? 'appear' : null }
-				options={ { origin: animateYAxis + ' ' + animateXAxis } }
+				type={ animate && animateOrigin ? 'appear' : null }
+				options={ { origin: animateOrigin } }
 			>
 				{ ( { className: animateClassName } ) => (
-					<IsolatedEventContainer
```

Ok, it looks like the only uses of `IsolatedEventContainer` are `Popover` and `Modal`, and it mentions those, so maybe I should restore it. Too bad it creates an extra `div` though.

#### ellatrix on 2019-12-04T09:49:44Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353641621
Path: packages/components/src/popover/index.js

```diff
@@ -451,53 +380,33 @@ const Popover = ( {
 		onClickOutside( clickEvent );
 	}
 
-	// Compute the animation position
-	const yAxisMapping = {
-		top: 'bottom',
-		bottom: 'top',
-	};
-	const xAxisMapping = {
-		left: 'right',
-		right: 'left',
-	};
-	const animateYAxis = yAxisMapping[ popoverPosition.yAxis ] || 'middle';
-	const animateXAxis = xAxisMapping[ popoverPosition.xAxis ] || 'center';
-
-	const classes = classnames(
-		'components-popover',
-		className,
-		'is-' + popoverPosition.yAxis,
-		'is-' + popoverPosition.xAxis,
-		{
-			'is-mobile': popoverPosition.isMobile,
-			'is-without-arrow': noArrow || (
-				popoverPosition.xAxis === 'center' &&
-				popoverPosition.yAxis === 'middle'
-			),
-		}
-	);
-
 	// Disable reason: We care to capture the _bubbled_ events from inputs
 	// within popover as inferring close intent.
 
 	let content = (
 		<PopoverDetectOutside onFocusOutside={ handleOnFocusOutside }>
 			<Animate
-				type={ animate && isReadyToAnimate ? 'appear' : null }
-				options={ { origin: animateYAxis + ' ' + animateXAxis } }
+				type={ animate && animateOrigin ? 'appear' : null }
+				options={ { origin: animateOrigin } }
 			>
 				{ ( { className: animateClassName } ) => (
-					<IsolatedEventContainer
```

I restored `IsolatedEventContainer`, rewrote it with hooks, and modified it to forward the DOM ref.

### Review thread 4 (resolved)

#### youknowriad on 2019-12-04T10:57:13Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353675724
Path: packages/components/src/popover/index.js

```diff
@@ -361,38 +216,122 @@ const Popover = ( {
 } ) => {
 	const anchorRefFallback = useRef( null );
 	const contentRef = useRef( null );
+	const containerRef = useRef();
+	const [ isMobileViewport, setIsMobileViewport ] = useState( false );
+	const [ animateOrigin, setAnimateOrigin ] = useState();
 
-	// Animation
-	const [ isReadyToAnimate, setIsReadyToAnimate ] = useState( false );
-
-	// Anchor position
-	const anchor = useAnchor(
-		anchorRefFallback,
-		contentRef,
-		anchorRect,
-		getAnchorRect,
-		anchorRef,
-		shouldAnchorIncludePadding,
-		anchorVerticalBuffer,
-		anchorHorizontalBuffer
-	);
+	useEffect( () => {
+		const resize = () => setIsMobileViewport( window.innerWidth < 782 );
+		window.addEventListener( 'resize', resize );
+		return () => window.removeEventListener( 'resize', resize );
+	} );
 
 	// Content size
 	const contentSize = useInitialContentSize( contentRef );
+
 	useEffect( () => {
-		if ( contentSize ) {
-			setIsReadyToAnimate( true );
+		if ( isMobileViewport && expandOnMobile ) {
+			return;
+		}
+
+		if ( ! contentSize ) {
+			return;
 		}
-	}, [ contentSize ] );
 
-	// Compute the position
-	const popoverPosition = usePopoverPosition(
-		anchor,
+		const refresh = () => {
+			let anchor = computeAnchorRect(
+				anchorRefFallback,
+				anchorRect,
+				getAnchorRect,
+				anchorRef,
+				shouldAnchorIncludePadding
+			);
+
+			if ( ! anchor ) {
+				return;
+			}
+
+			anchor = addBuffer(
+				anchor,
+				anchorVerticalBuffer,
+				anchorHorizontalBuffer
+			);
+
+			const {
+				popoverTop,
+				popoverLeft,
+				xAxis,
+				yAxis,
+				contentHeight,
+				contentWidth,
+			} = computePopoverPosition( anchor, contentSize, position );
+
+			// Compute the animation position
+			const yAxisMapping = {
+				top: 'bottom',
+				bottom: 'top',
+			};
+			const xAxisMapping = {
+				left: 'right',
+				right: 'left',
+			};
+			const animateYAxis = yAxisMapping[ yAxis ] || 'middle';
+			const animateXAxis = xAxisMapping[ xAxis ] || 'center';
+
+			containerRef.current.setAttribute( 'data-x-axis', xAxis );
+			containerRef.current.setAttribute( 'data-y-axis', yAxis );
+			containerRef.current.style.top = popoverTop + 'px';
+			containerRef.current.style.left = popoverLeft + 'px';
+
+			if ( contentHeight ) {
+				contentRef.current.style.maxHeight = contentHeight + 'px';
+			}
+
+			if ( contentWidth ) {
+				contentRef.current.style.maxWidth = contentWidth + 'px';
+			}
+
+			if ( xAxis === 'center' && yAxis === 'middle' ) {
+				contentRef.current.classList.add( 'is-without-arrow' );
+			} else {
+				contentRef.current.classList.remove( 'is-without-arrow' );
+			}
+
+			setAnimateOrigin( animateXAxis + ' ' + animateYAxis );
+		};
+
+		refresh();
+
+		/*
+		 * There are sometimes we need to reposition or resize the popover that
+		 * are not handled by the resize/scroll window events (i.e. CSS changes
+		 * in the layout that changes the position of the anchor).
+		 *
+		 * For these situations, we refresh the popover every 0.5s
+		 */
+		const intervalHandle = window.setInterval( refresh, 500 );
+		window.addEventListener( 'resize', refresh );
+		window.addEventListener( 'scroll', refresh, true );
+
+		return () => {
+			window.clearInterval( intervalHandle );
+			window.removeEventListener( 'resize', refresh );
+			window.removeEventListener( 'scroll', refresh, true );
+		};
+	}, [
+		isMobileViewport,
+		expandOnMobile,
 		contentSize,
+		anchorRefFallback.current,
```

I'm not certain that adding refs in dependencies is a good practice as changes to these don't rerender anyway.

#### ellatrix on 2019-12-04T11:07:55Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353680436
Path: packages/components/src/popover/index.js

```diff
@@ -361,38 +216,122 @@ const Popover = ( {
 } ) => {
 	const anchorRefFallback = useRef( null );
 	const contentRef = useRef( null );
+	const containerRef = useRef();
+	const [ isMobileViewport, setIsMobileViewport ] = useState( false );
+	const [ animateOrigin, setAnimateOrigin ] = useState();
 
-	// Animation
-	const [ isReadyToAnimate, setIsReadyToAnimate ] = useState( false );
-
-	// Anchor position
-	const anchor = useAnchor(
-		anchorRefFallback,
-		contentRef,
-		anchorRect,
-		getAnchorRect,
-		anchorRef,
-		shouldAnchorIncludePadding,
-		anchorVerticalBuffer,
-		anchorHorizontalBuffer
-	);
+	useEffect( () => {
+		const resize = () => setIsMobileViewport( window.innerWidth < 782 );
+		window.addEventListener( 'resize', resize );
+		return () => window.removeEventListener( 'resize', resize );
+	} );
 
 	// Content size
 	const contentSize = useInitialContentSize( contentRef );
+
 	useEffect( () => {
-		if ( contentSize ) {
-			setIsReadyToAnimate( true );
+		if ( isMobileViewport && expandOnMobile ) {
+			return;
+		}
+
+		if ( ! contentSize ) {
+			return;
 		}
-	}, [ contentSize ] );
 
-	// Compute the position
-	const popoverPosition = usePopoverPosition(
-		anchor,
+		const refresh = () => {
+			let anchor = computeAnchorRect(
+				anchorRefFallback,
+				anchorRect,
+				getAnchorRect,
+				anchorRef,
+				shouldAnchorIncludePadding
+			);
+
+			if ( ! anchor ) {
+				return;
+			}
+
+			anchor = addBuffer(
+				anchor,
+				anchorVerticalBuffer,
+				anchorHorizontalBuffer
+			);
+
+			const {
+				popoverTop,
+				popoverLeft,
+				xAxis,
+				yAxis,
+				contentHeight,
+				contentWidth,
+			} = computePopoverPosition( anchor, contentSize, position );
+
+			// Compute the animation position
+			const yAxisMapping = {
+				top: 'bottom',
+				bottom: 'top',
+			};
+			const xAxisMapping = {
+				left: 'right',
+				right: 'left',
+			};
+			const animateYAxis = yAxisMapping[ yAxis ] || 'middle';
+			const animateXAxis = xAxisMapping[ xAxis ] || 'center';
+
+			containerRef.current.setAttribute( 'data-x-axis', xAxis );
+			containerRef.current.setAttribute( 'data-y-axis', yAxis );
+			containerRef.current.style.top = popoverTop + 'px';
+			containerRef.current.style.left = popoverLeft + 'px';
+
+			if ( contentHeight ) {
+				contentRef.current.style.maxHeight = contentHeight + 'px';
+			}
+
+			if ( contentWidth ) {
+				contentRef.current.style.maxWidth = contentWidth + 'px';
+			}
+
+			if ( xAxis === 'center' && yAxis === 'middle' ) {
+				contentRef.current.classList.add( 'is-without-arrow' );
+			} else {
+				contentRef.current.classList.remove( 'is-without-arrow' );
+			}
+
+			setAnimateOrigin( animateXAxis + ' ' + animateYAxis );
+		};
+
+		refresh();
+
+		/*
+		 * There are sometimes we need to reposition or resize the popover that
+		 * are not handled by the resize/scroll window events (i.e. CSS changes
+		 * in the layout that changes the position of the anchor).
+		 *
+		 * For these situations, we refresh the popover every 0.5s
+		 */
+		const intervalHandle = window.setInterval( refresh, 500 );
+		window.addEventListener( 'resize', refresh );
+		window.addEventListener( 'scroll', refresh, true );
+
+		return () => {
+			window.clearInterval( intervalHandle );
+			window.removeEventListener( 'resize', refresh );
+			window.removeEventListener( 'scroll', refresh, true );
+		};
+	}, [
+		isMobileViewport,
+		expandOnMobile,
 		contentSize,
+		anchorRefFallback.current,
```

Ah, you're right. I didn't think that through.

### Review thread 5 (resolved)

#### youknowriad on 2019-12-04T10:57:44Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353675983
Path: packages/components/src/popover/style.scss

```diff
@@ -183,6 +183,14 @@ $arrow-size: 8px;
 	height: 100%;
 }
 
+.components-popover {
+	opacity: 0;
+}
+
+.components-popover[data-x-axis][data-y-axis] {
+	opacity: 1;
+}
```

I'm not sure I understand why the opacity trick here

#### ellatrix on 2019-12-04T11:06:56Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353679998
Path: packages/components/src/popover/style.scss

```diff
@@ -183,6 +183,14 @@ $arrow-size: 8px;
 	height: 100%;
 }
 
+.components-popover {
+	opacity: 0;
+}
+
+.components-popover[data-x-axis][data-y-axis] {
+	opacity: 1;
+}
```

It's more or less the same as before, where for `master` it's setting `visibility: hidden;` for as long as the position hasn't been calculated. The calculation can only happen when the DOM element has already rendered, but before the calculation happens we don't want the DOM element to show.

#### ellatrix on 2019-12-04T11:07:07Z

URL: https://github.com/WordPress/gutenberg/pull/18813#discussion_r353680074
Path: packages/components/src/popover/style.scss

```diff
@@ -183,6 +183,14 @@ $arrow-size: 8px;
 	height: 100%;
 }
 
+.components-popover {
+	opacity: 0;
+}
+
+.components-popover[data-x-axis][data-y-axis] {
+	opacity: 1;
+}
```

I'll leave an inline comment.

## Changed files

- packages/block-editor/src/components/link-control/test/__snapshots__/index.js.snap (MODIFIED, +1/-1)
- packages/components/src/isolated-event-container/index.js (MODIFIED, +18/-29)
- packages/components/src/popover/index.js (MODIFIED, +111/-232)
- packages/components/src/popover/style.scss (MODIFIED, +24/-13)
- packages/components/src/popover/test/__snapshots__/index.js.snap (MODIFIED, +8/-4)
- packages/components/src/popover/test/utils.js (MODIFIED, +0/-23)
- packages/components/src/popover/utils.js (MODIFIED, +1/-4)
- packages/components/src/tooltip/style.scss (MODIFIED, +2/-2)
- packages/nux/src/components/dot-tip/style.scss (MODIFIED, +15/-15)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +2/-28)
