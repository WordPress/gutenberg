---
number: 53383
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/53383
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Package] Components","Needs Accessibility Feedback"]
---

# PR #53383: ProgressBar: Add default `tabIndex` to make focusable

- URL: https://github.com/WordPress/gutenberg/pull/53383
- Author: tyxla
- Created: 2023-08-07T09:20:15Z
- Updated: 2023-08-10T10:16:56Z
- Comments: 1 of 1
- Review threads: 0 of 0
- Changed files: 3 of 3
- Diff: ../pr-diffs/53383.diff

## Body

## What?
This PR updates the `ProgressBar` component to make it focusable.

cc @alexstine @andrewhayward for accessibility feedback.

## Why?
While working on #53032 I noticed that the underlying `progress` element is not focusable and that makes it inaccessible. That meant we have to make it focusable so users with screen readers will be able to focus it and get it announced properly.

## How?
This PR adds a default `tabIndex` of `0` to the underlying `progress` HTML element to make it focusable. We still allow the component user to override the value if they wish.

I'm also adding a unit test to verify the focusing works as expected. You can try removing the `tabIndex` and you should see the new test failing.

## Testing Instructions
* This PR is part of #53032, so you can test it there.
* Alternatively, spin up Storybook with `npm run storybook:dev`, load http://localhost:50240/?path=/story/components-experimental-progressbar--default
* Verify that the progress bar is focusable and announces properly that it's a progress bar in a loading state with a screen reader.
* Verify all tests pass - the change is covered by a new test I'm introducing. 

### Testing Instructions for Keyboard
Whether in the site editor or in Storybook, the element should be the sole focusable element in the canvas, so tabbing once should be enough.

## Screenshots or screencast <!-- if applicable -->
None.

## Issue comments

### tyxla on 2023-08-10T10:16:56Z

URL: https://github.com/WordPress/gutenberg/pull/53383#issuecomment-1672950148

Thanks for the info, @alexstine and @mirka 🙌 I'm happy to follow up on improving the component if anything occurs in the meantime.

## Review threads

_No review threads._
## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/progress-bar/index.tsx (MODIFIED, +1/-0)
- packages/components/src/progress-bar/test/index.tsx (MODIFIED, +13/-0)
