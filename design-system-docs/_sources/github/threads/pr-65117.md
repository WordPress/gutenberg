---
number: 65117
type: pull_request
state: MERGED
merged_at: 2024-09-11T08:24:18Z
url: https://github.com/WordPress/gutenberg/pull/65117
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #65117: DatePicker: better hover/focus styles

- URL: https://github.com/WordPress/gutenberg/pull/65117
- Author: ciampo
- Created: 2024-09-06T10:08:42Z
- Updated: 2024-09-11T08:24:50Z
- Comments: 4 of 4
- Review threads: 4 of 4
- Changed files: 2 of 2
- Diff: ../pr-diffs/65117.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

DatePicker: fix buggy hover styles and improve focus styles for selected day, add high-contrast mode styles.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Fix bugs, improve accessibility and UI polish

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Tweaking the day button styles.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

- Open the `DatePicker` in storybook
- Make sure that, when hovering over the selected day, the color of the button remains white
- Make sure that, when focusing the selected day, a more clear focus ring is shown
- Make sure that, when in high-contrast more, users can effectively understand what is the selected day
  - In chrome, this can be emulated using dev tools by setting the `forced-colors: active` media feature
 

## Screenshots or screencast <!-- if applicable -->

| Before | After |
|---|---| 
| <video src="https://github.com/user-attachments/assets/59f2f437-b258-4fc8-b6ef-6e9eea3fb6c6" /> | <video src="https://github.com/user-attachments/assets/850bbe1c-a521-4d55-b5b4-f76fbc77b4e3" /> |

## Issue comments

### github-actions on 2024-09-06T10:08:56Z

URL: https://github.com/WordPress/gutenberg/pull/65117#issuecomment-2333725435

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### ciampo on 2024-09-06T16:19:52Z

URL: https://github.com/WordPress/gutenberg/pull/65117#issuecomment-2334403207

Well spotted, @tyxla  — the bug that you highlighted was caused by my code. I had initially used the `::before` pseudo-element for the high-contrast mode, but as you found out, that pseudo-element is already used to display the "event dot".
Switching to the `::after` pseudo-element for high-contrast styles seems to fix the issue.

I also took the opportunity to make the "event dot" work in high contrast mode, too.

### ciampo on 2024-09-09T08:12:58Z

URL: https://github.com/WordPress/gutenberg/pull/65117#issuecomment-2337428693

> is it possible to fix the misaligned event dots when a button gets focus? This issue doesn't seem to occur on trunk

I'm aware of that little jump — it's a consequence of showing an extra "white" focus ring to better indicate when a selected day is focused.

I'll try to apply Lena's [suggestion](https://github.com/WordPress/gutenberg/pull/65117#discussion_r1747713024) and see how that goes

### ciampo on 2024-09-11T07:53:20Z

URL: https://github.com/WordPress/gutenberg/pull/65117#issuecomment-2342917320

I addressed all pending feedback — the component is testing well for me

https://github.com/user-attachments/assets/39064e62-b919-4d5f-a5d0-0863f19834e9


Thank you all for the review, I'll merge once CI checks are 🟢

## Review threads

### Review thread 1 (resolved)

#### mirka on 2024-09-06T20:55:08Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1747706299
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -106,15 +125,14 @@ export const DayButton = styled( Button, {
 		props.hasEvents &&
 		`
 		::before {
-			background: ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
+			border: 2px solid ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
 			border-radius: ${ CONFIG.radiusRound };
+			box-sizing: border-box;
```

Looks like we're adding box-sizing in multiple places in this file, maybe we should just put a box-sizing reset (`packages/components/src/utils/box-sizing.ts`) in the top wrapper.

### Review thread 2 (resolved)

#### mirka on 2024-09-06T20:59:24Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1747710433
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -90,8 +90,27 @@ export const DayButton = styled( Button, {
 		${ ( props ) =>
 			props.isSelected &&
 			`
-			background: ${ COLORS.theme.accent };
-			color: ${ COLORS.white };
+				background: ${ COLORS.theme.accent };
+
+				&,
+				&:hover:not(:disabled, [aria-disabled=true]) {
+					color: ${ COLORS.white };
```

Not sure how relevant this still is, but since we're here, using a semantic color here may be better for eventual theming.

```suggestion
					color: ${ COLORS.theme.foregroundInverted };
```

#### ciampo on 2024-09-09T08:13:23Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1749785315
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -90,8 +90,27 @@ export const DayButton = styled( Button, {
 		${ ( props ) =>
 			props.isSelected &&
 			`
-			background: ${ COLORS.theme.accent };
-			color: ${ COLORS.white };
+				background: ${ COLORS.theme.accent };
+
+				&,
+				&:hover:not(:disabled, [aria-disabled=true]) {
+					color: ${ COLORS.white };
```

Sounds good!

### Review thread 3 (resolved)

#### mirka on 2024-09-06T21:00:10Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1747711098
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -106,15 +125,14 @@ export const DayButton = styled( Button, {
 		props.hasEvents &&
 		`
 		::before {
-			background: ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
+			border: 2px solid ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
```

Same nit re: theming. Similarly, the `COLORS.gray`s can be switched to `COLORS.theme.gray` if we still care. (Do we...? 😅)

```suggestion
			border: 2px solid ${ props.isSelected ? COLORS.theme.accentInverted : COLORS.theme.accent };
```

#### ciampo on 2024-09-09T08:13:29Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1749785455
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -106,15 +125,14 @@ export const DayButton = styled( Button, {
 		props.hasEvents &&
 		`
 		::before {
-			background: ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
+			border: 2px solid ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
```

Why not 😅

### Review thread 4 (resolved)

#### mirka on 2024-09-06T21:02:22Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1747713024
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -106,15 +125,14 @@ export const DayButton = styled( Button, {
 		props.hasEvents &&
 		`
 		::before {
-			background: ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
+			border: 2px solid ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
 			border-radius: ${ CONFIG.radiusRound };
+			box-sizing: border-box;
 			bottom: 2px;
```

I noticed that since we added the focus border, it changes the metrics for the absolute `bottom: 2px` positioning and bumps the dot position every time the focus moves in and out. Maybe we should position it with `translateY`? `translateY(9px)` seems to work for me.

#### ciampo on 2024-09-09T08:14:13Z

URL: https://github.com/WordPress/gutenberg/pull/65117#discussion_r1749786551
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -106,15 +125,14 @@ export const DayButton = styled( Button, {
 		props.hasEvents &&
 		`
 		::before {
-			background: ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
+			border: 2px solid ${ props.isSelected ? COLORS.white : COLORS.theme.accent };
 			border-radius: ${ CONFIG.radiusRound };
+			box-sizing: border-box;
 			bottom: 2px;
```

Good idea, I'll try that out and see how it goes

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/date-time/date/styles.ts (MODIFIED, +31/-11)
