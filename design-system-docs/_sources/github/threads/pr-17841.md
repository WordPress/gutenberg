---
number: 17841
type: pull_request
state: MERGED
merged_at: 2019-11-01T21:03:34Z
url: https://github.com/WordPress/gutenberg/pull/17841
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","[Package] Block editor","[Feature] Themes"]
---

# PR #17841: Add editor-gradient-presets to get_theme_support

- URL: https://github.com/WordPress/gutenberg/pull/17841
- Author: spacedmonkey
- Created: 2019-10-08T10:34:03Z
- Updated: 2020-02-12T11:35:36Z
- Comments: 4 of 4
- Review threads: 4 of 4
- Changed files: 3 of 3
- Diff: ../pr-diffs/17841.diff

## Body

## Description
Fixes #17774

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->

## Issue comments

### swissspidy on 2019-10-08T11:52:22Z

URL: https://github.com/WordPress/gutenberg/pull/17841#issuecomment-539477528

Let's add this to the Theme Support documentation, in the same manner as the color palettes docs:

https://github.com/WordPress/gutenberg/blob/0ad0414e7a469254ef1ad105fdcf14248a986686/docs/designers-developers/developers/themes/theme-support.md#block-color-palettes

### spacedmonkey on 2019-10-08T12:49:34Z

URL: https://github.com/WordPress/gutenberg/pull/17841#issuecomment-539497076

> Let's add this to the Theme Support documentation, in the same manner as the color palettes docs:
> 
@swissspidy I have made a pass at [docs](https://github.com/WordPress/gutenberg/pull/17841/files#diff-c14ad3a4c41ef0d7bf709f5ebe52ba64R137-R168). But I am not sure we should continue with docs, until the api is design is complete.

### jorgefilipecosta on 2019-11-01T21:03:29Z

URL: https://github.com/WordPress/gutenberg/pull/17841#issuecomment-548950091

I added a small commit that applies the suggestions. I think we can merge this PR and continue the work. If there is any comment on the last changes, we can update the things in a follow-up PR. Thank you for submitting this PR @spacedmonkey 👍

### spacedmonkey on 2019-11-07T12:34:38Z

URL: https://github.com/WordPress/gutenberg/pull/17841#issuecomment-551061135

Thanks for the feedback. Great to see this merged!

## Review threads

### Review thread 1 (resolved)

#### jorgefilipecosta on 2019-10-21T08:59:33Z

URL: https://github.com/WordPress/gutenberg/pull/17841#discussion_r336905120
Path: docs/designers-developers/developers/themes/theme-support.md

```diff
@@ -134,6 +134,38 @@ Themes are responsible for creating the classes that apply the colors in differe
 
 The class name is built appending 'has-', followed by the class name _using_ kebab case and ending with the context name.
 
+### Block Gradient Presents
+
+Different blocks have the possibility of selecting from a list of predined of gradients. The block editor provides a default gradient presets, but a theme can overwrite them and provide its own:
+
+```php
+add_theme_support( 'editor-gradient-presets', array(
+	array(
+		'name' => __( 'Vivid cyan blue to vivid purple', 'themeLangDomain' ),
```

In https://github.com/WordPress/gutenberg/pull/18008, I'm adding the class support for present gradients. To use classes, we need a slug to uniquely identify a gradient independently of the language or gradient value. Could we already add a slug to our docs given that we know it is going to be needed?

### Review thread 2

#### jorgefilipecosta on 2019-10-21T09:05:16Z

URL: https://github.com/WordPress/gutenberg/pull/17841#discussion_r336907558
Path: lib/experiments-page.php

```diff
@@ -138,6 +138,12 @@ function gutenberg_experiments_editor_settings( $settings ) {
 		'__experimentalEnableFullSiteEditing'   => $experiments_exist ? array_key_exists( 'gutenberg-full-site-editing', get_option( 'gutenberg-experiments' ) ) : false,
 
 	);
+
+	$gradient_presets = current( (array) get_theme_support( 'editor-gradient-presets' ) );
+	if ( false !== $gradient_presets ) {
+		$experiments_settings['gradients'] = $gradient_presets;
```

We have a problem where KSES rules in multi-sites remove spacing from gradients and then make the block look invalid.
PR fixing the problem here for default gradients here: https://github.com/WordPress/gutenberg/pull/17940

I guess we need to process the gradient values against our KSES function that removes the spaces, before setting it as setting, so even if devs pass values with space the spacing is removed.

If we prefer to solve this issue in a follow up PR, I guess we should update our samples to not include the spaces.

### Review thread 3 (resolved)

#### jorgefilipecosta on 2019-10-21T09:07:02Z

URL: https://github.com/WordPress/gutenberg/pull/17841#discussion_r336908342
Path: lib/experiments-page.php

```diff
@@ -138,6 +138,12 @@ function gutenberg_experiments_editor_settings( $settings ) {
 		'__experimentalEnableFullSiteEditing'   => $experiments_exist ? array_key_exists( 'gutenberg-full-site-editing', get_option( 'gutenberg-experiments' ) ) : false,
 
 	);
+
+	$gradient_presets = current( (array) get_theme_support( 'editor-gradient-presets' ) );
```

We are doing some work around these APIs, e.g: automatically generate the styles, provide better hooks/API for blocks, etc...
I guess until this work is on a good path we should keep this API experimental e.g.:
```
$gradient_presets = current( (array) get_theme_support( '__experimental-editor-gradient-presets' ) );
```

### Review thread 4

#### youknowriad on 2019-11-19T08:24:06Z

URL: https://github.com/WordPress/gutenberg/pull/17841#discussion_r347783621
Path: docs/designers-developers/developers/themes/theme-support.md

```diff
@@ -134,6 +134,46 @@ Themes are responsible for creating the classes that apply the colors in differe
 
 The class name is built appending 'has-', followed by the class name _using_ kebab case and ending with the context name.
 
+### Block Gradient Presents
+
+Different blocks have the possibility of selecting from a list of predined of gradients. The block editor provides a default gradient presets, but a theme can overwrite them and provide its own:
+
+```php
+add_theme_support(
+	'__experimental-editor-gradient-presets',
```

Any particular reason to keep this as experimental for now? When do you think we should make it stable?

## Changed files

- docs/designers-developers/developers/themes/theme-support.md (MODIFIED, +40/-0)
- lib/experiments-page.php (MODIFIED, +6/-0)
- packages/editor/src/components/provider/index.js (MODIFIED, +1/-0)
