---
number: 40627
type: pull_request
state: MERGED
merged_at: 2022-04-28T13:12:29Z
url: https://github.com/WordPress/gutenberg/pull/40627
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #40627: UnitControl: Add flag for larger default size

- URL: https://github.com/WordPress/gutenberg/pull/40627
- Author: mirka
- Created: 2022-04-26T14:39:06Z
- Updated: 2022-05-10T20:49:02Z
- Comments: 0 of 0
- Review threads: 1 of 1
- Changed files: 3 of 3
- Diff: ../pr-diffs/40627.diff

## Body

Part of #39397

## What?

Adds the temporary prop `__next36pxDefaultSize` to opt into the new 36px height default size.

## Why?

As part of the effort to move toward consistent component sizes.

## How?

Details of the migration plan are written in the original issue #39397.

## Testing Instructions

1. `npm run storybook:dev`
2. Go to the story for `UnitControl`. By setting the `__next36pxDefaultSize` control to `true`, the height of the `default` size variant should increase to 36px. All other size variants are unchanged.

## Screenshots or screencast <!-- if applicable -->

### Old default height

<img src="https://user-images.githubusercontent.com/555336/165324584-a6260081-3514-4fc0-8845-64a5b4f92e1b.png" alt="Old default height">

### New default height (opt-in for now)

<img src="https://user-images.githubusercontent.com/555336/165324484-b04f420c-95ce-4cd0-875a-81fdb210cc3f.png" alt="New default height">

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### mirka on 2022-04-26T14:43:01Z

URL: https://github.com/WordPress/gutenberg/pull/40627#discussion_r858803211
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -26,21 +26,11 @@ export const Root = styled.div`
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits, size }: InputProps ) => {
-	const paddings = {
-		default: {
-			paddingRight: 8,
-		},
-		small: {
-			paddingRight: 8,
-		},
-		'__unstable-large': {
-			paddingRight: disableUnits ? 16 : 8,
-		},
-	};
+const paddingStyles = ( { disableUnits }: InputProps ) => {
+	if ( disableUnits ) return '';
 
 	return css`
-		${ rtl( paddings[ size ] )() };
+		${ rtl( { paddingRight: 8 } )() };
```

I simplified the logic here to capture the intent better. Basically, we want the padding-right to always be 8px when units are enabled. Otherwise, it simply inherits from the paddings that `InputControl` specifies.

#### ciampo on 2022-04-27T17:30:47Z

URL: https://github.com/WordPress/gutenberg/pull/40627#discussion_r860076963
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -26,21 +26,11 @@ export const Root = styled.div`
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits, size }: InputProps ) => {
-	const paddings = {
-		default: {
-			paddingRight: 8,
-		},
-		small: {
-			paddingRight: 8,
-		},
-		'__unstable-large': {
-			paddingRight: disableUnits ? 16 : 8,
-		},
-	};
+const paddingStyles = ( { disableUnits }: InputProps ) => {
+	if ( disableUnits ) return '';
 
 	return css`
-		${ rtl( paddings[ size ] )() };
+		${ rtl( { paddingRight: 8 } )() };
```

Love this, so much easier to read!

Not for this PR, but I wonder if we should specify these amounts using the space utility (i.e. multiples of `4`) — we should probably do an overhaul of the config before that, though.

#### mirka on 2022-04-28T11:45:28Z

URL: https://github.com/WordPress/gutenberg/pull/40627#discussion_r860791881
Path: packages/components/src/unit-control/styles/unit-control-styles.ts

```diff
@@ -26,21 +26,11 @@ export const Root = styled.div`
 	position: relative;
 `;
 
-const paddingStyles = ( { disableUnits, size }: InputProps ) => {
-	const paddings = {
-		default: {
-			paddingRight: 8,
-		},
-		small: {
-			paddingRight: 8,
-		},
-		'__unstable-large': {
-			paddingRight: disableUnits ? 16 : 8,
-		},
-	};
+const paddingStyles = ( { disableUnits }: InputProps ) => {
+	if ( disableUnits ) return '';
 
 	return css`
-		${ rtl( paddings[ size ] )() };
+		${ rtl( { paddingRight: 8 } )() };
```

Yes, good point. I think we're supposed to move to this final design for the unit dropdown eventually, so maybe that would be a good time to clean up 👇 

<img width="158" alt="Final UnitControl design with a larger unit dropdown" src="https://user-images.githubusercontent.com/555336/165744940-f515cb48-ef89-46a5-ab98-456a2b516503.png">

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/unit-control/styles/unit-control-styles.ts (MODIFIED, +3/-13)
- packages/components/src/unit-control/types.ts (MODIFIED, +4/-1)
