---
number: 69905
type: pull_request
state: MERGED
merged_at: 2025-05-09T08:33:14Z
url: https://github.com/WordPress/gutenberg/pull/69905
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components"]
---

# PR #69905: Snackbar: Add support to open links in new tab

- URL: https://github.com/WordPress/gutenberg/pull/69905
- Author: yogeshbhutkar
- Created: 2025-04-15T09:48:43Z
- Updated: 2025-05-15T13:39:26Z
- Comments: 4 of 4
- Review threads: 2 of 2
- Changed files: 9 of 9
- Diff: ../pr-diffs/69905.diff

## Body

## What?
Closes #59139

This PR enhances the Snackbar component by adding support for opening links in a new tab. Accordingly, the View Post, View Site, and View Preview links have been updated to open in a new tab by default.

## Why?

Opening these links (View Post, View Site, and View Preview) in a new tab improves the user experience by preserving the current editing context.

## How?

Consumers can now pass an `openInNewTab` boolean property to an action item to open that specific item in a new browser tab.

## Testing Instructions

1. Create a `Post`.
2. Write some content and publish it.
3. After publishing, a snackbar notification will appear.
4. Click on `View Post` in the snackbar notification.
5. Confirm that the link opens in a new tab.

### Testing Instructions for Keyboard

Same.

## Screencast

https://github.com/user-attachments/assets/e4035e27-ebb7-4013-a50b-3e58c74fd4ee

## Issue comments

### github-actions on 2025-04-15T10:56:55Z

URL: https://github.com/WordPress/gutenberg/pull/69905#issuecomment-2804657646

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: yogeshbhutkar <yogeshbhutkar@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mtias <matveb@git.wordpress.org>
Co-authored-by: swissspidy <swissspidy@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### yogeshbhutkar on 2025-04-21T06:38:50Z

URL: https://github.com/WordPress/gutenberg/pull/69905#issuecomment-2817760430

The failing test appears unrelated to the changes introduced in this PR and passes locally. I believe it should pass upon re-running.

### yogeshbhutkar on 2025-05-06T06:17:31Z

URL: https://github.com/WordPress/gutenberg/pull/69905#issuecomment-2853394102

Hi @Mamaduka, if you have some time, I'd really appreciate it if you could review this PR. Thanks in advance!

### Mamaduka on 2025-05-09T08:33:04Z

URL: https://github.com/WordPress/gutenberg/pull/69905#issuecomment-2865651809

Thanks for the feedback, @ciampo!

Let's merge this. We can leave `onClick` and `url` behavior as it is for now, changing it might break backward compatibility.

## Review threads

### Review thread 1 (resolved)

#### Mamaduka on 2025-05-07T12:24:53Z

URL: https://github.com/WordPress/gutenberg/pull/69905#discussion_r2077506964
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -147,22 +148,34 @@ function UnforwardedSnackbar(
 					<div className="components-snackbar__icon">{ icon }</div>
 				) }
 				{ children }
-				{ actions.map( ( { label, onClick, url }, index ) => {
-					return (
-						<Button
-							__next40pxDefaultSize
-							key={ index }
-							href={ url }
-							variant="link"
-							onClick={ (
-								event: MouseEvent< HTMLButtonElement >
-							) => onActionClick( event, onClick ) }
-							className="components-snackbar__action"
-						>
-							{ label }
-						</Button>
-					);
-				} ) }
+				{ actions.map(
+					( { label, onClick, url, openInNewTab = false }, index ) =>
+						url !== undefined && openInNewTab ? (
+							<ExternalLink
+								key={ index }
+								href={ url }
+								onClick={ ( event ) => {
+									event.stopPropagation();
+								} }
```

What's the reason for not passing `onActionClick` as a callback here? Currently, the actions `onClick` will be disregarded when `openInNewTab` is true.

We either need to document this behavior or honor the `onClick` callback. I think the latter option should be preferred if consumers want to dispatch some side events when an action is clicked. For example: track user activity.

cc @WordPress/gutenberg-components

#### yogeshbhutkar on 2025-05-08T09:44:59Z

URL: https://github.com/WordPress/gutenberg/pull/69905#discussion_r2079336744
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -147,22 +148,34 @@ function UnforwardedSnackbar(
 					<div className="components-snackbar__icon">{ icon }</div>
 				) }
 				{ children }
-				{ actions.map( ( { label, onClick, url }, index ) => {
-					return (
-						<Button
-							__next40pxDefaultSize
-							key={ index }
-							href={ url }
-							variant="link"
-							onClick={ (
-								event: MouseEvent< HTMLButtonElement >
-							) => onActionClick( event, onClick ) }
-							className="components-snackbar__action"
-						>
-							{ label }
-						</Button>
-					);
-				} ) }
+				{ actions.map(
+					( { label, onClick, url, openInNewTab = false }, index ) =>
+						url !== undefined && openInNewTab ? (
+							<ExternalLink
+								key={ index }
+								href={ url }
+								onClick={ ( event ) => {
+									event.stopPropagation();
+								} }
```

Thanks for the review, @Mamaduka.

Looking at the types, it feels that both `URL` and `onClick` should not be passed together.

Ref:
https://github.com/WordPress/gutenberg/blob/69b10daa2ae85e3f64153e8e658066676abd8456/packages/components/src/notice/types.ts#L15-L27

In fact, assigning both `url` and `onClick` to an object of type `NoticeAction` results in a type error, which goes away after removing the `onClick`.

![type-mismatch](https://github.com/user-attachments/assets/54091f9a-51d6-41e1-b538-227749af91b4)

Am I missing something here?


Edit: I understand that the comment above the type declaration might point otherwise; it should be refactored for clarification.

Ref:
https://github.com/WordPress/gutenberg/blob/69b10daa2ae85e3f64153e8e658066676abd8456/packages/components/src/snackbar/README.md#L39

https://github.com/WordPress/gutenberg/blob/69b10daa2ae85e3f64153e8e658066676abd8456/packages/components/src/snackbar/types.ts#L49-L50

Here are some references that indicate either `url` or `onClick` should be defined.

#### Mamaduka on 2025-05-08T09:51:07Z

URL: https://github.com/WordPress/gutenberg/pull/69905#discussion_r2079355694
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -147,22 +148,34 @@ function UnforwardedSnackbar(
 					<div className="components-snackbar__icon">{ icon }</div>
 				) }
 				{ children }
-				{ actions.map( ( { label, onClick, url }, index ) => {
-					return (
-						<Button
-							__next40pxDefaultSize
-							key={ index }
-							href={ url }
-							variant="link"
-							onClick={ (
-								event: MouseEvent< HTMLButtonElement >
-							) => onActionClick( event, onClick ) }
-							className="components-snackbar__action"
-						>
-							{ label }
-						</Button>
-					);
-				} ) }
+				{ actions.map(
+					( { label, onClick, url, openInNewTab = false }, index ) =>
+						url !== undefined && openInNewTab ? (
+							<ExternalLink
+								key={ index }
+								href={ url }
+								onClick={ ( event ) => {
+									event.stopPropagation();
+								} }
```

Unfortunately, TS errors don't mean that consumers won't use it.

Additionally, `onActionClick` already contains logic that we're duplicating. What was the initial reason for using a separate `onClick` callback?

#### yogeshbhutkar on 2025-05-08T11:04:49Z

URL: https://github.com/WordPress/gutenberg/pull/69905#discussion_r2079500716
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -147,22 +148,34 @@ function UnforwardedSnackbar(
 					<div className="components-snackbar__icon">{ icon }</div>
 				) }
 				{ children }
-				{ actions.map( ( { label, onClick, url }, index ) => {
-					return (
-						<Button
-							__next40pxDefaultSize
-							key={ index }
-							href={ url }
-							variant="link"
-							onClick={ (
-								event: MouseEvent< HTMLButtonElement >
-							) => onActionClick( event, onClick ) }
-							className="components-snackbar__action"
-						>
-							{ label }
-						</Button>
-					);
-				} ) }
+				{ actions.map(
+					( { label, onClick, url, openInNewTab = false }, index ) =>
+						url !== undefined && openInNewTab ? (
+							<ExternalLink
+								key={ index }
+								href={ url }
+								onClick={ ( event ) => {
+									event.stopPropagation();
+								} }
```

> What was the initial reason for using a separate onClick callback?

I was under the assumption that if `URL` is provided then according to the types, it's certain that onClick might not be provided, therefore, I wrote a short callback to avoid the `onClick` checks.

> TS errors don't mean that consumers won't use it. 

I now understand this point and have implemented the suggested changes. Please review it at your convenience.

Thanks for the guidance, and apologies for any inconvenience caused.

### Review thread 2 (resolved)

#### ciampo on 2025-05-08T19:30:21Z

URL: https://github.com/WordPress/gutenberg/pull/69905#discussion_r2080348436
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -147,22 +148,34 @@ function UnforwardedSnackbar(
 					<div className="components-snackbar__icon">{ icon }</div>
 				) }
 				{ children }
-				{ actions.map( ( { label, onClick, url }, index ) => {
-					return (
-						<Button
-							__next40pxDefaultSize
-							key={ index }
-							href={ url }
-							variant="link"
-							onClick={ (
-								event: MouseEvent< HTMLButtonElement >
-							) => onActionClick( event, onClick ) }
-							className="components-snackbar__action"
-						>
-							{ label }
-						</Button>
-					);
-				} ) }
+				{ actions.map(
+					( { label, onClick, url, openInNewTab = false }, index ) =>
+						url !== undefined && openInNewTab ? (
+							<ExternalLink
+								key={ index }
+								href={ url }
+								onClick={ (
+									event: MouseEvent< HTMLAnchorElement >
```

Is typing the `event` argument necessary, or can we let TS infer it?

#### yogeshbhutkar on 2025-05-09T04:08:39Z

URL: https://github.com/WordPress/gutenberg/pull/69905#discussion_r2080888308
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -147,22 +148,34 @@ function UnforwardedSnackbar(
 					<div className="components-snackbar__icon">{ icon }</div>
 				) }
 				{ children }
-				{ actions.map( ( { label, onClick, url }, index ) => {
-					return (
-						<Button
-							__next40pxDefaultSize
-							key={ index }
-							href={ url }
-							variant="link"
-							onClick={ (
-								event: MouseEvent< HTMLButtonElement >
-							) => onActionClick( event, onClick ) }
-							className="components-snackbar__action"
-						>
-							{ label }
-						</Button>
-					);
-				} ) }
+				{ actions.map(
+					( { label, onClick, url, openInNewTab = false }, index ) =>
+						url !== undefined && openInNewTab ? (
+							<ExternalLink
+								key={ index }
+								href={ url }
+								onClick={ (
+									event: MouseEvent< HTMLAnchorElement >
```

You're right. Updated the PR to implement this. Thanks!

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/notice/README.md (MODIFIED, +1/-0)
- packages/components/src/notice/types.ts (MODIFIED, +3/-1)
- packages/components/src/snackbar/index.tsx (MODIFIED, +30/-17)
- packages/components/src/snackbar/style.scss (MODIFIED, +2/-1)
- packages/components/src/snackbar/types.ts (MODIFIED, +4/-1)
- packages/editor/src/store/private-actions.js (MODIFIED, +1/-0)
- packages/editor/src/store/utils/notice-builder.js (MODIFIED, +1/-0)
- packages/editor/src/store/utils/test/notice-builder.js (MODIFIED, +3/-1)
