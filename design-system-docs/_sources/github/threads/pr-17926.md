---
number: 17926
type: pull_request
state: MERGED
merged_at: 2019-11-28T01:22:55Z
url: https://github.com/WordPress/gutenberg/pull/17926
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Package] Components","Needs Accessibility Feedback"]
---

# PR #17926: Components: Add a WAI-ARIA compliant custom select.

- URL: https://github.com/WordPress/gutenberg/pull/17926
- Author: epiqueras
- Created: 2019-10-13T19:54:04Z
- Updated: 2023-07-24T12:52:50Z
- Comments: 52 of 52
- Review threads: 11 of 11
- Changed files: 14 of 14
- Diff: ../pr-diffs/17926.diff

## Body

Closes #16473

## Description

This PR implements a fully accessible and customizable select component with individually style-able items, in the `@wordpress/components` package.

It uses [Downshift](https://github.com/downshift-js/downshift), a library that will be very useful for any of our other complex a11y input needs.

It has some modifications hooked on top of it to make it fully compliant with [the spec](https://www.w3.org/TR/wai-aria-practices/examples/listbox/listbox-collapsible.html). These modifications might soon make it into a future [Downshift](https://github.com/downshift-js/downshift) release and at that point we should remove them from our implementations.

**The story for the component is:**

https://deploy-preview-17926--gutenberg-playground.netlify.com/design-system/components/?path=/story/customselect--default

## How has this been tested?

It was verified that the component works as expected.

Although the libraries used are heavily tested, unit testing this implementation once the approach is approved wouldn't be a bad idea to make sure we never regress to configuring something incorrectly.

## Screenshots

<img width="789" alt="Screen Shot 2019-10-13 at 12 46 18 PM" src="https://user-images.githubusercontent.com/19157096/66721104-79d04080-edb7-11e9-8e2c-5563098ef779.png">

## Types of Changes

*New Feature:* `@wordpress/components` now has a fully WAI-ARIA compliant custom select.

## Checklist:

- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->

## Issue comments

### youknowriad on 2019-10-14T09:28:49Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-541576327

Design-wise the options should be shown in a Popover I think.

### epiqueras on 2019-10-14T18:38:37Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-541848233

>Design-wise the options should be shown in a Popover I think.

I want to tick all the `a11y` boxes first, and then see what we can do with styling without regressing.

### epiqueras on 2019-10-18T21:11:24Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-543948352

@tellthemachines Thanks for the help!

>Removing display: inline-flex and adding text-align: left to components-button will fix it for both browsers 🙂

Done :smile:

### enriquesanchez on 2019-10-22T22:31:44Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-545184079

I ran some tests of the component in https://deploy-preview-17926--gutenberg-playground.netlify.com/design-system/components/?path=/story/customselect--default with Dragon, NVDA and Voice Over.

Here are the videos of the test:

**With Dragon: https://cldup.com/cihauaUTN4.m4v**
So far with Dragon I haven't able to select an option from the list by saying "Click Large", but I'm able to do so if I use the "move up/down" commands to switch focus to an option and then say "press enter".  I'm a fairly new user of speech recognition software, so I'm not sure how common or uncommon this flow is. That being said, I was able to interact with the component and select an option.

**With NVDA: https://cldup.com/7n79VbZMo3.m4v** (sorry for the shaky camera)
Besides "Font Size" being announced repetitively, the only thing that I felt was missing was that on Windows I can't change the selection by using the up/down keys, like on Mac.

**With Voice Over: https://cldup.com/A12FmcNrSn.m4v**
Same as with NVDA, "Font Size" is announced repetitively. Other than that, interaction was as expected.

### tellthemachines on 2019-10-22T22:42:08Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-545186839

> Besides "Font Size" being announced repetitively, the only thing that I felt was missing was that on Windows I can't change the selection by using the up/down keys, like on Mac.

@enriquesanchez this happens because NVDA is not switching into forms mode when entering this component. If you manually switch by pressing Insert+Space then you'll be able to use up/down keys. It would be worth investigating if it's possible to make NVDA switch automatically as that would be closer in behaviour to a native control.

### adamsilverstein on 2019-11-03T15:29:58Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-549148194

Wanted to mention previous work to use Downshift for the post author selector in https://github.com/WordPress/gutenberg/pull/7478. It might be worth reviewing the comments on that ticket - ultimately, the accessibility team rejected this approach and we settled instead on using `accessible-autocomplete` - see https://github.com/WordPress/gutenberg/pull/7385.

@enriquesanchez - had you reviewed this previous work? What do you think of Downshift vs. accessible-autocomplete?

### silviuaavram on 2019-11-03T15:40:36Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-549149313

Adam can you mention the reasons for making that decision? I am interested
for improvement reasons at least. Thanks!

On Sun, 3 Nov 2019 at 15:30, Adam Silverstein <notifications@github.com>
wrote:

> Wanted to mention previous work to use Downshift for the post author
> selector in #7478 <https://github.com/WordPress/gutenberg/pull/7478>. It
> might be worth reviewing the comments on that ticket - ultimately, the
> accessibility team rejected this approach and we settled instead on using
> accessible-autocomplete - see #7385
> <https://github.com/WordPress/gutenberg/pull/7385>.
>
> @enriquesanchez <https://github.com/enriquesanchez> - had you reviewed
> this previous work? What do you think of Downshift vs.
> accessible-autocomplete?
>
> —
> You are receiving this because you commented.
> Reply to this email directly, view it on GitHub
> <https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=ACWAZACSHLQ5J4LF4BSAP3TQR3N7PA5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEC5VMIQ#issuecomment-549148194>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/ACWAZAHD6WIVNVM6P4PRGDTQR3N7PANCNFSM4JAIMOLQ>
> .
>

### epiqueras on 2019-11-05T01:09:09Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-549618239

I've made some optimizations and replaced the font size picker's select with this one.

Can we move forward with this PR and iterate in following ones now?

@tellthemachines @enriquesanchez @youknowriad

### tellthemachines on 2019-11-06T00:49:44Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-550090522

> Adam can you mention the reasons for making that decision? I am interested for improvement reasons at least. Thanks!

@silviuavram [this comment](https://github.com/WordPress/gutenberg/pull/7478#issuecomment-400599022) explains the reasons, though that was over a year ago so perhaps some of those issues have been fixed by now.

### silviuaavram on 2019-11-06T08:17:45Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-550197433

@tellthemachines yes, it comes from Downshift. I followed the pattern from [the ARIA website](https://www.w3.org/TR/wai-aria-practices/examples/listbox/listbox-collapsible.html).

It doesn't make sense to me to have a labelledby if the screen reader already knows how to read the text from it. It does not really help if the button has no text either.

I can add a PR in Downshift for this tomorrow, since I am at WebConference right now. Or you can do that and I will approve tomorrow. The labelledby should probably point only to the label. What do you think?

### epiqueras on 2019-11-06T18:06:24Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-550431736

@tellthemachines Thanks for the thorough reviews and fixes :smile:! It should be good to go now.

>The labelledby should probably point only to the label. What do you think?

We don't need it in this case, because we explicitly set an `aria-label` which has better support from voice control software. It was double labelled, once by the `aria-label` and once by the `aria-labelledby`. I cleared the `aria-labelledby`.

### silviuaavram on 2019-11-06T18:10:39Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-550433396

Oh ok, I understand. Interesting why a native button with text would ever need an aria-label. :(

### epiqueras on 2019-11-06T18:30:29Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-550441076

Voice control software doesn't follow the spec :cry:

### enriquesanchez on 2019-11-16T00:02:44Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-554576307

I did a quick test using Voice Control on the Mac:

![voiceControl 2019-11-15 17_53_55](https://user-images.githubusercontent.com/3276087/68983531-087a1800-07d1-11ea-8675-4aae97bfeaf7.gif)

[Here's the video recording](https://cldup.com/f0JbC7gh0m.m4v) 

Based on the little experience I've had with it, Voice Control is a bit different that Dragon. Manipulation is done via a grid and numbers, so labels have no effect here.

That being said, I was able to interact with the component and select different font sizes.

### epiqueras on 2019-11-20T22:48:01Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-556506696

Can we get another review please :smile:

cc @youknowriad @tellthemachines @gziolo

### tellthemachines on 2019-11-27T06:22:09Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-558948246

The labelling is behaving much better now! 
For clarity though, because "font size" is the fieldset legend that applies to all 3 fields inside it, each field should have its own label. That will help for Dragon users to be able to interact with them too. @enriquesanchez and I discussed this previously and came up with this design:

<img width="561" alt="Screen Shot 2019-09-25 at 6 33 31 PM" src="https://user-images.githubusercontent.com/8096000/69698382-48c68800-1139-11ea-9cea-ef97423390c1.png">

Where 
- the "font size" fieldset legend is visually hidden, because sighted users should be able to grasp what those fields are about from the surrounding context;
- "Size preset" (or "Preset size", which sounds better?) is the visible label for the picker, and
- "Custom" is the visible label for the range control.

There's also a little weirdness with the alignment of the options; the checked one is bumped slightly to the left due to the negative margin on the SVG. But that could be left for a follow-up PR 😅

### epiqueras on 2019-11-27T17:50:56Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-559190577

@tellthemachines Thanks for taking another look! :smiley:

I think "Preset Size" sounds better. It makes more sense for "Size" to be the noun since its the more important word in this context.

I implemented that design and fixed the weirdness in the alignment with the check mark.

### epiqueras on 2019-11-28T01:24:10Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-559304468

Just in time for the :turkey: :tada: !

### youknowriad on 2019-11-28T08:00:03Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-559383035

Nice work here :) A good follow-up would be to check where we're using `per_page: -1` in REST API calls and replace them with a paginated Select.

### epiqueras on 2019-11-28T16:48:42Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-559558449

>Nice work here :) A good follow-up would be to check where we're using per_page: -1 in REST API calls and replace them with a paginated Select.

It's on my list :smile:

### afercia on 2019-12-03T17:00:32Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561260619

Looks like there are a few missing things in this component, will comment on the issue.

### aduth on 2019-12-03T17:30:52Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561273740

Noting that this Downshift dependency is fairly large at 32.4kb, contributing ~200ms additional load time over 3G.

https://bundlephobia.com/result?p=downshift@3.4.5

### silviuaavram on 2019-12-03T17:47:26Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561280203

It is true, the bundle size is getting bigger, and I am close to release `useCombobox`. I am considering having useSelect and other hooks as part of a a11y monorepo, as this size issue popped up to us recently as well. If you'd like to help out in converging to a solution to the bundle size that would be fantastic.

It should not break the current behavior (`useSelect` being as part of `Downshift`) and also it should be part of a monorepo with a11y hooks. It would be interesting if it can be imported in Downshift from that monorepo and tree-shaken if not used. I am pretty new to these concepts so I would love some opinions. Maybe @kentcdodds can help out with an idea.

Thoughts?

### epiqueras on 2019-12-03T17:57:57Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561284068

>It should not break the current behavior (useSelect being as part of Downshift) and also it should be part of a monorepo with a11y hooks. It would be interesting if it can be imported in Downshift from that monorepo and tree-shaken if not used.

Sounds like a reasonable plan.

### silviuaavram on 2019-12-03T18:24:58Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561294780

Although I am thinking ... if you are using `import` to get only `useSelect`, and I am using `rollup` to bundle, shouldn't you get only that hook, and not the whole library? @aduth

### epiqueras on 2019-12-03T18:37:05Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561299515

Yeah `Downshift` has an `esm` distribution. I think our tooling doesn't support it, but we can work on that right @aduth?

### aduth on 2019-12-03T22:15:25Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561381871

If it can be tree-shaken, our build should be able to drop the unused code. I was judging based on the [BundlePhobia page](https://bundlephobia.com/result?p=downshift@3.4.5), specifically this text under "Exports Analysis":

>Exports analysis is available only for packages that export ES Modules and are side-effect free. This package exports ES6 modules, but isn't marked side-effect free.

### epiqueras on 2019-12-03T22:18:46Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561383063

I guess it's not explicitly marked side-effect free, but I don't think it has any side-effects does it?

### aduth on 2019-12-03T23:20:58Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561402130

It still needs to be included in the published `package.json` for Webpack to consider it for tree shaking:

>The new webpack 4 release expands on this capability with a way to provide hints to the compiler via the "`sideEffects`" `package.json` property to denote which files in your project are "pure" and therefore safe to prune if unused.

https://webpack.js.org/guides/tree-shaking/#mark-the-file-as-side-effect-free

### epiqueras on 2019-12-03T23:41:47Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561407644

It looks like it will still try to tree-shake most things, but will trip up with things like HOCs without directives:

https://webpack.js.org/guides/tree-shaking/#clarifying-tree-shaking-and-sideeffects

@silviuavram can we add the `sideEffects` hint to make this more efficient?

### silviuaavram on 2019-12-04T08:13:01Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561526984

I am really interested to try this out, I will make changes on `Downshift` side if needed. Let me know on the progress please!

### epiqueras on 2019-12-04T17:19:37Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561744625

@silviuavram it's `Downshift` that is missing the `sideEffects` property, we need to add it there.

### silviuaavram on 2019-12-04T17:23:27Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561749029

Can you create a PR and I will look over it tomorrow? Thank you! Do we need
to publish an alpha version to test if it works?

On Wed, 4 Dec 2019 at 18:19, Enrique Piqueras <notifications@github.com>
wrote:

> @silviuavram <https://github.com/silviuavram> it's Downshift that is
> missing the sideEffects property, we need to add it there.
>
> —
> You are receiving this because you were mentioned.
> Reply to this email directly, view it on GitHub
> <https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=ACWAZAEBES6R3WBPJB3BDA3QW7RCXA5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEF5YV4I#issuecomment-561744625>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/ACWAZAEFWUKASHJVNY2NJW3QW7RCXANCNFSM4JAIMOLQ>
> .
>

### epiqueras on 2019-12-04T17:35:37Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561757650

@silviuavram https://github.com/downshift-js/downshift/pull/843

>Do we need
to publish an alpha version to test if it works?

Yeah, that would be helpful. It should work, unless `Downshift` has side effects, but better to be safe.

### silviuaavram on 2019-12-04T19:42:41Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-561807930

Thanks very much! I will publish an alpha tomorrow from that branch and
test it out.

On Wed, 4 Dec 2019 at 18:35, Enrique Piqueras <notifications@github.com>
wrote:

> @silviuavram <https://github.com/silviuavram> downshift-js/downshift#843
> <https://github.com/downshift-js/downshift/pull/843>
>
> Do we need
> to publish an alpha version to test if it works?
>
> Yeah, that would be helpful. It should work, unless Downshift has side
> effects, but better to be safe.
>
> —
> You are receiving this because you were mentioned.
> Reply to this email directly, view it on GitHub
> <https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=ACWAZACDGJCFX4WO4ZEKWXDQW7S6XA5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEF533UQ#issuecomment-561757650>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/ACWAZADXQ3NX46KJ3SOKF23QW7S6XANCNFSM4JAIMOLQ>
> .
>

### silviuaavram on 2019-12-05T20:15:11Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562298698

I published downshift@3.5.0-alpha.0 and could not manage to properly test it. Maybe my testing project is not configured as it should. If you have any tips that will help me looking into it tomorrow that will be super helpful.

### epiqueras on 2019-12-05T20:55:56Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562314017

What was the issue?

On Thu, Dec 5, 2019 at 12:15 PM Silviu Alexandru Avram <
notifications@github.com> wrote:

> I published downshift@3.5.0-alpha.0 and could not manage to properly test
> it. Maybe my testing project is not configured as it should. If you have
> any tips that will help me looking into it tomorrow that will be super
> helpful.
>
> —
> You are receiving this because you modified the open/close state.
> Reply to this email directly, view it on GitHub
> <https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=AESFA2GOTNEADODBACKASW3QXFONDA5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEGB76SQ#issuecomment-562298698>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/AESFA2EGR6DXCORG43C43ETQXFONDANCNFSM4JAIMOLQ>
> .
>

### silviuaavram on 2019-12-05T21:08:32Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562318809

I probably did not know what to look for in the minified file. In the
main.map.js I could still find all the exports (useSelect, Downshift,
useCombobox) even though I only imported: import {useCombobox} from
‘downshift’. I probably did not configure the webpack in my test project
correctly. Or it’s something wrong in Downshift’s rollup config or
index.js. Or maybe it works and I could not figure tjat out from the
minified file. I don’t know yet.

On Thu, 5 Dec 2019 at 21:55, Enrique Piqueras <notifications@github.com>
wrote:

> What was the issue?
>
> On Thu, Dec 5, 2019 at 12:15 PM Silviu Alexandru Avram <
> notifications@github.com> wrote:
>
> > I published downshift@3.5.0-alpha.0 and could not manage to properly
> test
> > it. Maybe my testing project is not configured as it should. If you have
> > any tips that will help me looking into it tomorrow that will be super
> > helpful.
> >
> > —
> > You are receiving this because you modified the open/close state.
> > Reply to this email directly, view it on GitHub
> > <
> https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=AESFA2GOTNEADODBACKASW3QXFONDA5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEGB76SQ#issuecomment-562298698
> >,
> > or unsubscribe
> > <
> https://github.com/notifications/unsubscribe-auth/AESFA2EGR6DXCORG43C43ETQXFONDANCNFSM4JAIMOLQ
> >
> > .
> >
>
> —
> You are receiving this because you were mentioned.
> Reply to this email directly, view it on GitHub
> <https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=ACWAZABWTCLZN6KD6HMHY63QXFTF5A5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEGCDWII#issuecomment-562314017>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/ACWAZAGQVQCTX7ZLIGTEZ5TQXFTF5ANCNFSM4JAIMOLQ>
> .
>

### epiqueras on 2019-12-05T22:35:23Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562349435

Can you push your test somewhere?

### silviuaavram on 2019-12-06T08:35:24Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562482658

https://github.com/silviuavram/check-downshift-shake tried it here. But I probably did not configure webpack or babel. The alpha version contains `useCombobx` along with the `sideEffects` in package.json.

### silviuaavram on 2019-12-06T14:11:29Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562586057

I updated the repo with some things I tried on babel. Still it imports the whole things in the ES bundle.

### epiqueras on 2019-12-06T17:40:10Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562668958

Yeah, you're building in development mode:

https://github.com/silviuavram/check-downshift-shake/blob/27a5fcb763ce2b837017a948a6c859ccfa0a0148/package.json#L11-L12

See, https://webpack.js.org/guides/tree-shaking/.

### silviuaavram on 2019-12-06T17:42:53Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562669900

I tried to build with —production as well. The es has the same size as the
cjs.

On Fri, 6 Dec 2019 at 18:40, Enrique Piqueras <notifications@github.com>
wrote:

> Yeah, you're building in development mode:
>
>
> https://github.com/silviuavram/check-downshift-shake/blob/27a5fcb763ce2b837017a948a6c859ccfa0a0148/package.json#L11-L12
>
> See, https://webpack.js.org/guides/tree-shaking/.
>
> —
> You are receiving this because you were mentioned.
> Reply to this email directly, view it on GitHub
> <https://github.com/WordPress/gutenberg/pull/17926?email_source=notifications&email_token=ACWAZAGDSWVR44R3RIMB25TQXKE7ZA5CNFSM4JAIMOL2YY3PNVWWK3TUL52HS4DFVREXG43VMVBW63LNMVXHJKTDN5WW2ZLOORPWSZGOEGE2LHQ#issuecomment-562668958>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/ACWAZAEL6VZESKP6KJFGAKTQXKE7ZANCNFSM4JAIMOLQ>
> .
>

### epiqueras on 2019-12-06T18:24:34Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562684675

I just debugged this and it's probably because of how `Downshift` bundles its `esm` module.

There are a lot of static property assignments in there like `Downshift.stateChangeTypes = stateChangeTypes;` and `useCombobox.stateChangeTypes = stateChangeTypes$2;`.

Some prior discussion:

https://github.com/downshift-js/downshift/issues/380

### silviuaavram on 2019-12-07T15:17:55Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562859730

Looks good, I was also suspicious about those state change things while going through the bundled code. Thanks so much for looking into this!

The bad part is the fact that we need to deliver a breaking change to make the bundle tree-shakeable again: exporting those `stateChangeTypes` separately and removing the static allocation. Will check if indeed, with these changes, the problem will be fixed.

### epiqueras on 2019-12-07T17:47:21Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562871751

There are other workarounds:

- You could break down the output into multiple files with their ESM imports intact and that would allow the `sideEffects` flag to work. Currently, Webpack evaluates the single output file, because there are used exports there, and it can't prune the unused ones because they are mutated. If they were imported and mutated from a different file, Webpack could use the `sideEffects` hint and avoid evaluating those files.

- Another, perhaps easier change, would be to use the `/*#__PURE__*/` annotations. They're like the `sideEffects` hint, but for statements instead of modules.

Related discussion: https://github.com/styled-components/babel-plugin-styled-components/issues/245.

### silviuaavram on 2019-12-07T18:53:12Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562876792

Found a few problems with today's investigation:

The hooks were not tree-shaken because of
```js
const validatePropTypes = getPropTypesValidator(useSelect, propTypes)
```

`Downshift` component not tree-shaken because of the `static` props `stateChangeTypes` and `defaultProps!!!`. The `propTypes` static one was correctly removed by a babel transpiler (I think) at bundle time, like:
```js
process.env.NODE_ENV !== "production" ? Downshift.propTypes = {
  children: PropTypes.func,
  ...
```

Probably I could add this same check manually to the hooks, because I don't need that prop types check function to run in production. On the `Downshift` static fields `stateChangeTypes` and `defaultProps` maybe there is a solution with one of your suggestions?

### epiqueras on 2019-12-07T19:13:36Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-562878346

Yeah, both should work, albeit the first one is a better long-term solution that will work for future code as well.

### youknowriad on 2019-12-09T10:12:21Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-563160032

Any plans to add a README for this component.

### epiqueras on 2019-12-09T22:07:53Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-563462664

#19026

### adamsilverstein on 2019-12-10T23:22:48Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-564305276

With Downshift merged, we should reconsider using it elsewhere (eg for Author selection, page parent selection, etc... eg https://github.com/WordPress/gutenberg/pull/7478). 

@afercia I missed most of the work/conversation here - does the progress on Downshift address the feedback from the original ticket? shall I work on a new PR for you to review?

### epiqueras on 2019-12-10T23:36:02Z

URL: https://github.com/WordPress/gutenberg/pull/17926#issuecomment-564308779

Yes, that is a good next step with it.

## Review threads

### Review thread 1 (resolved)

#### silviuaavram on 2019-10-14T08:00:26Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r334361475
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import Downshift, { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	switch ( type ) {
+		case Downshift.stateChangeTypes.ToggleButtonKeyDownArrowDown:
```

it's actually `useSelect.stateChangeTypes` here and below.

#### epiqueras on 2019-10-14T18:35:52Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r334611992
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import Downshift, { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	switch ( type ) {
+		case Downshift.stateChangeTypes.ToggleButtonKeyDownArrowDown:
```

Thank you!

### Review thread 2 (resolved)

#### silviuaavram on 2019-10-14T08:05:41Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r334363051
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import Downshift, { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
```

related to the Dragon Speech issue, I can look into it. if you are faster at it, I would suggest adding a console log here in the stateReducer and check the `type`, `changes` and `state`. If there is a state change in the select, then it should pass here.

#### epiqueras on 2019-10-14T18:36:45Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r334612337
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import Downshift, { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
```

Great idea!

### Review thread 3

#### tellthemachines on 2019-10-18T00:07:49Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r336276233
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
```

Just checked this on Windows and the behaviour is identical to Mac: on arrow down/up the menu opens. I don't see that as a major issue though. If we did want the arrow keys to work with the menu closed we'd probably have to make sure the options always render, and hide them with CSS, but then it's not clear that we could limit that behaviour to Windows 🤷‍♀

#### silviuaavram on 2019-10-18T15:44:43Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r336556153
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
```

the issue here is the returned `changes`. @epiqueras just return the `selectedItem` because if you also return the rest of the `changes` then one of them is `isOpen` that comes as true :D

#### epiqueras on 2019-10-18T20:55:49Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r336672735
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
```

Fixed :smile:

### Review thread 4

#### tellthemachines on 2019-10-18T00:18:46Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r336278223
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	// TODO: Remove this.
+	// eslint-disable-next-line no-console
+	console.debug(
+		'Selected Item: ',
+		selectedItem,
+		'Type: ',
+		type,
+		'Changes: ',
+		changes,
+		'Items: ',
+		items
+	);
+	switch ( type ) {
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowDown:
+			// If we already have a selected item, try to select the next one,
+			// without circular navigation. Otherwise, select the first item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.min( items.indexOf( selectedItem ) + 1, items.length - 1 ) :
+							0
+					],
+			};
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowUp:
+			// If we already have a selected item, try to select the previous one,
+			// without circular navigation. Otherwise, select the last item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.max( items.indexOf( selectedItem ) - 1, 0 ) :
+							items.length - 1
+					],
+			};
+		default:
+			return changes;
+	}
+};
+export default function CustomSelect( { label, items } ) {
+	const {
+		getLabelProps,
+		getToggleButtonProps,
+		getMenuProps,
+		getItemProps,
+		isOpen,
+		highlightedIndex,
+		selectedItem,
+	} = useSelect( {
+		initialSelectedItem: items[ 0 ],
+		items,
+		itemToString,
+		stateReducer,
+	} );
+	const menuProps = getMenuProps( {
+		className: 'components-custom-select__menu',
+	} );
+	// We need this here, because the null active descendant is not
+	// fully ARIA compliant.
+	if (
+		menuProps[ 'aria-activedescendant' ] &&
+		menuProps[ 'aria-activedescendant' ].slice( 0, 'downshift-null'.length ) ===
+			'downshift-null'
+	) {
+		delete menuProps[ 'aria-activedescendant' ];
+	}
+	return (
+		<div className="components-custom-select">
+			{ /* eslint-disable-next-line jsx-a11y/label-has-associated-control, jsx-a11y/label-has-for */ }
+			<span
+				{ ...getLabelProps( { className: 'components-custom-select__label' } ) }
+			>
+				{ label }
+			</span>
```

Any reason not to make this an actual `<label>` instead of using `aria-labelledby`? Then we might not need the `aria-label`. Having both, VoiceOver and NVDA are announcing the button as "Font size font size". Would be good to avoid the repetition.

#### silviuaavram on 2019-10-18T15:46:52Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r336557180
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	// TODO: Remove this.
+	// eslint-disable-next-line no-console
+	console.debug(
+		'Selected Item: ',
+		selectedItem,
+		'Type: ',
+		type,
+		'Changes: ',
+		changes,
+		'Items: ',
+		items
+	);
+	switch ( type ) {
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowDown:
+			// If we already have a selected item, try to select the next one,
+			// without circular navigation. Otherwise, select the first item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.min( items.indexOf( selectedItem ) + 1, items.length - 1 ) :
+							0
+					],
+			};
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowUp:
+			// If we already have a selected item, try to select the previous one,
+			// without circular navigation. Otherwise, select the last item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.max( items.indexOf( selectedItem ) - 1, 0 ) :
+							items.length - 1
+					],
+			};
+		default:
+			return changes;
+	}
+};
+export default function CustomSelect( { label, items } ) {
+	const {
+		getLabelProps,
+		getToggleButtonProps,
+		getMenuProps,
+		getItemProps,
+		isOpen,
+		highlightedIndex,
+		selectedItem,
+	} = useSelect( {
+		initialSelectedItem: items[ 0 ],
+		items,
+		itemToString,
+		stateReducer,
+	} );
+	const menuProps = getMenuProps( {
+		className: 'components-custom-select__menu',
+	} );
+	// We need this here, because the null active descendant is not
+	// fully ARIA compliant.
+	if (
+		menuProps[ 'aria-activedescendant' ] &&
+		menuProps[ 'aria-activedescendant' ].slice( 0, 'downshift-null'.length ) ===
+			'downshift-null'
+	) {
+		delete menuProps[ 'aria-activedescendant' ];
+	}
+	return (
+		<div className="components-custom-select">
+			{ /* eslint-disable-next-line jsx-a11y/label-has-associated-control, jsx-a11y/label-has-for */ }
+			<span
+				{ ...getLabelProps( { className: 'components-custom-select__label' } ) }
+			>
+				{ label }
+			</span>
```

The way I wrote `useSelect` was with having a physical label that also labels the button and the menu. As I understood there was an issue with Dragon Speech because of this.

Having said that, anyone tried Dragonn Speech to select an item then check what the console.log prints?

#### epiqueras on 2019-10-18T21:03:20Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r336674931
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	// TODO: Remove this.
+	// eslint-disable-next-line no-console
+	console.debug(
+		'Selected Item: ',
+		selectedItem,
+		'Type: ',
+		type,
+		'Changes: ',
+		changes,
+		'Items: ',
+		items
+	);
+	switch ( type ) {
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowDown:
+			// If we already have a selected item, try to select the next one,
+			// without circular navigation. Otherwise, select the first item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.min( items.indexOf( selectedItem ) + 1, items.length - 1 ) :
+							0
+					],
+			};
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowUp:
+			// If we already have a selected item, try to select the previous one,
+			// without circular navigation. Otherwise, select the last item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.max( items.indexOf( selectedItem ) - 1, 0 ) :
+							items.length - 1
+					],
+			};
+		default:
+			return changes;
+	}
+};
+export default function CustomSelect( { label, items } ) {
+	const {
+		getLabelProps,
+		getToggleButtonProps,
+		getMenuProps,
+		getItemProps,
+		isOpen,
+		highlightedIndex,
+		selectedItem,
+	} = useSelect( {
+		initialSelectedItem: items[ 0 ],
+		items,
+		itemToString,
+		stateReducer,
+	} );
+	const menuProps = getMenuProps( {
+		className: 'components-custom-select__menu',
+	} );
+	// We need this here, because the null active descendant is not
+	// fully ARIA compliant.
+	if (
+		menuProps[ 'aria-activedescendant' ] &&
+		menuProps[ 'aria-activedescendant' ].slice( 0, 'downshift-null'.length ) ===
+			'downshift-null'
+	) {
+		delete menuProps[ 'aria-activedescendant' ];
+	}
+	return (
+		<div className="components-custom-select">
+			{ /* eslint-disable-next-line jsx-a11y/label-has-associated-control, jsx-a11y/label-has-for */ }
+			<span
+				{ ...getLabelProps( { className: 'components-custom-select__label' } ) }
+			>
+				{ label }
+			</span>
```

>Any reason not to make this an actual <label> instead of using aria-labelledby? Then we might not need the aria-label. Having both, VoiceOver and NVDA are announcing the button as "Font size font size". Would be good to avoid the repetition.

Dragon Speech still needs the explicit aria label on the toggle to be able to click on it.

>Having said that, anyone tried Dragonn Speech to select an item then check what the console.log prints?

It registers a click on the toggle button I think:

<img width="815" alt="Screen Shot 2019-10-18 at 1 58 17 PM" src="https://user-images.githubusercontent.com/19157096/67128001-5fc7a100-f1af-11e9-9900-a70da5d4d519.png">

In that test^^ "Small" was already the selected item.

#### tellthemachines on 2019-10-22T00:54:28Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r337300470
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	// TODO: Remove this.
+	// eslint-disable-next-line no-console
+	console.debug(
+		'Selected Item: ',
+		selectedItem,
+		'Type: ',
+		type,
+		'Changes: ',
+		changes,
+		'Items: ',
+		items
+	);
+	switch ( type ) {
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowDown:
+			// If we already have a selected item, try to select the next one,
+			// without circular navigation. Otherwise, select the first item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.min( items.indexOf( selectedItem ) + 1, items.length - 1 ) :
+							0
+					],
+			};
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowUp:
+			// If we already have a selected item, try to select the previous one,
+			// without circular navigation. Otherwise, select the last item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.max( items.indexOf( selectedItem ) - 1, 0 ) :
+							items.length - 1
+					],
+			};
+		default:
+			return changes;
+	}
+};
+export default function CustomSelect( { label, items } ) {
+	const {
+		getLabelProps,
+		getToggleButtonProps,
+		getMenuProps,
+		getItemProps,
+		isOpen,
+		highlightedIndex,
+		selectedItem,
+	} = useSelect( {
+		initialSelectedItem: items[ 0 ],
+		items,
+		itemToString,
+		stateReducer,
+	} );
+	const menuProps = getMenuProps( {
+		className: 'components-custom-select__menu',
+	} );
+	// We need this here, because the null active descendant is not
+	// fully ARIA compliant.
+	if (
+		menuProps[ 'aria-activedescendant' ] &&
+		menuProps[ 'aria-activedescendant' ].slice( 0, 'downshift-null'.length ) ===
+			'downshift-null'
+	) {
+		delete menuProps[ 'aria-activedescendant' ];
+	}
+	return (
+		<div className="components-custom-select">
+			{ /* eslint-disable-next-line jsx-a11y/label-has-associated-control, jsx-a11y/label-has-for */ }
+			<span
+				{ ...getLabelProps( { className: 'components-custom-select__label' } ) }
+			>
+				{ label }
+			</span>
```

Hmm can we use the semantic `<label>` and add `aria-label` too? @enriquesanchez 's testing on #17418 showed that approach works with Dragon, and `aria-label` seems to supersede `<label>` so the screen readers don't read out both.

With the current font size controls we're already having an issue where VoiceOver reads out the fieldset legend twice (that's a VO bug, but still annoying), so it would be good to avoid unnecessary repetition wherever we can.

#### epiqueras on 2019-11-05T00:54:49Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r342341634
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,137 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	// TODO: Remove this.
+	// eslint-disable-next-line no-console
+	console.debug(
+		'Selected Item: ',
+		selectedItem,
+		'Type: ',
+		type,
+		'Changes: ',
+		changes,
+		'Items: ',
+		items
+	);
+	switch ( type ) {
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowDown:
+			// If we already have a selected item, try to select the next one,
+			// without circular navigation. Otherwise, select the first item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.min( items.indexOf( selectedItem ) + 1, items.length - 1 ) :
+							0
+					],
+			};
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowUp:
+			// If we already have a selected item, try to select the previous one,
+			// without circular navigation. Otherwise, select the last item.
+			return {
+				...changes,
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.max( items.indexOf( selectedItem ) - 1, 0 ) :
+							items.length - 1
+					],
+			};
+		default:
+			return changes;
+	}
+};
+export default function CustomSelect( { label, items } ) {
+	const {
+		getLabelProps,
+		getToggleButtonProps,
+		getMenuProps,
+		getItemProps,
+		isOpen,
+		highlightedIndex,
+		selectedItem,
+	} = useSelect( {
+		initialSelectedItem: items[ 0 ],
+		items,
+		itemToString,
+		stateReducer,
+	} );
+	const menuProps = getMenuProps( {
+		className: 'components-custom-select__menu',
+	} );
+	// We need this here, because the null active descendant is not
+	// fully ARIA compliant.
+	if (
+		menuProps[ 'aria-activedescendant' ] &&
+		menuProps[ 'aria-activedescendant' ].slice( 0, 'downshift-null'.length ) ===
+			'downshift-null'
+	) {
+		delete menuProps[ 'aria-activedescendant' ];
+	}
+	return (
+		<div className="components-custom-select">
+			{ /* eslint-disable-next-line jsx-a11y/label-has-associated-control, jsx-a11y/label-has-for */ }
+			<span
+				{ ...getLabelProps( { className: 'components-custom-select__label' } ) }
+			>
+				{ label }
+			</span>
```

Done!

### Review thread 5

#### tellthemachines on 2019-10-22T00:56:47Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r337300804
Path: packages/components/src/custom-select/style.scss

```diff
@@ -0,0 +1,50 @@
+.components-custom-select {
+	color: $dark-gray-500;
+}
+
+.components-custom-select__label {
+	display: block;
+	margin-bottom: 5px;
+}
+
+.components-custom-select__button {
+	border: 1px solid $dark-gray-200;
+	border-radius: 4px;
+	color: $dark-gray-500;
+	display: initial;
```

I'm afraid IE doesn't support `initial` 🙀 
Can we replace this with `inline`, which is iirc the default value for `<button>`?

#### epiqueras on 2019-11-05T00:55:41Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r342341841
Path: packages/components/src/custom-select/style.scss

```diff
@@ -0,0 +1,50 @@
+.components-custom-select {
+	color: $dark-gray-500;
+}
+
+.components-custom-select__label {
+	display: block;
+	margin-bottom: 5px;
+}
+
+.components-custom-select__button {
+	border: 1px solid $dark-gray-200;
+	border-radius: 4px;
+	color: $dark-gray-500;
+	display: initial;
```

Done!

### Review thread 6 (resolved)

#### tellthemachines on 2019-11-06T04:23:48Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r342911766
Path: packages/components/src/custom-select/style.scss

```diff
@@ -0,0 +1,55 @@
+.components-custom-select {
+	color: $dark-gray-500;
+	position: relative;
+}
+
+.components-custom-select__label {
+	display: block;
+	margin-bottom: 5px;
+}
+
+.components-custom-select__button {
+	border: 1px solid $dark-gray-200;
+	border-radius: 4px;
+	color: $dark-gray-500;
+	display: inline;
+	min-height: 28px;
+	min-width: 130px;
+	position: relative;
+	text-align: left;
+
+	&:focus {
+		border-color: $blue-medium-500;
+	}
+
+	&-icon {
+		height: 100%;
+		padding: 0 8px;
```

Should probably be `0 2px` because as it is the icon is being pushed in by the padding and is appearing tiny.

### Review thread 7 (resolved)

#### tellthemachines on 2019-11-06T05:33:31Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r342923097
Path: packages/components/src/custom-select/index.js

```diff
@@ -0,0 +1,148 @@
+/**
+ * External dependencies
+ */
+import { useSelect } from 'downshift';
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { Button, Dashicon } from '../';
+
+const itemToString = ( item ) => item.name;
+// This is needed so that in Windows, where
+// the menu does not necessarily open on
+// key up/down, you can still switch between
+// options with the menu closed.
+const stateReducer = (
+	{ selectedItem },
+	{ type, changes, props: { items } }
+) => {
+	// TODO: Remove this.
+	// eslint-disable-next-line no-console
+	console.debug(
+		'Selected Item: ',
+		selectedItem,
+		'Type: ',
+		type,
+		'Changes: ',
+		changes,
+		'Items: ',
+		items
+	);
+	switch ( type ) {
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowDown:
+			// If we already have a selected item, try to select the next one,
+			// without circular navigation. Otherwise, select the first item.
+			return {
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.min( items.indexOf( selectedItem ) + 1, items.length - 1 ) :
+							0
+					],
+			};
+		case useSelect.stateChangeTypes.ToggleButtonKeyDownArrowUp:
+			// If we already have a selected item, try to select the previous one,
+			// without circular navigation. Otherwise, select the last item.
+			return {
+				selectedItem:
+					items[
+						selectedItem ?
+							Math.max( items.indexOf( selectedItem ) - 1, 0 ) :
+							items.length - 1
+					],
+			};
+		default:
+			return changes;
+	}
+};
+export default function CustomSelect( {
+	className,
+	hideLabelFromVision,
+	label,
+	items,
+	onSelectedItemChange,
+	selectedItem: _selectedItem,
+} ) {
+	const {
+		getLabelProps,
+		getToggleButtonProps,
+		getMenuProps,
+		getItemProps,
+		isOpen,
+		highlightedIndex,
+		selectedItem,
+	} = useSelect( {
+		initialSelectedItem: items[ 0 ],
+		items,
+		itemToString,
+		onSelectedItemChange,
+		selectedItem: _selectedItem,
+		stateReducer,
+	} );
+	const menuProps = getMenuProps( {
+		className: 'components-custom-select__menu',
+	} );
+	// We need this here, because the null active descendant is not
+	// fully ARIA compliant.
+	if (
+		menuProps[ 'aria-activedescendant' ] &&
+		menuProps[ 'aria-activedescendant' ].slice( 0, 'downshift-null'.length ) ===
+			'downshift-null'
+	) {
+		delete menuProps[ 'aria-activedescendant' ];
+	}
+	return (
+		<div className={ classnames( 'components-custom-select', className ) }>
+			{ /* eslint-disable-next-line jsx-a11y/label-has-associated-control, jsx-a11y/label-has-for */ }
+			<label
+				{ ...getLabelProps( {
+					className: classnames( 'components-custom-select__label', {
+						'screen-reader-text': hideLabelFromVision,
+					} ),
+				} ) }
+			>
+				{ label }
+			</label>
+			<Button
+				{ ...getToggleButtonProps( {
+					// This is needed because some speech recognition software don't support `aria-labelledby`.
+					'aria-label': label,
```

The aria-label on the button will need to be the same as the visible label, because that's what speech to text users will try to activate the button with. In the previous iteration @enriquesanchez and I settled on making the labels for the font-size controls visible and instead hiding the "font size" fieldset legend:

<img width="561" alt="Screen Shot 2019-09-25 at 6 33 31 PM" src="https://user-images.githubusercontent.com/8096000/68270912-17671900-00b3-11ea-8c66-70acf6699b60.png">

### Review thread 8

#### youknowriad on 2019-11-28T17:46:41Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351892894
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

Not sure I understand why the changes in this file are needed for this particular PR?

#### epiqueras on 2019-11-28T17:52:07Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351894124
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

https://github.com/WordPress/gutenberg/pull/17926#pullrequestreview-312188131

#### youknowriad on 2019-11-28T17:53:25Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351894379
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

Why it doesn't just use Popover, this issue is already solved there, and I think the changes here might cause more unexpected bugs (like the scrolling issue we noticed)

#### epiqueras on 2019-11-28T17:56:30Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351895080
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

Is the `Popover` fully accessible?

cc @tellthemachines @enriquesanchez

#### youknowriad on 2019-11-28T18:00:03Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351895809
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

If it's not it should be :) but we did a lot of a11y work on Popover back then.

#### enriquesanchez on 2019-11-28T19:03:57Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351907425
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

I'm not aware of any accessibility issues with `Popover`.

#### tellthemachines on 2019-11-28T22:24:01Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351936442
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

There are no issues that I know of with `Popover` itself, but we had some trouble with focus management when using it in our previous attempt to implement this component: https://github.com/WordPress/gutenberg/pull/17418#pullrequestreview-294158647

#### epiqueras on 2019-11-28T22:35:05Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351937699
Path: packages/edit-post/src/components/sidebar/style.scss

```diff
@@ -25,7 +26,7 @@
 		position: relative;
 
 		@include break-small() {
-			overflow: hidden;
+			overflow: visible;
 			height: auto;
 			max-height: none;
 		}
```

Yes, that's what I was thinking of.

Part of the reason I didn't use `Popover` here from the start is that previous attempts had failed with it.

### Review thread 9

#### youknowriad on 2019-11-28T17:47:11Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351893008
Path: packages/components/src/index.js

```diff
@@ -16,6 +16,7 @@ export { default as ClipboardButton } from './clipboard-button';
 export { default as ColorIndicator } from './color-indicator';
 export { default as ColorPalette } from './color-palette';
 export { default as ColorPicker } from './color-picker';
+export { default as CustomSelect } from './custom-select';
```

Why it's named CustomSelect and not just Select

#### epiqueras on 2019-11-28T17:54:06Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351894510
Path: packages/components/src/index.js

```diff
@@ -16,6 +16,7 @@ export { default as ClipboardButton } from './clipboard-button';
 export { default as ColorIndicator } from './color-indicator';
 export { default as ColorPalette } from './color-palette';
 export { default as ColorPicker } from './color-picker';
+export { default as CustomSelect } from './custom-select';
```

We already have `SelectControl` so that felt confusing, but I don't have a strong opinion on it.

#### youknowriad on 2019-11-28T17:59:13Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351895665
Path: packages/components/src/index.js

```diff
@@ -16,6 +16,7 @@ export { default as ClipboardButton } from './clipboard-button';
 export { default as ColorIndicator } from './color-indicator';
 export { default as ColorPalette } from './color-palette';
 export { default as ColorPicker } from './color-picker';
+export { default as CustomSelect } from './custom-select';
```

Good remark, I do think in that case that it should be SomethingControl. `CustomSelectControl` maybe. I don't like Custom that much though but I also don't have a lot of ideas.

#### epiqueras on 2019-11-28T18:34:49Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351902428
Path: packages/components/src/index.js

```diff
@@ -16,6 +16,7 @@ export { default as ClipboardButton } from './clipboard-button';
 export { default as ColorIndicator } from './color-indicator';
 export { default as ColorPalette } from './color-palette';
 export { default as ColorPicker } from './color-picker';
+export { default as CustomSelect } from './custom-select';
```

I prefer `Select` to be honest. The goal is for it to be THE React version of `select` so that makes sense.

`Control` makes it seem like it uses `BaseControl` which it doesn't.

#### tellthemachines on 2019-11-28T22:18:01Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351935816
Path: packages/components/src/index.js

```diff
@@ -16,6 +16,7 @@ export { default as ClipboardButton } from './clipboard-button';
 export { default as ColorIndicator } from './color-indicator';
 export { default as ColorPalette } from './color-palette';
 export { default as ColorPicker } from './color-picker';
+export { default as CustomSelect } from './custom-select';
```

I think it would be good to keep the "Custom", or some similar indication that this is not a standard HTML select (which we should also continue to provide, for cases where the options don't need to be visually distinguishable from one another)

### Review thread 10

#### youknowriad on 2019-11-28T17:54:49Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351894667
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

useless snapshots that we change as the implementation is updated?

#### epiqueras on 2019-11-28T17:58:17Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351895448
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

Yes, from the "storyshots".

#### tellthemachines on 2019-11-28T22:13:21Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351935294
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

Why useless? I'd expect snapshots to change as the markup changes. I'd also expect to review those changes and make sure they are consistent with what we were trying to do, and nothing changed that shouldn't. That's the whole purpose of snapshots 🙂

#### youknowriad on 2019-11-29T07:04:36Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352001615
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

In my experience, each time you change the implementation, you have to change the snapshot and oftentimes you don't really check the details as it's a big blob of content.

Also, with snapshots it's not clear what are we testing exactly, we're not testing a specific behavior but just random content, it makes it hard to know what's a regression and what's not.

Another question: did you ever find a regression with these snapshots?

#### epiqueras on 2019-11-29T15:38:35Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352181395
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

I think we use them for things like highlighting missing/removed class names that plugins rely on.

#### tellthemachines on 2019-12-02T00:04:29Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352384692
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

My take on it is snapshots are purely for testing markup: making sure no HTML has been accidentally stripped out or made unsemantic, classname or aria attribute changes, etc. It'll only be a big blob if we make a lot of changes! In this instance it's quite big because we changed the markup for the whole component. Still, I find markup changes much more readable in this format than in JSX.

I have found regressions before with snapshots; mostly of the kind mentioned above, but sometimes I've spotted issues with semantics on components being built from scratch too, because the snapshot view is pretty close to the output HTML.

Of course snapshots are only useful when we read them, but that is just a cultural issue that we can work on improving through educating on good testing and code review practice 😀

#### epiqueras on 2019-12-02T00:13:26Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352385304
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

Yeah, this is my take on them too.

#### youknowriad on 2019-12-02T08:23:02Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352464453
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

It's very hard to argue against tests :) and I had this exact same conversation over and over again with different people and everytime we land the snapshots and decide to rollback later :). Nothing personal here but I think this is just one of these occasions.

#### tellthemachines on 2019-12-02T23:50:05Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352920771
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

I'd love to know why you decided to roll back! In my previous project snapshots were pretty much universally agreed on. This PR might not be the best place for discussion 😄, but maybe we could bring it up at a Slack meeting at some point?

#### youknowriad on 2019-12-03T09:04:15Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r353053556
Path: storybook/test/__snapshots__/index.js.snap

```diff
@@ -1539,61 +1539,78 @@ exports[`Storyshots Components|FontSizePicker Default 1`] = `
 <fieldset
   className="components-font-size-picker"
 >
-  <legend>
+  <legend
```

What I said above. basically after some time using them, we noticed that they are not that useful.

### Review thread 11

#### youknowriad on 2019-11-28T17:57:53Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351895373
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

Why not just onChange (I'm trying to see if we can mirror the SelectControl API props, so it's consistent and switching between the two becomes easy. Same  for "value" and "options"

#### epiqueras on 2019-11-28T18:40:57Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351903525
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

I tried to mirror the `Downshift` props.

Their reasoning is probably that you also have `highlightedItem`, `onHighlightedItemChange`, etc. They prefer to be explicit about the two instead of having `selectedItem == value`.

#### youknowriad on 2019-11-28T18:42:45Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351903863
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

I have a preference for staying consistent across our components, Downshift is just an implementation detail that can be changed at any time.

#### epiqueras on 2019-11-28T18:59:57Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351906749
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

I think that having `onChange` and `onHighlightedValueChange` is a bit confusing. It's not totally clear that `onChange` does not include highlighted value changes.

Also, `selectedItem` takes the whole object, not just the key.

Similarly, `onSelectedItemChange` gives you the whole object, not just the key.

This already makes them fundamentally different to `SelectControl`'s `value` and `onChange`, but they are a much simpler to use API as you don't have to handle all of the key-to-object normalization/searching on the consumer side.

#### youknowriad on 2019-11-28T19:04:50Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351907572
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

I feel like highlighting is not something you'd need to care about often as a consumer, I'm not sure we even need it as an API in our component to be honest.

Also, I just noticed that the component is lacking a README.

#### epiqueras on 2019-11-28T22:49:17Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r351939348
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

We already have the functionality for free and this is a reusable component meant to be used outside of Gutenberg as well so I don't see how we gain from not exposing it.

Also, `selectedItem` and `items` very clearly convey the contract that the `selectedItem` should be an object from the `items` list.

>Also, I just noticed that the component is lacking a README.

I will work on one, but are you still unhappy with the prop names? What props would you expose?

#### youknowriad on 2019-11-29T07:08:06Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352002476
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

> Also, selectedItem and items very clearly convey the contract that the selectedItem should be an object from the items list.

I get that but the boat has sailed for a lot of components now, and I'd rather have users learn one way to use all of our controls rather than having each component with its own specifics.

> I will work on one, but are you still unhappy with the prop names? What props would you expose?

value, onChange and options being the important ones. 

>  I don't see how we gain from not exposing it.

we gain that if we ever change the implementation or anything, we're less likely to break backward compatibility. Adding new APIs anywhere in our codebase shouldn't be considered a "good thing", it's something to do with a lot of caution.

#### epiqueras on 2019-11-29T16:01:05Z

URL: https://github.com/WordPress/gutenberg/pull/17926#discussion_r352189311
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -51,38 +57,40 @@ function FontSizePicker( {
 		onChange( Number( newValue ) );
 	};
 
-	const onSelectChangeValue = ( eventValue ) => {
-		setCurrentSelectValue( eventValue );
-		const selectedFont = fontSizes.find( ( font ) => font.slug === eventValue );
-		if ( selectedFont ) {
-			onChange( selectedFont.size );
-		}
+	const onSelectChangeValue = ( { selectedItem } ) => {
+		setCurrentSelectValue( selectedItem.key );
+		onChange( selectedItem.style && selectedItem.style.fontSize );
 	};
 
+	const items = getSelectOptions( fontSizes );
+	const rangeControlNumberId = `components-range-control__number#${ instanceId }`;
 	return (
 		<fieldset className="components-font-size-picker">
-			<legend>
+			<legend className="screen-reader-text">
 				{ __( 'Font Size' ) }
 			</legend>
 			<div className="components-font-size-picker__controls">
 				{ ( fontSizes.length > 0 ) &&
-					<SelectControl
+					<CustomSelect
 						className={ 'components-font-size-picker__select' }
-						label={ 'Choose preset' }
-						hideLabelFromVision={ true }
-						value={ currentSelectValue }
-						onChange={ onSelectChangeValue }
-						options={ getSelectOptions( fontSizes ) }
+						label={ __( 'Preset Size' ) }
+						items={ items }
+						selectedItem={ items.find( ( item ) => item.key === currentSelectValue ) || items[ 0 ] }
+						onSelectedItemChange={ onSelectChangeValue }
```

>I get that but the boat has sailed for a lot of components now, and I'd rather have users learn one way to use all of our controls rather than having each component with its own specifics.

That's fair and I can definitely see value in that if we stick with it across all inputs. I updated the prop names in this PR: #18827.

>we gain that if we ever change the implementation or anything, we're less likely to break backward compatibility. Adding new APIs anywhere in our codebase shouldn't be considered a "good thing", it's something to do with a lot of caution.

Agreed, but in this case, it's something all select implementations could easily expose, so I wouldn't worry about backwards compatibility with it. I still haven't exposed it for now to be safe, we can do it when the need for it arises.

## Changed files

- package-lock.json (MODIFIED, +26/-2)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/components/src/custom-select/index.js (ADDED, +149/-0)
- packages/components/src/custom-select/stories/index.js (ADDED, +30/-0)
- packages/components/src/custom-select/style.scss (ADDED, +56/-0)
- packages/components/src/font-size-picker/index.js (MODIFIED, +32/-24)
- packages/components/src/font-size-picker/style.scss (MODIFIED, +11/-5)
- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/style.scss (MODIFIED, +1/-0)
- packages/e2e-tests/specs/editor/various/change-detection.test.js (MODIFIED, +4/-2)
- packages/e2e-tests/specs/editor/various/editor-modes.test.js (MODIFIED, +2/-1)
- packages/e2e-tests/specs/editor/various/font-size-picker.test.js (MODIFIED, +6/-3)
- packages/edit-post/src/components/sidebar/style.scss (MODIFIED, +4/-3)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +201/-121)
