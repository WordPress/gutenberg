---
number: 79254
type: pull_request
state: MERGED
merged_at: 2026-06-23T11:54:59Z
url: https://github.com/WordPress/gutenberg/pull/79254
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Code Quality","[Package] DataViews","[Package] Theme"]
---

# PR #79254: Theme: Drop `--wpds-dimension-base` from the public token surface

- URL: https://github.com/WordPress/gutenberg/pull/79254
- Author: ciampo
- Created: 2026-06-16T22:21:07Z
- Updated: 2026-06-23T11:57:36Z
- Comments: 3 of 3
- Review threads: 6 of 6
- Changed files: 22 of 22
- Diff: ../pr-diffs/79254.diff

## Body

## What?
See https://github.com/WordPress/gutenberg/issues/77462 (point T-N1). Follow-up to #79093.

Removes the `--wpds-dimension-base` design token from the public surface of `@wordpress/theme`.

## Why?
`--wpds-dimension-base` is a primitive (the `4px` base unit) and shouldn't be exposed publicly, in the same way primitive color tokens are excluded.

## How?
- Migrate the remaining `var(--wpds-dimension-base)` consumers off the token: semantic `--wpds-dimension-size-*` tokens where the value maps exactly, literal `px` for arbitrary layout values.
- Drop the `base` token from `tokens/dimension.json` and regenerate the prebuilt CSS/JS/docs.

No visual changes are intended.

## Testing Instructions
- `npm run build:tokens` in `packages/theme` produces no diff.
- Theme package unit tests and stylelint pass.
- DataViews, admin UI, and dashboard widgets render unchanged.

## Screenshots or screencast

## Use of AI Tools
AI assistance: Yes
Tool(s): Cursor
Model(s): Claude Opus 4.8
Used for: Migrating consumers and regenerating tokens; all changes reviewed manually.

## Issue comments

### github-actions on 2026-06-16T22:30:16Z

URL: https://github.com/WordPress/gutenberg/pull/79254#issuecomment-4724167455

**Size Change:** -104 B (0%) 

**Total Size:** 7.51 MB

<details open><summary>📦 <strong>View Changed</strong></summary>

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/modules/boot/index.min.js` | 52.9 kB | -7 B (-0.01%) |
| `build/modules/content-types/index.min.js` | 159 kB | -12 B (-0.01%) |
| `build/scripts/edit-site/index.min.js` | 299 kB | -2 B (0%) |
| `build/styles/edit-site/style-rtl.css` | 22.1 kB | -12 B (-0.05%) |
| `build/styles/edit-site/style-rtl.min.css` | 18.2 kB | +6 B (+0.03%) |
| `build/styles/edit-site/style.css` | 22.1 kB | -12 B (-0.05%) |
| `build/styles/edit-site/style.min.css` | 18.2 kB | +5 B (+0.03%) |
| `build/styles/editor/style-rtl.css` | 30.7 kB | -12 B (-0.04%) |
| `build/styles/editor/style-rtl.min.css` | 26.2 kB | -1 B (0%) |
| `build/styles/editor/style.css` | 30.8 kB | -13 B (-0.04%) |
| `build/styles/theme/design-tokens-rtl.css` | 3.38 kB | -18 B (-0.53%) |
| `build/styles/theme/design-tokens-rtl.min.css` | 1.62 kB | -4 B (-0.25%) |
| `build/styles/theme/design-tokens.css` | 3.38 kB | -18 B (-0.53%) |
| `build/styles/theme/design-tokens.min.css` | 1.62 kB | -4 B (-0.25%) |

</details>

<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-16T22:54:44Z

URL: https://github.com/WordPress/gutenberg/pull/79254#issuecomment-4724296558

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 2ea45cceb3271c6bd2e3ffbdc2755752ddadd9bd.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/28022578859
📝  Reported issues:
- #78755 in `/test/e2e/specs/editor/blocks/navigation-colors.spec.js`
- #78753 in `/test/e2e/specs/editor/collaboration/http-only/collaboration-document-size-lock.spec.ts`

### github-actions on 2026-06-17T08:13:28Z

URL: https://github.com/WordPress/gutenberg/pull/79254#issuecomment-4727575717

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: fcoveram <fcoveram@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### ciampo on 2026-06-17T08:08:13Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3426565887
Path: packages/dataviews/src/components/dataviews-layouts/list/style.scss

```diff
@@ -1,5 +1,12 @@
 @use "../../../dataviews/style" as *;
 
+// Square media (thumbnail) size per density. The compact density uses the
+// `--wpds-dimension-size-md` token; these larger densities have no matching
+// token, so they're kept as local constants. The field wrapper reuses the
+// same value as its `min-height` to keep the title centered against the media.
+$media-size: 52px;
+$media-size-comfortable: 64px;
```

@WordPress/gutenberg-design 

When introducing WPDS element size tokens, we discussed adding `48px` and potentially `64px` to the scale, but decided to exclude them, at least initially ([comment](https://github.com/WordPress/gutenberg/pull/76545#issuecomment-4089376548), [comment](https://github.com/WordPress/gutenberg/pull/76545#issuecomment-4092207253)).

As we start to refactor code around the codebase to the WPDS tokens, should we consider adding these values, or do we still think they don't belong to the DS scale? (they already appear a few times throughout this PR)

Also, the `52px` here feels out of place — I'd expect it to be `48px`, although I didn't work on dataview density and overall design

#### fcoveram on 2026-06-18T14:29:19Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3436580556
Path: packages/dataviews/src/components/dataviews-layouts/list/style.scss

```diff
@@ -1,5 +1,12 @@
 @use "../../../dataviews/style" as *;
 
+// Square media (thumbnail) size per density. The compact density uses the
+// `--wpds-dimension-size-md` token; these larger densities have no matching
+// token, so they're kept as local constants. The field wrapper reuses the
+// same value as its `min-height` to keep the title centered against the media.
+$media-size: 52px;
+$media-size-comfortable: 64px;
```

I asked AI to look at where the 52px comes from to understand the rationale, but it seems that it was a 4x-based value to fix a visual unbalanced. With that in mind, I'm drawn to apply the `48px` token.

The density preference is extendable to other UI areas. I now wonder about the relationship between this component setting and a future system setting that modifies the general spacing approach.

#### ciampo on 2026-06-22T17:05:19Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3454073887
Path: packages/dataviews/src/components/dataviews-layouts/list/style.scss

```diff
@@ -1,5 +1,12 @@
 @use "../../../dataviews/style" as *;
 
+// Square media (thumbnail) size per density. The compact density uses the
+// `--wpds-dimension-size-md` token; these larger densities have no matching
+// token, so they're kept as local constants. The field wrapper reuses the
+// same value as its `min-height` to keep the title centered against the media.
+$media-size: 52px;
+$media-size-comfortable: 64px;
```

> With that in mind, I'm drawn to apply the 48px token.

Sounds good, I'll update to 48

> I now wonder about the relationship between this component setting and a future system setting that modifies the general spacing approach.

When discussing approaches to supporting density preferences in `@wordpress/theme`, we've always mentioned dataviews as the prime example of where that could be useful. But the real struggle is to define a clear set of rules that can be applied to DS tokens and UIs in general, ie. not just to the specific dataviews case.

My suggestion is to work backwards: ie. start with designing a few UIs at different densities, and then try to extract rules from those designs.

### Review thread 2 (resolved)

#### ciampo on 2026-06-17T08:12:01Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3426587795
Path: packages/widget-dashboard/src/components/widgets/widget-resize-handle.module.css

```diff
@@ -13,8 +13,8 @@
 
 .handle:focus-visible {
 	outline: var(--wpds-border-width-focus) solid var(--wpds-color-stroke-focus);
-	/* @TODO: Replace with a `size` token once that category exists in the theme package. */
-	outline-offset: calc(var(--wpds-dimension-base) * 0.5);
+	/* Offset the ring by an amount equal to its own width. */
+	outline-offset: var(--wpds-border-width-focus);
```

@WordPress/gutenberg-design  Switching from `2px` to `1.5px`, am I assuming the intent of this outline offset correctly?

#### fcoveram on 2026-06-18T14:32:00Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3436599138
Path: packages/widget-dashboard/src/components/widgets/widget-resize-handle.module.css

```diff
@@ -13,8 +13,8 @@
 
 .handle:focus-visible {
 	outline: var(--wpds-border-width-focus) solid var(--wpds-color-stroke-focus);
-	/* @TODO: Replace with a `size` token once that category exists in the theme package. */
-	outline-offset: calc(var(--wpds-dimension-base) * 0.5);
+	/* Offset the ring by an amount equal to its own width. */
+	outline-offset: var(--wpds-border-width-focus);
```

I shared in a design review ticket for the dashboard (#79182) the idea of having this action as the new `button`. It doesn't answer your question but linking it in case it makes sense

#### ciampo on 2026-06-23T11:17:08Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3459268383
Path: packages/widget-dashboard/src/components/widgets/widget-resize-handle.module.css

```diff
@@ -13,8 +13,8 @@
 
 .handle:focus-visible {
 	outline: var(--wpds-border-width-focus) solid var(--wpds-color-stroke-focus);
-	/* @TODO: Replace with a `size` token once that category exists in the theme package. */
-	outline-offset: calc(var(--wpds-dimension-base) * 0.5);
+	/* Offset the ring by an amount equal to its own width. */
+	outline-offset: var(--wpds-border-width-focus);
```

> am I assuming the intent of this outline offset correctly?

Confirm the assumption, as per this comment on a recent PR: https://github.com/WordPress/gutenberg/pull/78823#discussion_r3334499904

### Review thread 3 (resolved)

#### ciampo on 2026-06-17T08:12:43Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3426591724
Path: widgets/site-health/components/circle-progress/circle-progress.module.css

```diff
@@ -1,7 +1,7 @@
 .root {
 	flex-shrink: 0;
-	width: calc(var(--wpds-dimension-base) * 17);
-	height: calc(var(--wpds-dimension-base) * 17);
+	width: 68px;
+	height: 68px;
```

@WordPress/gutenberg-design This `68px` here also feels out of place, should it have been `64px`?

#### fcoveram on 2026-06-18T14:32:16Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3436600954
Path: widgets/site-health/components/circle-progress/circle-progress.module.css

```diff
@@ -1,7 +1,7 @@
 .root {
 	flex-shrink: 0;
-	width: calc(var(--wpds-dimension-base) * 17);
-	height: calc(var(--wpds-dimension-base) * 17);
+	width: 68px;
+	height: 68px;
```

Yes

### Review thread 4 (resolved)

#### mirka on 2026-06-17T18:42:11Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3430646404
Path: packages/theme/CHANGELOG.md

I believe the Breaking Changes section should be at the very top of each release.

### Review thread 5

#### mirka on 2026-06-17T18:45:36Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3430665055
Path: widgets/site-health/style.module.css

```diff
@@ -1,11 +1,11 @@
 .root {
 	margin: 0 auto;
-	max-width: calc(var(--wpds-dimension-base) * 80);
+	max-width: 320px;
 	text-align: center;
 	height: 100%;
 }
 
 .loading {
-	min-height: calc(var(--wpds-dimension-base) * 55);
+	min-height: 220px;
```

Possibly better as `surface-width` tokens?

#### ciampo on 2026-06-23T11:24:18Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3459309471
Path: widgets/site-health/style.module.css

```diff
@@ -1,11 +1,11 @@
 .root {
 	margin: 0 auto;
-	max-width: calc(var(--wpds-dimension-base) * 80);
+	max-width: 320px;
 	text-align: center;
 	height: 100%;
 }
 
 .loading {
-	min-height: calc(var(--wpds-dimension-base) * 55);
+	min-height: 220px;
```

- Refactored `320px` -> `--wpds-dimension-surface-width-sm` (exact match)
- Kept `220px` hardcoded, since there isn't an exact match in the surface tokens, and since the value is used for the `height`, while the tokens are explicitly for the `width`

### Review thread 6 (resolved)

#### mirka on 2026-06-17T18:46:17Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3430668971
Path: widgets/quick-draft/style.module.css

```diff
@@ -30,7 +30,7 @@
 /* Wide tile: drafts list sits beside the form, split by a vertical rule. */
 .row .primaryPane {
 	border-inline-end: var(--wpds-border-width-xs) solid var(--wpds-color-stroke-surface-neutral-weak);
-	max-width: calc(var(--wpds-dimension-base) * 100);
+	max-width: 400px;
```

Possibly better as a `surface-width` token?

#### ciampo on 2026-06-23T11:25:02Z

URL: https://github.com/WordPress/gutenberg/pull/79254#discussion_r3459314019
Path: widgets/quick-draft/style.module.css

```diff
@@ -30,7 +30,7 @@
 /* Wide tile: drafts list sits beside the form, split by a vertical rule. */
 .row .primaryPane {
 	border-inline-end: var(--wpds-border-width-xs) solid var(--wpds-color-stroke-surface-neutral-weak);
-	max-width: calc(var(--wpds-dimension-base) * 100);
+	max-width: 400px;
```

Refactored from `400px` to `var(--wpds-dimension-surface-width-md)` (exact match)

## Changed files

- packages/admin-ui/CHANGELOG.md (MODIFIED, +4/-0)
- packages/admin-ui/src/page/style.module.css (MODIFIED, +3/-5)
- packages/dataviews/CHANGELOG.md (MODIFIED, +1/-0)
- packages/dataviews/src/components/dataviews-filters/style.scss (MODIFIED, +1/-1)
- packages/dataviews/src/components/dataviews-layouts/activity/style.scss (MODIFIED, +8/-8)
- packages/dataviews/src/components/dataviews-layouts/grid/style.scss (MODIFIED, +7/-1)
- packages/dataviews/src/components/dataviews-layouts/list/style.scss (MODIFIED, +15/-6)
- packages/dataviews/src/components/dataviews-layouts/picker-activity/style.scss (MODIFIED, +8/-8)
- packages/dataviews/src/components/dataviews-layouts/picker-grid/style.scss (MODIFIED, +8/-2)
- packages/dataviews/src/components/dataviews-layouts/picker-table/style.scss (MODIFIED, +3/-1)
- packages/dataviews/src/dataviews/style.scss (MODIFIED, +1/-1)
- packages/theme/CHANGELOG.md (MODIFIED, +1/-0)
- packages/theme/docs/tokens.md (MODIFIED, +0/-1)
- packages/theme/src/prebuilt/css/design-tokens.css (MODIFIED, +0/-2)
- packages/theme/src/prebuilt/js/design-token-fallbacks.mjs (MODIFIED, +0/-1)
- packages/theme/src/prebuilt/js/design-tokens.mjs (MODIFIED, +0/-1)
- packages/theme/tokens/dimension.json (MODIFIED, +0/-4)
- packages/widget-dashboard/src/components/widgets/widget-resize-handle.module.css (MODIFIED, +2/-2)
- widgets/events/components/location-picker/location-picker.module.css (MODIFIED, +1/-1)
- widgets/quick-draft/style.module.css (MODIFIED, +1/-1)
- widgets/site-health/components/circle-progress/circle-progress.module.css (MODIFIED, +3/-2)
- widgets/site-health/style.module.css (MODIFIED, +5/-2)
