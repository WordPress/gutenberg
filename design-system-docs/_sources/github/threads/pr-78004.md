---
number: 78004
type: pull_request
state: MERGED
merged_at: 2026-05-11T14:39:05Z
url: https://github.com/WordPress/gutenberg/pull/78004
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #78004: ColorPalette: Fix duplicate-key warnings and incorrect selection with identical color values

- URL: https://github.com/WordPress/gutenberg/pull/78004
- Author: USERSATOSHI
- Created: 2026-05-06T10:35:00Z
- Updated: 2026-05-11T14:40:02Z
- Comments: 4 of 4
- Review threads: 10 of 10
- Changed files: 7 of 7
- Diff: ../pr-diffs/78004.diff

## Body

## What?
Closes #9357

Fix React duplicate-key warning and incorrect swatch selection when two palette entries share the same color value.

## Why?
more information in the issue but in nutshell:

Themes can legitimately have two palette slots with the same color (e.g. "Header Background" and "Body Background" both white, so users can later change them independently). Gutenberg used the CSS color string as both the React key and the selection identifier, causing:

- React warning: `Encountered two children with the same key, rgb(29, 16, 22)`
- Both swatches highlighted when either is selected

## How?
- `ColorPalette` now uses `slug` as the React key when available, and accepts a `selectedSlug` prop for accurate swatch highlighting
- `onChange` passes `(color, index, slug)` so callers can round-trip the slug
- Block editor color panel threads the slug from the stored `var:preset|color|{slug}` value down to `ColorPalette`, and passes it back through `encodeColorValue` on change, bypassing the first-match color search

## Testing Instructions
1. Register two palette entries with the same color value (different slugs)
2. Open a block's color panel in the sidebar
3. Confirm no console key warning
4. Confirm clicking each swatch ticks only that swatch
5. Confirm the correct slug is saved in the block markup after saving

## Use of AI Tools
Developed with GitHub Copilot (Claude Sonnet 4.6). All code reviewed and tested manually.

## Issue comments

### github-actions on 2026-05-06T12:38:08Z

URL: https://github.com/WordPress/gutenberg/pull/78004#issuecomment-4388018623

<!-- Enforce labels on Pull Request/type-related-labels/__mheap_github-action-required-labels -->
**Warning: Type of PR label mismatch**

 To merge this PR, it requires exactly 1 label indicating the type of PR. Other labels are optional and not being checked here. 
- **Required label**: Any label starting with `[Type]`.
- **Labels found**: [Package] Components, [Package] Block editor.

Read more about [Type labels in Gutenberg](https://github.com/WordPress/gutenberg/labels?q=type). Don't worry if you don't have the required permissions to add labels; the PR reviewer should be able to help with the task.

### github-actions on 2026-05-06T12:38:19Z

URL: https://github.com/WordPress/gutenberg/pull/78004#issuecomment-4388021034

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @DesignerThan95, @metasaman, @timelsass.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: DesignerThan95, metasaman, timelsass.

Co-authored-by: USERSATOSHI <tusharbharti@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: designsimply <designsimply@git.wordpress.org>
Co-authored-by: JiveDig <jivedig@git.wordpress.org>
Co-authored-by: timnicholson <timnicholson@git.wordpress.org>
Co-authored-by: nathanrodrigues2111 <nathanrbsf@git.wordpress.org>
Co-authored-by: acketon <acketon@git.wordpress.org>
Co-authored-by: SirLouen <sirlouen@git.wordpress.org>
Co-authored-by: ritoban23 <devrito@git.wordpress.org>
Co-authored-by: catthetech <catthetech@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### USERSATOSHI on 2026-05-06T14:37:47Z

URL: https://github.com/WordPress/gutenberg/pull/78004#issuecomment-4389159783

> Thank you for looking into this!
> 
> My preference would be to keep this PR focused on the `@wordpress/components` code changes, and then follow-up with changes to the `@wordpress/block-editor` package in a separate PR

I see, Then I will split this PR into 2, this one will be related to components and I will follow up with block editor changes. 👍🏻

### USERSATOSHI on 2026-05-11T08:04:22Z

URL: https://github.com/WordPress/gutenberg/pull/78004#issuecomment-4418667552

<img width="2135" height="1398" alt="image" src="https://github.com/user-attachments/assets/ba3946ce-1f55-4de6-8dd2-204e889be938" />
<img width="1014" height="457" alt="image" src="https://github.com/user-attachments/assets/4c034afe-2059-419b-bddb-f735fe6044a7" />

I have updated the code for both storybook and the cases you mentioned

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2026-05-06T13:47:59Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3195914393
Path: packages/components/CHANGELOG.md

```diff
@@ -12,6 +12,7 @@
 -   `TabPanel`: Fix tab indicator animation while switching tabs ([#77812](https://github.com/WordPress/gutenberg/pull/77812)).
 -   `ColorPicker`: Fix issue where clearing the hex input entirely doesn't reset the selected color to black ([#77912](https://github.com/WordPress/gutenberg/pull/77912)).
 -   `ExternalLink`: Fix focus outline rendered in wp-admin ([#77935](https://github.com/WordPress/gutenberg/pull/77935)).
+-   `ColorPalette`: Fix duplicate-key warnings and incorrect selected swatch behavior when multiple palette entries share the same color value.
```

Missing PR number / link here (see other CHANGELOG entries)

### Review thread 2 (resolved)

#### ciampo on 2026-05-06T13:48:32Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3195918409
Path: packages/components/src/color-palette/index.native.js

Is this change necessary and related with the overall goal of this PR?

#### USERSATOSHI on 2026-05-06T14:39:29Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3196303227
Path: packages/components/src/color-palette/index.native.js

I was not sure if we want changes in the native file, so I just fixed the keys warning that comes on same key ( same color ). If this is not needed, I can remove it.

#### ciampo on 2026-05-07T10:20:20Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3200680917
Path: packages/components/src/color-palette/index.native.js

Yeah, I think my preference is to avoid touching the native files. Thank you!

### Review thread 3 (resolved)

#### copilot-pull-request-reviewer on 2026-05-06T13:53:24Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3195957294
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -50,16 +50,24 @@ function SinglePalette( {
 	colors,
 	onChange,
 	value,
+	selectedSlug,
 	...additionalProps
 }: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
-		return colors.map( ( { color, name }, index ) => {
+		return colors.map( ( { color, name, slug }, index ) => {
 			const colordColor = colord( color );
-			const isSelected = value === color;
+			// Prefer slug-based selection when both the palette entry and the
+			// caller provide a slug — this correctly distinguishes two entries
+			// that share the same color value. Fall back to comparing by color
+			// value when no slug is provided.
+			const isSelected =
+				selectedSlug !== undefined && slug !== undefined
```

When `selectedSlug` is provided, entries without a `slug` currently fall back to `value === color`, which can still mark duplicate-color swatches as selected (reintroducing the issue this PR is trying to solve) if the palette contains a mix of slugged and unslugged entries. Consider making slug selection exclusive when `selectedSlug` is defined (e.g., `selectedSlug !== undefined ? slug === selectedSlug : value === color`).

#### USERSATOSHI on 2026-05-07T06:25:53Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3199305522
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -50,16 +50,24 @@ function SinglePalette( {
 	colors,
 	onChange,
 	value,
+	selectedSlug,
 	...additionalProps
 }: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
-		return colors.map( ( { color, name }, index ) => {
+		return colors.map( ( { color, name, slug }, index ) => {
 			const colordColor = colord( color );
-			const isSelected = value === color;
+			// Prefer slug-based selection when both the palette entry and the
+			// caller provide a slug — this correctly distinguishes two entries
+			// that share the same color value. Fall back to comparing by color
+			// value when no slug is provided.
+			const isSelected =
+				selectedSlug !== undefined && slug !== undefined
```

@ciampo Regarding this, I think the previous code looked correct to me. When swatches without slugs are provided, my understanding was that all swatches should remain selected since there’s no unique slug to differentiate them.

I think, this can be left as is.

#### ciampo on 2026-05-07T14:51:16Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3202355707
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -50,16 +50,24 @@ function SinglePalette( {
 	colors,
 	onChange,
 	value,
+	selectedSlug,
 	...additionalProps
 }: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
-		return colors.map( ( { color, name }, index ) => {
+		return colors.map( ( { color, name, slug }, index ) => {
 			const colordColor = colord( color );
-			const isSelected = value === color;
+			// Prefer slug-based selection when both the palette entry and the
+			// caller provide a slug — this correctly distinguishes two entries
+			// that share the same color value. Fall back to comparing by color
+			// value when no slug is provided.
+			const isSelected =
+				selectedSlug !== undefined && slug !== undefined
```

@USERSATOSHI, I think that the case the comment it's flagging is the *mixed* one — some entries with a `slug`, some without, plus `selectedSlug` provided:

```js
const colors = [
    { name: 'Brand White', slug: 'brand-white', color: '#fff' },
    { name: 'Plain White', color: '#fff' },
];
```

With `value="#fff"` and `selectedSlug="brand-white"`, the first entry matches by slug, but the second falls back to `value === color` and is also selected — re-introducing the original bug.

We don't hit this inside Gutenberg core, but `ColorPalette` is public API, so I think it's best to handle it deterministically. The smallest fix:

```js
const isSelected =
    selectedSlug !== undefined
        ? slug === selectedSlug
        : value === color;
```

All existing and new tests in this PR should still pass under this form — only the mixed case changes.

I suggest:
1. Apply the change above in `SinglePalette`.
2. Add a test for the mixed case to lock in the contract.
3. Optional: a JSDoc note on `selectedSlug` clarifying that unslugged entries won't appear selected when `selectedSlug` is provided.

What do you think?

#### USERSATOSHI on 2026-05-08T06:17:45Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3206673002
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -50,16 +50,24 @@ function SinglePalette( {
 	colors,
 	onChange,
 	value,
+	selectedSlug,
 	...additionalProps
 }: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
-		return colors.map( ( { color, name }, index ) => {
+		return colors.map( ( { color, name, slug }, index ) => {
 			const colordColor = colord( color );
-			const isSelected = value === color;
+			// Prefer slug-based selection when both the palette entry and the
+			// caller provide a slug — this correctly distinguishes two entries
+			// that share the same color value. Fall back to comparing by color
+			// value when no slug is provided.
+			const isSelected =
+				selectedSlug !== undefined && slug !== undefined
```

I see, that is possible, I will update the code to that.

### Review thread 4 (resolved)

#### copilot-pull-request-reviewer on 2026-05-06T13:53:25Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3195957428
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -262,4 +266,108 @@ describe( 'ColorPalette', () => {
 			} )
 		).toBeInTheDocument();
 	} );
+
+	describe( 'duplicate colors in palette', () => {
+		const DUPLICATE_COLOR_PALETTE = [
+			{ name: 'Dark Background', slug: 'dark-background', color: '#000' },
+			{ name: 'Dark Text', slug: 'dark-text', color: '#000' },
+		];
+
+		it( 'should render all swatches even when two entries share the same color value', () => {
+			render(
+				<ColorPalette
+					colors={ DUPLICATE_COLOR_PALETTE }
+					value={ undefined }
+					onChange={ jest.fn() }
+				/>
+			);
+
+			expect( screen.getAllByRole( 'option' ) ).toHaveLength( 2 );
+		} );
+
+		it( 'should use slug as the React key so each option is individually identifiable', () => {
+			render(
+				<ColorPalette
+					colors={ DUPLICATE_COLOR_PALETTE }
+					value={ undefined }
+					onChange={ jest.fn() }
+				/>
+			);
+
+			// If duplicate keys were present React would silently drop one option;
+			// having two rendered options confirms each slug produced a unique key.
+			expect( screen.getAllByRole( 'option' ) ).toHaveLength( 2 );
+		} );
```

This test/comment doesn’t actually verify that `slug` is used as the React key: React does not reliably “silently drop” children with duplicate keys, so asserting `getAllByRole('option').toHaveLength(2)` is redundant with the previous test and can pass even if keys are still duplicated. Consider removing this test or asserting the absence of the duplicate-key warning by spying on `console.error` during render.

### Review thread 5 (resolved)

#### copilot-pull-request-reviewer on 2026-05-06T13:53:25Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3195957518
Path: packages/components/CHANGELOG.md

```diff
@@ -12,6 +12,7 @@
 -   `TabPanel`: Fix tab indicator animation while switching tabs ([#77812](https://github.com/WordPress/gutenberg/pull/77812)).
 -   `ColorPicker`: Fix issue where clearing the hex input entirely doesn't reset the selected color to black ([#77912](https://github.com/WordPress/gutenberg/pull/77912)).
 -   `ExternalLink`: Fix focus outline rendered in wp-admin ([#77935](https://github.com/WordPress/gutenberg/pull/77935)).
+-   `ColorPalette`: Fix duplicate-key warnings and incorrect selected swatch behavior when multiple palette entries share the same color value.
```

Changelog entries in this section consistently include the PR reference link (e.g. `([#NNNNN](...))`). Please add the PR number/link for this `ColorPalette` bug fix entry for consistency and traceability.

### Review thread 6 (resolved)

#### ciampo on 2026-05-08T13:07:20Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3208838979
Path: packages/components/src/color-palette/index.tsx

Looking at the `extractColorNameFromCurrentValue`, it stills seems that it includes the same "first-match-by-color bug" that we fixed in other places in the PR, which will cause visible and accessible labels to potentially show the wrong values.

We should pass `selectedSlug` to `extractColorNameFromCurrentValue` and  prefer slug matching when both sides provide one.

<details>

<summary>I also tested this bug with a unit test (that we could include in this PR, too)</summary>

```ts
		it( 'should display the slug-matched entry name in the custom color button label', () => {
			render(
				<ColorPalette
					colors={ DUPLICATE_COLOR_PALETTE }
					value="#000"
					selectedSlug="dark-text"
					onChange={ jest.fn() }
				/>
			);

			expect(
				screen.getByRole( 'button', {
					name: 'Custom color picker. The currently selected color is called "Dark Text" and has a value of "#000".',
				} )
			).toBeInTheDocument();
		} );
```

</details>

### Review thread 7 (resolved)

#### ciampo on 2026-05-08T13:14:39Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3208881013
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -50,16 +50,24 @@ function SinglePalette( {
 	colors,
 	onChange,
 	value,
+	selectedSlug,
 	...additionalProps
 }: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
-		return colors.map( ( { color, name }, index ) => {
+		return colors.map( ( { color, name, slug }, index ) => {
 			const colordColor = colord( color );
-			const isSelected = value === color;
+			// When selectedSlug is provided, only match entries with that slug.
+			// This correctly handles mixed palettes where some entries have slugs
+			// and others don't. Fall back to color value matching when no slug
+			// is provided by the caller.
+			const isSelected =
+				selectedSlug !== undefined
+					? slug === selectedSlug
+					: value === color;
```

Mostly guarding from an unlikely edge case, but the `selectedSlug !== undefined` check will enable "slug matching mode" even when `selectedSlug` is `null` or `''`.

Maybe we could change to


```suggestion
			const isSelected = selectedSlug
				? slug === selectedSlug
				: value === color;
```



<details>

<summary>Here's another test we could add, which fails currently (but shouldn't fail with the suggested change)</summary>

```ts
		it( 'should treat an empty-string selectedSlug as "no slug" and fall back to color-value selection', () => {
			render(
				<ColorPalette
					colors={ DUPLICATE_COLOR_PALETTE }
					value="#000"
					selectedSlug=""
					onChange={ jest.fn() }
				/>
			);
			const options = screen.getAllByRole( 'option' );
			expect( options[ 0 ] ).toHaveAttribute( 'aria-selected', 'true' );
			expect( options[ 1 ] ).toHaveAttribute( 'aria-selected', 'true' );
		} );
```

</details>

### Review thread 8 (resolved)

#### ciampo on 2026-05-11T12:43:45Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3218962119
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -50,16 +50,23 @@ function SinglePalette( {
 	colors,
 	onChange,
 	value,
+	selectedSlug,
 	...additionalProps
 }: SinglePaletteProps ) {
 	const colorOptions = useMemo( () => {
-		return colors.map( ( { color, name }, index ) => {
+		return colors.map( ( { color, name, slug }, index ) => {
 			const colordColor = colord( color );
-			const isSelected = value === color;
+			// When selectedSlug is provided, only match entries with that slug.
+			// This correctly handles mixed palettes where some entries have slugs
+			// and others don't. Fall back to color value matching when no slug
+			// is provided by the caller.
```

Nit: The "When provided" part could be slightly misleading (`''` and other falsey values also fall back to color matching). Maybe a better version could be:

```suggestion
			// When a non-empty selectedSlug is provided, selection is decided
			// strictly by slug — entries without a slug or with a different slug
			// are not selected, even when their color value matches `value`.
			// This correctly handles mixed palettes where some entries have slugs
			// and others don't. Fall back to color value matching otherwise
			// (including when selectedSlug is an empty string).
```

### Review thread 9 (resolved)

#### ciampo on 2026-05-11T12:46:06Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3218976532
Path: packages/components/src/color-palette/types.ts

```diff
@@ -24,9 +25,18 @@ type PaletteProps = {
 	clearColor: () => void;
 	/**
 	 * Callback called when a color is selected.
+	 * The third argument is the slug of the selected palette entry, when available.
 	 */
-	onChange: ( newColor?: string, index?: number ) => void;
+	onChange: ( newColor?: string, index?: number, slug?: string ) => void;
 	value?: string;
+	/**
+	 * The slug of the currently selected palette entry.
+	 * When provided, selection is determined by slug rather than color value,
+	 * which correctly handles palettes where two entries share the same color.
+	 * Note: Palette entries without a slug will not appear selected when this prop is provided,
+	 * even if their color value matches the `value` prop.
+	 */
+	selectedSlug?: string;
```

Same re-wording re. the "when provided" part

```suggestion
	/**
	 * The slug of the currently selected palette entry.
	 *
	 * When set to a non-empty string, selection is determined by slug rather
	 * than by color value — this correctly handles palettes where two entries
	 * share the same color. Palette entries without a slug will not appear
	 * selected in this mode, even if their color value matches `value`.
	 *
	 * An empty string is treated the same as `undefined`: selection falls
	 * back to matching by color value.
 	 */
	selectedSlug?: string;
```

### Review thread 10 (resolved)

#### ciampo on 2026-05-11T12:47:23Z

URL: https://github.com/WordPress/gutenberg/pull/78004#discussion_r3218984135
Path: packages/components/src/color-palette/utils.ts

```diff
@@ -50,12 +51,25 @@ export const extractColorNameFromCurrentValue = (
 		? ( colors as PaletteObject[] )
 		: [ { colors: colors as ColorObject[] } ];
 	for ( const { colors: paletteColors } of colorPalettes ) {
-		for ( const { name: colorName, color: colorValue } of paletteColors ) {
+		for ( const {
+			name: colorName,
+			color: colorValue,
+			slug,
+		} of paletteColors ) {
 			const normalizedColorValue = currentValueIsSimpleColor
 				? colord( colorValue ).toHex()
 				: colorValue;
+			const matchesSelectedSlug =
+				selectedSlug && slug ? slug === selectedSlug : false;
 
-			if ( normalizedCurrentValue === normalizedColorValue ) {
+			if ( matchesSelectedSlug ) {
+				return colorName;
+			}
+
+			if (
+				! selectedSlug &&
+				normalizedCurrentValue === normalizedColorValue
+			) {
 				return colorName;
 			}
 		}
```

Could this part be simplified to

```ts
for ( const { name: colorName, color: colorValue, slug } of paletteColors ) {
	if ( selectedSlug ) {
		if ( slug === selectedSlug ) {
			return colorName;
		}
		continue;
	}

	const normalizedColorValue = currentValueIsSimpleColor
		? colord( colorValue ).toHex()
		: colorValue;
	if ( normalizedCurrentValue === normalizedColorValue ) {
		return colorName;
	}
}
``` 

?

It would also skip the `colord()` call when `selectedSlug` is truthy

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/color-palette/index.tsx (MODIFIED, +25/-9)
- packages/components/src/color-palette/stories/index.story.tsx (MODIFIED, +20/-2)
- packages/components/src/color-palette/test/index.tsx (MODIFIED, +152/-1)
- packages/components/src/color-palette/test/utils.ts (MODIFIED, +18/-0)
- packages/components/src/color-palette/types.ts (MODIFIED, +19/-2)
- packages/components/src/color-palette/utils.ts (MODIFIED, +14/-2)
