---
number: 74778
type: pull_request
state: MERGED
merged_at: 2026-01-28T16:41:53Z
url: https://github.com/WordPress/gutenberg/pull/74778
matched_labels: ["Design System"]
all_labels: ["[Type] Enhancement","Design System"]
---

# PR #74778: Design System: Add guidelines for destructive actions UX

- URL: https://github.com/WordPress/gutenberg/pull/74778
- Author: jameskoster
- Created: 2026-01-20T15:38:07Z
- Updated: 2026-02-04T10:55:41Z
- Comments: 5 of 5
- Review threads: 3 of 3
- Changed files: 1 of 1
- Diff: ../pr-diffs/74778.diff

## Body

To test:

- `npm run storybook`
- Visit Design System → Patterns → Destructive actions

cc @poligilad-auto

## Issue comments

### github-actions on 2026-01-27T19:12:45Z

URL: https://github.com/WordPress/gutenberg/pull/74778#issuecomment-3807021533

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: jameskoster <jameskoster@git.wordpress.org>
Co-authored-by: fcoveram <fcoveram@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### jameskoster on 2026-01-28T16:03:48Z

URL: https://github.com/WordPress/gutenberg/pull/74778#issuecomment-3812185309

Added a note about additional confirmation patterns since it came up [yesterday](https://github.com/WordPress/gutenberg/issues/58136#issuecomment-3805079569).

### poligilad-auto on 2026-02-04T09:32:45Z

URL: https://github.com/WordPress/gutenberg/pull/74778#issuecomment-3846334779

Apologies for just getting to it now. I think we should add some specific guidance on when to use the red destructive CTAs vs regular ones. WDYT @jameskoster.

Something like:
"Use a red/destructive CTA only when the primary action is irreversible (or effectively irreversible) and causes permanent loss or removal (e.g., delete, remove access)."

### jameskoster on 2026-02-04T10:32:39Z

URL: https://github.com/WordPress/gutenberg/pull/74778#issuecomment-3846620803

@poligilad-auto [it's there](https://wordpress.github.io/gutenberg/?path=/docs/design-system-patterns-destructive-actions--docs#use-modal-with-destructive-button) :)

### poligilad-auto on 2026-02-04T10:55:41Z

URL: https://github.com/WordPress/gutenberg/pull/74778#issuecomment-3846734258

Sorry, was looking elsewhere 😄

## Review threads

### Review thread 1 (resolved)

#### aduth on 2026-01-27T19:01:39Z

URL: https://github.com/WordPress/gutenberg/pull/74778#discussion_r2733422809
Path: storybook/stories/design-system/patterns/destructive-actions.mdx

```diff
@@ -0,0 +1,104 @@
+import { Meta } from '@storybook/addon-docs/blocks';
+
+<Meta title="Design System/Patterns/Destructive Actions" />
+
+# Destructive Actions
+
+Destructive actions are operations that remove, delete, or permanently alter content or data. The appropriate pattern to use depends on the reversibility and immediacy of the action's result.
+
+## Decision Tree
+
+When implementing a destructive action, follow this decision tree:
+
+1. **Does the action have an immediate visible result on the frontend?**
+   - If **no** → [No confirmation needed](#no-confirmation-needed)
+   - If **yes** → Continue to question 2
+
+2. **Can the action be easily undone?**
+   - If **yes** → [Use confirmDialog](#use-confirmdialog)
+   - If **no** → [Use Modal with destructive button](#use-modal-with-destructive-button)
+
+## No Confirmation Needed
+
+Destructive actions that have no immediate visible result on the frontend (e.g., deleting a block) do not require any destructive styling or confirmation.
+
+### When to Use
+
+- The action doesn't produce an immediate visible change on the frontend
+- The action can be reversed through standard undo functionality
+- The user expects the action to be reversible
+
+### Implementation
+
+- Use standard trigger styling (no destructive (red) styling)
```

The nested parentheses are a bit awkward to read. Maybe it can be rephrased as a sentence?

```suggestion
- Use standard trigger styling, avoiding destructive (red) styling
```

### Review thread 2 (resolved)

#### aduth on 2026-01-27T19:03:45Z

URL: https://github.com/WordPress/gutenberg/pull/74778#discussion_r2733429753
Path: storybook/stories/design-system/patterns/destructive-actions.mdx

```diff
@@ -0,0 +1,104 @@
+import { Meta } from '@storybook/addon-docs/blocks';
+
+<Meta title="Design System/Patterns/Destructive Actions" />
+
+# Destructive Actions
+
+Destructive actions are operations that remove, delete, or permanently alter content or data. The appropriate pattern to use depends on the reversibility and immediacy of the action's result.
+
+## Decision Tree
+
+When implementing a destructive action, follow this decision tree:
+
+1. **Does the action have an immediate visible result on the frontend?**
+   - If **no** → [No confirmation needed](#no-confirmation-needed)
+   - If **yes** → Continue to question 2
+
+2. **Can the action be easily undone?**
+   - If **yes** → [Use confirmDialog](#use-confirmdialog)
+   - If **no** → [Use Modal with destructive button](#use-modal-with-destructive-button)
+
+## No Confirmation Needed
+
+Destructive actions that have no immediate visible result on the frontend (e.g., deleting a block) do not require any destructive styling or confirmation.
+
+### When to Use
+
+- The action doesn't produce an immediate visible change on the frontend
+- The action can be reversed through standard undo functionality
+- The user expects the action to be reversible
+
+### Implementation
+
+- Use standard trigger styling (no destructive (red) styling)
+- No confirmation dialog required
+- Ensure undo functionality is available
+
+**Example**: Deleting a block in the editor. The block is removed but the change can be immediately undone using the undo command.
+
+## Use confirmDialog
+
+Destructive actions that have an immediate visible result but can be easily undone (e.g., moving to trash) require a confirmation using `confirmDialog`. A snackbar with undo affordance should be displayed after the action completes.
```

The capitalization here had me confused whether we had a `confirmDialog` helper function. Since this is referring to the component, we should capitalize it as `ConfirmDialog` and mention  that it's a component. This is clearer below in "Implementation", but I think we can make it obvious sooner.

```suggestion
## Use ConfirmDialog

Destructive actions that have an immediate visible result but can be easily undone (e.g., moving to trash) require a confirmation using the `ConfirmDialog` component. A snackbar with undo affordance should be displayed after the action completes.
```

### Review thread 3 (resolved)

#### aduth on 2026-01-27T19:09:52Z

URL: https://github.com/WordPress/gutenberg/pull/74778#discussion_r2733452601
Path: storybook/stories/design-system/patterns/destructive-actions.mdx

```diff
@@ -0,0 +1,104 @@
+import { Meta } from '@storybook/addon-docs/blocks';
+
+<Meta title="Design System/Patterns/Destructive Actions" />
+
+# Destructive Actions
+
+Destructive actions are operations that remove, delete, or permanently alter content or data. The appropriate pattern to use depends on the reversibility and immediacy of the action's result.
+
+## Decision Tree
+
+When implementing a destructive action, follow this decision tree:
+
+1. **Does the action have an immediate visible result on the frontend?**
+   - If **no** → [No confirmation needed](#no-confirmation-needed)
+   - If **yes** → Continue to question 2
+
+2. **Can the action be easily undone?**
+   - If **yes** → [Use confirmDialog](#use-confirmdialog)
+   - If **no** → [Use Modal with destructive button](#use-modal-with-destructive-button)
+
+## No Confirmation Needed
+
+Destructive actions that have no immediate visible result on the frontend (e.g., deleting a block) do not require any destructive styling or confirmation.
+
+### When to Use
+
+- The action doesn't produce an immediate visible change on the frontend
+- The action can be reversed through standard undo functionality
+- The user expects the action to be reversible
+
+### Implementation
+
+- Use standard trigger styling (no destructive (red) styling)
+- No confirmation dialog required
+- Ensure undo functionality is available
+
+**Example**: Deleting a block in the editor. The block is removed but the change can be immediately undone using the undo command.
+
+## Use confirmDialog
+
+Destructive actions that have an immediate visible result but can be easily undone (e.g., moving to trash) require a confirmation using `confirmDialog`. A snackbar with undo affordance should be displayed after the action completes.
+
+### When to Use
+
+- The action produces an immediate visible change
+- The action can be reversed through a clear, accessible mechanism (e.g., trash/restore)
+- The user should be aware of the change before it happens
+
+### Implementation
+
+- Use `__experimentalConfirmDialog` from `@wordpress/components`
+- The dialog can be dismissed (ESC key, click outside, cancel button)
+- Standard button styling for the trigger (no destructive styling)
+- The confirmation button uses primary variant (not destructive)
+- After the action completes, display a Snackbar with an undo action
+
+### Component Details
+
+`ConfirmDialog` is built on top of `Modal` and provides:
+- Automatic focus management
+- Keyboard navigation (Enter to confirm, ESC to cancel)
+- Accessible dialog structure
+- Standard button layout (Cancel + Confirm)
+
+**Example**: Moving a post to trash. The post status changes immediately, but it can be restored from the trash.
+
+## Use Modal with Destructive Button
+
+Destructive actions that cannot be undone require a confirmation, but the primary action button should be `destructive`. Use `Modal` without a close button. A snackbar should confirm the action completes.
+
+### When to Use
+
+- The action produces an immediate, permanent change
+- The action cannot be reversed or undone
+- The consequences are significant and irreversible
+
+### Implementation
+
+- Use `Modal` component (not `ConfirmDialog`)
+- Set `isDismissible={ false }` to prevent closing via the header button
+- Set `shouldCloseOnEsc={ false }` to prevent closing via ESC key
+- Set `shouldCloseOnClickOutside={ false }` to prevent closing via overlay click
+- Set `__experimentalHideHeader={ true }` to hide the header and close button
```

1. Do we actually want the header (and specifically the title) to be hidden? Maybe this also leads into a question of content guidelines for these types of confirmation modals.
2. We might want to avoid the `={ true }` in the code snippets here because we use a [lint rule which forbids that syntax](https://github.com/jsx-eslint/eslint-plugin-react/blob/master/docs/rules/jsx-boolean-value.md).

#### jameskoster on 2026-01-28T11:24:44Z

URL: https://github.com/WordPress/gutenberg/pull/74778#discussion_r2736189949
Path: storybook/stories/design-system/patterns/destructive-actions.mdx

```diff
@@ -0,0 +1,104 @@
+import { Meta } from '@storybook/addon-docs/blocks';
+
+<Meta title="Design System/Patterns/Destructive Actions" />
+
+# Destructive Actions
+
+Destructive actions are operations that remove, delete, or permanently alter content or data. The appropriate pattern to use depends on the reversibility and immediacy of the action's result.
+
+## Decision Tree
+
+When implementing a destructive action, follow this decision tree:
+
+1. **Does the action have an immediate visible result on the frontend?**
+   - If **no** → [No confirmation needed](#no-confirmation-needed)
+   - If **yes** → Continue to question 2
+
+2. **Can the action be easily undone?**
+   - If **yes** → [Use confirmDialog](#use-confirmdialog)
+   - If **no** → [Use Modal with destructive button](#use-modal-with-destructive-button)
+
+## No Confirmation Needed
+
+Destructive actions that have no immediate visible result on the frontend (e.g., deleting a block) do not require any destructive styling or confirmation.
+
+### When to Use
+
+- The action doesn't produce an immediate visible change on the frontend
+- The action can be reversed through standard undo functionality
+- The user expects the action to be reversible
+
+### Implementation
+
+- Use standard trigger styling (no destructive (red) styling)
+- No confirmation dialog required
+- Ensure undo functionality is available
+
+**Example**: Deleting a block in the editor. The block is removed but the change can be immediately undone using the undo command.
+
+## Use confirmDialog
+
+Destructive actions that have an immediate visible result but can be easily undone (e.g., moving to trash) require a confirmation using `confirmDialog`. A snackbar with undo affordance should be displayed after the action completes.
+
+### When to Use
+
+- The action produces an immediate visible change
+- The action can be reversed through a clear, accessible mechanism (e.g., trash/restore)
+- The user should be aware of the change before it happens
+
+### Implementation
+
+- Use `__experimentalConfirmDialog` from `@wordpress/components`
+- The dialog can be dismissed (ESC key, click outside, cancel button)
+- Standard button styling for the trigger (no destructive styling)
+- The confirmation button uses primary variant (not destructive)
+- After the action completes, display a Snackbar with an undo action
+
+### Component Details
+
+`ConfirmDialog` is built on top of `Modal` and provides:
+- Automatic focus management
+- Keyboard navigation (Enter to confirm, ESC to cancel)
+- Accessible dialog structure
+- Standard button layout (Cancel + Confirm)
+
+**Example**: Moving a post to trash. The post status changes immediately, but it can be restored from the trash.
+
+## Use Modal with Destructive Button
+
+Destructive actions that cannot be undone require a confirmation, but the primary action button should be `destructive`. Use `Modal` without a close button. A snackbar should confirm the action completes.
+
+### When to Use
+
+- The action produces an immediate, permanent change
+- The action cannot be reversed or undone
+- The consequences are significant and irreversible
+
+### Implementation
+
+- Use `Modal` component (not `ConfirmDialog`)
+- Set `isDismissible={ false }` to prevent closing via the header button
+- Set `shouldCloseOnEsc={ false }` to prevent closing via ESC key
+- Set `shouldCloseOnClickOutside={ false }` to prevent closing via overlay click
+- Set `__experimentalHideHeader={ true }` to hide the header and close button
```

Good catch, I think the suggestion from @poligilad-auto was to keep the header. But we want to hide the close button. I suppose that's handled by `isDismissible`. I'll update accordingly.

## Changed files

- storybook/stories/design-system/patterns/destructive-actions.mdx (ADDED, +106/-0)
