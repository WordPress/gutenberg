---
number: 74094
type: pull_request
state: MERGED
merged_at: 2025-12-31T12:21:47Z
url: https://github.com/WordPress/gutenberg/pull/74094
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","Needs Testing","[Package] Components"]
---

# PR #74094: Components: Enhance Notice actions to allow more props like disabled and onClick with url

- URL: https://github.com/WordPress/gutenberg/pull/74094
- Author: manzoorwanijk
- Created: 2025-12-18T09:36:40Z
- Updated: 2025-12-31T12:22:24Z
- Comments: 12 of 12
- Review threads: 11 of 11
- Changed files: 7 of 7
- Diff: ../pr-diffs/74094.diff

## Body

Related to #74090

## What?

Enhances the `Notice` component's action buttons to support additional `Button` component features.

## Why?

Notice action buttons were missing common Button features like `disabled`, etc. The `openInNewTab` prop existed in types but wasn't implemented, and `onClick` was ignored when `url` was provided.

## How?

- Updated `NoticeAction` types to allow `disabled` and `onClick` alongside `url`
- Remove `openInNewTab` from the types.

## Testing Instructions

1. Check out this branch
2. Run `npm run storybook:dev`
3. Navigate to Components > Feedback > Notice
4. Test the new stories:
   - ** With Disabled Action**: Verify the disabled button cannot be clicked
   - ** With Open In New Tab**: Verify link opens in new tab

## Testing Instructions for Keyboard

- Tab to action buttons and verify they receive focus
- Press Enter/Space to activate buttons
- Disabled buttons should still be focusable via keyboard

## Screenshots

<img width="296" height="125" alt="Screenshot 2025-12-18 at 2 59 46 PM" src="https://github.com/user-attachments/assets/0b8824da-ea93-4e34-a982-e3cb32d4aead" />

<img width="264" height="104" alt="Screenshot 2025-12-28 at 11 59 16 AM" src="https://github.com/user-attachments/assets/6f7213c4-983b-4c25-b884-450571b09498" />

## Issue comments

### github-actions on 2025-12-18T09:41:52Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3669371895

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: manzoorwanijk <manzoorwanijk@git.wordpress.org>
Co-authored-by: gmjuhasz <gmjuhasz@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: SirLouen <sirlouen@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### Mamaduka on 2025-12-18T09:49:49Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3669404699

I wonder if some of this was intentional to keep UI more or less consistent.

### manzoorwanijk on 2025-12-18T09:59:16Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3669444498

> I wonder if some of this was intentional to keep UI more or less consistent.

Actions were [added 7 years ago](https://github.com/WordPress/gutenberg/pull/9617/changes/08a18a4333c5fe1afaf5fb466488b4bf4e6f25db) by @aduth and @gziolo and I couldn't find any mention of any intentions there, except [this for future enhancements](https://github.com/WordPress/gutenberg/pull/9617#issue-356975179):

> Most of our existing use of elements could be better served by a formal "actions" option (e.g., "View Post" on the save success) which is more future-compatible to notices design revisions.

As mentioned and explained in the linked issue, actions for those notices need to be versatile, it makes it hard to use them for real-world cases.

### github-actions on 2025-12-18T12:18:13Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3670014531

<!-- flaky-tests-report-comment -->
**Flaky tests detected in c71acc2f6a766f7ea87386f2aede420e14e9e736.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/20552994612
📝  Reported issues:
- #68604 in `/test/e2e/specs/site-editor/template-revert.spec.js`

### t-hamano on 2025-12-20T14:51:30Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3677876072

If you want to build a completely free UI, you can render it via the `children` prop:

```jsx
<Notice status="info">
	<Flex justify="start" wrap>
		<Button variant="primary">Push Me</Button>
		<Button variant="primary" disabled>Push Me</Button>
		<Button variant="primary" isBusy>Push Me</Button>
		<Button variant="primary" size="small">Push Me</Button>
	</Flex>
</Notice>
```

I wonder if there is anything that can't be achieved without extending the actions property 🤔

### manzoorwanijk on 2025-12-21T10:51:30Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3678684979

> If you want to build a completely free UI, you can render it via the `children` prop:

Yes, we can do that but having the actual component fixed is probably better and has no harm.

In future, I would expect the actions to be completely flexible by making them composable by directly rendering `{ actions }` instead of looping through them.

### ciampo on 2025-12-22T09:41:04Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3681251754

Left some thoughts in https://github.com/WordPress/gutenberg/issues/74090#issuecomment-3681251227

### manzoorwanijk on 2025-12-24T15:53:18Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3690147311

As mentioned in https://github.com/WordPress/gutenberg/issues/74090#issuecomment-3690067578, we want to go with a non-controversial approach, that is what I have done for now.

### SirLouen on 2025-12-27T23:45:03Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3694301227

> So what do we want to do here? Maybe we can close this PR, live with what we have for now, and hope to resolve the issues in @wordpress/ui?

@manzoorwanijk my conclusion reading through the whole convo is the following: limit the scope of this PR, by removing the open in new tab elements (alas, we missed `rel` & `target`). You will be able to push at least something in this PR, but not entirely.

### manzoorwanijk on 2025-12-28T06:46:34Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3694510839

I have updated the PR (along with the description) to reduce the scope to do only three things:
- Allow `disabled` prop for buttons.
- Allow `onClick` alongside `url`.
- Wire up `openInNewTab` to render `ExternalLink` when it's `true`.

### SirLouen on 2025-12-29T19:20:15Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3697310791

> I have updated the PR (along with the description) to reduce the scope to do only three things:
> 
> * Allow `disabled` prop for buttons.
> * Allow `onClick` alongside `url`.
> * Wire up `openInNewTab` to render `ExternalLink` when it's `true`.

👍 Looking better to me, I would like to test a bit, but want to see what others think now.

### mirka on 2025-12-30T18:28:04Z

URL: https://github.com/WordPress/gutenberg/pull/74094#issuecomment-3700135845

I'm afraid I can't enthusiastically greenlight the `openInNewTab` part, but everything else is good on my end.

## Review threads

### Review thread 1 (resolved)

#### gmjuhasz on 2025-12-18T10:16:55Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2630422370
Path: packages/components/src/notice/stories/index.story.tsx

```diff
@@ -141,3 +142,145 @@ export const NoticeListSubcomponent: StoryFn< typeof NoticeList > = () => {
 	);
 };
 NoticeListSubcomponent.storyName = 'NoticeList Subcomponent';
+
+export const WithDisabledAction = Template.bind( {} );
+WithDisabledAction.args = {
+	...Default.args,
+	children: 'This notice has a disabled action.',
+	actions: [
+		{
+			label: 'Disabled action',
+			onClick: () => {},
+			disabled: true,
+		},
+		{
+			label: 'Enabled action',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithIconActions = Template.bind( {} );
+WithIconActions.args = {
+	...Default.args,
+	children: 'This notice has actions with icons.',
+	actions: [
+		{
+			label: 'Upload',
+			onClick: () => {},
+			icon: upload,
+			variant: 'primary',
+		},
+		{
+			label: 'Done',
+			onClick: () => {},
+			icon: check,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithLoadingAction: StoryFn< typeof Notice > = ( props ) => {
+	const [ isLoading, setIsLoading ] = useState( false );
+
+	const handleClick = () => {
+		setIsLoading( true );
+		setTimeout( () => setIsLoading( false ), 2000 );
+	};
+
+	return (
+		<Notice
+			{ ...props }
+			actions={ [
+				{
+					label: isLoading ? 'Saving...' : 'Save',
+					onClick: handleClick,
+					isBusy: isLoading,
+					disabled: isLoading,
+					variant: 'primary',
+				},
+			] }
+		>
+			Click the button to see the loading state.
+		</Notice>
+	);
+};
+
+export const WithDestructiveAction = Template.bind( {} );
+WithDestructiveAction.args = {
+	...Default.args,
+	status: 'warning',
+	children: 'Are you sure you want to delete this item?',
+	actions: [
+		{
+			label: 'Delete',
+			onClick: () => {},
+			isDestructive: true,
+			variant: 'primary',
+		},
+		{
+			label: 'Cancel',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithOpenInNewTab = Template.bind( {} );
+WithOpenInNewTab.args = {
+	...Default.args,
+	children: 'Learn more about this feature.',
+	actions: [
+		{
+			label: 'Documentation',
+			url: 'https://wordpress.org',
+			openInNewTab: true,
+			icon: external,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithDifferentSizes = Template.bind( {} );
+WithDifferentSizes.args = {
+	...Default.args,
+	children: 'Actions with different sizes.',
+	actions: [
+		{
+			label: 'Small',
+			onClick: () => {},
+			size: 'small',
+		},
+		{
+			label: 'Compact',
+			onClick: () => {},
+			size: 'compact',
+		},
+		{
+			label: 'Default',
+			onClick: () => {},
+			size: 'default',
+		},
+	],
+};
+
+export const WithOnClickAndUrl: StoryFn< typeof Notice > = ( props ) => {
+	const [ clickCount, setClickCount ] = useState( 0 );
+
+	return (
+		<>
```

Do we need this extra Wrapper?

#### manzoorwanijk on 2025-12-18T10:38:30Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2630495928
Path: packages/components/src/notice/stories/index.story.tsx

```diff
@@ -141,3 +142,145 @@ export const NoticeListSubcomponent: StoryFn< typeof NoticeList > = () => {
 	);
 };
 NoticeListSubcomponent.storyName = 'NoticeList Subcomponent';
+
+export const WithDisabledAction = Template.bind( {} );
+WithDisabledAction.args = {
+	...Default.args,
+	children: 'This notice has a disabled action.',
+	actions: [
+		{
+			label: 'Disabled action',
+			onClick: () => {},
+			disabled: true,
+		},
+		{
+			label: 'Enabled action',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithIconActions = Template.bind( {} );
+WithIconActions.args = {
+	...Default.args,
+	children: 'This notice has actions with icons.',
+	actions: [
+		{
+			label: 'Upload',
+			onClick: () => {},
+			icon: upload,
+			variant: 'primary',
+		},
+		{
+			label: 'Done',
+			onClick: () => {},
+			icon: check,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithLoadingAction: StoryFn< typeof Notice > = ( props ) => {
+	const [ isLoading, setIsLoading ] = useState( false );
+
+	const handleClick = () => {
+		setIsLoading( true );
+		setTimeout( () => setIsLoading( false ), 2000 );
+	};
+
+	return (
+		<Notice
+			{ ...props }
+			actions={ [
+				{
+					label: isLoading ? 'Saving...' : 'Save',
+					onClick: handleClick,
+					isBusy: isLoading,
+					disabled: isLoading,
+					variant: 'primary',
+				},
+			] }
+		>
+			Click the button to see the loading state.
+		</Notice>
+	);
+};
+
+export const WithDestructiveAction = Template.bind( {} );
+WithDestructiveAction.args = {
+	...Default.args,
+	status: 'warning',
+	children: 'Are you sure you want to delete this item?',
+	actions: [
+		{
+			label: 'Delete',
+			onClick: () => {},
+			isDestructive: true,
+			variant: 'primary',
+		},
+		{
+			label: 'Cancel',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithOpenInNewTab = Template.bind( {} );
+WithOpenInNewTab.args = {
+	...Default.args,
+	children: 'Learn more about this feature.',
+	actions: [
+		{
+			label: 'Documentation',
+			url: 'https://wordpress.org',
+			openInNewTab: true,
+			icon: external,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithDifferentSizes = Template.bind( {} );
+WithDifferentSizes.args = {
+	...Default.args,
+	children: 'Actions with different sizes.',
+	actions: [
+		{
+			label: 'Small',
+			onClick: () => {},
+			size: 'small',
+		},
+		{
+			label: 'Compact',
+			onClick: () => {},
+			size: 'compact',
+		},
+		{
+			label: 'Default',
+			onClick: () => {},
+			size: 'default',
+		},
+	],
+};
+
+export const WithOnClickAndUrl: StoryFn< typeof Notice > = ( props ) => {
+	const [ clickCount, setClickCount ] = useState( 0 );
+
+	return (
+		<>
```

Not anymore. I initially had the count text outside the notice and then I moved it inside but forgot to get rid of the fragment. Thanks

### Review thread 2 (resolved)

#### gmjuhasz on 2025-12-18T10:18:36Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2630426854
Path: packages/components/src/notice/stories/index.story.tsx

```diff
@@ -141,3 +142,145 @@ export const NoticeListSubcomponent: StoryFn< typeof NoticeList > = () => {
 	);
 };
 NoticeListSubcomponent.storyName = 'NoticeList Subcomponent';
+
+export const WithDisabledAction = Template.bind( {} );
+WithDisabledAction.args = {
+	...Default.args,
+	children: 'This notice has a disabled action.',
+	actions: [
+		{
+			label: 'Disabled action',
+			onClick: () => {},
+			disabled: true,
+		},
+		{
+			label: 'Enabled action',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithIconActions = Template.bind( {} );
+WithIconActions.args = {
+	...Default.args,
+	children: 'This notice has actions with icons.',
+	actions: [
+		{
+			label: 'Upload',
+			onClick: () => {},
+			icon: upload,
+			variant: 'primary',
+		},
+		{
+			label: 'Done',
+			onClick: () => {},
+			icon: check,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithLoadingAction: StoryFn< typeof Notice > = ( props ) => {
+	const [ isLoading, setIsLoading ] = useState( false );
+
+	const handleClick = () => {
+		setIsLoading( true );
+		setTimeout( () => setIsLoading( false ), 2000 );
+	};
+
+	return (
+		<Notice
+			{ ...props }
+			actions={ [
+				{
+					label: isLoading ? 'Saving...' : 'Save',
+					onClick: handleClick,
+					isBusy: isLoading,
+					disabled: isLoading,
+					variant: 'primary',
+				},
+			] }
+		>
+			Click the button to see the loading state.
+		</Notice>
+	);
+};
+
+export const WithDestructiveAction = Template.bind( {} );
+WithDestructiveAction.args = {
+	...Default.args,
+	status: 'warning',
+	children: 'Are you sure you want to delete this item?',
+	actions: [
+		{
+			label: 'Delete',
+			onClick: () => {},
+			isDestructive: true,
+			variant: 'primary',
+		},
+		{
+			label: 'Cancel',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithOpenInNewTab = Template.bind( {} );
+WithOpenInNewTab.args = {
+	...Default.args,
+	children: 'Learn more about this feature.',
+	actions: [
+		{
+			label: 'Documentation',
+			url: 'https://wordpress.org',
+			openInNewTab: true,
+			icon: external,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithDifferentSizes = Template.bind( {} );
+WithDifferentSizes.args = {
+	...Default.args,
+	children: 'Actions with different sizes.',
+	actions: [
+		{
+			label: 'Small',
+			onClick: () => {},
+			size: 'small',
+		},
+		{
+			label: 'Compact',
+			onClick: () => {},
+			size: 'compact',
+		},
+		{
+			label: 'Default',
+			onClick: () => {},
+			size: 'default',
+		},
+	],
+};
+
+export const WithOnClickAndUrl: StoryFn< typeof Notice > = ( props ) => {
+	const [ clickCount, setClickCount ] = useState( 0 );
+
+	return (
+		<>
+			<Notice
+				{ ...props }
+				actions={ [
+					{
+						label: 'Visit WordPress.org',
+						url: 'https://wordpress.org',
+						onClick: () => setClickCount( ( c ) => c + 1 ),
+						openInNewTab: true,
+					},
+				] }
+			>
+				onClick now works with url. Click count: { clickCount }
```

Nitpick: This is fixed now, but in the feature this sentence could make less sense in the story, should we just have "Click count"?

#### manzoorwanijk on 2025-12-18T10:38:39Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2630496598
Path: packages/components/src/notice/stories/index.story.tsx

```diff
@@ -141,3 +142,145 @@ export const NoticeListSubcomponent: StoryFn< typeof NoticeList > = () => {
 	);
 };
 NoticeListSubcomponent.storyName = 'NoticeList Subcomponent';
+
+export const WithDisabledAction = Template.bind( {} );
+WithDisabledAction.args = {
+	...Default.args,
+	children: 'This notice has a disabled action.',
+	actions: [
+		{
+			label: 'Disabled action',
+			onClick: () => {},
+			disabled: true,
+		},
+		{
+			label: 'Enabled action',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithIconActions = Template.bind( {} );
+WithIconActions.args = {
+	...Default.args,
+	children: 'This notice has actions with icons.',
+	actions: [
+		{
+			label: 'Upload',
+			onClick: () => {},
+			icon: upload,
+			variant: 'primary',
+		},
+		{
+			label: 'Done',
+			onClick: () => {},
+			icon: check,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithLoadingAction: StoryFn< typeof Notice > = ( props ) => {
+	const [ isLoading, setIsLoading ] = useState( false );
+
+	const handleClick = () => {
+		setIsLoading( true );
+		setTimeout( () => setIsLoading( false ), 2000 );
+	};
+
+	return (
+		<Notice
+			{ ...props }
+			actions={ [
+				{
+					label: isLoading ? 'Saving...' : 'Save',
+					onClick: handleClick,
+					isBusy: isLoading,
+					disabled: isLoading,
+					variant: 'primary',
+				},
+			] }
+		>
+			Click the button to see the loading state.
+		</Notice>
+	);
+};
+
+export const WithDestructiveAction = Template.bind( {} );
+WithDestructiveAction.args = {
+	...Default.args,
+	status: 'warning',
+	children: 'Are you sure you want to delete this item?',
+	actions: [
+		{
+			label: 'Delete',
+			onClick: () => {},
+			isDestructive: true,
+			variant: 'primary',
+		},
+		{
+			label: 'Cancel',
+			onClick: () => {},
+		},
+	],
+};
+
+export const WithOpenInNewTab = Template.bind( {} );
+WithOpenInNewTab.args = {
+	...Default.args,
+	children: 'Learn more about this feature.',
+	actions: [
+		{
+			label: 'Documentation',
+			url: 'https://wordpress.org',
+			openInNewTab: true,
+			icon: external,
+			iconPosition: 'right',
+		},
+	],
+};
+
+export const WithDifferentSizes = Template.bind( {} );
+WithDifferentSizes.args = {
+	...Default.args,
+	children: 'Actions with different sizes.',
+	actions: [
+		{
+			label: 'Small',
+			onClick: () => {},
+			size: 'small',
+		},
+		{
+			label: 'Compact',
+			onClick: () => {},
+			size: 'compact',
+		},
+		{
+			label: 'Default',
+			onClick: () => {},
+			size: 'default',
+		},
+	],
+};
+
+export const WithOnClickAndUrl: StoryFn< typeof Notice > = ( props ) => {
+	const [ clickCount, setClickCount ] = useState( 0 );
+
+	return (
+		<>
+			<Notice
+				{ ...props }
+				actions={ [
+					{
+						label: 'Visit WordPress.org',
+						url: 'https://wordpress.org',
+						onClick: () => setClickCount( ( c ) => c + 1 ),
+						openInNewTab: true,
+					},
+				] }
+			>
+				onClick now works with url. Click count: { clickCount }
```

True. Let me update it.

### Review thread 3 (resolved)

#### aduth on 2025-12-18T19:00:38Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2632303221
Path: packages/components/src/notice/index.tsx

```diff
@@ -123,6 +123,8 @@ function Notice( {
 									noDefaultClasses = false,
 									onClick,
 									url,
+									openInNewTab,
```

I think I'm generally on board with the idea of notice actions basically being a pass-through to button, but I'm less convinced about this unique property. Since along the same line of thinking, why would `Notice` actions support this and not `Button` , if they're basically the same concept. My sense is that the consumer could be assigning this via `target` as expecting to interface with Button.

#### manzoorwanijk on 2025-12-19T05:43:07Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2633701062
Path: packages/components/src/notice/index.tsx

```diff
@@ -123,6 +123,8 @@ function Notice( {
 									noDefaultClasses = false,
 									onClick,
 									url,
+									openInNewTab,
```

I agree on that. It's there because the action accepts `url` instead of `href`. May be it's better to simply remove it regardless.

#### manzoorwanijk on 2025-12-19T05:58:22Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2633765476
Path: packages/components/src/notice/index.tsx

```diff
@@ -123,6 +123,8 @@ function Notice( {
 									noDefaultClasses = false,
 									onClick,
 									url,
+									openInNewTab,
```

Removed that. I went ahead and also added support for `href` prop along with URL to make it more consistent. The consumer can pass one of the two but not both.

### Review thread 4 (resolved)

#### aduth on 2025-12-19T17:08:29Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635752871
Path: packages/components/src/notice/types.ts

```diff
@@ -3,27 +3,53 @@
  */
 import type { MouseEventHandler, ReactNode } from 'react';
 
-type CommonNoticeActionProps = {
-	label: string;
+/**
+ * Internal dependencies
+ */
+import type { ButtonAsButtonProps, ButtonAsAnchorProps } from '../button/types';
+
+type CommonNoticeActionProps = Omit<
+	ButtonAsButtonProps,
+	// Omit props that are handled internally or don't apply to notice actions
+	'href' | 'children' | 'className'
+> & {
 	className?: string;
 	noDefaultClasses?: boolean;
-	variant?: 'primary' | 'secondary' | 'link';
 };
-// `url` and `onClick` can both be provided, but `url` takes precedence. If
-// `url` is provided, the action's button will be rendered as an anchor and
-// `onClick` will be ignored.
-type NoticeActionWithURL = CommonNoticeActionProps & {
-	url: string;
-	openInNewTab?: boolean;
```

Hm, maybe I hadn't realized that this was a prop we already supported. I thought were adding something new. We probably shouldn't break support for that. Although I think it could be deprecated / discouraged in favor of `target`.

On a related note, we'll want to update the documented props for actions, and ideally explain how additional props for Button are supported:

https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/notice/README.md#actions-arraynoticeaction

#### manzoorwanijk on 2025-12-19T17:27:25Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635798826
Path: packages/components/src/notice/types.ts

```diff
@@ -3,27 +3,53 @@
  */
 import type { MouseEventHandler, ReactNode } from 'react';
 
-type CommonNoticeActionProps = {
-	label: string;
+/**
+ * Internal dependencies
+ */
+import type { ButtonAsButtonProps, ButtonAsAnchorProps } from '../button/types';
+
+type CommonNoticeActionProps = Omit<
+	ButtonAsButtonProps,
+	// Omit props that are handled internally or don't apply to notice actions
+	'href' | 'children' | 'className'
+> & {
 	className?: string;
 	noDefaultClasses?: boolean;
-	variant?: 'primary' | 'secondary' | 'link';
 };
-// `url` and `onClick` can both be provided, but `url` takes precedence. If
-// `url` is provided, the action's button will be rendered as an anchor and
-// `onClick` will be ignored.
-type NoticeActionWithURL = CommonNoticeActionProps & {
-	url: string;
-	openInNewTab?: boolean;
```

As mentioned in the PR description, `openInNewTab` was only defined as a type but not implemented in the component, so it was unused anyway.

#### aduth on 2025-12-19T17:33:19Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635812250
Path: packages/components/src/notice/types.ts

```diff
@@ -3,27 +3,53 @@
  */
 import type { MouseEventHandler, ReactNode } from 'react';
 
-type CommonNoticeActionProps = {
-	label: string;
+/**
+ * Internal dependencies
+ */
+import type { ButtonAsButtonProps, ButtonAsAnchorProps } from '../button/types';
+
+type CommonNoticeActionProps = Omit<
+	ButtonAsButtonProps,
+	// Omit props that are handled internally or don't apply to notice actions
+	'href' | 'children' | 'className'
+> & {
 	className?: string;
 	noDefaultClasses?: boolean;
-	variant?: 'primary' | 'secondary' | 'link';
 };
-// `url` and `onClick` can both be provided, but `url` takes precedence. If
-// `url` is provided, the action's button will be rendered as an anchor and
-// `onClick` will be ignored.
-type NoticeActionWithURL = CommonNoticeActionProps & {
-	url: string;
-	openInNewTab?: boolean;
```

Interesting! Sorry I missed that in your prior comment.

### Review thread 5 (resolved)

#### aduth on 2025-12-19T17:09:25Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635755569
Path: packages/components/src/notice/types.ts

```diff
@@ -3,27 +3,53 @@
  */
 import type { MouseEventHandler, ReactNode } from 'react';
 
-type CommonNoticeActionProps = {
-	label: string;
+/**
+ * Internal dependencies
+ */
+import type { ButtonAsButtonProps, ButtonAsAnchorProps } from '../button/types';
+
+type CommonNoticeActionProps = Omit<
+	ButtonAsButtonProps,
+	// Omit props that are handled internally or don't apply to notice actions
+	'href' | 'children' | 'className'
+> & {
 	className?: string;
 	noDefaultClasses?: boolean;
-	variant?: 'primary' | 'secondary' | 'link';
 };
-// `url` and `onClick` can both be provided, but `url` takes precedence. If
-// `url` is provided, the action's button will be rendered as an anchor and
-// `onClick` will be ignored.
-type NoticeActionWithURL = CommonNoticeActionProps & {
-	url: string;
-	openInNewTab?: boolean;
-	onClick?: never;
+type AnchorProps = {
+	/**
+	 * If provided with `url` or `href`, sets the `target` attribute to the anchor.
+	 */
+	target?: ButtonAsAnchorProps[ 'target' ];
+	/**
+	 * If provided with `url` or `href`, sets the `rel` attribute to the anchor.
+	 */
+	rel?: string;
+	onClick?: MouseEventHandler< HTMLButtonElement | HTMLAnchorElement >;
 };
+// `url`/`href` and `onClick` can both be provided. If `url`/`href` is provided,
+// the action's button will be rendered as an anchor and `onClick` will still be called.
+type NoticeActionWithURL = CommonNoticeActionProps &
+	AnchorProps & {
+		url: string;
+		href?: never;
+	};
+type NoticeActionWithHref = CommonNoticeActionProps &
+	AnchorProps & {
+		href: string;
+		url?: never;
+	};
```

Is all this extra stuff so that we enforce that someone has to pass one or the other of `url` and `href` but not both? I'm not sure it's worth the extra complexity if so. I'm also curious if we could use `ButtonProps` rather than both `ButtonAsButtonProps` and `ButtonAsAnchorProps`

#### manzoorwanijk on 2025-12-19T17:28:41Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635801989
Path: packages/components/src/notice/types.ts

```diff
@@ -3,27 +3,53 @@
  */
 import type { MouseEventHandler, ReactNode } from 'react';
 
-type CommonNoticeActionProps = {
-	label: string;
+/**
+ * Internal dependencies
+ */
+import type { ButtonAsButtonProps, ButtonAsAnchorProps } from '../button/types';
+
+type CommonNoticeActionProps = Omit<
+	ButtonAsButtonProps,
+	// Omit props that are handled internally or don't apply to notice actions
+	'href' | 'children' | 'className'
+> & {
 	className?: string;
 	noDefaultClasses?: boolean;
-	variant?: 'primary' | 'secondary' | 'link';
 };
-// `url` and `onClick` can both be provided, but `url` takes precedence. If
-// `url` is provided, the action's button will be rendered as an anchor and
-// `onClick` will be ignored.
-type NoticeActionWithURL = CommonNoticeActionProps & {
-	url: string;
-	openInNewTab?: boolean;
-	onClick?: never;
+type AnchorProps = {
+	/**
+	 * If provided with `url` or `href`, sets the `target` attribute to the anchor.
+	 */
+	target?: ButtonAsAnchorProps[ 'target' ];
+	/**
+	 * If provided with `url` or `href`, sets the `rel` attribute to the anchor.
+	 */
+	rel?: string;
+	onClick?: MouseEventHandler< HTMLButtonElement | HTMLAnchorElement >;
 };
+// `url`/`href` and `onClick` can both be provided. If `url`/`href` is provided,
+// the action's button will be rendered as an anchor and `onClick` will still be called.
+type NoticeActionWithURL = CommonNoticeActionProps &
+	AnchorProps & {
+		url: string;
+		href?: never;
+	};
+type NoticeActionWithHref = CommonNoticeActionProps &
+	AnchorProps & {
+		href: string;
+		url?: never;
+	};
```

Yes, it’s to ensure only one is passed when the consumer uses TypeScript. I think it’s fine this way. The current behaviour is retained and it won’t break anything.

### Review thread 6 (resolved)

#### aduth on 2025-12-19T17:10:41Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635759064
Path: packages/components/src/notice/test/index.tsx

```diff
@@ -120,4 +122,164 @@ describe( 'Notice', () => {
 			expect( speak ).toHaveBeenCalledTimes( 1 );
 		} );
 	} );
+
+	describe( 'actions', () => {
+		it( 'should render a disabled action button', () => {
```

These tests might be a bit excessive in testing for all the different supported properties. Really all we care about is that props are passed through, right? So that's 1 test? At least for the ones where we're just looking at what happens with the button. It makes sense for the `should call onClick` regression test to be its own thing.

Same could said for the stories.

#### manzoorwanijk on 2025-12-19T17:34:42Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635815313
Path: packages/components/src/notice/test/index.tsx

```diff
@@ -120,4 +122,164 @@ describe( 'Notice', () => {
 			expect( speak ).toHaveBeenCalledTimes( 1 );
 		} );
 	} );
+
+	describe( 'actions', () => {
+		it( 'should render a disabled action button', () => {
```

Those tests and stories can act as a documentation to showcase how it works?

#### aduth on 2025-12-19T17:53:05Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2635855886
Path: packages/components/src/notice/test/index.tsx

```diff
@@ -120,4 +122,164 @@ describe( 'Notice', () => {
 			expect( speak ).toHaveBeenCalledTimes( 1 );
 		} );
 	} );
+
+	describe( 'actions', () => {
+		it( 'should render a disabled action button', () => {
```

I guess my concern is that this creates additional maintenance burden and potential for drift†, increases the runtime of tests, and doesn't really create much value over communicating that the actions can use any of the props from button and setting the expectation that they're passed through.

† On the topic of drift, there's already drift here because we only cover 5-6 of the available 15+ Button props.

#### manzoorwanijk on 2025-12-21T10:49:39Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2637743146
Path: packages/components/src/notice/test/index.tsx

```diff
@@ -120,4 +122,164 @@ describe( 'Notice', () => {
 			expect( speak ).toHaveBeenCalledTimes( 1 );
 		} );
 	} );
+
+	describe( 'actions', () => {
+		it( 'should render a disabled action button', () => {
```

Thanks. I have cleaned up the tests and stories to consolidate them.

### Review thread 7 (resolved)

#### mirka on 2025-12-24T20:25:31Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2646229895
Path: packages/components/src/notice/index.tsx

```diff
@@ -151,8 +154,12 @@ function Notice( {
 										__next40pxDefaultSize
 										key={ index }
 										href={ url }
+										target={ target }
+										rel={ rel }
 										variant={ computedVariant }
-										onClick={ url ? undefined : onClick }
+										onClick={ onClick }
+										disabled={ disabled }
+										accessibleWhenDisabled
```

Nice 🧑‍🍳👌

### Review thread 8 (resolved)

#### mirka on 2025-12-24T20:28:27Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2646231974
Path: packages/components/src/notice/stories/index.story.tsx

```diff
@@ -141,3 +141,65 @@ export const NoticeListSubcomponent: StoryFn< typeof NoticeList > = () => {
 	);
 };
 NoticeListSubcomponent.storyName = 'NoticeList Subcomponent';
+
+/**
+ * Action buttons can be disabled.
+ */
+export const WithDisabledAction = Template.bind( {} );
+WithDisabledAction.args = {
+	...Default.args,
+	children: 'This notice has a disabled action.',
+	actions: [
+		{
+			label: 'Disabled action',
+			onClick: () => {},
+			disabled: true,
+		},
+		{
+			label: 'Enabled action',
+			onClick: () => {},
+		},
+	],
+};
+
+/**
+ * Links can open in a new tab using `target="_blank"`.
+ */
+export const WithOpenInNewTab = Template.bind( {} );
+WithOpenInNewTab.args = {
+	...Default.args,
+	children: 'This link opens in a new tab.',
+	actions: [
+		{
+			label: 'Learn more',
+			url: 'https://wordpress.org',
+			target: '_blank',
+			rel: 'noreferrer',
+		},
+	],
+};
+
+/**
+ * When both `url` and `onClick` are provided, the action renders as a link
+ * and `onClick` is still called when clicked.
+ */
+export const WithOnClickAndUrl: StoryFn< typeof Notice > = ( props ) => {
```

This story is probably fine to remove since we have a unit test, and it's something that should "just work" rather than a feature that needs to be highlighted.

### Review thread 9 (resolved)

#### mirka on 2025-12-24T20:30:58Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2646233535
Path: packages/components/src/notice/types.ts

```diff
@@ -4,23 +4,46 @@
 import type { MouseEventHandler, ReactNode } from 'react';
 
 type CommonNoticeActionProps = {
+	/**
+	 * The label for the action button.
+	 */
```

Thanks for adding prop descriptions!

### Review thread 10

#### mirka on 2025-12-24T22:19:18Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2646312462
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

Ugh I'm conflicted about this API divergence between Snackbar and Notice now. Snackbar added the feature through a `openInNewTab` prop, and swaps the `Button` for an `ExternalLink` in that case (#69905).

Actually I think that might be preferable to supporting `rel` and `target` directly, because one of the guidelines of the design system is to have consistent cues (visual and accessible) on links that open new tabs. `ExternalLink` has those cues, while `Button` does not.

The problem is that `Snackbar` only has to support `variant="link"` buttons, while `Notice` actions have a wider API surface that conflicts with `ExternalLink`, so the TypeScript/documentation complexity is probably not worth the trouble to make it work polymorphically.

This alone may be a good reason to [support components directly](https://github.com/WordPress/gutenberg/issues/74090#issuecomment-3681296174) in `actions`, for the `@wordpress/ui` version of `Notice`.

Sorry @manzoorwanijk, this aspect was a bit more complicated that expected. We can probably merge this PR faster if we punt this `rel`/`target` forwarding part out of scope 😅 (The type fix here for `openInNewTab` where you moved it out from Notice and into Snackbar is good though.)

#### manzoorwanijk on 2025-12-25T01:38:37Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2646414346
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

The issue here was that `openInNewTab` was only defined in the type but not implemented. So, it was unused anyway.

So, I replaced it with target and rel to have better control.

As you rightly said, here we have to support different variants and thus it makes more sense to have it this way.

#### mirka on 2025-12-25T13:19:52Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2647023829
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

The problem is that:

- We don't really want to allow tab-opening links without the visual/accessible cues like `ExternalLink` has.
- It's not good that `Snackbar` actions and `Notice` actions have different APIs to achieve the same thing.

We can solve these cleanly in a new API in `@wordpress/ui`, but it's hard to do this in `@wordpress/components`.

#### manzoorwanijk on 2025-12-27T09:00:32Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2649041066
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

So what do we want to do here? Maybe we can close this PR, live with what we have for now, and hope to resolve the issues in `@wordpress/ui`?

#### SirLouen on 2025-12-27T23:45:42Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2649384179
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

> It's not good that Snackbar actions and Notice actions have different APIs to achieve the same thing.

Why not replicate then? It shouldn't be too difficult. Maybe something like the Cover Block `cover/shared.js` could work here?

#### manzoorwanijk on 2025-12-28T06:33:46Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2649509826
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

I have updated the implementation to use `ExternalLink` when `openInNewTab` is `true`.

<img width="264" height="104" alt="Screenshot 2025-12-28 at 11 59 16 AM" src="https://github.com/user-attachments/assets/6f7213c4-983b-4c25-b884-450571b09498" />

#### mirka on 2025-12-30T18:27:23Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2653594451
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

Sorry if I was unclear. The main reason it's hard to do this in `@wordpress/components` is the type/documentation complexity it adds:

> The problem is that `Snackbar` only has to support `variant="link"` buttons, while `Notice` actions have a wider API surface that conflicts with `ExternalLink`, so the TypeScript/documentation complexity is probably not worth the trouble to make it work polymorphically.

For example we now have this discriminated union:

```ts
export type NoticeAction = NoticeActionWithURL | NoticeActionWithoutUrl;
```

The components squad was initially quite enthusiastic to do this kind of typing, but in practice, it gave us a lot more trouble than we imagined 😅 It can cause difficulty for consumers, especially those not advanced in TS, when they have to reuse these types for their own components. And no matter how feasible the TS representation is, it's always complicated to translate that into human-facing documentation.

And there are already some inaccuracies in the types, even with the attempted discriminated union, because certain props are not valid for the `ExternalLink` case (e.g. `disabled`, `noDefaultClasses`, `variant`). Just a lot of opportunities for human error and confusion to creep in.

So although I wouldn't go as far as to say these are "blockers", it does make me weigh the cost–benefit critically. It's almost like adding tech debt to a component that's about to be sunsetted. @ciampo or @aduth may have a second opinion.

#### manzoorwanijk on 2025-12-31T05:10:55Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2654816592
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

I agree that those complicated types can sometimes be difficult to deal with, but I am still not sure what we want to do here?

Should we instead allow `Notice` `actions` composition by allowing it to be a `React.ReactNode` along with the current array structure, and then render it directly?

```jsx
<div className="components-notice__actions">
	{ Array.isArray( actions ) ? actions.map(...) : actions }
</div>
```

#### mirka on 2025-12-31T10:42:22Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2655192419
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

Like Marco said in https://github.com/WordPress/gutenberg/issues/74090#issuecomment-3681251227, "quick wins and fixes" are fine, but we don't want to introduce new features that add maintenance/migration cost to these soon-to-be-deprecated componentry.

The `onClick` and `disabled` are quick fixes, but `openInNewTab` turned out to be on the level of a "new feature" that does indeed add to the future cost. So I'd prefer we remove the `openInNewTab` feature from the scope of this PR, given that we can support it cleaner in the new `@wordpress/ui` package.

#### manzoorwanijk on 2025-12-31T10:47:13Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2655198564
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

`openInNewTab` was already defined there (in types) before this PR but was not implemented.

#### mirka on 2025-12-31T11:22:39Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2655242423
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

Yes, it's messed up but let's leave it as is, unimplemented. I haven't looked into whether the types can be corrected easily so a `Notice` action won't accept an `onInNewTab` property, but that doesn't have to be done in this PR if it isn't simple.

#### manzoorwanijk on 2025-12-31T11:34:43Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2655256223
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

Done. Thanks.

#### manzoorwanijk on 2025-12-31T11:38:23Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2655260544
Path: packages/components/src/snackbar/types.ts

```diff
@@ -56,10 +56,11 @@ export type SnackbarProps = Pick<
 		 *
 		 * @default []
 		 */
-		actions?: Pick<
-			NoticeAction,
-			'label' | 'url' | 'onClick' | 'openInNewTab'
-		>[];
+		actions?: Array<
+			Pick< NoticeAction, 'label' | 'url' | 'onClick' > & {
+				openInNewTab?: boolean;
+			}
```

The types for `Notice` are now clean and straightforward.

### Review thread 11 (resolved)

#### manzoorwanijk on 2025-12-25T01:42:44Z

URL: https://github.com/WordPress/gutenberg/pull/74094#discussion_r2646416806
Path: packages/components/src/notice/stories/index.story.tsx

```diff
@@ -141,3 +141,65 @@ export const NoticeListSubcomponent: StoryFn< typeof NoticeList > = () => {
 	);
 };
 NoticeListSubcomponent.storyName = 'NoticeList Subcomponent';
+
+/**
+ * Action buttons can be disabled.
+ */
+export const WithDisabledAction = Template.bind( {} );
+WithDisabledAction.args = {
+	...Default.args,
+	children: 'This notice has a disabled action.',
+	actions: [
+		{
+			label: 'Disabled action',
+			onClick: () => {},
+			disabled: true,
+		},
+		{
+			label: 'Enabled action',
+			onClick: () => {},
+		},
+	],
+};
+
+/**
+ * Links can open in a new tab using `target="_blank"`.
+ */
+export const WithOpenInNewTab = Template.bind( {} );
+WithOpenInNewTab.args = {
+	...Default.args,
+	children: 'This link opens in a new tab.',
+	actions: [
+		{
+			label: 'Learn more',
+			url: 'https://wordpress.org',
+			target: '_blank',
+			rel: 'noreferrer',
+		},
+	],
+};
+
+/**
+ * When both `url` and `onClick` are provided, the action renders as a link
+ * and `onClick` is still called when clicked.
+ */
+export const WithOnClickAndUrl: StoryFn< typeof Notice > = ( props ) => {
+	const [ clickCount, setClickCount ] = useState( 0 );
+
+	return (
+		<Notice
+			{ ...props }
+			actions={ [
+				{
+					label: 'Visit WordPress.org',
+					url: 'https://wordpress.org',
+					onClick: () => setClickCount( ( c ) => c + 1 ),
+					target: '_blank',
+					rel: 'noreferrer',
+				},
+			] }
+		>
+			Click count: { clickCount }
+		</Notice>
+	);
+};
```

```suggestion
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/notice/README.md (MODIFIED, +4/-3)
- packages/components/src/notice/index.tsx (MODIFIED, +4/-1)
- packages/components/src/notice/stories/index.story.tsx (MODIFIED, +20/-0)
- packages/components/src/notice/test/index.tsx (MODIFIED, +52/-1)
- packages/components/src/notice/types.ts (MODIFIED, +25/-18)
- packages/components/src/snackbar/types.ts (MODIFIED, +7/-10)
