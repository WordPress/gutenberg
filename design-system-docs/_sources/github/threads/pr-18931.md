---
number: 18931
type: pull_request
state: MERGED
merged_at: 2020-01-07T16:22:40Z
url: https://github.com/WordPress/gutenberg/pull/18931
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #18931: Components: Improve ToolbarButton

- URL: https://github.com/WordPress/gutenberg/pull/18931
- Author: diegohaz
- Created: 2019-12-05T05:30:08Z
- Updated: 2020-01-20T10:00:18Z
- Comments: 1 of 1
- Review threads: 11 of 11
- Changed files: 13 of 13
- Diff: ../pr-diffs/18931.diff

## Body

## Description

This PR is part of #18619. Please, refer to the issue for more context.

I confess that coming up with a good API here is really challenging, considering all the use cases in Gutenberg. So I'd really appreciate any feedback on that. The main use cases I identified and am trying to address with this PR are:

- `ToolbarButton`

  ```jsx
  import { Toolbar, ToolbarButton } from "@wordpress/components";

  <Toolbar __experimentalAccessibilityLabel="label">
    <ToolbarButton>Button 1</ToolbarButton>
    <ToolbarButton icon="icon">Button 2</ToolbarButton>
  </Toolbar>
  ```

  `Button` supports icons since #19193. `ToolbarButton` passes all the props down to `Button`, so it also supports it.

- `ToolbarItem` (generic headless component for any other kind of toolbar item)

  ```jsx
  import { Toolbar, __experimentalToolbarItem as ToolbarItem } from "@wordpress/components";

  <Toolbar __experimentalAccessibilityLabel="label">
    <ToolbarItem>
      {toggleProps => (
        <DropdownMenu
          icon="icon"
          label="label"
          controls={ ... }
          toggleProps={ toggleProps }
        />
      )}
    </ToolbarItem>
  </Toolbar>
  ```

Eventually, if this is common enough, we can create something like `ToolbarDropdownMenu` to abstract the usage above. But for this first iteration I thought that `Button` and `IconButton` would be enough. The dropdown use case can also use `<ToolbarGroup isCollapsed>`, but some usages in Gutenberg (and in third party blocks, I suppose), can't be easily converted into `ToolbarGroup` due to styling conflicts.

There are other complicated cases like `BlockSwitcher`, which uses `Dropdown` directly with custom configuration. `ToolbarItem` could be used for that as well.

All the new features have been added as `__experimental`.

## How has this been tested?
Check Storybook

## Screenshots

![image](https://user-images.githubusercontent.com/3068563/70255327-601efa00-1765-11ea-874e-65fb772fa0f8.png)

## Types of changes
New feature

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
-  [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->.

## Issue comments

### mtias on 2020-01-07T17:17:41Z

URL: https://github.com/WordPress/gutenberg/pull/18931#issuecomment-571683894

Thanks for all the work here!

## Review threads

### Review thread 1 (resolved)

#### diegohaz on 2019-12-05T07:01:09Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354136549
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -28,48 +23,42 @@ function ToolbarButton( {
 	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
-	const accessibleToolbarState = useContext( ToolbarContext );
-
-	const button = (
-		<IconButton
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-				{ 'is-active': isActive }
-			) }
-			aria-pressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( icon ) {
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<IconButton
+					icon={ icon }
+					label={ title }
+					shortcut={ shortcut }
+					data-subscript={ subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( onClick ) {
+							onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className, {
+						'is-active': isActive,
+					} ) }
+					aria-pressed={ isActive }
```

Should `isActive` be a state on `IconButton` instead of toolbar icon button?

#### gziolo on 2019-12-05T14:36:29Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354347973
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -28,48 +23,42 @@ function ToolbarButton( {
 	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
-	const accessibleToolbarState = useContext( ToolbarContext );
-
-	const button = (
-		<IconButton
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-				{ 'is-active': isActive }
-			) }
-			aria-pressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( icon ) {
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<IconButton
+					icon={ icon }
+					label={ title }
+					shortcut={ shortcut }
+					data-subscript={ subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( onClick ) {
+							onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className, {
+						'is-active': isActive,
+					} ) }
+					aria-pressed={ isActive }
```

See https://github.com/WordPress/gutenberg/pull/17748. I want to make `isPressed` (it's still `isToggled` but we need a different name) part of the `Button` component. In effect, `IconButton` would inherit it.

#### gziolo on 2019-12-05T14:37:31Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354348523
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -28,48 +23,42 @@ function ToolbarButton( {
 	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
-	const accessibleToolbarState = useContext( ToolbarContext );
-
-	const button = (
-		<IconButton
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-				{ 'is-active': isActive }
-			) }
-			aria-pressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( icon ) {
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<IconButton
+					icon={ icon }
+					label={ title }
+					shortcut={ shortcut }
+					data-subscript={ subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( onClick ) {
+							onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className, {
+						'is-active': isActive,
+					} ) }
+					aria-pressed={ isActive }
```

In the similar vein, I'd like to replicate `focusable` and `disabled` fair implemented in Reakit for `Button` :)

I don't like the fact that we use ARIA attributes as of today.

#### diegohaz on 2019-12-05T16:39:01Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354421784
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -28,48 +23,42 @@ function ToolbarButton( {
 	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
-	const accessibleToolbarState = useContext( ToolbarContext );
-
-	const button = (
-		<IconButton
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-				{ 'is-active': isActive }
-			) }
-			aria-pressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( icon ) {
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<IconButton
+					icon={ icon }
+					label={ title }
+					shortcut={ shortcut }
+					data-subscript={ subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( onClick ) {
+							onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className, {
+						'is-active': isActive,
+					} ) }
+					aria-pressed={ isActive }
```

Cool! So, I'll keep that unchanged on this PR.

### Review thread 2 (resolved)

#### diegohaz on 2019-12-05T16:42:03Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354423540
Path: packages/components/src/toolbar-button/style.scss

```diff
@@ -1,3 +1,16 @@
+.components-toolbar-button {
+	padding: 0 9px;
+	border-radius: $radius-round-rectangle;
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
+		@include button-style__hover;
+	}
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active {
+		@include button-style__active;
+	}
+}
+
```

I've added some `IconButton` styles here, since `components-toolbar-button` is just `Button`, which doesn't have the border styles on hover and active states. But maybe that should be a `Button` state that `IconButton` (and therefore any toolbar button) inherits?

#### gziolo on 2019-12-06T06:48:40Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354686098
Path: packages/components/src/toolbar-button/style.scss

```diff
@@ -1,3 +1,16 @@
+.components-toolbar-button {
+	padding: 0 9px;
+	border-radius: $radius-round-rectangle;
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
+		@include button-style__hover;
+	}
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active {
+		@include button-style__active;
+	}
+}
+
```

This is how the toolbar button with text only looks like at the moment:
![Screen Shot 2019-12-06 at 07 45 37](https://user-images.githubusercontent.com/699132/70302087-7330d900-17fc-11ea-9e4c-b02dae93229b.png)

I think it will change in the future given that there is this ongoing exploration of how to redefine the look and behavior of the block toolbar. That's why I would say, as long as it looks the same, it doesn't matter how we approach it :)

#### gziolo on 2019-12-06T13:40:29Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354836542
Path: packages/components/src/toolbar-button/style.scss

```diff
@@ -1,3 +1,16 @@
+.components-toolbar-button {
+	padding: 0 9px;
+	border-radius: $radius-round-rectangle;
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
+		@include button-style__hover;
+	}
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active {
+		@include button-style__active;
+	}
+}
+
```

Well, I nearly removed styles for the toolbar button in #17748.

I brought them back in https://github.com/WordPress/gutenberg/pull/17748/commits/a282f319b05ebf5af597cb839c0e7455c711ddec.

We definitely should find a better way to handle them. I guess your proposal is closer to what we need as it allows to easily style the button without an icon.

#### gziolo on 2019-12-10T15:34:37Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r356107937
Path: packages/components/src/toolbar-button/style.scss

```diff
@@ -1,3 +1,16 @@
+.components-toolbar-button {
+	padding: 0 9px;
+	border-radius: $radius-round-rectangle;
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
+		@include button-style__hover;
+	}
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active {
+		@include button-style__active;
+	}
+}
+
```

I landed #17748 so you might want to update PR with whatever I changed there. It would be nice to get rid of `.components-tab-button` styles in favor of your proposal.

#### gziolo on 2019-12-11T10:56:40Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r356530811
Path: packages/components/src/toolbar-button/style.scss

```diff
@@ -1,3 +1,16 @@
+.components-toolbar-button {
+	padding: 0 9px;
+	border-radius: $radius-round-rectangle;
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
+		@include button-style__hover;
+	}
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active {
+		@include button-style__active;
+	}
+}
+
```

In addition, @youknowriad opened PR which brings more consistency to all types of buttons in https://github.com/WordPress/gutenberg/pull/19058.

#### diegohaz on 2019-12-15T11:41:10Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r357972592
Path: packages/components/src/toolbar-button/style.scss

```diff
@@ -1,3 +1,16 @@
+.components-toolbar-button {
+	padding: 0 9px;
+	border-radius: $radius-round-rectangle;
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
+		@include button-style__hover;
+	}
+
+	&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active {
+		@include button-style__active;
+	}
+}
+
```

I removed these styles since they've been already added to `Button` on #19058

This is a generic toolbar button. If we abstract the tab button into a new component (`Tab`, `TabButton`?), we may have `ToolbarTab`, `ToolbarTabButton`.

### Review thread 3 (resolved)

#### gziolo on 2019-12-06T07:05:32Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r354689873
Path: packages/components/src/toolbar-icon-button/index.js

```diff
@@ -0,0 +1,29 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarItem from '../toolbar-item';
+import IconButton from '../icon-button';
+
+function ToolbarIconButton( props, ref ) {
```

Do we really need to have this distinction between the button and the icon button? I'm aware that there are some legacy considerations to take into account but it probably would be easier to maintain only one component moving forward.

See the `Button` & `IconButton` audit https://github.com/WordPress/gutenberg/issues/16541 and the following part in particular:

> ### Two options for Button and IconButton:
>
> **1. Keep Button and IconButton separate components**
> 
> Now that it seems like both components will have text and icon options in the component, here would be the main differences:
> 
> They handle the text label differently. Button is primarily for text (with an option for a supplementary icon) and IconButton is primarily for an Icon (with an option for supplementary text).
> For IconButton, text is hidden by default. When text is shown, it’s centered underneath the icon. When text is not shown, it's visible as a hover tooltip. There's always an icon shown.
> For Button, text is shown by default. There's always text shown. When an icon is shown, it's aligned to the left. There's no need for a tooltip because the text is always shown.
> We simplify the components by automatically handling the text label. The label is always the same whether it's shown or not. The components automatically handles the tooltip.
> We can consider renaming Button to TextButton.
> 
> **2. Combine Button and IconButton**
> 
> It could be a simpler experience for designers/developers if we combined the two components. Here is an example of a [component library](https://mineral-ui.com/components/button) that does this.
> 
> The main advantage is that the user of the system won't have to figure out which component to use, since it's all-in-one. The disadvantage is that it might be complex figuring out the different props, and the combination of certain props (e.g. isPrimary + isIconOnly). We might have to split the two components for technical reasons.

 @mtias @davewhitley

#### diegohaz on 2019-12-15T11:38:49Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r357972471
Path: packages/components/src/toolbar-icon-button/index.js

```diff
@@ -0,0 +1,29 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarItem from '../toolbar-item';
+import IconButton from '../icon-button';
+
+function ToolbarIconButton( props, ref ) {
```

Ideally, if `Button` supports both text and icon button, `ToolbarButton` should inherit that and we can get rid of this `__experimentalToolbarIconButton`.

#### diegohaz on 2019-12-17T10:33:30Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r358714846
Path: packages/components/src/toolbar-icon-button/index.js

```diff
@@ -0,0 +1,29 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarItem from '../toolbar-item';
+import IconButton from '../icon-button';
+
+function ToolbarIconButton( props, ref ) {
```

Linking related PR: #19193

#### diegohaz on 2019-12-26T03:05:00Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r361357075
Path: packages/components/src/toolbar-icon-button/index.js

```diff
@@ -0,0 +1,29 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarItem from '../toolbar-item';
+import IconButton from '../icon-button';
+
+function ToolbarIconButton( props, ref ) {
```

Removed `ToolbarIconButton` thanks to #19299 and #19193. `ToolbarButton` should be used instead.

### Review thread 4 (resolved)

#### gziolo on 2019-12-19T12:22:35Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r359833402
Path: packages/components/src/toolbar-item/index.js

```diff
@@ -0,0 +1,36 @@
+/**
+ * External dependencies
+ */
+import { useToolbarItem } from 'reakit/Toolbar';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarContext from '../toolbar-context';
+
+function ToolbarItem( { children, ...props }, ref ) {
+	const accessibleToolbarState = useContext( ToolbarContext );
+	// https://reakit.io/docs/composition/#props-hooks
+	const itemProps = useToolbarItem( accessibleToolbarState, { ...props, ref } );
+
+	if ( typeof children !== 'function' ) {
+		// eslint-disable-next-line no-console
+		console.warn( '`ToolbarItem` is a generic headless component that accepts only function children props' );
```

We need to introduce a more robust way of handling developer warnings. It's something for separate PR but this one reminds me about it again :)

Ideally, we port what Reakit uses or React internally.

#### diegohaz on 2019-12-19T12:28:47Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r359835573
Path: packages/components/src/toolbar-item/index.js

```diff
@@ -0,0 +1,36 @@
+/**
+ * External dependencies
+ */
+import { useToolbarItem } from 'reakit/Toolbar';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarContext from '../toolbar-context';
+
+function ToolbarItem( { children, ...props }, ref ) {
+	const accessibleToolbarState = useContext( ToolbarContext );
+	// https://reakit.io/docs/composition/#props-hooks
+	const itemProps = useToolbarItem( accessibleToolbarState, { ...props, ref } );
+
+	if ( typeof children !== 'function' ) {
+		// eslint-disable-next-line no-console
+		console.warn( '`ToolbarItem` is a generic headless component that accepts only function children props' );
```

That's on my radar! Will work on that later.

#### diegohaz on 2019-12-24T04:26:16Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r361061320
Path: packages/components/src/toolbar-item/index.js

```diff
@@ -0,0 +1,36 @@
+/**
+ * External dependencies
+ */
+import { useToolbarItem } from 'reakit/Toolbar';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarContext from '../toolbar-context';
+
+function ToolbarItem( { children, ...props }, ref ) {
+	const accessibleToolbarState = useContext( ToolbarContext );
+	// https://reakit.io/docs/composition/#props-hooks
+	const itemProps = useToolbarItem( accessibleToolbarState, { ...props, ref } );
+
+	if ( typeof children !== 'function' ) {
+		// eslint-disable-next-line no-console
+		console.warn( '`ToolbarItem` is a generic headless component that accepts only function children props' );
```

Opened a PR #19317

#### gziolo on 2019-12-24T21:33:02Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r361225882
Path: packages/components/src/toolbar-item/index.js

```diff
@@ -0,0 +1,36 @@
+/**
+ * External dependencies
+ */
+import { useToolbarItem } from 'reakit/Toolbar';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useContext } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import ToolbarContext from '../toolbar-context';
+
+function ToolbarItem( { children, ...props }, ref ) {
+	const accessibleToolbarState = useContext( ToolbarContext );
+	// https://reakit.io/docs/composition/#props-hooks
+	const itemProps = useToolbarItem( accessibleToolbarState, { ...props, ref } );
+
+	if ( typeof children !== 'function' ) {
+		// eslint-disable-next-line no-console
+		console.warn( '`ToolbarItem` is a generic headless component that accepts only function children props' );
```

Awesome, thanks for doing it. I commented on the PR, it looks great. I just left some thoughts about the integration part, which is something we just need to figure out and pick the best way to move forward for 3rd party developers using this new package in their own projects.

### Review thread 5

#### diegohaz on 2019-12-26T02:58:51Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r361356394
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
```

Removed most of the props here so we can spread `{ ...props }` directly into `Button`. This way, `ToolbarButton` (when used within `<Toolbar __experimentalAccessibilityLabel="label">`) and `Button` have identical API.

### Review thread 6

#### diegohaz on 2019-12-26T03:01:25Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r361356675
Path: packages/components/src/toolbar/test/index.js

```diff
@@ -14,8 +14,8 @@ describe( 'Toolbar', () => {
 		it( 'should render a toolbar with toolbar buttons', () => {
 			const wrapper = mount(
 				<Toolbar __experimentalAccessibilityLabel="blocks">
-					<ToolbarButton title="control1" />
-					<ToolbarButton title="control2" />
+					<ToolbarButton label="control1" />
+					<ToolbarButton label="control2" />
```

When used within `<Toolbar __experimentalAccessibilityLabel>`, `ToolbarButton` passes all the props down to `Button` so they have the same API.

#### gziolo on 2020-01-03T10:28:42Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362763602
Path: packages/components/src/toolbar/test/index.js

```diff
@@ -14,8 +14,8 @@ describe( 'Toolbar', () => {
 		it( 'should render a toolbar with toolbar buttons', () => {
 			const wrapper = mount(
 				<Toolbar __experimentalAccessibilityLabel="blocks">
-					<ToolbarButton title="control1" />
-					<ToolbarButton title="control2" />
+					<ToolbarButton label="control1" />
+					<ToolbarButton label="control2" />
```

Nice 👍

### Review thread 7

#### gziolo on 2020-01-03T10:30:48Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362764104
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
```

I will investigate in a follow-up whether we can get rid of `subscript` as I think we no longer use it in code and it probably was never meant to exist as public API.

#### diegohaz on 2020-01-06T14:08:19Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363307500
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
```

I kept it there so as to avoid breaking anything, but it's not being used in the new implementation (using `ToolbarItem`) anyway.

### Review thread 8

#### gziolo on 2020-01-03T10:39:41Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362766472
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
```

Does it mean that `title` should be deprecated in favor of `label`?

#### diegohaz on 2020-01-06T14:16:51Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363310919
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
```

I'd say that these props should be deprecated:

- `title` (in favor of `label`)
- `isActive` (in favor of `isPressed`)
- `isDisabled` (in favor of `disabled`)

Still not sure what to do with `isDisabled` though.

This PR only changes the behavior of `ToolbarButton` when it's used inside `<Toolbar __experimentalAccessibilityLabel>`, so if we decide to deprecate those props, I guess it should be in another PR.

#### gziolo on 2020-01-07T10:50:07Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363692453
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
```

I like the idea, let's go this way in the follow-up PRs.

### Review thread 9

#### gziolo on 2020-01-03T10:40:27Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362766686
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( props.onClick ) {
+							props.onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className ) }
+					isPressed={ props.isActive }
```

A similar question, should we align API and deprecated `isActive` in favor of `isPressed`?

#### gziolo on 2020-01-03T11:03:37Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362772960
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( props.onClick ) {
+							props.onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className ) }
+					isPressed={ props.isActive }
```

In stories, I still can see `isActive`:
![Screen Shot 2020-01-03 at 12 03 10](https://user-images.githubusercontent.com/699132/71720169-0c193b00-2e21-11ea-9f65-a6bd085a75ac.png)

#### diegohaz on 2020-01-06T14:19:52Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363311878
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( props.onClick ) {
+							props.onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className ) }
+					isPressed={ props.isActive }
```

That's the current API. This PR only changes `ToolbarButton` when used within `<Toolbar __experimentalAccessibilityLabel>`.

#### gziolo on 2020-01-07T10:51:05Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363692817
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( props.onClick ) {
+							props.onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className ) }
+					isPressed={ props.isActive }
```

Right, we will need to align it at some point with the Button component as discussed.

### Review thread 10

#### gziolo on 2020-01-03T10:43:02Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362767365
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( props.onClick ) {
+							props.onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className ) }
+					isPressed={ props.isActive }
+					disabled={ props.isDisabled }
```

I think this was discussed in other PRs, we should make a final call on whether we use `isDisabled` in all places instead of `disabled` to potentially break the direct association with the HTML attribute.

/cc @youknowriad @aduth

#### diegohaz on 2020-01-06T14:22:29Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363312788
Path: packages/components/src/toolbar-button/index.js

```diff
@@ -12,63 +12,55 @@ import { useContext } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../button';
+import ToolbarItem from '../toolbar-item';
 import ToolbarContext from '../toolbar-context';
-import AccessibleToolbarButtonContainer from './accessible-toolbar-button-container';
 import ToolbarButtonContainer from './toolbar-button-container';
 
 function ToolbarButton( {
 	containerClassName,
-	icon,
-	title,
-	shortcut,
-	subscript,
-	onClick,
 	className,
-	isActive,
-	isDisabled,
 	extraProps,
 	children,
+	...props
 } ) {
-	// It'll contain state if `ToolbarButton` is being used within
-	// `<Toolbar __experimentalAccessibilityLabel="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const button = (
-		<Button
-			icon={ icon }
-			label={ title }
-			shortcut={ shortcut }
-			data-subscript={ subscript }
-			onClick={ ( event ) => {
-				event.stopPropagation();
-				if ( onClick ) {
-					onClick( event );
-				}
-			} }
-			className={ classnames(
-				'components-toolbar__control',
-				className,
-			) }
-			isPressed={ isActive }
-			disabled={ isDisabled }
-			{ ...extraProps }
-		/>
-	);
-
-	if ( accessibleToolbarState ) {
+	if ( ! accessibleToolbarState ) {
+		// This should be deprecated when <Toolbar __experimentalAccessibilityLabel="label">
+		// becomes stable.
 		return (
-			<AccessibleToolbarButtonContainer className={ containerClassName }>
-				{ button }
-			</AccessibleToolbarButtonContainer>
+			<ToolbarButtonContainer className={ containerClassName }>
+				<Button
+					icon={ props.icon }
+					label={ props.title }
+					shortcut={ props.shortcut }
+					data-subscript={ props.subscript }
+					onClick={ ( event ) => {
+						event.stopPropagation();
+						if ( props.onClick ) {
+							props.onClick( event );
+						}
+					} }
+					className={ classnames( 'components-toolbar__control', className ) }
+					isPressed={ props.isActive }
+					disabled={ props.isDisabled }
```

I think it makes sense to adopt `isDisabled`. Specially if #19337 is merged as it'll not be the same as the HTML `disabled` prop.

### Review thread 11

#### gziolo on 2020-01-03T11:11:11Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r362774746
Path: packages/components/src/toolbar/stories/index.js

```diff
@@ -20,22 +27,30 @@ export const _default = () => {
 		// id is required for server side rendering
 		<Toolbar __experimentalAccessibilityLabel="Options" id="options-toolbar">
 			<ToolbarGroup>
-				<ToolbarButton icon="editor-paragraph" title="Paragraph" />
+				<ToolbarButton icon="editor-paragraph" label="Paragraph" />
+			</ToolbarGroup>
+			<ToolbarGroup>
```

Is there any difference between this and:

![Screen Shot 2020-01-03 at 12 09 57](https://user-images.githubusercontent.com/699132/71720424-fc4e2680-2e21-11ea-9cb6-4486d1fc8200.png)

It looks like we have the collapsed group case covered :)

#### diegohaz on 2020-01-06T14:29:55Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363315893
Path: packages/components/src/toolbar/stories/index.js

```diff
@@ -20,22 +27,30 @@ export const _default = () => {
 		// id is required for server side rendering
 		<Toolbar __experimentalAccessibilityLabel="Options" id="options-toolbar">
 			<ToolbarGroup>
-				<ToolbarButton icon="editor-paragraph" title="Paragraph" />
+				<ToolbarButton icon="editor-paragraph" label="Paragraph" />
+			</ToolbarGroup>
+			<ToolbarGroup>
```

There are some cases in Gutenberg where we're using `DropdownMenu` directly instead `<ToolbarGroup isCollapsed>`. For example: https://github.com/WordPress/gutenberg/blob/2c1e3be7d1cec76176db6cccee1c6add93cc3e4d/packages/block-editor/src/components/block-settings-menu/index.js#L59-L66

I'm not sure how easy is it to refactor those edge cases to use `<ToolbarGroup isCollapsed>`, so I just wanted to make sure  `ToolbarItem` can be used.

#### gziolo on 2020-01-07T10:46:02Z

URL: https://github.com/WordPress/gutenberg/pull/18931#discussion_r363690803
Path: packages/components/src/toolbar/stories/index.js

```diff
@@ -20,22 +27,30 @@ export const _default = () => {
 		// id is required for server side rendering
 		<Toolbar __experimentalAccessibilityLabel="Options" id="options-toolbar">
 			<ToolbarGroup>
-				<ToolbarButton icon="editor-paragraph" title="Paragraph" />
+				<ToolbarButton icon="editor-paragraph" label="Paragraph" />
+			</ToolbarGroup>
+			<ToolbarGroup>
```

Cool, it all makes sense. I think I mentioned it already, I just wanted to ensure you are aware of the current state of the art :)

## Changed files

- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/index.native.js (MODIFIED, +1/-0)
- packages/components/src/toolbar-button/accessible-toolbar-button-container.js (DELETED, +0/-26)
- packages/components/src/toolbar-button/accessible-toolbar-button-container.native.js (DELETED, +0/-6)
- packages/components/src/toolbar-button/index.js (MODIFIED, +33/-41)
- packages/components/src/toolbar-group/index.js (MODIFIED, +3/-5)
- packages/components/src/toolbar-group/toolbar-group-collapsed.js (MODIFIED, +3/-21)
- packages/components/src/toolbar-group/toolbar-group-collapsed.native.js (MODIFIED, +2/-10)
- packages/components/src/toolbar-item/index.js (ADDED, +36/-0)
- packages/components/src/toolbar-item/index.native.js (ADDED, +16/-0)
- packages/components/src/toolbar/stories/index.js (MODIFIED, +37/-19)
- packages/components/src/toolbar/test/index.js (MODIFIED, +2/-2)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +177/-173)
