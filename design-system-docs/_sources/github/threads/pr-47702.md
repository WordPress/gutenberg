---
number: 47702
type: pull_request
state: MERGED
merged_at: 2023-03-02T20:14:28Z
url: https://github.com/WordPress/gutenberg/pull/47702
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #47702: PanelBody: Convert to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/47702
- Author: brookewp
- Created: 2023-02-02T17:17:07Z
- Updated: 2023-03-02T20:14:54Z
- Comments: 5 of 5
- Review threads: 18 of 18
- Changed files: 8 of 8
- Diff: ../pr-diffs/47702.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Refactor the `PanelBody` component to TypeScript following Panel: https://github.com/WordPress/gutenberg/pull/47259

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
Part of the refactor to Typescript: https://github.com/WordPress/gutenberg/issues/35744

## How?
Followed the steps in the [TypeScript migration guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md?rgh-link-date=2022-11-29T14%3A45%3A34Z#refactoring-a-component-to-typescript)

## Testing Instructions

1. Ensure all tests pass
2. `run npm storybook:dev` and check the docs for `PanelBody`

## Issue comments

### ciampo on 2023-02-02T19:10:30Z

URL: https://github.com/WordPress/gutenberg/pull/47702#issuecomment-1414231957

PS — now that https://github.com/WordPress/gutenberg/pull/47259 is merged, you should be able to rebase on top of `trunk` (or merge `trunk` into this branch, whichever option you prefer) in order to get the latest TypeScript types for the rest of the `Panel` component and remove a lot of duplicated code changes

### github-actions on 2023-02-07T04:07:27Z

URL: https://github.com/WordPress/gutenberg/pull/47702#issuecomment-1420170197

**Size Change:** -9 B (0%) 

**Total Size:** 1.34 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/components/index.min.js` | 207 kB | -9 B (0%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 993 B |
| `build/annotations/index.min.js` | 2.78 kB |
| `build/api-fetch/index.min.js` | 2.27 kB |
| `build/autop/index.min.js` | 2.15 kB |
| `build/blob/index.min.js` | 483 B |
| `build/block-directory/index.min.js` | 7.2 kB |
| `build/block-directory/style-rtl.css` | 1.04 kB |
| `build/block-directory/style.css` | 1.04 kB |
| `build/block-editor/content-rtl.css` | 4.11 kB |
| `build/block-editor/content.css` | 4.1 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 403 B |
| `build/block-editor/default-editor-styles.css` | 403 B |
| `build/block-editor/index.min.js` | 196 kB |
| `build/block-editor/style-rtl.css` | 14.4 kB |
| `build/block-editor/style.css` | 14.4 kB |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 138 B |
| `build/block-library/blocks/audio/theme.css` | 138 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 91 B |
| `build/block-library/blocks/avatar/style.css` | 91 B |
| `build/block-library/blocks/block/editor-rtl.css` | 305 B |
| `build/block-library/blocks/block/editor.css` | 305 B |
| `build/block-library/blocks/button/editor-rtl.css` | 587 B |
| `build/block-library/blocks/button/editor.css` | 587 B |
| `build/block-library/blocks/button/style-rtl.css` | 628 B |
| `build/block-library/blocks/button/style.css` | 627 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 84 B |
| `build/block-library/blocks/categories/editor.css` | 83 B |
| `build/block-library/blocks/categories/style-rtl.css` | 100 B |
| `build/block-library/blocks/categories/style.css` | 100 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 406 B |
| `build/block-library/blocks/columns/style.css` | 406 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 612 B |
| `build/block-library/blocks/cover/editor.css` | 613 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.57 kB |
| `build/block-library/blocks/cover/style.css` | 1.56 kB |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 138 B |
| `build/block-library/blocks/embed/theme.css` | 138 B |
| `build/block-library/blocks/file/editor-rtl.css` | 300 B |
| `build/block-library/blocks/file/editor.css` | 300 B |
| `build/block-library/blocks/file/style-rtl.css` | 265 B |
| `build/block-library/blocks/file/style.css` | 265 B |
| `build/block-library/blocks/file/view.min.js` | 353 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.44 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.44 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 984 B |
| `build/block-library/blocks/gallery/editor.css` | 988 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.55 kB |
| `build/block-library/blocks/gallery/style.css` | 1.55 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 654 B |
| `build/block-library/blocks/group/editor.css` | 654 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 76 B |
| `build/block-library/blocks/heading/style.css` | 76 B |
| `build/block-library/blocks/html/editor-rtl.css` | 332 B |
| `build/block-library/blocks/html/editor.css` | 333 B |
| `build/block-library/blocks/image/editor-rtl.css` | 830 B |
| `build/block-library/blocks/image/editor.css` | 829 B |
| `build/block-library/blocks/image/style-rtl.css` | 652 B |
| `build/block-library/blocks/image/style.css` | 652 B |
| `build/block-library/blocks/image/theme-rtl.css` | 137 B |
| `build/block-library/blocks/image/theme.css` | 137 B |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 357 B |
| `build/block-library/blocks/latest-comments/style.css` | 357 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 213 B |
| `build/block-library/blocks/latest-posts/editor.css` | 212 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 478 B |
| `build/block-library/blocks/latest-posts/style.css` | 478 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 507 B |
| `build/block-library/blocks/media-text/style.css` | 505 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 716 B |
| `build/block-library/blocks/navigation-link/editor.css` | 715 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.13 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.14 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.22 kB |
| `build/block-library/blocks/navigation/style.css` | 2.2 kB |
| `build/block-library/blocks/navigation/view-modal.min.js` | 2.81 kB |
| `build/block-library/blocks/navigation/view.min.js` | 447 B |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 376 B |
| `build/block-library/blocks/page-list/editor.css` | 376 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 174 B |
| `build/block-library/blocks/paragraph/editor.css` | 174 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 279 B |
| `build/block-library/blocks/paragraph/style.css` | 281 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 501 B |
| `build/block-library/blocks/post-comments-form/style.css` | 501 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 71 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 71 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 134 B |
| `build/block-library/blocks/post-excerpt/style.css` | 134 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 586 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 584 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 318 B |
| `build/block-library/blocks/post-featured-image/style.css` | 318 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 153 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 153 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 282 B |
| `build/block-library/blocks/post-template/style.css` | 282 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 103 B |
| `build/block-library/blocks/preformatted/style.css` | 103 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 326 B |
| `build/block-library/blocks/pullquote/style.css` | 325 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 167 B |
| `build/block-library/blocks/pullquote/theme.css` | 167 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 288 B |
| `build/block-library/blocks/query-pagination/style.css` | 284 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 463 B |
| `build/block-library/blocks/query/editor.css` | 463 B |
| `build/block-library/blocks/quote/style-rtl.css` | 222 B |
| `build/block-library/blocks/quote/style.css` | 222 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 149 B |
| `build/block-library/blocks/rss/editor.css` | 149 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 165 B |
| `build/block-library/blocks/search/editor.css` | 165 B |
| `build/block-library/blocks/search/style-rtl.css` | 409 B |
| `build/block-library/blocks/search/style.css` | 406 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 234 B |
| `build/block-library/blocks/separator/style.css` | 234 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 474 B |
| `build/block-library/blocks/shortcode/editor.css` | 474 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 489 B |
| `build/block-library/blocks/site-logo/editor.css` | 489 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 203 B |
| `build/block-library/blocks/site-logo/style.css` | 203 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 116 B |
| `build/block-library/blocks/site-title/editor.css` | 116 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 57 B |
| `build/block-library/blocks/site-title/style.css` | 57 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 184 B |
| `build/block-library/blocks/social-link/editor.css` | 184 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 674 B |
| `build/block-library/blocks/social-links/editor.css` | 673 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.4 kB |
| `build/block-library/blocks/social-links/style.css` | 1.39 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 332 B |
| `build/block-library/blocks/spacer/editor.css` | 332 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 433 B |
| `build/block-library/blocks/table/editor.css` | 433 B |
| `build/block-library/blocks/table/style-rtl.css` | 651 B |
| `build/block-library/blocks/table/style.css` | 650 B |
| `build/block-library/blocks/table/theme-rtl.css` | 157 B |
| `build/block-library/blocks/table/theme.css` | 157 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 251 B |
| `build/block-library/blocks/tag-cloud/style.css` | 253 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 404 B |
| `build/block-library/blocks/template-part/editor.css` | 404 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 179 B |
| `build/block-library/blocks/video/style.css` | 179 B |
| `build/block-library/blocks/video/theme-rtl.css` | 139 B |
| `build/block-library/blocks/video/theme.css` | 139 B |
| `build/block-library/classic-rtl.css` | 179 B |
| `build/block-library/classic.css` | 179 B |
| `build/block-library/common-rtl.css` | 1.11 kB |
| `build/block-library/common.css` | 1.11 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 11.6 kB |
| `build/block-library/editor.css` | 11.6 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/index.min.js` | 201 kB |
| `build/block-library/reset-rtl.css` | 478 B |
| `build/block-library/reset.css` | 478 B |
| `build/block-library/style-rtl.css` | 12.7 kB |
| `build/block-library/style.css` | 12.7 kB |
| `build/block-library/theme-rtl.css` | 698 B |
| `build/block-library/theme.css` | 703 B |
| `build/block-serialization-default-parser/index.min.js` | 1.13 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.83 kB |
| `build/blocks/index.min.js` | 51 kB |
| `build/components/style-rtl.css` | 11.7 kB |
| `build/components/style.css` | 11.7 kB |
| `build/compose/index.min.js` | 12.4 kB |
| `build/core-data/index.min.js` | 16.2 kB |
| `build/customize-widgets/index.min.js` | 12.2 kB |
| `build/customize-widgets/style-rtl.css` | 1.41 kB |
| `build/customize-widgets/style.css` | 1.41 kB |
| `build/data-controls/index.min.js` | 663 B |
| `build/data/index.min.js` | 8.58 kB |
| `build/date/index.min.js` | 40.4 kB |
| `build/deprecated/index.min.js` | 518 B |
| `build/dom-ready/index.min.js` | 336 B |
| `build/dom/index.min.js` | 4.72 kB |
| `build/edit-post/classic-rtl.css` | 571 B |
| `build/edit-post/classic.css` | 571 B |
| `build/edit-post/index.min.js` | 34.8 kB |
| `build/edit-post/style-rtl.css` | 7.53 kB |
| `build/edit-post/style.css` | 7.52 kB |
| `build/edit-site/index.min.js` | 63.8 kB |
| `build/edit-site/style-rtl.css` | 10.1 kB |
| `build/edit-site/style.css` | 10.1 kB |
| `build/edit-widgets/index.min.js` | 17.3 kB |
| `build/edit-widgets/style-rtl.css` | 4.55 kB |
| `build/edit-widgets/style.css` | 4.55 kB |
| `build/editor/index.min.js` | 45.7 kB |
| `build/editor/style-rtl.css` | 3.54 kB |
| `build/editor/style.css` | 3.53 kB |
| `build/element/index.min.js` | 4.95 kB |
| `build/escape-html/index.min.js` | 548 B |
| `build/format-library/index.min.js` | 7.27 kB |
| `build/format-library/style-rtl.css` | 557 B |
| `build/format-library/style.css` | 556 B |
| `build/hooks/index.min.js` | 1.66 kB |
| `build/html-entities/index.min.js` | 454 B |
| `build/i18n/index.min.js` | 3.79 kB |
| `build/is-shallow-equal/index.min.js` | 535 B |
| `build/keyboard-shortcuts/index.min.js` | 1.79 kB |
| `build/keycodes/index.min.js` | 1.94 kB |
| `build/list-reusable-blocks/index.min.js` | 2.14 kB |
| `build/list-reusable-blocks/style-rtl.css` | 865 B |
| `build/list-reusable-blocks/style.css` | 865 B |
| `build/media-utils/index.min.js` | 2.99 kB |
| `build/notices/index.min.js` | 977 B |
| `build/plugins/index.min.js` | 1.95 kB |
| `build/preferences-persistence/index.min.js` | 2.23 kB |
| `build/preferences/index.min.js` | 1.35 kB |
| `build/primitives/index.min.js` | 960 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/private-apis/index.min.js` | 937 B |
| `build/react-i18n/index.min.js` | 702 B |
| `build/react-refresh-entry/index.min.js` | 8.44 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.75 kB |
| `build/reusable-blocks/index.min.js` | 2.26 kB |
| `build/reusable-blocks/style-rtl.css` | 265 B |
| `build/reusable-blocks/style.css` | 265 B |
| `build/rich-text/index.min.js` | 10.8 kB |
| `build/server-side-render/index.min.js` | 2.09 kB |
| `build/shortcode/index.min.js` | 1.52 kB |
| `build/style-engine/index.min.js` | 1.53 kB |
| `build/token-list/index.min.js` | 650 B |
| `build/url/index.min.js` | 3.69 kB |
| `build/vendors/inert-polyfill.min.js` | 2.48 kB |
| `build/vendors/react-dom.min.js` | 41.8 kB |
| `build/vendors/react.min.js` | 4.02 kB |
| `build/viewport/index.min.js` | 1.09 kB |
| `build/warning/index.min.js` | 280 B |
| `build/widgets/index.min.js` | 7.31 kB |
| `build/widgets/style-rtl.css` | 1.18 kB |
| `build/widgets/style.css` | 1.18 kB |
| `build/wordcount/index.min.js` | 1.06 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2023-02-07T04:23:49Z

URL: https://github.com/WordPress/gutenberg/pull/47702#issuecomment-1420181645

<!-- flaky-tests-report-comment -->
**Flaky tests detected in ecbb8faf81416e8eaee2d5bc26a7cd48d39ef00e.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/4317279213
📝  Reported issues:
- #48033 in `/test/e2e/specs/editor/blocks/navigation.spec.js`
- #44462 in `specs/site-editor/global-styles-sidebar.test.js`

### ciampo on 2023-03-02T07:52:43Z

URL: https://github.com/WordPress/gutenberg/pull/47702#issuecomment-1451439049

🚀

### tyxla on 2023-03-02T09:11:57Z

URL: https://github.com/WordPress/gutenberg/pull/47702#issuecomment-1451537387

Thank you for the fantastic work here, @brookewp 🙌

## Review threads

### Review thread 1 (resolved)

#### brookewp on 2023-02-02T17:25:25Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1094842423
Path: packages/components/src/panel/types.ts

```diff
@@ -0,0 +1,98 @@
+export type PanelProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Text will be rendered inside an `<h2>` tag.
+	 */
+	header?: PanelHeaderProps[ 'label' ];
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelHeaderProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Will be rendered in an `<h2>` tag.
+	 */
+	label?: string;
+	/**
+	 * The content to display within the panel header.
+	 */
+	children?: React.ReactNode;
+};
+
+export type PanelRowProps = {
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel row.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {};
+	 */
+	buttonProps?: object;
```

I have this as an object for now, but I believe I'll need to import the types from Button. I'm not sure how to specify it'll need the `BaseButtonProps` and not the `AnchorProps` in the union type `ButtonProps`. From what I understand, it looks like it should check for the tag so not sure why it's being recognized as an anchor. 

But I might be misinterpreting the error related to `Button`:

```
Type '{ children: (string | Element | undefined)[]; onClick: (event: MouseEvent) => void; className: string; slot?: string | undefined; style?: CSSProperties | undefined; ... 255 more ...; ref: ForwardedRef<...>; }' is not assignable to type 'IntrinsicAttributes & (ButtonProps & RefAttributes<any>)'.
  Property 'href' is missing in type '{ children: (string | Element | undefined)[]; onClick: (event: MouseEvent) => void; className: string; slot?: string | undefined; style?: CSSProperties | undefined; ... 255 more ...; ref: ForwardedRef<...>; }' but required in type 'AnchorProps'.
``` 

Perhaps @mirka or @ciampo or @chad1008 could point me in the right direction?

#### ciampo on 2023-02-02T19:06:50Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1094959137
Path: packages/components/src/panel/types.ts

```diff
@@ -0,0 +1,98 @@
+export type PanelProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Text will be rendered inside an `<h2>` tag.
+	 */
+	header?: PanelHeaderProps[ 'label' ];
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelHeaderProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Will be rendered in an `<h2>` tag.
+	 */
+	label?: string;
+	/**
+	 * The content to display within the panel header.
+	 */
+	children?: React.ReactNode;
+};
+
+export type PanelRowProps = {
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel row.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {};
+	 */
+	buttonProps?: object;
```

Hey! here's a few things that I noticed so far that may help you move forward a little

1. Why are we [typing the `PanelBodyTitle` props](https://github.com/WordPress/gutenberg/blob/44ec4975e1beba42237b7d67d68819426cca3bb6/packages/components/src/panel/body.tsx#L106-L107) using both `PanelBodyTitleProps` and `PanelBodyProps` ? `PanelBodyProps` seems extra. If you intended to use any props from `PanelBodyProps` in `PanelBodyTitle`, you should refactor the types in types.ts and only pick the ones that are needed
2. I noticed that you typed [`buttonProps` as `object`](https://github.com/WordPress/gutenberg/blob/44ec4975e1beba42237b7d67d68819426cca3bb6/packages/components/src/panel/types.ts):
  - generally the `object` type is not used in TypeScript. We prefer to use [the `Record` type](https://www.typescriptlang.org/docs/handbook/utility-types.html#recordkeys-type)
  - for this specific case, we should probably use [the `ButtonProps` type ](https://github.com/WordPress/gutenberg/blob/c160a7f2602b080b3cd55ee3e72e1825f23c4b42/packages/components/src/button/types.ts#L13)from the `Button` component


I hope this can help you a little bit! Feel free to ask help again to either me or the rest of the team, I'm sure we'll be able to help :)

#### brookewp on 2023-02-15T02:21:03Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106579421
Path: packages/components/src/panel/types.ts

```diff
@@ -0,0 +1,98 @@
+export type PanelProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Text will be rendered inside an `<h2>` tag.
+	 */
+	header?: PanelHeaderProps[ 'label' ];
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelHeaderProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Will be rendered in an `<h2>` tag.
+	 */
+	label?: string;
+	/**
+	 * The content to display within the panel header.
+	 */
+	children?: React.ReactNode;
+};
+
+export type PanelRowProps = {
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel row.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {};
+	 */
+	buttonProps?: object;
```

>I'm not sure how to specify it'll need the `BaseButtonProps` and not the `AnchorProps` in the union type ButtonProps. From what I understand, it looks like it should check for the tag so not sure why it's being recognized as an anchor.

Your [ comment here](https://github.com/WordPress/gutenberg/pull/47702/files#r1104990536) answered my initial question quoted above, so I just wanted to share it here to resolve this thread. 🙂 The portion of the comment I am referring to: 

>Knowing that Button can be used to render either a `<button />` or an `<a />`, I think that in this case we're only interested in rendering a button — therefore, I'm thinking of using ButtonAsButtonProps, rather than the whole ButtonProps type

I was initially confused about how to specify importing the button props and not the anchor props, but I see [you added that feature](https://github.com/WordPress/gutenberg/pull/47754) the following week. Awesome!

#### ciampo on 2023-02-15T18:35:00Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107546853
Path: packages/components/src/panel/types.ts

```diff
@@ -0,0 +1,98 @@
+export type PanelProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Text will be rendered inside an `<h2>` tag.
+	 */
+	header?: PanelHeaderProps[ 'label' ];
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelHeaderProps = {
+	/**
+	 * The text that will be rendered as the title of the panel.
+	 * Will be rendered in an `<h2>` tag.
+	 */
+	label?: string;
+	/**
+	 * The content to display within the panel header.
+	 */
+	children?: React.ReactNode;
+};
+
+export type PanelRowProps = {
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * The content to display within the panel row.
+	 */
+	children: React.ReactNode;
+};
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {};
+	 */
+	buttonProps?: object;
```

Great, I'm glad I could clarify better how to solve this!

### Review thread 2 (resolved)

#### tyxla on 2023-02-08T12:21:34Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1100055567
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,15 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
+	(
+		{
+			isOpened,
+			icon,
+			title,
+			buttonProps,
```

Since we previously used to pass all other props down, `buttonProps` is going to be missing `onClick`.

Should be easy to fix if we manually extract it from here and pass it to  `<Button />` below manually.

This is one of the reasons why the tests are currently failing.

#### brookewp on 2023-02-09T04:43:38Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1100975635
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,15 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
+	(
+		{
+			isOpened,
+			icon,
+			title,
+			buttonProps,
```

Ah, that makes sense. Thanks for sharing!

### Review thread 3 (resolved)

#### ciampo on 2023-02-13T18:29:18Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1104870992
Path: packages/components/src/panel/body.tsx

```diff
@@ -30,27 +36,29 @@ export function PanelBody(
 		opened,
 		title,
 		scrollAfterOpen = true,
-	},
-	ref
-) {
-	const [ isOpened, setIsOpened ] = useControlledState( opened, {
-		initial: initialOpen === undefined ? true : initialOpen,
-	} );
-	const nodeRef = useRef();
+	} = props;
+	const [ isOpened, setIsOpened ] = useControlledState< boolean | undefined >(
+		opened,
+		{
+			initial: initialOpen === undefined ? true : initialOpen,
+			fallback: false,
+		}
+	);
```

Nice!

Even if this is a runtime change, I agree that adding `fallback: false` is the correct approach here, since `useControlledState` requires it.

Also, nicely done about `< boolean | undefined >`! [Generics](https://www.typescriptlang.org/docs/handbook/2/generics.html) are an advanced TypeScript topic and definitely not an easy one to understand/get right.

### Review thread 4 (resolved)

#### ciampo on 2023-02-13T18:33:09Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1104874570
Path: packages/components/src/panel/body.tsx

```diff
@@ -8,19 +8,25 @@ import classnames from 'classnames';
  */
 import { useReducedMotion, useMergeRefs } from '@wordpress/compose';
 import { forwardRef, useRef } from '@wordpress/element';
+import type { ForwardedRef } from 'react';
 import { chevronUp, chevronDown } from '@wordpress/icons';
 
 /**
  * Internal dependencies
  */
+import type { PanelBodyProps, PanelBodyTitleProps } from './types';
+import type { WordPressComponentProps } from '../ui/context';
 import Button from '../button';
 import Icon from '../icon';
 import { useControlledState, useUpdateEffect } from '../utils';
 
 const noop = () => {};
 
-export function PanelBody(
-	{
+export function UnforwardedPanelBody(
+	props: PanelBodyProps,
+	ref: ForwardedRef< HTMLDivElement >
```

Small nit: since in other parts of the file we kept React types namespaced under the React global (e.g. `React.MouseEvent`), for coherency we could do the same thing here and just use `React.ForwardedRef< HTMLDivElement >`.

This is mostly a personal preference, but in some cases I also find it helpful — for example when in need of differentiating `React.KeyboardEvent` from the native `KeyboardEvent` ([example](https://stackoverflow.com/questions/46462841/typescript-react-whats-the-correct-type-of-the-parameter-for-onkeypress))

#### brookewp on 2023-02-15T01:49:28Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106565528
Path: packages/components/src/panel/body.tsx

```diff
@@ -8,19 +8,25 @@ import classnames from 'classnames';
  */
 import { useReducedMotion, useMergeRefs } from '@wordpress/compose';
 import { forwardRef, useRef } from '@wordpress/element';
+import type { ForwardedRef } from 'react';
 import { chevronUp, chevronDown } from '@wordpress/icons';
 
 /**
  * Internal dependencies
  */
+import type { PanelBodyProps, PanelBodyTitleProps } from './types';
+import type { WordPressComponentProps } from '../ui/context';
 import Button from '../button';
 import Icon from '../icon';
 import { useControlledState, useUpdateEffect } from '../utils';
 
 const noop = () => {};
 
-export function PanelBody(
-	{
+export function UnforwardedPanelBody(
+	props: PanelBodyProps,
+	ref: ForwardedRef< HTMLDivElement >
```

Ah, good point. I agree. Thanks for pointing this out and for the extra context/example of where it is good to differentiate!

### Review thread 5 (resolved)

#### ciampo on 2023-02-13T20:03:14Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1104961866
Path: packages/components/src/panel/types.ts

```diff
@@ -36,3 +41,73 @@ export type PanelRowProps = {
 	 */
 	children: React.ReactNode;
 };
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {}
+	 */
+	buttonProps?: ButtonProps;
+	/**
+	 * The content to display within the panel body.
+	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 */
+	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
```

This is another tricky one!

When reading the description, I noticed that it says "If the children is a Function, it will be called with an object with the opened property and return its value.". But the `opened` property is not reflected in the actual type.

Here's how we can add this information in the type:

```diff
diff --git a/packages/components/src/panel/types.ts b/packages/components/src/panel/types.ts
index e39cd771da..aac2203f6d 100644
--- a/packages/components/src/panel/types.ts
+++ b/packages/components/src/panel/types.ts
@@ -51,10 +51,12 @@ export type PanelBodyProps = {
 	 */
 	buttonProps?: Omit< ButtonAsButtonProps, 'icon' >;
 	/**
-	 * The content to display within the panel body.
-	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 * The content to display in the `PanelBody`. If a function is provided for
+	 * this prop, it will receive an object with the `opened` prop as an argument.
 	 */
-	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
+	children?:
+		| React.ReactNode
+		| ( ( props: { opened: boolean } ) => React.ReactNode );
 	/**
 	 * The CSS class to apply to the wrapper element.
 	 */
```

We first let TypeScript know that the function takes an argument (that we call `props`), and next to it we define the object's type — ie. an object with the `opened` property, typed as a `boolean` and non-optional.

We will also need to update `body.tsx`, where we pass `isOpened` as the value for the `opened` prop. TypeScript will complain that we can't assign an emtpy string to a `boolean`. Like we did before, we can cast `isOpened` to a `boolean` again:

```diff
diff --git a/packages/components/src/panel/body.tsx b/packages/components/src/panel/body.tsx
index a7e97edae3..ef60aede63 100644
--- a/packages/components/src/panel/body.tsx
+++ b/packages/components/src/panel/body.tsx
@@ -94,7 +94,7 @@ export function UnforwardedPanelBody(
 				{ ...buttonProps }
 			/>
 			{ typeof children === 'function'
-				? children( { opened: isOpened } )
+				? children( { opened: Boolean( isOpened ) } )
 				: isOpened && children }
 		</div>
 	);
```

As a consequence, this change also allows us to avoid some type casting in the `test/body.tsx` file:

```diff
diff --git a/packages/components/src/panel/test/body.tsx b/packages/components/src/panel/test/body.tsx
index 226c6b2b50..5801fd3b3f 100644
--- a/packages/components/src/panel/test/body.tsx
+++ b/packages/components/src/panel/test/body.tsx
@@ -51,7 +51,7 @@ describe( 'PanelBody', () => {
 		it( 'should call the children function, if specified', () => {
 			const { rerender } = render(
 				<PanelBody opened>
-					{ ( { opened }: PanelBodyProps ) => (
+					{ ( { opened } ) => (
 						<div hidden={ opened } data-testid="inner-content">
 							Content
 						</div>
@@ -67,7 +67,7 @@ describe( 'PanelBody', () => {
 
 			rerender(
 				<PanelBody opened={ false }>
-					{ ( { opened }: PanelBodyProps ) => (
+					{ ( { opened } ) => (
 						<div hidden={ opened } data-testid="inner-content">
 							Content
 						</div>
```

#### brookewp on 2023-02-15T02:04:19Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106572169
Path: packages/components/src/panel/types.ts

```diff
@@ -36,3 +41,73 @@ export type PanelRowProps = {
 	 */
 	children: React.ReactNode;
 };
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {}
+	 */
+	buttonProps?: ButtonProps;
+	/**
+	 * The content to display within the panel body.
+	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 */
+	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
```

Thank you for the detailed explanation! Adding a type to an object seemed a bit tricky as the syntax looks like it's the value, so I had a bit of trouble there. But this makes sense!

And I think it's a nice consequence, not to have to import types to the test. 🙂

#### ciampo on 2023-02-15T18:40:20Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107553312
Path: packages/components/src/panel/types.ts

```diff
@@ -36,3 +41,73 @@ export type PanelRowProps = {
 	 */
 	children: React.ReactNode;
 };
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {}
+	 */
+	buttonProps?: ButtonProps;
+	/**
+	 * The content to display within the panel body.
+	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 */
+	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
```

Yeah sometimes the code that we write / suggest can be a bit too "implicit" — in those cases, you can always create some intermediate variables while you're working on it, which may help to break down the problem into smaller bits

<details>
<summary>For example, in this case, the type could be broken down into</summary>

```diff
diff --git a/packages/components/src/panel/types.ts b/packages/components/src/panel/types.ts
index ef2fc65df1..59b44ddde7 100644
--- a/packages/components/src/panel/types.ts
+++ b/packages/components/src/panel/types.ts
@@ -43,6 +43,11 @@ export type PanelRowProps = {
 	children: React.ReactNode;
 };
 
+type ChildrenAsAFunctionProps = { opened: boolean };
+type ChildrenAsAFunction = (
+	props: ChildrenAsAFunctionProps
+) => React.ReactNode;
+
 export type PanelBodyProps = {
 	/**
 	 * Props that are passed to the `Button` component in title within the
@@ -59,9 +64,7 @@ export type PanelBodyProps = {
 	 * The content to display in the `PanelBody`.If a function is provided for
 	 * this prop, it will receive an object with the `opened` prop as an argument.
 	 */
-	children?:
-		| React.ReactNode
-		| ( ( props: { opened: boolean } ) => React.ReactNode );
+	children?: React.ReactNode | ChildrenAsAFunction;
 
 	/**
 	 * The CSS class to apply to the wrapper element.
```

</details>

#### ciampo on 2023-03-01T17:50:18Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122114551
Path: packages/components/src/panel/types.ts

```diff
@@ -36,3 +41,73 @@ export type PanelRowProps = {
 	 */
 	children: React.ReactNode;
 };
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {}
+	 */
+	buttonProps?: ButtonProps;
+	/**
+	 * The content to display within the panel body.
+	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 */
+	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
```

I'm going to mark this convo as resolved, but feel free to reopen/ask more questions if that was premature :)

### Review thread 6 (resolved)

#### ciampo on 2023-02-13T20:36:06Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1104990536
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

Typing the forwarding of the button props is definitely a tricky one — I took a look at the PR myself and I had to tweak things around a bit before finding a good solution.

I'm going to try and do my best to put my train of thoughts in writing.

This change in particular caught my attention, because I could see that **a new `buttonProps` prop was introduced in `PanelBodyTitle`**. While I understand why it would be tempting to make these changes, we are introducing a regression: adding explicit `buttonProps` means that we could technically specify a `buttonProps.onClick` function, and TypeScript would allow it. But then, in the `PanelBodyTitle`, spreading `{ ...buttonProps }` would override the `onClick` prop. The same would happen with `buttonProps.icon`.

Therefore, we can't really introduce this change, and we need to get back to how the code was originally:

```diff
diff --git a/packages/components/src/panel/body.tsx b/packages/components/src/panel/body.tsx
index 48bf0bdf4f..ebbceb7ab3 100644
--- a/packages/components/src/panel/body.tsx
+++ b/packages/components/src/panel/body.tsx
@@ -91,7 +91,7 @@ export function UnforwardedPanelBody(
 				isOpened={ Boolean( isOpened ) }
 				onClick={ handleOnToggle }
 				title={ title }
-				buttonProps={ buttonProps }
+				{ ...buttonProps }
 			/>
 			{ typeof children === 'function'
 				? children( { opened: Boolean( isOpened ) } )
@@ -106,8 +106,7 @@ const PanelBodyTitle = forwardRef(
 			isOpened,
 			icon,
 			title,
-			buttonProps,
-			onClick,
+			...props
 		}: WordPressComponentProps< PanelBodyTitleProps, 'button' >,
 		ref: ForwardedRef< any >
 	) => {
@@ -119,8 +118,7 @@ const PanelBodyTitle = forwardRef(
 					className="components-panel__body-toggle"
 					aria-expanded={ isOpened }
 					ref={ ref }
-					{ ...{ onClick } }
-					{ ...buttonProps }
+					{ ...props }
 				>
 					{ /*
 					Firefox + NVDA don't announce aria-expanded because the browser
```

Now, we're kind of back to the original code, and we need to update the types to suit the code.

Here's a few thoughts that come to mind:

- Looking at `PanelBodyTitle`, I can see that it explicitly extracts `isOpened`, `icon` and `title`, while the rest of the props are forwarded to `Button`. This makes me think that `PanelBodyTitleProps` is basically `ButtonProps`, plus a few more additions (`isOpened`, `icon` and `title`)
- Knowing that `Button` can be used to render either a `<button />` or an `<a />`, I think that in this case we're only interested in rendering a `button` — therefore, I'm thinking of using [`ButtonAsButtonProps`](https://github.com/WordPress/gutenberg/blob/2ce55071deefcc8b386439e9bc71a8e3a9c8a6ea/packages/components/src/button/types.ts#L17), rather than the whole `ButtonProps` type
- Another thing that we need to keep in mind is that `icon` is also defined in `ButtonProps`, and that `title` and `onClick` are also added to the `props` type definition when using `WordPressComponentProps`. Therefore:
  - since we want to use a specific type for `icon`, we will need to `Omit` it from `ButtonProps` — otherwise TypeScript will be upset that we're passing a `JSX.Element` instead of [what `Button` expects](https://github.com/WordPress/gutenberg/blob/2ce55071deefcc8b386439e9bc71a8e3a9c8a6ea/packages/components/src/button/types.ts#L36))
  - since `onClick` is very standard, I think we can remove it from `PanelBodyTitleProps` and rely on it being implicitly added by `WordPressComponentProps`
  - regarding `title`, I think we should define it explicitly in `PanelBodyTitleProps`. The version that we get via `WordPressComponentProps` is related to the [`title` attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/title), while `title` here is used as text displayed in the DOM.
- finally, the same treatment needs to be applied to the type for the `buttonProps` prop in `PanelBodyProps`. But there's one more nuance: since [`ButtonAsButtonProps` is not wrapped in `WordPressComponentProps`](https://github.com/WordPress/gutenberg/blob/2ce55071deefcc8b386439e9bc71a8e3a9c8a6ea/packages/components/src/button/types.ts#L17) , it doesn't include a bunch of props that `PanelBodyTitle` expects, like `onClick`. The solution here is to wrap `ButtonAsButtonProps` with `WordPressComponentProps` directly in `PanelBodyProps`

Applying these changes results in this diff:

```diff
diff --git a/packages/components/src/panel/types.ts b/packages/components/src/panel/types.ts
index 6ef7935803..badcf44e76 100644
--- a/packages/components/src/panel/types.ts
+++ b/packages/components/src/panel/types.ts
@@ -1,7 +1,8 @@
 /**
  * Internal dependencies
  */
-import type { ButtonProps } from '../button/types';
+import type { ButtonAsButtonProps } from '../button/types';
+import type { WordPressComponentProps } from '../ui/context';
 
 export type PanelProps = {
 	/**
@@ -44,11 +45,16 @@ export type PanelRowProps = {
 
 export type PanelBodyProps = {
 	/**
-	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 * Props that are passed to the `Button` component in title within the
+	 * `PanelBody`.
 	 *
 	 * @default {}
 	 */
-	buttonProps?: ButtonProps;
+	buttonProps?: WordPressComponentProps<
+		Omit< ButtonAsButtonProps, 'icon' >,
+		'button',
+		false
+	>;
 	/**
 	 * The content to display in the `PanelBody`. If a function is provided for
 	 * this prop, it will receive an object with the `opened` prop as an argument.
@@ -92,13 +98,7 @@ export type PanelBodyProps = {
 	scrollAfterOpen?: boolean;
 };
 
-export type PanelBodyTitleProps = {
-	/**
-	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
-	 *
-	 * @default {}
-	 */
-	buttonProps?: ButtonProps;
+export type PanelBodyTitleProps = Omit< ButtonAsButtonProps, 'icon' > & {
 	/**
 	 * An icon to be shown next to the PanelBody title.
 	 */
@@ -108,8 +108,7 @@ export type PanelBodyTitleProps = {
 	 */
 	isOpened?: boolean;
 	/**
-	 * A callback invoked when 'PanelBodyTitle' is clicked. It is used to
-	 * toggle the body opened or closed.
+	 * The title text.
 	 */
-	onClick: ( event: React.MouseEvent ) => void;
+	title?: string;
 };
```

Hope this is clear enough and shows the thought process I'd go through!

#### brookewp on 2023-02-15T02:38:43Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106587255
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

Thanks again! I really appreciate that you shared your thought process. Not only have I learned new things from your comment but also, it's really nice to see that we shared some of the same thoughts (even if I didn't have the context to execute them at the time). 

Anyways, this was all very clear! I'd just like to summarize my understanding of the below change to types, to ensure I am following:

```ts
buttonProps?: WordPressComponentProps< 
Omit< ButtonAsButtonProps, 'icon' >,
'button',
false>;
```
For our `buttonProps` prop, we are assigning the imported type `ButtonAsButtonProps` but omitting the icon prop since we need to specify a different type, `JSX.Element` in this case. By wrapping the above in the imported`WordPressComponentProps` type, it allows us to inherit props from `button` so we get props like `onClick` which would otherwise be missing. 

>But there's one more nuance: since `ButtonAsButtonProps` is not wrapped in `WordPressComponentProps` it doesn't include a bunch of props that PanelBodyTitle expects, like `onClick`

I'm curious, why is this the case?

#### brookewp on 2023-02-15T03:06:50Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106600140
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

Also, do you mind sharing more context on the following change, with the utility type being on the right side of the operator? I'm not sure what this is called to look for it in the documentation. 

```ts
export type PanelBodyTitleProps = Omit< ButtonAsButtonProps, 'icon' > & {
``` 

It appears to be passing `ButtonAsButtonProps` (minus the icon) to the rest props. In that case, would it make sense to change the above to this instead: 

```ts
export type PanelBodyTitleProps = WordPressComponentProps<
	Omit< ButtonAsButtonProps, 'icon' >,
	'button',
	false
> & {
```
And remove the following from `packages/components/src/panel/body.tsx` so we don't import `WordPressComponentProps` in two different files? 

https://github.com/WordPress/gutenberg/blob/db1449e741312ad48d8c412767117cd00dafb834/packages/components/src/panel/body.tsx#L111

#### ciampo on 2023-02-15T18:59:44Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107578757
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

> For our buttonProps prop, we are assigning the imported type ButtonAsButtonProps but omitting the icon prop since we need to specify a different type, JSX.Element in this case.

Correct!

> By wrapping the above in the importedWordPressComponentProps type, it allows us to inherit props from button so we get props like onClick which would otherwise be missing.

Yup :) 


> > But there's one more nuance: since `ButtonAsButtonProps` is not wrapped in `WordPressComponentProps` it doesn't include a bunch of props that PanelBodyTitle expects, like `onClick`
> 
> I'm curious, why is this the case?

`PanelBodyTitle` expects an `onClick` prop, but `ButtonAsButtonProps` doesn't have an `onClick`. For the `Button` component, that is not an issue because those [props are being wrapped in `WordPressComponentProps` when declaring `ButtonProps`](https://github.com/WordPress/gutenberg/blob/2ce55071deefcc8b386439e9bc71a8e3a9c8a6ea/packages/components/src/button/types.ts) — as you also explained above, that's where the `onClick` prop is added, together with every other prop that an HTML `button` element expects.

In our case, we need to add that `WordPressComponentsProps` wrap again so that we also get the `onClick` etc.

> Also, do you mind sharing more context on the following change, with the utility type being on the right side of the operator? 

I'm not sure exactly what you're referring to with "utility type", but I'll try my best to explain what's going on there:

The `&` operator allows to "put together" two different types (ie. `ResultingType = TypeA & TypeB`) — the resulting type is going to have all properties from `TypeA` and all propertied from `TypeB` together.
In this case, it means that `PanelBodyTitleProps` is going to have all props from `ButtonAsButtonProps` (minus `icon`), plus all props from that second type (ie. the `icon`, `isOpened` and `title` props).

> In that case, would it make sense to change the above to this instead:
> 
> ```ts
> export type PanelBodyTitleProps = WordPressComponentProps<
> 	Omit< ButtonAsButtonProps, 'icon' >,
> 	'button',
> 	false
> > & {
> ```
> 
> And remove the following from `packages/components/src/panel/body.tsx` so we don't import `WordPressComponentProps` in two different files?

Yes, that could be done — in this case it would be mostly a "stylistic" preference.

Personally, for example, I prefer to wrap prop types with `WordPressComponentProps` where the React component is declared, since in my mind it makes more sense to me that I know about all the extra props inherited by a component on top of its "own" props when looking at the component itself.

Again, hopefully I was clear enough! But as always feel free to ask as many questions as you need!

#### brookewp on 2023-02-16T03:13:16Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107960951
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

I should have been more clear when asking 'why is this the case'. I was referring to: 

>ButtonAsButtonProps is not wrapped in WordPressComponentProps

So I was just curious about why `ButtonAsButtonProps` isn't wrapped in `WordPressComponentProps`. 

>I'm not sure exactly what you're referring to with "utility type"

I found [documentation on Omit here](https://www.typescriptlang.org/docs/handbook/utility-types.html#omittype-keys) under 'Utility Types', which is what I was referring to there.

#### ciampo on 2023-02-16T09:06:17Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1108194628
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

> So I was just curious about why ButtonAsButtonProps isn't wrapped in `WordPressComponentProps`.

It gets wrapped in `WordPressComponentProps` when it's used in `ButtonProps`:

https://github.com/WordPress/gutenberg/blob/2ce55071deefcc8b386439e9bc71a8e3a9c8a6ea/packages/components/src/button/types.ts#L13-L15

This ties a bit with what I mentioned about regarding the separation of a component's "own props" vs the props that a component inherits via `WordPressComponentProps`.

Having the `ButtonAsButtonProps` type not wrapped in `WordPressComponentProps` gives us more flexibility on how to use it.

Having said that, this is not an area where is only one correct approach — we also constantly iterate and re-assess our approaches, in case we find a new "pattern" that suits our needs better.

> I found [documentation on Omit here](https://www.typescriptlang.org/docs/handbook/utility-types.html#omittype-keys) under 'Utility Types', which is what I was referring to there.

Got it! Do you still have questions around that line of code?

#### ciampo on 2023-03-01T17:53:05Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122117428
Path: packages/components/src/panel/body.tsx

```diff
@@ -93,7 +101,16 @@ export function PanelBody(
 }
 
 const PanelBodyTitle = forwardRef(
-	( { isOpened, icon, title, ...props }, ref ) => {
```

Same as above — marking this convo as resolved, since the initial feedback was addressed!

Feel free to ask more questions / reopen this convo if that was premature :)

### Review thread 7 (resolved)

#### ciampo on 2023-02-13T20:46:13Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1104998913
Path: packages/components/src/panel/test/body.tsx

```diff
@@ -89,29 +90,25 @@ describe( 'PanelBody', () => {
 				</PanelBody>
 			);
 
-			let panelContent = screen.getByTestId( 'inner-content' );
-
-			expect( panelContent ).toBeVisible();
+			expect( screen.getByTestId( 'inner-content' ) ).toBeVisible();
```

Well done on fixing these errors in the tests 🎉

### Review thread 8 (resolved)

#### ciampo on 2023-02-13T20:48:49Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1105001040
Path: packages/components/src/panel/types.ts

```diff
@@ -36,3 +41,73 @@ export type PanelRowProps = {
 	 */
 	children: React.ReactNode;
 };
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {}
+	 */
+	buttonProps?: ButtonProps;
+	/**
+	 * The content to display within the panel body.
+	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 */
+	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * An icon to be shown next to the PanelBody title.
+	 */
+	icon?: JSX.Element;
+	/**
+	 * Whether or not the panel will start open.
+	 */
+	initialOpen?: boolean;
+	/**
+	 * A function that is called when the user clicks on the PanelBody title after the open state is changed.
+	 *
+	 * @default true
```

The default value for the `onToggle` prop i[s assigned when destructuring the prop](https://github.com/WordPress/gutenberg/blob/db1449e741312ad48d8c412767117cd00dafb834/packages/components/src/panel/body.tsx#L35), and in this case is `noop`

```suggestion
	 * @default noop
```

#### brookewp on 2023-02-15T01:55:31Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106568216
Path: packages/components/src/panel/types.ts

```diff
@@ -36,3 +41,73 @@ export type PanelRowProps = {
 	 */
 	children: React.ReactNode;
 };
+
+export type PanelBodyProps = {
+	/**
+	 * Props that are passed to the Button component in the PanelBodyTitle within the panel body.
+	 *
+	 * @default {}
+	 */
+	buttonProps?: ButtonProps;
+	/**
+	 * The content to display within the panel body.
+	 *  If the children is a Function, it will be called with an object with the opened property and return its value.
+	 */
+	children?: React.ReactNode | ( ( {} ) => React.ReactNode );
+	/**
+	 * The CSS class to apply to the wrapper element.
+	 */
+	className?: string;
+	/**
+	 * An icon to be shown next to the PanelBody title.
+	 */
+	icon?: JSX.Element;
+	/**
+	 * Whether or not the panel will start open.
+	 */
+	initialOpen?: boolean;
+	/**
+	 * A function that is called when the user clicks on the PanelBody title after the open state is changed.
+	 *
+	 * @default true
```

Ah, that was a copy/paste error, thanks for catching it! 🙇‍♀️

### Review thread 9 (resolved)

#### ciampo on 2023-02-13T20:56:24Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1105007193
Path: packages/components/src/panel/body.tsx

```diff
@@ -30,27 +36,29 @@ export function PanelBody(
 		opened,
 		title,
 		scrollAfterOpen = true,
-	},
-	ref
-) {
-	const [ isOpened, setIsOpened ] = useControlledState( opened, {
-		initial: initialOpen === undefined ? true : initialOpen,
-	} );
-	const nodeRef = useRef();
+	} = props;
+	const [ isOpened, setIsOpened ] = useControlledState< boolean | undefined >(
+		opened,
+		{
+			initial: initialOpen === undefined ? true : initialOpen,
+			fallback: false,
+		}
+	);
+	const nodeRef = useRef< HTMLElement >( null );
 
 	// Defaults to 'smooth' scrolling
 	// https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView
 	const scrollBehavior = useReducedMotion() ? 'auto' : 'smooth';
 
-	const handleOnToggle = ( event ) => {
+	const handleOnToggle = ( event: React.MouseEvent ) => {
 		event.preventDefault();
 		const next = ! isOpened;
 		setIsOpened( next );
 		onToggle( next );
 	};
 
 	// Ref is used so that the effect does not re-run upon scrollAfterOpen changing value.
-	const scrollAfterOpenRef = useRef();
+	const scrollAfterOpenRef = useRef< boolean | null >( null );
```

The `null` value is never assigned to `scrollAfterOpenRef`. Therefore we can avoid introducing a runtime change, and use `undefined` instead of `null`

```suggestion
	const scrollAfterOpenRef = useRef< boolean | undefined >( undefined );
```

You may be asking — why was using `null` OK for `nodeRef` a few lines above?

That's because this `ref` is keeping track of a DOM node. In this case, React will pass a `null` value if for any reason the DOM element can't be found. That's why, in this case, we initialise the `ref` to `null`.

Hope this was clear!

#### brookewp on 2023-02-15T01:53:26Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1106567301
Path: packages/components/src/panel/body.tsx

```diff
@@ -30,27 +36,29 @@ export function PanelBody(
 		opened,
 		title,
 		scrollAfterOpen = true,
-	},
-	ref
-) {
-	const [ isOpened, setIsOpened ] = useControlledState( opened, {
-		initial: initialOpen === undefined ? true : initialOpen,
-	} );
-	const nodeRef = useRef();
+	} = props;
+	const [ isOpened, setIsOpened ] = useControlledState< boolean | undefined >(
+		opened,
+		{
+			initial: initialOpen === undefined ? true : initialOpen,
+			fallback: false,
+		}
+	);
+	const nodeRef = useRef< HTMLElement >( null );
 
 	// Defaults to 'smooth' scrolling
 	// https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView
 	const scrollBehavior = useReducedMotion() ? 'auto' : 'smooth';
 
-	const handleOnToggle = ( event ) => {
+	const handleOnToggle = ( event: React.MouseEvent ) => {
 		event.preventDefault();
 		const next = ! isOpened;
 		setIsOpened( next );
 		onToggle( next );
 	};
 
 	// Ref is used so that the effect does not re-run upon scrollAfterOpen changing value.
-	const scrollAfterOpenRef = useRef();
+	const scrollAfterOpenRef = useRef< boolean | null >( null );
```

Yes, it is clear. Thanks for explaining about `nodeRef` because I would have had that question. 😄  

And just to be certain I fully understand - the only reason we would want to switch it to `undefined` in this case is to avoid introducing a runtime change?

#### ciampo on 2023-02-15T19:07:54Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107589719
Path: packages/components/src/panel/body.tsx

```diff
@@ -30,27 +36,29 @@ export function PanelBody(
 		opened,
 		title,
 		scrollAfterOpen = true,
-	},
-	ref
-) {
-	const [ isOpened, setIsOpened ] = useControlledState( opened, {
-		initial: initialOpen === undefined ? true : initialOpen,
-	} );
-	const nodeRef = useRef();
+	} = props;
+	const [ isOpened, setIsOpened ] = useControlledState< boolean | undefined >(
+		opened,
+		{
+			initial: initialOpen === undefined ? true : initialOpen,
+			fallback: false,
+		}
+	);
+	const nodeRef = useRef< HTMLElement >( null );
 
 	// Defaults to 'smooth' scrolling
 	// https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView
 	const scrollBehavior = useReducedMotion() ? 'auto' : 'smooth';
 
-	const handleOnToggle = ( event ) => {
+	const handleOnToggle = ( event: React.MouseEvent ) => {
 		event.preventDefault();
 		const next = ! isOpened;
 		setIsOpened( next );
 		onToggle( next );
 	};
 
 	// Ref is used so that the effect does not re-run upon scrollAfterOpen changing value.
-	const scrollAfterOpenRef = useRef();
+	const scrollAfterOpenRef = useRef< boolean | null >( null );
```

> the only reason we would want to switch it to undefined in this case is to avoid introducing a runtime change?

That's definitely the main reason, yeah.

If I had to write this piece of code from scratch, I would have assigned a default value of `false`, instead of `undefined` — that way, the type associated to `useRef` would only be `boolean`, instead of `boolean | undefined`.

In general, I always try to avoid mixing-and-matching types when possible — unless a certain type has a clear meaning (like `null` in the case of `nodeRef`).

Also, writing `useRef( undefined )` is just a more explicit version of `useRef()` — and therefore, you could change the code to a less verbose

```ts
const scrollAfterOpenRef = useRef< boolean | undefined >();
```

#### brookewp on 2023-02-16T03:14:35Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107961433
Path: packages/components/src/panel/body.tsx

```diff
@@ -30,27 +36,29 @@ export function PanelBody(
 		opened,
 		title,
 		scrollAfterOpen = true,
-	},
-	ref
-) {
-	const [ isOpened, setIsOpened ] = useControlledState( opened, {
-		initial: initialOpen === undefined ? true : initialOpen,
-	} );
-	const nodeRef = useRef();
+	} = props;
+	const [ isOpened, setIsOpened ] = useControlledState< boolean | undefined >(
+		opened,
+		{
+			initial: initialOpen === undefined ? true : initialOpen,
+			fallback: false,
+		}
+	);
+	const nodeRef = useRef< HTMLElement >( null );
 
 	// Defaults to 'smooth' scrolling
 	// https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView
 	const scrollBehavior = useReducedMotion() ? 'auto' : 'smooth';
 
-	const handleOnToggle = ( event ) => {
+	const handleOnToggle = ( event: React.MouseEvent ) => {
 		event.preventDefault();
 		const next = ! isOpened;
 		setIsOpened( next );
 		onToggle( next );
 	};
 
 	// Ref is used so that the effect does not re-run upon scrollAfterOpen changing value.
-	const scrollAfterOpenRef = useRef();
+	const scrollAfterOpenRef = useRef< boolean | null >( null );
```

Thanks, this is helpful!

### Review thread 10 (resolved)

#### ciampo on 2023-02-15T19:22:14Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107610279
Path: packages/components/src/panel/types.ts

```diff
@@ -1,3 +1,9 @@
+/**
```

Here's also a few improvements that we could do to the JSDoc descriptions in this file:

- we should keep each line within 80 characters, for improved readability in IDEs
- For better formatting, we should surround technical terms (like prop names, component names, or values) with backticks, so that they get printed using a monospace font
- Since we're in the `PanelBody` props, we can also refer to `PanelBody` as "the component"
- In general, we should avoid documenting what I like to call "implementation details". Anything that we document can be assumed by the consumers of a component, and therefore we won't really be able to change it. For example, the fact that the `onToggle` callback is called when clicking the title, or after the open state is changed. The only thing that a user should know is that `onToggle` is called when the `PanelBody` component is toggled from its open state to its closed state, or viceversa

<details>

<summary>Here's the result of the tweaks that I applied on my machine</summary>

```diff
diff --git a/packages/components/src/panel/types.ts b/packages/components/src/panel/types.ts
index ef2fc65df1..79fb2071d1 100644
--- a/packages/components/src/panel/types.ts
+++ b/packages/components/src/panel/types.ts
@@ -68,7 +68,7 @@ export type PanelBodyProps = {
 	 */
 	className?: string;
 	/**
-	 * An icon to be shown next to the PanelBody title.
+	 * An icon to be shown next to the title.
 	 */
 	icon?: JSX.Element;
 	/**
@@ -76,23 +76,24 @@ export type PanelBodyProps = {
 	 */
 	initialOpen?: boolean;
 	/**
-	 * A function that is called when the user clicks on the PanelBody title after the open state is changed.
+	 * A function that is called any time the component is toggled from its closed
+	 * state to its opened state, or viceversa.
 	 *
 	 * @default noop
 	 */
 	onToggle?: ( next: boolean ) => void;
 	/**
-	 * If opened is true then the Panel will remain open regardless of the initialOpen prop and the panel will be prevented from being closed.
+	 * When set to `true`, the component will remain open regardless of the
+	 * `initialOpen` prop and the panel will be prevented from being closed.
 	 */
 	opened?: boolean;
 	/**
-	 * Title of the PanelBody. This shows even when it is closed.
+	 * Title text. It shows even when it is closed.
 	 */
 	title?: string;
 	/**
-	 * Scrolls the content into view when visible.
-	 * This improves the UX when there are multiple stacking Panel Body components.
-	 * components in a scrollable container.
+	 * Scrolls the content into view when visible. This improves the UX when
+	 * multiple `PanelBody` are stacked in a scrollable container.
 	 *
 	 * @default true
 	 */
@@ -101,11 +102,11 @@ export type PanelBodyProps = {
 
 export type PanelBodyTitleProps = Omit< ButtonAsButtonProps, 'icon' > & {
 	/**
-	 * An icon to be shown next to the PanelBody title.
+	 * An icon to be shown next to the title.
 	 */
 	icon?: JSX.Element;
 	/**
-	 * Whether or not the panel body is currently opened or not.
+	 * Whether or not the `PanelBody` is currently opened or not.
 	 */
 	isOpened?: boolean;
 	/**

```

</details>

#### brookewp on 2023-02-16T03:30:49Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107968196
Path: packages/components/src/panel/types.ts

```diff
@@ -1,3 +1,9 @@
+/**
```

Thanks for the context! I had copied the text from the README initially and the following comment from the component since `scrollAfterOpen` isn't in the current README: 

https://github.com/WordPress/gutenberg/blob/8f23e951a0e6a4ddbb022dca3dac2dece948ae38/packages/components/src/panel/body.js#L63-L65

Should the README descriptions be identical to the JSDocs? And are there any other changes we make to the README? I'd like to at least match them to the order in the component file.

#### ciampo on 2023-02-16T09:20:06Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1108210219
Path: packages/components/src/panel/types.ts

```diff
@@ -1,3 +1,9 @@
+/**
```

> I had copied the text from the README initially and the following comment from the component since scrollAfterOpen isn't in the current README:

Thank you for the context!

The components package has been around for a while, and it didn't get much attention to such things until recently. Working on these migrations is also a way we can improve the docs, following the latest guidelines that we set for ourselves.

> Should the README descriptions be identical to the JSDocs? And are there any other changes we make to the README? I'd like to at least match them to the order in the component file.

Yup! That was actually going to be the focus of my next round of review.

Now that we're happy with the JSDoc descriptions in the `types.ts` file, we should apply those same descriptions to the README.

The goal is to have README, `types.ts` and the component's implemenations "synchronised":

- all props listed in `types.ts` should be listed (apart from deprecated/unstable ones)
- the types of the props should be the same across README and `types`
- the default values for props should be the same across code implementation, README and `types`
- the example snippet in the README should be applied to the default export (for `Panel`, [it already is](https://github.com/WordPress/gutenberg/blob/0e9295905a3a8ab4b6791fd90a9473be016b7b62/packages/components/src/panel/index.tsx#L30-L46))

#### brookewp on 2023-02-18T01:43:49Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1110478200
Path: packages/components/src/panel/types.ts

```diff
@@ -1,3 +1,9 @@
+/**
```

Thanks! And from looking at other examples with multiple types exported, it looks like I won't be adding info for `PanelBodyTitleProps` to the README. Is that right?

#### ciampo on 2023-02-20T22:59:52Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1112381184
Path: packages/components/src/panel/types.ts

```diff
@@ -1,3 +1,9 @@
+/**
```

> And from looking at other examples with multiple types exported, it looks like I won't be adding info for PanelBodyTitleProps to the README. Is that right?

Correct! We document in the README only the props of the components that are going to be used by consumers of the package (ie. `PanelBody`). `PanelTitle` is not an exported component, therefore no need to document it :)

### Review thread 11 (resolved)

#### ciampo on 2023-02-16T09:07:30Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1108195888
Path: packages/components/src/panel/body.tsx

```diff
@@ -30,27 +35,29 @@ export function PanelBody(
 		opened,
 		title,
 		scrollAfterOpen = true,
-	},
-	ref
-) {
-	const [ isOpened, setIsOpened ] = useControlledState( opened, {
-		initial: initialOpen === undefined ? true : initialOpen,
-	} );
-	const nodeRef = useRef();
+	} = props;
+	const [ isOpened, setIsOpened ] = useControlledState< boolean | undefined >(
+		opened,
+		{
+			initial: initialOpen === undefined ? true : initialOpen,
+			fallback: false,
+		}
+	);
+	const nodeRef = useRef< HTMLElement >( null );
 
 	// Defaults to 'smooth' scrolling
 	// https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView
 	const scrollBehavior = useReducedMotion() ? 'auto' : 'smooth';
 
-	const handleOnToggle = ( event ) => {
+	const handleOnToggle = ( event: React.MouseEvent ) => {
 		event.preventDefault();
 		const next = ! isOpened;
 		setIsOpened( next );
 		onToggle( next );
 	};
 
 	// Ref is used so that the effect does not re-run upon scrollAfterOpen changing value.
-	const scrollAfterOpenRef = useRef();
+	const scrollAfterOpenRef = useRef< boolean | undefined >( undefined );
```

Small tweak, as discussed in https://github.com/WordPress/gutenberg/pull/47702#discussion_r1107589719

```suggestion
	const scrollAfterOpenRef = useRef< boolean | undefined >();
```

### Review thread 12 (resolved)

#### chad1008 on 2023-02-22T21:50:19Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1115001664
Path: packages/components/src/panel/README.md

```diff
@@ -77,18 +77,23 @@ const MyPanel = () => (
 
 ##### Props
 
-###### className
+###### header:`string`
```

Nit: I believe the prop names (throughout the README) should also be monospaced, in addition to the type itself.

<details>

<summary>Example:</summary>

```diff
diff --git a/packages/components/src/panel/README.md b/packages/components/src/panel/README.md
index f69079e9fc..177f985248 100644
--- a/packages/components/src/panel/README.md
+++ b/packages/components/src/panel/README.md
@@ -77,7 +77,7 @@ const MyPanel = () => (
 
 ##### Props
 
-###### header:`string`
+###### `header`:`string`
 
 The text that will be rendered as the title of the panel. Text will be rendered inside an
 `<h2>` tag.

```

</details>

### Review thread 13 (resolved)

#### chad1008 on 2023-02-22T21:52:56Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1115003751
Path: packages/components/src/panel/README.md

```diff
@@ -99,64 +104,68 @@ The `PanelBody` creates a collapsible container that can be toggled open or clos
 
 ##### Props
 
-###### title
+###### title: `string`
 
-Title of the `PanelBody`. This shows even when it is closed.
+Title text. It shows even when it is closed.
```

This description feels a little unclear ("It shows even when it is closed") The previous description made it more clear that the title displays even when the `PanelBody` is closed.

#### brookewp on 2023-02-24T07:12:11Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1116584983
Path: packages/components/src/panel/README.md

```diff
@@ -99,64 +104,68 @@ The `PanelBody` creates a collapsible container that can be toggled open or clos
 
 ##### Props
 
-###### title
+###### title: `string`
 
-Title of the `PanelBody`. This shows even when it is closed.
+Title text. It shows even when it is closed.
```

Thanks for the review @chad1008! @ciampo initially suggested this change in his review, and also suggested:

>Since we're in the PanelBody props, we can also refer to PanelBody as "the component"

So would it make sense to change it to the following?

`Title of the component. It shows even when it is closed.`

#### chad1008 on 2023-03-01T14:13:30Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1121801084
Path: packages/components/src/panel/README.md

```diff
@@ -99,64 +104,68 @@ The `PanelBody` creates a collapsible container that can be toggled open or clos
 
 ##### Props
 
-###### title
+###### title: `string`
 
-Title of the `PanelBody`. This shows even when it is closed.
+Title text. It shows even when it is closed.
```

That could work! Or maybe something like

`Title text. It shows even when the component is closed.`? Either could work!

### Review thread 14 (resolved)

#### chad1008 on 2023-02-22T21:55:52Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1115006241
Path: packages/components/src/panel/README.md

```diff
@@ -99,64 +104,68 @@ The `PanelBody` creates a collapsible container that can be toggled open or clos
 
 ##### Props
 
-###### title
+###### title: `string`
 
-Title of the `PanelBody`. This shows even when it is closed.
+Title text. It shows even when it is closed.
 
--   Type: `String`
 -   Required: No
 
-###### opened
+###### opened: `boolean`
 
-If opened is true then the `Panel` will remain open regardless of the `initialOpen` prop and the panel will be prevented from being closed.
+When set to `true`, the component will remain open regardless of the `initialOpen` prop and the
+panel will be prevented from being closed.
 
--   Type: `Boolean`
 -   Required: No
 
-###### className
+###### className: `string`
 
-The class that will be added with `components-panel__body`, if the panel is currently open, the `is-opened` class will also be passed to the classes of the wrapper div. If no `className` is passed then only `components-panel__body` and `is-opened` is used.
+The CSS class to apply to the wrapper element.
 
--   Type: `String`
 -   Required: No
 
-###### icon
+###### icon: `JSX.Element`
 
-An icon to be shown next to the `PanelBody` title.
+An icon to be shown next to the title.
 
--   Type: `String`
 -   Required: No
 
-###### onToggle
+###### onToggle: `( next: boolean ) => void;`
 
-A function that is called when the user clicks on the `PanelBody` title after the open state is changed.
+A function that is called any time the component is toggled from its closed state to its
+opened state, or vice versa.
 
--   Type: `function`
 -   Required: No
+-   Default: `noop`
 
-###### initialOpen
+###### initialOpen: `boolean`
 
 Whether or not the panel will start open.
 
--   Type: `Boolean`
 -   Required: No
--   Default: true
+-   Default: `true`
 
-###### children
+###### children: `| React.ReactNode | ( ( props: { opened: boolean } ) => React.ReactNode )`
 
-The rendered children. If the children is a `Function`, it will be called with an object with the `opened` property and return its value.
+The content to display in the `PanelBody`.If a function is provided for this prop, it will
```

Nit/typo: missing space after the first sentence on this line.

### Review thread 15 (resolved)

#### ciampo on 2023-03-01T17:43:46Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122107861
Path: packages/components/CHANGELOG.md

```diff
@@ -43,6 +43,7 @@
 -   `Navigator`: add more pattern matching tests, refine existing tests ([47910](https://github.com/WordPress/gutenberg/pull/47910)).
 -   `ToolsPanel`: Refactor Storybook examples to TypeScript ([47944](https://github.com/WordPress/gutenberg/pull/47944)).
 -   `ToolsPanel`: Refactor unit tests to TypeScript ([48275](https://github.com/WordPress/gutenberg/pull/48275)).
+-   `PanelBody`: Convert to TypeScript ([#47702](https://github.com/WordPress/gutenberg/pull/47702)).
```

Nit: since a new version of the package was released since opening this PR, we'll need to rebase (or merge trunk into this PR) and move this CHANGELOG entry under the `Unreleased` section at the top (we should also create a new `Internal` sub-section, in case there isn't one yet in the `Unreleased` section)

#### brookewp on 2023-03-02T01:26:37Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122507632
Path: packages/components/CHANGELOG.md

```diff
@@ -43,6 +43,7 @@
 -   `Navigator`: add more pattern matching tests, refine existing tests ([47910](https://github.com/WordPress/gutenberg/pull/47910)).
 -   `ToolsPanel`: Refactor Storybook examples to TypeScript ([47944](https://github.com/WordPress/gutenberg/pull/47944)).
 -   `ToolsPanel`: Refactor unit tests to TypeScript ([48275](https://github.com/WordPress/gutenberg/pull/48275)).
+-   `PanelBody`: Convert to TypeScript ([#47702](https://github.com/WordPress/gutenberg/pull/47702)).
```

I've made the updates, but I would appreciate it if you could take a second look to make sure I understood what you meant about the Unreleased/Internal section. 🙂

#### ciampo on 2023-03-02T07:50:07Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122708902
Path: packages/components/CHANGELOG.md

```diff
@@ -43,6 +43,7 @@
 -   `Navigator`: add more pattern matching tests, refine existing tests ([47910](https://github.com/WordPress/gutenberg/pull/47910)).
 -   `ToolsPanel`: Refactor Storybook examples to TypeScript ([47944](https://github.com/WordPress/gutenberg/pull/47944)).
 -   `ToolsPanel`: Refactor unit tests to TypeScript ([48275](https://github.com/WordPress/gutenberg/pull/48275)).
+-   `PanelBody`: Convert to TypeScript ([#47702](https://github.com/WordPress/gutenberg/pull/47702)).
```

Looking good!

### Review thread 16 (resolved)

#### ciampo on 2023-03-01T17:47:19Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122111515
Path: packages/components/CHANGELOG.md

```diff
@@ -20,10 +20,10 @@
 
 ### Enhancements
 
--  `ColorPalette`, `GradientPicker`, `PaletteEdit`, `ToolsPanel`: add new props to set a custom heading level ([43848](https://github.com/WordPress/gutenberg/pull/43848) and [#47788](https://github.com/WordPress/gutenberg/pull/47788)).
+-   `ColorPalette`, `GradientPicker`, `PaletteEdit`, `ToolsPanel`: add new props to set a custom heading level ([43848](https://github.com/WordPress/gutenberg/pull/43848) and [#47788](https://github.com/WordPress/gutenberg/pull/47788)).
```

Thank you for fixing all of these small formatting issues in the CHANGELOG!

#### brookewp on 2023-03-02T00:17:46Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122464976
Path: packages/components/CHANGELOG.md

```diff
@@ -20,10 +20,10 @@
 
 ### Enhancements
 
--  `ColorPalette`, `GradientPicker`, `PaletteEdit`, `ToolsPanel`: add new props to set a custom heading level ([43848](https://github.com/WordPress/gutenberg/pull/43848) and [#47788](https://github.com/WordPress/gutenberg/pull/47788)).
+-   `ColorPalette`, `GradientPicker`, `PaletteEdit`, `ToolsPanel`: add new props to set a custom heading level ([43848](https://github.com/WordPress/gutenberg/pull/43848) and [#47788](https://github.com/WordPress/gutenberg/pull/47788)).
```

I'd like to take the credit, but it is my format on save settings 😂

### Review thread 17 (resolved)

#### ciampo on 2023-03-01T17:48:27Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122112604
Path: packages/components/CHANGELOG.md

```diff
@@ -65,7 +66,7 @@
 -   `ColorListPicker`: Convert to TypeScript ([#46358](https://github.com/WordPress/gutenberg/pull/46358)).
 -   `KeyboardShortcuts`: Convert to TypeScript ([#47429](https://github.com/WordPress/gutenberg/pull/47429)).
 -   `ColorPalette`, `BorderControl`, `GradientPicker`: refine types and logic around single vs multiple palettes
- ([#47384](https://github.com/WordPress/gutenberg/pull/47384)).
+    ([#47384](https://github.com/WordPress/gutenberg/pull/47384)).
```

Super nitty: for consistency reasons with how the rest of the CHANGELOG file is written, I wouldn't add a line break here (even if the line is super long)

#### brookewp on 2023-03-02T00:58:40Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122493123
Path: packages/components/CHANGELOG.md

```diff
@@ -65,7 +66,7 @@
 -   `ColorListPicker`: Convert to TypeScript ([#46358](https://github.com/WordPress/gutenberg/pull/46358)).
 -   `KeyboardShortcuts`: Convert to TypeScript ([#47429](https://github.com/WordPress/gutenberg/pull/47429)).
 -   `ColorPalette`, `BorderControl`, `GradientPicker`: refine types and logic around single vs multiple palettes
- ([#47384](https://github.com/WordPress/gutenberg/pull/47384)).
+    ([#47384](https://github.com/WordPress/gutenberg/pull/47384)).
```

This is where I put the blame on my format settings whereas before I credited it 😄

### Review thread 18 (resolved)

#### ciampo on 2023-03-01T17:59:08Z

URL: https://github.com/WordPress/gutenberg/pull/47702#discussion_r1122123651
Path: packages/components/src/panel/README.md

```diff
@@ -77,18 +77,23 @@ const MyPanel = () => (
 
 ##### Props
 
-###### className
+###### `header`: `string`
 
-The class that will be added with `components-panel`. If no `className` is passed only `components-panel__body` and `is-opened` is used.
+The text that will be rendered as the title of the panel. Text will be rendered inside an
+`<h2>` tag.
 
--   Type: `String`
 -   Required: No
 
-###### header
+###### `className`: `string`
 
-Title of the `Panel`. Text will be rendered inside an `<h2>` tag.
+The CSS class to apply to the wrapper element.
+
+-   Required: No
+
+###### `children`: `React.ReactNode`
+
+The content to display within the panel row.
 
--   Type: `String`
 -   Required: No
```

Looking at the `types.ts` file, the `children` prop on the `Panel` component is marked as required — could you update the README to reflect that?

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +8/-7)
- packages/components/src/panel/README.md (MODIFIED, +53/-35)
- packages/components/src/panel/body.tsx (RENAMED, +31/-17)
- packages/components/src/panel/stories/index.tsx (RENAMED, +16/-6)
- packages/components/src/panel/test/__snapshots__/body.tsx.snap (RENAMED, +0/-0)
- packages/components/src/panel/test/body.tsx (RENAMED, +12/-17)
- packages/components/src/panel/types.ts (MODIFIED, +78/-0)
- packages/components/tsconfig.json (MODIFIED, +1/-2)
