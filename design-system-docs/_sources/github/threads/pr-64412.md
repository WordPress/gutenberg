---
number: 64412
type: pull_request
state: MERGED
merged_at: 2024-08-16T21:51:32Z
url: https://github.com/WordPress/gutenberg/pull/64412
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #64412: CustomSelectControl: improve props type inferring

- URL: https://github.com/WordPress/gutenberg/pull/64412
- Author: meteorlxy
- Created: 2024-08-10T04:16:53Z
- Updated: 2024-08-16T21:51:54Z
- Comments: 1 of 1
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/64412.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Improve props type inferring for `CustomSelectControl` component.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

The current implementation (introduced in #63985) has some drawbacks:

1. The option type could be inferred from `value` and `onChange`, which is not expected.
2. The `options` does not accept immutable array.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Followed the implementation of #64069 :

1. Make use of `NoInfer`
2. Use `ReadonlyArray` for `options`

cc @mirka 

## Testing Instructions

Added type checking in unit tests

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

N/A

## Screenshots or screencast <!-- if applicable -->

N/A

## Issue comments

### github-actions on 2024-08-10T04:17:07Z

URL: https://github.com/WordPress/gutenberg/pull/64412#issuecomment-2279077493

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: meteorlxy <meteorlxy@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### mirka on 2024-08-12T18:49:03Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1714231427
Path: packages/components/src/custom-select-control/test/index.tsx

```diff
@@ -689,3 +690,127 @@ describe.each( [
 		} );
 	} );
 } );
+
+describe( 'Type checking', () => {
+	// eslint-disable-next-line jest/expect-expect
+	it( 'should infer the value type from available `options`, but not the `value` or `onChange` prop', () => {
+		const options = [
+			{
+				key: 'narrow',
+				name: 'Narrow',
+			},
+			{
+				key: 'value',
+				name: 'Value',
+			},
+		];
+		const optionsReadOnly = [
+			{
+				key: 'narrow',
+				name: 'Narrow',
+			},
+			{
+				key: 'value',
+				name: 'Value',
+			},
+		] as const;
+
+		const onChange: (
+			changeObject: CustomSelectChangeObject< CustomSelectOption >
+		) => void = () => {};
+		const onChangeWithFoo: (
+			changeObject: CustomSelectChangeObject<
+				CustomSelectOption & {
+					foo: string;
+				}
+			>
+		) => void = () => {};
```

Ideally we would write these tests without having to import the `CustomSelectChangeObject` and `CustomSelectOption` types directly. Consumers outside of this package don't have access to these internal types.

```suggestion
		const onChange: React.ComponentProps<
			typeof UncontrolledCustomSelectControl
		>[ 'onChange' ] = () => {};
```

And for the `onChangeWithFoo` cases, I think we could simplify it into a more realistic example and just inline it:

```tsx
		<UncontrolledCustomSelectControl
			label="Label"
			options={ options }
			value={ {
				key: 'narrow',
				name: 'Narrow',
			} }
			// @ts-expect-error: the option type should not be inferred from `onChange`
			onChange={ ( obj ) => obj.foo }
		/>;
```

#### meteorlxy on 2024-08-13T09:21:10Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1714961149
Path: packages/components/src/custom-select-control/test/index.tsx

```diff
@@ -689,3 +690,127 @@ describe.each( [
 		} );
 	} );
 } );
+
+describe( 'Type checking', () => {
+	// eslint-disable-next-line jest/expect-expect
+	it( 'should infer the value type from available `options`, but not the `value` or `onChange` prop', () => {
+		const options = [
+			{
+				key: 'narrow',
+				name: 'Narrow',
+			},
+			{
+				key: 'value',
+				name: 'Value',
+			},
+		];
+		const optionsReadOnly = [
+			{
+				key: 'narrow',
+				name: 'Narrow',
+			},
+			{
+				key: 'value',
+				name: 'Value',
+			},
+		] as const;
+
+		const onChange: (
+			changeObject: CustomSelectChangeObject< CustomSelectOption >
+		) => void = () => {};
+		const onChangeWithFoo: (
+			changeObject: CustomSelectChangeObject<
+				CustomSelectOption & {
+					foo: string;
+				}
+			>
+		) => void = () => {};
```

Updated.

As for the `onChangeWithFoo` cases, what you provided above should not be what we want.

We need to test if the `NoInfer` in the `onChange` method works as expected, so we have to define the type of the `onChange` prop explicitly.

It should not be a realistic use case for sure, but we still need to test it. So I added some comments to explain it.

#### mirka on 2024-08-16T21:49:16Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1720385320
Path: packages/components/src/custom-select-control/test/index.tsx

```diff
@@ -689,3 +690,127 @@ describe.each( [
 		} );
 	} );
 } );
+
+describe( 'Type checking', () => {
+	// eslint-disable-next-line jest/expect-expect
+	it( 'should infer the value type from available `options`, but not the `value` or `onChange` prop', () => {
+		const options = [
+			{
+				key: 'narrow',
+				name: 'Narrow',
+			},
+			{
+				key: 'value',
+				name: 'Value',
+			},
+		];
+		const optionsReadOnly = [
+			{
+				key: 'narrow',
+				name: 'Narrow',
+			},
+			{
+				key: 'value',
+				name: 'Value',
+			},
+		] as const;
+
+		const onChange: (
+			changeObject: CustomSelectChangeObject< CustomSelectOption >
+		) => void = () => {};
+		const onChangeWithFoo: (
+			changeObject: CustomSelectChangeObject<
+				CustomSelectOption & {
+					foo: string;
+				}
+			>
+		) => void = () => {};
```

Ah, you're right. Thanks for the simplification and code comment though, it reads a lot better 👍

### Review thread 2

#### mirka on 2024-08-12T18:49:42Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1714232074
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,7 +24,7 @@ export type CustomSelectOption = {
 /**
  * The object returned from the onChange event.
  */
-type CustomSelectChangeObject< T extends CustomSelectOption > = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

Better if we could remove this export, as noted above.

#### meteorlxy on 2024-08-13T09:17:23Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1714955422
Path: packages/components/src/custom-select-control/types.ts

```diff
@@ -24,7 +24,7 @@ export type CustomSelectOption = {
 /**
  * The object returned from the onChange event.
  */
-type CustomSelectChangeObject< T extends CustomSelectOption > = {
+export type CustomSelectChangeObject< T extends CustomSelectOption > = {
```

Removed

### Review thread 3

#### tyxla on 2024-08-13T10:53:15Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1715089804
Path: packages/components/src/custom-select-control/test/index.tsx

```diff
@@ -689,3 +689,128 @@ describe.each( [
 		} );
 	} );
 } );
+
+describe( 'Type checking', () => {
```

Not something for this PR, but I wonder if we should start putting these type-checking pseudo-tests in a separate file under `/test` (types.tsx for example).

Then we could add a global ESLint disable rule for `jest/expect-expect` and we'll no longer have to do it every time. 

Thoughts?

#### tyxla on 2024-08-13T10:53:47Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1715090398
Path: packages/components/src/custom-select-control/test/index.tsx

```diff
@@ -689,3 +689,128 @@ describe.each( [
 		} );
 	} );
 } );
+
+describe( 'Type checking', () => {
```

cc @mirka and @DaniGuardiola

#### mirka on 2024-08-16T21:46:36Z

URL: https://github.com/WordPress/gutenberg/pull/64412#discussion_r1720383642
Path: packages/components/src/custom-select-control/test/index.tsx

```diff
@@ -689,3 +689,128 @@ describe.each( [
 		} );
 	} );
 } );
+
+describe( 'Type checking', () => {
```

Extracted to #64588

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/custom-select-control/test/index.tsx (MODIFIED, +125/-0)
- packages/components/src/custom-select-control/types.ts (MODIFIED, +3/-3)
