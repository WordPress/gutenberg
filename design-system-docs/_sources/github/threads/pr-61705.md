---
number: 61705
type: pull_request
state: MERGED
merged_at: 2024-05-17T08:43:42Z
url: https://github.com/WordPress/gutenberg/pull/61705
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #61705: Components: introduce Combobox `expandOnFocus` property

- URL: https://github.com/WordPress/gutenberg/pull/61705
- Author: retrofox
- Created: 2024-05-16T08:33:41Z
- Updated: 2025-11-24T09:25:08Z
- Comments: 1 of 1
- Review threads: 5 of 5
- Changed files: 5 of 5
- Diff: ../pr-diffs/61705.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
This property introduces the `expandOnFocus` property to the Combobox control component.

<!-- In a few words, what is the PR actually doing? -->

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Sometimes, auto-expanding the combo box dropdown is not a desired behavior and could even be considered intrusive.
For instance, AriaKit combobox never auto-expands the dropdown in the focus event.

https://github.com/WordPress/gutenberg/assets/77539/9a2636a6-75b6-4f4c-bebf-a072e867015c

The dropdown opens only when the user types in the input field or clicks on it.

Note: it doesn't change the current behavior to ensure backward compatibilities, which we would consider it.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

* It adds the `onClick` property to the `<TokenInput />` component, since it's mandatory to differetiate the `onFocus` and the `onClick` event in the `<ComboboxControl />` component context
* When focusing the element, it sets `isExpanded` as true only when the `expandOnFocus` property is true as well.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->

1. Run storybook
2. Compare the ComboboxControl stories
3. Confirm that `Not Expand On Focus` does not auto-expand the dropdown when the user focuses on the input element. It's required to type or click on it to open the dropdown:

https://github.com/WordPress/gutenberg/assets/77539/ce7bb078-876a-4c0e-bdb1-2ff7a0828467


### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

## Issue comments

### github-actions on 2024-05-16T08:33:58Z

URL: https://github.com/WordPress/gutenberg/pull/61705#issuecomment-2114488960

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: retrofox <retrofox@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1 (resolved)

#### mirka on 2024-05-16T22:05:07Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r1604114806
Path: packages/components/src/form-token-field/token-input.tsx

Do we need the changes in this file? It seems like `onClick` would just pass through automatically via the `restProps`.

#### retrofox on 2024-05-17T07:43:41Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r1604491421
Path: packages/components/src/form-token-field/token-input.tsx

correct. Tackled here 6543f6a9e5b124c61e3bad03b865d87b04029760

### Review thread 2 (resolved)

#### mirka on 2024-05-16T22:09:04Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r1604118265
Path: packages/components/CHANGELOG.md

```diff
@@ -1,6 +1,7 @@
 <!-- Learn how to maintain this file at https://github.com/WordPress/gutenberg/tree/HEAD/packages#maintaining-changelogs. -->
 
 ## Unreleased
+-   `Combobox`: Introduce Combobox expandOnFocus property ([#61705](https://github.com/WordPress/gutenberg/pull/61705)).
```

```suggestion

### Enhancements

-   `ComboboxControl`: Introduce Combobox expandOnFocus prop ([#61705](https://github.com/WordPress/gutenberg/pull/61705)).
```

(The full component name really helps for searchability!)

### Review thread 3 (resolved)

#### mirka on 2024-05-16T22:17:33Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r1604124023
Path: packages/components/src/combobox-control/types.ts

```diff
@@ -74,4 +74,11 @@ export type ComboboxControlProps = Pick<
 	 * The current value of the control.
 	 */
 	value?: string | null;
+
+	/**
+	 * Automatically expand the dropdown when the control is focused.
```

```suggestion
	 * Automatically expand the dropdown when the control is focused.
	 * If the control is clicked, the dropdown will expand regardless of this prop.
```

I think this will be a cause of confusion if we don't mention this explicitly. Also, it will help clarify the intended expansion behavior if we decide to rewrite the underlying implementation of this v1 component with Ariakit someday.

### Review thread 4 (resolved)

#### mirka on 2024-05-16T22:17:52Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r1604124216
Path: packages/components/src/combobox-control/types.ts

Let's also document this prop in the component README.

#### retrofox on 2024-05-17T07:43:54Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r1604491780
Path: packages/components/src/combobox-control/types.ts

23fd776781ad8c3c4678c20db4c00067e776f555

### Review thread 5

#### jeryj on 2025-11-20T20:03:45Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r2547508817
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -236,11 +237,18 @@ function ComboboxControl( props: ComboboxControlProps ) {
 
 	const onFocus = () => {
 		setInputHasFocus( true );
-		setIsExpanded( true );
+		if ( expandOnFocus ) {
+			setIsExpanded( true );
+		}
+
 		onFilterValueChange( '' );
 		setInputValue( '' );
 	};
 
+	const onClick = () => {
+		setIsExpanded( true );
```

@retrofox, @mirka - Why doesn't `onClick` check for `expandOnFocus` as well? I'm looking at implementing the combobox and might not want the value cleared and the options expanded when the input is clicked.

#### jeryj on 2025-11-21T16:00:21Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r2550240325
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -236,11 +237,18 @@ function ComboboxControl( props: ComboboxControlProps ) {
 
 	const onFocus = () => {
 		setInputHasFocus( true );
-		setIsExpanded( true );
+		if ( expandOnFocus ) {
+			setIsExpanded( true );
+		}
+
 		onFilterValueChange( '' );
 		setInputValue( '' );
 	};
 
+	const onClick = () => {
+		setIsExpanded( true );
```

The reason I'd like the onClick to have the same behavior (if using expandOnFocus=false, DON'T expand on click either) is that I want the same behavior for tabbing or clicking into the combobox for [this PR to convert the Link Search Control to use the combobox control](https://github.com/WordPress/gutenberg/pull/73469/) component. There's already an X to reset the field if it has a value, so I don't know why we need to force clicking into the input to also clear the search.

#### mirka on 2025-11-21T16:37:09Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r2550349587
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -236,11 +237,18 @@ function ComboboxControl( props: ComboboxControlProps ) {
 
 	const onFocus = () => {
 		setInputHasFocus( true );
-		setIsExpanded( true );
+		if ( expandOnFocus ) {
+			setIsExpanded( true );
+		}
+
 		onFilterValueChange( '' );
 		setInputValue( '' );
 	};
 
+	const onClick = () => {
+		setIsExpanded( true );
```

Do you have a target date for that work? There's a much improved and customizable set of combobox components that we're planning to add to the `@wordpress/ui` package here, let's say within the next two months. I wonder if that would make your work easier.

From what you're asking, I think you want a combobox that acts like an autocomplete ("google search field" style) rather than a searchable select. The current `ComboboxControl` is tailored to the latter use case, and is not architected to be much customizable beyond that.

#### jeryj on 2025-11-21T17:07:17Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r2550435091
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -236,11 +237,18 @@ function ComboboxControl( props: ComboboxControlProps ) {
 
 	const onFocus = () => {
 		setInputHasFocus( true );
-		setIsExpanded( true );
+		if ( expandOnFocus ) {
+			setIsExpanded( true );
+		}
+
 		onFilterValueChange( '' );
 		setInputValue( '' );
 	};
 
+	const onClick = () => {
+		setIsExpanded( true );
```

We are wanting it in WP 7.0. Will the upgrade path be easy if I work on it now? I'd really like to get an implementation going sooner than that. Or is there a way to use a private/beta version of the new combobox?

#### mirka on 2025-11-24T09:25:08Z

URL: https://github.com/WordPress/gutenberg/pull/61705#discussion_r2555374442
Path: packages/components/src/combobox-control/index.tsx

```diff
@@ -236,11 +237,18 @@ function ComboboxControl( props: ComboboxControlProps ) {
 
 	const onFocus = () => {
 		setInputHasFocus( true );
-		setIsExpanded( true );
+		if ( expandOnFocus ) {
+			setIsExpanded( true );
+		}
+
 		onFilterValueChange( '' );
 		setInputValue( '' );
 	};
 
+	const onClick = () => {
+		setIsExpanded( true );
```

You can maybe try implementing with [Base UI `Autocomplete`](https://base-ui.com/react/components/autocomplete) directly? The `@wordpress/ui` component will likely have similar mechanics.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/combobox-control/README.md (MODIFIED, +9/-0)
- packages/components/src/combobox-control/index.tsx (MODIFIED, +10/-1)
- packages/components/src/combobox-control/stories/index.story.tsx (MODIFIED, +14/-0)
- packages/components/src/combobox-control/types.ts (MODIFIED, +7/-0)
