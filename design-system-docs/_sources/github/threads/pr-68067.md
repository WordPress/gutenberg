---
number: 68067
type: pull_request
state: MERGED
merged_at: 2024-12-18T18:24:04Z
url: https://github.com/WordPress/gutenberg/pull/68067
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components","has dev note"]
---

# PR #68067: RadioGroup: Log deprecation warning

- URL: https://github.com/WordPress/gutenberg/pull/68067
- Author: mirka
- Created: 2024-12-17T19:31:54Z
- Updated: 2024-12-18T19:07:11Z
- Comments: 2 of 2
- Review threads: 2 of 2
- Changed files: 4 of 4
- Diff: ../pr-diffs/68067.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Soft deprecates (#61099) the RadioGroup component.

## Why?
All remaining instances of RadioGroup in the app have been removed as of #67702.

## How?
This was already passive deprecated, so just escalating the deprecation so it notifies consumers through a console warning and Dev Note.

## Testing Instructions
Go to the Storybook for RadioGroup and check that the deprecation warning is being logged in dev tools.

## ✍️ Dev Note

The `RadioGroup` component has been deprecated. To be consistent with the current WordPress design system, use [`RadioControl`](https://wordpress.github.io/gutenberg/?path=/docs/components-radiocontrol--docs) or [`ToggleGroupControl`](https://wordpress.github.io/gutenberg/?path=/docs/components-experimental-togglegroupcontrol--docs) instead.

## Issue comments

### github-actions on 2024-12-17T19:33:21Z

URL: https://github.com/WordPress/gutenberg/pull/68067#issuecomment-2549437421

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-12-17T19:57:07Z

URL: https://github.com/WordPress/gutenberg/pull/68067#issuecomment-2549484911

<!-- flaky-tests-report-comment -->
**Flaky tests detected in a78d424e480a95ba1243ec502318552e72b01a8f.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/12380096509
📝  Reported issues:
- #61884 in `/test/e2e/specs/editor/blocks/navigation-frontend-interactivity.spec.js`

## Review threads

### Review thread 1

#### mirka on 2024-12-17T21:11:04Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1889227776
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
```

Adds a mechanism to suppress a confusing "double warning" coming from the ButtonGroup used internally in RadioGroup.

#### jsnajdr on 2024-12-18T07:51:51Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1889771204
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
```

React uses global variables for this purpose:
```js
let didWarnAboutSomething = false;
function something() {
  if ( ! didWarnAboutSomething ) {
    didWarnAboutSomething = true;
    console.error( /* ... */ );
  }
}
```
They also use `Set`s as `didWarn*` values, when they want to track a warning for each component name or something else separately.

We could use this, too, instead of the semi-official prop.

#### tyxla on 2024-12-18T08:53:51Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1889849449
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
```

If we decide to go the `didWarn` way, I think it makes sense to implement it directly in the `deprecated()` helper function.

#### mirka on 2024-12-18T18:19:15Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1890658052
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
```

I was thinking about smarter ways to implement this mechanism when I started logging the size deprecation warnings, but came to the conclusion that an explicit prop was the only way. I am imagining consumer code like this, where the ButtonGroup warning inside the RadioGroup should be suppressed, but the standalone ButtonGroup instance should still warn:

```jsx
const MyUI = () => (
  <RadioGroup />
  <ButtonGroup />
);
```

I'm all ears if you have any clever ideas!

#### ciampo on 2024-12-18T18:54:42Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1890698182
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
```

Could component's internal context system help?

#### mirka on 2024-12-18T19:07:11Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1890711484
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
```

Not in a way that's "more automatic" than the current manual prop-based system, I don't think. Also context could be problematic when render props or slots are involved (e.g. we don't want to suppress warnings on a component that the consumer passed in through a `prefix` prop).

### Review thread 2

#### mirka on 2024-12-17T21:11:35Z

URL: https://github.com/WordPress/gutenberg/pull/68067#discussion_r1889228496
Path: packages/components/src/button-group/index.tsx

```diff
@@ -20,13 +20,15 @@ function UnforwardedButtonGroup(
 	props: WordPressComponentProps< ButtonGroupProps, 'div', false >,
 	ref: ForwardedRef< HTMLDivElement >
 ) {
-	const { className, ...restProps } = props;
+	const { className, __shouldNotWarnDeprecated, ...restProps } = props;
 	const classes = clsx( 'components-button-group', className );
 
-	deprecated( 'wp.components.ButtonGroup', {
-		since: '6.8',
-		alternative: 'wp.components.ToggleGroupControl',
-	} );
+	if ( ! __shouldNotWarnDeprecated ) {
+		deprecated( 'wp.components.ButtonGroup', {
+			since: '6.8',
+			alternative: 'wp.components.__experimentalToggleGroupControl',
```

Added the `__experimental` prefix for consistency.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/button-group/index.tsx (MODIFIED, +7/-5)
- packages/components/src/button-group/types.ts (MODIFIED, +7/-0)
- packages/components/src/radio-group/index.tsx (MODIFIED, +12/-1)
