---
number: 64293
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/64293
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #64293: TextControl: support generic props type

- URL: https://github.com/WordPress/gutenberg/pull/64293
- Author: meteorlxy
- Created: 2024-08-06T03:24:29Z
- Updated: 2025-12-04T20:46:25Z
- Comments: 4 of 4
- Review threads: 4 of 4
- Changed files: 5 of 5
- Diff: ../pr-diffs/64293.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Fixes #64292

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

It would be helpful to infer the `onChange` type

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Making the `TextControlProps` a generic type
- Emit `event.target.valueAsNumber` or `event.target.value` according to the type of `type` prop

## Testing Instructions

I've added unit tests for type checking.

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

N/A

## Screenshots or screencast <!-- if applicable -->

**Before**

<img width="1018" alt="image" src="https://github.com/user-attachments/assets/dbb61d0d-fbf2-491a-bf66-2f83d29f52e7">


**After**

<img width="463" alt="image" src="https://github.com/user-attachments/assets/0f579e30-c5f8-466c-9e98-7b85464a03e4">

## Issue comments

### github-actions on 2024-08-06T03:24:45Z

URL: https://github.com/WordPress/gutenberg/pull/64293#issuecomment-2270309833

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: meteorlxy <meteorlxy@git.wordpress.org>
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: youknowriad <youknowriad@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### meteorlxy on 2024-08-10T03:34:56Z

URL: https://github.com/WordPress/gutenberg/pull/64293#issuecomment-2278962292

@mirka I just added type checking in the unit tests following your PR #64069. Maybe you could also help to take a look. Thanks!

### youknowriad on 2025-12-04T19:44:59Z

URL: https://github.com/WordPress/gutenberg/pull/64293#issuecomment-3614056494

@aduth @mirka this PR seems decent, do we want to ship it or close it?

### mirka on 2025-12-04T20:46:24Z

URL: https://github.com/WordPress/gutenberg/pull/64293#issuecomment-3614260303

It looks like we [concluded](https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715131828) that this is a breaking change, and not worth the upside.

I'm also starting to be wary of conditional types and discriminated unions in these kinds of component libraries, to be honest 😅 I've now seen many times that it tends to cause a lot of complications for downstream consumers who want to do simple manipulations on props object types, for example `Omit`s, which cannot be unblocked without a somewhat advanced understanding of TypeScript. There's definitely a trade-off there between the technical accuracy of the types and the everyday devex of a normal consumer.

## Review threads

### Review thread 1 (resolved)

#### DaniGuardiola on 2024-08-10T09:36:48Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1712613170
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
```

Can we obtain this union from the JSX namespace? Otherwise we'll need to manually maintain it and keep it in sync when new types are added.

#### mirka on 2024-08-12T18:57:14Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1714239787
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
```

I believe it's intentional that we maintain this list manually, since not all `input` types are compatible with this component (e.g. range, radio, etc).

#### DaniGuardiola on 2024-08-12T19:56:12Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1714299386
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
```

Ah, I think I missed the fact that this list was already there before the change. I thought it was being added in this PR. You can disregard this note.

### Review thread 2

#### DaniGuardiola on 2024-08-10T09:38:14Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1712613301
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
+	| 'date'
+	| 'datetime-local'
+	| 'email'
+	| 'number'
+	| 'password'
+	| 'tel'
+	| 'text'
+	| 'time'
+	| 'search'
+	| 'url';
+
+export type TextControlProps<
+	Type extends TextControlPropsType = 'text',
```

I think we'll want to use the classic naming strategy for type arguments, and go with T and V for consistency with other instances in the package.

#### meteorlxy on 2024-08-13T09:45:48Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715004124
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
+	| 'date'
+	| 'datetime-local'
+	| 'email'
+	| 'number'
+	| 'password'
+	| 'tel'
+	| 'text'
+	| 'time'
+	| 'search'
+	| 'url';
+
+export type TextControlProps<
+	Type extends TextControlPropsType = 'text',
```

updated

### Review thread 3

#### DaniGuardiola on 2024-08-10T09:44:06Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1712614169
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
+	| 'date'
+	| 'datetime-local'
+	| 'email'
+	| 'number'
+	| 'password'
+	| 'tel'
+	| 'text'
+	| 'time'
+	| 'search'
+	| 'url';
+
+export type TextControlProps<
+	Type extends TextControlPropsType = 'text',
+	Value extends string | number = Type extends 'number' ? number : string,
```

It might be better to make this a generic type (e.g. `ValueFromType<T>` and then use it in place of the `NoInfer<Value>`'s. The value type is not really a type argument, but a computed type from "Type", which is the actual type argument.

This would also allow passing a Type explicitly without the need to pass a Value (there's no partial inference). Why you'd want to do that? I have no idea. But it's possible, so...

---

Edit: now that I think about it, since it has a default it might work if omitted, but I think the general point still stands

---

Plus this allows an invalid combination, e.g. type text and value number. It could be fixed by repeating the condition in the extends clause, but I think it's cleaner to just compute it in each prop with a utility generic type as I suggested.

#### meteorlxy on 2024-08-13T09:46:03Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715004438
Path: packages/components/src/text-control/types.ts

```diff
@@ -3,7 +3,22 @@
  */
 import type { BaseControlProps } from '../base-control/types';
 
-export type TextControlProps = Pick<
+export type TextControlPropsType =
+	| 'date'
+	| 'datetime-local'
+	| 'email'
+	| 'number'
+	| 'password'
+	| 'tel'
+	| 'text'
+	| 'time'
+	| 'search'
+	| 'url';
+
+export type TextControlProps<
+	Type extends TextControlPropsType = 'text',
+	Value extends string | number = Type extends 'number' ? number : string,
```

updated

### Review thread 4

#### mirka on 2024-08-12T19:59:00Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1714302106
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

I think this assumption is wrong 🤔 The value that can be retrieved from the change event of an `input` will always be a `string`, as far as I can tell. Even if the type is number-like, e.g.`number` or `range`. The types for `HTMLInputElement` confirm this as well.

<img width="658" alt="event.target.value has string type" src="https://github.com/user-attachments/assets/f0e02484-665a-4a1a-a074-ca9422087e1a">

#### DaniGuardiola on 2024-08-12T20:04:55Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1714307993
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

I was wondering about this, actually. I had assumed that maybe React does something special with the number input to make it work directly with numbers, but it seems like that assumption is wrong. Unfortunately, that means the PR does not make sense and should be closed.

#### meteorlxy on 2024-08-13T09:44:59Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715003056
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

Ummm 🤔 Not sure if you have noticed it... (assume that you ignored it?)

In fact it's updated to make use of the `valueAsNumber` property for `type="number"`:

<img width="1433" alt="image" src="https://github.com/user-attachments/assets/4d921190-c6aa-427b-ae6f-78a153d40b7e">

It should work well, and should have great browser support: https://caniuse.com/mdn-api_htmlinputelement_valueasnumber

<img width="1264" alt="image" src="https://github.com/user-attachments/assets/0efacc49-5fe6-461e-bf35-b1945ecc2ae3">

#### meteorlxy on 2024-08-13T10:15:23Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715043958
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

Just added unit tests for this in https://github.com/WordPress/gutenberg/pull/64293/commits/c2e292ac7fffe79119ea2b535c19ccc816b2a1d0

cc @mirka @DaniGuardiola

#### tyxla on 2024-08-13T10:47:59Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715083597
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

Confirming that `valueAsNumber` browser support is good.

We should still make sure to support a `NaN` case, as it appears to be a potential return value (see [docs](https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement#instance_properties)):

> `valueAsNumber`
> A number that represents the value of the element, interpreted as one of the following, in order: A time value, a number, or `NaN` if conversion is impossible.

A unit test to cover that scenario would be useful.

#### mirka on 2024-08-13T11:25:59Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715131828
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

Oh, sorry 🤦 But then this is a breaking change, and I don’t think we can ship it on such a widely used and established component, no? I can’t say this is unambiguously a “bug fix” rather than an enhancement.

#### DaniGuardiola on 2024-08-13T13:14:36Z

URL: https://github.com/WordPress/gutenberg/pull/64293#discussion_r1715273198
Path: packages/components/src/text-control/types.ts

```diff
@@ -14,27 +29,17 @@ export type TextControlProps = Pick<
 	/**
 	 * A function that receives the value of the input.
 	 */
-	onChange: ( value: string ) => void;
+	onChange: ( value: NoInfer< Value > ) => void;
```

Yep, this is a big breaking change. I don't think there's nothing to do here unfortunately. At most expose the valueAsNumber attribute and make the generic depend on it. That seems quite convoluted though.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/text-control/index.tsx (MODIFIED, +20/-6)
- packages/components/src/text-control/stories/index.story.tsx (MODIFIED, +1/-1)
- packages/components/src/text-control/test/text-control.tsx (MODIFIED, +90/-0)
- packages/components/src/text-control/types.ts (MODIFIED, +20/-14)
