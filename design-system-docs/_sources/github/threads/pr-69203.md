---
number: 69203
type: pull_request
state: MERGED
merged_at: 2025-04-25T07:35:14Z
url: https://github.com/WordPress/gutenberg/pull/69203
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #69203: ColorPicker: Add tests for `ColorPicker` Alpha slider

- URL: https://github.com/WordPress/gutenberg/pull/69203
- Author: im3dabasia
- Created: 2025-02-14T16:17:24Z
- Updated: 2025-05-07T06:44:06Z
- Comments: 9 of 9
- Review threads: 9 of 9
- Changed files: 2 of 2
- Diff: ../pr-diffs/69203.diff

## Body

## What?
Closes https://github.com/WordPress/gutenberg/issues/49344
Related comment: https://github.com/WordPress/gutenberg/pull/49214#discussion_r1146267260

Related PRs: https://github.com/WordPress/gutenberg/pull/49214, https://github.com/WordPress/gutenberg/pull/49698

This PR improves the unit tests for the `ColorPicker` component by adding test coverage for the Alpha slider and its corresponding input field when `enableAlpha`. Previously, tests covered most input fields (hex, HSL, RGB, and hue slider), but the Alpha slider had no dedicated tests.


## Why?

The maintainers have emphasized that these tests are important for the editor. While previous tests validated various input interactions, they did not:

- Ensure that the Alpha slider updates correctly when the input field is changed.
- Cover `enableAlpha={true}`, leaving a gap in test coverage.

## How?

- Added tests to check that the Alpha slider updates when the user types in the Alpha input field and when adjusting the slider.
- Verified that the correct RGBA/HSLA values are passed to onChange when changing transparency via both the input field and the slider.
- Used user-event for more accurate simulation of real user interactions.

## Testing Instructions
1. Run the test by the following command:

```shell
npm run test:unit packages/components/src/color-picker/test/index.tsx
```
2. Check that the new test cases for the Alpha slider and input field pass.
3. Confirm that the test verifies the expected value in onChange when modifying alpha.
4. Review the test implementation to ensure coverage of both slider and input interactions.


## Screenshot

<img width="919" alt="image" src="https://github.com/user-attachments/assets/fd8b17c5-aecb-4889-9284-3a96b68ae232" />

## Issue comments

### github-actions on 2025-02-14T16:17:53Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2659749626

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: im3dabasia <im3dabasia1@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### im3dabasia on 2025-03-31T06:13:47Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2765238708

Hi @ciampo,

Since you had proposed adding alpha tests to the `ColorPicker` component, I've implemented those changes. Could you please review my PR when you have a moment?

CC: @t-hamano

### im3dabasia on 2025-04-09T11:16:21Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2789338489

Hi @ciampo , When you have a moment please review this PR. I have incorporated the changes that you had requested for. 

Looking forward!

### im3dabasia on 2025-04-16T06:06:17Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2808473605

> LGTM 🚀
> 
> Good to merge once a CHANGELOG entry is added

Thanks for the many iterations @ciampo . 

I have added an entry in the CHANGELOG.md in this https://github.com/WordPress/gutenberg/pull/69203/commits/9258a6b8e56f02969054632b78b5d8d9d3612ce6. 

When you a moment, please have a look at it. Lmk if any further changes need to made before 🚢

### ciampo on 2025-04-16T09:00:54Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2808890221

It's looking good! The PR will merge as soon as CI checks are completed

### im3dabasia on 2025-04-16T10:06:51Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2809076585

They unit tests appear to be failing. I'll rebase the PR. After this https://github.com/WordPress/gutenberg/pull/69912 , The failing tests should pass.

### im3dabasia on 2025-04-17T05:41:16Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2811811630

@ciampo , Thanks for https://github.com/WordPress/gutenberg/pull/69203/commits/36f90e1e077f74bdaa91f8bf5dd90d3260cb4cd4. 

I guess we can now ship it 🚢  . All CI tests are passing 🚀

### Mamaduka on 2025-04-25T07:03:06Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2829546445

Can we merge this? @im3dabasia, @ciampo

### im3dabasia on 2025-04-25T07:05:54Z

URL: https://github.com/WordPress/gutenberg/pull/69203#issuecomment-2829551741

> Can we merge this? @im3dabasia, @ciampo

Yes @Mamaduka , We can. Actually @ciampo had enabled auto-merge before but due to some CI checks failing we rebased this PR. 


Now since all CI checks are passing we can merge this PR to trunk . No issues

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2025-04-04T09:02:00Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028406199
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
```

Can we use `ColorPicker` directly, instead of creating `ControlledColorPicker`?

#### im3dabasia on 2025-04-04T10:18:25Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028541011
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
```

Thanks for the suggestion here. But I think using a controlled component would make sense here. Since when working with sliders I am using the fireEvent.change which would require a rerender, using a controlled env would allow us to have that color state in the parent component and this would cause a rerender in the component. Following this, the slider value updates would be rerendered.

One of the above test cases uses the similar pattern when working with slider related test cases. I tried using ColorPicker directly but didn't get success working on it.

This is the case:
https://github.com/WordPress/gutenberg/blob/e69bc532b0f7dccda14c77ff157d07f7e8bf19e5/packages/components/src/color-picker/test/index.tsx#L142-L177

#### ciampo on 2025-04-04T12:23:06Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028713213
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
```

> the fireEvent.change which would require a rerender

Can you explain this aspect a bit more? I don't think I'm aware of this aspect.

Have you tried without the controlled component, and following the order suggested in https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028423160?

#### im3dabasia on 2025-04-04T13:33:23Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028826161
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
```

> Have you tried without the controlled component, and following the order suggested in https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028423160


Firstly @ciampo. Thank you for the feedback and continuous iterations.

Ok so after working on this for some time, I can make the following observations. 

1. Without the controlled component

      a) **Only AlphaInput (ie. the input box)**: Here the tests work perfectly fine. I am able to clear the existing values, update the new values and also verify the updated values. However, the slider does not get updated it remains 50 as it is, only the AlphaInput gets updated to the new value.
      
      b) **Only AlphaSlider**: the test did not get through this line of code`fireEvent.change( alphaSlider, { target: { value: 75 } } );`,  the onChange is triggered after this line but the the values are not updated in the alphaSlider input element in the DOM.  I checked both just to confirm neither AlphaSlider is updated not AlphaInput gets updated.

All in all the AlphaSlider does not work in a uncontrolled environment for me.



<details>
<summary>Sharing a small snippet  </summary>

```
it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
	const user = userEvent.setup();
	const onChange = jest.fn();

	render(
		<ColorPicker
			onChange={ onChange }
			enableAlpha
			color="#ffffff80"
		/>
	);

	const formatSelector = screen.getByRole( 'combobox' );
	expect( formatSelector ).toBeVisible();
	await user.selectOptions( formatSelector, format );

	const alphaInput = screen.getByRole( 'spinbutton', {
		name: 'Alpha',
	} );
	expect( alphaInput ).toBeVisible();

	const alphaSliders = screen.getAllByRole( 'slider', {
		name: 'Alpha',
	} );

	const alphaSlider = alphaSliders.at( -1 )!;

	expect( alphaSlider ).toHaveValue( '50' );
	expect( alphaInput ).toHaveValue( 50 );

	await waitFor( () => {
		expect( onChange ).toHaveBeenCalledTimes( 0 ); // Returns 0 : Pass: expected it to be called 0
	} );

	fireEvent.change( alphaSlider, { target: { value: 75 } } );

	await waitFor( () => {
		expect( onChange ).toHaveBeenCalledTimes( 1 ); // Returns 1 : Pass: the onChange is called once as expected
	} );

	await waitFor( () => {
		expect( alphaSlider ).toHaveValue( '75' ); // Returns 50 : Fail : the value is not updated in the slider 
	} );
} );
```
</details>

2. With the controlled component

a) Here both the AlphaSlider and AlphaInput work as expected updating either of them causes the other to be updated and this works well.

#### ciampo on 2025-04-07T16:18:07Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2031589591
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
```

Got it — thank you for the detailed explanation 🙏 . Let's keep using the controlled component, but maybe:

- let's extract the controlled component at the start of the file, instead of re-creating it in different tests
- let's add code comments to explain why we're using a controlled component

### Review thread 2

#### ciampo on 2025-04-04T09:03:02Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028407670
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

Why don't we use `getByRole` instead of `getAllByRole` ? If we did that, there wouldn't be a need for the `at( -1 )` call

I see that there are two "alpha" sliders, which is unfortunate, although only the second one (the one that we need for the test) seems to be an actual `input[type="range"]`

#### im3dabasia on 2025-04-04T10:28:11Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028553268
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

The reason I could not use `getByRole` is because as you correctly identified there are 2 Alpha sliders for HSL and RGB color formats. That is why I used this work around  `getAllByRole` followed by `at( -1 )`. 

This is similar to this case here, where HSL has 2 Hue sliders rendered 

https://github.com/WordPress/gutenberg/blob/e69bc532b0f7dccda14c77ff157d07f7e8bf19e5/packages/components/src/color-picker/test/index.tsx#L184-L191

#### ciampo on 2025-04-04T12:17:52Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028701941
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

Sounds like we could open an issue to work on an improvement here — grouping the HSLA and RGBA inputs in a fieldset (or similar) for better labelling and discoverability.

![Screenshot 2025-04-04 at 14 16 36](https://github.com/user-attachments/assets/1cce6f03-caa9-4bcc-b594-223d136e141a)

Would you be able to open an issue about it? Thank you!

#### im3dabasia on 2025-04-17T05:59:39Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2048287842
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

@ciampo, could you please share a bit more about what issue we should open? Just need some clarification. Thanks!

#### im3dabasia on 2025-05-02T11:58:53Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2071511172
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

Hi for this issue I researched and can say that there are 2 ways we can work on this 

Currently in trunk the code is returning a fragment like shown in the snippet down below.


<details>

<summary>Code in trunk with fragments being returned as parent </summary>

https://github.com/WordPress/gutenberg/blob/72c922b679cbc0d83220514165ea66837de131ca/packages/components/src/color-picker/hsl-input.tsx#L63-L106

</details>





1. We can remove the fragments and add a div like this instead of the fragments. This pattern is visible in places like the [duotone-picker/color-list-picker](https://github.com/WordPress/gutenberg/blob/72c922b679cbc0d83220514165ea66837de131ca/packages/components/src/duotone-picker/color-list-picker/index.tsx#L52-L57) , [link-control](https://github.com/im3dabasia/gutenberg/blob/72c922b679cbc0d83220514165ea66837de131ca/packages/block-editor/src/components/link-control/link-preview.js#L98-L109). 

```html
<div group="role" aria-label="HSL Inputs"> 
    <InputWithSlider/>
    <InputWithSlider/>
    <InputWithSlider/>
</div>
```

2. Another approach is use a `VStack`. VStack has spacing prop which we can use to handle the spacing between the `InputWithSlider`.  A spacing of 2 would match the current column gaps of 8px which is (4px *2). Whereas in the custom div approach we would have to manage the gap by adding additional CSS class and the properties.

```html
<VStack spacing="2"> 
    <InputWithSlider/>
    <InputWithSlider/>
    <InputWithSlider/>
</VStack>
```

I would like to hear your thoughts on which approach would be better.

cc: @ciampo , @t-hamano , @Mamaduka

#### t-hamano on 2025-05-05T05:19:54Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2072853894
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

We probably need to decide first what the issues are and what the ideal structure would be in terms of accessibility.

The current problem of the `ColorPicker` component is that when the color type is "HSL", the slider for changing the overall color and the slider for changing only the "H" value are both labeled "HSL", so screen reader users won't know if they're editing the overall color or just the H value. And as far as I know, the current testing library doesn't offer an option to distinguish between the two using `getByRole`. Is this correct?

![color-picker](https://github.com/user-attachments/assets/b0e63899-e542-408c-b7b9-03e127b4ecde)

In other words, the DOM tree is structured as follows:

- slider "Color"
- slider "Hue”: This control is for the overall color
- slider "Alpha"
- combobox "Color format"
- button: "Copy"
- spinbutton "Hue"
- slider "Hue": This control is for the "H" value
- spinbutton "Saturation"
- slider "Saturation"
- spinbutton "Lightness"
- slider "Lightness"

I'm not sure if this is acceptable from an accessibility perspective, but how about differentiating the labels? For example, by changing the latter "Hue" label to "Hue (HSL H value)".

If this isn't ideal, maybe we could group the H, S, L inputs as you say.

cc @joedolson @afercia @alexstine

#### im3dabasia on 2025-05-07T06:44:06Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2076907547
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
```

Thanks @t-hamano , While others chip in their suggestions, what you said is possible we can do that as well in that scenario we need to change 2 labels ie. 

1. "Hue" label to "Hue (HSL H value)". This would give us a new label for both RGB/HSL for the top input slider. 
2. Secondly when alpha (opacity) is enabled we have 2 opacity sliders which once again cause a conflict so for this as well we need to change the label. 

 Since this issue is anyway closed i'm thinking to open a PR for the same and continue the discussions there?

<details>
<summary> Screenshots of Alpha label</summary>

<img width="1167" alt="Screenshot 2025-05-07 at 12 10 42 PM" src="https://github.com/user-attachments/assets/33d74b67-db68-4fec-8eb4-ea9d7e45b3d7" />

<img width="1172" alt="Screenshot 2025-05-07 at 12 09 43 PM" src="https://github.com/user-attachments/assets/6ce2d7c3-53bb-4f9e-aa23-4ecabc420f6a" />

</details>

### Review thread 3 (resolved)

#### ciampo on 2025-04-04T09:03:51Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028408766
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
+
+				expect( alphaSlider ).toHaveValue( '50' );
+				expect( alphaInput ).toHaveValue( Number( '50' ) );
```

Personal preferences, but I'd prefer `50` instead of `Number( '50' )`. Here and everywhere else throughout the file

### Review thread 4 (resolved)

#### ciampo on 2025-04-04T09:11:08Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028423160
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
+
+				expect( alphaSlider ).toHaveValue( '50' );
+				expect( alphaInput ).toHaveValue( Number( '50' ) );
+
+				fireEvent.change( alphaSlider, {
+					target: { value: alphaValue },
+				} );
+				expect( onChange ).not.toHaveBeenCalled();
+
+				await waitFor( () => {
+					expect( alphaInput ).toHaveValue( Number( alphaValue ) );
+				} );
+
+				await waitFor( () => {
+					expect( alphaSlider ).toHaveValue( alphaValue );
+				} );
```

I think the test here would be more clear if:

- slider event triggers
- we `waitFor` `onChange` to be called
- we check that both the slider and the number input have updated values

The same idea / order would apply when typing in the number input field

#### im3dabasia on 2025-04-09T09:39:43Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2034956368
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
+
+				expect( alphaSlider ).toHaveValue( '50' );
+				expect( alphaInput ).toHaveValue( Number( '50' ) );
+
+				fireEvent.change( alphaSlider, {
+					target: { value: alphaValue },
+				} );
+				expect( onChange ).not.toHaveBeenCalled();
+
+				await waitFor( () => {
+					expect( alphaInput ).toHaveValue( Number( alphaValue ) );
+				} );
+
+				await waitFor( () => {
+					expect( alphaSlider ).toHaveValue( alphaValue );
+				} );
```

`fireEvent.change` triggers the onChange but this as you pointed out needs to be checked by wrapping the expect callback in a `waitFor()`. Did this and it worked like a charm. 

For both the slider and the input i have now followed the above pattern

### Review thread 5 (resolved)

#### ciampo on 2025-04-04T09:17:43Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2028433004
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
+
+				expect( alphaSlider ).toHaveValue( '50' );
+				expect( alphaInput ).toHaveValue( Number( '50' ) );
+
+				fireEvent.change( alphaSlider, {
+					target: { value: alphaValue },
+				} );
+				expect( onChange ).not.toHaveBeenCalled();
+
+				await waitFor( () => {
+					expect( alphaInput ).toHaveValue( Number( alphaValue ) );
+				} );
+
+				await waitFor( () => {
+					expect( alphaSlider ).toHaveValue( alphaValue );
+				} );
+
+				await user.clear( alphaInput );
+				await user.type( alphaInput, alphaValue );
+
+				expect( alphaInput ).toHaveValue( Number( alphaValue ) );
+
+				expect( onChange ).toHaveBeenCalledTimes( 3 );
```

Let's add the `expect( onChange ).toHaveBeenCalledTimes()` assertion in more places in the test:

- every time it gets called — ie. `.toHaveBeenCalledTimes( 1 )`, `.toHaveBeenCalledTimes( 2 )`, ...
- together with that assertion, it's also useful to add `toHaveBeenLastCalledWith(...)`

For example, my expectations is:

- no calls on initial render
- no calls when switching color space
- first call when clicking on the slider
- assuming the `clear` changes alpha from `75` to `` in one shot, second call when clearing
- assuming that `type` is realistic, third call when user types `7` and fourth call when user types `5`

If calling `fireEvent.change` doesn't trigger the `onChange` callback to fire, we may need to investigate that separately — it's potentially a shortcoming of `testing-library`

#### im3dabasia on 2025-04-09T09:50:25Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2034987132
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +342,85 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL', '75', '#ffffff12' ],
+		[ 'rgb', 'RGB', '75', '#ffffff12' ],
+	] )(
+		'Alpha-enabled %s format',
+		( format, formatLabel, alphaValue, expected ) => {
+			it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+				const user = userEvent.setup();
+				const onChange = jest.fn();
+
+				const ControlledColorPicker = ( {
+					onChange: onChangeProp,
+					...restProps
+				}: React.ComponentProps< typeof ColorPicker > ) => {
+					const [ colorState, setColorState ] =
+						useState( '#ffffff80' );
+
+					const internalOnChange: typeof onChangeProp = (
+						newColor
+					) => {
+						onChangeProp?.( newColor );
+						setColorState( newColor );
+					};
+
+					return (
+						<>
+							<ColorPicker
+								{ ...restProps }
+								onChange={ internalOnChange }
+								color={ colorState }
+							/>
+						</>
+					);
+				};
+
+				render(
+					<ControlledColorPicker onChange={ onChange } enableAlpha />
+				);
+
+				const formatSelector = screen.getByRole( 'combobox' );
+				expect( formatSelector ).toBeVisible();
+				await user.selectOptions( formatSelector, format );
+
+				const alphaInput = screen.getByRole( 'spinbutton', {
+					name: 'Alpha',
+				} );
+				expect( alphaInput ).toBeVisible();
+
+				const alphaSliders = screen.getAllByRole( 'slider', {
+					name: 'Alpha',
+				} );
+
+				const alphaSlider = alphaSliders.at( -1 )!;
+
+				expect( alphaSlider ).toHaveValue( '50' );
+				expect( alphaInput ).toHaveValue( Number( '50' ) );
+
+				fireEvent.change( alphaSlider, {
+					target: { value: alphaValue },
+				} );
+				expect( onChange ).not.toHaveBeenCalled();
+
+				await waitFor( () => {
+					expect( alphaInput ).toHaveValue( Number( alphaValue ) );
+				} );
+
+				await waitFor( () => {
+					expect( alphaSlider ).toHaveValue( alphaValue );
+				} );
+
+				await user.clear( alphaInput );
+				await user.type( alphaInput, alphaValue );
+
+				expect( alphaInput ).toHaveValue( Number( alphaValue ) );
+
+				expect( onChange ).toHaveBeenCalledTimes( 3 );
```

> If calling fireEvent.change doesn't trigger the onChange callback to fire, we may need to investigate that separately — it's potentially a shortcoming of testing-library

`fireEvent.change` does trigger the onChange, but we have to add `waitFor()`.

> 
> For example, my expectations is:
> 
> no calls on initial render
> no calls when switching color space
> first call when clicking on the slider
> assuming the clear changes alpha from 75 to `` in one shot, second call when clearing
> assuming that type is realistic, third call when user types 7 and fourth call when user types 5


To further make this interesting I have gone one step deeper by checking the value of which onChange is called with when user has typed 7, and when user has typed 75. This here will check for the deepest of details and make our tests robust. further I have now added `toHaveBeenCalledTimes`() in multiple places

### Review thread 6 (resolved)

#### ciampo on 2025-04-14T12:40:39Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2042058601
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
```

```suggestion
			expect( onChange ).not.toHaveBeenCalled();
```

### Review thread 7 (resolved)

#### ciampo on 2025-04-14T12:42:48Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2042061967
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
+
+			await waitFor( () => {
+				expect( alphaInput ).toHaveValue( 75 );
+			} );
+			await waitFor( () => {
+				expect( alphaSlider ).toHaveValue( '75' );
+			} );
```

We probably don't need to wrap these 3 assertions in `await waitFor`. Once we `await` for `onChange` to have been called, the rest of the assertions can already be checked without the need for waiting:

```suggestion
			expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
			
			expect( alphaInput ).toHaveValue( 75 );

			expect( alphaSlider ).toHaveValue( '75' );
```

### Review thread 8 (resolved)

#### ciampo on 2025-04-14T12:47:32Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2042069682
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
```

Personal preference, but I always prefer to use `toHaveBeenLastCalledWith` ([docs](https://jestjs.io/docs/expect#tohavebeenlastcalledwitharg1-arg2-))

### Review thread 9 (resolved)

#### ciampo on 2025-04-14T12:49:57Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2042073801
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
+
+			await waitFor( () => {
+				expect( alphaInput ).toHaveValue( 75 );
+			} );
+			await waitFor( () => {
+				expect( alphaSlider ).toHaveValue( '75' );
+			} );
+
+			onChange.mockClear();
+
+			// Test pattern 2: Update the alphaInput
+			await user.clear( alphaInput );
+			expect( onChange ).toHaveBeenCalledTimes( 1 );
+
+			// Initially type 7 in the alpha input, we expect it to be called with #ffffff12
+			await user.type( alphaInput, '7' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 2 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffff12' );
+			} );
+
+			// Now with 75% opacity we expect it to be called with #ffffffbf
+			await user.type( alphaInput, '5' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 3 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
```

`user.type` should already simulate distinct keypresses for each character of the string ([docs](https://testing-library.com/docs/user-event/utility/#type))

I wonder if we could change the test to something like:

```suggestion
			await user.type( alphaInput, '75' );
			
			await waitFor( () => {
				// Called twice, once per key stroke (`7` and `5`)
				expect( onChange ).toHaveBeenCalledTimes( 3 );
			} );

			// Called first with `5` and then `75` % opacity
			expect( onChange ).toHaveBeenNthCalledWith(2, '#ffffff12');
			expect( onChange ).toHaveBeenNthCalledWith(3, '#ffffffbf');
```

#### im3dabasia on 2025-04-15T10:02:56Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2044157724
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
+
+			await waitFor( () => {
+				expect( alphaInput ).toHaveValue( 75 );
+			} );
+			await waitFor( () => {
+				expect( alphaSlider ).toHaveValue( '75' );
+			} );
+
+			onChange.mockClear();
+
+			// Test pattern 2: Update the alphaInput
+			await user.clear( alphaInput );
+			expect( onChange ).toHaveBeenCalledTimes( 1 );
+
+			// Initially type 7 in the alpha input, we expect it to be called with #ffffff12
+			await user.type( alphaInput, '7' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 2 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffff12' );
+			} );
+
+			// Now with 75% opacity we expect it to be called with #ffffffbf
+			await user.type( alphaInput, '5' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 3 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
```

Thanks for the feedback @ciampo .

Well, I tried this earlier as well. 

But when actually implementing this, I notice a really weird behavior. If we type a 3-digit number, let's say 100, then the alphaInput would have all 3 digits (i.e., 100). But if we type a 2-digit number like 75 or any other, then only the first digit is received.

for a 2-digit number: user.type( alphaInput, '75' ) triggers onChange 1 time ie only for 7


The problem is weird. I tried debugging for an hour or so, but I don't know why this happens. The same user.type has been used in numerous places in the codebase; there it works as expected. I'm not sure what could be causing this problem.

I tried multiple different approaches like adding a timeout to give enough time for the type event to complete, added `waitFor(() => {})`, etc. None of them worked correctly.


This snippet works well, but its based on `fireEvent` and not user.type event. 

```js
fireEvent.change( alphaInput, { target: { value: '75' } } );

await waitFor( () => {
expect( onChange ).toHaveBeenCalledTimes( 2 );
} );
expect( onChange ).toHaveBeenNthCalledWith( 2, '#ffffffbf' );

expect( alphaSlider ).toHaveValue( '75' );

expect( alphaInput ).toHaveValue( 75 );
```

I would appreciate your feedback on this issue, and help if you have faced this kind of problem before.

#### im3dabasia on 2025-04-15T10:11:14Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2044174279
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
+
+			await waitFor( () => {
+				expect( alphaInput ).toHaveValue( 75 );
+			} );
+			await waitFor( () => {
+				expect( alphaSlider ).toHaveValue( '75' );
+			} );
+
+			onChange.mockClear();
+
+			// Test pattern 2: Update the alphaInput
+			await user.clear( alphaInput );
+			expect( onChange ).toHaveBeenCalledTimes( 1 );
+
+			// Initially type 7 in the alpha input, we expect it to be called with #ffffff12
+			await user.type( alphaInput, '7' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 2 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffff12' );
+			} );
+
+			// Now with 75% opacity we expect it to be called with #ffffffbf
+			await user.type( alphaInput, '5' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 3 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
```

Currently I have added this code https://github.com/WordPress/gutenberg/pull/69203/commits/8a05ee167e4a5a3a4d5ae641f5ea06f547fa8252, it's as close as the above suggested snippet, barring the use of 2 `user.type()` for 7 and 5 individually.

#### ciampo on 2025-04-15T10:35:19Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2044221937
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
+
+			await waitFor( () => {
+				expect( alphaInput ).toHaveValue( 75 );
+			} );
+			await waitFor( () => {
+				expect( alphaSlider ).toHaveValue( '75' );
+			} );
+
+			onChange.mockClear();
+
+			// Test pattern 2: Update the alphaInput
+			await user.clear( alphaInput );
+			expect( onChange ).toHaveBeenCalledTimes( 1 );
+
+			// Initially type 7 in the alpha input, we expect it to be called with #ffffff12
+			await user.type( alphaInput, '7' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 2 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffff12' );
+			} );
+
+			// Now with 75% opacity we expect it to be called with #ffffffbf
+			await user.type( alphaInput, '5' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 3 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
```

That's weird — it's clearly not working as expected (other folks seem to [have related issues too](https://github.com/testing-library/user-event/issues/1150)).

Let's switch to using `user.keyboard` instead of `user.type`, at least we get the expected behavior:

```ts
			// Initially type 7 in the alpha input, we expect it to be called with #ffffff12
			await user.keyboard( '7' );

			// Now with 75% opacity we expect it to be called with #ffffffbf
			await user.keyboard( '5' );
```

#### im3dabasia on 2025-04-15T10:45:28Z

URL: https://github.com/WordPress/gutenberg/pull/69203#discussion_r2044236739
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -342,4 +345,89 @@ describe( 'ColorPicker', () => {
 			} );
 		} );
 	} );
+
+	describe.each( [
+		[ 'hsl', 'HSL' ],
+		[ 'rgb', 'RGB' ],
+	] )( 'Alpha-enabled %s format', ( format, formatLabel ) => {
+		it( `should update alpha correctly when ${ formatLabel } format is selected`, async () => {
+			const user = userEvent.setup();
+			const onChange = jest.fn();
+
+			render(
+				<ControlledColorPicker
+					onChange={ onChange }
+					enableAlpha
+					initialColor="#ffffff80"
+				/>
+			);
+
+			const formatSelector = screen.getByRole( 'combobox' );
+			expect( formatSelector ).toBeVisible();
+			await user.selectOptions( formatSelector, format );
+
+			const alphaInput = screen.getByRole( 'spinbutton', {
+				name: 'Alpha',
+			} );
+			expect( alphaInput ).toBeVisible();
+
+			const alphaSliders = screen.getAllByRole( 'slider', {
+				name: 'Alpha',
+			} );
+
+			expect( alphaSliders ).toHaveLength( 2 );
+
+			// Choose the second slider which is the actual slider of type: input[type="range"]
+			const alphaSlider = alphaSliders.at( -1 )!;
+
+			expect( alphaSlider ).toHaveValue( '50' );
+			expect( alphaInput ).toHaveValue( 50 );
+
+			expect( onChange ).not.toHaveBeenCalledWith();
+
+			// Test pattern 1: Update the slider
+			fireEvent.change( alphaSlider, {
+				target: { value: 75 },
+			} );
+
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledTimes( 1 );
+			} );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
+
+			await waitFor( () => {
+				expect( alphaInput ).toHaveValue( 75 );
+			} );
+			await waitFor( () => {
+				expect( alphaSlider ).toHaveValue( '75' );
+			} );
+
+			onChange.mockClear();
+
+			// Test pattern 2: Update the alphaInput
+			await user.clear( alphaInput );
+			expect( onChange ).toHaveBeenCalledTimes( 1 );
+
+			// Initially type 7 in the alpha input, we expect it to be called with #ffffff12
+			await user.type( alphaInput, '7' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 2 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffff12' );
+			} );
+
+			// Now with 75% opacity we expect it to be called with #ffffffbf
+			await user.type( alphaInput, '5' );
+
+			expect( onChange ).toHaveBeenCalledTimes( 3 );
+			await waitFor( () => {
+				expect( onChange ).toHaveBeenCalledWith( '#ffffffbf' );
+			} );
```

Works like a charm thanks for the quick input 🙇

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +5/-1)
- packages/components/src/color-picker/test/index.tsx (MODIFIED, +103/-26)
