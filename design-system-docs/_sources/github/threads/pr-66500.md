---
number: 66500
type: pull_request
state: MERGED
merged_at: 2024-10-28T16:06:10Z
url: https://github.com/WordPress/gutenberg/pull/66500
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components"]
---

# PR #66500: BaseControl: Auto-generate readme

- URL: https://github.com/WordPress/gutenberg/pull/66500
- Author: mirka
- Created: 2024-10-26T12:47:05Z
- Updated: 2024-10-28T16:06:59Z
- Comments: 1 of 1
- Review threads: 5 of 5
- Changed files: 6 of 6
- Diff: ../pr-diffs/66500.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?

Switch the README for BaseControl to an auto-generated one (see also #66035).

## How?

To extract subcomponent descriptions from the [main component's `Object.assign`](https://github.com/WordPress/gutenberg/blob/76a5ba19f87a8391e8a1365dcd9e1318d73e1458/packages/components/src/base-control/index.tsx#L143-L165), we need some custom parsing with Babel because `react-docgen-typescript` does not pick this up.

This is somewhat unfortunate because:

1. The subcomponent description does need to be inside the `Object.assign` for the IDE (at least in VS Code) to pick it up.
2. Storybook currently doesn't display the subcomponent descriptions anywhere, but if they start to do so in a future version, it may not be able to pick up our descriptions.

But... it's fine for now.

## Testing Instructions

1. Verify the changes to the Storybook for BaseControl.
2. `npm run docs:components` to verify the changes to the README.

## Issue comments

### github-actions on 2024-10-26T12:47:25Z

URL: https://github.com/WordPress/gutenberg/pull/66500#issuecomment-2439571506

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### mirka on 2024-10-26T12:50:39Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1817846178
Path: packages/components/src/base-control/stories/index.story.tsx

```diff
@@ -12,6 +12,10 @@ import Button from '../../button';
 const meta: Meta< typeof BaseControl > = {
 	title: 'Components/BaseControl',
 	component: BaseControl,
+	subcomponents: {
+		// @ts-expect-error - See https://github.com/storybookjs/storybook/issues/23170
+		'BaseControl.VisualLabel': BaseControl.VisualLabel,
+	},
```

This was missing in the subcomponents.

### Review thread 2

#### mirka on 2024-10-26T12:51:26Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1817846300
Path: packages/components/src/base-control/types.ts

```diff
@@ -49,5 +49,8 @@ export type BaseControlProps = {
 };
 
 export type BaseControlVisualLabelProps = {
+	/**
+	 * The content to be displayed within the `BaseControl.VisualLabel`.
+	 */
 	children: ReactNode;
```

Adding a description ensures that this prop is displayed explicitly as a required prop in the props table.

#### ciampo on 2024-10-28T09:42:33Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1818723868
Path: packages/components/src/base-control/types.ts

```diff
@@ -49,5 +49,8 @@ export type BaseControlProps = {
 };
 
 export type BaseControlVisualLabelProps = {
+	/**
+	 * The content to be displayed within the `BaseControl.VisualLabel`.
+	 */
 	children: ReactNode;
```

TIL

### Review thread 3

#### tyxla on 2024-10-28T12:31:10Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1818978038
Path: bin/api-docs/gen-components-docs/get-subcomponent-descriptions.mjs

```diff
@@ -0,0 +1,46 @@
+/**
+ * External dependencies
+ */
+import fs from 'node:fs/promises';
+import babel from '@babel/core';
+import { parse as commentParser } from 'comment-parser';
+
+/**
+ * Try to get subcomponent descriptions from the main component Object.assign() call.
+ */
+export async function getDescriptionsForSubcomponents(
+	filePath,
+	mainComponentName
+) {
+	const fileContent = await fs.readFile( filePath, 'utf8' );
+	const parsedFile = babel.parse( fileContent, {
```

I guess we're planning to rely on the system errors coming from node / babel in case of an error? 

We can improve this if necessary as we go, of course.

### Review thread 4

#### tyxla on 2024-10-28T12:32:17Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1818979428
Path: bin/api-docs/gen-components-docs/index.mjs

```diff
@@ -91,10 +94,29 @@ await Promise.all(
 					docs.displayName = subcomponent.preferredDisplayName;
 				}
 
+				if ( ! subcomponent.description ) {
+					subcomponentDescriptions ??=
```

Nice use for nullish coalescing assignment 👍

### Review thread 5

#### tyxla on 2024-10-28T12:32:36Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1818979853
Path: bin/api-docs/gen-components-docs/index.mjs

```diff
@@ -91,10 +94,29 @@ await Promise.all(
 					docs.displayName = subcomponent.preferredDisplayName;
 				}
 
+				if ( ! subcomponent.description ) {
+					subcomponentDescriptions ??=
+						getDescriptionsForSubcomponents(
+							path.resolve(
+								path.dirname( manifestPath ),
+								manifest.filePath
+							),
+							manifest.displayName
+						);
+
+					docs.description = ( await subcomponentDescriptions )?.[
```

Should we default to an empty string here instead of `undefined`?

#### mirka on 2024-10-28T16:05:18Z

URL: https://github.com/WordPress/gutenberg/pull/66500#discussion_r1819341141
Path: bin/api-docs/gen-components-docs/index.mjs

```diff
@@ -91,10 +94,29 @@ await Promise.all(
 					docs.displayName = subcomponent.preferredDisplayName;
 				}
 
+				if ( ! subcomponent.description ) {
+					subcomponentDescriptions ??=
+						getDescriptionsForSubcomponents(
+							path.resolve(
+								path.dirname( manifestPath ),
+								manifest.filePath
+							),
+							manifest.displayName
+						);
+
+					docs.description = ( await subcomponentDescriptions )?.[
```

Subcomponent descriptions aren't required, so `undefined` should be easier to handle downstream.

## Changed files

- bin/api-docs/gen-components-docs/get-subcomponent-descriptions.mjs (ADDED, +46/-0)
- bin/api-docs/gen-components-docs/index.mjs (MODIFIED, +26/-4)
- packages/components/src/base-control/README.md (MODIFIED, +58/-47)
- packages/components/src/base-control/docs-manifest.json (ADDED, +12/-0)
- packages/components/src/base-control/stories/index.story.tsx (MODIFIED, +4/-0)
- packages/components/src/base-control/types.ts (MODIFIED, +3/-0)
