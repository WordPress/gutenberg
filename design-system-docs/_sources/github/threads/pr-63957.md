---
number: 63957
type: pull_request
state: MERGED
merged_at: 2024-07-26T15:57:36Z
url: https://github.com/WordPress/gutenberg/pull/63957
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Type] Regression","[Package] Components"]
---

# PR #63957: CustomSelectControl: Restore `describedBy` functionality

- URL: https://github.com/WordPress/gutenberg/pull/63957
- Author: mirka
- Created: 2024-07-25T18:52:03Z
- Updated: 2024-07-26T15:58:04Z
- Comments: 2 of 2
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/63957.diff

## Body

Fixes #63568

## What?

Restores the `describedBy` functionality in CustomSelectControl (v1) that was broken when we switched the underlying implementation in #63258.

## How?

See code comments.

## Testing Instructions

New unit tests should pass. You can also manually inspect the rendered HTML in Storybook.

## Issue comments

### github-actions on 2024-07-25T19:01:47Z

URL: https://github.com/WordPress/gutenberg/pull/63957#issuecomment-2251207803

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: afercia <afercia@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### afercia on 2024-07-26T12:48:43Z

URL: https://github.com/WordPress/gutenberg/pull/63957#issuecomment-2252695385

Please remember to manually add props to @rohitmathur-7 for the alternative PR at https://github.com/WordPress/gutenberg/pull/63884

## Review threads

### Review thread 1

#### mirka on 2024-07-25T18:53:07Z

URL: https://github.com/WordPress/gutenberg/pull/63957#discussion_r1691983136
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -153,23 +174,30 @@ function CustomSelectControl( props: CustomSelectProps ) {
 	} )();
 
 	return (
-		<_CustomSelect
-			aria-describedby={ describedBy }
-			renderSelectedValue={
-				showSelectedHint ? renderSelectedValueHint : undefined
-			}
-			size={ translatedSize }
-			store={ store }
-			className={ clsx(
-				// Keeping the classname for legacy reasons
-				'components-custom-select-control',
-				classNameProp
-			) }
-			isLegacy
-			{ ...restProps }
-		>
-			{ children }
-		</_CustomSelect>
+		<>
+			<_CustomSelect
+				aria-describedby={ descriptionId }
+				renderSelectedValue={
+					showSelectedHint ? renderSelectedValueHint : undefined
+				}
+				size={ translatedSize }
+				store={ store }
+				className={ clsx(
+					// Keeping the classname for legacy reasons
+					'components-custom-select-control',
+					classNameProp
+				) }
+				isLegacy
+				{ ...restProps }
+			>
+				{ children }
+			</_CustomSelect>
+			<VisuallyHidden>
+				<span id={ descriptionId }>
+					{ getDescribedBy( currentValue, describedBy ) }
+				</span>
+			</VisuallyHidden>
```

The original implementation passed the `describedBy` prop down to `Button`. Therefore, we want to replicate how Button rendered the `describedBy` text.

https://github.com/WordPress/gutenberg/blob/cad81e428aed155fa3e490cc9c8d9673a170c3fd/packages/components/src/button/index.tsx#L282-L284

### Review thread 2

#### mirka on 2024-07-25T18:55:55Z

URL: https://github.com/WordPress/gutenberg/pull/63957#discussion_r1691985946
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -35,6 +42,15 @@ function applyOptionDeprecations( {
 	};
 }
 
+function getDescribedBy( currentValue: string, describedBy?: string ) {
+	if ( describedBy ) {
+		return describedBy;
+	}
+
+	// translators: %s: The selected option.
+	return sprintf( __( 'Currently selected: %s' ), currentValue );
+}
```

This function copies the original logic, which looked like this:

https://github.com/WordPress/gutenberg/blob/9bb75b35ec0daa46e8cd60c46619f522c7edd453/packages/components/src/custom-select-control/index.js#L130-L141

I removed the case for no selection, because that won't happen in this Ariakit-based version.

### Review thread 3

#### mirka on 2024-07-25T18:58:50Z

URL: https://github.com/WordPress/gutenberg/pull/63957#discussion_r1691988930
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -48,8 +64,13 @@ function CustomSelectControl( props: CustomSelectProps ) {
 		...restProps
 	} = useDeprecatedProps( props );
 
+	const descriptionId = useInstanceId(
+		CustomSelectControl,
+		'custom-select-control__description'
+	);
+
 	// Forward props + store from v2 implementation
-	const store = Ariakit.useSelectStore( {
+	const store = Ariakit.useSelectStore< string >( {
```

The Ariakit store allows `string | string[]` because it can handle multiple selection. Since this v1 never supported multiple selection, let's narrow the type to just `string`. (This has the effect of narrowing the type of `store.getState().value` to string.)

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/custom-select-control/index.tsx (MODIFIED, +48/-20)
- packages/components/src/custom-select-control/test/index.tsx (MODIFIED, +22/-0)
