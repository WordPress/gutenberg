---
number: 14851
type: pull_request
state: MERGED
merged_at: 2019-08-02T01:46:22Z
url: https://github.com/WordPress/gutenberg/pull/14851
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Feature] UI Components","[Package] Components"]
---

# PR #14851: Components: Add onFocusOutside replacement to Popover onClickOutside

- URL: https://github.com/WordPress/gutenberg/pull/14851
- Author: aduth
- Created: 2019-04-05T20:32:30Z
- Updated: 2019-08-06T08:53:20Z
- Comments: 13 of 13
- Review threads: 4 of 4
- Changed files: 13 of 13
- Diff: ../pr-diffs/14851.diff

## Body

Fixes #14754, fixes #14849.

This pull request seeks to refactor popovers to avoid relying on "click outside" behaviors in popovers, deprecating the `Popover` component's `onClickOutside` prop in favor of a substitute `onFocusOutside`. This accounts for more reasons for focus transitions not previously reflected in `onClickOutside`, such as that described by the bug of #14754.

**Status:** While functionally complete, I'm considering this to be blocked by an apparent incompatibility with a tangentially-related bug described at #14849 . Notably, when toggling an option in the More Options menu, the menu will no longer become closed again from clicks elsewhere in the page.

**Testing instructions:**

Repeat Steps to Reproduce from #14754 , verifying that the menu is closed as expected.

Verify there are no changes in behavior from other Popover usage, particularly specialized cases such as:

- Clicking a DropdownMenu toggle button should toggle itself closed when currently opened
- Clicking a result in the link suggestions autocomplete does not dismiss the URL popover

## Issue comments

### gziolo on 2019-04-10T14:05:05Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-481705159

> Clicking a result in the link suggestions autocomplete does not dismiss the URL popover

There seems to be a different behavior when using mouse and keyboard. Well, there might be something wrong with the URL popover and autocomplete:
- when I use an arrow key, select one of the results and press `space` it doesn't get selected
- when I use an arrow key, select one of the results and press `enter` it gets selected and focus goes back to RichText field

### aduth on 2019-05-22T21:24:38Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-494979296

This one was a pain to rebase, in good part because of #15053 (cc @youknowriad). 

Worth noting that this will resolve some lingering errors after #11360 logged frequently in StrictMode concerning `findDOMNode` from `react-click-outside` (related: https://github.com/kentor/react-click-outside/issues/45). Also related to prior conversation https://github.com/WordPress/gutenberg/pull/14851#discussion_r273979181.

### aduth on 2019-07-29T18:36:22Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-516111764

Thanks @noisysocks for the assist here. Looking again over the code and with some final testing, I think this is in good shape for merge. 👍

### aduth on 2019-07-29T18:44:23Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-516114618

Hmm, the end-to-end test failures may be legitimate.

### aduth on 2019-07-29T19:04:00Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-516121338

The end-to-end tests seem to be getting stuck when trying to switch from "Code" editor back to "Visual" in the [container blocks test cases](https://github.com/WordPress/gutenberg/blob/master/packages/e2e-tests/specs/plugins/container-blocks.test.js). I can't see any reason why it would be such a specific issue here.

My hunches are:

- Something specific with the [plugin under test](https://github.com/WordPress/gutenberg/blob/master/packages/e2e-tests/plugins/inner-blocks-templates/index.js) and its template locking which is causing trouble?
- The implementation of [`withFocusOutside` using timeouts](https://github.com/WordPress/gutenberg/blob/d45c8ea9f3adafba76676a29f07f28dfd0f2d444/packages/components/src/higher-order/with-focus-outside/index.js#L81-L85) could be flawed and allow for race conditions

Separately, I stumbled upon this code, which was [introduced more recently](https://github.com/WordPress/gutenberg/pull/15570) and seems like something we might want to update:

https://github.com/WordPress/gutenberg/blob/d45c8ea9f3adafba76676a29f07f28dfd0f2d444/packages/block-library/src/image/edit.js#L208

### noisysocks on 2019-07-31T05:42:24Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-516704545

I figured out how to recreate the bug with some simple steps, but I'm no closer to figuring out what's causing it:

1. Create a new post
2. Open the _More tools & options_ menu
3. Click any menu item, e.g. enable _Spotlight mode_
4. Click outside of the menu to dismiss it. The menu does not close

Any thoughts, @aduth?

### noisysocks on 2019-08-01T04:46:46Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517117361

Figured it out! It's because clicking on the menu item causes a focus loss. This was reported in https://github.com/WordPress/gutenberg/issues/14849. I'll include a fix here.

### noisysocks on 2019-08-01T05:21:30Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517123898

I pushed up 738d987 which fixes the E2E test failures and fixes #14849. 

Would you mind giving this a quick test, @gziolo?

---

**Testing instructions:**

Verify that #14849 is fixed:

1. Navigate to Posts > Add New
2. Click or otherwise activate the "More Options" menu in the top-right
3. Press <kbd>Spacebar</kbd> (assumes first menu item focused)
4. Note focus loss

Verify there are no changes in behavior from other Popover usage, particularly specialized cases such as:

- Clicking a DropdownMenu toggle button should toggle itself closed when currently opened
- Clicking a result in the link suggestions autocomplete does not dismiss the URL popover

### gziolo on 2019-08-01T10:21:48Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517223958

@afercia, would you mind running some sanity check for the `Manage All Reusable Blocks` link in the menu. I have issues confirming that it gets announced as link as expected. I outputted its HTML content and it is still an anchor with role `menuitem` as expected but I can't confirm it with VoiceOVer.

Aside: yes, I still keep it in mind that we should have no links in the dropdown menu :) I need to find a related issue where @mapk proposes we move it to the Manage Blocks modal and tackle it at last.

Regardless, I think we should move forward with this PR given that it makes More Menu finally usable for keyboard users. The last step to make the experience good is to stop closing this menu  when opening modals 🎉

### gziolo on 2019-08-01T10:57:34Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517234635

This PR will also partially resolve https://github.com/WordPress/gutenberg/issues/15501. There is a remaining issue with focus loss when switching between Code and Visual editor when the post is empty.

### afercia on 2019-08-01T12:43:41Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517270665

Thanks for working on this! @gziolo I tested a bit and seems to me everything works as expected. Just one small thing: should the small horizontal "jump" of menuitems with the check icon be fixed in this PR? See animated GIF:

![jump](https://user-images.githubusercontent.com/1682452/62293383-c8cd3180-b468-11e9-877a-53f095d86fea.gif)

Regarding the way screen readers announce `Manage All Reusable Blocks`: it has a `menuitem` role so it's announced as a "menu button" (or similar, depending on the browser / screen reader). ARIA roles override the native role.

However, it still creates some issues. For example: in VoiceOver, this menuitem is not listed in the "Form Controls" list while the other ones are:

<img width="1141" alt="Screenshot 2019-08-01 at 13 40 40" src="https://user-images.githubusercontent.com/1682452/62293592-3ed19880-b469-11e9-82cb-a17bab4ceefd.png">

- open the More menu
- press Command Option U to open the VoiceOver "rotor"
- right / left arrows to go to the "Form Controls" panel
- down / up arrows to browse the list of form controls
- see that `Manage All Reusable Blocks` is missing

For clarity, menuitems can be links. However, an ARIA menu is supposed to be _either_ a menu of actions _or_ a navigation menu. When it's a navigation menu, it should be wrapped in a `<nav>` element, see the W3C example:

Menu or Menu bar design pattern:
https://www.w3.org/TR/wai-aria-practices-1.1/#menu

Navigation Menubar Example: 
https://www.w3.org/TR/wai-aria-practices-1.1/examples/menubar/menubar-1/menubar-1.html

> Since the menubar presents a site navigation system, it is wrapped in a navigation region implemented with a `nav` element that has an `aria-label` that matches the label on the menubar.

The point is that an ARIA menu _can't be_ a mix of the two things. In this case, all the menuitems perform an action in the current page except `Manage All Reusable Blocks`, which triggers navigation to another page. This is unexpected for users and there's no way to communicate it properly. See #13390

### gziolo on 2019-08-01T13:04:52Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517279136

> Thanks for working on this! @gziolo I tested a bit and seems to me everything works as expected. Just one small thing: should the small horizontal "jump" of menuitems with the check icon be fixed in this PR?

Good catch, it needs to be fixed, it would be a regression.

### noisysocks on 2019-08-02T01:46:07Z

URL: https://github.com/WordPress/gutenberg/pull/14851#issuecomment-517516238

Thanks for testing all!

> The point is that an ARIA menu can't be a mix of the two things. In this case, all the menuitems perform an action in the current page except Manage All Reusable Blocks, which triggers navigation to another page. This is unexpected for users and there's no way to communicate it properly. See #13390

Thanks for going into this. Let's address the issues with _Manage All Reusable Blocks_ as part of #13390. I agree we should remove the menu item altogether.

> Just one small thing: should the small horizontal "jump" of menuitems with the check icon be fixed in this PR?

This bug exists in `master`, so I'll create a separate PR to fix it. I'm keen to get this PR merged as it is blocking some other work.

## Review threads

### Review thread 1

#### gziolo on 2019-04-10T14:07:29Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r273979181
Path: packages/components/src/popover/detect-outside.js

```diff
@@ -1,24 +1,21 @@
 /**
- * External dependencies
+ * WordPress dependencies
  */
-import clickOutside from 'react-click-outside';
```

Unrelated question to this PR. Can we also refactor `Modal` component to stop using `react-click-outside`? :)

#### aduth on 2019-04-11T20:07:17Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r274628760
Path: packages/components/src/popover/detect-outside.js

```diff
@@ -1,24 +1,21 @@
 /**
- * External dependencies
+ * WordPress dependencies
  */
-import clickOutside from 'react-click-outside';
```

> Unrelated question to this PR. Can we also refactor `Modal` component to stop using `react-click-outside`? :)

I would like to, yes.

( https://github.com/WordPress/gutenberg/pull/6261#discussion_r191758392 😞 )

#### gziolo on 2019-04-12T07:06:19Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r274782237
Path: packages/components/src/popover/detect-outside.js

```diff
@@ -1,24 +1,21 @@
 /**
- * External dependencies
+ * WordPress dependencies
  */
-import clickOutside from 'react-click-outside';
```

There is still hope then :)

#### gziolo on 2019-04-12T07:10:49Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r274783440
Path: packages/components/src/popover/detect-outside.js

```diff
@@ -1,24 +1,21 @@
 /**
- * External dependencies
+ * WordPress dependencies
  */
-import clickOutside from 'react-click-outside';
```

https://unpkg.com/react-click-outside@3.0.1/index.js

Well, it looks like `react-click-outside` is a simplified version of our own HOC :)

#### gziolo on 2019-08-02T09:59:43Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r310065463
Path: packages/components/src/popover/detect-outside.js

```diff
@@ -1,24 +1,21 @@
 /**
- * External dependencies
+ * WordPress dependencies
  */
-import clickOutside from 'react-click-outside';
```

I opened a follow-up to address it – #16878.

### Review thread 2

#### gziolo on 2019-04-10T14:11:04Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r273981198
Path: packages/components/src/popover/index.js

```diff
@@ -94,6 +96,47 @@ class Popover extends Component {
 		this.toggleAutoRefresh( false );
 	}
 
+	/**
+	 * Shims an onFocusOutside callback to be compatible with a deprecated
+	 * onClickOutside prop function, if provided.
+	 *
+	 * @param {FocusEvent} event Focus event from onFocusOutside.
+	 */
+	onFocusOutside( event ) {
```

How can I test this shim? By updating one of the places with usage?

#### aduth on 2019-04-11T20:05:20Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r274628058
Path: packages/components/src/popover/index.js

```diff
@@ -94,6 +96,47 @@ class Popover extends Component {
 		this.toggleAutoRefresh( false );
 	}
 
+	/**
+	 * Shims an onFocusOutside callback to be compatible with a deprecated
+	 * onClickOutside prop function, if provided.
+	 *
+	 * @param {FocusEvent} event Focus event from onFocusOutside.
+	 */
+	onFocusOutside( event ) {
```

You could locally revert one or both of 55ca76a5427876ab91e6966272e973fe295e0275 or 3e8ffc4673db9f9527173d03b5018b53d7c78a9d to restore those components' use of `onClickOutside`.

### Review thread 3

#### youknowriad on 2019-05-23T08:40:04Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r286834956
Path: packages/components/src/popover/detect-outside.js

```diff
@@ -1,24 +1,21 @@
 /**
- * External dependencies
+ * WordPress dependencies
  */
-import clickOutside from 'react-click-outside';
+import { Component } from '@wordpress/element';
 
 /**
- * WordPress dependencies
+ * Internal dependencies
  */
-import { Component } from '@wordpress/element';
+import withFocusOutside from '../higher-order/with-focus-outside';
 
 class PopoverDetectOutside extends Component {
-	handleClickOutside( event ) {
-		const { onClickOutside } = this.props;
-		if ( onClickOutside ) {
-			onClickOutside( event );
-		}
+	handleFocusOutside( event ) {
+		this.props.onFocusOutside( event );
 	}
 
 	render() {
 		return this.props.children;
 	}
 }
 
-export default clickOutside( PopoverDetectOutside );
+export default withFocusOutside( PopoverDetectOutside );
```

This could be a nice hook const `useOnFocusOutside( ref, handler )`

### Review thread 4

#### gziolo on 2019-08-01T10:17:19Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r309625516
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

It's a bit stretched use of IconButton in the case when there is no icon 😅 

However, I can't think of another simple way of handling it where this doesn't trigger re-render of the button. I noticed that this behavior is broken only because we swap `IconButton` with `Button` for no reason when only the icon gets removed/added.

#### noisysocks on 2019-08-02T01:32:22Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r309953802
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

Yep, exactly! Switching between the two components causes React to remount the component and lose focus. Looking at `IconButton`, it seemed to me that `icon={}` is an optional prop so I felt comfortable doing it this way.

#### youknowriad on 2019-08-02T09:58:14Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r310064955
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

One thing we keep discussing here and there is why not just add an `icon` prop to the `Button` block and deprecate the `IconButton` (or make it just a shortcut)

#### gziolo on 2019-08-02T10:08:47Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r310068366
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

I'm sure @drw158 @cburton4 have some good ideas on how to approach it :)

I'm fine with anything which makes it less confusing. I guess having more flexible `Button` and `IconButton` as a backward-compatible alias makes a lot of sense.

#### davewhitley on 2019-08-02T13:30:39Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r310131918
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

We have an issue open to discuss combining IconButton and Button in #16541 

We are leaning towards keeping them separate, but I'll look at this closer to understand the disadvantage.

#### davewhitley on 2019-08-02T18:48:13Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r310255812
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

I'm assuming from the code that this is an issue because Button does not have an icon option. Switching between icon and non-icon buttons for Menu Items is causing technical problems.

Could this be solved by adding an icon option to the Button component, but still keep IconButton a separate component? That way, the unchecked and checked versions of the Menu Item could use just a single component, Button.

#### youknowriad on 2019-08-03T16:37:46Z

URL: https://github.com/WordPress/gutenberg/pull/14851#discussion_r310356711
Path: packages/components/src/menu-item/index.js

```diff
@@ -47,32 +46,26 @@ export function MenuItem( {
 		);
 	}
 
-	let tagName = Button;
-
-	if ( icon ) {
-		if ( ! isString( icon ) ) {
-			icon = cloneElement( icon, {
-				className: 'components-menu-items__item-icon',
-				height: 20,
-				width: 20,
-			} );
-		}
-
-		tagName = IconButton;
-		props.icon = icon;
+	if ( icon && ! isString( icon ) ) {
+		icon = cloneElement( icon, {
+			className: 'components-menu-items__item-icon',
+			height: 20,
+			width: 20,
+		} );
 	}
 
-	return createElement(
-		tagName,
-		{
+	return (
+		<IconButton
```

To be fair, we already do the opposite here. Not pass the "icon" prop to the IconButton (so it behaves like a Button).  If it doesn't make sense to merge the two components into one, I feel we shouldn't do anything specific about it.

## Changed files

- package-lock.json (MODIFIED, +1/-0)
- packages/components/CHANGELOG.md (MODIFIED, +9/-0)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/components/src/dropdown-menu/style.scss (MODIFIED, +2/-1)
- packages/components/src/dropdown/index.js (MODIFIED, +8/-10)
- packages/components/src/menu-item/index.js (MODIFIED, +18/-25)
- packages/components/src/menu-item/test/__snapshots__/index.js.snap (MODIFIED, +4/-4)
- packages/components/src/popover/README.md (MODIFIED, +4/-2)
- packages/components/src/popover/detect-outside.js (MODIFIED, +7/-10)
- packages/components/src/popover/index.js (MODIFIED, +43/-2)
- packages/components/src/popover/test/__snapshots__/index.js.snap (MODIFIED, +19/-15)
- packages/format-library/CHANGELOG.md (MODIFIED, +6/-0)
- packages/format-library/src/link/inline.js (MODIFIED, +5/-5)
