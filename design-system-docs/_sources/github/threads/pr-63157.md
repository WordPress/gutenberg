---
number: 63157
type: pull_request
state: MERGED
merged_at: 2024-07-08T11:53:54Z
url: https://github.com/WordPress/gutenberg/pull/63157
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","Needs Dev Note","First-time Contributor"]
---

# PR #63157: Tooltip Component: Add custom class name support

- URL: https://github.com/WordPress/gutenberg/pull/63157
- Author: aliaghdam
- Created: 2024-07-05T00:30:52Z
- Updated: 2024-10-01T12:14:47Z
- Comments: 2 of 2
- Review threads: 9 of 9
- Changed files: 4 of 4
- Diff: ../pr-diffs/63157.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
The Tooltip component does not supports custom css class name.

## Why?
I added the custom class name support that that for many reasons can be used for design customization of tooltip.

## How?
I added the `className` prop and also added test to make sure it works properly.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

Unlinked contributors: aliaghdam.

## Issue comments

### github-actions on 2024-07-05T00:31:06Z

URL: https://github.com/WordPress/gutenberg/pull/63157#issuecomment-2209667379

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: aliaghdam <aliaghdam@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-07-05T00:31:13Z

URL: https://github.com/WordPress/gutenberg/pull/63157#issuecomment-2209667430

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @aliaghdam! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2024-07-05T13:22:09Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666803801
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -112,7 +113,7 @@ function UnforwardedTooltip(
 			{ isOnlyChild && ( text || shortcut ) && (
 				<Ariakit.Tooltip
 					{ ...restProps }
-					className="components-tooltip"
+					className={ className === undefined ? "components-tooltip" : "components-tooltip " + className }
```

This can be done with `clsx`:

```suggestion
					className={ clsx( 'components-tooltip', className ) }
```

You'll need to add an import at the top:

```
import clsx from 'clsx';
```

### Review thread 2 (resolved)

#### tyxla on 2024-07-05T13:26:35Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666808718
Path: packages/components/src/tooltip/types.ts

```diff
@@ -58,6 +58,12 @@ export type TooltipProps = {
 	 * The text shown in the tooltip when anchor element is focused or hovered.
 	 */
 	text?: string;
+	/**
+	 * The custom class name for the tooltip.
+	 * The default class for tooltip is `components-tooltip` and always will be added.
+	 * The custom class name will be appended to the default class.
```

Having a default class and appending a custom one to it is pretty regular behavior, so probably the description could be shortened to something like:

```suggestion
	 * Custom class name for the tooltip, in addition to the default `components-tooltip`.
```

### Review thread 3 (resolved)

#### tyxla on 2024-07-05T13:27:16Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666809538
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -105,6 +105,33 @@ describe( 'Tooltip', () => {
 				screen.getByRole( 'button', { name: 'Anchor' } )
 			).not.toHaveAttribute( 'data-foo' );
 		} );
+
+		it( 'should add custom class to the tooltip', async () => {
+			render( <Tooltip { ...props } className='foo' /> );
```

Nit: With string literals as component props, we usually use double quotes:

```suggestion
			render( <Tooltip { ...props } className="foo" /> );
```

### Review thread 4 (resolved)

#### tyxla on 2024-07-05T13:29:44Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666812404
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -105,6 +105,33 @@ describe( 'Tooltip', () => {
 				screen.getByRole( 'button', { name: 'Anchor' } )
 			).not.toHaveAttribute( 'data-foo' );
 		} );
+
+		it( 'should add custom class to the tooltip', async () => {
+			render( <Tooltip { ...props } className='foo' /> );
+
+			// Hover over the anchor, tooltip should show
+			await hover(
+				screen.getByRole( 'button', { name: 'Tooltip anchor' } )
+			);
+
+			// Check core class
+			await waitFor( () =>
+				expect(
+					screen.getByRole( 'tooltip', {
+						name: 'tooltip text',
+					} )
+				).toHaveClass('components-tooltip')
+			);
+
+			// Check custom class
+			await waitFor( () =>
+				expect(
+					screen.getByRole( 'tooltip', {
+						name: 'tooltip text',
+					} )
+				).toHaveClass('foo')
+			);
```

This could likely be cleaned up a bit, no need to do 2 separate `await waitFor` and 2 duplicate queries:

```suggestion
			// Check default and custom classnames
			await waitFor( () =>
				expect(
					screen.getByRole( 'tooltip', {
						name: 'tooltip text',
					} )
				).toHaveClass( 'components-tooltip', 'foo' )
			);
```

Also, we might want to keep the code style in consideration, there were some spacing issues that were causing the static analysis CI check to fail.

### Review thread 5 (resolved)

#### tyxla on 2024-07-05T13:30:22Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666813109
Path: packages/components/CHANGELOG.md

```diff
@@ -1,6 +1,7 @@
 <!-- Learn how to maintain this file at https://github.com/WordPress/gutenberg/tree/HEAD/packages#maintaining-changelogs. -->
 
 ## Unreleased
+-   `Tooltip`: Add support for `className` prop ([#63157](https://github.com/WordPress/gutenberg/pull/63157)).
```

I think we should move this under the "Enhancements" section.

### Review thread 6 (resolved)

#### tyxla on 2024-07-05T13:31:01Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666813826
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -50,6 +50,7 @@ function UnforwardedTooltip(
 		position,
 		shortcut,
 		text,
+		className,
```

Nit: The pre-existing props are alphabetically sorted, so perhaps we should move this one to the top to keep that order.

### Review thread 7 (resolved)

#### tyxla on 2024-07-05T13:31:26Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1666814299
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -105,6 +105,33 @@ describe( 'Tooltip', () => {
 				screen.getByRole( 'button', { name: 'Anchor' } )
 			).not.toHaveAttribute( 'data-foo' );
 		} );
+
+		it( 'should add custom class to the tooltip', async () => {
```

Should the test name reflect that we're testing both the original default class, and the additional custom one?

### Review thread 8 (resolved)

#### ciampo on 2024-07-08T09:40:10Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1668321198
Path: packages/components/src/tooltip/types.ts

```diff
@@ -16,6 +16,10 @@ export type TooltipProps = {
 	 * **Note**: Accepts only one child element.
 	 */
 	children: React.ReactElement;
+	/**
+	 * Custom class name for the tooltip, in addition to the default `components-tooltip`.
```

Internal class names are not part of a components' public APIs and we discourage their usage — let's not mention `.components-tooltip`

```suggestion
	 * Custom class name for the tooltip.
```

### Review thread 9 (resolved)

#### tyxla on 2024-07-08T09:46:50Z

URL: https://github.com/WordPress/gutenberg/pull/63157#discussion_r1668330316
Path: packages/components/CHANGELOG.md

```diff
@@ -2,8 +2,10 @@
 
 ## Unreleased
 
+
```

We can remove this empty line now:
```suggestion
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/tooltip/index.tsx (MODIFIED, +3/-1)
- packages/components/src/tooltip/test/index.tsx (MODIFIED, +18/-0)
- packages/components/src/tooltip/types.ts (MODIFIED, +4/-0)
