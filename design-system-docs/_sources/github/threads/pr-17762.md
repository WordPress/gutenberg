---
number: 17762
type: pull_request
state: MERGED
merged_at: 2019-10-08T17:55:22Z
url: https://github.com/WordPress/gutenberg/pull/17762
matched_labels: ["[Package] Components"]
all_labels: ["Framework","[Type] Build Tooling","[Feature] UI Components","[Package] Components","Storybook"]
---

# PR #17762: Framework: Add Component Storybook (tweaks)

- URL: https://github.com/WordPress/gutenberg/pull/17762
- Author: gziolo
- Created: 2019-10-04T06:36:23Z
- Updated: 2019-10-10T12:37:04Z
- Comments: 7 of 7
- Review threads: 6 of 6
- Changed files: 10 of 10
- Diff: ../pr-diffs/17762.diff

## Body

## Description
Some tweaks for the general setup of #17475.

Changes included:

- Eslint rule updated consolidated with the existing one
- scripts for building and watching packages updated to ignore Storybook files
- `@storybook/addon-docs` downgraded to the same version as other packages
- `babel-loader` removed from `devDependencies` - it isn't explicitly used so dependencies should have more freedom to pick a proper version
- `design-system:build` runs also `npm run build:packages` as one of the steps to align with the playground and to fix the issue which will pop up as soon as we start using internal dependencies in stories
- `@wordpress/babel-preset-default` preset added to the custom Babel config so we don't have to explicitly load `React` as a dependnecy but rather use `@wordpress/element`

Unrelated cleanup changes:

- Lerna config updated to ignore some missed files

## How has this been tested?

The new storybook was verified to work in development (`npm run design-system:dev`) and production (`npm run design-system:build`) modes.

## Issue comments

### gziolo on 2019-10-04T10:10:34Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-538337715

I see a failing unit test:

https://travis-ci.com/WordPress/gutenberg/jobs/241941170#L5643

```
FAIL packages/env/tests/cli.test.js
  ● env cli › handles successful commands with messages.
    expect(jest.fn()).toHaveBeenCalledWith(expected)
    Expected mock function to have been called with:
      "success message (in 0s 0ms)"
    as argument 1, but it was called with
      "success message (in 0s 1ms)".
      72 | 		const { spinner } = env.start.mock.calls[ 0 ][ 0 ];
      73 | 		await env.start.mock.results[ 0 ].value;
    > 74 | 		expect( spinner.succeed ).toHaveBeenCalledWith( 'success message (in 0s 0ms)' );
         | 		                          ^
      75 | 	} );
      76 | 	it( 'handles successful commands with spinner text.', async () => {
      77 | 		env.start.mockResolvedValueOnce();
      at Object.toHaveBeenCalledWith (packages/env/tests/cli.test.js:74:29)
```

It looks like this test depends on the execution time so it might fail from time to time. We probably should limit this check to whether this string starts with `success message`.

### gziolo on 2019-10-04T10:15:36Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-538339172

I rebased changes against the `add/storybook-for-components` branch which was updated.

### gziolo on 2019-10-04T10:38:40Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-538345714

We should also explore how to setup Storybook for RN:
https://storybook.js.org/docs/guides/guide-react-native/

### epiqueras on 2019-10-04T19:28:28Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-538530920

@gziolo I've addressed the timing test issue in one of the Env follow ups in case there are some requirements that stop us from mocking timers: https://github.com/WordPress/gutenberg/pull/17732/commits/43f1b239b4f87383b1cd9b943365da8c795664e8.

It'd be good to get those merged soon.

### gziolo on 2019-10-07T09:59:26Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-538929017

> > We should also explore how to setup Storybook for RN:
> 
> For running `on-device`? Or are you proposing we have a second deployment that runs a web emulator?

For running on the real device or the simulator. This is the only way to ensure that we build cross-platform components.

### epiqueras on 2019-10-07T16:50:48Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-539104592

>For running on the real device or the simulator. This is the only way to ensure that we build cross-platform components.

I'll add it to my list.

### epiqueras on 2019-10-07T21:36:49Z

URL: https://github.com/WordPress/gutenberg/pull/17762#issuecomment-539215031

>For running on the real device or the simulator. This is the only way to ensure that we build cross-platform components.

#17829

## Review threads

### Review thread 1 (resolved)

#### epiqueras on 2019-10-04T18:10:40Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331624226
Path: bin/packages/build.js

```diff
@@ -126,8 +126,8 @@ if ( files.length ) {
 		`${ PACKAGES_DIR }/*/src/*.scss`,
 	], {
 		ignore: [
-			`**/test/**`,
-			`**/__mocks__/**`,
+			`**/{__mocks__,__tests__,test}/**`,
```

Where do we use `__tests__`? We should stick to one for consistency if possible.

#### gziolo on 2019-10-07T09:33:12Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331924713
Path: bin/packages/build.js

```diff
@@ -126,8 +126,8 @@ if ( files.length ) {
 		`${ PACKAGES_DIR }/*/src/*.scss`,
 	], {
 		ignore: [
-			`**/test/**`,
-			`**/__mocks__/**`,
+			`**/{__mocks__,__tests__,test}/**`,
```

There were some reasons to use a different approach here:
https://github.com/WordPress/gutenberg/tree/master/packages/eslint-plugin/rules

See also explanation here:
https://github.com/WordPress/gutenberg/pull/12828/files#r249209974

> Note: I named the directory `__tests__` per the behavior of [`requireindex`](https://www.npmjs.com/package/requireindex) to avoid treating "tests" as an export, as it's documented to ignore any entries prefixed with an underscore, and the default Jest preset configuration allows it

### Review thread 2 (resolved)

#### epiqueras on 2019-10-04T18:11:55Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331624741
Path: package.json

```diff
@@ -73,7 +73,7 @@
 		"@babel/traverse": "7.4.5",
 		"@octokit/rest": "16.26.0",
 		"@storybook/addon-a11y": "5.2.1",
-		"@storybook/addon-docs": "5.3.0-alpha.2",
```

We want to be on the latest for the new features.

#### gziolo on 2019-10-07T09:38:35Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331926997
Path: package.json

```diff
@@ -73,7 +73,7 @@
 		"@babel/traverse": "7.4.5",
 		"@octokit/rest": "16.26.0",
 		"@storybook/addon-a11y": "5.2.1",
-		"@storybook/addon-docs": "5.3.0-alpha.2",
```

I couldn't find any explanation of why we would want to use a non-stable version of this addon. Everything else is on the latest published version which is what you would expect when using external tooling. I'd like to learn why do you think it's valuable to use `alpha` release instead.

#### epiqueras on 2019-10-07T16:48:46Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332125431
Path: package.json

```diff
@@ -73,7 +73,7 @@
 		"@babel/traverse": "7.4.5",
 		"@octokit/rest": "16.26.0",
 		"@storybook/addon-a11y": "5.2.1",
-		"@storybook/addon-docs": "5.3.0-alpha.2",
```

It had a lot less bugs and more features, but I see these were stabilized in `5.2.2`. Can we use that or `5.2.3` (`latest`)?

#### gziolo on 2019-10-08T11:04:22Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332453266
Path: package.json

```diff
@@ -73,7 +73,7 @@
 		"@babel/traverse": "7.4.5",
 		"@octokit/rest": "16.26.0",
 		"@storybook/addon-a11y": "5.2.1",
-		"@storybook/addon-docs": "5.3.0-alpha.2",
```

Sure, thing. `5.2.3` for all those dependencies sounds great 👍

### Review thread 3 (resolved)

#### epiqueras on 2019-10-04T18:14:45Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331625823
Path: package.json

```diff
@@ -94,7 +94,6 @@
 		"@wordpress/npm-package-json-lint-config": "file:packages/npm-package-json-lint-config",
 		"@wordpress/postcss-themes": "file:packages/postcss-themes",
 		"@wordpress/scripts": "file:packages/scripts",
-		"babel-loader": "8.0.6",
```

This needs to be explicit as long as one dependency has it as a peer dependency, which they do.

Otherwise you are relying on another dependency bringing it in as a dependency, but whenever that dependency is removed, things break.

#### gziolo on 2019-10-07T09:24:23Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331920743
Path: package.json

```diff
@@ -94,7 +94,6 @@
 		"@wordpress/npm-package-json-lint-config": "file:packages/npm-package-json-lint-config",
 		"@wordpress/postcss-themes": "file:packages/postcss-themes",
 		"@wordpress/scripts": "file:packages/scripts",
-		"babel-loader": "8.0.6",
```

I see it now 👍 

There is also `@babel/core` and `react` but we already satisfy those.

### Review thread 4 (resolved)

#### epiqueras on 2019-10-04T18:16:03Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331626296
Path: package.json

```diff
@@ -225,12 +224,12 @@
 		"test-unit-php-multisite": "cross-env WP_MULTISITE=1 wp-scripts env test-php",
 		"test-unit:native": "cd test/native/ && cross-env NODE_ENV=test jest --config ./jest.config.js",
 		"test-unit:native:debug": "cd test/native/ && node --inspect ../../node_modules/.bin/jest --runInBand --config ./jest.config.js",
+		"design-system:build": "npm run build:packages && build-storybook -c ./packages/components/storybook -o ./playground/dist/design-system/components",
```

This means we'll build packages twice when deploying, can we somehow only do it once?

#### gziolo on 2019-10-07T09:40:13Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331927705
Path: package.json

```diff
@@ -225,12 +224,12 @@
 		"test-unit-php-multisite": "cross-env WP_MULTISITE=1 wp-scripts env test-php",
 		"test-unit:native": "cd test/native/ && cross-env NODE_ENV=test jest --config ./jest.config.js",
 		"test-unit:native:debug": "cd test/native/ && node --inspect ../../node_modules/.bin/jest --runInBand --config ./jest.config.js",
+		"design-system:build": "npm run build:packages && build-storybook -c ./packages/components/storybook -o ./playground/dist/design-system/components",
```

I guess we should consolidate the playground in Storybook as a next step. There is no reason to keep it as a standalone app.

#### epiqueras on 2019-10-07T16:49:31Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332125759
Path: package.json

```diff
@@ -225,12 +224,12 @@
 		"test-unit-php-multisite": "cross-env WP_MULTISITE=1 wp-scripts env test-php",
 		"test-unit:native": "cd test/native/ && cross-env NODE_ENV=test jest --config ./jest.config.js",
 		"test-unit:native:debug": "cd test/native/ && node --inspect ../../node_modules/.bin/jest --runInBand --config ./jest.config.js",
+		"design-system:build": "npm run build:packages && build-storybook -c ./packages/components/storybook -o ./playground/dist/design-system/components",
```

That makes sense.

### Review thread 5 (resolved)

#### epiqueras on 2019-10-04T18:17:19Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331626799
Path: packages/components/src/button/stories/index.js

```diff
@@ -1,8 +1,3 @@
-/**
- * External dependencies
- */
-import React from 'react';
```

If this works then we can remove the ignore from `eslint`.

#### gziolo on 2019-10-07T09:19:18Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r331918510
Path: packages/components/src/button/stories/index.js

```diff
@@ -1,8 +1,3 @@
-/**
- * External dependencies
- */
-import React from 'react';
```

I think we still need this override in `eslint` because Storybook runs in development mode and will have development dependencies defined in the `package.json` in the root folder. 

`import/no-extraneous-dependencies` with the current setup ensure that production code has all dependencies defined in the packages that use them.

#### epiqueras on 2019-10-07T16:40:03Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332121460
Path: packages/components/src/button/stories/index.js

```diff
@@ -1,8 +1,3 @@
-/**
- * External dependencies
- */
-import React from 'react';
```

Yeah, but none of those dev dependencies get explicitly imported in story files so we would be good.

#### gziolo on 2019-10-08T11:05:05Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332453519
Path: packages/components/src/button/stories/index.js

```diff
@@ -1,8 +1,3 @@
-/**
- * External dependencies
- */
-import React from 'react';
```

I tested it:
```
/Users/gziolo/PhpstormProjects/gutenberg/packages/components/storybook/addons.js
  4:1  error  '@storybook/addon-a11y' should be listed in the project's dependencies. Run 'npm i -S @storybook/addon-a11y' to add it          import/no-extraneous-dependencies
  5:1  error  '@storybook/addon-viewport' should be listed in the project's dependencies. Run 'npm i -S @storybook/addon-viewport' to add it  import/no-extraneous-dependencies

/Users/gziolo/PhpstormProjects/gutenberg/packages/components/storybook/config.js
  4:1  error  '@storybook/react' should be listed in the project's dependencies. Run 'npm i -S @storybook/react' to add it            import/no-extraneous-dependencies
  5:1  error  '@storybook/addon-a11y' should be listed in the project's dependencies. Run 'npm i -S @storybook/addon-a11y' to add it  import/no-extraneous-dependencies
```

We can relax those checks for stories but not the storybook itself.

### Review thread 6

#### gziolo on 2019-10-08T11:11:20Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332455644
Path: .travis.yml

```diff
@@ -199,8 +199,9 @@ jobs:
       if: branch = master
       name: Deploy Playground
       env: INSTALL_WORDPRESS=false
+      install:
```

How about we override `install` here to skip `npm run build`?

#### epiqueras on 2019-10-08T16:55:59Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332623895
Path: .travis.yml

```diff
@@ -199,8 +199,9 @@ jobs:
       if: branch = master
       name: Deploy Playground
       env: INSTALL_WORDPRESS=false
+      install:
```

`if: branch = master` stops the entire stage from running outside of master, right?

#### epiqueras on 2019-10-08T17:50:42Z

URL: https://github.com/WordPress/gutenberg/pull/17762#discussion_r332648160
Path: .travis.yml

```diff
@@ -199,8 +199,9 @@ jobs:
       if: branch = master
       name: Deploy Playground
       env: INSTALL_WORDPRESS=false
+      install:
```

Oh I see what you mean, because the playground already runs build. Yeah, good catch!

## Changed files

- .eslintrc.js (MODIFIED, +3/-2)
- .travis.yml (MODIFIED, +2/-1)
- bin/packages/build.js (MODIFIED, +3/-2)
- bin/packages/watch.js (MODIFIED, +1/-1)
- lerna.json (MODIFIED, +1/-1)
- package-lock.json (MODIFIED, +270/-1112)
- package.json (MODIFIED, +8/-8)
- packages/components/src/button/stories/index.js (MODIFIED, +0/-5)
- packages/components/storybook/.babelrc (MODIFIED, +3/-0)
- packages/scripts/package.json (MODIFIED, +1/-1)
