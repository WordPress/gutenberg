---
number: 76021
type: pull_request
state: OPEN
url: https://github.com/WordPress/gutenberg/pull/76021
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #76021: Components: Fix Tabs selected indicator position when sibling tab resizes

- URL: https://github.com/WordPress/gutenberg/pull/76021
- Author: Mustafabharmal
- Created: 2026-02-27T22:05:28Z
- Updated: 2026-06-10T12:52:02Z
- Comments: 2 of 2
- Review threads: 2 of 2
- Changed files: 1 of 1
- Diff: ../pr-diffs/76021.diff

## Body

## What?
Closes #75716

Fixes the Tabs component's selected tab underline indicator being positioned incorrectly when a sibling tab changes size.

## Why?

The `useTrackElementOffsetRect` hook only observed the selected tab element for resize changes. When a sibling tab changed size (e.g., the label changed from "Template" to "Template Part"), the selected tab's position shifted but
the `ResizeObserver` didn't fire because the selected tab itself didn't resize only its position within the tablist changed.

This bug was visible in the site editor when navigating between a template and a template part, where the inspector tab label changes and the neighboring tab's underline indicator could end up in the wrong position.

## How?

Added a `ResizeObserver` in `TabList` that observes all rendered tab item elements. When any tab item resizes, it triggers a remeasurement of the selected tab's offset rect via the existing `deps` escape hatch in `useTrackElementOffsetRect`. This ensures the underline indicator is repositioned correctly even when the size change comes from a sibling tab.

## Testing Instructions

1. Open the site editor, and navigate to a template like 'Blog home'
2. Click the header template part, and open the block inspector sidebar.
3. Click 'Edit original' to access the template part editor
4. When the template part editor opens, select a block in the template part and ensure the 'Block' tab is selected
5. From the editor topbar, click the back button
6. Verify the selected tab underline is in the correct position


## Screenshots or screencast <!-- if applicable -->

<!-- If you would like to upload screenshots, feel free to use the table below when it is useful to show the difference between before and after the change. -->

Before:

https://github.com/user-attachments/assets/0082517d-006a-40cc-9b7c-4f515f715286


After:


https://github.com/user-attachments/assets/cf0f4549-7146-406b-a1fa-141b211e68fd

## Issue comments

### github-actions on 2026-02-27T22:05:51Z

URL: https://github.com/WordPress/gutenberg/pull/76021#issuecomment-3975354031

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: Mustafabharmal <mustafabharmal@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: talldan <talldanwp@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### ciampo on 2026-03-07T14:12:14Z

URL: https://github.com/WordPress/gutenberg/pull/76021#issuecomment-4016587255

We should consider moving this fix to `useTrackElementOffsetRect` — it's going to be more generic (and with some unconvered edge case), but it should apply the fix potentially to other components too, such as `ToggleGroupControl`.

<details>
<summary>Potential changes</summary>

```diff
diff --git i/packages/components/src/tabs/tablist.tsx w/packages/components/src/tabs/tablist.tsx
index d41a9535c93..b7cfef7e19a 100644
--- i/packages/components/src/tabs/tablist.tsx
+++ w/packages/components/src/tabs/tablist.tsx
@@ -87,34 +87,10 @@ export const TabList = forwardRef<
 		renderedItems && selectedItem
 			? renderedItems.indexOf( selectedItem )
 			: -1;
-
-	// Track when any tab item resizes, so that the selected indicator
-	// position is recalculated even when a sibling tab changes size.
-	const [ tabsResizeCount, setTabsResizeCount ] = useState( 0 );
-	useLayoutEffect( () => {
-		if ( ! renderedItems?.length ) {
-			return;
-		}
-
-		const observer = new ResizeObserver( () => {
-			setTabsResizeCount( ( prev ) => prev + 1 );
-		} );
-
-		for ( const item of renderedItems ) {
-			if ( item.element ) {
-				observer.observe( item.element );
-			}
-		}
-
-		return () => observer.disconnect();
-	}, [ renderedItems ] );
-
 	// Use selectedItemIndex as a dependency to force recalculation when the
 	// selected item index changes (elements are swapped / added / removed).
-	// Use tabsResizeCount to force recalculation when any tab resizes.
 	const selectedRect = useTrackElementOffsetRect( selectedItem?.element, [
 		selectedItemIndex,
-		tabsResizeCount,
 	] );
 
 	// Track overflow to show scroll hints.
diff --git i/packages/components/src/utils/element-rect.ts w/packages/components/src/utils/element-rect.ts
index e578d0b279e..8d8e002d895 100644
--- i/packages/components/src/utils/element-rect.ts
+++ w/packages/components/src/utils/element-rect.ts
@@ -128,6 +128,19 @@ const POLL_RATE = 100;
  * When no element is provided (`null` or `undefined`), the hook will return
  * a "null" rect, in which all values are `0` and `element` is `undefined`.
  *
+ * The hook automatically remeasures when:
+ * - The target element itself resizes.
+ * - The target element's offset parent or any of its direct children resize
+ *   (to detect position shifts caused by sibling layout changes).
+ * - The provided `deps` change (escape hatch for other triggers).
+ *
+ * **Note:** the sibling observation assumes the offset parent is the direct
+ * parent of the target and its siblings. If the DOM structure has intermediate
+ * positioned wrappers or non-positioned grouping elements between the offset
+ * parent and the actual siblings, those sibling resizes may not be detected
+ * automatically. In such cases, use the `deps` parameter as an escape hatch
+ * to trigger remeasurement manually.
+ *
  * **Note:** sometimes, the measurement will fail (see `getElementOffsetRect`'s
  * documentation for more details). When that happens, this hook will attempt
  * to measure again after a frame, and if that fails, it will poll every 100
@@ -174,14 +187,43 @@ export function useTrackElementOffsetRect(
 		}
 	}, [ setElement, targetElement ] );
 
+	// Observe the offset parent and sibling elements for resize, so that the
+	// measured position is updated when sibling layout changes shift the
+	// target element within the offset parent.
+	useLayoutEffect( () => {
+		if ( ! targetElement ) {
+			return;
+		}
+
+		const offsetParent = targetElement.offsetParent;
+		if ( ! ( offsetParent instanceof HTMLElement ) ) {
+			return;
+		}
+
+		const resizeObserver = new ResizeObserver( () => {
+			measure();
+		} );
+
+		resizeObserver.observe( offsetParent );
+		for ( const child of offsetParent.children ) {
+			if ( child !== targetElement ) {
+				resizeObserver.observe( child );
+			}
+		}
+
+		return () => resizeObserver.disconnect();
+		// `measure` is stable (created via `useEvent`).
+	}, [ targetElement, measure ] );
+
 	// Escape hatch to force a remeasurement when something else changes rather
 	// than the target elements' ref or size (for example, the target element
 	// can change its position within the tablist).
+	// `measure` is stable (created via `useEvent`).
+	const escapeDeps = [ ...deps, measure ];
 	useLayoutEffect( () => {
 		measure();
-		// `measure` is a stable function, so it's safe to omit it from the deps array.
-		// deps can't be statically analyzed by ESLint
-	}, deps );
+		// `escapeDeps` can't be statically analyzed by ESLint.
+	}, escapeDeps );
 
 	return indicatorPosition;
 }

```

</details>

## Review threads

### Review thread 1 (resolved)

#### copilot-pull-request-reviewer on 2026-02-28T10:09:53Z

URL: https://github.com/WordPress/gutenberg/pull/76021#discussion_r2867320832
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -87,10 +87,34 @@ export const TabList = forwardRef<
 		renderedItems && selectedItem
 			? renderedItems.indexOf( selectedItem )
 			: -1;
+
+	// Track when any tab item resizes, so that the selected indicator
+	// position is recalculated even when a sibling tab changes size.
+	const [ tabsResizeCount, setTabsResizeCount ] = useState( 0 );
+	useLayoutEffect( () => {
+		if ( ! renderedItems?.length ) {
+			return;
+		}
+
+		const observer = new ResizeObserver( () => {
```

`ResizeObserver` is used without feature detection. In environments where it’s missing (older browsers, some test/DOM setups), `new ResizeObserver(...)` will throw and break `Tabs.TabList`. Please guard with `window.ResizeObserver` (or reuse the existing `@wordpress/compose` resize observer helper) and bail out cleanly when unavailable.
```suggestion
		const ResizeObserverConstructor =
			typeof window !== 'undefined' ? window.ResizeObserver : undefined;

		if ( ! ResizeObserverConstructor ) {
			return;
		}

		const observer = new ResizeObserverConstructor( () => {
```

### Review thread 2

#### copilot-pull-request-reviewer on 2026-02-28T10:09:54Z

URL: https://github.com/WordPress/gutenberg/pull/76021#discussion_r2867320846
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -87,10 +87,34 @@ export const TabList = forwardRef<
 		renderedItems && selectedItem
 			? renderedItems.indexOf( selectedItem )
 			: -1;
+
+	// Track when any tab item resizes, so that the selected indicator
+	// position is recalculated even when a sibling tab changes size.
+	const [ tabsResizeCount, setTabsResizeCount ] = useState( 0 );
+	useLayoutEffect( () => {
+		if ( ! renderedItems?.length ) {
+			return;
+		}
+
+		const observer = new ResizeObserver( () => {
+			setTabsResizeCount( ( prev ) => prev + 1 );
+		} );
+
+		for ( const item of renderedItems ) {
+			if ( item.element ) {
+				observer.observe( item.element );
+			}
+		}
+
+		return () => observer.disconnect();
+	}, [ renderedItems ] );
+
 	// Use selectedItemIndex as a dependency to force recalculation when the
 	// selected item index changes (elements are swapped / added / removed).
+	// Use tabsResizeCount to force recalculation when any tab resizes.
 	const selectedRect = useTrackElementOffsetRect( selectedItem?.element, [
 		selectedItemIndex,
+		tabsResizeCount,
 	] );
```

This change adds new behavior (remeasuring the selected indicator when any sibling tab resizes) but there’s no automated test covering it. Since the Tabs package already has a comprehensive test suite, please add a regression test that simulates a sibling tab resize (e.g., via a mocked `ResizeObserver` callback) and asserts the indicator position is recomputed.

#### ciampo on 2026-03-07T14:12:36Z

URL: https://github.com/WordPress/gutenberg/pull/76021#discussion_r2899794511
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -87,10 +87,34 @@ export const TabList = forwardRef<
 		renderedItems && selectedItem
 			? renderedItems.indexOf( selectedItem )
 			: -1;
+
+	// Track when any tab item resizes, so that the selected indicator
+	// position is recalculated even when a sibling tab changes size.
+	const [ tabsResizeCount, setTabsResizeCount ] = useState( 0 );
+	useLayoutEffect( () => {
+		if ( ! renderedItems?.length ) {
+			return;
+		}
+
+		const observer = new ResizeObserver( () => {
+			setTabsResizeCount( ( prev ) => prev + 1 );
+		} );
+
+		for ( const item of renderedItems ) {
+			if ( item.element ) {
+				observer.observe( item.element );
+			}
+		}
+
+		return () => observer.disconnect();
+	}, [ renderedItems ] );
+
 	// Use selectedItemIndex as a dependency to force recalculation when the
 	// selected item index changes (elements are swapped / added / removed).
+	// Use tabsResizeCount to force recalculation when any tab resizes.
 	const selectedRect = useTrackElementOffsetRect( selectedItem?.element, [
 		selectedItemIndex,
+		tabsResizeCount,
 	] );
```

Yes, possibly we would add more tests.

## Changed files

- packages/components/src/tabs/tablist.tsx (MODIFIED, +24/-0)
