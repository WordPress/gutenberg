---
number: 23077
type: pull_request
state: MERGED
merged_at: 2020-06-22T15:41:32Z
url: https://github.com/WordPress/gutenberg/pull/23077
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","[Block] Spacer","[Feature] Design Tools"]
---

# PR #23077: Spacer: Show tooltip with height value on resize

- URL: https://github.com/WordPress/gutenberg/pull/23077
- Author: ItsJonQ
- Created: 2020-06-10T21:12:57Z
- Updated: 2020-06-23T07:44:22Z
- Comments: 20 of 20
- Review threads: 9 of 9
- Changed files: 13 of 13
- Diff: ../pr-diffs/23077.diff

## Body

## Description

![Screen Capture on 2020-06-10 at 16-38-00](https://user-images.githubusercontent.com/2322354/84317813-6d190d80-ab3b-11ea-8f16-5bc29e227234.gif)

This update enhances the **Spacer** block by rendering a tooltip element above the cursor when the user engages in a "drag to resize" interaction.

#### ✨ New `<ResizeTooltip />` component

The tooltip comes from a new `<ResizeTooltip />` component. This component is a general resize-aware component that renders a tooltip on size changes. It isn't bound to `<ResizableBox />` (which is what Spacer uses), but it can be integrated with it.

As an aside... This component has 2 different positions:

* `bottom`
* `corner` (default)

##### 📄 Corner

![Screen Capture on 2020-06-10 at 16-06-28](https://user-images.githubusercontent.com/2322354/84318842-0eed2a00-ab3d-11ea-988a-af93697a9795.gif)

The Tooltip will render on the top/right of the parent element.

##### ⬇️ Bottom

<img width="569" alt="Screen Shot 2020-06-11 at 10 03 13 AM" src="https://user-images.githubusercontent.com/2322354/84394864-7a7dd880-abcb-11ea-921b-49b92dcee287.png">

The Tooltip will render on the bottom/center of the parent element.

#### 🎨 Design

As far as the Tooltip design goes... I went with a UI that was in the spirit of "G2". Something minimal, sporting the all black/white look.

## How has this been tested?

* Tested in local Storybook
* Tested in local Gutenberg

To test...

* Run `npm install`
* Run `npm run dev`
* Add a Spacer block
* Drag to resize. You should see the Tooltip.
* Drag the RangeSlider (sidebar) to resize the Spacer. You should NOT see the Tooltip.


## Types of changes

* Add `react-resize-aware` as dependency in `@wordpress/components` (it's also used in `@wrodpress/compose`)
* Add new `<ResizeTooltip />` component
* Exports `ResizeTooltip` from `@wordpress/components`
* Integrate `<ResizeTooltip />` into Spacer block

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

The idea for this PR originally came from:
https://github.com/WordPress/gutenberg/issues/22956

## Issue comments

### github-actions on 2020-06-10T21:19:26Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642273440

**Size Change:** +1.77 kB (0%) 

**Total Size:** 1.13 MB

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/annotations/index.js` | 3.62 kB | +1 B |  |
| `build/block-directory/index.js` | 7.27 kB | +3 B (0%) |  |
| `build/block-library/index.js` | 129 kB | +87 B (0%) |  |
| `build/blocks/index.js` | 48.1 kB | +1 B |  |
| `build/components/index.js` | 197 kB | +1.66 kB (0%) |  |
| `build/compose/index.js` | 9.62 kB | +16 B (0%) |  |
| `build/data/index.js` | 8.44 kB | +1 B |  |
| `build/edit-navigation/index.js` | 8.26 kB | -1 B |  |
| `build/edit-post/index.js` | 303 kB | +6 B (0%) |  |
| `build/edit-site/index.js` | 16.6 kB | +1 B |  |
| `build/edit-widgets/index.js` | 9.33 kB | +1 B |  |
| `build/editor/index.js` | 44.8 kB | -1 B |  |
| `build/format-library/index.js` | 7.72 kB | -1 B |  |
| `build/redux-routine/index.js` | 2.85 kB | -3 B (0%) |  |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/a11y/index.js` | 1.14 kB | 0 B |  |
| `build/api-fetch/index.js` | 3.4 kB | 0 B |  |
| `build/autop/index.js` | 2.83 kB | 0 B |  |
| `build/blob/index.js` | 620 B | 0 B |  |
| `build/block-directory/style-rtl.css` | 937 B | 0 B |  |
| `build/block-directory/style.css` | 937 B | 0 B |  |
| `build/block-editor/index.js` | 106 kB | 0 B |  |
| `build/block-editor/style-rtl.css` | 10.7 kB | 0 B |  |
| `build/block-editor/style.css` | 10.7 kB | 0 B |  |
| `build/block-library/editor-rtl.css` | 7.86 kB | 0 B |  |
| `build/block-library/editor.css` | 7.87 kB | 0 B |  |
| `build/block-library/style-rtl.css` | 8 kB | 0 B |  |
| `build/block-library/style.css` | 8.01 kB | 0 B |  |
| `build/block-library/theme-rtl.css` | 730 B | 0 B |  |
| `build/block-library/theme.css` | 732 B | 0 B |  |
| `build/block-serialization-default-parser/index.js` | 1.88 kB | 0 B |  |
| `build/block-serialization-spec-parser/index.js` | 3.1 kB | 0 B |  |
| `build/components/style-rtl.css` | 15.9 kB | 0 B |  |
| `build/components/style.css` | 15.8 kB | 0 B |  |
| `build/core-data/index.js` | 11.4 kB | 0 B |  |
| `build/data-controls/index.js` | 1.29 kB | 0 B |  |
| `build/date/index.js` | 5.47 kB | 0 B |  |
| `build/deprecated/index.js` | 772 B | 0 B |  |
| `build/dom-ready/index.js` | 569 B | 0 B |  |
| `build/dom/index.js` | 3.17 kB | 0 B |  |
| `build/edit-navigation/style-rtl.css` | 1.02 kB | 0 B |  |
| `build/edit-navigation/style.css` | 1.02 kB | 0 B |  |
| `build/edit-post/style-rtl.css` | 5.5 kB | 0 B |  |
| `build/edit-post/style.css` | 5.5 kB | 0 B |  |
| `build/edit-site/style-rtl.css` | 3.03 kB | 0 B |  |
| `build/edit-site/style.css` | 3.03 kB | 0 B |  |
| `build/edit-widgets/style-rtl.css` | 2.43 kB | 0 B |  |
| `build/edit-widgets/style.css` | 2.43 kB | 0 B |  |
| `build/editor/editor-styles-rtl.css` | 468 B | 0 B |  |
| `build/editor/editor-styles.css` | 469 B | 0 B |  |
| `build/editor/style-rtl.css` | 3.8 kB | 0 B |  |
| `build/editor/style.css` | 3.8 kB | 0 B |  |
| `build/element/index.js` | 4.65 kB | 0 B |  |
| `build/escape-html/index.js` | 733 B | 0 B |  |
| `build/format-library/style-rtl.css` | 542 B | 0 B |  |
| `build/format-library/style.css` | 543 B | 0 B |  |
| `build/hooks/index.js` | 2.13 kB | 0 B |  |
| `build/html-entities/index.js` | 622 B | 0 B |  |
| `build/i18n/index.js` | 3.56 kB | 0 B |  |
| `build/is-shallow-equal/index.js` | 711 B | 0 B |  |
| `build/keyboard-shortcuts/index.js` | 2.51 kB | 0 B |  |
| `build/keycodes/index.js` | 1.94 kB | 0 B |  |
| `build/list-reusable-blocks/index.js` | 3.12 kB | 0 B |  |
| `build/list-reusable-blocks/style-rtl.css` | 446 B | 0 B |  |
| `build/list-reusable-blocks/style.css` | 447 B | 0 B |  |
| `build/media-utils/index.js` | 5.29 kB | 0 B |  |
| `build/notices/index.js` | 1.79 kB | 0 B |  |
| `build/nux/index.js` | 3.4 kB | 0 B |  |
| `build/nux/style-rtl.css` | 662 B | 0 B |  |
| `build/nux/style.css` | 657 B | 0 B |  |
| `build/plugins/index.js` | 2.56 kB | 0 B |  |
| `build/primitives/index.js` | 1.5 kB | 0 B |  |
| `build/priority-queue/index.js` | 788 B | 0 B |  |
| `build/rich-text/index.js` | 14 kB | 0 B |  |
| `build/server-side-render/index.js` | 2.68 kB | 0 B |  |
| `build/shortcode/index.js` | 1.7 kB | 0 B |  |
| `build/token-list/index.js` | 1.28 kB | 0 B |  |
| `build/url/index.js` | 4.06 kB | 0 B |  |
| `build/viewport/index.js` | 1.85 kB | 0 B |  |
| `build/warning/index.js` | 1.14 kB | 0 B |  |
| `build/wordcount/index.js` | 1.17 kB | 0 B |  |


</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### MichaelArestad on 2020-06-10T21:23:02Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642275055

This is awesome. I notice there is some strange behavior on FF. It's not sticking to the cursor. It seems the position is only updating when I pause for a second. 

![2020-06-10 15 19 36](https://user-images.githubusercontent.com/1123119/84319905-0b05db80-ab2e-11ea-9388-0582be7aeb61.gif)

As far as the design and functionality, this is pretty great. I think sticking it to the cursor more useful because my eye is already looking there.

Once that minor bug is fixed, I can see it being useful for all sorts of things. Nice work.

### ItsJonQ on 2020-06-10T21:23:38Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642275365

@MichaelArestad  Oooo! Thank you for verifying!! I'll take a look on Firefox 🦊

### ItsJonQ on 2020-06-10T21:35:41Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642280549

@MichaelArestad Can confirm your experience. Hmm! It's so weird...

On Firefox, the tooltip can follow my mouse just fine. However, when I drag to resize (using the `ResizableBox`), that's when it goes weird.

It may be something funky in that `ResizableBox` library. Will keep tinkering!

### MichaelArestad on 2020-06-10T21:48:41Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642285623

> It may be something funky in that ResizableBox library. Will keep tinkering!

Funky. Thanks for confirming.

### ItsJonQ on 2020-06-10T22:06:52Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642292565

@MichaelArestad I think I did it!

Made some minor tweaks.. The biggest one was binding the `mousemove` handler to `document` instead of `window`. Who would have known 🤷‍♀️

### MichaelArestad on 2020-06-10T22:36:40Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642303412

> @MichaelArestad I think I did it!

@ItsJonQ Nice! Can confirm! However, I have a new bug for you. I have to click the spacer drag handle twice before it the measurement shows up. I have to do this for every spacer I add to the same document.

### ItsJonQ on 2020-06-10T23:29:07Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642319014

@MichaelArestad Thank you!! I pushed a fix. Try now 😊 

(Really appreciate the feedback!)

### MichaelArestad on 2020-06-11T15:06:28Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642721642

This is much better! I'm not seeing either of those bugs anymore. Well done!

### ItsJonQ on 2020-06-11T15:08:34Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642723328

@MichaelArestad Thank you!!

@mcsf 's feedback helped me rethink + optimize things 😉

### shaunandrews on 2020-06-11T17:29:28Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642826033

Love this. I do wonder about the capitalization of "PX". Maybe its just the size of that unit's label, but I wonder if lowercase ("px") would be better? I guess its mostly personal preference.

### ItsJonQ on 2020-06-11T18:40:05Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642861554

@shaunandrews  + @MichaelArestad 

Lowecase `px`

<img width="378" alt="Screen Shot 2020-06-11 at 2 37 24 PM" src="https://user-images.githubusercontent.com/2322354/84426502-57feb600-abf1-11ea-943d-4875457eabb5.png">

![Screen Capture on 2020-06-11 at 14-37-19](https://user-images.githubusercontent.com/2322354/84426538-664cd200-abf1-11ea-8902-76c961271e48.gif)

Vote with 👍 / 👎 

Thank you!

### ItsJonQ on 2020-06-11T20:14:03Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642904298

Thanks @MichaelArestad ! Lowercase it is :)

### shaunandrews on 2020-06-11T21:51:48Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-642945246

Do we use units like this elsewhere? What does padding do?

### ItsJonQ on 2020-06-16T12:25:01Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-644729746

@mcsf Those are fair points. I've removed the "cursor" position from the ResizeTooltip. I've adjusted some logic and styles to improve the integration of `bottom` position.

We can revisit adding a cursor positioned Tooltip in the future, if needed 👍

### mcsf on 2020-06-16T14:09:08Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-644790093

> @mcsf Those are fair points. I've removed the "cursor" position from the ResizeTooltip. I've adjusted some logic and styles to improve the integration of `bottom` position.
> 
> We can revisit adding a cursor positioned Tooltip in the future, if needed 👍

Thanks for humouring me!

### ItsJonQ on 2020-06-16T14:09:54Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-644790512

> Thanks for humouring me!

Anytime 😉 ❤️

### ItsJonQ on 2020-06-18T18:46:52Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-646242555

@mcsf I just moved the ResizeTooltip under ResizableBox. Lemme know if this works! <3

### ItsJonQ on 2020-06-22T15:41:27Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-647604427

Alrighty! Travis is green and happy. Thanks for the review @mcsf !!

I'll merge it up

### mcsf on 2020-06-22T16:23:15Z

URL: https://github.com/WordPress/gutenberg/pull/23077#issuecomment-647627889

🎉

## Review threads

### Review thread 1 (resolved)

#### ItsJonQ on 2020-06-10T21:15:29Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438412633
Path: packages/components/src/resize-tooltip/index.js

```diff
@@ -0,0 +1,77 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useCallback, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Label from './label';
+import { useResizeLabel, VARIANTS } from './utils';
+import { Root } from './styles/resize-tooltip.styles';
+
+function ResizeTooltip(
+	{
+		axis,
+		className,
+		isEnabled = true,
+		fadeTimeout = 180,
+		labelRef,
+		onMove = noop,
+		onResize = noop,
+		showPx = true,
+		variant = VARIANTS.cursor,
+		zIndex = 1000,
+		...props
+	},
+	ref
+) {
+	const [ tooltipPosition, setTooltipPosition ] = useState( { x: 0, y: 0 } );
```

Using this custom implementation rather than `Tooltip` or `Popover`. I initially tried using `Popover`. However, it's (re)rendering cycles felt janky given the cursor-following experience I wanted to achieve.

#### mcsf on 2020-06-11T10:23:21Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438688179
Path: packages/components/src/resize-tooltip/index.js

```diff
@@ -0,0 +1,77 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useCallback, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Label from './label';
+import { useResizeLabel, VARIANTS } from './utils';
+import { Root } from './styles/resize-tooltip.styles';
+
+function ResizeTooltip(
+	{
+		axis,
+		className,
+		isEnabled = true,
+		fadeTimeout = 180,
+		labelRef,
+		onMove = noop,
+		onResize = noop,
+		showPx = true,
+		variant = VARIANTS.cursor,
+		zIndex = 1000,
+		...props
+	},
+	ref
+) {
+	const [ tooltipPosition, setTooltipPosition ] = useState( { x: 0, y: 0 } );
```

By using one piece of state composed of an object like this, this component can't skip effects/rendering whenever the state changes but the coordinates themselves are unchanged. This affects other components affected by this shape, like Label.

It's something to think about and compare for performance against having two props `cursorX` + `cursorY`.

#### ItsJonQ on 2020-06-11T14:19:55Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438822282
Path: packages/components/src/resize-tooltip/index.js

```diff
@@ -0,0 +1,77 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useCallback, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Label from './label';
+import { useResizeLabel, VARIANTS } from './utils';
+import { Root } from './styles/resize-tooltip.styles';
+
+function ResizeTooltip(
+	{
+		axis,
+		className,
+		isEnabled = true,
+		fadeTimeout = 180,
+		labelRef,
+		onMove = noop,
+		onResize = noop,
+		showPx = true,
+		variant = VARIANTS.cursor,
+		zIndex = 1000,
+		...props
+	},
+	ref
+) {
+	const [ tooltipPosition, setTooltipPosition ] = useState( { x: 0, y: 0 } );
```

Great suggestion! Done :)

### Review thread 2

#### ItsJonQ on 2020-06-10T21:19:17Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438414320
Path: packages/components/src/resize-tooltip/index.js

```diff
@@ -0,0 +1,77 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useCallback, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Label from './label';
+import { useResizeLabel, VARIANTS } from './utils';
+import { Root } from './styles/resize-tooltip.styles';
+
+function ResizeTooltip(
+	{
+		axis,
+		className,
+		isEnabled = true,
+		fadeTimeout = 180,
+		labelRef,
+		onMove = noop,
+		onResize = noop,
+		showPx = true,
+		variant = VARIANTS.cursor,
+		zIndex = 1000,
+		...props
+	},
+	ref
+) {
+	const [ tooltipPosition, setTooltipPosition ] = useState( { x: 0, y: 0 } );
+
+	const handleOnMove = useCallback(
+		( event ) => {
+			setTooltipPosition( { x: event.clientX, y: event.clientY } );
+			onMove( event );
+		},
+		[ onMove, variant ]
+	);
+
+	const { isActive, label, resizeListener } = useResizeLabel( {
+		axis,
+		fadeTimeout,
+		onMove: handleOnMove,
+		onResize,
+		showPx,
+		variant,
+	} );
+
+	if ( ! isEnabled ) return null;
+
+	const classes = classnames( 'components-resizable-visualizer', className );
+
+	return (
+		<Root aria-hidden="true" className={ classes } ref={ ref } { ...props }>
```

By default, I felt like it may be better to hide this tooltip via `aria-hidden`. This can be overridden via incoming props.

### Review thread 3

#### ItsJonQ on 2020-06-10T21:22:35Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438415796
Path: packages/components/src/resize-tooltip/index.js

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { forwardRef, useCallback, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Label from './label';
+import { useResizeLabel, VARIANTS } from './utils';
+import { Root } from './styles/resize-tooltip.styles';
+
+function ResizeTooltip(
+	{
+		axis,
+		className,
+		isEnabled = true,
+		fadeTimeout = 180,
+		labelRef,
+		onMove = noop,
+		onResize = noop,
+		showPx = true,
+		variant = VARIANTS.cursor,
+		zIndex = 1000,
+		...props
+	},
+	ref
+) {
+	const [ tooltipPosition, setTooltipPosition ] = useState( { x: 0, y: 0 } );
+
+	const handleOnMove = useCallback(
+		( event ) => {
+			setTooltipPosition( { x: event.clientX, y: event.clientY } );
+			onMove( event );
+		},
+		[ onMove, variant ]
+	);
+
+	const { isActive, label, resizeListener } = useResizeLabel( {
+		axis,
+		fadeTimeout,
+		onMove: handleOnMove,
+		onResize,
+		showPx,
+		variant,
+	} );
+
+	if ( ! isEnabled ) return null;
+
+	const classes = classnames( 'components-resizable-visualizer', className );
+
+	return (
+		<Root aria-hidden="true" className={ classes } ref={ ref } { ...props }>
+			{ resizeListener }
+			<Label
+				aria-hidden={ props[ 'aria-hidden' ] }
```

We have to pass this through manually to apply to the inner label element. The reason is because the inner label is Portal'ed, which means it will not inherit the `aria-hidden` attribute from the `<Root />` component.

### Review thread 4 (resolved)

#### mcsf on 2020-06-11T10:11:12Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438682093
Path: packages/components/src/resize-tooltip/styles/resize-tooltip.styles.js

```diff
@@ -0,0 +1,49 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Text from '../../text';
+import { color } from '../../utils/style-mixins';
+
+export const Root = styled.div`
+	bottom: 0;
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	position: absolute;
+	right: 0;
+	top: 0;
+`;
+
+export const TooltipWrapper = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	display: inline-flex;
+	justify-content: center;
+	opacity: 0;
+	pointer-events: none;
+	transition: opacity 120ms linear;
+
+	${ ( { isActive } ) =>
+		isActive &&
+		`
+	opacity: 1;
+` }
```

We should indent these lines to make it clear they are part of the condition.

#### ItsJonQ on 2020-06-11T12:52:05Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438758570
Path: packages/components/src/resize-tooltip/styles/resize-tooltip.styles.js

```diff
@@ -0,0 +1,49 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
+/**
+ * Internal dependencies
+ */
+import Text from '../../text';
+import { color } from '../../utils/style-mixins';
+
+export const Root = styled.div`
+	bottom: 0;
+	box-sizing: border-box;
+	left: 0;
+	pointer-events: none;
+	position: absolute;
+	right: 0;
+	top: 0;
+`;
+
+export const TooltipWrapper = styled.div`
+	align-items: center;
+	box-sizing: border-box;
+	display: inline-flex;
+	justify-content: center;
+	opacity: 0;
+	pointer-events: none;
+	transition: opacity 120ms linear;
+
+	${ ( { isActive } ) =>
+		isActive &&
+		`
+	opacity: 1;
+` }
```

This is how my VSCode auto-formatted it (with Prettier and ESLint).
I admit... it doesn't look very nice. I'll rewrite it another way 🙌 

Thanks for the note!

### Review thread 5

#### mcsf on 2020-06-11T10:17:00Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438685010
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,229 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	const [ isActive, setIsActive ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
+	 * This custom hook uses injects an iFrame into the element, allowing it
+	 * to tap into the onResize (window) callback events.
+	 */
+	const [ resizeListener, sizes ] = useResizeAware();
+
+	/**
+	 * The moveX and moveY values are used to track whether the label should
+	 * display width, height, or width x height.
+	 */
+	const [ moveX, setMoveX ] = useState( false );
+	const [ moveY, setMoveY ] = useState( false );
+
+	/**
+	 * Cached dimension values to check for width/height updates from the
+	 * sizes property from useResizeAware()
+	 */
+	const heightRef = useRef( height );
+	const widthRef = useRef( width );
+
+	/**
+	 * This timeout is used with setMoveX and setMoveY to determine of
+	 * both width and height values have changed at (roughly) the same time.
+	 */
+	const moveTimeoutRef = useRef();
+
+	const { width, height } = sizes;
+
+	useEffect( () => {
+		/**
+		 * On the initial render of useResizeAware, the height and width values are
+		 * null. They are calculated then set using via an internal useEffect hook.
+		 */
+		const isRendered = width !== null || height !== null;
+
+		if ( ! isRendered ) return;
+
+		const didWidthChange = width !== widthRef.current;
+		const didHeightChange = height !== heightRef.current;
+
+		if ( ! didWidthChange && ! didHeightChange ) return;
+
+		if ( didWidthChange ) {
+			setMoveX( true );
+		}
+
+		if ( didHeightChange ) {
+			setMoveY( true );
+		}
+
+		onResize( { width, height } );
+	}, [ width, height ] );
+
+	useEffect( () => {
+		const handleOnMouseDown = () => {
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+			setIsDragging( true );
+			setMoveX( false );
+			setMoveY( false );
+		};
+
+		const handleOnMouseUp = () => {
+			setIsDragging( false );
+			setIsActive( false );
+
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+
+			moveTimeoutRef.current = setTimeout( () => {
+				setMoveX( false );
+				setMoveY( false );
+			}, fadeTimeout * 2 );
+		};
+
+		const handleOnMouseMove = ( event ) => {
+			if ( ! isDragging ) return;
+
+			if ( width !== widthRef.current || height !== heightRef.current ) {
+				widthRef.current = width;
+				heightRef.current = height;
+
+				onMove( event );
+				setIsActive( true );
+			}
+		};
+
+		document.addEventListener( 'mousedown', handleOnMouseDown );
+		document.addEventListener( 'mousemove', handleOnMouseMove );
+		document.addEventListener( 'mouseup', handleOnMouseUp );
+
+		return () => {
+			document.removeEventListener( 'mousedown', handleOnMouseDown );
+			document.removeEventListener( 'mousemove', handleOnMouseMove );
+			document.removeEventListener( 'mouseup', handleOnMouseUp );
```

Just looking at the code, I don't think this cleanup ever does anything. That's because the `handle*` functions are re-instantiated every time the effect runs.

The other thing is that I believe we could be more judicious about the dependencies of this effect. By refactoring the handler functions and properly looking at what values are needed, I believe we could reduce the `[ width, height, isActive, isDragging ]` dependency list, and therefore not tear down and reset this effect so often.

#### ItsJonQ on 2020-06-11T14:04:49Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438812126
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,229 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	const [ isActive, setIsActive ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
+	 * This custom hook uses injects an iFrame into the element, allowing it
+	 * to tap into the onResize (window) callback events.
+	 */
+	const [ resizeListener, sizes ] = useResizeAware();
+
+	/**
+	 * The moveX and moveY values are used to track whether the label should
+	 * display width, height, or width x height.
+	 */
+	const [ moveX, setMoveX ] = useState( false );
+	const [ moveY, setMoveY ] = useState( false );
+
+	/**
+	 * Cached dimension values to check for width/height updates from the
+	 * sizes property from useResizeAware()
+	 */
+	const heightRef = useRef( height );
+	const widthRef = useRef( width );
+
+	/**
+	 * This timeout is used with setMoveX and setMoveY to determine of
+	 * both width and height values have changed at (roughly) the same time.
+	 */
+	const moveTimeoutRef = useRef();
+
+	const { width, height } = sizes;
+
+	useEffect( () => {
+		/**
+		 * On the initial render of useResizeAware, the height and width values are
+		 * null. They are calculated then set using via an internal useEffect hook.
+		 */
+		const isRendered = width !== null || height !== null;
+
+		if ( ! isRendered ) return;
+
+		const didWidthChange = width !== widthRef.current;
+		const didHeightChange = height !== heightRef.current;
+
+		if ( ! didWidthChange && ! didHeightChange ) return;
+
+		if ( didWidthChange ) {
+			setMoveX( true );
+		}
+
+		if ( didHeightChange ) {
+			setMoveY( true );
+		}
+
+		onResize( { width, height } );
+	}, [ width, height ] );
+
+	useEffect( () => {
+		const handleOnMouseDown = () => {
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+			setIsDragging( true );
+			setMoveX( false );
+			setMoveY( false );
+		};
+
+		const handleOnMouseUp = () => {
+			setIsDragging( false );
+			setIsActive( false );
+
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+
+			moveTimeoutRef.current = setTimeout( () => {
+				setMoveX( false );
+				setMoveY( false );
+			}, fadeTimeout * 2 );
+		};
+
+		const handleOnMouseMove = ( event ) => {
+			if ( ! isDragging ) return;
+
+			if ( width !== widthRef.current || height !== heightRef.current ) {
+				widthRef.current = width;
+				heightRef.current = height;
+
+				onMove( event );
+				setIsActive( true );
+			}
+		};
+
+		document.addEventListener( 'mousedown', handleOnMouseDown );
+		document.addEventListener( 'mousemove', handleOnMouseMove );
+		document.addEventListener( 'mouseup', handleOnMouseUp );
+
+		return () => {
+			document.removeEventListener( 'mousedown', handleOnMouseDown );
+			document.removeEventListener( 'mousemove', handleOnMouseMove );
+			document.removeEventListener( 'mouseup', handleOnMouseUp );
```

Thank you for pointing this out! I just pushed up some changes which should feel better (from a code/useEffect perspective). Let me know what you think of these revisions 🙏

#### mcsf on 2020-06-11T16:34:47Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438920361
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,229 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	const [ isActive, setIsActive ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
+	 * This custom hook uses injects an iFrame into the element, allowing it
+	 * to tap into the onResize (window) callback events.
+	 */
+	const [ resizeListener, sizes ] = useResizeAware();
+
+	/**
+	 * The moveX and moveY values are used to track whether the label should
+	 * display width, height, or width x height.
+	 */
+	const [ moveX, setMoveX ] = useState( false );
+	const [ moveY, setMoveY ] = useState( false );
+
+	/**
+	 * Cached dimension values to check for width/height updates from the
+	 * sizes property from useResizeAware()
+	 */
+	const heightRef = useRef( height );
+	const widthRef = useRef( width );
+
+	/**
+	 * This timeout is used with setMoveX and setMoveY to determine of
+	 * both width and height values have changed at (roughly) the same time.
+	 */
+	const moveTimeoutRef = useRef();
+
+	const { width, height } = sizes;
+
+	useEffect( () => {
+		/**
+		 * On the initial render of useResizeAware, the height and width values are
+		 * null. They are calculated then set using via an internal useEffect hook.
+		 */
+		const isRendered = width !== null || height !== null;
+
+		if ( ! isRendered ) return;
+
+		const didWidthChange = width !== widthRef.current;
+		const didHeightChange = height !== heightRef.current;
+
+		if ( ! didWidthChange && ! didHeightChange ) return;
+
+		if ( didWidthChange ) {
+			setMoveX( true );
+		}
+
+		if ( didHeightChange ) {
+			setMoveY( true );
+		}
+
+		onResize( { width, height } );
+	}, [ width, height ] );
+
+	useEffect( () => {
+		const handleOnMouseDown = () => {
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+			setIsDragging( true );
+			setMoveX( false );
+			setMoveY( false );
+		};
+
+		const handleOnMouseUp = () => {
+			setIsDragging( false );
+			setIsActive( false );
+
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+
+			moveTimeoutRef.current = setTimeout( () => {
+				setMoveX( false );
+				setMoveY( false );
+			}, fadeTimeout * 2 );
+		};
+
+		const handleOnMouseMove = ( event ) => {
+			if ( ! isDragging ) return;
+
+			if ( width !== widthRef.current || height !== heightRef.current ) {
+				widthRef.current = width;
+				heightRef.current = height;
+
+				onMove( event );
+				setIsActive( true );
+			}
+		};
+
+		document.addEventListener( 'mousedown', handleOnMouseDown );
+		document.addEventListener( 'mousemove', handleOnMouseMove );
+		document.addEventListener( 'mouseup', handleOnMouseUp );
+
+		return () => {
+			document.removeEventListener( 'mousedown', handleOnMouseDown );
+			document.removeEventListener( 'mousemove', handleOnMouseMove );
+			document.removeEventListener( 'mouseup', handleOnMouseUp );
```

> Just looking at the code, I don't think this cleanup ever does anything. That's because the `handle*` functions are re-instantiated every time the effect runs.

I don't why I said this, as it didn't make sense: the registered listeners are properly referenced in the tear-down function returned by the effect, so we should always be able to remove the old event listeners.

#### mcsf on 2020-06-11T16:52:59Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438931006
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,229 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	const [ isActive, setIsActive ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
+	 * This custom hook uses injects an iFrame into the element, allowing it
+	 * to tap into the onResize (window) callback events.
+	 */
+	const [ resizeListener, sizes ] = useResizeAware();
+
+	/**
+	 * The moveX and moveY values are used to track whether the label should
+	 * display width, height, or width x height.
+	 */
+	const [ moveX, setMoveX ] = useState( false );
+	const [ moveY, setMoveY ] = useState( false );
+
+	/**
+	 * Cached dimension values to check for width/height updates from the
+	 * sizes property from useResizeAware()
+	 */
+	const heightRef = useRef( height );
+	const widthRef = useRef( width );
+
+	/**
+	 * This timeout is used with setMoveX and setMoveY to determine of
+	 * both width and height values have changed at (roughly) the same time.
+	 */
+	const moveTimeoutRef = useRef();
+
+	const { width, height } = sizes;
+
+	useEffect( () => {
+		/**
+		 * On the initial render of useResizeAware, the height and width values are
+		 * null. They are calculated then set using via an internal useEffect hook.
+		 */
+		const isRendered = width !== null || height !== null;
+
+		if ( ! isRendered ) return;
+
+		const didWidthChange = width !== widthRef.current;
+		const didHeightChange = height !== heightRef.current;
+
+		if ( ! didWidthChange && ! didHeightChange ) return;
+
+		if ( didWidthChange ) {
+			setMoveX( true );
+		}
+
+		if ( didHeightChange ) {
+			setMoveY( true );
+		}
+
+		onResize( { width, height } );
+	}, [ width, height ] );
+
+	useEffect( () => {
+		const handleOnMouseDown = () => {
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+			setIsDragging( true );
+			setMoveX( false );
+			setMoveY( false );
+		};
+
+		const handleOnMouseUp = () => {
+			setIsDragging( false );
+			setIsActive( false );
+
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+
+			moveTimeoutRef.current = setTimeout( () => {
+				setMoveX( false );
+				setMoveY( false );
+			}, fadeTimeout * 2 );
+		};
+
+		const handleOnMouseMove = ( event ) => {
+			if ( ! isDragging ) return;
+
+			if ( width !== widthRef.current || height !== heightRef.current ) {
+				widthRef.current = width;
+				heightRef.current = height;
+
+				onMove( event );
+				setIsActive( true );
+			}
+		};
+
+		document.addEventListener( 'mousedown', handleOnMouseDown );
+		document.addEventListener( 'mousemove', handleOnMouseMove );
+		document.addEventListener( 'mouseup', handleOnMouseUp );
+
+		return () => {
+			document.removeEventListener( 'mousedown', handleOnMouseDown );
+			document.removeEventListener( 'mousemove', handleOnMouseMove );
+			document.removeEventListener( 'mouseup', handleOnMouseUp );
```

> Let me know what you think of these revisions

So I've been going back and forth looking at this effect and feeling generally unsatisfied when pondering things like the dependencies, and I think I know why. We're dealing with effects of first degree and effects of second degree, so to speak: the first-degree effects are the registration of the `mouse*` event listeners; the second-degree ones are the listeners themselves. This makes the reasoning about dependencies more confusing.

If we were stricter about constructing these effects, we might refactor this as follows:

```js
export function useResizeLabel( … ) {
  // ...

  const clearMoveTimeout = useCallback( () => {
    if ( moveTimeoutRef.current ) {
      clearTimeout( moveTimeoutRef.current );
    }
  }, [ moveTimeoutRef ] );

  const handleOnMouseDown = useCallback( () => {
    if ( ! isCursor ) return;

    setIsDragging( true );
    clearMoveTimeout();
    unsetMoveXY();
  }, [
    // meaningful dependencies (mutable)
    isCursor,
    // formal dependencies (assumed constant)
    setIsDragging, clearMoveTimeout, unsetMoveYX,
  ] );

  const handleOnMouseMove = useCallback( /* same as above */ );

  const handleOnMouseUp = useCallback( /* same as above */ );
  
  useEffect( () => {
    document.addEventListener( 'mousedown', handleOnMouseDown );
    return () => {
      document.removeEventListener( 'mousedown', handleOnMouseDown );
    };
  }, [ handleOnMouseDown ] );

  useEffect( /* same as above for `mousemove` */ );

  useEffect( /* same as above for `mouseup` */ );

  return { ... };
}
```

Note those comments about "meaningful" vs. "formal" dependencies. That was my of not making a decision as to whether we should be a strict as possible when listing dependencies or not. :) I'll leave that up to you.

#### ItsJonQ on 2020-06-11T18:47:52Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438998733
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,229 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	const [ isActive, setIsActive ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
+	 * This custom hook uses injects an iFrame into the element, allowing it
+	 * to tap into the onResize (window) callback events.
+	 */
+	const [ resizeListener, sizes ] = useResizeAware();
+
+	/**
+	 * The moveX and moveY values are used to track whether the label should
+	 * display width, height, or width x height.
+	 */
+	const [ moveX, setMoveX ] = useState( false );
+	const [ moveY, setMoveY ] = useState( false );
+
+	/**
+	 * Cached dimension values to check for width/height updates from the
+	 * sizes property from useResizeAware()
+	 */
+	const heightRef = useRef( height );
+	const widthRef = useRef( width );
+
+	/**
+	 * This timeout is used with setMoveX and setMoveY to determine of
+	 * both width and height values have changed at (roughly) the same time.
+	 */
+	const moveTimeoutRef = useRef();
+
+	const { width, height } = sizes;
+
+	useEffect( () => {
+		/**
+		 * On the initial render of useResizeAware, the height and width values are
+		 * null. They are calculated then set using via an internal useEffect hook.
+		 */
+		const isRendered = width !== null || height !== null;
+
+		if ( ! isRendered ) return;
+
+		const didWidthChange = width !== widthRef.current;
+		const didHeightChange = height !== heightRef.current;
+
+		if ( ! didWidthChange && ! didHeightChange ) return;
+
+		if ( didWidthChange ) {
+			setMoveX( true );
+		}
+
+		if ( didHeightChange ) {
+			setMoveY( true );
+		}
+
+		onResize( { width, height } );
+	}, [ width, height ] );
+
+	useEffect( () => {
+		const handleOnMouseDown = () => {
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+			setIsDragging( true );
+			setMoveX( false );
+			setMoveY( false );
+		};
+
+		const handleOnMouseUp = () => {
+			setIsDragging( false );
+			setIsActive( false );
+
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+
+			moveTimeoutRef.current = setTimeout( () => {
+				setMoveX( false );
+				setMoveY( false );
+			}, fadeTimeout * 2 );
+		};
+
+		const handleOnMouseMove = ( event ) => {
+			if ( ! isDragging ) return;
+
+			if ( width !== widthRef.current || height !== heightRef.current ) {
+				widthRef.current = width;
+				heightRef.current = height;
+
+				onMove( event );
+				setIsActive( true );
+			}
+		};
+
+		document.addEventListener( 'mousedown', handleOnMouseDown );
+		document.addEventListener( 'mousemove', handleOnMouseMove );
+		document.addEventListener( 'mouseup', handleOnMouseUp );
+
+		return () => {
+			document.removeEventListener( 'mousedown', handleOnMouseDown );
+			document.removeEventListener( 'mousemove', handleOnMouseMove );
+			document.removeEventListener( 'mouseup', handleOnMouseUp );
```

Thanks for the detailed feedback @mcsf !

I'm still unsure of when `useEffect`/`useCallback` dependencies should be used. I use to be in the camp of over-using `useCallback` (and would have done what you had suggested).

However, after reading Kent C Dodd's posts on `useCallback`, I've eased up a lot:
https://kentcdodds.com/blog/usememo-and-usecallback

Nowadays, I'm more in the mindset of...
Use `useCallback` if it causes an obvious problem (too many executions, infinite loops, slow perf). Otherwise, it should be okay.

That being said! I'm happy to make changes to accommodate the preferred styles of the project 🙇

#### mcsf on 2020-06-12T11:23:34Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r439362397
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,229 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	const [ isActive, setIsActive ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
+	 * This custom hook uses injects an iFrame into the element, allowing it
+	 * to tap into the onResize (window) callback events.
+	 */
+	const [ resizeListener, sizes ] = useResizeAware();
+
+	/**
+	 * The moveX and moveY values are used to track whether the label should
+	 * display width, height, or width x height.
+	 */
+	const [ moveX, setMoveX ] = useState( false );
+	const [ moveY, setMoveY ] = useState( false );
+
+	/**
+	 * Cached dimension values to check for width/height updates from the
+	 * sizes property from useResizeAware()
+	 */
+	const heightRef = useRef( height );
+	const widthRef = useRef( width );
+
+	/**
+	 * This timeout is used with setMoveX and setMoveY to determine of
+	 * both width and height values have changed at (roughly) the same time.
+	 */
+	const moveTimeoutRef = useRef();
+
+	const { width, height } = sizes;
+
+	useEffect( () => {
+		/**
+		 * On the initial render of useResizeAware, the height and width values are
+		 * null. They are calculated then set using via an internal useEffect hook.
+		 */
+		const isRendered = width !== null || height !== null;
+
+		if ( ! isRendered ) return;
+
+		const didWidthChange = width !== widthRef.current;
+		const didHeightChange = height !== heightRef.current;
+
+		if ( ! didWidthChange && ! didHeightChange ) return;
+
+		if ( didWidthChange ) {
+			setMoveX( true );
+		}
+
+		if ( didHeightChange ) {
+			setMoveY( true );
+		}
+
+		onResize( { width, height } );
+	}, [ width, height ] );
+
+	useEffect( () => {
+		const handleOnMouseDown = () => {
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+			setIsDragging( true );
+			setMoveX( false );
+			setMoveY( false );
+		};
+
+		const handleOnMouseUp = () => {
+			setIsDragging( false );
+			setIsActive( false );
+
+			if ( moveTimeoutRef.current ) {
+				clearTimeout( moveTimeoutRef.current );
+			}
+
+			moveTimeoutRef.current = setTimeout( () => {
+				setMoveX( false );
+				setMoveY( false );
+			}, fadeTimeout * 2 );
+		};
+
+		const handleOnMouseMove = ( event ) => {
+			if ( ! isDragging ) return;
+
+			if ( width !== widthRef.current || height !== heightRef.current ) {
+				widthRef.current = width;
+				heightRef.current = height;
+
+				onMove( event );
+				setIsActive( true );
+			}
+		};
+
+		document.addEventListener( 'mousedown', handleOnMouseDown );
+		document.addEventListener( 'mousemove', handleOnMouseMove );
+		document.addEventListener( 'mouseup', handleOnMouseUp );
+
+		return () => {
+			document.removeEventListener( 'mousedown', handleOnMouseDown );
+			document.removeEventListener( 'mousemove', handleOnMouseMove );
+			document.removeEventListener( 'mouseup', handleOnMouseUp );
```

> I'm happy to make changes to accommodate the preferred styles of the project

First, a disclaimer: this is really not a preferred style, just the result of my spitballing, and it's important to remember that in general, when it comes to React hooks, much is up in the air! I don't think you should just change to what I suggest. :)

I totally agree on being careful about `useCallback`, and in that aspect my example is really not compelling.

My focus was on isolating each effect as much as possible and then being _exhaustive_ about its dependencies. I use that term based on the idea of the [`exhaustive-deps` lint rule](https://github.com/facebook/react/issues/14920). The idea is quite interesting in theory.

In my mind, it can prevent a lot of subtle bugs that could happen, especially in the long term as components are tweaked and it becomes harder to trace the implicit (i.e. not exhaustively declared) dependencies of an effect or other hook.

In practice, though, it becomes awkward. Any function called inside an effect or callback is technically a dependency, and if we're dealing with a function declared in the body of the component then this dependency changes all the time. At that point, we have three choices:

1. Drop the requirement for "exhaustive" deps
2. Generously use `useCallback` to cache those functions so that the effect in question doesn't re-run for no reason
3. Accept that the effect will run frequently

This obviously only opens the door for more questions. :) Like, for choices 2 and 3, how much is too much function caching, and how much it too much cache busting? For choice 1, how can design hooks so as to minimise bugs related to state, cache and closure?

* * *

Finally, and to help unblock things here: I really don't mind that we write effects in the way that makes most intuitive sense and is performant. In the case of these effects, it's clear that the the point is to register three DOM-event handlers that will more or less be active for as long as the component exists. We can totally keep the effects as you wrote them. ;)

### Review thread 6 (resolved)

#### mcsf on 2020-06-11T16:27:26Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438916062
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,264 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout, setTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	/**
+	 * The width/height values derive from this special useResizeAwere hook.
```

Typo at `Awere`.

### Review thread 7 (resolved)

#### mcsf on 2020-06-11T16:29:38Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438917387
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,264 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout, setTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	/**
```

Our situation in WP-JS is a little awkward when it comes to multi-line comments, but in any case [we should avoid the leading double asterisk](https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/#multi-line-comments) unless it's opening a JSDoc block.

#### ItsJonQ on 2020-06-11T18:30:55Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r438989898
Path: packages/components/src/resize-tooltip/utils.js

```diff
@@ -0,0 +1,264 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import useResizeAware from 'react-resize-aware';
+
+/**
+ * WordPress dependencies
+ */
+import { useEffect, useRef, useState } from '@wordpress/element';
+
+const { clearTimeout, setTimeout } = window;
+
+export const POSITIONS = {
+	bottom: 'bottom',
+	cursor: 'cursor',
+	corner: 'corner',
+};
+
+/**
+ * @typedef {Object} UseResizeLabelProps
+ *
+ * @property {boolean} isActive The visibility of the label.
+ * @property {undefined|string} label The label value.
+ * @property {Function} resizeListener Element to be rendered for resize listening events.
+ */
+
+/**
+ * Custom hook that manages resize listener events. It also provides a label
+ * based on current resize width x height values.
+ *
+ * @param {Object} props
+ * @param {string} props.axis Only shows the label corresponding to the axis.
+ * @param {number} props.fadeTimeout Duration (ms) before deactivating the resize label.
+ * @param {Function} props.onMove Callback when a resize occurs. Provides onMouseEvent event callback.
+ * @param {boolean} props.onResize Callback when a resize occurs. Provides { width, height } callback.
+ * @param {string} props.position Adjusts label value.
+ * @param {boolean} props.showPx Whether to add `PX` to the label.
+ *
+ * @return {UseResizeLabelProps} Properties for hook.
+ */
+export function useResizeLabel( {
+	axis,
+	fadeTimeout = 180,
+	onMove = noop,
+	onResize = noop,
+	position = POSITIONS.cursor,
+	showPx = false,
+} ) {
+	const [ isDragging, setIsDragging ] = useState( false );
+	/**
```

Oop! 🙊 . Thank you for the reminder! This is something I have to get use to!

### Review thread 8

#### mcsf on 2020-06-16T14:31:54Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r440897596
Path: packages/block-library/src/spacer/edit.js

```diff
@@ -52,17 +74,16 @@ const SpacerEdit = ( {
 					bottomLeft: false,
 					topLeft: false,
 				} }
-				onResizeStart={ onResizeStart }
-				onResizeStop={ ( event, direction, elt, delta ) => {
-					onResizeStop();
-					const spacerHeight = Math.min(
-						parseInt( height + delta.height, 10 ),
-						MAX_SPACER_HEIGHT
-					);
-					updateHeight( spacerHeight );
-				} }
+				onResizeStart={ handleOnResizeStart }
+				onResizeStop={ handleOnResizeStop }
 				showHandle={ isSelected }
-			/>
+			>
+				<ResizeTooltip
+					axis="y"
+					isVisible={ isResizing }
+					position="bottom"
+				/>
```

Have you considered rendering actually _in_ ResizableBox? If there are reasons to keep it separate, do let me know. Otherwise, it might be a boon in developer experience that ResizableBox could handle everything related to resizing on its own.

I can't help but think that `axis` and `position` can be derived from ResizableBox's `enable` and to some extent `size` props. If we were to move Tooltip into Box the consumer of the component wouldn't have to worry about such extra props.

#### ItsJonQ on 2020-06-16T15:14:56Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r440930833
Path: packages/block-library/src/spacer/edit.js

```diff
@@ -52,17 +74,16 @@ const SpacerEdit = ( {
 					bottomLeft: false,
 					topLeft: false,
 				} }
-				onResizeStart={ onResizeStart }
-				onResizeStop={ ( event, direction, elt, delta ) => {
-					onResizeStop();
-					const spacerHeight = Math.min(
-						parseInt( height + delta.height, 10 ),
-						MAX_SPACER_HEIGHT
-					);
-					updateHeight( spacerHeight );
-				} }
+				onResizeStart={ handleOnResizeStart }
+				onResizeStop={ handleOnResizeStop }
 				showHandle={ isSelected }
-			/>
+			>
+				<ResizeTooltip
+					axis="y"
+					isVisible={ isResizing }
+					position="bottom"
+				/>
```

@mcsf I have considered putting it in `ResizableBox` :). That's how this idea started out.
The reason I thought of keeping it independent was for added flexibility. That is, to show dimensions on resize that aren't triggered by `ResizableBox`. For example, browser `window` resize.

To start, we could have this be a default (experimental) feature of `ResizableBox`. Something like `<ResizableBox __experimentalShowTooltip={true} />` would show the Tooltip.

What do you think? :)

#### mcsf on 2020-06-18T10:45:38Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442136659
Path: packages/block-library/src/spacer/edit.js

```diff
@@ -52,17 +74,16 @@ const SpacerEdit = ( {
 					bottomLeft: false,
 					topLeft: false,
 				} }
-				onResizeStart={ onResizeStart }
-				onResizeStop={ ( event, direction, elt, delta ) => {
-					onResizeStop();
-					const spacerHeight = Math.min(
-						parseInt( height + delta.height, 10 ),
-						MAX_SPACER_HEIGHT
-					);
-					updateHeight( spacerHeight );
-				} }
+				onResizeStart={ handleOnResizeStart }
+				onResizeStop={ handleOnResizeStop }
 				showHandle={ isSelected }
-			/>
+			>
+				<ResizeTooltip
+					axis="y"
+					isVisible={ isResizing }
+					position="bottom"
+				/>
```

>  The reason I thought of keeping it independent was for added flexibility. That is, to show dimensions on resize that aren't triggered by `ResizableBox`. For example, browser `window` resize.

Hm, I could see that as an interaction, although personally I would wonder _when_ we actually expect users to resize their proper window in a context where we'd show the tool tip. :)

So I think we're still in this territory: "the position I tend to come from is to minimise how much is exposed at first, and see over time what should be opened up". Internally, ResizeTooltip is a good component to build separately, but I would **keep it private** to our components.

> To start, we could have this be a default (experimental) feature of `ResizableBox`. Something like `<ResizableBox __experimentalShowTooltip={true} />` would show the Tooltip.

I'd be happy to see that experiment. :) Don't feel like you need to do it in this PR, though, as long as we keep the right things private/experimental.

#### ItsJonQ on 2020-06-18T16:41:32Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442361201
Path: packages/block-library/src/spacer/edit.js

```diff
@@ -52,17 +74,16 @@ const SpacerEdit = ( {
 					bottomLeft: false,
 					topLeft: false,
 				} }
-				onResizeStart={ onResizeStart }
-				onResizeStop={ ( event, direction, elt, delta ) => {
-					onResizeStop();
-					const spacerHeight = Math.min(
-						parseInt( height + delta.height, 10 ),
-						MAX_SPACER_HEIGHT
-					);
-					updateHeight( spacerHeight );
-				} }
+				onResizeStart={ handleOnResizeStart }
+				onResizeStop={ handleOnResizeStop }
 				showHandle={ isSelected }
-			/>
+			>
+				<ResizeTooltip
+					axis="y"
+					isVisible={ isResizing }
+					position="bottom"
+				/>
```

> Internally, ResizeTooltip is a good component to build separately, but I would keep it private to our components.

@mcsf Sounds good to me 👍 . With this PR, in it's current state, does exporting it as `__experimental` qualify? Or would it be better to integrate it into `ResizableBox` and not export it at all?

Thanks!

#### mcsf on 2020-06-18T17:16:12Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442381586
Path: packages/block-library/src/spacer/edit.js

```diff
@@ -52,17 +74,16 @@ const SpacerEdit = ( {
 					bottomLeft: false,
 					topLeft: false,
 				} }
-				onResizeStart={ onResizeStart }
-				onResizeStop={ ( event, direction, elt, delta ) => {
-					onResizeStop();
-					const spacerHeight = Math.min(
-						parseInt( height + delta.height, 10 ),
-						MAX_SPACER_HEIGHT
-					);
-					updateHeight( spacerHeight );
-				} }
+				onResizeStart={ handleOnResizeStart }
+				onResizeStop={ handleOnResizeStop }
 				showHandle={ isSelected }
-			/>
+			>
+				<ResizeTooltip
+					axis="y"
+					isVisible={ isResizing }
+					position="bottom"
+				/>
```

> in it's current state, does exporting it as `__experimental` qualify? Or would it be better to integrate it into `ResizableBox` and not export it at all?

If you don't mind too much, integrating in `ResizableBox` and not exporting would be the best, IMO!

#### ItsJonQ on 2020-06-18T17:19:18Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442383295
Path: packages/block-library/src/spacer/edit.js

```diff
@@ -52,17 +74,16 @@ const SpacerEdit = ( {
 					bottomLeft: false,
 					topLeft: false,
 				} }
-				onResizeStart={ onResizeStart }
-				onResizeStop={ ( event, direction, elt, delta ) => {
-					onResizeStop();
-					const spacerHeight = Math.min(
-						parseInt( height + delta.height, 10 ),
-						MAX_SPACER_HEIGHT
-					);
-					updateHeight( spacerHeight );
-				} }
+				onResizeStart={ handleOnResizeStart }
+				onResizeStop={ handleOnResizeStop }
 				showHandle={ isSelected }
-			/>
+			>
+				<ResizeTooltip
+					axis="y"
+					isVisible={ isResizing }
+					position="bottom"
+				/>
```

@mcsf Alrighty! Let's go with that for now :)

### Review thread 9

#### mcsf on 2020-06-18T20:48:38Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442493952
Path: docs/manifest.json

```diff
@@ -1022,7 +1022,7 @@
 	{
 		"title": "ResizeTooltip",
 		"slug": "resize-tooltip",
-		"markdown_source": "../packages/components/src/resize-tooltip/README.md",
+		"markdown_source": "../packages/components/src/resizable-box/resize-tooltip/README.md",
```

Should no longer be in the manifest, right?

#### ItsJonQ on 2020-06-18T22:25:28Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442534648
Path: docs/manifest.json

```diff
@@ -1022,7 +1022,7 @@
 	{
 		"title": "ResizeTooltip",
 		"slug": "resize-tooltip",
-		"markdown_source": "../packages/components/src/resize-tooltip/README.md",
+		"markdown_source": "../packages/components/src/resizable-box/resize-tooltip/README.md",
```

I'm... not sure. This was auto generated. There's a `README.md` inside the directory. I guess I can remove it? Ideally I'd like to preserve the docs though.

An alternative would be to inline comment some of the documentation (for now)

#### mcsf on 2020-06-18T22:39:58Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r442539211
Path: docs/manifest.json

```diff
@@ -1022,7 +1022,7 @@
 	{
 		"title": "ResizeTooltip",
 		"slug": "resize-tooltip",
-		"markdown_source": "../packages/components/src/resize-tooltip/README.md",
+		"markdown_source": "../packages/components/src/resizable-box/resize-tooltip/README.md",
```

Hmm... yeah, I see now. Well, I don’t have a preference. Maybe @nosolosw has a different take on how we should document components and let these tools work for us? Anyway, LGTM :)

#### oandregal on 2020-06-23T07:44:22Z

URL: https://github.com/WordPress/gutenberg/pull/23077#discussion_r444026305
Path: docs/manifest.json

```diff
@@ -1022,7 +1022,7 @@
 	{
 		"title": "ResizeTooltip",
 		"slug": "resize-tooltip",
-		"markdown_source": "../packages/components/src/resize-tooltip/README.md",
+		"markdown_source": "../packages/components/src/resizable-box/resize-tooltip/README.md",
```

I took a peek at how the script that processes the handbook docs works. It seems that it looks for README files within the components folder ([see](https://github.com/WordPress/gutenberg/blob/master/docs/tool/manifest.js#L9)) and then updates the manifest used to expose them in the handbook (see [ResizableBox page](https://developer.wordpress.org/block-editor/components/resize-tooltip/)).

I'm unfamiliar with how we deal with subcomponents that aren't exposed via the `@wordpress/components` package. I had a quick look and this seems to be the only case? Anyway, it seems that we should either expose the resize-tooltip as a component proper or look at ways to remove its page from the handbook as it can't be used by external consumers of the API. For the latter, we could go with a quick hack (use a lowercased readme, use any other name for the file) or fix the issue (update the glob pattern to only match README files within the top-level component folder).

## Changed files

- docs/manifest.json (MODIFIED, +6/-0)
- package-lock.json (MODIFIED, +1/-0)
- packages/block-library/src/spacer/edit.js (MODIFIED, +25/-9)
- packages/components/package.json (MODIFIED, +1/-0)
- packages/components/src/resizable-box/index.js (MODIFIED, +17/-2)
- packages/components/src/resizable-box/resize-tooltip/README.md (ADDED, +105/-0)
- packages/components/src/resizable-box/resize-tooltip/index.js (ADDED, +64/-0)
- packages/components/src/resizable-box/resize-tooltip/label.js (ADDED, +85/-0)
- packages/components/src/resizable-box/resize-tooltip/styles/resize-tooltip.styles.js (ADDED, +53/-0)
- packages/components/src/resizable-box/resize-tooltip/utils.js (ADDED, +222/-0)
- packages/components/src/resizable-box/stories/index.js (MODIFIED, +5/-0)
- packages/components/src/resizable-box/styles/resizable-box.styles.js (ADDED, +0/-0)
- packages/components/src/utils/colors-values.js (MODIFIED, +2/-0)
