---
number: 38994
type: pull_request
state: MERGED
merged_at: 2022-03-02T10:32:00Z
url: https://github.com/WordPress/gutenberg/pull/38994
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Type] Enhancement","[Package] Components","[Feature] Component System"]
---

# PR #38994: Confirmdialog: add custom button text

- URL: https://github.com/WordPress/gutenberg/pull/38994
- Author: chad1008
- Created: 2022-02-22T16:43:28Z
- Updated: 2022-03-15T23:24:11Z
- Comments: 1 of 1
- Review threads: 9 of 9
- Changed files: 6 of 6
- Diff: ../pr-diffs/38994.diff

## Body

## Description
Follow up to https://github.com/WordPress/gutenberg/pull/37602#discussion_r793911945, this PR adds the ability to set custom label text for the confirm and cancel buttons in the experimental `ConfirmDialog` component.

cc @fullofcaffeine @ciampo 

## Testing Instructions
- Launch Storybook locally
- locate the ConfirmDialog component and select the new 'With Custom Button Labels" Story
- manipulate the new "cancel button" and "confirm button" knobs to test different text strings
- make sure all components tests pass, as this PR also adds a new test to confirm the specified button text is being rendered

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->
- [ ] I've updated related schemas if appropriate. <!-- Reference: https://github.com/WordPress/gutenberg/tree/trunk/schemas -->

## Issue comments

### chad1008 on 2022-02-28T20:05:14Z

URL: https://github.com/WordPress/gutenberg/pull/38994#issuecomment-1054616073

Component readme and changelog updated ([7c46610](https://github.com/WordPress/gutenberg/pull/38994/commits/7c466108b452bcf4a346c13cf45bdaff364e59aa))  ✅

## Review threads

### Review thread 1 (resolved)

#### mirka on 2022-02-22T21:33:59Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r812376378
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -29,6 +29,34 @@ describe( 'Confirm', () => {
 
 				expect( dialog ).toBeInTheDocument();
 
+				elementsTexts.forEach( ( txt ) => {
+					const el = wrapper.getByText( txt );
+					expect( el ).toBeInTheDocument();
+				} );
+			} );
+			it( 'should render correctly with custom button lables', () => {
+				const cancelButtonText = 'No thanks';
+				const confirmButtonText = 'Yes please!';
+				const wrapper = render(
```

Non-blocking: I see this "wrapper" pattern is taken from other tests in the file, but dropping this link in case you want your tests to be more up-to-date 🙂
https://kentcdodds.com/blog/common-mistakes-with-react-testing-library#not-using-screen

#### chad1008 on 2022-02-28T15:20:37Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r815985495
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -29,6 +29,34 @@ describe( 'Confirm', () => {
 
 				expect( dialog ).toBeInTheDocument();
 
+				elementsTexts.forEach( ( txt ) => {
+					const el = wrapper.getByText( txt );
+					expect( el ).toBeInTheDocument();
+				} );
+			} );
+			it( 'should render correctly with custom button lables', () => {
+				const cancelButtonText = 'No thanks';
+				const confirmButtonText = 'Yes please!';
+				const wrapper = render(
```

Much appreciated! I read up a bit on `screen` and implemented it in the new test (f43500b). I can do the same for other, similar `wrappers` in the file as well... unless you think that would be better left for a separate PR?

#### mirka on 2022-03-01T09:08:40Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816576148
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -29,6 +29,34 @@ describe( 'Confirm', () => {
 
 				expect( dialog ).toBeInTheDocument();
 
+				elementsTexts.forEach( ( txt ) => {
+					const el = wrapper.getByText( txt );
+					expect( el ).toBeInTheDocument();
+				} );
+			} );
+			it( 'should render correctly with custom button lables', () => {
+				const cancelButtonText = 'No thanks';
+				const confirmButtonText = 'Yes please!';
+				const wrapper = render(
```

Cool! No strong opinion either way, but personally I don't think the `wrapper` thing is worth going in _just_ to fix it. More like a "fix it if you touch it" thing.

#### chad1008 on 2022-03-01T21:08:34Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r817141578
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -29,6 +29,34 @@ describe( 'Confirm', () => {
 
 				expect( dialog ).toBeInTheDocument();
 
+				elementsTexts.forEach( ( txt ) => {
+					const el = wrapper.getByText( txt );
+					expect( el ).toBeInTheDocument();
+				} );
+			} );
+			it( 'should render correctly with custom button lables', () => {
+				const cancelButtonText = 'No thanks';
+				const confirmButtonText = 'Yes please!';
+				const wrapper = render(
```

Sounds good! Thanks again!

### Review thread 2 (resolved)

#### mirka on 2022-02-22T21:38:31Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r812379285
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -29,6 +29,34 @@ describe( 'Confirm', () => {
 
 				expect( dialog ).toBeInTheDocument();
 
+				elementsTexts.forEach( ( txt ) => {
+					const el = wrapper.getByText( txt );
+					expect( el ).toBeInTheDocument();
+				} );
```

I see these are also taken from existing patterns in this test file, but I would recommend doing a `getByRole( 'button', { name: ... } )` query for the buttons.
https://testing-library.com/docs/queries/about/#priority

#### chad1008 on 2022-02-28T15:20:48Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r815985659
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -29,6 +29,34 @@ describe( 'Confirm', () => {
 
 				expect( dialog ).toBeInTheDocument();
 
+				elementsTexts.forEach( ( txt ) => {
+					const el = wrapper.getByText( txt );
+					expect( el ).toBeInTheDocument();
+				} );
```

Got it, and love the idea - thanks for the helpful links! pushed 64e34a8

### Review thread 3 (resolved)

#### mirka on 2022-02-22T21:41:54Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r812381620
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -75,8 +77,8 @@ function ConfirmDialog(
 		[ handleEvent, onConfirm ]
 	);
 
-	const cancelLabel = __( 'Cancel' );
-	const confirmLabel = __( 'OK' );
+	const cancelLabel = cancelButtonText ? cancelButtonText : __( 'Cancel' );
+	const confirmLabel = confirmButtonText ? confirmButtonText : __( 'OK' );
```

Non-blocking: Maybe better for readability?

```suggestion
	const cancelLabel = cancelButtonText ?? __( 'Cancel' );
	const confirmLabel = confirmButtonText ?? : __( 'OK' );
```

### Review thread 4 (resolved)

#### mirka on 2022-02-22T21:45:58Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r812384325
Path: packages/components/src/confirm-dialog/stories/index.js

```diff
@@ -25,6 +25,8 @@ export default {
 
 const daText = () =>
 	text( 'message', 'Would you like to privately publish the post now?' );
+const daCancelText = () => text( 'cancel button', 'No thanks' );
+const daConfirmText = () => text( 'confirm button', 'Yes please!' );
```

Non-blocking: I'm a tiny bit concerned that we're adding more Knob stories when we have to refactor them very soon (#35665), but I'm fine if you don't have the appetite to take that on right now. Just mentioning in case you're planning on adding more stories soon.

(We'll likely also have to put the "uncontrolled" stories behind a trigger button too, because the auto-opening of the modal will be a usability issue in our docs pages.)

#### ciampo on 2022-02-23T14:35:52Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r812956800
Path: packages/components/src/confirm-dialog/stories/index.js

```diff
@@ -25,6 +25,8 @@ export default {
 
 const daText = () =>
 	text( 'message', 'Would you like to privately publish the post now?' );
+const daCancelText = () => text( 'cancel button', 'No thanks' );
+const daConfirmText = () => text( 'confirm button', 'Yes please!' );
```

Agree on both points — these could be two small follow-ups to this PR:

1. Refactor stories to Controls
2. Add a trigger to the stories with auto-opening modals

#### chad1008 on 2022-02-28T14:34:57Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r815943540
Path: packages/components/src/confirm-dialog/stories/index.js

```diff
@@ -25,6 +25,8 @@ export default {
 
 const daText = () =>
 	text( 'message', 'Would you like to privately publish the post now?' );
+const daCancelText = () => text( 'cancel button', 'No thanks' );
+const daConfirmText = () => text( 'confirm button', 'Yes please!' );
```

> I'm a tiny bit concerned that we're adding more Knob stories when we have to refactor them very soon (https://github.com/WordPress/gutenberg/issues/35665)

Ah, that's really good to know, thanks - I'll bear that in mind for future Components work, and address it for this one in another PR, along with looking into the trigger button implementation!

### Review thread 5 (resolved)

#### ciampo on 2022-02-23T14:38:28Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r812959760
Path: packages/components/src/confirm-dialog/types.ts

```diff
@@ -10,6 +10,8 @@ export type DialogInputEvent =
 type BaseProps = {
 	children: ReactNode;
 	onConfirm: ( event: DialogInputEvent ) => void;
+	confirmButtonText: string;
+	cancelButtonText: string;
```

We should consider whether we want to allow only text, or if we want to be more permissive and allow any React component

Switching to allowing any React component (using the type `React.ReactNode`) would make this component more flexible, although in this case we may want to be more strict on purpose and only allow simple text in those buttons. What do folks think?

#### alexstine on 2022-02-25T17:13:30Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r814947393
Path: packages/components/src/confirm-dialog/types.ts

```diff
@@ -10,6 +10,8 @@ export type DialogInputEvent =
 type BaseProps = {
 	children: ReactNode;
 	onConfirm: ( event: DialogInputEvent ) => void;
+	confirmButtonText: string;
+	cancelButtonText: string;
```

For this type of basic dialog, basic text would be my vote.

Since we have fallback text, shouldn't we be marking these as optional? E.g.

```
confirmButtonText?: string;
cancelButtonText?: string;
```

#### ciampo on 2022-02-25T18:12:05Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r814988620
Path: packages/components/src/confirm-dialog/types.ts

```diff
@@ -10,6 +10,8 @@ export type DialogInputEvent =
 type BaseProps = {
 	children: ReactNode;
 	onConfirm: ( event: DialogInputEvent ) => void;
+	confirmButtonText: string;
+	cancelButtonText: string;
```

> For this type of basic dialog, basic text would be my vote.

That makes sense

> Since we have fallback text, shouldn't we be marking these as optional? E.g.

Absolutely, I must have missed it during my review. Thank you for flagging it!

#### chad1008 on 2022-02-28T15:21:47Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r815986537
Path: packages/components/src/confirm-dialog/types.ts

```diff
@@ -10,6 +10,8 @@ export type DialogInputEvent =
 type BaseProps = {
 	children: ReactNode;
 	onConfirm: ( event: DialogInputEvent ) => void;
+	confirmButtonText: string;
+	cancelButtonText: string;
```

> Since we have fallback text, shouldn't we be marking these as optional?

Good call, thanks! Pushed 750e4d3

### Review thread 6 (resolved)

#### ciampo on 2022-02-28T19:36:18Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816192659
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -75,8 +77,8 @@ function ConfirmDialog(
 		[ handleEvent, onConfirm ]
 	);
 
-	const cancelLabel = __( 'Cancel' );
-	const confirmLabel = __( 'OK' );
+	const cancelLabel = cancelButtonText ?? __( 'Cancel' );
+	const confirmLabel = confirmButtonText ?? : __( 'OK' );
```

```suggestion
	const confirmLabel = confirmButtonText ??  __( 'OK' );
```

#### ciampo on 2022-02-28T19:37:01Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816193113
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -75,8 +77,8 @@ function ConfirmDialog(
 		[ handleEvent, onConfirm ]
 	);
 
-	const cancelLabel = __( 'Cancel' );
-	const confirmLabel = __( 'OK' );
+	const cancelLabel = cancelButtonText ?? __( 'Cancel' );
+	const confirmLabel = confirmButtonText ?? : __( 'OK' );
```

@chad1008 I just pushed a tiny commit to fix a typo/syntax error :)

#### mirka on 2022-03-01T09:10:34Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816577543
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -75,8 +77,8 @@ function ConfirmDialog(
 		[ handleEvent, onConfirm ]
 	);
 
-	const cancelLabel = __( 'Cancel' );
-	const confirmLabel = __( 'OK' );
+	const cancelLabel = cancelButtonText ?? __( 'Cancel' );
+	const confirmLabel = confirmButtonText ?? : __( 'OK' );
```

Lol my bad! 🙈

### Review thread 7 (resolved)

#### ciampo on 2022-02-28T19:37:53Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816193654
Path: packages/components/src/confirm-dialog/component.tsx

```diff
@@ -75,8 +77,8 @@ function ConfirmDialog(
 		[ handleEvent, onConfirm ]
 	);
 
-	const cancelLabel = __( 'Cancel' );
-	const confirmLabel = __( 'OK' );
+	const cancelLabel = cancelButtonText ?? __( 'Cancel' );
+	const confirmLabel = confirmButtonText ??  __( 'OK' );
```

```suggestion
	const confirmLabel = confirmButtonText ?? __( 'OK' );
```

### Review thread 8 (resolved)

#### fullofcaffeine on 2022-03-01T01:44:05Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816385865
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -34,6 +35,33 @@ describe( 'Confirm', () => {
 					expect( el ).toBeInTheDocument();
 				} );
 			} );
+			it( 'should render correctly with custom button lables', () => {
```

Small typo here: `lables` -> `labels`

#### chad1008 on 2022-03-01T21:07:49Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r817141124
Path: packages/components/src/confirm-dialog/test/index.js

```diff
@@ -34,6 +35,33 @@ describe( 'Confirm', () => {
 					expect( el ).toBeInTheDocument();
 				} );
 			} );
+			it( 'should render correctly with custom button lables', () => {
```

Thank you! (and thanks for the fix!)

### Review thread 9 (resolved)

#### mirka on 2022-03-01T09:13:33Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r816579874
Path: packages/components/src/confirm-dialog/README.md

```diff
@@ -82,47 +79,61 @@ Multiple `ConfirmDialog's is an edge case that's currently not officially suppor
 ```ts
 type DialogInputEvent =
 	| KeyboardEvent< HTMLDivElement >
-	| MouseEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
 ```
 
 ## Props
 
 ### `title`: `string`
 
-- Required: No
+-   Required: No
 
 An optional `title` for the dialog. Setting a title will render it in a title bar at the top of the dialog, making it a bit taller. The bar will also include an `x` close button at the top-right corner.
 
 ### `children`: `React.ReactNode`
 
-- Required: Yes
+-   Required: Yes
 
 The actual message for the dialog. It's passed as children and any valid `ReactNode` is accepted:
 
 ```jsx
 <ConfirmDialog>
- Are you sure? <strong>This action cannot be undone!</strong>
+	Are you sure? <strong>This action cannot be undone!</strong>
 </ConfirmDialog>
 ```
 
 ### `isOpen`: `boolean`
 
-- Required: No
+-   Required: No
 
 Defines if the dialog is open (displayed) or closed (not rendered/displayed). It also implicitly toggles the controlled mode if set or the uncontrolled mode if it's not set.
 
 ### `onConfirm`: `( event: DialogInputEvent ) => void`
 
-- Required: Yes
+-   Required: Yes
 
 The callback that's called when the user confirms. A confirmation can happen when the `OK` button is clicked or when `Enter` is pressed.
 
 ### `onCancel`: `( event: DialogInputEvent ) => void`
 
-- Required: Only if `isOpen` is not set
+-   Required: Only if `isOpen` is not set
 
 The callback that's called when the user cancels. A cancellation can happen when the `Cancel` button is clicked, when the `ESC` key is pressed, or when a click outside of the dialog focus is detected (i.e. in the overlay).
 
 It's not required if `isOpen` is not set (uncontrolled mode), as the component will take care of closing itself, but you can still pass a callback if something must be done upon cancelling (the component will still close itself in this case).
 
 If `isOpen` is set (controlled mode), then it's required, and you need to set the state that defines `isOpen` to `false` as part of this callback if you want the dialog to close when the user cancels.
+
+### `confirmButtonText`: `string`
+
+-   Required: No
+-   Default: OK
+
+The optional custom text to display as the confirmation button's label
+
+### `cancelButtonText`: `string`
+
+-   Required: No
+-   Default: Cancel
```

Nit:

```suggestion
-   Default: "OK"

The optional custom text to display as the confirmation button's label

### `cancelButtonText`: `string`

-   Required: No
-   Default: "Cancel"
```

#### chad1008 on 2022-03-01T21:05:32Z

URL: https://github.com/WordPress/gutenberg/pull/38994#discussion_r817139724
Path: packages/components/src/confirm-dialog/README.md

```diff
@@ -82,47 +79,61 @@ Multiple `ConfirmDialog's is an edge case that's currently not officially suppor
 ```ts
 type DialogInputEvent =
 	| KeyboardEvent< HTMLDivElement >
-	| MouseEvent< HTMLButtonElement >
+	| MouseEvent< HTMLButtonElement >;
 ```
 
 ## Props
 
 ### `title`: `string`
 
-- Required: No
+-   Required: No
 
 An optional `title` for the dialog. Setting a title will render it in a title bar at the top of the dialog, making it a bit taller. The bar will also include an `x` close button at the top-right corner.
 
 ### `children`: `React.ReactNode`
 
-- Required: Yes
+-   Required: Yes
 
 The actual message for the dialog. It's passed as children and any valid `ReactNode` is accepted:
 
 ```jsx
 <ConfirmDialog>
- Are you sure? <strong>This action cannot be undone!</strong>
+	Are you sure? <strong>This action cannot be undone!</strong>
 </ConfirmDialog>
 ```
 
 ### `isOpen`: `boolean`
 
-- Required: No
+-   Required: No
 
 Defines if the dialog is open (displayed) or closed (not rendered/displayed). It also implicitly toggles the controlled mode if set or the uncontrolled mode if it's not set.
 
 ### `onConfirm`: `( event: DialogInputEvent ) => void`
 
-- Required: Yes
+-   Required: Yes
 
 The callback that's called when the user confirms. A confirmation can happen when the `OK` button is clicked or when `Enter` is pressed.
 
 ### `onCancel`: `( event: DialogInputEvent ) => void`
 
-- Required: Only if `isOpen` is not set
+-   Required: Only if `isOpen` is not set
 
 The callback that's called when the user cancels. A cancellation can happen when the `Cancel` button is clicked, when the `ESC` key is pressed, or when a click outside of the dialog focus is detected (i.e. in the overlay).
 
 It's not required if `isOpen` is not set (uncontrolled mode), as the component will take care of closing itself, but you can still pass a callback if something must be done upon cancelling (the component will still close itself in this case).
 
 If `isOpen` is set (controlled mode), then it's required, and you need to set the state that defines `isOpen` to `false` as part of this callback if you want the dialog to close when the user cancels.
+
+### `confirmButtonText`: `string`
+
+-   Required: No
+-   Default: OK
+
+The optional custom text to display as the confirmation button's label
+
+### `cancelButtonText`: `string`
+
+-   Required: No
+-   Default: Cancel
```

thanks!

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +9/-5)
- packages/components/src/confirm-dialog/README.md (MODIFIED, +56/-45)
- packages/components/src/confirm-dialog/component.tsx (MODIFIED, +4/-2)
- packages/components/src/confirm-dialog/stories/index.js (MODIFIED, +19/-0)
- packages/components/src/confirm-dialog/test/index.js (MODIFIED, +28/-0)
- packages/components/src/confirm-dialog/types.ts (MODIFIED, +2/-0)
