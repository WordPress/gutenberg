---
number: 58604
type: pull_request
state: MERGED
merged_at: 2024-02-02T13:10:04Z
url: https://github.com/WordPress/gutenberg/pull/58604
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #58604: Components: Fix the Snackbar auto-dismissal timers

- URL: https://github.com/WordPress/gutenberg/pull/58604
- Author: Mamaduka
- Created: 2024-02-02T09:12:00Z
- Updated: 2024-02-02T13:10:29Z
- Comments: 2 of 2
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/58604.diff

## Body

## What?
Resolves #58594.

PR removes `onDismiss/onRemove` callbacks from side-effect dependencies to avoid resetting timers on re-render.

## Why?
See #58594.

## Testing Instructions
1. Open a post or page.
2. Insert a block.
3. Trigger a few snackbar notices. Copying a block can do that.

### Expected behavior

_Let's assume there is a one-second difference between message triggers._

```
Message 1 - 10s
Message 2 - 10s + 1s
Message 3 - 10s + 2s
```

## Screenshots or screencast <!-- if applicable -->

## Issue comments

### github-actions on 2024-02-02T09:12:18Z

URL: https://github.com/WordPress/gutenberg/pull/58604#issuecomment-1923381966

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Core SVN

If you're a Core Committer, use this list when committing to `wordpress-develop` in SVN:
```
Props: mamaduka, mciampini.
```

## GitHub Merge commits

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### Mamaduka on 2024-02-02T13:09:31Z

URL: https://github.com/WordPress/gutenberg/pull/58604#issuecomment-1923770431

Thanks for testing and review, @ciampo!

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2024-02-02T10:35:46Z

URL: https://github.com/WordPress/gutenberg/pull/58604#discussion_r1475872540
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -98,7 +98,10 @@ function UnforwardedSnackbar(
 		}, NOTICE_TIMEOUT );
 
 		return () => clearTimeout( timeoutHandle );
-	}, [ onDismiss, onRemove, explicitDismiss ] );
+		// The `onDismiss/onRemove` can have unstable references,
+		// trigger side-effect cleanup, and reset timers.
+		// eslint-disable-next-line react-hooks/exhaustive-deps
```

Disabling exhaustive deps checks is risky because it will prevent ESlint from catching potential bugs for this hook in the future. Could we instead use refs for `onDismiss` and `onRemove` ? Something like:

```js
const onRemoveRef = useRef( onRemove );
useEffect( () => {
  onRemoveRef.current = onRemove;
, [ onRemove ] );

const onDismissRef = useRef( onDismiss );
useEffect( () => {
  onDismissRef.current = onDismiss;
, [ onDismiss ] );

// ..useEffect handling the auto-dismissal timers
```

#### Mamaduka on 2024-02-02T10:37:26Z

URL: https://github.com/WordPress/gutenberg/pull/58604#discussion_r1475874450
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -98,7 +98,10 @@ function UnforwardedSnackbar(
 		}, NOTICE_TIMEOUT );
 
 		return () => clearTimeout( timeoutHandle );
-	}, [ onDismiss, onRemove, explicitDismiss ] );
+		// The `onDismiss/onRemove` can have unstable references,
+		// trigger side-effect cleanup, and reset timers.
+		// eslint-disable-next-line react-hooks/exhaustive-deps
```

I was just thinking about using the same pattern 😅 I will push updates shortly.

#### Mamaduka on 2024-02-02T12:13:32Z

URL: https://github.com/WordPress/gutenberg/pull/58604#discussion_r1475985049
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -98,7 +98,10 @@ function UnforwardedSnackbar(
 		}, NOTICE_TIMEOUT );
 
 		return () => clearTimeout( timeoutHandle );
-	}, [ onDismiss, onRemove, explicitDismiss ] );
+		// The `onDismiss/onRemove` can have unstable references,
+		// trigger side-effect cleanup, and reset timers.
+		// eslint-disable-next-line react-hooks/exhaustive-deps
```

Updated in [6bacb67](https://github.com/WordPress/gutenberg/pull/58604/commits/6bacb67d1baa6f0a7aada90fd22d960e4369bb1a).

### Review thread 2

#### ciampo on 2024-02-02T12:32:26Z

URL: https://github.com/WordPress/gutenberg/pull/58604#discussion_r1476001957
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -88,17 +94,24 @@ function UnforwardedSnackbar(
 
 	useSpokenMessage( spokenMessage, politeness );
 
-	// Only set up the timeout dismiss if we're not explicitly dismissing.
+	// The `onDismiss/onRemove` can have unstable references,
+	// trigger side-effect cleanup, and reset timers.
+	const callbackRefs = useRef( { onDismiss, onRemove } );
+	useLayoutEffect( () => {
```

What's the reason for `useLayoutEffect` instead of `useEffect` ?  In theory `useEffect`s should be called in order, so we should be guaranteed that the `onDimiss` and `onRemove` refs are always up to date

#### Mamaduka on 2024-02-02T12:39:15Z

URL: https://github.com/WordPress/gutenberg/pull/58604#discussion_r1476008529
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -88,17 +94,24 @@ function UnforwardedSnackbar(
 
 	useSpokenMessage( spokenMessage, politeness );
 
-	// Only set up the timeout dismiss if we're not explicitly dismissing.
+	// The `onDismiss/onRemove` can have unstable references,
+	// trigger side-effect cleanup, and reset timers.
+	const callbackRefs = useRef( { onDismiss, onRemove } );
+	useLayoutEffect( () => {
```

Nothing specific. This is just a common pattern; we even have [a `useLatestRef` hook](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/utils/hooks/use-latest-ref.ts), but I don't use here because returned value isn't recognized as Ref and linter complains.

Related: https://epicreact.dev/the-latest-ref-pattern-in-react/

#### ciampo on 2024-02-02T13:04:07Z

URL: https://github.com/WordPress/gutenberg/pull/58604#discussion_r1476033865
Path: packages/components/src/snackbar/index.tsx

```diff
@@ -88,17 +94,24 @@ function UnforwardedSnackbar(
 
 	useSpokenMessage( spokenMessage, politeness );
 
-	// Only set up the timeout dismiss if we're not explicitly dismissing.
+	// The `onDismiss/onRemove` can have unstable references,
+	// trigger side-effect cleanup, and reset timers.
+	const callbackRefs = useRef( { onDismiss, onRemove } );
+	useLayoutEffect( () => {
```

Thank you for the link, super interesting read!

I took a look back at [this article](https://kentcdodds.com/blog/useeffect-vs-uselayouteffect) by Kent that I usually refer to when talking `useEffect` and `useLayoutEffect`, I noticed the ["special case"](https://kentcdodds.com/blog/useeffect-vs-uselayouteffect#one-special-case) section too!

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/snackbar/index.tsx (MODIFIED, +18/-5)
