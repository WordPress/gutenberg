---
number: 72336
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/72336
matched_labels: ["[Feature] Component System"]
all_labels: ["[Type] Enhancement","[Status] In Progress","[Feature] Component System","[Feature] DataViews"]
---

# Issue #72336: DataViewsPicker: Improve appearance when used in a `Modal` component

- URL: https://github.com/WordPress/gutenberg/issues/72336
- Author: talldan
- Created: 2025-10-15T03:20:28Z
- Updated: 2025-12-08T10:24:35Z
- Comments: 55 of 55

## Body

Related - [Try using DataViewsPicker in an updated media modal](https://github.com/WordPress/gutenberg/pull/71944#top)

## What problem does this address?
One of the main use cases for DataViewsPicker will be using it within a modal, so it's important for it to look and behave well in a Modal.

At the moment there are some styling issues that could be resolved and it'd be good to add a story demonstrating the picker in a modal:

### DataViews sticky footer doesn't work in a modal
This root cause of this is reported here - [Modal: When 'fill' size is used it's difficult to make content take up the full height of the modal](https://github.com/WordPress/gutenberg/issues/70611)

Modals have an inner `div` that wraps content, and this doesn't work well with the sticky footer, I think because it has its own overflow and doesn't take up the full height of the modal.

### DataViewsPicker has extra padding when used in a Modal
All of the main components that make up DataViews/DataViewsPicker have extra padding around them, when placing in the Modal which also has its own padding around content, it results in a lot of empty space.

In the following screenshot you can see it causes the modal's header to be misaligned with the content:

<img width="500" alt="Image" src="https://github.com/user-attachments/assets/11b83e41-f2d5-46f0-aa65-5b1a3cbb62c8" />

It should perhaps instead look more like this (padding removed from DataViewsPicker's components):

<img width="500" alt="Image" src="https://github.com/user-attachments/assets/f607105a-30d5-4e98-9320-d8409d85cba1" />

Perhaps DataViewsPicker can adopt similar behavior to how DataViews works in a `Card` (https://wordpress.github.io/gutenberg/?path=/story/dataviews-dataviews--with-card) and automatically reduce its own padding. Or it doesn't have the padding to begin with?

## Issue comments

### jameskoster on 2025-10-23T09:01:38Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3435843856

> Or it doesn't have the padding to begin with?

In a vacuum I think this makes the most sense. Then DataViews just inherits padding from its container, nice and easy.

However, I think that would make it impossible (without some hacks) to achieve the current table/list layout designs where the row background and stroke is 'full width'. In other words, I don't think this is ideal:

<img width="1630" height="382" alt="Image" src="https://github.com/user-attachments/assets/46ac4264-6cf9-4e07-8391-2c06c7c4f532" />

Compared to trunk:

<img width="1625" height="391" alt="Image" src="https://github.com/user-attachments/assets/98ea7c89-80fe-44df-9d1e-ea8134ef4b97" />

cc @WordPress/gutenberg-design for thoughts on that though. Maybe it's not a huge deal and can be tweaked a bit to work?

---

Another option would be to allow container components to customise padding, ideally in a way that references semantic spacing tokens. So in the above case we'd do something like:

```
<PageBody paddingX={ 0 }>
	<DataViews paddingX={ spacing.page } />
</PageBody>
```

And for the media picker something like:

```
<ModalBody paddingX={ 0 }>
	<DataViewsPicker paddingX={ spacing.modal.full } />
</ModalBody>
```

And for DataViews in Cards:

```
<CardBody paddingX={ 0 }>
	<DataViews paddingX={ spacing.card.medium } />
</CardBody>
```

But obviously this would be a larger undertaking 😅 It also unclear if this level of customisation is something we'd definitely want to offer.

I'd welcome the thoughts of @oandregal @mirka @aduth on this issue.

### jasmussen on 2025-10-23T09:37:38Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3435989127

Can be solved visually in a few ways, including _removing_ the hover effect entirely (😱), or by letting that hover effect extend the padding-width beyond its container left and right (a la Google Docs, and would require visible overflow). 

I think I'd optimize for reusability. If we need the edge to edge effect, which I think is primarily for table and list layouts, then probably the entire _modal_ with its padding rules should be part of the picker. Otherwise I'd go for cleaner separation of concerns. If that makes sense.

### fcoveram on 2025-10-23T10:02:44Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3436104304

> …including removing the hover effect entirely (😱), or by letting that hover effect extend the padding-width beyond its container left and right (a la Google Docs, and would require visible overflow).

-1 😅 I see this impacting the component negatively. Unless I'm understanding the implementation wrongly.

I second the effort of reaching the current layout designs. So if we can reach the spacing approach shown in the screenshot attached in the initial comment and include the other layouts in follow-up tickets, that would be great.

### jameskoster on 2025-10-23T10:58:17Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3436310741

Maybe the hover effect could be achieved using an absolutely positioned pseudo element? That way the cell contents remain aligned, but the hover style doesn't need to span the full width, and DataViews can inherit padding from its parent container. Something like this:

<img width="1126" height="417" alt="Image" src="https://github.com/user-attachments/assets/c990e903-d40b-4609-b1be-c67a4dcb8c05" />

### jasmussen on 2025-10-23T11:54:10Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3436533530

Perhaps a wee bit rounded, but yes that is what I imagined with my comment around negative offset paddings.

### jameskoster on 2025-10-23T14:42:15Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3437409511

This could simplify the responsive styling of DataViews too, passing the responsibility on to the Page component (arguably where it belongs).

### jameskoster on 2025-10-27T14:01:12Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3451445113

I'd welcome @oandregal's thoughts on the idea of removing layout padding at the DataViews layer. If we can find a design that works (which seems possible based on the discussion above) I think this would be a nice enhancement.

### oandregal on 2025-10-29T07:19:53Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3460153502

👍 It seems easier to handle it at the container level, and I can't think of a reason that'd be an issue. It'd also allow us to remove [these styles for card](https://github.com/WordPress/gutenberg/blob/b1b6faef2bac866cb91500681d2cbef8e962b09c/packages/dataviews/src/components/dataviews/style.scss#L112).

### jameskoster on 2025-10-29T11:49:15Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3461115029

<img width="2350" height="895" alt="Image" src="https://github.com/user-attachments/assets/1d48fdc8-d474-4601-9fab-13440a2b7e3c" />

Here's a quick mockup to demonstrate how DataViews Table and List would appear in containers with different padding values. All horizontal padding on _DataViews_ is removed. The selected state would be a separate element that is 100% + 32px wide, and offset by 16px.

### p-jackson on 2025-10-30T06:06:47Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3466253220

> In a vacuum I think this makes the most sense. Then DataViews just inherits padding from its container, nice and easy.

I'm a fan of the "parents decide padding" approach to styling. Ideally it would be a wholistic DS-wide strategy. I think props like `__nextHasNoMarginBottom` (basically saying the parent is responsible for the spacing) suggest that perhaps we're already moving in that direction?

The wrinkle with that approach for dataviews is that we'd like the row borders to extend out to the edge of the card, which requires the dataview (not the container) to be the one applying the actual CSS.

> allow container components to customise padding

This kinda feels unorthodox initially, but I think once you decide on the "parents decide padding" strategy, then this actually feels natural: the nature of these row borders means the padding CSS needs to be inside the dataview, but we've decided parents should decide the padding, therefore it is a prop.

### andrewserong on 2025-11-03T04:01:34Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3478827272

>  it'd be good to add a story demonstrating the picker in a modal

I've opened a small PR to add a storybook story, hopefully it'll help a little with debugging / testing some of the issues raised here (and potential solutions): https://github.com/WordPress/gutenberg/pull/72913

### andrewserong on 2025-11-05T04:46:58Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3489302679

Just double-checking what the consensus or general feeling is here as to a good solution for this. I see a few references to the container deciding what the padding should be, along with the idea of a prop. I also see @StevenDufresne has a couple of WIP PRs in the works to try things out, too.

I find the idea of a `paddingX` prop pretty compelling, so have tried out a WIP PR for that over in https://github.com/WordPress/gutenberg/pull/72989 if anyone would like to take that for a spin. I'm not sure if it's the desired API, so very happy to close out that PR and go in a different direction if we have better ideas on how we might handle this.

I suppose part of the challenge is a) making the padding configurable while b) being backwards compatible. In #72989 I went with the idea of using a CSS variable if `paddingX` is set, and otherwise falling back to the existing padding rules.

### oandregal on 2025-11-05T13:41:36Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3491286800

> a) making the padding configurable while b) being backwards compatible

@andrewserong with the caveat that I wasn't able to look at all the things that are being experimented right now in this area, my understanding is that the way forward is:

1. remove padding from dataviews as Jay suggested
2. call it a breaking change, so consumers need to adapt their containers to the desire padding

Is there any case where that approach would not work?

### andrewserong on 2025-11-05T22:12:00Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3493769526

@oandregal I think removing the padding from dataviews altogether would mean we don't get the full edge-to-edge width of the top border for the footer. DataViews is used in lots of places, too, so I'm pretty hesitant about the idea of introducing a breaking change for this.

To show what I mean, this is the footer extending to the horizontal edges, so as @p-jackson mentioned above, I think the padding probably needs to occur within DataViews / DataViewsPicker:

<img width="1262" height="323" alt="Image" src="https://github.com/user-attachments/assets/f1116f8e-ba18-4a26-8443-f5a94a393963" />

### tellthemachines on 2025-11-06T00:30:40Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3494256268

Fwiw regarding optional padding as a prop, we're already doing it in the [Page component](https://github.com/WordPress/gutenberg/blob/trunk/packages/admin-ui/src/page/index.tsx#L20). No opinion on whether this should be the solution 😄 but it would be good to have a consistent approach to this problem, whichever way we go.

### ramonjd on 2025-11-06T01:14:44Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3494365147

Sorry I'm just catching up. 

Is there a way to avoid making "removing padding from dataviews altogether" a dependency? 

Especially if it's a breaking change. So I mean, make the modal work right now, without creating blockers for removing padding later.

This might be completely dumb, but maybe extend the [existing pattern to set dataviews styles in cards](https://github.com/WordPress/gutenberg/blob/trunk/packages/dataviews/src/components/dataviews/style.scss#L110) to also handle Modal components via CSS `:has()` selector for common cases, and then add an optional `paddingX` prop
for custom cases, which would overwrite them via CSS var. 

Or is that what https://github.com/WordPress/gutenberg/pull/72989 proposes anyway? 😅

### andrewserong on 2025-11-06T01:28:22Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3494392958

> Or is that what https://github.com/WordPress/gutenberg/pull/72989 proposes anyway? 😅

This one's only proposing the `paddingX` prop at the moment, though adding rules for the padding to be aware of whether or not it's within a `Modal` is also a compelling idea. For now, that PR manually sets `0` padding on the modal content within the Storybook story.

I see @aduth also linked to this issue from https://github.com/WordPress/gutenberg/issues/72784, which proposes adding the Box component (PR in https://github.com/WordPress/gutenberg/pull/72984), but I'm not too sure what the implications for that component might be for DataViews?

I don't feel _too_ strongly about which direction we go in, except for ensuring backwards compatibility / we don't create unnecessary work for existing consumers. But I gotta admit I find the nuances in selecting an approach for how we handle padding here quite challenging!

So far we've got:

* https://github.com/WordPress/gutenberg/pull/72912 (set a CSS variable for the padding, inherited based on context — in this case whether the DataViews is rendered within a card)
* https://github.com/WordPress/gutenberg/pull/72989 (set a CSS variable based on a `paddingX` prop on DataViews / DataViewsPicker, the consumer has to zero out their padding on the modal. This could be expanded / adjusted to be a `size` prop if we want consistency with https://github.com/WordPress/gutenberg/pull/72511)
* The idea of removing padding altogether from DataViews/DataViewsPicker — this has the downside of backwards compatibility issues and limits the ability to do things like having the footer top border extend full-width.
* See what customisation the new `Box` component affords, and whether that can be used within DataViews, or if we can borrow a similar approach.

For now, I might pick up other unrelated tasks while we discuss options here. I'd love to help come up with a clear path forward, so very open to any ideas at this stage on how to progress this.

### StevenDufresne on 2025-11-06T01:36:13Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3494410252

I think the first step, mentioned by @jameskoster elsewhere (to which I agree), would be to replace DataView hard-coded padding with CSS variables, maintain the fallbacks, and let the consumer figure out how they want to modify those variables for now. 

Looking at how the design system is evolving, CSS variables will be at the core. Exactly how they are applied is step two, a problem that we can solve when things are more settled.

### oandregal on 2025-11-06T11:19:12Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3496671459

> would be to replace DataView hard-coded padding with CSS variables, maintain the fallbacks, and let the consumer figure out how they want to modify those variables for now.

This means the CSS Custom Properties become public API for the component.

I haven't caught up with all the work for the design system, and perhaps that will be the way forward when that's settled. Right now, my concern is that we'd be committing to use something that's still uncertain. For example, unlike a button or a box, DataViews/DataForm has a lot of UI surface to cover, and it's still evolving. That means that there's still no clarity of which "style hook points" we should provide to style with CSS Custom Properties. We'd be increasing the public API without a clear sense of how to handle it, impact, and so on.

I'm also not a big fan of creating style top-level style properties for DataViews/DataForm. I'd rather do what [Ramon suggested](https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3494365147) if that helps unblock the picker work, and then look at the _right way_ to do this when the design system work is more stablished.

Said this, I'd welcome thoughts from the components folks.

### jameskoster on 2025-11-06T14:04:32Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3497416933

I think the picker work is just one example of a common problem so it would be better to solve this systematically if possible.
 
Stepping back, I'm not sure there's a compelling argument for DataViews to handle it's own padding. The main point seems to be about ensuring the dividers between table rows are 'full width', but I'm not convinced it's worth adding a bunch of complexity just to support that. We mocked up some designs above to work around this and I don't see it as a down grade to be honest. Perhaps there's another argument I missed?

I not then I still lean towards removing DataViews padding entirely and adjusting the styling a bit to suit. I appreciate this would be a visual breaking change, but it is a very easy one to fix. It also leaves the door open to a more advanced padding solution later _if it's required_.

### aduth on 2025-11-06T20:01:27Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3499182839

> I see [@aduth](https://github.com/aduth) also linked to this issue from [#72784](https://github.com/WordPress/gutenberg/issues/72784), which proposes adding the Box component (PR in [#72984](https://github.com/WordPress/gutenberg/pull/72984)), but I'm not too sure what the implications for that component might be for DataViews?

More of a "future" solution, but the idea I had in mind was related to configurable theme density affecting default box paddings, where if DataViews were to implement itself within a Box, its paddings would be based on the inherited (`ThemeProvider`-provided) density configuration, providing a way to control the paddings externally (i.e. modals altering or unsetting the padding by rendering a `ThemeProvider` with the desired density).

### andrewserong on 2025-11-06T22:18:22Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3499597328

> I not then I still lean towards removing DataViews padding entirely and adjusting the styling a bit to suit. I appreciate this would be a visual breaking change, but it is a very easy one to fix. It also leaves the door open to a more advanced padding solution later if it's required.

Thanks for clarifying, folks! It's highly possible I've been overthinking this — for me it was more the caution of not quite knowing the implications of making a visually breaking change. But if we don't mind losing full-width dividers, I agree, I think this will simplify things quite a bit.

Very happy to give this a try. If not today, I'll take a look at trying out removing the padding next week, if no-one beat me to it!

### jameskoster on 2025-11-07T09:59:02Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3501601438

@andrewserong I'm happy to assist by finessing the design, feel free to ping me on any PR's.

### ramonjd on 2025-11-09T22:24:56Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3508893337

Also see this issue for design reference:

- https://github.com/WordPress/gutenberg/issues/73085

### andrewserong on 2025-11-10T05:08:01Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3509419005

Hi folks, I've put up a draft PR that looks at removing outer padding from DataViews and its layouts over in #73127. While it's definitely the simplest of the approaches from a technical standpoint, I'm not sure I'm sold on the overall idea. I've listed out the bugs and issues with the approach so far on the issue, and included some screenshots.

In addition to the issue of items not stretching all the way horizontally for the border-top, there's now the problem that scrollbars sit flush against items within the DataViews, default padding in things like Modal don't always sit well with the internal padding of DataViews (look at the footer), and outline styles on the filter / config buttons gets cut off, as there's now no longer padding to give the buttons clearance.

Very happy for feedback on the PR if it looks like any of these issues have simple fixes, or if there's other things we should try changing. Overall it doesn't feel as polished a UI to me as making padding _within_ DataViews configurable. But I do agree that it if we can get it to work, it makes the overall API of DataViews simpler than adding in more configuration.

All that said, I like to try to practice having weakly held opinions, so while I'm of the opinion that configurable padding would result in a more polished UI, I'm content to continue on with #73127 if anyone would like to help out with seeing how we can address or mitigate the issues we uncover with it. Thanks!

### jameskoster on 2025-11-10T10:37:31Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3510763449

Thanks for putting that PR together. The visual design details seem fixable, but I completely agree that the scrollbar issue is problematic.

This might be a bit of a tangent, but it’s related: I wonder whether DataViews should hand off scrolling responsibility to the container as well, and simply define which elements are sticky. When I remove `overflow: auto` on `.dataviews-wrapper` in Storybook, everything still appears to work correctly, but when I do the same in the Site Editor it breaks. That breakage could (and maybe should) be addressed within the `Page` component family. In fact... I notice the `.admin-ui-page__content` container _already_ handles scrolling, but DataViews in the Site Editor don’t seem to make use of it 🤔

In any case, it does feel like we should align on the higher-level question before committing to a direction: do we actually want configurable DataViews padding? My current (also weakly held) view remains that it'd be simpler not to. That's based on previous feedback that allowing configuration risks inconsistency, and that DataViews would be more adaptive out of the box if it managed fewer layout details.

I'd welcome @youknowriad's thoughts on this as I think he's been looking at broader admin page composition recently.

### youknowriad on 2025-11-10T12:31:26Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3511350428

For me whatever solution that prevents users from writing custom CSS is the right one. So if we agree on which designs should be doable with DataViews, we can decide whether it needs a size/padding prop or not.

### jameskoster on 2025-11-10T14:11:18Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3511910108

Both solutions accomplish that haha. 

Making padding configurable means consumers can fine tune to suit whatever need they might have. The down side is that this flexibility opens the door to many 'inconsistent' layouts. It also creates an extra initial setup burden as consumers have to manually match the DataViews padding to the container padding. The complexity compounds when handling responsive behavior and density preferences. 

By _not_ making padding configurable DataViews layout will 'just work' in any context. The trade-off with this approach is less flexibility on the design. The main thing we lose in that respect is 'full width' horizontal rules between table rows, but based on the discussion in this issue it's not clear that is a deal breaker.

### youknowriad on 2025-11-10T14:28:18Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3512023059

So it's a design decision. Do we want borders that expand to the full container in cards, frame... or not basically.

### jameskoster on 2025-11-11T09:59:33Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3515966612

For _me_ that's not a deal breaker, the design can be made to work either way, so I'll defer to y'all on the implementation.

### p-jackson on 2025-11-13T04:27:15Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3525281415

The mockups [shared above](https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3461115029) show how the borders don't have to extend beyond the bounds of the dataview. And I agree it looks fine. But—unless I'm interpreting them wrong—it seems like the hover/selected state still extends beyond the bounds of the dataview?

<img width="586" alt="Image" src="https://github.com/user-attachments/assets/2afcaa06-79a5-4ebd-9b00-3ce8107e0d2c" />

It says the card has a 16px padding, so I'm not sure how to interpret the mockup.

> the design can be made to work either way

@jameskoster I'm wondering if you have a different design for the selection/hover state in mind that doesn't rely on background colour?

### youknowriad on 2025-11-13T09:22:32Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3526697955

Good point @p-jackson We can't extend the hover styles outside the dataviews unless we use some negative margins or something but that just contradicts the whole purpose of removing the padding (it's the same issue in another form basically)

### jameskoster on 2025-11-13T11:55:20Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3527464637

The idea was to use a pseudo element for the background that partially extends outside the DataViews boundary by a _fixed_ amount. It's slightly different compared to the horizontal rules because the positional offset wouldn't need to be a 1:1 match with the container padding to behave as expected. I appreciate why this might be a deciding factor in the approach though.

### youknowriad on 2025-11-13T11:59:04Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3527478686

@jameskoster I see, do you think all use-cases will be satisfied with that "fixed amount" or are they going to resort to CSS overriding for this?

### jameskoster on 2025-11-13T14:52:46Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3528191570

It's hard to say for certain as it's ultimately a bit subjective. The mockups above look okay to me. The '8px padding card' version is a bit clunky, but that down to the general table spacing, not the selected row styling specifically.

### youknowriad on 2025-11-13T14:56:49Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3528205951

To clarify I think technically speaking, removing the padding is clearly a good option to make the dataviews more portable.

What I'm a bit worried about is the potential for a revert because we're not sure whether it accommodates all use-cases or not.

### jameskoster on 2025-11-13T15:30:40Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3528363240

Yeah, it's difficult to be 100% confident about that as it's a somewhat opinionated design change. There's potential for small visual breakage, and I'm not sure what the backwards compatibility policy is for things like that.

Maybe it's safer to make the padding configurable after all. I suppose the introduction of semantic spacing tokens should help minimise the scope for inconsistency. However, one other observation; even if DataViews padding is configurable, a table layout DataPicker in a modal is still going to look broken unless we also allow negative margins, or make the modal padding configurable as well.

### p-jackson on 2025-11-14T02:48:16Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3530589917

> The idea was to use a pseudo element for the background that partially extends outside the DataViews boundary by a fixed amount.

Ah I see. Personally I worry it would end up looking like or getting reported as a bug. I can certainly imagine it being pointed out in a design review, especially if the fixed amount is _just_ shy of the card padding.

> even if DataViews padding is configurable, a table layout DataPicker in a modal is still going to look broken unless we also allow negative margins, or **make the modal padding configurable as well**

Yes it is turtles all the way down 😅 I'm particularly interested in the `Card` case, and there we basically have fixed the issue by effectively configuring the card padding. So the same would go for the modal I guess.

### andrewserong on 2025-11-14T05:01:01Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3530842463

> So the same would go for the modal I guess.

In that case, we'd do something similar to [the existing card rules](https://github.com/WordPress/gutenberg/blob/b1b6faef2bac866cb91500681d2cbef8e962b09c/packages/dataviews/src/components/dataviews/style.scss#L112) and add a set of `.components-modal__content:has(> .dataviews-wrapper)` rules that zero out the modal content padding, etc? That's probably the quickest option for dealing with the modal use case, and would side-step the larger discussions about what to do for padding in general 🤔

I've been a bit side-tracked this week as I've been a bit more focused on contentOnly features, but would love to continue to help come up with a fix here.

### jameskoster on 2025-11-14T15:30:51Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3533304671

> So the same would go for the modal I guess.

Making DataViews padding configurable is one thing... making padding configurable for modal (and all other containers DataViews could conceivably appear in) seems like a slippery slope to me 😬

I suppose @andrewserong's approach is an alternative, but do you think it's sustainable to add this kind of rule to _all_ container components? I'm not even sure components should be 'aware' of each other in this way, it seems a bit unconventional from a DS perspective. Especially when we factor in container size options, density preferences, and so on. The logic could get quite complex.

> Ah I see. Personally I worry it would end up looking like or getting reported as a bug. I can certainly imagine it being pointed out in a design review, especially if the fixed amount is just shy of the card padding.

That's valid feedback for sure. Maybe it would work better if the offset was a bit less pronounced, like this?

<img width="867" height="800" alt="Image" src="https://github.com/user-attachments/assets/dd358373-8edc-4370-8566-38704641326c" />

I suppose another angle to explore on the design side is a different treatment for hovered and selected items. I don't love this but sharing in case it inspires other ideas:

<img width="975" height="668" alt="Image" src="https://github.com/user-attachments/assets/25ff56d5-8c09-4d62-b8d1-5a4a18ce2931" />

---

One other option to consider (sorry 😅)... it could be argued that the current DataViews padding is problematic because it's _so_ large. 48px is a lot. If we kept the padding, but reduced it to something more appropriate like 24px maybe it would work better in spite of the alignment details?

<img width="2530" height="1158" alt="Image" src="https://github.com/user-attachments/assets/7b574a32-9a6f-4981-8eeb-3e6563947a39" />

Finally—and I share this mostly to cauterize the idea—we could say DataViews tables are always 'contained', with sensible padding, a bit like Polaris [Index Tables](https://shopify.dev/docs/api/app-home/patterns/compositions/index-table).

<img width="3452" height="1158" alt="Image" src="https://github.com/user-attachments/assets/c532c964-f435-4875-a27b-6564822db941" />

### p-jackson on 2025-11-17T02:57:58Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3539755587

> Maybe it would work better if the offset was a bit less pronounced, like this?

Yes I think that looks good for 32px, but I think the issue is definitely more to do with small screens. Would it still look like "not a bug" when the padding is 16px? I recognise I'm raising a hypothetical point about someone else's subjective reaction during a design review though. I don't think I'm making the strongest argument against negative margins here 🙂 

---

I like these ideas of "just reduce padding across the board" or "just wrap the dataview in a border" though! If we can live with the design, they would be a lot simpler.

### andrewserong on 2025-11-17T03:08:43Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3539773888

> I like these ideas of "just reduce padding across the board" or "just wrap the dataview in a border" though! If we can live with the design, they would be a lot simpler.

> If we kept the padding, but reduced it to something more appropriate like 24px maybe it would work better in spite of the alignment details?

Yeah, I lean a little toward seeing if we can "just reduce padding across the board". If we're consistent with it, i.e. using the desired `24px` currently used when DataViews is a child of the Card component, then the "rules" we might need for the modal case could be much simpler. I.e. when a direct child of a modal, just zero out the padding on the modal itself. While still not ideal, at least it would be a single rule, which feels more sustainable to me than adding a whole bunch of conditions 🤔

I know this has been a long conversation for a seemingly simple topic, but I'm glad we're having it! Better to tease these ideas out now than rush ahead with configurability and end up with an API that's more complex and harder to unwind.

### StevenDufresne on 2025-11-17T05:08:01Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3540005964

What information do we need to make a final decision here? What are the next steps?

### andrewserong on 2025-11-17T05:16:51Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3540022828

> What information do we need to make a final decision here? What are the next steps?

Good question! I'm about to throw up a PR to try out padding at `24px`. My gut feeling is that until we have a PR that we can all rally behind, it'll be challenging to commit to a particular approach. I'll add another comment here when it's up.

### andrewserong on 2025-11-17T05:23:09Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3540035097

Alrighty, I've given the `24px` padding standardisation idea a shot, and have put up a PR over in https://github.com/WordPress/gutenberg/pull/73334 — I think something in this direction is likely a decent pragmatic bet for now as it simplifies the existing CSS without creating a new API, and results in less breakage for existing usages than removing the padding entirely.

I've added most folks here as reviewers in case we can go with that direction, but as always, happy to close out the PR if there are better ideas for what to try next.

### jameskoster on 2025-11-17T14:01:37Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3541978718

> Would it still look like "not a bug" when the padding is 16px?

I think it's okay:

<img width="945" height="429" alt="Image" src="https://github.com/user-attachments/assets/5476bd0e-cc58-4cb9-b643-de97f1e392a3" />

I don't know if we need to optimise beyond that, 24px seems like a good default for Cards in general.

### andrewserong on 2025-11-21T04:57:57Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3561394588

Alrighty, I've closed out a few of my experiments as the discussion appears to have consolidated toward the idea of customisable padding on the DataViews or DataViewsPicker component, using a controlled vocabulary of token names (for consistency with the Card component).

As always, very happy for feedback and ideas. The PR I'm working from is https://github.com/WordPress/gutenberg/pull/72989 and it should be ready for review now.

I'll be working on some other things next week, so I may be a little slow in implementing review feedback, but I'm keen to help solve this problem. Thanks again for all the collaboration here, everyone!

### jasmussen on 2025-11-21T12:07:37Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3562737593

The conversation on the PR and here seems on track with some generally good instincts. 

I suspect based on the conversation that this is already being thought of, but combining negative margins with paddings seems to be a sticking point that could potentially allow edge-to-edge-borders while the other content still aligning with modal titles, is that right? And does that account for cases where perhaps the modal padding change between breakpoints, i.e. does negative margins account for that somehow?

### andrewserong on 2025-11-23T23:10:55Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3568443745

Update: I'd love to hear from folks as to how everyone's feeling about the different proposed options. While there's been some positive feedback on the idea of customising via a `padding` prop, there's also a concern raised about the added complexity. This leans me back toward considering standardising on `24px` for padding for DataViews as it simplifies the CSS a bit without adding an additional API.

Keen to hear everyone's thoughts before I go further on the customisable padding PR. I'd prefer (if we can) to avoid adding extra complexity or APIs if we don't need to. These are the main PRs I'm proposing:

* https://github.com/WordPress/gutenberg/pull/72989 (customisable padding)
* https://github.com/WordPress/gutenberg/pull/73334 (standardise on `24px` and on balance, reduce the CSS included in DataViews) — this is the one I'm leaning towards

### StevenDufresne on 2025-11-23T23:26:11Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3568456873

> Update: I'd love to hear from folks as to how everyone's feeling about the different proposed options. While there's been some positive feedback on the idea of customising via a `padding` prop, there's also a concern raised about the added complexity. This leans me back toward considering standardising on `24px` for padding for DataViews as it simplifies the CSS a bit without adding an additional API.

I don't want to unnecessarily nudge us back into philosophical talks, but are we simply trying to solve DataView layout issues in modals? We've covered a lot of ground in these chats, hoping to solve the general rigidness with DataView spacing. Normalizing to `24px` itself does have ecosystem impacts. So while the code changes are minimal, the changes are noticeable, mostly everywhere.

Using `48px` for padding is very opinionated, `24px` less so. That seems like a change we should make provided it still aligns within the overall spacing system used in `wordpress/components`. 

I don't want the overall padding conversation to end. On that front, I'm not certain if we're lacking data, or hoping time will force us into a decision. I admittedly have skin in the game, hoping for controls so I can provide a better mobile experience to my users.

### oandregal on 2025-11-24T19:28:16Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3572386237

One global aspect I'd like to consider is  _how_ do we enable consumers to style DataViews. No matter if it's padding like in this issue or background-color, like this [other one](https://github.com/WordPress/gutenberg/pull/73390/#issuecomment-3562165044). It wouldn't be a great experience if consumers use CSS Custom Properties for background, but a component property for padding.

Over at https://github.com/WordPress/gutenberg/pull/73390/#issuecomment-3562165044 Jay was looking at leveraging CSS Custom Properties for consumers to be able to change the background color. Given that's already what we do in existing components, and how we expect the Design System to work, that approach has familiarity. I haven't caught up on all the conversations here and in the PRs (will do over the following days), but I wonder if we can consolidate everything around that.

### andrewserong on 2025-11-24T23:44:38Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3573179303

> It wouldn't be a great experience if consumers use CSS Custom Properties for background, but a component property for padding.

Indeed. Thanks for taking a look here, I'd love to see how we can simplify (and/or) consolidate where possible, rather than adding too much complexity. I feel like we kind of need: a) a better and simpler padding default so that folks don't need to adjust padding very often, and b) provide a simple method to adjust that padding when needed.

The more that I think about the customisable padding PR in [72989](https://github.com/WordPress/gutenberg/pull/72989), I feel like it's probably overkill — we likely don't want folks to change the padding all that often. So perhaps something like #73334 (standardise on `24px`), plus using CSS variables to allow overrides (potentially in a follow-up PR) might make for a simpler path forward 🤔

### ramonjd on 2025-11-25T00:01:36Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3573211603

> So perhaps something like https://github.com/WordPress/gutenberg/pull/73334 (standardise on 24px), plus using CSS variables to allow overrides (potentially in a follow-up PR) might make for a simpler path forward 🤔

This seems like a sound proposal to unblock the impasse: provide a baseline value and allow overrides via CSS vars.

### ntsekouras on 2025-11-25T18:02:05Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3576867255

Lot's of discussion in many places 😄 .  I'd echo @oandregal to have a single way of adjusting styles and it seems the CSS vars is a good approach for this. We might need more style adjustments in the future and adding specific props wouldn't scale well.

Said that, I believe `standardise on 24px` and using CSS vars could be in separate PRs. Ideally the default padding for these components should be zero and not opinionated at all, so I'm not sure how valuable a change like this (reducing to 24px) at this point, since all WP usages right now are based on the current padding (see page header paddings etc..). No strong opinions though for that.

### andrewserong on 2025-11-25T22:53:11Z

URL: https://github.com/WordPress/gutenberg/issues/72336#issuecomment-3577929094

> Ideally the default padding for these components should be zero and not opinionated at all

We covered some of this ground in https://github.com/WordPress/gutenberg/pull/73127 that tried removing padding altogether. It causes issues with the scrollbar (too close to content) and buttons on the outer edge of the DataViews where the focus styles get cut off. So there'd be more to figure out if we go down that path.

Standardising on _some_ padding seems the lowest barrier to clear to me, and it allows us to remove some of the container queries as `24px` is already a default in narrower containers. So I'm still leaning toward https://github.com/WordPress/gutenberg/pull/73334 as a first step. Let me know if folks think that's an okay path to pursue, and I can tidy it up.

