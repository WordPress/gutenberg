---
number: 54260
type: pull_request
state: MERGED
merged_at: 2023-09-12T17:29:15Z
url: https://github.com/WordPress/gutenberg/pull/54260
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Automated Testing","[Package] Components"]
---

# PR #54260: Button: Update test assertion to match test name

- URL: https://github.com/WordPress/gutenberg/pull/54260
- Author: brookewp
- Created: 2023-09-07T20:29:12Z
- Updated: 2023-09-12T17:29:38Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 1 of 1
- Diff: ../pr-diffs/54260.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

While working on #48222, I encountered a Button test where the assertion didn't match the test's intention based on the test name `should populate tooltip with description content for buttons with visible labels (buttons with children)'`. 

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

As mentioned in the test name, the test is meant to check if the tooltip has been populated with the description content, but the assertion checks the button is visible instead. This will pass, even if the tooltip isn't there or it is populated with the description content. Therefore, if we intend to test the tooltip, we should query for that instead. 

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

By changing the role from `button` to `tooltip`. There are no actions taken to show the tooltip, so we also need to add that it is hidden and change the assertion to find by text content instead. I believe the description comes from the `aria-label` rather than `aria-describedby`, so we can't check for an accessible description. 

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

Ensure tests pass: `npm run test:unit packages/components/src/button/index.tsx`

## Issue comments

### tyxla on 2023-09-12T10:36:19Z

URL: https://github.com/WordPress/gutenberg/pull/54260#issuecomment-1715473623

Restarted the performance tests, seems like a flaky run.

## Review threads

### Review thread 1

#### tyxla on 2023-09-08T09:22:04Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1319600649
Path: packages/components/src/button/test/index.tsx

```diff
@@ -254,10 +254,10 @@ describe( 'Button', () => {
 			);
 
 			expect(
-				screen.getByRole( 'button', {
-					description: 'Description text',
+				screen.getByRole( 'tooltip', {
+					hidden: true,
 				} )
-			).toBeVisible();
+			).toHaveTextContent( 'Description text' );
```

I'd prefer to keep the existing assertion for the button, since it tests the existence of the actual button described by  the visually hidden description text. Then I'd just add the assertion you created here for the tooltip, since that tests the hidden tooltip part. 

In other words, this tests two different parts of what's rendered and to me it makes sense to test them both for a better test coverage.

#### brookewp on 2023-09-08T17:55:22Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1320178284
Path: packages/components/src/button/test/index.tsx

```diff
@@ -254,10 +254,10 @@ describe( 'Button', () => {
 			);
 
 			expect(
-				screen.getByRole( 'button', {
-					description: 'Description text',
+				screen.getByRole( 'tooltip', {
+					hidden: true,
 				} )
-			).toBeVisible();
+			).toHaveTextContent( 'Description text' );
```

Fair point!

#### ciampo on 2023-09-09T12:44:41Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1320557625
Path: packages/components/src/button/test/index.tsx

```diff
@@ -254,10 +254,10 @@ describe( 'Button', () => {
 			);
 
 			expect(
-				screen.getByRole( 'button', {
-					description: 'Description text',
+				screen.getByRole( 'tooltip', {
+					hidden: true,
 				} )
-			).toBeVisible();
+			).toHaveTextContent( 'Description text' );
```

Just flagging that, after #54312 will get merged, the tooltip won't be found in the DOM until visible (ie. triggered with hover/focus)

### Review thread 2

#### tyxla on 2023-09-11T09:43:39Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1321281109
Path: packages/components/src/button/test/index.tsx

```diff
@@ -258,6 +258,11 @@ describe( 'Button', () => {
 					description: 'Description text',
 				} )
 			).toBeVisible();
+			expect(
+				screen.getByRole( 'tooltip', {
+					hidden: true,
+				} )
+			).toHaveTextContent( 'Description text' );
```

> Just flagging that, when https://github.com/WordPress/gutenberg/pull/54312 merged, the tooltip won't be found in the DOM until visible (ie. triggered with hover/focus)

Good point @ciampo! I guess then we'll need to trigger a hover, and will need to add a `waitFor()` so the assertion is called again when the tooltip appears. Something like:

```suggestion
			const user = userEvent.setup();
			await user.hover(
				screen.getByRole( 'button', {
					description: 'Description text',
				} )
			);

			await waitFor( () => {
				expect(
					screen.getByRole( 'tooltip', {
						hidden: true,
					} )
				).toHaveTextContent( 'Description text' );
			} );
```

#### brookewp on 2023-09-12T03:16:53Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1322309439
Path: packages/components/src/button/test/index.tsx

```diff
@@ -258,6 +258,11 @@ describe( 'Button', () => {
 					description: 'Description text',
 				} )
 			).toBeVisible();
+			expect(
+				screen.getByRole( 'tooltip', {
+					hidden: true,
+				} )
+			).toHaveTextContent( 'Description text' );
```

The other tooltip tests used `tab` so I followed that, which lets us get around using `waitFor()`:

https://github.com/WordPress/gutenberg/blob/ea90b505595a89fc4c522f388ffd7fbfbf922343/packages/components/src/button/test/index.tsx#L258-L272

But if `hover` is preferable, we could use `findByRole` instead: 

```tsx
const button = screen.getByRole( 'button', {
	description: 'Description text',
} );

expect( button ).toBeVisible();

await user.hover( button );

expect(
	await screen.findByRole( 'tooltip', {
		name: 'Description text',
	} )
).toBeVisible();
```

Not sure if [this info](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library#using-waitfor-to-wait-for-elements-that-can-be-queried-with-find) is still relevant, but it seems like `find*` is preferable over `waitFor()`.

#### ciampo on 2023-09-12T10:32:51Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1322842283
Path: packages/components/src/button/test/index.tsx

```diff
@@ -258,6 +258,11 @@ describe( 'Button', () => {
 					description: 'Description text',
 				} )
 			).toBeVisible();
+			expect(
+				screen.getByRole( 'tooltip', {
+					hidden: true,
+				} )
+			).toHaveTextContent( 'Description text' );
```

I'm personally ok with both `tab` and `hover`, and I also prefer using `findBy` instead of `waitFor`.

I guess we can keep the code as-is and wrap up the PR :)

#### tyxla on 2023-09-12T10:35:14Z

URL: https://github.com/WordPress/gutenberg/pull/54260#discussion_r1322844692
Path: packages/components/src/button/test/index.tsx

```diff
@@ -258,6 +258,11 @@ describe( 'Button', () => {
 					description: 'Description text',
 				} )
 			).toBeVisible();
+			expect(
+				screen.getByRole( 'tooltip', {
+					hidden: true,
+				} )
+			).toHaveTextContent( 'Description text' );
```

I just quickly drafted this as an example, obviously, you can use whatever makes sense the most. In any case you can totally go with `tab` and `findBy` if you wish, I think this is a minor stylistic choice that wouldn't affect the quality of the test anyway.

## Changed files

- packages/components/src/button/test/index.tsx (MODIFIED, +12/-0)
