---
number: 15482
type: pull_request
state: MERGED
merged_at: 2019-05-24T11:08:36Z
url: https://github.com/WordPress/gutenberg/pull/15482
matched_labels: ["[Package] Components"]
all_labels: ["Mobile App - i.e. Android or iOS","[Package] Components"]
---

# PR #15482: Port blockquote block to RN mobile

- URL: https://github.com/WordPress/gutenberg/pull/15482
- Author: SergioEstevao
- Created: 2019-05-07T12:48:14Z
- Updated: 2019-05-28T07:25:58Z
- Comments: 4 of 4
- Review threads: 9 of 9
- Changed files: 14 of 14
- Diff: ../pr-diffs/15482.diff

## Body

## Description

Implements the quote block in react native.

This is done with minimal changes on the quote block edit code. 
It implements a new Blockquote component that in the web continues to wrap the richtext using a blockquote element. On the native side it uses a View to implement the necessary styling and wrapping of the RichText component.

It also changes the RichText in native to a have a default tagName of `div` this allows the RichText component using inside the quote block to not to be changed and allow in native the proper wrapping of internal elements and final removal of the root tag.

~~This PR implement a specific native version of the edit of the block because of the following:~~

~~- Use of an HTML element, `<blockquote>` to set the style of the richtext~~
~~- Use of two richtext elements but not define the top element of each of them, so this need to be changed for mobile in order for their style to be presented correctly.~~


This can be tested using the following PR on GB-mobile:https://github.com/wordpress-mobile/gutenberg-mobile/pull/963

## Issue comments

### hypest on 2019-05-22T06:54:37Z

URL: https://github.com/WordPress/gutenberg/pull/15482#issuecomment-494676804

This seems to work nicely @SergioEstevao , nice one!

By the way, I think we might have an opportunity here to make this block cross-platform. I've opened https://github.com/WordPress/gutenberg/pull/15769 with some early experiments towards that. Have a look and we can chat about this!

### SergioEstevao on 2019-05-24T09:37:06Z

URL: https://github.com/WordPress/gutenberg/pull/15482#issuecomment-495544131

@koke do you want to give it another look, or can it be merged it in?

### gziolo on 2019-05-24T09:49:52Z

URL: https://github.com/WordPress/gutenberg/pull/15482#issuecomment-495549272

I see Travis detected some issue with style import:

> Error: File to import not found or unreadable: ./quote/style.scss.

Needs to be fixed to unblock merge.

### SergioEstevao on 2019-05-24T10:27:42Z

URL: https://github.com/WordPress/gutenberg/pull/15482#issuecomment-495563412

I move the style file to be in the BlockQuotation package. It looks something was using it too.

## Review threads

### Review thread 1

#### koke on 2019-05-22T12:53:23Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286473138
Path: packages/block-library/src/quote/blockquote.native.js

```diff
@@ -0,0 +1,19 @@
+/**
+ * External dependencies
+ */
+import { View } from 'react-native';
+/**
+ * Internal dependencies
+ */
+import styles from './style';
+
+export const Blockquote = ( props ) => {
+	return (
+		<View style={ { flex: 1, flexDirection: 'row', alignItems: 'stretch' } } >
```

I think this can be simpler, Views in react native seem to support border styles:

```jsx
<View style={ styles.wpBlockQuote } >
	{ props.children }
</View>
```

```css
.wpBlockQuote {
	border-left-width: 4px;
	border-left-color: $black;
	border-left-style: solid;
	padding-left: 8px;
	margin-left: 8px;
}
```

I'll let Thomas review the exact colors, but the web version seems to be using black. If you're seeing a blue line, that's coming from Twenty Nineteen

### Review thread 2

#### SergioEstevao on 2019-05-22T12:56:43Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286474758
Path: packages/block-editor/src/components/rich-text/index.native.js

```diff
@@ -809,6 +809,7 @@ export class RichText extends Component {
 RichText.defaultProps = {
 	formattingControls: [ 'bold', 'italic', 'link', 'strikethrough' ],
 	format: 'string',
+	tagName: 'div',
```

This is replicating the behaviour of the web where there the default tagName is a Div.

This allows the blockquote block to work properly because the <p> are surrounded by the div.

Our other blocks should work correctly because they define a tagName and don't use the default.

#### hypest on 2019-05-22T12:58:10Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286475417
Path: packages/block-editor/src/components/rich-text/index.native.js

```diff
@@ -809,6 +809,7 @@ export class RichText extends Component {
 RichText.defaultProps = {
 	formattingControls: [ 'bold', 'italic', 'link', 'strikethrough' ],
 	format: 'string',
+	tagName: 'div',
```

> are surrounded by the div

Hmm, how do the `div`s play a role in the native mobile case, considering that this change is in the `.native.js` file? 🤔

#### SergioEstevao on 2019-05-22T13:08:49Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286480227
Path: packages/block-editor/src/components/rich-text/index.native.js

```diff
@@ -809,6 +809,7 @@ export class RichText extends Component {
 RichText.defaultProps = {
 	formattingControls: [ 'bold', 'italic', 'link', 'strikethrough' ],
 	format: 'string',
+	tagName: 'div',
```

If you look at the file we expect a tagName in several places to be defined in order to be able to produce the HTML. It also allows the wrapping of the multiple <p> that can exist in a blockquote inside a root tag.

#### hypest on 2019-05-22T13:12:02Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286481650
Path: packages/block-editor/src/components/rich-text/index.native.js

```diff
@@ -809,6 +809,7 @@ export class RichText extends Component {
 RichText.defaultProps = {
 	formattingControls: [ 'bold', 'italic', 'link', 'strikethrough' ],
 	format: 'string',
+	tagName: 'div',
```

Oh, so it's for proper support of the `save` method of the block?

Let's add this expanded explanation to the PR description @SergioEstevao , cool?

### Review thread 3

#### koke on 2019-05-22T13:28:47Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286489573
Path: packages/viewport/src/with-viewport-match.native.js

```diff
@@ -0,0 +1,44 @@
+/**
+ * External dependencies
+ */
+import { mapValues } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { createHigherOrderComponent } from '@wordpress/compose';
+import { withSelect } from '@wordpress/data';
+
+/**
+ * Higher-order component creator, creating a new component which renders with
+ * the given prop names, where the value passed to the underlying component is
+ * the result of the query assigned as the object's value.
+ *
+ * @see isViewportMatch
+ *
+ * @param {Object} queries  Object of prop name to viewport query.
+ *
+ * @example
+ *
+ * ```jsx
+ * function MyComponent( { isMobile } ) {
+ * 	return (
+ * 		<div>Currently: { isMobile ? 'Mobile' : 'Not Mobile' }</div>
+ * 	);
+ * }
+ *
+ * MyComponent = withViewportMatch( { isMobile: '< small' } )( MyComponent );
+ * ```
+ *
+ * @return {Function} Higher-order component.
+ */
+const withViewportMatch = ( queries ) => createHigherOrderComponent(
+	withSelect( ( ) => {
+		return mapValues( queries, ( query ) => {
+			return query === 'mobile';
```

As mentioned on Slack, this isn't behaving like the web version. For now, let's make the AlignmentToolbar a fake component on native, and leave that and the viewport implementation for https://github.com/wordpress-mobile/gutenberg-mobile/issues/198

### Review thread 4

#### koke on 2019-05-22T13:47:44Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286498702
Path: packages/viewport/src/index.native.js

```diff
@@ -0,0 +1 @@
+export { default as withViewportMatch } from './with-viewport-match';
```

I don't think we need this one anymore right?

### Review thread 5

#### koke on 2019-05-22T13:54:26Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r286502153
Path: packages/block-library/src/quote/edit.js

```diff
@@ -3,6 +3,10 @@
  */
 import { __ } from '@wordpress/i18n';
 import { AlignmentToolbar, BlockControls, RichText } from '@wordpress/block-editor';
+/**
+ * Internal dependencies
+ */
+import { Blockquote } from './blockquote';
```

Following @gziolo's [comment on the other PR](https://github.com/WordPress/gutenberg/pull/15769#pullrequestreview-240492296), I'm not 100% sure about this naming.

I don't think this should be a standalone component, but having `Blockquote` inside of the `quote` block is a bit confusing to me. I think I'd suggest something along the lines of "Container" or "Wrapper" but I'd like to hear from @gziolo as well.

#### gziolo on 2019-05-24T08:32:30Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287264159
Path: packages/block-library/src/quote/edit.js

```diff
@@ -3,6 +3,10 @@
  */
 import { __ } from '@wordpress/i18n';
 import { AlignmentToolbar, BlockControls, RichText } from '@wordpress/block-editor';
+/**
+ * Internal dependencies
+ */
+import { Blockquote } from './blockquote';
```

I shared my thoughts already:

> Ideally, this new component would be renamed to `BlockQuotation` and placed inside `@wordpress/components` package (`packages/components/primitives/block-quotation.js`?) together with the corresponding documentation. See the prior art from @koke for `HorizontalRule` implemented in #14361.
> 
> Related MDN page which I used to reason about the name of the component: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/blockquote

#### gziolo on 2019-05-24T08:33:37Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287264555
Path: packages/block-library/src/quote/edit.js

```diff
@@ -3,6 +3,10 @@
  */
 import { __ } from '@wordpress/i18n';
 import { AlignmentToolbar, BlockControls, RichText } from '@wordpress/block-editor';
+/**
+ * Internal dependencies
+ */
+import { Blockquote } from './blockquote';
```

You will have to run into the same issue when porting Pullquote block. It is also something that plugin developers will need as well.

#### koke on 2019-05-27T10:05:40Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287730678
Path: packages/block-library/src/quote/edit.js

```diff
@@ -3,6 +3,10 @@
  */
 import { __ } from '@wordpress/i18n';
 import { AlignmentToolbar, BlockControls, RichText } from '@wordpress/block-editor';
+/**
+ * Internal dependencies
+ */
+import { Blockquote } from './blockquote';
```

What I meant is that, while this gets translated into a `blockquote` HTML element in the web version, I don't see it being a good standalone "quote" component right now, like the HorizontalRule is.

The current web implementation only looks like a quote because it's (IMO, wrongly) [stealing the styles for the quote block](https://github.com/WordPress/gutenberg/pull/15482/files#r287723792), which won't work when we want to do pullquote as well, and the native version is defining a non-configurable style as well.

Also, those styles are only applied to the `blockquote` because of the `wp-block-quote` class, but there's no specific mention of `blockquote` in the CSS. From what I see, all `blockquote` provides on the web is semantic markup, but nothing more over a generic `div`. Which is why, more abstractly, I wonder if it would make sense to make this a generic `View`/`Container` element, where one can pass a `tagName` prop for semantic reasons on the web implementation.

We still have the unresolved issue that we have to use inline styles for native, and CSS classes for the web, but otherwise the code could be pretty similar for both:

```jsx
<View tagName="blockquote" className={ className } style={ style }>
    <RichText
        identifier="value"
        // ...
    />
    { ( ! RichText.isEmpty( citation ) || isSelected ) && (
        <RichText
	    identifier="citation"
            // ...
	/>
    ) }
</View>
```

### Review thread 6 (resolved)

#### gziolo on 2019-05-24T09:31:33Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287285051
Path: packages/components/CHANGELOG.md

```diff
@@ -1,3 +1,9 @@
+## 7.5.0
```

It might be 8.0.0 as well so it's safe to put and:
```suggestion
## Master
```
and update after release :)

### Review thread 7

#### youknowriad on 2019-05-24T09:39:47Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287287998
Path: packages/block-library/src/quote/edit.js

```diff
@@ -3,6 +3,7 @@
  */
 import { __ } from '@wordpress/i18n';
 import { AlignmentToolbar, BlockControls, RichText } from '@wordpress/block-editor';
+import { BlockQuotation } from '@wordpress/components';
```

I wonder if we should gather all these Platform specific components in a separate package or otherwise under the same parent variable. 

Whether it's `import { Blockquote, Div } from '@wordpress/platform'` 

or 

```js
import { PlatformComponents } from '@wordpress/components';
const { Blockquote, Div } = PlatformComponents;
````

#### gziolo on 2019-05-24T09:48:13Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287291103
Path: packages/block-library/src/quote/edit.js

```diff
@@ -3,6 +3,7 @@
  */
 import { __ } from '@wordpress/i18n';
 import { AlignmentToolbar, BlockControls, RichText } from '@wordpress/block-editor';
+import { BlockQuotation } from '@wordpress/components';
```

Yep, this is a good point. I think we should extract all those components into their own package and maybe re-export from `@wordpress/components` for backward compatibility only. It's definitely out of the scope of this PR @SergioEstevao. Anyway, it's something we should decide on quite soon as you will need to use more HTML tags like:
- `figure`, `img`
- `ul`, `ol`, `li`
- `div`

### Review thread 8

#### hypest on 2019-05-26T23:36:58Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287618893
Path: packages/block-editor/src/components/rich-text/index.native.js

```diff
@@ -851,6 +859,11 @@ const RichTextContainer = compose( [
 				selectionStart.clientId === clientId &&
 				selectionStart.attributeKey === identifier
 			);
+		} else {
+			isSelected = isSelected && (
```

This change (the addition of the "else" block with its setting of the `isSelected` flag) seems to be causing https://github.com/wordpress-mobile/gutenberg-mobile/issues/1031. @SergioEstevao , do you recall what this change was added to do? I'm going to open a PR to remove it but, I'm afraid I might be missing some context and the removal might add some other problem.

#### hypest on 2019-05-28T07:25:58Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287961722
Path: packages/block-editor/src/components/rich-text/index.native.js

```diff
@@ -851,6 +859,11 @@ const RichTextContainer = compose( [
 				selectionStart.clientId === clientId &&
 				selectionStart.attributeKey === identifier
 			);
+		} else {
+			isSelected = isSelected && (
```

@SergioEstevao added a comment about this here: https://github.com/WordPress/gutenberg/pull/15833#issuecomment-496262819

### Review thread 9

#### koke on 2019-05-27T09:47:38Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287723792
Path: packages/components/src/primitives/block-quotation/style.scss

```diff
@@ -0,0 +1,19 @@
+.wp-block-quote {
```

This is the style for the block, I don't think it should have been moved to the component.

#### gziolo on 2019-05-27T10:21:52Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287735382
Path: packages/components/src/primitives/block-quotation/style.scss

```diff
@@ -0,0 +1,19 @@
+.wp-block-quote {
```

Yes, you are correct. I missed it...

#### gziolo on 2019-05-27T10:26:16Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287736601
Path: packages/components/src/primitives/block-quotation/style.scss

```diff
@@ -0,0 +1,19 @@
+.wp-block-quote {
```

This file was never referenced so it only needs to be deleted.

#### gziolo on 2019-05-27T10:29:46Z

URL: https://github.com/WordPress/gutenberg/pull/15482#discussion_r287737539
Path: packages/components/src/primitives/block-quotation/style.scss

```diff
@@ -0,0 +1,19 @@
+.wp-block-quote {
```

https://github.com/WordPress/gutenberg/pull/15845 fixes it.

## Changed files

- docs/manifest-devhub.json (MODIFIED, +6/-0)
- packages/block-editor/src/components/alignment-toolbar/index.native.js (ADDED, +5/-0)
- packages/block-editor/src/components/index.native.js (MODIFIED, +1/-0)
- packages/block-editor/src/components/rich-text/index.native.js (MODIFIED, +6/-0)
- packages/block-library/src/index.native.js (MODIFIED, +1/-0)
- packages/block-library/src/quote/edit.js (MODIFIED, +3/-2)
- packages/components/CHANGELOG.md (MODIFIED, +6/-0)
- packages/components/src/primitives/block-quotation/README.md (ADDED, +15/-0)
- packages/components/src/primitives/block-quotation/index.js (ADDED, +1/-0)
- packages/components/src/primitives/block-quotation/index.native.js (ADDED, +16/-0)
- packages/components/src/primitives/block-quotation/style.native.scss (ADDED, +7/-0)
- packages/components/src/primitives/block-quotation/style.scss (ADDED, +19/-0)
- packages/components/src/primitives/index.js (MODIFIED, +1/-0)
- packages/viewport/src/index.native.js (MODIFIED, +1/-0)
