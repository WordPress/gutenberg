---
number: 57261
type: pull_request
state: MERGED
merged_at: 2023-12-21T10:53:49Z
url: https://github.com/WordPress/gutenberg/pull/57261
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #57261: Truncate: improve handling of non-string children

- URL: https://github.com/WordPress/gutenberg/pull/57261
- Author: ciampo
- Created: 2023-12-20T13:04:04Z
- Updated: 2023-12-21T10:54:16Z
- Comments: 3 of 3
- Review threads: 2 of 2
- Changed files: 5 of 5
- Diff: ../pr-diffs/57261.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

This PR introduces a few changes to the `Truncate` component to make it more resilient to different types of `children` received:
- it makes the component able to truncate children of type `number`
- when `children` have types other than `string` or `number`, the component renders the `children` without any attempted truncation (instead of not rendering the `children` at all)

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

While working on #55625, I [discovered](https://github.com/WordPress/gutenberg/pull/55625#discussion_r1432445608) that `Truncate` only handles `children` of type `string`, and otherwise renders an empty `<span />` tag.

The component falsely advertises that its `children` are of type `ReactNode`, while in reality the component only knows how to deal with `string`.

This behaviour is not great, and can cause confusion and disruption in the consumers of `Truncate`

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Tweaked the logic of the truncate hook:
- numbers are converted to string so that they can be truncated
- when detecting `children` that are not `string` of `number` type, the component simply no-ops and renders `children` as-is
- added unit tests for this new behaviour

## Issue comments

### ciampo on 2023-12-20T17:05:23Z

URL: https://github.com/WordPress/gutenberg/pull/57261#issuecomment-1864834262

Thank you for the review, @ntsekouras and @mirka !

>  I'd like to suggest a few more things to mitigate consumer confusion:

Sounds good, I went ahead and implemented the suggested changes.

### ciampo on 2023-12-21T10:21:54Z

URL: https://github.com/WordPress/gutenberg/pull/57261#issuecomment-1866005204

Looking more into unit tests failures, it emerges that a lot of components use `Truncate` indirectly via the `Text` and `Heading` components.

The problem with that is that the `Text` component adds some extra logic to its hook which in some cases completely ignores the result of calling `useTruncate`:

https://github.com/WordPress/gutenberg/blob/830d13b9502b9dd32175098342f090f5d46fcb29/packages/components/src/text/hook.ts#L144-L183

Therefore, I don't think that showing a warning makes a lot of sense because it would just pollute the console for a lot of 	Text` consumers.

### github-actions on 2023-12-21T10:46:00Z

URL: https://github.com/WordPress/gutenberg/pull/57261#issuecomment-1866038150

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 23818de41c72024871f09e87516e24544f7751a6.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/7286939991
📝  Reported issues:
- #40369 in `specs/editor/various/block-switcher.test.js`

## Review threads

### Review thread 1

#### mirka on 2023-12-21T09:43:10Z

URL: https://github.com/WordPress/gutenberg/pull/57261#discussion_r1433819848
Path: packages/components/src/truncate/hook.ts

```diff
@@ -33,17 +33,29 @@ export default function useTruncate(
 
 	const cx = useCx();
 
-	const truncatedContent = truncateContent(
-		typeof children === 'string' ? children : '',
-		{
-			ellipsis,
-			ellipsizeMode,
-			limit,
-			numberOfLines,
-		}
-	);
+	let childrenAsText;
+	if ( typeof children === 'string' ) {
+		childrenAsText = children;
+	} else if ( typeof children === 'number' ) {
+		childrenAsText = children.toString();
+	} else {
+		// eslint-disable-next-line no-console
+		console.warn(
```

I think it's preferable to use `@wordpress/warning`?

### Review thread 2

#### mirka on 2023-12-21T09:45:09Z

URL: https://github.com/WordPress/gutenberg/pull/57261#discussion_r1433823014
Path: packages/components/src/truncate/hook.ts

```diff
@@ -33,17 +33,29 @@ export default function useTruncate(
 
 	const cx = useCx();
 
-	const truncatedContent = truncateContent(
-		typeof children === 'string' ? children : '',
-		{
-			ellipsis,
-			ellipsizeMode,
-			limit,
-			numberOfLines,
-		}
-	);
+	let childrenAsText;
+	if ( typeof children === 'string' ) {
+		childrenAsText = children;
+	} else if ( typeof children === 'number' ) {
+		childrenAsText = children.toString();
+	} else {
+		// eslint-disable-next-line no-console
+		console.warn(
+			`Truncate: text truncation has been disabled, since it is only available when passing 'children' of type 'string' or 'number'`
```

```suggestion
			`wp.components.Truncate: text truncation has been disabled, since it is only available when passing 'children' of type 'string' or 'number'`
```

Probably better for clarity, since `Truncate` is used under the hood for a handful of more common components and the consumer may not be aware of what "Truncate" is specifically.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/truncate/README.md (MODIFIED, +8/-0)
- packages/components/src/truncate/hook.ts (MODIFIED, +17/-10)
- packages/components/src/truncate/test/index.tsx (MODIFIED, +54/-27)
- packages/components/src/truncate/types.ts (MODIFIED, +4/-0)
