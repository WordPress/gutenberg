---
number: 67280
type: pull_request
state: MERGED
merged_at: 2024-11-26T16:46:43Z
url: https://github.com/WordPress/gutenberg/pull/67280
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components","Storybook"]
---

# PR #67280: Icon: Improve `icon` prop usage docs in Storybook

- URL: https://github.com/WordPress/gutenberg/pull/67280
- Author: mirka
- Created: 2024-11-25T18:22:09Z
- Updated: 2024-12-04T14:42:06Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 1 of 1
- Diff: ../pr-diffs/67280.diff

## Body

In preparation for #67242

## What?

Improve the `icon` prop usage examples in the Storybook for `Icon`.

(The overall type docs and readme are improved in #67282.)

## Why?

The "With a Function" and "With a Component" examples didn't really make sense, and didn't demonstrate how the `size` prop is passed.

## Testing Instructions

See the Storybook docs for the `Icon` component in `@wordpress/components`.

## Screenshots or screencast <!-- if applicable -->

<img width="828" alt="Improved descriptions and code snippets for the function and component usages" src="https://github.com/user-attachments/assets/cbcd92d0-3f58-4125-ab08-e968c1f6d0a1">

## Issue comments

### github-actions on 2024-11-25T20:07:23Z

URL: https://github.com/WordPress/gutenberg/pull/67280#issuecomment-2498931254

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### mirka on 2024-11-25T18:23:53Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1857154961
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,70 @@ FillColor.args = {
 	...Default.args,
 };
 
+/**
+ * When `icon` is a function, it will be passed the `size` prop and any other additional props.
+ */
 export const WithAFunction = Template.bind( {} );
 WithAFunction.args = {
 	...Default.args,
-	icon: () => (
-		<SVG>
-			<Path d="M5 4v3h5.5v12h3V7H19V4z" />
-		</SVG>
+	icon: ( { size }: { size?: number } ) => (
+		<img
+			width={ size }
+			height={ size }
+			src="https://s.w.org/style/images/about/WordPress-logotype-wmark.png"
+			alt="WordPress"
+		/>
 	),
 };
+WithAFunction.parameters = {
+	docs: {
+		source: {
+			code: `
+<Icon
+  icon={ ( { size } ) => (
+    <img
```

I'd like to avoid manual snippets as much as possible, but I think it's unavoidable in this case.

#### tyxla on 2024-11-26T12:10:13Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1858395953
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,70 @@ FillColor.args = {
 	...Default.args,
 };
 
+/**
+ * When `icon` is a function, it will be passed the `size` prop and any other additional props.
+ */
 export const WithAFunction = Template.bind( {} );
 WithAFunction.args = {
 	...Default.args,
-	icon: () => (
-		<SVG>
-			<Path d="M5 4v3h5.5v12h3V7H19V4z" />
-		</SVG>
+	icon: ( { size }: { size?: number } ) => (
+		<img
+			width={ size }
+			height={ size }
+			src="https://s.w.org/style/images/about/WordPress-logotype-wmark.png"
+			alt="WordPress"
+		/>
 	),
 };
+WithAFunction.parameters = {
+	docs: {
+		source: {
+			code: `
+<Icon
+  icon={ ( { size } ) => (
+    <img
```

That's understandable, but on the contrary, those snippets at least look good in the story:

![Screenshot 2024-11-26 at 14 09 09](https://github.com/user-attachments/assets/10935b2a-486e-4702-a5e0-eb20a4596321)

while the code snipped for the default story looks like this:

![Screenshot 2024-11-26 at 14 09 22](https://github.com/user-attachments/assets/b6dabee4-bd9a-4968-bbfb-eb72f8ac7961)

I wish they were all properly formatted.

#### mirka on 2024-11-26T13:59:27Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1858594715
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,70 @@ FillColor.args = {
 	...Default.args,
 };
 
+/**
+ * When `icon` is a function, it will be passed the `size` prop and any other additional props.
+ */
 export const WithAFunction = Template.bind( {} );
 WithAFunction.args = {
 	...Default.args,
-	icon: () => (
-		<SVG>
-			<Path d="M5 4v3h5.5v12h3V7H19V4z" />
-		</SVG>
+	icon: ( { size }: { size?: number } ) => (
+		<img
+			width={ size }
+			height={ size }
+			src="https://s.w.org/style/images/about/WordPress-logotype-wmark.png"
+			alt="WordPress"
+		/>
 	),
 };
+WithAFunction.parameters = {
+	docs: {
+		source: {
+			code: `
+<Icon
+  icon={ ( { size } ) => (
+    <img
```

> I wish they were all properly formatted.

Right 😅 The automatic code snippet engine leaves a lot to be desired. I feel like we might need to start investing more in manual code snippets, since some of them are quite inscrutable.

#### tyxla on 2024-11-26T14:39:01Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1858661143
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,70 @@ FillColor.args = {
 	...Default.args,
 };
 
+/**
+ * When `icon` is a function, it will be passed the `size` prop and any other additional props.
+ */
 export const WithAFunction = Template.bind( {} );
 WithAFunction.args = {
 	...Default.args,
-	icon: () => (
-		<SVG>
-			<Path d="M5 4v3h5.5v12h3V7H19V4z" />
-		</SVG>
+	icon: ( { size }: { size?: number } ) => (
+		<img
+			width={ size }
+			height={ size }
+			src="https://s.w.org/style/images/about/WordPress-logotype-wmark.png"
+			alt="WordPress"
+		/>
 	),
 };
+WithAFunction.parameters = {
+	docs: {
+		source: {
+			code: `
+<Icon
+  icon={ ( { size } ) => (
+    <img
```

I wonder if we can layer prettier on top of it 😉

### Review thread 2

#### tyxla on 2024-11-26T12:13:23Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1858410078
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,68 @@ FillColor.args = {
 	...Default.args,
```

I don't see a control to add a classname anywhere in the stories. Should we have one?

#### mirka on 2024-11-26T13:56:01Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1858589062
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,68 @@ FillColor.args = {
 	...Default.args,
```

My thoughts on the `className` issue are [here](https://github.com/WordPress/gutenberg/pull/67282#discussion_r1858568220). I would prefer to align with the standard docgen behavior (i.e. allowed element props are not listed out unless explicitly highlighted).

Although if we get signals that the lack of `className` documentation is causing confusion, I'm also open to standardizing on "always document `className` explicitly".

#### tyxla on 2024-11-26T14:40:46Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1858664368
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,68 @@ FillColor.args = {
 	...Default.args,
```

Too much documentation is rarely an issue, while too little docs has often been an issue. In favor of extenders, I'd personally suggest proactively documenting all props, even if they're general and available for every component. Not something to block this PR though.

#### ciampo on 2024-12-04T09:09:48Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1869023098
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,68 @@ FillColor.args = {
 	...Default.args,
```

@tyxla , while I agree with you in general, I'm not sure it would be a good idea for our use-case. "All props" means, in many cases, every prop that the underlying HTML element can accept:

- if we list them all, the docs would become very long and it would be hard for consumers of the component to spot the "main" props;
- if we don't list them all, how do we draw a line? It feels very arbitrary and potentially even more confusing, since a user may well assume that if a prop is not listed, it can't be specified.

Can we find an alternative solution, maybe adding a paragraph where we explain that the component also accepts props X Y Z (or "every valid attribute and property for the X HTML element")?

#### tyxla on 2024-12-04T14:42:05Z

URL: https://github.com/WordPress/gutenberg/pull/67280#discussion_r1869667296
Path: packages/components/src/icon/stories/index.story.tsx

```diff
@@ -47,26 +47,68 @@ FillColor.args = {
 	...Default.args,
```

All props might be a bit too much, I agree. But useful props folks want to use most often make sense to be documented IMHO. 

> Can we find an alternative solution, maybe adding a paragraph where we explain that the component also accepts props X Y Z (or "every valid attribute and property for the X HTML element")?

That could work too 👍

## Changed files

- packages/components/src/icon/stories/index.story.tsx (MODIFIED, +50/-8)
