---
number: 64613
type: pull_request
state: MERGED
merged_at: 2024-09-27T15:07:23Z
url: https://github.com/WordPress/gutenberg/pull/64613
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components","[Type] New API","has dev note"]
---

# PR #64613: Navigator: stabilize and export APIs

- URL: https://github.com/WordPress/gutenberg/pull/64613
- Author: ciampo
- Created: 2024-08-19T13:46:09Z
- Updated: 2024-10-14T09:10:06Z
- Comments: 9 of 9
- Review threads: 27 of 27
- Changed files: 24 of 24
- Diff: ../pr-diffs/64613.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Part of #59418
Supersedes #60927
Follow-up to #63317

Export the `Navigator` component, its subcomponents, and the `useNavigator` hook as stable APIs from the `@wordpress/components` package

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->


This is part of a [larger effort](https://github.com/WordPress/gutenberg/issues/59418) to remove `__experimental` prefix from all "experimental" components, effectively promoting them to regular stable components. See the related issue for more context.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Aside from the existing legacy exports, which use the `__experimental` prefix, add new, unprefixed exports:
  - Legacy exports stay untouched;
  - The new, stable exports will adopt slightly different names compared to legacy and the overloaded naming convention that we agreed to apply for component components:
    - `NavigatorProvider` => `Navigator`
    - `NavigatorScreen` => `Navigator.Screen`
    - `NavigatorButton` => `Navigator.Button`
    - `NavigatorBackButton` => `Navigator.BackButton`
    - `NavigatorToParentButton` won't be exported since it's already deprecated in the experimental version of the component
- Updated all JSDocs and README
- Updated Storybook and Unit Tests to use the stable version of the component
- Added Storybook redirect to avoid getting broken links
- A few smaller changes:
  - Updated the internal context namespacing (which shouldn't be a breaking change since context is an internal API of the `@wordpress/components` package)

## Next steps?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- refactor existing usages in Gutenberg to stable version
- deprecate experimental version (both in JSDocs and with a runtime warning) (+ add unit tests for deprecation warnings)

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

- Technically there shouldn't be any runtime (behavioral) changes to `Navigator`;
- Make sure that Storybook examples are documented correctly and work as expected;
- Smoke test `Navigator` usages across the editor;
- Review JSDocs, READMEs, and IntelliSense snippets for both legacy and stable APIs

## ✍️  Dev note

The legacy set of `__experimentalNavigator*` APIs is deprecated and should instead be imported as `Navigator`. All of the sub-components are also available via the `Navigator` namespace.

Moreover, the `__experimentalNavigatorToParentButton` component and the `goToParent` method available via the `__experimentalUseNavigator` hook are now deprecated, and they now behave identically to the `__experimentalNavigatorBackButton` and the `goBack` method.

To recap:

- `__experimentalNavigatorProvider` => `Navigator`
- `__experimentalNavigatorScreen` => `Navigator.Screen`
-  `__experimentalNavigatorButton` => `Navigator.Button`
- `__experimentalNavigatorBackButton` => `Navigator.BackButton`
- `__experimentalNavigatorToParentButton` => `Navigator.BackButton`
- `__experimentalUseNavigator` => `useNavigator`

## Issue comments

### github-actions on 2024-08-19T14:51:25Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2296771543

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### ciampo on 2024-08-22T10:27:00Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2304315964

I'm going to resume work on this PR after https://github.com/WordPress/gutenberg/pull/64675 is merged.

### tyxla on 2024-08-23T10:24:33Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2306788454

> I'm going to resume work on this PR after #64675 is merged.

Sounds good. I was starting to review it, but I will halt it for now.

I wonder if it's a good idea to approach this modularly and split whatever we can to separate PRs to keep this one smaller.

### ciampo on 2024-08-26T09:09:00Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2309726976

I rebased and addressed simple feedback.

I would still wait before reviewing this PR. The current plan of action is:

- [x] Review and merge https://github.com/WordPress/gutenberg/pull/64786
- [x] Review and merge https://github.com/WordPress/gutenberg/pull/64777
- [x] Rebase this PR and prepare for final review

I've also updated the [tracking issue](https://github.com/WordPress/gutenberg/issues/59418) adding all of the planned follow-up step to this PR.

> I wonder if it's a good idea to approach this modularly and split whatever we can to separate PRs to keep this one smaller.

@tyxla , Even if the size of the PR is medium-large, I believe 99% of the changes left in this PR are simple renames — and in that sense I feel ok with the current size. Having said that, I'll re-assess once all dependency PRs are merged, before asking for a final round of review.

### tyxla on 2024-08-28T17:55:02Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2315947747

> @tyxla , Even if the size of the PR is medium-large, I believe 99% of the changes left in this PR are simple renames — and in that sense I feel ok with the current size. Having said that, I'll re-assess once all dependency PRs are merged, before asking for a final round of review.

That makes sense, @ciampo, will help with the review once all the dependencies are merged 👍

### github-actions on 2024-09-26T09:04:05Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2376376203

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 3a6e917c037b029c0f44617a5754245932312dda.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/11048723716
📝  Reported issues:
- #64763 in `/test/e2e/specs/editor/blocks/navigation-colors.spec.js`

### ciampo on 2024-09-26T10:08:09Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2376518238

Rebased on top of trunk to include the latest changes.



> > I wonder if it's a good idea to approach this modularly and split whatever we can to separate PRs to keep this one smaller.
> 
> @tyxla , Even if the size of the PR is medium-large, I believe 99% of the changes left in this PR are simple renames — and in that sense I feel ok with the current size. Having said that, I'll re-assess once all dependency PRs are merged, before asking for a final round of review.

I split some code changes to #65671, which is ready for review.

### ciampo on 2024-09-26T14:41:32Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2377171985

Rebased to include changes from https://github.com/WordPress/gutenberg/pull/65671

This PR is ready for a final round of review @tyxla @mirka

### ciampo on 2024-09-27T08:52:06Z

URL: https://github.com/WordPress/gutenberg/pull/64613#issuecomment-2378782291

All feedback should be addressed, including [this conversation](https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777390024) on keeping one vs many READMEs.

## Review threads

### Review thread 1

#### ciampo on 2024-08-19T14:18:47Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1721871019
Path: packages/components/src/navigator/legacy.ts

Legacy exports have been moved to a separate `legacy.ts` file (including the legacy JSDocs mentioning the components with their experimental export name).

This makes it easy to have separate names from the stable version, different JSDocs, and deprecation warnings.

### Review thread 2

#### ciampo on 2024-08-19T14:19:24Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1721872034
Path: packages/components/src/navigator/index.ts

New exports (and their up-to-date JSDocs) have been moved to the folder's main `index.ts` file

### Review thread 3

#### ciampo on 2024-08-19T14:20:40Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1721873998
Path: packages/components/src/navigator/navigator-back-button/README.md

All READMEs have also been updated:
- removed experimental snippet
- updated the naming of all `Navigator` sub-components, including internal links to other READMEs

### Review thread 4

#### ciampo on 2024-08-19T14:21:33Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1721875333
Path: packages/components/src/navigator/navigator-provider/index.ts

This file was actually renamed to `packages/components/src/navigator/navigator/index.tsx`, but GitHub is not picking that up

### Review thread 5

#### ciampo on 2024-08-19T14:23:25Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1721878192
Path: packages/components/src/navigator/navigator/component.tsx

```diff
@@ -273,41 +273,31 @@ function UnconnectedNavigatorProvider(
 }
 
 /**
- * The `NavigatorProvider` component allows rendering nested views/panels/menus
- * (via the `NavigatorScreen` component and navigate between these different
- * view (via the `NavigatorButton` and `NavigatorBackButton` components or the
+ * The `Navigator` component allows rendering nested views/panels/menus
```

The top level `Navigator` component (previously named `NavigatorProvider`) is the only component for which I've kept a JSDoc for the internal implementation, because apparently Storybook uses it when displaying docs 🤷  Not sure why it doesn't use the one defined in `packages/components/src/navigator/index.ts`

#### ciampo on 2024-08-21T15:38:44Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1725316681
Path: packages/components/src/navigator/navigator/component.tsx

```diff
@@ -273,41 +273,31 @@ function UnconnectedNavigatorProvider(
 }
 
 /**
- * The `NavigatorProvider` component allows rendering nested views/panels/menus
- * (via the `NavigatorScreen` component and navigate between these different
- * view (via the `NavigatorButton` and `NavigatorBackButton` components or the
+ * The `Navigator` component allows rendering nested views/panels/menus
```

it seems like the `ts` extension was the problem — changing it to `.tsx` seems to allow Storybook to pick the JSDoc up 😅  https://github.com/WordPress/gutenberg/pull/64613/commits/85471a9224994d8977221fc4915a159622c3f307


cc @mirka

### Review thread 6

#### ciampo on 2024-08-19T14:24:07Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1721879270
Path: packages/components/src/navigator/stories/index.story.tsx

```diff
@@ -10,20 +10,20 @@ import Button from '../../button';
 import { Card, CardBody, CardFooter, CardHeader } from '../../card';
 import { VStack } from '../../v-stack';
 import Dropdown from '../../dropdown';
-import {
-	NavigatorProvider,
-	NavigatorScreen,
-	NavigatorButton,
-	NavigatorBackButton,
-	NavigatorToParentButton,
-	useNavigator,
-} from '..';
 
-const meta: Meta< typeof NavigatorProvider > = {
-	component: NavigatorProvider,
-	// @ts-expect-error - See https://github.com/storybookjs/storybook/issues/23170
-	subcomponents: { NavigatorScreen, NavigatorButton, NavigatorBackButton },
-	title: 'Components (Experimental)/Navigator',
+import { Navigator, useNavigator } from '../';
+
+const meta: Meta< typeof Navigator > = {
+	component: Navigator,
+	subcomponents: {
+		// @ts-expect-error - See https://github.com/storybookjs/storybook/issues/23170
+		Screen: Navigator.Screen,
+		// @ts-expect-error - See https://github.com/storybookjs/storybook/issues/23170
+		Button: Navigator.Button,
+		// @ts-expect-error - See https://github.com/storybookjs/storybook/issues/23170
+		BackButton: Navigator.BackButton,
+	},
+	title: 'Components/Navigator',
```

Updated the `PREVIOUSLY_EXPERIMENTAL_COMPONENTS` in `storybook/manager-head.html` to make sure old links still work

### Review thread 7

#### mirka on 2024-08-22T09:16:00Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726675467
Path: docs/manifest.json

```diff
@@ -1103,22 +1103,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
 		"parent": "components"
 	},
-	{
-		"title": "NavigatorProvider",
-		"slug": "navigator-provider",
-		"markdown_source": "../packages/components/src/navigator/navigator-provider/README.md",
-		"parent": "components"
-	},
```

FYI, removing things from the manifest is not going to remove it from the [Block Editor Handbook](https://developer.wordpress.org/block-editor/reference-guides/components/) 😱 There are some extra steps, see https://github.com/WordPress/gutenberg/pull/60003#issuecomment-2010288927

#### ciampo on 2024-08-22T10:21:38Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726786139
Path: docs/manifest.json

```diff
@@ -1103,22 +1103,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
 		"parent": "components"
 	},
-	{
-		"title": "NavigatorProvider",
-		"slug": "navigator-provider",
-		"markdown_source": "../packages/components/src/navigator/navigator-provider/README.md",
-		"parent": "components"
-	},
```

Thank you! I'll follow-up accordingly

#### tyxla on 2024-09-26T15:56:08Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777363375
Path: docs/manifest.json

```diff
@@ -1103,22 +1103,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
 		"parent": "components"
 	},
-	{
-		"title": "NavigatorProvider",
-		"slug": "navigator-provider",
-		"markdown_source": "../packages/components/src/navigator/navigator-provider/README.md",
-		"parent": "components"
-	},
```

Just a reminder we'll need to take care of this one after merging.

#### ciampo on 2024-09-30T16:22:02Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1781424606
Path: docs/manifest.json

```diff
@@ -1103,22 +1103,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
 		"parent": "components"
 	},
-	{
-		"title": "NavigatorProvider",
-		"slug": "navigator-provider",
-		"markdown_source": "../packages/components/src/navigator/navigator-provider/README.md",
-		"parent": "components"
-	},
```

Hey @WordPress/wordpress-org-meta-team , could you help with this? We need to update the Block Editor Handbook, removing the following components:

- [`NavigatorProvider`](https://developer.wordpress.org/block-editor/reference-guides/components/navigator-provider/)
- [`NavigatorButton`](https://developer.wordpress.org/block-editor/reference-guides/components/navigator-button/)
- [`NavigatorBackButton`](https://developer.wordpress.org/block-editor/reference-guides/components/navigator-back-button/)
- [`NavigatorScreen`](https://developer.wordpress.org/block-editor/reference-guides/components/navigator-screen/)
- [`NavigatorToParentButton`](https://developer.wordpress.org/block-editor/reference-guides/components/navigator-to-parent-button/)


If possible, we'd also like to set a redirect for those pages being removed. The redirect should point to [the `Navigator` component](https://developer.wordpress.org/block-editor/reference-guides/components/navigator/).


Could you please help with that? Thank you 🙏

#### adamwoodnz on 2024-09-30T21:44:43Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1781774570
Path: docs/manifest.json

```diff
@@ -1103,22 +1103,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
 		"parent": "components"
 	},
-	{
-		"title": "NavigatorProvider",
-		"slug": "navigator-provider",
-		"markdown_source": "../packages/components/src/navigator/navigator-provider/README.md",
-		"parent": "components"
-	},
```

These updates have shipped. I noticed the formatting on the new doc loses consistency around the `Navigator.Button` point (heading is not bold):

![Screenshot 2024-10-01 at 10 42 06 AM](https://github.com/user-attachments/assets/37da3574-ee2c-4830-9ed8-fe50b6d62564)

#### ciampo on 2024-09-30T22:08:11Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1781839881
Path: docs/manifest.json

```diff
@@ -1103,22 +1103,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
 		"parent": "components"
 	},
-	{
-		"title": "NavigatorProvider",
-		"slug": "navigator-provider",
-		"markdown_source": "../packages/components/src/navigator/navigator-provider/README.md",
-		"parent": "components"
-	},
```

Thank you!



> I noticed the formatting on the new doc loses consistency around the `Navigator.Button` point (heading is not bold):

Good catch! Opened https://github.com/WordPress/gutenberg/pull/65763 to fix it

### Review thread 8 (resolved)

#### mirka on 2024-08-22T09:17:05Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726677282
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,10 @@
 
 ## Unreleased
 
+### New Features
```

Not "Enhancements"? (Enhancement == new feature)

#### ciampo on 2024-08-22T10:23:15Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726788515
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,10 @@
 
 ## Unreleased
 
+### New Features
```

My reasoning was "New API" (stable `Navigator`) => "New feature" section

Although I'm very much fine putting it under "Enhancements"

#### ciampo on 2024-08-26T09:04:17Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1730939895
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,10 @@
 
 ## Unreleased
 
+### New Features
```

Done https://github.com/WordPress/gutenberg/pull/64613/commits/af97de9356dd7a8ba9a3ceeef705edebc0d36e71

### Review thread 9 (resolved)

#### mirka on 2024-08-22T09:22:49Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726687113
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	dislayName: 'Navigator',
```

```suggestion
```

I can confirm this is unnecessary because it's misspelled 😄

#### ciampo on 2024-08-22T10:23:39Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726789092
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	dislayName: 'Navigator',
```

Lol. That's one way to test changes out

#### ciampo on 2024-08-26T09:04:34Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1730940281
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	dislayName: 'Navigator',
```

Done https://github.com/WordPress/gutenberg/pull/64613/commits/81931212710d2112376156173907acaa10d7158a

### Review thread 10 (resolved)

#### mirka on 2024-08-22T09:25:28Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726691740
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	dislayName: 'Navigator',
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
```

I actually like this better than doing `NavigatorScreen.displayName = 'Navigator.Screen'` in the individual subcomponent files. It makes more sense to handle that concern in this top-level index file. Should we update the contributing guide accordingly?

#### ciampo on 2024-08-22T10:24:09Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726790024
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	dislayName: 'Navigator',
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
```

Sounds good!

#### ciampo on 2024-08-26T09:38:25Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1730984599
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	dislayName: 'Navigator',
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
```

https://github.com/WordPress/gutenberg/pull/64793

### Review thread 11 (resolved)

#### mirka on 2024-08-22T09:26:02Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726692698
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
```

```suggestion
```

Should we remove this `@example` tag so the snippet shows up in the Storybook?

#### ciampo on 2024-08-26T09:04:43Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1730940478
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,134 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator';
+import { NavigatorScreen } from './navigator-screen';
+import { NavigatorButton } from './navigator-button';
+import { NavigatorBackButton } from './navigator-back-button';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * view (via the `Navigator.Button` and `Navigator.BackButton` components or the
+ * `useNavigator` hook).
+ *
+ * @example
```

https://github.com/WordPress/gutenberg/pull/64613/commits/e7d20195ce125e78a2594874445bb34cb939766d

### Review thread 12 (resolved)

#### mirka on 2024-08-22T09:51:33Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726736870
Path: packages/components/src/navigator/navigator-back-button/component.tsx

```diff
@@ -21,43 +21,7 @@ function UnconnectedNavigatorBackButton(
 	return <View ref={ forwardedRef } { ...navigatorBackButtonProps } />;
 }
 
-/**
- * The `NavigatorBackButton` component can be used to navigate to a screen and
- * should be used in combination with the `NavigatorProvider`, the
- * `NavigatorScreen` and the `NavigatorButton` components (or the `useNavigator`
- * hook).
- *
- * @example
- * ```jsx
- * import {
- *   __experimentalNavigatorProvider as NavigatorProvider,
- *   __experimentalNavigatorScreen as NavigatorScreen,
- *   __experimentalNavigatorButton as NavigatorButton,
- *   __experimentalNavigatorBackButton as NavigatorBackButton,
- * } from '@wordpress/components';
- *
- * const MyNavigation = () => (
- *   <NavigatorProvider initialPath="/">
- *     <NavigatorScreen path="/">
- *       <p>This is the home screen.</p>
- *        <NavigatorButton path="/child">
- *          Navigate to child screen.
- *       </NavigatorButton>
- *     </NavigatorScreen>
- *
- *     <NavigatorScreen path="/child">
- *       <p>This is the child screen.</p>
- *       <NavigatorBackButton>
- *         Go back (to parent)
- *       </NavigatorBackButton>
- *     </NavigatorScreen>
- *   </NavigatorProvider>
- * );
- * ```
- */
 export const NavigatorBackButton = contextConnect(
 	UnconnectedNavigatorBackButton,
-	'NavigatorBackButton'
+	'Navigator.BackButton'
 );
```

Hmm, this is an interesting one. If we do it this way, the context name is going to change for consumers of the legacy `NavigatorBackButton`, right? I wonder if we need separate context-connnected exports for the current and the legacy?

#### ciampo on 2024-08-22T10:26:56Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726794395
Path: packages/components/src/navigator/navigator-back-button/component.tsx

```diff
@@ -21,43 +21,7 @@ function UnconnectedNavigatorBackButton(
 	return <View ref={ forwardedRef } { ...navigatorBackButtonProps } />;
 }
 
-/**
- * The `NavigatorBackButton` component can be used to navigate to a screen and
- * should be used in combination with the `NavigatorProvider`, the
- * `NavigatorScreen` and the `NavigatorButton` components (or the `useNavigator`
- * hook).
- *
- * @example
- * ```jsx
- * import {
- *   __experimentalNavigatorProvider as NavigatorProvider,
- *   __experimentalNavigatorScreen as NavigatorScreen,
- *   __experimentalNavigatorButton as NavigatorButton,
- *   __experimentalNavigatorBackButton as NavigatorBackButton,
- * } from '@wordpress/components';
- *
- * const MyNavigation = () => (
- *   <NavigatorProvider initialPath="/">
- *     <NavigatorScreen path="/">
- *       <p>This is the home screen.</p>
- *        <NavigatorButton path="/child">
- *          Navigate to child screen.
- *       </NavigatorButton>
- *     </NavigatorScreen>
- *
- *     <NavigatorScreen path="/child">
- *       <p>This is the child screen.</p>
- *       <NavigatorBackButton>
- *         Go back (to parent)
- *       </NavigatorBackButton>
- *     </NavigatorScreen>
- *   </NavigatorProvider>
- * );
- * ```
- */
 export const NavigatorBackButton = contextConnect(
 	UnconnectedNavigatorBackButton,
-	'NavigatorBackButton'
+	'Navigator.BackButton'
 );
```

It is, yeah. although I didn't think it would be a problem — from the PR description:

> Updated the internal context namespacing (which shouldn't be a breaking change since context is an internal API of the `@wordpress/components` package)

The approach that you propose probably won't be enough, since we'd need to also change the `useContextSystem` call in the component render functions.

#### mirka on 2024-08-22T11:23:20Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726871126
Path: packages/components/src/navigator/navigator-back-button/component.tsx

```diff
@@ -21,43 +21,7 @@ function UnconnectedNavigatorBackButton(
 	return <View ref={ forwardedRef } { ...navigatorBackButtonProps } />;
 }
 
-/**
- * The `NavigatorBackButton` component can be used to navigate to a screen and
- * should be used in combination with the `NavigatorProvider`, the
- * `NavigatorScreen` and the `NavigatorButton` components (or the `useNavigator`
- * hook).
- *
- * @example
- * ```jsx
- * import {
- *   __experimentalNavigatorProvider as NavigatorProvider,
- *   __experimentalNavigatorScreen as NavigatorScreen,
- *   __experimentalNavigatorButton as NavigatorButton,
- *   __experimentalNavigatorBackButton as NavigatorBackButton,
- * } from '@wordpress/components';
- *
- * const MyNavigation = () => (
- *   <NavigatorProvider initialPath="/">
- *     <NavigatorScreen path="/">
- *       <p>This is the home screen.</p>
- *        <NavigatorButton path="/child">
- *          Navigate to child screen.
- *       </NavigatorButton>
- *     </NavigatorScreen>
- *
- *     <NavigatorScreen path="/child">
- *       <p>This is the child screen.</p>
- *       <NavigatorBackButton>
- *         Go back (to parent)
- *       </NavigatorBackButton>
- *     </NavigatorScreen>
- *   </NavigatorProvider>
- * );
- * ```
- */
 export const NavigatorBackButton = contextConnect(
 	UnconnectedNavigatorBackButton,
-	'NavigatorBackButton'
+	'Navigator.BackButton'
 );
```

Wow, you're right. I always assumed the context API was public! 😂

### Review thread 13 (resolved)

#### mirka on 2024-08-22T09:53:15Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726739725
Path: packages/components/src/navigator/test/index.tsx

```diff
@@ -14,14 +14,14 @@ import { useState } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../../button';
+import { Navigator, useNavigator } from '..';
 import {
-	NavigatorProvider,
-	NavigatorScreen,
-	NavigatorButton,
-	NavigatorBackButton,
+	// NavigatorProvider,
+	// NavigatorScreen,
+	// NavigatorButton,
+	// NavigatorBackButton,
```

🧹 ?

#### ciampo on 2024-08-26T09:04:53Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1730940684
Path: packages/components/src/navigator/test/index.tsx

```diff
@@ -14,14 +14,14 @@ import { useState } from '@wordpress/element';
  * Internal dependencies
  */
 import Button from '../../button';
+import { Navigator, useNavigator } from '..';
 import {
-	NavigatorProvider,
-	NavigatorScreen,
-	NavigatorButton,
-	NavigatorBackButton,
+	// NavigatorProvider,
+	// NavigatorScreen,
+	// NavigatorButton,
+	// NavigatorBackButton,
```

🧹  https://github.com/WordPress/gutenberg/pull/64613/commits/0f2d077905f721ecb89023192f16656b4b08fccc

### Review thread 14 (resolved)

#### tyxla on 2024-09-26T15:57:10Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777365122
Path: packages/components/CHANGELOG.md

```diff
@@ -17,6 +17,7 @@
 -   `MenuGroup`: Simplify the MenuGroup styles within dropdown menus. ([#65561](https://github.com/WordPress/gutenberg/pull/65561)).
 -   `DatePicker`: Use compact button size. ([#65653](https://github.com/WordPress/gutenberg/pull/65653)).
 -   `Navigator`: add support for exit animation ([#64777](https://github.com/WordPress/gutenberg/pull/64777)).
+-   `Navigator`: stabilize APIs ([#64613](https://github.com/WordPress/gutenberg/pull/64613)).
```

I think it makes sense to list it under "New features" as well, now that it's stable

#### ciampo on 2024-09-26T16:34:04Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777421853
Path: packages/components/CHANGELOG.md

```diff
@@ -17,6 +17,7 @@
 -   `MenuGroup`: Simplify the MenuGroup styles within dropdown menus. ([#65561](https://github.com/WordPress/gutenberg/pull/65561)).
 -   `DatePicker`: Use compact button size. ([#65653](https://github.com/WordPress/gutenberg/pull/65653)).
 -   `Navigator`: add support for exit animation ([#64777](https://github.com/WordPress/gutenberg/pull/64777)).
+-   `Navigator`: stabilize APIs ([#64613](https://github.com/WordPress/gutenberg/pull/64613)).
```

We had already [discussed](https://github.com/WordPress/gutenberg/pull/64613#discussion_r1726677282) which CHANGELOG section to put this under, and we went for Enhancements (also, similar PRs also added an entry under Enhancements)

#### tyxla on 2024-09-26T16:35:36Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777423638
Path: packages/components/CHANGELOG.md

```diff
@@ -17,6 +17,7 @@
 -   `MenuGroup`: Simplify the MenuGroup styles within dropdown menus. ([#65561](https://github.com/WordPress/gutenberg/pull/65561)).
 -   `DatePicker`: Use compact button size. ([#65653](https://github.com/WordPress/gutenberg/pull/65653)).
 -   `Navigator`: add support for exit animation ([#64777](https://github.com/WordPress/gutenberg/pull/64777)).
+-   `Navigator`: stabilize APIs ([#64613](https://github.com/WordPress/gutenberg/pull/64613)).
```

I just think it makes sense since it's part of the formalization of the stable API, but I have no strong feelings about it. Leaving to your judgment.

#### ciampo on 2024-09-26T17:59:58Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777530581
Path: packages/components/CHANGELOG.md

```diff
@@ -17,6 +17,7 @@
 -   `MenuGroup`: Simplify the MenuGroup styles within dropdown menus. ([#65561](https://github.com/WordPress/gutenberg/pull/65561)).
 -   `DatePicker`: Use compact button size. ([#65653](https://github.com/WordPress/gutenberg/pull/65653)).
 -   `Navigator`: add support for exit animation ([#64777](https://github.com/WordPress/gutenberg/pull/64777)).
+-   `Navigator`: stabilize APIs ([#64613](https://github.com/WordPress/gutenberg/pull/64613)).
```

I will favour consistency and leave it under the `Enhancements` section, as we've done for other PRs

### Review thread 15 (resolved)

#### tyxla on 2024-09-26T15:58:31Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777367722
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,132 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator/component';
+import { NavigatorScreen } from './navigator-screen/component';
+import { NavigatorButton } from './navigator-button/component';
+import { NavigatorBackButton } from './navigator-back-button/component';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * views (via the `Navigator.Button` and `Navigator.BackButton` components or
+ * the `useNavigator` hook).
+ *
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
+	/**
+	 * The `Navigator.Button` component can be used to navigate to a screen and
+	 * should be used in combination with the `Navigator`, the `Navigator.Screen`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
```

We claim that these components should be used also with `useNavigator`, but then should we include the hook usage in the examples?

#### ciampo on 2024-09-26T16:36:02Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777424133
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,132 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator/component';
+import { NavigatorScreen } from './navigator-screen/component';
+import { NavigatorButton } from './navigator-button/component';
+import { NavigatorBackButton } from './navigator-back-button/component';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * views (via the `Navigator.Button` and `Navigator.BackButton` components or
+ * the `useNavigator` hook).
+ *
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
+	/**
+	 * The `Navigator.Button` component can be used to navigate to a screen and
+	 * should be used in combination with the `Navigator`, the `Navigator.Screen`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
```

Good point. TBH, `useNavigator` is more of an advanced usage (and there's an example of it in Storybook). Do you think that's enough, or should I look into making the snippet longer and incorporate `useNavigator`?

#### tyxla on 2024-09-26T16:37:03Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777425358
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,132 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator/component';
+import { NavigatorScreen } from './navigator-screen/component';
+import { NavigatorButton } from './navigator-button/component';
+import { NavigatorBackButton } from './navigator-back-button/component';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * views (via the `Navigator.Button` and `Navigator.BackButton` components or
+ * the `useNavigator` hook).
+ *
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
+	/**
+	 * The `Navigator.Button` component can be used to navigate to a screen and
+	 * should be used in combination with the `Navigator`, the `Navigator.Screen`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
```

Or we can just exclude the hook from the description?

#### ciampo on 2024-09-26T16:43:38Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777433487
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,132 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator/component';
+import { NavigatorScreen } from './navigator-screen/component';
+import { NavigatorButton } from './navigator-button/component';
+import { NavigatorBackButton } from './navigator-back-button/component';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * views (via the `Navigator.Button` and `Navigator.BackButton` components or
+ * the `useNavigator` hook).
+ *
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
+	/**
+	 * The `Navigator.Button` component can be used to navigate to a screen and
+	 * should be used in combination with the `Navigator`, the `Navigator.Screen`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
```

Sure, I can do that

#### ciampo on 2024-09-26T18:02:07Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777533297
Path: packages/components/src/navigator/index.tsx

```diff
@@ -0,0 +1,132 @@
+/**
+ * Internal dependencies
+ */
+import { Navigator as TopLevelNavigator } from './navigator/component';
+import { NavigatorScreen } from './navigator-screen/component';
+import { NavigatorButton } from './navigator-button/component';
+import { NavigatorBackButton } from './navigator-back-button/component';
+export { useNavigator } from './use-navigator';
+
+/**
+ * The `Navigator` component allows rendering nested views/panels/menus
+ * (via the `Navigator.Screen` component) and navigate between these different
+ * views (via the `Navigator.Button` and `Navigator.BackButton` components or
+ * the `useNavigator` hook).
+ *
+ * ```jsx
+ * import { Navigator } from '@wordpress/components';
+ *
+ * const MyNavigation = () => (
+ *   <Navigator initialPath="/">
+ *     <Navigator.Screen path="/">
+ *       <p>This is the home screen.</p>
+ *        <Navigator.Button path="/child">
+ *          Navigate to child screen.
+ *       </Navigator.Button>
+ *     </Navigator.Screen>
+ *
+ *     <Navigator.Screen path="/child">
+ *       <p>This is the child screen.</p>
+ *       <Navigator.BackButton>
+ *         Go back
+ *       </Navigator.BackButton>
+ *     </Navigator.Screen>
+ *   </Navigator>
+ * );
+ * ```
+ */
+export const Navigator = Object.assign( TopLevelNavigator, {
+	/**
+	 * The `Navigator.Screen` component represents a single view/screen/panel and
+	 * should be used in combination with the `Navigator`, the `Navigator.Button`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
+	 *
+	 * @example
+	 * ```jsx
+	 * import { Navigator } from '@wordpress/components';
+	 *
+	 * const MyNavigation = () => (
+	 *   <Navigator initialPath="/">
+	 *     <Navigator.Screen path="/">
+	 *       <p>This is the home screen.</p>
+	 *        <Navigator.Button path="/child">
+	 *          Navigate to child screen.
+	 *       </Navigator.Button>
+	 *     </Navigator.Screen>
+	 *
+	 *     <Navigator.Screen path="/child">
+	 *       <p>This is the child screen.</p>
+	 *       <Navigator.BackButton>
+	 *         Go back
+	 *       </Navigator.BackButton>
+	 *     </Navigator.Screen>
+	 *   </Navigator>
+	 * );
+	 * ```
+	 */
+	Screen: Object.assign( NavigatorScreen, {
+		displayName: 'Navigator.Screen',
+	} ),
+	/**
+	 * The `Navigator.Button` component can be used to navigate to a screen and
+	 * should be used in combination with the `Navigator`, the `Navigator.Screen`
+	 * and the `Navigator.BackButton` components, and the `useNavigator` hook.
```

Mentions removed in https://github.com/WordPress/gutenberg/pull/64613/commits/3c3df877bd654c92a8898d363c047e06934dc276.

Folks can always refer to [this section](https://github.com/WordPress/gutenberg/blob/3c3df877bd654c92a8898d363c047e06934dc276/packages/components/src/navigator/navigator/README.md#the-navigator-object) in the main README for docs about the `useNavigator` hook (plus, I've added better JSDocs in https://github.com/WordPress/gutenberg/pull/64613/commits/567f41879cd72147f8db95be614c7fafeddf2d6b)

### Review thread 16

#### tyxla on 2024-09-26T16:11:36Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777390024
Path: docs/manifest.json

```diff
@@ -1109,22 +1109,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
```

I see we continue calling these for example `NavigatorButton`, but they're actually exposed as `Navigator.Button`? Should this be reflected in the manifest? Should we just leave the single `Navigator` and have all READMEs in it?

#### ciampo on 2024-09-26T16:40:36Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777429724
Path: docs/manifest.json

```diff
@@ -1109,22 +1109,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
```

I think the name is auto-generated from the filename or something else. I like the idea of moving everything under one README, though. We currently have some compound components having one unique README, and other compound components with one README per sub-component. It would be good if we agreed on an approach going forward — @WordPress/gutenberg-components , any preferences?

#### ciampo on 2024-09-26T18:36:51Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777574522
Path: docs/manifest.json

```diff
@@ -1109,22 +1109,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
```

I merged all READMEs into one: https://github.com/WordPress/gutenberg/pull/64613/commits/5e33f26ba86f2308b331a0b7af60feb931836727, https://github.com/WordPress/gutenberg/pull/64613/commits/0cff1065813626fde5a2e14f444e69dbe39c6bfd and https://github.com/WordPress/gutenberg/pull/64613/commits/cea2b8e66c939f20fbdaa931ec501c7991622cf7.

I think I like keeping one README more, but happy to hear other's thoughts before merging the PR.

#### mirka on 2024-09-26T23:21:44Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777837686
Path: docs/manifest.json

```diff
@@ -1109,22 +1109,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
```

> I think I like keeping one README more

No strong opinion. I might have an updated opinion after I attempt auto-generating the READMEs 🙈

#### tyxla on 2024-09-27T08:27:43Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778235775
Path: docs/manifest.json

```diff
@@ -1109,22 +1109,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
```

I prefer a single README too - primarily because it's a better developer experience. It's way easier to understand the entire component family, rather than having to jump between multiple READMEs and figure out the puzzle on your own.

#### ciampo on 2024-09-27T08:49:41Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778268914
Path: docs/manifest.json

```diff
@@ -1109,22 +1109,16 @@
 		"markdown_source": "../packages/components/src/navigator/navigator-button/README.md",
```

Sounds good — I guess we can go with the one README for now and tweak later if necessary, together with the rest of the components that will need similar changes.

### Review thread 17 (resolved)

#### tyxla on 2024-09-26T16:12:24Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777390966
Path: packages/components/src/index.ts

```diff
@@ -132,6 +132,7 @@ export {
 	NavigatorToParentButton as __experimentalNavigatorToParentButton,
 	useNavigator as __experimentalUseNavigator,
```

Are we planning to deprecate the legacy ones at some point? Perhaps in a separate PR after we update the existing usages?

#### ciampo on 2024-09-26T16:36:51Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777425027
Path: packages/components/src/index.ts

```diff
@@ -132,6 +132,7 @@ export {
 	NavigatorToParentButton as __experimentalNavigatorToParentButton,
 	useNavigator as __experimentalUseNavigator,
```

Yup! From the "Next steps" section in the PR description:

> - refactor existing usages in Gutenberg to stable version
> - deprecate experimental version (both in JSDocs and with a runtime warning) (+ add unit tests for deprecation warnings)

#### tyxla on 2024-09-26T16:37:56Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777426337
Path: packages/components/src/index.ts

```diff
@@ -132,6 +132,7 @@ export {
 	NavigatorToParentButton as __experimentalNavigatorToParentButton,
 	useNavigator as __experimentalUseNavigator,
```

Thanks for confirming 😉 It's a long-going PR, so it never hurts to confirm.

### Review thread 18 (resolved)

#### tyxla on 2024-09-26T16:30:35Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777417380
Path: packages/components/src/navigator/navigator-back-button/component.tsx

```diff
@@ -23,5 +23,5 @@ function UnconnectedNavigatorBackButton(
 
 export const NavigatorBackButton = contextConnect(
 	UnconnectedNavigatorBackButton,
-	'NavigatorBackButton'
+	'Navigator.BackButton'
```

Double-checking if this is OK - I found it unexpected that here we use the `Navigator.BackButton` context name, but in the button hook, we use `NavigatorBackButton` without the dot notation:

https://github.com/WordPress/gutenberg/blob/fdff00c9b442dd4263da62a7dd8a3f9fe617b3ad/packages/components/src/navigator/navigator-back-button/hook.ts#L23

Same for the rest of the subcomponents and their context.

#### ciampo on 2024-09-26T16:37:56Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777426341
Path: packages/components/src/navigator/navigator-back-button/component.tsx

```diff
@@ -23,5 +23,5 @@ function UnconnectedNavigatorBackButton(
 
 export const NavigatorBackButton = contextConnect(
 	UnconnectedNavigatorBackButton,
-	'NavigatorBackButton'
+	'Navigator.BackButton'
```

Nope, that's a bug — well spotted! I'm going to push an update

#### ciampo on 2024-09-26T18:00:46Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1777531584
Path: packages/components/src/navigator/navigator-back-button/component.tsx

```diff
@@ -23,5 +23,5 @@ function UnconnectedNavigatorBackButton(
 
 export const NavigatorBackButton = contextConnect(
 	UnconnectedNavigatorBackButton,
-	'NavigatorBackButton'
+	'Navigator.BackButton'
```

Fixed in https://github.com/WordPress/gutenberg/pull/64613/commits/d153bcf0db769fb57c960c90301e09a2ba24c6c2

### Review thread 19 (resolved)

#### tyxla on 2024-09-27T12:24:06Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778533547
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
+
+## Usage
+
+```jsx
+import { Navigator } from '@wordpress/components';
+
+const MyNavigation = () => (
+	<Navigator initialPath="/">
+		<Navigator.Screen path="/">
+			<p>This is the home screen.</p>
+			<Navigator.Button path="/child">
+				Navigate to child screen.
+			</Navigator.Button>
+		</Navigator.Screen>
+		<Navigator.Screen path="/child">
+			<p>This is the child screen.</p>
+			<Navigator.BackButton>Go back</Navigator.BackButton>
+		</Navigator.Screen>
+	</Navigator>
+);
+```
+
+### Hierarchical `path`s
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
+
+For example:
+
+-   `/` is the root of all paths. There should always be a screen with `path="/"`;
+-   `/parent/child` is a child of `/parent`;
+-   `/parent/child/grand-child` is a child of `/parent/child`;
+-   `/parent/:param` is a child of `/parent` as well;
+-   if the current screen has a `path="/parent/child/grand-child"`, when going "back" `Navigator` will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
+
+### Height and animations
+
+Due to how `Navigator.Screen` animations work, it is recommended that the `Navigator` component is assigned a `height` to prevent some potential UI jumps while moving across screens.
+
+### Individual components
+
+`Navigator` is comprised of four individual components:
+
+-   `Navigator`: a wrapper component and context provider. It holds the main logic for hiding and showing screens.
+-   `Navigator.Screen`: represents a single view/screen/panel;
+-   `Navigator.Button`: renders a button that allows navigating to a different `Navigator.Screen`;
+-   `Navigator.BackButton`: renders a button that allows navigating to the parent `Navigator.Screen` (see the section above about hierarchical paths).
+
+For advanced usages, consumers can use the `useNavigator` hook.
+
+#### `Navigator`
+
+##### Props
+
+###### `initialPath`: `string`
+
+The initial active path.
+
+-   Required: Yes
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: Yes
+
+#### `Navigator.Screen`
+
+###### `path`: `string`
+
+The screen&quot;s path, matched against the current path stored in the navigator.
```

```suggestion
The screen's path, matched against the current path stored in the navigator.
```

### Review thread 20 (resolved)

#### tyxla on 2024-09-27T12:24:21Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778533849
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
```

Didn't we remove these READMEs? 

```suggestion
`Navigator` is a collection of components that allow rendering nested views/panels/menus (via the `Navigator.Screen`) and navigate between them (via the `Navigator.Button` and `Navigator.BackButton` components).
```

#### ciampo on 2024-09-27T14:23:23Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778710888
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
```

🤦

### Review thread 21 (resolved)

#### tyxla on 2024-09-27T12:37:32Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778550183
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
+
+## Usage
+
+```jsx
+import { Navigator } from '@wordpress/components';
+
+const MyNavigation = () => (
+	<Navigator initialPath="/">
+		<Navigator.Screen path="/">
+			<p>This is the home screen.</p>
+			<Navigator.Button path="/child">
+				Navigate to child screen.
+			</Navigator.Button>
+		</Navigator.Screen>
+		<Navigator.Screen path="/child">
+			<p>This is the child screen.</p>
+			<Navigator.BackButton>Go back</Navigator.BackButton>
+		</Navigator.Screen>
+	</Navigator>
+);
+```
+
+### Hierarchical `path`s
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
+
+For example:
+
+-   `/` is the root of all paths. There should always be a screen with `path="/"`;
+-   `/parent/child` is a child of `/parent`;
+-   `/parent/child/grand-child` is a child of `/parent/child`;
+-   `/parent/:param` is a child of `/parent` as well;
+-   if the current screen has a `path="/parent/child/grand-child"`, when going "back" `Navigator` will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
+
+### Height and animations
+
+Due to how `Navigator.Screen` animations work, it is recommended that the `Navigator` component is assigned a `height` to prevent some potential UI jumps while moving across screens.
+
+### Individual components
+
+`Navigator` is comprised of four individual components:
+
+-   `Navigator`: a wrapper component and context provider. It holds the main logic for hiding and showing screens.
+-   `Navigator.Screen`: represents a single view/screen/panel;
+-   `Navigator.Button`: renders a button that allows navigating to a different `Navigator.Screen`;
+-   `Navigator.BackButton`: renders a button that allows navigating to the parent `Navigator.Screen` (see the section above about hierarchical paths).
+
+For advanced usages, consumers can use the `useNavigator` hook.
+
+#### `Navigator`
+
+##### Props
+
+###### `initialPath`: `string`
+
+The initial active path.
+
+-   Required: Yes
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: Yes
+
+#### `Navigator.Screen`
+
+###### `path`: `string`
+
+The screen&quot;s path, matched against the current path stored in the navigator.
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
+
+For example:
+
+-   `/` is the root of all paths. There should always be a screen with `path="/"`.
+-   `/parent/child` is a child of `/parent`.
+-   `/parent/child/grand-child` is a child of `/parent/child`.
+-   `/parent/:param` is a child of `/parent` as well.
+-   if the current screen has a `path` with value `/parent/child/grand-child`, when going "back" `Navigator` will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
+
+-   Required: Yes
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: Yes
+
+##### `Navigator.Button`
+
+###### `path`: `string`
+
+The path of the screen to navigate to. The value of this prop needs to be [a valid value for an HTML attribute](https://html.spec.whatwg.org/multipage/syntax.html#attributes-2).
+
+-   Required: Yes
+
+###### `attributeName`: `string`
+
+The HTML attribute used to identify the `Navigator.Button`, which is used by `Navigator` to restore focus.
+
+-   Required: No
+-   Default: `id`
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: No
+
+###### Inherited props
+
+`Navigator.Button` also inherits all of the [`Button` props](/packages/components/src/button/README.md#props), except for `href` and `target`.
+
+##### `Navigator.BackButton`
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: No
+
+###### Inherited props
+
+`Navigator.BackButton` also inherits all of the [`Button` props](/packages/components/src/button/README.md#props), except for `href` and `target`.
+
+###### `useNavigator`
+
+You can retrieve a `navigator` instance by using the `useNavigator` hook.
+
+The `navigator` instance has a few properties:
+
+###### `goTo`: `( path: string, options: NavigateOptions ) => void`
+
+The `goTo` function allows navigating to a given path. The second argument can augment the navigation operations with different options.
+
+The available options are:
+
+-   `focusTargetSelector`: `string`. An optional property used to specify the CSS selector used to restore focus on the matching element when navigating back;
+-   `isBack`: `boolean`. An optional property used to specify whether the navigation should be considered as backwards (thus enabling focus restoration when possible, and causing the animation to be backwards too);
+-   `skipFocus`: `boolean`. An optional property used to opt out of `Navigator`'s focus management, useful when the consumer of the component wants to manage focus themselves;
+
+###### `goBack`: `( path: string, options: NavigateOptions ) => void`
+
+The `goBack` function allows navigating to the parent screen. Parent/child navigation only works if the paths you define are hierarchical (see note above).
+
+When a match is not found, the function will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) are found.
```

```suggestion
When a match is not found, the function will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
```

### Review thread 22 (resolved)

#### tyxla on 2024-09-27T12:38:31Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778551390
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
+
+## Usage
+
+```jsx
+import { Navigator } from '@wordpress/components';
+
+const MyNavigation = () => (
+	<Navigator initialPath="/">
+		<Navigator.Screen path="/">
+			<p>This is the home screen.</p>
+			<Navigator.Button path="/child">
+				Navigate to child screen.
+			</Navigator.Button>
+		</Navigator.Screen>
+		<Navigator.Screen path="/child">
+			<p>This is the child screen.</p>
+			<Navigator.BackButton>Go back</Navigator.BackButton>
+		</Navigator.Screen>
+	</Navigator>
+);
+```
+
+### Hierarchical `path`s
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
+
+For example:
+
+-   `/` is the root of all paths. There should always be a screen with `path="/"`;
+-   `/parent/child` is a child of `/parent`;
+-   `/parent/child/grand-child` is a child of `/parent/child`;
+-   `/parent/:param` is a child of `/parent` as well;
+-   if the current screen has a `path="/parent/child/grand-child"`, when going "back" `Navigator` will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
+
+### Height and animations
+
+Due to how `Navigator.Screen` animations work, it is recommended that the `Navigator` component is assigned a `height` to prevent some potential UI jumps while moving across screens.
+
+### Individual components
+
+`Navigator` is comprised of four individual components:
+
+-   `Navigator`: a wrapper component and context provider. It holds the main logic for hiding and showing screens.
+-   `Navigator.Screen`: represents a single view/screen/panel;
+-   `Navigator.Button`: renders a button that allows navigating to a different `Navigator.Screen`;
+-   `Navigator.BackButton`: renders a button that allows navigating to the parent `Navigator.Screen` (see the section above about hierarchical paths).
+
+For advanced usages, consumers can use the `useNavigator` hook.
+
+#### `Navigator`
+
+##### Props
+
+###### `initialPath`: `string`
+
+The initial active path.
+
+-   Required: Yes
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: Yes
+
+#### `Navigator.Screen`
+
+###### `path`: `string`
+
+The screen&quot;s path, matched against the current path stored in the navigator.
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
+
+For example:
+
+-   `/` is the root of all paths. There should always be a screen with `path="/"`.
+-   `/parent/child` is a child of `/parent`.
+-   `/parent/child/grand-child` is a child of `/parent/child`.
+-   `/parent/:param` is a child of `/parent` as well.
+-   if the current screen has a `path` with value `/parent/child/grand-child`, when going "back" `Navigator` will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
+
+-   Required: Yes
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: Yes
+
+##### `Navigator.Button`
+
+###### `path`: `string`
+
+The path of the screen to navigate to. The value of this prop needs to be [a valid value for an HTML attribute](https://html.spec.whatwg.org/multipage/syntax.html#attributes-2).
+
+-   Required: Yes
+
+###### `attributeName`: `string`
+
+The HTML attribute used to identify the `Navigator.Button`, which is used by `Navigator` to restore focus.
+
+-   Required: No
+-   Default: `id`
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: No
+
+###### Inherited props
+
+`Navigator.Button` also inherits all of the [`Button` props](/packages/components/src/button/README.md#props), except for `href` and `target`.
+
+##### `Navigator.BackButton`
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: No
+
+###### Inherited props
+
+`Navigator.BackButton` also inherits all of the [`Button` props](/packages/components/src/button/README.md#props), except for `href` and `target`.
+
+###### `useNavigator`
+
+You can retrieve a `navigator` instance by using the `useNavigator` hook.
+
+The `navigator` instance has a few properties:
+
+###### `goTo`: `( path: string, options: NavigateOptions ) => void`
+
+The `goTo` function allows navigating to a given path. The second argument can augment the navigation operations with different options.
+
+The available options are:
+
+-   `focusTargetSelector`: `string`. An optional property used to specify the CSS selector used to restore focus on the matching element when navigating back;
+-   `isBack`: `boolean`. An optional property used to specify whether the navigation should be considered as backwards (thus enabling focus restoration when possible, and causing the animation to be backwards too);
+-   `skipFocus`: `boolean`. An optional property used to opt out of `Navigator`'s focus management, useful when the consumer of the component wants to manage focus themselves;
+
+###### `goBack`: `( path: string, options: NavigateOptions ) => void`
+
+The `goBack` function allows navigating to the parent screen. Parent/child navigation only works if the paths you define are hierarchical (see note above).
+
+When a match is not found, the function will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) are found.
+
+The available options are the same as for the `goTo` method, except for the `isBack` property, which is not available for the `goBack` method.
+
+###### `location`: `NavigatorLocation`
+
+The `location` object represent the current location, and has a few properties:
```

```suggestion
The `location` object represents the current location, and has a few properties:
```

### Review thread 23 (resolved)

#### tyxla on 2024-09-27T13:08:48Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778601501
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
+
+## Usage
+
+```jsx
+import { Navigator } from '@wordpress/components';
+
+const MyNavigation = () => (
+	<Navigator initialPath="/">
+		<Navigator.Screen path="/">
+			<p>This is the home screen.</p>
+			<Navigator.Button path="/child">
+				Navigate to child screen.
+			</Navigator.Button>
+		</Navigator.Screen>
+		<Navigator.Screen path="/child">
+			<p>This is the child screen.</p>
+			<Navigator.BackButton>Go back</Navigator.BackButton>
+		</Navigator.Screen>
+	</Navigator>
+);
+```
+
+### Hierarchical `path`s
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
```

```suggestion
`Navigator` will treat "back" navigations as going to the parent screen — it is, therefore, the responsibility of the consumer of the component to create the correct screen hierarchy.
```

### Review thread 24 (resolved)

#### tyxla on 2024-09-27T13:09:55Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778603348
Path: packages/components/src/navigator/README.md

```diff
@@ -0,0 +1,168 @@
+# `Navigator`
+
+`Navigator` is a collection components that allow rendering nested views/panels/menus (via the [`Navigator.Screen` component](/packages/components/src/navigator/navigator-screen/README.md)) and navigate between them (via the [`Navigator.Button`](/packages/components/src/navigator/navigator-button/README.md) and [`Navigator.BackButton`](/packages/components/src/navigator/navigator-back-button/README.md) components).
+
+## Usage
+
+```jsx
+import { Navigator } from '@wordpress/components';
+
+const MyNavigation = () => (
+	<Navigator initialPath="/">
+		<Navigator.Screen path="/">
+			<p>This is the home screen.</p>
+			<Navigator.Button path="/child">
+				Navigate to child screen.
+			</Navigator.Button>
+		</Navigator.Screen>
+		<Navigator.Screen path="/child">
+			<p>This is the child screen.</p>
+			<Navigator.BackButton>Go back</Navigator.BackButton>
+		</Navigator.Screen>
+	</Navigator>
+);
+```
+
+### Hierarchical `path`s
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
+
+For example:
+
+-   `/` is the root of all paths. There should always be a screen with `path="/"`;
+-   `/parent/child` is a child of `/parent`;
+-   `/parent/child/grand-child` is a child of `/parent/child`;
+-   `/parent/:param` is a child of `/parent` as well;
+-   if the current screen has a `path="/parent/child/grand-child"`, when going "back" `Navigator` will try to recursively navigate the path hierarchy until a matching screen (or the root `/`) is found.
+
+### Height and animations
+
+Due to how `Navigator.Screen` animations work, it is recommended that the `Navigator` component is assigned a `height` to prevent some potential UI jumps while moving across screens.
+
+### Individual components
+
+`Navigator` is comprised of four individual components:
+
+-   `Navigator`: a wrapper component and context provider. It holds the main logic for hiding and showing screens.
+-   `Navigator.Screen`: represents a single view/screen/panel;
+-   `Navigator.Button`: renders a button that allows navigating to a different `Navigator.Screen`;
+-   `Navigator.BackButton`: renders a button that allows navigating to the parent `Navigator.Screen` (see the section above about hierarchical paths).
+
+For advanced usages, consumers can use the `useNavigator` hook.
+
+#### `Navigator`
+
+##### Props
+
+###### `initialPath`: `string`
+
+The initial active path.
+
+-   Required: Yes
+
+###### `children`: `string`
+
+The children elements.
+
+-   Required: Yes
+
+#### `Navigator.Screen`
+
+###### `path`: `string`
+
+The screen&quot;s path, matched against the current path stored in the navigator.
+
+`Navigator` assumes that screens are organized hierarchically according to their `path`, which should follow a URL-like scheme where each path segment starts with and is separated by the `/` character.
+
+`Navigator` will treat "back" navigations as going to the parent screen — it is therefore responsibility of the consumer of the component to create the correct screen hierarchy.
```

```suggestion
`Navigator` will treat "back" navigations as going to the parent screen — it is, therefore, the responsibility of the consumer of the component to create the correct screen hierarchy.
```

### Review thread 25 (resolved)

#### tyxla on 2024-09-27T13:15:10Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778612179
Path: packages/components/src/navigator/test/index.tsx

```diff
@@ -286,30 +286,30 @@ const MyNavigation = ( {
 						} }
 						value={ innerInputValue }
 					/>
-				</NavigatorScreen>
+				</Navigator.Screen>
 
-				<NavigatorScreen path={ PATHS.NESTED }>
+				<Navigator.Screen path={ PATHS.NESTED }>
 					<p>{ SCREEN_TEXT.nested }</p>
 					<CustomNavigatorBackButton
 						onClick={ onNavigatorButtonClick }
 					>
 						{ BUTTON_TEXT.back }
 					</CustomNavigatorBackButton>
-				</NavigatorScreen>
+				</Navigator.Screen>
 
 				<ProductScreen onBackButtonClick={ onNavigatorButtonClick } />
 
-				<NavigatorScreen path={ PATHS.INVALID_HTML_ATTRIBUTE }>
+				<Navigator.Screen path={ PATHS.INVALID_HTML_ATTRIBUTE }>
 					<p>{ SCREEN_TEXT.invalidHtmlPath }</p>
 					<CustomNavigatorBackButton
 						onClick={ onNavigatorButtonClick }
 					>
 						{ BUTTON_TEXT.back }
 					</CustomNavigatorBackButton>
-				</NavigatorScreen>
+				</Navigator.Screen>
 
 				{ /* A `NavigatorScreen` with `path={ PATHS.NOT_FOUND }` is purposefully not included. */ }
```

```suggestion
				{ /* A `Navigator.Screen` with `path={ PATHS.NOT_FOUND }` is purposefully not included. */ }
```

### Review thread 26 (resolved)

#### tyxla on 2024-09-27T13:18:35Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778619455
Path: packages/components/src/navigator/stories/index.story.tsx

```diff
@@ -40,7 +40,7 @@ const meta: Meta< typeof NavigatorProvider > = {
 						 * detail of the Navigator component. Do not use outside of
 						 * its source code.
 						 */
-						[data-wp-component="NavigatorProvider"] {
+						[data-wp-component="Navigator"] {
 							height: 250px;
 						}
 						[data-wp-component="NavigatorScreen"]:not([data-sticky]) {
```

I think we need to change this one too:

```suggestion
						[data-wp-component="Navigator.Screen"]:not([data-sticky]) {
```

#### ciampo on 2024-09-27T14:23:03Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778710379
Path: packages/components/src/navigator/stories/index.story.tsx

```diff
@@ -40,7 +40,7 @@ const meta: Meta< typeof NavigatorProvider > = {
 						 * detail of the Navigator component. Do not use outside of
 						 * its source code.
 						 */
-						[data-wp-component="NavigatorProvider"] {
+						[data-wp-component="Navigator"] {
 							height: 250px;
 						}
 						[data-wp-component="NavigatorScreen"]:not([data-sticky]) {
```

The current version is correct — we're setting the height on the wrapper, as also suggested in the README. Each screen, if the wrapper's height is set, will follow that height thanks to the grid layout.

#### tyxla on 2024-09-27T14:24:43Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778712694
Path: packages/components/src/navigator/stories/index.story.tsx

```diff
@@ -40,7 +40,7 @@ const meta: Meta< typeof NavigatorProvider > = {
 						 * detail of the Navigator component. Do not use outside of
 						 * its source code.
 						 */
-						[data-wp-component="NavigatorProvider"] {
+						[data-wp-component="Navigator"] {
 							height: 250px;
 						}
 						[data-wp-component="NavigatorScreen"]:not([data-sticky]) {
```

I think you're looking at the wrong place - this is about the padding, and in my testing the padding appears only if we use the dot notation.

#### tyxla on 2024-09-27T14:29:42Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778719589
Path: packages/components/src/navigator/stories/index.story.tsx

```diff
@@ -40,7 +40,7 @@ const meta: Meta< typeof NavigatorProvider > = {
 						 * detail of the Navigator component. Do not use outside of
 						 * its source code.
 						 */
-						[data-wp-component="NavigatorProvider"] {
+						[data-wp-component="Navigator"] {
 							height: 250px;
 						}
 						[data-wp-component="NavigatorScreen"]:not([data-sticky]) {
```

Alternatively, feel free to remove this style if you think we don't need it. But my point was that if you want the padding, you need to add the dot.

#### ciampo on 2024-09-27T14:30:58Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778721447
Path: packages/components/src/navigator/stories/index.story.tsx

```diff
@@ -40,7 +40,7 @@ const meta: Meta< typeof NavigatorProvider > = {
 						 * detail of the Navigator component. Do not use outside of
 						 * its source code.
 						 */
-						[data-wp-component="NavigatorProvider"] {
+						[data-wp-component="Navigator"] {
 							height: 250px;
 						}
 						[data-wp-component="NavigatorScreen"]:not([data-sticky]) {
```

You're right — I looked at the wrong diff 😅 

I pushed a fix as you suggested.

### Review thread 27 (resolved)

#### tyxla on 2024-09-27T13:21:45Z

URL: https://github.com/WordPress/gutenberg/pull/64613#discussion_r1778623718
Path: packages/components/src/navigator/types.ts

```diff
@@ -100,6 +100,24 @@ export type NavigatorProps = {
 export type NavigatorScreenProps = {
 	/**
 	 * The screen's path, matched against the current path stored in the navigator.
+	 *
+	 * `Navigator` assumes that screens are organized hierarchically according
+	 * to their `path`, which should follow a URL-like scheme where each path
+	 * segment starts with and is separated by the `/` character.
+	 *
+	 * `Navigator` will treat "back" navigations as going to the parent screen —
+	 * it is therefore responsibility of the consumer of the component to create
+	 * the correct screen hierarchy.
```

```suggestion
	 * `Navigator` will treat "back" navigations as going to the parent screen —
	 * it is, therefore, the responsibility of the consumer of the component to
	 * create the correct screen hierarchy.
```

## Changed files

- docs/manifest.json (MODIFIED, +3/-27)
- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/index.ts (MODIFIED, +1/-0)
- packages/components/src/navigator/README.md (ADDED, +168/-0)
- packages/components/src/navigator/index.tsx (ADDED, +131/-0)
- packages/components/src/navigator/legacy.ts (MODIFIED, +6/-9)
- packages/components/src/navigator/navigator-back-button/README.md (DELETED, +0/-15)
- packages/components/src/navigator/navigator-back-button/component.tsx (MODIFIED, +1/-1)
- packages/components/src/navigator/navigator-back-button/hook.ts (MODIFIED, +1/-1)
- packages/components/src/navigator/navigator-button/README.md (DELETED, +0/-38)
- packages/components/src/navigator/navigator-button/component.tsx (MODIFIED, +1/-1)
- packages/components/src/navigator/navigator-button/hook.ts (MODIFIED, +1/-1)
- packages/components/src/navigator/navigator-provider/README.md (DELETED, +0/-98)
- packages/components/src/navigator/navigator-screen/README.md (DELETED, +0/-33)
- packages/components/src/navigator/navigator-screen/component.tsx (MODIFIED, +3/-3)
- packages/components/src/navigator/navigator-to-parent-button/README.md (DELETED, +0/-17)
- packages/components/src/navigator/navigator-to-parent-button/component.tsx (MODIFIED, +5/-2)
- packages/components/src/navigator/navigator/component.tsx (RENAMED, +3/-41)
- packages/components/src/navigator/stories/index.story.tsx (MODIFIED, +49/-49)
- packages/components/src/navigator/styles.ts (MODIFIED, +1/-1)
- packages/components/src/navigator/test/index.tsx (MODIFIED, +47/-47)
- packages/components/src/navigator/types.ts (MODIFIED, +18/-0)
- packages/components/src/navigator/use-navigator.ts (MODIFIED, +4/-1)
- storybook/manager-head.html (MODIFIED, +1/-0)
