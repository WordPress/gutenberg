---
number: 12629
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/12629
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Focus] Accessibility (a11y)","Needs Design Feedback","[Status] Stale","[Package] Components"]
---

# PR #12629: Improve the flat terms sidebar panel's user information

- URL: https://github.com/WordPress/gutenberg/pull/12629
- Author: imath
- Created: 2018-12-06T02:28:45Z
- Updated: 2019-04-05T20:46:40Z
- Comments: 5 of 5
- Review threads: 5 of 5
- Changed files: 3 of 3
- Diff: ../pr-diffs/12629.diff

## Body

It's not obvious for a user a comma needs to be used to separate tags.
Using the `separate_items_with_commas` label to display it under the tags input should help him to understand how he can do it.

## Description
I've added a paragraph under the flat terms input field displaying the `separate_items_with_commas` label of the taxonomy object.

## How has this been tested?
Tested using VVV, WordPress 4.9.8, Gutenberg master.

## Screenshots
![separate-tags](https://user-images.githubusercontent.com/1834524/49557089-c977ed00-f906-11e8-8e0d-83e2af987838.png)

The "how to" paragraph.

## Types of changes
Bug fix (for new users it can be challenging)

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->

## Issue comments

### afercia on 2019-01-14T18:23:11Z

URL: https://github.com/WordPress/gutenberg/pull/12629#issuecomment-454108084

> Removing the classname makes it visible. I'm not sure why that decision was made over having something visible, it might be nice to find out.

Because designers didn't want it visible 🙂Personally, I'm all for making it visible. At this point I'd make also the default help visible and I guess code could be simplified to just use something like:

`help || __( 'Separate with commas' )`

### gziolo on 2019-02-06T11:43:59Z

URL: https://github.com/WordPress/gutenberg/pull/12629#issuecomment-460994485

@imath do you plan to revisit this PR? It wasn't updated for almost 2 months. I'm doing triage of all open PRs and therefore will mark it as `Stale` to indicate that it doesn't need to be reviewed until it's refreshed.

### imath on 2019-02-06T12:35:30Z

URL: https://github.com/WordPress/gutenberg/pull/12629#issuecomment-461007866

@gziolo Thanks a lot for the ping. I'll revisit the PR but not so soon. I'll ping you or @talldan when it will be refreshed. (sorry for not being too much available at the moment).

### gziolo on 2019-02-06T12:41:24Z

URL: https://github.com/WordPress/gutenberg/pull/12629#issuecomment-461009412

> I'll ping you or @talldan when it will be refreshed. (sorry for not being too much available at the moment).

That's totally fine, thanks for letting us know and your contribution so far 👍

### mapk on 2019-04-05T20:46:40Z

URL: https://github.com/WordPress/gutenberg/pull/12629#issuecomment-480416639

@imath I'm closing this PR for now because it looks like there's been over 2 months of no progress. If you choose to open it back up again, we can.

## Review threads

### Review thread 1

#### talldan on 2018-12-12T04:56:01Z

URL: https://github.com/WordPress/gutenberg/pull/12629#discussion_r240882324
Path: packages/editor/src/components/post-taxonomies/flat-term-selector.js

```diff
@@ -185,23 +185,33 @@ class FlatTermSelector extends Component {
 		const termAddedLabel = sprintf( _x( '%s added', 'term' ), singularName );
 		const termRemovedLabel = sprintf( _x( '%s removed', 'term' ), singularName );
 		const removeTermLabel = sprintf( _x( 'Remove %s', 'term' ), singularName );
+		const howTo = get(
+			taxonomy,
+			[ 'labels', 'separate_items_with_commas' ],
+			_x( 'Separate terms with commas', 'how to separate term items' )
```

`FormTokenField` already seems to have similar text, but it's hidden just for screen reader use:
https://github.com/WordPress/gutenberg/blob/0fba129ab9dd7755d4e94a275f738657470fc170/packages/components/src/form-token-field/index.js#L586-L588

Removing the classname makes it visible. I'm not sure why that decision was made over having something visible, it might be nice to find out.

A way to move forwards with this might be to allow FormTokenField to have a `help` prop so that implementors can specify their own help message.

Naming the prop `help` would make this consistent with other controls that are implemented using BaseControl (it's a shame FormTokenField wasn't written using BaseControl as it would make this much easier!):
https://github.com/WordPress/gutenberg/blob/0fba129ab9dd7755d4e94a275f738657470fc170/packages/components/src/base-control/index.js#L14

#### imath on 2018-12-12T16:17:47Z

URL: https://github.com/WordPress/gutenberg/pull/12629#discussion_r241081952
Path: packages/editor/src/components/post-taxonomies/flat-term-selector.js

```diff
@@ -185,23 +185,33 @@ class FlatTermSelector extends Component {
 		const termAddedLabel = sprintf( _x( '%s added', 'term' ), singularName );
 		const termRemovedLabel = sprintf( _x( '%s removed', 'term' ), singularName );
 		const removeTermLabel = sprintf( _x( 'Remove %s', 'term' ), singularName );
+		const howTo = get(
+			taxonomy,
+			[ 'labels', 'separate_items_with_commas' ],
+			_x( 'Separate terms with commas', 'how to separate term items' )
```

Hi @talldan thanks a lot for your review and advices. I've just applyed them and updated the pull request.

### Review thread 2 (resolved)

#### talldan on 2018-12-12T04:59:07Z

URL: https://github.com/WordPress/gutenberg/pull/12629#discussion_r240882715
Path: packages/editor/src/components/post-taxonomies/flat-term-selector.js

```diff
@@ -185,23 +185,33 @@ class FlatTermSelector extends Component {
 		const termAddedLabel = sprintf( _x( '%s added', 'term' ), singularName );
 		const termRemovedLabel = sprintf( _x( '%s removed', 'term' ), singularName );
 		const removeTermLabel = sprintf( _x( 'Remove %s', 'term' ), singularName );
+		const howTo = get(
+			taxonomy,
+			[ 'labels', 'separate_items_with_commas' ],
+			_x( 'Separate terms with commas', 'how to separate term items' )
+		);
 
 		return (
-			<FormTokenField
-				value={ selectedTerms }
-				displayTransform={ unescapeString }
-				suggestions={ termNames }
-				onChange={ this.onChange }
-				onInputChange={ this.searchTerms }
-				maxSuggestions={ MAX_TERMS_SUGGESTIONS }
-				disabled={ loading }
-				label={ newTermLabel }
-				messages={ {
-					added: termAddedLabel,
-					removed: termRemovedLabel,
-					remove: removeTermLabel,
-				} }
-			/>
+			<Fragment>
+				<FormTokenField
+					value={ selectedTerms }
+					displayTransform={ unescapeString }
+					suggestions={ termNames }
+					onChange={ this.onChange }
+					onInputChange={ this.searchTerms }
+					maxSuggestions={ MAX_TERMS_SUGGESTIONS }
+					disabled={ loading }
+					label={ newTermLabel }
+					messages={ {
+						added: termAddedLabel,
+						removed: termRemovedLabel,
+						remove: removeTermLabel,
+					} }
+				/>
+				<p className="howto">
```

The styling of 'howto' doesn't match similar examples within Gutenberg (for example, see the Paragraph block's drop cap toggle, which has a help message underneath). The font color is very slightly different.

It should also use a BEM style classname (in the form of *block*__*element*), i.e. `components-form-token-field__help`.

### Review thread 3

#### talldan on 2018-12-13T09:18:31Z

URL: https://github.com/WordPress/gutenberg/pull/12629#discussion_r241322574
Path: packages/components/src/form-token-field/index.js

```diff
@@ -608,6 +617,7 @@ FormTokenField.defaultProps = {
 		removed: __( 'Item removed.' ),
 		remove: __( 'Remove item' ),
 	},
+	help: '',
```

nitpick: as far as I can see this default prop is not really necessary, since the body of the render function handles `help` being undefined.

### Review thread 4

#### talldan on 2018-12-13T09:20:30Z

URL: https://github.com/WordPress/gutenberg/pull/12629#discussion_r241323177
Path: packages/components/src/form-token-field/index.js

```diff
@@ -551,6 +552,14 @@ class FormTokenField extends Component {
 			} );
 		}
 
+		const howTo = !! help ? {
```

nitpick: The `!!` isn't required in a ternary, it's mostly useful for boolean `&&` or `||` operators where the value might end up being the result of the expression.

### Review thread 5

#### talldan on 2018-12-13T09:22:41Z

URL: https://github.com/WordPress/gutenberg/pull/12629#discussion_r241323842
Path: packages/components/src/form-token-field/index.js

```diff
@@ -530,6 +530,7 @@ class FormTokenField extends Component {
 			label = __( 'Add item' ),
 			instanceId,
 			className,
+			help,
```

The README.md  for FormTokenField should be updated for this new prop—sorry, could've mentioned that in the previous review! I think once that's done the changelog for the components package should also be updated.

## Changed files

- packages/components/src/form-token-field/index.js (MODIFIED, +12/-2)
- packages/components/src/form-token-field/style.scss (MODIFIED, +6/-0)
- packages/editor/src/components/post-taxonomies/flat-term-selector.js (MODIFIED, +6/-0)
