---
number: 46001
type: pull_request
state: MERGED
merged_at: 2022-11-26T11:45:27Z
url: https://github.com/WordPress/gutenberg/pull/46001
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #46001: `ColorPalette`: show `Clear` button even when `colors` array is empty

- URL: https://github.com/WordPress/gutenberg/pull/46001
- Author: ciampo
- Created: 2022-11-23T11:39:23Z
- Updated: 2022-11-26T11:45:50Z
- Comments: 2 of 2
- Review threads: 4 of 4
- Changed files: 5 of 5
- Diff: ../pr-diffs/46001.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Fixes #45928 by showing the `Clear` button in `ColorPalette` even when the array of `colors` is empty.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Fixing a regression introduced in #44632 

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Removed the check that was forcing the component not to render `CircularOptionPicker`, which was effectively the source of the regression
- Fixed an issue in the Storybook example, which was causing the example to crash when `colors=[]`
- Added a unit test to prevent this regression from happening again
- In the process, tidied up a little the unit tests file (including changing example colors to be less US-centric)

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a Post or Page. -->
<!-- 2. Insert a Heading Block. -->
<!-- 3. etc. -->

- Try to reproduce the bug flagged in #44632 — the bug should not be reproducible
- Make sure that new test makes sense

## Screenshots

| Before (trunk) | After (this PR) |
|---|---|
| <img width="547" alt="Screenshot 2022-11-23 at 12 41 09" src="https://user-images.githubusercontent.com/1083581/203538077-dd3a4902-7cb4-48fe-a07c-cc5a6cb7cec4.png"> | <img width="540" alt="Screenshot 2022-11-23 at 12 40 33" src="https://user-images.githubusercontent.com/1083581/203537951-18f6007b-bf14-4a6c-bf24-9254cbfeb660.png"> |

## Issue comments

### codesandbox on 2022-11-23T11:39:26Z

URL: https://github.com/WordPress/gutenberg/pull/46001#issuecomment-1324923581

<img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.light.svg#gh-dark-mode-only" /><img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.svg#gh-light-mode-only" />&nbsp; Open in CodeSandbox <a href="https://codesandbox.io/p/github/WordPress/gutenberg/fix/color-palette-clear-button?workspace=%7B%22gitSidebarPanel%22:%22PR%22,%22sidebarPanel%22:%22GIT%22%7D">Web Editor</a> | <a href="https://codesandbox.io/p/vscode?owner=WordPress&repo=gutenberg&branch=fix/color-palette-clear-button">VS Code</a> | <a href="https://codesandbox.io/p/vscode?owner=WordPress&repo=gutenberg&branch=fix/color-palette-clear-button&insiders=true">VS Code Insiders</a>

<!-- open-in-codesandbox:complete -->

### tyxla on 2022-11-23T12:42:53Z

URL: https://github.com/WordPress/gutenberg/pull/46001#issuecomment-1325001377

Note that the failing check is not related to this PR and I've filed a fix for it in #45999.

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2022-11-23T12:39:52Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030393346
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -153,12 +161,40 @@ describe( 'ColorPalette', () => {
 		expect( dropdownButton ).toBeVisible();
 
 		expect(
-			within( dropdownButton ).getByText( colors[ 0 ].name )
+			within( dropdownButton ).getByText( EXAMPLE_COLORS[ 0 ].name )
 		).toBeVisible();
 		expect(
 			within( dropdownButton ).getByText(
-				colors[ 0 ].color.replace( '#', '' )
+				EXAMPLE_COLORS[ 0 ].color.replace( '#', '' )
 			)
 		).toBeVisible();
 	} );
+
+	it( 'should show the clear button even when `colors` is an empty array', () => {
+		const onChange = jest.fn();
+
+		const { rerender } = render(
+			<ColorPalette
+				colors={ EXAMPLE_COLORS }
+				value={ INITIAL_COLOR }
+				onChange={ onChange }
+			/>
+		);
+
+		expect(
+			screen.getByRole( 'button', { name: 'Clear' } )
+		).toBeInTheDocument();
```

Is this part of the test relevant? Won't it be the same if we remove it and just render once with `colors: []`?

#### ciampo on 2022-11-23T14:26:18Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030509413
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -153,12 +161,40 @@ describe( 'ColorPalette', () => {
 		expect( dropdownButton ).toBeVisible();
 
 		expect(
-			within( dropdownButton ).getByText( colors[ 0 ].name )
+			within( dropdownButton ).getByText( EXAMPLE_COLORS[ 0 ].name )
 		).toBeVisible();
 		expect(
 			within( dropdownButton ).getByText(
-				colors[ 0 ].color.replace( '#', '' )
+				EXAMPLE_COLORS[ 0 ].color.replace( '#', '' )
 			)
 		).toBeVisible();
 	} );
+
+	it( 'should show the clear button even when `colors` is an empty array', () => {
+		const onChange = jest.fn();
+
+		const { rerender } = render(
+			<ColorPalette
+				colors={ EXAMPLE_COLORS }
+				value={ INITIAL_COLOR }
+				onChange={ onChange }
+			/>
+		);
+
+		expect(
+			screen.getByRole( 'button', { name: 'Clear' } )
+		).toBeInTheDocument();
```

I thought it was relevant because we never check for the `Clear` button to be visible by default — do you think it would be better to split the check to a separate test?

#### tyxla on 2022-11-23T14:27:40Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030511653
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -153,12 +161,40 @@ describe( 'ColorPalette', () => {
 		expect( dropdownButton ).toBeVisible();
 
 		expect(
-			within( dropdownButton ).getByText( colors[ 0 ].name )
+			within( dropdownButton ).getByText( EXAMPLE_COLORS[ 0 ].name )
 		).toBeVisible();
 		expect(
 			within( dropdownButton ).getByText(
-				colors[ 0 ].color.replace( '#', '' )
+				EXAMPLE_COLORS[ 0 ].color.replace( '#', '' )
 			)
 		).toBeVisible();
 	} );
+
+	it( 'should show the clear button even when `colors` is an empty array', () => {
+		const onChange = jest.fn();
+
+		const { rerender } = render(
+			<ColorPalette
+				colors={ EXAMPLE_COLORS }
+				value={ INITIAL_COLOR }
+				onChange={ onChange }
+			/>
+		);
+
+		expect(
+			screen.getByRole( 'button', { name: 'Clear' } )
+		).toBeInTheDocument();
```

Okay, that makes sense. And I agree a separate test would be better - it would help express the intentions better. We don't seem to earn anything by re-rendering and staying in the same test.

#### ciampo on 2022-11-23T14:34:38Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030519750
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -153,12 +161,40 @@ describe( 'ColorPalette', () => {
 		expect( dropdownButton ).toBeVisible();
 
 		expect(
-			within( dropdownButton ).getByText( colors[ 0 ].name )
+			within( dropdownButton ).getByText( EXAMPLE_COLORS[ 0 ].name )
 		).toBeVisible();
 		expect(
 			within( dropdownButton ).getByText(
-				colors[ 0 ].color.replace( '#', '' )
+				EXAMPLE_COLORS[ 0 ].color.replace( '#', '' )
 			)
 		).toBeVisible();
 	} );
+
+	it( 'should show the clear button even when `colors` is an empty array', () => {
+		const onChange = jest.fn();
+
+		const { rerender } = render(
+			<ColorPalette
+				colors={ EXAMPLE_COLORS }
+				value={ INITIAL_COLOR }
+				onChange={ onChange }
+			/>
+		);
+
+		expect(
+			screen.getByRole( 'button', { name: 'Clear' } )
+		).toBeInTheDocument();
```

Done in https://github.com/WordPress/gutenberg/pull/46001/commits/b6bae63553b3dac60b4e6fcd32fd718cea158511

### Review thread 2 (resolved)

#### tyxla on 2022-11-23T14:39:51Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030525701
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -153,12 +161,38 @@ describe( 'ColorPalette', () => {
 		expect( dropdownButton ).toBeVisible();
 
 		expect(
-			within( dropdownButton ).getByText( colors[ 0 ].name )
+			within( dropdownButton ).getByText( EXAMPLE_COLORS[ 0 ].name )
 		).toBeVisible();
 		expect(
 			within( dropdownButton ).getByText(
-				colors[ 0 ].color.replace( '#', '' )
+				EXAMPLE_COLORS[ 0 ].color.replace( '#', '' )
 			)
 		).toBeVisible();
 	} );
+
+	it( 'should show the clear button by default', () => {
+		const onChange = jest.fn();
+
+		render(
+			<ColorPalette
+				colors={ EXAMPLE_COLORS }
+				value={ INITIAL_COLOR }
+				onChange={ onChange }
+			/>
+		);
+
+		expect(
+			screen.getByRole( 'button', { name: 'Clear' } )
+		).toBeInTheDocument();
+	} );
+
+	it( 'should show the clear button even when `colors` is an empty array', () => {
+		const onChange = jest.fn();
+
+		render( <ColorPalette colors={ [] } onChange={ onChange } /> );
```

Do we even need the `onChange` if we render no color option pickers?

#### ciampo on 2022-11-23T14:52:59Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030540992
Path: packages/components/src/color-palette/test/index.tsx

```diff
@@ -153,12 +161,38 @@ describe( 'ColorPalette', () => {
 		expect( dropdownButton ).toBeVisible();
 
 		expect(
-			within( dropdownButton ).getByText( colors[ 0 ].name )
+			within( dropdownButton ).getByText( EXAMPLE_COLORS[ 0 ].name )
 		).toBeVisible();
 		expect(
 			within( dropdownButton ).getByText(
-				colors[ 0 ].color.replace( '#', '' )
+				EXAMPLE_COLORS[ 0 ].color.replace( '#', '' )
 			)
 		).toBeVisible();
 	} );
+
+	it( 'should show the clear button by default', () => {
+		const onChange = jest.fn();
+
+		render(
+			<ColorPalette
+				colors={ EXAMPLE_COLORS }
+				value={ INITIAL_COLOR }
+				onChange={ onChange }
+			/>
+		);
+
+		expect(
+			screen.getByRole( 'button', { name: 'Clear' } )
+		).toBeInTheDocument();
+	} );
+
+	it( 'should show the clear button even when `colors` is an empty array', () => {
+		const onChange = jest.fn();
+
+		render( <ColorPalette colors={ [] } onChange={ onChange } /> );
```

It's a required prop, so I'm afraid we do 🤷

### Review thread 3 (resolved)

#### mirka on 2022-11-23T16:33:47Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030660286
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -43,10 +43,20 @@ const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
 	...args
 } ) => {
-	const firstColor =
-		( args.colors as ColorObject[] )[ 0 ].color ||
-		( args.colors as PaletteObject[] )[ 0 ].colors[ 0 ].color;
-	const [ color, setColor ] = useState< string | undefined >( firstColor );
+	let initialColor;
+	if ( args.color?.length ) {
```

I assume we mean this?

```suggestion
	if ( args.colors?.length ) {
```

#### ciampo on 2022-11-25T23:12:55Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1032707665
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -43,10 +43,20 @@ const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
 	...args
 } ) => {
-	const firstColor =
-		( args.colors as ColorObject[] )[ 0 ].color ||
-		( args.colors as PaletteObject[] )[ 0 ].colors[ 0 ].color;
-	const [ color, setColor ] = useState< string | undefined >( firstColor );
+	let initialColor;
+	if ( args.color?.length ) {
```

Woops! Good spot

### Review thread 4

#### mirka on 2022-11-23T16:45:25Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1030677748
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -43,10 +43,20 @@ const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
 	...args
 } ) => {
-	const firstColor =
-		( args.colors as ColorObject[] )[ 0 ].color ||
-		( args.colors as PaletteObject[] )[ 0 ].colors[ 0 ].color;
-	const [ color, setColor ] = useState< string | undefined >( firstColor );
+	let initialColor;
+	if ( args.color?.length ) {
+		const asColorObject = ( args.colors as ColorObject[] )?.[ 0 ].color;
+		let asPaletteObject;
+
+		if ( ( args.colors as PaletteObject[] )[ 0 ].colors?.length ) {
+			asPaletteObject = ( args.colors as PaletteObject[] )[ 0 ]
+				.colors[ 0 ].color;
+		}
+
+		initialColor = asColorObject || asPaletteObject;
```

I personally find it more readable if we just added some question marks to the original code — what do you think?

```tsx
	const initialColor =
		( args.colors as ColorObject[] )?.[ 0 ]?.color ||
		( args.colors as PaletteObject[] )?.[ 0 ]?.colors[ 0 ].color;
```

FWIW I'm also fine with removing this "initial color" selection logic altogether. It's not part of the default component logic, and it's probably not a common use case either.

#### ciampo on 2022-11-25T23:13:06Z

URL: https://github.com/WordPress/gutenberg/pull/46001#discussion_r1032707697
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -43,10 +43,20 @@ const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
 	...args
 } ) => {
-	const firstColor =
-		( args.colors as ColorObject[] )[ 0 ].color ||
-		( args.colors as PaletteObject[] )[ 0 ].colors[ 0 ].color;
-	const [ color, setColor ] = useState< string | undefined >( firstColor );
+	let initialColor;
+	if ( args.color?.length ) {
+		const asColorObject = ( args.colors as ColorObject[] )?.[ 0 ].color;
+		let asPaletteObject;
+
+		if ( ( args.colors as PaletteObject[] )[ 0 ].colors?.length ) {
+			asPaletteObject = ( args.colors as PaletteObject[] )[ 0 ]
+				.colors[ 0 ].color;
+		}
+
+		initialColor = asColorObject || asPaletteObject;
```

I'd rather remove it altogether, to be honest — done in https://github.com/WordPress/gutenberg/pull/46001/commits/5be28fbc2e2686c193e1511f5aac588aa0b29e7d

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/color-palette/index.tsx (MODIFIED, +0/-4)
- packages/components/src/color-palette/stories/index.tsx (MODIFIED, +1/-5)
- packages/components/src/color-palette/test/__snapshots__/index.tsx.snap (MODIFIED, +4/-4)
- packages/components/src/color-palette/test/index.tsx (MODIFIED, +69/-35)
