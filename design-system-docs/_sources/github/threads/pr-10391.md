---
number: 10391
type: pull_request
state: MERGED
merged_at: 2018-10-18T02:11:36Z
url: https://github.com/WordPress/gutenberg/pull/10391
matched_labels: ["[Package] Components"]
all_labels: ["Framework","[Package] Components","[Package] Editor"]
---

# PR #10391: Deprecate PanelColor components

- URL: https://github.com/WordPress/gutenberg/pull/10391
- Author: chrisvanpatten
- Created: 2018-10-08T01:07:32Z
- Updated: 2018-10-18T15:05:06Z
- Comments: 5 of 5
- Review threads: 4 of 4
- Changed files: 6 of 6
- Diff: ../pr-diffs/10391.diff

## Body

Fixes #10367.

This PR deprecates `wp.components.PanelColor` and `wp.editor.PanelColor` in favour of `wp.editor.PanelColorSettings`, which is a more robust component for the reasons documented in #10367:

> Overall `PanelColorSettings` seems superior because of its support for multiple color values and for not having the clear button float issue.

It also updates the `@wordpress/editor` and `@wordpress/components` changelogs to mention the deprecation.

One note is that deprecating `wp.components.PanelColor` will mean there's no component to provide a color panel that isn't wrapped inside `withColorContext`. Users who wish to have a panel with a color palette that uses a separate set of colors than what's defined with `add_theme_support('editor-color-palette');` will need to build their own panel component.

As such, it may be desired to deprecate `wp.editor.PanelColor`, while _keeping_ `wp.components.PanelColor` and simply fixing the visual bug noted in #10367. However, this would be a use-case that Gutenberg itself does not need/use, so it might not be worth continuing to support.

I've never done a deprecation like this… I think I got everything but would very much appreciate a close look/review 😄

## Issue comments

### chrisvanpatten on 2018-10-08T13:24:28Z

URL: https://github.com/WordPress/gutenberg/pull/10391#issuecomment-427831310

Need a little direction on the failing unit test. Is there a way to tell the test to expect the deprecation warning?

### talldan on 2018-10-10T07:17:08Z

URL: https://github.com/WordPress/gutenberg/pull/10391#issuecomment-428464188

Thanks for picking this up @chrisvanpatten! 

I think to resolve the test failure it'll be something along the lines of adding `expect( console ).toHaveWarnedWith( // ... )`, as has been done here:
https://github.com/WordPress/gutenberg/blob/master/packages/block-library/src/text-columns/test/index.js#L12

### chrisvanpatten on 2018-10-10T14:28:13Z

URL: https://github.com/WordPress/gutenberg/pull/10391#issuecomment-428594059

This _should_ pass the failing test now, but I think is likely to miss the 4.0 milestone, so the deprecation version may need to be updated.

### chrisvanpatten on 2018-10-17T20:16:30Z

URL: https://github.com/WordPress/gutenberg/pull/10391#issuecomment-430772787

Updated the version numbers with the assumption it can land in 4.1.

### chrisvanpatten on 2018-10-18T02:12:02Z

URL: https://github.com/WordPress/gutenberg/pull/10391#issuecomment-430851044

Thanks @talldan! 🚢

## Review threads

### Review thread 1 (resolved)

#### jorgefilipecosta on 2018-10-09T11:21:43Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223659301
Path: packages/editor/src/components/panel-color/index.js

```diff
@@ -17,6 +18,12 @@ import withColorContext from '../color-palette/with-color-context';
 import { getColorObjectByColorValue } from '../colors';
 
 function PanelColor( { colors, title, colorValue, initialOpen, ...props } ) {
+	deprecated( 'wp.editor.PanelColor', {
```

Here we should have wp.components.PanelColor.

#### chrisvanpatten on 2018-10-09T22:54:34Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223890069
Path: packages/editor/src/components/panel-color/index.js

```diff
@@ -17,6 +18,12 @@ import withColorContext from '../color-palette/with-color-context';
 import { getColorObjectByColorValue } from '../colors';
 
 function PanelColor( { colors, title, colorValue, initialOpen, ...props } ) {
+	deprecated( 'wp.editor.PanelColor', {
```

Fixed!

### Review thread 2 (resolved)

#### jorgefilipecosta on 2018-10-09T11:25:12Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223660276
Path: packages/editor/src/components/panel-color/index.js

```diff
@@ -17,6 +18,12 @@ import withColorContext from '../color-palette/with-color-context';
 import { getColorObjectByColorValue } from '../colors';
 
 function PanelColor( { colors, title, colorValue, initialOpen, ...props } ) {
+	deprecated( 'wp.editor.PanelColor', {
+		version: '4.2',
+		alternative: 'wp.editor.PanelColorSettings',
```

Unfortunately wp.editor.PanelColorSettings is not a viable alternative to wp.components.PanelColor. wp.components.PanelColor allows a block to specify the colors that appear instead of using the default or theme specified ones which is useful for some blocks that only make sense with some colors or blocks that should expand the color options, e.g., a drawing block.

#### chrisvanpatten on 2018-10-09T12:30:07Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223678838
Path: packages/editor/src/components/panel-color/index.js

```diff
@@ -17,6 +18,12 @@ import withColorContext from '../color-palette/with-color-context';
 import { getColorObjectByColorValue } from '../colors';
 
 function PanelColor( { colors, title, colorValue, initialOpen, ...props } ) {
+	deprecated( 'wp.editor.PanelColor', {
+		version: '4.2',
+		alternative: 'wp.editor.PanelColorSettings',
```

Right, I noted that we could have an alternate option in the PR:

> One note is that deprecating `wp.components.PanelColor` will mean there's no component to provide a color panel that isn't wrapped inside `withColorContext`. As such, it may be desired to deprecate `wp.editor.PanelColor`, while _keeping_ `wp.components.PanelColor` and simply fixing the visual bug noted in #10367.

Happy to tackle that in this PR too, if you'd like @jorgefilipecosta :)

#### chrisvanpatten on 2018-10-09T12:32:30Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223679529
Path: packages/editor/src/components/panel-color/index.js

```diff
@@ -17,6 +18,12 @@ import withColorContext from '../color-palette/with-color-context';
 import { getColorObjectByColorValue } from '../colors';
 
 function PanelColor( { colors, title, colorValue, initialOpen, ...props } ) {
+	deprecated( 'wp.editor.PanelColor', {
+		version: '4.2',
+		alternative: 'wp.editor.PanelColorSettings',
```

Also just to clarify, it makes sense to me to keep the version in `components` and get rid of the one in `editor` but is there any reason to do the reverse instead?

#### chrisvanpatten on 2018-10-09T22:54:19Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223890018
Path: packages/editor/src/components/panel-color/index.js

```diff
@@ -17,6 +18,12 @@ import withColorContext from '../color-palette/with-color-context';
 import { getColorObjectByColorValue } from '../colors';
 
 function PanelColor( { colors, title, colorValue, initialOpen, ...props } ) {
+	deprecated( 'wp.editor.PanelColor', {
+		version: '4.2',
+		alternative: 'wp.editor.PanelColorSettings',
```

This will be fixed by #10457!

### Review thread 3 (resolved)

#### jorgefilipecosta on 2018-10-09T11:27:53Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223660973
Path: packages/components/CHANGELOG.md

```diff
@@ -4,6 +4,10 @@
 
 - Added a new `ResizableBox` component.
 
+### Deprecation
+
+- `wp.components.PanelColor` has been deprecated in favor of `wp.editor.PanelColorSettings`.
```

We should also add the deprecation information to /docs/reference/deprecated.md.

#### chrisvanpatten on 2018-10-09T22:53:57Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r223889948
Path: packages/components/CHANGELOG.md

```diff
@@ -4,6 +4,10 @@
 
 - Added a new `ResizableBox` component.
 
+### Deprecation
+
+- `wp.components.PanelColor` has been deprecated in favor of `wp.editor.PanelColorSettings`.
```

Done!

### Review thread 4

#### aduth on 2018-10-18T15:05:06Z

URL: https://github.com/WordPress/gutenberg/pull/10391#discussion_r226343678
Path: packages/components/CHANGELOG.md

```diff
@@ -1,3 +1,9 @@
+## 4.2.0 (Unreleased)
+
+### Deprecation
+
+- `wp.components.PanelColor` has been deprecated in favor of `wp.editor.PanelColorSettings`.
```

In the context of `packages`, we should avoid references to WordPress-specific globals when possible (at least for changelog). Console messaging is trickier since we can't yet customize for the WordPress context, but it's a hope there we could.

See: #9753

## Changed files

- docs/reference/deprecated.md (MODIFIED, +2/-0)
- packages/components/CHANGELOG.md (MODIFIED, +6/-0)
- packages/components/src/panel/color.js (MODIFIED, +7/-0)
- packages/components/src/panel/test/color.js (MODIFIED, +4/-0)
- packages/editor/CHANGELOG.md (MODIFIED, +6/-0)
- packages/editor/src/components/panel-color/index.js (MODIFIED, +7/-0)
