---
number: 75157
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/75157
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #75157: Components: Preserve ColorPicker hue when saturation picker reaches black/white

- URL: https://github.com/WordPress/gutenberg/pull/75157
- Author: JosVelasco
- Created: 2026-02-03T00:54:15Z
- Updated: 2026-02-16T18:47:39Z
- Comments: 12 of 12
- Review threads: 9 of 9
- Changed files: 3 of 3
- Diff: ../pr-diffs/75157.diff

## Body

## What?
Fixes the hue slider jumping to red when dragging the saturation picker to black, white, or gray areas.

## Why?
When dragging to color-space extremes like black (`v=0`), white (`s=0, v=100`), or gray (`s=0`), the hue becomes mathematically undefined. The previous implementation used `RgbStringColorPicker`, which serialized the color as an RGB string (e.g. `"rgb(0,0,0)"`). Since RGB carries zero hue information for achromatic colors, every re-render fed `h: 0` back into react-colorful, causing the hue slider to snap to red.

This is a well-known problem in color science and has been solved by every major color picker — Figma, Photoshop, react-colorful's own examples, MyPaint ([mypaint/mypaint#838](https://github.com/mypaint/mypaint/issues/838)), and react-color's `oldHue` pattern ([casesandberg/react-color#29](https://github.com/casesandberg/react-color/issues/29)).

## How?
Switches from `RgbStringColorPicker` to `HsvColorPicker`/`HsvaColorPicker` — HSVA is react-colorful's native internal model. Because the saturation panel works directly in HSVA, there is no intermediate conversion that could lose precision or reset the hue. This also eliminates the thumb jitter that an HSL-based approach would introduce (HSL→hex→HSL round-trips lose floating-point precision, causing the thumb to shift on re-render).

A `useRef` preserves the last meaningful hue for the one remaining edge case: when the parent re-renders with a hex-derived achromatic color (e.g. `#000000` → `hsv(0, 0%, 0%)`), the ref substitutes the last known chromatic hue instead of `0`.

A second pair of refs (`lastHsvRef` / `lastHexRef`) caches the most recent HSV↔hex pair. When the parent re-renders with the same hex we last emitted, the cached HSV is returned directly — bypassing the lossy hex→HSV conversion and preventing precision drift during dragging.

This is the standard approach — HSVA as the internal model, where each UI control only mutates its own channel. The hue is structurally isolated from saturation/value changes. The alternative (caching `oldHue`) is what react-color uses but is more fragile and harder to maintain.

**Changes:** ~50 lines in a single file (`picker.tsx`), no API changes. Added unit tests for hue preservation and a changelog entry.

## Testing Instructions
1. Create a new post/page
2. Add a **Cover block** with a solid color overlay
3. Open the custom color picker
4. Enter `#3366FF` (blue) in the hex input
5. Drag the saturation picker to the **bottom-left corner** (black)
6. Continue dragging up along the left edge toward white
7. Drag back out to a colored area
8. **Verify:** The hue slider stays blue throughout and the color returns as blue

Also test with:
- `#38CC38` (green) to confirm hue preservation works for other hues
- **Paragraph block** text color
- **Button block** background color
- Typing achromatic values directly in hex input (`#000000`, `#FFFFFF`, `#999999`) then dragging the saturation picker
- **Storybook:** drag the saturation thumb smoothly — no jitter or jumping to (0,0,0)
- - Dragging **outside the picker area** during a drag gesture (pointer capture test)

## Before / After
https://github.com/user-attachments/assets/b9c89a9d-bb9b-40a0-bdc8-7ba1b2ffdd9a
https://github.com/user-attachments/assets/137c5f7e-1f60-470a-bef5-e6be5c5f5ddb

Fixes #47504

**AI assistance:** Yes
**Tool(s):** Claude (Anthropic)
**Used for:** Researching how other editors solve this problem (Figma, Photoshop, react-color, react-colorful, MyPaint), identifying HSVA as the standard solution, debugging the HSL jitter issue, and implementation. Final code was reviewed, tested, and validated by me.

## Issue comments

### github-actions on 2026-02-03T00:54:24Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3838134720

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @yllip.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: yllip.

Co-authored-by: JosVelasco <josvelasco@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: stokesman <presstoke@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: jeflopodev <jeflopodev@git.wordpress.org>
Co-authored-by: hanneslsm <hanneslsm@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### JosVelasco on 2026-02-03T02:05:50Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3838447492

I've found some issues with other blocks, like the heading block. I'll convert this to draft so I can keep working.

### JosVelasco on 2026-02-05T00:35:49Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3850435242

Could you take a look at this proposal? @t-hamano 

I think this issue is unrelated:
https://github.com/WordPress/gutenberg/issues/75223

### JosVelasco on 2026-02-07T17:12:47Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3864871352

Updated the approach: instead of caching oldHue, I switched from RgbStringColorPicker to HslColorPicker so hue is passed as a discrete channel and never lost during RGB serialization. A useRef handles the remaining edge case for achromatic colors from hex round-tripping. This is the same pattern used by Figma, Photoshop, and React-Colorful's own examples. Single file change, no API changes. Ready for review! @t-hamano

### Mamaduka on 2026-02-09T09:06:32Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3870328951

Thanks, @JosVelasco!

Do you mind adding the changelog entry as requested by CI?

I also wonder if it's related to #75243.

### stokesman on 2026-02-09T23:09:14Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3874355134

Thank you for the PR @JosVelasco. I’d love to see this issue fixed. As noted by Aki https://github.com/WordPress/gutenberg/issues/47504#issuecomment-1406693396, there are some conditions that avoid the issue and that has me curious if there’s some other way to resolve this.

I tinkered around a bit and found one that seems to work – use the non-string picker components from `react-colorful`. It would seem to make more sense given the incoming `color` value is already an object. I wondered how that came about and found #35670 where the picker components were changed. I’ll need to test more and make sure the issues that PR addressed do not resurface but I thought it worth a mention.

### JosVelasco on 2026-02-09T23:42:24Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3874461683

Thanks for looking into this and for the pointer to #35670! The latest revision actually landed in the same place — I switched from RgbStringColorPicker to HsvColorPicker/HsvaColorPicker. Using HSVA (react-colorful's native internal model) seems to eliminate both the hue reset and the thumb jitter that an HSL-based approach introduced. Since the saturation panel works directly in HSVA, there's no intermediate conversion that could lose precision or reset the hue.
I also added unit tests for hue preservation and a changelog entry. I relied heavily on AI assistance for this, so I'd really appreciate a thorough review! @stokesman

### github-actions on 2026-02-11T16:22:18Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3885498884

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @yllip.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: yllip.

Co-authored-by: JosVelasco <josvelasco@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: stokesman <presstoke@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: jeflopodev <jeflopodev@git.wordpress.org>
Co-authored-by: hanneslsm <hanneslsm@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### JosVelasco on 2026-02-11T16:39:51Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3885610974

Consolidated the tests into a single test, as suggested — thanks, @stokesman!
And yes, @ciampo, happy to look into the HSL inputs issue as a follow-up PR.

### JosVelasco on 2026-02-12T16:11:16Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3891853997

To fix the HSL inputs issue, I think we'd need a full refactor, so this PR could wait. This is what I have so far, but I haven't started/tested yet:

component.tsx: Keep internal HSLA state as the single source of truth (authoritative H/S).
picker.tsx: Switch to HslStringColorPicker / HslaStringColorPicker.
hsl-input.tsx: Remove internal state entirely — parent manages everything.
color-input.tsx: Route hsla to HSL input, Colord to RGB/hex.
types.ts: Update interfaces to match the new data flow.

@stokesman @ciampo

### JosVelasco on 2026-02-12T23:33:02Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3893968108

Here is the new PR draft: https://github.com/WordPress/gutenberg/pull/75493

### JosVelasco on 2026-02-16T18:47:38Z

URL: https://github.com/WordPress/gutenberg/pull/75157#issuecomment-3910023792

Closing in favour of #75493, which takes a more comprehensive approach by lifting HSLA state to the parent component and preserving both hue and saturation (not just hue) through achromatic colors.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2026-02-09T12:14:25Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2782305509
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,29 +1,63 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HslColorPicker, HslaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HslaColorPicker : HslColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsl(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSL picker — which
+	// internally converts to HSVA passing h through unchanged — the hue
+	// slider stays stable.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsl().h );
+
+	const hslColor = useMemo( () => {
+		const hsl = color.toHsl();
+
+		// Update the preserved hue only when the color carries meaningful
+		// chromatic information. Achromatic colors (very low saturation or
+		// lightness at the extremes) report hue as 0 after hex round-tripping,
+		// so we ignore those values.
+		if ( hsl.s >= 1 && hsl.l >= 1 && hsl.l <= 99 ) {
+			preservedHueRef.current = hsl.h;
```

Using a ref inside a memoized function makes im-pure. Understanding that this likely ok (and a widespread pattern for synchronous ref tracking/updating), let's at least add a short comment to acknowldge this trade-off

### Review thread 2 (resolved)

#### ciampo on 2026-02-09T12:15:22Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2782309541
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,29 +1,63 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HslColorPicker, HslaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HslaColorPicker : HslColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsl(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSL picker — which
+	// internally converts to HSVA passing h through unchanged — the hue
+	// slider stays stable.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsl().h );
+
+	const hslColor = useMemo( () => {
+		const hsl = color.toHsl();
+
+		// Update the preserved hue only when the color carries meaningful
+		// chromatic information. Achromatic colors (very low saturation or
+		// lightness at the extremes) report hue as 0 after hex round-tripping,
+		// so we ignore those values.
+		if ( hsl.s >= 1 && hsl.l >= 1 && hsl.l <= 99 ) {
+			preservedHueRef.current = hsl.h;
+		}
+
+		return {
+			h: preservedHueRef.current,
+			s: hsl.s,
+			l: hsl.l,
+			a: hsl.a ?? 1,
+		};
+	}, [ color ] );
 
 	return (
 		<Component
-			color={ rgbColor }
-			onChange={ ( nextColor ) => {
-				onChange( colord( nextColor ) );
+			color={ hslColor }
+			onChange={ ( nextHsl ) => {
+				// Track hue from direct picker interaction when chromatic.
+				if ( nextHsl.s >= 1 && nextHsl.l >= 1 && nextHsl.l <= 99 ) {
```

Let's abstract the `s >= 1 && l >= 1 && l <= 99` check in a separate, reusable utlity (it's also used in computing the `hslColor`  variable above)

### Review thread 3 (resolved)

#### ciampo on 2026-02-09T12:16:10Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2782312819
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,29 +1,63 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HslColorPicker, HslaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HslaColorPicker : HslColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsl(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSL picker — which
+	// internally converts to HSVA passing h through unchanged — the hue
+	// slider stays stable.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsl().h );
+
+	const hslColor = useMemo( () => {
+		const hsl = color.toHsl();
+
+		// Update the preserved hue only when the color carries meaningful
+		// chromatic information. Achromatic colors (very low saturation or
+		// lightness at the extremes) report hue as 0 after hex round-tripping,
+		// so we ignore those values.
+		if ( hsl.s >= 1 && hsl.l >= 1 && hsl.l <= 99 ) {
+			preservedHueRef.current = hsl.h;
+		}
+
+		return {
+			h: preservedHueRef.current,
+			s: hsl.s,
+			l: hsl.l,
+			a: hsl.a ?? 1,
```

`hsl.a` should be always defined, in which case we don't need a `?? 1` fallback

### Review thread 4 (resolved)

#### ciampo on 2026-02-09T12:19:33Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2782326535
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,29 +1,63 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HslColorPicker, HslaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HslaColorPicker : HslColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsl(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSL picker — which
+	// internally converts to HSVA passing h through unchanged — the hue
+	// slider stays stable.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsl().h );
+
+	const hslColor = useMemo( () => {
+		const hsl = color.toHsl();
+
+		// Update the preserved hue only when the color carries meaningful
+		// chromatic information. Achromatic colors (very low saturation or
+		// lightness at the extremes) report hue as 0 after hex round-tripping,
+		// so we ignore those values.
+		if ( hsl.s >= 1 && hsl.l >= 1 && hsl.l <= 99 ) {
+			preservedHueRef.current = hsl.h;
+		}
+
+		return {
+			h: preservedHueRef.current,
+			s: hsl.s,
+			l: hsl.l,
+			a: hsl.a ?? 1,
```

Nit:

When `enableAlpha` is `false`, `Component` is `HslColorPicker`.  `HslColorPicker` expects `{ h, s, l }`, but `hslColor` always includes the `a` prop. Extra properties are silently ignored by react-colorful, and TypeScript won't flag it since hslColor is not an inline object literal.

Maybe worth including/excluding the `a` property from `hslColor` based on the `enableAlpha` value?

### Review thread 5 (resolved)

#### ciampo on 2026-02-09T12:23:25Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2782344009
Path: packages/components/src/color-picker/picker.tsx

It would be great to add some unit tests for the new hue preservation — you can take a look at the existing tests to take inspiration around how to interact with the picker and set/get colors

### Review thread 6 (resolved)

#### ciampo on 2026-02-10T12:06:44Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2787577039
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -419,4 +419,102 @@ describe( 'ColorPicker', () => {
 			expect( alphaInput ).toHaveValue( 75 );
 		} );
 	} );
+
+	describe( 'Hue preservation', () => {
+		it( 'should preserve the picker hue when the color becomes black', async () => {
+			const user = userEvent.setup();
+
+			render(
+				<ControlledColorPicker
+					onChange={ jest.fn() }
+					enableAlpha={ false }
+					initialColor="#3366ff"
+				/>
+			);
+
+			const pickerHueSlider = screen.getByRole( 'slider', {
+				name: 'Hue',
+			} );
+
+			// Blue (#3366ff) has a hue of 225°.
+			expect( pickerHueSlider ).toHaveAttribute( 'aria-valuenow', '225' );
```

Can we use `toHaveValue` instead of checking more specific implementation details, such as `aria-valuenow`?

In general, let's use the same conventions that existing tests are already adopting for better consistency.

#### JosVelasco on 2026-02-10T17:23:40Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2789279639
Path: packages/components/src/color-picker/test/index.tsx

```diff
@@ -419,4 +419,102 @@ describe( 'ColorPicker', () => {
 			expect( alphaInput ).toHaveValue( 75 );
 		} );
 	} );
+
+	describe( 'Hue preservation', () => {
+		it( 'should preserve the picker hue when the color becomes black', async () => {
+			const user = userEvent.setup();
+
+			render(
+				<ControlledColorPicker
+					onChange={ jest.fn() }
+					enableAlpha={ false }
+					initialColor="#3366ff"
+				/>
+			);
+
+			const pickerHueSlider = screen.getByRole( 'slider', {
+				name: 'Hue',
+			} );
+
+			// Blue (#3366ff) has a hue of 225°.
+			expect( pickerHueSlider ).toHaveAttribute( 'aria-valuenow', '225' );
```

Done — switched to `toHaveValue` for consistency with the existing tests.

### Review thread 7 (resolved)

#### ciampo on 2026-02-10T12:22:51Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2787648381
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,39 +1,111 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HsvColorPicker, HsvaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
+/**
+ * Checks whether an HSV color carries meaningful chromatic information.
+ * Achromatic colors (very low saturation or value near zero) report
+ * hue as 0 after hex round-tripping, so their hue is unreliable.
+ */
+function isChromatic( hsv: { s: number; v: number } ): boolean {
+	return hsv.s >= 1 && hsv.v >= 1;
+}
+
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HsvaColorPicker : HsvColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsv(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSV picker — which
+	// uses HSVA as its native model — the hue slider stays stable with no
+	// intermediate conversion.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsv().h );
+
+	// Cache the last HSV we produced and the corresponding hex to avoid
+	// precision jitter during dragging. Without this, the HSV→hex→HSV
+	// round-trip through the parent can produce slightly different values,
+	// causing the thumb to shift on re-render.
+	const lastHsvRef = useRef< {
+		h: number;
+		s: number;
+		v: number;
+		a: number;
+	} | null >( null );
+	const lastHexRef = useRef( '' );
+
+	// Note: this useMemo intentionally reads and writes refs for hue
+	// preservation and jitter avoidance. This is an accepted trade-off
+	// since the ref updates are synchronous and idempotent — the same
+	// input color will always produce the same ref values.
+	const hsvColor = useMemo( () => {
+		const currentHex = color.toHex();
+
+		// If the hex matches what we last emitted, return the cached HSV
+		// to avoid floating-point precision drift from hex → HSV conversion.
+		if ( lastHsvRef.current && currentHex === lastHexRef.current ) {
+			return lastHsvRef.current;
+		}
+
+		// External change — parse from hex and preserve hue if chromatic.
+		const hsv = color.toHsv();
+
+		if ( isChromatic( hsv ) ) {
+			preservedHueRef.current = hsv.h;
+		}
+
+		const result = {
+			h: preservedHueRef.current,
+			s: hsv.s,
+			v: hsv.v,
+			a: hsv.a,
+		};
+
+		lastHsvRef.current = result;
+		lastHexRef.current = currentHex;
+
+		return result;
+	}, [ color ] );
 
 	return (
 		<Component
-			color={ rgbColor }
-			onChange={ ( nextColor ) => {
-				onChange( colord( nextColor ) );
-			} }
-			// Pointer capture fortifies drag gestures so that they continue to
-			// work while dragging outside the component over objects like
-			// iframes. If a newer version of react-colorful begins to employ
-			// pointer capture this will be redundant and should be removed.
-			onPointerDown={ ( { currentTarget, pointerId } ) => {
-				currentTarget.setPointerCapture( pointerId );
-			} }
-			onPointerUp={ ( { currentTarget, pointerId } ) => {
-				currentTarget.releasePointerCapture( pointerId );
```

What's the reason for removing these two pointer capture handlers? Can we add them back?

#### JosVelasco on 2026-02-10T17:26:17Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2789291904
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,39 +1,111 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HsvColorPicker, HsvaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
+/**
+ * Checks whether an HSV color carries meaningful chromatic information.
+ * Achromatic colors (very low saturation or value near zero) report
+ * hue as 0 after hex round-tripping, so their hue is unreliable.
+ */
+function isChromatic( hsv: { s: number; v: number } ): boolean {
+	return hsv.s >= 1 && hsv.v >= 1;
+}
+
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HsvaColorPicker : HsvColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsv(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSV picker — which
+	// uses HSVA as its native model — the hue slider stays stable with no
+	// intermediate conversion.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsv().h );
+
+	// Cache the last HSV we produced and the corresponding hex to avoid
+	// precision jitter during dragging. Without this, the HSV→hex→HSV
+	// round-trip through the parent can produce slightly different values,
+	// causing the thumb to shift on re-render.
+	const lastHsvRef = useRef< {
+		h: number;
+		s: number;
+		v: number;
+		a: number;
+	} | null >( null );
+	const lastHexRef = useRef( '' );
+
+	// Note: this useMemo intentionally reads and writes refs for hue
+	// preservation and jitter avoidance. This is an accepted trade-off
+	// since the ref updates are synchronous and idempotent — the same
+	// input color will always produce the same ref values.
+	const hsvColor = useMemo( () => {
+		const currentHex = color.toHex();
+
+		// If the hex matches what we last emitted, return the cached HSV
+		// to avoid floating-point precision drift from hex → HSV conversion.
+		if ( lastHsvRef.current && currentHex === lastHexRef.current ) {
+			return lastHsvRef.current;
+		}
+
+		// External change — parse from hex and preserve hue if chromatic.
+		const hsv = color.toHsv();
+
+		if ( isChromatic( hsv ) ) {
+			preservedHueRef.current = hsv.h;
+		}
+
+		const result = {
+			h: preservedHueRef.current,
+			s: hsv.s,
+			v: hsv.v,
+			a: hsv.a,
+		};
+
+		lastHsvRef.current = result;
+		lastHexRef.current = currentHex;
+
+		return result;
+	}, [ color ] );
 
 	return (
 		<Component
-			color={ rgbColor }
-			onChange={ ( nextColor ) => {
-				onChange( colord( nextColor ) );
-			} }
-			// Pointer capture fortifies drag gestures so that they continue to
-			// work while dragging outside the component over objects like
-			// iframes. If a newer version of react-colorful begins to employ
-			// pointer capture this will be redundant and should be removed.
-			onPointerDown={ ( { currentTarget, pointerId } ) => {
-				currentTarget.setPointerCapture( pointerId );
-			} }
-			onPointerUp={ ( { currentTarget, pointerId } ) => {
-				currentTarget.releasePointerCapture( pointerId );
```

I had removed them while debugging the jitter issue. Sorry for the confusion. I added them back.

### Review thread 8 (resolved)

#### ciampo on 2026-02-10T12:23:21Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2787650510
Path: packages/components/CHANGELOG.md

```diff
@@ -8,6 +8,7 @@
 -   `Text`: Remove `text-wrap: balance` fallback. Only `text-wrap: pretty` is now used ([#75089](https://github.com/WordPress/gutenberg/pull/75089)).
 -   `RangeControl`: support forced-colors mode ([#75165](https://github.com/WordPress/gutenberg/pull/75165)).
 -   `ToggleControl`: Prevent `__nextHasNoMarginBottom` from logging a console warning ([#75296](https://github.com/WordPress/gutenberg/pull/75296)).
+-   `ColorPicker`: Preserve hue when the saturation picker reaches black, white, or gray ([#75157](https://github.com/WordPress/gutenberg/pull/75157)).
```

```suggestion
-   `ColorPicker`: Preserve hue when the selected color becomes a-chromatic ([#75157](https://github.com/WordPress/gutenberg/pull/75157)).
```

### Review thread 9 (resolved)

#### ciampo on 2026-02-10T12:24:06Z

URL: https://github.com/WordPress/gutenberg/pull/75157#discussion_r2787653765
Path: packages/components/src/color-picker/picker.tsx

```diff
@@ -1,39 +1,111 @@
 /**
  * External dependencies
  */
-import { RgbStringColorPicker, RgbaStringColorPicker } from 'react-colorful';
+import { HsvColorPicker, HsvaColorPicker } from 'react-colorful';
 import { colord } from 'colord';
 
 /**
  * WordPress dependencies
  */
-import { useMemo } from '@wordpress/element';
+import { useMemo, useRef } from '@wordpress/element';
+
 /**
  * Internal dependencies
  */
 import type { PickerProps } from './types';
 
+/**
+ * Checks whether an HSV color carries meaningful chromatic information.
+ * Achromatic colors (very low saturation or value near zero) report
+ * hue as 0 after hex round-tripping, so their hue is unreliable.
+ */
+function isChromatic( hsv: { s: number; v: number } ): boolean {
+	return hsv.s >= 1 && hsv.v >= 1;
+}
+
 export const Picker = ( { color, enableAlpha, onChange }: PickerProps ) => {
-	const Component = enableAlpha
-		? RgbaStringColorPicker
-		: RgbStringColorPicker;
-	const rgbColor = useMemo( () => color.toRgbString(), [ color ] );
+	const Component = enableAlpha ? HsvaColorPicker : HsvColorPicker;
+
+	// Preserve the last meaningful hue to prevent it resetting to 0 (red)
+	// when the color reaches achromatic points (pure black, white, or gray).
+	//
+	// This is needed because the parent component stores color as hex, which
+	// discards hue for achromatic colors (e.g. #000000 → hsv(0, 0%, 0%)).
+	// By feeding the preserved hue into react-colorful's HSV picker — which
+	// uses HSVA as its native model — the hue slider stays stable with no
+	// intermediate conversion.
+	//
+	// This is the standard approach used by Figma, Photoshop, react-colorful
+	// itself, and other professional color pickers.
+	const preservedHueRef = useRef( color.toHsv().h );
+
+	// Cache the last HSV we produced and the corresponding hex to avoid
+	// precision jitter during dragging. Without this, the HSV→hex→HSV
+	// round-trip through the parent can produce slightly different values,
+	// causing the thumb to shift on re-render.
+	const lastHsvRef = useRef< {
+		h: number;
+		s: number;
+		v: number;
+		a: number;
+	} | null >( null );
+	const lastHexRef = useRef( '' );
+
+	// Note: this useMemo intentionally reads and writes refs for hue
+	// preservation and jitter avoidance. This is an accepted trade-off
+	// since the ref updates are synchronous and idempotent — the same
+	// input color will always produce the same ref values.
+	const hsvColor = useMemo( () => {
+		const currentHex = color.toHex();
+
+		// If the hex matches what we last emitted, return the cached HSV
+		// to avoid floating-point precision drift from hex → HSV conversion.
+		if ( lastHsvRef.current && currentHex === lastHexRef.current ) {
+			return lastHsvRef.current;
+		}
+
+		// External change — parse from hex and preserve hue if chromatic.
+		const hsv = color.toHsv();
+
+		if ( isChromatic( hsv ) ) {
+			preservedHueRef.current = hsv.h;
+		}
+
+		const result = {
+			h: preservedHueRef.current,
+			s: hsv.s,
+			v: hsv.v,
+			a: hsv.a,
+		};
+
+		lastHsvRef.current = result;
+		lastHexRef.current = currentHex;
+
+		return result;
+	}, [ color ] );
 
 	return (
 		<Component
-			color={ rgbColor }
-			onChange={ ( nextColor ) => {
-				onChange( colord( nextColor ) );
-			} }
-			// Pointer capture fortifies drag gestures so that they continue to
-			// work while dragging outside the component over objects like
-			// iframes. If a newer version of react-colorful begins to employ
-			// pointer capture this will be redundant and should be removed.
-			onPointerDown={ ( { currentTarget, pointerId } ) => {
-				currentTarget.setPointerCapture( pointerId );
-			} }
-			onPointerUp={ ( { currentTarget, pointerId } ) => {
-				currentTarget.releasePointerCapture( pointerId );
+			color={ hsvColor }
+			onChange={ ( nextHsv ) => {
+				if ( isChromatic( nextHsv ) ) {
```

`isChromatic( nextHsv )` is called twice in the `onChange` callback, can we call it only once and store it in a local variable?

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/color-picker/picker.tsx (MODIFIED, +91/-9)
- packages/components/src/color-picker/test/index.tsx (MODIFIED, +33/-0)
