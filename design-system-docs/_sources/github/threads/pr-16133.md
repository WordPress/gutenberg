---
number: 16133
type: pull_request
state: MERGED
merged_at: 2019-08-12T15:04:19Z
url: https://github.com/WordPress/gutenberg/pull/16133
matched_labels: ["[Package] Components"]
all_labels: ["Backwards Compatibility","[Package] Server Side Render","[Package] Components"]
---

# PR #16133: Add deprecated call to wp.components.ServerSideRender; Remove some missed wp.components.ServerSideRender usages.

- URL: https://github.com/WordPress/gutenberg/pull/16133
- Author: jorgefilipecosta
- Created: 2019-06-12T21:56:37Z
- Updated: 2019-09-23T08:57:03Z
- Comments: 3 of 3
- Review threads: 5 of 5
- Changed files: 4 of 4
- Diff: ../pr-diffs/16133.diff

## Body

## Description
Follow up from https://github.com/WordPress/gutenberg/pull/15635.

In https://github.com/WordPress/gutenberg/pull/15635 we kept the back-compatibility for usages of wp.components.ServerSideRender but we did not implement a deprecated call.

This PR adds the missing deprecated call.

## How has this been tested?
I changed the archives block to use ServerSideRender from wp.components.
I verified the block works as expected, but a deprecated warning is thrown in the browser console.

## Issue comments

### jorgefilipecosta on 2019-08-06T14:06:16Z

URL: https://github.com/WordPress/gutenberg/pull/16133#issuecomment-518684429

This PR was updated to avoid arrow function usage, it should be ready.

### jorgefilipecosta on 2019-08-12T12:05:59Z

URL: https://github.com/WordPress/gutenberg/pull/16133#issuecomment-520397726

Hi @gziolo the PR was rebased.

### gziolo on 2019-08-12T14:38:58Z

URL: https://github.com/WordPress/gutenberg/pull/16133#issuecomment-520452669

I noticed that PHP and JS linters are still unhappy. You will have to convince them this PR is ready to go :)

## Review threads

### Review thread 1 (resolved)

#### aduth on 2019-06-25T17:14:57Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r297299862
Path: lib/client-assets.php

```diff
@@ -297,8 +297,16 @@ function gutenberg_register_scripts_and_styles() {
 			array(
 				'( function() {',
 				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
```

Doesn't the last part of this condition become unnecessary, or at least defeat the purpose of adding a deprecation? In other words, should we override `wp.components.ServerSideRender` even if it already exists?

#### jorgefilipecosta on 2019-06-27T17:17:08Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r298282749
Path: lib/client-assets.php

```diff
@@ -297,8 +297,16 @@ function gutenberg_register_scripts_and_styles() {
 			array(
 				'( function() {',
 				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
```

I guess it is safe to override wp.components.ServerSideRender even if it already exists, the code was updated.

### Review thread 2 (resolved)

#### aduth on 2019-07-29T13:44:23Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r308237108
Path: lib/client-assets.php

```diff
@@ -296,9 +296,17 @@ function gutenberg_register_scripts_and_styles() {
 			"\n",
 			array(
 				'( function() {',
-				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
-				'		wp.components.ServerSideRender = wp.serverSideRender;',
-				'	};',
+				'	if ( wp && wp.components && wp.serverSideRender ) {',
+				'		wp.components.ServerSideRender = wp.element.forwardRef( ( props, ref ) => {',
```

We can't use arrow function syntax here, it will certainly break in IE11.

The `Object.assign` later is okay because it's polyfilled, but we can't polyfill these syntaxes.

### Review thread 3

#### gziolo on 2019-08-12T11:27:22Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r312878734
Path: lib/client-assets.php

```diff
@@ -282,9 +282,17 @@ function gutenberg_register_scripts_and_styles() {
 			"\n",
 			array(
 				'( function() {',
-				'	if ( wp && wp.components && wp.serverSideRender && ! wp.components.ServerSideRender ) {',
-				'		wp.components.ServerSideRender = wp.serverSideRender;',
-				'	};',
+				'	if ( wp && wp.components && wp.serverSideRender ) {',
```

I'm a bit worried that this might not work in all cases. What if someone has a block which imports `ServerSideRender` from `wp.components` but never references `wp-server-side-render` as a dependency. It's a tough problem. I see that we expose `ServerSideRender` from `wp.editor`, so maybe we should attach this polyfill there without any guard clauses? Or even we could include it inside `@wordpress/editor` which is less likely to be used outside of WordPress.

### Review thread 4

#### gziolo on 2019-08-12T14:07:35Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r312942702
Path: packages/server-side-render/src/index.js

```diff
@@ -44,3 +44,14 @@ export default withSelect(
 		);
 	}
 );
+
+if( window && window.wp && window.wp.components ) {
+	window.wp.components.ServerSideRender = forwardRef( ( props, ref ) => {
+		wp.deprecated( 'wp.components.ServerSideRender', {
```

Can you import `deprecated` to ensure it's listed as a dependency of the module when generating deps file for WordPress?

#### jorgefilipecosta on 2019-08-12T14:19:53Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r312949101
Path: packages/server-side-render/src/index.js

```diff
@@ -44,3 +44,14 @@ export default withSelect(
 		);
 	}
 );
+
+if( window && window.wp && window.wp.components ) {
+	window.wp.components.ServerSideRender = forwardRef( ( props, ref ) => {
+		wp.deprecated( 'wp.components.ServerSideRender', {
```

PR updated 👍

### Review thread 5

#### gziolo on 2019-08-12T14:08:38Z

URL: https://github.com/WordPress/gutenberg/pull/16133#discussion_r312943206
Path: packages/server-side-render/src/index.js

```diff
@@ -44,3 +44,14 @@ export default withSelect(
 		);
 	}
 );
+
+if( window && window.wp && window.wp.components ) {
+	window.wp.components.ServerSideRender = forwardRef( ( props, ref ) => {
+		wp.deprecated( 'wp.components.ServerSideRender', {
+			alternative: 'wp.serverSideRender',
```

It's a bit unfortunate that this global variable starts with lowercase.

## Changed files

- lib/client-assets.php (MODIFIED, +0/-15)
- package-lock.json (MODIFIED, +1/-0)
- packages/server-side-render/package.json (MODIFIED, +1/-0)
- packages/server-side-render/src/index.js (MODIFIED, +16/-2)
