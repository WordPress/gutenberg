---
number: 60898
type: pull_request
state: MERGED
merged_at: 2024-04-23T16:38:44Z
url: https://github.com/WordPress/gutenberg/pull/60898
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components","Storybook"]
---

# PR #60898: InputControl: added password visibility story

- URL: https://github.com/WordPress/gutenberg/pull/60898
- Author: DaniGuardiola
- Created: 2024-04-19T12:10:59Z
- Updated: 2024-04-23T16:39:18Z
- Comments: 2 of 2
- Review threads: 4 of 4
- Changed files: 2 of 2
- Diff: ../pr-diffs/60898.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Fixes https://github.com/WordPress/gutenberg/issues/60502

Add a story with an example of a "password input visibility toggle" pattern, as suggested in linked issue.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

To provide users with an example of how to implement this pattern.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Adding a story.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

See InputControl > Show Password story on Storybook.

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

Tab around to focus the input or the button.

## Screenshots or screencast <!-- if applicable -->

https://github.com/WordPress/gutenberg/assets/8862967/9fa1e7e1-79ea-4564-b31f-c552dfd4e06f

## Issue comments

### github-actions on 2024-04-19T12:20:15Z

URL: https://github.com/WordPress/gutenberg/pull/60898#issuecomment-2066453742

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: jasmussen <joen@git.wordpress.org>
Co-authored-by: jameskoster <jameskoster@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### mirka on 2024-04-22T20:00:08Z

URL: https://github.com/WordPress/gutenberg/pull/60898#issuecomment-2070851479

Just FYI because this happens a lot: After there is a new release of the wp-components package on trunk, any open PRs with changelog modifications that haven't been manually reconciled with trunk can get placed under the wrong release section upon landing, due limitations of the automatic text merging. 7e0f0eae8a88a1494119c8ce38cb8c04be64cf7b

Let me know if you have any ideas how to prevent that from happening 😅 For now we have to keep an eye out for releases (about twice monthly), and reconcile manually.

## Review threads

### Review thread 1 (resolved)

#### mirka on 2024-04-19T14:48:45Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1572492899
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -9,6 +9,13 @@ import type { Meta, StoryFn } from '@storybook/react';
 import InputControl from '..';
 import { InputControlPrefixWrapper } from '../input-prefix-wrapper';
 import { InputControlSuffixWrapper } from '../input-suffix-wrapper';
+/**
+ * WordPress dependencies
+ */
+import { seen, unseen } from '@wordpress/icons';
+import { useReducer } from '@wordpress/element';
+import Tooltip from '../../tooltip';
+import Button from '../../button';
```

This is just our formatting convention (which isn't fully covered by linting), but the import block order is External → WordPress → Internal, and component imports from within the components package go under Internal.

### Review thread 2 (resolved)

#### mirka on 2024-04-19T14:51:41Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1572496538
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
```

I'm totally fine with this in our own code, but since this is a demo snippet for the public, I feel like we could keep it more approachable by sticking with `useState()`.

### Review thread 3 (resolved)

#### mirka on 2024-04-19T14:55:50Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1572504038
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
+							icon={ visible ? unseen : seen }
+							onClick={ toggleVisible }
+							aria-label={
+								visible ? 'Hide password' : 'Show password'
+							}
```

The tooltip functionality is actually built into Button. It will set both a tooltip and the aria-label if you do this:

```suggestion
							label={
								visible ? 'Hide password' : 'Show password'
							}
```

#### DaniGuardiola on 2024-04-22T09:19:55Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1574425444
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
+							icon={ visible ? unseen : seen }
+							onClick={ toggleVisible }
+							aria-label={
+								visible ? 'Hide password' : 'Show password'
+							}
```

Ok, I will go with that. Off-topic but I'm not sure I like bundling components like this, though I understand the reasoning behind it.

#### mirka on 2024-04-22T19:42:46Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1575278060
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
+							icon={ visible ? unseen : seen }
+							onClick={ toggleVisible }
+							aria-label={
+								visible ? 'Hide password' : 'Show password'
+							}
```

Yes, the Button component is ancient and we'd probably design a very different API if we started from scratch today.

### Review thread 4

#### mirka on 2024-04-19T14:57:20Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1572506088
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
```

Might be better to use the `size="small"` variant so the focus rings don't overlap.

#### DaniGuardiola on 2024-04-22T09:06:47Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1574406999
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
```

Good call.

#### DaniGuardiola on 2024-04-22T09:18:12Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1574423059
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
```

I don't know what's the right way to style things in this context, so I went with inline styles to vertically center it. I also originally went with flex + align items center, but then realized flex was enough (not sure why though). Any suggestions welcome!

#### mirka on 2024-04-22T19:47:34Z

URL: https://github.com/WordPress/gutenberg/pull/60898#discussion_r1575282438
Path: packages/components/src/input-control/stories/index.story.tsx

```diff
@@ -82,3 +89,32 @@ WithEdgeLabel.args = {
 	__unstableInputWidth: '20em',
 	labelPosition: 'edge',
 };
+
+export const ShowPassword: StoryFn< typeof InputControl > = ( args ) => {
+	const [ visible, toggleVisible ] = useReducer(
+		( value ) => ! value,
+		false
+	);
+	return (
+		<InputControl
+			type={ visible ? 'text' : 'password' }
+			label="Password"
+			suffix={
+				<InputControlSuffixWrapper>
+					<Tooltip
+						text={ visible ? 'Hide password' : 'Show password' }
+					>
+						<Button
```

I think it's just the fact the Button became `display: block` because of the flex parent. Which is equivalent to setting `line-height: 0`.

An inline style is perfectly appropriate here 👍

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/input-control/stories/index.story.tsx (MODIFIED, +31/-1)
