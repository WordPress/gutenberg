---
number: 39316
type: pull_request
state: MERGED
merged_at: 2022-03-14T18:34:57Z
url: https://github.com/WordPress/gutenberg/pull/39316
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Type] Developer Documentation","[Package] Components","Storybook"]
---

# PR #39316: Divider: Improve default behavior when in vertical orientation

- URL: https://github.com/WordPress/gutenberg/pull/39316
- Author: mirka
- Created: 2022-03-09T13:53:25Z
- Updated: 2022-03-14T18:35:30Z
- Comments: 0 of 0
- Review threads: 1 of 1
- Changed files: 4 of 4
- Diff: ../pr-diffs/39316.diff

## Body

Follow-up to #38842

## What?

- Conditionally renders the divider as an `inline` element when the `orientation` is `vertical`.
- Improves stories and documentation.

## Why?

This ensures that the divider is visible "as expected" in both orientations when inside a flow layout container (not flex). Initially I was only going to improve the docs to demonstrate proper usage in the flex layout case, but I realized that we can also add a conditional `display` declaration so it just works out of the box.

I'm inclined to classify this as a non-breaking change, because any proper usage of the vertical divider should already be `display: inline` or inside a flex container (in which case the `inline` won't matter).

## Testing Instructions

`npm run storybook:dev` and check out the stories for `Divider`.

- The `vertical` orientation works without a flex container.
- Inside a flex container, it still works as expected as long as it's `stretch` aligned.

## Issue comments

_No issue comments._
## Review threads

### Review thread 1 (resolved)

#### mirka on 2022-03-09T13:54:19Z

URL: https://github.com/WordPress/gutenberg/pull/39316#discussion_r822671094
Path: packages/components/src/divider/types.ts

```diff
@@ -24,6 +24,20 @@ export interface OwnProps {
 	marginEnd?: SpaceInput;
 }
 
+interface SeparatorPropsWithAdditionalDescription {
```

Too much? Should I just put it inside `OwnProps`?

#### ciampo on 2022-03-10T19:27:12Z

URL: https://github.com/WordPress/gutenberg/pull/39316#discussion_r824082618
Path: packages/components/src/divider/types.ts

```diff
@@ -24,6 +24,20 @@ export interface OwnProps {
 	marginEnd?: SpaceInput;
 }
 
+interface SeparatorPropsWithAdditionalDescription {
```

Yeah, I'd personally add it to `OwnProps`. For added clarity, we could at most add a comment where we `omit` the prop from `SeparatorProps` to clarify that we're redefining for better docs? 

Alternatively, we could override it just in Storybook via `argTypes` (although I think I'd prefer the TypeScript solution)

#### mirka on 2022-03-11T16:13:01Z

URL: https://github.com/WordPress/gutenberg/pull/39316#discussion_r824863735
Path: packages/components/src/divider/types.ts

```diff
@@ -24,6 +24,20 @@ export interface OwnProps {
 	marginEnd?: SpaceInput;
 }
 
+interface SeparatorPropsWithAdditionalDescription {
```

Agreed. I guess it's pretty self-evident why it's being overridden.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +5/-1)
- packages/components/src/divider/stories/index.tsx (MODIFIED, +26/-23)
- packages/components/src/divider/styles.ts (MODIFIED, +9/-0)
- packages/components/src/divider/types.ts (MODIFIED, +11/-1)
