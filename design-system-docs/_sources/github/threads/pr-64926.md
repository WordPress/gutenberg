---
number: 64926
type: pull_request
state: MERGED
merged_at: 2024-09-11T15:24:02Z
url: https://github.com/WordPress/gutenberg/pull/64926
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #64926: Improve Tabs indicator animation and related utils

- URL: https://github.com/WordPress/gutenberg/pull/64926
- Author: DaniGuardiola
- Created: 2024-08-30T11:50:11Z
- Updated: 2024-09-11T15:24:25Z
- Comments: 36 of 36
- Review threads: 11 of 11
- Changed files: 4 of 4
- Diff: ../pr-diffs/64926.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

This PR simplifies the utils used in animating the indicator, which is also the base for the upcoming ToggleGroupControl animation which is done but will be submitted in a follow-up.

Additionally, it switches to a `transform`-based strategy, which should speed up the animation even further.

This work is also related to the `useResizeObserver` refactoring efforts (#64820). Here, the utility is radically simplified (and renamed to `useResizeObserver`) with the goal of unifying with that separate effort in a separate PR.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

We had to make some compromises to prevent overflow due to the previous method of measuring positions of elements relative to their offset parent, which resulted in a sub-0.5px error margin (see https://github.com/WordPress/gutenberg/pull/61979). This updated approach has exact precision which eliminates some critical issues in the upcoming ToggleGroupControl animation work.

I made a playground to illustrate this: https://stackblitz.com/edit/solidjs-templates-grt7kw?file=src%2FApp.tsx

The switch to transform provides better animation performance.

The refactor of `useResizeObserver` aims for upcoming unification in relation to #64820.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

See diff, it's all extensively documented.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

Try the tabs indicator animation in the Storybook, plus a few Tabs instances in Gutenberg for good measure (there's a complete list of instances and where to find them in the description of https://github.com/WordPress/gutenberg/pull/64371 if that's useful).

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

Interact with tabs through keyboard.

## Screenshots or screencast <!-- if applicable -->

n/a (just check the storybook, there are no appreciable changes other than the improved subpixel precision)

## Issue comments

### github-actions on 2024-08-30T15:47:32Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2321722039

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### DaniGuardiola on 2024-08-30T17:11:12Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2322003109

@ciampo thanks for the review! Do you know if the RTL issue is pre-existing? Because if so, this can be merged and that can be treated as a separate bug. Though I can't really think of a reason RTL would break things, must be missing something 🤔

### github-actions on 2024-08-30T17:33:06Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2322034716

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 522d816c2d808c12cd776802ab0369a39ec003be.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/10814115022
📝  Reported issues:
- #64334 in `/test/e2e/specs/site-editor/dataviews-list-layout-keyboard.spec.js`

### ciampo on 2024-08-31T08:54:55Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2322833429

[Trunk looks fine to me](https://wordpress.github.io/gutenberg/?path=/story/components-experimental-tabs--default&globals=direction:rtl), so it seems like something introduced by this PR.

![Screenshot 2024-08-31 at 10 51 17](https://github.com/user-attachments/assets/d7f16ba9-6936-487a-ad8c-be2e3cabcace)


I haven't looked deeply into it, but here's where I'd look:

- the first suspect that I see is the `transform-origin` ?
- in general, we need to flip anything that is explicitly or implicitly anchored to the left and scaling to the right
- when the tabs don't "fill" the whole tablist width, that may affect the positioning of the indicator in RTL screen? Maybe we could consider changing from `display: flex` to `display: inline-flex` ?

### ciampo on 2024-09-02T07:39:31Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324030250

Update: we should look at @t-hamano 's proposed fixes in #64965 as an inspiration for what tweaks to make to support the animation for RTL writing directions

### t-hamano on 2024-09-02T09:42:35Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324292613

Curiously, the visual width of the indicator seems to change depending on the display and browser zoom. Is `transform-origin` or `transform: scaleX()` affecting it? I can't see this issue in trunk.

✅ Display scale: 100%

![image](https://github.com/user-attachments/assets/5f62bae9-6a5b-4be0-9c85-d871f49e6def)

❌ Display scale: 125%

![image](https://github.com/user-attachments/assets/5321768e-eea8-4708-8b98-a3a13446cd00)

❌ Display scale: 100%,  Browser zoom: 150%

![image](https://github.com/user-attachments/assets/9906f4b5-0841-4517-8cae-6a7856a67828)

This issue occurs in Chrome but not in Firefox.

I'm testing on Windows OS, can you reproduce this issue on MacOS?

### tyxla on 2024-09-02T09:50:09Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324308248

> I'm testing on Windows OS, can you reproduce this issue on MacOS?

Good find @t-hamano 👍 Can reproduce in the latest Chrome on MacOS:

![Screenshot 2024-09-02 at 12 49 01](https://github.com/user-attachments/assets/3b8750b2-55aa-434b-94f8-d4d165bb42ce)

Can't reproduce in `trunk`:

![Screenshot 2024-09-02 at 12 49 54](https://github.com/user-attachments/assets/ac013f94-0e94-4cc5-a622-284830ab73f2)

### DaniGuardiola on 2024-09-02T10:14:57Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324358207

Just added RTL support. Thanks @ciampo and @t-hamano for detecting the issue and @t-hamano for attempting a fix, although now obsolete due to the change to the transform strategy. Your PR will be helpful to me in the follow-up I'm working on for the `ToggleGroupControl`, since that one will need to use the previous positioning-based strategy, so thank you!

Regarding indicator size, I cannot reproduce in any browser, including Chrome in Mac OS. Can y'all double-check that you're in latest? Either way, it seems like a bug to me that's probably just been fixed so I think we'll be fine.

### DaniGuardiola on 2024-09-02T10:17:19Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324362768

Do y'all think this is changelog-worthy? I'm not really sure since it's a minor internal detail.

### t-hamano on 2024-09-02T10:19:11Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324366166

@DaniGuardiola 

> Regarding indicator size, I cannot reproduce in any browser, including Chrome in Mac OS.

Can you reproduce the issue by changing the zoom level of the Chrome browser? My Chrome version is `128.0.6613.114`, but I can reproduce the issue by changing the browser zoom level to something other than 100%.

### ciampo on 2024-09-02T10:20:09Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324367913

I think it needs a CHANGELOG entry under the "Bug fixes" section since it implicitly fixes the RTL bug flagged by @t-hamano

### DaniGuardiola on 2024-09-02T10:25:35Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324380100

@t-hamano I'm on the same version and managed to reproduce it under specific zoom levels. Investigating now.

### t-hamano on 2024-09-02T10:40:16Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324407170

For some reason, the scaling by `transform: scaleX()` doesn't seem to be accurate. Maybe it has to do with subpixel rendering.

I have prepared a playground:

https://codepen.io/Tetsuaki-Hamno/pen/MWMPebv

Both boxes are expected to be 100px wide, but the boxes scaled by `transform: scaleX()` are not visually the correct width due to the browser zoom level.


https://github.com/user-attachments/assets/30ef7aca-d63d-47d7-a807-6ce2d109fd8f

### t-hamano on 2024-09-02T10:48:51Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324423747

One approach to the solution is to replace the base width value with a larger value, but this might not be ideal.

<details><summary>Diff</summary>

```diff
diff --git a/packages/components/src/tabs/styles.ts b/packages/components/src/tabs/styles.ts
index f335a90c02..9c8f70179c 100644
--- a/packages/components/src/tabs/styles.ts
+++ b/packages/components/src/tabs/styles.ts
@@ -43,7 +43,7 @@ export const TabListWrapper = styled.div`
                &::after {
                        bottom: 0;
                        height: 0;
-                       width: 1px;
+                       width: 100px;
index 2977d6a628..a68b662c8e 100644
--- a/packages/components/src/tabs/tablist.tsx
+++ b/packages/components/src/tabs/tablist.tsx
@@ -81,7 +81,7 @@ export const TabList = forwardRef<
                                '--indicator-top': indicatorPosition.top,
                                '--indicator-right': indicatorPosition.right,
                                '--indicator-left': indicatorPosition.left,
-                               '--indicator-width': indicatorPosition.width,
+                               '--indicator-width': indicatorPosition.width / 100,
                                '--indicator-height': indicatorPosition.height,
                                ...otherProps.style,
                        } }
```

</details>

### DaniGuardiola on 2024-09-02T10:50:21Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324426953

Seems like a bug related to anti-aliasing and rounding: https://stackoverflow.com/a/52159123

I've been trying some ideas like the one in that SO question: https://stackoverflow.com/a/65992479

However, none have been successful so far. Thanks for also looking into it @t-hamano, I think using a larger scale might actually help, since it's an eager rounding issue.

### DaniGuardiola on 2024-09-02T11:05:01Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324455275

Update: found a solution that seems to work correctly in my testing. When changing zoom levels live, sometimes it hangs onto the previously computed value which makes it look wrong, however I don't think it's a big deal since as soon as the tabs are interacted with it gets corrected. See last commit.


https://github.com/user-attachments/assets/83bd3956-da45-44aa-9229-d2134fbca3be

### DaniGuardiola on 2024-09-02T11:17:08Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324480635

Update: this fix causes problems on other browsers that do not exhibit this behavior. A solution might be to find a way that the issue is happening in the first place, and only apply the fix then. This is getting a bit messy so using a larger base width might be the simpler solution after all, and probably good enough for our use case.

Open to ideas/thoughts. I can't currently think of a way to detect this behavior.

Also, there's always the option to just give up on using transform and going with the positioning strategy which, while worse performant than transform, is probably absolutely fine. This is kind of annoying :/

### DaniGuardiola on 2024-09-02T11:50:59Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324564169

Final (?) update: I ended up going with @t-hamano's suggestion of using a larger base value, and it works great!

Also @ciampo I DRYed up the RTL styling logic, check it out. This is ready for final (?) review!

### t-hamano on 2024-09-02T14:40:48Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324904256

Finally: As mentioned in [this comment](https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324367913), I think the changelog needs to be updated.

### t-hamano on 2024-09-02T15:03:12Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324946660

> it looks like the indicator is not correct when tabs are rendered inside a popover

I can't reproduce the issue now 🤔 Is ​​it a browser-specific issue?

### DaniGuardiola on 2024-09-02T15:13:37Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324964440

can't repro either

(╯°□°）╯︵ ┻━┻

### ciampo on 2024-09-02T15:17:10Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2324970229

Let me try to nuke my local env, rebuild everything, install fresh WP, and test again.

### ciampo on 2024-09-02T15:44:00Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325010643

Tested again on a fresh install — I can still reproduce on latest Chrome (version `128.0.6613.114`), Firefox and Safari MacOs 🤷 

@WordPress/gutenberg-components am I the only one?

### t-hamano on 2024-09-02T15:58:21Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325029797

The latest commit hash for this PR is 4db2b351ce, is the issue occurring as of this commit?

### ciampo on 2024-09-02T16:29:54Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325073660

Yup, that's the commit. 

Are you both testing changes on Windows, or is @DaniGuardiola testing on MacOS? Just trying to find what could be the difference.

https://github.com/user-attachments/assets/38fd0b28-6718-4563-a476-dc8b1e823bec

### DaniGuardiola on 2024-09-02T16:44:32Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325090687

@ciampo tested in Mac.

### t-hamano on 2024-09-02T16:54:28Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325101095

Can you reproduce this issue in Playground? If you enter the PR number, we all should be able to test it in the same environment.

https://playground.wordpress.net/gutenberg.html


https://github.com/user-attachments/assets/da6a02f1-1ac8-4e10-a52b-6d42b77cf57d

### DaniGuardiola on 2024-09-02T17:23:01Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325126966

Tested on Windows now using the playground, still can't repro.

### ciampo on 2024-09-02T19:30:04Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325232262

I can 🤷 


https://github.com/user-attachments/assets/3925c419-324f-4e08-9f82-acfd0250cda6

### ciampo on 2024-09-02T19:32:52Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325234634

I also tested on latest chrome on MacOS Sequoia via browserstack (free trial), and still managed to repro 🤷 

![Screenshot 2024-09-02 at 21 32 02](https://github.com/user-attachments/assets/7216c942-6720-49e7-be36-a6bbfda9308a)

### DaniGuardiola on 2024-09-02T19:33:50Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325235349

HOW

### tyxla on 2024-09-02T20:01:02Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325255515

Very easy to repro with the latest Chrome in macOS:

https://github.com/user-attachments/assets/596782cd-5cda-4a65-b3c7-28a36db29baf

### DaniGuardiola on 2024-09-02T22:17:14Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2325341775

AH! Needed to set a gradient, I was missing that. I'm onto something now...

![image](https://github.com/user-attachments/assets/e8a38f2e-4537-48a3-bb15-93b17aab145d)

### t-hamano on 2024-09-03T10:37:03Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2326187420

I looked into the cause of the problem and found that when the `Tabs` component is rendered in the `Popover` component, `rect.width` and `rect.height` becomes zero [here](https://github.com/WordPress/gutenberg/pull/64926/files#diff-5a5a740f296a0fb34ea44283d6e5ab1d4a96f8af6ac06db3dd003681c8d99765R179).

This may lead to some CSS variables becoming `NaN` as a result.

Perhaps the problem is related to the animation. If I enable `prefers-reduced-motion` to disable the popover animation, the issue does not occur. Is there any way to solve this issue..?

https://github.com/user-attachments/assets/6abbef54-20f4-4d9a-b3fc-fb8cad888243

### DaniGuardiola on 2024-09-09T11:56:46Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2337923079

I've updated the PR with two details:

- Synced the `useResizeObserver` hook with the latest version from #64943, as the ultimate goal is to remove it from here and use it from `@wordpress/compose`.
- Added a system to `useTrackElementOffsetRect` for resilience to an edge case (like the one we're running into with the background color popover) by attempting to measure again after a frame, and if that fails, polling until it succeeds. Thanks @ciampo for pairing with me to look into this, and @t-hamano for helping out as well.

If you look at the popover now, you'll see that it's properly sized. In this case, the first fallback (requestAnimationFrame) succeeds. Polling is really just a last resort fallback, in case there's a similar case where there's also an animation delay. It should be really rare, if it ever happens at all, but at least we can rest assured it will never break the indicator. It also should not be too heavy on the CPU, 10 measurements per second seems like a good way to balance performance with speed so that there's no significant flashes or delays for the user.

### DaniGuardiola on 2024-09-10T00:24:27Z

URL: https://github.com/WordPress/gutenberg/pull/64926#issuecomment-2339378096

Update: I have now removed the duplicated utility since https://github.com/WordPress/gutenberg/pull/64943 has been merged.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2024-08-30T12:01:27Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1738503601
Path: packages/components/src/utils/element-rect.ts

```diff
@@ -152,28 +150,44 @@ export const NULL_ELEMENT_OFFSET_RECT = {
 
 /**
  * Returns the position and dimensions of an element, relative to its offset
- * parent. This is useful in contexts where `getBoundingClientRect` is not
- * suitable, such as when the element is transformed.
+ * parent, with subpixel precision. Values reflect the real measures before any
+ * potential scaling distortions along the X and Y axes.
  *
- * **Note:** the `left` and `right` values are adjusted due to a limitation
- * in the way the browser calculates the offset position of the element,
- * which can cause unwanted scrollbars to appear. This adjustment makes the
- * values potentially inaccurate within a range of 1 pixel.
+ * Useful in contexts where plain `getBoundingClientRect` calls or `ResizeObserver`
+ * entries are not suitable, such as when the element is transformed, and when
+ * `element.offset<Top|Left|Width|Height>` methods are not precise enough.
  */
 export function getElementOffsetRect(
 	element: HTMLElement
 ): ElementOffsetRect {
+	// Position and dimension values computed with `getBoundingClientRect` have
+	// subpixel precision, but are affected by distortions since they represent
+	// the "real" measures, or in other words, the actual final values as rendered
+	// by the browser.
+	const rect = element.getBoundingClientRect();
+	const offsetParentRect =
+		element.offsetParent?.getBoundingClientRect() ??
```

If the offset parent also was affected by a transform compared to its own offset parent, would that have an impact? In other words, do we need recursion going up the DOM tree?

#### DaniGuardiola on 2024-08-30T15:24:13Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1738926614
Path: packages/components/src/utils/element-rect.ts

```diff
@@ -152,28 +150,44 @@ export const NULL_ELEMENT_OFFSET_RECT = {
 
 /**
  * Returns the position and dimensions of an element, relative to its offset
- * parent. This is useful in contexts where `getBoundingClientRect` is not
- * suitable, such as when the element is transformed.
+ * parent, with subpixel precision. Values reflect the real measures before any
+ * potential scaling distortions along the X and Y axes.
  *
- * **Note:** the `left` and `right` values are adjusted due to a limitation
- * in the way the browser calculates the offset position of the element,
- * which can cause unwanted scrollbars to appear. This adjustment makes the
- * values potentially inaccurate within a range of 1 pixel.
+ * Useful in contexts where plain `getBoundingClientRect` calls or `ResizeObserver`
+ * entries are not suitable, such as when the element is transformed, and when
+ * `element.offset<Top|Left|Width|Height>` methods are not precise enough.
  */
 export function getElementOffsetRect(
 	element: HTMLElement
 ): ElementOffsetRect {
+	// Position and dimension values computed with `getBoundingClientRect` have
+	// subpixel precision, but are affected by distortions since they represent
+	// the "real" measures, or in other words, the actual final values as rendered
+	// by the browser.
+	const rect = element.getBoundingClientRect();
+	const offsetParentRect =
+		element.offsetParent?.getBoundingClientRect() ??
```

This is something I thought of, and it just seems like an edge case we just don't have. Even then, it seems like a relative measure between parent and child would be of little use in a context where the parent and the child have different scaling values applied.

### Review thread 2 (resolved)

#### ciampo on 2024-08-30T16:15:56Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1739050794
Path: packages/components/src/tabs/styles.ts

```diff
@@ -24,7 +24,7 @@ export const TabListWrapper = styled.div`
 
 	@media not ( prefers-reduced-motion: reduce ) {
```

```suggestion
	@media not ( prefers-reduced-motion ) {
```

#### DaniGuardiola on 2024-08-30T17:11:20Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1739150968
Path: packages/components/src/tabs/styles.ts

```diff
@@ -24,7 +24,7 @@ export const TabListWrapper = styled.div`
 
 	@media not ( prefers-reduced-motion: reduce ) {
```

good catch!

### Review thread 3 (resolved)

#### ciampo on 2024-09-02T10:30:55Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740685483
Path: packages/components/src/tabs/styles.ts

```diff
@@ -33,25 +33,38 @@ export const TabListWrapper = styled.div`
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: top left;
 
 		// Windows high contrast mode.
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		&::after {
+			bottom: 0;
+			height: 0;
+			width: 1px;
+			transform: translateX( calc( var( --indicator-left ) * 1px ) )
+				scaleX( var( --indicator-width ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
+
+		&:dir( rtl )::after {
+			right: 0;
```

Instead of setting `right` here as a RTL exception, could we use `inset-inline-start` ?

#### DaniGuardiola on 2024-09-02T10:41:33Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740697853
Path: packages/components/src/tabs/styles.ts

```diff
@@ -33,25 +33,38 @@ export const TabListWrapper = styled.div`
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: top left;
 
 		// Windows high contrast mode.
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		&::after {
+			bottom: 0;
+			height: 0;
+			width: 1px;
+			transform: translateX( calc( var( --indicator-left ) * 1px ) )
+				scaleX( var( --indicator-width ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
+
+		&:dir( rtl )::after {
+			right: 0;
```

Actually I just realized none are necessary as the pseudo-element naturally falls in the right place by default. So I'll just remove the property 👍

### Review thread 4 (resolved)

#### ciampo on 2024-09-02T10:31:39Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740686422
Path: packages/components/src/tabs/styles.ts

```diff
@@ -33,25 +33,38 @@ export const TabListWrapper = styled.div`
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: top left;
 
 		// Windows high contrast mode.
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		&::after {
+			bottom: 0;
+			height: 0;
+			width: 1px;
+			transform: translateX( calc( var( --indicator-left ) * 1px ) )
+				scaleX( var( --indicator-width ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
+
+		&:dir( rtl )::after {
+			right: 0;
+			transform-origin: top right;
```

I wonder if there is a logical version to `transform-origin` ? Maybe using numbers/percentages instead of keywords?

#### DaniGuardiola on 2024-09-02T10:46:55Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740705630
Path: packages/components/src/tabs/styles.ts

```diff
@@ -33,25 +33,38 @@ export const TabListWrapper = styled.div`
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: top left;
 
 		// Windows high contrast mode.
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		&::after {
+			bottom: 0;
+			height: 0;
+			width: 1px;
+			transform: translateX( calc( var( --indicator-left ) * 1px ) )
+				scaleX( var( --indicator-width ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
+
+		&:dir( rtl )::after {
+			right: 0;
+			transform-origin: top right;
```

That'd be cool but it doesn't seem to be the case. I think it'd need to be a `start` or `end` keyword.

### Review thread 5 (resolved)

#### ciampo on 2024-09-02T10:32:55Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740687858
Path: packages/components/src/utils/element-rect.ts

```diff
@@ -144,36 +152,56 @@ export type ElementOffsetRect = {
  * An `ElementOffsetRect` object with all values set to zero.
  */
 export const NULL_ELEMENT_OFFSET_RECT = {
-	left: 0,
 	top: 0,
+	right: 0,
+	bottom: 0,
+	left: 0,
 	width: 0,
 	height: 0,
 } satisfies ElementOffsetRect;
 
 /**
  * Returns the position and dimensions of an element, relative to its offset
- * parent. This is useful in contexts where `getBoundingClientRect` is not
- * suitable, such as when the element is transformed.
+ * parent, with subpixel precision. Values reflect the real measures before any
+ * potential scaling distortions along the X and Y axes.
  *
- * **Note:** the `left` and `right` values are adjusted due to a limitation
- * in the way the browser calculates the offset position of the element,
- * which can cause unwanted scrollbars to appear. This adjustment makes the
- * values potentially inaccurate within a range of 1 pixel.
+ * Useful in contexts where plain `getBoundingClientRect` calls or `ResizeObserver`
+ * entries are not suitable, such as when the element is transformed, and when
+ * `element.offset<Top|Left|Width|Height>` methods are not precise enough.
  */
 export function getElementOffsetRect(
 	element: HTMLElement
 ): ElementOffsetRect {
+	// Position and dimension values computed with `getBoundingClientRect` have
+	// subpixel precision, but are affected by distortions since they represent
+	// the "real" measures, or in other words, the actual final values as rendered
+	// by the browser.
+	const rect = element.getBoundingClientRect();
+	const offsetParentRect =
+		element.offsetParent?.getBoundingClientRect() ??
+		NULL_ELEMENT_OFFSET_RECT;
+
+	// Computed widths and heights have subpixel precision, and are not affected
+	// by distortions.
+	const computedWidth = parseFloat( getComputedStyle( element ).width );
+	const computedHeight = parseFloat( getComputedStyle( element ).height );
+
+	// We can obtain the current scale factor for the element by comparing "computed"
+	// dimensions with the "real" ones.
+	const scaleX = computedWidth / rect.width;
+	const scaleY = computedHeight / rect.height;
+
 	return {
-		// The adjustments mentioned in the documentation above are necessary
-		// because `offsetLeft` and `offsetTop` are rounded to the nearest pixel,
-		// which can result in a position mismatch that causes unwanted overflow.
-		// For context, see: https://github.com/WordPress/gutenberg/pull/61979
-		left: Math.max( element.offsetLeft - 1, 0 ),
-		top: Math.max( element.offsetTop - 1, 0 ),
-		// This is a workaround to obtain these values with a sub-pixel precision,
-		// since `offsetWidth` and `offsetHeight` are rounded to the nearest pixel.
-		width: parseFloat( getComputedStyle( element ).width ),
-		height: parseFloat( getComputedStyle( element ).height ),
+		// To obtain the right values for the position:
```

Nit — but just to disambiguate in a context where `right` is used for different meanings

```suggestion
		// To obtain the correct values for the position:
```

#### DaniGuardiola on 2024-09-02T10:47:02Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740705898
Path: packages/components/src/utils/element-rect.ts

```diff
@@ -144,36 +152,56 @@ export type ElementOffsetRect = {
  * An `ElementOffsetRect` object with all values set to zero.
  */
 export const NULL_ELEMENT_OFFSET_RECT = {
-	left: 0,
 	top: 0,
+	right: 0,
+	bottom: 0,
+	left: 0,
 	width: 0,
 	height: 0,
 } satisfies ElementOffsetRect;
 
 /**
  * Returns the position and dimensions of an element, relative to its offset
- * parent. This is useful in contexts where `getBoundingClientRect` is not
- * suitable, such as when the element is transformed.
+ * parent, with subpixel precision. Values reflect the real measures before any
+ * potential scaling distortions along the X and Y axes.
  *
- * **Note:** the `left` and `right` values are adjusted due to a limitation
- * in the way the browser calculates the offset position of the element,
- * which can cause unwanted scrollbars to appear. This adjustment makes the
- * values potentially inaccurate within a range of 1 pixel.
+ * Useful in contexts where plain `getBoundingClientRect` calls or `ResizeObserver`
+ * entries are not suitable, such as when the element is transformed, and when
+ * `element.offset<Top|Left|Width|Height>` methods are not precise enough.
  */
 export function getElementOffsetRect(
 	element: HTMLElement
 ): ElementOffsetRect {
+	// Position and dimension values computed with `getBoundingClientRect` have
+	// subpixel precision, but are affected by distortions since they represent
+	// the "real" measures, or in other words, the actual final values as rendered
+	// by the browser.
+	const rect = element.getBoundingClientRect();
+	const offsetParentRect =
+		element.offsetParent?.getBoundingClientRect() ??
+		NULL_ELEMENT_OFFSET_RECT;
+
+	// Computed widths and heights have subpixel precision, and are not affected
+	// by distortions.
+	const computedWidth = parseFloat( getComputedStyle( element ).width );
+	const computedHeight = parseFloat( getComputedStyle( element ).height );
+
+	// We can obtain the current scale factor for the element by comparing "computed"
+	// dimensions with the "real" ones.
+	const scaleX = computedWidth / rect.width;
+	const scaleY = computedHeight / rect.height;
+
 	return {
-		// The adjustments mentioned in the documentation above are necessary
-		// because `offsetLeft` and `offsetTop` are rounded to the nearest pixel,
-		// which can result in a position mismatch that causes unwanted overflow.
-		// For context, see: https://github.com/WordPress/gutenberg/pull/61979
-		left: Math.max( element.offsetLeft - 1, 0 ),
-		top: Math.max( element.offsetTop - 1, 0 ),
-		// This is a workaround to obtain these values with a sub-pixel precision,
-		// since `offsetWidth` and `offsetHeight` are rounded to the nearest pixel.
-		width: parseFloat( getComputedStyle( element ).width ),
-		height: parseFloat( getComputedStyle( element ).height ),
+		// To obtain the right values for the position:
```

Good idea.

#### DaniGuardiola on 2024-09-02T10:50:40Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740712709
Path: packages/components/src/utils/element-rect.ts

```diff
@@ -144,36 +152,56 @@ export type ElementOffsetRect = {
  * An `ElementOffsetRect` object with all values set to zero.
  */
 export const NULL_ELEMENT_OFFSET_RECT = {
-	left: 0,
 	top: 0,
+	right: 0,
+	bottom: 0,
+	left: 0,
 	width: 0,
 	height: 0,
 } satisfies ElementOffsetRect;
 
 /**
  * Returns the position and dimensions of an element, relative to its offset
- * parent. This is useful in contexts where `getBoundingClientRect` is not
- * suitable, such as when the element is transformed.
+ * parent, with subpixel precision. Values reflect the real measures before any
+ * potential scaling distortions along the X and Y axes.
  *
- * **Note:** the `left` and `right` values are adjusted due to a limitation
- * in the way the browser calculates the offset position of the element,
- * which can cause unwanted scrollbars to appear. This adjustment makes the
- * values potentially inaccurate within a range of 1 pixel.
+ * Useful in contexts where plain `getBoundingClientRect` calls or `ResizeObserver`
+ * entries are not suitable, such as when the element is transformed, and when
+ * `element.offset<Top|Left|Width|Height>` methods are not precise enough.
  */
 export function getElementOffsetRect(
 	element: HTMLElement
 ): ElementOffsetRect {
+	// Position and dimension values computed with `getBoundingClientRect` have
+	// subpixel precision, but are affected by distortions since they represent
+	// the "real" measures, or in other words, the actual final values as rendered
+	// by the browser.
+	const rect = element.getBoundingClientRect();
+	const offsetParentRect =
+		element.offsetParent?.getBoundingClientRect() ??
+		NULL_ELEMENT_OFFSET_RECT;
+
+	// Computed widths and heights have subpixel precision, and are not affected
+	// by distortions.
+	const computedWidth = parseFloat( getComputedStyle( element ).width );
+	const computedHeight = parseFloat( getComputedStyle( element ).height );
+
+	// We can obtain the current scale factor for the element by comparing "computed"
+	// dimensions with the "real" ones.
+	const scaleX = computedWidth / rect.width;
+	const scaleY = computedHeight / rect.height;
+
 	return {
-		// The adjustments mentioned in the documentation above are necessary
-		// because `offsetLeft` and `offsetTop` are rounded to the nearest pixel,
-		// which can result in a position mismatch that causes unwanted overflow.
-		// For context, see: https://github.com/WordPress/gutenberg/pull/61979
-		left: Math.max( element.offsetLeft - 1, 0 ),
-		top: Math.max( element.offsetTop - 1, 0 ),
-		// This is a workaround to obtain these values with a sub-pixel precision,
-		// since `offsetWidth` and `offsetHeight` are rounded to the nearest pixel.
-		width: parseFloat( getComputedStyle( element ).width ),
-		height: parseFloat( getComputedStyle( element ).height ),
+		// To obtain the right values for the position:
```

Went with "adjusted".

### Review thread 6 (resolved)

#### t-hamano on 2024-09-02T12:21:06Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740831322
Path: packages/components/src/tabs/stories/index.story.tsx

```diff
@@ -70,6 +70,15 @@ const Template: StoryFn< typeof Tabs > = ( props ) => {
 
 export const Default = Template.bind( {} );
 
+export const RTL = () => {
+	return (
+		<div dir="rtl" style={ { padding: '1rem' } }>
+			{ /* @ts-expect-error - This is a temporary hack until we migrate to CSF 3 and my sanity is restored. */ }
+			<Template />
+		</div>
+	);
+};
+
```

Since Storybook currently has an option to switch language direction, is this unnecessary?

![a9a889fac500c8c4515d78370e86c5b5](https://github.com/user-attachments/assets/962e6fe7-bc36-428a-8c50-cf3f7a3c31bb)

#### DaniGuardiola on 2024-09-02T12:38:50Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740864616
Path: packages/components/src/tabs/stories/index.story.tsx

```diff
@@ -70,6 +70,15 @@ const Template: StoryFn< typeof Tabs > = ( props ) => {
 
 export const Default = Template.bind( {} );
 
+export const RTL = () => {
+	return (
+		<div dir="rtl" style={ { padding: '1rem' } }>
+			{ /* @ts-expect-error - This is a temporary hack until we migrate to CSF 3 and my sanity is restored. */ }
+			<Template />
+		</div>
+	);
+};
+
```

TIL! Removing the story.

#### DaniGuardiola on 2024-09-02T13:41:38Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740945489
Path: packages/components/src/tabs/stories/index.story.tsx

```diff
@@ -70,6 +70,15 @@ const Template: StoryFn< typeof Tabs > = ( props ) => {
 
 export const Default = Template.bind( {} );
 
+export const RTL = () => {
+	return (
+		<div dir="rtl" style={ { padding: '1rem' } }>
+			{ /* @ts-expect-error - This is a temporary hack until we migrate to CSF 3 and my sanity is restored. */ }
+			<Template />
+		</div>
+	);
+};
+
```

Done

### Review thread 7 (resolved)

#### t-hamano on 2024-09-02T12:24:29Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740838065
Path: packages/components/src/tabs/styles.ts

```diff
@@ -38,20 +38,41 @@ export const TabListWrapper = styled.div`
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
+			--direction-factor: -1;
+			--indicator-start: var( --indicator-right );
+		}
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

```suggestion
		transform: translateY( calc( var( --indicator-top ) * 1px ) )
			scaleY( calc( var( --indicator-height ) / 100 ) );
		transform-origin: top;
```

In a vertical layout, I think we need to start from the top. Otherwise, the background color will be misaligned:

![image](https://github.com/user-attachments/assets/3e68058b-72b8-4403-b5ae-f8e7431b0564)

```

#### DaniGuardiola on 2024-09-02T12:38:39Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740864383
Path: packages/components/src/tabs/styles.ts

```diff
@@ -38,20 +38,41 @@ export const TabListWrapper = styled.div`
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
+			--direction-factor: -1;
+			--indicator-start: var( --indicator-right );
+		}
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

We start from the top already. Can't repro this.

#### t-hamano on 2024-09-02T13:09:25Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740903982
Path: packages/components/src/tabs/styles.ts

```diff
@@ -38,20 +38,41 @@ export const TabListWrapper = styled.div`
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
+			--direction-factor: -1;
+			--indicator-start: var( --indicator-right );
+		}
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

Strangely enough, I can reproduce this in my Storybook. The initial value of `transform-origin` is `50% 50% 0`, and `transform-origin` does not seem to be applied 🤔

![tabs](https://github.com/user-attachments/assets/199ede36-b02a-411e-a1da-fc4c37f74cda)

![image](https://github.com/user-attachments/assets/943662eb-fee9-4795-84cb-6a4fdab330b9)

I can also see the problem in the Preferences panel of the block editor:

#### DaniGuardiola on 2024-09-02T13:35:59Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740938668
Path: packages/components/src/tabs/styles.ts

```diff
@@ -38,20 +38,41 @@ export const TabListWrapper = styled.div`
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
+			--direction-factor: -1;
+			--indicator-start: var( --indicator-right );
+		}
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

Are you sure you're in the latest version of the branch? Can you check that you have the `transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;` like in the styles source? Can you investigate whether it's applied correctly? Depending on the `--direction-factor` variable, the result for the first parameter (X axis) should be 0% (LTR) or 100% (RTL), and the second (Y axis) should always be 0% since it's a static value.

#### DaniGuardiola on 2024-09-02T13:37:49Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740940922
Path: packages/components/src/tabs/styles.ts

```diff
@@ -38,20 +38,41 @@ export const TabListWrapper = styled.div`
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
+			--direction-factor: -1;
+			--indicator-start: var( --indicator-right );
+		}
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

Wait, I managed to repro. Weird, let me take a look.

#### DaniGuardiola on 2024-09-02T13:41:26Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740945271
Path: packages/components/src/tabs/styles.ts

```diff
@@ -38,20 +38,41 @@ export const TabListWrapper = styled.div`
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
+			--direction-factor: -1;
+			--indicator-start: var( --indicator-right );
+		}
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

Alright, it was a dumb mistake I made haha. I placed the origin only in the horizontal tabs styles instead of in the more global scope. Fixed now!

### Review thread 8 (resolved)

#### t-hamano on 2024-09-02T14:13:26Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740983454
Path: packages/components/src/tabs/styles.ts

```diff
@@ -34,24 +34,46 @@ export const TabListWrapper = styled.div`
 		position: absolute;
 		pointer-events: none;
 
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
```

Due to the influence of c347fad, this selector is now applied to the `::after` pseudo-element. As I understand it, we cannot use `:dir()` on the pseudo-element itself. As a result, in RTL languages, `--direction-factor` remains `1` instead of `-1`, and the indicator is not displayed 😅

![direction-factor](https://github.com/user-attachments/assets/21e07f18-e95a-412d-841b-94d6c1959397)

#### DaniGuardiola on 2024-09-02T14:23:27Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1740995010
Path: packages/components/src/tabs/styles.ts

```diff
@@ -34,24 +34,46 @@ export const TabListWrapper = styled.div`
 		position: absolute;
 		pointer-events: none;
 
+		--direction-factor: 1;
+		--indicator-start: var( --indicator-left );
+		&:dir( rtl ) {
```

Good catch lol. Fixed!

### Review thread 9

#### ciampo on 2024-09-02T14:47:09Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1741021702
Path: packages/components/src/tabs/styles.ts

```diff
@@ -22,36 +22,57 @@ export const TabListWrapper = styled.div`
 		text-align: start;
 	}
 
-	@media not ( prefers-reduced-motion: reduce ) {
+	@media not ( prefers-reduced-motion ) {
 		&.is-animation-enabled::after {
-			transition-property: left, top, width, height;
+			transition-property: transform;
 			transition-duration: 0.2s;
 			transition-timing-function: ease-out;
 		}
 	}
+	--direction-factor: 1;
+	--indicator-start: var( --indicator-left );
+	&:dir( rtl ) {
+		--direction-factor: -1;
+		--indicator-start: var( --indicator-right );
+	}
 	&::after {
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
```

This may be tricky to understand for a developer with fresh eyes — should we add a comment explaining that, based on the writing direction, the x-axis transform origin may be either `0%` or `100%` ?

#### DaniGuardiola on 2024-09-02T15:14:38Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1741055835
Path: packages/components/src/tabs/styles.ts

```diff
@@ -22,36 +22,57 @@ export const TabListWrapper = styled.div`
 		text-align: start;
 	}
 
-	@media not ( prefers-reduced-motion: reduce ) {
+	@media not ( prefers-reduced-motion ) {
 		&.is-animation-enabled::after {
-			transition-property: left, top, width, height;
+			transition-property: transform;
 			transition-duration: 0.2s;
 			transition-timing-function: ease-out;
 		}
 	}
+	--direction-factor: 1;
+	--indicator-start: var( --indicator-left );
+	&:dir( rtl ) {
+		--direction-factor: -1;
+		--indicator-start: var( --indicator-right );
+	}
 	&::after {
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
```

Good point. Honestly I think I'm just gonna use another variable (something like `direction-x-origin`) and explicitly write the value. Readable > smart.

#### DaniGuardiola on 2024-09-02T15:16:44Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1741057717
Path: packages/components/src/tabs/styles.ts

```diff
@@ -22,36 +22,57 @@ export const TabListWrapper = styled.div`
 		text-align: start;
 	}
 
-	@media not ( prefers-reduced-motion: reduce ) {
+	@media not ( prefers-reduced-motion ) {
 		&.is-animation-enabled::after {
-			transition-property: left, top, width, height;
+			transition-property: transform;
 			transition-duration: 0.2s;
 			transition-timing-function: ease-out;
 		}
 	}
+	--direction-factor: 1;
+	--indicator-start: var( --indicator-left );
+	&:dir( rtl ) {
+		--direction-factor: -1;
+		--indicator-start: var( --indicator-right );
+	}
 	&::after {
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: calc( ( 1 - var( --direction-factor ) ) * 50% ) 0%;
```

I did it, let me know what you think.

### Review thread 10 (resolved)

#### ciampo on 2024-09-10T17:00:49Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1752333282
Path: packages/components/src/tabs/styles.ts

```diff
@@ -22,36 +22,59 @@ export const TabListWrapper = styled.div`
 		text-align: start;
 	}
 
-	@media not ( prefers-reduced-motion: reduce ) {
+	@media not ( prefers-reduced-motion ) {
 		&.is-animation-enabled::after {
-			transition-property: left, top, width, height;
+			transition-property: transform;
 			transition-duration: 0.2s;
 			transition-timing-function: ease-out;
 		}
 	}
+	--direction-factor: 1;
+	--direction-origin-x: left;
+	--indicator-start: var( --indicator-left );
+	&:dir( rtl ) {
+		--direction-factor: -1;
+		--direction-origin-x: right;
+		--indicator-start: var( --indicator-right );
+	}
 	&::after {
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: var( --direction-origin-x ) top;
 
 		// Windows high contrast mode.
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
```

Nit: if the `width: 100px` and the division by `100` are related because of the anti-aliasing fix, we could extract `100` to a CSS variable to better show what's going on.

### Review thread 11 (resolved)

#### ciampo on 2024-09-10T17:01:32Z

URL: https://github.com/WordPress/gutenberg/pull/64926#discussion_r1752334168
Path: packages/components/src/tabs/styles.ts

```diff
@@ -22,36 +22,59 @@ export const TabListWrapper = styled.div`
 		text-align: start;
 	}
 
-	@media not ( prefers-reduced-motion: reduce ) {
+	@media not ( prefers-reduced-motion ) {
 		&.is-animation-enabled::after {
-			transition-property: left, top, width, height;
+			transition-property: transform;
 			transition-duration: 0.2s;
 			transition-timing-function: ease-out;
 		}
 	}
+	--direction-factor: 1;
+	--direction-origin-x: left;
+	--indicator-start: var( --indicator-left );
+	&:dir( rtl ) {
+		--direction-factor: -1;
+		--direction-origin-x: right;
+		--indicator-start: var( --indicator-right );
+	}
 	&::after {
 		content: '';
 		position: absolute;
 		pointer-events: none;
+		transform-origin: var( --direction-origin-x ) top;
 
 		// Windows high contrast mode.
 		outline: 2px solid transparent;
 		outline-offset: -1px;
 	}
-	&:not( [aria-orientation='vertical'] )::after {
-		bottom: 0;
-		left: var( --indicator-left );
-		width: var( --indicator-width );
-		height: 0;
-		border-bottom: var( --wp-admin-border-width-focus ) solid
-			${ COLORS.theme.accent };
+	&:not( [aria-orientation='vertical'] ) {
+		&::after {
+			bottom: 0;
+			height: 0;
+			/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+			width: 100px;
+			transform: translateX(
+					calc(
+						var( --indicator-start ) * var( --direction-factor ) *
+							1px
+					)
+				)
+				scaleX( calc( var( --indicator-width ) / 100 ) );
+			border-bottom: var( --wp-admin-border-width-focus ) solid
+				${ COLORS.theme.accent };
+		}
 	}
 	&[aria-orientation='vertical']::after {
 		z-index: -1;
+		top: 0;
 		left: 0;
 		width: 100%;
-		top: var( --indicator-top );
-		height: var( --indicator-height );
+		/* Using a large value to avoid antialiasing rounding issues
+			when scaling in the transform, see: https://stackoverflow.com/a/52159123 */
+		height: 100px;
+		transform: translateY( calc( var( --indicator-top ) * 1px ) )
+			scaleY( calc( var( --indicator-height ) / 100 ) );
```

Same here re. potentially extracting the `100` anti-aliasing factor

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +5/-0)
- packages/components/src/tabs/styles.ts (MODIFIED, +40/-11)
- packages/components/src/tabs/tablist.tsx (MODIFIED, +5/-4)
- packages/components/src/utils/element-rect.ts (MODIFIED, +93/-130)
