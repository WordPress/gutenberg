---
number: 11404
type: pull_request
state: MERGED
merged_at: 2018-11-09T14:44:44Z
url: https://github.com/WordPress/gutenberg/pull/11404
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Type] Developer Documentation","[Package] Components"]
---

# PR #11404: Update: Font Size Picker component documentation

- URL: https://github.com/WordPress/gutenberg/pull/11404
- Author: jorgefilipecosta
- Created: 2018-11-02T12:16:02Z
- Updated: 2018-11-09T17:31:20Z
- Comments: 1 of 1
- Review threads: 6 of 6
- Changed files: 2 of 2
- Diff: ../pr-diffs/11404.diff

## Body

## Description
This pr updates the documentation of @wordpress/components/FontSizePicker.

The component was updated with additional props added and the structure of existing props changed but the updates to the docs were missing.

## How has this been tested?
Just documentation updates. I checked the sample code works and does not contain linting error, by pasting it in an existing block.

## Issue comments

### jorgefilipecosta on 2018-11-09T14:44:59Z

URL: https://github.com/WordPress/gutenberg/pull/11404#issuecomment-437380323

Thank you for your review @chrisvanpatten!

## Review threads

### Review thread 1

#### ZebulanStanphill on 2018-11-02T14:25:31Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r230391630
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -25,7 +25,7 @@ function FontSizePicker( {
 	disableCustomFontSizes = false,
 	onChange,
 	value,
-	withSlider,
+	withSlider = false,
```

Is this supposed to be part of this PR?

#### jorgefilipecosta on 2018-11-08T23:23:09Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232096819
Path: packages/components/src/font-size-picker/index.js

```diff
@@ -25,7 +25,7 @@ function FontSizePicker( {
 	disableCustomFontSizes = false,
 	onChange,
 	value,
-	withSlider,
+	withSlider = false,
```

Hi @ZebulanStanphill,
By default withSlider was already "falsy" but not explicitly. If we add an explicit false value, we can document that it is false by default. I think documenting this is helpful for the users, that is the reason this change was added here.

### Review thread 2 (resolved)

#### chrisvanpatten on 2018-11-09T13:00:31Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232244730
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
```

```suggestion
If `true`, it will not be possible to choose a custom fontSize. The user will be forced to pick one of the pre-defined sizes passed in fontSizes.
```

### Review thread 3 (resolved)

#### chrisvanpatten on 2018-11-09T13:01:10Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232244903
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
```

```suggestion
If no value exists, this prop defines the starting position for the font size picker slider. Only relevant if `withSlider` is `true`.
```

### Review thread 4 (resolved)

#### chrisvanpatten on 2018-11-09T13:01:43Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232245037
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
 
 - Type: `Number`
 - Required: No
 
-### value
+### fontSizes
 
-The current font size value. If a button value matches the font size value that button is pressed. RangeControl is rendered with this value.
+An array of font size objects. The object should contain properties size and name.
+The property "size" contains a number with the font size value. The "name" property includes a label for that fontSize e.g: "Small".
```

```suggestion
The property `size` contains a number with the font size value, in `px`. The `name` property includes a label for that fontSize e.g: `Small`.
```

### Review thread 5

#### chrisvanpatten on 2018-11-09T13:03:18Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232245427
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
 
 - Type: `Number`
 - Required: No
 
-### value
+### fontSizes
 
-The current font size value. If a button value matches the font size value that button is pressed. RangeControl is rendered with this value.
+An array of font size objects. The object should contain properties size and name.
```

Do you also need to provide a "slug"? I might be wrong but I think it's expected in terms of generating CSS classes (although maybe that doesn't get passed into this version of the component?)

I'd also like to see an example here… happy to add that, if you want. Something simple like

```
{
    size: 12,
    name: "My Font Size",
    slug: "font-slug" // if needed
}
```

#### chrisvanpatten on 2018-11-09T13:03:58Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232245560
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
 
 - Type: `Number`
 - Required: No
 
-### value
+### fontSizes
 
-The current font size value. If a button value matches the font size value that button is pressed. RangeControl is rendered with this value.
+An array of font size objects. The object should contain properties size and name.
```

(I know we show it above, but being more explicit will be good for users who are newer to JS.)

#### jorgefilipecosta on 2018-11-09T13:40:18Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232255636
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
 
 - Type: `Number`
 - Required: No
 
-### value
+### fontSizes
 
-The current font size value. If a button value matches the font size value that button is pressed. RangeControl is rendered with this value.
+An array of font size objects. The object should contain properties size and name.
```

You are totally right I missed the slug property and it is used to generate classes in the component. The situation was resolved. thank you for catching this.

#### chrisvanpatten on 2018-11-09T13:52:48Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232259079
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
 
 - Type: `Number`
 - Required: No
 
-### value
+### fontSizes
 
-The current font size value. If a button value matches the font size value that button is pressed. RangeControl is rendered with this value.
+An array of font size objects. The object should contain properties size and name.
```

@jorgefilipecosta No prob! I also thought that it was possible we only used `slug` inside the editor version of this component so I'm glad I checked :)

### Review thread 6 (resolved)

#### chrisvanpatten on 2018-11-09T13:04:57Z

URL: https://github.com/WordPress/gutenberg/pull/11404#discussion_r232245812
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -10,57 +10,79 @@ The component renders a series of buttons that allow the user to select predefin
 import { FontSizePicker } from '@wordpress/components';
 import { withState } from '@wordpress/compose';
 
+...
 const MyFontSizePicker = withState( {
 	fontSize: 16,
-} )( ( { fontSize, setState } ) => { 
+} )( ( { fontSize, setState } ) => {
 	const fontSizes = [
-		{ shortName: 'S', size: 12 },
-		{ shortName: 'M', size: 16 }
+		{ name: 'Small', size: 12 },
+		{ name: 'Big', size: 26 },
 	];
 	const fallbackFontSize = 16;
-	
-	return ( 
-		<FontSizePicker 
-			fontSizes={ fontSizes } 
+
+	return (
+		<FontSizePicker
+			fontSizes={ fontSizes }
 			value={ fontSize }
 			fallbackFontSize={ fallbackFontSize }
-			onChange={ fontSize => setState( { fontSize } ) } 
+			onChange={ ( newFontSize ) => {
+				setState( { fontSize: newFontSize } );
+			} }
 		/>
-	); 
+	);
 } );
+
+...
+
+<MyFontSizePicker />
 ```
 
 ## Props
 
 The component accepts the following props:
 
-### fontSizes
+### disableCustomFontSizes
 
-An array of font size objects. The object should contain properties size, name, shortName.
-The property "size" contains a number with the font size value. The "shortName" property includes a small label used in the buttons. Property "name" is used as the label when shortName is not provided.
+If true it will not be possible to choose a custom fontSize, the user will be forced to pick one of the sizes passed in fontSizes.
 
-- Type: `Array`
-- Required: No
+- Type: `Boolean`
+- Required: no
+- Default: `false`
 
 ### fallbackFontSize
 
-In no value exists this prop contains the font size picker slider starting position.
+In no value exists this prop contains the font size picker slider starting position. Only relevant if withSlider is true.
 
 - Type: `Number`
 - Required: No
 
-### value
+### fontSizes
 
-The current font size value. If a button value matches the font size value that button is pressed. RangeControl is rendered with this value.
+An array of font size objects. The object should contain properties size and name.
+The property "size" contains a number with the font size value. The "name" property includes a label for that fontSize e.g: "Small".
 
-- Type: `Number`
+- Type: `Array`
 - Required: No
 
-## onChange
+### onChange
 
 A function that receives the new font size value.
 If onChange is called without any parameter, it should reset the value, attending to what reset means in that context, e.g., set the font size to undefined or set the font size a starting value.
 
 - Type: `function`
 - Required: Yes
 
+### value
+
+The current font size value.
+
+- Type: `Number`
+- Required: No
+
+### withSlider
+
+If true the UI will contain a slider. If false no slider will be present.
```

```suggestion
If `true`, the UI will contain a slider, instead of a numeric text input field. If `false`, no slider will be present.
```

## Changed files

- packages/components/src/font-size-picker/README.md (MODIFIED, +52/-19)
- packages/components/src/font-size-picker/index.js (MODIFIED, +1/-1)
