---
number: 56214
type: pull_request
state: MERGED
merged_at: 2023-12-05T07:43:12Z
url: https://github.com/WordPress/gutenberg/pull/56214
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #56214: Use tooltip for the Timezone only when necessary.

- URL: https://github.com/WordPress/gutenberg/pull/56214
- Author: afercia
- Created: 2023-11-16T16:13:05Z
- Updated: 2023-12-05T07:43:34Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/56214.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->
Fixes https://github.com/WordPress/gutenberg/issues/56211

## What?
<!-- In a few words, what is the PR actually doing? -->
The Timezone indication may render an unnecessary tooltip when the visible text and the tooltip text are the same.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
There is no need to repeat in the tooltip the same information that is already in the visible text e.g. `UTC+3` and again `UTC+3`.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
Checks whether the abbreviation and the detailed values contain the same information and add the tooltip conditionally.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->
 - Repeat the steps described in the issue https://github.com/WordPress/gutenberg/issues/56211
 - Observe that the tooltip shows only when the detailed information is actually different from the visible text.

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

## Issue comments

### github-actions on 2023-12-04T13:31:57Z

URL: https://github.com/WordPress/gutenberg/pull/56214#issuecomment-1838651402

<!-- flaky-tests-report-comment -->
**Flaky tests detected in a7dd3b9e1242d94c13cce46ededa45c10ce6d8ec.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/7087119295
📝  Reported issues:
- #53524 in `/test/e2e/specs/editor/various/multi-block-selection.spec.js`

## Review threads

### Review thread 1

#### ciampo on 2023-11-29T18:44:14Z

URL: https://github.com/WordPress/gutenberg/pull/56214#discussion_r1409724345
Path: packages/components/src/date-time/time/timezone.tsx

```diff
@@ -32,12 +32,22 @@ const TimeZone = () => {
 			? timezone.abbr
 			: `UTC${ offsetSymbol }${ timezone.offset }`;
 
+	// Replace underscore with space in strings like `America/Costa_Rica`.
+	const prettyTimezoneString = timezone.string.replace( '_', ' ' );
+
 	const timezoneDetail =
 		'UTC' === timezone.string
 			? __( 'Coordinated Universal Time' )
-			: `(${ zoneAbbr }) ${ timezone.string.replace( '_', ' ' ) }`;
+			: `(${ zoneAbbr }) ${ prettyTimezoneString }`;
```

```suggestion
			: `(${ zoneAbbr }) ${ prettyTimezoneString }`.trim();
```

### Review thread 2

#### ciampo on 2023-11-29T18:44:18Z

URL: https://github.com/WordPress/gutenberg/pull/56214#discussion_r1409724398
Path: packages/components/src/date-time/time/timezone.tsx

```diff
@@ -32,12 +32,22 @@ const TimeZone = () => {
 			? timezone.abbr
 			: `UTC${ offsetSymbol }${ timezone.offset }`;
 
+	// Replace underscore with space in strings like `America/Costa_Rica`.
+	const prettyTimezoneString = timezone.string.replace( '_', ' ' );
+
 	const timezoneDetail =
 		'UTC' === timezone.string
 			? __( 'Coordinated Universal Time' )
-			: `(${ zoneAbbr }) ${ timezone.string.replace( '_', ' ' ) }`;
+			: `(${ zoneAbbr }) ${ prettyTimezoneString }`;
+
+	const isAbbrSameAsDetails =
+		`${ zoneAbbr } ${ prettyTimezoneString }`.trim() === zoneAbbr;
```

Could we instead check for 

```suggestion
	const isAbbrSameAsDetails = timezoneDetail === zoneAbbr;
```

?

#### afercia on 2023-11-30T11:55:41Z

URL: https://github.com/WordPress/gutenberg/pull/56214#discussion_r1410566472
Path: packages/components/src/date-time/time/timezone.tsx

```diff
@@ -32,12 +32,22 @@ const TimeZone = () => {
 			? timezone.abbr
 			: `UTC${ offsetSymbol }${ timezone.offset }`;
 
+	// Replace underscore with space in strings like `America/Costa_Rica`.
+	const prettyTimezoneString = timezone.string.replace( '_', ' ' );
+
 	const timezoneDetail =
 		'UTC' === timezone.string
 			? __( 'Coordinated Universal Time' )
-			: `(${ zoneAbbr }) ${ timezone.string.replace( '_', ' ' ) }`;
+			: `(${ zoneAbbr }) ${ prettyTimezoneString }`;
+
+	const isAbbrSameAsDetails =
+		`${ zoneAbbr } ${ prettyTimezoneString }`.trim() === zoneAbbr;
```

It would not work because `timezoneDetail` may contain the parenthesis around `zoneAbbr` in fact the string in the ternary above contains parenthesis `( ... )` so that the comparison would happen between, for example:
`(UTC+5.30)` and `UTC+5.30` and thus be falsy.

#### afercia on 2023-11-30T12:05:32Z

URL: https://github.com/WordPress/gutenberg/pull/56214#discussion_r1410577031
Path: packages/components/src/date-time/time/timezone.tsx

```diff
@@ -32,12 +32,22 @@ const TimeZone = () => {
 			? timezone.abbr
 			: `UTC${ offsetSymbol }${ timezone.offset }`;
 
+	// Replace underscore with space in strings like `America/Costa_Rica`.
+	const prettyTimezoneString = timezone.string.replace( '_', ' ' );
+
 	const timezoneDetail =
 		'UTC' === timezone.string
 			? __( 'Coordinated Universal Time' )
-			: `(${ zoneAbbr }) ${ timezone.string.replace( '_', ' ' ) }`;
+			: `(${ zoneAbbr }) ${ prettyTimezoneString }`;
+
+	const isAbbrSameAsDetails =
+		`${ zoneAbbr } ${ prettyTimezoneString }`.trim() === zoneAbbr;
```

We could entirely remove the parenthesis maybe? They are only used in the Tooltip:

![Screenshot 2023-11-30 at 12 59 46](https://github.com/WordPress/gutenberg/assets/1682452/a1fe2b4b-87ec-4110-aebc-5ea958e34954)

This is how it would look without parenthesis:

![Screenshot 2023-11-30 at 13 02 53](https://github.com/WordPress/gutenberg/assets/1682452/45c3744b-be57-4700-85c2-8711a9ccb57d)

#### ciampo on 2023-11-30T20:46:50Z

URL: https://github.com/WordPress/gutenberg/pull/56214#discussion_r1411244955
Path: packages/components/src/date-time/time/timezone.tsx

```diff
@@ -32,12 +32,22 @@ const TimeZone = () => {
 			? timezone.abbr
 			: `UTC${ offsetSymbol }${ timezone.offset }`;
 
+	// Replace underscore with space in strings like `America/Costa_Rica`.
+	const prettyTimezoneString = timezone.string.replace( '_', ' ' );
+
 	const timezoneDetail =
 		'UTC' === timezone.string
 			? __( 'Coordinated Universal Time' )
-			: `(${ zoneAbbr }) ${ timezone.string.replace( '_', ' ' ) }`;
+			: `(${ zoneAbbr }) ${ prettyTimezoneString }`;
+
+	const isAbbrSameAsDetails =
+		`${ zoneAbbr } ${ prettyTimezoneString }`.trim() === zoneAbbr;
```

New idea that doesn't involve making formatting changes: could we check instead that 


```suggestion
	const isAbbrSameAsDetails = prettyTimezoneString.trim().length === 0;
```

#### afercia on 2023-12-04T13:14:02Z

URL: https://github.com/WordPress/gutenberg/pull/56214#discussion_r1413855942
Path: packages/components/src/date-time/time/timezone.tsx

```diff
@@ -32,12 +32,22 @@ const TimeZone = () => {
 			? timezone.abbr
 			: `UTC${ offsetSymbol }${ timezone.offset }`;
 
+	// Replace underscore with space in strings like `America/Costa_Rica`.
+	const prettyTimezoneString = timezone.string.replace( '_', ' ' );
+
 	const timezoneDetail =
 		'UTC' === timezone.string
 			? __( 'Coordinated Universal Time' )
-			: `(${ zoneAbbr }) ${ timezone.string.replace( '_', ' ' ) }`;
+			: `(${ zoneAbbr }) ${ prettyTimezoneString }`;
+
+	const isAbbrSameAsDetails =
+		`${ zoneAbbr } ${ prettyTimezoneString }`.trim() === zoneAbbr;
```

Ok. I renamed the variable and added a comment for better clarity.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/date-time/time/timezone.tsx (MODIFIED, +15/-3)
