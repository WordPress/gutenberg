---
number: 66974
type: pull_request
state: MERGED
merged_at: 2024-12-02T23:08:03Z
url: https://github.com/WordPress/gutenberg/pull/66974
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","First-time Contributor"]
---

# PR #66974: Added enableAlpha prop to CustomGradientPicker and GradientPicker components

- URL: https://github.com/WordPress/gutenberg/pull/66974
- Author: wwdes
- Created: 2024-11-13T16:44:35Z
- Updated: 2024-12-04T17:36:58Z
- Comments: 3 of 3
- Review threads: 7 of 7
- Changed files: 6 of 6
- Diff: ../pr-diffs/66974.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
This PR adds enableAlpha prop to GradientPicker component, which is already supported within CustomGradientBar subcomponent.

## Why?
Firstly, both ColorPicker and ColorPalette component's have this feature under enableAlppha prop, so adding it to GradientPicker would only be consistent. Secondly, I have found a use for this feature within my own project, where a block can not be allowed transparency, but wants to utilize other features of the GradientPicker component.

## How?
The GradientPicker's subcomponent's subcomponent "CustomGradientBar" already supports disabling alpha for custom gradients. All i did was add enableAlpha prop to GradientPicker and its subcomponent, CustomGradientPicker, in order to take advantage of the feature within CustomGradientBar component.

This is my first attempt at contributing, and I do not expect it to be free of faults.

## Testing Instructions
When using GradientPicker component, simply add the enableAlpha prop with 'false' as the value, or omit the prop to let it resort to default value of 'true'.

## Issue comments

### github-actions on 2024-11-13T16:44:48Z

URL: https://github.com/WordPress/gutenberg/pull/66974#issuecomment-2474165497

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @wwdes.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: wwdes.

Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-11-13T16:45:11Z

URL: https://github.com/WordPress/gutenberg/pull/66974#issuecomment-2474166410

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @wwdes! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### github-actions on 2024-12-02T23:08:36Z

URL: https://github.com/WordPress/gutenberg/pull/66974#issuecomment-2513166364

Congratulations on your first merged pull request, @wwdes! We'd like to credit you for your contribution in the post announcing the next WordPress release, but we can't find a WordPress.org profile associated with your GitHub account. When you have a moment, visit the following URL and click "link your GitHub account" under "GitHub Username" to link your accounts:

https://profiles.wordpress.org/me/profile/edit/

And if you don't have a WordPress.org account, you can create one on this page:

https://login.wordpress.org/register

Kudos!

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2024-11-20T07:33:56Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1849699018
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,9 @@
 
 ## Unreleased
```

A new version was released, and this section is now released. You'll have to rebase and move the new entries to the new "Unreleased" section.

### Review thread 2 (resolved)

#### tyxla on 2024-11-20T07:34:40Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1849700917
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,9 @@
 
 ## Unreleased
 
+-	`GradientPicker`: Add disableAlpha prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
+-	`CustomGradientPicker`: Add disableAlpha prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
```

I think we can tweak the spacing a bit to be consistent with the rest of the CHANGELOG:

```suggestion
-   `GradientPicker`: Add disableAlpha prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
-   `CustomGradientPicker`: Add disableAlpha prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
```

### Review thread 3

#### tyxla on 2024-11-20T07:40:54Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1849726724
Path: packages/components/src/gradient-picker/index.tsx

```diff
@@ -213,6 +213,7 @@ export function GradientPicker( {
 	onChange,
 	value,
 	clearable = true,
+	disableAlpha = false,
```

It will make sense to add the new prop to the `GradientPicker` README.

#### mirka on 2024-11-23T01:47:09Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1854996119
Path: packages/components/src/gradient-picker/index.tsx

```diff
@@ -213,6 +213,7 @@ export function GradientPicker( {
 	onChange,
 	value,
 	clearable = true,
+	disableAlpha = false,
```

I'll try to get #67250 merged first so we don't need to do this manually 🙂

#### mirka on 2024-11-25T14:52:01Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1856754251
Path: packages/components/src/gradient-picker/index.tsx

```diff
@@ -213,6 +213,7 @@ export function GradientPicker( {
 	onChange,
 	value,
 	clearable = true,
+	disableAlpha = false,
```

Done. @wwdes If you rebase on or merge back `trunk`, running `npm run docs:components` should automatically update the README according to the TypeScript data.

### Review thread 4

#### tyxla on 2024-11-20T07:43:41Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1849735864
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,10 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
+	 */
+	disableAlpha?: boolean;
```

`disableAlpha` was deprecated for the underlying `ColorPicker`, but `CustomGradientPicker` correctly uses the proper `enableAlpha` prop, so we should be good. 

I wonder if we intended to follow the same pattern for this `disableAlpha` prop, @ciampo @mirka?

#### mirka on 2024-11-23T01:52:46Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1854998547
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,10 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
+	 */
+	disableAlpha?: boolean;
```

> The only thing I'm worried about is the prop name.  `ColorPicker`'s `disableAlpha` prop is deprecated in favor of the opposite `enableAlpha` prop. Ideally it would be nice to follow the same pattern for all components that use `ColorPicker` under the hood. Whether we want to do it here or in another PR

Yes, I think we should flip it to be `enableAlpha`, if only for consistency between relevant components. And we should do it in this PR.

#### wwdes on 2024-11-26T16:25:10Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1858856225
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,10 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
+	 */
+	disableAlpha?: boolean;
```

If we flip the prop to enableAlpha, what should the default value be? ColorPicker defaults it to false, but GradientPicker was always set to have alpha enabled.

#### mirka on 2024-11-26T21:24:47Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1859245576
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,10 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
+	 */
+	disableAlpha?: boolean;
```

We don't want to change the default behavior, so the default should be `true` 👍

#### wwdes on 2024-11-26T21:28:48Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1859250677
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,10 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
+	 */
+	disableAlpha?: boolean;
```

In that case, I'll make the flip within GradientPicker component, and leave disableAlpha prop in custom-gradient-picker and custom-gradient-bar as is.

#### wwdes on 2024-11-27T00:05:26Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1859443346
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,10 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
+	 */
+	disableAlpha?: boolean;
```

OK, the changes are made.

I changed the prop name to enableAlpha in both GradientPicker and CustomGradientPicker components and the flip is done within CustomGradientPicker. I left the disableAlpha prop as it was in CustomGradientBar.

Note that I rolled back previous commits and did everything in one go instead while also updating and rebasing trunk. However, I am not sure that I did everything correctly regarding git workflow.

### Review thread 5 (resolved)

#### mirka on 2024-11-27T23:30:05Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1861342663
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,9 @@
 
 ## Unreleased
 
+-   `GradientPicker`: Add enableAlpha prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
+-   `CustomGradientPicker`: Add enableAlpha prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
```

```suggestion
### Enhancements

-   `GradientPicker`: Add `enableAlpha` prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
-   `CustomGradientPicker`: Add `enableAlpha` prop ([#66974](https://github.com/WordPress/gutenberg/pull/66974))
```

### Review thread 6 (resolved)

#### mirka on 2024-11-27T23:30:27Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1861342817
Path: packages/components/src/custom-gradient-picker/types.ts

```diff
@@ -26,6 +26,12 @@ export type CustomGradientPickerProps = {
 	 * the `currentGradient` as an argument.
 	 */
 	onChange: ( currentGradient: string ) => void;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
```

Minor nit:

```suggestion
	 * Whether to enable alpha transparency options in the picker.
```

### Review thread 7 (resolved)

#### mirka on 2024-11-27T23:30:41Z

URL: https://github.com/WordPress/gutenberg/pull/66974#discussion_r1861342918
Path: packages/components/src/gradient-picker/types.ts

```diff
@@ -56,6 +56,12 @@ type GradientPickerBaseProps = {
 	 * @default true
 	 */
 	loop?: boolean;
+	/**
+	 * Whether to disable alpha transparency options in the picker.
```

```suggestion
	 * Whether to enable alpha transparency options in the picker.
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +5/-0)
- packages/components/src/custom-gradient-picker/index.tsx (MODIFIED, +2/-0)
- packages/components/src/custom-gradient-picker/types.ts (MODIFIED, +6/-0)
- packages/components/src/gradient-picker/README.md (MODIFIED, +8/-0)
- packages/components/src/gradient-picker/index.tsx (MODIFIED, +2/-0)
- packages/components/src/gradient-picker/types.ts (MODIFIED, +6/-0)
