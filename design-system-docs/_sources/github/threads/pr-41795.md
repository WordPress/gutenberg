---
number: 41795
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/41795
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #41795: ColorPicker: Downsize inputs to 36px

- URL: https://github.com/WordPress/gutenberg/pull/41795
- Author: mirka
- Created: 2022-06-17T17:37:43Z
- Updated: 2025-04-22T10:21:17Z
- Comments: 3 of 3
- Review threads: 1 of 1
- Changed files: 4 of 4
- Diff: ../pr-diffs/41795.diff

## Body

🚧 Currently on hold to reassess approach 🚧 

Part of #39397

## What?

Downsize the 40px inputs in `ColorPicker` to the new default 36px.

We can land these size changes immediately without coordination via `__next` prop, since they are already in an inconsistent state.

## Why?

As part of the effort to move toward consistent component sizes.

## Testing Instructions

1. `npm run storybook:dev`
2. See the story for `ColorPicker`. The RGB/HSL/HEX inputs should all be at 36px height.

## Issue comments

### ciampo on 2022-06-21T08:19:12Z

URL: https://github.com/WordPress/gutenberg/pull/41795#issuecomment-1161419364

> 🚧 Currently on hold to reassess approach 🚧

Sounds good — just ping once we have a new approach and this PR is ready for review again :)

### mirka on 2022-06-21T21:22:40Z

URL: https://github.com/WordPress/gutenberg/pull/41795#issuecomment-1162381721

New plan: Instead of removing the 40px hack, I will be switching it to a clean implementation via size props on the underlying input controls. On hold until I add a large size variant on `NumberControl`.

### mirka on 2022-06-28T10:33:35Z

URL: https://github.com/WordPress/gutenberg/pull/41795#issuecomment-1168546592

Closing in favor of #42002

## Review threads

### Review thread 1

#### mirka on 2022-06-17T17:42:09Z

URL: https://github.com/WordPress/gutenberg/pull/41795#discussion_r900386320
Path: packages/components/src/color-picker/styles.ts

```diff
@@ -42,14 +41,6 @@ export const RangeControl = styled( InnerRangeControl )`
 	}
 `;
 
-// All inputs should be the same height so this should be changed at the component level.
-// That involves changing heights of multiple input types probably buttons too etc.
-// So until that is done we are already using the new height on the color picker so it matches the mockups.
-const inputHeightStyle = `
-&&& ${ Input } {
-	height: 40px;
-}`;
-
```

I'll go ahead and remove these 40px styles without moving them to a `large` size variant, since it's just a hack anyway. We can first add a `large` size variant to `InputControl` once we're ready to introduce large variants.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/color-picker/hex-input.tsx (MODIFIED, +1/-0)
- packages/components/src/color-picker/input-with-slider.tsx (MODIFIED, +1/-0)
- packages/components/src/color-picker/styles.ts (MODIFIED, +0/-11)
