---
number: 60261
type: pull_request
state: MERGED
merged_at: 2024-05-28T15:22:41Z
url: https://github.com/WordPress/gutenberg/pull/60261
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #60261: CustomSelectControlV2: Use `InputBase` for styling

- URL: https://github.com/WordPress/gutenberg/pull/60261
- Author: mirka
- Created: 2024-03-27T21:39:39Z
- Updated: 2024-05-28T18:05:15Z
- Comments: 4 of 4
- Review threads: 9 of 9
- Changed files: 6 of 6
- Diff: ../pr-diffs/60261.diff

## Body

After #60226
Part of #55023

## What?

Reuse `InputBase` and `SelectControlChevronDown` for styling `CustomSelectControlV2`.

## Why?

For more DRY and consistent styling.

(`InputBase` is an internal component used to style the standard borders for an input, as well as handle the layout for prefix/suffix elements.)

## Testing Instructions

- Focus styles for CustomSelectControlV2 Legacy should match the styles for v1.
- The chevron should have the correct padding in all possible sizes.

> [!NOTE]  
> Long strings will still overlap with the chevron. I'll address this separately.

## Issue comments

### github-actions on 2024-05-24T13:29:14Z

URL: https://github.com/WordPress/gutenberg/pull/60261#issuecomment-2129542112

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: fullofcaffeine <fullofcaffeine@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-05-28T15:06:42Z

URL: https://github.com/WordPress/gutenberg/pull/60261#issuecomment-2135465716

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 106f28abf8fd177aa90c06a2558cd357d4479fe3.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/9271457879
📝  Reported issues:
- #61701 in `/test/e2e/specs/site-editor/dataviews-list-layout-keyboard.spec.js`
- #60678 in `/test/e2e/specs/site-editor/site-editor-url-navigation.spec.js`

### mirka on 2024-05-28T15:07:27Z

URL: https://github.com/WordPress/gutenberg/pull/60261#issuecomment-2135467457

> I didn't know about `InputBase`; I can see it's part of the `input-control` component. Would it make more sense to make it a separate component if it's going to be used as the basis for others like this (in a follow-up, ofc)? It feels weird it doesn't have its own doc, too. For now, it'd be nice if you could add a bit more information/background on `InputBase` in the PR description, if possible.

Sorry about that. I added a quick note to the PR description, and a JSDoc on the component for good measure. And good point, we might want to consider extracting out the `InputBase` from the `input-control` folder, since it's starting to be used in more places.

### DaniGuardiola on 2024-05-28T15:40:59Z

URL: https://github.com/WordPress/gutenberg/pull/60261#issuecomment-2135569856

@mirka fwiw, I wanna take a closer look at this InputBase thingy. A while back I looked at it and it seems to be doing a whole lot of not really that much :P - I think there's potential for simplifying or splitting this up into something more manageable and way simpler. Note that I'm including a few different layers that I noticed in input code, so maybe I'm getting things mixed up here and this one's as simple as it gets. In any case, this is for another day, just wanted to make a note!

## Review threads

### Review thread 1

#### mirka on 2024-03-27T21:41:30Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542081247
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -81,21 +75,18 @@ const UnconnectedCustomSelectButton = (
 			showOnKeyDown={ false }
 		>
 			<div>{ computedRenderSelectedValue( currentValue ) }</div>
-			<Icon icon={ chevronDown } size={ 18 } />
 		</Styled.Select>
 	);
 };
 
-const CustomSelectButton = contextConnectWithoutRef(
-	UnconnectedCustomSelectButton,
-	'CustomSelectControlButton'
-);
```

We don't need to connect this to the context system anymore.

#### fullofcaffeine on 2024-05-27T18:12:36Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1616307373
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -81,21 +75,18 @@ const UnconnectedCustomSelectButton = (
 			showOnKeyDown={ false }
 		>
 			<div>{ computedRenderSelectedValue( currentValue ) }</div>
-			<Icon icon={ chevronDown } size={ 18 } />
 		</Styled.Select>
 	);
 };
 
-const CustomSelectButton = contextConnectWithoutRef(
-	UnconnectedCustomSelectButton,
-	'CustomSelectControlButton'
-);
```

Would you mind elaborating on why?

#### fullofcaffeine on 2024-05-27T19:05:02Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1616333269
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -81,21 +75,18 @@ const UnconnectedCustomSelectButton = (
 			showOnKeyDown={ false }
 		>
 			<div>{ computedRenderSelectedValue( currentValue ) }</div>
-			<Icon icon={ chevronDown } size={ 18 } />
 		</Styled.Select>
 	);
 };
 
-const CustomSelectButton = contextConnectWithoutRef(
-	UnconnectedCustomSelectButton,
-	'CustomSelectControlButton'
-);
```

Nevermind, already seen https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542085948 👍🏻

### Review thread 2

#### mirka on 2024-03-27T21:45:09Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542084126
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -92,39 +89,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
-		};
-	}, [ __next40pxDefaultSize, size ] );
-
-	const translatedProps = {
-		'aria-describedby': props.describedBy,
-		children,
-		renderSelectedValue: __experimentalShowSelectedHint
-			? renderSelectedValueHint
-			: undefined,
-		...restProps,
-	};
```

I replaced this with direct prop assignments on the `_CustomSelect` component for simplicity.

### Review thread 3

#### mirka on 2024-03-27T21:46:43Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542085232
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -92,39 +89,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
-		};
-	}, [ __next40pxDefaultSize, size ] );
-
-	const translatedProps = {
-		'aria-describedby': props.describedBy,
```

`describedBy` wasn't being destructured from the props, and therefore it was still being passed invalidly to `_CustomSelect` through the rest props. Fixed.

### Review thread 4

#### mirka on 2024-03-27T21:47:45Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542085948
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -92,39 +89,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
```

No more need to tunnel this via context system, because we can directly set the newly exposed `size` prop on `_CustomSelect`.

#### fullofcaffeine on 2024-05-27T18:32:45Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1616318077
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -92,39 +89,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
```

Ah I see 👍🏻 (regarding https://github.com/WordPress/gutenberg/pull/60261/files#r1616307373).

### Review thread 5

#### mirka on 2024-03-27T21:51:10Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542088418
Path: packages/components/src/input-control/input-base.tsx

```diff
@@ -64,7 +64,7 @@ function getUIFlexProps( labelPosition?: LabelPosition ) {
 	return props;
 }
 
-export function InputBase(
```

Removed this unused export, because a consumer can erroneously import this named version instead of the default export which is properly wrapped in a `forwardRef()`.

(This happened to me while working on this PR and it wasted a couple minutes of my life 😇)

#### fullofcaffeine on 2024-05-27T18:46:27Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1616324791
Path: packages/components/src/input-control/input-base.tsx

```diff
@@ -64,7 +64,7 @@ function getUIFlexProps( labelPosition?: LabelPosition ) {
 	return props;
 }
 
-export function InputBase(
```

Nit, but, what is the current convention for components - should we provide both (named, default) or just one - and if so, it seems `default` is preferred?

Another approach would be to connect before while assigning to a `const` and then export as needed.

#### mirka on 2024-05-28T12:44:39Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617182357
Path: packages/components/src/input-control/input-base.tsx

```diff
@@ -64,7 +64,7 @@ function getUIFlexProps( labelPosition?: LabelPosition ) {
 	return props;
 }
 
-export function InputBase(
```

Good question. We do indeed have [some conventions](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#considerations-for-the-docgen) that are actually for accommodating our current docgen, more than anything else.

Doesn't matter that much for internal components like `InputBase`, which aren't fed into the docgen.

### Review thread 6

#### mirka on 2024-03-27T21:54:37Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1542090952
Path: packages/components/src/custom-select-control-v2/types.ts

I refactored some of these types because the rejiggering work I did in this PR surfaced some mistakes in how we were using them.

### Review thread 7

#### mirka on 2024-05-24T13:07:38Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1613458416
Path: packages/components/src/custom-select-control-v2/types.ts

```diff
@@ -14,6 +14,19 @@ export type CustomSelectStore = {
 
 export type CustomSelectContext = CustomSelectStore | undefined;
 
+type CustomSelectSize< Size = 'compact' | 'default' > = {
```

For context, the public interface of `CustomSelectControlV2` only has these two size variants, whereas the private (internal) `CustomSelectButton` has three size variants for back compat.

### Review thread 8

#### tyxla on 2024-05-28T10:36:10Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1616995408
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
```

Is there a good reason for the IIFE here? Can be a simple `if` without an IIFE IMHO.

#### mirka on 2024-05-28T14:29:59Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617356560
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
```

To me, this takes the least cognitive load to read because it doesn't introduce a mutable variable (`let`), and it's a bit of overkill to extract out the entire function. I'm probably more mutation-averse than average though 😓

#### tyxla on 2024-05-28T14:39:02Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617376586
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
```

Primitive values cannot be mutated, so we're technically not really mutating anything. Also, the IIFE makes the code a bit more complex to read than if we were using a `let`. No strong feelings either way, so I'm happy to leave this to your judgment!

#### mirka on 2024-05-28T15:03:16Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617425277
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
```

Touché, I should've said _reassignment_! I always considered variable reassignment to be a form of mutation (as in mutable state), but I guess those two terms should be differentiated in JS at least.

### Review thread 9

#### tyxla on 2024-05-28T10:47:22Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617008816
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
-		};
-	}, [ __next40pxDefaultSize, size ] );
-
-	const translatedProps = {
-		'aria-describedby': props.describedBy,
-		children,
-		renderSelectedValue: __experimentalShowSelectedHint
-			? renderSelectedValueHint
-			: undefined,
-		...restProps,
-	};
+		if ( ! __next40pxDefaultSize && size === 'default' ) {
+			return 'compact';
+		}
+		return size;
+	} )();
 
 	return (
-		<ContextSystemProvider value={ contextSystemValue }>
-			<_CustomSelect { ...translatedProps } store={ store } />
-		</ContextSystemProvider>
+		<_CustomSelect
+			aria-describedby={ describedBy }
+			renderSelectedValue={
+				__experimentalShowSelectedHint
+					? renderSelectedValueHint
+					: undefined
+			}
+			size={ translatedSize }
+			store={ store }
+			{ ...restProps }
```

Do we intentionally allow the `restProps` to override the above props like `store` or `renderSelectedValue`? Should these props be declared below the `restProps` to prevent that from happening?

#### DaniGuardiola on 2024-05-28T13:11:24Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617222320
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
-		};
-	}, [ __next40pxDefaultSize, size ] );
-
-	const translatedProps = {
-		'aria-describedby': props.describedBy,
-		children,
-		renderSelectedValue: __experimentalShowSelectedHint
-			? renderSelectedValueHint
-			: undefined,
-		...restProps,
-	};
+		if ( ! __next40pxDefaultSize && size === 'default' ) {
+			return 'compact';
+		}
+		return size;
+	} )();
 
 	return (
-		<ContextSystemProvider value={ contextSystemValue }>
-			<_CustomSelect { ...translatedProps } store={ store } />
-		</ContextSystemProvider>
+		<_CustomSelect
+			aria-describedby={ describedBy }
+			renderSelectedValue={
+				__experimentalShowSelectedHint
+					? renderSelectedValueHint
+					: undefined
+			}
+			size={ translatedSize }
+			store={ store }
+			{ ...restProps }
```

Everything that can't be merged should be overridable, as per the open/closed pattern.

#### mirka on 2024-05-28T14:37:22Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617373405
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
-		};
-	}, [ __next40pxDefaultSize, size ] );
-
-	const translatedProps = {
-		'aria-describedby': props.describedBy,
-		children,
-		renderSelectedValue: __experimentalShowSelectedHint
-			? renderSelectedValueHint
-			: undefined,
-		...restProps,
-	};
+		if ( ! __next40pxDefaultSize && size === 'default' ) {
+			return 'compact';
+		}
+		return size;
+	} )();
 
 	return (
-		<ContextSystemProvider value={ contextSystemValue }>
-			<_CustomSelect { ...translatedProps } store={ store } />
-		</ContextSystemProvider>
+		<_CustomSelect
+			aria-describedby={ describedBy }
+			renderSelectedValue={
+				__experimentalShowSelectedHint
+					? renderSelectedValueHint
+					: undefined
+			}
+			size={ translatedSize }
+			store={ store }
+			{ ...restProps }
```

Also `store` or `renderSelectedValue` are not documented in the public interface for this, so it would be very at-your-own-risk if anyone tried that.

No strong opinion, but I think the general strategy of "keep everything overridable unless there is a specific, strong reason not to" would be fine.

#### fullofcaffeine on 2024-05-28T18:05:15Z

URL: https://github.com/WordPress/gutenberg/pull/60261#discussion_r1617707151
Path: packages/components/src/custom-select-control-v2/legacy-component/index.tsx

```diff
@@ -94,39 +91,33 @@ function CustomSelectControl( props: LegacyCustomSelectProps ) {
 		);
 	};
 
-	// translate legacy button sizing
-	const contextSystemValue = useMemo( () => {
-		let selectedSize;
-
+	const translatedSize = ( () => {
 		if (
 			( __next40pxDefaultSize && size === 'default' ) ||
 			size === '__unstable-large'
 		) {
-			selectedSize = 'default';
-		} else if ( ! __next40pxDefaultSize && size === 'default' ) {
-			selectedSize = 'compact';
-		} else {
-			selectedSize = size;
+			return 'default';
 		}
-
-		return {
-			CustomSelectControlButton: { _overrides: { size: selectedSize } },
-		};
-	}, [ __next40pxDefaultSize, size ] );
-
-	const translatedProps = {
-		'aria-describedby': props.describedBy,
-		children,
-		renderSelectedValue: __experimentalShowSelectedHint
-			? renderSelectedValueHint
-			: undefined,
-		...restProps,
-	};
+		if ( ! __next40pxDefaultSize && size === 'default' ) {
+			return 'compact';
+		}
+		return size;
+	} )();
 
 	return (
-		<ContextSystemProvider value={ contextSystemValue }>
-			<_CustomSelect { ...translatedProps } store={ store } />
-		</ContextSystemProvider>
+		<_CustomSelect
+			aria-describedby={ describedBy }
+			renderSelectedValue={
+				__experimentalShowSelectedHint
+					? renderSelectedValueHint
+					: undefined
+			}
+			size={ translatedSize }
+			store={ store }
+			{ ...restProps }
```

Would be much better if JS supported `if` expressions :/

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +2/-1)
- packages/components/src/custom-select-control-v2/custom-select.tsx (MODIFIED, +37/-36)
- packages/components/src/custom-select-control-v2/legacy-component/index.tsx (MODIFIED, +22/-31)
- packages/components/src/custom-select-control-v2/styles.ts (MODIFIED, +10/-8)
- packages/components/src/custom-select-control-v2/types.ts (MODIFIED, +16/-15)
- packages/components/src/input-control/input-base.tsx (MODIFIED, +5/-1)
