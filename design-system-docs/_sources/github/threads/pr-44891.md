---
number: 44891
type: pull_request
state: MERGED
merged_at: 2022-11-02T20:55:52Z
url: https://github.com/WordPress/gutenberg/pull/44891
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #44891: FontSizePicker: Use components instead of helper functions

- URL: https://github.com/WordPress/gutenberg/pull/44891
- Author: noisysocks
- Created: 2022-10-12T06:05:18Z
- Updated: 2022-11-02T20:56:30Z
- Comments: 8 of 8
- Review threads: 14 of 14
- Changed files: 8 of 8
- Diff: ../pr-diffs/44891.diff

## Body

## What?
Simplify how `FontSizePicker` is implemented by using nested components for the `CustomSelectControl` and `ToggleGroupControl` logic instead of helper functions.

## Why?
While adding types to `FontSizePicker` in https://github.com/WordPress/gutenberg/pull/44449 I ran into difficulty typing `getFontSizeOptions` as its return type depends on the runtime value of `useSelectControl`. See https://github.com/WordPress/gutenberg/pull/44449#discussion_r980489635 for some relevant discussion there.

The end result is lots of yucky `as` casts scattered throughout the component:

https://github.com/WordPress/gutenberg/blob/3983fee52b8fa0b9336d6202e8b92d407b99187c/packages/components/src/font-size-picker/index.tsx#L218

In addition to being difficult to type, it's just plain confusing. @ramonjd learned this the other day when trying to make a simple change to `FontSizePicker` in https://github.com/WordPress/gutenberg/pull/44791. He only needed to change one line but the design of this component tricked him into thinking it was more complicated than it was. (Sorry to pick on you @ramonjd.)

To do away with all of this, I've settled on  _embracing_ that `FontSizePicker` works differently depending on if there are more than 5 font sizes or not. I've split out the `CustomSelectControl` (>5 font sizes) and `ToggleGroupControl` (≤5 font sizes) into their own components. Now each component can do whatever it needs to do without having to worry about the other. We can then remove the difficult-to-type helper functions altogether.

## How?
- New `FontSizePickerSelect` component: this contains the `CustomSelectControl` and logic for when there are > 5 font sizes.
- New `FontSizePickerToggleGroup` component: this contains the `ToggleGroupControl` and logic for when there are ≤5 font sizes.
- Removed no-longer-necessary helpers from `utils.ts`.

## Testing Instructions
Either:

- Look at `FontSizePicker` in Storybook, or;
- Go to Site Editor → Global Styles → Typography → Text → Size.

The font size picker should work exactly as it does in `trunk` and tests should pass.

## Issue comments

### noisysocks on 2022-10-13T04:39:52Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1277023273

Thanks for the feedback @ciampo!

> 1. I noticed that the user can still enter a custom value even if `disableCustomFontSizes` is set to `true`, when `withSlider` is also set to `true`

I think https://github.com/WordPress/gutenberg/pull/44598 should accidentally fix this!

> 2. The logic around `fallbackFontSize`, `withReset` and `withSlider` is very confusing and should be reviewed.

The whole component is very confusing 😂. I'd be keen to deprecate `withReset`. `fallbackFontSize` is named poorly (suggestion: `defaultSliderValue`) but probably needs to be there.

### noisysocks on 2022-10-13T05:53:19Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1277066061

Feedback addressed or responded to! Thanks again.

### ciampo on 2022-10-13T09:22:48Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1277302322

Thank you! I will try to have another look at this before EOW

### noisysocks on 2022-10-25T05:33:51Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1290007607

I rebased this and addressed all the inline feedback. I'll improve the `FontSizePicker` tests tomorrow in a seperate PR so that we can merge that one first.

### noisysocks on 2022-11-01T05:26:08Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1298047059

I stacked this PR onto https://github.com/WordPress/gutenberg/pull/45298/.

### noisysocks on 2022-11-01T06:31:29Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1298086364

Cool, the unit tests added in https://github.com/WordPress/gutenberg/pull/45298 pass! That should reassure us that this refactor doesn't break `FontSizePicker`. How are you feeling @ciampo?

### noisysocks on 2022-11-02T02:32:03Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1299479554

Nice catch @ciampo, I've fixed that and added regression tests.

### ramonjd on 2022-11-02T05:20:36Z

URL: https://github.com/WordPress/gutenberg/pull/44891#issuecomment-1299588365

> He only needed to change one line but the design of this component tricked him into thinking it was more complicated than it was. 

I tricked myself, I think :)

## Review threads

### Review thread 1 (resolved)

#### noisysocks on 2022-10-12T06:08:28Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993032147
Path: packages/components/src/font-size-picker/test/utils.ts

```diff
@@ -80,74 +76,3 @@ describe( 'splitValueAndUnitFromSize', () => {
 		}
 	);
 } );
-
-describe( 'getToggleGroupOptions', () => {
```

I don't think these tests add much value beyond what's already tested in `test/index.tsx`.

#### ciampo on 2022-10-12T17:13:38Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993717118
Path: packages/components/src/font-size-picker/test/utils.ts

```diff
@@ -80,74 +76,3 @@ describe( 'splitValueAndUnitFromSize', () => {
 		}
 	);
 } );
-
-describe( 'getToggleGroupOptions', () => {
```

I had a look and couldn't really see a test checking for t-shirt sizes. Could you add a test for that in `test/index.tsx` ?

#### noisysocks on 2022-10-13T05:53:04Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994176259
Path: packages/components/src/font-size-picker/test/utils.ts

```diff
@@ -80,74 +76,3 @@ describe( 'splitValueAndUnitFromSize', () => {
 		}
 	);
 } );
-
-describe( 'getToggleGroupOptions', () => {
```

👍 added a test that checks all the t-shirt labels are used even when there's no font size names.

### Review thread 2 (resolved)

#### ciampo on 2022-10-12T16:02:48Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993652622
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -81,18 +76,25 @@ export type FontSize = {
 	slug: string;
 };
 
-export type FontSizeOption = Omit< FontSize, 'size' > &
-	Partial< Pick< FontSize, 'size' > >;
-
-export type FontSizeSelectOption = Pick< FontSizeOption, 'size' > & {
-	key: string;
-	name?: string;
-	__experimentalHint: ReactNode;
+export type FontSizePickerSelectProps = {
+	fontSizes: FontSize[];
+	value: FontSizePickerProps[ 'value' ];
+	size: FontSizePickerProps[ 'size' ];
+	onChange: ( value: number | string | undefined ) => void;
+	onSelectCustom: () => void;
 };
```

We can reuse more prop types from `FontSizePickerProps` by using the `NonNullable` utility, and we can use `Pick` for more clarity:

```suggestion
export type FontSizePickerSelectProps = Pick<
	FontSizePickerProps,
	'value' | 'size'
> & {
	fontSizes: NonNullable< FontSizePickerProps[ 'fontSizes' ] >;
	onChange: NonNullable< FontSizePickerProps[ 'onChange' ] >;
	onSelectCustom: () => void;
};
```

### Review thread 3 (resolved)

#### ciampo on 2022-10-12T16:03:19Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993653152
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -81,18 +76,25 @@ export type FontSize = {
 	slug: string;
 };
 
-export type FontSizeOption = Omit< FontSize, 'size' > &
-	Partial< Pick< FontSize, 'size' > >;
-
-export type FontSizeSelectOption = Pick< FontSizeOption, 'size' > & {
-	key: string;
-	name?: string;
-	__experimentalHint: ReactNode;
+export type FontSizePickerSelectProps = {
+	fontSizes: FontSize[];
+	value: FontSizePickerProps[ 'value' ];
+	size: FontSizePickerProps[ 'size' ];
+	onChange: ( value: number | string | undefined ) => void;
+	onSelectCustom: () => void;
 };
 
-export type FontSizeToggleGroupOption = {
+export type FontSizePickerSelectOption = {
 	key: string;
-	value: number | string;
-	label: string;
 	name: string;
+	value?: FontSize[ 'size' ];
+	__experimentalHint?: string;
+};
+
+export type FontSizePickerToggleGroupProps = {
+	fontSizes: FontSize[];
+	value: FontSizePickerProps[ 'value' ];
+	__nextHasNoMarginBottom?: boolean;
+	size: FontSizePickerProps[ 'size' ];
+	onChange: ( value: number | string | undefined ) => void;
```

Same as previous comment

```suggestion
export type FontSizePickerToggleGroupProps = Pick<
	FontSizePickerProps,
	'value' | 'size' | '__nextHasNoMarginBottom'
> & {
	fontSizes: NonNullable< FontSizePickerProps[ 'fontSizes' ] >;
	onChange: NonNullable< FontSizePickerProps[ 'onChange' ] >;
```

### Review thread 4 (resolved)

#### ciampo on 2022-10-12T16:07:28Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993657205
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
```

Minor nit: let's not call this variable `number`, since its type is actually a `string` (my brain refused to process this for a while 😅 )

```suggestion
			const [ parsedValue ] = splitValueAndUnitFromSize( fontSize.size );
			return {
				key: fontSize.slug,
				name: fontSize.name ?? fontSize.slug,
				value: fontSize.size,
				__experimentalHint: parsedValue,
```

#### noisysocks on 2022-10-13T04:48:43Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994138717
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
```

In https://github.com/WordPress/gutenberg/pull/44598 I changed this function to actually return a number. But yeah for now makes sense to name it what it is.

### Review thread 5 (resolved)

#### ciampo on 2022-10-12T16:15:01Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993664654
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

Previously, the `getSelectedOption` utility function used to fallback to `DEFAULT_FONT_SIZE_OPTION` when `value` was not defined, and to fallback to `CUSTOM_FONT_SIZE_OPTION` when there were not `fontSizes` matching the `value`.

How is that piece of logic implemented in this new version of the component?

#### noisysocks on 2022-10-13T04:54:51Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994141159
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

That logic only ever had any effect when `shouldUseSelectControl` was `true` and the `CustomSelectControl` was rendered, because `ToggleGroupControl` used `value={ value }` and not `value={ selectedOption.size }`. I therefore moved this logic to `FontSizePickerSelect` so that it is near to where it's needed and not executed unnecessarily. 

https://github.com/WordPress/gutenberg/blob/bbf17a3f5c90f488bbb9b6b34146d5b3cadca859/packages/components/src/font-size-picker/select.tsx#L42-L43

#### ciampo on 2022-10-14T17:46:57Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r995995137
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

Thank you for the explanation! This makes sense for the fallback to `customOption`, but I'm still confused about where this piece of logic is implemented in this new version: 

https://github.com/WordPress/gutenberg/blob/d20cc8ba7e84d2385b1e257ac8ef397a515a66a8/packages/components/src/font-size-picker/utils.ts#L147-L149

#### ciampo on 2022-10-14T18:44:27Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r996041319
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

Maybe I get it — when `value` is `undefined`, this code will actually select the default option (since it also have a value of `undefined`):


https://github.com/WordPress/gutenberg/blob/bbf17a3f5c90f488bbb9b6b34146d5b3cadca859/packages/components/src/font-size-picker/select.tsx#L42-L43

If that's the reason, I must admit that the logic here is much less explicit than it used to be, and I would argue that it would greatly benefit from a unit test to make sure that we don't introduce a regression in the future without realizing.

#### ciampo on 2022-10-14T18:46:37Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r996042750
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

I also realised that when the `value` prop is `undefined` the header hint:
 - is `"Default"` on `trunk`
 - but it's an empty string in this PR

#### noisysocks on 2022-10-25T05:09:46Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r1004003026
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

> Maybe I get it — when `value` is `undefined`, this code will actually select the default option (since it also have a value of `undefined`):

Yes that's right. I'll make the `find()` code more explicit with a ternary.

> I also realised that when the `value` prop is `undefined` the header hint:

👍 I've fixed this and restored most of the changes I made to `headerHint`. Got a bit ahead of myself there, I'll wait for https://github.com/WordPress/gutenberg/pull/44966 which has more substantial changes to the hint text.

#### ciampo on 2022-11-01T12:11:15Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r1010358774
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

> Yes that's right. I'll make the find() code more explicit with a ternary.

Looking more into this, I found a couple more more differences with `trunk`

1. With more than 5 font sizes and when passing `value=""` to `FontSizePicker`, prior to your latest change that introduces the ternary, the `selectedOption` would have been `CUSTOM_OPTION` instead of `DEFAULT_OPTION` — that's because the empty string wouldn't match against the `undefined` value of the default option. This has been fixed by using the `value ?` ternary, but I though I'd flag it nonetheless
2. One difference that still remains between this PR and `trunk` is that, when `value` is `""`:
   - on `trunk`, the component shows a header hint that says "size default" and:
     - with 5+ options, the `CustomSelectControl` with the default option selected (and 
     - with less than 5 options, the `ToggleGroupControl` without no options selected 
   - in this PR, the component shows an empty custom value control input and the header hint says "size (custom)"

| Story (with `value=""`) | `trunk` | This PR |
|---|---|---|
| Default | ![Screenshot 2022-11-01 at 13 43 10](https://user-images.githubusercontent.com/1083581/199235356-c6d6ef6b-64e5-4f8f-9947-aa126c60d05e.png) | ![Screenshot 2022-11-01 at 13 43 56](https://user-images.githubusercontent.com/1083581/199235389-1753f343-7feb-4f24-ab2f-265e345a60d3.png) |
| With More Font Sizes | ![Screenshot 2022-11-01 at 13 43 19](https://user-images.githubusercontent.com/1083581/199235467-d8647de9-1b1d-4e40-a8f8-97ae5e467675.png) | ![Screenshot 2022-11-01 at 13 43 56](https://user-images.githubusercontent.com/1083581/199235490-bd05796d-87fa-4d47-bd9a-8cd093255ef5.png) |

After fixing those discrepancies, I also suggest that we add unit tests to protect against these regressions in the future!

#### ciampo on 2022-11-02T07:54:14Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r1011329595
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -94,28 +82,16 @@ const UnforwardedFontSizePicker = (
 		availableUnits: [ 'px', 'em', 'rem' ],
 	} );
 
-	/**
-	 * The main font size UI displays a toggle group when the presets are less
-	 * than six and a select control when they are more.
-	 */
-	const fontSizesContainComplexValues = fontSizes.some(
-		( { size: sizeArg } ) => ! isSimpleCssValue( sizeArg )
-	);
 	const shouldUseSelectControl = fontSizes.length > 5;
-	const options = useMemo(
-		() =>
-			getFontSizeOptions(
-				shouldUseSelectControl,
-				fontSizes,
-				disableCustomFontSizes
-			),
-		[ shouldUseSelectControl, fontSizes, disableCustomFontSizes ]
+	const selectedFontSize = fontSizes.find(
+		( fontSize ) => fontSize.size === value
```

Fixed in https://github.com/WordPress/gutenberg/pull/44891/commits/639db98a1e4bacd081de03609b57524f9fbb8507

### Review thread 6 (resolved)

#### ciampo on 2022-10-12T16:24:52Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993674129
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -124,51 +100,49 @@ const UnforwardedFontSizePicker = (
 		// If we have a custom value that is not available in the font sizes,
 		// show it as a hint as long as it's a simple CSS value.
 		if ( isCustomValue ) {
-			return (
-				value !== undefined &&
-				isSimpleCssValue( value ) &&
-				`(${ value })`
-			);
+			return value !== undefined && isSimpleCssValue( value )
+				? `(${ value })`
+				: '';
```

Super nitty: probably `undefined` or `null` are a better value for when there's no `headerHint` to render than an empty string?

But really, this is mostly down to your preference — changing from `false` to `''` is already a nice improvement

#### noisysocks on 2022-10-13T04:56:16Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994141803
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -124,51 +100,49 @@ const UnforwardedFontSizePicker = (
 		// If we have a custom value that is not available in the font sizes,
 		// show it as a hint as long as it's a simple CSS value.
 		if ( isCustomValue ) {
-			return (
-				value !== undefined &&
-				isSimpleCssValue( value ) &&
-				`(${ value })`
-			);
+			return value !== undefined && isSimpleCssValue( value )
+				? `(${ value })`
+				: '';
```

I like that this way the type is `headerHint: string`. Good old string. Does what it says on the tin.

### Review thread 7

#### ciampo on 2022-10-12T16:40:22Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993688243
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
+			};
+		} ),
+		customOption,
+	];
+
+	const selectedOption =
+		options.find( ( option ) => option.value === value ) ?? customOption;
```

Previously, the `selectedOption` was computed by matching `key` and `slug`:

```js
option.key === selectedOption.slug
```

By matching the option's value instead, there's a risk of matching the wrong options, in cese two different options share the same value. I believe we should revert to using `slug`s instead

#### noisysocks on 2022-10-13T05:03:42Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994144967
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
+			};
+		} ),
+		customOption,
+	];
+
+	const selectedOption =
+		options.find( ( option ) => option.value === value ) ?? customOption;
```

Yes it's true that we grabbed the option with `option.key === selectedOption.slug` to pass to `value`:

https://github.com/WordPress/gutenberg/blob/d7e9b2904b188ddbe8a7edc753739cca5438b834/packages/components/src/font-size-picker/index.tsx#L219-L224

But drill down into where `selectedOption` came from and you'll see that it's the font size where `fontSize.size === value`:

https://github.com/WordPress/gutenberg/blob/d7e9b2904b188ddbe8a7edc753739cca5438b834/packages/components/src/font-size-picker/utils.ts#L151

This layer of indirection is now gone.

#### ciampo on 2022-10-14T17:53:04Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r995999663
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
+			};
+		} ),
+		customOption,
+	];
+
+	const selectedOption =
+		options.find( ( option ) => option.value === value ) ?? customOption;
```

I see, thank you for the explanation!

I still wonder if this `value`-base check is too brittle and we're going to end up in a situation [like the `ColorPalette` one](https://github.com/WordPress/gutenberg/issues/43197) — but this definitely seems out of scope for this PR.

### Review thread 8 (resolved)

#### ciampo on 2022-10-12T16:41:51Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993689561
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
```

Nit: These values are constants and should be defined outside of the `FontSizePickerSelect` function

### Review thread 9 (resolved)

#### ciampo on 2022-10-12T16:44:52Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993692196
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -209,64 +183,32 @@ const UnforwardedFontSizePicker = (
 					{ !! fontSizes.length &&
 						shouldUseSelectControl &&
 						! showCustomValueControl && (
-							<CustomSelectControl
-								__nextUnconstrainedWidth
-								className="components-font-size-picker__select"
-								label={ __( 'Font size' ) }
-								hideLabelFromVision
-								describedBy={ currentFontSizeSR }
-								options={ options as FontSizeSelectOption[] }
-								value={ (
-									options as FontSizeSelectOption[]
-								 ).find(
-									( option ) =>
-										option.key === selectedOption.slug
-								) }
-								onChange={ ( {
-									selectedItem,
-								}: {
-									selectedItem: FontSizeSelectOption;
-								} ) => {
+							<FontSizePickerSelect
+								fontSizes={ fontSizes }
+								value={ value }
+								size={ size }
+								onChange={ ( newValue ) => {
 									onChange?.(
-										hasUnits
-											? selectedItem.size
-											: Number( selectedItem.size )
+										hasUnits ? newValue : Number( newValue )
 									);
-									if (
-										selectedItem.key === CUSTOM_FONT_SIZE
-									) {
-										setShowCustomValueControl( true );
-									}
 								} }
-								size={ size }
+								onSelectCustom={ () =>
+									setShowCustomValueControl( true )
```

Previously, the `onChange` callback prop was getting called also when the `custom` select option was chosen. With the new changes, it looks like `onChange` won't be fired.

Although previously, when fired, the value passed as an argument to `onChange` seemed to be `NaN` — can you confirm too? In that case, we could treat this as a bug fix.

#### noisysocks on 2022-10-13T05:10:55Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994148096
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -209,64 +183,32 @@ const UnforwardedFontSizePicker = (
 					{ !! fontSizes.length &&
 						shouldUseSelectControl &&
 						! showCustomValueControl && (
-							<CustomSelectControl
-								__nextUnconstrainedWidth
-								className="components-font-size-picker__select"
-								label={ __( 'Font size' ) }
-								hideLabelFromVision
-								describedBy={ currentFontSizeSR }
-								options={ options as FontSizeSelectOption[] }
-								value={ (
-									options as FontSizeSelectOption[]
-								 ).find(
-									( option ) =>
-										option.key === selectedOption.slug
-								) }
-								onChange={ ( {
-									selectedItem,
-								}: {
-									selectedItem: FontSizeSelectOption;
-								} ) => {
+							<FontSizePickerSelect
+								fontSizes={ fontSizes }
+								value={ value }
+								size={ size }
+								onChange={ ( newValue ) => {
 									onChange?.(
-										hasUnits
-											? selectedItem.size
-											: Number( selectedItem.size )
+										hasUnits ? newValue : Number( newValue )
 									);
-									if (
-										selectedItem.key === CUSTOM_FONT_SIZE
-									) {
-										setShowCustomValueControl( true );
-									}
 								} }
-								size={ size }
+								onSelectCustom={ () =>
+									setShowCustomValueControl( true )
```

The NaN bug I actually fixed in https://github.com/WordPress/gutenberg/pull/44598/files#r986406892.

You're right that this no longer calls `onChange( undefined )` when switching to Custom. I think that this is correct though. We don't call `onChange( undefined )` when using the toggle button in the top right, so why should we when using the dropdown?

#### ciampo on 2022-10-14T17:54:16Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r996000543
Path: packages/components/src/font-size-picker/index.tsx

```diff
@@ -209,64 +183,32 @@ const UnforwardedFontSizePicker = (
 					{ !! fontSizes.length &&
 						shouldUseSelectControl &&
 						! showCustomValueControl && (
-							<CustomSelectControl
-								__nextUnconstrainedWidth
-								className="components-font-size-picker__select"
-								label={ __( 'Font size' ) }
-								hideLabelFromVision
-								describedBy={ currentFontSizeSR }
-								options={ options as FontSizeSelectOption[] }
-								value={ (
-									options as FontSizeSelectOption[]
-								 ).find(
-									( option ) =>
-										option.key === selectedOption.slug
-								) }
-								onChange={ ( {
-									selectedItem,
-								}: {
-									selectedItem: FontSizeSelectOption;
-								} ) => {
+							<FontSizePickerSelect
+								fontSizes={ fontSizes }
+								value={ value }
+								size={ size }
+								onChange={ ( newValue ) => {
 									onChange?.(
-										hasUnits
-											? selectedItem.size
-											: Number( selectedItem.size )
+										hasUnits ? newValue : Number( newValue )
 									);
-									if (
-										selectedItem.key === CUSTOM_FONT_SIZE
-									) {
-										setShowCustomValueControl( true );
-									}
 								} }
-								size={ size }
+								onSelectCustom={ () =>
+									setShowCustomValueControl( true )
```

Sounds good to me. Afterall, if the actual `value` doesn't change, I don't see the point of calling `onChange`

### Review thread 10 (resolved)

#### ciampo on 2022-10-12T16:47:55Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993694905
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
+			};
+		} ),
+		customOption,
```

Previously, `customOption` was being added only when `disableCustomFontSizes === false` (see the deleted `getSelectOptions` function). We should probably add that check back

#### noisysocks on 2022-10-13T05:14:35Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994150031
Path: packages/components/src/font-size-picker/select.tsx

```diff
@@ -0,0 +1,74 @@
+/**
+ * WordPress dependencies
+ */
+import { __, sprintf } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import CustomSelectControl from '../custom-select-control';
+import type {
+	FontSizePickerSelectProps,
+	FontSizePickerSelectOption,
+} from './types';
+import { splitValueAndUnitFromSize } from './utils';
+
+const FontSizePickerSelect = ( props: FontSizePickerSelectProps ) => {
+	const { fontSizes = [], value, size, onChange, onSelectCustom } = props;
+
+	const defaultOption: FontSizePickerSelectOption = {
+		key: 'default',
+		name: __( 'Default' ),
+		value: undefined,
+	};
+	const customOption: FontSizePickerSelectOption = {
+		key: 'custom',
+		name: __( 'Custom' ),
+	};
+	const options: FontSizePickerSelectOption[] = [
+		defaultOption,
+		...fontSizes.map( ( fontSize ) => {
+			const [ number ] = splitValueAndUnitFromSize( fontSize.size );
+			return {
+				key: fontSize.slug,
+				name: fontSize.name ?? fontSize.slug,
+				value: fontSize.size,
+				__experimentalHint: number,
+			};
+		} ),
+		customOption,
```

Oops, good catch!

### Review thread 11 (resolved)

#### ciampo on 2022-10-12T17:02:40Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993708148
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -81,18 +76,25 @@ export type FontSize = {
 	slug: string;
 };
 
-export type FontSizeOption = Omit< FontSize, 'size' > &
-	Partial< Pick< FontSize, 'size' > >;
-
-export type FontSizeSelectOption = Pick< FontSizeOption, 'size' > & {
-	key: string;
-	name?: string;
-	__experimentalHint: ReactNode;
+export type FontSizePickerSelectProps = {
+	fontSizes: FontSize[];
+	value: FontSizePickerProps[ 'value' ];
+	size: FontSizePickerProps[ 'size' ];
+	onChange: ( value: number | string | undefined ) => void;
+	onSelectCustom: () => void;
 };
 
-export type FontSizeToggleGroupOption = {
+export type FontSizePickerSelectOption = {
 	key: string;
-	value: number | string;
-	label: string;
 	name: string;
+	value?: FontSize[ 'size' ];
```

Is there any particular reason why this prop name was changed from `size` to `value` ? It doesn't look like this affects `CustomSelectControl`, but was curious to know the reason.

#### noisysocks on 2022-10-13T05:19:14Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994152150
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -81,18 +76,25 @@ export type FontSize = {
 	slug: string;
 };
 
-export type FontSizeOption = Omit< FontSize, 'size' > &
-	Partial< Pick< FontSize, 'size' > >;
-
-export type FontSizeSelectOption = Pick< FontSizeOption, 'size' > & {
-	key: string;
-	name?: string;
-	__experimentalHint: ReactNode;
+export type FontSizePickerSelectProps = {
+	fontSizes: FontSize[];
+	value: FontSizePickerProps[ 'value' ];
+	size: FontSizePickerProps[ 'size' ];
+	onChange: ( value: number | string | undefined ) => void;
+	onSelectCustom: () => void;
 };
 
-export type FontSizeToggleGroupOption = {
+export type FontSizePickerSelectOption = {
 	key: string;
-	value: number | string;
-	label: string;
 	name: string;
+	value?: FontSize[ 'size' ];
```

Two reasons.

1) These things are called `value` once given to `onChange`:


     https://github.com/WordPress/gutenberg/blob/bbf17a3f5c90f488bbb9b6b34146d5b3cadca859/packages/components/src/font-size-picker/types.ts#L25

2) It makes it consistent with how the `ToggleGroupControl` works where each `ToggleGroupControlOption` has a `value` prop:

   https://github.com/WordPress/gutenberg/blob/bbf17a3f5c90f488bbb9b6b34146d5b3cadca859/packages/components/src/font-size-picker/toggle-group.tsx#L48

Hopefully soon we can make `CustomSelectControl` work more like `ToggleGroupControl` in that we have a `CustomSelectControlOption` component. It would make `FontSizePickerSelect` much more comprehensible imo 😀

#### ciampo on 2022-10-14T17:57:25Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r996002960
Path: packages/components/src/font-size-picker/types.ts

```diff
@@ -81,18 +76,25 @@ export type FontSize = {
 	slug: string;
 };
 
-export type FontSizeOption = Omit< FontSize, 'size' > &
-	Partial< Pick< FontSize, 'size' > >;
-
-export type FontSizeSelectOption = Pick< FontSizeOption, 'size' > & {
-	key: string;
-	name?: string;
-	__experimentalHint: ReactNode;
+export type FontSizePickerSelectProps = {
+	fontSizes: FontSize[];
+	value: FontSizePickerProps[ 'value' ];
+	size: FontSizePickerProps[ 'size' ];
+	onChange: ( value: number | string | undefined ) => void;
+	onSelectCustom: () => void;
 };
 
-export type FontSizeToggleGroupOption = {
+export type FontSizePickerSelectOption = {
 	key: string;
-	value: number | string;
-	label: string;
 	name: string;
+	value?: FontSize[ 'size' ];
```

> Hopefully soon we can make CustomSelectControl work more like ToggleGroupControl in that we have a CustomSelectControlOption component. It would make FontSizePickerSelect much more comprehensible imo 😀

I hope I'll be able to resume the work on `CustomSelectControl` soon enough, although I'm not sure if we're going to be able to have `option`-like children, given the data-processing (ie. filtering) needs of the component (see [this comment](https://github.com/WordPress/gutenberg/pull/41952#issuecomment-1171140086))

### Review thread 12 (resolved)

#### ciampo on 2022-10-12T17:08:09Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r993712582
Path: packages/components/src/font-size-picker/toggle-group.tsx

```diff
@@ -0,0 +1,58 @@
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import {
+	ToggleGroupControl,
+	ToggleGroupControlOption,
+} from '../toggle-group-control';
+import type { FontSizePickerToggleGroupProps } from './types';
+
+/**
+ * In case we have at most five font sizes, show a `T-shirt size` alias as a
+ * label of the font size. The label assumes that the font sizes are ordered
+ * accordingly - from smallest to largest.
+ */
+const FONT_SIZES_ALIASES = [
+	/* translators: S stands for 'small' and is a size label. */
+	__( 'S' ),
+	/* translators: M stands for 'medium' and is a size label. */
+	__( 'M' ),
+	/* translators: L stands for 'large' and is a size label. */
+	__( 'L' ),
+	/* translators: XL stands for 'extra large' and is a size label. */
+	__( 'XL' ),
+	/* translators: XXL stands for 'extra extra large' and is a size label. */
+	__( 'XXL' ),
+];
+
+const FontSizePickerToggleGroup = ( props: FontSizePickerToggleGroupProps ) => {
+	const { fontSizes, value, __nextHasNoMarginBottom, size, onChange } = props;
+	return (
+		<ToggleGroupControl
+			__nextHasNoMarginBottom={ __nextHasNoMarginBottom }
+			label={ __( 'Font size' ) }
+			hideLabelFromVision
+			value={ value }
+			onChange={ onChange }
+			isBlock
+			size={ size }
+		>
+			{ fontSizes.map( ( fontSize, index ) => (
+				<ToggleGroupControlOption
+					key={ fontSize.slug }
+					value={ fontSize.size }
+					label={ FONT_SIZES_ALIASES[ index ] }
+					aria-label={ fontSize.name }
```

We should preserve the same logic implemented in the old `getToggleGroupOptions`

```suggestion
					aria-label={ fontSize.name || FONT_SIZES_ALIASES[ index ] }
```

#### noisysocks on 2022-10-13T05:30:30Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r994157895
Path: packages/components/src/font-size-picker/toggle-group.tsx

```diff
@@ -0,0 +1,58 @@
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import {
+	ToggleGroupControl,
+	ToggleGroupControlOption,
+} from '../toggle-group-control';
+import type { FontSizePickerToggleGroupProps } from './types';
+
+/**
+ * In case we have at most five font sizes, show a `T-shirt size` alias as a
+ * label of the font size. The label assumes that the font sizes are ordered
+ * accordingly - from smallest to largest.
+ */
+const FONT_SIZES_ALIASES = [
+	/* translators: S stands for 'small' and is a size label. */
+	__( 'S' ),
+	/* translators: M stands for 'medium' and is a size label. */
+	__( 'M' ),
+	/* translators: L stands for 'large' and is a size label. */
+	__( 'L' ),
+	/* translators: XL stands for 'extra large' and is a size label. */
+	__( 'XL' ),
+	/* translators: XXL stands for 'extra extra large' and is a size label. */
+	__( 'XXL' ),
+];
+
+const FontSizePickerToggleGroup = ( props: FontSizePickerToggleGroupProps ) => {
+	const { fontSizes, value, __nextHasNoMarginBottom, size, onChange } = props;
+	return (
+		<ToggleGroupControl
+			__nextHasNoMarginBottom={ __nextHasNoMarginBottom }
+			label={ __( 'Font size' ) }
+			hideLabelFromVision
+			value={ value }
+			onChange={ onChange }
+			isBlock
+			size={ size }
+		>
+			{ fontSizes.map( ( fontSize, index ) => (
+				<ToggleGroupControlOption
+					key={ fontSize.slug }
+					value={ fontSize.size }
+					label={ FONT_SIZES_ALIASES[ index ] }
+					aria-label={ fontSize.name }
```

Oops yep looks like I mixed up `label` and `aria-label`.

### Review thread 13 (resolved)

#### ciampo on 2022-10-14T18:03:40Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r996007710
Path: packages/components/src/font-size-picker/test/index.tsx

```diff
@@ -244,6 +244,27 @@ describe( 'FontSizePicker', () => {
 					'XL'
 				);
 			} );
+			it( 'should use t-shirt labels if sizes have no names', () => {
+				const fontSizes = [];
+				for ( let i = 1; i <= 5; i++ ) {
+					fontSizes.push( {
+						slug: `size-${ i }`,
+						size: `${ i }em`,
+					} );
+				}
+				render(
+					<FontSizePicker
+						fontSizes={ fontSizes }
+						value={ fontSizes[ 0 ].size }
+						__nextHasNoMarginBottom
+					/>
+				);
+				for ( const label of [ 'S', 'M', 'L', 'XL', 'XXL' ] ) {
+					const element = screen.getByLabelText( label );
+					expect( element ).toBeInTheDocument();
+					expect( element.children[ 0 ].textContent ).toBe( label );
```

Let's use the `toHaveTextContent` selector instead. Also, no need to drill down the DOM

```suggestion
					expect( element ).toHaveTextContent( label );
```

### Review thread 14

#### ramonjd on 2022-11-02T05:21:38Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r1011206220
Path: packages/components/src/font-size-picker/utils.ts

```diff
@@ -56,77 +21,3 @@ export function isSimpleCssValue(
 	const sizeRegex = /^[\d\.]+(px|em|rem|vw|vh|%)?$/i;
 	return sizeRegex.test( String( value ) );
 }
-
-/**
- * Return font size options in the proper format depending
- * on the currently used control (select, toggle group).
- *
- * @param  useSelectControl       Whether to use a select control.
- * @param  optionsArray           Array of available font sizes objects.
- * @param  disableCustomFontSizes Flag that indicates if custom font sizes are disabled.
- * @return Array of font sizes in proper format for the used control.
- */
-export function getFontSizeOptions(
-	useSelectControl: boolean,
-	optionsArray: FontSize[],
-	disableCustomFontSizes: boolean
-): FontSizeSelectOption[] | FontSizeToggleGroupOption[] | null {
-	if ( disableCustomFontSizes && ! optionsArray.length ) {
-		return null;
-	}
-	return useSelectControl
-		? getSelectOptions( optionsArray, disableCustomFontSizes )
-		: getToggleGroupOptions( optionsArray );
-}
-
-function getSelectOptions(
-	optionsArray: FontSize[],
-	disableCustomFontSizes: boolean
-): FontSizeSelectOption[] {
-	const options: FontSizeOption[] = [
-		DEFAULT_FONT_SIZE_OPTION,
-		...optionsArray,
-		...( disableCustomFontSizes ? [] : [ CUSTOM_FONT_SIZE_OPTION ] ),
-	];
-	return options.map( ( { slug, name, size } ) => ( {
-		key: slug,
-		name: name || slug,
-		size,
-		__experimentalHint:
-			size && isSimpleCssValue( size ) && parseFloat( String( size ) ),
-	} ) );
-}
-
-/**
- * Build options for the toggle group options.
- *
- * @param  optionsArray An array of font size options.
- * @param  labelAliases An array of alternative labels.
- * @return Remapped optionsArray.
- */
-export function getToggleGroupOptions(
-	optionsArray: FontSize[],
-	labelAliases: string[] = FONT_SIZES_ALIASES
-): FontSizeToggleGroupOption[] {
-	return optionsArray.map( ( { slug, size, name }, index ) => {
-		return {
-			key: slug,
-			value: size,
-			label: labelAliases[ index ],
-			name: name || labelAliases[ index ],
-		};
-	} );
-}
-
-export function getSelectedOption(
```

Heads up, it's possible that I'll need this again for https://github.com/WordPress/gutenberg/pull/44967

Maybe not. Either way, it's going to be a gnarly rebase if this goes in first.

🐎

#### ramonjd on 2022-11-02T05:26:14Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r1011208374
Path: packages/components/src/font-size-picker/utils.ts

```diff
@@ -56,77 +21,3 @@ export function isSimpleCssValue(
 	const sizeRegex = /^[\d\.]+(px|em|rem|vw|vh|%)?$/i;
 	return sizeRegex.test( String( value ) );
 }
-
-/**
- * Return font size options in the proper format depending
- * on the currently used control (select, toggle group).
- *
- * @param  useSelectControl       Whether to use a select control.
- * @param  optionsArray           Array of available font sizes objects.
- * @param  disableCustomFontSizes Flag that indicates if custom font sizes are disabled.
- * @return Array of font sizes in proper format for the used control.
- */
-export function getFontSizeOptions(
-	useSelectControl: boolean,
-	optionsArray: FontSize[],
-	disableCustomFontSizes: boolean
-): FontSizeSelectOption[] | FontSizeToggleGroupOption[] | null {
-	if ( disableCustomFontSizes && ! optionsArray.length ) {
-		return null;
-	}
-	return useSelectControl
-		? getSelectOptions( optionsArray, disableCustomFontSizes )
-		: getToggleGroupOptions( optionsArray );
-}
-
-function getSelectOptions(
-	optionsArray: FontSize[],
-	disableCustomFontSizes: boolean
-): FontSizeSelectOption[] {
-	const options: FontSizeOption[] = [
-		DEFAULT_FONT_SIZE_OPTION,
-		...optionsArray,
-		...( disableCustomFontSizes ? [] : [ CUSTOM_FONT_SIZE_OPTION ] ),
-	];
-	return options.map( ( { slug, name, size } ) => ( {
-		key: slug,
-		name: name || slug,
-		size,
-		__experimentalHint:
-			size && isSimpleCssValue( size ) && parseFloat( String( size ) ),
-	} ) );
-}
-
-/**
- * Build options for the toggle group options.
- *
- * @param  optionsArray An array of font size options.
- * @param  labelAliases An array of alternative labels.
- * @return Remapped optionsArray.
- */
-export function getToggleGroupOptions(
-	optionsArray: FontSize[],
-	labelAliases: string[] = FONT_SIZES_ALIASES
-): FontSizeToggleGroupOption[] {
-	return optionsArray.map( ( { slug, size, name }, index ) => {
-		return {
-			key: slug,
-			value: size,
-			label: labelAliases[ index ],
-			name: name || labelAliases[ index ],
-		};
-	} );
-}
-
-export function getSelectedOption(
```

Oh wait, I can probably use `selectedFontSize` or something similar. Never mind me.

#### noisysocks on 2022-11-02T05:28:15Z

URL: https://github.com/WordPress/gutenberg/pull/44891#discussion_r1011209283
Path: packages/components/src/font-size-picker/utils.ts

```diff
@@ -56,77 +21,3 @@ export function isSimpleCssValue(
 	const sizeRegex = /^[\d\.]+(px|em|rem|vw|vh|%)?$/i;
 	return sizeRegex.test( String( value ) );
 }
-
-/**
- * Return font size options in the proper format depending
- * on the currently used control (select, toggle group).
- *
- * @param  useSelectControl       Whether to use a select control.
- * @param  optionsArray           Array of available font sizes objects.
- * @param  disableCustomFontSizes Flag that indicates if custom font sizes are disabled.
- * @return Array of font sizes in proper format for the used control.
- */
-export function getFontSizeOptions(
-	useSelectControl: boolean,
-	optionsArray: FontSize[],
-	disableCustomFontSizes: boolean
-): FontSizeSelectOption[] | FontSizeToggleGroupOption[] | null {
-	if ( disableCustomFontSizes && ! optionsArray.length ) {
-		return null;
-	}
-	return useSelectControl
-		? getSelectOptions( optionsArray, disableCustomFontSizes )
-		: getToggleGroupOptions( optionsArray );
-}
-
-function getSelectOptions(
-	optionsArray: FontSize[],
-	disableCustomFontSizes: boolean
-): FontSizeSelectOption[] {
-	const options: FontSizeOption[] = [
-		DEFAULT_FONT_SIZE_OPTION,
-		...optionsArray,
-		...( disableCustomFontSizes ? [] : [ CUSTOM_FONT_SIZE_OPTION ] ),
-	];
-	return options.map( ( { slug, name, size } ) => ( {
-		key: slug,
-		name: name || slug,
-		size,
-		__experimentalHint:
-			size && isSimpleCssValue( size ) && parseFloat( String( size ) ),
-	} ) );
-}
-
-/**
- * Build options for the toggle group options.
- *
- * @param  optionsArray An array of font size options.
- * @param  labelAliases An array of alternative labels.
- * @return Remapped optionsArray.
- */
-export function getToggleGroupOptions(
-	optionsArray: FontSize[],
-	labelAliases: string[] = FONT_SIZES_ALIASES
-): FontSizeToggleGroupOption[] {
-	return optionsArray.map( ( { slug, size, name }, index ) => {
-		return {
-			key: slug,
-			value: size,
-			label: labelAliases[ index ],
-			name: name || labelAliases[ index ],
-		};
-	} );
-}
-
-export function getSelectedOption(
```

Yeah `selectedFontSize` should work. Or you can `fontSizes.find()`. Happy to help you with the rebase if it's too gnarly.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/font-size-picker/font-size-picker-select.tsx (ADDED, +87/-0)
- packages/components/src/font-size-picker/font-size-picker-toggle-group.tsx (ADDED, +58/-0)
- packages/components/src/font-size-picker/index.tsx (MODIFIED, +45/-95)
- packages/components/src/font-size-picker/test/index.tsx (MODIFIED, +66/-2)
- packages/components/src/font-size-picker/test/utils.ts (MODIFIED, +1/-72)
- packages/components/src/font-size-picker/types.ts (MODIFIED, +21/-15)
- packages/components/src/font-size-picker/utils.ts (MODIFIED, +1/-110)
