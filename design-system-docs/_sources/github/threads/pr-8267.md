---
number: 8267
type: pull_request
state: MERGED
merged_at: 2018-08-02T06:08:56Z
url: https://github.com/WordPress/gutenberg/pull/8267
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Feature] UI Components","[Package] Components"]
---

# PR #8267: Add components examples in README.md files - Part 1

- URL: https://github.com/WordPress/gutenberg/pull/8267
- Author: mmtr
- Created: 2018-07-28T17:51:35Z
- Updated: 2018-08-04T10:03:02Z
- Comments: 4 of 4
- Review threads: 27 of 27
- Changed files: 26 of 26
- Diff: ../pr-diffs/8267.diff

## Body

The aim of this PR is to include *working* examples in the components documentation. 

Based on [this idea](https://github.com/Automattic/wp-calypso/pull/26314#pullrequestreview-140618214) from @gziolo for avoiding to have Gutenberg examples in Calypso.

This task is continued on #8338

### Components (Part 1)
- [x] Autocomplete
- [x] BaseControl
- [x] Button
- [x] ButtonGroup
- [x] CheckboxControl
- [x] ClipboardButton
- [x] ColorIndicator
- [x] ColorPalette
- [x] Dashicon
- [x] DateTime
- [x] Disabled
- [x] Draggable
- [x] DropZone
- [x] DropdownMenu
- [x] Dropdown
- [x] ExternalLink
- [x] FocusableIframe
- [x] FontSizePicker
- [x] FormFileUpload
- [x] FormToggle
- [x] FormTokenField
- [x] IconButton
- [x] KeyboardShortcuts
- [x] MenuGroup
- [x] MenuItem
- [x] MenuItemsChoice

## Issue comments

### mmtr on 2018-07-31T20:54:35Z

URL: https://github.com/WordPress/gutenberg/pull/8267#issuecomment-409364554

@gziolo as discussed, I've split this PR into 2 parts, so this first one should be ready to review :) thanks!

### mmtr on 2018-07-31T20:55:54Z

URL: https://github.com/WordPress/gutenberg/pull/8267#issuecomment-409364959

note that some of the current README files already provide working examples, so I have not modified them

### gziolo on 2018-08-02T06:07:51Z

URL: https://github.com/WordPress/gutenberg/pull/8267#issuecomment-409816549

It looks solid, I don't want to block you from progressing on your work because of nitpicks. If you find a way to include `render` or at least `const` declaration it would be amazing. It can be done inside the 2nd part of this PR.

Awesome work and very quick iterations loop 💯

### gziolo on 2018-08-02T06:08:51Z

URL: https://github.com/WordPress/gutenberg/pull/8267#issuecomment-409816736

There are only `README.md` changes, Travis can't complain ... merging.

## Review threads

### Review thread 1

#### gziolo on 2018-07-29T10:11:37Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r205967907
Path: packages/components/src/disabled/README.md

```diff
@@ -8,28 +8,39 @@ Disabled is a component which disables descendant tabbable elements and prevents
 Assuming you have a form component, you can disable all form inputs by wrapping the form with `<Disabled>`.
 
 ```jsx
-const DisableToggleForm = withState( {
-	isDisabled: true,
-} )( ( { isDisabled, setState } ) => {
-	let form = <form><input /></form>;
-
-	if ( isDisabled ) {
-		form = <Disabled>{ form }</Disabled>;
+class ToggleDisable extends React.Component {
```

We favor functional components, it should stay as it was. Using Component class with lifecycle methods is more complex, requires more code and might be subject of change in the future. Actually, React 16.3 is a great example how those API can get deprecated.

#### mmtr on 2018-07-29T14:43:35Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r205976607
Path: packages/components/src/disabled/README.md

```diff
@@ -8,28 +8,39 @@ Disabled is a component which disables descendant tabbable elements and prevents
 Assuming you have a form component, you can disable all form inputs by wrapping the form with `<Disabled>`.
 
 ```jsx
-const DisableToggleForm = withState( {
-	isDisabled: true,
-} )( ( { isDisabled, setState } ) => {
-	let form = <form><input /></form>;
-
-	if ( isDisabled ) {
-		form = <Disabled>{ form }</Disabled>;
+class ToggleDisable extends React.Component {
```

got it!

### Review thread 2

#### gziolo on 2018-07-29T10:13:23Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r205967977
Path: packages/components/src/clipboard-button/README.md

```diff
@@ -0,0 +1,28 @@
+ClipboardButton
+===============
+
+## Usage
+
+```jsx
+class MyClipboardButton extends React.Component {
```

This example can also be expressed as functional component wrapped with withState higher-order component (HOC). I believe it is code this way in the existing codebase.

### Review thread 3

#### gziolo on 2018-07-29T10:14:45Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r205968009
Path: packages/components/src/base-control/README.md

```diff
@@ -8,17 +8,19 @@ BaseControl component is used to generate labels and help text for components ha
 
 Render a BaseControl for a textarea input:
 ```jsx
-	<BaseControl
```

Can we also include import statement to make it possible to extract code as is and run it without any modifications?

#### mmtr on 2018-07-29T12:59:45Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r205972957
Path: packages/components/src/base-control/README.md

```diff
@@ -8,17 +8,19 @@ BaseControl component is used to generate labels and help text for components ha
 
 Render a BaseControl for a textarea input:
 ```jsx
-	<BaseControl
```

Yeah, that would be great but I'm afraid I have a limitation here. 

I'm using [`react-live`](https://github.com/FormidableLabs/react-live) for compiling and running these snippets of code in real time, so we avoid to change our build process.

That forces us to import the dependencies outside the snippets in order to define the scope needed by `react-live` to run the extracted code.

#### mmtr on 2018-07-29T13:32:34Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r205973952
Path: packages/components/src/base-control/README.md

```diff
@@ -8,17 +8,19 @@ BaseControl component is used to generate labels and help text for components ha
 
 Render a BaseControl for a textarea input:
 ```jsx
-	<BaseControl
```

Anyway, I think we should maintain them in the examples in order to provide documentation as complete as possible. We can always ignore them when running the snippets (you can check how I'm handling that here: https://github.com/Automattic/wp-calypso/pull/26367/files#diff-d0c3bde7d6504cb5de69c0488b877342R36)

### Review thread 4

#### gziolo on 2018-08-01T04:53:33Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206754776
Path: packages/components/src/autocomplete/README.md

```diff
@@ -105,33 +105,59 @@ Whether to apply debouncing for the autocompleter. Set to true to enable debounc
 - Type: `Boolean`
 - Required: No
 
-### Examples
+## Usage
 
 The following is a contrived completer for fresh fruit.
 
 ```jsx
-const fruitCompleter = {
-	name: 'fruit',
-	// The prefix that triggers this completer
-	triggerPrefix: '~',
-	// The option data
-	options: [
-		{ visual: '🍎', name: 'Apple' },
-		{ visual: '🍊', name: 'Orange' },
-		{ visual: '🍇', name: 'Grapes' },
-	],
-	// Returns a label for an option like "🍊 Orange"
-	getOptionLabel: option => [
-		<span class="icon">{ option.visual }</span>,
-		option.name
-	],
-	// Declares that options should be matched by their name
-	getOptionKeywords: option => [ option.name ],
-	// Declares that the Grapes option is disabled
-	isOptionDisabled: option => option.name === 'Grapes',
-	// Declares completions should be inserted as abbreviations
-	getOptionCompletion: option => (
-		<abbr title={ option.name }>{ option.visual }</abbr>
-	),
+import { Autocomplete } from '@wordpress/components';
+
+function FreshFruitAutocomplete() {
```

Out of curiosity, don't you need to export those components to be able to use them in Calypso devdocs? For the purpose of docs it's fine as is. However, if that would help, don't hesitate to add export statements.

#### mmtr on 2018-08-01T19:03:21Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206996898
Path: packages/components/src/autocomplete/README.md

```diff
@@ -105,33 +105,59 @@ Whether to apply debouncing for the autocompleter. Set to true to enable debounc
 - Type: `Boolean`
 - Required: No
 
-### Examples
+## Usage
 
 The following is a contrived completer for fresh fruit.
 
 ```jsx
-const fruitCompleter = {
-	name: 'fruit',
-	// The prefix that triggers this completer
-	triggerPrefix: '~',
-	// The option data
-	options: [
-		{ visual: '🍎', name: 'Apple' },
-		{ visual: '🍊', name: 'Orange' },
-		{ visual: '🍇', name: 'Grapes' },
-	],
-	// Returns a label for an option like "🍊 Orange"
-	getOptionLabel: option => [
-		<span class="icon">{ option.visual }</span>,
-		option.name
-	],
-	// Declares that options should be matched by their name
-	getOptionKeywords: option => [ option.name ],
-	// Declares that the Grapes option is disabled
-	isOptionDisabled: option => option.name === 'Grapes',
-	// Declares completions should be inserted as abbreviations
-	getOptionCompletion: option => (
-		<abbr title={ option.name }>{ option.visual }</abbr>
-	),
+import { Autocomplete } from '@wordpress/components';
+
+function FreshFruitAutocomplete() {
```

`react-live` doesn't need it. As long as you inject a string with either a component class, a functional component or JSX code, it will be able to render it: https://react-live.kitten.sh/

#### gziolo on 2018-08-02T06:04:17Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r207109194
Path: packages/components/src/autocomplete/README.md

```diff
@@ -105,33 +105,59 @@ Whether to apply debouncing for the autocompleter. Set to true to enable debounc
 - Type: `Boolean`
 - Required: No
 
-### Examples
+## Usage
 
 The following is a contrived completer for fresh fruit.
 
 ```jsx
-const fruitCompleter = {
-	name: 'fruit',
-	// The prefix that triggers this completer
-	triggerPrefix: '~',
-	// The option data
-	options: [
-		{ visual: '🍎', name: 'Apple' },
-		{ visual: '🍊', name: 'Orange' },
-		{ visual: '🍇', name: 'Grapes' },
-	],
-	// Returns a label for an option like "🍊 Orange"
-	getOptionLabel: option => [
-		<span class="icon">{ option.visual }</span>,
-		option.name
-	],
-	// Declares that options should be matched by their name
-	getOptionKeywords: option => [ option.name ],
-	// Declares that the Grapes option is disabled
-	isOptionDisabled: option => option.name === 'Grapes',
-	// Declares completions should be inserted as abbreviations
-	getOptionCompletion: option => (
-		<abbr title={ option.name }>{ option.visual }</abbr>
-	),
+import { Autocomplete } from '@wordpress/components';
+
+function FreshFruitAutocomplete() {
```

How do you feel about using the version which calls `render`?

```jsx
const Wrapper = ({ children }) => (
  <div style={{
    background: 'papayawhip',
    width: '100%',
    padding: '2rem'
  }}>
    {children}
  </div>
)

const Title = () => (
  <h3 style={{ color: 'palevioletred' }}>
    Hello World!
  </h3>
)

render(
  <Wrapper>
    <Title />
  </Wrapper>
)
```

3rd example on the demo page.

#### mmtr on 2018-08-02T21:46:34Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r207385108
Path: packages/components/src/autocomplete/README.md

```diff
@@ -105,33 +105,59 @@ Whether to apply debouncing for the autocompleter. Set to true to enable debounc
 - Type: `Boolean`
 - Required: No
 
-### Examples
+## Usage
 
 The following is a contrived completer for fresh fruit.
 
 ```jsx
-const fruitCompleter = {
-	name: 'fruit',
-	// The prefix that triggers this completer
-	triggerPrefix: '~',
-	// The option data
-	options: [
-		{ visual: '🍎', name: 'Apple' },
-		{ visual: '🍊', name: 'Orange' },
-		{ visual: '🍇', name: 'Grapes' },
-	],
-	// Returns a label for an option like "🍊 Orange"
-	getOptionLabel: option => [
-		<span class="icon">{ option.visual }</span>,
-		option.name
-	],
-	// Declares that options should be matched by their name
-	getOptionKeywords: option => [ option.name ],
-	// Declares that the Grapes option is disabled
-	isOptionDisabled: option => option.name === 'Grapes',
-	// Declares completions should be inserted as abbreviations
-	getOptionCompletion: option => (
-		<abbr title={ option.name }>{ option.visual }</abbr>
-	),
+import { Autocomplete } from '@wordpress/components';
+
+function FreshFruitAutocomplete() {
```

It's not trivial. 

That `render` call should be performed in Calypso (it doesn't make sense to include it in the Gutenberg examples), but how Calypso can know what are the components that need to be rendered?

For rendering the Autocomplete example it should use `render( <FreshFruitAutocomplete /> )`, but for the Button example it should use `render( <MyButton /> )`. But there is no way for getting this in a dynamic way.

Maybe we can use always the `My<ComponentName>` pattern and assume that in Calypso. But we need to be sure that the name of the variables doesn't change in the example or we'll broke the Calypso's DevDocs. What do you think?

#### gziolo on 2018-08-04T10:03:01Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r207704978
Path: packages/components/src/autocomplete/README.md

```diff
@@ -105,33 +105,59 @@ Whether to apply debouncing for the autocompleter. Set to true to enable debounc
 - Type: `Boolean`
 - Required: No
 
-### Examples
+## Usage
 
 The following is a contrived completer for fresh fruit.
 
 ```jsx
-const fruitCompleter = {
-	name: 'fruit',
-	// The prefix that triggers this completer
-	triggerPrefix: '~',
-	// The option data
-	options: [
-		{ visual: '🍎', name: 'Apple' },
-		{ visual: '🍊', name: 'Orange' },
-		{ visual: '🍇', name: 'Grapes' },
-	],
-	// Returns a label for an option like "🍊 Orange"
-	getOptionLabel: option => [
-		<span class="icon">{ option.visual }</span>,
-		option.name
-	],
-	// Declares that options should be matched by their name
-	getOptionKeywords: option => [ option.name ],
-	// Declares that the Grapes option is disabled
-	isOptionDisabled: option => option.name === 'Grapes',
-	// Declares completions should be inserted as abbreviations
-	getOptionCompletion: option => (
-		<abbr title={ option.name }>{ option.visual }</abbr>
-	),
+import { Autocomplete } from '@wordpress/components';
+
+function FreshFruitAutocomplete() {
```

I missed that comment. It seems like a good pattern, let's discuss further in the 2nd part of the PR where those changes are applied.

### Review thread 5

#### gziolo on 2018-08-01T04:55:44Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206754993
Path: packages/components/src/base-control/README.md

```diff
@@ -1,5 +1,5 @@
 BaseControl
-=======
+===========
```

To avoid some hassle with adding those equality signs, you can also simply use:
```
# BaseControl
```

We use this form in `README.md` files in the root folder of each npm package.

### Review thread 6

#### gziolo on 2018-08-01T04:56:49Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206755106
Path: packages/components/src/button/README.md

```diff
@@ -1,8 +1,28 @@
+Button
+======
+
 This component is used to implement dang sweet buttons.
 
-#### Props
+## Props
 
 The following props are used to control the display of the component. Any additional props will be passed to the rendered `<a />` or `<button />` element. The presence of a `href` prop determines whether an anchor element is rendered instead of a button.
 
 * `isPrimary`: (bool) whether the button is styled as a primary button.
 * `href`: (string) if this property is added, it will use an `a` rather than a `button` element.
+
+## Usage
+
+```jsx
+import { Button } from '@wordpress/components';
+
+function MyButton() {
+	return (
+		<Button 
+			isPrimary 
+			href="https://wordpress.com"
```

Gutenberg, it's a community project. Let's use `https://wordpress.org/` instead 😄

### Review thread 7

#### gziolo on 2018-08-01T04:59:56Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206755521
Path: packages/components/src/checkbox-control/README.md

```diff
@@ -8,13 +8,18 @@ CheckboxControl component is used to generate a checkbox input field.
 
 Render an is author checkbox:
 ```jsx
-    <CheckboxControl
-        heading="User"
-        label="Is author"
-        help="Is the user a author or not?"
-        checked={ checked }
-        onChange={ onChange }
-    />
+import { CheckboxControl } from '@wordpress/components';
+
+function MyCheckboxControl() {
+	return (
+		<CheckboxControl
+			heading="User"
+			label="Is author"
+			help="Is the user a author or not?"
+			onChange={ () => {} }
```

I think it would make sense to include `checked` property, too. Otherwise this example won't be very useful. In such case `onChange` would need to be update. We can use something similar to what we have in `ClipboardButton` using `withState`.

### Review thread 8

#### gziolo on 2018-08-01T05:03:03Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206755832
Path: packages/components/src/clipboard-button/README.md

```diff
@@ -0,0 +1,22 @@
+ClipboardButton
+===============
+
+## Usage
+
+```jsx
+import { ClipboardButton } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
+	hasCopied: false,
+} )( ( { hasCopied, setState } ) => ( 
+	<ClipboardButton
+		isPrimary
+		text="WordPress"
```

It might to make it easier to understand if we would add some description inside `text` prop, .e.g.: `Text to be copied.`.

### Review thread 9

#### gziolo on 2018-08-01T05:05:13Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756070
Path: packages/components/src/clipboard-button/README.md

```diff
@@ -0,0 +1,22 @@
+ClipboardButton
+===============
+
+## Usage
+
+```jsx
+import { ClipboardButton } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

Should we assign it to some `const`?

#### mmtr on 2018-08-01T20:27:12Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r207020076
Path: packages/components/src/clipboard-button/README.md

```diff
@@ -0,0 +1,22 @@
+ClipboardButton
+===============
+
+## Usage
+
+```jsx
+import { ClipboardButton } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

I agree it would make the docs nicer, but `react-live` won't be able to parse it and render the example. I'll try to figure out an alternative in Calypso so we can have better docs here (as we do with the imports)

### Review thread 10

#### gziolo on 2018-08-01T05:05:28Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756102
Path: packages/components/src/color-palette/README.md

```diff
@@ -0,0 +1,22 @@
+ColorPalette
+============
+
+## Usage
+```jsx
+import { ColorPalette } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

Should we assign it to some `const`?

### Review thread 11

#### gziolo on 2018-08-01T05:07:10Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756261
Path: packages/components/src/color-palette/README.md

```diff
@@ -0,0 +1,22 @@
+ColorPalette
+============
+
+## Usage
+```jsx
+import { ColorPalette } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
+	color: '#f00',
+} )( ( { color, setState } ) => { 
+	const colors = [ { name: 'red', color: '#f00' }, { name: 'white', color: '#fff' }, { name: 'blue', color: '#00f' } ];
```

Can we put each color definition in its own line?
```js
const colors = [
    { name: 'red', color: '#f00' },
    { name: 'white', color: '#fff' },
    { name: 'blue', color: '#00f' },
];
```

### Review thread 12

#### gziolo on 2018-08-01T05:08:24Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756350
Path: packages/components/src/disabled/README.md

```diff
@@ -8,25 +8,27 @@ Disabled is a component which disables descendant tabbable elements and prevents
 Assuming you have a form component, you can disable all form inputs by wrapping the form with `<Disabled>`.
 
 ```jsx
-const DisableToggleForm = withState( {
-	isDisabled: true,
-} )( ( { isDisabled, setState } ) => {
-	let form = <form><input /></form>;
+import { Button, Disabled, TextControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
 
+withState( {
+	isDisabled: true,
+} )( ( { isDisabled, setState } ) => { 
+	let input = <TextControl label="Input" onChange={ () => {} } />;
 	if ( isDisabled ) {
-		form = <Disabled>{ form }</Disabled>;
+		input = <Disabled>{ input }</Disabled>;
 	}
-
-	const toggleDisabled = setState( ( state ) => ( {
-		isDisabled: ! state.isDisabled,
-	} ) );
-
+	
+	const toggleDisabled = () => {
```

Nice catch 👍

### Review thread 13

#### gziolo on 2018-08-01T05:09:26Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756435
Path: packages/components/src/disabled/README.md

```diff
@@ -8,25 +8,27 @@ Disabled is a component which disables descendant tabbable elements and prevents
 Assuming you have a form component, you can disable all form inputs by wrapping the form with `<Disabled>`.
 
 ```jsx
-const DisableToggleForm = withState( {
-	isDisabled: true,
-} )( ( { isDisabled, setState } ) => {
-	let form = <form><input /></form>;
+import { Button, Disabled, TextControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
 
+withState( {
+	isDisabled: true,
+} )( ( { isDisabled, setState } ) => { 
+	let input = <TextControl label="Input" onChange={ () => {} } />;
 	if ( isDisabled ) {
-		form = <Disabled>{ form }</Disabled>;
+		input = <Disabled>{ input }</Disabled>;
 	}
-
-	const toggleDisabled = setState( ( state ) => ( {
-		isDisabled: ! state.isDisabled,
-	} ) );
-
+	
+	const toggleDisabled = () => {
+		setState( state => ( { isDisabled: ! state.isDisabled } ) );
```

`setState( ( state ) => ...` - our linter would complain otherwise 😅

### Review thread 14

#### gziolo on 2018-08-01T05:10:00Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756489
Path: packages/components/src/disabled/README.md

```diff
@@ -8,25 +8,27 @@ Disabled is a component which disables descendant tabbable elements and prevents
 Assuming you have a form component, you can disable all form inputs by wrapping the form with `<Disabled>`.
 
 ```jsx
-const DisableToggleForm = withState( {
-	isDisabled: true,
-} )( ( { isDisabled, setState } ) => {
-	let form = <form><input /></form>;
+import { Button, Disabled, TextControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
 
+withState( {
```

Should we assign to some `const`?

### Review thread 15

#### gziolo on 2018-08-01T05:11:15Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756592
Path: packages/components/src/drop-zone/README.md

```diff
@@ -5,13 +5,18 @@
 ## Usage
 
 ```jsx
-import { DropZoneProvider, DropZone } from '@wordpress/components';
+import { DropZoneProvider, DropZone, Panel } from '@wordpress/components';
 
 function MyComponent() {
 	return (
 		<DropZoneProvider>
 			<div>
-				<DropZone onDrop={ () => console.log( 'do something' ) } />
+				Drop something here
+				<DropZone 
+					onFilesDrop={ () => console.log( 'do something' ) }
```

Assuming that it is going to be rendered inside Calypso devdocs, we might want to customize those messages.

### Review thread 16

#### gziolo on 2018-08-01T05:11:53Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756670
Path: packages/components/src/external-link/README.md

```diff
@@ -0,0 +1,14 @@
+ExternalLink
+============
+
+## Usage
+
+```jsx
+import { ExternalLink } from '@wordpress/components';
+
+function MyExternalLink() {
+	return (
+		<ExternalLink href="https://wordpress.com">WordPress.com</ExternalLink>
```

Again, WordPress.org please :)

### Review thread 17

#### gziolo on 2018-08-01T05:12:58Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756797
Path: packages/components/src/focusable-iframe/README.md

```diff
@@ -13,8 +13,8 @@ import { FocusableIframe } from '@wordpress/components';
 function MyIframe() {
 	return (
 		<FocusableIframe
-			src="https://example.com"
-			onFocus={ /* ... */ }
+			src="/"
+			onFocus={ () => console.log('iframe is focused') }
```

Linter would be sad and prefer `console.log( 'iframe is focused' )` 😄

### Review thread 18

#### gziolo on 2018-08-01T05:13:31Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756849
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -8,21 +8,26 @@ The component renders a series of buttons that allow the user to select predefin
 
 
 ```jsx
-import { Dropdown } from '@wordpress/components';
-
-function MyFontSizePicker() {
-	return (
-		<FontSizePicker
-			fontSizes={ [
-				{ shortName: 'S', size: 12 },
-				{ shortName: 'M', size: 16 }
-			] }
-			fallbackFontSize={ fallbackFontSize }
+import { FontSizePicker } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

`const MyFontSizePicker = `?

### Review thread 19

#### gziolo on 2018-08-01T05:14:29Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206756954
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -8,21 +8,26 @@ The component renders a series of buttons that allow the user to select predefin
 
 
 ```jsx
-import { Dropdown } from '@wordpress/components';
-
-function MyFontSizePicker() {
-	return (
-		<FontSizePicker
-			fontSizes={ [
-				{ shortName: 'S', size: 12 },
-				{ shortName: 'M', size: 16 }
-			] }
-			fallbackFontSize={ fallbackFontSize }
+import { FontSizePicker } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
+	fontSize: 16,
+} )( ( { fontSize, setState } ) => { 
+	const fontSizes = [
+		{ shortName: 'S', size: 12 },
+		{ shortName: 'M', size: 16 }
+	];
+	const fallbackFontSize = 16;
+	
+	return ( 
+		<FontSizePicker 
+			fontSizes={ fontSizes } 
 			value={ fontSize }
-			onChange={ this.setFontSize }
+			onChange={ fontSize => setState( { fontSize } ) } 
 		/>
-	);
-}
+	)
```

Missing semicolon;

### Review thread 20

#### gziolo on 2018-08-01T05:14:55Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757009
Path: packages/components/src/font-size-picker/README.md

```diff
@@ -8,21 +8,26 @@ The component renders a series of buttons that allow the user to select predefin
 
 
 ```jsx
-import { Dropdown } from '@wordpress/components';
-
-function MyFontSizePicker() {
-	return (
-		<FontSizePicker
-			fontSizes={ [
-				{ shortName: 'S', size: 12 },
-				{ shortName: 'M', size: 16 }
-			] }
-			fallbackFontSize={ fallbackFontSize }
+import { FontSizePicker } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
+	fontSize: 16,
+} )( ( { fontSize, setState } ) => { 
+	const fontSizes = [
+		{ shortName: 'S', size: 12 },
+		{ shortName: 'M', size: 16 }
+	];
+	const fallbackFontSize = 16;
+	
+	return ( 
+		<FontSizePicker 
+			fontSizes={ fontSizes }
```

`fallbackFontSize={ fallbackFontSize }` line is now missing.

### Review thread 21

#### gziolo on 2018-08-01T05:15:28Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757084
Path: packages/components/src/form-toggle/README.md

```diff
@@ -0,0 +1,18 @@
+FormToggle
+==========
+
+## Usage
+
+```jsx
+import { FormToggle } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

`const MyFormToggle = `?

### Review thread 22

#### gziolo on 2018-08-01T05:16:26Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757203
Path: packages/components/src/form-token-field/README.md

```diff
@@ -52,21 +52,21 @@ The `value` property is handled in a manner similar to controlled form component
 - `disabled` - When true, tokens are not able to be added or removed.
 - `placeholder` - If passed, the `TokenField` input will show a placeholder string if no value tokens are present.
 
-### Example
+## Usage
 
 ```jsx
-class extends Component {
-	render() {
-		return (
-			<FormTokenField
-				value={ this.state.tokens }
-				onChange={ this.onTokensChange }
-				suggestions={ this.state.suggestions } />
-		);
-	}
+import { FormTokenField } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
 
-	onTokensChange( value ) {
-		this.setState( { tokens: value } );
-	}
-}
+withState( {
```

`const ContinentsFormTokenField =`?

### Review thread 23

#### gziolo on 2018-08-01T05:17:12Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757281
Path: packages/components/src/form-token-field/README.md

```diff
@@ -52,21 +52,21 @@ The `value` property is handled in a manner similar to controlled form component
 - `disabled` - When true, tokens are not able to be added or removed.
 - `placeholder` - If passed, the `TokenField` input will show a placeholder string if no value tokens are present.
 
-### Example
+## Usage
 
 ```jsx
-class extends Component {
-	render() {
-		return (
-			<FormTokenField
-				value={ this.state.tokens }
-				onChange={ this.onTokensChange }
-				suggestions={ this.state.suggestions } />
-		);
-	}
+import { FormTokenField } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
 
-	onTokensChange( value ) {
-		this.setState( { tokens: value } );
-	}
-}
+withState( {
+	tokens: [],
+	suggestions: ['Africa', 'America', 'Antarctica', 'Asia', 'Europe', 'Oceania'],
```

Missing spaces after `[` and before `]`, sorry about WordPress conventions 😄

### Review thread 24

#### gziolo on 2018-08-01T05:18:03Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757382
Path: packages/components/src/keyboard-shortcuts/README.md

```diff
@@ -12,30 +12,25 @@ It uses the [Mousetrap](https://craig.is/killing/mice) library to implement keyb
 Render `<KeyboardShortcuts />` with a `shortcuts` prop object:
 
 ```jsx
-class SelectAllDetection extends Component {
-	constructor() {
-		super( ...arguments );
-
-		this.setAllSelected = this.setAllSelected.bind( this );
-
-		this.state = { isAllSelected: false };
-	}
-
-	setAllSelected() {
-		this.setState( { isAllSelected: true } );
-	}
-
-	render() {
-		return (
-			<div>
-				<KeyboardShortcuts shortcuts={ {
-					'mod+a': this.setAllSelected,
-				} } />
-				Combination pressed? { isAllSelected ? 'Yes' : 'No' }
-			</div>
-		);
-	}
-}
+import { KeyboardShortcuts } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

`const My KeyboardShortcuts = `?

### Review thread 25

#### gziolo on 2018-08-01T05:20:20Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757624
Path: packages/components/src/menu-group/README.md

```diff
@@ -0,0 +1,17 @@
+MenuGroup
+=========
+
+## Usage
+
+```jsx
+import { MenuGroup } from '@wordpress/components';
+
+function MyMenuGroup() {
+	return (
+		<MenuGroup label="Settings">
+			<div>Setting 1</div>
```

It might be better to put `MenuItem` in here:

```jsx
<MenuGroup label="Settings">
	<MenuItem>Setting 1</MenuItem>
	<MenuItem>Setting 2</MenuItem>
</MenuGroup>
```

This is how it should be used in general.

### Review thread 26

#### gziolo on 2018-08-01T05:21:04Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757688
Path: packages/components/src/menu-item/README.md

```diff
@@ -0,0 +1,21 @@
+MenuItem
+========
+
+## Usage
+
+```jsx
+import { MenuItem } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

`const MyMenuItem = `?

### Review thread 27

#### gziolo on 2018-08-01T05:21:37Z

URL: https://github.com/WordPress/gutenberg/pull/8267#discussion_r206757744
Path: packages/components/src/menu-items-choice/README.md

```diff
@@ -0,0 +1,31 @@
+MenuItemsChoice
+===============
+
+## Usage
+
+```jsx
+import { MenuGroup, MenuItemsChoice } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+withState( {
```

`const MyMenuItemsChoice = `?

## Changed files

- packages/components/src/autocomplete/README.md (MODIFIED, +51/-26)
- packages/components/src/base-control/README.md (MODIFIED, +16/-13)
- packages/components/src/button-group/README.md (ADDED, +16/-0)
- packages/components/src/button/README.md (MODIFIED, +1/-4)
- packages/components/src/checkbox-control/README.md (MODIFIED, +15/-9)
- packages/components/src/clipboard-button/README.md (ADDED, +21/-0)
- packages/components/src/color-indicator/README.md (ADDED, +13/-0)
- packages/components/src/color-palette/README.md (ADDED, +25/-0)
- packages/components/src/dashicon/README.md (ADDED, +17/-0)
- packages/components/src/date-time/README.md (MODIFIED, +1/-2)
- packages/components/src/disabled/README.md (MODIFIED, +16/-15)
- packages/components/src/draggable/README.md (MODIFIED, +23/-0)
- packages/components/src/drop-zone/README.md (MODIFIED, +16/-10)
- packages/components/src/dropdown-menu/README.md (MODIFIED, +6/-7)
- packages/components/src/dropdown/README.md (MODIFIED, +4/-5)
- packages/components/src/external-link/README.md (ADDED, +13/-0)
- packages/components/src/focusable-iframe/README.md (MODIFIED, +3/-4)
- packages/components/src/font-size-picker/README.md (MODIFIED, +20/-15)
- packages/components/src/form-file-upload/README.md (ADDED, +18/-0)
- packages/components/src/form-toggle/README.md (ADDED, +17/-0)
- packages/components/src/form-token-field/README.md (MODIFIED, +17/-18)
- packages/components/src/icon-button/README.md (ADDED, +16/-0)
- packages/components/src/keyboard-shortcuts/README.md (MODIFIED, +20/-26)
- packages/components/src/menu-group/README.md (ADDED, +16/-0)
- packages/components/src/menu-item/README.md (ADDED, +20/-0)
- packages/components/src/menu-items-choice/README.md (ADDED, +30/-0)
