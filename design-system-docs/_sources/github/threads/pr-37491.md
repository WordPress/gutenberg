---
number: 37491
type: pull_request
state: MERGED
merged_at: 2022-02-16T19:24:50Z
url: https://github.com/WordPress/gutenberg/pull/37491
matched_labels: ["[Package] Components","[Feature] Component System"]
all_labels: ["[Package] Components","[Feature] Component System"]
---

# PR #37491: Migrate the Post 'Switch to draft' button to `ConfirmDialog` component

- URL: https://github.com/WordPress/gutenberg/pull/37491
- Author: chad1008
- Created: 2021-12-17T15:51:34Z
- Updated: 2022-02-16T19:25:15Z
- Comments: 11 of 11
- Review threads: 6 of 6
- Changed files: 2 of 2
- Diff: ../pr-diffs/37491.diff

## Body

Related: https://github.com/WordPress/gutenberg/pull/34153

<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/repository-management.md#pull-requests. -->

<!-- Gutenberg's license is in the process of updating to be dual-licensed under the GPL and MPL. As part of that transition, all new contributions are dual-licensed. For more information, see: https://github.com/WordPress/gutenberg/blob/trunk/LICENSE.md -->

## Description
This PR aims to migrate the post editor's `Switch to draft` button away from the current `confirm()` implementation and instead use the new experimental `ConfirmDialog` component.

## How has this been tested?
Running WordPress 5.8.2 via `wp-env`:
1. Create a new post
2. Add some content, a title, etc.
3. Publish the post
4. Click the `Switch to draft` button
5. Monitor console for any unexpected errors
6. Confirm that the new `ConfirmDialog` component is rendered instead of the previous browser-based confirmation triggered by `confirm()`
7. Click `Cancel`, and confirm that the post remains published
8. Click `Switch to draft` again, and this time hit `OK`. Confirm the post is reverted to a draft

I've tested in the latest Chrome, Firefox, and Safari.

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->

## Issue comments

### chad1008 on 2021-12-17T23:58:41Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-997097966

Marking as draft - there's a bug that causes the `Switch to draft` button to be disabled if the user clicks `Cancel` the first time the confirmation is displayed - I can fix it by lifting the `showConfirmDialog` state to a parent component. I'll reopen after making that adjustment.

### chad1008 on 2021-12-20T18:17:33Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-998162904

Fixed - state is now handled more effectively, and the confirmation dialog is reset after the cancel button is clicked.

### chad1008 on 2022-01-07T18:09:50Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1007621850

Updated this PR to simplify the implementation of `ConfirmDialog` (cc @ciampo)

### ciampo on 2022-01-21T09:21:54Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1018327343

Update:

- I marked all of the memoization-related conversations as resolved for now, as discussed in [this comment](https://github.com/WordPress/gutenberg/pull/37492#discussion_r780807376)
- there's still an [open comment](https://github.com/WordPress/gutenberg/pull/37491#discussion_r780805958) that needs addressing
- not sure if you managed to look into fixing any broken e2e/unit tests (if any) and potentially adding any new ones

Even with the last comment addressed, I'd still be reluctant to merge this PR until we find a good fix for the `z-index` issue (see https://github.com/WordPress/gutenberg/pull/37959).

### ciampo on 2022-02-04T15:14:32Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1030078991

With #37959 merged, we can consider this PR unblocked.

@chad1008 , feel free to continue working on this PR (I believe we're only missing any potential e2e test work) and ping me when it's ready for the next round of review!

### chad1008 on 2022-02-08T22:19:22Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1033120357

e2e tests added! I didn't see any existing tests for the switch to draft button so I created new ones. The new tests should cover:
- confirming and canceling the confirmation
- posts and pages
- published vs scheduled
- large and small viewports

...working out to 16 tests in total, all passing consistent locally.

### chad1008 on 2022-02-10T11:13:58Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1034793612

Thanks for the feedback @ciampo, I'll take a look!

This is helpful for me on a more general level... these are both sections of the test I was able to "borrow" from other tests I've worked on recently (i.e. "I need to schedule this post, OH, I know where someone else did that, I'll just reuse that flow!").

After some of your feedback on other PRs, I think I've gotten better about relying on DOM structure when writing my own XPaths/selectors, but I clearly need to apply that same philosophy to code snippets I'm re-using!

### ciampo on 2022-02-10T13:19:14Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1034913604

> these are both sections of the test I was able to "borrow" from other tests I've worked on recently
> ...
> After some of your feedback on other PRs, I think I've gotten better about relying on DOM structure when writing my own XPaths/selectors, but I clearly need to apply that same philosophy to code snippets I'm re-using!

Absolutely! It'd be also great if, once you find a good way to write the e22e tests on this PR, you open another PR to improve the tests you "borrowed" from :)

This [blog post](https://kentcdodds.com/blog/testing-implementation-details) partially explains the reasoning behind not to test the implementation details (it's focused on React unit testing, but some core principles are the same)

### chad1008 on 2022-02-10T20:57:10Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1035508426

I've updated the two class-based XPaths mentioned above, and made a couple other small improvements - there are now no selectors reliant on DOM structure in the test!

### Mamaduka on 2022-02-11T10:37:36Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1036068751

I just merged the #38524. I will try to have a look on e2e tests later today.

### aaronrobertshaw on 2022-02-14T03:41:48Z

URL: https://github.com/WordPress/gutenberg/pull/37491#issuecomment-1038601945

> Before merging, it's probably better to double-check that the approach we're taking (ie. avoiding testing implementation details like dom structure, and writing tests more from the point of view of the user) — just cc'ing a few folks who have recently worked in the e2e-tests package in case they any potential issues cc @aaronrobertshaw

In general, I'd agree if we can avoid relying on the DOM structure and write them from the user's point of view, we should. 

In practice, for a lot of e2e tests, I'm not sure how feasible that is. It appears to be pretty common practice in the existing tests to target classes etc. Just because that's the way it was done doesn't mean we should continue that way.

I haven't had a chance to properly test this but I did leave one suggestion. If we leverage the `matches()` XPath function we could possibly use its case-insensitive flag to avoid having to find and manipulate text capitalization.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-01-09T16:44:51Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r780804949
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
```

This is a small optimization, but at the moment the `onClick` callback is being re-created everytime, potentially causing unnecessary re-renders for the `Button` component.

An alternative could be to leverage `useCallback` and memoize that function:

```js
const handleButtonClick = useCallback( () => {
  setShowConfirmDialog( true );
}, [] );

// ...

return (
  <>
    <Button onClick={ handleButtonClick } {/* ... */} />
     {/* ... */} 
  </>
);
```

No dependencies should be needed in the array of dependencies for `useCallback`, since React guarantees that `setShowConfirmDialog`'s identity is stable across re-renders ([#](https://reactjs.org/docs/hooks-reference.html#usestate))

### Review thread 2 (resolved)

#### ciampo on 2022-01-09T16:46:55Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r780805225
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
+				onCancel={ () => setShowConfirmDialog( false ) }
```

Same observation here around memoizing the `onCancel` callback (see [related](https://github.com/WordPress/gutenberg/pull/37491#discussion_r780804949))

### Review thread 3 (resolved)

#### ciampo on 2022-01-09T16:52:55Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r780805958
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Should we also close the dialog when it's confirmed? This seems to be the case with the current implementation (native confirm). E.g.

```js
const handleConfirm = useCallback( () => {
  setShowConfirmDialog( false );
  onClick();
}, [ onClick ]);

// ...

return (
  <>
    {/* ... */}
    <ConfirmDialog
      isOpen={ showConfirmDialog }
      onConfirm={ handleConfirm }
      {/* ... */}
    />

    {/* ... */}
  </>
);
```

#### chad1008 on 2022-01-11T22:24:56Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r782571387
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Happy to change that, though I would like to understand the benefit a little better. In my tests, the dialog does close when confirmed, and when I inspect the next render of the `DeleteMenu` component, `showConfirmDialog` is once again `false`.

If both of those things are happening by default, what do we gain by explicitly closing as you've suggested?

(To be clear, I'm asking from genuine curiosity and a desire to learn, not because I disagree with the recommendation 😊 )

#### ciampo on 2022-01-11T22:30:25Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r782574166
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

> In my tests, the dialog does close when confirmed, and when I inspect the next render of the DeleteMenu component, showConfirmDialog is once again false.

I thought that the dialog doesn't self-close on confirm when the `isOpen` prop is set (see [here](https://github.com/WordPress/gutenberg/blob/59dce7546f86abece759066769d8c60b139cca31/packages/components/src/confirm-dialog/component.tsx#L43-L51)) — that's why I suggested to close it explicitly.

Curious to see then if that snippet of code that I highlighted is not working as expected?

#### chad1008 on 2022-01-12T13:51:04Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r783092121
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Ah, I see your point, thank you for clarifying. The `ConfirmDialog` README also supports that interpretation when in Controlled mode (i.e. if `isOpen` is set):
> You'll want to update the state that controls isOpen by updating it from the onCancel and onConfirm callbacks.

So it really sounds like I should need to explicitly close the dialog on confirm, but I'm not actually needing to for some reason. @fullofcaffeine does this sound like a bug, or is this expected due to a nuance I'm not picking up on?

#### fullofcaffeine on 2022-01-13T18:12:42Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r784211612
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

> @fullofcaffeine does this sound like a bug, or is this expected due to a nuance I'm not picking up on?

As far as I can remember, it shouldn't close itself if `isOpen` is passed, so it does sound like a bug 🤔 I'll take note and have a look if no one beats me to it.

#### ciampo on 2022-01-21T09:21:18Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r789486345
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Could the reason be the same as explained in https://github.com/WordPress/gutenberg/pull/37535#discussion_r784291648 ?

In any case, I believe that we should still call `setShowConfirmDialog( false )` here

#### chad1008 on 2022-01-21T17:14:56Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r789852298
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Sounds good, I'll update the PR either later today or early on Monday!

#### chad1008 on 2022-01-21T19:04:37Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r789924046
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Done in f6fcb32. I didn't apply a `useCallback` because it triggered an error due to the conditional when placed after the early return declared by `if ( ! isPublished && ! isScheduled ) {...}`

#### ciampo on 2022-01-22T09:56:53Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r790125705
Path: packages/editor/src/components/post-switch-to-draft-button/index.js

```diff
@@ -18,37 +22,39 @@ function PostSwitchToDraftButton( {
 	onClick,
 } ) {
 	const isMobileViewport = useViewportMatch( 'small', '<' );
+	const [ showConfirmDialog, setShowConfirmDialog ] = useState( false );
 
 	if ( ! isPublished && ! isScheduled ) {
 		return null;
 	}
 
-	const onSwitch = () => {
-		let alertMessage;
-		if ( isPublished ) {
-			alertMessage = __(
-				'Are you sure you want to unpublish this post?'
-			);
-		} else if ( isScheduled ) {
-			alertMessage = __(
-				'Are you sure you want to unschedule this post?'
-			);
-		}
-		// eslint-disable-next-line no-alert
-		if ( window.confirm( alertMessage ) ) {
-			onClick();
-		}
-	};
+	let alertMessage;
+	if ( isPublished ) {
+		alertMessage = __( 'Are you sure you want to unpublish this post?' );
+	} else if ( isScheduled ) {
+		alertMessage = __( 'Are you sure you want to unschedule this post?' );
+	}
 
 	return (
-		<Button
-			className="editor-post-switch-to-draft"
-			onClick={ onSwitch }
-			disabled={ isSaving }
-			variant="tertiary"
-		>
-			{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
-		</Button>
+		<>
+			<Button
+				className="editor-post-switch-to-draft"
+				onClick={ () => {
+					setShowConfirmDialog( true );
+				} }
+				disabled={ isSaving }
+				variant="tertiary"
+			>
+				{ isMobileViewport ? __( 'Draft' ) : __( 'Switch to draft' ) }
+			</Button>
+			<ConfirmDialog
+				isOpen={ showConfirmDialog }
+				onConfirm={ onClick }
```

Thank you!

> I didn't apply a useCallback because it triggered an error due to the conditional when placed after the early return declared by if ( ! isPublished && ! isScheduled ) {...}

That's because hooks, including `useCallback`, can't run conditionally. We can leave the code as-is, but FYI an easy way to get around that issue would have been to declare the callback before that `if` statement :)

### Review thread 4 (resolved)

#### ciampo on 2022-02-10T10:44:44Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r803537079
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -0,0 +1,241 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	createNewPost,
+	openDocumentSettingsSidebar,
+	publishPost,
+	setBrowserViewport,
+	trashAllPosts,
+} from '@wordpress/e2e-test-utils';
+
+async function prepTestPost( postType, viewport ) {
+	// Create a post
+	await createNewPost( { postType } );
+
+	await page.type(
+		'.editor-post-title__input',
+		`Switch scheduled ${ postType } to draft`
+	);
+	await page.keyboard.press( 'Enter' );
+	await page.keyboard.type(
+		`This will be a scheduled ${ postType } edited in a ${ viewport } viewport`
+	);
+
+	// Unselect the blocks.
+	await page.evaluate( () => {
+		wp.data.dispatch( 'core/block-editor' ).clearSelectedBlock();
+	} );
+}
+
+async function publishTestPost( postType, viewport ) {
+	// Capitalize postType for use in snackbar XPath
+	const capitalizedPostType =
+		postType.charAt( 0 ).toUpperCase() + postType.slice( 1 );
+
+	// Create a post
+	await prepTestPost( postType, viewport );
+
+	// Publish the post
+	await publishPost();
+
+	await page.waitForXPath(
+		`//*[contains(@class, "components-snackbar")]/*[text()="${ capitalizedPostType } published."]`
```

Can we avoid using `components-snackbar` ? Maybe we could look at its `role` or `aria-label` ?

#### chad1008 on 2022-02-10T20:55:44Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r804097914
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -0,0 +1,241 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	createNewPost,
+	openDocumentSettingsSidebar,
+	publishPost,
+	setBrowserViewport,
+	trashAllPosts,
+} from '@wordpress/e2e-test-utils';
+
+async function prepTestPost( postType, viewport ) {
+	// Create a post
+	await createNewPost( { postType } );
+
+	await page.type(
+		'.editor-post-title__input',
+		`Switch scheduled ${ postType } to draft`
+	);
+	await page.keyboard.press( 'Enter' );
+	await page.keyboard.type(
+		`This will be a scheduled ${ postType } edited in a ${ viewport } viewport`
+	);
+
+	// Unselect the blocks.
+	await page.evaluate( () => {
+		wp.data.dispatch( 'core/block-editor' ).clearSelectedBlock();
+	} );
+}
+
+async function publishTestPost( postType, viewport ) {
+	// Capitalize postType for use in snackbar XPath
+	const capitalizedPostType =
+		postType.charAt( 0 ).toUpperCase() + postType.slice( 1 );
+
+	// Create a post
+	await prepTestPost( postType, viewport );
+
+	// Publish the post
+	await publishPost();
+
+	await page.waitForXPath(
+		`//*[contains(@class, "components-snackbar")]/*[text()="${ capitalizedPostType } published."]`
```

✅

### Review thread 5 (resolved)

#### ciampo on 2022-02-10T10:48:39Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r803540573
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -0,0 +1,241 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	createNewPost,
+	openDocumentSettingsSidebar,
+	publishPost,
+	setBrowserViewport,
+	trashAllPosts,
+} from '@wordpress/e2e-test-utils';
+
+async function prepTestPost( postType, viewport ) {
+	// Create a post
+	await createNewPost( { postType } );
+
+	await page.type(
+		'.editor-post-title__input',
+		`Switch scheduled ${ postType } to draft`
+	);
+	await page.keyboard.press( 'Enter' );
+	await page.keyboard.type(
+		`This will be a scheduled ${ postType } edited in a ${ viewport } viewport`
+	);
+
+	// Unselect the blocks.
+	await page.evaluate( () => {
+		wp.data.dispatch( 'core/block-editor' ).clearSelectedBlock();
+	} );
+}
+
+async function publishTestPost( postType, viewport ) {
+	// Capitalize postType for use in snackbar XPath
+	const capitalizedPostType =
+		postType.charAt( 0 ).toUpperCase() + postType.slice( 1 );
+
+	// Create a post
+	await prepTestPost( postType, viewport );
+
+	// Publish the post
+	await publishPost();
+
+	await page.waitForXPath(
+		`//*[contains(@class, "components-snackbar")]/*[text()="${ capitalizedPostType } published."]`
+	);
+
+	const closePublishingPanel = await page.waitForXPath(
+		'//button[@aria-label="Close panel"]'
+	);
+	await closePublishingPanel.click();
+}
+
+async function scheduleTestPost( postType, viewport ) {
+	// Create a post
+	await prepTestPost( postType, viewport );
+
+	if ( viewport === 'small' ) {
+		await openDocumentSettingsSidebar();
+	}
+	// Set a publish date for the next month.
+	await page.click( '.edit-post-post-schedule__toggle' );
+	await page.click(
+		'div[aria-label="Move forward to switch to the next month."]'
+	);
+
+	await (
+		await page.$x(
+			'//td[contains(concat(" ", @class, " "), " CalendarDay ")]/div[contains(concat(" ", @class, " "), " components-datetime__date__day ")][text() = "15"]'
```

Can we avoid using `components-datetime__date__day` and in general relying on the specific dom structure of the `Datetime` component?

#### chad1008 on 2022-02-10T20:56:08Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r804098254
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -0,0 +1,241 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	createNewPost,
+	openDocumentSettingsSidebar,
+	publishPost,
+	setBrowserViewport,
+	trashAllPosts,
+} from '@wordpress/e2e-test-utils';
+
+async function prepTestPost( postType, viewport ) {
+	// Create a post
+	await createNewPost( { postType } );
+
+	await page.type(
+		'.editor-post-title__input',
+		`Switch scheduled ${ postType } to draft`
+	);
+	await page.keyboard.press( 'Enter' );
+	await page.keyboard.type(
+		`This will be a scheduled ${ postType } edited in a ${ viewport } viewport`
+	);
+
+	// Unselect the blocks.
+	await page.evaluate( () => {
+		wp.data.dispatch( 'core/block-editor' ).clearSelectedBlock();
+	} );
+}
+
+async function publishTestPost( postType, viewport ) {
+	// Capitalize postType for use in snackbar XPath
+	const capitalizedPostType =
+		postType.charAt( 0 ).toUpperCase() + postType.slice( 1 );
+
+	// Create a post
+	await prepTestPost( postType, viewport );
+
+	// Publish the post
+	await publishPost();
+
+	await page.waitForXPath(
+		`//*[contains(@class, "components-snackbar")]/*[text()="${ capitalizedPostType } published."]`
+	);
+
+	const closePublishingPanel = await page.waitForXPath(
+		'//button[@aria-label="Close panel"]'
+	);
+	await closePublishingPanel.click();
+}
+
+async function scheduleTestPost( postType, viewport ) {
+	// Create a post
+	await prepTestPost( postType, viewport );
+
+	if ( viewport === 'small' ) {
+		await openDocumentSettingsSidebar();
+	}
+	// Set a publish date for the next month.
+	await page.click( '.edit-post-post-schedule__toggle' );
+	await page.click(
+		'div[aria-label="Move forward to switch to the next month."]'
+	);
+
+	await (
+		await page.$x(
+			'//td[contains(concat(" ", @class, " "), " CalendarDay ")]/div[contains(concat(" ", @class, " "), " components-datetime__date__day ")][text() = "15"]'
```

✅

### Review thread 6

#### ciampo on 2022-02-11T10:13:26Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r804513783
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -137,6 +135,11 @@ describe( 'Clicking "Switch to draft" on a published post/page', () => {
 					expect( postStatus ).toBe( 'publish' );
 				} );
 				it( `should revert a published ${ postType } to a draft if confirmed`, async () => {
+					// Capitalize postType for use in snackbar XPath
+					const capitalizedPostType =
```

Idea: is there a way in Gutenberg (eg. a utility function) to match text in a case-insensitive way?

In case there isn't, in order to make the test more resilient to capitalisation changes, we could instead:

- convert the expected text `toLowerCase()`
- convert the text to be found in the DOM to lower case too, [by using the `translate` function](https://stackoverflow.com/a/8474109)

I haven't tried this approach myself, but it could (should) work!

#### aaronrobertshaw on 2022-02-14T03:31:58Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r805482593
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -137,6 +135,11 @@ describe( 'Clicking "Switch to draft" on a published post/page', () => {
 					expect( postStatus ).toBe( 'publish' );
 				} );
 				it( `should revert a published ${ postType } to a draft if confirmed`, async () => {
+					// Capitalize postType for use in snackbar XPath
+					const capitalizedPostType =
```

We might also be able to use the [`lower-case()`](https://www.w3.org/TR/xpath-functions/#func-lower-case) or [`matches()`](https://www.w3.org/TR/xpath-functions/#func-matches) functions within the XPath selector.

#### chad1008 on 2022-02-14T19:49:07Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r806185817
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -137,6 +135,11 @@ describe( 'Clicking "Switch to draft" on a published post/page', () => {
 					expect( postStatus ).toBe( 'publish' );
 				} );
 				it( `should revert a published ${ postType } to a draft if confirmed`, async () => {
+					// Capitalize postType for use in snackbar XPath
+					const capitalizedPostType =
```

Those are both new to me, thanks for the links @aaronrobertshaw! I wasn't able to implement either, even when testing directly in Chrome's dev tools with strings I _know_ should match it didn't recognize the elements I was targeting. Not sure if it's user error on my part (likely) or something else, but for now I've gone with @ciampo's `toLowerCase()` suggestion.

I don't _love_ everything about this implementation... for one thing we're locating the snackbar notification by finding just the word "published" or "reverted" respectively, then pulling the text, lower casing it, and comparing it a full string to my current post type. Feels kind of like taking the long way around, and almost makes me wonder if I shouldn't just stop at identifying a snackbar notif with published/reverted inside it 🤔

#### ciampo on 2022-02-16T19:20:04Z

URL: https://github.com/WordPress/gutenberg/pull/37491#discussion_r808374442
Path: packages/e2e-tests/specs/editor/various/switch-to-draft.test.js

```diff
@@ -137,6 +135,11 @@ describe( 'Clicking "Switch to draft" on a published post/page', () => {
 					expect( postStatus ).toBe( 'publish' );
 				} );
 				it( `should revert a published ${ postType } to a draft if confirmed`, async () => {
+					// Capitalize postType for use in snackbar XPath
+					const capitalizedPostType =
```

>  Not sure if it's user error on my part (likely) or something else,

Maybe it's related to the version of XPath (it seems like those functions are available from version 2.0)?

> I don't love everything about this implementation... for one thing we're locating the snackbar notification by finding just the word "published" or "reverted" respectively, then pulling the text, lower casing it, and comparing it a full string to my current post type. Feels kind of like taking the long way around, and almost makes me wonder if I shouldn't just stop at identifying a snackbar notif with published/reverted inside it 🤔

It does seem convoluted, but I don't know if there's a better way to test for a confirmation that the post was published / reverted. Let's keep it as-is for now, and iterate on it in case we find a better solution!

## Changed files

- packages/e2e-tests/specs/editor/various/switch-to-draft.test.js (ADDED, +254/-0)
- packages/editor/src/components/post-switch-to-draft-button/index.js (MODIFIED, +35/-24)
