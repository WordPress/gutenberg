---
number: 75652
type: pull_request
state: MERGED
merged_at: 2026-03-09T20:24:23Z
url: https://github.com/WordPress/gutenberg/pull/75652
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Task","[Type] Enhancement","[Package] Components","[Package] Editor","[Feature] Real-time Collaboration","Backported to WP Core"]
---

# PR #75652: Editor: Polish real-time collaboration presence UI and move Avatar to editor package

- URL: https://github.com/WordPress/gutenberg/pull/75652
- Author: dabowman
- Created: 2026-02-18T01:09:26Z
- Updated: 2026-03-18T16:03:05Z
- Comments: 10 of 10
- Review threads: 33 of 33
- Changed files: 30 of 30
- Diff: ../pr-diffs/75652.diff

## Body

## What?

Polishes the real-time collaboration (RTC) presence UI — the collaborator button, list popover, and block highlight labels — and moves the `Avatar` and `AvatarGroup` components from `@wordpress/components` private APIs into the `@wordpress/editor` package where they are consumed.

## Why?

Follow-up to #75595. That PR introduced the collaborator overlay with cursor and block highlight rendering. This PR refines the visual design of the presence UI to match the Figma spec and addresses component ownership:

- **Avatar belongs in editor, not components** — The Avatar component is tightly coupled to the RTC collaboration feature and is not yet general-purpose. Keeping it in `@wordpress/components` as a private API adds unnecessary coupling and prevents the editor package from owning its own styling (e.g. `$components-color-accent` is restricted to the components package by stylelint).
- **Presence button needed polish** — Background color states (resting, hover, pressed) and max avatar count didn't match the design.
- **List popover didn't match spec** — Layout, typography, hover states, and header design were out of sync with the Figma design.
- **Block highlight labels were missing** — The collaborator block highlight outline needed an avatar badge label positioned above it to identify who selected the block.

## How?

### Avatar component changes (`badge` → `variant`, `status` → `dimmed`)
- Replaced `badge` boolean prop with `variant: 'badge'` enum (extensible for future variants)
- Replaced `status` string prop with `dimmed` boolean (simpler API for the single use case)
- Added contrast-aware badge text color using `colord` — automatically switches to dark text when `borderColor` is light
- Moved `background-color` from root to `.is-badge` so non-badge avatars don't show a colored background behind the white ring

### Move to editor package
- Moved `Avatar` and `AvatarGroup` from `packages/components/src/avatar{,-group}/` to `packages/editor/src/components/collaborators-presence/avatar{,-group}/`
- Removed from `@wordpress/components` private APIs (`private-apis.ts`) and `style.scss`
- Consumers import directly (`import Avatar from './avatar'`) instead of using `unlock()`
- Added `colord` to editor `package.json`
- Uses `var(--wp-admin-theme-color, #3858e9)` instead of `$components-color-accent` (which is restricted by stylelint outside the components package)
- Component types use `Props & Omit<React.HTMLAttributes<HTMLDivElement>, keyof Props>` instead of `WordPressComponentProps` (not publicly exported)

### Presence button polish
- Container handles background color (resting `$gray-100`, hover/pressed `$gray-200`); inner `Button` stays transparent in all states
- `AvatarGroup max={4}` to match design
- Overflow count has `margin-inline-end` for right padding

### Collaborators list popover
- Full-width list items with no border-radius, `$grid-unit-15 $grid-unit-20` padding
- Header: normal-case title + count, `closeSmall` icon at 24px
- Name: `$font-size-medium` / `$font-weight-medium`
- Hover: theme-tinted `rgba(#3858e9, 0.04)`

### Block highlight avatar labels
- Each highlighted block now shows an Avatar badge (`variant="badge"`, `size="small"`) at its top-left corner
- Positioned `8px` above the outline via `transform: translateY(calc(-100% - 8px))`
- Label expands on hover to show the collaborator's name
- `useBlockHighlighting` refactored to return `BlockHighlightData[]` (same pattern as `useRenderCursors`)

### Overlay inline styles
- Updated compiled Avatar CSS in `COLLABORATORS_OVERLAY_STYLES` to stay in sync with the SCSS source
- Dimmed/status-indicator styles intentionally omitted (not used in overlay)

## Testing Instructions

> **Note:** RTC collaboration features require a collaborative editing environment with multiple users connected to the same post via the sync provider.

1. Open a post in the editor with RTC/collaboration enabled
2. Have a second user connect to the same post
3. **Presence button**: Verify the collaborator avatars appear in the header toolbar, stacked with a `+N` overflow count when > 4 users. Check hover/pressed background states match surrounding toolbar items.
4. **Collaborators list**: Click the avatar group to open the popover. Verify:
   - Header shows "Collaborators" with count and close button
   - Each row shows avatar with colored border and user name
   - Rows highlight on hover with a subtle tint
5. **Cursor labels**: Move the second user's cursor within a block. Verify the pill-shaped avatar badge appears at the cursor position and expands on hover to show the name.
6. **Block highlight labels**: Have the second user select an entire block. Verify:
   - The block gets a colored outline
   - An avatar badge appears above the top-left corner of the outline
   - The badge expands on hover to show the collaborator's name
7. For users without a custom Gravatar, verify initials are shown instead of a placeholder image.

### Keyboard testing

1. Tab to the collaborator avatars button in the header toolbar
2. Press Enter/Space to open the popover
3. Verify focus moves into the popover
4. Press Escape to close — verify focus returns to the trigger button

## Screenshots or screencast

<!-- Screenshots showing the polished presence button, list popover, and block highlight labels -->

## Issue comments

### github-actions on 2026-02-18T01:09:34Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-3917910302

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @claude.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: claude.

Co-authored-by: dabowman <davidabowman@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: jameskoster <jameskoster@git.wordpress.org>
Co-authored-by: maxschmeling <maxschmeling@git.wordpress.org>
Co-authored-by: chriszarate <czarate@git.wordpress.org>
Co-authored-by: jasmussen <joen@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### jasmussen on 2026-02-18T13:01:43Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-3920714722

Thanks for the ping. In general I ihave some trust that this can move forward on a dev heavy side while it's still relatively exotic, but if you need any visual reviews, are you able to provide screenshots? It can be a bit tricky to review this particular feature.

### dabowman on 2026-02-18T16:21:33Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-3921776360

@jasmussen here's a screenshot of the current state of the UI with a bunch of collaborators in a session. You can also see the same colors being used between notes and the collab components. 

<img width="1725" height="1425" alt="Screenshot 2026-02-18 at 9 19 31 AM" src="https://github.com/user-attachments/assets/a1a5c40a-7c70-4fec-9880-bea67f6006d0" />

### dabowman on 2026-02-18T18:08:12Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-3922361040

@ciampo I tried to make that big css string a bit more friendly. I split it into two dedicated files. One for the avatar component and one for the rest of the overlay styling. I also added a file of variables that maps to the scss variables we'd like to reference but can't. That way the css is at least pulling what it can from a shared file that we can map to the system and try to keep in sync. The core issue is that we can't really add a lot of styles to the iframe without putting stuff in the block-editor package or adding some plumbing to allow us to enqueue styles from here to get compiled with the rest of the block-editor styles. It's a bit janky but hopefully this makes it a bit more sustainable until folks have time to build out a more permanent solution.

### jasmussen on 2026-02-19T08:23:52Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-3925523074

Looks good!

### Mamaduka on 2026-02-25T15:22:18Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-3960047877

Not sure if this was already reported, but new avatars doesn't seem to be work with Safari.

## Screeenshot
<img width="634" height="172" alt="CleanShot 2026-02-25 at 19 13 58" src="https://github.com/user-attachments/assets/e5227659-3909-4a75-8b45-18b1bef12f3e" />

### dabowman on 2026-03-09T18:04:43Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-4025742277

@chriszarate @maxschmeling I think this is ready to go. No merge conflicts. Tests are passing. I addressed all feedback from @ciampo and fixed that weird Safari rendering issue with the avatars. I think we're all set.

### ciampo on 2026-03-10T08:37:16Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-4029617998

@chriszarate @maxschmeling @dabowman just wanted to flag that this PR is merged, but not backported to wp/7.0. I don't know if you intented to backport it at all, but flagging just in case.

### chriszarate on 2026-03-10T15:52:12Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-4032532957

> @chriszarate @maxschmeling @dabowman just wanted to flag that this PR is merged, but not backported to wp/7.0. I don't know if you intented to backport it at all, but flagging just in case.

Thanks, backported

### github-actions on 2026-03-10T15:52:35Z

URL: https://github.com/WordPress/gutenberg/pull/75652#issuecomment-4032536770

There was a conflict while trying to cherry-pick the commit to the wp/7.0 branch. Please resolve the conflict manually and create a PR to the wp/7.0 branch.

  PRs to wp/7.0 are similar to PRs to trunk, but you should base your PR on the wp/7.0 branch instead of trunk.

  ```
  # Checkout the wp/7.0 branch instead of trunk.
  git checkout wp/7.0

  # Create a new branch for your PR.
  git checkout -b my-branch

  # Cherry-pick the commit.
  git cherry-pick 2bb53b10ce353e4785574f8267076639cadff48b

  # Check which files have conflicts.
  git status

  # Resolve the conflict...
  # Add the resolved files to the staging area.
  git status
  git add .
  git cherry-pick --continue

  # Push the branch to the repository
  git push origin my-branch

  # Create a PR and set the base to the wp/7.0 branch.
  # See https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/changing-the-base-branch-of-a-pull-request.
  ```

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2026-02-18T11:21:53Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821786890
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -37,7 +44,17 @@ function Avatar( {
 		...style,
 		...( src ? { '--components-avatar-url': `url(${ src })` } : {} ),
 		...( borderColor
-			? { '--components-avatar-outline-color': borderColor }
+			? {
+					'--components-avatar-outline-color': borderColor,
```

Let's not use `--components-` as a prefix for CSS variables, as it may wrongly suggest that this is a component from `@wordpress/components` package. 

We should follow whatever pattern other CSS variables follow based on the @wordpress package name. The namespacing will also help prenvent naming conflicts

#### dabowman on 2026-02-18T16:12:47Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823182686
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -37,7 +44,17 @@ function Avatar( {
 		...style,
 		...( src ? { '--components-avatar-url': `url(${ src })` } : {} ),
 		...( borderColor
-			? { '--components-avatar-outline-color': borderColor }
+			? {
+					'--components-avatar-outline-color': borderColor,
```

yeah, that's an easy fix. I was on the fence about whether to switch it over to --editor or whether to leave it --components for the moment.

#### dabowman on 2026-02-18T17:02:58Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823436383
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -37,7 +44,17 @@ function Avatar( {
 		...style,
 		...( src ? { '--components-avatar-url': `url(${ src })` } : {} ),
 		...( borderColor
-			? { '--components-avatar-outline-color': borderColor }
+			? {
+					'--components-avatar-outline-color': borderColor,
```

latest commit should resolve this

### Review thread 2 (resolved)

#### ciampo on 2026-02-18T11:23:21Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821793347
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -37,7 +44,17 @@ function Avatar( {
 		...style,
 		...( src ? { '--components-avatar-url': `url(${ src })` } : {} ),
 		...( borderColor
-			? { '--components-avatar-outline-color': borderColor }
+			? {
+					'--components-avatar-outline-color': borderColor,
+					'--components-avatar-name-color': colord(
+						borderColor
+					).isReadable( '#000', {
+						level: 'AA',
+						size: 'normal',
+					} )
+						? '#000'
+						: '#fff',
```

Not sure about using hardcoded colors here, we should try to use existing variables for text color (for example, I believe we don't use `#000` but `#1e1e1e` through the `$gray-100` SCSS variable).

cc @WordPress/gutenberg-design

#### dabowman on 2026-02-18T16:16:10Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823200426
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -37,7 +44,17 @@ function Avatar( {
 		...style,
 		...( src ? { '--components-avatar-url': `url(${ src })` } : {} ),
 		...( borderColor
-			? { '--components-avatar-outline-color': borderColor }
+			? {
+					'--components-avatar-outline-color': borderColor,
+					'--components-avatar-name-color': colord(
+						borderColor
+					).isReadable( '#000', {
+						level: 'AA',
+						size: 'normal',
+					} )
+						? '#000'
+						: '#fff',
```

I'll take a closer look. I don't know why I didn't use them here ... I might have just been thinking about maximizing the contrast but grays should be fine.

### Review thread 3 (resolved)

#### ciampo on 2026-02-18T11:24:38Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821799082
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -1,16 +1,13 @@
 // @ts-expect-error No exported types
 import { useStyleOverride } from '@wordpress/block-editor';
-import { privateApis as componentsPrivateApis } from '@wordpress/components';
 import { useResizeObserver, useMergeRefs } from '@wordpress/compose';
-import { useEffect, useState } from '@wordpress/element';
+import { useEffect, useMemo, useState } from '@wordpress/element';
 
-import { unlock } from '../../lock-unlock';
+import Avatar from '../collaborators-presence/avatar';
 import { useBlockHighlighting } from './use-block-highlighting';
 import { useRenderCursors } from './use-render-cursors';
 import { ELEVATION_X_SMALL } from './collaborator-styles';
 
-const { Avatar } = unlock( componentsPrivateApis );
-
 // wp-components styles are excluded from the editor canvas iframe, so the
```

Let's remove any mention of the components, package, since these components are being removed from that package anyway.

#### dabowman on 2026-02-18T17:02:44Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823435126
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -1,16 +1,13 @@
 // @ts-expect-error No exported types
 import { useStyleOverride } from '@wordpress/block-editor';
-import { privateApis as componentsPrivateApis } from '@wordpress/components';
 import { useResizeObserver, useMergeRefs } from '@wordpress/compose';
-import { useEffect, useState } from '@wordpress/element';
+import { useEffect, useMemo, useState } from '@wordpress/element';
 
-import { unlock } from '../../lock-unlock';
+import Avatar from '../collaborators-presence/avatar';
 import { useBlockHighlighting } from './use-block-highlighting';
 import { useRenderCursors } from './use-render-cursors';
 import { ELEVATION_X_SMALL } from './collaborator-styles';
 
-const { Avatar } = unlock( componentsPrivateApis );
-
 // wp-components styles are excluded from the editor canvas iframe, so the
```

latest commit should resolve this

### Review thread 4 (resolved)

#### ciampo on 2026-02-18T11:26:15Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821806417
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -1,16 +1,13 @@
 // @ts-expect-error No exported types
 import { useStyleOverride } from '@wordpress/block-editor';
-import { privateApis as componentsPrivateApis } from '@wordpress/components';
 import { useResizeObserver, useMergeRefs } from '@wordpress/compose';
-import { useEffect, useState } from '@wordpress/element';
+import { useEffect, useMemo, useState } from '@wordpress/element';
 
-import { unlock } from '../../lock-unlock';
+import Avatar from '../collaborators-presence/avatar';
 import { useBlockHighlighting } from './use-block-highlighting';
 import { useRenderCursors } from './use-render-cursors';
 import { ELEVATION_X_SMALL } from './collaborator-styles';
 
-const { Avatar } = unlock( componentsPrivateApis );
-
 // wp-components styles are excluded from the editor canvas iframe, so the
 // Avatar component's SCSS is not available there. We inject compiled versions
 // of the relevant rules alongside the overlay-specific positioning styles.
```

Keeping a hardcoded copy of the styles feels like a sub-optimal solution, so I would look for better alternatives.

Regardless, is it actually needed now that the avatar components are defined locally in the same package?

#### dabowman on 2026-02-18T16:09:45Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823167037
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -1,16 +1,13 @@
 // @ts-expect-error No exported types
 import { useStyleOverride } from '@wordpress/block-editor';
-import { privateApis as componentsPrivateApis } from '@wordpress/components';
 import { useResizeObserver, useMergeRefs } from '@wordpress/compose';
-import { useEffect, useState } from '@wordpress/element';
+import { useEffect, useMemo, useState } from '@wordpress/element';
 
-import { unlock } from '../../lock-unlock';
+import Avatar from '../collaborators-presence/avatar';
 import { useBlockHighlighting } from './use-block-highlighting';
 import { useRenderCursors } from './use-render-cursors';
 import { ELEVATION_X_SMALL } from './collaborator-styles';
 
-const { Avatar } = unlock( componentsPrivateApis );
-
 // wp-components styles are excluded from the editor canvas iframe, so the
 // Avatar component's SCSS is not available there. We inject compiled versions
 // of the relevant rules alongside the overlay-specific positioning styles.
```

Yeah, the hardcoded styles are a bit janky but as far as I know it's required because of how the styles get injected into the iframe for the overlay UI. Correct me if I'm wrong there @chriszarate

### Review thread 5 (resolved)

#### ciampo on 2026-02-18T11:27:08Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821810025
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -48,14 +45,16 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	animation: collaborators-overlay-cursor-blink 1s infinite;
 }
 
-/* ── Avatar component (compiled from packages/components/src/avatar/styles.scss) ── */
+/* ── Avatar component (compiled from collaborators-presence/avatar/styles.scss) ──
+   Dimmed and status-indicator styles are intentionally omitted — not used in
+   the overlay. Keep in sync when editing the SCSS source. ── */
 .components-avatar {
```

Same comment re. prefixes for CSS variables. Let's not use `.components-*` for styles that are not defined in the `@wordpress/components` package.

#### dabowman on 2026-02-18T17:02:36Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823434372
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -48,14 +45,16 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	animation: collaborators-overlay-cursor-blink 1s infinite;
 }
 
-/* ── Avatar component (compiled from packages/components/src/avatar/styles.scss) ── */
+/* ── Avatar component (compiled from collaborators-presence/avatar/styles.scss) ──
+   Dimmed and status-indicator styles are intentionally omitted — not used in
+   the overlay. Keep in sync when editing the SCSS source. ── */
 .components-avatar {
```

latest commit should resolve this

### Review thread 6 (resolved)

#### ciampo on 2026-02-18T11:28:29Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821815831
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -48,14 +45,16 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	animation: collaborators-overlay-cursor-blink 1s infinite;
 }
 
-/* ── Avatar component (compiled from packages/components/src/avatar/styles.scss) ── */
+/* ── Avatar component (compiled from collaborators-presence/avatar/styles.scss) ──
```

Can you explain a bit more why need to duplicate styles and keep them in sync here? This feels like a fragile solution that will easily go out of sync over future iterations

#### dabowman on 2026-02-18T19:16:24Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2824016292
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -48,14 +45,16 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	animation: collaborators-overlay-cursor-blink 1s infinite;
 }
 
-/* ── Avatar component (compiled from packages/components/src/avatar/styles.scss) ── */
+/* ── Avatar component (compiled from collaborators-presence/avatar/styles.scss) ──
```

latest commit tries to improve this

### Review thread 7 (resolved)

#### ciampo on 2026-02-18T11:29:39Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821820667
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -100,36 +99,38 @@ const COLLABORATORS_OVERLAY_STYLES = `
 }
 .components-avatar__name {
 	font-size: 13px;
+	font-weight: 499;
 	line-height: 20px;
```

Let's use shared variables where possible. For example, `$font-weight-medium`, `$font-line-height-small`, `$font-size-medium`, etc. Here and throughout the rest of the styles

#### dabowman on 2026-02-18T16:11:17Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823174954
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -100,36 +99,38 @@ const COLLABORATORS_OVERLAY_STYLES = `
 }
 .components-avatar__name {
 	font-size: 13px;
+	font-weight: 499;
 	line-height: 20px;
```

yeah, this has to do with the way the styles get injected. It all needs to get resolved and hardcoded, at least as I understand it. It's not an awesome solution and I think folks are working on something more sustainable for the overlay in future releases.

#### dabowman on 2026-02-18T19:16:38Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2824017270
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -100,36 +99,38 @@ const COLLABORATORS_OVERLAY_STYLES = `
 }
 .components-avatar__name {
 	font-size: 13px;
+	font-weight: 499;
 	line-height: 20px;
```

latest commit tries to improve this

### Review thread 8 (resolved)

#### ciampo on 2026-02-18T11:35:21Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2821843868
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -143,6 +144,14 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	overflow: visible;
 	width: max-content;
 }
+/* Avatar label positioned above a highlighted block. */
+.collaborators-overlay-block-label.components-avatar {
```

This may be less of a concern with the Avatar components as a local editor package component, but in general I'd avoid mixing the classname used internally by the component (in this case, `.components-avatar`) with a class used by its consumer (`.collaborators-overlay-block-label`), as it creates a coupling and overrides/specificity that are going to be hard to de-couple if/when moving the component to be a DS component.

If the component needs to support a specific usecase (like, for example, a different label positioning), that should be supported via the component's official APIs (for example, a dedicated prop).

#### dabowman on 2026-02-18T16:18:20Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823212004
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -143,6 +144,14 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	overflow: visible;
 	width: max-content;
 }
+/* Avatar label positioned above a highlighted block. */
+.collaborators-overlay-block-label.components-avatar {
```

yeah, that makes sense. I'm pretty sure this is local to the overlay component, which is separate from the avatar. But I'll make sure stuff isn't getting mixed there. I think the comment there is saying it's positioning the entire avatar, which is acting as a label for the highlight. It's not re-positioning part of the component.

#### dabowman on 2026-02-18T17:05:58Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2823451440
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -143,6 +144,14 @@ const COLLABORATORS_OVERLAY_STYLES = `
 	overflow: visible;
 	width: max-content;
 }
+/* Avatar label positioned above a highlighted block. */
+.collaborators-overlay-block-label.components-avatar {
```

Yeah, confirmed. This is just positioning the entire component in the overlay. It's specific to the overlay and doesn't leak into the component.

### Review thread 9 (resolved)

#### ciampo on 2026-02-19T09:48:47Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826668417
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -237,23 +45,37 @@ export function Overlay( {
 		postType ?? null
 	);
 
+	const { highlights, rerenderHighlightsAfterDelay } = useBlockHighlighting(
+		overlayElement,
+		blockEditorDocument ?? null,
+		postId ?? null,
+		postType ?? null
+	);
+
+	// Combine both delayed rerenders so layout changes recompute everything.
+	const rerenderAfterDelay = useMemo(
+		() => () => {
+			const cleanupCursors = rerenderCursorsAfterDelay();
+			const cleanupHighlights = rerenderHighlightsAfterDelay();
+			return () => {
+				cleanupCursors();
+				cleanupHighlights();
+			};
+		},
+		[ rerenderCursorsAfterDelay, rerenderHighlightsAfterDelay ]
+	);
```

The usage of `useMemo` here is a bit weird. `useMemo` is usually for computed values, and not for callback functions.

We should use `useCallback` in these situations.

### Review thread 10 (resolved)

#### ciampo on 2026-02-19T09:50:34Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826680029
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -103,16 +128,42 @@ export function useBlockHighlighting(
 				return;
 			}
 
-			if ( blockElement ) {
-				blockElement.classList.add( 'is-collaborator-selected' );
-				blockElement.style.setProperty(
-					'--collaborator-outline-color',
-					color
-				);
-				highlightedBlockIds.current.add( blockId );
+			blockElement.classList.add( 'is-collaborator-selected' );
+			blockElement.style.setProperty(
+				'--collaborator-outline-color',
+				color
+			);
+			highlightedBlockIds.current.add( blockId );
+
+			if ( overlayElement ) {
+				const blockRect = blockElement.getBoundingClientRect();
+				const overlayRect = overlayElement.getBoundingClientRect();
+
+				results.push( {
+					blockId,
+					userName,
+					avatarUrl,
+					color,
+					x: blockRect.left - overlayRect.left,
+					y: blockRect.top - overlayRect.top,
+				} );
 			}
 		} );
-	}, [ userStates, blockEditorDocument ] );
+
+		setHighlights( results );
+	}, [ userStates, blockEditorDocument, overlayElement ] );
+
+	useEffect( computeHighlights, [ computeHighlights ] );
+
+	const rerenderHighlightsAfterDelay = useMemo(
+		() => () => {
+			const timeout = setTimeout( computeHighlights, 500 );
+			return () => clearTimeout( timeout );
+		},
+		[ computeHighlights ]
+	);
```

Same here re. preferring `useCallback` over `useMemo` in this scenario.

### Review thread 11 (resolved)

#### copilot-pull-request-reviewer on 2026-02-19T09:53:23Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826696521
Path: packages/editor/src/components/collab-sidebar/utils.js

```diff
@@ -23,14 +23,13 @@ export function noop() {}
  * @see https://www.figma.com/design/HOJTpCFfa3tR0EccUlu0CM/WordPress.org-Design-Library?node-id=1-2193&t=M6WdRvTpt0mh8n6T-1
  */
 const AVATAR_BORDER_COLORS = [
-	'#3858E9', // Blueberry
-	'#9fB1FF', // Blueberry 2
-	'#1D35B4', // Dark Blueberry
-	'#1A1919', // Charcoal 0
-	'#E26F56', // Pomegranate
-	'#33F078', // Acid Green
-	'#FFF972', // Lemon
-	'#7A00DF', // Purple
+	'#C36EFF',
+	'#FF51A8',
+	'#E4780A',
+	'#FF35EE',
+	'#879F11',
+	'#46A494',
+	'#00A2C3',
```

`AVATAR_BORDER_COLORS` now has 7 entries. If the intent is to keep an 8-color deterministic palette (as described in related RTC work), this reduces variety and increases color collisions. Confirm the intended palette size and add the missing color (or update the documentation/spec reference accordingly).
```suggestion
	'#00A2C3',
	'#2F6FE4',
```

#### dabowman on 2026-02-20T18:14:36Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2834500004
Path: packages/editor/src/components/collab-sidebar/utils.js

```diff
@@ -23,14 +23,13 @@ export function noop() {}
  * @see https://www.figma.com/design/HOJTpCFfa3tR0EccUlu0CM/WordPress.org-Design-Library?node-id=1-2193&t=M6WdRvTpt0mh8n6T-1
  */
 const AVATAR_BORDER_COLORS = [
-	'#3858E9', // Blueberry
-	'#9fB1FF', // Blueberry 2
-	'#1D35B4', // Dark Blueberry
-	'#1A1919', // Charcoal 0
-	'#E26F56', // Pomegranate
-	'#33F078', // Acid Green
-	'#FFF972', // Lemon
-	'#7A00DF', // Purple
+	'#C36EFF',
+	'#FF51A8',
+	'#E4780A',
+	'#FF35EE',
+	'#879F11',
+	'#46A494',
+	'#00A2C3',
```

7 is the most colors we can fit in here while still maintaining reasonable distinguishability, which I defined as a minimum DeltaE of 10. We avoid collisions by using 1 fewer colors here.

### Review thread 12 (resolved)

#### copilot-pull-request-reviewer on 2026-02-19T09:53:24Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826696589
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,94 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
+	const customProperties = {
+		...style,
+		...( src ? { '--editor-avatar-url': `url(${ src })` } : {} ),
+		...( borderColor
+			? {
+					'--editor-avatar-outline-color': borderColor,
+					'--editor-avatar-name-color': colord(
+						borderColor
+					).isReadable( '#1e1e1e', {
+						level: 'AA',
+						size: 'normal',
+					} )
+						? '#1e1e1e'
+						: '#fff',
+			  }
+			: {} ),
+	} as React.CSSProperties;
+
+	const avatar = (
+		<div
+			className={ clsx( 'editor-avatar', className, {
+				'has-avatar-border-color': !! borderColor,
+				'has-src': !! src,
+				'is-badge': showBadge,
+				'is-small': size === 'small',
+				'is-dimmed': dimmed,
+			} ) }
+			style={ customProperties }
+			role="img"
+			aria-label={ name }
```

`role="img"` is always set, but `aria-label` is derived only from `name` (which is optional in `AvatarProps`). If `name` is ever omitted, this creates an unlabeled image role. Consider making `name` required, or fall back to `label` for `aria-label`, or only set `role="img"` when an accessible name is available.
```suggestion
			role={ name || label ? 'img' : undefined }
			aria-label={ name || label }
```

### Review thread 13 (resolved)

#### copilot-pull-request-reviewer on 2026-02-19T09:53:24Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826696630
Path: packages/editor/src/components/collaborators-presence/list.tsx

```diff
@@ -64,11 +58,6 @@ export function CollaboratorsList( {
 							key={ collaboratorState.clientId }
 							className="editor-collaborators-presence__list-item"
 							disabled
```

`disabled` is hard-coded on each collaborator row button, which prevents keyboard focus and also disables the `:hover:not(:disabled)` / `:active:not(:disabled)` highlight styles in the accompanying SCSS. If these rows are meant to show hover/focus styling per the design, remove the `disabled` attribute (or make it conditional) and rely on `aria-disabled` / no-op handlers instead.
```suggestion
							aria-disabled="true"
							onClick={ ( event ) => {
								event.preventDefault();
							} }
```

#### dabowman on 2026-02-25T19:36:15Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2855008596
Path: packages/editor/src/components/collaborators-presence/list.tsx

```diff
@@ -64,11 +58,6 @@ export function CollaboratorsList( {
 							key={ collaboratorState.clientId }
 							className="editor-collaborators-presence__list-item"
 							disabled
```

The un-used states in the CSS is anticipating its eventual move to components. Not currently used but it will be used here in the future and elsewhere.

### Review thread 14 (resolved)

#### ciampo on 2026-02-19T09:54:41Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826704037
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -103,16 +128,42 @@ export function useBlockHighlighting(
 				return;
 			}
 
-			if ( blockElement ) {
-				blockElement.classList.add( 'is-collaborator-selected' );
-				blockElement.style.setProperty(
-					'--collaborator-outline-color',
-					color
-				);
-				highlightedBlockIds.current.add( blockId );
+			blockElement.classList.add( 'is-collaborator-selected' );
+			blockElement.style.setProperty(
+				'--collaborator-outline-color',
+				color
+			);
+			highlightedBlockIds.current.add( blockId );
+
+			if ( overlayElement ) {
+				const blockRect = blockElement.getBoundingClientRect();
+				const overlayRect = overlayElement.getBoundingClientRect();
+
+				results.push( {
+					blockId,
+					userName,
+					avatarUrl,
+					color,
+					x: blockRect.left - overlayRect.left,
+					y: blockRect.top - overlayRect.top,
+				} );
 			}
 		} );
-	}, [ userStates, blockEditorDocument ] );
+
+		setHighlights( results );
+	}, [ userStates, blockEditorDocument, overlayElement ] );
+
+	useEffect( computeHighlights, [ computeHighlights ] );
```

The original `useEffect` already lacked a cleanup function (i.e., it never un-highlights blocks when the component unmounts), but we should address it.

When the overlay unmounts, any `is-collaborator-selected` classes and `--collaborator-outline-color` custom properties will remain on block elements. 

We should add a cleanup effect, for example

```ts
useEffect( () => {
    computeHighlights();
    return () => {
        // Clear all highlights on unmount
        for ( const blockId of highlightedBlockIds.current ) {
            const el = getBlockElementById( blockEditorDocument, blockId );
            if ( el ) {
                el.classList.remove( 'is-collaborator-selected' );
                el.style.removeProperty( '--collaborator-outline-color' );
            }
        }
        highlightedBlockIds.current.clear();
    };
}, [ computeHighlights ] );
```

### Review thread 15 (resolved)

#### ciampo on 2026-02-19T10:01:19Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826747575
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -4,44 +4,100 @@
 import {
 	privateApis as coreDataPrivateApis,
 	SelectionType,
+	type PostEditorAwarenessState,
 	type SelectionWholeBlock,
 } from '@wordpress/core-data';
-import { useEffect, useRef } from '@wordpress/element';
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
 import { unlock } from '../../lock-unlock';
 import { getAvatarBorderColor } from '../collab-sidebar/utils';
+import { getAvatarUrl } from './get-avatar-url';
 
 const { useActiveCollaborators } = unlock( coreDataPrivateApis );
 
+export interface BlockHighlightData {
+	blockId: string;
+	userName: string;
+	avatarUrl?: string;
+	color: string;
+	x: number;
+	y: number;
+}
+
 /**
- * Custom hook for highlighting selected blocks in the editor
- * @param blockEditorDocument - Ref to the block editor document, used to directly style block elements.
- * @param postId              - The ID of the post
- * @param postType            - The type of the post
+ * Custom hook for highlighting selected blocks in the editor and computing
+ * their positions for rendering avatar labels in the overlay.
+ *
+ * @param overlayElement      - The overlay element used as position reference.
+ * @param blockEditorDocument - Ref to the block editor document.
+ * @param postId              - The ID of the post.
+ * @param postType            - The type of the post.
+ * @return Highlight data for rendering and a delayed recompute function.
  */
 export function useBlockHighlighting(
+	overlayElement: HTMLElement | null,
 	blockEditorDocument: Document | null,
 	postId: number | null,
 	postType: string | null
-) {
+): {
+	highlights: BlockHighlightData[];
+	rerenderHighlightsAfterDelay: () => () => void;
+} {
 	const highlightedBlockIds = useRef< Set< string > >( new Set() );
-	const userStates = useActiveCollaborators(
+	const userStates: PostEditorAwarenessState[] = useActiveCollaborators(
 		postId ?? null,
 		postType ?? null
 	);
 
-	// Draw block highlights
-	useEffect( () => {
-		// Don't do anything if editor is not rendered yet.
-		if ( blockEditorDocument === null ) {
+	const [ highlights, setHighlights ] = useState< BlockHighlightData[] >(
+		[]
+	);
+
+	const computeHighlights = useCallback( () => {
```

The `computeHighlights` function mixes imperative DOM manipulation with React rendering concerns in one callback:
- it both mutates the DOM (adding/removing CSS classes and style properties on block elements)
- it computes position data for React state.

I understand this is a pre-existing architectural choice (carried over from the original `useEffect`), but this PR refactors is into `useCallback` + separate `useEffect` (plus a `setTimeout` call), making the side effects less obvious since `useCallback` doesn't conventionally carry side effects.

Usually React side-effects belong in `useEffect`, and using `setTimeout` breaks this pattern a bit.

One possible solution is to switch to an "invalidation token" pattern: instead of calling the side-effectful function directly from the timeout, the timeout just bumps a piece of state, which causes the useEffect to re-run. That way, all DOM mutations stay inside useEffect.

<details>

<summary>Suggested changes (at a high-level)</summary>

```tsx
export function useBlockHighlighting(
	overlayElement: HTMLElement | null,
	blockEditorDocument: Document | null,
	postId: number | null,
	postType: string | null
): {
	highlights: BlockHighlightData[];
	rerenderHighlightsAfterDelay: () => () => void;
} {
	const highlightedBlockIds = useRef< Set< string > >( new Set() );
	const userStates: PostEditorAwarenessState[] = useActiveCollaborators(
		postId ?? null,
		postType ?? null
	);

	const [ highlights, setHighlights ] = useState< BlockHighlightData[] >( [] );

	// Bump this to force the effect to re-run (e.g. after a layout shift).
	const [ recomputeToken, setRecomputeToken ] = useState( 0 );

	// All DOM mutations and position computations live inside useEffect.
	useEffect( () => {
		if ( ! blockEditorDocument ) {
			setHighlights( [] );
			return;
		}

		const blocksToHighlight = userStates
			.filter( /* ... */ )
			.map( /* ... */ );

		// Unhighlight stale blocks (DOM mutation)
		const selectedBlockIds = new Set( blocksToHighlight.map( b => b.blockId ) );
		for ( const blockId of highlightedBlockIds.current ) {
			if ( ! selectedBlockIds.has( blockId ) ) {
				const el = getBlockElementById( blockEditorDocument, blockId );
				if ( el ) {
					el.classList.remove( 'is-collaborator-selected' );
					el.style.removeProperty( '--collaborator-outline-color' );
				}
				highlightedBlockIds.current.delete( blockId );
			}
		}

		// Highlight current blocks and compute positions (DOM mutation + read)
		const results: BlockHighlightData[] = [];
		blocksToHighlight.forEach( ( block ) => {
			// ... classList.add, setProperty, getBoundingClientRect ...
			// ... results.push( ... ) ...
		} );

		setHighlights( results );
	}, [ userStates, blockEditorDocument, overlayElement, recomputeToken ] );

	// The delayed rerender just bumps state — no direct DOM mutation.
	const rerenderHighlightsAfterDelay = useCallback( () => {
		const timeout = setTimeout( () => {
			setRecomputeToken( ( t ) => t + 1 );
		}, 500 );
		return () => clearTimeout( timeout );
	}, [] );

	return { highlights, rerenderHighlightsAfterDelay };
}

```

</details>




With the suggested approach:
 
- DOM mutations stay in `useEffect` (and not in `useCallback`)
- `rerenderHighlightsAfterDelay` is pure (it just schedules a state update, instead of mutating the DOM outside of React's lifecycle)
- `rerenderHighlightsAfterDelay` becomes a stable reference across re-renders
- `setRecomputeToken` is a state setter (stable by guarantee). This means the parent's combined `rerenderAfterDelay` in `overlay.tsx` will also be more stable.
- Cleanup becomes natural -- we can add a cleanup return to the `useEffect` to un-highlight everything on unmount (related to my previous review comment).

### Review thread 16 (resolved)

#### ciampo on 2026-02-19T10:07:00Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826783811
Path: packages/editor/src/components/collaborators-presence/styles/collaborators-list.scss

```diff
@@ -61,24 +60,23 @@
 		// Layout styles
 		display: flex;
 		align-items: center;
-		padding: $grid-unit-05 + 2;
+		padding: $grid-unit-15 $grid-unit-20;
 		gap: $grid-unit-10;
-		border-radius: $radius-large + $grid-unit-05;
 		width: 100%;
 		cursor: pointer;
 		transition: background-color 0.2s ease;
 
 		&:hover:not(:disabled) {
-			background-color: rgba($black, 0.05);
+			background-color: rgba(#3858e9, 0.04);
 		}
 
 		&:active:not(:disabled) {
-			background-color: rgba($black, 0.08);
+			background-color: rgba(#3858e9, 0.04);
```

Let's not use hardcoded colors, especially for WP blueberry. We should use `var(--wp-admin-theme-color, #3858e9)`.

Also:
- instead of using opacity, we could use other variables such as `--wp-admin-theme-color-darker-10` or  `--wp-admin-theme-color-darker-20`
- `hover` and `active` are currently using the same value, should `active` be a bit darker?

(cc @WordPress/gutenberg-design )

#### jameskoster on 2026-02-19T13:57:22Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2828050083
Path: packages/editor/src/components/collaborators-presence/styles/collaborators-list.scss

```diff
@@ -61,24 +60,23 @@
 		// Layout styles
 		display: flex;
 		align-items: center;
-		padding: $grid-unit-05 + 2;
+		padding: $grid-unit-15 $grid-unit-20;
 		gap: $grid-unit-10;
-		border-radius: $radius-large + $grid-unit-05;
 		width: 100%;
 		cursor: pointer;
 		transition: background-color 0.2s ease;
 
 		&:hover:not(:disabled) {
-			background-color: rgba($black, 0.05);
+			background-color: rgba(#3858e9, 0.04);
 		}
 
 		&:active:not(:disabled) {
-			background-color: rgba($black, 0.08);
+			background-color: rgba(#3858e9, 0.04);
```

I’d generally prefer distinct styles for `hover` and `active` states, as they provide helpful visual feedback. There are surprisingly few examples of this across the codebase though. I'm not sure why, or if that should inform how we act here.

#### dabowman on 2026-02-20T18:17:11Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2834509850
Path: packages/editor/src/components/collaborators-presence/styles/collaborators-list.scss

```diff
@@ -61,24 +60,23 @@
 		// Layout styles
 		display: flex;
 		align-items: center;
-		padding: $grid-unit-05 + 2;
+		padding: $grid-unit-15 $grid-unit-20;
 		gap: $grid-unit-10;
-		border-radius: $radius-large + $grid-unit-05;
 		width: 100%;
 		cursor: pointer;
 		transition: background-color 0.2s ease;
 
 		&:hover:not(:disabled) {
-			background-color: rgba($black, 0.05);
+			background-color: rgba(#3858e9, 0.04);
 		}
 
 		&:active:not(:disabled) {
-			background-color: rgba($black, 0.08);
+			background-color: rgba(#3858e9, 0.04);
```

Good catch on the hardcoded blueberry. Not sure how that snuck in.

### Review thread 17 (resolved)

#### ciampo on 2026-02-19T10:08:33Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826793740
Path: packages/editor/src/components/collab-sidebar/utils.js

```diff
@@ -23,14 +23,13 @@ export function noop() {}
  * @see https://www.figma.com/design/HOJTpCFfa3tR0EccUlu0CM/WordPress.org-Design-Library?node-id=1-2193&t=M6WdRvTpt0mh8n6T-1
  */
 const AVATAR_BORDER_COLORS = [
-	'#3858E9', // Blueberry
-	'#9fB1FF', // Blueberry 2
-	'#1D35B4', // Dark Blueberry
-	'#1A1919', // Charcoal 0
-	'#E26F56', // Pomegranate
-	'#33F078', // Acid Green
-	'#FFF972', // Lemon
-	'#7A00DF', // Purple
+	'#C36EFF',
+	'#FF51A8',
+	'#E4780A',
+	'#FF35EE',
+	'#879F11',
+	'#46A494',
+	'#00A2C3',
```

I still think it would be useful to leave a comment next to each color to indicate what color it is.

### Review thread 18 (resolved)

#### ciampo on 2026-02-19T10:09:00Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826796566
Path: packages/editor/src/components/collab-sidebar/utils.js

```diff
@@ -23,14 +23,13 @@ export function noop() {}
  * @see https://www.figma.com/design/HOJTpCFfa3tR0EccUlu0CM/WordPress.org-Design-Library?node-id=1-2193&t=M6WdRvTpt0mh8n6T-1
```

With the colors being updated, is this Figma reference still up-to-date / valid?

#### dabowman on 2026-02-20T18:20:58Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2834524573
Path: packages/editor/src/components/collab-sidebar/utils.js

```diff
@@ -23,14 +23,13 @@ export function noop() {}
  * @see https://www.figma.com/design/HOJTpCFfa3tR0EccUlu0CM/WordPress.org-Design-Library?node-id=1-2193&t=M6WdRvTpt0mh8n6T-1
```

I can update it.

### Review thread 19 (resolved)

#### ciampo on 2026-02-19T10:10:31Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826804989
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,94 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
+	const customProperties = {
+		...style,
+		...( src ? { '--editor-avatar-url': `url(${ src })` } : {} ),
+		...( borderColor
+			? {
+					'--editor-avatar-outline-color': borderColor,
+					'--editor-avatar-name-color': colord(
+						borderColor
+					).isReadable( '#1e1e1e', {
+						level: 'AA',
+						size: 'normal',
+					} )
+						? '#1e1e1e'
+						: '#fff',
+			  }
+			: {} ),
+	} as React.CSSProperties;
```

Currently, `colord()` is being invoked on every React render.

Since it's a potentially an expensive calculation to perform, we should wrap the computation of `customProperties` in a `useMemo`.

Something like this:
```ts
const nameColor = useMemo(
	() =>
		borderColor &&
		colord( borderColor ).isReadable( '#1e1e1e', {
			level: 'AA',
			size: 'normal',
		} )
			? '#1e1e1e'
			: '#fff',
	[ borderColor ]
);

const customProperties = {
	...style,
	...( src ? { '--editor-avatar-url': `url(${ src })` } : {} ),
	...( borderColor
		? {
				'--editor-avatar-outline-color': borderColor,
				'--editor-avatar-name-color': nameColor,
		  }
		: {} ),
} as React.CSSProperties;
```

### Review thread 20 (resolved)

#### ciampo on 2026-02-19T10:14:43Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826828931
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,94 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
+	const customProperties = {
+		...style,
+		...( src ? { '--editor-avatar-url': `url(${ src })` } : {} ),
+		...( borderColor
+			? {
+					'--editor-avatar-outline-color': borderColor,
+					'--editor-avatar-name-color': colord(
+						borderColor
+					).isReadable( '#1e1e1e', {
+						level: 'AA',
+						size: 'normal',
+					} )
+						? '#1e1e1e'
+						: '#fff',
```

Let's as least extract these hardcoded hex values to some local consts, `WHITE` and `GRAY_100`. It should improve readibility and code searchability.

### Review thread 21 (resolved)

#### ciampo on 2026-02-19T10:19:27Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826856334
Path: packages/editor/src/components/collaborators-overlay/avatar-iframe-styles.ts

I'm still quite concerned about the fragility of duplicating code, especially when the duplication can not be a simple copy-paste (since we're using ad-hoc JS variables instead of CSS variables, and partially omitting some styles).

We should prioritize solving this caveat by either not needing duplication at all, or at least by automating the generation of the duplicated styles.

Finally, let's also add comments in the SCSS source about keeping them in sync with this file.

#### dabowman on 2026-02-20T18:22:59Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2834533623
Path: packages/editor/src/components/collaborators-overlay/avatar-iframe-styles.ts

Yeah, this is definitely janky. As I understand It, the real fix is a change in the block-editor package to allow us to enqueue the scss directly inside the iframe instead of hand-compiling it and injecting, which is very suboptimal. 

I believe @chriszarate has more context there.

### Review thread 22 (resolved)

#### ciampo on 2026-02-19T10:25:43Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826894732
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -276,14 +98,29 @@ export function Overlay( {
 					/>
 					<Avatar
 						className="collaborators-overlay-user-label"
-						badge
+						variant="badge"
 						size="small"
 						src={ cursor.avatarUrl }
 						name={ cursor.userName }
 						borderColor={ cursor.color }
 					/>
 				</div>
 			) ) }
+			{ highlights.map( ( highlight ) => (
+				<Avatar
+					key={ highlight.blockId }
```

If two collaborators select the same block, only the last one in the `blocksToHighlight` array will have its highlight data pushed to results, resulting in only one avatar label.

This was pre-existing behavior for the outline, but now that avatar labels are rendered, it's more visually apparent.

If we want to embrace this approach (ie. only show one avatar per block even if there are more), we should make this explicit in the code (ie. grouping by `blockId` before the loop and explicitly chose one collaborator per block). This is my suggested approach, at least for this PR.

Later on, we can work on refining the approach and support multiple highlights / avatars.

#### dabowman on 2026-02-20T18:24:02Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2834538429
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -276,14 +98,29 @@ export function Overlay( {
 					/>
 					<Avatar
 						className="collaborators-overlay-user-label"
-						badge
+						variant="badge"
 						size="small"
 						src={ cursor.avatarUrl }
 						name={ cursor.userName }
 						borderColor={ cursor.color }
 					/>
 				</div>
 			) ) }
+			{ highlights.map( ( highlight ) => (
+				<Avatar
+					key={ highlight.blockId }
```

I've got designs to handle this collision when two people select the same thing. Just need to work on implementing it. This seems like a good solution for now.

#### ciampo on 2026-02-24T17:22:15Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2848515897
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -276,14 +98,29 @@ export function Overlay( {
 					/>
 					<Avatar
 						className="collaborators-overlay-user-label"
-						badge
+						variant="badge"
 						size="small"
 						src={ cursor.avatarUrl }
 						name={ cursor.userName }
 						borderColor={ cursor.color }
 					/>
 				</div>
 			) ) }
+			{ highlights.map( ( highlight ) => (
+				<Avatar
+					key={ highlight.blockId }
```

Sounds good — in this case, let's 

> If we want to embrace this approach (ie. only show one avatar per block even if there are more), we should make this explicit in the code (ie. grouping by blockId before the loop and explicitly chose one collaborator per block). This is my suggested approach, at least for this PR.

And we can later refine the approach

### Review thread 23 (resolved)

#### ciampo on 2026-02-19T10:32:08Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826932207
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -103,16 +128,42 @@ export function useBlockHighlighting(
 				return;
 			}
 
-			if ( blockElement ) {
-				blockElement.classList.add( 'is-collaborator-selected' );
-				blockElement.style.setProperty(
-					'--collaborator-outline-color',
-					color
-				);
-				highlightedBlockIds.current.add( blockId );
+			blockElement.classList.add( 'is-collaborator-selected' );
+			blockElement.style.setProperty(
+				'--collaborator-outline-color',
+				color
+			);
+			highlightedBlockIds.current.add( blockId );
+
+			if ( overlayElement ) {
+				const blockRect = blockElement.getBoundingClientRect();
+				const overlayRect = overlayElement.getBoundingClientRect();
+
+				results.push( {
+					blockId,
+					userName,
+					avatarUrl,
+					color,
+					x: blockRect.left - overlayRect.left,
+					y: blockRect.top - overlayRect.top,
+				} );
 			}
 		} );
```

- Recalculating `overlayElement.getBoundingClientRect()` in the `forEach` loop is a waste of computation, since `overlayElement` shouldn't change during the loop.
- When `overlayElement` is `null`, the classnames and the CSS vars are still added before the check even if the `results` array is empty (meaning no avatar labels to render). Is it intentional?

A potentially cleaner version could be to move the `overlayElement` check and rect computation before the `forEach` loop:

```tsx
const overlayRect = overlayElement?.getBoundingClientRect() ?? null;

blocksToHighlight.forEach( ( block ) => {
    const { color, blockId, userName, avatarUrl } = block;
    const blockElement = getBlockElementById( blockEditorDocument, blockId );
    if ( ! blockElement ) {
        return;
    }

    blockElement.classList.add( 'is-collaborator-selected' );
    blockElement.style.setProperty( '--collaborator-outline-color', color );
    highlightedBlockIds.current.add( blockId );

    if ( overlayRect ) {
        const blockRect = blockElement.getBoundingClientRect();
        results.push( {
            blockId,
            userName,
            avatarUrl,
            color,
            x: blockRect.left - overlayRect.left,
            y: blockRect.top - overlayRect.top,
        } );
    }
} );
```

### Review thread 24 (resolved)

#### ciampo on 2026-02-19T10:39:05Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2826976187
Path: packages/components/src/avatar/component.tsx

Let's also add another `### Internal` CHANGELOG entry in the `Unreleased` section for the components package about the removal of the `Avatar` components

### Review thread 25

#### maxschmeling on 2026-02-26T20:13:52Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2861088310
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -237,23 +45,34 @@ export function Overlay( {
 		postType ?? null
 	);
 
+	const { highlights, rerenderHighlightsAfterDelay } = useBlockHighlighting(
+		overlayElement,
+		blockEditorDocument ?? null,
+		postId ?? null,
+		postType ?? null
+	);
+
+	// Combine both delayed rerenders so layout changes recompute everything.
+	const rerenderAfterDelay = useCallback( () => {
+		const cleanupCursors = rerenderCursorsAfterDelay();
+		const cleanupHighlights = rerenderHighlightsAfterDelay();
+		return () => {
+			cleanupCursors();
+			cleanupHighlights();
+		};
+	}, [ rerenderCursorsAfterDelay, rerenderHighlightsAfterDelay ] );
+
 	// Detect layout changes on overlay (e.g. turning on "Show Template") and window
-	// resizes, and re-render the cursors.
-	const resizeObserverRef = useResizeObserver( rerenderCursorsAfterDelay );
-	useEffect( rerenderCursorsAfterDelay, [ rerenderCursorsAfterDelay ] );
+	// resizes, and re-render the cursors and block highlights.
+	const resizeObserverRef = useResizeObserver( rerenderAfterDelay );
+	useEffect( rerenderAfterDelay, [ rerenderAfterDelay ] );
```

Is it necessary to run the `rerenderAfterDelay` when it changes? Having a `useEffect` where the function is also the dependency seems strange.

#### dabowman on 2026-02-26T21:24:40Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2861358682
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -237,23 +45,34 @@ export function Overlay( {
 		postType ?? null
 	);
 
+	const { highlights, rerenderHighlightsAfterDelay } = useBlockHighlighting(
+		overlayElement,
+		blockEditorDocument ?? null,
+		postId ?? null,
+		postType ?? null
+	);
+
+	// Combine both delayed rerenders so layout changes recompute everything.
+	const rerenderAfterDelay = useCallback( () => {
+		const cleanupCursors = rerenderCursorsAfterDelay();
+		const cleanupHighlights = rerenderHighlightsAfterDelay();
+		return () => {
+			cleanupCursors();
+			cleanupHighlights();
+		};
+	}, [ rerenderCursorsAfterDelay, rerenderHighlightsAfterDelay ] );
+
 	// Detect layout changes on overlay (e.g. turning on "Show Template") and window
-	// resizes, and re-render the cursors.
-	const resizeObserverRef = useResizeObserver( rerenderCursorsAfterDelay );
-	useEffect( rerenderCursorsAfterDelay, [ rerenderCursorsAfterDelay ] );
+	// resizes, and re-render the cursors and block highlights.
+	const resizeObserverRef = useResizeObserver( rerenderAfterDelay );
+	useEffect( rerenderAfterDelay, [ rerenderAfterDelay ] );
```

that makes sense, I refactored.

### Review thread 26 (resolved)

#### maxschmeling on 2026-02-26T20:15:02Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2861092573
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -37,15 +56,77 @@ export function useBlockHighlighting(
 		postType ?? null
 	);
 
-	// Draw block highlights
+	const [ highlights, setHighlights ] = useState< BlockHighlightData[] >(
+		[]
+	);
+
+	// Bump this counter to force the effect to re-run (e.g. after a layout shift).
+	const [ recomputeToken, setRecomputeToken ] = useState( 0 );
+
+	// All DOM mutations and position computations live inside useEffect.
 	useEffect( () => {
-		// Don't do anything if editor is not rendered yet.
-		if ( blockEditorDocument === null ) {
+		if ( ! blockEditorDocument ) {
+			setHighlights( [] );
 			return;
 		}
 
-		const unhighlightBlocks = ( blockIds: string[] ) => {
-			blockIds.forEach( ( blockId ) => {
+		// Capture the ref value so the cleanup closure sees the same Set
+		// even if a later render replaces it.
+		const currentHighlightedIds = highlightedBlockIds.current;
+
+		// Deduplicate by blockId — when multiple collaborators select the
+		// same block, only the first one gets the highlight and avatar label.
```

Why not put an avatar group?

#### dabowman on 2026-02-26T21:12:04Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2861312923
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -37,15 +56,77 @@ export function useBlockHighlighting(
 		postType ?? null
 	);
 
-	// Draw block highlights
+	const [ highlights, setHighlights ] = useState< BlockHighlightData[] >(
+		[]
+	);
+
+	// Bump this counter to force the effect to re-run (e.g. after a layout shift).
+	const [ recomputeToken, setRecomputeToken ] = useState( 0 );
+
+	// All DOM mutations and position computations live inside useEffect.
 	useEffect( () => {
-		// Don't do anything if editor is not rendered yet.
-		if ( blockEditorDocument === null ) {
+		if ( ! blockEditorDocument ) {
+			setHighlights( [] );
 			return;
 		}
 
-		const unhighlightBlocks = ( blockIds: string[] ) => {
-			blockIds.forEach( ( blockId ) => {
+		// Capture the ref value so the cleanup closure sees the same Set
+		// even if a later render replaces it.
+		const currentHighlightedIds = highlightedBlockIds.current;
+
+		// Deduplicate by blockId — when multiple collaborators select the
+		// same block, only the first one gets the highlight and avatar label.
```

I think we should, just in another PR. I've gotta think through a couple design details on how it should work there.

### Review thread 27

#### chriszarate on 2026-03-06T16:40:43Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896759136
Path: packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts

```diff
@@ -58,51 +139,16 @@ export function useBlockHighlighting(
 					);
 				}
 
-				highlightedBlockIds.current.delete( blockId );
-			} );
-		};
-
-		const blocksToHighlight = userStates
-			.map( ( userState: any ) => {
-				const isWholeBlockSelected =
-					userState.editorState?.selection?.type ===
-					SelectionType.WholeBlock;
-				const shouldDrawUser = ! userState.isMe;
-
-				if ( isWholeBlockSelected && shouldDrawUser ) {
-					const { localClientId } = resolveSelection(
-						userState.editorState?.selection
-					);
-
-					if ( ! localClientId ) {
-						return null;
-					}
-
-					return {
-						blockId: localClientId,
-						color: getAvatarBorderColor(
-							userState.collaboratorInfo.id
-						),
-					};
-				}
-
-				return null;
-			} )
-			.filter( ( block: any ) => block !== null );
-
-		// Unhighlight blocks that are no longer highlighted.
-		const selectedBlockIds = blocksToHighlight.map(
-			( block: any ) => block.blockId
-		);
-		const blocksIdsToUnhighlight = Array.from(
-			highlightedBlockIds.current
-		).filter( ( blockId ) => ! selectedBlockIds.includes( blockId ) );
+				currentHighlightedIds.delete( blockId );
+			}
+		}
 
-		unhighlightBlocks( blocksIdsToUnhighlight );
+		// Highlight blocks and compute positions for avatar labels.
+		const results: BlockHighlightData[] = [];
+		const overlayRect = overlayElement?.getBoundingClientRect() ?? null;
```

Use of `getBoundingClientRect()` in render. No need for action on this david, just flagging for @alecgeatches

### Review thread 28 (resolved)

#### chriszarate on 2026-03-06T16:44:14Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896779946
Path: packages/editor/src/components/collaborators-overlay/use-render-cursors.ts

```diff
@@ -110,33 +116,38 @@ export function useRenderCursors(
 							overlayElement
 						);
 					}
+				} catch {
+					// Selection may reference a stale Yjs position.
 				}
+			}
 
-				if ( coords ) {
-					results.push( {
-						userName,
-						clientId,
-						color,
-						avatarUrl,
-						...coords,
-					} );
-				}
-			} );
-
-			setCursorPositions( results );
-		},
-		[ blockEditorDocument, resolveSelection, overlayElement, sortedUsers ]
-	);
-
-	useEffect( computeCursors, [ computeCursors ] );
-
-	const rerenderCursorsAfterDelay = useMemo(
-		() => () => {
-			const timeout = setTimeout( computeCursors, 500 );
-			return () => clearTimeout( timeout );
-		},
-		[ computeCursors ]
-	);
+			if ( coords ) {
+				results.push( {
+					userName,
+					clientId,
+					color,
+					avatarUrl,
+					...coords,
+				} );
+			}
+		} );
+
+		setCursorPositions( results );
+	}, [
+		blockEditorDocument,
+		resolveSelection,
+		overlayElement,
+		sortedUsers,
+		recomputeToken,
+	] );
+
+	// The delayed rerender just bumps state — no direct DOM mutation.
+	const rerenderCursorsAfterDelay = useCallback( () => {
+		const timeout = setTimeout( () => {
+			setRecomputeToken( ( t ) => t + 1 );
+		}, 500 );
```

It would be a little better if this timeout was passed in instead of being hardcoded in two places.

#### dabowman on 2026-03-06T18:39:04Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2897269537
Path: packages/editor/src/components/collaborators-overlay/use-render-cursors.ts

```diff
@@ -110,33 +116,38 @@ export function useRenderCursors(
 							overlayElement
 						);
 					}
+				} catch {
+					// Selection may reference a stale Yjs position.
 				}
+			}
 
-				if ( coords ) {
-					results.push( {
-						userName,
-						clientId,
-						color,
-						avatarUrl,
-						...coords,
-					} );
-				}
-			} );
-
-			setCursorPositions( results );
-		},
-		[ blockEditorDocument, resolveSelection, overlayElement, sortedUsers ]
-	);
-
-	useEffect( computeCursors, [ computeCursors ] );
-
-	const rerenderCursorsAfterDelay = useMemo(
-		() => () => {
-			const timeout = setTimeout( computeCursors, 500 );
-			return () => clearTimeout( timeout );
-		},
-		[ computeCursors ]
-	);
+			if ( coords ) {
+				results.push( {
+					userName,
+					clientId,
+					color,
+					avatarUrl,
+					...coords,
+				} );
+			}
+		} );
+
+		setCursorPositions( results );
+	}, [
+		blockEditorDocument,
+		resolveSelection,
+		overlayElement,
+		sortedUsers,
+		recomputeToken,
+	] );
+
+	// The delayed rerender just bumps state — no direct DOM mutation.
+	const rerenderCursorsAfterDelay = useCallback( () => {
+		const timeout = setTimeout( () => {
+			setRecomputeToken( ( t ) => t + 1 );
+		}, 500 );
```

fixed

### Review thread 29 (resolved)

#### chriszarate on 2026-03-06T16:50:53Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896811028
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+import { useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+import { useImageLoadingStatus } from './use-image-loading-status';
+
+// Runtime equivalents of @wordpress/base-styles tokens ($gray-900, $white).
+const GRAY_900 = '#1e1e1e';
+const WHITE = '#fff';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const {
+		status: imageStatus,
+		handleLoad,
+		handleError,
+	} = useImageLoadingStatus( src );
+	const imageLoaded = imageStatus === 'loaded';
+
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
+	const nameColor = useMemo(
+		() =>
+			borderColor &&
+			colord( borderColor ).isReadable( GRAY_900, {
+				level: 'AA',
+				size: 'normal',
+			} )
+				? GRAY_900
+				: WHITE,
+		[ borderColor ]
+	);
+
+	const customProperties = {
+		...style,
+		...( borderColor
+			? {
+					'--editor-avatar-outline-color': borderColor,
+					'--editor-avatar-name-color': nameColor,
+			  }
+			: {} ),
+	} as React.CSSProperties;
+
+	const avatar = (
+		<div
+			className={ clsx( 'editor-avatar', className, {
+				'has-avatar-border-color': !! borderColor,
+				'has-src': imageLoaded,
+				'is-badge': showBadge,
+				'is-small': size === 'small',
+				'is-dimmed': dimmed,
+			} ) }
+			style={ customProperties }
+			role={ name ? 'img' : undefined }
+			aria-label={ name || undefined }
+			{ ...props }
+		>
+			<span className="editor-avatar__image">
+				{ src && (
+					<img
+						src={ src }
+						alt=""
```

Just double-checking, but `alt` is intentionally blank because we will have an announcable `Tooltip` when the name is available?

#### dabowman on 2026-03-06T18:45:29Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2897294577
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+import { useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+import { useImageLoadingStatus } from './use-image-loading-status';
+
+// Runtime equivalents of @wordpress/base-styles tokens ($gray-900, $white).
+const GRAY_900 = '#1e1e1e';
+const WHITE = '#fff';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const {
+		status: imageStatus,
+		handleLoad,
+		handleError,
+	} = useImageLoadingStatus( src );
+	const imageLoaded = imageStatus === 'loaded';
+
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
+	const nameColor = useMemo(
+		() =>
+			borderColor &&
+			colord( borderColor ).isReadable( GRAY_900, {
+				level: 'AA',
+				size: 'normal',
+			} )
+				? GRAY_900
+				: WHITE,
+		[ borderColor ]
+	);
+
+	const customProperties = {
+		...style,
+		...( borderColor
+			? {
+					'--editor-avatar-outline-color': borderColor,
+					'--editor-avatar-name-color': nameColor,
+			  }
+			: {} ),
+	} as React.CSSProperties;
+
+	const avatar = (
+		<div
+			className={ clsx( 'editor-avatar', className, {
+				'has-avatar-border-color': !! borderColor,
+				'has-src': imageLoaded,
+				'is-badge': showBadge,
+				'is-small': size === 'small',
+				'is-dimmed': dimmed,
+			} ) }
+			style={ customProperties }
+			role={ name ? 'img' : undefined }
+			aria-label={ name || undefined }
+			{ ...props }
+		>
+			<span className="editor-avatar__image">
+				{ src && (
+					<img
+						src={ src }
+						alt=""
```

Yes, alt="" is intentionally blank and correct here. The <img> is decorative — the meaningful accessible name is already on the parent <div> via role="img" + aria-label={name} (lines 88-89). A non-empty alt on the <img> would cause screen readers to announce the image twice. The blank alt tells screen readers to skip the <img> element entirely, letting the outer role="img" container carry the full accessible name.

### Review thread 30

#### ciampo on 2026-03-06T16:58:17Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896846751
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+import { useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+import { useImageLoadingStatus } from './use-image-loading-status';
+
+// Runtime equivalents of @wordpress/base-styles tokens ($gray-900, $white).
+const GRAY_900 = '#1e1e1e';
+const WHITE = '#fff';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const {
+		status: imageStatus,
+		handleLoad,
+		handleError,
+	} = useImageLoadingStatus( src );
+	const imageLoaded = imageStatus === 'loaded';
+
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
```

This may be ok, but it's worth flagging that it will show the initials of up to the two first words, ie.

"Name Middlename FamilyName" => "N M"
"Justasinglename" => "J"

#### dabowman on 2026-03-06T18:44:52Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2897292323
Path: packages/editor/src/components/collaborators-presence/avatar/component.tsx

```diff
@@ -0,0 +1,123 @@
+/**
+ * External dependencies
+ */
+import clsx from 'clsx';
+import { colord, extend } from 'colord';
+import a11yPlugin from 'colord/plugins/a11y';
+
+extend( [ a11yPlugin ] );
+
+/**
+ * WordPress dependencies
+ */
+import { Icon, Tooltip } from '@wordpress/components';
+import { useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { AvatarProps } from './types';
+import { useImageLoadingStatus } from './use-image-loading-status';
+
+// Runtime equivalents of @wordpress/base-styles tokens ($gray-900, $white).
+const GRAY_900 = '#1e1e1e';
+const WHITE = '#fff';
+
+function Avatar( {
+	className,
+	src,
+	name,
+	label,
+	variant,
+	size = 'default',
+	borderColor,
+	dimmed = false,
+	statusIndicator,
+	style,
+	...props
+}: AvatarProps &
+	Omit< React.HTMLAttributes< HTMLDivElement >, keyof AvatarProps > ) {
+	const {
+		status: imageStatus,
+		handleLoad,
+		handleError,
+	} = useImageLoadingStatus( src );
+	const imageLoaded = imageStatus === 'loaded';
+
+	const showBadge = variant === 'badge' && !! name;
+	const initials = name
+		? name
+				.split( /\s+/ )
+				.slice( 0, 2 )
+				.map( ( word ) => word[ 0 ] )
+				.join( '' )
+				.toUpperCase()
+		: undefined;
```

Yeah, that was intentional. I'm not super opinionated about it though. I figured two letters gave us more unique identifiers.

### Review thread 31

#### ciampo on 2026-03-06T17:10:53Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896902669
Path: packages/components/CHANGELOG.md

```diff
@@ -6,6 +6,10 @@
 
 -   Revert `word-break: break-word` addition ([#76230](https://github.com/WordPress/gutenberg/pull/76230)).
 
+### Internal
```

Is the goal of this PR to be merged ASAP and backported to the WordPress 7.0 release?

Asking because I'd like to avoid shipping WordPress 7.0 with `Avatar` and `AvatarGroup`, and as such, I'd like to remove them from the `@wordpress/components` package ASAP.

#### dabowman on 2026-03-06T18:28:17Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2897224470
Path: packages/components/CHANGELOG.md

```diff
@@ -6,6 +6,10 @@
 
 -   Revert `word-break: break-word` addition ([#76230](https://github.com/WordPress/gutenberg/pull/76230)).
 
+### Internal
```

Yeah, the goal is to get this merged so we get those two components out of components.

### Review thread 32 (resolved)

#### ciampo on 2026-03-06T17:13:07Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896912198
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -230,28 +45,40 @@ export function Overlay( {
 		postType ?? null
 	);
 
+	const { highlights, rerenderHighlightsAfterDelay } = useBlockHighlighting(
+		overlayElement,
+		blockEditorDocument ?? null,
+		postId ?? null,
+		postType ?? null
+	);
+
 	// Detect layout changes on overlay (e.g. turning on "Show Template") and window
-	// resizes, and re-render the cursors.
-	const resizeObserverRef = useResizeObserver( rerenderCursorsAfterDelay );
-	useEffect( rerenderCursorsAfterDelay, [ rerenderCursorsAfterDelay ] );
+	// resizes, and re-render the cursors and block highlights.
+	const resizeObserverRef = useResizeObserver( () => {
+		rerenderCursorsAfterDelay();
+		rerenderHighlightsAfterDelay();
+	} );
```

Perf nit: let's extract the callback and wrap in `useCallback`, otherwise we risk having intersection observers being destroyed and re-created every re-render.




```suggestion
	const handleResize = useCallback( () => {
		rerenderCursorsAfterDelay();
		rerenderHighlightsAfterDelay();
	}, [ rerenderCursorsAfterDelay, rerenderHighlightsAfterDelay ] );
	const resizeObserverRef = useResizeObserver( handleResize );
```

#### dabowman on 2026-03-06T18:49:05Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2897309106
Path: packages/editor/src/components/collaborators-overlay/overlay.tsx

```diff
@@ -230,28 +45,40 @@ export function Overlay( {
 		postType ?? null
 	);
 
+	const { highlights, rerenderHighlightsAfterDelay } = useBlockHighlighting(
+		overlayElement,
+		blockEditorDocument ?? null,
+		postId ?? null,
+		postType ?? null
+	);
+
 	// Detect layout changes on overlay (e.g. turning on "Show Template") and window
-	// resizes, and re-render the cursors.
-	const resizeObserverRef = useResizeObserver( rerenderCursorsAfterDelay );
-	useEffect( rerenderCursorsAfterDelay, [ rerenderCursorsAfterDelay ] );
+	// resizes, and re-render the cursors and block highlights.
+	const resizeObserverRef = useResizeObserver( () => {
+		rerenderCursorsAfterDelay();
+		rerenderHighlightsAfterDelay();
+	} );
```

fixed!

### Review thread 33 (resolved)

#### ciampo on 2026-03-06T17:15:38Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2896923219
Path: packages/editor/src/components/collaborators-overlay/use-render-cursors.ts

```diff
@@ -110,33 +116,38 @@ export function useRenderCursors(
 							overlayElement
 						);
 					}
+				} catch {
+					// Selection may reference a stale Yjs position.
 				}
+			}
 
-				if ( coords ) {
-					results.push( {
-						userName,
-						clientId,
-						color,
-						avatarUrl,
-						...coords,
-					} );
-				}
-			} );
-
-			setCursorPositions( results );
-		},
-		[ blockEditorDocument, resolveSelection, overlayElement, sortedUsers ]
-	);
-
-	useEffect( computeCursors, [ computeCursors ] );
-
-	const rerenderCursorsAfterDelay = useMemo(
-		() => () => {
-			const timeout = setTimeout( computeCursors, 500 );
-			return () => clearTimeout( timeout );
-		},
-		[ computeCursors ]
-	);
+			if ( coords ) {
+				results.push( {
+					userName,
+					clientId,
+					color,
+					avatarUrl,
+					...coords,
+				} );
+			}
+		} );
+
+		setCursorPositions( results );
+	}, [
+		blockEditorDocument,
+		resolveSelection,
+		overlayElement,
+		sortedUsers,
+		recomputeToken,
+	] );
+
+	// The delayed rerender just bumps state — no direct DOM mutation.
+	const rerenderCursorsAfterDelay = useCallback( () => {
```

Each call to `rerenderCursorsAfterDelay()` (and `rerenderHighlightsAfterDelay()` creates a new `setTimeout` without clearing any previous one. During a window resize, the resize observer fires many times in quick succession, queueing multiple independent 500ms timeouts. Each one bumps the recompute token, causing redundant effect runs
.
This is not a correctness bug, but it's wasteful. To debounce properly, the timeout should be stored in a useRef so subsequent calls clear the previous timeout:

```ts
const rerenderHighlightsAfterDelay = useCallback( () => {
    if ( timeoutRef.current ) {
        clearTimeout( timeoutRef.current );
    }
    timeoutRef.current = setTimeout( () => {
        setRecomputeToken( ( t ) => t + 1 );
    }, 500 );
    return () => {
        if ( timeoutRef.current ) {
            clearTimeout( timeoutRef.current );
        }
    };
}, [] );
```

Alternatively, since the same pattern appears in both hooks, consider extracting a small useDebouncedRecompute utility.

#### dabowman on 2026-03-06T18:53:16Z

URL: https://github.com/WordPress/gutenberg/pull/75652#discussion_r2897326134
Path: packages/editor/src/components/collaborators-overlay/use-render-cursors.ts

```diff
@@ -110,33 +116,38 @@ export function useRenderCursors(
 							overlayElement
 						);
 					}
+				} catch {
+					// Selection may reference a stale Yjs position.
 				}
+			}
 
-				if ( coords ) {
-					results.push( {
-						userName,
-						clientId,
-						color,
-						avatarUrl,
-						...coords,
-					} );
-				}
-			} );
-
-			setCursorPositions( results );
-		},
-		[ blockEditorDocument, resolveSelection, overlayElement, sortedUsers ]
-	);
-
-	useEffect( computeCursors, [ computeCursors ] );
-
-	const rerenderCursorsAfterDelay = useMemo(
-		() => () => {
-			const timeout = setTimeout( computeCursors, 500 );
-			return () => clearTimeout( timeout );
-		},
-		[ computeCursors ]
-	);
+			if ( coords ) {
+				results.push( {
+					userName,
+					clientId,
+					color,
+					avatarUrl,
+					...coords,
+				} );
+			}
+		} );
+
+		setCursorPositions( results );
+	}, [
+		blockEditorDocument,
+		resolveSelection,
+		overlayElement,
+		sortedUsers,
+		recomputeToken,
+	] );
+
+	// The delayed rerender just bumps state — no direct DOM mutation.
+	const rerenderCursorsAfterDelay = useCallback( () => {
```

fixed with the useDebouncedRecompute utility!

## Changed files

- package-lock.json (MODIFIED, +1/-0)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/avatar/component.tsx (DELETED, +0/-82)
- packages/components/src/private-apis.ts (MODIFIED, +0/-4)
- packages/components/src/style.scss (MODIFIED, +0/-2)
- packages/editor/package.json (MODIFIED, +1/-0)
- packages/editor/src/components/collab-sidebar/utils.js (MODIFIED, +9/-10)
- packages/editor/src/components/collaborators-overlay/avatar-iframe-styles.ts (ADDED, +126/-0)
- packages/editor/src/components/collaborators-overlay/collaborator-styles.ts (MODIFIED, +43/-2)
- packages/editor/src/components/collaborators-overlay/overlay-iframe-styles.ts (ADDED, +125/-0)
- packages/editor/src/components/collaborators-overlay/overlay.tsx (MODIFIED, +54/-207)
- packages/editor/src/components/collaborators-overlay/use-block-highlighting.ts (MODIFIED, +147/-64)
- packages/editor/src/components/collaborators-overlay/use-debounced-recompute.ts (ADDED, +32/-0)
- packages/editor/src/components/collaborators-overlay/use-render-cursors.ts (MODIFIED, +72/-66)
- packages/editor/src/components/collaborators-presence/avatar-group/component.tsx (RENAMED, +14/-5)
- packages/editor/src/components/collaborators-presence/avatar-group/index.ts (RENAMED, +0/-0)
- packages/editor/src/components/collaborators-presence/avatar-group/styles.scss (RENAMED, +5/-4)
- packages/editor/src/components/collaborators-presence/avatar-group/test/index.tsx (ADDED, +139/-0)
- packages/editor/src/components/collaborators-presence/avatar-group/types.ts (RENAMED, +0/-0)
- packages/editor/src/components/collaborators-presence/avatar/component.tsx (ADDED, +123/-0)
- packages/editor/src/components/collaborators-presence/avatar/index.ts (RENAMED, +0/-0)
- packages/editor/src/components/collaborators-presence/avatar/styles.scss (RENAMED, +65/-49)
- packages/editor/src/components/collaborators-presence/avatar/test/index.tsx (ADDED, +389/-0)
- packages/editor/src/components/collaborators-presence/avatar/types.ts (RENAMED, +20/-13)
- packages/editor/src/components/collaborators-presence/avatar/use-image-loading-status.ts (ADDED, +36/-0)
- packages/editor/src/components/collaborators-presence/index.tsx (MODIFIED, +4/-6)
- packages/editor/src/components/collaborators-presence/list.tsx (MODIFIED, +7/-17)
- packages/editor/src/components/collaborators-presence/styles/collaborators-list.scss (MODIFIED, +26/-19)
- packages/editor/src/components/collaborators-presence/styles/collaborators-presence.scss (MODIFIED, +6/-2)
- packages/editor/src/style.scss (MODIFIED, +2/-0)
