---
number: 65988
type: pull_request
state: MERGED
merged_at: 2024-10-10T11:24:40Z
url: https://github.com/WordPress/gutenberg/pull/65988
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components","First-time Contributor","has dev note"]
---

# PR #65988: SearchControl: Deprecate onClose prop

- URL: https://github.com/WordPress/gutenberg/pull/65988
- Author: Vrishabhsk
- Created: 2024-10-09T12:02:14Z
- Updated: 2024-10-14T03:56:31Z
- Comments: 4 of 4
- Review threads: 5 of 5
- Changed files: 6 of 6
- Diff: ../pr-diffs/65988.diff

## Body

## What?
- Add `@deprecated` for `onClose` function prop for `SearchControl component`
- Update `README.md`, `JSDoc` and `StoryBook` with `_Note: this prop is deprecated._`
- When `onClose` prop is provided, execute `deprecated` from `@wordpress/deprecated`
- Update `test` to expect `console.warn` when `onClose` is provided + fix `should should` typo

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
- https://github.com/WordPress/gutenberg/issues/65334
> The onClose prop is not used anymore in the Gutenberg app
> The prop itself is causing confusion to consumers (https://github.com/WordPress/gutenberg/issues/64921),

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
- `Soft Deprecates` the `onClose` prop
- Add `deprecation` notice in the `SearchControl's docs`
- Execute `deprecated` when `onClose` is provided
- Add `test` to expect `deprecation warning`

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->
- Clone the branch into the `plugins` directory of your `WordPress` installation
```
git clone https://github.com/Vrishabhsk/gutenberg/tree/update/deprecate-onClose
```
- Build the files
```
cd gutenberg && npm install & npm build
```
- Activate the `Gutenberg`plugin via the `Plugins Admin Page`.
- Create a custom block
```
npx @wordpress/create-block@latest demo --variant=dynamic
```
- Modify the `edit.js` to use `SelectControl` component
```
export default function Edit() {
	return (
		<p {...useBlockProps()}>
			<h1>Shut up</h1>
			<InspectorControls>
				<PanelBody title="Form Settings" initialOpen={false}>
					<PanelRow>
						<SearchControl
							label={__("Search posts")}
							onChange={(val) => {}}
						/>
					</PanelRow>
				</PanelBody>
			</InspectorControls>
		</p>
	);
}
```
- To test the deprecated log, use the following snippet : [`Deprecated Hook`](https://developer.wordpress.org/block-editor/reference-guides/packages/packages-deprecated/#hook)

## Issue comments

### github-actions on 2024-10-09T12:02:30Z

URL: https://github.com/WordPress/gutenberg/pull/65988#issuecomment-2402128499

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: Vrishabhsk <vrishabhsk@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-10-09T12:02:40Z

URL: https://github.com/WordPress/gutenberg/pull/65988#issuecomment-2402128808

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @Vrishabhsk! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### mirka on 2024-10-09T20:41:38Z

URL: https://github.com/WordPress/gutenberg/pull/65988#issuecomment-2403391308

Could you also add a [changelog](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CHANGELOG.md) entry please? Thanks!

### mirka on 2024-10-10T13:25:00Z

URL: https://github.com/WordPress/gutenberg/pull/65988#issuecomment-2405089255

## ✍️ Dev Note

### SearchControl: Deprecated `onClose` prop

This prop was originally intended for adding a custom click handler to the suffix button to close the search field entirely, rather than just clear the input value.

The pattern of repurposing the search clear button as a search close button is no longer used in WordPress, and is no longer recommended as a UI pattern since it can be confusing to users.

If you were relying on this prop, we recommend adding a separate close button to your UI.

## Review threads

### Review thread 1 (resolved)

#### mirka on 2024-10-09T20:26:27Z

URL: https://github.com/WordPress/gutenberg/pull/65988#discussion_r1794182826
Path: packages/components/src/search-control/index.tsx

```diff
@@ -31,6 +32,14 @@ function SuffixItem( {
 		return <Icon icon={ search } />;
 	}
 
+	if ( onClose ) {
+		deprecated( 'onClose function prop', {
+			since: '6.7',
+			version: '',
+			plugin: 'wp.components.SearchControl',
+		} );
```

- Let's phrase it this way so the component name is visible in the console warning. (Apparently the `plugin` string won't show unless [`version` is truthy](https://github.com/WordPress/gutenberg/blob/e86152bc8b45d5fe213ed688f145e82f73bb2fd4/packages/deprecated/src/index.js#L46-L48)).
- This isn't going to land in 6.7, so it'll be the next release.
- We can just omit the `version` property.

```suggestion
		deprecated( '`onClose` prop in wp.components.SearchControl', {
			since: '6.8',
		} );
```

### Review thread 2 (resolved)

#### mirka on 2024-10-09T20:27:10Z

URL: https://github.com/WordPress/gutenberg/pull/65988#discussion_r1794183570
Path: packages/components/src/search-control/stories/index.story.tsx

```diff
@@ -52,6 +52,8 @@ Default.args = {
 };
 
 /**
+ * _Note: this prop is deprecated._
+ *
  * When an `onClose` callback is provided, the search control will render a close button
  * that will trigger the given callback.
  *
```

I think we can remove this entire story, since we don't want to recommend it.

### Review thread 3 (resolved)

#### mirka on 2024-10-09T20:27:37Z

URL: https://github.com/WordPress/gutenberg/pull/65988#discussion_r1794184097
Path: packages/components/src/search-control/test/index.tsx

```diff
@@ -72,13 +72,14 @@ describe( 'SearchControl', () => {
 			expect( onChangeSpy ).toHaveBeenLastCalledWith( '' );
 		} );
 
-		it( 'should should render a Close button (instead of Reset) when onClose function is provided', async () => {
```

Thanks for the cleanup! 🧹

### Review thread 4 (resolved)

#### mirka on 2024-10-09T20:33:53Z

URL: https://github.com/WordPress/gutenberg/pull/65988#discussion_r1794191480
Path: packages/components/src/search-control/types.ts

```diff
@@ -40,6 +40,8 @@ export type SearchControlProps = Pick< InputControlProps, 'help' | 'value' > & {
 	 *
 	 * Use this if you want the button to trigger your own logic to close the search field entirely,
 	 * rather than just clearing the input value.
+	 *
+	 * @deprecated
```

Let's also add a `@ignore` tag, which will suppress this from the Storybook props table.

```suggestion
	 *
	 * @deprecated
	 * @ignore
```

### Review thread 5 (resolved)

#### ciampo on 2024-10-10T08:33:41Z

URL: https://github.com/WordPress/gutenberg/pull/65988#discussion_r1794971241
Path: packages/components/src/search-control/test/index.tsx

```diff
@@ -72,13 +72,14 @@ describe( 'SearchControl', () => {
 			expect( onChangeSpy ).toHaveBeenLastCalledWith( '' );
 		} );
 
-		it( 'should should render a Close button (instead of Reset) when onClose function is provided', async () => {
+		it( 'should render a Close button (instead of Reset) when onClose function is provided', async () => {
 			const onChangeSpy = jest.fn();
 			const onCloseSpy = jest.fn();
 			render(
 				<Component onChange={ onChangeSpy } onClose={ onCloseSpy } />
 			);
 
+			expect( console ).toHaveWarned();
```

Nit: can we use `toHaveWarnedWith` and specify the deprecation message? It would make the test easier to understand and would guard against any other unwanted warnings

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/search-control/README.md (MODIFIED, +2/-0)
- packages/components/src/search-control/index.tsx (MODIFIED, +7/-0)
- packages/components/src/search-control/stories/index.story.tsx (MODIFIED, +0/-15)
- packages/components/src/search-control/test/index.tsx (MODIFIED, +4/-1)
- packages/components/src/search-control/types.ts (MODIFIED, +3/-0)
