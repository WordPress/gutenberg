---
number: 14179
type: pull_request
state: MERGED
merged_at: 2019-03-28T12:03:55Z
url: https://github.com/WordPress/gutenberg/pull/14179
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #14179: Code quality: Remove unnecessary BaseControl usage from video block

- URL: https://github.com/WordPress/gutenberg/pull/14179
- Author: jorgefilipecosta
- Created: 2019-02-28T22:42:19Z
- Updated: 2019-03-29T12:12:38Z
- Comments: 4 of 4
- Review threads: 6 of 6
- Changed files: 4 of 4
- Diff: ../pr-diffs/14179.diff

## Body

## Description
This PR applies a simple change to remove an unnecessary BaseControl usage in the video block.
It makes the video block code compliant with the lint rule being added on https://github.com/WordPress/gutenberg/pull/14151.

I tried a different approach use the BaseControl as a label for the button being rendered inside it, but in my tests with a screen reader the button text stops being used and BaseControl label starts getting used, in this case, this change does not make sense, the button text should be used.

## How has this been tested?
I checked that the Poster Image buttons still work.
I checked that no visual changes happen.

## Issue comments

### afercia on 2019-03-06T15:14:15Z

URL: https://github.com/WordPress/gutenberg/pull/14179#issuecomment-470144770

Preferably, I'd like the Editor to enforce a proper usage of _visible_ `<label>` elements. I don't think the component should take into account the styling of "visual labels". Instead, _not_ providing a default styling for visual labels would encourage developers to use real labels 🙂 

Alternatively, this option:
> Have a separate boolean prop which is considered by the ESLint rule to allow it as an exception, e.g. hasVisualLabel, hasOrphanedLabel

should also make sure the input if labelled, wither with an `aria-label` or an `aria-labelledby` that references an existing ID. I realize it gets more complicated: one more reason to enforce only the first case?

### jorgefilipecosta on 2019-03-07T11:35:39Z

URL: https://github.com/WordPress/gutenberg/pull/14179#issuecomment-470492837

What if we have a BaseControl.VisualLabel component that contains the correct padding and can be used for these cases as a child component?
It would be just a child component, and it is not as practical to use as a label prop so it would only be used for this type of exceptions.

### aduth on 2019-03-11T19:17:36Z

URL: https://github.com/WordPress/gutenberg/pull/14179#issuecomment-471682677

@jorgefilipecosta That's a pretty clever idea!

### jorgefilipecosta on 2019-03-13T21:47:54Z

URL: https://github.com/WordPress/gutenberg/pull/14179#issuecomment-472619193

Hi @aduth thank you for analyzing this hypothesis, I updated this PR to implement this idea so we can get a better feel of how it looks.

## Review threads

### Review thread 1

#### aduth on 2019-03-01T18:53:23Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r261717995
Path: packages/block-library/src/video/editor.scss

```diff
@@ -9,3 +9,8 @@
 .editor-video-poster-control .components-button + .components-button {
 	margin-top: 1em;
 }
+
+.editor-video-poster-control > span {
+	display: block;
```

If we want a non-semantic block element, why not just render a `div`, rather than a `span` that we re-style?

### Review thread 2

#### aduth on 2019-03-01T19:06:06Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r261722258
Path: packages/block-library/src/video/editor.scss

```diff
@@ -9,3 +9,8 @@
 .editor-video-poster-control .components-button + .components-button {
 	margin-top: 1em;
 }
+
+.editor-video-poster-control > span {
+	display: block;
+	margin-bottom: 4px;
```

Is `BaseControl` truly "unnecessary" when we had to recreate the margin it applies to the label it generates? This has a high risk of falling out of sync, and at the very least is only incidentally aligned to the base control margin (which is assigned by the [value of the `$grid-size-small` variable](https://github.com/WordPress/gutenberg/blob/54e34bf5fc566339cfcce3910b52a19e7a77d27f/packages/components/src/base-control/style.scss#L15), not hard-coded).

I realize in some ways this contradicts what's proposed in https://github.com/WordPress/gutenberg/pull/9802#discussion_r257269125 with having the "just another child" text element.

Maybe the compromise is: Leave it as `BaseControl`, render the label as a `div` / `span`, and effect the margin by some "padding between" all children of the base control field?

```scss
.components-base-control__field > * {
	margin: $grid-size-small 0;

	&:first-child {
		margin-top: 0;
	}

	&:last-child {
		margin-bottom: 0;
	}
}
```

#### jorgefilipecosta on 2019-03-04T09:27:25Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r261977307
Path: packages/block-library/src/video/editor.scss

```diff
@@ -9,3 +9,8 @@
 .editor-video-poster-control .components-button + .components-button {
 	margin-top: 1em;
 }
+
+.editor-video-poster-control > span {
+	display: block;
+	margin-bottom: 4px;
```

Hi @aduth the code was updated following a logic similar to the one you described.
The specific CSS rule is a little bit simpler and just applies the margin to the first child of components-base-control__field, it seems safer e.g: Toggle control works exactly as before while if we apply  "padding between" all children the design get broken.

### Review thread 3

#### aduth on 2019-03-06T14:49:24Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r262973025
Path: packages/components/src/base-control/style.scss

```diff
@@ -5,14 +5,18 @@
 	.components-base-control__field {
 		margin-bottom: $grid-size;
 
+		& > *:first-child {
+			margin-bottom: $grid-size-small;
+		}
+
 		.components-panel__row & {
 			margin-bottom: inherit;
 		}
 	}
 
 	.components-base-control__label {
 		display: block;
-		margin-bottom: $grid-size-small;
+
```

```suggestion
```

### Review thread 4

#### aduth on 2019-03-18T20:28:11Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r266627591
Path: packages/components/src/base-control/index.js

```diff
@@ -16,4 +16,14 @@ function BaseControl( { id, label, help, className, children } ) {
 	);
 }
 
+BaseControl.VisualLabel = ( { className, children } ) => {
+	return (
+		<div className={
+			classnames( 'components-base-control-visual-label', className )
```

Two notes:

- If the original idea was that `BaseControl` could use this visual label in the default implementation, should that be done here? In other words, I don't see a reason why we need multiple styles selectors to cover both `.components-base-control__label` and `.components-base-control-visual-label`, when it should be the same (replace `<span className="components-base-control__label">` above with `<BaseControl.VisualLabel>`).
- If there's a worry about backwards-compatibility, we should consider just applying the existing `.components-base-control__label`. In fact, I'd go so far as to suggest this is _not compliant_ with the [standard recommendations](https://github.com/WordPress/gutenberg/blob/master/docs/contributors/coding-guidelines.md#naming) (it is not in a folder `base-control-visual-label`). While it is its own root component (which complicates things), I think it's fair to say this would qualify as a descendant of `BaseControl`, and thus `.components-base-control__label` would be a correct/ideal class name anyways.

(_Aside:_ I have been meaning to propose some improved phrasing for the naming recommendations, as they're admittedly a bit unclear and editor-oriented at the moment)

#### jorgefilipecosta on 2019-03-19T09:22:46Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r266792423
Path: packages/components/src/base-control/index.js

```diff
@@ -16,4 +16,14 @@ function BaseControl( { id, label, help, className, children } ) {
 	);
 }
 
+BaseControl.VisualLabel = ( { className, children } ) => {
+	return (
+		<div className={
+			classnames( 'components-base-control-visual-label', className )
```

Hi @aduth, thank you for the suggestion I am now using the class `components-base-control__label`.

> If the original idea was that BaseControl could use this visual label in the default implementation, should that be done here? In other words, I don't see a reason why we need multiple styles selectors to cover both .components-base-control__label and .components-base-control-visual-label, when it should be the same (replace <span className="components-base-control__label"> above with <BaseControl.VisualLabel>

I'm not sure If I understood correctly. Should we use BaseControl.VisualLabel inside BaseControl itself to replace `<span className="components-base-control__label">` ? Or the current code is correct and we just need to use the same class?

#### aduth on 2019-03-19T13:25:19Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r266883309
Path: packages/components/src/base-control/index.js

```diff
@@ -16,4 +16,14 @@ function BaseControl( { id, label, help, className, children } ) {
 	);
 }
 
+BaseControl.VisualLabel = ( { className, children } ) => {
+	return (
+		<div className={
+			classnames( 'components-base-control-visual-label', className )
```

>Should we use BaseControl.VisualLabel inside BaseControl itself to replace <span className="components-base-control__label"> ? Or the current code is correct and we just need to use the same class?

The first is what I'd imagined. Though, in retrospect, I suppose the idea is that we'd soon remove that `span` altogether, preferring instead that `VisualLabel` be used? I guess the argument could be made that it must be kept for compatibility (at least to not render a `label`, the worst case). I'd think if we keep the `span`, it probably makes sense to render `VisualLabel` in its place, since they effectively represent the same thing. Would you agree?

#### jorgefilipecosta on 2019-03-22T09:49:23Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r268098847
Path: packages/components/src/base-control/index.js

```diff
@@ -16,4 +16,14 @@ function BaseControl( { id, label, help, className, children } ) {
 	);
 }
 
+BaseControl.VisualLabel = ( { className, children } ) => {
+	return (
+		<div className={
+			classnames( 'components-base-control-visual-label', className )
```

Hi @aduth, I updated the code to follow your feedback. Let me know if I understood everything correctly, or if missed something. Thank you for the feedback.

### Review thread 5

#### aduth on 2019-03-18T20:37:12Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r266630948
Path: packages/components/src/base-control/index.js

```diff
@@ -16,4 +16,14 @@ function BaseControl( { id, label, help, className, children } ) {
 	);
 }
 
+BaseControl.VisualLabel = ( { className, children } ) => {
+	return (
+		<div className={
+			classnames( 'components-base-control-visual-label', className )
+		}>
```

Minor: We don't have much guideline around JSX formatting, but in my experience I tend to see mostly one of either:

```jsx
<div className="its-a-single-line">
```

Or:

```jsx
<div
	className="its-multiple-lines"
>
```

Where if in the multi-line, the value of the attribute spanned multiple lines, it would look more like:

```jsx
<div
	className={
		'its-even-more-lines'
	}
>
```

In this case, I think all of it could be avoided by just assigning the result of `classnames` into a variable, depending on our level of comfort with overriding the value of the `className` prop:

```jsx
className = classnames( 'components-base-control-visual-label', className );

return (
	<div className={ className }>
```

I don't feel very strongly about any of this, though I've thought on a few occasions whether it's worth starting a discussion on more rigid conventions around JSX formatting.

#### jorgefilipecosta on 2019-03-19T09:19:38Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r266791220
Path: packages/components/src/base-control/index.js

```diff
@@ -16,4 +16,14 @@ function BaseControl( { id, label, help, className, children } ) {
 	);
 }
 
+BaseControl.VisualLabel = ( { className, children } ) => {
+	return (
+		<div className={
+			classnames( 'components-base-control-visual-label', className )
+		}>
```

Hi @aduth,
I agree maybe we can analyse a stricter JSX formatting rule.
Although we normally prefer this formats:
```
<div className="its-a-single-line">
```
```
<div
	className="its-multiple-lines"
>
````

The other format has an advantage regarding internacionalization -- I think it is the one that guarantees translator comments correctly reference the string.
```
<MyComponent
	mySpecialLabel={
		// the translator comment
		__( 'my special label' )
	}
>
```

I noticed it is even safer than using variables before e.g:
```
// the translator comment
const mySpecialLabel = __( 'my special label' )
````

Because during the transpiling the variable declarations may be reordered/set in a different way.


But in this case, the usage of a variable is the one that makes more sense. The code was updated.

### Review thread 6 (resolved)

#### aduth on 2019-03-27T21:29:34Z

URL: https://github.com/WordPress/gutenberg/pull/14179#discussion_r269778501
Path: packages/components/CHANGELOG.md

```diff
@@ -1,3 +1,9 @@
+## V.V.V (Unrealeased)
```

It's on you to assign the version, depending on the type of change. It's meant to make the publisher's job easier.

https://github.com/WordPress/gutenberg/blob/master/packages/README.md#maintaining-changelogs

Since it's a new feature, I'd suggest:

```suggestion
## 7.3.0 (Unreleased)
```

## Changed files

- packages/block-library/src/video/edit.js (MODIFIED, +3/-1)
- packages/components/CHANGELOG.md (MODIFIED, +2/-1)
- packages/components/src/base-control/README.md (MODIFIED, +41/-0)
- packages/components/src/base-control/index.js (MODIFIED, +10/-1)
