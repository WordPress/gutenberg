---
number: 48440
type: pull_request
state: MERGED
merged_at: 2023-09-07T12:19:55Z
url: https://github.com/WordPress/gutenberg/pull/48440
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","has dev note"]
---

# PR #48440: ToolTip: refactor using ariakit

- URL: https://github.com/WordPress/gutenberg/pull/48440
- Author: brookewp
- Created: 2023-02-25T08:12:56Z
- Updated: 2023-10-12T21:23:59Z
- Comments: 28 of 28
- Review threads: 46 of 46
- Changed files: 23 of 23
- Diff: ../pr-diffs/48440.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Closes  #48222
Fixes #43129

The goal for this PR is to refactor our current tooltip using Ariakit's tooltip without changing the existing API. 
## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

After multiple issues arose, we assessed our current 'legacy' Tooltip and decided it would be better to replace it with a new version, using Ariakit. The legacy tooltip has a complex way of handling events and has not been migrated to Typescript. The changes in this PR uses Ariakit's Tooltip with modifications, to create a typed component with simplified implementation details and improved accessibility. 

More details can be found in the tracking issue: https://github.com/WordPress/gutenberg/issues/48222
## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

By using Ariakit's tooltip and modifications to match the API and behaviors of our legacy tooltip. In the testing instructions below, details on each of the changes are outlined with testing steps. 

<details>
<summary><h2>Testing Instructions</h2></summary>

`npm run test:unit packages/components/src/tooltip/test/index.tsx`

Unit tests should match legacy and test for the same behaviors (+ one new test for Esc feature which is mentioned later in the testing steps).

Six other tests failed after replacing tooltip. Two were resolved outside of this PR (#53703 and #53704). The remaining four had to be resolved within this PR, as the failures are tied to the changes. These started to fail mainly because tests were checking `not.toBeInTheDocument`, but this version of tooltip remains in the DOM when it is hidden. This is in the case of these tests: 

`npm run test:unit packages/components/src/button/test/index.tsx`
`npm run test:unit packages/components/src/tab-panel/test/index.tsx`

The tests for `ToggleControlGroup` called a helper function which references a wrapper's CSS class that no longer wraps tooltip. Also, there is a custom tooltip component `WithTooltip` added with its own types, which needed to be updated as well. 

`npm run test:unit packages/components/src/toggle-group-control/test/index.tsx`

In `PostSavedState`, the test query wasn't specific enough, so multiple elements were being found when only one was expected: 

`npm run test:unit packages/editor/src/components/post-saved-state/test/index.js`


### Existing behavior: 
The following behaviors are default in both versions of tooltip:

####  Only accepts one child component 

This is an expected existing behavior, but [there is a bug when the only child is an Icon component]( https://github.com/WordPress/gutenberg/issues/43129). The new tooltip fixes this issue and prevent the workarounds from breaking upon replacement. An example of this workaround can be found in `packages/edit-site/src/components/sidebar-navigation-screen-patterns/index.js` which can be tested in the editor: 

1. Navigate to the site editor (Appearance > Editor)
2. Click on 'Patterns'
3. Hover lock icon to see tooltip 

We can also test to make sure the workaround isn't needed by removing the `span` wrapper. With the legacy tooltip, the anchor won't render and there will be a warning that looks like this in the console:

```
Warning: Function components cannot be given refs. Attempts to access this ref will fail. Did you mean to use React.forwardRef()?

Check the render method of Tooltip. 
```
In the new version, it will still warn, but the lock icon will render and the tooltip will behave as expected. 

####  If the tooltip overflows, the position should be flipped. 

<img width="162" alt="Screenshot 2023-08-11 at 8 41 31 PM" src="https://github.com/WordPress/gutenberg/assets/35543432/f2af8d23-59e5-484c-bbe3-2848813c062f">

1. In the editor sidebar, hover over the close icon 'X' to see tooltip 'Close Settings ⇧⌘,'. 
2. Although it's position is set to be centered, the position will be adjusted to not overflow the document

### Workarounds 

A few behaviors differ between the tooltip components. Therefore, modifications have been made to match the legacy. 

#### Leaving the document hides the tooltip 

<img src="https://github.com/WordPress/gutenberg/assets/35543432/b268bac8-a4e3-4119-aeca-7b32772f7c64" width="350"/>
<br>
<br>
The intended default behavior in ariakit (<a href="https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1654483851">context on why here</a>) is to keep the tooltip visible when focus leaves the anchor to the document, but not in our legacy. To amend this, an `onBlur` has been added to the anchor. This scenario is covered in our unit tests but it can be manually tested: 
<br>
<br>
<ol>
<li>It is quickest to test this by viewing a story in isolation (<a href="http://localhost:50240/iframe.html?args=&id=components-tooltip--keyboard-shortcut&viewMode=story">example</a>)</li>
<li>Then tab between the anchor and the document </li>
<li>The tooltip should disappear when the focus leaves the anchor </li>
</ol>

#### Clicking on the anchor should hide the tooltip 

<img src="https://github.com/WordPress/gutenberg/assets/35543432/e0840e85-9335-44cd-a2ff-1c867497c427" width="350"/>

1. Focus or hover the anchor to make tooltip visible
5. Click on the anchor
6. Tooltip should disappear upon mouse click 

### New features 

The only 'new' feature is the tooltip can now be closed by using the 'Esc' key, which is in [ARIA guidelines for Tooltip](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/). It can be disabled, but I think it is okay to add it in this first iteration as long as it doesn't break any existing features that use the Escape key. For example, Modal works the same as before, with Escape closing the modal rather than tooltip. I've also added a test for this case, but if there are other cases where this would be a problem, please share! 

#### Tooltip should close on Esc keydown

1. Focus or hover the anchor to make tooltip visible
2. Use 'Esc' key to close the tooltip 

As mentioned, there is a unit test for modal, but this can be tested manually as well: 

1. Navigate to the Modal component in Storybook 
2. Click button 'Open Modal'
4. Hover over the close button 'X' to show the tooltip
7. Use 'Esc' while tooltip is showing 
8. Modal should close

### Testing Instructions for Keyboard

Most of the above steps include keyboard testing but these are the main things to look for when using a keyboard: 

1. `npm run storybook:dev`
2. Navigate to Tooltip
4. Click `tab` key to focus the anchor element (Button) and see a tooltip appear
5. Click `Esc` key to close the tooltip 

</details>

## ✍️  Dev note

The `Tooltip` and the `TabPanel` components have been completely rewritten to leverage third-party, headless libraries (specifically `ariakit`) as the first tasks of a new experimental effort within the package.

Both migrations were intentionally designed to avoid changes to the API or developer experience, while also bringing several benefits:
- better compliance with semantics, accessibility requirements, and related WAI-ARIA patterns;
- better user experience thanks to using a widely used, well-tested underlying implementation;
- better types and improved unit tests;
- less maintenance required in the long term.

Specifically to the `Tooltip` component, the refactor also fixed [a long-standing issue](https://github.com/WordPress/gutenberg/issues/43129) and presented the opportunity to align with other components already using a new `placement` prop in lieu of the legacy, now deprecated `position` prop.

For `TabPanel`, the only noteworthy change is that `tabpanel` elements now get a tabstop. This means that when focused on a `tab`, pressing the `[Tab]` key will apply focus to the `tabpanel` itself, rather than jumping directly to the next focusable element within the `tabpanel` element.

## Issue comments

### github-actions on 2023-03-22T01:58:50Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1478816772

**Size Change:** +7.55 kB (0%) 

**Total Size:** 1.52 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/components/index.min.js` | 255 kB | +7.68 kB (+3%) |
| `build/components/style-rtl.css` | 11.7 kB | -69 B (-1%) |
| `build/components/style.css` | 11.7 kB | -68 B (-1%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 955 B |
| `build/annotations/index.min.js` | 2.69 kB |
| `build/api-fetch/index.min.js` | 2.28 kB |
| `build/autop/index.min.js` | 2.1 kB |
| `build/blob/index.min.js` | 451 B |
| `build/block-directory/index.min.js` | 7.01 kB |
| `build/block-directory/style-rtl.css` | 1.02 kB |
| `build/block-directory/style.css` | 1.02 kB |
| `build/block-editor/content-rtl.css` | 4.25 kB |
| `build/block-editor/content.css` | 4.24 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 381 B |
| `build/block-editor/default-editor-styles.css` | 381 B |
| `build/block-editor/index.min.js` | 215 kB |
| `build/block-editor/style-rtl.css` | 15.1 kB |
| `build/block-editor/style.css` | 15.1 kB |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 90 B |
| `build/block-library/blocks/archives/style.css` | 90 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 122 B |
| `build/block-library/blocks/audio/style.css` | 122 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 126 B |
| `build/block-library/blocks/audio/theme.css` | 126 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 104 B |
| `build/block-library/blocks/avatar/style.css` | 104 B |
| `build/block-library/blocks/block/editor-rtl.css` | 305 B |
| `build/block-library/blocks/block/editor.css` | 305 B |
| `build/block-library/blocks/button/editor-rtl.css` | 584 B |
| `build/block-library/blocks/button/editor.css` | 582 B |
| `build/block-library/blocks/button/style-rtl.css` | 629 B |
| `build/block-library/blocks/button/style.css` | 628 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 337 B |
| `build/block-library/blocks/buttons/editor.css` | 337 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 332 B |
| `build/block-library/blocks/buttons/style.css` | 332 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 239 B |
| `build/block-library/blocks/calendar/style.css` | 239 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 113 B |
| `build/block-library/blocks/categories/editor.css` | 112 B |
| `build/block-library/blocks/categories/style-rtl.css` | 124 B |
| `build/block-library/blocks/categories/style.css` | 124 B |
| `build/block-library/blocks/code/editor-rtl.css` | 53 B |
| `build/block-library/blocks/code/editor.css` | 53 B |
| `build/block-library/blocks/code/style-rtl.css` | 121 B |
| `build/block-library/blocks/code/style.css` | 121 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 421 B |
| `build/block-library/blocks/columns/style.css` | 421 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 199 B |
| `build/block-library/blocks/comment-template/style.css` | 198 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 840 B |
| `build/block-library/blocks/comments/editor.css` | 839 B |
| `build/block-library/blocks/comments/style-rtl.css` | 637 B |
| `build/block-library/blocks/comments/style.css` | 636 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 647 B |
| `build/block-library/blocks/cover/editor.css` | 650 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.63 kB |
| `build/block-library/blocks/cover/style.css` | 1.62 kB |
| `build/block-library/blocks/details/editor-rtl.css` | 65 B |
| `build/block-library/blocks/details/editor.css` | 65 B |
| `build/block-library/blocks/details/style-rtl.css` | 98 B |
| `build/block-library/blocks/details/style.css` | 98 B |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 410 B |
| `build/block-library/blocks/embed/style.css` | 410 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 126 B |
| `build/block-library/blocks/embed/theme.css` | 126 B |
| `build/block-library/blocks/file/editor-rtl.css` | 316 B |
| `build/block-library/blocks/file/editor.css` | 316 B |
| `build/block-library/blocks/file/style-rtl.css` | 280 B |
| `build/block-library/blocks/file/style.css` | 281 B |
| `build/block-library/blocks/file/view-interactivity.min.js` | 317 B |
| `build/block-library/blocks/file/view.min.js` | 375 B |
| `build/block-library/blocks/footnotes/style-rtl.css` | 201 B |
| `build/block-library/blocks/footnotes/style.css` | 199 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.61 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.61 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 947 B |
| `build/block-library/blocks/gallery/editor.css` | 952 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.53 kB |
| `build/block-library/blocks/gallery/style.css` | 1.53 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 108 B |
| `build/block-library/blocks/gallery/theme.css` | 108 B |
| `build/block-library/blocks/group/editor-rtl.css` | 654 B |
| `build/block-library/blocks/group/editor.css` | 654 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 76 B |
| `build/block-library/blocks/heading/style.css` | 76 B |
| `build/block-library/blocks/html/editor-rtl.css` | 336 B |
| `build/block-library/blocks/html/editor.css` | 337 B |
| `build/block-library/blocks/image/editor-rtl.css` | 834 B |
| `build/block-library/blocks/image/editor.css` | 833 B |
| `build/block-library/blocks/image/style-rtl.css` | 1.42 kB |
| `build/block-library/blocks/image/style.css` | 1.41 kB |
| `build/block-library/blocks/image/theme-rtl.css` | 126 B |
| `build/block-library/blocks/image/theme.css` | 126 B |
| `build/block-library/blocks/image/view-interactivity.min.js` | 1.83 kB |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 357 B |
| `build/block-library/blocks/latest-comments/style.css` | 357 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 213 B |
| `build/block-library/blocks/latest-posts/editor.css` | 212 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 478 B |
| `build/block-library/blocks/latest-posts/style.css` | 478 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 505 B |
| `build/block-library/blocks/media-text/style.css` | 503 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 712 B |
| `build/block-library/blocks/navigation-link/editor.css` | 711 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 296 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 295 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.26 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.26 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 2.23 kB |
| `build/block-library/blocks/navigation/style.css` | 2.22 kB |
| `build/block-library/blocks/navigation/view-interactivity.min.js` | 988 B |
| `build/block-library/blocks/navigation/view-modal.min.js` | 2.85 kB |
| `build/block-library/blocks/navigation/view.min.js` | 469 B |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 401 B |
| `build/block-library/blocks/page-list/editor.css` | 401 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 235 B |
| `build/block-library/blocks/paragraph/editor.css` | 235 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 335 B |
| `build/block-library/blocks/paragraph/style.css` | 335 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 96 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 96 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 508 B |
| `build/block-library/blocks/post-comments-form/style.css` | 508 B |
| `build/block-library/blocks/post-date/style-rtl.css` | 61 B |
| `build/block-library/blocks/post-date/style.css` | 61 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 71 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 71 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 141 B |
| `build/block-library/blocks/post-excerpt/style.css` | 141 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 588 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 586 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 319 B |
| `build/block-library/blocks/post-featured-image/style.css` | 319 B |
| `build/block-library/blocks/post-navigation-link/style-rtl.css` | 215 B |
| `build/block-library/blocks/post-navigation-link/style.css` | 214 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 314 B |
| `build/block-library/blocks/post-template/style.css` | 314 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 96 B |
| `build/block-library/blocks/post-terms/style.css` | 96 B |
| `build/block-library/blocks/post-time-to-read/style-rtl.css` | 69 B |
| `build/block-library/blocks/post-time-to-read/style.css` | 69 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 100 B |
| `build/block-library/blocks/post-title/style.css` | 100 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 125 B |
| `build/block-library/blocks/preformatted/style.css` | 125 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 135 B |
| `build/block-library/blocks/pullquote/editor.css` | 135 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 335 B |
| `build/block-library/blocks/pullquote/style.css` | 335 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 168 B |
| `build/block-library/blocks/pullquote/theme.css` | 168 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 302 B |
| `build/block-library/blocks/query-pagination/style.css` | 299 B |
| `build/block-library/blocks/query-title/style-rtl.css` | 63 B |
| `build/block-library/blocks/query-title/style.css` | 63 B |
| `build/block-library/blocks/query/editor-rtl.css` | 450 B |
| `build/block-library/blocks/query/editor.css` | 449 B |
| `build/block-library/blocks/query/style-rtl.css` | 370 B |
| `build/block-library/blocks/query/style.css` | 368 B |
| `build/block-library/blocks/query/view.min.js` | 559 B |
| `build/block-library/blocks/quote/style-rtl.css` | 222 B |
| `build/block-library/blocks/quote/style.css` | 222 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 149 B |
| `build/block-library/blocks/rss/editor.css` | 149 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 178 B |
| `build/block-library/blocks/search/editor.css` | 178 B |
| `build/block-library/blocks/search/style-rtl.css` | 607 B |
| `build/block-library/blocks/search/style.css` | 607 B |
| `build/block-library/blocks/search/theme-rtl.css` | 114 B |
| `build/block-library/blocks/search/theme.css` | 114 B |
| `build/block-library/blocks/search/view.min.js` | 631 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 234 B |
| `build/block-library/blocks/separator/style.css` | 234 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 323 B |
| `build/block-library/blocks/shortcode/editor.css` | 323 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 754 B |
| `build/block-library/blocks/site-logo/editor.css` | 754 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 204 B |
| `build/block-library/blocks/site-logo/style.css` | 204 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 116 B |
| `build/block-library/blocks/site-title/editor.css` | 116 B |
| `build/block-library/blocks/site-title/style-rtl.css` | 57 B |
| `build/block-library/blocks/site-title/style.css` | 57 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 184 B |
| `build/block-library/blocks/social-link/editor.css` | 184 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 682 B |
| `build/block-library/blocks/social-links/editor.css` | 681 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.44 kB |
| `build/block-library/blocks/social-links/style.css` | 1.43 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 348 B |
| `build/block-library/blocks/spacer/editor.css` | 348 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 432 B |
| `build/block-library/blocks/table/editor.css` | 432 B |
| `build/block-library/blocks/table/style-rtl.css` | 639 B |
| `build/block-library/blocks/table/style.css` | 639 B |
| `build/block-library/blocks/table/theme-rtl.css` | 146 B |
| `build/block-library/blocks/table/theme.css` | 146 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 251 B |
| `build/block-library/blocks/tag-cloud/style.css` | 253 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 403 B |
| `build/block-library/blocks/template-part/editor.css` | 403 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/term-description/style-rtl.css` | 111 B |
| `build/block-library/blocks/term-description/style.css` | 111 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 99 B |
| `build/block-library/blocks/verse/style.css` | 99 B |
| `build/block-library/blocks/video/editor-rtl.css` | 552 B |
| `build/block-library/blocks/video/editor.css` | 555 B |
| `build/block-library/blocks/video/style-rtl.css` | 185 B |
| `build/block-library/blocks/video/style.css` | 185 B |
| `build/block-library/blocks/video/theme-rtl.css` | 126 B |
| `build/block-library/blocks/video/theme.css` | 126 B |
| `build/block-library/classic-rtl.css` | 179 B |
| `build/block-library/classic.css` | 179 B |
| `build/block-library/common-rtl.css` | 1.1 kB |
| `build/block-library/common.css` | 1.1 kB |
| `build/block-library/editor-elements-rtl.css` | 75 B |
| `build/block-library/editor-elements.css` | 75 B |
| `build/block-library/editor-rtl.css` | 12.2 kB |
| `build/block-library/editor.css` | 12.1 kB |
| `build/block-library/elements-rtl.css` | 54 B |
| `build/block-library/elements.css` | 54 B |
| `build/block-library/index.min.js` | 204 kB |
| `build/block-library/reset-rtl.css` | 478 B |
| `build/block-library/reset.css` | 478 B |
| `build/block-library/style-rtl.css` | 13.8 kB |
| `build/block-library/style.css` | 13.8 kB |
| `build/block-library/theme-rtl.css` | 688 B |
| `build/block-library/theme.css` | 693 B |
| `build/block-serialization-default-parser/index.min.js` | 1.12 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.87 kB |
| `build/blocks/index.min.js` | 51.4 kB |
| `build/commands/index.min.js` | 15.5 kB |
| `build/commands/style-rtl.css` | 932 B |
| `build/commands/style.css` | 929 B |
| `build/compose/index.min.js` | 12.1 kB |
| `build/core-commands/index.min.js` | 2.6 kB |
| `build/core-data/index.min.js` | 16.8 kB |
| `build/customize-widgets/index.min.js` | 12 kB |
| `build/customize-widgets/style-rtl.css` | 1.46 kB |
| `build/customize-widgets/style.css` | 1.45 kB |
| `build/data-controls/index.min.js` | 640 B |
| `build/data/index.min.js` | 8.84 kB |
| `build/date/index.min.js` | 17.8 kB |
| `build/deprecated/index.min.js` | 451 B |
| `build/dom-ready/index.min.js` | 324 B |
| `build/dom/index.min.js` | 4.64 kB |
| `build/edit-post/classic-rtl.css` | 544 B |
| `build/edit-post/classic.css` | 545 B |
| `build/edit-post/index.min.js` | 35.3 kB |
| `build/edit-post/style-rtl.css` | 7.6 kB |
| `build/edit-post/style.css` | 7.59 kB |
| `build/edit-site/index.min.js` | 90.9 kB |
| `build/edit-site/style-rtl.css` | 13.2 kB |
| `build/edit-site/style.css` | 13.2 kB |
| `build/edit-widgets/index.min.js` | 16.9 kB |
| `build/edit-widgets/style-rtl.css` | 4.52 kB |
| `build/edit-widgets/style.css` | 4.52 kB |
| `build/editor/index.min.js` | 45.5 kB |
| `build/editor/style-rtl.css` | 3.53 kB |
| `build/editor/style.css` | 3.52 kB |
| `build/element/index.min.js` | 4.82 kB |
| `build/escape-html/index.min.js` | 537 B |
| `build/format-library/index.min.js` | 7.71 kB |
| `build/format-library/style-rtl.css` | 554 B |
| `build/format-library/style.css` | 553 B |
| `build/hooks/index.min.js` | 1.55 kB |
| `build/html-entities/index.min.js` | 448 B |
| `build/i18n/index.min.js` | 3.58 kB |
| `build/interactivity/index.min.js` | 11.3 kB |
| `build/is-shallow-equal/index.min.js` | 527 B |
| `build/keyboard-shortcuts/index.min.js` | 1.72 kB |
| `build/keycodes/index.min.js` | 1.87 kB |
| `build/list-reusable-blocks/index.min.js` | 2.2 kB |
| `build/list-reusable-blocks/style-rtl.css` | 836 B |
| `build/list-reusable-blocks/style.css` | 836 B |
| `build/media-utils/index.min.js` | 2.9 kB |
| `build/notices/index.min.js` | 948 B |
| `build/nux/index.min.js` | 1.99 kB |
| `build/nux/style-rtl.css` | 735 B |
| `build/nux/style.css` | 732 B |
| `build/patterns/index.min.js` | 2.71 kB |
| `build/patterns/style-rtl.css` | 240 B |
| `build/patterns/style.css` | 240 B |
| `build/plugins/index.min.js` | 1.79 kB |
| `build/preferences-persistence/index.min.js` | 1.84 kB |
| `build/preferences/index.min.js` | 1.24 kB |
| `build/primitives/index.min.js` | 943 B |
| `build/priority-queue/index.min.js` | 1.52 kB |
| `build/private-apis/index.min.js` | 958 B |
| `build/react-i18n/index.min.js` | 615 B |
| `build/react-refresh-entry/index.min.js` | 9.47 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.7 kB |
| `build/reusable-blocks/index.min.js` | 2.7 kB |
| `build/reusable-blocks/style-rtl.css` | 243 B |
| `build/reusable-blocks/style.css` | 243 B |
| `build/rich-text/index.min.js` | 11 kB |
| `build/router/index.min.js` | 1.78 kB |
| `build/server-side-render/index.min.js` | 1.94 kB |
| `build/shortcode/index.min.js` | 1.39 kB |
| `build/style-engine/index.min.js` | 1.85 kB |
| `build/sync/index.min.js` | 53.8 kB |
| `build/token-list/index.min.js` | 582 B |
| `build/url/index.min.js` | 3.73 kB |
| `build/vendors/inert-polyfill.min.js` | 2.48 kB |
| `build/vendors/react-dom.min.js` | 41.8 kB |
| `build/vendors/react.min.js` | 4.02 kB |
| `build/viewport/index.min.js` | 958 B |
| `build/warning/index.min.js` | 249 B |
| `build/widgets/index.min.js` | 7.16 kB |
| `build/widgets/style-rtl.css` | 1.15 kB |
| `build/widgets/style.css` | 1.16 kB |
| `build/wordcount/index.min.js` | 1.02 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2023-07-08T01:44:52Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1626419134

<!-- flaky-tests-report-comment -->
**Flaky tests detected in ef7d83716f8c5f62db27c73a03a2358deef486fc.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/5898187692
📝  Reported issues:
- #53524 in `/test/e2e/specs/editor/various/multi-block-selection.spec.js`

### brookewp on 2023-07-26T22:49:12Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1652644536

Hey @tyxla! I requested a review from you because of some strange changes in the unit tests since I returned to this PR. The significant change was updating the branch to the updated `@ariakit/react` instead of `ariakit`. Since then, unit tests that were passing are now failing. 

I found that only one test failed on its own and I've added [an inline comment here](https://github.com/WordPress/gutenberg/pull/48440/files#r1275548600) on what I found so far. 

For the remaining three failing tests, they all pass on their own. They also pass when the previous test is skipped. So it seems to be a weird side effect. I thought maybe it was an issue with async/await, mutation, or timers/mocks not being reset, but I didn't find any solutions from going down those paths. 

The only solution I found was if I moved the tests to before the first failing test. This is the test they needed to be moved before: https://github.com/WordPress/gutenberg/blob/9182f1ef034fd03674238a72fe4bee75b32fba0a/packages/components/src/ui/ariakit-tooltip/test/index.tsx#L198

And three tests were moved, beginning here: 

https://github.com/WordPress/gutenberg/blob/9182f1ef034fd03674238a72fe4bee75b32fba0a/packages/components/src/ui/ariakit-tooltip/test/index.tsx#L93

So technically, all unit tests pass now but it's a bit confusing. If you have any insights, I'd appreciate it!

### tyxla on 2023-07-27T12:38:15Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1653539531

From what I've seen in my initial look, it appears that the first failing test expects that after tabbing, the tooltip will be hidden. However, in my testing with the Storybook instance, it appears that it doesn't. So the test is actually accurately failing, because we don't seem to properly be processing the blur event. So I'd start from investigating what changed between `ariakit` and `@ariakit/react` in terms of tabbing away from the tooltip anchor and the disappearance of the tooltip in that case. Haven't checked it myself, but wanted to point that out as a first step towards addressing this.

### tyxla on 2023-07-27T12:40:40Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1653543298

For the protocol, when testing the ariakit `Tooltip` here: https://ariakit.org/components/tooltip#api, it correctly hides the tooltip upon tabbing. So it would be a safe bet to say we're likely not utilizing the component API properly somehow. Let me know what you think.

### brookewp on 2023-07-27T18:29:17Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1654193872

>For the protocol, when testing the ariakit Tooltip here: https://ariakit.org/components/tooltip#api, it correctly hides the tooltip upon tabbing. So it would be a safe bet to say we're likely not utilizing the component API properly somehow. 

Thanks for looking, @tyxla! Could you let me know if you tested by tabbing on the page you shared above? It's interesting because I tested on the 'docs' page in Storybook and couldn't replicate the failures. But now I see the issue in the 'Canvas' view or when opening it in isolation. 

And I found the same result in ariakit. It works on the docs page you shared, but when opening the preview where it's in isolation, the tooltip is not disappearing on tab: https://ariakit.org/previews/tooltip 🤔

### diegohaz on 2023-07-27T18:57:42Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1654250726

> And I found the same result in ariakit. It works on the docs page you shared, but when opening the preview where it's in isolation, the tooltip is not disappearing on tab: https://ariakit.org/previews/tooltip 🤔

That behavior is intended. The tooltip is not dismissed when the focus leaves the document. But that's not new, as far as I can tell. It's always worked like that.

Also, unit tests might be failing for this same reason: https://github.com/WordPress/gutenberg/pull/52133#issuecomment-1613691258

### brookewp on 2023-07-27T19:37:22Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1654427469

>That behavior is intended. The tooltip is not dismissed when the focus leaves the document. 

Thanks for sharing, @diegohaz! I'm curious about why that is the intended behavior. The [WAI-ARIA pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/) linked in the tooltip docs states the opposite for elements `onBlur`:

>If the tooltip is invoked when the trigger element receives focus, then it is dismissed when it no longer has focus (onBlur).

Is there an a11y reason why the behavior would be different when the focus leaves the document rather than an element? 

---

>Also, unit tests might be failing for this same reason: https://github.com/WordPress/gutenberg/pull/52133#issuecomment-1613691258

Thank you! I thought the same after speaking with @chad1008. However, I might not have arrived at the same solution. Could that explain why the unit tests pass when they are run in a different order?

### diegohaz on 2023-07-27T20:07:11Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1654483851

> > That behavior is intended. The tooltip is not dismissed when the focus leaves the document.
> 
> Thanks for sharing, @diegohaz! I'm curious about why that is the intended behavior. The [WAI-ARIA pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/) linked in the tooltip docs states the opposite for elements `onBlur`:
> 
> > If the tooltip is invoked when the trigger element receives focus, then it is dismissed when it no longer has focus (onBlur).
> 
> Is there an a11y reason why the behavior would be different when the focus leaves the document rather than an element?

The Authoring Practices Guide (APG) is a set of recommendations, not a spec. Ariakit follows it as a base but may do some things differently when it makes sense.

Dismissing tooltips (and other popups) when the document loses focus is problematic. For example, I use a third-party app to take screenshots on my Mac. Clicking on that app's "Take screenshot" button will blur the web document. If the tooltip is dismissed, I can't include it in the screenshot. Other apps like third-party magnifiers can also be affected by this.

If you do want to hide the tooltip, though, you can probably do so by calling `tooltipStore.hide()` on the window's blur event.

> > Also, unit tests might be failing for this same reason: [#52133 (comment)](https://github.com/WordPress/gutenberg/pull/52133#issuecomment-1613691258)
> 
> Thank you! I thought the same after speaking with @chad1008. However, I might not have arrived at the same solution. Could that explain why the unit tests pass when they are run in a different order?

I believe that could be a reason, yes.

### brookewp on 2023-07-27T21:45:45Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1654627028

>The Authoring Practices Guide (APG) is a set of recommendations, not a spec. Ariakit follows it as a base but may do some things differently when it makes sense.

>Dismissing tooltips (and other popups) when the document loses focus is problematic. For example, I use a third-party app to take screenshots on my Mac. Clicking on that app's "Take screenshot" button will blur the web document. If the tooltip is dismissed, I can't include it in the screenshot. Other apps like third-party magnifiers can also be affected by this.

Thanks for the info and example, much appreciated!

### tyxla on 2023-07-28T07:47:42Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1655210821

Thanks for clarifying @diegohaz! The idea to keep the focus when leaving the document does make sense in many contexts. 

@brookewp, in that case, to workaround the issue, you can render another focusable element after the tooltip anchor, and that way when tabbing, it won't tab away from the document, but will rather tab into the next element, and that will hide the tooltip.

Alternatively, we could add a change to specifically trigger a blur event when leaving the document, but I'd likely prefer to let that behavior be inherited from ariakit.

### brookewp on 2023-07-28T16:45:55Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1655993460

After learning it was the intended behavior for Ariakit's tooltip, I ended up hiding the tooltip `onBlur` of the anchor: https://github.com/WordPress/gutenberg/pull/48440/commits/15dfbbd5a7514ac303dc0cdafa08428e86748788

This is to match the legacy behavior in our current tooltip since our goal is to match the existing API so we can seamlessly replace it when this one is ready. 

In a follow-up/v2, we can see if it makes sense to keep this behavior or not. There will be a list of other features to consider then as well!

### brookewp on 2023-08-18T03:26:06Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1683291503

Just a note: there's one test failing still which should be resolved when #53704 is merged

### brookewp on 2023-09-02T02:24:27Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1703650438

Thanks, @chad1008 for the review! 

>I am still seeing the bug the legacy ToolTip had when the only child is an Icon component. When testing your example in the site editor if I remove the span wrapper around the icon I get the JS error and the icon doesn't render for me. Not sure if that's just something weird happening for me locally?

It's not just you, so good find! I've updated this, and now I'm seeing the Icon as described in my original testing instructions. Would you mind testing again to confirm this?

### brookewp on 2023-09-07T04:21:30Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1709453316

Thanks for the review @chad1008! 

>There is still one failing CI check, not sure why, but other than that I say ship it! 🚢 🚀

The test `packages/block-editor/src/components/link-control/test/index.js` started failing after the last merge of trunk. There are two failing tests with the error `TypeError: resolver is not a function`:

- **Basic rendering › should have aria-selected attribute only on the highlighted item**
- **Searching for a link › should display loading UI when input is valid but search results have yet to be returned**

These tests are the only ones that query for the role `presentation`, for the spinner. This new tooltip has a wrapper with the role `presentation`, so I thought this was causing the issue. I tried specifying the query, thinking it would solve it, but that wasn't the case. 

Since @jsnajdr has been CCed for tooltip tests, maybe we could add link control to the list? 😅

### ciampo on 2023-09-07T10:13:39Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1709886907

The problem seems a side-effect caused by #52620:

- In that PR, a `Button` is added to `LinkControl`
- For some reason, it looks like that `Button` shows a tooltip
- The tooltip is also wrapped in a div with `role="presentation"`, which is always rendered in the page (even when the tooltip is hidden)
- The failing unit tests are asserting the presence of the spinner by looking for the `presentation` role, but now that a tooltip is rendered, the test mistakingly gets the tooltip instead of the spinner, which causes the remaining assumptions in the test (ie. a search query is performed) to fail

To confirm my assumption, I've added temporarily a `data-testid` attribute to the `Spinner` component, so that we can query `byTestId` and make sure that we're targeting the spinner (see https://github.com/WordPress/gutenberg/pull/48440/commits/5761d7921f78c79f856630d2c81d249036040de1)

A better fix would be to somehow differentiate the spinner from the tooltip wrapper without using test-id (@diegohaz @jsnajdr @tyxla do you have any ideas?)

### jsnajdr on 2023-09-07T10:47:36Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1709931561

One easy fix would be to look for the spinner not globally (`screen.findByRole( 'presentation' )`) but scope the search to some element related to the search input.

Another idea is to give the spinner a more specific role. There is a [StackOverflow question](https://stackoverflow.com/questions/38704467/how-to-label-a-loading-animation-for-wai-aria) that offers many sensible alternatives -- `role="status"`, `role="progressbar"`, `aria-busy="true"`, ...

### ciampo on 2023-09-07T12:19:51Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1710052027

Thank you @tyxla and @jsnajdr for your ideas!

> Happy to see it in a follow-up PR!

I also personally agree with merging this PR and working on improving the fix in a separate follow-up (another one to add to the list, Brooke!)

Once again, well done @brookewp for taking this one to the finish line 🎉

### ciampo on 2023-09-07T20:54:11Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1710758645

To recap, "final final" list of follow-ups:

* https://github.com/WordPress/gutenberg/pull/48440/files#r1303311736
* deprecate the `position` prop in favour of a new `placement` prop
* delete the other `Tooltip` component currently living in the `ui` folder, and migrate any usages to this ariakit component
* improving the `LinkControl` unit test fix (see https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1709931561 and https://github.com/WordPress/gutenberg/pull/48440#pullrequestreview-1615162434)
* Remove the workaround in `packages/edit-site/src/components/sidebar-navigation-screen-patterns/index.js` which is not necessary anymore to display the icon correctly

### youknowriad on 2023-09-08T09:36:56Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711374818

Looks like this PR had a somewhat important (not in a good sense) impact on some performance metrics (block selection, inserter opening). Would be cool if we could investigate the reasons and address them.

### ciampo on 2023-09-08T11:07:41Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711493087

> Looks like this PR had a somewhat important (not in a good sense) impact on some performance metrics (block selection, inserter opening). Would be cool if we could investigate the reasons and address them.

Absolutely, I was about to reach out to you to ask for some help in how to investigate the regressions, since I've never had to do it before.

Was the Performance CI check supposed to flag this before merging?

### tyxla on 2023-09-08T11:18:10Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711505464

Haven't dug deeply into this theory, but is it possible that the Ariakit version just deals differently with various timeouts (show timeout, skip timeout, hide timeout), and playing with those timeouts, like reducing them a bit, could have a big positive impact? For example, the default Ariakit timeout is `500` while we set it to `700`.

### ciampo on 2023-09-08T11:42:39Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711538847

I believe that the delay was `700ms` also in the previous version of `Tooltip`.

It is weird that inserting a block got slower with a new tooltip, since tooltips shouldn't be involved in those user actions.

As noticed [previously](https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1709886907), it seems that ariakit renders the tooltip in the DOM at all times, and only toggles its visibility when needed. Maybe this could be affecting metrics?

### youknowriad on 2023-09-08T11:52:22Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711549618

> Was the Performance CI check supposed to flag this before merging?

The performance test run before merging but at the moment doesn't notify about the impact, you have to manually open the job and check the results.

> As noticed https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1709886907, it seems that ariakit renders the tooltip in the DOM at all times, and only toggles its visibility when needed. Maybe this could be affecting metrics?

That seems like a good hint to me. Especially if these components react to changes...

### ciampo on 2023-09-08T12:12:42Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711573715

Also pinging @diegohaz to see if he can help us to look into this

### diegohaz on 2023-09-08T16:10:39Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711913551

Yeah, if the tooltip was dynamically rendered before, and it's always rendered now, this could have an impact.

The Ariakit Tooltip can be dynamically rendered too:

```diff
	const tooltipStore = Ariakit.useTooltipStore( {
		placement: positionToPlacement( position ),
		timeout: delay,
	} );

+	const isTooltipOpen = tooltipStore.useState( 'open' );

	return (
		<>
			<Ariakit.TooltipAnchor
				onBlur={ tooltipStore.hide }
				onClick={ tooltipStore.hide }
				store={ tooltipStore }
				render={ isOnlyChild ? children : undefined }
			>
				{ isOnlyChild ? undefined : children }
			</Ariakit.TooltipAnchor>
-			{ isOnlyChild && ( text || shortcut ) &&
+			{ isOnlyChild && ( text || shortcut ) && isTooltipOpen && (
				<Ariakit.Tooltip
```

### ciampo on 2023-09-08T16:43:33Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1711954968

Thank you for the suggestion @diegohaz , trying this out in #54312

### ciampo on 2023-10-12T21:23:59Z

URL: https://github.com/WordPress/gutenberg/pull/48440#issuecomment-1760384800

Added a dev note in the PR description

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2023-03-17T18:21:21Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1140570772
Path: package-lock.json

👋 

Just popping by in case you don't know why the project is not building correctly and is not able to pick up the `ariakit` dependency that you've added. That's because we're missing a line that says that the components package needs `ariakit` as a dependency

```diff
diff --git a/package-lock.json b/package-lock.json
index ca04477093..0a99a68a32 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -16723,6 +16723,7 @@
 				"@wordpress/private-apis": "file:packages/private-apis",
 				"@wordpress/rich-text": "file:packages/rich-text",
 				"@wordpress/warning": "file:packages/warning",
+				"ariakit": "^2.0.0-next.43",
 				"change-case": "^4.1.2",
 				"classnames": "^2.3.1",
 				"colord": "^2.7.0",


```


Why is that? It's a configuration needed by the Gutenberg repository. This repository is, in fact, what can be referred to as a ["monorepo"](https://monorepo.tools/).

In particular, Gutenberg uses `lerna` to manage the monorepo and its dependencies. To generate the line of code above, all I did was to run `npx lerna bootsrap` (which is a trick I learned through the years — it actually looks like [it will be soon deprecated](https://lerna.js.org/docs/getting-started#package-dependency-management) 😅 )

### Review thread 2

#### brookewp on 2023-03-24T06:17:04Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147159922
Path: packages/components/src/ui/ariakit-tooltip/test/index.tsx

```diff
@@ -0,0 +1,330 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../../button';
+import { ToolTip } from '..';
+import { TOOLTIP_DELAY } from '../../../tooltip/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'ToolTip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		const user = userEvent.setup();
+
+		render(
+			// @ts-expect-error
+			<ToolTip { ...props }>
+				<Button>Button 1</Button>
+				<Button>Button 2</Button>
+			</ToolTip>
+		);
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <ToolTip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.hover( button );
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup( { delay: TOOLTIP_DELAY } );
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <ToolTip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.hover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should show tooltip when an element is aria-disabled', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<ToolTip { ...props }>
+				<Button aria-disabled>Button</Button>
+			</ToolTip>
+		);
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		expect( button ).toBeVisible();
+		//expect( button ).toBeDisabled();
```

In the legacy tooltip, the tooltip will show if an element is disabled. From what I've seen, it shouldn't show unless `aria-disabled` is added instead. In order to be backwards compatible, we may need to add a temporary solution to be deprecated 🤔

### Review thread 3 (resolved)

#### brookewp on 2023-03-24T06:17:40Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147160239
Path: packages/components/src/ui/ariakit-tooltip/test/index.tsx

```diff
@@ -0,0 +1,330 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../../button';
+import { ToolTip } from '..';
+import { TOOLTIP_DELAY } from '../../../tooltip/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'ToolTip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		const user = userEvent.setup();
+
+		render(
+			// @ts-expect-error
+			<ToolTip { ...props }>
+				<Button>Button 1</Button>
+				<Button>Button 2</Button>
+			</ToolTip>
+		);
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <ToolTip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.hover( button );
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup( { delay: TOOLTIP_DELAY } );
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <ToolTip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.hover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should show tooltip when an element is aria-disabled', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<ToolTip { ...props }>
+				<Button aria-disabled>Button</Button>
+			</ToolTip>
+		);
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		expect( button ).toBeVisible();
+		//expect( button ).toBeDisabled();
+
+		await user.hover( button );
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should not show tooltip if the mouse leaves the tooltip anchor before set delay', async () => {
+		const user = userEvent.setup();
+		const onMouseEnterMock = jest.fn();
+		const onMouseLeaveMock = jest.fn();
+		const MOUSE_LEAVE_DELAY = TOOLTIP_DELAY - 200;
+
+		render(
+			<>
+				<ToolTip { ...props }>
+					<Button
+						onMouseEnter={ onMouseEnterMock }
+						onMouseLeave={ onMouseLeaveMock }
+					>
+						Button 1
+					</Button>
+				</ToolTip>
+				<Button>Button 2</Button>
+			</>
+		);
+
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 1',
+			} )
+		);
+
+		// ToolTip hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		//expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
```

The assertions related to the mocks are commented out at the moment because they aren't passing. On my to-do list to look into.

#### brookewp on 2023-03-30T14:59:13Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1153397594
Path: packages/components/src/ui/ariakit-tooltip/test/index.tsx

```diff
@@ -0,0 +1,330 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../../button';
+import { ToolTip } from '..';
+import { TOOLTIP_DELAY } from '../../../tooltip/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'ToolTip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		const user = userEvent.setup();
+
+		render(
+			// @ts-expect-error
+			<ToolTip { ...props }>
+				<Button>Button 1</Button>
+				<Button>Button 2</Button>
+			</ToolTip>
+		);
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <ToolTip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.hover( button );
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup( { delay: TOOLTIP_DELAY } );
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <ToolTip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.hover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should show tooltip when an element is aria-disabled', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<ToolTip { ...props }>
+				<Button aria-disabled>Button</Button>
+			</ToolTip>
+		);
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		expect( button ).toBeVisible();
+		//expect( button ).toBeDisabled();
+
+		await user.hover( button );
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.unhover( button );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Wait for the tooltip element to be positioned (aligned with the button)
+		// await waitFor( () =>
+		// 	expect(
+		// 		getWrappingPopoverElement( screen.getByText( 'tooltip text' ) )
+		// 	).toBePositionedPopover()
+		// );
+	} );
+
+	it( 'should not show tooltip if the mouse leaves the tooltip anchor before set delay', async () => {
+		const user = userEvent.setup();
+		const onMouseEnterMock = jest.fn();
+		const onMouseLeaveMock = jest.fn();
+		const MOUSE_LEAVE_DELAY = TOOLTIP_DELAY - 200;
+
+		render(
+			<>
+				<ToolTip { ...props }>
+					<Button
+						onMouseEnter={ onMouseEnterMock }
+						onMouseLeave={ onMouseLeaveMock }
+					>
+						Button 1
+					</Button>
+				</ToolTip>
+				<Button>Button 2</Button>
+			</>
+		);
+
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 1',
+			} )
+		);
+
+		// ToolTip hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		//expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
```

This has been fixed

### Review thread 4 (resolved)

#### brookewp on 2023-03-24T06:19:13Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147161065
Path: packages/components/src/ui/ariakit-tooltip/types.ts

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import type { TooltipStateProps } from 'ariakit/tooltip';
+
+export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
+	/**
+	 * The anchor for the tooltip. Accepts only one child element.
+	 */
+	children: React.ReactElement | string;
+	/**
+	 * The amount of time in milliseconds to wait before showing the tooltip.
+	 *
+	 * @default 700
+	 */
+	delay?: number;
+	/**
+	 * Where the tooltip should be positioned relative to its parent.
+	 *
+	 * @default
+	 */
+	placement?: any; //Placement | undefinded;
```

I believe this needs to be `Placement | undefined` (not undefinded 😂 ). I thought I could achieve this with Pick but it's not quite there so another to-do item.

### Review thread 5 (resolved)

#### brookewp on 2023-03-24T06:23:25Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147163402
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -0,0 +1,98 @@
+/**
+ * External dependencies
+ */
+import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { cloneElement, isValidElement } from '@wordpress/element';
+import { useInstanceId } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
+
+/**
+ * Internal dependencies
+ */
+import { TOOLTIP_DELAY } from '../../tooltip/';
+import type { ToolTipProps } from './types';
+import Shortcut from '../../shortcut';
+import { positionToPlacement as __experimentalPopoverLegacyPositionToPlacement } from '../../popover/utils';
+import * as styles from './styles';
+import { contextConnectWithoutRef } from '../context/context-connect';
+import { useCx } from '../../utils/hooks/use-cx';
+
+function AriaToolTip( props: ToolTipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		placement,
+		position,
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( ToolTip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - give priority to `placement` prop, if defined
+	// - otherwise, compute it from the legacy `position` prop (if defined)
+	// - finally, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+	if ( placement !== undefined ) {
+		computedPlacement = placement;
+	} else if ( position !== undefined ) {
+		computedPlacement =
+			// @ts-expect-error
+			__experimentalPopoverLegacyPositionToPlacement( position );
+	}
+	computedPlacement = computedPlacement || DEFAULT_PLACEMENT;
+
+	if ( position !== undefined ) {
+		deprecated( '`position` prop in wp.components.tooltip', {
+			since: '6.3',
+			alternative: '`placement` prop',
+		} );
+	}
+
+	const tooltipState = useTooltipState( {
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	const cx = useCx();
+	const ToolTipClassName = cx( styles.ToolTip );
+	const ShortcutClassName = cx( styles.Shortcut );
+
+	return (
+		<>
+			<TooltipAnchor described state={ tooltipState }>
+				{ isValidElement( children )
+					? ( childProps ) =>
+							cloneElement( children, {
+								...childProps,
+							} )
```

using `cloneElement` and render props as suggested in https://github.com/ariakit/ariakit/issues/1245#issuecomment-1101208316 which seems to be a workaround (`cloneElement` is deemed as fragile)

this is to ensure the props are passed down to the child and so we don't end up with an extra div, which would break legacy styles/the usage

### Review thread 6 (resolved)

#### brookewp on 2023-03-24T06:25:49Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147164787
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -0,0 +1,98 @@
+/**
+ * External dependencies
+ */
+import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { cloneElement, isValidElement } from '@wordpress/element';
+import { useInstanceId } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
+
+/**
+ * Internal dependencies
+ */
+import { TOOLTIP_DELAY } from '../../tooltip/';
+import type { ToolTipProps } from './types';
+import Shortcut from '../../shortcut';
+import { positionToPlacement as __experimentalPopoverLegacyPositionToPlacement } from '../../popover/utils';
```

Using the solution from Popover to add a deprecation warning to use placement over position while ensuring position still works - related to #44401

### Review thread 7

#### brookewp on 2023-03-24T16:30:00Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147814263
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -0,0 +1,98 @@
+/**
+ * External dependencies
+ */
+import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { cloneElement, isValidElement } from '@wordpress/element';
+import { useInstanceId } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
+
+/**
+ * Internal dependencies
+ */
+import { TOOLTIP_DELAY } from '../../tooltip/';
+import type { ToolTipProps } from './types';
+import Shortcut from '../../shortcut';
+import { positionToPlacement as __experimentalPopoverLegacyPositionToPlacement } from '../../popover/utils';
+import * as styles from './styles';
+import { contextConnectWithoutRef } from '../context/context-connect';
+import { useCx } from '../../utils/hooks/use-cx';
+
+function AriaToolTip( props: ToolTipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		placement,
+		position,
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( ToolTip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - give priority to `placement` prop, if defined
+	// - otherwise, compute it from the legacy `position` prop (if defined)
+	// - finally, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+	if ( placement !== undefined ) {
+		computedPlacement = placement;
+	} else if ( position !== undefined ) {
+		computedPlacement =
+			// @ts-expect-error
+			__experimentalPopoverLegacyPositionToPlacement( position );
+	}
+	computedPlacement = computedPlacement || DEFAULT_PLACEMENT;
+
+	if ( position !== undefined ) {
+		deprecated( '`position` prop in wp.components.tooltip', {
+			since: '6.3',
+			alternative: '`placement` prop',
+		} );
+	}
+
+	const tooltipState = useTooltipState( {
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	const cx = useCx();
+	const ToolTipClassName = cx( styles.ToolTip );
+	const ShortcutClassName = cx( styles.Shortcut );
+
+	return (
+		<>
+			<TooltipAnchor described state={ tooltipState }>
```

After seeing [this comment](https://github.com/WordPress/gutenberg/issues/48222#issuecomment-1482937115), I just wanted to add a note to clarify what the `described` prop is. In ariakit, `aria-labelledby` is the default but `aria-describedby` can be added by adding the `described` prop: 
https://github.com/ariakit/ariakit/blob/5d8a1f047fcadcf117073c70359663a3946b73bf/packages/ariakit/src/tooltip/tooltip-anchor.ts#L122-L136

According to ARIA guidelines, `aria-describedby` is required for tooltip so I'm not sure why it isn't the default option. @ciampo pointed out it might be worth asking the author about this choice.

### Review thread 8

#### ciampo on 2023-03-24T18:03:09Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147909699
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -22,12 +25,37 @@ function AriaToolTip( props: ToolTipProps ) {
 		children,
 		delay = TOOLTIP_DELAY,
 		placement,
+		position,
 		shortcut,
 		text,
 	} = props;
 
+
+	const DEFAULT_PLACEMENT = 'bottom';
```

Interestingly, [the legacy tooltip has a default `position` of `bottom middle` in code](https://github.com/WordPress/gutenberg/blob/8c97cb45b138c5ed10f14eed66923e14870833c6/packages/components/src/tooltip/index.js#L120), while the README states that [the default is `top center`](https://github.com/WordPress/gutenberg/blob/71308e4d69c32a9785b0353418a2074535190b29/packages/components/src/tooltip/README.md?plain=1#L31).

Personally, I think that it makes more sense for a tooltip's default position to be on top of its anchor, but it makes sense to keep `bottom` as the default `placement`, since it corresponds to the current legacy tooltip behaviour

### Review thread 9 (resolved)

#### ciampo on 2023-03-24T18:10:16Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147915605
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -7,12 +7,15 @@ import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
  * WordPress dependencies
  */
 import { Children } from '@wordpress/element';
+import deprecated from '@wordpress/deprecated';
 
 /**
  * Internal dependencies
  */
 import Shortcut from '../../shortcut';
+import { TOOLTIP_DELAY } from '../../tooltip/';
 import type { ToolTipProps } from './types';
+import { positionToPlacement as __experimentalPopoverLegacyPositionToPlacement } from '../../popover/utils';
```

There's no need to rename the function to `__experimentalPopoverLegacyPositionToPlacement`. The rename is necessary only when exporting the function outside of the components package

### Review thread 10 (resolved)

#### ciampo on 2023-03-24T18:24:45Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147927828
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -0,0 +1,98 @@
+/**
+ * External dependencies
+ */
+import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { cloneElement, isValidElement } from '@wordpress/element';
+import { useInstanceId } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
+
+/**
+ * Internal dependencies
+ */
+import { TOOLTIP_DELAY } from '../../tooltip/';
+import type { ToolTipProps } from './types';
+import Shortcut from '../../shortcut';
+import { positionToPlacement as __experimentalPopoverLegacyPositionToPlacement } from '../../popover/utils';
+import * as styles from './styles';
+import { contextConnectWithoutRef } from '../context/context-connect';
+import { useCx } from '../../utils/hooks/use-cx';
+
+function AriaToolTip( props: ToolTipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		placement,
+		position,
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( ToolTip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - give priority to `placement` prop, if defined
+	// - otherwise, compute it from the legacy `position` prop (if defined)
+	// - finally, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+	if ( placement !== undefined ) {
+		computedPlacement = placement;
+	} else if ( position !== undefined ) {
+		computedPlacement =
+			// @ts-expect-error
```

This error is happening because the legacy to position function only accepts a range of values representing only valid Popover `position`s.

The way we can fix this is to set the correct types for the `position` and `placement` props:
- for the `placement` prop, we can borrow the types from the ariakit tooltip
- for the `position` prop, we can borrow types from `Popover`


```diff
diff --git a/packages/components/src/ui/ariakit-tooltip/index.tsx b/packages/components/src/ui/ariakit-tooltip/index.tsx
index 4ff8859de0..178bd76aab 100644
--- a/packages/components/src/ui/ariakit-tooltip/index.tsx
+++ b/packages/components/src/ui/ariakit-tooltip/index.tsx
@@ -45,7 +45,6 @@ function AriaToolTip( props: ToolTipProps ) {
 		computedPlacement = placement;
 	} else if ( position !== undefined ) {
 		computedPlacement =
-			// @ts-expect-error
 			__experimentalPopoverLegacyPositionToPlacement( position );
 	}
 	computedPlacement = computedPlacement || DEFAULT_PLACEMENT;
diff --git a/packages/components/src/ui/ariakit-tooltip/types.ts b/packages/components/src/ui/ariakit-tooltip/types.ts
index d7684710fe..d4bde191d3 100644
--- a/packages/components/src/ui/ariakit-tooltip/types.ts
+++ b/packages/components/src/ui/ariakit-tooltip/types.ts
@@ -3,6 +3,11 @@
  */
 import type { TooltipStateProps } from 'ariakit/tooltip';
 
+/**
+ * Internal dependencies
+ */
+import type { PopoverProps } from '../../popover/types';
+
 export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
 	/**
 	 * The anchor for the tooltip. Accepts only one child element.
@@ -19,7 +24,7 @@ export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
 	 *
 	 * @default
 	 */
-	placement?: any; //Placement | undefinded;
+	placement?: TooltipStateProps[ 'placement' ];
 	/**
 	 * _Note: this prop is deprecated. Please use the `placement` prop instead._
 	 *
@@ -27,7 +32,7 @@ export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
 	 *
 	 * @default top center
 	 */
-	position?: string;
+	position?: PopoverProps[ 'position' ];
 	/**
 	 * Option for adding accessible keyboard shortcuts.
 	 */

```

But we'll still incur in a TS error, because the return type of `__experimentalPopoverLegacyPositionToPlacement` currently also include a special placement that we introduced in our `Popover` (called `overlay`). This placement won't work for the ariakit popover (ie. _vanilla_ floating-ui, without any customization).

The best way to fix this is to actually realise that the `positionToPlacement` function currently doesn't return `overlay` in any scenario — we can then update its types, and fix the TS error in tooltip!


```diff
diff --git a/packages/components/src/popover/utils.ts b/packages/components/src/popover/utils.ts
index 022e7796fe..1e03c9ed19 100644
--- a/packages/components/src/popover/utils.ts
+++ b/packages/components/src/popover/utils.ts
@@ -16,7 +16,7 @@ import type {
 
 const POSITION_TO_PLACEMENT: Record<
 	NonNullable< PopoverProps[ 'position' ] >,
-	NonNullable< PopoverProps[ 'placement' ] >
+	NonNullable< Exclude< PopoverProps[ 'placement' ], 'overlay' > >
 > = {
 	bottom: 'bottom',
 	top: 'top',
@@ -79,8 +79,7 @@ const POSITION_TO_PLACEMENT: Record<
  */
 export const positionToPlacement = (
 	position: NonNullable< PopoverProps[ 'position' ] >
-): NonNullable< PopoverProps[ 'placement' ] > =>
-	POSITION_TO_PLACEMENT[ position ] ?? 'bottom';
+) => POSITION_TO_PLACEMENT[ position ] ?? 'bottom';
 
 /**
  * @typedef AnimationOrigin

```

We can actually update the types of `POSITION_TO_PLACEMENT` and then remove the explicit return type from the `positionToPlacement` function — typescript will be able to infer them correctly.

### Review thread 11 (resolved)

#### ciampo on 2023-03-24T18:28:59Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147931735
Path: packages/components/src/ui/ariakit-tooltip/types.ts

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import type { TooltipStateProps } from 'ariakit/tooltip';
+
+export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
+	/**
+	 * The anchor for the tooltip. Accepts only one child element.
+	 */
+	children: React.ReactElement | string;
+	/**
+	 * The amount of time in milliseconds to wait before showing the tooltip.
+	 *
+	 * @default 700
+	 */
+	delay?: number;
+	/**
+	 * Where the tooltip should be positioned relative to its parent.
+	 *
+	 * @default
```

Missing the actual default value in the JSDocs (`'bottom'`)

### Review thread 12 (resolved)

#### ciampo on 2023-03-24T18:29:49Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147932387
Path: packages/components/src/ui/ariakit-tooltip/test/index.tsx

```diff
@@ -0,0 +1,330 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../../button';
+import { ToolTip } from '..';
+import { TOOLTIP_DELAY } from '../../../tooltip/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'ToolTip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		const user = userEvent.setup();
+
+		render(
+			// @ts-expect-error
```

It's good practice to add a little inline comment explaining why we're expecting a TypeScript error

### Review thread 13 (resolved)

#### ciampo on 2023-03-24T18:30:32Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147932934
Path: packages/components/src/ui/ariakit-tooltip/types.ts

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import type { TooltipStateProps } from 'ariakit/tooltip';
+
+export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
+	/**
+	 * The anchor for the tooltip. Accepts only one child element.
+	 */
+	children: React.ReactElement | string;
+	/**
+	 * The amount of time in milliseconds to wait before showing the tooltip.
+	 *
+	 * @default 700
+	 */
+	delay?: number;
+	/**
+	 * Where the tooltip should be positioned relative to its parent.
+	 *
+	 * @default
+	 */
+	placement?: any; //Placement | undefinded;
+	/**
+	 * _Note: this prop is deprecated. Please use the `placement` prop instead._
+	 *
+	 * Legacy way of specifying the tooltip's position relative to its parent.
+	 *
+	 * @default top center
+	 */
+	position?: string;
+	/**
+	 * Option for adding accessible keyboard shortcuts.
+	 */
+	shortcut?: string | { display: string; ariaLabel: string };
```

We could reuse `Shortcut`'s types here

### Review thread 14 (resolved)

#### ciampo on 2023-03-24T18:34:12Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147936235
Path: packages/components/src/ui/ariakit-tooltip/types.ts

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import type { TooltipStateProps } from 'ariakit/tooltip';
+
+export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
+	/**
+	 * The anchor for the tooltip. Accepts only one child element.
+	 */
+	children: React.ReactElement | string;
+	/**
+	 * The amount of time in milliseconds to wait before showing the tooltip.
+	 *
+	 * @default 700
+	 */
+	delay?: number;
+	/**
+	 * Where the tooltip should be positioned relative to its parent.
+	 *
+	 * @default
+	 */
+	placement?: any; //Placement | undefinded;
+	/**
+	 * _Note: this prop is deprecated. Please use the `placement` prop instead._
+	 *
+	 * Legacy way of specifying the tooltip's position relative to its parent.
+	 *
+	 * @default top center
```

We should also mark the `position` prop as deprecated in the JSDocs

```suggestion
	 * @deprecated
	 * @default top center
```

### Review thread 15 (resolved)

#### ciampo on 2023-03-24T18:36:31Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147938030
Path: packages/components/src/ui/ariakit-tooltip/types.ts

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import type { TooltipStateProps } from 'ariakit/tooltip';
+
+export type ToolTipProps = Pick< TooltipStateProps, 'placement' > & {
+	/**
+	 * The anchor for the tooltip. Accepts only one child element.
+	 */
+	children: React.ReactElement | string;
+	/**
+	 * The amount of time in milliseconds to wait before showing the tooltip.
+	 *
+	 * @default 700
+	 */
+	delay?: number;
+	/**
+	 * Where the tooltip should be positioned relative to its parent.
+	 *
+	 * @default
+	 */
+	placement?: any; //Placement | undefinded;
+	/**
+	 * _Note: this prop is deprecated. Please use the `placement` prop instead._
+	 *
+	 * Legacy way of specifying the tooltip's position relative to its parent.
+	 *
+	 * @default top center
```

The `position` prop doesn't have a default value anymore (since it's being replaced by the `placement` prop, which has its own equivalent default value). We should therefore remove this `@default` tag in the JSDocs

### Review thread 16 (resolved)

#### brookewp on 2023-03-30T15:00:28Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1153399767
Path: packages/components/src/ui/ariakit-tooltip/style.scss

```diff
@@ -0,0 +1,14 @@
+.components-ariakit-tooltip {
```

I replaced emotion with sass so the `z-index` function could be utilized and because we may be moving away from emotion.

### Review thread 17

#### brookewp on 2023-03-30T15:02:31Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1153402791
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -0,0 +1,87 @@
+/**
+ * External dependencies
+ */
+import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
+import { Slot } from '@radix-ui/react-slot';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
+
+/**
+ * Internal dependencies
+ */
+import { TOOLTIP_DELAY } from '../../tooltip/';
+import type { ToolTipProps } from './types';
+import Shortcut from '../../shortcut';
+import { positionToPlacement } from '../../popover/utils';
+import { contextConnectWithoutRef } from '../context/context-connect';
+
+function AriaToolTip( props: ToolTipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		placement,
+		position,
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( ToolTip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - give priority to `placement` prop, if defined
+	// - otherwise, compute it from the legacy `position` prop (if defined)
+	// - finally, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+	if ( placement !== undefined ) {
+		computedPlacement = placement;
+	} else if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	}
+	computedPlacement = computedPlacement || DEFAULT_PLACEMENT;
+
+	if ( position !== undefined ) {
+		deprecated( '`position` prop in wp.components.tooltip', {
+			since: '6.3',
+			alternative: '`placement` prop',
+		} );
+	}
+
+	const tooltipState = useTooltipState( {
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	return (
+		<>
+			<TooltipAnchor as={ Slot } described state={ tooltipState }>
```

We need to use a solution like [Slot](https://www.radix-ui.com/docs/primitives/utilities/slot) from Radix to merge props to the immediate child. 

This solution was one of the options proposed by ariakit author: https://github.com/ariakit/ariakit/issues/1245#issuecomment-1101208316

#### diegohaz on 2023-06-05T08:33:19Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1217721905
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -0,0 +1,87 @@
+/**
+ * External dependencies
+ */
+import { Tooltip, TooltipAnchor, useTooltipState } from 'ariakit/tooltip';
+import { Slot } from '@radix-ui/react-slot';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import deprecated from '@wordpress/deprecated';
+
+/**
+ * Internal dependencies
+ */
+import { TOOLTIP_DELAY } from '../../tooltip/';
+import type { ToolTipProps } from './types';
+import Shortcut from '../../shortcut';
+import { positionToPlacement } from '../../popover/utils';
+import { contextConnectWithoutRef } from '../context/context-connect';
+
+function AriaToolTip( props: ToolTipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		placement,
+		position,
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( ToolTip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - give priority to `placement` prop, if defined
+	// - otherwise, compute it from the legacy `position` prop (if defined)
+	// - finally, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+	if ( placement !== undefined ) {
+		computedPlacement = placement;
+	} else if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	}
+	computedPlacement = computedPlacement || DEFAULT_PLACEMENT;
+
+	if ( position !== undefined ) {
+		deprecated( '`position` prop in wp.components.tooltip', {
+			since: '6.3',
+			alternative: '`placement` prop',
+		} );
+	}
+
+	const tooltipState = useTooltipState( {
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	return (
+		<>
+			<TooltipAnchor as={ Slot } described state={ tooltipState }>
```

That's not needed anymore. See https://ariakit.org/examples/dialog-radix for an example of how to implement an API similar to Radix UI.

This should be as easy as:
```jsx
<TooltipAnchor render={children} />
```

### Review thread 18 (resolved)

#### brookewp on 2023-07-08T01:14:50Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1256708223
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -53,21 +56,20 @@ function AriaToolTip( props: ToolTipProps ) {
 		} );
 	}
 
-	const tooltipState = useTooltipState( {
+	const tooltipStore = useTooltipStore( {
+		// @ts-ignore
 		placement: computedPlacement,
 		timeout: delay,
 	} );
 
 	return (
 		<>
-			<TooltipAnchor as={ Slot } described state={ tooltipState }>
-				{ children }
-			</TooltipAnchor>
+			<TooltipAnchor render={ children } store={ tooltipStore } />
```

We were able to remove `Slot` from `radix-ui` in favour of `render` thanks to info shared here: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1217721905

There is a temporary `ts-ignore` added due to errors related to `placement` (only occurring after the slot to render change). I'll need to investigate this further

### Review thread 19 (resolved)

#### brookewp on 2023-07-21T22:10:06Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1271141953
Path: packages/components/src/ui/ariakit-tooltip/index.tsx

```diff
@@ -80,6 +81,7 @@ function AriaToolTip( props: ToolTipProps ) {
 							shortcut={ shortcut }
 						/>
 					) }
+					<TooltipArrow size={ 0 } />
```

The `placement` of tooltip was no longer working after recent updates, with the following error in unit tests: 

```
TypeError: Cannot destructure property 'name' of '_ref' as it is undefined.

      at node_modules/@floating-ui/core/dist/floating-ui.core.umd.js:110:11
          at Array.filter (<anonymous>)
      at Object.computePosition (node_modules/@floating-ui/core/dist/floating-ui.core.umd.js:108:22)
      at updatePosition2 (node_modules/@ariakit/react-core/cjs/__chunks/P3RGUF6X.cjs:228:21)
      at update (node_modules/@ariakit/react-core/cjs/__chunks/P3RGUF6X.cjs:257:11)
```

This issue was resolved in `floating-ui/core@1.0.2`, but we use `1.0.1`, the required minimum by `floating-ui/dom`. Currently, we have `floating-ui/react-dom` pinned, and [we're manually updating](https://github.com/WordPress/gutenberg/pull/48402#issuecomment-1621484780) the `package-lock.json` for releases. So I wonder if we can update this without causing issues or accidental overrides in the future. 

**Specifically for tooltip**, the error above occurs when the `ArrowElement` is `null`. If we pass in the `TooltipArrow` component, this resolves the issue. 

I was thinking because of the [component stores](https://ariakit.org/guide/component-stores), that we might be able to set the arrow to an empty `HTMLElement` or mock it, as there are the methods `setState` and `setArrowElement`, but I'm not sure of the best approach there. 

A simple workaround would be setting the arrow size to 0, which avoids `null` and is hidden visually. I _think_ it would be ok in this case since the arrow is set to `aria-hidden`, and the size wouldn't be exposed in our API for changes to be made. Or at least until we decided to add the arrow in the future, which I think would be ideal; but, for now, it isn't a feature of the current tooltip we are replacing.

### Review thread 20 (resolved)

#### brookewp on 2023-07-26T22:39:05Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1275548600
Path: packages/components/src/ui/ariakit-tooltip/test/index.tsx

```diff
@@ -0,0 +1,301 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../../button';
+import { ToolTip } from '..';
+import { TOOLTIP_DELAY } from '../../../tooltip/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'ToolTip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		const user = userEvent.setup();
+		// mock expected console error
+		const err = jest
+			.spyOn( console, 'error' )
+			.mockImplementation( () => {} );
+
+		expect( () => {
+			render(
+				// expected TS error since Tooltip cannot have more than one child element
+				// @ts-expect-error
+				<ToolTip { ...props }>
+					<Button>This is a button</Button>
+					<Button>This is another button</Button>
+				</ToolTip>
+			);
+		} ).toThrow();
+
+		expect( console ).toHaveErrored();
+		expect( err ).toHaveBeenCalled();
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		err.mockReset();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <ToolTip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.tab();
+
+		expect( button ).toHaveFocus();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.tab();
+		await user.unhover( button );
```

After tabbing away from the child element, the tooltip should disappear. The only way I could get it to pass was to 'unhover' the button after tabbing, which is quite strange... the behaviour appears to work differently than what I see from manually testing this process in the browser.

#### tyxla on 2023-07-27T12:52:10Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1276238315
Path: packages/components/src/ui/ariakit-tooltip/test/index.tsx

```diff
@@ -0,0 +1,301 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../../button';
+import { ToolTip } from '..';
+import { TOOLTIP_DELAY } from '../../../tooltip/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'ToolTip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		const user = userEvent.setup();
+		// mock expected console error
+		const err = jest
+			.spyOn( console, 'error' )
+			.mockImplementation( () => {} );
+
+		expect( () => {
+			render(
+				// expected TS error since Tooltip cannot have more than one child element
+				// @ts-expect-error
+				<ToolTip { ...props }>
+					<Button>This is a button</Button>
+					<Button>This is another button</Button>
+				</ToolTip>
+			);
+		} ).toThrow();
+
+		expect( console ).toHaveErrored();
+		expect( err ).toHaveBeenCalled();
+
+		await user.tab();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		err.mockReset();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <ToolTip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <ToolTip { ...props } /> );
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		await user.tab();
+
+		expect( button ).toHaveFocus();
+
+		await waitFor( () =>
+			expect(
+				screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			).toBeVisible()
+		);
+
+		await user.tab();
+		await user.unhover( button );
```

As I mentioned in my earlier comment, when testing in storybook the behavior seems to match what the test sees - tabbing doesn't hide the tooltip, at least in my testing in the latest Chrome.

### Review thread 21

#### brookewp on 2023-08-18T03:28:19Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297948564
Path: packages/components/src/button/test/index.tsx

tests were checking `not.toBeInTheDocument`, but this version of tooltip remains in the DOM when it is hidden

### Review thread 22 (resolved)

#### brookewp on 2023-08-18T03:29:05Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297948873
Path: packages/components/src/tab-panel/test/index.tsx

`Popover.Slot` is no longer needed and tests were checking `not.toBeInTheDocument`, but this version of tooltip remains in the DOM when it is hidden

### Review thread 23 (resolved)

#### brookewp on 2023-08-18T03:30:54Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297949688
Path: packages/components/src/tooltip/test/utils/index.tsx

```diff
@@ -0,0 +1,15 @@
+// TODO: may need to be tested with Playwright; further context:
+// https://github.com/WordPress/gutenberg/pull/52133#issuecomment-1613691258
+// below workaround ensures tooltip is umounted after each test to prevent leaking
+// similarly to ariakit tooltip tests: https://github.com/ariakit/ariakit/blob/249d376e41115e6d4ceba244e231a95fa457bd04/examples/tooltip/test.ts#L12-L14
+
+/**
+ * Ensures the async behaviors and animations in tooltip complete
+ * to avoid leaking into other tests
+ *
+ */
+export default async function cleanupTooltip( user: any ) {
```

This type needs to be updated from `any` but I wasn't sure what type to use. I thought `userEvent` but I ran into some more type issues when trying that

#### mirka on 2023-08-31T17:44:50Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1311985584
Path: packages/components/src/tooltip/test/utils/index.tsx

```diff
@@ -0,0 +1,15 @@
+// TODO: may need to be tested with Playwright; further context:
+// https://github.com/WordPress/gutenberg/pull/52133#issuecomment-1613691258
+// below workaround ensures tooltip is umounted after each test to prevent leaking
+// similarly to ariakit tooltip tests: https://github.com/ariakit/ariakit/blob/249d376e41115e6d4ceba244e231a95fa457bd04/examples/tooltip/test.ts#L12-L14
+
+/**
+ * Ensures the async behaviors and animations in tooltip complete
+ * to avoid leaking into other tests
+ *
+ */
+export default async function cleanupTooltip( user: any ) {
```

Looks like the `UserEvent` type is available from `import type { UserEvent } from '@testing-library/user-event/dist/types/setup/setup'`.

#### brookewp on 2023-08-31T20:43:21Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312207643
Path: packages/components/src/tooltip/test/utils/index.tsx

```diff
@@ -0,0 +1,15 @@
+// TODO: may need to be tested with Playwright; further context:
+// https://github.com/WordPress/gutenberg/pull/52133#issuecomment-1613691258
+// below workaround ensures tooltip is umounted after each test to prevent leaking
+// similarly to ariakit tooltip tests: https://github.com/ariakit/ariakit/blob/249d376e41115e6d4ceba244e231a95fa457bd04/examples/tooltip/test.ts#L12-L14
+
+/**
+ * Ensures the async behaviors and animations in tooltip complete
+ * to avoid leaking into other tests
+ *
+ */
+export default async function cleanupTooltip( user: any ) {
```

Thank you! I must have been using a different import when I ran into type errors

### Review thread 24 (resolved)

#### brookewp on 2023-08-18T03:32:18Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297950438
Path: packages/components/src/toggle-group-control/test/index.tsx

Replaced helper function as `toBePositionedPopover` checks for a CSS class on `Popover` which no longer wraps tootlip

### Review thread 25

#### brookewp on 2023-08-18T03:34:04Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297951492
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,276 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should show tooltip when an element is disabled', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<Tooltip { ...props }>
+				<Button aria-disabled>Button</Button>
+			</Tooltip>
+		);
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		expect( button ).toBeVisible();
+		expect( button ).toHaveAttribute( 'aria-disabled' );
+
+		await user.hover( button );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip if the mouse leaves the tooltip anchor before set delay', async () => {
+		const user = userEvent.setup();
+		const onMouseEnterMock = jest.fn();
+		const onMouseLeaveMock = jest.fn();
+		const MOUSE_LEAVE_DELAY = TOOLTIP_DELAY - 200;
+
+		render(
+			<>
+				<Tooltip { ...props }>
+					<Button
+						onMouseEnter={ onMouseEnterMock }
+						onMouseLeave={ onMouseLeaveMock }
+					>
+						Button 1
+					</Button>
+				</Tooltip>
+				<Button>Button 2</Button>
+			</>
+		);
+
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 1',
+			} )
+		);
+
+		// Tooltip hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
+
+		// Advance time by MOUSE_LEAVE_DELAY time
+		await new Promise( ( resolve ) =>
+			setTimeout( resolve, MOUSE_LEAVE_DELAY )
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Hover the other button, meaning that the mouse will leave the tooltip anchor
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 2',
+			} )
+		);
+
+		// Tooltip still hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
+		expect( onMouseLeaveMock ).toHaveBeenCalledTimes( 1 );
+
+		// Advance time again, so that we reach the full TOOLTIP_DELAY time
+		await new Promise( ( resolve ) =>
+			setTimeout( resolve, TOOLTIP_DELAY )
+		);
+
+		// Tooltip won't show, since the mouse has left the tooltip anchor
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the shortcut display text when a string is passed as the shortcut', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } shortcut="shortcut text" /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect( await screen.findByText( 'shortcut text' ) ).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the keyboard shortcut display text and aria-label when an object is passed as the shortcut', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<Tooltip
+				{ ...props }
+				shortcut={ {
+					display: '⇧⌘,',
+					ariaLabel: shortcutAriaLabel.primaryShift( ',' ),
+				} }
+			/>
+		);
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		await waitFor( () =>
```

I tried to replace all `getBy` queries with `findBy` and therefore remove `waitFor()` but it did not work in this case

### Review thread 26

#### brookewp on 2023-08-18T03:34:45Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297951753
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,276 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should show tooltip when an element is disabled', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<Tooltip { ...props }>
+				<Button aria-disabled>Button</Button>
+			</Tooltip>
+		);
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		expect( button ).toBeVisible();
+		expect( button ).toHaveAttribute( 'aria-disabled' );
+
+		await user.hover( button );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip if the mouse leaves the tooltip anchor before set delay', async () => {
+		const user = userEvent.setup();
+		const onMouseEnterMock = jest.fn();
+		const onMouseLeaveMock = jest.fn();
+		const MOUSE_LEAVE_DELAY = TOOLTIP_DELAY - 200;
+
+		render(
+			<>
+				<Tooltip { ...props }>
+					<Button
+						onMouseEnter={ onMouseEnterMock }
+						onMouseLeave={ onMouseLeaveMock }
+					>
+						Button 1
+					</Button>
+				</Tooltip>
+				<Button>Button 2</Button>
+			</>
+		);
+
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 1',
+			} )
+		);
+
+		// Tooltip hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
+
+		// Advance time by MOUSE_LEAVE_DELAY time
+		await new Promise( ( resolve ) =>
+			setTimeout( resolve, MOUSE_LEAVE_DELAY )
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Hover the other button, meaning that the mouse will leave the tooltip anchor
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 2',
+			} )
+		);
+
+		// Tooltip still hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
+		expect( onMouseLeaveMock ).toHaveBeenCalledTimes( 1 );
+
+		// Advance time again, so that we reach the full TOOLTIP_DELAY time
+		await new Promise( ( resolve ) =>
+			setTimeout( resolve, TOOLTIP_DELAY )
+		);
+
+		// Tooltip won't show, since the mouse has left the tooltip anchor
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the shortcut display text when a string is passed as the shortcut', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } shortcut="shortcut text" /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect( await screen.findByText( 'shortcut text' ) ).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the keyboard shortcut display text and aria-label when an object is passed as the shortcut', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<Tooltip
+				{ ...props }
+				shortcut={ {
+					display: '⇧⌘,',
+					ariaLabel: shortcutAriaLabel.primaryShift( ',' ),
+				} }
+			/>
+		);
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		await waitFor( () =>
+			expect( screen.getByText( '⇧⌘,' ) ).toBeVisible()
+		);
+
+		expect( screen.getByText( '⇧⌘,' ) ).toHaveAttribute(
+			'aria-label',
+			'Control + Shift + Comma'
+		);
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'esc should close modal even when tooltip is visible', async () => {
+		const user = userEvent.setup();
+		const onRequestClose = jest.fn();
+		render(
+			<Modal onRequestClose={ onRequestClose }>
+				<p>Modal content</p>
+			</Modal>
+		);
+
+		const tooltip = await screen.findByRole( 'tooltip', { hidden: true } );
+
+		expect( tooltip ).toBeInTheDocument();
+
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: /Close/i,
+			} )
+		);
+
+		await waitFor( () => expect( tooltip ).toBeVisible() );
```

I also couldn't replace this `waitFor()` with `findBy`

### Review thread 27

#### brookewp on 2023-08-18T03:35:15Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297951924
Path: packages/editor/src/components/post-saved-state/test/index.js

The query needs to be more specific to avoid finding tooltip in the DOM

### Review thread 28

#### brookewp on 2023-08-18T03:38:21Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297953125
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
```

This type error is due to `overlay` from `position`/`Popover`. I thought maybe we could `Omit` it since it isn't exposed in the legacy tooltip API, but I wasn't sure how to execute that.

#### mirka on 2023-08-31T19:08:25Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312093852
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
```

How about:

```diff
diff --git a/packages/components/src/popover/utils.ts b/packages/components/src/popover/utils.ts
index 2112b28596..cf410fbd9a 100644
--- a/packages/components/src/popover/utils.ts
+++ b/packages/components/src/popover/utils.ts
@@ -3,7 +3,7 @@
  */
 // eslint-disable-next-line no-restricted-imports
 import type { MotionProps } from 'framer-motion';
-import type { ReferenceType } from '@floating-ui/react-dom';
+import type { Placement, ReferenceType } from '@floating-ui/react-dom';
 
 /**
  * Internal dependencies
@@ -16,7 +16,7 @@ import type {
 
 const POSITION_TO_PLACEMENT: Record<
 	NonNullable< PopoverProps[ 'position' ] >,
-	NonNullable< PopoverProps[ 'placement' ] >
+	Placement
 > = {
 	bottom: 'bottom',
 	top: 'top',
@@ -79,8 +79,7 @@ const POSITION_TO_PLACEMENT: Record<
  */
 export const positionToPlacement = (
 	position: NonNullable< PopoverProps[ 'position' ] >
-): NonNullable< PopoverProps[ 'placement' ] > =>
-	POSITION_TO_PLACEMENT[ position ] ?? 'bottom';
+) => POSITION_TO_PLACEMENT[ position ] ?? 'bottom';
 
 /**
  * @typedef AnimationOrigin
```

#### brookewp on 2023-08-31T23:26:18Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312380449
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
```

Ah, okay. So instead of using the types for `placement` from `Popover`, we are using it from `floating-ui` instead, which doesn't contain `overlay`. 

And `NonNullable` isn't needed anymore?

#### ciampo on 2023-09-04T07:16:26Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1314527239
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
```

We needed `NonNullable` because the `placement` prop could also be `undefined`. In contrast, the `Placement` type from `floating-ui` does not contain `undefined`, and therefore we don't need to wrap it in `NonNullable` :) 


Once https://github.com/WordPress/gutenberg/pull/54101 is merged, we can rebase / merge trunk to include its changes in this PR :) 

(interestingly, I had already left a similar comment in https://github.com/WordPress/gutenberg/pull/48440#discussion_r1147927828)

### Review thread 29

#### brookewp on 2023-08-18T03:44:34Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297955431
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	return (
+		<>
+			<Ariakit.TooltipAnchor
+				onBlur={ () => tooltipStore.hide() }
+				store={ tooltipStore }
+				render={ isOnlyChild ? children : undefined }
+			>
+				{ ! isOnlyChild ? children : null }
+			</Ariakit.TooltipAnchor>
+			{ isOnlyChild && ( text || shortcut ) && (
+				<Ariakit.Tooltip
+					className="components-tooltip"
+					gutter={ 4 }
+					// 	hide tooltip when interacting with its anchor to match legacy tooltip
+					hideOnInteractOutside={ () => {
```

This allows us match [the behaviour of our legacy tooltip](https://user-images.githubusercontent.com/35543432/260192090-e0840e85-9335-44cd-a2ff-1c867497c427.gif) so that the tooltip is hidden when the anchor is clicked. I don't believe this is the correct usage of [`hideOnInteractOutside`](https://ariakit.org/reference/tooltip#hideoninteractoutside), so if there is another way, that would be preferable. Perhaps @diegohaz has a suggestion for this? 

We did try adding this behavior to a `div` wrapper but that caused styles to break in existing use cases of our legacy tooltip.

#### diegohaz on 2023-08-18T08:47:21Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1298187518
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	return (
+		<>
+			<Ariakit.TooltipAnchor
+				onBlur={ () => tooltipStore.hide() }
+				store={ tooltipStore }
+				render={ isOnlyChild ? children : undefined }
+			>
+				{ ! isOnlyChild ? children : null }
+			</Ariakit.TooltipAnchor>
+			{ isOnlyChild && ( text || shortcut ) && (
+				<Ariakit.Tooltip
+					className="components-tooltip"
+					gutter={ 4 }
+					// 	hide tooltip when interacting with its anchor to match legacy tooltip
+					hideOnInteractOutside={ () => {
```

Yeah, I don't think `hideOnInteractOutside` is the right place to put that logic. I'd put it in the `onClick` handler on the tooltip anchor:

```jsx
<Ariakit.TooltipAnchor onClick={tooltipStore.hide} />
```

#### brookewp on 2023-08-21T21:52:53Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1300684443
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,106 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
+		timeout: delay,
+	} );
+
+	return (
+		<>
+			<Ariakit.TooltipAnchor
+				onBlur={ () => tooltipStore.hide() }
+				store={ tooltipStore }
+				render={ isOnlyChild ? children : undefined }
+			>
+				{ ! isOnlyChild ? children : null }
+			</Ariakit.TooltipAnchor>
+			{ isOnlyChild && ( text || shortcut ) && (
+				<Ariakit.Tooltip
+					className="components-tooltip"
+					gutter={ 4 }
+					// 	hide tooltip when interacting with its anchor to match legacy tooltip
+					hideOnInteractOutside={ () => {
```

Thank you!

### Review thread 30

#### brookewp on 2023-08-18T03:46:42Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1297956269
Path: packages/base-styles/_z-index.scss

```diff
@@ -164,9 +164,6 @@ $z-layers: (
 	// And block manager sticky disabled block count is higher still
 	".edit-post-block-manager__disabled-blocks-count": 2,
 
-	// Needs to be higher than any other element as this is an overlay to catch all events
```

We no longer have a custom event catcher

### Review thread 31

#### brookewp on 2023-08-21T22:00:22Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1300689877
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,278 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should show tooltip when an element is disabled', async () => {
+		const user = userEvent.setup();
+
+		render(
+			<Tooltip { ...props }>
+				<Button aria-disabled>Button</Button>
+			</Tooltip>
+		);
+
+		const button = screen.getByRole( 'button', { name: /Button/i } );
+
+		expect( button ).toBeVisible();
+		expect( button ).toHaveAttribute( 'aria-disabled' );
+
+		await user.hover( button );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip if the mouse leaves the tooltip anchor before set delay', async () => {
+		const user = userEvent.setup();
+		const onMouseEnterMock = jest.fn();
+		const onMouseLeaveMock = jest.fn();
+		const MOUSE_LEAVE_DELAY = TOOLTIP_DELAY - 200;
+
+		render(
+			<>
+				<Tooltip { ...props }>
+					<Button
+						onMouseEnter={ onMouseEnterMock }
+						onMouseLeave={ onMouseLeaveMock }
+					>
+						Button 1
+					</Button>
+				</Tooltip>
+				<Button>Button 2</Button>
+			</>
+		);
+
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 1',
+			} )
+		);
+
+		// Tooltip hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
+
+		// Advance time by MOUSE_LEAVE_DELAY time
+		await new Promise( ( resolve ) =>
+			setTimeout( resolve, MOUSE_LEAVE_DELAY )
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		// Hover the other button, meaning that the mouse will leave the tooltip anchor
+		await user.hover(
+			screen.getByRole( 'button', {
+				name: 'Button 2',
+			} )
+		);
+
+		// Tooltip still hasn't appeared yet
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+		expect( onMouseEnterMock ).toHaveBeenCalledTimes( 1 );
+		expect( onMouseLeaveMock ).toHaveBeenCalledTimes( 1 );
+
+		// Advance time again, so that we reach the full TOOLTIP_DELAY time
+		await new Promise( ( resolve ) =>
+			setTimeout( resolve, TOOLTIP_DELAY )
+		);
+
+		// Tooltip won't show, since the mouse has left the tooltip anchor
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the shortcut display text when a string is passed as the shortcut', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } shortcut="shortcut text" /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		await waitFor( () =>
```

Strangely, it worked to replace `waitFor` with `findBy` in this assertion until updating the event handling in this commit: https://github.com/WordPress/gutenberg/pull/48440/commits/956bffd5b4ba05398387aa06fed8015abe2075e6

### Review thread 32 (resolved)

#### brookewp on 2023-08-22T17:26:23Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1301970844
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -101,10 +95,12 @@ describe( 'Tooltip', () => {
 		expect(
 			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
 		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
 	} );
 
 	it( 'should respect custom delay prop when showing tooltip', async () => {
-		const user = userEvent.setup( { delay: TOOLTIP_DELAY } );
```

This caused the test to exceed timeout after changing to the utility function. However, I don't think this is correctly used for the context we are testing here.

### Review thread 33

#### brookewp on 2023-08-23T17:06:25Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1303311736
Path: packages/components/src/button/test/index.tsx

```diff
@@ -230,12 +231,14 @@ describe( 'Button', () => {
 				/>
 			);
 
-			expect( screen.queryByText( 'Label' ) ).not.toBeInTheDocument();
+			expect( screen.queryByText( 'Label' ) ).not.toBeVisible();
 
 			// Move focus to the button
 			await user.tab();
 
 			expect( screen.getByText( 'Label' ) ).toBeVisible();
+
+			await cleanupTooltip( user );
 		} );
 
 		it( 'should populate tooltip with description content for buttons with visible labels (buttons with children)', async () => {
```

Kind of unrelated to this PR, I don't think this test has the right assertion based on the test title. The test queries the button but not the tootlip. I bring this up because I would have thought we'd use the cleanup function here too. But this could be a follow-up PR.

### Review thread 34

#### mirka on 2023-08-31T17:54:39Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1311997867
Path: packages/components/src/tooltip/README.md

```diff
@@ -22,49 +22,38 @@ const MyTooltip = () => (
 
 The component accepts the following props:
 
-### position
-
-The direction in which the tooltip should open relative to its parent node. Specify y- and x-axis as a space-separated string. Supports `"top"`, `"bottom"` y axis, and `"left"`, `"center"`, `"right"` x axis.
-
--   Type: `String`
--   Required: No
--   Default: `"bottom"`
-
-### children
+#### `children`: `Element`
 
 The element to which the tooltip should anchor.
 
-**NOTE:** You must pass only a single child. Tooltip renders itself as a clone of `children` with a [`Popover`](/packages/components/src/popover/README.md) added as an additional child.
+**NOTE:** Accepts only one child element.
 
--   Type: `Element`
 -   Required: Yes
 
-### text
+#### `delay`: `number`
 
-The tooltip text to show on focus or hover.
+The amount of time in milliseconds to wait before showing the tooltip.
 
--   Type: `String`
 -   Required: No
+-   Default: `700`
+
+#### `position`: `string`
 
-### shortcut (web only)
+The direction in which the tooltip should open relative to its parent node. Specify y- and x-axis as a space-separated string. Supports `"top"`, `"bottom"` y axis, and `"left"`, `"center"`, `"right"` x axis.
```

I think we've discussed this, but noting so we don't forget: Let's start the deprecation process for this (`position` → `placement`) as a follow-up issue once this PR is merged.

### Review thread 35 (resolved)

#### mirka on 2023-08-31T18:31:04Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312049859
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,97 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
```

```suggestion
/**
 * Time over anchor to wait before showing tooltip
 */
```

Nit: Type annotation unnecessary?

### Review thread 36 (resolved)

#### mirka on 2023-08-31T18:49:24Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312070008
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,97 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
```

Could we simplify this a bit? I mainly want to avoid the `let`.

```suggestion
		placement:
			position === undefined
				? DEFAULT_PLACEMENT
				: positionToPlacement( position ),
```

#### brookewp on 2023-08-31T20:44:48Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312208937
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,97 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ *
+ * @type {number}
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	// Compute tooltip's placement:
+	// - compute it from the legacy `position` prop (if defined)
+	// - otherwise, fallback to the DEFAULT_PLACEMENT.
+	let computedPlacement;
+
+	if ( position !== undefined ) {
+		computedPlacement = positionToPlacement( position );
+	} else {
+		computedPlacement = DEFAULT_PLACEMENT;
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// TODO: can remove the ignore once position has been fully deprecated
+		// Type error due to 'overlay' type from positionToPlacement
+		// @ts-ignore
+		placement: computedPlacement,
```

Great suggestion!

### Review thread 37 (resolved)

#### mirka on 2023-08-31T19:15:57Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312114917
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,278 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
```

Not exactly sure why we need this here given that `TOOLTIP_DELAY` is the default.

#### brookewp on 2023-08-31T20:42:29Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312206545
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,278 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+	delay: TOOLTIP_DELAY,
```

Good catch! I may have added that in when testing, as I often had the delay off, so it was quicker to test.

### Review thread 38 (resolved)

#### mirka on 2023-08-31T19:30:40Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312129755
Path: packages/components/src/tooltip/test/index.tsx

I think we should have at least one test to assert that the tooltip text is accessibly associated with the button.

So something like:

```js
		expect(
			screen.getByRole( 'button', { description: 'tooltip text' } )
		).toBeInTheDocument();
```

#### brookewp on 2023-08-31T20:46:45Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312210805
Path: packages/components/src/tooltip/test/index.tsx

Great idea ❤️

### Review thread 39 (resolved)

#### brookewp on 2023-09-01T01:07:28Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312428454
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,88 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		//@ts-ignore
```

I opened a separate PR for @mirka's [suggested diff ](https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312093852) to keep the history with Popover and so it doesn't get lost here. 

The ignore above will be removed once that PR is merged.

### Review thread 40

#### brookewp on 2023-09-01T01:08:56Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1312429121
Path: packages/components/CHANGELOG.md

The changelog needs to be updated, but I'll wait until the Popover changes are merged, and this PR is updated respectively.

### Review thread 41 (resolved)

#### chad1008 on 2023-09-01T15:48:00Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1313203699
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,88 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		//@ts-ignore
+		placement:
+			// Compute tooltip's placement:
+			// - compute it from the legacy `position` prop (if defined)
+			// - otherwise, fallback to the DEFAULT_PLACEMENT.
+			position === undefined
+				? DEFAULT_PLACEMENT
+				: positionToPlacement( position ),
+		timeout: delay,
```

It looks like `positionToPlacement` [will properly handle our default](https://github.com/WordPress/gutenberg/blob/b873f437e4f34370f02cf4b99cc90530911b8a9b/packages/components/src/popover/utils.ts#L21) of `bottom`, so maybe we don't need this logic.

<details>

<summary>Would it work to default the `position` prop to `bottom` like this?</summary>

```diff

diff --git a/packages/components/src/tooltip/index.tsx b/packages/components/src/tooltip/index.tsx
index 2dbfc5fcc4..26a0aea374 100644
--- a/packages/components/src/tooltip/index.tsx
+++ b/packages/components/src/tooltip/index.tsx
@@ -21,8 +21,16 @@ import { positionToPlacement } from '../popover/utils';
  */
 export const TOOLTIP_DELAY = 700;
 
+const DEFAULT_PLACEMENT = 'bottom';
+
 function Tooltip( props: TooltipProps ) {
-	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		position = DEFAULT_PLACEMENT,
+		shortcut,
+		text,
+	} = props;
 
 	const baseId = useInstanceId( Tooltip, 'tooltip' );
 	const describedById = text || shortcut ? baseId : undefined;
@@ -38,17 +46,13 @@ function Tooltip( props: TooltipProps ) {
 		}
 	}
 
-	const DEFAULT_PLACEMENT = 'bottom';
-
 	const tooltipStore = Ariakit.useTooltipStore( {
 		//@ts-ignore
 		placement:
-			// Compute tooltip's placement:
-			// - compute it from the legacy `position` prop (if defined)
-			// - otherwise, fallback to the DEFAULT_PLACEMENT.
-			position === undefined
-				? DEFAULT_PLACEMENT
-				: positionToPlacement( position ),
+			positionToPlacement( position ),
 		timeout: delay,
 	} );
 
```

</details>

Granted, that might make it more complex in the future if we wanted to _change_ the `DEFAULT_PLACEMENT` value to something `positionToPlacement` didn't handle cleanly, but I don't know what the likelihood of that might be.

#### brookewp on 2023-09-02T01:10:40Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1313673672
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,88 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const { children, delay = TOOLTIP_DELAY, position, shortcut, text } = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const DEFAULT_PLACEMENT = 'bottom';
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		//@ts-ignore
+		placement:
+			// Compute tooltip's placement:
+			// - compute it from the legacy `position` prop (if defined)
+			// - otherwise, fallback to the DEFAULT_PLACEMENT.
+			position === undefined
+				? DEFAULT_PLACEMENT
+				: positionToPlacement( position ),
+		timeout: delay,
```

This is a good point! The logic made more sense when we had `placement` **and** `position,` but we removed `placement` for the MVP (and will start the deprecation of `position` in the next PR). For now, I agree it's unnecessary, and it makes sense to simplify it in this PR and then reassess it in the next PR.

### Review thread 42

#### chad1008 on 2023-09-01T16:27:18Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1313252500
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,285 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
```

I know this test isn't new to this PR, so this comment may be better for a followup, but I don't feel like this test actually does what we want it to. It tests that _some_ delay is being respected, but not specifically the custom delay. If I run this test without that `+ 25` it still passes, because it checks right away, sees no tooltip and then just `awaits` an undefined amount of time to check again. If we want to truly test the custom delay prop, we may want to wait for the duration default delay for the first assertion, then wait whatever additional time is needed for the custom delay to expire.

<details>
<summary>Super rough example, I don't don't know if this is the best way to implement the timers:</summary>

```diff
diff --git a/packages/components/src/tooltip/test/index.tsx b/packages/components/src/tooltip/test/index.tsx
index f450a5addd..2723f14d79 100644
--- a/packages/components/src/tooltip/test/index.tsx
+++ b/packages/components/src/tooltip/test/index.tsx
@@ -16,6 +16,7 @@ import Button from '../../button';
 import Modal from '../../modal';
 import Tooltip, { TOOLTIP_DELAY } from '..';
 import cleanupTooltip from './utils/';
+import { act } from 'react-dom/test-utils';
 
 const props = {
 	children: <Button>Button</Button>,
@@ -98,18 +99,28 @@ describe( 'Tooltip', () => {
 
 	it( 'should respect custom delay prop when showing tooltip', async () => {
 		const user = userEvent.setup();
-		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+		const ADDITIONAL_DELAY = 100;
 
-		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+		render(
+			<Tooltip { ...props } delay={ TOOLTIP_DELAY + ADDITIONAL_DELAY } />
+		);
 
 		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
 
+		await new Promise( ( r ) => setTimeout( r, TOOLTIP_DELAY ) )
+
 		expect(
 			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
 		).not.toBeInTheDocument();
 
+		await act(
+			() => new Promise( ( r ) => setTimeout( r, ADDITIONAL_DELAY ) )
+		);
+
 		expect(
-			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
 		).toBeVisible();
 
 		await cleanupTooltip( user );

```

</details>

If that second timer is shortened, the test will fail because the custom delay hasn't elapsed yet.

#### brookewp on 2023-09-02T01:42:34Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1313683302
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,285 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
```

I thought about this too, so I'm glad you brought it up! I thought it was testing that the custom delay passed, but I'm not sure how valuable it is just like that. 🤔 

We have a very similar test to make sure the tooltip doesn't show if the anchor leaves before the delay time, so along with your suggestions, we can also borrow from that: https://github.com/WordPress/gutenberg/blob/50b7f5dc20dc0393a04ffe10a21e5c48d512a652/packages/components/src/tooltip/test/index.tsx#L141

I'm not sure if it's better to wrap the promise in `waitFor` rather than `act`, but we can do that to avoid adding another method, and `waitFor` is already part of other tests. 

https://github.com/WordPress/gutenberg/blob/ed4c6aa3ae689cf15343d5c3f2cd19f7acc3669e/packages/components/src/tooltip/test/index.tsx#L119-L125

#### ciampo on 2023-09-04T13:23:44Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1314945363
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,285 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
```

Is the `waitFor` wrap necessary? In other instances in the same file, we've simply `await`ed for the promise, without `waitFor` or `act`

#### brookewp on 2023-09-05T19:14:16Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1316299288
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,285 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
```

If we use just `await` without `waitFor` in this case, we get `act()` warnings: 

>    `When testing, code that causes React state updates should be wrapped into act(...):`

But this is a bit of an odd test, and I'm still unsure of its value. When first looking at this test, it makes sense to incrementally advance the time to ensure it reaches the custom delay and doesn't display the tooltip before that. 

But if we set a custom delay on tooltip, expect to find the tooltip, and it passes, it seems that would be enough without manually setting timeouts. 

We could remove the second promise and use `findBy` which passes: 

```diff
--- a/packages/components/src/tooltip/test/index.tsx
+++ b/packages/components/src/tooltip/test/index.tsx
@@ -116,16 +116,8 @@ describe( 'Tooltip', () => {
 			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
 		).not.toBeInTheDocument();
 
-		// wait for additional delay for tooltip to appear
-		await waitFor(
-			() =>
-				new Promise( ( resolve ) =>
-					setTimeout( resolve, ADDITIONAL_DELAY )
-				)
-		);
-
 		expect(
-			screen.getByRole( 'tooltip', { name: /tooltip text/i } )
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
 		).toBeVisible();
 
 		await cleanupTooltip( user );
```

But we could also remove both promises, achieve the same result, and return to where it started. So I wonder: 

1. Is it valuable as it is with timeouts?
2. As it was without them?
3. Or do we need to find another way to test it? Like maybe checking if the time passed when tooltip appears is the same as the custom delay?

#### ciampo on 2023-09-06T13:11:26Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1317262263
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,285 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
```

Let's not delay this PR further and keep the test as-is now, we can always make improvements in a follow-up PR.

Cc'ing @jsnajdr if/when he has a moment to take a look at this, since he's already worked on fixing a few other aspects of tooltip unit tests

#### jsnajdr on 2023-09-07T09:01:16Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1318309715
Path: packages/components/src/tooltip/test/index.tsx

```diff
@@ -0,0 +1,285 @@
+/**
+ * External dependencies
+ */
+import { render, screen, waitFor } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+
+/**
+ * WordPress dependencies
+ */
+import { shortcutAriaLabel } from '@wordpress/keycodes';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import Modal from '../../modal';
+import Tooltip, { TOOLTIP_DELAY } from '..';
+import cleanupTooltip from './utils/';
+
+const props = {
+	children: <Button>Button</Button>,
+	text: 'tooltip text',
+};
+
+describe( 'Tooltip', () => {
+	it( 'should not render the tooltip if multiple children are passed', async () => {
+		render(
+			// expected TS error since Tooltip cannot have more than one child element
+			// @ts-expect-error
+			<Tooltip { ...props }>
+				<Button>This is a button</Button>
+				<Button>This is another button</Button>
+			</Tooltip>
+		);
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should not render the tooltip if there is no focus', () => {
+		render( <Tooltip { ...props } /> );
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toBeVisible();
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+	} );
+
+	it( 'should render the tooltip when focusing on the tooltip anchor via tab', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.tab();
+
+		expect(
+			screen.getByRole( 'button', { name: /Button/i } )
+		).toHaveFocus();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should render the tooltip when the tooltip anchor is hovered', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should not show tooltip on focus as result of mouse click', async () => {
+		const user = userEvent.setup();
+
+		render( <Tooltip { ...props } /> );
+
+		await user.click( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		await cleanupTooltip( user );
+	} );
+
+	it( 'should respect custom delay prop when showing tooltip', async () => {
+		const user = userEvent.setup();
+		const CUSTOM_DELAY = TOOLTIP_DELAY + 25;
+
+		render( <Tooltip { ...props } delay={ CUSTOM_DELAY } /> );
+
+		await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );
+
+		expect(
+			screen.queryByRole( 'tooltip', { name: /tooltip text/i } )
+		).not.toBeInTheDocument();
+
+		expect(
+			await screen.findByRole( 'tooltip', { name: /tooltip text/i } )
+		).toBeVisible();
+
+		await cleanupTooltip( user );
+	} );
```

If we expect React update inside a timeout, it must be wrapped in `act()`. Like:
```js
render( <Tooltip delay={ TOOLTIP_DELAY } /> );
await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );

// an instant before timeout
await sleep( TOOLTIP_DELAY - EPSILON );
expect( screen.queryByRole( 'tooltip' ) ) .not.toBeInTheDocument();

// an instant after timeout
await act( () => sleep( 2 * EPSILON ) );
expect( screen.getByRole( 'tooltip' ) ).toBeInTheDocument();
```
This test is passing for me without any warnings.

It's also a good idea to use fake timers here and advance them manually. Makes the test more reliable and slightly faster, as it doesn't need to wait the real 700ms:
```js
render( <Tooltip delay={ TOOLTIP_DELAY } /> );
await user.hover( screen.getByRole( 'button', { name: /Button/i } ) );

jest.advanceTimersByTime( TOOLTIP_DELAY - EPSILON );
expect( screen.queryByRole( 'tooltip' ) ) .not.toBeInTheDocument();

await act( async () => jest.advanceTimersByTime( 2 * EPSILON ) );
expect( screen.getByRole( 'tooltip' ) ).toBeInTheDocument();
```
But when I tried this fake timers approach I ran into big trouble, because an `act` warning suddenly appears. It's caused by Ariakit's internal usage of Floating UI. There is a `setState` call similar to this:
```js
useEffect( () => {
  setPositioned( true );
}
```
Which sets state that's already `true` to `true`. Therefore it never has any observable effect, but the `act` warning is triggered by the `setState` being merely scheduled, no matter what is the result. I don't know how to fix it except trying random variants of `await act( () => waitForMicrotaskOrTimeoutOrAnimFrame() )`.

It's this line in Ariakit: https://github.com/ariakit/ariakit/blob/main/packages/ariakit-react-core/src/popover/popover.tsx#L294

Floating UI's native `update` function is very careful to not set the state unless it's sure that it's different:

https://github.com/floating-ui/floating-ui/blob/master/packages/react-dom/src/useFloating.ts#L101-L107

It could help if Ariakit started doing this in its `updatePosition2`, too. Cc @diegohaz

### Review thread 43 (resolved)

#### brookewp on 2023-09-02T02:26:48Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1313693144
Path: packages/components/src/tooltip/types.ts

```diff
@@ -0,0 +1,44 @@
+/**
+ * External dependencies
+ */
+import type * as Ariakit from '@ariakit/react/tooltip';
+/**
+ * Internal dependencies
+ */
+import type { PopoverProps } from '../popover/types';
+import type { ShortcutProps } from '../shortcut/types';
+
+export type TooltipProps = {
+	/**
+	 * The anchor for the tooltip.
+	 *
+	 * **Note**: Accepts only one child element.
+	 */
+	children: Ariakit.TooltipProps[ 'render' ];
```

When I add more than one child, I do not see a type error, but we get one in our test for it:

https://github.com/WordPress/gutenberg/blob/cf59cd437ac97551491af8228ee9aa8563e2870d/packages/components/src/tooltip/test/index.tsx#L26-L29

I'm not sure why that is. @chad1008 or @ciampo, do you have any insight here?

#### ciampo on 2023-09-04T13:17:02Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1314938729
Path: packages/components/src/tooltip/types.ts

```diff
@@ -0,0 +1,44 @@
+/**
+ * External dependencies
+ */
+import type * as Ariakit from '@ariakit/react/tooltip';
+/**
+ * Internal dependencies
+ */
+import type { PopoverProps } from '../popover/types';
+import type { ShortcutProps } from '../shortcut/types';
+
+export type TooltipProps = {
+	/**
+	 * The anchor for the tooltip.
+	 *
+	 * **Note**: Accepts only one child element.
+	 */
+	children: Ariakit.TooltipProps[ 'render' ];
```

> we get one in our test for it

The one that we get in our tests is working as expected (the `@ts-expect-error` is correct).

Where else have you tried adding more than one child? Can you share an example?

#### brookewp on 2023-09-05T19:38:10Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1316319669
Path: packages/components/src/tooltip/types.ts

```diff
@@ -0,0 +1,44 @@
+/**
+ * External dependencies
+ */
+import type * as Ariakit from '@ariakit/react/tooltip';
+/**
+ * Internal dependencies
+ */
+import type { PopoverProps } from '../popover/types';
+import type { ShortcutProps } from '../shortcut/types';
+
+export type TooltipProps = {
+	/**
+	 * The anchor for the tooltip.
+	 *
+	 * **Note**: Accepts only one child element.
+	 */
+	children: Ariakit.TooltipProps[ 'render' ];
```

>Where else have you tried adding more than one child? Can you share an example?

Ignore me! I just realized it was in a `.js` file 🤦‍♀️

### Review thread 44 (resolved)

#### ciampo on 2023-09-04T13:28:55Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1314950763
Path: packages/components/src/tooltip/README.md

```diff
@@ -1,8 +1,8 @@
-# Tooltip
+# ToolTip
 
-Tooltip is a React component to render floating help text relative to a node when it receives focus or when the user places the mouse cursor atop it. If the tooltip exceeds the bounds of the page in the direction it opens, its position will be flipped automatically.
+Tooltip is a React component to render floating help text relative to a node when it receives focus or it is hovered upon by a mouse. If the tooltip exceeds the bounds of the page in the direction it opens, its position will be flipped automatically.
 
-Accessibility note: the tooltip text is hidden from screen readers and assistive technologies that understand ARIA. To make it accessible, use an `aria-label` attribute on the same element the tooltip is applied to, preferably using the same text used for the tooltip.
+This component utilizes [Ariakit's tooltip](https://ariakit.org/components/tooltip) which is based on the [WAI-ARIA Tooltip Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/).
```

Should we omit the fact that we use `Ariakit` and keep that as an internal implementation detail? Just a thought.

### Review thread 45 (resolved)

#### ciampo on 2023-09-04T13:32:07Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1314954044
Path: packages/components/src/tooltip/types.ts

```diff
@@ -0,0 +1,44 @@
+/**
+ * External dependencies
+ */
+import type * as Ariakit from '@ariakit/react/tooltip';
+/**
+ * Internal dependencies
+ */
+import type { PopoverProps } from '../popover/types';
+import type { ShortcutProps } from '../shortcut/types';
+
+export type TooltipProps = {
+	/**
+	 * The anchor for the tooltip.
+	 *
+	 * **Note**: Accepts only one child element.
+	 */
+	children: Ariakit.TooltipProps[ 'render' ];
```

The `render` prop from `Ariakit` equals to

```ts
RenderProp | React.ReactElement
```

A couple of questions:

1. Do we intend to support the render prop case too in our `Tooltip`?
2. In any case, we'd need to update the README, since it currently says that `children` has type `Element`

#### brookewp on 2023-09-05T20:27:44Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1316373600
Path: packages/components/src/tooltip/types.ts

```diff
@@ -0,0 +1,44 @@
+/**
+ * External dependencies
+ */
+import type * as Ariakit from '@ariakit/react/tooltip';
+/**
+ * Internal dependencies
+ */
+import type { PopoverProps } from '../popover/types';
+import type { ShortcutProps } from '../shortcut/types';
+
+export type TooltipProps = {
+	/**
+	 * The anchor for the tooltip.
+	 *
+	 * **Note**: Accepts only one child element.
+	 */
+	children: Ariakit.TooltipProps[ 'render' ];
```

Thanks for pointing this out! I don't think it makes sense to support it, especially now, with our intention to match legacy. I think this resulted from my novice-level understanding of Typescript, but it is clearer to me now. I've updated this to `React.ReactElement` and the README to match. 🙂

### Review thread 46

#### ciampo on 2023-09-06T13:12:28Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1317263468
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,86 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		position = 'bottom',
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// @ts-ignore
```

I think we should be able to remove this comment :)

#### brookewp on 2023-09-06T17:57:39Z

URL: https://github.com/WordPress/gutenberg/pull/48440#discussion_r1317644872
Path: packages/components/src/tooltip/index.tsx

```diff
@@ -0,0 +1,86 @@
+/**
+ * External dependencies
+ */
+import * as Ariakit from '@ariakit/react/tooltip';
+
+/**
+ * WordPress dependencies
+ */
+import { useInstanceId } from '@wordpress/compose';
+import { Children } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import type { TooltipProps } from './types';
+import Shortcut from '../shortcut';
+import { positionToPlacement } from '../popover/utils';
+
+/**
+ * Time over anchor to wait before showing tooltip
+ */
+export const TOOLTIP_DELAY = 700;
+
+function Tooltip( props: TooltipProps ) {
+	const {
+		children,
+		delay = TOOLTIP_DELAY,
+		position = 'bottom',
+		shortcut,
+		text,
+	} = props;
+
+	const baseId = useInstanceId( Tooltip, 'tooltip' );
+	const describedById = text || shortcut ? baseId : undefined;
+
+	const isOnlyChild = Children.count( children ) === 1;
+	// console error if more than one child element is added
+	if ( ! isOnlyChild ) {
+		if ( 'development' === process.env.NODE_ENV ) {
+			// eslint-disable-next-line no-console
+			console.error(
+				'Tooltip should be called with only a single child element.'
+			);
+		}
+	}
+
+	const tooltipStore = Ariakit.useTooltipStore( {
+		// @ts-ignore
```

Ah yes, good catch!

## Changed files

- packages/base-styles/_z-index.scss (MODIFIED, +0/-3)
- packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap (MODIFIED, +1/-0)
- packages/block-editor/src/components/link-control/test/index.js (MODIFIED, +12/-4)
- packages/components/CHANGELOG.md (MODIFIED, +2/-0)
- packages/components/src/button/test/index.tsx (MODIFIED, +11/-4)
- packages/components/src/color-palette/test/index.tsx (MODIFIED, +5/-1)
- packages/components/src/spinner/index.tsx (MODIFIED, +1/-0)
- packages/components/src/tab-panel/test/index.tsx (MODIFIED, +14/-9)
- packages/components/src/toggle-group-control/test/__snapshots__/index.tsx.snap (MODIFIED, +4/-0)
- packages/components/src/toggle-group-control/test/index.tsx (MODIFIED, +3/-2)
- packages/components/src/toggle-group-control/types.ts (MODIFIED, +2/-1)
- packages/components/src/tooltip/README.md (MODIFIED, +16/-29)
- packages/components/src/tooltip/index.js (DELETED, +0/-293)
- packages/components/src/tooltip/index.tsx (ADDED, +85/-0)
- packages/components/src/tooltip/stories/index.story.js (DELETED, +0/-85)
- packages/components/src/tooltip/stories/index.story.tsx (ADDED, +59/-0)
- packages/components/src/tooltip/style.scss (MODIFIED, +2/-27)
- packages/components/src/tooltip/test/index.js (DELETED, +0/-309)
- packages/components/src/tooltip/test/index.tsx (ADDED, +301/-0)
- packages/components/src/tooltip/test/utils/index.tsx (ADDED, +20/-0)
- packages/components/src/tooltip/types.ts (ADDED, +40/-0)
- packages/editor/src/components/post-publish-panel/test/__snapshots__/index.js.snap (MODIFIED, +1/-0)
- packages/editor/src/components/post-saved-state/test/index.js (MODIFIED, +3/-1)
