---
number: 35211
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/35211
matched_labels: ["[Package] Components"]
all_labels: ["[Status] Needs More Info","[Status] Stale","[Package] Components"]
---

# Issue #35211: Wrapping TextControl in Tooltip results in error

- URL: https://github.com/WordPress/gutenberg/issues/35211
- Author: Lovor01
- Created: 2021-09-29T09:28:36Z
- Updated: 2022-01-12T08:16:04Z
- Comments: 3 of 3

## Body

### Description

Having this fragment of code in Edit function of the block:

```jsx
<Tooltip text="Klikni na prazan element za opcije ili počni tipkati">
	<TextControl
		className="prominent"
		value={ attributes.prominent }
		onChange={ ( prominent ) => setAttributes( { prominent } ) }
		// help="Upiši grad u kojem se održava događaj"
		placeholder="Istakni..."
		list="prominent-list"
		style={{color: attributes.prominentColor}}
	/>
</Tooltip>
```
gives this error:
```
The above error occurred in the <input> component:
    in input (created by ForwardRef(TextControl))
    in div (created by Context.Consumer)
    in StyledField (created by BaseControl)
    in div (created by Context.Consumer)
    in Wrapper (created by BaseControl)
    in BaseControl (created by ForwardRef(TextControl))
    in ForwardRef(TextControl) (created by Edit)
    in tooltip_Tooltip (created by Edit)
    in div (created by Edit)
    in div (created by Edit)
    in Edit (created by edit_Edit)
    in edit_Edit (created by WithToolbarControls(edit_Edit))
    in WithToolbarControls(edit_Edit) (created by WithInspectorControl(WithToolbarControls(edit_Edit)))
    in WithInspectorControl(WithToolbarControls(edit_Edit)) (created by WithInspectorControl(WithInspectorControl(WithToolbarControls(edit_Edit))))
    in WithInspectorControl(WithInspectorControl(WithToolbarControls(edit_Edit))) (created by WithToolbarControls(WithInspectorControl(WithInspectorControl(WithToolbarControls(edit_…
```
It is `Tooltip` component that causes error, without it it works fine.
I expect adding tooltips to `TextControl` being common necessity. I resolved problem temporarily by adding title attribute, but with it I do not have so much control.

### Step-by-step reproduction instructions

Enter above code in Edit component of the block.

### Screenshots, screen recording, code snippet

_No response_

### Environment info

_No response_

### Please confirm that you have searched existing issues in the repo.

Yes

### Please confirm that you have tested with all plugins deactivated except Gutenberg.

Yes

## Issue comments

### talldan on 2021-10-12T07:27:45Z

URL: https://github.com/WordPress/gutenberg/issues/35211#issuecomment-940740154

@Lovor01 It looks like you didn't include the error when copy/pasting. It just says 'The above error occurred in the <input> component'. Would you be able to check the console for the error? Thanks.

### github-actions on 2021-10-28T01:47:33Z

URL: https://github.com/WordPress/gutenberg/issues/35211#issuecomment-953434326

Help us move this issue forward. This issue is being marked stale since it has no activity after 15 days of requesting more information. Please add info requested so we can help move the issue forward. Note: The triage policy is to close stale issues that need more info and no response after 2 weeks.

### devinmaeztri on 2022-01-12T08:16:03Z

URL: https://github.com/WordPress/gutenberg/issues/35211#issuecomment-1010759287

Hi! I'm closing this issue as the author @Lovor01 hasn't responded in 2+ weeks. It can be reopened when needed. Thanks!

