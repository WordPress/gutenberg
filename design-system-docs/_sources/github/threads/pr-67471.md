---
number: 67471
type: pull_request
state: MERGED
merged_at: 2024-12-02T16:02:52Z
url: https://github.com/WordPress/gutenberg/pull/67471
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #67471: SlotFill: remove explicit rerender from portal version

- URL: https://github.com/WordPress/gutenberg/pull/67471
- Author: jsnajdr
- Created: 2024-12-02T11:55:23Z
- Updated: 2024-12-03T08:56:55Z
- Comments: 3 of 3
- Review threads: 5 of 5
- Changed files: 5 of 5
- Diff: ../pr-diffs/67471.diff

## Body

Simplifies the `SlotFill` implementation (the portal "bubbles virtually" version by removing explicit `rerender` API and its calls from `Fill`. After this patch the rerender happens automatically when the slot in the `slots` `observableMap` is changed with the `slots.set( name, ... )` call.

**How to test:**
All unit tests and e2e tests should pass.

## Issue comments

### github-actions on 2024-12-02T11:55:43Z

URL: https://github.com/WordPress/gutenberg/pull/67471#issuecomment-2511335236

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-12-02T14:31:29Z

URL: https://github.com/WordPress/gutenberg/pull/67471#issuecomment-2511700595

<!-- flaky-tests-report-comment -->
**Flaky tests detected in efbd839637f89cc238327a35a6b38f4c740f30d1.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/12121082461
📝  Reported issues:
- #36677 in `/test/e2e/specs/site-editor/template-revert.spec.js`
- #61884 in `/test/e2e/specs/editor/blocks/navigation-frontend-interactivity.spec.js`

### Mamaduka on 2024-12-03T08:56:54Z

URL: https://github.com/WordPress/gutenberg/pull/67471#issuecomment-2513910541

Thank you, @jsnajdr!

## Review threads

### Review thread 1

#### jsnajdr on 2024-12-02T11:56:23Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865722671
Path: packages/components/src/slot-fill/bubbles-virtually/fill.tsx

```diff
@@ -20,18 +19,15 @@ import type { FillComponentProps } from '../types';
 export default function Fill( { name, children }: FillComponentProps ) {
 	const registry = useContext( SlotFillContext );
 	const slot = useObservableValue( registry.slots, name );
-	const [ , rerender ] = useReducer( () => [], [] );
-	const ref = useRef( { rerender } );
+	const instanceRef = useRef( {} );
```

The `rerender` API goes away and the fill now registers only an unique instance object. I'm renaming it from `ref` to `instance` when registering, something that @tyxla asked for in one of the previous PRs.

#### tyxla on 2024-12-02T13:52:26Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865889398
Path: packages/components/src/slot-fill/bubbles-virtually/fill.tsx

```diff
@@ -20,18 +19,15 @@ import type { FillComponentProps } from '../types';
 export default function Fill( { name, children }: FillComponentProps ) {
 	const registry = useContext( SlotFillContext );
 	const slot = useObservableValue( registry.slots, name );
-	const [ , rerender ] = useReducer( () => [], [] );
-	const ref = useRef( { rerender } );
+	const instanceRef = useRef( {} );
```

Thanks!

### Review thread 2

#### jsnajdr on 2024-12-02T11:57:03Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865723540
Path: packages/components/src/slot-fill/bubbles-virtually/fill.tsx

```diff
@@ -20,18 +19,15 @@ import type { FillComponentProps } from '../types';
 export default function Fill( { name, children }: FillComponentProps ) {
 	const registry = useContext( SlotFillContext );
 	const slot = useObservableValue( registry.slots, name );
-	const [ , rerender ] = useReducer( () => [], [] );
-	const ref = useRef( { rerender } );
+	const instanceRef = useRef( {} );
 
+	// We register fills so we can keep track of their existence.
+	// Slots can use the `useSlotFills` hook to know if there're already fills
+	// registered so they can choose to render themselves or not.
 	useEffect( () => {
-		// We register fills so we can keep track of their existence.
-		// Some Slot implementations need to know if there're already fills
-		// registered so they can choose to render themselves or not.
-		const refValue = ref.current;
-		registry.registerFill( name, refValue );
-		return () => {
-			registry.unregisterFill( name, refValue );
-		};
+		const instance = instanceRef.current;
+		registry.registerFill( name, instance );
+		return () => registry.unregisterFill( name, instance );
```

This effect stays the same except renaming the `ref` variable to `instanceRef` etc.

### Review thread 3

#### jsnajdr on 2024-12-02T11:58:04Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865724767
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -23,13 +23,7 @@ function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
 		ref,
 		fillProps
 	) => {
-		const slot = slots.get( name );
-
-		slots.set( name, {
-			...slot,
-			ref: ref || slot?.ref,
-			fillProps: fillProps || slot?.fillProps || {},
-		} );
+		slots.set( name, { ref, fillProps } );
```

A drive-by change removing redundant code. All the `registerSlot` calls always include both the `ref` and `fillProps` values. Inheriting them from the previous `slot` value has no effect.

#### tyxla on 2024-12-02T13:52:39Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865889796
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -23,13 +23,7 @@ function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
 		ref,
 		fillProps
 	) => {
-		const slot = slots.get( name );
-
-		slots.set( name, {
-			...slot,
-			ref: ref || slot?.ref,
-			fillProps: fillProps || slot?.fillProps || {},
-		} );
+		slots.set( name, { ref, fillProps } );
```

Makes sense 👍

### Review thread 4

#### jsnajdr on 2024-12-02T11:59:34Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865726630
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -66,12 +60,7 @@ function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
 			return;
 		}
 
-		slot.fillProps = fillProps;
-		const slotFills = fills.get( name );
-		if ( slotFills ) {
-			// Force update fills.
-			slotFills.forEach( ( fill ) => fill.rerender() );
-		}
+		slots.set( name, { ref, fillProps } );
```

This is the key change in this PR! Instead of silently updating a field with `slots.get( name ).fillProps = fillProps`, we call `slots.set`. That sets a new value and triggers calling all `observableMap` listeners, leading to automatic rerendering of all fills.

#### tyxla on 2024-12-02T13:52:55Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865890154
Path: packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx

```diff
@@ -66,12 +60,7 @@ function createSlotRegistry(): SlotFillBubblesVirtuallyContext {
 			return;
 		}
 
-		slot.fillProps = fillProps;
-		const slotFills = fills.get( name );
-		if ( slotFills ) {
-			// Force update fills.
-			slotFills.forEach( ( fill ) => fill.rerender() );
-		}
+		slots.set( name, { ref, fillProps } );
```

Much clearer and more concise, looks great 🚀

### Review thread 5

#### jsnajdr on 2024-12-02T12:02:43Z

URL: https://github.com/WordPress/gutenberg/pull/67471#discussion_r1865730740
Path: packages/components/src/slot-fill/bubbles-virtually/slot.tsx

```diff
@@ -54,11 +53,9 @@ function Slot(
 	}, [ fillProps ] );
 
 	useLayoutEffect( () => {
-		registerSlot( name, ref, fillPropsRef.current );
-		return () => {
-			unregisterSlot( name, ref );
-		};
-	}, [ registerSlot, unregisterSlot, name ] );
+		registry.registerSlot( name, ref, fillPropsRef.current );
+		return () => registry.unregisterSlot( name, ref );
+	}, [ registry, name ] );
```

A mere style change: don't destructure fields of `registry`. Simplifies the hook dependencies.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/slot-fill/bubbles-virtually/fill.tsx (MODIFIED, +7/-11)
- packages/components/src/slot-fill/bubbles-virtually/slot-fill-provider.tsx (MODIFIED, +2/-13)
- packages/components/src/slot-fill/bubbles-virtually/slot.tsx (MODIFIED, +4/-7)
- packages/components/src/slot-fill/types.ts (MODIFIED, +4/-3)
