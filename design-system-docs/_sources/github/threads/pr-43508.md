---
number: 43508
type: pull_request
state: MERGED
merged_at: 2022-09-08T06:54:26Z
url: https://github.com/WordPress/gutenberg/pull/43508
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #43508: `Disabled`: preserve input values when toggling the `isDisabled` prop

- URL: https://github.com/WordPress/gutenberg/pull/43508
- Author: torounit
- Created: 2022-08-23T09:16:53Z
- Updated: 2022-09-08T06:55:07Z
- Comments: 2 of 2
- Review threads: 3 of 3
- Changed files: 4 of 4
- Diff: ../pr-diffs/43508.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->
There is a bug in the `Disabled` component where changing `isDisabled` resets the form value. This has been fixed.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

https://github.com/WordPress/gutenberg/pull/42708#pullrequestreview-1078596447


## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

## Testing Instructions

1. `npm run storybook:dev`
1. Go to localhost:50240?path=/story/components-disabled--default
1. Change `isDisabled` prop to `false` in controls.
1. Change text box or select.
1. Change `isDisabled` prop to `true` in controls.

## Screenshots or screencast <!-- if applicable -->

https://user-images.githubusercontent.com/1908815/186121476-d5e32793-671c-4aca-8df3-c4cf31131622.mov

## Issue comments

### torounit on 2022-08-23T14:37:40Z

URL: https://github.com/WordPress/gutenberg/pull/43508#issuecomment-1224168756

@ciampo Certainly a unit test is needed. I will add it.

### torounit on 2022-09-05T04:27:44Z

URL: https://github.com/WordPress/gutenberg/pull/43508#issuecomment-1236528550

@ciampo Thanks for reviewing! Sorry for the delay in responding.

I have committed the fixed version.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-08-23T14:22:20Z

URL: https://github.com/WordPress/gutenberg/pull/43508#discussion_r952694973
Path: packages/components/CHANGELOG.md

```diff
@@ -16,6 +16,7 @@
 -   `Popover`: make sure offset middleware always applies the latest frame offset values ([#43329](https://github.com/WordPress/gutenberg/pull/43329/)).
 -   `Dropdown`: anchor popover to the dropdown wrapper (instead of the toggle) ([#43377](https://github.com/WordPress/gutenberg/pull/43377/)).
 -   `Guide`: Fix error when rendering with no pages ([#43380](https://github.com/WordPress/gutenberg/pull/43380/)).
+-   `Disabled`: Fixed so that the internal value remains when isDisabled is changed ([#43508](https://github.com/WordPress/gutenberg/pull/43508/))
```

```suggestion
-   `Disabled`: preserve input values when toggling the `isDisabled` prop ([#43508](https://github.com/WordPress/gutenberg/pull/43508/))
```

### Review thread 2 (resolved)

#### ciampo on 2022-08-23T17:31:34Z

URL: https://github.com/WordPress/gutenberg/pull/43508#discussion_r952931880
Path: packages/components/src/disabled/test/index.tsx

```diff
@@ -134,6 +135,45 @@ describe( 'Disabled', () => {
 		);
 	} );
 
+	it( 'should preserve input values when toggling the isDisabled prop', async () => {
```

A few improvements for this new test:

- let's always use `user-event` instead of `fireEvent`
- let's use the `toHaveValue` and `toHaveTextContent` assertions
- let's use the `user.type` function (which takes care of simulating clicks and keyboard events in one line)

<details>
<summary>This is how we could apply these suggestions</summary>

```diff
diff --git a/packages/components/src/disabled/test/index.tsx b/packages/components/src/disabled/test/index.tsx
index 8123ac25ea..c0858d6c5f 100644
--- a/packages/components/src/disabled/test/index.tsx
+++ b/packages/components/src/disabled/test/index.tsx
@@ -1,7 +1,7 @@
 /**
  * External dependencies
  */
-import { render, screen, waitFor, fireEvent } from '@testing-library/react';
+import { render, screen, waitFor } from '@testing-library/react';
 
 /**
  * Internal dependencies
@@ -148,30 +148,24 @@ describe( 'Disabled', () => {
 
 		const { rerender } = render( <MaybeDisable isDisabled={ false } /> );
 
-		const getInputValue = () =>
-			screen.getByRole< HTMLInputElement >( 'textbox' ).value;
+		const input = screen.getByRole( 'textbox' );
+		const contentEditable = screen.getByTitle( 'edit my content' );
 
-		const getContentEditableValue = () =>
-			screen.getByTitle( 'edit my content' ).textContent;
+		await user.type( input, 'Input field' );
+		expect( input ).toHaveValue( 'Input field' );
 
-		const input = screen.getByRole< HTMLInputElement >( 'textbox' );
-		fireEvent.change( input, { target: { value: 'This is input.' } } );
-		expect( input.value ).toBe( 'This is input.' );
-
-		const contentEditable = screen.getByTitle( 'edit my content' );
-		await user.click( contentEditable );
-		await user.keyboard( 'This is contentEditable.' );
-		expect( contentEditable.textContent ).toBe(
-			'This is contentEditable.'
-		);
+		await user.type( contentEditable, 'contentEditable field' );
+		expect( contentEditable ).toHaveTextContent( 'contentEditable field' );
 
 		rerender( <MaybeDisable isDisabled={ true } /> );
-		expect( getInputValue() ).toBe( 'This is input.' );
-		expect( getContentEditableValue() ).toBe( 'This is contentEditable.' );
+
+		expect( input ).toHaveValue( 'Input field' );
+		expect( contentEditable ).toHaveTextContent( 'contentEditable field' );
 
 		rerender( <MaybeDisable isDisabled={ false } /> );
-		expect( getInputValue() ).toBe( 'This is input.' );
-		expect( getContentEditableValue() ).toBe( 'This is contentEditable.' );
+
+		expect( input ).toHaveValue( 'Input field' );
+		expect( contentEditable ).toHaveTextContent( 'contentEditable field' );
 	} );
 
 	describe( 'Consumer', () => {
```
</details>

### Review thread 3

#### torounit on 2022-09-05T04:23:58Z

URL: https://github.com/WordPress/gutenberg/pull/43508#discussion_r962465034
Path: packages/components/src/disabled/test/index.tsx

```diff
@@ -134,6 +135,43 @@ describe( 'Disabled', () => {
 		);
 	} );
 
+	it( 'should preserve input values when toggling the isDisabled prop', async () => {
+		const user = userEvent.setup( {
+			advanceTimers: jest.advanceTimersByTime,
+		} );
+
+		const MaybeDisable = ( { isDisabled = true } ) => (
+			<Disabled isDisabled={ isDisabled }>
+				<Form />
+			</Disabled>
+		);
+
+		const getInput = () => screen.getByRole( 'textbox' );
+		const getContentEditable = () => screen.getByTitle( 'edit my content' );
+
+		const { rerender } = render( <MaybeDisable isDisabled={ false } /> );
+
+		await user.type( getInput(), 'This is input.' );
+		expect( getInput() ).toHaveValue( 'This is input.' );
+
+		await user.type( getContentEditable(), 'This is contentEditable.' );
+		expect( getContentEditable() ).toHaveTextContent(
+			'This is contentEditable.'
+		);
+
+		rerender( <MaybeDisable isDisabled={ true } /> );
+		expect( getInput() ).toHaveValue( 'This is input.' );
+		expect( getContentEditable() ).toHaveTextContent(
+			'This is contentEditable.'
+		);
```

If an instance previously obtained by `screen.getByRole( 'textbox' )` is used, `rerender` is not reflected, so a new instance is obtained.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/disabled/index.tsx (MODIFIED, +26/-8)
- packages/components/src/disabled/styles/disabled-styles.tsx (MODIFIED, +2/-2)
- packages/components/src/disabled/test/index.tsx (MODIFIED, +38/-0)
