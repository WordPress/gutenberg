---
number: 79148
type: pull_request
state: MERGED
merged_at: 2026-06-12T14:38:21Z
url: https://github.com/WordPress/gutenberg/pull/79148
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Enhancement","[Package] Theme"]
---

# PR #79148: Theme: Enforce sRGB seed-color input contract for ThemeProvider

- URL: https://github.com/WordPress/gutenberg/pull/79148
- Author: ciampo
- Created: 2026-06-12T10:10:27Z
- Updated: 2026-06-12T14:38:58Z
- Comments: 5 of 5
- Review threads: 2 of 2
- Changed files: 6 of 6
- Diff: ../pr-diffs/79148.diff

## Body

## What?

See #77462 (point A6).

`ThemeProvider`'s `color.primary` / `color.background` seed colors are documented as sRGB strings (hex, `rgb()`/`rgba()`, CSS named colors). This makes the runtime match the docs: colors outside sRGB (e.g. `oklch()`) now throw a clear error instead of being accepted by accident.

## Why?

The accepted input set was an accidental side effect of which color spaces happened to be registered internally — not a deliberate contract.

<details>
<summary>Background</summary>

`clampToGamut` registers the `OKLCH` color space as a required workaround for an open upstream gamut-mapping limitation ([color-js/color.js#734](https://github.com/color-js/color.js/pull/734)). That registration is what incidentally made `oklch()` seed strings parse. So whether an input was accepted depended on registration order rather than a documented rule.

</details>

## How?

Validate the seed string at the single user-input chokepoint (`buildRamp`) and reject any color whose space isn't sRGB — deterministically, regardless of what's globally registered. `clampToGamut` and its `OKLCH` registration are left untouched.

<details>
<summary>Implementation notes</summary>

- New `assertValidSeedColor` helper parses the seed with the registered `sRGB` space and throws unless the parsed color's space id is in the allowlist `{ 'srgb' }`.
- This rejects `oklch()` either way: if `OKLCH` is registered, the parsed space id is `oklch` (≠ `srgb`); if not, parsing throws.
- Internal recursive callers pass color objects (not strings) to `clampToGamut`, so `buildRamp` is the correct boundary for string validation.
- Docs (`types.ts`, `README.md`) and a CHANGELOG entry updated accordingly.

</details>

## Testing Instructions

```
npm run test:unit packages/theme
```

<details>
<summary>What the new tests cover</summary>

`packages/theme/src/color-ramps/test/seed-input.test.ts`, exercised through the public `buildBgRamp` / `buildAccentRamp` / `buildRamp` entry points:

- **Accepted**: `#3858e9`, `#3858e94d`, `rgb(56,88,233)`, `rgb(56 88 233 / 0.3)`, `rgba(56,88,233,0.3)`, `blue`, `transparent`.
- **Rejected**: `oklch(0.7 0.15 250)`, `hsl(230 80% 56%)`, `lab(50% 40 59)`, `hwb(230 10% 20%)`, `color(display-p3 1 0 0)`, `''`, `not-a-color`.
- **Determinism regression**: with `OKLCH` explicitly registered in the test, `oklch(...)` is still rejected.

Existing snapshot tests still pass.

</details>

## Use of AI Tools

This PR was authored with AI assistance (Cursor). All changes were reviewed by a human.

## Issue comments

### github-actions on 2026-06-12T10:19:27Z

URL: https://github.com/WordPress/gutenberg/pull/79148#issuecomment-4690138806

**Size Change:** +146 B (0%) 

**Total Size:** 8.59 MB

<details open><summary>📦 <strong>View Changed</strong></summary>

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/scripts/theme/index.min.js` | 22.1 kB | +146 B (+0.66%) |

</details>

<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-12T12:59:38Z

URL: https://github.com/WordPress/gutenberg/pull/79148#issuecomment-4691473278

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-12T13:46:13Z

URL: https://github.com/WordPress/gutenberg/pull/79148#issuecomment-4691845730

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 0885832fca48c2fa3c86ad46bd3782357a18b360.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27420025732
📝  Reported issues:
- #78753 in `/test/e2e/specs/editor/collaboration/http-only/collaboration-document-size-lock.spec.ts`

### aduth on 2026-06-12T13:51:14Z

URL: https://github.com/WordPress/gutenberg/pull/79148#issuecomment-4691889606

Out of curiosity, if we can get to a point where we don't need to register OKLCH (e.g. upstream fix), do we still expect to need and maintain this as separate function? Or could we depend on default behavior of colorjs handling of unregistered color spaces?

### ciampo on 2026-06-12T14:36:12Z

URL: https://github.com/WordPress/gutenberg/pull/79148#issuecomment-4692257849

I think we should keep the explicit allowlist even after a bug fix, because the OKLCH registration is incidental to why this exists. Even with an upstream fix, we'd still have to register sRGB ourselves (to accept `hex` / `rgb()` / named colors), and registration is shared global state that can be mutate by anyone.

I asked an AI agent to perform a local check, and it seems to confirm this theory:

`ColorSpace.registry` is a process-wide singleton shared by all `colorjs.io/fn` importers, and `parse()` resolves against it. I tested this locally, and the non-determinism is demonstrable without any third party: in normal operation, building any ramp registers OKLCH into that registry (via `clampToGamut`, the #734 workaround). After that, a bare `parse('oklch(0.7 0.15 250)')` succeeds with `spaceId: 'oklch'` — so if we relied on the default behavior, we'd start accepting `oklch()` seeds the moment the first ramp was built. Any other `/fn` consumer can register more spaces (HSL, lab, …), making those parseable too. The spaceId allowlist pins acceptance to sRGB regardless; if/when we drop the OKLCH registration, this check stays and we'd just simplify the comment that references it.

## Review threads

### Review thread 1

#### aduth on 2026-06-12T13:43:36Z

URL: https://github.com/WordPress/gutenberg/pull/79148#discussion_r3403778726
Path: packages/theme/src/color-ramps/test/seed-input.test.ts

Conventionally I'd expect unit test files to match a filename of functions under test, and the top-level `describe` blocks to map to those functions' behaviors. Maybe we could have a utility in `index.test.ts` to generate test cases that can be called from each of `buildBgRamp`, `buildAccentRamp`, `buildRamp` tests. I don't feel too strongly because this does keep related tests together, but the implementation feels a bit unconventional.

### Review thread 2

#### aduth on 2026-06-12T13:48:00Z

URL: https://github.com/WordPress/gutenberg/pull/79148#discussion_r3403805553
Path: packages/theme/src/color-ramps/lib/color-utils.ts

```diff
@@ -35,6 +38,37 @@ export function getContrast(
 	return contrastWCAG21( colorA, colorB );
 }
 
+/**
+ * Assert that a seed-color string is sRGB-parseable (hex, `rgb()`/`rgba()`, or
+ * a CSS named color), throwing otherwise.
+ *
+ * Rejection is deterministic regardless of which `ColorSpace`s are globally
+ * registered: `clampToGamut` registers `OKLCH`, which would otherwise make
+ * `oklch()` strings parse, but their space id is `oklch` (not `srgb`) so they
+ * are still rejected.
+ *
+ * @param seed The seed-color string to validate.
+ * @throws If `seed` is not an sRGB-parseable string.
+ */
+export function assertValidSeedColor( seed: string ): void {
+	ColorSpace.register( sRGB );
```

There's a bit of a disconnect in how we hard-code the colorspace registration here, but use a module-level constant for checking the spaceId below. 

Another thought could be something like...

```js
const ALLOWED_SEED_COLOR_SPACES = [ sRGB ];

// ...

ALLOWED_SEED_COLOR_SPACES.forEach( ( space ) => ColorSpace.register( space ) );

// ...

if ( ! ALLOWED_SEED_COLOR_SPACES.some( ( space ) => space.id === spaceId ) ) {
```

Or just drop the constant and hard-code the check for `'srgb'` spaceId 🤷

## Changed files

- packages/theme/CHANGELOG.md (MODIFIED, +4/-0)
- packages/theme/README.md (MODIFIED, +1/-1)
- packages/theme/src/color-ramps/lib/color-utils.ts (MODIFIED, +38/-0)
- packages/theme/src/color-ramps/lib/index.ts (MODIFIED, +10/-1)
- packages/theme/src/color-ramps/test/seed-input.test.ts (ADDED, +54/-0)
- packages/theme/src/types.ts (MODIFIED, +10/-4)
