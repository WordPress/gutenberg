---
number: 46004
type: pull_request
state: MERGED
merged_at: 2022-11-28T21:58:27Z
url: https://github.com/WordPress/gutenberg/pull/46004
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #46004: `TabPanel`: support manual tab activation

- URL: https://github.com/WordPress/gutenberg/pull/46004
- Author: ciampo
- Created: 2022-11-23T12:51:28Z
- Updated: 2022-11-30T21:10:01Z
- Comments: 8 of 8
- Review threads: 7 of 7
- Changed files: 5 of 5
- Diff: ../pr-diffs/46004.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

Closes #45390

## What?
<!-- In a few words, what is the PR actually doing? -->

Adds support for manual tab activation to the `TabPanel` component (see https://github.com/WordPress/gutenberg/issues/45390 and  [W3C docs](https://www.w3.org/WAI/ARIA/apg/patterns/tabpanel/) for more info)

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

New feature for the `TabPanel` component, for enhanced accessibility when necessary.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Introduced a new prop `hasManualTabActivation` (name TBD)
- The component keeps behaving as before when the new prop is not specified (backwards-compat)
- Added unit tests for both automatic and manual tab activation (including a necessary mock to test keyboard interactions)

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a Post or Page. -->
<!-- 2. Insert a Heading Block. -->
<!-- 3. etc. -->

- Load the default `TabPanel` example in Storybook
- Click on the first tab, then use arrow key to switch tab. Note how the currently shown tabpanel changes as the arrow keys are pressed
- Via Storybook controls, change the value of the `hasManualTabActivation` prop to `true`
- Click on the first tab, then use arrow keys to navigate through the list of tabs. Notice how focusing a tab doesn't cause the currently shown tab panel to change.
- Make sure that the tabpanel changes only when the space or enter keys are pressed while focusing a tab.

## Screenshots or screencast <!-- if applicable -->

https://user-images.githubusercontent.com/1083581/203551184-5f6f76d9-fa39-4128-bc72-dfd88d2b3f75.mp4

## Issue comments

### codesandbox on 2022-11-23T12:51:30Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1325009950

<img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.light.svg#gh-dark-mode-only" /><img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.svg#gh-light-mode-only" />&nbsp; Open in CodeSandbox <a href="https://codesandbox.io/p/github/WordPress/gutenberg/feat/tab-panel-manual-tab-activation?workspace=%7B%22gitSidebarPanel%22:%22PR%22,%22sidebarPanel%22:%22GIT%22%7D">Web Editor</a> | <a href="https://codesandbox.io/p/vscode?owner=WordPress&repo=gutenberg&branch=feat/tab-panel-manual-tab-activation">VS Code</a> | <a href="https://codesandbox.io/p/vscode?owner=WordPress&repo=gutenberg&branch=feat/tab-panel-manual-tab-activation&insiders=true">VS Code Insiders</a>

<!-- open-in-codesandbox:complete -->

### ciampo on 2022-11-23T12:52:19Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1325010772

@jasmussen and @jameskoster , this may require design feedback as I don't think that `TabPanel` styles ever accounted for a tab to be focused while not being active.

### jameskoster on 2022-11-23T13:27:16Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1325069370

It's working for me:

https://user-images.githubusercontent.com/846565/203558051-9e94a613-e57b-4909-a5d5-0213c3702315.mp4

I don't have a great idea for the name either, but perhaps something that relates more to the states involved; `openPanelOnFocus`?

### ciampo on 2022-11-23T13:52:26Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1325097970

> I don't have a great idea for the name either, but perhaps something that relates more to the states involved; `openPanelOnFocus`?

On a similar note, `ariakit` calls this feature `selectOnMove` — do we think it would be a better prop name?

### jameskoster on 2022-11-23T14:16:01Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1325137309

> @jasmussen and @jameskoster , this may require design feedback as I don't think that TabPanel styles ever accounted for a tab to be focused while not being active.

We can probably consider this when we get to https://github.com/WordPress/gutenberg/issues/41904.

### chad1008 on 2022-11-23T14:25:58Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1325152341

One thing that's occurring to me is that `ariakit` and this change both default to displaying the tab on focus, which is good, but we come at it from opposite directions. To enable manual selection:
- `ariakit` wants `selectOnMove` to be `false`
- This PR will want `hasManualTabActivation` (or whatever we name it) to be `true`

So we'd eventually end up managing the `ariakit` prop with something like `selectOnMove = { ! hasManualTabActivation }`

That's doable, of course, but maybe it'd be more readable in the long run if we went (as @ciampo suggests) with the same `selectOnMove` prop name, and flipped our boolean accordingly. That way our component and `ariakit`'s implementation are completely in sync.

### ciampo on 2022-11-25T22:20:49Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1327913680

I [renamed the prop](https://github.com/WordPress/gutenberg/pull/46004/commits/ebcb84d1c7f4a3e6d9dfbc8f568e02807bc2aa30) to `selectOnMove` as agreed, and addressed all remaining feedback.

I guess the last thing to decide is whether we're ok to merge this PR with the current way tabs look in manual activation mode, when they are focused but not active (see video in the description).

### ciampo on 2022-11-28T21:26:41Z

URL: https://github.com/WordPress/gutenberg/pull/46004#issuecomment-1329782576

> whether we're ok to merge this PR with the current way tabs look in manual activation mode

Given the absence of strong feedback against merging, I'll go ahead and merge this PR once CI is ✅ . As @jameskoster mentioned above:

> We can probably consider this when we get to #41904.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-11-23T12:58:51Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030411838
Path: packages/components/src/tab-panel/README.md

```diff
@@ -143,6 +143,14 @@ The name of the tab to be selected upon mounting of component. If this prop is n
 -   Required: No
 -   Default: none
 
+#### hasManualTabActivation
```

Not entirely convinced about this new prop's name, open to suggestions!

#### ntsekouras on 2022-11-23T13:23:15Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030438471
Path: packages/components/src/tab-panel/README.md

```diff
@@ -143,6 +143,14 @@ The name of the tab to be selected upon mounting of component. If this prop is n
 -   Required: No
 -   Default: none
 
+#### hasManualTabActivation
```

Maybe just `manualActivation`?

#### ciampo on 2022-11-23T13:51:12Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030469013
Path: packages/components/src/tab-panel/README.md

```diff
@@ -143,6 +143,14 @@ The name of the tab to be selected upon mounting of component. If this prop is n
 -   Required: No
 -   Default: none
 
+#### hasManualTabActivation
```

For boolean props I like to use names that start with `is` or `has` when possible (or at most, action-focused names), which makes it more obvious that the prop is a boolean — so maybe `hasManualActivation` or `activateTabsManually` ?

#### ntsekouras on 2022-11-23T14:30:07Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030514631
Path: packages/components/src/tab-panel/README.md

```diff
@@ -143,6 +143,14 @@ The name of the tab to be selected upon mounting of component. If this prop is n
 -   Required: No
 -   Default: none
 
+#### hasManualTabActivation
```

Your suggestion for `selectOnMove` sound good to me.

#### ciampo on 2022-11-25T22:22:48Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1032700580
Path: packages/components/src/tab-panel/README.md

```diff
@@ -143,6 +143,14 @@ The name of the tab to be selected upon mounting of component. If this prop is n
 -   Required: No
 -   Default: none
 
+#### hasManualTabActivation
```

Done!

### Review thread 2 (resolved)

#### ntsekouras on 2022-11-23T13:20:17Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030435425
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -34,7 +34,23 @@ const TABS = [
 
 const getSelectedTab = () => screen.getByRole( 'tab', { selected: true } );
 
+const originalGetClientRects = window.HTMLElement.prototype.getClientRects;
+
 describe( 'TabPanel', () => {
+	beforeAll( () => {
+		// Mocking `getClientRects()` is necessary to pass a check performed by
+		// the `focus.tabbable.find()` and by the `focus.focusable.find()` functions
+		// from the `@wordpress/dom` package.
+		// @ts-expect-error We're not trying to comply to the DOM spec, only mocking
+		window.HTMLElement.prototype.getClientRects = function () {
```

Curious why we need this.. I saw it's also the case for some other tests like `NavigableMenu`, but I'd love to understand better. Is it related with the testing library used?

#### ciampo on 2022-11-23T13:49:36Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030467327
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -34,7 +34,23 @@ const TABS = [
 
 const getSelectedTab = () => screen.getByRole( 'tab', { selected: true } );
 
+const originalGetClientRects = window.HTMLElement.prototype.getClientRects;
+
 describe( 'TabPanel', () => {
+	beforeAll( () => {
+		// Mocking `getClientRects()` is necessary to pass a check performed by
+		// the `focus.tabbable.find()` and by the `focus.focusable.find()` functions
+		// from the `@wordpress/dom` package.
+		// @ts-expect-error We're not trying to comply to the DOM spec, only mocking
+		window.HTMLElement.prototype.getClientRects = function () {
```

`NavigableMenu` internally uses `focus.tabbable.find()` and `focus.focusable.find()` to search for the tabbable/focusable items — the logic requires a tabbable item [to be visible](https://github.com/WordPress/gutenberg/blob/feat/tab-panel-manual-tab-activation/packages/dom/build-module/focusable.js) — unfortunately, by default `js-dom` doesn't satisfy any of those checks, meaning that by default in our tests `NavigableMenu` won't move the selection to the next/previous tabs when interacting with the keyboard.

Mocking `getClientRects()` makes the above `isVisible` check pass and allows the test to work :)

#### ntsekouras on 2022-11-23T14:29:25Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1030513871
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -34,7 +34,23 @@ const TABS = [
 
 const getSelectedTab = () => screen.getByRole( 'tab', { selected: true } );
 
+const originalGetClientRects = window.HTMLElement.prototype.getClientRects;
+
 describe( 'TabPanel', () => {
+	beforeAll( () => {
+		// Mocking `getClientRects()` is necessary to pass a check performed by
+		// the `focus.tabbable.find()` and by the `focus.focusable.find()` functions
+		// from the `@wordpress/dom` package.
+		// @ts-expect-error We're not trying to comply to the DOM spec, only mocking
+		window.HTMLElement.prototype.getClientRects = function () {
```

So it's related to `js-dom`.. Thanks for elaborating Marco!

#### ciampo on 2022-11-25T22:22:40Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1032700556
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -34,7 +34,23 @@ const TABS = [
 
 const getSelectedTab = () => screen.getByRole( 'tab', { selected: true } );
 
+const originalGetClientRects = window.HTMLElement.prototype.getClientRects;
+
 describe( 'TabPanel', () => {
+	beforeAll( () => {
+		// Mocking `getClientRects()` is necessary to pass a check performed by
+		// the `focus.tabbable.find()` and by the `focus.focusable.find()` functions
+		// from the `@wordpress/dom` package.
+		// @ts-expect-error We're not trying to comply to the DOM spec, only mocking
+		window.HTMLElement.prototype.getClientRects = function () {
```

No worries! It took me a while to find out at first too 😅

### Review thread 3 (resolved)

#### tyxla on 2022-11-24T11:05:00Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1031382993
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -93,7 +94,12 @@ export function TabPanel( {
 		[ onSelect ]
 	);
 
-	const onNavigate = ( _childIndex: number, child: HTMLButtonElement ) => {
+	// Simulate a click on the newly focused tab, which causes the component
+	// to show the `tab-panel` associated with the clicked tab.
+	const activeTabAutomatically = (
```

Nit: naming seems off here, did we mean to say `activateTabAutomatically`?

#### ciampo on 2022-11-25T22:21:08Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1032700296
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -93,7 +94,12 @@ export function TabPanel( {
 		[ onSelect ]
 	);
 
-	const onNavigate = ( _childIndex: number, child: HTMLButtonElement ) => {
+	// Simulate a click on the newly focused tab, which causes the component
+	// to show the `tab-panel` associated with the clicked tab.
+	const activeTabAutomatically = (
```

Yup! Done in https://github.com/WordPress/gutenberg/pull/46004/commits/8ef6ea4fe0c64949ad0a4a30d48a0560ae37d498

### Review thread 4

#### tyxla on 2022-11-24T11:06:12Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1031384092
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -34,7 +34,23 @@ const TABS = [
 
 const getSelectedTab = () => screen.getByRole( 'tab', { selected: true } );
 
+const originalGetClientRects = window.HTMLElement.prototype.getClientRects;
```

Nit: should this be declared right before we override it in `beforeAll`? This would then be a `let` and we'd just set it in `beforeAll()`.

#### ciampo on 2022-11-25T22:21:17Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1032700314
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -34,7 +34,23 @@ const TABS = [
 
 const getSelectedTab = () => screen.getByRole( 'tab', { selected: true } );
 
+const originalGetClientRects = window.HTMLElement.prototype.getClientRects;
```

Done in https://github.com/WordPress/gutenberg/pull/46004/commits/3128c2684b703a8636e6af1c5a29aad01cde82eb

### Review thread 5 (resolved)

#### tyxla on 2022-11-24T11:07:58Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1031385642
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -194,4 +210,98 @@ describe( 'TabPanel', () => {
 			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
 		} );
 	} );
+
+	describe( 'tab activation', () => {
+		it( 'defaults to automatic tab activation', async () => {
+			const user = setupUser();
+			const mockOnSelect = jest.fn();
+
+			render(
+				<TabPanel
+					tabs={ TABS }
+					children={ () => undefined }
+					onSelect={ mockOnSelect }
+				/>
+			);
+
+			// onSelect gets called on the initial render.
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 1 );
+
+			// Click on Alpha, make sure Alpha is selected
+			await user.click( screen.getByRole( 'tab', { name: 'Alpha' } ) );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 2 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
+
+			// Navigate forward with arrow keys,
+			// make sure Beta is selected automatically.
+			await user.keyboard( '[ArrowRight]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 3 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
+
+			// Navigate forward with arrow keys,
+			// make sure Gamma (last tab) is selected automatically.
+			await user.keyboard( '[ArrowRight]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 4 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'gamma' );
+
+			// Navigate forward with arrow keys,
+			// make sure Alpha (first tab) is selected automatically.
+			await user.keyboard( '[ArrowRight]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 5 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
+
+			// Navigate backwards with arrow keys,
+			// make sure Gamma (last tab) is selected automatically
+			await user.keyboard( '[ArrowLeft]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 6 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'gamma' );
+		} );
+
+		it( 'switches to manual tab activation when the `hasManualTabActivation` prop is set to `true`', async () => {
+			const user = setupUser();
+			const mockOnSelect = jest.fn();
+
+			render(
+				<TabPanel
+					tabs={ TABS }
+					children={ () => undefined }
+					onSelect={ mockOnSelect }
+					hasManualTabActivation={ true }
```

```suggestion
					hasManualTabActivation
```

#### ciampo on 2022-11-25T22:22:10Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1032700456
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -194,4 +210,98 @@ describe( 'TabPanel', () => {
 			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
 		} );
 	} );
+
+	describe( 'tab activation', () => {
+		it( 'defaults to automatic tab activation', async () => {
+			const user = setupUser();
+			const mockOnSelect = jest.fn();
+
+			render(
+				<TabPanel
+					tabs={ TABS }
+					children={ () => undefined }
+					onSelect={ mockOnSelect }
+				/>
+			);
+
+			// onSelect gets called on the initial render.
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 1 );
+
+			// Click on Alpha, make sure Alpha is selected
+			await user.click( screen.getByRole( 'tab', { name: 'Alpha' } ) );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 2 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
+
+			// Navigate forward with arrow keys,
+			// make sure Beta is selected automatically.
+			await user.keyboard( '[ArrowRight]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 3 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
+
+			// Navigate forward with arrow keys,
+			// make sure Gamma (last tab) is selected automatically.
+			await user.keyboard( '[ArrowRight]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 4 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'gamma' );
+
+			// Navigate forward with arrow keys,
+			// make sure Alpha (first tab) is selected automatically.
+			await user.keyboard( '[ArrowRight]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 5 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'alpha' );
+
+			// Navigate backwards with arrow keys,
+			// make sure Gamma (last tab) is selected automatically
+			await user.keyboard( '[ArrowLeft]' );
+			expect( mockOnSelect ).toHaveBeenCalledTimes( 6 );
+			expect( mockOnSelect ).toHaveBeenLastCalledWith( 'gamma' );
+		} );
+
+		it( 'switches to manual tab activation when the `hasManualTabActivation` prop is set to `true`', async () => {
+			const user = setupUser();
+			const mockOnSelect = jest.fn();
+
+			render(
+				<TabPanel
+					tabs={ TABS }
+					children={ () => undefined }
+					onSelect={ mockOnSelect }
+					hasManualTabActivation={ true }
```

No need for this anymore, since the new `selectOnMove` prop needs a `false` value here

### Review thread 6 (resolved)

#### tyxla on 2022-11-28T11:38:44Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1033436842
Path: packages/components/src/tab-panel/README.md

```diff
@@ -143,6 +143,14 @@ The name of the tab to be selected upon mounting of component. If this prop is n
 -   Required: No
 -   Default: none
 
+#### selectOnMove
+
+When `true`, the tab will selected when receiving focus (automatic tab activation). When `false`, the tab will be selected only when clicked (manual tab activation). See the [official W3C docs](https://www.w3.org/WAI/ARIA/apg/patterns/tabpanel/) for more info.
```

```suggestion
When `true`, the tab will be selected when receiving focus (automatic tab activation). When `false`, the tab will be selected only when clicked (manual tab activation). See the [official W3C docs](https://www.w3.org/WAI/ARIA/apg/patterns/tabpanel/) for more info.
```

### Review thread 7 (resolved)

#### tyxla on 2022-11-28T11:39:38Z

URL: https://github.com/WordPress/gutenberg/pull/46004#discussion_r1033437632
Path: packages/components/src/tab-panel/types.ts

```diff
@@ -70,4 +70,15 @@ export type TabPanelProps = {
 	 * Array of tab objects. Each tab object should contain at least a `name` and a `title`.
 	 */
 	tabs: Tab[];
+	/**
+	 * When `true`, the tab will selected when receiving focus (automatic tab
```

```suggestion
	 * When `true`, the tab will be selected when receiving focus (automatic tab
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/tab-panel/README.md (MODIFIED, +8/-0)
- packages/components/src/tab-panel/index.tsx (MODIFIED, +10/-2)
- packages/components/src/tab-panel/test/index.tsx (MODIFIED, +111/-0)
- packages/components/src/tab-panel/types.ts (MODIFIED, +11/-0)
