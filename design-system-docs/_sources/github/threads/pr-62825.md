---
number: 62825
type: pull_request
state: MERGED
merged_at: 2024-07-01T17:01:15Z
url: https://github.com/WordPress/gutenberg/pull/62825
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #62825: CustomSelectControlV2: fix item styles

- URL: https://github.com/WordPress/gutenberg/pull/62825
- Author: ciampo
- Created: 2024-06-25T11:24:03Z
- Updated: 2024-07-01T17:01:39Z
- Comments: 1 of 1
- Review threads: 5 of 5
- Changed files: 2 of 2
- Diff: ../pr-diffs/62825.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Part of #55023

Fix items (options) styles of `CustomSelectControlV2` component

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

- To fix visual bugs
- To match more closely look of V1 component
- To add quality of life improvements

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

- Update V2 items in order to match the same line height of the V1 version
- Add `disabled` styles
- Added `user-select: none` to trigger button and option items
- Added `scroll-margin` to improve scroll experience when selecting items that are out of view in the popover

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

- Compare V1 and V2 (legacy) of `CustomSelectControl`, make sure that the items in the select popover now render with the same line height (28px).
- Try to select with the cursor the text of the items in the select popover — V2 should prevent text selection

| V1 | V2 before (trunk) | V2 after (this PR) |
|---|---|---|
| ![Screenshot 2024-06-25 at 13 13 47](https://github.com/WordPress/gutenberg/assets/1083581/86b1f169-8306-49dd-8d77-5206908cd9a0) | ![Screenshot 2024-06-25 at 13 14 27](https://github.com/WordPress/gutenberg/assets/1083581/d613ecaa-6d10-4777-b62f-733de1cf64e6) | ![Screenshot 2024-06-25 at 13 23 21](https://github.com/WordPress/gutenberg/assets/1083581/5a7ccf76-8546-44d5-9980-e4c1534e38ad) |

## Issue comments

### github-actions on 2024-06-25T11:27:03Z

URL: https://github.com/WordPress/gutenberg/pull/62825#issuecomment-2188681029

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### ciampo on 2024-06-25T11:28:26Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1652593155
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -108,7 +108,14 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
```

To match 28px, the line height value should have been `2.15` (without the `rem` unit). I felt like a more explicit `calc` syntax would help to understand the reason for an arbitrary number.

#### tyxla on 2024-07-01T11:28:39Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660908070
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -108,7 +108,14 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
```

It's curious where the 2.15 came from originally...

#### ciampo on 2024-07-01T12:27:15Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660976177
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -108,7 +108,14 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
```

It's the ratio of the line height (28px) to the font size (13px)

Also, since `28 / 13 = 2.15384615`, another consequence of using `2.15` is that the resulting line height wouldn't be exactly 28px (as per design), but slightly less because of the lower decimal precision.

In short: I prefer to let the browser (or sass) do the calculation, and if not possible, to state the `line-height` directly.

#### tyxla on 2024-07-01T12:46:18Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661000435
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -108,7 +108,14 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
```

> Also, since 28 / 13 = 2.15384615, another consequence of using 2.15 is that the resulting line height wouldn't be exactly 28px (as per design), but slightly less because of the lower decimal precision.

Which also makes me question if 28/13 was the intent. But yes, having a clear calc there makes things much clearer.

### Review thread 2

#### mirka on 2024-06-28T18:24:49Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1659153023
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

Apparently this `calc` [doesn't work](https://developer.mozilla.org/en-US/docs/Web/CSS/calc#rules_and_best_practices_while_using_calc) 😕 ("For `/`, the right operand must be unitless".)

#### tyxla on 2024-07-01T11:28:53Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660908274
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

Maybe if we wrap `CONFIG.fontSize()` in a `stringToNumber()`? I appreciate the clarity that the explicit calc adds.

#### ciampo on 2024-07-01T12:27:42Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660977057
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

Sometimes less is more — we can also just remove the `${ CONFIG.fontSize }` part, and simply state `28px` — pushed in https://github.com/WordPress/gutenberg/pull/62825/commits/9a5d26fa9b60ff173e0ec61f509857027ca565da

#### tyxla on 2024-07-01T12:49:26Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661004733
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

But these 2 aren't the same, are they:

```
font-size: 13px;
line-height: 2.15384615
```

and:

```
font-size: 13px
line-height: 28px
```

because if someone overrides only the `font-size`, the `line-height` will behave differently - considering the new font-size in the first example, and ignoring it in the second one.

#### ciampo on 2024-07-01T16:15:36Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661279512
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

To adhere more closely to legacy behavior, It's actually better to keep the hardcoded `28px` as V1 does, exactly for the reason you state. For example, in the default Storybook example of `CustomSelectControl` each select item has a custom `font-size`, but the line-height is supposed to stay the same instead of scaling up/down.

#### tyxla on 2024-07-01T16:19:29Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661283512
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

> the line-height is supposed to stay the same instead of scaling up/down.

Interesting - is this always the case? What if I have font-size 56px, won't it be ugly with line-height of 28px?

#### ciampo on 2024-07-01T16:46:25Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661310349
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
```

I guess we can review that when assessing what `line-height` to adopt for the V2 — keeping it as hardcoded `28px` is probably the safest choice while swapping the V1 implementation

### Review thread 3

#### mirka on 2024-06-28T18:27:42Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1659156101
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
+	scroll-margin: ${ space( 1 ) };
+	user-select: none;
+
+	&[aria-disabled='true'] {
+		opacity: 0.5;
+	}
```

I actually omitted the disabled styles on purpose, because the item styles are fully customizable and the appropriate disabled style will vary. For example, adding a `disabled` to the "Purple" item in the [Default story](https://wordpress.github.io/gutenberg/?path=/story/components-customselectcontrol-v2-default--default) doesn't result in an understandable visual.

https://github.com/WordPress/gutenberg/blob/9a331f13e8e1c59b9defb27a0bae38265de38578/packages/components/src/custom-select-control-v2/types.ts#L196-L202

What do you think?

#### ciampo on 2024-07-01T12:30:17Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660980188
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,16 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// Legacy line height is 28px.
+	// TODO: reassess for non-legacy v2
+	line-height: calc( 28px / ${ CONFIG.fontSize } );
+	scroll-margin: ${ space( 1 ) };
+	user-select: none;
+
+	&[aria-disabled='true'] {
+		opacity: 0.5;
+	}
```

You raise a good point — although I also think that the component should offer some good defaults.

Testing a middle ground in https://github.com/WordPress/gutenberg/pull/62825/commits/37ee79c6bb721b8fe7f3dade34603b56de7ce459 , using `cursor: not-allowed` as a more un-opinionated way to apply default disabled item styles.

Let me know if you still think this is too much

### Review thread 4 (resolved)

#### tyxla on 2024-07-01T10:59:38Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660867347
Path: packages/components/CHANGELOG.md

```diff
@@ -22,6 +23,7 @@
 -   `CustomSelectControlV2`: fix handling of extra option attributes in the `onChange` callbacks and when forwarding them to the option DOM elements. ([#62255](https://github.com/WordPress/gutenberg/pull/62255))
 -   `CustomSelectControlV2`: fix setting initial value and reacting to external controlled updates. ([#62733](https://github.com/WordPress/gutenberg/pull/62733))
 -   `CustomSelectControlV2`: Handle long strings in selected value ([#62198](https://github.com/WordPress/gutenberg/pull/62198)).
+-   `CustomSelectControlV2`: fix item styles ([#62825](https://github.com/WordPress/gutenberg/pull/62825))
```

We should remove this in favor of the top entry.

#### ciampo on 2024-07-01T12:31:46Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1660981988
Path: packages/components/CHANGELOG.md

```diff
@@ -22,6 +23,7 @@
 -   `CustomSelectControlV2`: fix handling of extra option attributes in the `onChange` callbacks and when forwarding them to the option DOM elements. ([#62255](https://github.com/WordPress/gutenberg/pull/62255))
 -   `CustomSelectControlV2`: fix setting initial value and reacting to external controlled updates. ([#62733](https://github.com/WordPress/gutenberg/pull/62733))
 -   `CustomSelectControlV2`: Handle long strings in selected value ([#62198](https://github.com/WordPress/gutenberg/pull/62198)).
+-   `CustomSelectControlV2`: fix item styles ([#62825](https://github.com/WordPress/gutenberg/pull/62825))
```

Woops! Rebase artifact. Thank you for flagging

### Review thread 5

#### tyxla on 2024-07-01T14:07:48Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661114635
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,15 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// TODO: reassess line-height for non-legacy v2
+	line-height: 28px;
```

I'd prefer `2.15384615` and adding a comment about how it's calculated, because that way we'll keep the flexibility/relativity in case someone modifies only the `font-size`.

#### ciampo on 2024-07-01T16:16:21Z

URL: https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661280276
Path: packages/components/src/custom-select-control-v2/styles.ts

```diff
@@ -133,7 +134,15 @@ export const SelectItem = styled( Ariakit.SelectItem )`
 	justify-content: space-between;
 	padding: ${ ITEM_PADDING };
 	font-size: ${ CONFIG.fontSize };
-	line-height: 2.15rem; // TODO: Remove this in default but keep for back-compat in legacy
+	// TODO: reassess line-height for non-legacy v2
+	line-height: 28px;
```

See https://github.com/WordPress/gutenberg/pull/62825#discussion_r1661279512

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/custom-select-control-v2/styles.ts (MODIFIED, +10/-1)
