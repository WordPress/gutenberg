---
number: 18165
type: pull_request
state: MERGED
merged_at: 2019-11-04T13:02:58Z
url: https://github.com/WordPress/gutenberg/pull/18165
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #18165: Components: Switch screen-reader-text to VisuallyHidden

- URL: https://github.com/WordPress/gutenberg/pull/18165
- Author: mkaz
- Created: 2019-10-29T13:16:01Z
- Updated: 2019-11-11T10:36:04Z
- Comments: 3 of 3
- Review threads: 3 of 3
- Changed files: 3 of 3
- Diff: ../pr-diffs/18165.diff

## Body

## Description

This is the follow up PR from comment in #18127 it addresses changing the other spots in components that use `screen-reader-text`  to using `VisuallyHidden` component.

The two spots changed are: `base-control` and `form-text-token`

## How has this been tested?

I created a story in Storybook to be committed in separate PR and confirmed that using the component shows the label as intended, 


## Types of changes

In essence it changes `<span className="screen-reader-text"> ... </span>` to
`<VisuallyHidden> ... </VisuallyHidden>`

The base-control change uses label, and had a bit more logic wrapped around it. 


## Checklist:
- [X] My code is tested.
- [X] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [X] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [X] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [X] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->

## Issue comments

### gziolo on 2019-10-31T13:24:48Z

URL: https://github.com/WordPress/gutenberg/pull/18165#issuecomment-548372951

Travis raises some concerns in e2e tests related to taxonomies. I restarted them but they might be valid looking at the scope of the changes. It might be also related to some unrelated changes in WordPress core.

### mkaz on 2019-10-31T17:44:17Z

URL: https://github.com/WordPress/gutenberg/pull/18165#issuecomment-548491024

@gziolo yeh, I can't see why those tests are failing. I tried rebasing to see if that was part of the issue but didn't help.

### mkaz on 2019-11-01T16:30:30Z

URL: https://github.com/WordPress/gutenberg/pull/18165#issuecomment-548854455

@gziolo - it was a valid issue with the test. It had a check for `.screen-reader-text` which was changed in the form-input-text. I've corrected it and tests are now green :+1:

I've gotten to used to restarting tests to get them to pass. :laughing:

## Review threads

### Review thread 1 (resolved)

#### brentswisher on 2019-10-30T03:55:22Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340421827
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
```

Just noting that a ternary on `hideLabelFromVision` may make the intent easier to understand than having two separate conditionally rendering groups, but that may just be personal preference, nothing to hold up the PR on

#### mkaz on 2019-10-30T13:13:11Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340603639
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
```

My thought was the logic can be addressed in this issue it needs to change it due to a missing case also.
https://github.com/WordPress/gutenberg/issues/18166

#### brentswisher on 2019-10-30T19:20:47Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340813017
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
```

Sounds good to me

### Review thread 2 (resolved)

#### brentswisher on 2019-10-30T04:09:54Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340424216
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
+					as="label"
+					htmlFor={ id }>{ label }</VisuallyHidden> }
```

I'm wondering if there is any reason to be concerned with losing the `components-base-control__label` class on this label? Maybe it's fine, but wondering if it would be better to set up VisuallyHidden to pass through any additional class names and just replace screen-reader-text?

#### mkaz on 2019-10-30T13:11:04Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340602668
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
+					as="label"
+					htmlFor={ id }>{ label }</VisuallyHidden> }
```

Great noticing, I did consider this (for awhile) and I came to the conclusion that it is better to remove it.

First, having the extra class does not provide any semantic knowledge, the class name isn't used for this and it is already a label tag, so we don't need more info here.

Second, it is probably better to remove because all the CSS properties we want set are the ones to used to hide it visually, any additional attributes set via another class may break the visually hiding. Likely unintentional because using the same class for both visually shown and not shown would be unexpected.

#### gziolo on 2019-10-30T15:02:30Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340670810
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
+					as="label"
+					htmlFor={ id }>{ label }</VisuallyHidden> }
```

I had a similar thinking process and found the same answers as @mkaz :) It should be hidden, so we should prevent including all other class names which might break it in any way. A very good change in my opinion 👍

#### brentswisher on 2019-10-30T19:21:16Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340813200
Path: packages/components/src/base-control/index.js

```diff
@@ -3,14 +3,21 @@
  */
 import classnames from 'classnames';
 
+/**
+ * Internal dependencies
+ */
+import VisuallyHidden from '../visually-hidden';
+
 function BaseControl( { id, label, hideLabelFromVision, help, className, children } ) {
 	return (
 		<div className={ classnames( 'components-base-control', className ) }>
 			<div className="components-base-control__field">
-				{ label && id && <label
-					className={ classnames( 'components-base-control__label', { 'screen-reader-text': hideLabelFromVision } ) }
-					htmlFor={ id }>{ label }
-				</label> }
+				{ label && id && hideLabelFromVision && <VisuallyHidden
+					as="label"
+					htmlFor={ id }>{ label }</VisuallyHidden> }
```

Cool, just thought I would call it out to make sure, that makes sense to me

### Review thread 3 (resolved)

#### gziolo on 2019-10-30T15:03:03Z

URL: https://github.com/WordPress/gutenberg/pull/18165#discussion_r340671156
Path: packages/components/src/form-token-field/token.js

```diff
@@ -60,7 +61,7 @@ function Token( {
 				className="components-form-token-field__token-text"
 				id={ `components-form-token-field__token-text-${ instanceId }` }
 			>
-				<span className="screen-reader-text">{ termPositionAndCount }</span>
+				<VisuallyHidden>{ termPositionAndCount }</VisuallyHidden>
```

Shouldn't it be:
```suggestion
				<VisuallyHidden as="span">{ termPositionAndCount }</VisuallyHidden>
```

## Changed files

- packages/components/src/base-control/index.js (MODIFIED, +11/-4)
- packages/components/src/form-token-field/token.js (MODIFIED, +2/-1)
- packages/e2e-tests/specs/editor/various/taxonomies.test.js (MODIFIED, +1/-1)
