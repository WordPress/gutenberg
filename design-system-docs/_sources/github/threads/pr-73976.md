---
number: 73976
type: pull_request
state: MERGED
merged_at: 2025-12-15T15:59:35Z
url: https://github.com/WordPress/gutenberg/pull/73976
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Developer Documentation","[Package] Theme"]
---

# PR #73976: Theme: Include density documentation in README.md

- URL: https://github.com/WordPress/gutenberg/pull/73976
- Author: aduth
- Created: 2025-12-12T16:29:41Z
- Updated: 2025-12-15T16:56:35Z
- Comments: 2 of 2
- Review threads: 1 of 1
- Changed files: 1 of 1
- Diff: ../pr-diffs/73976.diff

## Body

## What?

Updates `@wordpress/theme`'s `README.md` to describe the behavior of the `density` prop.

## Why?

- For consistency with how we're documenting `color`
- To clarify usage of the `ThemeProvider` component and its available features
- To acknowledge and capture the intended purpose of the available density options

## Testing Instructions

Review proposed documentation for accuracy.

## Issue comments

### github-actions on 2025-12-12T16:29:51Z

URL: https://github.com/WordPress/gutenberg/pull/73976#issuecomment-3647280849

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: juanfra <juanfra@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2025-12-12T17:08:19Z

URL: https://github.com/WordPress/gutenberg/pull/73976#issuecomment-3647408247

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 3f3d40a2f1d9b0cbfaac3885e23cc25bac040e7f.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/20173375481
📝  Reported issues:
- #73743 in `/test/e2e/specs/site-editor/template-id-format.spec.js`
- #73752 in `/test/e2e/specs/site-editor/template-id-format.spec.js`

## Review threads

### Review thread 1

#### juanfra on 2025-12-14T20:48:22Z

URL: https://github.com/WordPress/gutenberg/pull/73976#discussion_r2617473979
Path: packages/theme/README.md

```diff
@@ -152,17 +152,27 @@ The `color` prop accepts an object with the following optional properties:
 
 Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
 
+The `density` prop controls the spacing scale throughout the UI:
+
+-   `'default'`: Standard spacing for general use
+-   `'compact'`: Reduced spacing for information-dense interfaces like data tables or dashboards
+-   `'comfortable'`: Increased spacing for focused experiences like modals, dialogs, or full-screen settings panels
```

Nit: we might want to end these list items with a full stop, to stay consistent with the style used elsewhere.

```suggestion
-   `'default'`: Standard spacing for general use.
-   `'compact'`: Reduced spacing for information-dense interfaces like data tables or dashboards.
-   `'comfortable'`: Increased spacing for focused experiences like modals, dialogs, or full-screen settings panels.
```

#### aduth on 2025-12-15T14:50:38Z

URL: https://github.com/WordPress/gutenberg/pull/73976#discussion_r2619749786
Path: packages/theme/README.md

```diff
@@ -152,17 +152,27 @@ The `color` prop accepts an object with the following optional properties:
 
 Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
 
+The `density` prop controls the spacing scale throughout the UI:
+
+-   `'default'`: Standard spacing for general use
+-   `'compact'`: Reduced spacing for information-dense interfaces like data tables or dashboards
+-   `'comfortable'`: Increased spacing for focused experiences like modals, dialogs, or full-screen settings panels
```

Could you clarify what you mean by "elsewhere" ? We're not especially consistent in this file 😅 The original proposed content follows from the color prop listing above, though there's also lists elsewhere in the file (e.g. "Building") that include periods.

I guess it's a broader question of whether we have content guidelines for this and whether they're discoverable enough.

#### aduth on 2025-12-15T15:01:06Z

URL: https://github.com/WordPress/gutenberg/pull/73976#discussion_r2619787912
Path: packages/theme/README.md

```diff
@@ -152,17 +152,27 @@ The `color` prop accepts an object with the following optional properties:
 
 Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
 
+The `density` prop controls the spacing scale throughout the UI:
+
+-   `'default'`: Standard spacing for general use
+-   `'compact'`: Reduced spacing for information-dense interfaces like data tables or dashboards
+-   `'comfortable'`: Increased spacing for focused experiences like modals, dialogs, or full-screen settings panels
```

There's [some guidance in the documentation copy guide for bulleted lists](https://github.com/WordPress/gutenberg/blob/trunk/docs/contributors/documentation/copy-guide.md#bulleted-lists), though it's more about consistency and less about prescribing dots or not dots.

In any case, I pushed changes in b3bb942d1a5 to make it consistent across the files: Dots at the end of list items, and no dots within tables.

#### juanfra on 2025-12-15T15:01:12Z

URL: https://github.com/WordPress/gutenberg/pull/73976#discussion_r2619788291
Path: packages/theme/README.md

```diff
@@ -152,17 +152,27 @@ The `color` prop accepts an object with the following optional properties:
 
 Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
 
+The `density` prop controls the spacing scale throughout the UI:
+
+-   `'default'`: Standard spacing for general use
+-   `'compact'`: Reduced spacing for information-dense interfaces like data tables or dashboards
+-   `'comfortable'`: Increased spacing for focused experiences like modals, dialogs, or full-screen settings panels
```

Fair point 😅

Before leaving the nit I just checked a couple of other readme's as a reference, like [dataviews](https://github.com/WordPress/gutenberg/tree/trunk/packages/dataviews), which felt fairly up to date and uses full stops in lists.

No strong feelings either way though. ~But indeed, maybe it’s a sign we could use a lightweight guideline at some point~ (updated after you shared the copy guide)

#### mirka on 2025-12-15T16:56:35Z

URL: https://github.com/WordPress/gutenberg/pull/73976#discussion_r2620163610
Path: packages/theme/README.md

```diff
@@ -152,17 +152,27 @@ The `color` prop accepts an object with the following optional properties:
 
 Both properties accept any valid CSS color value. The theme system automatically generates appropriate color ramps and determines light/dark mode based on these seed colors.
 
+The `density` prop controls the spacing scale throughout the UI:
+
+-   `'default'`: Standard spacing for general use
+-   `'compact'`: Reduced spacing for information-dense interfaces like data tables or dashboards
+-   `'comfortable'`: Increased spacing for focused experiences like modals, dialogs, or full-screen settings panels
```

I also have a preference for full stops at the end of bullet points 🙂

## Changed files

- packages/theme/README.md (MODIFIED, +43/-33)
