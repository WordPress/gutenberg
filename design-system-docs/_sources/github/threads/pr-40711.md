---
number: 40711
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/40711
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","First-time Contributor"]
---

# PR #40711: [components] Add panel toggle icon overrides

- URL: https://github.com/WordPress/gutenberg/pull/40711
- Author: danieliser
- Created: 2022-04-29T03:14:56Z
- Updated: 2023-08-06T02:13:21Z
- Comments: 14 of 14
- Review threads: 5 of 5
- Changed files: 3 of 3
- Diff: ../pr-diffs/40711.diff

## Body

## What?
This adds support for customizing the PanelBody toggle icons beyond just `chevronUp` & `chevronDown`.

## Why?
This allows replacing the up & down icons with something else for better context.

Without this a developer is required to duplicate 4+ files to remake the needed components. This is primarily due to usage of internal functions that are not exposed `import { useControlledState, useUpdateEffect } from '../utils';`

## How?
- [x] Adds new `iconOpen` & `iconClosed` props with defaults set to current `chevron*` icons. I had considered `iconUp` & `iconDown` but decided these were more explicit. They could be even more specific by changing them to `toggleIconUp` or similar. Open to suggestion.
- [x] Updated readme to reflect new props.
- [x] Updated stories with new example.

## Testing Instructions
1. Create a `Panel` & `PanelBody`.
2. Add `iconOpen` and/or `iconClosed` props to `PanelBody`.
3. Render/run and view new ability to customize them.

## Screenshots or screencast <!-- if applicable -->
![image](https://user-images.githubusercontent.com/847818/165879626-b730f54d-6927-4014-90cd-ccc089032711.png)

## Issue comments

### github-actions on 2022-04-29T03:15:19Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1112838924

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @danieliser! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/), where we hold [regularly weekly meetings](https://make.wordpress.org/core/tag/core-editor-summary/) open to anyone to coordinate with each other.

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### danieliser on 2022-04-29T20:16:31Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1113686214

@amustaque97 - Not a problem. Scratched an itch. Looking forward to doing so more often 💯

### danieliser on 2022-04-29T21:46:34Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1113775969

I didn't add changelog note, wasn't sure if that was something I do as part of the PR or done when it was merged.

Happy to add one now if I should have done so.

### amustaque97 on 2022-05-01T19:12:08Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1114312940

> I didn't add changelog note, wasn't sure if that was something I do as part of the PR or done when it was merged.
> 
> Happy to add one now if I should have done so.

Yeah, feel free to add changelog now. 🙂

### danieliser on 2022-05-02T16:37:35Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115104479

@mirka I'm open to consensus, but don't have an experienced enough opinion within this project to weigh in. Will see what others say.

My only 2 cents would be that I would personally have no clue as a developer using this component what `disclosureIcon` would be referencing. I've never seen this particular web standard I'm guessing.

### ciampo on 2022-05-02T18:07:40Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115200895

> I feel strongly that we should use the word "disclosure" in the prop name. An unqualified `icon` is a little too vague here since we already have an `icon` prop

Given that @danieliser is proposing to add two props, at the moment named `iconExpanded` and `iconCollapsed`, what do you mean when you refer to "an unqualified `icon`" ?

> What do we think about just adding one prop (e.g. `disclosureIcon`), and have the consumer switch their own icon states via `onToggle`? [...]  I don't feel _super_ strongly about this point, but I wanted to suggest it because this particular customization falls outside of established Gutenberg design patterns, so I'm hesitant to add two extra props just for this relatively niche use case.

How would this approach work when the component is _uncontrolled_?

---

Apart from that, for the newly added prop(s), should their name _start_ or _end_ with `icon` ? (e.g. `iconCollapsed` vs `collapsedIcon`)?

### mirka on 2022-05-02T19:15:19Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115265564

> My only 2 cents would be that I would personally have no clue as a developer using this component what `disclosureIcon` would be referencing. I've never seen this particular web standard I'm guessing.

Fair point! That is something that we can mitigate via docs (readme and Storybook) though. In general we'll want to keep our terminology in line with the underlying web standards (e.g. [ARIA disclosure widget](https://www.google.com/search?q=aria+disclosure+widget), [`<detail>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/details)) so we don't make up our own local terms or clash with existing concepts.

> > I feel strongly that we should use the word "disclosure" in the prop name. An unqualified `icon` is a little too vague here since we already have an `icon` prop
> 
> Given that @danieliser is proposing to add two props, at the moment named `iconExpanded` and `iconCollapsed`, what do you mean when you refer to "an unqualified `icon`" ?

Yeah that was unclear, sorry. Unqualified in the sense that expanded/collapsed are states of `icon`. If you only have two words "icon + collapsed", you are describing the state of `icon`. It would be less vague if we qualified _what_ icon we're talking about (e.g. `collapsedDisclosureIcon`).

> How would this approach work when the component is _uncontrolled_?

From my skimming of the code, this component _only_ works uncontrolled. Hence the need to hook into `onToggle` to get the current state.

> Apart from that, for the newly added prop(s), should their name _start_ or _end_ with `icon` ? (e.g. `iconCollapsed` vs `collapsedIcon`)?

My rule of thumb for naming things like this is "Can it be mistaken for a boolean?". So my vote goes to "ends with `icon`" 🙂

### mirka on 2022-05-02T19:23:21Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115273264

I'll also note that, if we were to design this component API from scratch today, I think we should be using the `prefix`/`suffix` pattern ([like with `InputControl`](https://wordpress.github.io/gutenberg/?path=/docs/components-experimental-inputcontrol--default)) and not even mention "icon" or "disclosure" 😄 This is another reason why I slightly prefer not to introduce states into prop names (`collapseFoo`) if possible. Because theoretically, someone should be able to place their disclosure icon in the `prefix` slot instead of the `suffix` slot, and that would require yet another pair of `collapse/expand` props!

### ciampo on 2022-05-02T19:53:07Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115298294

I think that @mirka 's points here are definitely relevant — I'm in favour of applying her suggestion and, therefore, switch to one prop called `disclosureIcon`.

We will need to update docs, Storybook and CHANGELOG accordingly.

> From my skimming of the code, this component only works uncontrolled

From a quick look, I thought `PanelBody` could be [_controlled_ via the `opened` prop](https://github.com/WordPress/gutenberg/blob/0c3cd7ad28988c6aa13abaf911d8d17957d2b82a/packages/components/src/panel/body.js#L37-L39) (although this doesn't really affect the decision of switching to having one `disclosureIcon` prop)

### mirka on 2022-05-02T21:15:32Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115376714

> From a quick look, I thought `PanelBody` could be [_controlled_ via the `opened` prop](https://github.com/WordPress/gutenberg/blob/0c3cd7ad28988c6aa13abaf911d8d17957d2b82a/packages/components/src/panel/body.js#L37-L39)

Ah you're right, TIL `useControlledState`. Hopefully we can improve the [docs](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/panel/README.md#opened) around this in the near future

### danieliser on 2022-05-02T21:30:39Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115389473

I had considered handling it in a more programattic and single interface way where you could pass an icon, string or callback and the callback would get isExpanded as an argument.

In that way you could theoretically also forward current toggle state to the icon prop callback function.

Something like

```
<PanelBody disclosureIcon={ ( isOpened ) => isOpened ? chevronUp : chevronDown }>
```

And for rendering something along the lines of

```
const toggleIcon = typeof disclosureIcon === 'function' 
        ? disclosureIcon( isOpened )
        : ( isOpened
                ? chevronUp
                : chevronDown );

<Icon icon={ toggleIcon  } />
```

One thing of note, we changed icon labeling from open/closed, to expanded/collapsed, but all the internal logic is labeled open/closed. Just something to consider.

### ciampo on 2022-05-03T09:43:27Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1115913106

My preference would be to follow @mirka 's suggestion, and have `disclosureIcon` accepting an SVG icon or a string.

I wouldn't add a callback, simply because the consumer of `PanelBody` would be able to know anyway if the component is in its expanded/opened or collapsed/closed state, both in controlled and uncontrolled modes.

> we changed icon labeling from open/closed, to expanded/collapsed, but all the internal logic is labeled open/closed. Just something to consider.

There seems to be a lack of coherence in the way the docs and the code have been written: in some cases, it refer to the terms "expand" and "collapse" ([example](https://github.com/WordPress/gutenberg/blob/b647120f6eaf559aa822321642e389cc062cbb8d/packages/components/src/panel/README.md?plain=1#L45-L53)), but in others, it refers to "opened" and "closed" (like for [the `opened` prop](https://github.com/WordPress/gutenberg/blob/b647120f6eaf559aa822321642e389cc062cbb8d/packages/components/src/panel/README.md?plain=1#L109) and the internal logic).

Anyway, if we introduce just a new `disclosureIcon` prop, we should avoid this altogether

### mtias on 2022-05-21T21:45:32Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1133772838

May I ask for more examples of how this would be customized and for what reasons? Expandable panels have some assumed mechanics and the design might fluctuate assuming arrows (for example, relocating them on the left for ToolsPanels).

### danieliser on 2022-05-21T21:49:21Z

URL: https://github.com/WordPress/gutenberg/pull/40711#issuecomment-1133773242

@mtias - I can, though mine goes a little beyond what is proposed here. 

![image](https://user-images.githubusercontent.com/847818/169670001-36d4486a-a72a-44b7-a35f-139804b6f1bc.png)

Our version has a dropdown in the header when open, but the idea was similar and had this component supported what I was trying to do before I wrote a custom version I'd have gone a different route.

In our case showing a panel also enables features on the block immediately, it the panel being open is a setting in itself toggling on more advanced stuff.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-05-02T10:43:27Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862749402
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
```

Let's use "expanded" instead of "open"

#### danieliser on 2022-05-02T16:27:06Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862992149
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
```

Changed `iconOpen` to `iconExpanded`

### Review thread 2 (resolved)

#### ciampo on 2022-05-02T10:43:42Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862749516
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
+
+###### iconClosed
```

Let's use "collapsed" instead of "closed"

#### danieliser on 2022-05-02T16:29:26Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862993968
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
+
+###### iconClosed
```

Changed to `iconCollapsed`

### Review thread 3

#### ciampo on 2022-05-02T10:47:46Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862751369
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
```

These props are not `String`s — let's update the description as follow:

```suggestion
An icon to be shown as the toggle button within the `PanelBody` title, in its expanded state. See [the `@wordpress/icons` package](https://github.com/WordPress/gutenberg/blob/trunk/packages/icons/README.md) for a list of available icons.

-   Type: `React.ReactNode`
-   Required: No
-   Default: `chevronUp` icon from `@wordpress/icons`
```

#### danieliser on 2022-05-02T16:33:01Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862996546
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
```

@ciampo - Sorry, wasn't sure what type they should be, I copied from the lines above documenting the existing `icon` prop which also said `string`.

Should I update the original `icon` prop documentation to match?

#### ciampo on 2022-05-02T17:50:00Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r863053366
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
```

> Should I update the original icon prop documentation to match?

Yes please. I believe the `icon` prop being a `String` is a leftover from earlier times when `dashicons` were the main way to provide icons.

### Review thread 4 (resolved)

#### ciampo on 2022-05-02T10:48:06Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862751511
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
+
+###### iconClosed
+
+An icon to be shown as the toggle button within the `PanelBody` title when closed.
+
+-   Type: `String`
+-   Required: No
```

These props are not `String`s — let's update the description as follow:

```suggestion
An icon to be shown as the toggle button within the `PanelBody` title, in its collapsed state. See [the `@wordpress/icons` package](https://github.com/WordPress/gutenberg/blob/trunk/packages/icons/README.md) for a list of available icons.

-   Type: `React.ReactNode`
-   Required: No
-   Default: `chevronDown` icon from `@wordpress/icons`
```

#### danieliser on 2022-05-02T16:33:19Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862996804
Path: packages/components/src/panel/README.md

```diff
@@ -127,6 +127,20 @@ An icon to be shown next to the `PanelBody` title.
 -   Type: `String`
 -   Required: No
 
+###### iconOpen
+
+An icon to be shown as the toggle button within the `PanelBody` title when open.
+
+-   Type: `String`
+-   Required: No
+
+###### iconClosed
+
+An icon to be shown as the toggle button within the `PanelBody` title when closed.
+
+-   Type: `String`
+-   Required: No
```

Done.

### Review thread 5

#### ciampo on 2022-05-02T10:49:45Z

URL: https://github.com/WordPress/gutenberg/pull/40711#discussion_r862752295
Path: packages/components/src/panel/stories/index.js

```diff
@@ -84,6 +84,26 @@ export const withIcon = () => {
 	);
 };
 
+export const CustomToggleIcons = () => {
+	const bodyTitle = text( 'Body Title', 'My Block Settings' );
+	const rowText = text( 'Row Text', 'My Panel Inputs and Labels' );
+	const iconOpen = boolean( 'Open Icon', true ) ? closeSmall : undefined;
+	const iconClosed = boolean( 'Closed Icon', true ) ? plus : undefined;
```

We should either:

- use a `select` control to give users a few icons to choose from
- use `boolean` controls, but call them `customIconExpanded` and `customIconCollapsed`

## Changed files

- packages/components/src/panel/README.md (MODIFIED, +16/-0)
- packages/components/src/panel/body.js (MODIFIED, +9/-2)
- packages/components/src/panel/stories/index.js (MODIFIED, +23/-1)
