---
number: 74781
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/74781
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Compose","[Package] Components","[Package] Block library","[Package] Block editor"]
---

# PR #74781: Switch React HTML inert prop to boolean type

- URL: https://github.com/WordPress/gutenberg/pull/74781
- Author: jsnajdr
- Created: 2026-01-20T15:55:57Z
- Updated: 2026-01-26T09:26:37Z
- Comments: 5 of 5
- Review threads: 0 of 0
- Changed files: 10 of 10
- Diff: ../pr-diffs/74781.diff

## Body

Spinoff from #61521 that converts the type of the HTML `inert` attributes to `boolean`, as it should be according to the HTML spec.

This is part of React 19 migration that will include the `boolean` type for `inert` by default. Here, where we're still on React 18, we need to explicitly include the `react/experimental` typings so that `inert` gets the right type.

The PR also updates unit and e2e tests. In the HTML markup, the valid values for the attribute are either `inert=""` or `inert="inert"`. It cannot be `inert="true"`. This is specified in the HTML spec: https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#boolean-attributes

## Issue comments

### Mamaduka on 2026-01-20T16:30:42Z

URL: https://github.com/WordPress/gutenberg/pull/74781#issuecomment-3773829850

I think we were using this odd prop type because the new `inert` attribute wasn't supported in React 18.

### github-actions on 2026-01-20T16:30:53Z

URL: https://github.com/WordPress/gutenberg/pull/74781#issuecomment-3773830803

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### jsnajdr on 2026-01-20T19:12:23Z

URL: https://github.com/WordPress/gutenberg/pull/74781#issuecomment-3774519338

> I think we were using this odd prop type because the new `inert` attribute wasn't supported in React 18.

The v18 `@types/react` ship the boolean `inert` type in `experimental.d.ts` that you need to import separately. That's why I'm adding it to the `tsconfig.json`. In v19 it starts to be shipped by default.

One problem is that the experimental types add a new `onToggle` prop for a generic HTML element (like `<div>`) which is part of native popover support. And it conflicts with our `DropdownComponentProps` which also have `onToggle`. That's why the CI checks fail now.

The `onToggle` prop is part of the default set in v19, so we'll need to deal with it somehow sooner or later.

### Mamaduka on 2026-01-20T19:17:44Z

URL: https://github.com/WordPress/gutenberg/pull/74781#issuecomment-3774540708

I think the e2e test and some unit tests are failing for different reasons. The test runners fail to assert the `inert` attribute.

### jsnajdr on 2026-01-26T09:26:37Z

URL: https://github.com/WordPress/gutenberg/pull/74781#issuecomment-3798623610

Turns out this PR was a bad idea based on a wrong assumption that the `inert` prop type change is only about TypeScript types. But there is also `react-dom` code that runs at runtime and which sets the `inert: boolean` prop as an actual HTML attribute. But that fails with a warning, because the React 18 runtime doesn't support boolean `inert` yet:
```
Received `true` for a non-boolean attribute `inert`.
If you want to write it to the DOM, pass a string instead:
```

We can't spin off this change, it will need to be part of the big upgrade PR.

## Review threads

_No review threads._
## Changed files

- packages/block-editor/src/components/block-list/use-block-props/index.js (MODIFIED, +1/-1)
- packages/block-editor/src/components/inserter/media-tab/utils.js (MODIFIED, +1/-1)
- packages/block-library/src/comments/edit/placeholder.js (MODIFIED, +1/-1)
- packages/boot/src/components/canvas/index.tsx (MODIFIED, +1/-2)
- packages/components/src/disabled/index.tsx (MODIFIED, +1/-2)
- packages/components/tsconfig.json (MODIFIED, +1/-0)
- packages/compose/src/hooks/use-disabled/index.ts (MODIFIED, +1/-1)
- packages/compose/src/hooks/use-disabled/test/index.js (MODIFIED, +4/-4)
- test/e2e/specs/editor/various/pattern-overrides.spec.js (MODIFIED, +23/-59)
- tsconfig.base.json (MODIFIED, +1/-1)
