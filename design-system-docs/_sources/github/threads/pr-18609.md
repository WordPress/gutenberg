---
number: 18609
type: pull_request
state: MERGED
merged_at: 2019-12-03T14:30:16Z
url: https://github.com/WordPress/gutenberg/pull/18609
matched_labels: ["[Package] Components"]
all_labels: ["[Feature] UI Components","[Package] Components","Needs Technical Feedback","[Package] Block editor"]
---

# PR #18609: Moves <TextHighlight /> component to @wordpress/components package

- URL: https://github.com/WordPress/gutenberg/pull/18609
- Author: getdave
- Created: 2019-11-19T14:04:28Z
- Updated: 2019-12-09T11:27:51Z
- Comments: 3 of 3
- Review threads: 4 of 4
- Changed files: 8 of 8
- Diff: ../pr-diffs/18609.diff

## Body

Closes https://github.com/WordPress/gutenberg/issues/18074.

This PR extracts the `TextHighlight` used within `LinkControl` into a standalone component.

`TextHighlight` is a component which applies a `<mark>` tag around all/any matching strings within a string of text. This is useful for highlighting search terms within a string of text. 

## How has this been tested?
* Manually verifying `LinkControl` hasn't broken and search terms are still highlighted.
* Checking Storybook behaviour.

## Screenshots <!-- if applicable -->

## Types of changes
New feature (non-breaking change which adds functionality).

## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->.

## Issue comments

### getdave on 2019-11-27T11:28:57Z

URL: https://github.com/WordPress/gutenberg/pull/18609#issuecomment-559048994

Thanks I'm going to get back to this just as soon as I can. Likely this Friday.

### getdave on 2019-12-03T09:14:34Z

URL: https://github.com/WordPress/gutenberg/pull/18609#issuecomment-561072478

> I was wondering whether this component could be refactored to use this new experimental API just introduced by @nerrad in #17376:
> `__experimentalCreateInterpolateElement`
> #18623 illustrates how it can be used in action. In this case, it would be possible to avoid wrapping text with `span` elements.

@gziolo I was intending to keep the `<span>` as it might be useful to be able to style things but I've realised that perhaps it's not necessary as CSS will let us style the default case and have overides for the highlight text anyway. 

As a result, I've refactored to the new experimental API.

### getdave on 2019-12-03T14:31:52Z

URL: https://github.com/WordPress/gutenberg/pull/18609#issuecomment-561193236

Thanks for the reviews @gziolo! Much appreciated.

## Review threads

### Review thread 1 (resolved)

#### gziolo on 2019-11-20T10:06:44Z

URL: https://github.com/WordPress/gutenberg/pull/18609#discussion_r348392315
Path: packages/components/src/text-highlight/README.md

```diff
@@ -0,0 +1,39 @@
+# TextHighlight
+
+Highlights occurances of a given string within another string of text. Wraps each match with a [`<mark>` tag](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/mark) which provides browser default styling.
+
+## Usage
+
+Pass in the `text` and the `highlight` string to be matched against. 
+
+In the example below, the string `Gutenberg` would be highlighted twice. 
+
+```jsx
+import { TextHighlight } from '@wordpress/components';
+
+const MyTextHighlight = () => (
+	<TextHighlight
+		text={ 'Why do we like Gutenberg? Because Gutenberg is the best!' }
```

Nit:
```suggestion
		text="Why do we like Gutenberg? Because Gutenberg is the best!"
```

### Review thread 2 (resolved)

#### gziolo on 2019-11-20T10:07:01Z

URL: https://github.com/WordPress/gutenberg/pull/18609#discussion_r348392454
Path: packages/components/src/text-highlight/README.md

```diff
@@ -0,0 +1,39 @@
+# TextHighlight
+
+Highlights occurances of a given string within another string of text. Wraps each match with a [`<mark>` tag](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/mark) which provides browser default styling.
+
+## Usage
+
+Pass in the `text` and the `highlight` string to be matched against. 
+
+In the example below, the string `Gutenberg` would be highlighted twice. 
+
+```jsx
+import { TextHighlight } from '@wordpress/components';
+
+const MyTextHighlight = () => (
+	<TextHighlight
+		text={ 'Why do we like Gutenberg? Because Gutenberg is the best!' }
+		highlight={ 'Gutenberg' }
```

Nit:
```suggestion
		highlight="Gutenberg"
```

### Review thread 3 (resolved)

#### gziolo on 2019-11-20T10:07:56Z

URL: https://github.com/WordPress/gutenberg/pull/18609#discussion_r348392899
Path: packages/components/src/text-highlight/stories/index.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * External dependencies
+ */
+import { text } from '@storybook/addon-knobs';
+
+/**
+ * Internal dependencies
+ */
+import TextHighlight from '../';
+
+export default { title: 'Components|TextHighlight', component: TextHighlight };
+
+export const _default = () => {
+	const defaultText = 'We call the new editor Gutenberg. The entire editing experience has been rebuilt for media rich pages and posts.';
```

Nit: this const can be inlined.

#### getdave on 2019-12-03T08:52:26Z

URL: https://github.com/WordPress/gutenberg/pull/18609#discussion_r353048306
Path: packages/components/src/text-highlight/stories/index.js

```diff
@@ -0,0 +1,22 @@
+/**
+ * External dependencies
+ */
+import { text } from '@storybook/addon-knobs';
+
+/**
+ * Internal dependencies
+ */
+import TextHighlight from '../';
+
+export default { title: 'Components|TextHighlight', component: TextHighlight };
+
+export const _default = () => {
+	const defaultText = 'We call the new editor Gutenberg. The entire editing experience has been rebuilt for media rich pages and posts.';
```

Fixed.

### Review thread 4 (resolved)

#### gziolo on 2019-12-03T09:31:06Z

URL: https://github.com/WordPress/gutenberg/pull/18609#discussion_r353066220
Path: packages/components/src/text-highlight/index.js

```diff
@@ -16,13 +16,17 @@ const TextHighlight = ( { text = '', highlight = '' } ) => {
 	}
 
 	const regex = new RegExp( `(${ escapeRegExp( highlight ) })`, 'gi' );
-	const parts = text.split( regex );
-	return (
-		<Fragment>
-			{ parts.filter( ( part ) => part ).map( ( part, i ) => (
-				regex.test( part ) ? <mark key={ i }>{ part }</mark> : <span key={ i }>{ part }</span>
-			) ) }
-		</Fragment>
+	const parts = text.split( regex ).filter( ( part ) => part );
```

Could it use [String.replace](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/replace) with the RegExp or do I miss something specific in the logic?

#### getdave on 2019-12-03T10:00:28Z

URL: https://github.com/WordPress/gutenberg/pull/18609#discussion_r353081287
Path: packages/components/src/text-highlight/index.js

```diff
@@ -16,13 +16,17 @@ const TextHighlight = ( { text = '', highlight = '' } ) => {
 	}
 
 	const regex = new RegExp( `(${ escapeRegExp( highlight ) })`, 'gi' );
-	const parts = text.split( regex );
-	return (
-		<Fragment>
-			{ parts.filter( ( part ) => part ).map( ( part, i ) => (
-				regex.test( part ) ? <mark key={ i }>{ part }</mark> : <span key={ i }>{ part }</span>
-			) ) }
-		</Fragment>
+	const parts = text.split( regex ).filter( ( part ) => part );
```

Actually I think it's fine. Probably me overengineering things.

## Changed files

- docs/manifest-devhub.json (MODIFIED, +6/-0)
- packages/block-editor/src/components/link-control/search-item.js (MODIFIED, +1/-5)
- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/text-highlight/README.md (ADDED, +39/-0)
- packages/components/src/text-highlight/index.js (RENAMED, +7/-8)
- packages/components/src/text-highlight/stories/index.js (ADDED, +21/-0)
- packages/components/src/text-highlight/test/index.js (ADDED, +112/-0)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +10/-0)
