---
number: 77956
type: pull_request
state: MERGED
merged_at: 2026-05-06T15:32:35Z
url: https://github.com/WordPress/gutenberg/pull/77956
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #77956: Modal: Render as a bottom sheet on mobile

- URL: https://github.com/WordPress/gutenberg/pull/77956
- Author: crisbusquets
- Created: 2026-05-05T13:18:26Z
- Updated: 2026-06-28T10:16:56Z
- Comments: 5 of 5
- Review threads: 5 of 5
- Changed files: 2 of 2
- Diff: ../pr-diffs/77956.diff

## Body

## What?

Below the `break-small` (600px) breakpoint, the `Modal` component now renders as a **bottom sheet**:

- Anchored to the bottom of the viewport (instead of the top).
- Edge-to-edge width with rounded top corners only.
- Height adapts to the content, capped so the dimmed overlay stays visible above.
- Slides up from the bottom on open / down on close (instead of the desktop scale animation).

Behavior at and above `break-small` is unchanged.

## Why?

On the previous mobile layout the modal was anchored to the top with a 40px gap and stretched to fill the remaining viewport height regardless of content. Two consequences:

- Short-content modals looked oversized.
- Primary CTAs ended up at the top of the screen, far from the user's thumb on touch devices.

A bottom-sheet pattern fixes both: the modal hugs its content and CTAs land in the comfortable thumb zone.

## How?

Pure CSS change in `packages/components/src/modal/style.scss`:

- **Bottom anchoring + content-hugging height.** Replaced `margin: $grid-unit-50 0 0 0` with `align-self: flex-end` (plus `margin: 0`). `align-self: flex-end` simultaneously anchors the modal to the bottom of the existing `display: flex` overlay and breaks the default `align-items: stretch`, so the modal sizes to its content rather than filling the viewport. `align-self: auto` is restored inside `@include break-small()` so the desktop centered layout is unchanged.
- **Max height.** Added `max-height: calc(100% - #{$grid-unit-50})` so very tall content scrolls inside the sheet rather than covering the overlay.
- **Consolidated `&.is-full-screen` block.** All `is-full-screen` rules are now grouped into a single block, with mobile defaults (full-viewport coverage), shared content rules, and `@include break-small()` / `@include break-medium()` overrides for desktop centered full-screen behavior. This also fixes a `no-duplicate-selectors` stylelint error introduced by the initial revision and removes a pre-existing redundant nested `@include break-small()` inside an already-`break-small` block.
- **Slide animation on mobile, scale on desktop.** The `components-modal__appear-animation` / `components-modal__disappear-animation` keyframes default to a `translateY(100%) ↔ 0` slide, and are redefined inside `@include break-small()` to keep the original `scale(0.9) ↔ 1` desktop animation. The keyframe **names are unchanged** so `use-modal-exit-animation.ts`'s `animationend` listener keeps working without any JS changes.
- Reduced motion is respected via the existing `@media not (prefers-reduced-motion)` guard around `animation-duration`.

No public API changes.

## Testing Instructions

1. `npm run storybook:dev`
2. Open **Components → Modal → Default**.
3. **Mobile** (DevTools device toolbar, viewport < 600px): open the modal — it should slide up from the bottom, hug the content height, and have rounded top corners only.
4. **Desktop** (viewport ≥ 600px): open the modal — should be centered with the scale animation, identical to before.
5. Toggle `isFullScreen` on:
   - Mobile: should cover the full viewport, no rounded corners.
   - Desktop: should be centered with a 20px gap and rounded corners (40px gap above the `break-medium` breakpoint).
6. Verify reduced motion: enable "Reduce motion" (macOS System Settings → Accessibility → Display, or DevTools → Rendering → emulate `prefers-reduced-motion: reduce`). Modal open/close should be instant, with no slide or scale animation.

### Testing Instructions for Keyboard

- Open and close the modal with the keyboard (Tab to a trigger, Enter to open; Escape to close). Focus management is unchanged from the previous behavior.

## Screenshots or screencast

|         | Before | After |
| ------- | ------ | ----- |
| Mobile  | <img width="750" height="1334" alt="wordpress github io_gutenberg_iframe html_id=components-modal--default viewMode=story(iPhone SE)" src="https://github.com/user-attachments/assets/140686b8-7c50-476b-99d4-0b9f3de15888" /> | <img width="750" height="1334" alt="localhost_50240_iframe html_args=isFullScreen_!false id=components-modal--default viewMode=story(iPhone SE)" src="https://github.com/user-attachments/assets/d250ac93-963b-4eef-89e8-b582d8803401" /> |
| Desktop | (unchanged) | (unchanged) |

## Use of AI Tools

<!-- Please disclose any AI tooling used to author this PR. -->

## Types of changes

Bug fix — restores expected mobile UX without changing the public API.

## Checklist

- [x] My code is tested.
- [x] My code follows the WordPress code style.
- [x] My code follows the accessibility standards.
- [x] My code has proper inline documentation.
- [x] I've included developer documentation if appropriate.
- [x] I've updated all React Native files affected by the changes. (N/A — CSS only, no native equivalent.)

## Issue comments

### github-actions on 2026-05-05T13:18:52Z

URL: https://github.com/WordPress/gutenberg/pull/77956#issuecomment-4379615916

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: crisbusquets <cbusquets1989@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### crisbusquets on 2026-05-05T16:54:35Z

URL: https://github.com/WordPress/gutenberg/pull/77956#issuecomment-4381315248

> This PR seems to be based on a very old version of the repository, would you be able to rebase on top of latest `trunk`?
> 
> <img alt="Screenshot 2026-05-05 at 17 19 26" width="1004" height="396" src="https://private-user-images.githubusercontent.com/1083581/587770718-7769770d-1553-427e-8f8e-ef79a350d307.jpg?jwt=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..9N1HSNh_2-jSkKRHGY6wkMuDDdUvpdfMpkpc3GEYS8E">
> We will also need to add a CHANGELOG entry for the `@wordpress/components` package

@ciampo Fixed! I'll review your other suggestions shortly. Thanks for reviewing it.

### ciampo on 2026-05-06T08:02:06Z

URL: https://github.com/WordPress/gutenberg/pull/77956#issuecomment-4386123503

@crisbusquets as you address the remaining feedback, make sure also to lint the code changes (currently style linting is throwing an error)

### crisbusquets on 2026-05-06T12:03:16Z

URL: https://github.com/WordPress/gutenberg/pull/77956#issuecomment-4387712461

@ciampo I've cleaned up the redundant nested `@include break-small()`.

While I was there, I also consolidated all `&.is-full-screen `rules into a single block: Previously the styles were spread across three places (mobile dimensions, desktop dimensions inside `@include break-small()`, and shared content rules), and the two without a media-query wrapper were also tripping a `no-duplicate-selectors` stylelint error this PR was producing 🤷‍♀️

Now there's one` &.is-full-screen` block with viewport-specific overrides nested via @includes. Happy to revert the consolidation and keep just the inner-mixin removal if you'd prefer.

### t-hamano on 2026-06-28T10:16:56Z

URL: https://github.com/WordPress/gutenberg/pull/77956#issuecomment-4825767582

This PR works well for a single modal content, but it feels very unnatural when the modal content changes dynamically. For example, open the Preferences modal and, in the mobile layout, change the tab content.

https://github.com/user-attachments/assets/9a0a9e14-7922-4e10-ae75-37317b1a769e

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2026-05-05T14:57:06Z

URL: https://github.com/WordPress/gutenberg/pull/77956#discussion_r3189433670
Path: packages/components/src/modal/style.scss

```diff
@@ -29,48 +22,55 @@
 // The modal window element.
 .components-modal__frame {
 	@include reset;
-	// Use the entire viewport on smaller screens.
-	margin: $grid-unit-50 0 0 0;
+	// On smaller screens, render as a bottom sheet: edge-to-edge, anchored to
+	// the bottom of the viewport, with a height that adapts to the content
+	// (capped so it never covers the full viewport).
+	margin: auto 0 0 0;
```

Could we use `align-self: flex-end` here instead of `auto` margin?

```suggestion
	// On smaller screens, render as a bottom sheet: edge-to-edge, anchored
	// to the bottom of the viewport. `align-self: flex-end` both anchors
	// the modal to the bottom of the flex overlay and breaks the default
	// `align-items: stretch`, so the modal hugs its content height.
	align-self: flex-end;
	margin: 0;
```

### Review thread 2 (resolved)

#### ciampo on 2026-05-05T14:58:24Z

URL: https://github.com/WordPress/gutenberg/pull/77956#discussion_r3189443399
Path: packages/components/src/modal/style.scss

```diff
@@ -29,48 +22,55 @@
 // The modal window element.
 .components-modal__frame {
 	@include reset;
-	// Use the entire viewport on smaller screens.
-	margin: $grid-unit-50 0 0 0;
+	// On smaller screens, render as a bottom sheet: edge-to-edge, anchored to
+	// the bottom of the viewport, with a height that adapts to the content
+	// (capped so it never covers the full viewport).
+	margin: auto 0 0 0;
 	width: 100%;
+	max-height: calc(100% - #{$grid-unit-50});
 	background: $white;
 	box-shadow: $elevation-large;
 	border-radius: $radius-large $radius-large 0 0;
 	overflow: hidden;
 	// Have the content element fill the vertical space yet not overflow.
 	display: flex;
-	color: $gray-900;
 	// Animate the modal frame/contents appearing on the page.
 	animation-name: components-modal__appear-animation;
+	animation-duration: var(--modal-frame-animation-duration);
 	animation-fill-mode: forwards;
 	animation-timing-function: cubic-bezier(0.29, 0, 0, 1);
-
-	// Ensure all headings use the proper editor text color, overriding wp-admin common.css
-	h1,
-	h2,
-	h3 {
-		color: $gray-900;
-	}
-
-	@media not (prefers-reduced-motion) {
-		animation-duration: var(--modal-frame-animation-duration);
-	}
+	@include reduce-motion("animation");
 
 	.components-modal__screen-overlay.is-animating-out & {
 		animation-name: components-modal__disappear-animation;
 		animation-timing-function: cubic-bezier(1, 0, 0.2, 1);
 	}
 
+	// Full-screen modals still cover the entire viewport on mobile.
+	&.is-full-screen {
+		margin: 0;
+		width: 100%;
+		height: 100%;
+		max-height: none;
+		border-radius: 0;
+	}
+
 	// Show a centered modal on bigger screens.
 	@include break-small() {
 		border-radius: $radius-large;
 		margin: auto;
```

If we go with the previous suggestion, we may have to also add `align-self: auto;` here

### Review thread 3 (resolved)

#### ciampo on 2026-05-05T15:01:30Z

URL: https://github.com/WordPress/gutenberg/pull/77956#discussion_r3189468000
Path: packages/components/src/modal/style.scss

```diff
@@ -29,48 +22,55 @@
 // The modal window element.
 .components-modal__frame {
 	@include reset;
-	// Use the entire viewport on smaller screens.
-	margin: $grid-unit-50 0 0 0;
+	// On smaller screens, render as a bottom sheet: edge-to-edge, anchored to
+	// the bottom of the viewport, with a height that adapts to the content
+	// (capped so it never covers the full viewport).
+	margin: auto 0 0 0;
 	width: 100%;
+	max-height: calc(100% - #{$grid-unit-50});
 	background: $white;
 	box-shadow: $elevation-large;
 	border-radius: $radius-large $radius-large 0 0;
 	overflow: hidden;
 	// Have the content element fill the vertical space yet not overflow.
 	display: flex;
-	color: $gray-900;
 	// Animate the modal frame/contents appearing on the page.
 	animation-name: components-modal__appear-animation;
+	animation-duration: var(--modal-frame-animation-duration);
 	animation-fill-mode: forwards;
 	animation-timing-function: cubic-bezier(0.29, 0, 0, 1);
-
-	// Ensure all headings use the proper editor text color, overriding wp-admin common.css
-	h1,
-	h2,
-	h3 {
-		color: $gray-900;
-	}
-
-	@media not (prefers-reduced-motion) {
-		animation-duration: var(--modal-frame-animation-duration);
-	}
+	@include reduce-motion("animation");
 
 	.components-modal__screen-overlay.is-animating-out & {
 		animation-name: components-modal__disappear-animation;
 		animation-timing-function: cubic-bezier(1, 0, 0.2, 1);
 	}
 
+	// Full-screen modals still cover the entire viewport on mobile.
+	&.is-full-screen {
+		margin: 0;
+		width: 100%;
+		height: 100%;
+		max-height: none;
+		border-radius: 0;
+	}
+
 	// Show a centered modal on bigger screens.
 	@include break-small() {
 		border-radius: $radius-large;
 		margin: auto;
 		width: auto;
-		min-width: var(--wpds-dimension-surface-width-sm);
+		min-width: $modal-min-width;
 		max-width: calc(100% - #{$grid-unit-20 * 2});
 		max-height: calc(100% - #{$header-height * 2});
 
 		&.is-full-screen {
 			@include break-small() {
```

Nit (and not introduced by this PR): it looks like the `@include break-small() {` here is superflous, since this whole CSS block is already nested in a `@include break-small() {` block a few lines above?

We could take this opportunity to clean this code up a little

### Review thread 4 (resolved)

#### ciampo on 2026-05-06T14:37:43Z

URL: https://github.com/WordPress/gutenberg/pull/77956#discussion_r3196291110
Path: packages/components/CHANGELOG.md

```diff
@@ -5,7 +5,11 @@
 ### Enhancements
 
 -   `Button`: Align `link` variant underline (offset and thickness) with `ExternalLink` and `Link` from `@wordpress/ui` ([#77842](https://github.com/WordPress/gutenberg/pull/77842)).
+<<<<<<< fix/modal-mobile-bottom-sheet
+-   `Modal`: render as a bottom sheet on mobile so the height adapts to the content and CTAs stay within thumb reach ([#77956](https://github.com/WordPress/gutenberg/pull/77956)).
+=======
```

```suggestion
-   `Modal`: render as a bottom sheet on mobile so the height adapts to the content and CTAs stay within thumb reach ([#77956](https://github.com/WordPress/gutenberg/pull/77956)).
```

### Review thread 5 (resolved)

#### ciampo on 2026-05-06T14:38:12Z

URL: https://github.com/WordPress/gutenberg/pull/77956#discussion_r3196294317
Path: packages/components/CHANGELOG.md

```diff
@@ -5,7 +5,9 @@
 ### Enhancements
 
 -   `Button`: Align `link` variant underline (offset and thickness) with `ExternalLink` and `Link` from `@wordpress/ui` ([#77842](https://github.com/WordPress/gutenberg/pull/77842)).
+-   `Modal`: render as a bottom sheet on mobile so the height adapts to the content and CTAs stay within thumb reach ([#77956](https://github.com/WordPress/gutenberg/pull/77956)).
 -   `Text`: Use a theme-aware gray token for the muted variant ([#77999](https://github.com/WordPress/gutenberg/pull/77999)).
+>>>>>>> trunk
```

```suggestion
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/modal/style.scss (MODIFIED, +63/-19)
