---
number: 21091
type: pull_request
state: MERGED
merged_at: 2020-05-13T14:46:50Z
url: https://github.com/WordPress/gutenberg/pull/21091
matched_labels: ["[Package] Components"]
all_labels: ["[Focus] Accessibility (a11y)","[Package] Components","Needs Accessibility Feedback","[Block] Cover"]
---

# PR #21091: Cover: Customizing Alignment of inner content

- URL: https://github.com/WordPress/gutenberg/pull/21091
- Author: ItsJonQ
- Created: 2020-03-23T21:04:37Z
- Updated: 2020-12-31T18:44:35Z
- Comments: 38 of 38
- Review threads: 24 of 24
- Changed files: 24 of 24
- Diff: ../pr-diffs/21091.diff

## Body

## Description

![Screen Capture on 2020-03-25 at 17-26-51](https://user-images.githubusercontent.com/2322354/77587850-14808f80-6ebf-11ea-86be-72a013d7452d.gif)

This update brings the ability to adjust the vertical and horizontal alignment of content with the Cover block. The control is provided by a new (experimental) `AlignmentMatrixControl` component from the `@wordpress/components` package.

Attempts to resolve: https://github.com/WordPress/gutenberg/issues/20836

### Control

![Screen Capture on 2020-03-23 at 16-52-09](https://user-images.githubusercontent.com/2322354/77362601-9342c480-6d27-11ea-975d-c3c7b9fb8ab8.gif)


The new `AlignmentMatrixControl` provides a "9 point" alignment interface. This UI can be controlled by clicking on any of the points. Alternatively, pressing `UP`, `DOWN`, `LEFT`, or `RIGHT` on the keyboard would change the alignment values.

### Icon

![76532112-70d2c080-6476-11ea-8dd0-1fedee597c2f](https://user-images.githubusercontent.com/2322354/77362771-e3218b80-6d27-11ea-908e-be3a71981244.png)

In addition to the control, the `AlignmentMatrixControl` provides a companion `icon`, which can be loaded into UI like the Toolbar. It accepts an `alignment` prop, which updates the UI to visually reflect the alignment.

The icon implementation is designed to work like an icon from `@wordpress/icons`

##### Example

```jsx
import { __experimentalAlignmentMatrixControl as AlignmentMatrixControl } from "@wordpress/components";
import { Icon } from "@wordpress/icons";

const Example = () => {
  return <Icon icon={AlignmentMatrixControl.icon} alignment="bottom left" />;
};

```


## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [x] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->

## Issue comments

### github-actions on 2020-03-23T21:09:50Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-602857588

**Size Change:** +2.51 kB (0%) 

**Total Size:** 831 kB

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/annotations/index.js` | 3.62 kB | -3 B (0%) |  |
| `build/block-directory/index.js` | 6.59 kB | -2 B (0%) |  |
| `build/block-editor/index.js` | 104 kB | +231 B (0%) |  |
| `build/block-editor/style-rtl.css` | 10.8 kB | +32 B (0%) |  |
| `build/block-editor/style.css` | 10.8 kB | +32 B (0%) |  |
| `build/block-library/index.js` | 116 kB | +499 B (0%) |  |
| `build/block-library/style-rtl.css` | 7.48 kB | +145 B (1%) |  |
| `build/block-library/style.css` | 7.49 kB | +143 B (1%) |  |
| `build/blocks/index.js` | 48.1 kB | +15 B (0%) |  |
| `build/components/index.js` | 182 kB | +1.35 kB (0%) |  |
| `build/compose/index.js` | 6.68 kB | +16 B (0%) |  |
| `build/core-data/index.js` | 11.4 kB | -1 B |  |
| `build/data-controls/index.js` | 1.29 kB | +2 B (0%) |  |
| `build/data/index.js` | 8.43 kB | +5 B (0%) |  |
| `build/date/index.js` | 5.47 kB | +1 B |  |
| `build/edit-post/index.js` | 28 kB | +28 B (0%) |  |
| `build/edit-site/index.js` | 12.1 kB | -3 B (0%) |  |
| `build/edit-widgets/index.js` | 8.37 kB | -3 B (0%) |  |
| `build/editor/index.js` | 44.3 kB | +6 B (0%) |  |
| `build/element/index.js` | 4.65 kB | +2 B (0%) |  |
| `build/escape-html/index.js` | 733 B | -1 B |  |
| `build/format-library/index.js` | 7.63 kB | +2 B (0%) |  |
| `build/is-shallow-equal/index.js` | 712 B | +2 B (0%) |  |
| `build/nux/index.js` | 3.4 kB | +2 B (0%) |  |
| `build/plugins/index.js` | 2.56 kB | +2 B (0%) |  |
| `build/redux-routine/index.js` | 2.85 kB | +1 B |  |
| `build/rich-text/index.js` | 14.8 kB | -3 B (0%) |  |
| `build/token-list/index.js` | 1.28 kB | +1 B |  |
| `build/wordcount/index.js` | 1.18 kB | +1 B |  |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size | Change | |
|:--- |:---:|:---:|:---:|
| `build/a11y/index.js` | 1.02 kB | 0 B |  |
| `build/api-fetch/index.js` | 4.08 kB | 0 B |  |
| `build/autop/index.js` | 2.82 kB | 0 B |  |
| `build/blob/index.js` | 620 B | 0 B |  |
| `build/block-directory/style-rtl.css` | 764 B | 0 B |  |
| `build/block-directory/style.css` | 764 B | 0 B |  |
| `build/block-library/editor-rtl.css` | 7.25 kB | 0 B |  |
| `build/block-library/editor.css` | 7.25 kB | 0 B |  |
| `build/block-library/theme-rtl.css` | 683 B | 0 B |  |
| `build/block-library/theme.css` | 685 B | 0 B |  |
| `build/block-serialization-default-parser/index.js` | 1.88 kB | 0 B |  |
| `build/block-serialization-spec-parser/index.js` | 3.1 kB | 0 B |  |
| `build/components/style-rtl.css` | 17.1 kB | 0 B |  |
| `build/components/style.css` | 17 kB | 0 B |  |
| `build/deprecated/index.js` | 772 B | 0 B |  |
| `build/dom-ready/index.js` | 568 B | 0 B |  |
| `build/dom/index.js` | 3.1 kB | 0 B |  |
| `build/edit-navigation/index.js` | 5.59 kB | 0 B |  |
| `build/edit-navigation/style-rtl.css` | 618 B | 0 B |  |
| `build/edit-navigation/style.css` | 617 B | 0 B |  |
| `build/edit-post/style-rtl.css` | 12.2 kB | 0 B |  |
| `build/edit-post/style.css` | 12.2 kB | 0 B |  |
| `build/edit-site/style-rtl.css` | 5.22 kB | 0 B |  |
| `build/edit-site/style.css` | 5.22 kB | 0 B |  |
| `build/edit-widgets/style-rtl.css` | 4.69 kB | 0 B |  |
| `build/edit-widgets/style.css` | 4.69 kB | 0 B |  |
| `build/editor/editor-styles-rtl.css` | 425 B | 0 B |  |
| `build/editor/editor-styles.css` | 428 B | 0 B |  |
| `build/editor/style-rtl.css` | 5.07 kB | 0 B |  |
| `build/editor/style.css` | 5.08 kB | 0 B |  |
| `build/format-library/style-rtl.css` | 502 B | 0 B |  |
| `build/format-library/style.css` | 502 B | 0 B |  |
| `build/hooks/index.js` | 2.13 kB | 0 B |  |
| `build/html-entities/index.js` | 622 B | 0 B |  |
| `build/i18n/index.js` | 3.56 kB | 0 B |  |
| `build/keyboard-shortcuts/index.js` | 2.51 kB | 0 B |  |
| `build/keycodes/index.js` | 1.94 kB | 0 B |  |
| `build/list-reusable-blocks/index.js` | 3.13 kB | 0 B |  |
| `build/list-reusable-blocks/style-rtl.css` | 226 B | 0 B |  |
| `build/list-reusable-blocks/style.css` | 226 B | 0 B |  |
| `build/media-utils/index.js` | 5.29 kB | 0 B |  |
| `build/notices/index.js` | 1.79 kB | 0 B |  |
| `build/nux/style-rtl.css` | 616 B | 0 B |  |
| `build/nux/style.css` | 613 B | 0 B |  |
| `build/primitives/index.js` | 1.5 kB | 0 B |  |
| `build/priority-queue/index.js` | 789 B | 0 B |  |
| `build/server-side-render/index.js` | 2.68 kB | 0 B |  |
| `build/shortcode/index.js` | 1.7 kB | 0 B |  |
| `build/url/index.js` | 4.02 kB | 0 B |  |
| `build/viewport/index.js` | 1.84 kB | 0 B |  |
| `build/warning/index.js` | 1.14 kB | 0 B |  |


</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### ItsJonQ on 2020-03-24T16:50:18Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-603362490

<img width="650" alt="Screen Shot 2020-03-24 at 12 32 11 PM" src="https://user-images.githubusercontent.com/2322354/77453674-eec78d80-6dcd-11ea-9e2c-bf1318837a31.png">

Still very WIP! I have this so far.

In doing so... I'm realizing that it's quite tricky to adjust the size of the Dropdown Menu within this context 🤔 .

### ItsJonQ on 2020-03-25T21:36:16Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-604100617

Oh boy!!

![Screen Capture on 2020-03-25 at 17-26-51](https://user-images.githubusercontent.com/2322354/77587870-1ea28e00-6ebf-11ea-9fea-67addcf1aba4.gif)

### ItsJonQ on 2020-03-26T20:31:53Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-604669285

An observation!

In order for non-centered alignment to look okay, I had to add extra padding around the content. That way, the text doesn't touch the edges.

### pablohoneyhoney on 2020-03-26T20:49:30Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-604678023

Fair! 

It’d be good to test it with an image if reduced within the cover area.

### mtias on 2020-03-26T21:11:48Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-604689069

@ItsJonQ some default padding makes sense, indeed, but it should be just the natural padding of the "cover" block — [which as part of this work](https://github.com/WordPress/gutenberg/pull/16730) we should make configurable as well.

This is looking great!

### ItsJonQ on 2020-03-27T13:21:16Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-604997026

> but it should be just the natural padding of the "cover" block

Ah gotcha! Thanks for that feedback @mtias ! I'll make those adjustments now :)

### ItsJonQ on 2020-03-27T15:13:15Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-605054531

<img width="860" alt="Screen Shot 2020-03-27 at 11 12 22 AM" src="https://user-images.githubusercontent.com/2322354/77770615-e9f81900-701b-11ea-9ac8-f8fd624dd870.png">

Working on a11y support. After that, will add RTL support!

### ItsJonQ on 2020-03-27T15:56:24Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-605077790

## a11y + RTL updates

![Screen Capture on 2020-03-27 at 11-43-18](https://user-images.githubusercontent.com/2322354/77774498-8f61bb80-7021-11ea-8e6f-f3b77f2bdde5.gif)

The GIF above showcases Mac OS voiceover describing the alignment in when changes are made (keyboard navigation). The control is also rendering in `rtl`, where the labels and values are flipped.

The goal for this control is to be able to successfully navigate the 9x9 grid with keyboard (as well as clicks).
The a11y labels should describe the position for each cell.
Lastly, the navigation + labels should flip to reflect RTL documents.

I gave it my best shot, although admittedly, I'm lacking some experience when it comes to a11y support for custom controls such as this one.

(cc'ing @diegohaz)

I would love some insight and expertise here!

I made an attempt at `aria` labels - I am unsure what `WAI-ARIA` role fits this particular 9x9 alignment UI the best.

@gziolo Also pointed out the [Composite component from Reakit](https://next.reakit.io/docs/composite/) to me. If possible, it would be great to leverage it here 😍 

Thank you all so much!

### diegohaz on 2020-03-31T00:02:18Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-606315456

Great job! It's working really well with keyboard. It can be improved for screen readers though. 

> I am unsure what WAI-ARIA role fits this particular 9x9 alignment UI the best.

It's definitely a [grid](https://www.w3.org/TR/wai-aria-practices/#grid). Listbox is unidirectional. Even though sighted users would see that it's a grid and they could guess they could use arrow up and down to navigate vertically, there's no hint for screen reader users, unless you use `role="grid"` (which is equivalente to `<table>`).

I couldn't reproduce what you show on the GIF using VoiceOver + Chrome and Safari. I can only navigate between the grid items using VO (option+ctrl) + directional keys. And it only navigates horizontally. Using up and down arrow keys (without VO key) only moves to the previous and next elements of the type I selected in the VoiceOver Rotor (by pressing up and right keys fast). For example, if it's set to navigate between links, pressing arrow down would put me in the next link. Maybe it's the MacOS version (10.15.3) or some configuration on VoiceOver?

If that's a `role="grid"`, in combination with `role="row"` and `role="gridcell"`, the screen reader will properly announce the row and column numbers and let you use VO + up or down keys to move vertically.

> @gziolo Also pointed out the Composite component from Reakit to me. If possible, it would be great to leverage it here 😍

That's still experimental, but you can see many implementation examples here, including grid: https://codesandbox.io/s/composite-gcqs2

### ItsJonQ on 2020-03-31T13:23:52Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-606624494

@diegohaz Wonderful 😍 ! Thank you so much for your feedback! This is tremendously helpful!
I'll move forward with your suggestions

### ItsJonQ on 2020-04-01T18:04:25Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-607405389

@diegohaz Haii! I started looking into using `Composite`. The API feels great :)

The only thing I'm unsure of is if I'm able to tap into an `onChange` callback of some kind. For example, when I'm using it to display the 9x9 grid, I need to manage state (and pass up) changes.

```jsx
const composite = useCompositeState({
    ...
    onChange: handleOnChange
});
```

Is it possible to do something like this? If not, is there a preferred way of handling this?

Thank you! <3

### diegohaz on 2020-04-01T18:10:39Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-607408614

@ItsJonQ Depending on what you want to do, you can simply attach an `onFocus` or `onClick` handler to the item:
```jsx
<Composite>
  ...
  <CompositeItem onFocus={select} onClick={select} />
  ...
</Composite>
```

If it's not enough, you can track the `composite.currentId` state:
```jsx
// sets initial currentId
const composite = useCompositeState({ currentId: "unique-id-center-center" });

// selects the currentId whenever it changes
React.useEffect(() => {
  select(composite.currentId);
}, [composite.currentId]);

<Composite>
  ...
  <CompositeItem id="unique-id-center-center" />
  ...
</Composite>
```

### ItsJonQ on 2020-04-01T18:39:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-607423451

@diegohaz Ooo! Gotcha. Thank you! I'll give that a shot!

### ItsJonQ on 2020-04-01T20:12:24Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-607466007

Update! I attempted to implement `Composite` into `AlignmentMatrixControl`.
It seemed to work okay, but it's having some issues in the editor.

I've created a separate branch + Draft PR here:
https://github.com/WordPress/gutenberg/pull/21333

### ItsJonQ on 2020-04-03T14:11:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-608455290

Just merged in the a11y work to integrate [Reakit Composite](https://next.reakit.io/docs/composite/) to take care of the interactions and `aria` semantics 🙌 

(Massive props to @diegohaz !)

With that, I think this PR is ready for review

### mtias on 2020-04-06T13:58:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609811870

@ItsJonQ this is really nice. There are a few design details to polish and we should be good to merge it:

![image](https://user-images.githubusercontent.com/548849/78566082-07e52b00-781f-11ea-8944-b6a94d16a534.png)

- The toolbar group needs to combine the width alignments with the matrix alignment (and the color, in the future).
- The toolbar icon should be slightly smaller (the gaps between the points).
- When the dropdown is inactive, we should leave all the dots dark (no blue for the active one).

### ItsJonQ on 2020-04-06T15:05:42Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609851592

@diegohaz + @mtias Thank you both for your feedback! I'm working through the updates now.

P.S. I tried adding `Tooltip` to the matrix cells. When keyboard navigating... this happens:

<img width="144" alt="Screen Shot 2020-04-06 at 11 03 47 AM" src="https://user-images.githubusercontent.com/2322354/78573258-88dbfc80-77f6-11ea-8543-94b91d9379ec.png">

I think I'll avoid Tooltip for now 😂

### mtias on 2020-04-06T15:10:19Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609854145

Ugh, why does that happen?

### diegohaz on 2020-04-06T15:24:45Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609862512

@ItsJonQ I'm pretty sure that's because of `aria-activedescendant`. If you remove `unstable_virtual: true` from `useCompositeState`, that should work.

### ItsJonQ on 2020-04-06T16:27:48Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609898653

I was able to apply the fix to enable Tooltips (thanks @diegohaz !)
Now to group the Toolbar items together (which is proving to be tricky)

### mtias on 2020-04-06T16:50:00Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609910601

We could look at that separately if it's more convoluted.

### ItsJonQ on 2020-04-06T16:54:23Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-609913027

> We could look at that separately if it's more convoluted.

I think that would be better! We can a follow up that improves the Toolbar rendering and perhaps (style) attribute registration.

Maybe something like:

```
export const settings = {
	title: __( 'Cover' ),
	...
	supports: {
		align: true,
		html: false,
		__experimentalContentPosition: true,
	},
	...
```

Which would automatically render the control into the Toolbar.
At the moment, the Control is manually added in the Cover's `edit.js`.

### ItsJonQ on 2020-04-07T14:00:42Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-610404295

@mtias + @pablohoneyhoney 👋 Haii!

No rush! I just wanted to check if you folks were okay merging this PR as it is right now :).
I'm happy to make any adjustments if needed!

Thank you! 🙏

### mtias on 2020-04-08T15:22:13Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-611022176

@ItsJonQ yes, let's get it in! We can keep refining the experience later.

### ItsJonQ on 2020-04-08T21:28:37Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-611204429

> yes, let's get it in! We can keep refining the experience later.

@mtias Awesome! Thank you!! If I could get a review and 👍 (from you, @youknowriad , or anyone else), we can merge it 🙌 

(I've rebased it with latest `master`)

### paaljoachim on 2020-04-10T09:23:18Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-611955623

A cover block can contain multiple inner blocks. For instance a Cover title and a button. How would multiple blocks inside a cover block work together?

### ItsJonQ on 2020-05-11T13:43:58Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-626710493

Haii!!

Following up on this one. How are folks feeling? Do you think we're ready to land this?

Thank you!

### paaljoachim on 2020-05-11T13:58:26Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-626718452

I look forward to testing this out using real world cases.
This should work great with one inner block. 
The tricky part as I see it is when using 2 or more inner blocks and how these would through alignment relate to each other.

### ItsJonQ on 2020-05-11T14:36:57Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-626743011

> The tricky part as I see it is when using 2 or more inner blocks and how these would through alignment relate to each other.

@paaljoachim Thanks! All inner blocks align together based on the matrix control:

![Screen Capture on 2020-05-11 at 10-33-30](https://user-images.githubusercontent.com/2322354/81573851-0c978480-9373-11ea-8270-d7ad963b25cd.gif)

Having finer control for how multiple inner items align with each other would be an interesting evolution to this experience!

### ZebulanStanphill on 2020-05-11T16:05:09Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-626796747

I still have some unaddressed comments on the code, but aside from those, this seems good to go.

### ItsJonQ on 2020-05-11T16:39:51Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-626815916

@ZebulanStanphill Thanks for following up! I went over your items and pushed the updates. Let me know if there's anything I may have missed! 🙏

### ZebulanStanphill on 2020-05-11T18:14:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-626868067

The dropdown for the matrix control lacks G2 borders, but I think that might just be because the branch needs a rebase? It might also not be a problem with this PR specifically, but a general styling issue that needs to be fixed.

![image](https://user-images.githubusercontent.com/19592990/81596169-378cd300-9389-11ea-8e05-3646152b7950.png)

### ItsJonQ on 2020-05-13T13:28:11Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-627985920

@mtias Thank you for reviewing! I've rebased with latest `master`. Will merge this up once Travis is green and happy :)

### ItsJonQ on 2020-05-13T14:46:45Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-628039976

Yay! Merging! 😊

### mtias on 2020-05-13T15:20:51Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-628062337

Nice work, really like this one :)

### paaljoachim on 2020-06-08T21:14:23Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-640890288

Hey

I added a background image. A title and a button below it. But I am having a hard time aligning the title and the button to the middle of the Cover Block.

Title and button have no alignment selected.

<img width="665" alt="Screen Shot 2020-06-08 at 22 41 17" src="https://user-images.githubusercontent.com/5323259/84078426-32975f80-a9d9-11ea-8b2b-bae6f197166c.png">


Aligning the title and the button to center looks like this:
(Grid is center aligned.) Here I have selected both the button and the title.

<img width="670" alt="Screen Shot 2020-06-08 at 22 45 00" src="https://user-images.githubusercontent.com/5323259/84078761-bb160000-a9d9-11ea-9ddc-efbeda1c928a.png">

I am guessing a new issue is needed to address the centering of the Button.
Here is the issue: https://github.com/WordPress/gutenberg/issues/23010

### rmorse on 2020-12-31T18:44:35Z

URL: https://github.com/WordPress/gutenberg/pull/21091#issuecomment-753025007

Found a little bug when opening the control, focus is triggered (and so is onChange) - the fix is here  https://github.com/WordPress/gutenberg/pull/27945

## Review threads

### Review thread 1 (resolved)

#### ZebulanStanphill on 2020-03-23T23:42:48Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r396824444
Path: packages/components/src/alignment-control/utils.js

```diff
@@ -0,0 +1,207 @@
+/**
+ * External dependencies
+ */
+import { isEqual, isUndefined } from 'lodash';
+
+export const DIRECTION = {
+	UP: 'up',
+	DOWN: 'down',
+	LEFT: 'left',
+	RIGHT: 'right',
+};
+
+export const BASE_ALIGNMENTS = [
+	[ 'top', 'left' ],
+	[ 'top', 'center' ],
+	[ 'top', 'right' ],
+	[ 'center', 'left' ],
+	[ 'center', 'center' ],
+	[ 'center', 'right' ],
+	[ 'bottom', 'left' ],
+	[ 'bottom', 'center' ],
+	[ 'bottom', 'right' ],
+];
+
+export const ALIGNMENTS = BASE_ALIGNMENTS.map( transformAlignment );
+export const ALIGNMENT_VALUES = ALIGNMENTS.map( ( a ) => a.join( ' ' ) );
+export const ALIGNMENT_MATRIX = [
+	[ 0, 1, 2 ],
+	[ 3, 4, 5 ],
+	[ 6, 7, 8 ],
+];
+
+export const FLEX_ALIGNMENT_PROPS = [
+	[ 'flex-start', 'flex-start' ],
+	[ 'flex-start', 'center' ],
+	[ 'flex-start', 'flex-end' ],
+	[ 'center', 'flex-start' ],
+	[ 'center', 'center' ],
+	[ 'center', 'flex-end' ],
+	[ 'flex-end', 'flex-start' ],
+	[ 'flex-end', 'center' ],
+	[ 'flex-end', 'flex-end' ],
+];
+
+/**
+ * Transforms an alignment value to an [x, y] alignment data.
+ *
+ * @param {Array|string} alignments Value to transform.
+ *
+ * @return {Array} The [x, y] alignment array, if applicable.
+ */
+export function transformAlignment( alignments = [] ) {
+	let value = alignments;
+
+	if ( typeof alignments === 'string' ) {
+		value = alignments.split( / |-/g );
+	}
+
+	if ( ! Array.isArray( value ) ) {
+		value = [];
+	}
+
+	value = value
+		.map( ( v ) => v.toLowerCase() )
+		// Supports remapping of 'middle' to 'center'
+		.map( ( v ) => v.replace( 'middle', 'center' ) );
+
+	// Handles cases were only 'center' or ['center'] is provided
+	if ( value.length === 1 && value[ 0 ] === 'center' ) {
+		value.push( 'center' );
+	}
+
+	return value.sort();
+}
+
+/**
+ * Checks if a value is a valid alignment.
+ *
+ * @param {string} alignment Value to check.
+ *
+ * @return {boolean} Whether alignment is valid.
+ */
+export function isAlignmentValid( alignment = 'center' ) {
+	const match = getAlignmentValues( alignment );
+
+	return !! match;
+}
+
+/**
+ * Retrieves a matching alignment [x, y] data from a value.
+ *
+ * @param {string} alignment Value to check.
+ *
+ * @return {Array} Matching alignment data.
+ */
+export function getAlignmentValues( alignment = 'center' ) {
+	const values = transformAlignment( alignment );
+
+	if ( values.length > 2 ) {
+		return undefined;
+	}
+
+	const match = ALIGNMENTS.find( ( a ) => isEqual( values, a ) );
+
+	return match;
+}
+
+/**
+ * Retrieves the [x, y] alignment data index from a value.
+ *
+ * @param {string} alignment Value to check.
+ *
+ * @return {number} The index of a matching alignment.
+ */
+export function getAlignmentIndex( alignment = 'center' ) {
+	const item = getAlignmentValues( alignment );
+	const index = ALIGNMENTS.indexOf( item );
+
+	return index > -1 ? index : undefined;
+}
+
+/**
+ * Retrieves the alignment value from an index.
+ *
+ * @param {number} index An alignment index.
+ *
+ * @return {string} The alignment value
+ */
+export function getAlignmentValueFromIndex( index = 0 ) {
+	const align = ALIGNMENTS[ index ] || [];
+
+	return align.join( ' ' );
+}
+
+/**
+ * Retrieves the [x, y] coordinates from the alignment matrix from an index.
+ *
+ * @param {number} index The index to reference.
+ *
+ * @return {Array<number, number>} The [x, y] coordinates.
+ */
+export function getCoordsFromIndex( index = 0 ) {
+	const coords = [ 0, 0 ];
+
+	ALIGNMENT_MATRIX.forEach( ( xv, x ) => {
+		xv.forEach( ( v, y ) => {
+			if ( v === index ) {
+				coords[ 0 ] = x;
+				coords[ 1 ] = y;
+			}
+		} );
+	} );
+
+	return coords;
+}
+
+/**
+ * Retrieves the alignment index given an [x, y] alignment matrix coordinate.
+ *
+ * @param {Array<number, number>} coords The [x, y] matrix coordinates.
+ * @param {number} fallback The fallback index.
+ *
+ * @return {number} The index given a [x, y] coordinate.
+ */
+export function getIndexFromCoords( coords, fallback = 0 ) {
+	const [ x, y ] = coords;
+	const exists =
+		! isUndefined( ALIGNMENT_MATRIX[ x ] ) &&
+		! isUndefined( ALIGNMENT_MATRIX[ x ][ y ] );
```

```suggestion
		ALIGNMENT_MATRIX[ x ] !== undefined &&
		ALIGNMENT_MATRIX[ x ][ y ] !== undefined;
```

### Review thread 2

#### mtias on 2020-03-24T12:38:25Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r397119846
Path: packages/components/src/alignment-control/README.md

```diff
@@ -0,0 +1,18 @@
+# AlignmentControl
```

Should we call this `AlignmentMatrix` to better separate from `BlockAlignment`, etc?

#### ItsJonQ on 2020-03-24T13:11:25Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r397139550
Path: packages/components/src/alignment-control/README.md

```diff
@@ -0,0 +1,18 @@
+# AlignmentControl
```

We could! Although I think having `Control` in the component name would be helpful, as it has the `value/onChange` interface.

`AlignmentMatrixControl`? or `AlignmentGridControl`?

#### mtias on 2020-03-24T17:47:09Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r397346804
Path: packages/components/src/alignment-control/README.md

```diff
@@ -0,0 +1,18 @@
+# AlignmentControl
```

Makes sense, especially if we reserve the `AlignmentMatrix` for the toolbar component.

### Review thread 3 (resolved)

#### ZebulanStanphill on 2020-03-27T14:42:17Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r399314522
Path: packages/block-library/src/cover/style.scss

```diff
@@ -106,6 +106,57 @@
 			color: inherit;
 		}
 	}
+
+	// Position: Top
+	&.is-position-top-left {
+		align-items: flex-start;
+		justify-content: flex-start;
+	}
+	&.is-position-top-center {
+		align-items: flex-start;
+		justify-content: center;
+	}
+	&.is-position-top-right {
+		align-items: flex-start;
+		justify-content: flex-end;
+	}
+	// Position: Center
+	&.is-position-center-left {
+		align-items: center;
+		justify-content: flex-start;
+	}
+	&.is-position-center-center {
+		align-items: center;
+		justify-content: center;
+	}
+	&.is-position-center-right {
+		align-items: center;
+		justify-content: flex-end;
+	}
+	// Position: Bottom
+	&.is-position-bottom-left {
+		align-items: flex-end;
+		justify-content: flex-start;
+	}
+	&.is-position-bottom-center {
+		align-items: flex-end;
+		justify-content: center;
+	}
+	&.is-position-bottom-right {
+		align-items: flex-end;
+		justify-content: flex-end;
+	}
+
+	&.has-custom-content-position.has-custom-content-position {
+		.wp-block-cover__inner-container {
+			margin: 0;
+			width: auto;
+		}
+	}
+}
+
+.wp-block-cover {
+	padding: $grid-unit-20;
```

This style belongs with the other general Cover styles at the top of the file, doesn't it?

#### ItsJonQ on 2020-05-11T16:37:58Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423170369
Path: packages/block-library/src/cover/style.scss

```diff
@@ -106,6 +106,57 @@
 			color: inherit;
 		}
 	}
+
+	// Position: Top
+	&.is-position-top-left {
+		align-items: flex-start;
+		justify-content: flex-start;
+	}
+	&.is-position-top-center {
+		align-items: flex-start;
+		justify-content: center;
+	}
+	&.is-position-top-right {
+		align-items: flex-start;
+		justify-content: flex-end;
+	}
+	// Position: Center
+	&.is-position-center-left {
+		align-items: center;
+		justify-content: flex-start;
+	}
+	&.is-position-center-center {
+		align-items: center;
+		justify-content: center;
+	}
+	&.is-position-center-right {
+		align-items: center;
+		justify-content: flex-end;
+	}
+	// Position: Bottom
+	&.is-position-bottom-left {
+		align-items: flex-end;
+		justify-content: flex-start;
+	}
+	&.is-position-bottom-center {
+		align-items: flex-end;
+		justify-content: center;
+	}
+	&.is-position-bottom-right {
+		align-items: flex-end;
+		justify-content: flex-end;
+	}
+
+	&.has-custom-content-position.has-custom-content-position {
+		.wp-block-cover__inner-container {
+			margin: 0;
+			width: auto;
+		}
+	}
+}
+
+.wp-block-cover {
+	padding: $grid-unit-20;
```

Indeed it does! I can't remember why I had kept it outside that scope. In any case, I'll make that update.

Thanks!

### Review thread 4 (resolved)

#### ZebulanStanphill on 2020-03-27T14:44:37Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r399316298
Path: packages/block-library/src/cover/editor.scss

```diff
@@ -36,6 +36,10 @@
 	}
 }
 
+.wp-block-cover {
+	padding: $grid-unit-20 $grid-unit-40;
```

Again, these styles belong at the top of the file. Also, why are we setting the padding in `editor.scss` if we already set it in `style.scss`? And why is the value different here?

#### ItsJonQ on 2020-05-11T16:28:18Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423164431
Path: packages/block-library/src/cover/editor.scss

```diff
@@ -36,6 +36,10 @@
 	}
 }
 
+.wp-block-cover {
+	padding: $grid-unit-20 $grid-unit-40;
```

Thank you for catching that. I'll move this one 👍

### Review thread 5

#### diegohaz on 2020-04-03T16:55:13Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r403143810
Path: packages/components/src/alignment-matrix-control/stories/index.js

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import { number, select } from '@storybook/addon-knobs';
+/**
+ * WordPress dependencies
+ */
+import { Icon as BaseIcon } from '@wordpress/icons';
+/**
+ * Internal dependencies
+ */
+import AlignmentMatrixControl from '../';
+import { ALIGNMENTS } from '../utils';
+
+const alignmentOptions = ALIGNMENTS.reduce( ( options, item ) => {
+	return { ...options, [ item ]: item };
+}, {} );
+
+export default {
+	title: 'Components/AlignmentMatrixControl',
+	component: AlignmentMatrixControl,
+};
+
+export const _default = () => {
+	return <AlignmentMatrixControl />;
+};
+
+export const icon = () => {
+	const props = {
+		value: select( 'value', alignmentOptions, 'center center' ),
+		size: number( 'size', 24 ),
+	};
+
+	return (
+		<>
+			<BaseIcon icon={ AlignmentMatrixControl.icon } { ...props } />
+		</>
```

Is the fragment needed?

#### ItsJonQ on 2020-04-06T13:10:45Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r404077686
Path: packages/components/src/alignment-matrix-control/stories/index.js

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import { number, select } from '@storybook/addon-knobs';
+/**
+ * WordPress dependencies
+ */
+import { Icon as BaseIcon } from '@wordpress/icons';
+/**
+ * Internal dependencies
+ */
+import AlignmentMatrixControl from '../';
+import { ALIGNMENTS } from '../utils';
+
+const alignmentOptions = ALIGNMENTS.reduce( ( options, item ) => {
+	return { ...options, [ item ]: item };
+}, {} );
+
+export default {
+	title: 'Components/AlignmentMatrixControl',
+	component: AlignmentMatrixControl,
+};
+
+export const _default = () => {
+	return <AlignmentMatrixControl />;
+};
+
+export const icon = () => {
+	const props = {
+		value: select( 'value', alignmentOptions, 'center center' ),
+		size: number( 'size', 24 ),
+	};
+
+	return (
+		<>
+			<BaseIcon icon={ AlignmentMatrixControl.icon } { ...props } />
+		</>
```

Nope! Thanks for catching! Will remove

### Review thread 6

#### diegohaz on 2020-04-03T17:02:01Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r403149840
Path: packages/components/src/alignment-matrix-control/stories/index.js

```diff
@@ -0,0 +1,39 @@
+/**
+ * External dependencies
+ */
+import { number, select } from '@storybook/addon-knobs';
+/**
+ * WordPress dependencies
+ */
+import { Icon as BaseIcon } from '@wordpress/icons';
+/**
+ * Internal dependencies
+ */
+import AlignmentMatrixControl from '../';
+import { ALIGNMENTS } from '../utils';
+
+const alignmentOptions = ALIGNMENTS.reduce( ( options, item ) => {
+	return { ...options, [ item ]: item };
+}, {} );
+
+export default {
+	title: 'Components/AlignmentMatrixControl',
+	component: AlignmentMatrixControl,
+};
+
+export const _default = () => {
+	return <AlignmentMatrixControl />;
```

Maybe we can add knobs here too. I found it useful to check if the controlled version of `AlignmentMatrixControl` was working while working on #21333.

### Review thread 7

#### diegohaz on 2020-04-03T21:12:38Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r403338440
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,55 @@
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Transforms GRID into a flat Array of values
+export const ALIGNMENTS = GRID.reduce( ( alignments, row ) => {
+	row.forEach( ( cell ) => {
+		alignments.push( cell );
+	} );
+
+	return alignments;
+}, [] );
+
+/**
+ * Parses and transforms an incoming value to better match the alignment values
+ *
+ * @param {string} value An alignment value to parse.
+ *
+ * @return {string} The parsed value.
+ */
+export function parseValue( value ) {
+	const nextValue = value === 'center' ? 'center center' : value;
+
+	return nextValue.replace( '-', ' ' );
+}
+
+/**
+ * Creates an item ID based on a prefix ID and an alignment value.
+ *
+ * @param {string} prefixId An ID to prefix.
+ * @param {string} value An alignment value.
+ *
+ * @return {string} The item id.
+ */
+export function getItemId( prefixId, value ) {
+	const valueId = parseValue( value ).replace( '-', ' ' );
```

It's generating IDs with spaces. Shouldn't it be `replace( ' ', '-' )`?

### Review thread 8

#### diegohaz on 2020-04-03T21:13:30Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r403338805
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,55 @@
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Transforms GRID into a flat Array of values
+export const ALIGNMENTS = GRID.reduce( ( alignments, row ) => {
+	row.forEach( ( cell ) => {
+		alignments.push( cell );
+	} );
+
+	return alignments;
+}, [] );
+
+/**
+ * Parses and transforms an incoming value to better match the alignment values
+ *
+ * @param {string} value An alignment value to parse.
+ *
+ * @return {string} The parsed value.
+ */
+export function parseValue( value ) {
+	const nextValue = value === 'center' ? 'center center' : value;
+
+	return nextValue.replace( '-', ' ' );
+}
+
+/**
+ * Creates an item ID based on a prefix ID and an alignment value.
+ *
+ * @param {string} prefixId An ID to prefix.
+ * @param {string} value An alignment value.
+ *
+ * @return {string} The item id.
+ */
+export function getItemId( prefixId, value ) {
+	const valueId = parseValue( value ).replace( '-', ' ' );
+
+	return `${ prefixId }-${ valueId }`;
+}
+
+/**
+ * Retrieves the alignment index from a value.
+ *
+ * @param {string} alignment Value to check.
+ *
+ * @return {number} The index of a matching alignment.
+ */
+export function getAlignmentIndex( alignment = 'center' ) {
+	const item = parseValue( alignment ).replace( '-', ' ' );
```

`parseValue` is already replacing `-` by spaces.

### Review thread 9

#### diegohaz on 2020-04-03T21:28:51Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r403345240
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,55 @@
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Transforms GRID into a flat Array of values
+export const ALIGNMENTS = GRID.reduce( ( alignments, row ) => {
+	row.forEach( ( cell ) => {
+		alignments.push( cell );
+	} );
+
+	return alignments;
+}, [] );
+
+/**
+ * Parses and transforms an incoming value to better match the alignment values
+ *
+ * @param {string} value An alignment value to parse.
+ *
+ * @return {string} The parsed value.
+ */
+export function parseValue( value ) {
```

Maybe it should have a better name to communicate that it's replacing `-` by spaces?

### Review thread 10

#### mtias on 2020-04-06T13:50:03Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r404105432
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,124 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	hasFocusBorder = true,
+	defaultValue = 'center center',
+	value,
+	onBlur = noop,
+	onChange = noop,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+		unstable_virtual: true,
+	} );
+
+	const handleOnBlur = ( event ) => {
+		const isItemCurrent = composite.items.some(
+			( item ) => item.ref.current === event.relatedTarget
+		);
+
+		if ( isItemCurrent ) {
+			event.stopPropagation();
+		}
+
+		onBlur( event );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			hasFocusBorder={ hasFocusBorder }
+			onBlur={ handleOnBlur }
+			role="grid"
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+
+						return (
+							<Cell
+								{ ...composite }
+								isActive={ composite.currentId === cellId }
+								key={ cell }
+								id={ cellId }
+								value={ cell }
+								onFocus={ () => onChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
```

Should we include some instructions on what it does here? "Align the contents of the block to one of the grid points". Maybe it's unnecessary.

### Review thread 11

#### diegohaz on 2020-04-06T14:46:04Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r404149308
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,131 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	hasFocusBorder = true,
+	defaultValue = 'center center',
+	value,
+	onBlur = noop,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+		unstable_virtual: true,
```

If items need to show tooltips, I would recommend not to use `aria-activedescendant` here (by removing this `unstable_virtual: true`). It would also make the `handleOnBlur` workaround unnecessary.

#### ItsJonQ on 2020-04-06T15:34:26Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r404186802
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,131 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	hasFocusBorder = true,
+	defaultValue = 'center center',
+	value,
+	onBlur = noop,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+		unstable_virtual: true,
```

That works @diegohaz !! Thank you 🙏

### Review thread 12 (resolved)

#### youknowriad on 2020-04-09T11:08:55Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406129321
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
```

I know we have this tendency to make a lot of things experimental but I'd like to ask what level of confidence we have over the naming and the props (API) of this component. If it's a high level, maybe we can just make it stable?

#### ItsJonQ on 2020-04-14T14:44:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408193169
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
```

> what level of confidence we have over the naming and the props (API) of this component

I wasn't sure of that distinction either. I've been being conservative, and defaulting to using the `__experimental` prefix.

At being said... My thinking is that I'm currently feeling quite confident. In that case...

* Merge
* Wait a couple of weeks to see if any feedback/bugs pop up
* If nothing, then, safe to remove experimental flag

#### mtias on 2020-05-12T16:52:49Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423886428
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
```

Agreed, I think we should possibly apply this to the _Group_ block once merged and see if it works fine before considering removing the _experimental prefix.

### Review thread 13

#### youknowriad on 2020-04-09T11:13:05Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406131276
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/style.scss

```diff
@@ -0,0 +1,7 @@
+.block-editor-block-alignment-matrix-toolbar {
+	.components-popover__content {
+		min-width: 0;
+		padding: $grid-unit;
+		width: auto;
+	}
```

Curious to know why we need custom styling and whether these could be abstracted somehow in Dropdown.

#### ItsJonQ on 2020-04-16T14:39:35Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409610342
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/style.scss

```diff
@@ -0,0 +1,7 @@
+.block-editor-block-alignment-matrix-toolbar {
+	.components-popover__content {
+		min-width: 0;
+		padding: $grid-unit;
+		width: auto;
+	}
```

Currently needed as there's no other way to style the dropdown menu.
It would be lovely to abstract it to Dropdown - perhaps out of scope for this PR?

### Review thread 14 (resolved)

#### youknowriad on 2020-04-09T11:13:30Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406131481
Path: packages/block-editor/src/components/index.js

```diff
@@ -8,6 +8,7 @@ export * from './font-sizes';
 export { default as AlignmentToolbar } from './alignment-toolbar';
 export { default as Autocomplete } from './autocomplete';
 export { default as BlockAlignmentToolbar } from './block-alignment-toolbar';
+export { default as __experimentalBlockAlignmentMatrixToolbar } from './block-alignment-matrix-toolbar';
```

Same question about the "experimental" aspect.

### Review thread 15

#### youknowriad on 2020-04-09T11:19:38Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406134357
Path: packages/components/src/alignment-matrix-control/icon.js

```diff
@@ -0,0 +1,62 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { ALIGNMENTS, getAlignmentIndex } from './utils';
+import {
+	Root,
+	Cell,
+	Point,
+} from './styles/alignment-matrix-control-icon-styles';
+
+const BASE_SIZE = 24;
+
+export default function AlignmentMatrixControlIcon( {
+	className,
+	disablePointerEvents = true,
+	height,
+	size: sizeProp = BASE_SIZE,
+	style = {},
+	value = 'center',
+	width,
+	...props
```

Feels like there's too much props for a public API component. Do we really need all these configs? I'd have thought we need just "value" and potentially "size"

#### ItsJonQ on 2020-04-14T15:55:44Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408249019
Path: packages/components/src/alignment-matrix-control/icon.js

```diff
@@ -0,0 +1,62 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { ALIGNMENTS, getAlignmentIndex } from './utils';
+import {
+	Root,
+	Cell,
+	Point,
+} from './styles/alignment-matrix-control-icon-styles';
+
+const BASE_SIZE = 24;
+
+export default function AlignmentMatrixControlIcon( {
+	className,
+	disablePointerEvents = true,
+	height,
+	size: sizeProp = BASE_SIZE,
+	style = {},
+	value = 'center',
+	width,
+	...props
```

Adding a code comment re: `width` and `height`

```
	/**
	 * When this component is used on it's own,
	 * the size prop is preferred.
	 *
	 * Example:
	 * <AligmentMatrixControlIcon size={ 20 } />
	 *
	 * However, when used with the <Icon /> component from @wordpress/icons,
	 * width/height are passed in from <Icon />.
	 *
	 * Example:
	 * <Icon icon={ AlignmmentMatrixControlIcon } />
	 */
```

#### youknowriad on 2020-04-16T10:02:37Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409435572
Path: packages/components/src/alignment-matrix-control/icon.js

```diff
@@ -0,0 +1,62 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { ALIGNMENTS, getAlignmentIndex } from './utils';
+import {
+	Root,
+	Cell,
+	Point,
+} from './styles/alignment-matrix-control-icon-styles';
+
+const BASE_SIZE = 24;
+
+export default function AlignmentMatrixControlIcon( {
+	className,
+	disablePointerEvents = true,
+	height,
+	size: sizeProp = BASE_SIZE,
+	style = {},
+	value = 'center',
+	width,
+	...props
```

This would solve it right ?

```
<Icon icon={ <AlignmentMatrixControl.Icon size={ something } /> } />
```

#### ItsJonQ on 2020-04-16T14:36:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409607721
Path: packages/components/src/alignment-matrix-control/icon.js

```diff
@@ -0,0 +1,62 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { ALIGNMENTS, getAlignmentIndex } from './utils';
+import {
+	Root,
+	Cell,
+	Point,
+} from './styles/alignment-matrix-control-icon-styles';
+
+const BASE_SIZE = 24;
+
+export default function AlignmentMatrixControlIcon( {
+	className,
+	disablePointerEvents = true,
+	height,
+	size: sizeProp = BASE_SIZE,
+	style = {},
+	value = 'center',
+	width,
+	...props
```

Indeed it would! Making those changes now

### Review thread 16 (resolved)

#### youknowriad on 2020-04-09T11:21:11Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406135152
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
```

Do we really need this customized id? Why not just use the regular `useInstanceId`. 
Alternatively, we could support prefixes in `useInstanceId` directly

#### ItsJonQ on 2020-04-14T16:06:10Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408256624
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
```

> Alternatively, we could support prefixes in useInstanceId directly

I think that may be a good idea! It looks like manually prefixing `instanceId` seems to be the common pattern, example:

```js
	const instanceId = useInstanceId( AnglePickerControl );
	const inputId = `components-angle-picker-control__input-${ instanceId }`;
```

### Review thread 17

#### youknowriad on 2020-04-09T11:22:04Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406135590
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			role="grid"
+			width={ width }
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+						const isActive = composite.currentId === cellId;
+
+						return (
+							<Cell
+								{ ...composite }
+								id={ cellId }
+								isActive={ isActive }
+								key={ cell }
+								value={ cell }
+								onFocus={ () => handleOnChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
+		</Composite>
+	);
+}
+
+AlignmentMatrixControl.Icon = AlignmentMatrixControlIcon;
+AlignmentMatrixControl.icon = <AlignmentMatrixControlIcon />;
```

Very weird to expose both the component and the element? do we really need the element?

#### ItsJonQ on 2020-04-14T16:13:51Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408262056
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			role="grid"
+			width={ width }
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+						const isActive = composite.currentId === cellId;
+
+						return (
+							<Cell
+								{ ...composite }
+								id={ cellId }
+								isActive={ isActive }
+								key={ cell }
+								value={ cell }
+								onFocus={ () => handleOnChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
+		</Composite>
+	);
+}
+
+AlignmentMatrixControl.Icon = AlignmentMatrixControlIcon;
+AlignmentMatrixControl.icon = <AlignmentMatrixControlIcon />;
```

I just added this comment:

```
/**
 * Exported rendered icon, which can be used with the <Icon /> component
 * from @wordpress/icons.
 *
 * Example:
 * <Icon icon={ AlignmentMatrixControl.icon } />
 */
```

The idea was to respect how the `<Icon />` component's `icon` prop worked, which expects elements for it to render correctly.

Let me know if this makes sense! I'm open and happy to make changes

#### youknowriad on 2020-04-15T08:04:40Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408655734
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			role="grid"
+			width={ width }
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+						const isActive = composite.currentId === cellId;
+
+						return (
+							<Cell
+								{ ...composite }
+								id={ cellId }
+								isActive={ isActive }
+								key={ cell }
+								value={ cell }
+								onFocus={ () => handleOnChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
+		</Composite>
+	);
+}
+
+AlignmentMatrixControl.Icon = AlignmentMatrixControlIcon;
+AlignmentMatrixControl.icon = <AlignmentMatrixControlIcon />;
```

I still find it confusing personally to export the same thing twice. One way to solve this is

```
<Icon icon={ <AlignmentMatrixControl.Icon /> } />
```

#### ItsJonQ on 2020-04-15T13:25:19Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408838497
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			role="grid"
+			width={ width }
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+						const isActive = composite.currentId === cellId;
+
+						return (
+							<Cell
+								{ ...composite }
+								id={ cellId }
+								isActive={ isActive }
+								key={ cell }
+								value={ cell }
+								onFocus={ () => handleOnChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
+		</Composite>
+	);
+}
+
+AlignmentMatrixControl.Icon = AlignmentMatrixControlIcon;
+AlignmentMatrixControl.icon = <AlignmentMatrixControlIcon />;
```

We totally could! Does it feel strange that the following are inconsistent? (I'm not opposed, just wanted to double check)

```
<Icon icon={ <AlignmentMatrixControl.Icon /> } />

<Icon icon={ chevronLeft } />
```

#### youknowriad on 2020-04-16T10:01:23Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409434850
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			role="grid"
+			width={ width }
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+						const isActive = composite.currentId === cellId;
+
+						return (
+							<Cell
+								{ ...composite }
+								id={ cellId }
+								isActive={ isActive }
+								key={ cell }
+								value={ cell }
+								onFocus={ () => handleOnChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
+		</Composite>
+	);
+}
+
+AlignmentMatrixControl.Icon = AlignmentMatrixControlIcon;
+AlignmentMatrixControl.icon = <AlignmentMatrixControlIcon />;
```

I personally prefer it this way, `<Icon icon={ <AlignmentMatrixControl.Icon /> } />` This is totally reasonable for me. In fact we already do that for somethings. I believe we had something like that for the heading icons at some point (levels)

#### ItsJonQ on 2020-04-16T14:36:02Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409607617
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId( AlignmentMatrixControl );
+	const prefix = id || 'alignment-matrix-control';
+
+	return `${ prefix }-${ instanceId }`;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
+			composite.setCurrentId( getItemId( baseId, value ) );
+		}
+	}, [ value, composite.setCurrentId ] );
+
+	const classes = classnames(
+		'component-alignment-matrix-control',
+		className
+	);
+
+	return (
+		<Composite
+			{ ...props }
+			{ ...composite }
+			aria-label={ label }
+			as={ Root }
+			className={ classes }
+			role="grid"
+			width={ width }
+		>
+			{ GRID.map( ( cells, index ) => (
+				<CompositeGroup
+					{ ...composite }
+					as={ Row }
+					role="row"
+					key={ index }
+				>
+					{ cells.map( ( cell ) => {
+						const cellId = getItemId( baseId, cell );
+						const isActive = composite.currentId === cellId;
+
+						return (
+							<Cell
+								{ ...composite }
+								id={ cellId }
+								isActive={ isActive }
+								key={ cell }
+								value={ cell }
+								onFocus={ () => handleOnChange( cell ) }
+								onClick={ () =>
+									// VoiceOver doesn't focus elements on click
+									composite.move( cellId )
+								}
+							/>
+						);
+					} ) }
+				</CompositeGroup>
+			) ) }
+		</Composite>
+	);
+}
+
+AlignmentMatrixControl.Icon = AlignmentMatrixControlIcon;
+AlignmentMatrixControl.icon = <AlignmentMatrixControlIcon />;
```

Cool! Works for me :D. I'll make that update

### Review thread 18 (resolved)

#### youknowriad on 2020-04-09T11:24:31Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406136780
Path: packages/components/src/utils/hooks.js

```diff
@@ -0,0 +1,28 @@
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect, useRef } from '@wordpress/element';
+
+/**
+ * An enhanced useState hook that stores and updates an internal state based on
+ * an incoming state value.
+ *
+ * This is useful for input/control based components.
+ *
+ * @param {any} initialState
+ *
+ * @return {Array<any, Function>} Controlled [state, setState] values.
+ */
+export function useControlledState( initialState ) {
```

This doesn't seem used right?

#### ItsJonQ on 2020-04-14T14:45:58Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408194701
Path: packages/components/src/utils/hooks.js

```diff
@@ -0,0 +1,28 @@
+/**
+ * WordPress dependencies
+ */
+import { useState, useEffect, useRef } from '@wordpress/element';
+
+/**
+ * An enhanced useState hook that stores and updates an internal state based on
+ * an incoming state value.
+ *
+ * This is useful for input/control based components.
+ *
+ * @param {any} initialState
+ *
+ * @return {Array<any, Function>} Controlled [state, setState] values.
+ */
+export function useControlledState( initialState ) {
```

Ah! You are correct. It was previously used before we implemented `Composite` from Reakit.
I'll remove it now. Thank you 🙏

### Review thread 19

#### ZebulanStanphill on 2020-04-09T17:15:00Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406354221
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,73 @@
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Stored as map as i18n __() only accepts strings (not variables)
+export const ALIGNMENT_LABEL = {
+	'top left': __( 'Top Left' ),
+	'top center': __( 'Top Center' ),
+	'top right': __( 'Top Right' ),
+	'center left': __( 'Center Left' ),
+	'center center': __( 'Center Center' ),
+	'center right': __( 'Center Right' ),
+	'bottom left': __( 'Bottom Left' ),
+	'bottom center': __( 'Bottom Center' ),
+	'bottom right': __( 'Bottom Right' ),
+};
+
+// Transforms GRID into a flat Array of values
+export const ALIGNMENTS = GRID.reduce( ( alignments, row ) => {
+	row.forEach( ( cell ) => {
+		alignments.push( cell );
+	} );
```

This could be simplified a bit:
```suggestion
	for ( const cell of row ) {
		alignments.push( cell );
	}
```

### Review thread 20 (resolved)

#### ZebulanStanphill on 2020-04-09T17:20:39Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r406357433
Path: packages/components/src/alignment-matrix-control/icon.js

```diff
@@ -0,0 +1,62 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { ALIGNMENTS, getAlignmentIndex } from './utils';
+import {
+	Root,
+	Cell,
+	Point,
+} from './styles/alignment-matrix-control-icon-styles';
+
+const BASE_SIZE = 24;
+
+export default function AlignmentMatrixControlIcon( {
+	className,
+	disablePointerEvents = true,
+	height,
+	size: sizeProp = BASE_SIZE,
+	style = {},
+	value = 'center',
+	width,
+	...props
+} ) {
+	const alignIndex = getAlignmentIndex( value );
+	const size = width || height || sizeProp;
+	const scale = ( size / BASE_SIZE ).toFixed( 2 );
+
+	const classes = classnames(
+		'component-alignment-matrix-control-icon',
+		className
+	);
+
+	const styles = {
+		...style,
+		transform: `scale(${ scale })`,
+	};
+
+	return (
+		<Root
+			{ ...props }
+			className={ classes }
+			disablePointerEvents={ disablePointerEvents }
+			role="presentation"
+			size={ size }
+			style={ styles }
+		>
+			{ ALIGNMENTS.map( ( align, index ) => {
+				const isActive = alignIndex === index;
```

Why not just compare the current `align` to `value`?

#### ItsJonQ on 2020-04-14T16:01:08Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r408252965
Path: packages/components/src/alignment-matrix-control/icon.js

```diff
@@ -0,0 +1,62 @@
+/**
+ * External dependencies
+ */
+import classnames from 'classnames';
+
+/**
+ * Internal dependencies
+ */
+import { ALIGNMENTS, getAlignmentIndex } from './utils';
+import {
+	Root,
+	Cell,
+	Point,
+} from './styles/alignment-matrix-control-icon-styles';
+
+const BASE_SIZE = 24;
+
+export default function AlignmentMatrixControlIcon( {
+	className,
+	disablePointerEvents = true,
+	height,
+	size: sizeProp = BASE_SIZE,
+	style = {},
+	value = 'center',
+	width,
+	...props
+} ) {
+	const alignIndex = getAlignmentIndex( value );
+	const size = width || height || sizeProp;
+	const scale = ( size / BASE_SIZE ).toFixed( 2 );
+
+	const classes = classnames(
+		'component-alignment-matrix-control-icon',
+		className
+	);
+
+	const styles = {
+		...style,
+		transform: `scale(${ scale })`,
+	};
+
+	return (
+		<Root
+			{ ...props }
+			className={ classes }
+			disablePointerEvents={ disablePointerEvents }
+			role="presentation"
+			size={ size }
+			style={ styles }
+		>
+			{ ALIGNMENTS.map( ( align, index ) => {
+				const isActive = alignIndex === index;
```

The `value` coming in may not match `align`.
The `getAlignmentIndex` function parses/transforms a `value` prop for accurate reading and comparison purposes.

Originally, the transforming part was more complicated as it allowed for more flexibility in positions, example: both `top center` and `center top` evaluated to the same thing.

Since working with @diegohaz , that logic has been simplified.
It now handles the ability to read `center` as `center center`.

### Review thread 21 (resolved)

#### ZebulanStanphill on 2020-04-13T13:41:18Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r407483355
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
+} from '@wordpress/components';
+
+export function BlockAlignmentMatrixToolbar( props ) {
+	const {
+		label = __( 'Change matrix alignment' ),
+		onChange = noop,
+		value = 'center',
+	} = props;
+
+	const icon = <AlignmentMatrixControl.Icon value={ value } />;
+	const className = 'block-editor-block-alignment-matrix-toolbar';
+
+	return (
+		<Dropdown
+			position="bottom right"
+			className={ className }
+			popoverProps={ { className } }
```

I'm pretty sure you shouldn't be using the same `className` for both of these.

#### ItsJonQ on 2020-04-16T14:49:48Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409618589
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
+} from '@wordpress/components';
+
+export function BlockAlignmentMatrixToolbar( props ) {
+	const {
+		label = __( 'Change matrix alignment' ),
+		onChange = noop,
+		value = 'center',
+	} = props;
+
+	const icon = <AlignmentMatrixControl.Icon value={ value } />;
+	const className = 'block-editor-block-alignment-matrix-toolbar';
+
+	return (
+		<Dropdown
+			position="bottom right"
+			className={ className }
+			popoverProps={ { className } }
```

It is needed to target the Dropdown (popover) menu, since the menu is being Portal'ed elsewhere in the DOM.

#### ZebulanStanphill on 2020-04-16T18:31:33Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409765622
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
+} from '@wordpress/components';
+
+export function BlockAlignmentMatrixToolbar( props ) {
+	const {
+		label = __( 'Change matrix alignment' ),
+		onChange = noop,
+		value = 'center',
+	} = props;
+
+	const icon = <AlignmentMatrixControl.Icon value={ value } />;
+	const className = 'block-editor-block-alignment-matrix-toolbar';
+
+	return (
+		<Dropdown
+			position="bottom right"
+			className={ className }
+			popoverProps={ { className } }
```

Maybe I'm misunderstanding what you mean, but it should still use a distinct `className`, shouldn't it?

#### ItsJonQ on 2020-05-11T16:29:49Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423165418
Path: packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js

```diff
@@ -0,0 +1,66 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { DOWN } from '@wordpress/keycodes';
+import {
+	Button,
+	Dropdown,
+	ToolbarGroup,
+	__experimentalAlignmentMatrixControl as AlignmentMatrixControl,
+} from '@wordpress/components';
+
+export function BlockAlignmentMatrixToolbar( props ) {
+	const {
+		label = __( 'Change matrix alignment' ),
+		onChange = noop,
+		value = 'center',
+	} = props;
+
+	const icon = <AlignmentMatrixControl.Icon value={ value } />;
+	const className = 'block-editor-block-alignment-matrix-toolbar';
+
+	return (
+		<Dropdown
+			position="bottom right"
+			className={ className }
+			popoverProps={ { className } }
```

I suppose it could. I'll make that adjustment

### Review thread 22 (resolved)

#### ZebulanStanphill on 2020-04-16T18:33:14Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r409766615
Path: packages/components/src/alignment-matrix-control/index.js

```diff
@@ -0,0 +1,115 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import classnames from 'classnames';
+import {
+	unstable_useCompositeState as useCompositeState,
+	unstable_Composite as Composite,
+	unstable_CompositeGroup as CompositeGroup,
+} from 'reakit';
+
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+import { useInstanceId } from '@wordpress/compose';
+import { useState, useEffect } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import Cell from './cell';
+import { Root, Row } from './styles/alignment-matrix-control-styles';
+import { useRTL } from '../utils/rtl';
+import AlignmentMatrixControlIcon from './icon';
+import { GRID, getItemId } from './utils';
+
+function useBaseId( id ) {
+	const instanceId = useInstanceId(
+		AlignmentMatrixControl,
+		'alignment-matrix-control'
+	);
+
+	return id || instanceId;
+}
+
+export default function AlignmentMatrixControl( {
+	className,
+	id,
+	label = __( 'Alignment Matrix Control' ),
+	defaultValue = 'center center',
+	value,
+	onChange = noop,
+	width = 92,
+	...props
+} ) {
+	const [ immutableDefaultValue ] = useState( value ?? defaultValue );
+	const isRTL = useRTL();
+	const baseId = useBaseId( id );
+	const initialCurrentId = getItemId( baseId, immutableDefaultValue );
+
+	const composite = useCompositeState( {
+		baseId,
+		currentId: initialCurrentId,
+		rtl: isRTL,
+	} );
+
+	const handleOnChange = ( nextValue ) => {
+		onChange( nextValue );
+	};
+
+	useEffect( () => {
+		if ( typeof value !== 'undefined' ) {
```

```suggestion
		if ( value !== undefined ) {
```

### Review thread 23

#### mtias on 2020-05-12T10:22:45Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423626008
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,67 @@
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Stored as map as i18n __() only accepts strings (not variables)
+export const ALIGNMENT_LABEL = {
+	'top left': __( 'Top Left' ),
+	'top center': __( 'Top Center' ),
+	'top right': __( 'Top Right' ),
+	'center left': __( 'Center Left' ),
+	'center center': __( 'Center Center' ),
```

nit: but I think we should use `middle` for the vertical axis and `center` for the horizontal to avoid this "center center" repetition.

#### ZebulanStanphill on 2020-05-12T13:50:04Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423748854
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,67 @@
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Stored as map as i18n __() only accepts strings (not variables)
+export const ALIGNMENT_LABEL = {
+	'top left': __( 'Top Left' ),
+	'top center': __( 'Top Center' ),
+	'top right': __( 'Top Right' ),
+	'center left': __( 'Center Left' ),
+	'center center': __( 'Center Center' ),
```

As far as I'm aware, there's no difference in meaning between "middle" and "center", or at least not relating to axis. We could just have the displayed title be simply "Center".

### Review thread 24 (resolved)

#### ZebulanStanphill on 2020-05-12T13:51:01Z

URL: https://github.com/WordPress/gutenberg/pull/21091#discussion_r423749661
Path: packages/components/src/alignment-matrix-control/utils.js

```diff
@@ -0,0 +1,67 @@
+/**
+ * WordPress dependencies
+ */
+import { __ } from '@wordpress/i18n';
+
+export const GRID = [
+	[ 'top left', 'top center', 'top right' ],
+	[ 'center left', 'center center', 'center right' ],
+	[ 'bottom left', 'bottom center', 'bottom right' ],
+];
+
+// Stored as map as i18n __() only accepts strings (not variables)
+export const ALIGNMENT_LABEL = {
+	'top left': __( 'Top Left' ),
+	'top center': __( 'Top Center' ),
+	'top right': __( 'Top Right' ),
+	'center left': __( 'Center Left' ),
+	'center center': __( 'Center Center' ),
```

```suggestion
	'center center': __( 'Center' ),
```

## Changed files

- docs/manifest.json (MODIFIED, +6/-0)
- packages/block-editor/src/components/block-alignment-matrix-toolbar/index.js (ADDED, +67/-0)
- packages/block-editor/src/components/block-alignment-matrix-toolbar/style.scss (ADDED, +7/-0)
- packages/block-editor/src/components/index.js (MODIFIED, +1/-0)
- packages/block-editor/src/style.scss (MODIFIED, +1/-0)
- packages/block-library/src/cover/block.json (MODIFIED, +3/-0)
- packages/block-library/src/cover/edit.js (MODIFIED, +27/-8)
- packages/block-library/src/cover/save.js (MODIFIED, +8/-1)
- packages/block-library/src/cover/shared.js (MODIFIED, +27/-0)
- packages/block-library/src/cover/style.scss (MODIFIED, +49/-0)
- packages/components/src/alignment-matrix-control/README.md (ADDED, +18/-0)
- packages/components/src/alignment-matrix-control/cell.js (ADDED, +35/-0)
- packages/components/src/alignment-matrix-control/icon.js (ADDED, +59/-0)
- packages/components/src/alignment-matrix-control/index.js (ADDED, +115/-0)
- packages/components/src/alignment-matrix-control/stories/index.js (ADDED, +44/-0)
- packages/components/src/alignment-matrix-control/styles/alignment-matrix-control-icon-styles.js (ADDED, +69/-0)
- packages/components/src/alignment-matrix-control/styles/alignment-matrix-control-styles.js (ADDED, +92/-0)
- packages/components/src/alignment-matrix-control/test/index.js (ADDED, +85/-0)
- packages/components/src/alignment-matrix-control/utils.js (ADDED, +72/-0)
- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/range-control/index.js (MODIFIED, +2/-2)
- packages/components/src/utils/rtl.js (MODIFIED, +6/-6)
- packages/compose/README.md (MODIFIED, +1/-0)
- packages/compose/src/hooks/use-instance-id/index.js (MODIFIED, +6/-2)
