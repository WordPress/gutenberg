---
number: 57770
type: pull_request
state: MERGED
merged_at: 2024-01-13T11:38:59Z
url: https://github.com/WordPress/gutenberg/pull/57770
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #57770: ToggleGroupControl: Improve controlled value detection

- URL: https://github.com/WordPress/gutenberg/pull/57770
- Author: mirka
- Created: 2024-01-11T16:49:04Z
- Updated: 2024-01-13T11:39:28Z
- Comments: 1 of 1
- Review threads: 7 of 7
- Changed files: 4 of 4
- Diff: ../pr-diffs/57770.diff

## Body

Prerequisite for #57562
Related to #56678

## What?

Fixes some bugs in ToggleGroupControl where it didn't correctly update its internal state to reflect a controlled `value` change:

- in cases where the initial `value` was undefined
- in some cases where the `value` was set back to undefined

## Why?

To correctly detect controlled mode `value` changes.

## Testing Instructions

- Check out the commit with the added unit tests, and see that the Reset tests and the test for external updates starting from `undefined` fails. The next commit with the logic fix should make this test pass.
- Smoke test in Storybook and/or editor.

## Issue comments

### ciampo on 2024-01-12T16:08:26Z

URL: https://github.com/WordPress/gutenberg/pull/57770#issuecomment-1889573424

The failing unit tests are flaky and should be fixed by https://github.com/WordPress/gutenberg/pull/57809

## Review threads

### Review thread 1

#### mirka on 2024-01-11T16:54:03Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1449142157
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -219,45 +235,48 @@ describe.each( [
 			expect( jackOption ).not.toBeChecked();
 		} );
 
-		it( 'should update correctly when triggered by external updates', async () => {
-			render(
-				<Component
-					value="rigas"
-					label="Test Toggle Group Control"
-					extraButtonOptions={ [
-						{ name: 'Rigas', value: 'rigas' },
-						{ name: 'Jack', value: 'jack' },
-					] }
-				>
-					{ options }
-				</Component>
-			);
-
-			expect( screen.getByRole( 'radio', { name: 'R' } ) ).toBeChecked();
-			expect(
-				screen.getByRole( 'radio', { name: 'J' } )
-			).not.toBeChecked();
```

Extracted these initial value checks to a more explicit and comprehensive test ("should render the correct initial value when defined") earlier in the file.

### Review thread 2

#### mirka on 2024-01-11T17:00:50Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1449150154
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -219,45 +235,48 @@ describe.each( [
 			expect( jackOption ).not.toBeChecked();
 		} );
 
-		it( 'should update correctly when triggered by external updates', async () => {
-			render(
-				<Component
-					value="rigas"
-					label="Test Toggle Group Control"
-					extraButtonOptions={ [
-						{ name: 'Rigas', value: 'rigas' },
-						{ name: 'Jack', value: 'jack' },
-					] }
-				>
-					{ options }
-				</Component>
-			);
-
-			expect( screen.getByRole( 'radio', { name: 'R' } ) ).toBeChecked();
-			expect(
-				screen.getByRole( 'radio', { name: 'J' } )
-			).not.toBeChecked();
-
-			await click( screen.getByRole( 'button', { name: 'Jack' } ) );
-			expect( screen.getByRole( 'radio', { name: 'J' } ) ).toBeChecked();
-			expect(
-				screen.getByRole( 'radio', { name: 'R' } )
-			).not.toBeChecked();
-
-			await click( screen.getByRole( 'button', { name: 'Rigas' } ) );
-			expect( screen.getByRole( 'radio', { name: 'R' } ) ).toBeChecked();
-			expect(
-				screen.getByRole( 'radio', { name: 'J' } )
-			).not.toBeChecked();
-
-			await click( screen.getByRole( 'button', { name: 'Reset' } ) );
-			expect(
-				screen.getByRole( 'radio', { name: 'R' } )
-			).not.toBeChecked();
-			expect(
-				screen.getByRole( 'radio', { name: 'J' } )
-			).not.toBeChecked();
-		} );
+		describe.each( [
+			[ 'undefined', undefined ],
+			[ 'defined', 'rigas' ],
+		] )(
+			'should update correctly when triggered by external updates',
+			( defaultValueType, defaultValue ) => {
+				it( `when default value is ${ defaultValueType }`, async () => {
```

Previously, this test only checked a case where the default value was defined. Now we also test for when the default value is undefined.

### Review thread 3

#### mirka on 2024-01-11T17:24:45Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1449176355
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -208,56 +224,77 @@ describe.each( [
 			expect( jackOption ).not.toBeChecked();
 			expect( rigasOption ).toBeChecked();
 
-			await press.ArrowRight();
-
-			expect( rigasOption ).not.toBeChecked();
-			expect( jackOption ).toBeChecked();
```

@ciampo Let me know if this second option change was an intentional part of this test. I'd like to remove it to make it a more minimal test for the reset, and because this second maneuver was what was making this test pass with the previous logic.

#### ciampo on 2024-01-12T15:53:25Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1450634251
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -208,56 +224,77 @@ describe.each( [
 			expect( jackOption ).not.toBeChecked();
 			expect( rigasOption ).toBeChecked();
 
-			await press.ArrowRight();
-
-			expect( rigasOption ).not.toBeChecked();
-			expect( jackOption ).toBeChecked();
```

Sounds good to me.

### Review thread 4

#### mirka on 2024-01-11T17:43:42Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1449202497
Path: packages/components/src/toggle-group-control/toggle-group-control/utils.ts

```diff
@@ -21,16 +21,21 @@ type ValueProp = ToggleGroupControlProps[ 'value' ];
 export function useComputeControlledOrUncontrolledValue(
 	valueProp: ValueProp
 ): { value: ValueProp; defaultValue: ValueProp } {
+	const isInitialRender = useRef( true );
 	const prevValueProp = usePrevious( valueProp );
 	const prevIsControlled = useRef( false );
 
+	useEffect( () => {
+		if ( isInitialRender.current ) {
+			isInitialRender.current = false;
+		}
+	}, [] );
+
 	// Assume the component is being used in controlled mode on the first re-render
 	// that has a different `valueProp` from the previous render.
 	const isControlled =
 		prevIsControlled.current ||
-		( prevValueProp !== undefined &&
-			valueProp !== undefined &&
-			prevValueProp !== valueProp );
```

In essence, all we want to do here is check that the current value is different from the previous value.

We can't use `prevValueProp !== undefined` or `valueProp !== undefined` as checks, because it's possible in the first few renders of a controlled usage that either the previous value or the current value is undefined.

#### ciampo on 2024-01-12T16:06:54Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1450650521
Path: packages/components/src/toggle-group-control/toggle-group-control/utils.ts

```diff
@@ -21,16 +21,21 @@ type ValueProp = ToggleGroupControlProps[ 'value' ];
 export function useComputeControlledOrUncontrolledValue(
 	valueProp: ValueProp
 ): { value: ValueProp; defaultValue: ValueProp } {
+	const isInitialRender = useRef( true );
 	const prevValueProp = usePrevious( valueProp );
 	const prevIsControlled = useRef( false );
 
+	useEffect( () => {
+		if ( isInitialRender.current ) {
+			isInitialRender.current = false;
+		}
+	}, [] );
+
 	// Assume the component is being used in controlled mode on the first re-render
 	// that has a different `valueProp` from the previous render.
 	const isControlled =
 		prevIsControlled.current ||
-		( prevValueProp !== undefined &&
-			valueProp !== undefined &&
-			prevValueProp !== valueProp );
```

This also makes sense.

The intention behind the previous check was:

_"the `value` prop was updated from a defined value to another defined value"_

But as you say, I believe that those checks can be removed, since they were kind of redundant: the `prevIsControlled.current` flag makes sure that there is no going back to uncontrolled once controlled, even if `value` suddenly becomes `undefined` again.

### Review thread 5

#### mirka on 2024-01-11T17:56:07Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1449215190
Path: packages/components/src/toggle-group-control/toggle-group-control/utils.ts

```diff
@@ -21,16 +21,21 @@ type ValueProp = ToggleGroupControlProps[ 'value' ];
 export function useComputeControlledOrUncontrolledValue(
 	valueProp: ValueProp
 ): { value: ValueProp; defaultValue: ValueProp } {
+	const isInitialRender = useRef( true );
 	const prevValueProp = usePrevious( valueProp );
 	const prevIsControlled = useRef( false );
 
+	useEffect( () => {
+		if ( isInitialRender.current ) {
+			isInitialRender.current = false;
+		}
+	}, [] );
+
 	// Assume the component is being used in controlled mode on the first re-render
 	// that has a different `valueProp` from the previous render.
 	const isControlled =
 		prevIsControlled.current ||
-		( prevValueProp !== undefined &&
-			valueProp !== undefined &&
-			prevValueProp !== valueProp );
+		( ! isInitialRender.current && prevValueProp !== valueProp );
```

In the initial render, it is still unknown whether we're in controlled or uncontrolled mode. For all intents and purposes, they are the same because we just consider the given `value` prop as the initial value. We just don't want to say that we're `isControlled` yet, since this boolean is effectively irreversible once we set it to true.

Also the `prevValueProp !== valueProp` check is meaningless in the initial render because the previous value will always be undefined.

#### ciampo on 2024-01-12T16:05:26Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1450648352
Path: packages/components/src/toggle-group-control/toggle-group-control/utils.ts

```diff
@@ -21,16 +21,21 @@ type ValueProp = ToggleGroupControlProps[ 'value' ];
 export function useComputeControlledOrUncontrolledValue(
 	valueProp: ValueProp
 ): { value: ValueProp; defaultValue: ValueProp } {
+	const isInitialRender = useRef( true );
 	const prevValueProp = usePrevious( valueProp );
 	const prevIsControlled = useRef( false );
 
+	useEffect( () => {
+		if ( isInitialRender.current ) {
+			isInitialRender.current = false;
+		}
+	}, [] );
+
 	// Assume the component is being used in controlled mode on the first re-render
 	// that has a different `valueProp` from the previous render.
 	const isControlled =
 		prevIsControlled.current ||
-		( prevValueProp !== undefined &&
-			valueProp !== undefined &&
-			prevValueProp !== valueProp );
+		( ! isInitialRender.current && prevValueProp !== valueProp );
```

Makes sense.

### Review thread 6 (resolved)

#### ciampo on 2024-01-12T15:51:54Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1450632138
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -117,6 +117,22 @@ describe.each( [
 			expect( container ).toMatchSnapshot();
 		} );
 	} );
+	it( 'should render the correct initial value when defined', () => {
+		render(
+			<Component value="jack" label="Test Toggle Group Control">
+				{ options }
+			</Component>
+		);
+		expect( screen.getByRole( 'radio', { name: 'R' } ) ).not.toBeChecked();
+		expect( screen.getByRole( 'radio', { name: 'J' } ) ).toBeChecked();
+	} );
+	it( 'should render the correct initial value when undefined', () => {
```

```suggestion
	it( 'should render without a selected option when `value` is `undefined`', () => {
```

### Review thread 7 (resolved)

#### ciampo on 2024-01-12T15:52:18Z

URL: https://github.com/WordPress/gutenberg/pull/57770#discussion_r1450632802
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -117,6 +117,22 @@ describe.each( [
 			expect( container ).toMatchSnapshot();
 		} );
 	} );
+	it( 'should render the correct initial value when defined', () => {
```

```suggestion
	it( 'should render with the correct option initially selected when `value` is defined', () => {
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/toggle-group-control/test/__snapshots__/index.tsx.snap (MODIFIED, +6/-6)
- packages/components/src/toggle-group-control/test/index.tsx (MODIFIED, +73/-36)
- packages/components/src/toggle-group-control/toggle-group-control/utils.ts (MODIFIED, +8/-3)
