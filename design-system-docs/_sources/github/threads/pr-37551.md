---
number: 37551
type: pull_request
state: MERGED
merged_at: 2022-02-01T12:22:11Z
url: https://github.com/WordPress/gutenberg/pull/37551
matched_labels: ["Design System","[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","Design System"]
---

# PR #37551: Update the Spinner design

- URL: https://github.com/WordPress/gutenberg/pull/37551
- Author: jameskoster
- Created: 2021-12-21T12:44:51Z
- Updated: 2024-09-24T19:42:45Z
- Comments: 42 of 42
- Review threads: 21 of 21
- Changed files: 9 of 9
- Diff: ../pr-diffs/37551.diff

## Body

This PR updates the Spinner component based on the designs in #36725. It also adjusts/fixes the position of the Spinner that appears when searching for documents in the add-link interface. Finally, it adds the gray-300 value to color-values.js, which has until now been missing. On this point I did notice that gray-200 has the incorrect value, but as I'm not sure where that is used, I figured we should fix it separately.

Things seem to be working well in the following scenarios:

* Loading a document that has internal dependencies like template parts, or the site logo
* Embedding third party content like a Tweet.
* Searching blocks in the Inserter
* Searching in the link input

<img src="https://user-images.githubusercontent.com/846565/149160764-8e0cbc4c-79eb-423c-bc7c-ec5f622fe993.gif" width="600" />


### Testing instructions

* Perform any action that invokes a spinner and check that it displays and behaves as expected, examples:
  * Edit a template in the site editor that includes template parts
  * Embed third party content like a tweet
  * Search for a block in the Inserter
  * Add a link and search for a post/page
  * Others that I'm missing? :)
* Try different browsers. This is working in Chrome, Firefox, and Safari, but I am unable to test Edge

## Issue comments

### github-actions on 2021-12-21T12:53:35Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-998754864

**Size Change:** +79 B (0%) 

**Total Size:** 1.14 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/block-editor/style-rtl.css` | 14.6 kB | -1 B (0%) |
| `build/block-editor/style.css` | 14.6 kB | -1 B (0%) |
| `build/block-library/editor-rtl.css` | 10 kB | -1 B (0%) |
| `build/components/index.min.js` | 215 kB | +82 B (0%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 960 B |
| `build/admin-manifest/index.min.js` | 1.1 kB |
| `build/annotations/index.min.js` | 2.75 kB |
| `build/api-fetch/index.min.js` | 2.22 kB |
| `build/autop/index.min.js` | 2.12 kB |
| `build/blob/index.min.js` | 459 B |
| `build/block-directory/index.min.js` | 6.28 kB |
| `build/block-directory/style-rtl.css` | 1.01 kB |
| `build/block-directory/style.css` | 1.01 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 378 B |
| `build/block-editor/default-editor-styles.css` | 378 B |
| `build/block-editor/index.min.js` | 141 kB |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 65 B |
| `build/block-library/blocks/archives/style.css` | 65 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 111 B |
| `build/block-library/blocks/audio/style.css` | 111 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 125 B |
| `build/block-library/blocks/audio/theme.css` | 125 B |
| `build/block-library/blocks/block/editor-rtl.css` | 161 B |
| `build/block-library/blocks/block/editor.css` | 161 B |
| `build/block-library/blocks/button/editor-rtl.css` | 470 B |
| `build/block-library/blocks/button/editor.css` | 470 B |
| `build/block-library/blocks/button/style-rtl.css` | 560 B |
| `build/block-library/blocks/button/style.css` | 560 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 292 B |
| `build/block-library/blocks/buttons/editor.css` | 292 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 275 B |
| `build/block-library/blocks/buttons/style.css` | 275 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 207 B |
| `build/block-library/blocks/calendar/style.css` | 207 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 84 B |
| `build/block-library/blocks/categories/editor.css` | 83 B |
| `build/block-library/blocks/categories/style-rtl.css` | 79 B |
| `build/block-library/blocks/categories/style.css` | 79 B |
| `build/block-library/blocks/code/style-rtl.css` | 90 B |
| `build/block-library/blocks/code/style.css` | 90 B |
| `build/block-library/blocks/code/theme-rtl.css` | 131 B |
| `build/block-library/blocks/code/theme.css` | 131 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 406 B |
| `build/block-library/blocks/columns/style.css` | 406 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 127 B |
| `build/block-library/blocks/comment-template/style.css` | 127 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-query-loop/editor-rtl.css` | 95 B |
| `build/block-library/blocks/comments-query-loop/editor.css` | 95 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 546 B |
| `build/block-library/blocks/cover/editor.css` | 547 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.22 kB |
| `build/block-library/blocks/cover/style.css` | 1.22 kB |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 417 B |
| `build/block-library/blocks/embed/style.css` | 417 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 124 B |
| `build/block-library/blocks/embed/theme.css` | 124 B |
| `build/block-library/blocks/file/editor-rtl.css` | 300 B |
| `build/block-library/blocks/file/editor.css` | 300 B |
| `build/block-library/blocks/file/style-rtl.css` | 255 B |
| `build/block-library/blocks/file/style.css` | 255 B |
| `build/block-library/blocks/file/view.min.js` | 322 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.44 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.44 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 965 B |
| `build/block-library/blocks/gallery/editor.css` | 967 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.61 kB |
| `build/block-library/blocks/gallery/style.css` | 1.61 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 159 B |
| `build/block-library/blocks/group/editor.css` | 159 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 114 B |
| `build/block-library/blocks/heading/style.css` | 114 B |
| `build/block-library/blocks/html/editor-rtl.css` | 332 B |
| `build/block-library/blocks/html/editor.css` | 333 B |
| `build/block-library/blocks/image/editor-rtl.css` | 810 B |
| `build/block-library/blocks/image/editor.css` | 809 B |
| `build/block-library/blocks/image/style-rtl.css` | 507 B |
| `build/block-library/blocks/image/style.css` | 511 B |
| `build/block-library/blocks/image/theme-rtl.css` | 124 B |
| `build/block-library/blocks/image/theme.css` | 124 B |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 284 B |
| `build/block-library/blocks/latest-comments/style.css` | 284 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 199 B |
| `build/block-library/blocks/latest-posts/editor.css` | 198 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 447 B |
| `build/block-library/blocks/latest-posts/style.css` | 446 B |
| `build/block-library/blocks/list/style-rtl.css` | 94 B |
| `build/block-library/blocks/list/style.css` | 94 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 493 B |
| `build/block-library/blocks/media-text/style.css` | 490 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 649 B |
| `build/block-library/blocks/navigation-link/editor.css` | 650 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 94 B |
| `build/block-library/blocks/navigation-link/style.css` | 94 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/view.min.js` | 343 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 1.92 kB |
| `build/block-library/blocks/navigation/editor.css` | 1.93 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 1.85 kB |
| `build/block-library/blocks/navigation/style.css` | 1.84 kB |
| `build/block-library/blocks/navigation/view.min.js` | 2.81 kB |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 401 B |
| `build/block-library/blocks/page-list/editor.css` | 402 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 157 B |
| `build/block-library/blocks/paragraph/editor.css` | 157 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 273 B |
| `build/block-library/blocks/paragraph/style.css` | 273 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 446 B |
| `build/block-library/blocks/post-comments-form/style.css` | 446 B |
| `build/block-library/blocks/post-comments/style-rtl.css` | 521 B |
| `build/block-library/blocks/post-comments/style.css` | 521 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 73 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 73 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 69 B |
| `build/block-library/blocks/post-excerpt/style.css` | 69 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 721 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 721 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 153 B |
| `build/block-library/blocks/post-featured-image/style.css` | 153 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 323 B |
| `build/block-library/blocks/post-template/style.css` | 323 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 73 B |
| `build/block-library/blocks/post-terms/style.css` | 73 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 80 B |
| `build/block-library/blocks/post-title/style.css` | 80 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 103 B |
| `build/block-library/blocks/preformatted/style.css` | 103 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 198 B |
| `build/block-library/blocks/pullquote/editor.css` | 198 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 389 B |
| `build/block-library/blocks/pullquote/style.css` | 388 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 167 B |
| `build/block-library/blocks/pullquote/theme.css` | 167 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 234 B |
| `build/block-library/blocks/query-pagination/style.css` | 231 B |
| `build/block-library/blocks/query/editor-rtl.css` | 131 B |
| `build/block-library/blocks/query/editor.css` | 132 B |
| `build/block-library/blocks/quote/style-rtl.css` | 201 B |
| `build/block-library/blocks/quote/style.css` | 201 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 202 B |
| `build/block-library/blocks/rss/editor.css` | 204 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 165 B |
| `build/block-library/blocks/search/editor.css` | 165 B |
| `build/block-library/blocks/search/style-rtl.css` | 397 B |
| `build/block-library/blocks/search/style.css` | 398 B |
| `build/block-library/blocks/search/theme-rtl.css` | 64 B |
| `build/block-library/blocks/search/theme.css` | 64 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 99 B |
| `build/block-library/blocks/separator/editor.css` | 99 B |
| `build/block-library/blocks/separator/style-rtl.css` | 245 B |
| `build/block-library/blocks/separator/style.css` | 245 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 172 B |
| `build/block-library/blocks/separator/theme.css` | 172 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 474 B |
| `build/block-library/blocks/shortcode/editor.css` | 474 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 744 B |
| `build/block-library/blocks/site-logo/editor.css` | 744 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 181 B |
| `build/block-library/blocks/site-logo/style.css` | 181 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 84 B |
| `build/block-library/blocks/site-title/editor.css` | 84 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 177 B |
| `build/block-library/blocks/social-link/editor.css` | 177 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 674 B |
| `build/block-library/blocks/social-links/editor.css` | 673 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.37 kB |
| `build/block-library/blocks/social-links/style.css` | 1.36 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 332 B |
| `build/block-library/blocks/spacer/editor.css` | 332 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 471 B |
| `build/block-library/blocks/table/editor.css` | 472 B |
| `build/block-library/blocks/table/style-rtl.css` | 481 B |
| `build/block-library/blocks/table/style.css` | 481 B |
| `build/block-library/blocks/table/theme-rtl.css` | 188 B |
| `build/block-library/blocks/table/theme.css` | 188 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 214 B |
| `build/block-library/blocks/tag-cloud/style.css` | 215 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 560 B |
| `build/block-library/blocks/template-part/editor.css` | 559 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 87 B |
| `build/block-library/blocks/verse/style.css` | 87 B |
| `build/block-library/blocks/video/editor-rtl.css` | 571 B |
| `build/block-library/blocks/video/editor.css` | 572 B |
| `build/block-library/blocks/video/style-rtl.css` | 173 B |
| `build/block-library/blocks/video/style.css` | 173 B |
| `build/block-library/blocks/video/theme-rtl.css` | 124 B |
| `build/block-library/blocks/video/theme.css` | 124 B |
| `build/block-library/common-rtl.css` | 908 B |
| `build/block-library/common.css` | 905 B |
| `build/block-library/editor.css` | 10 kB |
| `build/block-library/index.min.js` | 166 kB |
| `build/block-library/reset-rtl.css` | 474 B |
| `build/block-library/reset.css` | 474 B |
| `build/block-library/style-rtl.css` | 10.8 kB |
| `build/block-library/style.css` | 10.8 kB |
| `build/block-library/theme-rtl.css` | 672 B |
| `build/block-library/theme.css` | 676 B |
| `build/block-serialization-default-parser/index.min.js` | 1.09 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.79 kB |
| `build/blocks/index.min.js` | 46.4 kB |
| `build/components/style-rtl.css` | 15.5 kB |
| `build/components/style.css` | 15.5 kB |
| `build/compose/index.min.js` | 11.2 kB |
| `build/core-data/index.min.js` | 13.4 kB |
| `build/customize-widgets/index.min.js` | 11.4 kB |
| `build/customize-widgets/style-rtl.css` | 1.5 kB |
| `build/customize-widgets/style.css` | 1.49 kB |
| `build/data-controls/index.min.js` | 631 B |
| `build/data/index.min.js` | 7.49 kB |
| `build/date/index.min.js` | 31.9 kB |
| `build/deprecated/index.min.js` | 485 B |
| `build/dom-ready/index.min.js` | 304 B |
| `build/dom/index.min.js` | 4.5 kB |
| `build/edit-navigation/index.min.js` | 16.2 kB |
| `build/edit-navigation/style-rtl.css` | 3.76 kB |
| `build/edit-navigation/style.css` | 3.76 kB |
| `build/edit-post/classic-rtl.css` | 546 B |
| `build/edit-post/classic.css` | 547 B |
| `build/edit-post/index.min.js` | 29.6 kB |
| `build/edit-post/style-rtl.css` | 7.15 kB |
| `build/edit-post/style.css` | 7.14 kB |
| `build/edit-site/index.min.js` | 41.5 kB |
| `build/edit-site/style-rtl.css` | 7.22 kB |
| `build/edit-site/style.css` | 7.21 kB |
| `build/edit-widgets/index.min.js` | 16.6 kB |
| `build/edit-widgets/style-rtl.css` | 4.17 kB |
| `build/edit-widgets/style.css` | 4.17 kB |
| `build/editor/index.min.js` | 38.4 kB |
| `build/editor/style-rtl.css` | 3.71 kB |
| `build/editor/style.css` | 3.71 kB |
| `build/element/index.min.js` | 3.29 kB |
| `build/escape-html/index.min.js` | 517 B |
| `build/format-library/index.min.js` | 6.58 kB |
| `build/format-library/style-rtl.css` | 571 B |
| `build/format-library/style.css` | 571 B |
| `build/hooks/index.min.js` | 1.63 kB |
| `build/html-entities/index.min.js` | 424 B |
| `build/i18n/index.min.js` | 3.75 kB |
| `build/is-shallow-equal/index.min.js` | 501 B |
| `build/keyboard-shortcuts/index.min.js` | 1.8 kB |
| `build/keycodes/index.min.js` | 1.39 kB |
| `build/list-reusable-blocks/index.min.js` | 1.72 kB |
| `build/list-reusable-blocks/style-rtl.css` | 838 B |
| `build/list-reusable-blocks/style.css` | 838 B |
| `build/media-utils/index.min.js` | 2.92 kB |
| `build/notices/index.min.js` | 925 B |
| `build/nux/index.min.js` | 2.08 kB |
| `build/nux/style-rtl.css` | 747 B |
| `build/nux/style.css` | 743 B |
| `build/plugins/index.min.js` | 1.84 kB |
| `build/primitives/index.min.js` | 917 B |
| `build/priority-queue/index.min.js` | 582 B |
| `build/react-i18n/index.min.js` | 671 B |
| `build/react-refresh-entry/index.min.js` | 8.44 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.65 kB |
| `build/reusable-blocks/index.min.js` | 2.22 kB |
| `build/reusable-blocks/style-rtl.css` | 256 B |
| `build/reusable-blocks/style.css` | 256 B |
| `build/rich-text/index.min.js` | 11 kB |
| `build/server-side-render/index.min.js` | 1.58 kB |
| `build/shortcode/index.min.js` | 1.49 kB |
| `build/token-list/index.min.js` | 639 B |
| `build/url/index.min.js` | 1.9 kB |
| `build/viewport/index.min.js` | 1.05 kB |
| `build/warning/index.min.js` | 248 B |
| `build/widgets/index.min.js` | 7.15 kB |
| `build/widgets/style-rtl.css` | 1.16 kB |
| `build/widgets/style.css` | 1.16 kB |
| `build/wordcount/index.min.js` | 1.04 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### jasmussen on 2022-01-10T08:49:24Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1008655241

This is very nice! Thanks for working on it.

I also really love these designs:
<img width="613" alt="Screenshot 2022-01-10 at 09 48 00" src="https://user-images.githubusercontent.com/1204802/148738922-3c3f0068-e8db-495a-9e31-0cc7db7dc774.png">

Is there any way we can get closer to these in terms of design? Stroke-width, white instead of spot color in dark contexts, a gray base circle track?

### jameskoster on 2022-01-10T16:19:56Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1009044257

The only way I can think to include the track would be to use an svg rather than css alone, otherwise the mask is interfering too much. I've pushed an update to try this. 


https://user-images.githubusercontent.com/846565/148800185-1bc87154-5c99-447e-a1cd-811793ad959e.mp4



I'm not sure what the stroke width is in those screenshots, but I've set them to 1.5px for now. Easily changed.

I can't think of a way to make the colors adapt based on the background as the Spinner isn't aware of that. This might be something better left as an option on the Spinner itself.

### jasmussen on 2022-01-11T12:22:08Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1009913155

Another reason to maybe reconsider the expansion and contraction of the indicator is that it isn't relative to the size of the spinner. While I don't know exactly how often we'll show a 128x128 spinner, a few things broke apart when I tried it:

![spin 128](https://user-images.githubusercontent.com/1204802/148941544-af79b018-b876-40b0-bfa0-c10bd2373410.gif)

It's actually still expanding and contracting, it's just not very visible because the dashoffset change is insignificant when large. I wonder if you could use `em`s there? Probably not? 🤔 

Another one is the fact that the gray track is outset compared to the blue indicator. I don't know why that happens. **Edit: Oh I know, the radius of the circle is 8.25, it should be 9 to match with half of the 18x18 svg viewbox. Combined with overflow: visible; on the SVG, it works.**

### jasmussen on 2022-01-11T12:43:59Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1009929106

Using an SVG seems appropriate enough for this, but I'm starting to think that using `dasharray` might not be. Mainly because I can't figure out how to make a dasharray that behaves in a relatively scaling way. Shown here a dasharray that approaches (but isn't precisely) a quarter circle as from the initial mockup, but only when the spinner is really big:
<img width="221" alt="Screenshot 2022-01-11 at 13 41 35" src="https://user-images.githubusercontent.com/1204802/148944664-54601ec7-33f6-40e5-9e9e-5e3d8c5d2569.png">

We might need a static SVG that just draws that quarter-circle precisely, and not use the dasharray trick 🤔 

Edit: Feel free to tinker in this codepen: https://codepen.io/joen/pen/RwLeByP?editors=1100

### jameskoster on 2022-01-11T15:06:17Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1010053594

> because I can't figure out how to make a dasharray that behaves in a relatively scaling way

Perhaps we can add some logic so that the dasharray value(s) are based on the spinner size?

I appreciate it's subjective/intangible, but I quite like the expansion/contraction effect. I can't put my finger on why but it makes it _feel_ like something is happening compared with the linear spinner. Maybe it's the pulsing.

Then again, if we eliminate that effect it would make the original design much easier to accomplish without the need for an svg. Maybe we can add a pulsing effect another way 🤔 I will also have a play in codepen :D

### jameskoster on 2022-01-11T15:24:18Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1010071911

I added a "Pulsey no svg" version to [this codepen](https://cdpn.io/jameskoster/debug/OJxBrGE).

Gif:

![spinner](https://user-images.githubusercontent.com/846565/148970557-e99cbf5f-8a7b-49f5-b908-cb4f605858ac.gif)

The main drawback to this approach is that it uses borders for the indicator line, which cannot be set to a 1.5px width.

### jasmussen on 2022-01-11T15:40:19Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1010086866

Impressive work going on in these codepens, I love it. I'm also starting to see your point about the contraction/expansions being a bit more dynamic. But just looking at them side by side, I'm personally still most attracted to the complete calmness of the linearly rotating circle, I feel like it might just work the best in noisy contexts.

### jameskoster on 2022-01-11T16:15:35Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1010123612

Reverted to html + css only, and simplified the animation. Here's the result:

![spinner](https://user-images.githubusercontent.com/846565/148979668-6b8bb4f2-509a-46ca-8d83-f392707e1129.gif)

The last question seems to be what to do about the discrepancy between track/indicator width.

### jasmussen on 2022-01-12T09:08:09Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1010814125

Clever trick to use border-top to grab exactly one quarter of the circle! I suppose box-shadow wouldn't work for that, as it would be affected by the contours of the circle shape. I do think it's important we get the right stroke width, though — maybe the SVG was fine after all? That might also let us keep that neat rounded edgecap.

Alternatively we could create another full circle and mask a quadrant of it?

### jameskoster on 2022-01-12T12:43:21Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1011007595

To get the strokes to be 1.5px I think we'll have to return to the SVG approach. I just pushed a change that does this, and includes logic so that the dasharray will scale correctly. You can change the `spinnerSize` value in config-values.js to test this. Here's an example at 180px:

<img src="https://user-images.githubusercontent.com/846565/149142250-230f238d-6501-452e-9876-e452c5b1db42.gif" width="350" />

### jasmussen on 2022-01-12T12:44:43Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1011008762

I mean, I really dig that one 👌 

Is this one ready for final review?

### jameskoster on 2022-01-12T14:32:31Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1011105374

Yup, I think so!

### jameskoster on 2022-01-20T10:23:43Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1017326054

Thanks @ciampo.

> would you also be able to add an entry to the CHANGELOG ?

I thought this got updated automatically? 🤔

### ciampo on 2022-01-20T10:39:15Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1017342391

> I thought this got updated automatically? 🤔

It is a manual process, at least for the `@wordpress/components` package (can't guarantee for other packages/repos)

### jameskoster on 2022-01-20T13:43:21Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1017517188

One other thing we'll need to do is update the screenshot in https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/spinner/README.md. Looks like it will need to be uploaded to https://wordpress.org/gutenberg/files/2019/07/spinner.png, I'm not sure how to do that though.

### ciampo on 2022-01-20T19:15:23Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1017838747

> One other thing we'll need to do is update the screenshot in https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/spinner/README.md. Looks like it will need to be uploaded to https://wordpress.org/gutenberg/files/2019/07/spinner.png, I'm not sure how to do that though.

~You can add a new image to this folder (e.g. `packages/components/src/spinner/screenshot.png`) and reference it directly in the README :)~

Edit: Actually, another alternative would be just to remove the preview image — we can assume that folks can visit the [online Storybook showcase](https://wordpress.github.io/gutenberg/) to see the component in action.

### jasmussen on 2022-01-21T07:12:04Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1018242266

Yep, storybook sounds great. It's real, it and it will automatically be up to date, so we won't end up with drift as visuals get tweaked 👌

### jameskoster on 2022-01-24T09:52:30Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1019912509

So close! The only issue outstanding seems to be the link search. For some reason the js styles aren't being applied in that instance:

https://user-images.githubusercontent.com/846565/150759187-06a66eda-5a7b-4de6-96f4-fe18aceafdcc.mp4

Any ideas?

### ciampo on 2022-01-24T10:59:34Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1019971898

I tested on my local machine, using a blank new `wp-env` install, and couldn't reproduce the issue — everything seems fine (apart from a slight vertical misalignment of the spinner, probably due to the new `16px` height):

<img width="1048" alt="Screenshot 2022-01-24 at 11 55 37" src="https://user-images.githubusercontent.com/1083581/150770536-18c9015a-d83f-43ac-bb4a-52b723f730ee.png">

I also tried on [`gutengerg.run`](http://gutenberg.run/37551) and everything looked fine.

Could you share a bit more info on how to reproduce the issue?

---

Edit: ah, I see you're in the template editor, while I was in the post editor. Maybe that makes a difference?

### ciampo on 2022-01-24T11:23:15Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1019995128

Ok, this is weird.

Same WordPress instance.

In the template editor, it looks like the `Spinner` styles (defined through emotion) are not loaded, despite the Emotion-generate classnames being there:

![image](https://user-images.githubusercontent.com/1083581/150773751-301ec752-ec01-4ec3-8cf0-396de696740a.png)

In the post editor, everything looks fine:

![image](https://user-images.githubusercontent.com/1083581/150774113-20b31fcc-9c53-4d74-8f86-03ebbcc00ce7.png)

So, from a first look it seems like there's some issue in loading Emotion's styles in the template editor — @youknowriad and @sarayourfriend , any idea of why that may be happening? Could this be related to [this other conversation](https://github.com/WordPress/gutenberg/pull/35619#discussion_r783270274)?

### youknowriad on 2022-01-24T16:53:51Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1020312397

> So, from a first look it seems like there's some issue in loading Emotion's styles in the template editor — @youknowriad and @sarayourfriend , any idea of why that may be happening? Could this be related to this other conversation?

Initially, CSS in JS styles didn't load in iframes by default, we tried solving that (and we did) using the `StyleProvider` in iframe and portals... but lately, it seems that the issues are happening again here and in the related conversation. Unfortunately, this is not really something we know exactly how to fix, it's something in the internals of emotion as our use-case is a bit weird.

### sarayourfriend on 2022-01-24T18:04:25Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1020387292

Yes that does look iframe related.

It seems like iframe styles are broken completely in the editor somehow?

I have a strong suspicion it probably has to do with something being done in the `IFrame` component, as the other isolated parts of the chain for making iframe styles work are unchanged and continue to work in isolation (see the Storybook for `useCx` which renders styles inside an iframe no problem).

I suppose not implementing components in Emotion (or potentially any other CSS-in-JS solution? I believe almost all of them would have this problem, at least in dev mode if not in production as well) is a solution to this, but I believe a lot of components have been implemented in it by this point, so it seems untenable to switch back to sass for all those components.

Solving the iframe style issue should be high priority to avoid having to reimplement things.

### jameskoster on 2022-01-25T09:35:22Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1020987160

How do y'all think we should proceed here? It would be a shame to go back to sass at this point, but I am keen to get this merged.

### sarayourfriend on 2022-01-25T13:20:30Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021176451

What's odd is that it appears that the spinner works when the site editor is loading, but on subsequent appearances of the component it loses the styles.

I'm prioritizing this today to try to find a fix.

### ciampo on 2022-01-25T13:24:21Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021179548

> How do y'all think we should proceed here? It would be a shame to go back to sass at this point, but I am keen to get this merged.

I suggest we try to come up with a fix, as it is quite an important issue for the editor and components overall. In case a fix isn't possible or takes too long, we can consider alternative approaches

Edit: just realised Sara also answered, looks like we're aligned :)

### sarayourfriend on 2022-01-25T13:54:32Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021205455

The issue in this particular case appears to be a problem with how we're using slot/fill.

Take this example:

```
<Iframe>
  <Fill name="popover>
    <Spinner />
  </Fill>
</Iframe>
<Slot name="popover" />
```

The `Spinner` component will receive the Emotion cache context of the `Iframe` component but then be dislocated (in the DOM) outside of the iframe element. This causes the `Spinner` component to render with the incorrect emotion cache and therefore when it renders the CSS for the generated styles are placed in the wrong head element (that of the iframe).

I honestly have zero ideas currently for how to resolve this.

cc @youknowriad

### youknowriad on 2022-01-25T14:04:18Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021214259

Can we try to maybe add the right `StyleProvider` in `Fill` component or `Slot` (not sure exactly)?

### sarayourfriend on 2022-01-25T14:33:56Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021245582

Well, I'm stumped again because I tried to reproduce this in Storybook but it seems to just work perfectly:

```js
export const slotFill = () => {
	const redText = css`
		color: red;
	`;
	const blueText = css`
		color: blue;
	`;
	const orangeText = css`
		color: orange;
	`;
	return (
		<SlotFillProvider>
			<IFrame>
				<IFrame>
					<Example args={ [ redText ] }>
						This text is inside an iframe and is red!
					</Example>
					<Fill name="test-slot">
						<Example args={ [ blueText ] }>
							This text is also inside the iframe, but is
							relocated by a slot/fill
						</Example>
					</Fill>
					<Fill name="outside-frame">
						<Example args={ [ orangeText ] }>
							This text is also inside the iframe, but is
							relocated by a slot/fill
						</Example>
					</Fill>
				</IFrame>
				<Slot name="test-slot" />
			</IFrame>
			<Slot name="outside-frame" />
		</SlotFillProvider>
	);
};
```

@youknowriad I tried both adding it to Slot and to Fill and neither had any effect.

I'm going to take a look at everywhere `createPortal` is used and see if there's anything else outstanding.

### sarayourfriend on 2022-01-25T14:54:27Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021266125

I think I figured it out! No idea how to solve this though 😅 

I believe the problem comes from this pattern:

```js
<Iframe>
  <Spinner />
  <StyleProvider document={ document }>
    <Spinner />
  </StyleProvider>
</Iframe>
```

The first spinner will have the expected styles, but the second, inside the nested StyleProvider, will not.

This is becaue the nested StyleProvider is using a global `document` reference, which will reference the root document of the page rather than the iframe's document.

I was able to reproduce this with slot/fill as well:

```js
<SlotFillProvider>
	<StyleProvider document={ document }>
		<IFrame>
			<IFrame>
				<Example args={ [ redText ] }>
					This text is inside an iframe and is red!
				</Example>
				<Fill name="test-slot">
					<Example args={ [ blueText ] }>
						This text is also inside the iframe, but is
						relocated by a slot/fill
					</Example>
				</Fill>
				<Fill name="outside-frame">
					<Example args={ [ orangeText ] }>
						This text is also inside the iframe, but is
						relocated by a slot/fill
					</Example>
				</Fill>
			</IFrame>
			<StyleProvider document={ document }>
				<Slot name="test-slot" />
			</StyleProvider>
		</IFrame>
		<Slot name="outside-frame" />
	</StyleProvider>
</SlotFillProvider>
```

The text in `test-slot` ends up unstyled.

I have no idea how to fix this though.

### sarayourfriend on 2022-01-25T14:57:00Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021269246

We have a lot of `StyleProvider` instances that reference the global document and these could be arbitrarily nested inside an `Iframe` component but then transported using slot/fill.

I believe there is no issue with slot/fill, it's just a problem with these StyleProviders getting called with the root document inside the iframe.

### ciampo on 2022-01-25T16:00:29Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021340028

> I believe there is no issue with slot/fill, it's just a problem with these StyleProviders getting called with the root document inside the iframe

Has this always been an issue (that we only noticed recently), or could it be cause by some recent changes, either on our side or Emotion's side?

### sarayourfriend on 2022-01-25T17:43:50Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021447478

I'm reluctant to say it's an issue with Emotion as it appears to work entirely as expected in Storybook otherwise.

I haven't been able to solve the problem of the spinner in the site editor. This is what I updated `style-provider/index.js` to, with some debugging code left in to make it easy for anyone coming after me:

```js
//@ts-nocheck

/**
 * WordPress dependencies
 */
import { createContext, useContext } from '@wordpress/element';

/**
 * External dependencies
 */
import { CacheProvider } from '@emotion/react';
import createCache from '@emotion/cache';
import memoize from 'memize';
import * as uuid from 'uuid';

const uuidCache = new Set();

const memoizedCreateCacheWithContainer = memoize( ( container ) => {
	// emotion only accepts alphabetical and hyphenated keys so we just strip the numbers from the UUID. It _should_ be fine.
	let key = uuid.v4().replace( /[0-9]/g, '' );
	while ( uuidCache.has( key ) ) {
		key = uuid.v4().replace( /[0-9]/g, '' );
	}
	uuidCache.add( key );
	container.dataset.emotionCacheKey = key;
	return createCache( { container, key } );
} );

const heads = [];
window._heads = heads;

/** @type {import('react').Context<HTMLHeadElement>} */
const DocumentHeadContext = createContext( null );

export default function StyleProvider( { children, document } ) {
	const contextHead = useContext( DocumentHeadContext );

	const head = document?.head ?? contextHead ?? null;

	if ( ! head ) return null;

	if ( ! heads.includes( head ) ) heads.push( head );

	if ( head === contextHead ) {
		// If using same head as in context no need to insert a new cache provider
		return children;
	}

	const cache = memoizedCreateCacheWithContainer( head );

	const content = <CacheProvider value={ cache }>{ children }</CacheProvider>;

	return (
		<DocumentHeadContext.Provider value={ head }>
			{ content }
		</DocumentHeadContext.Provider>
	);
}
```

I'm pretty stumped on this, but I might be doing something totally wacky (I haven't thought in "react" in a few months now 🙂) so please review and let me know if there's anything obviously wrong here!

Kudos to @ciampo for the idea to try using React context to avoid using the global document head.

### youknowriad on 2022-01-26T08:33:49Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1021975962

@ciampo @sarayourfriend Thanks for the debugging. I pushed a small commit which I think fixes the issue but let me know if there's anything missing.

The idea is that when you use a `Fill`, the children will be rendered in the document of the `Slot` meaning we need to wrap in another `StyleProvider` otherwise we'll be using the context of the parent of the `Fill` which can be any document/iframe and not necessarily the right one.

### ciampo on 2022-01-26T10:03:56Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1022045903

> Kudos to @ciampo for the idea to try using React context to avoid using the global document head.

It was definitely more of a joint effort with most of the work coming from you 🙌 

> Thanks for the debugging. I pushed a small commit which I think fixes the issue but let me know if there's anything missing.

Thank you @youknowriad! I gave it a quick look at it seems to fix the issue with the `Spinner` in the template editor:


<img width="1792" alt="Screenshot 2022-01-26 at 11 00 35" src="https://user-images.githubusercontent.com/1083581/151143001-8b3bdeac-858c-4f22-8cfa-2725ed166979.png">

I'm going to spend some more time looking into this solution, maybe open a separate PR for it so that the fix doesn't get mixed up / lost with the `Spinner` restyle.

### ciampo on 2022-01-26T11:04:08Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1022092954

Update: I opened a separate PR (#38237) with @youknowriad 's fix where we can discuss the issue separately from this PR (focused on the `Spinner`).

If folks agrees, we can remove Riad's fix from this PR. Once the fix in #38237 gets merged, we'll be able to rebase this PR and merge it too.

### jameskoster on 2022-01-26T12:23:25Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1022150105

> If folks agrees, we can remove Riad's fix from this PR. Once the fix in #38237 gets merged, we'll be able to rebase this PR and merge it too.

Seems sensible to me :)

### ciampo on 2022-01-28T13:27:06Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1024225552

Alright, #38237 has been merged.

@jameskoster , would you mind rebasing on top of `trunk` to include those changes and solve conflicts? (Feel free to delete Riad's fix on this PR, as it was already included with the merge of #38237).

We can have a final look at the PR and hopefully everything will be in a good shape 🤞

### pablohoneyhoney on 2022-01-31T20:21:58Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1026174125

Round of applause!

It really needed an update 😄

### jameskoster on 2022-02-01T10:02:18Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1026664857

Aside from the margin issue above, everything seems to be working well. Thanks for all the help :)

### getdave on 2022-02-18T11:30:57Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1044374465

@jameskoster First up - great work here. This is a big improvement! 🤝 👏 .

I've been looking into improving the communication around loading states in a block I'm working on. I'm curious did you do any research into whether using `speak` from `@wordpress/a11y` would be helpful as part of the component?

I noticed you added `aria-busy` so I'm interested to understand more about this.

Also did anyone from the Core Accessibility team review this PR?

### jameskoster on 2022-02-18T11:41:13Z

URL: https://github.com/WordPress/gutenberg/pull/37551#issuecomment-1044384762

> I noticed you added aria-busy so I'm interested to understand more about this.

I think you are referring to a [different Spinner](https://github.com/WordPress/gutenberg/tree/trunk/packages/components/src/ui/spinner) :D

In terms of this PR there was [a little discussion](https://github.com/WordPress/gutenberg/pull/37551#discussion_r787979904) around a11y, but the primary focus was to update the visuals.

## Review threads

### Review thread 1

#### jasmussen on 2022-01-11T12:07:00Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r782083181
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,44 @@
+.components-spinner--svg {
```

The SVG is working fairly well here! However just as far as classnames go, should we call it "track" instead? Also, I think it should be __ instad of --. I.e. `.components-spinner__track` for example.

### Review thread 2

#### jasmussen on 2022-01-11T12:08:57Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r782084534
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,44 @@
+.components-spinner--svg {
+	animation: 2s linear infinite both rotatesvg;
+	position: absolute;
+	top: 0;
+	left: 0;
+}
+
+@keyframes rotatesvg {
```

Same comment with above, denoting the element as "track" might be better.

It seems correct to me to put this keyframe statement right in this components file where it will be used, especially since it's unique to this component and not reused. But naming-wise, we could use some BEM niceness here as well, for example `components-spinner__spin-track` perhaps?

### Review thread 3

#### jasmussen on 2022-01-11T12:10:17Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r782085402
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,44 @@
+.components-spinner--svg {
+	animation: 2s linear infinite both rotatesvg;
+	position: absolute;
+	top: 0;
+	left: 0;
+}
+
+@keyframes rotatesvg {
+	0% {
+		transform: rotateZ(0deg);
+	}
+	100% {
+		transform: rotateZ(360deg);
+	}
+}
+
+.components-spinner--circle {
```

If I understand correctly, this is the blue part of the spinner that in this PR contracts and expands, so it's more of a quarter-circle. I realize this is nitpick territory, but I think it'd help readability. How about something like `.components-spinner__indicator`? Note the __ instead of --.

### Review thread 4

#### jasmussen on 2022-01-11T12:10:48Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r782085727
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,44 @@
+.components-spinner--svg {
+	animation: 2s linear infinite both rotatesvg;
+	position: absolute;
+	top: 0;
+	left: 0;
+}
+
+@keyframes rotatesvg {
+	0% {
+		transform: rotateZ(0deg);
+	}
+	100% {
+		transform: rotateZ(360deg);
+	}
+}
+
+.components-spinner--circle {
+	animation: 1.4s ease-in-out infinite both rotatecircle;
+	fill: transparent;
+	stroke: currentColor;
+	stroke-dasharray: 185;
+	stroke-linecap: round;
+	stroke-width: 1.5px;
+	transform-origin: 50% 50%;
+}
+
+@keyframes rotatecircle {
```

BEM all the way. How about `components-spinner__spin-indicator`?

### Review thread 5 (resolved)

#### jasmussen on 2022-01-13T08:21:07Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r783726056
Path: packages/block-editor/src/components/link-control/style.scss

```diff
@@ -482,16 +482,11 @@ $preview-image-height: 140px;
 		 * Position spinner to the left of the actions.
 		 *
 		 * Compensate for:
-		 *  - Input margin ($grid-unit-20)
-		 *  - Border (1px)
-		 *  - Vertically, for the difference in height between the input (40px)
-		 *    and the spinner.
-		 *  - Horizontally, adjust for the width occupied by the icon buttons,
-		 *    then artificially create spacing that mimics as if the spinner
-		 *    were center-padded to the same width as an icon button.
+		 *  - Input padding top ($grid-unit-15)
+		 *  - Input padding right ($button-size)
 		 */
-		top: $grid-unit-20 + 1px + ( ( 40px - $spinner-size ) * 0.5 );
-		right: $grid-unit-20 + 1px + ( $button-size * $block-editor-link-control-number-of-actions ) + ( ( $button-size - $spinner-size ) * 0.5 );
+		top: $grid-unit-15;
+		right: $button-size;
```

Glorious simplification, very nice.

### Review thread 6 (resolved)

#### jasmussen on 2022-01-13T08:22:45Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r783727200
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,52 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
```

Should we add a comment describing why there are two shadows stacked? Something like "The SVG can only have a centered stroke, and a stacked box-shadow accomplishes a matching 1.5px border.

### Review thread 7 (resolved)

#### jasmussen on 2022-01-13T08:25:41Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r783729286
Path: packages/components/src/utils/colors-values.js

```diff
@@ -31,6 +31,7 @@ export const G2 = {
 		700: '#757575', // Meets 4.6:1 text contrast against white.
 		600: '#949494', // Meets 3:1 UI or large text contrast against white.
 		400: '#ccc',
+		300: '#ddd',
 		200: '#ddd', // Used for most borders.
```

Hold up, now 200 and 300 are the same 🤔 

I guess the error is that 200 should be `#e0e0e0`:

<img width="562" alt="Screenshot 2022-01-13 at 09 24 29" src="https://user-images.githubusercontent.com/1204802/149293065-ed90b182-5116-4f23-b10b-ab35477c0901.png">

Since it looks like 200 was already erroneous before this PR, it seems fine to not fix that in this one, but this is an error we should correct.

#### jameskoster on 2022-01-13T10:34:47Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r783831740
Path: packages/components/src/utils/colors-values.js

```diff
@@ -31,6 +31,7 @@ export const G2 = {
 		700: '#757575', // Meets 4.6:1 text contrast against white.
 		600: '#949494', // Meets 3:1 UI or large text contrast against white.
 		400: '#ccc',
+		300: '#ddd',
 		200: '#ddd', // Used for most borders.
```

Yeah I noted that on the updated OP and agree, we should fix it separately.

#### ciampo on 2022-01-20T10:27:52Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788623690
Path: packages/components/src/utils/colors-values.js

```diff
@@ -31,6 +31,7 @@ export const G2 = {
 		700: '#757575', // Meets 4.6:1 text contrast against white.
 		600: '#949494', // Meets 3:1 UI or large text contrast against white.
 		400: '#ccc',
+		300: '#ddd',
 		200: '#ddd', // Used for most borders.
```

Opened https://github.com/WordPress/gutenberg/pull/38109 to address this separately

#### ciampo on 2022-01-20T11:30:37Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788674226
Path: packages/components/src/utils/colors-values.js

```diff
@@ -31,6 +31,7 @@ export const G2 = {
 		700: '#757575', // Meets 4.6:1 text contrast against white.
 		600: '#949494', // Meets 3:1 UI or large text contrast against white.
 		400: '#ccc',
+		300: '#ddd',
 		200: '#ddd', // Used for most borders.
```

#38109 has been merged, this PR will likely require a rebase to solve conflicts

### Review thread 8 (resolved)

#### jasmussen on 2022-01-13T08:26:05Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r783729561
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: '18',
```

Just for my own curiosity, why is it unitless?

#### jameskoster on 2022-01-13T10:35:40Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r783832420
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: '18',
```

In order for the spinner to scale correctly this value is used on attributes like the svg viewbox, and the circle radius. I think the radius would accept a unit, but unfortunately the viewbox does not.

#### ciampo on 2022-01-19T17:42:34Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787993938
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: '18',
```

Since it's unitless, should be change it to be a number instead of a string?

#### ciampo on 2022-01-20T10:33:07Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788629220
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: '18',
```

Marking as resolved, in favour of https://github.com/WordPress/gutenberg/pull/37551#discussion_r788614818

### Review thread 9 (resolved)

#### sarayourfriend on 2022-01-18T20:54:37Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787141770
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

Sorry to pop in 🙂 Why does this component use a mix of Scss and Emotion? Is it possible for it to just use Emotion? (I think that's the way things are headed)

Also, if this component replaces the existing spinner, we could probably go ahead and remove the spinner in `components/src/ui/spinner` as well to avoid any confusion for future contributors to the components package.

cc @ciampo

#### jameskoster on 2022-01-19T09:33:05Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787539467
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

> Sorry to pop in 🙂

Welcome! :D I'm using emotion where I need to inject dynamic values (like the spinner size) in to the css, then scss everywhere else. I figured plain scss is more contributor friendly. 

It certainly could be all-emotion if that's preferable.

> Also, if this component replaces the existing spinner, we could probably go ahead and remove the spinner in components/src/ui/spinner as well to avoid any confusion for future contributors to the components package.

Do you think it would be ok to do that in a follow-up? I'm not entirely sure how/where it is used.

#### sarayourfriend on 2022-01-19T11:24:17Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787650184
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

> It certainly could be all-emotion if that's preferable.

I think @ciampo should be able to answer that. I'm not familiar with the intentions of the components package since I stopped working on it a few months ago. If he doesn't get back to you in a timely manner I'd say a fast-follow PR to convert it to all Emotion would probably be fine.

> Do you think it would be ok to do that in a follow-up? I'm not entirely sure how/where it is used.

Sure thing 👍 FWIW I think it is unused so will hopefully be a straightforward PR (but don't quote me on that! 😅)

#### jameskoster on 2022-01-19T13:23:50Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787739506
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

I think Woo may have used it, so to be safe I'll do that separately :)

#### sarayourfriend on 2022-01-19T13:30:15Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787745120
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

It doesn't look like the `ui/spinner` is exported from the components package, so hopefully Woo isn't doing something to force it to be available 😱

#### jameskoster on 2022-01-19T14:02:07Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787774129
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

Oh, I just realised that ui/spinner was the original intended replacement for the Spinner that I worked on in this PR (IE that one that is actually published and in use). In which case it would seem very safe to remove ui/spinner. I'll still probably do that separately, though :D

#### ciampo on 2022-01-19T17:01:38Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787958692
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

Thank you @sarayourfriend for the ping — I haven't taken a deeper look yet, but we would definitely like to [use Emotion to style our components](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#styling).

@jameskoster , would you be able to move back to CSS-in-JS styles (Emotion) in this PR?

The removal of `ui/spinner` can definitely happen in a separate PR

#### jameskoster on 2022-01-19T17:36:40Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787989133
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

> would you be able to move back to CSS-in-JS styles (Emotion) in this PR?

Yup np, I just pushed an update.

#### ciampo on 2022-01-20T11:40:43Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788681325
Path: packages/components/src/spinner/style.scss

```diff
@@ -0,0 +1,32 @@
+.components-spinner {
```

> Yup np, I just pushed an update.

Thank you! One last thing on this end — would you mind moving the code for the styles (i.e. `spinAnimation`, `StyledSpinner` and `SpinnerIndicator`) to a separate file? `packages/components/src/spinner/styles.js` would be perfect

### Review thread 10 (resolved)

#### ciampo on 2022-01-19T17:06:50Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787963354
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
```

After converting to Emotion, let's remove this hardcoded classname

### Review thread 11 (resolved)

#### ciampo on 2022-01-19T17:07:17Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787963712
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
+			>
+				<Circle
+					className="components-spinner__indicator"
+					cx="50%"
+					cy="50%"
+					r={ radius }
+					stroke-dasharray={ strokeDasharray }
```

I believe in React this prop should be specific camel-case, i.e. `strokeDasharray`

#### ciampo on 2022-01-20T09:59:48Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788595708
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
+			>
+				<Circle
+					className="components-spinner__indicator"
+					cx="50%"
+					cy="50%"
+					r={ radius }
+					stroke-dasharray={ strokeDasharray }
```

This was actually done in https://github.com/WordPress/gutenberg/pull/37551/commits/cd30bdb1fb1c1de37ea37953edf1305425300385, marking as resolved

### Review thread 12 (resolved)

#### ciampo on 2022-01-19T17:07:32Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787963921
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
+			>
+				<Circle
+					className="components-spinner__indicator"
```

After converting to Emotion, let's remove this hardcoded classname

### Review thread 13 (resolved)

#### ciampo on 2022-01-19T17:09:08Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787965365
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
+			>
+				<Circle
+					className="components-spinner__indicator"
+					cx="50%"
+					cy="50%"
+					r={ radius }
+					stroke-dasharray={ strokeDasharray }
+					vector-effect="non-scaling-stroke"
```

I believe in React this prop should be specific camel-case, i.e. `vectorEffect`

#### ciampo on 2022-01-20T10:00:02Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788595940
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
+			>
+				<Circle
+					className="components-spinner__indicator"
+					cx="50%"
+					cy="50%"
+					r={ radius }
+					stroke-dasharray={ strokeDasharray }
+					vector-effect="non-scaling-stroke"
```

Done in https://github.com/WordPress/gutenberg/pull/37551/commits/cd30bdb1fb1c1de37ea37953edf1305425300385, marking as resolved

### Review thread 14 (resolved)

#### ciampo on 2022-01-19T17:11:14Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787967135
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
```

Why is this attribute necessary? SVG elements shouldn't be focusable by default

#### jasmussen on 2022-01-20T09:10:20Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788543036
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
```

I'm fairily sure we've been adding `focusable="false"` because IE11 would focus SVGs regardless. Since we don't support IE11 anymore, we can remove it, however I wonder if we shouldn't do that in a separate PR, and remove across all SVGs in the codebase? Not a strong opinion.

#### ciampo on 2022-01-20T10:01:12Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788596999
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
```

Thank you for the additional context!

> I wonder if we shouldn't do that in a separate PR, and remove across all SVGs in the codebase?

Makes sense to me. Let's tackle this separately

#### jameskoster on 2022-01-20T10:16:50Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788612898
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
+				focusable="false"
```

Similar to my comment on a11y, let's keep this PR focussed on the visual stuff and address these details separately?

### Review thread 15 (resolved)

#### ciampo on 2022-01-19T17:25:35Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787979904
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
```

Using `role="img"` and `aria-hidden="true"` seems contradictory: on one hand we're telling the browser that this is a meaningful image (for which we don't provide alt text), on the other we're telling the browser (and any assistive technology) to ignore this element.

I'm not an expert in a11y patterns, but from a quick look, it seems like a good combination could be using `role="alert"` and `aria-live="assertive"` ([example from carbon design system](https://www.carbondesignsystem.com/components/loading/accessibility)). If we don't want to investigate this route too much, in my opinion we should at least:

- remove `aria-hidden="true"`
- keep the `role="img"` attribute
- provide an accessible label. According to [this article](https://css-tricks.com/accessible-svgs/), the best way to achieve this would be to add a `<title id="uniqueTitleID">The title of the SVG</title>` tag inside the SVG element, and then to add `aria-labelledby="uniqueTitleID"` to the SVG element itself

#### jameskoster on 2022-01-20T09:58:46Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788594697
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
```

These are excellent points, but as this PR seeks only to update the design may I suggest we investigate a11y in a dedicated issue?

#### ciampo on 2022-01-20T10:05:16Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788601203
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
+			<SVG
+				viewBox={ viewBox }
+				xmlns="http://www.w3.org/2000/svg"
+				className="components-spinner__track"
+				role="img"
+				aria-hidden="true"
```

That's a fair point.

Maybe a good interim solution would be to remove `aria-hidden="true"` and change the role to `role="presentation"`. We can then investigate the a11y in a follow-up PR as you suggested

### Review thread 16 (resolved)

#### ciampo on 2022-01-19T17:27:49Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787981774
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
+
+	return (
+		<StyledSpinner className="components-spinner">
```

I believe we may need to keep this hardcoded classname even after switching to Emotion for styles, in order to maintain compat

### Review thread 17

#### ciampo on 2022-01-19T17:34:49Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r787987574
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
```

Sorry if missed this while skimming through the previous conversations, but is there a reason why we compute dynamically `viewBox`, `radius` and `strokeDasharray`, all in relation to `spinnerSize` ?

With SVG we should be able to define an arbitrary `viewBox` and then scale everything accordingly. We could then use `spinnerSize` in the styles to change `width` and `height` of the `SVG` element itself ([example](https://codepen.io/ciampo/pen/RwLmgPo))

#### jameskoster on 2022-01-20T10:02:23Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788598211
Path: packages/components/src/spinner/index.js

```diff
@@ -1,8 +1,54 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner } from './styles/spinner-styles';
+import { COLORS, CONFIG } from '../utils';
+
+/**
+ * WordPress dependencies
+ */
+import { SVG, Circle } from '@wordpress/primitives';
+
+// A dedicated wrapper allows us to apply width and shadow color based on config values
+// The SVG can only have a centered stroke so a stacked box-shadow accomplishes a matching 1.5px border
+export const StyledSpinner = styled.span`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
+		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
+`;
 
 export default function Spinner() {
-	return <StyledSpinner className="components-spinner" />;
+	const viewBox = `0 0 ${ CONFIG.spinnerSize } ${ CONFIG.spinnerSize }`;
+	const radius = Number( CONFIG.spinnerSize ) / 2;
+	const strokeDasharray = [
+		Number( CONFIG.spinnerSize ),
+		Number( CONFIG.spinnerSize ) * 10,
+	];
```

Yeah that would be a lot simpler wouldn't it :) We'll need to keep the computed `stroke-dasharray` though in order for the indicator to scale correctly, so we'll probably need to add a `size` property to the Spinner in the future.

### Review thread 18

#### jameskoster on 2022-01-20T10:18:47Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788614818
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

@ciampo let me know if this is correct.

#### ciampo on 2022-01-20T10:38:06Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788633228
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

You should be able to just use the value `18`, without wrapping it in `Number()`. You could then also update the `stroke-dasharray` attribute, since it doesn't require a runtime `calc` anymore

<details>

<summary>For example (click to expand):</summary>

```diff
diff --git a/packages/components/src/spinner/index.js b/packages/components/src/spinner/index.js
index b256e44a75..f30a852403 100644
--- a/packages/components/src/spinner/index.js
+++ b/packages/components/src/spinner/index.js
@@ -39,8 +39,7 @@ export const SpinnerIndicator = styled.circle`
 	stroke-linecap: round;
 	stroke-width: 1.5px;
 	transform-origin: 50% 50%;
-	stroke-dasharray: ${ CONFIG.spinnerSize },
-		calc( ${ CONFIG.spinnerSize } * 10 );
+	stroke-dasharray: ${ CONFIG.spinnerSize }, ${ CONFIG.spinnerSize * 10 };
 `;
 
 export default function Spinner() {
diff --git a/packages/components/src/utils/config-values.js b/packages/components/src/utils/config-values.js
index 526f85c457..7ba2b9d3dc 100644
--- a/packages/components/src/utils/config-values.js
+++ b/packages/components/src/utils/config-values.js
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: Number( '18' ),
+	spinnerSize: 18,
 	fontSize: '13px',
 	fontSizeH1: 'calc(2.44 * 13px)',
 	fontSizeH2: 'calc(1.95 * 13px)',
```
</details>

#### jameskoster on 2022-01-20T12:52:19Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788733760
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

I get an error when I try it this way 🤔 

<img width="876" alt="Screenshot 2022-01-20 at 12 46 21" src="https://user-images.githubusercontent.com/846565/150341268-3ff0db1e-b9da-4c82-be3e-2cbe9fe46060.png">

Thinking about this a little more, do we actually need the `spinnerSize` config value? It doesn't seem to serve much of a purpose right now, especially as all Spinner instances are currently the same size. Might a better approach be to allow a `size` prop on the `Spinner` component? That way it's easy to use different sized spinners ad hoc.

#### ciampo on 2022-01-20T13:08:10Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788746175
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

> I get an error when I try it this way 🤔

It seems like your IDE thinks that `spinnerSize` is a `string` — did you also update `spinnerSize` in the config (as part of the diff above)? i.e

```suggestion
	spinnerSize: 18,
```

> Thinking about this a little more, do we actually need the spinnerSize config value? It doesn't seem to serve much of a purpose right now, especially as all Spinner instances are currently the same size. Might a better approach be to allow a size prop on the Spinner component? That way it's easy to use different sized spinners ad hoc.

Let's leave it in the config for now, as it matches [the `$spinner-size` variable in the base styles](https://github.com/WordPress/gutenberg/blob/eb8c7690b4cacab028dd74e971a2e000bf5147d6/packages/base-styles/_variables.scss#L58). We have plans later this year to take a closer look at the overall config variables :) 

Re. adding a `size` prop, let's investigate that separately from this PR, maybe while we also address some of the ideas discussed in https://github.com/WordPress/gutenberg/pull/37551#discussion_r787987574

#### jameskoster on 2022-01-20T13:28:09Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788762607
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

> did you also update spinnerSize in the config (as part of the diff above)? i.e

Derp, I did not :)

> Re. adding a size prop, let's investigate that separately from this PR

Okay! I think we'll need to do _something_, because atm if you use a `Spinner` and resize it with css alone it won't scale correctly:

<img width="234" alt="Screenshot 2022-01-20 at 13 26 18" src="https://user-images.githubusercontent.com/846565/150347718-af58237a-0cc2-4e32-9604-3575d4afdd00.png">

#### ciampo on 2022-01-20T13:37:08Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r788770236
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

Oh, I see. Would that be considered a regression introduced by this PR ? Do we have instances in which the `Spinner` is rendered at a bigger size from its default?

It would still be good to add a Storybook example for this case scenario — (either in this PR or in a separate PR)

#### ciampo on 2022-01-20T19:04:21Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r789070270
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

Alright, I went ahead and had some fun trying out a couple of different approaches — I landed on what seems like a good solution, which avoids using `stroke-dasharray` and therefore scales better when making the spinner bigger (without adding a `size` prop).

These are the steps I went through:

1. Use a `circle` to render the gray "track" (instead of `box-shadow`)
2. Render the indicator (the blue arc) by using the [`path` element](https://developer.mozilla.org/en-US/docs/Web/SVG/Tutorial/Paths#arcs) (instead of `stroke-dasharray`)
3. Clean-up the CSS code by
  - removing the `box-shadow`, `border-radius` and `stroke-dasharray` CSS rules
  - applying the animation only on the indicator arc
  - refactoring some common styles

In order to showcase these changes in Storybook, I went ahead and allowed the `Spinner` component to receive `props` (including a classname). Finally, I added 2 more spinners to the Storybook example:

<img width="343" alt="Screenshot 2022-01-20 at 19 59 02" src="https://user-images.githubusercontent.com/1083581/150404331-d8351268-993b-4753-b567-782e99b1e3ff.png">

<details>

<summary>Click to show code changes</summary>

```diff
diff --git a/packages/components/src/spinner/index.js b/packages/components/src/spinner/index.js
index 3a28c732d8..7ea6a41b91 100644
--- a/packages/components/src/spinner/index.js
+++ b/packages/components/src/spinner/index.js
@@ -1,23 +1,47 @@
+/**
+ * External dependencies
+ */
+import classNames from 'classnames';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner, SpinnerIndicator } from './styles';
+import { StyledSpinner, SpinnerTrack, SpinnerIndicator } from './styles';
 
-export default function Spinner() {
+/**
+ * @typedef OwnProps
+ *
+ * @property {string} [className] Class name
+ */
+/** @typedef {import('react').ComponentPropsWithoutRef<'svg'> & OwnProps} Props */
+
+/**
+ * @param {Props} props
+ * @return {JSX.Element} Element
+ */
+export default function Spinner( { className, ...props } ) {
 	return (
 		<StyledSpinner
-			className="components-spinner"
+			{ ...props }
+			className={ classNames( 'components-spinner', className ) }
 			viewBox="0 0 100 100"
 			xmlns="http://www.w3.org/2000/svg"
 			role="presentation"
 			focusable="false"
 		>
-			<SpinnerIndicator
-				cx="50%"
-				cy="50%"
+			{ /* Gray circular background */ }
+			<SpinnerTrack
+				cx="50"
+				cy="50"
 				r="50"
 				vectorEffect="non-scaling-stroke"
 			/>
+
+			{ /* Theme-colored arc */ }
+			<SpinnerIndicator
+				d="m 50 0 a 50 50 0 0 1 50 50"
+				vectorEffect="non-scaling-stroke"
+			/>
 		</StyledSpinner>
 	);
 }
diff --git a/packages/components/src/spinner/stories/index.js b/packages/components/src/spinner/stories/index.js
index 12b60f457b..69ab2694a2 100644
--- a/packages/components/src/spinner/stories/index.js
+++ b/packages/components/src/spinner/stories/index.js
@@ -1,10 +1,34 @@
+/**
+ * External dependencies
+ */
+import { css } from '@emotion/react';
+
 /**
  * Internal dependencies
  */
+import { useCx } from '../../utils';
 import Spinner from '../';
 
 export default { title: 'Components/Spinner', component: Spinner };
 
 export const _default = () => {
-	return <Spinner />;
+	const cx = useCx();
+	const mediumSpinner = cx( css`
+		width: 100px;
+		height: 100px;
+	` );
+	const largeSpinner = cx( css`
+		width: 200px;
+		height: 200px;
+	` );
+	return (
+		<>
+			<Spinner />
+			<br />
+			<Spinner className={ mediumSpinner } />
+			<br />
+			<Spinner className={ largeSpinner } />
+			<br />
+		</>
+	);
 };
diff --git a/packages/components/src/spinner/styles.js b/packages/components/src/spinner/styles.js
index 0d2e4c0761..c28c117ce3 100644
--- a/packages/components/src/spinner/styles.js
+++ b/packages/components/src/spinner/styles.js
@@ -2,7 +2,7 @@
  * External dependencies
  */
 import styled from '@emotion/styled';
-import { keyframes } from '@emotion/react';
+import { css, keyframes } from '@emotion/react';
 
 /**
  * Internal dependencies
@@ -10,34 +10,39 @@ import { keyframes } from '@emotion/react';
 import { COLORS, CONFIG } from '../utils';
 
 const spinAnimation = keyframes`
-     from {
-         transform: rotate(0deg);
-     }
-     to {
-         transform: rotate(360deg);
-     }
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
  `;
 
 // The Circle can only have a centered stroke so a stacked box-shadow on the svg accomplishes a matching 1.5px border
 export const StyledSpinner = styled.svg`
 	width: ${ CONFIG.spinnerSize }px;
 	height: ${ CONFIG.spinnerSize }px;
-	box-shadow: inset 0 0 0 0.75px ${ COLORS.gray[ 300 ] },
-		0 0 0 0.75px ${ COLORS.gray[ 300 ] };
 	display: block;
-	border-radius: 50%;
 	margin: auto;
 	position: relative;
 	color: var( --wp-admin-theme-color );
 	overflow: visible;
-	animation: 1.4s linear infinite both ${ spinAnimation };
 `;
 
-export const SpinnerIndicator = styled.circle`
+const commonPathProps = css`
 	fill: transparent;
+	stroke-width: 1.5px;
+`;
+
+export const SpinnerTrack = styled.circle`
+	${ commonPathProps };
+	stroke: ${ COLORS.gray[ 300 ] };
+`;
+
+export const SpinnerIndicator = styled.path`
+	${ commonPathProps };
 	stroke: currentColor;
 	stroke-linecap: round;
-	stroke-width: 1.5px;
 	transform-origin: 50% 50%;
-	stroke-dasharray: ${ CONFIG.spinnerSize }, ${ CONFIG.spinnerSize * 10 };
+	animation: 1.4s linear infinite both ${ spinAnimation };
 `;
```
</summary>

I think this approach could solve a couple of problems without introducing much complexity, what do you think?

#### jameskoster on 2022-01-21T10:05:46Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r789522489
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

Oh nice, this looks more comprehensive – thanks so much :) Please feel free to push the changes.

Do you think it's worth including the preset sizes in the component as a part of this PR? If so the only tweak I would suggest would be to use multiples of our grid unit (or spinner-size) values for the medium/large spinners. 

Now that I think of it, it might be a good idea to make the default `spinnerSize` 16px so that it better aligns with the broader grid.

#### ciampo on 2022-01-21T12:07:16Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r789605029
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

> Please feel free to push the changes.

Thank you ! I went ahead and pushed those changes (including a change to the README to remove the out of date screenshot).

> Do you think it's worth including the preset sizes in the component as a part of this PR? If so the only tweak I would suggest would be to use multiples of our grid unit (or spinner-size) values for the medium/large spinners.

I did include the updates to the Storybook examples, if anything to test that the stroke width is not changing as we increase the overall size of the spinner — I also made sure that those storybook examples use multiples of the grid unit.

For now, I wouldn't include any new APIs to the component (e.g. `size`), since the previous one also didn't have it.

> Now that I think of it, it might be a good idea to make the default spinnerSize 16px so that it better aligns with the broader grid.

I haven't made that change yet as I'm not sure if resizing the spinner (from `18px` to `16px`) would have any ripercussions / would be considered breaking change. @jasmussen do you have any opinions on this?

#### jameskoster on 2022-01-21T16:37:16Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r789816438
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

Thanks, this is looking fantastic :) 

I think there's a reasonable case to resize from 18px to 16px. It aligns with our grid unit, and most other icons tend to render at ~16px, e.g. Styles icon:

<img width="170" alt="Screenshot 2022-01-21 at 16 36 14" src="https://user-images.githubusercontent.com/846565/150565004-ea98b958-dee4-435a-99e3-fedaca659c00.png">

Not a strong feeling though, and happy to defer to @jasmussen on this point.

#### jasmussen on 2022-01-21T16:41:58Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r789820964
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

I like it.

#### ciampo on 2022-01-22T09:47:00Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r790125003
Path: packages/components/src/utils/config-values.js

```diff
@@ -48,7 +48,7 @@ export default {
 	borderWidth: '1px',
 	borderWidthFocus: '1.5px',
 	borderWidthTab: '4px',
-	spinnerSize: '18px',
+	spinnerSize: Number( '18' ),
```

Done in https://github.com/WordPress/gutenberg/pull/37551/commits/91a861fa30bc0a8ebc957027214d3952c7d79796

Let me know once you've had a look, I think we may be able to merge this PR :)

### Review thread 19

#### sarayourfriend on 2022-01-24T11:30:58Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r790654591
Path: packages/components/src/spinner/index.js

```diff
@@ -1,23 +1,47 @@
+/**
+ * External dependencies
+ */
+import classNames from 'classnames';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner, SpinnerIndicator } from './styles';
+import { StyledSpinner, SpinnerTrack, SpinnerIndicator } from './styles';
 
-export default function Spinner() {
+/**
+ * @typedef OwnProps
+ *
+ * @property {string} [className] Class name
+ */
+/** @typedef {import('react').ComponentPropsWithoutRef<'svg'> & OwnProps} Props */
+
+/**
+ * @param {Props} props
+ * @return {JSX.Element} Element
+ */
+export default function Spinner( { className, ...props } ) {
```

Typically we forward refs for all components. I guess this is useful for integrating with a library like Reakit where passing refs for important components happens on a regular basis.

Any reason not to foward the ref for Spinner as well?

#### ciampo on 2022-01-24T11:34:07Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r790656836
Path: packages/components/src/spinner/index.js

```diff
@@ -1,23 +1,47 @@
+/**
+ * External dependencies
+ */
+import classNames from 'classnames';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner, SpinnerIndicator } from './styles';
+import { StyledSpinner, SpinnerTrack, SpinnerIndicator } from './styles';
 
-export default function Spinner() {
+/**
+ * @typedef OwnProps
+ *
+ * @property {string} [className] Class name
+ */
+/** @typedef {import('react').ComponentPropsWithoutRef<'svg'> & OwnProps} Props */
+
+/**
+ * @param {Props} props
+ * @return {JSX.Element} Element
+ */
+export default function Spinner( { className, ...props } ) {
```

No reason in particular. I guess this PR was initially focused on updating the visual styles of Spinner, and so we didn't focus as much on other aspects (like forwarding refs, rewriting in typescript, using context etcc)

But it's a quick change and it won't hurt to add it to this PR

#### sarayourfriend on 2022-01-24T11:48:14Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r790666622
Path: packages/components/src/spinner/index.js

```diff
@@ -1,23 +1,47 @@
+/**
+ * External dependencies
+ */
+import classNames from 'classnames';
+
 /**
  * Internal dependencies
  */
-import { StyledSpinner, SpinnerIndicator } from './styles';
+import { StyledSpinner, SpinnerTrack, SpinnerIndicator } from './styles';
 
-export default function Spinner() {
+/**
+ * @typedef OwnProps
+ *
+ * @property {string} [className] Class name
+ */
+/** @typedef {import('react').ComponentPropsWithoutRef<'svg'> & OwnProps} Props */
+
+/**
+ * @param {Props} props
+ * @return {JSX.Element} Element
+ */
+export default function Spinner( { className, ...props } ) {
```

Oh yeah, good point 🙂 Feel free to do in another PR!

If we do forward the refs then we should be able to use `WordPressComponentProps` with it as well.

Might as well do that in a separate PR as it'll require a few other lines to change as well (including calling `useContext` on props).

### Review thread 20

#### ciampo on 2022-01-24T13:23:40Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r790736997
Path: packages/block-editor/src/components/link-control/style.scss

```diff
@@ -483,16 +483,11 @@ $preview-image-height: 140px;
 		 * Position spinner to the left of the actions.
 		 *
 		 * Compensate for:
-		 *  - Input margin ($grid-unit-20)
-		 *  - Border (1px)
-		 *  - Vertically, for the difference in height between the input (40px)
-		 *    and the spinner.
-		 *  - Horizontally, adjust for the width occupied by the icon buttons,
-		 *    then artificially create spacing that mimics as if the spinner
-		 *    were center-padded to the same width as an icon button.
+		 *  - Input padding top ($grid-unit-15) plus difference between effective input line-height (18px) and spinner height (16px).
+		 *  - Input padding right ($button-size)
```

We should probably translate the calculations explained in the comment in the code too, so that the value stays correct and up to date ?

#### jameskoster on 2022-01-25T09:33:29Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r791521808
Path: packages/block-editor/src/components/link-control/style.scss

```diff
@@ -483,16 +483,11 @@ $preview-image-height: 140px;
 		 * Position spinner to the left of the actions.
 		 *
 		 * Compensate for:
-		 *  - Input margin ($grid-unit-20)
-		 *  - Border (1px)
-		 *  - Vertically, for the difference in height between the input (40px)
-		 *    and the spinner.
-		 *  - Horizontally, adjust for the width occupied by the icon buttons,
-		 *    then artificially create spacing that mimics as if the spinner
-		 *    were center-padded to the same width as an icon button.
+		 *  - Input padding top ($grid-unit-15) plus difference between effective input line-height (18px) and spinner height (16px).
+		 *  - Input padding right ($button-size)
```

The only issue there is that the 18px seems to be circumstantial rather than based on any registered value that I can see. 

Thinking about it a little more, we can probably just do something like `top: calc(50% - #{$spinner-size} / 2);` which seems simpler :)

### Review thread 21

#### ciampo on 2022-01-31T19:44:17Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796007814
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

As I gave one last look at the code changes, I just noticed a couple of differences that I'd like to discuss:

- `display`: it used to be `inline-block`, it's currently `block`. I personally like `block` more, but this change could be potentially breaking for some layouts
- `margin`: it used to be `5px 11px 0`, it's not `auto`. This is also a potentially breaking change. I personally believe that a component should not define rules for its own margin, and so my preference would be to either keep things as they are, or to introduce a breaking change by removing the `margin` rule altogether.

What do folks think? (also cc @mirka @jasmussen )

#### jasmussen on 2022-01-31T19:54:39Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796015021
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

High level I do like the idea of unifying and systematizing how the spinner appears, for example always centered. But it also seems like something that needs a little exploration and guideline in terms of where the spinner is currently used, and might involve updating the places where it's used. That seems to suggest a separate exploratory PR for that, and then in order to land this big improvement, perhaps going back to inline-block. Though I do think we find a better margin, perhaps `$grid-unit-10 $grid-unit-10 0`?

#### ciampo on 2022-02-01T08:48:49Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796374439
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

I agree with your point.

Probably best thing to do here is to switch back to the values we had before (`display: inline-block` and `margin: 5px 11px 0`), to make sure we don't introduce any breaking changes.

We can then definitely explore what to do separately, as you suggested

#### jameskoster on 2022-02-01T10:01:11Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796433534
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

I'm unaware of the history around the margin value, but the results are suboptimal in places like the Site Logo and Template Part blocks. `margin: auto;` works better as a default in that regard.

Then again the idea of having no margin on the Spinner at all does seem better on the surface. So I'm happy to leave the margin/display values as-is and revisit separately.

#### jasmussen on 2022-02-01T10:03:11Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796435060
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

Oh definitely we need that. Just in order to land this one with minimal disruption, we can pull back on those, and then follow up in small PRs, either a separate PR that just re-adds the auto margin entirely, or adds the margin in the contexts where they are needed.

#### ciampo on 2022-02-01T10:14:05Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796443524
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

Cool, I'll go ahead, push these changes, give it a "final final" look and hopefully merge

#### ciampo on 2022-02-01T12:50:40Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796563101
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

Merged!

> follow up in small PRs, either a separate PR that just re-adds the auto margin entirely, or adds the margin in the contexts where they are needed.

My preference would be to remove the `margin` rule altogether from `Spinner` and add any necessary `margin` where each instance of `Spinner` is used

#### jasmussen on 2022-02-01T13:02:04Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r796573236
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

Generally agree that the component itself should be unopinionated and subject to its container. But given very few existing containers are likely to be aware of this new responsibility, it might still make sense to have something. We could then consider that a default margin (either a fixed clearance all around, or auto margins), which would then be easily overriden by it's container?

#### mirka on 2022-02-07T13:42:15Z

URL: https://github.com/WordPress/gutenberg/pull/37551#discussion_r800666169
Path: packages/components/src/spinner/styles.js

```diff
@@ -0,0 +1,47 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css, keyframes } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../utils';
+
+const spinAnimation = keyframes`
+	from {
+		transform: rotate(0deg);
+	}
+	to {
+		transform: rotate(360deg);
+	}
+ `;
+
+export const StyledSpinner = styled.svg`
+	width: ${ CONFIG.spinnerSize }px;
+	height: ${ CONFIG.spinnerSize }px;
+	display: block;
+	margin: auto;
```

> My preference would be to remove the `margin` rule altogether from `Spinner` and add any necessary `margin` where each instance of `Spinner` is used

This is my general preference, too. And if the audit shows that it's frequently used as an inline item, I'd consider giving it some padding (instead of margin) so it isn't flush with the bounding box. Basically like an icon component.

## Changed files

- packages/base-styles/_variables.scss (MODIFIED, +1/-1)
- packages/block-editor/src/components/link-control/style.scss (MODIFIED, +3/-9)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/spinner/README.md (MODIFIED, +10/-10)
- packages/components/src/spinner/index.js (MODIFIED, +42/-3)
- packages/components/src/spinner/stories/index.js (MODIFIED, +36/-3)
- packages/components/src/spinner/styles.js (ADDED, +47/-0)
- packages/components/src/spinner/styles/spinner-styles.js (DELETED, +0/-47)
- packages/components/src/utils/config-values.js (MODIFIED, +1/-1)
