---
number: 41580
type: pull_request
state: MERGED
merged_at: 2022-06-30T10:36:58Z
url: https://github.com/WordPress/gutenberg/pull/41580
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Type] Code Quality","[Package] Components"]
---

# PR #41580: HStack: Convert component to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/41580
- Author: ralucaStan
- Created: 2022-06-07T16:35:21Z
- Updated: 2022-06-30T10:37:25Z
- Comments: 1 of 1
- Review threads: 5 of 5
- Changed files: 9 of 9
- Diff: ../pr-diffs/41580.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->
Converts the HStack component to TypeScript

Part of https://github.com/WordPress/gutenberg/issues/35744
## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
Part of Contributor Day at WordCamp EU 2022
## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
I followed the Transition to TS Guide.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
1. Run `npm run dev` and ensure no typing errors.
2. Run `npm run storybook:dev` and visit the `HStack` component.

cc @ciampo 
## Screenshots or screencast <!-- if applicable -->

## Issue comments

### ralucaStan on 2022-06-24T13:49:55Z

URL: https://github.com/WordPress/gutenberg/pull/41580#issuecomment-1165596044

@ciampo thank you so much for your support with this PR. I have addressed the requested changes and the Story for this component now works. Please take a look again.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-06-09T13:46:25Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893527586
Path: packages/components/src/h-stack/utils.ts

```diff
@@ -2,9 +2,14 @@
  * External dependencies
  */
 import { isNil } from 'lodash';
+/**
```

Nit: spacing

```suggestion

/**
```

### Review thread 2 (resolved)

#### ciampo on 2022-06-09T13:54:38Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893537097
Path: packages/components/src/h-stack/utils.ts

```diff
@@ -33,23 +37,21 @@ const V_ALIGNMENTS = {
 	topRight: { justify: 'flex-start', align: 'flex-end' },
 };
 
-/* eslint-disable jsdoc/valid-types */
-/**
- * @param {import('./types').HStackAlignment | import('react').CSSProperties[ 'alignItems' ]} alignment         Where to align.
- * @param {import('../flex/types').FlexDirection}                                             [direction='row'] Direction to align.
- * @return {import('./types').AlignmentProps} Alignment props.
- */
-/* eslint-enable jsdoc/valid-types */
-export function getAlignmentProps( alignment, direction = 'row' ) {
+export function getAlignmentProps(
+	alignment: HStackAlignment | CSSProperties[ 'alignItems' ],
+	direction: FlexDirection = 'row'
+): AlignmentProps {
 	if ( isNil( alignment ) ) {
 		return {};
 	}
 	const isVertical = direction === 'column';
-	const props = isVertical ? V_ALIGNMENTS : ALIGNMENTS;
+	const props = isVertical ? V_ALIGNMENTS : H_ALIGNMENTS;
+
+	// alignment?: HStackAlignment | CSSProperties[ 'alignItems' ];
```

We should probably remove this leftover comment

### Review thread 3 (resolved)

#### ciampo on 2022-06-09T13:57:08Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893540083
Path: packages/components/src/ui/utils/get-valid-children.ts

```diff
@@ -15,7 +16,9 @@ import { Children, isValidElement } from '@wordpress/element';
  *
  * @return An array of available children.
  */
-export function getValidChildren( children: ReactNode ): ReactNodeArray {
+export function getValidChildren(
+	children: ReactNode
+): Array< React.ReactChild | React.ReactFragment | React.ReactPortal > {
```

We can use `ReactNode[]` for the return type here (as suggested by the deprecation message for the `ReactNodeArray` type)

![Screenshot 2022-06-09 at 15 56 45](https://user-images.githubusercontent.com/1083581/172864766-ab6f93db-0801-4e32-a7a6-9e64402e2f98.png)

#### ralucaStan on 2022-06-09T16:22:53Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893717506
Path: packages/components/src/ui/utils/get-valid-children.ts

```diff
@@ -15,7 +16,9 @@ import { Children, isValidElement } from '@wordpress/element';
  *
  * @return An array of available children.
  */
-export function getValidChildren( children: ReactNode ): ReactNodeArray {
+export function getValidChildren(
+	children: ReactNode
+): Array< React.ReactChild | React.ReactFragment | React.ReactPortal > {
```

Actually that was the initial approach but it was not relevant to what the function was returning. `ReactNode` is `ReactChild | ReactFragment | ReactPortal | boolean | null | undefined` and this function removes invalid elements, so the returned array does not have boolean, null or undefined values.

#### ralucaStan on 2022-06-09T17:07:12Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893757198
Path: packages/components/src/ui/utils/get-valid-children.ts

```diff
@@ -15,7 +16,9 @@ import { Children, isValidElement } from '@wordpress/element';
  *
  * @return An array of available children.
  */
-export function getValidChildren( children: ReactNode ): ReactNodeArray {
+export function getValidChildren(
+	children: ReactNode
+): Array< React.ReactChild | React.ReactFragment | React.ReactPortal > {
```

But it does not trigger any error if I use ReactNode[] so please advise how to move fw on this.

#### ciampo on 2022-06-10T08:03:28Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r894261818
Path: packages/components/src/ui/utils/get-valid-children.ts

```diff
@@ -15,7 +16,9 @@ import { Children, isValidElement } from '@wordpress/element';
  *
  * @return An array of available children.
  */
-export function getValidChildren( children: ReactNode ): ReactNodeArray {
+export function getValidChildren(
+	children: ReactNode
+): Array< React.ReactChild | React.ReactFragment | React.ReactPortal > {
```

Thank you for the context!

In that case, I would either

<details>
<summary>keep `ReactChild | ReactFragment | ReactPortal` (but using only one import)</summary>

```diff
diff --git a/packages/components/src/ui/utils/get-valid-children.ts b/packages/components/src/ui/utils/get-valid-children.ts
index e4f0726cb0..5b147029a7 100644
--- a/packages/components/src/ui/utils/get-valid-children.ts
+++ b/packages/components/src/ui/utils/get-valid-children.ts
@@ -1,13 +1,12 @@
 /**
  * External dependencies
  */
-import type { ReactNode } from 'react';
+import type { ReactNode, ReactChild, ReactFragment, ReactPortal } from 'react';
 
 /**
  * WordPress dependencies
  */
 import { Children, isValidElement } from '@wordpress/element';
-import type React from 'react';
 
 /**
  * Gets a collection of available children elements from a React component's children prop.
@@ -18,7 +17,7 @@ import type React from 'react';
  */
 export function getValidChildren(
 	children: ReactNode
-): Array< React.ReactChild | React.ReactFragment | React.ReactPortal > {
+): Array< ReactChild | ReactFragment | ReactPortal > {
 	if ( typeof children === 'string' ) return [ children ];
 
 	return Children.toArray( children ).filter( ( child ) =>
```

</details>


or

<details>
<summary>exclude `boolean | null | undefined` from the `ReactNode` type</summary>

```diff
diff --git a/packages/components/src/ui/utils/get-valid-children.ts b/packages/components/src/ui/utils/get-valid-children.ts
index e4f0726cb0..2a6d88861d 100644
--- a/packages/components/src/ui/utils/get-valid-children.ts
+++ b/packages/components/src/ui/utils/get-valid-children.ts
@@ -7,7 +7,8 @@ import type { ReactNode } from 'react';
  * WordPress dependencies
  */
 import { Children, isValidElement } from '@wordpress/element';
-import type React from 'react';
+
+type ValidReactNode = Exclude< ReactNode, null | undefined | boolean >;
 
 /**
  * Gets a collection of available children elements from a React component's children prop.
@@ -16,9 +17,7 @@ import type React from 'react';
  *
  * @return An array of available children.
  */
-export function getValidChildren(
-	children: ReactNode
-): Array< React.ReactChild | React.ReactFragment | React.ReactPortal > {
+export function getValidChildren( children: ReactNode ): ValidReactNode[] {
 	if ( typeof children === 'string' ) return [ children ];
 
 	return Children.toArray( children ).filter( ( child ) =>
```
</details>


Do you have any preferences?

### Review thread 4 (resolved)

#### ciampo on 2022-06-09T13:58:27Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893541596
Path: packages/components/src/ui/utils/get-valid-children.ts

```diff
@@ -1,12 +1,13 @@
 /**
  * External dependencies
  */
-import type { ReactNode, ReactNodeArray } from 'react';
+import type { ReactNode } from 'react';
 
 /**
  * WordPress dependencies
  */
 import { Children, isValidElement } from '@wordpress/element';
+import type React from 'react';
```

This won't be needed anymore with the changes proposed in the previous comment, but in case it was, we should merge this line into the `import type` statement from the "External dependencies" section a few lines above

### Review thread 5 (resolved)

#### ciampo on 2022-06-09T13:59:01Z

URL: https://github.com/WordPress/gutenberg/pull/41580#discussion_r893542268
Path: packages/components/src/h-stack/hook.tsx

```diff
@@ -22,19 +28,18 @@ export function useHStack( props ) {
 	const align = getAlignmentProps( alignment, direction );
 
 	const validChildren = getValidChildren( children );
-	const clonedChildren = validChildren.map(
-		// @ts-ignore
-		( /** @type {import('react').ReactElement} */ child, index ) => {
-			const _key = child.key || `hstack-${ index }`;
-			const _isSpacer = hasConnectNamespace( child, [ 'Spacer' ] );
+	const clonedChildren = validChildren.map( ( child, index ) => {
+		const _isSpacer = hasConnectNamespace( child, [ 'Spacer' ] );
 
-			if ( _isSpacer ) {
-				return <FlexItem isBlock key={ _key } { ...child.props } />;
-			}
+		if ( _isSpacer ) {
+			const childElement = child as ReactElement;
+			const _key = childElement.key || `hstack-${ index }`;
 
-			return child;
+			return <FlexItem isBlock key={ _key } { ...childElement.props } />;
 		}
-	);
+
+		return child;
+	} );
```

Great rewrite 🚀

## Changed files

- packages/components/src/h-stack/component.tsx (RENAMED, +8/-8)
- packages/components/src/h-stack/hook.tsx (RENAMED, +21/-16)
- packages/components/src/h-stack/index.ts (RENAMED, +0/-0)
- packages/components/src/h-stack/stories/index.js (DELETED, +0/-22)
- packages/components/src/h-stack/stories/index.tsx (ADDED, +92/-0)
- packages/components/src/h-stack/test/__snapshots__/index.tsx.snap (RENAMED, +0/-0)
- packages/components/src/h-stack/test/index.tsx (RENAMED, +0/-0)
- packages/components/src/h-stack/utils.ts (RENAMED, +14/-14)
- packages/components/src/ui/utils/get-valid-children.ts (MODIFIED, +4/-2)
