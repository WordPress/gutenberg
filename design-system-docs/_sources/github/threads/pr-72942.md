---
number: 72942
type: pull_request
state: MERGED
merged_at: 2025-11-12T18:26:07Z
url: https://github.com/WordPress/gutenberg/pull/72942
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Performance","[Package] Theme"]
---

# PR #72942: Theme: Simplify ramp object by flattening warnings to optional top-level property

- URL: https://github.com/WordPress/gutenberg/pull/72942
- Author: aduth
- Created: 2025-11-03T19:45:02Z
- Updated: 2025-11-12T18:26:43Z
- Comments: 2 of 2
- Review threads: 1 of 1
- Changed files: 10 of 10
- Diff: ../pr-diffs/72942.diff

## Body

## What?

Closes #72931

Updates the object shape generated by the theme package to simplify how colors and warnings are referenced from a generated color ramp.

Follow-up to #72847 

## Why?

- Reduce bundle size
- Simplified code structure for easier understanding

## How?

Rather than assigning each ramp value as an object of `{ color: string, warning: boolean }`, optimistically assumes that colors will be generated without warnings and instead assigns the value as `string`, with warnings only assigned as a top-level object key if any exist.

Optionally, as mentioned by @jsnajdr in https://github.com/WordPress/gutenberg/pull/72847#discussion_r2485701020, we could remove `warning` behavior altogether, as its primary purpose is for debugging. Personally I feel it might be valuable to keep for the short-term to protect against possible regressions in contrast matching, but this should serve as an incremental step in that direction, since it makes it easier to remove `warning` altogether in assuming that it's optional and likely excluded in most cases.

## Testing Instructions

Validate no regressions in behavior of behavior of theme provider:

1. Run `npm run storybook:start`
2. Go to http://localhost:50240/?path=/docs/design-system-theme-provider--docs
3. Observe that colors generate as expected

Verify that building theme package succeeds and produces no local changes:

1. Run `npm run --workspace @wordpress/theme build`
2. Observe successful run
3. Run `git status`
4. Observe clean working branch

## Issue comments

### github-actions on 2025-11-03T19:45:14Z

URL: https://github.com/WordPress/gutenberg/pull/72942#issuecomment-3482224182

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2025-11-12T16:53:46Z

URL: https://github.com/WordPress/gutenberg/pull/72942#issuecomment-3522930993

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 58eb0556ac24b9e721e0ee2ade024be5c0cf1035.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/19304365111
📝  Reported issues:
- #69177 in `/test/e2e/specs/editor/blocks/navigation-colors.spec.js`
- #50325 in `/test/e2e/specs/editor/blocks/image.spec.js`

## Review threads

### Review thread 1

#### jsnajdr on 2025-11-12T12:56:00Z

URL: https://github.com/WordPress/gutenberg/pull/72942#discussion_r2518207960
Path: packages/theme/src/color-ramps/lib/index.ts

```diff
@@ -68,10 +68,8 @@ function calculateRamp( {
 		value: number;
 	};
 } ) {
-	const rampResults = {} as Record<
-		keyof Ramp,
-		{ color: string; warning: boolean }
-	>;
+	const rampResults = {} as Record< keyof Ramp, string >;
+	let warnings: string[] | undefined;
```

`warnings` could always be an array, mostly empty. Adding the `undefined` option just forces users to do extra `?.` checks.

#### aduth on 2025-11-12T13:26:26Z

URL: https://github.com/WordPress/gutenberg/pull/72942#discussion_r2518305766
Path: packages/theme/src/color-ramps/lib/index.ts

```diff
@@ -68,10 +68,8 @@ function calculateRamp( {
 		value: number;
 	};
 } ) {
-	const rampResults = {} as Record<
-		keyof Ramp,
-		{ color: string; warning: boolean }
-	>;
+	const rampResults = {} as Record< keyof Ramp, string >;
+	let warnings: string[] | undefined;
```

The idea with making it `undefined` is that it could be excluded from serialized output, further reducing size (see `default-ramps.ts`). But it's a good point that this benefit is partially offset by having to do all the subsequent optional handling in the code.

#### aduth on 2025-11-12T16:26:43Z

URL: https://github.com/WordPress/gutenberg/pull/72942#discussion_r2518983647
Path: packages/theme/src/color-ramps/lib/index.ts

```diff
@@ -68,10 +68,8 @@ function calculateRamp( {
 		value: number;
 	};
 } ) {
-	const rampResults = {} as Record<
-		keyof Ramp,
-		{ color: string; warning: boolean }
-	>;
+	const rampResults = {} as Record< keyof Ramp, string >;
+	let warnings: string[] | undefined;
```

I think I might keep this optional. I don't hold the opinion very strongly, but with the point of this pull request being to make the ramp objects more concise, this is an intentional and effective part of that.

## Changed files

- packages/theme/bin/generate-primitive-tokens/index.ts (MODIFIED, +1/-1)
- packages/theme/src/color-ramps/index.ts (MODIFIED, +5/-5)
- packages/theme/src/color-ramps/lib/default-ramps.ts (MODIFIED, +154/-154)
- packages/theme/src/color-ramps/lib/index.ts (MODIFIED, +26/-22)
- packages/theme/src/color-ramps/lib/types.ts (MODIFIED, +2/-7)
- packages/theme/src/color-ramps/stories/index.story.tsx (MODIFIED, +4/-1)
- packages/theme/src/color-ramps/stories/ramp-table.tsx (MODIFIED, +15/-26)
- packages/theme/src/color-ramps/test/__snapshots__/index.test.ts.snap (MODIFIED, +11717/-41231)
- packages/theme/src/color-ramps/test/index.test.ts (MODIFIED, +2/-2)
- packages/theme/src/use-theme-provider-styles.ts (MODIFIED, +1/-4)
