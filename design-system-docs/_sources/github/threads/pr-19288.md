---
number: 19288
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/19288
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #19288: Adds ColorControl component

- URL: https://github.com/WordPress/gutenberg/pull/19288
- Author: ryanwelcher
- Created: 2019-12-21T02:57:41Z
- Updated: 2022-10-04T13:58:32Z
- Comments: 13 of 13
- Review threads: 7 of 7
- Changed files: 5 of 5
- Diff: ../pr-diffs/19288.diff

## Body

## Description
Adds ColorControl component. Closes #14378

## How has this been tested?
Tests still to be implemented. I have tested this locally on master using the example provided in the README.


## Checklist:
- [ ] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/javascript/ -->
- [x] My code follows the accessibility standards. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/coding-standards/accessibility-coding-standards/ -->
- [x] My code has proper inline documentation. <!-- Guidelines: https://make.wordpress.org/core/handbook/best-practices/inline-documentation-standards/javascript/ -->
- [x] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR. <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/master/docs/contributors/native-mobile.md -->.

## Issue comments

### chrisvanpatten on 2019-12-24T01:04:24Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-568617586

@jorgefilipecosta PanelColorSettings provides a palette, right? A ColorControl would be more useful for cases where you aren’t providing a palette.

We’re also using ColorPicker directly in a sidebar (we don’t support defined palettes, it’s all freeform for our editorial teams) so I think this could be very valuable.

### ryanwelcher on 2020-01-02T19:04:10Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-570308077

> Hi @ryanwelcher, thank you for this PR! Currently, we are exporting PanelColorSettings (packages/block-editor/src/components/panel-color-settings/index.js). Would it be possible to provide some details of why PanelColorSettings did not accomplished your needs?

@jorgefilipecosta I wanted more flexibility for color choice/options.

### ryanwelcher on 2020-01-07T17:35:37Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-571691181

@chrisvanpatten I've addressed your feedback. Is there anything else I can do to move this forward? cc @jorgefilipecosta

### chrisvanpatten on 2020-01-07T19:40:35Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-571739717

Sorry for the delay @ryanwelcher — I'm pretty satisfied with this from a code perspective but I think we'd probably need signoff from a lead on including this component.

I agree on offering it, and I think it makes a ton of sense; I know I'd use it.

### chrisvanpatten on 2020-01-07T19:44:52Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-571741388

It might make sense to raise this during tomorrow's #core-editor chat; I think that'd be an appropriate venue to bring it up and ask for reviews.

### youknowriad on 2020-01-09T09:14:22Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-572465396

I'm pretty sure I saw a mockup revisiting the color selectors to use a nice color control (related to #18667) cc @jasmussen if you have any idea where to find this.

I wonder if this could be the opportunity to start leveraging those in some blocks.

### jasmussen on 2020-01-09T09:44:53Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-572477792

Hi! Not sure if this is helpful, or even necessary for this PR, but thought I'd share a mockups that are being explored as part of #18667:

<img width="291" alt="Screenshot 2020-01-09 at 10 40 22" src="https://user-images.githubusercontent.com/1204802/72056435-e41c5280-32cc-11ea-8ced-5d5753b28dff.png">

In this interface, the color picker would hold color swatches, like Figma:

<img width="248" alt="Screenshot 2020-01-09 at 10 42 00" src="https://user-images.githubusercontent.com/1204802/72056500-0a41f280-32cd-11ea-9e4a-7d75ab418e33.png">

But this is an aspect to explore a bit further as we get to it!

### youknowriad on 2020-01-09T09:51:03Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-572480273

It would be cool to add a storybook story to this PR to be able to test the component properly.

### ryanwelcher on 2020-01-14T15:42:05Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-574236011

@youknowriad I have added stories for the new component.

### youknowriad on 2020-01-14T17:49:57Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-574294383

Should we try to implement the proposed UI https://github.com/WordPress/gutenberg/pull/19288#issuecomment-572477792 ?

### youknowriad on 2020-01-23T09:06:27Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-577589994

Related #19785

### Mamaduka on 2022-09-26T09:34:34Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-1257761776

Hi, @ryanwelcher

Can you share the status of this PR? Can it be closed after the [recent ColorPicker refactoring](https://github.com/WordPress/gutenberg/issues/34284)?

### ryanwelcher on 2022-10-04T13:58:31Z

URL: https://github.com/WordPress/gutenberg/pull/19288#issuecomment-1267049437

I think we can close this out. thanks for the followup!

## Review threads

### Review thread 1 (resolved)

#### chrisvanpatten on 2019-12-24T01:05:57Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036550
Path: packages/components/src/color-control/README.md

```diff
@@ -0,0 +1,61 @@
+# ColorControl
+
+ColorControl is used to generate a color picker user interface.
+
+
+## Usage
+
+Render a user interface to change fixed background setting.
+```jsx
+import { ColorControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+const MyColorControl = withState( {
+	backgroundColor: '#fff',
+} )( ( { backgroundColor, setState } ) => (
+	<ColorControl
+			label={ 'Background Color' }
+			color={ backgroundColor }
+			onChange={ val => setState( { backgroundColor: val.hex } ) }
+			disableAlpha
+		/>
```

There’s some funky indentation happening in here. It’ also be nice to see `val` as `value` just to be more explicit.

#### ryanwelcher on 2020-01-02T18:51:25Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r362585124
Path: packages/components/src/color-control/README.md

```diff
@@ -0,0 +1,61 @@
+# ColorControl
+
+ColorControl is used to generate a color picker user interface.
+
+
+## Usage
+
+Render a user interface to change fixed background setting.
+```jsx
+import { ColorControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+const MyColorControl = withState( {
+	backgroundColor: '#fff',
+} )( ( { backgroundColor, setState } ) => (
+	<ColorControl
+			label={ 'Background Color' }
+			color={ backgroundColor }
+			onChange={ val => setState( { backgroundColor: val.hex } ) }
+			disableAlpha
+		/>
```

Update.

### Review thread 2 (resolved)

#### chrisvanpatten on 2019-12-24T01:06:08Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036573
Path: packages/components/src/color-control/README.md

```diff
@@ -0,0 +1,61 @@
+# ColorControl
+
+ColorControl is used to generate a color picker user interface.
+
+
+## Usage
+
+Render a user interface to change fixed background setting.
+```jsx
+import { ColorControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+const MyColorControl = withState( {
+	backgroundColor: '#fff',
+} )( ( { backgroundColor, setState } ) => (
+	<ColorControl
+			label={ 'Background Color' }
+			color={ backgroundColor }
+			onChange={ val => setState( { backgroundColor: val.hex } ) }
+			disableAlpha
+		/>
+) );
+```
+
+## Props
+
+The component accepts the following props:
+
+### label
+
+If this property is added, a label will be generated using label property as the content.
+
+- Type: `String`
+- Required: No
+
+### help
+
+If this property is added, a help text will be generated using help property as the content.
+
+- Type: `String|WPElement`
+- Required: No
+
+### color
+The color that will be selected.
+
+- Type: `String`
+- Required: No
+
+### onChange
+
+A function that receives the selected color value
+
+- Type: `function`
+- Required: Yes
+
+### className
+
+The class that will be added with `components-base-control` and `component-color-control` to the classes of the wrapper div. If no className is passed only `components-base-control` and `component-color-control` are used.
+
+Type: String
+Required: No
```

Missing dashes

### Review thread 3 (resolved)

#### chrisvanpatten on 2019-12-24T01:06:23Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036601
Path: packages/components/src/color-control/README.md

```diff
@@ -0,0 +1,61 @@
+# ColorControl
+
+ColorControl is used to generate a color picker user interface.
+
+
+## Usage
+
+Render a user interface to change fixed background setting.
+```jsx
+import { ColorControl } from '@wordpress/components';
+import { withState } from '@wordpress/compose';
+
+const MyColorControl = withState( {
+	backgroundColor: '#fff',
+} )( ( { backgroundColor, setState } ) => (
+	<ColorControl
+			label={ 'Background Color' }
+			color={ backgroundColor }
+			onChange={ val => setState( { backgroundColor: val.hex } ) }
+			disableAlpha
+		/>
+) );
+```
+
+## Props
+
+The component accepts the following props:
+
+### label
+
+If this property is added, a label will be generated using label property as the content.
+
+- Type: `String`
+- Required: No
+
+### help
+
+If this property is added, a help text will be generated using help property as the content.
+
+- Type: `String|WPElement`
+- Required: No
+
+### color
+The color that will be selected.
```

It’s a nit, but the other items had a space below the heading.

### Review thread 4 (resolved)

#### chrisvanpatten on 2019-12-24T01:06:35Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036619
Path: packages/components/src/color-control/README.md

```diff
@@ -0,0 +1,61 @@
+# ColorControl
+
+ColorControl is used to generate a color picker user interface.
+
+
```

another nit, but no newline needed here.

### Review thread 5 (resolved)

#### chrisvanpatten on 2019-12-24T01:06:48Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036660
Path: packages/components/src/color-control/README.md

```diff
@@ -0,0 +1,61 @@
+# ColorControl
+
+ColorControl is used to generate a color picker user interface.
```

```suggestion
ColorControl is used to generate a color picker user interface with the option to include a label and help text.
```

### Review thread 6 (resolved)

#### chrisvanpatten on 2019-12-24T01:07:09Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036698
Path: packages/components/src/color-control/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * External dependencies
+ */
+import { isFunction } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { withInstanceId } from '@wordpress/compose';
+
+/**
+ * Internal dependencies
+ */
+import BaseControl from '../base-control';
+import ColorPicker from '../color-picker';
+
+const ColorControl = ( props ) => {
+	const { label, color, help, instanceId, className, onChange, disableAlpha } = props;
+	const id = `color-toggle-control-${ instanceId }`;
+
+	let describedBy, helpLabel;
+	if ( help ) {
+		describedBy = id + '__help';
+		helpLabel = isFunction( help ) ? help( color ) : help;
+	}
```

```suggestion
	}

```

Another small nit but might be nice to put a line here.

#### ryanwelcher on 2020-01-02T18:52:35Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r362585508
Path: packages/components/src/color-control/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * External dependencies
+ */
+import { isFunction } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { withInstanceId } from '@wordpress/compose';
+
+/**
+ * Internal dependencies
+ */
+import BaseControl from '../base-control';
+import ColorPicker from '../color-picker';
+
+const ColorControl = ( props ) => {
+	const { label, color, help, instanceId, className, onChange, disableAlpha } = props;
+	const id = `color-toggle-control-${ instanceId }`;
+
+	let describedBy, helpLabel;
+	if ( help ) {
+		describedBy = id + '__help';
+		helpLabel = isFunction( help ) ? help( color ) : help;
+	}
```

This was disallowed by the linter.

### Review thread 7 (resolved)

#### chrisvanpatten on 2019-12-24T01:08:15Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r361036818
Path: packages/components/src/color-control/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * External dependencies
+ */
+import { isFunction } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { withInstanceId } from '@wordpress/compose';
+
+/**
+ * Internal dependencies
+ */
+import BaseControl from '../base-control';
+import ColorPicker from '../color-picker';
+
+const ColorControl = ( props ) => {
+	const { label, color, help, instanceId, className, onChange, disableAlpha } = props;
+	const id = `color-toggle-control-${ instanceId }`;
+
+	let describedBy, helpLabel;
+	if ( help ) {
+		describedBy = id + '__help';
+		helpLabel = isFunction( help ) ? help( color ) : help;
+	}
+	return (
+		<BaseControl
+			id={ id }
```

With the BaseControls, aren’t the `id`s used to construct the `for` attribute for label accessibility? Not sure if that factors in here at all, just wanted to note in case there’s something else that needs to be added to support that.

#### ryanwelcher on 2020-01-02T18:59:04Z

URL: https://github.com/WordPress/gutenberg/pull/19288#discussion_r362587810
Path: packages/components/src/color-control/index.js

```diff
@@ -0,0 +1,46 @@
+/**
+ * External dependencies
+ */
+import { isFunction } from 'lodash';
+import classnames from 'classnames';
+
+/**
+ * WordPress dependencies
+ */
+import { withInstanceId } from '@wordpress/compose';
+
+/**
+ * Internal dependencies
+ */
+import BaseControl from '../base-control';
+import ColorPicker from '../color-picker';
+
+const ColorControl = ( props ) => {
+	const { label, color, help, instanceId, className, onChange, disableAlpha } = props;
+	const id = `color-toggle-control-${ instanceId }`;
+
+	let describedBy, helpLabel;
+	if ( help ) {
+		describedBy = id + '__help';
+		helpLabel = isFunction( help ) ? help( color ) : help;
+	}
+	return (
+		<BaseControl
+			id={ id }
```

You're probably correct @chrisvanpatten however the linter tool fails for me if I don't provide an ID.

## Changed files

- docs/manifest.json (MODIFIED, +6/-0)
- packages/components/src/color-control/README.md (ADDED, +61/-0)
- packages/components/src/color-control/index.js (ADDED, +46/-0)
- packages/components/src/color-control/stories/index.js (ADDED, +40/-0)
- packages/components/src/index.js (MODIFIED, +1/-0)
