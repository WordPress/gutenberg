---
number: 41165
type: pull_request
state: MERGED
merged_at: 2022-05-23T09:57:22Z
url: https://github.com/WordPress/gutenberg/pull/41165
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Type] Automated Testing","[Package] Components"]
---

# PR #41165: CheckboxControl: Add unit tests

- URL: https://github.com/WordPress/gutenberg/pull/41165
- Author: walbo
- Created: 2022-05-19T18:36:25Z
- Updated: 2022-05-23T09:57:53Z
- Comments: 2 of 2
- Review threads: 5 of 5
- Changed files: 3 of 3
- Diff: ../pr-diffs/41165.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Add unit tests to `CheckboxControl`

## Why?
`CheckboxControl` is missing unit tests. Follow-up on https://github.com/WordPress/gutenberg/pull/40915#issuecomment-1123981296

## How?
Added unit tests.

## Testing Instructions
Run `npm run test-unit packages/components/src/checkbox-control` and confirm all tests passes.

## Issue comments

### walbo on 2022-05-20T16:20:32Z

URL: https://github.com/WordPress/gutenberg/pull/41165#issuecomment-1133092216

@ciampo Updated the PR to use `@testing-library/user-event`

### walbo on 2022-05-20T20:50:27Z

URL: https://github.com/WordPress/gutenberg/pull/41165#issuecomment-1133359878

Thanks for the great feedback @ciampo! Much appreciated. Have implemented the suggestions.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2022-05-20T16:40:14Z

URL: https://github.com/WordPress/gutenberg/pull/41165#discussion_r878342386
Path: packages/components/src/checkbox-control/test/index.tsx

```diff
@@ -0,0 +1,105 @@
+/**
+ * External dependencies
+ */
+import { render, screen } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import BaseCheckboxControl from '..';
+import type { CheckboxControlProps } from '../types';
+
+const getInput = () => screen.getByTestId( 'checkbox' );
```

Instead of applying a test id to the markup, let's write the tests from the point of view of our users — and therefore let's use roles whenever possible:

```suggestion
const getInput = () => screen.getByRole( 'checkbox' ) as HTMLInputElement;
```

### Review thread 2 (resolved)

#### ciampo on 2022-05-20T16:43:09Z

URL: https://github.com/WordPress/gutenberg/pull/41165#discussion_r878345287
Path: packages/components/src/checkbox-control/test/index.tsx

```diff
@@ -0,0 +1,105 @@
+/**
+ * External dependencies
+ */
+import { render, screen } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import BaseCheckboxControl from '..';
+import type { CheckboxControlProps } from '../types';
+
+const getInput = () => screen.getByTestId( 'checkbox' );
+
+const CheckboxControl = ( props: Omit< CheckboxControlProps, 'onChange' > ) => {
+	return (
+		<BaseCheckboxControl
+			onChange={ noop }
+			{ ...props }
+			data-testid="checkbox"
```

This line is not necessary anymore, after applying the suggestion from the previous comment

```suggestion
```

### Review thread 3 (resolved)

#### ciampo on 2022-05-20T16:43:23Z

URL: https://github.com/WordPress/gutenberg/pull/41165#discussion_r878345502
Path: packages/components/src/checkbox-control/test/index.tsx

```diff
@@ -0,0 +1,105 @@
+/**
+ * External dependencies
+ */
+import { render, screen } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import BaseCheckboxControl from '..';
+import type { CheckboxControlProps } from '../types';
+
+const getInput = () => screen.getByTestId( 'checkbox' );
+
+const CheckboxControl = ( props: Omit< CheckboxControlProps, 'onChange' > ) => {
+	return (
+		<BaseCheckboxControl
+			onChange={ noop }
+			{ ...props }
+			data-testid="checkbox"
+		/>
+	);
+};
+
+const ControlledCheckboxControl = ( { onChange }: CheckboxControlProps ) => {
+	const [ isChecked, setChecked ] = useState( false );
+	return (
+		<BaseCheckboxControl
+			checked={ isChecked }
+			onChange={ ( value ) => {
+				setChecked( value );
+				onChange( value );
+			} }
+			data-testid="checkbox"
```

Same

```suggestion
```

### Review thread 4 (resolved)

#### ciampo on 2022-05-20T16:52:54Z

URL: https://github.com/WordPress/gutenberg/pull/41165#discussion_r878353411
Path: packages/components/src/checkbox-control/test/index.tsx

```diff
@@ -0,0 +1,105 @@
+/**
+ * External dependencies
+ */
+import { render, screen } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import BaseCheckboxControl from '..';
+import type { CheckboxControlProps } from '../types';
+
+const getInput = () => screen.getByTestId( 'checkbox' );
+
+const CheckboxControl = ( props: Omit< CheckboxControlProps, 'onChange' > ) => {
+	return (
+		<BaseCheckboxControl
+			onChange={ noop }
+			{ ...props }
+			data-testid="checkbox"
+		/>
+	);
+};
+
+const ControlledCheckboxControl = ( { onChange }: CheckboxControlProps ) => {
+	const [ isChecked, setChecked ] = useState( false );
+	return (
+		<BaseCheckboxControl
+			checked={ isChecked }
+			onChange={ ( value ) => {
+				setChecked( value );
+				onChange( value );
+			} }
+			data-testid="checkbox"
+		/>
+	);
+};
+
+describe( 'CheckboxControl', () => {
+	describe( 'Basic rendering', () => {
+		it( 'should render', () => {
+			render( <CheckboxControl /> );
+			expect( getInput() ).not.toBeNull();
+		} );
+
+		it( 'should render an unchecked `checkbox` by default', () => {
+			render( <CheckboxControl /> );
+			expect( getInput() ).toHaveProperty( 'checked', false );
+		} );
+
+		it( 'should render an checked `checkbox` when `checked={ true }`', () => {
+			render( <CheckboxControl checked /> );
+			expect( getInput() ).toHaveProperty( 'checked', true );
+		} );
+
+		it( 'should render label', () => {
+			render( <CheckboxControl label="Hello" /> );
+
+			const label = screen.getByText( 'Hello' );
+			expect( label ).toBeTruthy();
+		} );
+
+		it( 'should render an indeterminate icon', () => {
+			const { container } = render( <CheckboxControl indeterminate /> );
+
+			const indeterminateIcon = container.getElementsByClassName(
+				'components-checkbox-control__indeterminate'
+			);
```

Let's avoid querying the DOM looking for classnames, as we'd be testing implementation details.

What matters to the user is that that the `input` element has an `indeterminate` property with a value of `true`

```tsx
		it( 'should render a checkbox in an indeterminate state', () => {
			render( <CheckboxControl indeterminate /> );
			expect( getInput() ).toHaveProperty( 'indeterminate', true );
		} );
 ```

If we wanted to check also for the icon, we could, for example, use snapshot diffing to target that icon specifically:

```tsx
		it( 'should render the indeterminate icon when in the indeterminate state', () => {
			const { container: containerDefault } = render(
				<CheckboxControl />
			);

			const { container: containerIndeterminate } = render(
				<CheckboxControl indeterminate />
			);

			// Expect the diff snapshot to be mostly about the indeterminate icon
			expect( containerDefault ).toMatchDiffSnapshot(
				containerIndeterminate
			);
		} );
```

### Review thread 5 (resolved)

#### ciampo on 2022-05-23T07:48:58Z

URL: https://github.com/WordPress/gutenberg/pull/41165#discussion_r879125062
Path: packages/components/src/checkbox-control/test/index.tsx

```diff
@@ -0,0 +1,109 @@
+/**
+ * External dependencies
+ */
+import { render, screen } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
+import { noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import BaseCheckboxControl from '..';
+import type { CheckboxControlProps } from '../types';
+
+const getInput = () => screen.getByRole( 'checkbox' ) as HTMLInputElement;
+
+const CheckboxControl = ( props: Omit< CheckboxControlProps, 'onChange' > ) => {
+	return <BaseCheckboxControl onChange={ noop } { ...props } />;
+};
+
+const ControlledCheckboxControl = ( { onChange }: CheckboxControlProps ) => {
+	const [ isChecked, setChecked ] = useState( false );
+	return (
+		<BaseCheckboxControl
+			checked={ isChecked }
+			onChange={ ( value ) => {
+				setChecked( value );
+				onChange( value );
+			} }
+		/>
+	);
+};
+
+describe( 'CheckboxControl', () => {
+	describe( 'Basic rendering', () => {
+		it( 'should render', () => {
+			render( <CheckboxControl /> );
+			expect( getInput() ).not.toBeNull();
+		} );
+
+		it( 'should render an unchecked `checkbox` by default', () => {
+			render( <CheckboxControl /> );
+			expect( getInput() ).toHaveProperty( 'checked', false );
+		} );
+
+		it( 'should render an checked `checkbox` when `checked={ true }`', () => {
+			render( <CheckboxControl checked /> );
+			expect( getInput() ).toHaveProperty( 'checked', true );
+		} );
+
+		it( 'should render label', () => {
+			render( <CheckboxControl label="Hello" /> );
+
+			const label = screen.getByText( 'Hello' );
+			expect( label ).toBeTruthy();
```

`toBeInTheDocument()` is a better way to [assert an element's presence](https://testing-library.com/docs/guide-disappearance/)

```suggestion
			expect( label ).toBeInTheDocument();
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/checkbox-control/test/__snapshots__/index.tsx.snap (ADDED, +42/-0)
- packages/components/src/checkbox-control/test/index.tsx (ADDED, +109/-0)
