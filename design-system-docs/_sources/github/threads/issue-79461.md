---
number: 79461
type: issue
state: CLOSED
url: https://github.com/WordPress/gutenberg/issues/79461
matched_labels: ["[Package] UI"]
all_labels: ["[Type] Bug","[Status] In Progress","[Feature] UI Components","[Package] UI"]
---

# Issue #79461: `@wordpress/ui` IconButton hardcodes tooltip delay to 0

- URL: https://github.com/WordPress/gutenberg/issues/79461
- Author: shaunandrews
- Created: 2026-06-23T23:04:41Z
- Updated: 2026-06-25T09:53:10Z
- Comments: 2 of 2

## Body

## What problem does this address?

`@wordpress/ui`’s `IconButton` wraps its tooltip in a local `Tooltip.Provider` with `delay={ 0 }`:

https://github.com/WordPress/gutenberg/blob/trunk/packages/ui/src/icon-button/icon-button.tsx#L33

This makes every `IconButton` tooltip appear immediately on hover. In dense app UIs, immediate tooltips can feel noisy and get in the way when users are moving across controls or clicking buttons.

`Tooltip.Provider` already supports delayed and grouped tooltip behavior through Base UI. The default delay provides a better baseline and still allows adjacent tooltips to show quickly once a tooltip is active.

## What is your proposed solution?

Remove the hardcoded `delay={ 0 }` from `IconButton`’s internal `Tooltip.Provider`, so it uses the tooltip provider’s default behavior.

Alternatively, if immediate tooltips are intentional for some consumers, expose a prop for configuring the tooltip delay while keeping a nonzero default.

## Issue comments

### aduth on 2026-06-24T16:32:32Z

URL: https://github.com/WordPress/gutenberg/issues/79461#issuecomment-4791510454

I wouldn't necessarily call this a bug, as it was an intentional decision. Though in practice, I agree it tends to be too distracting and we should remove the delay override. I believe @fcoveram @mirka @ciampo have each raised this independently at some point in the past as well, in favor of returning to the default delay.

One important caveat is that we should still make sure the tooltip shows instantly when navigating with keyboard. That's likely the default behavior if we remove the delay override, but we should double-check.

### fcoveram on 2026-06-25T09:06:19Z

URL: https://github.com/WordPress/gutenberg/issues/79461#issuecomment-4797585245

As per my UX approach: add a default and consistent delay to prevent distraction during hover. With that in mind, I agree that it should be consistent but I cannot speak much for the implementation. If this change simplifies it and make it consistent, then +1 to it.

