---
number: 15968
type: pull_request
state: MERGED
merged_at: 2019-08-12T12:44:26Z
url: https://github.com/WordPress/gutenberg/pull/15968
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #15968: Components: Refactor DropdownMenu to stop using unstable props

- URL: https://github.com/WordPress/gutenberg/pull/15968
- Author: gziolo
- Created: 2019-06-03T15:10:32Z
- Updated: 2019-08-12T12:44:30Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 9 of 9
- Diff: ../pr-diffs/15968.diff

## Body

## Description
Fixes #15960.

There were 4 unstable props introduced temporarily in #14843:
https://github.com/WordPress/gutenberg/blob/c2768eccc3ef53be437ad90fece0c43c8a843ce3/packages/components/src/dropdown-menu/index.js#L27-L30

They were added for backward compatibility. This PR further explores how to approach it differently.

Proposed changes:
- `__unstableLabelPosition` gets replaced with `position` property in `popoverProps` prop.
- `__unstablePopoverClassName` got implemented as `className` property in `popoverProps` props as it seems to be essential for styling of the popover.
- `__unstableToggleClassName` and `__unstableMenuClassName` got remove as they can be expressed as a combination of `className` property of `popoverProps` prop and default classes the same UI elements provide. This might be considered as a breaking change but I don't expect it would influence plugins and sites given how specific edge case it is.

This PR introduces new props for `DropdownMenu` as suggested by @jorgefilipecosta and @youknowriad:

- Added a new `popoverProps` prop to the `DropdownMenu` component which allows passing props directly to the nested `Popover` component.
- Added a new `toggleProps` prop to the `DropdownMenu` component which allows passing props directly to the nested `IconButton` component.
- Added a new `menuProps` prop to the `DropdownMenu` component which allows passing props directly to the nested `NavigableMenu` component.

There are also 2 deprecations introduced:

 `menuLabel` prop in `DropdownComponent` has been deprecated. Consider using `menuProps` object and its `aria-label` property instead. 
- `position` prop in `DropdownComponent` has been deprecated. Consider using `popoverProps` object and its `position` property instead.

## How has this been tested?
There should be no visual changes in the block's More Options menu and in the More Menu.

![Screen Shot 2019-06-03 at 17 12 14](https://user-images.githubusercontent.com/699132/58812757-c8f4b080-8622-11e9-9988-c85159b5f36c.png)
![Screen Shot 2019-07-26 at 16 28 55](https://user-images.githubusercontent.com/699132/61958909-8f0cae80-afc2-11e9-8316-ef8445ae6bde.png)
![Screen Shot 2019-07-26 at 16 28 58](https://user-images.githubusercontent.com/699132/61958910-8fa54500-afc2-11e9-98d6-3f618ac5faf0.png)

![Screen Shot 2019-06-03 at 17 10 48](https://user-images.githubusercontent.com/699132/58812650-95198b00-8622-11e9-9d36-b15d7bdb610f.png)
![Screen Shot 2019-06-03 at 17 10 56](https://user-images.githubusercontent.com/699132/58812651-95198b00-8622-11e9-9ec3-470b6867772c.png)
![Screen Shot 2019-07-26 at 16 29 11](https://user-images.githubusercontent.com/699132/61958920-9338cc00-afc2-11e9-9b47-d0553c1a875f.png)

## Issue comments

### gziolo on 2019-07-26T14:34:47Z

URL: https://github.com/WordPress/gutenberg/pull/15968#issuecomment-515478404

@youknowriad and @jorgefilipecosta - your feedback was addressed and this is ready for final review.

## Review threads

### Review thread 1

#### youknowriad on 2019-06-14T09:15:51Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r293725884
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

I'm not sure about this change? Can you clarify? It seems that the position prop is meant to be used for something else?

#### youknowriad on 2019-06-14T09:18:25Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r293726857
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

If I'd make it a stable api,  I'd probably name it `tooltipPosition`

#### gziolo on 2019-06-14T10:09:04Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r293745450
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

Yep, but in general, it aligns where you see the popover with where you would expect to see the tooltip :)

I can add `tooltipPosition` as well but maybe we should introduce `toggleProps` and `contentPropos` instead so you could have more control over them. The only issue I see here is that it's not easy to pass an object as a prop because it will cause re-renders every time `DropdownMenu` re-renders.

#### youknowriad on 2019-06-24T10:10:12Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r296648969
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

I don't think the props align, I can see valid use-cases for showing a dropdown under the toggle but the tooltip on top of it. I don't have strong opinions on whether it should be a `toggleProps` prop or just a position one.

#### gziolo on 2019-06-24T11:21:57Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r296674721
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

@jorgefilipecosta added `popoverProps` prop described as an object for `Dropdown` components in #14867. There is a bit different handling for `Popover` component where all remaining props are passed down using rest operator:

https://github.com/WordPress/gutenberg/blob/master/packages/components/src/popover/index.js#L267

We probably should optimize APIs to go in one direction moving forward. In this particular case, it feels like using `toggleProps` would solve the issue we have and keep it more flexible for future use cases.

#### jorgefilipecosta on 2019-07-02T13:39:01Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r299487753
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

> We probably should optimize APIs to go in one direction moving forward. In this particular case, it feels like using toggleProps would solve the issue we have and keep it more flexible for future use cases.

I think using a toggleProps object as referred seems like a good option.

#### gziolo on 2019-07-26T14:28:00Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r307767881
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -67,7 +64,7 @@ function DropdownMenu( {
 						aria-haspopup="true"
 						aria-expanded={ isOpen }
 						label={ label }
-						labelPosition={ __unstableLabelPosition }
+						labelPosition={ position }
```

I introduced 3 new props based on the prior work with `Dropdown` component as implemented in #14867:
- `popoverProps`
- `toggleProps`
- `menuProps`

This allows us to deprecate two props which can be set using this pass-through objects and further simplify public API. For backward compatibility, they are still supported but they will warn on the JS console. I assumed they aren't used widely outside of Gutenberg.

### Review thread 2 (resolved)

#### jorgefilipecosta on 2019-08-09T09:27:19Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r312399334
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -16,20 +17,47 @@ import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
+function mergeProps( props = {}, defaultProps = {} ) {
```

I guess in the future we will have other cases for this function. When that happens we should consider exposing it as util.
Minor: maybe we should reorder the parameters to be `function mergeProps( defaultProps = {}, props = {} )` to work in the same way Object.assign.

#### gziolo on 2019-08-09T13:13:57Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r312472758
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -16,20 +17,47 @@ import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
+function mergeProps( props = {}, defaultProps = {} ) {
```

`Object.assign`, `lodash.assign` or `jQuery.extend` - they mutate the first param. This helper is pure in that regard. I can swap params if you think that it will make it easier to read. and change the logic in a way where `defaultProps` is used as a base for mutations.

Yes, it feels like we have other places where we can reuse the same pattern.

#### gziolo on 2019-08-12T12:33:23Z

URL: https://github.com/WordPress/gutenberg/pull/15968#discussion_r312900848
Path: packages/components/src/dropdown-menu/index.js

```diff
@@ -16,20 +17,47 @@ import IconButton from '../icon-button';
 import Dropdown from '../dropdown';
 import { NavigableMenu } from '../navigable-container';
 
+function mergeProps( props = {}, defaultProps = {} ) {
```

Updated in bb25919abca92e684925cdc7946897c5093fb4c0 to follow the order of params you recommended. I didn't enable mutations, we can revisit it later when we start using the same helper on a larger scale.

## Changed files

- packages/block-editor/src/components/block-settings-menu/index.js (MODIFIED, +6/-4)
- packages/block-editor/src/components/block-settings-menu/style.scss (MODIFIED, +4/-4)
- packages/block-editor/src/components/rich-text/format-toolbar/index.js (MODIFIED, +5/-1)
- packages/components/CHANGELOG.md (MODIFIED, +9/-1)
- packages/components/src/dropdown-menu/README.md (MODIFIED, +25/-16)
- packages/components/src/dropdown-menu/index.js (MODIFIED, +50/-13)
- packages/e2e-tests/specs/block-grouping.test.js (MODIFIED, +1/-1)
- packages/edit-post/src/components/header/more-menu/index.js (MODIFIED, +10/-3)
- packages/edit-post/src/components/header/more-menu/test/__snapshots__/index.js.snap (MODIFIED, +17/-5)
