---
number: 57398
type: pull_request
state: MERGED
merged_at: 2024-01-02T17:55:53Z
url: https://github.com/WordPress/gutenberg/pull/57398
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #57398: Add `compact` size variant to InputControl-based components

- URL: https://github.com/WordPress/gutenberg/pull/57398
- Author: mirka
- Created: 2023-12-27T14:07:36Z
- Updated: 2024-01-02T17:56:17Z
- Comments: 0 of 0
- Review threads: 1 of 1
- Changed files: 5 of 5
- Diff: ../pr-diffs/57398.diff

## Body

Part of #46734
Prerequisite for #56388

## What?

Adds an explicit `compact` size variant to InputControl and all the components based on those styles. This variant has 32px height, in line with the new sizing scheme.

### Affected components

- InputControl
- NumberControl
- UnitControl
- SelectControl
- TreeSelect

## Why?

We will sometimes need a compact variant for a smaller footprint.

## How?

This size variant is actually identical with the current `default` size variants. Since they will eventually be superseded by the new 40px default, we need to keep an explicit `compact` variant.

## Testing Instructions

See the Storybook stories for the affected components.

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### ciampo on 2024-01-02T17:11:00Z

URL: https://github.com/WordPress/gutenberg/pull/57398#discussion_r1439648817
Path: packages/components/src/select-control/styles/select-control-styles.ts

```diff
@@ -30,27 +31,6 @@ const disabledStyles = ( { disabled }: SelectProps ) => {
 	} );
 };
 
-const fontSizeStyles = ( { selectSize = 'default' }: SelectProps ) => {
-	const sizes = {
-		default: '13px',
-		small: '11px',
-		'__unstable-large': '13px',
-	};
-
-	const fontSize = sizes[ selectSize ];
-	const fontSizeMobile = '16px';
-
-	if ( ! fontSize ) return '';
-
-	return css`
-		font-size: ${ fontSizeMobile };
-
-		@media ( min-width: 600px ) {
-			font-size: ${ fontSize };
-		}
-	`;
-};
```

Nice 🧹

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/input-control/styles/input-control-styles.tsx (MODIFIED, +10/-8)
- packages/components/src/input-control/types.ts (MODIFIED, +1/-1)
- packages/components/src/select-control/styles/select-control-styles.ts (MODIFIED, +10/-28)
- packages/components/src/unit-control/index.tsx (MODIFIED, +1/-1)
