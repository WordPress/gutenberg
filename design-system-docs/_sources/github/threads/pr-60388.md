---
number: 60388
type: pull_request
state: MERGED
merged_at: 2024-04-03T12:52:52Z
url: https://github.com/WordPress/gutenberg/pull/60388
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components","First-time Contributor"]
---

# PR #60388: ProgressBar: moved width to css var for perf

- URL: https://github.com/WordPress/gutenberg/pull/60388
- Author: DaniGuardiola
- Created: 2024-04-02T16:19:00Z
- Updated: 2024-04-03T13:42:09Z
- Comments: 5 of 5
- Review threads: 6 of 6
- Changed files: 4 of 4
- Diff: ../pr-diffs/60388.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

Moved width styles for the bar (depends on `value` prop) out of emotion for performance and replaced with CSS variable.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

Dynamic changes in emotion (CSS-in-JS) are slow.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

As described.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

Manually in Storybook (ProgressBar component, change `value`. There is a unit test that checks the rendered CSS too.

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

n/a

## Screenshots or screencast <!-- if applicable -->

n/a

## Issue comments

### github-actions on 2024-04-02T16:19:15Z

URL: https://github.com/WordPress/gutenberg/pull/60388#issuecomment-2032500331

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: DaniGuardiola <daniguardiola@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-04-02T16:19:25Z

URL: https://github.com/WordPress/gutenberg/pull/60388#issuecomment-2032500630

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @DaniGuardiola! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### DaniGuardiola on 2024-04-03T10:33:52Z

URL: https://github.com/WordPress/gutenberg/pull/60388#issuecomment-2034199693

@tyxla

> Thank you for working on this and congrats on your first Gutenberg PR 🙌

Thank you! :)

> I wonder if there's a good reason to not pass width through the style prop directly?

Yes, a few:

- Keeping all styles in the same place.
- Inline styles take precedence over almost anything, which greatly hurts extensibility / overridability.
- Injecting JS-derived values as variables and then using those variables from CSS is an easier mental frame than mixing JS with styles directly.
- Having the variable available like this enables future changes in a very easy way.

E.g. imagine we switch to an svg stroke here, which might actually make sense since animating that is more performant than animating width - then we'd need to do some `calc()`ulations to turn the percentage into the right value for `stroke-dasharray` or `stroke-offset`, and this can be achieved without changing any of the JavaScript logic. It's a useful separation that becomes more and more useful the more consistently that it is applied.

Ariakit does this for most of the public elements that it renders. Since these components are an extension of Ariakit components, there's also a point about having a consistent pattern.

All of this said, I'd like to know if you have any arguments for using inline styles for this (other than it being slightly shorter, which is the only one I can think of at the moment)?

> I was also curious if you were able to observe any measurable performance improvements.

I wondered about this too and tried to manually test it, but it's not really noticeable in the Storybook. I was pairing with Lena and she told me that this kind of dynamic emotion changes are not performant in general, which makes a lot of sense to me.

> Finally, it may be a good idea to add a CHANGELOG entry, since it's a neat little enhancement.

That was my last commit yesterday, but I forgot to push it :P

### tyxla on 2024-04-03T10:56:06Z

URL: https://github.com/WordPress/gutenberg/pull/60388#issuecomment-2034247144

Thanks for elaborating @DaniGuardiola! I think that rationale is in total favor of keeping the CSS var 👍

### mirka on 2024-04-03T13:42:08Z

URL: https://github.com/WordPress/gutenberg/pull/60388#issuecomment-2034651719

For context, I'll add that this change was not motivated by any actual measured performance issue, but more to adhere to the best practices recommended by Emotion itself ([Use the `style` prop for dynamic styles](https://emotion.sh/docs/best-practices#use-the-style-prop-for-dynamic-styles) and [CSS variables with `style`](https://emotion.sh/docs/best-practices#advanced-css-variables-with-style)). These changes will also help us move off of Emotion, as they are closer to more standard ways of styling.

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2024-04-03T09:25:14Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549330881
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -43,3 +47,12 @@ function UnforwardedProgressBar(
 export const ProgressBar = forwardRef( UnforwardedProgressBar );
 
 export default ProgressBar;
+
+type Test1 = { asd: 123 };
+type Test2 = { kljk: 121233 };
+
+type Compute< T > = { [ K in keyof T ]: T[ K ] };
+
+type Test = Compute< Test1 & Test2 >;
+//   ^?
+declare const test: Test;
```

Should we clean this up before shipping?

#### DaniGuardiola on 2024-04-03T10:35:23Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549445441
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -43,3 +47,12 @@ function UnforwardedProgressBar(
 export const ProgressBar = forwardRef( UnforwardedProgressBar );
 
 export default ProgressBar;
+
+type Test1 = { asd: 123 };
+type Test2 = { kljk: 121233 };
+
+type Compute< T > = { [ K in keyof T ]: T[ K ] };
+
+type Test = Compute< Test1 & Test2 >;
+//   ^?
+declare const test: Test;
```

Oops! I was demoing [twoslash](https://github.com/twoslashes/twoslash) and some TypeScript tricks for Lena, and forgot to clean up. My bad!

### Review thread 2 (resolved)

#### tyxla on 2024-04-03T09:44:11Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549368540
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -26,8 +26,12 @@ function UnforwardedProgressBar(
 	return (
 		<ProgressBarStyled.Track className={ className }>
 			<ProgressBarStyled.Indicator
+				style={
+					{
+						'--indicator-width': `${ value }%`,
+					} as CSSProperties
+				}
```

Do we even need a CSS var? Can't we just set the `width` directly?

```suggestion
				style={ {
					width: `${ value }%`,
				} }
```

That would allow us to avoid the typecasting too.

#### DaniGuardiola on 2024-04-03T10:38:22Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549452125
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -26,8 +26,12 @@ function UnforwardedProgressBar(
 	return (
 		<ProgressBarStyled.Track className={ className }>
 			<ProgressBarStyled.Indicator
+				style={
+					{
+						'--indicator-width': `${ value }%`,
+					} as CSSProperties
+				}
```

See https://github.com/WordPress/gutenberg/pull/60388#issuecomment-2034199693 regarding this.

The typecasting is definitely unfortunate, and something that people have tried to fix in the upstream types - hopefully it'll be solved at some point. I don't think that a minor typing limitation should prevent us from using a core CSS feature though :P

#### tyxla on 2024-04-03T10:48:08Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549467282
Path: packages/components/src/progress-bar/index.tsx

```diff
@@ -26,8 +26,12 @@ function UnforwardedProgressBar(
 	return (
 		<ProgressBarStyled.Track className={ className }>
 			<ProgressBarStyled.Indicator
+				style={
+					{
+						'--indicator-width': `${ value }%`,
+					} as CSSProperties
+				}
```

It's totally fine to keep it. I was nitpicking 😉

### Review thread 3 (resolved)

#### tyxla on 2024-04-03T09:45:49Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549371468
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -70,7 +69,7 @@ export const Indicator = styled.div< {
 					width: `${ INDETERMINATE_TRACK_WIDTH }%`,
 			  } )
 			: css( {
-					width: `${ value }%`,
+					width: 'var(--indicator-width)',
```

Can be removed if we end up setting `width` directly through the `style` prop of the component.

```suggestion
```

#### DaniGuardiola on 2024-04-03T10:38:30Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549452304
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -70,7 +69,7 @@ export const Indicator = styled.div< {
 					width: `${ INDETERMINATE_TRACK_WIDTH }%`,
 			  } )
 			: css( {
-					width: `${ value }%`,
+					width: 'var(--indicator-width)',
```

If we do, it could yeah.

### Review thread 4

#### tyxla on 2024-04-03T09:48:57Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549377981
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

We will be able to revert this if we use the prop directly.

#### DaniGuardiola on 2024-04-03T10:38:41Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549452529
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

Correct.

#### DaniGuardiola on 2024-04-03T10:40:15Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549455328
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

I'd add that I don't think this test should exist, as it's testing implementation details and not necessarily observable behavior. Instead, this should be tested through visual snapshot testing or similar IMO.

#### tyxla on 2024-04-03T10:47:45Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549466885
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

Hm, I'd disagree this is testing an implementation detail. The [definition](https://kentcdodds.com/blog/testing-implementation-details#so-what-are-implementation-detailsthen) by Kent C. Dodds says:

> Implementation details are things which users of your code will not typically use, see, or even know about.

I'd argue this isn't the case for the width of the progress bar indicator - the indicator is actually defined by its width for the user, and it's definitely the primary thing the user will see from that component.

That being said, I still think a variation of this test is necessary, but it doesn't necessarily have to test a CSS var, but rather, the width of the element for example.

#### DaniGuardiola on 2024-04-03T12:15:45Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549618397
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

The width is resolved by the web rendering engine, which jsdom doesn't have, so it's not possible to test this in the context of a unit test. Leaving the (potentially very interesting!) philosophical debate about what qualifies as an implementation detail, in this specific case I'd highlight the fact that what the test is doing (in the previous and current versions) is test React/emotion for the most part, and whether when you set a value, the value is set. For that alone I don't see much value.

Regarding implementation details (and with no intention to prolong this more than necessary, just for fun :P), I'd argue that what's observable here is what the user sees or whether pixels are rendered a certain way. My understanding of testing observable behavior is that, if you change the implementation, the test doesn't need to be modified. In this case, if we were to switch to an SVG path based implementation, or maybe a CSS transform one, we'd need to change the test since it directly depends on the way it is implemented.

You can definitely test the observable part here though, through visual snapshot testing, or even with a headless browser that does implement a rendering engine (cypress, puppeteer, etc).

#### tyxla on 2024-04-03T12:25:33Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549632225
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

I think we're more or less on the same page about what an implementation detail is, but it's also about compromise in favor of keeping as few e2e tests as possible, because they come at their own added cost. Is an e2e test more useful in this case, being less about the actual implementation and more about what the user sees - absolutely. Is it more expensive to have and maintain - yes. Thus the compromise. 

On another note, visual regression testing is definitely something we've been aiming to have for some scenarios, and it can be handy as we iterate on components. I know @mirka has been experimenting with that before.

#### mirka on 2024-04-03T13:31:34Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549754821
Path: packages/components/src/progress-bar/test/index.tsx

```diff
@@ -56,7 +56,7 @@ describe( 'ProgressBar', () => {
 		const indicator = container.firstChild?.firstChild;
 
 		expect( indicator ).toHaveStyle( {
-			width: '55%',
+			'--indicator-width': '55%',
```

While testing styles in Jest are indeed a lower-cost compromise over a full vizreg test, it also isn't cost-free in the sense that it can add a maintenance cost that can be arguably disproportionate to the low confidence that it adds — similar to a snapshot test.

In this particular case I don't think it's likely that someone would break the style and land that PR without anyone noticing (at least for the happy path), so to be honest I don't care that much about whether or how we do the test. If it does ever break, we can perhaps alter the strategy based on how it broke.

### Review thread 5 (resolved)

#### tyxla on 2024-04-03T10:50:36Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549470472
Path: packages/components/CHANGELOG.md

```diff
@@ -2,6 +2,10 @@
 
 ## Unreleased
 
+### Enhancement
+
+-   `ProgressBar`: Improved CSS performance by moving the bar width styles away from emotion and into a CSS variable ([#60388](https://github.com/WordPress/gutenberg/pull/60388)).
```

Optional nit: the entry could perhaps be shorter:

```suggestion
-   `ProgressBar`: Move the indicator width styles from emotion to a CSS variable ([#60388](https://github.com/WordPress/gutenberg/pull/60388)).
```

### Review thread 6

#### tyxla on 2024-04-03T10:55:00Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549476994
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -70,7 +69,7 @@ export const Indicator = styled.div< {
 					width: `${ INDETERMINATE_TRACK_WIDTH }%`,
 			  } )
 			: css( {
-					width: `${ value }%`,
+					width: 'var(--indicator-width)',
```

Do we need to prefix this var somehow so it doesn't accidentally bump into another one? Sometimes we use `--wp-admin`, sometimes we use `--wp-components`, and sometimes `--wp`, so perhaps it can be something like `--wp-progressbar-indicator-width` or something? @mirka thoughts?

#### DaniGuardiola on 2024-04-03T12:19:57Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549624096
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -70,7 +69,7 @@ export const Indicator = styled.div< {
 					width: `${ INDETERMINATE_TRACK_WIDTH }%`,
 			  } )
 			: css( {
-					width: `${ value }%`,
+					width: 'var(--indicator-width)',
```

We discussed this during our pairing session and settled on this shorter one. I think it's very unlikely to collide with other css vars, and it's internal to a component that doesn't have any slots either. The cool thing about CSS vars is that they are local to the element tree where they are declared, so collisions are almost never a concern unless the tree contains external, user-provided elements.

#### tyxla on 2024-04-03T12:26:21Z

URL: https://github.com/WordPress/gutenberg/pull/60388#discussion_r1549633819
Path: packages/components/src/progress-bar/styles.ts

```diff
@@ -70,7 +69,7 @@ export const Indicator = styled.div< {
 					width: `${ INDETERMINATE_TRACK_WIDTH }%`,
 			  } )
 			: css( {
-					width: `${ value }%`,
+					width: 'var(--indicator-width)',
```

Sounds good - let's keep it as-is 👍

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +8/-4)
- packages/components/src/progress-bar/index.tsx (MODIFIED, +6/-2)
- packages/components/src/progress-bar/styles.ts (MODIFIED, +2/-3)
- packages/components/src/progress-bar/test/index.tsx (MODIFIED, +1/-1)
