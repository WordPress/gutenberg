---
number: 55962
type: pull_request
state: MERGED
merged_at: 2023-11-09T16:48:30Z
url: https://github.com/WordPress/gutenberg/pull/55962
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #55962: DropdownMenuV2: prevent default on Escape key presses

- URL: https://github.com/WordPress/gutenberg/pull/55962
- Author: ciampo
- Created: 2023-11-08T10:43:32Z
- Updated: 2023-11-09T16:48:53Z
- Comments: 1 of 1
- Review threads: 3 of 3
- Changed files: 6 of 6
- Diff: ../pr-diffs/55962.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->

This PR changes the default behaviour of the new experimental `DropdownMenu` component so that the keyboard event related to pressing the `Escape` key is default prevented.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->

As pointed out in https://github.com/WordPress/gutenberg/pull/55625#issuecomment-1786703818, pressing the `Escape` key to close the dropdown menu can have unintended consequences (like, for example, causing a browser on MacOs to exit full screen mode).

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

Instead of using a boolean for the `hideOnEscape` prop, we can use a callback function that accepts the `event` object. We can then call `preventDefault()` on it.

Also, note that this PR only changes the default behaviour of the component. Folks wishing to have the keyboard event bubbling up can simply pass a different value for the prop.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

1. Make sure that, when pressing the Escape key, the dropdown menu still closes correctly
2. Make sure that pressing the event associated to the Escape key pressed when closing the dropdown menu is default prevented:
    - With changes from this PR downloaded on a local machine, run `npm run storybook:dev`
    - Visit the dropdown menu v2 storybook example, and open the example in a new window (ie. [likely this URL](http://localhost:50240/iframe.html?id=components-experimental-dropdownmenu-v2-ariakit--default&viewMode=story))
    - Open the browser dev tools, and in the console type `document.addEventListener('keydown', (e) => console.log(e.key, e.defaultPrevented))`
    - Interact with the dropdown menu. When pressing the Escape key with an open menu, the console output should be `Escape true`. When pressing the escape key when the menu is closed, the console output should be `Escape false`.
3. As an optional test, load the Menu's storybook example on MacOS safari and make the Safari window full screen. Pressing the Escape key while the menu is open should not cause Safari to exit full screen.

## Issue comments

### github-actions on 2023-11-09T15:40:47Z

URL: https://github.com/WordPress/gutenberg/pull/55962#issuecomment-1804070615

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 897f3fc9ad3dd0815e5245919064622771beac9d.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/6813741126
📝  Reported issues:
- #53524 in `/test/e2e/specs/editor/various/multi-block-selection.spec.js`

## Review threads

### Review thread 1 (resolved)

#### youknowriad on 2023-11-08T11:18:40Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1386463249
Path: packages/components/src/dropdown-menu-v2-ariakit/README.md

```diff
@@ -118,7 +118,7 @@ The skidding of the popover along the anchor element. Can be set to negative val
 Determines whether the menu popover will be hidden when the user presses the Escape key.
```

I guess I always lean to the safe side but I'm not sure why we even support a function for this prop (on our API, not aria kit one)

#### ciampo on 2023-11-09T11:11:38Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1387852712
Path: packages/components/src/dropdown-menu-v2-ariakit/README.md

```diff
@@ -118,7 +118,7 @@ The skidding of the popover along the anchor element. Can be set to negative val
 Determines whether the menu popover will be hidden when the user presses the Escape key.
```

I'm ok removing this prop from the component's public APIs for now. We can always add it back later if consumers of the component have such a necessity

#### ciampo on 2023-11-09T15:15:10Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1388154485
Path: packages/components/src/dropdown-menu-v2-ariakit/README.md

```diff
@@ -118,7 +118,7 @@ The skidding of the popover along the anchor element. Can be set to negative val
 Determines whether the menu popover will be hidden when the user presses the Escape key.
```

https://github.com/WordPress/gutenberg/pull/55962/commits/43fb40f2dc6bc64c789610abd3cbac11f713211a

### Review thread 2

#### youknowriad on 2023-11-08T11:20:05Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1386464700
Path: packages/components/src/dropdown-menu-v2-ariakit/index.tsx

```diff
@@ -181,7 +181,13 @@ const UnconnectedDropdownMenu = (
 		children,
 		shift,
 		modal = true,
-		hideOnEscape = true,
+		hideOnEscape = ( event ) => {
```

IMO, since we're just passing down this prop and it's ariakit that is handling the ESCAPE key, I think the ideal fix would have to be done in ariakit directly

#### youknowriad on 2023-11-08T11:20:22Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1386464991
Path: packages/components/src/dropdown-menu-v2-ariakit/index.tsx

```diff
@@ -181,7 +181,13 @@ const UnconnectedDropdownMenu = (
 		children,
 		shift,
 		modal = true,
-		hideOnEscape = true,
+		hideOnEscape = ( event ) => {
```

I'm fine with a fix here for now, but maybe this could benefit more users.

#### ciampo on 2023-11-09T11:10:45Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1387851260
Path: packages/components/src/dropdown-menu-v2-ariakit/index.tsx

```diff
@@ -181,7 +181,13 @@ const UnconnectedDropdownMenu = (
 		children,
 		shift,
 		modal = true,
-		hideOnEscape = true,
+		hideOnEscape = ( event ) => {
```

@diegohaz  maybe you can share some perspective as to why this is not the case in `ariakit`?

#### diegohaz on 2023-11-09T13:35:58Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1388010832
Path: packages/components/src/dropdown-menu-v2-ariakit/index.tsx

```diff
@@ -181,7 +181,13 @@ const UnconnectedDropdownMenu = (
 		children,
 		shift,
 		modal = true,
-		hideOnEscape = true,
+		hideOnEscape = ( event ) => {
```

I can't remember any discussion about `hideOnEscape` and fullscreen mode in particular, so there's likely no specific reason. It seems it just hasn't been considered yet.

But there's an issue with fullscreen and portal: https://github.com/ariakit/ariakit/issues/865

### Review thread 3

#### youknowriad on 2023-11-09T15:15:33Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1388154996
Path: packages/components/src/dropdown-menu-v2-ariakit/index.tsx

```diff
@@ -248,6 +248,29 @@ const UnconnectedDropdownMenu = (
 		);
 	}
 
+	const hideOnEscape = useCallback(
+		( event: React.KeyboardEvent< Element > ) => {
+			// Pressing Escape can cause unexpected consequences (ie. exiting
+			// full screen mode on MacOs, close parent modals...).
+			event.preventDefault();
+			event.stopPropagation();
```

Is there any particular reason to stop propagation?

#### ciampo on 2023-11-09T15:32:58Z

URL: https://github.com/WordPress/gutenberg/pull/55962#discussion_r1388179728
Path: packages/components/src/dropdown-menu-v2-ariakit/index.tsx

```diff
@@ -248,6 +248,29 @@ const UnconnectedDropdownMenu = (
 		);
 	}
 
+	const hideOnEscape = useCallback(
+		( event: React.KeyboardEvent< Element > ) => {
+			// Pressing Escape can cause unexpected consequences (ie. exiting
+			// full screen mode on MacOs, close parent modals...).
+			event.preventDefault();
+			event.stopPropagation();
```

Without it, I thought that, when a DropdownMenu is rendered inside a Modal, pressing Escape would close the DropdownMenu and the parent modal.

That was my impression because, when talking to Diego, I seem to remember him suggesting [this change](https://github.com/WordPress/gutenberg/blob/1b5f4ef781b83d27918a21865302b914d2eb4281/packages/components/src/dropdown-menu-v2-ariakit/stories/index.story.tsx#L497-L500) specifically for when a DropdownMenu is rendered inside a Modal.

But I just tested without it, and things seem to work well.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/dropdown-menu-v2-ariakit/README.md (MODIFIED, +0/-7)
- packages/components/src/dropdown-menu-v2-ariakit/index.tsx (MODIFIED, +24/-7)
- packages/components/src/dropdown-menu-v2-ariakit/stories/index.story.tsx (MODIFIED, +1/-4)
- packages/components/src/dropdown-menu-v2-ariakit/test/index.tsx (MODIFIED, +0/-26)
- packages/components/src/dropdown-menu-v2-ariakit/types.ts (MODIFIED, +1/-1)
