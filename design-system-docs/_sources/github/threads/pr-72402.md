---
number: 72402
type: pull_request
state: MERGED
merged_at: 2026-06-10T00:43:10Z
url: https://github.com/WordPress/gutenberg/pull/72402
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components","[Package] Block editor","First-time Contributor"]
---

# PR #72402: `ColorPalette`: don't render when custom colors disabled and no colors passed

- URL: https://github.com/WordPress/gutenberg/pull/72402
- Author: n8finch
- Created: 2025-10-16T18:06:07Z
- Updated: 2026-06-10T00:43:45Z
- Comments: 10 of 10
- Review threads: 10 of 10
- Changed files: 4 of 4
- Diff: ../pr-diffs/72402.diff

## Body

First PR for Gutenberg, woohoo!

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Closes https://github.com/WordPress/gutenberg/issues/59567

This PR fixes the `disableCustomColors` prop that was not being set in the `BorderBoxControl` element.

## Why?
If the custom color setting is disabled in the `theme.json` the `BorderBoxControl` element was not picking up on that setting. 

The setting was respected when picking text and background colors though, as these use the `ColorPanelDropdown` element.

## How?
This PR adds a local method to the `BorderBoxControl` element and checks for the theme setting in a similar manner that other settings are being checked and set.

I've tried to make it as close as possible to the implementation in the `ColorPanelDropdown` element, see related code:
- getting the value: https://github.com/WordPress/gutenberg/blob/trunk/packages/block-editor/src/components/global-styles/color-panel.js#L332
- passing the value: https://github.com/WordPress/gutenberg/blob/trunk/packages/block-editor/src/components/global-styles/color-panel.js#L742

## Testing Instructions
1. Make sure you're working with a custom `theme.json` file, and that you have the `settings.color.custom` property set to `false` (see screenshots)
2. Open the Outline for the block editor
3. Insert or select a Group Block
4. Open the right sidebar Inspector
5. Scroll to the "Border & Shadow" panel section
6. Click on the Border button trigger (see screenshots)
7. You _should not_ see the custom color picker
8. Change the value in the `theme.json` to be `true` or remove it completely, reload the page, and repeat steps 3 to 6, at this point _you should see_ the custom color picker.

### Testing Instructions for Keyboard
1. Make sure you're working with a custom `theme.json` file, and that you have the `settings.color.custom` property set to `false` (see screenshots)
2. Tab to select the group block in the block editor or Outline
3. Open the Inspector with `CMD + Shift + ,` on a Mac or `Ctrl + Shift + ,` on a PC.
4. Tab until you get to the Border button trigger labeled `Border color and style picker`
5. Hit Enter or Spacebar on your keyboard
6. You should not be able to see or select the custom color picker
7. Change the value in the `theme.json` to be `true` or remove it completely, reload the page, and repeat steps 3 to 6, at this point _you should see_ the custom color picker.

## Screenshots or screencast

<!-- If you would like to upload screenshots, feel free to use the table below when it is useful to show the difference between before and after the change. -->

|Before|After|
|-|-|
| <img width="1624" height="831" alt="customcolors-before" src="https://github.com/user-attachments/assets/254838a1-e6fd-4f63-bb87-9a1ada3dace1" /> | <img width="1625" height="946" alt="customcolors-after" src="https://github.com/user-attachments/assets/39d1d6cf-4021-4e72-bc90-e59fa18c1182" /> |

Also note that it respects setting to `true` and if it's removed
|True|Removed|
|-|-|
| <img width="1601" height="802" alt="customcolors-after-true" src="https://github.com/user-attachments/assets/fd770a87-77d3-4a07-9334-3fdcd6787f97" /> | <img width="1624" height="796" alt="customcolors-after-null" src="https://github.com/user-attachments/assets/d5dc1e39-5622-4b4a-a2d7-40661107d6c9" /> |

## Issue comments

### github-actions on 2025-10-16T18:06:19Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-3412168297

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

## Unlinked Accounts

The following contributors have not linked their GitHub and WordPress.org accounts: @n8finch@git.wordpress.org, @snibe.

Contributors, please [read how to link your accounts](https://make.wordpress.org/core/2020/03/19/associating-github-accounts-with-wordpress-org-profiles/) to ensure your work is properly credited in WordPress releases.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Unlinked contributors: n8finch@git.wordpress.org, snibe.

Co-authored-by: n8finch <n8finch@git.wordpress.org>
Co-authored-by: t-hamano <wildworks@git.wordpress.org>
Co-authored-by: aaronrobertshaw <aaronrobertshaw@git.wordpress.org>
Co-authored-by: ntsekouras <ntsekouras@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: ramonjd <ramonopoly@git.wordpress.org>
Co-authored-by: annezazu <annezazu@git.wordpress.org>
Co-authored-by: cbravobernal <cbravobernal@git.wordpress.org>
Co-authored-by: vraja-pro <vrajadas@git.wordpress.org>
Co-authored-by: andreawetzel <awetz583@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2025-10-16T18:06:43Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-3412170404

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @n8finch! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### n8finch on 2026-06-02T02:31:07Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4598203749

@ellatrix and @ajitbohra would love to get an initial review on this as it's been sitting around a while. I just updated the branch so it could be merged. Thanks so much! 🙂

### n8finch on 2026-06-02T16:32:32Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4604525921

@t-hamano thank you for the quick review and help with the wrapper there. I've got the code update. Let me know if it needs anything else. 🙌

### ramonjd on 2026-06-03T00:37:51Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4608104369

This is testing well for me after the latest update. 

The CI is complaining about this prop:

> BorderBoxControl should have the `__next40pxDefaultSize` prop when using the default size.

### n8finch on 2026-06-05T17:13:26Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4633805479

> A lot has changed recently and there's now a conflict on this branch, so we'll need a quick rebase.
> 
> The tests also need some tweaking. I've left further inline comments towards this end.

@aaronrobertshaw ok, got the branch rebased, so, good there.

I've moved the tests to the BorderColor test suite. There's already tests there that check things render when colors are present, so that's good. I added two tests:
- one to make sure things don't render if colors is empty and disableCustomColors is `true`
- another to make sure that the color circles render but not the  custom color picker if disableCustomColors is `true`

Thank you for the back and forth!

### aaronrobertshaw on 2026-06-05T22:07:47Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4635948427

Thanks, I'll be able to review properly on Monday but for now there are a few linting errors that need cleaning up.

### n8finch on 2026-06-08T21:22:25Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4653667764

> A few notes on my end, too:
> 
> * we'll need a CHANGELOG entry;
> * as also flagged by @aaronrobertshaw , the added tests don't really exercise the guard, and don't protect against regressions (they assert the element is absent, regardless of the fix)
> 
> But most importantly, it looks like the logic introduced in `packages/components/src/border-control/border-control-dropdown/component.tsx` is duplicating `ColorPalette` logic:
> 
> https://github.com/WordPress/gutenberg/blob/dfb650e76aeb3c8019578be558b5387c0c29bd26/packages/components/src/color-palette/index.tsx#L281
> 
> It seems to me that the better fix is to have `ColorPalette` return `null` instead of an empty `VStack` ?

Thanks @ciampo for the review. I've:
- updated the changelog
- changed the logic to check in the ColorPalette component and return `null` so there's no empty VStack
- made sure the tests pass.

**Note on the tests**: I've added only the tests that were needed to check that the custom color picker and circular color options _do not show_ if they're opted out of or there are no `colors`, there are already tests above that check to make sure these items render see: `it( 'should render color and style popover'...`:

https://github.com/WordPress/gutenberg/blob/dfb650e76aeb3c8019578be558b5387c0c29bd26/packages/components/src/border-control/test/index.js#L136-L161

### n8finch on 2026-06-09T17:56:11Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4662524055

> LGTM 🚀
> 
> We'll be able to merge once the remaining feedback is addressed, and conflicts with trunk are solved.
> 
> Thank you!

@ciampo Thanks for your help! I think (hope!) everything is good here.

### ramonjd on 2026-06-10T00:42:19Z

URL: https://github.com/WordPress/gutenberg/pull/72402#issuecomment-4665380634

I can merge today. Thanks everyone for the feedback, and the continued iteration @n8finch

## Review threads

### Review thread 1 (resolved)

#### t-hamano on 2026-06-03T00:54:02Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3345314524
Path: packages/components/src/border-box-control/test/index.tsx

```diff
@@ -230,6 +230,45 @@ describe( 'BorderBoxControl', () => {
 				} )
 			).not.toBeInTheDocument();
 		} );
+
+		it( 'should disable the custom color picker when disableCustomColors is true', async () => {
+			const user = userEvent.setup();
+
+			// disableCustomColors = true → custom picker should NOT appear
+			render( <BorderBoxControl { ...props } disableCustomColors /> );
```

```suggestion
			render( <TestBorderBoxControl { ...props } disableCustomColors /> );
```

### Review thread 2 (resolved)

#### t-hamano on 2026-06-03T00:54:08Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3345314764
Path: packages/components/src/border-box-control/test/index.tsx

```diff
@@ -230,6 +230,45 @@ describe( 'BorderBoxControl', () => {
 				} )
 			).not.toBeInTheDocument();
 		} );
+
+		it( 'should disable the custom color picker when disableCustomColors is true', async () => {
+			const user = userEvent.setup();
+
+			// disableCustomColors = true → custom picker should NOT appear
+			render( <BorderBoxControl { ...props } disableCustomColors /> );
+
+			const colorButton = screen.getByLabelText( toggleLabelRegex );
+			await user.click( colorButton );
+
+			// Wait to make sure the dropdown opens
+			await waitFor( () =>
+				expect(
+					screen.queryByRole( 'button', {
+						name: 'Custom color picker',
+					} )
+				).not.toBeInTheDocument()
+			);
+		} );
+
+		it( 'should show the custom color picker when disableCustomColors is false', async () => {
+			const user = userEvent.setup();
+
+			// disableCustomColors = false → custom picker SHOULD appear
+			render(
+				<BorderBoxControl { ...props } disableCustomColors={ false } />
```

```suggestion
				<TestBorderBoxControl { ...props } disableCustomColors={ false } />
```

### Review thread 3 (resolved)

#### aaronrobertshaw on 2026-06-03T06:46:02Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3346452705
Path: packages/block-editor/src/components/global-styles/border-panel.js

```diff
@@ -40,6 +40,10 @@ export function useHasBorderPanelControls( settings ) {
 	return controls;
 }
 
+function useCustomSolids( settings ) {
```

I know this is following the pattern elsewhere in this file, which itself followed other existing components but the naming with the `use` prefix when the function isn't a custom hook is something I think we need to move away from. 

This doesn't have to be a blocker just something we should revisit in a follow up soon.

#### ntsekouras on 2026-06-03T09:29:48Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3347366201
Path: packages/block-editor/src/components/global-styles/border-panel.js

```diff
@@ -40,6 +40,10 @@ export function useHasBorderPanelControls( settings ) {
 	return controls;
 }
 
+function useCustomSolids( settings ) {
```

Do we even need a function for this? It's only used once..

#### n8finch on 2026-06-03T14:37:43Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3349366351
Path: packages/block-editor/src/components/global-styles/border-panel.js

```diff
@@ -40,6 +40,10 @@ export function useHasBorderPanelControls( settings ) {
 	return controls;
 }
 
+function useCustomSolids( settings ) {
```

I'm good either way, just let me know if we should change the name of the function or just remove it as a function and change line 112 to `const areCustomSolidsEnabled = settings?.color?.custom;` . 

What seems better or reads better?

I prefer having a function, even if it's used once, would it be better to name it `shouldUseCustomSolids`?

Thoughts @aaronrobertshaw and @ntsekouras ?

#### aaronrobertshaw on 2026-06-03T23:14:17Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3352459877
Path: packages/block-editor/src/components/global-styles/border-panel.js

```diff
@@ -40,6 +40,10 @@ export function useHasBorderPanelControls( settings ) {
 	return controls;
 }
 
+function useCustomSolids( settings ) {
```

I don't have a strong feeling about this one. 

If we are renaming the `use` prefixed function it should be done for all functions named like that in the component. If we aren't renaming it, I don't think we really need a function at this stage.

#### n8finch on 2026-06-04T23:08:42Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3359398994
Path: packages/block-editor/src/components/global-styles/border-panel.js

```diff
@@ -40,6 +40,10 @@ export function useHasBorderPanelControls( settings ) {
 	return controls;
 }
 
+function useCustomSolids( settings ) {
```

Left a note where the constant is now declared instead of a function being used.

### Review thread 4

#### aaronrobertshaw on 2026-06-03T06:49:00Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3346471687
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -203,17 +203,19 @@ const BorderControlDropdown = (
 	const renderContent = () => (
 		<DropdownContentWrapper paddingSize="medium">
 			<VStack className={ popoverControlsClassName } spacing={ 6 }>
-				<ColorPalette
-					className={ popoverContentClassName }
-					value={ color }
-					onChange={ onColorChange }
-					{ ...{ colors, disableCustomColors } }
-					__experimentalIsRenderedInSidebar={
-						__experimentalIsRenderedInSidebar
-					}
-					clearable={ false }
-					enableAlpha={ enableAlpha }
-				/>
+				{ ( colors.length > 0 || ! disableCustomColors ) && (
```

We should add a test to cover excluding the `ColorPalette` when there are no colors.

#### n8finch on 2026-06-03T14:59:32Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3349558493
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -203,17 +203,19 @@ const BorderControlDropdown = (
 	const renderContent = () => (
 		<DropdownContentWrapper paddingSize="medium">
 			<VStack className={ popoverControlsClassName } spacing={ 6 }>
-				<ColorPalette
-					className={ popoverContentClassName }
-					value={ color }
-					onChange={ onColorChange }
-					{ ...{ colors, disableCustomColors } }
-					__experimentalIsRenderedInSidebar={
-						__experimentalIsRenderedInSidebar
-					}
-					clearable={ false }
-					enableAlpha={ enableAlpha }
-				/>
+				{ ( colors.length > 0 || ! disableCustomColors ) && (
```

@aaronrobertshaw got a test added for that.

#### aaronrobertshaw on 2026-06-03T23:13:08Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3352456432
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -203,17 +203,19 @@ const BorderControlDropdown = (
 	const renderContent = () => (
 		<DropdownContentWrapper paddingSize="medium">
 			<VStack className={ popoverControlsClassName } spacing={ 6 }>
-				<ColorPalette
-					className={ popoverContentClassName }
-					value={ color }
-					onChange={ onColorChange }
-					{ ...{ colors, disableCustomColors } }
-					__experimentalIsRenderedInSidebar={
-						__experimentalIsRenderedInSidebar
-					}
-					clearable={ false }
-					enableAlpha={ enableAlpha }
-				/>
+				{ ( colors.length > 0 || ! disableCustomColors ) && (
```

Thanks for the quick iteration @n8finch. Unfortunately, this test doesn't actually cover the change.

The guard you added prevents an empty `ColorPalette` wrapper from rendering when there are no colors and custom colors are disabled. This new test though only asserts that the "Custom color picker" button is absent. That button is never present when `disableCustomColors` is `true` regardless of this fix, so the test passes with or without your change and provides no regression protection.

#### n8finch on 2026-06-04T23:05:39Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3359390438
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -203,17 +203,19 @@ const BorderControlDropdown = (
 	const renderContent = () => (
 		<DropdownContentWrapper paddingSize="medium">
 			<VStack className={ popoverControlsClassName } spacing={ 6 }>
-				<ColorPalette
-					className={ popoverContentClassName }
-					value={ color }
-					onChange={ onColorChange }
-					{ ...{ colors, disableCustomColors } }
-					__experimentalIsRenderedInSidebar={
-						__experimentalIsRenderedInSidebar
-					}
-					clearable={ false }
-					enableAlpha={ enableAlpha }
-				/>
+				{ ( colors.length > 0 || ! disableCustomColors ) && (
```

@aaronrobertshaw right, ok, there are four tests now covering:
- custom color picker should be enabled
- custom color picker should be disabled
- the colors array has colors and should show the palette pickers
- the colors array does not have colors and should not show the palette pickers

So that will check both sides of the guard now.

#### aaronrobertshaw on 2026-06-05T04:37:06Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3360393658
Path: packages/components/src/border-control/border-control-dropdown/component.tsx

```diff
@@ -203,17 +203,19 @@ const BorderControlDropdown = (
 	const renderContent = () => (
 		<DropdownContentWrapper paddingSize="medium">
 			<VStack className={ popoverControlsClassName } spacing={ 6 }>
-				<ColorPalette
-					className={ popoverContentClassName }
-					value={ color }
-					onChange={ onColorChange }
-					{ ...{ colors, disableCustomColors } }
-					__experimentalIsRenderedInSidebar={
-						__experimentalIsRenderedInSidebar
-					}
-					clearable={ false }
-					enableAlpha={ enableAlpha }
-				/>
+				{ ( colors.length > 0 || ! disableCustomColors ) && (
```

@n8finch unfortunately, the tests still do not cover the guards behaviour. 

The guard prevents rendering the entire `ColorPalette` not just the custom color picker. So as it stands, the tests still don't protect against a regression here.

Also, I missed this on my earlier review but the tests here are for the `BorderBoxControl` not the `BorderControl` component that has actually been updated. That probably needs addressing too.

### Review thread 5 (resolved)

#### n8finch on 2026-06-04T23:08:06Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3359397166
Path: packages/block-editor/src/components/global-styles/border-panel.js

```diff
@@ -105,6 +105,7 @@ export default function BorderPanel( {
 	defaultControls = DEFAULT_CONTROLS,
 } ) {
 	const colors = useColorsPerOrigin( settings );
+	const areCustomSolidsEnabled = settings?.color?.custom;
```

@aaronrobertshaw I've changed the assignment here to reference the settings instead of using the single-use function.

### Review thread 6 (resolved)

#### ciampo on 2026-06-09T07:47:27Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3378783447
Path: packages/components/CHANGELOG.md

```diff
@@ -14,6 +14,10 @@
 
 -   `Tooltip`: Use `--wpds-border-radius-md` for portaled popup surfaces, aligning with menus and popovers ([#78983](https://github.com/WordPress/gutenberg/pull/78983)).
 
+### Bug Fixes
+
+-   Fix bug displaying custom color option even when opted out ([74402](https://github.com/WordPress/gutenberg/pull/72402))
```

```suggestion
-   -   `ColorPalette`: Avoid rendering an empty palette when custom colors are disabled and no color presets are available ([#72402](https://github.com/WordPress/gutenberg/pull/72402)).
```

### Review thread 7 (resolved)

#### ciampo on 2026-06-09T07:51:20Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3378805097
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -276,6 +276,11 @@ function UnforwardedColorPalette(
 		ariaLabelledby
 	);
 
+	// If disableCustomColors is true and colors.length is 0, return null to avoid rendering an empty circular option picker.
+	if ( disableCustomColors && colors.length === 0 ) {
```

This check ignores `actions` (ie. the `Clear` button): with the current code, a consumer using default `clearable` with `disableCustomColors` and empty `colors` now gets `null` — losing the Clear button (and the picker that previously rendered it).

`BorderControl` is unaffected because it hardcodes `clearable={ false }`, but `ColorPalette` is a broadly-consumed public component (duotone, cover block, panel color settings, etc.)



```suggestion
	if ( disableCustomColors && colors.length === 0 && ! actions ) {
```

### Review thread 8 (resolved)

#### ciampo on 2026-06-09T08:05:01Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3378880970
Path: packages/components/src/border-control/test/index.js

```diff
@@ -176,6 +176,60 @@ describe( 'BorderControl', () => {
 			expect( dashedButton ).not.toBeInTheDocument();
 			expect( dottedButton ).not.toBeInTheDocument();
 		} );
+
+		it( 'should not render ColorPalette if colors is empty and disableCustomColors is true', async () => {
+			const user = userEvent.setup();
+			const props = createProps( {
+				colors: [],
+				disableCustomColors: true,
+			} );
+			render( <TestBorderControl { ...props } /> );
+
+			const toggleButton = screen.getByLabelText( toggleLabelRegex );
+			await user.click( toggleButton );
+
+			// Wait for the popover to appear by checking for the style picker.
+			expect( await screen.findByText( 'Style' ) ).toBeInTheDocument();
+			// The ColorPalette contains both the custom color picker button and the listbox of color options, so neither should be rendered when there are no colors and custom colors are disabled.
+			expect(
+				screen.queryByRole( 'button', {
+					name: /^Custom color picker/,
+				} )
+			).not.toBeInTheDocument();
+			expect(
+				screen.queryByRole( 'listbox', {
+					name: 'Custom color picker',
+				} )
+			).not.toBeInTheDocument();
+		} );
```

The test are still not testing / protecting the newly added `null` guard, ie. they would likely also pass on `trunk`.


Since the fix now lives in `ColorPalette`, we could be able to remove the tests added to `BorderControl`, and instead add tests to`ColorPalette`:


```tsx
it( 'should render nothing when custom colors are disabled, there are no colors, and it is not clearable', () => {
	const { container } = render(
		<ColorPalette
			colors={ [] }
			disableCustomColors
			clearable={ false }
			onChange={ jest.fn() }
		/>
	);

	expect( container ).toBeEmptyDOMElement();
} );

it( 'should still show the clear button when colors is empty and custom colors are disabled', () => {
	render( <ColorPalette colors={ [] } disableCustomColors onChange={ jest.fn() } /> );

	expect( screen.getByRole( 'button', { name: 'Clear' } ) ).toBeInTheDocument();
} );
```

#### n8finch on 2026-06-09T13:50:45Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3381143598
Path: packages/components/src/border-control/test/index.js

```diff
@@ -176,6 +176,60 @@ describe( 'BorderControl', () => {
 			expect( dashedButton ).not.toBeInTheDocument();
 			expect( dottedButton ).not.toBeInTheDocument();
 		} );
+
+		it( 'should not render ColorPalette if colors is empty and disableCustomColors is true', async () => {
+			const user = userEvent.setup();
+			const props = createProps( {
+				colors: [],
+				disableCustomColors: true,
+			} );
+			render( <TestBorderControl { ...props } /> );
+
+			const toggleButton = screen.getByLabelText( toggleLabelRegex );
+			await user.click( toggleButton );
+
+			// Wait for the popover to appear by checking for the style picker.
+			expect( await screen.findByText( 'Style' ) ).toBeInTheDocument();
+			// The ColorPalette contains both the custom color picker button and the listbox of color options, so neither should be rendered when there are no colors and custom colors are disabled.
+			expect(
+				screen.queryByRole( 'button', {
+					name: /^Custom color picker/,
+				} )
+			).not.toBeInTheDocument();
+			expect(
+				screen.queryByRole( 'listbox', {
+					name: 'Custom color picker',
+				} )
+			).not.toBeInTheDocument();
+		} );
```

Thanks for the guidance here @ciampo , very helpful!

I've got those added to the ColorPalette test file.

### Review thread 9 (resolved)

#### ciampo on 2026-06-09T15:14:17Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3381769316
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -276,6 +276,11 @@ function UnforwardedColorPalette(
 		ariaLabelledby
 	);
 
+	// If disableCustomColors is true and colors.length is 0, return null to avoid rendering an empty circular option picker.
```

```suggestion
	// If disableCustomColors is true and colors.length is 0, return null to avoid rendering an empty palette wrapper.
```

### Review thread 10 (resolved)

#### ciampo on 2026-06-09T15:14:41Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3381772041
Path: packages/components/CHANGELOG.md

```diff
@@ -14,6 +14,10 @@
 
 -   `Tooltip`: Use `--wpds-border-radius-md` for portaled popup surfaces, aligning with menus and popovers ([#78983](https://github.com/WordPress/gutenberg/pull/78983)).
 
+### Bug Fixes
+
+-   -   `ColorPalette`: Avoid rendering an empty palette when custom colors are disabled and no color presets are available ([#72402](https://github.com/WordPress/gutenberg/pull/72402)).
```

```suggestion
-   `ColorPalette`: Avoid rendering an empty palette when custom colors are disabled and no color presets are available ([#72402](https://github.com/WordPress/gutenberg/pull/72402)).
```

#### n8finch on 2026-06-09T17:55:05Z

URL: https://github.com/WordPress/gutenberg/pull/72402#discussion_r3382773190
Path: packages/components/CHANGELOG.md

```diff
@@ -14,6 +14,10 @@
 
 -   `Tooltip`: Use `--wpds-border-radius-md` for portaled popup surfaces, aligning with menus and popovers ([#78983](https://github.com/WordPress/gutenberg/pull/78983)).
 
+### Bug Fixes
+
+-   -   `ColorPalette`: Avoid rendering an empty palette when custom colors are disabled and no color presets are available ([#72402](https://github.com/WordPress/gutenberg/pull/72402)).
```

@ciampo got this fixed locally while resolving merge conflicts.

## Changed files

- packages/block-editor/src/components/global-styles/border-panel.js (MODIFIED, +2/-0)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/color-palette/index.tsx (MODIFIED, +5/-0)
- packages/components/src/color-palette/test/index.tsx (MODIFIED, +30/-0)
