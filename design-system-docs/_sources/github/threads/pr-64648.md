---
number: 64648
type: pull_request
state: MERGED
merged_at: 2024-08-21T13:28:55Z
url: https://github.com/WordPress/gutenberg/pull/64648
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #64648: Components: Use `useStoreState()` instead of `store.useState()`

- URL: https://github.com/WordPress/gutenberg/pull/64648
- Author: tyxla
- Created: 2024-08-20T14:53:15Z
- Updated: 2024-12-11T09:44:23Z
- Comments: 3 of 3
- Review threads: 8 of 8
- Changed files: 13 of 13
- Diff: ../pr-diffs/64648.diff

## Body

## What?
After updating to Ariakit `v0.4.10`, we're changing `store.useState()` instances to the new `useStoreState()` API.

## Why?
To resolve problems with the React Compiler raising errors when using `store.useState()`:

```
Hooks must be the same function on every render, but this value may change over time to a different function. See https://react.dev/reference/rules/react-calls-components-and-hooks#dont-dynamically-use-hooks  react-compiler/react-compiler
```

See #61788.

## How?
We're updating the syntax.
For instances that were conditionally called, we're moving them above the condition as needed. 

## Testing Instructions
* Test the affected components in Storybook and verify they still work well.
* Verify all checks are green.

### Testing Instructions for Keyboard
None

## Screenshots or screencast <!-- if applicable -->
None

## Issue comments

### github-actions on 2024-08-20T14:53:32Z

URL: https://github.com/WordPress/gutenberg/pull/64648#issuecomment-2299059981

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-08-20T15:17:27Z

URL: https://github.com/WordPress/gutenberg/pull/64648#issuecomment-2299115859

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 13619bdaa5058a6a2f5f2973c62717c3c8d53e1d.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/10474021732
📝  Reported issues:
- #58009 in `/test/e2e/specs/editor/various/pattern-overrides.spec.js`

### tyxla on 2024-08-21T12:55:40Z

URL: https://github.com/WordPress/gutenberg/pull/64648#issuecomment-2301988300

Thank you @Mamaduka and @ciampo 🙌

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2024-08-20T14:59:51Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723474443
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -42,8 +44,7 @@ export const TabList = forwardRef<
 		return null;
 	}
 	const { store } = context;
-
-	const { activeId, selectOnMove } = store.useState();
+	const { activeId, selectOnMove } = tabStore as Ariakit.TabStoreState;
```

Moved the `useState` -> `useStoreState` call above the condition in order to fulfill the rules of hooks. 

Since TS can't be sure that `tabStore` won't be `undefined`, we're casting it to the right type. 

The pre-existing `context` check helps us be more certain that the `store` will be there.

#### ciampo on 2024-08-20T20:23:03Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723935009
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -42,8 +44,7 @@ export const TabList = forwardRef<
 		return null;
 	}
 	const { store } = context;
-
-	const { activeId, selectOnMove } = store.useState();
+	const { activeId, selectOnMove } = tabStore as Ariakit.TabStoreState;
```

An alternative approach that doesn't involve type casting would be to check that `tabStore` is not `undefined` — we could change the `if ( ! context ) {` check to `if ( ! context || ! tabStoreState ) {`, which would be also safer at runtime

#### tyxla on 2024-08-21T12:56:13Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1725006599
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -42,8 +44,7 @@ export const TabList = forwardRef<
 		return null;
 	}
 	const { store } = context;
-
-	const { activeId, selectOnMove } = store.useState();
+	const { activeId, selectOnMove } = tabStore as Ariakit.TabStoreState;
```

Resolved as part of c9b238f3e3faf8f5e6ce711b79c73e10f9841a8f.

### Review thread 2

#### tyxla on 2024-08-20T15:00:23Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723475274
Path: packages/components/src/tabs/tabpanel.tsx

```diff
@@ -22,13 +26,13 @@ export const TabPanel = forwardRef<
 	ref
 ) {
 	const context = useTabsContext();
+	const selectedId = useStoreState( context?.store, 'selectedId' );
 	if ( ! context ) {
 		warning( '`Tabs.TabPanel` must be wrapped in a `Tabs` component.' );
 		return null;
 	}
 	const { store, instanceId } = context;
 	const instancedTabId = `${ instanceId }-${ tabId }`;
-	const selectedId = store.useState( ( state ) => state.selectedId );
```

Moved up above the condition to fulfill the rules of hooks. Simplified the callback syntax.

### Review thread 3

#### tyxla on 2024-08-20T15:01:42Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723477354
Path: packages/dataviews/src/dataviews-layouts/list/index.tsx

```diff
@@ -359,10 +361,11 @@ export default function ViewList< Item >( props: ViewListProps< Item > ) {
 
 	const store = useCompositeStore( {
 		defaultActiveId: getItemDomId( selectedItem ),
-	} );
+	} ) as CompositeStore; // TODO, remove once composite APIs are public
```

Necessary for the `useStoreState` call. Previously was `any` due to the locked nature of the private API.

### Review thread 4

#### tyxla on 2024-08-20T15:06:02Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723484134
Path: packages/dataviews/src/dataviews-layouts/list/index.tsx

```diff
@@ -3,6 +3,8 @@
  */
 import clsx from 'clsx';
 // TODO: use the @wordpress/components one once public
+// eslint-disable-next-line no-restricted-imports
+import { useStoreState } from '@ariakit/react';
```

@WordPress/gutenberg-components WDYT - should we re-export this from `@wordpress/components`?

#### ciampo on 2024-08-20T19:54:40Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723905947
Path: packages/dataviews/src/dataviews-layouts/list/index.tsx

```diff
@@ -3,6 +3,8 @@
  */
 import clsx from 'clsx';
 // TODO: use the @wordpress/components one once public
+// eslint-disable-next-line no-restricted-imports
+import { useStoreState } from '@ariakit/react';
```

I guess it depends on what we decide around exposing stores in https://github.com/WordPress/gutenberg/issues/63704. Better hold off for now

### Review thread 5 (resolved)

#### ciampo on 2024-08-20T20:17:35Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723929418
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -26,7 +27,8 @@ export const TabList = forwardRef<
 >( function TabList( { children, ...otherProps }, ref ) {
 	const context = useTabsContext();
 
-	const selectedId = context?.store.useState( 'selectedId' );
+	const tabStore = useStoreState( context?.store );
```

Nit: the tab store is actually the one in `context.store`. This one is the store state

```suggestion
	const tabStoreState = useStoreState( context?.store );
```

#### tyxla on 2024-08-21T12:54:29Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1725003776
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -26,7 +27,8 @@ export const TabList = forwardRef<
 >( function TabList( { children, ...otherProps }, ref ) {
 	const context = useTabsContext();
 
-	const selectedId = context?.store.useState( 'selectedId' );
+	const tabStore = useStoreState( context?.store );
```

Right, thanks. Renamed as part of c9b238f3e3faf8f5e6ce711b79c73e10f9841a8f

### Review thread 6 (resolved)

#### ciampo on 2024-08-20T20:18:29Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723930328
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -26,7 +27,8 @@ export const TabList = forwardRef<
 >( function TabList( { children, ...otherProps }, ref ) {
 	const context = useTabsContext();
 
-	const selectedId = context?.store.useState( 'selectedId' );
+	const tabStore = useStoreState( context?.store );
+	const selectedId = useStoreState( context?.store, 'selectedId' );
```

Since we have the whole store state already, we don't need to call the `useStoreState` hook again

```suggestion
	const selectedId = tabStoreState?.selectedId;
```

#### tyxla on 2024-08-21T12:54:36Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1725003937
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -26,7 +27,8 @@ export const TabList = forwardRef<
 >( function TabList( { children, ...otherProps }, ref ) {
 	const context = useTabsContext();
 
-	const selectedId = context?.store.useState( 'selectedId' );
+	const tabStore = useStoreState( context?.store );
+	const selectedId = useStoreState( context?.store, 'selectedId' );
```

Indeed, done as part of c9b238f3e3faf8f5e6ce711b79c73e10f9841a8f

### Review thread 7 (resolved)

#### ciampo on 2024-08-20T20:20:29Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1723932515
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -42,8 +44,7 @@ export const TabList = forwardRef<
 		return null;
 	}
 	const { store } = context;
-
-	const { activeId, selectOnMove } = store.useState();
+	const { activeId, selectOnMove } = tabStore as Ariakit.TabStoreState;
 	const { setActiveId } = store;
```

This can be changed to 
```suggestion
	const { setActiveId } = context.store;
```

and then we can delete `const { store } = context;` two lines above

#### tyxla on 2024-08-21T12:55:08Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1725004728
Path: packages/components/src/tabs/tablist.tsx

```diff
@@ -42,8 +44,7 @@ export const TabList = forwardRef<
 		return null;
 	}
 	const { store } = context;
-
-	const { activeId, selectOnMove } = store.useState();
+	const { activeId, selectOnMove } = tabStore as Ariakit.TabStoreState;
 	const { setActiveId } = store;
```

We could, but I preferred to leave it, because we need `store` separately as we're passing it as a separate prop to `Ariakit.TabList`.

### Review thread 8

#### Mamaduka on 2024-12-11T09:33:40Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1879705779
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -2,6 +2,7 @@
  * External dependencies
  */
 import * as Ariakit from '@ariakit/react';
+import { useStoreState } from '@ariakit/react';
```

Any reason for using a separate import instead of `Ariakit.useStoreState`?

#### tyxla on 2024-12-11T09:42:22Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1879720663
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -2,6 +2,7 @@
  * External dependencies
  */
 import * as Ariakit from '@ariakit/react';
+import { useStoreState } from '@ariakit/react';
```

I don't think so, no. I think it was caused by the automated way I addressed these changes. Let me fix these in a follow-up PR.

#### tyxla on 2024-12-11T09:44:23Z

URL: https://github.com/WordPress/gutenberg/pull/64648#discussion_r1879724316
Path: packages/components/src/custom-select-control-v2/custom-select.tsx

```diff
@@ -2,6 +2,7 @@
  * External dependencies
  */
 import * as Ariakit from '@ariakit/react';
+import { useStoreState } from '@ariakit/react';
```

#67818 - thanks for spotting @Mamaduka 🙌

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/alignment-matrix-control/index.tsx (MODIFIED, +2/-1)
- packages/components/src/circular-option-picker/circular-option-picker-option.tsx (MODIFIED, +2/-1)
- packages/components/src/custom-select-control-v2/custom-select.tsx (MODIFIED, +2/-1)
- packages/components/src/dropdown-menu-v2/index.tsx (MODIFIED, +5/-3)
- packages/components/src/radio-group/radio.tsx (MODIFIED, +2/-1)
- packages/components/src/tab-panel/index.tsx (MODIFIED, +4/-1)
- packages/components/src/tabs/index.tsx (MODIFIED, +2/-1)
- packages/components/src/tabs/tablist.tsx (MODIFIED, +6/-4)
- packages/components/src/tabs/tabpanel.tsx (MODIFIED, +6/-2)
- packages/components/src/toggle-group-control/toggle-group-control/as-radio-group.tsx (MODIFIED, +2/-1)
- packages/components/src/tooltip/index.tsx (MODIFIED, +2/-1)
- packages/dataviews/src/dataviews-layouts/list/index.tsx (MODIFIED, +5/-2)
