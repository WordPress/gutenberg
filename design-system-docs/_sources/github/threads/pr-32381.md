---
number: 32381
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/32381
matched_labels: ["[Package] Components"]
all_labels: ["[Package] Components"]
---

# PR #32381: Components: Integrate TextInput component [WIP]

- URL: https://github.com/WordPress/gutenberg/pull/32381
- Author: ramonjd
- Created: 2021-06-02T04:40:49Z
- Updated: 2022-03-24T11:51:56Z
- Comments: 6 of 6
- Review threads: 30 of 30
- Changed files: 27 of 27
- Diff: ../pr-diffs/32381.diff

## Body

🚧 WIP

## Description
This PR integrates the g2 [TextInput](https://github.com/ItsJonQ/g2/tree/main/packages/components/src/TextInput) component.

Integration tracking issue: https://github.com/WordPress/gutenberg/issues/30503

Related: https://github.com/WordPress/gutenberg/issues/28399


## How has this been tested?
This is a new component.

We'll check that:

- [ ] storybook story works ❓ There are a lot Typescript errors, some from other sources that prevent me from running `npm run storybook:dev` so I've removed the import from `tsconfig.json` for now.
- [ ] unit tests pass


```bash
 npm run test-unit packages/components/src/utils/test/values.js
 npm run test-unit packages/components/src/text-input/test
```

## Screenshots 

To come...

## Types of changes
A new feature

## Checklist:
- [ ] My code is tested.
- [ ] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->

## Issue comments

### ciampo on 2021-06-03T16:05:52Z

URL: https://github.com/WordPress/gutenberg/pull/32381#issuecomment-853986865

Hey @ramonjd, thank you for working on this PR!

I took a look at this PR, and it looks like there are still quite a few build errors that prevent you from running the Storybook story correctly. I tinkered around on my local machine and came up with these fixes:

1. Rebased on top of latest `trunk` in order to include #32401
2. Fixed the `Icon` import in text-input-arrows.js
3. Ran some combination of `npm run distclean && npm i` in order to include `react-autosize-textarea` and `react-number-format` in the `package-lock.json` file
4. Interpolated some `CONFIG` variables in `${…}` in order not to break `styles.js` at runtime
5. Added the `text-input` folder to `tsconfig.json`, so that it gets checked by TypeScript

<details>
<summary>Since I don't seem to have write rights on your PR branch, <b>I've prepared a diff including the code changes from points 2 to 5</b> (expand to reveal)</summary>

```diff
diff --git a/package-lock.json b/package-lock.json
index 5a52107892..e450e3268b 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -13224,7 +13224,9 @@
 				"memize": "^1.1.0",
 				"moment": "^2.22.1",
 				"re-resizable": "^6.4.0",
+				"react-autosize-textarea": "^7.1.0",
 				"react-dates": "^17.1.1",
+				"react-number-format": "^4.6.0",
 				"react-resize-aware": "^3.1.0",
 				"react-spring": "^8.0.20",
 				"react-use-gesture": "^9.0.0",
@@ -51265,6 +51267,14 @@
 				"prop-types": "^15.5.10"
 			}
 		},
+		"react-number-format": {
+			"version": "4.6.0",
+			"resolved": "https://registry.npmjs.org/react-number-format/-/react-number-format-4.6.0.tgz",
+			"integrity": "sha512-0Td7QM3FGFVOw+DzYooLae50d6r67hZcinbDy5TQRQJB9+/p1caX+NQQ6PW408H+nZTKgykknQHv6L6PPoRO2w==",
+			"requires": {
+				"prop-types": "^15.7.2"
+			}
+		},
 		"react-outside-click-handler": {
 			"version": "1.3.0",
 			"resolved": "https://registry.npmjs.org/react-outside-click-handler/-/react-outside-click-handler-1.3.0.tgz",
diff --git a/packages/components/src/text-input/styles.js b/packages/components/src/text-input/styles.js
index 1e690a4b54..b302a7cc1a 100644
--- a/packages/components/src/text-input/styles.js
+++ b/packages/components/src/text-input/styles.js
@@ -26,7 +26,7 @@ export const inputMultiline = css`
 `;
 
 export const inputFontSize = css`
-	font-size: CONFIG.fontSizeInputMobile;
+	font-size: ${ CONFIG.fontSizeInputMobile };
 	@media ( min-width: 36em ) {
 		font-size: ${ CONFIG.fontSize };
 	}
@@ -36,10 +36,10 @@ export const Input = css`
 	appearance: none;
 	background: transparent;
 	border: none;
-	border-radius: CONFIG.controlBorderRadius;
+	border-radius: ${ CONFIG.controlBorderRadius };
 	box-shadow: none;
 	box-sizing: border-box;
-	color: CONFIG.controlTextColor;
+	color: ${ CONFIG.controlTextColor };
 	display: block;
 	flex: 1;
 	line-height: 18px;
diff --git a/packages/components/src/text-input/text-input-arrows.js b/packages/components/src/text-input/text-input-arrows.js
index 5ba2dd8dc0..90b4b8a0c7 100644
--- a/packages/components/src/text-input/text-input-arrows.js
+++ b/packages/components/src/text-input/text-input-arrows.js
@@ -18,7 +18,7 @@ import {
 /**
  * Internal dependencies
  */
-import { Icon } from '../icon';
+import Icon from '../icon';
 import { View } from '../view';
 import { VStack } from '../v-stack';
 import * as styles from './styles';
diff --git a/packages/components/tsconfig.json b/packages/components/tsconfig.json
index 188a594d27..65df915199 100644
--- a/packages/components/tsconfig.json
+++ b/packages/components/tsconfig.json
@@ -33,6 +33,7 @@
 		"src/shortcut/**/*",
 		"src/spinner/**/*",
 		"src/spacer/**/*",
+		"src/text-input/**/*",
 		"src/tip/**/*",
 		"src/truncate/**/*",
 		"src/ui/**/*",

```

</details>


I recommend running `npm run distclean && npm run ci` after applying the changes above.

With these fixes in place, the `npm run storybook:dev` command should run successfully — but the `TextInput` story will still not appear (or error when forced to). There seem to be some issues with the `useTextInput()` hook — I commented it out and the errors were gone.

Unfortunately, I didn't have enough time to go to the bottom of this, but I noticed that the `types.ts` file is showing some errors — that could be a good start to understand what's causing the errors!

Another good resource is [this short guide by @sarayourfriend](https://github.com/WordPress/gutenberg/issues/30503#issuecomment-845461455) on migrating g2 components to `gutenberg`.

### ramonjd on 2021-06-03T21:03:42Z

URL: https://github.com/WordPress/gutenberg/pull/32381#issuecomment-854178206

@ciampo Thanks so much for looking at this, and sorry I left so many silly errors in there. I had fixed some of the more basic ones like imports and interpolation locally but, as always, forgot to push 🤦 

I appreciate your help, and will follow your advice above to keep digging away 🙏

### ramonjd on 2021-06-04T07:01:44Z

URL: https://github.com/WordPress/gutenberg/pull/32381#issuecomment-854416735

I might chip away slowly at this.

There are a bunch of Typescript errors that run deep and/or across files, and whose priorities I'm not quite sure.

Whenever @sarayourfriend is next available to lend some 👀 I'd be sure grateful for your advice too! Thanks!

### ciampo on 2021-06-04T08:34:17Z

URL: https://github.com/WordPress/gutenberg/pull/32381#issuecomment-854483130

> I appreciate your help, and will follow your advice above to keep digging away

Don't mention it! And feel free to ping me again :)

### ramonjd on 2021-06-16T07:02:41Z

URL: https://github.com/WordPress/gutenberg/pull/32381#issuecomment-862105439

Thanks so much for the detailed review @sarayourfriend ❤️ . Really helpful.

Yes, this is a big one! :) My eyes were too big for my 🧠, but I'm learning a whole bunch along the way.

I'll slowly chip away where I can. 

There are some type errors I've ignored, mostly related to `PolymorphicComponentProps`, which are cryptic to me so I might ping later to see if we need to do anything about it now.

Also I can look into moving those utils into packages if you think it's okay. Would it make more sense to do it in separate PRs?

### sarayourfriend on 2021-06-16T13:51:47Z

URL: https://github.com/WordPress/gutenberg/pull/32381#issuecomment-862398986

> Also I can look into moving those utils into packages if you think it's okay. Would it make more sense to do it in separate PRs?

I think based on @gziolo's answer we can ignore those questions of mine 🙂 

Feel free to ping with any questions regarding the types. They can be pretty non-obvious to work with... 😞

## Review threads

### Review thread 1 (resolved)

#### ramonjd on 2021-06-03T09:35:10Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r644642423
Path: packages/components/src/text-input/hooks/use-text-input.js

```diff
@@ -0,0 +1,194 @@
+/**
+ * External dependencies
+ */
+import { cx } from 'emotion';
+import TextareaAutosize from 'react-autosize-textarea';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useContextSystem } from '../../ui/context';
+import { useBaseField } from '../../base-field';
+import { useFormGroupContextId } from '../../ui/form-group';
+import * as styles from '../styles';
+import { useTextInputState } from './use-text-input-state';
+
+const useRootEventHandlers = ( {
+	dragHandlersRef,
+	inputRef,
+	isFocused,
+	isTypeNumeric,
+} ) => {
+	const canScroll = isTypeNumeric && isFocused;
+	const dragHandlers = dragHandlersRef.current;
+
+	useEffect( () => {
+		const handleOnWheel = () => {};
+		if ( inputRef.current.addEventListener ) {
+			inputRef.current.addEventListener( 'wheel', handleOnWheel, {
+				passive: false,
+			} );
+		}
+	}, [ inputRef, canScroll ] );
+
+	const handleOnClick = useCallback( () => {
+		inputRef.current.focus();
+	}, [ inputRef ] );
+
+	const handleOnTouchStart = useCallback( () => {
+		inputRef.current.focus();
+	}, [ inputRef ] );
+
+	return {
+		...dragHandlers,
+		onClick: handleOnClick,
+		onTouchStart: handleOnTouchStart,
+	};
+};
+
+/**
+ *
+ * @param {import('@wp-g2/create-styles').ViewOwnProps<import('../types').Props, 'input'>} props
+ */
+export function useTextInput( props ) {
+	const {
+		align,
+		arrows = true,
+		className,
+		defaultValue = '',
+		disabled,
+		dragAxis,
+		error = false,
+		format,
+		gap = 2.5,
+		id: idProp,
+		incrementFromNonNumericValue = false,
+		isCommitOnBlurOrEnter = false,
+		isFocused: isFocusedProp,
+		isInline = false,
+		isResizable = false,
+		isShiftStepEnabled = true,
+		justify,
+		max,
+		min,
+		multiline = false,
+		prefix,
+		shiftStep = 10,
+		size = 'medium',
+		step,
+		suffix,
+		type = 'text',
+		validate,
+		value: valueProp,
+		...otherProps
+	} = useContextSystem( props, 'TextInput' );
+
+	const id = useFormGroupContextId( idProp );
+
+	const {
+		decrement,
+		dragHandlersRef,
+		increment,
+		inputRef,
+		isFocused,
+		isInputTypeNumeric,
+		isTypeNumeric,
+		scrollHandlers,
+		value,
+		...textInputState
+	} = useTextInputState( {
+		...otherProps,
+		format,
+		defaultValue,
+		incrementFromNonNumericValue,
+		isCommitOnBlurOrEnter,
+		isFocused: isFocusedProp,
+		isShiftStepEnabled,
+		max,
+		min,
+		shiftStep,
+		step,
+		value: valueProp,
+		validate,
+		type,
+	} );
+
+	const rootEventHandlers = useRootEventHandlers( {
+		inputRef,
+		dragHandlersRef,
+		isTypeNumeric,
+		isFocused,
+	} );
+
+	const baseFieldProps = useBaseField( {
+		align,
+		disabled,
+		error,
+		gap,
+		isFocused,
+		isInline,
+		justify,
+	} );
+
+	const InputComponent = multiline ? TextareaAutosize : 'input';
+
+	const classes = useMemo(
+		() =>
+			cx(
+				baseFieldProps.className,
+				multiline && styles.multiline,
+				className
+			),
+		[ baseFieldProps.className, className, multiline ]
+	);
+
+	const inputClasses = useMemo(
+		() =>
+			cx(
+				styles.Input,
```

Storybook is having trouble displaying this component. 

When it works, it borks around here. I'm not entirely sure why. 

Have I imported the styles correctly?

```bash
index.js:54 ReferenceError: Cannot access 'Input' before initialization
    at Module.Input (main.1bb8fefae19da236e3d5.hot-update.js:17)
    at use-text-input.js:150
    at mountMemo (react-dom.development.js:15442)
    at Object.useMemo (react-dom.development.js:15738)
    at useMemo (react.development.js:1521)
    at useTextInput (use-text-input.js:150)
    at TextInput (text-input.js:22)
    at renderWithHooks (react-dom.development.js:14803)
    at updateForwardRef (react-dom.development.js:16816)
    at beginWork (react-dom.development.js:18645)
```

### Review thread 2

#### ramonjd on 2021-06-03T09:37:36Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r644644059
Path: packages/components/src/utils/config-values.js

```diff
@@ -40,6 +40,7 @@ export default {
 	controlPaddingXLarge: `calc(${ CONTROL_PADDING_X } * 1.3334)`,
 	controlPaddingXSmall: `calc(${ CONTROL_PADDING_X } / 1.3334)`,
 	controlBackgroundColor: COLORS.white,
+	controlBackgroundColorHover: 'rgba(0, 0, 0, 0.5)',
```

I took this straight from G2's [controlBackgroundColorHover](https://github.com/ItsJonQ/g2/blob/60816e28df2ea7910997390ba16cd1d2d54c3197/packages/styles/src/theme/config.js#L91).

What's the best way to migrate these styles and know which is the default?

Or are we going to clean up/make things consistent across the board later?

### Review thread 3

#### ramonjd on 2021-06-03T09:38:21Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r644644617
Path: packages/components/src/utils/config-values.js

```diff
@@ -50,6 +51,8 @@ export default {
 	controlHeightLarge: `calc( ${ CONTROL_HEIGHT } * 1.2 )`,
 	controlHeightSmall: `calc( ${ CONTROL_HEIGHT } * 0.8 )`,
 	controlHeightXSmall: `calc( ${ CONTROL_HEIGHT } * 0.6 )`,
+	controlInnerControlTextColor: COLORS.darkGray,
+	controlTextColor: COLORS.darkGray,
```

Same as above. These are fairy arbitrary and are probably wrong, or belong in COLORS. 

I'm not aware of any system to follow 🤷

### Review thread 4 (resolved)

#### ramonjd on 2021-06-03T09:42:36Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r644647518
Path: packages/components/src/text-input/text-input.js

```diff
@@ -0,0 +1,85 @@
+/**
+ * WordPress dependencies
+ */
+import { useMergeRefs } from '@wordpress/compose';
+
+/**
+ * Internal dependencies
+ */
+import { contextConnect } from '../ui/context';
+import { View } from '../view';
+import TextInputArrows from './text-input-arrows';
+import TextInputSteppers from './text-input-steppers';
+import { useTextInput } from './hooks/use-text-input';
+import * as styles from './styles';
+
+/**
+ *
+ * @param {Object} props
```

This [was formally](https://github.com/ItsJonQ/g2/blob/main/packages/components/src/TextInput/TextInput.js#L13):

```js
* @param {import('@wp-g2/create-styles').ViewOwnProps<import('./types').Props, 'input'>} props
```

There was no `TextInput.d.ts` declaration file in https://github.com/ItsJonQ/g2/tree/main/packages/components/types/components so I'm a bit unsure about how to approach things. 

Plus my Typescript needs a little refresher :)

### Review thread 5 (resolved)

#### ramonjd on 2021-06-03T09:43:46Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r644648304
Path: packages/components/src/text-input/stories/index.js

```diff
@@ -0,0 +1,143 @@
+/**
+ * External dependencies
+ */
+import NumberFormat from 'react-number-format';
```

I've added this to `package.json` 

Is there an alternative or are we okay with adding more dependencies here?

#### sarayourfriend on 2021-06-15T16:31:19Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651960922
Path: packages/components/src/text-input/stories/index.js

```diff
@@ -0,0 +1,143 @@
+/**
+ * External dependencies
+ */
+import NumberFormat from 'react-number-format';
```

This one is actually bigger than the other: https://bundlephobia.com/package/react-number-format@4.6.3

If it's just for the story it's probably fine.

### Review thread 6 (resolved)

#### ramonjd on 2021-06-03T09:44:25Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r644648768
Path: packages/components/src/text-input/hooks/use-text-input.js

```diff
@@ -0,0 +1,194 @@
+/**
+ * External dependencies
+ */
+import { cx } from 'emotion';
+import TextareaAutosize from 'react-autosize-textarea';
```

I've added this to `package.json` 

Is there an alternative or are we okay with adding more dependencies here?

#### sarayourfriend on 2021-06-15T16:28:43Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651958920
Path: packages/components/src/text-input/hooks/use-text-input.js

```diff
@@ -0,0 +1,194 @@
+/**
+ * External dependencies
+ */
+import { cx } from 'emotion';
+import TextareaAutosize from 'react-autosize-textarea';
```

It should be okay to add this dependency: https://bundlephobia.com/package/react-autosize-textarea@7.1.0 it's pretty small.

### Review thread 7 (resolved)

#### ramonjd on 2021-06-04T02:57:57Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r645256182
Path: packages/components/src/text-input/text-input.js

```diff
@@ -0,0 +1,85 @@
+/**
+ * WordPress dependencies
+ */
+import { useMergeRefs } from '@wordpress/compose';
```

The compiler cannot find module '@wordpress/compose' or its corresponding type declarations.

For the FormGroup + ControlLabel components migration the method was manually copied into the directory.

https://github.com/WordPress/gutenberg/pull/28568#issuecomment-770091755

Is that something we want to do?

#### sarayourfriend on 2021-06-15T16:40:46Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651967930
Path: packages/components/src/text-input/text-input.js

```diff
@@ -0,0 +1,85 @@
+/**
+ * WordPress dependencies
+ */
+import { useMergeRefs } from '@wordpress/compose';
```

This will be fixed soon by #32676 when we'll be able to add a `references` entry in the `components/tsconfig.json` for `@wordpress/compose`. Then the TS compiler will be able to find this.

### Review thread 8 (resolved)

#### sarayourfriend on 2021-06-15T16:21:18Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651951960
Path: packages/components/src/text-input/README.md

```diff
@@ -0,0 +1,232 @@
+# TextInput
+
+`TextInput` is a form component that users can enter content into.
+
+## Table of contents
+
+<!-- START doctoc generated TOC please keep comment here to allow auto update -->
+<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
+
+
+- [Usage](#usage)
+- [Props](#props)
+- [See Also](#see-also)
+
+<!-- END doctoc generated TOC please keep comment here to allow auto update -->
+
+<!-- Automatically Generated. DO NOT EDIT THIS FILE. -->
+<!-- Instead, edit packages/website/src/docs/components/forms/textinput.mdx -->
+
+<!-- props -->
+
+<!-- Automatically Generated -->
```

We should remove this from the README as well as add the `experimental` callout that we've added to the other experimental components.

### Review thread 9 (resolved)

#### sarayourfriend on 2021-06-15T16:21:34Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651952172
Path: packages/components/src/text-input/README.md

```diff
@@ -0,0 +1,232 @@
+# TextInput
+
+`TextInput` is a form component that users can enter content into.
+
+## Table of contents
+
+<!-- START doctoc generated TOC please keep comment here to allow auto update -->
+<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
+
+
+- [Usage](#usage)
+- [Props](#props)
+- [See Also](#see-also)
+
+<!-- END doctoc generated TOC please keep comment here to allow auto update -->
+
+<!-- Automatically Generated. DO NOT EDIT THIS FILE. -->
+<!-- Instead, edit packages/website/src/docs/components/forms/textinput.mdx -->
+
+<!-- props -->
+
+<!-- Automatically Generated -->
+
+## Usage
+
+```jsx live
+import { TextInput } from '@wp-g2/components';
```

```suggestion
import { __experimentalTextInput as TextInput } from '@wordpress/components';
```

### Review thread 10 (resolved)

#### sarayourfriend on 2021-06-15T16:22:15Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651952764
Path: packages/components/src/text-input/hooks/use-text-input-state-utils.js

```diff
@@ -0,0 +1,114 @@
+/**
+ * External dependencies
+ */
+import { noop } from 'lodash';
+import { useDrag } from 'react-use-gesture';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+
+import { clearSelection } from '../../utils/clear-selection';
+
+import * as styles from '../styles';
+
+/**
+ * @typedef Props
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {string} [dragAxis] The drag axis, e.g., `x` or `y`.
+ */
+
+/**
+ * @param {Props} props
+ * @return {Object} gestures
```

If we remove the return type TypeScript should correctly infer the return type without us needing to specify it. Just need to delete this line.

### Review thread 11 (resolved)

#### sarayourfriend on 2021-06-15T16:23:47Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651954348
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
```

We should fix this return type. I think it should be something like `[T, (value: T) => void, () => void]`
```suggestion
 * @return {[T, (value: T) => void, () => void]} The controlled value and the value setter.
```

### Review thread 12 (resolved)

#### sarayourfriend on 2021-06-15T16:24:29Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651954865
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
```

```suggestion
/**
 * @param {Object} props
 * @param {boolean} [props.isShiftStepEnable]
 * @param {number} [props.shiftStep]
 * @return {boolean}
 */
function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
```

### Review thread 13 (resolved)

#### sarayourfriend on 2021-06-15T16:25:03Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651955354
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
```

This needs type definitions.

### Review thread 14

#### sarayourfriend on 2021-06-15T16:25:48Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651955918
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
+	const [ isFocused, setFocused ] = useState( isFocusedProp );
+
+	useEffect( () => {
+		setFocused( isFocusedProp );
+	}, [ isFocusedProp ] );
+
+	return [ isFocused, setFocused ];
+}
+
+/**
+ * @param {Object} useChangeHandlersProps
+ * @param {Function} useChangeHandlersProps.onChange
+ * @return {Object} A collection of change handlers.
```

We can remove the return type as TS will infer it, but the `Function` type is too broad, it should be something like `(value: string) => void` I think.

### Review thread 15

#### sarayourfriend on 2021-06-15T16:26:02Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651956182
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
+	const [ isFocused, setFocused ] = useState( isFocusedProp );
+
+	useEffect( () => {
+		setFocused( isFocusedProp );
+	}, [ isFocusedProp ] );
+
+	return [ isFocused, setFocused ];
+}
+
+/**
+ * @param {Object} useChangeHandlersProps
+ * @param {Function} useChangeHandlersProps.onChange
+ * @return {Object} A collection of change handlers.
+ */
+function useChangeHandlers( { onChange } ) {
+	const handleOnChange = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+		},
+		[ onChange ]
+	);
+
+	return {
+		onChange: handleOnChange,
+	};
+}
+
+/**
+ * @param {Object} useFocusHandlersProps
+ * @param {Function} useFocusHandlersProps.onChange
+ * @param {Function} useFocusHandlersProps.setFocused
```

Same here, these Function types are too broad.

### Review thread 16

#### sarayourfriend on 2021-06-15T16:26:33Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651956856
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
+	const [ isFocused, setFocused ] = useState( isFocusedProp );
+
+	useEffect( () => {
+		setFocused( isFocusedProp );
+	}, [ isFocusedProp ] );
+
+	return [ isFocused, setFocused ];
+}
+
+/**
+ * @param {Object} useChangeHandlersProps
+ * @param {Function} useChangeHandlersProps.onChange
+ * @return {Object} A collection of change handlers.
+ */
+function useChangeHandlers( { onChange } ) {
+	const handleOnChange = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+		},
+		[ onChange ]
+	);
+
+	return {
+		onChange: handleOnChange,
+	};
+}
+
+/**
+ * @param {Object} useFocusHandlersProps
+ * @param {Function} useFocusHandlersProps.onChange
+ * @param {Function} useFocusHandlersProps.setFocused
+ * @return {Object} A collection of focus handlers.
+ */
+function useFocusHandlers( { onChange, setFocused } ) {
+	const handleOnBlur = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+			setFocused( false );
+		},
+		[ onChange, setFocused ]
+	);
+
+	const handleOnFocus = useCallback( () => {
+		setFocused( true );
+	}, [ setFocused ] );
+
+	return {
+		onBlur: handleOnBlur,
+		onFocus: handleOnFocus,
+	};
+}
+
+/**
+ * @param {Object} useKeyboardHandlersProps
+ * @param {Function} useKeyboardHandlersProps.onChange
+ * @return {Object} A collection of keyboard handlers.
+ */
+function useKeyboardHandlers( { onChange } ) {
+	const keyboardHandlers = useMemo(
+		() => ( {
+			Enter(
+				/** @type {import('react').KeyboardEvent<HTMLInputElement>} */ event
+			) {
+				if ( event.isDefaultPrevented() ) {
+					return;
+				}
+				onChange( event.currentTarget.value );
+			},
+		} ),
+		[ onChange ]
+	);
+
+	const handleOnKeyDown = useCallback(
+		( /** @type {import('react').KeyboardEvent}} */ event ) => {
+			const key = normalizeArrowKey( event );
+			if ( key && keyboardHandlers[ key ] ) {
+				keyboardHandlers[ key ]( event );
+			}
+		},
+		[ keyboardHandlers ]
+	);
+
+	return {
+		onKeyDown: handleOnKeyDown,
+	};
+}
+
+/**
+ * @typedef Props
+ * @property {number|string|undefined|null} [value] On change callback.
+ * @property {Function} [onChange] On change callback.
+ * @property {string} [type] Value type.
+ * @property {number} [max] Max value.
+ * @property {number} [min] Min value.
+ * @property {number} [shiftStep] Shift step value.
+ * @property {number} [step] Step value.
+ * @property {boolean} [isShiftStepEnabled] Is shift step enabled?
+ * @property {boolean} [incrementFromNonNumericValue] Increment from non-numeric value.
+ */
+
+/**
+ * @param {Props} props
+ * @return {Object} Return object
```

We should remove this return type and let TS infer it.

### Review thread 17

#### sarayourfriend on 2021-06-15T16:26:50Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651957276
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
+	const [ isFocused, setFocused ] = useState( isFocusedProp );
+
+	useEffect( () => {
+		setFocused( isFocusedProp );
+	}, [ isFocusedProp ] );
+
+	return [ isFocused, setFocused ];
+}
+
+/**
+ * @param {Object} useChangeHandlersProps
+ * @param {Function} useChangeHandlersProps.onChange
+ * @return {Object} A collection of change handlers.
+ */
+function useChangeHandlers( { onChange } ) {
+	const handleOnChange = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+		},
+		[ onChange ]
+	);
+
+	return {
+		onChange: handleOnChange,
+	};
+}
+
+/**
+ * @param {Object} useFocusHandlersProps
+ * @param {Function} useFocusHandlersProps.onChange
+ * @param {Function} useFocusHandlersProps.setFocused
+ * @return {Object} A collection of focus handlers.
+ */
+function useFocusHandlers( { onChange, setFocused } ) {
+	const handleOnBlur = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+			setFocused( false );
+		},
+		[ onChange, setFocused ]
+	);
+
+	const handleOnFocus = useCallback( () => {
+		setFocused( true );
+	}, [ setFocused ] );
+
+	return {
+		onBlur: handleOnBlur,
+		onFocus: handleOnFocus,
+	};
+}
+
+/**
+ * @param {Object} useKeyboardHandlersProps
+ * @param {Function} useKeyboardHandlersProps.onChange
+ * @return {Object} A collection of keyboard handlers.
+ */
+function useKeyboardHandlers( { onChange } ) {
+	const keyboardHandlers = useMemo(
+		() => ( {
+			Enter(
+				/** @type {import('react').KeyboardEvent<HTMLInputElement>} */ event
+			) {
+				if ( event.isDefaultPrevented() ) {
+					return;
+				}
+				onChange( event.currentTarget.value );
+			},
+		} ),
+		[ onChange ]
+	);
+
+	const handleOnKeyDown = useCallback(
+		( /** @type {import('react').KeyboardEvent}} */ event ) => {
+			const key = normalizeArrowKey( event );
+			if ( key && keyboardHandlers[ key ] ) {
+				keyboardHandlers[ key ]( event );
+			}
+		},
+		[ keyboardHandlers ]
+	);
+
+	return {
+		onKeyDown: handleOnKeyDown,
+	};
+}
+
+/**
+ * @typedef Props
+ * @property {number|string|undefined|null} [value] On change callback.
+ * @property {Function} [onChange] On change callback.
+ * @property {string} [type] Value type.
+ * @property {number} [max] Max value.
+ * @property {number} [min] Min value.
+ * @property {number} [shiftStep] Shift step value.
+ * @property {number} [step] Step value.
+ * @property {boolean} [isShiftStepEnabled] Is shift step enabled?
+ * @property {boolean} [incrementFromNonNumericValue] Increment from non-numeric value.
+ */
+
+/**
+ * @param {Props} props
+ * @return {Object} Return object
+ */
+function useNumberActions( {
+	incrementFromNonNumericValue,
+	isShiftStepEnabled,
+	max,
+	min,
+	onChange,
+	shiftStep: shiftStepProp = 10,
+	step = 1,
+	type,
+	value,
+} ) {
+	const stepMultiplier =
+		useShiftStep( {
+			isShiftStepEnabled,
+			shiftStep: shiftStepProp,
+		} ) || step;
+	const shiftStep = stepMultiplier * step;
+
+	const isInputTypeNumeric = type === 'number';
+	const isNumeric = isValueNumeric( value );
+
+	const skipAction =
+		! isInputTypeNumeric && ! isNumeric && ! incrementFromNonNumericValue;
+
+	/**
+	 * Create (synced) references to avoid recreating increment and decrement
+	 * callbacks.
+	 */
+	const propRefs = usePropRef( {
+		min,
+		max,
+		value,
+		shiftStep,
+		onChange,
+	} );
+
+	const increment = useCallback(
+		( /** @type {number} */ jumpStep = 0 ) => {
+			if ( skipAction ) return;
+			// TODO: rename variables, since they're in the upper scope?
+			// eslint-disable-next-line no-shadow
+			const { max, min, onChange, shiftStep, value } = propRefs.current;
+			const baseValue = isValueNumeric( value ) ? value : 0;
+			const nextValue = add( jumpStep * step, shiftStep );
+			const next = roundClampString(
+				add( baseValue, nextValue ),
+				min,
+				max,
+				shiftStep
+			);
+
+			onChange( next );
+		},
+		[ skipAction, propRefs, step ]
+	);
+
+	const decrement = useCallback(
+		( /** @type {number} */ jumpStep = 0 ) => {
+			if ( skipAction ) return;
+			// TODO: rename variables, since they're in the upper scope?
+			// eslint-disable-next-line no-shadow
+			const { max, min, onChange, shiftStep, value } = propRefs.current;
+			const baseValue = isValueNumeric( value ) ? value : 0;
+			const nextValue = add( jumpStep * step, shiftStep );
+			const next = roundClampString(
+				subtract( baseValue, nextValue ),
+				min,
+				max,
+				shiftStep
+			);
+
+			onChange( next );
+		},
+		[ skipAction, propRefs, step ]
+	);
+
+	return { increment, decrement };
+}
+
+/**
+ * @typedef UseNumberKeyboardHandlersProps
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {boolean} [stopIfEventDefaultPrevented] Stop if event default prevented.
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ */
+
+/**
+ *
+ * @param {UseNumberKeyboardHandlersProps} props
+ * @return {Object} Return object
```

Same here, just remove the return type and let TypeScript infer it.

### Review thread 18

#### sarayourfriend on 2021-06-15T16:27:11Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651957691
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
+	const [ isFocused, setFocused ] = useState( isFocusedProp );
+
+	useEffect( () => {
+		setFocused( isFocusedProp );
+	}, [ isFocusedProp ] );
+
+	return [ isFocused, setFocused ];
+}
+
+/**
+ * @param {Object} useChangeHandlersProps
+ * @param {Function} useChangeHandlersProps.onChange
+ * @return {Object} A collection of change handlers.
+ */
+function useChangeHandlers( { onChange } ) {
+	const handleOnChange = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+		},
+		[ onChange ]
+	);
+
+	return {
+		onChange: handleOnChange,
+	};
+}
+
+/**
+ * @param {Object} useFocusHandlersProps
+ * @param {Function} useFocusHandlersProps.onChange
+ * @param {Function} useFocusHandlersProps.setFocused
+ * @return {Object} A collection of focus handlers.
+ */
+function useFocusHandlers( { onChange, setFocused } ) {
+	const handleOnBlur = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+			setFocused( false );
+		},
+		[ onChange, setFocused ]
+	);
+
+	const handleOnFocus = useCallback( () => {
+		setFocused( true );
+	}, [ setFocused ] );
+
+	return {
+		onBlur: handleOnBlur,
+		onFocus: handleOnFocus,
+	};
+}
+
+/**
+ * @param {Object} useKeyboardHandlersProps
+ * @param {Function} useKeyboardHandlersProps.onChange
+ * @return {Object} A collection of keyboard handlers.
+ */
+function useKeyboardHandlers( { onChange } ) {
+	const keyboardHandlers = useMemo(
+		() => ( {
+			Enter(
+				/** @type {import('react').KeyboardEvent<HTMLInputElement>} */ event
+			) {
+				if ( event.isDefaultPrevented() ) {
+					return;
+				}
+				onChange( event.currentTarget.value );
+			},
+		} ),
+		[ onChange ]
+	);
+
+	const handleOnKeyDown = useCallback(
+		( /** @type {import('react').KeyboardEvent}} */ event ) => {
+			const key = normalizeArrowKey( event );
+			if ( key && keyboardHandlers[ key ] ) {
+				keyboardHandlers[ key ]( event );
+			}
+		},
+		[ keyboardHandlers ]
+	);
+
+	return {
+		onKeyDown: handleOnKeyDown,
+	};
+}
+
+/**
+ * @typedef Props
+ * @property {number|string|undefined|null} [value] On change callback.
+ * @property {Function} [onChange] On change callback.
+ * @property {string} [type] Value type.
+ * @property {number} [max] Max value.
+ * @property {number} [min] Min value.
+ * @property {number} [shiftStep] Shift step value.
+ * @property {number} [step] Step value.
+ * @property {boolean} [isShiftStepEnabled] Is shift step enabled?
+ * @property {boolean} [incrementFromNonNumericValue] Increment from non-numeric value.
+ */
+
+/**
+ * @param {Props} props
+ * @return {Object} Return object
+ */
+function useNumberActions( {
+	incrementFromNonNumericValue,
+	isShiftStepEnabled,
+	max,
+	min,
+	onChange,
+	shiftStep: shiftStepProp = 10,
+	step = 1,
+	type,
+	value,
+} ) {
+	const stepMultiplier =
+		useShiftStep( {
+			isShiftStepEnabled,
+			shiftStep: shiftStepProp,
+		} ) || step;
+	const shiftStep = stepMultiplier * step;
+
+	const isInputTypeNumeric = type === 'number';
+	const isNumeric = isValueNumeric( value );
+
+	const skipAction =
+		! isInputTypeNumeric && ! isNumeric && ! incrementFromNonNumericValue;
+
+	/**
+	 * Create (synced) references to avoid recreating increment and decrement
+	 * callbacks.
+	 */
+	const propRefs = usePropRef( {
+		min,
+		max,
+		value,
+		shiftStep,
+		onChange,
+	} );
+
+	const increment = useCallback(
+		( /** @type {number} */ jumpStep = 0 ) => {
+			if ( skipAction ) return;
+			// TODO: rename variables, since they're in the upper scope?
+			// eslint-disable-next-line no-shadow
+			const { max, min, onChange, shiftStep, value } = propRefs.current;
+			const baseValue = isValueNumeric( value ) ? value : 0;
+			const nextValue = add( jumpStep * step, shiftStep );
+			const next = roundClampString(
+				add( baseValue, nextValue ),
+				min,
+				max,
+				shiftStep
+			);
+
+			onChange( next );
+		},
+		[ skipAction, propRefs, step ]
+	);
+
+	const decrement = useCallback(
+		( /** @type {number} */ jumpStep = 0 ) => {
+			if ( skipAction ) return;
+			// TODO: rename variables, since they're in the upper scope?
+			// eslint-disable-next-line no-shadow
+			const { max, min, onChange, shiftStep, value } = propRefs.current;
+			const baseValue = isValueNumeric( value ) ? value : 0;
+			const nextValue = add( jumpStep * step, shiftStep );
+			const next = roundClampString(
+				subtract( baseValue, nextValue ),
+				min,
+				max,
+				shiftStep
+			);
+
+			onChange( next );
+		},
+		[ skipAction, propRefs, step ]
+	);
+
+	return { increment, decrement };
+}
+
+/**
+ * @typedef UseNumberKeyboardHandlersProps
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {boolean} [stopIfEventDefaultPrevented] Stop if event default prevented.
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ */
+
+/**
+ *
+ * @param {UseNumberKeyboardHandlersProps} props
+ * @return {Object} Return object
+ */
+function useNumberKeyboardHandlers( {
+	decrement = noop,
+	increment = noop,
+	isTypeNumeric,
+	stopIfEventDefaultPrevented = true,
+} ) {
+	const keyboardHandlers = useMemo(
+		() => ( {
+			ArrowUp( /** @type {import('react').KeyboardEvent}} */ event ) {
+				if ( ! isTypeNumeric ) return;
+
+				if ( stopIfEventDefaultPrevented && event.isDefaultPrevented() )
+					return;
+
+				event.preventDefault();
+
+				increment();
+			},
+			ArrowDown( /** @type {import('react').KeyboardEvent}} */ event ) {
+				if ( ! isTypeNumeric ) return;
+
+				if ( stopIfEventDefaultPrevented && event.isDefaultPrevented() )
+					return;
+
+				event.preventDefault();
+
+				decrement();
+			},
+		} ),
+		[ decrement, increment, isTypeNumeric, stopIfEventDefaultPrevented ]
+	);
+
+	const handleOnKeyDown = useCallback(
+		( event ) => {
```

We'll need to add a type annotation to `event` for TS to correctly infer the return type of the hook.

### Review thread 19 (resolved)

#### sarayourfriend on 2021-06-15T16:27:44Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651958145
Path: packages/components/src/text-input/hooks/use-text-input-state.js

```diff
@@ -0,0 +1,494 @@
+/**
+ * External dependencies
+ */
+import { isNil, noop } from 'lodash';
+
+/**
+ * WordPress dependencies
+ */
+import {
+	useCallback,
+	useEffect,
+	useMemo,
+	useRef,
+	useState,
+} from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useBaseDragHandlers } from './use-text-input-state-utils';
+import { isValueNumeric } from '../../utils/values';
+import { add, roundClampString, subtract } from '../../utils/math';
+import { mergeEventHandlers } from '../../utils/events';
+import { normalizeArrowKey } from '../../utils/keyboard';
+import { useControlledValue, usePropRef } from '../../utils';
+
+/**
+ * @template T
+ * @param {Object} props
+ * @param {T} [props.value]
+ * @return {Array<any|Function>} The controlled value and the value setter.
+ */
+function useCommitValue( { value } ) {
+	const [ commitValue, setCommitValue ] = useState( null );
+	const resetCommitValue = useCallback( () => setCommitValue( null ), [] );
+
+	useEffect( resetCommitValue, [ value ] );
+
+	return [ commitValue, setCommitValue, resetCommitValue ];
+}
+
+function useShiftStep( { isShiftStepEnabled = true, shiftStep = 10 } ) {
+	const [ on, setOn ] = useState( false );
+	useEffect( () => {
+		const handleOnKeyDown = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( true );
+			}
+		};
+		const handleOnKeyUp = ( /** @type {KeyboardEvent}} */ event ) => {
+			if ( ! isShiftStepEnabled ) {
+				return;
+			}
+			if ( event.shiftKey ) {
+				setOn( false );
+			}
+		};
+
+		window.addEventListener( 'keydown', handleOnKeyDown );
+		window.addEventListener( 'keyup', handleOnKeyUp );
+
+		return () => {
+			window.removeEventListener( 'keydown', handleOnKeyDown );
+			window.removeEventListener( 'keyup', handleOnKeyUp );
+		};
+	}, [ isShiftStepEnabled ] );
+
+	return isShiftStepEnabled && on ? shiftStep : 1;
+}
+
+function useFocusedState( { isFocused: isFocusedProp = false } ) {
+	const [ isFocused, setFocused ] = useState( isFocusedProp );
+
+	useEffect( () => {
+		setFocused( isFocusedProp );
+	}, [ isFocusedProp ] );
+
+	return [ isFocused, setFocused ];
+}
+
+/**
+ * @param {Object} useChangeHandlersProps
+ * @param {Function} useChangeHandlersProps.onChange
+ * @return {Object} A collection of change handlers.
+ */
+function useChangeHandlers( { onChange } ) {
+	const handleOnChange = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+		},
+		[ onChange ]
+	);
+
+	return {
+		onChange: handleOnChange,
+	};
+}
+
+/**
+ * @param {Object} useFocusHandlersProps
+ * @param {Function} useFocusHandlersProps.onChange
+ * @param {Function} useFocusHandlersProps.setFocused
+ * @return {Object} A collection of focus handlers.
+ */
+function useFocusHandlers( { onChange, setFocused } ) {
+	const handleOnBlur = useCallback(
+		( event ) => {
+			onChange( event.target.value );
+			setFocused( false );
+		},
+		[ onChange, setFocused ]
+	);
+
+	const handleOnFocus = useCallback( () => {
+		setFocused( true );
+	}, [ setFocused ] );
+
+	return {
+		onBlur: handleOnBlur,
+		onFocus: handleOnFocus,
+	};
+}
+
+/**
+ * @param {Object} useKeyboardHandlersProps
+ * @param {Function} useKeyboardHandlersProps.onChange
+ * @return {Object} A collection of keyboard handlers.
+ */
+function useKeyboardHandlers( { onChange } ) {
+	const keyboardHandlers = useMemo(
+		() => ( {
+			Enter(
+				/** @type {import('react').KeyboardEvent<HTMLInputElement>} */ event
+			) {
+				if ( event.isDefaultPrevented() ) {
+					return;
+				}
+				onChange( event.currentTarget.value );
+			},
+		} ),
+		[ onChange ]
+	);
+
+	const handleOnKeyDown = useCallback(
+		( /** @type {import('react').KeyboardEvent}} */ event ) => {
+			const key = normalizeArrowKey( event );
+			if ( key && keyboardHandlers[ key ] ) {
+				keyboardHandlers[ key ]( event );
+			}
+		},
+		[ keyboardHandlers ]
+	);
+
+	return {
+		onKeyDown: handleOnKeyDown,
+	};
+}
+
+/**
+ * @typedef Props
+ * @property {number|string|undefined|null} [value] On change callback.
+ * @property {Function} [onChange] On change callback.
+ * @property {string} [type] Value type.
+ * @property {number} [max] Max value.
+ * @property {number} [min] Min value.
+ * @property {number} [shiftStep] Shift step value.
+ * @property {number} [step] Step value.
+ * @property {boolean} [isShiftStepEnabled] Is shift step enabled?
+ * @property {boolean} [incrementFromNonNumericValue] Increment from non-numeric value.
+ */
+
+/**
+ * @param {Props} props
+ * @return {Object} Return object
+ */
+function useNumberActions( {
+	incrementFromNonNumericValue,
+	isShiftStepEnabled,
+	max,
+	min,
+	onChange,
+	shiftStep: shiftStepProp = 10,
+	step = 1,
+	type,
+	value,
+} ) {
+	const stepMultiplier =
+		useShiftStep( {
+			isShiftStepEnabled,
+			shiftStep: shiftStepProp,
+		} ) || step;
+	const shiftStep = stepMultiplier * step;
+
+	const isInputTypeNumeric = type === 'number';
+	const isNumeric = isValueNumeric( value );
+
+	const skipAction =
+		! isInputTypeNumeric && ! isNumeric && ! incrementFromNonNumericValue;
+
+	/**
+	 * Create (synced) references to avoid recreating increment and decrement
+	 * callbacks.
+	 */
+	const propRefs = usePropRef( {
+		min,
+		max,
+		value,
+		shiftStep,
+		onChange,
+	} );
+
+	const increment = useCallback(
+		( /** @type {number} */ jumpStep = 0 ) => {
+			if ( skipAction ) return;
+			// TODO: rename variables, since they're in the upper scope?
+			// eslint-disable-next-line no-shadow
+			const { max, min, onChange, shiftStep, value } = propRefs.current;
+			const baseValue = isValueNumeric( value ) ? value : 0;
+			const nextValue = add( jumpStep * step, shiftStep );
+			const next = roundClampString(
+				add( baseValue, nextValue ),
+				min,
+				max,
+				shiftStep
+			);
+
+			onChange( next );
+		},
+		[ skipAction, propRefs, step ]
+	);
+
+	const decrement = useCallback(
+		( /** @type {number} */ jumpStep = 0 ) => {
+			if ( skipAction ) return;
+			// TODO: rename variables, since they're in the upper scope?
+			// eslint-disable-next-line no-shadow
+			const { max, min, onChange, shiftStep, value } = propRefs.current;
+			const baseValue = isValueNumeric( value ) ? value : 0;
+			const nextValue = add( jumpStep * step, shiftStep );
+			const next = roundClampString(
+				subtract( baseValue, nextValue ),
+				min,
+				max,
+				shiftStep
+			);
+
+			onChange( next );
+		},
+		[ skipAction, propRefs, step ]
+	);
+
+	return { increment, decrement };
+}
+
+/**
+ * @typedef UseNumberKeyboardHandlersProps
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {boolean} [stopIfEventDefaultPrevented] Stop if event default prevented.
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ */
+
+/**
+ *
+ * @param {UseNumberKeyboardHandlersProps} props
+ * @return {Object} Return object
+ */
+function useNumberKeyboardHandlers( {
+	decrement = noop,
+	increment = noop,
+	isTypeNumeric,
+	stopIfEventDefaultPrevented = true,
+} ) {
+	const keyboardHandlers = useMemo(
+		() => ( {
+			ArrowUp( /** @type {import('react').KeyboardEvent}} */ event ) {
+				if ( ! isTypeNumeric ) return;
+
+				if ( stopIfEventDefaultPrevented && event.isDefaultPrevented() )
+					return;
+
+				event.preventDefault();
+
+				increment();
+			},
+			ArrowDown( /** @type {import('react').KeyboardEvent}} */ event ) {
+				if ( ! isTypeNumeric ) return;
+
+				if ( stopIfEventDefaultPrevented && event.isDefaultPrevented() )
+					return;
+
+				event.preventDefault();
+
+				decrement();
+			},
+		} ),
+		[ decrement, increment, isTypeNumeric, stopIfEventDefaultPrevented ]
+	);
+
+	const handleOnKeyDown = useCallback(
+		( event ) => {
+			const key = normalizeArrowKey( event );
+			if ( key && keyboardHandlers[ key ] ) {
+				keyboardHandlers[ key ]( event );
+			}
+		},
+		[ keyboardHandlers ]
+	);
+
+	return {
+		onKeyDown: handleOnKeyDown,
+	};
+}
+
+/**
+ * @typedef UseScrollHandlersProps
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {boolean} [isFocused] Is the field focussed.
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ */
+
+/**
+ *
+ * @param {UseScrollHandlersProps} props
+ * @return {Object} Return object
+ */
+const useScrollHandlers = ( {
+	decrement,
+	increment,
+	isFocused,
+	isTypeNumeric,
+} ) => {
+	const handleOnWheel = useCallback(
+		( event ) => {
+			if ( ! isTypeNumeric ) return;
+			if ( ! isFocused ) return;
+			if ( event?.deltaY === 0 ) return;
+
+			const isScrollUp = event?.deltaY < 0;
+
+			if ( isScrollUp ) {
+				increment();
+			} else {
+				decrement();
+			}
+
+			return false;
+		},
+		[ decrement, increment, isFocused, isTypeNumeric ]
+	);
+
+	return {
+		onWheel: handleOnWheel,
+	};
+};
+
+export function useTextInputState( props ) {
```

We should add a type to `props` here.

### Review thread 20

#### sarayourfriend on 2021-06-15T16:29:03Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651959191
Path: packages/components/src/text-input/hooks/use-text-input.js

```diff
@@ -0,0 +1,222 @@
+/**
+ * External dependencies
+ */
+import { cx } from 'emotion';
+import TextareaAutosize from 'react-autosize-textarea';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useContextSystem } from '../../ui/context';
+import { useBaseField } from '../../base-field';
+import { useFormGroupContextId } from '../../ui/form-group';
+import * as styles from '../styles';
+import { useTextInputState } from './use-text-input-state';
+
+/**
+ * @typedef Props
+ * @property {import('react').MutableRefObject<null | HTMLElement>} [dragHandlersRef] Renders an error.
+ * @property {import('react').RefObject<HTMLInputElement>} [inputRef] Whether the field is disabled.
```

These prop descriptions are wrong.

### Review thread 21

#### sarayourfriend on 2021-06-15T16:29:58Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651959944
Path: packages/components/src/text-input/hooks/use-text-input.js

```diff
@@ -0,0 +1,222 @@
+/**
+ * External dependencies
+ */
+import { cx } from 'emotion';
+import TextareaAutosize from 'react-autosize-textarea';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useContextSystem } from '../../ui/context';
+import { useBaseField } from '../../base-field';
+import { useFormGroupContextId } from '../../ui/form-group';
+import * as styles from '../styles';
+import { useTextInputState } from './use-text-input-state';
+
+/**
+ * @typedef Props
+ * @property {import('react').MutableRefObject<null | HTMLElement>} [dragHandlersRef] Renders an error.
+ * @property {import('react').RefObject<HTMLInputElement>} [inputRef] Whether the field is disabled.
+ * @property {boolean} [isFocused] Renders focus styles.
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ */
+
+/**
+ * @param {Props} props
+ */
+const useRootEventHandlers = ( {
+	dragHandlersRef,
+	inputRef,
+	isFocused,
+	isTypeNumeric,
+} ) => {
+	const canScroll = isTypeNumeric && isFocused;
+	const dragHandlers = dragHandlersRef.current;
+
+	useEffect( () => {
+		const handleOnWheel = () => {};
+		if ( inputRef.current.addEventListener ) {
+			inputRef.current.addEventListener( 'wheel', handleOnWheel, {
+				passive: false,
+			} );
+		}
+	}, [ inputRef, canScroll ] );
+
+	const handleOnClick = useCallback( () => {
+		inputRef.current.focus();
+	}, [ inputRef ] );
+
+	const handleOnTouchStart = useCallback( () => {
+		inputRef.current.focus();
+	}, [ inputRef ] );
+
+	return {
+		...dragHandlers,
+		onClick: handleOnClick,
+		onTouchStart: handleOnTouchStart,
+	};
+};
+
+/**
+ * @typedef ReturnProps
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ * @property {import('react').MutableRefObject<null | HTMLElement>} [dragHandlersRef] Renders an error.
+ * @property {import('react').RefObject<HTMLInputElement>} [inputRef] Whether the field is disabled.
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {boolean} [isInputTypeNumeric] Whether the input type is numeric.
+ * @property {string} [className] Classes
+ * @property {Object} [inputProps] Input props
+ */
+
+/** @typedef {import('../types').Props & ReturnProps} ReturnPropsObject */
+
+/**
+ *
+ * @param  {import('../../ui/context').PolymorphicComponentProps<import('../types').Props, 'input'>} props
+ * @return {ReturnPropsObject} props
+ */
+export function useTextInput( props ) {
+	const {
+		align,
+		arrows = true,
+		className,
+		defaultValue = '',
+		disabled,
+		dragAxis,
+		error = false,
+		format,
+		gap = 2.5,
+		id: idProp,
+		incrementFromNonNumericValue = false,
+		isCommitOnBlurOrEnter = false,
+		isFocused: isFocusedProp,
+		isInline = false,
+		isResizable = false,
+		isShiftStepEnabled = true,
+		justify,
+		max,
+		min,
+		multiline = false,
+		prefix,
+		shiftStep = 10,
+		size = 'medium',
+		step,
+		suffix,
+		type = 'text',
+		validate,
+		value: valueProp,
+		...otherProps
+	} = useContextSystem( props, 'TextInput' );
+
+	const id = useFormGroupContextId( idProp );
+
+	const {
+		decrement,
+		dragHandlersRef,
+		increment,
+		inputRef,
+		isFocused,
+		isInputTypeNumeric,
+		isTypeNumeric,
+		scrollHandlers,
+		value,
+		...textInputState
+	} = useTextInputState( {
+		...otherProps,
+		format,
+		defaultValue,
+		incrementFromNonNumericValue,
+		isCommitOnBlurOrEnter,
+		isFocused: isFocusedProp,
+		isShiftStepEnabled,
+		max,
+		min,
+		shiftStep,
+		step,
+		value: valueProp,
+		validate,
+		type,
+	} );
+
+	const rootEventHandlers = useRootEventHandlers( {
+		inputRef,
+		dragHandlersRef,
+		isTypeNumeric,
+		isFocused,
+	} );
+
+	const baseFieldProps = useBaseField( {
+		align,
+		disabled,
+		// @ts-ignore Looks like we need to extend the Props @typedef here? packages/components/src/base-field/hook.js
```

`useBaseField` was changed from the original G2 implementation, we can just remove/rename (in the case of error it was renamed to `hasError` I believe) the props it no longer accepts.

#### ramonjd on 2021-06-16T06:34:47Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652390045
Path: packages/components/src/text-input/hooks/use-text-input.js

```diff
@@ -0,0 +1,222 @@
+/**
+ * External dependencies
+ */
+import { cx } from 'emotion';
+import TextareaAutosize from 'react-autosize-textarea';
+
+/**
+ * WordPress dependencies
+ */
+import { useCallback, useEffect, useMemo } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import { useContextSystem } from '../../ui/context';
+import { useBaseField } from '../../base-field';
+import { useFormGroupContextId } from '../../ui/form-group';
+import * as styles from '../styles';
+import { useTextInputState } from './use-text-input-state';
+
+/**
+ * @typedef Props
+ * @property {import('react').MutableRefObject<null | HTMLElement>} [dragHandlersRef] Renders an error.
+ * @property {import('react').RefObject<HTMLInputElement>} [inputRef] Whether the field is disabled.
+ * @property {boolean} [isFocused] Renders focus styles.
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ */
+
+/**
+ * @param {Props} props
+ */
+const useRootEventHandlers = ( {
+	dragHandlersRef,
+	inputRef,
+	isFocused,
+	isTypeNumeric,
+} ) => {
+	const canScroll = isTypeNumeric && isFocused;
+	const dragHandlers = dragHandlersRef.current;
+
+	useEffect( () => {
+		const handleOnWheel = () => {};
+		if ( inputRef.current.addEventListener ) {
+			inputRef.current.addEventListener( 'wheel', handleOnWheel, {
+				passive: false,
+			} );
+		}
+	}, [ inputRef, canScroll ] );
+
+	const handleOnClick = useCallback( () => {
+		inputRef.current.focus();
+	}, [ inputRef ] );
+
+	const handleOnTouchStart = useCallback( () => {
+		inputRef.current.focus();
+	}, [ inputRef ] );
+
+	return {
+		...dragHandlers,
+		onClick: handleOnClick,
+		onTouchStart: handleOnTouchStart,
+	};
+};
+
+/**
+ * @typedef ReturnProps
+ * @property {(int: number) => void} [increment] Increment text input number value callback.
+ * @property {(int: number) => void} [decrement] Decrement text input number value callback.
+ * @property {import('react').MutableRefObject<null | HTMLElement>} [dragHandlersRef] Renders an error.
+ * @property {import('react').RefObject<HTMLInputElement>} [inputRef] Whether the field is disabled.
+ * @property {boolean} [isTypeNumeric] Whether the type is numeric.
+ * @property {boolean} [isInputTypeNumeric] Whether the input type is numeric.
+ * @property {string} [className] Classes
+ * @property {Object} [inputProps] Input props
+ */
+
+/** @typedef {import('../types').Props & ReturnProps} ReturnPropsObject */
+
+/**
+ *
+ * @param  {import('../../ui/context').PolymorphicComponentProps<import('../types').Props, 'input'>} props
+ * @return {ReturnPropsObject} props
+ */
+export function useTextInput( props ) {
+	const {
+		align,
+		arrows = true,
+		className,
+		defaultValue = '',
+		disabled,
+		dragAxis,
+		error = false,
+		format,
+		gap = 2.5,
+		id: idProp,
+		incrementFromNonNumericValue = false,
+		isCommitOnBlurOrEnter = false,
+		isFocused: isFocusedProp,
+		isInline = false,
+		isResizable = false,
+		isShiftStepEnabled = true,
+		justify,
+		max,
+		min,
+		multiline = false,
+		prefix,
+		shiftStep = 10,
+		size = 'medium',
+		step,
+		suffix,
+		type = 'text',
+		validate,
+		value: valueProp,
+		...otherProps
+	} = useContextSystem( props, 'TextInput' );
+
+	const id = useFormGroupContextId( idProp );
+
+	const {
+		decrement,
+		dragHandlersRef,
+		increment,
+		inputRef,
+		isFocused,
+		isInputTypeNumeric,
+		isTypeNumeric,
+		scrollHandlers,
+		value,
+		...textInputState
+	} = useTextInputState( {
+		...otherProps,
+		format,
+		defaultValue,
+		incrementFromNonNumericValue,
+		isCommitOnBlurOrEnter,
+		isFocused: isFocusedProp,
+		isShiftStepEnabled,
+		max,
+		min,
+		shiftStep,
+		step,
+		value: valueProp,
+		validate,
+		type,
+	} );
+
+	const rootEventHandlers = useRootEventHandlers( {
+		inputRef,
+		dragHandlersRef,
+		isTypeNumeric,
+		isFocused,
+	} );
+
+	const baseFieldProps = useBaseField( {
+		align,
+		disabled,
+		// @ts-ignore Looks like we need to extend the Props @typedef here? packages/components/src/base-field/hook.js
```

Thanks for the guidance here. I removed the props that `useBaseField` no longer specifies, and renamed `hasError`.

```console
TS2345: Argument of type '{ disabled: boolean | undefined; hasError: boolean; isInline: boolean; }' is not assignable to parameter of type 'PolymorphicComponentProps<Props, "div">'.   

Property 'children' is missing in type '{ disabled: boolean | undefined; hasError: boolean; isInline: boolean; }' but required in type 'FlexProps'.

```

I've ignored the line for now as I'm not sure how to tackle that.

### Review thread 22

#### sarayourfriend on 2021-06-15T16:31:55Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651961332
Path: packages/components/src/text-input/stories/index.js

```diff
@@ -0,0 +1,143 @@
+/**
+ * External dependencies
+ */
+import NumberFormat from 'react-number-format';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	__experimentalText as Text,
+	__experimentalVStack as VStack,
+} from '../../';
+import { TextInput } from '../';
+import { View } from '../../view';
+
+export default {
+	component: TextInput,
+	title: 'G2 Components (Experimental)/TextInput',
+};
+
+export const _default = () => {
+	return <TextInput placeholder="Into The Unknown" />;
+};
+
+export const Number = () => {
+	const [ value, setValue ] = useState( 0 );
+
+	return (
+		<View
+			css={ `
+				margin-top: 20vh;
+			` }
```

There is no longer a `css` prop on View. We should change this to use `style`.

### Review thread 23

#### sarayourfriend on 2021-06-15T16:32:07Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651961505
Path: packages/components/src/text-input/stories/index.js

```diff
@@ -0,0 +1,143 @@
+/**
+ * External dependencies
+ */
+import NumberFormat from 'react-number-format';
+
+/**
+ * WordPress dependencies
+ */
+import { useState } from '@wordpress/element';
+
+/**
+ * Internal dependencies
+ */
+import {
+	__experimentalText as Text,
+	__experimentalVStack as VStack,
+} from '../../';
+import { TextInput } from '../';
+import { View } from '../../view';
+
+export default {
+	component: TextInput,
+	title: 'G2 Components (Experimental)/TextInput',
+};
+
+export const _default = () => {
+	return <TextInput placeholder="Into The Unknown" />;
```

```suggestion
	return <TextInput placeholder="Code is Poetry" />;
```

### Review thread 24

#### sarayourfriend on 2021-06-15T16:33:33Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651962517
Path: packages/components/src/text-input/test/index.js

```diff
@@ -0,0 +1,93 @@
+/**
```

We should update these tests to only minimally use `toMatchSnapshot`. Snapshot diffs and snapshot style diffs are probably more helpful.

#### ramonjd on 2021-06-16T06:35:40Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652390542
Path: packages/components/src/text-input/test/index.js

```diff
@@ -0,0 +1,93 @@
+/**
```

Could you expand on what you mean here? By "minimally use" do we mean that we can remove some or most of the tests?

### Review thread 25

#### sarayourfriend on 2021-06-15T16:41:07Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651968165
Path: packages/components/src/text-input/text-input.js

```diff
@@ -0,0 +1,86 @@
+/**
+ * WordPress dependencies
+ */
+import { useMergeRefs } from '@wordpress/compose';
+
+/**
+ * Internal dependencies
+ */
+import { contextConnect } from '../ui/context';
+import { View } from '../view';
+import TextInputArrows from './text-input-arrows';
+import TextInputSteppers from './text-input-steppers';
+import { useTextInput } from './hooks/use-text-input';
+import * as styles from './styles';
+
+/**
+ *
+ * @param {import('./types').Props} props
```

We should wrap `Props` here in `PolymorphicComponentProps`

### Review thread 26 (resolved)

#### sarayourfriend on 2021-06-15T16:41:55Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651968773
Path: packages/components/src/utils/clear-selection.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * Clears the text selection on screen.
+ */
+export function clearSelection() {
+	if ( window?.getSelection ) {
+		// eslint-disable-next-line @wordpress/no-global-get-selection
+		const selection = window?.getSelection();
+		if ( ! selection ) {
+			return;
+		}
+
+		if ( selection.empty ) {
+			// Chrome.
+			selection.empty();
+		} else if ( selection.removeAllRanges ) {
+			// Firefox.
+			selection.removeAllRanges();
+		}
+		// @ts-ignore IE specific property.
+	} else if ( document.selection ) {
+		// @ts-ignore IE specific property.
+		document.selection.empty();
+	}
+}
```

Should this actually go in the `dom` package?

#### ramonjd on 2021-06-16T06:42:40Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652394156
Path: packages/components/src/utils/clear-selection.js

```diff
@@ -0,0 +1,24 @@
+/**
+ * Clears the text selection on screen.
+ */
+export function clearSelection() {
+	if ( window?.getSelection ) {
+		// eslint-disable-next-line @wordpress/no-global-get-selection
+		const selection = window?.getSelection();
+		if ( ! selection ) {
+			return;
+		}
+
+		if ( selection.empty ) {
+			// Chrome.
+			selection.empty();
+		} else if ( selection.removeAllRanges ) {
+			// Firefox.
+			selection.removeAllRanges();
+		}
+		// @ts-ignore IE specific property.
+	} else if ( document.selection ) {
+		// @ts-ignore IE specific property.
+		document.selection.empty();
+	}
+}
```

I wasn't sure about a lot of these utils that seems to be only required by TextInput. 

I was hoping you could advise! 😆 

If you believe that it would be useful to expose this method, and `dom` is the correct place then I'll take your advice :)

### Review thread 27 (resolved)

#### sarayourfriend on 2021-06-15T16:42:48Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651969423
Path: packages/components/src/utils/events.js

```diff
@@ -0,0 +1,41 @@
+/**
+ * Merges event handlers together.
+ *
+ * @template TEvent
+ * @param {(event: TEvent) => void} handler
+ * @param {(event: TEvent) => void} otherHandler
+ */
+export function mergeEvent( handler, otherHandler ) {
+	return (
+		/* eslint-disable jsdoc/no-undefined-types */
+		/** @type {TEvent} */
+		/* eslint-enable jsdoc/no-undefined-types */
+		event
+	) => {
+		if ( typeof handler === 'function' ) {
+			handler( event );
+		}
+		if ( typeof otherHandler === 'function' ) {
+			otherHandler( event );
+		}
+	};
+}
+
+/**
+ * Merges a set of event handlers together.
+ *
+ * @template TEvent
+ * @param {Record<string, (event: TEvent) => void>} handlers
+ * @param {Record<string, (event: TEvent) => void>} extraHandlers
+ */
+export function mergeEventHandlers( handlers = {}, extraHandlers = {} ) {
+	const mergedHandlers = { ...handlers };
+
+	for ( const [ key, handler ] of Object.entries( mergedHandlers ) ) {
+		if ( typeof extraHandlers[ key ] === 'function' ) {
+			mergedHandlers[ key ] = mergeEvent( handler, extraHandlers[ key ] );
+		}
+	}
+
+	return mergedHandlers;
+}
```

This seems like it should go into `compose` under a new `useMergedEventHandlers` hook. What do you think?

#### ramonjd on 2021-06-16T06:42:20Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652393946
Path: packages/components/src/utils/events.js

```diff
@@ -0,0 +1,41 @@
+/**
+ * Merges event handlers together.
+ *
+ * @template TEvent
+ * @param {(event: TEvent) => void} handler
+ * @param {(event: TEvent) => void} otherHandler
+ */
+export function mergeEvent( handler, otherHandler ) {
+	return (
+		/* eslint-disable jsdoc/no-undefined-types */
+		/** @type {TEvent} */
+		/* eslint-enable jsdoc/no-undefined-types */
+		event
+	) => {
+		if ( typeof handler === 'function' ) {
+			handler( event );
+		}
+		if ( typeof otherHandler === 'function' ) {
+			otherHandler( event );
+		}
+	};
+}
+
+/**
+ * Merges a set of event handlers together.
+ *
+ * @template TEvent
+ * @param {Record<string, (event: TEvent) => void>} handlers
+ * @param {Record<string, (event: TEvent) => void>} extraHandlers
+ */
+export function mergeEventHandlers( handlers = {}, extraHandlers = {} ) {
+	const mergedHandlers = { ...handlers };
+
+	for ( const [ key, handler ] of Object.entries( mergedHandlers ) ) {
+		if ( typeof extraHandlers[ key ] === 'function' ) {
+			mergedHandlers[ key ] = mergeEvent( handler, extraHandlers[ key ] );
+		}
+	}
+
+	return mergedHandlers;
+}
```

I'll defer to your instincts here. 

Would it make sense to do these package migrations in a separate PR?

### Review thread 28 (resolved)

#### sarayourfriend on 2021-06-15T16:43:30Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651969948
Path: packages/components/src/utils/hooks/index.js

```diff
@@ -1,3 +1,5 @@
 export { default as useControlledState } from './use-controlled-state';
 export { default as useJumpStep } from './use-jump-step';
 export { default as useUpdateEffect } from './use-update-effect';
+export { default as useControlledValue } from './use-controlled-value';
+export { default as usePropRef } from './use-prop-ref';
```

Both of these should go into compose I think unless we want to avoid exposing them as public APIs. We can always make them experimental/unstable if we wanted to, but they probably better fit in `compose`. @gziolo do you have any opinion here?

#### gziolo on 2021-06-16T07:49:12Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652437548
Path: packages/components/src/utils/hooks/index.js

```diff
@@ -1,3 +1,5 @@
 export { default as useControlledState } from './use-controlled-state';
 export { default as useJumpStep } from './use-jump-step';
 export { default as useUpdateEffect } from './use-update-effect';
+export { default as useControlledValue } from './use-controlled-value';
+export { default as usePropRef } from './use-prop-ref';
```

I'd personally leave this type of decision for later. It's perfectly fine to keep those hooks internal and present only in `@wordpress/components` for now. It's better to have a single PR per every new API we want to expose because it makes the discussion focused. Otherwise, you could have this PR opened for months because some parts of the public API might seem controversial, or folks could have objections if it is necessary to introduce some hooks as public API. I vote to finish building the UI  for Global Styles first and introduce new public APIs as we see to use them in other packages, or we know that it is so popular internally that it begs to get exposed to the world.

#### sarayourfriend on 2021-06-16T13:50:43Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652710033
Path: packages/components/src/utils/hooks/index.js

```diff
@@ -1,3 +1,5 @@
 export { default as useControlledState } from './use-controlled-state';
 export { default as useJumpStep } from './use-jump-step';
 export { default as useUpdateEffect } from './use-update-effect';
+export { default as useControlledValue } from './use-controlled-value';
+export { default as usePropRef } from './use-prop-ref';
```

@ramonjd Looks like this is the answer we can follow for the rest of the places I asked the same question.

Thanks Greg!

### Review thread 29 (resolved)

#### sarayourfriend on 2021-06-15T16:45:12Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651971227
Path: packages/components/src/utils/keyboard.js

```diff
@@ -0,0 +1,18 @@
+/**
+ * Normalizes the 'key' property of a KeyboardEvent in IE/Edge
+ *
+ * Source:
+ * https://github.com/downshift-js/downshift/blob/a71005bd879d05d7dcb892d1e0dc5c6ca74e9d39/src/utils.js#L279
+ *
+ * @param {import('react').KeyboardEvent} event A keyboardEvent object
+ *
+ * @return {string} The keyboard key
+ */
+export function normalizeArrowKey( event ) {
+	const { key, keyCode } = event;
+	/* istanbul ignore next (ie) */
+	if ( keyCode >= 37 && keyCode <= 40 && key.indexOf( 'Arrow' ) !== 0 ) {
+		return `Arrow${ key }`;
+	}
+	return key;
+}
```

I wonder if this shouldn't go into `keyboard-shortcuts`?

### Review thread 30 (resolved)

#### sarayourfriend on 2021-06-15T16:49:32Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r651974551
Path: packages/components/src/utils/values.js

```diff
@@ -40,3 +40,20 @@ export function isValueEmpty( value ) {
 export function getDefinedValue( values = [], fallbackValue ) {
 	return values.find( isValueDefined ) ?? fallbackValue;
 }
+
+/**
+ * Checks to see if a value is a numeric value (`number` or `string`).
+ *
+ * @param {any} value
+ *
+ * @return {boolean} Whether value is numeric.
+ */
+export function isValueNumeric( value ) {
+	const obj = typeof value === 'string' ? value.replace( /,/g, '' ) : value;
+	return (
+		! isNaN( parseFloat( obj ) ) &&
+		! isNaN( Number( obj ) ) &&
```

Do we actually need to pass it to `Number` first? Doesn't `isNaN` already coerce the value into a `Number`?

#### ramonjd on 2021-06-16T06:44:53Z

URL: https://github.com/WordPress/gutenberg/pull/32381#discussion_r652395300
Path: packages/components/src/utils/values.js

```diff
@@ -40,3 +40,20 @@ export function isValueEmpty( value ) {
 export function getDefinedValue( values = [], fallbackValue ) {
 	return values.find( isValueDefined ) ?? fallbackValue;
 }
+
+/**
+ * Checks to see if a value is a numeric value (`number` or `string`).
+ *
+ * @param {any} value
+ *
+ * @return {boolean} Whether value is numeric.
+ */
+export function isValueNumeric( value ) {
+	const obj = typeof value === 'string' ? value.replace( /,/g, '' ) : value;
+	return (
+		! isNaN( parseFloat( obj ) ) &&
+		! isNaN( Number( obj ) ) &&
```

> isNaN already coerce the value into a Number

I believe it does. [Source](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/isNaN)

This was a copy paste from G2. Happy to refactor here or in another PR.

## Changed files

- docs/manifest.json (MODIFIED, +6/-0)
- package-lock.json (MODIFIED, +10/-0)
- packages/components/package.json (MODIFIED, +2/-0)
- packages/components/src/index.js (MODIFIED, +1/-0)
- packages/components/src/text-input/README.md (ADDED, +215/-0)
- packages/components/src/text-input/hooks/use-text-input-state-utils.js (ADDED, +113/-0)
- packages/components/src/text-input/hooks/use-text-input-state.js (ADDED, +535/-0)
- packages/components/src/text-input/hooks/use-text-input.js (ADDED, +200/-0)
- packages/components/src/text-input/index.js (ADDED, +2/-0)
- packages/components/src/text-input/stories/index.js (ADDED, +143/-0)
- packages/components/src/text-input/styles.js (ADDED, +178/-0)
- packages/components/src/text-input/test/__snapshots__/index.js.snap (ADDED, +1714/-0)
- packages/components/src/text-input/test/index.js (ADDED, +81/-0)
- packages/components/src/text-input/text-input-arrows.js (ADDED, +145/-0)
- packages/components/src/text-input/text-input-steppers.js (ADDED, +187/-0)
- packages/components/src/text-input/text-input.js (ADDED, +86/-0)
- packages/components/src/text-input/types.ts (ADDED, +133/-0)
- packages/components/src/utils/clear-selection.js (ADDED, +24/-0)
- packages/components/src/utils/config-values.js (MODIFIED, +4/-0)
- packages/components/src/utils/events.js (ADDED, +41/-0)
- packages/components/src/utils/hooks/index.js (MODIFIED, +2/-0)
- packages/components/src/utils/hooks/use-controlled-value.js (ADDED, +35/-0)
- packages/components/src/utils/hooks/use-prop-ref.js (ADDED, +49/-0)
- packages/components/src/utils/keyboard.js (ADDED, +18/-0)
- packages/components/src/utils/test/values.js (ADDED, +24/-0)
- packages/components/src/utils/values.js (MODIFIED, +13/-0)
- packages/components/tsconfig.json (MODIFIED, +2/-0)
