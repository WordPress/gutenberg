---
number: 78913
type: pull_request
state: MERGED
merged_at: 2026-06-12T12:34:54Z
url: https://github.com/WordPress/gutenberg/pull/78913
matched_labels: ["[Package] Theme"]
all_labels: ["[Type] Enhancement","[Package] Edit Site","[Package] Theme"]
---

# PR #78913: Add xl border radius token for page shell surfaces.

- URL: https://github.com/WordPress/gutenberg/pull/78913
- Author: jameskoster
- Created: 2026-06-03T14:45:47Z
- Updated: 2026-06-12T12:35:40Z
- Comments: 7 of 7
- Review threads: 3 of 3
- Changed files: 11 of 11
- Diff: ../pr-diffs/78913.diff

## Body

## What

Adds `--wpds-border-radius-xl` to the WPDS border radius scale and uses it on outer admin **page shell** surfaces.

**Token & docs**
- New `xl` step in `packages/theme/tokens/border.json` and regenerated prebuild artifacts
- Documented in `packages/theme/docs/tokens.md` and `packages/theme/CHANGELOG.md`

**Consumers**
- **Boot:** `boot-layout__stage`, `boot-layout__inspector`, and `boot-layout__canvas` (desktop); view-transition pseudo-elements
- **Site editor:** `.edit-site-layout__area` and `.edit-site-layout:not(.is-full-canvas) .edit-site-layout__canvas .edit-site-resizable-frame__inner-content`
- **Storybook:** theme example application mock page chrome

## Why

The WPDS design language calls for radius to scale with container size. However, page shells and contained surfaces (Card, Notice) share an 8px radius, so when the latter are nested within the former, the hierarchy is broken. See the Gutenberg Experiments page as an example.

Extending the scale upward (rather than shrinking cards/notices) keeps `lg` aligned with the existing guidance for cards and modals.

## How

1. **Token:** `xs` → `sm` → `md` → `lg` → **`xl`** via Terrazzo; run `npm run build:tokens` in `@wordpress/theme`.
2. **Shells:** Replace hardcoded `8px` / `$radius-large` on outer layout chrome with `var(--wpds-border-radius-xl)`.
3. **Nested surfaces:** No changes to `@wordpress/ui` Card/Notice (still `lg`).

## Screenshots

| Before | After |
| --- | --- |
| <img width="812" height="818" alt="Screenshot 2026-06-03 at 15 44 37" src="https://github.com/user-attachments/assets/db6c4369-cf3c-41d0-8562-90eee7c5e6b3" /> | <img width="811" height="817" alt="Screenshot 2026-06-03 at 15 43 18" src="https://github.com/user-attachments/assets/70328aa4-9490-463f-9a53-1f752f567882" /> |

Note the radius of the shell is now one step larger than the radius of the nested cards.

## Test plan

- [ ] Storybook → **Design System/Theme/Theme Provider/Example Application** — page chrome is more rounded than inner Card
- [ ] Storybook or local app → **boot** routes with Card/Notice inside stage — parent radius visibly larger than card
- [ ] **Site editor** (non–full-canvas) — preview frame and layout area use 12px corners; full-canvas mode unchanged
- [ ] Confirm dashboard widget tiles still match Card `lg` (8px)
- [ ] `npm run build` / theme token build if CI requires fresh prebuild

## Issue comments

### github-actions on 2026-06-03T14:46:00Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4613607665

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: jameskoster <jameskoster@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: jasmussen <joen@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-08T11:39:23Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4648571224

**Size Change:** +63 B (0%) 

**Total Size:** 8.59 MB

<details open><summary>📦 <strong>View Changed</strong></summary>

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/modules/boot/index.min.js` | 52.7 kB | +15 B (+0.03%) |
| `build/styles/edit-site/style-rtl.css` | 22.1 kB | +24 B (+0.11%) |
| `build/styles/edit-site/style-rtl.min.css` | 18.2 kB | +3 B (+0.02%) |
| `build/styles/edit-site/style.css` | 22.1 kB | +17 B (+0.08%) |
| `build/styles/edit-site/style.min.css` | 18.2 kB | +4 B (+0.02%) |

</details>

<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-08T12:02:47Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4648769040

<!-- flaky-tests-report-comment -->
**Flaky tests detected in c293debd5c7379d69358032e9b4596fab7b10b92.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/27413277646
📝  Reported issues:
- #78689 in `/test/e2e/specs/editor/plugins/wp-editor-meta-box.spec.js`
- #78706 in `/test/e2e/specs/editor/various/autosave.spec.js`

### jasmussen on 2026-06-08T12:18:43Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4648892811

Not blocking this. But a brief moment of pause and reflection—the math checks out, but I think it's worth asking whether the page should be so rounded after all. 

Consider that we are looking to potentially replace the default 2px corner radius for buttons and inputs with 4px, which whould shift the entire range. Presumably we'd be looking at a 16px outer corner radius for the page itself. Does that get too bubbly?

I suspect the answer is: no, it's fine. But is there a way we can quickly gut-check this with a sketched visual before we land this?

### jameskoster on 2026-06-08T15:30:26Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4650597300

I don't think the value of a token in the default scale needs to prescribe the value in a different scale.

In other words, if the `xl` token in the default scale is 12px, that doesn't necessarily mean it needs to be 16px in the next scale up. The default progression might be `2 → 4 → 8 → 12`; the next could be `4 → 6 → 10 → 14`, or even `3 → 5 → 9 → 13`. We can choose. As the lower end of the scale gets more rounded we could even taper the upper-end to reduce bubblyness, e.g. `6 → 9 → 12 → 15`.

If we agree the new `xl` token is warranted then what I'd like to do is merge this PR then refine the per-scale values in https://github.com/WordPress/gutenberg/pull/78913 (which includes a story to easily test these details).

### jameskoster on 2026-06-09T14:58:04Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4661065421

I see a 👍 from @jasmussen, but will wait for a green check before merging.

### jameskoster on 2026-06-12T12:34:28Z

URL: https://github.com/WordPress/gutenberg/pull/78913#issuecomment-4691299145

I'm going to merge this as it closes a gap in the tokens exposed by a genuine need in the design language. We can continue to iterate as needed :)

## Review threads

### Review thread 1 (resolved)

#### aduth on 2026-06-04T18:53:23Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3358190467
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

While we're here and since what we're trying to articulate is the stacking scale between page elements, should we describe what each of those steps are that leads up to `xl` ?

#### jameskoster on 2026-06-05T12:17:58Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3362562034
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

Now that you mention it I think it's probably better to be a little arbitrary with the descriptions. The overriding principle is elegant nesting of corners which is tricky to express with per-token descriptions.

#### jameskoster on 2026-06-05T12:24:07Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3362593633
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

Simplified the individual description and added a group description to the `radius` object in `border.json`.

#### aduth on 2026-06-05T12:27:34Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3362609928
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

> The overriding principle is elegant nesting of corners which is tricky to express with per-token descriptions.

Hm, do we not have a strong sense of what those general layers of nesting are? 

Like: Buttons/controls < cards < modals < page, or something like that?

#### jameskoster on 2026-06-05T13:15:43Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3362870499
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

We do, but I'm unsure if it's helpful to document explicitly. 

```
xs = buttons nested inside controls
sm = buttons and controls
md = menus, popovers, and other small portalled UI
lg = larger content containers; cards, dialogs, notices
xl = page shell surfaces
```

WDYT?

#### aduth on 2026-06-05T13:21:02Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3362899595
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

Where I'm coming from is a fear that we'll fail at building a compelling case for why semantic tokens matter and how they differentiate our theming approach from what existed previously with `@wordpress/base-styles`, if we can't effectively articulate what those semantics are. "Large" is more likely to be associated to its specific scale value, has no context to how it should be applied, and therefore loses any meaning.

So in that framing, I think that absolutely yes it's helpful and the example you shared is a big improvement over generic labels.

But if my starting point is misguided, then maybe we don't need it 🤷

#### jameskoster on 2026-06-05T14:30:02Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3363321704
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

Yes, this is always a balancing act between primitive scales and highly explicit semantic tokens. Most of our token categories currently sit somewhere in the middle of that spectrum.

Radius is particularly tricky because it's a purely visual property, so the meaning of individual values may not always translate to different design challenges.

One lens worth keeping in mind (speaking largely to myself here) is that these tokens are primarily consumed by people working on the design system itself. Most product teams will interact with the published components rather than applying radius tokens directly. From that perspective, I agree that descriptions are probably valuable as lightweight guidance for system contributors without forcing us towards anything more prescriptive.

#### aduth on 2026-06-05T14:49:34Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3363444157
Path: packages/theme/docs/tokens.md

```diff
@@ -125,17 +125,18 @@ The interactive state of the element. The default (no modifier) is the idle stat
 
 ### Border
 
-| Variable name               | Description                 |
-| --------------------------- | --------------------------- |
-| `--wpds-border-radius-xs`   | Extra small radius          |
-| `--wpds-border-radius-sm`   | Small radius                |
-| `--wpds-border-radius-md`   | Medium radius               |
-| `--wpds-border-radius-lg`   | Large radius                |
-| `--wpds-border-width-xs`    | Extra small width           |
-| `--wpds-border-width-sm`    | Small width                 |
-| `--wpds-border-width-md`    | Medium width                |
-| `--wpds-border-width-lg`    | Large width                 |
-| `--wpds-border-width-focus` | Border width for focus ring |
+| Variable name               | Description                                        |
+| --------------------------- | -------------------------------------------------- |
+| `--wpds-border-radius-xs`   | Extra small radius                                 |
+| `--wpds-border-radius-sm`   | Small radius                                       |
+| `--wpds-border-radius-md`   | Medium radius                                      |
+| `--wpds-border-radius-lg`   | Large radius                                       |
+| `--wpds-border-radius-xl`   | Extra large radius for page and app shell surfaces |
```

> Radius is particularly tricky because it's a purely visual property, so the meaning of individual values may not always translate to different design challenges.

I guess that's where I come back to my original comment in this thread, where the mere existence of this pull request implies that we have at least some understanding of how the current scale translates to different elements of a page, and how the need for a new token relates to that. As a reviewer, that's also helpful context to understand where this fits.

In any case, this is treading philosophical and is good to align around, but for the purpose of this pull request I think we're in good shape 👍

### Review thread 2 (resolved)

#### ciampo on 2026-06-08T15:59:43Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3374631799
Path: packages/theme/CHANGELOG.md

```diff
@@ -2,6 +2,10 @@
 
 ## Unreleased
 
+### New Features
+
+-   Add `--wpds-border-radius-xl` for page and app shell surfaces so nested cards and notices can stay on `--wpds-border-radius-lg` without matching the parent radius.
```

Missing PR number / URL

### Review thread 3

#### ciampo on 2026-06-12T11:15:11Z

URL: https://github.com/WordPress/gutenberg/pull/78913#discussion_r3402907847
Path: packages/theme/CHANGELOG.md

```diff
@@ -5,6 +5,7 @@
 ### New Features
 
 -   Add `--wpds-dimension-size-*` design tokens for element sizing ([#76545](https://github.com/WordPress/gutenberg/pull/76545)).
+-   Add `--wpds-border-radius-xl` for page and app shell surfaces so nested cards and notices can stay on `--wpds-border-radius-lg` without matching the parent radius ([#78913](https://github.com/WordPress/gutenberg/pull/78913)).
```

We may want to rebase, move CHANGELOG to latest unreleased section before merging

## Changed files

- packages/boot/src/components/root/style.scss (MODIFIED, +2/-2)
- packages/boot/src/view-transitions.scss (MODIFIED, +1/-1)
- packages/edit-site/src/components/layout/style.scss (MODIFIED, +2/-2)
- packages/theme/CHANGELOG.md (MODIFIED, +4/-0)
- packages/theme/docs/tokens.md (MODIFIED, +12/-11)
- packages/theme/src/prebuilt/css/design-tokens.css (MODIFIED, +6/-4)
- packages/theme/src/prebuilt/js/design-token-fallbacks.mjs (MODIFIED, +1/-0)
- packages/theme/src/prebuilt/js/design-tokens.mjs (MODIFIED, +1/-0)
- packages/theme/src/prebuilt/ts/token-types.ts (MODIFIED, +1/-1)
- packages/theme/tokens/border.json (MODIFIED, +9/-4)
- storybook/stories/design-system/theme-example-application.story.tsx (MODIFIED, +1/-1)
