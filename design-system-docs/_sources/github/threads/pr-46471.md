---
number: 46471
type: pull_request
state: MERGED
merged_at: 2023-01-02T21:03:13Z
url: https://github.com/WordPress/gutenberg/pull/46471
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Focus] Accessibility (a11y)","[Package] Components","Needs Accessibility Feedback"]
---

# PR #46471: TabPanel: Add prop to allow disabling of a tab button

- URL: https://github.com/WordPress/gutenberg/pull/46471
- Author: joshuatf
- Created: 2022-12-12T18:23:24Z
- Updated: 2023-01-12T06:25:47Z
- Comments: 6 of 6
- Review threads: 5 of 5
- Changed files: 6 of 6
- Diff: ../pr-diffs/46471.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Adds a prop to allow disabling of a given tab.

## Why?
In WooCommerce we plan to disable tabs depending on a product configuration.  While we could hide the disabled tabs from view to solve this, it's helpful for users to see disabled tabs and hover them for hints as to why certain tabs are disabled.

## How?
Adds a prop to disable the tab.  Alternatively we could pass `...rest` to the tab button to allow passing of any props, but I'm not sure if this is wanted and would not have the advantages of TS.

## Testing Instructions
1. Run `npm run test:unit tab-panel ` and make sure all tests pass
2. Optionally create a tab panel with a a disabled prop
```
			<TabPanel
				tabs={ [
					{
						name: 'delta',
						title: 'Delta',
						className: 'delta-class',
						disabled: true,
					},
				] }
				children={ () => undefined }
				onSelect={ () => undefined }
			/>
```
3. Make sure the tab is disabled

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->
<img width="320" alt="Screen Shot 2022-12-12 at 10 22 17 AM" src="https://user-images.githubusercontent.com/10561050/207124015-f93aeed6-a7de-49f3-a347-04704f36766c.png">

## Issue comments

### joshuatf on 2022-12-14T20:33:54Z

URL: https://github.com/WordPress/gutenberg/pull/46471#issuecomment-1352124983

Thanks for the feedback @alexstine and @mirka.  I added some additional logic for updating selection when tabs are disabled as well as additional tests around this logic.

This is now visible in Storybook which demonstrates the initial tab selection, but you can also verify that selection updates to the first enabled tab if the current tab is disabled by editing the raw `tabs` value in Storybook.  E.g., select "Tab 3" and then update the prop data to:

```
[
  {
    "name": "tab1",
    "title": "Tab 1"
  },
  {
    "name": "tab2",
    "title": "Tab 2"
  },
  {
    "name": "tab3",
    "title": "Tab 3",
    "disabled": true
  }
]
```

Please let me know if everything looks good now- thanks!

### joshuatf on 2022-12-19T22:21:06Z

URL: https://github.com/WordPress/gutenberg/pull/46471#issuecomment-1358487993

Hi @mirka @alexstine - could you give this PR another look when you have time?  Thanks!

### mirka on 2022-12-23T11:08:50Z

URL: https://github.com/WordPress/gutenberg/pull/46471#issuecomment-1363858069

Almost forgot, let's update the [component readme](https://github.com/WordPress/gutenberg/blob/66091754995f286d8e6fd688bd88ab6c19643a5a/packages/components/src/tab-panel/README.md?plain=1#L116) before we merge.

### joshuatf on 2022-12-29T21:37:22Z

URL: https://github.com/WordPress/gutenberg/pull/46471#issuecomment-1367597149

Thank you both for the reviews, @alexstine and @mirka!  Updated the readme in https://github.com/WordPress/gutenberg/pull/46471/commits/b99186265db8aa73bf81b7bc9d80535d282e968b.

### scruffian on 2023-01-11T17:23:16Z

URL: https://github.com/WordPress/gutenberg/pull/46471#issuecomment-1379226454

I think this might have caused this issue: https://github.com/WordPress/gutenberg/issues/47079

### talldan on 2023-01-12T06:25:47Z

URL: https://github.com/WordPress/gutenberg/pull/46471#issuecomment-1379870820

> I think this might have caused this issue: https://github.com/WordPress/gutenberg/issues/47079

It seems like there are two causes:
1. The Navigation block's List View tab isn't declared on first render, it only becomes present in the `tabs` array on later renders.
2. The TabPanel no longer supports lazily adding initial tabs after this PR, it now always falls back to the first enabled tab in the `tabs` array.

I did try looking to see if this could be fixed by always rendering the tab navigation block, but I couldn't find the source of the problem. It could be worth some further investigation though.

I've put together a fix for the `TabPanel` component - https://github.com/WordPress/gutenberg/pull/47100.

## Review threads

### Review thread 1 (resolved)

#### mirka on 2022-12-13T17:44:47Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1047512924
Path: packages/components/src/tab-panel/types.ts

```diff
@@ -32,6 +32,7 @@ export type TabButtonProps< IconProps = unknown > = {
 	selected: boolean;
 	showTooltip?: boolean;
 	tabId: string;
+	disabled?: boolean;
```

I think `disabled` (and `icon`, too) should be defined in the `Tab` type first and foremost, as they are now part of the standard `Tab` interface. And then they can be `Pick`ed into `TabButtonProps`.

For context, the `Record< any, any >` part of the `Tab` type is just a catch-all intended for consumers who want to pass arbitrary data down to the tabpanel render function. Personally I don't love this API design since it isn't necessary (i.e. consumers should be able to augment whatever data they want as long as they know the tab id), but I think we have to continue supporting it for back compat.

#### joshuatf on 2022-12-14T20:29:09Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1048943410
Path: packages/components/src/tab-panel/types.ts

```diff
@@ -32,6 +32,7 @@ export type TabButtonProps< IconProps = unknown > = {
 	selected: boolean;
 	showTooltip?: boolean;
 	tabId: string;
+	disabled?: boolean;
```

Added in https://github.com/WordPress/gutenberg/pull/46471/commits/3b0ea90b1014388662a069b25f112639bf797da4.

### Review thread 2 (resolved)

#### mirka on 2022-12-13T17:51:02Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1047522339
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -160,6 +160,28 @@ describe( 'TabPanel', () => {
 		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
 	} );
 
+	it( 'should disable the tab when `disabled` is true', () => {
+		const mockOnSelect = jest.fn();
+
+		render(
+			<TabPanel
+				tabs={ [
+					...TABS,
+					{
+						name: 'delta',
+						title: 'Delta',
+						className: 'delta-class',
+						disabled: true,
+					},
+				] }
+				children={ () => undefined }
+				onSelect={ mockOnSelect }
+			/>
+		);
+
+		expect( screen.getByRole( 'tab', { name: 'Delta' } ) ).toBeDisabled();
```

Thanks for adding the test ✨

Another thing I think we need to handle and test for is when the `selected` tab happens to be disabled. For example, on the initial load of a tab list like this:

```js
[
		{
			name: 'tab1',
			title: 'Tab 1',
			disabled: true,
		},
		{
			name: 'tab2',
			title: 'Tab 2',
		},
],
```

#### joshuatf on 2022-12-14T20:29:39Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1048943814
Path: packages/components/src/tab-panel/test/index.tsx

```diff
@@ -160,6 +160,28 @@ describe( 'TabPanel', () => {
 		expect( mockOnSelect ).toHaveBeenLastCalledWith( 'beta' );
 	} );
 
+	it( 'should disable the tab when `disabled` is true', () => {
+		const mockOnSelect = jest.fn();
+
+		render(
+			<TabPanel
+				tabs={ [
+					...TABS,
+					{
+						name: 'delta',
+						title: 'Delta',
+						className: 'delta-class',
+						disabled: true,
+					},
+				] }
+				children={ () => undefined }
+				onSelect={ mockOnSelect }
+			/>
+		);
+
+		expect( screen.getByRole( 'tab', { name: 'Delta' } ) ).toBeDisabled();
```

Adding this for selected and initially selected tabs in https://github.com/WordPress/gutenberg/pull/46471/commits/529683594274bbbb9cb6682a4bd5e91b507f698d.

### Review thread 3 (resolved)

#### mirka on 2022-12-13T17:52:59Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1047525163
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -31,6 +32,7 @@ const TabButton = ( {
 		aria-selected={ selected }
 		id={ tabId }
 		onClick={ onClick }
+		disabled={ disabled }
```

I feel like it might be better if the button were still focusable even when disabled. Especially since you mention in the PR description that it would be helpful for users to hover tabs for hints on why they're disabled. What do you think @alexstine?

There is a `__experimentalIsFocusable` prop we can enable on `Button` to make this happen.

Either way, it would be great if we could add an explicit keyboard navigation test for whichever behavior we decide on. Normally it's sufficient to just test for `toBeDisabled()`, but in this case there is a `NavigableMenu` layer that converts the usual tab-key-based navigation to be arrow-key-based, so I think it's worth testing that integration point.

#### alexstine on 2022-12-13T20:05:37Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1047679665
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -31,6 +32,7 @@ const TabButton = ( {
 		aria-selected={ selected }
 		id={ tabId }
 		onClick={ onClick }
+		disabled={ disabled }
```

That prop is set to true by default I think so should not be an issue. Need to test to confirm.

#### joshuatf on 2022-12-14T20:30:28Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1048944418
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -31,6 +32,7 @@ const TabButton = ( {
 		aria-selected={ selected }
 		id={ tabId }
 		onClick={ onClick }
+		disabled={ disabled }
```

I ended up having to use `__experimentalIsFocusable` in order to focus the disabled button.  Added in https://github.com/WordPress/gutenberg/pull/46471/commits/662bfd7d97e099bf7de425b8a887f35ced0166a4.

### Review thread 4 (resolved)

#### mirka on 2022-12-13T18:17:08Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1047558063
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -23,6 +23,7 @@ const TabButton = ( {
 	onClick,
 	children,
 	selected,
+	disabled,
 	...rest
```

Nit: No strong opinion, but the prop passing is getting a little verbose here so it might be a good time to simplify:

```jsx
const TabButton = ( { tabId, selected, ...rest }: TabButtonProps ) => (
	<Button
		role="tab"
		tabIndex={ selected ? null : -1 }
		aria-selected={ selected }
		id={ tabId }
		{ ...rest }
	/>
);
```

### Review thread 5

#### ciampo on 2023-01-02T14:48:45Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1060075038
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -106,10 +105,25 @@ export function TabPanel( {
 	const selectedId = `${ instanceId }-${ selectedTab?.name ?? 'none' }`;
 
 	useEffect( () => {
-		if ( ! selectedTab?.name && tabs.length > 0 ) {
-			handleTabSelection( initialTabName || tabs[ 0 ].name );
+		const firstEnabledTab = tabs.find( ( tab ) => ! tab.disabled );
+		const initialTab = tabs.find( ( tab ) => tab.name === initialTabName );
+		if ( ! selectedTab?.name && firstEnabledTab ) {
+			handleTabSelection(
+				initialTab && ! initialTab.disabled
+					? initialTab.name
+					: firstEnabledTab.name
+			);
 		}
-	}, [ tabs, selectedTab?.name, initialTabName, handleTabSelection ] );
+		if ( selectedTab?.disabled && firstEnabledTab ) {
```

Nitty, but — right now, the code makes it look like the `handleTabSelection` function can be called twice during the same effect.

Should we rewrite the logic slightly (e.g. `else if`) to make it clear that these two code branches should be exclusive?

#### joshuatf on 2023-01-02T19:08:40Z

URL: https://github.com/WordPress/gutenberg/pull/46471#discussion_r1060165734
Path: packages/components/src/tab-panel/index.tsx

```diff
@@ -106,10 +105,25 @@ export function TabPanel( {
 	const selectedId = `${ instanceId }-${ selectedTab?.name ?? 'none' }`;
 
 	useEffect( () => {
-		if ( ! selectedTab?.name && tabs.length > 0 ) {
-			handleTabSelection( initialTabName || tabs[ 0 ].name );
+		const firstEnabledTab = tabs.find( ( tab ) => ! tab.disabled );
+		const initialTab = tabs.find( ( tab ) => tab.name === initialTabName );
+		if ( ! selectedTab?.name && firstEnabledTab ) {
+			handleTabSelection(
+				initialTab && ! initialTab.disabled
+					? initialTab.name
+					: firstEnabledTab.name
+			);
 		}
-	}, [ tabs, selectedTab?.name, initialTabName, handleTabSelection ] );
+		if ( selectedTab?.disabled && firstEnabledTab ) {
```

Sure!  Done in https://github.com/WordPress/gutenberg/pull/46471/commits/c64730cc15148882be85a50c5536de3261138152.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/tab-panel/README.md (MODIFIED, +1/-0)
- packages/components/src/tab-panel/index.tsx (MODIFIED, +19/-5)
- packages/components/src/tab-panel/stories/index.tsx (MODIFIED, +20/-0)
- packages/components/src/tab-panel/test/index.tsx (MODIFIED, +89/-0)
- packages/components/src/tab-panel/types.ts (MODIFIED, +10/-4)
