---
number: 76975
type: pull_request
state: MERGED
merged_at: 2026-04-03T15:10:04Z
url: https://github.com/WordPress/gutenberg/pull/76975
matched_labels: ["[Package] Components","[Package] UI"]
all_labels: ["[Type] Developer Documentation","[Package] Components","[Package] DataViews","[Package] UI"]
---

# PR #76975: Improve CSS setup instructions in package readmes

- URL: https://github.com/WordPress/gutenberg/pull/76975
- Author: mirka
- Created: 2026-04-01T21:08:59Z
- Updated: 2026-04-08T08:25:26Z
- Comments: 3 of 3
- Review threads: 4 of 4
- Changed files: 4 of 4
- Diff: ../pr-diffs/76975.diff

## Body

## What?

Adds a **Set up** section to the readmes for `@wordpress/admin-ui`, `@wordpress/components`, `@wordpress/dataviews`, and `@wordpress/ui`, covering stylesheet dependencies for WordPress plugins and for apps outside WordPress.

## Why?

These packages depend on CSS from themselves and/or other packages. Instructions were split across sections or easy to miss, and non‑WordPress setups did not always spell out install steps, import order, RTL paths, or which `wp_enqueue_style` handles to depend on.

## How?

- **Within WordPress**: Document registering the right stylesheet handles as dependencies of the consumer’s stylesheet (with a link to `wp_enqueue_style`).
- **Outside WordPress**: Document `npm install` where needed and `import` examples for `@wordpress/theme/design-tokens.css` and `@wordpress/components/build-style/style.css`, plus RTL paths for components CSS where applicable.
- **`@wordpress/ui`**: Clarify that WordPress loads styles for you; outside WordPress, recommend design tokens for full theming and add a short note about `isolation: isolate` on the layout root for portaled popovers.

## Testing Instructions

See that the instructions are correct and make sense.

## Use of AI Tools

Composer 2 was used to draft this PR description.

## Issue comments

### github-actions on 2026-04-01T21:52:48Z

URL: https://github.com/WordPress/gutenberg/pull/76975#issuecomment-4173183864

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-04-01T21:56:10Z

URL: https://github.com/WordPress/gutenberg/pull/76975#issuecomment-4173201636

<!-- flaky-tests-report-comment -->
**Flaky tests detected in b8eb232ff334d72639bcc6d085d55fed0baeed24.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/23949739516
📝  Reported issues:
- #36480 in `/test/e2e/specs/editor/various/multi-entity-saving.spec.js`
- #76524 in `/test/e2e/specs/editor/various/post-content-focus-mode.spec.js`
- #76179 in `/test/e2e/specs/editor/various/publish-panel.spec.js`
- #69568 in `/test/e2e/specs/site-editor/user-global-styles-revisions.spec.js`
- #68892 in `/test/e2e/specs/site-editor/homepage-settings.spec.js`

### oandregal on 2026-04-06T08:42:16Z

URL: https://github.com/WordPress/gutenberg/pull/76975#issuecomment-4191196392

Thanks for the changes, this reads a lot better.

## Review threads

### Review thread 1

#### mirka on 2026-04-01T21:52:30Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3024849109
Path: packages/dataviews/README.md

```diff
@@ -14,19 +14,30 @@ Install the module
 npm install @wordpress/dataviews --save
 ```
 
-## Stylesheet Dependencies
+## Set up
 
-DataViews depends on stylesheets from `@wordpress/components` and `@wordpress/theme`. In a WordPress admin page context, these are loaded automatically. For applications outside WordPress, you will need to include these stylesheets:
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
```

`@wordpress/dataviews` and `@wordpress/admin-ui` don't have a stylesheet handle, so there's currently no reliable way for a third-party WP plugin to load the package styles without potentially loading duplicate or conflicting styles on a given route. (If something else is loading the same package styles on the same route.)

Maybe that's fine, but mentioning here for completeness (cc @oandregal).

### Review thread 2

#### aduth on 2026-04-02T17:53:24Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3029484493
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
```

Super nit: But I kept wondering if this should be "setup" or "set up", but in turn it made me think it's not a very common heading I see in READMEs, compared to something like "Getting started", "Usage", maybe "Setup instructions", etc.

#### aduth on 2026-04-02T17:59:43Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3029514748
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
```

Although in the case of `components` package here, it already has "Usage". And we're treating this as something different 🤔 Maybe it's fine to have a separate section for this "necessary steps between installation and usage". Or we could fold it into an existing section. "Setup" is probably more consistent as a noun heading though, if we kept it.

#### mirka on 2026-04-03T14:32:50Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033099076
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
```

Good point about the noun/verb agreement. Turns out "Setup" is used as a heading in some other packages in here as well, so I went with that ecf4fd6cbccd852c9346c6b4a03edf769b0f1b37.

### Review thread 3

#### aduth on 2026-04-02T17:56:59Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3029501017
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
```

Aside: It would be really nice if we made a subpath export with a nicer import path here, to avoid `build-style` being part of the import path. Feels like an implementation detail we shouldn't need to expose to end-users 🤷

#### mirka on 2026-04-03T14:00:47Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3032985031
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
```

On the other hand, it's probably a pretty niche use case to use these libraries outside of WordPress?

#### aduth on 2026-04-03T14:07:46Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033008541
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
```

True, not an especially urgent problem. More to the general consistency of how we want to use subpath exports. They're a public API. But `build-style` means nothing and could disappear or be renamed tomorrow if we wanted it to.

#### simison on 2026-04-08T08:04:45Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3049971249
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
```

> it's probably a pretty niche use case to use these libraries outside of WordPress?

Note that until admin-ui is [bundled by default](https://github.com/WordPress/gutenberg/blob/1dc1a7f23676ca6ac336a86c1a0aa8987d099915/packages/dependency-extraction-webpack-plugin/lib/util.js#L2-L12) by tooling provided by Gutenberg, the import is mandatory also in WP Admin contexts.

It might even help clarify in docs that component style is required to import only outside WP contexts, and the other two in all contexts?

#### mirka on 2026-04-08T08:25:25Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3050081215
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
```

> Note that until admin-ui is [bundled by default](https://github.com/WordPress/gutenberg/blob/1dc1a7f23676ca6ac336a86c1a0aa8987d099915/packages/dependency-extraction-webpack-plugin/lib/util.js#L2-L12) by tooling provided by Gutenberg, the import is mandatory also in WP Admin contexts.

I've raised this as a structural [problem](https://github.com/WordPress/gutenberg/pull/76975/changes/BASE..1aca2676195b2631378402ac80ba440ebfd8e0f4#r3024849109) for both DataViews and Admin UI. Possibly less of a problem for Admin UI, if there's less chance of stylesheet conflicts on a given route (as opposed to e.g. DataForm, which could more plausibly have multiple separate bundles loading into a single route). I believe the simplest solution for Admin UI would be to switch to our CSS module injection system (#77032).

### Review thread 4

#### aduth on 2026-04-02T18:01:15Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3029521756
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
+```
+
+RTL versions of the stylesheets are available in the same paths, but with `-rtl` appended to the filename (`style-rtl.css`). The design tokens stylesheet is universal and does not have a separate RTL version.
```

I wonder if it's worth clarifying that we don't ship an RTL-specific stylesheet for the `ui` package.

I also wonder what level of effort would be involved in removing it from this `admin-ui` package, since it doesn't include a whole lot and might be easy to migrate to logical properties.

#### mirka on 2026-04-03T14:14:48Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033035373
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
+```
+
+RTL versions of the stylesheets are available in the same paths, but with `-rtl` appended to the filename (`style-rtl.css`). The design tokens stylesheet is universal and does not have a separate RTL version.
```

> I wonder if it's worth clarifying that we don't ship an RTL-specific stylesheet for the `ui` package.

The "The design tokens stylesheet is universal and does not have a separate RTL version." line at the end was supposed to be this clarification. Was that unclear or did you mean something else?

> I also wonder what level of effort would be involved in removing it from this `admin-ui` package, since it doesn't include a whole lot and might be easy to migrate to logical properties.

Oh yes, should be pretty easy especially at this early stage. Also a move to CSS modules. In light of the #76744 problem too, ~I'm thinking we can enforce the "CSS modules + logical properties" combination repo-wide on packages that want to adopt this new setup~ [we already do](https://github.com/WordPress/gutenberg/blob/592e7c8eddb157fb809aae3545d632bce7f8507c/.stylelintrc.js#L58) 😄.

#### aduth on 2026-04-03T14:18:26Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033048434
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
+```
+
+RTL versions of the stylesheets are available in the same paths, but with `-rtl` appended to the filename (`style-rtl.css`). The design tokens stylesheet is universal and does not have a separate RTL version.
```

> The "The design tokens stylesheet is universal and does not have a separate RTL version." line at the end was supposed to be this clarification. Was that unclear or did you mean something else?

I was more referring to a note in `packages/ui/README.md` for someone who might be confused about the absence of an RTL stylesheet. There's an implication here that downstream consumers will be handling RTL concerns, so if I'm an app developer and I know that I need to handle RTL, I might be confused if I can't find any mention of a stylesheet I need to load for RTL in `@wordpress/ui` documentation.

#### mirka on 2026-04-03T14:33:17Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033100581
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
+```
+
+RTL versions of the stylesheets are available in the same paths, but with `-rtl` appended to the filename (`style-rtl.css`). The design tokens stylesheet is universal and does not have a separate RTL version.
```

Ah, I see. Added in b8eb232ff334d72639bcc6d085d55fed0baeed24.

#### aduth on 2026-04-03T14:34:15Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033103775
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
+```
+
+RTL versions of the stylesheets are available in the same paths, but with `-rtl` appended to the filename (`style-rtl.css`). The design tokens stylesheet is universal and does not have a separate RTL version.
```

> Ah, I see. Added in [b8eb232](https://github.com/WordPress/gutenberg/commit/b8eb232ff334d72639bcc6d085d55fed0baeed24).

Thanks, looks great 👍

#### mirka on 2026-04-03T14:42:12Z

URL: https://github.com/WordPress/gutenberg/pull/76975#discussion_r3033129496
Path: packages/admin-ui/README.md

```diff
@@ -12,6 +12,30 @@ Install the module
 npm install @wordpress/admin-ui --save
 ```
 
+## Set up
+
+This package requires CSS from this package and from multiple dependency packages.
+
+### Within WordPress
+
+To ensure proper load order, add the `wp-components` stylesheet as a dependency of your plugin's stylesheet. See [wp_enqueue_style documentation](https://developer.wordpress.org/reference/functions/wp_enqueue_style/#parameters) for how to specify dependencies.
+
+### Outside WordPress
+
+Install and load these stylesheets in your application:
+
+```bash
+npm install @wordpress/admin-ui @wordpress/theme @wordpress/components
+```
+
+```js
+import '@wordpress/theme/design-tokens.css';
+import '@wordpress/components/build-style/style.css';
+import '@wordpress/admin-ui/build-style/style.css';
+```
+
+RTL versions of the stylesheets are available in the same paths, but with `-rtl` appended to the filename (`style-rtl.css`). The design tokens stylesheet is universal and does not have a separate RTL version.
```

Issue for CSS module migration of admin-ui package: #77032

## Changed files

- packages/admin-ui/README.md (MODIFIED, +24/-0)
- packages/components/README.md (MODIFIED, +18/-4)
- packages/dataviews/README.md (MODIFIED, +16/-5)
- packages/ui/README.md (MODIFIED, +22/-2)
