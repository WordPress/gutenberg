---
number: 36835
type: pull_request
state: MERGED
merged_at: 2021-12-15T14:44:21Z
url: https://github.com/WordPress/gutenberg/pull/36835
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #36835: Refactor DatePicker component to react hooks and function component

- URL: https://github.com/WordPress/gutenberg/pull/36835
- Author: amustaque97
- Created: 2021-11-24T19:31:50Z
- Updated: 2021-12-15T15:41:23Z
- Comments: 8 of 8
- Review threads: 4 of 4
- Changed files: 6 of 6
- Diff: ../pr-diffs/36835.diff

## Body

This PR is a part of issue #22890
<!-- Learn the overall process and best practices for pull requests at https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/repository-management.md#pull-requests. -->

<!-- Gutenberg's license is in the process of updating to be dual-licensed under the GPL and MPL. As part of that transition, all new contributions are dual-licensed. For more information, see: https://github.com/WordPress/gutenberg/blob/trunk/LICENSE.md -->

## Description
<!-- Please describe what you have changed or added -->
It was already done as a part of different PR #22897. After code review
it looks like the original author is not able to find time to reply or
address review comments. I'm doing takeover from here and will take care
of PR until it gets merged.

Changes done as a part of PR
- Using react hook `useRef` in the component.
- Refactor from class component to function component.

## How has this been tested?
<!-- Please describe in detail how you tested your changes. -->
<!-- Include details of your testing environment, tests ran to see how -->
<!-- your change affects other areas of the code, etc. -->

1. Edit page or post
2. Click on post schedule date
3. Change date
4. Publish post/page

## Screenshots <!-- if applicable -->

## Types of changes
<!-- What types of changes does your code introduce?  -->
<!-- Bug fix (non-breaking change which fixes an issue) -->
<!-- New feature (non-breaking change which adds functionality) -->
<!-- Breaking change (fix or feature that would cause existing functionality to not work as expected) -->
Code Quality - Refactor
## Checklist:
- [x] My code is tested.
- [x] My code follows the WordPress code style. <!-- Check code: `npm run lint`, Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/javascript/ -->
- [ ] My code follows the accessibility standards. <!-- Guidelines: https://developer.wordpress.org/coding-standards/wordpress-coding-standards/accessibility/ -->
- [ ] I've tested my changes with keyboard and screen readers. <!-- Instructions: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/accessibility-testing.md -->
- [ ] My code has proper inline documentation. <!-- Guidelines: https://developer.wordpress.org/coding-standards/inline-documentation-standards/javascript/ -->
- [ ] I've included developer documentation if appropriate. <!-- Handbook: https://developer.wordpress.org/block-editor/ -->
- [ ] I've updated all React Native files affected by any refactorings/renamings in this PR (please manually search all `*.native.js` files for terms that need renaming or removal). <!-- React Native mobile Gutenberg guidelines: https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/native-mobile.md -->

## Issue comments

### ciampo on 2021-11-26T13:22:03Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-979974857

Thank you @ntsekouras for the ping!

Hey @amustaque97, thank you for picking up this piece of work! I'm currently working on some `DatePicker` related changes in #36878, so maybe it's better to wait until that PR is merged, and rebase this PR to include those changes.

In the meantime, I'll give this PR a better look next week!

### ciampo on 2021-11-30T13:22:58Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-982632246

Alright —  #36878 has been merged!

@amustaque97 , would you mind rebasing this PR on top of `trunk` in order to include the latest changes to the component?

I also 100% agree with this comment:

> After looking at the test cases I believe we should remove those test cases because we should test component behaviour, not implementation details. What do you think?

~Although I'm curious to know why tests are failing, since I didn't expect code changes in this PR to cause them to fail.~ Nevermind, I just realised that they're expecting certain functions to exist on the component class's instance!

Code changes so far look good, but I'll go through a detailed code review after the rebase if that's ok! Thank you for working on this once again!

### amustaque97 on 2021-11-30T13:59:50Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-982662183

> I just realised that they're expecting certain functions to exist on the component class's instance!

Yeah right, that's why, we should focus on overall component behavior rather than its internal functions.

I have taken merge from trunk branch. Please take a look once GitHub actions is complete.

### amustaque97 on 2021-12-12T10:12:30Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-991869843

Removing `getMomentDate` test cases. Since in real word scenario, in UI we never deal with `getMomentDate` directly so in the test case we cannot write some event or simulate any behaviour that will give access to `getMomentDate` method. Now if we think of taking help of `currentDate` props then test cases are already in the place.

If you have any better idea please share. I would love to implement them.

### mirka on 2021-12-14T18:37:50Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-993868825

Great refactor!

> Removing `getMomentDate` test cases. [...] If you have any better idea please share. I would love to implement them.

Since those tests are more like a unit test of the pure function `getMomentDate` to begin with (and doesn't even seem to be intended to test any component behavior at all), I could see the argument to move `getMomentDate` out of the `DatePicker` function and export it separately as a named export to test. But the tests aren't particularly complex and the behavior seems pretty much covered by the actual component-based `currentDate` tests like you say. So personally I'm fine with deletion too if you're happy with it!

Are we ready for final review?

### amustaque97 on 2021-12-14T18:42:05Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-993871865

Yeah, sure we can have a final round of review. :smile:

### amustaque97 on 2021-12-15T13:15:11Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-994780482

> I take it that you don't intend to make further changes to the tests (as suggested by options 2 and 3) ? I'm asking just to confirm

Yes, I don't plan as of now 😅 . I'm not getting enough time to up-skill myself to learn about react-testing-library and make those changes. My suggestion is that we can create a separate issue and assign it to me. Maybe in coming new year holidays I can plan to submit a new PR `migrating existing test cases to react-testing-library`.

Let me know if that make sense to you  😄

### ntsekouras on 2021-12-15T15:41:23Z

URL: https://github.com/WordPress/gutenberg/pull/36835#issuecomment-994912762

Great work here @amustaque97 ! Also many thanks to the reviewers! 💯

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2021-11-30T16:53:54Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r759476695
Path: packages/components/src/date-time/date.js

```diff
@@ -70,43 +70,40 @@ function DatePickerDay( { day, events = [] } ) {
 	);
 }
 
-class DatePicker extends Component {
-	constructor() {
-		super( ...arguments );
-
-		this.onChangeMoment = this.onChangeMoment.bind( this );
-		this.nodeRef = createRef();
-		this.onMonthPreviewedHandler = this.onMonthPreviewedHandler.bind(
-			this
-		);
-	}
-
-	onMonthPreviewedHandler( newMonthDate ) {
-		this.props.onMonthPreviewed?.( newMonthDate.toISOString() );
-		this.keepFocusInside();
-	}
+function DatePicker( {
+	currentDate,
+	onChange,
+	events,
+	isInvalidDate,
+	onMonthPreviewed,
+} ) {
+	const nodeRef = useRef();
+	const onMonthPreviewedHandler = ( newMonthDate ) => {
+		onMonthPreviewed?.( newMonthDate.toISOString() );
+		keepFocusInside();
+	};
 
 	/*
 	 * Todo: We should remove this function ASAP.
 	 * It is kept because focus is lost when we click on the previous and next month buttons.
 	 * This focus loss closes the date picker popover.
 	 * Ideally we should add an upstream commit on react-dates to fix this issue.
 	 */
-	keepFocusInside() {
-		if ( ! this.nodeRef.current ) {
+	const keepFocusInside = () => {
```

Shouldn't `keepFocusInside` be defined _before_ `onMonthPreviewedHandler`?

#### ntsekouras on 2021-12-07T09:09:25Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r763781356
Path: packages/components/src/date-time/date.js

```diff
@@ -70,43 +70,40 @@ function DatePickerDay( { day, events = [] } ) {
 	);
 }
 
-class DatePicker extends Component {
-	constructor() {
-		super( ...arguments );
-
-		this.onChangeMoment = this.onChangeMoment.bind( this );
-		this.nodeRef = createRef();
-		this.onMonthPreviewedHandler = this.onMonthPreviewedHandler.bind(
-			this
-		);
-	}
-
-	onMonthPreviewedHandler( newMonthDate ) {
-		this.props.onMonthPreviewed?.( newMonthDate.toISOString() );
-		this.keepFocusInside();
-	}
+function DatePicker( {
+	currentDate,
+	onChange,
+	events,
+	isInvalidDate,
+	onMonthPreviewed,
+} ) {
+	const nodeRef = useRef();
+	const onMonthPreviewedHandler = ( newMonthDate ) => {
+		onMonthPreviewed?.( newMonthDate.toISOString() );
+		keepFocusInside();
+	};
 
 	/*
 	 * Todo: We should remove this function ASAP.
 	 * It is kept because focus is lost when we click on the previous and next month buttons.
 	 * This focus loss closes the date picker popover.
 	 * Ideally we should add an upstream commit on react-dates to fix this issue.
 	 */
-	keepFocusInside() {
-		if ( ! this.nodeRef.current ) {
+	const keepFocusInside = () => {
```

`onMonthPreviewedHandler` isn't called until all the code has run, so there isn't any TDZ(temporal dead zone) issue here.

### Review thread 2 (resolved)

#### ciampo on 2021-11-30T19:22:25Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r759590611
Path: packages/components/src/date-time/date.js

```diff
@@ -70,43 +70,40 @@ function DatePickerDay( { day, events = [] } ) {
 	);
 }
 
-class DatePicker extends Component {
-	constructor() {
-		super( ...arguments );
-
-		this.onChangeMoment = this.onChangeMoment.bind( this );
-		this.nodeRef = createRef();
-		this.onMonthPreviewedHandler = this.onMonthPreviewedHandler.bind(
-			this
-		);
-	}
-
-	onMonthPreviewedHandler( newMonthDate ) {
-		this.props.onMonthPreviewed?.( newMonthDate.toISOString() );
-		this.keepFocusInside();
-	}
+function DatePicker( {
+	currentDate,
+	onChange,
+	events,
+	isInvalidDate,
+	onMonthPreviewed,
```

A bit of a nit (and one may argue out of scope), but I noticed that the `onMonthPreviewed` prop isn't listed in the [README](https://github.com/WordPress/gutenberg/blob/cb8c98adc9a9071b2f6151499b6cc53d4900df56/packages/components/src/date-time/README.md#props) — would you mind adding it?

#### amustaque97 on 2021-12-11T21:35:56Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r767196027
Path: packages/components/src/date-time/date.js

```diff
@@ -70,43 +70,40 @@ function DatePickerDay( { day, events = [] } ) {
 	);
 }
 
-class DatePicker extends Component {
-	constructor() {
-		super( ...arguments );
-
-		this.onChangeMoment = this.onChangeMoment.bind( this );
-		this.nodeRef = createRef();
-		this.onMonthPreviewedHandler = this.onMonthPreviewedHandler.bind(
-			this
-		);
-	}
-
-	onMonthPreviewedHandler( newMonthDate ) {
-		this.props.onMonthPreviewed?.( newMonthDate.toISOString() );
-		this.keepFocusInside();
-	}
+function DatePicker( {
+	currentDate,
+	onChange,
+	events,
+	isInvalidDate,
+	onMonthPreviewed,
```

I have added `onMonthPreviewed` props in the README.md file. Please check it once, I know I may have not written enough info but I tried my best to update the docs and not to confuse users. 😅

### Review thread 3 (resolved)

#### ciampo on 2021-12-15T10:05:29Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r769467790
Path: packages/components/CHANGELOG.md

```diff
@@ -25,6 +25,7 @@
 ### Enhancements
 
 -   Wrapped `Modal` in a `forwardRef` call ([#36831](https://github.com/WordPress/gutenberg/pull/36831)).
+-	  Refactor `DateTime` class component to functional component ([#36835](https://github.com/WordPress/gutenberg/pull/36835))
```

Aligning white space to the other entries

```suggestion
-   Refactor `DateTime` class component to functional component ([#36835](https://github.com/WordPress/gutenberg/pull/36835))
```

#### amustaque97 on 2021-12-15T13:00:16Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r769603186
Path: packages/components/CHANGELOG.md

```diff
@@ -25,6 +25,7 @@
 ### Enhancements
 
 -   Wrapped `Modal` in a `forwardRef` call ([#36831](https://github.com/WordPress/gutenberg/pull/36831)).
+-	  Refactor `DateTime` class component to functional component ([#36835](https://github.com/WordPress/gutenberg/pull/36835))
```

OOPs silly mistakes. 😛

### Review thread 4 (resolved)

#### ciampo on 2021-12-15T10:09:12Z

URL: https://github.com/WordPress/gutenberg/pull/36835#discussion_r769470837
Path: packages/components/src/date-time/README.md

```diff
@@ -64,3 +64,10 @@ A callback function which receives a Date object representing a day as an argume
 
 -   Type: `Function`
 -   Required: No
+
+### onMonthPreviewed
+
+A callback function to display a new date and keep the focus on the calendar view while changing month.
```

```suggestion
A callback invoked when selecting the previous/next month in the date picker. The callback receives the new month date in the ISO format as an argument.
```

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/date-time/README.md (MODIFIED, +7/-0)
- packages/components/src/date-time/date.js (MODIFIED, +67/-84)
- packages/components/src/date-time/test/date.js (MODIFIED, +3/-29)
- packages/components/src/date-time/test/utils.js (ADDED, +32/-0)
- packages/components/src/date-time/utils.js (ADDED, +18/-0)
