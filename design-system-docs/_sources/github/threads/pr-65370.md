---
number: 65370
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/65370
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Feature] UI Components","[Package] Components","First-time Contributor","[Feature] Zoom Out"]
---

# PR #65370: Fix: Vertical toolbar placement in Zoom Out mode

- URL: https://github.com/WordPress/gutenberg/pull/65370
- Author: AhmarZaidi
- Created: 2024-09-16T10:48:04Z
- Updated: 2024-10-23T16:13:00Z
- Comments: 6 of 6
- Review threads: 2 of 2
- Changed files: 1 of 1
- Diff: ../pr-diffs/65370.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- In a few words, what is the PR actually doing? -->
This PR fixes the issue [#65188](https://github.com/WordPress/gutenberg/issues/65188)
It updates the Popover component (used by the zoom out toolbar) to observe changes in the `.interface-interface-skeleton__body` element and trigger position updates accordingly.
The `.interface-interface-skeleton__body` element is the parent div of the inserter.


## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
The current implementation does not update the popover toolbar's position when the inserter menu is opened in the zoom out mode. This PR ensures that the popover toolbar's position is updated correctly when layout changes occur to the parent inserter.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->
- Added a `MutationObserver` to observe changes in the parent div of the inserter.
- Updated the `whileElementsMounted` function (which updates the toolbar's position relative to the reference element) to include the parent div of the inserter as a reference for useFloating.

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
1. Enable experimental zoom out mode from Gutenberg settings. 
2. Open a post or page.
3. Insert a block or select an existing block.
4. Enter zoom out mode.
5. Verify if the floating toolbar is present, if not select a block again.
6. Now open the inserter menu (+ icon in the top left).
7. Verify that the floating toolbar is not getting detached from the block.


### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->
1. Enable experimental zoom out mode from Gutenberg settings. 
2. Open a post or page.
3. Insert a block or select an existing block.
4. Enter zoom out mode using tab to go to the zoom but button (top right).
5. Verify if the floating toolbar is present, if not select a block again.
6. Now open the inserter menu using tab key to navigate keyboard (+ icon in the top left).
7. Verify that the floating toolbar is not getting detached from the block.


## Screenshots or screencast <!-- if applicable -->
Before:
<img width="380" alt="image" src="https://github.com/user-attachments/assets/c507870e-24e4-4cee-bcba-9caff9ee54ee">


After:
<img width="380" alt="image" src="https://github.com/user-attachments/assets/24d392da-c690-4da7-95ca-56f0cd9e9ca2">

## Issue comments

### github-actions on 2024-09-16T10:48:26Z

URL: https://github.com/WordPress/gutenberg/pull/65370#issuecomment-2352579068

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @AhmarZaidi! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### github-actions on 2024-09-17T06:28:24Z

URL: https://github.com/WordPress/gutenberg/pull/65370#issuecomment-2354639946

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: AhmarZaidi <ahmarzaidi@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: getdave <get_dave@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### getdave on 2024-09-19T07:54:33Z

URL: https://github.com/WordPress/gutenberg/pull/65370#issuecomment-2360252689

Thanks for the PR. Will look to review 👍

### getdave on 2024-09-19T07:56:00Z

URL: https://github.com/WordPress/gutenberg/pull/65370#issuecomment-2360255534

@WordPress/gutenberg-components you are probably well placed to advise on this PR.

### getdave on 2024-10-03T09:08:19Z

URL: https://github.com/WordPress/gutenberg/pull/65370#issuecomment-2390905196

~@draganescu Did you notice if https://github.com/WordPress/gutenberg/pull/65627 solves the issue here?~

It doesn't solve it.

### jeryj on 2024-10-23T16:12:59Z

URL: https://github.com/WordPress/gutenberg/pull/65370#issuecomment-2432755962

Should this get closed now that the vertical toolbar has been replaced with the default block toolbar?

## Review threads

### Review thread 1

#### tyxla on 2024-09-23T19:21:55Z

URL: https://github.com/WordPress/gutenberg/pull/65370#discussion_r1771963007
Path: packages/components/src/popover/index.tsx

```diff
@@ -287,11 +287,45 @@ const UnforwardedPopover = (
 				? undefined
 				: normalizedPlacementFromProps,
 		middleware,
-		whileElementsMounted: ( referenceParam, floatingParam, updateParam ) =>
-			autoUpdate( referenceParam, floatingParam, updateParam, {
-				layoutShift: false,
-				animationFrame: true,
-			} ),
+		whileElementsMounted: (
+			referenceParam,
+			floatingParam,
+			updateParam
+		) => {
+			const cleanupAutoUpdate = autoUpdate(
+				referenceParam,
+				floatingParam,
+				updateParam,
+				{
+					layoutShift: false,
+					animationFrame: true,
+				}
+			);
+
+			// Observe mutations on the parent div of the inserter to update toolbar position.
+			const targetElement = document.querySelector(
+				'.interface-interface-skeleton__body'
+			);
```

Usually, we shouldn't work directly with DOM. Have you considered using a ref instead?

### Review thread 2

#### tyxla on 2024-09-23T19:24:41Z

URL: https://github.com/WordPress/gutenberg/pull/65370#discussion_r1771965799
Path: packages/components/src/popover/index.tsx

```diff
@@ -287,11 +287,45 @@ const UnforwardedPopover = (
 				? undefined
 				: normalizedPlacementFromProps,
 		middleware,
-		whileElementsMounted: ( referenceParam, floatingParam, updateParam ) =>
-			autoUpdate( referenceParam, floatingParam, updateParam, {
-				layoutShift: false,
-				animationFrame: true,
-			} ),
+		whileElementsMounted: (
+			referenceParam,
+			floatingParam,
+			updateParam
+		) => {
+			const cleanupAutoUpdate = autoUpdate(
+				referenceParam,
+				floatingParam,
+				updateParam,
+				{
+					layoutShift: false,
+					animationFrame: true,
+				}
+			);
+
+			// Observe mutations on the parent div of the inserter to update toolbar position.
+			const targetElement = document.querySelector(
+				'.interface-interface-skeleton__body'
```

The `Popover` component is part of `@wordpress/components`'s API. `interface-interface-skeleton__body` is declared within `InterfaceSkeleton`, which is part of `@wordpress/interface`. 

However, `@wordpress/components` does not know about `@wordpress/interface` at all, nor it should care about the inserter, so tracking mutations of an unknown, unrelated to `Popover` element doesn't make much sense. 

If this is the right fix, it likely needs to be part of `InterfaceSkeleton`'s implementation, or the inserter, and not part of `Popover`'s internals. 

I'm not convinced this is the right fix, anyway. This looks like something that should be resolved within `floating-ui`, cc @ciampo and @jsnajdr who know its internals better than me.

#### ciampo on 2024-09-24T09:22:02Z

URL: https://github.com/WordPress/gutenberg/pull/65370#discussion_r1772980538
Path: packages/components/src/popover/index.tsx

```diff
@@ -287,11 +287,45 @@ const UnforwardedPopover = (
 				? undefined
 				: normalizedPlacementFromProps,
 		middleware,
-		whileElementsMounted: ( referenceParam, floatingParam, updateParam ) =>
-			autoUpdate( referenceParam, floatingParam, updateParam, {
-				layoutShift: false,
-				animationFrame: true,
-			} ),
+		whileElementsMounted: (
+			referenceParam,
+			floatingParam,
+			updateParam
+		) => {
+			const cleanupAutoUpdate = autoUpdate(
+				referenceParam,
+				floatingParam,
+				updateParam,
+				{
+					layoutShift: false,
+					animationFrame: true,
+				}
+			);
+
+			// Observe mutations on the parent div of the inserter to update toolbar position.
+			const targetElement = document.querySelector(
+				'.interface-interface-skeleton__body'
```

Yup, Marin is right. The `@wordpress/components` package shouldn't be aware of the `@wordpress/interface` package (since it's part of the Gutenberg app), and can't be tightly coupled to any of its references.

We should look for another way to fix this.

My gut feeling tells me that the bug is currently caused by popover anchor updates not being tracked correctly and used to re-render the popover.

#### AhmarZaidi on 2024-09-24T12:26:21Z

URL: https://github.com/WordPress/gutenberg/pull/65370#discussion_r1773245321
Path: packages/components/src/popover/index.tsx

```diff
@@ -287,11 +287,45 @@ const UnforwardedPopover = (
 				? undefined
 				: normalizedPlacementFromProps,
 		middleware,
-		whileElementsMounted: ( referenceParam, floatingParam, updateParam ) =>
-			autoUpdate( referenceParam, floatingParam, updateParam, {
-				layoutShift: false,
-				animationFrame: true,
-			} ),
+		whileElementsMounted: (
+			referenceParam,
+			floatingParam,
+			updateParam
+		) => {
+			const cleanupAutoUpdate = autoUpdate(
+				referenceParam,
+				floatingParam,
+				updateParam,
+				{
+					layoutShift: false,
+					animationFrame: true,
+				}
+			);
+
+			// Observe mutations on the parent div of the inserter to update toolbar position.
+			const targetElement = document.querySelector(
+				'.interface-interface-skeleton__body'
```

Hey @ciampo & @tyxla,
Thanks for the review!

I understand now that `interface-interface-skeleton__body` is part of `@wordpress/interface` and code from `@wordpress/components` should not interact/depend on it.

The popover toolbar ref points to the currently selected block div, so that appears to be set correctly but as mentioned by @ciampo, the block div does not register layout changes when the inserter is toggled, thus it's not updating the position of toolbar. 

Since, the inserter is added to the `interface-interface-skeleton__body`, I added a mutation observer to it. It works but it's not the right solution. I have also tested putting `layoutShift` to true but that doesn't seem to work.
https://github.com/WordPress/gutenberg/blob/2a80118931756c72b5cb5d1b728117ddb6749ffc/packages/components/src/popover/index.tsx#L292

Will explore more in the suggested direction to find another fix for this issue.

## Changed files

- packages/components/src/popover/index.tsx (MODIFIED, +39/-5)
