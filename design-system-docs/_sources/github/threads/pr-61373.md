---
number: 61373
type: pull_request
state: MERGED
merged_at: 2024-05-10T13:00:20Z
url: https://github.com/WordPress/gutenberg/pull/61373
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Automated Testing","[Package] Components","First-time Contributor"]
---

# PR #61373: Convert FocalPointPicker tests to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/61373
- Author: jpstevens
- Created: 2024-05-03T20:37:05Z
- Updated: 2024-05-10T14:53:37Z
- Comments: 3 of 3
- Review threads: 2 of 2
- Changed files: 2 of 2
- Diff: ../pr-diffs/61373.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
This PR converts the `index` and `media` tests for the `FocalPointPicker` to TypeScript.

## Why?
Adding TypeScript support resolves this issue: https://github.com/WordPress/gutenberg/issues/60529

## How?
- converted both files to `.tsx`
- fixed missing property errors by passing default `{ ...props }` to components (similar to [this test](https://github.com/WordPress/gutenberg/blob/c56672504bada434aee1ec331366cd2a10ab2b38/packages/components/src/border-box-control/test/index.tsx#L40))
- for `"should handle legacy string values"` test, cast string values `as unknown as number` to avoid changing `FocalPoint` type to _officially_ support `string` values as well as `number`

If the preference is to _officially_ support string values for focal points let me know and I'll update `FocalPoint` type to `Record< FocalPointAxis, number | string >` - however this will require further changes to the `FocalPointPicker` implementation.

## Testing Instructions

`npm run test:unit -- packages/components/src/focal-point-picker` tests pass (see screenshot below)
`npm run build:package-types` returns a zero exit code

## Screenshots or screencast <!-- if applicable -->
<img width="844" alt="image" src="https://github.com/WordPress/gutenberg/assets/6638657/7b19dbee-c0f2-414d-9792-0ff69c76def6">

## Issue comments

### github-actions on 2024-05-03T20:37:21Z

URL: https://github.com/WordPress/gutenberg/pull/61373#issuecomment-2093729253

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: jpstevens <jpstevens@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2024-05-03T20:37:27Z

URL: https://github.com/WordPress/gutenberg/pull/61373#issuecomment-2093729378

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @jpstevens! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### jpstevens on 2024-05-10T12:58:43Z

URL: https://github.com/WordPress/gutenberg/pull/61373#issuecomment-2104566526

I synced with `trunk`, seems to have resolved the failing task. Thanks for the review @tyxla!

## Review threads

### Review thread 1

#### tyxla on 2024-05-09T10:33:04Z

URL: https://github.com/WordPress/gutenberg/pull/61373#discussion_r1595275904
Path: packages/components/src/focal-point-picker/test/media.tsx

```diff
@@ -7,19 +7,25 @@ import { render, screen } from '@testing-library/react';
  * Internal dependencies
  */
 import Media from '../media';
+import type { FocalPointPickerMediaProps } from '../types';
+
+const props: FocalPointPickerMediaProps = {
+	alt: '',
+	src: '',
```

Literally every test will have the `data-testid="media"` prop, should we add this to `props` and remove that prop from the tests?

We might need to add the `data-testid` prop to `FocalPointPickerMediaProps` since that prop doesn't exist there.

#### jpstevens on 2024-05-09T18:01:20Z

URL: https://github.com/WordPress/gutenberg/pull/61373#discussion_r1595797495
Path: packages/components/src/focal-point-picker/test/media.tsx

```diff
@@ -7,19 +7,25 @@ import { render, screen } from '@testing-library/react';
  * Internal dependencies
  */
 import Media from '../media';
+import type { FocalPointPickerMediaProps } from '../types';
+
+const props: FocalPointPickerMediaProps = {
+	alt: '',
+	src: '',
```

Good question - I've updated the default prop object to include `'data-testid': 'media'`

The change is only required for testing, so I opted **_not_** to change the underlying `FocalPointPickerMediaProps` type definition. 

The current implementation of `Media` uses pass-through props, which allow any prop to be passed to the child component, including `data-testid`

#### jpstevens on 2024-05-09T18:01:25Z

URL: https://github.com/WordPress/gutenberg/pull/61373#discussion_r1595797594
Path: packages/components/src/focal-point-picker/test/media.tsx

```diff
@@ -7,19 +7,25 @@ import { render, screen } from '@testing-library/react';
  * Internal dependencies
  */
 import Media from '../media';
+import type { FocalPointPickerMediaProps } from '../types';
+
+const props: FocalPointPickerMediaProps = {
+	alt: '',
+	src: '',
```

Interestingly, I couldn't find any tests in the codebase where the `data-testid` prop is not repeated on each component:
- [Elevation](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/elevation/test/index.tsx) repeats `data-testid="elevation"`
- [Grid](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/grid/test/grid.tsx) repeats `data-testid="grid"`
- [Panel](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/panel/test/body.tsx) repeats `data-testid="inner-content"`

There is one example that extracts the `data-testid` value to a const, but it doesn't prevent the prop repetition on the components:
- [Icon](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/icon/test/index.tsx) repeats `data-testid={ testId }`

#### tyxla on 2024-05-10T09:06:50Z

URL: https://github.com/WordPress/gutenberg/pull/61373#discussion_r1596495575
Path: packages/components/src/focal-point-picker/test/media.tsx

```diff
@@ -7,19 +7,25 @@ import { render, screen } from '@testing-library/react';
  * Internal dependencies
  */
 import Media from '../media';
+import type { FocalPointPickerMediaProps } from '../types';
+
+const props: FocalPointPickerMediaProps = {
+	alt: '',
+	src: '',
```

Thanks for taking such a thorough look! This was a very minor detail and TBH I'm good with you've come up with 👍

### Review thread 2 (resolved)

#### tyxla on 2024-05-09T10:34:35Z

URL: https://github.com/WordPress/gutenberg/pull/61373#discussion_r1595277332
Path: packages/components/src/focal-point-picker/test/index.tsx

```diff
@@ -8,6 +8,19 @@ import userEvent from '@testing-library/user-event';
  * Internal dependencies
  */
 import Picker from '..';
+import type { FocalPointPickerProps } from '../types';
+
+type Log = { name: string; args: any[] };
+type EventLogger = ( name: string, args: any[] ) => void;
```

Should we use `unknown` instead of `any` for the `args`?

```suggestion
type Log = { name: string; args: unknown[] };
type EventLogger = ( name: string, args: unknown[] ) => void;
```

## Changed files

- packages/components/src/focal-point-picker/test/index.tsx (RENAMED, +44/-17)
- packages/components/src/focal-point-picker/test/media.tsx (RENAMED, +18/-13)
