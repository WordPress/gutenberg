---
number: 67815
type: pull_request
state: MERGED
merged_at: 2024-12-12T07:33:58Z
url: https://github.com/WordPress/gutenberg/pull/67815
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Focus] Accessibility (a11y)","[Package] Components"]
---

# PR #67815: CustomSelectControl: Refactor to use Ariakit store state for current value

- URL: https://github.com/WordPress/gutenberg/pull/67815
- Author: yogeshbhutkar
- Created: 2024-12-11T08:28:24Z
- Updated: 2024-12-12T07:34:38Z
- Comments: 3 of 3
- Review threads: 3 of 3
- Changed files: 2 of 2
- Diff: ../pr-diffs/67815.diff

## Body

Fixes: #67759 

## What?  
This PR fixes a bug where the Button component's description fails to update correctly when uncontrolled.

## How?  
The implementation replaces `store.getState()` with `Ariakit.useStoreState(store)` to accurately retrieve the current value, ensuring the description updates as expected.

## Testing Instructions
1. Add the below code snippet to the storybook.
```js
export const Uncontrolled = {
	args: {
		...Default.args,
	},
};
```
2. In DevTools, checkout out the accessibility panel for the main combobox button. The button description will now update after changing the selected options.

## Screencast

### Before:

https://github.com/user-attachments/assets/89980a97-6762-4efd-bb74-22d46fd1dcf4


### After:

![Custom Select Control After](https://github.com/user-attachments/assets/1aa60325-a69e-410f-a6d7-a6a8d33a2c2d)

## Issue comments

### yogeshbhutkar on 2024-12-11T08:44:38Z

URL: https://github.com/WordPress/gutenberg/pull/67815#issuecomment-2535168256

This PR also works well with the previously raised issue #67721 and takes a different approach as mentioned in #67733.


https://github.com/user-attachments/assets/172fe0a0-a140-4ce0-81cc-2c79fd3b0f7d

### github-actions on 2024-12-11T08:46:36Z

URL: https://github.com/WordPress/gutenberg/pull/67815#issuecomment-2535182881

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: yogeshbhutkar <yogeshbhutkar@git.wordpress.org>
Co-authored-by: Mamaduka <mamaduka@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
Co-authored-by: mirka <0mirka00@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### yogeshbhutkar on 2024-12-12T04:53:37Z

URL: https://github.com/WordPress/gutenberg/pull/67815#issuecomment-2537812221

Added changelog entry.

CC: @Mamaduka

## Review threads

### Review thread 1 (resolved)

#### Mamaduka on 2024-12-11T08:56:16Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879643681
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +151,14 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
-
 	const renderSelectedValueHint = () => {
 		const selectedOptionHint = options
 			?.map( applyOptionDeprecations )
 			?.find( ( { name } ) => store.getState().value === name )?.hint;
```

I think changes on this line from #67733 also need to be reverted.

#### yogeshbhutkar on 2024-12-11T08:58:24Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879646871
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +151,14 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
-
 	const renderSelectedValueHint = () => {
 		const selectedOptionHint = options
 			?.map( applyOptionDeprecations )
 			?.find( ( { name } ) => store.getState().value === name )?.hint;
```

@Mamaduka, in the new PR, these have already been reverted to use currentValue instead of getState().value again. Of course, the way currentValue is determined is slightly different. Does this align with what we were aiming for?

#### Mamaduka on 2024-12-11T09:04:33Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879655716
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +151,14 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
-
 	const renderSelectedValueHint = () => {
 		const selectedOptionHint = options
 			?.map( applyOptionDeprecations )
 			?.find( ( { name } ) => store.getState().value === name )?.hint;
```

Which new PR? Here, I only see the current value reverted on line 161.

#### yogeshbhutkar on 2024-12-11T09:09:51Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879663290
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +151,14 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
-
 	const renderSelectedValueHint = () => {
 		const selectedOptionHint = options
 			?.map( applyOptionDeprecations )
 			?.find( ( { name } ) => store.getState().value === name )?.hint;
```

Got it, I think you're referring to the change that happened on line 157. I'll add a commit to fix it. It would just become:

```js
const renderSelectedValueHint = () => {
		const selectedOptionHint = options
			?.map( applyOptionDeprecations )
			?.find( ( { name } ) => currentValue === name )?.hint;
```

Apologies for missing it 🙇‍♂️

#### Mamaduka on 2024-12-11T09:11:59Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879666268
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +151,14 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
-
 	const renderSelectedValueHint = () => {
 		const selectedOptionHint = options
 			?.map( applyOptionDeprecations )
 			?.find( ( { name } ) => store.getState().value === name )?.hint;
```

Corret! Thanks, @yogeshbhutkar!

### Review thread 2 (resolved)

#### Mamaduka on 2024-12-11T09:35:12Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879709161
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +149,16 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
+	const { value: currentValue } = Ariakit.useStoreState( store );
```

You can also access a specific value from the state by passing a second argument. However, I'm not sure if it makes any difference performance-wise.

```js
const currentValue = Ariakit.useStoreState( store, 'value' );
```

#### yogeshbhutkar on 2024-12-11T10:31:49Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1879808271
Path: packages/components/src/custom-select-control/index.tsx

```diff
@@ -149,16 +149,16 @@ function CustomSelectControl< T extends CustomSelectOption >(
 			);
 		} );
 
-	const { value: currentValue } = store.getState();
+	const { value: currentValue } = Ariakit.useStoreState( store );
```

Thanks for the suggestion @Mamaduka. It looks more readable for sure. I've incorporated it in the latest commit 🙌.

### Review thread 3

#### Mamaduka on 2024-12-12T06:21:54Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1881452471
Path: packages/components/CHANGELOG.md

```diff
@@ -46,6 +46,7 @@
 
 -   `ResizableBox`: Make drag handles focusable ([#67305](https://github.com/WordPress/gutenberg/pull/67305)).
 -   `CustomSelectControl`: Update correctly when `showSelectedHint` is enabled ([#67733](https://github.com/WordPress/gutenberg/pull/67733)).
+-   `CustomSelectControl`: use `useStoreState` to get `currentValue` for optimistic updates ([#67815](https://github.com/WordPress/gutenberg/pull/67815))
```

```suggestion
-   `CustomSelectControl`: Use `useStoreState` to get `currentValue` and avoid stale values ([#67815](https://github.com/WordPress/gutenberg/pull/67815)).
```

A minor suggestion. The optimistic updates refer to a different pattern, especially after the React 19 release.

#### yogeshbhutkar on 2024-12-12T06:45:47Z

URL: https://github.com/WordPress/gutenberg/pull/67815#discussion_r1881472372
Path: packages/components/CHANGELOG.md

```diff
@@ -46,6 +46,7 @@
 
 -   `ResizableBox`: Make drag handles focusable ([#67305](https://github.com/WordPress/gutenberg/pull/67305)).
 -   `CustomSelectControl`: Update correctly when `showSelectedHint` is enabled ([#67733](https://github.com/WordPress/gutenberg/pull/67733)).
+-   `CustomSelectControl`: use `useStoreState` to get `currentValue` for optimistic updates ([#67815](https://github.com/WordPress/gutenberg/pull/67815))
```

Thanks for the suggestion @Mamaduka. Fixed in the latest commit 🚀.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/custom-select-control/index.tsx (MODIFIED, +3/-3)
