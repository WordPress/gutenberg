---
number: 63106
type: pull_request
state: MERGED
merged_at: 2024-07-04T10:30:14Z
url: https://github.com/WordPress/gutenberg/pull/63106
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #63106: TimeInput: Add `label` prop

- URL: https://github.com/WordPress/gutenberg/pull/63106
- Author: mirka
- Created: 2024-07-03T20:37:51Z
- Updated: 2024-07-04T11:35:56Z
- Comments: 1 of 1
- Review threads: 1 of 1
- Changed files: 4 of 4
- Diff: ../pr-diffs/63106.diff

## Body

Part of #52734
Follow-up to #60613

## What?

Adds a `label` prop to `TimeInput`.

## Why?

Since some people will be using this as an alternative to `TimePicker`, we need a way to add a visual label as well. (Technically, it needs to be a fieldset legend rather than a `label`.)

## Testing Instructions

See Storybook for TimeInput to test behavior.

(Code changes are easier to review with whitespace hidden)

## Issue comments

### github-actions on 2024-07-03T20:38:42Z

URL: https://github.com/WordPress/gutenberg/pull/63106#issuecomment-2207225901

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### ciampo on 2024-07-04T10:32:10Z

URL: https://github.com/WordPress/gutenberg/pull/63106#discussion_r1665507643
Path: packages/components/src/date-time/types.ts

```diff
@@ -61,6 +61,11 @@ export type TimeInputProps = {
 	 */
 	minutesProps?: React.ComponentProps< typeof MinutesInput >;
 
+	/**
+	 * The label for the time input.
+	 */
+	label?: string;
```

Should we consider making the `label` required by default, so that when the input is used standalone consumers are nudged towards labelling it? We could disable the rule when compositing is in `DateTimePicker`.

#### mirka on 2024-07-04T11:35:56Z

URL: https://github.com/WordPress/gutenberg/pull/63106#discussion_r1665580249
Path: packages/components/src/date-time/types.ts

```diff
@@ -61,6 +61,11 @@ export type TimeInputProps = {
 	 */
 	minutesProps?: React.ComponentProps< typeof MinutesInput >;
 
+	/**
+	 * The label for the time input.
+	 */
+	label?: string;
```

I don't think it's a huge issue for this one specifically, because the hours and minutes inputs are both labeled.

In the general case though (re: #51740), I'm starting to think that an eslint rule for our repo might be a good way to nudge devs without compromising the "truthfulness" of our TS types.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/date-time/stories/time-input.story.tsx (MODIFIED, +3/-0)
- packages/components/src/date-time/time-input/index.tsx (MODIFIED, +96/-74)
- packages/components/src/date-time/types.ts (MODIFIED, +5/-0)
