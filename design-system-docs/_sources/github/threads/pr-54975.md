---
number: 54975
type: pull_request
state: MERGED
merged_at: 2023-10-03T07:28:06Z
url: https://github.com/WordPress/gutenberg/pull/54975
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #54975: Wrapped `TextareaControl` in a `forwardRef` call

- URL: https://github.com/WordPress/gutenberg/pull/54975
- Author: t-hamano
- Created: 2023-10-02T14:51:42Z
- Updated: 2023-10-03T07:28:36Z
- Comments: 0 of 0
- Review threads: 1 of 1
- Changed files: 2 of 2
- Diff: ../pr-diffs/54975.diff

## Body

Closes: #54723

## What?

This PR wraps the `TextAreaControl` component in a `forwardRef` call.

## Why?

Many components, not just form elements, are wrapped in `forwardRef` calls. This component is also a form element, and exposing the ref may also be useful to third-party developers.

## Testing Instructions

I wrote a simple code to test the `ref` prop. Update the code block's Edit component (`packages/block-library/src/code/edit.js`) with this code.

```javascript
/**
 * WordPress dependencies
 */
import { useBlockProps } from '@wordpress/block-editor';
import { TextareaControl } from '@wordpress/components';
import { useRef } from '@wordpress/element';

export default function CodeEdit() {
	const ref = useRef();
	const blockProps = useBlockProps();

	return (
		<div { ...blockProps }>
			<TextareaControl ref={ ref } />
			<button
				onClick={ () => {
					ref.current.focus();
					ref.current.value = 'Focued!';
				} }
			>
				Focus on textarea!
			</button>
		</div>
	);
}
```

- Insert a Code block in the block editor and click the button.
- The textarea should now have focus and be filled with a value.

## Screenshots or screencast <!-- if applicable -->

https://github.com/WordPress/gutenberg/assets/54422211/1f0acd17-ad4c-40c4-a733-c724561c6db2

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### ciampo on 2023-10-02T21:44:43Z

URL: https://github.com/WordPress/gutenberg/pull/54975#discussion_r1343195713
Path: packages/components/src/textarea-control/index.tsx

```diff
@@ -1,12 +1,13 @@
 /**
  * External dependencies
  */
-import type { ChangeEvent } from 'react';
+import type { ChangeEvent, ForwardedRef } from 'react';
```

Nit: could use the `React` global instead of importing the type?

Something like `React.ForwardedRef` and `React.ChangeEvent`. This is mostly useful to avoid getting confused between react synthetic event types and standard DOM event types

#### t-hamano on 2023-10-03T01:50:11Z

URL: https://github.com/WordPress/gutenberg/pull/54975#discussion_r1343346655
Path: packages/components/src/textarea-control/index.tsx

```diff
@@ -1,12 +1,13 @@
 /**
  * External dependencies
  */
-import type { ChangeEvent } from 'react';
+import type { ChangeEvent, ForwardedRef } from 'react';
```

Thanks for the review!

- In d3eba32, I used React global.
- In e649d10, I forgot to move the code example comment, so I fixed it.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/textarea-control/index.tsx (MODIFIED, +31/-31)
