---
number: 68301
type: pull_request
state: MERGED
merged_at: 2024-12-26T16:36:28Z
url: https://github.com/WordPress/gutenberg/pull/68301
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Package] Components"]
---

# PR #68301: Components: Prevent broken lists in auto-generated readmes

- URL: https://github.com/WordPress/gutenberg/pull/68301
- Author: mirka
- Created: 2024-12-25T23:30:22Z
- Updated: 2024-12-26T16:37:05Z
- Comments: 2 of 2
- Review threads: 2 of 2
- Changed files: 12 of 12
- Diff: ../pr-diffs/68301.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?

Prevents broken lists (`ul`) in the auto-generated component READMEs, by inverting the order of the "prop data" `ul` and the prop description.

## Why?

Because we can assume that a prop description will always start with a paragraph (not `ul`), flipping the order here should solve the "ambiguous run-on list" problem when the prop description ends with a list.

### Before

```md
### `myProp`

This is my prop description. The prop does three things:

- Foo
- Bar
- Baz

- Type: `string`
- Required: No
```

The ambiguously separated Markdown lists will render unreliably, depending on the HTML converter implementation.

### After

```md
### `myProp`

- Type: `string`
- Required: No

This is my prop description. The prop does three things:

- Foo
- Bar
- Baz
```

Now the lists are clearly separated.

## Testing Instructions

The READMEs re-generated by `npm run docs:components` do not include ambiguous lists, even when a prop description ends with a list.

## Issue comments

### github-actions on 2024-12-25T23:52:25Z

URL: https://github.com/WordPress/gutenberg/pull/68301#issuecomment-2562026964

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 4690b1de6f09beb04fa3b3911fff07c78afdc3eb.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/12503550187
📝  Reported issues:
- #64334 in `/test/e2e/specs/site-editor/dataviews-list-layout-keyboard.spec.js`
- #61884 in `/test/e2e/specs/editor/blocks/navigation-frontend-interactivity.spec.js`

### github-actions on 2024-12-26T12:13:38Z

URL: https://github.com/WordPress/gutenberg/pull/68301#issuecomment-2562594909

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: mirka <0mirka00@git.wordpress.org>
Co-authored-by: tyxla <tyxla@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1

#### mirka on 2024-12-26T11:57:19Z

URL: https://github.com/WordPress/gutenberg/pull/68301#discussion_r1897875718
Path: bin/api-docs/gen-components-docs/markdown/index.mjs

```diff
@@ -9,17 +9,14 @@ import json2md from 'json2md';
 import { generateMarkdownPropsJson } from './props.mjs';
 
 /**
- * If the string is contentful, ensure that it ends with a single newline.
- * Otherwise normalize to `undefined`.
+ * Converter for strings that are already formatted as Markdown.
  *
- * @param {string} [str]
+ * @param {string} [input]
+ * @return {string} The trimmed input if it is contentful, otherwise an empty string.
  */
-function normalizeTrailingNewline( str ) {
-	if ( ! str?.trim() ) {
-		return undefined;
-	}
-	return str.replace( /\n*$/, '\n' );
-}
+json2md.converters.md = ( input ) => {
+	return input?.trim() || '';
+};
```

I realized you can add custom converters like this, which makes things more simpler and readable overall. When you use a converter, `json2md` adds a trailing newline automatically.

The removal of redundant newlines is moved to the end of this file, which will clean the entire output.

#### tyxla on 2024-12-26T12:25:36Z

URL: https://github.com/WordPress/gutenberg/pull/68301#discussion_r1897889009
Path: bin/api-docs/gen-components-docs/markdown/index.mjs

```diff
@@ -9,17 +9,14 @@ import json2md from 'json2md';
 import { generateMarkdownPropsJson } from './props.mjs';
 
 /**
- * If the string is contentful, ensure that it ends with a single newline.
- * Otherwise normalize to `undefined`.
+ * Converter for strings that are already formatted as Markdown.
  *
- * @param {string} [str]
+ * @param {string} [input]
+ * @return {string} The trimmed input if it is contentful, otherwise an empty string.
  */
-function normalizeTrailingNewline( str ) {
-	if ( ! str?.trim() ) {
-		return undefined;
-	}
-	return str.replace( /\n*$/, '\n' );
-}
+json2md.converters.md = ( input ) => {
+	return input?.trim() || '';
+};
```

TIL, this is very cool!

### Review thread 2

#### tyxla on 2024-12-26T12:26:53Z

URL: https://github.com/WordPress/gutenberg/pull/68301#discussion_r1897889707
Path: bin/api-docs/gen-components-docs/markdown/index.mjs

```diff
@@ -49,5 +46,5 @@ export function generateMarkdownDocs( { typeDocs, subcomponentTypeDocs } ) {
 
 	return json2md(
 		[ ...mainDocsJson, ...subcomponentDocsJson ].filter( Boolean )
-	);
+	).replace( /\n+$/gm, '\n' ); // clean unnecessary consecutive newlines
```

Noting that this cleans up only trailing ones, maybe we want to update the comment to reflect that?

#### mirka on 2024-12-26T16:35:35Z

URL: https://github.com/WordPress/gutenberg/pull/68301#discussion_r1898010668
Path: bin/api-docs/gen-components-docs/markdown/index.mjs

```diff
@@ -49,5 +46,5 @@ export function generateMarkdownDocs( { typeDocs, subcomponentTypeDocs } ) {
 
 	return json2md(
 		[ ...mainDocsJson, ...subcomponentDocsJson ].filter( Boolean )
-	);
+	).replace( /\n+$/gm, '\n' ); // clean unnecessary consecutive newlines
```

[This](https://regex101.com/r/YHxZ1L/2) is a `global` + `multiline` regex, intended to remove all excessive newlines within in the entire readme output.

## Changed files

- bin/api-docs/gen-components-docs/markdown/index.mjs (MODIFIED, +9/-12)
- bin/api-docs/gen-components-docs/markdown/props.mjs (MODIFIED, +1/-1)
- packages/components/src/alignment-matrix-control/README.md (MODIFIED, +15/-15)
- packages/components/src/angle-picker-control/README.md (MODIFIED, +9/-9)
- packages/components/src/badge/README.md (MODIFIED, +4/-4)
- packages/components/src/base-control/README.md (MODIFIED, +20/-21)
- packages/components/src/box-control/README.md (MODIFIED, +29/-29)
- packages/components/src/button/README.md (MODIFIED, +50/-50)
- packages/components/src/form-file-upload/README.md (MODIFIED, +21/-21)
- packages/components/src/gradient-picker/README.md (MODIFIED, +35/-35)
- packages/components/src/icon/README.md (MODIFIED, +8/-8)
- packages/components/src/tree-select/README.md (MODIFIED, +39/-39)
