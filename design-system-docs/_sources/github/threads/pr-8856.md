---
number: 8856
type: pull_request
state: MERGED
merged_at: 2018-08-16T14:46:42Z
url: https://github.com/WordPress/gutenberg/pull/8856
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Package] Components"]
---

# PR #8856: Add styles for Notice component

- URL: https://github.com/WordPress/gutenberg/pull/8856
- Author: mmtr
- Created: 2018-08-10T22:35:15Z
- Updated: 2018-08-30T09:54:43Z
- Comments: 5 of 5
- Review threads: 5 of 5
- Changed files: 12 of 12
- Diff: ../pr-diffs/8856.diff

## Body

It adds the needed styles by the `Notice` component, so it is displayed correctly even if the WordPress Core styles are not available.

## Issue comments

### mmtr on 2018-08-11T11:06:24Z

URL: https://github.com/WordPress/gutenberg/pull/8856#issuecomment-412268246

@gziolo @youknowriad

### youknowriad on 2018-08-15T13:16:53Z

URL: https://github.com/WordPress/gutenberg/pull/8856#issuecomment-413194198

The dismiss button doesn't look centered vertically to me. Is this related to this changes?
<img width="91" alt="screen shot 2018-08-15 at 14 16 09" src="https://user-images.githubusercontent.com/272444/44149977-da696d6a-a095-11e8-927f-dfe271a245b4.png">

### mmtr on 2018-08-15T14:39:06Z

URL: https://github.com/WordPress/gutenberg/pull/8856#issuecomment-413218388

I've just realized that the original WordPress notice was placing the dismiss button at the top right corner, so I have removed the vertical alignment.

Original:
![screen shot 2018-08-15 at 16 32 16](https://user-images.githubusercontent.com/1233880/44153871-9e157426-a0a9-11e8-9db1-092b1ac348ef.png)

New:
![screen shot 2018-08-15 at 16 38 32](https://user-images.githubusercontent.com/1233880/44153899-ab12bfa8-a0a9-11e8-8bae-98b3c571679f.png)

### youknowriad on 2018-08-16T14:47:02Z

URL: https://github.com/WordPress/gutenberg/pull/8856#issuecomment-413571522

I went ahead and tweaked the margin a bit. Let me know if it doesn't work for you.

### afercia on 2018-08-30T09:46:40Z

URL: https://github.com/WordPress/gutenberg/pull/8856#issuecomment-417259600

This PR made the dismiss button tooltip not visible with `tooltip={ false }`. Worth reminding all UI controls must have a visible label, or at least a way to expose their accessible name (and tooltips are already a trade-off).
Imagine speech recognition software user: what command are they supposed to voice to dismiss the notices?
"Click X"? won't work
"Click close"? won't work
How are they supposed to discover the control name is "Dismiss this notice" if it's not exposed in the UI in any way? The tooltip allows, at least, to discover what the control name is and once users have learned it, they can use it directly the following times just voicing the command "Click Dismiss this button".

Will fix in #9443.

## Review threads

### Review thread 1

#### youknowriad on 2018-08-13T07:41:08Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r209515438
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,19 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice components-notice--' + status, {
```

Should this be `__` instead of `--`

#### youknowriad on 2018-08-13T07:44:03Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r209516036
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,19 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice components-notice--' + status, {
```

Actually, it's looks like this is a modifier. I think in general our guideline is to use unprefixed booleans. So maybe just: `is-warning`, `is-error`...

#### gziolo on 2018-08-13T07:58:00Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r209519562
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,19 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice components-notice--' + status, {
```

@jasmussen will know what's the best way to deal with it 😃

#### jasmussen on 2018-08-13T08:06:44Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r209521553
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,19 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice components-notice--' + status, {
```

I think I'm feeling what Riad is saying. 

I believe we have these notices:

- Green (Success)
- Orange (Warning)
- Red (Error)

I can't recall if there's also a blue informational notice, if there isn't there probably should be.

The default notice should be an informational notice. In addition to that, we could have is-succes, is-warning, is-error. 

Did this make sense?

#### mmtr on 2018-08-13T13:45:09Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r209613762
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,19 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice components-notice--' + status, {
```

Yep. It's consistent with what we do in other components. I have just committed the change using these new modifiers.

### Review thread 2

#### youknowriad on 2018-08-15T14:22:02Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210284551
Path: packages/editor/src/components/contrast-checker/index.js

```diff
@@ -36,7 +36,7 @@ function ContrastChecker( {
 		__( 'This color combination may be hard for people to read. Try using a brighter background color and/or a darker text color.' );
 	return (
 		<div className="editor-contrast-checker">
-			<Notice status="warning" isDismissible={ false }>
+			<Notice status="warning" isDismissible={ true }>
```

debug change ;)

#### mmtr on 2018-08-15T14:39:38Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210290802
Path: packages/editor/src/components/contrast-checker/index.js

```diff
@@ -36,7 +36,7 @@ function ContrastChecker( {
 		__( 'This color combination may be hard for people to read. Try using a brighter background color and/or a darker text color.' );
 	return (
 		<div className="editor-contrast-checker">
-			<Notice status="warning" isDismissible={ false }>
+			<Notice status="warning" isDismissible={ true }>
```

oops :)

### Review thread 3

#### youknowriad on 2018-08-15T15:06:20Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210300475
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
```

This should be moved to "Internal dependencies"

### Review thread 4

#### youknowriad on 2018-08-15T15:07:28Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210300839
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

Can we replace this by `IconButton` instead of `button`.  I think it already takes care of the scree reader label (label prop) etc...

#### mmtr on 2018-08-15T15:24:41Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210306467
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

I tried to do it, but it presents a problem with the hover styles. Either we use exactly the same [rule](https://github.com/WordPress/gutenberg/blob/a0e08470d64366d5edb1017f249429f1afd18ee7/packages/components/src/icon-button/style.scss#L24) defined in the `IconButton` styles for the `Notice` styles so we have the same specificness, or we add a `!important` to override it.

![screen shot 2018-08-15 at 17 20 33](https://user-images.githubusercontent.com/1233880/44156168-8ec96e22-a0af-11e8-87a9-eaff1ab0d408.png)

I didn't like any of the options, so that's why I left the `button`. What do you think?

#### youknowriad on 2018-08-15T15:28:52Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210308033
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

cc @jasmussen for design insights but I'm pretty sure we can override without `!important` (repeating the same specificity, granted that it's not great either)

#### youknowriad on 2018-08-15T15:29:24Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210308198
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

Btw, I don't consider this a blocker for the PR.

#### jasmussen on 2018-08-16T06:41:06Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210490187
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

Yeah, those hover styles are a bit arduous due to having to work with a number of accessibility concerns. However you can override them without !importants, by using the same level of specificity.

For example, if the hover is `.components-icon-button:not(:disabled):_not([aria-disabled="true"]):not(.is-default):hover`, you can override it with `.components-notice__dismiss.components-icon-button:not(:disabled):_not([aria-disabled="true"]):not(.is-default):hover`.

But would also not call that a blocker.

#### mmtr on 2018-08-16T07:59:03Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210507151
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

Yep. My only concern is that this is less maintainable. If we change the style rules for `IconButton` component, we might need to also change them in the `Notice` component.

Anyway, I have overridden them with:
```css
&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover,
&:not(:disabled):not([aria-disabled="true"]):not(.is-default):active,
&:not(:disabled):not([aria-disabled="true"]):focus {
	color: $alert-red;
	background-color: transparent;
}
&:not(:disabled):not([aria-disabled="true"]):not(.is-default):hover {
	box-shadow: none;
}
```

#### jasmussen on 2018-08-16T08:10:18Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210509903
Path: packages/components/src/notice/index.js

```diff
@@ -7,17 +7,21 @@ import classnames from 'classnames';
 /**
  * WordPress dependencies
  */
+import Dashicon from '../dashicon';
 import { __ } from '@wordpress/i18n';
 
 function Notice( { className, status, children, onRemove = noop, isDismissible = true } ) {
-	const classNames = classnames( className, 'notice notice-alt notice-' + status, {
+	const classNames = classnames( className, 'components-notice', {
+		[ `is-${ status }` ]: ! ! status,
+	}, {
 		'is-dismissible': isDismissible,
 	} );
 	return (
 		<div className={ classNames }>
-			{ isString( children ) ? <p>{ children }</p> : children }
+			{ isString( children ) ? <div className="components-notice__content">{ children }</div> : children }
 			{ isDismissible && (
-				<button className="notice-dismiss" type="button" onClick={ onRemove }>
+				<button className="components-notice__dismiss" type="button" onClick={ onRemove }>
```

Cool — do you really need to override the color though? $alert-red as a hover/active color doesn't seem necessary, and it probably will run into contrast issues. Probably just keep the `color` as is?

### Review thread 5

#### youknowriad on 2018-08-16T09:06:33Z

URL: https://github.com/WordPress/gutenberg/pull/8856#discussion_r210525772
Path: edit-post/components/layout/style.scss

```diff
@@ -17,12 +17,12 @@
 			top: inherit;
 		}
 
-		.notice {
+		.components-notice {
 			margin: 0 0 5px;
 			padding: 6px 12px;
 			min-height: $panel-header-height;
 
-			.notice-dismiss {
+			.components-notice__dismiss {
 				margin: 5px;
```

Can we tweak this margin to make it appear centered by default if the message is in a single line ?

```css
margin: 10px 5px;
```

## Changed files

- edit-post/components/layout/style.scss (MODIFIED, +3/-3)
- packages/components/src/notice/README.md (MODIFIED, +1/-1)
- packages/components/src/notice/index.js (MODIFIED, +16/-5)
- packages/components/src/notice/style.scss (MODIFIED, +50/-0)
- packages/components/src/notice/test/__snapshots__/index.js.snap (MODIFIED, +7/-11)
- packages/editor/src/components/block-list/style.scss (MODIFIED, +2/-2)
- packages/editor/src/components/contrast-checker/style.scss (MODIFIED, +1/-1)
- packages/editor/src/components/contrast-checker/test/__snapshots__/index.js.snap (MODIFIED, +30/-18)
- test/e2e/specs/change-detection.test.js (MODIFIED, +3/-1)
- test/e2e/specs/preview.test.js (MODIFIED, +1/-1)
- test/e2e/specs/reusable-blocks.test.js (MODIFIED, +2/-2)
- test/e2e/support/utils.js (MODIFIED, +1/-1)
