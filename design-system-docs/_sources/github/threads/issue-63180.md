---
number: 63180
type: issue
state: OPEN
url: https://github.com/WordPress/gutenberg/issues/63180
matched_labels: ["[Package] Components"]
all_labels: ["[Status] In Progress","[Package] Components"]
---

# Issue #63180: CustomSelectControl V2 popover renders below position: sticky elements

- URL: https://github.com/WordPress/gutenberg/issues/63180
- Author: ciampo
- Created: 2024-07-05T10:26:34Z
- Updated: 2026-05-06T08:40:43Z
- Comments: 2 of 2

## Body

As flagged in https://github.com/WordPress/gutenberg/pull/62821 (see PR description):

The V2 version of the component automatically "flips" the popover to render on top of the trigger if there isn't enough space below it. In the specific example of the block toolbar, this causes a small visual glitch when the browser's viewport is short enough to cause the flip:

![Screenshot 2024-06-25 at 12 13 32](https://github.com/WordPress/gutenberg/assets/1083581/87dfe892-1bc6-4cf2-b1c0-e28bc3952923)

This glitch happens because the block toolbar's header has `position: sticky`, which causes it to render on top of the select popover despite a lower `z-index`.


A few preliminary ideas that try to avoid the problem rather than solving it:

- disallow the popover to flip
- allow for lower minimum height

I'm not sure we can solve this systematically at the CSS level. At most we could look into where in the DOM the popover is rendered, and consider portalling it so that it plays better with the stacking context?

## Issue comments

### ciampo on 2024-07-10T14:19:42Z

URL: https://github.com/WordPress/gutenberg/issues/63180#issuecomment-2220645256

While https://github.com/WordPress/gutenberg/pull/63357 should avoid the issue for the V2 legacy adapter (soon to be the new V1) component, we will still need to understand how to best solve this for the V2:

- keep `flip` disabled (although we could encounter the same issue in future layouts / etc)
- somewhat set as "boundaries" for the popover the correct height of the panel (excluding the header)
- solve it via allowing the popover to be `portal`ed to a plan in the DOM where the stacking context is not a constraints
- ?

### ciampo on 2026-05-06T08:40:43Z

URL: https://github.com/WordPress/gutenberg/issues/63180#issuecomment-4386368843

Attempted a solution in #77969 , although I decided to close the PR with [the following reasoning](https://github.com/WordPress/gutenberg/pull/77969#issuecomment-4386353767):

> Update:
> 
> Local tests show that the approach tested in this PR would fix both #76126 and #63180.
> 
> However, we're in the middle of developing a new package of components (`@wordpress/ui`) that will include the same portalling features from the start. In fact, [the PR adding the new `SelectControl` component](https://github.com/WordPress/gutenberg/pull/77809) is currently under review.
> 
> After `SelectControl` is added to the `@wordpress/ui`, we will need to partially reworok how overlays are displayed and stacked in Gutenberg, moving from a list of discretionary portal slots and hardcoded z-indexes to a more implicit, modern architecture that allows the coexistence of legacy `@wordpress/components` and new `@wordpress/ui` components.
> 
> With that in mind, I'll close this PR, since it would delay the plan described above.
> 
> We can always reconsider the decision and merge this PR in case. (cc @WordPress/gutenberg-components )


In other words: we know the general approach that will fix this issue, but we're not ready to apply it in the most optimal shape just yet.

