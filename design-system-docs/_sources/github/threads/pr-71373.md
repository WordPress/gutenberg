---
number: 71373
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/71373
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Package] Components"]
---

# PR #71373: Components: Fix Autocomplete retriggering when triggerPrefix is included in output

- URL: https://github.com/WordPress/gutenberg/pull/71373
- Author: shrivastavanolo
- Created: 2025-08-27T12:14:10Z
- Updated: 2026-04-20T09:10:17Z
- Comments: 2 of 2
- Review threads: 1 of 1
- Changed files: 1 of 1
- Diff: ../pr-diffs/71373.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Closes #42925
Fixes an issue where the Autocomplete component retriggers immediately after selecting an option if the triggerPrefix (`@`) remains in the output.

## Why?
When a user selects a mention (e.g. `@john`), the autocompleter reopens right away. This creates a poor UX by repeatedly showing the same suggestions after selection.

## How?
- Appended a non-breaking space (`\u00A0`) after inserting a user mention so it no longer matches the completer regex.
- Scoped the change only to the user mention completer so other completers (e.g. slash inserter) remain unaffected.

## Testing Instructions
1. Create two users: `john` and `johndoe`.
2. Create a new post and type `@` to launch the user mention completer.
3. Select `john` by clicking → The autocompleter should **not** reopen.
4. Repeat with `johndoe` → The autocompleter should **not** reopen.
5. Try selecting a user via keyboard (arrows + Enter), then press space and keep typing → The autocompleter should **not** reappear.

### Testing Instructions for Keyboard
- Navigate to a block where you can type.
- Type `@joh`, use arrow keys to highlight `john`, and press Enter.
- Continue typing with the keyboard → The autocompleter should not reopen.
- Verify focus remains in the editor as expected.

## Screenshots or screencast

https://github.com/user-attachments/assets/d619a84d-fa69-409c-b063-3846823d8175

## Issue comments

### github-actions on 2025-08-27T12:14:21Z

URL: https://github.com/WordPress/gutenberg/pull/71373#issuecomment-3227955989

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: shrivastavanolo <shreya0shrivastava@git.wordpress.org>
Co-authored-by: chad1008 <shireling@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### Mamaduka on 2026-04-20T09:10:17Z

URL: https://github.com/WordPress/gutenberg/pull/71373#issuecomment-4279292862

Closing in favor of #77185.

## Review threads

### Review thread 1

#### Mamaduka on 2025-08-27T12:40:56Z

URL: https://github.com/WordPress/gutenberg/pull/71373#discussion_r2303813585
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -110,7 +110,10 @@ export function useAutocomplete( {
 				? completion
 				: ( {
 						action: 'insert-at-caret',
-						value: completion,
+						value:
+							autocompleter?.triggerPrefix === '@'
+								? completion + '\u00A0'
+								: completion,
```

What happens when `triggerPrefix` is something other than `@`? There is more than one autocompleter, and users can also have custom ones.

Inserting a non-breaking space can be unexpected here.

#### shrivastavanolo on 2025-08-27T12:57:29Z

URL: https://github.com/WordPress/gutenberg/pull/71373#discussion_r2303855547
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -110,7 +110,10 @@ export function useAutocomplete( {
 				? completion
 				: ( {
 						action: 'insert-at-caret',
-						value: completion,
+						value:
+							autocompleter?.triggerPrefix === '@'
+								? completion + '\u00A0'
+								: completion,
```

@Mamaduka Thanks for pointing this out! The current change already checks for `autocompleter?.triggerPrefix === '@'` , so the non-breaking space is only applied to the mention completer and doesn’t affect others. Do you think that’s sufficient, or would you suggest a different approach?

#### Mamaduka on 2025-08-27T13:00:11Z

URL: https://github.com/WordPress/gutenberg/pull/71373#discussion_r2303861956
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -110,7 +110,10 @@ export function useAutocomplete( {
 				? completion
 				: ( {
 						action: 'insert-at-caret',
-						value: completion,
+						value:
+							autocompleter?.triggerPrefix === '@'
+								? completion + '\u00A0'
+								: completion,
```

What I mean is that the same problem probably affects other autocompleters, with a different trigger prefix. I don't think fixing it for a single case is the right solution.

@t-hamano, I think you've worked in this area some time ago, what do you think?

#### t-hamano on 2025-08-28T02:49:05Z

URL: https://github.com/WordPress/gutenberg/pull/71373#discussion_r2306002340
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -110,7 +110,10 @@ export function useAutocomplete( {
 				? completion
 				: ( {
 						action: 'insert-at-caret',
-						value: completion,
+						value:
+							autocompleter?.triggerPrefix === '@'
+								? completion + '\u00A0'
+								: completion,
```

Thanks for the ping!

My understanding is that this issue occurs when the string returned by the `getOptionCompletion` function contains a string defined by the trigger prefix. Because the autocompleter can be extended by consumers, hard-coding the trigger prefix isn't a good approach.

Please test this plugin, which implements a custom autocompleter. This completer uses the trigger prefix `~`.

[fruit-autocompleter.zip](https://github.com/user-attachments/files/22016590/fruit-autocompleter.zip)

https://github.com/user-attachments/assets/e4e54ce5-1d05-4dd6-ba49-af9911fb6283

I haven't found a good approach yet, but I think we need an approach that doesn't rely on a specific trigger prefix.

#### shrivastavanolo on 2025-08-28T05:11:30Z

URL: https://github.com/WordPress/gutenberg/pull/71373#discussion_r2306188551
Path: packages/components/src/autocomplete/index.tsx

```diff
@@ -110,7 +110,10 @@ export function useAutocomplete( {
 				? completion
 				: ( {
 						action: 'insert-at-caret',
-						value: completion,
+						value:
+							autocompleter?.triggerPrefix === '@'
+								? completion + '\u00A0'
+								: completion,
```

Thanks for clarifying and for the test plugin! I see the issue now, it’s tied to the trigger prefix being included in the completion, not just @. I’ll test with the custom ~ autocompleter.

## Changed files

- packages/components/src/autocomplete/index.tsx (MODIFIED, +4/-1)
