---
number: 15472
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/15472
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Feature] Drag and Drop","[Package] Components","[Feature] Widgets Screen"]
---

# PR #15472: Fix: Draggable component depends on an element with id editor being available.

- URL: https://github.com/WordPress/gutenberg/pull/15472
- Author: jorgefilipecosta
- Created: 2019-05-06T17:31:57Z
- Updated: 2019-05-17T12:58:07Z
- Comments: 1 of 1
- Review threads: 1 of 1
- Changed files: 1 of 1
- Diff: ../pr-diffs/15472.diff

## Body

If the Draggable component was used in a document without an element with "editor" id, the component crashed when the user tried to use it.

The components should be as generic as possible and not relying on specific elements being available.


## How has this been tested?
I cherry-picked the commit from https://github.com/WordPress/gutenberg/pull/15470 that makes sure the movers can be used (only required if testing before that PR is merged).
I went to /wp-admin/admin.php?page=gutenberg-widgets.
I added some blocks I tried to drag them and verified the draggable element did not crash because of an element with id editor not being found. (dropping blocks still does not work because of another unrelated problem).

## Issue comments

### jorgefilipecosta on 2019-05-17T12:58:02Z

URL: https://github.com/WordPress/gutenberg/pull/15472#issuecomment-493443966

Closing this PR, with the changes being made in https://github.com/WordPress/gutenberg/pull/15665 this PR is irrelevant.

## Review threads

### Review thread 1

#### youknowriad on 2019-05-07T08:58:27Z

URL: https://github.com/WordPress/gutenberg/pull/15472#discussion_r281535046
Path: packages/components/src/draggable/index.js

```diff
@@ -15,7 +18,9 @@ const cloneHeightTransformationBreakpoint = 700;
 const clonePadding = 20;
 
 const isChromeUA = ( ) => /Chrome/i.test( window.navigator.userAgent );
-const documentHasIframes = ( ) => [ ...document.getElementById( 'editor' ).querySelectorAll( 'iframe' ) ].length > 0;
+const documentHasIframes = () => {
+	return !! invoke( document, [ 'body', 'querySelector' ], 'iframe' );
+};
```

@nosolosw Any reason this check was editor specific? Is it because WP can use iframe outside of the "content" canvas (maybe in menus, topbars)? What impact could this have?

#### oandregal on 2019-05-07T15:02:25Z

URL: https://github.com/WordPress/gutenberg/pull/15472#discussion_r281676288
Path: packages/components/src/draggable/index.js

```diff
@@ -15,7 +18,9 @@ const cloneHeightTransformationBreakpoint = 700;
 const clonePadding = 20;
 
 const isChromeUA = ( ) => /Chrome/i.test( window.navigator.userAgent );
-const documentHasIframes = ( ) => [ ...document.getElementById( 'editor' ).querySelectorAll( 'iframe' ) ].length > 0;
+const documentHasIframes = () => {
+	return !! invoke( document, [ 'body', 'querySelector' ], 'iframe' );
+};
```

Exactly. My old self did document this as problematic: https://github.com/WordPress/gutenberg/pull/9511#discussion_r214390996

If we do this, what will happen in chrome browsers that don't have the fix yet ([<72 if I'm reading correctly the upstream bugfix](https://bugs.chromium.org/p/chromium/issues/detail?id=737691#c8)?) is that it'll fire the `onDrop` and then `onDragEnd`, effectively executing the `onDragEnd` property twice.

An alternative fix would be to keep track of whether the drag operation was already reseted keeping a `isDragging` variable similar to `isChromeAndHasIframes` and reset the drag state & call the `onDragEnd` prop only if it still wasn't done.

#### jorgefilipecosta on 2019-05-15T12:39:20Z

URL: https://github.com/WordPress/gutenberg/pull/15472#discussion_r284233111
Path: packages/components/src/draggable/index.js

```diff
@@ -15,7 +18,9 @@ const cloneHeightTransformationBreakpoint = 700;
 const clonePadding = 20;
 
 const isChromeUA = ( ) => /Chrome/i.test( window.navigator.userAgent );
-const documentHasIframes = ( ) => [ ...document.getElementById( 'editor' ).querySelectorAll( 'iframe' ) ].length > 0;
+const documentHasIframes = () => {
+	return !! invoke( document, [ 'body', 'querySelector' ], 'iframe' );
+};
```

@nosolosw, @youknowriad  Can we just remove this logic and assume the problem is fixed in chrome? I guess the last two versions that we need to support already have this problem fixed given that the problem was fixed in Wed, Nov 21, 2018, and chrome updates are fairly regular.

#### youknowriad on 2019-05-15T12:43:16Z

URL: https://github.com/WordPress/gutenberg/pull/15472#discussion_r284234722
Path: packages/components/src/draggable/index.js

```diff
@@ -15,7 +18,9 @@ const cloneHeightTransformationBreakpoint = 700;
 const clonePadding = 20;
 
 const isChromeUA = ( ) => /Chrome/i.test( window.navigator.userAgent );
-const documentHasIframes = ( ) => [ ...document.getElementById( 'editor' ).querySelectorAll( 'iframe' ) ].length > 0;
+const documentHasIframes = () => {
+	return !! invoke( document, [ 'body', 'querySelector' ], 'iframe' );
+};
```

@jorgefilipecosta That makes sense to me

#### oandregal on 2019-05-15T15:38:48Z

URL: https://github.com/WordPress/gutenberg/pull/15472#discussion_r284321489
Path: packages/components/src/draggable/index.js

```diff
@@ -15,7 +18,9 @@ const cloneHeightTransformationBreakpoint = 700;
 const clonePadding = 20;
 
 const isChromeUA = ( ) => /Chrome/i.test( window.navigator.userAgent );
-const documentHasIframes = ( ) => [ ...document.getElementById( 'editor' ).querySelectorAll( 'iframe' ) ].length > 0;
+const documentHasIframes = () => {
+	return !! invoke( document, [ 'body', 'querySelector' ], 'iframe' );
+};
```

Agreed to that: https://github.com/WordPress/gutenberg/pull/15665 (wanted to test this effectively had been fixed so I had to prepare the revert anyway).

## Changed files

- packages/components/src/draggable/index.js (MODIFIED, +7/-2)
