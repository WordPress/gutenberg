---
number: 63175
type: issue
state: OPEN
url: https://github.com/WordPress/gutenberg/issues/63175
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Focus] Accessibility (a11y)","[Status] In Progress","[Package] Components","[Package] Icons","[Package] Edit Site"]
---

# Issue #63175: Site Hub and ExternalLink: replace the North East Arrow unicode character with equivalent SVG icon

- URL: https://github.com/WordPress/gutenberg/issues/63175
- Author: afercia
- Created: 2024-07-05T09:40:15Z
- Updated: 2026-06-19T18:25:18Z
- Comments: 3 of 3

## Body

### Description

See https://core.trac.wordpress.org/ticket/40428

In https://github.com/WordPress/gutenberg/pull/60255 the ExternalLink component was changed to use the North East Arrow unicode character `↗` rather than the `external` SVG icon.

In https://github.com/WordPress/gutenberg/pull/61258, the site editor 'hub' was changed to use the North East Arrow unicode character but only display it on hover and focus via an `::after` CSS pseudo element.

Both unicode characters and pseudo generated content are treated as _text_ and screen readers do read out text. 

In the ExternalLink component the only reason why screen readers do not announce `North East Arrow` is because the unicode character is wrapped within a span element with an aria-label `opens in a new tab)`:

https://github.com/WordPress/gutenberg/blob/2c5f7689e83691f7cdba8e35a114ab9829ef76da/packages/components/src/external-link/index.tsx#L69-L77

Instead, in the site hub after https://github.com/WordPress/gutenberg/pull/62648 is merged, the unicode character in the pseudo generated element will be announced. Screen readers will read out:

`link, {site title} opens in a new tab North East Arrow`

This could be fixed in the site hub implementation but I'd rather suggest a more general approach, also for educational purposes. The most important thing to take into consideration for accessibility is as follows:

> [!IMPORTANT]
> Content of CSS pseudo elements and Unicode characters are _text_ and are announced by screen readers.

To avoid future similar cases I'd like to propose to establish a guide line and never use unicode characters or pseudo generated content this way.

Also, unicode characters may be rendered in a slighly different way on different operating systems.

The ExternalLink component and the Site Hub custom implementation should use an SVG icon instead. Icons in the `@package/icons` are hidden from assistive technologies by default.











### Step-by-step reproduction instructions

- Checkout to https://github.com/WordPress/gutenberg/pull/62648 it it hasn't be merged yet.
- Use Safari and VoiceOver.
- Go to the site editor and tab to the site title in the navigation panel.
- Observe the unicode character in the site title is announced. VoiceOver will read out: `link, {site title} opens in a new tab North East Arrow`

### Screenshots, screen recording, code snippet

Note: the VoiceOver caption panel shows the character `↗` but it actually announces it as 'North East Arrow`:

![Screenshot 2024-07-05 at 10 10 25](https://github.com/WordPress/gutenberg/assets/1682452/873217dc-1cf7-42f7-b2ce-b39a4a2b3582)


### Environment info

_No response_

### Please confirm that you have searched existing issues in the repo.

Yes

### Please confirm that you have tested with all plugins deactivated except Gutenberg.

Yes

## Issue comments

### richtabor on 2024-07-07T01:54:06Z

URL: https://github.com/WordPress/gutenberg/issues/63175#issuecomment-2212243296

I'm cool if we do the unicode symbol as an icon, resulting in no change visually.

### sabernhardt on 2025-07-30T08:25:17Z

URL: https://github.com/WordPress/gutenberg/issues/63175#issuecomment-3135323086

The PR is closed right now, but the arrow character needs to be silenced or replaced in the Site Hub title.

A quick fix option could involve adding empty [alternative text for CSS `content`](https://caniuse.com/mdn-css_properties_content_alt_text):
`content: "↗" / "";`

Other arrows in the editor use a `<span>` with an `aria-label`. However, when I pasted `<span class="components-external-link__icon" aria-label="(opens in a new tab)">↗</span>` from the post slug control, the emoji script replaced the character with a very different image.
<img width="300" height="60" alt="emoji replacement for arrow character" src="https://github.com/user-attachments/assets/0404b792-bd6b-4fdf-94da-a46a4715817a" />

If SVG is preferred over Unicode options, I do not like creating a custom SVG for one place. Maybe the `@wordpress/icons` package could add 'arrowUpRight' and 'arrowDownLeft' to complement the other six directions ([which it already has](https://wordpress.github.io/gutenberg/?path=/story/icons-icon--library)).

On another note, the arrow has meaning: to indicate a link that opens in a new tab. It probably should not be hidden with `opacity: 0` (or need the `transition` and `opacity: 1` for hover/focus).

### Mayank-Tripathi32 on 2026-06-19T18:24:36Z

URL: https://github.com/WordPress/gutenberg/issues/63175#issuecomment-4753883266

Hiyo,

I looked into the discussion and was reaching for the new Link in @wordpress/ui, since it handles the arrow with role="img". Turns out it's not a drop-in though: link-control hides the arrow in link previews and the boot Site Hub only shows it on hover, both targeting .components-external-link__icon. Link's icon class is scoped and there's no prop to hide/show it, so a swap would make the arrow show up in both.

The Site Hub itself is easy, that arrow is a ::after { content } with nothing hiding it from screen readers, so content: "↗" / "" handles it (and we could drop the opacity: 0 hover bit too).

Is it worth going the extra effort to work on link component first or should we open a PR for the Site Hub fix on its own? 

Ps: Please correct me if I misunderstood anything or If link component shouldn't be used here. Thanks

