---
number: 53132
type: pull_request
state: MERGED
merged_at: 2023-08-10T02:35:16Z
url: https://github.com/WordPress/gutenberg/pull/53132
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #53132: `MenuItem`: Refactor to TypeScript

- URL: https://github.com/WordPress/gutenberg/pull/53132
- Author: bangank36
- Created: 2023-07-29T03:37:51Z
- Updated: 2023-09-05T20:28:09Z
- Comments: 11 of 11
- Review threads: 7 of 7
- Changed files: 5 of 5
- Diff: ../pr-diffs/53132.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Refactor [MenuItem component](https://github.com/WordPress/gutenberg/tree/6e4bc840699ed2742dba27e585b38ee136f882be/packages/components/src/menu-item) to TypeScript

## Why?
Part of https://github.com/WordPress/gutenberg/issues/35744, as suggested in https://github.com/WordPress/gutenberg/issues/36128#issuecomment-1650618499

## How?
- Followed general suggestions from the [TypeScript Migration guide](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/CONTRIBUTING.md#refactoring-a-component-to-typescript).
- Renamed file to .tsx and created a types.ts file with relevant types
- Fix conflicting of MenuItem reference on [menu-items-choice](https://github.com/WordPress/gutenberg/blob/6e4bc840699ed2742dba27e585b38ee136f882be/packages/components/src/menu-items-choice/index.tsx#L61) and [tools-panel-header](https://github.com/WordPress/gutenberg/blob/6e4bc840699ed2742dba27e585b38ee136f882be/packages/components/src/tools-panel/tools-panel-header/component.tsx#L114)

## Testing Instructions
- Ensure no type error
- Component should work as expected

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

## Issue comments

### bangank36 on 2023-08-10T15:46:09Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1673480271

> I really enjoyed collaborating with you on these. Looking forward to see you around more 🙌

Thank you for taking the time to review these commits, I actually created some other PRs while waiting for the updates
https://github.com/WordPress/gutenberg/pull/53379
https://github.com/WordPress/gutenberg/pull/53172

Next step would be provide the missing stories for the MenuItem component, FYI I updated the list of [missing stories](https://github.com/WordPress/gutenberg/issues/36128#issuecomment-1648975642)? Though I think it would be wiser to wait for the update on your notes https://github.com/WordPress/gutenberg/issues/36128#issuecomment-1672462735

### noahtallen on 2023-08-31T18:44:15Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1701578136

Hi @bangank36! We use `MenuItem` in a couple of places, and I noticed it no longer accepts a couple of Button props: 

- `variant`
- `isPressed`

Are these supposed to be accepted?

### mirka on 2023-08-31T20:21:00Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1701727831

@noahtallen We basically omitted a bunch of Button props we thought were inappropriate in a MenuItem context. Can you point to some use cases for `variant`/`isPressed` that you think are legitimate? We're open to reconsider.

### noahtallen on 2023-08-31T20:33:13Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1701742589

Good question! This is older code, so I'm not sure exactly how much these props are needed, but here is an example:

https://github.com/Automattic/wp-calypso/blob/5956bfa5df72839c5e29cae607f5c0b152d77147/packages/design-picker/src/components/design-picker-category-filter/index.tsx#L69-L86

Here is where it is rendered:

<img width="1016" alt="image" src="https://github.com/WordPress/gutenberg/assets/6265975/2b408bc6-2f4b-441e-8d90-178aed2beaea">

### noahtallen on 2023-08-31T20:36:20Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1701746825

Though it does look like the design is changing slightly after the update:

<img width="995" alt="image" src="https://github.com/WordPress/gutenberg/assets/6265975/99062b6f-0a8e-4083-8b83-0f8016288e5a">

### mirka on 2023-09-01T11:37:46Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1702610607

Yeah, given that the [styles](https://github.com/Automattic/wp-calypso/blob/b7a1da863452490af162a0f8df2362dda72b14fa/packages/design-picker/src/components/design-picker-category-filter/style.scss#L14) are also heavily overridden in that case, it's not a prop that MenuItem will want to officially allow, at least in its current form.

If there's a need for a specific state/style variant, we can add that to MenuItem in a way that's appropriately styled and named. Otherwise, I think for now you should `ts-expect-error` and `TODO` on the Calypso side. (We actually did that in [one place](https://github.com/WordPress/gutenberg/pull/53613/files#diff-9ce2148da3b0c49549523f34dbc5d5c7953b4da67992d9c7b7b30b0e9daf4c7d), too.)

### bangank36 on 2023-09-01T13:19:44Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1702738996

> given that the [styles](https://github.com/Automattic/wp-calypso/blob/b7a1da863452490af162a0f8df2362dda72b14fa/packages/design-picker/src/components/design-picker-category-filter/style.scss#L14) are also heavily overridden in that case

So do you think MenuItem `isSelected` is more appropriate in this case @mirka, I also noticed that there is no classname for MenuItem `selected` state yet

### mirka on 2023-09-01T13:45:44Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1702774993

> So do you think MenuItem `isSelected` is more appropriate in this case

We'd have to look into it, but off the top of my head, no. Because I don't think the ARIA [Menu pattern](https://www.w3.org/WAI/ARIA/apg/patterns/menubar/) has a semantic "selected" state. You'd either use a `menuitemradio` or `menuitemcheckbox` and set the state on that.

(Also FYI a [DropdownMenu v2](https://wordpress.github.io/gutenberg/?path=/docs/components-experimental-dropdownmenu-v2--docs) is in the works)

### noahtallen on 2023-09-01T20:58:47Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1703313127

> Otherwise, I think for now you should ts-expect-error and TODO on the Calypso side

Got it -- I did add a couple of one-off wrapper types inline, but I can remove those. Should I just delete the variant/isPressed props if they don't actually do anything?

### mirka on 2023-09-04T12:21:15Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1705174625

> Got it -- I did add a couple of one-off wrapper types inline, but I can remove those. Should I just delete the variant/isPressed props if they don't actually do anything?

They _will_ do something, because those props are still passed through to the underlying Button. It's just that we "discouraged" it at the TypeScript level.

### noahtallen on 2023-09-05T20:27:49Z

URL: https://github.com/WordPress/gutenberg/pull/53132#issuecomment-1707266528

Good to know! Thanks for the clarification.

## Review threads

### Review thread 1 (resolved)

#### mirka on 2023-08-05T00:49:28Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284929580
Path: packages/components/src/tools-panel/tools-panel-header/component.tsx

```diff
@@ -213,7 +213,6 @@ const ToolsPanelHeader = (
 							<MenuGroup>
 								<MenuItem
 									aria-disabled={ ! canResetAll }
-									variant={ 'tertiary' }
```

We can't remove this because it will alter the styling.

<img width="299" alt="The 'reset all' option in the ToolsPanel dropdown menu" src="https://github.com/WordPress/gutenberg/assets/555336/858f7681-0c32-4a17-8dcf-4d5925c01e88">

#### bangank36 on 2023-08-06T03:32:19Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1285147340
Path: packages/components/src/tools-panel/tools-panel-header/component.tsx

```diff
@@ -213,7 +213,6 @@ const ToolsPanelHeader = (
 							<MenuGroup>
 								<MenuItem
 									aria-disabled={ ! canResetAll }
-									variant={ 'tertiary' }
```

Restored
https://github.com/WordPress/gutenberg/blob/87b0620d0723796d1399d093df81d4683ad03410/packages/components/src/tools-panel/tools-panel-header/component.tsx#L216

### Review thread 2 (resolved)

#### mirka on 2023-08-05T00:54:32Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284930454
Path: packages/components/src/menu-item/index.tsx

```diff
@@ -15,8 +15,12 @@ import { cloneElement, forwardRef } from '@wordpress/element';
 import Shortcut from '../shortcut';
 import Button from '../button';
 import Icon from '../icon';
+import type { MenuItemProps } from './types';
 
-export function MenuItem( props, ref ) {
+export function MenuItem(
+	props: MenuItemProps,
```

```suggestion
	props: WordPressComponentProps< MenuItemProps, 'button', false >,
```

Since we're passing down all the rest props to the underlying `button`, let's make sure we allow all the standard `button` props as well, like `onClick`. The `WordPressComponentProps` is a utility type to mix in those standard `button` props.

#### bangank36 on 2023-08-06T03:33:46Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1285147419
Path: packages/components/src/menu-item/index.tsx

```diff
@@ -15,8 +15,12 @@ import { cloneElement, forwardRef } from '@wordpress/element';
 import Shortcut from '../shortcut';
 import Button from '../button';
 import Icon from '../icon';
+import type { MenuItemProps } from './types';
 
-export function MenuItem( props, ref ) {
+export function MenuItem(
+	props: MenuItemProps,
```

Thanks for pointing this out 👍 , I should have missed those tips from the [Contributing guidance](https://github.com/WordPress/gutenberg/blob/25215a997e23b8b4b05b122ccb1c2778dcd4cfa7/packages/components/CONTRIBUTING.md#refactoring-a-component-to-typescript)

> Extend existing components’ props if possible, especially when a component internally forwards its props to another component in the package.
> If the component forwards its ...restProps to an underlying element/component, you should use the WordPressComponentProps type for the component's props:

#### mirka on 2023-08-10T02:33:08Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1289469060
Path: packages/components/src/menu-item/index.tsx

```diff
@@ -15,8 +15,12 @@ import { cloneElement, forwardRef } from '@wordpress/element';
 import Shortcut from '../shortcut';
 import Button from '../button';
 import Icon from '../icon';
+import type { MenuItemProps } from './types';
 
-export function MenuItem( props, ref ) {
+export function MenuItem(
+	props: MenuItemProps,
```

No worries, there's a lot of information in there 😆

### Review thread 3 (resolved)

#### mirka on 2023-08-05T00:59:13Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284931080
Path: packages/components/src/menu-item/index.tsx

Above the default export at the bottom of the file, let's add a JSDoc so a basic readme shows up in IntelliSense:

```js
/**
 * MenuItem is a component which renders a button intended to be used in combination with the `DropdownMenu` component.
 *
 * ```jsx
 * import { MenuItem } from '@wordpress/components';
 * import { useState } from '@wordpress/element';
 *
 * const MyMenuItem = () => {
 * 	const [ isActive, setIsActive ] = useState( true );
 *
 * 	return (
 * 		<MenuItem
 * 			icon={ isActive ? 'yes' : 'no' }
 * 			isSelected={ isActive }
 * 			onClick={ () => setIsActive( ( state ) => ! state ) }
 * 		>
 * 			Toggle
 * 		</MenuItem>
 * 	);
 * };
 * ```
 */
export default forwardRef( MenuItem );
```

#### bangank36 on 2023-08-06T03:34:01Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1285147434
Path: packages/components/src/menu-item/index.tsx

Sorted
<img width="1800" alt="image" src="https://github.com/WordPress/gutenberg/assets/10071857/2117fd3e-09d9-43de-ac96-7dabebc521e1">

### Review thread 4 (resolved)

#### mirka on 2023-08-05T01:00:07Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284931167
Path: packages/components/src/menu-item/types.d.ts

We want this file to be a normal TS file (`types.ts`), not a `.d.ts`.

### Review thread 5 (resolved)

#### mirka on 2023-08-05T01:04:06Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284931984
Path: packages/components/src/menu-item/types.d.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+/**
+ * Internal dependencies
+ */
+import type { IconType } from '../icon';
+
+export type MenuItemProps = {
```

```suggestion
import type { ButtonAsButtonProps } from '../button/types';

export type MenuItemProps = ButtonAsButtonProps & {
```

Since all the rest props are being passed down to the underlying `Button` component, let's add that into the allowed types. I personally think a lot of those `Button` props shouldn't be allowed in this context, but the current implementation allows it, so I guess we can keep it this way for now 🤷 This will also fix the `variant` problem in `ToolsPanel`.

#### bangank36 on 2023-08-06T03:39:42Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1285147709
Path: packages/components/src/menu-item/types.d.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+/**
+ * Internal dependencies
+ */
+import type { IconType } from '../icon';
+
+export type MenuItemProps = {
```

`variant` issue solved!

### Review thread 6 (resolved)

#### mirka on 2023-08-05T01:05:17Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284932129
Path: packages/components/src/menu-item/types.d.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+/**
+ * Internal dependencies
+ */
+import type { IconType } from '../icon';
+
+export type MenuItemProps = {
+	/**
+	 * A CSS `class` to give to the container element.
+	 */
+	className?: string;
+	/**
+	 * The children elements.
+	 */
+	children?: ReactNode;
+	/**
+	 * Text to use as description for button text.
+	 */
+	info?: string;
+	/**
+	 * The icon to render. Supported values are: Dashicons (specified as
+	 * strings), functions, Component instances and `null`.
+	 *
+	 * @default null
+	 */
+	icon?: JSX.Element | null;
+	/**
+	 * Determines where to display the provided `icon`.
+	 */
+	iconPosition?: string;
```

```suggestion
	iconPosition?: ButtonAsButtonProps[ 'iconPosition' ];
```

For this one specifically, I think it would be better to use the types from `Button` because it's stricter and more helpful.

#### bangank36 on 2023-08-06T03:50:42Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1285148405
Path: packages/components/src/menu-item/types.d.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+/**
+ * Internal dependencies
+ */
+import type { IconType } from '../icon';
+
+export type MenuItemProps = {
+	/**
+	 * A CSS `class` to give to the container element.
+	 */
+	className?: string;
+	/**
+	 * The children elements.
+	 */
+	children?: ReactNode;
+	/**
+	 * Text to use as description for button text.
+	 */
+	info?: string;
+	/**
+	 * The icon to render. Supported values are: Dashicons (specified as
+	 * strings), functions, Component instances and `null`.
+	 *
+	 * @default null
+	 */
+	icon?: JSX.Element | null;
+	/**
+	 * Determines where to display the provided `icon`.
+	 */
+	iconPosition?: string;
```

Sorted

### Review thread 7 (resolved)

#### mirka on 2023-08-05T01:07:05Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1284932383
Path: packages/components/src/menu-item/types.d.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+/**
+ * Internal dependencies
+ */
+import type { IconType } from '../icon';
+
+export type MenuItemProps = {
+	/**
+	 * A CSS `class` to give to the container element.
+	 */
+	className?: string;
+	/**
+	 * The children elements.
+	 */
+	children?: ReactNode;
+	/**
+	 * Text to use as description for button text.
+	 */
+	info?: string;
+	/**
+	 * The icon to render. Supported values are: Dashicons (specified as
+	 * strings), functions, Component instances and `null`.
+	 *
+	 * @default null
+	 */
+	icon?: JSX.Element | null;
+	/**
+	 * Determines where to display the provided `icon`.
+	 */
+	iconPosition?: string;
+	/**
+	 * Whether or not the menu item is currently selected.
+	 */
+	isSelected?: boolean;
+	/**
+	 * If shortcut is a string, it is expecting the display text. If shortcut is an object,
+	 * it will accept the properties of `display` (string) and `ariaLabel` (string).
+	 */
+	shortcut?: string | { display: string; ariaLabel: string };
+	/**
+	 * If you need to have selectable menu items use menuitemradio for single select,
+	 * and menuitemcheckbox for multiselect.
+	 *
+	 * @default 'menuitem'
+	 */
+	role?: string;
+	/**
+	 * Allows for markup other than icons or shortcuts to be added to the menu item.
+	 *
+	 */
+	suffix?: ReactNode;
+	/**
+	 * Human-readable label for item.
+	 */
+	label?: string;
+	/**
+	 * On click handler.
+	 */
+	onClick?( event: React.MouseEvent< HTMLButtonElement, MouseEvent > ): void;
+	/**
+	 * On mouse enter handler.
+	 * @default noop
+	 */
+	onMouseEnter?(
+		event: React.MouseEvent< HTMLButtonElement, MouseEvent >
+	): void;
+	/**
+	 * On mouse leave handler.
+	 * @default noop
+	 */
+	onMouseLeave?(
+		event: React.MouseEvent< HTMLButtonElement, MouseEvent >
+	): void;
```

```suggestion
```

These can all be removed once we use `WordPressComponentProps` to mix in the standard `button` props.

#### bangank36 on 2023-08-06T03:39:54Z

URL: https://github.com/WordPress/gutenberg/pull/53132#discussion_r1285147730
Path: packages/components/src/menu-item/types.d.ts

```diff
@@ -0,0 +1,78 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+/**
+ * Internal dependencies
+ */
+import type { IconType } from '../icon';
+
+export type MenuItemProps = {
+	/**
+	 * A CSS `class` to give to the container element.
+	 */
+	className?: string;
+	/**
+	 * The children elements.
+	 */
+	children?: ReactNode;
+	/**
+	 * Text to use as description for button text.
+	 */
+	info?: string;
+	/**
+	 * The icon to render. Supported values are: Dashicons (specified as
+	 * strings), functions, Component instances and `null`.
+	 *
+	 * @default null
+	 */
+	icon?: JSX.Element | null;
+	/**
+	 * Determines where to display the provided `icon`.
+	 */
+	iconPosition?: string;
+	/**
+	 * Whether or not the menu item is currently selected.
+	 */
+	isSelected?: boolean;
+	/**
+	 * If shortcut is a string, it is expecting the display text. If shortcut is an object,
+	 * it will accept the properties of `display` (string) and `ariaLabel` (string).
+	 */
+	shortcut?: string | { display: string; ariaLabel: string };
+	/**
+	 * If you need to have selectable menu items use menuitemradio for single select,
+	 * and menuitemcheckbox for multiselect.
+	 *
+	 * @default 'menuitem'
+	 */
+	role?: string;
+	/**
+	 * Allows for markup other than icons or shortcuts to be added to the menu item.
+	 *
+	 */
+	suffix?: ReactNode;
+	/**
+	 * Human-readable label for item.
+	 */
+	label?: string;
+	/**
+	 * On click handler.
+	 */
+	onClick?( event: React.MouseEvent< HTMLButtonElement, MouseEvent > ): void;
+	/**
+	 * On mouse enter handler.
+	 * @default noop
+	 */
+	onMouseEnter?(
+		event: React.MouseEvent< HTMLButtonElement, MouseEvent >
+	): void;
+	/**
+	 * On mouse leave handler.
+	 * @default noop
+	 */
+	onMouseLeave?(
+		event: React.MouseEvent< HTMLButtonElement, MouseEvent >
+	): void;
```

Sorted

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +3/-0)
- packages/components/src/menu-item/index.tsx (RENAMED, +29/-2)
- packages/components/src/menu-item/types.ts (ADDED, +60/-0)
- packages/components/src/menu-items-choice/index.tsx (MODIFIED, +1/-1)
- packages/components/src/tools-panel/tools-panel-header/component.tsx (MODIFIED, +1/-1)
