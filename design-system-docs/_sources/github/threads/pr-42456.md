---
number: 42456
type: pull_request
state: MERGED
merged_at: 2022-07-19T14:03:05Z
url: https://github.com/WordPress/gutenberg/pull/42456
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] UI Components","[Package] Components"]
---

# PR #42456: SelectControl: Add flag for larger default size

- URL: https://github.com/WordPress/gutenberg/pull/42456
- Author: mirka
- Created: 2022-07-15T07:48:09Z
- Updated: 2022-07-19T14:04:55Z
- Comments: 0 of 0
- Review threads: 1 of 1
- Changed files: 3 of 3
- Diff: ../pr-diffs/42456.diff

## Body

Part of #39397
In preparation for #41973

## What?

Adds the `__next36pxDefaultSize` prop to SelectControl for the larger default size.

## Why?

The #39397 project is on hold, but this one is necessary for implementing size variants for CustomSelectControl (#42460). Basically, I want both SelectControl and CustomSelectControl to share the underlying styling components, and CustomSelectControl already has a `__next36pxDefaultSize` flag. So we need to implement the flag on SelectControl for parity.

## Testing Instructions

1. `npm run storybook:dev` and see the SelectControl story.
2. `__next36pxDefaultSize` should only affect the `default` size. Other size variants should be unaffected.

## Issue comments

_No issue comments._
## Review threads

### Review thread 1

#### ciampo on 2022-07-15T10:59:56Z

URL: https://github.com/WordPress/gutenberg/pull/42456#discussion_r922060108
Path: packages/components/src/select-control/styles/select-control-styles.ts

```diff
@@ -67,16 +71,28 @@ const sizeStyles = ( { selectSize = 'default' }: SelectProps ) => {
 		},
 	};
 
-	const style = sizes[ selectSize ];
+	if ( ! __next36pxDefaultSize ) {
+		sizes.default = {
+			height: 30,
+			minHeight: 30,
+			paddingTop: 0,
+			paddingBottom: 0,
+		};
+	}
+
+	const style = sizes[ selectSize ] || sizes.default;
```

In which situation is `sizes[ selectSize ]` returning `undefined`, so that we have to fallback to the default? (same applies to the similar code change in the `sizePaddings` mixin)

#### mirka on 2022-07-19T12:55:18Z

URL: https://github.com/WordPress/gutenberg/pull/42456#discussion_r924466981
Path: packages/components/src/select-control/styles/select-control-styles.ts

```diff
@@ -67,16 +71,28 @@ const sizeStyles = ( { selectSize = 'default' }: SelectProps ) => {
 		},
 	};
 
-	const style = sizes[ selectSize ];
+	if ( ! __next36pxDefaultSize ) {
+		sizes.default = {
+			height: 30,
+			minHeight: 30,
+			paddingTop: 0,
+			paddingBottom: 0,
+		};
+	}
+
+	const style = sizes[ selectSize ] || sizes.default;
```

Just a courtesy fallback in case a consumer passes an invalid size string.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/select-control/styles/select-control-styles.ts (MODIFIED, +33/-9)
- packages/components/src/select-control/types.ts (MODIFIED, +1/-0)
