---
number: 54317
type: pull_request
state: MERGED
merged_at: 2023-09-15T17:01:57Z
url: https://github.com/WordPress/gutenberg/pull/54317
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #54317: ToolbarGroup - Typescript

- URL: https://github.com/WordPress/gutenberg/pull/54317
- Author: margolisj
- Created: 2023-09-08T20:51:18Z
- Updated: 2023-09-15T17:02:32Z
- Comments: 6 of 6
- Review threads: 12 of 12
- Changed files: 10 of 10
- Diff: ../pr-diffs/54317.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
Converts ToolbarGroup to typescript.

## Why?
<!-- Why is this PR necessary? What problem is it solving? Reference any existing previous issue(s) or PR(s), but please add a short summary here, too -->
Typescript is the only sane was to maintain a codebase this large.

## How?
<!-- How is your PR addressing the issue at hand? What are the implementation details? -->

## Testing Instructions
<!-- Please include step by step instructions on how to test this PR. -->
<!-- 1. Open a post or page. -->
<!-- 2. Insert a heading block. -->
<!-- 3. etc. -->

### Testing Instructions for Keyboard
<!-- How can you test the changes by using the keyboard only? Please note, this is required for PRs that change the user interface (UI). This ensures the PR can be tested for any possible accessibility regressions. -->

## Screenshots or screencast <!-- if applicable -->

## Issue comments

### margolisj on 2023-09-08T21:02:43Z

URL: https://github.com/WordPress/gutenberg/pull/54317#issuecomment-1712218825

@mirka @ciampo #35744. Restarting / recovering this code from my previous attempt at ToolbarGroup from before I pulled from trunk on the wrong branch... Anyways, looking like this one is going to be a bear with how tightly Dropdown and Toolbar are integrated together.

### ciampo on 2023-09-13T08:21:34Z

URL: https://github.com/WordPress/gutenberg/pull/54317#issuecomment-1717167787

Hey @margolisj , I see some comments have been marked as resolved, but not commits were pushed to the PR. I asusme that you're still working through the feedback.

Let me know when all comments have been addressed, and I can take another look

### margolisj on 2023-09-14T16:50:07Z

URL: https://github.com/WordPress/gutenberg/pull/54317#issuecomment-1719802803

I think the types got a little wonky with how many changes there were. I'll spend a little time this afternoon going back through your comments and making sure I didn't foobar too many of them.

Dangling type error:
```
packages/components/src/toolbar/toolbar-group/index.tsx(103,7): error TS2322: Type '{ icon?: IconType | null | undefined; title: string; isDisabled?: boolean | undefined; onClick?: ((event?: MouseEvent<Element, MouseEvent> | undefined) => void) | undefined; ... 5 more ...; containerClassName: string | undefined; }' is not assignable to type 'Omit<Pick<BaseButtonProps & _ButtonProps & Omit<Pick<DetailedHTMLProps<ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>, "key" | keyof ButtonHTMLAttributes<...>>, "as" | ... 1 more ... | keyof BaseButtonProps> & RefAttributes<...>, "hidden" | ... 264 more ... | keyof BaseButtonProps> | Pick<...>, "as" | k...'.
  Types of property 'role' are incompatible.
    Type 'string | null | undefined' is not assignable to type 'AriaRole | undefined'.
      Type 'null' is not assignable to type 'AriaRole | undefined'.
 ```

### ciampo on 2023-09-15T14:41:48Z

URL: https://github.com/WordPress/gutenberg/pull/54317#issuecomment-1721395349

Current TS issues should be fixed by:

<details>

<summary>Fixing the `role` type in the dropdown options to be more specific</summary>


```diff
diff --git a/packages/components/src/dropdown-menu/types.ts b/packages/components/src/dropdown-menu/types.ts
index aea558b73d..4781761037 100644
--- a/packages/components/src/dropdown-menu/types.ts
+++ b/packages/components/src/dropdown-menu/types.ts
@@ -1,7 +1,7 @@
 /**
  * External dependencies
  */
-import type { ReactNode } from 'react';
+import type { HTMLAttributes, ReactNode } from 'react';
 /**
  * Internal dependencies
  */
@@ -41,7 +41,7 @@ export type DropdownOption = {
 	/**
 	 * The role to apply to the option's HTML element
 	 */
-	role?: HTMLElement[ 'role' ];
+	role?: HTMLAttributes< HTMLElement >[ 'role' ];
 };
 
 type DropdownCallbackProps = {

```

</details>

and

<details>

<summary>Adding an `icon` prop to the ToolbarGroup props</summary>


```diff
diff --git a/packages/components/src/dropdown-menu/types.ts b/packages/components/src/dropdown-menu/types.ts
index 4781761037..7b141c97ac 100644
--- a/packages/components/src/dropdown-menu/types.ts
+++ b/packages/components/src/dropdown-menu/types.ts
@@ -13,7 +13,7 @@ import type { NavigableMenuProps } from '../navigable-container/types';
 
 export type DropdownOption = {
 	/**
-	 * The Dashicon icon slug to be shown for the option.
+	 * The icon to be shown for the option.
 	 */
 	icon?: IconProps[ 'icon' ];
 	/**
@@ -64,7 +64,7 @@ type ToggleProps = Partial<
 
 export type DropdownMenuProps = {
 	/**
-	 * The Dashicon icon slug to be shown in the collapsed menu button.
+	 * The icon to be shown in the collapsed menu button.
 	 *
 	 * @default "menu"
 	 */
diff --git a/packages/components/src/toolbar/toolbar-group/types.ts b/packages/components/src/toolbar/toolbar-group/types.ts
index a11fc2da8f..1bccdfc6f7 100644
--- a/packages/components/src/toolbar/toolbar-group/types.ts
+++ b/packages/components/src/toolbar/toolbar-group/types.ts
@@ -91,6 +91,10 @@ export type ToolbarGroupProps = ToolbarGroupPropsBase &
 				 * ARIA label for dropdown menu if is collapsed.
 				 */
 				title: string;
+				/**
+				 * The icon to be shown in the collapsed menu button.
+				 */
+				icon?: ToolbarGroupCollapsedProps[ 'icon' ];
 		  }
 	 );
 
```

</details>

Let's also cleanup the old, commented-out code

### ciampo on 2023-09-15T16:13:37Z

URL: https://github.com/WordPress/gutenberg/pull/54317#issuecomment-1721530225

@margolisj  let's also add a CHANGELOG entry (under `Internal`)

### margolisj on 2023-09-15T16:15:12Z

URL: https://github.com/WordPress/gutenberg/pull/54317#issuecomment-1721532199

> @margolisj let's also add a CHANGELOG entry (under `Internal`)

Too fast for me haha. Had just noticed. Pushed it.

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2023-09-11T11:49:54Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321423778
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
+
+export type ToolbarGroupProps = {
+	/**
+	 * The controls to render in this toolbar.
+	 */
+	controls?: ToolbarControls[];
+
+	/**
+	 * Any other things to render inside the toolbar besides the controls.
+	 */
+	children?: ReactNode;
+
+	/**
+	 * Class to set on the container div.
+	 */
+	className?: string;
+
+	/**
+	 * Turns ToolbarGroup into a dropdown menu.
+	 */
+	isCollapsed?: boolean;
+
+	/**
+	 * ARIA label for dropdown menu if is collapsed.
+	 */
+	title?: string;
+
+	// TODO: Rekit
+	icon?: any;
+	label?: string;
+
+	/**
+	 * Props to be passed.
+	 */
+	props?: any;
```

I don't think this is needed

#### margolisj on 2023-09-12T18:25:21Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323410255
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
+
+export type ToolbarGroupProps = {
+	/**
+	 * The controls to render in this toolbar.
+	 */
+	controls?: ToolbarControls[];
+
+	/**
+	 * Any other things to render inside the toolbar besides the controls.
+	 */
+	children?: ReactNode;
+
+	/**
+	 * Class to set on the container div.
+	 */
+	className?: string;
+
+	/**
+	 * Turns ToolbarGroup into a dropdown menu.
+	 */
+	isCollapsed?: boolean;
+
+	/**
+	 * ARIA label for dropdown menu if is collapsed.
+	 */
+	title?: string;
+
+	// TODO: Rekit
+	icon?: any;
+	label?: string;
+
+	/**
+	 * Props to be passed.
+	 */
+	props?: any;
```

Removed.

### Review thread 2 (resolved)

#### ciampo on 2023-09-11T11:50:58Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321425270
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
+
+export type ToolbarGroupProps = {
+	/**
+	 * The controls to render in this toolbar.
+	 */
+	controls?: ToolbarControls[];
+
+	/**
+	 * Any other things to render inside the toolbar besides the controls.
+	 */
+	children?: ReactNode;
+
+	/**
+	 * Class to set on the container div.
+	 */
+	className?: string;
+
+	/**
+	 * Turns ToolbarGroup into a dropdown menu.
+	 */
+	isCollapsed?: boolean;
+
+	/**
+	 * ARIA label for dropdown menu if is collapsed.
+	 */
+	title?: string;
+
+	// TODO: Rekit
+	icon?: any;
+	label?: string;
```

Here too, I don't think those props are needed

#### margolisj on 2023-09-12T18:25:15Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323410146
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
+
+export type ToolbarGroupProps = {
+	/**
+	 * The controls to render in this toolbar.
+	 */
+	controls?: ToolbarControls[];
+
+	/**
+	 * Any other things to render inside the toolbar besides the controls.
+	 */
+	children?: ReactNode;
+
+	/**
+	 * Class to set on the container div.
+	 */
+	className?: string;
+
+	/**
+	 * Turns ToolbarGroup into a dropdown menu.
+	 */
+	isCollapsed?: boolean;
+
+	/**
+	 * ARIA label for dropdown menu if is collapsed.
+	 */
+	title?: string;
+
+	// TODO: Rekit
+	icon?: any;
+	label?: string;
```

Removed.

### Review thread 3 (resolved)

#### ciampo on 2023-09-11T11:51:31Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321426047
Path: packages/components/src/dropdown-menu/types.ts

For better compat, let's add `event` to the `onClick` prop:

```diff
diff --git a/packages/components/src/dropdown-menu/types.ts b/packages/components/src/dropdown-menu/types.ts
index 066cd0087d..96b9c798b0 100644
--- a/packages/components/src/dropdown-menu/types.ts
+++ b/packages/components/src/dropdown-menu/types.ts
@@ -2,6 +2,7 @@
  * External dependencies
  */
 import type { ReactNode } from 'react';
+
 /**
  * Internal dependencies
  */
@@ -29,7 +30,7 @@ export type DropdownOption = {
 	/**
 	 * A callback function to invoke when the option is selected.
 	 */
-	onClick?: () => void;
+	onClick?: ( event?: React.MouseEvent ) => void;
 	/**
 	 * Whether or not the control is currently active.
 	 */


```

#### margolisj on 2023-09-12T18:10:01Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323396307
Path: packages/components/src/dropdown-menu/types.ts

Done.

### Review thread 4 (resolved)

#### ciampo on 2023-09-11T11:52:17Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321427227
Path: packages/components/src/dropdown-menu/types.ts

```diff
@@ -59,6 +59,7 @@ type ToggleProps = Partial<
 	>
 > & {
 	as?: React.ElementType | keyof JSX.IntrinsicElements;
+	'data-toolbar-item': boolean;
```

I think this should be optional?

```suggestion
	'data-toolbar-item'?: boolean;
```

#### margolisj on 2023-09-12T18:13:00Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323399049
Path: packages/components/src/dropdown-menu/types.ts

```diff
@@ -59,6 +59,7 @@ type ToggleProps = Partial<
 	>
 > & {
 	as?: React.ElementType | keyof JSX.IntrinsicElements;
+	'data-toolbar-item': boolean;
```

Done.

### Review thread 5 (resolved)

#### ciampo on 2023-09-11T11:53:37Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321429235
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
```

I think this can reuse the `DropdownOption` type, something like 

```ts
/**
 * Internal dependencies
 */
import type {	DropdownOption } from '../../dropdown-menu/types';

export type ToolbarGroupControls = DropdownOption & {
	/**
	 * An optional subscript associated to the control.
	 */
	subscript?: string;
};
```

### Review thread 6 (resolved)

#### ciampo on 2023-09-11T11:56:26Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321433266
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
+
+export type ToolbarGroupProps = {
```

Let's differentiate between when `isCollapsed` is `true` and when it's `false|undefined`, since it looks like `children` and `title` change based off that. Also, let's re-use the type def for the `children` prop from `DropdownMenu`.

Also, note that `controls` can either be an array, or an array or arrays (as hinted in the `index.tsx` file when trying to normalize the `controls` prop, and also as done in the `DropdownMenu` component)

```ts
/**
 * Internal dependencies
 */
import type {	DropdownMenuProps } from '../../dropdown-menu/types';

type ToolbarGroupPropsBase = {
	/**
	 * The controls to render in this toolbar.
	 */
	controls?: ToolbarGroupControls[] | ToolbarGroupControls[][];

	/**
	 * Class to set on the container div.
	 */
	className?: string;
};

export type ToolbarGroupProps = ToolbarGroupPropsBase &
	(
		| {
				/**
				 * When true, turns `ToolbarGroup` into a dropdown menu.
				 */
				isCollapsed?: false;
				/**
				 * Any other things to render inside the toolbar besides the controls.
				 */
				children?: ReactNode;
				title?: never;
		  }
		| {
				/**
				 * When true, turns `ToolbarGroup` into a dropdown menu.
				 */
				isCollapsed: true;
				/**
				 * Any other things to render inside the toolbar besides the controls.
				 */
				children?: ToolbarGroupCollapsedProps[ 'children' ];
				/**
				 * ARIA label for dropdown menu if is collapsed.
				 */
				title: string;
		  }
	 );

### Review thread 7 (resolved)

#### ciampo on 2023-09-11T11:57:39Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321435174
Path: packages/components/src/toolbar/toolbar-group/types.ts

```diff
@@ -0,0 +1,75 @@
+/**
+ * External dependencies
+ */
+import type { ReactNode } from 'react';
+
+// TODO: Type these props correctly
+type ToolbarControls = {
+	// icon?: string;
+	// title?: string;
+	// onClick: ( event?: Event ) => void;
+	// isActive?: boolean;
+};
+
+export type ToolbarGroupProps = {
+	/**
+	 * The controls to render in this toolbar.
+	 */
+	controls?: ToolbarControls[];
+
+	/**
+	 * Any other things to render inside the toolbar besides the controls.
+	 */
+	children?: ReactNode;
+
+	/**
+	 * Class to set on the container div.
+	 */
+	className?: string;
+
+	/**
+	 * Turns ToolbarGroup into a dropdown menu.
+	 */
+	isCollapsed?: boolean;
+
+	/**
+	 * ARIA label for dropdown menu if is collapsed.
+	 */
+	title?: string;
+
+	// TODO: Rekit
+	icon?: any;
+	label?: string;
+
+	/**
+	 * Props to be passed.
+	 */
+	props?: any;
+};
+
+export type ToolbarGroupCollapsedProps = Omit< ToolbarGroupProps, 'props' > & {
```

We can probably use `DropdownMenuProps` here too

```ts
/**
 * Internal dependencies
 */
import type {	DropdownMenuProps } from '../../dropdown-menu/types';

export type ToolbarGroupCollapsedProps = DropdownMenuProps;
```

### Review thread 8 (resolved)

#### ciampo on 2023-09-11T11:58:35Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321436842
Path: packages/components/src/toolbar/toolbar-group/toolbar-group-collapsed.tsx

```diff
@@ -11,14 +9,23 @@ import { useContext } from '@wordpress/element';
 import DropdownMenu from '../../dropdown-menu';
 import ToolbarContext from '../toolbar-context';
 import ToolbarItem from '../toolbar-item';
+import type { ToolbarGroupCollapsedProps } from './types';
 
-function ToolbarGroupCollapsed( { controls = [], toggleProps, ...props } ) {
+function ToolbarGroupCollapsed( {
+	controls = [],
+	toggleProps,
+	...props
+}: ToolbarGroupCollapsedProps ) {
 	// It'll contain state if `ToolbarGroup` is being used within
 	// `<Toolbar label="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const renderDropdownMenu = ( internalToggleProps ) => (
+	const renderDropdownMenu = (
+		internalToggleProps?: Record< string, any > // ExtractHTMLAttributes<any>
+	) => (
 		<DropdownMenu
+			// TODO: Any idea how best to solve this
+			// @ts-expect-error Dropdown Menu expects dropdown menu types.
```

After the proposed changes in `types.ts`, we should be able to remove this comment

#### margolisj on 2023-09-12T18:23:12Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323408322
Path: packages/components/src/toolbar/toolbar-group/toolbar-group-collapsed.tsx

```diff
@@ -11,14 +9,23 @@ import { useContext } from '@wordpress/element';
 import DropdownMenu from '../../dropdown-menu';
 import ToolbarContext from '../toolbar-context';
 import ToolbarItem from '../toolbar-item';
+import type { ToolbarGroupCollapsedProps } from './types';
 
-function ToolbarGroupCollapsed( { controls = [], toggleProps, ...props } ) {
+function ToolbarGroupCollapsed( {
+	controls = [],
+	toggleProps,
+	...props
+}: ToolbarGroupCollapsedProps ) {
 	// It'll contain state if `ToolbarGroup` is being used within
 	// `<Toolbar label="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const renderDropdownMenu = ( internalToggleProps ) => (
+	const renderDropdownMenu = (
+		internalToggleProps?: Record< string, any > // ExtractHTMLAttributes<any>
+	) => (
 		<DropdownMenu
+			// TODO: Any idea how best to solve this
+			// @ts-expect-error Dropdown Menu expects dropdown menu types.
```

Done.

### Review thread 9 (resolved)

#### ciampo on 2023-09-11T11:59:19Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321437960
Path: packages/components/src/toolbar/toolbar-group/toolbar-group-collapsed.tsx

```diff
@@ -11,14 +9,23 @@ import { useContext } from '@wordpress/element';
 import DropdownMenu from '../../dropdown-menu';
 import ToolbarContext from '../toolbar-context';
 import ToolbarItem from '../toolbar-item';
+import type { ToolbarGroupCollapsedProps } from './types';
 
-function ToolbarGroupCollapsed( { controls = [], toggleProps, ...props } ) {
+function ToolbarGroupCollapsed( {
+	controls = [],
+	toggleProps,
+	...props
+}: ToolbarGroupCollapsedProps ) {
 	// It'll contain state if `ToolbarGroup` is being used within
 	// `<Toolbar label="label" />`
 	const accessibleToolbarState = useContext( ToolbarContext );
 
-	const renderDropdownMenu = ( internalToggleProps ) => (
+	const renderDropdownMenu = (
+		internalToggleProps?: Record< string, any > // ExtractHTMLAttributes<any>
```

Should this be something like `DropdownMenuProps[ 'toggleProps' ]` ?

### Review thread 10 (resolved)

#### ciampo on 2023-09-11T12:01:39Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321441409
Path: packages/components/src/toolbar/toolbar-group/index.tsx

Let's remove type definitions in JSDocs

```diff
diff --git a/packages/components/src/toolbar/toolbar-group/index.tsx b/packages/components/src/toolbar/toolbar-group/index.tsx
index 20793f5811..0347b7dc6f 100644
--- a/packages/components/src/toolbar/toolbar-group/index.tsx
+++ b/packages/components/src/toolbar/toolbar-group/index.tsx
@@ -44,12 +44,12 @@ function isNestedArray< T = any >( arr: T[] | T[][] ): arr is T[][] {
  * Either `controls` or `children` is required, otherwise this components
  * renders nothing.
  *
- * @param {Object}    props               Component props.
- * @param {Array}     [props.controls]    The controls to render in this toolbar.
- * @param {WPElement} [props.children]    Any other things to render inside the toolbar besides the controls.
- * @param {string}    [props.className]   Class to set on the container div.
- * @param {boolean}   [props.isCollapsed] Turns ToolbarGroup into a dropdown menu.
- * @param {string}    [props.title]       ARIA label for dropdown menu if is collapsed.
+ * @param props               Component props.
+ * @param [props.controls]    The controls to render in this toolbar.
+ * @param [props.children]    Any other things to render inside the toolbar besides the controls.
+ * @param [props.className]   Class to set on the container div.
+ * @param [props.isCollapsed] Turns ToolbarGroup into a dropdown menu.
+ * @param [props.title]       ARIA label for dropdown menu if is collapsed.
  */
 function ToolbarGroup( {
 	controls = [],

```

#### margolisj on 2023-09-12T18:18:03Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323403650
Path: packages/components/src/toolbar/toolbar-group/index.tsx

Done.

### Review thread 11 (resolved)

#### ciampo on 2023-09-11T12:03:53Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321444672
Path: packages/components/src/toolbar/toolbar-group/index.tsx

```diff
@@ -94,7 +93,8 @@ function ToolbarGroup( {
 	return (
 		<ToolbarGroupContainer className={ finalClassName } { ...props }>
 			{ controlSets?.flatMap( ( controlSet, indexOfSet ) =>
-				controlSet.map( ( control, indexOfControl ) => (
+				// @ts-expect-error Type issue
+				controlSet.map( ( control: any, indexOfControl: number ) => (
```

We can fix this by:
- changing the type of the `controls` prop to reflect the fact that it can also be an array of arrays (see previous comment on `types.ts`)
- refactoring slightly the code above, to ensure that `controlsSet` is always a nested array. To do that, I've move the inline check to a separate function acting as a type guard

```diff
diff --git a/packages/components/src/toolbar/toolbar-group/index.tsx b/packages/components/src/toolbar/toolbar-group/index.tsx
index 443d46dee0..0347b7dc6f 100644
--- a/packages/components/src/toolbar/toolbar-group/index.tsx
+++ b/packages/components/src/toolbar/toolbar-group/index.tsx
@@ -15,7 +15,11 @@ import ToolbarButton from '../toolbar-button';
 import ToolbarGroupContainer from './toolbar-group-container';
 import ToolbarGroupCollapsed from './toolbar-group-collapsed';
 import ToolbarContext from '../toolbar-context';
-import type { ToolbarGroupProps } from './types';
+import type { ToolbarGroupProps, ToolbarGroupControls } from './types';
+
+function isNestedArray< T = any >( arr: T[] | T[][] ): arr is T[][] {
+	return Array.isArray( arr ) && Array.isArray( arr[ 0 ] );
+}
 
 /**
  * Renders a collapsible group of controls
@@ -73,9 +77,11 @@ function ToolbarGroup( {
 	);
 
 	// Normalize controls to nested array of objects (sets of controls)
-	let controlSets = controls;
-	if ( ! Array.isArray( controlSets[ 0 ] ) ) {
-		controlSets = [ controlSets ];
+	let controlSets: ToolbarGroupControls[][];
+	if ( isNestedArray( controls ) ) {
+		controlSets = controls;
+	} else {
+		controlSets = [ controls ];
 	}
 
 	if ( isCollapsed ) {
@@ -93,7 +99,6 @@ function ToolbarGroup( {
 	return (
 		<ToolbarGroupContainer className={ finalClassName } { ...props }>
 			{ controlSets?.flatMap( ( controlSet, indexOfSet ) =>
-				// @ts-expect-error Type issue
 				controlSet.map( ( control: any, indexOfControl: number ) => (
 					<ToolbarButton
 						key={ [ indexOfSet, indexOfControl ].join() }
```

### Review thread 12 (resolved)

#### ciampo on 2023-09-11T13:23:44Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1321546087
Path: packages/components/src/toolbar/stories/index.story.tsx

Addings types revealed a few inconsistencies with the way the components are used — `label` is used a couple of times instead of `title`, and `icon` is given a value of `false` instead of `null` (to signify "no icon")

```diff
diff --git a/packages/components/src/toolbar/stories/index.story.tsx b/packages/components/src/toolbar/stories/index.story.tsx
index 35032caf69..2eecf54a4a 100644
--- a/packages/components/src/toolbar/stories/index.story.tsx
+++ b/packages/components/src/toolbar/stories/index.story.tsx
@@ -117,8 +117,8 @@ Default.args = {
 				<ToolbarButton icon={ link } label="Link" />
 				<ToolbarGroup
 					isCollapsed
-					icon={ false }
-					label="More rich text controls"
+					icon={ null }
+					title="More rich text controls"
 					controls={ [
 						{ icon: code, title: 'Inline code' },
 						{ icon: <InlineImageIcon />, title: 'Inline image' },
@@ -131,7 +131,7 @@ Default.args = {
 			</ToolbarGroup>
 			<ToolbarGroup
 				icon={ more }
-				label="Align"
+				title="Align"
 				isCollapsed
 				controls={ [
 					{

```

#### margolisj on 2023-09-12T18:15:38Z

URL: https://github.com/WordPress/gutenberg/pull/54317#discussion_r1323401436
Path: packages/components/src/toolbar/stories/index.story.tsx

Done.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/dropdown-menu/types.ts (MODIFIED, +7/-6)
- packages/components/src/toolbar/stories/index.story.tsx (MODIFIED, +3/-5)
- packages/components/src/toolbar/test/toolbar-group.tsx (MODIFIED, +13/-7)
- packages/components/src/toolbar/toolbar-group/index.tsx (RENAMED, +19/-14)
- packages/components/src/toolbar/toolbar-group/toolbar-group-collapsed.tsx (RENAMED, +10/-4)
- packages/components/src/toolbar/toolbar-group/toolbar-group-container.js (DELETED, +0/-8)
- packages/components/src/toolbar/toolbar-group/toolbar-group-container.tsx (ADDED, +16/-0)
- packages/components/src/toolbar/toolbar-group/types.ts (ADDED, +92/-0)
- packages/components/src/toolbar/toolbar/index.tsx (MODIFIED, +9/-1)
