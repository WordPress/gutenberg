---
number: 20118
type: pull_request
state: MERGED
merged_at: 2020-02-10T15:38:20Z
url: https://github.com/WordPress/gutenberg/pull/20118
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Developer Documentation","[Feature] UI Components","[Package] Components"]
---

# PR #20118: Stabilize and document the angle picker component; Rename to AnglePickerControl;

- URL: https://github.com/WordPress/gutenberg/pull/20118
- Author: jorgefilipecosta
- Created: 2020-02-08T19:02:41Z
- Updated: 2020-02-12T20:26:22Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 11 of 11
- Diff: ../pr-diffs/20118.diff

## Body

## Description
This PR stabilizes and documents the angle picker component.

## How has this been tested?
I verified in the browser console that the angle picker component was now exposed as wp.components.AnglePicker.

## Issue comments

### jorgefilipecosta on 2020-02-09T23:05:52Z

URL: https://github.com/WordPress/gutenberg/pull/20118#issuecomment-583905134

Hi @chrisvanpatten, I renamed the component to AnglePickerControl.

## Review threads

### Review thread 1 (resolved)

#### youknowriad on 2020-02-10T08:20:12Z

URL: https://github.com/WordPress/gutenberg/pull/20118#discussion_r376915971
Path: packages/components/src/angle-picker-control/README.md

```diff
@@ -0,0 +1,40 @@
+# AnglePickerControl
+
+AnglePickerControl is a React component to render a UI that allows users to pick an angle.
+Users can choose an angle in a visual UI with the mouse by dragging an angle indicator inside a circle or by directly inserting the desired angle in a text field.
+
+## Usage
+
+
+```jsx
+import { useState } from '@wordpress/element';
+import { AnglePickerControl } from '@wordpress/components';
+
+const MyAnglePicker = () => {
+	const [ angle, setAngle ] = useState();
+	return <AnglePickerControl value={ angle } onChange={ setAngle } />;
+};
+```
+
+## Props
+
+The component accepts the following props.
+
+### label
+
+Label to use for the angle picker. If not set the a translated label "Angle" is used.
+
+- Type: `String`
+- Required: No
+
+### value
+The current value of the input.
+
+- Type: `String | Number`
```

Why is it a string or number? It's not clear what the expected value should be? 0 to 360?

#### jorgefilipecosta on 2020-02-10T12:05:27Z

URL: https://github.com/WordPress/gutenberg/pull/20118#discussion_r377022907
Path: packages/components/src/angle-picker-control/README.md

```diff
@@ -0,0 +1,40 @@
+# AnglePickerControl
+
+AnglePickerControl is a React component to render a UI that allows users to pick an angle.
+Users can choose an angle in a visual UI with the mouse by dragging an angle indicator inside a circle or by directly inserting the desired angle in a text field.
+
+## Usage
+
+
+```jsx
+import { useState } from '@wordpress/element';
+import { AnglePickerControl } from '@wordpress/components';
+
+const MyAnglePicker = () => {
+	const [ angle, setAngle ] = useState();
+	return <AnglePickerControl value={ angle } onChange={ setAngle } />;
+};
+```
+
+## Props
+
+The component accepts the following props.
+
+### label
+
+Label to use for the angle picker. If not set the a translated label "Angle" is used.
+
+- Type: `String`
+- Required: No
+
+### value
+The current value of the input.
+
+- Type: `String | Number`
```

Hi @youknowriad, I documented as a String or Number because passing a value as a string or number would work well, also if the value is not between 0 and 360 things would also work well as mathematically these values are possible (although they could be represented as something between 0 and 360). But on second thought even though things work it does not mean they should be used that way so I updated type to be a number and said value should be between 0 and 360.

### Review thread 2

#### aduth on 2020-02-12T20:24:49Z

URL: https://github.com/WordPress/gutenberg/pull/20118#discussion_r378493053
Path: package-lock.json

```diff
@@ -19511,7 +19511,7 @@
 				},
 				"node-pre-gyp": {
 					"version": "0.12.0",
-					"resolved": "https://registry.npmjs.org/node-pre-gyp/-/node-pre-gyp-0.12.0.tgz",
+					"resolved": false,
```

I don't see why these changes would be expected here?

When I run `npm install` on `master` using the recommended (latest) version of NPM (6.13.7), I see this being flipped back to the value it was previously.

#### aduth on 2020-02-12T20:26:22Z

URL: https://github.com/WordPress/gutenberg/pull/20118#discussion_r378493801
Path: package-lock.json

```diff
@@ -19511,7 +19511,7 @@
 				},
 				"node-pre-gyp": {
 					"version": "0.12.0",
-					"resolved": "https://registry.npmjs.org/node-pre-gyp/-/node-pre-gyp-0.12.0.tgz",
+					"resolved": false,
```

See a59a5d9ab761bb2e0ee37eb67aab96d7f3dcf91b

## Changed files

- docs/manifest-devhub.json (MODIFIED, +6/-0)
- docs/manifest.json (MODIFIED, +6/-0)
- package-lock.json (MODIFIED, +2/-2)
- packages/components/src/angle-picker-control/README.md (ADDED, +40/-0)
- packages/components/src/angle-picker-control/index.js (RENAMED, +8/-8)
- packages/components/src/angle-picker-control/stories/index.js (RENAMED, +6/-3)
- packages/components/src/angle-picker-control/style.scss (RENAMED, +5/-5)
- packages/components/src/custom-gradient-picker/index.js (MODIFIED, +2/-2)
- packages/components/src/index.js (MODIFIED, +1/-1)
- packages/components/src/style.scss (MODIFIED, +1/-1)
- storybook/test/__snapshots__/index.js.snap (MODIFIED, +8/-8)
