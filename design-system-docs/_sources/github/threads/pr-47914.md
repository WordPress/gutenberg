---
number: 47914
type: pull_request
state: MERGED
merged_at: 2023-03-02T06:20:41Z
url: https://github.com/WordPress/gutenberg/pull/47914
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Bug","[Feature] UI Components","[Package] Components"]
---

# PR #47914: Fix HStack and VStack alignment prop

- URL: https://github.com/WordPress/gutenberg/pull/47914
- Author: youknowriad
- Created: 2023-02-09T09:52:30Z
- Updated: 2023-03-02T06:21:05Z
- Comments: 3 of 3
- Review threads: 7 of 7
- Changed files: 11 of 11
- Diff: ../pr-diffs/47914.diff

## Body

## What?

While working on #47827 I've noticed a bug in VStack component. The alignment prop was not working as expected. It turns out that there are several bugs in the alignment prop of both VStack and HStack, This PR fixes it.

## Testing Instructions

1- Open the storybook of both VStack and HStack
2- Check that all the alignment options behave as expected.

## Issue comments

### github-actions on 2023-02-09T10:17:03Z

URL: https://github.com/WordPress/gutenberg/pull/47914#issuecomment-1423945727

<!-- flaky-tests-report-comment -->
**Flaky tests detected in 172ee459d61b4904f3b037a97fc9071b82a6f14c.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/4265445081
📝  Reported issues:
- #47052 in `specs/editor/blocks/cover.test.js`

### youknowriad on 2023-02-24T08:27:55Z

URL: https://github.com/WordPress/gutenberg/pull/47914#issuecomment-1443117327

Any thoughts on this PR @ciampo

### ciampo on 2023-02-24T10:18:16Z

URL: https://github.com/WordPress/gutenberg/pull/47914#issuecomment-1443414270

It's in my review queue — I hope to get to it by EOD.

 I've been working on implementing some VizReg tests specifically on HStack and VStack (#48260) in the meantime, which will help while reviewing this PR.

## Review threads

### Review thread 1

#### youknowriad on 2023-02-09T09:53:06Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1101216251
Path: packages/components/src/flex/flex/hook.ts

```diff
@@ -62,7 +62,7 @@ export function useFlex( props: WordPressComponentProps< FlexProps, 'div' > ) {
 
 	const classes = useMemo( () => {
 		const base = css( {
-			alignItems: isColumn ? 'normal' : align,
+			alignItems: align,
```

This is the most important change. I'm not sure I understand the reason for the check originally, I checked the original PR and it's not mentioned.

### Review thread 2

#### youknowriad on 2023-02-09T09:53:20Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1101216496
Path: packages/components/src/h-stack/utils.ts

```diff
@@ -11,7 +11,7 @@ import { isValueDefined } from '../utils/values';
 
 const H_ALIGNMENTS: Alignments = {
 	bottom: { align: 'flex-end', justify: 'center' },
-	bottomLeft: { align: 'flex-start', justify: 'flex-end' },
+	bottomLeft: { align: 'flex-end', justify: 'flex-start' },
```

This was buggy (inverted values)

### Review thread 3

#### youknowriad on 2023-02-09T09:53:26Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1101216630
Path: packages/components/src/h-stack/utils.ts

```diff
@@ -25,13 +25,13 @@ const H_ALIGNMENTS: Alignments = {
 
 const V_ALIGNMENTS: Alignments = {
 	bottom: { justify: 'flex-end', align: 'center' },
-	bottomLeft: { justify: 'flex-start', align: 'flex-end' },
+	bottomLeft: { justify: 'flex-end', align: 'flex-start' },
```

This was buggy (inverted values)

### Review thread 4

#### youknowriad on 2023-02-09T09:53:34Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1101216802
Path: packages/components/src/h-stack/utils.ts

```diff
@@ -25,13 +25,13 @@ const H_ALIGNMENTS: Alignments = {
 
 const V_ALIGNMENTS: Alignments = {
 	bottom: { justify: 'flex-end', align: 'center' },
-	bottomLeft: { justify: 'flex-start', align: 'flex-end' },
+	bottomLeft: { justify: 'flex-end', align: 'flex-start' },
 	bottomRight: { justify: 'flex-end', align: 'flex-end' },
 	center: { justify: 'center', align: 'center' },
 	edge: { justify: 'space-between', align: 'center' },
 	left: { justify: 'center', align: 'flex-start' },
 	right: { justify: 'center', align: 'flex-end' },
-	stretch: { justify: 'stretch' },
+	stretch: { align: 'stretch' },
```

This was buggy too.

### Review thread 5 (resolved)

#### ciampo on 2023-02-24T18:07:42Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1117425585
Path: packages/components/src/v-stack/hook.ts

```diff
@@ -8,14 +8,16 @@ import type { VStackProps } from './types';
 export function useVStack(
 	props: WordPressComponentProps< VStackProps, 'div' >
 ) {
-	const { expanded = false, ...otherProps } = useContextSystem(
-		props,
-		'VStack'
-	);
+	const {
+		expanded = false,
+		alignment = 'strech',
```

Typo

```suggestion
		alignment = 'stretch',
```

Changing the default value of the `alignment` property to `stretch` is a breaking change, since currently on `trunk` the default value is `edge` (due to the `useHStack` hook) — can we roll this change back, and pass the `alignment = "stretch"` to `VStack` where necessary?

#### youknowriad on 2023-02-24T21:50:27Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1117705768
Path: packages/components/src/v-stack/hook.ts

```diff
@@ -8,14 +8,16 @@ import type { VStackProps } from './types';
 export function useVStack(
 	props: WordPressComponentProps< VStackProps, 'div' >
 ) {
-	const { expanded = false, ...otherProps } = useContextSystem(
-		props,
-		'VStack'
-	);
+	const {
+		expanded = false,
+		alignment = 'strech',
```

I don't remember exactly the reasoning here but I believe the previous default value was broken or something like that and that I've used "stretch" here because that's what we expect as a result (the closest to the previous state)

#### mirka on 2023-02-28T10:50:05Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1119885209
Path: packages/components/src/v-stack/hook.ts

```diff
@@ -8,14 +8,16 @@ import type { VStackProps } from './types';
 export function useVStack(
 	props: WordPressComponentProps< VStackProps, 'div' >
 ) {
-	const { expanded = false, ...otherProps } = useContextSystem(
-		props,
-		'VStack'
-	);
+	const {
+		expanded = false,
+		alignment = 'strech',
```

Yeah, we have a slight problem here 🤔 The previous VStack defaulted to
`justify-content: space-between` and `align-items: normal` (== `stretch` in most cases). This combination of styles does not exist in the newly corrected vertical `alignment` values.

I think I agree that we'll cause the least amount of damage by defaulting to `stretch` here. The new defaults will be `justify-content: center` and `align-items: stretch`, but I believe the justify will not affect the vast majority of usages because very few people set explicit heights on their VStacks.

Let's just fix the type docs 👉 https://github.com/WordPress/gutenberg/commit/d88a8c833e7e01cb6ad11a8b3346fe873a4c8afa

### Review thread 6

#### ciampo on 2023-02-24T18:40:30Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1117471988
Path: packages/components/src/flex/flex/hook.ts

```diff
@@ -62,7 +62,7 @@ export function useFlex( props: WordPressComponentProps< FlexProps, 'div' > ) {
 
 	const classes = useMemo( () => {
 		const base = css( {
-			alignItems: isColumn ? 'normal' : align,
+			alignItems: align ?? ( isColumn ? 'normal' : 'center' ),
```

This change definitely makes sense, but I'm not sure if the new behaviour of `HStack` is 100% correct either.

I was taking a look at `HStack` when `direction="column"` , and I was playing around with the `alignment` prop:
- on `trunk`, in this case changing the `alignment` prop doesn't seem to make any differences
- on this PR, it seems to work as expected, except for the `alignment="edge"` case - the items appear to be centered, but I believed they should be stretched (especially given the description of the `edge` value: "Aligns content to the edges of the container.")

<details>

<summary>Looks like this change would fix it</summary>

```diff
diff --git a/packages/components/src/h-stack/utils.ts b/packages/components/src/h-stack/utils.ts
index b1413ee2dc..60151b00a2 100644
--- a/packages/components/src/h-stack/utils.ts
+++ b/packages/components/src/h-stack/utils.ts
@@ -28,7 +28,7 @@ const V_ALIGNMENTS: Alignments = {
 	bottomLeft: { justify: 'flex-end', align: 'flex-start' },
 	bottomRight: { justify: 'flex-end', align: 'flex-end' },
 	center: { justify: 'center', align: 'center' },
-	edge: { justify: 'space-between', align: 'center' },
+	edge: { justify: 'space-between', align: 'stretch' },
 	left: { justify: 'center', align: 'flex-start' },
 	right: { justify: 'center', align: 'flex-end' },
 	stretch: { align: 'stretch' },

```
</details>

#### youknowriad on 2023-02-24T21:51:31Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1117706974
Path: packages/components/src/flex/flex/hook.ts

```diff
@@ -62,7 +62,7 @@ export function useFlex( props: WordPressComponentProps< FlexProps, 'div' > ) {
 
 	const classes = useMemo( () => {
 		const base = css( {
-			alignItems: isColumn ? 'normal' : align,
+			alignItems: align ?? ( isColumn ? 'normal' : 'center' ),
```

If I remember properly, I based the behavior of "edge" here by comparing the behavior of "h" in HStack and applied the same behavior but in the other axis.

#### mirka on 2023-02-28T10:06:46Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1119831557
Path: packages/components/src/flex/flex/hook.ts

```diff
@@ -62,7 +62,7 @@ export function useFlex( props: WordPressComponentProps< FlexProps, 'div' > ) {
 
 	const classes = useMemo( () => {
 		const base = css( {
-			alignItems: isColumn ? 'normal' : align,
+			alignItems: align ?? ( isColumn ? 'normal' : 'center' ),
```

As much as I don't love the arbitrariness of this `align` prop, I agree with Riad that this new `edge` behavior for VStack is correct/consistent within the existing logic of the system.

In this system, I understand `stretch` to apply only to the cross axis, and `edge` to apply only to the main axis.

How about we tweak the description to make that clearer?

```diff
-	 * * `edge`: Aligns content to the edges of the container.
+	 * * `edge`: Justifies content to be evenly spread out up to the main axis edges of the container.
-	 * * `stretch`: Stretches content to the edges of the container.
+	 * * `stretch`: Stretches content to the cross axis edges of the container.
```

#### ciampo on 2023-02-28T17:22:35Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1120473117
Path: packages/components/src/flex/flex/hook.ts

```diff
@@ -62,7 +62,7 @@ export function useFlex( props: WordPressComponentProps< FlexProps, 'div' > ) {
 
 	const classes = useMemo( () => {
 		const base = css( {
-			alignItems: isColumn ? 'normal' : align,
+			alignItems: align ?? ( isColumn ? 'normal' : 'center' ),
```

Thank you both.

Given that the separation between breaking change and bug fix can be blurry here, I agree with Lena's proposed solution

### Review thread 7

#### mirka on 2023-02-28T09:38:26Z

URL: https://github.com/WordPress/gutenberg/pull/47914#discussion_r1119796249
Path: packages/components/src/v-stack/stories/index.tsx

```diff
@@ -28,12 +46,15 @@ export default meta;
 
 const Template: ComponentStory< typeof VStack > = ( props ) => {
 	return (
-		<VStack { ...props }>
-			<View>One</View>
-			<View>Two</View>
-			<View>Three</View>
-			<View>Four</View>
-			<View>Five</View>
+		<VStack
+			{ ...props }
+			style={ { background: '#eee', minHeight: '200px' } }
+		>
+			{ [ 'One', 'Two', 'Three', 'Four', 'Five' ].map( ( text ) => (
+				<View key={ text } style={ { background: '#b9f9ff' } }>
+					{ text }
+				</View>
+			) ) }
```

Thanks for this improvement!

## Changed files

- packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap (MODIFIED, +16/-14)
- packages/components/src/color-palette/test/__snapshots__/index.tsx.snap (MODIFIED, +16/-14)
- packages/components/src/flex/flex/hook.ts (MODIFIED, +2/-2)
- packages/components/src/h-stack/README.md (MODIFIED, +2/-2)
- packages/components/src/h-stack/types.ts (MODIFIED, +2/-2)
- packages/components/src/h-stack/utils.ts (MODIFIED, +3/-3)
- packages/components/src/v-stack/README.md (MODIFIED, +2/-2)
- packages/components/src/v-stack/hook.ts (MODIFIED, +6/-4)
- packages/components/src/v-stack/stories/index.tsx (MODIFIED, +28/-7)
- packages/components/src/v-stack/test/__snapshots__/index.tsx.snap (MODIFIED, +20/-18)
- packages/components/src/v-stack/types.ts (MODIFIED, +5/-3)
