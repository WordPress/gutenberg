---
number: 41097
type: pull_request
state: MERGED
merged_at: 2022-05-27T02:04:44Z
url: https://github.com/WordPress/gutenberg/pull/41097
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Feature] Document Settings","[Package] Components","Needs User Documentation"]
---

# PR #41097: Design updates to the Publish popover (`DateTimePicker`)

- URL: https://github.com/WordPress/gutenberg/pull/41097
- Author: noisysocks
- Created: 2022-05-17T05:32:22Z
- Updated: 2022-09-02T17:56:04Z
- Comments: 14 of 14
- Review threads: 26 of 26
- Changed files: 34 of 34
- Diff: ../pr-diffs/41097.diff

## Body

## What?
Implements the new design for the Publish popover (`DateTimePicker`) that's in https://github.com/WordPress/gutenberg/issues/39082.

Since we're changing most of the CSS anyway, I've taken this opportunity to convert most of `DateTimePicker` to use existing WordPress components and Emotion styling.

## Why?
Part of https://github.com/WordPress/gutenberg/issues/39082.

## How?
- Updates the design of `DateTimePicker`, `DatePicker` and `TimePicker`.
- Changes `DateTimePicker`, `DatePicker` and `TimePicker` to use existing WordPress components and Emotion styling where it makes sense to do so.
  - I kept all of the `react-dates` theming as CSS. Emotion doesn't really add much here since we have to use CSS selectors anyway.
- Re-organises `DateTimePicker` folder structure to be more consistent with newer components.
  - Each sub-component in `date-time` now has its own folder.
- Allows removal of _Reset_ button and _Help_ functionality from `DateTimePicker`.
  - The new design does not have this.
  - This is implemented using a `__next` prop. We should eventually make this the default.
- Adds `PublishDateTimePicker` to block-editor.
  - This exposes a specialised `DateTimePicker` that is used for changing a _post_ publish date.
  - Basically it just adds the _Publish_ heading.

## Testing Instructions
1. Create a post.
2. Open the post settings sidebar.
3. Change the _Publish_ date.4. 

Also play with the `DateTimePicker` storybook in `npm run storybook:dev`.

There are existing unit tests for `DateTimePicker` which hopefully I didn't break.

## Screenshots or screencast
| English | Arabic |
| - | - |
| <img width="280" alt="Screen Shot 2022-05-19 at 18 03 10" src="https://user-images.githubusercontent.com/612155/169244006-2eee5bc4-eaf5-4781-b17a-120c85edf8d6.png"> | <img width="279" alt="Screen Shot 2022-05-19 at 18 02 22" src="https://user-images.githubusercontent.com/612155/169244041-c169b8ee-783e-4d8b-88aa-934d4eb9cb22.png"> |

## Issue comments

### github-actions on 2022-05-17T05:39:36Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1128434222

**Size Change:** +352 B (0%) 

**Total Size:** 1.24 MB

| Filename | Size | Change |
| :--- | :---: | :---: |
| `build/block-editor/index.min.js` | 151 kB | +285 B (0%) |
| `build/block-editor/style-rtl.css` | 14.6 kB | +61 B (0%) |
| `build/block-editor/style.css` | 14.6 kB | +60 B (0%) |
| `build/block-library/index.min.js` | 181 kB | +16 B (0%) |
| `build/components/index.min.js` | 228 kB | +1.11 kB (0%) |
| `build/components/style-rtl.css` | 13.9 kB | -584 B (-4%) |
| `build/components/style.css` | 13.9 kB | -587 B (-4%) |
| `build/edit-post/index.min.js` | 30.4 kB | -1 B (0%) |
| `build/editor/index.min.js` | 38.5 kB | -11 B (0%) |

<details><summary>ℹ️ <strong>View Unchanged</strong></summary>

| Filename | Size |
| :--- | :---: |
| `build/a11y/index.min.js` | 993 B |
| `build/annotations/index.min.js` | 2.77 kB |
| `build/api-fetch/index.min.js` | 2.27 kB |
| `build/autop/index.min.js` | 2.15 kB |
| `build/blob/index.min.js` | 487 B |
| `build/block-directory/index.min.js` | 6.51 kB |
| `build/block-directory/style-rtl.css` | 1.01 kB |
| `build/block-directory/style.css` | 1.01 kB |
| `build/block-editor/default-editor-styles-rtl.css` | 378 B |
| `build/block-editor/default-editor-styles.css` | 378 B |
| `build/block-library/blocks/archives/editor-rtl.css` | 61 B |
| `build/block-library/blocks/archives/editor.css` | 60 B |
| `build/block-library/blocks/archives/style-rtl.css` | 65 B |
| `build/block-library/blocks/archives/style.css` | 65 B |
| `build/block-library/blocks/audio/editor-rtl.css` | 150 B |
| `build/block-library/blocks/audio/editor.css` | 150 B |
| `build/block-library/blocks/audio/style-rtl.css` | 111 B |
| `build/block-library/blocks/audio/style.css` | 111 B |
| `build/block-library/blocks/audio/theme-rtl.css` | 125 B |
| `build/block-library/blocks/audio/theme.css` | 125 B |
| `build/block-library/blocks/avatar/editor-rtl.css` | 116 B |
| `build/block-library/blocks/avatar/editor.css` | 116 B |
| `build/block-library/blocks/avatar/style-rtl.css` | 59 B |
| `build/block-library/blocks/avatar/style.css` | 59 B |
| `build/block-library/blocks/block/editor-rtl.css` | 161 B |
| `build/block-library/blocks/block/editor.css` | 161 B |
| `build/block-library/blocks/button/editor-rtl.css` | 445 B |
| `build/block-library/blocks/button/editor.css` | 445 B |
| `build/block-library/blocks/button/style-rtl.css` | 560 B |
| `build/block-library/blocks/button/style.css` | 560 B |
| `build/block-library/blocks/buttons/editor-rtl.css` | 292 B |
| `build/block-library/blocks/buttons/editor.css` | 292 B |
| `build/block-library/blocks/buttons/style-rtl.css` | 275 B |
| `build/block-library/blocks/buttons/style.css` | 275 B |
| `build/block-library/blocks/calendar/style-rtl.css` | 207 B |
| `build/block-library/blocks/calendar/style.css` | 207 B |
| `build/block-library/blocks/categories/editor-rtl.css` | 84 B |
| `build/block-library/blocks/categories/editor.css` | 83 B |
| `build/block-library/blocks/categories/style-rtl.css` | 79 B |
| `build/block-library/blocks/categories/style.css` | 79 B |
| `build/block-library/blocks/code/style-rtl.css` | 103 B |
| `build/block-library/blocks/code/style.css` | 103 B |
| `build/block-library/blocks/code/theme-rtl.css` | 124 B |
| `build/block-library/blocks/code/theme.css` | 124 B |
| `build/block-library/blocks/columns/editor-rtl.css` | 108 B |
| `build/block-library/blocks/columns/editor.css` | 108 B |
| `build/block-library/blocks/columns/style-rtl.css` | 406 B |
| `build/block-library/blocks/columns/style.css` | 406 B |
| `build/block-library/blocks/comment-author-avatar/editor-rtl.css` | 125 B |
| `build/block-library/blocks/comment-author-avatar/editor.css` | 125 B |
| `build/block-library/blocks/comment-content/style-rtl.css` | 92 B |
| `build/block-library/blocks/comment-content/style.css` | 92 B |
| `build/block-library/blocks/comment-template/style-rtl.css` | 127 B |
| `build/block-library/blocks/comment-template/style.css` | 127 B |
| `build/block-library/blocks/comments-pagination-numbers/editor-rtl.css` | 123 B |
| `build/block-library/blocks/comments-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/comments-pagination/editor-rtl.css` | 222 B |
| `build/block-library/blocks/comments-pagination/editor.css` | 209 B |
| `build/block-library/blocks/comments-pagination/style-rtl.css` | 235 B |
| `build/block-library/blocks/comments-pagination/style.css` | 231 B |
| `build/block-library/blocks/comments-title/editor-rtl.css` | 75 B |
| `build/block-library/blocks/comments-title/editor.css` | 75 B |
| `build/block-library/blocks/comments/editor-rtl.css` | 95 B |
| `build/block-library/blocks/comments/editor.css` | 95 B |
| `build/block-library/blocks/cover/editor-rtl.css` | 546 B |
| `build/block-library/blocks/cover/editor.css` | 547 B |
| `build/block-library/blocks/cover/style-rtl.css` | 1.56 kB |
| `build/block-library/blocks/cover/style.css` | 1.56 kB |
| `build/block-library/blocks/embed/editor-rtl.css` | 293 B |
| `build/block-library/blocks/embed/editor.css` | 293 B |
| `build/block-library/blocks/embed/style-rtl.css` | 417 B |
| `build/block-library/blocks/embed/style.css` | 417 B |
| `build/block-library/blocks/embed/theme-rtl.css` | 124 B |
| `build/block-library/blocks/embed/theme.css` | 124 B |
| `build/block-library/blocks/file/editor-rtl.css` | 300 B |
| `build/block-library/blocks/file/editor.css` | 300 B |
| `build/block-library/blocks/file/style-rtl.css` | 255 B |
| `build/block-library/blocks/file/style.css` | 255 B |
| `build/block-library/blocks/file/view.min.js` | 353 B |
| `build/block-library/blocks/freeform/editor-rtl.css` | 2.44 kB |
| `build/block-library/blocks/freeform/editor.css` | 2.44 kB |
| `build/block-library/blocks/gallery/editor-rtl.css` | 961 B |
| `build/block-library/blocks/gallery/editor.css` | 964 B |
| `build/block-library/blocks/gallery/style-rtl.css` | 1.51 kB |
| `build/block-library/blocks/gallery/style.css` | 1.51 kB |
| `build/block-library/blocks/gallery/theme-rtl.css` | 122 B |
| `build/block-library/blocks/gallery/theme.css` | 122 B |
| `build/block-library/blocks/group/editor-rtl.css` | 333 B |
| `build/block-library/blocks/group/editor.css` | 333 B |
| `build/block-library/blocks/group/style-rtl.css` | 57 B |
| `build/block-library/blocks/group/style.css` | 57 B |
| `build/block-library/blocks/group/theme-rtl.css` | 78 B |
| `build/block-library/blocks/group/theme.css` | 78 B |
| `build/block-library/blocks/heading/style-rtl.css` | 76 B |
| `build/block-library/blocks/heading/style.css` | 76 B |
| `build/block-library/blocks/html/editor-rtl.css` | 332 B |
| `build/block-library/blocks/html/editor.css` | 333 B |
| `build/block-library/blocks/image/editor-rtl.css` | 738 B |
| `build/block-library/blocks/image/editor.css` | 737 B |
| `build/block-library/blocks/image/style-rtl.css` | 529 B |
| `build/block-library/blocks/image/style.css` | 535 B |
| `build/block-library/blocks/image/theme-rtl.css` | 124 B |
| `build/block-library/blocks/image/theme.css` | 124 B |
| `build/block-library/blocks/latest-comments/style-rtl.css` | 284 B |
| `build/block-library/blocks/latest-comments/style.css` | 284 B |
| `build/block-library/blocks/latest-posts/editor-rtl.css` | 199 B |
| `build/block-library/blocks/latest-posts/editor.css` | 198 B |
| `build/block-library/blocks/latest-posts/style-rtl.css` | 463 B |
| `build/block-library/blocks/latest-posts/style.css` | 462 B |
| `build/block-library/blocks/list/style-rtl.css` | 88 B |
| `build/block-library/blocks/list/style.css` | 88 B |
| `build/block-library/blocks/media-text/editor-rtl.css` | 266 B |
| `build/block-library/blocks/media-text/editor.css` | 263 B |
| `build/block-library/blocks/media-text/style-rtl.css` | 493 B |
| `build/block-library/blocks/media-text/style.css` | 490 B |
| `build/block-library/blocks/more/editor-rtl.css` | 431 B |
| `build/block-library/blocks/more/editor.css` | 431 B |
| `build/block-library/blocks/navigation-link/editor-rtl.css` | 708 B |
| `build/block-library/blocks/navigation-link/editor.css` | 706 B |
| `build/block-library/blocks/navigation-link/style-rtl.css` | 115 B |
| `build/block-library/blocks/navigation-link/style.css` | 115 B |
| `build/block-library/blocks/navigation-submenu/editor-rtl.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/editor.css` | 299 B |
| `build/block-library/blocks/navigation-submenu/view.min.js` | 375 B |
| `build/block-library/blocks/navigation/editor-rtl.css` | 2.03 kB |
| `build/block-library/blocks/navigation/editor.css` | 2.04 kB |
| `build/block-library/blocks/navigation/style-rtl.css` | 1.95 kB |
| `build/block-library/blocks/navigation/style.css` | 1.94 kB |
| `build/block-library/blocks/navigation/view-modal.min.js` | 2.78 kB |
| `build/block-library/blocks/navigation/view.min.js` | 395 B |
| `build/block-library/blocks/nextpage/editor-rtl.css` | 395 B |
| `build/block-library/blocks/nextpage/editor.css` | 395 B |
| `build/block-library/blocks/page-list/editor-rtl.css` | 363 B |
| `build/block-library/blocks/page-list/editor.css` | 363 B |
| `build/block-library/blocks/page-list/style-rtl.css` | 175 B |
| `build/block-library/blocks/page-list/style.css` | 175 B |
| `build/block-library/blocks/paragraph/editor-rtl.css` | 157 B |
| `build/block-library/blocks/paragraph/editor.css` | 157 B |
| `build/block-library/blocks/paragraph/style-rtl.css` | 260 B |
| `build/block-library/blocks/paragraph/style.css` | 260 B |
| `build/block-library/blocks/post-author/style-rtl.css` | 175 B |
| `build/block-library/blocks/post-author/style.css` | 176 B |
| `build/block-library/blocks/post-comments-form/editor-rtl.css` | 69 B |
| `build/block-library/blocks/post-comments-form/editor.css` | 69 B |
| `build/block-library/blocks/post-comments-form/style-rtl.css` | 495 B |
| `build/block-library/blocks/post-comments-form/style.css` | 495 B |
| `build/block-library/blocks/post-comments/editor-rtl.css` | 77 B |
| `build/block-library/blocks/post-comments/editor.css` | 77 B |
| `build/block-library/blocks/post-comments/style-rtl.css` | 628 B |
| `build/block-library/blocks/post-comments/style.css` | 628 B |
| `build/block-library/blocks/post-excerpt/editor-rtl.css` | 73 B |
| `build/block-library/blocks/post-excerpt/editor.css` | 73 B |
| `build/block-library/blocks/post-excerpt/style-rtl.css` | 69 B |
| `build/block-library/blocks/post-excerpt/style.css` | 69 B |
| `build/block-library/blocks/post-featured-image/editor-rtl.css` | 721 B |
| `build/block-library/blocks/post-featured-image/editor.css` | 721 B |
| `build/block-library/blocks/post-featured-image/style-rtl.css` | 153 B |
| `build/block-library/blocks/post-featured-image/style.css` | 153 B |
| `build/block-library/blocks/post-template/editor-rtl.css` | 99 B |
| `build/block-library/blocks/post-template/editor.css` | 98 B |
| `build/block-library/blocks/post-template/style-rtl.css` | 323 B |
| `build/block-library/blocks/post-template/style.css` | 323 B |
| `build/block-library/blocks/post-terms/style-rtl.css` | 73 B |
| `build/block-library/blocks/post-terms/style.css` | 73 B |
| `build/block-library/blocks/post-title/style-rtl.css` | 80 B |
| `build/block-library/blocks/post-title/style.css` | 80 B |
| `build/block-library/blocks/preformatted/style-rtl.css` | 103 B |
| `build/block-library/blocks/preformatted/style.css` | 103 B |
| `build/block-library/blocks/pullquote/editor-rtl.css` | 198 B |
| `build/block-library/blocks/pullquote/editor.css` | 198 B |
| `build/block-library/blocks/pullquote/style-rtl.css` | 370 B |
| `build/block-library/blocks/pullquote/style.css` | 370 B |
| `build/block-library/blocks/pullquote/theme-rtl.css` | 167 B |
| `build/block-library/blocks/pullquote/theme.css` | 167 B |
| `build/block-library/blocks/query-pagination-numbers/editor-rtl.css` | 122 B |
| `build/block-library/blocks/query-pagination-numbers/editor.css` | 121 B |
| `build/block-library/blocks/query-pagination/editor-rtl.css` | 221 B |
| `build/block-library/blocks/query-pagination/editor.css` | 211 B |
| `build/block-library/blocks/query-pagination/style-rtl.css` | 234 B |
| `build/block-library/blocks/query-pagination/style.css` | 231 B |
| `build/block-library/blocks/query/editor-rtl.css` | 369 B |
| `build/block-library/blocks/query/editor.css` | 369 B |
| `build/block-library/blocks/quote/style-rtl.css` | 213 B |
| `build/block-library/blocks/quote/style.css` | 213 B |
| `build/block-library/blocks/quote/theme-rtl.css` | 223 B |
| `build/block-library/blocks/quote/theme.css` | 226 B |
| `build/block-library/blocks/read-more/style-rtl.css` | 132 B |
| `build/block-library/blocks/read-more/style.css` | 132 B |
| `build/block-library/blocks/rss/editor-rtl.css` | 202 B |
| `build/block-library/blocks/rss/editor.css` | 204 B |
| `build/block-library/blocks/rss/style-rtl.css` | 289 B |
| `build/block-library/blocks/rss/style.css` | 288 B |
| `build/block-library/blocks/search/editor-rtl.css` | 165 B |
| `build/block-library/blocks/search/editor.css` | 165 B |
| `build/block-library/blocks/search/style-rtl.css` | 397 B |
| `build/block-library/blocks/search/style.css` | 398 B |
| `build/block-library/blocks/search/theme-rtl.css` | 64 B |
| `build/block-library/blocks/search/theme.css` | 64 B |
| `build/block-library/blocks/separator/editor-rtl.css` | 146 B |
| `build/block-library/blocks/separator/editor.css` | 146 B |
| `build/block-library/blocks/separator/style-rtl.css` | 233 B |
| `build/block-library/blocks/separator/style.css` | 233 B |
| `build/block-library/blocks/separator/theme-rtl.css` | 194 B |
| `build/block-library/blocks/separator/theme.css` | 194 B |
| `build/block-library/blocks/shortcode/editor-rtl.css` | 474 B |
| `build/block-library/blocks/shortcode/editor.css` | 474 B |
| `build/block-library/blocks/site-logo/editor-rtl.css` | 759 B |
| `build/block-library/blocks/site-logo/editor.css` | 759 B |
| `build/block-library/blocks/site-logo/style-rtl.css` | 192 B |
| `build/block-library/blocks/site-logo/style.css` | 192 B |
| `build/block-library/blocks/site-tagline/editor-rtl.css` | 86 B |
| `build/block-library/blocks/site-tagline/editor.css` | 86 B |
| `build/block-library/blocks/site-title/editor-rtl.css` | 84 B |
| `build/block-library/blocks/site-title/editor.css` | 84 B |
| `build/block-library/blocks/social-link/editor-rtl.css` | 177 B |
| `build/block-library/blocks/social-link/editor.css` | 177 B |
| `build/block-library/blocks/social-links/editor-rtl.css` | 674 B |
| `build/block-library/blocks/social-links/editor.css` | 673 B |
| `build/block-library/blocks/social-links/style-rtl.css` | 1.37 kB |
| `build/block-library/blocks/social-links/style.css` | 1.36 kB |
| `build/block-library/blocks/spacer/editor-rtl.css` | 332 B |
| `build/block-library/blocks/spacer/editor.css` | 332 B |
| `build/block-library/blocks/spacer/style-rtl.css` | 48 B |
| `build/block-library/blocks/spacer/style.css` | 48 B |
| `build/block-library/blocks/table/editor-rtl.css` | 504 B |
| `build/block-library/blocks/table/editor.css` | 504 B |
| `build/block-library/blocks/table/style-rtl.css` | 625 B |
| `build/block-library/blocks/table/style.css` | 625 B |
| `build/block-library/blocks/table/theme-rtl.css` | 188 B |
| `build/block-library/blocks/table/theme.css` | 188 B |
| `build/block-library/blocks/tag-cloud/style-rtl.css` | 226 B |
| `build/block-library/blocks/tag-cloud/style.css` | 227 B |
| `build/block-library/blocks/template-part/editor-rtl.css` | 149 B |
| `build/block-library/blocks/template-part/editor.css` | 149 B |
| `build/block-library/blocks/template-part/theme-rtl.css` | 101 B |
| `build/block-library/blocks/template-part/theme.css` | 101 B |
| `build/block-library/blocks/text-columns/editor-rtl.css` | 95 B |
| `build/block-library/blocks/text-columns/editor.css` | 95 B |
| `build/block-library/blocks/text-columns/style-rtl.css` | 166 B |
| `build/block-library/blocks/text-columns/style.css` | 166 B |
| `build/block-library/blocks/verse/style-rtl.css` | 87 B |
| `build/block-library/blocks/verse/style.css` | 87 B |
| `build/block-library/blocks/video/editor-rtl.css` | 561 B |
| `build/block-library/blocks/video/editor.css` | 563 B |
| `build/block-library/blocks/video/style-rtl.css` | 173 B |
| `build/block-library/blocks/video/style.css` | 173 B |
| `build/block-library/blocks/video/theme-rtl.css` | 124 B |
| `build/block-library/blocks/video/theme.css` | 124 B |
| `build/block-library/common-rtl.css` | 993 B |
| `build/block-library/common.css` | 990 B |
| `build/block-library/editor-rtl.css` | 10.2 kB |
| `build/block-library/editor.css` | 10.3 kB |
| `build/block-library/reset-rtl.css` | 478 B |
| `build/block-library/reset.css` | 478 B |
| `build/block-library/style-rtl.css` | 11.6 kB |
| `build/block-library/style.css` | 11.6 kB |
| `build/block-library/theme-rtl.css` | 689 B |
| `build/block-library/theme.css` | 694 B |
| `build/block-serialization-default-parser/index.min.js` | 1.12 kB |
| `build/block-serialization-spec-parser/index.min.js` | 2.83 kB |
| `build/blocks/index.min.js` | 47.1 kB |
| `build/compose/index.min.js` | 11.7 kB |
| `build/core-data/index.min.js` | 14.6 kB |
| `build/customize-widgets/index.min.js` | 11.2 kB |
| `build/customize-widgets/style-rtl.css` | 1.4 kB |
| `build/customize-widgets/style.css` | 1.4 kB |
| `build/data-controls/index.min.js` | 663 B |
| `build/data/index.min.js` | 7.98 kB |
| `build/date/index.min.js` | 32 kB |
| `build/deprecated/index.min.js` | 518 B |
| `build/dom-ready/index.min.js` | 336 B |
| `build/dom/index.min.js` | 4.69 kB |
| `build/edit-navigation/index.min.js` | 16 kB |
| `build/edit-navigation/style-rtl.css` | 4.05 kB |
| `build/edit-navigation/style.css` | 4.05 kB |
| `build/edit-post/classic-rtl.css` | 546 B |
| `build/edit-post/classic.css` | 547 B |
| `build/edit-post/style-rtl.css` | 7.08 kB |
| `build/edit-post/style.css` | 7.08 kB |
| `build/edit-site/index.min.js` | 47.9 kB |
| `build/edit-site/style-rtl.css` | 7.73 kB |
| `build/edit-site/style.css` | 7.71 kB |
| `build/edit-widgets/index.min.js` | 16.4 kB |
| `build/edit-widgets/style-rtl.css` | 4.4 kB |
| `build/edit-widgets/style.css` | 4.4 kB |
| `build/editor/style-rtl.css` | 3.71 kB |
| `build/editor/style.css` | 3.7 kB |
| `build/element/index.min.js` | 4.3 kB |
| `build/escape-html/index.min.js` | 548 B |
| `build/format-library/index.min.js` | 6.62 kB |
| `build/format-library/style-rtl.css` | 571 B |
| `build/format-library/style.css` | 571 B |
| `build/hooks/index.min.js` | 1.66 kB |
| `build/html-entities/index.min.js` | 454 B |
| `build/i18n/index.min.js` | 3.79 kB |
| `build/is-shallow-equal/index.min.js` | 535 B |
| `build/keyboard-shortcuts/index.min.js` | 1.83 kB |
| `build/keycodes/index.min.js` | 1.41 kB |
| `build/list-reusable-blocks/index.min.js` | 1.75 kB |
| `build/list-reusable-blocks/style-rtl.css` | 835 B |
| `build/list-reusable-blocks/style.css` | 835 B |
| `build/media-utils/index.min.js` | 2.9 kB |
| `build/notices/index.min.js` | 957 B |
| `build/nux/index.min.js` | 2.1 kB |
| `build/nux/style-rtl.css` | 744 B |
| `build/nux/style.css` | 741 B |
| `build/plugins/index.min.js` | 1.98 kB |
| `build/preferences-persistence/index.min.js` | 2.23 kB |
| `build/preferences/index.min.js` | 1.32 kB |
| `build/primitives/index.min.js` | 949 B |
| `build/priority-queue/index.min.js` | 628 B |
| `build/react-i18n/index.min.js` | 704 B |
| `build/react-refresh-entry/index.min.js` | 8.44 kB |
| `build/react-refresh-runtime/index.min.js` | 7.31 kB |
| `build/redux-routine/index.min.js` | 2.69 kB |
| `build/reusable-blocks/index.min.js` | 2.24 kB |
| `build/reusable-blocks/style-rtl.css` | 256 B |
| `build/reusable-blocks/style.css` | 256 B |
| `build/rich-text/index.min.js` | 11.2 kB |
| `build/server-side-render/index.min.js` | 1.61 kB |
| `build/shortcode/index.min.js` | 1.52 kB |
| `build/token-list/index.min.js` | 668 B |
| `build/url/index.min.js` | 1.99 kB |
| `build/vendors/react-dom.min.js` | 38.5 kB |
| `build/vendors/react.min.js` | 4.34 kB |
| `build/viewport/index.min.js` | 1.08 kB |
| `build/warning/index.min.js` | 280 B |
| `build/widgets/index.min.js` | 7.2 kB |
| `build/widgets/style-rtl.css` | 1.16 kB |
| `build/widgets/style.css` | 1.16 kB |
| `build/wordcount/index.min.js` | 1.07 kB |

</details>



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### noisysocks on 2022-05-18T05:19:04Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1129577339

I've still got some tasks remaining before we can merge this (see description) but I'm happy with the broad strokes and think this is ready for a first round of review.

### andrewserong on 2022-05-18T06:33:56Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1129625525

Thanks for the ping @noisysocks, I think I'll run out of time today to take a closer look, but I'm looking forward to getting stuck into review 🤓

At first glance, the design's looking really nice in the editor 👍

<img src="https://user-images.githubusercontent.com/14988353/168972018-678bed68-c6e4-420e-8b7c-00dbae22275f.png" width="250" />

One thing I noticed is that if you click on the AM / PM buttons it closes the popover, unlike the other buttons in the popover. Not sure if focus is being moved somewhere?

### noisysocks on 2022-05-18T07:04:05Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1129646917

> One thing I noticed is that if you click on the AM / PM buttons it closes the popover, unlike the other buttons in the popover. Not sure if focus is being moved somewhere?

👍 I made it so that the popover remains open. The alternative is that we auto-close it after a date is selected but I don't like that because then it makes it hard to select a date **and** a time. Also we have an X now.

### javierarce on 2022-05-18T10:35:22Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1129847027

This is looking great! I just have just one comment:

When you have scheduled another post we show a small blue dot under the day number. But if you hover over the day or select it, the dot disappears. I propose changing the color of the dot so it's always visible. That would mean white for when the day is selected or selected and hovered. 

https://user-images.githubusercontent.com/4933/169019189-7e6ee1f2-7e64-4a1d-96e4-9f084f75c6ba.mov


Then I noticed a weird reload the first time you select a day from a different month. I guess this is not related to this PR, but I just wanted to mention it anyway in case it's helpful. 

https://user-images.githubusercontent.com/4933/169019483-191eb3a4-c15b-4829-81b0-935f231670df.mov

### noisysocks on 2022-05-19T06:49:28Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1131296643

> When you have scheduled another post we show a small blue dot under the day number. But if you hover over the day or select it, the dot disappears. I propose changing the color of the dot so it's always visible. That would mean white for when the day is selected or selected and hovered.

Good thinking! I've done this.

> Then I noticed a weird reload the first time you select a day from a different month. I guess this is not related to this PR, but I just wanted to mention it anyway in case it's helpful.

Let's look into this separately. It happens because of https://github.com/react-dates/react-dates/issues/1320 which is fixed but not yet released.

### noisysocks on 2022-05-19T08:08:57Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1131377552

OK, happy with this now 🙂 I fixed unit tests, tweaked some of the margins, fixed some issues with RTL languages, and tested everything in multiple browsers.

I'll split out some of the `@wordpress/components` stuff into a separate PR for ease of review tomorrow, but don't let that stop you from giving this a whirl!

### noisysocks on 2022-05-20T07:06:59Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1132550246

Thanks for the reviews! I've addressed all the feedback. I'll split the PR up on Monday.

### noisysocks on 2022-05-24T06:00:43Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1135445055

OK I split these three PRs out:

- https://github.com/WordPress/gutenberg/pull/41266
- https://github.com/WordPress/gutenberg/pull/41269
- https://github.com/WordPress/gutenberg/pull/41270

Will switch this PR back to draft until they're merged.

I'd like to land everything else together in this PR as that way all of the user-facing changes are sure to appear at the same time.

### ciampo on 2022-05-24T12:50:28Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1135881263

> OK I split these three PRs out:

Thank you! All PRs have been approved :)

> I'd like to land everything else together in this PR as that way all of the user-facing changes are sure to appear at the same time.

Makes sense. My initial preference was that changes to `DateTimePicker` would be applied in a separate PR, but you make a valid point — let's proceed as you suggested

### noisysocks on 2022-05-25T06:35:20Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1136798742

OK, I've rebased this to include those PRs above which were merged. This PR now contains just the user-facing changes.

I recommend setting GitHub to only show the second commit in this PR (b87608b) with _Hide whitespace_ checked so that the diff is less noisy.

### noisysocks on 2022-05-25T07:16:24Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1136871915

Some of the e2e failures are legitimate, probably because some classnames have been removed, I'll take a look at them tomorrow 🙂

### noisysocks on 2022-05-26T05:52:53Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1138180173

OK, tests fixed! I want to keep the e2e changes minimal here so I did not switch away from using CSS selectors, even though that's not really ideal. It would be good to update `datepicker.test.js` to use Playwright / ARIA selectors in a future PR.

### noisysocks on 2022-05-27T02:05:11Z

URL: https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1139209956

Thanks everyone, big love ❤️

## Review threads

### Review thread 1 (resolved)

#### noisysocks on 2022-05-17T05:33:42Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r874383917
Path: packages/block-editor/src/components/publish-date-time-picker/index.js

```diff
@@ -0,0 +1,43 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	DateTimePicker,
+	__experimentalHStack as HStack,
+	__experimentalSpacer as Spacer,
+	Button,
+} from '@wordpress/components';
+import { close as closeIcon } from '@wordpress/icons';
+import { __ } from '@wordpress/i18n';
+
+export default function PublishDateTimePicker( {
+	onClose,
+	onChange,
+	...additionalProps
+} ) {
+	return (
+		<div className="block-editor-publish-date-time-picker">
+			{ /* TODO: This header is essentially the same as the one in <PostVisiblity />. DRY it up. */ }
```

I'll do this in a follow-up as it warrants some discussion. Also this PR is big enough 😀

### Review thread 2 (resolved)

#### noisysocks on 2022-05-17T05:34:50Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r874384455
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -2,30 +2,25 @@
  * External dependencies
  */
 import moment from 'moment';
-import classnames from 'classnames';
 import type { Moment } from 'moment';
 import { noop } from 'lodash';
-
-// `react-dates` doesn't tree-shake correctly, so we import from the individual
-// component here.
```

Oops, don't delete these two lines.

### Review thread 3 (resolved)

#### noisysocks on 2022-05-17T05:39:04Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r874386518
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -69,14 +64,13 @@ function DatePickerDay( { day, events = [] }: DatePickerDayProps ) {
 	}, [ events.length ] );
 
 	return (
-		<div
+		<Day
 			ref={ ref }
-			className={ classnames( 'components-datetime__date__day', {
-				'has-events': events?.length,
-			} ) }
+			className="components-datetime__date__day"
```

We're no longer using most of the class names but I've tried to keep them for backwards compatibility. I don't think this is a hard requirement, but maybe nice to do anyway.

#### ciampo on 2022-05-19T13:33:53Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877062581
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -69,14 +64,13 @@ function DatePickerDay( { day, events = [] }: DatePickerDayProps ) {
 	}, [ events.length ] );
 
 	return (
-		<div
+		<Day
 			ref={ ref }
-			className={ classnames( 'components-datetime__date__day', {
-				'has-events': events?.length,
-			} ) }
+			className="components-datetime__date__day"
```

Yes, we should keep all of those legacy classnames around for compat reasons. Maybe we should add a comment about that, so that it doesn't get considered "dead code" and gets removed in the future.

### Review thread 4 (resolved)

#### andrewserong on 2022-05-18T06:35:26Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r875517761
Path: packages/components/src/date-time/time/index.tsx

```diff
@@ -0,0 +1,309 @@
+/**
+ * External dependencies
+ */
+import moment from 'moment';
+import type { Moment } from 'moment';
+
+/**
+ * WordPress dependencies
+ */
+import { useState, useMemo, useEffect } from '@wordpress/element';
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import ButtonGroup from '../../button-group';
+import TimeZone from './timezone';
+import type { TimePickerProps } from '../types';
+import {
+	Wrapper,
+	Fieldset,
+	Legend,
+	HoursInput,
+	TimeSeparator,
+	MinutesInput,
+	MonthSelectWrapper,
+	MonthSelect,
+	DayInput,
+	YearInput,
+	TimeWrapper,
+} from './styles';
+import { HStack } from '../../h-stack';
+import { Spacer } from '../../spacer';
+import type { InputChangeCallback } from '../../input-control/types';
+
+const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+
+function from12hTo24h( hours: number, isPm: boolean ) {
+	return isPm ? ( ( hours % 12 ) + 12 ) % 24 : hours % 12;
+}
+
+/**
+ * TimePicker is a React component that renders a clock for time selection.
+ *
+ * ```jsx
+ * import { TimePicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyTimePicker = () => {
+ *   const [ time, setTime ] = useState( new Date() );
+ *
+ *   return (
+ *     <TimePicker
+ *       currentTime={ date }
+ *       onChange={ ( newTime ) => setTime( newTime ) }
+ *       is12Hour
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function TimePicker( {
+	is12Hour,
+	currentTime,
+	onChange,
+}: TimePickerProps ) {
+	const [ date, setDate ] = useState( () =>
+		// Truncate the date at the minutes, see: #15495.
+		currentTime ? moment( currentTime ).startOf( 'minutes' ) : moment()
+	);
+
+	// Reset the state when currentTime changed.
+	useEffect( () => {
+		setDate(
+			currentTime ? moment( currentTime ).startOf( 'minutes' ) : moment()
+		);
+	}, [ currentTime ] );
+
+	const { day, month, year, minutes, hours, am } = useMemo(
+		() => ( {
+			day: date.format( 'DD' ),
+			month: date.format( 'MM' ),
+			year: date.format( 'YYYY' ),
+			minutes: date.format( 'mm' ),
+			hours: date.format( is12Hour ? 'hh' : 'HH' ),
+			am: Number( date.format( 'H' ) ) <= 11 ? 'AM' : 'PM',
+		} ),
+		[ date, is12Hour ]
+	);
+
+	/**
+	 * Function that sets the date state and calls the onChange with a new date.
+	 * The date is truncated at the minutes.
+	 *
+	 * @param {Moment} newDate The date object.
+	 */
+	function changeDate( newDate: Moment ) {
+		setDate( newDate );
+		onChange?.( newDate.format( TIMEZONELESS_FORMAT ) );
+	}
+
+	const buildNumberControlChangeCallback = (
+		method: 'hours' | 'minutes' | 'date' | 'year'
+	) => {
+		const callback: InputChangeCallback = ( value, { event } ) => {
+			if ( ! ( event.target instanceof HTMLInputElement ) ) {
+				return;
+			}
+
+			if ( ! event.target.validity.valid ) {
+				return;
+			}
+
+			// We can safely assume value is a number if target is valid.
+			let numberValue = Number( value );
+
+			// If the 12-hour format is being used and the 'PM' period is
+			// selected, then the incoming value (which ranges 1-12) should be
+			// increased by 12 to match the expected 24-hour format.
+			if ( method === 'hours' && is12Hour ) {
+				numberValue = from12hTo24h( numberValue, am === 'PM' );
+			}
+
+			const newDate = date.clone()[ method ]( numberValue );
+			setDate( newDate );
+			onChange?.( newDate.format( TIMEZONELESS_FORMAT ) );
+		};
+		return callback;
+	};
+
+	function buildAmPmChangeCallback( value: 'AM' | 'PM' ) {
+		return () => {
+			if ( am === value ) {
+				return;
+			}
+
+			const parsedHours = parseInt( hours, 10 );
+
+			const newDate = date
+				.clone()
+				.hours( from12hTo24h( parsedHours, value === 'PM' ) );
+
+			changeDate( newDate );
+		};
+	}
+
+	const dayField = (
+		<DayInput
+			className="components-datetime__time-field-day-input"
+			label={ __( 'Day' ) }
+			hideLabelFromVision
+			__next36pxDefaultSize
+			value={ day }
+			step={ 1 }
+			min={ 1 }
+			max={ 31 }
+			hideHTMLArrows
+			isPressEnterToChange
+			isDragEnabled={ false }
```

Just wondering why we're disabling the drag feature? We don't currently have this behaviour on `trunk`, but I wondered if it'd make it quicker for Day and Time to be able to click and drag it up and down?

#### noisysocks on 2022-05-18T07:15:14Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r875546446
Path: packages/components/src/date-time/time/index.tsx

```diff
@@ -0,0 +1,309 @@
+/**
+ * External dependencies
+ */
+import moment from 'moment';
+import type { Moment } from 'moment';
+
+/**
+ * WordPress dependencies
+ */
+import { useState, useMemo, useEffect } from '@wordpress/element';
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import ButtonGroup from '../../button-group';
+import TimeZone from './timezone';
+import type { TimePickerProps } from '../types';
+import {
+	Wrapper,
+	Fieldset,
+	Legend,
+	HoursInput,
+	TimeSeparator,
+	MinutesInput,
+	MonthSelectWrapper,
+	MonthSelect,
+	DayInput,
+	YearInput,
+	TimeWrapper,
+} from './styles';
+import { HStack } from '../../h-stack';
+import { Spacer } from '../../spacer';
+import type { InputChangeCallback } from '../../input-control/types';
+
+const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+
+function from12hTo24h( hours: number, isPm: boolean ) {
+	return isPm ? ( ( hours % 12 ) + 12 ) % 24 : hours % 12;
+}
+
+/**
+ * TimePicker is a React component that renders a clock for time selection.
+ *
+ * ```jsx
+ * import { TimePicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyTimePicker = () => {
+ *   const [ time, setTime ] = useState( new Date() );
+ *
+ *   return (
+ *     <TimePicker
+ *       currentTime={ date }
+ *       onChange={ ( newTime ) => setTime( newTime ) }
+ *       is12Hour
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function TimePicker( {
+	is12Hour,
+	currentTime,
+	onChange,
+}: TimePickerProps ) {
+	const [ date, setDate ] = useState( () =>
+		// Truncate the date at the minutes, see: #15495.
+		currentTime ? moment( currentTime ).startOf( 'minutes' ) : moment()
+	);
+
+	// Reset the state when currentTime changed.
+	useEffect( () => {
+		setDate(
+			currentTime ? moment( currentTime ).startOf( 'minutes' ) : moment()
+		);
+	}, [ currentTime ] );
+
+	const { day, month, year, minutes, hours, am } = useMemo(
+		() => ( {
+			day: date.format( 'DD' ),
+			month: date.format( 'MM' ),
+			year: date.format( 'YYYY' ),
+			minutes: date.format( 'mm' ),
+			hours: date.format( is12Hour ? 'hh' : 'HH' ),
+			am: Number( date.format( 'H' ) ) <= 11 ? 'AM' : 'PM',
+		} ),
+		[ date, is12Hour ]
+	);
+
+	/**
+	 * Function that sets the date state and calls the onChange with a new date.
+	 * The date is truncated at the minutes.
+	 *
+	 * @param {Moment} newDate The date object.
+	 */
+	function changeDate( newDate: Moment ) {
+		setDate( newDate );
+		onChange?.( newDate.format( TIMEZONELESS_FORMAT ) );
+	}
+
+	const buildNumberControlChangeCallback = (
+		method: 'hours' | 'minutes' | 'date' | 'year'
+	) => {
+		const callback: InputChangeCallback = ( value, { event } ) => {
+			if ( ! ( event.target instanceof HTMLInputElement ) ) {
+				return;
+			}
+
+			if ( ! event.target.validity.valid ) {
+				return;
+			}
+
+			// We can safely assume value is a number if target is valid.
+			let numberValue = Number( value );
+
+			// If the 12-hour format is being used and the 'PM' period is
+			// selected, then the incoming value (which ranges 1-12) should be
+			// increased by 12 to match the expected 24-hour format.
+			if ( method === 'hours' && is12Hour ) {
+				numberValue = from12hTo24h( numberValue, am === 'PM' );
+			}
+
+			const newDate = date.clone()[ method ]( numberValue );
+			setDate( newDate );
+			onChange?.( newDate.format( TIMEZONELESS_FORMAT ) );
+		};
+		return callback;
+	};
+
+	function buildAmPmChangeCallback( value: 'AM' | 'PM' ) {
+		return () => {
+			if ( am === value ) {
+				return;
+			}
+
+			const parsedHours = parseInt( hours, 10 );
+
+			const newDate = date
+				.clone()
+				.hours( from12hTo24h( parsedHours, value === 'PM' ) );
+
+			changeDate( newDate );
+		};
+	}
+
+	const dayField = (
+		<DayInput
+			className="components-datetime__time-field-day-input"
+			label={ __( 'Day' ) }
+			hideLabelFromVision
+			__next36pxDefaultSize
+			value={ day }
+			step={ 1 }
+			min={ 1 }
+			max={ 31 }
+			hideHTMLArrows
+			isPressEnterToChange
+			isDragEnabled={ false }
```

It's not a great UX, unfortunately, because editing the post date (using the `editPost()` action) is fairly slow and re-rendering the `react-dates` calendar component is _very_ slow. The drag gesture ends up feeling really sluggish. We could spend some time optimising all of that but I don't really think it's worth the investment for now.

#### andrewserong on 2022-05-18T07:17:42Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r875548451
Path: packages/components/src/date-time/time/index.tsx

```diff
@@ -0,0 +1,309 @@
+/**
+ * External dependencies
+ */
+import moment from 'moment';
+import type { Moment } from 'moment';
+
+/**
+ * WordPress dependencies
+ */
+import { useState, useMemo, useEffect } from '@wordpress/element';
+import { __ } from '@wordpress/i18n';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import ButtonGroup from '../../button-group';
+import TimeZone from './timezone';
+import type { TimePickerProps } from '../types';
+import {
+	Wrapper,
+	Fieldset,
+	Legend,
+	HoursInput,
+	TimeSeparator,
+	MinutesInput,
+	MonthSelectWrapper,
+	MonthSelect,
+	DayInput,
+	YearInput,
+	TimeWrapper,
+} from './styles';
+import { HStack } from '../../h-stack';
+import { Spacer } from '../../spacer';
+import type { InputChangeCallback } from '../../input-control/types';
+
+const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
+
+function from12hTo24h( hours: number, isPm: boolean ) {
+	return isPm ? ( ( hours % 12 ) + 12 ) % 24 : hours % 12;
+}
+
+/**
+ * TimePicker is a React component that renders a clock for time selection.
+ *
+ * ```jsx
+ * import { TimePicker } from '@wordpress/components';
+ * import { useState } from '@wordpress/element';
+ *
+ * const MyTimePicker = () => {
+ *   const [ time, setTime ] = useState( new Date() );
+ *
+ *   return (
+ *     <TimePicker
+ *       currentTime={ date }
+ *       onChange={ ( newTime ) => setTime( newTime ) }
+ *       is12Hour
+ *     />
+ *   );
+ * };
+ * ```
+ */
+export function TimePicker( {
+	is12Hour,
+	currentTime,
+	onChange,
+}: TimePickerProps ) {
+	const [ date, setDate ] = useState( () =>
+		// Truncate the date at the minutes, see: #15495.
+		currentTime ? moment( currentTime ).startOf( 'minutes' ) : moment()
+	);
+
+	// Reset the state when currentTime changed.
+	useEffect( () => {
+		setDate(
+			currentTime ? moment( currentTime ).startOf( 'minutes' ) : moment()
+		);
+	}, [ currentTime ] );
+
+	const { day, month, year, minutes, hours, am } = useMemo(
+		() => ( {
+			day: date.format( 'DD' ),
+			month: date.format( 'MM' ),
+			year: date.format( 'YYYY' ),
+			minutes: date.format( 'mm' ),
+			hours: date.format( is12Hour ? 'hh' : 'HH' ),
+			am: Number( date.format( 'H' ) ) <= 11 ? 'AM' : 'PM',
+		} ),
+		[ date, is12Hour ]
+	);
+
+	/**
+	 * Function that sets the date state and calls the onChange with a new date.
+	 * The date is truncated at the minutes.
+	 *
+	 * @param {Moment} newDate The date object.
+	 */
+	function changeDate( newDate: Moment ) {
+		setDate( newDate );
+		onChange?.( newDate.format( TIMEZONELESS_FORMAT ) );
+	}
+
+	const buildNumberControlChangeCallback = (
+		method: 'hours' | 'minutes' | 'date' | 'year'
+	) => {
+		const callback: InputChangeCallback = ( value, { event } ) => {
+			if ( ! ( event.target instanceof HTMLInputElement ) ) {
+				return;
+			}
+
+			if ( ! event.target.validity.valid ) {
+				return;
+			}
+
+			// We can safely assume value is a number if target is valid.
+			let numberValue = Number( value );
+
+			// If the 12-hour format is being used and the 'PM' period is
+			// selected, then the incoming value (which ranges 1-12) should be
+			// increased by 12 to match the expected 24-hour format.
+			if ( method === 'hours' && is12Hour ) {
+				numberValue = from12hTo24h( numberValue, am === 'PM' );
+			}
+
+			const newDate = date.clone()[ method ]( numberValue );
+			setDate( newDate );
+			onChange?.( newDate.format( TIMEZONELESS_FORMAT ) );
+		};
+		return callback;
+	};
+
+	function buildAmPmChangeCallback( value: 'AM' | 'PM' ) {
+		return () => {
+			if ( am === value ) {
+				return;
+			}
+
+			const parsedHours = parseInt( hours, 10 );
+
+			const newDate = date
+				.clone()
+				.hours( from12hTo24h( parsedHours, value === 'PM' ) );
+
+			changeDate( newDate );
+		};
+	}
+
+	const dayField = (
+		<DayInput
+			className="components-datetime__time-field-day-input"
+			label={ __( 'Day' ) }
+			hideLabelFromVision
+			__next36pxDefaultSize
+			value={ day }
+			step={ 1 }
+			min={ 1 }
+			max={ 31 }
+			hideHTMLArrows
+			isPressEnterToChange
+			isDragEnabled={ false }
```

Good call... perhaps an exploration for an enhancement at some point after this lands!

### Review thread 5

#### andrewserong on 2022-05-19T01:55:39Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r876517209
Path: packages/components/src/number-control/index.js

```diff
@@ -43,9 +44,10 @@ export function NumberControl(
 	const baseValue = roundClamp( 0, min, max, baseStep );
 	const constrainValue = ( value, stepOverride ) => {
 		// When step is "any" clamp the value, otherwise round and clamp it.
-		return isStepAny
+		const number = isStepAny
 			? Math.min( max, Math.max( min, value ) )
 			: roundClamp( value, min, max, stepOverride ?? baseStep );
+		return number.toString().padStart( pad, '0' );
```

The `0` padding looks like a useful feature!

From a quick glance, it looks like this changes the implicit return type for `constrainValue` from a number to a string. Would it be worth splitting out this change (and the change to `SelectControl`) into a separate PR(s)? Since NumberControl is fairly widely used, and can receive a state reducer, it might be worth us pinging a few folks on the change in isolation to get a few more eyes on the potential implications (I've been tripped up trying to make changes to NumberControl before 😄).

#### andrewserong on 2022-05-19T02:05:45Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r876520829
Path: packages/components/src/number-control/index.js

```diff
@@ -43,9 +44,10 @@ export function NumberControl(
 	const baseValue = roundClamp( 0, min, max, baseStep );
 	const constrainValue = ( value, stepOverride ) => {
 		// When step is "any" clamp the value, otherwise round and clamp it.
-		return isStepAny
+		const number = isStepAny
 			? Math.min( max, Math.max( min, value ) )
 			: roundClamp( value, min, max, stepOverride ?? baseStep );
+		return number.toString().padStart( pad, '0' );
```

Now that I think of it, the conversion to `string` might not be such a problem (and could be desired). I see from @ciampo's earlier NumberControl PR to refactor to TypeScript (#38753) it proposes introducing an `ensureString` function that gets used in the reducer, so the change here might be useful for that, too.

#### noisysocks on 2022-05-19T02:12:54Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r876523475
Path: packages/components/src/number-control/index.js

```diff
@@ -43,9 +44,10 @@ export function NumberControl(
 	const baseValue = roundClamp( 0, min, max, baseStep );
 	const constrainValue = ( value, stepOverride ) => {
 		// When step is "any" clamp the value, otherwise round and clamp it.
-		return isStepAny
+		const number = isStepAny
 			? Math.min( max, Math.max( min, value ) )
 			: roundClamp( value, min, max, stepOverride ?? baseStep );
+		return number.toString().padStart( pad, '0' );
```

Sure, I can split this out!

Changing the return type to string is intentional. If you look at the typings for the reducer state in `SelectControl` you'll see that `value` is supposed to be a string.

#### andrewserong on 2022-05-19T02:22:18Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r876527000
Path: packages/components/src/number-control/index.js

```diff
@@ -43,9 +44,10 @@ export function NumberControl(
 	const baseValue = roundClamp( 0, min, max, baseStep );
 	const constrainValue = ( value, stepOverride ) => {
 		// When step is "any" clamp the value, otherwise round and clamp it.
-		return isStepAny
+		const number = isStepAny
 			? Math.min( max, Math.max( min, value ) )
 			: roundClamp( value, min, max, stepOverride ?? baseStep );
+		return number.toString().padStart( pad, '0' );
```

Great, thanks for confirming!

#### ciampo on 2022-05-19T14:37:33Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877149138
Path: packages/components/src/number-control/index.js

```diff
@@ -43,9 +44,10 @@ export function NumberControl(
 	const baseValue = roundClamp( 0, min, max, baseStep );
 	const constrainValue = ( value, stepOverride ) => {
 		// When step is "any" clamp the value, otherwise round and clamp it.
-		return isStepAny
+		const number = isStepAny
 			? Math.min( max, Math.max( min, value ) )
 			: roundClamp( value, min, max, stepOverride ?? baseStep );
+		return number.toString().padStart( pad, '0' );
```

I took `NumberControl` for a spin and played around with the new `pad` property, and found a few instances where it behaved unexpectedly (for example, the `0` padding is not added when typing a value and blurring the input field).

https://user-images.githubusercontent.com/1083581/169321627-1e57b64c-7a1c-4162-9826-4aa8bb83b847.mp4

Considering that `InputControl` (and consequently `NumberControl`) [is undergoing a few changes / fixes](https://github.com/WordPress/gutenberg/pull/40518), and that `NumberControl` isn't typed yet, my recommendation for now is to add this feature directly to `TimePicker` if possible (and consider porting it to `NumberControl` once those fixes/improvements are applied).

#### andrewserong on 2022-05-20T03:29:48Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877701178
Path: packages/components/src/number-control/index.js

```diff
@@ -43,9 +44,10 @@ export function NumberControl(
 	const baseValue = roundClamp( 0, min, max, baseStep );
 	const constrainValue = ( value, stepOverride ) => {
 		// When step is "any" clamp the value, otherwise round and clamp it.
-		return isStepAny
+		const number = isStepAny
 			? Math.min( max, Math.max( min, value ) )
 			: roundClamp( value, min, max, stepOverride ?? baseStep );
+		return number.toString().padStart( pad, '0' );
```

> my recommendation for now is to add this feature directly to TimePicker if possible (and consider porting it to `NumberControl` once those fixes/improvements are applied).

That sounds like a good idea to me, too.

### Review thread 6 (resolved)

#### ciampo on 2022-05-19T13:10:35Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877035943
Path: packages/block-editor/src/components/publish-date-time-picker/style.scss

```diff
@@ -0,0 +1,20 @@
+.block-editor-publish-date-time-picker__header {
+	margin-bottom: 1em;
+}
+
+.block-editor-publish-date-time-picker__heading {
+	font-size: $default-font-size;
+	margin: 0;
+}
+
+.block-editor-publish-date-time-picker__reset {
+	height: $icon-size;
+	margin: 0;
+	text-decoration: underline;
+}
+
+.block-editor-publish-date-time-picker__close.has-icon {
```

I wonder if there's a way to apply these styles that doesn't involve using the `has-icon` classname, which should be considered as an implementation detail of the `Button` component

#### noisysocks on 2022-05-20T00:38:47Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877637122
Path: packages/block-editor/src/components/publish-date-time-picker/style.scss

```diff
@@ -0,0 +1,20 @@
+.block-editor-publish-date-time-picker__header {
+	margin-bottom: 1em;
+}
+
+.block-editor-publish-date-time-picker__heading {
+	font-size: $default-font-size;
+	margin: 0;
+}
+
+.block-editor-publish-date-time-picker__reset {
+	height: $icon-size;
+	margin: 0;
+	text-decoration: underline;
+}
+
+.block-editor-publish-date-time-picker__close.has-icon {
```

Do you think the `&&&` hack would be better? It's just specificity that we need.

#### ciampo on 2022-05-20T11:57:18Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r878068893
Path: packages/block-editor/src/components/publish-date-time-picker/style.scss

```diff
@@ -0,0 +1,20 @@
+.block-editor-publish-date-time-picker__header {
+	margin-bottom: 1em;
+}
+
+.block-editor-publish-date-time-picker__heading {
+	font-size: $default-font-size;
+	margin: 0;
+}
+
+.block-editor-publish-date-time-picker__reset {
+	height: $icon-size;
+	margin: 0;
+	text-decoration: underline;
+}
+
+.block-editor-publish-date-time-picker__close.has-icon {
```

I think so — the `&&&` hack is more upfront about why is being used (specificity) and would allow us to avoid using hardcoded internal components classnames :)

#### noisysocks on 2022-05-23T07:18:50Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r879099469
Path: packages/block-editor/src/components/publish-date-time-picker/style.scss

```diff
@@ -0,0 +1,20 @@
+.block-editor-publish-date-time-picker__header {
+	margin-bottom: 1em;
+}
+
+.block-editor-publish-date-time-picker__heading {
+	font-size: $default-font-size;
+	margin: 0;
+}
+
+.block-editor-publish-date-time-picker__reset {
+	height: $icon-size;
+	margin: 0;
+	text-decoration: underline;
+}
+
+.block-editor-publish-date-time-picker__close.has-icon {
```

Hm, doesn't look like `sass` lets you do the `&&&` hack. I think how it is currently is the best we can do.

```
Error: "&" may only used at the beginning of a compound selector.
```

#### ciampo on 2022-05-23T07:33:04Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r879111281
Path: packages/block-editor/src/components/publish-date-time-picker/style.scss

```diff
@@ -0,0 +1,20 @@
+.block-editor-publish-date-time-picker__header {
+	margin-bottom: 1em;
+}
+
+.block-editor-publish-date-time-picker__heading {
+	font-size: $default-font-size;
+	margin: 0;
+}
+
+.block-editor-publish-date-time-picker__reset {
+	height: $icon-size;
+	margin: 0;
+	text-decoration: underline;
+}
+
+.block-editor-publish-date-time-picker__close.has-icon {
```

Off the top of my head, I remember being able to do something like that by using [interpolation](https://sass-lang.com/documentation/interpolation) — something like

```sass
  #{ & }#{ & }#{ & } {
    // styles
  }
```

although I'm not 100% sure

#### stokesman on 2022-05-23T15:35:24Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r879601519
Path: packages/block-editor/src/components/publish-date-time-picker/style.scss

```diff
@@ -0,0 +1,20 @@
+.block-editor-publish-date-time-picker__header {
+	margin-bottom: 1em;
+}
+
+.block-editor-publish-date-time-picker__heading {
+	font-size: $default-font-size;
+	margin: 0;
+}
+
+.block-editor-publish-date-time-picker__reset {
+	height: $icon-size;
+	margin: 0;
+	text-decoration: underline;
+}
+
+.block-editor-publish-date-time-picker__close.has-icon {
```

The hack I usually reach for is attribute selectors.
```suggestion
[class].block-editor-publish-date-time-picker__close {
```
You can repeat the same one as required for specificity but here you might only need the single.

### Review thread 7 (resolved)

#### ciampo on 2022-05-19T13:27:24Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877055256
Path: packages/components/src/select-control/types.ts

```diff
@@ -20,7 +20,7 @@ export interface SelectControlProps
 			| 'size'
 			| 'suffix'
 		>,
-		Pick< BaseControlProps, 'help' > {
+		Pick< BaseControlProps, 'help' | '__nextHasNoMarginBottom' > {
```

While we're slowly moving towards using Storybook as the main reference for props docs, we should still update the component's README for now — could we add an entry for the `__nextHasNoMarginBottom` prop? (we could copy the text from `BaseControl`'s README)

### Review thread 8 (resolved)

#### ciampo on 2022-05-19T13:29:44Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877057858
Path: packages/components/package.json

```diff
@@ -62,7 +62,7 @@
 		"highlight-words-core": "^1.2.2",
 		"lodash": "^4.17.21",
 		"memize": "^1.1.0",
-		"moment": "^2.22.1",
+		"moment": "^2.26.0",
```

Is there a particular reason why we're updating `moment`, or is it just to keep it up to date?

#### noisysocks on 2022-05-20T00:42:06Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877638259
Path: packages/components/package.json

```diff
@@ -62,7 +62,7 @@
 		"highlight-words-core": "^1.2.2",
 		"lodash": "^4.17.21",
 		"memize": "^1.1.0",
-		"moment": "^2.22.1",
+		"moment": "^2.26.0",
```

Yeah, this was a little dependency nightmare. I had to update `@types/react-dates` because some functions were missing from the older type definitions (`renderMonthElement`, I think) and then that newer version of the typings required `moment` to be updated because it expected to see more functions in the `moment.Moment` interface than what existed. It's all very silly since none of it actually makes a difference to the runtime code that's generated.

#### ciampo on 2022-05-20T11:57:44Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r878069195
Path: packages/components/package.json

```diff
@@ -62,7 +62,7 @@
 		"highlight-words-core": "^1.2.2",
 		"lodash": "^4.17.21",
 		"memize": "^1.1.0",
-		"moment": "^2.22.1",
+		"moment": "^2.26.0",
```

Got it, thank you!

### Review thread 9 (resolved)

#### ciampo on 2022-05-19T13:47:34Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877078627
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -2,30 +2,25 @@
  * External dependencies
  */
 import moment from 'moment';
-import classnames from 'classnames';
 import type { Moment } from 'moment';
 import { noop } from 'lodash';
-
 // `react-dates` doesn't tree-shake correctly, so we import from the individual
 // component here.
-// @ts-expect-error TypeScript won't find any type declarations at
-// `react-dates/lib/components/DayPickerSingleDateController` as they're located
-// at `react-dates`.
-import UntypedDayPickerSingleDateController from 'react-dates/lib/components/DayPickerSingleDateController';
-import type { DayPickerSingleDateController } from 'react-dates';
-const TypedDayPickerSingleDateController = UntypedDayPickerSingleDateController as DayPickerSingleDateController;
+import DayPickerSingleDateController from 'react-dates/lib/components/DayPickerSingleDateController';
```

Is this fixed by updating `@types/react-dates` to its latest version?

Neat!

#### noisysocks on 2022-05-20T00:42:42Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877638449
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -2,30 +2,25 @@
  * External dependencies
  */
 import moment from 'moment';
-import classnames from 'classnames';
 import type { Moment } from 'moment';
 import { noop } from 'lodash';
-
 // `react-dates` doesn't tree-shake correctly, so we import from the individual
 // component here.
-// @ts-expect-error TypeScript won't find any type declarations at
-// `react-dates/lib/components/DayPickerSingleDateController` as they're located
-// at `react-dates`.
-import UntypedDayPickerSingleDateController from 'react-dates/lib/components/DayPickerSingleDateController';
-import type { DayPickerSingleDateController } from 'react-dates';
-const TypedDayPickerSingleDateController = UntypedDayPickerSingleDateController as DayPickerSingleDateController;
+import DayPickerSingleDateController from 'react-dates/lib/components/DayPickerSingleDateController';
```

Yes! A happy accident.

### Review thread 10 (resolved)

#### ciampo on 2022-05-19T14:04:10Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877102112
Path: packages/components/src/date-time/style.scss

```diff
@@ -1,262 +1 @@
-@import "./datepicker";
-
-.components-datetime {
-	// This padding is leveraged when the component is used alone,
-	// usually inside popovers.
-	padding: $grid-unit-20;
-
-	// This rule removes the padding when used inside a panel.
-	.components-panel__body & {
-		padding: 0;
-	}
-
-	.components-datetime__calendar-help {
-		padding: $grid-unit-20;
-		min-width: 260px;
-
-		h4 {
-			margin: 0;
-		}
-	}
-
-	.components-datetime__buttons {
-		display: flex;
-		justify-content: space-between;
-	}
-
-	.components-datetime__date-help-toggle {
-		display: block;
-		margin-left: auto;
-	}
-
-	fieldset {
-		border: 0;
-		padding: 0;
-		margin: 0;
-	}
-
-	select,
-	input {
-		@include input-style__neutral();
-	}
-
-	// Override inherited conflicting styles to be consistent.
-	select,
-	input[type="number"],
-	.components-button {
-		height: 30px;
-		margin-top: 0;
-		margin-bottom: 0;
-	}
-
-	.components-button:focus {
-		z-index: z-index(".components-button {:focus or .is-primary}");
-	}
-}
-
-.components-datetime__date {
-	min-height: 236px;
-	border-top: 1px solid $gray-300;
-
-	// Override external DatePicker styles.
-	.DayPickerNavigation_leftButton__horizontalDefault {
-		/*!rtl:begin:ignore*/
-		left: 13px;
-		/*!rtl:end:ignore*/
-	}
-
-	.CalendarMonth_caption {
-		font-size: $default-font-size;
-	}
-
-	// Seperate borders so that border respect border radius
-	.CalendarMonth_table {
-		border-collapse: separate;
-		border-spacing: 2px;
-	}
-
-	.CalendarDay {
-		font-size: $default-font-size;
-		border: none;
-		border-radius: $radius-round;
-		text-align: center;
-
-		&:focus {
-			box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) var(--wp-admin-theme-color), inset 0 0 0 #{ $border-width-focus + $border-width } $white;
-			outline: 2px solid transparent; // Shown in Windows 10 high contrast mode.
-		}
-	}
-
-	.CalendarDay__selected {
-		background: var(--wp-admin-theme-color);
-		border: 2px solid transparent; // This indicates selection in Windows 10 high contrast mode.
-
-		&:hover {
-			background: var(--wp-admin-theme-color-darker-20);
-		}
-
-		&:focus {
-			box-shadow: inset 0 0 0 $border-width $white;
-		}
-	}
-
-	.DayPickerNavigation_button__horizontalDefault {
-		padding: 2px 8px;
-		top: 20px;
-
-		&:focus {
-			@include input-style__focus();
-		}
-	}
-
-	.DayPicker_weekHeader {
-		top: 50px;
-		.DayPicker_weekHeader_ul {
-			margin: 1px;
-			padding-left: 0;
-			padding-right: 0;
-		}
-	}
-
-	&.is-description-visible .DayPicker {
-		visibility: hidden;
-	}
-}
-
-.components-datetime__date .CalendarDay .components-datetime__date__day {
-	height: 100%;
-	display: flex;
-	justify-content: center;
-	align-content: center;
-	flex-direction: column;
-	position: relative;
-
-	&.has-events::before {
-		content: " ";
-		width: 4px;
-		height: 4px;
-		border-radius: 2px;
-		position: absolute;
-		left: 50%;
-		margin-left: -2px;
-		bottom: 0;
-		background-color: $white;
-	}
-}
-
-.components-datetime__date .CalendarDay:not(.CalendarDay__selected) .components-datetime__date__day.has-events::before {
-	background: var(--wp-admin-theme-color);
-}
-
-.components-datetime__time {
-	padding-bottom: $grid-unit-20;
-
-	fieldset {
-		position: relative;
-		margin-bottom: 0.5em;
-	}
-
-	fieldset + fieldset {
-		margin-bottom: 0;
-	}
-
-	.components-datetime__time-field-am-pm fieldset {
-		margin-top: 0;
-	}
-
-	.components-datetime__time-wrapper {
-		display: flex;
-
-		.components-datetime__time-separator {
-			display: inline-block;
-			padding: 0 3px 0 0;
-		}
-
-		.components-datetime__time-field {
-
-			&-time {
-				/*rtl:ignore*/
-				direction: ltr;
-			}
-
-			select {
-				margin-right: $grid-unit-05;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-			}
-
-			input[type="number"] {
-				padding: 2px;
-				margin-right: $grid-unit-05;
-				text-align: center;
-				-moz-appearance: textfield;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-
-				&::-webkit-inner-spin-button {
-					-webkit-appearance: none;
-					margin: 0;
-				}
-			}
-		}
-	}
-
-	// Makes the month appear before the day if time format uses AM/PM
-	// We are assuming MM-DD-YYY correlates with AM/PM
-	&.is-12-hour {
-		.components-datetime__time-field-day input {
-			margin: 0 -$grid-unit-05 0 0 !important;
-			border-radius: $radius-block-ui 0 0 $radius-block-ui !important;
-		}
-		.components-datetime__time-field-year input {
-			border-radius: 0 $radius-block-ui $radius-block-ui 0 !important;
-		}
-	}
-}
-
-.components-datetime__timezone {
-	line-height: 30px;
-	margin-left: $grid-unit-05;
-	text-decoration: underline dotted;
-}
-
-.components-datetime__time-legend {
-	font-weight: 600;
-	margin-top: 0.5em;
-
-	&.invisible {
-		position: absolute;
-		top: -999em;
-		left: -999em;
-	}
-}
-
-.components-datetime__time-field-integer-field {
-	font-family: inherit;
-}
-
-.components-datetime__time-field-hours-input,
-.components-datetime__time-field-minutes-input,
-.components-datetime__time-field-day-input {
-	width: 35px;
-}
-
-.components-datetime__time-field-year-input {
-	width: 55px;
-}
-
-.components-datetime__time-field-month-select {
-	max-width: 145px;
-}
-
-// Hack to center the datepicker component within the popover.
-// It sets its own styles so centering is tricky.
-.components-popover .components-datetime__date {
-	padding-left: $grid-unit-05;
-}
+@import "./date/style.scss";
```

To recap:

- `date-time/date/datepicker.scss` is basically a copy-paste of the original styles in `react-dates`, plus a few tweaks
- `date-time/date/style.scss` overrides/adds theming to those styles, using the classnames from `react-dates`
- `date-time/styles.scss` just imports the styles mentioned above
- `date-time/styles.ts` contains all remaining styles for the components written with Emotion


In case that's correct, to make things a bit more simple/clear we could:

- rename `date-time/date/datepicker.scss` to `date-time/date/lib.scss`
- move the code in `date-time/date/style.scss` to `date-time/styles.scss` (which would import `./date/lib.scss`)
- keep `date-time/styles.ts` as-is

Not a strong preference at all, though — WDYT?

#### noisysocks on 2022-05-20T00:50:44Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877641118
Path: packages/components/src/date-time/style.scss

```diff
@@ -1,262 +1 @@
-@import "./datepicker";
-
-.components-datetime {
-	// This padding is leveraged when the component is used alone,
-	// usually inside popovers.
-	padding: $grid-unit-20;
-
-	// This rule removes the padding when used inside a panel.
-	.components-panel__body & {
-		padding: 0;
-	}
-
-	.components-datetime__calendar-help {
-		padding: $grid-unit-20;
-		min-width: 260px;
-
-		h4 {
-			margin: 0;
-		}
-	}
-
-	.components-datetime__buttons {
-		display: flex;
-		justify-content: space-between;
-	}
-
-	.components-datetime__date-help-toggle {
-		display: block;
-		margin-left: auto;
-	}
-
-	fieldset {
-		border: 0;
-		padding: 0;
-		margin: 0;
-	}
-
-	select,
-	input {
-		@include input-style__neutral();
-	}
-
-	// Override inherited conflicting styles to be consistent.
-	select,
-	input[type="number"],
-	.components-button {
-		height: 30px;
-		margin-top: 0;
-		margin-bottom: 0;
-	}
-
-	.components-button:focus {
-		z-index: z-index(".components-button {:focus or .is-primary}");
-	}
-}
-
-.components-datetime__date {
-	min-height: 236px;
-	border-top: 1px solid $gray-300;
-
-	// Override external DatePicker styles.
-	.DayPickerNavigation_leftButton__horizontalDefault {
-		/*!rtl:begin:ignore*/
-		left: 13px;
-		/*!rtl:end:ignore*/
-	}
-
-	.CalendarMonth_caption {
-		font-size: $default-font-size;
-	}
-
-	// Seperate borders so that border respect border radius
-	.CalendarMonth_table {
-		border-collapse: separate;
-		border-spacing: 2px;
-	}
-
-	.CalendarDay {
-		font-size: $default-font-size;
-		border: none;
-		border-radius: $radius-round;
-		text-align: center;
-
-		&:focus {
-			box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) var(--wp-admin-theme-color), inset 0 0 0 #{ $border-width-focus + $border-width } $white;
-			outline: 2px solid transparent; // Shown in Windows 10 high contrast mode.
-		}
-	}
-
-	.CalendarDay__selected {
-		background: var(--wp-admin-theme-color);
-		border: 2px solid transparent; // This indicates selection in Windows 10 high contrast mode.
-
-		&:hover {
-			background: var(--wp-admin-theme-color-darker-20);
-		}
-
-		&:focus {
-			box-shadow: inset 0 0 0 $border-width $white;
-		}
-	}
-
-	.DayPickerNavigation_button__horizontalDefault {
-		padding: 2px 8px;
-		top: 20px;
-
-		&:focus {
-			@include input-style__focus();
-		}
-	}
-
-	.DayPicker_weekHeader {
-		top: 50px;
-		.DayPicker_weekHeader_ul {
-			margin: 1px;
-			padding-left: 0;
-			padding-right: 0;
-		}
-	}
-
-	&.is-description-visible .DayPicker {
-		visibility: hidden;
-	}
-}
-
-.components-datetime__date .CalendarDay .components-datetime__date__day {
-	height: 100%;
-	display: flex;
-	justify-content: center;
-	align-content: center;
-	flex-direction: column;
-	position: relative;
-
-	&.has-events::before {
-		content: " ";
-		width: 4px;
-		height: 4px;
-		border-radius: 2px;
-		position: absolute;
-		left: 50%;
-		margin-left: -2px;
-		bottom: 0;
-		background-color: $white;
-	}
-}
-
-.components-datetime__date .CalendarDay:not(.CalendarDay__selected) .components-datetime__date__day.has-events::before {
-	background: var(--wp-admin-theme-color);
-}
-
-.components-datetime__time {
-	padding-bottom: $grid-unit-20;
-
-	fieldset {
-		position: relative;
-		margin-bottom: 0.5em;
-	}
-
-	fieldset + fieldset {
-		margin-bottom: 0;
-	}
-
-	.components-datetime__time-field-am-pm fieldset {
-		margin-top: 0;
-	}
-
-	.components-datetime__time-wrapper {
-		display: flex;
-
-		.components-datetime__time-separator {
-			display: inline-block;
-			padding: 0 3px 0 0;
-		}
-
-		.components-datetime__time-field {
-
-			&-time {
-				/*rtl:ignore*/
-				direction: ltr;
-			}
-
-			select {
-				margin-right: $grid-unit-05;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-			}
-
-			input[type="number"] {
-				padding: 2px;
-				margin-right: $grid-unit-05;
-				text-align: center;
-				-moz-appearance: textfield;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-
-				&::-webkit-inner-spin-button {
-					-webkit-appearance: none;
-					margin: 0;
-				}
-			}
-		}
-	}
-
-	// Makes the month appear before the day if time format uses AM/PM
-	// We are assuming MM-DD-YYY correlates with AM/PM
-	&.is-12-hour {
-		.components-datetime__time-field-day input {
-			margin: 0 -$grid-unit-05 0 0 !important;
-			border-radius: $radius-block-ui 0 0 $radius-block-ui !important;
-		}
-		.components-datetime__time-field-year input {
-			border-radius: 0 $radius-block-ui $radius-block-ui 0 !important;
-		}
-	}
-}
-
-.components-datetime__timezone {
-	line-height: 30px;
-	margin-left: $grid-unit-05;
-	text-decoration: underline dotted;
-}
-
-.components-datetime__time-legend {
-	font-weight: 600;
-	margin-top: 0.5em;
-
-	&.invisible {
-		position: absolute;
-		top: -999em;
-		left: -999em;
-	}
-}
-
-.components-datetime__time-field-integer-field {
-	font-family: inherit;
-}
-
-.components-datetime__time-field-hours-input,
-.components-datetime__time-field-minutes-input,
-.components-datetime__time-field-day-input {
-	width: 35px;
-}
-
-.components-datetime__time-field-year-input {
-	width: 55px;
-}
-
-.components-datetime__time-field-month-select {
-	max-width: 145px;
-}
-
-// Hack to center the datepicker component within the popover.
-// It sets its own styles so centering is tricky.
-.components-popover .components-datetime__date {
-	padding-left: $grid-unit-05;
-}
+@import "./date/style.scss";
```

> * `date-time/date/datepicker.scss` is basically a copy-paste of the original styles in `react-dates`

Correct.

> * plus a few tweaks

I don't actually think there are any tweaks. I'm not really sure why we vendor the stylesheet instead of using the one that's assumedly in `node_modules`.

> * `date-time/styles.scss` just imports the styles mentioned above

Correct.

> * `date-time/styles.ts` contains all remaining styles for the components written with Emotion

The `styles.ts` files are all within the component subfolders, so it's `date-time/date/styles.ts`.

---

> * rename `date-time/date/datepicker.scss` to `date-time/date/lib.scss`

Sounds good. I'll first quickly check to see we can just use what's in `node_modules`.

> * move the code in `date-time/date/style.scss` to `date-time/styles.scss` (which would import `./date/lib.scss`)

No strong preference here. I suppose it's one less "go to definition" command you have to do when following the code, so yep sure!

#### ciampo on 2022-05-20T12:01:53Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r878072082
Path: packages/components/src/date-time/style.scss

```diff
@@ -1,262 +1 @@
-@import "./datepicker";
-
-.components-datetime {
-	// This padding is leveraged when the component is used alone,
-	// usually inside popovers.
-	padding: $grid-unit-20;
-
-	// This rule removes the padding when used inside a panel.
-	.components-panel__body & {
-		padding: 0;
-	}
-
-	.components-datetime__calendar-help {
-		padding: $grid-unit-20;
-		min-width: 260px;
-
-		h4 {
-			margin: 0;
-		}
-	}
-
-	.components-datetime__buttons {
-		display: flex;
-		justify-content: space-between;
-	}
-
-	.components-datetime__date-help-toggle {
-		display: block;
-		margin-left: auto;
-	}
-
-	fieldset {
-		border: 0;
-		padding: 0;
-		margin: 0;
-	}
-
-	select,
-	input {
-		@include input-style__neutral();
-	}
-
-	// Override inherited conflicting styles to be consistent.
-	select,
-	input[type="number"],
-	.components-button {
-		height: 30px;
-		margin-top: 0;
-		margin-bottom: 0;
-	}
-
-	.components-button:focus {
-		z-index: z-index(".components-button {:focus or .is-primary}");
-	}
-}
-
-.components-datetime__date {
-	min-height: 236px;
-	border-top: 1px solid $gray-300;
-
-	// Override external DatePicker styles.
-	.DayPickerNavigation_leftButton__horizontalDefault {
-		/*!rtl:begin:ignore*/
-		left: 13px;
-		/*!rtl:end:ignore*/
-	}
-
-	.CalendarMonth_caption {
-		font-size: $default-font-size;
-	}
-
-	// Seperate borders so that border respect border radius
-	.CalendarMonth_table {
-		border-collapse: separate;
-		border-spacing: 2px;
-	}
-
-	.CalendarDay {
-		font-size: $default-font-size;
-		border: none;
-		border-radius: $radius-round;
-		text-align: center;
-
-		&:focus {
-			box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) var(--wp-admin-theme-color), inset 0 0 0 #{ $border-width-focus + $border-width } $white;
-			outline: 2px solid transparent; // Shown in Windows 10 high contrast mode.
-		}
-	}
-
-	.CalendarDay__selected {
-		background: var(--wp-admin-theme-color);
-		border: 2px solid transparent; // This indicates selection in Windows 10 high contrast mode.
-
-		&:hover {
-			background: var(--wp-admin-theme-color-darker-20);
-		}
-
-		&:focus {
-			box-shadow: inset 0 0 0 $border-width $white;
-		}
-	}
-
-	.DayPickerNavigation_button__horizontalDefault {
-		padding: 2px 8px;
-		top: 20px;
-
-		&:focus {
-			@include input-style__focus();
-		}
-	}
-
-	.DayPicker_weekHeader {
-		top: 50px;
-		.DayPicker_weekHeader_ul {
-			margin: 1px;
-			padding-left: 0;
-			padding-right: 0;
-		}
-	}
-
-	&.is-description-visible .DayPicker {
-		visibility: hidden;
-	}
-}
-
-.components-datetime__date .CalendarDay .components-datetime__date__day {
-	height: 100%;
-	display: flex;
-	justify-content: center;
-	align-content: center;
-	flex-direction: column;
-	position: relative;
-
-	&.has-events::before {
-		content: " ";
-		width: 4px;
-		height: 4px;
-		border-radius: 2px;
-		position: absolute;
-		left: 50%;
-		margin-left: -2px;
-		bottom: 0;
-		background-color: $white;
-	}
-}
-
-.components-datetime__date .CalendarDay:not(.CalendarDay__selected) .components-datetime__date__day.has-events::before {
-	background: var(--wp-admin-theme-color);
-}
-
-.components-datetime__time {
-	padding-bottom: $grid-unit-20;
-
-	fieldset {
-		position: relative;
-		margin-bottom: 0.5em;
-	}
-
-	fieldset + fieldset {
-		margin-bottom: 0;
-	}
-
-	.components-datetime__time-field-am-pm fieldset {
-		margin-top: 0;
-	}
-
-	.components-datetime__time-wrapper {
-		display: flex;
-
-		.components-datetime__time-separator {
-			display: inline-block;
-			padding: 0 3px 0 0;
-		}
-
-		.components-datetime__time-field {
-
-			&-time {
-				/*rtl:ignore*/
-				direction: ltr;
-			}
-
-			select {
-				margin-right: $grid-unit-05;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-			}
-
-			input[type="number"] {
-				padding: 2px;
-				margin-right: $grid-unit-05;
-				text-align: center;
-				-moz-appearance: textfield;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-
-				&::-webkit-inner-spin-button {
-					-webkit-appearance: none;
-					margin: 0;
-				}
-			}
-		}
-	}
-
-	// Makes the month appear before the day if time format uses AM/PM
-	// We are assuming MM-DD-YYY correlates with AM/PM
-	&.is-12-hour {
-		.components-datetime__time-field-day input {
-			margin: 0 -$grid-unit-05 0 0 !important;
-			border-radius: $radius-block-ui 0 0 $radius-block-ui !important;
-		}
-		.components-datetime__time-field-year input {
-			border-radius: 0 $radius-block-ui $radius-block-ui 0 !important;
-		}
-	}
-}
-
-.components-datetime__timezone {
-	line-height: 30px;
-	margin-left: $grid-unit-05;
-	text-decoration: underline dotted;
-}
-
-.components-datetime__time-legend {
-	font-weight: 600;
-	margin-top: 0.5em;
-
-	&.invisible {
-		position: absolute;
-		top: -999em;
-		left: -999em;
-	}
-}
-
-.components-datetime__time-field-integer-field {
-	font-family: inherit;
-}
-
-.components-datetime__time-field-hours-input,
-.components-datetime__time-field-minutes-input,
-.components-datetime__time-field-day-input {
-	width: 35px;
-}
-
-.components-datetime__time-field-year-input {
-	width: 55px;
-}
-
-.components-datetime__time-field-month-select {
-	max-width: 145px;
-}
-
-// Hack to center the datepicker component within the popover.
-// It sets its own styles so centering is tricky.
-.components-popover .components-datetime__date {
-	padding-left: $grid-unit-05;
-}
+@import "./date/style.scss";
```

Your plan sounds good to me!

> I don't actually think there are any tweaks. I'm not really sure why we vendor the stylesheet instead of using the one that's assumedly in node_modules.

The reason why I mentioned that is because of this comment at the start of the file: https://github.com/WordPress/gutenberg/blob/2aadd5fcea1c15dd6768886d8af635de5780b362/packages/components/src/date-time/date/datepicker.scss#L2-L3

#### noisysocks on 2022-05-23T07:59:10Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r879134489
Path: packages/components/src/date-time/style.scss

```diff
@@ -1,262 +1 @@
-@import "./datepicker";
-
-.components-datetime {
-	// This padding is leveraged when the component is used alone,
-	// usually inside popovers.
-	padding: $grid-unit-20;
-
-	// This rule removes the padding when used inside a panel.
-	.components-panel__body & {
-		padding: 0;
-	}
-
-	.components-datetime__calendar-help {
-		padding: $grid-unit-20;
-		min-width: 260px;
-
-		h4 {
-			margin: 0;
-		}
-	}
-
-	.components-datetime__buttons {
-		display: flex;
-		justify-content: space-between;
-	}
-
-	.components-datetime__date-help-toggle {
-		display: block;
-		margin-left: auto;
-	}
-
-	fieldset {
-		border: 0;
-		padding: 0;
-		margin: 0;
-	}
-
-	select,
-	input {
-		@include input-style__neutral();
-	}
-
-	// Override inherited conflicting styles to be consistent.
-	select,
-	input[type="number"],
-	.components-button {
-		height: 30px;
-		margin-top: 0;
-		margin-bottom: 0;
-	}
-
-	.components-button:focus {
-		z-index: z-index(".components-button {:focus or .is-primary}");
-	}
-}
-
-.components-datetime__date {
-	min-height: 236px;
-	border-top: 1px solid $gray-300;
-
-	// Override external DatePicker styles.
-	.DayPickerNavigation_leftButton__horizontalDefault {
-		/*!rtl:begin:ignore*/
-		left: 13px;
-		/*!rtl:end:ignore*/
-	}
-
-	.CalendarMonth_caption {
-		font-size: $default-font-size;
-	}
-
-	// Seperate borders so that border respect border radius
-	.CalendarMonth_table {
-		border-collapse: separate;
-		border-spacing: 2px;
-	}
-
-	.CalendarDay {
-		font-size: $default-font-size;
-		border: none;
-		border-radius: $radius-round;
-		text-align: center;
-
-		&:focus {
-			box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) var(--wp-admin-theme-color), inset 0 0 0 #{ $border-width-focus + $border-width } $white;
-			outline: 2px solid transparent; // Shown in Windows 10 high contrast mode.
-		}
-	}
-
-	.CalendarDay__selected {
-		background: var(--wp-admin-theme-color);
-		border: 2px solid transparent; // This indicates selection in Windows 10 high contrast mode.
-
-		&:hover {
-			background: var(--wp-admin-theme-color-darker-20);
-		}
-
-		&:focus {
-			box-shadow: inset 0 0 0 $border-width $white;
-		}
-	}
-
-	.DayPickerNavigation_button__horizontalDefault {
-		padding: 2px 8px;
-		top: 20px;
-
-		&:focus {
-			@include input-style__focus();
-		}
-	}
-
-	.DayPicker_weekHeader {
-		top: 50px;
-		.DayPicker_weekHeader_ul {
-			margin: 1px;
-			padding-left: 0;
-			padding-right: 0;
-		}
-	}
-
-	&.is-description-visible .DayPicker {
-		visibility: hidden;
-	}
-}
-
-.components-datetime__date .CalendarDay .components-datetime__date__day {
-	height: 100%;
-	display: flex;
-	justify-content: center;
-	align-content: center;
-	flex-direction: column;
-	position: relative;
-
-	&.has-events::before {
-		content: " ";
-		width: 4px;
-		height: 4px;
-		border-radius: 2px;
-		position: absolute;
-		left: 50%;
-		margin-left: -2px;
-		bottom: 0;
-		background-color: $white;
-	}
-}
-
-.components-datetime__date .CalendarDay:not(.CalendarDay__selected) .components-datetime__date__day.has-events::before {
-	background: var(--wp-admin-theme-color);
-}
-
-.components-datetime__time {
-	padding-bottom: $grid-unit-20;
-
-	fieldset {
-		position: relative;
-		margin-bottom: 0.5em;
-	}
-
-	fieldset + fieldset {
-		margin-bottom: 0;
-	}
-
-	.components-datetime__time-field-am-pm fieldset {
-		margin-top: 0;
-	}
-
-	.components-datetime__time-wrapper {
-		display: flex;
-
-		.components-datetime__time-separator {
-			display: inline-block;
-			padding: 0 3px 0 0;
-		}
-
-		.components-datetime__time-field {
-
-			&-time {
-				/*rtl:ignore*/
-				direction: ltr;
-			}
-
-			select {
-				margin-right: $grid-unit-05;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-			}
-
-			input[type="number"] {
-				padding: 2px;
-				margin-right: $grid-unit-05;
-				text-align: center;
-				-moz-appearance: textfield;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-
-				&::-webkit-inner-spin-button {
-					-webkit-appearance: none;
-					margin: 0;
-				}
-			}
-		}
-	}
-
-	// Makes the month appear before the day if time format uses AM/PM
-	// We are assuming MM-DD-YYY correlates with AM/PM
-	&.is-12-hour {
-		.components-datetime__time-field-day input {
-			margin: 0 -$grid-unit-05 0 0 !important;
-			border-radius: $radius-block-ui 0 0 $radius-block-ui !important;
-		}
-		.components-datetime__time-field-year input {
-			border-radius: 0 $radius-block-ui $radius-block-ui 0 !important;
-		}
-	}
-}
-
-.components-datetime__timezone {
-	line-height: 30px;
-	margin-left: $grid-unit-05;
-	text-decoration: underline dotted;
-}
-
-.components-datetime__time-legend {
-	font-weight: 600;
-	margin-top: 0.5em;
-
-	&.invisible {
-		position: absolute;
-		top: -999em;
-		left: -999em;
-	}
-}
-
-.components-datetime__time-field-integer-field {
-	font-family: inherit;
-}
-
-.components-datetime__time-field-hours-input,
-.components-datetime__time-field-minutes-input,
-.components-datetime__time-field-day-input {
-	width: 35px;
-}
-
-.components-datetime__time-field-year-input {
-	width: 55px;
-}
-
-.components-datetime__time-field-month-select {
-	max-width: 145px;
-}
-
-// Hack to center the datepicker component within the popover.
-// It sets its own styles so centering is tricky.
-.components-popover .components-datetime__date {
-	padding-left: $grid-unit-05;
-}
+@import "./date/style.scss";
```

OK looked more into this and there's some history on why we have a vendored copy of `datepicker.scss`: https://github.com/WordPress/gutenberg/pull/26534. Basically it uses `darken()` for some reason and so needs to have the `.scss` extension. Vendoring it is the easiest way to do so.

I also am thinking it's still best to keep both `date-time/date/style.scss` and `date-time/style.scss` as it makes it more explicit that **only** `DatePicker` uses `react-dates`.

Note to self: Really ought to investigate using something other than `react-dates` (maybe `react-calendar`).

#### ciampo on 2022-05-23T08:07:33Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r879141902
Path: packages/components/src/date-time/style.scss

```diff
@@ -1,262 +1 @@
-@import "./datepicker";
-
-.components-datetime {
-	// This padding is leveraged when the component is used alone,
-	// usually inside popovers.
-	padding: $grid-unit-20;
-
-	// This rule removes the padding when used inside a panel.
-	.components-panel__body & {
-		padding: 0;
-	}
-
-	.components-datetime__calendar-help {
-		padding: $grid-unit-20;
-		min-width: 260px;
-
-		h4 {
-			margin: 0;
-		}
-	}
-
-	.components-datetime__buttons {
-		display: flex;
-		justify-content: space-between;
-	}
-
-	.components-datetime__date-help-toggle {
-		display: block;
-		margin-left: auto;
-	}
-
-	fieldset {
-		border: 0;
-		padding: 0;
-		margin: 0;
-	}
-
-	select,
-	input {
-		@include input-style__neutral();
-	}
-
-	// Override inherited conflicting styles to be consistent.
-	select,
-	input[type="number"],
-	.components-button {
-		height: 30px;
-		margin-top: 0;
-		margin-bottom: 0;
-	}
-
-	.components-button:focus {
-		z-index: z-index(".components-button {:focus or .is-primary}");
-	}
-}
-
-.components-datetime__date {
-	min-height: 236px;
-	border-top: 1px solid $gray-300;
-
-	// Override external DatePicker styles.
-	.DayPickerNavigation_leftButton__horizontalDefault {
-		/*!rtl:begin:ignore*/
-		left: 13px;
-		/*!rtl:end:ignore*/
-	}
-
-	.CalendarMonth_caption {
-		font-size: $default-font-size;
-	}
-
-	// Seperate borders so that border respect border radius
-	.CalendarMonth_table {
-		border-collapse: separate;
-		border-spacing: 2px;
-	}
-
-	.CalendarDay {
-		font-size: $default-font-size;
-		border: none;
-		border-radius: $radius-round;
-		text-align: center;
-
-		&:focus {
-			box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) var(--wp-admin-theme-color), inset 0 0 0 #{ $border-width-focus + $border-width } $white;
-			outline: 2px solid transparent; // Shown in Windows 10 high contrast mode.
-		}
-	}
-
-	.CalendarDay__selected {
-		background: var(--wp-admin-theme-color);
-		border: 2px solid transparent; // This indicates selection in Windows 10 high contrast mode.
-
-		&:hover {
-			background: var(--wp-admin-theme-color-darker-20);
-		}
-
-		&:focus {
-			box-shadow: inset 0 0 0 $border-width $white;
-		}
-	}
-
-	.DayPickerNavigation_button__horizontalDefault {
-		padding: 2px 8px;
-		top: 20px;
-
-		&:focus {
-			@include input-style__focus();
-		}
-	}
-
-	.DayPicker_weekHeader {
-		top: 50px;
-		.DayPicker_weekHeader_ul {
-			margin: 1px;
-			padding-left: 0;
-			padding-right: 0;
-		}
-	}
-
-	&.is-description-visible .DayPicker {
-		visibility: hidden;
-	}
-}
-
-.components-datetime__date .CalendarDay .components-datetime__date__day {
-	height: 100%;
-	display: flex;
-	justify-content: center;
-	align-content: center;
-	flex-direction: column;
-	position: relative;
-
-	&.has-events::before {
-		content: " ";
-		width: 4px;
-		height: 4px;
-		border-radius: 2px;
-		position: absolute;
-		left: 50%;
-		margin-left: -2px;
-		bottom: 0;
-		background-color: $white;
-	}
-}
-
-.components-datetime__date .CalendarDay:not(.CalendarDay__selected) .components-datetime__date__day.has-events::before {
-	background: var(--wp-admin-theme-color);
-}
-
-.components-datetime__time {
-	padding-bottom: $grid-unit-20;
-
-	fieldset {
-		position: relative;
-		margin-bottom: 0.5em;
-	}
-
-	fieldset + fieldset {
-		margin-bottom: 0;
-	}
-
-	.components-datetime__time-field-am-pm fieldset {
-		margin-top: 0;
-	}
-
-	.components-datetime__time-wrapper {
-		display: flex;
-
-		.components-datetime__time-separator {
-			display: inline-block;
-			padding: 0 3px 0 0;
-		}
-
-		.components-datetime__time-field {
-
-			&-time {
-				/*rtl:ignore*/
-				direction: ltr;
-			}
-
-			select {
-				margin-right: $grid-unit-05;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-			}
-
-			input[type="number"] {
-				padding: 2px;
-				margin-right: $grid-unit-05;
-				text-align: center;
-				-moz-appearance: textfield;
-
-				&:focus {
-					position: relative;
-					z-index: 1;
-				}
-
-				&::-webkit-inner-spin-button {
-					-webkit-appearance: none;
-					margin: 0;
-				}
-			}
-		}
-	}
-
-	// Makes the month appear before the day if time format uses AM/PM
-	// We are assuming MM-DD-YYY correlates with AM/PM
-	&.is-12-hour {
-		.components-datetime__time-field-day input {
-			margin: 0 -$grid-unit-05 0 0 !important;
-			border-radius: $radius-block-ui 0 0 $radius-block-ui !important;
-		}
-		.components-datetime__time-field-year input {
-			border-radius: 0 $radius-block-ui $radius-block-ui 0 !important;
-		}
-	}
-}
-
-.components-datetime__timezone {
-	line-height: 30px;
-	margin-left: $grid-unit-05;
-	text-decoration: underline dotted;
-}
-
-.components-datetime__time-legend {
-	font-weight: 600;
-	margin-top: 0.5em;
-
-	&.invisible {
-		position: absolute;
-		top: -999em;
-		left: -999em;
-	}
-}
-
-.components-datetime__time-field-integer-field {
-	font-family: inherit;
-}
-
-.components-datetime__time-field-hours-input,
-.components-datetime__time-field-minutes-input,
-.components-datetime__time-field-day-input {
-	width: 35px;
-}
-
-.components-datetime__time-field-year-input {
-	width: 55px;
-}
-
-.components-datetime__time-field-month-select {
-	max-width: 145px;
-}
-
-// Hack to center the datepicker component within the popover.
-// It sets its own styles so centering is tricky.
-.components-popover .components-datetime__date {
-	padding-left: $grid-unit-05;
-}
+@import "./date/style.scss";
```

Thank you for the context!

> Really ought to investigate using something other than `react-dates`

That would be great!

I personally would also love to find an alternative to `moment`, mostly to save on bundle size

### Review thread 11

#### ciampo on 2022-05-19T14:08:48Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877108779
Path: packages/components/src/date-time/time/styles.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../../utils';
+import { space } from '../../ui/utils/space';
+import {
+	Input,
+	BackdropUI,
+} from '../../input-control/styles/input-control-styles';
+import NumberControl from '../../number-control';
+import SelectControl from '../../select-control';
+
+export const Wrapper = styled.div``;
```

@sarayourfriend pardon the ping, but I kind of lost track on whether we have a strong preference for Styled components vs classnames + `cx` approach

#### noisysocks on 2022-05-20T00:51:39Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877641420
Path: packages/components/src/date-time/time/styles.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../../utils';
+import { space } from '../../ui/utils/space';
+import {
+	Input,
+	BackdropUI,
+} from '../../input-control/styles/input-control-styles';
+import NumberControl from '../../number-control';
+import SelectControl from '../../select-control';
+
+export const Wrapper = styled.div``;
```

We have both patterns in the codebase so I was confused about this too. I landed on using `styled()` because I find the `useCx` / `cx` statements to be kind of distracting when reading the component's code. Not a strong opinion though!

### Review thread 12 (resolved)

#### ciampo on 2022-05-19T14:40:50Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877154330
Path: packages/components/src/number-control/test/index.js

```diff
@@ -91,7 +91,7 @@ describe( 'NumberControl', () => {
 			await waitFor( () => {
 				expect( spy ).toHaveBeenCalledTimes( 2 );
 				expect( spy ).toHaveBeenNthCalledWith( 1, '1' );
-				expect( spy ).toHaveBeenNthCalledWith( 2, 4 );
+				expect( spy ).toHaveBeenNthCalledWith( 2, '4' );
```

The fact that we had to update a few of the expected values from numbers to strings also in `NumberControl`'s unit tests makes me want to stay on the side of caution here and delay these changes until we have some more clarity on how `NumberControl` handles its `value` internally (number vs string)

#### noisysocks on 2022-05-20T00:56:47Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877643252
Path: packages/components/src/number-control/test/index.js

```diff
@@ -91,7 +91,7 @@ describe( 'NumberControl', () => {
 			await waitFor( () => {
 				expect( spy ).toHaveBeenCalledTimes( 2 );
 				expect( spy ).toHaveBeenNthCalledWith( 1, '1' );
-				expect( spy ).toHaveBeenNthCalledWith( 2, 4 );
+				expect( spy ).toHaveBeenNthCalledWith( 2, '4' );
```

Yep ok, I'll try to keep the changes in `TimePicker` for now.

I do think this is a mistake in `NumberControl`, though, and that `value` should always be a string. You can see from these tests that currently sometimes `onChange` is called with a number and sometimes with a string which is bananas! 🍌 It will come up when `NumberControl` is converted to TypeScript because we'll see errors about coercing `NumberControl`'s `value: number | string` to `InputControl`'s `value: string`. `NumberControl` is supposed to be a glorified `input[type=number]` so I think it makes sense that value should work the same as it does in `HTMLInputElement`.

#### ciampo on 2022-05-20T12:06:08Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r878074986
Path: packages/components/src/number-control/test/index.js

```diff
@@ -91,7 +91,7 @@ describe( 'NumberControl', () => {
 			await waitFor( () => {
 				expect( spy ).toHaveBeenCalledTimes( 2 );
 				expect( spy ).toHaveBeenNthCalledWith( 1, '1' );
-				expect( spy ).toHaveBeenNthCalledWith( 2, 4 );
+				expect( spy ).toHaveBeenNthCalledWith( 2, '4' );
```

I had already one attempt at typing `NumberControl`, and I had to give up mostly because of the ambiguity around which type should be used for `value` (`string`, like `HTMLInputElement`, or `number`, since it's an input field dealing with numbers?). We decided to punt that refactor forward, and instead refactor to TypeScript its dependent components: `UnitControl` (done) and `RangeControl` (WIP).

Once `RangeControl` is typed, I'll attempt the refactor once again, and I'll make sure to involve you and the rest of the folks helping this effort!

### Review thread 13 (resolved)

#### ciampo on 2022-05-19T14:42:22Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877158385
Path: packages/components/src/date-time/time/test/index.tsx

```diff
@@ -1,15 +1,20 @@
 /**
  * External dependencies
  */
-import { render, fireEvent, screen } from '@testing-library/react';
+import { render, screen } from '@testing-library/react';
+import userEvent from '@testing-library/user-event';
 
 /**
  * Internal dependencies
  */
-import TimePicker from '../time';
+import TimePicker from '..';
 
 describe( 'TimePicker', () => {
-	it( 'should call onChange with updated date values', () => {
+	it( 'should call onChange with updated date values', async () => {
+		const user = userEvent.setup( {
```

Great to see `user-event` being used instead of `fireEvent`!

### Review thread 14 (resolved)

#### ciampo on 2022-05-19T14:43:30Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877159805
Path: packages/components/src/date-time/time/test/index.tsx

```diff
@@ -89,13 +106,18 @@ describe( 'TimePicker', () => {
 
 		const hoursInput = screen.getByLabelText( 'Hours' );
 
-		fireEvent.change( hoursInput, { target: { value: '22' } } );
-		fireEvent.blur( hoursInput );
+		await user.clear( hoursInput );
+		await user.type( hoursInput, '22' );
+		await user.keyboard( '{Enter}' );
 
-		expect( onChangeSpy ).not.toHaveBeenCalled();
+		expect( onChangeSpy ).toHaveBeenCalledWith( '1986-10-18T00:00:00' );
```

This change of behavior in the tests is interesting — any ideas why it happened?

I wonder whether it is a good change or if we should instead treat it as a regression?

#### noisysocks on 2022-05-20T01:03:36Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877645439
Path: packages/components/src/date-time/time/test/index.tsx

```diff
@@ -89,13 +106,18 @@ describe( 'TimePicker', () => {
 
 		const hoursInput = screen.getByLabelText( 'Hours' );
 
-		fireEvent.change( hoursInput, { target: { value: '22' } } );
-		fireEvent.blur( hoursInput );
+		await user.clear( hoursInput );
+		await user.type( hoursInput, '22' );
+		await user.keyboard( '{Enter}' );
 
-		expect( onChangeSpy ).not.toHaveBeenCalled();
+		expect( onChangeSpy ).toHaveBeenCalledWith( '1986-10-18T00:00:00' );
```

Yeah, so previously `TimePicker` rolled its own solution using regular `<input type="number">` elements and was written so that `onChange` would not be called if e.g. `value > max`. We're now using `NumberControl` though which is written so that `onChange` is called with `max` if `value > max`.

I don't really think either behaviour is better or worse, just that it should be consistent across components. I think letting `NumberControl` dictate what happens in this case is the correct approach as that's the point of using standardised components... to make these decisions for us!

#### ciampo on 2022-05-20T12:07:06Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r878075912
Path: packages/components/src/date-time/time/test/index.tsx

```diff
@@ -89,13 +106,18 @@ describe( 'TimePicker', () => {
 
 		const hoursInput = screen.getByLabelText( 'Hours' );
 
-		fireEvent.change( hoursInput, { target: { value: '22' } } );
-		fireEvent.blur( hoursInput );
+		await user.clear( hoursInput );
+		await user.type( hoursInput, '22' );
+		await user.keyboard( '{Enter}' );
 
-		expect( onChangeSpy ).not.toHaveBeenCalled();
+		expect( onChangeSpy ).toHaveBeenCalledWith( '1986-10-18T00:00:00' );
```

Sounds good, I agree with your points!

### Review thread 15 (resolved)

#### ciampo on 2022-05-19T14:43:52Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877160229
Path: packages/components/src/date-time/time/test/index.tsx

```diff
@@ -127,13 +154,18 @@ describe( 'TimePicker', () => {
 
 		const minutesInput = screen.getByLabelText( 'Minutes' );
 
-		fireEvent.change( minutesInput, { target: { value: '99' } } );
-		fireEvent.blur( minutesInput );
+		await user.clear( minutesInput );
+		await user.type( minutesInput, '99' );
+		await user.keyboard( '{Enter}' );
 
-		expect( onChangeSpy ).not.toHaveBeenCalled();
+		expect( onChangeSpy ).toHaveBeenCalledWith( '1986-10-18T11:59:00' );
```

Same as previous comment

### Review thread 16 (resolved)

#### ciampo on 2022-05-19T14:45:21Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877162045
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -0,0 +1,58 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import { COLORS } from '../../utils';
+
+// Styles that overrides the calendar styling provided by react-dates go in
+// style.scss. Everything else goes here.
+
+export const Day = styled.div< { hasEvents: boolean } >`
+	align-content: center;
+	display: flex;
```

Out of curiosity, have you considered using the `Flex` (or `VStack`) components instead?

#### noisysocks on 2022-05-20T04:11:18Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r877714751
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -0,0 +1,58 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import { COLORS } from '../../utils';
+
+// Styles that overrides the calendar styling provided by react-dates go in
+// style.scss. Everything else goes here.
+
+export const Day = styled.div< { hasEvents: boolean } >`
+	align-content: center;
+	display: flex;
```

It did not occur to me! I've swapped it out for `VStack`. This stuff is pretty new to me... do you think there's a benefit one way or the other? On the one hand it's nice that by using `display: flex` all the styling is together in `styles.ts`, but on the other hand it's nice that by using `VStack` we can remove a few lines of code.

#### ciampo on 2022-05-20T12:10:16Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r878078213
Path: packages/components/src/date-time/date/styles.ts

```diff
@@ -0,0 +1,58 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import Button from '../../button';
+import { COLORS } from '../../utils';
+
+// Styles that overrides the calendar styling provided by react-dates go in
+// style.scss. Everything else goes here.
+
+export const Day = styled.div< { hasEvents: boolean } >`
+	align-content: center;
+	display: flex;
```

The reason why I'm suggesting `VStack` is mostly because I believe that by being the first users of our own components, we can have more chances at improving the library and at promoting components composition

### Review thread 17 (resolved)

#### ciampo on 2022-05-25T08:24:48Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r881367023
Path: packages/components/src/date-time/date-time/index.tsx

```diff
@@ -31,6 +32,8 @@ function UnforwardedDateTimePicker(
 		onMonthPreviewed = noop,
 		onChange,
 		events,
+		__nextRemoveHelpButton = false,
+		__nextRemoveResetButton = false,
```

We should probably add a deprecation warning for both props ([example](https://github.com/WordPress/gutenberg/blob/6eff9a514591f3b14f667e94b745eaaa507d912a/packages/components/src/card/card/hook.js#L26-L29))

### Review thread 18

#### ciampo on 2022-05-25T08:26:09Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r881368449
Path: packages/components/src/date-time/date-time/index.tsx

```diff
@@ -1,9 +1,6 @@
 /**
  * External dependencies
  */
-// Needed to initialise the default datepicker styles.
-// See: https://github.com/airbnb/react-dates#initialize
-import 'react-dates/initialize';
```

For ease of review, noting that this line has been moved to `packages/components/src/date-time/date/index.tsx`

### Review thread 19 (resolved)

#### ciampo on 2022-05-25T08:40:56Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r881384451
Path: packages/components/src/date-time/time/styles.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../../utils';
+import { space } from '../../ui/utils/space';
+import {
+	Input,
+	BackdropUI,
+} from '../../input-control/styles/input-control-styles';
+import NumberControl from '../../number-control';
+import SelectControl from '../../select-control';
+
+export const Wrapper = styled.div``;
+
+export const Fieldset = styled.fieldset`
+	border: 0;
+	margin: 0 0 ${ space( 2 * 2 ) } 0;
+	padding: 0;
+
+	&:last-child {
+		margin-bottom: 0;
+	}
+`;
+
+export const Legend = styled.legend`
+	margin-bottom: ${ space( 2 ) };
+	padding: 0;
+`;
+
+export const TimeWrapper = styled.div`
+	direction: ltr;
+	display: flex;
+`;
+
+const baseInput = css`
+	&&& ${ Input } {
+		padding-left: ${ space( 2 ) };
+		padding-right: ${ space( 2 ) };
+		text-align: center;
+	}
+`;
+
+export const HoursInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-right: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-right: 0;
+		border-top-right-radius: 0;
+		border-bottom-right-radius: 0;
+	}
+`;
+
+export const TimeSeparator = styled.span`
+	border-top: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	border-bottom: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	line-height: calc(
+		${ CONFIG.controlHeight } - ${ CONFIG.borderWidth } * 2
+	);
+	display: inline-block;
+`;
+
+export const MinutesInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-left: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-left: 0;
+		border-top-left-radius: 0;
+		border-bottom-left-radius: 0;
+	}
+`;
+
+// Ideally we wouldn't need a wrapper, but can't otherwise target the
+// <BaseControl> in <SelectControl>
+export const MonthSelectWrapper = styled.div`
+	flex-grow: 1;
+`;
+
+export const MonthSelect = styled( SelectControl )`
+	height: 36px;
+
+	// Ideally would target the component instead of a classname.
+	.components-select-control__input {
+		line-height: 30px;
+	}
```

Have you tried using the `Select` styled component from `SelectControl` as the selector here? (excuses for the "selection" of terms used).

Something like

```diff
diff --git a/packages/components/src/date-time/time/styles.ts b/packages/components/src/date-time/time/styles.ts
index 7f8ff90de4..567f32c0c5 100644
--- a/packages/components/src/date-time/time/styles.ts
+++ b/packages/components/src/date-time/time/styles.ts
@@ -15,6 +15,7 @@ import {
 } from '../../input-control/styles/input-control-styles';
 import NumberControl from '../../number-control';
 import SelectControl from '../../select-control';
+import { Select } from '../../select-control/styles/select-control-styles';
 
 export const Wrapper = styled.div``;
 
@@ -96,8 +97,7 @@ export const MonthSelectWrapper = styled.div`
 export const MonthSelect = styled( SelectControl )`
 	height: 36px;
 
-	// Ideally would target the component instead of a classname.
-	.components-select-control__input {
+	${ Select } {
 		line-height: 30px;
 	}
 `;
```

(Haven't tested on my local machine though)

#### noisysocks on 2022-05-26T01:09:34Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r882233309
Path: packages/components/src/date-time/time/styles.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../../utils';
+import { space } from '../../ui/utils/space';
+import {
+	Input,
+	BackdropUI,
+} from '../../input-control/styles/input-control-styles';
+import NumberControl from '../../number-control';
+import SelectControl from '../../select-control';
+
+export const Wrapper = styled.div``;
+
+export const Fieldset = styled.fieldset`
+	border: 0;
+	margin: 0 0 ${ space( 2 * 2 ) } 0;
+	padding: 0;
+
+	&:last-child {
+		margin-bottom: 0;
+	}
+`;
+
+export const Legend = styled.legend`
+	margin-bottom: ${ space( 2 ) };
+	padding: 0;
+`;
+
+export const TimeWrapper = styled.div`
+	direction: ltr;
+	display: flex;
+`;
+
+const baseInput = css`
+	&&& ${ Input } {
+		padding-left: ${ space( 2 ) };
+		padding-right: ${ space( 2 ) };
+		text-align: center;
+	}
+`;
+
+export const HoursInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-right: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-right: 0;
+		border-top-right-radius: 0;
+		border-bottom-right-radius: 0;
+	}
+`;
+
+export const TimeSeparator = styled.span`
+	border-top: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	border-bottom: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	line-height: calc(
+		${ CONFIG.controlHeight } - ${ CONFIG.borderWidth } * 2
+	);
+	display: inline-block;
+`;
+
+export const MinutesInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-left: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-left: 0;
+		border-top-left-radius: 0;
+		border-bottom-left-radius: 0;
+	}
+`;
+
+// Ideally we wouldn't need a wrapper, but can't otherwise target the
+// <BaseControl> in <SelectControl>
+export const MonthSelectWrapper = styled.div`
+	flex-grow: 1;
+`;
+
+export const MonthSelect = styled( SelectControl )`
+	height: 36px;
+
+	// Ideally would target the component instead of a classname.
+	.components-select-control__input {
+		line-height: 30px;
+	}
```

That works, thanks! (Not sure why I didn't do this in the first place 😛)

### Review thread 20 (resolved)

#### ciampo on 2022-05-25T08:42:41Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r881386349
Path: packages/components/src/date-time/time/styles.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../../utils';
+import { space } from '../../ui/utils/space';
+import {
+	Input,
+	BackdropUI,
+} from '../../input-control/styles/input-control-styles';
+import NumberControl from '../../number-control';
+import SelectControl from '../../select-control';
+
+export const Wrapper = styled.div``;
+
+export const Fieldset = styled.fieldset`
+	border: 0;
+	margin: 0 0 ${ space( 2 * 2 ) } 0;
+	padding: 0;
+
+	&:last-child {
+		margin-bottom: 0;
+	}
+`;
+
+export const Legend = styled.legend`
+	margin-bottom: ${ space( 2 ) };
+	padding: 0;
+`;
+
+export const TimeWrapper = styled.div`
+	direction: ltr;
+	display: flex;
+`;
+
+const baseInput = css`
+	&&& ${ Input } {
+		padding-left: ${ space( 2 ) };
+		padding-right: ${ space( 2 ) };
+		text-align: center;
+	}
+`;
+
+export const HoursInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-right: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-right: 0;
+		border-top-right-radius: 0;
+		border-bottom-right-radius: 0;
+	}
+`;
+
+export const TimeSeparator = styled.span`
+	border-top: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	border-bottom: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	line-height: calc(
+		${ CONFIG.controlHeight } - ${ CONFIG.borderWidth } * 2
+	);
+	display: inline-block;
+`;
+
+export const MinutesInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-left: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-left: 0;
+		border-top-left-radius: 0;
+		border-bottom-left-radius: 0;
+	}
+`;
+
+// Ideally we wouldn't need a wrapper, but can't otherwise target the
+// <BaseControl> in <SelectControl>
+export const MonthSelectWrapper = styled.div`
+	flex-grow: 1;
+`;
+
+export const MonthSelect = styled( SelectControl )`
+	height: 36px;
+
+	// Ideally would target the component instead of a classname.
+	.components-select-control__input {
+		line-height: 30px;
+	}
+`;
+
+export const DayInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+`;
+
+export const YearInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 55px;
```

Are these numbers "pixel perfect" with the reference design? From what I remember, we usually try to stick to multiple of 4 (and use the `space` utility function)

#### noisysocks on 2022-05-26T00:38:21Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r882222718
Path: packages/components/src/date-time/time/styles.ts

```diff
@@ -0,0 +1,119 @@
+/**
+ * External dependencies
+ */
+import styled from '@emotion/styled';
+import { css } from '@emotion/react';
+
+/**
+ * Internal dependencies
+ */
+import { COLORS, CONFIG } from '../../utils';
+import { space } from '../../ui/utils/space';
+import {
+	Input,
+	BackdropUI,
+} from '../../input-control/styles/input-control-styles';
+import NumberControl from '../../number-control';
+import SelectControl from '../../select-control';
+
+export const Wrapper = styled.div``;
+
+export const Fieldset = styled.fieldset`
+	border: 0;
+	margin: 0 0 ${ space( 2 * 2 ) } 0;
+	padding: 0;
+
+	&:last-child {
+		margin-bottom: 0;
+	}
+`;
+
+export const Legend = styled.legend`
+	margin-bottom: ${ space( 2 ) };
+	padding: 0;
+`;
+
+export const TimeWrapper = styled.div`
+	direction: ltr;
+	display: flex;
+`;
+
+const baseInput = css`
+	&&& ${ Input } {
+		padding-left: ${ space( 2 ) };
+		padding-right: ${ space( 2 ) };
+		text-align: center;
+	}
+`;
+
+export const HoursInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-right: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-right: 0;
+		border-top-right-radius: 0;
+		border-bottom-right-radius: 0;
+	}
+`;
+
+export const TimeSeparator = styled.span`
+	border-top: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	border-bottom: ${ CONFIG.borderWidth } solid ${ COLORS.gray[ 700 ] };
+	line-height: calc(
+		${ CONFIG.controlHeight } - ${ CONFIG.borderWidth } * 2
+	);
+	display: inline-block;
+`;
+
+export const MinutesInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+
+	&&& ${ Input } {
+		padding-left: 0;
+	}
+
+	&&& ${ BackdropUI } {
+		border-left: 0;
+		border-top-left-radius: 0;
+		border-bottom-left-radius: 0;
+	}
+`;
+
+// Ideally we wouldn't need a wrapper, but can't otherwise target the
+// <BaseControl> in <SelectControl>
+export const MonthSelectWrapper = styled.div`
+	flex-grow: 1;
+`;
+
+export const MonthSelect = styled( SelectControl )`
+	height: 36px;
+
+	// Ideally would target the component instead of a classname.
+	.components-select-control__input {
+		line-height: 30px;
+	}
+`;
+
+export const DayInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 35px;
+`;
+
+export const YearInput = styled( NumberControl )`
+	${ baseInput }
+
+	width: 55px;
```

I think I copied them from from the old design. I'll look at using `space()`—good idea.

### Review thread 21 (resolved)

#### ciampo on 2022-05-25T08:47:26Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r881391359
Path: packages/editor/src/components/post-schedule/index.js

```diff
@@ -75,20 +74,14 @@ export default function PostSchedule() {
 			.join( '' ) // Reverse the string and test for "a" not followed by a slash.
 	);
 
-	function onChange( newDate ) {
-		onUpdateDate( newDate );
-		const { ownerDocument } = ref.current;
-		ownerDocument.activeElement.blur();
```

Is the "blur" functionality being removed on purpose? (apologies in case I missed it)

#### noisysocks on 2022-05-26T00:39:04Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r882223002
Path: packages/editor/src/components/post-schedule/index.js

```diff
@@ -75,20 +74,14 @@ export default function PostSchedule() {
 			.join( '' ) // Reverse the string and test for "a" not followed by a slash.
 	);
 
-	function onChange( newDate ) {
-		onUpdateDate( newDate );
-		const { ownerDocument } = ref.current;
-		ownerDocument.activeElement.blur();
```

Yes this was in response to an earlier bit of feedback—see https://github.com/WordPress/gutenberg/pull/41097#issuecomment-1129646917.

#### ciampo on 2022-05-26T21:29:42Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883098044
Path: packages/editor/src/components/post-schedule/index.js

```diff
@@ -75,20 +74,14 @@ export default function PostSchedule() {
 			.join( '' ) // Reverse the string and test for "a" not followed by a slash.
 	);
 
-	function onChange( newDate ) {
-		onUpdateDate( newDate );
-		const { ownerDocument } = ref.current;
-		ownerDocument.activeElement.blur();
```

Makes sense, thank you!

### Review thread 22

#### ciampo on 2022-05-26T21:33:31Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883100170
Path: packages/components/src/date-time/date-time/index.tsx

```diff
@@ -37,6 +38,23 @@ function UnforwardedDateTimePicker(
 	}: DateTimePickerProps,
 	ref: ForwardedRef< any >
 ) {
+	if ( ! __nextRemoveHelpButton ) {
+		deprecated( 'Help button in wp.components.DateTimePicker', {
+			since: '13.4',
+			version: '14.6', // Six months of plugin releases.
+			hint:
+				'Set the `__nextRemoveHelpButton` prop to `true` to remove this warning and opt in to the new behaviour, which will become the default in a future version.',
+		} );
+	}
+	if ( ! __nextRemoveResetButton ) {
+		deprecated( 'Reset button in wp.components.DateTimePicker', {
+			since: '13.4',
+			version: '14.6', // Six months of plugin releases.
```

Interesting. I believe that I always used the WordPress version (and not the Gutenberg plugin version) when working on components. Would be interesting to see which approach is the expected one!

#### noisysocks on 2022-05-26T23:30:02Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883155058
Path: packages/components/src/date-time/date-time/index.tsx

```diff
@@ -37,6 +38,23 @@ function UnforwardedDateTimePicker(
 	}: DateTimePickerProps,
 	ref: ForwardedRef< any >
 ) {
+	if ( ! __nextRemoveHelpButton ) {
+		deprecated( 'Help button in wp.components.DateTimePicker', {
+			since: '13.4',
+			version: '14.6', // Six months of plugin releases.
+			hint:
+				'Set the `__nextRemoveHelpButton` prop to `true` to remove this warning and opt in to the new behaviour, which will become the default in a future version.',
+		} );
+	}
+	if ( ! __nextRemoveResetButton ) {
+		deprecated( 'Reset button in wp.components.DateTimePicker', {
+			since: '13.4',
+			version: '14.6', // Six months of plugin releases.
```

I've always seen and used the Gutenberg version... it's an interesting question because the code ends up both in the plugin and Core... hmmm! 🤔

#### andrewserong on 2022-05-27T00:59:18Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883193435
Path: packages/components/src/date-time/date-time/index.tsx

```diff
@@ -37,6 +38,23 @@ function UnforwardedDateTimePicker(
 	}: DateTimePickerProps,
 	ref: ForwardedRef< any >
 ) {
+	if ( ! __nextRemoveHelpButton ) {
+		deprecated( 'Help button in wp.components.DateTimePicker', {
+			since: '13.4',
+			version: '14.6', // Six months of plugin releases.
+			hint:
+				'Set the `__nextRemoveHelpButton` prop to `true` to remove this warning and opt in to the new behaviour, which will become the default in a future version.',
+		} );
+	}
+	if ( ! __nextRemoveResetButton ) {
+		deprecated( 'Reset button in wp.components.DateTimePicker', {
+			since: '13.4',
+			version: '14.6', // Six months of plugin releases.
```

At least the version numbers in Gutenberg are _way_ ahead of core, so it's easy to tell them apart 😁

### Review thread 23

#### ciampo on 2022-05-26T21:36:40Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883101872
Path: packages/e2e-tests/specs/editor/various/datepicker.test.js

```diff
@@ -15,12 +15,24 @@ async function getSelectedOptionLabel( selector ) {
 }
 
 async function getDatePickerValues() {
-	const year = await getInputValue( '[aria-label="Year"]' );
-	const month = await getInputValue( '[aria-label="Month"]' );
-	const monthLabel = await getSelectedOptionLabel( '[aria-label="Month"]' );
-	const day = await getInputValue( '[aria-label="Day"]' );
-	const hours = await getInputValue( '[aria-label="Hours"]' );
-	const minutes = await getInputValue( '[aria-label="Minutes"]' );
```

Just wondering if it wouldn't be a good thing to add these `aria-label`s in our code (unless those inputs are already labelled in an accessible way)

#### noisysocks on 2022-05-26T23:31:54Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883155782
Path: packages/e2e-tests/specs/editor/various/datepicker.test.js

```diff
@@ -15,12 +15,24 @@ async function getSelectedOptionLabel( selector ) {
 }
 
 async function getDatePickerValues() {
-	const year = await getInputValue( '[aria-label="Year"]' );
-	const month = await getInputValue( '[aria-label="Month"]' );
-	const monthLabel = await getSelectedOptionLabel( '[aria-label="Month"]' );
-	const day = await getInputValue( '[aria-label="Day"]' );
-	const hours = await getInputValue( '[aria-label="Hours"]' );
-	const minutes = await getInputValue( '[aria-label="Minutes"]' );
```

The inputs are labelled, but it's done using `<label for="">` which you can't target using a CSS selector. I thought about using `puppeteer-testing-library` here so that we can do this properly but I think it's too great of a change for this PR.

#### ciampo on 2022-05-27T07:49:16Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883359701
Path: packages/e2e-tests/specs/editor/various/datepicker.test.js

```diff
@@ -15,12 +15,24 @@ async function getSelectedOptionLabel( selector ) {
 }
 
 async function getDatePickerValues() {
-	const year = await getInputValue( '[aria-label="Year"]' );
-	const month = await getInputValue( '[aria-label="Month"]' );
-	const monthLabel = await getSelectedOptionLabel( '[aria-label="Month"]' );
-	const day = await getInputValue( '[aria-label="Day"]' );
-	const hours = await getInputValue( '[aria-label="Hours"]' );
-	const minutes = await getInputValue( '[aria-label="Minutes"]' );
```

Agreed!

### Review thread 24 (resolved)

#### andrewserong on 2022-05-27T00:56:07Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883192448
Path: packages/block-editor/src/components/publish-date-time-picker/README.md

```diff
@@ -0,0 +1,54 @@
+# `PublishDateTimePicker`
+
+`<PublishDateTimePicker />` is a component used to select the date and time that
+a post will be published. It wraps the `<DateTimePicker />` component found in
+`@wordpress/components` and adds additional post-specific controls.
+
+See [the documentation for
+DateTimePicker](https://github.com/WordPress/gutenberg/tree/trunk/packages/components/src/date-time)
```

Tiny nit (and feel free to ignore): I think we can use an absolute path to the readme file to get links in the handbook to be directed to other pages within the handbook, rather than redirecting to the Gutenberg repo? E.g. links like the following: https://github.com/WordPress/gutenberg/blob/b964485122fa05ca305d544946ade1d3c1eeabc4/packages/components/CONTRIBUTING.md#L428

Ideally, for someone browsing in the handbook they'd ultimately be directed to: https://developer.wordpress.org/block-editor/reference-guides/components/date-time/

#### noisysocks on 2022-05-27T01:15:43Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883198717
Path: packages/block-editor/src/components/publish-date-time-picker/README.md

```diff
@@ -0,0 +1,54 @@
+# `PublishDateTimePicker`
+
+`<PublishDateTimePicker />` is a component used to select the date and time that
+a post will be published. It wraps the `<DateTimePicker />` component found in
+`@wordpress/components` and adds additional post-specific controls.
+
+See [the documentation for
+DateTimePicker](https://github.com/WordPress/gutenberg/tree/trunk/packages/components/src/date-time)
```

Cool! I'll do that.

### Review thread 25 (resolved)

#### ramonjd on 2022-05-27T01:02:14Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883194304
Path: packages/block-editor/src/components/publish-date-time-picker/index.js

```diff
@@ -0,0 +1,50 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	DateTimePicker,
+	__experimentalHStack as HStack,
+	__experimentalSpacer as Spacer,
+	Button,
+} from '@wordpress/components';
+import { closeSmall } from '@wordpress/icons';
+import { __ } from '@wordpress/i18n';
+import { forwardRef } from '@wordpress/element';
+
+function PublishDateTimePicker(
+	{ onClose, onChange, ...additionalProps },
+	ref
+) {
+	return (
+		<div ref={ ref } className="block-editor-publish-date-time-picker">
+			{ /* TODO: This header is essentially the same as the one in <PostVisiblity />. DRY it up. */ }
+			<HStack className="block-editor-publish-date-time-picker__header">
+				<h2 className="block-editor-publish-date-time-picker__heading">
+					{ __( 'Publish' ) }
+				</h2>
+				<Spacer />
+				<Button
+					className="block-editor-publish-date-time-picker__reset"
+					variant="tertiary"
+					onClick={ () => onChange?.( null ) }
```

Is there a reason why `onChange` is treated as optional but `onClose` not?

No biggie, just asking.

#### andrewserong on 2022-05-27T01:25:23Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883201887
Path: packages/block-editor/src/components/publish-date-time-picker/index.js

```diff
@@ -0,0 +1,50 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	DateTimePicker,
+	__experimentalHStack as HStack,
+	__experimentalSpacer as Spacer,
+	Button,
+} from '@wordpress/components';
+import { closeSmall } from '@wordpress/icons';
+import { __ } from '@wordpress/i18n';
+import { forwardRef } from '@wordpress/element';
+
+function PublishDateTimePicker(
+	{ onClose, onChange, ...additionalProps },
+	ref
+) {
+	return (
+		<div ref={ ref } className="block-editor-publish-date-time-picker">
+			{ /* TODO: This header is essentially the same as the one in <PostVisiblity />. DRY it up. */ }
+			<HStack className="block-editor-publish-date-time-picker__header">
+				<h2 className="block-editor-publish-date-time-picker__heading">
+					{ __( 'Publish' ) }
+				</h2>
+				<Spacer />
+				<Button
+					className="block-editor-publish-date-time-picker__reset"
+					variant="tertiary"
+					onClick={ () => onChange?.( null ) }
```

From my reading of it, it looks like `onClose` is passed as a reference to the Button's `onClick`, so it can happily be `undefined`, whereas in this handler, `onChange` is being called, so it needs optional chaining in case it's `undefined`? I could be wrong, though!

#### noisysocks on 2022-05-27T01:30:21Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883203634
Path: packages/block-editor/src/components/publish-date-time-picker/index.js

```diff
@@ -0,0 +1,50 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	DateTimePicker,
+	__experimentalHStack as HStack,
+	__experimentalSpacer as Spacer,
+	Button,
+} from '@wordpress/components';
+import { closeSmall } from '@wordpress/icons';
+import { __ } from '@wordpress/i18n';
+import { forwardRef } from '@wordpress/element';
+
+function PublishDateTimePicker(
+	{ onClose, onChange, ...additionalProps },
+	ref
+) {
+	return (
+		<div ref={ ref } className="block-editor-publish-date-time-picker">
+			{ /* TODO: This header is essentially the same as the one in <PostVisiblity />. DRY it up. */ }
+			<HStack className="block-editor-publish-date-time-picker__header">
+				<h2 className="block-editor-publish-date-time-picker__heading">
+					{ __( 'Publish' ) }
+				</h2>
+				<Spacer />
+				<Button
+					className="block-editor-publish-date-time-picker__reset"
+					variant="tertiary"
+					onClick={ () => onChange?.( null ) }
```

With `onChange` I'm just matching the behaviour of `DateTimePicker` which has it as optional.

You're right that `onClose` should probably also be optional for consistency. 

It just so happens that the next PR I have lined up (https://github.com/WordPress/gutenberg/pull/41362) makes `onClose` optional so let's avoid re-triggering GitHub Actions yet again and wait for that.

#### ramonjd on 2022-05-27T01:42:38Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883207700
Path: packages/block-editor/src/components/publish-date-time-picker/index.js

```diff
@@ -0,0 +1,50 @@
+/**
+ * WordPress dependencies
+ */
+import {
+	DateTimePicker,
+	__experimentalHStack as HStack,
+	__experimentalSpacer as Spacer,
+	Button,
+} from '@wordpress/components';
+import { closeSmall } from '@wordpress/icons';
+import { __ } from '@wordpress/i18n';
+import { forwardRef } from '@wordpress/element';
+
+function PublishDateTimePicker(
+	{ onClose, onChange, ...additionalProps },
+	ref
+) {
+	return (
+		<div ref={ ref } className="block-editor-publish-date-time-picker">
+			{ /* TODO: This header is essentially the same as the one in <PostVisiblity />. DRY it up. */ }
+			<HStack className="block-editor-publish-date-time-picker__header">
+				<h2 className="block-editor-publish-date-time-picker__heading">
+					{ __( 'Publish' ) }
+				</h2>
+				<Spacer />
+				<Button
+					className="block-editor-publish-date-time-picker__reset"
+					variant="tertiary"
+					onClick={ () => onChange?.( null ) }
```

Thanks for the explanation!

### Review thread 26

#### ramonjd on 2022-05-27T01:40:25Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883206911
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -15,12 +16,14 @@ import DayPickerSingleDateController from 'react-dates/lib/components/DayPickerS
  */
 import { useEffect, useRef } from '@wordpress/element';
 import { isRTL, _n, sprintf } from '@wordpress/i18n';
+import { arrowLeft, arrowRight } from '@wordpress/icons';
 
 /**
  * Internal dependencies
  */
 import { getMomentDate } from './utils';
-import type { DatePickerDayProps, DatePickerProps } from './types';
+import type { DatePickerDayProps, DatePickerProps } from '../types';
+import { Day, NavPrevButton, NavNextButton } from './styles';
 
 const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
 const ARIAL_LABEL_TIME_FORMAT = 'dddd, LL';
```

Not necessary for this PR (it's in trunk) I noticed that the aria-label on the day component outputs symbols.

<img width="1119" alt="Screen Shot 2022-05-27 at 11 17 15 am" src="https://user-images.githubusercontent.com/6458278/170611316-f0f499da-9e82-4916-8589-c285781077c7.png">


Not sure if the problem is in `moment()`. It's something to do with the locale aware format  `LL` 

This works

```js
const ARIAL_LABEL_TIME_FORMAT = 'dddd, MMMM DD, YYYY'; // Monday, May 09, 2022
```

But it's not locale aware I presume so it might need some further 👀

#### noisysocks on 2022-05-27T01:44:35Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883208339
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -15,12 +16,14 @@ import DayPickerSingleDateController from 'react-dates/lib/components/DayPickerS
  */
 import { useEffect, useRef } from '@wordpress/element';
 import { isRTL, _n, sprintf } from '@wordpress/i18n';
+import { arrowLeft, arrowRight } from '@wordpress/icons';
 
 /**
  * Internal dependencies
  */
 import { getMomentDate } from './utils';
-import type { DatePickerDayProps, DatePickerProps } from './types';
+import type { DatePickerDayProps, DatePickerProps } from '../types';
+import { Day, NavPrevButton, NavNextButton } from './styles';
 
 const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
 const ARIAL_LABEL_TIME_FORMAT = 'dddd, LL';
```

Oh interesting, good catch. We probably should use our own `dateFormat` function that's in `@wordpress/date` here:

https://github.com/WordPress/gutenberg/blob/ef6c2fe106788c7efec627606b3de892974a0d9d/packages/components/src/date-time/date.tsx#L44

I'll turn your comment into an issue.

#### noisysocks on 2022-05-27T01:45:57Z

URL: https://github.com/WordPress/gutenberg/pull/41097#discussion_r883208762
Path: packages/components/src/date-time/date/index.tsx

```diff
@@ -15,12 +16,14 @@ import DayPickerSingleDateController from 'react-dates/lib/components/DayPickerS
  */
 import { useEffect, useRef } from '@wordpress/element';
 import { isRTL, _n, sprintf } from '@wordpress/i18n';
+import { arrowLeft, arrowRight } from '@wordpress/icons';
 
 /**
  * Internal dependencies
  */
 import { getMomentDate } from './utils';
-import type { DatePickerDayProps, DatePickerProps } from './types';
+import type { DatePickerDayProps, DatePickerProps } from '../types';
+import { Day, NavPrevButton, NavNextButton } from './styles';
 
 const TIMEZONELESS_FORMAT = 'YYYY-MM-DDTHH:mm:ss';
 const ARIAL_LABEL_TIME_FORMAT = 'dddd, LL';
```

https://github.com/WordPress/gutenberg/issues/41384

## Changed files

- packages/block-editor/src/components/index.js (MODIFIED, +1/-0)
- packages/block-editor/src/components/publish-date-time-picker/README.md (ADDED, +52/-0)
- packages/block-editor/src/components/publish-date-time-picker/index.js (ADDED, +50/-0)
- packages/block-editor/src/components/publish-date-time-picker/style.scss (ADDED, +20/-0)
- packages/block-editor/src/style.scss (MODIFIED, +1/-0)
- packages/block-library/src/post-date/edit.js (MODIFIED, +4/-3)
- packages/components/CHANGELOG.md (MODIFIED, +2/-0)
- packages/components/src/date-time/README.md (MODIFIED, +16/-0)
- packages/components/src/date-time/date-time/index.tsx (ADDED, +217/-0)
- packages/components/src/date-time/date-time/styles.ts (ADDED, +8/-0)
- packages/components/src/date-time/date/datepicker.scss (RENAMED, +0/-0)
- packages/components/src/date-time/date/index.tsx (RENAMED, +35/-8)
- packages/components/src/date-time/date/style.scss (ADDED, +75/-0)
- packages/components/src/date-time/date/styles.ts (ADDED, +55/-0)
- packages/components/src/date-time/date/test/index.tsx (RENAMED, +1/-1)
- packages/components/src/date-time/date/test/utils.ts (RENAMED, +0/-0)
- packages/components/src/date-time/date/utils.ts (RENAMED, +0/-0)
- packages/components/src/date-time/index.ts (ADDED, +9/-0)
- packages/components/src/date-time/index.tsx (DELETED, +0/-196)
- packages/components/src/date-time/stories/date-time.tsx (RENAMED, +1/-1)
- packages/components/src/date-time/style.scss (MODIFIED, +1/-262)
- packages/components/src/date-time/time.tsx (DELETED, +0/-321)
- packages/components/src/date-time/time/index.tsx (ADDED, +356/-0)
- packages/components/src/date-time/time/styles.ts (ADDED, +119/-0)
- packages/components/src/date-time/time/test/index.tsx (RENAMED, +5/-5)
- packages/components/src/date-time/time/timezone.tsx (RENAMED, +5/-2)
- packages/components/src/date-time/types.ts (MODIFIED, +16/-18)
- packages/e2e-tests/specs/editor/various/datepicker.test.js (MODIFIED, +19/-7)
- packages/e2e-tests/specs/editor/various/post-visibility.test.js (MODIFIED, +1/-1)
- packages/e2e-tests/specs/editor/various/scheduling.test.js (MODIFIED, +2/-2)
- packages/e2e-tests/specs/editor/various/switch-to-draft.test.js (MODIFIED, +1/-1)
- packages/edit-post/src/components/sidebar/post-schedule/index.js (MODIFIED, +3/-1)
- packages/edit-post/src/components/sidebar/post-schedule/style.scss (MODIFIED, +1/-3)
- packages/editor/src/components/post-schedule/index.js (MODIFIED, +6/-13)
