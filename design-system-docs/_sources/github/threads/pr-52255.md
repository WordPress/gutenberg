---
number: 52255
type: pull_request
state: MERGED
merged_at: 2023-09-04T14:46:52Z
url: https://github.com/WordPress/gutenberg/pull/52255
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Enhancement","[Focus] Accessibility (a11y)","[Package] Components","First-time Contributor","has dev note"]
---

# PR #52255: Making Circular Option Picker a `listbox`

- URL: https://github.com/WordPress/gutenberg/pull/52255
- Author: andrewhayward
- Created: 2023-07-03T16:07:33Z
- Updated: 2023-10-12T20:36:25Z
- Comments: 5 of 5
- Review threads: 17 of 17
- Changed files: 24 of 24
- Diff: ../pr-diffs/52255.diff

## Body

## What?
As per #35292, This patch modifies [`CircularOptionPicker`](https://github.com/WordPress/gutenberg/tree/trunk/packages/components/src/circular-option-picker) to become a [`listbox`](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/listbox_role), but using [the `Composite` package](https://github.com/WordPress/gutenberg/blob/trunk/packages/components/src/composite/index.js). Additionally, it updates the various components that use `CircularOptionPicker`.

## Why?
Currently, keyboard interaction with `CircularOptionPicker`s is difficult, as each option presents as an individual tab stop. By changing the component to behave as a `listbox`, the entire control becomes a single tab stop, with individual colour options accessed using arrow keys.

## How?
The `CircularOptionPicker` has been partially rebuilt using the various `Composite` elements, which reduces any complex behavioural additions on our part.

## Testing Instructions
Nothing should visually change, as the underlying base components are still being used.

### Testing Instructions for Keyboard
Every use of the `CircularOptionPicker` ([`ColorPalette`](https://github.com/WordPress/gutenberg/tree/trunk/packages/components/src/color-palette), for example) should now present as a single tab stop, with options being picked using arrow keys.

## Additional Notes
 - ~~Initial values aren't currently picked up correctly.~~

## ✍️ Dev note

To improve `CircularOptionPicker`'s semantics and keyboard navigation, the component has been tweaked to render and behave as a `listbox` by default. This change also causes the component to become a single tab stop, with the individual color options accessed using arrow keys.

In the (few) instances in which it makes sense for `CircularOptionPicker` to still render as a list of individual buttons,  consumers of the component can use the `asButtons` prop to switch back to the legacy behavior.

## Issue comments

### github-actions on 2023-07-03T16:07:58Z

URL: https://github.com/WordPress/gutenberg/pull/52255#issuecomment-1618795906

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @andrewhayward! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/), where we hold [regularly weekly meetings](https://make.wordpress.org/core/tag/core-editor-summary/) open to anyone to coordinate with each other.

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### andrewhayward on 2023-08-29T16:39:51Z

URL: https://github.com/WordPress/gutenberg/pull/52255#issuecomment-1697796959

@alexstine @joedolson could you give this a quick look, if you have a chance, and give me any thoughts/feedback? Thanks.

### alexstine on 2023-08-29T18:25:40Z

URL: https://github.com/WordPress/gutenberg/pull/52255#issuecomment-1697931902

@andrewhayward Thanks for the ping. This is testing well for me in Chrome and Firefox.

### ciampo on 2023-09-04T14:42:44Z

URL: https://github.com/WordPress/gutenberg/pull/52255#issuecomment-1705385824

Noticed https://github.com/WordPress/gutenberg/issues/54156 while testing, @andrewhayward this is also another good follow-up task

### ciampo on 2023-10-12T20:35:51Z

URL: https://github.com/WordPress/gutenberg/pull/52255#issuecomment-1760330629

Added a dev note in the PR description

## Review threads

### Review thread 1 (resolved)

#### mirka on 2023-07-04T11:35:07Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1251906479
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -36,6 +36,11 @@ export type CircularOptionPickerProps = {
 	 * The child elements.
 	 */
 	children?: ReactNode;
+	/**
+	 * Whether the keyboard interaction should wrap around.
+	 * Defaults to `true`.
```

```suggestion
	 *
	 * @default true
```

### Review thread 2 (resolved)

#### mirka on 2023-07-04T11:45:49Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1251917161
Path: packages/components/src/circular-option-picker/index.tsx

```diff
@@ -58,6 +73,34 @@ export function Option( {
 	);
 }
 
+export function OptionGroup( {
+	className,
+	options,
+	...additionalProps
+}: OptionGroupProps ) {
+	const compositeState = useContext( CircularOptionPickerContext );
+
+	// This is unlikely to happen, but on the off-chance that we've ended up
+	// with a list of `OptionGroup`s, we will just return those instead.
+	if ( Array.isArray( options ) && options[ 0 ] instanceof OptionGroup ) {
+		return <>options</>;
```

Let's make sure this works visually, and also add a unit test. I don't think we can use `instanceof` in this way.

```suggestion
	if ( Array.isArray( options ) && options[ 0 ] instanceof OptionGroup ) {
		return <>{ options }</>;
```

### Review thread 3 (resolved)

#### mirka on 2023-07-04T11:51:27Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1251922472
Path: packages/components/src/circular-option-picker/index.tsx

```diff
@@ -152,28 +195,37 @@ export function ButtonAction( {
  */
 
 function CircularOptionPicker( props: CircularOptionPickerProps ) {
-	const { actions, className, options, children } = props;
+	const { actions, className, options, children, loop = true } = props;
+	const compositeState = useCompositeState( { loop } );
+
 	return (
-		<div
+		<Composite
+			{ ...compositeState }
+			role={ 'listbox' }
 			className={ classnames(
 				'components-circular-option-picker',
 				className
 			) }
 		>
-			<div className="components-circular-option-picker__swatches">
-				{ options }
-			</div>
-			{ children }
-			{ actions && (
-				<div className="components-circular-option-picker__custom-clear-wrapper">
-					{ actions }
-				</div>
-			) }
-		</div>
+			<CircularOptionPickerContext.Provider value={ compositeState }>
+				{ Array.isArray( options ) ? (
```

`options` is not necessarily an array, as can be [seen](https://github.com/WordPress/gutenberg/blob/5ba748472b3e481e89ec3ac4c4e215031cc1eb3c/packages/components/src/circular-option-picker/stories/index.tsx#L109C1-L110) in the [default Storybook story](https://wordpress.github.io/gutenberg/?path=/story/components-circularoptionpicker--default):

### Review thread 4 (resolved)

#### mirka on 2023-07-04T12:11:30Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1251944987
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -105,19 +106,21 @@ function MultiplePalettes( {
 	colors,
 	onChange,
 	value,
-	actions,
 	headingLevel,
 }: MultiplePalettesProps ) {
+	const instanceId = useInstanceId( MultiplePalettes );
+
 	if ( colors.length === 0 ) {
 		return null;
 	}
 
 	return (
 		<VStack spacing={ 3 } className={ className }>
 			{ colors.map( ( { name, colors: colorPalette }, index ) => {
+				const id = `color-palette-${ instanceId }-${ index }`;
```

Can we use the `prefix` argument of [`useInstanceId()`](https://github.com/WordPress/gutenberg/blob/5ba748472b3e481e89ec3ac4c4e215031cc1eb3c/packages/compose/src/hooks/use-instance-id/index.ts#L40-L48) to simplify this?

### Review thread 5

#### mirka on 2023-07-04T12:33:53Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1251971417
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -91,10 +92,10 @@ function SinglePalette( {
 	}, [ colors, value, onChange, clearColor ] );
 
 	return (
-		<CircularOptionPicker
+		<CircularOptionPicker.OptionGroup
```

I'm unsure about modeling the multiple palette case as `group`s in a single `listbox`. From reading the [WAI guidance](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/), I think a `listbox` is assumed to be one-dimensional (i.e. a list not a grid), with a `aria-orientation` to dictate which arrow keys to use.

What do you think about dropping the `group` construct and modeling them as separate listboxes?

#### ciampo on 2023-08-31T13:48:51Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311662557
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -91,10 +92,10 @@ function SinglePalette( {
 	}, [ colors, value, onChange, clearColor ] );
 
 	return (
-		<CircularOptionPicker
+		<CircularOptionPicker.OptionGroup
```

> What do you think about dropping the group construct and modeling them as separate listboxes?

After interacting with the `ColorPalette` storybook demo with multiple origins, I also got the feeling that having each `OptionGroup` as a separate tab stop feels better.

#### ciampo on 2023-09-04T13:37:07Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1314959036
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -91,10 +92,10 @@ function SinglePalette( {
 	}, [ colors, value, onChange, clearColor ] );
 
 	return (
-		<CircularOptionPicker
+		<CircularOptionPicker.OptionGroup
```

After further discussion, we've decided that we're going to keep the current behaviour, and iterate if/when necessary in a future PR.

### Review thread 6

#### mirka on 2023-07-04T12:36:34Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1251974074
Path: packages/components/src/circular-option-picker/index.tsx

```diff
@@ -152,28 +195,37 @@ export function ButtonAction( {
  */
 
 function CircularOptionPicker( props: CircularOptionPickerProps ) {
-	const { actions, className, options, children } = props;
+	const { actions, className, options, children, loop = true } = props;
+	const compositeState = useCompositeState( { loop } );
```

I'm actually not up to date on this, but is there a reason why we're using `useCompositeState` instead of [`useCompositeStore`](https://github.com/ariakit/ariakit/blob/c30e6a67bebcde1b2bff9a8183e1f098259a14c7/packages/ariakit-react-core/src/composite/composite.tsx#L438-L449)?

#### andrewhayward on 2023-08-15T19:10:02Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1295006640
Path: packages/components/src/circular-option-picker/index.tsx

```diff
@@ -152,28 +195,37 @@ export function ButtonAction( {
  */
 
 function CircularOptionPicker( props: CircularOptionPickerProps ) {
-	const { actions, className, options, children } = props;
+	const { actions, className, options, children, loop = true } = props;
+	const compositeState = useCompositeState( { loop } );
```

We're still using `reakit` under the hood for this, which is where `useCompositeState` is coming from. As and when we move `composite` to `ariakit` we'll have to change that.

### Review thread 7 (resolved)

#### mirka on 2023-07-04T15:13:17Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1252153757
Path: packages/components/src/circular-option-picker/index.tsx

```diff
@@ -7,35 +7,50 @@ import classnames from 'classnames';
  * WordPress dependencies
  */
 import { Icon, check } from '@wordpress/icons';
+import { createContext, useContext } from '@wordpress/element';
 
 /**
  * Internal dependencies
  */
 import Button from '../button';
+import {
+	Composite,
+	CompositeGroup,
+	CompositeItem,
+	useCompositeState,
+} from '../composite';
 import Dropdown from '../dropdown';
 import Tooltip from '../tooltip';
 import type {
 	CircularOptionPickerProps,
 	DropdownLinkActionProps,
+	OptionGroupProps,
 	OptionProps,
 } from './types';
 import type { WordPressComponentProps } from '../ui/context';
 import type { ButtonAsButtonProps } from '../button/types';
 
+const CircularOptionPickerContext = createContext( {} );
+
 export function Option( {
 	className,
 	isSelected,
 	selectedIconProps,
 	tooltipText,
 	...additionalProps
 }: OptionProps ) {
-	const optionButton = (
-		<Button
+	const compositeState = useContext( CircularOptionPickerContext );
+
+	const optionControl = (
+		<CompositeItem
+			as={ Button }
+			className={ 'components-circular-option-picker__option' }
 			isPressed={ isSelected }
```

I think we need to change it from `aria-pressed` to `aria-selected`?

We may also want to make sure a consumer can add `aria-multiselectable` to the `listbox`.

### Review thread 8

#### mirka on 2023-07-04T15:18:12Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1252158414
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -33,9 +33,10 @@ export default meta;
 
 const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
+	value,
 	...args
 } ) => {
-	const [ color, setColor ] = useState< string | undefined >();
+	const [ color, setColor ] = useState< string | undefined >( value );
```

(Marking this as TODO so we don't forget to address it)

#### ciampo on 2023-08-31T12:55:56Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311587391
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -33,9 +33,10 @@ export default meta;
 
 const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
+	value,
 	...args
 } ) => {
-	const [ color, setColor ] = useState< string | undefined >();
+	const [ color, setColor ] = useState< string | undefined >( value );
```

Out of curiosity, what exactly was the TODO item related to this line of code?

#### ciampo on 2023-09-04T13:38:20Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1314960239
Path: packages/components/src/color-palette/stories/index.tsx

```diff
@@ -33,9 +33,10 @@ export default meta;
 
 const Template: ComponentStory< typeof ColorPalette > = ( {
 	onChange,
+	value,
 	...args
 } ) => {
-	const [ color, setColor ] = useState< string | undefined >();
+	const [ color, setColor ] = useState< string | undefined >( value );
```

@andrewhayward would you happen to know what needed to be done around this line?

### Review thread 9

#### ciampo on 2023-08-31T12:59:57Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311592383
Path: packages/components/src/border-control/test/index.js

```diff
@@ -75,6 +79,10 @@ const clickButton = ( name ) => {
 	fireEvent.click( getButton( name ) );
 };
 
+const selectColorOption = ( name ) => {
+	fireEvent.click( getColorOption( name ) );
```

Another idea for a follow-up PR: refactor `BorderControl`'s unit tests from `fireEvent` to `testing-library/user-event`

#### andrewhayward on 2023-09-04T15:47:57Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1315079078
Path: packages/components/src/border-control/test/index.js

```diff
@@ -75,6 +79,10 @@ const clickButton = ( name ) => {
 	fireEvent.click( getButton( name ) );
 };
 
+const selectColorOption = ( name ) => {
+	fireEvent.click( getColorOption( name ) );
```

See #54155.

### Review thread 10 (resolved)

#### ciampo on 2023-08-31T13:50:20Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311664802
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -105,19 +106,21 @@ function MultiplePalettes( {
 	colors,
 	onChange,
 	value,
-	actions,
```

Just to make sure I'm following here, both `SinglePalette` and `MultiplePalettes` are not expecting an `actions` prop anymore because, later in the file, we're wrapping them in a `CircularOptionPicker` component (which accepts the `actions` itself). Is that correct?

#### andrewhayward on 2023-09-01T12:38:11Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1312974583
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -105,19 +106,21 @@ function MultiplePalettes( {
 	colors,
 	onChange,
 	value,
-	actions,
```

Yes, that's correct. `CircularOptionPicker` now acts as a container for one or more palettes, so the palettes themselves no longer need to know about `actions`.

### Review thread 11 (resolved)

#### ciampo on 2023-08-31T14:08:25Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311695155
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -234,18 +237,26 @@ function UnforwardedColorPalette(
 		: __( 'Custom color picker.' );
 
 	const paletteCommonProps = {
-		clearable,
 		clearColor,
 		onChange,
 		value,
-		actions: !! clearable && (
-			<CircularOptionPicker.ButtonAction onClick={ clearColor }>
-				{ __( 'Clear' ) }
-			</CircularOptionPicker.ButtonAction>
-		),
-		headingLevel,
 	};
 
+	const actions = !! clearable && (
+		<CircularOptionPicker.ButtonAction onClick={ clearColor }>
+			{ __( 'Clear' ) }
+		</CircularOptionPicker.ButtonAction>
+	);
+
+	let ariaProps: { 'aria-label': string } | { 'aria-labelledby': string };
+	if ( ariaLabel ) {
+		ariaProps = { 'aria-label': ariaLabel as string };
+	} else if ( ariaLabelledby ) {
+		ariaProps = { 'aria-labelledby': ariaLabelledby as string };
+	} else {
+		ariaProps = { 'aria-label': __( 'Custom color picker.' ) };
+	}
```

The `as string` type casts shouldn't be necessary:

```suggestion
	let ariaProps: { 'aria-label': string } | { 'aria-labelledby': string };
	if ( ariaLabel ) {
		ariaProps = { 'aria-label': ariaLabel };
	} else if ( ariaLabelledby ) {
		ariaProps = { 'aria-labelledby': ariaLabelledby };
	} else {
		ariaProps = { 'aria-label': __( 'Custom color picker.' ) };
	}
```

#### andrewhayward on 2023-09-01T13:13:10Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1313010711
Path: packages/components/src/color-palette/index.tsx

```diff
@@ -234,18 +237,26 @@ function UnforwardedColorPalette(
 		: __( 'Custom color picker.' );
 
 	const paletteCommonProps = {
-		clearable,
 		clearColor,
 		onChange,
 		value,
-		actions: !! clearable && (
-			<CircularOptionPicker.ButtonAction onClick={ clearColor }>
-				{ __( 'Clear' ) }
-			</CircularOptionPicker.ButtonAction>
-		),
-		headingLevel,
 	};
 
+	const actions = !! clearable && (
+		<CircularOptionPicker.ButtonAction onClick={ clearColor }>
+			{ __( 'Clear' ) }
+		</CircularOptionPicker.ButtonAction>
+	);
+
+	let ariaProps: { 'aria-label': string } | { 'aria-labelledby': string };
+	if ( ariaLabel ) {
+		ariaProps = { 'aria-label': ariaLabel as string };
+	} else if ( ariaLabelledby ) {
+		ariaProps = { 'aria-labelledby': ariaLabelledby as string };
+	} else {
+		ariaProps = { 'aria-label': __( 'Custom color picker.' ) };
+	}
```

I wonder if I changed something somewhere, because I'm sure at one point it was complaining that the value was `string or undefined` and so couldn't be coerced into a `string`. Ho hum. Will fix.

### Review thread 12

#### ciampo on 2023-08-31T14:19:50Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311711606
Path: packages/components/src/color-palette/types.ts

```diff
@@ -88,4 +88,16 @@ export type ColorPaletteProps = Pick< PaletteProps, 'onChange' > & {
 	 * @default false
 	 */
 	__experimentalIsRenderedInSidebar?: boolean;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

If you want either prop `a` or prop `b` to be required, you should be able to do so by using the `never` keyword — see [this playground example](https://www.typescriptlang.org/play?#-YPAI3H0m1O8LgD0R1AAAsA4ALQQAB6QFMA3GFgYxdh4AAZ6n-ooSp8zL8KHoUCg4MAoGZoGYIY0GA9mAAzZh+PSQprQHB6BDQYDMXEAOhIVTmDUaHjhSw+K10XRoe0m21G4UZWx25jZBz4J3Olxu9wgj2erygIkhABsIHoaqhoGBglCIMBahAICgoN9fqCAUComqFHhgHAEULkaiFMTSZD5o0AHJy6kaQiremoLnM6ZAA) that I created to illustrate how to.

#### andrewhayward on 2023-09-01T13:01:23Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1312998526
Path: packages/components/src/color-palette/types.ts

```diff
@@ -88,4 +88,16 @@ export type ColorPaletteProps = Pick< PaletteProps, 'onChange' > & {
 	 * @default false
 	 */
 	__experimentalIsRenderedInSidebar?: boolean;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

For the purposes of this PR, I wanted to leave both `aria-label` and `aria-labelledby` as optional props here, because of the number of components that use this and don't provide either; fixing all of those would dramatically increase the scope of the ticket. I created [a follow-up ticket to track that work](https://github.com/WordPress/gutenberg/issues/54055).

The desired mutual exclusivity could probably be clearer here though, and it may be worth adopting the `never` union syntax, even if they both remain optional...

```TypeScript
export type ColorPaletteProps = Pick< PaletteProps, 'onChange' > & {
  ...
} & (
  | {
      'aria-label'?: string;
      'aria-labelledby'?: never;
    }
  | {
      'aria-label'?: never;
      'aria-labelledby'?: string;
    }
);
```

#### ciampo on 2023-09-04T13:40:54Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1314962825
Path: packages/components/src/color-palette/types.ts

```diff
@@ -88,4 +88,16 @@ export type ColorPaletteProps = Pick< PaletteProps, 'onChange' > & {
 	 * @default false
 	 */
 	__experimentalIsRenderedInSidebar?: boolean;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

Sounds good, we can do this as a follow-up

### Review thread 13 (resolved)

#### ciampo on 2023-08-31T14:20:16Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311712278
Path: packages/components/src/duotone-picker/duotone-picker.tsx

```diff
@@ -120,8 +122,18 @@ function DuotonePicker( {
 		);
 	} );
 
+	let ariaProps: { 'aria-label': string } | { 'aria-labelledby': string };
+	if ( ariaLabel ) {
+		ariaProps = { 'aria-label': ariaLabel as string };
+	} else if ( ariaLabelledby ) {
+		ariaProps = { 'aria-labelledby': ariaLabelledby as string };
```

Same as above re. the `as string` type cast

### Review thread 14 (resolved)

#### ciampo on 2023-08-31T14:20:34Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311712715
Path: packages/components/src/duotone-picker/types.ts

```diff
@@ -39,6 +39,18 @@ export type DuotonePickerProps = {
 	 * Callback which is called when the duotone colors change.
 	 */
 	onChange: ( value: DuotonePickerProps[ 'value' ] | undefined ) => void;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

Same as above re. requiring one prop or the other

#### andrewhayward on 2023-09-01T13:02:14Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1312999383
Path: packages/components/src/duotone-picker/types.ts

```diff
@@ -39,6 +39,18 @@ export type DuotonePickerProps = {
 	 * Callback which is called when the duotone colors change.
 	 */
 	onChange: ( value: DuotonePickerProps[ 'value' ] | undefined ) => void;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

See [response above](#discussion_r1312998526).

### Review thread 15 (resolved)

#### ciampo on 2023-08-31T14:20:57Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311713242
Path: packages/components/src/gradient-picker/types.ts

```diff
@@ -42,6 +42,18 @@ type GradientPickerBaseProps = {
 	 * @default 2
 	 */
 	headingLevel?: HeadingSize;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

Same as above

#### andrewhayward on 2023-09-01T13:02:39Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1312999792
Path: packages/components/src/gradient-picker/types.ts

```diff
@@ -42,6 +42,18 @@ type GradientPickerBaseProps = {
 	 * @default 2
 	 */
 	headingLevel?: HeadingSize;
+	/**
+	 * A label to identify the purpose of the control.
+	 *
+	 * @todo Either this or `aria-labelledby` should be required
+	 */
+	'aria-label'?: string;
+	/**
+	 * An ID of an element to provide a label for the control.
+	 *
+	 * @todo Either this or `aria-label` should be required
+	 */
+	'aria-labelledby'?: string;
```

See [response above](#discussion_r1312998526).

### Review thread 16 (resolved)

#### ciampo on 2023-08-31T14:25:54Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311720152
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -36,7 +40,20 @@ export type CircularOptionPickerProps = {
 	 * The child elements.
 	 */
 	children?: ReactNode;
-};
+	/**
+	 * Whether the keyboard interaction should wrap around.
+	 *
+	 * @default true
+	 */
+	loop?: boolean;
+} & (
+	| {
+			'aria-label': string;
+	  }
+	| {
+			'aria-labelledby': string;
+	  }
```

Since these 2 props should probably be mutually exclusive, we could adopt the same approach suggested in a previous comment

#### andrewhayward on 2023-09-01T13:04:12Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1313001328
Path: packages/components/src/circular-option-picker/types.ts

```diff
@@ -36,7 +40,20 @@ export type CircularOptionPickerProps = {
 	 * The child elements.
 	 */
 	children?: ReactNode;
-};
+	/**
+	 * Whether the keyboard interaction should wrap around.
+	 *
+	 * @default true
+	 */
+	loop?: boolean;
+} & (
+	| {
+			'aria-label': string;
+	  }
+	| {
+			'aria-labelledby': string;
+	  }
```

This is me mixing up my Flow and TypeScript 🤦 Will fix.

### Review thread 17 (resolved)

#### ciampo on 2023-08-31T15:39:42Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1311829976
Path: packages/components/CHANGELOG.md

```diff
@@ -7,6 +7,7 @@
 -   Make the `Popover.Slot` optional and render popovers at the bottom of the document's body by default. ([#53889](https://github.com/WordPress/gutenberg/pull/53889)).
 -   `ProgressBar`: Add transition to determinate indicator ([#53877](https://github.com/WordPress/gutenberg/pull/53877)).
 -   Prevent nested `SlotFillProvider` from rendering ([#53940](https://github.com/WordPress/gutenberg/pull/53940)).
+-   Making Circular Option Picker a `listbox`. Note that while this changes some public API, new props are optional, and currently have default values; this will change in another patch. ([#52255](https://github.com/WordPress/gutenberg/pull/52255))
```

What are we planning on changing exactly in a future patch?

#### andrewhayward on 2023-09-01T13:09:54Z

URL: https://github.com/WordPress/gutenberg/pull/52255#discussion_r1313007300
Path: packages/components/CHANGELOG.md

```diff
@@ -7,6 +7,7 @@
 -   Make the `Popover.Slot` optional and render popovers at the bottom of the document's body by default. ([#53889](https://github.com/WordPress/gutenberg/pull/53889)).
 -   `ProgressBar`: Add transition to determinate indicator ([#53877](https://github.com/WordPress/gutenberg/pull/53877)).
 -   Prevent nested `SlotFillProvider` from rendering ([#53940](https://github.com/WordPress/gutenberg/pull/53940)).
+-   Making Circular Option Picker a `listbox`. Note that while this changes some public API, new props are optional, and currently have default values; this will change in another patch. ([#52255](https://github.com/WordPress/gutenberg/pull/52255))
```

Sort of [touched on this above](#discussion_r1312998526), but ultimately all consumers should be providing their own label for the picker. `CircularOptionPicker` now requires an `aria-label` or `aria-labelledby`, and while `ColorPalette`, `DuotonePicker` and `GradientPicker` can all pass them through, they also provide a default value to obfuscate the need. Eventually they too will require one or the other, and not have a default; this will be addressed in #54055.

## Changed files

- packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap (MODIFIED, +29/-19)
- packages/block-library/src/cover/test/edit.js (MODIFIED, +7/-7)
- packages/components/CHANGELOG.md (MODIFIED, +4/-0)
- packages/components/src/border-control/test/index.js (MODIFIED, +17/-9)
- packages/components/src/circular-option-picker/index.tsx (MODIFIED, +101/-17)
- packages/components/src/circular-option-picker/types.ts (MODIFIED, +25/-1)
- packages/components/src/color-palette/index.tsx (MODIFIED, +46/-26)
- packages/components/src/color-palette/stories/index.story.tsx (MODIFIED, +16/-2)
- packages/components/src/color-palette/test/__snapshots__/index.tsx.snap (DELETED, +0/-288)
- packages/components/src/color-palette/test/index.tsx (MODIFIED, +26/-22)
- packages/components/src/color-palette/types.ts (MODIFIED, +20/-1)
- packages/components/src/duotone-picker/duotone-picker.tsx (MODIFIED, +12/-0)
- packages/components/src/duotone-picker/types.ts (MODIFIED, +20/-1)
- packages/components/src/gradient-picker/index.tsx (MODIFIED, +37/-11)
- packages/components/src/gradient-picker/types.ts (MODIFIED, +20/-1)
- test/e2e/specs/editor/blocks/buttons.spec.js (MODIFIED, +3/-3)
- test/e2e/specs/editor/blocks/cover.spec.js (MODIFIED, +3/-3)
- test/e2e/specs/editor/blocks/heading.spec.js (MODIFIED, +1/-1)
- test/e2e/specs/editor/blocks/navigation-colors.spec.js (MODIFIED, +10/-10)
- test/e2e/specs/editor/various/keep-styles-on-block-transforms.spec.js (MODIFIED, +1/-1)
- test/e2e/specs/editor/various/list-view.spec.js (MODIFIED, +1/-1)
- test/e2e/specs/editor/various/rich-text.spec.js (MODIFIED, +1/-1)
- test/e2e/specs/site-editor/style-variations.spec.js (MODIFIED, +3/-3)
- test/e2e/specs/site-editor/user-global-styles-revisions.spec.js (MODIFIED, +2/-2)
