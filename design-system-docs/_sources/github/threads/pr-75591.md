---
number: 75591
type: pull_request
state: CLOSED
url: https://github.com/WordPress/gutenberg/pull/75591
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Task","[Package] Components","First-time Contributor","[Feature] Real-time Collaboration"]
---

# PR #75591: Components: Add Avatar and AvatarGroup private components

- URL: https://github.com/WordPress/gutenberg/pull/75591
- Author: dabowman
- Created: 2026-02-16T22:43:26Z
- Updated: 2026-03-30T15:10:03Z
- Comments: 10 of 10
- Review threads: 6 of 6
- Changed files: 15 of 15
- Diff: ../pr-diffs/75591.diff

## Body

## Summary

Adds two new private components to `@wordpress/components` for rendering user avatars, intended for use in real-time collaboration UI and other internal features.

- **Avatar** — Circular avatar with Gravatar image support, initials fallback, accent border ring, hover-expand badge with name/label, status dimming with icon overlay, and Tooltip integration.
- **AvatarGroup** — Overlapping avatar layout with configurable `max` and overflow indicator (`+N`), CSS-based z-index stacking, and `role="group"` for accessibility.

Both are registered as **private APIs** via `lock(privateApis, { Avatar, AvatarGroup })` — not exposed publicly.

### Key design decisions

- **Status is consumer-driven**: The `status` prop accepts any string and applies uniform dimming. A dynamic `is-{status}` class is added for consumer-specific styling. No built-in status-specific overrides.
- **Badge + Tooltip**: Badge mode (hover-expand pill) shows `label` or `name`. When `label` differs from `name`, a Tooltip also wraps the avatar so the full name is accessible on hover.
- **CSS-only z-index stacking**: AvatarGroup uses an SCSS `@for` loop on `:nth-child` instead of `cloneElement` for stacking order.
- **Grid-based badge animation**: `grid-template-columns: 0fr → 1fr` for exact natural width expansion (avoids `max-width` overshoot). `prefers-reduced-motion` disables transitions.
- **Design tokens throughout**: Uses `$components-color-accent`, `$elevation-x-small`, `$radius-full`, `$button-size-compact`, `$grid-unit-*`, etc. — no hardcoded values.

## Test plan

- [x] Unit tests pass (30/30 — 24 Avatar + 6 AvatarGroup)
- [x] Stylelint clean on both SCSS files
- [x] ESLint clean on all component/test/story files
- [x] Pre-commit hooks pass (format, lint:js strict, lint:css, api-docs)
- [ ] Verify Storybook stories render correctly (`npm run storybook:dev` → Design System/Components/Avatar and AvatarGroup)
- [ ] Verify `status-private` tag excludes stories from public Storybook

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Issue comments

### github-actions on 2026-02-16T22:43:52Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-3910809241

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: dabowman <davidabowman@git.wordpress.org>
Co-authored-by: aduth <aduth@git.wordpress.org>
Co-authored-by: juanfra <juanfra@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-02-16T22:44:28Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-3910810825

:wave: Thanks for your first Pull Request and for helping build the future of Gutenberg and WordPress, @dabowman! In case you missed it, we'd love to have you join us in our [Slack community](https://make.wordpress.org/chat/).

If you want to learn more about WordPress development in general, check out the [Core Handbook](https://make.wordpress.org/core/handbook/) full of helpful information.

### dabowman on 2026-02-17T02:19:48Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-3911614912

This component is needed for Real-time Collaboration and for the Notes features. I made a clean PR here with just the component so we can discuss it in isolation, but I've also added the code to the PR that refines the RTC UI over here #75595 . If need be we can relocate the component code in that PR into the editor package where the RTC UI lives. That way it's not making an addition to the components package if we want more time to review this before making an addition.

### aduth on 2026-02-17T19:23:36Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-3916664764

Looping in @WordPress/gutenberg-components here for review and awareness. It looks like an initial version of this landed already in #75595 . It'd be good to get some eyes from the broader components group, though as a private API it gives us some buffer to iterate. Aside from being used in real-time collaboration, I could imagine many use-cases of a component like this for general reusability, and is common in design systems (e.g. we've been using a lot of BaseUI recently, which also includes [an Avatar component](https://base-ui.com/react/components/avatar)).

It'd be good to try to stabilize and shift this toward consistency with other components in `@wordpress/ui`, including prop naming, tokens, composability, etc. I'd recommend either addressing those consistency points here after a rebase, or creating a tracking issue(s) for that work.

### aduth on 2026-03-24T20:13:33Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-4121071540

Is this pull request effectively superseded by #75652?

### ciampo on 2026-03-25T17:14:16Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-4128313701

IMO we should close this PR, assess the features and APIs of `Avatar` (and potentially `AvatarGroup`), and consider adding them to `@wordpress/ui` instead.

### aduth on 2026-03-26T14:35:39Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-4135450990

I created a tracking issue for the `@wordpress/ui` `Avatar` component at #76836.

@dabowman Up to you if you want to close this and we can open a new pull request dedicated to that component, or you could update and adapt this one to target the `@wordpress/ui` implementation. My hunch is that things have evolved enough that it probably makes sense as a new pull request.

### ciampo on 2026-03-27T16:35:51Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-4143823213

Regardless of whether we keep this PR or open a new one, I'd like to propose some differences to the implementation. At a high level:

- We should use [Base UI's `Avatar`](https://base-ui.com/react/components/avatar) under the hook
- I'd consider removing gravatar support and keep `Avatart` as a UI component. We can keep gravatar integration as a dedicated Storybook example, and potentially consider greating a higher-level dedicated component, coupled with the gravatar APIs
- I'd consider whether `AvatarGroup` deserves to be a dedicated component, since it's basically a horizontal stack with negative margin — we could even consider tweaking `Stack` to support this use case?
- a deeper API surface review, to make sure we have good consistency with other components in the package

### aduth on 2026-03-30T14:16:30Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-4155406968

> Regardless of whether we keep this PR or open a new one, I'd like to propose some differences to the implementation.

👍 I [copied those over to the tracking issue](https://github.com/WordPress/gutenberg/issues/76836#issuecomment-4155397557) as well.

I felt similarly about `AvatarGroup` in writing that issue:

> The scope of this issue doesn't necessarily include AvatarGroup. It's not clear to me there's a demonstrated wide use-case for this, and it could continue to exist as a component specific to the `@wordpress/editor` package.

### ciampo on 2026-03-30T15:10:03Z

URL: https://github.com/WordPress/gutenberg/pull/75591#issuecomment-4155804836

With https://github.com/WordPress/gutenberg/issues/76836 to track this, I feel like we can safely close this PR.

We can always reopen in case we change our mind

## Review threads

### Review thread 1

#### aduth on 2026-02-17T19:32:30Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818721525
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
+	/**
+	 * CSS color value for an accent border ring around the avatar.
+	 *
+	 * When not provided, no border is rendered and the hover badge
+	 * and avatar status uses the admin theme color as its background.
+	 */
+	borderColor?: string;
+	/**
+	 * A status string applied to the avatar. When set, the image is
+	 * dimmed to indicate a non-default state. A corresponding
+	 * `is-{status}` class is added for custom styling.
+	 */
+	status?: string;
```

If this moves to `@wordpress/ui`, we may want to consider the reliance on CSS modifier class names like is being introduced here, since the components implemented there heavily depend on CSS modules that have randomly-generated class names, for the _expressed purpose_ of preventing this kind of ad hoc style extensibility (citation needed). 

Instead, I feel like if we want to support a stylized effect of dimming, that should be baked-in to the component. Or maybe support some customization through composability (nested components, etc.). It's not entirely clear to me how this is intended to be used, since it doesn't appear to be used yet with real-time collaboration based on what I see in #75595.

#### dabowman on 2026-02-17T19:48:54Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818779983
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
+	/**
+	 * CSS color value for an accent border ring around the avatar.
+	 *
+	 * When not provided, no border is rendered and the hover badge
+	 * and avatar status uses the admin theme color as its background.
+	 */
+	borderColor?: string;
+	/**
+	 * A status string applied to the avatar. When set, the image is
+	 * dimmed to indicate a non-default state. A corresponding
+	 * `is-{status}` class is added for custom styling.
+	 */
+	status?: string;
```

yeah, it's not being used yet. The addition of the status-based class was mostly just to have the option of customizing things beyond what the component already does. But it makes sense that we don't want that. We also don't really need it for the way this is intended to be used. I think I'll just remove the class generation, especially since it's not being used yet.

#### dabowman on 2026-02-18T15:44:06Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2823004570
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
+	/**
+	 * CSS color value for an accent border ring around the avatar.
+	 *
+	 * When not provided, no border is rendered and the hover badge
+	 * and avatar status uses the admin theme color as its background.
+	 */
+	borderColor?: string;
+	/**
+	 * A status string applied to the avatar. When set, the image is
+	 * dimmed to indicate a non-default state. A corresponding
+	 * `is-{status}` class is added for custom styling.
+	 */
+	status?: string;
```

I changed this to a basic "dimmed" option. So it's style specific and doesn't add arbitrary css anymore.

### Review thread 2

#### aduth on 2026-02-17T19:35:26Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818731931
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
```

I'm wondering about alignment between these props and how we think of [the `label` prop in `IconButton`](https://github.com/WordPress/gutenberg/blob/trunk/packages/ui/src/icon-button/types.ts), where we intentionally try to discourage mismatch between the accessible name and the tooltip content, though I think the use-case of something like "You" is interesting to consider, and feels similar in purpose to the sort of suffixing that was implemented in `IconButton`'s shortcut support (e.g. maybe still deriving tooltip from name, but allowing the option for additional suffixing detail).

#### dabowman on 2026-02-17T19:52:08Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818791392
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
```

Yeah, I'm happy to name things whatever y'all think is best. The intent was just to have one prop that provides the accessible name that's used for the tooltip and generating any fallback initials. This would be the "main" name or label or whatever. In addition to that I wanted to provide a secondary label/name/whatever to use contextually if we need to call a collaborator/user/entity something different. I can see this being useful when agents or other connected agentic things start appearing as collaborators. Instances where an entities exact name is less important than the role they're playing in the given situation. Happy to align that with how the button or other components handle that difference in meaning.

### Review thread 3

#### aduth on 2026-02-17T19:37:42Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818740778
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
```

The way this affects the visual presentation of the avatar reminds me of how we use `variant` props in other components (e.g. `Button` variant ["describes the visual style treatment"](https://github.com/WordPress/gutenberg/blob/6524f065cb5bb4c4c2bb2a3290385e8f0841174e/packages/ui/src/button/types.ts#L10)). Though I think this surfaces the need for documented consistency of how we use these common props.

#### dabowman on 2026-02-17T19:53:38Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818797071
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
```

Yeah, making this an enum instead of a bool makes a lot of sense. Then we could have other secondary displays for the component. I saw mentions of other status "badges" like green dots for connected or online, things like that. That stuff might fall under this category that just becomes a broader "variant" prop.

#### dabowman on 2026-02-18T15:44:34Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2823007838
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
```

changed this to a variant enum. badge is currently the only option but we could add more

### Review thread 4

#### aduth on 2026-02-17T19:38:38Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818743971
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
```

From a systems perspective, I think it'd be helpful if we provided some guidance on when each size should be used, [similar to what we do in `Button`](https://github.com/WordPress/gutenberg/blob/6524f065cb5bb4c4c2bb2a3290385e8f0841174e/packages/ui/src/button/types.ts#L27-L36), rather than simply communicating the raw pixel size and expecting someone to figure it out on their own.

#### dabowman on 2026-02-17T19:54:08Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818799204
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
```

Yeah, totally. The documentation needs to be more prescriptive here.

#### dabowman on 2026-02-18T15:44:44Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2823009106
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
```

fixed the docs here

### Review thread 5

#### aduth on 2026-02-17T19:41:18Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818753396
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
+	/**
+	 * CSS color value for an accent border ring around the avatar.
+	 *
+	 * When not provided, no border is rendered and the hover badge
+	 * and avatar status uses the admin theme color as its background.
+	 */
+	borderColor?: string;
```

Interesting to think how we might want to think about token usage here rather than hard-coded values. In other places we've expressly moved away from supporting hard-coded values in favor of tokens ([example](https://github.com/WordPress/gutenberg/pull/74008)). Although in how this is used in real-time collaboration with a bunch of different colors for each collaborator, I'm not sure we can really _tokenize_ this as-is, without either baking in tokens for complementary colors (like what you're exploring in #74875), or wrapping every `Avatar` with `ThemeProvider` (excessive), or allowing hard-coded values as you're considering here.

#### dabowman on 2026-02-17T19:55:18Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2818804389
Path: packages/components/src/avatar/types.ts

```diff
@@ -0,0 +1,59 @@
+import type { IconType } from '../icon';
+
+export type AvatarProps = {
+	/**
+	 * URL of the avatar image.
+	 *
+	 * When not provided, initials derived from `name` are shown.
+	 */
+	src?: string;
+	/**
+	 * Name of the user. Used as an accessible label and to generate
+	 * initials when no image is provided.
+	 */
+	name?: string;
+	/**
+	 * Visible text shown in the hover badge. When not provided, `name`
+	 * is used instead. Use this to provide contextual labels like "You"
+	 * without affecting the accessible name or initials.
+	 */
+	label?: string;
+	/**
+	 * Whether to show the hover-expand badge that reveals the user's
+	 * name (or `label`) on hover. Requires `name` to be set.
+	 *
+	 * @default false
+	 */
+	badge?: boolean;
+	/**
+	 * Size of the avatar.
+	 *
+	 * - `'default'`: 32px
+	 * - `'small'`: 24px
+	 *
+	 * @default 'default'
+	 */
+	size?: 'default' | 'small';
+	/**
+	 * CSS color value for an accent border ring around the avatar.
+	 *
+	 * When not provided, no border is rendered and the hover badge
+	 * and avatar status uses the admin theme color as its background.
+	 */
+	borderColor?: string;
```

Yeah, ideally this is locked to tokens, but realistically I think it needs to support arbitrary colors. Especially since different uses might need to generate their own colors. If we could provide a method for doing that from the theme package we might be able to tighten that up. But until then it probably needs to accept hex values.

### Review thread 6

#### juanfra on 2026-02-18T08:34:00Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2821015017
Path: packages/components/src/avatar/styles.scss

```diff
@@ -0,0 +1,165 @@
+@use "@wordpress/base-styles/colors" as *;
+@use "@wordpress/base-styles/variables" as *;
+@use "../utils/theme-variables" as *;
+
+.components-avatar {
+	position: relative;
+	display: inline-flex;
+	align-items: center;
+	border-radius: $radius-full;
+	overflow: clip;
+	flex-shrink: 0;
+	box-shadow: 0 0 0 var(--wp-admin-border-width-focus) $white, $elevation-x-small;
+}
+
+.components-avatar__image {
+	box-sizing: border-box;
+	position: relative;
+	width: $button-size-compact;
+	height: $button-size-compact;
+	border-radius: $radius-full;
+	border: 0;
+	background-color: $components-color-accent;
+	overflow: clip;
+	flex-shrink: 0;
+	font-size: 0;
+	color: $white;
+
+	.is-small > & {
+		width: $button-size-small;
+		height: $button-size-small;
+	}
+
+	.has-src > & {
+		background-image: var(--components-avatar-url);
+		background-size: cover;
+		background-position: center;
+	}
+
+	.has-avatar-border-color > & {
+		border: var(--wp-admin-border-width-focus) solid var(--components-avatar-outline-color);
+		box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) $white;
+		background-clip: padding-box;
+	}
+}
+
+// Initials fallback: show name characters when no image.
+.components-avatar:not(.has-src) > .components-avatar__image {
```

If we go with the initials fallback it'd be worth to loop in the design team, so that we can have a pass on how it'd look like for that case.

#### dabowman on 2026-02-18T15:46:25Z

URL: https://github.com/WordPress/gutenberg/pull/75591#discussion_r2823020583
Path: packages/components/src/avatar/styles.scss

```diff
@@ -0,0 +1,165 @@
+@use "@wordpress/base-styles/colors" as *;
+@use "@wordpress/base-styles/variables" as *;
+@use "../utils/theme-variables" as *;
+
+.components-avatar {
+	position: relative;
+	display: inline-flex;
+	align-items: center;
+	border-radius: $radius-full;
+	overflow: clip;
+	flex-shrink: 0;
+	box-shadow: 0 0 0 var(--wp-admin-border-width-focus) $white, $elevation-x-small;
+}
+
+.components-avatar__image {
+	box-sizing: border-box;
+	position: relative;
+	width: $button-size-compact;
+	height: $button-size-compact;
+	border-radius: $radius-full;
+	border: 0;
+	background-color: $components-color-accent;
+	overflow: clip;
+	flex-shrink: 0;
+	font-size: 0;
+	color: $white;
+
+	.is-small > & {
+		width: $button-size-small;
+		height: $button-size-small;
+	}
+
+	.has-src > & {
+		background-image: var(--components-avatar-url);
+		background-size: cover;
+		background-position: center;
+	}
+
+	.has-avatar-border-color > & {
+		border: var(--wp-admin-border-width-focus) solid var(--components-avatar-outline-color);
+		box-shadow: inset 0 0 0 var(--wp-admin-border-width-focus) $white;
+		background-clip: padding-box;
+	}
+}
+
+// Initials fallback: show name characters when no image.
+.components-avatar:not(.has-src) > .components-avatar__image {
```

Yeah, the initials here are following the designs they worked on for Notes and RTC. So it should be in line with what they expect.

## Changed files

- packages/components/CHANGELOG.md (MODIFIED, +1/-0)
- packages/components/src/avatar-group/component.tsx (ADDED, +46/-0)
- packages/components/src/avatar-group/index.ts (ADDED, +2/-0)
- packages/components/src/avatar-group/stories/index.story.tsx (ADDED, +131/-0)
- packages/components/src/avatar-group/styles.scss (ADDED, +36/-0)
- packages/components/src/avatar-group/test/index.tsx (ADDED, +104/-0)
- packages/components/src/avatar-group/types.ts (ADDED, +13/-0)
- packages/components/src/avatar/component.tsx (ADDED, +95/-0)
- packages/components/src/avatar/index.ts (ADDED, +2/-0)
- packages/components/src/avatar/stories/index.story.tsx (ADDED, +139/-0)
- packages/components/src/avatar/styles.scss (ADDED, +165/-0)
- packages/components/src/avatar/test/index.tsx (ADDED, +193/-0)
- packages/components/src/avatar/types.ts (ADDED, +66/-0)
- packages/components/src/private-apis.ts (MODIFIED, +4/-0)
- packages/components/src/style.scss (MODIFIED, +2/-0)
