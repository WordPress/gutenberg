---
number: 77332
type: pull_request
state: MERGED
merged_at: 2026-04-15T13:51:57Z
url: https://github.com/WordPress/gutenberg/pull/77332
matched_labels: ["[Package] UI"]
all_labels: ["[Type] Enhancement","[Package] UI"]
---

# PR #77332: UI: use Text in Notice.ActionLink typography

- URL: https://github.com/WordPress/gutenberg/pull/77332
- Author: Adi-ty
- Created: 2026-04-14T14:10:41Z
- Updated: 2026-04-15T13:52:46Z
- Comments: 1 of 1
- Review threads: 2 of 2
- Changed files: 3 of 3
- Diff: ../pr-diffs/77332.diff

## Body

<!-- Thanks for contributing to Gutenberg! Please follow the Gutenberg Contributing Guidelines:
https://github.com/WordPress/gutenberg/blob/trunk/CONTRIBUTING.md -->

## What?
<!-- Link this PR to its associated issue with an appropriate keyword: Closes, See, Follow up to, etc. -->
Part of #76929

Updates `Notice.ActionLink` in `@wordpress/ui` to use the 'Text` component for typography instead of ad hoc typography CSS tokens.

## Why?
This follows the design system direction in #76929 to standardize typography via `Text` variants. For `Notice.ActionLink`, the target pairing is an exact match to `body-md`.

## How?
Refactored `Notice.ActionLink` to compose:
- `Text` with `variant="body-md"`
- `render={ <Link tone="neutral" variant="default" /> }`

Removed direct typography declarations from `.action-link` styles in `style.module.css`


## Testing Instructions
1. Run focused unit tests: `npm run test:unit -- packages/ui/src/notice/test/index.test.tsx`
2. Verify in Storybook: `npm run storybook:dev`.


## Use of AI Tools
Used Claude AI to help draft the PR description and assist with review.

## Issue comments

### github-actions on 2026-04-15T01:13:48Z

URL: https://github.com/WordPress/gutenberg/pull/77332#issuecomment-4248449062

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: Adi-ty <iamadisingh@git.wordpress.org>
Co-authored-by: ciampo <mciampini@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

## Review threads

### Review thread 1 (resolved)

#### ciampo on 2026-04-15T09:53:36Z

URL: https://github.com/WordPress/gutenberg/pull/77332#discussion_r3085566365
Path: packages/ui/src/notice/action-link.tsx

```diff
@@ -10,12 +11,12 @@ import styles from './style.module.css';
 export const ActionLink = forwardRef< HTMLAnchorElement, ActionLinkProps >(
 	function NoticeActionLink( { className, ...props }, ref ) {
 		return (
-			<Link
+			<Text
 				ref={ ref }
 				className={ clsx( styles[ 'action-link' ], className ) }
 				{ ...props }
-				tone="neutral"
-				variant="default"
+				variant="body-md"
+				render={ <Link tone="neutral" variant="default" /> }
```

The changes in this PR introduce a bug causing the`render` prop in `{...props}` to be always overridden by `render={ <Link /> }`.

The correct fix is to destructure `render` from the component props (line 12) and pass it to `Link` explicitly:

```jsx
render={ <Link tone="neutral" variant="default" render={ render } /> }
```

#### Adi-ty on 2026-04-15T10:07:38Z

URL: https://github.com/WordPress/gutenberg/pull/77332#discussion_r3085644571
Path: packages/ui/src/notice/action-link.tsx

```diff
@@ -10,12 +11,12 @@ import styles from './style.module.css';
 export const ActionLink = forwardRef< HTMLAnchorElement, ActionLinkProps >(
 	function NoticeActionLink( { className, ...props }, ref ) {
 		return (
-			<Link
+			<Text
 				ref={ ref }
 				className={ clsx( styles[ 'action-link' ], className ) }
 				{ ...props }
-				tone="neutral"
-				variant="default"
+				variant="body-md"
+				render={ <Link tone="neutral" variant="default" /> }
```

Thanks for catching that @ciampo I've updated the component to destructure the render prop and pass it explicitly to the Link component so any custom render passed to ActionLink is preserved.

### Review thread 2

#### ciampo on 2026-04-15T12:11:56Z

URL: https://github.com/WordPress/gutenberg/pull/77332#discussion_r3086322528
Path: packages/ui/src/notice/style.module.css

```diff
@@ -64,10 +64,6 @@
 
 	.action-link {
 		flex-shrink: 0;
-		font-family: var(--wpds-typography-font-family-body);
-		font-size: var(--wpds-typography-font-size-md);
-		font-weight: var(--wpds-typography-font-weight-regular);
-		line-height: var(--wpds-typography-line-height-sm);
 		margin-block: auto;
```

Currently we're not certain that `margin-block` will always override the `margin:0` rule set in `Text`. To make sure, let's move the rule to the compositions layer:

<details>
<summary>Suggested diff</summary>

```diff
 @layer wp-ui-components {
 	.action-link {
 		flex-shrink: 0;
-		margin-block: auto;
 
 		/* Add more horizontal space when following another action link/button */
 		&:not(:first-child) {
 			margin-inline-start: var(--wpds-dimension-gap-xs);
 		}
 
 		/* Add more horizontal space when followed by another action link/button */
 		&:not(:last-child) {
 			margin-inline-end: var(--wpds-dimension-gap-xs);
 		}
 	}
 }

 @layer wp-ui-compositions {
+	// Override `Text` margin
+	.action-link {
+		margin-block: auto;
+	}
+
 	/* Add partial transparency to CloseIcon and outline/minimal ActionButton
 	 * for a better look over tinted backgrounds */
 	.close-icon,
```

</details>

#### Adi-ty on 2026-04-15T12:21:41Z

URL: https://github.com/WordPress/gutenberg/pull/77332#discussion_r3086376313
Path: packages/ui/src/notice/style.module.css

```diff
@@ -64,10 +64,6 @@
 
 	.action-link {
 		flex-shrink: 0;
-		font-family: var(--wpds-typography-font-family-body);
-		font-size: var(--wpds-typography-font-size-md);
-		font-weight: var(--wpds-typography-font-weight-regular);
-		line-height: var(--wpds-typography-line-height-sm);
 		margin-block: auto;
```

I've moved the `margin-block: auto` rule to the wp-ui-compositions layer so it overrides the default `margin: 0` from the Text component.

#### Adi-ty on 2026-04-15T12:41:35Z

URL: https://github.com/WordPress/gutenberg/pull/77332#discussion_r3086488852
Path: packages/ui/src/notice/style.module.css

```diff
@@ -64,10 +64,6 @@
 
 	.action-link {
 		flex-shrink: 0;
-		font-family: var(--wpds-typography-font-family-body);
-		font-size: var(--wpds-typography-font-size-md);
-		font-weight: var(--wpds-typography-font-weight-regular);
-		line-height: var(--wpds-typography-line-height-sm);
 		margin-block: auto;
```

<img width="1163" height="194" alt="Screenshot 2026-04-15 at 6 07 07 PM" src="https://github.com/user-attachments/assets/55e807a4-887d-4fc0-9f20-633d7c802eb4" />

## Changed files

- packages/ui/CHANGELOG.md (MODIFIED, +1/-0)
- packages/ui/src/notice/action-link.tsx (MODIFIED, +7/-4)
- packages/ui/src/notice/style.module.css (MODIFIED, +5/-5)
