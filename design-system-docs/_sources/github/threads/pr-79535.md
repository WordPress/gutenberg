---
number: 79535
type: pull_request
state: OPEN
url: https://github.com/WordPress/gutenberg/pull/79535
matched_labels: ["[Package] Components","[Package] UI"]
all_labels: ["[Type] Code Quality","[Package] Components","[Package] Editor","[Package] Block editor","[Package] UI"]
---

# PR #79535: Jest: Mock CSS module class names

- URL: https://github.com/WordPress/gutenberg/pull/79535
- Author: ciampo
- Created: 2026-06-25T11:54:06Z
- Updated: 2026-06-26T15:25:58Z
- Comments: 3 of 3
- Review threads: 6 of 6
- Changed files: 11 of 11
- Diff: ../pr-diffs/79535.diff

## Body

## What?
Follow up to #79490.

Updates the CSS Module class guidance in `packages/components/CONTRIBUTING.md` to recommend `clsx` object syntax for conditional module classes.

## Why?
The previous guidance avoided object syntax because Jest mocked CSS Module imports as empty objects, which could produce a literal `undefined` class. As suggested in https://github.com/WordPress/gutenberg/pull/79490#issuecomment-4798777972, that is better fixed in Jest instead of discouraging the normal `clsx` pattern.

## How?
- Changes the default Jest CSS/SCSS mock to return prefixed kebab-case class names for accessed properties, using `change-case` for the kebab-case conversion.
- Adds unit coverage for CSS Module property access and `clsx` object syntax.
- Updates the components contributing guide prose and example to use `clsx` object syntax.
- Adds package changelog entries for `@wordpress/components` and `@wordpress/jest-preset-default`.

## Testing Instructions
1. Run `npm run lint:md:docs -- packages/components/CONTRIBUTING.md`.
2. Run `npm run lint:js -- packages/jest-preset-default/scripts/style-mock.js packages/jest-preset-default/test/style-mock.js packages/ui/src/popover/test/index.test.tsx`.
3. Run `npm run lint:pkg-json -- packages/jest-preset-default/package.json`.
4. Run `npm run lint:lockfile`.
5. Run `npm run lint:deps`.
6. Run `npm run test:unit -- packages/block-editor/src/components/color-palette/test/control.js packages/editor/src/components/post-publish-panel/test/index.js packages/components/src/card/test/index.tsx packages/ui/src/popover/test/index.test.tsx packages/jest-preset-default/test/style-mock.js`.

### Testing Instructions for Keyboard
Not applicable; documentation and test infrastructure only.

## Screenshots or screencast
Not applicable.

## Use of AI Tools
Created with assistance from ChatGPT/Codex and reviewed before opening this PR.

## Issue comments

### github-actions on 2026-06-25T12:03:54Z

URL: https://github.com/WordPress/gutenberg/pull/79535#issuecomment-4799000994

**Size Change:** 0 B 

**Total Size:** 7.51 MB



<a href="https://github.com/preactjs/compressed-size-action"><sub>compressed-size-action</sub></a>

### github-actions on 2026-06-25T13:29:11Z

URL: https://github.com/WordPress/gutenberg/pull/79535#issuecomment-4799818455

The following accounts have interacted with this PR and/or linked issues. I will continue to update these lists as activity occurs. You can also manually ask me to refresh this list by adding the `props-bot` label.

If you're merging code through a pull request on GitHub, copy and paste the following into the bottom of the merge commit message.

```
Co-authored-by: ciampo <mciampini@git.wordpress.org>
Co-authored-by: jsnajdr <jsnajdr@git.wordpress.org>
```

**To understand the WordPress project's expectations around crediting contributors, please [review the Contributor Attribution page in the Core Handbook](https://make.wordpress.org/core/handbook/best-practices/contributor-attribution-props/).**

### github-actions on 2026-06-26T14:19:46Z

URL: https://github.com/WordPress/gutenberg/pull/79535#issuecomment-4810432490

<!-- flaky-tests-report-comment -->
**Flaky tests detected in d9cc1919d342bf1b40008582d23b5b5cf41726f3.**
Some tests passed with failed attempts. The failures may not be related to this commit but are still reported for visibility. See [the documentation](https://github.com/WordPress/gutenberg/blob/HEAD/docs/contributors/code/testing-overview.md#flaky-tests) for more information.

🔍  Workflow run URL: https://github.com/WordPress/gutenberg/actions/runs/28245833659
📝  Reported issues:
- #78689 in `/test/e2e/specs/editor/plugins/wp-editor-meta-box.spec.js`

## Review threads

### Review thread 1

#### ciampo on 2026-06-25T13:38:08Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3474808082
Path: packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap

Previously, imported CSS modules resolved to an empty object in tests, so class lookups like `styles.truncate` evaluated to `undefined` and were omitted from the `className` output.

This PR makes the mock return the requested class key instead (matching the runtime CSS modules behaviour). As a result, snapshots now include the previously missing CSS module classnames.

### Review thread 2 (resolved)

#### jsnajdr on 2026-06-25T13:43:09Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3474842887
Path: packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap

```diff
@@ -176,14 +176,14 @@ exports[`ColorPaletteControl matches the snapshot 1`] = `
                     data-wp-component="VStack"
                   >
                     <span
-                      class="components-truncate components-color-palette__custom-color-name emotion-14 emotion-5"
+                      class="truncate components-truncate components-color-palette__custom-color-name emotion-14 emotion-5"
```

It would be nice if the mock added some prefix or suffix, like `style-surface`. So that it's clear where the class comes from. Just like we know where `emotion-14` comes from.

#### ciampo on 2026-06-25T14:19:36Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3475107371
Path: packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap

```diff
@@ -176,14 +176,14 @@ exports[`ColorPaletteControl matches the snapshot 1`] = `
                     data-wp-component="VStack"
                   >
                     <span
-                      class="components-truncate components-color-palette__custom-color-name emotion-14 emotion-5"
+                      class="truncate components-truncate components-color-palette__custom-color-name emotion-14 emotion-5"
```

Good idea, will prefix `style-`

### Review thread 3 (resolved)

#### jsnajdr on 2026-06-25T13:54:52Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3474933724
Path: packages/jest-preset-default/scripts/test/style-mock.js

```diff
@@ -0,0 +1,31 @@
+const styles = require( '../style-mock' );
+
+describe( 'style mock', () => {
+	it( 'returns identity class names for CSS Module properties', () => {
+		expect( styles.root ).toBe( 'root' );
+		expect( styles.className ).toBe( 'className' );
+		expect( styles.singleLineClamp ).toBe( 'singleLineClamp' );
+		expect( styles[ 'already-kebab' ] ).toBe( 'already-kebab' );
```

We're not really converting to `kebab-case` right now (IMO we should) so this unit test is a bit pointless right now.

#### ciampo on 2026-06-25T14:19:24Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3475105989
Path: packages/jest-preset-default/scripts/test/style-mock.js

```diff
@@ -0,0 +1,31 @@
+const styles = require( '../style-mock' );
+
+describe( 'style mock', () => {
+	it( 'returns identity class names for CSS Module properties', () => {
+		expect( styles.root ).toBe( 'root' );
+		expect( styles.className ).toBe( 'className' );
+		expect( styles.singleLineClamp ).toBe( 'singleLineClamp' );
+		expect( styles[ 'already-kebab' ] ).toBe( 'already-kebab' );
```

I removed those tests

### Review thread 4 (resolved)

#### jsnajdr on 2026-06-25T13:56:50Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3474947561
Path: packages/jest-preset-default/scripts/test/style-mock.js

```diff
@@ -0,0 +1,31 @@
+const styles = require( '../style-mock' );
+
+describe( 'style mock', () => {
+	it( 'returns identity class names for CSS Module properties', () => {
+		expect( styles.root ).toBe( 'root' );
+		expect( styles.className ).toBe( 'className' );
+		expect( styles.singleLineClamp ).toBe( 'singleLineClamp' );
+		expect( styles[ 'already-kebab' ] ).toBe( 'already-kebab' );
+	} );
+
+	it( 'does not emit an undefined class when used as an object key', () => {
+		const classes = {
+			[ styles.conditionalClass ]: true,
+		};
+
+		expect(
+			Object.keys( classes ).filter(
+				( className ) => classes[ className ]
+			)
+		).toEqual( [ 'conditionalClass' ] );
+		expect( classes ).not.toHaveProperty( 'undefined' );
```

We could use `clsx` to implement this test, it's closer to real world usage.

#### ciampo on 2026-06-25T14:20:10Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3475111876
Path: packages/jest-preset-default/scripts/test/style-mock.js

```diff
@@ -0,0 +1,31 @@
+const styles = require( '../style-mock' );
+
+describe( 'style mock', () => {
+	it( 'returns identity class names for CSS Module properties', () => {
+		expect( styles.root ).toBe( 'root' );
+		expect( styles.className ).toBe( 'className' );
+		expect( styles.singleLineClamp ).toBe( 'singleLineClamp' );
+		expect( styles[ 'already-kebab' ] ).toBe( 'already-kebab' );
+	} );
+
+	it( 'does not emit an undefined class when used as an object key', () => {
+		const classes = {
+			[ styles.conditionalClass ]: true,
+		};
+
+		expect(
+			Object.keys( classes ).filter(
+				( className ) => classes[ className ]
+			)
+		).toEqual( [ 'conditionalClass' ] );
+		expect( classes ).not.toHaveProperty( 'undefined' );
```

Good point, i'll switch to `clsx`

### Review thread 5 (resolved)

#### jsnajdr on 2026-06-25T14:01:59Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3474981408
Path: packages/jest-preset-default/scripts/style-mock.js

```diff
@@ -1 +1,18 @@
-module.exports = {};
+const styles = new Proxy(
+	{},
+	{
+		get( target, property ) {
+			if ( property === '__esModule' ) {
+				return false;
+			}
+
+			if ( typeof property === 'symbol' ) {
+				return target[ property ];
```

Proxies usually use `Reflect.get( target, property )`, we already have a similar proxy in `wp-data` to implement private fields on `select` and `dispatch` objects.

The implementation could be like:
```js
if ( typeof prop === 'string' && prop !== '__esModule' ) {
  return cssClass( prop );
}
// otherwise fall back to the empty `target` object
return Reflect.get( target, prop );
```

#### ciampo on 2026-06-25T14:21:45Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3475123566
Path: packages/jest-preset-default/scripts/style-mock.js

```diff
@@ -1 +1,18 @@
-module.exports = {};
+const styles = new Proxy(
+	{},
+	{
+		get( target, property ) {
+			if ( property === '__esModule' ) {
+				return false;
+			}
+
+			if ( typeof property === 'symbol' ) {
+				return target[ property ];
```

Nice, I don't think I have actually written production code with proxies before. Learning every day :)

### Review thread 6

#### jsnajdr on 2026-06-26T11:15:35Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3481016986
Path: packages/jest-preset-default/scripts/style-mock.js

```diff
@@ -1 +1,16 @@
-module.exports = {};
+const cssClass = ( property ) => `style-${ property }`;
```

Still don't want to convert to `kebab-case` here? I think it would be nice 🙂

#### ciampo on 2026-06-26T13:51:34Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3481811999
Path: packages/jest-preset-default/scripts/style-mock.js

```diff
@@ -1 +1,16 @@
-module.exports = {};
+const cssClass = ( property ) => `style-${ property }`;
```

like so ? https://github.com/WordPress/gutenberg/pull/79535/changes/a2e032f0e163a2d1522e5f1dd9b3141dc3b36065

#### jsnajdr on 2026-06-26T14:35:24Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3482075867
Path: packages/jest-preset-default/scripts/style-mock.js

```diff
@@ -1 +1,16 @@
-module.exports = {};
+const cssClass = ( property ) => `style-${ property }`;
```

We could use the `change-case` library for this, one extra dependency for the `jest-preset-default` package doesn't hurt. Especially given we have plenty of other usages.

#### ciampo on 2026-06-26T15:02:14Z

URL: https://github.com/WordPress/gutenberg/pull/79535#discussion_r3482240369
Path: packages/jest-preset-default/scripts/style-mock.js

```diff
@@ -1 +1,16 @@
-module.exports = {};
+const cssClass = ( property ) => `style-${ property }`;
```

Sure, [done](https://github.com/WordPress/gutenberg/pull/79535/commits/d9cc1919d342bf1b40008582d23b5b5cf41726f3)!

## Changed files

- package-lock.json (MODIFIED, +3/-1)
- packages/block-editor/src/components/color-palette/test/__snapshots__/control.js.snap (MODIFIED, +2/-2)
- packages/components/CHANGELOG.md (MODIFIED, +1/-1)
- packages/components/CONTRIBUTING.md (MODIFIED, +5/-3)
- packages/components/src/card/test/__snapshots__/index.tsx.snap (MODIFIED, +5/-5)
- packages/editor/src/components/post-publish-panel/test/__snapshots__/index.js.snap (MODIFIED, +1/-0)
- packages/jest-preset-default/CHANGELOG.md (MODIFIED, +4/-0)
- packages/jest-preset-default/package.json (MODIFIED, +2/-1)
- packages/jest-preset-default/scripts/style-mock.js (MODIFIED, +18/-1)
- packages/jest-preset-default/test/style-mock.js (ADDED, +29/-0)
- packages/ui/src/popover/test/index.test.tsx (MODIFIED, +3/-1)
