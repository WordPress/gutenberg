---
number: 45627
type: pull_request
state: MERGED
merged_at: 2022-11-09T18:19:10Z
url: https://github.com/WordPress/gutenberg/pull/45627
matched_labels: ["[Package] Components"]
all_labels: ["[Type] Code Quality","[Package] Components"]
---

# PR #45627: Components: improve tests for `ToggleGroupControl`

- URL: https://github.com/WordPress/gutenberg/pull/45627
- Author: flootr
- Created: 2022-11-09T14:05:31Z
- Updated: 2022-11-09T23:56:24Z
- Comments: 1 of 1
- Review threads: 3 of 3
- Changed files: 1 of 1
- Diff: ../pr-diffs/45627.diff

## Body

## What?

Improve tests for the `ToggleGroupControl` component.

## Why?

Follow along RTL best practices to improve code quality.

## How?

- Using `@testing-library/user-event` instead of `fireEvent`
- Assert using `.toBeVisible` rather than `.toBeInTheDocument` in one case

## Testing Instructions

Verify unit tests still pass

```
npm run test:unit packages/components/src/toggle-group-control/test
```

## Issue comments

### codesandbox on 2022-11-09T14:05:34Z

URL: https://github.com/WordPress/gutenberg/pull/45627#issuecomment-1308815969

<img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.light.svg#gh-dark-mode-only" /><img alt="CodeSandbox logo" src="https://raw.githubusercontent.com/codesandbox/open-in-codesandbox/main/assets/csb.svg#gh-light-mode-only" />&nbsp; Open in CodeSandbox <a href="https://codesandbox.io/p/github/flootr/gutenberg/refactor/fire-user-event/toggle-group-control?workspace=%7B%22gitSidebarPanel%22:%22PR%22,%22sidebarPanel%22:%22GIT%22%7D">Web Editor</a> | <a href="https://codesandbox.io/p/vscode?owner=flootr&repo=gutenberg&branch=refactor/fire-user-event/toggle-group-control">VS Code</a> | <a href="https://codesandbox.io/p/vscode?owner=flootr&repo=gutenberg&branch=refactor/fire-user-event/toggle-group-control&insiders=true">VS Code Insiders</a>

<!-- open-in-codesandbox:complete -->

## Review threads

### Review thread 1 (resolved)

#### tyxla on 2022-11-09T14:10:18Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1017987976
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -86,31 +89,31 @@ describe( 'ToggleGroupControl', () => {
 			</ToggleGroupControl>
 		);
 
-		const firstRadio = screen.getByRole( 'radio', { name: 'R' } );
-
-		fireEvent.click( firstRadio );
+		await user.click( screen.getByRole( 'radio', { name: 'R' } ) );
 
 		expect( mockOnChange ).toHaveBeenCalledWith( 'rigas' );
 	} );
-	it( 'should render tooltip where `showTooltip` === `true`', () => {
+	it( 'should render tooltip where `showTooltip` === `true`', async () => {
+		const user = userEvent.setup( {
+			advanceTimers: jest.advanceTimersByTime,
+		} );
 		render(
 			<ToggleGroupControl label="Test Toggle Group Control">
 				{ optionsWithTooltip }
 			</ToggleGroupControl>
 		);
 
-		const firstRadio = screen.getByLabelText(
-			'Click for Delicious Gnocchi'
-		);
-
-		fireEvent.focus( firstRadio );
+		await user.tab();
```

It's not immediately clear what this does. Should we keep the `firstRadio` and then just do a `firstRadio.focus()`?

#### flootr on 2022-11-09T15:18:25Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018074936
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -86,31 +89,31 @@ describe( 'ToggleGroupControl', () => {
 			</ToggleGroupControl>
 		);
 
-		const firstRadio = screen.getByRole( 'radio', { name: 'R' } );
-
-		fireEvent.click( firstRadio );
+		await user.click( screen.getByRole( 'radio', { name: 'R' } ) );
 
 		expect( mockOnChange ).toHaveBeenCalledWith( 'rigas' );
 	} );
-	it( 'should render tooltip where `showTooltip` === `true`', () => {
+	it( 'should render tooltip where `showTooltip` === `true`', async () => {
+		const user = userEvent.setup( {
+			advanceTimers: jest.advanceTimersByTime,
+		} );
 		render(
 			<ToggleGroupControl label="Test Toggle Group Control">
 				{ optionsWithTooltip }
 			</ToggleGroupControl>
 		);
 
-		const firstRadio = screen.getByLabelText(
-			'Click for Delicious Gnocchi'
-		);
-
-		fireEvent.focus( firstRadio );
+		await user.tab();
```

Addressed via https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018074252

### Review thread 2

#### tyxla on 2022-11-09T14:10:58Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1017988743
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -121,7 +124,7 @@ describe( 'ToggleGroupControl', () => {
 			'Click for Sumptuous Caponata'
 		);
 
-		fireEvent.focus( secondRadio );
+		await user.click( secondRadio );
```

Did we mean to change the focus to be click instead?

#### jsnajdr on 2022-11-09T14:36:20Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018020123
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -121,7 +124,7 @@ describe( 'ToggleGroupControl', () => {
 			'Click for Sumptuous Caponata'
 		);
 
-		fireEvent.focus( secondRadio );
+		await user.click( secondRadio );
```

In both tests, we're testing whether a tooltip appears or not over a radio form item when the item is focused. If `userEvent` has some method for simulating "move the mouse over the input", that would be great to use here. The tooltip is then supposed to appear.

`user.tab()` works because it focuses the right element, but it's quite an indirect and non-obvious way to do that.

I think `user.click` is wrong. The tooltip indeed doesn't appear, so the test passes, but it doesn't appear for the wrong reasons.

#### tyxla on 2022-11-09T15:05:27Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018058540
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -121,7 +124,7 @@ describe( 'ToggleGroupControl', () => {
 			'Click for Sumptuous Caponata'
 		);
 
-		fireEvent.focus( secondRadio );
+		await user.click( secondRadio );
```

I agree with Jarda on this. Also IMHO the safest bet is to just keep triggering the focus event, but just not with `fireEvent`:

```suggestion
		secondRadio.focus();
```

#### flootr on 2022-11-09T15:17:54Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018074252
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -121,7 +124,7 @@ describe( 'ToggleGroupControl', () => {
 			'Click for Sumptuous Caponata'
 		);
 
-		fireEvent.focus( secondRadio );
+		await user.click( secondRadio );
```

Thanks for the feedback! I agree on keeping it simple in this case. Does https://github.com/WordPress/gutenberg/pull/45627/commits/848e112a1007b83794893e676c78a48a938619b9 work?

### Review thread 3

#### tyxla on 2022-11-09T16:52:52Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018188674
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -103,11 +104,11 @@ describe( 'ToggleGroupControl', () => {
 			'Click for Delicious Gnocchi'
 		);
 
-		fireEvent.focus( firstRadio );
+		firstRadio.focus();
```

As discussed face-to-face, we can try using `userEvent.hover()` and if that's enough, it resembles the user behavior much better, but it also is wrapped in an `act()` so it'll be compatible to React 18.

#### flootr on 2022-11-09T17:16:51Z

URL: https://github.com/WordPress/gutenberg/pull/45627#discussion_r1018214232
Path: packages/components/src/toggle-group-control/test/index.tsx

```diff
@@ -103,11 +104,11 @@ describe( 'ToggleGroupControl', () => {
 			'Click for Delicious Gnocchi'
 		);
 
-		fireEvent.focus( firstRadio );
+		firstRadio.focus();
```

Implemented via https://github.com/WordPress/gutenberg/pull/45627/commits/443a1279a806eae3eb34945857d39969e2176dc6

## Changed files

- packages/components/src/toggle-group-control/test/index.tsx (MODIFIED, +28/-16)
