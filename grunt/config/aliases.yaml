# Grunt aliases
---
# Build everything
build:
  - 'build:js'
# Build JavaScript from assets to production
'build:js':
  - 'uglify'
# Check health of the project
check:
  - 'eslint'
# Default task
default:
  - check
  - build
