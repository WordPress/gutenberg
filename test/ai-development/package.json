{
	"name": "@wordpress/agent-skill-evals",
	"version": "0.0.0",
	"description": "Promptfoo evals that verify agent skills in this repository are discovered, followed, and produce output that meets our rules.",
	"private": true,
	"author": "The WordPress Contributors",
	"license": "GPL-2.0-or-later",
	"keywords": [
		"wordpress",
		"gutenberg",
		"agents",
		"evals"
	],
	"homepage": "https://github.com/WordPress/gutenberg/tree/HEAD/test/ai-development",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/WordPress/gutenberg.git",
		"directory": "test/ai-development"
	},
	"bugs": {
		"url": "https://github.com/WordPress/gutenberg/issues"
	},
	"engines": {
		"node": ">=22.22.0"
	},
	"type": "module",
	"devDependencies": {
		"@anthropic-ai/claude-agent-sdk": "^0.3.201",
		"promptfoo": "^0.121.19"
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"validate": "for c in specs/*/*.eval.js; do promptfoo validate config --config \"$c\" || exit 1; done",
		"test:sandbox": "node --test lib/test/sandbox.mjs",
		"test:grader": "for t in specs/*/test-grader.mjs; do [ -e \"$t\" ] || continue; node --test \"$t\" || exit 1; done",
		"eval": "sh -c 'case \"$*\" in *--config*) exec promptfoo eval \"$@\";; *) for c in specs/*/*.eval.js; do promptfoo eval --config \"$c\" \"$@\" || exit 1; done;; esac' sh",
		"view": "promptfoo view"
	}
}
