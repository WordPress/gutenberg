This PR updates `packages/editor/src/components/collab-sidebar/format.js` (48 lines changed) and `test/e2e/specs/editor/various/block-notes.spec.js` (205 lines changed).

The changes in format.js modify the applyFormat logic and add new imports. The e2e spec file adds several new test blocks and refactors existing helpers into utility functions.

Testing: run the e2e suite and verify everything passes.
