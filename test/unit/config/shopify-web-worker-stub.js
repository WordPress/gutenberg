/**
 * Stub for @shopify/web-worker transformed imports.
 *
 * When the @shopify/web-worker babel plugin transforms createWorkerFactory calls,
 * it creates webpack loader paths that Jest cannot resolve. This stub is used
 * as a replacement for those paths.
 */
module.exports = {};
