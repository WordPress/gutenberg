/**
 * Internal dependencies
 */
import { test, expect } from './fixtures';

test.describe( 'data-wp-each', () => {
	test( 'should hydrate correctly', async ( { page } ) => {} );
	test( 'should not remount items when the order changes', async ( {
		page,
	} ) => {} );
	test( 'should use `item` as the defaul item name in the context', async ( {
		page,
	} ) => {} );
	test( 'should use the specified item name in the context', async ( {
		page,
	} ) => {} );
	test( 'should use `item` as the key if `wp-each-key` is not specified', async ( {
		page,
	} ) => {} );
	test( 'should use the specified `wp-each-key` to compute item ids', async ( {
		page,
	} ) => {} );
	test( 'should work on navigation', async ( { page } ) => {} );
} );
