# @wordpress/monorepo-tools

Tools for maintaining the Gutenberg monorepo itself. This is a private workspace package and is not published to npm.

## Tools

-   [`patch`](./patch): creates and applies the dependency patches in [`patches/`](../../patches). `patch apply` runs from the repository root's `postinstall`.
