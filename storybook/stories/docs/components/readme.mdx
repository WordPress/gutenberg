import { Meta, Markdown } from '@storybook/addon-docs/blocks';
import Readme from '@wordpress/components/README.md?raw';

<Meta id="components-introduction" title="Classic Components/Introduction" />

> **Classic Components** documents the `@wordpress/components` package: the
> original WordPress UI library, still widely used and still the place to look
> for what [`@wordpress/ui`](?path=/docs/design-system-introduction--docs) does
> not cover yet. Each component page shows its recommendation status. For new
> UI, start with the [Design System](?path=/docs/design-system-introduction--docs).

<Markdown>{Readme}</Markdown>
