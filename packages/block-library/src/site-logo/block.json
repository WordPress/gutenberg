{
	"apiVersion": 2,
	"name": "core/site-logo",
	"title": "Site Logo",
	"category": "layout",
	"description": "Useful for displaying a symbol to represent the site. Once a site logo is set, it can be reused in different places. This is not the same as a site icon, which is the small image used in the dashboard, browser tabs, etc, to help identify a site.",
	"textdomain": "default",
	"attributes": {
		"align": {
			"type": "string"
		},
		"width": {
			"type": "number"
		},
		"isLink": {
			"type": "boolean",
			"default": true
		},
		"linkTarget": {
			"type": "string",
			"default": "_self"
		}
	},
	"supports": {
		"html": false,
		"align": true,
		"alignWide": false
	},
	"styles": [
		{
			"name": "default",
			"label": "Default",
			"isDefault": true
		},
		{ "name": "rounded", "label": "Rounded" }
	],
	"editorStyle": "wp-block-site-logo-editor",
	"style": "wp-block-site-logo"
}
