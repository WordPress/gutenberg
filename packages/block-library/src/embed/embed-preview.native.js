/**
 * External dependencies
 */
import { View } from 'react-native';

const EmbedPreview = () => {
	return (
		<View style={ { height: 50, widht: '100%', backgroundColor: 'red' } } />
	);
};

export default EmbedPreview;
