{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 2,
	"name": "core/pattern",
	"title": "Pattern",
	"category": "theme",
	"description": "Show a block pattern.",
	"textdomain": "default",
	"attributes": {
		"slug": {
			"type": "string"
		},
		"syncStatus": {
			"type": [ "string", "boolean" ],
			"enum": [ "full", "partial" ]
		}
	}
}
