const { login } = require( './login' );
const { createNewPost } = require( './create-new-post' );
const { closeWelcomeGuide } = require( './close-welcome-guide' );

module.exports = {
	login,
	createNewPost,
	closeWelcomeGuide,
};
