import { createSlotFill } from '@wordpress/components';

// Items that go with Copy styles and Paste styles in the block options menu.
const BlockStylesMenuItemsSlotFill = createSlotFill(
	Symbol( 'BlockStylesMenuItemsSlotFill' )
);

export default BlockStylesMenuItemsSlotFill;
