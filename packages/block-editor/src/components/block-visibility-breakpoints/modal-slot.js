/**
 * WordPress dependencies
 */
import { createSlotFill } from '@wordpress/components';

const { Fill, Slot } = createSlotFill( 'BlockVisibilityBreakpointsModal' );

export const BlockVisibilityBreakpointsModalFill = Fill;
export const BlockVisibilityBreakpointsModalSlot = Slot;

