/**
 * WordPress dependencies
 */
// import { DataRegistry } from '@wordpress/data';
//
// const x = {} as DataRegistry;
// x.select( 'core-data' ).getCurrentUser();
// // x.select( 'core-data' ).getEntityRecord('');

export {};

export interface AnyFunction {
	( ...args: any[] ): any;
}
