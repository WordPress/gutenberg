/**
 * Type declaration for the generated worker code module.
 *
 * The real implementation is written to build-module/worker-code.mjs by the
 * build process after the worker bundle is compiled.
 */
export declare const workerCode: string;
