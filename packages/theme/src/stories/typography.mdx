import { Meta } from '@storybook/addon-docs/blocks';
import { TypographyTokenPreview } from './typography-preview';
import '@wordpress/theme/design-tokens.css';

<Meta title="Design System/Tokens/Typography" />

# Typography tokens

`@wordpress/theme` publishes typography tokens for font families, sizes, line heights, and weights as CSS custom properties.

<TypographyTokenPreview />

See the [Design Tokens Reference](?path=/docs/design-system-tokens-introduction--docs) for token descriptions and guidance on choosing tokens.
