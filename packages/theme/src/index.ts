import type {
	BorderRadiusSize as InternalBorderRadiusSize,
	BorderWidthSize as InternalBorderWidthSize,
	ContentForegroundColor as InternalContentForegroundColor,
	DurationSize as InternalDurationSize,
	Easing as InternalEasing,
	ElementSize as InternalElementSize,
	FontFamily as InternalFontFamily,
	FontSize as InternalFontSize,
	FontWeight as InternalFontWeight,
	ForegroundColor as InternalForegroundColor,
	GapSize as InternalGapSize,
	InteractiveBackgroundColor as InternalInteractiveBackgroundColor,
	InteractiveForegroundColor as InternalInteractiveForegroundColor,
	InteractiveStrokeColor as InternalInteractiveStrokeColor,
	LineHeight as InternalLineHeight,
	PaddingSize as InternalPaddingSize,
	SurfaceBackgroundColor as InternalSurfaceBackgroundColor,
	SurfaceStrokeColor as InternalSurfaceStrokeColor,
	SurfaceWidthSize as InternalSurfaceWidthSize,
	Target as InternalTarget,
} from './prebuilt/ts/token-types';
import type { CornerRadiusPreset as InternalCornerRadiusPreset } from './types';

export { privateApis } from './private-apis';
export { ThemeProvider } from './theme-provider';

/**
 * @deprecated This type is not part of the intended public API and is scheduled
 * for deletion as `@wordpress/theme` approaches stabilization.
 */
export type CornerRadiusPreset = InternalCornerRadiusPreset;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type PaddingSize = InternalPaddingSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type GapSize = InternalGapSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type ElementSize = InternalElementSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type SurfaceWidthSize = InternalSurfaceWidthSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type DurationSize = InternalDurationSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type Easing = InternalEasing;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type BorderRadiusSize = InternalBorderRadiusSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type BorderWidthSize = InternalBorderWidthSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type Target = InternalTarget;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type SurfaceBackgroundColor = InternalSurfaceBackgroundColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type InteractiveBackgroundColor = InternalInteractiveBackgroundColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type ContentForegroundColor = InternalContentForegroundColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type InteractiveForegroundColor = InternalInteractiveForegroundColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type SurfaceStrokeColor = InternalSurfaceStrokeColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type InteractiveStrokeColor = InternalInteractiveStrokeColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type ForegroundColor = InternalForegroundColor;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type FontFamily = InternalFontFamily;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type FontSize = InternalFontSize;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type FontWeight = InternalFontWeight;

/**
 * @deprecated This generated token type is not part of the intended public API
 * and is scheduled for deletion as `@wordpress/theme` approaches stabilization.
 */
export type LineHeight = InternalLineHeight;
