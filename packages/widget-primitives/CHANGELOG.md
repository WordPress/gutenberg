<!-- Learn how to maintain this file at https://github.com/WordPress/gutenberg/tree/HEAD/packages#maintaining-changelogs. -->

## Unreleased

### New Features

-   Initial experimental release: the widget contract types (`WidgetType`,
    `WidgetName`, `WidgetRenderProps`, `ResolveWidgetModule`), the
    `useWidgetTypes` discovery hook, and the `<WidgetRender>` host entry
    point.
