import { requireNativeComponent } from 'react-native';

export const PatternPickerViewManager = requireNativeComponent(
  'PatternPickerViewManager'
);

export { PatternPicker } from './pattern-picker-view';
