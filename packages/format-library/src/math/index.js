/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';

const name = 'core/math';
const title = __( 'Math' );

export const math = {
	name,
	title,
	tagName: 'math',
	className: null,
	contentEditable: false,
};
