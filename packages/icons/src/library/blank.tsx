/**
 * WordPress dependencies
 */
import { SVG } from '@wordpress/primitives';

const blank = (
	<SVG xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
		{ /* Empty SVG. Used to maintain the 24x24 viewBox for consistent spacing */ }
	</SVG>
);

export default blank;
