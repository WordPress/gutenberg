/**
 * Stub of createBlobURL for React Native.
 *
 * @param {string} url should be a local file URL
 *
 * @return {string} The same string has.
 */
export function createBlobURL( url ) {
	return url;
}
