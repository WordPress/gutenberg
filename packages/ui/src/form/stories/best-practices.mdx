import { Meta, Stories } from '@storybook/addon-docs/blocks';
import * as BestPracticesStories from './best-practices.story';

<Meta of={ BestPracticesStories } />

# Best practices

Prefer a `*Control` for a single labeled field. Use this page when more than one control shares one visible label. See the [Form primitives overview](?path=/docs/design-system-components-form-primitives--docs).

<Stories title="Examples" />
