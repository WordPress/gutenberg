/**
 * Shared positioning defaults for item-list popups, e.g. Select, Combobox,
 * Autocomplete, or Menu.
 */
export const ITEM_POPUP_POSITIONER_PROPS = {
	align: 'start',
	sideOffset: 8,
	collisionPadding: 12,
} as const;
