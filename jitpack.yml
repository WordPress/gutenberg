install:
   - echo "Changing into the android folder of the Bridge module"
   - git submodule update --init --recursive
   - pushd react-native-gutenberg-bridge/android
   - ./gradlew clean -Pgroup=cogithub.wordpress-mobile.gutenberg-mobile -Pversion=$VERSION install
   - popd && pushd react-native-aztec/android
   - ./gradlew clean -Pgroup=cogithub.wordpress-mobile.gutenberg-mobile -Pversion=$VERSION install

