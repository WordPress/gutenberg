{
	"name": "sync-foundations-prototype",
	"version": "0.0.0",
	"private": true,
	"description": "Standalone prototype of the sync foundations: block identity, intent vocabulary, reducer, rebase engine, and deterministic simulation harness. Not part of the Gutenberg build.",
	"type": "module",
	"scripts": {
		"test": "node --test test/",
		"sweep": "node tools/sweep.js"
	}
}
