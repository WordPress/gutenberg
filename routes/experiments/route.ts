/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';

/**
 * Route configuration for the experiments page.
 */
export const route = {
	title: () => __( 'Experimental Settings' ),
};
