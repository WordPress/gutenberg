# import-normalize

1. Remove the doc comments preceding the import statements.
2. Make the minimal changes required to make [this ordering rule lint](../../.eslintrc.strict.js) pass.
3. Remove any extra blank lines between import statements.
